/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"EscrowContract","structs":{},"methods":[{"name":"create_escrow","args":[{"type":"address","name":"freelancer"},{"type":"uint64","name":"amount"},{"type":"pay","name":"fund"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Initialise escrow. Caller becomes the client; the grouped Payment must\ndeliver `amount` microalgos to the application account.","events":[],"recommendations":{}},{"name":"submit_work","args":[{"type":"string","name":"ipfs_hash"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Freelancer records deliverable CID; status -> SUBMITTED.","events":[],"recommendations":{}},{"name":"approve","args":[],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Client approves submitted deliverable; status -> APPROVED.","events":[],"recommendations":{}},{"name":"release_payment","args":[],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Freelancer pulls escrow after approval; status -> RELEASED.","events":[],"recommendations":{}},{"name":"refund","args":[],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Client refund window: only CREATED or SUBMITTED, never APPROVED+.","events":[],"recommendations":{}}],"arcs":[22,28],"desc":"ARC-4 escrow locking ALGO behind client / freelancer approvals.","networks":{},"state":{"schema":{"global":{"ints":2,"bytes":3},"local":{"ints":0,"bytes":0}},"keys":{"global":{"amount":{"keyType":"AVMString","valueType":"AVMUint64","key":"YW1vdW50"},"client":{"keyType":"AVMString","valueType":"address","key":"Y2xpZW50"},"freelancer":{"keyType":"AVMString","valueType":"address","key":"ZnJlZWxhbmNlcg=="},"status":{"keyType":"AVMString","valueType":"AVMUint64","key":"c3RhdHVz"},"work_hash":{"keyType":"AVMString","valueType":"AVMBytes","key":"d29ya19oYXNo"}},"local":{},"box":{}},"maps":{"global":{},"local":{},"box":{}}},"bareActions":{"create":["NoOp"],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[175],"errorMessage":"amount must be > 0"},{"pc":[321],"errorMessage":"approval required"},{"pc":[325,380],"errorMessage":"check self.amount exists"},{"pc":[289,354,387],"errorMessage":"check self.client exists"},{"pc":[258,309],"errorMessage":"check self.freelancer exists"},{"pc":[169,264,295,318,360,369],"errorMessage":"check self.status exists"},{"pc":[213],"errorMessage":"close_remainder_to must be zero"},{"pc":[171],"errorMessage":"escrow already initialised"},{"pc":[181],"errorMessage":"freelancer must differ from client"},{"pc":[242],"errorMessage":"invalid array length header"},{"pc":[252],"errorMessage":"invalid number of bytes for arc4.dynamic_array<arc4.uint8>"},{"pc":[146],"errorMessage":"invalid number of bytes for arc4.static_array<arc4.uint8, 32>"},{"pc":[155],"errorMessage":"invalid number of bytes for arc4.uint64"},{"pc":[267],"errorMessage":"invalid status for submit"},{"pc":[269],"errorMessage":"ipfs_hash required"},{"pc":[274],"errorMessage":"ipfs_hash too long"},{"pc":[205],"errorMessage":"locked amount mismatch"},{"pc":[327,382],"errorMessage":"no funds locked"},{"pc":[291,356],"errorMessage":"only client"},{"pc":[260,314],"errorMessage":"only freelancer"},{"pc":[197],"errorMessage":"payment receiver must be app"},{"pc":[189],"errorMessage":"payment sender must be client"},{"pc":[376],"errorMessage":"refund not allowed"},{"pc":[220],"errorMessage":"rekey_to must be zero"},{"pc":[165],"errorMessage":"transaction type is pay"},{"pc":[298],"errorMessage":"work not submitted"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"events":[]} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


/**
 * The argument types for the EscrowContract contract
 */
export type EscrowContractArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'create_escrow(address,uint64,pay)void': {
      freelancer: string
      amount: bigint | number
      fund: AppMethodCallTransactionArgument
    }
    'submit_work(string)void': {
      ipfsHash: string
    }
    'approve()void': Record<string, never>
    'release_payment()void': Record<string, never>
    'refund()void': Record<string, never>
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'create_escrow(address,uint64,pay)void': [freelancer: string, amount: bigint | number, fund: AppMethodCallTransactionArgument]
    'submit_work(string)void': [ipfsHash: string]
    'approve()void': []
    'release_payment()void': []
    'refund()void': []
  }
}

/**
 * The return type for each method
 */
export type EscrowContractReturns = {
  'create_escrow(address,uint64,pay)void': void
  'submit_work(string)void': void
  'approve()void': void
  'release_payment()void': void
  'refund()void': void
}

/**
 * Defines the types of available calls and state of the EscrowContract smart contract.
 */
export type EscrowContractTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'create_escrow(address,uint64,pay)void' | 'create_escrow', {
      argsObj: EscrowContractArgs['obj']['create_escrow(address,uint64,pay)void']
      argsTuple: EscrowContractArgs['tuple']['create_escrow(address,uint64,pay)void']
      returns: EscrowContractReturns['create_escrow(address,uint64,pay)void']
    }>
    & Record<'submit_work(string)void' | 'submit_work', {
      argsObj: EscrowContractArgs['obj']['submit_work(string)void']
      argsTuple: EscrowContractArgs['tuple']['submit_work(string)void']
      returns: EscrowContractReturns['submit_work(string)void']
    }>
    & Record<'approve()void' | 'approve', {
      argsObj: EscrowContractArgs['obj']['approve()void']
      argsTuple: EscrowContractArgs['tuple']['approve()void']
      returns: EscrowContractReturns['approve()void']
    }>
    & Record<'release_payment()void' | 'release_payment', {
      argsObj: EscrowContractArgs['obj']['release_payment()void']
      argsTuple: EscrowContractArgs['tuple']['release_payment()void']
      returns: EscrowContractReturns['release_payment()void']
    }>
    & Record<'refund()void' | 'refund', {
      argsObj: EscrowContractArgs['obj']['refund()void']
      argsTuple: EscrowContractArgs['tuple']['refund()void']
      returns: EscrowContractReturns['refund()void']
    }>
  /**
   * Defines the shape of the state of the application.
   */
  state: {
    global: {
      keys: {
        amount: bigint
        client: string
        freelancer: string
        status: bigint
        workHash: BinaryState
      }
      maps: {}
    }
  }
}

/**
 * Defines the possible abi call signatures.
 */
export type EscrowContractSignatures = keyof EscrowContractTypes['methods']
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the EscrowContract smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends EscrowContractSignatures> = EscrowContractTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the EscrowContract smart contract to the method's return type
 */
export type MethodReturn<TSignature extends EscrowContractSignatures> = EscrowContractTypes['methods'][TSignature]['returns']

/**
 * Defines the shape of the keyed global state of the application.
 */
export type GlobalKeysState = EscrowContractTypes['state']['global']['keys']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the EscrowContract smart contract
 */
export abstract class EscrowContractParamsFactory {
  /**
   * Constructs a no op call for the create_escrow(address,uint64,pay)void ABI method
   *
  * Initialise escrow. Caller becomes the client; the grouped Payment must
  deliver `amount` microalgos to the application account.

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static createEscrow(params: CallParams<EscrowContractArgs['obj']['create_escrow(address,uint64,pay)void'] | EscrowContractArgs['tuple']['create_escrow(address,uint64,pay)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'create_escrow(address,uint64,pay)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.freelancer, params.args.amount, params.args.fund],
    }
  }
  /**
   * Constructs a no op call for the submit_work(string)void ABI method
   *
   * Freelancer records deliverable CID; status -> SUBMITTED.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static submitWork(params: CallParams<EscrowContractArgs['obj']['submit_work(string)void'] | EscrowContractArgs['tuple']['submit_work(string)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'submit_work(string)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.ipfsHash],
    }
  }
  /**
   * Constructs a no op call for the approve()void ABI method
   *
   * Client approves submitted deliverable; status -> APPROVED.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static approve(params: CallParams<EscrowContractArgs['obj']['approve()void'] | EscrowContractArgs['tuple']['approve()void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'approve()void' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the release_payment()void ABI method
   *
   * Freelancer pulls escrow after approval; status -> RELEASED.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static releasePayment(params: CallParams<EscrowContractArgs['obj']['release_payment()void'] | EscrowContractArgs['tuple']['release_payment()void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'release_payment()void' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the refund()void ABI method
   *
   * Client refund window: only CREATED or SUBMITTED, never APPROVED+.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static refund(params: CallParams<EscrowContractArgs['obj']['refund()void'] | EscrowContractArgs['tuple']['refund()void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'refund()void' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
}

/**
 * A client to make calls to the EscrowContract smart contract
 */
export class EscrowContractClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `EscrowContractClient`
   *
   * @param appClient An `AppClient` instance which has been created with the EscrowContract app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `EscrowContractClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Returns a new `EscrowContractClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<EscrowContractClient> {
    return new EscrowContractClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `EscrowContractClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<EscrowContractClient> {
    return new EscrowContractClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Makes a clear_state call to an existing instance of the EscrowContract smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the EscrowContract smart contract using the `create_escrow(address,uint64,pay)void` ABI method.
     *
    * Initialise escrow. Caller becomes the client; the grouped Payment must
    deliver `amount` microalgos to the application account.

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    createEscrow: (params: CallParams<EscrowContractArgs['obj']['create_escrow(address,uint64,pay)void'] | EscrowContractArgs['tuple']['create_escrow(address,uint64,pay)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(EscrowContractParamsFactory.createEscrow(params))
    },

    /**
     * Makes a call to the EscrowContract smart contract using the `submit_work(string)void` ABI method.
     *
     * Freelancer records deliverable CID; status -> SUBMITTED.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    submitWork: (params: CallParams<EscrowContractArgs['obj']['submit_work(string)void'] | EscrowContractArgs['tuple']['submit_work(string)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(EscrowContractParamsFactory.submitWork(params))
    },

    /**
     * Makes a call to the EscrowContract smart contract using the `approve()void` ABI method.
     *
     * Client approves submitted deliverable; status -> APPROVED.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    approve: (params: CallParams<EscrowContractArgs['obj']['approve()void'] | EscrowContractArgs['tuple']['approve()void']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(EscrowContractParamsFactory.approve(params))
    },

    /**
     * Makes a call to the EscrowContract smart contract using the `release_payment()void` ABI method.
     *
     * Freelancer pulls escrow after approval; status -> RELEASED.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    releasePayment: (params: CallParams<EscrowContractArgs['obj']['release_payment()void'] | EscrowContractArgs['tuple']['release_payment()void']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(EscrowContractParamsFactory.releasePayment(params))
    },

    /**
     * Makes a call to the EscrowContract smart contract using the `refund()void` ABI method.
     *
     * Client refund window: only CREATED or SUBMITTED, never APPROVED+.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    refund: (params: CallParams<EscrowContractArgs['obj']['refund()void'] | EscrowContractArgs['tuple']['refund()void']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(EscrowContractParamsFactory.refund(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Makes a clear_state call to an existing instance of the EscrowContract smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the EscrowContract smart contract using the `create_escrow(address,uint64,pay)void` ABI method.
     *
    * Initialise escrow. Caller becomes the client; the grouped Payment must
    deliver `amount` microalgos to the application account.

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    createEscrow: (params: CallParams<EscrowContractArgs['obj']['create_escrow(address,uint64,pay)void'] | EscrowContractArgs['tuple']['create_escrow(address,uint64,pay)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(EscrowContractParamsFactory.createEscrow(params))
    },

    /**
     * Makes a call to the EscrowContract smart contract using the `submit_work(string)void` ABI method.
     *
     * Freelancer records deliverable CID; status -> SUBMITTED.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    submitWork: (params: CallParams<EscrowContractArgs['obj']['submit_work(string)void'] | EscrowContractArgs['tuple']['submit_work(string)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(EscrowContractParamsFactory.submitWork(params))
    },

    /**
     * Makes a call to the EscrowContract smart contract using the `approve()void` ABI method.
     *
     * Client approves submitted deliverable; status -> APPROVED.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    approve: (params: CallParams<EscrowContractArgs['obj']['approve()void'] | EscrowContractArgs['tuple']['approve()void']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(EscrowContractParamsFactory.approve(params))
    },

    /**
     * Makes a call to the EscrowContract smart contract using the `release_payment()void` ABI method.
     *
     * Freelancer pulls escrow after approval; status -> RELEASED.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    releasePayment: (params: CallParams<EscrowContractArgs['obj']['release_payment()void'] | EscrowContractArgs['tuple']['release_payment()void']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(EscrowContractParamsFactory.releasePayment(params))
    },

    /**
     * Makes a call to the EscrowContract smart contract using the `refund()void` ABI method.
     *
     * Client refund window: only CREATED or SUBMITTED, never APPROVED+.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    refund: (params: CallParams<EscrowContractArgs['obj']['refund()void'] | EscrowContractArgs['tuple']['refund()void']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(EscrowContractParamsFactory.refund(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Makes a clear_state call to an existing instance of the EscrowContract smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the EscrowContract smart contract using the `create_escrow(address,uint64,pay)void` ABI method.
     *
    * Initialise escrow. Caller becomes the client; the grouped Payment must
    deliver `amount` microalgos to the application account.

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    createEscrow: async (params: CallParams<EscrowContractArgs['obj']['create_escrow(address,uint64,pay)void'] | EscrowContractArgs['tuple']['create_escrow(address,uint64,pay)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(EscrowContractParamsFactory.createEscrow(params))
      return {...result, return: result.return as unknown as (undefined | EscrowContractReturns['create_escrow(address,uint64,pay)void'])}
    },

    /**
     * Makes a call to the EscrowContract smart contract using the `submit_work(string)void` ABI method.
     *
     * Freelancer records deliverable CID; status -> SUBMITTED.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    submitWork: async (params: CallParams<EscrowContractArgs['obj']['submit_work(string)void'] | EscrowContractArgs['tuple']['submit_work(string)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(EscrowContractParamsFactory.submitWork(params))
      return {...result, return: result.return as unknown as (undefined | EscrowContractReturns['submit_work(string)void'])}
    },

    /**
     * Makes a call to the EscrowContract smart contract using the `approve()void` ABI method.
     *
     * Client approves submitted deliverable; status -> APPROVED.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    approve: async (params: CallParams<EscrowContractArgs['obj']['approve()void'] | EscrowContractArgs['tuple']['approve()void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(EscrowContractParamsFactory.approve(params))
      return {...result, return: result.return as unknown as (undefined | EscrowContractReturns['approve()void'])}
    },

    /**
     * Makes a call to the EscrowContract smart contract using the `release_payment()void` ABI method.
     *
     * Freelancer pulls escrow after approval; status -> RELEASED.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    releasePayment: async (params: CallParams<EscrowContractArgs['obj']['release_payment()void'] | EscrowContractArgs['tuple']['release_payment()void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(EscrowContractParamsFactory.releasePayment(params))
      return {...result, return: result.return as unknown as (undefined | EscrowContractReturns['release_payment()void'])}
    },

    /**
     * Makes a call to the EscrowContract smart contract using the `refund()void` ABI method.
     *
     * Client refund window: only CREATED or SUBMITTED, never APPROVED+.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    refund: async (params: CallParams<EscrowContractArgs['obj']['refund()void'] | EscrowContractArgs['tuple']['refund()void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(EscrowContractParamsFactory.refund(params))
      return {...result, return: result.return as unknown as (undefined | EscrowContractReturns['refund()void'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new EscrowContractClient(this.appClient.clone(params))
  }

  /**
   * Methods to access state for the current EscrowContract app
   */
  state = {
    /**
     * Methods to access global state for the current EscrowContract app
     */
    global: {
      /**
       * Get all current keyed values from global state
       */
      getAll: async (): Promise<Partial<Expand<GlobalKeysState>>> => {
        const result = await this.appClient.state.global.getAll()
        return {
          amount: result.amount,
          client: result.client,
          freelancer: result.freelancer,
          status: result.status,
          workHash: new BinaryStateValue(result.work_hash),
        }
      },
      /**
       * Get the current value of the amount key in global state
       */
      amount: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("amount")) as bigint | undefined },
      /**
       * Get the current value of the client key in global state
       */
      client: async (): Promise<string | undefined> => { return (await this.appClient.state.global.getValue("client")) as string | undefined },
      /**
       * Get the current value of the freelancer key in global state
       */
      freelancer: async (): Promise<string | undefined> => { return (await this.appClient.state.global.getValue("freelancer")) as string | undefined },
      /**
       * Get the current value of the status key in global state
       */
      status: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("status")) as bigint | undefined },
      /**
       * Get the current value of the work_hash key in global state
       */
      workHash: async (): Promise<BinaryState> => { return new BinaryStateValue((await this.appClient.state.global.getValue("work_hash")) as Uint8Array | undefined) },
    },
  }

  public newGroup(): EscrowContractComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a create_escrow(address,uint64,pay)void method call against the EscrowContract contract
       */
      createEscrow(params: CallParams<EscrowContractArgs['obj']['create_escrow(address,uint64,pay)void'] | EscrowContractArgs['tuple']['create_escrow(address,uint64,pay)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.createEscrow(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a submit_work(string)void method call against the EscrowContract contract
       */
      submitWork(params: CallParams<EscrowContractArgs['obj']['submit_work(string)void'] | EscrowContractArgs['tuple']['submit_work(string)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.submitWork(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a approve()void method call against the EscrowContract contract
       */
      approve(params: CallParams<EscrowContractArgs['obj']['approve()void'] | EscrowContractArgs['tuple']['approve()void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.approve(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a release_payment()void method call against the EscrowContract contract
       */
      releasePayment(params: CallParams<EscrowContractArgs['obj']['release_payment()void'] | EscrowContractArgs['tuple']['release_payment()void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.releasePayment(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a refund()void method call against the EscrowContract contract
       */
      refund(params: CallParams<EscrowContractArgs['obj']['refund()void'] | EscrowContractArgs['tuple']['refund()void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.refund(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a clear state call to the EscrowContract contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as EscrowContractComposer
  }
}
export type EscrowContractComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the create_escrow(address,uint64,pay)void ABI method.
   *
  * Initialise escrow. Caller becomes the client; the grouped Payment must
  deliver `amount` microalgos to the application account.

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  createEscrow(params?: CallParams<EscrowContractArgs['obj']['create_escrow(address,uint64,pay)void'] | EscrowContractArgs['tuple']['create_escrow(address,uint64,pay)void']>): EscrowContractComposer<[...TReturns, EscrowContractReturns['create_escrow(address,uint64,pay)void'] | undefined]>

  /**
   * Calls the submit_work(string)void ABI method.
   *
   * Freelancer records deliverable CID; status -> SUBMITTED.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  submitWork(params?: CallParams<EscrowContractArgs['obj']['submit_work(string)void'] | EscrowContractArgs['tuple']['submit_work(string)void']>): EscrowContractComposer<[...TReturns, EscrowContractReturns['submit_work(string)void'] | undefined]>

  /**
   * Calls the approve()void ABI method.
   *
   * Client approves submitted deliverable; status -> APPROVED.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  approve(params?: CallParams<EscrowContractArgs['obj']['approve()void'] | EscrowContractArgs['tuple']['approve()void']>): EscrowContractComposer<[...TReturns, EscrowContractReturns['approve()void'] | undefined]>

  /**
   * Calls the release_payment()void ABI method.
   *
   * Freelancer pulls escrow after approval; status -> RELEASED.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  releasePayment(params?: CallParams<EscrowContractArgs['obj']['release_payment()void'] | EscrowContractArgs['tuple']['release_payment()void']>): EscrowContractComposer<[...TReturns, EscrowContractReturns['release_payment()void'] | undefined]>

  /**
   * Calls the refund()void ABI method.
   *
   * Client refund window: only CREATED or SUBMITTED, never APPROVED+.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  refund(params?: CallParams<EscrowContractArgs['obj']['refund()void'] | EscrowContractArgs['tuple']['refund()void']>): EscrowContractComposer<[...TReturns, EscrowContractReturns['refund()void'] | undefined]>

  /**
   * Makes a clear_state call to an existing instance of the EscrowContract smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): EscrowContractComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): EscrowContractComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<EscrowContractComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<EscrowContractComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<EscrowContractComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<EscrowContractComposerResults<TReturns>>
}
export type EscrowContractComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

