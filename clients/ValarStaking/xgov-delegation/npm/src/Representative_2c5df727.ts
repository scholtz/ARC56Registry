/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"Representative","structs":{"Vote":[{"name":"approval","type":"uint64"},{"name":"rejection","type":"uint64"}]},"methods":[{"name":"create","args":[{"type":"address","name":"representative_address","desc":"Address of the representative."}],"returns":{"type":"void"},"actions":{"create":["NoOp"],"call":[]},"readonly":false,"desc":"Create a new representative.\nMUST BE CALLED BY THE REGISTRY CONTRACT.","events":[],"recommendations":{}},{"name":"update","args":[],"returns":{"type":"void"},"actions":{"create":[],"call":["UpdateApplication"]},"readonly":false,"desc":"Update the contract.\nMUST BE CALLED BY THE REGISTRY CONTRACT.","events":[],"recommendations":{}},{"name":"pause","args":[],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Pause the representation.","events":[],"recommendations":{}},{"name":"resume","args":[],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Resume the representation.","events":[],"recommendations":{}},{"name":"publish_vote","args":[{"type":"pay","name":"payment","desc":"Payment to cover MBR."},{"type":"uint64","name":"proposal_id","desc":"App ID of proposal to vote on."},{"type":"(uint64,uint64)","struct":"Vote","name":"vote","desc":"Representative's vote for the proposal."}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Publish representative's vote.","events":[],"recommendations":{}},{"name":"delete_vote","args":[{"type":"uint64","name":"proposal_id","desc":"App ID of proposal which vote to delete."}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Delete a representative's vote.\nCannot delete vote during voting stage of a proposal.","events":[],"recommendations":{}},{"name":"delete","args":[],"returns":{"type":"void"},"actions":{"create":[],"call":["DeleteApplication"]},"readonly":false,"desc":"Delete the representative.\nMUST BE CALLED BY THE REGISTRY CONTRACT.","events":[],"recommendations":{}},{"name":"get_vote_box","args":[{"type":"uint64","name":"proposal_id","desc":"App ID of the proposal."}],"returns":{"type":"((uint64,uint64),bool)","desc":"Representative's vote. bool: `True` if representative's vote exists, else `False`."},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"desc":"Get the representative's vote on a proposal.","events":[],"recommendations":{}},{"name":"get_vote","args":[{"type":"uint64","name":"proposal_id","desc":"App ID of the proposal."}],"returns":{"type":"((uint64,uint64),bool)","desc":"Representative's vote. bool: True if representative's vote exists and representative isn't paused."},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"desc":"Get the representative's vote on a proposal.","events":[],"recommendations":{}}],"arcs":[22,28],"networks":{},"state":{"schema":{"global":{"ints":32,"bytes":32},"local":{"ints":7,"bytes":9}},"keys":{"global":{"representative_address":{"keyType":"AVMBytes","valueType":"address","key":"cmVwcmVzZW50YXRpdmVfYWRkcmVzcw=="},"registry_app":{"keyType":"AVMBytes","valueType":"AVMUint64","key":"cmVnaXN0cnlfYXBw"},"paused":{"keyType":"AVMBytes","valueType":"AVMUint64","key":"cGF1c2Vk"}},"local":{},"box":{}},"maps":{"global":{},"local":{},"box":{"proposals_vote_box":{"keyType":"uint64","valueType":"Vote","prefix":"cHY="}}}},"bareActions":{"create":[],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[345],"errorMessage":"Contract is paused"},{"pc":[492],"errorMessage":"Not all boxes deleted"},{"pc":[202],"errorMessage":"OnCompletion must be DeleteApplication && can only call when not creating"},{"pc":[124],"errorMessage":"OnCompletion must be NoOp"},{"pc":[214],"errorMessage":"OnCompletion must be UpdateApplication && can only call when not creating"},{"pc":[440],"errorMessage":"Proposal is in voting stage"},{"pc":[339],"errorMessage":"Proposal is not part of xGov Registry"},{"pc":[355],"errorMessage":"Representative vote was already published"},{"pc":[246,483],"errorMessage":"Sender is not app creator"},{"pc":[228,252,261,302,416],"errorMessage":"Unauthorized"},{"pc":[363,371],"errorMessage":"Vote not in PPM"},{"pc":[72],"errorMessage":"Wrong Global Bytes allocation"},{"pc":[77],"errorMessage":"Wrong Global UInts allocation"},{"pc":[83],"errorMessage":"Wrong Local Bytes allocation"},{"pc":[89],"errorMessage":"Wrong Local UInts allocation"},{"pc":[389],"errorMessage":"Wrong Receiver"},{"pc":[394],"errorMessage":"Wrong payment amount"},{"pc":[298,379,412,453,488],"errorMessage":"account funded"},{"pc":[309,337],"errorMessage":"application exists"},{"pc":[343,556],"errorMessage":"check self.paused exists"},{"pc":[605],"errorMessage":"check self.proposals_vote_box entry exists"},{"pc":[313],"errorMessage":"check self.registry_app exists"},{"pc":[639],"errorMessage":"check self.representative_address exists"},{"pc":[225],"errorMessage":"invalid number of bytes for arc4.static_array<arc4.uint8, 32>"},{"pc":[284,404,520,546],"errorMessage":"invalid number of bytes for arc4.uint64"},{"pc":[293],"errorMessage":"invalid number of bytes for smart_contracts.common.abi_types.Vote"},{"pc":[276],"errorMessage":"transaction type is pay"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"events":[]} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


// Type definitions for ARC-56 structs

export type Vote = {
  approval: bigint,
  rejection: bigint
}


/**
 * Converts the ABI tuple representation of a Vote to the struct representation
 */
export function VoteFromTuple(abiTuple: [bigint, bigint]) {
  return getABIStructFromABITuple(abiTuple, APP_SPEC.structs.Vote, APP_SPEC.structs) as Vote
}

/**
 * The argument types for the Representative contract
 */
export type RepresentativeArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'create(address)void': {
      /**
       * Address of the representative.
       */
      representativeAddress: string
    }
    'update()void': Record<string, never>
    'pause()void': Record<string, never>
    'resume()void': Record<string, never>
    'publish_vote(pay,uint64,(uint64,uint64))void': {
      /**
       * Payment to cover MBR.
       */
      payment: AppMethodCallTransactionArgument
      /**
       * App ID of proposal to vote on.
       */
      proposalId: bigint | number
      /**
       * Representative's vote for the proposal.
       */
      vote: Vote
    }
    'delete_vote(uint64)void': {
      /**
       * App ID of proposal which vote to delete.
       */
      proposalId: bigint | number
    }
    'delete()void': Record<string, never>
    'get_vote_box(uint64)((uint64,uint64),bool)': {
      /**
       * App ID of the proposal.
       */
      proposalId: bigint | number
    }
    'get_vote(uint64)((uint64,uint64),bool)': {
      /**
       * App ID of the proposal.
       */
      proposalId: bigint | number
    }
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'create(address)void': [representativeAddress: string]
    'update()void': []
    'pause()void': []
    'resume()void': []
    'publish_vote(pay,uint64,(uint64,uint64))void': [payment: AppMethodCallTransactionArgument, proposalId: bigint | number, vote: Vote]
    'delete_vote(uint64)void': [proposalId: bigint | number]
    'delete()void': []
    'get_vote_box(uint64)((uint64,uint64),bool)': [proposalId: bigint | number]
    'get_vote(uint64)((uint64,uint64),bool)': [proposalId: bigint | number]
  }
}

/**
 * The return type for each method
 */
export type RepresentativeReturns = {
  'create(address)void': void
  'update()void': void
  'pause()void': void
  'resume()void': void
  'publish_vote(pay,uint64,(uint64,uint64))void': void
  'delete_vote(uint64)void': void
  'delete()void': void
  'get_vote_box(uint64)((uint64,uint64),bool)': [[bigint, bigint], boolean]
  'get_vote(uint64)((uint64,uint64),bool)': [[bigint, bigint], boolean]
}

/**
 * Defines the types of available calls and state of the Representative smart contract.
 */
export type RepresentativeTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'create(address)void' | 'create', {
      argsObj: RepresentativeArgs['obj']['create(address)void']
      argsTuple: RepresentativeArgs['tuple']['create(address)void']
      returns: RepresentativeReturns['create(address)void']
    }>
    & Record<'update()void' | 'update', {
      argsObj: RepresentativeArgs['obj']['update()void']
      argsTuple: RepresentativeArgs['tuple']['update()void']
      returns: RepresentativeReturns['update()void']
    }>
    & Record<'pause()void' | 'pause', {
      argsObj: RepresentativeArgs['obj']['pause()void']
      argsTuple: RepresentativeArgs['tuple']['pause()void']
      returns: RepresentativeReturns['pause()void']
    }>
    & Record<'resume()void' | 'resume', {
      argsObj: RepresentativeArgs['obj']['resume()void']
      argsTuple: RepresentativeArgs['tuple']['resume()void']
      returns: RepresentativeReturns['resume()void']
    }>
    & Record<'publish_vote(pay,uint64,(uint64,uint64))void' | 'publish_vote', {
      argsObj: RepresentativeArgs['obj']['publish_vote(pay,uint64,(uint64,uint64))void']
      argsTuple: RepresentativeArgs['tuple']['publish_vote(pay,uint64,(uint64,uint64))void']
      returns: RepresentativeReturns['publish_vote(pay,uint64,(uint64,uint64))void']
    }>
    & Record<'delete_vote(uint64)void' | 'delete_vote', {
      argsObj: RepresentativeArgs['obj']['delete_vote(uint64)void']
      argsTuple: RepresentativeArgs['tuple']['delete_vote(uint64)void']
      returns: RepresentativeReturns['delete_vote(uint64)void']
    }>
    & Record<'delete()void' | 'delete', {
      argsObj: RepresentativeArgs['obj']['delete()void']
      argsTuple: RepresentativeArgs['tuple']['delete()void']
      returns: RepresentativeReturns['delete()void']
    }>
    & Record<'get_vote_box(uint64)((uint64,uint64),bool)' | 'get_vote_box', {
      argsObj: RepresentativeArgs['obj']['get_vote_box(uint64)((uint64,uint64),bool)']
      argsTuple: RepresentativeArgs['tuple']['get_vote_box(uint64)((uint64,uint64),bool)']
      /**
       * Representative's vote. bool: `True` if representative's vote exists, else `False`.
       */
      returns: RepresentativeReturns['get_vote_box(uint64)((uint64,uint64),bool)']
    }>
    & Record<'get_vote(uint64)((uint64,uint64),bool)' | 'get_vote', {
      argsObj: RepresentativeArgs['obj']['get_vote(uint64)((uint64,uint64),bool)']
      argsTuple: RepresentativeArgs['tuple']['get_vote(uint64)((uint64,uint64),bool)']
      /**
       * Representative's vote. bool: True if representative's vote exists and representative isn't paused.
       */
      returns: RepresentativeReturns['get_vote(uint64)((uint64,uint64),bool)']
    }>
  /**
   * Defines the shape of the state of the application.
   */
  state: {
    global: {
      keys: {
        representativeAddress: string
        registryApp: bigint
        paused: bigint
      }
      maps: {}
    }
    box: {
      keys: {}
      maps: {
        proposalsVoteBox: Map<bigint | number, Vote>
      }
    }
  }
}

/**
 * Defines the possible abi call signatures.
 */
export type RepresentativeSignatures = keyof RepresentativeTypes['methods']
/**
 * Defines the possible abi call signatures for methods that return a non-void value.
 */
export type RepresentativeNonVoidMethodSignatures = keyof RepresentativeTypes['methods'] extends infer T ? T extends keyof RepresentativeTypes['methods'] ? MethodReturn<T> extends void ? never : T  : never : never
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the Representative smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends RepresentativeSignatures> = RepresentativeTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the Representative smart contract to the method's return type
 */
export type MethodReturn<TSignature extends RepresentativeSignatures> = RepresentativeTypes['methods'][TSignature]['returns']

/**
 * Defines the shape of the keyed global state of the application.
 */
export type GlobalKeysState = RepresentativeTypes['state']['global']['keys']

/**
 * Defines the shape of the keyed box state of the application.
 */
export type BoxKeysState = RepresentativeTypes['state']['box']['keys']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the Representative smart contract
 */
export abstract class RepresentativeParamsFactory {
  /**
   * Constructs a no op call for the pause()void ABI method
   *
   * Pause the representation.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static pause(params: CallParams<RepresentativeArgs['obj']['pause()void'] | RepresentativeArgs['tuple']['pause()void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'pause()void' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the resume()void ABI method
   *
   * Resume the representation.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static resume(params: CallParams<RepresentativeArgs['obj']['resume()void'] | RepresentativeArgs['tuple']['resume()void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'resume()void' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the publish_vote(pay,uint64,(uint64,uint64))void ABI method
   *
   * Publish representative's vote.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static publishVote(params: CallParams<RepresentativeArgs['obj']['publish_vote(pay,uint64,(uint64,uint64))void'] | RepresentativeArgs['tuple']['publish_vote(pay,uint64,(uint64,uint64))void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'publish_vote(pay,uint64,(uint64,uint64))void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.payment, params.args.proposalId, params.args.vote],
    }
  }
  /**
   * Constructs a no op call for the delete_vote(uint64)void ABI method
   *
  * Delete a representative's vote.
  Cannot delete vote during voting stage of a proposal.

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static deleteVote(params: CallParams<RepresentativeArgs['obj']['delete_vote(uint64)void'] | RepresentativeArgs['tuple']['delete_vote(uint64)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'delete_vote(uint64)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.proposalId],
    }
  }
  /**
   * Constructs a no op call for the get_vote_box(uint64)((uint64,uint64),bool) ABI method
   *
   * Get the representative's vote on a proposal.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static getVoteBox(params: CallParams<RepresentativeArgs['obj']['get_vote_box(uint64)((uint64,uint64),bool)'] | RepresentativeArgs['tuple']['get_vote_box(uint64)((uint64,uint64),bool)']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'get_vote_box(uint64)((uint64,uint64),bool)' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.proposalId],
    }
  }
  /**
   * Constructs a no op call for the get_vote(uint64)((uint64,uint64),bool) ABI method
   *
   * Get the representative's vote on a proposal.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static getVote(params: CallParams<RepresentativeArgs['obj']['get_vote(uint64)((uint64,uint64),bool)'] | RepresentativeArgs['tuple']['get_vote(uint64)((uint64,uint64),bool)']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'get_vote(uint64)((uint64,uint64),bool)' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.proposalId],
    }
  }
}

/**
 * A client to make calls to the Representative smart contract
 */
export class RepresentativeClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `RepresentativeClient`
   *
   * @param appClient An `AppClient` instance which has been created with the Representative app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `RepresentativeClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Checks for decode errors on the given return value and maps the return value to the return type for the given method
   * @returns The typed return value or undefined if there was no value
   */
  decodeReturnValue<TSignature extends RepresentativeNonVoidMethodSignatures>(method: TSignature, returnValue: ABIReturn | undefined) {
    return returnValue !== undefined ? getArc56ReturnValue<MethodReturn<TSignature>>(returnValue, this.appClient.getABIMethod(method), APP_SPEC.structs) : undefined
  }

  /**
   * Returns a new `RepresentativeClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<RepresentativeClient> {
    return new RepresentativeClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `RepresentativeClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<RepresentativeClient> {
    return new RepresentativeClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Makes a clear_state call to an existing instance of the Representative smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the Representative smart contract using the `pause()void` ABI method.
     *
     * Pause the representation.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    pause: (params: CallParams<RepresentativeArgs['obj']['pause()void'] | RepresentativeArgs['tuple']['pause()void']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(RepresentativeParamsFactory.pause(params))
    },

    /**
     * Makes a call to the Representative smart contract using the `resume()void` ABI method.
     *
     * Resume the representation.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    resume: (params: CallParams<RepresentativeArgs['obj']['resume()void'] | RepresentativeArgs['tuple']['resume()void']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(RepresentativeParamsFactory.resume(params))
    },

    /**
     * Makes a call to the Representative smart contract using the `publish_vote(pay,uint64,(uint64,uint64))void` ABI method.
     *
     * Publish representative's vote.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    publishVote: (params: CallParams<RepresentativeArgs['obj']['publish_vote(pay,uint64,(uint64,uint64))void'] | RepresentativeArgs['tuple']['publish_vote(pay,uint64,(uint64,uint64))void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(RepresentativeParamsFactory.publishVote(params))
    },

    /**
     * Makes a call to the Representative smart contract using the `delete_vote(uint64)void` ABI method.
     *
    * Delete a representative's vote.
    Cannot delete vote during voting stage of a proposal.

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    deleteVote: (params: CallParams<RepresentativeArgs['obj']['delete_vote(uint64)void'] | RepresentativeArgs['tuple']['delete_vote(uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(RepresentativeParamsFactory.deleteVote(params))
    },

    /**
     * Makes a call to the Representative smart contract using the `get_vote_box(uint64)((uint64,uint64),bool)` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * Get the representative's vote on a proposal.
     *
     * @param params The params for the smart contract call
     * @returns The call params: Representative's vote. bool: `True` if representative's vote exists, else `False`.
     */
    getVoteBox: (params: CallParams<RepresentativeArgs['obj']['get_vote_box(uint64)((uint64,uint64),bool)'] | RepresentativeArgs['tuple']['get_vote_box(uint64)((uint64,uint64),bool)']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(RepresentativeParamsFactory.getVoteBox(params))
    },

    /**
     * Makes a call to the Representative smart contract using the `get_vote(uint64)((uint64,uint64),bool)` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * Get the representative's vote on a proposal.
     *
     * @param params The params for the smart contract call
     * @returns The call params: Representative's vote. bool: True if representative's vote exists and representative isn't paused.
     */
    getVote: (params: CallParams<RepresentativeArgs['obj']['get_vote(uint64)((uint64,uint64),bool)'] | RepresentativeArgs['tuple']['get_vote(uint64)((uint64,uint64),bool)']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(RepresentativeParamsFactory.getVote(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Makes a clear_state call to an existing instance of the Representative smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the Representative smart contract using the `pause()void` ABI method.
     *
     * Pause the representation.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    pause: (params: CallParams<RepresentativeArgs['obj']['pause()void'] | RepresentativeArgs['tuple']['pause()void']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(RepresentativeParamsFactory.pause(params))
    },

    /**
     * Makes a call to the Representative smart contract using the `resume()void` ABI method.
     *
     * Resume the representation.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    resume: (params: CallParams<RepresentativeArgs['obj']['resume()void'] | RepresentativeArgs['tuple']['resume()void']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(RepresentativeParamsFactory.resume(params))
    },

    /**
     * Makes a call to the Representative smart contract using the `publish_vote(pay,uint64,(uint64,uint64))void` ABI method.
     *
     * Publish representative's vote.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    publishVote: (params: CallParams<RepresentativeArgs['obj']['publish_vote(pay,uint64,(uint64,uint64))void'] | RepresentativeArgs['tuple']['publish_vote(pay,uint64,(uint64,uint64))void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(RepresentativeParamsFactory.publishVote(params))
    },

    /**
     * Makes a call to the Representative smart contract using the `delete_vote(uint64)void` ABI method.
     *
    * Delete a representative's vote.
    Cannot delete vote during voting stage of a proposal.

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    deleteVote: (params: CallParams<RepresentativeArgs['obj']['delete_vote(uint64)void'] | RepresentativeArgs['tuple']['delete_vote(uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(RepresentativeParamsFactory.deleteVote(params))
    },

    /**
     * Makes a call to the Representative smart contract using the `get_vote_box(uint64)((uint64,uint64),bool)` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * Get the representative's vote on a proposal.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction: Representative's vote. bool: `True` if representative's vote exists, else `False`.
     */
    getVoteBox: (params: CallParams<RepresentativeArgs['obj']['get_vote_box(uint64)((uint64,uint64),bool)'] | RepresentativeArgs['tuple']['get_vote_box(uint64)((uint64,uint64),bool)']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(RepresentativeParamsFactory.getVoteBox(params))
    },

    /**
     * Makes a call to the Representative smart contract using the `get_vote(uint64)((uint64,uint64),bool)` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * Get the representative's vote on a proposal.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction: Representative's vote. bool: True if representative's vote exists and representative isn't paused.
     */
    getVote: (params: CallParams<RepresentativeArgs['obj']['get_vote(uint64)((uint64,uint64),bool)'] | RepresentativeArgs['tuple']['get_vote(uint64)((uint64,uint64),bool)']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(RepresentativeParamsFactory.getVote(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Makes a clear_state call to an existing instance of the Representative smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the Representative smart contract using the `pause()void` ABI method.
     *
     * Pause the representation.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    pause: async (params: CallParams<RepresentativeArgs['obj']['pause()void'] | RepresentativeArgs['tuple']['pause()void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(RepresentativeParamsFactory.pause(params))
      return {...result, return: result.return as unknown as (undefined | RepresentativeReturns['pause()void'])}
    },

    /**
     * Makes a call to the Representative smart contract using the `resume()void` ABI method.
     *
     * Resume the representation.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    resume: async (params: CallParams<RepresentativeArgs['obj']['resume()void'] | RepresentativeArgs['tuple']['resume()void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(RepresentativeParamsFactory.resume(params))
      return {...result, return: result.return as unknown as (undefined | RepresentativeReturns['resume()void'])}
    },

    /**
     * Makes a call to the Representative smart contract using the `publish_vote(pay,uint64,(uint64,uint64))void` ABI method.
     *
     * Publish representative's vote.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    publishVote: async (params: CallParams<RepresentativeArgs['obj']['publish_vote(pay,uint64,(uint64,uint64))void'] | RepresentativeArgs['tuple']['publish_vote(pay,uint64,(uint64,uint64))void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(RepresentativeParamsFactory.publishVote(params))
      return {...result, return: result.return as unknown as (undefined | RepresentativeReturns['publish_vote(pay,uint64,(uint64,uint64))void'])}
    },

    /**
     * Makes a call to the Representative smart contract using the `delete_vote(uint64)void` ABI method.
     *
    * Delete a representative's vote.
    Cannot delete vote during voting stage of a proposal.

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    deleteVote: async (params: CallParams<RepresentativeArgs['obj']['delete_vote(uint64)void'] | RepresentativeArgs['tuple']['delete_vote(uint64)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(RepresentativeParamsFactory.deleteVote(params))
      return {...result, return: result.return as unknown as (undefined | RepresentativeReturns['delete_vote(uint64)void'])}
    },

    /**
     * Makes a call to the Representative smart contract using the `get_vote_box(uint64)((uint64,uint64),bool)` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * Get the representative's vote on a proposal.
     *
     * @param params The params for the smart contract call
     * @returns The call result: Representative's vote. bool: `True` if representative's vote exists, else `False`.
     */
    getVoteBox: async (params: CallParams<RepresentativeArgs['obj']['get_vote_box(uint64)((uint64,uint64),bool)'] | RepresentativeArgs['tuple']['get_vote_box(uint64)((uint64,uint64),bool)']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(RepresentativeParamsFactory.getVoteBox(params))
      return {...result, return: result.return as unknown as (undefined | RepresentativeReturns['get_vote_box(uint64)((uint64,uint64),bool)'])}
    },

    /**
     * Makes a call to the Representative smart contract using the `get_vote(uint64)((uint64,uint64),bool)` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * Get the representative's vote on a proposal.
     *
     * @param params The params for the smart contract call
     * @returns The call result: Representative's vote. bool: True if representative's vote exists and representative isn't paused.
     */
    getVote: async (params: CallParams<RepresentativeArgs['obj']['get_vote(uint64)((uint64,uint64),bool)'] | RepresentativeArgs['tuple']['get_vote(uint64)((uint64,uint64),bool)']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(RepresentativeParamsFactory.getVote(params))
      return {...result, return: result.return as unknown as (undefined | RepresentativeReturns['get_vote(uint64)((uint64,uint64),bool)'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new RepresentativeClient(this.appClient.clone(params))
  }

  /**
   * Makes a readonly (simulated) call to the Representative smart contract using the `get_vote_box(uint64)((uint64,uint64),bool)` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * Get the representative's vote on a proposal.
   *
   * @param params The params for the smart contract call
   * @returns The call result: Representative's vote. bool: `True` if representative's vote exists, else `False`.
   */
  async getVoteBox(params: CallParams<RepresentativeArgs['obj']['get_vote_box(uint64)((uint64,uint64),bool)'] | RepresentativeArgs['tuple']['get_vote_box(uint64)((uint64,uint64),bool)']>) {
    const result = await this.appClient.send.call(RepresentativeParamsFactory.getVoteBox(params))
    return result.return as unknown as RepresentativeReturns['get_vote_box(uint64)((uint64,uint64),bool)']
  }

  /**
   * Makes a readonly (simulated) call to the Representative smart contract using the `get_vote(uint64)((uint64,uint64),bool)` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * Get the representative's vote on a proposal.
   *
   * @param params The params for the smart contract call
   * @returns The call result: Representative's vote. bool: True if representative's vote exists and representative isn't paused.
   */
  async getVote(params: CallParams<RepresentativeArgs['obj']['get_vote(uint64)((uint64,uint64),bool)'] | RepresentativeArgs['tuple']['get_vote(uint64)((uint64,uint64),bool)']>) {
    const result = await this.appClient.send.call(RepresentativeParamsFactory.getVote(params))
    return result.return as unknown as RepresentativeReturns['get_vote(uint64)((uint64,uint64),bool)']
  }

  /**
   * Methods to access state for the current Representative app
   */
  state = {
    /**
     * Methods to access global state for the current Representative app
     */
    global: {
      /**
       * Get all current keyed values from global state
       */
      getAll: async (): Promise<Partial<Expand<GlobalKeysState>>> => {
        const result = await this.appClient.state.global.getAll()
        return {
          representativeAddress: result.representative_address,
          registryApp: result.registry_app,
          paused: result.paused,
        }
      },
      /**
       * Get the current value of the representative_address key in global state
       */
      representativeAddress: async (): Promise<string | undefined> => { return (await this.appClient.state.global.getValue("representative_address")) as string | undefined },
      /**
       * Get the current value of the registry_app key in global state
       */
      registryApp: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("registry_app")) as bigint | undefined },
      /**
       * Get the current value of the paused key in global state
       */
      paused: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("paused")) as bigint | undefined },
    },
    /**
     * Methods to access box state for the current Representative app
     */
    box: {
      /**
       * Get all current keyed values from box state
       */
      getAll: async (): Promise<Partial<Expand<BoxKeysState>>> => {
        const result = await this.appClient.state.box.getAll()
        return {
        }
      },
      /**
       * Get values from the proposals_vote_box map in box state
       */
      proposalsVoteBox: {
        /**
         * Get all current values of the proposals_vote_box map in box state
         */
        getMap: async (): Promise<Map<bigint, Vote>> => { return (await this.appClient.state.box.getMap("proposals_vote_box")) as Map<bigint, Vote> },
        /**
         * Get a current value of the proposals_vote_box map by key from box state
         */
        value: async (key: bigint | number): Promise<Vote | undefined> => { return await this.appClient.state.box.getMapValue("proposals_vote_box", key) as Vote | undefined },
      },
    },
  }

  public newGroup(): RepresentativeComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a pause()void method call against the Representative contract
       */
      pause(params: CallParams<RepresentativeArgs['obj']['pause()void'] | RepresentativeArgs['tuple']['pause()void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.pause(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a resume()void method call against the Representative contract
       */
      resume(params: CallParams<RepresentativeArgs['obj']['resume()void'] | RepresentativeArgs['tuple']['resume()void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.resume(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a publish_vote(pay,uint64,(uint64,uint64))void method call against the Representative contract
       */
      publishVote(params: CallParams<RepresentativeArgs['obj']['publish_vote(pay,uint64,(uint64,uint64))void'] | RepresentativeArgs['tuple']['publish_vote(pay,uint64,(uint64,uint64))void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.publishVote(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a delete_vote(uint64)void method call against the Representative contract
       */
      deleteVote(params: CallParams<RepresentativeArgs['obj']['delete_vote(uint64)void'] | RepresentativeArgs['tuple']['delete_vote(uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.deleteVote(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a get_vote_box(uint64)((uint64,uint64),bool) method call against the Representative contract
       */
      getVoteBox(params: CallParams<RepresentativeArgs['obj']['get_vote_box(uint64)((uint64,uint64),bool)'] | RepresentativeArgs['tuple']['get_vote_box(uint64)((uint64,uint64),bool)']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.getVoteBox(params)))
        resultMappers.push((v) => client.decodeReturnValue('get_vote_box(uint64)((uint64,uint64),bool)', v))
        return this
      },
      /**
       * Add a get_vote(uint64)((uint64,uint64),bool) method call against the Representative contract
       */
      getVote(params: CallParams<RepresentativeArgs['obj']['get_vote(uint64)((uint64,uint64),bool)'] | RepresentativeArgs['tuple']['get_vote(uint64)((uint64,uint64),bool)']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.getVote(params)))
        resultMappers.push((v) => client.decodeReturnValue('get_vote(uint64)((uint64,uint64),bool)', v))
        return this
      },
      /**
       * Add a clear state call to the Representative contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as RepresentativeComposer
  }
}
export type RepresentativeComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the pause()void ABI method.
   *
   * Pause the representation.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  pause(params?: CallParams<RepresentativeArgs['obj']['pause()void'] | RepresentativeArgs['tuple']['pause()void']>): RepresentativeComposer<[...TReturns, RepresentativeReturns['pause()void'] | undefined]>

  /**
   * Calls the resume()void ABI method.
   *
   * Resume the representation.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  resume(params?: CallParams<RepresentativeArgs['obj']['resume()void'] | RepresentativeArgs['tuple']['resume()void']>): RepresentativeComposer<[...TReturns, RepresentativeReturns['resume()void'] | undefined]>

  /**
   * Calls the publish_vote(pay,uint64,(uint64,uint64))void ABI method.
   *
   * Publish representative's vote.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  publishVote(params?: CallParams<RepresentativeArgs['obj']['publish_vote(pay,uint64,(uint64,uint64))void'] | RepresentativeArgs['tuple']['publish_vote(pay,uint64,(uint64,uint64))void']>): RepresentativeComposer<[...TReturns, RepresentativeReturns['publish_vote(pay,uint64,(uint64,uint64))void'] | undefined]>

  /**
   * Calls the delete_vote(uint64)void ABI method.
   *
  * Delete a representative's vote.
  Cannot delete vote during voting stage of a proposal.

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  deleteVote(params?: CallParams<RepresentativeArgs['obj']['delete_vote(uint64)void'] | RepresentativeArgs['tuple']['delete_vote(uint64)void']>): RepresentativeComposer<[...TReturns, RepresentativeReturns['delete_vote(uint64)void'] | undefined]>

  /**
   * Calls the get_vote_box(uint64)((uint64,uint64),bool) ABI method.
   *
   * Get the representative's vote on a proposal.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  getVoteBox(params?: CallParams<RepresentativeArgs['obj']['get_vote_box(uint64)((uint64,uint64),bool)'] | RepresentativeArgs['tuple']['get_vote_box(uint64)((uint64,uint64),bool)']>): RepresentativeComposer<[...TReturns, RepresentativeReturns['get_vote_box(uint64)((uint64,uint64),bool)'] | undefined]>

  /**
   * Calls the get_vote(uint64)((uint64,uint64),bool) ABI method.
   *
   * Get the representative's vote on a proposal.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  getVote(params?: CallParams<RepresentativeArgs['obj']['get_vote(uint64)((uint64,uint64),bool)'] | RepresentativeArgs['tuple']['get_vote(uint64)((uint64,uint64),bool)']>): RepresentativeComposer<[...TReturns, RepresentativeReturns['get_vote(uint64)((uint64,uint64),bool)'] | undefined]>

  /**
   * Makes a clear_state call to an existing instance of the Representative smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): RepresentativeComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): RepresentativeComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<RepresentativeComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<RepresentativeComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<RepresentativeComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<RepresentativeComposerResults<TReturns>>
}
export type RepresentativeComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

