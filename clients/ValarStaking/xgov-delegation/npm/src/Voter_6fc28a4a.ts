/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"Voter","structs":{"VoteRaw":[{"name":"approvals","type":"uint64"},{"name":"rejections","type":"uint64"}]},"methods":[{"name":"create","args":[],"returns":{"type":"void"},"actions":{"create":["NoOp"],"call":[]},"readonly":false,"desc":"Create a new Voter.\nMUST BE CALLED BY THE REGISTRY CONTRACT.","events":[],"recommendations":{}},{"name":"update","args":[],"returns":{"type":"void"},"actions":{"create":[],"call":["UpdateApplication"]},"readonly":false,"desc":"Update the contract.\nMUST BE CALLED BY THE REGISTRY CONTRACT.","events":[],"recommendations":{}},{"name":"assign_xgov","args":[{"type":"address","name":"xgov_address","desc":"Address of the xGov."},{"type":"address","name":"manager_address","desc":"Address of the new manager."}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Assign the contract to an xGov.","events":[],"recommendations":{}},{"name":"set_manager","args":[{"type":"address","name":"manager_address","desc":"Address of the new manager."}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Set a new manager.","events":[],"recommendations":{}},{"name":"set_representative","args":[{"type":"uint64","name":"representative_id","desc":"The application ID of the new representative."}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Set a new representative.","events":[],"recommendations":{}},{"name":"set_window","args":[{"type":"uint64","name":"window_ts","desc":"The new time window in seconds before proposal voting period ends that representative can cast the vote. Set to 0 to not have any delay."}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Set a new window when vote can be cast as representative.","events":[],"recommendations":{}},{"name":"vote_representative","args":[{"type":"uint64","name":"proposal_id","desc":"App ID of proposal to vote on."}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Vote on a proposal according to vote of representative if time is within window.\nMUST BE CALLED BY THE REGISTRY CONTRACT.","events":[],"recommendations":{}},{"name":"vote_direct","args":[{"type":"uint64","name":"proposal_id","desc":"App ID of proposal to vote on."},{"type":"(uint64,uint64)","struct":"VoteRaw","name":"vote","desc":"xGov's vote for the proposal. Expressed in number of approvals and rejections."}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Vote directly on a proposal.","events":[],"recommendations":{}},{"name":"yield_voting_rights","args":[{"type":"address","name":"voting_address","desc":"The voting account address to delegate voting power to."}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Yield voting rights to another account.","events":[],"recommendations":{}},{"name":"add_votes","args":[{"type":"uint64","name":"add_votes","desc":"Number of approved votes to add."}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Increase votes left.\nMUST BE CALLED BY THE REGISTRY CONTRACT.","events":[],"recommendations":{}},{"name":"delete","args":[],"returns":{"type":"void"},"actions":{"create":[],"call":["DeleteApplication"]},"readonly":false,"desc":"Delete the Voter.\nMUST BE CALLED BY THE REGISTRY CONTRACT.","events":[],"recommendations":{}}],"arcs":[22,28],"networks":{},"state":{"schema":{"global":{"ints":32,"bytes":32},"local":{"ints":8,"bytes":8}},"keys":{"global":{"xgov_address":{"keyType":"AVMBytes","valueType":"address","key":"eGdvdl9hZGRyZXNz"},"registry_app":{"keyType":"AVMBytes","valueType":"AVMUint64","key":"cmVnaXN0cnlfYXBw"},"representative_app":{"keyType":"AVMBytes","valueType":"AVMUint64","key":"cmVwcmVzZW50YXRpdmVfYXBw"},"window_ts":{"keyType":"AVMBytes","valueType":"AVMUint64","key":"d2luZG93X3Rz"},"votes_left":{"keyType":"AVMBytes","valueType":"AVMUint64","key":"dm90ZXNfbGVmdA=="},"manager_address":{"keyType":"AVMBytes","valueType":"address","key":"bWFuYWdlcl9hZGRyZXNz"}},"local":{},"box":{}},"maps":{"global":{},"local":{},"box":{}}},"bareActions":{"create":[],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[411],"errorMessage":"App was not created by registry"},{"pc":[456],"errorMessage":"No paid votes left to cast"},{"pc":[285],"errorMessage":"OnCompletion must be DeleteApplication && can only call when not creating"},{"pc":[193],"errorMessage":"OnCompletion must be NoOp"},{"pc":[297],"errorMessage":"OnCompletion must be UpdateApplication && can only call when not creating"},{"pc":[450,741],"errorMessage":"Proposal is not part of xGov Registry"},{"pc":[541],"errorMessage":"Representative is nonexistent"},{"pc":[595],"errorMessage":"Representative vote is invalid"},{"pc":[334,358,446,873,891],"errorMessage":"Sender is not app creator"},{"pc":[530],"errorMessage":"Too soon to vote"},{"pc":[303,380,398,425,735,817],"errorMessage":"Unauthorized"},{"pc":[131],"errorMessage":"Wrong Global Bytes allocation"},{"pc":[136],"errorMessage":"Wrong Global UInts allocation"},{"pc":[141],"errorMessage":"Wrong Local Bytes allocation"},{"pc":[146],"errorMessage":"Wrong Local UInts allocation"},{"pc":[407,945,956],"errorMessage":"application exists"},{"pc":[585,645],"errorMessage":"application log value is not the result of an ABI return"},{"pc":[928],"errorMessage":"check self.manager_address exists"},{"pc":[681,760,826,949],"errorMessage":"check self.registry_app exists"},{"pc":[535],"errorMessage":"check self.representative_app exists"},{"pc":[454,877],"errorMessage":"check self.votes_left exists"},{"pc":[465,524],"errorMessage":"check self.window_ts exists"},{"pc":[600,746,822,917],"errorMessage":"check self.xgov_address exists"},{"pc":[344,352,376,801],"errorMessage":"invalid number of bytes for arc4.static_array<arc4.uint8, 32>"},{"pc":[636],"errorMessage":"invalid number of bytes for arc4.tuple<arc4.uint64,arc4.bool>"},{"pc":[576],"errorMessage":"invalid number of bytes for arc4.tuple<smart_contracts.common.abi_types.Vote,arc4.bool>"},{"pc":[394,421,440,722,867],"errorMessage":"invalid number of bytes for arc4.uint64"},{"pc":[731],"errorMessage":"invalid number of bytes for smart_contracts.common.abi_types.VoteRaw"},{"pc":[649],"errorMessage":"xGov does not have any votes"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"events":[]} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


// Type definitions for ARC-56 structs

export type VoteRaw = {
  approvals: bigint,
  rejections: bigint
}


/**
 * Converts the ABI tuple representation of a VoteRaw to the struct representation
 */
export function VoteRawFromTuple(abiTuple: [bigint, bigint]) {
  return getABIStructFromABITuple(abiTuple, APP_SPEC.structs.VoteRaw, APP_SPEC.structs) as VoteRaw
}

/**
 * The argument types for the Voter contract
 */
export type VoterArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'create()void': Record<string, never>
    'update()void': Record<string, never>
    'assign_xgov(address,address)void': {
      /**
       * Address of the xGov.
       */
      xgovAddress: string
      /**
       * Address of the new manager.
       */
      managerAddress: string
    }
    'set_manager(address)void': {
      /**
       * Address of the new manager.
       */
      managerAddress: string
    }
    'set_representative(uint64)void': {
      /**
       * The application ID of the new representative.
       */
      representativeId: bigint | number
    }
    'set_window(uint64)void': {
      /**
       * The new time window in seconds before proposal voting period ends that representative can cast the vote. Set to 0 to not have any delay.
       */
      windowTs: bigint | number
    }
    'vote_representative(uint64)void': {
      /**
       * App ID of proposal to vote on.
       */
      proposalId: bigint | number
    }
    'vote_direct(uint64,(uint64,uint64))void': {
      /**
       * App ID of proposal to vote on.
       */
      proposalId: bigint | number
      /**
       * xGov's vote for the proposal. Expressed in number of approvals and rejections.
       */
      vote: VoteRaw
    }
    'yield_voting_rights(address)void': {
      /**
       * The voting account address to delegate voting power to.
       */
      votingAddress: string
    }
    'add_votes(uint64)void': {
      /**
       * Number of approved votes to add.
       */
      addVotes: bigint | number
    }
    'delete()void': Record<string, never>
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'create()void': []
    'update()void': []
    'assign_xgov(address,address)void': [xgovAddress: string, managerAddress: string]
    'set_manager(address)void': [managerAddress: string]
    'set_representative(uint64)void': [representativeId: bigint | number]
    'set_window(uint64)void': [windowTs: bigint | number]
    'vote_representative(uint64)void': [proposalId: bigint | number]
    'vote_direct(uint64,(uint64,uint64))void': [proposalId: bigint | number, vote: VoteRaw]
    'yield_voting_rights(address)void': [votingAddress: string]
    'add_votes(uint64)void': [addVotes: bigint | number]
    'delete()void': []
  }
}

/**
 * The return type for each method
 */
export type VoterReturns = {
  'create()void': void
  'update()void': void
  'assign_xgov(address,address)void': void
  'set_manager(address)void': void
  'set_representative(uint64)void': void
  'set_window(uint64)void': void
  'vote_representative(uint64)void': void
  'vote_direct(uint64,(uint64,uint64))void': void
  'yield_voting_rights(address)void': void
  'add_votes(uint64)void': void
  'delete()void': void
}

/**
 * Defines the types of available calls and state of the Voter smart contract.
 */
export type VoterTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'create()void' | 'create', {
      argsObj: VoterArgs['obj']['create()void']
      argsTuple: VoterArgs['tuple']['create()void']
      returns: VoterReturns['create()void']
    }>
    & Record<'update()void' | 'update', {
      argsObj: VoterArgs['obj']['update()void']
      argsTuple: VoterArgs['tuple']['update()void']
      returns: VoterReturns['update()void']
    }>
    & Record<'assign_xgov(address,address)void' | 'assign_xgov', {
      argsObj: VoterArgs['obj']['assign_xgov(address,address)void']
      argsTuple: VoterArgs['tuple']['assign_xgov(address,address)void']
      returns: VoterReturns['assign_xgov(address,address)void']
    }>
    & Record<'set_manager(address)void' | 'set_manager', {
      argsObj: VoterArgs['obj']['set_manager(address)void']
      argsTuple: VoterArgs['tuple']['set_manager(address)void']
      returns: VoterReturns['set_manager(address)void']
    }>
    & Record<'set_representative(uint64)void' | 'set_representative', {
      argsObj: VoterArgs['obj']['set_representative(uint64)void']
      argsTuple: VoterArgs['tuple']['set_representative(uint64)void']
      returns: VoterReturns['set_representative(uint64)void']
    }>
    & Record<'set_window(uint64)void' | 'set_window', {
      argsObj: VoterArgs['obj']['set_window(uint64)void']
      argsTuple: VoterArgs['tuple']['set_window(uint64)void']
      returns: VoterReturns['set_window(uint64)void']
    }>
    & Record<'vote_representative(uint64)void' | 'vote_representative', {
      argsObj: VoterArgs['obj']['vote_representative(uint64)void']
      argsTuple: VoterArgs['tuple']['vote_representative(uint64)void']
      returns: VoterReturns['vote_representative(uint64)void']
    }>
    & Record<'vote_direct(uint64,(uint64,uint64))void' | 'vote_direct', {
      argsObj: VoterArgs['obj']['vote_direct(uint64,(uint64,uint64))void']
      argsTuple: VoterArgs['tuple']['vote_direct(uint64,(uint64,uint64))void']
      returns: VoterReturns['vote_direct(uint64,(uint64,uint64))void']
    }>
    & Record<'yield_voting_rights(address)void' | 'yield_voting_rights', {
      argsObj: VoterArgs['obj']['yield_voting_rights(address)void']
      argsTuple: VoterArgs['tuple']['yield_voting_rights(address)void']
      returns: VoterReturns['yield_voting_rights(address)void']
    }>
    & Record<'add_votes(uint64)void' | 'add_votes', {
      argsObj: VoterArgs['obj']['add_votes(uint64)void']
      argsTuple: VoterArgs['tuple']['add_votes(uint64)void']
      returns: VoterReturns['add_votes(uint64)void']
    }>
    & Record<'delete()void' | 'delete', {
      argsObj: VoterArgs['obj']['delete()void']
      argsTuple: VoterArgs['tuple']['delete()void']
      returns: VoterReturns['delete()void']
    }>
  /**
   * Defines the shape of the state of the application.
   */
  state: {
    global: {
      keys: {
        xgovAddress: string
        registryApp: bigint
        representativeApp: bigint
        windowTs: bigint
        votesLeft: bigint
        managerAddress: string
      }
      maps: {}
    }
  }
}

/**
 * Defines the possible abi call signatures.
 */
export type VoterSignatures = keyof VoterTypes['methods']
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the Voter smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends VoterSignatures> = VoterTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the Voter smart contract to the method's return type
 */
export type MethodReturn<TSignature extends VoterSignatures> = VoterTypes['methods'][TSignature]['returns']

/**
 * Defines the shape of the keyed global state of the application.
 */
export type GlobalKeysState = VoterTypes['state']['global']['keys']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the Voter smart contract
 */
export abstract class VoterParamsFactory {
  /**
   * Constructs a no op call for the assign_xgov(address,address)void ABI method
   *
   * Assign the contract to an xGov.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static assignXgov(params: CallParams<VoterArgs['obj']['assign_xgov(address,address)void'] | VoterArgs['tuple']['assign_xgov(address,address)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'assign_xgov(address,address)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.xgovAddress, params.args.managerAddress],
    }
  }
  /**
   * Constructs a no op call for the set_manager(address)void ABI method
   *
   * Set a new manager.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static setManager(params: CallParams<VoterArgs['obj']['set_manager(address)void'] | VoterArgs['tuple']['set_manager(address)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'set_manager(address)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.managerAddress],
    }
  }
  /**
   * Constructs a no op call for the set_representative(uint64)void ABI method
   *
   * Set a new representative.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static setRepresentative(params: CallParams<VoterArgs['obj']['set_representative(uint64)void'] | VoterArgs['tuple']['set_representative(uint64)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'set_representative(uint64)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.representativeId],
    }
  }
  /**
   * Constructs a no op call for the set_window(uint64)void ABI method
   *
   * Set a new window when vote can be cast as representative.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static setWindow(params: CallParams<VoterArgs['obj']['set_window(uint64)void'] | VoterArgs['tuple']['set_window(uint64)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'set_window(uint64)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.windowTs],
    }
  }
  /**
   * Constructs a no op call for the vote_representative(uint64)void ABI method
   *
  * Vote on a proposal according to vote of representative if time is within window.
  MUST BE CALLED BY THE REGISTRY CONTRACT.

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static voteRepresentative(params: CallParams<VoterArgs['obj']['vote_representative(uint64)void'] | VoterArgs['tuple']['vote_representative(uint64)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'vote_representative(uint64)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.proposalId],
    }
  }
  /**
   * Constructs a no op call for the vote_direct(uint64,(uint64,uint64))void ABI method
   *
   * Vote directly on a proposal.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static voteDirect(params: CallParams<VoterArgs['obj']['vote_direct(uint64,(uint64,uint64))void'] | VoterArgs['tuple']['vote_direct(uint64,(uint64,uint64))void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'vote_direct(uint64,(uint64,uint64))void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.proposalId, params.args.vote],
    }
  }
  /**
   * Constructs a no op call for the yield_voting_rights(address)void ABI method
   *
   * Yield voting rights to another account.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static yieldVotingRights(params: CallParams<VoterArgs['obj']['yield_voting_rights(address)void'] | VoterArgs['tuple']['yield_voting_rights(address)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'yield_voting_rights(address)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.votingAddress],
    }
  }
  /**
   * Constructs a no op call for the add_votes(uint64)void ABI method
   *
  * Increase votes left.
  MUST BE CALLED BY THE REGISTRY CONTRACT.

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static addVotes(params: CallParams<VoterArgs['obj']['add_votes(uint64)void'] | VoterArgs['tuple']['add_votes(uint64)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'add_votes(uint64)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.addVotes],
    }
  }
}

/**
 * A client to make calls to the Voter smart contract
 */
export class VoterClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `VoterClient`
   *
   * @param appClient An `AppClient` instance which has been created with the Voter app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `VoterClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Returns a new `VoterClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<VoterClient> {
    return new VoterClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `VoterClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<VoterClient> {
    return new VoterClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Makes a clear_state call to an existing instance of the Voter smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the Voter smart contract using the `assign_xgov(address,address)void` ABI method.
     *
     * Assign the contract to an xGov.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    assignXgov: (params: CallParams<VoterArgs['obj']['assign_xgov(address,address)void'] | VoterArgs['tuple']['assign_xgov(address,address)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(VoterParamsFactory.assignXgov(params))
    },

    /**
     * Makes a call to the Voter smart contract using the `set_manager(address)void` ABI method.
     *
     * Set a new manager.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    setManager: (params: CallParams<VoterArgs['obj']['set_manager(address)void'] | VoterArgs['tuple']['set_manager(address)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(VoterParamsFactory.setManager(params))
    },

    /**
     * Makes a call to the Voter smart contract using the `set_representative(uint64)void` ABI method.
     *
     * Set a new representative.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    setRepresentative: (params: CallParams<VoterArgs['obj']['set_representative(uint64)void'] | VoterArgs['tuple']['set_representative(uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(VoterParamsFactory.setRepresentative(params))
    },

    /**
     * Makes a call to the Voter smart contract using the `set_window(uint64)void` ABI method.
     *
     * Set a new window when vote can be cast as representative.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    setWindow: (params: CallParams<VoterArgs['obj']['set_window(uint64)void'] | VoterArgs['tuple']['set_window(uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(VoterParamsFactory.setWindow(params))
    },

    /**
     * Makes a call to the Voter smart contract using the `vote_representative(uint64)void` ABI method.
     *
    * Vote on a proposal according to vote of representative if time is within window.
    MUST BE CALLED BY THE REGISTRY CONTRACT.

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    voteRepresentative: (params: CallParams<VoterArgs['obj']['vote_representative(uint64)void'] | VoterArgs['tuple']['vote_representative(uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(VoterParamsFactory.voteRepresentative(params))
    },

    /**
     * Makes a call to the Voter smart contract using the `vote_direct(uint64,(uint64,uint64))void` ABI method.
     *
     * Vote directly on a proposal.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    voteDirect: (params: CallParams<VoterArgs['obj']['vote_direct(uint64,(uint64,uint64))void'] | VoterArgs['tuple']['vote_direct(uint64,(uint64,uint64))void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(VoterParamsFactory.voteDirect(params))
    },

    /**
     * Makes a call to the Voter smart contract using the `yield_voting_rights(address)void` ABI method.
     *
     * Yield voting rights to another account.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    yieldVotingRights: (params: CallParams<VoterArgs['obj']['yield_voting_rights(address)void'] | VoterArgs['tuple']['yield_voting_rights(address)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(VoterParamsFactory.yieldVotingRights(params))
    },

    /**
     * Makes a call to the Voter smart contract using the `add_votes(uint64)void` ABI method.
     *
    * Increase votes left.
    MUST BE CALLED BY THE REGISTRY CONTRACT.

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    addVotes: (params: CallParams<VoterArgs['obj']['add_votes(uint64)void'] | VoterArgs['tuple']['add_votes(uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(VoterParamsFactory.addVotes(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Makes a clear_state call to an existing instance of the Voter smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the Voter smart contract using the `assign_xgov(address,address)void` ABI method.
     *
     * Assign the contract to an xGov.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    assignXgov: (params: CallParams<VoterArgs['obj']['assign_xgov(address,address)void'] | VoterArgs['tuple']['assign_xgov(address,address)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(VoterParamsFactory.assignXgov(params))
    },

    /**
     * Makes a call to the Voter smart contract using the `set_manager(address)void` ABI method.
     *
     * Set a new manager.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    setManager: (params: CallParams<VoterArgs['obj']['set_manager(address)void'] | VoterArgs['tuple']['set_manager(address)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(VoterParamsFactory.setManager(params))
    },

    /**
     * Makes a call to the Voter smart contract using the `set_representative(uint64)void` ABI method.
     *
     * Set a new representative.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    setRepresentative: (params: CallParams<VoterArgs['obj']['set_representative(uint64)void'] | VoterArgs['tuple']['set_representative(uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(VoterParamsFactory.setRepresentative(params))
    },

    /**
     * Makes a call to the Voter smart contract using the `set_window(uint64)void` ABI method.
     *
     * Set a new window when vote can be cast as representative.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    setWindow: (params: CallParams<VoterArgs['obj']['set_window(uint64)void'] | VoterArgs['tuple']['set_window(uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(VoterParamsFactory.setWindow(params))
    },

    /**
     * Makes a call to the Voter smart contract using the `vote_representative(uint64)void` ABI method.
     *
    * Vote on a proposal according to vote of representative if time is within window.
    MUST BE CALLED BY THE REGISTRY CONTRACT.

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    voteRepresentative: (params: CallParams<VoterArgs['obj']['vote_representative(uint64)void'] | VoterArgs['tuple']['vote_representative(uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(VoterParamsFactory.voteRepresentative(params))
    },

    /**
     * Makes a call to the Voter smart contract using the `vote_direct(uint64,(uint64,uint64))void` ABI method.
     *
     * Vote directly on a proposal.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    voteDirect: (params: CallParams<VoterArgs['obj']['vote_direct(uint64,(uint64,uint64))void'] | VoterArgs['tuple']['vote_direct(uint64,(uint64,uint64))void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(VoterParamsFactory.voteDirect(params))
    },

    /**
     * Makes a call to the Voter smart contract using the `yield_voting_rights(address)void` ABI method.
     *
     * Yield voting rights to another account.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    yieldVotingRights: (params: CallParams<VoterArgs['obj']['yield_voting_rights(address)void'] | VoterArgs['tuple']['yield_voting_rights(address)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(VoterParamsFactory.yieldVotingRights(params))
    },

    /**
     * Makes a call to the Voter smart contract using the `add_votes(uint64)void` ABI method.
     *
    * Increase votes left.
    MUST BE CALLED BY THE REGISTRY CONTRACT.

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    addVotes: (params: CallParams<VoterArgs['obj']['add_votes(uint64)void'] | VoterArgs['tuple']['add_votes(uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(VoterParamsFactory.addVotes(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Makes a clear_state call to an existing instance of the Voter smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the Voter smart contract using the `assign_xgov(address,address)void` ABI method.
     *
     * Assign the contract to an xGov.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    assignXgov: async (params: CallParams<VoterArgs['obj']['assign_xgov(address,address)void'] | VoterArgs['tuple']['assign_xgov(address,address)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(VoterParamsFactory.assignXgov(params))
      return {...result, return: result.return as unknown as (undefined | VoterReturns['assign_xgov(address,address)void'])}
    },

    /**
     * Makes a call to the Voter smart contract using the `set_manager(address)void` ABI method.
     *
     * Set a new manager.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    setManager: async (params: CallParams<VoterArgs['obj']['set_manager(address)void'] | VoterArgs['tuple']['set_manager(address)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(VoterParamsFactory.setManager(params))
      return {...result, return: result.return as unknown as (undefined | VoterReturns['set_manager(address)void'])}
    },

    /**
     * Makes a call to the Voter smart contract using the `set_representative(uint64)void` ABI method.
     *
     * Set a new representative.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    setRepresentative: async (params: CallParams<VoterArgs['obj']['set_representative(uint64)void'] | VoterArgs['tuple']['set_representative(uint64)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(VoterParamsFactory.setRepresentative(params))
      return {...result, return: result.return as unknown as (undefined | VoterReturns['set_representative(uint64)void'])}
    },

    /**
     * Makes a call to the Voter smart contract using the `set_window(uint64)void` ABI method.
     *
     * Set a new window when vote can be cast as representative.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    setWindow: async (params: CallParams<VoterArgs['obj']['set_window(uint64)void'] | VoterArgs['tuple']['set_window(uint64)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(VoterParamsFactory.setWindow(params))
      return {...result, return: result.return as unknown as (undefined | VoterReturns['set_window(uint64)void'])}
    },

    /**
     * Makes a call to the Voter smart contract using the `vote_representative(uint64)void` ABI method.
     *
    * Vote on a proposal according to vote of representative if time is within window.
    MUST BE CALLED BY THE REGISTRY CONTRACT.

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    voteRepresentative: async (params: CallParams<VoterArgs['obj']['vote_representative(uint64)void'] | VoterArgs['tuple']['vote_representative(uint64)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(VoterParamsFactory.voteRepresentative(params))
      return {...result, return: result.return as unknown as (undefined | VoterReturns['vote_representative(uint64)void'])}
    },

    /**
     * Makes a call to the Voter smart contract using the `vote_direct(uint64,(uint64,uint64))void` ABI method.
     *
     * Vote directly on a proposal.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    voteDirect: async (params: CallParams<VoterArgs['obj']['vote_direct(uint64,(uint64,uint64))void'] | VoterArgs['tuple']['vote_direct(uint64,(uint64,uint64))void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(VoterParamsFactory.voteDirect(params))
      return {...result, return: result.return as unknown as (undefined | VoterReturns['vote_direct(uint64,(uint64,uint64))void'])}
    },

    /**
     * Makes a call to the Voter smart contract using the `yield_voting_rights(address)void` ABI method.
     *
     * Yield voting rights to another account.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    yieldVotingRights: async (params: CallParams<VoterArgs['obj']['yield_voting_rights(address)void'] | VoterArgs['tuple']['yield_voting_rights(address)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(VoterParamsFactory.yieldVotingRights(params))
      return {...result, return: result.return as unknown as (undefined | VoterReturns['yield_voting_rights(address)void'])}
    },

    /**
     * Makes a call to the Voter smart contract using the `add_votes(uint64)void` ABI method.
     *
    * Increase votes left.
    MUST BE CALLED BY THE REGISTRY CONTRACT.

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    addVotes: async (params: CallParams<VoterArgs['obj']['add_votes(uint64)void'] | VoterArgs['tuple']['add_votes(uint64)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(VoterParamsFactory.addVotes(params))
      return {...result, return: result.return as unknown as (undefined | VoterReturns['add_votes(uint64)void'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new VoterClient(this.appClient.clone(params))
  }

  /**
   * Methods to access state for the current Voter app
   */
  state = {
    /**
     * Methods to access global state for the current Voter app
     */
    global: {
      /**
       * Get all current keyed values from global state
       */
      getAll: async (): Promise<Partial<Expand<GlobalKeysState>>> => {
        const result = await this.appClient.state.global.getAll()
        return {
          xgovAddress: result.xgov_address,
          registryApp: result.registry_app,
          representativeApp: result.representative_app,
          windowTs: result.window_ts,
          votesLeft: result.votes_left,
          managerAddress: result.manager_address,
        }
      },
      /**
       * Get the current value of the xgov_address key in global state
       */
      xgovAddress: async (): Promise<string | undefined> => { return (await this.appClient.state.global.getValue("xgov_address")) as string | undefined },
      /**
       * Get the current value of the registry_app key in global state
       */
      registryApp: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("registry_app")) as bigint | undefined },
      /**
       * Get the current value of the representative_app key in global state
       */
      representativeApp: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("representative_app")) as bigint | undefined },
      /**
       * Get the current value of the window_ts key in global state
       */
      windowTs: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("window_ts")) as bigint | undefined },
      /**
       * Get the current value of the votes_left key in global state
       */
      votesLeft: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("votes_left")) as bigint | undefined },
      /**
       * Get the current value of the manager_address key in global state
       */
      managerAddress: async (): Promise<string | undefined> => { return (await this.appClient.state.global.getValue("manager_address")) as string | undefined },
    },
  }

  public newGroup(): VoterComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a assign_xgov(address,address)void method call against the Voter contract
       */
      assignXgov(params: CallParams<VoterArgs['obj']['assign_xgov(address,address)void'] | VoterArgs['tuple']['assign_xgov(address,address)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.assignXgov(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a set_manager(address)void method call against the Voter contract
       */
      setManager(params: CallParams<VoterArgs['obj']['set_manager(address)void'] | VoterArgs['tuple']['set_manager(address)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.setManager(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a set_representative(uint64)void method call against the Voter contract
       */
      setRepresentative(params: CallParams<VoterArgs['obj']['set_representative(uint64)void'] | VoterArgs['tuple']['set_representative(uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.setRepresentative(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a set_window(uint64)void method call against the Voter contract
       */
      setWindow(params: CallParams<VoterArgs['obj']['set_window(uint64)void'] | VoterArgs['tuple']['set_window(uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.setWindow(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a vote_representative(uint64)void method call against the Voter contract
       */
      voteRepresentative(params: CallParams<VoterArgs['obj']['vote_representative(uint64)void'] | VoterArgs['tuple']['vote_representative(uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.voteRepresentative(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a vote_direct(uint64,(uint64,uint64))void method call against the Voter contract
       */
      voteDirect(params: CallParams<VoterArgs['obj']['vote_direct(uint64,(uint64,uint64))void'] | VoterArgs['tuple']['vote_direct(uint64,(uint64,uint64))void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.voteDirect(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a yield_voting_rights(address)void method call against the Voter contract
       */
      yieldVotingRights(params: CallParams<VoterArgs['obj']['yield_voting_rights(address)void'] | VoterArgs['tuple']['yield_voting_rights(address)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.yieldVotingRights(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a add_votes(uint64)void method call against the Voter contract
       */
      addVotes(params: CallParams<VoterArgs['obj']['add_votes(uint64)void'] | VoterArgs['tuple']['add_votes(uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.addVotes(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a clear state call to the Voter contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as VoterComposer
  }
}
export type VoterComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the assign_xgov(address,address)void ABI method.
   *
   * Assign the contract to an xGov.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  assignXgov(params?: CallParams<VoterArgs['obj']['assign_xgov(address,address)void'] | VoterArgs['tuple']['assign_xgov(address,address)void']>): VoterComposer<[...TReturns, VoterReturns['assign_xgov(address,address)void'] | undefined]>

  /**
   * Calls the set_manager(address)void ABI method.
   *
   * Set a new manager.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  setManager(params?: CallParams<VoterArgs['obj']['set_manager(address)void'] | VoterArgs['tuple']['set_manager(address)void']>): VoterComposer<[...TReturns, VoterReturns['set_manager(address)void'] | undefined]>

  /**
   * Calls the set_representative(uint64)void ABI method.
   *
   * Set a new representative.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  setRepresentative(params?: CallParams<VoterArgs['obj']['set_representative(uint64)void'] | VoterArgs['tuple']['set_representative(uint64)void']>): VoterComposer<[...TReturns, VoterReturns['set_representative(uint64)void'] | undefined]>

  /**
   * Calls the set_window(uint64)void ABI method.
   *
   * Set a new window when vote can be cast as representative.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  setWindow(params?: CallParams<VoterArgs['obj']['set_window(uint64)void'] | VoterArgs['tuple']['set_window(uint64)void']>): VoterComposer<[...TReturns, VoterReturns['set_window(uint64)void'] | undefined]>

  /**
   * Calls the vote_representative(uint64)void ABI method.
   *
  * Vote on a proposal according to vote of representative if time is within window.
  MUST BE CALLED BY THE REGISTRY CONTRACT.

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  voteRepresentative(params?: CallParams<VoterArgs['obj']['vote_representative(uint64)void'] | VoterArgs['tuple']['vote_representative(uint64)void']>): VoterComposer<[...TReturns, VoterReturns['vote_representative(uint64)void'] | undefined]>

  /**
   * Calls the vote_direct(uint64,(uint64,uint64))void ABI method.
   *
   * Vote directly on a proposal.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  voteDirect(params?: CallParams<VoterArgs['obj']['vote_direct(uint64,(uint64,uint64))void'] | VoterArgs['tuple']['vote_direct(uint64,(uint64,uint64))void']>): VoterComposer<[...TReturns, VoterReturns['vote_direct(uint64,(uint64,uint64))void'] | undefined]>

  /**
   * Calls the yield_voting_rights(address)void ABI method.
   *
   * Yield voting rights to another account.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  yieldVotingRights(params?: CallParams<VoterArgs['obj']['yield_voting_rights(address)void'] | VoterArgs['tuple']['yield_voting_rights(address)void']>): VoterComposer<[...TReturns, VoterReturns['yield_voting_rights(address)void'] | undefined]>

  /**
   * Calls the add_votes(uint64)void ABI method.
   *
  * Increase votes left.
  MUST BE CALLED BY THE REGISTRY CONTRACT.

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  addVotes(params?: CallParams<VoterArgs['obj']['add_votes(uint64)void'] | VoterArgs['tuple']['add_votes(uint64)void']>): VoterComposer<[...TReturns, VoterReturns['add_votes(uint64)void'] | undefined]>

  /**
   * Makes a clear_state call to an existing instance of the Voter smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): VoterComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): VoterComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<VoterComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<VoterComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<VoterComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<VoterComposerResults<TReturns>>
}
export type VoterComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

