/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"Algochi","structs":{},"methods":[{"name":"bootstrap","args":[{"type":"uint64","name":"beaconAppId"}],"returns":{"type":"uint64"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Bootstraps the contract by creating the reward ASA and setting the beacon app ID.\nMust be called once before any other operations.","events":[],"recommendations":{}},{"name":"createPet","args":[{"type":"pay","name":"payment"}],"returns":{"type":"uint64"},"actions":{"create":[],"call":["OptIn"]},"readonly":false,"desc":"Creates a new pet and mints its corresponding NFT.\nRequires payment transaction and initializes pet state with default values.","events":[],"recommendations":{}},{"name":"feed","args":[],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Feed the pet to increase hunger and earn health-scaled $TAMA rewards.\nCommits to a future VRF round for the follow-up play() action.","events":[],"recommendations":{}},{"name":"play","args":[],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Play with the pet to boost happiness using VRF randomness.\nRequires a VRF commitment from a previous feed() call and distributes health-scaled rewards.","events":[],"recommendations":{}},{"name":"evolve","args":[],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Evolve the pet to the next stage if age requirements are met.\nBurns $TAMA and applies a happiness boost.","events":[],"recommendations":{}},{"name":"revive","args":[],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Revive a dead pet by burning $TAMA and resetting stats to baseline values.","events":[],"recommendations":{}},{"name":"finalizeTraits","args":[],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Finalize pet traits using VRF randomness from the beacon or fallback hash.\nCan only be called once per pet after the VRF round delay has passed.","events":[],"recommendations":{}},{"name":"vrfTest","args":[{"type":"uint64","name":"committedRound"}],"returns":{"type":"(byte[],bool)"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Test method for VRF beacon functionality.","events":[],"recommendations":{}}],"arcs":[22,28],"desc":"Algochi - Tamagotchi-style NFT pet on Algorand\n\nA Tamagotchi-inspired digital pet game where players care for their pets,\nearning $TAMA tokens through feeding and playing, with 3-stage evolutions\nand on-chain traits determined by secure VRF.","networks":{},"state":{"schema":{"global":{"ints":21,"bytes":0},"local":{"ints":13,"bytes":0}},"keys":{"global":{"totalPets":{"keyType":"AVMString","valueType":"AVMUint64","key":"dG90YWxQZXRz"},"totalRewardsMined":{"keyType":"AVMString","valueType":"AVMUint64","key":"dG90YWxSZXdhcmRzTWluZWQ="},"totalRewardsBurned":{"keyType":"AVMString","valueType":"AVMUint64","key":"dG90YWxSZXdhcmRzQnVybmVk"},"rewardAsaId":{"keyType":"AVMString","valueType":"AVMUint64","key":"cmV3YXJkQXNhSWQ="},"beaconAppId":{"keyType":"AVMString","valueType":"AVMUint64","key":"YmVhY29uQXBwSWQ="},"randomnessGapRounds":{"keyType":"AVMString","valueType":"AVMUint64","key":"cmFuZG9tbmVzc0dhcFJvdW5kcw=="},"randomDelay":{"keyType":"AVMString","valueType":"AVMUint64","key":"cmFuZG9tRGVsYXk="},"maxStat":{"keyType":"AVMString","valueType":"AVMUint64","key":"bWF4U3RhdA=="},"ageSecondsPerDay":{"keyType":"AVMString","valueType":"AVMUint64","key":"YWdlU2Vjb25kc1BlckRheQ=="},"isBootstrapped":{"keyType":"AVMString","valueType":"AVMUint64","key":"aXNCb290c3RyYXBwZWQ="},"hungerIncrease":{"keyType":"AVMString","valueType":"AVMUint64","key":"aHVuZ2VySW5jcmVhc2U="},"petHungerCost":{"keyType":"AVMString","valueType":"AVMUint64","key":"cGV0SHVuZ2VyQ29zdA=="},"hungerDecayNumerator":{"keyType":"AVMString","valueType":"AVMUint64","key":"aHVuZ2VyRGVjYXlOdW1lcmF0b3I="},"hungerDecayDenominator":{"keyType":"AVMString","valueType":"AVMUint64","key":"aHVuZ2VyRGVjYXlEZW5vbWluYXRvcg=="},"happinessDecayNumerator":{"keyType":"AVMString","valueType":"AVMUint64","key":"aGFwcGluZXNzRGVjYXlOdW1lcmF0b3I="},"happinessDecayDenominator":{"keyType":"AVMString","valueType":"AVMUint64","key":"aGFwcGluZXNzRGVjYXlEZW5vbWluYXRvcg=="},"evolveCost":{"keyType":"AVMString","valueType":"AVMUint64","key":"ZXZvbHZlQ29zdA=="},"reviveCost":{"keyType":"AVMString","valueType":"AVMUint64","key":"cmV2aXZlQ29zdA=="},"stageRewardBaby":{"keyType":"AVMString","valueType":"AVMUint64","key":"c3RhZ2VSZXdhcmRCYWJ5"},"stageRewardTeen":{"keyType":"AVMString","valueType":"AVMUint64","key":"c3RhZ2VSZXdhcmRUZWVu"},"stageRewardAdult":{"keyType":"AVMString","valueType":"AVMUint64","key":"c3RhZ2VSZXdhcmRBZHVsdA=="}},"local":{"assetId":{"keyType":"AVMString","valueType":"AVMUint64","key":"YXNzZXRJZA=="},"hunger":{"keyType":"AVMString","valueType":"AVMUint64","key":"aHVuZ2Vy"},"happiness":{"keyType":"AVMString","valueType":"AVMUint64","key":"aGFwcGluZXNz"},"ageDays":{"keyType":"AVMString","valueType":"AVMUint64","key":"YWdlRGF5cw=="},"stage":{"keyType":"AVMString","valueType":"AVMUint64","key":"c3RhZ2U="},"lastUpdate":{"keyType":"AVMString","valueType":"AVMUint64","key":"bGFzdFVwZGF0ZQ=="},"isDead":{"keyType":"AVMString","valueType":"AVMUint64","key":"aXNEZWFk"},"committedRound":{"keyType":"AVMString","valueType":"AVMUint64","key":"Y29tbWl0dGVkUm91bmQ="},"feedHungerSnapshot":{"keyType":"AVMString","valueType":"AVMUint64","key":"ZmVlZEh1bmdlclNuYXBzaG90"},"traitColor":{"keyType":"AVMString","valueType":"AVMUint64","key":"dHJhaXRDb2xvcg=="},"traitPattern":{"keyType":"AVMString","valueType":"AVMUint64","key":"dHJhaXRQYXR0ZXJu"},"traitAccessory":{"keyType":"AVMString","valueType":"AVMUint64","key":"dHJhaXRBY2Nlc3Nvcnk="},"traitsFinalized":{"keyType":"AVMString","valueType":"AVMUint64","key":"dHJhaXRzRmluYWxpemVk"}},"box":{}},"maps":{"global":{},"local":{},"box":{}}},"bareActions":{"create":["NoOp"],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[788],"errorMessage":"Already bootstrapped"},{"pc":[1556],"errorMessage":"Beacon app not configured"},{"pc":[1612,1984],"errorMessage":"Bytes has valid prefix"},{"pc":[2216],"errorMessage":"Insufficient $TAMA"},{"pc":[907],"errorMessage":"Invalid payment transaction"},{"pc":[1877],"errorMessage":"No pending VRF commitment"},{"pc":[1351],"errorMessage":"Not ready to evolve"},{"pc":[655],"errorMessage":"OnCompletion must be NoOp"},{"pc":[734],"errorMessage":"OnCompletion must be NoOp && can only call when creating"},{"pc":[723],"errorMessage":"OnCompletion must be OptIn && can only call when not creating"},{"pc":[1669],"errorMessage":"Pet is dead"},{"pc":[1418],"errorMessage":"Pet is not dead"},{"pc":[1410,1461,1661],"errorMessage":"Pet not created"},{"pc":[1889],"errorMessage":"Randomness not ready - delay not satisfied"},{"pc":[2133,2198],"errorMessage":"Reward ASA not configured"},{"pc":[1469],"errorMessage":"Traits already finalized"},{"pc":[786,912,1100,1105,1206,1228,1237,1356,1423,1718,1729,1740,1746,1809,1884,1897,1937,2054,2116,2127,2157,2175,2183,2246],"errorMessage":"check GlobalState exists"},{"pc":[1095,1159,1198,1223,1318,1326,1344,1415,1467,1478,1667,1682,1761,1770,1820,1842,1875,1921,2024,2030,2046,2108],"errorMessage":"check LocalState exists"},{"pc":[780,1551],"errorMessage":"invalid number of bytes for arc4.uint64"},{"pc":[883],"errorMessage":"transaction type is pay"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"events":[]} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


/**
 * The argument types for the Algochi contract
 */
export type AlgochiArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'bootstrap(uint64)uint64': {
      beaconAppId: bigint | number
    }
    'createPet(pay)uint64': {
      payment: AppMethodCallTransactionArgument
    }
    'feed()void': Record<string, never>
    'play()void': Record<string, never>
    'evolve()void': Record<string, never>
    'revive()void': Record<string, never>
    'finalizeTraits()void': Record<string, never>
    'vrfTest(uint64)(byte[],bool)': {
      committedRound: bigint | number
    }
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'bootstrap(uint64)uint64': [beaconAppId: bigint | number]
    'createPet(pay)uint64': [payment: AppMethodCallTransactionArgument]
    'feed()void': []
    'play()void': []
    'evolve()void': []
    'revive()void': []
    'finalizeTraits()void': []
    'vrfTest(uint64)(byte[],bool)': [committedRound: bigint | number]
  }
}

/**
 * The return type for each method
 */
export type AlgochiReturns = {
  'bootstrap(uint64)uint64': bigint
  'createPet(pay)uint64': bigint
  'feed()void': void
  'play()void': void
  'evolve()void': void
  'revive()void': void
  'finalizeTraits()void': void
  'vrfTest(uint64)(byte[],bool)': [Uint8Array, boolean]
}

/**
 * Defines the types of available calls and state of the Algochi smart contract.
 */
export type AlgochiTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'bootstrap(uint64)uint64' | 'bootstrap', {
      argsObj: AlgochiArgs['obj']['bootstrap(uint64)uint64']
      argsTuple: AlgochiArgs['tuple']['bootstrap(uint64)uint64']
      returns: AlgochiReturns['bootstrap(uint64)uint64']
    }>
    & Record<'createPet(pay)uint64' | 'createPet', {
      argsObj: AlgochiArgs['obj']['createPet(pay)uint64']
      argsTuple: AlgochiArgs['tuple']['createPet(pay)uint64']
      returns: AlgochiReturns['createPet(pay)uint64']
    }>
    & Record<'feed()void' | 'feed', {
      argsObj: AlgochiArgs['obj']['feed()void']
      argsTuple: AlgochiArgs['tuple']['feed()void']
      returns: AlgochiReturns['feed()void']
    }>
    & Record<'play()void' | 'play', {
      argsObj: AlgochiArgs['obj']['play()void']
      argsTuple: AlgochiArgs['tuple']['play()void']
      returns: AlgochiReturns['play()void']
    }>
    & Record<'evolve()void' | 'evolve', {
      argsObj: AlgochiArgs['obj']['evolve()void']
      argsTuple: AlgochiArgs['tuple']['evolve()void']
      returns: AlgochiReturns['evolve()void']
    }>
    & Record<'revive()void' | 'revive', {
      argsObj: AlgochiArgs['obj']['revive()void']
      argsTuple: AlgochiArgs['tuple']['revive()void']
      returns: AlgochiReturns['revive()void']
    }>
    & Record<'finalizeTraits()void' | 'finalizeTraits', {
      argsObj: AlgochiArgs['obj']['finalizeTraits()void']
      argsTuple: AlgochiArgs['tuple']['finalizeTraits()void']
      returns: AlgochiReturns['finalizeTraits()void']
    }>
    & Record<'vrfTest(uint64)(byte[],bool)' | 'vrfTest', {
      argsObj: AlgochiArgs['obj']['vrfTest(uint64)(byte[],bool)']
      argsTuple: AlgochiArgs['tuple']['vrfTest(uint64)(byte[],bool)']
      returns: AlgochiReturns['vrfTest(uint64)(byte[],bool)']
    }>
  /**
   * Defines the shape of the state of the application.
   */
  state: {
    global: {
      keys: {
        totalPets: bigint
        totalRewardsMined: bigint
        totalRewardsBurned: bigint
        rewardAsaId: bigint
        beaconAppId: bigint
        randomnessGapRounds: bigint
        randomDelay: bigint
        maxStat: bigint
        ageSecondsPerDay: bigint
        isBootstrapped: bigint
        hungerIncrease: bigint
        petHungerCost: bigint
        hungerDecayNumerator: bigint
        hungerDecayDenominator: bigint
        happinessDecayNumerator: bigint
        happinessDecayDenominator: bigint
        evolveCost: bigint
        reviveCost: bigint
        stageRewardBaby: bigint
        stageRewardTeen: bigint
        stageRewardAdult: bigint
      }
      maps: {}
    }
    local: {
      keys: {
        assetId: bigint
        hunger: bigint
        happiness: bigint
        ageDays: bigint
        stage: bigint
        lastUpdate: bigint
        isDead: bigint
        committedRound: bigint
        feedHungerSnapshot: bigint
        traitColor: bigint
        traitPattern: bigint
        traitAccessory: bigint
        traitsFinalized: bigint
      }
      maps: {}
    }
  }
}

/**
 * Defines the possible abi call signatures.
 */
export type AlgochiSignatures = keyof AlgochiTypes['methods']
/**
 * Defines the possible abi call signatures for methods that return a non-void value.
 */
export type AlgochiNonVoidMethodSignatures = keyof AlgochiTypes['methods'] extends infer T ? T extends keyof AlgochiTypes['methods'] ? MethodReturn<T> extends void ? never : T  : never : never
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the Algochi smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends AlgochiSignatures> = AlgochiTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the Algochi smart contract to the method's return type
 */
export type MethodReturn<TSignature extends AlgochiSignatures> = AlgochiTypes['methods'][TSignature]['returns']

/**
 * Defines the shape of the keyed global state of the application.
 */
export type GlobalKeysState = AlgochiTypes['state']['global']['keys']

/**
 * Defines the shape of the keyed local state of the application.
 */
export type LocalKeysState = AlgochiTypes['state']['local']['keys']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the Algochi smart contract
 */
export abstract class AlgochiParamsFactory {
  /**
   * Gets available optIn ABI call param factories
   */
  static get optIn() {
    return {
      /**
       * Constructs opt-in ABI call params for the Algochi smart contract using the createPet(pay)uint64 ABI method
       *
       * @param params Parameters for the call
       * @returns An `AppClientMethodCallParams` object for the call
       */
      createPet(params: CallParams<AlgochiArgs['obj']['createPet(pay)uint64'] | AlgochiArgs['tuple']['createPet(pay)uint64']>): AppClientMethodCallParams {
        return {
          ...params,
          method: 'createPet(pay)uint64' as const,
          args: Array.isArray(params.args) ? params.args : [params.args.payment],
        }
      },
    }
  }

  /**
   * Constructs a no op call for the bootstrap(uint64)uint64 ABI method
   *
  * Bootstraps the contract by creating the reward ASA and setting the beacon app ID.
  Must be called once before any other operations.

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static bootstrap(params: CallParams<AlgochiArgs['obj']['bootstrap(uint64)uint64'] | AlgochiArgs['tuple']['bootstrap(uint64)uint64']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'bootstrap(uint64)uint64' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.beaconAppId],
    }
  }
  /**
   * Constructs a no op call for the feed()void ABI method
   *
  * Feed the pet to increase hunger and earn health-scaled $TAMA rewards.
  Commits to a future VRF round for the follow-up play() action.

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static feed(params: CallParams<AlgochiArgs['obj']['feed()void'] | AlgochiArgs['tuple']['feed()void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'feed()void' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the play()void ABI method
   *
  * Play with the pet to boost happiness using VRF randomness.
  Requires a VRF commitment from a previous feed() call and distributes health-scaled rewards.

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static play(params: CallParams<AlgochiArgs['obj']['play()void'] | AlgochiArgs['tuple']['play()void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'play()void' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the evolve()void ABI method
   *
  * Evolve the pet to the next stage if age requirements are met.
  Burns $TAMA and applies a happiness boost.

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static evolve(params: CallParams<AlgochiArgs['obj']['evolve()void'] | AlgochiArgs['tuple']['evolve()void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'evolve()void' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the revive()void ABI method
   *
   * Revive a dead pet by burning $TAMA and resetting stats to baseline values.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static revive(params: CallParams<AlgochiArgs['obj']['revive()void'] | AlgochiArgs['tuple']['revive()void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'revive()void' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the finalizeTraits()void ABI method
   *
  * Finalize pet traits using VRF randomness from the beacon or fallback hash.
  Can only be called once per pet after the VRF round delay has passed.

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static finalizeTraits(params: CallParams<AlgochiArgs['obj']['finalizeTraits()void'] | AlgochiArgs['tuple']['finalizeTraits()void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'finalizeTraits()void' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the vrfTest(uint64)(byte[],bool) ABI method
   *
   * Test method for VRF beacon functionality.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static vrfTest(params: CallParams<AlgochiArgs['obj']['vrfTest(uint64)(byte[],bool)'] | AlgochiArgs['tuple']['vrfTest(uint64)(byte[],bool)']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'vrfTest(uint64)(byte[],bool)' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.committedRound],
    }
  }
}

/**
 * A client to make calls to the Algochi smart contract
 */
export class AlgochiClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `AlgochiClient`
   *
   * @param appClient An `AppClient` instance which has been created with the Algochi app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `AlgochiClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Checks for decode errors on the given return value and maps the return value to the return type for the given method
   * @returns The typed return value or undefined if there was no value
   */
  decodeReturnValue<TSignature extends AlgochiNonVoidMethodSignatures>(method: TSignature, returnValue: ABIReturn | undefined) {
    return returnValue !== undefined ? getArc56ReturnValue<MethodReturn<TSignature>>(returnValue, this.appClient.getABIMethod(method), APP_SPEC.structs) : undefined
  }

  /**
   * Returns a new `AlgochiClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<AlgochiClient> {
    return new AlgochiClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `AlgochiClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<AlgochiClient> {
    return new AlgochiClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Gets available optIn methods
     */
    optIn: {
      /**
       * Opts the user into an existing instance of the Algochi smart contract using the `createPet(pay)uint64` ABI method.
       *
      * Creates a new pet and mints its corresponding NFT.
      Requires payment transaction and initializes pet state with default values.

       *
       * @param params The params for the smart contract call
       * @returns The optIn params
       */
      createPet: (params: CallParams<AlgochiArgs['obj']['createPet(pay)uint64'] | AlgochiArgs['tuple']['createPet(pay)uint64']>) => {
        return this.appClient.params.optIn(AlgochiParamsFactory.optIn.createPet(params))
      },

    },

    /**
     * Makes a clear_state call to an existing instance of the Algochi smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the Algochi smart contract using the `bootstrap(uint64)uint64` ABI method.
     *
    * Bootstraps the contract by creating the reward ASA and setting the beacon app ID.
    Must be called once before any other operations.

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    bootstrap: (params: CallParams<AlgochiArgs['obj']['bootstrap(uint64)uint64'] | AlgochiArgs['tuple']['bootstrap(uint64)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(AlgochiParamsFactory.bootstrap(params))
    },

    /**
     * Makes a call to the Algochi smart contract using the `feed()void` ABI method.
     *
    * Feed the pet to increase hunger and earn health-scaled $TAMA rewards.
    Commits to a future VRF round for the follow-up play() action.

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    feed: (params: CallParams<AlgochiArgs['obj']['feed()void'] | AlgochiArgs['tuple']['feed()void']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(AlgochiParamsFactory.feed(params))
    },

    /**
     * Makes a call to the Algochi smart contract using the `play()void` ABI method.
     *
    * Play with the pet to boost happiness using VRF randomness.
    Requires a VRF commitment from a previous feed() call and distributes health-scaled rewards.

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    play: (params: CallParams<AlgochiArgs['obj']['play()void'] | AlgochiArgs['tuple']['play()void']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(AlgochiParamsFactory.play(params))
    },

    /**
     * Makes a call to the Algochi smart contract using the `evolve()void` ABI method.
     *
    * Evolve the pet to the next stage if age requirements are met.
    Burns $TAMA and applies a happiness boost.

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    evolve: (params: CallParams<AlgochiArgs['obj']['evolve()void'] | AlgochiArgs['tuple']['evolve()void']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(AlgochiParamsFactory.evolve(params))
    },

    /**
     * Makes a call to the Algochi smart contract using the `revive()void` ABI method.
     *
     * Revive a dead pet by burning $TAMA and resetting stats to baseline values.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    revive: (params: CallParams<AlgochiArgs['obj']['revive()void'] | AlgochiArgs['tuple']['revive()void']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(AlgochiParamsFactory.revive(params))
    },

    /**
     * Makes a call to the Algochi smart contract using the `finalizeTraits()void` ABI method.
     *
    * Finalize pet traits using VRF randomness from the beacon or fallback hash.
    Can only be called once per pet after the VRF round delay has passed.

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    finalizeTraits: (params: CallParams<AlgochiArgs['obj']['finalizeTraits()void'] | AlgochiArgs['tuple']['finalizeTraits()void']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(AlgochiParamsFactory.finalizeTraits(params))
    },

    /**
     * Makes a call to the Algochi smart contract using the `vrfTest(uint64)(byte[],bool)` ABI method.
     *
     * Test method for VRF beacon functionality.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    vrfTest: (params: CallParams<AlgochiArgs['obj']['vrfTest(uint64)(byte[],bool)'] | AlgochiArgs['tuple']['vrfTest(uint64)(byte[],bool)']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(AlgochiParamsFactory.vrfTest(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Gets available optIn methods
     */
    optIn: {
      /**
       * Opts the user into an existing instance of the Algochi smart contract using the `createPet(pay)uint64` ABI method.
       *
      * Creates a new pet and mints its corresponding NFT.
      Requires payment transaction and initializes pet state with default values.

       *
       * @param params The params for the smart contract call
       * @returns The optIn transaction
       */
      createPet: (params: CallParams<AlgochiArgs['obj']['createPet(pay)uint64'] | AlgochiArgs['tuple']['createPet(pay)uint64']>) => {
        return this.appClient.createTransaction.optIn(AlgochiParamsFactory.optIn.createPet(params))
      },

    },

    /**
     * Makes a clear_state call to an existing instance of the Algochi smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the Algochi smart contract using the `bootstrap(uint64)uint64` ABI method.
     *
    * Bootstraps the contract by creating the reward ASA and setting the beacon app ID.
    Must be called once before any other operations.

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    bootstrap: (params: CallParams<AlgochiArgs['obj']['bootstrap(uint64)uint64'] | AlgochiArgs['tuple']['bootstrap(uint64)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(AlgochiParamsFactory.bootstrap(params))
    },

    /**
     * Makes a call to the Algochi smart contract using the `feed()void` ABI method.
     *
    * Feed the pet to increase hunger and earn health-scaled $TAMA rewards.
    Commits to a future VRF round for the follow-up play() action.

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    feed: (params: CallParams<AlgochiArgs['obj']['feed()void'] | AlgochiArgs['tuple']['feed()void']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(AlgochiParamsFactory.feed(params))
    },

    /**
     * Makes a call to the Algochi smart contract using the `play()void` ABI method.
     *
    * Play with the pet to boost happiness using VRF randomness.
    Requires a VRF commitment from a previous feed() call and distributes health-scaled rewards.

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    play: (params: CallParams<AlgochiArgs['obj']['play()void'] | AlgochiArgs['tuple']['play()void']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(AlgochiParamsFactory.play(params))
    },

    /**
     * Makes a call to the Algochi smart contract using the `evolve()void` ABI method.
     *
    * Evolve the pet to the next stage if age requirements are met.
    Burns $TAMA and applies a happiness boost.

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    evolve: (params: CallParams<AlgochiArgs['obj']['evolve()void'] | AlgochiArgs['tuple']['evolve()void']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(AlgochiParamsFactory.evolve(params))
    },

    /**
     * Makes a call to the Algochi smart contract using the `revive()void` ABI method.
     *
     * Revive a dead pet by burning $TAMA and resetting stats to baseline values.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    revive: (params: CallParams<AlgochiArgs['obj']['revive()void'] | AlgochiArgs['tuple']['revive()void']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(AlgochiParamsFactory.revive(params))
    },

    /**
     * Makes a call to the Algochi smart contract using the `finalizeTraits()void` ABI method.
     *
    * Finalize pet traits using VRF randomness from the beacon or fallback hash.
    Can only be called once per pet after the VRF round delay has passed.

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    finalizeTraits: (params: CallParams<AlgochiArgs['obj']['finalizeTraits()void'] | AlgochiArgs['tuple']['finalizeTraits()void']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(AlgochiParamsFactory.finalizeTraits(params))
    },

    /**
     * Makes a call to the Algochi smart contract using the `vrfTest(uint64)(byte[],bool)` ABI method.
     *
     * Test method for VRF beacon functionality.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    vrfTest: (params: CallParams<AlgochiArgs['obj']['vrfTest(uint64)(byte[],bool)'] | AlgochiArgs['tuple']['vrfTest(uint64)(byte[],bool)']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(AlgochiParamsFactory.vrfTest(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Gets available optIn methods
     */
    optIn: {
      /**
       * Opts the user into an existing instance of the Algochi smart contract using the `createPet(pay)uint64` ABI method.
       *
      * Creates a new pet and mints its corresponding NFT.
      Requires payment transaction and initializes pet state with default values.

       *
       * @param params The params for the smart contract call
       * @returns The optIn result
       */
      createPet: async (params: CallParams<AlgochiArgs['obj']['createPet(pay)uint64'] | AlgochiArgs['tuple']['createPet(pay)uint64']> & SendParams) => {
        const result = await this.appClient.send.optIn(AlgochiParamsFactory.optIn.createPet(params))
        return {...result, return: result.return as unknown as (undefined | AlgochiReturns['createPet(pay)uint64'])}
      },

    },

    /**
     * Makes a clear_state call to an existing instance of the Algochi smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the Algochi smart contract using the `bootstrap(uint64)uint64` ABI method.
     *
    * Bootstraps the contract by creating the reward ASA and setting the beacon app ID.
    Must be called once before any other operations.

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    bootstrap: async (params: CallParams<AlgochiArgs['obj']['bootstrap(uint64)uint64'] | AlgochiArgs['tuple']['bootstrap(uint64)uint64']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(AlgochiParamsFactory.bootstrap(params))
      return {...result, return: result.return as unknown as (undefined | AlgochiReturns['bootstrap(uint64)uint64'])}
    },

    /**
     * Makes a call to the Algochi smart contract using the `feed()void` ABI method.
     *
    * Feed the pet to increase hunger and earn health-scaled $TAMA rewards.
    Commits to a future VRF round for the follow-up play() action.

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    feed: async (params: CallParams<AlgochiArgs['obj']['feed()void'] | AlgochiArgs['tuple']['feed()void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(AlgochiParamsFactory.feed(params))
      return {...result, return: result.return as unknown as (undefined | AlgochiReturns['feed()void'])}
    },

    /**
     * Makes a call to the Algochi smart contract using the `play()void` ABI method.
     *
    * Play with the pet to boost happiness using VRF randomness.
    Requires a VRF commitment from a previous feed() call and distributes health-scaled rewards.

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    play: async (params: CallParams<AlgochiArgs['obj']['play()void'] | AlgochiArgs['tuple']['play()void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(AlgochiParamsFactory.play(params))
      return {...result, return: result.return as unknown as (undefined | AlgochiReturns['play()void'])}
    },

    /**
     * Makes a call to the Algochi smart contract using the `evolve()void` ABI method.
     *
    * Evolve the pet to the next stage if age requirements are met.
    Burns $TAMA and applies a happiness boost.

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    evolve: async (params: CallParams<AlgochiArgs['obj']['evolve()void'] | AlgochiArgs['tuple']['evolve()void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(AlgochiParamsFactory.evolve(params))
      return {...result, return: result.return as unknown as (undefined | AlgochiReturns['evolve()void'])}
    },

    /**
     * Makes a call to the Algochi smart contract using the `revive()void` ABI method.
     *
     * Revive a dead pet by burning $TAMA and resetting stats to baseline values.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    revive: async (params: CallParams<AlgochiArgs['obj']['revive()void'] | AlgochiArgs['tuple']['revive()void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(AlgochiParamsFactory.revive(params))
      return {...result, return: result.return as unknown as (undefined | AlgochiReturns['revive()void'])}
    },

    /**
     * Makes a call to the Algochi smart contract using the `finalizeTraits()void` ABI method.
     *
    * Finalize pet traits using VRF randomness from the beacon or fallback hash.
    Can only be called once per pet after the VRF round delay has passed.

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    finalizeTraits: async (params: CallParams<AlgochiArgs['obj']['finalizeTraits()void'] | AlgochiArgs['tuple']['finalizeTraits()void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(AlgochiParamsFactory.finalizeTraits(params))
      return {...result, return: result.return as unknown as (undefined | AlgochiReturns['finalizeTraits()void'])}
    },

    /**
     * Makes a call to the Algochi smart contract using the `vrfTest(uint64)(byte[],bool)` ABI method.
     *
     * Test method for VRF beacon functionality.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    vrfTest: async (params: CallParams<AlgochiArgs['obj']['vrfTest(uint64)(byte[],bool)'] | AlgochiArgs['tuple']['vrfTest(uint64)(byte[],bool)']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(AlgochiParamsFactory.vrfTest(params))
      return {...result, return: result.return as unknown as (undefined | AlgochiReturns['vrfTest(uint64)(byte[],bool)'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new AlgochiClient(this.appClient.clone(params))
  }

  /**
   * Methods to access state for the current Algochi app
   */
  state = {
    /**
     * Methods to access global state for the current Algochi app
     */
    global: {
      /**
       * Get all current keyed values from global state
       */
      getAll: async (): Promise<Partial<Expand<GlobalKeysState>>> => {
        const result = await this.appClient.state.global.getAll()
        return {
          totalPets: result.totalPets,
          totalRewardsMined: result.totalRewardsMined,
          totalRewardsBurned: result.totalRewardsBurned,
          rewardAsaId: result.rewardAsaId,
          beaconAppId: result.beaconAppId,
          randomnessGapRounds: result.randomnessGapRounds,
          randomDelay: result.randomDelay,
          maxStat: result.maxStat,
          ageSecondsPerDay: result.ageSecondsPerDay,
          isBootstrapped: result.isBootstrapped,
          hungerIncrease: result.hungerIncrease,
          petHungerCost: result.petHungerCost,
          hungerDecayNumerator: result.hungerDecayNumerator,
          hungerDecayDenominator: result.hungerDecayDenominator,
          happinessDecayNumerator: result.happinessDecayNumerator,
          happinessDecayDenominator: result.happinessDecayDenominator,
          evolveCost: result.evolveCost,
          reviveCost: result.reviveCost,
          stageRewardBaby: result.stageRewardBaby,
          stageRewardTeen: result.stageRewardTeen,
          stageRewardAdult: result.stageRewardAdult,
        }
      },
      /**
       * Get the current value of the totalPets key in global state
       */
      totalPets: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("totalPets")) as bigint | undefined },
      /**
       * Get the current value of the totalRewardsMined key in global state
       */
      totalRewardsMined: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("totalRewardsMined")) as bigint | undefined },
      /**
       * Get the current value of the totalRewardsBurned key in global state
       */
      totalRewardsBurned: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("totalRewardsBurned")) as bigint | undefined },
      /**
       * Get the current value of the rewardAsaId key in global state
       */
      rewardAsaId: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("rewardAsaId")) as bigint | undefined },
      /**
       * Get the current value of the beaconAppId key in global state
       */
      beaconAppId: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("beaconAppId")) as bigint | undefined },
      /**
       * Get the current value of the randomnessGapRounds key in global state
       */
      randomnessGapRounds: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("randomnessGapRounds")) as bigint | undefined },
      /**
       * Get the current value of the randomDelay key in global state
       */
      randomDelay: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("randomDelay")) as bigint | undefined },
      /**
       * Get the current value of the maxStat key in global state
       */
      maxStat: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("maxStat")) as bigint | undefined },
      /**
       * Get the current value of the ageSecondsPerDay key in global state
       */
      ageSecondsPerDay: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("ageSecondsPerDay")) as bigint | undefined },
      /**
       * Get the current value of the isBootstrapped key in global state
       */
      isBootstrapped: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("isBootstrapped")) as bigint | undefined },
      /**
       * Get the current value of the hungerIncrease key in global state
       */
      hungerIncrease: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("hungerIncrease")) as bigint | undefined },
      /**
       * Get the current value of the petHungerCost key in global state
       */
      petHungerCost: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("petHungerCost")) as bigint | undefined },
      /**
       * Get the current value of the hungerDecayNumerator key in global state
       */
      hungerDecayNumerator: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("hungerDecayNumerator")) as bigint | undefined },
      /**
       * Get the current value of the hungerDecayDenominator key in global state
       */
      hungerDecayDenominator: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("hungerDecayDenominator")) as bigint | undefined },
      /**
       * Get the current value of the happinessDecayNumerator key in global state
       */
      happinessDecayNumerator: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("happinessDecayNumerator")) as bigint | undefined },
      /**
       * Get the current value of the happinessDecayDenominator key in global state
       */
      happinessDecayDenominator: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("happinessDecayDenominator")) as bigint | undefined },
      /**
       * Get the current value of the evolveCost key in global state
       */
      evolveCost: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("evolveCost")) as bigint | undefined },
      /**
       * Get the current value of the reviveCost key in global state
       */
      reviveCost: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("reviveCost")) as bigint | undefined },
      /**
       * Get the current value of the stageRewardBaby key in global state
       */
      stageRewardBaby: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("stageRewardBaby")) as bigint | undefined },
      /**
       * Get the current value of the stageRewardTeen key in global state
       */
      stageRewardTeen: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("stageRewardTeen")) as bigint | undefined },
      /**
       * Get the current value of the stageRewardAdult key in global state
       */
      stageRewardAdult: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("stageRewardAdult")) as bigint | undefined },
    },
    /**
     * Methods to access local state for the current Algochi app
     */
    local: (address: string | Address) => {
      const encodedAddress = typeof address === 'string' ? address : encodeAddress(address.publicKey)
      return {
        /**
         * Get all current keyed values from local state
         */
        getAll: async (): Promise<Partial<Expand<LocalKeysState>>> => {
          const result = await this.appClient.state.local(encodedAddress).getAll()
          return {
            assetId: result.assetId,
            hunger: result.hunger,
            happiness: result.happiness,
            ageDays: result.ageDays,
            stage: result.stage,
            lastUpdate: result.lastUpdate,
            isDead: result.isDead,
            committedRound: result.committedRound,
            feedHungerSnapshot: result.feedHungerSnapshot,
            traitColor: result.traitColor,
            traitPattern: result.traitPattern,
            traitAccessory: result.traitAccessory,
            traitsFinalized: result.traitsFinalized,
          }
        },
        /**
         * Get the current value of the assetId key in local state
         */
        assetId: async (): Promise<bigint | undefined> => { return (await this.appClient.state.local(encodedAddress).getValue("assetId")) as bigint | undefined },
        /**
         * Get the current value of the hunger key in local state
         */
        hunger: async (): Promise<bigint | undefined> => { return (await this.appClient.state.local(encodedAddress).getValue("hunger")) as bigint | undefined },
        /**
         * Get the current value of the happiness key in local state
         */
        happiness: async (): Promise<bigint | undefined> => { return (await this.appClient.state.local(encodedAddress).getValue("happiness")) as bigint | undefined },
        /**
         * Get the current value of the ageDays key in local state
         */
        ageDays: async (): Promise<bigint | undefined> => { return (await this.appClient.state.local(encodedAddress).getValue("ageDays")) as bigint | undefined },
        /**
         * Get the current value of the stage key in local state
         */
        stage: async (): Promise<bigint | undefined> => { return (await this.appClient.state.local(encodedAddress).getValue("stage")) as bigint | undefined },
        /**
         * Get the current value of the lastUpdate key in local state
         */
        lastUpdate: async (): Promise<bigint | undefined> => { return (await this.appClient.state.local(encodedAddress).getValue("lastUpdate")) as bigint | undefined },
        /**
         * Get the current value of the isDead key in local state
         */
        isDead: async (): Promise<bigint | undefined> => { return (await this.appClient.state.local(encodedAddress).getValue("isDead")) as bigint | undefined },
        /**
         * Get the current value of the committedRound key in local state
         */
        committedRound: async (): Promise<bigint | undefined> => { return (await this.appClient.state.local(encodedAddress).getValue("committedRound")) as bigint | undefined },
        /**
         * Get the current value of the feedHungerSnapshot key in local state
         */
        feedHungerSnapshot: async (): Promise<bigint | undefined> => { return (await this.appClient.state.local(encodedAddress).getValue("feedHungerSnapshot")) as bigint | undefined },
        /**
         * Get the current value of the traitColor key in local state
         */
        traitColor: async (): Promise<bigint | undefined> => { return (await this.appClient.state.local(encodedAddress).getValue("traitColor")) as bigint | undefined },
        /**
         * Get the current value of the traitPattern key in local state
         */
        traitPattern: async (): Promise<bigint | undefined> => { return (await this.appClient.state.local(encodedAddress).getValue("traitPattern")) as bigint | undefined },
        /**
         * Get the current value of the traitAccessory key in local state
         */
        traitAccessory: async (): Promise<bigint | undefined> => { return (await this.appClient.state.local(encodedAddress).getValue("traitAccessory")) as bigint | undefined },
        /**
         * Get the current value of the traitsFinalized key in local state
         */
        traitsFinalized: async (): Promise<bigint | undefined> => { return (await this.appClient.state.local(encodedAddress).getValue("traitsFinalized")) as bigint | undefined },
      }
    },
  }

  public newGroup(): AlgochiComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a bootstrap(uint64)uint64 method call against the Algochi contract
       */
      bootstrap(params: CallParams<AlgochiArgs['obj']['bootstrap(uint64)uint64'] | AlgochiArgs['tuple']['bootstrap(uint64)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.bootstrap(params)))
        resultMappers.push((v) => client.decodeReturnValue('bootstrap(uint64)uint64', v))
        return this
      },
      /**
       * Add a feed()void method call against the Algochi contract
       */
      feed(params: CallParams<AlgochiArgs['obj']['feed()void'] | AlgochiArgs['tuple']['feed()void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.feed(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a play()void method call against the Algochi contract
       */
      play(params: CallParams<AlgochiArgs['obj']['play()void'] | AlgochiArgs['tuple']['play()void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.play(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a evolve()void method call against the Algochi contract
       */
      evolve(params: CallParams<AlgochiArgs['obj']['evolve()void'] | AlgochiArgs['tuple']['evolve()void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.evolve(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a revive()void method call against the Algochi contract
       */
      revive(params: CallParams<AlgochiArgs['obj']['revive()void'] | AlgochiArgs['tuple']['revive()void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.revive(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a finalizeTraits()void method call against the Algochi contract
       */
      finalizeTraits(params: CallParams<AlgochiArgs['obj']['finalizeTraits()void'] | AlgochiArgs['tuple']['finalizeTraits()void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.finalizeTraits(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a vrfTest(uint64)(byte[],bool) method call against the Algochi contract
       */
      vrfTest(params: CallParams<AlgochiArgs['obj']['vrfTest(uint64)(byte[],bool)'] | AlgochiArgs['tuple']['vrfTest(uint64)(byte[],bool)']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.vrfTest(params)))
        resultMappers.push((v) => client.decodeReturnValue('vrfTest(uint64)(byte[],bool)', v))
        return this
      },
      get optIn() {
        return {
          createPet: (params: CallParams<AlgochiArgs['obj']['createPet(pay)uint64'] | AlgochiArgs['tuple']['createPet(pay)uint64']>) => {
            promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.optIn.createPet(params)))
            resultMappers.push((v) => client.decodeReturnValue('createPet(pay)uint64', v))
            return this
          },
        }
      },
      /**
       * Add a clear state call to the Algochi contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as AlgochiComposer
  }
}
export type AlgochiComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the bootstrap(uint64)uint64 ABI method.
   *
  * Bootstraps the contract by creating the reward ASA and setting the beacon app ID.
  Must be called once before any other operations.

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  bootstrap(params?: CallParams<AlgochiArgs['obj']['bootstrap(uint64)uint64'] | AlgochiArgs['tuple']['bootstrap(uint64)uint64']>): AlgochiComposer<[...TReturns, AlgochiReturns['bootstrap(uint64)uint64'] | undefined]>

  /**
   * Calls the feed()void ABI method.
   *
  * Feed the pet to increase hunger and earn health-scaled $TAMA rewards.
  Commits to a future VRF round for the follow-up play() action.

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  feed(params?: CallParams<AlgochiArgs['obj']['feed()void'] | AlgochiArgs['tuple']['feed()void']>): AlgochiComposer<[...TReturns, AlgochiReturns['feed()void'] | undefined]>

  /**
   * Calls the play()void ABI method.
   *
  * Play with the pet to boost happiness using VRF randomness.
  Requires a VRF commitment from a previous feed() call and distributes health-scaled rewards.

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  play(params?: CallParams<AlgochiArgs['obj']['play()void'] | AlgochiArgs['tuple']['play()void']>): AlgochiComposer<[...TReturns, AlgochiReturns['play()void'] | undefined]>

  /**
   * Calls the evolve()void ABI method.
   *
  * Evolve the pet to the next stage if age requirements are met.
  Burns $TAMA and applies a happiness boost.

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  evolve(params?: CallParams<AlgochiArgs['obj']['evolve()void'] | AlgochiArgs['tuple']['evolve()void']>): AlgochiComposer<[...TReturns, AlgochiReturns['evolve()void'] | undefined]>

  /**
   * Calls the revive()void ABI method.
   *
   * Revive a dead pet by burning $TAMA and resetting stats to baseline values.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  revive(params?: CallParams<AlgochiArgs['obj']['revive()void'] | AlgochiArgs['tuple']['revive()void']>): AlgochiComposer<[...TReturns, AlgochiReturns['revive()void'] | undefined]>

  /**
   * Calls the finalizeTraits()void ABI method.
   *
  * Finalize pet traits using VRF randomness from the beacon or fallback hash.
  Can only be called once per pet after the VRF round delay has passed.

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  finalizeTraits(params?: CallParams<AlgochiArgs['obj']['finalizeTraits()void'] | AlgochiArgs['tuple']['finalizeTraits()void']>): AlgochiComposer<[...TReturns, AlgochiReturns['finalizeTraits()void'] | undefined]>

  /**
   * Calls the vrfTest(uint64)(byte[],bool) ABI method.
   *
   * Test method for VRF beacon functionality.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  vrfTest(params?: CallParams<AlgochiArgs['obj']['vrfTest(uint64)(byte[],bool)'] | AlgochiArgs['tuple']['vrfTest(uint64)(byte[],bool)']>): AlgochiComposer<[...TReturns, AlgochiReturns['vrfTest(uint64)(byte[],bool)'] | undefined]>

  /**
   * Gets available optIn methods
   */
  readonly optIn: {
    /**
     * Opts the user into an existing instance of the Algochi smart contract using the createPet(pay)uint64 ABI method.
     *
     * @param args The arguments for the smart contract call
     * @param params Any additional parameters for the call
     * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
     */
    createPet(params?: CallParams<AlgochiArgs['obj']['createPet(pay)uint64'] | AlgochiArgs['tuple']['createPet(pay)uint64']>): AlgochiComposer<[...TReturns, AlgochiReturns['createPet(pay)uint64'] | undefined]>
  }

  /**
   * Makes a clear_state call to an existing instance of the Algochi smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): AlgochiComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): AlgochiComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<AlgochiComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<AlgochiComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<AlgochiComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<AlgochiComposerResults<TReturns>>
}
export type AlgochiComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

