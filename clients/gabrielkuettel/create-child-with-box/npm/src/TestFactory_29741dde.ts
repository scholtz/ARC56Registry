/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"TestFactory","structs":{},"methods":[{"name":"set_test_contract_child","args":[{"type":"uint64","name":"test_contract_child_app"}],"returns":{"type":"void"},"actions":{"create":["NoOp"],"call":[]},"readonly":false,"events":[],"recommendations":{}},{"name":"create_test_contract_child_and_box","args":[{"type":"pay","name":"mbr_payment"}],"returns":{"type":"uint64"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}}],"arcs":[22,28],"networks":{},"state":{"schema":{"global":{"ints":1,"bytes":0},"local":{"ints":0,"bytes":0}},"keys":{"global":{"test_contract_child":{"keyType":"AVMString","valueType":"AVMUint64","key":"dGVzdF9jb250cmFjdF9jaGlsZA=="}},"local":{},"box":{}},"maps":{"global":{},"local":{},"box":{}}},"bareActions":{"create":[],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[48],"errorMessage":"OnCompletion must be NoOp"},{"pc":[113,219],"errorMessage":"account funded"},{"pc":[122,126,150,167],"errorMessage":"application exists"},{"pc":[213],"errorMessage":"application log value is not the result of an ABI return"},{"pc":[118],"errorMessage":"check self.test_contract_child exists"},{"pc":[100],"errorMessage":"transaction type is pay"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"events":[]} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


/**
 * The argument types for the TestFactory contract
 */
export type TestFactoryArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'set_test_contract_child(uint64)void': {
      testContractChildApp: bigint | number
    }
    'create_test_contract_child_and_box(pay)uint64': {
      mbrPayment: AppMethodCallTransactionArgument
    }
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'set_test_contract_child(uint64)void': [testContractChildApp: bigint | number]
    'create_test_contract_child_and_box(pay)uint64': [mbrPayment: AppMethodCallTransactionArgument]
  }
}

/**
 * The return type for each method
 */
export type TestFactoryReturns = {
  'set_test_contract_child(uint64)void': void
  'create_test_contract_child_and_box(pay)uint64': bigint
}

/**
 * Defines the types of available calls and state of the TestFactory smart contract.
 */
export type TestFactoryTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'set_test_contract_child(uint64)void' | 'set_test_contract_child', {
      argsObj: TestFactoryArgs['obj']['set_test_contract_child(uint64)void']
      argsTuple: TestFactoryArgs['tuple']['set_test_contract_child(uint64)void']
      returns: TestFactoryReturns['set_test_contract_child(uint64)void']
    }>
    & Record<'create_test_contract_child_and_box(pay)uint64' | 'create_test_contract_child_and_box', {
      argsObj: TestFactoryArgs['obj']['create_test_contract_child_and_box(pay)uint64']
      argsTuple: TestFactoryArgs['tuple']['create_test_contract_child_and_box(pay)uint64']
      returns: TestFactoryReturns['create_test_contract_child_and_box(pay)uint64']
    }>
  /**
   * Defines the shape of the state of the application.
   */
  state: {
    global: {
      keys: {
        testContractChild: bigint
      }
      maps: {}
    }
  }
}

/**
 * Defines the possible abi call signatures.
 */
export type TestFactorySignatures = keyof TestFactoryTypes['methods']
/**
 * Defines the possible abi call signatures for methods that return a non-void value.
 */
export type TestFactoryNonVoidMethodSignatures = keyof TestFactoryTypes['methods'] extends infer T ? T extends keyof TestFactoryTypes['methods'] ? MethodReturn<T> extends void ? never : T  : never : never
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the TestFactory smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends TestFactorySignatures> = TestFactoryTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the TestFactory smart contract to the method's return type
 */
export type MethodReturn<TSignature extends TestFactorySignatures> = TestFactoryTypes['methods'][TSignature]['returns']

/**
 * Defines the shape of the keyed global state of the application.
 */
export type GlobalKeysState = TestFactoryTypes['state']['global']['keys']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the TestFactory smart contract
 */
export abstract class TestFactoryParamsFactory {
  /**
   * Constructs a no op call for the create_test_contract_child_and_box(pay)uint64 ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static createTestContractChildAndBox(params: CallParams<TestFactoryArgs['obj']['create_test_contract_child_and_box(pay)uint64'] | TestFactoryArgs['tuple']['create_test_contract_child_and_box(pay)uint64']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'create_test_contract_child_and_box(pay)uint64' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.mbrPayment],
    }
  }
}

/**
 * A client to make calls to the TestFactory smart contract
 */
export class TestFactoryClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `TestFactoryClient`
   *
   * @param appClient An `AppClient` instance which has been created with the TestFactory app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `TestFactoryClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Checks for decode errors on the given return value and maps the return value to the return type for the given method
   * @returns The typed return value or undefined if there was no value
   */
  decodeReturnValue<TSignature extends TestFactoryNonVoidMethodSignatures>(method: TSignature, returnValue: ABIReturn | undefined) {
    return returnValue !== undefined ? getArc56ReturnValue<MethodReturn<TSignature>>(returnValue, this.appClient.getABIMethod(method), APP_SPEC.structs) : undefined
  }

  /**
   * Returns a new `TestFactoryClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<TestFactoryClient> {
    return new TestFactoryClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `TestFactoryClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<TestFactoryClient> {
    return new TestFactoryClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Makes a clear_state call to an existing instance of the TestFactory smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the TestFactory smart contract using the `create_test_contract_child_and_box(pay)uint64` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    createTestContractChildAndBox: (params: CallParams<TestFactoryArgs['obj']['create_test_contract_child_and_box(pay)uint64'] | TestFactoryArgs['tuple']['create_test_contract_child_and_box(pay)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(TestFactoryParamsFactory.createTestContractChildAndBox(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Makes a clear_state call to an existing instance of the TestFactory smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the TestFactory smart contract using the `create_test_contract_child_and_box(pay)uint64` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    createTestContractChildAndBox: (params: CallParams<TestFactoryArgs['obj']['create_test_contract_child_and_box(pay)uint64'] | TestFactoryArgs['tuple']['create_test_contract_child_and_box(pay)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(TestFactoryParamsFactory.createTestContractChildAndBox(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Makes a clear_state call to an existing instance of the TestFactory smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the TestFactory smart contract using the `create_test_contract_child_and_box(pay)uint64` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    createTestContractChildAndBox: async (params: CallParams<TestFactoryArgs['obj']['create_test_contract_child_and_box(pay)uint64'] | TestFactoryArgs['tuple']['create_test_contract_child_and_box(pay)uint64']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(TestFactoryParamsFactory.createTestContractChildAndBox(params))
      return {...result, return: result.return as unknown as (undefined | TestFactoryReturns['create_test_contract_child_and_box(pay)uint64'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new TestFactoryClient(this.appClient.clone(params))
  }

  /**
   * Methods to access state for the current TestFactory app
   */
  state = {
    /**
     * Methods to access global state for the current TestFactory app
     */
    global: {
      /**
       * Get all current keyed values from global state
       */
      getAll: async (): Promise<Partial<Expand<GlobalKeysState>>> => {
        const result = await this.appClient.state.global.getAll()
        return {
          testContractChild: result.test_contract_child,
        }
      },
      /**
       * Get the current value of the test_contract_child key in global state
       */
      testContractChild: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("test_contract_child")) as bigint | undefined },
    },
  }

  public newGroup(): TestFactoryComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a create_test_contract_child_and_box(pay)uint64 method call against the TestFactory contract
       */
      createTestContractChildAndBox(params: CallParams<TestFactoryArgs['obj']['create_test_contract_child_and_box(pay)uint64'] | TestFactoryArgs['tuple']['create_test_contract_child_and_box(pay)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.createTestContractChildAndBox(params)))
        resultMappers.push((v) => client.decodeReturnValue('create_test_contract_child_and_box(pay)uint64', v))
        return this
      },
      /**
       * Add a clear state call to the TestFactory contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as TestFactoryComposer
  }
}
export type TestFactoryComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the create_test_contract_child_and_box(pay)uint64 ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  createTestContractChildAndBox(params?: CallParams<TestFactoryArgs['obj']['create_test_contract_child_and_box(pay)uint64'] | TestFactoryArgs['tuple']['create_test_contract_child_and_box(pay)uint64']>): TestFactoryComposer<[...TReturns, TestFactoryReturns['create_test_contract_child_and_box(pay)uint64'] | undefined]>

  /**
   * Makes a clear_state call to an existing instance of the TestFactory smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): TestFactoryComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): TestFactoryComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<TestFactoryComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<TestFactoryComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<TestFactoryComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<TestFactoryComposerResults<TReturns>>
}
export type TestFactoryComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

