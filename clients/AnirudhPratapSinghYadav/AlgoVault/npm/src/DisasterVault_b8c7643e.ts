/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"DisasterVault","structs":{},"methods":[{"name":"bootstrap","args":[{"type":"address","name":"admin"},{"type":"address","name":"treasury"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"One-time setup: admin creates campaigns; treasury receives expire() refunds.","events":[],"recommendations":{}},{"name":"create_campaign","args":[{"type":"string","name":"name"},{"type":"uint64","name":"target_amount"},{"type":"string","name":"region"},{"type":"address[]","name":"approvers"},{"type":"uint64","name":"threshold"},{"type":"uint64","name":"expiry_round"},{"type":"uint64","name":"asset_id"}],"returns":{"type":"uint64"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Admin-only campaign creation. Approvers stored on-chain (max 5).","events":[],"recommendations":{}},{"name":"donate","args":[{"type":"uint64","name":"campaign_id"},{"type":"axfer","name":"donation"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Anyone may donate the campaign ASA via grouped axfer into the app account.","events":[],"recommendations":{}},{"name":"submit_approval","args":[{"type":"uint64","name":"campaign_id"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Increment approval_count when sender is in the campaign approver list.","events":[],"recommendations":{}},{"name":"disburse","args":[{"type":"uint64","name":"campaign_id"},{"type":"address[]","name":"beneficiaries"},{"type":"uint64[]","name":"amounts"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"After status == approved, send ASA to beneficiaries via inner asset transfers.","events":[{"name":"Disbursed","args":[{"type":"uint64","name":"campaign_id"},{"type":"uint64","name":"total_micro"},{"type":"uint64","name":"beneficiary_count"}],"desc":"ARC-28 style event payload emitted after successful disburse."}],"recommendations":{}},{"name":"expire","args":[{"type":"uint64","name":"campaign_id"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"After expiry_round, move remaining ASA balance for this asset to treasury (simplified refund path).\nProduction would track per-donor shares in boxes — MVP sends contract ASA balance to treasury.","events":[],"recommendations":{}},{"name":"get_campaign","args":[{"type":"uint64","name":"campaign_id"}],"returns":{"type":"(uint64,uint64,uint64,uint64,uint64)"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"desc":"Returns target, raised, approval_count, threshold, status.","events":[],"recommendations":{}}],"arcs":[22,28],"networks":{},"state":{"schema":{"global":{"ints":1,"bytes":2},"local":{"ints":0,"bytes":0}},"keys":{"global":{"campaign_count":{"keyType":"AVMString","valueType":"AVMUint64","key":"Y2FtcGFpZ25fY291bnQ="},"admin":{"keyType":"AVMString","valueType":"address","key":"YWRtaW4="},"treasury":{"keyType":"AVMString","valueType":"address","key":"dHJlYXN1cnk="}},"local":{},"box":{}},"maps":{"global":{},"local":{},"box":{}}},"bareActions":{"create":["NoOp"],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[847,985],"errorMessage":"Address length is 32 bytes"},{"pc":[158,262],"errorMessage":"check self.admin exists"},{"pc":[287],"errorMessage":"check self.campaign_count exists"},{"pc":[980],"errorMessage":"check self.treasury exists"},{"pc":[375,842],"errorMessage":"index access is out of bounds"},{"pc":[178,202,218,694,717],"errorMessage":"invalid array length header"},{"pc":[232,708],"errorMessage":"invalid number of bytes for arc4.dynamic_array<arc4.static_array<arc4.uint8, 32>>"},{"pc":[728],"errorMessage":"invalid number of bytes for arc4.dynamic_array<arc4.uint64>"},{"pc":[185,209],"errorMessage":"invalid number of bytes for arc4.dynamic_array<arc4.uint8>"},{"pc":[146,154],"errorMessage":"invalid number of bytes for arc4.static_array<arc4.uint8, 32>"},{"pc":[193,240,248,256,455,526,685,922,1040],"errorMessage":"invalid number of bytes for arc4.uint64"},{"pc":[466],"errorMessage":"transaction type is axfer"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"events":[{"name":"Disbursed","args":[{"type":"uint64","name":"campaign_id"},{"type":"uint64","name":"total_micro"},{"type":"uint64","name":"beneficiary_count"}],"desc":"ARC-28 style event payload emitted after successful disburse."}]} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


/**
 * The argument types for the DisasterVault contract
 */
export type DisasterVaultArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'bootstrap(address,address)void': {
      admin: string
      treasury: string
    }
    'create_campaign(string,uint64,string,address[],uint64,uint64,uint64)uint64': {
      name: string
      targetAmount: bigint | number
      region: string
      approvers: string[]
      threshold: bigint | number
      expiryRound: bigint | number
      assetId: bigint | number
    }
    'donate(uint64,axfer)void': {
      campaignId: bigint | number
      donation: AppMethodCallTransactionArgument
    }
    'submit_approval(uint64)void': {
      campaignId: bigint | number
    }
    'disburse(uint64,address[],uint64[])void': {
      campaignId: bigint | number
      beneficiaries: string[]
      amounts: bigint[] | number[]
    }
    'expire(uint64)void': {
      campaignId: bigint | number
    }
    'get_campaign(uint64)(uint64,uint64,uint64,uint64,uint64)': {
      campaignId: bigint | number
    }
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'bootstrap(address,address)void': [admin: string, treasury: string]
    'create_campaign(string,uint64,string,address[],uint64,uint64,uint64)uint64': [name: string, targetAmount: bigint | number, region: string, approvers: string[], threshold: bigint | number, expiryRound: bigint | number, assetId: bigint | number]
    'donate(uint64,axfer)void': [campaignId: bigint | number, donation: AppMethodCallTransactionArgument]
    'submit_approval(uint64)void': [campaignId: bigint | number]
    'disburse(uint64,address[],uint64[])void': [campaignId: bigint | number, beneficiaries: string[], amounts: bigint[] | number[]]
    'expire(uint64)void': [campaignId: bigint | number]
    'get_campaign(uint64)(uint64,uint64,uint64,uint64,uint64)': [campaignId: bigint | number]
  }
}

/**
 * The return type for each method
 */
export type DisasterVaultReturns = {
  'bootstrap(address,address)void': void
  'create_campaign(string,uint64,string,address[],uint64,uint64,uint64)uint64': bigint
  'donate(uint64,axfer)void': void
  'submit_approval(uint64)void': void
  'disburse(uint64,address[],uint64[])void': void
  'expire(uint64)void': void
  'get_campaign(uint64)(uint64,uint64,uint64,uint64,uint64)': [bigint, bigint, bigint, bigint, bigint]
}

/**
 * Defines the types of available calls and state of the DisasterVault smart contract.
 */
export type DisasterVaultTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'bootstrap(address,address)void' | 'bootstrap', {
      argsObj: DisasterVaultArgs['obj']['bootstrap(address,address)void']
      argsTuple: DisasterVaultArgs['tuple']['bootstrap(address,address)void']
      returns: DisasterVaultReturns['bootstrap(address,address)void']
    }>
    & Record<'create_campaign(string,uint64,string,address[],uint64,uint64,uint64)uint64' | 'create_campaign', {
      argsObj: DisasterVaultArgs['obj']['create_campaign(string,uint64,string,address[],uint64,uint64,uint64)uint64']
      argsTuple: DisasterVaultArgs['tuple']['create_campaign(string,uint64,string,address[],uint64,uint64,uint64)uint64']
      returns: DisasterVaultReturns['create_campaign(string,uint64,string,address[],uint64,uint64,uint64)uint64']
    }>
    & Record<'donate(uint64,axfer)void' | 'donate', {
      argsObj: DisasterVaultArgs['obj']['donate(uint64,axfer)void']
      argsTuple: DisasterVaultArgs['tuple']['donate(uint64,axfer)void']
      returns: DisasterVaultReturns['donate(uint64,axfer)void']
    }>
    & Record<'submit_approval(uint64)void' | 'submit_approval', {
      argsObj: DisasterVaultArgs['obj']['submit_approval(uint64)void']
      argsTuple: DisasterVaultArgs['tuple']['submit_approval(uint64)void']
      returns: DisasterVaultReturns['submit_approval(uint64)void']
    }>
    & Record<'disburse(uint64,address[],uint64[])void' | 'disburse', {
      argsObj: DisasterVaultArgs['obj']['disburse(uint64,address[],uint64[])void']
      argsTuple: DisasterVaultArgs['tuple']['disburse(uint64,address[],uint64[])void']
      returns: DisasterVaultReturns['disburse(uint64,address[],uint64[])void']
    }>
    & Record<'expire(uint64)void' | 'expire', {
      argsObj: DisasterVaultArgs['obj']['expire(uint64)void']
      argsTuple: DisasterVaultArgs['tuple']['expire(uint64)void']
      returns: DisasterVaultReturns['expire(uint64)void']
    }>
    & Record<'get_campaign(uint64)(uint64,uint64,uint64,uint64,uint64)' | 'get_campaign', {
      argsObj: DisasterVaultArgs['obj']['get_campaign(uint64)(uint64,uint64,uint64,uint64,uint64)']
      argsTuple: DisasterVaultArgs['tuple']['get_campaign(uint64)(uint64,uint64,uint64,uint64,uint64)']
      returns: DisasterVaultReturns['get_campaign(uint64)(uint64,uint64,uint64,uint64,uint64)']
    }>
  /**
   * Defines the shape of the state of the application.
   */
  state: {
    global: {
      keys: {
        campaignCount: bigint
        admin: string
        treasury: string
      }
      maps: {}
    }
  }
}

/**
 * Defines the possible abi call signatures.
 */
export type DisasterVaultSignatures = keyof DisasterVaultTypes['methods']
/**
 * Defines the possible abi call signatures for methods that return a non-void value.
 */
export type DisasterVaultNonVoidMethodSignatures = keyof DisasterVaultTypes['methods'] extends infer T ? T extends keyof DisasterVaultTypes['methods'] ? MethodReturn<T> extends void ? never : T  : never : never
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the DisasterVault smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends DisasterVaultSignatures> = DisasterVaultTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the DisasterVault smart contract to the method's return type
 */
export type MethodReturn<TSignature extends DisasterVaultSignatures> = DisasterVaultTypes['methods'][TSignature]['returns']

/**
 * Defines the shape of the keyed global state of the application.
 */
export type GlobalKeysState = DisasterVaultTypes['state']['global']['keys']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the DisasterVault smart contract
 */
export abstract class DisasterVaultParamsFactory {
  /**
   * Constructs a no op call for the bootstrap(address,address)void ABI method
   *
   * One-time setup: admin creates campaigns; treasury receives expire() refunds.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static bootstrap(params: CallParams<DisasterVaultArgs['obj']['bootstrap(address,address)void'] | DisasterVaultArgs['tuple']['bootstrap(address,address)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'bootstrap(address,address)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.admin, params.args.treasury],
    }
  }
  /**
   * Constructs a no op call for the create_campaign(string,uint64,string,address[],uint64,uint64,uint64)uint64 ABI method
   *
   * Admin-only campaign creation. Approvers stored on-chain (max 5).
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static createCampaign(params: CallParams<DisasterVaultArgs['obj']['create_campaign(string,uint64,string,address[],uint64,uint64,uint64)uint64'] | DisasterVaultArgs['tuple']['create_campaign(string,uint64,string,address[],uint64,uint64,uint64)uint64']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'create_campaign(string,uint64,string,address[],uint64,uint64,uint64)uint64' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.name, params.args.targetAmount, params.args.region, params.args.approvers, params.args.threshold, params.args.expiryRound, params.args.assetId],
    }
  }
  /**
   * Constructs a no op call for the donate(uint64,axfer)void ABI method
   *
   * Anyone may donate the campaign ASA via grouped axfer into the app account.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static donate(params: CallParams<DisasterVaultArgs['obj']['donate(uint64,axfer)void'] | DisasterVaultArgs['tuple']['donate(uint64,axfer)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'donate(uint64,axfer)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.campaignId, params.args.donation],
    }
  }
  /**
   * Constructs a no op call for the submit_approval(uint64)void ABI method
   *
   * Increment approval_count when sender is in the campaign approver list.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static submitApproval(params: CallParams<DisasterVaultArgs['obj']['submit_approval(uint64)void'] | DisasterVaultArgs['tuple']['submit_approval(uint64)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'submit_approval(uint64)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.campaignId],
    }
  }
  /**
   * Constructs a no op call for the disburse(uint64,address[],uint64[])void ABI method
   *
   * After status == approved, send ASA to beneficiaries via inner asset transfers.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static disburse(params: CallParams<DisasterVaultArgs['obj']['disburse(uint64,address[],uint64[])void'] | DisasterVaultArgs['tuple']['disburse(uint64,address[],uint64[])void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'disburse(uint64,address[],uint64[])void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.campaignId, params.args.beneficiaries, params.args.amounts],
    }
  }
  /**
   * Constructs a no op call for the expire(uint64)void ABI method
   *
  * After expiry_round, move remaining ASA balance for this asset to treasury (simplified refund path).
  Production would track per-donor shares in boxes — MVP sends contract ASA balance to treasury.

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static expire(params: CallParams<DisasterVaultArgs['obj']['expire(uint64)void'] | DisasterVaultArgs['tuple']['expire(uint64)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'expire(uint64)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.campaignId],
    }
  }
  /**
   * Constructs a no op call for the get_campaign(uint64)(uint64,uint64,uint64,uint64,uint64) ABI method
   *
   * Returns target, raised, approval_count, threshold, status.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static getCampaign(params: CallParams<DisasterVaultArgs['obj']['get_campaign(uint64)(uint64,uint64,uint64,uint64,uint64)'] | DisasterVaultArgs['tuple']['get_campaign(uint64)(uint64,uint64,uint64,uint64,uint64)']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'get_campaign(uint64)(uint64,uint64,uint64,uint64,uint64)' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.campaignId],
    }
  }
}

/**
 * A client to make calls to the DisasterVault smart contract
 */
export class DisasterVaultClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `DisasterVaultClient`
   *
   * @param appClient An `AppClient` instance which has been created with the DisasterVault app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `DisasterVaultClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Checks for decode errors on the given return value and maps the return value to the return type for the given method
   * @returns The typed return value or undefined if there was no value
   */
  decodeReturnValue<TSignature extends DisasterVaultNonVoidMethodSignatures>(method: TSignature, returnValue: ABIReturn | undefined) {
    return returnValue !== undefined ? getArc56ReturnValue<MethodReturn<TSignature>>(returnValue, this.appClient.getABIMethod(method), APP_SPEC.structs) : undefined
  }

  /**
   * Returns a new `DisasterVaultClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<DisasterVaultClient> {
    return new DisasterVaultClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `DisasterVaultClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<DisasterVaultClient> {
    return new DisasterVaultClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Makes a clear_state call to an existing instance of the DisasterVault smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the DisasterVault smart contract using the `bootstrap(address,address)void` ABI method.
     *
     * One-time setup: admin creates campaigns; treasury receives expire() refunds.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    bootstrap: (params: CallParams<DisasterVaultArgs['obj']['bootstrap(address,address)void'] | DisasterVaultArgs['tuple']['bootstrap(address,address)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(DisasterVaultParamsFactory.bootstrap(params))
    },

    /**
     * Makes a call to the DisasterVault smart contract using the `create_campaign(string,uint64,string,address[],uint64,uint64,uint64)uint64` ABI method.
     *
     * Admin-only campaign creation. Approvers stored on-chain (max 5).
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    createCampaign: (params: CallParams<DisasterVaultArgs['obj']['create_campaign(string,uint64,string,address[],uint64,uint64,uint64)uint64'] | DisasterVaultArgs['tuple']['create_campaign(string,uint64,string,address[],uint64,uint64,uint64)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(DisasterVaultParamsFactory.createCampaign(params))
    },

    /**
     * Makes a call to the DisasterVault smart contract using the `donate(uint64,axfer)void` ABI method.
     *
     * Anyone may donate the campaign ASA via grouped axfer into the app account.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    donate: (params: CallParams<DisasterVaultArgs['obj']['donate(uint64,axfer)void'] | DisasterVaultArgs['tuple']['donate(uint64,axfer)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(DisasterVaultParamsFactory.donate(params))
    },

    /**
     * Makes a call to the DisasterVault smart contract using the `submit_approval(uint64)void` ABI method.
     *
     * Increment approval_count when sender is in the campaign approver list.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    submitApproval: (params: CallParams<DisasterVaultArgs['obj']['submit_approval(uint64)void'] | DisasterVaultArgs['tuple']['submit_approval(uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(DisasterVaultParamsFactory.submitApproval(params))
    },

    /**
     * Makes a call to the DisasterVault smart contract using the `disburse(uint64,address[],uint64[])void` ABI method.
     *
     * After status == approved, send ASA to beneficiaries via inner asset transfers.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    disburse: (params: CallParams<DisasterVaultArgs['obj']['disburse(uint64,address[],uint64[])void'] | DisasterVaultArgs['tuple']['disburse(uint64,address[],uint64[])void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(DisasterVaultParamsFactory.disburse(params))
    },

    /**
     * Makes a call to the DisasterVault smart contract using the `expire(uint64)void` ABI method.
     *
    * After expiry_round, move remaining ASA balance for this asset to treasury (simplified refund path).
    Production would track per-donor shares in boxes — MVP sends contract ASA balance to treasury.

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    expire: (params: CallParams<DisasterVaultArgs['obj']['expire(uint64)void'] | DisasterVaultArgs['tuple']['expire(uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(DisasterVaultParamsFactory.expire(params))
    },

    /**
     * Makes a call to the DisasterVault smart contract using the `get_campaign(uint64)(uint64,uint64,uint64,uint64,uint64)` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * Returns target, raised, approval_count, threshold, status.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    getCampaign: (params: CallParams<DisasterVaultArgs['obj']['get_campaign(uint64)(uint64,uint64,uint64,uint64,uint64)'] | DisasterVaultArgs['tuple']['get_campaign(uint64)(uint64,uint64,uint64,uint64,uint64)']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(DisasterVaultParamsFactory.getCampaign(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Makes a clear_state call to an existing instance of the DisasterVault smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the DisasterVault smart contract using the `bootstrap(address,address)void` ABI method.
     *
     * One-time setup: admin creates campaigns; treasury receives expire() refunds.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    bootstrap: (params: CallParams<DisasterVaultArgs['obj']['bootstrap(address,address)void'] | DisasterVaultArgs['tuple']['bootstrap(address,address)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(DisasterVaultParamsFactory.bootstrap(params))
    },

    /**
     * Makes a call to the DisasterVault smart contract using the `create_campaign(string,uint64,string,address[],uint64,uint64,uint64)uint64` ABI method.
     *
     * Admin-only campaign creation. Approvers stored on-chain (max 5).
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    createCampaign: (params: CallParams<DisasterVaultArgs['obj']['create_campaign(string,uint64,string,address[],uint64,uint64,uint64)uint64'] | DisasterVaultArgs['tuple']['create_campaign(string,uint64,string,address[],uint64,uint64,uint64)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(DisasterVaultParamsFactory.createCampaign(params))
    },

    /**
     * Makes a call to the DisasterVault smart contract using the `donate(uint64,axfer)void` ABI method.
     *
     * Anyone may donate the campaign ASA via grouped axfer into the app account.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    donate: (params: CallParams<DisasterVaultArgs['obj']['donate(uint64,axfer)void'] | DisasterVaultArgs['tuple']['donate(uint64,axfer)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(DisasterVaultParamsFactory.donate(params))
    },

    /**
     * Makes a call to the DisasterVault smart contract using the `submit_approval(uint64)void` ABI method.
     *
     * Increment approval_count when sender is in the campaign approver list.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    submitApproval: (params: CallParams<DisasterVaultArgs['obj']['submit_approval(uint64)void'] | DisasterVaultArgs['tuple']['submit_approval(uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(DisasterVaultParamsFactory.submitApproval(params))
    },

    /**
     * Makes a call to the DisasterVault smart contract using the `disburse(uint64,address[],uint64[])void` ABI method.
     *
     * After status == approved, send ASA to beneficiaries via inner asset transfers.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    disburse: (params: CallParams<DisasterVaultArgs['obj']['disburse(uint64,address[],uint64[])void'] | DisasterVaultArgs['tuple']['disburse(uint64,address[],uint64[])void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(DisasterVaultParamsFactory.disburse(params))
    },

    /**
     * Makes a call to the DisasterVault smart contract using the `expire(uint64)void` ABI method.
     *
    * After expiry_round, move remaining ASA balance for this asset to treasury (simplified refund path).
    Production would track per-donor shares in boxes — MVP sends contract ASA balance to treasury.

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    expire: (params: CallParams<DisasterVaultArgs['obj']['expire(uint64)void'] | DisasterVaultArgs['tuple']['expire(uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(DisasterVaultParamsFactory.expire(params))
    },

    /**
     * Makes a call to the DisasterVault smart contract using the `get_campaign(uint64)(uint64,uint64,uint64,uint64,uint64)` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * Returns target, raised, approval_count, threshold, status.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    getCampaign: (params: CallParams<DisasterVaultArgs['obj']['get_campaign(uint64)(uint64,uint64,uint64,uint64,uint64)'] | DisasterVaultArgs['tuple']['get_campaign(uint64)(uint64,uint64,uint64,uint64,uint64)']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(DisasterVaultParamsFactory.getCampaign(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Makes a clear_state call to an existing instance of the DisasterVault smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the DisasterVault smart contract using the `bootstrap(address,address)void` ABI method.
     *
     * One-time setup: admin creates campaigns; treasury receives expire() refunds.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    bootstrap: async (params: CallParams<DisasterVaultArgs['obj']['bootstrap(address,address)void'] | DisasterVaultArgs['tuple']['bootstrap(address,address)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(DisasterVaultParamsFactory.bootstrap(params))
      return {...result, return: result.return as unknown as (undefined | DisasterVaultReturns['bootstrap(address,address)void'])}
    },

    /**
     * Makes a call to the DisasterVault smart contract using the `create_campaign(string,uint64,string,address[],uint64,uint64,uint64)uint64` ABI method.
     *
     * Admin-only campaign creation. Approvers stored on-chain (max 5).
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    createCampaign: async (params: CallParams<DisasterVaultArgs['obj']['create_campaign(string,uint64,string,address[],uint64,uint64,uint64)uint64'] | DisasterVaultArgs['tuple']['create_campaign(string,uint64,string,address[],uint64,uint64,uint64)uint64']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(DisasterVaultParamsFactory.createCampaign(params))
      return {...result, return: result.return as unknown as (undefined | DisasterVaultReturns['create_campaign(string,uint64,string,address[],uint64,uint64,uint64)uint64'])}
    },

    /**
     * Makes a call to the DisasterVault smart contract using the `donate(uint64,axfer)void` ABI method.
     *
     * Anyone may donate the campaign ASA via grouped axfer into the app account.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    donate: async (params: CallParams<DisasterVaultArgs['obj']['donate(uint64,axfer)void'] | DisasterVaultArgs['tuple']['donate(uint64,axfer)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(DisasterVaultParamsFactory.donate(params))
      return {...result, return: result.return as unknown as (undefined | DisasterVaultReturns['donate(uint64,axfer)void'])}
    },

    /**
     * Makes a call to the DisasterVault smart contract using the `submit_approval(uint64)void` ABI method.
     *
     * Increment approval_count when sender is in the campaign approver list.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    submitApproval: async (params: CallParams<DisasterVaultArgs['obj']['submit_approval(uint64)void'] | DisasterVaultArgs['tuple']['submit_approval(uint64)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(DisasterVaultParamsFactory.submitApproval(params))
      return {...result, return: result.return as unknown as (undefined | DisasterVaultReturns['submit_approval(uint64)void'])}
    },

    /**
     * Makes a call to the DisasterVault smart contract using the `disburse(uint64,address[],uint64[])void` ABI method.
     *
     * After status == approved, send ASA to beneficiaries via inner asset transfers.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    disburse: async (params: CallParams<DisasterVaultArgs['obj']['disburse(uint64,address[],uint64[])void'] | DisasterVaultArgs['tuple']['disburse(uint64,address[],uint64[])void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(DisasterVaultParamsFactory.disburse(params))
      return {...result, return: result.return as unknown as (undefined | DisasterVaultReturns['disburse(uint64,address[],uint64[])void'])}
    },

    /**
     * Makes a call to the DisasterVault smart contract using the `expire(uint64)void` ABI method.
     *
    * After expiry_round, move remaining ASA balance for this asset to treasury (simplified refund path).
    Production would track per-donor shares in boxes — MVP sends contract ASA balance to treasury.

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    expire: async (params: CallParams<DisasterVaultArgs['obj']['expire(uint64)void'] | DisasterVaultArgs['tuple']['expire(uint64)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(DisasterVaultParamsFactory.expire(params))
      return {...result, return: result.return as unknown as (undefined | DisasterVaultReturns['expire(uint64)void'])}
    },

    /**
     * Makes a call to the DisasterVault smart contract using the `get_campaign(uint64)(uint64,uint64,uint64,uint64,uint64)` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * Returns target, raised, approval_count, threshold, status.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    getCampaign: async (params: CallParams<DisasterVaultArgs['obj']['get_campaign(uint64)(uint64,uint64,uint64,uint64,uint64)'] | DisasterVaultArgs['tuple']['get_campaign(uint64)(uint64,uint64,uint64,uint64,uint64)']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(DisasterVaultParamsFactory.getCampaign(params))
      return {...result, return: result.return as unknown as (undefined | DisasterVaultReturns['get_campaign(uint64)(uint64,uint64,uint64,uint64,uint64)'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new DisasterVaultClient(this.appClient.clone(params))
  }

  /**
   * Makes a readonly (simulated) call to the DisasterVault smart contract using the `get_campaign(uint64)(uint64,uint64,uint64,uint64,uint64)` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * Returns target, raised, approval_count, threshold, status.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async getCampaign(params: CallParams<DisasterVaultArgs['obj']['get_campaign(uint64)(uint64,uint64,uint64,uint64,uint64)'] | DisasterVaultArgs['tuple']['get_campaign(uint64)(uint64,uint64,uint64,uint64,uint64)']>) {
    const result = await this.appClient.send.call(DisasterVaultParamsFactory.getCampaign(params))
    return result.return as unknown as DisasterVaultReturns['get_campaign(uint64)(uint64,uint64,uint64,uint64,uint64)']
  }

  /**
   * Methods to access state for the current DisasterVault app
   */
  state = {
    /**
     * Methods to access global state for the current DisasterVault app
     */
    global: {
      /**
       * Get all current keyed values from global state
       */
      getAll: async (): Promise<Partial<Expand<GlobalKeysState>>> => {
        const result = await this.appClient.state.global.getAll()
        return {
          campaignCount: result.campaign_count,
          admin: result.admin,
          treasury: result.treasury,
        }
      },
      /**
       * Get the current value of the campaign_count key in global state
       */
      campaignCount: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("campaign_count")) as bigint | undefined },
      /**
       * Get the current value of the admin key in global state
       */
      admin: async (): Promise<string | undefined> => { return (await this.appClient.state.global.getValue("admin")) as string | undefined },
      /**
       * Get the current value of the treasury key in global state
       */
      treasury: async (): Promise<string | undefined> => { return (await this.appClient.state.global.getValue("treasury")) as string | undefined },
    },
  }

  public newGroup(): DisasterVaultComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a bootstrap(address,address)void method call against the DisasterVault contract
       */
      bootstrap(params: CallParams<DisasterVaultArgs['obj']['bootstrap(address,address)void'] | DisasterVaultArgs['tuple']['bootstrap(address,address)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.bootstrap(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a create_campaign(string,uint64,string,address[],uint64,uint64,uint64)uint64 method call against the DisasterVault contract
       */
      createCampaign(params: CallParams<DisasterVaultArgs['obj']['create_campaign(string,uint64,string,address[],uint64,uint64,uint64)uint64'] | DisasterVaultArgs['tuple']['create_campaign(string,uint64,string,address[],uint64,uint64,uint64)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.createCampaign(params)))
        resultMappers.push((v) => client.decodeReturnValue('create_campaign(string,uint64,string,address[],uint64,uint64,uint64)uint64', v))
        return this
      },
      /**
       * Add a donate(uint64,axfer)void method call against the DisasterVault contract
       */
      donate(params: CallParams<DisasterVaultArgs['obj']['donate(uint64,axfer)void'] | DisasterVaultArgs['tuple']['donate(uint64,axfer)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.donate(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a submit_approval(uint64)void method call against the DisasterVault contract
       */
      submitApproval(params: CallParams<DisasterVaultArgs['obj']['submit_approval(uint64)void'] | DisasterVaultArgs['tuple']['submit_approval(uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.submitApproval(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a disburse(uint64,address[],uint64[])void method call against the DisasterVault contract
       */
      disburse(params: CallParams<DisasterVaultArgs['obj']['disburse(uint64,address[],uint64[])void'] | DisasterVaultArgs['tuple']['disburse(uint64,address[],uint64[])void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.disburse(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a expire(uint64)void method call against the DisasterVault contract
       */
      expire(params: CallParams<DisasterVaultArgs['obj']['expire(uint64)void'] | DisasterVaultArgs['tuple']['expire(uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.expire(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a get_campaign(uint64)(uint64,uint64,uint64,uint64,uint64) method call against the DisasterVault contract
       */
      getCampaign(params: CallParams<DisasterVaultArgs['obj']['get_campaign(uint64)(uint64,uint64,uint64,uint64,uint64)'] | DisasterVaultArgs['tuple']['get_campaign(uint64)(uint64,uint64,uint64,uint64,uint64)']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.getCampaign(params)))
        resultMappers.push((v) => client.decodeReturnValue('get_campaign(uint64)(uint64,uint64,uint64,uint64,uint64)', v))
        return this
      },
      /**
       * Add a clear state call to the DisasterVault contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as DisasterVaultComposer
  }
}
export type DisasterVaultComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the bootstrap(address,address)void ABI method.
   *
   * One-time setup: admin creates campaigns; treasury receives expire() refunds.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  bootstrap(params?: CallParams<DisasterVaultArgs['obj']['bootstrap(address,address)void'] | DisasterVaultArgs['tuple']['bootstrap(address,address)void']>): DisasterVaultComposer<[...TReturns, DisasterVaultReturns['bootstrap(address,address)void'] | undefined]>

  /**
   * Calls the create_campaign(string,uint64,string,address[],uint64,uint64,uint64)uint64 ABI method.
   *
   * Admin-only campaign creation. Approvers stored on-chain (max 5).
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  createCampaign(params?: CallParams<DisasterVaultArgs['obj']['create_campaign(string,uint64,string,address[],uint64,uint64,uint64)uint64'] | DisasterVaultArgs['tuple']['create_campaign(string,uint64,string,address[],uint64,uint64,uint64)uint64']>): DisasterVaultComposer<[...TReturns, DisasterVaultReturns['create_campaign(string,uint64,string,address[],uint64,uint64,uint64)uint64'] | undefined]>

  /**
   * Calls the donate(uint64,axfer)void ABI method.
   *
   * Anyone may donate the campaign ASA via grouped axfer into the app account.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  donate(params?: CallParams<DisasterVaultArgs['obj']['donate(uint64,axfer)void'] | DisasterVaultArgs['tuple']['donate(uint64,axfer)void']>): DisasterVaultComposer<[...TReturns, DisasterVaultReturns['donate(uint64,axfer)void'] | undefined]>

  /**
   * Calls the submit_approval(uint64)void ABI method.
   *
   * Increment approval_count when sender is in the campaign approver list.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  submitApproval(params?: CallParams<DisasterVaultArgs['obj']['submit_approval(uint64)void'] | DisasterVaultArgs['tuple']['submit_approval(uint64)void']>): DisasterVaultComposer<[...TReturns, DisasterVaultReturns['submit_approval(uint64)void'] | undefined]>

  /**
   * Calls the disburse(uint64,address[],uint64[])void ABI method.
   *
   * After status == approved, send ASA to beneficiaries via inner asset transfers.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  disburse(params?: CallParams<DisasterVaultArgs['obj']['disburse(uint64,address[],uint64[])void'] | DisasterVaultArgs['tuple']['disburse(uint64,address[],uint64[])void']>): DisasterVaultComposer<[...TReturns, DisasterVaultReturns['disburse(uint64,address[],uint64[])void'] | undefined]>

  /**
   * Calls the expire(uint64)void ABI method.
   *
  * After expiry_round, move remaining ASA balance for this asset to treasury (simplified refund path).
  Production would track per-donor shares in boxes — MVP sends contract ASA balance to treasury.

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  expire(params?: CallParams<DisasterVaultArgs['obj']['expire(uint64)void'] | DisasterVaultArgs['tuple']['expire(uint64)void']>): DisasterVaultComposer<[...TReturns, DisasterVaultReturns['expire(uint64)void'] | undefined]>

  /**
   * Calls the get_campaign(uint64)(uint64,uint64,uint64,uint64,uint64) ABI method.
   *
   * Returns target, raised, approval_count, threshold, status.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  getCampaign(params?: CallParams<DisasterVaultArgs['obj']['get_campaign(uint64)(uint64,uint64,uint64,uint64,uint64)'] | DisasterVaultArgs['tuple']['get_campaign(uint64)(uint64,uint64,uint64,uint64,uint64)']>): DisasterVaultComposer<[...TReturns, DisasterVaultReturns['get_campaign(uint64)(uint64,uint64,uint64,uint64,uint64)'] | undefined]>

  /**
   * Makes a clear_state call to an existing instance of the DisasterVault smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): DisasterVaultComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): DisasterVaultComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<DisasterVaultComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<DisasterVaultComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<DisasterVaultComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<DisasterVaultComposerResults<TReturns>>
}
export type DisasterVaultComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

