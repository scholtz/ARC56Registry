/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"OracleAggregator","structs":{},"methods":[{"name":"create","args":[{"type":"uint64","name":"registry_app_id"}],"returns":{"type":"void"},"actions":{"create":["NoOp"],"call":[]},"readonly":false,"events":[],"recommendations":{}},{"name":"push_price","args":[{"type":"string","name":"symbol"},{"type":"uint64","name":"price_usd_x1000"},{"type":"uint64","name":"source_count"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}},{"name":"get_price","args":[{"type":"string","name":"symbol"}],"returns":{"type":"uint64"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"get_price_with_timestamp","args":[{"type":"string","name":"symbol"}],"returns":{"type":"(uint64,uint64)"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}}],"arcs":[22,28],"networks":{},"state":{"schema":{"global":{"ints":1,"bytes":0},"local":{"ints":0,"bytes":0}},"keys":{"global":{"registry_app_id":{"keyType":"AVMString","valueType":"AVMUint64","key":"cmVnaXN0cnlfYXBwX2lk"}},"local":{},"box":{}},"maps":{"global":{},"local":{},"box":{}}},"bareActions":{"create":[],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[108,175,222],"errorMessage":"invalid array length header"},{"pc":[115,182,229],"errorMessage":"invalid number of bytes for arc4.dynamic_array<arc4.uint8>"},{"pc":[80,127,135],"errorMessage":"invalid number of bytes for arc4.uint64"},{"pc":[140],"errorMessage":"need >= 2 sources"},{"pc":[193,240],"errorMessage":"no price for symbol"},{"pc":[207],"errorMessage":"price stale"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"events":[]} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


/**
 * The argument types for the OracleAggregator contract
 */
export type OracleAggregatorArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'create(uint64)void': {
      registryAppId: bigint | number
    }
    'push_price(string,uint64,uint64)void': {
      symbol: string
      priceUsdX1000: bigint | number
      sourceCount: bigint | number
    }
    'get_price(string)uint64': {
      symbol: string
    }
    'get_price_with_timestamp(string)(uint64,uint64)': {
      symbol: string
    }
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'create(uint64)void': [registryAppId: bigint | number]
    'push_price(string,uint64,uint64)void': [symbol: string, priceUsdX1000: bigint | number, sourceCount: bigint | number]
    'get_price(string)uint64': [symbol: string]
    'get_price_with_timestamp(string)(uint64,uint64)': [symbol: string]
  }
}

/**
 * The return type for each method
 */
export type OracleAggregatorReturns = {
  'create(uint64)void': void
  'push_price(string,uint64,uint64)void': void
  'get_price(string)uint64': bigint
  'get_price_with_timestamp(string)(uint64,uint64)': [bigint, bigint]
}

/**
 * Defines the types of available calls and state of the OracleAggregator smart contract.
 */
export type OracleAggregatorTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'create(uint64)void' | 'create', {
      argsObj: OracleAggregatorArgs['obj']['create(uint64)void']
      argsTuple: OracleAggregatorArgs['tuple']['create(uint64)void']
      returns: OracleAggregatorReturns['create(uint64)void']
    }>
    & Record<'push_price(string,uint64,uint64)void' | 'push_price', {
      argsObj: OracleAggregatorArgs['obj']['push_price(string,uint64,uint64)void']
      argsTuple: OracleAggregatorArgs['tuple']['push_price(string,uint64,uint64)void']
      returns: OracleAggregatorReturns['push_price(string,uint64,uint64)void']
    }>
    & Record<'get_price(string)uint64' | 'get_price', {
      argsObj: OracleAggregatorArgs['obj']['get_price(string)uint64']
      argsTuple: OracleAggregatorArgs['tuple']['get_price(string)uint64']
      returns: OracleAggregatorReturns['get_price(string)uint64']
    }>
    & Record<'get_price_with_timestamp(string)(uint64,uint64)' | 'get_price_with_timestamp', {
      argsObj: OracleAggregatorArgs['obj']['get_price_with_timestamp(string)(uint64,uint64)']
      argsTuple: OracleAggregatorArgs['tuple']['get_price_with_timestamp(string)(uint64,uint64)']
      returns: OracleAggregatorReturns['get_price_with_timestamp(string)(uint64,uint64)']
    }>
  /**
   * Defines the shape of the state of the application.
   */
  state: {
    global: {
      keys: {
        registryAppId: bigint
      }
      maps: {}
    }
  }
}

/**
 * Defines the possible abi call signatures.
 */
export type OracleAggregatorSignatures = keyof OracleAggregatorTypes['methods']
/**
 * Defines the possible abi call signatures for methods that return a non-void value.
 */
export type OracleAggregatorNonVoidMethodSignatures = keyof OracleAggregatorTypes['methods'] extends infer T ? T extends keyof OracleAggregatorTypes['methods'] ? MethodReturn<T> extends void ? never : T  : never : never
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the OracleAggregator smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends OracleAggregatorSignatures> = OracleAggregatorTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the OracleAggregator smart contract to the method's return type
 */
export type MethodReturn<TSignature extends OracleAggregatorSignatures> = OracleAggregatorTypes['methods'][TSignature]['returns']

/**
 * Defines the shape of the keyed global state of the application.
 */
export type GlobalKeysState = OracleAggregatorTypes['state']['global']['keys']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the OracleAggregator smart contract
 */
export abstract class OracleAggregatorParamsFactory {
  /**
   * Constructs a no op call for the push_price(string,uint64,uint64)void ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static pushPrice(params: CallParams<OracleAggregatorArgs['obj']['push_price(string,uint64,uint64)void'] | OracleAggregatorArgs['tuple']['push_price(string,uint64,uint64)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'push_price(string,uint64,uint64)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.symbol, params.args.priceUsdX1000, params.args.sourceCount],
    }
  }
  /**
   * Constructs a no op call for the get_price(string)uint64 ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static getPrice(params: CallParams<OracleAggregatorArgs['obj']['get_price(string)uint64'] | OracleAggregatorArgs['tuple']['get_price(string)uint64']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'get_price(string)uint64' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.symbol],
    }
  }
  /**
   * Constructs a no op call for the get_price_with_timestamp(string)(uint64,uint64) ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static getPriceWithTimestamp(params: CallParams<OracleAggregatorArgs['obj']['get_price_with_timestamp(string)(uint64,uint64)'] | OracleAggregatorArgs['tuple']['get_price_with_timestamp(string)(uint64,uint64)']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'get_price_with_timestamp(string)(uint64,uint64)' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.symbol],
    }
  }
}

/**
 * A client to make calls to the OracleAggregator smart contract
 */
export class OracleAggregatorClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `OracleAggregatorClient`
   *
   * @param appClient An `AppClient` instance which has been created with the OracleAggregator app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `OracleAggregatorClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Checks for decode errors on the given return value and maps the return value to the return type for the given method
   * @returns The typed return value or undefined if there was no value
   */
  decodeReturnValue<TSignature extends OracleAggregatorNonVoidMethodSignatures>(method: TSignature, returnValue: ABIReturn | undefined) {
    return returnValue !== undefined ? getArc56ReturnValue<MethodReturn<TSignature>>(returnValue, this.appClient.getABIMethod(method), APP_SPEC.structs) : undefined
  }

  /**
   * Returns a new `OracleAggregatorClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<OracleAggregatorClient> {
    return new OracleAggregatorClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `OracleAggregatorClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<OracleAggregatorClient> {
    return new OracleAggregatorClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Makes a clear_state call to an existing instance of the OracleAggregator smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the OracleAggregator smart contract using the `push_price(string,uint64,uint64)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    pushPrice: (params: CallParams<OracleAggregatorArgs['obj']['push_price(string,uint64,uint64)void'] | OracleAggregatorArgs['tuple']['push_price(string,uint64,uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(OracleAggregatorParamsFactory.pushPrice(params))
    },

    /**
     * Makes a call to the OracleAggregator smart contract using the `get_price(string)uint64` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    getPrice: (params: CallParams<OracleAggregatorArgs['obj']['get_price(string)uint64'] | OracleAggregatorArgs['tuple']['get_price(string)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(OracleAggregatorParamsFactory.getPrice(params))
    },

    /**
     * Makes a call to the OracleAggregator smart contract using the `get_price_with_timestamp(string)(uint64,uint64)` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    getPriceWithTimestamp: (params: CallParams<OracleAggregatorArgs['obj']['get_price_with_timestamp(string)(uint64,uint64)'] | OracleAggregatorArgs['tuple']['get_price_with_timestamp(string)(uint64,uint64)']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(OracleAggregatorParamsFactory.getPriceWithTimestamp(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Makes a clear_state call to an existing instance of the OracleAggregator smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the OracleAggregator smart contract using the `push_price(string,uint64,uint64)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    pushPrice: (params: CallParams<OracleAggregatorArgs['obj']['push_price(string,uint64,uint64)void'] | OracleAggregatorArgs['tuple']['push_price(string,uint64,uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(OracleAggregatorParamsFactory.pushPrice(params))
    },

    /**
     * Makes a call to the OracleAggregator smart contract using the `get_price(string)uint64` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    getPrice: (params: CallParams<OracleAggregatorArgs['obj']['get_price(string)uint64'] | OracleAggregatorArgs['tuple']['get_price(string)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(OracleAggregatorParamsFactory.getPrice(params))
    },

    /**
     * Makes a call to the OracleAggregator smart contract using the `get_price_with_timestamp(string)(uint64,uint64)` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    getPriceWithTimestamp: (params: CallParams<OracleAggregatorArgs['obj']['get_price_with_timestamp(string)(uint64,uint64)'] | OracleAggregatorArgs['tuple']['get_price_with_timestamp(string)(uint64,uint64)']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(OracleAggregatorParamsFactory.getPriceWithTimestamp(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Makes a clear_state call to an existing instance of the OracleAggregator smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the OracleAggregator smart contract using the `push_price(string,uint64,uint64)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    pushPrice: async (params: CallParams<OracleAggregatorArgs['obj']['push_price(string,uint64,uint64)void'] | OracleAggregatorArgs['tuple']['push_price(string,uint64,uint64)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(OracleAggregatorParamsFactory.pushPrice(params))
      return {...result, return: result.return as unknown as (undefined | OracleAggregatorReturns['push_price(string,uint64,uint64)void'])}
    },

    /**
     * Makes a call to the OracleAggregator smart contract using the `get_price(string)uint64` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    getPrice: async (params: CallParams<OracleAggregatorArgs['obj']['get_price(string)uint64'] | OracleAggregatorArgs['tuple']['get_price(string)uint64']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(OracleAggregatorParamsFactory.getPrice(params))
      return {...result, return: result.return as unknown as (undefined | OracleAggregatorReturns['get_price(string)uint64'])}
    },

    /**
     * Makes a call to the OracleAggregator smart contract using the `get_price_with_timestamp(string)(uint64,uint64)` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    getPriceWithTimestamp: async (params: CallParams<OracleAggregatorArgs['obj']['get_price_with_timestamp(string)(uint64,uint64)'] | OracleAggregatorArgs['tuple']['get_price_with_timestamp(string)(uint64,uint64)']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(OracleAggregatorParamsFactory.getPriceWithTimestamp(params))
      return {...result, return: result.return as unknown as (undefined | OracleAggregatorReturns['get_price_with_timestamp(string)(uint64,uint64)'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new OracleAggregatorClient(this.appClient.clone(params))
  }

  /**
   * Makes a readonly (simulated) call to the OracleAggregator smart contract using the `get_price(string)uint64` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async getPrice(params: CallParams<OracleAggregatorArgs['obj']['get_price(string)uint64'] | OracleAggregatorArgs['tuple']['get_price(string)uint64']>) {
    const result = await this.appClient.send.call(OracleAggregatorParamsFactory.getPrice(params))
    return result.return as unknown as OracleAggregatorReturns['get_price(string)uint64']
  }

  /**
   * Makes a readonly (simulated) call to the OracleAggregator smart contract using the `get_price_with_timestamp(string)(uint64,uint64)` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async getPriceWithTimestamp(params: CallParams<OracleAggregatorArgs['obj']['get_price_with_timestamp(string)(uint64,uint64)'] | OracleAggregatorArgs['tuple']['get_price_with_timestamp(string)(uint64,uint64)']>) {
    const result = await this.appClient.send.call(OracleAggregatorParamsFactory.getPriceWithTimestamp(params))
    return result.return as unknown as OracleAggregatorReturns['get_price_with_timestamp(string)(uint64,uint64)']
  }

  /**
   * Methods to access state for the current OracleAggregator app
   */
  state = {
    /**
     * Methods to access global state for the current OracleAggregator app
     */
    global: {
      /**
       * Get all current keyed values from global state
       */
      getAll: async (): Promise<Partial<Expand<GlobalKeysState>>> => {
        const result = await this.appClient.state.global.getAll()
        return {
          registryAppId: result.registry_app_id,
        }
      },
      /**
       * Get the current value of the registry_app_id key in global state
       */
      registryAppId: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("registry_app_id")) as bigint | undefined },
    },
  }

  public newGroup(): OracleAggregatorComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a push_price(string,uint64,uint64)void method call against the OracleAggregator contract
       */
      pushPrice(params: CallParams<OracleAggregatorArgs['obj']['push_price(string,uint64,uint64)void'] | OracleAggregatorArgs['tuple']['push_price(string,uint64,uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.pushPrice(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a get_price(string)uint64 method call against the OracleAggregator contract
       */
      getPrice(params: CallParams<OracleAggregatorArgs['obj']['get_price(string)uint64'] | OracleAggregatorArgs['tuple']['get_price(string)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.getPrice(params)))
        resultMappers.push((v) => client.decodeReturnValue('get_price(string)uint64', v))
        return this
      },
      /**
       * Add a get_price_with_timestamp(string)(uint64,uint64) method call against the OracleAggregator contract
       */
      getPriceWithTimestamp(params: CallParams<OracleAggregatorArgs['obj']['get_price_with_timestamp(string)(uint64,uint64)'] | OracleAggregatorArgs['tuple']['get_price_with_timestamp(string)(uint64,uint64)']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.getPriceWithTimestamp(params)))
        resultMappers.push((v) => client.decodeReturnValue('get_price_with_timestamp(string)(uint64,uint64)', v))
        return this
      },
      /**
       * Add a clear state call to the OracleAggregator contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as OracleAggregatorComposer
  }
}
export type OracleAggregatorComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the push_price(string,uint64,uint64)void ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  pushPrice(params?: CallParams<OracleAggregatorArgs['obj']['push_price(string,uint64,uint64)void'] | OracleAggregatorArgs['tuple']['push_price(string,uint64,uint64)void']>): OracleAggregatorComposer<[...TReturns, OracleAggregatorReturns['push_price(string,uint64,uint64)void'] | undefined]>

  /**
   * Calls the get_price(string)uint64 ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  getPrice(params?: CallParams<OracleAggregatorArgs['obj']['get_price(string)uint64'] | OracleAggregatorArgs['tuple']['get_price(string)uint64']>): OracleAggregatorComposer<[...TReturns, OracleAggregatorReturns['get_price(string)uint64'] | undefined]>

  /**
   * Calls the get_price_with_timestamp(string)(uint64,uint64) ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  getPriceWithTimestamp(params?: CallParams<OracleAggregatorArgs['obj']['get_price_with_timestamp(string)(uint64,uint64)'] | OracleAggregatorArgs['tuple']['get_price_with_timestamp(string)(uint64,uint64)']>): OracleAggregatorComposer<[...TReturns, OracleAggregatorReturns['get_price_with_timestamp(string)(uint64,uint64)'] | undefined]>

  /**
   * Makes a clear_state call to an existing instance of the OracleAggregator smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): OracleAggregatorComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): OracleAggregatorComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<OracleAggregatorComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<OracleAggregatorComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<OracleAggregatorComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<OracleAggregatorComposerResults<TReturns>>
}
export type OracleAggregatorComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

