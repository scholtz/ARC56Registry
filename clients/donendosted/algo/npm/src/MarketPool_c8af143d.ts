/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"MarketPool","structs":{},"methods":[{"name":"create","args":[{"type":"string","name":"asset_symbol"},{"type":"uint64","name":"resolution_price"},{"type":"uint64","name":"expiry_ts"},{"type":"uint64","name":"registry_app_id"}],"returns":{"type":"void"},"actions":{"create":["NoOp"],"call":[]},"readonly":false,"events":[],"recommendations":{}},{"name":"opt_in","args":[],"returns":{"type":"void"},"actions":{"create":[],"call":["OptIn"]},"readonly":false,"events":[],"recommendations":{}},{"name":"place_bet","args":[{"type":"string","name":"direction"},{"type":"pay","name":"payment_txn"}],"returns":{"type":"uint64"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}},{"name":"get_probability","args":[],"returns":{"type":"uint64"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"settle","args":[{"type":"uint64","name":"final_price"},{"type":"uint64","name":"oracle_app_id"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}},{"name":"claim_winnings","args":[],"returns":{"type":"uint64"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}}],"arcs":[22,28],"networks":{},"state":{"schema":{"global":{"ints":6,"bytes":3},"local":{"ints":2,"bytes":1}},"keys":{"global":{"asset_symbol":{"keyType":"AVMString","valueType":"AVMBytes","key":"YXNzZXRfc3ltYm9s"},"resolution_price":{"keyType":"AVMString","valueType":"AVMUint64","key":"cmVzb2x1dGlvbl9wcmljZQ=="},"expiry_ts":{"keyType":"AVMString","valueType":"AVMUint64","key":"ZXhwaXJ5X3Rz"},"registry_app_id":{"keyType":"AVMString","valueType":"AVMUint64","key":"cmVnaXN0cnlfYXBwX2lk"},"yes_pool":{"keyType":"AVMString","valueType":"AVMUint64","key":"eWVzX3Bvb2w="},"no_pool":{"keyType":"AVMString","valueType":"AVMUint64","key":"bm9fcG9vbA=="},"total_bets":{"keyType":"AVMString","valueType":"AVMUint64","key":"dG90YWxfYmV0cw=="},"resolved":{"keyType":"AVMString","valueType":"bool","key":"cmVzb2x2ZWQ="},"outcome":{"keyType":"AVMString","valueType":"bool","key":"b3V0Y29tZQ=="}},"local":{"yes_shares":{"keyType":"AVMString","valueType":"AVMUint64","key":"eWVzX3NoYXJlcw=="},"no_shares":{"keyType":"AVMString","valueType":"AVMUint64","key":"bm9fc2hhcmVz"},"claimed":{"keyType":"AVMString","valueType":"bool","key":"Y2xhaW1lZA=="}},"box":{}},"maps":{"global":{},"local":{},"box":{}}},"bareActions":{"create":[],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[668],"errorMessage":"already claimed"},{"pc":[622],"errorMessage":"already resolved"},{"pc":[838],"errorMessage":"application exists"},{"pc":[594,816],"errorMessage":"application log value is not the result of an ABI return"},{"pc":[664],"errorMessage":"check self.claimed exists for account"},{"pc":[380,612],"errorMessage":"check self.expiry_ts exists"},{"pc":[414,468,486,516,730,899],"errorMessage":"check self.no_pool exists"},{"pc":[497,698,926],"errorMessage":"check self.no_shares exists for account"},{"pc":[673],"errorMessage":"check self.outcome exists"},{"pc":[558,777],"errorMessage":"check self.registry_app_id exists"},{"pc":[632],"errorMessage":"check self.resolution_price exists"},{"pc":[369,618,654],"errorMessage":"check self.resolved exists"},{"pc":[450],"errorMessage":"check self.total_bets exists"},{"pc":[410,428,472,512,720,892],"errorMessage":"check self.yes_pool exists"},{"pc":[438,685,914],"errorMessage":"check self.yes_shares exists for account"},{"pc":[231,344],"errorMessage":"invalid array length header"},{"pc":[239,352],"errorMessage":"invalid number of bytes for arc4.dynamic_array<arc4.uint8>"},{"pc":[250,259,268,542,551,602,826],"errorMessage":"invalid number of bytes for arc4.uint64"},{"pc":[382],"errorMessage":"market expired"},{"pc":[373],"errorMessage":"market resolved"},{"pc":[397],"errorMessage":"min 1 ALGO"},{"pc":[712],"errorMessage":"no position"},{"pc":[614],"errorMessage":"not expired"},{"pc":[657],"errorMessage":"not resolved"},{"pc":[365],"errorMessage":"transaction type is pay"},{"pc":[605],"errorMessage":"wrong oracle"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"events":[]} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


/**
 * The argument types for the MarketPool contract
 */
export type MarketPoolArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'create(string,uint64,uint64,uint64)void': {
      assetSymbol: string
      resolutionPrice: bigint | number
      expiryTs: bigint | number
      registryAppId: bigint | number
    }
    'opt_in()void': Record<string, never>
    'place_bet(string,pay)uint64': {
      direction: string
      paymentTxn: AppMethodCallTransactionArgument
    }
    'get_probability()uint64': Record<string, never>
    'settle(uint64,uint64)void': {
      finalPrice: bigint | number
      oracleAppId: bigint | number
    }
    'claim_winnings()uint64': Record<string, never>
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'create(string,uint64,uint64,uint64)void': [assetSymbol: string, resolutionPrice: bigint | number, expiryTs: bigint | number, registryAppId: bigint | number]
    'opt_in()void': []
    'place_bet(string,pay)uint64': [direction: string, paymentTxn: AppMethodCallTransactionArgument]
    'get_probability()uint64': []
    'settle(uint64,uint64)void': [finalPrice: bigint | number, oracleAppId: bigint | number]
    'claim_winnings()uint64': []
  }
}

/**
 * The return type for each method
 */
export type MarketPoolReturns = {
  'create(string,uint64,uint64,uint64)void': void
  'opt_in()void': void
  'place_bet(string,pay)uint64': bigint
  'get_probability()uint64': bigint
  'settle(uint64,uint64)void': void
  'claim_winnings()uint64': bigint
}

/**
 * Defines the types of available calls and state of the MarketPool smart contract.
 */
export type MarketPoolTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'create(string,uint64,uint64,uint64)void' | 'create', {
      argsObj: MarketPoolArgs['obj']['create(string,uint64,uint64,uint64)void']
      argsTuple: MarketPoolArgs['tuple']['create(string,uint64,uint64,uint64)void']
      returns: MarketPoolReturns['create(string,uint64,uint64,uint64)void']
    }>
    & Record<'opt_in()void' | 'opt_in', {
      argsObj: MarketPoolArgs['obj']['opt_in()void']
      argsTuple: MarketPoolArgs['tuple']['opt_in()void']
      returns: MarketPoolReturns['opt_in()void']
    }>
    & Record<'place_bet(string,pay)uint64' | 'place_bet', {
      argsObj: MarketPoolArgs['obj']['place_bet(string,pay)uint64']
      argsTuple: MarketPoolArgs['tuple']['place_bet(string,pay)uint64']
      returns: MarketPoolReturns['place_bet(string,pay)uint64']
    }>
    & Record<'get_probability()uint64' | 'get_probability', {
      argsObj: MarketPoolArgs['obj']['get_probability()uint64']
      argsTuple: MarketPoolArgs['tuple']['get_probability()uint64']
      returns: MarketPoolReturns['get_probability()uint64']
    }>
    & Record<'settle(uint64,uint64)void' | 'settle', {
      argsObj: MarketPoolArgs['obj']['settle(uint64,uint64)void']
      argsTuple: MarketPoolArgs['tuple']['settle(uint64,uint64)void']
      returns: MarketPoolReturns['settle(uint64,uint64)void']
    }>
    & Record<'claim_winnings()uint64' | 'claim_winnings', {
      argsObj: MarketPoolArgs['obj']['claim_winnings()uint64']
      argsTuple: MarketPoolArgs['tuple']['claim_winnings()uint64']
      returns: MarketPoolReturns['claim_winnings()uint64']
    }>
  /**
   * Defines the shape of the state of the application.
   */
  state: {
    global: {
      keys: {
        assetSymbol: BinaryState
        resolutionPrice: bigint
        expiryTs: bigint
        registryAppId: bigint
        yesPool: bigint
        noPool: bigint
        totalBets: bigint
        resolved: boolean
        outcome: boolean
      }
      maps: {}
    }
    local: {
      keys: {
        yesShares: bigint
        noShares: bigint
        claimed: boolean
      }
      maps: {}
    }
  }
}

/**
 * Defines the possible abi call signatures.
 */
export type MarketPoolSignatures = keyof MarketPoolTypes['methods']
/**
 * Defines the possible abi call signatures for methods that return a non-void value.
 */
export type MarketPoolNonVoidMethodSignatures = keyof MarketPoolTypes['methods'] extends infer T ? T extends keyof MarketPoolTypes['methods'] ? MethodReturn<T> extends void ? never : T  : never : never
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the MarketPool smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends MarketPoolSignatures> = MarketPoolTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the MarketPool smart contract to the method's return type
 */
export type MethodReturn<TSignature extends MarketPoolSignatures> = MarketPoolTypes['methods'][TSignature]['returns']

/**
 * Defines the shape of the keyed global state of the application.
 */
export type GlobalKeysState = MarketPoolTypes['state']['global']['keys']

/**
 * Defines the shape of the keyed local state of the application.
 */
export type LocalKeysState = MarketPoolTypes['state']['local']['keys']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the MarketPool smart contract
 */
export abstract class MarketPoolParamsFactory {
  /**
   * Gets available optIn ABI call param factories
   */
  static get optIn() {
    return {
      /**
       * Constructs opt-in ABI call params for the MarketPool smart contract using the opt_in()void ABI method
       *
       * @param params Parameters for the call
       * @returns An `AppClientMethodCallParams` object for the call
       */
      optIn(params: CallParams<MarketPoolArgs['obj']['opt_in()void'] | MarketPoolArgs['tuple']['opt_in()void']>): AppClientMethodCallParams {
        return {
          ...params,
          method: 'opt_in()void' as const,
          args: Array.isArray(params.args) ? params.args : [],
        }
      },
    }
  }

  /**
   * Constructs a no op call for the place_bet(string,pay)uint64 ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static placeBet(params: CallParams<MarketPoolArgs['obj']['place_bet(string,pay)uint64'] | MarketPoolArgs['tuple']['place_bet(string,pay)uint64']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'place_bet(string,pay)uint64' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.direction, params.args.paymentTxn],
    }
  }
  /**
   * Constructs a no op call for the get_probability()uint64 ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static getProbability(params: CallParams<MarketPoolArgs['obj']['get_probability()uint64'] | MarketPoolArgs['tuple']['get_probability()uint64']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'get_probability()uint64' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the settle(uint64,uint64)void ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static settle(params: CallParams<MarketPoolArgs['obj']['settle(uint64,uint64)void'] | MarketPoolArgs['tuple']['settle(uint64,uint64)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'settle(uint64,uint64)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.finalPrice, params.args.oracleAppId],
    }
  }
  /**
   * Constructs a no op call for the claim_winnings()uint64 ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static claimWinnings(params: CallParams<MarketPoolArgs['obj']['claim_winnings()uint64'] | MarketPoolArgs['tuple']['claim_winnings()uint64']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'claim_winnings()uint64' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
}

/**
 * A client to make calls to the MarketPool smart contract
 */
export class MarketPoolClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `MarketPoolClient`
   *
   * @param appClient An `AppClient` instance which has been created with the MarketPool app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `MarketPoolClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Checks for decode errors on the given return value and maps the return value to the return type for the given method
   * @returns The typed return value or undefined if there was no value
   */
  decodeReturnValue<TSignature extends MarketPoolNonVoidMethodSignatures>(method: TSignature, returnValue: ABIReturn | undefined) {
    return returnValue !== undefined ? getArc56ReturnValue<MethodReturn<TSignature>>(returnValue, this.appClient.getABIMethod(method), APP_SPEC.structs) : undefined
  }

  /**
   * Returns a new `MarketPoolClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<MarketPoolClient> {
    return new MarketPoolClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `MarketPoolClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<MarketPoolClient> {
    return new MarketPoolClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Gets available optIn methods
     */
    optIn: {
      /**
       * Opts the user into an existing instance of the MarketPool smart contract using the `opt_in()void` ABI method.
       *
       * @param params The params for the smart contract call
       * @returns The optIn params
       */
      optIn: (params: CallParams<MarketPoolArgs['obj']['opt_in()void'] | MarketPoolArgs['tuple']['opt_in()void']> = {args: []}) => {
        return this.appClient.params.optIn(MarketPoolParamsFactory.optIn.optIn(params))
      },

    },

    /**
     * Makes a clear_state call to an existing instance of the MarketPool smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the MarketPool smart contract using the `place_bet(string,pay)uint64` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    placeBet: (params: CallParams<MarketPoolArgs['obj']['place_bet(string,pay)uint64'] | MarketPoolArgs['tuple']['place_bet(string,pay)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(MarketPoolParamsFactory.placeBet(params))
    },

    /**
     * Makes a call to the MarketPool smart contract using the `get_probability()uint64` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    getProbability: (params: CallParams<MarketPoolArgs['obj']['get_probability()uint64'] | MarketPoolArgs['tuple']['get_probability()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(MarketPoolParamsFactory.getProbability(params))
    },

    /**
     * Makes a call to the MarketPool smart contract using the `settle(uint64,uint64)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    settle: (params: CallParams<MarketPoolArgs['obj']['settle(uint64,uint64)void'] | MarketPoolArgs['tuple']['settle(uint64,uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(MarketPoolParamsFactory.settle(params))
    },

    /**
     * Makes a call to the MarketPool smart contract using the `claim_winnings()uint64` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    claimWinnings: (params: CallParams<MarketPoolArgs['obj']['claim_winnings()uint64'] | MarketPoolArgs['tuple']['claim_winnings()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(MarketPoolParamsFactory.claimWinnings(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Gets available optIn methods
     */
    optIn: {
      /**
       * Opts the user into an existing instance of the MarketPool smart contract using the `opt_in()void` ABI method.
       *
       * @param params The params for the smart contract call
       * @returns The optIn transaction
       */
      optIn: (params: CallParams<MarketPoolArgs['obj']['opt_in()void'] | MarketPoolArgs['tuple']['opt_in()void']> = {args: []}) => {
        return this.appClient.createTransaction.optIn(MarketPoolParamsFactory.optIn.optIn(params))
      },

    },

    /**
     * Makes a clear_state call to an existing instance of the MarketPool smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the MarketPool smart contract using the `place_bet(string,pay)uint64` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    placeBet: (params: CallParams<MarketPoolArgs['obj']['place_bet(string,pay)uint64'] | MarketPoolArgs['tuple']['place_bet(string,pay)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(MarketPoolParamsFactory.placeBet(params))
    },

    /**
     * Makes a call to the MarketPool smart contract using the `get_probability()uint64` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    getProbability: (params: CallParams<MarketPoolArgs['obj']['get_probability()uint64'] | MarketPoolArgs['tuple']['get_probability()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(MarketPoolParamsFactory.getProbability(params))
    },

    /**
     * Makes a call to the MarketPool smart contract using the `settle(uint64,uint64)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    settle: (params: CallParams<MarketPoolArgs['obj']['settle(uint64,uint64)void'] | MarketPoolArgs['tuple']['settle(uint64,uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(MarketPoolParamsFactory.settle(params))
    },

    /**
     * Makes a call to the MarketPool smart contract using the `claim_winnings()uint64` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    claimWinnings: (params: CallParams<MarketPoolArgs['obj']['claim_winnings()uint64'] | MarketPoolArgs['tuple']['claim_winnings()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(MarketPoolParamsFactory.claimWinnings(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Gets available optIn methods
     */
    optIn: {
      /**
       * Opts the user into an existing instance of the MarketPool smart contract using the `opt_in()void` ABI method.
       *
       * @param params The params for the smart contract call
       * @returns The optIn result
       */
      optIn: async (params: CallParams<MarketPoolArgs['obj']['opt_in()void'] | MarketPoolArgs['tuple']['opt_in()void']> & SendParams = {args: []}) => {
        const result = await this.appClient.send.optIn(MarketPoolParamsFactory.optIn.optIn(params))
        return {...result, return: result.return as unknown as (undefined | MarketPoolReturns['opt_in()void'])}
      },

    },

    /**
     * Makes a clear_state call to an existing instance of the MarketPool smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the MarketPool smart contract using the `place_bet(string,pay)uint64` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    placeBet: async (params: CallParams<MarketPoolArgs['obj']['place_bet(string,pay)uint64'] | MarketPoolArgs['tuple']['place_bet(string,pay)uint64']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(MarketPoolParamsFactory.placeBet(params))
      return {...result, return: result.return as unknown as (undefined | MarketPoolReturns['place_bet(string,pay)uint64'])}
    },

    /**
     * Makes a call to the MarketPool smart contract using the `get_probability()uint64` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    getProbability: async (params: CallParams<MarketPoolArgs['obj']['get_probability()uint64'] | MarketPoolArgs['tuple']['get_probability()uint64']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(MarketPoolParamsFactory.getProbability(params))
      return {...result, return: result.return as unknown as (undefined | MarketPoolReturns['get_probability()uint64'])}
    },

    /**
     * Makes a call to the MarketPool smart contract using the `settle(uint64,uint64)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    settle: async (params: CallParams<MarketPoolArgs['obj']['settle(uint64,uint64)void'] | MarketPoolArgs['tuple']['settle(uint64,uint64)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(MarketPoolParamsFactory.settle(params))
      return {...result, return: result.return as unknown as (undefined | MarketPoolReturns['settle(uint64,uint64)void'])}
    },

    /**
     * Makes a call to the MarketPool smart contract using the `claim_winnings()uint64` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    claimWinnings: async (params: CallParams<MarketPoolArgs['obj']['claim_winnings()uint64'] | MarketPoolArgs['tuple']['claim_winnings()uint64']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(MarketPoolParamsFactory.claimWinnings(params))
      return {...result, return: result.return as unknown as (undefined | MarketPoolReturns['claim_winnings()uint64'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new MarketPoolClient(this.appClient.clone(params))
  }

  /**
   * Makes a readonly (simulated) call to the MarketPool smart contract using the `get_probability()uint64` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async getProbability(params: CallParams<MarketPoolArgs['obj']['get_probability()uint64'] | MarketPoolArgs['tuple']['get_probability()uint64']> = {args: []}) {
    const result = await this.appClient.send.call(MarketPoolParamsFactory.getProbability(params))
    return result.return as unknown as MarketPoolReturns['get_probability()uint64']
  }

  /**
   * Methods to access state for the current MarketPool app
   */
  state = {
    /**
     * Methods to access global state for the current MarketPool app
     */
    global: {
      /**
       * Get all current keyed values from global state
       */
      getAll: async (): Promise<Partial<Expand<GlobalKeysState>>> => {
        const result = await this.appClient.state.global.getAll()
        return {
          assetSymbol: new BinaryStateValue(result.asset_symbol),
          resolutionPrice: result.resolution_price,
          expiryTs: result.expiry_ts,
          registryAppId: result.registry_app_id,
          yesPool: result.yes_pool,
          noPool: result.no_pool,
          totalBets: result.total_bets,
          resolved: result.resolved,
          outcome: result.outcome,
        }
      },
      /**
       * Get the current value of the asset_symbol key in global state
       */
      assetSymbol: async (): Promise<BinaryState> => { return new BinaryStateValue((await this.appClient.state.global.getValue("asset_symbol")) as Uint8Array | undefined) },
      /**
       * Get the current value of the resolution_price key in global state
       */
      resolutionPrice: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("resolution_price")) as bigint | undefined },
      /**
       * Get the current value of the expiry_ts key in global state
       */
      expiryTs: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("expiry_ts")) as bigint | undefined },
      /**
       * Get the current value of the registry_app_id key in global state
       */
      registryAppId: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("registry_app_id")) as bigint | undefined },
      /**
       * Get the current value of the yes_pool key in global state
       */
      yesPool: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("yes_pool")) as bigint | undefined },
      /**
       * Get the current value of the no_pool key in global state
       */
      noPool: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("no_pool")) as bigint | undefined },
      /**
       * Get the current value of the total_bets key in global state
       */
      totalBets: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("total_bets")) as bigint | undefined },
      /**
       * Get the current value of the resolved key in global state
       */
      resolved: async (): Promise<boolean | undefined> => { return (await this.appClient.state.global.getValue("resolved")) as boolean | undefined },
      /**
       * Get the current value of the outcome key in global state
       */
      outcome: async (): Promise<boolean | undefined> => { return (await this.appClient.state.global.getValue("outcome")) as boolean | undefined },
    },
    /**
     * Methods to access local state for the current MarketPool app
     */
    local: (address: string | Address) => {
      const encodedAddress = typeof address === 'string' ? address : encodeAddress(address.publicKey)
      return {
        /**
         * Get all current keyed values from local state
         */
        getAll: async (): Promise<Partial<Expand<LocalKeysState>>> => {
          const result = await this.appClient.state.local(encodedAddress).getAll()
          return {
            yesShares: result.yes_shares,
            noShares: result.no_shares,
            claimed: result.claimed,
          }
        },
        /**
         * Get the current value of the yes_shares key in local state
         */
        yesShares: async (): Promise<bigint | undefined> => { return (await this.appClient.state.local(encodedAddress).getValue("yes_shares")) as bigint | undefined },
        /**
         * Get the current value of the no_shares key in local state
         */
        noShares: async (): Promise<bigint | undefined> => { return (await this.appClient.state.local(encodedAddress).getValue("no_shares")) as bigint | undefined },
        /**
         * Get the current value of the claimed key in local state
         */
        claimed: async (): Promise<boolean | undefined> => { return (await this.appClient.state.local(encodedAddress).getValue("claimed")) as boolean | undefined },
      }
    },
  }

  public newGroup(): MarketPoolComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a place_bet(string,pay)uint64 method call against the MarketPool contract
       */
      placeBet(params: CallParams<MarketPoolArgs['obj']['place_bet(string,pay)uint64'] | MarketPoolArgs['tuple']['place_bet(string,pay)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.placeBet(params)))
        resultMappers.push((v) => client.decodeReturnValue('place_bet(string,pay)uint64', v))
        return this
      },
      /**
       * Add a get_probability()uint64 method call against the MarketPool contract
       */
      getProbability(params: CallParams<MarketPoolArgs['obj']['get_probability()uint64'] | MarketPoolArgs['tuple']['get_probability()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.getProbability(params)))
        resultMappers.push((v) => client.decodeReturnValue('get_probability()uint64', v))
        return this
      },
      /**
       * Add a settle(uint64,uint64)void method call against the MarketPool contract
       */
      settle(params: CallParams<MarketPoolArgs['obj']['settle(uint64,uint64)void'] | MarketPoolArgs['tuple']['settle(uint64,uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.settle(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a claim_winnings()uint64 method call against the MarketPool contract
       */
      claimWinnings(params: CallParams<MarketPoolArgs['obj']['claim_winnings()uint64'] | MarketPoolArgs['tuple']['claim_winnings()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.claimWinnings(params)))
        resultMappers.push((v) => client.decodeReturnValue('claim_winnings()uint64', v))
        return this
      },
      get optIn() {
        return {
          optIn: (params: CallParams<MarketPoolArgs['obj']['opt_in()void'] | MarketPoolArgs['tuple']['opt_in()void']>) => {
            promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.optIn.optIn(params)))
            resultMappers.push(undefined)
            return this
          },
        }
      },
      /**
       * Add a clear state call to the MarketPool contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as MarketPoolComposer
  }
}
export type MarketPoolComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the place_bet(string,pay)uint64 ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  placeBet(params?: CallParams<MarketPoolArgs['obj']['place_bet(string,pay)uint64'] | MarketPoolArgs['tuple']['place_bet(string,pay)uint64']>): MarketPoolComposer<[...TReturns, MarketPoolReturns['place_bet(string,pay)uint64'] | undefined]>

  /**
   * Calls the get_probability()uint64 ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  getProbability(params?: CallParams<MarketPoolArgs['obj']['get_probability()uint64'] | MarketPoolArgs['tuple']['get_probability()uint64']>): MarketPoolComposer<[...TReturns, MarketPoolReturns['get_probability()uint64'] | undefined]>

  /**
   * Calls the settle(uint64,uint64)void ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  settle(params?: CallParams<MarketPoolArgs['obj']['settle(uint64,uint64)void'] | MarketPoolArgs['tuple']['settle(uint64,uint64)void']>): MarketPoolComposer<[...TReturns, MarketPoolReturns['settle(uint64,uint64)void'] | undefined]>

  /**
   * Calls the claim_winnings()uint64 ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  claimWinnings(params?: CallParams<MarketPoolArgs['obj']['claim_winnings()uint64'] | MarketPoolArgs['tuple']['claim_winnings()uint64']>): MarketPoolComposer<[...TReturns, MarketPoolReturns['claim_winnings()uint64'] | undefined]>

  /**
   * Gets available optIn methods
   */
  readonly optIn: {
    /**
     * Opts the user into an existing instance of the MarketPool smart contract using the opt_in()void ABI method.
     *
     * @param args The arguments for the smart contract call
     * @param params Any additional parameters for the call
     * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
     */
    optIn(params?: CallParams<MarketPoolArgs['obj']['opt_in()void'] | MarketPoolArgs['tuple']['opt_in()void']>): MarketPoolComposer<[...TReturns, MarketPoolReturns['opt_in()void'] | undefined]>
  }

  /**
   * Makes a clear_state call to an existing instance of the MarketPool smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): MarketPoolComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): MarketPoolComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<MarketPoolComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<MarketPoolComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<MarketPoolComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<MarketPoolComposerResults<TReturns>>
}
export type MarketPoolComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

