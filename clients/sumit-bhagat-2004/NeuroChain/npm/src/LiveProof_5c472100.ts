/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"LiveProof","structs":{"ProofRecord":[{"name":"nodeId","type":"string"},{"name":"textHash","type":"string"},{"name":"embeddingHash","type":"string"},{"name":"timestamp","type":"uint64"},{"name":"creator","type":"address"}]},"methods":[{"name":"initialize","args":[],"returns":{"type":"void"},"actions":{"create":["NoOp"],"call":[]},"readonly":false,"desc":"Deploy contract. Called once by creator.","events":[],"recommendations":{}},{"name":"anchor_proof","args":[{"type":"string","name":"node_id"},{"type":"string","name":"text_hash"},{"type":"string","name":"embedding_hash"},{"type":"uint64","name":"timestamp"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Anchor a knowledge node proof on-chain.\nAnyone can anchor — the caller's address is recorded as creator. node_id must be unique.","events":[],"recommendations":{}},{"name":"get_proof","args":[{"type":"string","name":"node_id"}],"returns":{"type":"(string,string,string,uint64,address)","struct":"ProofRecord"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"desc":"Fetch a proof record by node_id.\nFrontend calls this to display verification details.","events":[],"recommendations":{}},{"name":"proof_exists","args":[{"type":"string","name":"node_id"}],"returns":{"type":"bool"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"desc":"Check if a proof exists without fetching the full record.","events":[],"recommendations":{}},{"name":"get_total_proofs","args":[],"returns":{"type":"uint64"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"desc":"Return total number of proofs anchored.","events":[],"recommendations":{}}],"arcs":[22,28],"desc":"\n    LiveProof — on-chain knowledge proof anchoring.\n    Stores cryptographic fingerprints of ideas and their\n    AI semantic embeddings. Each proof is queryable by node_id.\n    ","networks":{},"state":{"schema":{"global":{"ints":1,"bytes":1},"local":{"ints":0,"bytes":0}},"keys":{"global":{"total_proofs":{"keyType":"AVMString","valueType":"AVMUint64","key":"dG90YWw="},"owner":{"keyType":"AVMString","valueType":"address","key":"b3duZXI="}},"local":{},"box":{}},"maps":{"global":{},"local":{},"box":{"proofs":{"keyType":"string","valueType":"ProofRecord","prefix":"cF8="}}}},"bareActions":{"create":[],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[162],"errorMessage":"Proof already anchored for this node"},{"pc":[250],"errorMessage":"Proof not found"},{"pc":[222,294],"errorMessage":"check self.total_proofs exists"},{"pc":[172],"errorMessage":"embedding_hash cannot be empty"},{"pc":[104,120,136,235,264],"errorMessage":"invalid array length header"},{"pc":[114,130,143,242,271],"errorMessage":"invalid number of bytes for arc4.dynamic_array<arc4.uint8>"},{"pc":[152],"errorMessage":"invalid number of bytes for arc4.uint64"},{"pc":[167],"errorMessage":"text_hash cannot be empty"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"events":[]} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


// Type definitions for ARC-56 structs

export type ProofRecord = {
  nodeId: string,
  textHash: string,
  embeddingHash: string,
  timestamp: bigint,
  creator: string
}


/**
 * Converts the ABI tuple representation of a ProofRecord to the struct representation
 */
export function ProofRecordFromTuple(abiTuple: [string, string, string, bigint, string]) {
  return getABIStructFromABITuple(abiTuple, APP_SPEC.structs.ProofRecord, APP_SPEC.structs) as ProofRecord
}

/**
 * The argument types for the LiveProof contract
 */
export type LiveProofArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'initialize()void': Record<string, never>
    'anchor_proof(string,string,string,uint64)void': {
      nodeId: string
      textHash: string
      embeddingHash: string
      timestamp: bigint | number
    }
    'get_proof(string)(string,string,string,uint64,address)': {
      nodeId: string
    }
    'proof_exists(string)bool': {
      nodeId: string
    }
    'get_total_proofs()uint64': Record<string, never>
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'initialize()void': []
    'anchor_proof(string,string,string,uint64)void': [nodeId: string, textHash: string, embeddingHash: string, timestamp: bigint | number]
    'get_proof(string)(string,string,string,uint64,address)': [nodeId: string]
    'proof_exists(string)bool': [nodeId: string]
    'get_total_proofs()uint64': []
  }
}

/**
 * The return type for each method
 */
export type LiveProofReturns = {
  'initialize()void': void
  'anchor_proof(string,string,string,uint64)void': void
  'get_proof(string)(string,string,string,uint64,address)': ProofRecord
  'proof_exists(string)bool': boolean
  'get_total_proofs()uint64': bigint
}

/**
 * Defines the types of available calls and state of the LiveProof smart contract.
 */
export type LiveProofTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'initialize()void' | 'initialize', {
      argsObj: LiveProofArgs['obj']['initialize()void']
      argsTuple: LiveProofArgs['tuple']['initialize()void']
      returns: LiveProofReturns['initialize()void']
    }>
    & Record<'anchor_proof(string,string,string,uint64)void' | 'anchor_proof', {
      argsObj: LiveProofArgs['obj']['anchor_proof(string,string,string,uint64)void']
      argsTuple: LiveProofArgs['tuple']['anchor_proof(string,string,string,uint64)void']
      returns: LiveProofReturns['anchor_proof(string,string,string,uint64)void']
    }>
    & Record<'get_proof(string)(string,string,string,uint64,address)' | 'get_proof', {
      argsObj: LiveProofArgs['obj']['get_proof(string)(string,string,string,uint64,address)']
      argsTuple: LiveProofArgs['tuple']['get_proof(string)(string,string,string,uint64,address)']
      returns: LiveProofReturns['get_proof(string)(string,string,string,uint64,address)']
    }>
    & Record<'proof_exists(string)bool' | 'proof_exists', {
      argsObj: LiveProofArgs['obj']['proof_exists(string)bool']
      argsTuple: LiveProofArgs['tuple']['proof_exists(string)bool']
      returns: LiveProofReturns['proof_exists(string)bool']
    }>
    & Record<'get_total_proofs()uint64' | 'get_total_proofs', {
      argsObj: LiveProofArgs['obj']['get_total_proofs()uint64']
      argsTuple: LiveProofArgs['tuple']['get_total_proofs()uint64']
      returns: LiveProofReturns['get_total_proofs()uint64']
    }>
  /**
   * Defines the shape of the state of the application.
   */
  state: {
    global: {
      keys: {
        totalProofs: bigint
        owner: string
      }
      maps: {}
    }
    box: {
      keys: {}
      maps: {
        proofs: Map<string, ProofRecord>
      }
    }
  }
}

/**
 * Defines the possible abi call signatures.
 */
export type LiveProofSignatures = keyof LiveProofTypes['methods']
/**
 * Defines the possible abi call signatures for methods that return a non-void value.
 */
export type LiveProofNonVoidMethodSignatures = keyof LiveProofTypes['methods'] extends infer T ? T extends keyof LiveProofTypes['methods'] ? MethodReturn<T> extends void ? never : T  : never : never
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the LiveProof smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends LiveProofSignatures> = LiveProofTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the LiveProof smart contract to the method's return type
 */
export type MethodReturn<TSignature extends LiveProofSignatures> = LiveProofTypes['methods'][TSignature]['returns']

/**
 * Defines the shape of the keyed global state of the application.
 */
export type GlobalKeysState = LiveProofTypes['state']['global']['keys']

/**
 * Defines the shape of the keyed box state of the application.
 */
export type BoxKeysState = LiveProofTypes['state']['box']['keys']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the LiveProof smart contract
 */
export abstract class LiveProofParamsFactory {
  /**
   * Constructs a no op call for the anchor_proof(string,string,string,uint64)void ABI method
   *
  * Anchor a knowledge node proof on-chain.
  Anyone can anchor — the caller's address is recorded as creator. node_id must be unique.

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static anchorProof(params: CallParams<LiveProofArgs['obj']['anchor_proof(string,string,string,uint64)void'] | LiveProofArgs['tuple']['anchor_proof(string,string,string,uint64)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'anchor_proof(string,string,string,uint64)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.nodeId, params.args.textHash, params.args.embeddingHash, params.args.timestamp],
    }
  }
  /**
   * Constructs a no op call for the get_proof(string)(string,string,string,uint64,address) ABI method
   *
  * Fetch a proof record by node_id.
  Frontend calls this to display verification details.

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static getProof(params: CallParams<LiveProofArgs['obj']['get_proof(string)(string,string,string,uint64,address)'] | LiveProofArgs['tuple']['get_proof(string)(string,string,string,uint64,address)']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'get_proof(string)(string,string,string,uint64,address)' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.nodeId],
    }
  }
  /**
   * Constructs a no op call for the proof_exists(string)bool ABI method
   *
   * Check if a proof exists without fetching the full record.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static proofExists(params: CallParams<LiveProofArgs['obj']['proof_exists(string)bool'] | LiveProofArgs['tuple']['proof_exists(string)bool']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'proof_exists(string)bool' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.nodeId],
    }
  }
  /**
   * Constructs a no op call for the get_total_proofs()uint64 ABI method
   *
   * Return total number of proofs anchored.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static getTotalProofs(params: CallParams<LiveProofArgs['obj']['get_total_proofs()uint64'] | LiveProofArgs['tuple']['get_total_proofs()uint64']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'get_total_proofs()uint64' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
}

/**
 * A client to make calls to the LiveProof smart contract
 */
export class LiveProofClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `LiveProofClient`
   *
   * @param appClient An `AppClient` instance which has been created with the LiveProof app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `LiveProofClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Checks for decode errors on the given return value and maps the return value to the return type for the given method
   * @returns The typed return value or undefined if there was no value
   */
  decodeReturnValue<TSignature extends LiveProofNonVoidMethodSignatures>(method: TSignature, returnValue: ABIReturn | undefined) {
    return returnValue !== undefined ? getArc56ReturnValue<MethodReturn<TSignature>>(returnValue, this.appClient.getABIMethod(method), APP_SPEC.structs) : undefined
  }

  /**
   * Returns a new `LiveProofClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<LiveProofClient> {
    return new LiveProofClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `LiveProofClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<LiveProofClient> {
    return new LiveProofClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Makes a clear_state call to an existing instance of the LiveProof smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the LiveProof smart contract using the `anchor_proof(string,string,string,uint64)void` ABI method.
     *
    * Anchor a knowledge node proof on-chain.
    Anyone can anchor — the caller's address is recorded as creator. node_id must be unique.

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    anchorProof: (params: CallParams<LiveProofArgs['obj']['anchor_proof(string,string,string,uint64)void'] | LiveProofArgs['tuple']['anchor_proof(string,string,string,uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(LiveProofParamsFactory.anchorProof(params))
    },

    /**
     * Makes a call to the LiveProof smart contract using the `get_proof(string)(string,string,string,uint64,address)` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
    * Fetch a proof record by node_id.
    Frontend calls this to display verification details.

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    getProof: (params: CallParams<LiveProofArgs['obj']['get_proof(string)(string,string,string,uint64,address)'] | LiveProofArgs['tuple']['get_proof(string)(string,string,string,uint64,address)']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(LiveProofParamsFactory.getProof(params))
    },

    /**
     * Makes a call to the LiveProof smart contract using the `proof_exists(string)bool` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * Check if a proof exists without fetching the full record.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    proofExists: (params: CallParams<LiveProofArgs['obj']['proof_exists(string)bool'] | LiveProofArgs['tuple']['proof_exists(string)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(LiveProofParamsFactory.proofExists(params))
    },

    /**
     * Makes a call to the LiveProof smart contract using the `get_total_proofs()uint64` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * Return total number of proofs anchored.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    getTotalProofs: (params: CallParams<LiveProofArgs['obj']['get_total_proofs()uint64'] | LiveProofArgs['tuple']['get_total_proofs()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(LiveProofParamsFactory.getTotalProofs(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Makes a clear_state call to an existing instance of the LiveProof smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the LiveProof smart contract using the `anchor_proof(string,string,string,uint64)void` ABI method.
     *
    * Anchor a knowledge node proof on-chain.
    Anyone can anchor — the caller's address is recorded as creator. node_id must be unique.

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    anchorProof: (params: CallParams<LiveProofArgs['obj']['anchor_proof(string,string,string,uint64)void'] | LiveProofArgs['tuple']['anchor_proof(string,string,string,uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(LiveProofParamsFactory.anchorProof(params))
    },

    /**
     * Makes a call to the LiveProof smart contract using the `get_proof(string)(string,string,string,uint64,address)` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
    * Fetch a proof record by node_id.
    Frontend calls this to display verification details.

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    getProof: (params: CallParams<LiveProofArgs['obj']['get_proof(string)(string,string,string,uint64,address)'] | LiveProofArgs['tuple']['get_proof(string)(string,string,string,uint64,address)']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(LiveProofParamsFactory.getProof(params))
    },

    /**
     * Makes a call to the LiveProof smart contract using the `proof_exists(string)bool` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * Check if a proof exists without fetching the full record.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    proofExists: (params: CallParams<LiveProofArgs['obj']['proof_exists(string)bool'] | LiveProofArgs['tuple']['proof_exists(string)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(LiveProofParamsFactory.proofExists(params))
    },

    /**
     * Makes a call to the LiveProof smart contract using the `get_total_proofs()uint64` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * Return total number of proofs anchored.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    getTotalProofs: (params: CallParams<LiveProofArgs['obj']['get_total_proofs()uint64'] | LiveProofArgs['tuple']['get_total_proofs()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(LiveProofParamsFactory.getTotalProofs(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Makes a clear_state call to an existing instance of the LiveProof smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the LiveProof smart contract using the `anchor_proof(string,string,string,uint64)void` ABI method.
     *
    * Anchor a knowledge node proof on-chain.
    Anyone can anchor — the caller's address is recorded as creator. node_id must be unique.

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    anchorProof: async (params: CallParams<LiveProofArgs['obj']['anchor_proof(string,string,string,uint64)void'] | LiveProofArgs['tuple']['anchor_proof(string,string,string,uint64)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(LiveProofParamsFactory.anchorProof(params))
      return {...result, return: result.return as unknown as (undefined | LiveProofReturns['anchor_proof(string,string,string,uint64)void'])}
    },

    /**
     * Makes a call to the LiveProof smart contract using the `get_proof(string)(string,string,string,uint64,address)` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
    * Fetch a proof record by node_id.
    Frontend calls this to display verification details.

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    getProof: async (params: CallParams<LiveProofArgs['obj']['get_proof(string)(string,string,string,uint64,address)'] | LiveProofArgs['tuple']['get_proof(string)(string,string,string,uint64,address)']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(LiveProofParamsFactory.getProof(params))
      return {...result, return: result.return as unknown as (undefined | LiveProofReturns['get_proof(string)(string,string,string,uint64,address)'])}
    },

    /**
     * Makes a call to the LiveProof smart contract using the `proof_exists(string)bool` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * Check if a proof exists without fetching the full record.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    proofExists: async (params: CallParams<LiveProofArgs['obj']['proof_exists(string)bool'] | LiveProofArgs['tuple']['proof_exists(string)bool']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(LiveProofParamsFactory.proofExists(params))
      return {...result, return: result.return as unknown as (undefined | LiveProofReturns['proof_exists(string)bool'])}
    },

    /**
     * Makes a call to the LiveProof smart contract using the `get_total_proofs()uint64` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * Return total number of proofs anchored.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    getTotalProofs: async (params: CallParams<LiveProofArgs['obj']['get_total_proofs()uint64'] | LiveProofArgs['tuple']['get_total_proofs()uint64']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(LiveProofParamsFactory.getTotalProofs(params))
      return {...result, return: result.return as unknown as (undefined | LiveProofReturns['get_total_proofs()uint64'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new LiveProofClient(this.appClient.clone(params))
  }

  /**
   * Makes a readonly (simulated) call to the LiveProof smart contract using the `get_proof(string)(string,string,string,uint64,address)` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
  * Fetch a proof record by node_id.
  Frontend calls this to display verification details.

   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async getProof(params: CallParams<LiveProofArgs['obj']['get_proof(string)(string,string,string,uint64,address)'] | LiveProofArgs['tuple']['get_proof(string)(string,string,string,uint64,address)']>) {
    const result = await this.appClient.send.call(LiveProofParamsFactory.getProof(params))
    return result.return as unknown as LiveProofReturns['get_proof(string)(string,string,string,uint64,address)']
  }

  /**
   * Makes a readonly (simulated) call to the LiveProof smart contract using the `proof_exists(string)bool` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * Check if a proof exists without fetching the full record.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async proofExists(params: CallParams<LiveProofArgs['obj']['proof_exists(string)bool'] | LiveProofArgs['tuple']['proof_exists(string)bool']>) {
    const result = await this.appClient.send.call(LiveProofParamsFactory.proofExists(params))
    return result.return as unknown as LiveProofReturns['proof_exists(string)bool']
  }

  /**
   * Makes a readonly (simulated) call to the LiveProof smart contract using the `get_total_proofs()uint64` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * Return total number of proofs anchored.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async getTotalProofs(params: CallParams<LiveProofArgs['obj']['get_total_proofs()uint64'] | LiveProofArgs['tuple']['get_total_proofs()uint64']> = {args: []}) {
    const result = await this.appClient.send.call(LiveProofParamsFactory.getTotalProofs(params))
    return result.return as unknown as LiveProofReturns['get_total_proofs()uint64']
  }

  /**
   * Methods to access state for the current LiveProof app
   */
  state = {
    /**
     * Methods to access global state for the current LiveProof app
     */
    global: {
      /**
       * Get all current keyed values from global state
       */
      getAll: async (): Promise<Partial<Expand<GlobalKeysState>>> => {
        const result = await this.appClient.state.global.getAll()
        return {
          totalProofs: result.total_proofs,
          owner: result.owner,
        }
      },
      /**
       * Get the current value of the total_proofs key in global state
       */
      totalProofs: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("total_proofs")) as bigint | undefined },
      /**
       * Get the current value of the owner key in global state
       */
      owner: async (): Promise<string | undefined> => { return (await this.appClient.state.global.getValue("owner")) as string | undefined },
    },
    /**
     * Methods to access box state for the current LiveProof app
     */
    box: {
      /**
       * Get all current keyed values from box state
       */
      getAll: async (): Promise<Partial<Expand<BoxKeysState>>> => {
        const result = await this.appClient.state.box.getAll()
        return {
        }
      },
      /**
       * Get values from the proofs map in box state
       */
      proofs: {
        /**
         * Get all current values of the proofs map in box state
         */
        getMap: async (): Promise<Map<string, ProofRecord>> => { return (await this.appClient.state.box.getMap("proofs")) as Map<string, ProofRecord> },
        /**
         * Get a current value of the proofs map by key from box state
         */
        value: async (key: string): Promise<ProofRecord | undefined> => { return await this.appClient.state.box.getMapValue("proofs", key) as ProofRecord | undefined },
      },
    },
  }

  public newGroup(): LiveProofComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a anchor_proof(string,string,string,uint64)void method call against the LiveProof contract
       */
      anchorProof(params: CallParams<LiveProofArgs['obj']['anchor_proof(string,string,string,uint64)void'] | LiveProofArgs['tuple']['anchor_proof(string,string,string,uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.anchorProof(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a get_proof(string)(string,string,string,uint64,address) method call against the LiveProof contract
       */
      getProof(params: CallParams<LiveProofArgs['obj']['get_proof(string)(string,string,string,uint64,address)'] | LiveProofArgs['tuple']['get_proof(string)(string,string,string,uint64,address)']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.getProof(params)))
        resultMappers.push((v) => client.decodeReturnValue('get_proof(string)(string,string,string,uint64,address)', v))
        return this
      },
      /**
       * Add a proof_exists(string)bool method call against the LiveProof contract
       */
      proofExists(params: CallParams<LiveProofArgs['obj']['proof_exists(string)bool'] | LiveProofArgs['tuple']['proof_exists(string)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.proofExists(params)))
        resultMappers.push((v) => client.decodeReturnValue('proof_exists(string)bool', v))
        return this
      },
      /**
       * Add a get_total_proofs()uint64 method call against the LiveProof contract
       */
      getTotalProofs(params: CallParams<LiveProofArgs['obj']['get_total_proofs()uint64'] | LiveProofArgs['tuple']['get_total_proofs()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.getTotalProofs(params)))
        resultMappers.push((v) => client.decodeReturnValue('get_total_proofs()uint64', v))
        return this
      },
      /**
       * Add a clear state call to the LiveProof contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as LiveProofComposer
  }
}
export type LiveProofComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the anchor_proof(string,string,string,uint64)void ABI method.
   *
  * Anchor a knowledge node proof on-chain.
  Anyone can anchor — the caller's address is recorded as creator. node_id must be unique.

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  anchorProof(params?: CallParams<LiveProofArgs['obj']['anchor_proof(string,string,string,uint64)void'] | LiveProofArgs['tuple']['anchor_proof(string,string,string,uint64)void']>): LiveProofComposer<[...TReturns, LiveProofReturns['anchor_proof(string,string,string,uint64)void'] | undefined]>

  /**
   * Calls the get_proof(string)(string,string,string,uint64,address) ABI method.
   *
  * Fetch a proof record by node_id.
  Frontend calls this to display verification details.

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  getProof(params?: CallParams<LiveProofArgs['obj']['get_proof(string)(string,string,string,uint64,address)'] | LiveProofArgs['tuple']['get_proof(string)(string,string,string,uint64,address)']>): LiveProofComposer<[...TReturns, LiveProofReturns['get_proof(string)(string,string,string,uint64,address)'] | undefined]>

  /**
   * Calls the proof_exists(string)bool ABI method.
   *
   * Check if a proof exists without fetching the full record.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  proofExists(params?: CallParams<LiveProofArgs['obj']['proof_exists(string)bool'] | LiveProofArgs['tuple']['proof_exists(string)bool']>): LiveProofComposer<[...TReturns, LiveProofReturns['proof_exists(string)bool'] | undefined]>

  /**
   * Calls the get_total_proofs()uint64 ABI method.
   *
   * Return total number of proofs anchored.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  getTotalProofs(params?: CallParams<LiveProofArgs['obj']['get_total_proofs()uint64'] | LiveProofArgs['tuple']['get_total_proofs()uint64']>): LiveProofComposer<[...TReturns, LiveProofReturns['get_total_proofs()uint64'] | undefined]>

  /**
   * Makes a clear_state call to an existing instance of the LiveProof smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): LiveProofComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): LiveProofComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<LiveProofComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<LiveProofComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<LiveProofComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<LiveProofComposerResults<TReturns>>
}
export type LiveProofComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

