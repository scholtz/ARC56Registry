[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "arc56-generated-maiko_965b59a0"
version = "1.2.2026072010"
description = "Generated ARC-56 Algorand smart-contract clients for maikoll120/algokit_workshop."
readme = "README.md"
requires-python = ">=3.10"
authors = [{ name = "maikoll120" }]
keywords = ["algorand", "arc56", "avm", "smart-contract", "generated-client", "maikoll120", "algokit_workshop"]
dependencies = [
    "algokit-utils>=4.2.3,<5.0.0",
    "py-algorand-sdk>=2.11.1,<3.0.0",
]

[project.urls]
Homepage = "https://github.com/scholtz/Arc56Registry"
Repository = "https://github.com/scholtz/Arc56Registry"

[tool.hatch.build.targets.wheel]
packages = ["src/arc56_generated_maiko_965b59a0"]
