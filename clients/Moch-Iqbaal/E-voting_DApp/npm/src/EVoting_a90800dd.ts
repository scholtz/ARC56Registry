/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"EVoting","structs":{},"methods":[{"name":"add_candidate","args":[{"type":"string","name":"name"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Tambah kandidat baru, hanya admin","events":[],"recommendations":{}},{"name":"vote","args":[{"type":"string","name":"candidate"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Voting untuk kandidat tertentu","events":[],"recommendations":{}},{"name":"get_votes","args":[{"type":"string","name":"candidate"}],"returns":{"type":"uint64"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Lihat jumlah suara kandidat","events":[],"recommendations":{}}],"arcs":[22,28],"networks":{},"state":{"schema":{"global":{"ints":0,"bytes":1},"local":{"ints":0,"bytes":0}},"keys":{"global":{"admin":{"keyType":"AVMString","valueType":"address","key":"YWRtaW4="}},"local":{},"box":{}},"maps":{"global":{},"local":{},"box":{"candidates":{"keyType":"AVMString","valueType":"uint64","prefix":"Y2FuZGlkYXRlcw=="},"voters":{"keyType":"address","valueType":"uint64","prefix":"dm90ZXJz"}}}},"bareActions":{"create":["NoOp"],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[115],"errorMessage":"Hanya admin yang boleh menambah kandidat"},{"pc":[148],"errorMessage":"Kamu sudah voting!"},{"pc":[156,197],"errorMessage":"Kandidat tidak ditemukan!"},{"pc":[49],"errorMessage":"OnCompletion must be NoOp"},{"pc":[89],"errorMessage":"OnCompletion must be NoOp && can only call when creating"},{"pc":[113],"errorMessage":"check self.admin exists"},{"pc":[97,129,179],"errorMessage":"invalid array length header"},{"pc":[104,136,186],"errorMessage":"invalid number of bytes for arc4.dynamic_array<arc4.uint8>"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"events":[]} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


/**
 * The argument types for the EVoting contract
 */
export type EVotingArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'add_candidate(string)void': {
      name: string
    }
    'vote(string)void': {
      candidate: string
    }
    'get_votes(string)uint64': {
      candidate: string
    }
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'add_candidate(string)void': [name: string]
    'vote(string)void': [candidate: string]
    'get_votes(string)uint64': [candidate: string]
  }
}

/**
 * The return type for each method
 */
export type EVotingReturns = {
  'add_candidate(string)void': void
  'vote(string)void': void
  'get_votes(string)uint64': bigint
}

/**
 * Defines the types of available calls and state of the EVoting smart contract.
 */
export type EVotingTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'add_candidate(string)void' | 'add_candidate', {
      argsObj: EVotingArgs['obj']['add_candidate(string)void']
      argsTuple: EVotingArgs['tuple']['add_candidate(string)void']
      returns: EVotingReturns['add_candidate(string)void']
    }>
    & Record<'vote(string)void' | 'vote', {
      argsObj: EVotingArgs['obj']['vote(string)void']
      argsTuple: EVotingArgs['tuple']['vote(string)void']
      returns: EVotingReturns['vote(string)void']
    }>
    & Record<'get_votes(string)uint64' | 'get_votes', {
      argsObj: EVotingArgs['obj']['get_votes(string)uint64']
      argsTuple: EVotingArgs['tuple']['get_votes(string)uint64']
      returns: EVotingReturns['get_votes(string)uint64']
    }>
  /**
   * Defines the shape of the state of the application.
   */
  state: {
    global: {
      keys: {
        admin: string
      }
      maps: {}
    }
    box: {
      keys: {}
      maps: {
        candidates: Map<string, bigint>
        voters: Map<string, bigint>
      }
    }
  }
}

/**
 * Defines the possible abi call signatures.
 */
export type EVotingSignatures = keyof EVotingTypes['methods']
/**
 * Defines the possible abi call signatures for methods that return a non-void value.
 */
export type EVotingNonVoidMethodSignatures = keyof EVotingTypes['methods'] extends infer T ? T extends keyof EVotingTypes['methods'] ? MethodReturn<T> extends void ? never : T  : never : never
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the EVoting smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends EVotingSignatures> = EVotingTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the EVoting smart contract to the method's return type
 */
export type MethodReturn<TSignature extends EVotingSignatures> = EVotingTypes['methods'][TSignature]['returns']

/**
 * Defines the shape of the keyed global state of the application.
 */
export type GlobalKeysState = EVotingTypes['state']['global']['keys']

/**
 * Defines the shape of the keyed box state of the application.
 */
export type BoxKeysState = EVotingTypes['state']['box']['keys']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the EVoting smart contract
 */
export abstract class EVotingParamsFactory {
  /**
   * Constructs a no op call for the add_candidate(string)void ABI method
   *
   * Tambah kandidat baru, hanya admin
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static addCandidate(params: CallParams<EVotingArgs['obj']['add_candidate(string)void'] | EVotingArgs['tuple']['add_candidate(string)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'add_candidate(string)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.name],
    }
  }
  /**
   * Constructs a no op call for the vote(string)void ABI method
   *
   * Voting untuk kandidat tertentu
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static vote(params: CallParams<EVotingArgs['obj']['vote(string)void'] | EVotingArgs['tuple']['vote(string)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'vote(string)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.candidate],
    }
  }
  /**
   * Constructs a no op call for the get_votes(string)uint64 ABI method
   *
   * Lihat jumlah suara kandidat
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static getVotes(params: CallParams<EVotingArgs['obj']['get_votes(string)uint64'] | EVotingArgs['tuple']['get_votes(string)uint64']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'get_votes(string)uint64' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.candidate],
    }
  }
}

/**
 * A client to make calls to the EVoting smart contract
 */
export class EVotingClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `EVotingClient`
   *
   * @param appClient An `AppClient` instance which has been created with the EVoting app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `EVotingClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Checks for decode errors on the given return value and maps the return value to the return type for the given method
   * @returns The typed return value or undefined if there was no value
   */
  decodeReturnValue<TSignature extends EVotingNonVoidMethodSignatures>(method: TSignature, returnValue: ABIReturn | undefined) {
    return returnValue !== undefined ? getArc56ReturnValue<MethodReturn<TSignature>>(returnValue, this.appClient.getABIMethod(method), APP_SPEC.structs) : undefined
  }

  /**
   * Returns a new `EVotingClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<EVotingClient> {
    return new EVotingClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `EVotingClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<EVotingClient> {
    return new EVotingClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Makes a clear_state call to an existing instance of the EVoting smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the EVoting smart contract using the `add_candidate(string)void` ABI method.
     *
     * Tambah kandidat baru, hanya admin
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    addCandidate: (params: CallParams<EVotingArgs['obj']['add_candidate(string)void'] | EVotingArgs['tuple']['add_candidate(string)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(EVotingParamsFactory.addCandidate(params))
    },

    /**
     * Makes a call to the EVoting smart contract using the `vote(string)void` ABI method.
     *
     * Voting untuk kandidat tertentu
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    vote: (params: CallParams<EVotingArgs['obj']['vote(string)void'] | EVotingArgs['tuple']['vote(string)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(EVotingParamsFactory.vote(params))
    },

    /**
     * Makes a call to the EVoting smart contract using the `get_votes(string)uint64` ABI method.
     *
     * Lihat jumlah suara kandidat
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    getVotes: (params: CallParams<EVotingArgs['obj']['get_votes(string)uint64'] | EVotingArgs['tuple']['get_votes(string)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(EVotingParamsFactory.getVotes(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Makes a clear_state call to an existing instance of the EVoting smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the EVoting smart contract using the `add_candidate(string)void` ABI method.
     *
     * Tambah kandidat baru, hanya admin
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    addCandidate: (params: CallParams<EVotingArgs['obj']['add_candidate(string)void'] | EVotingArgs['tuple']['add_candidate(string)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(EVotingParamsFactory.addCandidate(params))
    },

    /**
     * Makes a call to the EVoting smart contract using the `vote(string)void` ABI method.
     *
     * Voting untuk kandidat tertentu
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    vote: (params: CallParams<EVotingArgs['obj']['vote(string)void'] | EVotingArgs['tuple']['vote(string)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(EVotingParamsFactory.vote(params))
    },

    /**
     * Makes a call to the EVoting smart contract using the `get_votes(string)uint64` ABI method.
     *
     * Lihat jumlah suara kandidat
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    getVotes: (params: CallParams<EVotingArgs['obj']['get_votes(string)uint64'] | EVotingArgs['tuple']['get_votes(string)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(EVotingParamsFactory.getVotes(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Makes a clear_state call to an existing instance of the EVoting smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the EVoting smart contract using the `add_candidate(string)void` ABI method.
     *
     * Tambah kandidat baru, hanya admin
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    addCandidate: async (params: CallParams<EVotingArgs['obj']['add_candidate(string)void'] | EVotingArgs['tuple']['add_candidate(string)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(EVotingParamsFactory.addCandidate(params))
      return {...result, return: result.return as unknown as (undefined | EVotingReturns['add_candidate(string)void'])}
    },

    /**
     * Makes a call to the EVoting smart contract using the `vote(string)void` ABI method.
     *
     * Voting untuk kandidat tertentu
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    vote: async (params: CallParams<EVotingArgs['obj']['vote(string)void'] | EVotingArgs['tuple']['vote(string)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(EVotingParamsFactory.vote(params))
      return {...result, return: result.return as unknown as (undefined | EVotingReturns['vote(string)void'])}
    },

    /**
     * Makes a call to the EVoting smart contract using the `get_votes(string)uint64` ABI method.
     *
     * Lihat jumlah suara kandidat
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    getVotes: async (params: CallParams<EVotingArgs['obj']['get_votes(string)uint64'] | EVotingArgs['tuple']['get_votes(string)uint64']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(EVotingParamsFactory.getVotes(params))
      return {...result, return: result.return as unknown as (undefined | EVotingReturns['get_votes(string)uint64'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new EVotingClient(this.appClient.clone(params))
  }

  /**
   * Methods to access state for the current EVoting app
   */
  state = {
    /**
     * Methods to access global state for the current EVoting app
     */
    global: {
      /**
       * Get all current keyed values from global state
       */
      getAll: async (): Promise<Partial<Expand<GlobalKeysState>>> => {
        const result = await this.appClient.state.global.getAll()
        return {
          admin: result.admin,
        }
      },
      /**
       * Get the current value of the admin key in global state
       */
      admin: async (): Promise<string | undefined> => { return (await this.appClient.state.global.getValue("admin")) as string | undefined },
    },
    /**
     * Methods to access box state for the current EVoting app
     */
    box: {
      /**
       * Get all current keyed values from box state
       */
      getAll: async (): Promise<Partial<Expand<BoxKeysState>>> => {
        const result = await this.appClient.state.box.getAll()
        return {
        }
      },
      /**
       * Get values from the candidates map in box state
       */
      candidates: {
        /**
         * Get all current values of the candidates map in box state
         */
        getMap: async (): Promise<Map<string, bigint>> => { return (await this.appClient.state.box.getMap("candidates")) as Map<string, bigint> },
        /**
         * Get a current value of the candidates map by key from box state
         */
        value: async (key: string): Promise<bigint | undefined> => { return await this.appClient.state.box.getMapValue("candidates", key) as bigint | undefined },
      },
      /**
       * Get values from the voters map in box state
       */
      voters: {
        /**
         * Get all current values of the voters map in box state
         */
        getMap: async (): Promise<Map<string, bigint>> => { return (await this.appClient.state.box.getMap("voters")) as Map<string, bigint> },
        /**
         * Get a current value of the voters map by key from box state
         */
        value: async (key: string): Promise<bigint | undefined> => { return await this.appClient.state.box.getMapValue("voters", key) as bigint | undefined },
      },
    },
  }

  public newGroup(): EVotingComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a add_candidate(string)void method call against the EVoting contract
       */
      addCandidate(params: CallParams<EVotingArgs['obj']['add_candidate(string)void'] | EVotingArgs['tuple']['add_candidate(string)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.addCandidate(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a vote(string)void method call against the EVoting contract
       */
      vote(params: CallParams<EVotingArgs['obj']['vote(string)void'] | EVotingArgs['tuple']['vote(string)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.vote(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a get_votes(string)uint64 method call against the EVoting contract
       */
      getVotes(params: CallParams<EVotingArgs['obj']['get_votes(string)uint64'] | EVotingArgs['tuple']['get_votes(string)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.getVotes(params)))
        resultMappers.push((v) => client.decodeReturnValue('get_votes(string)uint64', v))
        return this
      },
      /**
       * Add a clear state call to the EVoting contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as EVotingComposer
  }
}
export type EVotingComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the add_candidate(string)void ABI method.
   *
   * Tambah kandidat baru, hanya admin
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  addCandidate(params?: CallParams<EVotingArgs['obj']['add_candidate(string)void'] | EVotingArgs['tuple']['add_candidate(string)void']>): EVotingComposer<[...TReturns, EVotingReturns['add_candidate(string)void'] | undefined]>

  /**
   * Calls the vote(string)void ABI method.
   *
   * Voting untuk kandidat tertentu
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  vote(params?: CallParams<EVotingArgs['obj']['vote(string)void'] | EVotingArgs['tuple']['vote(string)void']>): EVotingComposer<[...TReturns, EVotingReturns['vote(string)void'] | undefined]>

  /**
   * Calls the get_votes(string)uint64 ABI method.
   *
   * Lihat jumlah suara kandidat
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  getVotes(params?: CallParams<EVotingArgs['obj']['get_votes(string)uint64'] | EVotingArgs['tuple']['get_votes(string)uint64']>): EVotingComposer<[...TReturns, EVotingReturns['get_votes(string)uint64'] | undefined]>

  /**
   * Makes a clear_state call to an existing instance of the EVoting smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): EVotingComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): EVotingComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<EVotingComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<EVotingComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<EVotingComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<EVotingComposerResults<TReturns>>
}
export type EVotingComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

