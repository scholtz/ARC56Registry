/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"TodoApp","structs":{},"methods":[{"name":"create_todo","args":[{"type":"string","name":"task"}],"returns":{"type":"uint64"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Create a new todo item.\nReturns the ID of the created todo.","events":[],"recommendations":{}},{"name":"complete_todo","args":[{"type":"uint64","name":"todo_id"}],"returns":{"type":"string"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Mark a todo as completed.\nReturns a success message.","events":[],"recommendations":{}},{"name":"get_todo","args":[{"type":"uint64","name":"todo_id"}],"returns":{"type":"string"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Get a specific todo by its ID.\nReturns the todo text.","events":[],"recommendations":{}},{"name":"is_completed","args":[{"type":"uint64","name":"todo_id"}],"returns":{"type":"bool"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Check if a todo is completed.\nReturns True if completed, False otherwise.","events":[],"recommendations":{}},{"name":"get_total_todos","args":[],"returns":{"type":"uint64"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Get the total number of todos created.","events":[],"recommendations":{}},{"name":"delete_todo","args":[{"type":"uint64","name":"todo_id"}],"returns":{"type":"string"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Delete a todo item.\nReturns a success message.","events":[],"recommendations":{}}],"arcs":[22,28],"desc":"\n    A simple Todo application smart contract for Algorand.\n    Allows users to create, complete, and view their todos.\n    ","networks":{},"state":{"schema":{"global":{"ints":1,"bytes":0},"local":{"ints":0,"bytes":0}},"keys":{"global":{"todo_counter":{"keyType":"AVMString","valueType":"AVMUint64","key":"dG9kb19jb3VudGVy"}},"local":{},"box":{}},"maps":{"global":{},"local":{},"box":{"todos":{"keyType":"uint64","valueType":"string","prefix":"dG9kb3M="},"completed":{"keyType":"uint64","valueType":"bool","prefix":"Y29tcGxldGVk"}}}},"bareActions":{"create":["NoOp"],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[75],"errorMessage":"OnCompletion must be NoOp"},{"pc":[136],"errorMessage":"OnCompletion must be NoOp && can only call when creating"},{"pc":[315],"errorMessage":"check self.completed entry exists"},{"pc":[156,322],"errorMessage":"check self.todo_counter exists"},{"pc":[282],"errorMessage":"check self.todos entry exists"},{"pc":[144],"errorMessage":"invalid array length header"},{"pc":[152],"errorMessage":"invalid number of bytes for arc4.dynamic_array<arc4.uint8>"},{"pc":[196,261,293,338],"errorMessage":"invalid number of bytes for arc4.uint64"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"events":[]} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


/**
 * The argument types for the TodoApp contract
 */
export type TodoAppArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'create_todo(string)uint64': {
      task: string
    }
    'complete_todo(uint64)string': {
      todoId: bigint | number
    }
    'get_todo(uint64)string': {
      todoId: bigint | number
    }
    'is_completed(uint64)bool': {
      todoId: bigint | number
    }
    'get_total_todos()uint64': Record<string, never>
    'delete_todo(uint64)string': {
      todoId: bigint | number
    }
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'create_todo(string)uint64': [task: string]
    'complete_todo(uint64)string': [todoId: bigint | number]
    'get_todo(uint64)string': [todoId: bigint | number]
    'is_completed(uint64)bool': [todoId: bigint | number]
    'get_total_todos()uint64': []
    'delete_todo(uint64)string': [todoId: bigint | number]
  }
}

/**
 * The return type for each method
 */
export type TodoAppReturns = {
  'create_todo(string)uint64': bigint
  'complete_todo(uint64)string': string
  'get_todo(uint64)string': string
  'is_completed(uint64)bool': boolean
  'get_total_todos()uint64': bigint
  'delete_todo(uint64)string': string
}

/**
 * Defines the types of available calls and state of the TodoApp smart contract.
 */
export type TodoAppTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'create_todo(string)uint64' | 'create_todo', {
      argsObj: TodoAppArgs['obj']['create_todo(string)uint64']
      argsTuple: TodoAppArgs['tuple']['create_todo(string)uint64']
      returns: TodoAppReturns['create_todo(string)uint64']
    }>
    & Record<'complete_todo(uint64)string' | 'complete_todo', {
      argsObj: TodoAppArgs['obj']['complete_todo(uint64)string']
      argsTuple: TodoAppArgs['tuple']['complete_todo(uint64)string']
      returns: TodoAppReturns['complete_todo(uint64)string']
    }>
    & Record<'get_todo(uint64)string' | 'get_todo', {
      argsObj: TodoAppArgs['obj']['get_todo(uint64)string']
      argsTuple: TodoAppArgs['tuple']['get_todo(uint64)string']
      returns: TodoAppReturns['get_todo(uint64)string']
    }>
    & Record<'is_completed(uint64)bool' | 'is_completed', {
      argsObj: TodoAppArgs['obj']['is_completed(uint64)bool']
      argsTuple: TodoAppArgs['tuple']['is_completed(uint64)bool']
      returns: TodoAppReturns['is_completed(uint64)bool']
    }>
    & Record<'get_total_todos()uint64' | 'get_total_todos', {
      argsObj: TodoAppArgs['obj']['get_total_todos()uint64']
      argsTuple: TodoAppArgs['tuple']['get_total_todos()uint64']
      returns: TodoAppReturns['get_total_todos()uint64']
    }>
    & Record<'delete_todo(uint64)string' | 'delete_todo', {
      argsObj: TodoAppArgs['obj']['delete_todo(uint64)string']
      argsTuple: TodoAppArgs['tuple']['delete_todo(uint64)string']
      returns: TodoAppReturns['delete_todo(uint64)string']
    }>
  /**
   * Defines the shape of the state of the application.
   */
  state: {
    global: {
      keys: {
        todoCounter: bigint
      }
      maps: {}
    }
    box: {
      keys: {}
      maps: {
        todos: Map<bigint | number, string>
        completed: Map<bigint | number, boolean>
      }
    }
  }
}

/**
 * Defines the possible abi call signatures.
 */
export type TodoAppSignatures = keyof TodoAppTypes['methods']
/**
 * Defines the possible abi call signatures for methods that return a non-void value.
 */
export type TodoAppNonVoidMethodSignatures = keyof TodoAppTypes['methods'] extends infer T ? T extends keyof TodoAppTypes['methods'] ? MethodReturn<T> extends void ? never : T  : never : never
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the TodoApp smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends TodoAppSignatures> = TodoAppTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the TodoApp smart contract to the method's return type
 */
export type MethodReturn<TSignature extends TodoAppSignatures> = TodoAppTypes['methods'][TSignature]['returns']

/**
 * Defines the shape of the keyed global state of the application.
 */
export type GlobalKeysState = TodoAppTypes['state']['global']['keys']

/**
 * Defines the shape of the keyed box state of the application.
 */
export type BoxKeysState = TodoAppTypes['state']['box']['keys']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the TodoApp smart contract
 */
export abstract class TodoAppParamsFactory {
  /**
   * Constructs a no op call for the create_todo(string)uint64 ABI method
   *
  * Create a new todo item.
  Returns the ID of the created todo.

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static createTodo(params: CallParams<TodoAppArgs['obj']['create_todo(string)uint64'] | TodoAppArgs['tuple']['create_todo(string)uint64']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'create_todo(string)uint64' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.task],
    }
  }
  /**
   * Constructs a no op call for the complete_todo(uint64)string ABI method
   *
  * Mark a todo as completed.
  Returns a success message.

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static completeTodo(params: CallParams<TodoAppArgs['obj']['complete_todo(uint64)string'] | TodoAppArgs['tuple']['complete_todo(uint64)string']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'complete_todo(uint64)string' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.todoId],
    }
  }
  /**
   * Constructs a no op call for the get_todo(uint64)string ABI method
   *
  * Get a specific todo by its ID.
  Returns the todo text.

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static getTodo(params: CallParams<TodoAppArgs['obj']['get_todo(uint64)string'] | TodoAppArgs['tuple']['get_todo(uint64)string']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'get_todo(uint64)string' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.todoId],
    }
  }
  /**
   * Constructs a no op call for the is_completed(uint64)bool ABI method
   *
  * Check if a todo is completed.
  Returns True if completed, False otherwise.

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static isCompleted(params: CallParams<TodoAppArgs['obj']['is_completed(uint64)bool'] | TodoAppArgs['tuple']['is_completed(uint64)bool']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'is_completed(uint64)bool' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.todoId],
    }
  }
  /**
   * Constructs a no op call for the get_total_todos()uint64 ABI method
   *
   * Get the total number of todos created.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static getTotalTodos(params: CallParams<TodoAppArgs['obj']['get_total_todos()uint64'] | TodoAppArgs['tuple']['get_total_todos()uint64']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'get_total_todos()uint64' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the delete_todo(uint64)string ABI method
   *
  * Delete a todo item.
  Returns a success message.

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static deleteTodo(params: CallParams<TodoAppArgs['obj']['delete_todo(uint64)string'] | TodoAppArgs['tuple']['delete_todo(uint64)string']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'delete_todo(uint64)string' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.todoId],
    }
  }
}

/**
 * A client to make calls to the TodoApp smart contract
 */
export class TodoAppClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `TodoAppClient`
   *
   * @param appClient An `AppClient` instance which has been created with the TodoApp app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `TodoAppClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Checks for decode errors on the given return value and maps the return value to the return type for the given method
   * @returns The typed return value or undefined if there was no value
   */
  decodeReturnValue<TSignature extends TodoAppNonVoidMethodSignatures>(method: TSignature, returnValue: ABIReturn | undefined) {
    return returnValue !== undefined ? getArc56ReturnValue<MethodReturn<TSignature>>(returnValue, this.appClient.getABIMethod(method), APP_SPEC.structs) : undefined
  }

  /**
   * Returns a new `TodoAppClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<TodoAppClient> {
    return new TodoAppClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `TodoAppClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<TodoAppClient> {
    return new TodoAppClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Makes a clear_state call to an existing instance of the TodoApp smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the TodoApp smart contract using the `create_todo(string)uint64` ABI method.
     *
    * Create a new todo item.
    Returns the ID of the created todo.

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    createTodo: (params: CallParams<TodoAppArgs['obj']['create_todo(string)uint64'] | TodoAppArgs['tuple']['create_todo(string)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(TodoAppParamsFactory.createTodo(params))
    },

    /**
     * Makes a call to the TodoApp smart contract using the `complete_todo(uint64)string` ABI method.
     *
    * Mark a todo as completed.
    Returns a success message.

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    completeTodo: (params: CallParams<TodoAppArgs['obj']['complete_todo(uint64)string'] | TodoAppArgs['tuple']['complete_todo(uint64)string']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(TodoAppParamsFactory.completeTodo(params))
    },

    /**
     * Makes a call to the TodoApp smart contract using the `get_todo(uint64)string` ABI method.
     *
    * Get a specific todo by its ID.
    Returns the todo text.

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    getTodo: (params: CallParams<TodoAppArgs['obj']['get_todo(uint64)string'] | TodoAppArgs['tuple']['get_todo(uint64)string']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(TodoAppParamsFactory.getTodo(params))
    },

    /**
     * Makes a call to the TodoApp smart contract using the `is_completed(uint64)bool` ABI method.
     *
    * Check if a todo is completed.
    Returns True if completed, False otherwise.

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    isCompleted: (params: CallParams<TodoAppArgs['obj']['is_completed(uint64)bool'] | TodoAppArgs['tuple']['is_completed(uint64)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(TodoAppParamsFactory.isCompleted(params))
    },

    /**
     * Makes a call to the TodoApp smart contract using the `get_total_todos()uint64` ABI method.
     *
     * Get the total number of todos created.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    getTotalTodos: (params: CallParams<TodoAppArgs['obj']['get_total_todos()uint64'] | TodoAppArgs['tuple']['get_total_todos()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(TodoAppParamsFactory.getTotalTodos(params))
    },

    /**
     * Makes a call to the TodoApp smart contract using the `delete_todo(uint64)string` ABI method.
     *
    * Delete a todo item.
    Returns a success message.

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    deleteTodo: (params: CallParams<TodoAppArgs['obj']['delete_todo(uint64)string'] | TodoAppArgs['tuple']['delete_todo(uint64)string']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(TodoAppParamsFactory.deleteTodo(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Makes a clear_state call to an existing instance of the TodoApp smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the TodoApp smart contract using the `create_todo(string)uint64` ABI method.
     *
    * Create a new todo item.
    Returns the ID of the created todo.

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    createTodo: (params: CallParams<TodoAppArgs['obj']['create_todo(string)uint64'] | TodoAppArgs['tuple']['create_todo(string)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(TodoAppParamsFactory.createTodo(params))
    },

    /**
     * Makes a call to the TodoApp smart contract using the `complete_todo(uint64)string` ABI method.
     *
    * Mark a todo as completed.
    Returns a success message.

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    completeTodo: (params: CallParams<TodoAppArgs['obj']['complete_todo(uint64)string'] | TodoAppArgs['tuple']['complete_todo(uint64)string']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(TodoAppParamsFactory.completeTodo(params))
    },

    /**
     * Makes a call to the TodoApp smart contract using the `get_todo(uint64)string` ABI method.
     *
    * Get a specific todo by its ID.
    Returns the todo text.

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    getTodo: (params: CallParams<TodoAppArgs['obj']['get_todo(uint64)string'] | TodoAppArgs['tuple']['get_todo(uint64)string']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(TodoAppParamsFactory.getTodo(params))
    },

    /**
     * Makes a call to the TodoApp smart contract using the `is_completed(uint64)bool` ABI method.
     *
    * Check if a todo is completed.
    Returns True if completed, False otherwise.

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    isCompleted: (params: CallParams<TodoAppArgs['obj']['is_completed(uint64)bool'] | TodoAppArgs['tuple']['is_completed(uint64)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(TodoAppParamsFactory.isCompleted(params))
    },

    /**
     * Makes a call to the TodoApp smart contract using the `get_total_todos()uint64` ABI method.
     *
     * Get the total number of todos created.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    getTotalTodos: (params: CallParams<TodoAppArgs['obj']['get_total_todos()uint64'] | TodoAppArgs['tuple']['get_total_todos()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(TodoAppParamsFactory.getTotalTodos(params))
    },

    /**
     * Makes a call to the TodoApp smart contract using the `delete_todo(uint64)string` ABI method.
     *
    * Delete a todo item.
    Returns a success message.

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    deleteTodo: (params: CallParams<TodoAppArgs['obj']['delete_todo(uint64)string'] | TodoAppArgs['tuple']['delete_todo(uint64)string']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(TodoAppParamsFactory.deleteTodo(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Makes a clear_state call to an existing instance of the TodoApp smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the TodoApp smart contract using the `create_todo(string)uint64` ABI method.
     *
    * Create a new todo item.
    Returns the ID of the created todo.

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    createTodo: async (params: CallParams<TodoAppArgs['obj']['create_todo(string)uint64'] | TodoAppArgs['tuple']['create_todo(string)uint64']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(TodoAppParamsFactory.createTodo(params))
      return {...result, return: result.return as unknown as (undefined | TodoAppReturns['create_todo(string)uint64'])}
    },

    /**
     * Makes a call to the TodoApp smart contract using the `complete_todo(uint64)string` ABI method.
     *
    * Mark a todo as completed.
    Returns a success message.

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    completeTodo: async (params: CallParams<TodoAppArgs['obj']['complete_todo(uint64)string'] | TodoAppArgs['tuple']['complete_todo(uint64)string']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(TodoAppParamsFactory.completeTodo(params))
      return {...result, return: result.return as unknown as (undefined | TodoAppReturns['complete_todo(uint64)string'])}
    },

    /**
     * Makes a call to the TodoApp smart contract using the `get_todo(uint64)string` ABI method.
     *
    * Get a specific todo by its ID.
    Returns the todo text.

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    getTodo: async (params: CallParams<TodoAppArgs['obj']['get_todo(uint64)string'] | TodoAppArgs['tuple']['get_todo(uint64)string']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(TodoAppParamsFactory.getTodo(params))
      return {...result, return: result.return as unknown as (undefined | TodoAppReturns['get_todo(uint64)string'])}
    },

    /**
     * Makes a call to the TodoApp smart contract using the `is_completed(uint64)bool` ABI method.
     *
    * Check if a todo is completed.
    Returns True if completed, False otherwise.

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    isCompleted: async (params: CallParams<TodoAppArgs['obj']['is_completed(uint64)bool'] | TodoAppArgs['tuple']['is_completed(uint64)bool']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(TodoAppParamsFactory.isCompleted(params))
      return {...result, return: result.return as unknown as (undefined | TodoAppReturns['is_completed(uint64)bool'])}
    },

    /**
     * Makes a call to the TodoApp smart contract using the `get_total_todos()uint64` ABI method.
     *
     * Get the total number of todos created.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    getTotalTodos: async (params: CallParams<TodoAppArgs['obj']['get_total_todos()uint64'] | TodoAppArgs['tuple']['get_total_todos()uint64']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(TodoAppParamsFactory.getTotalTodos(params))
      return {...result, return: result.return as unknown as (undefined | TodoAppReturns['get_total_todos()uint64'])}
    },

    /**
     * Makes a call to the TodoApp smart contract using the `delete_todo(uint64)string` ABI method.
     *
    * Delete a todo item.
    Returns a success message.

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    deleteTodo: async (params: CallParams<TodoAppArgs['obj']['delete_todo(uint64)string'] | TodoAppArgs['tuple']['delete_todo(uint64)string']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(TodoAppParamsFactory.deleteTodo(params))
      return {...result, return: result.return as unknown as (undefined | TodoAppReturns['delete_todo(uint64)string'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new TodoAppClient(this.appClient.clone(params))
  }

  /**
   * Methods to access state for the current TodoApp app
   */
  state = {
    /**
     * Methods to access global state for the current TodoApp app
     */
    global: {
      /**
       * Get all current keyed values from global state
       */
      getAll: async (): Promise<Partial<Expand<GlobalKeysState>>> => {
        const result = await this.appClient.state.global.getAll()
        return {
          todoCounter: result.todo_counter,
        }
      },
      /**
       * Get the current value of the todo_counter key in global state
       */
      todoCounter: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("todo_counter")) as bigint | undefined },
    },
    /**
     * Methods to access box state for the current TodoApp app
     */
    box: {
      /**
       * Get all current keyed values from box state
       */
      getAll: async (): Promise<Partial<Expand<BoxKeysState>>> => {
        const result = await this.appClient.state.box.getAll()
        return {
        }
      },
      /**
       * Get values from the todos map in box state
       */
      todos: {
        /**
         * Get all current values of the todos map in box state
         */
        getMap: async (): Promise<Map<bigint, string>> => { return (await this.appClient.state.box.getMap("todos")) as Map<bigint, string> },
        /**
         * Get a current value of the todos map by key from box state
         */
        value: async (key: bigint | number): Promise<string | undefined> => { return await this.appClient.state.box.getMapValue("todos", key) as string | undefined },
      },
      /**
       * Get values from the completed map in box state
       */
      completed: {
        /**
         * Get all current values of the completed map in box state
         */
        getMap: async (): Promise<Map<bigint, boolean>> => { return (await this.appClient.state.box.getMap("completed")) as Map<bigint, boolean> },
        /**
         * Get a current value of the completed map by key from box state
         */
        value: async (key: bigint | number): Promise<boolean | undefined> => { return await this.appClient.state.box.getMapValue("completed", key) as boolean | undefined },
      },
    },
  }

  public newGroup(): TodoAppComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a create_todo(string)uint64 method call against the TodoApp contract
       */
      createTodo(params: CallParams<TodoAppArgs['obj']['create_todo(string)uint64'] | TodoAppArgs['tuple']['create_todo(string)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.createTodo(params)))
        resultMappers.push((v) => client.decodeReturnValue('create_todo(string)uint64', v))
        return this
      },
      /**
       * Add a complete_todo(uint64)string method call against the TodoApp contract
       */
      completeTodo(params: CallParams<TodoAppArgs['obj']['complete_todo(uint64)string'] | TodoAppArgs['tuple']['complete_todo(uint64)string']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.completeTodo(params)))
        resultMappers.push((v) => client.decodeReturnValue('complete_todo(uint64)string', v))
        return this
      },
      /**
       * Add a get_todo(uint64)string method call against the TodoApp contract
       */
      getTodo(params: CallParams<TodoAppArgs['obj']['get_todo(uint64)string'] | TodoAppArgs['tuple']['get_todo(uint64)string']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.getTodo(params)))
        resultMappers.push((v) => client.decodeReturnValue('get_todo(uint64)string', v))
        return this
      },
      /**
       * Add a is_completed(uint64)bool method call against the TodoApp contract
       */
      isCompleted(params: CallParams<TodoAppArgs['obj']['is_completed(uint64)bool'] | TodoAppArgs['tuple']['is_completed(uint64)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.isCompleted(params)))
        resultMappers.push((v) => client.decodeReturnValue('is_completed(uint64)bool', v))
        return this
      },
      /**
       * Add a get_total_todos()uint64 method call against the TodoApp contract
       */
      getTotalTodos(params: CallParams<TodoAppArgs['obj']['get_total_todos()uint64'] | TodoAppArgs['tuple']['get_total_todos()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.getTotalTodos(params)))
        resultMappers.push((v) => client.decodeReturnValue('get_total_todos()uint64', v))
        return this
      },
      /**
       * Add a delete_todo(uint64)string method call against the TodoApp contract
       */
      deleteTodo(params: CallParams<TodoAppArgs['obj']['delete_todo(uint64)string'] | TodoAppArgs['tuple']['delete_todo(uint64)string']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.deleteTodo(params)))
        resultMappers.push((v) => client.decodeReturnValue('delete_todo(uint64)string', v))
        return this
      },
      /**
       * Add a clear state call to the TodoApp contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as TodoAppComposer
  }
}
export type TodoAppComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the create_todo(string)uint64 ABI method.
   *
  * Create a new todo item.
  Returns the ID of the created todo.

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  createTodo(params?: CallParams<TodoAppArgs['obj']['create_todo(string)uint64'] | TodoAppArgs['tuple']['create_todo(string)uint64']>): TodoAppComposer<[...TReturns, TodoAppReturns['create_todo(string)uint64'] | undefined]>

  /**
   * Calls the complete_todo(uint64)string ABI method.
   *
  * Mark a todo as completed.
  Returns a success message.

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  completeTodo(params?: CallParams<TodoAppArgs['obj']['complete_todo(uint64)string'] | TodoAppArgs['tuple']['complete_todo(uint64)string']>): TodoAppComposer<[...TReturns, TodoAppReturns['complete_todo(uint64)string'] | undefined]>

  /**
   * Calls the get_todo(uint64)string ABI method.
   *
  * Get a specific todo by its ID.
  Returns the todo text.

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  getTodo(params?: CallParams<TodoAppArgs['obj']['get_todo(uint64)string'] | TodoAppArgs['tuple']['get_todo(uint64)string']>): TodoAppComposer<[...TReturns, TodoAppReturns['get_todo(uint64)string'] | undefined]>

  /**
   * Calls the is_completed(uint64)bool ABI method.
   *
  * Check if a todo is completed.
  Returns True if completed, False otherwise.

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  isCompleted(params?: CallParams<TodoAppArgs['obj']['is_completed(uint64)bool'] | TodoAppArgs['tuple']['is_completed(uint64)bool']>): TodoAppComposer<[...TReturns, TodoAppReturns['is_completed(uint64)bool'] | undefined]>

  /**
   * Calls the get_total_todos()uint64 ABI method.
   *
   * Get the total number of todos created.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  getTotalTodos(params?: CallParams<TodoAppArgs['obj']['get_total_todos()uint64'] | TodoAppArgs['tuple']['get_total_todos()uint64']>): TodoAppComposer<[...TReturns, TodoAppReturns['get_total_todos()uint64'] | undefined]>

  /**
   * Calls the delete_todo(uint64)string ABI method.
   *
  * Delete a todo item.
  Returns a success message.

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  deleteTodo(params?: CallParams<TodoAppArgs['obj']['delete_todo(uint64)string'] | TodoAppArgs['tuple']['delete_todo(uint64)string']>): TodoAppComposer<[...TReturns, TodoAppReturns['delete_todo(uint64)string'] | undefined]>

  /**
   * Makes a clear_state call to an existing instance of the TodoApp smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): TodoAppComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): TodoAppComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<TodoAppComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<TodoAppComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<TodoAppComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<TodoAppComposerResults<TReturns>>
}
export type TodoAppComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

