/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"EscrowContract","structs":{},"methods":[{"name":"deposit","args":[{"type":"string","name":"job_id"},{"type":"pay","name":"payment"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Renter calls this with a grouped Payment txn.","events":[],"recommendations":{}},{"name":"release","args":[{"type":"address","name":"provider"},{"type":"uint64","name":"amount"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"The Coordinator calls this to release funds to the Provider based on actual runtime.","events":[],"recommendations":{}},{"name":"refund","args":[{"type":"address","name":"renter"},{"type":"uint64","name":"amount"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"The Coordinator calls this to refund the Renter if the Provider's node goes offline.","events":[],"recommendations":{}}],"arcs":[22,28],"desc":"\n    Decentralized GPU Rental Escrow\n    Tokens are deposited by the Renter and released to the Provider\n    by the Coordinator (acting as an Oracle) when the job is done.\n    ","networks":{},"state":{"schema":{"global":{"ints":0,"bytes":1},"local":{"ints":0,"bytes":0}},"keys":{"global":{"coordinator":{"keyType":"AVMString","valueType":"address","key":"Y29vcmRpbmF0b3I="}},"local":{},"box":{}},"maps":{"global":{},"local":{},"box":{}}},"bareActions":{"create":["NoOp"],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[111],"errorMessage":"Amount must be greater than 0"},{"pc":[177],"errorMessage":"Only the Coordinator can process refunds"},{"pc":[138],"errorMessage":"Only the Coordinator can release funds"},{"pc":[108],"errorMessage":"Payment must be to Escrow"},{"pc":[136,175],"errorMessage":"check self.coordinator exists"},{"pc":[84],"errorMessage":"invalid array length header"},{"pc":[91],"errorMessage":"invalid number of bytes for arc4.dynamic_array<arc4.uint8>"},{"pc":[121,160],"errorMessage":"invalid number of bytes for arc4.static_array<arc4.uint8, 32>"},{"pc":[129,168],"errorMessage":"invalid number of bytes for arc4.uint64"},{"pc":[101],"errorMessage":"transaction type is pay"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"events":[]} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


/**
 * The argument types for the EscrowContract contract
 */
export type EscrowContractArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'deposit(string,pay)void': {
      jobId: string
      payment: AppMethodCallTransactionArgument
    }
    'release(address,uint64)void': {
      provider: string
      amount: bigint | number
    }
    'refund(address,uint64)void': {
      renter: string
      amount: bigint | number
    }
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'deposit(string,pay)void': [jobId: string, payment: AppMethodCallTransactionArgument]
    'release(address,uint64)void': [provider: string, amount: bigint | number]
    'refund(address,uint64)void': [renter: string, amount: bigint | number]
  }
}

/**
 * The return type for each method
 */
export type EscrowContractReturns = {
  'deposit(string,pay)void': void
  'release(address,uint64)void': void
  'refund(address,uint64)void': void
}

/**
 * Defines the types of available calls and state of the EscrowContract smart contract.
 */
export type EscrowContractTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'deposit(string,pay)void' | 'deposit', {
      argsObj: EscrowContractArgs['obj']['deposit(string,pay)void']
      argsTuple: EscrowContractArgs['tuple']['deposit(string,pay)void']
      returns: EscrowContractReturns['deposit(string,pay)void']
    }>
    & Record<'release(address,uint64)void' | 'release', {
      argsObj: EscrowContractArgs['obj']['release(address,uint64)void']
      argsTuple: EscrowContractArgs['tuple']['release(address,uint64)void']
      returns: EscrowContractReturns['release(address,uint64)void']
    }>
    & Record<'refund(address,uint64)void' | 'refund', {
      argsObj: EscrowContractArgs['obj']['refund(address,uint64)void']
      argsTuple: EscrowContractArgs['tuple']['refund(address,uint64)void']
      returns: EscrowContractReturns['refund(address,uint64)void']
    }>
  /**
   * Defines the shape of the state of the application.
   */
  state: {
    global: {
      keys: {
        coordinator: string
      }
      maps: {}
    }
  }
}

/**
 * Defines the possible abi call signatures.
 */
export type EscrowContractSignatures = keyof EscrowContractTypes['methods']
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the EscrowContract smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends EscrowContractSignatures> = EscrowContractTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the EscrowContract smart contract to the method's return type
 */
export type MethodReturn<TSignature extends EscrowContractSignatures> = EscrowContractTypes['methods'][TSignature]['returns']

/**
 * Defines the shape of the keyed global state of the application.
 */
export type GlobalKeysState = EscrowContractTypes['state']['global']['keys']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the EscrowContract smart contract
 */
export abstract class EscrowContractParamsFactory {
  /**
   * Constructs a no op call for the deposit(string,pay)void ABI method
   *
   * Renter calls this with a grouped Payment txn.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static deposit(params: CallParams<EscrowContractArgs['obj']['deposit(string,pay)void'] | EscrowContractArgs['tuple']['deposit(string,pay)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'deposit(string,pay)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.jobId, params.args.payment],
    }
  }
  /**
   * Constructs a no op call for the release(address,uint64)void ABI method
   *
   * The Coordinator calls this to release funds to the Provider based on actual runtime.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static release(params: CallParams<EscrowContractArgs['obj']['release(address,uint64)void'] | EscrowContractArgs['tuple']['release(address,uint64)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'release(address,uint64)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.provider, params.args.amount],
    }
  }
  /**
   * Constructs a no op call for the refund(address,uint64)void ABI method
   *
   * The Coordinator calls this to refund the Renter if the Provider's node goes offline.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static refund(params: CallParams<EscrowContractArgs['obj']['refund(address,uint64)void'] | EscrowContractArgs['tuple']['refund(address,uint64)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'refund(address,uint64)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.renter, params.args.amount],
    }
  }
}

/**
 * A client to make calls to the EscrowContract smart contract
 */
export class EscrowContractClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `EscrowContractClient`
   *
   * @param appClient An `AppClient` instance which has been created with the EscrowContract app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `EscrowContractClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Returns a new `EscrowContractClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<EscrowContractClient> {
    return new EscrowContractClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `EscrowContractClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<EscrowContractClient> {
    return new EscrowContractClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Makes a clear_state call to an existing instance of the EscrowContract smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the EscrowContract smart contract using the `deposit(string,pay)void` ABI method.
     *
     * Renter calls this with a grouped Payment txn.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    deposit: (params: CallParams<EscrowContractArgs['obj']['deposit(string,pay)void'] | EscrowContractArgs['tuple']['deposit(string,pay)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(EscrowContractParamsFactory.deposit(params))
    },

    /**
     * Makes a call to the EscrowContract smart contract using the `release(address,uint64)void` ABI method.
     *
     * The Coordinator calls this to release funds to the Provider based on actual runtime.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    release: (params: CallParams<EscrowContractArgs['obj']['release(address,uint64)void'] | EscrowContractArgs['tuple']['release(address,uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(EscrowContractParamsFactory.release(params))
    },

    /**
     * Makes a call to the EscrowContract smart contract using the `refund(address,uint64)void` ABI method.
     *
     * The Coordinator calls this to refund the Renter if the Provider's node goes offline.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    refund: (params: CallParams<EscrowContractArgs['obj']['refund(address,uint64)void'] | EscrowContractArgs['tuple']['refund(address,uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(EscrowContractParamsFactory.refund(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Makes a clear_state call to an existing instance of the EscrowContract smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the EscrowContract smart contract using the `deposit(string,pay)void` ABI method.
     *
     * Renter calls this with a grouped Payment txn.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    deposit: (params: CallParams<EscrowContractArgs['obj']['deposit(string,pay)void'] | EscrowContractArgs['tuple']['deposit(string,pay)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(EscrowContractParamsFactory.deposit(params))
    },

    /**
     * Makes a call to the EscrowContract smart contract using the `release(address,uint64)void` ABI method.
     *
     * The Coordinator calls this to release funds to the Provider based on actual runtime.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    release: (params: CallParams<EscrowContractArgs['obj']['release(address,uint64)void'] | EscrowContractArgs['tuple']['release(address,uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(EscrowContractParamsFactory.release(params))
    },

    /**
     * Makes a call to the EscrowContract smart contract using the `refund(address,uint64)void` ABI method.
     *
     * The Coordinator calls this to refund the Renter if the Provider's node goes offline.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    refund: (params: CallParams<EscrowContractArgs['obj']['refund(address,uint64)void'] | EscrowContractArgs['tuple']['refund(address,uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(EscrowContractParamsFactory.refund(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Makes a clear_state call to an existing instance of the EscrowContract smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the EscrowContract smart contract using the `deposit(string,pay)void` ABI method.
     *
     * Renter calls this with a grouped Payment txn.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    deposit: async (params: CallParams<EscrowContractArgs['obj']['deposit(string,pay)void'] | EscrowContractArgs['tuple']['deposit(string,pay)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(EscrowContractParamsFactory.deposit(params))
      return {...result, return: result.return as unknown as (undefined | EscrowContractReturns['deposit(string,pay)void'])}
    },

    /**
     * Makes a call to the EscrowContract smart contract using the `release(address,uint64)void` ABI method.
     *
     * The Coordinator calls this to release funds to the Provider based on actual runtime.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    release: async (params: CallParams<EscrowContractArgs['obj']['release(address,uint64)void'] | EscrowContractArgs['tuple']['release(address,uint64)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(EscrowContractParamsFactory.release(params))
      return {...result, return: result.return as unknown as (undefined | EscrowContractReturns['release(address,uint64)void'])}
    },

    /**
     * Makes a call to the EscrowContract smart contract using the `refund(address,uint64)void` ABI method.
     *
     * The Coordinator calls this to refund the Renter if the Provider's node goes offline.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    refund: async (params: CallParams<EscrowContractArgs['obj']['refund(address,uint64)void'] | EscrowContractArgs['tuple']['refund(address,uint64)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(EscrowContractParamsFactory.refund(params))
      return {...result, return: result.return as unknown as (undefined | EscrowContractReturns['refund(address,uint64)void'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new EscrowContractClient(this.appClient.clone(params))
  }

  /**
   * Methods to access state for the current EscrowContract app
   */
  state = {
    /**
     * Methods to access global state for the current EscrowContract app
     */
    global: {
      /**
       * Get all current keyed values from global state
       */
      getAll: async (): Promise<Partial<Expand<GlobalKeysState>>> => {
        const result = await this.appClient.state.global.getAll()
        return {
          coordinator: result.coordinator,
        }
      },
      /**
       * Get the current value of the coordinator key in global state
       */
      coordinator: async (): Promise<string | undefined> => { return (await this.appClient.state.global.getValue("coordinator")) as string | undefined },
    },
  }

  public newGroup(): EscrowContractComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a deposit(string,pay)void method call against the EscrowContract contract
       */
      deposit(params: CallParams<EscrowContractArgs['obj']['deposit(string,pay)void'] | EscrowContractArgs['tuple']['deposit(string,pay)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.deposit(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a release(address,uint64)void method call against the EscrowContract contract
       */
      release(params: CallParams<EscrowContractArgs['obj']['release(address,uint64)void'] | EscrowContractArgs['tuple']['release(address,uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.release(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a refund(address,uint64)void method call against the EscrowContract contract
       */
      refund(params: CallParams<EscrowContractArgs['obj']['refund(address,uint64)void'] | EscrowContractArgs['tuple']['refund(address,uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.refund(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a clear state call to the EscrowContract contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as EscrowContractComposer
  }
}
export type EscrowContractComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the deposit(string,pay)void ABI method.
   *
   * Renter calls this with a grouped Payment txn.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  deposit(params?: CallParams<EscrowContractArgs['obj']['deposit(string,pay)void'] | EscrowContractArgs['tuple']['deposit(string,pay)void']>): EscrowContractComposer<[...TReturns, EscrowContractReturns['deposit(string,pay)void'] | undefined]>

  /**
   * Calls the release(address,uint64)void ABI method.
   *
   * The Coordinator calls this to release funds to the Provider based on actual runtime.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  release(params?: CallParams<EscrowContractArgs['obj']['release(address,uint64)void'] | EscrowContractArgs['tuple']['release(address,uint64)void']>): EscrowContractComposer<[...TReturns, EscrowContractReturns['release(address,uint64)void'] | undefined]>

  /**
   * Calls the refund(address,uint64)void ABI method.
   *
   * The Coordinator calls this to refund the Renter if the Provider's node goes offline.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  refund(params?: CallParams<EscrowContractArgs['obj']['refund(address,uint64)void'] | EscrowContractArgs['tuple']['refund(address,uint64)void']>): EscrowContractComposer<[...TReturns, EscrowContractReturns['refund(address,uint64)void'] | undefined]>

  /**
   * Makes a clear_state call to an existing instance of the EscrowContract smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): EscrowContractComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): EscrowContractComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<EscrowContractComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<EscrowContractComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<EscrowContractComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<EscrowContractComposerResults<TReturns>>
}
export type EscrowContractComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

