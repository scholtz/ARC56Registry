/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"AlgoVault","structs":{},"methods":[{"name":"initialize","args":[],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}},{"name":"deposit","args":[{"type":"uint64","name":"amount"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}},{"name":"get_total_savings","args":[],"returns":{"type":"uint64"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}},{"name":"check_milestone","args":[],"returns":{"type":"uint64"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}}],"arcs":[22,28],"networks":{},"state":{"schema":{"global":{"ints":2,"bytes":0},"local":{"ints":0,"bytes":0}},"keys":{"global":{"total_savings":{"keyType":"AVMString","valueType":"AVMUint64","key":"dG90YWxfc2F2aW5ncw=="},"milestone":{"keyType":"AVMString","valueType":"AVMUint64","key":"bWlsZXN0b25l"}},"local":{},"box":{}},"maps":{"global":{},"local":{},"box":{}}},"bareActions":{"create":["NoOp"],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[139],"errorMessage":"check self.milestone exists"},{"pc":[114,124,135],"errorMessage":"check self.total_savings exists"},{"pc":[109],"errorMessage":"invalid number of bytes for arc4.uint64"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"events":[]} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


/**
 * The argument types for the AlgoVault contract
 */
export type AlgoVaultArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'initialize()void': Record<string, never>
    'deposit(uint64)void': {
      amount: bigint | number
    }
    'get_total_savings()uint64': Record<string, never>
    'check_milestone()uint64': Record<string, never>
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'initialize()void': []
    'deposit(uint64)void': [amount: bigint | number]
    'get_total_savings()uint64': []
    'check_milestone()uint64': []
  }
}

/**
 * The return type for each method
 */
export type AlgoVaultReturns = {
  'initialize()void': void
  'deposit(uint64)void': void
  'get_total_savings()uint64': bigint
  'check_milestone()uint64': bigint
}

/**
 * Defines the types of available calls and state of the AlgoVault smart contract.
 */
export type AlgoVaultTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'initialize()void' | 'initialize', {
      argsObj: AlgoVaultArgs['obj']['initialize()void']
      argsTuple: AlgoVaultArgs['tuple']['initialize()void']
      returns: AlgoVaultReturns['initialize()void']
    }>
    & Record<'deposit(uint64)void' | 'deposit', {
      argsObj: AlgoVaultArgs['obj']['deposit(uint64)void']
      argsTuple: AlgoVaultArgs['tuple']['deposit(uint64)void']
      returns: AlgoVaultReturns['deposit(uint64)void']
    }>
    & Record<'get_total_savings()uint64' | 'get_total_savings', {
      argsObj: AlgoVaultArgs['obj']['get_total_savings()uint64']
      argsTuple: AlgoVaultArgs['tuple']['get_total_savings()uint64']
      returns: AlgoVaultReturns['get_total_savings()uint64']
    }>
    & Record<'check_milestone()uint64' | 'check_milestone', {
      argsObj: AlgoVaultArgs['obj']['check_milestone()uint64']
      argsTuple: AlgoVaultArgs['tuple']['check_milestone()uint64']
      returns: AlgoVaultReturns['check_milestone()uint64']
    }>
  /**
   * Defines the shape of the state of the application.
   */
  state: {
    global: {
      keys: {
        totalSavings: bigint
        milestone: bigint
      }
      maps: {}
    }
  }
}

/**
 * Defines the possible abi call signatures.
 */
export type AlgoVaultSignatures = keyof AlgoVaultTypes['methods']
/**
 * Defines the possible abi call signatures for methods that return a non-void value.
 */
export type AlgoVaultNonVoidMethodSignatures = keyof AlgoVaultTypes['methods'] extends infer T ? T extends keyof AlgoVaultTypes['methods'] ? MethodReturn<T> extends void ? never : T  : never : never
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the AlgoVault smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends AlgoVaultSignatures> = AlgoVaultTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the AlgoVault smart contract to the method's return type
 */
export type MethodReturn<TSignature extends AlgoVaultSignatures> = AlgoVaultTypes['methods'][TSignature]['returns']

/**
 * Defines the shape of the keyed global state of the application.
 */
export type GlobalKeysState = AlgoVaultTypes['state']['global']['keys']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the AlgoVault smart contract
 */
export abstract class AlgoVaultParamsFactory {
  /**
   * Constructs a no op call for the initialize()void ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static initialize(params: CallParams<AlgoVaultArgs['obj']['initialize()void'] | AlgoVaultArgs['tuple']['initialize()void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'initialize()void' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the deposit(uint64)void ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static deposit(params: CallParams<AlgoVaultArgs['obj']['deposit(uint64)void'] | AlgoVaultArgs['tuple']['deposit(uint64)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'deposit(uint64)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.amount],
    }
  }
  /**
   * Constructs a no op call for the get_total_savings()uint64 ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static getTotalSavings(params: CallParams<AlgoVaultArgs['obj']['get_total_savings()uint64'] | AlgoVaultArgs['tuple']['get_total_savings()uint64']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'get_total_savings()uint64' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the check_milestone()uint64 ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static checkMilestone(params: CallParams<AlgoVaultArgs['obj']['check_milestone()uint64'] | AlgoVaultArgs['tuple']['check_milestone()uint64']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'check_milestone()uint64' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
}

/**
 * A client to make calls to the AlgoVault smart contract
 */
export class AlgoVaultClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `AlgoVaultClient`
   *
   * @param appClient An `AppClient` instance which has been created with the AlgoVault app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `AlgoVaultClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Checks for decode errors on the given return value and maps the return value to the return type for the given method
   * @returns The typed return value or undefined if there was no value
   */
  decodeReturnValue<TSignature extends AlgoVaultNonVoidMethodSignatures>(method: TSignature, returnValue: ABIReturn | undefined) {
    return returnValue !== undefined ? getArc56ReturnValue<MethodReturn<TSignature>>(returnValue, this.appClient.getABIMethod(method), APP_SPEC.structs) : undefined
  }

  /**
   * Returns a new `AlgoVaultClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<AlgoVaultClient> {
    return new AlgoVaultClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `AlgoVaultClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<AlgoVaultClient> {
    return new AlgoVaultClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Makes a clear_state call to an existing instance of the AlgoVault smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the AlgoVault smart contract using the `initialize()void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    initialize: (params: CallParams<AlgoVaultArgs['obj']['initialize()void'] | AlgoVaultArgs['tuple']['initialize()void']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(AlgoVaultParamsFactory.initialize(params))
    },

    /**
     * Makes a call to the AlgoVault smart contract using the `deposit(uint64)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    deposit: (params: CallParams<AlgoVaultArgs['obj']['deposit(uint64)void'] | AlgoVaultArgs['tuple']['deposit(uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(AlgoVaultParamsFactory.deposit(params))
    },

    /**
     * Makes a call to the AlgoVault smart contract using the `get_total_savings()uint64` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    getTotalSavings: (params: CallParams<AlgoVaultArgs['obj']['get_total_savings()uint64'] | AlgoVaultArgs['tuple']['get_total_savings()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(AlgoVaultParamsFactory.getTotalSavings(params))
    },

    /**
     * Makes a call to the AlgoVault smart contract using the `check_milestone()uint64` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    checkMilestone: (params: CallParams<AlgoVaultArgs['obj']['check_milestone()uint64'] | AlgoVaultArgs['tuple']['check_milestone()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(AlgoVaultParamsFactory.checkMilestone(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Makes a clear_state call to an existing instance of the AlgoVault smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the AlgoVault smart contract using the `initialize()void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    initialize: (params: CallParams<AlgoVaultArgs['obj']['initialize()void'] | AlgoVaultArgs['tuple']['initialize()void']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(AlgoVaultParamsFactory.initialize(params))
    },

    /**
     * Makes a call to the AlgoVault smart contract using the `deposit(uint64)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    deposit: (params: CallParams<AlgoVaultArgs['obj']['deposit(uint64)void'] | AlgoVaultArgs['tuple']['deposit(uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(AlgoVaultParamsFactory.deposit(params))
    },

    /**
     * Makes a call to the AlgoVault smart contract using the `get_total_savings()uint64` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    getTotalSavings: (params: CallParams<AlgoVaultArgs['obj']['get_total_savings()uint64'] | AlgoVaultArgs['tuple']['get_total_savings()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(AlgoVaultParamsFactory.getTotalSavings(params))
    },

    /**
     * Makes a call to the AlgoVault smart contract using the `check_milestone()uint64` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    checkMilestone: (params: CallParams<AlgoVaultArgs['obj']['check_milestone()uint64'] | AlgoVaultArgs['tuple']['check_milestone()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(AlgoVaultParamsFactory.checkMilestone(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Makes a clear_state call to an existing instance of the AlgoVault smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the AlgoVault smart contract using the `initialize()void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    initialize: async (params: CallParams<AlgoVaultArgs['obj']['initialize()void'] | AlgoVaultArgs['tuple']['initialize()void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(AlgoVaultParamsFactory.initialize(params))
      return {...result, return: result.return as unknown as (undefined | AlgoVaultReturns['initialize()void'])}
    },

    /**
     * Makes a call to the AlgoVault smart contract using the `deposit(uint64)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    deposit: async (params: CallParams<AlgoVaultArgs['obj']['deposit(uint64)void'] | AlgoVaultArgs['tuple']['deposit(uint64)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(AlgoVaultParamsFactory.deposit(params))
      return {...result, return: result.return as unknown as (undefined | AlgoVaultReturns['deposit(uint64)void'])}
    },

    /**
     * Makes a call to the AlgoVault smart contract using the `get_total_savings()uint64` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    getTotalSavings: async (params: CallParams<AlgoVaultArgs['obj']['get_total_savings()uint64'] | AlgoVaultArgs['tuple']['get_total_savings()uint64']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(AlgoVaultParamsFactory.getTotalSavings(params))
      return {...result, return: result.return as unknown as (undefined | AlgoVaultReturns['get_total_savings()uint64'])}
    },

    /**
     * Makes a call to the AlgoVault smart contract using the `check_milestone()uint64` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    checkMilestone: async (params: CallParams<AlgoVaultArgs['obj']['check_milestone()uint64'] | AlgoVaultArgs['tuple']['check_milestone()uint64']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(AlgoVaultParamsFactory.checkMilestone(params))
      return {...result, return: result.return as unknown as (undefined | AlgoVaultReturns['check_milestone()uint64'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new AlgoVaultClient(this.appClient.clone(params))
  }

  /**
   * Methods to access state for the current AlgoVault app
   */
  state = {
    /**
     * Methods to access global state for the current AlgoVault app
     */
    global: {
      /**
       * Get all current keyed values from global state
       */
      getAll: async (): Promise<Partial<Expand<GlobalKeysState>>> => {
        const result = await this.appClient.state.global.getAll()
        return {
          totalSavings: result.total_savings,
          milestone: result.milestone,
        }
      },
      /**
       * Get the current value of the total_savings key in global state
       */
      totalSavings: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("total_savings")) as bigint | undefined },
      /**
       * Get the current value of the milestone key in global state
       */
      milestone: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("milestone")) as bigint | undefined },
    },
  }

  public newGroup(): AlgoVaultComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a initialize()void method call against the AlgoVault contract
       */
      initialize(params: CallParams<AlgoVaultArgs['obj']['initialize()void'] | AlgoVaultArgs['tuple']['initialize()void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.initialize(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a deposit(uint64)void method call against the AlgoVault contract
       */
      deposit(params: CallParams<AlgoVaultArgs['obj']['deposit(uint64)void'] | AlgoVaultArgs['tuple']['deposit(uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.deposit(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a get_total_savings()uint64 method call against the AlgoVault contract
       */
      getTotalSavings(params: CallParams<AlgoVaultArgs['obj']['get_total_savings()uint64'] | AlgoVaultArgs['tuple']['get_total_savings()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.getTotalSavings(params)))
        resultMappers.push((v) => client.decodeReturnValue('get_total_savings()uint64', v))
        return this
      },
      /**
       * Add a check_milestone()uint64 method call against the AlgoVault contract
       */
      checkMilestone(params: CallParams<AlgoVaultArgs['obj']['check_milestone()uint64'] | AlgoVaultArgs['tuple']['check_milestone()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.checkMilestone(params)))
        resultMappers.push((v) => client.decodeReturnValue('check_milestone()uint64', v))
        return this
      },
      /**
       * Add a clear state call to the AlgoVault contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as AlgoVaultComposer
  }
}
export type AlgoVaultComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the initialize()void ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  initialize(params?: CallParams<AlgoVaultArgs['obj']['initialize()void'] | AlgoVaultArgs['tuple']['initialize()void']>): AlgoVaultComposer<[...TReturns, AlgoVaultReturns['initialize()void'] | undefined]>

  /**
   * Calls the deposit(uint64)void ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  deposit(params?: CallParams<AlgoVaultArgs['obj']['deposit(uint64)void'] | AlgoVaultArgs['tuple']['deposit(uint64)void']>): AlgoVaultComposer<[...TReturns, AlgoVaultReturns['deposit(uint64)void'] | undefined]>

  /**
   * Calls the get_total_savings()uint64 ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  getTotalSavings(params?: CallParams<AlgoVaultArgs['obj']['get_total_savings()uint64'] | AlgoVaultArgs['tuple']['get_total_savings()uint64']>): AlgoVaultComposer<[...TReturns, AlgoVaultReturns['get_total_savings()uint64'] | undefined]>

  /**
   * Calls the check_milestone()uint64 ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  checkMilestone(params?: CallParams<AlgoVaultArgs['obj']['check_milestone()uint64'] | AlgoVaultArgs['tuple']['check_milestone()uint64']>): AlgoVaultComposer<[...TReturns, AlgoVaultReturns['check_milestone()uint64'] | undefined]>

  /**
   * Makes a clear_state call to an existing instance of the AlgoVault smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): AlgoVaultComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): AlgoVaultComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<AlgoVaultComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<AlgoVaultComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<AlgoVaultComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<AlgoVaultComposerResults<TReturns>>
}
export type AlgoVaultComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

