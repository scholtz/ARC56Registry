/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"GenMark","structs":{"ContentRecord":[{"name":"creatorName","type":"string"},{"name":"creatorAddress","type":"address"},{"name":"platform","type":"string"},{"name":"timestamp","type":"uint64"},{"name":"asaId","type":"uint64"},{"name":"flagCount","type":"uint64"},{"name":"originalPhash","type":"string"},{"name":"morphedBy","type":"string"}]},"methods":[{"name":"register_content","args":[{"type":"string","name":"phash"},{"type":"string","name":"creator_name","desc":"Display name of the content creator."},{"type":"string","name":"platform"},{"type":"string","name":"original_phash"},{"type":"string","name":"morphed_by"},{"type":"pay","name":"pay_txn"}],"returns":{"type":"uint64","desc":"The Algorand Standard Asset ID of the minted soulbound certificate. This ID is stored in the record and returned to the frontend."},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Register a new AI-generated content item with its perceptual fingerprint.\nCalled silently at the moment of image generation. The user never sees blockchain terminology — they see only a \"Content Stamped ✓\" badge.","events":[],"recommendations":{}},{"name":"verify_content","args":[{"type":"string","name":"phash","desc":"Perceptual hash of the image to verify (16-char hex string)."}],"returns":{"type":"(bool,(string,address,string,uint64,uint64,uint64,string,string))","desc":"(found, record): ARC-4 tuple where: found=True  → content was registered; record has full origin details found=False → no registration found; treat as unregistered (suspicious)"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"desc":"Look up a content item by its perceptual hash and return the origin record.\nThis is a READ-ONLY method (readonly=True). It can be simulated without submitting a transaction — zero fees, zero state changes. Anyone, anywhere, can verify any image without a wallet or any blockchain knowledge.","events":[],"recommendations":{}},{"name":"flag_misuse","args":[{"type":"string","name":"phash"},{"type":"string","name":"description"},{"type":"pay","name":"pay_txn"}],"returns":{"type":"uint64","desc":"Zero-based index of this flag for this content item. Use (phash + flag_index) to retrieve the flag via get_flag()."},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"File an immutable misuse report against a registered content item.\nEach flag is stored in a dedicated box with a composite key:     key = b\"flg_\" + arc4_encode(phash) + itob(flag_index)\nThe transaction ID of this call is legally meaningful evidence — it proves a report was filed at a specific time and included a specific description. Flags can never be deleted or modified, creating a tamper-proof evidence chain.","events":[],"recommendations":{}},{"name":"get_flag","args":[{"type":"string","name":"phash"},{"type":"uint64","name":"flag_index"}],"returns":{"type":"string","desc":"The ARC-4 encoded description string of the flag."},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"desc":"Retrieve the description text of a specific misuse flag.\nFlags are immutable and indexed sequentially from 0. Use verify_content() first to get the flag_count, then iterate.","events":[],"recommendations":{}}],"arcs":[22,28],"desc":"\n    GenMark — AI Content Origin Registry\n\n    Core innovation: every AI-generated image receives a silent, unforgeable\n    digital birth certificate at the moment of creation. The perceptual hash\n    (pHash) fingerprint enables tracking even through resizing, compression,\n    format conversion, and minor editing — unlike SHA-256 which changes on\n    any pixel modification.\n\n    Registration flow (invisible to users):\n      1. User generates image on the GenMark platform\n      2. Backend computes pHash using the imagehash Python library\n      3. Backend calls register_content() in an atomic transaction group\n      4. A soulbound ASA is minted as the cryptographic ownership credential\n      5. Record is permanently stored in Algorand Box storage\n      6. User sees only a small \"Content Stamped ✓\" badge\n\n    Verification flow:\n      1. Investigator uploads suspicious image to the verify portal\n      2. Backend computes pHash of the uploaded image\n      3. Backend calls verify_content() (read-only, no fees)\n      4. Full origin record is returned: creator, platform, exact timestamp\n      5. Investigator can download a forensic PDF certificate\n\n    Misuse flagging:\n      1. Investigator clicks \"Report Misuse\" with a description\n      2. Backend calls flag_misuse() in an atomic transaction\n      3. Immutable flag record stored in a dedicated box (cannot be deleted)\n      4. Transaction ID serves as evidence of the report being filed\n\n    Design decisions:\n      • Box namespace \"reg_\" separates registry boxes from flag boxes (\"flg_\")\n      • Soulbound ASA: total=1, decimals=0, default_frozen=True, all roles=contract\n      • flag_count is the only field updated post-registration → box size stays constant\n      • Payment requirements prevent spam and cover box Minimum Balance Requirements\n    ","networks":{},"state":{"schema":{"global":{"ints":1,"bytes":0},"local":{"ints":0,"bytes":0}},"keys":{"global":{"total_registrations":{"keyType":"AVMString","valueType":"AVMUint64","key":"dG90YWxfcmVnaXN0cmF0aW9ucw=="}},"local":{},"box":{}},"maps":{"global":{},"local":{},"box":{"registry":{"keyType":"string","valueType":"ContentRecord","prefix":"cmVnXw=="}}}},"bareActions":{"create":["NoOp"],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[555],"errorMessage":"Cannot file a misuse report: content not registered on GenMark"},{"pc":[627],"errorMessage":"Flag not found at the specified index"},{"pc":[573],"errorMessage":"Minimum 0.05 ALGO (50,000 microAlgos) required for flag box storage"},{"pc":[245],"errorMessage":"Minimum 0.2 ALGO (200,000 microAlgos) required for box MBR + ASA creation"},{"pc":[95,117,149,176],"errorMessage":"OnCompletion is not NoOp"},{"pc":[563],"errorMessage":"Payment must be directed to the GenMark contract"},{"pc":[235],"errorMessage":"Payment must be directed to the GenMark contract to fund box storage"},{"pc":[255],"errorMessage":"This content fingerprint has already been registered on GenMark"},{"pc":[222],"errorMessage":"can only call when creating"},{"pc":[98,120,152,179],"errorMessage":"can only call when not creating"},{"pc":[456,576,601],"errorMessage":"check self.registry entry exists"},{"pc":[432],"errorMessage":"check self.total_registrations exists"},{"pc":[136,204],"errorMessage":"transaction type is pay"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"events":[]} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


// Type definitions for ARC-56 structs

export type ContentRecord = {
  creatorName: string,
  creatorAddress: string,
  platform: string,
  timestamp: bigint,
  asaId: bigint,
  flagCount: bigint,
  originalPhash: string,
  morphedBy: string
}


/**
 * Converts the ABI tuple representation of a ContentRecord to the struct representation
 */
export function ContentRecordFromTuple(abiTuple: [string, string, string, bigint, bigint, bigint, string, string]) {
  return getABIStructFromABITuple(abiTuple, APP_SPEC.structs.ContentRecord, APP_SPEC.structs) as ContentRecord
}

/**
 * The argument types for the GenMark contract
 */
export type GenMarkArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'register_content(string,string,string,string,string,pay)uint64': {
      phash: string
      /**
       * Display name of the content creator.
       */
      creatorName: string
      platform: string
      originalPhash: string
      morphedBy: string
      payTxn: AppMethodCallTransactionArgument
    }
    'verify_content(string)(bool,(string,address,string,uint64,uint64,uint64,string,string))': {
      /**
       * Perceptual hash of the image to verify (16-char hex string).
       */
      phash: string
    }
    'flag_misuse(string,string,pay)uint64': {
      phash: string
      description: string
      payTxn: AppMethodCallTransactionArgument
    }
    'get_flag(string,uint64)string': {
      phash: string
      flagIndex: bigint | number
    }
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'register_content(string,string,string,string,string,pay)uint64': [phash: string, creatorName: string, platform: string, originalPhash: string, morphedBy: string, payTxn: AppMethodCallTransactionArgument]
    'verify_content(string)(bool,(string,address,string,uint64,uint64,uint64,string,string))': [phash: string]
    'flag_misuse(string,string,pay)uint64': [phash: string, description: string, payTxn: AppMethodCallTransactionArgument]
    'get_flag(string,uint64)string': [phash: string, flagIndex: bigint | number]
  }
}

/**
 * The return type for each method
 */
export type GenMarkReturns = {
  'register_content(string,string,string,string,string,pay)uint64': bigint
  'verify_content(string)(bool,(string,address,string,uint64,uint64,uint64,string,string))': [boolean, [string, string, string, bigint, bigint, bigint, string, string]]
  'flag_misuse(string,string,pay)uint64': bigint
  'get_flag(string,uint64)string': string
}

/**
 * Defines the types of available calls and state of the GenMark smart contract.
 */
export type GenMarkTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'register_content(string,string,string,string,string,pay)uint64' | 'register_content', {
      argsObj: GenMarkArgs['obj']['register_content(string,string,string,string,string,pay)uint64']
      argsTuple: GenMarkArgs['tuple']['register_content(string,string,string,string,string,pay)uint64']
      /**
       * The Algorand Standard Asset ID of the minted soulbound certificate. This ID is stored in the record and returned to the frontend.
       */
      returns: GenMarkReturns['register_content(string,string,string,string,string,pay)uint64']
    }>
    & Record<'verify_content(string)(bool,(string,address,string,uint64,uint64,uint64,string,string))' | 'verify_content', {
      argsObj: GenMarkArgs['obj']['verify_content(string)(bool,(string,address,string,uint64,uint64,uint64,string,string))']
      argsTuple: GenMarkArgs['tuple']['verify_content(string)(bool,(string,address,string,uint64,uint64,uint64,string,string))']
      /**
       * (found, record): ARC-4 tuple where: found=True  → content was registered; record has full origin details found=False → no registration found; treat as unregistered (suspicious)
       */
      returns: GenMarkReturns['verify_content(string)(bool,(string,address,string,uint64,uint64,uint64,string,string))']
    }>
    & Record<'flag_misuse(string,string,pay)uint64' | 'flag_misuse', {
      argsObj: GenMarkArgs['obj']['flag_misuse(string,string,pay)uint64']
      argsTuple: GenMarkArgs['tuple']['flag_misuse(string,string,pay)uint64']
      /**
       * Zero-based index of this flag for this content item. Use (phash + flag_index) to retrieve the flag via get_flag().
       */
      returns: GenMarkReturns['flag_misuse(string,string,pay)uint64']
    }>
    & Record<'get_flag(string,uint64)string' | 'get_flag', {
      argsObj: GenMarkArgs['obj']['get_flag(string,uint64)string']
      argsTuple: GenMarkArgs['tuple']['get_flag(string,uint64)string']
      /**
       * The ARC-4 encoded description string of the flag.
       */
      returns: GenMarkReturns['get_flag(string,uint64)string']
    }>
  /**
   * Defines the shape of the state of the application.
   */
  state: {
    global: {
      keys: {
        totalRegistrations: bigint
      }
      maps: {}
    }
    box: {
      keys: {}
      maps: {
        registry: Map<string, ContentRecord>
      }
    }
  }
}

/**
 * Defines the possible abi call signatures.
 */
export type GenMarkSignatures = keyof GenMarkTypes['methods']
/**
 * Defines the possible abi call signatures for methods that return a non-void value.
 */
export type GenMarkNonVoidMethodSignatures = keyof GenMarkTypes['methods'] extends infer T ? T extends keyof GenMarkTypes['methods'] ? MethodReturn<T> extends void ? never : T  : never : never
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the GenMark smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends GenMarkSignatures> = GenMarkTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the GenMark smart contract to the method's return type
 */
export type MethodReturn<TSignature extends GenMarkSignatures> = GenMarkTypes['methods'][TSignature]['returns']

/**
 * Defines the shape of the keyed global state of the application.
 */
export type GlobalKeysState = GenMarkTypes['state']['global']['keys']

/**
 * Defines the shape of the keyed box state of the application.
 */
export type BoxKeysState = GenMarkTypes['state']['box']['keys']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the GenMark smart contract
 */
export abstract class GenMarkParamsFactory {
  /**
   * Constructs a no op call for the register_content(string,string,string,string,string,pay)uint64 ABI method
   *
  * Register a new AI-generated content item with its perceptual fingerprint.
  Called silently at the moment of image generation. The user never sees blockchain terminology — they see only a "Content Stamped ✓" badge.

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static registerContent(params: CallParams<GenMarkArgs['obj']['register_content(string,string,string,string,string,pay)uint64'] | GenMarkArgs['tuple']['register_content(string,string,string,string,string,pay)uint64']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'register_content(string,string,string,string,string,pay)uint64' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.phash, params.args.creatorName, params.args.platform, params.args.originalPhash, params.args.morphedBy, params.args.payTxn],
    }
  }
  /**
   * Constructs a no op call for the verify_content(string)(bool,(string,address,string,uint64,uint64,uint64,string,string)) ABI method
   *
  * Look up a content item by its perceptual hash and return the origin record.
  This is a READ-ONLY method (readonly=True). It can be simulated without submitting a transaction — zero fees, zero state changes. Anyone, anywhere, can verify any image without a wallet or any blockchain knowledge.

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static verifyContent(params: CallParams<GenMarkArgs['obj']['verify_content(string)(bool,(string,address,string,uint64,uint64,uint64,string,string))'] | GenMarkArgs['tuple']['verify_content(string)(bool,(string,address,string,uint64,uint64,uint64,string,string))']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'verify_content(string)(bool,(string,address,string,uint64,uint64,uint64,string,string))' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.phash],
    }
  }
  /**
   * Constructs a no op call for the flag_misuse(string,string,pay)uint64 ABI method
   *
  * File an immutable misuse report against a registered content item.
  Each flag is stored in a dedicated box with a composite key:     key = b"flg_" + arc4_encode(phash) + itob(flag_index)
  The transaction ID of this call is legally meaningful evidence — it proves a report was filed at a specific time and included a specific description. Flags can never be deleted or modified, creating a tamper-proof evidence chain.

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static flagMisuse(params: CallParams<GenMarkArgs['obj']['flag_misuse(string,string,pay)uint64'] | GenMarkArgs['tuple']['flag_misuse(string,string,pay)uint64']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'flag_misuse(string,string,pay)uint64' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.phash, params.args.description, params.args.payTxn],
    }
  }
  /**
   * Constructs a no op call for the get_flag(string,uint64)string ABI method
   *
  * Retrieve the description text of a specific misuse flag.
  Flags are immutable and indexed sequentially from 0. Use verify_content() first to get the flag_count, then iterate.

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static getFlag(params: CallParams<GenMarkArgs['obj']['get_flag(string,uint64)string'] | GenMarkArgs['tuple']['get_flag(string,uint64)string']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'get_flag(string,uint64)string' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.phash, params.args.flagIndex],
    }
  }
}

/**
 * A client to make calls to the GenMark smart contract
 */
export class GenMarkClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `GenMarkClient`
   *
   * @param appClient An `AppClient` instance which has been created with the GenMark app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `GenMarkClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Checks for decode errors on the given return value and maps the return value to the return type for the given method
   * @returns The typed return value or undefined if there was no value
   */
  decodeReturnValue<TSignature extends GenMarkNonVoidMethodSignatures>(method: TSignature, returnValue: ABIReturn | undefined) {
    return returnValue !== undefined ? getArc56ReturnValue<MethodReturn<TSignature>>(returnValue, this.appClient.getABIMethod(method), APP_SPEC.structs) : undefined
  }

  /**
   * Returns a new `GenMarkClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<GenMarkClient> {
    return new GenMarkClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `GenMarkClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<GenMarkClient> {
    return new GenMarkClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Makes a clear_state call to an existing instance of the GenMark smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the GenMark smart contract using the `register_content(string,string,string,string,string,pay)uint64` ABI method.
     *
    * Register a new AI-generated content item with its perceptual fingerprint.
    Called silently at the moment of image generation. The user never sees blockchain terminology — they see only a "Content Stamped ✓" badge.

     *
     * @param params The params for the smart contract call
     * @returns The call params: The Algorand Standard Asset ID of the minted soulbound certificate. This ID is stored in the record and returned to the frontend.
     */
    registerContent: (params: CallParams<GenMarkArgs['obj']['register_content(string,string,string,string,string,pay)uint64'] | GenMarkArgs['tuple']['register_content(string,string,string,string,string,pay)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(GenMarkParamsFactory.registerContent(params))
    },

    /**
     * Makes a call to the GenMark smart contract using the `verify_content(string)(bool,(string,address,string,uint64,uint64,uint64,string,string))` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
    * Look up a content item by its perceptual hash and return the origin record.
    This is a READ-ONLY method (readonly=True). It can be simulated without submitting a transaction — zero fees, zero state changes. Anyone, anywhere, can verify any image without a wallet or any blockchain knowledge.

     *
     * @param params The params for the smart contract call
     * @returns The call params: (found, record): ARC-4 tuple where: found=True  → content was registered; record has full origin details found=False → no registration found; treat as unregistered (suspicious)
     */
    verifyContent: (params: CallParams<GenMarkArgs['obj']['verify_content(string)(bool,(string,address,string,uint64,uint64,uint64,string,string))'] | GenMarkArgs['tuple']['verify_content(string)(bool,(string,address,string,uint64,uint64,uint64,string,string))']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(GenMarkParamsFactory.verifyContent(params))
    },

    /**
     * Makes a call to the GenMark smart contract using the `flag_misuse(string,string,pay)uint64` ABI method.
     *
    * File an immutable misuse report against a registered content item.
    Each flag is stored in a dedicated box with a composite key:     key = b"flg_" + arc4_encode(phash) + itob(flag_index)
    The transaction ID of this call is legally meaningful evidence — it proves a report was filed at a specific time and included a specific description. Flags can never be deleted or modified, creating a tamper-proof evidence chain.

     *
     * @param params The params for the smart contract call
     * @returns The call params: Zero-based index of this flag for this content item. Use (phash + flag_index) to retrieve the flag via get_flag().
     */
    flagMisuse: (params: CallParams<GenMarkArgs['obj']['flag_misuse(string,string,pay)uint64'] | GenMarkArgs['tuple']['flag_misuse(string,string,pay)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(GenMarkParamsFactory.flagMisuse(params))
    },

    /**
     * Makes a call to the GenMark smart contract using the `get_flag(string,uint64)string` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
    * Retrieve the description text of a specific misuse flag.
    Flags are immutable and indexed sequentially from 0. Use verify_content() first to get the flag_count, then iterate.

     *
     * @param params The params for the smart contract call
     * @returns The call params: The ARC-4 encoded description string of the flag.
     */
    getFlag: (params: CallParams<GenMarkArgs['obj']['get_flag(string,uint64)string'] | GenMarkArgs['tuple']['get_flag(string,uint64)string']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(GenMarkParamsFactory.getFlag(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Makes a clear_state call to an existing instance of the GenMark smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the GenMark smart contract using the `register_content(string,string,string,string,string,pay)uint64` ABI method.
     *
    * Register a new AI-generated content item with its perceptual fingerprint.
    Called silently at the moment of image generation. The user never sees blockchain terminology — they see only a "Content Stamped ✓" badge.

     *
     * @param params The params for the smart contract call
     * @returns The call transaction: The Algorand Standard Asset ID of the minted soulbound certificate. This ID is stored in the record and returned to the frontend.
     */
    registerContent: (params: CallParams<GenMarkArgs['obj']['register_content(string,string,string,string,string,pay)uint64'] | GenMarkArgs['tuple']['register_content(string,string,string,string,string,pay)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(GenMarkParamsFactory.registerContent(params))
    },

    /**
     * Makes a call to the GenMark smart contract using the `verify_content(string)(bool,(string,address,string,uint64,uint64,uint64,string,string))` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
    * Look up a content item by its perceptual hash and return the origin record.
    This is a READ-ONLY method (readonly=True). It can be simulated without submitting a transaction — zero fees, zero state changes. Anyone, anywhere, can verify any image without a wallet or any blockchain knowledge.

     *
     * @param params The params for the smart contract call
     * @returns The call transaction: (found, record): ARC-4 tuple where: found=True  → content was registered; record has full origin details found=False → no registration found; treat as unregistered (suspicious)
     */
    verifyContent: (params: CallParams<GenMarkArgs['obj']['verify_content(string)(bool,(string,address,string,uint64,uint64,uint64,string,string))'] | GenMarkArgs['tuple']['verify_content(string)(bool,(string,address,string,uint64,uint64,uint64,string,string))']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(GenMarkParamsFactory.verifyContent(params))
    },

    /**
     * Makes a call to the GenMark smart contract using the `flag_misuse(string,string,pay)uint64` ABI method.
     *
    * File an immutable misuse report against a registered content item.
    Each flag is stored in a dedicated box with a composite key:     key = b"flg_" + arc4_encode(phash) + itob(flag_index)
    The transaction ID of this call is legally meaningful evidence — it proves a report was filed at a specific time and included a specific description. Flags can never be deleted or modified, creating a tamper-proof evidence chain.

     *
     * @param params The params for the smart contract call
     * @returns The call transaction: Zero-based index of this flag for this content item. Use (phash + flag_index) to retrieve the flag via get_flag().
     */
    flagMisuse: (params: CallParams<GenMarkArgs['obj']['flag_misuse(string,string,pay)uint64'] | GenMarkArgs['tuple']['flag_misuse(string,string,pay)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(GenMarkParamsFactory.flagMisuse(params))
    },

    /**
     * Makes a call to the GenMark smart contract using the `get_flag(string,uint64)string` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
    * Retrieve the description text of a specific misuse flag.
    Flags are immutable and indexed sequentially from 0. Use verify_content() first to get the flag_count, then iterate.

     *
     * @param params The params for the smart contract call
     * @returns The call transaction: The ARC-4 encoded description string of the flag.
     */
    getFlag: (params: CallParams<GenMarkArgs['obj']['get_flag(string,uint64)string'] | GenMarkArgs['tuple']['get_flag(string,uint64)string']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(GenMarkParamsFactory.getFlag(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Makes a clear_state call to an existing instance of the GenMark smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the GenMark smart contract using the `register_content(string,string,string,string,string,pay)uint64` ABI method.
     *
    * Register a new AI-generated content item with its perceptual fingerprint.
    Called silently at the moment of image generation. The user never sees blockchain terminology — they see only a "Content Stamped ✓" badge.

     *
     * @param params The params for the smart contract call
     * @returns The call result: The Algorand Standard Asset ID of the minted soulbound certificate. This ID is stored in the record and returned to the frontend.
     */
    registerContent: async (params: CallParams<GenMarkArgs['obj']['register_content(string,string,string,string,string,pay)uint64'] | GenMarkArgs['tuple']['register_content(string,string,string,string,string,pay)uint64']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(GenMarkParamsFactory.registerContent(params))
      return {...result, return: result.return as unknown as (undefined | GenMarkReturns['register_content(string,string,string,string,string,pay)uint64'])}
    },

    /**
     * Makes a call to the GenMark smart contract using the `verify_content(string)(bool,(string,address,string,uint64,uint64,uint64,string,string))` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
    * Look up a content item by its perceptual hash and return the origin record.
    This is a READ-ONLY method (readonly=True). It can be simulated without submitting a transaction — zero fees, zero state changes. Anyone, anywhere, can verify any image without a wallet or any blockchain knowledge.

     *
     * @param params The params for the smart contract call
     * @returns The call result: (found, record): ARC-4 tuple where: found=True  → content was registered; record has full origin details found=False → no registration found; treat as unregistered (suspicious)
     */
    verifyContent: async (params: CallParams<GenMarkArgs['obj']['verify_content(string)(bool,(string,address,string,uint64,uint64,uint64,string,string))'] | GenMarkArgs['tuple']['verify_content(string)(bool,(string,address,string,uint64,uint64,uint64,string,string))']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(GenMarkParamsFactory.verifyContent(params))
      return {...result, return: result.return as unknown as (undefined | GenMarkReturns['verify_content(string)(bool,(string,address,string,uint64,uint64,uint64,string,string))'])}
    },

    /**
     * Makes a call to the GenMark smart contract using the `flag_misuse(string,string,pay)uint64` ABI method.
     *
    * File an immutable misuse report against a registered content item.
    Each flag is stored in a dedicated box with a composite key:     key = b"flg_" + arc4_encode(phash) + itob(flag_index)
    The transaction ID of this call is legally meaningful evidence — it proves a report was filed at a specific time and included a specific description. Flags can never be deleted or modified, creating a tamper-proof evidence chain.

     *
     * @param params The params for the smart contract call
     * @returns The call result: Zero-based index of this flag for this content item. Use (phash + flag_index) to retrieve the flag via get_flag().
     */
    flagMisuse: async (params: CallParams<GenMarkArgs['obj']['flag_misuse(string,string,pay)uint64'] | GenMarkArgs['tuple']['flag_misuse(string,string,pay)uint64']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(GenMarkParamsFactory.flagMisuse(params))
      return {...result, return: result.return as unknown as (undefined | GenMarkReturns['flag_misuse(string,string,pay)uint64'])}
    },

    /**
     * Makes a call to the GenMark smart contract using the `get_flag(string,uint64)string` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
    * Retrieve the description text of a specific misuse flag.
    Flags are immutable and indexed sequentially from 0. Use verify_content() first to get the flag_count, then iterate.

     *
     * @param params The params for the smart contract call
     * @returns The call result: The ARC-4 encoded description string of the flag.
     */
    getFlag: async (params: CallParams<GenMarkArgs['obj']['get_flag(string,uint64)string'] | GenMarkArgs['tuple']['get_flag(string,uint64)string']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(GenMarkParamsFactory.getFlag(params))
      return {...result, return: result.return as unknown as (undefined | GenMarkReturns['get_flag(string,uint64)string'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new GenMarkClient(this.appClient.clone(params))
  }

  /**
   * Makes a readonly (simulated) call to the GenMark smart contract using the `verify_content(string)(bool,(string,address,string,uint64,uint64,uint64,string,string))` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
  * Look up a content item by its perceptual hash and return the origin record.
  This is a READ-ONLY method (readonly=True). It can be simulated without submitting a transaction — zero fees, zero state changes. Anyone, anywhere, can verify any image without a wallet or any blockchain knowledge.

   *
   * @param params The params for the smart contract call
   * @returns The call result: (found, record): ARC-4 tuple where: found=True  → content was registered; record has full origin details found=False → no registration found; treat as unregistered (suspicious)
   */
  async verifyContent(params: CallParams<GenMarkArgs['obj']['verify_content(string)(bool,(string,address,string,uint64,uint64,uint64,string,string))'] | GenMarkArgs['tuple']['verify_content(string)(bool,(string,address,string,uint64,uint64,uint64,string,string))']>) {
    const result = await this.appClient.send.call(GenMarkParamsFactory.verifyContent(params))
    return result.return as unknown as GenMarkReturns['verify_content(string)(bool,(string,address,string,uint64,uint64,uint64,string,string))']
  }

  /**
   * Makes a readonly (simulated) call to the GenMark smart contract using the `get_flag(string,uint64)string` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
  * Retrieve the description text of a specific misuse flag.
  Flags are immutable and indexed sequentially from 0. Use verify_content() first to get the flag_count, then iterate.

   *
   * @param params The params for the smart contract call
   * @returns The call result: The ARC-4 encoded description string of the flag.
   */
  async getFlag(params: CallParams<GenMarkArgs['obj']['get_flag(string,uint64)string'] | GenMarkArgs['tuple']['get_flag(string,uint64)string']>) {
    const result = await this.appClient.send.call(GenMarkParamsFactory.getFlag(params))
    return result.return as unknown as GenMarkReturns['get_flag(string,uint64)string']
  }

  /**
   * Methods to access state for the current GenMark app
   */
  state = {
    /**
     * Methods to access global state for the current GenMark app
     */
    global: {
      /**
       * Get all current keyed values from global state
       */
      getAll: async (): Promise<Partial<Expand<GlobalKeysState>>> => {
        const result = await this.appClient.state.global.getAll()
        return {
          totalRegistrations: result.total_registrations,
        }
      },
      /**
       * Get the current value of the total_registrations key in global state
       */
      totalRegistrations: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("total_registrations")) as bigint | undefined },
    },
    /**
     * Methods to access box state for the current GenMark app
     */
    box: {
      /**
       * Get all current keyed values from box state
       */
      getAll: async (): Promise<Partial<Expand<BoxKeysState>>> => {
        const result = await this.appClient.state.box.getAll()
        return {
        }
      },
      /**
       * Get values from the registry map in box state
       */
      registry: {
        /**
         * Get all current values of the registry map in box state
         */
        getMap: async (): Promise<Map<string, ContentRecord>> => { return (await this.appClient.state.box.getMap("registry")) as Map<string, ContentRecord> },
        /**
         * Get a current value of the registry map by key from box state
         */
        value: async (key: string): Promise<ContentRecord | undefined> => { return await this.appClient.state.box.getMapValue("registry", key) as ContentRecord | undefined },
      },
    },
  }

  public newGroup(): GenMarkComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a register_content(string,string,string,string,string,pay)uint64 method call against the GenMark contract
       */
      registerContent(params: CallParams<GenMarkArgs['obj']['register_content(string,string,string,string,string,pay)uint64'] | GenMarkArgs['tuple']['register_content(string,string,string,string,string,pay)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.registerContent(params)))
        resultMappers.push((v) => client.decodeReturnValue('register_content(string,string,string,string,string,pay)uint64', v))
        return this
      },
      /**
       * Add a verify_content(string)(bool,(string,address,string,uint64,uint64,uint64,string,string)) method call against the GenMark contract
       */
      verifyContent(params: CallParams<GenMarkArgs['obj']['verify_content(string)(bool,(string,address,string,uint64,uint64,uint64,string,string))'] | GenMarkArgs['tuple']['verify_content(string)(bool,(string,address,string,uint64,uint64,uint64,string,string))']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.verifyContent(params)))
        resultMappers.push((v) => client.decodeReturnValue('verify_content(string)(bool,(string,address,string,uint64,uint64,uint64,string,string))', v))
        return this
      },
      /**
       * Add a flag_misuse(string,string,pay)uint64 method call against the GenMark contract
       */
      flagMisuse(params: CallParams<GenMarkArgs['obj']['flag_misuse(string,string,pay)uint64'] | GenMarkArgs['tuple']['flag_misuse(string,string,pay)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.flagMisuse(params)))
        resultMappers.push((v) => client.decodeReturnValue('flag_misuse(string,string,pay)uint64', v))
        return this
      },
      /**
       * Add a get_flag(string,uint64)string method call against the GenMark contract
       */
      getFlag(params: CallParams<GenMarkArgs['obj']['get_flag(string,uint64)string'] | GenMarkArgs['tuple']['get_flag(string,uint64)string']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.getFlag(params)))
        resultMappers.push((v) => client.decodeReturnValue('get_flag(string,uint64)string', v))
        return this
      },
      /**
       * Add a clear state call to the GenMark contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as GenMarkComposer
  }
}
export type GenMarkComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the register_content(string,string,string,string,string,pay)uint64 ABI method.
   *
  * Register a new AI-generated content item with its perceptual fingerprint.
  Called silently at the moment of image generation. The user never sees blockchain terminology — they see only a "Content Stamped ✓" badge.

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  registerContent(params?: CallParams<GenMarkArgs['obj']['register_content(string,string,string,string,string,pay)uint64'] | GenMarkArgs['tuple']['register_content(string,string,string,string,string,pay)uint64']>): GenMarkComposer<[...TReturns, GenMarkReturns['register_content(string,string,string,string,string,pay)uint64'] | undefined]>

  /**
   * Calls the verify_content(string)(bool,(string,address,string,uint64,uint64,uint64,string,string)) ABI method.
   *
  * Look up a content item by its perceptual hash and return the origin record.
  This is a READ-ONLY method (readonly=True). It can be simulated without submitting a transaction — zero fees, zero state changes. Anyone, anywhere, can verify any image without a wallet or any blockchain knowledge.

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  verifyContent(params?: CallParams<GenMarkArgs['obj']['verify_content(string)(bool,(string,address,string,uint64,uint64,uint64,string,string))'] | GenMarkArgs['tuple']['verify_content(string)(bool,(string,address,string,uint64,uint64,uint64,string,string))']>): GenMarkComposer<[...TReturns, GenMarkReturns['verify_content(string)(bool,(string,address,string,uint64,uint64,uint64,string,string))'] | undefined]>

  /**
   * Calls the flag_misuse(string,string,pay)uint64 ABI method.
   *
  * File an immutable misuse report against a registered content item.
  Each flag is stored in a dedicated box with a composite key:     key = b"flg_" + arc4_encode(phash) + itob(flag_index)
  The transaction ID of this call is legally meaningful evidence — it proves a report was filed at a specific time and included a specific description. Flags can never be deleted or modified, creating a tamper-proof evidence chain.

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  flagMisuse(params?: CallParams<GenMarkArgs['obj']['flag_misuse(string,string,pay)uint64'] | GenMarkArgs['tuple']['flag_misuse(string,string,pay)uint64']>): GenMarkComposer<[...TReturns, GenMarkReturns['flag_misuse(string,string,pay)uint64'] | undefined]>

  /**
   * Calls the get_flag(string,uint64)string ABI method.
   *
  * Retrieve the description text of a specific misuse flag.
  Flags are immutable and indexed sequentially from 0. Use verify_content() first to get the flag_count, then iterate.

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  getFlag(params?: CallParams<GenMarkArgs['obj']['get_flag(string,uint64)string'] | GenMarkArgs['tuple']['get_flag(string,uint64)string']>): GenMarkComposer<[...TReturns, GenMarkReturns['get_flag(string,uint64)string'] | undefined]>

  /**
   * Makes a clear_state call to an existing instance of the GenMark smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): GenMarkComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): GenMarkComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<GenMarkComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<GenMarkComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<GenMarkComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<GenMarkComposerResults<TReturns>>
}
export type GenMarkComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

