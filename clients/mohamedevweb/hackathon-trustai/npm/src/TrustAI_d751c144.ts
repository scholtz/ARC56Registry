/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"TrustAI","structs":{},"methods":[{"name":"fund_contract","args":[{"type":"address","name":"client"},{"type":"address","name":"freelancer"},{"type":"uint64","name":"amount"}],"returns":{"type":"string"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}},{"name":"validate_with_ai","args":[{"type":"bool","name":"ai_result"}],"returns":{"type":"string"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}},{"name":"release_payment","args":[],"returns":{"type":"string"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}}],"arcs":[22,28],"networks":{},"state":{"schema":{"global":{"ints":2,"bytes":2},"local":{"ints":0,"bytes":0}},"keys":{"global":{"client":{"keyType":"AVMString","valueType":"address","key":"Y2xpZW50"},"freelancer":{"keyType":"AVMString","valueType":"address","key":"ZnJlZWxhbmNlcg=="},"amount":{"keyType":"AVMString","valueType":"AVMUint64","key":"YW1vdW50"},"validated":{"keyType":"AVMString","valueType":"AVMUint64","key":"dmFsaWRhdGVk"}},"local":{},"box":{}},"maps":{"global":{},"local":{},"box":{}}},"bareActions":{"create":["NoOp"],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[78,102,131],"errorMessage":"OnCompletion is not NoOp"},{"pc":[170],"errorMessage":"can only call when creating"},{"pc":[81,105,134],"errorMessage":"can only call when not creating"},{"pc":[273,308,312],"errorMessage":"check GlobalState exists"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"events":[]} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


/**
 * The argument types for the TrustAi contract
 */
export type TrustAiArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'fund_contract(address,address,uint64)string': {
      client: string
      freelancer: string
      amount: bigint | number
    }
    'validate_with_ai(bool)string': {
      aiResult: boolean
    }
    'release_payment()string': Record<string, never>
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'fund_contract(address,address,uint64)string': [client: string, freelancer: string, amount: bigint | number]
    'validate_with_ai(bool)string': [aiResult: boolean]
    'release_payment()string': []
  }
}

/**
 * The return type for each method
 */
export type TrustAiReturns = {
  'fund_contract(address,address,uint64)string': string
  'validate_with_ai(bool)string': string
  'release_payment()string': string
}

/**
 * Defines the types of available calls and state of the TrustAi smart contract.
 */
export type TrustAiTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'fund_contract(address,address,uint64)string' | 'fund_contract', {
      argsObj: TrustAiArgs['obj']['fund_contract(address,address,uint64)string']
      argsTuple: TrustAiArgs['tuple']['fund_contract(address,address,uint64)string']
      returns: TrustAiReturns['fund_contract(address,address,uint64)string']
    }>
    & Record<'validate_with_ai(bool)string' | 'validate_with_ai', {
      argsObj: TrustAiArgs['obj']['validate_with_ai(bool)string']
      argsTuple: TrustAiArgs['tuple']['validate_with_ai(bool)string']
      returns: TrustAiReturns['validate_with_ai(bool)string']
    }>
    & Record<'release_payment()string' | 'release_payment', {
      argsObj: TrustAiArgs['obj']['release_payment()string']
      argsTuple: TrustAiArgs['tuple']['release_payment()string']
      returns: TrustAiReturns['release_payment()string']
    }>
  /**
   * Defines the shape of the state of the application.
   */
  state: {
    global: {
      keys: {
        client: string
        freelancer: string
        amount: bigint
        validated: bigint
      }
      maps: {}
    }
  }
}

/**
 * Defines the possible abi call signatures.
 */
export type TrustAiSignatures = keyof TrustAiTypes['methods']
/**
 * Defines the possible abi call signatures for methods that return a non-void value.
 */
export type TrustAiNonVoidMethodSignatures = keyof TrustAiTypes['methods'] extends infer T ? T extends keyof TrustAiTypes['methods'] ? MethodReturn<T> extends void ? never : T  : never : never
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the TrustAi smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends TrustAiSignatures> = TrustAiTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the TrustAi smart contract to the method's return type
 */
export type MethodReturn<TSignature extends TrustAiSignatures> = TrustAiTypes['methods'][TSignature]['returns']

/**
 * Defines the shape of the keyed global state of the application.
 */
export type GlobalKeysState = TrustAiTypes['state']['global']['keys']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the TrustAi smart contract
 */
export abstract class TrustAiParamsFactory {
  /**
   * Constructs a no op call for the fund_contract(address,address,uint64)string ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static fundContract(params: CallParams<TrustAiArgs['obj']['fund_contract(address,address,uint64)string'] | TrustAiArgs['tuple']['fund_contract(address,address,uint64)string']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'fund_contract(address,address,uint64)string' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.client, params.args.freelancer, params.args.amount],
    }
  }
  /**
   * Constructs a no op call for the validate_with_ai(bool)string ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static validateWithAi(params: CallParams<TrustAiArgs['obj']['validate_with_ai(bool)string'] | TrustAiArgs['tuple']['validate_with_ai(bool)string']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'validate_with_ai(bool)string' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.aiResult],
    }
  }
  /**
   * Constructs a no op call for the release_payment()string ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static releasePayment(params: CallParams<TrustAiArgs['obj']['release_payment()string'] | TrustAiArgs['tuple']['release_payment()string']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'release_payment()string' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
}

/**
 * A client to make calls to the TrustAI smart contract
 */
export class TrustAiClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `TrustAiClient`
   *
   * @param appClient An `AppClient` instance which has been created with the TrustAi app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `TrustAiClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Checks for decode errors on the given return value and maps the return value to the return type for the given method
   * @returns The typed return value or undefined if there was no value
   */
  decodeReturnValue<TSignature extends TrustAiNonVoidMethodSignatures>(method: TSignature, returnValue: ABIReturn | undefined) {
    return returnValue !== undefined ? getArc56ReturnValue<MethodReturn<TSignature>>(returnValue, this.appClient.getABIMethod(method), APP_SPEC.structs) : undefined
  }

  /**
   * Returns a new `TrustAiClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<TrustAiClient> {
    return new TrustAiClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `TrustAiClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<TrustAiClient> {
    return new TrustAiClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Makes a clear_state call to an existing instance of the TrustAI smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the TrustAI smart contract using the `fund_contract(address,address,uint64)string` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    fundContract: (params: CallParams<TrustAiArgs['obj']['fund_contract(address,address,uint64)string'] | TrustAiArgs['tuple']['fund_contract(address,address,uint64)string']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(TrustAiParamsFactory.fundContract(params))
    },

    /**
     * Makes a call to the TrustAI smart contract using the `validate_with_ai(bool)string` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    validateWithAi: (params: CallParams<TrustAiArgs['obj']['validate_with_ai(bool)string'] | TrustAiArgs['tuple']['validate_with_ai(bool)string']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(TrustAiParamsFactory.validateWithAi(params))
    },

    /**
     * Makes a call to the TrustAI smart contract using the `release_payment()string` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    releasePayment: (params: CallParams<TrustAiArgs['obj']['release_payment()string'] | TrustAiArgs['tuple']['release_payment()string']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(TrustAiParamsFactory.releasePayment(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Makes a clear_state call to an existing instance of the TrustAI smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the TrustAI smart contract using the `fund_contract(address,address,uint64)string` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    fundContract: (params: CallParams<TrustAiArgs['obj']['fund_contract(address,address,uint64)string'] | TrustAiArgs['tuple']['fund_contract(address,address,uint64)string']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(TrustAiParamsFactory.fundContract(params))
    },

    /**
     * Makes a call to the TrustAI smart contract using the `validate_with_ai(bool)string` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    validateWithAi: (params: CallParams<TrustAiArgs['obj']['validate_with_ai(bool)string'] | TrustAiArgs['tuple']['validate_with_ai(bool)string']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(TrustAiParamsFactory.validateWithAi(params))
    },

    /**
     * Makes a call to the TrustAI smart contract using the `release_payment()string` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    releasePayment: (params: CallParams<TrustAiArgs['obj']['release_payment()string'] | TrustAiArgs['tuple']['release_payment()string']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(TrustAiParamsFactory.releasePayment(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Makes a clear_state call to an existing instance of the TrustAI smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the TrustAI smart contract using the `fund_contract(address,address,uint64)string` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    fundContract: async (params: CallParams<TrustAiArgs['obj']['fund_contract(address,address,uint64)string'] | TrustAiArgs['tuple']['fund_contract(address,address,uint64)string']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(TrustAiParamsFactory.fundContract(params))
      return {...result, return: result.return as unknown as (undefined | TrustAiReturns['fund_contract(address,address,uint64)string'])}
    },

    /**
     * Makes a call to the TrustAI smart contract using the `validate_with_ai(bool)string` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    validateWithAi: async (params: CallParams<TrustAiArgs['obj']['validate_with_ai(bool)string'] | TrustAiArgs['tuple']['validate_with_ai(bool)string']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(TrustAiParamsFactory.validateWithAi(params))
      return {...result, return: result.return as unknown as (undefined | TrustAiReturns['validate_with_ai(bool)string'])}
    },

    /**
     * Makes a call to the TrustAI smart contract using the `release_payment()string` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    releasePayment: async (params: CallParams<TrustAiArgs['obj']['release_payment()string'] | TrustAiArgs['tuple']['release_payment()string']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(TrustAiParamsFactory.releasePayment(params))
      return {...result, return: result.return as unknown as (undefined | TrustAiReturns['release_payment()string'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new TrustAiClient(this.appClient.clone(params))
  }

  /**
   * Methods to access state for the current TrustAI app
   */
  state = {
    /**
     * Methods to access global state for the current TrustAI app
     */
    global: {
      /**
       * Get all current keyed values from global state
       */
      getAll: async (): Promise<Partial<Expand<GlobalKeysState>>> => {
        const result = await this.appClient.state.global.getAll()
        return {
          client: result.client,
          freelancer: result.freelancer,
          amount: result.amount,
          validated: result.validated,
        }
      },
      /**
       * Get the current value of the client key in global state
       */
      client: async (): Promise<string | undefined> => { return (await this.appClient.state.global.getValue("client")) as string | undefined },
      /**
       * Get the current value of the freelancer key in global state
       */
      freelancer: async (): Promise<string | undefined> => { return (await this.appClient.state.global.getValue("freelancer")) as string | undefined },
      /**
       * Get the current value of the amount key in global state
       */
      amount: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("amount")) as bigint | undefined },
      /**
       * Get the current value of the validated key in global state
       */
      validated: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("validated")) as bigint | undefined },
    },
  }

  public newGroup(): TrustAiComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a fund_contract(address,address,uint64)string method call against the TrustAI contract
       */
      fundContract(params: CallParams<TrustAiArgs['obj']['fund_contract(address,address,uint64)string'] | TrustAiArgs['tuple']['fund_contract(address,address,uint64)string']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.fundContract(params)))
        resultMappers.push((v) => client.decodeReturnValue('fund_contract(address,address,uint64)string', v))
        return this
      },
      /**
       * Add a validate_with_ai(bool)string method call against the TrustAI contract
       */
      validateWithAi(params: CallParams<TrustAiArgs['obj']['validate_with_ai(bool)string'] | TrustAiArgs['tuple']['validate_with_ai(bool)string']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.validateWithAi(params)))
        resultMappers.push((v) => client.decodeReturnValue('validate_with_ai(bool)string', v))
        return this
      },
      /**
       * Add a release_payment()string method call against the TrustAI contract
       */
      releasePayment(params: CallParams<TrustAiArgs['obj']['release_payment()string'] | TrustAiArgs['tuple']['release_payment()string']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.releasePayment(params)))
        resultMappers.push((v) => client.decodeReturnValue('release_payment()string', v))
        return this
      },
      /**
       * Add a clear state call to the TrustAI contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as TrustAiComposer
  }
}
export type TrustAiComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the fund_contract(address,address,uint64)string ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  fundContract(params?: CallParams<TrustAiArgs['obj']['fund_contract(address,address,uint64)string'] | TrustAiArgs['tuple']['fund_contract(address,address,uint64)string']>): TrustAiComposer<[...TReturns, TrustAiReturns['fund_contract(address,address,uint64)string'] | undefined]>

  /**
   * Calls the validate_with_ai(bool)string ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  validateWithAi(params?: CallParams<TrustAiArgs['obj']['validate_with_ai(bool)string'] | TrustAiArgs['tuple']['validate_with_ai(bool)string']>): TrustAiComposer<[...TReturns, TrustAiReturns['validate_with_ai(bool)string'] | undefined]>

  /**
   * Calls the release_payment()string ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  releasePayment(params?: CallParams<TrustAiArgs['obj']['release_payment()string'] | TrustAiArgs['tuple']['release_payment()string']>): TrustAiComposer<[...TReturns, TrustAiReturns['release_payment()string'] | undefined]>

  /**
   * Makes a clear_state call to an existing instance of the TrustAI smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): TrustAiComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): TrustAiComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<TrustAiComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<TrustAiComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<TrustAiComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<TrustAiComposerResults<TReturns>>
}
export type TrustAiComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

