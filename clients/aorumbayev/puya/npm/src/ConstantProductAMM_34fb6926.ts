/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"ConstantProductAMM","structs":{},"methods":[{"name":"set_governor","args":[{"type":"account","name":"new_governor"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"sets the governor of the contract, may only be called by the current governor","events":[],"recommendations":{}},{"name":"bootstrap","args":[{"type":"pay","name":"seed","desc":"Initial Payment transaction to the app account so it can opt in to assets and create pool token."},{"type":"asset","name":"a_asset","desc":"One of the two assets this pool should allow swapping between."},{"type":"asset","name":"b_asset","desc":"The other of the two assets this pool should allow swapping between."}],"returns":{"type":"uint64","desc":"The asset id of the pool token created."},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"bootstraps the contract by opting into the assets and creating the pool token.\nNote this method will fail if it is attempted more than once on the same contract since the assets and pool token application state values are marked as static and cannot be overridden.","events":[],"recommendations":{}},{"name":"mint","args":[{"type":"axfer","name":"a_xfer","desc":"Asset Transfer Transaction of asset A as a deposit to the pool in exchange for pool tokens."},{"type":"axfer","name":"b_xfer","desc":"Asset Transfer Transaction of asset B as a deposit to the pool in exchange for pool tokens."},{"type":"asset","name":"pool_asset","desc":"The asset ID of the pool token so that we may distribute it.","defaultValue":{"source":"global","data":"cG9vbF90b2tlbg==","type":"AVMString"}},{"type":"asset","name":"a_asset","desc":"The asset ID of the Asset A so that we may inspect our balance.","defaultValue":{"source":"global","data":"YXNzZXRfYQ==","type":"AVMString"}},{"type":"asset","name":"b_asset","desc":"The asset ID of the Asset B so that we may inspect our balance.","defaultValue":{"source":"global","data":"YXNzZXRfYg==","type":"AVMString"}}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"mint pool tokens given some amount of asset A and asset B.\nGiven some amount of Asset A and Asset B in the transfers, mint some number of pool tokens commensurate with the pools current balance and circulating supply of pool tokens.","events":[],"recommendations":{}},{"name":"burn","args":[{"type":"axfer","name":"pool_xfer","desc":"Asset Transfer Transaction of the pool token for the amount the sender wishes to redeem"},{"type":"asset","name":"pool_asset","desc":"Asset ID of the pool token so we may inspect balance.","defaultValue":{"source":"global","data":"cG9vbF90b2tlbg==","type":"AVMString"}},{"type":"asset","name":"a_asset","desc":"Asset ID of Asset A so we may inspect balance and distribute it","defaultValue":{"source":"global","data":"YXNzZXRfYQ==","type":"AVMString"}},{"type":"asset","name":"b_asset","desc":"Asset ID of Asset B so we may inspect balance and distribute it","defaultValue":{"source":"global","data":"YXNzZXRfYg==","type":"AVMString"}}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"burn pool tokens to get back some amount of asset A and asset B","events":[],"recommendations":{}},{"name":"swap","args":[{"type":"axfer","name":"swap_xfer","desc":"Asset Transfer Transaction of either Asset A or Asset B"},{"type":"asset","name":"a_asset","desc":"Asset ID of asset A so we may inspect balance and possibly transfer it","defaultValue":{"source":"global","data":"YXNzZXRfYQ==","type":"AVMString"}},{"type":"asset","name":"b_asset","desc":"Asset ID of asset B so we may inspect balance and possibly transfer it","defaultValue":{"source":"global","data":"YXNzZXRfYg==","type":"AVMString"}}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Swap some amount of either asset A or asset B for the other","events":[],"recommendations":{}}],"arcs":[22,28],"networks":{},"state":{"schema":{"global":{"ints":4,"bytes":1},"local":{"ints":0,"bytes":0}},"keys":{"global":{"asset_a":{"keyType":"AVMString","valueType":"AVMUint64","key":"YXNzZXRfYQ=="},"asset_b":{"keyType":"AVMString","valueType":"AVMUint64","key":"YXNzZXRfYg=="},"governor":{"keyType":"AVMString","valueType":"AVMBytes","key":"Z292ZXJub3I="},"pool_token":{"keyType":"AVMString","valueType":"AVMUint64","key":"cG9vbF90b2tlbg=="},"ratio":{"keyType":"AVMString","valueType":"AVMUint64","key":"cmF0aW8="}},"local":{},"box":{}},"maps":{"global":{},"local":{},"box":{}}},"bareActions":{"create":["NoOp"],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[131,165,205,256,300],"errorMessage":"OnCompletion is not NoOp"},{"pc":[997],"errorMessage":"Only the account set in global_state.governor may call this method"},{"pc":[1007,1017,1027],"errorMessage":"account opted into asset"},{"pc":[396,577,603,766,873],"errorMessage":"amount minimum not met"},{"pc":[369],"errorMessage":"application has already been bootstrapped"},{"pc":[528,570,744,859],"errorMessage":"asset a incorrect"},{"pc":[402],"errorMessage":"asset a must be less than asset b"},{"pc":[536,595,752,867],"errorMessage":"asset b incorrect"},{"pc":[418,437],"errorMessage":"asset exists"},{"pc":[900],"errorMessage":"asset id incorrect"},{"pc":[520,736,776],"errorMessage":"asset pool incorrect"},{"pc":[512,728,851],"errorMessage":"bootstrap method needs to be called first"},{"pc":[323],"errorMessage":"can only call when creating"},{"pc":[134,168,208,259,303],"errorMessage":"can only call when not creating"},{"pc":[415,478,524,568,740,820,855,885,970,1014],"errorMessage":"check self.asset_a exists"},{"pc":[434,489,532,593,748,831,863,889,915,1024],"errorMessage":"check self.asset_b exists"},{"pc":[995],"errorMessage":"check self.governor exists"},{"pc":[367,500,511,516,650,727,732,774,850,1004],"errorMessage":"check self.pool_token exists"},{"pc":[378],"errorMessage":"group size not 2"},{"pc":[386,560,585,760],"errorMessage":"receiver not app address"},{"pc":[644,942],"errorMessage":"send amount too low"},{"pc":[544,552,784,881],"errorMessage":"sender invalid"},{"pc":[144,178,219,229],"errorMessage":"transaction type is axfer"},{"pc":[269],"errorMessage":"transaction type is pay"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"events":[]} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


/**
 * The argument types for the ConstantProductAmm contract
 */
export type ConstantProductAmmArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'set_governor(account)void': {
      newGovernor: Uint8Array | string
    }
    'bootstrap(pay,asset,asset)uint64': {
      /**
       * Initial Payment transaction to the app account so it can opt in to assets and create pool token.
       */
      seed: AppMethodCallTransactionArgument
      /**
       * One of the two assets this pool should allow swapping between.
       */
      aAsset: bigint
      /**
       * The other of the two assets this pool should allow swapping between.
       */
      bAsset: bigint
    }
    'mint(axfer,axfer,asset,asset,asset)void': {
      /**
       * Asset Transfer Transaction of asset A as a deposit to the pool in exchange for pool tokens.
       */
      aXfer: AppMethodCallTransactionArgument
      /**
       * Asset Transfer Transaction of asset B as a deposit to the pool in exchange for pool tokens.
       */
      bXfer: AppMethodCallTransactionArgument
      /**
       * The asset ID of the pool token so that we may distribute it.
       */
      poolAsset?: bigint
      /**
       * The asset ID of the Asset A so that we may inspect our balance.
       */
      aAsset?: bigint
      /**
       * The asset ID of the Asset B so that we may inspect our balance.
       */
      bAsset?: bigint
    }
    'burn(axfer,asset,asset,asset)void': {
      /**
       * Asset Transfer Transaction of the pool token for the amount the sender wishes to redeem
       */
      poolXfer: AppMethodCallTransactionArgument
      /**
       * Asset ID of the pool token so we may inspect balance.
       */
      poolAsset?: bigint
      /**
       * Asset ID of Asset A so we may inspect balance and distribute it
       */
      aAsset?: bigint
      /**
       * Asset ID of Asset B so we may inspect balance and distribute it
       */
      bAsset?: bigint
    }
    'swap(axfer,asset,asset)void': {
      /**
       * Asset Transfer Transaction of either Asset A or Asset B
       */
      swapXfer: AppMethodCallTransactionArgument
      /**
       * Asset ID of asset A so we may inspect balance and possibly transfer it
       */
      aAsset?: bigint
      /**
       * Asset ID of asset B so we may inspect balance and possibly transfer it
       */
      bAsset?: bigint
    }
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'set_governor(account)void': [newGovernor: Uint8Array | string]
    'bootstrap(pay,asset,asset)uint64': [seed: AppMethodCallTransactionArgument, aAsset: bigint, bAsset: bigint]
    'mint(axfer,axfer,asset,asset,asset)void': [aXfer: AppMethodCallTransactionArgument, bXfer: AppMethodCallTransactionArgument, poolAsset: bigint | undefined, aAsset: bigint | undefined, bAsset: bigint | undefined]
    'burn(axfer,asset,asset,asset)void': [poolXfer: AppMethodCallTransactionArgument, poolAsset: bigint | undefined, aAsset: bigint | undefined, bAsset: bigint | undefined]
    'swap(axfer,asset,asset)void': [swapXfer: AppMethodCallTransactionArgument, aAsset: bigint | undefined, bAsset: bigint | undefined]
  }
}

/**
 * The return type for each method
 */
export type ConstantProductAmmReturns = {
  'set_governor(account)void': void
  'bootstrap(pay,asset,asset)uint64': bigint
  'mint(axfer,axfer,asset,asset,asset)void': void
  'burn(axfer,asset,asset,asset)void': void
  'swap(axfer,asset,asset)void': void
}

/**
 * Defines the types of available calls and state of the ConstantProductAmm smart contract.
 */
export type ConstantProductAmmTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'set_governor(account)void' | 'set_governor', {
      argsObj: ConstantProductAmmArgs['obj']['set_governor(account)void']
      argsTuple: ConstantProductAmmArgs['tuple']['set_governor(account)void']
      returns: ConstantProductAmmReturns['set_governor(account)void']
    }>
    & Record<'bootstrap(pay,asset,asset)uint64' | 'bootstrap', {
      argsObj: ConstantProductAmmArgs['obj']['bootstrap(pay,asset,asset)uint64']
      argsTuple: ConstantProductAmmArgs['tuple']['bootstrap(pay,asset,asset)uint64']
      /**
       * The asset id of the pool token created.
       */
      returns: ConstantProductAmmReturns['bootstrap(pay,asset,asset)uint64']
    }>
    & Record<'mint(axfer,axfer,asset,asset,asset)void' | 'mint', {
      argsObj: ConstantProductAmmArgs['obj']['mint(axfer,axfer,asset,asset,asset)void']
      argsTuple: ConstantProductAmmArgs['tuple']['mint(axfer,axfer,asset,asset,asset)void']
      returns: ConstantProductAmmReturns['mint(axfer,axfer,asset,asset,asset)void']
    }>
    & Record<'burn(axfer,asset,asset,asset)void' | 'burn', {
      argsObj: ConstantProductAmmArgs['obj']['burn(axfer,asset,asset,asset)void']
      argsTuple: ConstantProductAmmArgs['tuple']['burn(axfer,asset,asset,asset)void']
      returns: ConstantProductAmmReturns['burn(axfer,asset,asset,asset)void']
    }>
    & Record<'swap(axfer,asset,asset)void' | 'swap', {
      argsObj: ConstantProductAmmArgs['obj']['swap(axfer,asset,asset)void']
      argsTuple: ConstantProductAmmArgs['tuple']['swap(axfer,asset,asset)void']
      returns: ConstantProductAmmReturns['swap(axfer,asset,asset)void']
    }>
  /**
   * Defines the shape of the state of the application.
   */
  state: {
    global: {
      keys: {
        assetA: bigint
        assetB: bigint
        governor: BinaryState
        poolToken: bigint
        ratio: bigint
      }
      maps: {}
    }
  }
}

/**
 * Defines the possible abi call signatures.
 */
export type ConstantProductAmmSignatures = keyof ConstantProductAmmTypes['methods']
/**
 * Defines the possible abi call signatures for methods that return a non-void value.
 */
export type ConstantProductAmmNonVoidMethodSignatures = keyof ConstantProductAmmTypes['methods'] extends infer T ? T extends keyof ConstantProductAmmTypes['methods'] ? MethodReturn<T> extends void ? never : T  : never : never
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the ConstantProductAmm smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends ConstantProductAmmSignatures> = ConstantProductAmmTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the ConstantProductAmm smart contract to the method's return type
 */
export type MethodReturn<TSignature extends ConstantProductAmmSignatures> = ConstantProductAmmTypes['methods'][TSignature]['returns']

/**
 * Defines the shape of the keyed global state of the application.
 */
export type GlobalKeysState = ConstantProductAmmTypes['state']['global']['keys']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the ConstantProductAmm smart contract
 */
export abstract class ConstantProductAmmParamsFactory {
  /**
   * Constructs a no op call for the set_governor(account)void ABI method
   *
   * sets the governor of the contract, may only be called by the current governor
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static setGovernor(params: CallParams<ConstantProductAmmArgs['obj']['set_governor(account)void'] | ConstantProductAmmArgs['tuple']['set_governor(account)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'set_governor(account)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.newGovernor],
    }
  }
  /**
   * Constructs a no op call for the bootstrap(pay,asset,asset)uint64 ABI method
   *
  * bootstraps the contract by opting into the assets and creating the pool token.
  Note this method will fail if it is attempted more than once on the same contract since the assets and pool token application state values are marked as static and cannot be overridden.

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static bootstrap(params: CallParams<ConstantProductAmmArgs['obj']['bootstrap(pay,asset,asset)uint64'] | ConstantProductAmmArgs['tuple']['bootstrap(pay,asset,asset)uint64']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'bootstrap(pay,asset,asset)uint64' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.seed, params.args.aAsset, params.args.bAsset],
    }
  }
  /**
   * Constructs a no op call for the mint(axfer,axfer,asset,asset,asset)void ABI method
   *
  * mint pool tokens given some amount of asset A and asset B.
  Given some amount of Asset A and Asset B in the transfers, mint some number of pool tokens commensurate with the pools current balance and circulating supply of pool tokens.

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static mint(params: CallParams<ConstantProductAmmArgs['obj']['mint(axfer,axfer,asset,asset,asset)void'] | ConstantProductAmmArgs['tuple']['mint(axfer,axfer,asset,asset,asset)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'mint(axfer,axfer,asset,asset,asset)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.aXfer, params.args.bXfer, params.args.poolAsset, params.args.aAsset, params.args.bAsset],
    }
  }
  /**
   * Constructs a no op call for the burn(axfer,asset,asset,asset)void ABI method
   *
   * burn pool tokens to get back some amount of asset A and asset B
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static burn(params: CallParams<ConstantProductAmmArgs['obj']['burn(axfer,asset,asset,asset)void'] | ConstantProductAmmArgs['tuple']['burn(axfer,asset,asset,asset)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'burn(axfer,asset,asset,asset)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.poolXfer, params.args.poolAsset, params.args.aAsset, params.args.bAsset],
    }
  }
  /**
   * Constructs a no op call for the swap(axfer,asset,asset)void ABI method
   *
   * Swap some amount of either asset A or asset B for the other
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static swap(params: CallParams<ConstantProductAmmArgs['obj']['swap(axfer,asset,asset)void'] | ConstantProductAmmArgs['tuple']['swap(axfer,asset,asset)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'swap(axfer,asset,asset)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.swapXfer, params.args.aAsset, params.args.bAsset],
    }
  }
}

/**
 * A client to make calls to the ConstantProductAMM smart contract
 */
export class ConstantProductAmmClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `ConstantProductAmmClient`
   *
   * @param appClient An `AppClient` instance which has been created with the ConstantProductAmm app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `ConstantProductAmmClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Checks for decode errors on the given return value and maps the return value to the return type for the given method
   * @returns The typed return value or undefined if there was no value
   */
  decodeReturnValue<TSignature extends ConstantProductAmmNonVoidMethodSignatures>(method: TSignature, returnValue: ABIReturn | undefined) {
    return returnValue !== undefined ? getArc56ReturnValue<MethodReturn<TSignature>>(returnValue, this.appClient.getABIMethod(method), APP_SPEC.structs) : undefined
  }

  /**
   * Returns a new `ConstantProductAmmClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<ConstantProductAmmClient> {
    return new ConstantProductAmmClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `ConstantProductAmmClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<ConstantProductAmmClient> {
    return new ConstantProductAmmClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Makes a clear_state call to an existing instance of the ConstantProductAMM smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the ConstantProductAMM smart contract using the `set_governor(account)void` ABI method.
     *
     * sets the governor of the contract, may only be called by the current governor
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    setGovernor: (params: CallParams<ConstantProductAmmArgs['obj']['set_governor(account)void'] | ConstantProductAmmArgs['tuple']['set_governor(account)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(ConstantProductAmmParamsFactory.setGovernor(params))
    },

    /**
     * Makes a call to the ConstantProductAMM smart contract using the `bootstrap(pay,asset,asset)uint64` ABI method.
     *
    * bootstraps the contract by opting into the assets and creating the pool token.
    Note this method will fail if it is attempted more than once on the same contract since the assets and pool token application state values are marked as static and cannot be overridden.

     *
     * @param params The params for the smart contract call
     * @returns The call params: The asset id of the pool token created.
     */
    bootstrap: (params: CallParams<ConstantProductAmmArgs['obj']['bootstrap(pay,asset,asset)uint64'] | ConstantProductAmmArgs['tuple']['bootstrap(pay,asset,asset)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(ConstantProductAmmParamsFactory.bootstrap(params))
    },

    /**
     * Makes a call to the ConstantProductAMM smart contract using the `mint(axfer,axfer,asset,asset,asset)void` ABI method.
     *
    * mint pool tokens given some amount of asset A and asset B.
    Given some amount of Asset A and Asset B in the transfers, mint some number of pool tokens commensurate with the pools current balance and circulating supply of pool tokens.

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    mint: (params: CallParams<ConstantProductAmmArgs['obj']['mint(axfer,axfer,asset,asset,asset)void'] | ConstantProductAmmArgs['tuple']['mint(axfer,axfer,asset,asset,asset)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(ConstantProductAmmParamsFactory.mint(params))
    },

    /**
     * Makes a call to the ConstantProductAMM smart contract using the `burn(axfer,asset,asset,asset)void` ABI method.
     *
     * burn pool tokens to get back some amount of asset A and asset B
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    burn: (params: CallParams<ConstantProductAmmArgs['obj']['burn(axfer,asset,asset,asset)void'] | ConstantProductAmmArgs['tuple']['burn(axfer,asset,asset,asset)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(ConstantProductAmmParamsFactory.burn(params))
    },

    /**
     * Makes a call to the ConstantProductAMM smart contract using the `swap(axfer,asset,asset)void` ABI method.
     *
     * Swap some amount of either asset A or asset B for the other
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    swap: (params: CallParams<ConstantProductAmmArgs['obj']['swap(axfer,asset,asset)void'] | ConstantProductAmmArgs['tuple']['swap(axfer,asset,asset)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(ConstantProductAmmParamsFactory.swap(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Makes a clear_state call to an existing instance of the ConstantProductAMM smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the ConstantProductAMM smart contract using the `set_governor(account)void` ABI method.
     *
     * sets the governor of the contract, may only be called by the current governor
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    setGovernor: (params: CallParams<ConstantProductAmmArgs['obj']['set_governor(account)void'] | ConstantProductAmmArgs['tuple']['set_governor(account)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(ConstantProductAmmParamsFactory.setGovernor(params))
    },

    /**
     * Makes a call to the ConstantProductAMM smart contract using the `bootstrap(pay,asset,asset)uint64` ABI method.
     *
    * bootstraps the contract by opting into the assets and creating the pool token.
    Note this method will fail if it is attempted more than once on the same contract since the assets and pool token application state values are marked as static and cannot be overridden.

     *
     * @param params The params for the smart contract call
     * @returns The call transaction: The asset id of the pool token created.
     */
    bootstrap: (params: CallParams<ConstantProductAmmArgs['obj']['bootstrap(pay,asset,asset)uint64'] | ConstantProductAmmArgs['tuple']['bootstrap(pay,asset,asset)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(ConstantProductAmmParamsFactory.bootstrap(params))
    },

    /**
     * Makes a call to the ConstantProductAMM smart contract using the `mint(axfer,axfer,asset,asset,asset)void` ABI method.
     *
    * mint pool tokens given some amount of asset A and asset B.
    Given some amount of Asset A and Asset B in the transfers, mint some number of pool tokens commensurate with the pools current balance and circulating supply of pool tokens.

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    mint: (params: CallParams<ConstantProductAmmArgs['obj']['mint(axfer,axfer,asset,asset,asset)void'] | ConstantProductAmmArgs['tuple']['mint(axfer,axfer,asset,asset,asset)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(ConstantProductAmmParamsFactory.mint(params))
    },

    /**
     * Makes a call to the ConstantProductAMM smart contract using the `burn(axfer,asset,asset,asset)void` ABI method.
     *
     * burn pool tokens to get back some amount of asset A and asset B
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    burn: (params: CallParams<ConstantProductAmmArgs['obj']['burn(axfer,asset,asset,asset)void'] | ConstantProductAmmArgs['tuple']['burn(axfer,asset,asset,asset)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(ConstantProductAmmParamsFactory.burn(params))
    },

    /**
     * Makes a call to the ConstantProductAMM smart contract using the `swap(axfer,asset,asset)void` ABI method.
     *
     * Swap some amount of either asset A or asset B for the other
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    swap: (params: CallParams<ConstantProductAmmArgs['obj']['swap(axfer,asset,asset)void'] | ConstantProductAmmArgs['tuple']['swap(axfer,asset,asset)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(ConstantProductAmmParamsFactory.swap(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Makes a clear_state call to an existing instance of the ConstantProductAMM smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the ConstantProductAMM smart contract using the `set_governor(account)void` ABI method.
     *
     * sets the governor of the contract, may only be called by the current governor
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    setGovernor: async (params: CallParams<ConstantProductAmmArgs['obj']['set_governor(account)void'] | ConstantProductAmmArgs['tuple']['set_governor(account)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(ConstantProductAmmParamsFactory.setGovernor(params))
      return {...result, return: result.return as unknown as (undefined | ConstantProductAmmReturns['set_governor(account)void'])}
    },

    /**
     * Makes a call to the ConstantProductAMM smart contract using the `bootstrap(pay,asset,asset)uint64` ABI method.
     *
    * bootstraps the contract by opting into the assets and creating the pool token.
    Note this method will fail if it is attempted more than once on the same contract since the assets and pool token application state values are marked as static and cannot be overridden.

     *
     * @param params The params for the smart contract call
     * @returns The call result: The asset id of the pool token created.
     */
    bootstrap: async (params: CallParams<ConstantProductAmmArgs['obj']['bootstrap(pay,asset,asset)uint64'] | ConstantProductAmmArgs['tuple']['bootstrap(pay,asset,asset)uint64']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(ConstantProductAmmParamsFactory.bootstrap(params))
      return {...result, return: result.return as unknown as (undefined | ConstantProductAmmReturns['bootstrap(pay,asset,asset)uint64'])}
    },

    /**
     * Makes a call to the ConstantProductAMM smart contract using the `mint(axfer,axfer,asset,asset,asset)void` ABI method.
     *
    * mint pool tokens given some amount of asset A and asset B.
    Given some amount of Asset A and Asset B in the transfers, mint some number of pool tokens commensurate with the pools current balance and circulating supply of pool tokens.

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    mint: async (params: CallParams<ConstantProductAmmArgs['obj']['mint(axfer,axfer,asset,asset,asset)void'] | ConstantProductAmmArgs['tuple']['mint(axfer,axfer,asset,asset,asset)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(ConstantProductAmmParamsFactory.mint(params))
      return {...result, return: result.return as unknown as (undefined | ConstantProductAmmReturns['mint(axfer,axfer,asset,asset,asset)void'])}
    },

    /**
     * Makes a call to the ConstantProductAMM smart contract using the `burn(axfer,asset,asset,asset)void` ABI method.
     *
     * burn pool tokens to get back some amount of asset A and asset B
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    burn: async (params: CallParams<ConstantProductAmmArgs['obj']['burn(axfer,asset,asset,asset)void'] | ConstantProductAmmArgs['tuple']['burn(axfer,asset,asset,asset)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(ConstantProductAmmParamsFactory.burn(params))
      return {...result, return: result.return as unknown as (undefined | ConstantProductAmmReturns['burn(axfer,asset,asset,asset)void'])}
    },

    /**
     * Makes a call to the ConstantProductAMM smart contract using the `swap(axfer,asset,asset)void` ABI method.
     *
     * Swap some amount of either asset A or asset B for the other
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    swap: async (params: CallParams<ConstantProductAmmArgs['obj']['swap(axfer,asset,asset)void'] | ConstantProductAmmArgs['tuple']['swap(axfer,asset,asset)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(ConstantProductAmmParamsFactory.swap(params))
      return {...result, return: result.return as unknown as (undefined | ConstantProductAmmReturns['swap(axfer,asset,asset)void'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new ConstantProductAmmClient(this.appClient.clone(params))
  }

  /**
   * Methods to access state for the current ConstantProductAMM app
   */
  state = {
    /**
     * Methods to access global state for the current ConstantProductAMM app
     */
    global: {
      /**
       * Get all current keyed values from global state
       */
      getAll: async (): Promise<Partial<Expand<GlobalKeysState>>> => {
        const result = await this.appClient.state.global.getAll()
        return {
          assetA: result.asset_a,
          assetB: result.asset_b,
          governor: new BinaryStateValue(result.governor),
          poolToken: result.pool_token,
          ratio: result.ratio,
        }
      },
      /**
       * Get the current value of the asset_a key in global state
       */
      assetA: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("asset_a")) as bigint | undefined },
      /**
       * Get the current value of the asset_b key in global state
       */
      assetB: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("asset_b")) as bigint | undefined },
      /**
       * Get the current value of the governor key in global state
       */
      governor: async (): Promise<BinaryState> => { return new BinaryStateValue((await this.appClient.state.global.getValue("governor")) as Uint8Array | undefined) },
      /**
       * Get the current value of the pool_token key in global state
       */
      poolToken: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("pool_token")) as bigint | undefined },
      /**
       * Get the current value of the ratio key in global state
       */
      ratio: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("ratio")) as bigint | undefined },
    },
  }

  public newGroup(): ConstantProductAmmComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a set_governor(account)void method call against the ConstantProductAMM contract
       */
      setGovernor(params: CallParams<ConstantProductAmmArgs['obj']['set_governor(account)void'] | ConstantProductAmmArgs['tuple']['set_governor(account)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.setGovernor(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a bootstrap(pay,asset,asset)uint64 method call against the ConstantProductAMM contract
       */
      bootstrap(params: CallParams<ConstantProductAmmArgs['obj']['bootstrap(pay,asset,asset)uint64'] | ConstantProductAmmArgs['tuple']['bootstrap(pay,asset,asset)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.bootstrap(params)))
        resultMappers.push((v) => client.decodeReturnValue('bootstrap(pay,asset,asset)uint64', v))
        return this
      },
      /**
       * Add a mint(axfer,axfer,asset,asset,asset)void method call against the ConstantProductAMM contract
       */
      mint(params: CallParams<ConstantProductAmmArgs['obj']['mint(axfer,axfer,asset,asset,asset)void'] | ConstantProductAmmArgs['tuple']['mint(axfer,axfer,asset,asset,asset)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.mint(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a burn(axfer,asset,asset,asset)void method call against the ConstantProductAMM contract
       */
      burn(params: CallParams<ConstantProductAmmArgs['obj']['burn(axfer,asset,asset,asset)void'] | ConstantProductAmmArgs['tuple']['burn(axfer,asset,asset,asset)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.burn(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a swap(axfer,asset,asset)void method call against the ConstantProductAMM contract
       */
      swap(params: CallParams<ConstantProductAmmArgs['obj']['swap(axfer,asset,asset)void'] | ConstantProductAmmArgs['tuple']['swap(axfer,asset,asset)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.swap(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a clear state call to the ConstantProductAMM contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as ConstantProductAmmComposer
  }
}
export type ConstantProductAmmComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the set_governor(account)void ABI method.
   *
   * sets the governor of the contract, may only be called by the current governor
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  setGovernor(params?: CallParams<ConstantProductAmmArgs['obj']['set_governor(account)void'] | ConstantProductAmmArgs['tuple']['set_governor(account)void']>): ConstantProductAmmComposer<[...TReturns, ConstantProductAmmReturns['set_governor(account)void'] | undefined]>

  /**
   * Calls the bootstrap(pay,asset,asset)uint64 ABI method.
   *
  * bootstraps the contract by opting into the assets and creating the pool token.
  Note this method will fail if it is attempted more than once on the same contract since the assets and pool token application state values are marked as static and cannot be overridden.

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  bootstrap(params?: CallParams<ConstantProductAmmArgs['obj']['bootstrap(pay,asset,asset)uint64'] | ConstantProductAmmArgs['tuple']['bootstrap(pay,asset,asset)uint64']>): ConstantProductAmmComposer<[...TReturns, ConstantProductAmmReturns['bootstrap(pay,asset,asset)uint64'] | undefined]>

  /**
   * Calls the mint(axfer,axfer,asset,asset,asset)void ABI method.
   *
  * mint pool tokens given some amount of asset A and asset B.
  Given some amount of Asset A and Asset B in the transfers, mint some number of pool tokens commensurate with the pools current balance and circulating supply of pool tokens.

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  mint(params?: CallParams<ConstantProductAmmArgs['obj']['mint(axfer,axfer,asset,asset,asset)void'] | ConstantProductAmmArgs['tuple']['mint(axfer,axfer,asset,asset,asset)void']>): ConstantProductAmmComposer<[...TReturns, ConstantProductAmmReturns['mint(axfer,axfer,asset,asset,asset)void'] | undefined]>

  /**
   * Calls the burn(axfer,asset,asset,asset)void ABI method.
   *
   * burn pool tokens to get back some amount of asset A and asset B
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  burn(params?: CallParams<ConstantProductAmmArgs['obj']['burn(axfer,asset,asset,asset)void'] | ConstantProductAmmArgs['tuple']['burn(axfer,asset,asset,asset)void']>): ConstantProductAmmComposer<[...TReturns, ConstantProductAmmReturns['burn(axfer,asset,asset,asset)void'] | undefined]>

  /**
   * Calls the swap(axfer,asset,asset)void ABI method.
   *
   * Swap some amount of either asset A or asset B for the other
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  swap(params?: CallParams<ConstantProductAmmArgs['obj']['swap(axfer,asset,asset)void'] | ConstantProductAmmArgs['tuple']['swap(axfer,asset,asset)void']>): ConstantProductAmmComposer<[...TReturns, ConstantProductAmmReturns['swap(axfer,asset,asset)void'] | undefined]>

  /**
   * Makes a clear_state call to an existing instance of the ConstantProductAMM smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): ConstantProductAmmComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): ConstantProductAmmComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<ConstantProductAmmComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<ConstantProductAmmComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<ConstantProductAmmComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<ConstantProductAmmComposerResults<TReturns>>
}
export type ConstantProductAmmComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

