/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"BranchElimination","structs":{},"methods":[{"name":"umm","args":[],"returns":{"type":"uint64"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}},{"name":"umm2","args":[],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}},{"name":"calculate","args":[{"type":"uint64[][]","name":"nested_list"},{"type":"uint64","name":"threshold"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}}],"arcs":[22,28],"networks":{},"state":{"schema":{"global":{"ints":0,"bytes":0},"local":{"ints":0,"bytes":0}},"keys":{"global":{},"local":{},"box":{}},"maps":{"global":{},"local":{},"box":{}}},"bareActions":{"create":["NoOp"],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[285,337,388],"errorMessage":"Index access is out of bounds"},{"pc":[45,63,75],"errorMessage":"OnCompletion is not NoOp"},{"pc":[102],"errorMessage":"can only call when creating"},{"pc":[48,66,78],"errorMessage":"can only call when not creating"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"events":[]} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


/**
 * The argument types for the BranchElimination contract
 */
export type BranchEliminationArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'umm()uint64': Record<string, never>
    'umm2()void': Record<string, never>
    'calculate(uint64[][],uint64)void': {
      nestedList: bigint[] | number[][]
      threshold: bigint | number
    }
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'umm()uint64': []
    'umm2()void': []
    'calculate(uint64[][],uint64)void': [nestedList: bigint[] | number[][], threshold: bigint | number]
  }
}

/**
 * The return type for each method
 */
export type BranchEliminationReturns = {
  'umm()uint64': bigint
  'umm2()void': void
  'calculate(uint64[][],uint64)void': void
}

/**
 * Defines the types of available calls and state of the BranchElimination smart contract.
 */
export type BranchEliminationTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'umm()uint64' | 'umm', {
      argsObj: BranchEliminationArgs['obj']['umm()uint64']
      argsTuple: BranchEliminationArgs['tuple']['umm()uint64']
      returns: BranchEliminationReturns['umm()uint64']
    }>
    & Record<'umm2()void' | 'umm2', {
      argsObj: BranchEliminationArgs['obj']['umm2()void']
      argsTuple: BranchEliminationArgs['tuple']['umm2()void']
      returns: BranchEliminationReturns['umm2()void']
    }>
    & Record<'calculate(uint64[][],uint64)void' | 'calculate', {
      argsObj: BranchEliminationArgs['obj']['calculate(uint64[][],uint64)void']
      argsTuple: BranchEliminationArgs['tuple']['calculate(uint64[][],uint64)void']
      returns: BranchEliminationReturns['calculate(uint64[][],uint64)void']
    }>
}

/**
 * Defines the possible abi call signatures.
 */
export type BranchEliminationSignatures = keyof BranchEliminationTypes['methods']
/**
 * Defines the possible abi call signatures for methods that return a non-void value.
 */
export type BranchEliminationNonVoidMethodSignatures = keyof BranchEliminationTypes['methods'] extends infer T ? T extends keyof BranchEliminationTypes['methods'] ? MethodReturn<T> extends void ? never : T  : never : never
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the BranchElimination smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends BranchEliminationSignatures> = BranchEliminationTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the BranchElimination smart contract to the method's return type
 */
export type MethodReturn<TSignature extends BranchEliminationSignatures> = BranchEliminationTypes['methods'][TSignature]['returns']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the BranchElimination smart contract
 */
export abstract class BranchEliminationParamsFactory {
  /**
   * Constructs a no op call for the umm()uint64 ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static umm(params: CallParams<BranchEliminationArgs['obj']['umm()uint64'] | BranchEliminationArgs['tuple']['umm()uint64']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'umm()uint64' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the umm2()void ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static umm2(params: CallParams<BranchEliminationArgs['obj']['umm2()void'] | BranchEliminationArgs['tuple']['umm2()void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'umm2()void' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the calculate(uint64[][],uint64)void ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static calculate(params: CallParams<BranchEliminationArgs['obj']['calculate(uint64[][],uint64)void'] | BranchEliminationArgs['tuple']['calculate(uint64[][],uint64)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'calculate(uint64[][],uint64)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.nestedList, params.args.threshold],
    }
  }
}

/**
 * A client to make calls to the BranchElimination smart contract
 */
export class BranchEliminationClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `BranchEliminationClient`
   *
   * @param appClient An `AppClient` instance which has been created with the BranchElimination app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `BranchEliminationClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Checks for decode errors on the given return value and maps the return value to the return type for the given method
   * @returns The typed return value or undefined if there was no value
   */
  decodeReturnValue<TSignature extends BranchEliminationNonVoidMethodSignatures>(method: TSignature, returnValue: ABIReturn | undefined) {
    return returnValue !== undefined ? getArc56ReturnValue<MethodReturn<TSignature>>(returnValue, this.appClient.getABIMethod(method), APP_SPEC.structs) : undefined
  }

  /**
   * Returns a new `BranchEliminationClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<BranchEliminationClient> {
    return new BranchEliminationClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `BranchEliminationClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<BranchEliminationClient> {
    return new BranchEliminationClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Makes a clear_state call to an existing instance of the BranchElimination smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the BranchElimination smart contract using the `umm()uint64` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    umm: (params: CallParams<BranchEliminationArgs['obj']['umm()uint64'] | BranchEliminationArgs['tuple']['umm()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(BranchEliminationParamsFactory.umm(params))
    },

    /**
     * Makes a call to the BranchElimination smart contract using the `umm2()void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    umm2: (params: CallParams<BranchEliminationArgs['obj']['umm2()void'] | BranchEliminationArgs['tuple']['umm2()void']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(BranchEliminationParamsFactory.umm2(params))
    },

    /**
     * Makes a call to the BranchElimination smart contract using the `calculate(uint64[][],uint64)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    calculate: (params: CallParams<BranchEliminationArgs['obj']['calculate(uint64[][],uint64)void'] | BranchEliminationArgs['tuple']['calculate(uint64[][],uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(BranchEliminationParamsFactory.calculate(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Makes a clear_state call to an existing instance of the BranchElimination smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the BranchElimination smart contract using the `umm()uint64` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    umm: (params: CallParams<BranchEliminationArgs['obj']['umm()uint64'] | BranchEliminationArgs['tuple']['umm()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(BranchEliminationParamsFactory.umm(params))
    },

    /**
     * Makes a call to the BranchElimination smart contract using the `umm2()void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    umm2: (params: CallParams<BranchEliminationArgs['obj']['umm2()void'] | BranchEliminationArgs['tuple']['umm2()void']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(BranchEliminationParamsFactory.umm2(params))
    },

    /**
     * Makes a call to the BranchElimination smart contract using the `calculate(uint64[][],uint64)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    calculate: (params: CallParams<BranchEliminationArgs['obj']['calculate(uint64[][],uint64)void'] | BranchEliminationArgs['tuple']['calculate(uint64[][],uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(BranchEliminationParamsFactory.calculate(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Makes a clear_state call to an existing instance of the BranchElimination smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the BranchElimination smart contract using the `umm()uint64` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    umm: async (params: CallParams<BranchEliminationArgs['obj']['umm()uint64'] | BranchEliminationArgs['tuple']['umm()uint64']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(BranchEliminationParamsFactory.umm(params))
      return {...result, return: result.return as unknown as (undefined | BranchEliminationReturns['umm()uint64'])}
    },

    /**
     * Makes a call to the BranchElimination smart contract using the `umm2()void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    umm2: async (params: CallParams<BranchEliminationArgs['obj']['umm2()void'] | BranchEliminationArgs['tuple']['umm2()void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(BranchEliminationParamsFactory.umm2(params))
      return {...result, return: result.return as unknown as (undefined | BranchEliminationReturns['umm2()void'])}
    },

    /**
     * Makes a call to the BranchElimination smart contract using the `calculate(uint64[][],uint64)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    calculate: async (params: CallParams<BranchEliminationArgs['obj']['calculate(uint64[][],uint64)void'] | BranchEliminationArgs['tuple']['calculate(uint64[][],uint64)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(BranchEliminationParamsFactory.calculate(params))
      return {...result, return: result.return as unknown as (undefined | BranchEliminationReturns['calculate(uint64[][],uint64)void'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new BranchEliminationClient(this.appClient.clone(params))
  }

  /**
   * Methods to access state for the current BranchElimination app
   */
  state = {
  }

  public newGroup(): BranchEliminationComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a umm()uint64 method call against the BranchElimination contract
       */
      umm(params: CallParams<BranchEliminationArgs['obj']['umm()uint64'] | BranchEliminationArgs['tuple']['umm()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.umm(params)))
        resultMappers.push((v) => client.decodeReturnValue('umm()uint64', v))
        return this
      },
      /**
       * Add a umm2()void method call against the BranchElimination contract
       */
      umm2(params: CallParams<BranchEliminationArgs['obj']['umm2()void'] | BranchEliminationArgs['tuple']['umm2()void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.umm2(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a calculate(uint64[][],uint64)void method call against the BranchElimination contract
       */
      calculate(params: CallParams<BranchEliminationArgs['obj']['calculate(uint64[][],uint64)void'] | BranchEliminationArgs['tuple']['calculate(uint64[][],uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.calculate(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a clear state call to the BranchElimination contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as BranchEliminationComposer
  }
}
export type BranchEliminationComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the umm()uint64 ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  umm(params?: CallParams<BranchEliminationArgs['obj']['umm()uint64'] | BranchEliminationArgs['tuple']['umm()uint64']>): BranchEliminationComposer<[...TReturns, BranchEliminationReturns['umm()uint64'] | undefined]>

  /**
   * Calls the umm2()void ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  umm2(params?: CallParams<BranchEliminationArgs['obj']['umm2()void'] | BranchEliminationArgs['tuple']['umm2()void']>): BranchEliminationComposer<[...TReturns, BranchEliminationReturns['umm2()void'] | undefined]>

  /**
   * Calls the calculate(uint64[][],uint64)void ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  calculate(params?: CallParams<BranchEliminationArgs['obj']['calculate(uint64[][],uint64)void'] | BranchEliminationArgs['tuple']['calculate(uint64[][],uint64)void']>): BranchEliminationComposer<[...TReturns, BranchEliminationReturns['calculate(uint64[][],uint64)void'] | undefined]>

  /**
   * Makes a clear_state call to an existing instance of the BranchElimination smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): BranchEliminationComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): BranchEliminationComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<BranchEliminationComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<BranchEliminationComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<BranchEliminationComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<BranchEliminationComposerResults<TReturns>>
}
export type BranchEliminationComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

