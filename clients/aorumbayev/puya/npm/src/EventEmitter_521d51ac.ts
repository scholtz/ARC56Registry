/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"EventEmitter","structs":{},"methods":[{"name":"emit_swapped","args":[{"type":"uint64","name":"a"},{"type":"uint64","name":"b"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[{"name":"Swapped","args":[{"type":"uint64","name":"a"},{"type":"uint64","name":"b"}]},{"name":"Swapped","args":[{"type":"uint64","name":"field1"},{"type":"uint64","name":"field2"}]}],"recommendations":{}},{"name":"emit_ufixed","args":[{"type":"ufixed256x16","name":"a"},{"type":"ufixed64x2","name":"b"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[{"name":"AnEvent","args":[{"type":"ufixed256x16","name":"field1"},{"type":"ufixed64x2","name":"field2"}]}],"recommendations":{}}],"arcs":[22,28],"networks":{},"state":{"schema":{"global":{"ints":0,"bytes":0},"local":{"ints":0,"bytes":0}},"keys":{"global":{},"local":{},"box":{}},"maps":{"global":{},"local":{},"box":{}}},"bareActions":{"create":["NoOp"],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[33,52],"errorMessage":"OnCompletion is not NoOp"},{"pc":[76],"errorMessage":"can only call when creating"},{"pc":[36,55],"errorMessage":"can only call when not creating"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"events":[{"name":"Swapped","args":[{"type":"uint64","name":"a"},{"type":"uint64","name":"b"}]},{"name":"Swapped","args":[{"type":"uint64","name":"field1"},{"type":"uint64","name":"field2"}]},{"name":"AnEvent","args":[{"type":"ufixed256x16","name":"field1"},{"type":"ufixed64x2","name":"field2"}]}]} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


/**
 * The argument types for the EventEmitter contract
 */
export type EventEmitterArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'emit_swapped(uint64,uint64)void': {
      a: bigint | number
      b: bigint | number
    }
    'emit_ufixed(ufixed256x16,ufixed64x2)void': {
      a: number
      b: number
    }
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'emit_swapped(uint64,uint64)void': [a: bigint | number, b: bigint | number]
    'emit_ufixed(ufixed256x16,ufixed64x2)void': [a: number, b: number]
  }
}

/**
 * The return type for each method
 */
export type EventEmitterReturns = {
  'emit_swapped(uint64,uint64)void': void
  'emit_ufixed(ufixed256x16,ufixed64x2)void': void
}

/**
 * Defines the types of available calls and state of the EventEmitter smart contract.
 */
export type EventEmitterTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'emit_swapped(uint64,uint64)void' | 'emit_swapped', {
      argsObj: EventEmitterArgs['obj']['emit_swapped(uint64,uint64)void']
      argsTuple: EventEmitterArgs['tuple']['emit_swapped(uint64,uint64)void']
      returns: EventEmitterReturns['emit_swapped(uint64,uint64)void']
    }>
    & Record<'emit_ufixed(ufixed256x16,ufixed64x2)void' | 'emit_ufixed', {
      argsObj: EventEmitterArgs['obj']['emit_ufixed(ufixed256x16,ufixed64x2)void']
      argsTuple: EventEmitterArgs['tuple']['emit_ufixed(ufixed256x16,ufixed64x2)void']
      returns: EventEmitterReturns['emit_ufixed(ufixed256x16,ufixed64x2)void']
    }>
}

/**
 * Defines the possible abi call signatures.
 */
export type EventEmitterSignatures = keyof EventEmitterTypes['methods']
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the EventEmitter smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends EventEmitterSignatures> = EventEmitterTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the EventEmitter smart contract to the method's return type
 */
export type MethodReturn<TSignature extends EventEmitterSignatures> = EventEmitterTypes['methods'][TSignature]['returns']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the EventEmitter smart contract
 */
export abstract class EventEmitterParamsFactory {
  /**
   * Constructs a no op call for the emit_swapped(uint64,uint64)void ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static emitSwapped(params: CallParams<EventEmitterArgs['obj']['emit_swapped(uint64,uint64)void'] | EventEmitterArgs['tuple']['emit_swapped(uint64,uint64)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'emit_swapped(uint64,uint64)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.a, params.args.b],
    }
  }
  /**
   * Constructs a no op call for the emit_ufixed(ufixed256x16,ufixed64x2)void ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static emitUfixed(params: CallParams<EventEmitterArgs['obj']['emit_ufixed(ufixed256x16,ufixed64x2)void'] | EventEmitterArgs['tuple']['emit_ufixed(ufixed256x16,ufixed64x2)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'emit_ufixed(ufixed256x16,ufixed64x2)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.a, params.args.b],
    }
  }
}

/**
 * A client to make calls to the EventEmitter smart contract
 */
export class EventEmitterClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `EventEmitterClient`
   *
   * @param appClient An `AppClient` instance which has been created with the EventEmitter app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `EventEmitterClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Returns a new `EventEmitterClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<EventEmitterClient> {
    return new EventEmitterClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `EventEmitterClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<EventEmitterClient> {
    return new EventEmitterClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Makes a clear_state call to an existing instance of the EventEmitter smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the EventEmitter smart contract using the `emit_swapped(uint64,uint64)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    emitSwapped: (params: CallParams<EventEmitterArgs['obj']['emit_swapped(uint64,uint64)void'] | EventEmitterArgs['tuple']['emit_swapped(uint64,uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(EventEmitterParamsFactory.emitSwapped(params))
    },

    /**
     * Makes a call to the EventEmitter smart contract using the `emit_ufixed(ufixed256x16,ufixed64x2)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    emitUfixed: (params: CallParams<EventEmitterArgs['obj']['emit_ufixed(ufixed256x16,ufixed64x2)void'] | EventEmitterArgs['tuple']['emit_ufixed(ufixed256x16,ufixed64x2)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(EventEmitterParamsFactory.emitUfixed(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Makes a clear_state call to an existing instance of the EventEmitter smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the EventEmitter smart contract using the `emit_swapped(uint64,uint64)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    emitSwapped: (params: CallParams<EventEmitterArgs['obj']['emit_swapped(uint64,uint64)void'] | EventEmitterArgs['tuple']['emit_swapped(uint64,uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(EventEmitterParamsFactory.emitSwapped(params))
    },

    /**
     * Makes a call to the EventEmitter smart contract using the `emit_ufixed(ufixed256x16,ufixed64x2)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    emitUfixed: (params: CallParams<EventEmitterArgs['obj']['emit_ufixed(ufixed256x16,ufixed64x2)void'] | EventEmitterArgs['tuple']['emit_ufixed(ufixed256x16,ufixed64x2)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(EventEmitterParamsFactory.emitUfixed(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Makes a clear_state call to an existing instance of the EventEmitter smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the EventEmitter smart contract using the `emit_swapped(uint64,uint64)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    emitSwapped: async (params: CallParams<EventEmitterArgs['obj']['emit_swapped(uint64,uint64)void'] | EventEmitterArgs['tuple']['emit_swapped(uint64,uint64)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(EventEmitterParamsFactory.emitSwapped(params))
      return {...result, return: result.return as unknown as (undefined | EventEmitterReturns['emit_swapped(uint64,uint64)void'])}
    },

    /**
     * Makes a call to the EventEmitter smart contract using the `emit_ufixed(ufixed256x16,ufixed64x2)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    emitUfixed: async (params: CallParams<EventEmitterArgs['obj']['emit_ufixed(ufixed256x16,ufixed64x2)void'] | EventEmitterArgs['tuple']['emit_ufixed(ufixed256x16,ufixed64x2)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(EventEmitterParamsFactory.emitUfixed(params))
      return {...result, return: result.return as unknown as (undefined | EventEmitterReturns['emit_ufixed(ufixed256x16,ufixed64x2)void'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new EventEmitterClient(this.appClient.clone(params))
  }

  /**
   * Methods to access state for the current EventEmitter app
   */
  state = {
  }

  public newGroup(): EventEmitterComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a emit_swapped(uint64,uint64)void method call against the EventEmitter contract
       */
      emitSwapped(params: CallParams<EventEmitterArgs['obj']['emit_swapped(uint64,uint64)void'] | EventEmitterArgs['tuple']['emit_swapped(uint64,uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.emitSwapped(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a emit_ufixed(ufixed256x16,ufixed64x2)void method call against the EventEmitter contract
       */
      emitUfixed(params: CallParams<EventEmitterArgs['obj']['emit_ufixed(ufixed256x16,ufixed64x2)void'] | EventEmitterArgs['tuple']['emit_ufixed(ufixed256x16,ufixed64x2)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.emitUfixed(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a clear state call to the EventEmitter contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as EventEmitterComposer
  }
}
export type EventEmitterComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the emit_swapped(uint64,uint64)void ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  emitSwapped(params?: CallParams<EventEmitterArgs['obj']['emit_swapped(uint64,uint64)void'] | EventEmitterArgs['tuple']['emit_swapped(uint64,uint64)void']>): EventEmitterComposer<[...TReturns, EventEmitterReturns['emit_swapped(uint64,uint64)void'] | undefined]>

  /**
   * Calls the emit_ufixed(ufixed256x16,ufixed64x2)void ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  emitUfixed(params?: CallParams<EventEmitterArgs['obj']['emit_ufixed(ufixed256x16,ufixed64x2)void'] | EventEmitterArgs['tuple']['emit_ufixed(ufixed256x16,ufixed64x2)void']>): EventEmitterComposer<[...TReturns, EventEmitterReturns['emit_ufixed(ufixed256x16,ufixed64x2)void'] | undefined]>

  /**
   * Makes a clear_state call to an existing instance of the EventEmitter smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): EventEmitterComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): EventEmitterComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<EventEmitterComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<EventEmitterComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<EventEmitterComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<EventEmitterComposerResults<TReturns>>
}
export type EventEmitterComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

