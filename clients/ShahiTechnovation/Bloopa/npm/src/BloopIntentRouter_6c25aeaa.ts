/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"BloopIntentRouter","structs":{"Intent":[{"name":"locker","type":"address"},{"name":"paymentAmount","type":"uint64"},{"name":"apiCost","type":"uint64"},{"name":"expiryRound","type":"uint64"},{"name":"taskHash","type":"byte[32]"},{"name":"solverAddress","type":"address"},{"name":"assignedAgent","type":"address"},{"name":"resultHash","type":"byte[32]"},{"name":"state","type":"uint64"}]},"methods":[{"name":"bootstrap","args":[{"type":"uint64","name":"bloopa_app_id"}],"returns":{"type":"void"},"actions":{"create":["NoOp"],"call":[]},"readonly":false,"desc":"Initialise the router. Called once on contract creation by the admin.\nPreconditions:     - Txn.sender == Global.creator_address (enforced by create=\"require\")\nMutates:     - Sets all 5 global state fields.     - is_live = 1 (router goes live immediately)","events":[],"recommendations":{}},{"name":"lock_intent","args":[{"type":"byte[32]","name":"task_hash","desc":"sha256(task params) — 32 bytes"},{"type":"uint64","name":"expiry_rounds_from_now","desc":"Rounds until intent expires (10–86400)"},{"type":"uint64","name":"api_cost_estimate","desc":"Amount solver will borrow from Bloopa"},{"type":"address","name":"solver_address","desc":"The ONLY solver that may fulfill this intent"}],"returns":{"type":"uint64","desc":"intent_id (arc4.UInt64)"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Lock ALGO and create a private swap intent.\nuser1 sends a payment to the Router (as the previous gtxn) and specifies which solver can fulfill. Only the named solver_address can call borrow_to_execute().\nThe payment transaction must be the immediately preceding transaction in the group (gtxn[-1] / gtxn[Txn.group_index - 1]).","events":[],"recommendations":{}},{"name":"borrow_to_execute","args":[{"type":"uint64","name":"intent_id","desc":"Intent to claim"},{"type":"string","name":"task_description","desc":"Plain-English task description (for logging)"},{"type":"uint64","name":"expected_return_microalgo","desc":"Solver's expected return (for logging)"}],"returns":{"type":"bool","desc":"True if assigned successfully"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Claim a private intent as the designated solver.\nThe solver must have ALREADY drawn credit from Bloopa externally (via BloopaCreditAgent.draw()) before calling this method. This method only enforces the private order and marks the intent assigned.\nPRIVATE ORDER ENFORCEMENT: Only intent.solver_address can call this. Any other caller gets assertion failure.","events":[],"recommendations":{}},{"name":"settle","args":[{"type":"uint64","name":"intent_id","desc":"Intent to settle"},{"type":"byte[32]","name":"result_hash","desc":"sha256 of result (32 bytes)"},{"type":"string","name":"result_pointer","desc":"Short result summary string (≤200 chars)"}],"returns":{"type":"bool","desc":"True on success"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Settle a completed intent atomically.\nSends repayment to Bloopa from Router escrow + profit to solver. Calls Bloopa.record_payment() to build solver's tier history.\nInterest formula (Tier 0, matches contract.py exactly):     interest = (api_cost * 2400 * 86400) // (10000 * 31536000)     interest += 1  # minimum 1 μA","events":[],"recommendations":{}},{"name":"reclaim_expired","args":[{"type":"uint64","name":"intent_id","desc":"Intent to expire and reclaim"}],"returns":{"type":"bool","desc":"True on success"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Reclaim locked funds for an expired intent.\nOnly the original locker (user1) can reclaim. If the intent was assigned but never settled (solver took job, disappeared):     → Slash the assigned agent via Bloopa inner txn Return locked funds to locker regardless.","events":[],"recommendations":{}},{"name":"get_intent","args":[{"type":"uint64","name":"intent_id","desc":"ID of the intent to read"}],"returns":{"type":"(address,uint64,uint64,uint64,byte[32],address,address,byte[32],uint64)","struct":"Intent","desc":"Full Intent struct copy"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"desc":"Read a full intent struct. Does not modify state.","events":[],"recommendations":{}}],"arcs":[22,28],"desc":"\n    Intent-based swap fulfillment router with Bloopa credit integration.\n\n    Global state:\n        bloopa_app_id   — Bloopa core contract App ID\n        total_intents   — auto-increment intent counter\n        router_treasury — fee accumulation (reserved for future use)\n        admin           — admin address (creator)\n        is_live         — 0=paused, 1=live\n\n    Box storage:\n        intents[arc4.UInt64] = Intent  (key_prefix=b\"I\")\n    ","networks":{},"state":{"schema":{"global":{"ints":4,"bytes":1},"local":{"ints":0,"bytes":0}},"keys":{"global":{"bloopa_app_id":{"keyType":"AVMString","valueType":"AVMUint64","key":"Ymxvb3BhX2FwcF9pZA=="},"total_intents":{"keyType":"AVMString","valueType":"AVMUint64","key":"dG90YWxfaW50ZW50cw=="},"router_treasury":{"keyType":"AVMString","valueType":"AVMUint64","key":"cm91dGVyX3RyZWFzdXJ5"},"admin":{"keyType":"AVMString","valueType":"address","key":"YWRtaW4="},"is_live":{"keyType":"AVMString","valueType":"AVMUint64","key":"aXNfbGl2ZQ=="}},"local":{},"box":{}},"maps":{"global":{},"local":{},"box":{"intents":{"keyType":"uint64","valueType":"Intent","prefix":"SQ=="}}}},"bareActions":{"create":[],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[200],"errorMessage":"admin_only"},{"pc":[902],"errorMessage":"already_settled"},{"pc":[709],"errorMessage":"bloopa_app_not_found"},{"pc":[705,915],"errorMessage":"check self.bloopa_app_id exists"},{"pc":[312],"errorMessage":"check self.is_live exists"},{"pc":[319],"errorMessage":"check self.total_intents exists"},{"pc":[308],"errorMessage":"expiry_too_far"},{"pc":[301],"errorMessage":"expiry_too_soon"},{"pc":[701],"errorMessage":"insufficient_profit_after_repayment"},{"pc":[476],"errorMessage":"intent_expired"},{"pc":[655],"errorMessage":"intent_expired_during_execution"},{"pc":[627],"errorMessage":"intent_not_assigned"},{"pc":[449,616,863,1052],"errorMessage":"intent_not_found"},{"pc":[460],"errorMessage":"intent_not_open"},{"pc":[426,599],"errorMessage":"invalid array length header"},{"pc":[433,607],"errorMessage":"invalid number of bytes for arc4.dynamic_array<arc4.uint8>"},{"pc":[234,258,593],"errorMessage":"invalid number of bytes for arc4.static_array<arc4.uint8, 32>"},{"pc":[194,242,250,420,440,585,854,1044],"errorMessage":"invalid number of bytes for arc4.uint64"},{"pc":[639],"errorMessage":"not_assigned_solver"},{"pc":[488],"errorMessage":"not_authorized_solver"},{"pc":[894],"errorMessage":"not_expired_yet"},{"pc":[878],"errorMessage":"not_locker"},{"pc":[278],"errorMessage":"pay_to_router"},{"pc":[285],"errorMessage":"payment_must_exceed_api_cost"},{"pc":[293],"errorMessage":"payment_too_large_max_10_algo"},{"pc":[315],"errorMessage":"router_paused"},{"pc":[268],"errorMessage":"transaction type is pay"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"events":[]} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


// Type definitions for ARC-56 structs

export type Intent = {
  locker: string,
  paymentAmount: bigint,
  apiCost: bigint,
  expiryRound: bigint,
  taskHash: Uint8Array,
  solverAddress: string,
  assignedAgent: string,
  resultHash: Uint8Array,
  state: bigint
}


/**
 * Converts the ABI tuple representation of a Intent to the struct representation
 */
export function IntentFromTuple(abiTuple: [string, bigint, bigint, bigint, Uint8Array, string, string, Uint8Array, bigint]) {
  return getABIStructFromABITuple(abiTuple, APP_SPEC.structs.Intent, APP_SPEC.structs) as Intent
}

/**
 * The argument types for the BloopIntentRouter contract
 */
export type BloopIntentRouterArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'bootstrap(uint64)void': {
      bloopaAppId: bigint | number
    }
    'lock_intent(byte[32],uint64,uint64,address)uint64': {
      /**
       * sha256(task params) — 32 bytes
       */
      taskHash: Uint8Array
      /**
       * Rounds until intent expires (10–86400)
       */
      expiryRoundsFromNow: bigint | number
      /**
       * Amount solver will borrow from Bloopa
       */
      apiCostEstimate: bigint | number
      /**
       * The ONLY solver that may fulfill this intent
       */
      solverAddress: string
    }
    'borrow_to_execute(uint64,string,uint64)bool': {
      /**
       * Intent to claim
       */
      intentId: bigint | number
      /**
       * Plain-English task description (for logging)
       */
      taskDescription: string
      /**
       * Solver's expected return (for logging)
       */
      expectedReturnMicroalgo: bigint | number
    }
    'settle(uint64,byte[32],string)bool': {
      /**
       * Intent to settle
       */
      intentId: bigint | number
      /**
       * sha256 of result (32 bytes)
       */
      resultHash: Uint8Array
      /**
       * Short result summary string (≤200 chars)
       */
      resultPointer: string
    }
    'reclaim_expired(uint64)bool': {
      /**
       * Intent to expire and reclaim
       */
      intentId: bigint | number
    }
    'get_intent(uint64)(address,uint64,uint64,uint64,byte[32],address,address,byte[32],uint64)': {
      /**
       * ID of the intent to read
       */
      intentId: bigint | number
    }
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'bootstrap(uint64)void': [bloopaAppId: bigint | number]
    'lock_intent(byte[32],uint64,uint64,address)uint64': [taskHash: Uint8Array, expiryRoundsFromNow: bigint | number, apiCostEstimate: bigint | number, solverAddress: string]
    'borrow_to_execute(uint64,string,uint64)bool': [intentId: bigint | number, taskDescription: string, expectedReturnMicroalgo: bigint | number]
    'settle(uint64,byte[32],string)bool': [intentId: bigint | number, resultHash: Uint8Array, resultPointer: string]
    'reclaim_expired(uint64)bool': [intentId: bigint | number]
    'get_intent(uint64)(address,uint64,uint64,uint64,byte[32],address,address,byte[32],uint64)': [intentId: bigint | number]
  }
}

/**
 * The return type for each method
 */
export type BloopIntentRouterReturns = {
  'bootstrap(uint64)void': void
  'lock_intent(byte[32],uint64,uint64,address)uint64': bigint
  'borrow_to_execute(uint64,string,uint64)bool': boolean
  'settle(uint64,byte[32],string)bool': boolean
  'reclaim_expired(uint64)bool': boolean
  'get_intent(uint64)(address,uint64,uint64,uint64,byte[32],address,address,byte[32],uint64)': Intent
}

/**
 * Defines the types of available calls and state of the BloopIntentRouter smart contract.
 */
export type BloopIntentRouterTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'bootstrap(uint64)void' | 'bootstrap', {
      argsObj: BloopIntentRouterArgs['obj']['bootstrap(uint64)void']
      argsTuple: BloopIntentRouterArgs['tuple']['bootstrap(uint64)void']
      returns: BloopIntentRouterReturns['bootstrap(uint64)void']
    }>
    & Record<'lock_intent(byte[32],uint64,uint64,address)uint64' | 'lock_intent', {
      argsObj: BloopIntentRouterArgs['obj']['lock_intent(byte[32],uint64,uint64,address)uint64']
      argsTuple: BloopIntentRouterArgs['tuple']['lock_intent(byte[32],uint64,uint64,address)uint64']
      /**
       * intent_id (arc4.UInt64)
       */
      returns: BloopIntentRouterReturns['lock_intent(byte[32],uint64,uint64,address)uint64']
    }>
    & Record<'borrow_to_execute(uint64,string,uint64)bool' | 'borrow_to_execute', {
      argsObj: BloopIntentRouterArgs['obj']['borrow_to_execute(uint64,string,uint64)bool']
      argsTuple: BloopIntentRouterArgs['tuple']['borrow_to_execute(uint64,string,uint64)bool']
      /**
       * True if assigned successfully
       */
      returns: BloopIntentRouterReturns['borrow_to_execute(uint64,string,uint64)bool']
    }>
    & Record<'settle(uint64,byte[32],string)bool' | 'settle', {
      argsObj: BloopIntentRouterArgs['obj']['settle(uint64,byte[32],string)bool']
      argsTuple: BloopIntentRouterArgs['tuple']['settle(uint64,byte[32],string)bool']
      /**
       * True on success
       */
      returns: BloopIntentRouterReturns['settle(uint64,byte[32],string)bool']
    }>
    & Record<'reclaim_expired(uint64)bool' | 'reclaim_expired', {
      argsObj: BloopIntentRouterArgs['obj']['reclaim_expired(uint64)bool']
      argsTuple: BloopIntentRouterArgs['tuple']['reclaim_expired(uint64)bool']
      /**
       * True on success
       */
      returns: BloopIntentRouterReturns['reclaim_expired(uint64)bool']
    }>
    & Record<'get_intent(uint64)(address,uint64,uint64,uint64,byte[32],address,address,byte[32],uint64)' | 'get_intent', {
      argsObj: BloopIntentRouterArgs['obj']['get_intent(uint64)(address,uint64,uint64,uint64,byte[32],address,address,byte[32],uint64)']
      argsTuple: BloopIntentRouterArgs['tuple']['get_intent(uint64)(address,uint64,uint64,uint64,byte[32],address,address,byte[32],uint64)']
      /**
       * Full Intent struct copy
       */
      returns: BloopIntentRouterReturns['get_intent(uint64)(address,uint64,uint64,uint64,byte[32],address,address,byte[32],uint64)']
    }>
  /**
   * Defines the shape of the state of the application.
   */
  state: {
    global: {
      keys: {
        bloopaAppId: bigint
        totalIntents: bigint
        routerTreasury: bigint
        admin: string
        isLive: bigint
      }
      maps: {}
    }
    box: {
      keys: {}
      maps: {
        intents: Map<bigint | number, Intent>
      }
    }
  }
}

/**
 * Defines the possible abi call signatures.
 */
export type BloopIntentRouterSignatures = keyof BloopIntentRouterTypes['methods']
/**
 * Defines the possible abi call signatures for methods that return a non-void value.
 */
export type BloopIntentRouterNonVoidMethodSignatures = keyof BloopIntentRouterTypes['methods'] extends infer T ? T extends keyof BloopIntentRouterTypes['methods'] ? MethodReturn<T> extends void ? never : T  : never : never
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the BloopIntentRouter smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends BloopIntentRouterSignatures> = BloopIntentRouterTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the BloopIntentRouter smart contract to the method's return type
 */
export type MethodReturn<TSignature extends BloopIntentRouterSignatures> = BloopIntentRouterTypes['methods'][TSignature]['returns']

/**
 * Defines the shape of the keyed global state of the application.
 */
export type GlobalKeysState = BloopIntentRouterTypes['state']['global']['keys']

/**
 * Defines the shape of the keyed box state of the application.
 */
export type BoxKeysState = BloopIntentRouterTypes['state']['box']['keys']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the BloopIntentRouter smart contract
 */
export abstract class BloopIntentRouterParamsFactory {
  /**
   * Constructs a no op call for the lock_intent(byte[32],uint64,uint64,address)uint64 ABI method
   *
  * Lock ALGO and create a private swap intent.
  user1 sends a payment to the Router (as the previous gtxn) and specifies which solver can fulfill. Only the named solver_address can call borrow_to_execute().
  The payment transaction must be the immediately preceding transaction in the group (gtxn[-1] / gtxn[Txn.group_index - 1]).

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static lockIntent(params: CallParams<BloopIntentRouterArgs['obj']['lock_intent(byte[32],uint64,uint64,address)uint64'] | BloopIntentRouterArgs['tuple']['lock_intent(byte[32],uint64,uint64,address)uint64']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'lock_intent(byte[32],uint64,uint64,address)uint64' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.taskHash, params.args.expiryRoundsFromNow, params.args.apiCostEstimate, params.args.solverAddress],
    }
  }
  /**
   * Constructs a no op call for the borrow_to_execute(uint64,string,uint64)bool ABI method
   *
  * Claim a private intent as the designated solver.
  The solver must have ALREADY drawn credit from Bloopa externally (via BloopaCreditAgent.draw()) before calling this method. This method only enforces the private order and marks the intent assigned.
  PRIVATE ORDER ENFORCEMENT: Only intent.solver_address can call this. Any other caller gets assertion failure.

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static borrowToExecute(params: CallParams<BloopIntentRouterArgs['obj']['borrow_to_execute(uint64,string,uint64)bool'] | BloopIntentRouterArgs['tuple']['borrow_to_execute(uint64,string,uint64)bool']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'borrow_to_execute(uint64,string,uint64)bool' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.intentId, params.args.taskDescription, params.args.expectedReturnMicroalgo],
    }
  }
  /**
   * Constructs a no op call for the settle(uint64,byte[32],string)bool ABI method
   *
  * Settle a completed intent atomically.
  Sends repayment to Bloopa from Router escrow + profit to solver. Calls Bloopa.record_payment() to build solver's tier history.
  Interest formula (Tier 0, matches contract.py exactly):     interest = (api_cost * 2400 * 86400) // (10000 * 31536000)     interest += 1  # minimum 1 μA

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static settle(params: CallParams<BloopIntentRouterArgs['obj']['settle(uint64,byte[32],string)bool'] | BloopIntentRouterArgs['tuple']['settle(uint64,byte[32],string)bool']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'settle(uint64,byte[32],string)bool' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.intentId, params.args.resultHash, params.args.resultPointer],
    }
  }
  /**
   * Constructs a no op call for the reclaim_expired(uint64)bool ABI method
   *
  * Reclaim locked funds for an expired intent.
  Only the original locker (user1) can reclaim. If the intent was assigned but never settled (solver took job, disappeared):     → Slash the assigned agent via Bloopa inner txn Return locked funds to locker regardless.

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static reclaimExpired(params: CallParams<BloopIntentRouterArgs['obj']['reclaim_expired(uint64)bool'] | BloopIntentRouterArgs['tuple']['reclaim_expired(uint64)bool']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'reclaim_expired(uint64)bool' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.intentId],
    }
  }
  /**
   * Constructs a no op call for the get_intent(uint64)(address,uint64,uint64,uint64,byte[32],address,address,byte[32],uint64) ABI method
   *
   * Read a full intent struct. Does not modify state.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static getIntent(params: CallParams<BloopIntentRouterArgs['obj']['get_intent(uint64)(address,uint64,uint64,uint64,byte[32],address,address,byte[32],uint64)'] | BloopIntentRouterArgs['tuple']['get_intent(uint64)(address,uint64,uint64,uint64,byte[32],address,address,byte[32],uint64)']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'get_intent(uint64)(address,uint64,uint64,uint64,byte[32],address,address,byte[32],uint64)' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.intentId],
    }
  }
}

/**
 * A client to make calls to the BloopIntentRouter smart contract
 */
export class BloopIntentRouterClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `BloopIntentRouterClient`
   *
   * @param appClient An `AppClient` instance which has been created with the BloopIntentRouter app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `BloopIntentRouterClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Checks for decode errors on the given return value and maps the return value to the return type for the given method
   * @returns The typed return value or undefined if there was no value
   */
  decodeReturnValue<TSignature extends BloopIntentRouterNonVoidMethodSignatures>(method: TSignature, returnValue: ABIReturn | undefined) {
    return returnValue !== undefined ? getArc56ReturnValue<MethodReturn<TSignature>>(returnValue, this.appClient.getABIMethod(method), APP_SPEC.structs) : undefined
  }

  /**
   * Returns a new `BloopIntentRouterClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<BloopIntentRouterClient> {
    return new BloopIntentRouterClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `BloopIntentRouterClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<BloopIntentRouterClient> {
    return new BloopIntentRouterClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Makes a clear_state call to an existing instance of the BloopIntentRouter smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the BloopIntentRouter smart contract using the `lock_intent(byte[32],uint64,uint64,address)uint64` ABI method.
     *
    * Lock ALGO and create a private swap intent.
    user1 sends a payment to the Router (as the previous gtxn) and specifies which solver can fulfill. Only the named solver_address can call borrow_to_execute().
    The payment transaction must be the immediately preceding transaction in the group (gtxn[-1] / gtxn[Txn.group_index - 1]).

     *
     * @param params The params for the smart contract call
     * @returns The call params: intent_id (arc4.UInt64)
     */
    lockIntent: (params: CallParams<BloopIntentRouterArgs['obj']['lock_intent(byte[32],uint64,uint64,address)uint64'] | BloopIntentRouterArgs['tuple']['lock_intent(byte[32],uint64,uint64,address)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(BloopIntentRouterParamsFactory.lockIntent(params))
    },

    /**
     * Makes a call to the BloopIntentRouter smart contract using the `borrow_to_execute(uint64,string,uint64)bool` ABI method.
     *
    * Claim a private intent as the designated solver.
    The solver must have ALREADY drawn credit from Bloopa externally (via BloopaCreditAgent.draw()) before calling this method. This method only enforces the private order and marks the intent assigned.
    PRIVATE ORDER ENFORCEMENT: Only intent.solver_address can call this. Any other caller gets assertion failure.

     *
     * @param params The params for the smart contract call
     * @returns The call params: True if assigned successfully
     */
    borrowToExecute: (params: CallParams<BloopIntentRouterArgs['obj']['borrow_to_execute(uint64,string,uint64)bool'] | BloopIntentRouterArgs['tuple']['borrow_to_execute(uint64,string,uint64)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(BloopIntentRouterParamsFactory.borrowToExecute(params))
    },

    /**
     * Makes a call to the BloopIntentRouter smart contract using the `settle(uint64,byte[32],string)bool` ABI method.
     *
    * Settle a completed intent atomically.
    Sends repayment to Bloopa from Router escrow + profit to solver. Calls Bloopa.record_payment() to build solver's tier history.
    Interest formula (Tier 0, matches contract.py exactly):     interest = (api_cost * 2400 * 86400) // (10000 * 31536000)     interest += 1  # minimum 1 μA

     *
     * @param params The params for the smart contract call
     * @returns The call params: True on success
     */
    settle: (params: CallParams<BloopIntentRouterArgs['obj']['settle(uint64,byte[32],string)bool'] | BloopIntentRouterArgs['tuple']['settle(uint64,byte[32],string)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(BloopIntentRouterParamsFactory.settle(params))
    },

    /**
     * Makes a call to the BloopIntentRouter smart contract using the `reclaim_expired(uint64)bool` ABI method.
     *
    * Reclaim locked funds for an expired intent.
    Only the original locker (user1) can reclaim. If the intent was assigned but never settled (solver took job, disappeared):     → Slash the assigned agent via Bloopa inner txn Return locked funds to locker regardless.

     *
     * @param params The params for the smart contract call
     * @returns The call params: True on success
     */
    reclaimExpired: (params: CallParams<BloopIntentRouterArgs['obj']['reclaim_expired(uint64)bool'] | BloopIntentRouterArgs['tuple']['reclaim_expired(uint64)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(BloopIntentRouterParamsFactory.reclaimExpired(params))
    },

    /**
     * Makes a call to the BloopIntentRouter smart contract using the `get_intent(uint64)(address,uint64,uint64,uint64,byte[32],address,address,byte[32],uint64)` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * Read a full intent struct. Does not modify state.
     *
     * @param params The params for the smart contract call
     * @returns The call params: Full Intent struct copy
     */
    getIntent: (params: CallParams<BloopIntentRouterArgs['obj']['get_intent(uint64)(address,uint64,uint64,uint64,byte[32],address,address,byte[32],uint64)'] | BloopIntentRouterArgs['tuple']['get_intent(uint64)(address,uint64,uint64,uint64,byte[32],address,address,byte[32],uint64)']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(BloopIntentRouterParamsFactory.getIntent(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Makes a clear_state call to an existing instance of the BloopIntentRouter smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the BloopIntentRouter smart contract using the `lock_intent(byte[32],uint64,uint64,address)uint64` ABI method.
     *
    * Lock ALGO and create a private swap intent.
    user1 sends a payment to the Router (as the previous gtxn) and specifies which solver can fulfill. Only the named solver_address can call borrow_to_execute().
    The payment transaction must be the immediately preceding transaction in the group (gtxn[-1] / gtxn[Txn.group_index - 1]).

     *
     * @param params The params for the smart contract call
     * @returns The call transaction: intent_id (arc4.UInt64)
     */
    lockIntent: (params: CallParams<BloopIntentRouterArgs['obj']['lock_intent(byte[32],uint64,uint64,address)uint64'] | BloopIntentRouterArgs['tuple']['lock_intent(byte[32],uint64,uint64,address)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(BloopIntentRouterParamsFactory.lockIntent(params))
    },

    /**
     * Makes a call to the BloopIntentRouter smart contract using the `borrow_to_execute(uint64,string,uint64)bool` ABI method.
     *
    * Claim a private intent as the designated solver.
    The solver must have ALREADY drawn credit from Bloopa externally (via BloopaCreditAgent.draw()) before calling this method. This method only enforces the private order and marks the intent assigned.
    PRIVATE ORDER ENFORCEMENT: Only intent.solver_address can call this. Any other caller gets assertion failure.

     *
     * @param params The params for the smart contract call
     * @returns The call transaction: True if assigned successfully
     */
    borrowToExecute: (params: CallParams<BloopIntentRouterArgs['obj']['borrow_to_execute(uint64,string,uint64)bool'] | BloopIntentRouterArgs['tuple']['borrow_to_execute(uint64,string,uint64)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(BloopIntentRouterParamsFactory.borrowToExecute(params))
    },

    /**
     * Makes a call to the BloopIntentRouter smart contract using the `settle(uint64,byte[32],string)bool` ABI method.
     *
    * Settle a completed intent atomically.
    Sends repayment to Bloopa from Router escrow + profit to solver. Calls Bloopa.record_payment() to build solver's tier history.
    Interest formula (Tier 0, matches contract.py exactly):     interest = (api_cost * 2400 * 86400) // (10000 * 31536000)     interest += 1  # minimum 1 μA

     *
     * @param params The params for the smart contract call
     * @returns The call transaction: True on success
     */
    settle: (params: CallParams<BloopIntentRouterArgs['obj']['settle(uint64,byte[32],string)bool'] | BloopIntentRouterArgs['tuple']['settle(uint64,byte[32],string)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(BloopIntentRouterParamsFactory.settle(params))
    },

    /**
     * Makes a call to the BloopIntentRouter smart contract using the `reclaim_expired(uint64)bool` ABI method.
     *
    * Reclaim locked funds for an expired intent.
    Only the original locker (user1) can reclaim. If the intent was assigned but never settled (solver took job, disappeared):     → Slash the assigned agent via Bloopa inner txn Return locked funds to locker regardless.

     *
     * @param params The params for the smart contract call
     * @returns The call transaction: True on success
     */
    reclaimExpired: (params: CallParams<BloopIntentRouterArgs['obj']['reclaim_expired(uint64)bool'] | BloopIntentRouterArgs['tuple']['reclaim_expired(uint64)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(BloopIntentRouterParamsFactory.reclaimExpired(params))
    },

    /**
     * Makes a call to the BloopIntentRouter smart contract using the `get_intent(uint64)(address,uint64,uint64,uint64,byte[32],address,address,byte[32],uint64)` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * Read a full intent struct. Does not modify state.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction: Full Intent struct copy
     */
    getIntent: (params: CallParams<BloopIntentRouterArgs['obj']['get_intent(uint64)(address,uint64,uint64,uint64,byte[32],address,address,byte[32],uint64)'] | BloopIntentRouterArgs['tuple']['get_intent(uint64)(address,uint64,uint64,uint64,byte[32],address,address,byte[32],uint64)']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(BloopIntentRouterParamsFactory.getIntent(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Makes a clear_state call to an existing instance of the BloopIntentRouter smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the BloopIntentRouter smart contract using the `lock_intent(byte[32],uint64,uint64,address)uint64` ABI method.
     *
    * Lock ALGO and create a private swap intent.
    user1 sends a payment to the Router (as the previous gtxn) and specifies which solver can fulfill. Only the named solver_address can call borrow_to_execute().
    The payment transaction must be the immediately preceding transaction in the group (gtxn[-1] / gtxn[Txn.group_index - 1]).

     *
     * @param params The params for the smart contract call
     * @returns The call result: intent_id (arc4.UInt64)
     */
    lockIntent: async (params: CallParams<BloopIntentRouterArgs['obj']['lock_intent(byte[32],uint64,uint64,address)uint64'] | BloopIntentRouterArgs['tuple']['lock_intent(byte[32],uint64,uint64,address)uint64']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(BloopIntentRouterParamsFactory.lockIntent(params))
      return {...result, return: result.return as unknown as (undefined | BloopIntentRouterReturns['lock_intent(byte[32],uint64,uint64,address)uint64'])}
    },

    /**
     * Makes a call to the BloopIntentRouter smart contract using the `borrow_to_execute(uint64,string,uint64)bool` ABI method.
     *
    * Claim a private intent as the designated solver.
    The solver must have ALREADY drawn credit from Bloopa externally (via BloopaCreditAgent.draw()) before calling this method. This method only enforces the private order and marks the intent assigned.
    PRIVATE ORDER ENFORCEMENT: Only intent.solver_address can call this. Any other caller gets assertion failure.

     *
     * @param params The params for the smart contract call
     * @returns The call result: True if assigned successfully
     */
    borrowToExecute: async (params: CallParams<BloopIntentRouterArgs['obj']['borrow_to_execute(uint64,string,uint64)bool'] | BloopIntentRouterArgs['tuple']['borrow_to_execute(uint64,string,uint64)bool']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(BloopIntentRouterParamsFactory.borrowToExecute(params))
      return {...result, return: result.return as unknown as (undefined | BloopIntentRouterReturns['borrow_to_execute(uint64,string,uint64)bool'])}
    },

    /**
     * Makes a call to the BloopIntentRouter smart contract using the `settle(uint64,byte[32],string)bool` ABI method.
     *
    * Settle a completed intent atomically.
    Sends repayment to Bloopa from Router escrow + profit to solver. Calls Bloopa.record_payment() to build solver's tier history.
    Interest formula (Tier 0, matches contract.py exactly):     interest = (api_cost * 2400 * 86400) // (10000 * 31536000)     interest += 1  # minimum 1 μA

     *
     * @param params The params for the smart contract call
     * @returns The call result: True on success
     */
    settle: async (params: CallParams<BloopIntentRouterArgs['obj']['settle(uint64,byte[32],string)bool'] | BloopIntentRouterArgs['tuple']['settle(uint64,byte[32],string)bool']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(BloopIntentRouterParamsFactory.settle(params))
      return {...result, return: result.return as unknown as (undefined | BloopIntentRouterReturns['settle(uint64,byte[32],string)bool'])}
    },

    /**
     * Makes a call to the BloopIntentRouter smart contract using the `reclaim_expired(uint64)bool` ABI method.
     *
    * Reclaim locked funds for an expired intent.
    Only the original locker (user1) can reclaim. If the intent was assigned but never settled (solver took job, disappeared):     → Slash the assigned agent via Bloopa inner txn Return locked funds to locker regardless.

     *
     * @param params The params for the smart contract call
     * @returns The call result: True on success
     */
    reclaimExpired: async (params: CallParams<BloopIntentRouterArgs['obj']['reclaim_expired(uint64)bool'] | BloopIntentRouterArgs['tuple']['reclaim_expired(uint64)bool']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(BloopIntentRouterParamsFactory.reclaimExpired(params))
      return {...result, return: result.return as unknown as (undefined | BloopIntentRouterReturns['reclaim_expired(uint64)bool'])}
    },

    /**
     * Makes a call to the BloopIntentRouter smart contract using the `get_intent(uint64)(address,uint64,uint64,uint64,byte[32],address,address,byte[32],uint64)` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * Read a full intent struct. Does not modify state.
     *
     * @param params The params for the smart contract call
     * @returns The call result: Full Intent struct copy
     */
    getIntent: async (params: CallParams<BloopIntentRouterArgs['obj']['get_intent(uint64)(address,uint64,uint64,uint64,byte[32],address,address,byte[32],uint64)'] | BloopIntentRouterArgs['tuple']['get_intent(uint64)(address,uint64,uint64,uint64,byte[32],address,address,byte[32],uint64)']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(BloopIntentRouterParamsFactory.getIntent(params))
      return {...result, return: result.return as unknown as (undefined | BloopIntentRouterReturns['get_intent(uint64)(address,uint64,uint64,uint64,byte[32],address,address,byte[32],uint64)'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new BloopIntentRouterClient(this.appClient.clone(params))
  }

  /**
   * Makes a readonly (simulated) call to the BloopIntentRouter smart contract using the `get_intent(uint64)(address,uint64,uint64,uint64,byte[32],address,address,byte[32],uint64)` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * Read a full intent struct. Does not modify state.
   *
   * @param params The params for the smart contract call
   * @returns The call result: Full Intent struct copy
   */
  async getIntent(params: CallParams<BloopIntentRouterArgs['obj']['get_intent(uint64)(address,uint64,uint64,uint64,byte[32],address,address,byte[32],uint64)'] | BloopIntentRouterArgs['tuple']['get_intent(uint64)(address,uint64,uint64,uint64,byte[32],address,address,byte[32],uint64)']>) {
    const result = await this.appClient.send.call(BloopIntentRouterParamsFactory.getIntent(params))
    return result.return as unknown as BloopIntentRouterReturns['get_intent(uint64)(address,uint64,uint64,uint64,byte[32],address,address,byte[32],uint64)']
  }

  /**
   * Methods to access state for the current BloopIntentRouter app
   */
  state = {
    /**
     * Methods to access global state for the current BloopIntentRouter app
     */
    global: {
      /**
       * Get all current keyed values from global state
       */
      getAll: async (): Promise<Partial<Expand<GlobalKeysState>>> => {
        const result = await this.appClient.state.global.getAll()
        return {
          bloopaAppId: result.bloopa_app_id,
          totalIntents: result.total_intents,
          routerTreasury: result.router_treasury,
          admin: result.admin,
          isLive: result.is_live,
        }
      },
      /**
       * Get the current value of the bloopa_app_id key in global state
       */
      bloopaAppId: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("bloopa_app_id")) as bigint | undefined },
      /**
       * Get the current value of the total_intents key in global state
       */
      totalIntents: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("total_intents")) as bigint | undefined },
      /**
       * Get the current value of the router_treasury key in global state
       */
      routerTreasury: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("router_treasury")) as bigint | undefined },
      /**
       * Get the current value of the admin key in global state
       */
      admin: async (): Promise<string | undefined> => { return (await this.appClient.state.global.getValue("admin")) as string | undefined },
      /**
       * Get the current value of the is_live key in global state
       */
      isLive: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("is_live")) as bigint | undefined },
    },
    /**
     * Methods to access box state for the current BloopIntentRouter app
     */
    box: {
      /**
       * Get all current keyed values from box state
       */
      getAll: async (): Promise<Partial<Expand<BoxKeysState>>> => {
        const result = await this.appClient.state.box.getAll()
        return {
        }
      },
      /**
       * Get values from the intents map in box state
       */
      intents: {
        /**
         * Get all current values of the intents map in box state
         */
        getMap: async (): Promise<Map<bigint, Intent>> => { return (await this.appClient.state.box.getMap("intents")) as Map<bigint, Intent> },
        /**
         * Get a current value of the intents map by key from box state
         */
        value: async (key: bigint | number): Promise<Intent | undefined> => { return await this.appClient.state.box.getMapValue("intents", key) as Intent | undefined },
      },
    },
  }

  public newGroup(): BloopIntentRouterComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a lock_intent(byte[32],uint64,uint64,address)uint64 method call against the BloopIntentRouter contract
       */
      lockIntent(params: CallParams<BloopIntentRouterArgs['obj']['lock_intent(byte[32],uint64,uint64,address)uint64'] | BloopIntentRouterArgs['tuple']['lock_intent(byte[32],uint64,uint64,address)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.lockIntent(params)))
        resultMappers.push((v) => client.decodeReturnValue('lock_intent(byte[32],uint64,uint64,address)uint64', v))
        return this
      },
      /**
       * Add a borrow_to_execute(uint64,string,uint64)bool method call against the BloopIntentRouter contract
       */
      borrowToExecute(params: CallParams<BloopIntentRouterArgs['obj']['borrow_to_execute(uint64,string,uint64)bool'] | BloopIntentRouterArgs['tuple']['borrow_to_execute(uint64,string,uint64)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.borrowToExecute(params)))
        resultMappers.push((v) => client.decodeReturnValue('borrow_to_execute(uint64,string,uint64)bool', v))
        return this
      },
      /**
       * Add a settle(uint64,byte[32],string)bool method call against the BloopIntentRouter contract
       */
      settle(params: CallParams<BloopIntentRouterArgs['obj']['settle(uint64,byte[32],string)bool'] | BloopIntentRouterArgs['tuple']['settle(uint64,byte[32],string)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.settle(params)))
        resultMappers.push((v) => client.decodeReturnValue('settle(uint64,byte[32],string)bool', v))
        return this
      },
      /**
       * Add a reclaim_expired(uint64)bool method call against the BloopIntentRouter contract
       */
      reclaimExpired(params: CallParams<BloopIntentRouterArgs['obj']['reclaim_expired(uint64)bool'] | BloopIntentRouterArgs['tuple']['reclaim_expired(uint64)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.reclaimExpired(params)))
        resultMappers.push((v) => client.decodeReturnValue('reclaim_expired(uint64)bool', v))
        return this
      },
      /**
       * Add a get_intent(uint64)(address,uint64,uint64,uint64,byte[32],address,address,byte[32],uint64) method call against the BloopIntentRouter contract
       */
      getIntent(params: CallParams<BloopIntentRouterArgs['obj']['get_intent(uint64)(address,uint64,uint64,uint64,byte[32],address,address,byte[32],uint64)'] | BloopIntentRouterArgs['tuple']['get_intent(uint64)(address,uint64,uint64,uint64,byte[32],address,address,byte[32],uint64)']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.getIntent(params)))
        resultMappers.push((v) => client.decodeReturnValue('get_intent(uint64)(address,uint64,uint64,uint64,byte[32],address,address,byte[32],uint64)', v))
        return this
      },
      /**
       * Add a clear state call to the BloopIntentRouter contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as BloopIntentRouterComposer
  }
}
export type BloopIntentRouterComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the lock_intent(byte[32],uint64,uint64,address)uint64 ABI method.
   *
  * Lock ALGO and create a private swap intent.
  user1 sends a payment to the Router (as the previous gtxn) and specifies which solver can fulfill. Only the named solver_address can call borrow_to_execute().
  The payment transaction must be the immediately preceding transaction in the group (gtxn[-1] / gtxn[Txn.group_index - 1]).

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  lockIntent(params?: CallParams<BloopIntentRouterArgs['obj']['lock_intent(byte[32],uint64,uint64,address)uint64'] | BloopIntentRouterArgs['tuple']['lock_intent(byte[32],uint64,uint64,address)uint64']>): BloopIntentRouterComposer<[...TReturns, BloopIntentRouterReturns['lock_intent(byte[32],uint64,uint64,address)uint64'] | undefined]>

  /**
   * Calls the borrow_to_execute(uint64,string,uint64)bool ABI method.
   *
  * Claim a private intent as the designated solver.
  The solver must have ALREADY drawn credit from Bloopa externally (via BloopaCreditAgent.draw()) before calling this method. This method only enforces the private order and marks the intent assigned.
  PRIVATE ORDER ENFORCEMENT: Only intent.solver_address can call this. Any other caller gets assertion failure.

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  borrowToExecute(params?: CallParams<BloopIntentRouterArgs['obj']['borrow_to_execute(uint64,string,uint64)bool'] | BloopIntentRouterArgs['tuple']['borrow_to_execute(uint64,string,uint64)bool']>): BloopIntentRouterComposer<[...TReturns, BloopIntentRouterReturns['borrow_to_execute(uint64,string,uint64)bool'] | undefined]>

  /**
   * Calls the settle(uint64,byte[32],string)bool ABI method.
   *
  * Settle a completed intent atomically.
  Sends repayment to Bloopa from Router escrow + profit to solver. Calls Bloopa.record_payment() to build solver's tier history.
  Interest formula (Tier 0, matches contract.py exactly):     interest = (api_cost * 2400 * 86400) // (10000 * 31536000)     interest += 1  # minimum 1 μA

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  settle(params?: CallParams<BloopIntentRouterArgs['obj']['settle(uint64,byte[32],string)bool'] | BloopIntentRouterArgs['tuple']['settle(uint64,byte[32],string)bool']>): BloopIntentRouterComposer<[...TReturns, BloopIntentRouterReturns['settle(uint64,byte[32],string)bool'] | undefined]>

  /**
   * Calls the reclaim_expired(uint64)bool ABI method.
   *
  * Reclaim locked funds for an expired intent.
  Only the original locker (user1) can reclaim. If the intent was assigned but never settled (solver took job, disappeared):     → Slash the assigned agent via Bloopa inner txn Return locked funds to locker regardless.

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  reclaimExpired(params?: CallParams<BloopIntentRouterArgs['obj']['reclaim_expired(uint64)bool'] | BloopIntentRouterArgs['tuple']['reclaim_expired(uint64)bool']>): BloopIntentRouterComposer<[...TReturns, BloopIntentRouterReturns['reclaim_expired(uint64)bool'] | undefined]>

  /**
   * Calls the get_intent(uint64)(address,uint64,uint64,uint64,byte[32],address,address,byte[32],uint64) ABI method.
   *
   * Read a full intent struct. Does not modify state.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  getIntent(params?: CallParams<BloopIntentRouterArgs['obj']['get_intent(uint64)(address,uint64,uint64,uint64,byte[32],address,address,byte[32],uint64)'] | BloopIntentRouterArgs['tuple']['get_intent(uint64)(address,uint64,uint64,uint64,byte[32],address,address,byte[32],uint64)']>): BloopIntentRouterComposer<[...TReturns, BloopIntentRouterReturns['get_intent(uint64)(address,uint64,uint64,uint64,byte[32],address,address,byte[32],uint64)'] | undefined]>

  /**
   * Makes a clear_state call to an existing instance of the BloopIntentRouter smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): BloopIntentRouterComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): BloopIntentRouterComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<BloopIntentRouterComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<BloopIntentRouterComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<BloopIntentRouterComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<BloopIntentRouterComposerResults<TReturns>>
}
export type BloopIntentRouterComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

