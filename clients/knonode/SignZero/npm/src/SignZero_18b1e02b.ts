/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"SignZero","structs":{},"methods":[{"name":"createApplication","args":[],"returns":{"type":"void"},"actions":{"create":["NoOp"],"call":[]},"readonly":false,"desc":"Creates the application (empty state)","events":[],"recommendations":{}},{"name":"initialize","args":[{"type":"string","name":"title","desc":"- Opinion title (becomes ASA name, max 32 chars)"},{"type":"uint64","name":"textSize","desc":"- Size of opinion text in bytes (box is pre-allocated)"},{"type":"uint64","name":"duration","desc":"- Duration in rounds (min 25,000)"},{"type":"byte[]","name":"opinionType","desc":"- Opinion type (exactly 32 bytes, right-padded with zeros)"},{"type":"string","name":"url","desc":"- Optional author website (max 96 bytes)"}],"returns":{"type":"uint64"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Initializes the opinion with funding, title, text size, duration, type, and optional URL\nMust be called with a payment transaction in the same group\nText content is written separately via writeChunk calls (also in the same group)","events":[],"recommendations":{}},{"name":"writeChunk","args":[{"type":"uint64","name":"offset","desc":"- Byte offset within the box to start writing"},{"type":"byte[]","name":"data","desc":"- Chunk of text data to write"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Writes a chunk of opinion text into the pre-allocated box\nMust be called after initialize and before finalize","events":[],"recommendations":{}},{"name":"setGates","args":[{"type":"uint64","name":"flags","desc":"- Bitfield: bit0=asaHold, bit1=asaDeny, bit2=balMin, bit3=balMax, bit4=online, bit5=age, bit6=nfd"},{"type":"uint64","name":"balMin","desc":"- Min ALGO balance in microAlgos (used if bit2 set)"},{"type":"uint64","name":"balMax","desc":"- Max ALGO balance in microAlgos (used if bit3 set)"},{"type":"uint64","name":"minAge","desc":"- Min account age in rounds (used if bit5 set)"},{"type":"byte[]","name":"asaHold","desc":"- Packed uint64[] of ASA IDs signer must hold (used if bit0 set)"},{"type":"byte[]","name":"asaDeny","desc":"- Packed uint64[] of ASA IDs signer must NOT hold (used if bit1 set)"},{"type":"string","name":"nfdRoot","desc":"- NFD root name e.g. \"dao.algo\" (used if bit6 set)"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Sets signer gate requirements (author only, callable once)\nGates are frontend-enforced; the contract stores config for persistence/display.","events":[],"recommendations":{}},{"name":"sign","args":[],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Signs the opinion (validates atomic group with ASA opt-in)\nMust be called as first txn in atomic group where second txn is ASA opt-in","events":[],"recommendations":{}},{"name":"extend","args":[{"type":"uint64","name":"newEndRound","desc":"- New end round (must be greater than current)"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Extends the opinion duration (author only)","events":[],"recommendations":{}},{"name":"finalize","args":[],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Finalizes the opinion (anyone can call after end round)\nRemoves ASA manager and sends remaining balance to caller","events":[],"recommendations":{}},{"name":"getInfo","args":[],"returns":{"type":"(uint64,uint64,uint64,bool,bool)"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"desc":"Read-only: Get opinion info","events":[],"recommendations":{}}],"arcs":[22,28],"networks":{},"state":{"schema":{"global":{"ints":9,"bytes":0},"local":{"ints":0,"bytes":0}},"keys":{"global":{"startRound":{"keyType":"AVMString","valueType":"AVMUint64","key":"c3RhcnQ="},"endRound":{"keyType":"AVMString","valueType":"AVMUint64","key":"ZW5k"},"finalized":{"keyType":"AVMString","valueType":"AVMUint64","key":"ZmluYWxpemVk"},"asaId":{"keyType":"AVMString","valueType":"AVMUint64","key":"YXNh"},"initialized":{"keyType":"AVMString","valueType":"AVMUint64","key":"aW5pdA=="},"gFlags":{"keyType":"AVMString","valueType":"AVMUint64","key":"Z2Y="},"gBalMin":{"keyType":"AVMString","valueType":"AVMUint64","key":"Z2JtaW4="},"gBalMax":{"keyType":"AVMString","valueType":"AVMUint64","key":"Z2JtYXg="},"gMinAge":{"keyType":"AVMString","valueType":"AVMUint64","key":"Z2FnZQ=="}},"local":{},"box":{"opinionText":{"keyType":"AVMString","valueType":"AVMString","key":"dGV4dA=="},"gateHold":{"keyType":"AVMString","valueType":"AVMBytes","key":"Z2F0ZV9ob2xk"},"gateDeny":{"keyType":"AVMString","valueType":"AVMBytes","key":"Z2F0ZV9kZW55"},"gateNfd":{"keyType":"AVMString","valueType":"AVMString","key":"Z2F0ZV9uZmQ="}}},"maps":{"global":{},"local":{},"box":{}}},"bareActions":{"create":[],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[239],"errorMessage":"Already initialized"},{"pc":[747],"errorMessage":"Expected app call + ASA opt-in"},{"pc":[244],"errorMessage":"Expected payment + app call"},{"pc":[573],"errorMessage":"Gates already set"},{"pc":[267],"errorMessage":"Minimum 20 ALGO required"},{"pc":[275],"errorMessage":"Minimum duration 25,000 rounds"},{"pc":[768],"errorMessage":"Must be opt-in (amount 0)"},{"pc":[577],"errorMessage":"Must enable at least one gate"},{"pc":[763],"errorMessage":"Must opt into opinion ASA"},{"pc":[824],"errorMessage":"New end must be greater"},{"pc":[430,543,728,798,834],"errorMessage":"Not initialized"},{"pc":[83],"errorMessage":"OnCompletion must be NoOp"},{"pc":[810],"errorMessage":"Only author can extend"},{"pc":[554],"errorMessage":"Only author can set gates"},{"pc":[447],"errorMessage":"Only author can write"},{"pc":[436,742,816,848],"errorMessage":"Opinion already finalized"},{"pc":[736],"errorMessage":"Opinion has ended"},{"pc":[842],"errorMessage":"Opinion still active"},{"pc":[313],"errorMessage":"Opinion type cannot be empty"},{"pc":[306],"errorMessage":"Opinion type must be 32 bytes"},{"pc":[782],"errorMessage":"Opt-in receiver must match caller"},{"pc":[775],"errorMessage":"Opt-in sender must match caller"},{"pc":[257],"errorMessage":"Payment must go to app"},{"pc":[299],"errorMessage":"Text exceeds 32KB"},{"pc":[291],"errorMessage":"Text size must be > 0"},{"pc":[281],"errorMessage":"Title cannot be empty"},{"pc":[288],"errorMessage":"Title exceeds 32 bytes"},{"pc":[319],"errorMessage":"URL exceeds 96 bytes"},{"pc":[889,894],"errorMessage":"account funded"},{"pc":[674],"errorMessage":"asaDeny cannot be empty"},{"pc":[679],"errorMessage":"asaDeny must be N*8 bytes"},{"pc":[645],"errorMessage":"asaHold cannot be empty"},{"pc":[650],"errorMessage":"asaHold must be N*8 bytes"},{"pc":[445,552,808,860],"errorMessage":"asset exists"},{"pc":[237,429,434,442,542,549,568,727,734,740,761,797,802,814,820,833,840,846,852,922,926,930,934,938],"errorMessage":"check GlobalState exists"},{"pc":[173,207,223,415,496,512,528],"errorMessage":"invalid array length header"},{"pc":[180,214,230,422,503,519,535],"errorMessage":"invalid number of bytes for arc4.dynamic_array<arc4.uint8>"},{"pc":[191,200,408,462,471,480,489,792],"errorMessage":"invalid number of bytes for arc4.uint64"},{"pc":[704],"errorMessage":"nfdRoot cannot be empty"},{"pc":[754],"errorMessage":"transaction type is axfer"},{"pc":[250],"errorMessage":"transaction type is pay"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"events":[]} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


/**
 * The argument types for the SignZero contract
 */
export type SignZeroArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'createApplication()void': Record<string, never>
    'initialize(string,uint64,uint64,byte[],string)uint64': {
      /**
       * - Opinion title (becomes ASA name, max 32 chars)
       */
      title: string
      /**
       * - Size of opinion text in bytes (box is pre-allocated)
       */
      textSize: bigint | number
      /**
       * - Duration in rounds (min 25,000)
       */
      duration: bigint | number
      /**
       * - Opinion type (exactly 32 bytes, right-padded with zeros)
       */
      opinionType: Uint8Array
      /**
       * - Optional author website (max 96 bytes)
       */
      url: string
    }
    'writeChunk(uint64,byte[])void': {
      /**
       * - Byte offset within the box to start writing
       */
      offset: bigint | number
      /**
       * - Chunk of text data to write
       */
      data: Uint8Array
    }
    'setGates(uint64,uint64,uint64,uint64,byte[],byte[],string)void': {
      /**
       * - Bitfield: bit0=asaHold, bit1=asaDeny, bit2=balMin, bit3=balMax, bit4=online, bit5=age, bit6=nfd
       */
      flags: bigint | number
      /**
       * - Min ALGO balance in microAlgos (used if bit2 set)
       */
      balMin: bigint | number
      /**
       * - Max ALGO balance in microAlgos (used if bit3 set)
       */
      balMax: bigint | number
      /**
       * - Min account age in rounds (used if bit5 set)
       */
      minAge: bigint | number
      /**
       * - Packed uint64[] of ASA IDs signer must hold (used if bit0 set)
       */
      asaHold: Uint8Array
      /**
       * - Packed uint64[] of ASA IDs signer must NOT hold (used if bit1 set)
       */
      asaDeny: Uint8Array
      /**
       * - NFD root name e.g. "dao.algo" (used if bit6 set)
       */
      nfdRoot: string
    }
    'sign()void': Record<string, never>
    'extend(uint64)void': {
      /**
       * - New end round (must be greater than current)
       */
      newEndRound: bigint | number
    }
    'finalize()void': Record<string, never>
    'getInfo()(uint64,uint64,uint64,bool,bool)': Record<string, never>
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'createApplication()void': []
    'initialize(string,uint64,uint64,byte[],string)uint64': [title: string, textSize: bigint | number, duration: bigint | number, opinionType: Uint8Array, url: string]
    'writeChunk(uint64,byte[])void': [offset: bigint | number, data: Uint8Array]
    'setGates(uint64,uint64,uint64,uint64,byte[],byte[],string)void': [flags: bigint | number, balMin: bigint | number, balMax: bigint | number, minAge: bigint | number, asaHold: Uint8Array, asaDeny: Uint8Array, nfdRoot: string]
    'sign()void': []
    'extend(uint64)void': [newEndRound: bigint | number]
    'finalize()void': []
    'getInfo()(uint64,uint64,uint64,bool,bool)': []
  }
}

/**
 * The return type for each method
 */
export type SignZeroReturns = {
  'createApplication()void': void
  'initialize(string,uint64,uint64,byte[],string)uint64': bigint
  'writeChunk(uint64,byte[])void': void
  'setGates(uint64,uint64,uint64,uint64,byte[],byte[],string)void': void
  'sign()void': void
  'extend(uint64)void': void
  'finalize()void': void
  'getInfo()(uint64,uint64,uint64,bool,bool)': [bigint, bigint, bigint, boolean, boolean]
}

/**
 * Defines the types of available calls and state of the SignZero smart contract.
 */
export type SignZeroTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'createApplication()void' | 'createApplication', {
      argsObj: SignZeroArgs['obj']['createApplication()void']
      argsTuple: SignZeroArgs['tuple']['createApplication()void']
      returns: SignZeroReturns['createApplication()void']
    }>
    & Record<'initialize(string,uint64,uint64,byte[],string)uint64' | 'initialize', {
      argsObj: SignZeroArgs['obj']['initialize(string,uint64,uint64,byte[],string)uint64']
      argsTuple: SignZeroArgs['tuple']['initialize(string,uint64,uint64,byte[],string)uint64']
      returns: SignZeroReturns['initialize(string,uint64,uint64,byte[],string)uint64']
    }>
    & Record<'writeChunk(uint64,byte[])void' | 'writeChunk', {
      argsObj: SignZeroArgs['obj']['writeChunk(uint64,byte[])void']
      argsTuple: SignZeroArgs['tuple']['writeChunk(uint64,byte[])void']
      returns: SignZeroReturns['writeChunk(uint64,byte[])void']
    }>
    & Record<'setGates(uint64,uint64,uint64,uint64,byte[],byte[],string)void' | 'setGates', {
      argsObj: SignZeroArgs['obj']['setGates(uint64,uint64,uint64,uint64,byte[],byte[],string)void']
      argsTuple: SignZeroArgs['tuple']['setGates(uint64,uint64,uint64,uint64,byte[],byte[],string)void']
      returns: SignZeroReturns['setGates(uint64,uint64,uint64,uint64,byte[],byte[],string)void']
    }>
    & Record<'sign()void' | 'sign', {
      argsObj: SignZeroArgs['obj']['sign()void']
      argsTuple: SignZeroArgs['tuple']['sign()void']
      returns: SignZeroReturns['sign()void']
    }>
    & Record<'extend(uint64)void' | 'extend', {
      argsObj: SignZeroArgs['obj']['extend(uint64)void']
      argsTuple: SignZeroArgs['tuple']['extend(uint64)void']
      returns: SignZeroReturns['extend(uint64)void']
    }>
    & Record<'finalize()void' | 'finalize', {
      argsObj: SignZeroArgs['obj']['finalize()void']
      argsTuple: SignZeroArgs['tuple']['finalize()void']
      returns: SignZeroReturns['finalize()void']
    }>
    & Record<'getInfo()(uint64,uint64,uint64,bool,bool)' | 'getInfo', {
      argsObj: SignZeroArgs['obj']['getInfo()(uint64,uint64,uint64,bool,bool)']
      argsTuple: SignZeroArgs['tuple']['getInfo()(uint64,uint64,uint64,bool,bool)']
      returns: SignZeroReturns['getInfo()(uint64,uint64,uint64,bool,bool)']
    }>
  /**
   * Defines the shape of the state of the application.
   */
  state: {
    global: {
      keys: {
        startRound: bigint
        endRound: bigint
        finalized: bigint
        asaId: bigint
        initialized: bigint
        gFlags: bigint
        gBalMin: bigint
        gBalMax: bigint
        gMinAge: bigint
      }
      maps: {}
    }
    box: {
      keys: {
        opinionText: string
        gateHold: BinaryState
        gateDeny: BinaryState
        gateNfd: string
      }
      maps: {}
    }
  }
}

/**
 * Defines the possible abi call signatures.
 */
export type SignZeroSignatures = keyof SignZeroTypes['methods']
/**
 * Defines the possible abi call signatures for methods that return a non-void value.
 */
export type SignZeroNonVoidMethodSignatures = keyof SignZeroTypes['methods'] extends infer T ? T extends keyof SignZeroTypes['methods'] ? MethodReturn<T> extends void ? never : T  : never : never
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the SignZero smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends SignZeroSignatures> = SignZeroTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the SignZero smart contract to the method's return type
 */
export type MethodReturn<TSignature extends SignZeroSignatures> = SignZeroTypes['methods'][TSignature]['returns']

/**
 * Defines the shape of the keyed global state of the application.
 */
export type GlobalKeysState = SignZeroTypes['state']['global']['keys']

/**
 * Defines the shape of the keyed box state of the application.
 */
export type BoxKeysState = SignZeroTypes['state']['box']['keys']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the SignZero smart contract
 */
export abstract class SignZeroParamsFactory {
  /**
   * Constructs a no op call for the initialize(string,uint64,uint64,byte[],string)uint64 ABI method
   *
  * Initializes the opinion with funding, title, text size, duration, type, and optional URL
  Must be called with a payment transaction in the same group
  Text content is written separately via writeChunk calls (also in the same group)

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static initialize(params: CallParams<SignZeroArgs['obj']['initialize(string,uint64,uint64,byte[],string)uint64'] | SignZeroArgs['tuple']['initialize(string,uint64,uint64,byte[],string)uint64']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'initialize(string,uint64,uint64,byte[],string)uint64' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.title, params.args.textSize, params.args.duration, params.args.opinionType, params.args.url],
    }
  }
  /**
   * Constructs a no op call for the writeChunk(uint64,byte[])void ABI method
   *
  * Writes a chunk of opinion text into the pre-allocated box
  Must be called after initialize and before finalize

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static writeChunk(params: CallParams<SignZeroArgs['obj']['writeChunk(uint64,byte[])void'] | SignZeroArgs['tuple']['writeChunk(uint64,byte[])void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'writeChunk(uint64,byte[])void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.offset, params.args.data],
    }
  }
  /**
   * Constructs a no op call for the setGates(uint64,uint64,uint64,uint64,byte[],byte[],string)void ABI method
   *
  * Sets signer gate requirements (author only, callable once)
  Gates are frontend-enforced; the contract stores config for persistence/display.

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static setGates(params: CallParams<SignZeroArgs['obj']['setGates(uint64,uint64,uint64,uint64,byte[],byte[],string)void'] | SignZeroArgs['tuple']['setGates(uint64,uint64,uint64,uint64,byte[],byte[],string)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'setGates(uint64,uint64,uint64,uint64,byte[],byte[],string)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.flags, params.args.balMin, params.args.balMax, params.args.minAge, params.args.asaHold, params.args.asaDeny, params.args.nfdRoot],
    }
  }
  /**
   * Constructs a no op call for the sign()void ABI method
   *
  * Signs the opinion (validates atomic group with ASA opt-in)
  Must be called as first txn in atomic group where second txn is ASA opt-in

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static sign(params: CallParams<SignZeroArgs['obj']['sign()void'] | SignZeroArgs['tuple']['sign()void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'sign()void' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the extend(uint64)void ABI method
   *
   * Extends the opinion duration (author only)
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static extend(params: CallParams<SignZeroArgs['obj']['extend(uint64)void'] | SignZeroArgs['tuple']['extend(uint64)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'extend(uint64)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.newEndRound],
    }
  }
  /**
   * Constructs a no op call for the finalize()void ABI method
   *
  * Finalizes the opinion (anyone can call after end round)
  Removes ASA manager and sends remaining balance to caller

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static finalize(params: CallParams<SignZeroArgs['obj']['finalize()void'] | SignZeroArgs['tuple']['finalize()void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'finalize()void' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the getInfo()(uint64,uint64,uint64,bool,bool) ABI method
   *
   * Read-only: Get opinion info
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static getInfo(params: CallParams<SignZeroArgs['obj']['getInfo()(uint64,uint64,uint64,bool,bool)'] | SignZeroArgs['tuple']['getInfo()(uint64,uint64,uint64,bool,bool)']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'getInfo()(uint64,uint64,uint64,bool,bool)' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
}

/**
 * A client to make calls to the SignZero smart contract
 */
export class SignZeroClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `SignZeroClient`
   *
   * @param appClient An `AppClient` instance which has been created with the SignZero app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `SignZeroClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Checks for decode errors on the given return value and maps the return value to the return type for the given method
   * @returns The typed return value or undefined if there was no value
   */
  decodeReturnValue<TSignature extends SignZeroNonVoidMethodSignatures>(method: TSignature, returnValue: ABIReturn | undefined) {
    return returnValue !== undefined ? getArc56ReturnValue<MethodReturn<TSignature>>(returnValue, this.appClient.getABIMethod(method), APP_SPEC.structs) : undefined
  }

  /**
   * Returns a new `SignZeroClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<SignZeroClient> {
    return new SignZeroClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `SignZeroClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<SignZeroClient> {
    return new SignZeroClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Makes a clear_state call to an existing instance of the SignZero smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the SignZero smart contract using the `initialize(string,uint64,uint64,byte[],string)uint64` ABI method.
     *
    * Initializes the opinion with funding, title, text size, duration, type, and optional URL
    Must be called with a payment transaction in the same group
    Text content is written separately via writeChunk calls (also in the same group)

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    initialize: (params: CallParams<SignZeroArgs['obj']['initialize(string,uint64,uint64,byte[],string)uint64'] | SignZeroArgs['tuple']['initialize(string,uint64,uint64,byte[],string)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(SignZeroParamsFactory.initialize(params))
    },

    /**
     * Makes a call to the SignZero smart contract using the `writeChunk(uint64,byte[])void` ABI method.
     *
    * Writes a chunk of opinion text into the pre-allocated box
    Must be called after initialize and before finalize

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    writeChunk: (params: CallParams<SignZeroArgs['obj']['writeChunk(uint64,byte[])void'] | SignZeroArgs['tuple']['writeChunk(uint64,byte[])void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(SignZeroParamsFactory.writeChunk(params))
    },

    /**
     * Makes a call to the SignZero smart contract using the `setGates(uint64,uint64,uint64,uint64,byte[],byte[],string)void` ABI method.
     *
    * Sets signer gate requirements (author only, callable once)
    Gates are frontend-enforced; the contract stores config for persistence/display.

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    setGates: (params: CallParams<SignZeroArgs['obj']['setGates(uint64,uint64,uint64,uint64,byte[],byte[],string)void'] | SignZeroArgs['tuple']['setGates(uint64,uint64,uint64,uint64,byte[],byte[],string)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(SignZeroParamsFactory.setGates(params))
    },

    /**
     * Makes a call to the SignZero smart contract using the `sign()void` ABI method.
     *
    * Signs the opinion (validates atomic group with ASA opt-in)
    Must be called as first txn in atomic group where second txn is ASA opt-in

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    sign: (params: CallParams<SignZeroArgs['obj']['sign()void'] | SignZeroArgs['tuple']['sign()void']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(SignZeroParamsFactory.sign(params))
    },

    /**
     * Makes a call to the SignZero smart contract using the `extend(uint64)void` ABI method.
     *
     * Extends the opinion duration (author only)
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    extend: (params: CallParams<SignZeroArgs['obj']['extend(uint64)void'] | SignZeroArgs['tuple']['extend(uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(SignZeroParamsFactory.extend(params))
    },

    /**
     * Makes a call to the SignZero smart contract using the `finalize()void` ABI method.
     *
    * Finalizes the opinion (anyone can call after end round)
    Removes ASA manager and sends remaining balance to caller

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    finalize: (params: CallParams<SignZeroArgs['obj']['finalize()void'] | SignZeroArgs['tuple']['finalize()void']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(SignZeroParamsFactory.finalize(params))
    },

    /**
     * Makes a call to the SignZero smart contract using the `getInfo()(uint64,uint64,uint64,bool,bool)` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * Read-only: Get opinion info
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    getInfo: (params: CallParams<SignZeroArgs['obj']['getInfo()(uint64,uint64,uint64,bool,bool)'] | SignZeroArgs['tuple']['getInfo()(uint64,uint64,uint64,bool,bool)']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(SignZeroParamsFactory.getInfo(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Makes a clear_state call to an existing instance of the SignZero smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the SignZero smart contract using the `initialize(string,uint64,uint64,byte[],string)uint64` ABI method.
     *
    * Initializes the opinion with funding, title, text size, duration, type, and optional URL
    Must be called with a payment transaction in the same group
    Text content is written separately via writeChunk calls (also in the same group)

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    initialize: (params: CallParams<SignZeroArgs['obj']['initialize(string,uint64,uint64,byte[],string)uint64'] | SignZeroArgs['tuple']['initialize(string,uint64,uint64,byte[],string)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(SignZeroParamsFactory.initialize(params))
    },

    /**
     * Makes a call to the SignZero smart contract using the `writeChunk(uint64,byte[])void` ABI method.
     *
    * Writes a chunk of opinion text into the pre-allocated box
    Must be called after initialize and before finalize

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    writeChunk: (params: CallParams<SignZeroArgs['obj']['writeChunk(uint64,byte[])void'] | SignZeroArgs['tuple']['writeChunk(uint64,byte[])void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(SignZeroParamsFactory.writeChunk(params))
    },

    /**
     * Makes a call to the SignZero smart contract using the `setGates(uint64,uint64,uint64,uint64,byte[],byte[],string)void` ABI method.
     *
    * Sets signer gate requirements (author only, callable once)
    Gates are frontend-enforced; the contract stores config for persistence/display.

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    setGates: (params: CallParams<SignZeroArgs['obj']['setGates(uint64,uint64,uint64,uint64,byte[],byte[],string)void'] | SignZeroArgs['tuple']['setGates(uint64,uint64,uint64,uint64,byte[],byte[],string)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(SignZeroParamsFactory.setGates(params))
    },

    /**
     * Makes a call to the SignZero smart contract using the `sign()void` ABI method.
     *
    * Signs the opinion (validates atomic group with ASA opt-in)
    Must be called as first txn in atomic group where second txn is ASA opt-in

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    sign: (params: CallParams<SignZeroArgs['obj']['sign()void'] | SignZeroArgs['tuple']['sign()void']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(SignZeroParamsFactory.sign(params))
    },

    /**
     * Makes a call to the SignZero smart contract using the `extend(uint64)void` ABI method.
     *
     * Extends the opinion duration (author only)
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    extend: (params: CallParams<SignZeroArgs['obj']['extend(uint64)void'] | SignZeroArgs['tuple']['extend(uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(SignZeroParamsFactory.extend(params))
    },

    /**
     * Makes a call to the SignZero smart contract using the `finalize()void` ABI method.
     *
    * Finalizes the opinion (anyone can call after end round)
    Removes ASA manager and sends remaining balance to caller

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    finalize: (params: CallParams<SignZeroArgs['obj']['finalize()void'] | SignZeroArgs['tuple']['finalize()void']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(SignZeroParamsFactory.finalize(params))
    },

    /**
     * Makes a call to the SignZero smart contract using the `getInfo()(uint64,uint64,uint64,bool,bool)` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * Read-only: Get opinion info
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    getInfo: (params: CallParams<SignZeroArgs['obj']['getInfo()(uint64,uint64,uint64,bool,bool)'] | SignZeroArgs['tuple']['getInfo()(uint64,uint64,uint64,bool,bool)']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(SignZeroParamsFactory.getInfo(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Makes a clear_state call to an existing instance of the SignZero smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the SignZero smart contract using the `initialize(string,uint64,uint64,byte[],string)uint64` ABI method.
     *
    * Initializes the opinion with funding, title, text size, duration, type, and optional URL
    Must be called with a payment transaction in the same group
    Text content is written separately via writeChunk calls (also in the same group)

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    initialize: async (params: CallParams<SignZeroArgs['obj']['initialize(string,uint64,uint64,byte[],string)uint64'] | SignZeroArgs['tuple']['initialize(string,uint64,uint64,byte[],string)uint64']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(SignZeroParamsFactory.initialize(params))
      return {...result, return: result.return as unknown as (undefined | SignZeroReturns['initialize(string,uint64,uint64,byte[],string)uint64'])}
    },

    /**
     * Makes a call to the SignZero smart contract using the `writeChunk(uint64,byte[])void` ABI method.
     *
    * Writes a chunk of opinion text into the pre-allocated box
    Must be called after initialize and before finalize

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    writeChunk: async (params: CallParams<SignZeroArgs['obj']['writeChunk(uint64,byte[])void'] | SignZeroArgs['tuple']['writeChunk(uint64,byte[])void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(SignZeroParamsFactory.writeChunk(params))
      return {...result, return: result.return as unknown as (undefined | SignZeroReturns['writeChunk(uint64,byte[])void'])}
    },

    /**
     * Makes a call to the SignZero smart contract using the `setGates(uint64,uint64,uint64,uint64,byte[],byte[],string)void` ABI method.
     *
    * Sets signer gate requirements (author only, callable once)
    Gates are frontend-enforced; the contract stores config for persistence/display.

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    setGates: async (params: CallParams<SignZeroArgs['obj']['setGates(uint64,uint64,uint64,uint64,byte[],byte[],string)void'] | SignZeroArgs['tuple']['setGates(uint64,uint64,uint64,uint64,byte[],byte[],string)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(SignZeroParamsFactory.setGates(params))
      return {...result, return: result.return as unknown as (undefined | SignZeroReturns['setGates(uint64,uint64,uint64,uint64,byte[],byte[],string)void'])}
    },

    /**
     * Makes a call to the SignZero smart contract using the `sign()void` ABI method.
     *
    * Signs the opinion (validates atomic group with ASA opt-in)
    Must be called as first txn in atomic group where second txn is ASA opt-in

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    sign: async (params: CallParams<SignZeroArgs['obj']['sign()void'] | SignZeroArgs['tuple']['sign()void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(SignZeroParamsFactory.sign(params))
      return {...result, return: result.return as unknown as (undefined | SignZeroReturns['sign()void'])}
    },

    /**
     * Makes a call to the SignZero smart contract using the `extend(uint64)void` ABI method.
     *
     * Extends the opinion duration (author only)
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    extend: async (params: CallParams<SignZeroArgs['obj']['extend(uint64)void'] | SignZeroArgs['tuple']['extend(uint64)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(SignZeroParamsFactory.extend(params))
      return {...result, return: result.return as unknown as (undefined | SignZeroReturns['extend(uint64)void'])}
    },

    /**
     * Makes a call to the SignZero smart contract using the `finalize()void` ABI method.
     *
    * Finalizes the opinion (anyone can call after end round)
    Removes ASA manager and sends remaining balance to caller

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    finalize: async (params: CallParams<SignZeroArgs['obj']['finalize()void'] | SignZeroArgs['tuple']['finalize()void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(SignZeroParamsFactory.finalize(params))
      return {...result, return: result.return as unknown as (undefined | SignZeroReturns['finalize()void'])}
    },

    /**
     * Makes a call to the SignZero smart contract using the `getInfo()(uint64,uint64,uint64,bool,bool)` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * Read-only: Get opinion info
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    getInfo: async (params: CallParams<SignZeroArgs['obj']['getInfo()(uint64,uint64,uint64,bool,bool)'] | SignZeroArgs['tuple']['getInfo()(uint64,uint64,uint64,bool,bool)']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(SignZeroParamsFactory.getInfo(params))
      return {...result, return: result.return as unknown as (undefined | SignZeroReturns['getInfo()(uint64,uint64,uint64,bool,bool)'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new SignZeroClient(this.appClient.clone(params))
  }

  /**
   * Makes a readonly (simulated) call to the SignZero smart contract using the `getInfo()(uint64,uint64,uint64,bool,bool)` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * Read-only: Get opinion info
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async getInfo(params: CallParams<SignZeroArgs['obj']['getInfo()(uint64,uint64,uint64,bool,bool)'] | SignZeroArgs['tuple']['getInfo()(uint64,uint64,uint64,bool,bool)']> = {args: []}) {
    const result = await this.appClient.send.call(SignZeroParamsFactory.getInfo(params))
    return result.return as unknown as SignZeroReturns['getInfo()(uint64,uint64,uint64,bool,bool)']
  }

  /**
   * Methods to access state for the current SignZero app
   */
  state = {
    /**
     * Methods to access global state for the current SignZero app
     */
    global: {
      /**
       * Get all current keyed values from global state
       */
      getAll: async (): Promise<Partial<Expand<GlobalKeysState>>> => {
        const result = await this.appClient.state.global.getAll()
        return {
          startRound: result.startRound,
          endRound: result.endRound,
          finalized: result.finalized,
          asaId: result.asaId,
          initialized: result.initialized,
          gFlags: result.gFlags,
          gBalMin: result.gBalMin,
          gBalMax: result.gBalMax,
          gMinAge: result.gMinAge,
        }
      },
      /**
       * Get the current value of the startRound key in global state
       */
      startRound: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("startRound")) as bigint | undefined },
      /**
       * Get the current value of the endRound key in global state
       */
      endRound: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("endRound")) as bigint | undefined },
      /**
       * Get the current value of the finalized key in global state
       */
      finalized: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("finalized")) as bigint | undefined },
      /**
       * Get the current value of the asaId key in global state
       */
      asaId: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("asaId")) as bigint | undefined },
      /**
       * Get the current value of the initialized key in global state
       */
      initialized: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("initialized")) as bigint | undefined },
      /**
       * Get the current value of the gFlags key in global state
       */
      gFlags: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("gFlags")) as bigint | undefined },
      /**
       * Get the current value of the gBalMin key in global state
       */
      gBalMin: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("gBalMin")) as bigint | undefined },
      /**
       * Get the current value of the gBalMax key in global state
       */
      gBalMax: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("gBalMax")) as bigint | undefined },
      /**
       * Get the current value of the gMinAge key in global state
       */
      gMinAge: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("gMinAge")) as bigint | undefined },
    },
    /**
     * Methods to access box state for the current SignZero app
     */
    box: {
      /**
       * Get all current keyed values from box state
       */
      getAll: async (): Promise<Partial<Expand<BoxKeysState>>> => {
        const result = await this.appClient.state.box.getAll()
        return {
          opinionText: result.opinionText,
          gateHold: new BinaryStateValue(result.gateHold),
          gateDeny: new BinaryStateValue(result.gateDeny),
          gateNfd: result.gateNfd,
        }
      },
      /**
       * Get the current value of the opinionText key in box state
       */
      opinionText: async (): Promise<string | undefined> => { return (await this.appClient.state.box.getValue("opinionText")) as string | undefined },
      /**
       * Get the current value of the gateHold key in box state
       */
      gateHold: async (): Promise<BinaryState> => { return new BinaryStateValue((await this.appClient.state.box.getValue("gateHold")) as Uint8Array | undefined) },
      /**
       * Get the current value of the gateDeny key in box state
       */
      gateDeny: async (): Promise<BinaryState> => { return new BinaryStateValue((await this.appClient.state.box.getValue("gateDeny")) as Uint8Array | undefined) },
      /**
       * Get the current value of the gateNfd key in box state
       */
      gateNfd: async (): Promise<string | undefined> => { return (await this.appClient.state.box.getValue("gateNfd")) as string | undefined },
    },
  }

  public newGroup(): SignZeroComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a initialize(string,uint64,uint64,byte[],string)uint64 method call against the SignZero contract
       */
      initialize(params: CallParams<SignZeroArgs['obj']['initialize(string,uint64,uint64,byte[],string)uint64'] | SignZeroArgs['tuple']['initialize(string,uint64,uint64,byte[],string)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.initialize(params)))
        resultMappers.push((v) => client.decodeReturnValue('initialize(string,uint64,uint64,byte[],string)uint64', v))
        return this
      },
      /**
       * Add a writeChunk(uint64,byte[])void method call against the SignZero contract
       */
      writeChunk(params: CallParams<SignZeroArgs['obj']['writeChunk(uint64,byte[])void'] | SignZeroArgs['tuple']['writeChunk(uint64,byte[])void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.writeChunk(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a setGates(uint64,uint64,uint64,uint64,byte[],byte[],string)void method call against the SignZero contract
       */
      setGates(params: CallParams<SignZeroArgs['obj']['setGates(uint64,uint64,uint64,uint64,byte[],byte[],string)void'] | SignZeroArgs['tuple']['setGates(uint64,uint64,uint64,uint64,byte[],byte[],string)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.setGates(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a sign()void method call against the SignZero contract
       */
      sign(params: CallParams<SignZeroArgs['obj']['sign()void'] | SignZeroArgs['tuple']['sign()void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.sign(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a extend(uint64)void method call against the SignZero contract
       */
      extend(params: CallParams<SignZeroArgs['obj']['extend(uint64)void'] | SignZeroArgs['tuple']['extend(uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.extend(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a finalize()void method call against the SignZero contract
       */
      finalize(params: CallParams<SignZeroArgs['obj']['finalize()void'] | SignZeroArgs['tuple']['finalize()void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.finalize(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a getInfo()(uint64,uint64,uint64,bool,bool) method call against the SignZero contract
       */
      getInfo(params: CallParams<SignZeroArgs['obj']['getInfo()(uint64,uint64,uint64,bool,bool)'] | SignZeroArgs['tuple']['getInfo()(uint64,uint64,uint64,bool,bool)']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.getInfo(params)))
        resultMappers.push((v) => client.decodeReturnValue('getInfo()(uint64,uint64,uint64,bool,bool)', v))
        return this
      },
      /**
       * Add a clear state call to the SignZero contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as SignZeroComposer
  }
}
export type SignZeroComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the initialize(string,uint64,uint64,byte[],string)uint64 ABI method.
   *
  * Initializes the opinion with funding, title, text size, duration, type, and optional URL
  Must be called with a payment transaction in the same group
  Text content is written separately via writeChunk calls (also in the same group)

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  initialize(params?: CallParams<SignZeroArgs['obj']['initialize(string,uint64,uint64,byte[],string)uint64'] | SignZeroArgs['tuple']['initialize(string,uint64,uint64,byte[],string)uint64']>): SignZeroComposer<[...TReturns, SignZeroReturns['initialize(string,uint64,uint64,byte[],string)uint64'] | undefined]>

  /**
   * Calls the writeChunk(uint64,byte[])void ABI method.
   *
  * Writes a chunk of opinion text into the pre-allocated box
  Must be called after initialize and before finalize

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  writeChunk(params?: CallParams<SignZeroArgs['obj']['writeChunk(uint64,byte[])void'] | SignZeroArgs['tuple']['writeChunk(uint64,byte[])void']>): SignZeroComposer<[...TReturns, SignZeroReturns['writeChunk(uint64,byte[])void'] | undefined]>

  /**
   * Calls the setGates(uint64,uint64,uint64,uint64,byte[],byte[],string)void ABI method.
   *
  * Sets signer gate requirements (author only, callable once)
  Gates are frontend-enforced; the contract stores config for persistence/display.

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  setGates(params?: CallParams<SignZeroArgs['obj']['setGates(uint64,uint64,uint64,uint64,byte[],byte[],string)void'] | SignZeroArgs['tuple']['setGates(uint64,uint64,uint64,uint64,byte[],byte[],string)void']>): SignZeroComposer<[...TReturns, SignZeroReturns['setGates(uint64,uint64,uint64,uint64,byte[],byte[],string)void'] | undefined]>

  /**
   * Calls the sign()void ABI method.
   *
  * Signs the opinion (validates atomic group with ASA opt-in)
  Must be called as first txn in atomic group where second txn is ASA opt-in

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  sign(params?: CallParams<SignZeroArgs['obj']['sign()void'] | SignZeroArgs['tuple']['sign()void']>): SignZeroComposer<[...TReturns, SignZeroReturns['sign()void'] | undefined]>

  /**
   * Calls the extend(uint64)void ABI method.
   *
   * Extends the opinion duration (author only)
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  extend(params?: CallParams<SignZeroArgs['obj']['extend(uint64)void'] | SignZeroArgs['tuple']['extend(uint64)void']>): SignZeroComposer<[...TReturns, SignZeroReturns['extend(uint64)void'] | undefined]>

  /**
   * Calls the finalize()void ABI method.
   *
  * Finalizes the opinion (anyone can call after end round)
  Removes ASA manager and sends remaining balance to caller

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  finalize(params?: CallParams<SignZeroArgs['obj']['finalize()void'] | SignZeroArgs['tuple']['finalize()void']>): SignZeroComposer<[...TReturns, SignZeroReturns['finalize()void'] | undefined]>

  /**
   * Calls the getInfo()(uint64,uint64,uint64,bool,bool) ABI method.
   *
   * Read-only: Get opinion info
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  getInfo(params?: CallParams<SignZeroArgs['obj']['getInfo()(uint64,uint64,uint64,bool,bool)'] | SignZeroArgs['tuple']['getInfo()(uint64,uint64,uint64,bool,bool)']>): SignZeroComposer<[...TReturns, SignZeroReturns['getInfo()(uint64,uint64,uint64,bool,bool)'] | undefined]>

  /**
   * Makes a clear_state call to an existing instance of the SignZero smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): SignZeroComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): SignZeroComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<SignZeroComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<SignZeroComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<SignZeroComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<SignZeroComposerResults<TReturns>>
}
export type SignZeroComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

