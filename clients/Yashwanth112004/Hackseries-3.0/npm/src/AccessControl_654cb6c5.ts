/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"AccessControl","structs":{},"methods":[{"name":"initialize","args":[],"returns":{"type":"void"},"actions":{"create":["NoOp"],"call":[]},"readonly":false,"desc":"Called only on contract creation.","events":[],"recommendations":{}},{"name":"opt_in","args":[],"returns":{"type":"void"},"actions":{"create":[],"call":["OptIn"]},"readonly":false,"desc":"Accounts must opt in to use local state.","events":[],"recommendations":{}},{"name":"add_admin","args":[{"type":"address","name":"admin"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}},{"name":"remove_admin","args":[{"type":"address","name":"admin"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}}],"arcs":[22,28],"networks":{},"state":{"schema":{"global":{"ints":0,"bytes":1},"local":{"ints":0,"bytes":1}},"keys":{"global":{"super_admin":{"keyType":"AVMString","valueType":"address","key":"c3VwZXJfYWRtaW4="}},"local":{"admins":{"keyType":"AVMString","valueType":"bool","key":"YWRtaW5z"}},"box":{}},"maps":{"global":{},"local":{},"box":{}}},"bareActions":{"create":[],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[151,173],"errorMessage":"Not admin"},{"pc":[148,170],"errorMessage":"check self.admins exists for account"},{"pc":[120],"errorMessage":"check self.super_admin exists"},{"pc":[142,164],"errorMessage":"invalid number of bytes for arc4.static_array<arc4.uint8, 32>"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"events":[]} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


/**
 * The argument types for the AccessControl contract
 */
export type AccessControlArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'initialize()void': Record<string, never>
    'opt_in()void': Record<string, never>
    'add_admin(address)void': {
      admin: string
    }
    'remove_admin(address)void': {
      admin: string
    }
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'initialize()void': []
    'opt_in()void': []
    'add_admin(address)void': [admin: string]
    'remove_admin(address)void': [admin: string]
  }
}

/**
 * The return type for each method
 */
export type AccessControlReturns = {
  'initialize()void': void
  'opt_in()void': void
  'add_admin(address)void': void
  'remove_admin(address)void': void
}

/**
 * Defines the types of available calls and state of the AccessControl smart contract.
 */
export type AccessControlTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'initialize()void' | 'initialize', {
      argsObj: AccessControlArgs['obj']['initialize()void']
      argsTuple: AccessControlArgs['tuple']['initialize()void']
      returns: AccessControlReturns['initialize()void']
    }>
    & Record<'opt_in()void' | 'opt_in', {
      argsObj: AccessControlArgs['obj']['opt_in()void']
      argsTuple: AccessControlArgs['tuple']['opt_in()void']
      returns: AccessControlReturns['opt_in()void']
    }>
    & Record<'add_admin(address)void' | 'add_admin', {
      argsObj: AccessControlArgs['obj']['add_admin(address)void']
      argsTuple: AccessControlArgs['tuple']['add_admin(address)void']
      returns: AccessControlReturns['add_admin(address)void']
    }>
    & Record<'remove_admin(address)void' | 'remove_admin', {
      argsObj: AccessControlArgs['obj']['remove_admin(address)void']
      argsTuple: AccessControlArgs['tuple']['remove_admin(address)void']
      returns: AccessControlReturns['remove_admin(address)void']
    }>
  /**
   * Defines the shape of the state of the application.
   */
  state: {
    global: {
      keys: {
        superAdmin: string
      }
      maps: {}
    }
    local: {
      keys: {
        admins: boolean
      }
      maps: {}
    }
  }
}

/**
 * Defines the possible abi call signatures.
 */
export type AccessControlSignatures = keyof AccessControlTypes['methods']
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the AccessControl smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends AccessControlSignatures> = AccessControlTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the AccessControl smart contract to the method's return type
 */
export type MethodReturn<TSignature extends AccessControlSignatures> = AccessControlTypes['methods'][TSignature]['returns']

/**
 * Defines the shape of the keyed global state of the application.
 */
export type GlobalKeysState = AccessControlTypes['state']['global']['keys']

/**
 * Defines the shape of the keyed local state of the application.
 */
export type LocalKeysState = AccessControlTypes['state']['local']['keys']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the AccessControl smart contract
 */
export abstract class AccessControlParamsFactory {
  /**
   * Gets available optIn ABI call param factories
   */
  static get optIn() {
    return {
      /**
       * Constructs opt-in ABI call params for the AccessControl smart contract using the opt_in()void ABI method
       *
       * @param params Parameters for the call
       * @returns An `AppClientMethodCallParams` object for the call
       */
      optIn(params: CallParams<AccessControlArgs['obj']['opt_in()void'] | AccessControlArgs['tuple']['opt_in()void']>): AppClientMethodCallParams {
        return {
          ...params,
          method: 'opt_in()void' as const,
          args: Array.isArray(params.args) ? params.args : [],
        }
      },
    }
  }

  /**
   * Constructs a no op call for the add_admin(address)void ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static addAdmin(params: CallParams<AccessControlArgs['obj']['add_admin(address)void'] | AccessControlArgs['tuple']['add_admin(address)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'add_admin(address)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.admin],
    }
  }
  /**
   * Constructs a no op call for the remove_admin(address)void ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static removeAdmin(params: CallParams<AccessControlArgs['obj']['remove_admin(address)void'] | AccessControlArgs['tuple']['remove_admin(address)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'remove_admin(address)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.admin],
    }
  }
}

/**
 * A client to make calls to the AccessControl smart contract
 */
export class AccessControlClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `AccessControlClient`
   *
   * @param appClient An `AppClient` instance which has been created with the AccessControl app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `AccessControlClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Returns a new `AccessControlClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<AccessControlClient> {
    return new AccessControlClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `AccessControlClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<AccessControlClient> {
    return new AccessControlClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Gets available optIn methods
     */
    optIn: {
      /**
       * Opts the user into an existing instance of the AccessControl smart contract using the `opt_in()void` ABI method.
       *
       * Accounts must opt in to use local state.
       *
       * @param params The params for the smart contract call
       * @returns The optIn params
       */
      optIn: (params: CallParams<AccessControlArgs['obj']['opt_in()void'] | AccessControlArgs['tuple']['opt_in()void']> = {args: []}) => {
        return this.appClient.params.optIn(AccessControlParamsFactory.optIn.optIn(params))
      },

    },

    /**
     * Makes a clear_state call to an existing instance of the AccessControl smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the AccessControl smart contract using the `add_admin(address)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    addAdmin: (params: CallParams<AccessControlArgs['obj']['add_admin(address)void'] | AccessControlArgs['tuple']['add_admin(address)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(AccessControlParamsFactory.addAdmin(params))
    },

    /**
     * Makes a call to the AccessControl smart contract using the `remove_admin(address)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    removeAdmin: (params: CallParams<AccessControlArgs['obj']['remove_admin(address)void'] | AccessControlArgs['tuple']['remove_admin(address)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(AccessControlParamsFactory.removeAdmin(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Gets available optIn methods
     */
    optIn: {
      /**
       * Opts the user into an existing instance of the AccessControl smart contract using the `opt_in()void` ABI method.
       *
       * Accounts must opt in to use local state.
       *
       * @param params The params for the smart contract call
       * @returns The optIn transaction
       */
      optIn: (params: CallParams<AccessControlArgs['obj']['opt_in()void'] | AccessControlArgs['tuple']['opt_in()void']> = {args: []}) => {
        return this.appClient.createTransaction.optIn(AccessControlParamsFactory.optIn.optIn(params))
      },

    },

    /**
     * Makes a clear_state call to an existing instance of the AccessControl smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the AccessControl smart contract using the `add_admin(address)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    addAdmin: (params: CallParams<AccessControlArgs['obj']['add_admin(address)void'] | AccessControlArgs['tuple']['add_admin(address)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(AccessControlParamsFactory.addAdmin(params))
    },

    /**
     * Makes a call to the AccessControl smart contract using the `remove_admin(address)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    removeAdmin: (params: CallParams<AccessControlArgs['obj']['remove_admin(address)void'] | AccessControlArgs['tuple']['remove_admin(address)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(AccessControlParamsFactory.removeAdmin(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Gets available optIn methods
     */
    optIn: {
      /**
       * Opts the user into an existing instance of the AccessControl smart contract using the `opt_in()void` ABI method.
       *
       * Accounts must opt in to use local state.
       *
       * @param params The params for the smart contract call
       * @returns The optIn result
       */
      optIn: async (params: CallParams<AccessControlArgs['obj']['opt_in()void'] | AccessControlArgs['tuple']['opt_in()void']> & SendParams = {args: []}) => {
        const result = await this.appClient.send.optIn(AccessControlParamsFactory.optIn.optIn(params))
        return {...result, return: result.return as unknown as (undefined | AccessControlReturns['opt_in()void'])}
      },

    },

    /**
     * Makes a clear_state call to an existing instance of the AccessControl smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the AccessControl smart contract using the `add_admin(address)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    addAdmin: async (params: CallParams<AccessControlArgs['obj']['add_admin(address)void'] | AccessControlArgs['tuple']['add_admin(address)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(AccessControlParamsFactory.addAdmin(params))
      return {...result, return: result.return as unknown as (undefined | AccessControlReturns['add_admin(address)void'])}
    },

    /**
     * Makes a call to the AccessControl smart contract using the `remove_admin(address)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    removeAdmin: async (params: CallParams<AccessControlArgs['obj']['remove_admin(address)void'] | AccessControlArgs['tuple']['remove_admin(address)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(AccessControlParamsFactory.removeAdmin(params))
      return {...result, return: result.return as unknown as (undefined | AccessControlReturns['remove_admin(address)void'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new AccessControlClient(this.appClient.clone(params))
  }

  /**
   * Methods to access state for the current AccessControl app
   */
  state = {
    /**
     * Methods to access global state for the current AccessControl app
     */
    global: {
      /**
       * Get all current keyed values from global state
       */
      getAll: async (): Promise<Partial<Expand<GlobalKeysState>>> => {
        const result = await this.appClient.state.global.getAll()
        return {
          superAdmin: result.super_admin,
        }
      },
      /**
       * Get the current value of the super_admin key in global state
       */
      superAdmin: async (): Promise<string | undefined> => { return (await this.appClient.state.global.getValue("super_admin")) as string | undefined },
    },
    /**
     * Methods to access local state for the current AccessControl app
     */
    local: (address: string | Address) => {
      const encodedAddress = typeof address === 'string' ? address : encodeAddress(address.publicKey)
      return {
        /**
         * Get all current keyed values from local state
         */
        getAll: async (): Promise<Partial<Expand<LocalKeysState>>> => {
          const result = await this.appClient.state.local(encodedAddress).getAll()
          return {
            admins: result.admins,
          }
        },
        /**
         * Get the current value of the admins key in local state
         */
        admins: async (): Promise<boolean | undefined> => { return (await this.appClient.state.local(encodedAddress).getValue("admins")) as boolean | undefined },
      }
    },
  }

  public newGroup(): AccessControlComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a add_admin(address)void method call against the AccessControl contract
       */
      addAdmin(params: CallParams<AccessControlArgs['obj']['add_admin(address)void'] | AccessControlArgs['tuple']['add_admin(address)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.addAdmin(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a remove_admin(address)void method call against the AccessControl contract
       */
      removeAdmin(params: CallParams<AccessControlArgs['obj']['remove_admin(address)void'] | AccessControlArgs['tuple']['remove_admin(address)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.removeAdmin(params)))
        resultMappers.push(undefined)
        return this
      },
      get optIn() {
        return {
          optIn: (params: CallParams<AccessControlArgs['obj']['opt_in()void'] | AccessControlArgs['tuple']['opt_in()void']>) => {
            promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.optIn.optIn(params)))
            resultMappers.push(undefined)
            return this
          },
        }
      },
      /**
       * Add a clear state call to the AccessControl contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as AccessControlComposer
  }
}
export type AccessControlComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the add_admin(address)void ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  addAdmin(params?: CallParams<AccessControlArgs['obj']['add_admin(address)void'] | AccessControlArgs['tuple']['add_admin(address)void']>): AccessControlComposer<[...TReturns, AccessControlReturns['add_admin(address)void'] | undefined]>

  /**
   * Calls the remove_admin(address)void ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  removeAdmin(params?: CallParams<AccessControlArgs['obj']['remove_admin(address)void'] | AccessControlArgs['tuple']['remove_admin(address)void']>): AccessControlComposer<[...TReturns, AccessControlReturns['remove_admin(address)void'] | undefined]>

  /**
   * Gets available optIn methods
   */
  readonly optIn: {
    /**
     * Opts the user into an existing instance of the AccessControl smart contract using the opt_in()void ABI method.
     *
     * @param args The arguments for the smart contract call
     * @param params Any additional parameters for the call
     * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
     */
    optIn(params?: CallParams<AccessControlArgs['obj']['opt_in()void'] | AccessControlArgs['tuple']['opt_in()void']>): AccessControlComposer<[...TReturns, AccessControlReturns['opt_in()void'] | undefined]>
  }

  /**
   * Makes a clear_state call to an existing instance of the AccessControl smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): AccessControlComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): AccessControlComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<AccessControlComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<AccessControlComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<AccessControlComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<AccessControlComposerResults<TReturns>>
}
export type AccessControlComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

