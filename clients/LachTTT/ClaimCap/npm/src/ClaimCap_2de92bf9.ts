/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"ClaimCap","structs":{},"methods":[{"name":"buy_nft","args":[{"type":"uint64","name":"amount"}],"returns":{"type":"string"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}},{"name":"sell_nft","args":[{"type":"address","name":"buyer"}],"returns":{"type":"string"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}},{"name":"check_hold_days","args":[],"returns":{"type":"uint64"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}},{"name":"get_claim_count","args":[],"returns":{"type":"uint64"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}},{"name":"hello","args":[{"type":"string","name":"name"}],"returns":{"type":"string"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}}],"arcs":[22,28],"networks":{},"state":{"schema":{"global":{"ints":4,"bytes":0},"local":{"ints":0,"bytes":0}},"keys":{"global":{"claim_count":{"keyType":"AVMString","valueType":"AVMUint64","key":"Y2xhaW1fY291bnQ="},"MAX_CLAIMS":{"keyType":"AVMString","valueType":"AVMUint64","key":"TUFYX0NMQUlNUw=="},"PRICE":{"keyType":"AVMString","valueType":"AVMUint64","key":"UFJJQ0U="},"HOLD_PERIOD":{"keyType":"AVMString","valueType":"AVMUint64","key":"SE9MRF9QRVJJT0Q="}},"local":{},"box":{}},"maps":{"global":{},"local":{},"box":{"owners":{"keyType":"address","valueType":"uint64","prefix":"b3duZXJz"}}}},"bareActions":{"create":["NoOp"],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[95],"errorMessage":"OnCompletion must be NoOp"},{"pc":[149],"errorMessage":"OnCompletion must be NoOp && can only call when creating"},{"pc":[437],"errorMessage":"check self.HOLD_PERIOD exists"},{"pc":[273],"errorMessage":"check self.MAX_CLAIMS exists"},{"pc":[170],"errorMessage":"check self.PRICE exists"},{"pc":[269,308,554],"errorMessage":"check self.claim_count exists"},{"pc":[232,381,533],"errorMessage":"check self.owners entry exists"},{"pc":[567],"errorMessage":"invalid array length header"},{"pc":[574],"errorMessage":"invalid number of bytes for arc4.dynamic_array<arc4.uint8>"},{"pc":[374],"errorMessage":"invalid number of bytes for arc4.static_array<arc4.uint8, 32>"},{"pc":[161],"errorMessage":"invalid number of bytes for arc4.uint64"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"events":[]} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


/**
 * The argument types for the ClaimCap contract
 */
export type ClaimCapArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'buy_nft(uint64)string': {
      amount: bigint | number
    }
    'sell_nft(address)string': {
      buyer: string
    }
    'check_hold_days()uint64': Record<string, never>
    'get_claim_count()uint64': Record<string, never>
    'hello(string)string': {
      name: string
    }
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'buy_nft(uint64)string': [amount: bigint | number]
    'sell_nft(address)string': [buyer: string]
    'check_hold_days()uint64': []
    'get_claim_count()uint64': []
    'hello(string)string': [name: string]
  }
}

/**
 * The return type for each method
 */
export type ClaimCapReturns = {
  'buy_nft(uint64)string': string
  'sell_nft(address)string': string
  'check_hold_days()uint64': bigint
  'get_claim_count()uint64': bigint
  'hello(string)string': string
}

/**
 * Defines the types of available calls and state of the ClaimCap smart contract.
 */
export type ClaimCapTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'buy_nft(uint64)string' | 'buy_nft', {
      argsObj: ClaimCapArgs['obj']['buy_nft(uint64)string']
      argsTuple: ClaimCapArgs['tuple']['buy_nft(uint64)string']
      returns: ClaimCapReturns['buy_nft(uint64)string']
    }>
    & Record<'sell_nft(address)string' | 'sell_nft', {
      argsObj: ClaimCapArgs['obj']['sell_nft(address)string']
      argsTuple: ClaimCapArgs['tuple']['sell_nft(address)string']
      returns: ClaimCapReturns['sell_nft(address)string']
    }>
    & Record<'check_hold_days()uint64' | 'check_hold_days', {
      argsObj: ClaimCapArgs['obj']['check_hold_days()uint64']
      argsTuple: ClaimCapArgs['tuple']['check_hold_days()uint64']
      returns: ClaimCapReturns['check_hold_days()uint64']
    }>
    & Record<'get_claim_count()uint64' | 'get_claim_count', {
      argsObj: ClaimCapArgs['obj']['get_claim_count()uint64']
      argsTuple: ClaimCapArgs['tuple']['get_claim_count()uint64']
      returns: ClaimCapReturns['get_claim_count()uint64']
    }>
    & Record<'hello(string)string' | 'hello', {
      argsObj: ClaimCapArgs['obj']['hello(string)string']
      argsTuple: ClaimCapArgs['tuple']['hello(string)string']
      returns: ClaimCapReturns['hello(string)string']
    }>
  /**
   * Defines the shape of the state of the application.
   */
  state: {
    global: {
      keys: {
        claimCount: bigint
        maxClaims: bigint
        price: bigint
        holdPeriod: bigint
      }
      maps: {}
    }
    box: {
      keys: {}
      maps: {
        owners: Map<string, bigint>
      }
    }
  }
}

/**
 * Defines the possible abi call signatures.
 */
export type ClaimCapSignatures = keyof ClaimCapTypes['methods']
/**
 * Defines the possible abi call signatures for methods that return a non-void value.
 */
export type ClaimCapNonVoidMethodSignatures = keyof ClaimCapTypes['methods'] extends infer T ? T extends keyof ClaimCapTypes['methods'] ? MethodReturn<T> extends void ? never : T  : never : never
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the ClaimCap smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends ClaimCapSignatures> = ClaimCapTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the ClaimCap smart contract to the method's return type
 */
export type MethodReturn<TSignature extends ClaimCapSignatures> = ClaimCapTypes['methods'][TSignature]['returns']

/**
 * Defines the shape of the keyed global state of the application.
 */
export type GlobalKeysState = ClaimCapTypes['state']['global']['keys']

/**
 * Defines the shape of the keyed box state of the application.
 */
export type BoxKeysState = ClaimCapTypes['state']['box']['keys']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the ClaimCap smart contract
 */
export abstract class ClaimCapParamsFactory {
  /**
   * Constructs a no op call for the buy_nft(uint64)string ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static buyNft(params: CallParams<ClaimCapArgs['obj']['buy_nft(uint64)string'] | ClaimCapArgs['tuple']['buy_nft(uint64)string']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'buy_nft(uint64)string' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.amount],
    }
  }
  /**
   * Constructs a no op call for the sell_nft(address)string ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static sellNft(params: CallParams<ClaimCapArgs['obj']['sell_nft(address)string'] | ClaimCapArgs['tuple']['sell_nft(address)string']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'sell_nft(address)string' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.buyer],
    }
  }
  /**
   * Constructs a no op call for the check_hold_days()uint64 ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static checkHoldDays(params: CallParams<ClaimCapArgs['obj']['check_hold_days()uint64'] | ClaimCapArgs['tuple']['check_hold_days()uint64']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'check_hold_days()uint64' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the get_claim_count()uint64 ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static getClaimCount(params: CallParams<ClaimCapArgs['obj']['get_claim_count()uint64'] | ClaimCapArgs['tuple']['get_claim_count()uint64']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'get_claim_count()uint64' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the hello(string)string ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static hello(params: CallParams<ClaimCapArgs['obj']['hello(string)string'] | ClaimCapArgs['tuple']['hello(string)string']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'hello(string)string' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.name],
    }
  }
}

/**
 * A client to make calls to the ClaimCap smart contract
 */
export class ClaimCapClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `ClaimCapClient`
   *
   * @param appClient An `AppClient` instance which has been created with the ClaimCap app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `ClaimCapClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Checks for decode errors on the given return value and maps the return value to the return type for the given method
   * @returns The typed return value or undefined if there was no value
   */
  decodeReturnValue<TSignature extends ClaimCapNonVoidMethodSignatures>(method: TSignature, returnValue: ABIReturn | undefined) {
    return returnValue !== undefined ? getArc56ReturnValue<MethodReturn<TSignature>>(returnValue, this.appClient.getABIMethod(method), APP_SPEC.structs) : undefined
  }

  /**
   * Returns a new `ClaimCapClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<ClaimCapClient> {
    return new ClaimCapClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `ClaimCapClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<ClaimCapClient> {
    return new ClaimCapClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Makes a clear_state call to an existing instance of the ClaimCap smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the ClaimCap smart contract using the `buy_nft(uint64)string` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    buyNft: (params: CallParams<ClaimCapArgs['obj']['buy_nft(uint64)string'] | ClaimCapArgs['tuple']['buy_nft(uint64)string']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(ClaimCapParamsFactory.buyNft(params))
    },

    /**
     * Makes a call to the ClaimCap smart contract using the `sell_nft(address)string` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    sellNft: (params: CallParams<ClaimCapArgs['obj']['sell_nft(address)string'] | ClaimCapArgs['tuple']['sell_nft(address)string']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(ClaimCapParamsFactory.sellNft(params))
    },

    /**
     * Makes a call to the ClaimCap smart contract using the `check_hold_days()uint64` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    checkHoldDays: (params: CallParams<ClaimCapArgs['obj']['check_hold_days()uint64'] | ClaimCapArgs['tuple']['check_hold_days()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(ClaimCapParamsFactory.checkHoldDays(params))
    },

    /**
     * Makes a call to the ClaimCap smart contract using the `get_claim_count()uint64` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    getClaimCount: (params: CallParams<ClaimCapArgs['obj']['get_claim_count()uint64'] | ClaimCapArgs['tuple']['get_claim_count()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(ClaimCapParamsFactory.getClaimCount(params))
    },

    /**
     * Makes a call to the ClaimCap smart contract using the `hello(string)string` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    hello: (params: CallParams<ClaimCapArgs['obj']['hello(string)string'] | ClaimCapArgs['tuple']['hello(string)string']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(ClaimCapParamsFactory.hello(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Makes a clear_state call to an existing instance of the ClaimCap smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the ClaimCap smart contract using the `buy_nft(uint64)string` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    buyNft: (params: CallParams<ClaimCapArgs['obj']['buy_nft(uint64)string'] | ClaimCapArgs['tuple']['buy_nft(uint64)string']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(ClaimCapParamsFactory.buyNft(params))
    },

    /**
     * Makes a call to the ClaimCap smart contract using the `sell_nft(address)string` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    sellNft: (params: CallParams<ClaimCapArgs['obj']['sell_nft(address)string'] | ClaimCapArgs['tuple']['sell_nft(address)string']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(ClaimCapParamsFactory.sellNft(params))
    },

    /**
     * Makes a call to the ClaimCap smart contract using the `check_hold_days()uint64` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    checkHoldDays: (params: CallParams<ClaimCapArgs['obj']['check_hold_days()uint64'] | ClaimCapArgs['tuple']['check_hold_days()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(ClaimCapParamsFactory.checkHoldDays(params))
    },

    /**
     * Makes a call to the ClaimCap smart contract using the `get_claim_count()uint64` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    getClaimCount: (params: CallParams<ClaimCapArgs['obj']['get_claim_count()uint64'] | ClaimCapArgs['tuple']['get_claim_count()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(ClaimCapParamsFactory.getClaimCount(params))
    },

    /**
     * Makes a call to the ClaimCap smart contract using the `hello(string)string` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    hello: (params: CallParams<ClaimCapArgs['obj']['hello(string)string'] | ClaimCapArgs['tuple']['hello(string)string']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(ClaimCapParamsFactory.hello(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Makes a clear_state call to an existing instance of the ClaimCap smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the ClaimCap smart contract using the `buy_nft(uint64)string` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    buyNft: async (params: CallParams<ClaimCapArgs['obj']['buy_nft(uint64)string'] | ClaimCapArgs['tuple']['buy_nft(uint64)string']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(ClaimCapParamsFactory.buyNft(params))
      return {...result, return: result.return as unknown as (undefined | ClaimCapReturns['buy_nft(uint64)string'])}
    },

    /**
     * Makes a call to the ClaimCap smart contract using the `sell_nft(address)string` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    sellNft: async (params: CallParams<ClaimCapArgs['obj']['sell_nft(address)string'] | ClaimCapArgs['tuple']['sell_nft(address)string']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(ClaimCapParamsFactory.sellNft(params))
      return {...result, return: result.return as unknown as (undefined | ClaimCapReturns['sell_nft(address)string'])}
    },

    /**
     * Makes a call to the ClaimCap smart contract using the `check_hold_days()uint64` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    checkHoldDays: async (params: CallParams<ClaimCapArgs['obj']['check_hold_days()uint64'] | ClaimCapArgs['tuple']['check_hold_days()uint64']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(ClaimCapParamsFactory.checkHoldDays(params))
      return {...result, return: result.return as unknown as (undefined | ClaimCapReturns['check_hold_days()uint64'])}
    },

    /**
     * Makes a call to the ClaimCap smart contract using the `get_claim_count()uint64` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    getClaimCount: async (params: CallParams<ClaimCapArgs['obj']['get_claim_count()uint64'] | ClaimCapArgs['tuple']['get_claim_count()uint64']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(ClaimCapParamsFactory.getClaimCount(params))
      return {...result, return: result.return as unknown as (undefined | ClaimCapReturns['get_claim_count()uint64'])}
    },

    /**
     * Makes a call to the ClaimCap smart contract using the `hello(string)string` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    hello: async (params: CallParams<ClaimCapArgs['obj']['hello(string)string'] | ClaimCapArgs['tuple']['hello(string)string']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(ClaimCapParamsFactory.hello(params))
      return {...result, return: result.return as unknown as (undefined | ClaimCapReturns['hello(string)string'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new ClaimCapClient(this.appClient.clone(params))
  }

  /**
   * Methods to access state for the current ClaimCap app
   */
  state = {
    /**
     * Methods to access global state for the current ClaimCap app
     */
    global: {
      /**
       * Get all current keyed values from global state
       */
      getAll: async (): Promise<Partial<Expand<GlobalKeysState>>> => {
        const result = await this.appClient.state.global.getAll()
        return {
          claimCount: result.claim_count,
          maxClaims: result.MAX_CLAIMS,
          price: result.PRICE,
          holdPeriod: result.HOLD_PERIOD,
        }
      },
      /**
       * Get the current value of the claim_count key in global state
       */
      claimCount: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("claim_count")) as bigint | undefined },
      /**
       * Get the current value of the MAX_CLAIMS key in global state
       */
      maxClaims: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("MAX_CLAIMS")) as bigint | undefined },
      /**
       * Get the current value of the PRICE key in global state
       */
      price: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("PRICE")) as bigint | undefined },
      /**
       * Get the current value of the HOLD_PERIOD key in global state
       */
      holdPeriod: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("HOLD_PERIOD")) as bigint | undefined },
    },
    /**
     * Methods to access box state for the current ClaimCap app
     */
    box: {
      /**
       * Get all current keyed values from box state
       */
      getAll: async (): Promise<Partial<Expand<BoxKeysState>>> => {
        const result = await this.appClient.state.box.getAll()
        return {
        }
      },
      /**
       * Get values from the owners map in box state
       */
      owners: {
        /**
         * Get all current values of the owners map in box state
         */
        getMap: async (): Promise<Map<string, bigint>> => { return (await this.appClient.state.box.getMap("owners")) as Map<string, bigint> },
        /**
         * Get a current value of the owners map by key from box state
         */
        value: async (key: string): Promise<bigint | undefined> => { return await this.appClient.state.box.getMapValue("owners", key) as bigint | undefined },
      },
    },
  }

  public newGroup(): ClaimCapComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a buy_nft(uint64)string method call against the ClaimCap contract
       */
      buyNft(params: CallParams<ClaimCapArgs['obj']['buy_nft(uint64)string'] | ClaimCapArgs['tuple']['buy_nft(uint64)string']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.buyNft(params)))
        resultMappers.push((v) => client.decodeReturnValue('buy_nft(uint64)string', v))
        return this
      },
      /**
       * Add a sell_nft(address)string method call against the ClaimCap contract
       */
      sellNft(params: CallParams<ClaimCapArgs['obj']['sell_nft(address)string'] | ClaimCapArgs['tuple']['sell_nft(address)string']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.sellNft(params)))
        resultMappers.push((v) => client.decodeReturnValue('sell_nft(address)string', v))
        return this
      },
      /**
       * Add a check_hold_days()uint64 method call against the ClaimCap contract
       */
      checkHoldDays(params: CallParams<ClaimCapArgs['obj']['check_hold_days()uint64'] | ClaimCapArgs['tuple']['check_hold_days()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.checkHoldDays(params)))
        resultMappers.push((v) => client.decodeReturnValue('check_hold_days()uint64', v))
        return this
      },
      /**
       * Add a get_claim_count()uint64 method call against the ClaimCap contract
       */
      getClaimCount(params: CallParams<ClaimCapArgs['obj']['get_claim_count()uint64'] | ClaimCapArgs['tuple']['get_claim_count()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.getClaimCount(params)))
        resultMappers.push((v) => client.decodeReturnValue('get_claim_count()uint64', v))
        return this
      },
      /**
       * Add a hello(string)string method call against the ClaimCap contract
       */
      hello(params: CallParams<ClaimCapArgs['obj']['hello(string)string'] | ClaimCapArgs['tuple']['hello(string)string']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.hello(params)))
        resultMappers.push((v) => client.decodeReturnValue('hello(string)string', v))
        return this
      },
      /**
       * Add a clear state call to the ClaimCap contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as ClaimCapComposer
  }
}
export type ClaimCapComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the buy_nft(uint64)string ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  buyNft(params?: CallParams<ClaimCapArgs['obj']['buy_nft(uint64)string'] | ClaimCapArgs['tuple']['buy_nft(uint64)string']>): ClaimCapComposer<[...TReturns, ClaimCapReturns['buy_nft(uint64)string'] | undefined]>

  /**
   * Calls the sell_nft(address)string ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  sellNft(params?: CallParams<ClaimCapArgs['obj']['sell_nft(address)string'] | ClaimCapArgs['tuple']['sell_nft(address)string']>): ClaimCapComposer<[...TReturns, ClaimCapReturns['sell_nft(address)string'] | undefined]>

  /**
   * Calls the check_hold_days()uint64 ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  checkHoldDays(params?: CallParams<ClaimCapArgs['obj']['check_hold_days()uint64'] | ClaimCapArgs['tuple']['check_hold_days()uint64']>): ClaimCapComposer<[...TReturns, ClaimCapReturns['check_hold_days()uint64'] | undefined]>

  /**
   * Calls the get_claim_count()uint64 ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  getClaimCount(params?: CallParams<ClaimCapArgs['obj']['get_claim_count()uint64'] | ClaimCapArgs['tuple']['get_claim_count()uint64']>): ClaimCapComposer<[...TReturns, ClaimCapReturns['get_claim_count()uint64'] | undefined]>

  /**
   * Calls the hello(string)string ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  hello(params?: CallParams<ClaimCapArgs['obj']['hello(string)string'] | ClaimCapArgs['tuple']['hello(string)string']>): ClaimCapComposer<[...TReturns, ClaimCapReturns['hello(string)string'] | undefined]>

  /**
   * Makes a clear_state call to an existing instance of the ClaimCap smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): ClaimCapComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): ClaimCapComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<ClaimCapComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<ClaimCapComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<ClaimCapComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<ClaimCapComposerResults<TReturns>>
}
export type ClaimCapComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

