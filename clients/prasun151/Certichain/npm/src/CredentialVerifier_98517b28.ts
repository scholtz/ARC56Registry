/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"CredentialVerifier","structs":{},"methods":[{"name":"create","args":[{"type":"address","name":"institution"}],"returns":{"type":"void"},"actions":{"create":["NoOp"],"call":[]},"readonly":false,"desc":"Initialize the contract with the authorized institution","events":[],"recommendations":{}},{"name":"issue_credential","args":[{"type":"address","name":"student_address"},{"type":"string","name":"credential_name"},{"type":"string","name":"metadata_url"}],"returns":{"type":"uint64"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Issue a credential to a student.\nMints an NFT and returns the Asset ID. Only the authorized institution can call this.","events":[],"recommendations":{}},{"name":"verify_credential","args":[{"type":"uint64","name":"asset_id"}],"returns":{"type":"string"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"desc":"Verify if a credential is valid (dummy for now)","events":[],"recommendations":{}},{"name":"get_contract_info","args":[],"returns":{"type":"string"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"desc":"Get contract information","events":[],"recommendations":{}}],"arcs":[22,28],"desc":"Algorand Credential Verifier Smart Contract","networks":{},"state":{"schema":{"global":{"ints":0,"bytes":1},"local":{"ints":0,"bytes":0}},"keys":{"global":{"authorized_institution":{"keyType":"AVMString","valueType":"address","key":"YXV0aG9yaXplZF9pbnN0aXR1dGlvbg=="}},"local":{},"box":{}},"maps":{"global":{},"local":{},"box":{}}},"bareActions":{"create":[],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[202],"errorMessage":"Only the authorized institution can issue credentials"},{"pc":[200],"errorMessage":"check self.authorized_institution exists"},{"pc":[168,184],"errorMessage":"invalid array length header"},{"pc":[175,191],"errorMessage":"invalid number of bytes for arc4.dynamic_array<arc4.uint8>"},{"pc":[149,162],"errorMessage":"invalid number of bytes for arc4.static_array<arc4.uint8, 32>"},{"pc":[259],"errorMessage":"invalid number of bytes for arc4.uint64"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"events":[]} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


/**
 * The argument types for the CredentialVerifier contract
 */
export type CredentialVerifierArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'create(address)void': {
      institution: string
    }
    'issue_credential(address,string,string)uint64': {
      studentAddress: string
      credentialName: string
      metadataUrl: string
    }
    'verify_credential(uint64)string': {
      assetId: bigint | number
    }
    'get_contract_info()string': Record<string, never>
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'create(address)void': [institution: string]
    'issue_credential(address,string,string)uint64': [studentAddress: string, credentialName: string, metadataUrl: string]
    'verify_credential(uint64)string': [assetId: bigint | number]
    'get_contract_info()string': []
  }
}

/**
 * The return type for each method
 */
export type CredentialVerifierReturns = {
  'create(address)void': void
  'issue_credential(address,string,string)uint64': bigint
  'verify_credential(uint64)string': string
  'get_contract_info()string': string
}

/**
 * Defines the types of available calls and state of the CredentialVerifier smart contract.
 */
export type CredentialVerifierTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'create(address)void' | 'create', {
      argsObj: CredentialVerifierArgs['obj']['create(address)void']
      argsTuple: CredentialVerifierArgs['tuple']['create(address)void']
      returns: CredentialVerifierReturns['create(address)void']
    }>
    & Record<'issue_credential(address,string,string)uint64' | 'issue_credential', {
      argsObj: CredentialVerifierArgs['obj']['issue_credential(address,string,string)uint64']
      argsTuple: CredentialVerifierArgs['tuple']['issue_credential(address,string,string)uint64']
      returns: CredentialVerifierReturns['issue_credential(address,string,string)uint64']
    }>
    & Record<'verify_credential(uint64)string' | 'verify_credential', {
      argsObj: CredentialVerifierArgs['obj']['verify_credential(uint64)string']
      argsTuple: CredentialVerifierArgs['tuple']['verify_credential(uint64)string']
      returns: CredentialVerifierReturns['verify_credential(uint64)string']
    }>
    & Record<'get_contract_info()string' | 'get_contract_info', {
      argsObj: CredentialVerifierArgs['obj']['get_contract_info()string']
      argsTuple: CredentialVerifierArgs['tuple']['get_contract_info()string']
      returns: CredentialVerifierReturns['get_contract_info()string']
    }>
  /**
   * Defines the shape of the state of the application.
   */
  state: {
    global: {
      keys: {
        authorizedInstitution: string
      }
      maps: {}
    }
  }
}

/**
 * Defines the possible abi call signatures.
 */
export type CredentialVerifierSignatures = keyof CredentialVerifierTypes['methods']
/**
 * Defines the possible abi call signatures for methods that return a non-void value.
 */
export type CredentialVerifierNonVoidMethodSignatures = keyof CredentialVerifierTypes['methods'] extends infer T ? T extends keyof CredentialVerifierTypes['methods'] ? MethodReturn<T> extends void ? never : T  : never : never
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the CredentialVerifier smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends CredentialVerifierSignatures> = CredentialVerifierTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the CredentialVerifier smart contract to the method's return type
 */
export type MethodReturn<TSignature extends CredentialVerifierSignatures> = CredentialVerifierTypes['methods'][TSignature]['returns']

/**
 * Defines the shape of the keyed global state of the application.
 */
export type GlobalKeysState = CredentialVerifierTypes['state']['global']['keys']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the CredentialVerifier smart contract
 */
export abstract class CredentialVerifierParamsFactory {
  /**
   * Constructs a no op call for the issue_credential(address,string,string)uint64 ABI method
   *
  * Issue a credential to a student.
  Mints an NFT and returns the Asset ID. Only the authorized institution can call this.

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static issueCredential(params: CallParams<CredentialVerifierArgs['obj']['issue_credential(address,string,string)uint64'] | CredentialVerifierArgs['tuple']['issue_credential(address,string,string)uint64']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'issue_credential(address,string,string)uint64' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.studentAddress, params.args.credentialName, params.args.metadataUrl],
    }
  }
  /**
   * Constructs a no op call for the verify_credential(uint64)string ABI method
   *
   * Verify if a credential is valid (dummy for now)
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static verifyCredential(params: CallParams<CredentialVerifierArgs['obj']['verify_credential(uint64)string'] | CredentialVerifierArgs['tuple']['verify_credential(uint64)string']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'verify_credential(uint64)string' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.assetId],
    }
  }
  /**
   * Constructs a no op call for the get_contract_info()string ABI method
   *
   * Get contract information
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static getContractInfo(params: CallParams<CredentialVerifierArgs['obj']['get_contract_info()string'] | CredentialVerifierArgs['tuple']['get_contract_info()string']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'get_contract_info()string' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
}

/**
 * A client to make calls to the CredentialVerifier smart contract
 */
export class CredentialVerifierClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `CredentialVerifierClient`
   *
   * @param appClient An `AppClient` instance which has been created with the CredentialVerifier app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `CredentialVerifierClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Checks for decode errors on the given return value and maps the return value to the return type for the given method
   * @returns The typed return value or undefined if there was no value
   */
  decodeReturnValue<TSignature extends CredentialVerifierNonVoidMethodSignatures>(method: TSignature, returnValue: ABIReturn | undefined) {
    return returnValue !== undefined ? getArc56ReturnValue<MethodReturn<TSignature>>(returnValue, this.appClient.getABIMethod(method), APP_SPEC.structs) : undefined
  }

  /**
   * Returns a new `CredentialVerifierClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<CredentialVerifierClient> {
    return new CredentialVerifierClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `CredentialVerifierClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<CredentialVerifierClient> {
    return new CredentialVerifierClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Makes a clear_state call to an existing instance of the CredentialVerifier smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the CredentialVerifier smart contract using the `issue_credential(address,string,string)uint64` ABI method.
     *
    * Issue a credential to a student.
    Mints an NFT and returns the Asset ID. Only the authorized institution can call this.

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    issueCredential: (params: CallParams<CredentialVerifierArgs['obj']['issue_credential(address,string,string)uint64'] | CredentialVerifierArgs['tuple']['issue_credential(address,string,string)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(CredentialVerifierParamsFactory.issueCredential(params))
    },

    /**
     * Makes a call to the CredentialVerifier smart contract using the `verify_credential(uint64)string` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * Verify if a credential is valid (dummy for now)
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    verifyCredential: (params: CallParams<CredentialVerifierArgs['obj']['verify_credential(uint64)string'] | CredentialVerifierArgs['tuple']['verify_credential(uint64)string']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(CredentialVerifierParamsFactory.verifyCredential(params))
    },

    /**
     * Makes a call to the CredentialVerifier smart contract using the `get_contract_info()string` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * Get contract information
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    getContractInfo: (params: CallParams<CredentialVerifierArgs['obj']['get_contract_info()string'] | CredentialVerifierArgs['tuple']['get_contract_info()string']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(CredentialVerifierParamsFactory.getContractInfo(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Makes a clear_state call to an existing instance of the CredentialVerifier smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the CredentialVerifier smart contract using the `issue_credential(address,string,string)uint64` ABI method.
     *
    * Issue a credential to a student.
    Mints an NFT and returns the Asset ID. Only the authorized institution can call this.

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    issueCredential: (params: CallParams<CredentialVerifierArgs['obj']['issue_credential(address,string,string)uint64'] | CredentialVerifierArgs['tuple']['issue_credential(address,string,string)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(CredentialVerifierParamsFactory.issueCredential(params))
    },

    /**
     * Makes a call to the CredentialVerifier smart contract using the `verify_credential(uint64)string` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * Verify if a credential is valid (dummy for now)
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    verifyCredential: (params: CallParams<CredentialVerifierArgs['obj']['verify_credential(uint64)string'] | CredentialVerifierArgs['tuple']['verify_credential(uint64)string']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(CredentialVerifierParamsFactory.verifyCredential(params))
    },

    /**
     * Makes a call to the CredentialVerifier smart contract using the `get_contract_info()string` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * Get contract information
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    getContractInfo: (params: CallParams<CredentialVerifierArgs['obj']['get_contract_info()string'] | CredentialVerifierArgs['tuple']['get_contract_info()string']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(CredentialVerifierParamsFactory.getContractInfo(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Makes a clear_state call to an existing instance of the CredentialVerifier smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the CredentialVerifier smart contract using the `issue_credential(address,string,string)uint64` ABI method.
     *
    * Issue a credential to a student.
    Mints an NFT and returns the Asset ID. Only the authorized institution can call this.

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    issueCredential: async (params: CallParams<CredentialVerifierArgs['obj']['issue_credential(address,string,string)uint64'] | CredentialVerifierArgs['tuple']['issue_credential(address,string,string)uint64']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(CredentialVerifierParamsFactory.issueCredential(params))
      return {...result, return: result.return as unknown as (undefined | CredentialVerifierReturns['issue_credential(address,string,string)uint64'])}
    },

    /**
     * Makes a call to the CredentialVerifier smart contract using the `verify_credential(uint64)string` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * Verify if a credential is valid (dummy for now)
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    verifyCredential: async (params: CallParams<CredentialVerifierArgs['obj']['verify_credential(uint64)string'] | CredentialVerifierArgs['tuple']['verify_credential(uint64)string']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(CredentialVerifierParamsFactory.verifyCredential(params))
      return {...result, return: result.return as unknown as (undefined | CredentialVerifierReturns['verify_credential(uint64)string'])}
    },

    /**
     * Makes a call to the CredentialVerifier smart contract using the `get_contract_info()string` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * Get contract information
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    getContractInfo: async (params: CallParams<CredentialVerifierArgs['obj']['get_contract_info()string'] | CredentialVerifierArgs['tuple']['get_contract_info()string']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(CredentialVerifierParamsFactory.getContractInfo(params))
      return {...result, return: result.return as unknown as (undefined | CredentialVerifierReturns['get_contract_info()string'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new CredentialVerifierClient(this.appClient.clone(params))
  }

  /**
   * Makes a readonly (simulated) call to the CredentialVerifier smart contract using the `verify_credential(uint64)string` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * Verify if a credential is valid (dummy for now)
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async verifyCredential(params: CallParams<CredentialVerifierArgs['obj']['verify_credential(uint64)string'] | CredentialVerifierArgs['tuple']['verify_credential(uint64)string']>) {
    const result = await this.appClient.send.call(CredentialVerifierParamsFactory.verifyCredential(params))
    return result.return as unknown as CredentialVerifierReturns['verify_credential(uint64)string']
  }

  /**
   * Makes a readonly (simulated) call to the CredentialVerifier smart contract using the `get_contract_info()string` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * Get contract information
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async getContractInfo(params: CallParams<CredentialVerifierArgs['obj']['get_contract_info()string'] | CredentialVerifierArgs['tuple']['get_contract_info()string']> = {args: []}) {
    const result = await this.appClient.send.call(CredentialVerifierParamsFactory.getContractInfo(params))
    return result.return as unknown as CredentialVerifierReturns['get_contract_info()string']
  }

  /**
   * Methods to access state for the current CredentialVerifier app
   */
  state = {
    /**
     * Methods to access global state for the current CredentialVerifier app
     */
    global: {
      /**
       * Get all current keyed values from global state
       */
      getAll: async (): Promise<Partial<Expand<GlobalKeysState>>> => {
        const result = await this.appClient.state.global.getAll()
        return {
          authorizedInstitution: result.authorized_institution,
        }
      },
      /**
       * Get the current value of the authorized_institution key in global state
       */
      authorizedInstitution: async (): Promise<string | undefined> => { return (await this.appClient.state.global.getValue("authorized_institution")) as string | undefined },
    },
  }

  public newGroup(): CredentialVerifierComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a issue_credential(address,string,string)uint64 method call against the CredentialVerifier contract
       */
      issueCredential(params: CallParams<CredentialVerifierArgs['obj']['issue_credential(address,string,string)uint64'] | CredentialVerifierArgs['tuple']['issue_credential(address,string,string)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.issueCredential(params)))
        resultMappers.push((v) => client.decodeReturnValue('issue_credential(address,string,string)uint64', v))
        return this
      },
      /**
       * Add a verify_credential(uint64)string method call against the CredentialVerifier contract
       */
      verifyCredential(params: CallParams<CredentialVerifierArgs['obj']['verify_credential(uint64)string'] | CredentialVerifierArgs['tuple']['verify_credential(uint64)string']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.verifyCredential(params)))
        resultMappers.push((v) => client.decodeReturnValue('verify_credential(uint64)string', v))
        return this
      },
      /**
       * Add a get_contract_info()string method call against the CredentialVerifier contract
       */
      getContractInfo(params: CallParams<CredentialVerifierArgs['obj']['get_contract_info()string'] | CredentialVerifierArgs['tuple']['get_contract_info()string']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.getContractInfo(params)))
        resultMappers.push((v) => client.decodeReturnValue('get_contract_info()string', v))
        return this
      },
      /**
       * Add a clear state call to the CredentialVerifier contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as CredentialVerifierComposer
  }
}
export type CredentialVerifierComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the issue_credential(address,string,string)uint64 ABI method.
   *
  * Issue a credential to a student.
  Mints an NFT and returns the Asset ID. Only the authorized institution can call this.

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  issueCredential(params?: CallParams<CredentialVerifierArgs['obj']['issue_credential(address,string,string)uint64'] | CredentialVerifierArgs['tuple']['issue_credential(address,string,string)uint64']>): CredentialVerifierComposer<[...TReturns, CredentialVerifierReturns['issue_credential(address,string,string)uint64'] | undefined]>

  /**
   * Calls the verify_credential(uint64)string ABI method.
   *
   * Verify if a credential is valid (dummy for now)
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  verifyCredential(params?: CallParams<CredentialVerifierArgs['obj']['verify_credential(uint64)string'] | CredentialVerifierArgs['tuple']['verify_credential(uint64)string']>): CredentialVerifierComposer<[...TReturns, CredentialVerifierReturns['verify_credential(uint64)string'] | undefined]>

  /**
   * Calls the get_contract_info()string ABI method.
   *
   * Get contract information
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  getContractInfo(params?: CallParams<CredentialVerifierArgs['obj']['get_contract_info()string'] | CredentialVerifierArgs['tuple']['get_contract_info()string']>): CredentialVerifierComposer<[...TReturns, CredentialVerifierReturns['get_contract_info()string'] | undefined]>

  /**
   * Makes a clear_state call to an existing instance of the CredentialVerifier smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): CredentialVerifierComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): CredentialVerifierComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<CredentialVerifierComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<CredentialVerifierComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<CredentialVerifierComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<CredentialVerifierComposerResults<TReturns>>
}
export type CredentialVerifierComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

