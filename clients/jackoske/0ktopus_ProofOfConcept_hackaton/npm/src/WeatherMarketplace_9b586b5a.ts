/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"WeatherMarketplace","structs":{},"methods":[{"name":"get_token_price","args":[],"returns":{"type":"uint64","desc":"Token price in microAlgos"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Get the current token price.","events":[],"recommendations":{}},{"name":"get_weather_asa_id","args":[],"returns":{"type":"uint64","desc":"The weather token ASA ID"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Get the weather ASA ID.","events":[],"recommendations":{}},{"name":"set_weather_asa_id","args":[{"type":"uint64","name":"asa_id","desc":"The ASA ID to set"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Set the weather ASA ID (admin only for demo).","events":[],"recommendations":{}},{"name":"get_token_duration","args":[],"returns":{"type":"uint64","desc":"Duration in seconds"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Get the token validity duration.","events":[],"recommendations":{}},{"name":"record_token_sale","args":[],"returns":{"type":"uint64","desc":"Updated total sales count"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Record a token sale (simplified for demo).","events":[],"recommendations":{}},{"name":"get_total_sales","args":[],"returns":{"type":"uint64","desc":"Total number of tokens sold"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Get total token sales count.","events":[],"recommendations":{}},{"name":"is_contract_active","args":[],"returns":{"type":"bool","desc":"True if contract is active"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Check if the contract is active.","events":[],"recommendations":{}},{"name":"get_contract_info","args":[],"returns":{"type":"string","desc":"JSON string with contract info"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Get basic contract information as JSON string.","events":[],"recommendations":{}}],"arcs":[22,28],"desc":"\n    Simplified smart contract for tokenized weather API access demo.\n    \n    This contract manages basic state for weather access tokens.\n    For the MVP demo, we'll use a simplified approach.\n    ","networks":{},"state":{"schema":{"global":{"ints":4,"bytes":1},"local":{"ints":0,"bytes":0}},"keys":{"global":{"token_price":{"keyType":"AVMString","valueType":"AVMUint64","key":"dG9rZW5fcHJpY2U="},"weather_asa_id":{"keyType":"AVMString","valueType":"AVMUint64","key":"d2VhdGhlcl9hc2FfaWQ="},"token_duration":{"keyType":"AVMString","valueType":"AVMUint64","key":"dG9rZW5fZHVyYXRpb24="},"total_tokens_sold":{"keyType":"AVMString","valueType":"AVMUint64","key":"dG90YWxfdG9rZW5zX3NvbGQ="},"is_active":{"keyType":"AVMString","valueType":"bool","key":"aXNfYWN0aXZl"}},"local":{},"box":{}},"maps":{"global":{},"local":{},"box":{}}},"bareActions":{"create":["NoOp"],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[185,257,273,289,305,321,336,352],"errorMessage":"OnCompletion is not NoOp"},{"pc":[373],"errorMessage":"can only call when creating"},{"pc":[188,260,276,292,308,324,339,355],"errorMessage":"can only call when not creating"},{"pc":[427],"errorMessage":"check self.is_active exists"},{"pc":[402],"errorMessage":"check self.token_duration exists"},{"pc":[379],"errorMessage":"check self.token_price exists"},{"pc":[408,420],"errorMessage":"check self.total_tokens_sold exists"},{"pc":[385],"errorMessage":"check self.weather_asa_id exists"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"events":[]} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


/**
 * The argument types for the WeatherMarketplace contract
 */
export type WeatherMarketplaceArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'get_token_price()uint64': Record<string, never>
    'get_weather_asa_id()uint64': Record<string, never>
    'set_weather_asa_id(uint64)void': {
      /**
       * The ASA ID to set
       */
      asaId: bigint | number
    }
    'get_token_duration()uint64': Record<string, never>
    'record_token_sale()uint64': Record<string, never>
    'get_total_sales()uint64': Record<string, never>
    'is_contract_active()bool': Record<string, never>
    'get_contract_info()string': Record<string, never>
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'get_token_price()uint64': []
    'get_weather_asa_id()uint64': []
    'set_weather_asa_id(uint64)void': [asaId: bigint | number]
    'get_token_duration()uint64': []
    'record_token_sale()uint64': []
    'get_total_sales()uint64': []
    'is_contract_active()bool': []
    'get_contract_info()string': []
  }
}

/**
 * The return type for each method
 */
export type WeatherMarketplaceReturns = {
  'get_token_price()uint64': bigint
  'get_weather_asa_id()uint64': bigint
  'set_weather_asa_id(uint64)void': void
  'get_token_duration()uint64': bigint
  'record_token_sale()uint64': bigint
  'get_total_sales()uint64': bigint
  'is_contract_active()bool': boolean
  'get_contract_info()string': string
}

/**
 * Defines the types of available calls and state of the WeatherMarketplace smart contract.
 */
export type WeatherMarketplaceTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'get_token_price()uint64' | 'get_token_price', {
      argsObj: WeatherMarketplaceArgs['obj']['get_token_price()uint64']
      argsTuple: WeatherMarketplaceArgs['tuple']['get_token_price()uint64']
      /**
       * Token price in microAlgos
       */
      returns: WeatherMarketplaceReturns['get_token_price()uint64']
    }>
    & Record<'get_weather_asa_id()uint64' | 'get_weather_asa_id', {
      argsObj: WeatherMarketplaceArgs['obj']['get_weather_asa_id()uint64']
      argsTuple: WeatherMarketplaceArgs['tuple']['get_weather_asa_id()uint64']
      /**
       * The weather token ASA ID
       */
      returns: WeatherMarketplaceReturns['get_weather_asa_id()uint64']
    }>
    & Record<'set_weather_asa_id(uint64)void' | 'set_weather_asa_id', {
      argsObj: WeatherMarketplaceArgs['obj']['set_weather_asa_id(uint64)void']
      argsTuple: WeatherMarketplaceArgs['tuple']['set_weather_asa_id(uint64)void']
      returns: WeatherMarketplaceReturns['set_weather_asa_id(uint64)void']
    }>
    & Record<'get_token_duration()uint64' | 'get_token_duration', {
      argsObj: WeatherMarketplaceArgs['obj']['get_token_duration()uint64']
      argsTuple: WeatherMarketplaceArgs['tuple']['get_token_duration()uint64']
      /**
       * Duration in seconds
       */
      returns: WeatherMarketplaceReturns['get_token_duration()uint64']
    }>
    & Record<'record_token_sale()uint64' | 'record_token_sale', {
      argsObj: WeatherMarketplaceArgs['obj']['record_token_sale()uint64']
      argsTuple: WeatherMarketplaceArgs['tuple']['record_token_sale()uint64']
      /**
       * Updated total sales count
       */
      returns: WeatherMarketplaceReturns['record_token_sale()uint64']
    }>
    & Record<'get_total_sales()uint64' | 'get_total_sales', {
      argsObj: WeatherMarketplaceArgs['obj']['get_total_sales()uint64']
      argsTuple: WeatherMarketplaceArgs['tuple']['get_total_sales()uint64']
      /**
       * Total number of tokens sold
       */
      returns: WeatherMarketplaceReturns['get_total_sales()uint64']
    }>
    & Record<'is_contract_active()bool' | 'is_contract_active', {
      argsObj: WeatherMarketplaceArgs['obj']['is_contract_active()bool']
      argsTuple: WeatherMarketplaceArgs['tuple']['is_contract_active()bool']
      /**
       * True if contract is active
       */
      returns: WeatherMarketplaceReturns['is_contract_active()bool']
    }>
    & Record<'get_contract_info()string' | 'get_contract_info', {
      argsObj: WeatherMarketplaceArgs['obj']['get_contract_info()string']
      argsTuple: WeatherMarketplaceArgs['tuple']['get_contract_info()string']
      /**
       * JSON string with contract info
       */
      returns: WeatherMarketplaceReturns['get_contract_info()string']
    }>
  /**
   * Defines the shape of the state of the application.
   */
  state: {
    global: {
      keys: {
        tokenPrice: bigint
        weatherAsaId: bigint
        tokenDuration: bigint
        totalTokensSold: bigint
        isActive: boolean
      }
      maps: {}
    }
  }
}

/**
 * Defines the possible abi call signatures.
 */
export type WeatherMarketplaceSignatures = keyof WeatherMarketplaceTypes['methods']
/**
 * Defines the possible abi call signatures for methods that return a non-void value.
 */
export type WeatherMarketplaceNonVoidMethodSignatures = keyof WeatherMarketplaceTypes['methods'] extends infer T ? T extends keyof WeatherMarketplaceTypes['methods'] ? MethodReturn<T> extends void ? never : T  : never : never
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the WeatherMarketplace smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends WeatherMarketplaceSignatures> = WeatherMarketplaceTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the WeatherMarketplace smart contract to the method's return type
 */
export type MethodReturn<TSignature extends WeatherMarketplaceSignatures> = WeatherMarketplaceTypes['methods'][TSignature]['returns']

/**
 * Defines the shape of the keyed global state of the application.
 */
export type GlobalKeysState = WeatherMarketplaceTypes['state']['global']['keys']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the WeatherMarketplace smart contract
 */
export abstract class WeatherMarketplaceParamsFactory {
  /**
   * Constructs a no op call for the get_token_price()uint64 ABI method
   *
   * Get the current token price.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static getTokenPrice(params: CallParams<WeatherMarketplaceArgs['obj']['get_token_price()uint64'] | WeatherMarketplaceArgs['tuple']['get_token_price()uint64']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'get_token_price()uint64' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the get_weather_asa_id()uint64 ABI method
   *
   * Get the weather ASA ID.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static getWeatherAsaId(params: CallParams<WeatherMarketplaceArgs['obj']['get_weather_asa_id()uint64'] | WeatherMarketplaceArgs['tuple']['get_weather_asa_id()uint64']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'get_weather_asa_id()uint64' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the set_weather_asa_id(uint64)void ABI method
   *
   * Set the weather ASA ID (admin only for demo).
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static setWeatherAsaId(params: CallParams<WeatherMarketplaceArgs['obj']['set_weather_asa_id(uint64)void'] | WeatherMarketplaceArgs['tuple']['set_weather_asa_id(uint64)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'set_weather_asa_id(uint64)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.asaId],
    }
  }
  /**
   * Constructs a no op call for the get_token_duration()uint64 ABI method
   *
   * Get the token validity duration.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static getTokenDuration(params: CallParams<WeatherMarketplaceArgs['obj']['get_token_duration()uint64'] | WeatherMarketplaceArgs['tuple']['get_token_duration()uint64']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'get_token_duration()uint64' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the record_token_sale()uint64 ABI method
   *
   * Record a token sale (simplified for demo).
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static recordTokenSale(params: CallParams<WeatherMarketplaceArgs['obj']['record_token_sale()uint64'] | WeatherMarketplaceArgs['tuple']['record_token_sale()uint64']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'record_token_sale()uint64' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the get_total_sales()uint64 ABI method
   *
   * Get total token sales count.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static getTotalSales(params: CallParams<WeatherMarketplaceArgs['obj']['get_total_sales()uint64'] | WeatherMarketplaceArgs['tuple']['get_total_sales()uint64']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'get_total_sales()uint64' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the is_contract_active()bool ABI method
   *
   * Check if the contract is active.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static isContractActive(params: CallParams<WeatherMarketplaceArgs['obj']['is_contract_active()bool'] | WeatherMarketplaceArgs['tuple']['is_contract_active()bool']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'is_contract_active()bool' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the get_contract_info()string ABI method
   *
   * Get basic contract information as JSON string.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static getContractInfo(params: CallParams<WeatherMarketplaceArgs['obj']['get_contract_info()string'] | WeatherMarketplaceArgs['tuple']['get_contract_info()string']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'get_contract_info()string' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
}

/**
 * A client to make calls to the WeatherMarketplace smart contract
 */
export class WeatherMarketplaceClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `WeatherMarketplaceClient`
   *
   * @param appClient An `AppClient` instance which has been created with the WeatherMarketplace app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `WeatherMarketplaceClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Checks for decode errors on the given return value and maps the return value to the return type for the given method
   * @returns The typed return value or undefined if there was no value
   */
  decodeReturnValue<TSignature extends WeatherMarketplaceNonVoidMethodSignatures>(method: TSignature, returnValue: ABIReturn | undefined) {
    return returnValue !== undefined ? getArc56ReturnValue<MethodReturn<TSignature>>(returnValue, this.appClient.getABIMethod(method), APP_SPEC.structs) : undefined
  }

  /**
   * Returns a new `WeatherMarketplaceClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<WeatherMarketplaceClient> {
    return new WeatherMarketplaceClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `WeatherMarketplaceClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<WeatherMarketplaceClient> {
    return new WeatherMarketplaceClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Makes a clear_state call to an existing instance of the WeatherMarketplace smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the WeatherMarketplace smart contract using the `get_token_price()uint64` ABI method.
     *
     * Get the current token price.
     *
     * @param params The params for the smart contract call
     * @returns The call params: Token price in microAlgos
     */
    getTokenPrice: (params: CallParams<WeatherMarketplaceArgs['obj']['get_token_price()uint64'] | WeatherMarketplaceArgs['tuple']['get_token_price()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(WeatherMarketplaceParamsFactory.getTokenPrice(params))
    },

    /**
     * Makes a call to the WeatherMarketplace smart contract using the `get_weather_asa_id()uint64` ABI method.
     *
     * Get the weather ASA ID.
     *
     * @param params The params for the smart contract call
     * @returns The call params: The weather token ASA ID
     */
    getWeatherAsaId: (params: CallParams<WeatherMarketplaceArgs['obj']['get_weather_asa_id()uint64'] | WeatherMarketplaceArgs['tuple']['get_weather_asa_id()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(WeatherMarketplaceParamsFactory.getWeatherAsaId(params))
    },

    /**
     * Makes a call to the WeatherMarketplace smart contract using the `set_weather_asa_id(uint64)void` ABI method.
     *
     * Set the weather ASA ID (admin only for demo).
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    setWeatherAsaId: (params: CallParams<WeatherMarketplaceArgs['obj']['set_weather_asa_id(uint64)void'] | WeatherMarketplaceArgs['tuple']['set_weather_asa_id(uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(WeatherMarketplaceParamsFactory.setWeatherAsaId(params))
    },

    /**
     * Makes a call to the WeatherMarketplace smart contract using the `get_token_duration()uint64` ABI method.
     *
     * Get the token validity duration.
     *
     * @param params The params for the smart contract call
     * @returns The call params: Duration in seconds
     */
    getTokenDuration: (params: CallParams<WeatherMarketplaceArgs['obj']['get_token_duration()uint64'] | WeatherMarketplaceArgs['tuple']['get_token_duration()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(WeatherMarketplaceParamsFactory.getTokenDuration(params))
    },

    /**
     * Makes a call to the WeatherMarketplace smart contract using the `record_token_sale()uint64` ABI method.
     *
     * Record a token sale (simplified for demo).
     *
     * @param params The params for the smart contract call
     * @returns The call params: Updated total sales count
     */
    recordTokenSale: (params: CallParams<WeatherMarketplaceArgs['obj']['record_token_sale()uint64'] | WeatherMarketplaceArgs['tuple']['record_token_sale()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(WeatherMarketplaceParamsFactory.recordTokenSale(params))
    },

    /**
     * Makes a call to the WeatherMarketplace smart contract using the `get_total_sales()uint64` ABI method.
     *
     * Get total token sales count.
     *
     * @param params The params for the smart contract call
     * @returns The call params: Total number of tokens sold
     */
    getTotalSales: (params: CallParams<WeatherMarketplaceArgs['obj']['get_total_sales()uint64'] | WeatherMarketplaceArgs['tuple']['get_total_sales()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(WeatherMarketplaceParamsFactory.getTotalSales(params))
    },

    /**
     * Makes a call to the WeatherMarketplace smart contract using the `is_contract_active()bool` ABI method.
     *
     * Check if the contract is active.
     *
     * @param params The params for the smart contract call
     * @returns The call params: True if contract is active
     */
    isContractActive: (params: CallParams<WeatherMarketplaceArgs['obj']['is_contract_active()bool'] | WeatherMarketplaceArgs['tuple']['is_contract_active()bool']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(WeatherMarketplaceParamsFactory.isContractActive(params))
    },

    /**
     * Makes a call to the WeatherMarketplace smart contract using the `get_contract_info()string` ABI method.
     *
     * Get basic contract information as JSON string.
     *
     * @param params The params for the smart contract call
     * @returns The call params: JSON string with contract info
     */
    getContractInfo: (params: CallParams<WeatherMarketplaceArgs['obj']['get_contract_info()string'] | WeatherMarketplaceArgs['tuple']['get_contract_info()string']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(WeatherMarketplaceParamsFactory.getContractInfo(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Makes a clear_state call to an existing instance of the WeatherMarketplace smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the WeatherMarketplace smart contract using the `get_token_price()uint64` ABI method.
     *
     * Get the current token price.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction: Token price in microAlgos
     */
    getTokenPrice: (params: CallParams<WeatherMarketplaceArgs['obj']['get_token_price()uint64'] | WeatherMarketplaceArgs['tuple']['get_token_price()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(WeatherMarketplaceParamsFactory.getTokenPrice(params))
    },

    /**
     * Makes a call to the WeatherMarketplace smart contract using the `get_weather_asa_id()uint64` ABI method.
     *
     * Get the weather ASA ID.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction: The weather token ASA ID
     */
    getWeatherAsaId: (params: CallParams<WeatherMarketplaceArgs['obj']['get_weather_asa_id()uint64'] | WeatherMarketplaceArgs['tuple']['get_weather_asa_id()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(WeatherMarketplaceParamsFactory.getWeatherAsaId(params))
    },

    /**
     * Makes a call to the WeatherMarketplace smart contract using the `set_weather_asa_id(uint64)void` ABI method.
     *
     * Set the weather ASA ID (admin only for demo).
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    setWeatherAsaId: (params: CallParams<WeatherMarketplaceArgs['obj']['set_weather_asa_id(uint64)void'] | WeatherMarketplaceArgs['tuple']['set_weather_asa_id(uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(WeatherMarketplaceParamsFactory.setWeatherAsaId(params))
    },

    /**
     * Makes a call to the WeatherMarketplace smart contract using the `get_token_duration()uint64` ABI method.
     *
     * Get the token validity duration.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction: Duration in seconds
     */
    getTokenDuration: (params: CallParams<WeatherMarketplaceArgs['obj']['get_token_duration()uint64'] | WeatherMarketplaceArgs['tuple']['get_token_duration()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(WeatherMarketplaceParamsFactory.getTokenDuration(params))
    },

    /**
     * Makes a call to the WeatherMarketplace smart contract using the `record_token_sale()uint64` ABI method.
     *
     * Record a token sale (simplified for demo).
     *
     * @param params The params for the smart contract call
     * @returns The call transaction: Updated total sales count
     */
    recordTokenSale: (params: CallParams<WeatherMarketplaceArgs['obj']['record_token_sale()uint64'] | WeatherMarketplaceArgs['tuple']['record_token_sale()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(WeatherMarketplaceParamsFactory.recordTokenSale(params))
    },

    /**
     * Makes a call to the WeatherMarketplace smart contract using the `get_total_sales()uint64` ABI method.
     *
     * Get total token sales count.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction: Total number of tokens sold
     */
    getTotalSales: (params: CallParams<WeatherMarketplaceArgs['obj']['get_total_sales()uint64'] | WeatherMarketplaceArgs['tuple']['get_total_sales()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(WeatherMarketplaceParamsFactory.getTotalSales(params))
    },

    /**
     * Makes a call to the WeatherMarketplace smart contract using the `is_contract_active()bool` ABI method.
     *
     * Check if the contract is active.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction: True if contract is active
     */
    isContractActive: (params: CallParams<WeatherMarketplaceArgs['obj']['is_contract_active()bool'] | WeatherMarketplaceArgs['tuple']['is_contract_active()bool']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(WeatherMarketplaceParamsFactory.isContractActive(params))
    },

    /**
     * Makes a call to the WeatherMarketplace smart contract using the `get_contract_info()string` ABI method.
     *
     * Get basic contract information as JSON string.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction: JSON string with contract info
     */
    getContractInfo: (params: CallParams<WeatherMarketplaceArgs['obj']['get_contract_info()string'] | WeatherMarketplaceArgs['tuple']['get_contract_info()string']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(WeatherMarketplaceParamsFactory.getContractInfo(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Makes a clear_state call to an existing instance of the WeatherMarketplace smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the WeatherMarketplace smart contract using the `get_token_price()uint64` ABI method.
     *
     * Get the current token price.
     *
     * @param params The params for the smart contract call
     * @returns The call result: Token price in microAlgos
     */
    getTokenPrice: async (params: CallParams<WeatherMarketplaceArgs['obj']['get_token_price()uint64'] | WeatherMarketplaceArgs['tuple']['get_token_price()uint64']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(WeatherMarketplaceParamsFactory.getTokenPrice(params))
      return {...result, return: result.return as unknown as (undefined | WeatherMarketplaceReturns['get_token_price()uint64'])}
    },

    /**
     * Makes a call to the WeatherMarketplace smart contract using the `get_weather_asa_id()uint64` ABI method.
     *
     * Get the weather ASA ID.
     *
     * @param params The params for the smart contract call
     * @returns The call result: The weather token ASA ID
     */
    getWeatherAsaId: async (params: CallParams<WeatherMarketplaceArgs['obj']['get_weather_asa_id()uint64'] | WeatherMarketplaceArgs['tuple']['get_weather_asa_id()uint64']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(WeatherMarketplaceParamsFactory.getWeatherAsaId(params))
      return {...result, return: result.return as unknown as (undefined | WeatherMarketplaceReturns['get_weather_asa_id()uint64'])}
    },

    /**
     * Makes a call to the WeatherMarketplace smart contract using the `set_weather_asa_id(uint64)void` ABI method.
     *
     * Set the weather ASA ID (admin only for demo).
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    setWeatherAsaId: async (params: CallParams<WeatherMarketplaceArgs['obj']['set_weather_asa_id(uint64)void'] | WeatherMarketplaceArgs['tuple']['set_weather_asa_id(uint64)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(WeatherMarketplaceParamsFactory.setWeatherAsaId(params))
      return {...result, return: result.return as unknown as (undefined | WeatherMarketplaceReturns['set_weather_asa_id(uint64)void'])}
    },

    /**
     * Makes a call to the WeatherMarketplace smart contract using the `get_token_duration()uint64` ABI method.
     *
     * Get the token validity duration.
     *
     * @param params The params for the smart contract call
     * @returns The call result: Duration in seconds
     */
    getTokenDuration: async (params: CallParams<WeatherMarketplaceArgs['obj']['get_token_duration()uint64'] | WeatherMarketplaceArgs['tuple']['get_token_duration()uint64']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(WeatherMarketplaceParamsFactory.getTokenDuration(params))
      return {...result, return: result.return as unknown as (undefined | WeatherMarketplaceReturns['get_token_duration()uint64'])}
    },

    /**
     * Makes a call to the WeatherMarketplace smart contract using the `record_token_sale()uint64` ABI method.
     *
     * Record a token sale (simplified for demo).
     *
     * @param params The params for the smart contract call
     * @returns The call result: Updated total sales count
     */
    recordTokenSale: async (params: CallParams<WeatherMarketplaceArgs['obj']['record_token_sale()uint64'] | WeatherMarketplaceArgs['tuple']['record_token_sale()uint64']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(WeatherMarketplaceParamsFactory.recordTokenSale(params))
      return {...result, return: result.return as unknown as (undefined | WeatherMarketplaceReturns['record_token_sale()uint64'])}
    },

    /**
     * Makes a call to the WeatherMarketplace smart contract using the `get_total_sales()uint64` ABI method.
     *
     * Get total token sales count.
     *
     * @param params The params for the smart contract call
     * @returns The call result: Total number of tokens sold
     */
    getTotalSales: async (params: CallParams<WeatherMarketplaceArgs['obj']['get_total_sales()uint64'] | WeatherMarketplaceArgs['tuple']['get_total_sales()uint64']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(WeatherMarketplaceParamsFactory.getTotalSales(params))
      return {...result, return: result.return as unknown as (undefined | WeatherMarketplaceReturns['get_total_sales()uint64'])}
    },

    /**
     * Makes a call to the WeatherMarketplace smart contract using the `is_contract_active()bool` ABI method.
     *
     * Check if the contract is active.
     *
     * @param params The params for the smart contract call
     * @returns The call result: True if contract is active
     */
    isContractActive: async (params: CallParams<WeatherMarketplaceArgs['obj']['is_contract_active()bool'] | WeatherMarketplaceArgs['tuple']['is_contract_active()bool']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(WeatherMarketplaceParamsFactory.isContractActive(params))
      return {...result, return: result.return as unknown as (undefined | WeatherMarketplaceReturns['is_contract_active()bool'])}
    },

    /**
     * Makes a call to the WeatherMarketplace smart contract using the `get_contract_info()string` ABI method.
     *
     * Get basic contract information as JSON string.
     *
     * @param params The params for the smart contract call
     * @returns The call result: JSON string with contract info
     */
    getContractInfo: async (params: CallParams<WeatherMarketplaceArgs['obj']['get_contract_info()string'] | WeatherMarketplaceArgs['tuple']['get_contract_info()string']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(WeatherMarketplaceParamsFactory.getContractInfo(params))
      return {...result, return: result.return as unknown as (undefined | WeatherMarketplaceReturns['get_contract_info()string'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new WeatherMarketplaceClient(this.appClient.clone(params))
  }

  /**
   * Methods to access state for the current WeatherMarketplace app
   */
  state = {
    /**
     * Methods to access global state for the current WeatherMarketplace app
     */
    global: {
      /**
       * Get all current keyed values from global state
       */
      getAll: async (): Promise<Partial<Expand<GlobalKeysState>>> => {
        const result = await this.appClient.state.global.getAll()
        return {
          tokenPrice: result.token_price,
          weatherAsaId: result.weather_asa_id,
          tokenDuration: result.token_duration,
          totalTokensSold: result.total_tokens_sold,
          isActive: result.is_active,
        }
      },
      /**
       * Get the current value of the token_price key in global state
       */
      tokenPrice: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("token_price")) as bigint | undefined },
      /**
       * Get the current value of the weather_asa_id key in global state
       */
      weatherAsaId: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("weather_asa_id")) as bigint | undefined },
      /**
       * Get the current value of the token_duration key in global state
       */
      tokenDuration: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("token_duration")) as bigint | undefined },
      /**
       * Get the current value of the total_tokens_sold key in global state
       */
      totalTokensSold: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("total_tokens_sold")) as bigint | undefined },
      /**
       * Get the current value of the is_active key in global state
       */
      isActive: async (): Promise<boolean | undefined> => { return (await this.appClient.state.global.getValue("is_active")) as boolean | undefined },
    },
  }

  public newGroup(): WeatherMarketplaceComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a get_token_price()uint64 method call against the WeatherMarketplace contract
       */
      getTokenPrice(params: CallParams<WeatherMarketplaceArgs['obj']['get_token_price()uint64'] | WeatherMarketplaceArgs['tuple']['get_token_price()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.getTokenPrice(params)))
        resultMappers.push((v) => client.decodeReturnValue('get_token_price()uint64', v))
        return this
      },
      /**
       * Add a get_weather_asa_id()uint64 method call against the WeatherMarketplace contract
       */
      getWeatherAsaId(params: CallParams<WeatherMarketplaceArgs['obj']['get_weather_asa_id()uint64'] | WeatherMarketplaceArgs['tuple']['get_weather_asa_id()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.getWeatherAsaId(params)))
        resultMappers.push((v) => client.decodeReturnValue('get_weather_asa_id()uint64', v))
        return this
      },
      /**
       * Add a set_weather_asa_id(uint64)void method call against the WeatherMarketplace contract
       */
      setWeatherAsaId(params: CallParams<WeatherMarketplaceArgs['obj']['set_weather_asa_id(uint64)void'] | WeatherMarketplaceArgs['tuple']['set_weather_asa_id(uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.setWeatherAsaId(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a get_token_duration()uint64 method call against the WeatherMarketplace contract
       */
      getTokenDuration(params: CallParams<WeatherMarketplaceArgs['obj']['get_token_duration()uint64'] | WeatherMarketplaceArgs['tuple']['get_token_duration()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.getTokenDuration(params)))
        resultMappers.push((v) => client.decodeReturnValue('get_token_duration()uint64', v))
        return this
      },
      /**
       * Add a record_token_sale()uint64 method call against the WeatherMarketplace contract
       */
      recordTokenSale(params: CallParams<WeatherMarketplaceArgs['obj']['record_token_sale()uint64'] | WeatherMarketplaceArgs['tuple']['record_token_sale()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.recordTokenSale(params)))
        resultMappers.push((v) => client.decodeReturnValue('record_token_sale()uint64', v))
        return this
      },
      /**
       * Add a get_total_sales()uint64 method call against the WeatherMarketplace contract
       */
      getTotalSales(params: CallParams<WeatherMarketplaceArgs['obj']['get_total_sales()uint64'] | WeatherMarketplaceArgs['tuple']['get_total_sales()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.getTotalSales(params)))
        resultMappers.push((v) => client.decodeReturnValue('get_total_sales()uint64', v))
        return this
      },
      /**
       * Add a is_contract_active()bool method call against the WeatherMarketplace contract
       */
      isContractActive(params: CallParams<WeatherMarketplaceArgs['obj']['is_contract_active()bool'] | WeatherMarketplaceArgs['tuple']['is_contract_active()bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.isContractActive(params)))
        resultMappers.push((v) => client.decodeReturnValue('is_contract_active()bool', v))
        return this
      },
      /**
       * Add a get_contract_info()string method call against the WeatherMarketplace contract
       */
      getContractInfo(params: CallParams<WeatherMarketplaceArgs['obj']['get_contract_info()string'] | WeatherMarketplaceArgs['tuple']['get_contract_info()string']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.getContractInfo(params)))
        resultMappers.push((v) => client.decodeReturnValue('get_contract_info()string', v))
        return this
      },
      /**
       * Add a clear state call to the WeatherMarketplace contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as WeatherMarketplaceComposer
  }
}
export type WeatherMarketplaceComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the get_token_price()uint64 ABI method.
   *
   * Get the current token price.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  getTokenPrice(params?: CallParams<WeatherMarketplaceArgs['obj']['get_token_price()uint64'] | WeatherMarketplaceArgs['tuple']['get_token_price()uint64']>): WeatherMarketplaceComposer<[...TReturns, WeatherMarketplaceReturns['get_token_price()uint64'] | undefined]>

  /**
   * Calls the get_weather_asa_id()uint64 ABI method.
   *
   * Get the weather ASA ID.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  getWeatherAsaId(params?: CallParams<WeatherMarketplaceArgs['obj']['get_weather_asa_id()uint64'] | WeatherMarketplaceArgs['tuple']['get_weather_asa_id()uint64']>): WeatherMarketplaceComposer<[...TReturns, WeatherMarketplaceReturns['get_weather_asa_id()uint64'] | undefined]>

  /**
   * Calls the set_weather_asa_id(uint64)void ABI method.
   *
   * Set the weather ASA ID (admin only for demo).
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  setWeatherAsaId(params?: CallParams<WeatherMarketplaceArgs['obj']['set_weather_asa_id(uint64)void'] | WeatherMarketplaceArgs['tuple']['set_weather_asa_id(uint64)void']>): WeatherMarketplaceComposer<[...TReturns, WeatherMarketplaceReturns['set_weather_asa_id(uint64)void'] | undefined]>

  /**
   * Calls the get_token_duration()uint64 ABI method.
   *
   * Get the token validity duration.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  getTokenDuration(params?: CallParams<WeatherMarketplaceArgs['obj']['get_token_duration()uint64'] | WeatherMarketplaceArgs['tuple']['get_token_duration()uint64']>): WeatherMarketplaceComposer<[...TReturns, WeatherMarketplaceReturns['get_token_duration()uint64'] | undefined]>

  /**
   * Calls the record_token_sale()uint64 ABI method.
   *
   * Record a token sale (simplified for demo).
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  recordTokenSale(params?: CallParams<WeatherMarketplaceArgs['obj']['record_token_sale()uint64'] | WeatherMarketplaceArgs['tuple']['record_token_sale()uint64']>): WeatherMarketplaceComposer<[...TReturns, WeatherMarketplaceReturns['record_token_sale()uint64'] | undefined]>

  /**
   * Calls the get_total_sales()uint64 ABI method.
   *
   * Get total token sales count.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  getTotalSales(params?: CallParams<WeatherMarketplaceArgs['obj']['get_total_sales()uint64'] | WeatherMarketplaceArgs['tuple']['get_total_sales()uint64']>): WeatherMarketplaceComposer<[...TReturns, WeatherMarketplaceReturns['get_total_sales()uint64'] | undefined]>

  /**
   * Calls the is_contract_active()bool ABI method.
   *
   * Check if the contract is active.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  isContractActive(params?: CallParams<WeatherMarketplaceArgs['obj']['is_contract_active()bool'] | WeatherMarketplaceArgs['tuple']['is_contract_active()bool']>): WeatherMarketplaceComposer<[...TReturns, WeatherMarketplaceReturns['is_contract_active()bool'] | undefined]>

  /**
   * Calls the get_contract_info()string ABI method.
   *
   * Get basic contract information as JSON string.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  getContractInfo(params?: CallParams<WeatherMarketplaceArgs['obj']['get_contract_info()string'] | WeatherMarketplaceArgs['tuple']['get_contract_info()string']>): WeatherMarketplaceComposer<[...TReturns, WeatherMarketplaceReturns['get_contract_info()string'] | undefined]>

  /**
   * Makes a clear_state call to an existing instance of the WeatherMarketplace smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): WeatherMarketplaceComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): WeatherMarketplaceComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<WeatherMarketplaceComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<WeatherMarketplaceComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<WeatherMarketplaceComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<WeatherMarketplaceComposerResults<TReturns>>
}
export type WeatherMarketplaceComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

