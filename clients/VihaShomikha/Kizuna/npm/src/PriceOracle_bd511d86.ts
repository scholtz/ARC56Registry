/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"PriceOracle","structs":{"RateRecord":[{"name":"rateInr","type":"uint64"},{"name":"timestamp","type":"uint64"},{"name":"updater","type":"address"}]},"methods":[{"name":"create","args":[],"returns":{"type":"void"},"actions":{"create":["NoOp"],"call":[]},"readonly":false,"events":[],"recommendations":{}},{"name":"set_updater","args":[{"type":"address","name":"updater"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}},{"name":"set_max_staleness","args":[{"type":"uint64","name":"max_staleness"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}},{"name":"update_rate","args":[{"type":"uint64","name":"asset_id"},{"type":"uint64","name":"rate_inr"},{"type":"pay","name":"mbr_pay"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Update the price for a given asset. Only admin or designated updater.","events":[],"recommendations":{}},{"name":"update_rate_existing","args":[{"type":"uint64","name":"asset_id"},{"type":"uint64","name":"rate_inr"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Update price for an asset that already has a box allocated.","events":[],"recommendations":{}},{"name":"get_rate","args":[{"type":"uint64","name":"asset_id"}],"returns":{"type":"(uint64,uint64,address)","struct":"RateRecord"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"is_rate_fresh","args":[{"type":"uint64","name":"asset_id"}],"returns":{"type":"bool"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"get_rate_value","args":[{"type":"uint64","name":"asset_id"}],"returns":{"type":"uint64"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"desc":"Returns just the rate value for easy consumption.","events":[],"recommendations":{}}],"arcs":[22,28],"desc":"On-chain price oracle that stores rate snapshots for rate-locking.\n    Rates are stored as INR per 1 unit of crypto, scaled by 1e6 (6 decimals).\n    E.g., ALGO at ₹85.47 is stored as 85_470_000.","networks":{},"state":{"schema":{"global":{"ints":1,"bytes":2},"local":{"ints":0,"bytes":0}},"keys":{"global":{"admin":{"keyType":"AVMString","valueType":"address","key":"YWRtaW4="},"updater":{"keyType":"AVMString","valueType":"address","key":"dXBkYXRlcg=="},"max_staleness":{"keyType":"AVMString","valueType":"AVMUint64","key":"bWF4X3N0YWxlbmVzcw=="}},"local":{},"box":{}},"maps":{"global":{},"local":{},"box":{"rates":{"keyType":"uint64","valueType":"RateRecord","prefix":"cmF0ZV8="}}}},"bareActions":{"create":[],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[250],"errorMessage":"MBR pay to app"},{"pc":[240,314],"errorMessage":"Not authorized"},{"pc":[163,185],"errorMessage":"Only admin"},{"pc":[243,316],"errorMessage":"Rate must be positive"},{"pc":[325],"errorMessage":"Rate not initialized"},{"pc":[161,183,224,298],"errorMessage":"check self.admin exists"},{"pc":[410],"errorMessage":"check self.max_staleness exists"},{"pc":[357,403,427],"errorMessage":"check self.rates entry exists"},{"pc":[234,308],"errorMessage":"check self.updater exists"},{"pc":[155],"errorMessage":"invalid number of bytes for arc4.static_array<arc4.uint8, 32>"},{"pc":[176,198,207,282,291,352,372,422],"errorMessage":"invalid number of bytes for arc4.uint64"},{"pc":[218],"errorMessage":"transaction type is pay"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"events":[]} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


// Type definitions for ARC-56 structs

export type RateRecord = {
  rateInr: bigint,
  timestamp: bigint,
  updater: string
}


/**
 * Converts the ABI tuple representation of a RateRecord to the struct representation
 */
export function RateRecordFromTuple(abiTuple: [bigint, bigint, string]) {
  return getABIStructFromABITuple(abiTuple, APP_SPEC.structs.RateRecord, APP_SPEC.structs) as RateRecord
}

/**
 * The argument types for the PriceOracle contract
 */
export type PriceOracleArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'create()void': Record<string, never>
    'set_updater(address)void': {
      updater: string
    }
    'set_max_staleness(uint64)void': {
      maxStaleness: bigint | number
    }
    'update_rate(uint64,uint64,pay)void': {
      assetId: bigint | number
      rateInr: bigint | number
      mbrPay: AppMethodCallTransactionArgument
    }
    'update_rate_existing(uint64,uint64)void': {
      assetId: bigint | number
      rateInr: bigint | number
    }
    'get_rate(uint64)(uint64,uint64,address)': {
      assetId: bigint | number
    }
    'is_rate_fresh(uint64)bool': {
      assetId: bigint | number
    }
    'get_rate_value(uint64)uint64': {
      assetId: bigint | number
    }
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'create()void': []
    'set_updater(address)void': [updater: string]
    'set_max_staleness(uint64)void': [maxStaleness: bigint | number]
    'update_rate(uint64,uint64,pay)void': [assetId: bigint | number, rateInr: bigint | number, mbrPay: AppMethodCallTransactionArgument]
    'update_rate_existing(uint64,uint64)void': [assetId: bigint | number, rateInr: bigint | number]
    'get_rate(uint64)(uint64,uint64,address)': [assetId: bigint | number]
    'is_rate_fresh(uint64)bool': [assetId: bigint | number]
    'get_rate_value(uint64)uint64': [assetId: bigint | number]
  }
}

/**
 * The return type for each method
 */
export type PriceOracleReturns = {
  'create()void': void
  'set_updater(address)void': void
  'set_max_staleness(uint64)void': void
  'update_rate(uint64,uint64,pay)void': void
  'update_rate_existing(uint64,uint64)void': void
  'get_rate(uint64)(uint64,uint64,address)': RateRecord
  'is_rate_fresh(uint64)bool': boolean
  'get_rate_value(uint64)uint64': bigint
}

/**
 * Defines the types of available calls and state of the PriceOracle smart contract.
 */
export type PriceOracleTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'create()void' | 'create', {
      argsObj: PriceOracleArgs['obj']['create()void']
      argsTuple: PriceOracleArgs['tuple']['create()void']
      returns: PriceOracleReturns['create()void']
    }>
    & Record<'set_updater(address)void' | 'set_updater', {
      argsObj: PriceOracleArgs['obj']['set_updater(address)void']
      argsTuple: PriceOracleArgs['tuple']['set_updater(address)void']
      returns: PriceOracleReturns['set_updater(address)void']
    }>
    & Record<'set_max_staleness(uint64)void' | 'set_max_staleness', {
      argsObj: PriceOracleArgs['obj']['set_max_staleness(uint64)void']
      argsTuple: PriceOracleArgs['tuple']['set_max_staleness(uint64)void']
      returns: PriceOracleReturns['set_max_staleness(uint64)void']
    }>
    & Record<'update_rate(uint64,uint64,pay)void' | 'update_rate', {
      argsObj: PriceOracleArgs['obj']['update_rate(uint64,uint64,pay)void']
      argsTuple: PriceOracleArgs['tuple']['update_rate(uint64,uint64,pay)void']
      returns: PriceOracleReturns['update_rate(uint64,uint64,pay)void']
    }>
    & Record<'update_rate_existing(uint64,uint64)void' | 'update_rate_existing', {
      argsObj: PriceOracleArgs['obj']['update_rate_existing(uint64,uint64)void']
      argsTuple: PriceOracleArgs['tuple']['update_rate_existing(uint64,uint64)void']
      returns: PriceOracleReturns['update_rate_existing(uint64,uint64)void']
    }>
    & Record<'get_rate(uint64)(uint64,uint64,address)' | 'get_rate', {
      argsObj: PriceOracleArgs['obj']['get_rate(uint64)(uint64,uint64,address)']
      argsTuple: PriceOracleArgs['tuple']['get_rate(uint64)(uint64,uint64,address)']
      returns: PriceOracleReturns['get_rate(uint64)(uint64,uint64,address)']
    }>
    & Record<'is_rate_fresh(uint64)bool' | 'is_rate_fresh', {
      argsObj: PriceOracleArgs['obj']['is_rate_fresh(uint64)bool']
      argsTuple: PriceOracleArgs['tuple']['is_rate_fresh(uint64)bool']
      returns: PriceOracleReturns['is_rate_fresh(uint64)bool']
    }>
    & Record<'get_rate_value(uint64)uint64' | 'get_rate_value', {
      argsObj: PriceOracleArgs['obj']['get_rate_value(uint64)uint64']
      argsTuple: PriceOracleArgs['tuple']['get_rate_value(uint64)uint64']
      returns: PriceOracleReturns['get_rate_value(uint64)uint64']
    }>
  /**
   * Defines the shape of the state of the application.
   */
  state: {
    global: {
      keys: {
        admin: string
        updater: string
        maxStaleness: bigint
      }
      maps: {}
    }
    box: {
      keys: {}
      maps: {
        rates: Map<bigint | number, RateRecord>
      }
    }
  }
}

/**
 * Defines the possible abi call signatures.
 */
export type PriceOracleSignatures = keyof PriceOracleTypes['methods']
/**
 * Defines the possible abi call signatures for methods that return a non-void value.
 */
export type PriceOracleNonVoidMethodSignatures = keyof PriceOracleTypes['methods'] extends infer T ? T extends keyof PriceOracleTypes['methods'] ? MethodReturn<T> extends void ? never : T  : never : never
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the PriceOracle smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends PriceOracleSignatures> = PriceOracleTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the PriceOracle smart contract to the method's return type
 */
export type MethodReturn<TSignature extends PriceOracleSignatures> = PriceOracleTypes['methods'][TSignature]['returns']

/**
 * Defines the shape of the keyed global state of the application.
 */
export type GlobalKeysState = PriceOracleTypes['state']['global']['keys']

/**
 * Defines the shape of the keyed box state of the application.
 */
export type BoxKeysState = PriceOracleTypes['state']['box']['keys']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the PriceOracle smart contract
 */
export abstract class PriceOracleParamsFactory {
  /**
   * Constructs a no op call for the set_updater(address)void ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static setUpdater(params: CallParams<PriceOracleArgs['obj']['set_updater(address)void'] | PriceOracleArgs['tuple']['set_updater(address)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'set_updater(address)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.updater],
    }
  }
  /**
   * Constructs a no op call for the set_max_staleness(uint64)void ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static setMaxStaleness(params: CallParams<PriceOracleArgs['obj']['set_max_staleness(uint64)void'] | PriceOracleArgs['tuple']['set_max_staleness(uint64)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'set_max_staleness(uint64)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.maxStaleness],
    }
  }
  /**
   * Constructs a no op call for the update_rate(uint64,uint64,pay)void ABI method
   *
   * Update the price for a given asset. Only admin or designated updater.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static updateRate(params: CallParams<PriceOracleArgs['obj']['update_rate(uint64,uint64,pay)void'] | PriceOracleArgs['tuple']['update_rate(uint64,uint64,pay)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'update_rate(uint64,uint64,pay)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.assetId, params.args.rateInr, params.args.mbrPay],
    }
  }
  /**
   * Constructs a no op call for the update_rate_existing(uint64,uint64)void ABI method
   *
   * Update price for an asset that already has a box allocated.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static updateRateExisting(params: CallParams<PriceOracleArgs['obj']['update_rate_existing(uint64,uint64)void'] | PriceOracleArgs['tuple']['update_rate_existing(uint64,uint64)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'update_rate_existing(uint64,uint64)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.assetId, params.args.rateInr],
    }
  }
  /**
   * Constructs a no op call for the get_rate(uint64)(uint64,uint64,address) ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static getRate(params: CallParams<PriceOracleArgs['obj']['get_rate(uint64)(uint64,uint64,address)'] | PriceOracleArgs['tuple']['get_rate(uint64)(uint64,uint64,address)']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'get_rate(uint64)(uint64,uint64,address)' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.assetId],
    }
  }
  /**
   * Constructs a no op call for the is_rate_fresh(uint64)bool ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static isRateFresh(params: CallParams<PriceOracleArgs['obj']['is_rate_fresh(uint64)bool'] | PriceOracleArgs['tuple']['is_rate_fresh(uint64)bool']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'is_rate_fresh(uint64)bool' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.assetId],
    }
  }
  /**
   * Constructs a no op call for the get_rate_value(uint64)uint64 ABI method
   *
   * Returns just the rate value for easy consumption.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static getRateValue(params: CallParams<PriceOracleArgs['obj']['get_rate_value(uint64)uint64'] | PriceOracleArgs['tuple']['get_rate_value(uint64)uint64']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'get_rate_value(uint64)uint64' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.assetId],
    }
  }
}

/**
 * A client to make calls to the PriceOracle smart contract
 */
export class PriceOracleClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `PriceOracleClient`
   *
   * @param appClient An `AppClient` instance which has been created with the PriceOracle app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `PriceOracleClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Checks for decode errors on the given return value and maps the return value to the return type for the given method
   * @returns The typed return value or undefined if there was no value
   */
  decodeReturnValue<TSignature extends PriceOracleNonVoidMethodSignatures>(method: TSignature, returnValue: ABIReturn | undefined) {
    return returnValue !== undefined ? getArc56ReturnValue<MethodReturn<TSignature>>(returnValue, this.appClient.getABIMethod(method), APP_SPEC.structs) : undefined
  }

  /**
   * Returns a new `PriceOracleClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<PriceOracleClient> {
    return new PriceOracleClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `PriceOracleClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<PriceOracleClient> {
    return new PriceOracleClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Makes a clear_state call to an existing instance of the PriceOracle smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the PriceOracle smart contract using the `set_updater(address)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    setUpdater: (params: CallParams<PriceOracleArgs['obj']['set_updater(address)void'] | PriceOracleArgs['tuple']['set_updater(address)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(PriceOracleParamsFactory.setUpdater(params))
    },

    /**
     * Makes a call to the PriceOracle smart contract using the `set_max_staleness(uint64)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    setMaxStaleness: (params: CallParams<PriceOracleArgs['obj']['set_max_staleness(uint64)void'] | PriceOracleArgs['tuple']['set_max_staleness(uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(PriceOracleParamsFactory.setMaxStaleness(params))
    },

    /**
     * Makes a call to the PriceOracle smart contract using the `update_rate(uint64,uint64,pay)void` ABI method.
     *
     * Update the price for a given asset. Only admin or designated updater.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    updateRate: (params: CallParams<PriceOracleArgs['obj']['update_rate(uint64,uint64,pay)void'] | PriceOracleArgs['tuple']['update_rate(uint64,uint64,pay)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(PriceOracleParamsFactory.updateRate(params))
    },

    /**
     * Makes a call to the PriceOracle smart contract using the `update_rate_existing(uint64,uint64)void` ABI method.
     *
     * Update price for an asset that already has a box allocated.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    updateRateExisting: (params: CallParams<PriceOracleArgs['obj']['update_rate_existing(uint64,uint64)void'] | PriceOracleArgs['tuple']['update_rate_existing(uint64,uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(PriceOracleParamsFactory.updateRateExisting(params))
    },

    /**
     * Makes a call to the PriceOracle smart contract using the `get_rate(uint64)(uint64,uint64,address)` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    getRate: (params: CallParams<PriceOracleArgs['obj']['get_rate(uint64)(uint64,uint64,address)'] | PriceOracleArgs['tuple']['get_rate(uint64)(uint64,uint64,address)']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(PriceOracleParamsFactory.getRate(params))
    },

    /**
     * Makes a call to the PriceOracle smart contract using the `is_rate_fresh(uint64)bool` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    isRateFresh: (params: CallParams<PriceOracleArgs['obj']['is_rate_fresh(uint64)bool'] | PriceOracleArgs['tuple']['is_rate_fresh(uint64)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(PriceOracleParamsFactory.isRateFresh(params))
    },

    /**
     * Makes a call to the PriceOracle smart contract using the `get_rate_value(uint64)uint64` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * Returns just the rate value for easy consumption.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    getRateValue: (params: CallParams<PriceOracleArgs['obj']['get_rate_value(uint64)uint64'] | PriceOracleArgs['tuple']['get_rate_value(uint64)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(PriceOracleParamsFactory.getRateValue(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Makes a clear_state call to an existing instance of the PriceOracle smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the PriceOracle smart contract using the `set_updater(address)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    setUpdater: (params: CallParams<PriceOracleArgs['obj']['set_updater(address)void'] | PriceOracleArgs['tuple']['set_updater(address)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(PriceOracleParamsFactory.setUpdater(params))
    },

    /**
     * Makes a call to the PriceOracle smart contract using the `set_max_staleness(uint64)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    setMaxStaleness: (params: CallParams<PriceOracleArgs['obj']['set_max_staleness(uint64)void'] | PriceOracleArgs['tuple']['set_max_staleness(uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(PriceOracleParamsFactory.setMaxStaleness(params))
    },

    /**
     * Makes a call to the PriceOracle smart contract using the `update_rate(uint64,uint64,pay)void` ABI method.
     *
     * Update the price for a given asset. Only admin or designated updater.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    updateRate: (params: CallParams<PriceOracleArgs['obj']['update_rate(uint64,uint64,pay)void'] | PriceOracleArgs['tuple']['update_rate(uint64,uint64,pay)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(PriceOracleParamsFactory.updateRate(params))
    },

    /**
     * Makes a call to the PriceOracle smart contract using the `update_rate_existing(uint64,uint64)void` ABI method.
     *
     * Update price for an asset that already has a box allocated.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    updateRateExisting: (params: CallParams<PriceOracleArgs['obj']['update_rate_existing(uint64,uint64)void'] | PriceOracleArgs['tuple']['update_rate_existing(uint64,uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(PriceOracleParamsFactory.updateRateExisting(params))
    },

    /**
     * Makes a call to the PriceOracle smart contract using the `get_rate(uint64)(uint64,uint64,address)` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    getRate: (params: CallParams<PriceOracleArgs['obj']['get_rate(uint64)(uint64,uint64,address)'] | PriceOracleArgs['tuple']['get_rate(uint64)(uint64,uint64,address)']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(PriceOracleParamsFactory.getRate(params))
    },

    /**
     * Makes a call to the PriceOracle smart contract using the `is_rate_fresh(uint64)bool` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    isRateFresh: (params: CallParams<PriceOracleArgs['obj']['is_rate_fresh(uint64)bool'] | PriceOracleArgs['tuple']['is_rate_fresh(uint64)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(PriceOracleParamsFactory.isRateFresh(params))
    },

    /**
     * Makes a call to the PriceOracle smart contract using the `get_rate_value(uint64)uint64` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * Returns just the rate value for easy consumption.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    getRateValue: (params: CallParams<PriceOracleArgs['obj']['get_rate_value(uint64)uint64'] | PriceOracleArgs['tuple']['get_rate_value(uint64)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(PriceOracleParamsFactory.getRateValue(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Makes a clear_state call to an existing instance of the PriceOracle smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the PriceOracle smart contract using the `set_updater(address)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    setUpdater: async (params: CallParams<PriceOracleArgs['obj']['set_updater(address)void'] | PriceOracleArgs['tuple']['set_updater(address)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(PriceOracleParamsFactory.setUpdater(params))
      return {...result, return: result.return as unknown as (undefined | PriceOracleReturns['set_updater(address)void'])}
    },

    /**
     * Makes a call to the PriceOracle smart contract using the `set_max_staleness(uint64)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    setMaxStaleness: async (params: CallParams<PriceOracleArgs['obj']['set_max_staleness(uint64)void'] | PriceOracleArgs['tuple']['set_max_staleness(uint64)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(PriceOracleParamsFactory.setMaxStaleness(params))
      return {...result, return: result.return as unknown as (undefined | PriceOracleReturns['set_max_staleness(uint64)void'])}
    },

    /**
     * Makes a call to the PriceOracle smart contract using the `update_rate(uint64,uint64,pay)void` ABI method.
     *
     * Update the price for a given asset. Only admin or designated updater.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    updateRate: async (params: CallParams<PriceOracleArgs['obj']['update_rate(uint64,uint64,pay)void'] | PriceOracleArgs['tuple']['update_rate(uint64,uint64,pay)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(PriceOracleParamsFactory.updateRate(params))
      return {...result, return: result.return as unknown as (undefined | PriceOracleReturns['update_rate(uint64,uint64,pay)void'])}
    },

    /**
     * Makes a call to the PriceOracle smart contract using the `update_rate_existing(uint64,uint64)void` ABI method.
     *
     * Update price for an asset that already has a box allocated.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    updateRateExisting: async (params: CallParams<PriceOracleArgs['obj']['update_rate_existing(uint64,uint64)void'] | PriceOracleArgs['tuple']['update_rate_existing(uint64,uint64)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(PriceOracleParamsFactory.updateRateExisting(params))
      return {...result, return: result.return as unknown as (undefined | PriceOracleReturns['update_rate_existing(uint64,uint64)void'])}
    },

    /**
     * Makes a call to the PriceOracle smart contract using the `get_rate(uint64)(uint64,uint64,address)` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    getRate: async (params: CallParams<PriceOracleArgs['obj']['get_rate(uint64)(uint64,uint64,address)'] | PriceOracleArgs['tuple']['get_rate(uint64)(uint64,uint64,address)']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(PriceOracleParamsFactory.getRate(params))
      return {...result, return: result.return as unknown as (undefined | PriceOracleReturns['get_rate(uint64)(uint64,uint64,address)'])}
    },

    /**
     * Makes a call to the PriceOracle smart contract using the `is_rate_fresh(uint64)bool` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    isRateFresh: async (params: CallParams<PriceOracleArgs['obj']['is_rate_fresh(uint64)bool'] | PriceOracleArgs['tuple']['is_rate_fresh(uint64)bool']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(PriceOracleParamsFactory.isRateFresh(params))
      return {...result, return: result.return as unknown as (undefined | PriceOracleReturns['is_rate_fresh(uint64)bool'])}
    },

    /**
     * Makes a call to the PriceOracle smart contract using the `get_rate_value(uint64)uint64` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * Returns just the rate value for easy consumption.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    getRateValue: async (params: CallParams<PriceOracleArgs['obj']['get_rate_value(uint64)uint64'] | PriceOracleArgs['tuple']['get_rate_value(uint64)uint64']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(PriceOracleParamsFactory.getRateValue(params))
      return {...result, return: result.return as unknown as (undefined | PriceOracleReturns['get_rate_value(uint64)uint64'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new PriceOracleClient(this.appClient.clone(params))
  }

  /**
   * Makes a readonly (simulated) call to the PriceOracle smart contract using the `get_rate(uint64)(uint64,uint64,address)` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async getRate(params: CallParams<PriceOracleArgs['obj']['get_rate(uint64)(uint64,uint64,address)'] | PriceOracleArgs['tuple']['get_rate(uint64)(uint64,uint64,address)']>) {
    const result = await this.appClient.send.call(PriceOracleParamsFactory.getRate(params))
    return result.return as unknown as PriceOracleReturns['get_rate(uint64)(uint64,uint64,address)']
  }

  /**
   * Makes a readonly (simulated) call to the PriceOracle smart contract using the `is_rate_fresh(uint64)bool` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async isRateFresh(params: CallParams<PriceOracleArgs['obj']['is_rate_fresh(uint64)bool'] | PriceOracleArgs['tuple']['is_rate_fresh(uint64)bool']>) {
    const result = await this.appClient.send.call(PriceOracleParamsFactory.isRateFresh(params))
    return result.return as unknown as PriceOracleReturns['is_rate_fresh(uint64)bool']
  }

  /**
   * Makes a readonly (simulated) call to the PriceOracle smart contract using the `get_rate_value(uint64)uint64` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * Returns just the rate value for easy consumption.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async getRateValue(params: CallParams<PriceOracleArgs['obj']['get_rate_value(uint64)uint64'] | PriceOracleArgs['tuple']['get_rate_value(uint64)uint64']>) {
    const result = await this.appClient.send.call(PriceOracleParamsFactory.getRateValue(params))
    return result.return as unknown as PriceOracleReturns['get_rate_value(uint64)uint64']
  }

  /**
   * Methods to access state for the current PriceOracle app
   */
  state = {
    /**
     * Methods to access global state for the current PriceOracle app
     */
    global: {
      /**
       * Get all current keyed values from global state
       */
      getAll: async (): Promise<Partial<Expand<GlobalKeysState>>> => {
        const result = await this.appClient.state.global.getAll()
        return {
          admin: result.admin,
          updater: result.updater,
          maxStaleness: result.max_staleness,
        }
      },
      /**
       * Get the current value of the admin key in global state
       */
      admin: async (): Promise<string | undefined> => { return (await this.appClient.state.global.getValue("admin")) as string | undefined },
      /**
       * Get the current value of the updater key in global state
       */
      updater: async (): Promise<string | undefined> => { return (await this.appClient.state.global.getValue("updater")) as string | undefined },
      /**
       * Get the current value of the max_staleness key in global state
       */
      maxStaleness: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("max_staleness")) as bigint | undefined },
    },
    /**
     * Methods to access box state for the current PriceOracle app
     */
    box: {
      /**
       * Get all current keyed values from box state
       */
      getAll: async (): Promise<Partial<Expand<BoxKeysState>>> => {
        const result = await this.appClient.state.box.getAll()
        return {
        }
      },
      /**
       * Get values from the rates map in box state
       */
      rates: {
        /**
         * Get all current values of the rates map in box state
         */
        getMap: async (): Promise<Map<bigint, RateRecord>> => { return (await this.appClient.state.box.getMap("rates")) as Map<bigint, RateRecord> },
        /**
         * Get a current value of the rates map by key from box state
         */
        value: async (key: bigint | number): Promise<RateRecord | undefined> => { return await this.appClient.state.box.getMapValue("rates", key) as RateRecord | undefined },
      },
    },
  }

  public newGroup(): PriceOracleComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a set_updater(address)void method call against the PriceOracle contract
       */
      setUpdater(params: CallParams<PriceOracleArgs['obj']['set_updater(address)void'] | PriceOracleArgs['tuple']['set_updater(address)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.setUpdater(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a set_max_staleness(uint64)void method call against the PriceOracle contract
       */
      setMaxStaleness(params: CallParams<PriceOracleArgs['obj']['set_max_staleness(uint64)void'] | PriceOracleArgs['tuple']['set_max_staleness(uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.setMaxStaleness(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a update_rate(uint64,uint64,pay)void method call against the PriceOracle contract
       */
      updateRate(params: CallParams<PriceOracleArgs['obj']['update_rate(uint64,uint64,pay)void'] | PriceOracleArgs['tuple']['update_rate(uint64,uint64,pay)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.updateRate(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a update_rate_existing(uint64,uint64)void method call against the PriceOracle contract
       */
      updateRateExisting(params: CallParams<PriceOracleArgs['obj']['update_rate_existing(uint64,uint64)void'] | PriceOracleArgs['tuple']['update_rate_existing(uint64,uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.updateRateExisting(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a get_rate(uint64)(uint64,uint64,address) method call against the PriceOracle contract
       */
      getRate(params: CallParams<PriceOracleArgs['obj']['get_rate(uint64)(uint64,uint64,address)'] | PriceOracleArgs['tuple']['get_rate(uint64)(uint64,uint64,address)']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.getRate(params)))
        resultMappers.push((v) => client.decodeReturnValue('get_rate(uint64)(uint64,uint64,address)', v))
        return this
      },
      /**
       * Add a is_rate_fresh(uint64)bool method call against the PriceOracle contract
       */
      isRateFresh(params: CallParams<PriceOracleArgs['obj']['is_rate_fresh(uint64)bool'] | PriceOracleArgs['tuple']['is_rate_fresh(uint64)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.isRateFresh(params)))
        resultMappers.push((v) => client.decodeReturnValue('is_rate_fresh(uint64)bool', v))
        return this
      },
      /**
       * Add a get_rate_value(uint64)uint64 method call against the PriceOracle contract
       */
      getRateValue(params: CallParams<PriceOracleArgs['obj']['get_rate_value(uint64)uint64'] | PriceOracleArgs['tuple']['get_rate_value(uint64)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.getRateValue(params)))
        resultMappers.push((v) => client.decodeReturnValue('get_rate_value(uint64)uint64', v))
        return this
      },
      /**
       * Add a clear state call to the PriceOracle contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as PriceOracleComposer
  }
}
export type PriceOracleComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the set_updater(address)void ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  setUpdater(params?: CallParams<PriceOracleArgs['obj']['set_updater(address)void'] | PriceOracleArgs['tuple']['set_updater(address)void']>): PriceOracleComposer<[...TReturns, PriceOracleReturns['set_updater(address)void'] | undefined]>

  /**
   * Calls the set_max_staleness(uint64)void ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  setMaxStaleness(params?: CallParams<PriceOracleArgs['obj']['set_max_staleness(uint64)void'] | PriceOracleArgs['tuple']['set_max_staleness(uint64)void']>): PriceOracleComposer<[...TReturns, PriceOracleReturns['set_max_staleness(uint64)void'] | undefined]>

  /**
   * Calls the update_rate(uint64,uint64,pay)void ABI method.
   *
   * Update the price for a given asset. Only admin or designated updater.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  updateRate(params?: CallParams<PriceOracleArgs['obj']['update_rate(uint64,uint64,pay)void'] | PriceOracleArgs['tuple']['update_rate(uint64,uint64,pay)void']>): PriceOracleComposer<[...TReturns, PriceOracleReturns['update_rate(uint64,uint64,pay)void'] | undefined]>

  /**
   * Calls the update_rate_existing(uint64,uint64)void ABI method.
   *
   * Update price for an asset that already has a box allocated.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  updateRateExisting(params?: CallParams<PriceOracleArgs['obj']['update_rate_existing(uint64,uint64)void'] | PriceOracleArgs['tuple']['update_rate_existing(uint64,uint64)void']>): PriceOracleComposer<[...TReturns, PriceOracleReturns['update_rate_existing(uint64,uint64)void'] | undefined]>

  /**
   * Calls the get_rate(uint64)(uint64,uint64,address) ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  getRate(params?: CallParams<PriceOracleArgs['obj']['get_rate(uint64)(uint64,uint64,address)'] | PriceOracleArgs['tuple']['get_rate(uint64)(uint64,uint64,address)']>): PriceOracleComposer<[...TReturns, PriceOracleReturns['get_rate(uint64)(uint64,uint64,address)'] | undefined]>

  /**
   * Calls the is_rate_fresh(uint64)bool ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  isRateFresh(params?: CallParams<PriceOracleArgs['obj']['is_rate_fresh(uint64)bool'] | PriceOracleArgs['tuple']['is_rate_fresh(uint64)bool']>): PriceOracleComposer<[...TReturns, PriceOracleReturns['is_rate_fresh(uint64)bool'] | undefined]>

  /**
   * Calls the get_rate_value(uint64)uint64 ABI method.
   *
   * Returns just the rate value for easy consumption.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  getRateValue(params?: CallParams<PriceOracleArgs['obj']['get_rate_value(uint64)uint64'] | PriceOracleArgs['tuple']['get_rate_value(uint64)uint64']>): PriceOracleComposer<[...TReturns, PriceOracleReturns['get_rate_value(uint64)uint64'] | undefined]>

  /**
   * Makes a clear_state call to an existing instance of the PriceOracle smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): PriceOracleComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): PriceOracleComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<PriceOracleComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<PriceOracleComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<PriceOracleComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<PriceOracleComposerResults<TReturns>>
}
export type PriceOracleComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

