/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"VerifiedProtocol","structs":{},"methods":[{"name":"submit_skill_record","args":[{"type":"string","name":"mode"},{"type":"string","name":"domain"},{"type":"uint64","name":"score"},{"type":"string","name":"artifact_hash"},{"type":"uint64","name":"timestamp"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Append a new SkillRecord to the sender's Box.\n• If the Box does not exist yet, it is created with exactly the   bytes of the first length-prefixed record. • If the Box already exists, the new record is appended at the end. • The caller must ensure adequate MBR (Minimum Balance Requirement)   funding for box creation / growth via an accompanying payment txn.","events":[],"recommendations":{}},{"name":"get_skill_records","args":[{"type":"address","name":"wallet"}],"returns":{"type":"byte[]"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"desc":"Return the raw Box bytes for a given wallet address.\nReturns empty bytes if the wallet has no records. Callers can decode the result off-chain by iterating:   1. Read 2-byte big-endian length prefix → record_len   2. Read next record_len bytes → ARC-4 SkillRecord   3. Repeat until buffer exhausted.","events":[],"recommendations":{}},{"name":"get_record_count","args":[{"type":"address","name":"wallet"}],"returns":{"type":"uint64"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"desc":"Return the number of skill records stored for a wallet.\nWalks the length-prefixed buffer and counts entries. Returns 0 if the wallet has no Box.","events":[],"recommendations":{}}],"arcs":[22,28],"desc":"Skill Reputation Protocol — append-only Box ledger per wallet.\n\n    Every wallet (Algorand account) gets its own Box whose key is the\n    raw 32-byte sender address.  Skill records are ARC-4 encoded and\n    length-prefixed so they can be deterministically parsed off-chain.\n\n    ABI Methods\n    -----------\n    submit_skill_record(mode, domain, score, artifact_hash, timestamp)\n        Append a new SkillRecord to the caller's Box.\n    get_skill_records(wallet) → Bytes\n        Return the raw Box bytes for any wallet (read-only).\n    ","networks":{},"state":{"schema":{"global":{"ints":0,"bytes":0},"local":{"ints":0,"bytes":0}},"keys":{"global":{},"local":{},"box":{}},"maps":{"global":{},"local":{},"box":{}}},"bareActions":{"create":["NoOp"],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[69,85,109],"errorMessage":"invalid array length header"},{"pc":[79,95,116],"errorMessage":"invalid number of bytes for arc4.dynamic_array<arc4.uint8>"},{"pc":[215,250],"errorMessage":"invalid number of bytes for arc4.static_array<arc4.uint8, 32>"},{"pc":[103,124],"errorMessage":"invalid number of bytes for arc4.uint64"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"events":[]} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


/**
 * The argument types for the VerifiedProtocol contract
 */
export type VerifiedProtocolArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'submit_skill_record(string,string,uint64,string,uint64)void': {
      mode: string
      domain: string
      score: bigint | number
      artifactHash: string
      timestamp: bigint | number
    }
    'get_skill_records(address)byte[]': {
      wallet: string
    }
    'get_record_count(address)uint64': {
      wallet: string
    }
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'submit_skill_record(string,string,uint64,string,uint64)void': [mode: string, domain: string, score: bigint | number, artifactHash: string, timestamp: bigint | number]
    'get_skill_records(address)byte[]': [wallet: string]
    'get_record_count(address)uint64': [wallet: string]
  }
}

/**
 * The return type for each method
 */
export type VerifiedProtocolReturns = {
  'submit_skill_record(string,string,uint64,string,uint64)void': void
  'get_skill_records(address)byte[]': Uint8Array
  'get_record_count(address)uint64': bigint
}

/**
 * Defines the types of available calls and state of the VerifiedProtocol smart contract.
 */
export type VerifiedProtocolTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'submit_skill_record(string,string,uint64,string,uint64)void' | 'submit_skill_record', {
      argsObj: VerifiedProtocolArgs['obj']['submit_skill_record(string,string,uint64,string,uint64)void']
      argsTuple: VerifiedProtocolArgs['tuple']['submit_skill_record(string,string,uint64,string,uint64)void']
      returns: VerifiedProtocolReturns['submit_skill_record(string,string,uint64,string,uint64)void']
    }>
    & Record<'get_skill_records(address)byte[]' | 'get_skill_records', {
      argsObj: VerifiedProtocolArgs['obj']['get_skill_records(address)byte[]']
      argsTuple: VerifiedProtocolArgs['tuple']['get_skill_records(address)byte[]']
      returns: VerifiedProtocolReturns['get_skill_records(address)byte[]']
    }>
    & Record<'get_record_count(address)uint64' | 'get_record_count', {
      argsObj: VerifiedProtocolArgs['obj']['get_record_count(address)uint64']
      argsTuple: VerifiedProtocolArgs['tuple']['get_record_count(address)uint64']
      returns: VerifiedProtocolReturns['get_record_count(address)uint64']
    }>
}

/**
 * Defines the possible abi call signatures.
 */
export type VerifiedProtocolSignatures = keyof VerifiedProtocolTypes['methods']
/**
 * Defines the possible abi call signatures for methods that return a non-void value.
 */
export type VerifiedProtocolNonVoidMethodSignatures = keyof VerifiedProtocolTypes['methods'] extends infer T ? T extends keyof VerifiedProtocolTypes['methods'] ? MethodReturn<T> extends void ? never : T  : never : never
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the VerifiedProtocol smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends VerifiedProtocolSignatures> = VerifiedProtocolTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the VerifiedProtocol smart contract to the method's return type
 */
export type MethodReturn<TSignature extends VerifiedProtocolSignatures> = VerifiedProtocolTypes['methods'][TSignature]['returns']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the VerifiedProtocol smart contract
 */
export abstract class VerifiedProtocolParamsFactory {
  /**
   * Constructs a no op call for the submit_skill_record(string,string,uint64,string,uint64)void ABI method
   *
  * Append a new SkillRecord to the sender's Box.
  • If the Box does not exist yet, it is created with exactly the   bytes of the first length-prefixed record. • If the Box already exists, the new record is appended at the end. • The caller must ensure adequate MBR (Minimum Balance Requirement)   funding for box creation / growth via an accompanying payment txn.

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static submitSkillRecord(params: CallParams<VerifiedProtocolArgs['obj']['submit_skill_record(string,string,uint64,string,uint64)void'] | VerifiedProtocolArgs['tuple']['submit_skill_record(string,string,uint64,string,uint64)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'submit_skill_record(string,string,uint64,string,uint64)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.mode, params.args.domain, params.args.score, params.args.artifactHash, params.args.timestamp],
    }
  }
  /**
   * Constructs a no op call for the get_skill_records(address)byte[] ABI method
   *
  * Return the raw Box bytes for a given wallet address.
  Returns empty bytes if the wallet has no records. Callers can decode the result off-chain by iterating:   1. Read 2-byte big-endian length prefix → record_len   2. Read next record_len bytes → ARC-4 SkillRecord   3. Repeat until buffer exhausted.

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static getSkillRecords(params: CallParams<VerifiedProtocolArgs['obj']['get_skill_records(address)byte[]'] | VerifiedProtocolArgs['tuple']['get_skill_records(address)byte[]']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'get_skill_records(address)byte[]' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.wallet],
    }
  }
  /**
   * Constructs a no op call for the get_record_count(address)uint64 ABI method
   *
  * Return the number of skill records stored for a wallet.
  Walks the length-prefixed buffer and counts entries. Returns 0 if the wallet has no Box.

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static getRecordCount(params: CallParams<VerifiedProtocolArgs['obj']['get_record_count(address)uint64'] | VerifiedProtocolArgs['tuple']['get_record_count(address)uint64']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'get_record_count(address)uint64' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.wallet],
    }
  }
}

/**
 * A client to make calls to the VerifiedProtocol smart contract
 */
export class VerifiedProtocolClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `VerifiedProtocolClient`
   *
   * @param appClient An `AppClient` instance which has been created with the VerifiedProtocol app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `VerifiedProtocolClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Checks for decode errors on the given return value and maps the return value to the return type for the given method
   * @returns The typed return value or undefined if there was no value
   */
  decodeReturnValue<TSignature extends VerifiedProtocolNonVoidMethodSignatures>(method: TSignature, returnValue: ABIReturn | undefined) {
    return returnValue !== undefined ? getArc56ReturnValue<MethodReturn<TSignature>>(returnValue, this.appClient.getABIMethod(method), APP_SPEC.structs) : undefined
  }

  /**
   * Returns a new `VerifiedProtocolClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<VerifiedProtocolClient> {
    return new VerifiedProtocolClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `VerifiedProtocolClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<VerifiedProtocolClient> {
    return new VerifiedProtocolClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Makes a clear_state call to an existing instance of the VerifiedProtocol smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the VerifiedProtocol smart contract using the `submit_skill_record(string,string,uint64,string,uint64)void` ABI method.
     *
    * Append a new SkillRecord to the sender's Box.
    • If the Box does not exist yet, it is created with exactly the   bytes of the first length-prefixed record. • If the Box already exists, the new record is appended at the end. • The caller must ensure adequate MBR (Minimum Balance Requirement)   funding for box creation / growth via an accompanying payment txn.

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    submitSkillRecord: (params: CallParams<VerifiedProtocolArgs['obj']['submit_skill_record(string,string,uint64,string,uint64)void'] | VerifiedProtocolArgs['tuple']['submit_skill_record(string,string,uint64,string,uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(VerifiedProtocolParamsFactory.submitSkillRecord(params))
    },

    /**
     * Makes a call to the VerifiedProtocol smart contract using the `get_skill_records(address)byte[]` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
    * Return the raw Box bytes for a given wallet address.
    Returns empty bytes if the wallet has no records. Callers can decode the result off-chain by iterating:   1. Read 2-byte big-endian length prefix → record_len   2. Read next record_len bytes → ARC-4 SkillRecord   3. Repeat until buffer exhausted.

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    getSkillRecords: (params: CallParams<VerifiedProtocolArgs['obj']['get_skill_records(address)byte[]'] | VerifiedProtocolArgs['tuple']['get_skill_records(address)byte[]']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(VerifiedProtocolParamsFactory.getSkillRecords(params))
    },

    /**
     * Makes a call to the VerifiedProtocol smart contract using the `get_record_count(address)uint64` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
    * Return the number of skill records stored for a wallet.
    Walks the length-prefixed buffer and counts entries. Returns 0 if the wallet has no Box.

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    getRecordCount: (params: CallParams<VerifiedProtocolArgs['obj']['get_record_count(address)uint64'] | VerifiedProtocolArgs['tuple']['get_record_count(address)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(VerifiedProtocolParamsFactory.getRecordCount(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Makes a clear_state call to an existing instance of the VerifiedProtocol smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the VerifiedProtocol smart contract using the `submit_skill_record(string,string,uint64,string,uint64)void` ABI method.
     *
    * Append a new SkillRecord to the sender's Box.
    • If the Box does not exist yet, it is created with exactly the   bytes of the first length-prefixed record. • If the Box already exists, the new record is appended at the end. • The caller must ensure adequate MBR (Minimum Balance Requirement)   funding for box creation / growth via an accompanying payment txn.

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    submitSkillRecord: (params: CallParams<VerifiedProtocolArgs['obj']['submit_skill_record(string,string,uint64,string,uint64)void'] | VerifiedProtocolArgs['tuple']['submit_skill_record(string,string,uint64,string,uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(VerifiedProtocolParamsFactory.submitSkillRecord(params))
    },

    /**
     * Makes a call to the VerifiedProtocol smart contract using the `get_skill_records(address)byte[]` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
    * Return the raw Box bytes for a given wallet address.
    Returns empty bytes if the wallet has no records. Callers can decode the result off-chain by iterating:   1. Read 2-byte big-endian length prefix → record_len   2. Read next record_len bytes → ARC-4 SkillRecord   3. Repeat until buffer exhausted.

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    getSkillRecords: (params: CallParams<VerifiedProtocolArgs['obj']['get_skill_records(address)byte[]'] | VerifiedProtocolArgs['tuple']['get_skill_records(address)byte[]']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(VerifiedProtocolParamsFactory.getSkillRecords(params))
    },

    /**
     * Makes a call to the VerifiedProtocol smart contract using the `get_record_count(address)uint64` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
    * Return the number of skill records stored for a wallet.
    Walks the length-prefixed buffer and counts entries. Returns 0 if the wallet has no Box.

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    getRecordCount: (params: CallParams<VerifiedProtocolArgs['obj']['get_record_count(address)uint64'] | VerifiedProtocolArgs['tuple']['get_record_count(address)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(VerifiedProtocolParamsFactory.getRecordCount(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Makes a clear_state call to an existing instance of the VerifiedProtocol smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the VerifiedProtocol smart contract using the `submit_skill_record(string,string,uint64,string,uint64)void` ABI method.
     *
    * Append a new SkillRecord to the sender's Box.
    • If the Box does not exist yet, it is created with exactly the   bytes of the first length-prefixed record. • If the Box already exists, the new record is appended at the end. • The caller must ensure adequate MBR (Minimum Balance Requirement)   funding for box creation / growth via an accompanying payment txn.

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    submitSkillRecord: async (params: CallParams<VerifiedProtocolArgs['obj']['submit_skill_record(string,string,uint64,string,uint64)void'] | VerifiedProtocolArgs['tuple']['submit_skill_record(string,string,uint64,string,uint64)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(VerifiedProtocolParamsFactory.submitSkillRecord(params))
      return {...result, return: result.return as unknown as (undefined | VerifiedProtocolReturns['submit_skill_record(string,string,uint64,string,uint64)void'])}
    },

    /**
     * Makes a call to the VerifiedProtocol smart contract using the `get_skill_records(address)byte[]` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
    * Return the raw Box bytes for a given wallet address.
    Returns empty bytes if the wallet has no records. Callers can decode the result off-chain by iterating:   1. Read 2-byte big-endian length prefix → record_len   2. Read next record_len bytes → ARC-4 SkillRecord   3. Repeat until buffer exhausted.

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    getSkillRecords: async (params: CallParams<VerifiedProtocolArgs['obj']['get_skill_records(address)byte[]'] | VerifiedProtocolArgs['tuple']['get_skill_records(address)byte[]']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(VerifiedProtocolParamsFactory.getSkillRecords(params))
      return {...result, return: result.return as unknown as (undefined | VerifiedProtocolReturns['get_skill_records(address)byte[]'])}
    },

    /**
     * Makes a call to the VerifiedProtocol smart contract using the `get_record_count(address)uint64` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
    * Return the number of skill records stored for a wallet.
    Walks the length-prefixed buffer and counts entries. Returns 0 if the wallet has no Box.

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    getRecordCount: async (params: CallParams<VerifiedProtocolArgs['obj']['get_record_count(address)uint64'] | VerifiedProtocolArgs['tuple']['get_record_count(address)uint64']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(VerifiedProtocolParamsFactory.getRecordCount(params))
      return {...result, return: result.return as unknown as (undefined | VerifiedProtocolReturns['get_record_count(address)uint64'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new VerifiedProtocolClient(this.appClient.clone(params))
  }

  /**
   * Makes a readonly (simulated) call to the VerifiedProtocol smart contract using the `get_skill_records(address)byte[]` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
  * Return the raw Box bytes for a given wallet address.
  Returns empty bytes if the wallet has no records. Callers can decode the result off-chain by iterating:   1. Read 2-byte big-endian length prefix → record_len   2. Read next record_len bytes → ARC-4 SkillRecord   3. Repeat until buffer exhausted.

   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async getSkillRecords(params: CallParams<VerifiedProtocolArgs['obj']['get_skill_records(address)byte[]'] | VerifiedProtocolArgs['tuple']['get_skill_records(address)byte[]']>) {
    const result = await this.appClient.send.call(VerifiedProtocolParamsFactory.getSkillRecords(params))
    return result.return as unknown as VerifiedProtocolReturns['get_skill_records(address)byte[]']
  }

  /**
   * Makes a readonly (simulated) call to the VerifiedProtocol smart contract using the `get_record_count(address)uint64` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
  * Return the number of skill records stored for a wallet.
  Walks the length-prefixed buffer and counts entries. Returns 0 if the wallet has no Box.

   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async getRecordCount(params: CallParams<VerifiedProtocolArgs['obj']['get_record_count(address)uint64'] | VerifiedProtocolArgs['tuple']['get_record_count(address)uint64']>) {
    const result = await this.appClient.send.call(VerifiedProtocolParamsFactory.getRecordCount(params))
    return result.return as unknown as VerifiedProtocolReturns['get_record_count(address)uint64']
  }

  /**
   * Methods to access state for the current VerifiedProtocol app
   */
  state = {
  }

  public newGroup(): VerifiedProtocolComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a submit_skill_record(string,string,uint64,string,uint64)void method call against the VerifiedProtocol contract
       */
      submitSkillRecord(params: CallParams<VerifiedProtocolArgs['obj']['submit_skill_record(string,string,uint64,string,uint64)void'] | VerifiedProtocolArgs['tuple']['submit_skill_record(string,string,uint64,string,uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.submitSkillRecord(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a get_skill_records(address)byte[] method call against the VerifiedProtocol contract
       */
      getSkillRecords(params: CallParams<VerifiedProtocolArgs['obj']['get_skill_records(address)byte[]'] | VerifiedProtocolArgs['tuple']['get_skill_records(address)byte[]']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.getSkillRecords(params)))
        resultMappers.push((v) => client.decodeReturnValue('get_skill_records(address)byte[]', v))
        return this
      },
      /**
       * Add a get_record_count(address)uint64 method call against the VerifiedProtocol contract
       */
      getRecordCount(params: CallParams<VerifiedProtocolArgs['obj']['get_record_count(address)uint64'] | VerifiedProtocolArgs['tuple']['get_record_count(address)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.getRecordCount(params)))
        resultMappers.push((v) => client.decodeReturnValue('get_record_count(address)uint64', v))
        return this
      },
      /**
       * Add a clear state call to the VerifiedProtocol contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as VerifiedProtocolComposer
  }
}
export type VerifiedProtocolComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the submit_skill_record(string,string,uint64,string,uint64)void ABI method.
   *
  * Append a new SkillRecord to the sender's Box.
  • If the Box does not exist yet, it is created with exactly the   bytes of the first length-prefixed record. • If the Box already exists, the new record is appended at the end. • The caller must ensure adequate MBR (Minimum Balance Requirement)   funding for box creation / growth via an accompanying payment txn.

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  submitSkillRecord(params?: CallParams<VerifiedProtocolArgs['obj']['submit_skill_record(string,string,uint64,string,uint64)void'] | VerifiedProtocolArgs['tuple']['submit_skill_record(string,string,uint64,string,uint64)void']>): VerifiedProtocolComposer<[...TReturns, VerifiedProtocolReturns['submit_skill_record(string,string,uint64,string,uint64)void'] | undefined]>

  /**
   * Calls the get_skill_records(address)byte[] ABI method.
   *
  * Return the raw Box bytes for a given wallet address.
  Returns empty bytes if the wallet has no records. Callers can decode the result off-chain by iterating:   1. Read 2-byte big-endian length prefix → record_len   2. Read next record_len bytes → ARC-4 SkillRecord   3. Repeat until buffer exhausted.

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  getSkillRecords(params?: CallParams<VerifiedProtocolArgs['obj']['get_skill_records(address)byte[]'] | VerifiedProtocolArgs['tuple']['get_skill_records(address)byte[]']>): VerifiedProtocolComposer<[...TReturns, VerifiedProtocolReturns['get_skill_records(address)byte[]'] | undefined]>

  /**
   * Calls the get_record_count(address)uint64 ABI method.
   *
  * Return the number of skill records stored for a wallet.
  Walks the length-prefixed buffer and counts entries. Returns 0 if the wallet has no Box.

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  getRecordCount(params?: CallParams<VerifiedProtocolArgs['obj']['get_record_count(address)uint64'] | VerifiedProtocolArgs['tuple']['get_record_count(address)uint64']>): VerifiedProtocolComposer<[...TReturns, VerifiedProtocolReturns['get_record_count(address)uint64'] | undefined]>

  /**
   * Makes a clear_state call to an existing instance of the VerifiedProtocol smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): VerifiedProtocolComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): VerifiedProtocolComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<VerifiedProtocolComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<VerifiedProtocolComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<VerifiedProtocolComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<VerifiedProtocolComposerResults<TReturns>>
}
export type VerifiedProtocolComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

