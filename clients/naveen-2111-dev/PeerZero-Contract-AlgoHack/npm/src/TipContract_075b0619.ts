/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"TipContract","structs":{},"methods":[{"name":"depositTip","args":[{"type":"uint64","name":"jobId"},{"type":"uint64","name":"amount"},{"type":"address","name":"payer"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}},{"name":"releaseTip","args":[{"type":"uint64","name":"jobId"},{"type":"address","name":"receiver"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}},{"name":"refundTip","args":[{"type":"uint64","name":"jobId"},{"type":"address","name":"caller"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}}],"arcs":[22,28],"networks":{},"state":{"schema":{"global":{"ints":0,"bytes":0},"local":{"ints":0,"bytes":0}},"keys":{"global":{},"local":{},"box":{}},"maps":{"global":{},"local":{},"box":{"jobTips":{"keyType":"uint64","valueType":"uint64","prefix":"dGlw"},"jobOwners":{"keyType":"uint64","valueType":"address","prefix":"b3duZXI="}}}},"bareActions":{"create":["NoOp"],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[204,264,272],"errorMessage":"Box must have value"},{"pc":[201,259],"errorMessage":"No tip for this job"},{"pc":[97,115,133],"errorMessage":"OnCompletion is not NoOp"},{"pc":[280],"errorMessage":"Only job owner can refund"},{"pc":[178],"errorMessage":"Tip already deposited for this job"},{"pc":[275],"errorMessage":"Tip already refunded or released"},{"pc":[207],"errorMessage":"Tip already released"},{"pc":[168],"errorMessage":"Tip must be positive"},{"pc":[159],"errorMessage":"can only call when creating"},{"pc":[100,118,136],"errorMessage":"can only call when not creating"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"events":[]} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


/**
 * The argument types for the TipContract contract
 */
export type TipContractArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'depositTip(uint64,uint64,address)void': {
      jobId: bigint | number
      amount: bigint | number
      payer: string
    }
    'releaseTip(uint64,address)void': {
      jobId: bigint | number
      receiver: string
    }
    'refundTip(uint64,address)void': {
      jobId: bigint | number
      caller: string
    }
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'depositTip(uint64,uint64,address)void': [jobId: bigint | number, amount: bigint | number, payer: string]
    'releaseTip(uint64,address)void': [jobId: bigint | number, receiver: string]
    'refundTip(uint64,address)void': [jobId: bigint | number, caller: string]
  }
}

/**
 * The return type for each method
 */
export type TipContractReturns = {
  'depositTip(uint64,uint64,address)void': void
  'releaseTip(uint64,address)void': void
  'refundTip(uint64,address)void': void
}

/**
 * Defines the types of available calls and state of the TipContract smart contract.
 */
export type TipContractTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'depositTip(uint64,uint64,address)void' | 'depositTip', {
      argsObj: TipContractArgs['obj']['depositTip(uint64,uint64,address)void']
      argsTuple: TipContractArgs['tuple']['depositTip(uint64,uint64,address)void']
      returns: TipContractReturns['depositTip(uint64,uint64,address)void']
    }>
    & Record<'releaseTip(uint64,address)void' | 'releaseTip', {
      argsObj: TipContractArgs['obj']['releaseTip(uint64,address)void']
      argsTuple: TipContractArgs['tuple']['releaseTip(uint64,address)void']
      returns: TipContractReturns['releaseTip(uint64,address)void']
    }>
    & Record<'refundTip(uint64,address)void' | 'refundTip', {
      argsObj: TipContractArgs['obj']['refundTip(uint64,address)void']
      argsTuple: TipContractArgs['tuple']['refundTip(uint64,address)void']
      returns: TipContractReturns['refundTip(uint64,address)void']
    }>
  /**
   * Defines the shape of the state of the application.
   */
  state: {
    box: {
      keys: {}
      maps: {
        jobTips: Map<bigint | number, bigint>
        jobOwners: Map<bigint | number, string>
      }
    }
  }
}

/**
 * Defines the possible abi call signatures.
 */
export type TipContractSignatures = keyof TipContractTypes['methods']
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the TipContract smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends TipContractSignatures> = TipContractTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the TipContract smart contract to the method's return type
 */
export type MethodReturn<TSignature extends TipContractSignatures> = TipContractTypes['methods'][TSignature]['returns']

/**
 * Defines the shape of the keyed box state of the application.
 */
export type BoxKeysState = TipContractTypes['state']['box']['keys']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the TipContract smart contract
 */
export abstract class TipContractParamsFactory {
  /**
   * Constructs a no op call for the depositTip(uint64,uint64,address)void ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static depositTip(params: CallParams<TipContractArgs['obj']['depositTip(uint64,uint64,address)void'] | TipContractArgs['tuple']['depositTip(uint64,uint64,address)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'depositTip(uint64,uint64,address)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.jobId, params.args.amount, params.args.payer],
    }
  }
  /**
   * Constructs a no op call for the releaseTip(uint64,address)void ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static releaseTip(params: CallParams<TipContractArgs['obj']['releaseTip(uint64,address)void'] | TipContractArgs['tuple']['releaseTip(uint64,address)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'releaseTip(uint64,address)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.jobId, params.args.receiver],
    }
  }
  /**
   * Constructs a no op call for the refundTip(uint64,address)void ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static refundTip(params: CallParams<TipContractArgs['obj']['refundTip(uint64,address)void'] | TipContractArgs['tuple']['refundTip(uint64,address)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'refundTip(uint64,address)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.jobId, params.args.caller],
    }
  }
}

/**
 * A client to make calls to the TipContract smart contract
 */
export class TipContractClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `TipContractClient`
   *
   * @param appClient An `AppClient` instance which has been created with the TipContract app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `TipContractClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Returns a new `TipContractClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<TipContractClient> {
    return new TipContractClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `TipContractClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<TipContractClient> {
    return new TipContractClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Makes a clear_state call to an existing instance of the TipContract smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the TipContract smart contract using the `depositTip(uint64,uint64,address)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    depositTip: (params: CallParams<TipContractArgs['obj']['depositTip(uint64,uint64,address)void'] | TipContractArgs['tuple']['depositTip(uint64,uint64,address)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(TipContractParamsFactory.depositTip(params))
    },

    /**
     * Makes a call to the TipContract smart contract using the `releaseTip(uint64,address)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    releaseTip: (params: CallParams<TipContractArgs['obj']['releaseTip(uint64,address)void'] | TipContractArgs['tuple']['releaseTip(uint64,address)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(TipContractParamsFactory.releaseTip(params))
    },

    /**
     * Makes a call to the TipContract smart contract using the `refundTip(uint64,address)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    refundTip: (params: CallParams<TipContractArgs['obj']['refundTip(uint64,address)void'] | TipContractArgs['tuple']['refundTip(uint64,address)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(TipContractParamsFactory.refundTip(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Makes a clear_state call to an existing instance of the TipContract smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the TipContract smart contract using the `depositTip(uint64,uint64,address)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    depositTip: (params: CallParams<TipContractArgs['obj']['depositTip(uint64,uint64,address)void'] | TipContractArgs['tuple']['depositTip(uint64,uint64,address)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(TipContractParamsFactory.depositTip(params))
    },

    /**
     * Makes a call to the TipContract smart contract using the `releaseTip(uint64,address)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    releaseTip: (params: CallParams<TipContractArgs['obj']['releaseTip(uint64,address)void'] | TipContractArgs['tuple']['releaseTip(uint64,address)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(TipContractParamsFactory.releaseTip(params))
    },

    /**
     * Makes a call to the TipContract smart contract using the `refundTip(uint64,address)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    refundTip: (params: CallParams<TipContractArgs['obj']['refundTip(uint64,address)void'] | TipContractArgs['tuple']['refundTip(uint64,address)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(TipContractParamsFactory.refundTip(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Makes a clear_state call to an existing instance of the TipContract smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the TipContract smart contract using the `depositTip(uint64,uint64,address)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    depositTip: async (params: CallParams<TipContractArgs['obj']['depositTip(uint64,uint64,address)void'] | TipContractArgs['tuple']['depositTip(uint64,uint64,address)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(TipContractParamsFactory.depositTip(params))
      return {...result, return: result.return as unknown as (undefined | TipContractReturns['depositTip(uint64,uint64,address)void'])}
    },

    /**
     * Makes a call to the TipContract smart contract using the `releaseTip(uint64,address)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    releaseTip: async (params: CallParams<TipContractArgs['obj']['releaseTip(uint64,address)void'] | TipContractArgs['tuple']['releaseTip(uint64,address)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(TipContractParamsFactory.releaseTip(params))
      return {...result, return: result.return as unknown as (undefined | TipContractReturns['releaseTip(uint64,address)void'])}
    },

    /**
     * Makes a call to the TipContract smart contract using the `refundTip(uint64,address)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    refundTip: async (params: CallParams<TipContractArgs['obj']['refundTip(uint64,address)void'] | TipContractArgs['tuple']['refundTip(uint64,address)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(TipContractParamsFactory.refundTip(params))
      return {...result, return: result.return as unknown as (undefined | TipContractReturns['refundTip(uint64,address)void'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new TipContractClient(this.appClient.clone(params))
  }

  /**
   * Methods to access state for the current TipContract app
   */
  state = {
    /**
     * Methods to access box state for the current TipContract app
     */
    box: {
      /**
       * Get all current keyed values from box state
       */
      getAll: async (): Promise<Partial<Expand<BoxKeysState>>> => {
        const result = await this.appClient.state.box.getAll()
        return {
        }
      },
      /**
       * Get values from the jobTips map in box state
       */
      jobTips: {
        /**
         * Get all current values of the jobTips map in box state
         */
        getMap: async (): Promise<Map<bigint, bigint>> => { return (await this.appClient.state.box.getMap("jobTips")) as Map<bigint, bigint> },
        /**
         * Get a current value of the jobTips map by key from box state
         */
        value: async (key: bigint | number): Promise<bigint | undefined> => { return await this.appClient.state.box.getMapValue("jobTips", key) as bigint | undefined },
      },
      /**
       * Get values from the jobOwners map in box state
       */
      jobOwners: {
        /**
         * Get all current values of the jobOwners map in box state
         */
        getMap: async (): Promise<Map<bigint, string>> => { return (await this.appClient.state.box.getMap("jobOwners")) as Map<bigint, string> },
        /**
         * Get a current value of the jobOwners map by key from box state
         */
        value: async (key: bigint | number): Promise<string | undefined> => { return await this.appClient.state.box.getMapValue("jobOwners", key) as string | undefined },
      },
    },
  }

  public newGroup(): TipContractComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a depositTip(uint64,uint64,address)void method call against the TipContract contract
       */
      depositTip(params: CallParams<TipContractArgs['obj']['depositTip(uint64,uint64,address)void'] | TipContractArgs['tuple']['depositTip(uint64,uint64,address)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.depositTip(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a releaseTip(uint64,address)void method call against the TipContract contract
       */
      releaseTip(params: CallParams<TipContractArgs['obj']['releaseTip(uint64,address)void'] | TipContractArgs['tuple']['releaseTip(uint64,address)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.releaseTip(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a refundTip(uint64,address)void method call against the TipContract contract
       */
      refundTip(params: CallParams<TipContractArgs['obj']['refundTip(uint64,address)void'] | TipContractArgs['tuple']['refundTip(uint64,address)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.refundTip(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a clear state call to the TipContract contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as TipContractComposer
  }
}
export type TipContractComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the depositTip(uint64,uint64,address)void ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  depositTip(params?: CallParams<TipContractArgs['obj']['depositTip(uint64,uint64,address)void'] | TipContractArgs['tuple']['depositTip(uint64,uint64,address)void']>): TipContractComposer<[...TReturns, TipContractReturns['depositTip(uint64,uint64,address)void'] | undefined]>

  /**
   * Calls the releaseTip(uint64,address)void ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  releaseTip(params?: CallParams<TipContractArgs['obj']['releaseTip(uint64,address)void'] | TipContractArgs['tuple']['releaseTip(uint64,address)void']>): TipContractComposer<[...TReturns, TipContractReturns['releaseTip(uint64,address)void'] | undefined]>

  /**
   * Calls the refundTip(uint64,address)void ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  refundTip(params?: CallParams<TipContractArgs['obj']['refundTip(uint64,address)void'] | TipContractArgs['tuple']['refundTip(uint64,address)void']>): TipContractComposer<[...TReturns, TipContractReturns['refundTip(uint64,address)void'] | undefined]>

  /**
   * Makes a clear_state call to an existing instance of the TipContract smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): TipContractComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): TipContractComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<TipContractComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<TipContractComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<TipContractComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<TipContractComposerResults<TReturns>>
}
export type TipContractComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

