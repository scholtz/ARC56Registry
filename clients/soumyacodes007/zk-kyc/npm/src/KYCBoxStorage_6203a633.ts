/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"KYCBoxStorage","structs":{},"methods":[{"name":"create","args":[],"returns":{"type":"void"},"actions":{"create":["NoOp"],"call":[]},"readonly":false,"desc":"Initialize — deployer becomes issuer.","events":[],"recommendations":{}},{"name":"store_blob","args":[{"type":"byte[]","name":"nullifier"},{"type":"byte[]","name":"encrypted_blob"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Store ECIES-encrypted identity blob. Issuer-only.\nencrypted_blob = ECIES.encrypt(identity, issuer_public_key) Generated in user's browser — zero PII transmitted to contract.","events":[],"recommendations":{}},{"name":"get_blob","args":[{"type":"byte[]","name":"nullifier"}],"returns":{"type":"byte[]"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Retrieve encrypted blob. Issuer-only.\nBackend decrypts with reconstructed Shamir issuer key during court order.","events":[],"recommendations":{}},{"name":"delete_blob","args":[{"type":"byte[]","name":"nullifier"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Delete identity blob (DPDP Right to Erasure). Issuer-only.\nCalled on full credential revocation.","events":[],"recommendations":{}},{"name":"has_blob","args":[{"type":"byte[]","name":"nullifier"}],"returns":{"type":"bool"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"desc":"Check whether an encrypted blob exists for this nullifier.","events":[],"recommendations":{}},{"name":"get_total_blobs","args":[],"returns":{"type":"uint64"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"desc":"Total encrypted blobs currently stored.","events":[],"recommendations":{}}],"arcs":[22,28],"desc":"Box storage for ECIES-encrypted identity blobs keyed by nullifier.","networks":{},"state":{"schema":{"global":{"ints":1,"bytes":1},"local":{"ints":0,"bytes":0}},"keys":{"global":{"issuer":{"keyType":"AVMString","valueType":"address","key":"aXNzdWVy"},"total_blobs":{"keyType":"AVMString","valueType":"AVMUint64","key":"dG90YWxfYmxvYnM="}},"local":{},"box":{}},"maps":{"global":{},"local":{},"box":{"blobs":{"keyType":"AVMBytes","valueType":"AVMBytes","prefix":"YmxvYl8="}}}},"bareActions":{"create":[],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[225,273],"errorMessage":"Blob not found"},{"pc":[164,217,265],"errorMessage":"Only issuer"},{"pc":[162,215,263],"errorMessage":"check self.issuer exists"},{"pc":[186,279,286,332],"errorMessage":"check self.total_blobs exists"},{"pc":[130,146,199,247,299],"errorMessage":"invalid array length header"},{"pc":[137,153,206,254,306],"errorMessage":"invalid number of bytes for arc4.dynamic_array<arc4.uint8>"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"events":[]} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


/**
 * The argument types for the KycBoxStorage contract
 */
export type KycBoxStorageArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'create()void': Record<string, never>
    'store_blob(byte[],byte[])void': {
      nullifier: Uint8Array
      encryptedBlob: Uint8Array
    }
    'get_blob(byte[])byte[]': {
      nullifier: Uint8Array
    }
    'delete_blob(byte[])void': {
      nullifier: Uint8Array
    }
    'has_blob(byte[])bool': {
      nullifier: Uint8Array
    }
    'get_total_blobs()uint64': Record<string, never>
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'create()void': []
    'store_blob(byte[],byte[])void': [nullifier: Uint8Array, encryptedBlob: Uint8Array]
    'get_blob(byte[])byte[]': [nullifier: Uint8Array]
    'delete_blob(byte[])void': [nullifier: Uint8Array]
    'has_blob(byte[])bool': [nullifier: Uint8Array]
    'get_total_blobs()uint64': []
  }
}

/**
 * The return type for each method
 */
export type KycBoxStorageReturns = {
  'create()void': void
  'store_blob(byte[],byte[])void': void
  'get_blob(byte[])byte[]': Uint8Array
  'delete_blob(byte[])void': void
  'has_blob(byte[])bool': boolean
  'get_total_blobs()uint64': bigint
}

/**
 * Defines the types of available calls and state of the KycBoxStorage smart contract.
 */
export type KycBoxStorageTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'create()void' | 'create', {
      argsObj: KycBoxStorageArgs['obj']['create()void']
      argsTuple: KycBoxStorageArgs['tuple']['create()void']
      returns: KycBoxStorageReturns['create()void']
    }>
    & Record<'store_blob(byte[],byte[])void' | 'store_blob', {
      argsObj: KycBoxStorageArgs['obj']['store_blob(byte[],byte[])void']
      argsTuple: KycBoxStorageArgs['tuple']['store_blob(byte[],byte[])void']
      returns: KycBoxStorageReturns['store_blob(byte[],byte[])void']
    }>
    & Record<'get_blob(byte[])byte[]' | 'get_blob', {
      argsObj: KycBoxStorageArgs['obj']['get_blob(byte[])byte[]']
      argsTuple: KycBoxStorageArgs['tuple']['get_blob(byte[])byte[]']
      returns: KycBoxStorageReturns['get_blob(byte[])byte[]']
    }>
    & Record<'delete_blob(byte[])void' | 'delete_blob', {
      argsObj: KycBoxStorageArgs['obj']['delete_blob(byte[])void']
      argsTuple: KycBoxStorageArgs['tuple']['delete_blob(byte[])void']
      returns: KycBoxStorageReturns['delete_blob(byte[])void']
    }>
    & Record<'has_blob(byte[])bool' | 'has_blob', {
      argsObj: KycBoxStorageArgs['obj']['has_blob(byte[])bool']
      argsTuple: KycBoxStorageArgs['tuple']['has_blob(byte[])bool']
      returns: KycBoxStorageReturns['has_blob(byte[])bool']
    }>
    & Record<'get_total_blobs()uint64' | 'get_total_blobs', {
      argsObj: KycBoxStorageArgs['obj']['get_total_blobs()uint64']
      argsTuple: KycBoxStorageArgs['tuple']['get_total_blobs()uint64']
      returns: KycBoxStorageReturns['get_total_blobs()uint64']
    }>
  /**
   * Defines the shape of the state of the application.
   */
  state: {
    global: {
      keys: {
        issuer: string
        totalBlobs: bigint
      }
      maps: {}
    }
    box: {
      keys: {}
      maps: {
        blobs: Map<Uint8Array | string, Uint8Array>
      }
    }
  }
}

/**
 * Defines the possible abi call signatures.
 */
export type KycBoxStorageSignatures = keyof KycBoxStorageTypes['methods']
/**
 * Defines the possible abi call signatures for methods that return a non-void value.
 */
export type KycBoxStorageNonVoidMethodSignatures = keyof KycBoxStorageTypes['methods'] extends infer T ? T extends keyof KycBoxStorageTypes['methods'] ? MethodReturn<T> extends void ? never : T  : never : never
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the KycBoxStorage smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends KycBoxStorageSignatures> = KycBoxStorageTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the KycBoxStorage smart contract to the method's return type
 */
export type MethodReturn<TSignature extends KycBoxStorageSignatures> = KycBoxStorageTypes['methods'][TSignature]['returns']

/**
 * Defines the shape of the keyed global state of the application.
 */
export type GlobalKeysState = KycBoxStorageTypes['state']['global']['keys']

/**
 * Defines the shape of the keyed box state of the application.
 */
export type BoxKeysState = KycBoxStorageTypes['state']['box']['keys']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the KycBoxStorage smart contract
 */
export abstract class KycBoxStorageParamsFactory {
  /**
   * Constructs a no op call for the store_blob(byte[],byte[])void ABI method
   *
  * Store ECIES-encrypted identity blob. Issuer-only.
  encrypted_blob = ECIES.encrypt(identity, issuer_public_key) Generated in user's browser — zero PII transmitted to contract.

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static storeBlob(params: CallParams<KycBoxStorageArgs['obj']['store_blob(byte[],byte[])void'] | KycBoxStorageArgs['tuple']['store_blob(byte[],byte[])void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'store_blob(byte[],byte[])void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.nullifier, params.args.encryptedBlob],
    }
  }
  /**
   * Constructs a no op call for the get_blob(byte[])byte[] ABI method
   *
  * Retrieve encrypted blob. Issuer-only.
  Backend decrypts with reconstructed Shamir issuer key during court order.

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static getBlob(params: CallParams<KycBoxStorageArgs['obj']['get_blob(byte[])byte[]'] | KycBoxStorageArgs['tuple']['get_blob(byte[])byte[]']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'get_blob(byte[])byte[]' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.nullifier],
    }
  }
  /**
   * Constructs a no op call for the delete_blob(byte[])void ABI method
   *
  * Delete identity blob (DPDP Right to Erasure). Issuer-only.
  Called on full credential revocation.

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static deleteBlob(params: CallParams<KycBoxStorageArgs['obj']['delete_blob(byte[])void'] | KycBoxStorageArgs['tuple']['delete_blob(byte[])void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'delete_blob(byte[])void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.nullifier],
    }
  }
  /**
   * Constructs a no op call for the has_blob(byte[])bool ABI method
   *
   * Check whether an encrypted blob exists for this nullifier.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static hasBlob(params: CallParams<KycBoxStorageArgs['obj']['has_blob(byte[])bool'] | KycBoxStorageArgs['tuple']['has_blob(byte[])bool']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'has_blob(byte[])bool' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.nullifier],
    }
  }
  /**
   * Constructs a no op call for the get_total_blobs()uint64 ABI method
   *
   * Total encrypted blobs currently stored.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static getTotalBlobs(params: CallParams<KycBoxStorageArgs['obj']['get_total_blobs()uint64'] | KycBoxStorageArgs['tuple']['get_total_blobs()uint64']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'get_total_blobs()uint64' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
}

/**
 * A client to make calls to the KYCBoxStorage smart contract
 */
export class KycBoxStorageClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `KycBoxStorageClient`
   *
   * @param appClient An `AppClient` instance which has been created with the KycBoxStorage app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `KycBoxStorageClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Checks for decode errors on the given return value and maps the return value to the return type for the given method
   * @returns The typed return value or undefined if there was no value
   */
  decodeReturnValue<TSignature extends KycBoxStorageNonVoidMethodSignatures>(method: TSignature, returnValue: ABIReturn | undefined) {
    return returnValue !== undefined ? getArc56ReturnValue<MethodReturn<TSignature>>(returnValue, this.appClient.getABIMethod(method), APP_SPEC.structs) : undefined
  }

  /**
   * Returns a new `KycBoxStorageClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<KycBoxStorageClient> {
    return new KycBoxStorageClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `KycBoxStorageClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<KycBoxStorageClient> {
    return new KycBoxStorageClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Makes a clear_state call to an existing instance of the KYCBoxStorage smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the KYCBoxStorage smart contract using the `store_blob(byte[],byte[])void` ABI method.
     *
    * Store ECIES-encrypted identity blob. Issuer-only.
    encrypted_blob = ECIES.encrypt(identity, issuer_public_key) Generated in user's browser — zero PII transmitted to contract.

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    storeBlob: (params: CallParams<KycBoxStorageArgs['obj']['store_blob(byte[],byte[])void'] | KycBoxStorageArgs['tuple']['store_blob(byte[],byte[])void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(KycBoxStorageParamsFactory.storeBlob(params))
    },

    /**
     * Makes a call to the KYCBoxStorage smart contract using the `get_blob(byte[])byte[]` ABI method.
     *
    * Retrieve encrypted blob. Issuer-only.
    Backend decrypts with reconstructed Shamir issuer key during court order.

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    getBlob: (params: CallParams<KycBoxStorageArgs['obj']['get_blob(byte[])byte[]'] | KycBoxStorageArgs['tuple']['get_blob(byte[])byte[]']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(KycBoxStorageParamsFactory.getBlob(params))
    },

    /**
     * Makes a call to the KYCBoxStorage smart contract using the `delete_blob(byte[])void` ABI method.
     *
    * Delete identity blob (DPDP Right to Erasure). Issuer-only.
    Called on full credential revocation.

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    deleteBlob: (params: CallParams<KycBoxStorageArgs['obj']['delete_blob(byte[])void'] | KycBoxStorageArgs['tuple']['delete_blob(byte[])void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(KycBoxStorageParamsFactory.deleteBlob(params))
    },

    /**
     * Makes a call to the KYCBoxStorage smart contract using the `has_blob(byte[])bool` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * Check whether an encrypted blob exists for this nullifier.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    hasBlob: (params: CallParams<KycBoxStorageArgs['obj']['has_blob(byte[])bool'] | KycBoxStorageArgs['tuple']['has_blob(byte[])bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(KycBoxStorageParamsFactory.hasBlob(params))
    },

    /**
     * Makes a call to the KYCBoxStorage smart contract using the `get_total_blobs()uint64` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * Total encrypted blobs currently stored.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    getTotalBlobs: (params: CallParams<KycBoxStorageArgs['obj']['get_total_blobs()uint64'] | KycBoxStorageArgs['tuple']['get_total_blobs()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(KycBoxStorageParamsFactory.getTotalBlobs(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Makes a clear_state call to an existing instance of the KYCBoxStorage smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the KYCBoxStorage smart contract using the `store_blob(byte[],byte[])void` ABI method.
     *
    * Store ECIES-encrypted identity blob. Issuer-only.
    encrypted_blob = ECIES.encrypt(identity, issuer_public_key) Generated in user's browser — zero PII transmitted to contract.

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    storeBlob: (params: CallParams<KycBoxStorageArgs['obj']['store_blob(byte[],byte[])void'] | KycBoxStorageArgs['tuple']['store_blob(byte[],byte[])void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(KycBoxStorageParamsFactory.storeBlob(params))
    },

    /**
     * Makes a call to the KYCBoxStorage smart contract using the `get_blob(byte[])byte[]` ABI method.
     *
    * Retrieve encrypted blob. Issuer-only.
    Backend decrypts with reconstructed Shamir issuer key during court order.

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    getBlob: (params: CallParams<KycBoxStorageArgs['obj']['get_blob(byte[])byte[]'] | KycBoxStorageArgs['tuple']['get_blob(byte[])byte[]']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(KycBoxStorageParamsFactory.getBlob(params))
    },

    /**
     * Makes a call to the KYCBoxStorage smart contract using the `delete_blob(byte[])void` ABI method.
     *
    * Delete identity blob (DPDP Right to Erasure). Issuer-only.
    Called on full credential revocation.

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    deleteBlob: (params: CallParams<KycBoxStorageArgs['obj']['delete_blob(byte[])void'] | KycBoxStorageArgs['tuple']['delete_blob(byte[])void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(KycBoxStorageParamsFactory.deleteBlob(params))
    },

    /**
     * Makes a call to the KYCBoxStorage smart contract using the `has_blob(byte[])bool` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * Check whether an encrypted blob exists for this nullifier.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    hasBlob: (params: CallParams<KycBoxStorageArgs['obj']['has_blob(byte[])bool'] | KycBoxStorageArgs['tuple']['has_blob(byte[])bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(KycBoxStorageParamsFactory.hasBlob(params))
    },

    /**
     * Makes a call to the KYCBoxStorage smart contract using the `get_total_blobs()uint64` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * Total encrypted blobs currently stored.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    getTotalBlobs: (params: CallParams<KycBoxStorageArgs['obj']['get_total_blobs()uint64'] | KycBoxStorageArgs['tuple']['get_total_blobs()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(KycBoxStorageParamsFactory.getTotalBlobs(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Makes a clear_state call to an existing instance of the KYCBoxStorage smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the KYCBoxStorage smart contract using the `store_blob(byte[],byte[])void` ABI method.
     *
    * Store ECIES-encrypted identity blob. Issuer-only.
    encrypted_blob = ECIES.encrypt(identity, issuer_public_key) Generated in user's browser — zero PII transmitted to contract.

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    storeBlob: async (params: CallParams<KycBoxStorageArgs['obj']['store_blob(byte[],byte[])void'] | KycBoxStorageArgs['tuple']['store_blob(byte[],byte[])void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(KycBoxStorageParamsFactory.storeBlob(params))
      return {...result, return: result.return as unknown as (undefined | KycBoxStorageReturns['store_blob(byte[],byte[])void'])}
    },

    /**
     * Makes a call to the KYCBoxStorage smart contract using the `get_blob(byte[])byte[]` ABI method.
     *
    * Retrieve encrypted blob. Issuer-only.
    Backend decrypts with reconstructed Shamir issuer key during court order.

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    getBlob: async (params: CallParams<KycBoxStorageArgs['obj']['get_blob(byte[])byte[]'] | KycBoxStorageArgs['tuple']['get_blob(byte[])byte[]']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(KycBoxStorageParamsFactory.getBlob(params))
      return {...result, return: result.return as unknown as (undefined | KycBoxStorageReturns['get_blob(byte[])byte[]'])}
    },

    /**
     * Makes a call to the KYCBoxStorage smart contract using the `delete_blob(byte[])void` ABI method.
     *
    * Delete identity blob (DPDP Right to Erasure). Issuer-only.
    Called on full credential revocation.

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    deleteBlob: async (params: CallParams<KycBoxStorageArgs['obj']['delete_blob(byte[])void'] | KycBoxStorageArgs['tuple']['delete_blob(byte[])void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(KycBoxStorageParamsFactory.deleteBlob(params))
      return {...result, return: result.return as unknown as (undefined | KycBoxStorageReturns['delete_blob(byte[])void'])}
    },

    /**
     * Makes a call to the KYCBoxStorage smart contract using the `has_blob(byte[])bool` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * Check whether an encrypted blob exists for this nullifier.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    hasBlob: async (params: CallParams<KycBoxStorageArgs['obj']['has_blob(byte[])bool'] | KycBoxStorageArgs['tuple']['has_blob(byte[])bool']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(KycBoxStorageParamsFactory.hasBlob(params))
      return {...result, return: result.return as unknown as (undefined | KycBoxStorageReturns['has_blob(byte[])bool'])}
    },

    /**
     * Makes a call to the KYCBoxStorage smart contract using the `get_total_blobs()uint64` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * Total encrypted blobs currently stored.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    getTotalBlobs: async (params: CallParams<KycBoxStorageArgs['obj']['get_total_blobs()uint64'] | KycBoxStorageArgs['tuple']['get_total_blobs()uint64']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(KycBoxStorageParamsFactory.getTotalBlobs(params))
      return {...result, return: result.return as unknown as (undefined | KycBoxStorageReturns['get_total_blobs()uint64'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new KycBoxStorageClient(this.appClient.clone(params))
  }

  /**
   * Makes a readonly (simulated) call to the KYCBoxStorage smart contract using the `has_blob(byte[])bool` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * Check whether an encrypted blob exists for this nullifier.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async hasBlob(params: CallParams<KycBoxStorageArgs['obj']['has_blob(byte[])bool'] | KycBoxStorageArgs['tuple']['has_blob(byte[])bool']>) {
    const result = await this.appClient.send.call(KycBoxStorageParamsFactory.hasBlob(params))
    return result.return as unknown as KycBoxStorageReturns['has_blob(byte[])bool']
  }

  /**
   * Makes a readonly (simulated) call to the KYCBoxStorage smart contract using the `get_total_blobs()uint64` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * Total encrypted blobs currently stored.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async getTotalBlobs(params: CallParams<KycBoxStorageArgs['obj']['get_total_blobs()uint64'] | KycBoxStorageArgs['tuple']['get_total_blobs()uint64']> = {args: []}) {
    const result = await this.appClient.send.call(KycBoxStorageParamsFactory.getTotalBlobs(params))
    return result.return as unknown as KycBoxStorageReturns['get_total_blobs()uint64']
  }

  /**
   * Methods to access state for the current KYCBoxStorage app
   */
  state = {
    /**
     * Methods to access global state for the current KYCBoxStorage app
     */
    global: {
      /**
       * Get all current keyed values from global state
       */
      getAll: async (): Promise<Partial<Expand<GlobalKeysState>>> => {
        const result = await this.appClient.state.global.getAll()
        return {
          issuer: result.issuer,
          totalBlobs: result.total_blobs,
        }
      },
      /**
       * Get the current value of the issuer key in global state
       */
      issuer: async (): Promise<string | undefined> => { return (await this.appClient.state.global.getValue("issuer")) as string | undefined },
      /**
       * Get the current value of the total_blobs key in global state
       */
      totalBlobs: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("total_blobs")) as bigint | undefined },
    },
    /**
     * Methods to access box state for the current KYCBoxStorage app
     */
    box: {
      /**
       * Get all current keyed values from box state
       */
      getAll: async (): Promise<Partial<Expand<BoxKeysState>>> => {
        const result = await this.appClient.state.box.getAll()
        return {
        }
      },
      /**
       * Get values from the blobs map in box state
       */
      blobs: {
        /**
         * Get all current values of the blobs map in box state
         */
        getMap: async (): Promise<Map<Uint8Array, Uint8Array>> => { return (await this.appClient.state.box.getMap("blobs")) as Map<Uint8Array, Uint8Array> },
        /**
         * Get a current value of the blobs map by key from box state
         */
        value: async (key: Uint8Array | string): Promise<Uint8Array | undefined> => { return await this.appClient.state.box.getMapValue("blobs", key) as Uint8Array | undefined },
      },
    },
  }

  public newGroup(): KycBoxStorageComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a store_blob(byte[],byte[])void method call against the KYCBoxStorage contract
       */
      storeBlob(params: CallParams<KycBoxStorageArgs['obj']['store_blob(byte[],byte[])void'] | KycBoxStorageArgs['tuple']['store_blob(byte[],byte[])void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.storeBlob(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a get_blob(byte[])byte[] method call against the KYCBoxStorage contract
       */
      getBlob(params: CallParams<KycBoxStorageArgs['obj']['get_blob(byte[])byte[]'] | KycBoxStorageArgs['tuple']['get_blob(byte[])byte[]']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.getBlob(params)))
        resultMappers.push((v) => client.decodeReturnValue('get_blob(byte[])byte[]', v))
        return this
      },
      /**
       * Add a delete_blob(byte[])void method call against the KYCBoxStorage contract
       */
      deleteBlob(params: CallParams<KycBoxStorageArgs['obj']['delete_blob(byte[])void'] | KycBoxStorageArgs['tuple']['delete_blob(byte[])void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.deleteBlob(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a has_blob(byte[])bool method call against the KYCBoxStorage contract
       */
      hasBlob(params: CallParams<KycBoxStorageArgs['obj']['has_blob(byte[])bool'] | KycBoxStorageArgs['tuple']['has_blob(byte[])bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.hasBlob(params)))
        resultMappers.push((v) => client.decodeReturnValue('has_blob(byte[])bool', v))
        return this
      },
      /**
       * Add a get_total_blobs()uint64 method call against the KYCBoxStorage contract
       */
      getTotalBlobs(params: CallParams<KycBoxStorageArgs['obj']['get_total_blobs()uint64'] | KycBoxStorageArgs['tuple']['get_total_blobs()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.getTotalBlobs(params)))
        resultMappers.push((v) => client.decodeReturnValue('get_total_blobs()uint64', v))
        return this
      },
      /**
       * Add a clear state call to the KYCBoxStorage contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as KycBoxStorageComposer
  }
}
export type KycBoxStorageComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the store_blob(byte[],byte[])void ABI method.
   *
  * Store ECIES-encrypted identity blob. Issuer-only.
  encrypted_blob = ECIES.encrypt(identity, issuer_public_key) Generated in user's browser — zero PII transmitted to contract.

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  storeBlob(params?: CallParams<KycBoxStorageArgs['obj']['store_blob(byte[],byte[])void'] | KycBoxStorageArgs['tuple']['store_blob(byte[],byte[])void']>): KycBoxStorageComposer<[...TReturns, KycBoxStorageReturns['store_blob(byte[],byte[])void'] | undefined]>

  /**
   * Calls the get_blob(byte[])byte[] ABI method.
   *
  * Retrieve encrypted blob. Issuer-only.
  Backend decrypts with reconstructed Shamir issuer key during court order.

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  getBlob(params?: CallParams<KycBoxStorageArgs['obj']['get_blob(byte[])byte[]'] | KycBoxStorageArgs['tuple']['get_blob(byte[])byte[]']>): KycBoxStorageComposer<[...TReturns, KycBoxStorageReturns['get_blob(byte[])byte[]'] | undefined]>

  /**
   * Calls the delete_blob(byte[])void ABI method.
   *
  * Delete identity blob (DPDP Right to Erasure). Issuer-only.
  Called on full credential revocation.

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  deleteBlob(params?: CallParams<KycBoxStorageArgs['obj']['delete_blob(byte[])void'] | KycBoxStorageArgs['tuple']['delete_blob(byte[])void']>): KycBoxStorageComposer<[...TReturns, KycBoxStorageReturns['delete_blob(byte[])void'] | undefined]>

  /**
   * Calls the has_blob(byte[])bool ABI method.
   *
   * Check whether an encrypted blob exists for this nullifier.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  hasBlob(params?: CallParams<KycBoxStorageArgs['obj']['has_blob(byte[])bool'] | KycBoxStorageArgs['tuple']['has_blob(byte[])bool']>): KycBoxStorageComposer<[...TReturns, KycBoxStorageReturns['has_blob(byte[])bool'] | undefined]>

  /**
   * Calls the get_total_blobs()uint64 ABI method.
   *
   * Total encrypted blobs currently stored.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  getTotalBlobs(params?: CallParams<KycBoxStorageArgs['obj']['get_total_blobs()uint64'] | KycBoxStorageArgs['tuple']['get_total_blobs()uint64']>): KycBoxStorageComposer<[...TReturns, KycBoxStorageReturns['get_total_blobs()uint64'] | undefined]>

  /**
   * Makes a clear_state call to an existing instance of the KYCBoxStorage smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): KycBoxStorageComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): KycBoxStorageComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<KycBoxStorageComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<KycBoxStorageComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<KycBoxStorageComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<KycBoxStorageComposerResults<TReturns>>
}
export type KycBoxStorageComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

