/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"CarbonFootprintContract","structs":{},"methods":[{"name":"opt_in","args":[],"returns":{"type":"void"},"actions":{"create":[],"call":["OptIn"]},"readonly":false,"desc":"Opt into the contract to enable local state storage","events":[],"recommendations":{}},{"name":"register_device","args":[{"type":"string","name":"device_id"}],"returns":{"type":"uint64"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Register a new IoT device to the system","events":[],"recommendations":{}},{"name":"record_emission","args":[{"type":"string","name":"device_id"},{"type":"uint64","name":"emission_amount"}],"returns":{"type":"uint64"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Record carbon emission data from an IoT device","events":[],"recommendations":{}},{"name":"transfer_emissions","args":[{"type":"address","name":"receiver"},{"type":"uint64","name":"emission_amount"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Transfer emission credits from sender to receiver","events":[],"recommendations":{}},{"name":"get_total_emissions","args":[],"returns":{"type":"uint64"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"desc":"Get the total carbon emissions recorded","events":[],"recommendations":{}},{"name":"get_device_emissions","args":[],"returns":{"type":"uint64"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"desc":"Get emissions for the calling device (sender)","events":[],"recommendations":{}},{"name":"get_emissions_for_account","args":[{"type":"address","name":"account"}],"returns":{"type":"uint64"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"desc":"Get emissions for a specific account","events":[],"recommendations":{}},{"name":"get_device_count","args":[],"returns":{"type":"uint64"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"desc":"Get the total number of registered devices","events":[],"recommendations":{}},{"name":"get_sender_address","args":[],"returns":{"type":"address"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"desc":"Get the address of the transaction sender","events":[],"recommendations":{}}],"arcs":[22,28],"networks":{},"state":{"schema":{"global":{"ints":2,"bytes":0},"local":{"ints":1,"bytes":0}},"keys":{"global":{"total_emissions":{"keyType":"AVMString","valueType":"AVMUint64","key":"dG90YWxfZW1pc3Npb25z","desc":"Total carbon emissions from all devices"},"device_count":{"keyType":"AVMString","valueType":"AVMUint64","key":"ZGV2aWNlX2NvdW50","desc":"Number of registered IoT devices"}},"local":{"device_emissions":{"keyType":"AVMString","valueType":"AVMUint64","key":"ZGV2aWNlX2VtaXNzaW9ucw==","desc":"Emissions tracked per device"}},"box":{}},"maps":{"global":{},"local":{},"box":{}}},"bareActions":{"create":["NoOp"],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[300],"errorMessage":"Insufficient emissions to transfer"},{"pc":[92],"errorMessage":"OnCompletion must be NoOp"},{"pc":[178],"errorMessage":"OnCompletion must be NoOp && can only call when creating"},{"pc":[167],"errorMessage":"OnCompletion must be OptIn && can only call when not creating"},{"pc":[203,373],"errorMessage":"check self.device_count exists"},{"pc":[257,293,307,319,342,362],"errorMessage":"check self.device_emissions exists for account"},{"pc":[240,329],"errorMessage":"check self.total_emissions exists"},{"pc":[193,222],"errorMessage":"invalid array length header"},{"pc":[199,228],"errorMessage":"invalid number of bytes for arc4.dynamic_array<arc4.uint8>"},{"pc":[279,358],"errorMessage":"invalid number of bytes for arc4.static_array<arc4.uint8, 32>"},{"pc":[236,287],"errorMessage":"invalid number of bytes for arc4.uint64"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"events":[]} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


/**
 * The argument types for the CarbonFootprintContract contract
 */
export type CarbonFootprintContractArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'opt_in()void': Record<string, never>
    'register_device(string)uint64': {
      deviceId: string
    }
    'record_emission(string,uint64)uint64': {
      deviceId: string
      emissionAmount: bigint | number
    }
    'transfer_emissions(address,uint64)void': {
      receiver: string
      emissionAmount: bigint | number
    }
    'get_total_emissions()uint64': Record<string, never>
    'get_device_emissions()uint64': Record<string, never>
    'get_emissions_for_account(address)uint64': {
      account: string
    }
    'get_device_count()uint64': Record<string, never>
    'get_sender_address()address': Record<string, never>
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'opt_in()void': []
    'register_device(string)uint64': [deviceId: string]
    'record_emission(string,uint64)uint64': [deviceId: string, emissionAmount: bigint | number]
    'transfer_emissions(address,uint64)void': [receiver: string, emissionAmount: bigint | number]
    'get_total_emissions()uint64': []
    'get_device_emissions()uint64': []
    'get_emissions_for_account(address)uint64': [account: string]
    'get_device_count()uint64': []
    'get_sender_address()address': []
  }
}

/**
 * The return type for each method
 */
export type CarbonFootprintContractReturns = {
  'opt_in()void': void
  'register_device(string)uint64': bigint
  'record_emission(string,uint64)uint64': bigint
  'transfer_emissions(address,uint64)void': void
  'get_total_emissions()uint64': bigint
  'get_device_emissions()uint64': bigint
  'get_emissions_for_account(address)uint64': bigint
  'get_device_count()uint64': bigint
  'get_sender_address()address': string
}

/**
 * Defines the types of available calls and state of the CarbonFootprintContract smart contract.
 */
export type CarbonFootprintContractTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'opt_in()void' | 'opt_in', {
      argsObj: CarbonFootprintContractArgs['obj']['opt_in()void']
      argsTuple: CarbonFootprintContractArgs['tuple']['opt_in()void']
      returns: CarbonFootprintContractReturns['opt_in()void']
    }>
    & Record<'register_device(string)uint64' | 'register_device', {
      argsObj: CarbonFootprintContractArgs['obj']['register_device(string)uint64']
      argsTuple: CarbonFootprintContractArgs['tuple']['register_device(string)uint64']
      returns: CarbonFootprintContractReturns['register_device(string)uint64']
    }>
    & Record<'record_emission(string,uint64)uint64' | 'record_emission', {
      argsObj: CarbonFootprintContractArgs['obj']['record_emission(string,uint64)uint64']
      argsTuple: CarbonFootprintContractArgs['tuple']['record_emission(string,uint64)uint64']
      returns: CarbonFootprintContractReturns['record_emission(string,uint64)uint64']
    }>
    & Record<'transfer_emissions(address,uint64)void' | 'transfer_emissions', {
      argsObj: CarbonFootprintContractArgs['obj']['transfer_emissions(address,uint64)void']
      argsTuple: CarbonFootprintContractArgs['tuple']['transfer_emissions(address,uint64)void']
      returns: CarbonFootprintContractReturns['transfer_emissions(address,uint64)void']
    }>
    & Record<'get_total_emissions()uint64' | 'get_total_emissions', {
      argsObj: CarbonFootprintContractArgs['obj']['get_total_emissions()uint64']
      argsTuple: CarbonFootprintContractArgs['tuple']['get_total_emissions()uint64']
      returns: CarbonFootprintContractReturns['get_total_emissions()uint64']
    }>
    & Record<'get_device_emissions()uint64' | 'get_device_emissions', {
      argsObj: CarbonFootprintContractArgs['obj']['get_device_emissions()uint64']
      argsTuple: CarbonFootprintContractArgs['tuple']['get_device_emissions()uint64']
      returns: CarbonFootprintContractReturns['get_device_emissions()uint64']
    }>
    & Record<'get_emissions_for_account(address)uint64' | 'get_emissions_for_account', {
      argsObj: CarbonFootprintContractArgs['obj']['get_emissions_for_account(address)uint64']
      argsTuple: CarbonFootprintContractArgs['tuple']['get_emissions_for_account(address)uint64']
      returns: CarbonFootprintContractReturns['get_emissions_for_account(address)uint64']
    }>
    & Record<'get_device_count()uint64' | 'get_device_count', {
      argsObj: CarbonFootprintContractArgs['obj']['get_device_count()uint64']
      argsTuple: CarbonFootprintContractArgs['tuple']['get_device_count()uint64']
      returns: CarbonFootprintContractReturns['get_device_count()uint64']
    }>
    & Record<'get_sender_address()address' | 'get_sender_address', {
      argsObj: CarbonFootprintContractArgs['obj']['get_sender_address()address']
      argsTuple: CarbonFootprintContractArgs['tuple']['get_sender_address()address']
      returns: CarbonFootprintContractReturns['get_sender_address()address']
    }>
  /**
   * Defines the shape of the state of the application.
   */
  state: {
    global: {
      keys: {
        /**
         * Total carbon emissions from all devices
         */
        totalEmissions: bigint
        /**
         * Number of registered IoT devices
         */
        deviceCount: bigint
      }
      maps: {}
    }
    local: {
      keys: {
        /**
         * Emissions tracked per device
         */
        deviceEmissions: bigint
      }
      maps: {}
    }
  }
}

/**
 * Defines the possible abi call signatures.
 */
export type CarbonFootprintContractSignatures = keyof CarbonFootprintContractTypes['methods']
/**
 * Defines the possible abi call signatures for methods that return a non-void value.
 */
export type CarbonFootprintContractNonVoidMethodSignatures = keyof CarbonFootprintContractTypes['methods'] extends infer T ? T extends keyof CarbonFootprintContractTypes['methods'] ? MethodReturn<T> extends void ? never : T  : never : never
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the CarbonFootprintContract smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends CarbonFootprintContractSignatures> = CarbonFootprintContractTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the CarbonFootprintContract smart contract to the method's return type
 */
export type MethodReturn<TSignature extends CarbonFootprintContractSignatures> = CarbonFootprintContractTypes['methods'][TSignature]['returns']

/**
 * Defines the shape of the keyed global state of the application.
 */
export type GlobalKeysState = CarbonFootprintContractTypes['state']['global']['keys']

/**
 * Defines the shape of the keyed local state of the application.
 */
export type LocalKeysState = CarbonFootprintContractTypes['state']['local']['keys']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the CarbonFootprintContract smart contract
 */
export abstract class CarbonFootprintContractParamsFactory {
  /**
   * Gets available optIn ABI call param factories
   */
  static get optIn() {
    return {
      /**
       * Constructs opt-in ABI call params for the CarbonFootprintContract smart contract using the opt_in()void ABI method
       *
       * @param params Parameters for the call
       * @returns An `AppClientMethodCallParams` object for the call
       */
      optIn(params: CallParams<CarbonFootprintContractArgs['obj']['opt_in()void'] | CarbonFootprintContractArgs['tuple']['opt_in()void']>): AppClientMethodCallParams {
        return {
          ...params,
          method: 'opt_in()void' as const,
          args: Array.isArray(params.args) ? params.args : [],
        }
      },
    }
  }

  /**
   * Constructs a no op call for the register_device(string)uint64 ABI method
   *
   * Register a new IoT device to the system
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static registerDevice(params: CallParams<CarbonFootprintContractArgs['obj']['register_device(string)uint64'] | CarbonFootprintContractArgs['tuple']['register_device(string)uint64']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'register_device(string)uint64' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.deviceId],
    }
  }
  /**
   * Constructs a no op call for the record_emission(string,uint64)uint64 ABI method
   *
   * Record carbon emission data from an IoT device
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static recordEmission(params: CallParams<CarbonFootprintContractArgs['obj']['record_emission(string,uint64)uint64'] | CarbonFootprintContractArgs['tuple']['record_emission(string,uint64)uint64']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'record_emission(string,uint64)uint64' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.deviceId, params.args.emissionAmount],
    }
  }
  /**
   * Constructs a no op call for the transfer_emissions(address,uint64)void ABI method
   *
   * Transfer emission credits from sender to receiver
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static transferEmissions(params: CallParams<CarbonFootprintContractArgs['obj']['transfer_emissions(address,uint64)void'] | CarbonFootprintContractArgs['tuple']['transfer_emissions(address,uint64)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'transfer_emissions(address,uint64)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.receiver, params.args.emissionAmount],
    }
  }
  /**
   * Constructs a no op call for the get_total_emissions()uint64 ABI method
   *
   * Get the total carbon emissions recorded
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static getTotalEmissions(params: CallParams<CarbonFootprintContractArgs['obj']['get_total_emissions()uint64'] | CarbonFootprintContractArgs['tuple']['get_total_emissions()uint64']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'get_total_emissions()uint64' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the get_device_emissions()uint64 ABI method
   *
   * Get emissions for the calling device (sender)
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static getDeviceEmissions(params: CallParams<CarbonFootprintContractArgs['obj']['get_device_emissions()uint64'] | CarbonFootprintContractArgs['tuple']['get_device_emissions()uint64']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'get_device_emissions()uint64' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the get_emissions_for_account(address)uint64 ABI method
   *
   * Get emissions for a specific account
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static getEmissionsForAccount(params: CallParams<CarbonFootprintContractArgs['obj']['get_emissions_for_account(address)uint64'] | CarbonFootprintContractArgs['tuple']['get_emissions_for_account(address)uint64']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'get_emissions_for_account(address)uint64' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.account],
    }
  }
  /**
   * Constructs a no op call for the get_device_count()uint64 ABI method
   *
   * Get the total number of registered devices
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static getDeviceCount(params: CallParams<CarbonFootprintContractArgs['obj']['get_device_count()uint64'] | CarbonFootprintContractArgs['tuple']['get_device_count()uint64']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'get_device_count()uint64' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the get_sender_address()address ABI method
   *
   * Get the address of the transaction sender
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static getSenderAddress(params: CallParams<CarbonFootprintContractArgs['obj']['get_sender_address()address'] | CarbonFootprintContractArgs['tuple']['get_sender_address()address']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'get_sender_address()address' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
}

/**
 * A client to make calls to the CarbonFootprintContract smart contract
 */
export class CarbonFootprintContractClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `CarbonFootprintContractClient`
   *
   * @param appClient An `AppClient` instance which has been created with the CarbonFootprintContract app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `CarbonFootprintContractClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Checks for decode errors on the given return value and maps the return value to the return type for the given method
   * @returns The typed return value or undefined if there was no value
   */
  decodeReturnValue<TSignature extends CarbonFootprintContractNonVoidMethodSignatures>(method: TSignature, returnValue: ABIReturn | undefined) {
    return returnValue !== undefined ? getArc56ReturnValue<MethodReturn<TSignature>>(returnValue, this.appClient.getABIMethod(method), APP_SPEC.structs) : undefined
  }

  /**
   * Returns a new `CarbonFootprintContractClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<CarbonFootprintContractClient> {
    return new CarbonFootprintContractClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `CarbonFootprintContractClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<CarbonFootprintContractClient> {
    return new CarbonFootprintContractClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Gets available optIn methods
     */
    optIn: {
      /**
       * Opts the user into an existing instance of the CarbonFootprintContract smart contract using the `opt_in()void` ABI method.
       *
       * Opt into the contract to enable local state storage
       *
       * @param params The params for the smart contract call
       * @returns The optIn params
       */
      optIn: (params: CallParams<CarbonFootprintContractArgs['obj']['opt_in()void'] | CarbonFootprintContractArgs['tuple']['opt_in()void']> = {args: []}) => {
        return this.appClient.params.optIn(CarbonFootprintContractParamsFactory.optIn.optIn(params))
      },

    },

    /**
     * Makes a clear_state call to an existing instance of the CarbonFootprintContract smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the CarbonFootprintContract smart contract using the `register_device(string)uint64` ABI method.
     *
     * Register a new IoT device to the system
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    registerDevice: (params: CallParams<CarbonFootprintContractArgs['obj']['register_device(string)uint64'] | CarbonFootprintContractArgs['tuple']['register_device(string)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(CarbonFootprintContractParamsFactory.registerDevice(params))
    },

    /**
     * Makes a call to the CarbonFootprintContract smart contract using the `record_emission(string,uint64)uint64` ABI method.
     *
     * Record carbon emission data from an IoT device
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    recordEmission: (params: CallParams<CarbonFootprintContractArgs['obj']['record_emission(string,uint64)uint64'] | CarbonFootprintContractArgs['tuple']['record_emission(string,uint64)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(CarbonFootprintContractParamsFactory.recordEmission(params))
    },

    /**
     * Makes a call to the CarbonFootprintContract smart contract using the `transfer_emissions(address,uint64)void` ABI method.
     *
     * Transfer emission credits from sender to receiver
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    transferEmissions: (params: CallParams<CarbonFootprintContractArgs['obj']['transfer_emissions(address,uint64)void'] | CarbonFootprintContractArgs['tuple']['transfer_emissions(address,uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(CarbonFootprintContractParamsFactory.transferEmissions(params))
    },

    /**
     * Makes a call to the CarbonFootprintContract smart contract using the `get_total_emissions()uint64` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * Get the total carbon emissions recorded
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    getTotalEmissions: (params: CallParams<CarbonFootprintContractArgs['obj']['get_total_emissions()uint64'] | CarbonFootprintContractArgs['tuple']['get_total_emissions()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(CarbonFootprintContractParamsFactory.getTotalEmissions(params))
    },

    /**
     * Makes a call to the CarbonFootprintContract smart contract using the `get_device_emissions()uint64` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * Get emissions for the calling device (sender)
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    getDeviceEmissions: (params: CallParams<CarbonFootprintContractArgs['obj']['get_device_emissions()uint64'] | CarbonFootprintContractArgs['tuple']['get_device_emissions()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(CarbonFootprintContractParamsFactory.getDeviceEmissions(params))
    },

    /**
     * Makes a call to the CarbonFootprintContract smart contract using the `get_emissions_for_account(address)uint64` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * Get emissions for a specific account
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    getEmissionsForAccount: (params: CallParams<CarbonFootprintContractArgs['obj']['get_emissions_for_account(address)uint64'] | CarbonFootprintContractArgs['tuple']['get_emissions_for_account(address)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(CarbonFootprintContractParamsFactory.getEmissionsForAccount(params))
    },

    /**
     * Makes a call to the CarbonFootprintContract smart contract using the `get_device_count()uint64` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * Get the total number of registered devices
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    getDeviceCount: (params: CallParams<CarbonFootprintContractArgs['obj']['get_device_count()uint64'] | CarbonFootprintContractArgs['tuple']['get_device_count()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(CarbonFootprintContractParamsFactory.getDeviceCount(params))
    },

    /**
     * Makes a call to the CarbonFootprintContract smart contract using the `get_sender_address()address` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * Get the address of the transaction sender
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    getSenderAddress: (params: CallParams<CarbonFootprintContractArgs['obj']['get_sender_address()address'] | CarbonFootprintContractArgs['tuple']['get_sender_address()address']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(CarbonFootprintContractParamsFactory.getSenderAddress(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Gets available optIn methods
     */
    optIn: {
      /**
       * Opts the user into an existing instance of the CarbonFootprintContract smart contract using the `opt_in()void` ABI method.
       *
       * Opt into the contract to enable local state storage
       *
       * @param params The params for the smart contract call
       * @returns The optIn transaction
       */
      optIn: (params: CallParams<CarbonFootprintContractArgs['obj']['opt_in()void'] | CarbonFootprintContractArgs['tuple']['opt_in()void']> = {args: []}) => {
        return this.appClient.createTransaction.optIn(CarbonFootprintContractParamsFactory.optIn.optIn(params))
      },

    },

    /**
     * Makes a clear_state call to an existing instance of the CarbonFootprintContract smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the CarbonFootprintContract smart contract using the `register_device(string)uint64` ABI method.
     *
     * Register a new IoT device to the system
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    registerDevice: (params: CallParams<CarbonFootprintContractArgs['obj']['register_device(string)uint64'] | CarbonFootprintContractArgs['tuple']['register_device(string)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(CarbonFootprintContractParamsFactory.registerDevice(params))
    },

    /**
     * Makes a call to the CarbonFootprintContract smart contract using the `record_emission(string,uint64)uint64` ABI method.
     *
     * Record carbon emission data from an IoT device
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    recordEmission: (params: CallParams<CarbonFootprintContractArgs['obj']['record_emission(string,uint64)uint64'] | CarbonFootprintContractArgs['tuple']['record_emission(string,uint64)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(CarbonFootprintContractParamsFactory.recordEmission(params))
    },

    /**
     * Makes a call to the CarbonFootprintContract smart contract using the `transfer_emissions(address,uint64)void` ABI method.
     *
     * Transfer emission credits from sender to receiver
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    transferEmissions: (params: CallParams<CarbonFootprintContractArgs['obj']['transfer_emissions(address,uint64)void'] | CarbonFootprintContractArgs['tuple']['transfer_emissions(address,uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(CarbonFootprintContractParamsFactory.transferEmissions(params))
    },

    /**
     * Makes a call to the CarbonFootprintContract smart contract using the `get_total_emissions()uint64` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * Get the total carbon emissions recorded
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    getTotalEmissions: (params: CallParams<CarbonFootprintContractArgs['obj']['get_total_emissions()uint64'] | CarbonFootprintContractArgs['tuple']['get_total_emissions()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(CarbonFootprintContractParamsFactory.getTotalEmissions(params))
    },

    /**
     * Makes a call to the CarbonFootprintContract smart contract using the `get_device_emissions()uint64` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * Get emissions for the calling device (sender)
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    getDeviceEmissions: (params: CallParams<CarbonFootprintContractArgs['obj']['get_device_emissions()uint64'] | CarbonFootprintContractArgs['tuple']['get_device_emissions()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(CarbonFootprintContractParamsFactory.getDeviceEmissions(params))
    },

    /**
     * Makes a call to the CarbonFootprintContract smart contract using the `get_emissions_for_account(address)uint64` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * Get emissions for a specific account
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    getEmissionsForAccount: (params: CallParams<CarbonFootprintContractArgs['obj']['get_emissions_for_account(address)uint64'] | CarbonFootprintContractArgs['tuple']['get_emissions_for_account(address)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(CarbonFootprintContractParamsFactory.getEmissionsForAccount(params))
    },

    /**
     * Makes a call to the CarbonFootprintContract smart contract using the `get_device_count()uint64` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * Get the total number of registered devices
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    getDeviceCount: (params: CallParams<CarbonFootprintContractArgs['obj']['get_device_count()uint64'] | CarbonFootprintContractArgs['tuple']['get_device_count()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(CarbonFootprintContractParamsFactory.getDeviceCount(params))
    },

    /**
     * Makes a call to the CarbonFootprintContract smart contract using the `get_sender_address()address` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * Get the address of the transaction sender
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    getSenderAddress: (params: CallParams<CarbonFootprintContractArgs['obj']['get_sender_address()address'] | CarbonFootprintContractArgs['tuple']['get_sender_address()address']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(CarbonFootprintContractParamsFactory.getSenderAddress(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Gets available optIn methods
     */
    optIn: {
      /**
       * Opts the user into an existing instance of the CarbonFootprintContract smart contract using the `opt_in()void` ABI method.
       *
       * Opt into the contract to enable local state storage
       *
       * @param params The params for the smart contract call
       * @returns The optIn result
       */
      optIn: async (params: CallParams<CarbonFootprintContractArgs['obj']['opt_in()void'] | CarbonFootprintContractArgs['tuple']['opt_in()void']> & SendParams = {args: []}) => {
        const result = await this.appClient.send.optIn(CarbonFootprintContractParamsFactory.optIn.optIn(params))
        return {...result, return: result.return as unknown as (undefined | CarbonFootprintContractReturns['opt_in()void'])}
      },

    },

    /**
     * Makes a clear_state call to an existing instance of the CarbonFootprintContract smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the CarbonFootprintContract smart contract using the `register_device(string)uint64` ABI method.
     *
     * Register a new IoT device to the system
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    registerDevice: async (params: CallParams<CarbonFootprintContractArgs['obj']['register_device(string)uint64'] | CarbonFootprintContractArgs['tuple']['register_device(string)uint64']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(CarbonFootprintContractParamsFactory.registerDevice(params))
      return {...result, return: result.return as unknown as (undefined | CarbonFootprintContractReturns['register_device(string)uint64'])}
    },

    /**
     * Makes a call to the CarbonFootprintContract smart contract using the `record_emission(string,uint64)uint64` ABI method.
     *
     * Record carbon emission data from an IoT device
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    recordEmission: async (params: CallParams<CarbonFootprintContractArgs['obj']['record_emission(string,uint64)uint64'] | CarbonFootprintContractArgs['tuple']['record_emission(string,uint64)uint64']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(CarbonFootprintContractParamsFactory.recordEmission(params))
      return {...result, return: result.return as unknown as (undefined | CarbonFootprintContractReturns['record_emission(string,uint64)uint64'])}
    },

    /**
     * Makes a call to the CarbonFootprintContract smart contract using the `transfer_emissions(address,uint64)void` ABI method.
     *
     * Transfer emission credits from sender to receiver
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    transferEmissions: async (params: CallParams<CarbonFootprintContractArgs['obj']['transfer_emissions(address,uint64)void'] | CarbonFootprintContractArgs['tuple']['transfer_emissions(address,uint64)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(CarbonFootprintContractParamsFactory.transferEmissions(params))
      return {...result, return: result.return as unknown as (undefined | CarbonFootprintContractReturns['transfer_emissions(address,uint64)void'])}
    },

    /**
     * Makes a call to the CarbonFootprintContract smart contract using the `get_total_emissions()uint64` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * Get the total carbon emissions recorded
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    getTotalEmissions: async (params: CallParams<CarbonFootprintContractArgs['obj']['get_total_emissions()uint64'] | CarbonFootprintContractArgs['tuple']['get_total_emissions()uint64']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(CarbonFootprintContractParamsFactory.getTotalEmissions(params))
      return {...result, return: result.return as unknown as (undefined | CarbonFootprintContractReturns['get_total_emissions()uint64'])}
    },

    /**
     * Makes a call to the CarbonFootprintContract smart contract using the `get_device_emissions()uint64` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * Get emissions for the calling device (sender)
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    getDeviceEmissions: async (params: CallParams<CarbonFootprintContractArgs['obj']['get_device_emissions()uint64'] | CarbonFootprintContractArgs['tuple']['get_device_emissions()uint64']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(CarbonFootprintContractParamsFactory.getDeviceEmissions(params))
      return {...result, return: result.return as unknown as (undefined | CarbonFootprintContractReturns['get_device_emissions()uint64'])}
    },

    /**
     * Makes a call to the CarbonFootprintContract smart contract using the `get_emissions_for_account(address)uint64` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * Get emissions for a specific account
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    getEmissionsForAccount: async (params: CallParams<CarbonFootprintContractArgs['obj']['get_emissions_for_account(address)uint64'] | CarbonFootprintContractArgs['tuple']['get_emissions_for_account(address)uint64']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(CarbonFootprintContractParamsFactory.getEmissionsForAccount(params))
      return {...result, return: result.return as unknown as (undefined | CarbonFootprintContractReturns['get_emissions_for_account(address)uint64'])}
    },

    /**
     * Makes a call to the CarbonFootprintContract smart contract using the `get_device_count()uint64` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * Get the total number of registered devices
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    getDeviceCount: async (params: CallParams<CarbonFootprintContractArgs['obj']['get_device_count()uint64'] | CarbonFootprintContractArgs['tuple']['get_device_count()uint64']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(CarbonFootprintContractParamsFactory.getDeviceCount(params))
      return {...result, return: result.return as unknown as (undefined | CarbonFootprintContractReturns['get_device_count()uint64'])}
    },

    /**
     * Makes a call to the CarbonFootprintContract smart contract using the `get_sender_address()address` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * Get the address of the transaction sender
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    getSenderAddress: async (params: CallParams<CarbonFootprintContractArgs['obj']['get_sender_address()address'] | CarbonFootprintContractArgs['tuple']['get_sender_address()address']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(CarbonFootprintContractParamsFactory.getSenderAddress(params))
      return {...result, return: result.return as unknown as (undefined | CarbonFootprintContractReturns['get_sender_address()address'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new CarbonFootprintContractClient(this.appClient.clone(params))
  }

  /**
   * Makes a readonly (simulated) call to the CarbonFootprintContract smart contract using the `get_total_emissions()uint64` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * Get the total carbon emissions recorded
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async getTotalEmissions(params: CallParams<CarbonFootprintContractArgs['obj']['get_total_emissions()uint64'] | CarbonFootprintContractArgs['tuple']['get_total_emissions()uint64']> = {args: []}) {
    const result = await this.appClient.send.call(CarbonFootprintContractParamsFactory.getTotalEmissions(params))
    return result.return as unknown as CarbonFootprintContractReturns['get_total_emissions()uint64']
  }

  /**
   * Makes a readonly (simulated) call to the CarbonFootprintContract smart contract using the `get_device_emissions()uint64` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * Get emissions for the calling device (sender)
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async getDeviceEmissions(params: CallParams<CarbonFootprintContractArgs['obj']['get_device_emissions()uint64'] | CarbonFootprintContractArgs['tuple']['get_device_emissions()uint64']> = {args: []}) {
    const result = await this.appClient.send.call(CarbonFootprintContractParamsFactory.getDeviceEmissions(params))
    return result.return as unknown as CarbonFootprintContractReturns['get_device_emissions()uint64']
  }

  /**
   * Makes a readonly (simulated) call to the CarbonFootprintContract smart contract using the `get_emissions_for_account(address)uint64` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * Get emissions for a specific account
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async getEmissionsForAccount(params: CallParams<CarbonFootprintContractArgs['obj']['get_emissions_for_account(address)uint64'] | CarbonFootprintContractArgs['tuple']['get_emissions_for_account(address)uint64']>) {
    const result = await this.appClient.send.call(CarbonFootprintContractParamsFactory.getEmissionsForAccount(params))
    return result.return as unknown as CarbonFootprintContractReturns['get_emissions_for_account(address)uint64']
  }

  /**
   * Makes a readonly (simulated) call to the CarbonFootprintContract smart contract using the `get_device_count()uint64` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * Get the total number of registered devices
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async getDeviceCount(params: CallParams<CarbonFootprintContractArgs['obj']['get_device_count()uint64'] | CarbonFootprintContractArgs['tuple']['get_device_count()uint64']> = {args: []}) {
    const result = await this.appClient.send.call(CarbonFootprintContractParamsFactory.getDeviceCount(params))
    return result.return as unknown as CarbonFootprintContractReturns['get_device_count()uint64']
  }

  /**
   * Makes a readonly (simulated) call to the CarbonFootprintContract smart contract using the `get_sender_address()address` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * Get the address of the transaction sender
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async getSenderAddress(params: CallParams<CarbonFootprintContractArgs['obj']['get_sender_address()address'] | CarbonFootprintContractArgs['tuple']['get_sender_address()address']> = {args: []}) {
    const result = await this.appClient.send.call(CarbonFootprintContractParamsFactory.getSenderAddress(params))
    return result.return as unknown as CarbonFootprintContractReturns['get_sender_address()address']
  }

  /**
   * Methods to access state for the current CarbonFootprintContract app
   */
  state = {
    /**
     * Methods to access global state for the current CarbonFootprintContract app
     */
    global: {
      /**
       * Get all current keyed values from global state
       */
      getAll: async (): Promise<Partial<Expand<GlobalKeysState>>> => {
        const result = await this.appClient.state.global.getAll()
        return {
          totalEmissions: result.total_emissions,
          deviceCount: result.device_count,
        }
      },
      /**
       * Get the current value of the total_emissions key in global state
       */
      totalEmissions: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("total_emissions")) as bigint | undefined },
      /**
       * Get the current value of the device_count key in global state
       */
      deviceCount: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("device_count")) as bigint | undefined },
    },
    /**
     * Methods to access local state for the current CarbonFootprintContract app
     */
    local: (address: string | Address) => {
      const encodedAddress = typeof address === 'string' ? address : encodeAddress(address.publicKey)
      return {
        /**
         * Get all current keyed values from local state
         */
        getAll: async (): Promise<Partial<Expand<LocalKeysState>>> => {
          const result = await this.appClient.state.local(encodedAddress).getAll()
          return {
            deviceEmissions: result.device_emissions,
          }
        },
        /**
         * Get the current value of the device_emissions key in local state
         */
        deviceEmissions: async (): Promise<bigint | undefined> => { return (await this.appClient.state.local(encodedAddress).getValue("device_emissions")) as bigint | undefined },
      }
    },
  }

  public newGroup(): CarbonFootprintContractComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a register_device(string)uint64 method call against the CarbonFootprintContract contract
       */
      registerDevice(params: CallParams<CarbonFootprintContractArgs['obj']['register_device(string)uint64'] | CarbonFootprintContractArgs['tuple']['register_device(string)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.registerDevice(params)))
        resultMappers.push((v) => client.decodeReturnValue('register_device(string)uint64', v))
        return this
      },
      /**
       * Add a record_emission(string,uint64)uint64 method call against the CarbonFootprintContract contract
       */
      recordEmission(params: CallParams<CarbonFootprintContractArgs['obj']['record_emission(string,uint64)uint64'] | CarbonFootprintContractArgs['tuple']['record_emission(string,uint64)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.recordEmission(params)))
        resultMappers.push((v) => client.decodeReturnValue('record_emission(string,uint64)uint64', v))
        return this
      },
      /**
       * Add a transfer_emissions(address,uint64)void method call against the CarbonFootprintContract contract
       */
      transferEmissions(params: CallParams<CarbonFootprintContractArgs['obj']['transfer_emissions(address,uint64)void'] | CarbonFootprintContractArgs['tuple']['transfer_emissions(address,uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.transferEmissions(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a get_total_emissions()uint64 method call against the CarbonFootprintContract contract
       */
      getTotalEmissions(params: CallParams<CarbonFootprintContractArgs['obj']['get_total_emissions()uint64'] | CarbonFootprintContractArgs['tuple']['get_total_emissions()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.getTotalEmissions(params)))
        resultMappers.push((v) => client.decodeReturnValue('get_total_emissions()uint64', v))
        return this
      },
      /**
       * Add a get_device_emissions()uint64 method call against the CarbonFootprintContract contract
       */
      getDeviceEmissions(params: CallParams<CarbonFootprintContractArgs['obj']['get_device_emissions()uint64'] | CarbonFootprintContractArgs['tuple']['get_device_emissions()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.getDeviceEmissions(params)))
        resultMappers.push((v) => client.decodeReturnValue('get_device_emissions()uint64', v))
        return this
      },
      /**
       * Add a get_emissions_for_account(address)uint64 method call against the CarbonFootprintContract contract
       */
      getEmissionsForAccount(params: CallParams<CarbonFootprintContractArgs['obj']['get_emissions_for_account(address)uint64'] | CarbonFootprintContractArgs['tuple']['get_emissions_for_account(address)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.getEmissionsForAccount(params)))
        resultMappers.push((v) => client.decodeReturnValue('get_emissions_for_account(address)uint64', v))
        return this
      },
      /**
       * Add a get_device_count()uint64 method call against the CarbonFootprintContract contract
       */
      getDeviceCount(params: CallParams<CarbonFootprintContractArgs['obj']['get_device_count()uint64'] | CarbonFootprintContractArgs['tuple']['get_device_count()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.getDeviceCount(params)))
        resultMappers.push((v) => client.decodeReturnValue('get_device_count()uint64', v))
        return this
      },
      /**
       * Add a get_sender_address()address method call against the CarbonFootprintContract contract
       */
      getSenderAddress(params: CallParams<CarbonFootprintContractArgs['obj']['get_sender_address()address'] | CarbonFootprintContractArgs['tuple']['get_sender_address()address']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.getSenderAddress(params)))
        resultMappers.push((v) => client.decodeReturnValue('get_sender_address()address', v))
        return this
      },
      get optIn() {
        return {
          optIn: (params: CallParams<CarbonFootprintContractArgs['obj']['opt_in()void'] | CarbonFootprintContractArgs['tuple']['opt_in()void']>) => {
            promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.optIn.optIn(params)))
            resultMappers.push(undefined)
            return this
          },
        }
      },
      /**
       * Add a clear state call to the CarbonFootprintContract contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as CarbonFootprintContractComposer
  }
}
export type CarbonFootprintContractComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the register_device(string)uint64 ABI method.
   *
   * Register a new IoT device to the system
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  registerDevice(params?: CallParams<CarbonFootprintContractArgs['obj']['register_device(string)uint64'] | CarbonFootprintContractArgs['tuple']['register_device(string)uint64']>): CarbonFootprintContractComposer<[...TReturns, CarbonFootprintContractReturns['register_device(string)uint64'] | undefined]>

  /**
   * Calls the record_emission(string,uint64)uint64 ABI method.
   *
   * Record carbon emission data from an IoT device
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  recordEmission(params?: CallParams<CarbonFootprintContractArgs['obj']['record_emission(string,uint64)uint64'] | CarbonFootprintContractArgs['tuple']['record_emission(string,uint64)uint64']>): CarbonFootprintContractComposer<[...TReturns, CarbonFootprintContractReturns['record_emission(string,uint64)uint64'] | undefined]>

  /**
   * Calls the transfer_emissions(address,uint64)void ABI method.
   *
   * Transfer emission credits from sender to receiver
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  transferEmissions(params?: CallParams<CarbonFootprintContractArgs['obj']['transfer_emissions(address,uint64)void'] | CarbonFootprintContractArgs['tuple']['transfer_emissions(address,uint64)void']>): CarbonFootprintContractComposer<[...TReturns, CarbonFootprintContractReturns['transfer_emissions(address,uint64)void'] | undefined]>

  /**
   * Calls the get_total_emissions()uint64 ABI method.
   *
   * Get the total carbon emissions recorded
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  getTotalEmissions(params?: CallParams<CarbonFootprintContractArgs['obj']['get_total_emissions()uint64'] | CarbonFootprintContractArgs['tuple']['get_total_emissions()uint64']>): CarbonFootprintContractComposer<[...TReturns, CarbonFootprintContractReturns['get_total_emissions()uint64'] | undefined]>

  /**
   * Calls the get_device_emissions()uint64 ABI method.
   *
   * Get emissions for the calling device (sender)
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  getDeviceEmissions(params?: CallParams<CarbonFootprintContractArgs['obj']['get_device_emissions()uint64'] | CarbonFootprintContractArgs['tuple']['get_device_emissions()uint64']>): CarbonFootprintContractComposer<[...TReturns, CarbonFootprintContractReturns['get_device_emissions()uint64'] | undefined]>

  /**
   * Calls the get_emissions_for_account(address)uint64 ABI method.
   *
   * Get emissions for a specific account
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  getEmissionsForAccount(params?: CallParams<CarbonFootprintContractArgs['obj']['get_emissions_for_account(address)uint64'] | CarbonFootprintContractArgs['tuple']['get_emissions_for_account(address)uint64']>): CarbonFootprintContractComposer<[...TReturns, CarbonFootprintContractReturns['get_emissions_for_account(address)uint64'] | undefined]>

  /**
   * Calls the get_device_count()uint64 ABI method.
   *
   * Get the total number of registered devices
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  getDeviceCount(params?: CallParams<CarbonFootprintContractArgs['obj']['get_device_count()uint64'] | CarbonFootprintContractArgs['tuple']['get_device_count()uint64']>): CarbonFootprintContractComposer<[...TReturns, CarbonFootprintContractReturns['get_device_count()uint64'] | undefined]>

  /**
   * Calls the get_sender_address()address ABI method.
   *
   * Get the address of the transaction sender
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  getSenderAddress(params?: CallParams<CarbonFootprintContractArgs['obj']['get_sender_address()address'] | CarbonFootprintContractArgs['tuple']['get_sender_address()address']>): CarbonFootprintContractComposer<[...TReturns, CarbonFootprintContractReturns['get_sender_address()address'] | undefined]>

  /**
   * Gets available optIn methods
   */
  readonly optIn: {
    /**
     * Opts the user into an existing instance of the CarbonFootprintContract smart contract using the opt_in()void ABI method.
     *
     * @param args The arguments for the smart contract call
     * @param params Any additional parameters for the call
     * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
     */
    optIn(params?: CallParams<CarbonFootprintContractArgs['obj']['opt_in()void'] | CarbonFootprintContractArgs['tuple']['opt_in()void']>): CarbonFootprintContractComposer<[...TReturns, CarbonFootprintContractReturns['opt_in()void'] | undefined]>
  }

  /**
   * Makes a clear_state call to an existing instance of the CarbonFootprintContract smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): CarbonFootprintContractComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): CarbonFootprintContractComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<CarbonFootprintContractComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<CarbonFootprintContractComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<CarbonFootprintContractComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<CarbonFootprintContractComposerResults<TReturns>>
}
export type CarbonFootprintContractComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

