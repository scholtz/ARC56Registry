/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"GuessNumber","structs":{},"methods":[{"name":"generated_number","args":[{"type":"uint64","name":"hidden_number"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}},{"name":"guess","args":[{"type":"uint64","name":"user_guess"}],"returns":{"type":"string"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}},{"name":"get_winner","args":[],"returns":{"type":"address"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}}],"arcs":[22,28],"networks":{},"state":{"schema":{"global":{"ints":0,"bytes":2},"local":{"ints":0,"bytes":0}},"keys":{"global":{"guess_number":{"keyType":"AVMString","valueType":"uint64","key":"Z3Vlc3NfbnVtYmVy"},"winner":{"keyType":"AVMString","valueType":"address","key":"d2lubmVy"}},"local":{},"box":{}},"maps":{"global":{},"local":{},"box":{}}},"bareActions":{"create":["NoOp"],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[170],"errorMessage":"Game already won"},{"pc":[150],"errorMessage":"Number must be positive"},{"pc":[70,86,105],"errorMessage":"OnCompletion is not NoOp"},{"pc":[136],"errorMessage":"Only creator can set number"},{"pc":[125],"errorMessage":"can only call when creating"},{"pc":[73,89,108],"errorMessage":"can only call when not creating"},{"pc":[174,221],"errorMessage":"check self.guess_number exists"},{"pc":[166,258],"errorMessage":"check self.winner exists"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"events":[]} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


/**
 * The argument types for the GuessNumber contract
 */
export type GuessNumberArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'generated_number(uint64)void': {
      hiddenNumber: bigint | number
    }
    'guess(uint64)string': {
      userGuess: bigint | number
    }
    'get_winner()address': Record<string, never>
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'generated_number(uint64)void': [hiddenNumber: bigint | number]
    'guess(uint64)string': [userGuess: bigint | number]
    'get_winner()address': []
  }
}

/**
 * The return type for each method
 */
export type GuessNumberReturns = {
  'generated_number(uint64)void': void
  'guess(uint64)string': string
  'get_winner()address': string
}

/**
 * Defines the types of available calls and state of the GuessNumber smart contract.
 */
export type GuessNumberTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'generated_number(uint64)void' | 'generated_number', {
      argsObj: GuessNumberArgs['obj']['generated_number(uint64)void']
      argsTuple: GuessNumberArgs['tuple']['generated_number(uint64)void']
      returns: GuessNumberReturns['generated_number(uint64)void']
    }>
    & Record<'guess(uint64)string' | 'guess', {
      argsObj: GuessNumberArgs['obj']['guess(uint64)string']
      argsTuple: GuessNumberArgs['tuple']['guess(uint64)string']
      returns: GuessNumberReturns['guess(uint64)string']
    }>
    & Record<'get_winner()address' | 'get_winner', {
      argsObj: GuessNumberArgs['obj']['get_winner()address']
      argsTuple: GuessNumberArgs['tuple']['get_winner()address']
      returns: GuessNumberReturns['get_winner()address']
    }>
  /**
   * Defines the shape of the state of the application.
   */
  state: {
    global: {
      keys: {
        guessNumber: bigint
        winner: string
      }
      maps: {}
    }
  }
}

/**
 * Defines the possible abi call signatures.
 */
export type GuessNumberSignatures = keyof GuessNumberTypes['methods']
/**
 * Defines the possible abi call signatures for methods that return a non-void value.
 */
export type GuessNumberNonVoidMethodSignatures = keyof GuessNumberTypes['methods'] extends infer T ? T extends keyof GuessNumberTypes['methods'] ? MethodReturn<T> extends void ? never : T  : never : never
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the GuessNumber smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends GuessNumberSignatures> = GuessNumberTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the GuessNumber smart contract to the method's return type
 */
export type MethodReturn<TSignature extends GuessNumberSignatures> = GuessNumberTypes['methods'][TSignature]['returns']

/**
 * Defines the shape of the keyed global state of the application.
 */
export type GlobalKeysState = GuessNumberTypes['state']['global']['keys']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the GuessNumber smart contract
 */
export abstract class GuessNumberParamsFactory {
  /**
   * Constructs a no op call for the generated_number(uint64)void ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static generatedNumber(params: CallParams<GuessNumberArgs['obj']['generated_number(uint64)void'] | GuessNumberArgs['tuple']['generated_number(uint64)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'generated_number(uint64)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.hiddenNumber],
    }
  }
  /**
   * Constructs a no op call for the guess(uint64)string ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static guess(params: CallParams<GuessNumberArgs['obj']['guess(uint64)string'] | GuessNumberArgs['tuple']['guess(uint64)string']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'guess(uint64)string' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.userGuess],
    }
  }
  /**
   * Constructs a no op call for the get_winner()address ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static getWinner(params: CallParams<GuessNumberArgs['obj']['get_winner()address'] | GuessNumberArgs['tuple']['get_winner()address']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'get_winner()address' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
}

/**
 * A client to make calls to the GuessNumber smart contract
 */
export class GuessNumberClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `GuessNumberClient`
   *
   * @param appClient An `AppClient` instance which has been created with the GuessNumber app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `GuessNumberClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Checks for decode errors on the given return value and maps the return value to the return type for the given method
   * @returns The typed return value or undefined if there was no value
   */
  decodeReturnValue<TSignature extends GuessNumberNonVoidMethodSignatures>(method: TSignature, returnValue: ABIReturn | undefined) {
    return returnValue !== undefined ? getArc56ReturnValue<MethodReturn<TSignature>>(returnValue, this.appClient.getABIMethod(method), APP_SPEC.structs) : undefined
  }

  /**
   * Returns a new `GuessNumberClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<GuessNumberClient> {
    return new GuessNumberClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `GuessNumberClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<GuessNumberClient> {
    return new GuessNumberClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Makes a clear_state call to an existing instance of the GuessNumber smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the GuessNumber smart contract using the `generated_number(uint64)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    generatedNumber: (params: CallParams<GuessNumberArgs['obj']['generated_number(uint64)void'] | GuessNumberArgs['tuple']['generated_number(uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(GuessNumberParamsFactory.generatedNumber(params))
    },

    /**
     * Makes a call to the GuessNumber smart contract using the `guess(uint64)string` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    guess: (params: CallParams<GuessNumberArgs['obj']['guess(uint64)string'] | GuessNumberArgs['tuple']['guess(uint64)string']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(GuessNumberParamsFactory.guess(params))
    },

    /**
     * Makes a call to the GuessNumber smart contract using the `get_winner()address` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    getWinner: (params: CallParams<GuessNumberArgs['obj']['get_winner()address'] | GuessNumberArgs['tuple']['get_winner()address']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(GuessNumberParamsFactory.getWinner(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Makes a clear_state call to an existing instance of the GuessNumber smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the GuessNumber smart contract using the `generated_number(uint64)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    generatedNumber: (params: CallParams<GuessNumberArgs['obj']['generated_number(uint64)void'] | GuessNumberArgs['tuple']['generated_number(uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(GuessNumberParamsFactory.generatedNumber(params))
    },

    /**
     * Makes a call to the GuessNumber smart contract using the `guess(uint64)string` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    guess: (params: CallParams<GuessNumberArgs['obj']['guess(uint64)string'] | GuessNumberArgs['tuple']['guess(uint64)string']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(GuessNumberParamsFactory.guess(params))
    },

    /**
     * Makes a call to the GuessNumber smart contract using the `get_winner()address` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    getWinner: (params: CallParams<GuessNumberArgs['obj']['get_winner()address'] | GuessNumberArgs['tuple']['get_winner()address']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(GuessNumberParamsFactory.getWinner(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Makes a clear_state call to an existing instance of the GuessNumber smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the GuessNumber smart contract using the `generated_number(uint64)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    generatedNumber: async (params: CallParams<GuessNumberArgs['obj']['generated_number(uint64)void'] | GuessNumberArgs['tuple']['generated_number(uint64)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(GuessNumberParamsFactory.generatedNumber(params))
      return {...result, return: result.return as unknown as (undefined | GuessNumberReturns['generated_number(uint64)void'])}
    },

    /**
     * Makes a call to the GuessNumber smart contract using the `guess(uint64)string` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    guess: async (params: CallParams<GuessNumberArgs['obj']['guess(uint64)string'] | GuessNumberArgs['tuple']['guess(uint64)string']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(GuessNumberParamsFactory.guess(params))
      return {...result, return: result.return as unknown as (undefined | GuessNumberReturns['guess(uint64)string'])}
    },

    /**
     * Makes a call to the GuessNumber smart contract using the `get_winner()address` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    getWinner: async (params: CallParams<GuessNumberArgs['obj']['get_winner()address'] | GuessNumberArgs['tuple']['get_winner()address']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(GuessNumberParamsFactory.getWinner(params))
      return {...result, return: result.return as unknown as (undefined | GuessNumberReturns['get_winner()address'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new GuessNumberClient(this.appClient.clone(params))
  }

  /**
   * Methods to access state for the current GuessNumber app
   */
  state = {
    /**
     * Methods to access global state for the current GuessNumber app
     */
    global: {
      /**
       * Get all current keyed values from global state
       */
      getAll: async (): Promise<Partial<Expand<GlobalKeysState>>> => {
        const result = await this.appClient.state.global.getAll()
        return {
          guessNumber: result.guess_number,
          winner: result.winner,
        }
      },
      /**
       * Get the current value of the guess_number key in global state
       */
      guessNumber: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("guess_number")) as bigint | undefined },
      /**
       * Get the current value of the winner key in global state
       */
      winner: async (): Promise<string | undefined> => { return (await this.appClient.state.global.getValue("winner")) as string | undefined },
    },
  }

  public newGroup(): GuessNumberComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a generated_number(uint64)void method call against the GuessNumber contract
       */
      generatedNumber(params: CallParams<GuessNumberArgs['obj']['generated_number(uint64)void'] | GuessNumberArgs['tuple']['generated_number(uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.generatedNumber(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a guess(uint64)string method call against the GuessNumber contract
       */
      guess(params: CallParams<GuessNumberArgs['obj']['guess(uint64)string'] | GuessNumberArgs['tuple']['guess(uint64)string']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.guess(params)))
        resultMappers.push((v) => client.decodeReturnValue('guess(uint64)string', v))
        return this
      },
      /**
       * Add a get_winner()address method call against the GuessNumber contract
       */
      getWinner(params: CallParams<GuessNumberArgs['obj']['get_winner()address'] | GuessNumberArgs['tuple']['get_winner()address']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.getWinner(params)))
        resultMappers.push((v) => client.decodeReturnValue('get_winner()address', v))
        return this
      },
      /**
       * Add a clear state call to the GuessNumber contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as GuessNumberComposer
  }
}
export type GuessNumberComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the generated_number(uint64)void ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  generatedNumber(params?: CallParams<GuessNumberArgs['obj']['generated_number(uint64)void'] | GuessNumberArgs['tuple']['generated_number(uint64)void']>): GuessNumberComposer<[...TReturns, GuessNumberReturns['generated_number(uint64)void'] | undefined]>

  /**
   * Calls the guess(uint64)string ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  guess(params?: CallParams<GuessNumberArgs['obj']['guess(uint64)string'] | GuessNumberArgs['tuple']['guess(uint64)string']>): GuessNumberComposer<[...TReturns, GuessNumberReturns['guess(uint64)string'] | undefined]>

  /**
   * Calls the get_winner()address ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  getWinner(params?: CallParams<GuessNumberArgs['obj']['get_winner()address'] | GuessNumberArgs['tuple']['get_winner()address']>): GuessNumberComposer<[...TReturns, GuessNumberReturns['get_winner()address'] | undefined]>

  /**
   * Makes a clear_state call to an existing instance of the GuessNumber smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): GuessNumberComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): GuessNumberComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<GuessNumberComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<GuessNumberComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<GuessNumberComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<GuessNumberComposerResults<TReturns>>
}
export type GuessNumberComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

