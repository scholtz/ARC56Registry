/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'
import { AppFactory as _AppFactory, AppFactoryAppClientParams, AppFactoryResolveAppClientByCreatorAndNameParams, AppFactoryDeployParams, AppFactoryParams, CreateSchema } from '@algorandfoundation/algokit-utils/types/app-factory'
import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"ResumeVerifier","structs":{"AttestationRecord":[{"name":"roleId","type":"string"},{"name":"matchScore","type":"uint64"},{"name":"attestationRound","type":"uint64"},{"name":"feePaid","type":"uint64"}]},"methods":[{"name":"bootstrap","args":[{"type":"uint64","name":"min_fee","desc":"Minimum payment required to register an attestation, in µALGO. For the MVP this is set to 500_000 (0.5 ALGO)."}],"returns":{"type":"void"},"actions":{"create":["NoOp"],"call":[]},"readonly":false,"desc":"Initialize the contract on creation.  Must be the first call.\nCalled by deploy_config.py immediately after contract creation.","events":[],"recommendations":{}},{"name":"register_attestation","args":[{"type":"byte[32]","name":"resume_hash","desc":"SHA-256 hash of the resume file (exactly 32 bytes)."},{"type":"string","name":"role_id","desc":"Short identifier for the job role (e.g. \"backend-engineer\")."},{"type":"uint64","name":"match_score","desc":"Deterministic match score 0–100 computed by the scoring engine."},{"type":"pay","name":"payment","desc":"Reference to the grouped PaymentTransaction (group index 0)."}],"returns":{"type":"string","desc":"\"ATTESTED\" on success."},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Register a resume–role match attestation on-chain.\nThis method MUST be called as part of an atomic transaction group:   Group[0]: PaymentTransaction → this contract address, ≥ min_fee µALGO   Group[1]: ApplicationCallTransaction → this method\nThe AVM enforces atomicity: if any assertion fails, BOTH transactions in the group are rejected and the payment is not made.\nOnly the resume SHA-256 hash (not the resume content) and attestation metadata are stored on-chain.  No PII is stored.\nA second attestation for the same (applicant, resume_hash) pair will overwrite the previous record (e.g. re-attesting after a score update).","events":[],"recommendations":{}},{"name":"get_attestation","args":[{"type":"address","name":"applicant","desc":"Wallet address of the applicant who registered the attestation."},{"type":"byte[32]","name":"resume_hash","desc":"SHA-256 hash of the resume file (32 bytes)."}],"returns":{"type":"(string,uint64,uint64,uint64)","struct":"AttestationRecord","desc":"The AttestationRecord: role_id, match_score, attestation_round, fee_paid."},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"desc":"Retrieve the full attestation record for a given applicant and resume hash.\nCallable by anyone (employer, auditor, third party). Reverts if no attestation exists.","events":[],"recommendations":{}},{"name":"verify_resume_hash","args":[{"type":"address","name":"applicant","desc":"Wallet address of the applicant."},{"type":"byte[32]","name":"resume_hash","desc":"SHA-256 hash of the resume file (32 bytes)."}],"returns":{"type":"bool","desc":"True if an attestation exists, False otherwise."},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"desc":"Quick existence check: does an attestation exist for this (applicant, resume_hash)?\nDoes NOT revert — returns False if not found. Useful for a simple verified / not-verified badge in the employer UI.","events":[],"recommendations":{}},{"name":"withdraw_fees","args":[{"type":"uint64","name":"amount","desc":"Amount in µALGO to withdraw from the contract account."}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Admin-only: transfer accumulated attestation fees from the contract to admin.\nSends an inner PaymentTransaction from the contract account to the admin address. Only the admin (the address set during bootstrap) may call this.","events":[],"recommendations":{}}],"arcs":[22,28],"desc":"\n    ResumeVerifier — Resume Role Matcher on Algorand.\n\n    Contract account accumulates attestation fees (0.5 ALGO per attestation).\n    Admin (deployer) can withdraw accumulated fees via withdraw_fees().\n    ","networks":{},"state":{"schema":{"global":{"ints":1,"bytes":1},"local":{"ints":0,"bytes":0}},"keys":{"global":{"admin":{"keyType":"AVMString","valueType":"address","key":"YWRtaW4="},"min_fee":{"keyType":"AVMString","valueType":"AVMUint64","key":"bWluX2ZlZQ=="}},"local":{},"box":{}},"maps":{"global":{},"local":{},"box":{"attestations":{"keyType":"AVMBytes","valueType":"AttestationRecord","prefix":""}}}},"bareActions":{"create":[],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[168],"errorMessage":"Match score must be between 0 and 100"},{"pc":[240],"errorMessage":"No attestation found for this applicant and resume hash"},{"pc":[301],"errorMessage":"Only the admin may withdraw fees"},{"pc":[161],"errorMessage":"Payment amount is below the minimum attestation fee"},{"pc":[151],"errorMessage":"Payment receiver must be this contract's address"},{"pc":[296],"errorMessage":"check self.admin exists"},{"pc":[157],"errorMessage":"check self.min_fee exists"},{"pc":[118],"errorMessage":"invalid array length header"},{"pc":[126],"errorMessage":"invalid number of bytes for arc4.dynamic_array<arc4.uint8>"},{"pc":[112,226,234,256,264],"errorMessage":"invalid number of bytes for arc4.static_array<arc4.uint8, 32>"},{"pc":[94,134,289],"errorMessage":"invalid number of bytes for arc4.uint64"},{"pc":[144],"errorMessage":"transaction type is pay"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"source":{"approval":"","clear":"I3ByYWdtYSB2ZXJzaW9uIDExCiNwcmFnbWEgdHlwZXRyYWNrIGZhbHNlCgovLyBhbGdvcHkuYXJjNC5BUkM0Q29udHJhY3QuY2xlYXJfc3RhdGVfcHJvZ3JhbSgpIC0+IHVpbnQ2NDoKbWFpbjoKICAgIHB1c2hpbnQgMQogICAgcmV0dXJuCg=="},"byteCode":{"approval":"","clear":"C4EBQw=="},"events":[],"templateVariables":{}} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


// Type definitions for ARC-56 structs

export type AttestationRecord = {
  roleId: string,
  matchScore: bigint,
  attestationRound: bigint,
  feePaid: bigint
}


/**
 * Converts the ABI tuple representation of a AttestationRecord to the struct representation
 */
export function AttestationRecordFromTuple(abiTuple: [string, bigint, bigint, bigint]) {
  return getABIStructFromABITuple(abiTuple, APP_SPEC.structs.AttestationRecord, APP_SPEC.structs) as AttestationRecord
}

/**
 * The argument types for the ResumeVerifier contract
 */
export type ResumeVerifierArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'bootstrap(uint64)void': {
      /**
       * Minimum payment required to register an attestation, in µALGO. For the MVP this is set to 500_000 (0.5 ALGO).
       */
      minFee: bigint | number
    }
    'register_attestation(byte[32],string,uint64,pay)string': {
      /**
       * SHA-256 hash of the resume file (exactly 32 bytes).
       */
      resumeHash: Uint8Array
      /**
       * Short identifier for the job role (e.g. "backend-engineer").
       */
      roleId: string
      /**
       * Deterministic match score 0–100 computed by the scoring engine.
       */
      matchScore: bigint | number
      /**
       * Reference to the grouped PaymentTransaction (group index 0).
       */
      payment: AppMethodCallTransactionArgument
    }
    'get_attestation(address,byte[32])(string,uint64,uint64,uint64)': {
      /**
       * Wallet address of the applicant who registered the attestation.
       */
      applicant: string
      /**
       * SHA-256 hash of the resume file (32 bytes).
       */
      resumeHash: Uint8Array
    }
    'verify_resume_hash(address,byte[32])bool': {
      /**
       * Wallet address of the applicant.
       */
      applicant: string
      /**
       * SHA-256 hash of the resume file (32 bytes).
       */
      resumeHash: Uint8Array
    }
    'withdraw_fees(uint64)void': {
      /**
       * Amount in µALGO to withdraw from the contract account.
       */
      amount: bigint | number
    }
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'bootstrap(uint64)void': [minFee: bigint | number]
    'register_attestation(byte[32],string,uint64,pay)string': [resumeHash: Uint8Array, roleId: string, matchScore: bigint | number, payment: AppMethodCallTransactionArgument]
    'get_attestation(address,byte[32])(string,uint64,uint64,uint64)': [applicant: string, resumeHash: Uint8Array]
    'verify_resume_hash(address,byte[32])bool': [applicant: string, resumeHash: Uint8Array]
    'withdraw_fees(uint64)void': [amount: bigint | number]
  }
}

/**
 * The return type for each method
 */
export type ResumeVerifierReturns = {
  'bootstrap(uint64)void': void
  'register_attestation(byte[32],string,uint64,pay)string': string
  'get_attestation(address,byte[32])(string,uint64,uint64,uint64)': AttestationRecord
  'verify_resume_hash(address,byte[32])bool': boolean
  'withdraw_fees(uint64)void': void
}

/**
 * Defines the types of available calls and state of the ResumeVerifier smart contract.
 */
export type ResumeVerifierTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'bootstrap(uint64)void' | 'bootstrap', {
      argsObj: ResumeVerifierArgs['obj']['bootstrap(uint64)void']
      argsTuple: ResumeVerifierArgs['tuple']['bootstrap(uint64)void']
      returns: ResumeVerifierReturns['bootstrap(uint64)void']
    }>
    & Record<'register_attestation(byte[32],string,uint64,pay)string' | 'register_attestation', {
      argsObj: ResumeVerifierArgs['obj']['register_attestation(byte[32],string,uint64,pay)string']
      argsTuple: ResumeVerifierArgs['tuple']['register_attestation(byte[32],string,uint64,pay)string']
      /**
       * "ATTESTED" on success.
       */
      returns: ResumeVerifierReturns['register_attestation(byte[32],string,uint64,pay)string']
    }>
    & Record<'get_attestation(address,byte[32])(string,uint64,uint64,uint64)' | 'get_attestation', {
      argsObj: ResumeVerifierArgs['obj']['get_attestation(address,byte[32])(string,uint64,uint64,uint64)']
      argsTuple: ResumeVerifierArgs['tuple']['get_attestation(address,byte[32])(string,uint64,uint64,uint64)']
      /**
       * The AttestationRecord: role_id, match_score, attestation_round, fee_paid.
       */
      returns: ResumeVerifierReturns['get_attestation(address,byte[32])(string,uint64,uint64,uint64)']
    }>
    & Record<'verify_resume_hash(address,byte[32])bool' | 'verify_resume_hash', {
      argsObj: ResumeVerifierArgs['obj']['verify_resume_hash(address,byte[32])bool']
      argsTuple: ResumeVerifierArgs['tuple']['verify_resume_hash(address,byte[32])bool']
      /**
       * True if an attestation exists, False otherwise.
       */
      returns: ResumeVerifierReturns['verify_resume_hash(address,byte[32])bool']
    }>
    & Record<'withdraw_fees(uint64)void' | 'withdraw_fees', {
      argsObj: ResumeVerifierArgs['obj']['withdraw_fees(uint64)void']
      argsTuple: ResumeVerifierArgs['tuple']['withdraw_fees(uint64)void']
      returns: ResumeVerifierReturns['withdraw_fees(uint64)void']
    }>
  /**
   * Defines the shape of the state of the application.
   */
  state: {
    global: {
      keys: {
        admin: string
        minFee: bigint
      }
      maps: {}
    }
    box: {
      keys: {}
      maps: {
        attestations: Map<Uint8Array | string, AttestationRecord>
      }
    }
  }
}

/**
 * Defines the possible abi call signatures.
 */
export type ResumeVerifierSignatures = keyof ResumeVerifierTypes['methods']
/**
 * Defines the possible abi call signatures for methods that return a non-void value.
 */
export type ResumeVerifierNonVoidMethodSignatures = keyof ResumeVerifierTypes['methods'] extends infer T ? T extends keyof ResumeVerifierTypes['methods'] ? MethodReturn<T> extends void ? never : T  : never : never
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the ResumeVerifier smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends ResumeVerifierSignatures> = ResumeVerifierTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the ResumeVerifier smart contract to the method's return type
 */
export type MethodReturn<TSignature extends ResumeVerifierSignatures> = ResumeVerifierTypes['methods'][TSignature]['returns']

/**
 * Defines the shape of the keyed global state of the application.
 */
export type GlobalKeysState = ResumeVerifierTypes['state']['global']['keys']

/**
 * Defines the shape of the keyed box state of the application.
 */
export type BoxKeysState = ResumeVerifierTypes['state']['box']['keys']


/**
 * Defines supported create method params for this smart contract
 */
export type ResumeVerifierCreateCallParams =
  | Expand<CallParams<ResumeVerifierArgs['obj']['bootstrap(uint64)void'] | ResumeVerifierArgs['tuple']['bootstrap(uint64)void']> & {method: 'bootstrap'} & {onComplete?: OnApplicationComplete.NoOpOC} & CreateSchema>
  | Expand<CallParams<ResumeVerifierArgs['obj']['bootstrap(uint64)void'] | ResumeVerifierArgs['tuple']['bootstrap(uint64)void']> & {method: 'bootstrap(uint64)void'} & {onComplete?: OnApplicationComplete.NoOpOC} & CreateSchema>
/**
 * Defines arguments required for the deploy method.
 */
export type ResumeVerifierDeployParams = Expand<Omit<AppFactoryDeployParams, 'createParams' | 'updateParams' | 'deleteParams'> & {
  /**
   * Create transaction parameters to use if a create needs to be issued as part of deployment; use `method` to define ABI call (if available) or leave out for a bare call (if available)
   */
  createParams?: ResumeVerifierCreateCallParams
}>


/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the ResumeVerifier smart contract
 */
export abstract class ResumeVerifierParamsFactory {
  /**
   * Gets available create ABI call param factories
   */
  static get create() {
    return {
      _resolveByMethod<TParams extends ResumeVerifierCreateCallParams & {method: string}>(params: TParams) {
        switch(params.method) {
          case 'bootstrap':
          case 'bootstrap(uint64)void':
            return ResumeVerifierParamsFactory.create.bootstrap(params)
        }
        throw new Error(`Unknown ' + verb + ' method`)
      },

      /**
       * Constructs create ABI call params for the ResumeVerifier smart contract using the bootstrap(uint64)void ABI method
       *
       * @param params Parameters for the call
       * @returns An `AppClientMethodCallParams` object for the call
       */
      bootstrap(params: CallParams<ResumeVerifierArgs['obj']['bootstrap(uint64)void'] | ResumeVerifierArgs['tuple']['bootstrap(uint64)void']> & AppClientCompilationParams & {onComplete?: OnApplicationComplete.NoOpOC}): AppClientMethodCallParams & AppClientCompilationParams & {onComplete?: OnApplicationComplete.NoOpOC} {
        return {
          ...params,
          method: 'bootstrap(uint64)void' as const,
          args: Array.isArray(params.args) ? params.args : [params.args.minFee],
        }
      },
    }
  }

  /**
   * Constructs a no op call for the register_attestation(byte[32],string,uint64,pay)string ABI method
   *
  * Register a resume–role match attestation on-chain.
  This method MUST be called as part of an atomic transaction group:   Group[0]: PaymentTransaction → this contract address, ≥ min_fee µALGO   Group[1]: ApplicationCallTransaction → this method
  The AVM enforces atomicity: if any assertion fails, BOTH transactions in the group are rejected and the payment is not made.
  Only the resume SHA-256 hash (not the resume content) and attestation metadata are stored on-chain.  No PII is stored.
  A second attestation for the same (applicant, resume_hash) pair will overwrite the previous record (e.g. re-attesting after a score update).

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static registerAttestation(params: CallParams<ResumeVerifierArgs['obj']['register_attestation(byte[32],string,uint64,pay)string'] | ResumeVerifierArgs['tuple']['register_attestation(byte[32],string,uint64,pay)string']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'register_attestation(byte[32],string,uint64,pay)string' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.resumeHash, params.args.roleId, params.args.matchScore, params.args.payment],
    }
  }
  /**
   * Constructs a no op call for the get_attestation(address,byte[32])(string,uint64,uint64,uint64) ABI method
   *
  * Retrieve the full attestation record for a given applicant and resume hash.
  Callable by anyone (employer, auditor, third party). Reverts if no attestation exists.

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static getAttestation(params: CallParams<ResumeVerifierArgs['obj']['get_attestation(address,byte[32])(string,uint64,uint64,uint64)'] | ResumeVerifierArgs['tuple']['get_attestation(address,byte[32])(string,uint64,uint64,uint64)']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'get_attestation(address,byte[32])(string,uint64,uint64,uint64)' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.applicant, params.args.resumeHash],
    }
  }
  /**
   * Constructs a no op call for the verify_resume_hash(address,byte[32])bool ABI method
   *
  * Quick existence check: does an attestation exist for this (applicant, resume_hash)?
  Does NOT revert — returns False if not found. Useful for a simple verified / not-verified badge in the employer UI.

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static verifyResumeHash(params: CallParams<ResumeVerifierArgs['obj']['verify_resume_hash(address,byte[32])bool'] | ResumeVerifierArgs['tuple']['verify_resume_hash(address,byte[32])bool']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'verify_resume_hash(address,byte[32])bool' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.applicant, params.args.resumeHash],
    }
  }
  /**
   * Constructs a no op call for the withdraw_fees(uint64)void ABI method
   *
  * Admin-only: transfer accumulated attestation fees from the contract to admin.
  Sends an inner PaymentTransaction from the contract account to the admin address. Only the admin (the address set during bootstrap) may call this.

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static withdrawFees(params: CallParams<ResumeVerifierArgs['obj']['withdraw_fees(uint64)void'] | ResumeVerifierArgs['tuple']['withdraw_fees(uint64)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'withdraw_fees(uint64)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.amount],
    }
  }
}

/**
 * A factory to create and deploy one or more instance of the ResumeVerifier smart contract and to create one or more app clients to interact with those (or other) app instances
 */
export class ResumeVerifierFactory {
  /**
   * The underlying `AppFactory` for when you want to have more flexibility
   */
  public readonly appFactory: _AppFactory

  /**
   * Creates a new instance of `ResumeVerifierFactory`
   *
   * @param params The parameters to initialise the app factory with
   */
  constructor(params: Omit<AppFactoryParams, 'appSpec'>) {
    this.appFactory = new _AppFactory({
      ...params,
      appSpec: APP_SPEC,
    })
  }
  
  /** The name of the app (from the ARC-32 / ARC-56 app spec or override). */
  public get appName() {
    return this.appFactory.appName
  }
  
  /** The ARC-56 app spec being used */
  get appSpec() {
    return APP_SPEC
  }
  
  /** A reference to the underlying `AlgorandClient` this app factory is using. */
  public get algorand(): AlgorandClient {
    return this.appFactory.algorand
  }
  
  /**
   * Returns a new `AppClient` client for an app instance of the given ID.
   *
   * Automatically populates appName, defaultSender and source maps from the factory
   * if not specified in the params.
   * @param params The parameters to create the app client
   * @returns The `AppClient`
   */
  public getAppClientById(params: AppFactoryAppClientParams) {
    return new ResumeVerifierClient(this.appFactory.getAppClientById(params))
  }
  
  /**
   * Returns a new `AppClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   *
   * Automatically populates appName, defaultSender and source maps from the factory
   * if not specified in the params.
   * @param params The parameters to create the app client
   * @returns The `AppClient`
   */
  public async getAppClientByCreatorAndName(
    params: AppFactoryResolveAppClientByCreatorAndNameParams,
  ) {
    return new ResumeVerifierClient(await this.appFactory.getAppClientByCreatorAndName(params))
  }

  /**
   * Idempotently deploys the ResumeVerifier smart contract.
   *
   * @param params The arguments for the contract calls and any additional parameters for the call
   * @returns The deployment result
   */
  public async deploy(params: ResumeVerifierDeployParams = {}) {
    const result = await this.appFactory.deploy({
      ...params,
      createParams: params.createParams?.method ? ResumeVerifierParamsFactory.create._resolveByMethod(params.createParams) : params.createParams ? params.createParams as (ResumeVerifierCreateCallParams & { args: Uint8Array[] }) : undefined,
    })
    return { result: result.result, appClient: new ResumeVerifierClient(result.appClient) }
  }

  /**
   * Get parameters to create transactions (create and deploy related calls) for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Gets available create methods
     */
    create: {
      /**
       * Creates a new instance of the ResumeVerifier smart contract using the bootstrap(uint64)void ABI method.
       *
      * Initialize the contract on creation.  Must be the first call.
      Called by deploy_config.py immediately after contract creation.

       *
       * @param params The params for the smart contract call
       * @returns The create params
       */
      bootstrap: (params: CallParams<ResumeVerifierArgs['obj']['bootstrap(uint64)void'] | ResumeVerifierArgs['tuple']['bootstrap(uint64)void']> & AppClientCompilationParams & CreateSchema & {onComplete?: OnApplicationComplete.NoOpOC}) => {
        return this.appFactory.params.create(ResumeVerifierParamsFactory.create.bootstrap(params))
      },
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Gets available create methods
     */
    create: {
      /**
       * Creates a new instance of the ResumeVerifier smart contract using the bootstrap(uint64)void ABI method.
       *
      * Initialize the contract on creation.  Must be the first call.
      Called by deploy_config.py immediately after contract creation.

       *
       * @param params The params for the smart contract call
       * @returns The create transaction
       */
      bootstrap: (params: CallParams<ResumeVerifierArgs['obj']['bootstrap(uint64)void'] | ResumeVerifierArgs['tuple']['bootstrap(uint64)void']> & AppClientCompilationParams & CreateSchema & {onComplete?: OnApplicationComplete.NoOpOC}) => {
        return this.appFactory.createTransaction.create(ResumeVerifierParamsFactory.create.bootstrap(params))
      },
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Gets available create methods
     */
    create: {
      /**
       * Creates a new instance of the ResumeVerifier smart contract using an ABI method call using the bootstrap(uint64)void ABI method.
       *
      * Initialize the contract on creation.  Must be the first call.
      Called by deploy_config.py immediately after contract creation.

       *
       * @param params The params for the smart contract call
       * @returns The create result
       */
      bootstrap: async (params: CallParams<ResumeVerifierArgs['obj']['bootstrap(uint64)void'] | ResumeVerifierArgs['tuple']['bootstrap(uint64)void']> & AppClientCompilationParams & CreateSchema & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
        const result = await this.appFactory.send.create(ResumeVerifierParamsFactory.create.bootstrap(params))
        return { result: { ...result.result, return: result.result.return as unknown as (undefined | ResumeVerifierReturns['bootstrap(uint64)void']) }, appClient: new ResumeVerifierClient(result.appClient) }
      },
    },

  }

}
/**
 * A client to make calls to the ResumeVerifier smart contract
 */
export class ResumeVerifierClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `ResumeVerifierClient`
   *
   * @param appClient An `AppClient` instance which has been created with the ResumeVerifier app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `ResumeVerifierClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Checks for decode errors on the given return value and maps the return value to the return type for the given method
   * @returns The typed return value or undefined if there was no value
   */
  decodeReturnValue<TSignature extends ResumeVerifierNonVoidMethodSignatures>(method: TSignature, returnValue: ABIReturn | undefined) {
    return returnValue !== undefined ? getArc56ReturnValue<MethodReturn<TSignature>>(returnValue, this.appClient.getABIMethod(method), APP_SPEC.structs) : undefined
  }

  /**
   * Returns a new `ResumeVerifierClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<ResumeVerifierClient> {
    return new ResumeVerifierClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `ResumeVerifierClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<ResumeVerifierClient> {
    return new ResumeVerifierClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Makes a clear_state call to an existing instance of the ResumeVerifier smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the ResumeVerifier smart contract using the `register_attestation(byte[32],string,uint64,pay)string` ABI method.
     *
    * Register a resume–role match attestation on-chain.
    This method MUST be called as part of an atomic transaction group:   Group[0]: PaymentTransaction → this contract address, ≥ min_fee µALGO   Group[1]: ApplicationCallTransaction → this method
    The AVM enforces atomicity: if any assertion fails, BOTH transactions in the group are rejected and the payment is not made.
    Only the resume SHA-256 hash (not the resume content) and attestation metadata are stored on-chain.  No PII is stored.
    A second attestation for the same (applicant, resume_hash) pair will overwrite the previous record (e.g. re-attesting after a score update).

     *
     * @param params The params for the smart contract call
     * @returns The call params: "ATTESTED" on success.
     */
    registerAttestation: (params: CallParams<ResumeVerifierArgs['obj']['register_attestation(byte[32],string,uint64,pay)string'] | ResumeVerifierArgs['tuple']['register_attestation(byte[32],string,uint64,pay)string']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(ResumeVerifierParamsFactory.registerAttestation(params))
    },

    /**
     * Makes a call to the ResumeVerifier smart contract using the `get_attestation(address,byte[32])(string,uint64,uint64,uint64)` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
    * Retrieve the full attestation record for a given applicant and resume hash.
    Callable by anyone (employer, auditor, third party). Reverts if no attestation exists.

     *
     * @param params The params for the smart contract call
     * @returns The call params: The AttestationRecord: role_id, match_score, attestation_round, fee_paid.
     */
    getAttestation: (params: CallParams<ResumeVerifierArgs['obj']['get_attestation(address,byte[32])(string,uint64,uint64,uint64)'] | ResumeVerifierArgs['tuple']['get_attestation(address,byte[32])(string,uint64,uint64,uint64)']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(ResumeVerifierParamsFactory.getAttestation(params))
    },

    /**
     * Makes a call to the ResumeVerifier smart contract using the `verify_resume_hash(address,byte[32])bool` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
    * Quick existence check: does an attestation exist for this (applicant, resume_hash)?
    Does NOT revert — returns False if not found. Useful for a simple verified / not-verified badge in the employer UI.

     *
     * @param params The params for the smart contract call
     * @returns The call params: True if an attestation exists, False otherwise.
     */
    verifyResumeHash: (params: CallParams<ResumeVerifierArgs['obj']['verify_resume_hash(address,byte[32])bool'] | ResumeVerifierArgs['tuple']['verify_resume_hash(address,byte[32])bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(ResumeVerifierParamsFactory.verifyResumeHash(params))
    },

    /**
     * Makes a call to the ResumeVerifier smart contract using the `withdraw_fees(uint64)void` ABI method.
     *
    * Admin-only: transfer accumulated attestation fees from the contract to admin.
    Sends an inner PaymentTransaction from the contract account to the admin address. Only the admin (the address set during bootstrap) may call this.

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    withdrawFees: (params: CallParams<ResumeVerifierArgs['obj']['withdraw_fees(uint64)void'] | ResumeVerifierArgs['tuple']['withdraw_fees(uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(ResumeVerifierParamsFactory.withdrawFees(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Makes a clear_state call to an existing instance of the ResumeVerifier smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the ResumeVerifier smart contract using the `register_attestation(byte[32],string,uint64,pay)string` ABI method.
     *
    * Register a resume–role match attestation on-chain.
    This method MUST be called as part of an atomic transaction group:   Group[0]: PaymentTransaction → this contract address, ≥ min_fee µALGO   Group[1]: ApplicationCallTransaction → this method
    The AVM enforces atomicity: if any assertion fails, BOTH transactions in the group are rejected and the payment is not made.
    Only the resume SHA-256 hash (not the resume content) and attestation metadata are stored on-chain.  No PII is stored.
    A second attestation for the same (applicant, resume_hash) pair will overwrite the previous record (e.g. re-attesting after a score update).

     *
     * @param params The params for the smart contract call
     * @returns The call transaction: "ATTESTED" on success.
     */
    registerAttestation: (params: CallParams<ResumeVerifierArgs['obj']['register_attestation(byte[32],string,uint64,pay)string'] | ResumeVerifierArgs['tuple']['register_attestation(byte[32],string,uint64,pay)string']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(ResumeVerifierParamsFactory.registerAttestation(params))
    },

    /**
     * Makes a call to the ResumeVerifier smart contract using the `get_attestation(address,byte[32])(string,uint64,uint64,uint64)` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
    * Retrieve the full attestation record for a given applicant and resume hash.
    Callable by anyone (employer, auditor, third party). Reverts if no attestation exists.

     *
     * @param params The params for the smart contract call
     * @returns The call transaction: The AttestationRecord: role_id, match_score, attestation_round, fee_paid.
     */
    getAttestation: (params: CallParams<ResumeVerifierArgs['obj']['get_attestation(address,byte[32])(string,uint64,uint64,uint64)'] | ResumeVerifierArgs['tuple']['get_attestation(address,byte[32])(string,uint64,uint64,uint64)']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(ResumeVerifierParamsFactory.getAttestation(params))
    },

    /**
     * Makes a call to the ResumeVerifier smart contract using the `verify_resume_hash(address,byte[32])bool` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
    * Quick existence check: does an attestation exist for this (applicant, resume_hash)?
    Does NOT revert — returns False if not found. Useful for a simple verified / not-verified badge in the employer UI.

     *
     * @param params The params for the smart contract call
     * @returns The call transaction: True if an attestation exists, False otherwise.
     */
    verifyResumeHash: (params: CallParams<ResumeVerifierArgs['obj']['verify_resume_hash(address,byte[32])bool'] | ResumeVerifierArgs['tuple']['verify_resume_hash(address,byte[32])bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(ResumeVerifierParamsFactory.verifyResumeHash(params))
    },

    /**
     * Makes a call to the ResumeVerifier smart contract using the `withdraw_fees(uint64)void` ABI method.
     *
    * Admin-only: transfer accumulated attestation fees from the contract to admin.
    Sends an inner PaymentTransaction from the contract account to the admin address. Only the admin (the address set during bootstrap) may call this.

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    withdrawFees: (params: CallParams<ResumeVerifierArgs['obj']['withdraw_fees(uint64)void'] | ResumeVerifierArgs['tuple']['withdraw_fees(uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(ResumeVerifierParamsFactory.withdrawFees(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Makes a clear_state call to an existing instance of the ResumeVerifier smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the ResumeVerifier smart contract using the `register_attestation(byte[32],string,uint64,pay)string` ABI method.
     *
    * Register a resume–role match attestation on-chain.
    This method MUST be called as part of an atomic transaction group:   Group[0]: PaymentTransaction → this contract address, ≥ min_fee µALGO   Group[1]: ApplicationCallTransaction → this method
    The AVM enforces atomicity: if any assertion fails, BOTH transactions in the group are rejected and the payment is not made.
    Only the resume SHA-256 hash (not the resume content) and attestation metadata are stored on-chain.  No PII is stored.
    A second attestation for the same (applicant, resume_hash) pair will overwrite the previous record (e.g. re-attesting after a score update).

     *
     * @param params The params for the smart contract call
     * @returns The call result: "ATTESTED" on success.
     */
    registerAttestation: async (params: CallParams<ResumeVerifierArgs['obj']['register_attestation(byte[32],string,uint64,pay)string'] | ResumeVerifierArgs['tuple']['register_attestation(byte[32],string,uint64,pay)string']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(ResumeVerifierParamsFactory.registerAttestation(params))
      return {...result, return: result.return as unknown as (undefined | ResumeVerifierReturns['register_attestation(byte[32],string,uint64,pay)string'])}
    },

    /**
     * Makes a call to the ResumeVerifier smart contract using the `get_attestation(address,byte[32])(string,uint64,uint64,uint64)` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
    * Retrieve the full attestation record for a given applicant and resume hash.
    Callable by anyone (employer, auditor, third party). Reverts if no attestation exists.

     *
     * @param params The params for the smart contract call
     * @returns The call result: The AttestationRecord: role_id, match_score, attestation_round, fee_paid.
     */
    getAttestation: async (params: CallParams<ResumeVerifierArgs['obj']['get_attestation(address,byte[32])(string,uint64,uint64,uint64)'] | ResumeVerifierArgs['tuple']['get_attestation(address,byte[32])(string,uint64,uint64,uint64)']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(ResumeVerifierParamsFactory.getAttestation(params))
      return {...result, return: result.return as unknown as (undefined | ResumeVerifierReturns['get_attestation(address,byte[32])(string,uint64,uint64,uint64)'])}
    },

    /**
     * Makes a call to the ResumeVerifier smart contract using the `verify_resume_hash(address,byte[32])bool` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
    * Quick existence check: does an attestation exist for this (applicant, resume_hash)?
    Does NOT revert — returns False if not found. Useful for a simple verified / not-verified badge in the employer UI.

     *
     * @param params The params for the smart contract call
     * @returns The call result: True if an attestation exists, False otherwise.
     */
    verifyResumeHash: async (params: CallParams<ResumeVerifierArgs['obj']['verify_resume_hash(address,byte[32])bool'] | ResumeVerifierArgs['tuple']['verify_resume_hash(address,byte[32])bool']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(ResumeVerifierParamsFactory.verifyResumeHash(params))
      return {...result, return: result.return as unknown as (undefined | ResumeVerifierReturns['verify_resume_hash(address,byte[32])bool'])}
    },

    /**
     * Makes a call to the ResumeVerifier smart contract using the `withdraw_fees(uint64)void` ABI method.
     *
    * Admin-only: transfer accumulated attestation fees from the contract to admin.
    Sends an inner PaymentTransaction from the contract account to the admin address. Only the admin (the address set during bootstrap) may call this.

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    withdrawFees: async (params: CallParams<ResumeVerifierArgs['obj']['withdraw_fees(uint64)void'] | ResumeVerifierArgs['tuple']['withdraw_fees(uint64)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(ResumeVerifierParamsFactory.withdrawFees(params))
      return {...result, return: result.return as unknown as (undefined | ResumeVerifierReturns['withdraw_fees(uint64)void'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new ResumeVerifierClient(this.appClient.clone(params))
  }

  /**
   * Makes a readonly (simulated) call to the ResumeVerifier smart contract using the `get_attestation(address,byte[32])(string,uint64,uint64,uint64)` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
  * Retrieve the full attestation record for a given applicant and resume hash.
  Callable by anyone (employer, auditor, third party). Reverts if no attestation exists.

   *
   * @param params The params for the smart contract call
   * @returns The call result: The AttestationRecord: role_id, match_score, attestation_round, fee_paid.
   */
  async getAttestation(params: CallParams<ResumeVerifierArgs['obj']['get_attestation(address,byte[32])(string,uint64,uint64,uint64)'] | ResumeVerifierArgs['tuple']['get_attestation(address,byte[32])(string,uint64,uint64,uint64)']>) {
    const result = await this.appClient.send.call(ResumeVerifierParamsFactory.getAttestation(params))
    return result.return as unknown as ResumeVerifierReturns['get_attestation(address,byte[32])(string,uint64,uint64,uint64)']
  }

  /**
   * Makes a readonly (simulated) call to the ResumeVerifier smart contract using the `verify_resume_hash(address,byte[32])bool` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
  * Quick existence check: does an attestation exist for this (applicant, resume_hash)?
  Does NOT revert — returns False if not found. Useful for a simple verified / not-verified badge in the employer UI.

   *
   * @param params The params for the smart contract call
   * @returns The call result: True if an attestation exists, False otherwise.
   */
  async verifyResumeHash(params: CallParams<ResumeVerifierArgs['obj']['verify_resume_hash(address,byte[32])bool'] | ResumeVerifierArgs['tuple']['verify_resume_hash(address,byte[32])bool']>) {
    const result = await this.appClient.send.call(ResumeVerifierParamsFactory.verifyResumeHash(params))
    return result.return as unknown as ResumeVerifierReturns['verify_resume_hash(address,byte[32])bool']
  }

  /**
   * Methods to access state for the current ResumeVerifier app
   */
  state = {
    /**
     * Methods to access global state for the current ResumeVerifier app
     */
    global: {
      /**
       * Get all current keyed values from global state
       */
      getAll: async (): Promise<Partial<Expand<GlobalKeysState>>> => {
        const result = await this.appClient.state.global.getAll()
        return {
          admin: result.admin,
          minFee: result.min_fee,
        }
      },
      /**
       * Get the current value of the admin key in global state
       */
      admin: async (): Promise<string | undefined> => { return (await this.appClient.state.global.getValue("admin")) as string | undefined },
      /**
       * Get the current value of the min_fee key in global state
       */
      minFee: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("min_fee")) as bigint | undefined },
    },
    /**
     * Methods to access box state for the current ResumeVerifier app
     */
    box: {
      /**
       * Get all current keyed values from box state
       */
      getAll: async (): Promise<Partial<Expand<BoxKeysState>>> => {
        const result = await this.appClient.state.box.getAll()
        return {
        }
      },
      /**
       * Get values from the attestations map in box state
       */
      attestations: {
        /**
         * Get all current values of the attestations map in box state
         */
        getMap: async (): Promise<Map<Uint8Array, AttestationRecord>> => { return (await this.appClient.state.box.getMap("attestations")) as Map<Uint8Array, AttestationRecord> },
        /**
         * Get a current value of the attestations map by key from box state
         */
        value: async (key: Uint8Array | string): Promise<AttestationRecord | undefined> => { return await this.appClient.state.box.getMapValue("attestations", key) as AttestationRecord | undefined },
      },
    },
  }

  public newGroup(): ResumeVerifierComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a register_attestation(byte[32],string,uint64,pay)string method call against the ResumeVerifier contract
       */
      registerAttestation(params: CallParams<ResumeVerifierArgs['obj']['register_attestation(byte[32],string,uint64,pay)string'] | ResumeVerifierArgs['tuple']['register_attestation(byte[32],string,uint64,pay)string']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.registerAttestation(params)))
        resultMappers.push((v) => client.decodeReturnValue('register_attestation(byte[32],string,uint64,pay)string', v))
        return this
      },
      /**
       * Add a get_attestation(address,byte[32])(string,uint64,uint64,uint64) method call against the ResumeVerifier contract
       */
      getAttestation(params: CallParams<ResumeVerifierArgs['obj']['get_attestation(address,byte[32])(string,uint64,uint64,uint64)'] | ResumeVerifierArgs['tuple']['get_attestation(address,byte[32])(string,uint64,uint64,uint64)']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.getAttestation(params)))
        resultMappers.push((v) => client.decodeReturnValue('get_attestation(address,byte[32])(string,uint64,uint64,uint64)', v))
        return this
      },
      /**
       * Add a verify_resume_hash(address,byte[32])bool method call against the ResumeVerifier contract
       */
      verifyResumeHash(params: CallParams<ResumeVerifierArgs['obj']['verify_resume_hash(address,byte[32])bool'] | ResumeVerifierArgs['tuple']['verify_resume_hash(address,byte[32])bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.verifyResumeHash(params)))
        resultMappers.push((v) => client.decodeReturnValue('verify_resume_hash(address,byte[32])bool', v))
        return this
      },
      /**
       * Add a withdraw_fees(uint64)void method call against the ResumeVerifier contract
       */
      withdrawFees(params: CallParams<ResumeVerifierArgs['obj']['withdraw_fees(uint64)void'] | ResumeVerifierArgs['tuple']['withdraw_fees(uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.withdrawFees(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a clear state call to the ResumeVerifier contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as ResumeVerifierComposer
  }
}
export type ResumeVerifierComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the register_attestation(byte[32],string,uint64,pay)string ABI method.
   *
  * Register a resume–role match attestation on-chain.
  This method MUST be called as part of an atomic transaction group:   Group[0]: PaymentTransaction → this contract address, ≥ min_fee µALGO   Group[1]: ApplicationCallTransaction → this method
  The AVM enforces atomicity: if any assertion fails, BOTH transactions in the group are rejected and the payment is not made.
  Only the resume SHA-256 hash (not the resume content) and attestation metadata are stored on-chain.  No PII is stored.
  A second attestation for the same (applicant, resume_hash) pair will overwrite the previous record (e.g. re-attesting after a score update).

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  registerAttestation(params?: CallParams<ResumeVerifierArgs['obj']['register_attestation(byte[32],string,uint64,pay)string'] | ResumeVerifierArgs['tuple']['register_attestation(byte[32],string,uint64,pay)string']>): ResumeVerifierComposer<[...TReturns, ResumeVerifierReturns['register_attestation(byte[32],string,uint64,pay)string'] | undefined]>

  /**
   * Calls the get_attestation(address,byte[32])(string,uint64,uint64,uint64) ABI method.
   *
  * Retrieve the full attestation record for a given applicant and resume hash.
  Callable by anyone (employer, auditor, third party). Reverts if no attestation exists.

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  getAttestation(params?: CallParams<ResumeVerifierArgs['obj']['get_attestation(address,byte[32])(string,uint64,uint64,uint64)'] | ResumeVerifierArgs['tuple']['get_attestation(address,byte[32])(string,uint64,uint64,uint64)']>): ResumeVerifierComposer<[...TReturns, ResumeVerifierReturns['get_attestation(address,byte[32])(string,uint64,uint64,uint64)'] | undefined]>

  /**
   * Calls the verify_resume_hash(address,byte[32])bool ABI method.
   *
  * Quick existence check: does an attestation exist for this (applicant, resume_hash)?
  Does NOT revert — returns False if not found. Useful for a simple verified / not-verified badge in the employer UI.

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  verifyResumeHash(params?: CallParams<ResumeVerifierArgs['obj']['verify_resume_hash(address,byte[32])bool'] | ResumeVerifierArgs['tuple']['verify_resume_hash(address,byte[32])bool']>): ResumeVerifierComposer<[...TReturns, ResumeVerifierReturns['verify_resume_hash(address,byte[32])bool'] | undefined]>

  /**
   * Calls the withdraw_fees(uint64)void ABI method.
   *
  * Admin-only: transfer accumulated attestation fees from the contract to admin.
  Sends an inner PaymentTransaction from the contract account to the admin address. Only the admin (the address set during bootstrap) may call this.

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  withdrawFees(params?: CallParams<ResumeVerifierArgs['obj']['withdraw_fees(uint64)void'] | ResumeVerifierArgs['tuple']['withdraw_fees(uint64)void']>): ResumeVerifierComposer<[...TReturns, ResumeVerifierReturns['withdraw_fees(uint64)void'] | undefined]>

  /**
   * Makes a clear_state call to an existing instance of the ResumeVerifier smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): ResumeVerifierComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): ResumeVerifierComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<ResumeVerifierComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<ResumeVerifierComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<ResumeVerifierComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<ResumeVerifierComposerResults<TReturns>>
}
export type ResumeVerifierComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

