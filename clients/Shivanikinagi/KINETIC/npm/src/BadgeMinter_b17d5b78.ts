/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"BadgeMinter","structs":{},"methods":[{"name":"mint_badge","args":[{"type":"address","name":"recipient"},{"type":"byte[]","name":"campus_id"}],"returns":{"type":"uint64"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}},{"name":"verify_badge","args":[{"type":"address","name":"holder"}],"returns":{"type":"uint64"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"revoke_badge","args":[{"type":"address","name":"holder"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}}],"arcs":[22,28],"networks":{"current":{"appID":758813562}},"state":{"schema":{"global":{"ints":1,"bytes":0},"local":{"ints":0,"bytes":0}},"keys":{"global":{"total_minted":{"keyType":"AVMBytes","valueType":"AVMUint64","key":"dG90YWxfbWludGVk"}},"local":{},"box":{}},"maps":{"global":{},"local":{},"box":{"badge_holders":{"keyType":"address","valueType":"uint64","prefix":"YmFkZ2U="}}}},"bareActions":{"create":["NoOp"],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[274],"errorMessage":"check self.badge_holders entry exists"},{"pc":[232],"errorMessage":"check self.total_minted exists"},{"pc":[313],"errorMessage":"holder has no badge"},{"pc":[104],"errorMessage":"invalid array length header"},{"pc":[112],"errorMessage":"invalid number of bytes for arc4.dynamic_array<arc4.uint8>"},{"pc":[98,253,298],"errorMessage":"invalid number of bytes for arc4.static_array<arc4.uint8, 32>"},{"pc":[118],"errorMessage":"only admin can mint"},{"pc":[304],"errorMessage":"only admin can revoke"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"events":[]} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


/**
 * The argument types for the BadgeMinter contract
 */
export type BadgeMinterArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'mint_badge(address,byte[])uint64': {
      recipient: string
      campusId: Uint8Array
    }
    'verify_badge(address)uint64': {
      holder: string
    }
    'revoke_badge(address)void': {
      holder: string
    }
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'mint_badge(address,byte[])uint64': [recipient: string, campusId: Uint8Array]
    'verify_badge(address)uint64': [holder: string]
    'revoke_badge(address)void': [holder: string]
  }
}

/**
 * The return type for each method
 */
export type BadgeMinterReturns = {
  'mint_badge(address,byte[])uint64': bigint
  'verify_badge(address)uint64': bigint
  'revoke_badge(address)void': void
}

/**
 * Defines the types of available calls and state of the BadgeMinter smart contract.
 */
export type BadgeMinterTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'mint_badge(address,byte[])uint64' | 'mint_badge', {
      argsObj: BadgeMinterArgs['obj']['mint_badge(address,byte[])uint64']
      argsTuple: BadgeMinterArgs['tuple']['mint_badge(address,byte[])uint64']
      returns: BadgeMinterReturns['mint_badge(address,byte[])uint64']
    }>
    & Record<'verify_badge(address)uint64' | 'verify_badge', {
      argsObj: BadgeMinterArgs['obj']['verify_badge(address)uint64']
      argsTuple: BadgeMinterArgs['tuple']['verify_badge(address)uint64']
      returns: BadgeMinterReturns['verify_badge(address)uint64']
    }>
    & Record<'revoke_badge(address)void' | 'revoke_badge', {
      argsObj: BadgeMinterArgs['obj']['revoke_badge(address)void']
      argsTuple: BadgeMinterArgs['tuple']['revoke_badge(address)void']
      returns: BadgeMinterReturns['revoke_badge(address)void']
    }>
  /**
   * Defines the shape of the state of the application.
   */
  state: {
    global: {
      keys: {
        totalMinted: bigint
      }
      maps: {}
    }
    box: {
      keys: {}
      maps: {
        badgeHolders: Map<string, bigint>
      }
    }
  }
}

/**
 * Defines the possible abi call signatures.
 */
export type BadgeMinterSignatures = keyof BadgeMinterTypes['methods']
/**
 * Defines the possible abi call signatures for methods that return a non-void value.
 */
export type BadgeMinterNonVoidMethodSignatures = keyof BadgeMinterTypes['methods'] extends infer T ? T extends keyof BadgeMinterTypes['methods'] ? MethodReturn<T> extends void ? never : T  : never : never
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the BadgeMinter smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends BadgeMinterSignatures> = BadgeMinterTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the BadgeMinter smart contract to the method's return type
 */
export type MethodReturn<TSignature extends BadgeMinterSignatures> = BadgeMinterTypes['methods'][TSignature]['returns']

/**
 * Defines the shape of the keyed global state of the application.
 */
export type GlobalKeysState = BadgeMinterTypes['state']['global']['keys']

/**
 * Defines the shape of the keyed box state of the application.
 */
export type BoxKeysState = BadgeMinterTypes['state']['box']['keys']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the BadgeMinter smart contract
 */
export abstract class BadgeMinterParamsFactory {
  /**
   * Constructs a no op call for the mint_badge(address,byte[])uint64 ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static mintBadge(params: CallParams<BadgeMinterArgs['obj']['mint_badge(address,byte[])uint64'] | BadgeMinterArgs['tuple']['mint_badge(address,byte[])uint64']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'mint_badge(address,byte[])uint64' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.recipient, params.args.campusId],
    }
  }
  /**
   * Constructs a no op call for the verify_badge(address)uint64 ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static verifyBadge(params: CallParams<BadgeMinterArgs['obj']['verify_badge(address)uint64'] | BadgeMinterArgs['tuple']['verify_badge(address)uint64']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'verify_badge(address)uint64' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.holder],
    }
  }
  /**
   * Constructs a no op call for the revoke_badge(address)void ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static revokeBadge(params: CallParams<BadgeMinterArgs['obj']['revoke_badge(address)void'] | BadgeMinterArgs['tuple']['revoke_badge(address)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'revoke_badge(address)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.holder],
    }
  }
}

/**
 * A client to make calls to the BadgeMinter smart contract
 */
export class BadgeMinterClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `BadgeMinterClient`
   *
   * @param appClient An `AppClient` instance which has been created with the BadgeMinter app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `BadgeMinterClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Checks for decode errors on the given return value and maps the return value to the return type for the given method
   * @returns The typed return value or undefined if there was no value
   */
  decodeReturnValue<TSignature extends BadgeMinterNonVoidMethodSignatures>(method: TSignature, returnValue: ABIReturn | undefined) {
    return returnValue !== undefined ? getArc56ReturnValue<MethodReturn<TSignature>>(returnValue, this.appClient.getABIMethod(method), APP_SPEC.structs) : undefined
  }

  /**
   * Returns a new `BadgeMinterClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<BadgeMinterClient> {
    return new BadgeMinterClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `BadgeMinterClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<BadgeMinterClient> {
    return new BadgeMinterClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Makes a clear_state call to an existing instance of the BadgeMinter smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the BadgeMinter smart contract using the `mint_badge(address,byte[])uint64` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    mintBadge: (params: CallParams<BadgeMinterArgs['obj']['mint_badge(address,byte[])uint64'] | BadgeMinterArgs['tuple']['mint_badge(address,byte[])uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(BadgeMinterParamsFactory.mintBadge(params))
    },

    /**
     * Makes a call to the BadgeMinter smart contract using the `verify_badge(address)uint64` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    verifyBadge: (params: CallParams<BadgeMinterArgs['obj']['verify_badge(address)uint64'] | BadgeMinterArgs['tuple']['verify_badge(address)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(BadgeMinterParamsFactory.verifyBadge(params))
    },

    /**
     * Makes a call to the BadgeMinter smart contract using the `revoke_badge(address)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    revokeBadge: (params: CallParams<BadgeMinterArgs['obj']['revoke_badge(address)void'] | BadgeMinterArgs['tuple']['revoke_badge(address)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(BadgeMinterParamsFactory.revokeBadge(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Makes a clear_state call to an existing instance of the BadgeMinter smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the BadgeMinter smart contract using the `mint_badge(address,byte[])uint64` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    mintBadge: (params: CallParams<BadgeMinterArgs['obj']['mint_badge(address,byte[])uint64'] | BadgeMinterArgs['tuple']['mint_badge(address,byte[])uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(BadgeMinterParamsFactory.mintBadge(params))
    },

    /**
     * Makes a call to the BadgeMinter smart contract using the `verify_badge(address)uint64` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    verifyBadge: (params: CallParams<BadgeMinterArgs['obj']['verify_badge(address)uint64'] | BadgeMinterArgs['tuple']['verify_badge(address)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(BadgeMinterParamsFactory.verifyBadge(params))
    },

    /**
     * Makes a call to the BadgeMinter smart contract using the `revoke_badge(address)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    revokeBadge: (params: CallParams<BadgeMinterArgs['obj']['revoke_badge(address)void'] | BadgeMinterArgs['tuple']['revoke_badge(address)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(BadgeMinterParamsFactory.revokeBadge(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Makes a clear_state call to an existing instance of the BadgeMinter smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the BadgeMinter smart contract using the `mint_badge(address,byte[])uint64` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    mintBadge: async (params: CallParams<BadgeMinterArgs['obj']['mint_badge(address,byte[])uint64'] | BadgeMinterArgs['tuple']['mint_badge(address,byte[])uint64']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(BadgeMinterParamsFactory.mintBadge(params))
      return {...result, return: result.return as unknown as (undefined | BadgeMinterReturns['mint_badge(address,byte[])uint64'])}
    },

    /**
     * Makes a call to the BadgeMinter smart contract using the `verify_badge(address)uint64` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    verifyBadge: async (params: CallParams<BadgeMinterArgs['obj']['verify_badge(address)uint64'] | BadgeMinterArgs['tuple']['verify_badge(address)uint64']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(BadgeMinterParamsFactory.verifyBadge(params))
      return {...result, return: result.return as unknown as (undefined | BadgeMinterReturns['verify_badge(address)uint64'])}
    },

    /**
     * Makes a call to the BadgeMinter smart contract using the `revoke_badge(address)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    revokeBadge: async (params: CallParams<BadgeMinterArgs['obj']['revoke_badge(address)void'] | BadgeMinterArgs['tuple']['revoke_badge(address)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(BadgeMinterParamsFactory.revokeBadge(params))
      return {...result, return: result.return as unknown as (undefined | BadgeMinterReturns['revoke_badge(address)void'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new BadgeMinterClient(this.appClient.clone(params))
  }

  /**
   * Makes a readonly (simulated) call to the BadgeMinter smart contract using the `verify_badge(address)uint64` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async verifyBadge(params: CallParams<BadgeMinterArgs['obj']['verify_badge(address)uint64'] | BadgeMinterArgs['tuple']['verify_badge(address)uint64']>) {
    const result = await this.appClient.send.call(BadgeMinterParamsFactory.verifyBadge(params))
    return result.return as unknown as BadgeMinterReturns['verify_badge(address)uint64']
  }

  /**
   * Methods to access state for the current BadgeMinter app
   */
  state = {
    /**
     * Methods to access global state for the current BadgeMinter app
     */
    global: {
      /**
       * Get all current keyed values from global state
       */
      getAll: async (): Promise<Partial<Expand<GlobalKeysState>>> => {
        const result = await this.appClient.state.global.getAll()
        return {
          totalMinted: result.total_minted,
        }
      },
      /**
       * Get the current value of the total_minted key in global state
       */
      totalMinted: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("total_minted")) as bigint | undefined },
    },
    /**
     * Methods to access box state for the current BadgeMinter app
     */
    box: {
      /**
       * Get all current keyed values from box state
       */
      getAll: async (): Promise<Partial<Expand<BoxKeysState>>> => {
        const result = await this.appClient.state.box.getAll()
        return {
        }
      },
      /**
       * Get values from the badge_holders map in box state
       */
      badgeHolders: {
        /**
         * Get all current values of the badge_holders map in box state
         */
        getMap: async (): Promise<Map<string, bigint>> => { return (await this.appClient.state.box.getMap("badge_holders")) as Map<string, bigint> },
        /**
         * Get a current value of the badge_holders map by key from box state
         */
        value: async (key: string): Promise<bigint | undefined> => { return await this.appClient.state.box.getMapValue("badge_holders", key) as bigint | undefined },
      },
    },
  }

  public newGroup(): BadgeMinterComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a mint_badge(address,byte[])uint64 method call against the BadgeMinter contract
       */
      mintBadge(params: CallParams<BadgeMinterArgs['obj']['mint_badge(address,byte[])uint64'] | BadgeMinterArgs['tuple']['mint_badge(address,byte[])uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.mintBadge(params)))
        resultMappers.push((v) => client.decodeReturnValue('mint_badge(address,byte[])uint64', v))
        return this
      },
      /**
       * Add a verify_badge(address)uint64 method call against the BadgeMinter contract
       */
      verifyBadge(params: CallParams<BadgeMinterArgs['obj']['verify_badge(address)uint64'] | BadgeMinterArgs['tuple']['verify_badge(address)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.verifyBadge(params)))
        resultMappers.push((v) => client.decodeReturnValue('verify_badge(address)uint64', v))
        return this
      },
      /**
       * Add a revoke_badge(address)void method call against the BadgeMinter contract
       */
      revokeBadge(params: CallParams<BadgeMinterArgs['obj']['revoke_badge(address)void'] | BadgeMinterArgs['tuple']['revoke_badge(address)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.revokeBadge(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a clear state call to the BadgeMinter contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as BadgeMinterComposer
  }
}
export type BadgeMinterComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the mint_badge(address,byte[])uint64 ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  mintBadge(params?: CallParams<BadgeMinterArgs['obj']['mint_badge(address,byte[])uint64'] | BadgeMinterArgs['tuple']['mint_badge(address,byte[])uint64']>): BadgeMinterComposer<[...TReturns, BadgeMinterReturns['mint_badge(address,byte[])uint64'] | undefined]>

  /**
   * Calls the verify_badge(address)uint64 ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  verifyBadge(params?: CallParams<BadgeMinterArgs['obj']['verify_badge(address)uint64'] | BadgeMinterArgs['tuple']['verify_badge(address)uint64']>): BadgeMinterComposer<[...TReturns, BadgeMinterReturns['verify_badge(address)uint64'] | undefined]>

  /**
   * Calls the revoke_badge(address)void ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  revokeBadge(params?: CallParams<BadgeMinterArgs['obj']['revoke_badge(address)void'] | BadgeMinterArgs['tuple']['revoke_badge(address)void']>): BadgeMinterComposer<[...TReturns, BadgeMinterReturns['revoke_badge(address)void'] | undefined]>

  /**
   * Makes a clear_state call to an existing instance of the BadgeMinter smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): BadgeMinterComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): BadgeMinterComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<BadgeMinterComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<BadgeMinterComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<BadgeMinterComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<BadgeMinterComposerResults<TReturns>>
}
export type BadgeMinterComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

