/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"PactPoolAdapter","structs":{},"methods":[{"name":"swap_fixed_input","args":[{"type":"uint64","name":"pool_app_id","desc":"Pact pool application ID"},{"type":"uint64","name":"asset_in","desc":"Input asset"},{"type":"uint64","name":"asset_out","desc":"Output asset"},{"type":"uint64","name":"amount_in","desc":"Amount to swap in"},{"type":"uint64","name":"min_amount_out","desc":"Minimum acceptable output (slippage protection)"}],"returns":{"type":"uint64","desc":"Actual output amount received"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Execute a fixed-input swap on Pact pool\nPact uses a slightly different transaction structure than Tinyman. The pool expects: 1. Asset transfer to pool 2. Application call to trigger swap 3. Pool returns output asset","events":[],"recommendations":{}},{"name":"swap_algo_to_asa","args":[{"type":"uint64","name":"pool_app_id"},{"type":"uint64","name":"output_asset"},{"type":"uint64","name":"algo_amount"},{"type":"uint64","name":"min_amount_out"}],"returns":{"type":"uint64"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Special case: Swap ALGO (native currency) to ASA on Pact\nALGO swaps use payment transactions instead of asset transfers","events":[],"recommendations":{}},{"name":"swap_asa_to_algo","args":[{"type":"uint64","name":"pool_app_id"},{"type":"uint64","name":"input_asset"},{"type":"uint64","name":"amount_in"},{"type":"uint64","name":"min_amount_out"}],"returns":{"type":"uint64"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Special case: Swap ASA to ALGO on Pact\nReturns ALGO (native currency) to the caller","events":[],"recommendations":{}},{"name":"get_quote","args":[{"type":"uint64","name":"pool_app_id","desc":"Pool application ID"},{"type":"uint64","name":"asset_in_amount","desc":"Amount to swap"},{"type":"bool","name":"is_asset_in_first","desc":"True if swapping from first asset to second"}],"returns":{"type":"uint64","desc":"Expected output amount"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Get quote from Pact pool without executing swap\nThis calls the pool's quote method to estimate output","events":[],"recommendations":{}},{"name":"opt_into_asset","args":[{"type":"uint64","name":"asset","desc":"Asset to opt into"}],"returns":{"type":"bool","desc":"True if successful"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Opt the adapter contract into an asset\nRequired before the adapter can hold or transfer the asset. Only the contract creator can call this.","events":[],"recommendations":{}}],"arcs":[22,28],"desc":"\n    Adapter for interacting with Pact Finance pools\n    \n    Handles the specific ABI and transaction structure for Pact constant product pools.\n    Pact uses a different contract interface than Tinyman, so we need a separate adapter.\n    ","networks":{},"state":{"schema":{"global":{"ints":0,"bytes":0},"local":{"ints":0,"bytes":0}},"keys":{"global":{},"local":{},"box":{}},"maps":{"global":{},"local":{},"box":{}}},"bareActions":{"create":["NoOp"],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[29],"errorMessage":"OnCompletion must be NoOp"},{"pc":[83],"errorMessage":"OnCompletion must be NoOp && can only call when creating"},{"pc":[513],"errorMessage":"Only creator can opt-in"},{"pc":[314,420],"errorMessage":"Output below minimum"},{"pc":[203],"errorMessage":"Output below minimum - slippage exceeded"},{"pc":[145,261,366],"errorMessage":"application exists"},{"pc":[453],"errorMessage":"invalid number of bytes for arc4.bool"},{"pc":[93,102,111,120,129,218,227,236,245,329,338,347,356,435,444,506],"errorMessage":"invalid number of bytes for arc4.uint64"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"events":[]} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


/**
 * The argument types for the PactPoolAdapter contract
 */
export type PactPoolAdapterArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'swap_fixed_input(uint64,uint64,uint64,uint64,uint64)uint64': {
      /**
       * Pact pool application ID
       */
      poolAppId: bigint | number
      /**
       * Input asset
       */
      assetIn: bigint | number
      /**
       * Output asset
       */
      assetOut: bigint | number
      /**
       * Amount to swap in
       */
      amountIn: bigint | number
      /**
       * Minimum acceptable output (slippage protection)
       */
      minAmountOut: bigint | number
    }
    'swap_algo_to_asa(uint64,uint64,uint64,uint64)uint64': {
      poolAppId: bigint | number
      outputAsset: bigint | number
      algoAmount: bigint | number
      minAmountOut: bigint | number
    }
    'swap_asa_to_algo(uint64,uint64,uint64,uint64)uint64': {
      poolAppId: bigint | number
      inputAsset: bigint | number
      amountIn: bigint | number
      minAmountOut: bigint | number
    }
    'get_quote(uint64,uint64,bool)uint64': {
      /**
       * Pool application ID
       */
      poolAppId: bigint | number
      /**
       * Amount to swap
       */
      assetInAmount: bigint | number
      /**
       * True if swapping from first asset to second
       */
      isAssetInFirst: boolean
    }
    'opt_into_asset(uint64)bool': {
      /**
       * Asset to opt into
       */
      asset: bigint | number
    }
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'swap_fixed_input(uint64,uint64,uint64,uint64,uint64)uint64': [poolAppId: bigint | number, assetIn: bigint | number, assetOut: bigint | number, amountIn: bigint | number, minAmountOut: bigint | number]
    'swap_algo_to_asa(uint64,uint64,uint64,uint64)uint64': [poolAppId: bigint | number, outputAsset: bigint | number, algoAmount: bigint | number, minAmountOut: bigint | number]
    'swap_asa_to_algo(uint64,uint64,uint64,uint64)uint64': [poolAppId: bigint | number, inputAsset: bigint | number, amountIn: bigint | number, minAmountOut: bigint | number]
    'get_quote(uint64,uint64,bool)uint64': [poolAppId: bigint | number, assetInAmount: bigint | number, isAssetInFirst: boolean]
    'opt_into_asset(uint64)bool': [asset: bigint | number]
  }
}

/**
 * The return type for each method
 */
export type PactPoolAdapterReturns = {
  'swap_fixed_input(uint64,uint64,uint64,uint64,uint64)uint64': bigint
  'swap_algo_to_asa(uint64,uint64,uint64,uint64)uint64': bigint
  'swap_asa_to_algo(uint64,uint64,uint64,uint64)uint64': bigint
  'get_quote(uint64,uint64,bool)uint64': bigint
  'opt_into_asset(uint64)bool': boolean
}

/**
 * Defines the types of available calls and state of the PactPoolAdapter smart contract.
 */
export type PactPoolAdapterTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'swap_fixed_input(uint64,uint64,uint64,uint64,uint64)uint64' | 'swap_fixed_input', {
      argsObj: PactPoolAdapterArgs['obj']['swap_fixed_input(uint64,uint64,uint64,uint64,uint64)uint64']
      argsTuple: PactPoolAdapterArgs['tuple']['swap_fixed_input(uint64,uint64,uint64,uint64,uint64)uint64']
      /**
       * Actual output amount received
       */
      returns: PactPoolAdapterReturns['swap_fixed_input(uint64,uint64,uint64,uint64,uint64)uint64']
    }>
    & Record<'swap_algo_to_asa(uint64,uint64,uint64,uint64)uint64' | 'swap_algo_to_asa', {
      argsObj: PactPoolAdapterArgs['obj']['swap_algo_to_asa(uint64,uint64,uint64,uint64)uint64']
      argsTuple: PactPoolAdapterArgs['tuple']['swap_algo_to_asa(uint64,uint64,uint64,uint64)uint64']
      returns: PactPoolAdapterReturns['swap_algo_to_asa(uint64,uint64,uint64,uint64)uint64']
    }>
    & Record<'swap_asa_to_algo(uint64,uint64,uint64,uint64)uint64' | 'swap_asa_to_algo', {
      argsObj: PactPoolAdapterArgs['obj']['swap_asa_to_algo(uint64,uint64,uint64,uint64)uint64']
      argsTuple: PactPoolAdapterArgs['tuple']['swap_asa_to_algo(uint64,uint64,uint64,uint64)uint64']
      returns: PactPoolAdapterReturns['swap_asa_to_algo(uint64,uint64,uint64,uint64)uint64']
    }>
    & Record<'get_quote(uint64,uint64,bool)uint64' | 'get_quote', {
      argsObj: PactPoolAdapterArgs['obj']['get_quote(uint64,uint64,bool)uint64']
      argsTuple: PactPoolAdapterArgs['tuple']['get_quote(uint64,uint64,bool)uint64']
      /**
       * Expected output amount
       */
      returns: PactPoolAdapterReturns['get_quote(uint64,uint64,bool)uint64']
    }>
    & Record<'opt_into_asset(uint64)bool' | 'opt_into_asset', {
      argsObj: PactPoolAdapterArgs['obj']['opt_into_asset(uint64)bool']
      argsTuple: PactPoolAdapterArgs['tuple']['opt_into_asset(uint64)bool']
      /**
       * True if successful
       */
      returns: PactPoolAdapterReturns['opt_into_asset(uint64)bool']
    }>
}

/**
 * Defines the possible abi call signatures.
 */
export type PactPoolAdapterSignatures = keyof PactPoolAdapterTypes['methods']
/**
 * Defines the possible abi call signatures for methods that return a non-void value.
 */
export type PactPoolAdapterNonVoidMethodSignatures = keyof PactPoolAdapterTypes['methods'] extends infer T ? T extends keyof PactPoolAdapterTypes['methods'] ? MethodReturn<T> extends void ? never : T  : never : never
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the PactPoolAdapter smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends PactPoolAdapterSignatures> = PactPoolAdapterTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the PactPoolAdapter smart contract to the method's return type
 */
export type MethodReturn<TSignature extends PactPoolAdapterSignatures> = PactPoolAdapterTypes['methods'][TSignature]['returns']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the PactPoolAdapter smart contract
 */
export abstract class PactPoolAdapterParamsFactory {
  /**
   * Constructs a no op call for the swap_fixed_input(uint64,uint64,uint64,uint64,uint64)uint64 ABI method
   *
  * Execute a fixed-input swap on Pact pool
  Pact uses a slightly different transaction structure than Tinyman. The pool expects: 1. Asset transfer to pool 2. Application call to trigger swap 3. Pool returns output asset

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static swapFixedInput(params: CallParams<PactPoolAdapterArgs['obj']['swap_fixed_input(uint64,uint64,uint64,uint64,uint64)uint64'] | PactPoolAdapterArgs['tuple']['swap_fixed_input(uint64,uint64,uint64,uint64,uint64)uint64']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'swap_fixed_input(uint64,uint64,uint64,uint64,uint64)uint64' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.poolAppId, params.args.assetIn, params.args.assetOut, params.args.amountIn, params.args.minAmountOut],
    }
  }
  /**
   * Constructs a no op call for the swap_algo_to_asa(uint64,uint64,uint64,uint64)uint64 ABI method
   *
  * Special case: Swap ALGO (native currency) to ASA on Pact
  ALGO swaps use payment transactions instead of asset transfers

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static swapAlgoToAsa(params: CallParams<PactPoolAdapterArgs['obj']['swap_algo_to_asa(uint64,uint64,uint64,uint64)uint64'] | PactPoolAdapterArgs['tuple']['swap_algo_to_asa(uint64,uint64,uint64,uint64)uint64']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'swap_algo_to_asa(uint64,uint64,uint64,uint64)uint64' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.poolAppId, params.args.outputAsset, params.args.algoAmount, params.args.minAmountOut],
    }
  }
  /**
   * Constructs a no op call for the swap_asa_to_algo(uint64,uint64,uint64,uint64)uint64 ABI method
   *
  * Special case: Swap ASA to ALGO on Pact
  Returns ALGO (native currency) to the caller

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static swapAsaToAlgo(params: CallParams<PactPoolAdapterArgs['obj']['swap_asa_to_algo(uint64,uint64,uint64,uint64)uint64'] | PactPoolAdapterArgs['tuple']['swap_asa_to_algo(uint64,uint64,uint64,uint64)uint64']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'swap_asa_to_algo(uint64,uint64,uint64,uint64)uint64' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.poolAppId, params.args.inputAsset, params.args.amountIn, params.args.minAmountOut],
    }
  }
  /**
   * Constructs a no op call for the get_quote(uint64,uint64,bool)uint64 ABI method
   *
  * Get quote from Pact pool without executing swap
  This calls the pool's quote method to estimate output

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static getQuote(params: CallParams<PactPoolAdapterArgs['obj']['get_quote(uint64,uint64,bool)uint64'] | PactPoolAdapterArgs['tuple']['get_quote(uint64,uint64,bool)uint64']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'get_quote(uint64,uint64,bool)uint64' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.poolAppId, params.args.assetInAmount, params.args.isAssetInFirst],
    }
  }
  /**
   * Constructs a no op call for the opt_into_asset(uint64)bool ABI method
   *
  * Opt the adapter contract into an asset
  Required before the adapter can hold or transfer the asset. Only the contract creator can call this.

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static optIntoAsset(params: CallParams<PactPoolAdapterArgs['obj']['opt_into_asset(uint64)bool'] | PactPoolAdapterArgs['tuple']['opt_into_asset(uint64)bool']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'opt_into_asset(uint64)bool' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.asset],
    }
  }
}

/**
 * A client to make calls to the PactPoolAdapter smart contract
 */
export class PactPoolAdapterClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `PactPoolAdapterClient`
   *
   * @param appClient An `AppClient` instance which has been created with the PactPoolAdapter app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `PactPoolAdapterClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Checks for decode errors on the given return value and maps the return value to the return type for the given method
   * @returns The typed return value or undefined if there was no value
   */
  decodeReturnValue<TSignature extends PactPoolAdapterNonVoidMethodSignatures>(method: TSignature, returnValue: ABIReturn | undefined) {
    return returnValue !== undefined ? getArc56ReturnValue<MethodReturn<TSignature>>(returnValue, this.appClient.getABIMethod(method), APP_SPEC.structs) : undefined
  }

  /**
   * Returns a new `PactPoolAdapterClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<PactPoolAdapterClient> {
    return new PactPoolAdapterClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `PactPoolAdapterClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<PactPoolAdapterClient> {
    return new PactPoolAdapterClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Makes a clear_state call to an existing instance of the PactPoolAdapter smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the PactPoolAdapter smart contract using the `swap_fixed_input(uint64,uint64,uint64,uint64,uint64)uint64` ABI method.
     *
    * Execute a fixed-input swap on Pact pool
    Pact uses a slightly different transaction structure than Tinyman. The pool expects: 1. Asset transfer to pool 2. Application call to trigger swap 3. Pool returns output asset

     *
     * @param params The params for the smart contract call
     * @returns The call params: Actual output amount received
     */
    swapFixedInput: (params: CallParams<PactPoolAdapterArgs['obj']['swap_fixed_input(uint64,uint64,uint64,uint64,uint64)uint64'] | PactPoolAdapterArgs['tuple']['swap_fixed_input(uint64,uint64,uint64,uint64,uint64)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(PactPoolAdapterParamsFactory.swapFixedInput(params))
    },

    /**
     * Makes a call to the PactPoolAdapter smart contract using the `swap_algo_to_asa(uint64,uint64,uint64,uint64)uint64` ABI method.
     *
    * Special case: Swap ALGO (native currency) to ASA on Pact
    ALGO swaps use payment transactions instead of asset transfers

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    swapAlgoToAsa: (params: CallParams<PactPoolAdapterArgs['obj']['swap_algo_to_asa(uint64,uint64,uint64,uint64)uint64'] | PactPoolAdapterArgs['tuple']['swap_algo_to_asa(uint64,uint64,uint64,uint64)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(PactPoolAdapterParamsFactory.swapAlgoToAsa(params))
    },

    /**
     * Makes a call to the PactPoolAdapter smart contract using the `swap_asa_to_algo(uint64,uint64,uint64,uint64)uint64` ABI method.
     *
    * Special case: Swap ASA to ALGO on Pact
    Returns ALGO (native currency) to the caller

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    swapAsaToAlgo: (params: CallParams<PactPoolAdapterArgs['obj']['swap_asa_to_algo(uint64,uint64,uint64,uint64)uint64'] | PactPoolAdapterArgs['tuple']['swap_asa_to_algo(uint64,uint64,uint64,uint64)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(PactPoolAdapterParamsFactory.swapAsaToAlgo(params))
    },

    /**
     * Makes a call to the PactPoolAdapter smart contract using the `get_quote(uint64,uint64,bool)uint64` ABI method.
     *
    * Get quote from Pact pool without executing swap
    This calls the pool's quote method to estimate output

     *
     * @param params The params for the smart contract call
     * @returns The call params: Expected output amount
     */
    getQuote: (params: CallParams<PactPoolAdapterArgs['obj']['get_quote(uint64,uint64,bool)uint64'] | PactPoolAdapterArgs['tuple']['get_quote(uint64,uint64,bool)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(PactPoolAdapterParamsFactory.getQuote(params))
    },

    /**
     * Makes a call to the PactPoolAdapter smart contract using the `opt_into_asset(uint64)bool` ABI method.
     *
    * Opt the adapter contract into an asset
    Required before the adapter can hold or transfer the asset. Only the contract creator can call this.

     *
     * @param params The params for the smart contract call
     * @returns The call params: True if successful
     */
    optIntoAsset: (params: CallParams<PactPoolAdapterArgs['obj']['opt_into_asset(uint64)bool'] | PactPoolAdapterArgs['tuple']['opt_into_asset(uint64)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(PactPoolAdapterParamsFactory.optIntoAsset(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Makes a clear_state call to an existing instance of the PactPoolAdapter smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the PactPoolAdapter smart contract using the `swap_fixed_input(uint64,uint64,uint64,uint64,uint64)uint64` ABI method.
     *
    * Execute a fixed-input swap on Pact pool
    Pact uses a slightly different transaction structure than Tinyman. The pool expects: 1. Asset transfer to pool 2. Application call to trigger swap 3. Pool returns output asset

     *
     * @param params The params for the smart contract call
     * @returns The call transaction: Actual output amount received
     */
    swapFixedInput: (params: CallParams<PactPoolAdapterArgs['obj']['swap_fixed_input(uint64,uint64,uint64,uint64,uint64)uint64'] | PactPoolAdapterArgs['tuple']['swap_fixed_input(uint64,uint64,uint64,uint64,uint64)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(PactPoolAdapterParamsFactory.swapFixedInput(params))
    },

    /**
     * Makes a call to the PactPoolAdapter smart contract using the `swap_algo_to_asa(uint64,uint64,uint64,uint64)uint64` ABI method.
     *
    * Special case: Swap ALGO (native currency) to ASA on Pact
    ALGO swaps use payment transactions instead of asset transfers

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    swapAlgoToAsa: (params: CallParams<PactPoolAdapterArgs['obj']['swap_algo_to_asa(uint64,uint64,uint64,uint64)uint64'] | PactPoolAdapterArgs['tuple']['swap_algo_to_asa(uint64,uint64,uint64,uint64)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(PactPoolAdapterParamsFactory.swapAlgoToAsa(params))
    },

    /**
     * Makes a call to the PactPoolAdapter smart contract using the `swap_asa_to_algo(uint64,uint64,uint64,uint64)uint64` ABI method.
     *
    * Special case: Swap ASA to ALGO on Pact
    Returns ALGO (native currency) to the caller

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    swapAsaToAlgo: (params: CallParams<PactPoolAdapterArgs['obj']['swap_asa_to_algo(uint64,uint64,uint64,uint64)uint64'] | PactPoolAdapterArgs['tuple']['swap_asa_to_algo(uint64,uint64,uint64,uint64)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(PactPoolAdapterParamsFactory.swapAsaToAlgo(params))
    },

    /**
     * Makes a call to the PactPoolAdapter smart contract using the `get_quote(uint64,uint64,bool)uint64` ABI method.
     *
    * Get quote from Pact pool without executing swap
    This calls the pool's quote method to estimate output

     *
     * @param params The params for the smart contract call
     * @returns The call transaction: Expected output amount
     */
    getQuote: (params: CallParams<PactPoolAdapterArgs['obj']['get_quote(uint64,uint64,bool)uint64'] | PactPoolAdapterArgs['tuple']['get_quote(uint64,uint64,bool)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(PactPoolAdapterParamsFactory.getQuote(params))
    },

    /**
     * Makes a call to the PactPoolAdapter smart contract using the `opt_into_asset(uint64)bool` ABI method.
     *
    * Opt the adapter contract into an asset
    Required before the adapter can hold or transfer the asset. Only the contract creator can call this.

     *
     * @param params The params for the smart contract call
     * @returns The call transaction: True if successful
     */
    optIntoAsset: (params: CallParams<PactPoolAdapterArgs['obj']['opt_into_asset(uint64)bool'] | PactPoolAdapterArgs['tuple']['opt_into_asset(uint64)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(PactPoolAdapterParamsFactory.optIntoAsset(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Makes a clear_state call to an existing instance of the PactPoolAdapter smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the PactPoolAdapter smart contract using the `swap_fixed_input(uint64,uint64,uint64,uint64,uint64)uint64` ABI method.
     *
    * Execute a fixed-input swap on Pact pool
    Pact uses a slightly different transaction structure than Tinyman. The pool expects: 1. Asset transfer to pool 2. Application call to trigger swap 3. Pool returns output asset

     *
     * @param params The params for the smart contract call
     * @returns The call result: Actual output amount received
     */
    swapFixedInput: async (params: CallParams<PactPoolAdapterArgs['obj']['swap_fixed_input(uint64,uint64,uint64,uint64,uint64)uint64'] | PactPoolAdapterArgs['tuple']['swap_fixed_input(uint64,uint64,uint64,uint64,uint64)uint64']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(PactPoolAdapterParamsFactory.swapFixedInput(params))
      return {...result, return: result.return as unknown as (undefined | PactPoolAdapterReturns['swap_fixed_input(uint64,uint64,uint64,uint64,uint64)uint64'])}
    },

    /**
     * Makes a call to the PactPoolAdapter smart contract using the `swap_algo_to_asa(uint64,uint64,uint64,uint64)uint64` ABI method.
     *
    * Special case: Swap ALGO (native currency) to ASA on Pact
    ALGO swaps use payment transactions instead of asset transfers

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    swapAlgoToAsa: async (params: CallParams<PactPoolAdapterArgs['obj']['swap_algo_to_asa(uint64,uint64,uint64,uint64)uint64'] | PactPoolAdapterArgs['tuple']['swap_algo_to_asa(uint64,uint64,uint64,uint64)uint64']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(PactPoolAdapterParamsFactory.swapAlgoToAsa(params))
      return {...result, return: result.return as unknown as (undefined | PactPoolAdapterReturns['swap_algo_to_asa(uint64,uint64,uint64,uint64)uint64'])}
    },

    /**
     * Makes a call to the PactPoolAdapter smart contract using the `swap_asa_to_algo(uint64,uint64,uint64,uint64)uint64` ABI method.
     *
    * Special case: Swap ASA to ALGO on Pact
    Returns ALGO (native currency) to the caller

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    swapAsaToAlgo: async (params: CallParams<PactPoolAdapterArgs['obj']['swap_asa_to_algo(uint64,uint64,uint64,uint64)uint64'] | PactPoolAdapterArgs['tuple']['swap_asa_to_algo(uint64,uint64,uint64,uint64)uint64']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(PactPoolAdapterParamsFactory.swapAsaToAlgo(params))
      return {...result, return: result.return as unknown as (undefined | PactPoolAdapterReturns['swap_asa_to_algo(uint64,uint64,uint64,uint64)uint64'])}
    },

    /**
     * Makes a call to the PactPoolAdapter smart contract using the `get_quote(uint64,uint64,bool)uint64` ABI method.
     *
    * Get quote from Pact pool without executing swap
    This calls the pool's quote method to estimate output

     *
     * @param params The params for the smart contract call
     * @returns The call result: Expected output amount
     */
    getQuote: async (params: CallParams<PactPoolAdapterArgs['obj']['get_quote(uint64,uint64,bool)uint64'] | PactPoolAdapterArgs['tuple']['get_quote(uint64,uint64,bool)uint64']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(PactPoolAdapterParamsFactory.getQuote(params))
      return {...result, return: result.return as unknown as (undefined | PactPoolAdapterReturns['get_quote(uint64,uint64,bool)uint64'])}
    },

    /**
     * Makes a call to the PactPoolAdapter smart contract using the `opt_into_asset(uint64)bool` ABI method.
     *
    * Opt the adapter contract into an asset
    Required before the adapter can hold or transfer the asset. Only the contract creator can call this.

     *
     * @param params The params for the smart contract call
     * @returns The call result: True if successful
     */
    optIntoAsset: async (params: CallParams<PactPoolAdapterArgs['obj']['opt_into_asset(uint64)bool'] | PactPoolAdapterArgs['tuple']['opt_into_asset(uint64)bool']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(PactPoolAdapterParamsFactory.optIntoAsset(params))
      return {...result, return: result.return as unknown as (undefined | PactPoolAdapterReturns['opt_into_asset(uint64)bool'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new PactPoolAdapterClient(this.appClient.clone(params))
  }

  /**
   * Methods to access state for the current PactPoolAdapter app
   */
  state = {
  }

  public newGroup(): PactPoolAdapterComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a swap_fixed_input(uint64,uint64,uint64,uint64,uint64)uint64 method call against the PactPoolAdapter contract
       */
      swapFixedInput(params: CallParams<PactPoolAdapterArgs['obj']['swap_fixed_input(uint64,uint64,uint64,uint64,uint64)uint64'] | PactPoolAdapterArgs['tuple']['swap_fixed_input(uint64,uint64,uint64,uint64,uint64)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.swapFixedInput(params)))
        resultMappers.push((v) => client.decodeReturnValue('swap_fixed_input(uint64,uint64,uint64,uint64,uint64)uint64', v))
        return this
      },
      /**
       * Add a swap_algo_to_asa(uint64,uint64,uint64,uint64)uint64 method call against the PactPoolAdapter contract
       */
      swapAlgoToAsa(params: CallParams<PactPoolAdapterArgs['obj']['swap_algo_to_asa(uint64,uint64,uint64,uint64)uint64'] | PactPoolAdapterArgs['tuple']['swap_algo_to_asa(uint64,uint64,uint64,uint64)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.swapAlgoToAsa(params)))
        resultMappers.push((v) => client.decodeReturnValue('swap_algo_to_asa(uint64,uint64,uint64,uint64)uint64', v))
        return this
      },
      /**
       * Add a swap_asa_to_algo(uint64,uint64,uint64,uint64)uint64 method call against the PactPoolAdapter contract
       */
      swapAsaToAlgo(params: CallParams<PactPoolAdapterArgs['obj']['swap_asa_to_algo(uint64,uint64,uint64,uint64)uint64'] | PactPoolAdapterArgs['tuple']['swap_asa_to_algo(uint64,uint64,uint64,uint64)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.swapAsaToAlgo(params)))
        resultMappers.push((v) => client.decodeReturnValue('swap_asa_to_algo(uint64,uint64,uint64,uint64)uint64', v))
        return this
      },
      /**
       * Add a get_quote(uint64,uint64,bool)uint64 method call against the PactPoolAdapter contract
       */
      getQuote(params: CallParams<PactPoolAdapterArgs['obj']['get_quote(uint64,uint64,bool)uint64'] | PactPoolAdapterArgs['tuple']['get_quote(uint64,uint64,bool)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.getQuote(params)))
        resultMappers.push((v) => client.decodeReturnValue('get_quote(uint64,uint64,bool)uint64', v))
        return this
      },
      /**
       * Add a opt_into_asset(uint64)bool method call against the PactPoolAdapter contract
       */
      optIntoAsset(params: CallParams<PactPoolAdapterArgs['obj']['opt_into_asset(uint64)bool'] | PactPoolAdapterArgs['tuple']['opt_into_asset(uint64)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.optIntoAsset(params)))
        resultMappers.push((v) => client.decodeReturnValue('opt_into_asset(uint64)bool', v))
        return this
      },
      /**
       * Add a clear state call to the PactPoolAdapter contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as PactPoolAdapterComposer
  }
}
export type PactPoolAdapterComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the swap_fixed_input(uint64,uint64,uint64,uint64,uint64)uint64 ABI method.
   *
  * Execute a fixed-input swap on Pact pool
  Pact uses a slightly different transaction structure than Tinyman. The pool expects: 1. Asset transfer to pool 2. Application call to trigger swap 3. Pool returns output asset

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  swapFixedInput(params?: CallParams<PactPoolAdapterArgs['obj']['swap_fixed_input(uint64,uint64,uint64,uint64,uint64)uint64'] | PactPoolAdapterArgs['tuple']['swap_fixed_input(uint64,uint64,uint64,uint64,uint64)uint64']>): PactPoolAdapterComposer<[...TReturns, PactPoolAdapterReturns['swap_fixed_input(uint64,uint64,uint64,uint64,uint64)uint64'] | undefined]>

  /**
   * Calls the swap_algo_to_asa(uint64,uint64,uint64,uint64)uint64 ABI method.
   *
  * Special case: Swap ALGO (native currency) to ASA on Pact
  ALGO swaps use payment transactions instead of asset transfers

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  swapAlgoToAsa(params?: CallParams<PactPoolAdapterArgs['obj']['swap_algo_to_asa(uint64,uint64,uint64,uint64)uint64'] | PactPoolAdapterArgs['tuple']['swap_algo_to_asa(uint64,uint64,uint64,uint64)uint64']>): PactPoolAdapterComposer<[...TReturns, PactPoolAdapterReturns['swap_algo_to_asa(uint64,uint64,uint64,uint64)uint64'] | undefined]>

  /**
   * Calls the swap_asa_to_algo(uint64,uint64,uint64,uint64)uint64 ABI method.
   *
  * Special case: Swap ASA to ALGO on Pact
  Returns ALGO (native currency) to the caller

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  swapAsaToAlgo(params?: CallParams<PactPoolAdapterArgs['obj']['swap_asa_to_algo(uint64,uint64,uint64,uint64)uint64'] | PactPoolAdapterArgs['tuple']['swap_asa_to_algo(uint64,uint64,uint64,uint64)uint64']>): PactPoolAdapterComposer<[...TReturns, PactPoolAdapterReturns['swap_asa_to_algo(uint64,uint64,uint64,uint64)uint64'] | undefined]>

  /**
   * Calls the get_quote(uint64,uint64,bool)uint64 ABI method.
   *
  * Get quote from Pact pool without executing swap
  This calls the pool's quote method to estimate output

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  getQuote(params?: CallParams<PactPoolAdapterArgs['obj']['get_quote(uint64,uint64,bool)uint64'] | PactPoolAdapterArgs['tuple']['get_quote(uint64,uint64,bool)uint64']>): PactPoolAdapterComposer<[...TReturns, PactPoolAdapterReturns['get_quote(uint64,uint64,bool)uint64'] | undefined]>

  /**
   * Calls the opt_into_asset(uint64)bool ABI method.
   *
  * Opt the adapter contract into an asset
  Required before the adapter can hold or transfer the asset. Only the contract creator can call this.

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  optIntoAsset(params?: CallParams<PactPoolAdapterArgs['obj']['opt_into_asset(uint64)bool'] | PactPoolAdapterArgs['tuple']['opt_into_asset(uint64)bool']>): PactPoolAdapterComposer<[...TReturns, PactPoolAdapterReturns['opt_into_asset(uint64)bool'] | undefined]>

  /**
   * Makes a clear_state call to an existing instance of the PactPoolAdapter smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): PactPoolAdapterComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): PactPoolAdapterComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<PactPoolAdapterComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<PactPoolAdapterComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<PactPoolAdapterComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<PactPoolAdapterComposerResults<TReturns>>
}
export type PactPoolAdapterComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

