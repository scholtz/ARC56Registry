/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"TokenLaunchpad","structs":{},"methods":[{"name":"configure","args":[{"type":"uint64","name":"asa"},{"type":"uint64","name":"total_supply"},{"type":"uint64","name":"tokens_for_sale"},{"type":"uint64","name":"start_price"},{"type":"uint64","name":"target_price"},{"type":"uint64","name":"bonding_target"},{"type":"uint64","name":"curve_type"},{"type":"uint64","name":"max_buy_per_tx"},{"type":"uint64","name":"max_buy_per_user"},{"type":"uint64","name":"liquidity_percent"},{"type":"uint64","name":"liquidity_lock_days"},{"type":"address","name":"platform_address"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Configure sale parameters (creator only, pre-launch)","events":[],"recommendations":{}},{"name":"bootstrap","args":[{"type":"uint64","name":"asa"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Opt contract into ASA (creator only)","events":[],"recommendations":{}},{"name":"buy","args":[{"type":"uint64","name":"quantity"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Buy tokens via bonding curve.\nIMPORTANT: this is an ABI method — callers must invoke via ABI (app client) so the application call carries the encoded ABI args. Also a grouped Payment txn (gtxn[0]) must send ALGO to the app address prior to this AppCall (gtxn[1]).","events":[],"recommendations":{}},{"name":"finalize","args":[],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Finalize sale and distribute funds (creator only)","events":[],"recommendations":{}}],"arcs":[22,28],"desc":"\n    Token Launchpad with Bonding Curve (Linear implementation).\n    ","networks":{},"state":{"schema":{"global":{"ints":15,"bytes":2},"local":{"ints":0,"bytes":0}},"keys":{"global":{"creator":{"keyType":"AVMString","valueType":"address","key":"Y3JlYXRvcg=="},"asa_id":{"keyType":"AVMString","valueType":"AVMUint64","key":"YXNhX2lk"},"total_supply":{"keyType":"AVMString","valueType":"AVMUint64","key":"dG90YWxfc3VwcGx5"},"tokens_for_sale":{"keyType":"AVMString","valueType":"AVMUint64","key":"dG9rZW5zX2Zvcl9zYWxl"},"start_price":{"keyType":"AVMString","valueType":"AVMUint64","key":"c3RhcnRfcHJpY2U="},"target_price":{"keyType":"AVMString","valueType":"AVMUint64","key":"dGFyZ2V0X3ByaWNl"},"bonding_target":{"keyType":"AVMString","valueType":"AVMUint64","key":"Ym9uZGluZ190YXJnZXQ="},"curve_type":{"keyType":"AVMString","valueType":"AVMUint64","key":"Y3VydmVfdHlwZQ=="},"tokens_sold":{"keyType":"AVMString","valueType":"AVMUint64","key":"dG9rZW5zX3NvbGQ="},"algo_raised":{"keyType":"AVMString","valueType":"AVMUint64","key":"YWxnb19yYWlzZWQ="},"max_buy_per_tx":{"keyType":"AVMString","valueType":"AVMUint64","key":"bWF4X2J1eV9wZXJfdHg="},"max_buy_per_user":{"keyType":"AVMString","valueType":"AVMUint64","key":"bWF4X2J1eV9wZXJfdXNlcg=="},"liquidity_percent":{"keyType":"AVMString","valueType":"AVMUint64","key":"bGlxdWlkaXR5X3BlcmNlbnQ="},"liquidity_lock_days":{"keyType":"AVMString","valueType":"AVMUint64","key":"bGlxdWlkaXR5X2xvY2tfZGF5cw=="},"launch_status":{"keyType":"AVMString","valueType":"AVMUint64","key":"bGF1bmNoX3N0YXR1cw=="},"platform_address":{"keyType":"AVMString","valueType":"address","key":"cGxhdGZvcm1fYWRkcmVzcw=="},"platform_fee_percent":{"keyType":"AVMString","valueType":"AVMUint64","key":"cGxhdGZvcm1fZmVlX3BlcmNlbnQ="}},"local":{},"box":{}},"maps":{"global":{},"local":{},"box":{}}},"bareActions":{"create":["NoOp"],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[501],"errorMessage":"Already configured"},{"pc":[767],"errorMessage":"Exceeds max per tx"},{"pc":[758],"errorMessage":"Exceeds max per user"},{"pc":[811],"errorMessage":"Insufficient payment provided"},{"pc":[707],"errorMessage":"Not enough tokens"},{"pc":[329],"errorMessage":"OnCompletion must be NoOp"},{"pc":[376],"errorMessage":"OnCompletion must be NoOp && can only call when creating"},{"pc":[495,617,919],"errorMessage":"Only creator"},{"pc":[720],"errorMessage":"Payment must go to app address"},{"pc":[727],"errorMessage":"Payment sender must equal caller"},{"pc":[659],"errorMessage":"Quantity must be positive"},{"pc":[926],"errorMessage":"Sale not completed"},{"pc":[655],"errorMessage":"Sale not live"},{"pc":[819,897,930],"errorMessage":"check self.algo_raised exists"},{"pc":[844],"errorMessage":"check self.asa_id exists"},{"pc":[902],"errorMessage":"check self.bonding_target exists"},{"pc":[493,615,917,979],"errorMessage":"check self.creator exists"},{"pc":[499,652,923],"errorMessage":"check self.launch_status exists"},{"pc":[763],"errorMessage":"check self.max_buy_per_tx exists"},{"pc":[754],"errorMessage":"check self.max_buy_per_user exists"},{"pc":[957],"errorMessage":"check self.platform_address exists"},{"pc":[935],"errorMessage":"check self.platform_fee_percent exists"},{"pc":[688],"errorMessage":"check self.start_price exists"},{"pc":[696],"errorMessage":"check self.target_price exists"},{"pc":[670],"errorMessage":"check self.tokens_for_sale exists"},{"pc":[680,887],"errorMessage":"check self.tokens_sold exists"},{"pc":[487],"errorMessage":"invalid number of bytes for arc4.static_array<arc4.uint8, 32>"},{"pc":[397,406,414,422,430,438,446,454,462,470,478,608,648],"errorMessage":"invalid number of bytes for arc4.uint64"},{"pc":[520],"errorMessage":"start_price must be < target_price"},{"pc":[526],"errorMessage":"tokens_for_sale <= total_supply"},{"pc":[511],"errorMessage":"tokens_for_sale must be > 0"},{"pc":[699],"errorMessage":"tokens_for_sale not set"},{"pc":[506],"errorMessage":"total_supply must be > 0"},{"pc":[713],"errorMessage":"transaction type is pay"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"events":[]} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


/**
 * The argument types for the TokenLaunchpad contract
 */
export type TokenLaunchpadArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'configure(uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64,address)void': {
      asa: bigint | number
      totalSupply: bigint | number
      tokensForSale: bigint | number
      startPrice: bigint | number
      targetPrice: bigint | number
      bondingTarget: bigint | number
      curveType: bigint | number
      maxBuyPerTx: bigint | number
      maxBuyPerUser: bigint | number
      liquidityPercent: bigint | number
      liquidityLockDays: bigint | number
      platformAddress: string
    }
    'bootstrap(uint64)void': {
      asa: bigint | number
    }
    'buy(uint64)void': {
      quantity: bigint | number
    }
    'finalize()void': Record<string, never>
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'configure(uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64,address)void': [asa: bigint | number, totalSupply: bigint | number, tokensForSale: bigint | number, startPrice: bigint | number, targetPrice: bigint | number, bondingTarget: bigint | number, curveType: bigint | number, maxBuyPerTx: bigint | number, maxBuyPerUser: bigint | number, liquidityPercent: bigint | number, liquidityLockDays: bigint | number, platformAddress: string]
    'bootstrap(uint64)void': [asa: bigint | number]
    'buy(uint64)void': [quantity: bigint | number]
    'finalize()void': []
  }
}

/**
 * The return type for each method
 */
export type TokenLaunchpadReturns = {
  'configure(uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64,address)void': void
  'bootstrap(uint64)void': void
  'buy(uint64)void': void
  'finalize()void': void
}

/**
 * Defines the types of available calls and state of the TokenLaunchpad smart contract.
 */
export type TokenLaunchpadTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'configure(uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64,address)void' | 'configure', {
      argsObj: TokenLaunchpadArgs['obj']['configure(uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64,address)void']
      argsTuple: TokenLaunchpadArgs['tuple']['configure(uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64,address)void']
      returns: TokenLaunchpadReturns['configure(uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64,address)void']
    }>
    & Record<'bootstrap(uint64)void' | 'bootstrap', {
      argsObj: TokenLaunchpadArgs['obj']['bootstrap(uint64)void']
      argsTuple: TokenLaunchpadArgs['tuple']['bootstrap(uint64)void']
      returns: TokenLaunchpadReturns['bootstrap(uint64)void']
    }>
    & Record<'buy(uint64)void' | 'buy', {
      argsObj: TokenLaunchpadArgs['obj']['buy(uint64)void']
      argsTuple: TokenLaunchpadArgs['tuple']['buy(uint64)void']
      returns: TokenLaunchpadReturns['buy(uint64)void']
    }>
    & Record<'finalize()void' | 'finalize', {
      argsObj: TokenLaunchpadArgs['obj']['finalize()void']
      argsTuple: TokenLaunchpadArgs['tuple']['finalize()void']
      returns: TokenLaunchpadReturns['finalize()void']
    }>
  /**
   * Defines the shape of the state of the application.
   */
  state: {
    global: {
      keys: {
        creator: string
        asaId: bigint
        totalSupply: bigint
        tokensForSale: bigint
        startPrice: bigint
        targetPrice: bigint
        bondingTarget: bigint
        curveType: bigint
        tokensSold: bigint
        algoRaised: bigint
        maxBuyPerTx: bigint
        maxBuyPerUser: bigint
        liquidityPercent: bigint
        liquidityLockDays: bigint
        launchStatus: bigint
        platformAddress: string
        platformFeePercent: bigint
      }
      maps: {}
    }
  }
}

/**
 * Defines the possible abi call signatures.
 */
export type TokenLaunchpadSignatures = keyof TokenLaunchpadTypes['methods']
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the TokenLaunchpad smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends TokenLaunchpadSignatures> = TokenLaunchpadTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the TokenLaunchpad smart contract to the method's return type
 */
export type MethodReturn<TSignature extends TokenLaunchpadSignatures> = TokenLaunchpadTypes['methods'][TSignature]['returns']

/**
 * Defines the shape of the keyed global state of the application.
 */
export type GlobalKeysState = TokenLaunchpadTypes['state']['global']['keys']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the TokenLaunchpad smart contract
 */
export abstract class TokenLaunchpadParamsFactory {
  /**
   * Constructs a no op call for the configure(uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64,address)void ABI method
   *
   * Configure sale parameters (creator only, pre-launch)
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static configure(params: CallParams<TokenLaunchpadArgs['obj']['configure(uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64,address)void'] | TokenLaunchpadArgs['tuple']['configure(uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64,address)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'configure(uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64,address)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.asa, params.args.totalSupply, params.args.tokensForSale, params.args.startPrice, params.args.targetPrice, params.args.bondingTarget, params.args.curveType, params.args.maxBuyPerTx, params.args.maxBuyPerUser, params.args.liquidityPercent, params.args.liquidityLockDays, params.args.platformAddress],
    }
  }
  /**
   * Constructs a no op call for the bootstrap(uint64)void ABI method
   *
   * Opt contract into ASA (creator only)
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static bootstrap(params: CallParams<TokenLaunchpadArgs['obj']['bootstrap(uint64)void'] | TokenLaunchpadArgs['tuple']['bootstrap(uint64)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'bootstrap(uint64)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.asa],
    }
  }
  /**
   * Constructs a no op call for the buy(uint64)void ABI method
   *
  * Buy tokens via bonding curve.
  IMPORTANT: this is an ABI method — callers must invoke via ABI (app client) so the application call carries the encoded ABI args. Also a grouped Payment txn (gtxn[0]) must send ALGO to the app address prior to this AppCall (gtxn[1]).

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static buy(params: CallParams<TokenLaunchpadArgs['obj']['buy(uint64)void'] | TokenLaunchpadArgs['tuple']['buy(uint64)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'buy(uint64)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.quantity],
    }
  }
  /**
   * Constructs a no op call for the finalize()void ABI method
   *
   * Finalize sale and distribute funds (creator only)
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static finalize(params: CallParams<TokenLaunchpadArgs['obj']['finalize()void'] | TokenLaunchpadArgs['tuple']['finalize()void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'finalize()void' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
}

/**
 * A client to make calls to the TokenLaunchpad smart contract
 */
export class TokenLaunchpadClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `TokenLaunchpadClient`
   *
   * @param appClient An `AppClient` instance which has been created with the TokenLaunchpad app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `TokenLaunchpadClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Returns a new `TokenLaunchpadClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<TokenLaunchpadClient> {
    return new TokenLaunchpadClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `TokenLaunchpadClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<TokenLaunchpadClient> {
    return new TokenLaunchpadClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Makes a clear_state call to an existing instance of the TokenLaunchpad smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the TokenLaunchpad smart contract using the `configure(uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64,address)void` ABI method.
     *
     * Configure sale parameters (creator only, pre-launch)
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    configure: (params: CallParams<TokenLaunchpadArgs['obj']['configure(uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64,address)void'] | TokenLaunchpadArgs['tuple']['configure(uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64,address)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(TokenLaunchpadParamsFactory.configure(params))
    },

    /**
     * Makes a call to the TokenLaunchpad smart contract using the `bootstrap(uint64)void` ABI method.
     *
     * Opt contract into ASA (creator only)
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    bootstrap: (params: CallParams<TokenLaunchpadArgs['obj']['bootstrap(uint64)void'] | TokenLaunchpadArgs['tuple']['bootstrap(uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(TokenLaunchpadParamsFactory.bootstrap(params))
    },

    /**
     * Makes a call to the TokenLaunchpad smart contract using the `buy(uint64)void` ABI method.
     *
    * Buy tokens via bonding curve.
    IMPORTANT: this is an ABI method — callers must invoke via ABI (app client) so the application call carries the encoded ABI args. Also a grouped Payment txn (gtxn[0]) must send ALGO to the app address prior to this AppCall (gtxn[1]).

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    buy: (params: CallParams<TokenLaunchpadArgs['obj']['buy(uint64)void'] | TokenLaunchpadArgs['tuple']['buy(uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(TokenLaunchpadParamsFactory.buy(params))
    },

    /**
     * Makes a call to the TokenLaunchpad smart contract using the `finalize()void` ABI method.
     *
     * Finalize sale and distribute funds (creator only)
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    finalize: (params: CallParams<TokenLaunchpadArgs['obj']['finalize()void'] | TokenLaunchpadArgs['tuple']['finalize()void']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(TokenLaunchpadParamsFactory.finalize(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Makes a clear_state call to an existing instance of the TokenLaunchpad smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the TokenLaunchpad smart contract using the `configure(uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64,address)void` ABI method.
     *
     * Configure sale parameters (creator only, pre-launch)
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    configure: (params: CallParams<TokenLaunchpadArgs['obj']['configure(uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64,address)void'] | TokenLaunchpadArgs['tuple']['configure(uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64,address)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(TokenLaunchpadParamsFactory.configure(params))
    },

    /**
     * Makes a call to the TokenLaunchpad smart contract using the `bootstrap(uint64)void` ABI method.
     *
     * Opt contract into ASA (creator only)
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    bootstrap: (params: CallParams<TokenLaunchpadArgs['obj']['bootstrap(uint64)void'] | TokenLaunchpadArgs['tuple']['bootstrap(uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(TokenLaunchpadParamsFactory.bootstrap(params))
    },

    /**
     * Makes a call to the TokenLaunchpad smart contract using the `buy(uint64)void` ABI method.
     *
    * Buy tokens via bonding curve.
    IMPORTANT: this is an ABI method — callers must invoke via ABI (app client) so the application call carries the encoded ABI args. Also a grouped Payment txn (gtxn[0]) must send ALGO to the app address prior to this AppCall (gtxn[1]).

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    buy: (params: CallParams<TokenLaunchpadArgs['obj']['buy(uint64)void'] | TokenLaunchpadArgs['tuple']['buy(uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(TokenLaunchpadParamsFactory.buy(params))
    },

    /**
     * Makes a call to the TokenLaunchpad smart contract using the `finalize()void` ABI method.
     *
     * Finalize sale and distribute funds (creator only)
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    finalize: (params: CallParams<TokenLaunchpadArgs['obj']['finalize()void'] | TokenLaunchpadArgs['tuple']['finalize()void']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(TokenLaunchpadParamsFactory.finalize(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Makes a clear_state call to an existing instance of the TokenLaunchpad smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the TokenLaunchpad smart contract using the `configure(uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64,address)void` ABI method.
     *
     * Configure sale parameters (creator only, pre-launch)
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    configure: async (params: CallParams<TokenLaunchpadArgs['obj']['configure(uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64,address)void'] | TokenLaunchpadArgs['tuple']['configure(uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64,address)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(TokenLaunchpadParamsFactory.configure(params))
      return {...result, return: result.return as unknown as (undefined | TokenLaunchpadReturns['configure(uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64,address)void'])}
    },

    /**
     * Makes a call to the TokenLaunchpad smart contract using the `bootstrap(uint64)void` ABI method.
     *
     * Opt contract into ASA (creator only)
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    bootstrap: async (params: CallParams<TokenLaunchpadArgs['obj']['bootstrap(uint64)void'] | TokenLaunchpadArgs['tuple']['bootstrap(uint64)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(TokenLaunchpadParamsFactory.bootstrap(params))
      return {...result, return: result.return as unknown as (undefined | TokenLaunchpadReturns['bootstrap(uint64)void'])}
    },

    /**
     * Makes a call to the TokenLaunchpad smart contract using the `buy(uint64)void` ABI method.
     *
    * Buy tokens via bonding curve.
    IMPORTANT: this is an ABI method — callers must invoke via ABI (app client) so the application call carries the encoded ABI args. Also a grouped Payment txn (gtxn[0]) must send ALGO to the app address prior to this AppCall (gtxn[1]).

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    buy: async (params: CallParams<TokenLaunchpadArgs['obj']['buy(uint64)void'] | TokenLaunchpadArgs['tuple']['buy(uint64)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(TokenLaunchpadParamsFactory.buy(params))
      return {...result, return: result.return as unknown as (undefined | TokenLaunchpadReturns['buy(uint64)void'])}
    },

    /**
     * Makes a call to the TokenLaunchpad smart contract using the `finalize()void` ABI method.
     *
     * Finalize sale and distribute funds (creator only)
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    finalize: async (params: CallParams<TokenLaunchpadArgs['obj']['finalize()void'] | TokenLaunchpadArgs['tuple']['finalize()void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(TokenLaunchpadParamsFactory.finalize(params))
      return {...result, return: result.return as unknown as (undefined | TokenLaunchpadReturns['finalize()void'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new TokenLaunchpadClient(this.appClient.clone(params))
  }

  /**
   * Methods to access state for the current TokenLaunchpad app
   */
  state = {
    /**
     * Methods to access global state for the current TokenLaunchpad app
     */
    global: {
      /**
       * Get all current keyed values from global state
       */
      getAll: async (): Promise<Partial<Expand<GlobalKeysState>>> => {
        const result = await this.appClient.state.global.getAll()
        return {
          creator: result.creator,
          asaId: result.asa_id,
          totalSupply: result.total_supply,
          tokensForSale: result.tokens_for_sale,
          startPrice: result.start_price,
          targetPrice: result.target_price,
          bondingTarget: result.bonding_target,
          curveType: result.curve_type,
          tokensSold: result.tokens_sold,
          algoRaised: result.algo_raised,
          maxBuyPerTx: result.max_buy_per_tx,
          maxBuyPerUser: result.max_buy_per_user,
          liquidityPercent: result.liquidity_percent,
          liquidityLockDays: result.liquidity_lock_days,
          launchStatus: result.launch_status,
          platformAddress: result.platform_address,
          platformFeePercent: result.platform_fee_percent,
        }
      },
      /**
       * Get the current value of the creator key in global state
       */
      creator: async (): Promise<string | undefined> => { return (await this.appClient.state.global.getValue("creator")) as string | undefined },
      /**
       * Get the current value of the asa_id key in global state
       */
      asaId: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("asa_id")) as bigint | undefined },
      /**
       * Get the current value of the total_supply key in global state
       */
      totalSupply: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("total_supply")) as bigint | undefined },
      /**
       * Get the current value of the tokens_for_sale key in global state
       */
      tokensForSale: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("tokens_for_sale")) as bigint | undefined },
      /**
       * Get the current value of the start_price key in global state
       */
      startPrice: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("start_price")) as bigint | undefined },
      /**
       * Get the current value of the target_price key in global state
       */
      targetPrice: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("target_price")) as bigint | undefined },
      /**
       * Get the current value of the bonding_target key in global state
       */
      bondingTarget: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("bonding_target")) as bigint | undefined },
      /**
       * Get the current value of the curve_type key in global state
       */
      curveType: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("curve_type")) as bigint | undefined },
      /**
       * Get the current value of the tokens_sold key in global state
       */
      tokensSold: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("tokens_sold")) as bigint | undefined },
      /**
       * Get the current value of the algo_raised key in global state
       */
      algoRaised: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("algo_raised")) as bigint | undefined },
      /**
       * Get the current value of the max_buy_per_tx key in global state
       */
      maxBuyPerTx: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("max_buy_per_tx")) as bigint | undefined },
      /**
       * Get the current value of the max_buy_per_user key in global state
       */
      maxBuyPerUser: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("max_buy_per_user")) as bigint | undefined },
      /**
       * Get the current value of the liquidity_percent key in global state
       */
      liquidityPercent: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("liquidity_percent")) as bigint | undefined },
      /**
       * Get the current value of the liquidity_lock_days key in global state
       */
      liquidityLockDays: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("liquidity_lock_days")) as bigint | undefined },
      /**
       * Get the current value of the launch_status key in global state
       */
      launchStatus: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("launch_status")) as bigint | undefined },
      /**
       * Get the current value of the platform_address key in global state
       */
      platformAddress: async (): Promise<string | undefined> => { return (await this.appClient.state.global.getValue("platform_address")) as string | undefined },
      /**
       * Get the current value of the platform_fee_percent key in global state
       */
      platformFeePercent: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("platform_fee_percent")) as bigint | undefined },
    },
  }

  public newGroup(): TokenLaunchpadComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a configure(uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64,address)void method call against the TokenLaunchpad contract
       */
      configure(params: CallParams<TokenLaunchpadArgs['obj']['configure(uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64,address)void'] | TokenLaunchpadArgs['tuple']['configure(uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64,address)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.configure(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a bootstrap(uint64)void method call against the TokenLaunchpad contract
       */
      bootstrap(params: CallParams<TokenLaunchpadArgs['obj']['bootstrap(uint64)void'] | TokenLaunchpadArgs['tuple']['bootstrap(uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.bootstrap(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a buy(uint64)void method call against the TokenLaunchpad contract
       */
      buy(params: CallParams<TokenLaunchpadArgs['obj']['buy(uint64)void'] | TokenLaunchpadArgs['tuple']['buy(uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.buy(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a finalize()void method call against the TokenLaunchpad contract
       */
      finalize(params: CallParams<TokenLaunchpadArgs['obj']['finalize()void'] | TokenLaunchpadArgs['tuple']['finalize()void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.finalize(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a clear state call to the TokenLaunchpad contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as TokenLaunchpadComposer
  }
}
export type TokenLaunchpadComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the configure(uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64,address)void ABI method.
   *
   * Configure sale parameters (creator only, pre-launch)
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  configure(params?: CallParams<TokenLaunchpadArgs['obj']['configure(uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64,address)void'] | TokenLaunchpadArgs['tuple']['configure(uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64,address)void']>): TokenLaunchpadComposer<[...TReturns, TokenLaunchpadReturns['configure(uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64,address)void'] | undefined]>

  /**
   * Calls the bootstrap(uint64)void ABI method.
   *
   * Opt contract into ASA (creator only)
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  bootstrap(params?: CallParams<TokenLaunchpadArgs['obj']['bootstrap(uint64)void'] | TokenLaunchpadArgs['tuple']['bootstrap(uint64)void']>): TokenLaunchpadComposer<[...TReturns, TokenLaunchpadReturns['bootstrap(uint64)void'] | undefined]>

  /**
   * Calls the buy(uint64)void ABI method.
   *
  * Buy tokens via bonding curve.
  IMPORTANT: this is an ABI method — callers must invoke via ABI (app client) so the application call carries the encoded ABI args. Also a grouped Payment txn (gtxn[0]) must send ALGO to the app address prior to this AppCall (gtxn[1]).

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  buy(params?: CallParams<TokenLaunchpadArgs['obj']['buy(uint64)void'] | TokenLaunchpadArgs['tuple']['buy(uint64)void']>): TokenLaunchpadComposer<[...TReturns, TokenLaunchpadReturns['buy(uint64)void'] | undefined]>

  /**
   * Calls the finalize()void ABI method.
   *
   * Finalize sale and distribute funds (creator only)
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  finalize(params?: CallParams<TokenLaunchpadArgs['obj']['finalize()void'] | TokenLaunchpadArgs['tuple']['finalize()void']>): TokenLaunchpadComposer<[...TReturns, TokenLaunchpadReturns['finalize()void'] | undefined]>

  /**
   * Makes a clear_state call to an existing instance of the TokenLaunchpad smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): TokenLaunchpadComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): TokenLaunchpadComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<TokenLaunchpadComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<TokenLaunchpadComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<TokenLaunchpadComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<TokenLaunchpadComposerResults<TReturns>>
}
export type TokenLaunchpadComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

