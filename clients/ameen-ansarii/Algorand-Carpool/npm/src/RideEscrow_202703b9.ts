/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"RideEscrow","structs":{},"methods":[{"name":"create_ride","args":[{"type":"uint64","name":"price","desc":"price per seat in microALGO (e.g., 1_000_000 = 1 ALGO)"},{"type":"uint64","name":"seats","desc":"available seats (1-6)"}],"returns":{"type":"uint64"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Driver creates a new ride listing.","events":[],"recommendations":{}},{"name":"join_ride","args":[{"type":"uint64","name":"ride_id"},{"type":"pay","name":"payment"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Rider joins a ride by paying the price into escrow.\nMust be a grouped transaction: payment + app call.","events":[],"recommendations":{}},{"name":"cancel_booking","args":[{"type":"uint64","name":"ride_id"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Rider cancels their booking. Gets refund minus 0.1 ALGO penalty.\nPenalty stays in contract (goes to driver as compensation).","events":[],"recommendations":{}},{"name":"complete_ride","args":[{"type":"uint64","name":"ride_id"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Driver marks ride as completed.\nEscrowed ALGO (price × passengers) is released to the driver.","events":[],"recommendations":{}},{"name":"cancel_ride","args":[{"type":"uint64","name":"ride_id"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Driver cancels ride. All passengers are refunded.\nDriver pays 0.1 ALGO penalty per passenger.","events":[],"recommendations":{}},{"name":"get_ride_count","args":[],"returns":{"type":"uint64"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"desc":"Returns total number of rides ever created.","events":[],"recommendations":{}},{"name":"get_total_completed","args":[],"returns":{"type":"uint64"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"desc":"Returns number of completed rides.","events":[],"recommendations":{}},{"name":"get_total_rides","args":[],"returns":{"type":"uint64"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"desc":"Returns total rides created on the platform.","events":[],"recommendations":{}},{"name":"get_platform_info","args":[],"returns":{"type":"string"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"desc":"Returns platform identification string.","events":[],"recommendations":{}}],"arcs":[22,28],"desc":"\n    Ride Escrow Smart Contract for decentralized ride-sharing.\n\n    State layout per ride (box key = \"r\" + ride_id as 8 bytes):\n      driver address (32 bytes) + price (8) + seats (8) +\n      seats_taken (8) + is_active (8) + is_completed (8) = 72 bytes\n\n    Passenger tracking (box key = \"p\" + ride_id(8) + index(8)):\n      rider address (32 bytes)\n    ","networks":{},"state":{"schema":{"global":{"ints":3,"bytes":0},"local":{"ints":0,"bytes":0}},"keys":{"global":{"ride_counter":{"keyType":"AVMString","valueType":"AVMUint64","key":"cmlkZV9jb3VudGVy"},"total_completed":{"keyType":"AVMString","valueType":"AVMUint64","key":"dG90YWxfY29tcGxldGVk"},"total_rides_created":{"keyType":"AVMString","valueType":"AVMUint64","key":"dG90YWxfcmlkZXNfY3JlYXRlZA=="}},"local":{},"box":{}},"maps":{"global":{},"local":{},"box":{}}},"bareActions":{"create":["NoOp"],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[546,872],"errorMessage":"Address length is 32 bytes"},{"pc":[727],"errorMessage":"Already completed"},{"pc":[392],"errorMessage":"Driver can't join own ride"},{"pc":[509],"errorMessage":"Driver cannot cancel booking"},{"pc":[255],"errorMessage":"Max 6 seats"},{"pc":[250],"errorMessage":"Min 1 seat"},{"pc":[729],"errorMessage":"No passengers"},{"pc":[562],"errorMessage":"Not a passenger of this ride"},{"pc":[725,837],"errorMessage":"Not active"},{"pc":[834],"errorMessage":"Only driver can cancel"},{"pc":[721],"errorMessage":"Only driver can complete"},{"pc":[400],"errorMessage":"Pay the contract"},{"pc":[258],"errorMessage":"Price must be > 0"},{"pc":[386],"errorMessage":"Ride is full"},{"pc":[381,505],"errorMessage":"Ride not active"},{"pc":[355,481,692,807],"errorMessage":"Ride not found"},{"pc":[408],"errorMessage":"Wrong payment amount"},{"pc":[262,930],"errorMessage":"check self.ride_counter exists"},{"pc":[777,942],"errorMessage":"check self.total_completed exists"},{"pc":[315,954],"errorMessage":"check self.total_rides_created exists"},{"pc":[238,247,335,468,684,794],"errorMessage":"invalid number of bytes for arc4.uint64"},{"pc":[346],"errorMessage":"transaction type is pay"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"events":[]} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


/**
 * The argument types for the RideEscrow contract
 */
export type RideEscrowArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'create_ride(uint64,uint64)uint64': {
      /**
       * price per seat in microALGO (e.g., 1_000_000 = 1 ALGO)
       */
      price: bigint | number
      /**
       * available seats (1-6)
       */
      seats: bigint | number
    }
    'join_ride(uint64,pay)void': {
      rideId: bigint | number
      payment: AppMethodCallTransactionArgument
    }
    'cancel_booking(uint64)void': {
      rideId: bigint | number
    }
    'complete_ride(uint64)void': {
      rideId: bigint | number
    }
    'cancel_ride(uint64)void': {
      rideId: bigint | number
    }
    'get_ride_count()uint64': Record<string, never>
    'get_total_completed()uint64': Record<string, never>
    'get_total_rides()uint64': Record<string, never>
    'get_platform_info()string': Record<string, never>
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'create_ride(uint64,uint64)uint64': [price: bigint | number, seats: bigint | number]
    'join_ride(uint64,pay)void': [rideId: bigint | number, payment: AppMethodCallTransactionArgument]
    'cancel_booking(uint64)void': [rideId: bigint | number]
    'complete_ride(uint64)void': [rideId: bigint | number]
    'cancel_ride(uint64)void': [rideId: bigint | number]
    'get_ride_count()uint64': []
    'get_total_completed()uint64': []
    'get_total_rides()uint64': []
    'get_platform_info()string': []
  }
}

/**
 * The return type for each method
 */
export type RideEscrowReturns = {
  'create_ride(uint64,uint64)uint64': bigint
  'join_ride(uint64,pay)void': void
  'cancel_booking(uint64)void': void
  'complete_ride(uint64)void': void
  'cancel_ride(uint64)void': void
  'get_ride_count()uint64': bigint
  'get_total_completed()uint64': bigint
  'get_total_rides()uint64': bigint
  'get_platform_info()string': string
}

/**
 * Defines the types of available calls and state of the RideEscrow smart contract.
 */
export type RideEscrowTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'create_ride(uint64,uint64)uint64' | 'create_ride', {
      argsObj: RideEscrowArgs['obj']['create_ride(uint64,uint64)uint64']
      argsTuple: RideEscrowArgs['tuple']['create_ride(uint64,uint64)uint64']
      returns: RideEscrowReturns['create_ride(uint64,uint64)uint64']
    }>
    & Record<'join_ride(uint64,pay)void' | 'join_ride', {
      argsObj: RideEscrowArgs['obj']['join_ride(uint64,pay)void']
      argsTuple: RideEscrowArgs['tuple']['join_ride(uint64,pay)void']
      returns: RideEscrowReturns['join_ride(uint64,pay)void']
    }>
    & Record<'cancel_booking(uint64)void' | 'cancel_booking', {
      argsObj: RideEscrowArgs['obj']['cancel_booking(uint64)void']
      argsTuple: RideEscrowArgs['tuple']['cancel_booking(uint64)void']
      returns: RideEscrowReturns['cancel_booking(uint64)void']
    }>
    & Record<'complete_ride(uint64)void' | 'complete_ride', {
      argsObj: RideEscrowArgs['obj']['complete_ride(uint64)void']
      argsTuple: RideEscrowArgs['tuple']['complete_ride(uint64)void']
      returns: RideEscrowReturns['complete_ride(uint64)void']
    }>
    & Record<'cancel_ride(uint64)void' | 'cancel_ride', {
      argsObj: RideEscrowArgs['obj']['cancel_ride(uint64)void']
      argsTuple: RideEscrowArgs['tuple']['cancel_ride(uint64)void']
      returns: RideEscrowReturns['cancel_ride(uint64)void']
    }>
    & Record<'get_ride_count()uint64' | 'get_ride_count', {
      argsObj: RideEscrowArgs['obj']['get_ride_count()uint64']
      argsTuple: RideEscrowArgs['tuple']['get_ride_count()uint64']
      returns: RideEscrowReturns['get_ride_count()uint64']
    }>
    & Record<'get_total_completed()uint64' | 'get_total_completed', {
      argsObj: RideEscrowArgs['obj']['get_total_completed()uint64']
      argsTuple: RideEscrowArgs['tuple']['get_total_completed()uint64']
      returns: RideEscrowReturns['get_total_completed()uint64']
    }>
    & Record<'get_total_rides()uint64' | 'get_total_rides', {
      argsObj: RideEscrowArgs['obj']['get_total_rides()uint64']
      argsTuple: RideEscrowArgs['tuple']['get_total_rides()uint64']
      returns: RideEscrowReturns['get_total_rides()uint64']
    }>
    & Record<'get_platform_info()string' | 'get_platform_info', {
      argsObj: RideEscrowArgs['obj']['get_platform_info()string']
      argsTuple: RideEscrowArgs['tuple']['get_platform_info()string']
      returns: RideEscrowReturns['get_platform_info()string']
    }>
  /**
   * Defines the shape of the state of the application.
   */
  state: {
    global: {
      keys: {
        rideCounter: bigint
        totalCompleted: bigint
        totalRidesCreated: bigint
      }
      maps: {}
    }
  }
}

/**
 * Defines the possible abi call signatures.
 */
export type RideEscrowSignatures = keyof RideEscrowTypes['methods']
/**
 * Defines the possible abi call signatures for methods that return a non-void value.
 */
export type RideEscrowNonVoidMethodSignatures = keyof RideEscrowTypes['methods'] extends infer T ? T extends keyof RideEscrowTypes['methods'] ? MethodReturn<T> extends void ? never : T  : never : never
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the RideEscrow smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends RideEscrowSignatures> = RideEscrowTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the RideEscrow smart contract to the method's return type
 */
export type MethodReturn<TSignature extends RideEscrowSignatures> = RideEscrowTypes['methods'][TSignature]['returns']

/**
 * Defines the shape of the keyed global state of the application.
 */
export type GlobalKeysState = RideEscrowTypes['state']['global']['keys']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the RideEscrow smart contract
 */
export abstract class RideEscrowParamsFactory {
  /**
   * Constructs a no op call for the create_ride(uint64,uint64)uint64 ABI method
   *
   * Driver creates a new ride listing.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static createRide(params: CallParams<RideEscrowArgs['obj']['create_ride(uint64,uint64)uint64'] | RideEscrowArgs['tuple']['create_ride(uint64,uint64)uint64']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'create_ride(uint64,uint64)uint64' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.price, params.args.seats],
    }
  }
  /**
   * Constructs a no op call for the join_ride(uint64,pay)void ABI method
   *
  * Rider joins a ride by paying the price into escrow.
  Must be a grouped transaction: payment + app call.

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static joinRide(params: CallParams<RideEscrowArgs['obj']['join_ride(uint64,pay)void'] | RideEscrowArgs['tuple']['join_ride(uint64,pay)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'join_ride(uint64,pay)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.rideId, params.args.payment],
    }
  }
  /**
   * Constructs a no op call for the cancel_booking(uint64)void ABI method
   *
  * Rider cancels their booking. Gets refund minus 0.1 ALGO penalty.
  Penalty stays in contract (goes to driver as compensation).

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static cancelBooking(params: CallParams<RideEscrowArgs['obj']['cancel_booking(uint64)void'] | RideEscrowArgs['tuple']['cancel_booking(uint64)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'cancel_booking(uint64)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.rideId],
    }
  }
  /**
   * Constructs a no op call for the complete_ride(uint64)void ABI method
   *
  * Driver marks ride as completed.
  Escrowed ALGO (price × passengers) is released to the driver.

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static completeRide(params: CallParams<RideEscrowArgs['obj']['complete_ride(uint64)void'] | RideEscrowArgs['tuple']['complete_ride(uint64)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'complete_ride(uint64)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.rideId],
    }
  }
  /**
   * Constructs a no op call for the cancel_ride(uint64)void ABI method
   *
  * Driver cancels ride. All passengers are refunded.
  Driver pays 0.1 ALGO penalty per passenger.

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static cancelRide(params: CallParams<RideEscrowArgs['obj']['cancel_ride(uint64)void'] | RideEscrowArgs['tuple']['cancel_ride(uint64)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'cancel_ride(uint64)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.rideId],
    }
  }
  /**
   * Constructs a no op call for the get_ride_count()uint64 ABI method
   *
   * Returns total number of rides ever created.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static getRideCount(params: CallParams<RideEscrowArgs['obj']['get_ride_count()uint64'] | RideEscrowArgs['tuple']['get_ride_count()uint64']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'get_ride_count()uint64' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the get_total_completed()uint64 ABI method
   *
   * Returns number of completed rides.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static getTotalCompleted(params: CallParams<RideEscrowArgs['obj']['get_total_completed()uint64'] | RideEscrowArgs['tuple']['get_total_completed()uint64']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'get_total_completed()uint64' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the get_total_rides()uint64 ABI method
   *
   * Returns total rides created on the platform.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static getTotalRides(params: CallParams<RideEscrowArgs['obj']['get_total_rides()uint64'] | RideEscrowArgs['tuple']['get_total_rides()uint64']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'get_total_rides()uint64' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the get_platform_info()string ABI method
   *
   * Returns platform identification string.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static getPlatformInfo(params: CallParams<RideEscrowArgs['obj']['get_platform_info()string'] | RideEscrowArgs['tuple']['get_platform_info()string']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'get_platform_info()string' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
}

/**
 * A client to make calls to the RideEscrow smart contract
 */
export class RideEscrowClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `RideEscrowClient`
   *
   * @param appClient An `AppClient` instance which has been created with the RideEscrow app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `RideEscrowClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Checks for decode errors on the given return value and maps the return value to the return type for the given method
   * @returns The typed return value or undefined if there was no value
   */
  decodeReturnValue<TSignature extends RideEscrowNonVoidMethodSignatures>(method: TSignature, returnValue: ABIReturn | undefined) {
    return returnValue !== undefined ? getArc56ReturnValue<MethodReturn<TSignature>>(returnValue, this.appClient.getABIMethod(method), APP_SPEC.structs) : undefined
  }

  /**
   * Returns a new `RideEscrowClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<RideEscrowClient> {
    return new RideEscrowClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `RideEscrowClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<RideEscrowClient> {
    return new RideEscrowClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Makes a clear_state call to an existing instance of the RideEscrow smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the RideEscrow smart contract using the `create_ride(uint64,uint64)uint64` ABI method.
     *
     * Driver creates a new ride listing.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    createRide: (params: CallParams<RideEscrowArgs['obj']['create_ride(uint64,uint64)uint64'] | RideEscrowArgs['tuple']['create_ride(uint64,uint64)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(RideEscrowParamsFactory.createRide(params))
    },

    /**
     * Makes a call to the RideEscrow smart contract using the `join_ride(uint64,pay)void` ABI method.
     *
    * Rider joins a ride by paying the price into escrow.
    Must be a grouped transaction: payment + app call.

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    joinRide: (params: CallParams<RideEscrowArgs['obj']['join_ride(uint64,pay)void'] | RideEscrowArgs['tuple']['join_ride(uint64,pay)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(RideEscrowParamsFactory.joinRide(params))
    },

    /**
     * Makes a call to the RideEscrow smart contract using the `cancel_booking(uint64)void` ABI method.
     *
    * Rider cancels their booking. Gets refund minus 0.1 ALGO penalty.
    Penalty stays in contract (goes to driver as compensation).

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    cancelBooking: (params: CallParams<RideEscrowArgs['obj']['cancel_booking(uint64)void'] | RideEscrowArgs['tuple']['cancel_booking(uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(RideEscrowParamsFactory.cancelBooking(params))
    },

    /**
     * Makes a call to the RideEscrow smart contract using the `complete_ride(uint64)void` ABI method.
     *
    * Driver marks ride as completed.
    Escrowed ALGO (price × passengers) is released to the driver.

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    completeRide: (params: CallParams<RideEscrowArgs['obj']['complete_ride(uint64)void'] | RideEscrowArgs['tuple']['complete_ride(uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(RideEscrowParamsFactory.completeRide(params))
    },

    /**
     * Makes a call to the RideEscrow smart contract using the `cancel_ride(uint64)void` ABI method.
     *
    * Driver cancels ride. All passengers are refunded.
    Driver pays 0.1 ALGO penalty per passenger.

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    cancelRide: (params: CallParams<RideEscrowArgs['obj']['cancel_ride(uint64)void'] | RideEscrowArgs['tuple']['cancel_ride(uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(RideEscrowParamsFactory.cancelRide(params))
    },

    /**
     * Makes a call to the RideEscrow smart contract using the `get_ride_count()uint64` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * Returns total number of rides ever created.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    getRideCount: (params: CallParams<RideEscrowArgs['obj']['get_ride_count()uint64'] | RideEscrowArgs['tuple']['get_ride_count()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(RideEscrowParamsFactory.getRideCount(params))
    },

    /**
     * Makes a call to the RideEscrow smart contract using the `get_total_completed()uint64` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * Returns number of completed rides.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    getTotalCompleted: (params: CallParams<RideEscrowArgs['obj']['get_total_completed()uint64'] | RideEscrowArgs['tuple']['get_total_completed()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(RideEscrowParamsFactory.getTotalCompleted(params))
    },

    /**
     * Makes a call to the RideEscrow smart contract using the `get_total_rides()uint64` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * Returns total rides created on the platform.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    getTotalRides: (params: CallParams<RideEscrowArgs['obj']['get_total_rides()uint64'] | RideEscrowArgs['tuple']['get_total_rides()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(RideEscrowParamsFactory.getTotalRides(params))
    },

    /**
     * Makes a call to the RideEscrow smart contract using the `get_platform_info()string` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * Returns platform identification string.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    getPlatformInfo: (params: CallParams<RideEscrowArgs['obj']['get_platform_info()string'] | RideEscrowArgs['tuple']['get_platform_info()string']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(RideEscrowParamsFactory.getPlatformInfo(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Makes a clear_state call to an existing instance of the RideEscrow smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the RideEscrow smart contract using the `create_ride(uint64,uint64)uint64` ABI method.
     *
     * Driver creates a new ride listing.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    createRide: (params: CallParams<RideEscrowArgs['obj']['create_ride(uint64,uint64)uint64'] | RideEscrowArgs['tuple']['create_ride(uint64,uint64)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(RideEscrowParamsFactory.createRide(params))
    },

    /**
     * Makes a call to the RideEscrow smart contract using the `join_ride(uint64,pay)void` ABI method.
     *
    * Rider joins a ride by paying the price into escrow.
    Must be a grouped transaction: payment + app call.

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    joinRide: (params: CallParams<RideEscrowArgs['obj']['join_ride(uint64,pay)void'] | RideEscrowArgs['tuple']['join_ride(uint64,pay)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(RideEscrowParamsFactory.joinRide(params))
    },

    /**
     * Makes a call to the RideEscrow smart contract using the `cancel_booking(uint64)void` ABI method.
     *
    * Rider cancels their booking. Gets refund minus 0.1 ALGO penalty.
    Penalty stays in contract (goes to driver as compensation).

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    cancelBooking: (params: CallParams<RideEscrowArgs['obj']['cancel_booking(uint64)void'] | RideEscrowArgs['tuple']['cancel_booking(uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(RideEscrowParamsFactory.cancelBooking(params))
    },

    /**
     * Makes a call to the RideEscrow smart contract using the `complete_ride(uint64)void` ABI method.
     *
    * Driver marks ride as completed.
    Escrowed ALGO (price × passengers) is released to the driver.

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    completeRide: (params: CallParams<RideEscrowArgs['obj']['complete_ride(uint64)void'] | RideEscrowArgs['tuple']['complete_ride(uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(RideEscrowParamsFactory.completeRide(params))
    },

    /**
     * Makes a call to the RideEscrow smart contract using the `cancel_ride(uint64)void` ABI method.
     *
    * Driver cancels ride. All passengers are refunded.
    Driver pays 0.1 ALGO penalty per passenger.

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    cancelRide: (params: CallParams<RideEscrowArgs['obj']['cancel_ride(uint64)void'] | RideEscrowArgs['tuple']['cancel_ride(uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(RideEscrowParamsFactory.cancelRide(params))
    },

    /**
     * Makes a call to the RideEscrow smart contract using the `get_ride_count()uint64` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * Returns total number of rides ever created.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    getRideCount: (params: CallParams<RideEscrowArgs['obj']['get_ride_count()uint64'] | RideEscrowArgs['tuple']['get_ride_count()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(RideEscrowParamsFactory.getRideCount(params))
    },

    /**
     * Makes a call to the RideEscrow smart contract using the `get_total_completed()uint64` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * Returns number of completed rides.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    getTotalCompleted: (params: CallParams<RideEscrowArgs['obj']['get_total_completed()uint64'] | RideEscrowArgs['tuple']['get_total_completed()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(RideEscrowParamsFactory.getTotalCompleted(params))
    },

    /**
     * Makes a call to the RideEscrow smart contract using the `get_total_rides()uint64` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * Returns total rides created on the platform.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    getTotalRides: (params: CallParams<RideEscrowArgs['obj']['get_total_rides()uint64'] | RideEscrowArgs['tuple']['get_total_rides()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(RideEscrowParamsFactory.getTotalRides(params))
    },

    /**
     * Makes a call to the RideEscrow smart contract using the `get_platform_info()string` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * Returns platform identification string.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    getPlatformInfo: (params: CallParams<RideEscrowArgs['obj']['get_platform_info()string'] | RideEscrowArgs['tuple']['get_platform_info()string']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(RideEscrowParamsFactory.getPlatformInfo(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Makes a clear_state call to an existing instance of the RideEscrow smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the RideEscrow smart contract using the `create_ride(uint64,uint64)uint64` ABI method.
     *
     * Driver creates a new ride listing.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    createRide: async (params: CallParams<RideEscrowArgs['obj']['create_ride(uint64,uint64)uint64'] | RideEscrowArgs['tuple']['create_ride(uint64,uint64)uint64']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(RideEscrowParamsFactory.createRide(params))
      return {...result, return: result.return as unknown as (undefined | RideEscrowReturns['create_ride(uint64,uint64)uint64'])}
    },

    /**
     * Makes a call to the RideEscrow smart contract using the `join_ride(uint64,pay)void` ABI method.
     *
    * Rider joins a ride by paying the price into escrow.
    Must be a grouped transaction: payment + app call.

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    joinRide: async (params: CallParams<RideEscrowArgs['obj']['join_ride(uint64,pay)void'] | RideEscrowArgs['tuple']['join_ride(uint64,pay)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(RideEscrowParamsFactory.joinRide(params))
      return {...result, return: result.return as unknown as (undefined | RideEscrowReturns['join_ride(uint64,pay)void'])}
    },

    /**
     * Makes a call to the RideEscrow smart contract using the `cancel_booking(uint64)void` ABI method.
     *
    * Rider cancels their booking. Gets refund minus 0.1 ALGO penalty.
    Penalty stays in contract (goes to driver as compensation).

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    cancelBooking: async (params: CallParams<RideEscrowArgs['obj']['cancel_booking(uint64)void'] | RideEscrowArgs['tuple']['cancel_booking(uint64)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(RideEscrowParamsFactory.cancelBooking(params))
      return {...result, return: result.return as unknown as (undefined | RideEscrowReturns['cancel_booking(uint64)void'])}
    },

    /**
     * Makes a call to the RideEscrow smart contract using the `complete_ride(uint64)void` ABI method.
     *
    * Driver marks ride as completed.
    Escrowed ALGO (price × passengers) is released to the driver.

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    completeRide: async (params: CallParams<RideEscrowArgs['obj']['complete_ride(uint64)void'] | RideEscrowArgs['tuple']['complete_ride(uint64)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(RideEscrowParamsFactory.completeRide(params))
      return {...result, return: result.return as unknown as (undefined | RideEscrowReturns['complete_ride(uint64)void'])}
    },

    /**
     * Makes a call to the RideEscrow smart contract using the `cancel_ride(uint64)void` ABI method.
     *
    * Driver cancels ride. All passengers are refunded.
    Driver pays 0.1 ALGO penalty per passenger.

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    cancelRide: async (params: CallParams<RideEscrowArgs['obj']['cancel_ride(uint64)void'] | RideEscrowArgs['tuple']['cancel_ride(uint64)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(RideEscrowParamsFactory.cancelRide(params))
      return {...result, return: result.return as unknown as (undefined | RideEscrowReturns['cancel_ride(uint64)void'])}
    },

    /**
     * Makes a call to the RideEscrow smart contract using the `get_ride_count()uint64` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * Returns total number of rides ever created.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    getRideCount: async (params: CallParams<RideEscrowArgs['obj']['get_ride_count()uint64'] | RideEscrowArgs['tuple']['get_ride_count()uint64']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(RideEscrowParamsFactory.getRideCount(params))
      return {...result, return: result.return as unknown as (undefined | RideEscrowReturns['get_ride_count()uint64'])}
    },

    /**
     * Makes a call to the RideEscrow smart contract using the `get_total_completed()uint64` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * Returns number of completed rides.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    getTotalCompleted: async (params: CallParams<RideEscrowArgs['obj']['get_total_completed()uint64'] | RideEscrowArgs['tuple']['get_total_completed()uint64']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(RideEscrowParamsFactory.getTotalCompleted(params))
      return {...result, return: result.return as unknown as (undefined | RideEscrowReturns['get_total_completed()uint64'])}
    },

    /**
     * Makes a call to the RideEscrow smart contract using the `get_total_rides()uint64` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * Returns total rides created on the platform.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    getTotalRides: async (params: CallParams<RideEscrowArgs['obj']['get_total_rides()uint64'] | RideEscrowArgs['tuple']['get_total_rides()uint64']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(RideEscrowParamsFactory.getTotalRides(params))
      return {...result, return: result.return as unknown as (undefined | RideEscrowReturns['get_total_rides()uint64'])}
    },

    /**
     * Makes a call to the RideEscrow smart contract using the `get_platform_info()string` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * Returns platform identification string.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    getPlatformInfo: async (params: CallParams<RideEscrowArgs['obj']['get_platform_info()string'] | RideEscrowArgs['tuple']['get_platform_info()string']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(RideEscrowParamsFactory.getPlatformInfo(params))
      return {...result, return: result.return as unknown as (undefined | RideEscrowReturns['get_platform_info()string'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new RideEscrowClient(this.appClient.clone(params))
  }

  /**
   * Makes a readonly (simulated) call to the RideEscrow smart contract using the `get_ride_count()uint64` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * Returns total number of rides ever created.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async getRideCount(params: CallParams<RideEscrowArgs['obj']['get_ride_count()uint64'] | RideEscrowArgs['tuple']['get_ride_count()uint64']> = {args: []}) {
    const result = await this.appClient.send.call(RideEscrowParamsFactory.getRideCount(params))
    return result.return as unknown as RideEscrowReturns['get_ride_count()uint64']
  }

  /**
   * Makes a readonly (simulated) call to the RideEscrow smart contract using the `get_total_completed()uint64` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * Returns number of completed rides.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async getTotalCompleted(params: CallParams<RideEscrowArgs['obj']['get_total_completed()uint64'] | RideEscrowArgs['tuple']['get_total_completed()uint64']> = {args: []}) {
    const result = await this.appClient.send.call(RideEscrowParamsFactory.getTotalCompleted(params))
    return result.return as unknown as RideEscrowReturns['get_total_completed()uint64']
  }

  /**
   * Makes a readonly (simulated) call to the RideEscrow smart contract using the `get_total_rides()uint64` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * Returns total rides created on the platform.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async getTotalRides(params: CallParams<RideEscrowArgs['obj']['get_total_rides()uint64'] | RideEscrowArgs['tuple']['get_total_rides()uint64']> = {args: []}) {
    const result = await this.appClient.send.call(RideEscrowParamsFactory.getTotalRides(params))
    return result.return as unknown as RideEscrowReturns['get_total_rides()uint64']
  }

  /**
   * Makes a readonly (simulated) call to the RideEscrow smart contract using the `get_platform_info()string` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * Returns platform identification string.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async getPlatformInfo(params: CallParams<RideEscrowArgs['obj']['get_platform_info()string'] | RideEscrowArgs['tuple']['get_platform_info()string']> = {args: []}) {
    const result = await this.appClient.send.call(RideEscrowParamsFactory.getPlatformInfo(params))
    return result.return as unknown as RideEscrowReturns['get_platform_info()string']
  }

  /**
   * Methods to access state for the current RideEscrow app
   */
  state = {
    /**
     * Methods to access global state for the current RideEscrow app
     */
    global: {
      /**
       * Get all current keyed values from global state
       */
      getAll: async (): Promise<Partial<Expand<GlobalKeysState>>> => {
        const result = await this.appClient.state.global.getAll()
        return {
          rideCounter: result.ride_counter,
          totalCompleted: result.total_completed,
          totalRidesCreated: result.total_rides_created,
        }
      },
      /**
       * Get the current value of the ride_counter key in global state
       */
      rideCounter: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("ride_counter")) as bigint | undefined },
      /**
       * Get the current value of the total_completed key in global state
       */
      totalCompleted: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("total_completed")) as bigint | undefined },
      /**
       * Get the current value of the total_rides_created key in global state
       */
      totalRidesCreated: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("total_rides_created")) as bigint | undefined },
    },
  }

  public newGroup(): RideEscrowComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a create_ride(uint64,uint64)uint64 method call against the RideEscrow contract
       */
      createRide(params: CallParams<RideEscrowArgs['obj']['create_ride(uint64,uint64)uint64'] | RideEscrowArgs['tuple']['create_ride(uint64,uint64)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.createRide(params)))
        resultMappers.push((v) => client.decodeReturnValue('create_ride(uint64,uint64)uint64', v))
        return this
      },
      /**
       * Add a join_ride(uint64,pay)void method call against the RideEscrow contract
       */
      joinRide(params: CallParams<RideEscrowArgs['obj']['join_ride(uint64,pay)void'] | RideEscrowArgs['tuple']['join_ride(uint64,pay)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.joinRide(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a cancel_booking(uint64)void method call against the RideEscrow contract
       */
      cancelBooking(params: CallParams<RideEscrowArgs['obj']['cancel_booking(uint64)void'] | RideEscrowArgs['tuple']['cancel_booking(uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.cancelBooking(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a complete_ride(uint64)void method call against the RideEscrow contract
       */
      completeRide(params: CallParams<RideEscrowArgs['obj']['complete_ride(uint64)void'] | RideEscrowArgs['tuple']['complete_ride(uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.completeRide(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a cancel_ride(uint64)void method call against the RideEscrow contract
       */
      cancelRide(params: CallParams<RideEscrowArgs['obj']['cancel_ride(uint64)void'] | RideEscrowArgs['tuple']['cancel_ride(uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.cancelRide(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a get_ride_count()uint64 method call against the RideEscrow contract
       */
      getRideCount(params: CallParams<RideEscrowArgs['obj']['get_ride_count()uint64'] | RideEscrowArgs['tuple']['get_ride_count()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.getRideCount(params)))
        resultMappers.push((v) => client.decodeReturnValue('get_ride_count()uint64', v))
        return this
      },
      /**
       * Add a get_total_completed()uint64 method call against the RideEscrow contract
       */
      getTotalCompleted(params: CallParams<RideEscrowArgs['obj']['get_total_completed()uint64'] | RideEscrowArgs['tuple']['get_total_completed()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.getTotalCompleted(params)))
        resultMappers.push((v) => client.decodeReturnValue('get_total_completed()uint64', v))
        return this
      },
      /**
       * Add a get_total_rides()uint64 method call against the RideEscrow contract
       */
      getTotalRides(params: CallParams<RideEscrowArgs['obj']['get_total_rides()uint64'] | RideEscrowArgs['tuple']['get_total_rides()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.getTotalRides(params)))
        resultMappers.push((v) => client.decodeReturnValue('get_total_rides()uint64', v))
        return this
      },
      /**
       * Add a get_platform_info()string method call against the RideEscrow contract
       */
      getPlatformInfo(params: CallParams<RideEscrowArgs['obj']['get_platform_info()string'] | RideEscrowArgs['tuple']['get_platform_info()string']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.getPlatformInfo(params)))
        resultMappers.push((v) => client.decodeReturnValue('get_platform_info()string', v))
        return this
      },
      /**
       * Add a clear state call to the RideEscrow contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as RideEscrowComposer
  }
}
export type RideEscrowComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the create_ride(uint64,uint64)uint64 ABI method.
   *
   * Driver creates a new ride listing.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  createRide(params?: CallParams<RideEscrowArgs['obj']['create_ride(uint64,uint64)uint64'] | RideEscrowArgs['tuple']['create_ride(uint64,uint64)uint64']>): RideEscrowComposer<[...TReturns, RideEscrowReturns['create_ride(uint64,uint64)uint64'] | undefined]>

  /**
   * Calls the join_ride(uint64,pay)void ABI method.
   *
  * Rider joins a ride by paying the price into escrow.
  Must be a grouped transaction: payment + app call.

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  joinRide(params?: CallParams<RideEscrowArgs['obj']['join_ride(uint64,pay)void'] | RideEscrowArgs['tuple']['join_ride(uint64,pay)void']>): RideEscrowComposer<[...TReturns, RideEscrowReturns['join_ride(uint64,pay)void'] | undefined]>

  /**
   * Calls the cancel_booking(uint64)void ABI method.
   *
  * Rider cancels their booking. Gets refund minus 0.1 ALGO penalty.
  Penalty stays in contract (goes to driver as compensation).

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  cancelBooking(params?: CallParams<RideEscrowArgs['obj']['cancel_booking(uint64)void'] | RideEscrowArgs['tuple']['cancel_booking(uint64)void']>): RideEscrowComposer<[...TReturns, RideEscrowReturns['cancel_booking(uint64)void'] | undefined]>

  /**
   * Calls the complete_ride(uint64)void ABI method.
   *
  * Driver marks ride as completed.
  Escrowed ALGO (price × passengers) is released to the driver.

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  completeRide(params?: CallParams<RideEscrowArgs['obj']['complete_ride(uint64)void'] | RideEscrowArgs['tuple']['complete_ride(uint64)void']>): RideEscrowComposer<[...TReturns, RideEscrowReturns['complete_ride(uint64)void'] | undefined]>

  /**
   * Calls the cancel_ride(uint64)void ABI method.
   *
  * Driver cancels ride. All passengers are refunded.
  Driver pays 0.1 ALGO penalty per passenger.

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  cancelRide(params?: CallParams<RideEscrowArgs['obj']['cancel_ride(uint64)void'] | RideEscrowArgs['tuple']['cancel_ride(uint64)void']>): RideEscrowComposer<[...TReturns, RideEscrowReturns['cancel_ride(uint64)void'] | undefined]>

  /**
   * Calls the get_ride_count()uint64 ABI method.
   *
   * Returns total number of rides ever created.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  getRideCount(params?: CallParams<RideEscrowArgs['obj']['get_ride_count()uint64'] | RideEscrowArgs['tuple']['get_ride_count()uint64']>): RideEscrowComposer<[...TReturns, RideEscrowReturns['get_ride_count()uint64'] | undefined]>

  /**
   * Calls the get_total_completed()uint64 ABI method.
   *
   * Returns number of completed rides.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  getTotalCompleted(params?: CallParams<RideEscrowArgs['obj']['get_total_completed()uint64'] | RideEscrowArgs['tuple']['get_total_completed()uint64']>): RideEscrowComposer<[...TReturns, RideEscrowReturns['get_total_completed()uint64'] | undefined]>

  /**
   * Calls the get_total_rides()uint64 ABI method.
   *
   * Returns total rides created on the platform.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  getTotalRides(params?: CallParams<RideEscrowArgs['obj']['get_total_rides()uint64'] | RideEscrowArgs['tuple']['get_total_rides()uint64']>): RideEscrowComposer<[...TReturns, RideEscrowReturns['get_total_rides()uint64'] | undefined]>

  /**
   * Calls the get_platform_info()string ABI method.
   *
   * Returns platform identification string.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  getPlatformInfo(params?: CallParams<RideEscrowArgs['obj']['get_platform_info()string'] | RideEscrowArgs['tuple']['get_platform_info()string']>): RideEscrowComposer<[...TReturns, RideEscrowReturns['get_platform_info()string'] | undefined]>

  /**
   * Makes a clear_state call to an existing instance of the RideEscrow smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): RideEscrowComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): RideEscrowComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<RideEscrowComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<RideEscrowComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<RideEscrowComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<RideEscrowComposerResults<TReturns>>
}
export type RideEscrowComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

