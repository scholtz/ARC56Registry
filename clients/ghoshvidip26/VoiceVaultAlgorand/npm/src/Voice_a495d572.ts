/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"arcs":[],"name":"Voice","structs":{},"methods":[{"name":"registerVoice","args":[{"name":"name","type":"string"},{"name":"modelUri","type":"string"},{"name":"rights","type":"string"},{"name":"pricePerUse","type":"uint64"}],"returns":{"type":"void"},"events":[],"actions":{"create":[],"call":["NoOp"]}},{"name":"getVoiceId","args":[{"name":"owner","type":"address"}],"returns":{"type":"uint64"},"events":[],"readonly":true,"actions":{"create":[],"call":["NoOp"]}},{"name":"voice_exists","args":[{"name":"owner","type":"address"}],"returns":{"type":"bool"},"events":[],"readonly":true,"actions":{"create":[],"call":["NoOp"]}},{"name":"unregister_voice","args":[],"returns":{"type":"void"},"events":[],"actions":{"create":[],"call":["NoOp"]}}],"state":{"schema":{"global":{"ints":0,"bytes":0},"local":{"ints":0,"bytes":0}},"keys":{"global":{},"local":{},"box":{}},"maps":{"global":{},"local":{},"box":{}}},"bareActions":{"create":["NoOp"],"call":[]}} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


/**
 * The argument types for the Voice contract
 */
export type VoiceArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'registerVoice(string,string,string,uint64)void': {
      name: string
      modelUri: string
      rights: string
      pricePerUse: bigint | number
    }
    'getVoiceId(address)uint64': {
      owner: string
    }
    'voice_exists(address)bool': {
      owner: string
    }
    'unregister_voice()void': Record<string, never>
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'registerVoice(string,string,string,uint64)void': [name: string, modelUri: string, rights: string, pricePerUse: bigint | number]
    'getVoiceId(address)uint64': [owner: string]
    'voice_exists(address)bool': [owner: string]
    'unregister_voice()void': []
  }
}

/**
 * The return type for each method
 */
export type VoiceReturns = {
  'registerVoice(string,string,string,uint64)void': void
  'getVoiceId(address)uint64': bigint
  'voice_exists(address)bool': boolean
  'unregister_voice()void': void
}

/**
 * Defines the types of available calls and state of the Voice smart contract.
 */
export type VoiceTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'registerVoice(string,string,string,uint64)void' | 'registerVoice', {
      argsObj: VoiceArgs['obj']['registerVoice(string,string,string,uint64)void']
      argsTuple: VoiceArgs['tuple']['registerVoice(string,string,string,uint64)void']
      returns: VoiceReturns['registerVoice(string,string,string,uint64)void']
    }>
    & Record<'getVoiceId(address)uint64' | 'getVoiceId', {
      argsObj: VoiceArgs['obj']['getVoiceId(address)uint64']
      argsTuple: VoiceArgs['tuple']['getVoiceId(address)uint64']
      returns: VoiceReturns['getVoiceId(address)uint64']
    }>
    & Record<'voice_exists(address)bool' | 'voice_exists', {
      argsObj: VoiceArgs['obj']['voice_exists(address)bool']
      argsTuple: VoiceArgs['tuple']['voice_exists(address)bool']
      returns: VoiceReturns['voice_exists(address)bool']
    }>
    & Record<'unregister_voice()void' | 'unregister_voice', {
      argsObj: VoiceArgs['obj']['unregister_voice()void']
      argsTuple: VoiceArgs['tuple']['unregister_voice()void']
      returns: VoiceReturns['unregister_voice()void']
    }>
}

/**
 * Defines the possible abi call signatures.
 */
export type VoiceSignatures = keyof VoiceTypes['methods']
/**
 * Defines the possible abi call signatures for methods that return a non-void value.
 */
export type VoiceNonVoidMethodSignatures = keyof VoiceTypes['methods'] extends infer T ? T extends keyof VoiceTypes['methods'] ? MethodReturn<T> extends void ? never : T  : never : never
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the Voice smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends VoiceSignatures> = VoiceTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the Voice smart contract to the method's return type
 */
export type MethodReturn<TSignature extends VoiceSignatures> = VoiceTypes['methods'][TSignature]['returns']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the Voice smart contract
 */
export abstract class VoiceParamsFactory {
  /**
   * Constructs a no op call for the registerVoice(string,string,string,uint64)void ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static registerVoice(params: CallParams<VoiceArgs['obj']['registerVoice(string,string,string,uint64)void'] | VoiceArgs['tuple']['registerVoice(string,string,string,uint64)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'registerVoice(string,string,string,uint64)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.name, params.args.modelUri, params.args.rights, params.args.pricePerUse],
    }
  }
  /**
   * Constructs a no op call for the getVoiceId(address)uint64 ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static getVoiceId(params: CallParams<VoiceArgs['obj']['getVoiceId(address)uint64'] | VoiceArgs['tuple']['getVoiceId(address)uint64']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'getVoiceId(address)uint64' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.owner],
    }
  }
  /**
   * Constructs a no op call for the voice_exists(address)bool ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static voiceExists(params: CallParams<VoiceArgs['obj']['voice_exists(address)bool'] | VoiceArgs['tuple']['voice_exists(address)bool']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'voice_exists(address)bool' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.owner],
    }
  }
  /**
   * Constructs a no op call for the unregister_voice()void ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static unregisterVoice(params: CallParams<VoiceArgs['obj']['unregister_voice()void'] | VoiceArgs['tuple']['unregister_voice()void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'unregister_voice()void' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
}

/**
 * A client to make calls to the Voice smart contract
 */
export class VoiceClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `VoiceClient`
   *
   * @param appClient An `AppClient` instance which has been created with the Voice app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `VoiceClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Checks for decode errors on the given return value and maps the return value to the return type for the given method
   * @returns The typed return value or undefined if there was no value
   */
  decodeReturnValue<TSignature extends VoiceNonVoidMethodSignatures>(method: TSignature, returnValue: ABIReturn | undefined) {
    return returnValue !== undefined ? getArc56ReturnValue<MethodReturn<TSignature>>(returnValue, this.appClient.getABIMethod(method), APP_SPEC.structs) : undefined
  }

  /**
   * Returns a new `VoiceClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<VoiceClient> {
    return new VoiceClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `VoiceClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<VoiceClient> {
    return new VoiceClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Makes a clear_state call to an existing instance of the Voice smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the Voice smart contract using the `registerVoice(string,string,string,uint64)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    registerVoice: (params: CallParams<VoiceArgs['obj']['registerVoice(string,string,string,uint64)void'] | VoiceArgs['tuple']['registerVoice(string,string,string,uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(VoiceParamsFactory.registerVoice(params))
    },

    /**
     * Makes a call to the Voice smart contract using the `getVoiceId(address)uint64` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    getVoiceId: (params: CallParams<VoiceArgs['obj']['getVoiceId(address)uint64'] | VoiceArgs['tuple']['getVoiceId(address)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(VoiceParamsFactory.getVoiceId(params))
    },

    /**
     * Makes a call to the Voice smart contract using the `voice_exists(address)bool` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    voiceExists: (params: CallParams<VoiceArgs['obj']['voice_exists(address)bool'] | VoiceArgs['tuple']['voice_exists(address)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(VoiceParamsFactory.voiceExists(params))
    },

    /**
     * Makes a call to the Voice smart contract using the `unregister_voice()void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    unregisterVoice: (params: CallParams<VoiceArgs['obj']['unregister_voice()void'] | VoiceArgs['tuple']['unregister_voice()void']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(VoiceParamsFactory.unregisterVoice(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Makes a clear_state call to an existing instance of the Voice smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the Voice smart contract using the `registerVoice(string,string,string,uint64)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    registerVoice: (params: CallParams<VoiceArgs['obj']['registerVoice(string,string,string,uint64)void'] | VoiceArgs['tuple']['registerVoice(string,string,string,uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(VoiceParamsFactory.registerVoice(params))
    },

    /**
     * Makes a call to the Voice smart contract using the `getVoiceId(address)uint64` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    getVoiceId: (params: CallParams<VoiceArgs['obj']['getVoiceId(address)uint64'] | VoiceArgs['tuple']['getVoiceId(address)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(VoiceParamsFactory.getVoiceId(params))
    },

    /**
     * Makes a call to the Voice smart contract using the `voice_exists(address)bool` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    voiceExists: (params: CallParams<VoiceArgs['obj']['voice_exists(address)bool'] | VoiceArgs['tuple']['voice_exists(address)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(VoiceParamsFactory.voiceExists(params))
    },

    /**
     * Makes a call to the Voice smart contract using the `unregister_voice()void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    unregisterVoice: (params: CallParams<VoiceArgs['obj']['unregister_voice()void'] | VoiceArgs['tuple']['unregister_voice()void']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(VoiceParamsFactory.unregisterVoice(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Makes a clear_state call to an existing instance of the Voice smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the Voice smart contract using the `registerVoice(string,string,string,uint64)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    registerVoice: async (params: CallParams<VoiceArgs['obj']['registerVoice(string,string,string,uint64)void'] | VoiceArgs['tuple']['registerVoice(string,string,string,uint64)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(VoiceParamsFactory.registerVoice(params))
      return {...result, return: result.return as unknown as (undefined | VoiceReturns['registerVoice(string,string,string,uint64)void'])}
    },

    /**
     * Makes a call to the Voice smart contract using the `getVoiceId(address)uint64` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    getVoiceId: async (params: CallParams<VoiceArgs['obj']['getVoiceId(address)uint64'] | VoiceArgs['tuple']['getVoiceId(address)uint64']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(VoiceParamsFactory.getVoiceId(params))
      return {...result, return: result.return as unknown as (undefined | VoiceReturns['getVoiceId(address)uint64'])}
    },

    /**
     * Makes a call to the Voice smart contract using the `voice_exists(address)bool` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    voiceExists: async (params: CallParams<VoiceArgs['obj']['voice_exists(address)bool'] | VoiceArgs['tuple']['voice_exists(address)bool']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(VoiceParamsFactory.voiceExists(params))
      return {...result, return: result.return as unknown as (undefined | VoiceReturns['voice_exists(address)bool'])}
    },

    /**
     * Makes a call to the Voice smart contract using the `unregister_voice()void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    unregisterVoice: async (params: CallParams<VoiceArgs['obj']['unregister_voice()void'] | VoiceArgs['tuple']['unregister_voice()void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(VoiceParamsFactory.unregisterVoice(params))
      return {...result, return: result.return as unknown as (undefined | VoiceReturns['unregister_voice()void'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new VoiceClient(this.appClient.clone(params))
  }

  /**
   * Makes a readonly (simulated) call to the Voice smart contract using the `getVoiceId(address)uint64` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async getVoiceId(params: CallParams<VoiceArgs['obj']['getVoiceId(address)uint64'] | VoiceArgs['tuple']['getVoiceId(address)uint64']>) {
    const result = await this.appClient.send.call(VoiceParamsFactory.getVoiceId(params))
    return result.return as unknown as VoiceReturns['getVoiceId(address)uint64']
  }

  /**
   * Makes a readonly (simulated) call to the Voice smart contract using the `voice_exists(address)bool` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async voiceExists(params: CallParams<VoiceArgs['obj']['voice_exists(address)bool'] | VoiceArgs['tuple']['voice_exists(address)bool']>) {
    const result = await this.appClient.send.call(VoiceParamsFactory.voiceExists(params))
    return result.return as unknown as VoiceReturns['voice_exists(address)bool']
  }

  /**
   * Methods to access state for the current Voice app
   */
  state = {
  }

  public newGroup(): VoiceComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a registerVoice(string,string,string,uint64)void method call against the Voice contract
       */
      registerVoice(params: CallParams<VoiceArgs['obj']['registerVoice(string,string,string,uint64)void'] | VoiceArgs['tuple']['registerVoice(string,string,string,uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.registerVoice(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a getVoiceId(address)uint64 method call against the Voice contract
       */
      getVoiceId(params: CallParams<VoiceArgs['obj']['getVoiceId(address)uint64'] | VoiceArgs['tuple']['getVoiceId(address)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.getVoiceId(params)))
        resultMappers.push((v) => client.decodeReturnValue('getVoiceId(address)uint64', v))
        return this
      },
      /**
       * Add a voice_exists(address)bool method call against the Voice contract
       */
      voiceExists(params: CallParams<VoiceArgs['obj']['voice_exists(address)bool'] | VoiceArgs['tuple']['voice_exists(address)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.voiceExists(params)))
        resultMappers.push((v) => client.decodeReturnValue('voice_exists(address)bool', v))
        return this
      },
      /**
       * Add a unregister_voice()void method call against the Voice contract
       */
      unregisterVoice(params: CallParams<VoiceArgs['obj']['unregister_voice()void'] | VoiceArgs['tuple']['unregister_voice()void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.unregisterVoice(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a clear state call to the Voice contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as VoiceComposer
  }
}
export type VoiceComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the registerVoice(string,string,string,uint64)void ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  registerVoice(params?: CallParams<VoiceArgs['obj']['registerVoice(string,string,string,uint64)void'] | VoiceArgs['tuple']['registerVoice(string,string,string,uint64)void']>): VoiceComposer<[...TReturns, VoiceReturns['registerVoice(string,string,string,uint64)void'] | undefined]>

  /**
   * Calls the getVoiceId(address)uint64 ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  getVoiceId(params?: CallParams<VoiceArgs['obj']['getVoiceId(address)uint64'] | VoiceArgs['tuple']['getVoiceId(address)uint64']>): VoiceComposer<[...TReturns, VoiceReturns['getVoiceId(address)uint64'] | undefined]>

  /**
   * Calls the voice_exists(address)bool ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  voiceExists(params?: CallParams<VoiceArgs['obj']['voice_exists(address)bool'] | VoiceArgs['tuple']['voice_exists(address)bool']>): VoiceComposer<[...TReturns, VoiceReturns['voice_exists(address)bool'] | undefined]>

  /**
   * Calls the unregister_voice()void ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  unregisterVoice(params?: CallParams<VoiceArgs['obj']['unregister_voice()void'] | VoiceArgs['tuple']['unregister_voice()void']>): VoiceComposer<[...TReturns, VoiceReturns['unregister_voice()void'] | undefined]>

  /**
   * Makes a clear_state call to an existing instance of the Voice smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): VoiceComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): VoiceComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<VoiceComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<VoiceComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<VoiceComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<VoiceComposerResults<TReturns>>
}
export type VoiceComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

