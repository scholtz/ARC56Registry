/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"VoiceApp","structs":{},"methods":[{"name":"register_voice","args":[{"type":"string","name":"name"},{"type":"string","name":"model_uri"},{"type":"string","name":"rights"},{"type":"uint64","name":"price_per_use"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}},{"name":"get_voice_id","args":[{"type":"address","name":"owner"}],"returns":{"type":"uint64"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"voice_exists","args":[{"type":"address","name":"owner"}],"returns":{"type":"bool"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"unregister_voice","args":[],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}}],"arcs":[22,28],"desc":"\n    A contract that manages AI voice models, rights, and pricing.\n    ","networks":{},"state":{"schema":{"global":{"ints":0,"bytes":0},"local":{"ints":0,"bytes":0}},"keys":{"global":{},"local":{},"box":{}},"maps":{"global":{},"local":{},"box":{}}},"bareActions":{"create":["NoOp"],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[234],"errorMessage":"Voice ID not found for this owner"},{"pc":[122,135,148],"errorMessage":"invalid array length header"},{"pc":[129,142,155],"errorMessage":"invalid number of bytes for arc4.dynamic_array<arc4.uint8>"},{"pc":[227,250],"errorMessage":"invalid number of bytes for arc4.static_array<arc4.uint8, 32>"},{"pc":[164],"errorMessage":"invalid number of bytes for arc4.uint64"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"events":[]} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


/**
 * The argument types for the VoiceApp contract
 */
export type VoiceAppArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'register_voice(string,string,string,uint64)void': {
      name: string
      modelUri: string
      rights: string
      pricePerUse: bigint | number
    }
    'get_voice_id(address)uint64': {
      owner: string
    }
    'voice_exists(address)bool': {
      owner: string
    }
    'unregister_voice()void': Record<string, never>
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'register_voice(string,string,string,uint64)void': [name: string, modelUri: string, rights: string, pricePerUse: bigint | number]
    'get_voice_id(address)uint64': [owner: string]
    'voice_exists(address)bool': [owner: string]
    'unregister_voice()void': []
  }
}

/**
 * The return type for each method
 */
export type VoiceAppReturns = {
  'register_voice(string,string,string,uint64)void': void
  'get_voice_id(address)uint64': bigint
  'voice_exists(address)bool': boolean
  'unregister_voice()void': void
}

/**
 * Defines the types of available calls and state of the VoiceApp smart contract.
 */
export type VoiceAppTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'register_voice(string,string,string,uint64)void' | 'register_voice', {
      argsObj: VoiceAppArgs['obj']['register_voice(string,string,string,uint64)void']
      argsTuple: VoiceAppArgs['tuple']['register_voice(string,string,string,uint64)void']
      returns: VoiceAppReturns['register_voice(string,string,string,uint64)void']
    }>
    & Record<'get_voice_id(address)uint64' | 'get_voice_id', {
      argsObj: VoiceAppArgs['obj']['get_voice_id(address)uint64']
      argsTuple: VoiceAppArgs['tuple']['get_voice_id(address)uint64']
      returns: VoiceAppReturns['get_voice_id(address)uint64']
    }>
    & Record<'voice_exists(address)bool' | 'voice_exists', {
      argsObj: VoiceAppArgs['obj']['voice_exists(address)bool']
      argsTuple: VoiceAppArgs['tuple']['voice_exists(address)bool']
      returns: VoiceAppReturns['voice_exists(address)bool']
    }>
    & Record<'unregister_voice()void' | 'unregister_voice', {
      argsObj: VoiceAppArgs['obj']['unregister_voice()void']
      argsTuple: VoiceAppArgs['tuple']['unregister_voice()void']
      returns: VoiceAppReturns['unregister_voice()void']
    }>
}

/**
 * Defines the possible abi call signatures.
 */
export type VoiceAppSignatures = keyof VoiceAppTypes['methods']
/**
 * Defines the possible abi call signatures for methods that return a non-void value.
 */
export type VoiceAppNonVoidMethodSignatures = keyof VoiceAppTypes['methods'] extends infer T ? T extends keyof VoiceAppTypes['methods'] ? MethodReturn<T> extends void ? never : T  : never : never
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the VoiceApp smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends VoiceAppSignatures> = VoiceAppTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the VoiceApp smart contract to the method's return type
 */
export type MethodReturn<TSignature extends VoiceAppSignatures> = VoiceAppTypes['methods'][TSignature]['returns']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the VoiceApp smart contract
 */
export abstract class VoiceAppParamsFactory {
  /**
   * Constructs a no op call for the register_voice(string,string,string,uint64)void ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static registerVoice(params: CallParams<VoiceAppArgs['obj']['register_voice(string,string,string,uint64)void'] | VoiceAppArgs['tuple']['register_voice(string,string,string,uint64)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'register_voice(string,string,string,uint64)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.name, params.args.modelUri, params.args.rights, params.args.pricePerUse],
    }
  }
  /**
   * Constructs a no op call for the get_voice_id(address)uint64 ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static getVoiceId(params: CallParams<VoiceAppArgs['obj']['get_voice_id(address)uint64'] | VoiceAppArgs['tuple']['get_voice_id(address)uint64']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'get_voice_id(address)uint64' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.owner],
    }
  }
  /**
   * Constructs a no op call for the voice_exists(address)bool ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static voiceExists(params: CallParams<VoiceAppArgs['obj']['voice_exists(address)bool'] | VoiceAppArgs['tuple']['voice_exists(address)bool']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'voice_exists(address)bool' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.owner],
    }
  }
  /**
   * Constructs a no op call for the unregister_voice()void ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static unregisterVoice(params: CallParams<VoiceAppArgs['obj']['unregister_voice()void'] | VoiceAppArgs['tuple']['unregister_voice()void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'unregister_voice()void' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
}

/**
 * A client to make calls to the VoiceApp smart contract
 */
export class VoiceAppClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `VoiceAppClient`
   *
   * @param appClient An `AppClient` instance which has been created with the VoiceApp app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `VoiceAppClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Checks for decode errors on the given return value and maps the return value to the return type for the given method
   * @returns The typed return value or undefined if there was no value
   */
  decodeReturnValue<TSignature extends VoiceAppNonVoidMethodSignatures>(method: TSignature, returnValue: ABIReturn | undefined) {
    return returnValue !== undefined ? getArc56ReturnValue<MethodReturn<TSignature>>(returnValue, this.appClient.getABIMethod(method), APP_SPEC.structs) : undefined
  }

  /**
   * Returns a new `VoiceAppClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<VoiceAppClient> {
    return new VoiceAppClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `VoiceAppClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<VoiceAppClient> {
    return new VoiceAppClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Makes a clear_state call to an existing instance of the VoiceApp smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the VoiceApp smart contract using the `register_voice(string,string,string,uint64)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    registerVoice: (params: CallParams<VoiceAppArgs['obj']['register_voice(string,string,string,uint64)void'] | VoiceAppArgs['tuple']['register_voice(string,string,string,uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(VoiceAppParamsFactory.registerVoice(params))
    },

    /**
     * Makes a call to the VoiceApp smart contract using the `get_voice_id(address)uint64` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    getVoiceId: (params: CallParams<VoiceAppArgs['obj']['get_voice_id(address)uint64'] | VoiceAppArgs['tuple']['get_voice_id(address)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(VoiceAppParamsFactory.getVoiceId(params))
    },

    /**
     * Makes a call to the VoiceApp smart contract using the `voice_exists(address)bool` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    voiceExists: (params: CallParams<VoiceAppArgs['obj']['voice_exists(address)bool'] | VoiceAppArgs['tuple']['voice_exists(address)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(VoiceAppParamsFactory.voiceExists(params))
    },

    /**
     * Makes a call to the VoiceApp smart contract using the `unregister_voice()void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    unregisterVoice: (params: CallParams<VoiceAppArgs['obj']['unregister_voice()void'] | VoiceAppArgs['tuple']['unregister_voice()void']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(VoiceAppParamsFactory.unregisterVoice(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Makes a clear_state call to an existing instance of the VoiceApp smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the VoiceApp smart contract using the `register_voice(string,string,string,uint64)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    registerVoice: (params: CallParams<VoiceAppArgs['obj']['register_voice(string,string,string,uint64)void'] | VoiceAppArgs['tuple']['register_voice(string,string,string,uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(VoiceAppParamsFactory.registerVoice(params))
    },

    /**
     * Makes a call to the VoiceApp smart contract using the `get_voice_id(address)uint64` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    getVoiceId: (params: CallParams<VoiceAppArgs['obj']['get_voice_id(address)uint64'] | VoiceAppArgs['tuple']['get_voice_id(address)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(VoiceAppParamsFactory.getVoiceId(params))
    },

    /**
     * Makes a call to the VoiceApp smart contract using the `voice_exists(address)bool` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    voiceExists: (params: CallParams<VoiceAppArgs['obj']['voice_exists(address)bool'] | VoiceAppArgs['tuple']['voice_exists(address)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(VoiceAppParamsFactory.voiceExists(params))
    },

    /**
     * Makes a call to the VoiceApp smart contract using the `unregister_voice()void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    unregisterVoice: (params: CallParams<VoiceAppArgs['obj']['unregister_voice()void'] | VoiceAppArgs['tuple']['unregister_voice()void']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(VoiceAppParamsFactory.unregisterVoice(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Makes a clear_state call to an existing instance of the VoiceApp smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the VoiceApp smart contract using the `register_voice(string,string,string,uint64)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    registerVoice: async (params: CallParams<VoiceAppArgs['obj']['register_voice(string,string,string,uint64)void'] | VoiceAppArgs['tuple']['register_voice(string,string,string,uint64)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(VoiceAppParamsFactory.registerVoice(params))
      return {...result, return: result.return as unknown as (undefined | VoiceAppReturns['register_voice(string,string,string,uint64)void'])}
    },

    /**
     * Makes a call to the VoiceApp smart contract using the `get_voice_id(address)uint64` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    getVoiceId: async (params: CallParams<VoiceAppArgs['obj']['get_voice_id(address)uint64'] | VoiceAppArgs['tuple']['get_voice_id(address)uint64']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(VoiceAppParamsFactory.getVoiceId(params))
      return {...result, return: result.return as unknown as (undefined | VoiceAppReturns['get_voice_id(address)uint64'])}
    },

    /**
     * Makes a call to the VoiceApp smart contract using the `voice_exists(address)bool` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    voiceExists: async (params: CallParams<VoiceAppArgs['obj']['voice_exists(address)bool'] | VoiceAppArgs['tuple']['voice_exists(address)bool']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(VoiceAppParamsFactory.voiceExists(params))
      return {...result, return: result.return as unknown as (undefined | VoiceAppReturns['voice_exists(address)bool'])}
    },

    /**
     * Makes a call to the VoiceApp smart contract using the `unregister_voice()void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    unregisterVoice: async (params: CallParams<VoiceAppArgs['obj']['unregister_voice()void'] | VoiceAppArgs['tuple']['unregister_voice()void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(VoiceAppParamsFactory.unregisterVoice(params))
      return {...result, return: result.return as unknown as (undefined | VoiceAppReturns['unregister_voice()void'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new VoiceAppClient(this.appClient.clone(params))
  }

  /**
   * Makes a readonly (simulated) call to the VoiceApp smart contract using the `get_voice_id(address)uint64` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async getVoiceId(params: CallParams<VoiceAppArgs['obj']['get_voice_id(address)uint64'] | VoiceAppArgs['tuple']['get_voice_id(address)uint64']>) {
    const result = await this.appClient.send.call(VoiceAppParamsFactory.getVoiceId(params))
    return result.return as unknown as VoiceAppReturns['get_voice_id(address)uint64']
  }

  /**
   * Makes a readonly (simulated) call to the VoiceApp smart contract using the `voice_exists(address)bool` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async voiceExists(params: CallParams<VoiceAppArgs['obj']['voice_exists(address)bool'] | VoiceAppArgs['tuple']['voice_exists(address)bool']>) {
    const result = await this.appClient.send.call(VoiceAppParamsFactory.voiceExists(params))
    return result.return as unknown as VoiceAppReturns['voice_exists(address)bool']
  }

  /**
   * Methods to access state for the current VoiceApp app
   */
  state = {
  }

  public newGroup(): VoiceAppComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a register_voice(string,string,string,uint64)void method call against the VoiceApp contract
       */
      registerVoice(params: CallParams<VoiceAppArgs['obj']['register_voice(string,string,string,uint64)void'] | VoiceAppArgs['tuple']['register_voice(string,string,string,uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.registerVoice(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a get_voice_id(address)uint64 method call against the VoiceApp contract
       */
      getVoiceId(params: CallParams<VoiceAppArgs['obj']['get_voice_id(address)uint64'] | VoiceAppArgs['tuple']['get_voice_id(address)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.getVoiceId(params)))
        resultMappers.push((v) => client.decodeReturnValue('get_voice_id(address)uint64', v))
        return this
      },
      /**
       * Add a voice_exists(address)bool method call against the VoiceApp contract
       */
      voiceExists(params: CallParams<VoiceAppArgs['obj']['voice_exists(address)bool'] | VoiceAppArgs['tuple']['voice_exists(address)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.voiceExists(params)))
        resultMappers.push((v) => client.decodeReturnValue('voice_exists(address)bool', v))
        return this
      },
      /**
       * Add a unregister_voice()void method call against the VoiceApp contract
       */
      unregisterVoice(params: CallParams<VoiceAppArgs['obj']['unregister_voice()void'] | VoiceAppArgs['tuple']['unregister_voice()void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.unregisterVoice(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a clear state call to the VoiceApp contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as VoiceAppComposer
  }
}
export type VoiceAppComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the register_voice(string,string,string,uint64)void ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  registerVoice(params?: CallParams<VoiceAppArgs['obj']['register_voice(string,string,string,uint64)void'] | VoiceAppArgs['tuple']['register_voice(string,string,string,uint64)void']>): VoiceAppComposer<[...TReturns, VoiceAppReturns['register_voice(string,string,string,uint64)void'] | undefined]>

  /**
   * Calls the get_voice_id(address)uint64 ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  getVoiceId(params?: CallParams<VoiceAppArgs['obj']['get_voice_id(address)uint64'] | VoiceAppArgs['tuple']['get_voice_id(address)uint64']>): VoiceAppComposer<[...TReturns, VoiceAppReturns['get_voice_id(address)uint64'] | undefined]>

  /**
   * Calls the voice_exists(address)bool ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  voiceExists(params?: CallParams<VoiceAppArgs['obj']['voice_exists(address)bool'] | VoiceAppArgs['tuple']['voice_exists(address)bool']>): VoiceAppComposer<[...TReturns, VoiceAppReturns['voice_exists(address)bool'] | undefined]>

  /**
   * Calls the unregister_voice()void ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  unregisterVoice(params?: CallParams<VoiceAppArgs['obj']['unregister_voice()void'] | VoiceAppArgs['tuple']['unregister_voice()void']>): VoiceAppComposer<[...TReturns, VoiceAppReturns['unregister_voice()void'] | undefined]>

  /**
   * Makes a clear_state call to an existing instance of the VoiceApp smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): VoiceAppComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): VoiceAppComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<VoiceAppComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<VoiceAppComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<VoiceAppComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<VoiceAppComposerResults<TReturns>>
}
export type VoiceAppComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

