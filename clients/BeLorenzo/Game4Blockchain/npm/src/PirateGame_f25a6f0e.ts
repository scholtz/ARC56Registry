/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"PirateGame","structs":{"Object2AEA0877":[{"name":"proposer","type":"uint64"},{"name":"distribution","type":"byte[]"},{"name":"votesFor","type":"uint64"},{"name":"votesAgainst","type":"uint64"}],"Object76865934":[{"name":"phase","type":"uint64"},{"name":"currentRound","type":"uint64"},{"name":"totalPirates","type":"uint64"},{"name":"alivePirates","type":"uint64"},{"name":"currentProposerIndex","type":"uint64"},{"name":"pot","type":"uint64"},{"name":"commitDuration","type":"uint64"},{"name":"revealDuration","type":"uint64"}],"Object8F0328FD":[{"name":"pirateAddress","type":"address"},{"name":"seniorityIndex","type":"uint64"},{"name":"alive","type":"bool"},{"name":"claimed","type":"bool"}],"ObjectB555233E":[{"name":"startAt","type":"uint64"},{"name":"endCommitAt","type":"uint64"},{"name":"endRevealAt","type":"uint64"},{"name":"participation","type":"uint64"}]},"methods":[{"name":"createSession","args":[{"type":"(uint64,uint64,uint64,uint64)","struct":"ObjectB555233E","name":"config"},{"type":"pay","name":"mbrPayment"},{"type":"uint64","name":"maxPirates"}],"returns":{"type":"uint64"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Initializes a new game session with the provided configuration.\nValidates the Minimum Balance Requirement (MBR) payment.","events":[],"recommendations":{}},{"name":"registerPirate","args":[{"type":"uint64","name":"sessionID"},{"type":"pay","name":"payment"},{"type":"pay","name":"mbrPayment"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Allows a player to join the game during the registration phase.\nRequires the player to cover their own data storage cost (MBR).","events":[],"recommendations":{}},{"name":"proposeDistribution","args":[{"type":"uint64","name":"sessionID"},{"type":"byte[]","name":"distribution"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Submits a distribution proposal. Can only be called by the current proposer.\nThe distribution byte array must match the total pot amount.","events":[],"recommendations":{}},{"name":"commitVote","args":[{"type":"uint64","name":"sessionID"},{"type":"byte[]","name":"voteHash"},{"type":"pay","name":"mbrPayment"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Commits a hashed vote during the voting phase.","events":[],"recommendations":{}},{"name":"revealVote","args":[{"type":"uint64","name":"sessionID"},{"type":"uint64","name":"vote"},{"type":"byte[]","name":"salt"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Reveals a previously committed vote.\nUpdates the proposal vote count.","events":[],"recommendations":{}},{"name":"executeRound","args":[{"type":"uint64","name":"sessionID"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Executes the current round logic after the reveal phase.\nDetermines if the proposal passed or failed.\nEliminates the proposer if failed, or proceeds to next round/victory.","events":[],"recommendations":{}},{"name":"claimWinnings","args":[{"type":"uint64","name":"sessionID"}],"returns":{"type":"uint64"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Allows a pirate to claim their share of the pot after the game ends.","events":[],"recommendations":{}},{"name":"getRequiredMBR","args":[{"type":"string","name":"command"}],"returns":{"type":"uint64"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Calculates the MBR required for specific actions.","events":[],"recommendations":{}},{"name":"timeOut","args":[{"type":"uint64","name":"sessionID"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Handles timeouts in various phases.\nIf registration stuck: refunds pirates.\nIf proposal stuck: eliminates the current proposer and moves to next round.","events":[],"recommendations":{}},{"name":"getGameType","args":[],"returns":{"type":"byte[]"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Returns contract identification info.","events":[],"recommendations":{}},{"name":"initialize","args":[{"type":"string","name":"gameType"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Sets the game type. Immutable once set.\nCan only be called by the creator.","events":[],"recommendations":{}}],"arcs":[22,28],"networks":{},"state":{"schema":{"global":{"ints":1,"bytes":1},"local":{"ints":0,"bytes":0}},"keys":{"global":{"gameType":{"keyType":"AVMString","valueType":"AVMBytes","key":"Z2FtZVR5cGU=","desc":"Contract type identifier (e.g., 'RPS', 'GUESS').\nUsed for CLI validation."},"sessionIDCounter":{"keyType":"AVMString","valueType":"AVMUint64","key":"c2Vzc2lvbklEQ291bnRlcg==","desc":"Global counter for generating unique session IDs."}},"local":{},"box":{}},"maps":{"global":{},"local":{},"box":{"gameState":{"keyType":"uint64","valueType":"Object76865934","prefix":"Z3N0"},"pirates":{"keyType":"AVMBytes","valueType":"Object8F0328FD","prefix":"cGly"},"pirateList":{"keyType":"uint64","valueType":"AVMBytes","prefix":"cGxz"},"proposals":{"keyType":"uint64","valueType":"Object2AEA0877","prefix":"cHJw"},"playerCommit":{"keyType":"AVMBytes","valueType":"AVMBytes","desc":"Storage for player commits.\nKey: Hash(sessionID + playerAddress) OR Hash(sessionID + round + playerAddress)\nValue: Hash(choice + salt) [32 bytes]","prefix":"cGNvbQ=="},"playerChoice":{"keyType":"AVMBytes","valueType":"uint64","desc":"Storage for revealed choices.\nKey: Hash(sessionID + playerAddress) OR Hash(sessionID + round + playerAddress)\nValue: Player Choice [8 bytes]","prefix":"cGNobw=="},"gameSessions":{"keyType":"uint64","valueType":"ObjectB555233E","desc":"Configuration storage for each active session.","prefix":"Z3M="},"sessionBalances":{"keyType":"uint64","valueType":"uint64","desc":"Tracks the total pot (collected participation fees) for a specific session.","prefix":"c2JhbA=="}}}},"bareActions":{"create":["NoOp"],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[1789],"errorMessage":"Already claimed"},{"pc":[1107],"errorMessage":"Already committed this round"},{"pc":[2002],"errorMessage":"Already refunded"},{"pc":[597],"errorMessage":"Already registered"},{"pc":[555,624,669,794,850,1028,1201,1205,1259,1332,1443,1755,1796,1940,2325,2363,2392,2439],"errorMessage":"Box must have value"},{"pc":[2524],"errorMessage":"Command not supported"},{"pc":[2300],"errorMessage":"Contract already initialized"},{"pc":[927],"errorMessage":"Distribution must sum to pot"},{"pc":[631],"errorMessage":"Game is full"},{"pc":[1761],"errorMessage":"Game not finished"},{"pc":[803],"errorMessage":"Game not started yet"},{"pc":[661],"errorMessage":"Incorrect participation fee"},{"pc":[354],"errorMessage":"Insufficient MBR"},{"pc":[616],"errorMessage":"Insufficient MBR for pirate registration"},{"pc":[1086],"errorMessage":"Insufficient MBR for vote commit"},{"pc":[2496],"errorMessage":"Invalid command"},{"pc":[882],"errorMessage":"Invalid distribution length"},{"pc":[1306],"errorMessage":"Invalid reveal: hash mismatch"},{"pc":[378],"errorMessage":"Invalid start time: cannot start in the past"},{"pc":[372],"errorMessage":"Invalid timeline: commit end must be before reveal end"},{"pc":[364],"errorMessage":"Invalid timeline: start must be before commit end"},{"pc":[350,610,1080],"errorMessage":"MBR payment receiver must be contract"},{"pc":[580],"errorMessage":"Max pirates reached"},{"pc":[336],"errorMessage":"Minimum participation is 1 ALGO"},{"pc":[2422],"errorMessage":"No alive pirate found"},{"pc":[1288],"errorMessage":"No commit for this round"},{"pc":[1839],"errorMessage":"No winnings for you"},{"pc":[1779],"errorMessage":"Not a pirate"},{"pc":[1993],"errorMessage":"Not a registered pirate"},{"pc":[832],"errorMessage":"Not enough pirates to start"},{"pc":[1465],"errorMessage":"Not in execution phase"},{"pc":[846],"errorMessage":"Not in proposal phase"},{"pc":[1245],"errorMessage":"Not in reveal phase"},{"pc":[1039],"errorMessage":"Not in voting phase"},{"pc":[817,1061],"errorMessage":"Not registered"},{"pc":[866],"errorMessage":"Not your turn to propose"},{"pc":[2041,2044],"errorMessage":"Nothing to unlock in this phase"},{"pc":[124],"errorMessage":"OnCompletion must be NoOp"},{"pc":[220],"errorMessage":"OnCompletion must be NoOp && can only call when creating"},{"pc":[2306],"errorMessage":"Only creator can initialize"},{"pc":[649],"errorMessage":"Payment must go to contract"},{"pc":[324],"errorMessage":"Pirates must be between 3 and 20"},{"pc":[2052],"errorMessage":"Proposal deadline not passed yet"},{"pc":[566],"errorMessage":"Registration closed (Game Started)"},{"pc":[571],"errorMessage":"Registration phase has ended"},{"pc":[1977],"errorMessage":"Registration still active"},{"pc":[1221],"errorMessage":"Reveal deadline passed"},{"pc":[1474],"errorMessage":"Reveal phase not ended"},{"pc":[548,637,779,1022,1174,1428,1749,1925],"errorMessage":"Session does not exist"},{"pc":[1186],"errorMessage":"Vote must be 0 or 1"},{"pc":[1047],"errorMessage":"Voting deadline passed"},{"pc":[1213],"errorMessage":"Voting still open"},{"pc":[855,1067,1263],"errorMessage":"You are eliminated"},{"pc":[383,2260,2297],"errorMessage":"check GlobalState exists"},{"pc":[749,989,1146,1872,2281],"errorMessage":"invalid array length header"},{"pc":[289],"errorMessage":"invalid number of bytes for ObjectB555233E"},{"pc":[757,997,1154,1880,2289],"errorMessage":"invalid number of bytes for arc4.dynamic_array<arc4.uint8>"},{"pc":[307,514,742,982,1127,1136,1405,1739,1902],"errorMessage":"invalid number of bytes for arc4.uint64"},{"pc":[299,526,536,1010],"errorMessage":"transaction type is pay"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"events":[]} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


// Type definitions for ARC-56 structs

export type Object2Aea0877 = {
  proposer: bigint,
  distribution: Uint8Array,
  votesFor: bigint,
  votesAgainst: bigint
}


/**
 * Converts the ABI tuple representation of a Object2AEA0877 to the struct representation
 */
export function Object2Aea0877FromTuple(abiTuple: [bigint, Uint8Array, bigint, bigint]) {
  return getABIStructFromABITuple(abiTuple, APP_SPEC.structs.Object2AEA0877, APP_SPEC.structs) as Object2Aea0877
}

export type Object76865934 = {
  phase: bigint,
  currentRound: bigint,
  totalPirates: bigint,
  alivePirates: bigint,
  currentProposerIndex: bigint,
  pot: bigint,
  commitDuration: bigint,
  revealDuration: bigint
}


/**
 * Converts the ABI tuple representation of a Object76865934 to the struct representation
 */
export function Object76865934FromTuple(abiTuple: [bigint, bigint, bigint, bigint, bigint, bigint, bigint, bigint]) {
  return getABIStructFromABITuple(abiTuple, APP_SPEC.structs.Object76865934, APP_SPEC.structs) as Object76865934
}

export type Object8F0328Fd = {
  pirateAddress: string,
  seniorityIndex: bigint,
  alive: boolean,
  claimed: boolean
}


/**
 * Converts the ABI tuple representation of a Object8F0328FD to the struct representation
 */
export function Object8F0328FdFromTuple(abiTuple: [string, bigint, boolean, boolean]) {
  return getABIStructFromABITuple(abiTuple, APP_SPEC.structs.Object8F0328FD, APP_SPEC.structs) as Object8F0328Fd
}

export type ObjectB555233E = {
  startAt: bigint,
  endCommitAt: bigint,
  endRevealAt: bigint,
  participation: bigint
}


/**
 * Converts the ABI tuple representation of a ObjectB555233E to the struct representation
 */
export function ObjectB555233EFromTuple(abiTuple: [bigint, bigint, bigint, bigint]) {
  return getABIStructFromABITuple(abiTuple, APP_SPEC.structs.ObjectB555233E, APP_SPEC.structs) as ObjectB555233E
}

/**
 * The argument types for the PirateGame contract
 */
export type PirateGameArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'createSession((uint64,uint64,uint64,uint64),pay,uint64)uint64': {
      config: ObjectB555233E
      mbrPayment: AppMethodCallTransactionArgument
      maxPirates: bigint | number
    }
    'registerPirate(uint64,pay,pay)void': {
      sessionId: bigint | number
      payment: AppMethodCallTransactionArgument
      mbrPayment: AppMethodCallTransactionArgument
    }
    'proposeDistribution(uint64,byte[])void': {
      sessionId: bigint | number
      distribution: Uint8Array
    }
    'commitVote(uint64,byte[],pay)void': {
      sessionId: bigint | number
      voteHash: Uint8Array
      mbrPayment: AppMethodCallTransactionArgument
    }
    'revealVote(uint64,uint64,byte[])void': {
      sessionId: bigint | number
      vote: bigint | number
      salt: Uint8Array
    }
    'executeRound(uint64)void': {
      sessionId: bigint | number
    }
    'claimWinnings(uint64)uint64': {
      sessionId: bigint | number
    }
    'getRequiredMBR(string)uint64': {
      command: string
    }
    'timeOut(uint64)void': {
      sessionId: bigint | number
    }
    'getGameType()byte[]': Record<string, never>
    'initialize(string)void': {
      gameType: string
    }
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'createSession((uint64,uint64,uint64,uint64),pay,uint64)uint64': [config: ObjectB555233E, mbrPayment: AppMethodCallTransactionArgument, maxPirates: bigint | number]
    'registerPirate(uint64,pay,pay)void': [sessionId: bigint | number, payment: AppMethodCallTransactionArgument, mbrPayment: AppMethodCallTransactionArgument]
    'proposeDistribution(uint64,byte[])void': [sessionId: bigint | number, distribution: Uint8Array]
    'commitVote(uint64,byte[],pay)void': [sessionId: bigint | number, voteHash: Uint8Array, mbrPayment: AppMethodCallTransactionArgument]
    'revealVote(uint64,uint64,byte[])void': [sessionId: bigint | number, vote: bigint | number, salt: Uint8Array]
    'executeRound(uint64)void': [sessionId: bigint | number]
    'claimWinnings(uint64)uint64': [sessionId: bigint | number]
    'getRequiredMBR(string)uint64': [command: string]
    'timeOut(uint64)void': [sessionId: bigint | number]
    'getGameType()byte[]': []
    'initialize(string)void': [gameType: string]
  }
}

/**
 * The return type for each method
 */
export type PirateGameReturns = {
  'createSession((uint64,uint64,uint64,uint64),pay,uint64)uint64': bigint
  'registerPirate(uint64,pay,pay)void': void
  'proposeDistribution(uint64,byte[])void': void
  'commitVote(uint64,byte[],pay)void': void
  'revealVote(uint64,uint64,byte[])void': void
  'executeRound(uint64)void': void
  'claimWinnings(uint64)uint64': bigint
  'getRequiredMBR(string)uint64': bigint
  'timeOut(uint64)void': void
  'getGameType()byte[]': Uint8Array
  'initialize(string)void': void
}

/**
 * Defines the types of available calls and state of the PirateGame smart contract.
 */
export type PirateGameTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'createSession((uint64,uint64,uint64,uint64),pay,uint64)uint64' | 'createSession', {
      argsObj: PirateGameArgs['obj']['createSession((uint64,uint64,uint64,uint64),pay,uint64)uint64']
      argsTuple: PirateGameArgs['tuple']['createSession((uint64,uint64,uint64,uint64),pay,uint64)uint64']
      returns: PirateGameReturns['createSession((uint64,uint64,uint64,uint64),pay,uint64)uint64']
    }>
    & Record<'registerPirate(uint64,pay,pay)void' | 'registerPirate', {
      argsObj: PirateGameArgs['obj']['registerPirate(uint64,pay,pay)void']
      argsTuple: PirateGameArgs['tuple']['registerPirate(uint64,pay,pay)void']
      returns: PirateGameReturns['registerPirate(uint64,pay,pay)void']
    }>
    & Record<'proposeDistribution(uint64,byte[])void' | 'proposeDistribution', {
      argsObj: PirateGameArgs['obj']['proposeDistribution(uint64,byte[])void']
      argsTuple: PirateGameArgs['tuple']['proposeDistribution(uint64,byte[])void']
      returns: PirateGameReturns['proposeDistribution(uint64,byte[])void']
    }>
    & Record<'commitVote(uint64,byte[],pay)void' | 'commitVote', {
      argsObj: PirateGameArgs['obj']['commitVote(uint64,byte[],pay)void']
      argsTuple: PirateGameArgs['tuple']['commitVote(uint64,byte[],pay)void']
      returns: PirateGameReturns['commitVote(uint64,byte[],pay)void']
    }>
    & Record<'revealVote(uint64,uint64,byte[])void' | 'revealVote', {
      argsObj: PirateGameArgs['obj']['revealVote(uint64,uint64,byte[])void']
      argsTuple: PirateGameArgs['tuple']['revealVote(uint64,uint64,byte[])void']
      returns: PirateGameReturns['revealVote(uint64,uint64,byte[])void']
    }>
    & Record<'executeRound(uint64)void' | 'executeRound', {
      argsObj: PirateGameArgs['obj']['executeRound(uint64)void']
      argsTuple: PirateGameArgs['tuple']['executeRound(uint64)void']
      returns: PirateGameReturns['executeRound(uint64)void']
    }>
    & Record<'claimWinnings(uint64)uint64' | 'claimWinnings', {
      argsObj: PirateGameArgs['obj']['claimWinnings(uint64)uint64']
      argsTuple: PirateGameArgs['tuple']['claimWinnings(uint64)uint64']
      returns: PirateGameReturns['claimWinnings(uint64)uint64']
    }>
    & Record<'getRequiredMBR(string)uint64' | 'getRequiredMBR', {
      argsObj: PirateGameArgs['obj']['getRequiredMBR(string)uint64']
      argsTuple: PirateGameArgs['tuple']['getRequiredMBR(string)uint64']
      returns: PirateGameReturns['getRequiredMBR(string)uint64']
    }>
    & Record<'timeOut(uint64)void' | 'timeOut', {
      argsObj: PirateGameArgs['obj']['timeOut(uint64)void']
      argsTuple: PirateGameArgs['tuple']['timeOut(uint64)void']
      returns: PirateGameReturns['timeOut(uint64)void']
    }>
    & Record<'getGameType()byte[]' | 'getGameType', {
      argsObj: PirateGameArgs['obj']['getGameType()byte[]']
      argsTuple: PirateGameArgs['tuple']['getGameType()byte[]']
      returns: PirateGameReturns['getGameType()byte[]']
    }>
    & Record<'initialize(string)void' | 'initialize', {
      argsObj: PirateGameArgs['obj']['initialize(string)void']
      argsTuple: PirateGameArgs['tuple']['initialize(string)void']
      returns: PirateGameReturns['initialize(string)void']
    }>
  /**
   * Defines the shape of the state of the application.
   */
  state: {
    global: {
      keys: {
        /**
        * Contract type identifier (e.g., 'RPS', 'GUESS').
        Used for CLI validation.

         */
        gameType: BinaryState
        /**
         * Global counter for generating unique session IDs.
         */
        sessionIdCounter: bigint
      }
      maps: {}
    }
    box: {
      keys: {}
      maps: {
        gameState: Map<bigint | number, Object76865934>
        pirates: Map<Uint8Array | string, Object8F0328Fd>
        pirateList: Map<bigint | number, Uint8Array>
        proposals: Map<bigint | number, Object2Aea0877>
        /**
        * Storage for player commits.
        Key: Hash(sessionID + playerAddress) OR Hash(sessionID + round + playerAddress)
        Value: Hash(choice + salt) [32 bytes]

         */
        playerCommit: Map<Uint8Array | string, Uint8Array>
        /**
        * Storage for revealed choices.
        Key: Hash(sessionID + playerAddress) OR Hash(sessionID + round + playerAddress)
        Value: Player Choice [8 bytes]

         */
        playerChoice: Map<Uint8Array | string, bigint>
        /**
         * Configuration storage for each active session.
         */
        gameSessions: Map<bigint | number, ObjectB555233E>
        /**
         * Tracks the total pot (collected participation fees) for a specific session.
         */
        sessionBalances: Map<bigint | number, bigint>
      }
    }
  }
}

/**
 * Defines the possible abi call signatures.
 */
export type PirateGameSignatures = keyof PirateGameTypes['methods']
/**
 * Defines the possible abi call signatures for methods that return a non-void value.
 */
export type PirateGameNonVoidMethodSignatures = keyof PirateGameTypes['methods'] extends infer T ? T extends keyof PirateGameTypes['methods'] ? MethodReturn<T> extends void ? never : T  : never : never
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the PirateGame smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends PirateGameSignatures> = PirateGameTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the PirateGame smart contract to the method's return type
 */
export type MethodReturn<TSignature extends PirateGameSignatures> = PirateGameTypes['methods'][TSignature]['returns']

/**
 * Defines the shape of the keyed global state of the application.
 */
export type GlobalKeysState = PirateGameTypes['state']['global']['keys']

/**
 * Defines the shape of the keyed box state of the application.
 */
export type BoxKeysState = PirateGameTypes['state']['box']['keys']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the PirateGame smart contract
 */
export abstract class PirateGameParamsFactory {
  /**
   * Constructs a no op call for the createSession((uint64,uint64,uint64,uint64),pay,uint64)uint64 ABI method
   *
  * Initializes a new game session with the provided configuration.
  Validates the Minimum Balance Requirement (MBR) payment.

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static createSession(params: CallParams<PirateGameArgs['obj']['createSession((uint64,uint64,uint64,uint64),pay,uint64)uint64'] | PirateGameArgs['tuple']['createSession((uint64,uint64,uint64,uint64),pay,uint64)uint64']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'createSession((uint64,uint64,uint64,uint64),pay,uint64)uint64' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.config, params.args.mbrPayment, params.args.maxPirates],
    }
  }
  /**
   * Constructs a no op call for the registerPirate(uint64,pay,pay)void ABI method
   *
  * Allows a player to join the game during the registration phase.
  Requires the player to cover their own data storage cost (MBR).

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static registerPirate(params: CallParams<PirateGameArgs['obj']['registerPirate(uint64,pay,pay)void'] | PirateGameArgs['tuple']['registerPirate(uint64,pay,pay)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'registerPirate(uint64,pay,pay)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.sessionId, params.args.payment, params.args.mbrPayment],
    }
  }
  /**
   * Constructs a no op call for the proposeDistribution(uint64,byte[])void ABI method
   *
  * Submits a distribution proposal. Can only be called by the current proposer.
  The distribution byte array must match the total pot amount.

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static proposeDistribution(params: CallParams<PirateGameArgs['obj']['proposeDistribution(uint64,byte[])void'] | PirateGameArgs['tuple']['proposeDistribution(uint64,byte[])void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'proposeDistribution(uint64,byte[])void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.sessionId, params.args.distribution],
    }
  }
  /**
   * Constructs a no op call for the commitVote(uint64,byte[],pay)void ABI method
   *
   * Commits a hashed vote during the voting phase.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static commitVote(params: CallParams<PirateGameArgs['obj']['commitVote(uint64,byte[],pay)void'] | PirateGameArgs['tuple']['commitVote(uint64,byte[],pay)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'commitVote(uint64,byte[],pay)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.sessionId, params.args.voteHash, params.args.mbrPayment],
    }
  }
  /**
   * Constructs a no op call for the revealVote(uint64,uint64,byte[])void ABI method
   *
  * Reveals a previously committed vote.
  Updates the proposal vote count.

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static revealVote(params: CallParams<PirateGameArgs['obj']['revealVote(uint64,uint64,byte[])void'] | PirateGameArgs['tuple']['revealVote(uint64,uint64,byte[])void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'revealVote(uint64,uint64,byte[])void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.sessionId, params.args.vote, params.args.salt],
    }
  }
  /**
   * Constructs a no op call for the executeRound(uint64)void ABI method
   *
  * Executes the current round logic after the reveal phase.
  Determines if the proposal passed or failed.
  Eliminates the proposer if failed, or proceeds to next round/victory.

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static executeRound(params: CallParams<PirateGameArgs['obj']['executeRound(uint64)void'] | PirateGameArgs['tuple']['executeRound(uint64)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'executeRound(uint64)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.sessionId],
    }
  }
  /**
   * Constructs a no op call for the claimWinnings(uint64)uint64 ABI method
   *
   * Allows a pirate to claim their share of the pot after the game ends.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static claimWinnings(params: CallParams<PirateGameArgs['obj']['claimWinnings(uint64)uint64'] | PirateGameArgs['tuple']['claimWinnings(uint64)uint64']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'claimWinnings(uint64)uint64' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.sessionId],
    }
  }
  /**
   * Constructs a no op call for the getRequiredMBR(string)uint64 ABI method
   *
   * Calculates the MBR required for specific actions.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static getRequiredMbr(params: CallParams<PirateGameArgs['obj']['getRequiredMBR(string)uint64'] | PirateGameArgs['tuple']['getRequiredMBR(string)uint64']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'getRequiredMBR(string)uint64' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.command],
    }
  }
  /**
   * Constructs a no op call for the timeOut(uint64)void ABI method
   *
  * Handles timeouts in various phases.
  If registration stuck: refunds pirates.
  If proposal stuck: eliminates the current proposer and moves to next round.

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static timeOut(params: CallParams<PirateGameArgs['obj']['timeOut(uint64)void'] | PirateGameArgs['tuple']['timeOut(uint64)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'timeOut(uint64)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.sessionId],
    }
  }
  /**
   * Constructs a no op call for the getGameType()byte[] ABI method
   *
   * Returns contract identification info.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static getGameType(params: CallParams<PirateGameArgs['obj']['getGameType()byte[]'] | PirateGameArgs['tuple']['getGameType()byte[]']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'getGameType()byte[]' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the initialize(string)void ABI method
   *
  * Sets the game type. Immutable once set.
  Can only be called by the creator.

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static initialize(params: CallParams<PirateGameArgs['obj']['initialize(string)void'] | PirateGameArgs['tuple']['initialize(string)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'initialize(string)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.gameType],
    }
  }
}

/**
 * A client to make calls to the PirateGame smart contract
 */
export class PirateGameClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `PirateGameClient`
   *
   * @param appClient An `AppClient` instance which has been created with the PirateGame app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `PirateGameClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Checks for decode errors on the given return value and maps the return value to the return type for the given method
   * @returns The typed return value or undefined if there was no value
   */
  decodeReturnValue<TSignature extends PirateGameNonVoidMethodSignatures>(method: TSignature, returnValue: ABIReturn | undefined) {
    return returnValue !== undefined ? getArc56ReturnValue<MethodReturn<TSignature>>(returnValue, this.appClient.getABIMethod(method), APP_SPEC.structs) : undefined
  }

  /**
   * Returns a new `PirateGameClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<PirateGameClient> {
    return new PirateGameClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `PirateGameClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<PirateGameClient> {
    return new PirateGameClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Makes a clear_state call to an existing instance of the PirateGame smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the PirateGame smart contract using the `createSession((uint64,uint64,uint64,uint64),pay,uint64)uint64` ABI method.
     *
    * Initializes a new game session with the provided configuration.
    Validates the Minimum Balance Requirement (MBR) payment.

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    createSession: (params: CallParams<PirateGameArgs['obj']['createSession((uint64,uint64,uint64,uint64),pay,uint64)uint64'] | PirateGameArgs['tuple']['createSession((uint64,uint64,uint64,uint64),pay,uint64)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(PirateGameParamsFactory.createSession(params))
    },

    /**
     * Makes a call to the PirateGame smart contract using the `registerPirate(uint64,pay,pay)void` ABI method.
     *
    * Allows a player to join the game during the registration phase.
    Requires the player to cover their own data storage cost (MBR).

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    registerPirate: (params: CallParams<PirateGameArgs['obj']['registerPirate(uint64,pay,pay)void'] | PirateGameArgs['tuple']['registerPirate(uint64,pay,pay)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(PirateGameParamsFactory.registerPirate(params))
    },

    /**
     * Makes a call to the PirateGame smart contract using the `proposeDistribution(uint64,byte[])void` ABI method.
     *
    * Submits a distribution proposal. Can only be called by the current proposer.
    The distribution byte array must match the total pot amount.

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    proposeDistribution: (params: CallParams<PirateGameArgs['obj']['proposeDistribution(uint64,byte[])void'] | PirateGameArgs['tuple']['proposeDistribution(uint64,byte[])void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(PirateGameParamsFactory.proposeDistribution(params))
    },

    /**
     * Makes a call to the PirateGame smart contract using the `commitVote(uint64,byte[],pay)void` ABI method.
     *
     * Commits a hashed vote during the voting phase.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    commitVote: (params: CallParams<PirateGameArgs['obj']['commitVote(uint64,byte[],pay)void'] | PirateGameArgs['tuple']['commitVote(uint64,byte[],pay)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(PirateGameParamsFactory.commitVote(params))
    },

    /**
     * Makes a call to the PirateGame smart contract using the `revealVote(uint64,uint64,byte[])void` ABI method.
     *
    * Reveals a previously committed vote.
    Updates the proposal vote count.

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    revealVote: (params: CallParams<PirateGameArgs['obj']['revealVote(uint64,uint64,byte[])void'] | PirateGameArgs['tuple']['revealVote(uint64,uint64,byte[])void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(PirateGameParamsFactory.revealVote(params))
    },

    /**
     * Makes a call to the PirateGame smart contract using the `executeRound(uint64)void` ABI method.
     *
    * Executes the current round logic after the reveal phase.
    Determines if the proposal passed or failed.
    Eliminates the proposer if failed, or proceeds to next round/victory.

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    executeRound: (params: CallParams<PirateGameArgs['obj']['executeRound(uint64)void'] | PirateGameArgs['tuple']['executeRound(uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(PirateGameParamsFactory.executeRound(params))
    },

    /**
     * Makes a call to the PirateGame smart contract using the `claimWinnings(uint64)uint64` ABI method.
     *
     * Allows a pirate to claim their share of the pot after the game ends.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    claimWinnings: (params: CallParams<PirateGameArgs['obj']['claimWinnings(uint64)uint64'] | PirateGameArgs['tuple']['claimWinnings(uint64)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(PirateGameParamsFactory.claimWinnings(params))
    },

    /**
     * Makes a call to the PirateGame smart contract using the `getRequiredMBR(string)uint64` ABI method.
     *
     * Calculates the MBR required for specific actions.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    getRequiredMbr: (params: CallParams<PirateGameArgs['obj']['getRequiredMBR(string)uint64'] | PirateGameArgs['tuple']['getRequiredMBR(string)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(PirateGameParamsFactory.getRequiredMbr(params))
    },

    /**
     * Makes a call to the PirateGame smart contract using the `timeOut(uint64)void` ABI method.
     *
    * Handles timeouts in various phases.
    If registration stuck: refunds pirates.
    If proposal stuck: eliminates the current proposer and moves to next round.

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    timeOut: (params: CallParams<PirateGameArgs['obj']['timeOut(uint64)void'] | PirateGameArgs['tuple']['timeOut(uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(PirateGameParamsFactory.timeOut(params))
    },

    /**
     * Makes a call to the PirateGame smart contract using the `getGameType()byte[]` ABI method.
     *
     * Returns contract identification info.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    getGameType: (params: CallParams<PirateGameArgs['obj']['getGameType()byte[]'] | PirateGameArgs['tuple']['getGameType()byte[]']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(PirateGameParamsFactory.getGameType(params))
    },

    /**
     * Makes a call to the PirateGame smart contract using the `initialize(string)void` ABI method.
     *
    * Sets the game type. Immutable once set.
    Can only be called by the creator.

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    initialize: (params: CallParams<PirateGameArgs['obj']['initialize(string)void'] | PirateGameArgs['tuple']['initialize(string)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(PirateGameParamsFactory.initialize(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Makes a clear_state call to an existing instance of the PirateGame smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the PirateGame smart contract using the `createSession((uint64,uint64,uint64,uint64),pay,uint64)uint64` ABI method.
     *
    * Initializes a new game session with the provided configuration.
    Validates the Minimum Balance Requirement (MBR) payment.

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    createSession: (params: CallParams<PirateGameArgs['obj']['createSession((uint64,uint64,uint64,uint64),pay,uint64)uint64'] | PirateGameArgs['tuple']['createSession((uint64,uint64,uint64,uint64),pay,uint64)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(PirateGameParamsFactory.createSession(params))
    },

    /**
     * Makes a call to the PirateGame smart contract using the `registerPirate(uint64,pay,pay)void` ABI method.
     *
    * Allows a player to join the game during the registration phase.
    Requires the player to cover their own data storage cost (MBR).

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    registerPirate: (params: CallParams<PirateGameArgs['obj']['registerPirate(uint64,pay,pay)void'] | PirateGameArgs['tuple']['registerPirate(uint64,pay,pay)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(PirateGameParamsFactory.registerPirate(params))
    },

    /**
     * Makes a call to the PirateGame smart contract using the `proposeDistribution(uint64,byte[])void` ABI method.
     *
    * Submits a distribution proposal. Can only be called by the current proposer.
    The distribution byte array must match the total pot amount.

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    proposeDistribution: (params: CallParams<PirateGameArgs['obj']['proposeDistribution(uint64,byte[])void'] | PirateGameArgs['tuple']['proposeDistribution(uint64,byte[])void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(PirateGameParamsFactory.proposeDistribution(params))
    },

    /**
     * Makes a call to the PirateGame smart contract using the `commitVote(uint64,byte[],pay)void` ABI method.
     *
     * Commits a hashed vote during the voting phase.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    commitVote: (params: CallParams<PirateGameArgs['obj']['commitVote(uint64,byte[],pay)void'] | PirateGameArgs['tuple']['commitVote(uint64,byte[],pay)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(PirateGameParamsFactory.commitVote(params))
    },

    /**
     * Makes a call to the PirateGame smart contract using the `revealVote(uint64,uint64,byte[])void` ABI method.
     *
    * Reveals a previously committed vote.
    Updates the proposal vote count.

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    revealVote: (params: CallParams<PirateGameArgs['obj']['revealVote(uint64,uint64,byte[])void'] | PirateGameArgs['tuple']['revealVote(uint64,uint64,byte[])void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(PirateGameParamsFactory.revealVote(params))
    },

    /**
     * Makes a call to the PirateGame smart contract using the `executeRound(uint64)void` ABI method.
     *
    * Executes the current round logic after the reveal phase.
    Determines if the proposal passed or failed.
    Eliminates the proposer if failed, or proceeds to next round/victory.

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    executeRound: (params: CallParams<PirateGameArgs['obj']['executeRound(uint64)void'] | PirateGameArgs['tuple']['executeRound(uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(PirateGameParamsFactory.executeRound(params))
    },

    /**
     * Makes a call to the PirateGame smart contract using the `claimWinnings(uint64)uint64` ABI method.
     *
     * Allows a pirate to claim their share of the pot after the game ends.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    claimWinnings: (params: CallParams<PirateGameArgs['obj']['claimWinnings(uint64)uint64'] | PirateGameArgs['tuple']['claimWinnings(uint64)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(PirateGameParamsFactory.claimWinnings(params))
    },

    /**
     * Makes a call to the PirateGame smart contract using the `getRequiredMBR(string)uint64` ABI method.
     *
     * Calculates the MBR required for specific actions.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    getRequiredMbr: (params: CallParams<PirateGameArgs['obj']['getRequiredMBR(string)uint64'] | PirateGameArgs['tuple']['getRequiredMBR(string)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(PirateGameParamsFactory.getRequiredMbr(params))
    },

    /**
     * Makes a call to the PirateGame smart contract using the `timeOut(uint64)void` ABI method.
     *
    * Handles timeouts in various phases.
    If registration stuck: refunds pirates.
    If proposal stuck: eliminates the current proposer and moves to next round.

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    timeOut: (params: CallParams<PirateGameArgs['obj']['timeOut(uint64)void'] | PirateGameArgs['tuple']['timeOut(uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(PirateGameParamsFactory.timeOut(params))
    },

    /**
     * Makes a call to the PirateGame smart contract using the `getGameType()byte[]` ABI method.
     *
     * Returns contract identification info.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    getGameType: (params: CallParams<PirateGameArgs['obj']['getGameType()byte[]'] | PirateGameArgs['tuple']['getGameType()byte[]']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(PirateGameParamsFactory.getGameType(params))
    },

    /**
     * Makes a call to the PirateGame smart contract using the `initialize(string)void` ABI method.
     *
    * Sets the game type. Immutable once set.
    Can only be called by the creator.

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    initialize: (params: CallParams<PirateGameArgs['obj']['initialize(string)void'] | PirateGameArgs['tuple']['initialize(string)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(PirateGameParamsFactory.initialize(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Makes a clear_state call to an existing instance of the PirateGame smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the PirateGame smart contract using the `createSession((uint64,uint64,uint64,uint64),pay,uint64)uint64` ABI method.
     *
    * Initializes a new game session with the provided configuration.
    Validates the Minimum Balance Requirement (MBR) payment.

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    createSession: async (params: CallParams<PirateGameArgs['obj']['createSession((uint64,uint64,uint64,uint64),pay,uint64)uint64'] | PirateGameArgs['tuple']['createSession((uint64,uint64,uint64,uint64),pay,uint64)uint64']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(PirateGameParamsFactory.createSession(params))
      return {...result, return: result.return as unknown as (undefined | PirateGameReturns['createSession((uint64,uint64,uint64,uint64),pay,uint64)uint64'])}
    },

    /**
     * Makes a call to the PirateGame smart contract using the `registerPirate(uint64,pay,pay)void` ABI method.
     *
    * Allows a player to join the game during the registration phase.
    Requires the player to cover their own data storage cost (MBR).

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    registerPirate: async (params: CallParams<PirateGameArgs['obj']['registerPirate(uint64,pay,pay)void'] | PirateGameArgs['tuple']['registerPirate(uint64,pay,pay)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(PirateGameParamsFactory.registerPirate(params))
      return {...result, return: result.return as unknown as (undefined | PirateGameReturns['registerPirate(uint64,pay,pay)void'])}
    },

    /**
     * Makes a call to the PirateGame smart contract using the `proposeDistribution(uint64,byte[])void` ABI method.
     *
    * Submits a distribution proposal. Can only be called by the current proposer.
    The distribution byte array must match the total pot amount.

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    proposeDistribution: async (params: CallParams<PirateGameArgs['obj']['proposeDistribution(uint64,byte[])void'] | PirateGameArgs['tuple']['proposeDistribution(uint64,byte[])void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(PirateGameParamsFactory.proposeDistribution(params))
      return {...result, return: result.return as unknown as (undefined | PirateGameReturns['proposeDistribution(uint64,byte[])void'])}
    },

    /**
     * Makes a call to the PirateGame smart contract using the `commitVote(uint64,byte[],pay)void` ABI method.
     *
     * Commits a hashed vote during the voting phase.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    commitVote: async (params: CallParams<PirateGameArgs['obj']['commitVote(uint64,byte[],pay)void'] | PirateGameArgs['tuple']['commitVote(uint64,byte[],pay)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(PirateGameParamsFactory.commitVote(params))
      return {...result, return: result.return as unknown as (undefined | PirateGameReturns['commitVote(uint64,byte[],pay)void'])}
    },

    /**
     * Makes a call to the PirateGame smart contract using the `revealVote(uint64,uint64,byte[])void` ABI method.
     *
    * Reveals a previously committed vote.
    Updates the proposal vote count.

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    revealVote: async (params: CallParams<PirateGameArgs['obj']['revealVote(uint64,uint64,byte[])void'] | PirateGameArgs['tuple']['revealVote(uint64,uint64,byte[])void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(PirateGameParamsFactory.revealVote(params))
      return {...result, return: result.return as unknown as (undefined | PirateGameReturns['revealVote(uint64,uint64,byte[])void'])}
    },

    /**
     * Makes a call to the PirateGame smart contract using the `executeRound(uint64)void` ABI method.
     *
    * Executes the current round logic after the reveal phase.
    Determines if the proposal passed or failed.
    Eliminates the proposer if failed, or proceeds to next round/victory.

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    executeRound: async (params: CallParams<PirateGameArgs['obj']['executeRound(uint64)void'] | PirateGameArgs['tuple']['executeRound(uint64)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(PirateGameParamsFactory.executeRound(params))
      return {...result, return: result.return as unknown as (undefined | PirateGameReturns['executeRound(uint64)void'])}
    },

    /**
     * Makes a call to the PirateGame smart contract using the `claimWinnings(uint64)uint64` ABI method.
     *
     * Allows a pirate to claim their share of the pot after the game ends.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    claimWinnings: async (params: CallParams<PirateGameArgs['obj']['claimWinnings(uint64)uint64'] | PirateGameArgs['tuple']['claimWinnings(uint64)uint64']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(PirateGameParamsFactory.claimWinnings(params))
      return {...result, return: result.return as unknown as (undefined | PirateGameReturns['claimWinnings(uint64)uint64'])}
    },

    /**
     * Makes a call to the PirateGame smart contract using the `getRequiredMBR(string)uint64` ABI method.
     *
     * Calculates the MBR required for specific actions.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    getRequiredMbr: async (params: CallParams<PirateGameArgs['obj']['getRequiredMBR(string)uint64'] | PirateGameArgs['tuple']['getRequiredMBR(string)uint64']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(PirateGameParamsFactory.getRequiredMbr(params))
      return {...result, return: result.return as unknown as (undefined | PirateGameReturns['getRequiredMBR(string)uint64'])}
    },

    /**
     * Makes a call to the PirateGame smart contract using the `timeOut(uint64)void` ABI method.
     *
    * Handles timeouts in various phases.
    If registration stuck: refunds pirates.
    If proposal stuck: eliminates the current proposer and moves to next round.

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    timeOut: async (params: CallParams<PirateGameArgs['obj']['timeOut(uint64)void'] | PirateGameArgs['tuple']['timeOut(uint64)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(PirateGameParamsFactory.timeOut(params))
      return {...result, return: result.return as unknown as (undefined | PirateGameReturns['timeOut(uint64)void'])}
    },

    /**
     * Makes a call to the PirateGame smart contract using the `getGameType()byte[]` ABI method.
     *
     * Returns contract identification info.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    getGameType: async (params: CallParams<PirateGameArgs['obj']['getGameType()byte[]'] | PirateGameArgs['tuple']['getGameType()byte[]']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(PirateGameParamsFactory.getGameType(params))
      return {...result, return: result.return as unknown as (undefined | PirateGameReturns['getGameType()byte[]'])}
    },

    /**
     * Makes a call to the PirateGame smart contract using the `initialize(string)void` ABI method.
     *
    * Sets the game type. Immutable once set.
    Can only be called by the creator.

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    initialize: async (params: CallParams<PirateGameArgs['obj']['initialize(string)void'] | PirateGameArgs['tuple']['initialize(string)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(PirateGameParamsFactory.initialize(params))
      return {...result, return: result.return as unknown as (undefined | PirateGameReturns['initialize(string)void'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new PirateGameClient(this.appClient.clone(params))
  }

  /**
   * Methods to access state for the current PirateGame app
   */
  state = {
    /**
     * Methods to access global state for the current PirateGame app
     */
    global: {
      /**
       * Get all current keyed values from global state
       */
      getAll: async (): Promise<Partial<Expand<GlobalKeysState>>> => {
        const result = await this.appClient.state.global.getAll()
        return {
          gameType: new BinaryStateValue(result.gameType),
          sessionIdCounter: result.sessionIDCounter,
        }
      },
      /**
       * Get the current value of the gameType key in global state
       */
      gameType: async (): Promise<BinaryState> => { return new BinaryStateValue((await this.appClient.state.global.getValue("gameType")) as Uint8Array | undefined) },
      /**
       * Get the current value of the sessionIDCounter key in global state
       */
      sessionIdCounter: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("sessionIDCounter")) as bigint | undefined },
    },
    /**
     * Methods to access box state for the current PirateGame app
     */
    box: {
      /**
       * Get all current keyed values from box state
       */
      getAll: async (): Promise<Partial<Expand<BoxKeysState>>> => {
        const result = await this.appClient.state.box.getAll()
        return {
        }
      },
      /**
       * Get values from the gameState map in box state
       */
      gameState: {
        /**
         * Get all current values of the gameState map in box state
         */
        getMap: async (): Promise<Map<bigint, Object76865934>> => { return (await this.appClient.state.box.getMap("gameState")) as Map<bigint, Object76865934> },
        /**
         * Get a current value of the gameState map by key from box state
         */
        value: async (key: bigint | number): Promise<Object76865934 | undefined> => { return await this.appClient.state.box.getMapValue("gameState", key) as Object76865934 | undefined },
      },
      /**
       * Get values from the pirates map in box state
       */
      pirates: {
        /**
         * Get all current values of the pirates map in box state
         */
        getMap: async (): Promise<Map<Uint8Array, Object8F0328Fd>> => { return (await this.appClient.state.box.getMap("pirates")) as Map<Uint8Array, Object8F0328Fd> },
        /**
         * Get a current value of the pirates map by key from box state
         */
        value: async (key: Uint8Array | string): Promise<Object8F0328Fd | undefined> => { return await this.appClient.state.box.getMapValue("pirates", key) as Object8F0328Fd | undefined },
      },
      /**
       * Get values from the pirateList map in box state
       */
      pirateList: {
        /**
         * Get all current values of the pirateList map in box state
         */
        getMap: async (): Promise<Map<bigint, Uint8Array>> => { return (await this.appClient.state.box.getMap("pirateList")) as Map<bigint, Uint8Array> },
        /**
         * Get a current value of the pirateList map by key from box state
         */
        value: async (key: bigint | number): Promise<Uint8Array | undefined> => { return await this.appClient.state.box.getMapValue("pirateList", key) as Uint8Array | undefined },
      },
      /**
       * Get values from the proposals map in box state
       */
      proposals: {
        /**
         * Get all current values of the proposals map in box state
         */
        getMap: async (): Promise<Map<bigint, Object2Aea0877>> => { return (await this.appClient.state.box.getMap("proposals")) as Map<bigint, Object2Aea0877> },
        /**
         * Get a current value of the proposals map by key from box state
         */
        value: async (key: bigint | number): Promise<Object2Aea0877 | undefined> => { return await this.appClient.state.box.getMapValue("proposals", key) as Object2Aea0877 | undefined },
      },
      /**
       * Get values from the playerCommit map in box state
       */
      playerCommit: {
        /**
         * Get all current values of the playerCommit map in box state
         */
        getMap: async (): Promise<Map<Uint8Array, Uint8Array>> => { return (await this.appClient.state.box.getMap("playerCommit")) as Map<Uint8Array, Uint8Array> },
        /**
         * Get a current value of the playerCommit map by key from box state
         */
        value: async (key: Uint8Array | string): Promise<Uint8Array | undefined> => { return await this.appClient.state.box.getMapValue("playerCommit", key) as Uint8Array | undefined },
      },
      /**
       * Get values from the playerChoice map in box state
       */
      playerChoice: {
        /**
         * Get all current values of the playerChoice map in box state
         */
        getMap: async (): Promise<Map<Uint8Array, bigint>> => { return (await this.appClient.state.box.getMap("playerChoice")) as Map<Uint8Array, bigint> },
        /**
         * Get a current value of the playerChoice map by key from box state
         */
        value: async (key: Uint8Array | string): Promise<bigint | undefined> => { return await this.appClient.state.box.getMapValue("playerChoice", key) as bigint | undefined },
      },
      /**
       * Get values from the gameSessions map in box state
       */
      gameSessions: {
        /**
         * Get all current values of the gameSessions map in box state
         */
        getMap: async (): Promise<Map<bigint, ObjectB555233E>> => { return (await this.appClient.state.box.getMap("gameSessions")) as Map<bigint, ObjectB555233E> },
        /**
         * Get a current value of the gameSessions map by key from box state
         */
        value: async (key: bigint | number): Promise<ObjectB555233E | undefined> => { return await this.appClient.state.box.getMapValue("gameSessions", key) as ObjectB555233E | undefined },
      },
      /**
       * Get values from the sessionBalances map in box state
       */
      sessionBalances: {
        /**
         * Get all current values of the sessionBalances map in box state
         */
        getMap: async (): Promise<Map<bigint, bigint>> => { return (await this.appClient.state.box.getMap("sessionBalances")) as Map<bigint, bigint> },
        /**
         * Get a current value of the sessionBalances map by key from box state
         */
        value: async (key: bigint | number): Promise<bigint | undefined> => { return await this.appClient.state.box.getMapValue("sessionBalances", key) as bigint | undefined },
      },
    },
  }

  public newGroup(): PirateGameComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a createSession((uint64,uint64,uint64,uint64),pay,uint64)uint64 method call against the PirateGame contract
       */
      createSession(params: CallParams<PirateGameArgs['obj']['createSession((uint64,uint64,uint64,uint64),pay,uint64)uint64'] | PirateGameArgs['tuple']['createSession((uint64,uint64,uint64,uint64),pay,uint64)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.createSession(params)))
        resultMappers.push((v) => client.decodeReturnValue('createSession((uint64,uint64,uint64,uint64),pay,uint64)uint64', v))
        return this
      },
      /**
       * Add a registerPirate(uint64,pay,pay)void method call against the PirateGame contract
       */
      registerPirate(params: CallParams<PirateGameArgs['obj']['registerPirate(uint64,pay,pay)void'] | PirateGameArgs['tuple']['registerPirate(uint64,pay,pay)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.registerPirate(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a proposeDistribution(uint64,byte[])void method call against the PirateGame contract
       */
      proposeDistribution(params: CallParams<PirateGameArgs['obj']['proposeDistribution(uint64,byte[])void'] | PirateGameArgs['tuple']['proposeDistribution(uint64,byte[])void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.proposeDistribution(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a commitVote(uint64,byte[],pay)void method call against the PirateGame contract
       */
      commitVote(params: CallParams<PirateGameArgs['obj']['commitVote(uint64,byte[],pay)void'] | PirateGameArgs['tuple']['commitVote(uint64,byte[],pay)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.commitVote(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a revealVote(uint64,uint64,byte[])void method call against the PirateGame contract
       */
      revealVote(params: CallParams<PirateGameArgs['obj']['revealVote(uint64,uint64,byte[])void'] | PirateGameArgs['tuple']['revealVote(uint64,uint64,byte[])void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.revealVote(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a executeRound(uint64)void method call against the PirateGame contract
       */
      executeRound(params: CallParams<PirateGameArgs['obj']['executeRound(uint64)void'] | PirateGameArgs['tuple']['executeRound(uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.executeRound(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a claimWinnings(uint64)uint64 method call against the PirateGame contract
       */
      claimWinnings(params: CallParams<PirateGameArgs['obj']['claimWinnings(uint64)uint64'] | PirateGameArgs['tuple']['claimWinnings(uint64)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.claimWinnings(params)))
        resultMappers.push((v) => client.decodeReturnValue('claimWinnings(uint64)uint64', v))
        return this
      },
      /**
       * Add a getRequiredMBR(string)uint64 method call against the PirateGame contract
       */
      getRequiredMbr(params: CallParams<PirateGameArgs['obj']['getRequiredMBR(string)uint64'] | PirateGameArgs['tuple']['getRequiredMBR(string)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.getRequiredMbr(params)))
        resultMappers.push((v) => client.decodeReturnValue('getRequiredMBR(string)uint64', v))
        return this
      },
      /**
       * Add a timeOut(uint64)void method call against the PirateGame contract
       */
      timeOut(params: CallParams<PirateGameArgs['obj']['timeOut(uint64)void'] | PirateGameArgs['tuple']['timeOut(uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.timeOut(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a getGameType()byte[] method call against the PirateGame contract
       */
      getGameType(params: CallParams<PirateGameArgs['obj']['getGameType()byte[]'] | PirateGameArgs['tuple']['getGameType()byte[]']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.getGameType(params)))
        resultMappers.push((v) => client.decodeReturnValue('getGameType()byte[]', v))
        return this
      },
      /**
       * Add a initialize(string)void method call against the PirateGame contract
       */
      initialize(params: CallParams<PirateGameArgs['obj']['initialize(string)void'] | PirateGameArgs['tuple']['initialize(string)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.initialize(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a clear state call to the PirateGame contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as PirateGameComposer
  }
}
export type PirateGameComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the createSession((uint64,uint64,uint64,uint64),pay,uint64)uint64 ABI method.
   *
  * Initializes a new game session with the provided configuration.
  Validates the Minimum Balance Requirement (MBR) payment.

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  createSession(params?: CallParams<PirateGameArgs['obj']['createSession((uint64,uint64,uint64,uint64),pay,uint64)uint64'] | PirateGameArgs['tuple']['createSession((uint64,uint64,uint64,uint64),pay,uint64)uint64']>): PirateGameComposer<[...TReturns, PirateGameReturns['createSession((uint64,uint64,uint64,uint64),pay,uint64)uint64'] | undefined]>

  /**
   * Calls the registerPirate(uint64,pay,pay)void ABI method.
   *
  * Allows a player to join the game during the registration phase.
  Requires the player to cover their own data storage cost (MBR).

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  registerPirate(params?: CallParams<PirateGameArgs['obj']['registerPirate(uint64,pay,pay)void'] | PirateGameArgs['tuple']['registerPirate(uint64,pay,pay)void']>): PirateGameComposer<[...TReturns, PirateGameReturns['registerPirate(uint64,pay,pay)void'] | undefined]>

  /**
   * Calls the proposeDistribution(uint64,byte[])void ABI method.
   *
  * Submits a distribution proposal. Can only be called by the current proposer.
  The distribution byte array must match the total pot amount.

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  proposeDistribution(params?: CallParams<PirateGameArgs['obj']['proposeDistribution(uint64,byte[])void'] | PirateGameArgs['tuple']['proposeDistribution(uint64,byte[])void']>): PirateGameComposer<[...TReturns, PirateGameReturns['proposeDistribution(uint64,byte[])void'] | undefined]>

  /**
   * Calls the commitVote(uint64,byte[],pay)void ABI method.
   *
   * Commits a hashed vote during the voting phase.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  commitVote(params?: CallParams<PirateGameArgs['obj']['commitVote(uint64,byte[],pay)void'] | PirateGameArgs['tuple']['commitVote(uint64,byte[],pay)void']>): PirateGameComposer<[...TReturns, PirateGameReturns['commitVote(uint64,byte[],pay)void'] | undefined]>

  /**
   * Calls the revealVote(uint64,uint64,byte[])void ABI method.
   *
  * Reveals a previously committed vote.
  Updates the proposal vote count.

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  revealVote(params?: CallParams<PirateGameArgs['obj']['revealVote(uint64,uint64,byte[])void'] | PirateGameArgs['tuple']['revealVote(uint64,uint64,byte[])void']>): PirateGameComposer<[...TReturns, PirateGameReturns['revealVote(uint64,uint64,byte[])void'] | undefined]>

  /**
   * Calls the executeRound(uint64)void ABI method.
   *
  * Executes the current round logic after the reveal phase.
  Determines if the proposal passed or failed.
  Eliminates the proposer if failed, or proceeds to next round/victory.

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  executeRound(params?: CallParams<PirateGameArgs['obj']['executeRound(uint64)void'] | PirateGameArgs['tuple']['executeRound(uint64)void']>): PirateGameComposer<[...TReturns, PirateGameReturns['executeRound(uint64)void'] | undefined]>

  /**
   * Calls the claimWinnings(uint64)uint64 ABI method.
   *
   * Allows a pirate to claim their share of the pot after the game ends.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  claimWinnings(params?: CallParams<PirateGameArgs['obj']['claimWinnings(uint64)uint64'] | PirateGameArgs['tuple']['claimWinnings(uint64)uint64']>): PirateGameComposer<[...TReturns, PirateGameReturns['claimWinnings(uint64)uint64'] | undefined]>

  /**
   * Calls the getRequiredMBR(string)uint64 ABI method.
   *
   * Calculates the MBR required for specific actions.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  getRequiredMbr(params?: CallParams<PirateGameArgs['obj']['getRequiredMBR(string)uint64'] | PirateGameArgs['tuple']['getRequiredMBR(string)uint64']>): PirateGameComposer<[...TReturns, PirateGameReturns['getRequiredMBR(string)uint64'] | undefined]>

  /**
   * Calls the timeOut(uint64)void ABI method.
   *
  * Handles timeouts in various phases.
  If registration stuck: refunds pirates.
  If proposal stuck: eliminates the current proposer and moves to next round.

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  timeOut(params?: CallParams<PirateGameArgs['obj']['timeOut(uint64)void'] | PirateGameArgs['tuple']['timeOut(uint64)void']>): PirateGameComposer<[...TReturns, PirateGameReturns['timeOut(uint64)void'] | undefined]>

  /**
   * Calls the getGameType()byte[] ABI method.
   *
   * Returns contract identification info.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  getGameType(params?: CallParams<PirateGameArgs['obj']['getGameType()byte[]'] | PirateGameArgs['tuple']['getGameType()byte[]']>): PirateGameComposer<[...TReturns, PirateGameReturns['getGameType()byte[]'] | undefined]>

  /**
   * Calls the initialize(string)void ABI method.
   *
  * Sets the game type. Immutable once set.
  Can only be called by the creator.

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  initialize(params?: CallParams<PirateGameArgs['obj']['initialize(string)void'] | PirateGameArgs['tuple']['initialize(string)void']>): PirateGameComposer<[...TReturns, PirateGameReturns['initialize(string)void'] | undefined]>

  /**
   * Makes a clear_state call to an existing instance of the PirateGame smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): PirateGameComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): PirateGameComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<PirateGameComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<PirateGameComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<PirateGameComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<PirateGameComposerResults<TReturns>>
}
export type PirateGameComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

