/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"GuessGame","structs":{"Object0B783EB4":[{"name":"sum","type":"uint64"},{"name":"count","type":"uint64"}],"ObjectB555233E":[{"name":"startAt","type":"uint64"},{"name":"endCommitAt","type":"uint64"},{"name":"endRevealAt","type":"uint64"},{"name":"participation","type":"uint64"}]},"methods":[{"name":"createSession","args":[{"type":"(uint64,uint64,uint64,uint64)","struct":"ObjectB555233E","name":"config"},{"type":"pay","name":"mbrPayment"}],"returns":{"type":"uint64"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Initializes a new game session and allocates storage for counters.\nValidates that the MBR payment covers the large frequency blob.","events":[],"recommendations":{}},{"name":"joinSession","args":[{"type":"uint64","name":"sessionID"},{"type":"byte[]","name":"commit"},{"type":"pay","name":"payment"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Allows a player to join the session.","events":[],"recommendations":{}},{"name":"revealMove","args":[{"type":"uint64","name":"sessionID"},{"type":"uint64","name":"choice"},{"type":"byte[]","name":"salt"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Reveals a player's move and updates global game statistics.\nChoice must be between 0 and 100.","events":[],"recommendations":{}},{"name":"claimWinnings","args":[{"type":"uint64","name":"sessionID"}],"returns":{"type":"uint64"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Calculates the winner and distributes the prize using the Pull pattern.\nUses ensureBudget to guarantee sufficient opcodes for the calculation loop.","events":[],"recommendations":{}},{"name":"getRequiredMBR","args":[{"type":"string","name":"command"}],"returns":{"type":"uint64"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Calculates the required Minimum Balance Requirement (MBR) for storage allocation.","events":[],"recommendations":{}},{"name":"getGameType","args":[],"returns":{"type":"byte[]"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Returns contract identification info.","events":[],"recommendations":{}},{"name":"initialize","args":[{"type":"string","name":"gameType"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Sets the game type. Immutable once set.\nCan only be called by the creator.","events":[],"recommendations":{}}],"arcs":[22,28],"desc":"Implementation of the \"Guess 2/3 of the Average\" game.\n\nGame Mechanics:\n- Players choose a number between 0 and 100.\n- The system calculates the average of all choices.\n- The Target is defined as 2/3 of that average.\n- The winner is the player whose number is closest to the Target.\n- In case of a tie, the pot is split equally among the winners.","networks":{},"state":{"schema":{"global":{"ints":1,"bytes":1},"local":{"ints":0,"bytes":0}},"keys":{"global":{"gameType":{"keyType":"AVMString","valueType":"AVMBytes","key":"Z2FtZVR5cGU=","desc":"Contract type identifier (e.g., 'RPS', 'GUESS').\nUsed for CLI validation."},"sessionIDCounter":{"keyType":"AVMString","valueType":"AVMUint64","key":"c2Vzc2lvbklEQ291bnRlcg==","desc":"Global counter for generating unique session IDs."}},"local":{},"box":{}},"maps":{"global":{},"local":{},"box":{"stats":{"keyType":"uint64","valueType":"Object0B783EB4","desc":"Stores global session statistics required to calculate the average.\nKey: SessionID","prefix":"c3Q="},"frequency":{"keyType":"uint64","valueType":"AVMBytes","desc":"Compressed frequency map storing player choices.\nStructure: A single byte blob of 808 bytes (101 uint64s).\nIndex 'i' (0-100) holds the count of players who chose 'i'.\nKey: SessionID","prefix":"ZnI="},"playerCommit":{"keyType":"AVMBytes","valueType":"AVMBytes","desc":"Storage for player commits.\nKey: Hash(sessionID + playerAddress) OR Hash(sessionID + round + playerAddress)\nValue: Hash(choice + salt) [32 bytes]","prefix":"cGNvbQ=="},"playerChoice":{"keyType":"AVMBytes","valueType":"uint64","desc":"Storage for revealed choices.\nKey: Hash(sessionID + playerAddress) OR Hash(sessionID + round + playerAddress)\nValue: Player Choice [8 bytes]","prefix":"cGNobw=="},"gameSessions":{"keyType":"uint64","valueType":"ObjectB555233E","desc":"Configuration storage for each active session.","prefix":"Z3M="},"sessionBalances":{"keyType":"uint64","valueType":"uint64","desc":"Tracks the total pot (collected participation fees) for a specific session.","prefix":"c2JhbA=="}}}},"bareActions":{"create":["NoOp"],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[412,544,572,700,761],"errorMessage":"Box must have value"},{"pc":[464],"errorMessage":"Choice must be between 0 and 100"},{"pc":[1090],"errorMessage":"Command not supported"},{"pc":[362],"errorMessage":"Commit phase has ended"},{"pc":[488],"errorMessage":"Commit phase is still active"},{"pc":[1028],"errorMessage":"Contract already initialized"},{"pc":[370],"errorMessage":"Fee receiver must be the contract"},{"pc":[661],"errorMessage":"Game is not finished yet"},{"pc":[356],"errorMessage":"Game session has not started yet"},{"pc":[382],"errorMessage":"Incorrect participation fee amount"},{"pc":[203],"errorMessage":"Insufficient MBR for session and counters"},{"pc":[527],"errorMessage":"Invalid reveal: hash mismatch"},{"pc":[224],"errorMessage":"Invalid start time: cannot start in the past"},{"pc":[220],"errorMessage":"Invalid timeline: commit end must be before reveal end"},{"pc":[213],"errorMessage":"Invalid timeline: start must be before commit end"},{"pc":[198],"errorMessage":"MBR payment receiver must be contract"},{"pc":[509],"errorMessage":"No commit found for this player or already revealed"},{"pc":[97],"errorMessage":"OnCompletion must be NoOp"},{"pc":[165],"errorMessage":"OnCompletion must be NoOp && can only call when creating"},{"pc":[1034],"errorMessage":"Only creator can initialize"},{"pc":[398],"errorMessage":"Player already registered in this session"},{"pc":[683],"errorMessage":"Player has not revealed or already claimed"},{"pc":[494],"errorMessage":"Reveal phase has ended"},{"pc":[344,476,651,913],"errorMessage":"Session does not exist"},{"pc":[711],"errorMessage":"You did not win or no pot available"},{"pc":[229,991,1025],"errorMessage":"check GlobalState exists"},{"pc":[312,445,967,1011],"errorMessage":"invalid array length header"},{"pc":[176],"errorMessage":"invalid number of bytes for ObjectB555233E"},{"pc":[319,452,974,1018],"errorMessage":"invalid number of bytes for arc4.dynamic_array<arc4.uint8>"},{"pc":[305,428,437,603],"errorMessage":"invalid number of bytes for arc4.uint64"},{"pc":[186,332],"errorMessage":"transaction type is pay"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"events":[]} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


// Type definitions for ARC-56 structs

export type Object0B783Eb4 = {
  sum: bigint,
  count: bigint
}


/**
 * Converts the ABI tuple representation of a Object0B783EB4 to the struct representation
 */
export function Object0B783Eb4FromTuple(abiTuple: [bigint, bigint]) {
  return getABIStructFromABITuple(abiTuple, APP_SPEC.structs.Object0B783EB4, APP_SPEC.structs) as Object0B783Eb4
}

export type ObjectB555233E = {
  startAt: bigint,
  endCommitAt: bigint,
  endRevealAt: bigint,
  participation: bigint
}


/**
 * Converts the ABI tuple representation of a ObjectB555233E to the struct representation
 */
export function ObjectB555233EFromTuple(abiTuple: [bigint, bigint, bigint, bigint]) {
  return getABIStructFromABITuple(abiTuple, APP_SPEC.structs.ObjectB555233E, APP_SPEC.structs) as ObjectB555233E
}

/**
 * The argument types for the GuessGame contract
 */
export type GuessGameArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'createSession((uint64,uint64,uint64,uint64),pay)uint64': {
      config: ObjectB555233E
      mbrPayment: AppMethodCallTransactionArgument
    }
    'joinSession(uint64,byte[],pay)void': {
      sessionId: bigint | number
      commit: Uint8Array
      payment: AppMethodCallTransactionArgument
    }
    'revealMove(uint64,uint64,byte[])void': {
      sessionId: bigint | number
      choice: bigint | number
      salt: Uint8Array
    }
    'claimWinnings(uint64)uint64': {
      sessionId: bigint | number
    }
    'getRequiredMBR(string)uint64': {
      command: string
    }
    'getGameType()byte[]': Record<string, never>
    'initialize(string)void': {
      gameType: string
    }
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'createSession((uint64,uint64,uint64,uint64),pay)uint64': [config: ObjectB555233E, mbrPayment: AppMethodCallTransactionArgument]
    'joinSession(uint64,byte[],pay)void': [sessionId: bigint | number, commit: Uint8Array, payment: AppMethodCallTransactionArgument]
    'revealMove(uint64,uint64,byte[])void': [sessionId: bigint | number, choice: bigint | number, salt: Uint8Array]
    'claimWinnings(uint64)uint64': [sessionId: bigint | number]
    'getRequiredMBR(string)uint64': [command: string]
    'getGameType()byte[]': []
    'initialize(string)void': [gameType: string]
  }
}

/**
 * The return type for each method
 */
export type GuessGameReturns = {
  'createSession((uint64,uint64,uint64,uint64),pay)uint64': bigint
  'joinSession(uint64,byte[],pay)void': void
  'revealMove(uint64,uint64,byte[])void': void
  'claimWinnings(uint64)uint64': bigint
  'getRequiredMBR(string)uint64': bigint
  'getGameType()byte[]': Uint8Array
  'initialize(string)void': void
}

/**
 * Defines the types of available calls and state of the GuessGame smart contract.
 */
export type GuessGameTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'createSession((uint64,uint64,uint64,uint64),pay)uint64' | 'createSession', {
      argsObj: GuessGameArgs['obj']['createSession((uint64,uint64,uint64,uint64),pay)uint64']
      argsTuple: GuessGameArgs['tuple']['createSession((uint64,uint64,uint64,uint64),pay)uint64']
      returns: GuessGameReturns['createSession((uint64,uint64,uint64,uint64),pay)uint64']
    }>
    & Record<'joinSession(uint64,byte[],pay)void' | 'joinSession', {
      argsObj: GuessGameArgs['obj']['joinSession(uint64,byte[],pay)void']
      argsTuple: GuessGameArgs['tuple']['joinSession(uint64,byte[],pay)void']
      returns: GuessGameReturns['joinSession(uint64,byte[],pay)void']
    }>
    & Record<'revealMove(uint64,uint64,byte[])void' | 'revealMove', {
      argsObj: GuessGameArgs['obj']['revealMove(uint64,uint64,byte[])void']
      argsTuple: GuessGameArgs['tuple']['revealMove(uint64,uint64,byte[])void']
      returns: GuessGameReturns['revealMove(uint64,uint64,byte[])void']
    }>
    & Record<'claimWinnings(uint64)uint64' | 'claimWinnings', {
      argsObj: GuessGameArgs['obj']['claimWinnings(uint64)uint64']
      argsTuple: GuessGameArgs['tuple']['claimWinnings(uint64)uint64']
      returns: GuessGameReturns['claimWinnings(uint64)uint64']
    }>
    & Record<'getRequiredMBR(string)uint64' | 'getRequiredMBR', {
      argsObj: GuessGameArgs['obj']['getRequiredMBR(string)uint64']
      argsTuple: GuessGameArgs['tuple']['getRequiredMBR(string)uint64']
      returns: GuessGameReturns['getRequiredMBR(string)uint64']
    }>
    & Record<'getGameType()byte[]' | 'getGameType', {
      argsObj: GuessGameArgs['obj']['getGameType()byte[]']
      argsTuple: GuessGameArgs['tuple']['getGameType()byte[]']
      returns: GuessGameReturns['getGameType()byte[]']
    }>
    & Record<'initialize(string)void' | 'initialize', {
      argsObj: GuessGameArgs['obj']['initialize(string)void']
      argsTuple: GuessGameArgs['tuple']['initialize(string)void']
      returns: GuessGameReturns['initialize(string)void']
    }>
  /**
   * Defines the shape of the state of the application.
   */
  state: {
    global: {
      keys: {
        /**
        * Contract type identifier (e.g., 'RPS', 'GUESS').
        Used for CLI validation.

         */
        gameType: BinaryState
        /**
         * Global counter for generating unique session IDs.
         */
        sessionIdCounter: bigint
      }
      maps: {}
    }
    box: {
      keys: {}
      maps: {
        /**
        * Stores global session statistics required to calculate the average.
        Key: SessionID

         */
        stats: Map<bigint | number, Object0B783Eb4>
        /**
        * Compressed frequency map storing player choices.
        Structure: A single byte blob of 808 bytes (101 uint64s).
        Index 'i' (0-100) holds the count of players who chose 'i'.
        Key: SessionID

         */
        frequency: Map<bigint | number, Uint8Array>
        /**
        * Storage for player commits.
        Key: Hash(sessionID + playerAddress) OR Hash(sessionID + round + playerAddress)
        Value: Hash(choice + salt) [32 bytes]

         */
        playerCommit: Map<Uint8Array | string, Uint8Array>
        /**
        * Storage for revealed choices.
        Key: Hash(sessionID + playerAddress) OR Hash(sessionID + round + playerAddress)
        Value: Player Choice [8 bytes]

         */
        playerChoice: Map<Uint8Array | string, bigint>
        /**
         * Configuration storage for each active session.
         */
        gameSessions: Map<bigint | number, ObjectB555233E>
        /**
         * Tracks the total pot (collected participation fees) for a specific session.
         */
        sessionBalances: Map<bigint | number, bigint>
      }
    }
  }
}

/**
 * Defines the possible abi call signatures.
 */
export type GuessGameSignatures = keyof GuessGameTypes['methods']
/**
 * Defines the possible abi call signatures for methods that return a non-void value.
 */
export type GuessGameNonVoidMethodSignatures = keyof GuessGameTypes['methods'] extends infer T ? T extends keyof GuessGameTypes['methods'] ? MethodReturn<T> extends void ? never : T  : never : never
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the GuessGame smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends GuessGameSignatures> = GuessGameTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the GuessGame smart contract to the method's return type
 */
export type MethodReturn<TSignature extends GuessGameSignatures> = GuessGameTypes['methods'][TSignature]['returns']

/**
 * Defines the shape of the keyed global state of the application.
 */
export type GlobalKeysState = GuessGameTypes['state']['global']['keys']

/**
 * Defines the shape of the keyed box state of the application.
 */
export type BoxKeysState = GuessGameTypes['state']['box']['keys']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the GuessGame smart contract
 */
export abstract class GuessGameParamsFactory {
  /**
   * Constructs a no op call for the createSession((uint64,uint64,uint64,uint64),pay)uint64 ABI method
   *
  * Initializes a new game session and allocates storage for counters.
  Validates that the MBR payment covers the large frequency blob.

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static createSession(params: CallParams<GuessGameArgs['obj']['createSession((uint64,uint64,uint64,uint64),pay)uint64'] | GuessGameArgs['tuple']['createSession((uint64,uint64,uint64,uint64),pay)uint64']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'createSession((uint64,uint64,uint64,uint64),pay)uint64' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.config, params.args.mbrPayment],
    }
  }
  /**
   * Constructs a no op call for the joinSession(uint64,byte[],pay)void ABI method
   *
   * Allows a player to join the session.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static joinSession(params: CallParams<GuessGameArgs['obj']['joinSession(uint64,byte[],pay)void'] | GuessGameArgs['tuple']['joinSession(uint64,byte[],pay)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'joinSession(uint64,byte[],pay)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.sessionId, params.args.commit, params.args.payment],
    }
  }
  /**
   * Constructs a no op call for the revealMove(uint64,uint64,byte[])void ABI method
   *
  * Reveals a player's move and updates global game statistics.
  Choice must be between 0 and 100.

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static revealMove(params: CallParams<GuessGameArgs['obj']['revealMove(uint64,uint64,byte[])void'] | GuessGameArgs['tuple']['revealMove(uint64,uint64,byte[])void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'revealMove(uint64,uint64,byte[])void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.sessionId, params.args.choice, params.args.salt],
    }
  }
  /**
   * Constructs a no op call for the claimWinnings(uint64)uint64 ABI method
   *
  * Calculates the winner and distributes the prize using the Pull pattern.
  Uses ensureBudget to guarantee sufficient opcodes for the calculation loop.

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static claimWinnings(params: CallParams<GuessGameArgs['obj']['claimWinnings(uint64)uint64'] | GuessGameArgs['tuple']['claimWinnings(uint64)uint64']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'claimWinnings(uint64)uint64' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.sessionId],
    }
  }
  /**
   * Constructs a no op call for the getRequiredMBR(string)uint64 ABI method
   *
   * Calculates the required Minimum Balance Requirement (MBR) for storage allocation.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static getRequiredMbr(params: CallParams<GuessGameArgs['obj']['getRequiredMBR(string)uint64'] | GuessGameArgs['tuple']['getRequiredMBR(string)uint64']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'getRequiredMBR(string)uint64' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.command],
    }
  }
  /**
   * Constructs a no op call for the getGameType()byte[] ABI method
   *
   * Returns contract identification info.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static getGameType(params: CallParams<GuessGameArgs['obj']['getGameType()byte[]'] | GuessGameArgs['tuple']['getGameType()byte[]']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'getGameType()byte[]' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the initialize(string)void ABI method
   *
  * Sets the game type. Immutable once set.
  Can only be called by the creator.

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static initialize(params: CallParams<GuessGameArgs['obj']['initialize(string)void'] | GuessGameArgs['tuple']['initialize(string)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'initialize(string)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.gameType],
    }
  }
}

/**
 * A client to make calls to the GuessGame smart contract
 */
export class GuessGameClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `GuessGameClient`
   *
   * @param appClient An `AppClient` instance which has been created with the GuessGame app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `GuessGameClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Checks for decode errors on the given return value and maps the return value to the return type for the given method
   * @returns The typed return value or undefined if there was no value
   */
  decodeReturnValue<TSignature extends GuessGameNonVoidMethodSignatures>(method: TSignature, returnValue: ABIReturn | undefined) {
    return returnValue !== undefined ? getArc56ReturnValue<MethodReturn<TSignature>>(returnValue, this.appClient.getABIMethod(method), APP_SPEC.structs) : undefined
  }

  /**
   * Returns a new `GuessGameClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<GuessGameClient> {
    return new GuessGameClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `GuessGameClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<GuessGameClient> {
    return new GuessGameClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Makes a clear_state call to an existing instance of the GuessGame smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the GuessGame smart contract using the `createSession((uint64,uint64,uint64,uint64),pay)uint64` ABI method.
     *
    * Initializes a new game session and allocates storage for counters.
    Validates that the MBR payment covers the large frequency blob.

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    createSession: (params: CallParams<GuessGameArgs['obj']['createSession((uint64,uint64,uint64,uint64),pay)uint64'] | GuessGameArgs['tuple']['createSession((uint64,uint64,uint64,uint64),pay)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(GuessGameParamsFactory.createSession(params))
    },

    /**
     * Makes a call to the GuessGame smart contract using the `joinSession(uint64,byte[],pay)void` ABI method.
     *
     * Allows a player to join the session.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    joinSession: (params: CallParams<GuessGameArgs['obj']['joinSession(uint64,byte[],pay)void'] | GuessGameArgs['tuple']['joinSession(uint64,byte[],pay)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(GuessGameParamsFactory.joinSession(params))
    },

    /**
     * Makes a call to the GuessGame smart contract using the `revealMove(uint64,uint64,byte[])void` ABI method.
     *
    * Reveals a player's move and updates global game statistics.
    Choice must be between 0 and 100.

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    revealMove: (params: CallParams<GuessGameArgs['obj']['revealMove(uint64,uint64,byte[])void'] | GuessGameArgs['tuple']['revealMove(uint64,uint64,byte[])void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(GuessGameParamsFactory.revealMove(params))
    },

    /**
     * Makes a call to the GuessGame smart contract using the `claimWinnings(uint64)uint64` ABI method.
     *
    * Calculates the winner and distributes the prize using the Pull pattern.
    Uses ensureBudget to guarantee sufficient opcodes for the calculation loop.

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    claimWinnings: (params: CallParams<GuessGameArgs['obj']['claimWinnings(uint64)uint64'] | GuessGameArgs['tuple']['claimWinnings(uint64)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(GuessGameParamsFactory.claimWinnings(params))
    },

    /**
     * Makes a call to the GuessGame smart contract using the `getRequiredMBR(string)uint64` ABI method.
     *
     * Calculates the required Minimum Balance Requirement (MBR) for storage allocation.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    getRequiredMbr: (params: CallParams<GuessGameArgs['obj']['getRequiredMBR(string)uint64'] | GuessGameArgs['tuple']['getRequiredMBR(string)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(GuessGameParamsFactory.getRequiredMbr(params))
    },

    /**
     * Makes a call to the GuessGame smart contract using the `getGameType()byte[]` ABI method.
     *
     * Returns contract identification info.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    getGameType: (params: CallParams<GuessGameArgs['obj']['getGameType()byte[]'] | GuessGameArgs['tuple']['getGameType()byte[]']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(GuessGameParamsFactory.getGameType(params))
    },

    /**
     * Makes a call to the GuessGame smart contract using the `initialize(string)void` ABI method.
     *
    * Sets the game type. Immutable once set.
    Can only be called by the creator.

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    initialize: (params: CallParams<GuessGameArgs['obj']['initialize(string)void'] | GuessGameArgs['tuple']['initialize(string)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(GuessGameParamsFactory.initialize(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Makes a clear_state call to an existing instance of the GuessGame smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the GuessGame smart contract using the `createSession((uint64,uint64,uint64,uint64),pay)uint64` ABI method.
     *
    * Initializes a new game session and allocates storage for counters.
    Validates that the MBR payment covers the large frequency blob.

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    createSession: (params: CallParams<GuessGameArgs['obj']['createSession((uint64,uint64,uint64,uint64),pay)uint64'] | GuessGameArgs['tuple']['createSession((uint64,uint64,uint64,uint64),pay)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(GuessGameParamsFactory.createSession(params))
    },

    /**
     * Makes a call to the GuessGame smart contract using the `joinSession(uint64,byte[],pay)void` ABI method.
     *
     * Allows a player to join the session.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    joinSession: (params: CallParams<GuessGameArgs['obj']['joinSession(uint64,byte[],pay)void'] | GuessGameArgs['tuple']['joinSession(uint64,byte[],pay)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(GuessGameParamsFactory.joinSession(params))
    },

    /**
     * Makes a call to the GuessGame smart contract using the `revealMove(uint64,uint64,byte[])void` ABI method.
     *
    * Reveals a player's move and updates global game statistics.
    Choice must be between 0 and 100.

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    revealMove: (params: CallParams<GuessGameArgs['obj']['revealMove(uint64,uint64,byte[])void'] | GuessGameArgs['tuple']['revealMove(uint64,uint64,byte[])void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(GuessGameParamsFactory.revealMove(params))
    },

    /**
     * Makes a call to the GuessGame smart contract using the `claimWinnings(uint64)uint64` ABI method.
     *
    * Calculates the winner and distributes the prize using the Pull pattern.
    Uses ensureBudget to guarantee sufficient opcodes for the calculation loop.

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    claimWinnings: (params: CallParams<GuessGameArgs['obj']['claimWinnings(uint64)uint64'] | GuessGameArgs['tuple']['claimWinnings(uint64)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(GuessGameParamsFactory.claimWinnings(params))
    },

    /**
     * Makes a call to the GuessGame smart contract using the `getRequiredMBR(string)uint64` ABI method.
     *
     * Calculates the required Minimum Balance Requirement (MBR) for storage allocation.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    getRequiredMbr: (params: CallParams<GuessGameArgs['obj']['getRequiredMBR(string)uint64'] | GuessGameArgs['tuple']['getRequiredMBR(string)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(GuessGameParamsFactory.getRequiredMbr(params))
    },

    /**
     * Makes a call to the GuessGame smart contract using the `getGameType()byte[]` ABI method.
     *
     * Returns contract identification info.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    getGameType: (params: CallParams<GuessGameArgs['obj']['getGameType()byte[]'] | GuessGameArgs['tuple']['getGameType()byte[]']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(GuessGameParamsFactory.getGameType(params))
    },

    /**
     * Makes a call to the GuessGame smart contract using the `initialize(string)void` ABI method.
     *
    * Sets the game type. Immutable once set.
    Can only be called by the creator.

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    initialize: (params: CallParams<GuessGameArgs['obj']['initialize(string)void'] | GuessGameArgs['tuple']['initialize(string)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(GuessGameParamsFactory.initialize(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Makes a clear_state call to an existing instance of the GuessGame smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the GuessGame smart contract using the `createSession((uint64,uint64,uint64,uint64),pay)uint64` ABI method.
     *
    * Initializes a new game session and allocates storage for counters.
    Validates that the MBR payment covers the large frequency blob.

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    createSession: async (params: CallParams<GuessGameArgs['obj']['createSession((uint64,uint64,uint64,uint64),pay)uint64'] | GuessGameArgs['tuple']['createSession((uint64,uint64,uint64,uint64),pay)uint64']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(GuessGameParamsFactory.createSession(params))
      return {...result, return: result.return as unknown as (undefined | GuessGameReturns['createSession((uint64,uint64,uint64,uint64),pay)uint64'])}
    },

    /**
     * Makes a call to the GuessGame smart contract using the `joinSession(uint64,byte[],pay)void` ABI method.
     *
     * Allows a player to join the session.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    joinSession: async (params: CallParams<GuessGameArgs['obj']['joinSession(uint64,byte[],pay)void'] | GuessGameArgs['tuple']['joinSession(uint64,byte[],pay)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(GuessGameParamsFactory.joinSession(params))
      return {...result, return: result.return as unknown as (undefined | GuessGameReturns['joinSession(uint64,byte[],pay)void'])}
    },

    /**
     * Makes a call to the GuessGame smart contract using the `revealMove(uint64,uint64,byte[])void` ABI method.
     *
    * Reveals a player's move and updates global game statistics.
    Choice must be between 0 and 100.

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    revealMove: async (params: CallParams<GuessGameArgs['obj']['revealMove(uint64,uint64,byte[])void'] | GuessGameArgs['tuple']['revealMove(uint64,uint64,byte[])void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(GuessGameParamsFactory.revealMove(params))
      return {...result, return: result.return as unknown as (undefined | GuessGameReturns['revealMove(uint64,uint64,byte[])void'])}
    },

    /**
     * Makes a call to the GuessGame smart contract using the `claimWinnings(uint64)uint64` ABI method.
     *
    * Calculates the winner and distributes the prize using the Pull pattern.
    Uses ensureBudget to guarantee sufficient opcodes for the calculation loop.

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    claimWinnings: async (params: CallParams<GuessGameArgs['obj']['claimWinnings(uint64)uint64'] | GuessGameArgs['tuple']['claimWinnings(uint64)uint64']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(GuessGameParamsFactory.claimWinnings(params))
      return {...result, return: result.return as unknown as (undefined | GuessGameReturns['claimWinnings(uint64)uint64'])}
    },

    /**
     * Makes a call to the GuessGame smart contract using the `getRequiredMBR(string)uint64` ABI method.
     *
     * Calculates the required Minimum Balance Requirement (MBR) for storage allocation.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    getRequiredMbr: async (params: CallParams<GuessGameArgs['obj']['getRequiredMBR(string)uint64'] | GuessGameArgs['tuple']['getRequiredMBR(string)uint64']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(GuessGameParamsFactory.getRequiredMbr(params))
      return {...result, return: result.return as unknown as (undefined | GuessGameReturns['getRequiredMBR(string)uint64'])}
    },

    /**
     * Makes a call to the GuessGame smart contract using the `getGameType()byte[]` ABI method.
     *
     * Returns contract identification info.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    getGameType: async (params: CallParams<GuessGameArgs['obj']['getGameType()byte[]'] | GuessGameArgs['tuple']['getGameType()byte[]']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(GuessGameParamsFactory.getGameType(params))
      return {...result, return: result.return as unknown as (undefined | GuessGameReturns['getGameType()byte[]'])}
    },

    /**
     * Makes a call to the GuessGame smart contract using the `initialize(string)void` ABI method.
     *
    * Sets the game type. Immutable once set.
    Can only be called by the creator.

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    initialize: async (params: CallParams<GuessGameArgs['obj']['initialize(string)void'] | GuessGameArgs['tuple']['initialize(string)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(GuessGameParamsFactory.initialize(params))
      return {...result, return: result.return as unknown as (undefined | GuessGameReturns['initialize(string)void'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new GuessGameClient(this.appClient.clone(params))
  }

  /**
   * Methods to access state for the current GuessGame app
   */
  state = {
    /**
     * Methods to access global state for the current GuessGame app
     */
    global: {
      /**
       * Get all current keyed values from global state
       */
      getAll: async (): Promise<Partial<Expand<GlobalKeysState>>> => {
        const result = await this.appClient.state.global.getAll()
        return {
          gameType: new BinaryStateValue(result.gameType),
          sessionIdCounter: result.sessionIDCounter,
        }
      },
      /**
       * Get the current value of the gameType key in global state
       */
      gameType: async (): Promise<BinaryState> => { return new BinaryStateValue((await this.appClient.state.global.getValue("gameType")) as Uint8Array | undefined) },
      /**
       * Get the current value of the sessionIDCounter key in global state
       */
      sessionIdCounter: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("sessionIDCounter")) as bigint | undefined },
    },
    /**
     * Methods to access box state for the current GuessGame app
     */
    box: {
      /**
       * Get all current keyed values from box state
       */
      getAll: async (): Promise<Partial<Expand<BoxKeysState>>> => {
        const result = await this.appClient.state.box.getAll()
        return {
        }
      },
      /**
       * Get values from the stats map in box state
       */
      stats: {
        /**
         * Get all current values of the stats map in box state
         */
        getMap: async (): Promise<Map<bigint, Object0B783Eb4>> => { return (await this.appClient.state.box.getMap("stats")) as Map<bigint, Object0B783Eb4> },
        /**
         * Get a current value of the stats map by key from box state
         */
        value: async (key: bigint | number): Promise<Object0B783Eb4 | undefined> => { return await this.appClient.state.box.getMapValue("stats", key) as Object0B783Eb4 | undefined },
      },
      /**
       * Get values from the frequency map in box state
       */
      frequency: {
        /**
         * Get all current values of the frequency map in box state
         */
        getMap: async (): Promise<Map<bigint, Uint8Array>> => { return (await this.appClient.state.box.getMap("frequency")) as Map<bigint, Uint8Array> },
        /**
         * Get a current value of the frequency map by key from box state
         */
        value: async (key: bigint | number): Promise<Uint8Array | undefined> => { return await this.appClient.state.box.getMapValue("frequency", key) as Uint8Array | undefined },
      },
      /**
       * Get values from the playerCommit map in box state
       */
      playerCommit: {
        /**
         * Get all current values of the playerCommit map in box state
         */
        getMap: async (): Promise<Map<Uint8Array, Uint8Array>> => { return (await this.appClient.state.box.getMap("playerCommit")) as Map<Uint8Array, Uint8Array> },
        /**
         * Get a current value of the playerCommit map by key from box state
         */
        value: async (key: Uint8Array | string): Promise<Uint8Array | undefined> => { return await this.appClient.state.box.getMapValue("playerCommit", key) as Uint8Array | undefined },
      },
      /**
       * Get values from the playerChoice map in box state
       */
      playerChoice: {
        /**
         * Get all current values of the playerChoice map in box state
         */
        getMap: async (): Promise<Map<Uint8Array, bigint>> => { return (await this.appClient.state.box.getMap("playerChoice")) as Map<Uint8Array, bigint> },
        /**
         * Get a current value of the playerChoice map by key from box state
         */
        value: async (key: Uint8Array | string): Promise<bigint | undefined> => { return await this.appClient.state.box.getMapValue("playerChoice", key) as bigint | undefined },
      },
      /**
       * Get values from the gameSessions map in box state
       */
      gameSessions: {
        /**
         * Get all current values of the gameSessions map in box state
         */
        getMap: async (): Promise<Map<bigint, ObjectB555233E>> => { return (await this.appClient.state.box.getMap("gameSessions")) as Map<bigint, ObjectB555233E> },
        /**
         * Get a current value of the gameSessions map by key from box state
         */
        value: async (key: bigint | number): Promise<ObjectB555233E | undefined> => { return await this.appClient.state.box.getMapValue("gameSessions", key) as ObjectB555233E | undefined },
      },
      /**
       * Get values from the sessionBalances map in box state
       */
      sessionBalances: {
        /**
         * Get all current values of the sessionBalances map in box state
         */
        getMap: async (): Promise<Map<bigint, bigint>> => { return (await this.appClient.state.box.getMap("sessionBalances")) as Map<bigint, bigint> },
        /**
         * Get a current value of the sessionBalances map by key from box state
         */
        value: async (key: bigint | number): Promise<bigint | undefined> => { return await this.appClient.state.box.getMapValue("sessionBalances", key) as bigint | undefined },
      },
    },
  }

  public newGroup(): GuessGameComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a createSession((uint64,uint64,uint64,uint64),pay)uint64 method call against the GuessGame contract
       */
      createSession(params: CallParams<GuessGameArgs['obj']['createSession((uint64,uint64,uint64,uint64),pay)uint64'] | GuessGameArgs['tuple']['createSession((uint64,uint64,uint64,uint64),pay)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.createSession(params)))
        resultMappers.push((v) => client.decodeReturnValue('createSession((uint64,uint64,uint64,uint64),pay)uint64', v))
        return this
      },
      /**
       * Add a joinSession(uint64,byte[],pay)void method call against the GuessGame contract
       */
      joinSession(params: CallParams<GuessGameArgs['obj']['joinSession(uint64,byte[],pay)void'] | GuessGameArgs['tuple']['joinSession(uint64,byte[],pay)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.joinSession(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a revealMove(uint64,uint64,byte[])void method call against the GuessGame contract
       */
      revealMove(params: CallParams<GuessGameArgs['obj']['revealMove(uint64,uint64,byte[])void'] | GuessGameArgs['tuple']['revealMove(uint64,uint64,byte[])void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.revealMove(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a claimWinnings(uint64)uint64 method call against the GuessGame contract
       */
      claimWinnings(params: CallParams<GuessGameArgs['obj']['claimWinnings(uint64)uint64'] | GuessGameArgs['tuple']['claimWinnings(uint64)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.claimWinnings(params)))
        resultMappers.push((v) => client.decodeReturnValue('claimWinnings(uint64)uint64', v))
        return this
      },
      /**
       * Add a getRequiredMBR(string)uint64 method call against the GuessGame contract
       */
      getRequiredMbr(params: CallParams<GuessGameArgs['obj']['getRequiredMBR(string)uint64'] | GuessGameArgs['tuple']['getRequiredMBR(string)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.getRequiredMbr(params)))
        resultMappers.push((v) => client.decodeReturnValue('getRequiredMBR(string)uint64', v))
        return this
      },
      /**
       * Add a getGameType()byte[] method call against the GuessGame contract
       */
      getGameType(params: CallParams<GuessGameArgs['obj']['getGameType()byte[]'] | GuessGameArgs['tuple']['getGameType()byte[]']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.getGameType(params)))
        resultMappers.push((v) => client.decodeReturnValue('getGameType()byte[]', v))
        return this
      },
      /**
       * Add a initialize(string)void method call against the GuessGame contract
       */
      initialize(params: CallParams<GuessGameArgs['obj']['initialize(string)void'] | GuessGameArgs['tuple']['initialize(string)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.initialize(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a clear state call to the GuessGame contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as GuessGameComposer
  }
}
export type GuessGameComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the createSession((uint64,uint64,uint64,uint64),pay)uint64 ABI method.
   *
  * Initializes a new game session and allocates storage for counters.
  Validates that the MBR payment covers the large frequency blob.

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  createSession(params?: CallParams<GuessGameArgs['obj']['createSession((uint64,uint64,uint64,uint64),pay)uint64'] | GuessGameArgs['tuple']['createSession((uint64,uint64,uint64,uint64),pay)uint64']>): GuessGameComposer<[...TReturns, GuessGameReturns['createSession((uint64,uint64,uint64,uint64),pay)uint64'] | undefined]>

  /**
   * Calls the joinSession(uint64,byte[],pay)void ABI method.
   *
   * Allows a player to join the session.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  joinSession(params?: CallParams<GuessGameArgs['obj']['joinSession(uint64,byte[],pay)void'] | GuessGameArgs['tuple']['joinSession(uint64,byte[],pay)void']>): GuessGameComposer<[...TReturns, GuessGameReturns['joinSession(uint64,byte[],pay)void'] | undefined]>

  /**
   * Calls the revealMove(uint64,uint64,byte[])void ABI method.
   *
  * Reveals a player's move and updates global game statistics.
  Choice must be between 0 and 100.

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  revealMove(params?: CallParams<GuessGameArgs['obj']['revealMove(uint64,uint64,byte[])void'] | GuessGameArgs['tuple']['revealMove(uint64,uint64,byte[])void']>): GuessGameComposer<[...TReturns, GuessGameReturns['revealMove(uint64,uint64,byte[])void'] | undefined]>

  /**
   * Calls the claimWinnings(uint64)uint64 ABI method.
   *
  * Calculates the winner and distributes the prize using the Pull pattern.
  Uses ensureBudget to guarantee sufficient opcodes for the calculation loop.

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  claimWinnings(params?: CallParams<GuessGameArgs['obj']['claimWinnings(uint64)uint64'] | GuessGameArgs['tuple']['claimWinnings(uint64)uint64']>): GuessGameComposer<[...TReturns, GuessGameReturns['claimWinnings(uint64)uint64'] | undefined]>

  /**
   * Calls the getRequiredMBR(string)uint64 ABI method.
   *
   * Calculates the required Minimum Balance Requirement (MBR) for storage allocation.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  getRequiredMbr(params?: CallParams<GuessGameArgs['obj']['getRequiredMBR(string)uint64'] | GuessGameArgs['tuple']['getRequiredMBR(string)uint64']>): GuessGameComposer<[...TReturns, GuessGameReturns['getRequiredMBR(string)uint64'] | undefined]>

  /**
   * Calls the getGameType()byte[] ABI method.
   *
   * Returns contract identification info.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  getGameType(params?: CallParams<GuessGameArgs['obj']['getGameType()byte[]'] | GuessGameArgs['tuple']['getGameType()byte[]']>): GuessGameComposer<[...TReturns, GuessGameReturns['getGameType()byte[]'] | undefined]>

  /**
   * Calls the initialize(string)void ABI method.
   *
  * Sets the game type. Immutable once set.
  Can only be called by the creator.

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  initialize(params?: CallParams<GuessGameArgs['obj']['initialize(string)void'] | GuessGameArgs['tuple']['initialize(string)void']>): GuessGameComposer<[...TReturns, GuessGameReturns['initialize(string)void'] | undefined]>

  /**
   * Makes a clear_state call to an existing instance of the GuessGame smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): GuessGameComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): GuessGameComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<GuessGameComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<GuessGameComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<GuessGameComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<GuessGameComposerResults<TReturns>>
}
export type GuessGameComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

