/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"KingdomTreasury","structs":{},"methods":[{"name":"setupKingdomTreasury","args":[{"type":"uint64","name":"projectAppId"},{"type":"uint64","name":"reserveTokenId"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}},{"name":"depositToTreasury","args":[{"type":"pay","name":"depositTxn"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}}],"arcs":[22,28],"networks":{},"state":{"schema":{"global":{"ints":5,"bytes":0},"local":{"ints":0,"bytes":0}},"keys":{"global":{"DEV_TREASURY":{"keyType":"AVMString","valueType":"AVMUint64","key":"REVWX1RSRUFTVVJZ"},"COMMUNITY_TREASURY":{"keyType":"AVMString","valueType":"AVMUint64","key":"Q09NTVVOSVRZX1RSRUFTVVJZ"},"TREASURY_ASSET_ID":{"keyType":"AVMString","valueType":"AVMUint64","key":"VFJFQVNVUllfQVNTRVRfSUQ="},"total_treasury":{"keyType":"AVMString","valueType":"AVMUint64","key":"dG90YWxfdHJlYXN1cnk="},"total_withdrawn":{"keyType":"AVMString","valueType":"AVMUint64","key":"dG90YWxfd2l0aGRyYXdu"}},"local":{},"box":{}},"maps":{"global":{},"local":{},"box":{}}},"bareActions":{"create":["NoOp"],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[221],"errorMessage":"Deposit amount must be greater than 0"},{"pc":[71,93],"errorMessage":"OnCompletion is not NoOp"},{"pc":[118],"errorMessage":"can only call when creating"},{"pc":[74,96],"errorMessage":"can only call when not creating"},{"pc":[154,225],"errorMessage":"check GlobalState exists"},{"pc":[84],"errorMessage":"transaction type is pay"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"events":[]} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


/**
 * The argument types for the KingdomTreasury contract
 */
export type KingdomTreasuryArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'setupKingdomTreasury(uint64,uint64)void': {
      projectAppId: bigint | number
      reserveTokenId: bigint | number
    }
    'depositToTreasury(pay)void': {
      depositTxn: AppMethodCallTransactionArgument
    }
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'setupKingdomTreasury(uint64,uint64)void': [projectAppId: bigint | number, reserveTokenId: bigint | number]
    'depositToTreasury(pay)void': [depositTxn: AppMethodCallTransactionArgument]
  }
}

/**
 * The return type for each method
 */
export type KingdomTreasuryReturns = {
  'setupKingdomTreasury(uint64,uint64)void': void
  'depositToTreasury(pay)void': void
}

/**
 * Defines the types of available calls and state of the KingdomTreasury smart contract.
 */
export type KingdomTreasuryTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'setupKingdomTreasury(uint64,uint64)void' | 'setupKingdomTreasury', {
      argsObj: KingdomTreasuryArgs['obj']['setupKingdomTreasury(uint64,uint64)void']
      argsTuple: KingdomTreasuryArgs['tuple']['setupKingdomTreasury(uint64,uint64)void']
      returns: KingdomTreasuryReturns['setupKingdomTreasury(uint64,uint64)void']
    }>
    & Record<'depositToTreasury(pay)void' | 'depositToTreasury', {
      argsObj: KingdomTreasuryArgs['obj']['depositToTreasury(pay)void']
      argsTuple: KingdomTreasuryArgs['tuple']['depositToTreasury(pay)void']
      returns: KingdomTreasuryReturns['depositToTreasury(pay)void']
    }>
  /**
   * Defines the shape of the state of the application.
   */
  state: {
    global: {
      keys: {
        devTreasury: bigint
        communityTreasury: bigint
        treasuryAssetId: bigint
        totalTreasury: bigint
        totalWithdrawn: bigint
      }
      maps: {}
    }
  }
}

/**
 * Defines the possible abi call signatures.
 */
export type KingdomTreasurySignatures = keyof KingdomTreasuryTypes['methods']
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the KingdomTreasury smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends KingdomTreasurySignatures> = KingdomTreasuryTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the KingdomTreasury smart contract to the method's return type
 */
export type MethodReturn<TSignature extends KingdomTreasurySignatures> = KingdomTreasuryTypes['methods'][TSignature]['returns']

/**
 * Defines the shape of the keyed global state of the application.
 */
export type GlobalKeysState = KingdomTreasuryTypes['state']['global']['keys']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the KingdomTreasury smart contract
 */
export abstract class KingdomTreasuryParamsFactory {
  /**
   * Constructs a no op call for the setupKingdomTreasury(uint64,uint64)void ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static setupKingdomTreasury(params: CallParams<KingdomTreasuryArgs['obj']['setupKingdomTreasury(uint64,uint64)void'] | KingdomTreasuryArgs['tuple']['setupKingdomTreasury(uint64,uint64)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'setupKingdomTreasury(uint64,uint64)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.projectAppId, params.args.reserveTokenId],
    }
  }
  /**
   * Constructs a no op call for the depositToTreasury(pay)void ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static depositToTreasury(params: CallParams<KingdomTreasuryArgs['obj']['depositToTreasury(pay)void'] | KingdomTreasuryArgs['tuple']['depositToTreasury(pay)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'depositToTreasury(pay)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.depositTxn],
    }
  }
}

/**
 * A client to make calls to the KingdomTreasury smart contract
 */
export class KingdomTreasuryClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `KingdomTreasuryClient`
   *
   * @param appClient An `AppClient` instance which has been created with the KingdomTreasury app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `KingdomTreasuryClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Returns a new `KingdomTreasuryClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<KingdomTreasuryClient> {
    return new KingdomTreasuryClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `KingdomTreasuryClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<KingdomTreasuryClient> {
    return new KingdomTreasuryClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Makes a clear_state call to an existing instance of the KingdomTreasury smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the KingdomTreasury smart contract using the `setupKingdomTreasury(uint64,uint64)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    setupKingdomTreasury: (params: CallParams<KingdomTreasuryArgs['obj']['setupKingdomTreasury(uint64,uint64)void'] | KingdomTreasuryArgs['tuple']['setupKingdomTreasury(uint64,uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(KingdomTreasuryParamsFactory.setupKingdomTreasury(params))
    },

    /**
     * Makes a call to the KingdomTreasury smart contract using the `depositToTreasury(pay)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    depositToTreasury: (params: CallParams<KingdomTreasuryArgs['obj']['depositToTreasury(pay)void'] | KingdomTreasuryArgs['tuple']['depositToTreasury(pay)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(KingdomTreasuryParamsFactory.depositToTreasury(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Makes a clear_state call to an existing instance of the KingdomTreasury smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the KingdomTreasury smart contract using the `setupKingdomTreasury(uint64,uint64)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    setupKingdomTreasury: (params: CallParams<KingdomTreasuryArgs['obj']['setupKingdomTreasury(uint64,uint64)void'] | KingdomTreasuryArgs['tuple']['setupKingdomTreasury(uint64,uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(KingdomTreasuryParamsFactory.setupKingdomTreasury(params))
    },

    /**
     * Makes a call to the KingdomTreasury smart contract using the `depositToTreasury(pay)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    depositToTreasury: (params: CallParams<KingdomTreasuryArgs['obj']['depositToTreasury(pay)void'] | KingdomTreasuryArgs['tuple']['depositToTreasury(pay)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(KingdomTreasuryParamsFactory.depositToTreasury(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Makes a clear_state call to an existing instance of the KingdomTreasury smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the KingdomTreasury smart contract using the `setupKingdomTreasury(uint64,uint64)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    setupKingdomTreasury: async (params: CallParams<KingdomTreasuryArgs['obj']['setupKingdomTreasury(uint64,uint64)void'] | KingdomTreasuryArgs['tuple']['setupKingdomTreasury(uint64,uint64)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(KingdomTreasuryParamsFactory.setupKingdomTreasury(params))
      return {...result, return: result.return as unknown as (undefined | KingdomTreasuryReturns['setupKingdomTreasury(uint64,uint64)void'])}
    },

    /**
     * Makes a call to the KingdomTreasury smart contract using the `depositToTreasury(pay)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    depositToTreasury: async (params: CallParams<KingdomTreasuryArgs['obj']['depositToTreasury(pay)void'] | KingdomTreasuryArgs['tuple']['depositToTreasury(pay)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(KingdomTreasuryParamsFactory.depositToTreasury(params))
      return {...result, return: result.return as unknown as (undefined | KingdomTreasuryReturns['depositToTreasury(pay)void'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new KingdomTreasuryClient(this.appClient.clone(params))
  }

  /**
   * Methods to access state for the current KingdomTreasury app
   */
  state = {
    /**
     * Methods to access global state for the current KingdomTreasury app
     */
    global: {
      /**
       * Get all current keyed values from global state
       */
      getAll: async (): Promise<Partial<Expand<GlobalKeysState>>> => {
        const result = await this.appClient.state.global.getAll()
        return {
          devTreasury: result.DEV_TREASURY,
          communityTreasury: result.COMMUNITY_TREASURY,
          treasuryAssetId: result.TREASURY_ASSET_ID,
          totalTreasury: result.total_treasury,
          totalWithdrawn: result.total_withdrawn,
        }
      },
      /**
       * Get the current value of the DEV_TREASURY key in global state
       */
      devTreasury: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("DEV_TREASURY")) as bigint | undefined },
      /**
       * Get the current value of the COMMUNITY_TREASURY key in global state
       */
      communityTreasury: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("COMMUNITY_TREASURY")) as bigint | undefined },
      /**
       * Get the current value of the TREASURY_ASSET_ID key in global state
       */
      treasuryAssetId: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("TREASURY_ASSET_ID")) as bigint | undefined },
      /**
       * Get the current value of the total_treasury key in global state
       */
      totalTreasury: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("total_treasury")) as bigint | undefined },
      /**
       * Get the current value of the total_withdrawn key in global state
       */
      totalWithdrawn: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("total_withdrawn")) as bigint | undefined },
    },
  }

  public newGroup(): KingdomTreasuryComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a setupKingdomTreasury(uint64,uint64)void method call against the KingdomTreasury contract
       */
      setupKingdomTreasury(params: CallParams<KingdomTreasuryArgs['obj']['setupKingdomTreasury(uint64,uint64)void'] | KingdomTreasuryArgs['tuple']['setupKingdomTreasury(uint64,uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.setupKingdomTreasury(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a depositToTreasury(pay)void method call against the KingdomTreasury contract
       */
      depositToTreasury(params: CallParams<KingdomTreasuryArgs['obj']['depositToTreasury(pay)void'] | KingdomTreasuryArgs['tuple']['depositToTreasury(pay)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.depositToTreasury(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a clear state call to the KingdomTreasury contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as KingdomTreasuryComposer
  }
}
export type KingdomTreasuryComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the setupKingdomTreasury(uint64,uint64)void ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  setupKingdomTreasury(params?: CallParams<KingdomTreasuryArgs['obj']['setupKingdomTreasury(uint64,uint64)void'] | KingdomTreasuryArgs['tuple']['setupKingdomTreasury(uint64,uint64)void']>): KingdomTreasuryComposer<[...TReturns, KingdomTreasuryReturns['setupKingdomTreasury(uint64,uint64)void'] | undefined]>

  /**
   * Calls the depositToTreasury(pay)void ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  depositToTreasury(params?: CallParams<KingdomTreasuryArgs['obj']['depositToTreasury(pay)void'] | KingdomTreasuryArgs['tuple']['depositToTreasury(pay)void']>): KingdomTreasuryComposer<[...TReturns, KingdomTreasuryReturns['depositToTreasury(pay)void'] | undefined]>

  /**
   * Makes a clear_state call to an existing instance of the KingdomTreasury smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): KingdomTreasuryComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): KingdomTreasuryComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<KingdomTreasuryComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<KingdomTreasuryComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<KingdomTreasuryComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<KingdomTreasuryComposerResults<TReturns>>
}
export type KingdomTreasuryComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

