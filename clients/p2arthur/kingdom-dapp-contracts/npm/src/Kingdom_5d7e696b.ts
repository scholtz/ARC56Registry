/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"Kingdom","structs":{"CitizenType":[{"name":"citizenContribution","type":"uint64"}]},"methods":[{"name":"createKingdom","args":[{"type":"string","name":"kingdomName","desc":"The name of the kingdom"},{"type":"string","name":"kingdomDescription"},{"type":"string","name":"tokenName"},{"type":"string","name":"tokenSymbol"},{"type":"string","name":"kingdomImage"},{"type":"string","name":"kingdomPrmaryColor"},{"type":"string","name":"kingdomSecondaryColor"},{"type":"string","name":"kingdomBackgroundColor"},{"type":"string","name":"kingdomAccentColor"},{"type":"string","name":"kingdomTextColor"},{"type":"uint64","name":"repoId"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}},{"name":"claimNominationTokens","args":[],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"This method is used to claim the nomination tokens.\nIt checks if the sender is the king of the kingdom.","events":[],"recommendations":{}}],"arcs":[22,28],"networks":{},"state":{"schema":{"global":{"ints":4,"bytes":9},"local":{"ints":0,"bytes":1}},"keys":{"global":{"MAX_TOKEN_SUPPLY":{"keyType":"AVMString","valueType":"AVMUint64","key":"TUFYX1RPS0VOX1NVUFBMWQ=="},"REALM_APP_ID":{"keyType":"AVMString","valueType":"AVMUint64","key":"UkVBTE1fQVBQX0lE"},"kingdom_coin":{"keyType":"AVMString","valueType":"AVMUint64","key":"a2luZ2RvbV9jb2lu"},"kingdom_name":{"keyType":"AVMString","valueType":"string","key":"a2luZ2RvbV9uYW1l"},"king_address":{"keyType":"AVMString","valueType":"address","key":"a2luZ19hZGRyZXNz"},"kingdom_image_url":{"keyType":"AVMString","valueType":"string","key":"a2luZ2RvbV9pbWFnZV91cmw="},"total_funded":{"keyType":"AVMString","valueType":"AVMUint64","key":"dG90YWxfZnVuZGVk"},"kingdom_description":{"keyType":"AVMString","valueType":"string","key":"a2luZ2RvbV9kZXNjcmlwdGlvbg=="},"kingdom_primary_color":{"keyType":"AVMString","valueType":"string","key":"a2luZ2RvbV9wcmltYXJ5X2NvbG9y"},"kingdom_secondary_color":{"keyType":"AVMString","valueType":"string","key":"a2luZ2RvbV9zZWNvbmRhcnlfY29sb3I="},"kingdom_background_color":{"keyType":"AVMString","valueType":"string","key":"a2luZ2RvbV9iYWNrZ3JvdW5kX2NvbG9y"},"kingdom_accent_color":{"keyType":"AVMString","valueType":"string","key":"a2luZ2RvbV9hY2NlbnRfY29sb3I="},"kingdom_text_color":{"keyType":"AVMString","valueType":"string","key":"a2luZ2RvbV90ZXh0X2NvbG9y"}},"local":{"citizen":{"keyType":"AVMString","valueType":"CitizenType","key":"Y2l0aXplbg=="}},"box":{}},"maps":{"global":{},"local":{},"box":{}}},"bareActions":{"create":["NoOp"],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[94,106],"errorMessage":"OnCompletion is not NoOp"},{"pc":[456,476],"errorMessage":"Only citizens can perform this action"},{"pc":[464],"errorMessage":"Only the king can perform this action"},{"pc":[157],"errorMessage":"can only call when creating"},{"pc":[97,109],"errorMessage":"can only call when not creating"},{"pc":[394,401,462,471],"errorMessage":"check GlobalState exists"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"events":[]} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


// Type definitions for ARC-56 structs

export type CitizenType = {
  citizenContribution: bigint
}


/**
 * Converts the ABI tuple representation of a CitizenType to the struct representation
 */
export function CitizenTypeFromTuple(abiTuple: [bigint]) {
  return getABIStructFromABITuple(abiTuple, APP_SPEC.structs.CitizenType, APP_SPEC.structs) as CitizenType
}

/**
 * The argument types for the Kingdom contract
 */
export type KingdomArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'createKingdom(string,string,string,string,string,string,string,string,string,string,uint64)void': {
      /**
       * The name of the kingdom
       */
      kingdomName: string
      kingdomDescription: string
      tokenName: string
      tokenSymbol: string
      kingdomImage: string
      kingdomPrmaryColor: string
      kingdomSecondaryColor: string
      kingdomBackgroundColor: string
      kingdomAccentColor: string
      kingdomTextColor: string
      repoId: bigint | number
    }
    'claimNominationTokens()void': Record<string, never>
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'createKingdom(string,string,string,string,string,string,string,string,string,string,uint64)void': [kingdomName: string, kingdomDescription: string, tokenName: string, tokenSymbol: string, kingdomImage: string, kingdomPrmaryColor: string, kingdomSecondaryColor: string, kingdomBackgroundColor: string, kingdomAccentColor: string, kingdomTextColor: string, repoId: bigint | number]
    'claimNominationTokens()void': []
  }
}

/**
 * The return type for each method
 */
export type KingdomReturns = {
  'createKingdom(string,string,string,string,string,string,string,string,string,string,uint64)void': void
  'claimNominationTokens()void': void
}

/**
 * Defines the types of available calls and state of the Kingdom smart contract.
 */
export type KingdomTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'createKingdom(string,string,string,string,string,string,string,string,string,string,uint64)void' | 'createKingdom', {
      argsObj: KingdomArgs['obj']['createKingdom(string,string,string,string,string,string,string,string,string,string,uint64)void']
      argsTuple: KingdomArgs['tuple']['createKingdom(string,string,string,string,string,string,string,string,string,string,uint64)void']
      returns: KingdomReturns['createKingdom(string,string,string,string,string,string,string,string,string,string,uint64)void']
    }>
    & Record<'claimNominationTokens()void' | 'claimNominationTokens', {
      argsObj: KingdomArgs['obj']['claimNominationTokens()void']
      argsTuple: KingdomArgs['tuple']['claimNominationTokens()void']
      returns: KingdomReturns['claimNominationTokens()void']
    }>
  /**
   * Defines the shape of the state of the application.
   */
  state: {
    global: {
      keys: {
        maxTokenSupply: bigint
        realmAppId: bigint
        kingdomCoin: bigint
        kingdomName: string
        kingAddress: string
        kingdomImageUrl: string
        totalFunded: bigint
        kingdomDescription: string
        kingdomPrimaryColor: string
        kingdomSecondaryColor: string
        kingdomBackgroundColor: string
        kingdomAccentColor: string
        kingdomTextColor: string
      }
      maps: {}
    }
    local: {
      keys: {
        citizen: CitizenType
      }
      maps: {}
    }
  }
}

/**
 * Defines the possible abi call signatures.
 */
export type KingdomSignatures = keyof KingdomTypes['methods']
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the Kingdom smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends KingdomSignatures> = KingdomTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the Kingdom smart contract to the method's return type
 */
export type MethodReturn<TSignature extends KingdomSignatures> = KingdomTypes['methods'][TSignature]['returns']

/**
 * Defines the shape of the keyed global state of the application.
 */
export type GlobalKeysState = KingdomTypes['state']['global']['keys']

/**
 * Defines the shape of the keyed local state of the application.
 */
export type LocalKeysState = KingdomTypes['state']['local']['keys']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the Kingdom smart contract
 */
export abstract class KingdomParamsFactory {
  /**
   * Constructs a no op call for the createKingdom(string,string,string,string,string,string,string,string,string,string,uint64)void ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static createKingdom(params: CallParams<KingdomArgs['obj']['createKingdom(string,string,string,string,string,string,string,string,string,string,uint64)void'] | KingdomArgs['tuple']['createKingdom(string,string,string,string,string,string,string,string,string,string,uint64)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'createKingdom(string,string,string,string,string,string,string,string,string,string,uint64)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.kingdomName, params.args.kingdomDescription, params.args.tokenName, params.args.tokenSymbol, params.args.kingdomImage, params.args.kingdomPrmaryColor, params.args.kingdomSecondaryColor, params.args.kingdomBackgroundColor, params.args.kingdomAccentColor, params.args.kingdomTextColor, params.args.repoId],
    }
  }
  /**
   * Constructs a no op call for the claimNominationTokens()void ABI method
   *
  * This method is used to claim the nomination tokens.
  It checks if the sender is the king of the kingdom.

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static claimNominationTokens(params: CallParams<KingdomArgs['obj']['claimNominationTokens()void'] | KingdomArgs['tuple']['claimNominationTokens()void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'claimNominationTokens()void' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
}

/**
 * A client to make calls to the Kingdom smart contract
 */
export class KingdomClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `KingdomClient`
   *
   * @param appClient An `AppClient` instance which has been created with the Kingdom app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `KingdomClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Returns a new `KingdomClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<KingdomClient> {
    return new KingdomClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `KingdomClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<KingdomClient> {
    return new KingdomClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Makes a clear_state call to an existing instance of the Kingdom smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the Kingdom smart contract using the `createKingdom(string,string,string,string,string,string,string,string,string,string,uint64)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    createKingdom: (params: CallParams<KingdomArgs['obj']['createKingdom(string,string,string,string,string,string,string,string,string,string,uint64)void'] | KingdomArgs['tuple']['createKingdom(string,string,string,string,string,string,string,string,string,string,uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(KingdomParamsFactory.createKingdom(params))
    },

    /**
     * Makes a call to the Kingdom smart contract using the `claimNominationTokens()void` ABI method.
     *
    * This method is used to claim the nomination tokens.
    It checks if the sender is the king of the kingdom.

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    claimNominationTokens: (params: CallParams<KingdomArgs['obj']['claimNominationTokens()void'] | KingdomArgs['tuple']['claimNominationTokens()void']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(KingdomParamsFactory.claimNominationTokens(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Makes a clear_state call to an existing instance of the Kingdom smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the Kingdom smart contract using the `createKingdom(string,string,string,string,string,string,string,string,string,string,uint64)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    createKingdom: (params: CallParams<KingdomArgs['obj']['createKingdom(string,string,string,string,string,string,string,string,string,string,uint64)void'] | KingdomArgs['tuple']['createKingdom(string,string,string,string,string,string,string,string,string,string,uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(KingdomParamsFactory.createKingdom(params))
    },

    /**
     * Makes a call to the Kingdom smart contract using the `claimNominationTokens()void` ABI method.
     *
    * This method is used to claim the nomination tokens.
    It checks if the sender is the king of the kingdom.

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    claimNominationTokens: (params: CallParams<KingdomArgs['obj']['claimNominationTokens()void'] | KingdomArgs['tuple']['claimNominationTokens()void']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(KingdomParamsFactory.claimNominationTokens(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Makes a clear_state call to an existing instance of the Kingdom smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the Kingdom smart contract using the `createKingdom(string,string,string,string,string,string,string,string,string,string,uint64)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    createKingdom: async (params: CallParams<KingdomArgs['obj']['createKingdom(string,string,string,string,string,string,string,string,string,string,uint64)void'] | KingdomArgs['tuple']['createKingdom(string,string,string,string,string,string,string,string,string,string,uint64)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(KingdomParamsFactory.createKingdom(params))
      return {...result, return: result.return as unknown as (undefined | KingdomReturns['createKingdom(string,string,string,string,string,string,string,string,string,string,uint64)void'])}
    },

    /**
     * Makes a call to the Kingdom smart contract using the `claimNominationTokens()void` ABI method.
     *
    * This method is used to claim the nomination tokens.
    It checks if the sender is the king of the kingdom.

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    claimNominationTokens: async (params: CallParams<KingdomArgs['obj']['claimNominationTokens()void'] | KingdomArgs['tuple']['claimNominationTokens()void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(KingdomParamsFactory.claimNominationTokens(params))
      return {...result, return: result.return as unknown as (undefined | KingdomReturns['claimNominationTokens()void'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new KingdomClient(this.appClient.clone(params))
  }

  /**
   * Methods to access state for the current Kingdom app
   */
  state = {
    /**
     * Methods to access global state for the current Kingdom app
     */
    global: {
      /**
       * Get all current keyed values from global state
       */
      getAll: async (): Promise<Partial<Expand<GlobalKeysState>>> => {
        const result = await this.appClient.state.global.getAll()
        return {
          maxTokenSupply: result.MAX_TOKEN_SUPPLY,
          realmAppId: result.REALM_APP_ID,
          kingdomCoin: result.kingdom_coin,
          kingdomName: result.kingdom_name,
          kingAddress: result.king_address,
          kingdomImageUrl: result.kingdom_image_url,
          totalFunded: result.total_funded,
          kingdomDescription: result.kingdom_description,
          kingdomPrimaryColor: result.kingdom_primary_color,
          kingdomSecondaryColor: result.kingdom_secondary_color,
          kingdomBackgroundColor: result.kingdom_background_color,
          kingdomAccentColor: result.kingdom_accent_color,
          kingdomTextColor: result.kingdom_text_color,
        }
      },
      /**
       * Get the current value of the MAX_TOKEN_SUPPLY key in global state
       */
      maxTokenSupply: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("MAX_TOKEN_SUPPLY")) as bigint | undefined },
      /**
       * Get the current value of the REALM_APP_ID key in global state
       */
      realmAppId: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("REALM_APP_ID")) as bigint | undefined },
      /**
       * Get the current value of the kingdom_coin key in global state
       */
      kingdomCoin: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("kingdom_coin")) as bigint | undefined },
      /**
       * Get the current value of the kingdom_name key in global state
       */
      kingdomName: async (): Promise<string | undefined> => { return (await this.appClient.state.global.getValue("kingdom_name")) as string | undefined },
      /**
       * Get the current value of the king_address key in global state
       */
      kingAddress: async (): Promise<string | undefined> => { return (await this.appClient.state.global.getValue("king_address")) as string | undefined },
      /**
       * Get the current value of the kingdom_image_url key in global state
       */
      kingdomImageUrl: async (): Promise<string | undefined> => { return (await this.appClient.state.global.getValue("kingdom_image_url")) as string | undefined },
      /**
       * Get the current value of the total_funded key in global state
       */
      totalFunded: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("total_funded")) as bigint | undefined },
      /**
       * Get the current value of the kingdom_description key in global state
       */
      kingdomDescription: async (): Promise<string | undefined> => { return (await this.appClient.state.global.getValue("kingdom_description")) as string | undefined },
      /**
       * Get the current value of the kingdom_primary_color key in global state
       */
      kingdomPrimaryColor: async (): Promise<string | undefined> => { return (await this.appClient.state.global.getValue("kingdom_primary_color")) as string | undefined },
      /**
       * Get the current value of the kingdom_secondary_color key in global state
       */
      kingdomSecondaryColor: async (): Promise<string | undefined> => { return (await this.appClient.state.global.getValue("kingdom_secondary_color")) as string | undefined },
      /**
       * Get the current value of the kingdom_background_color key in global state
       */
      kingdomBackgroundColor: async (): Promise<string | undefined> => { return (await this.appClient.state.global.getValue("kingdom_background_color")) as string | undefined },
      /**
       * Get the current value of the kingdom_accent_color key in global state
       */
      kingdomAccentColor: async (): Promise<string | undefined> => { return (await this.appClient.state.global.getValue("kingdom_accent_color")) as string | undefined },
      /**
       * Get the current value of the kingdom_text_color key in global state
       */
      kingdomTextColor: async (): Promise<string | undefined> => { return (await this.appClient.state.global.getValue("kingdom_text_color")) as string | undefined },
    },
    /**
     * Methods to access local state for the current Kingdom app
     */
    local: (address: string | Address) => {
      const encodedAddress = typeof address === 'string' ? address : encodeAddress(address.publicKey)
      return {
        /**
         * Get all current keyed values from local state
         */
        getAll: async (): Promise<Partial<Expand<LocalKeysState>>> => {
          const result = await this.appClient.state.local(encodedAddress).getAll()
          return {
            citizen: result.citizen,
          }
        },
        /**
         * Get the current value of the citizen key in local state
         */
        citizen: async (): Promise<CitizenType | undefined> => { return (await this.appClient.state.local(encodedAddress).getValue("citizen")) as CitizenType | undefined },
      }
    },
  }

  public newGroup(): KingdomComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a createKingdom(string,string,string,string,string,string,string,string,string,string,uint64)void method call against the Kingdom contract
       */
      createKingdom(params: CallParams<KingdomArgs['obj']['createKingdom(string,string,string,string,string,string,string,string,string,string,uint64)void'] | KingdomArgs['tuple']['createKingdom(string,string,string,string,string,string,string,string,string,string,uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.createKingdom(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a claimNominationTokens()void method call against the Kingdom contract
       */
      claimNominationTokens(params: CallParams<KingdomArgs['obj']['claimNominationTokens()void'] | KingdomArgs['tuple']['claimNominationTokens()void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.claimNominationTokens(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a clear state call to the Kingdom contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as KingdomComposer
  }
}
export type KingdomComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the createKingdom(string,string,string,string,string,string,string,string,string,string,uint64)void ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  createKingdom(params?: CallParams<KingdomArgs['obj']['createKingdom(string,string,string,string,string,string,string,string,string,string,uint64)void'] | KingdomArgs['tuple']['createKingdom(string,string,string,string,string,string,string,string,string,string,uint64)void']>): KingdomComposer<[...TReturns, KingdomReturns['createKingdom(string,string,string,string,string,string,string,string,string,string,uint64)void'] | undefined]>

  /**
   * Calls the claimNominationTokens()void ABI method.
   *
  * This method is used to claim the nomination tokens.
  It checks if the sender is the king of the kingdom.

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  claimNominationTokens(params?: CallParams<KingdomArgs['obj']['claimNominationTokens()void'] | KingdomArgs['tuple']['claimNominationTokens()void']>): KingdomComposer<[...TReturns, KingdomReturns['claimNominationTokens()void'] | undefined]>

  /**
   * Makes a clear_state call to an existing instance of the Kingdom smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): KingdomComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): KingdomComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<KingdomComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<KingdomComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<KingdomComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<KingdomComposerResults<TReturns>>
}
export type KingdomComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

