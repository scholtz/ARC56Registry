/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"SourceFactory","structs":{"BountyDataType":[{"name":"bountyTotalValue","type":"uint64"},{"name":"bountyPaid","type":"bool"},{"name":"bountyWinner","type":"address"}],"BountyIdType":[{"name":"bountyId","type":"uint64"}]},"methods":[{"name":"bootstrap","args":[],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}},{"name":"create_bounty","args":[{"type":"uint64","name":"bounty_id"},{"type":"uint64","name":"bounty_total_value"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}},{"name":"withdraw_bounty","args":[{"type":"uint64","name":"bounty_id"},{"type":"address","name":"winner"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}}],"arcs":[22,28],"networks":{},"state":{"schema":{"global":{"ints":1,"bytes":1},"local":{"ints":0,"bytes":0}},"keys":{"global":{"MANAGER_ADDRESS":{"keyType":"AVMString","valueType":"address","key":"TUFOQUdFUl9BRERSRVNT"},"TOTAL_BOUNTIES":{"keyType":"AVMString","valueType":"AVMUint64","key":"VE9UQUxfQk9VTlRJRVM="}},"local":{},"box":{}},"maps":{"global":{},"local":{},"box":{"bounties":{"keyType":"BountyIdType","valueType":"BountyDataType","prefix":"Yl9f"}}}},"bareActions":{"create":["NoOp"],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[110],"errorMessage":"Already bootstrapped"},{"pc":[145],"errorMessage":"Bounty already exists"},{"pc":[220],"errorMessage":"Bounty already paid"},{"pc":[204],"errorMessage":"Bounty not found"},{"pc":[135,225],"errorMessage":"Bounty value must be > 0"},{"pc":[186],"errorMessage":"Contract not bootstrapped"},{"pc":[232],"errorMessage":"Insufficient app balance"},{"pc":[61],"errorMessage":"OnCompletion must be NoOp"},{"pc":[101],"errorMessage":"OnCompletion must be NoOp && can only call when creating"},{"pc":[190],"errorMessage":"Only manager"},{"pc":[195],"errorMessage":"Winner required"},{"pc":[158],"errorMessage":"check GlobalState exists"},{"pc":[182],"errorMessage":"invalid number of bytes for arc4.static_array<arc4.uint8, 32>"},{"pc":[124,132,173],"errorMessage":"invalid number of bytes for arc4.uint64"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"events":[]} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


// Type definitions for ARC-56 structs

export type BountyDataType = {
  bountyTotalValue: bigint,
  bountyPaid: boolean,
  bountyWinner: string
}


/**
 * Converts the ABI tuple representation of a BountyDataType to the struct representation
 */
export function BountyDataTypeFromTuple(abiTuple: [bigint, boolean, string]) {
  return getABIStructFromABITuple(abiTuple, APP_SPEC.structs.BountyDataType, APP_SPEC.structs) as BountyDataType
}

export type BountyIdType = {
  bountyId: bigint
}


/**
 * Converts the ABI tuple representation of a BountyIdType to the struct representation
 */
export function BountyIdTypeFromTuple(abiTuple: [bigint]) {
  return getABIStructFromABITuple(abiTuple, APP_SPEC.structs.BountyIdType, APP_SPEC.structs) as BountyIdType
}

/**
 * The argument types for the SourceFactory contract
 */
export type SourceFactoryArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'bootstrap()void': Record<string, never>
    'create_bounty(uint64,uint64)void': {
      bountyId: bigint | number
      bountyTotalValue: bigint | number
    }
    'withdraw_bounty(uint64,address)void': {
      bountyId: bigint | number
      winner: string
    }
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'bootstrap()void': []
    'create_bounty(uint64,uint64)void': [bountyId: bigint | number, bountyTotalValue: bigint | number]
    'withdraw_bounty(uint64,address)void': [bountyId: bigint | number, winner: string]
  }
}

/**
 * The return type for each method
 */
export type SourceFactoryReturns = {
  'bootstrap()void': void
  'create_bounty(uint64,uint64)void': void
  'withdraw_bounty(uint64,address)void': void
}

/**
 * Defines the types of available calls and state of the SourceFactory smart contract.
 */
export type SourceFactoryTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'bootstrap()void' | 'bootstrap', {
      argsObj: SourceFactoryArgs['obj']['bootstrap()void']
      argsTuple: SourceFactoryArgs['tuple']['bootstrap()void']
      returns: SourceFactoryReturns['bootstrap()void']
    }>
    & Record<'create_bounty(uint64,uint64)void' | 'create_bounty', {
      argsObj: SourceFactoryArgs['obj']['create_bounty(uint64,uint64)void']
      argsTuple: SourceFactoryArgs['tuple']['create_bounty(uint64,uint64)void']
      returns: SourceFactoryReturns['create_bounty(uint64,uint64)void']
    }>
    & Record<'withdraw_bounty(uint64,address)void' | 'withdraw_bounty', {
      argsObj: SourceFactoryArgs['obj']['withdraw_bounty(uint64,address)void']
      argsTuple: SourceFactoryArgs['tuple']['withdraw_bounty(uint64,address)void']
      returns: SourceFactoryReturns['withdraw_bounty(uint64,address)void']
    }>
  /**
   * Defines the shape of the state of the application.
   */
  state: {
    global: {
      keys: {
        managerAddress: string
        totalBounties: bigint
      }
      maps: {}
    }
    box: {
      keys: {}
      maps: {
        bounties: Map<BountyIdType, BountyDataType>
      }
    }
  }
}

/**
 * Defines the possible abi call signatures.
 */
export type SourceFactorySignatures = keyof SourceFactoryTypes['methods']
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the SourceFactory smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends SourceFactorySignatures> = SourceFactoryTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the SourceFactory smart contract to the method's return type
 */
export type MethodReturn<TSignature extends SourceFactorySignatures> = SourceFactoryTypes['methods'][TSignature]['returns']

/**
 * Defines the shape of the keyed global state of the application.
 */
export type GlobalKeysState = SourceFactoryTypes['state']['global']['keys']

/**
 * Defines the shape of the keyed box state of the application.
 */
export type BoxKeysState = SourceFactoryTypes['state']['box']['keys']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the SourceFactory smart contract
 */
export abstract class SourceFactoryParamsFactory {
  /**
   * Constructs a no op call for the bootstrap()void ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static bootstrap(params: CallParams<SourceFactoryArgs['obj']['bootstrap()void'] | SourceFactoryArgs['tuple']['bootstrap()void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'bootstrap()void' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the create_bounty(uint64,uint64)void ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static createBounty(params: CallParams<SourceFactoryArgs['obj']['create_bounty(uint64,uint64)void'] | SourceFactoryArgs['tuple']['create_bounty(uint64,uint64)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'create_bounty(uint64,uint64)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.bountyId, params.args.bountyTotalValue],
    }
  }
  /**
   * Constructs a no op call for the withdraw_bounty(uint64,address)void ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static withdrawBounty(params: CallParams<SourceFactoryArgs['obj']['withdraw_bounty(uint64,address)void'] | SourceFactoryArgs['tuple']['withdraw_bounty(uint64,address)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'withdraw_bounty(uint64,address)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.bountyId, params.args.winner],
    }
  }
}

/**
 * A client to make calls to the SourceFactory smart contract
 */
export class SourceFactoryClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `SourceFactoryClient`
   *
   * @param appClient An `AppClient` instance which has been created with the SourceFactory app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `SourceFactoryClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Returns a new `SourceFactoryClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<SourceFactoryClient> {
    return new SourceFactoryClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `SourceFactoryClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<SourceFactoryClient> {
    return new SourceFactoryClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Makes a clear_state call to an existing instance of the SourceFactory smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the SourceFactory smart contract using the `bootstrap()void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    bootstrap: (params: CallParams<SourceFactoryArgs['obj']['bootstrap()void'] | SourceFactoryArgs['tuple']['bootstrap()void']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(SourceFactoryParamsFactory.bootstrap(params))
    },

    /**
     * Makes a call to the SourceFactory smart contract using the `create_bounty(uint64,uint64)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    createBounty: (params: CallParams<SourceFactoryArgs['obj']['create_bounty(uint64,uint64)void'] | SourceFactoryArgs['tuple']['create_bounty(uint64,uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(SourceFactoryParamsFactory.createBounty(params))
    },

    /**
     * Makes a call to the SourceFactory smart contract using the `withdraw_bounty(uint64,address)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    withdrawBounty: (params: CallParams<SourceFactoryArgs['obj']['withdraw_bounty(uint64,address)void'] | SourceFactoryArgs['tuple']['withdraw_bounty(uint64,address)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(SourceFactoryParamsFactory.withdrawBounty(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Makes a clear_state call to an existing instance of the SourceFactory smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the SourceFactory smart contract using the `bootstrap()void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    bootstrap: (params: CallParams<SourceFactoryArgs['obj']['bootstrap()void'] | SourceFactoryArgs['tuple']['bootstrap()void']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(SourceFactoryParamsFactory.bootstrap(params))
    },

    /**
     * Makes a call to the SourceFactory smart contract using the `create_bounty(uint64,uint64)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    createBounty: (params: CallParams<SourceFactoryArgs['obj']['create_bounty(uint64,uint64)void'] | SourceFactoryArgs['tuple']['create_bounty(uint64,uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(SourceFactoryParamsFactory.createBounty(params))
    },

    /**
     * Makes a call to the SourceFactory smart contract using the `withdraw_bounty(uint64,address)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    withdrawBounty: (params: CallParams<SourceFactoryArgs['obj']['withdraw_bounty(uint64,address)void'] | SourceFactoryArgs['tuple']['withdraw_bounty(uint64,address)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(SourceFactoryParamsFactory.withdrawBounty(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Makes a clear_state call to an existing instance of the SourceFactory smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the SourceFactory smart contract using the `bootstrap()void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    bootstrap: async (params: CallParams<SourceFactoryArgs['obj']['bootstrap()void'] | SourceFactoryArgs['tuple']['bootstrap()void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(SourceFactoryParamsFactory.bootstrap(params))
      return {...result, return: result.return as unknown as (undefined | SourceFactoryReturns['bootstrap()void'])}
    },

    /**
     * Makes a call to the SourceFactory smart contract using the `create_bounty(uint64,uint64)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    createBounty: async (params: CallParams<SourceFactoryArgs['obj']['create_bounty(uint64,uint64)void'] | SourceFactoryArgs['tuple']['create_bounty(uint64,uint64)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(SourceFactoryParamsFactory.createBounty(params))
      return {...result, return: result.return as unknown as (undefined | SourceFactoryReturns['create_bounty(uint64,uint64)void'])}
    },

    /**
     * Makes a call to the SourceFactory smart contract using the `withdraw_bounty(uint64,address)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    withdrawBounty: async (params: CallParams<SourceFactoryArgs['obj']['withdraw_bounty(uint64,address)void'] | SourceFactoryArgs['tuple']['withdraw_bounty(uint64,address)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(SourceFactoryParamsFactory.withdrawBounty(params))
      return {...result, return: result.return as unknown as (undefined | SourceFactoryReturns['withdraw_bounty(uint64,address)void'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new SourceFactoryClient(this.appClient.clone(params))
  }

  /**
   * Methods to access state for the current SourceFactory app
   */
  state = {
    /**
     * Methods to access global state for the current SourceFactory app
     */
    global: {
      /**
       * Get all current keyed values from global state
       */
      getAll: async (): Promise<Partial<Expand<GlobalKeysState>>> => {
        const result = await this.appClient.state.global.getAll()
        return {
          managerAddress: result.MANAGER_ADDRESS,
          totalBounties: result.TOTAL_BOUNTIES,
        }
      },
      /**
       * Get the current value of the MANAGER_ADDRESS key in global state
       */
      managerAddress: async (): Promise<string | undefined> => { return (await this.appClient.state.global.getValue("MANAGER_ADDRESS")) as string | undefined },
      /**
       * Get the current value of the TOTAL_BOUNTIES key in global state
       */
      totalBounties: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("TOTAL_BOUNTIES")) as bigint | undefined },
    },
    /**
     * Methods to access box state for the current SourceFactory app
     */
    box: {
      /**
       * Get all current keyed values from box state
       */
      getAll: async (): Promise<Partial<Expand<BoxKeysState>>> => {
        const result = await this.appClient.state.box.getAll()
        return {
        }
      },
      /**
       * Get values from the bounties map in box state
       */
      bounties: {
        /**
         * Get all current values of the bounties map in box state
         */
        getMap: async (): Promise<Map<BountyIdType, BountyDataType>> => { return (await this.appClient.state.box.getMap("bounties")) as Map<BountyIdType, BountyDataType> },
        /**
         * Get a current value of the bounties map by key from box state
         */
        value: async (key: BountyIdType): Promise<BountyDataType | undefined> => { return await this.appClient.state.box.getMapValue("bounties", key) as BountyDataType | undefined },
      },
    },
  }

  public newGroup(): SourceFactoryComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a bootstrap()void method call against the SourceFactory contract
       */
      bootstrap(params: CallParams<SourceFactoryArgs['obj']['bootstrap()void'] | SourceFactoryArgs['tuple']['bootstrap()void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.bootstrap(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a create_bounty(uint64,uint64)void method call against the SourceFactory contract
       */
      createBounty(params: CallParams<SourceFactoryArgs['obj']['create_bounty(uint64,uint64)void'] | SourceFactoryArgs['tuple']['create_bounty(uint64,uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.createBounty(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a withdraw_bounty(uint64,address)void method call against the SourceFactory contract
       */
      withdrawBounty(params: CallParams<SourceFactoryArgs['obj']['withdraw_bounty(uint64,address)void'] | SourceFactoryArgs['tuple']['withdraw_bounty(uint64,address)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.withdrawBounty(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a clear state call to the SourceFactory contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as SourceFactoryComposer
  }
}
export type SourceFactoryComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the bootstrap()void ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  bootstrap(params?: CallParams<SourceFactoryArgs['obj']['bootstrap()void'] | SourceFactoryArgs['tuple']['bootstrap()void']>): SourceFactoryComposer<[...TReturns, SourceFactoryReturns['bootstrap()void'] | undefined]>

  /**
   * Calls the create_bounty(uint64,uint64)void ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  createBounty(params?: CallParams<SourceFactoryArgs['obj']['create_bounty(uint64,uint64)void'] | SourceFactoryArgs['tuple']['create_bounty(uint64,uint64)void']>): SourceFactoryComposer<[...TReturns, SourceFactoryReturns['create_bounty(uint64,uint64)void'] | undefined]>

  /**
   * Calls the withdraw_bounty(uint64,address)void ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  withdrawBounty(params?: CallParams<SourceFactoryArgs['obj']['withdraw_bounty(uint64,address)void'] | SourceFactoryArgs['tuple']['withdraw_bounty(uint64,address)void']>): SourceFactoryComposer<[...TReturns, SourceFactoryReturns['withdraw_bounty(uint64,address)void'] | undefined]>

  /**
   * Makes a clear_state call to an existing instance of the SourceFactory smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): SourceFactoryComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): SourceFactoryComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<SourceFactoryComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<SourceFactoryComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<SourceFactoryComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<SourceFactoryComposerResults<TReturns>>
}
export type SourceFactoryComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

