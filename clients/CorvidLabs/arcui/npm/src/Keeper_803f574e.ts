/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'
import { AppFactory as _AppFactory, AppFactoryAppClientParams, AppFactoryResolveAppClientByCreatorAndNameParams, AppFactoryDeployParams, AppFactoryParams, CreateSchema } from '@algorandfoundation/algokit-utils/types/app-factory'
import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"Keeper","structs":{},"methods":[{"name":"update","args":[],"returns":{"type":"void"},"actions":{"create":[],"call":["UpdateApplication"]},"readonly":false,"desc":"Replace the programs. Creator only, and only before `freeze`.\nThis exists because being unable to fix a bug is expensive while nobody depends on the deployment yet. Two earlier deployments were abandoned rather than repaired, which stranded box minimum balance and made every creator cancel and re-register by hand.\nIt is also a real power: while `frozen` is 0, the creator can change the rules after funds are escrowed, and no statement of intent removes that. So it is temporary by construction, readable on-chain, and given up before the network asks anyone to rely on it.","events":[],"recommendations":{}},{"name":"freeze","args":[],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Give up the ability to update, permanently. Creator only.\nOne way. Nothing sets `frozen` back to 0, and after this the only call that could add such a path is an update, which is now refused. From here the contract is exactly as immutable as one deployed with no update path at all, and `verify_build` proves which programs it is stuck with.","events":[],"recommendations":{}},{"name":"register","args":[{"type":"pay","name":"mbr_payment"},{"type":"pay","name":"funding_payment"},{"type":"uint64","name":"target_app"},{"type":"byte[][]","name":"call_args"},{"type":"uint64","name":"interval_rounds"},{"type":"uint64","name":"fee_per_execution"},{"type":"uint64","name":"policy"},{"type":"uint64","name":"fee_cap"},{"type":"uint64","name":"fee_asset"},{"type":"uint64","name":"asset_fee"}],"returns":{"type":"uint64"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Register an upkeep; returns its id.\n`call_args` is every app arg of the call, in order. `policy` is CATCH_UP or SKIP_AHEAD. `fee_cap` is the most this upkeep will ever pay for one execution in ALGO; zero means the fee never escalates. `fee_asset` and `asset_fee` add an ASA bonus on top of the ALGO fee; zero means ALGO only.","events":[],"recommendations":{}},{"name":"top_up","args":[{"type":"uint64","name":"upkeep_id"},{"type":"pay","name":"funding_payment"}],"returns":{"type":"uint64"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Add ALGO to an upkeep's escrow; returns the new balance.","events":[],"recommendations":{}},{"name":"cancel","args":[{"type":"uint64","name":"upkeep_id"}],"returns":{"type":"uint64"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Cancel an upkeep (creator only); refunds escrow and box MBR.\nDeleting the box releases its minimum balance, so the creator gets back the remaining escrow *and* the MBR it paid at registration — nothing is stranded in the app account. Returns the refunded amount.","events":[],"recommendations":{}},{"name":"execute","args":[{"type":"uint64","name":"upkeep_id"}],"returns":{"type":"uint64"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Execute a due upkeep (permissionless); pays the caller its fee.\nReturns the round the upkeep is next due.","events":[],"recommendations":{}},{"name":"opt_in_asset","args":[{"type":"pay","name":"mbr_payment"},{"type":"uint64","name":"upkeep_id"},{"type":"uint64","name":"asset"}],"returns":{"type":"uint64"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Let the app account hold `asset`, so an upkeep can escrow a bonus.\nPermissionless, but tied to an upkeep that actually names the asset: an app that anyone could opt in to anything would accrete junk holdings for good, since there is no opt-out. The deposit is not refundable — reference-counting it would cost a box per asset and more code than the 0.1 ALGO it would ever return.","events":[],"recommendations":{}},{"name":"top_up_asset","args":[{"type":"uint64","name":"upkeep_id"},{"type":"axfer","name":"asset_funding"}],"returns":{"type":"uint64"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Add ASA to an upkeep's bonus escrow; returns the new asset balance.\nSeparate from `register` because an asset transfer cannot be an optional member of a transaction group: folding it in would make every ALGO-only registration carry a zero-amount transfer of an asset it does not use.","events":[],"recommendations":{}}],"arcs":[22,28],"desc":"Permissionless keeper network.\n\n    Anyone registers an upkeep: \"call this app with this data every N rounds,\n    paying R µALGO per execution\", escrowing ALGO in the contract. Any keeper\n    may execute a due upkeep; the contract performs the registered inner app\n    call and pays the keeper from the escrow. No owner, no protocol rake.\n    ","networks":{},"state":{"schema":{"global":{"ints":2,"bytes":0},"local":{"ints":0,"bytes":0}},"keys":{"global":{"next_upkeep_id":{"keyType":"AVMString","valueType":"AVMUint64","key":"bmV4dF91cGtlZXBfaWQ="},"frozen":{"keyType":"AVMString","valueType":"AVMUint64","key":"ZnJvemVu"}},"local":{},"box":{}},"maps":{"global":{},"local":{},"box":{}}},"bareActions":{"create":["NoOp"],"call":[]},"events":[]} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


/**
 * The argument types for the Keeper contract
 */
export type KeeperArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'update()void': Record<string, never>
    'freeze()void': Record<string, never>
    'register(pay,pay,uint64,byte[][],uint64,uint64,uint64,uint64,uint64,uint64)uint64': {
      mbrPayment: AppMethodCallTransactionArgument
      fundingPayment: AppMethodCallTransactionArgument
      targetApp: bigint | number
      callArgs: Uint8Array[]
      intervalRounds: bigint | number
      feePerExecution: bigint | number
      policy: bigint | number
      feeCap: bigint | number
      feeAsset: bigint | number
      assetFee: bigint | number
    }
    'top_up(uint64,pay)uint64': {
      upkeepId: bigint | number
      fundingPayment: AppMethodCallTransactionArgument
    }
    'cancel(uint64)uint64': {
      upkeepId: bigint | number
    }
    'execute(uint64)uint64': {
      upkeepId: bigint | number
    }
    'opt_in_asset(pay,uint64,uint64)uint64': {
      mbrPayment: AppMethodCallTransactionArgument
      upkeepId: bigint | number
      asset: bigint | number
    }
    'top_up_asset(uint64,axfer)uint64': {
      upkeepId: bigint | number
      assetFunding: AppMethodCallTransactionArgument
    }
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'update()void': []
    'freeze()void': []
    'register(pay,pay,uint64,byte[][],uint64,uint64,uint64,uint64,uint64,uint64)uint64': [mbrPayment: AppMethodCallTransactionArgument, fundingPayment: AppMethodCallTransactionArgument, targetApp: bigint | number, callArgs: Uint8Array[], intervalRounds: bigint | number, feePerExecution: bigint | number, policy: bigint | number, feeCap: bigint | number, feeAsset: bigint | number, assetFee: bigint | number]
    'top_up(uint64,pay)uint64': [upkeepId: bigint | number, fundingPayment: AppMethodCallTransactionArgument]
    'cancel(uint64)uint64': [upkeepId: bigint | number]
    'execute(uint64)uint64': [upkeepId: bigint | number]
    'opt_in_asset(pay,uint64,uint64)uint64': [mbrPayment: AppMethodCallTransactionArgument, upkeepId: bigint | number, asset: bigint | number]
    'top_up_asset(uint64,axfer)uint64': [upkeepId: bigint | number, assetFunding: AppMethodCallTransactionArgument]
  }
}

/**
 * The return type for each method
 */
export type KeeperReturns = {
  'update()void': void
  'freeze()void': void
  'register(pay,pay,uint64,byte[][],uint64,uint64,uint64,uint64,uint64,uint64)uint64': bigint
  'top_up(uint64,pay)uint64': bigint
  'cancel(uint64)uint64': bigint
  'execute(uint64)uint64': bigint
  'opt_in_asset(pay,uint64,uint64)uint64': bigint
  'top_up_asset(uint64,axfer)uint64': bigint
}

/**
 * Defines the types of available calls and state of the Keeper smart contract.
 */
export type KeeperTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'update()void' | 'update', {
      argsObj: KeeperArgs['obj']['update()void']
      argsTuple: KeeperArgs['tuple']['update()void']
      returns: KeeperReturns['update()void']
    }>
    & Record<'freeze()void' | 'freeze', {
      argsObj: KeeperArgs['obj']['freeze()void']
      argsTuple: KeeperArgs['tuple']['freeze()void']
      returns: KeeperReturns['freeze()void']
    }>
    & Record<'register(pay,pay,uint64,byte[][],uint64,uint64,uint64,uint64,uint64,uint64)uint64' | 'register', {
      argsObj: KeeperArgs['obj']['register(pay,pay,uint64,byte[][],uint64,uint64,uint64,uint64,uint64,uint64)uint64']
      argsTuple: KeeperArgs['tuple']['register(pay,pay,uint64,byte[][],uint64,uint64,uint64,uint64,uint64,uint64)uint64']
      returns: KeeperReturns['register(pay,pay,uint64,byte[][],uint64,uint64,uint64,uint64,uint64,uint64)uint64']
    }>
    & Record<'top_up(uint64,pay)uint64' | 'top_up', {
      argsObj: KeeperArgs['obj']['top_up(uint64,pay)uint64']
      argsTuple: KeeperArgs['tuple']['top_up(uint64,pay)uint64']
      returns: KeeperReturns['top_up(uint64,pay)uint64']
    }>
    & Record<'cancel(uint64)uint64' | 'cancel', {
      argsObj: KeeperArgs['obj']['cancel(uint64)uint64']
      argsTuple: KeeperArgs['tuple']['cancel(uint64)uint64']
      returns: KeeperReturns['cancel(uint64)uint64']
    }>
    & Record<'execute(uint64)uint64' | 'execute', {
      argsObj: KeeperArgs['obj']['execute(uint64)uint64']
      argsTuple: KeeperArgs['tuple']['execute(uint64)uint64']
      returns: KeeperReturns['execute(uint64)uint64']
    }>
    & Record<'opt_in_asset(pay,uint64,uint64)uint64' | 'opt_in_asset', {
      argsObj: KeeperArgs['obj']['opt_in_asset(pay,uint64,uint64)uint64']
      argsTuple: KeeperArgs['tuple']['opt_in_asset(pay,uint64,uint64)uint64']
      returns: KeeperReturns['opt_in_asset(pay,uint64,uint64)uint64']
    }>
    & Record<'top_up_asset(uint64,axfer)uint64' | 'top_up_asset', {
      argsObj: KeeperArgs['obj']['top_up_asset(uint64,axfer)uint64']
      argsTuple: KeeperArgs['tuple']['top_up_asset(uint64,axfer)uint64']
      returns: KeeperReturns['top_up_asset(uint64,axfer)uint64']
    }>
  /**
   * Defines the shape of the state of the application.
   */
  state: {
    global: {
      keys: {
        nextUpkeepId: bigint
        frozen: bigint
      }
      maps: {}
    }
  }
}

/**
 * Defines the possible abi call signatures.
 */
export type KeeperSignatures = keyof KeeperTypes['methods']
/**
 * Defines the possible abi call signatures for methods that return a non-void value.
 */
export type KeeperNonVoidMethodSignatures = keyof KeeperTypes['methods'] extends infer T ? T extends keyof KeeperTypes['methods'] ? MethodReturn<T> extends void ? never : T  : never : never
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the Keeper smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends KeeperSignatures> = KeeperTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the Keeper smart contract to the method's return type
 */
export type MethodReturn<TSignature extends KeeperSignatures> = KeeperTypes['methods'][TSignature]['returns']

/**
 * Defines the shape of the keyed global state of the application.
 */
export type GlobalKeysState = KeeperTypes['state']['global']['keys']


/**
 * Defines supported create method params for this smart contract
 */
export type KeeperCreateCallParams =
  | Expand<AppClientBareCallParams & {method?: never} & {onComplete?: OnApplicationComplete.NoOpOC} & CreateSchema>
/**
 * Defines supported update method params for this smart contract
 */
export type KeeperUpdateCallParams =
  | Expand<CallParams<KeeperArgs['obj']['update()void'] | KeeperArgs['tuple']['update()void']> & {method: 'update'}>
  | Expand<CallParams<KeeperArgs['obj']['update()void'] | KeeperArgs['tuple']['update()void']> & {method: 'update()void'}>
/**
 * Defines arguments required for the deploy method.
 */
export type KeeperDeployParams = Expand<Omit<AppFactoryDeployParams, 'createParams' | 'updateParams' | 'deleteParams'> & {
  /**
   * Create transaction parameters to use if a create needs to be issued as part of deployment; use `method` to define ABI call (if available) or leave out for a bare call (if available)
   */
  createParams?: KeeperCreateCallParams
  /**
   * Update transaction parameters to use if a create needs to be issued as part of deployment; use `method` to define ABI call (if available) or leave out for a bare call (if available)
   */
  updateParams?: KeeperUpdateCallParams
}>


/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the Keeper smart contract
 */
export abstract class KeeperParamsFactory {
  /**
   * Gets available update ABI call param factories
   */
  static get update() {
    return {
      _resolveByMethod<TParams extends KeeperUpdateCallParams & {method: string}>(params: TParams) {
        switch(params.method) {
          case 'update':
          case 'update()void':
            return KeeperParamsFactory.update.update(params)
        }
        throw new Error(`Unknown ' + verb + ' method`)
      },

      /**
       * Constructs update ABI call params for the Keeper smart contract using the update()void ABI method
       *
       * @param params Parameters for the call
       * @returns An `AppClientMethodCallParams` object for the call
       */
      update(params: CallParams<KeeperArgs['obj']['update()void'] | KeeperArgs['tuple']['update()void']> & AppClientCompilationParams): AppClientMethodCallParams & AppClientCompilationParams {
        return {
          ...params,
          method: 'update()void' as const,
          args: Array.isArray(params.args) ? params.args : [],
        }
      },
    }
  }

  /**
   * Constructs a no op call for the freeze()void ABI method
   *
  * Give up the ability to update, permanently. Creator only.
  One way. Nothing sets `frozen` back to 0, and after this the only call that could add such a path is an update, which is now refused. From here the contract is exactly as immutable as one deployed with no update path at all, and `verify_build` proves which programs it is stuck with.

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static freeze(params: CallParams<KeeperArgs['obj']['freeze()void'] | KeeperArgs['tuple']['freeze()void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'freeze()void' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the register(pay,pay,uint64,byte[][],uint64,uint64,uint64,uint64,uint64,uint64)uint64 ABI method
   *
  * Register an upkeep; returns its id.
  `call_args` is every app arg of the call, in order. `policy` is CATCH_UP or SKIP_AHEAD. `fee_cap` is the most this upkeep will ever pay for one execution in ALGO; zero means the fee never escalates. `fee_asset` and `asset_fee` add an ASA bonus on top of the ALGO fee; zero means ALGO only.

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static register(params: CallParams<KeeperArgs['obj']['register(pay,pay,uint64,byte[][],uint64,uint64,uint64,uint64,uint64,uint64)uint64'] | KeeperArgs['tuple']['register(pay,pay,uint64,byte[][],uint64,uint64,uint64,uint64,uint64,uint64)uint64']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'register(pay,pay,uint64,byte[][],uint64,uint64,uint64,uint64,uint64,uint64)uint64' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.mbrPayment, params.args.fundingPayment, params.args.targetApp, params.args.callArgs, params.args.intervalRounds, params.args.feePerExecution, params.args.policy, params.args.feeCap, params.args.feeAsset, params.args.assetFee],
    }
  }
  /**
   * Constructs a no op call for the top_up(uint64,pay)uint64 ABI method
   *
   * Add ALGO to an upkeep's escrow; returns the new balance.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static topUp(params: CallParams<KeeperArgs['obj']['top_up(uint64,pay)uint64'] | KeeperArgs['tuple']['top_up(uint64,pay)uint64']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'top_up(uint64,pay)uint64' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.upkeepId, params.args.fundingPayment],
    }
  }
  /**
   * Constructs a no op call for the cancel(uint64)uint64 ABI method
   *
  * Cancel an upkeep (creator only); refunds escrow and box MBR.
  Deleting the box releases its minimum balance, so the creator gets back the remaining escrow *and* the MBR it paid at registration — nothing is stranded in the app account. Returns the refunded amount.

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static cancel(params: CallParams<KeeperArgs['obj']['cancel(uint64)uint64'] | KeeperArgs['tuple']['cancel(uint64)uint64']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'cancel(uint64)uint64' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.upkeepId],
    }
  }
  /**
   * Constructs a no op call for the execute(uint64)uint64 ABI method
   *
  * Execute a due upkeep (permissionless); pays the caller its fee.
  Returns the round the upkeep is next due.

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static execute(params: CallParams<KeeperArgs['obj']['execute(uint64)uint64'] | KeeperArgs['tuple']['execute(uint64)uint64']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'execute(uint64)uint64' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.upkeepId],
    }
  }
  /**
   * Constructs a no op call for the opt_in_asset(pay,uint64,uint64)uint64 ABI method
   *
  * Let the app account hold `asset`, so an upkeep can escrow a bonus.
  Permissionless, but tied to an upkeep that actually names the asset: an app that anyone could opt in to anything would accrete junk holdings for good, since there is no opt-out. The deposit is not refundable — reference-counting it would cost a box per asset and more code than the 0.1 ALGO it would ever return.

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static optInAsset(params: CallParams<KeeperArgs['obj']['opt_in_asset(pay,uint64,uint64)uint64'] | KeeperArgs['tuple']['opt_in_asset(pay,uint64,uint64)uint64']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'opt_in_asset(pay,uint64,uint64)uint64' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.mbrPayment, params.args.upkeepId, params.args.asset],
    }
  }
  /**
   * Constructs a no op call for the top_up_asset(uint64,axfer)uint64 ABI method
   *
  * Add ASA to an upkeep's bonus escrow; returns the new asset balance.
  Separate from `register` because an asset transfer cannot be an optional member of a transaction group: folding it in would make every ALGO-only registration carry a zero-amount transfer of an asset it does not use.

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static topUpAsset(params: CallParams<KeeperArgs['obj']['top_up_asset(uint64,axfer)uint64'] | KeeperArgs['tuple']['top_up_asset(uint64,axfer)uint64']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'top_up_asset(uint64,axfer)uint64' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.upkeepId, params.args.assetFunding],
    }
  }
}

/**
 * A factory to create and deploy one or more instance of the Keeper smart contract and to create one or more app clients to interact with those (or other) app instances
 */
export class KeeperFactory {
  /**
   * The underlying `AppFactory` for when you want to have more flexibility
   */
  public readonly appFactory: _AppFactory

  /**
   * Creates a new instance of `KeeperFactory`
   *
   * @param params The parameters to initialise the app factory with
   */
  constructor(params: Omit<AppFactoryParams, 'appSpec'>) {
    this.appFactory = new _AppFactory({
      ...params,
      appSpec: APP_SPEC,
    })
  }
  
  /** The name of the app (from the ARC-32 / ARC-56 app spec or override). */
  public get appName() {
    return this.appFactory.appName
  }
  
  /** The ARC-56 app spec being used */
  get appSpec() {
    return APP_SPEC
  }
  
  /** A reference to the underlying `AlgorandClient` this app factory is using. */
  public get algorand(): AlgorandClient {
    return this.appFactory.algorand
  }
  
  /**
   * Returns a new `AppClient` client for an app instance of the given ID.
   *
   * Automatically populates appName, defaultSender and source maps from the factory
   * if not specified in the params.
   * @param params The parameters to create the app client
   * @returns The `AppClient`
   */
  public getAppClientById(params: AppFactoryAppClientParams) {
    return new KeeperClient(this.appFactory.getAppClientById(params))
  }
  
  /**
   * Returns a new `AppClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   *
   * Automatically populates appName, defaultSender and source maps from the factory
   * if not specified in the params.
   * @param params The parameters to create the app client
   * @returns The `AppClient`
   */
  public async getAppClientByCreatorAndName(
    params: AppFactoryResolveAppClientByCreatorAndNameParams,
  ) {
    return new KeeperClient(await this.appFactory.getAppClientByCreatorAndName(params))
  }

  /**
   * Idempotently deploys the Keeper smart contract.
   *
   * @param params The arguments for the contract calls and any additional parameters for the call
   * @returns The deployment result
   */
  public async deploy(params: KeeperDeployParams = {}) {
    const result = await this.appFactory.deploy({
      ...params,
      updateParams: params.updateParams?.method ? KeeperParamsFactory.update._resolveByMethod(params.updateParams) : params.updateParams ? params.updateParams as (KeeperUpdateCallParams & { args: Uint8Array[] }) : undefined,
    })
    return { result: result.result, appClient: new KeeperClient(result.appClient) }
  }

  /**
   * Get parameters to create transactions (create and deploy related calls) for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Gets available create methods
     */
    create: {
      /**
       * Creates a new instance of the Keeper smart contract using a bare call.
       *
       * @param params The params for the bare (raw) call
       * @returns The params for a create call
       */
      bare: (params?: Expand<AppClientBareCallParams & AppClientCompilationParams & CreateSchema & {onComplete?: OnApplicationComplete.NoOpOC}>) => {
        return this.appFactory.params.bare.create(params)
      },
    },

    /**
     * Gets available deployUpdate methods
     */
    deployUpdate: {
      /**
       * Updates an existing instance of the Keeper smart contract using the update()void ABI method.
       *
      * Replace the programs. Creator only, and only before `freeze`.
      This exists because being unable to fix a bug is expensive while nobody depends on the deployment yet. Two earlier deployments were abandoned rather than repaired, which stranded box minimum balance and made every creator cancel and re-register by hand.
      It is also a real power: while `frozen` is 0, the creator can change the rules after funds are escrowed, and no statement of intent removes that. So it is temporary by construction, readable on-chain, and given up before the network asks anyone to rely on it.

       *
       * @param params The params for the smart contract call
       * @returns The deployUpdate params
       */
      update: (params: CallParams<KeeperArgs['obj']['update()void'] | KeeperArgs['tuple']['update()void']> & AppClientCompilationParams = {args: []}) => {
        return this.appFactory.params.deployUpdate(KeeperParamsFactory.update.update(params))
      },
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Gets available create methods
     */
    create: {
      /**
       * Creates a new instance of the Keeper smart contract using a bare call.
       *
       * @param params The params for the bare (raw) call
       * @returns The transaction for a create call
       */
      bare: (params?: Expand<AppClientBareCallParams & AppClientCompilationParams & CreateSchema & {onComplete?: OnApplicationComplete.NoOpOC}>) => {
        return this.appFactory.createTransaction.bare.create(params)
      },
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Gets available create methods
     */
    create: {
      /**
       * Creates a new instance of the Keeper smart contract using a bare call.
       *
       * @param params The params for the bare (raw) call
       * @returns The create result
       */
      bare: async (params?: Expand<AppClientBareCallParams & AppClientCompilationParams & CreateSchema & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}>) => {
        const result = await this.appFactory.send.bare.create(params)
        return { result: result.result, appClient: new KeeperClient(result.appClient) }
      },
    },

  }

}
/**
 * A client to make calls to the Keeper smart contract
 */
export class KeeperClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `KeeperClient`
   *
   * @param appClient An `AppClient` instance which has been created with the Keeper app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `KeeperClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Checks for decode errors on the given return value and maps the return value to the return type for the given method
   * @returns The typed return value or undefined if there was no value
   */
  decodeReturnValue<TSignature extends KeeperNonVoidMethodSignatures>(method: TSignature, returnValue: ABIReturn | undefined) {
    return returnValue !== undefined ? getArc56ReturnValue<MethodReturn<TSignature>>(returnValue, this.appClient.getABIMethod(method), APP_SPEC.structs) : undefined
  }

  /**
   * Returns a new `KeeperClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<KeeperClient> {
    return new KeeperClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `KeeperClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<KeeperClient> {
    return new KeeperClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Gets available update methods
     */
    update: {
      /**
       * Updates an existing instance of the Keeper smart contract using the `update()void` ABI method.
       *
      * Replace the programs. Creator only, and only before `freeze`.
      This exists because being unable to fix a bug is expensive while nobody depends on the deployment yet. Two earlier deployments were abandoned rather than repaired, which stranded box minimum balance and made every creator cancel and re-register by hand.
      It is also a real power: while `frozen` is 0, the creator can change the rules after funds are escrowed, and no statement of intent removes that. So it is temporary by construction, readable on-chain, and given up before the network asks anyone to rely on it.

       *
       * @param params The params for the smart contract call
       * @returns The update params
       */
      update: (params: CallParams<KeeperArgs['obj']['update()void'] | KeeperArgs['tuple']['update()void']> & AppClientCompilationParams = {args: []}) => {
        return this.appClient.params.update(KeeperParamsFactory.update.update(params))
      },

    },

    /**
     * Makes a clear_state call to an existing instance of the Keeper smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the Keeper smart contract using the `freeze()void` ABI method.
     *
    * Give up the ability to update, permanently. Creator only.
    One way. Nothing sets `frozen` back to 0, and after this the only call that could add such a path is an update, which is now refused. From here the contract is exactly as immutable as one deployed with no update path at all, and `verify_build` proves which programs it is stuck with.

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    freeze: (params: CallParams<KeeperArgs['obj']['freeze()void'] | KeeperArgs['tuple']['freeze()void']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(KeeperParamsFactory.freeze(params))
    },

    /**
     * Makes a call to the Keeper smart contract using the `register(pay,pay,uint64,byte[][],uint64,uint64,uint64,uint64,uint64,uint64)uint64` ABI method.
     *
    * Register an upkeep; returns its id.
    `call_args` is every app arg of the call, in order. `policy` is CATCH_UP or SKIP_AHEAD. `fee_cap` is the most this upkeep will ever pay for one execution in ALGO; zero means the fee never escalates. `fee_asset` and `asset_fee` add an ASA bonus on top of the ALGO fee; zero means ALGO only.

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    register: (params: CallParams<KeeperArgs['obj']['register(pay,pay,uint64,byte[][],uint64,uint64,uint64,uint64,uint64,uint64)uint64'] | KeeperArgs['tuple']['register(pay,pay,uint64,byte[][],uint64,uint64,uint64,uint64,uint64,uint64)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(KeeperParamsFactory.register(params))
    },

    /**
     * Makes a call to the Keeper smart contract using the `top_up(uint64,pay)uint64` ABI method.
     *
     * Add ALGO to an upkeep's escrow; returns the new balance.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    topUp: (params: CallParams<KeeperArgs['obj']['top_up(uint64,pay)uint64'] | KeeperArgs['tuple']['top_up(uint64,pay)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(KeeperParamsFactory.topUp(params))
    },

    /**
     * Makes a call to the Keeper smart contract using the `cancel(uint64)uint64` ABI method.
     *
    * Cancel an upkeep (creator only); refunds escrow and box MBR.
    Deleting the box releases its minimum balance, so the creator gets back the remaining escrow *and* the MBR it paid at registration — nothing is stranded in the app account. Returns the refunded amount.

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    cancel: (params: CallParams<KeeperArgs['obj']['cancel(uint64)uint64'] | KeeperArgs['tuple']['cancel(uint64)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(KeeperParamsFactory.cancel(params))
    },

    /**
     * Makes a call to the Keeper smart contract using the `execute(uint64)uint64` ABI method.
     *
    * Execute a due upkeep (permissionless); pays the caller its fee.
    Returns the round the upkeep is next due.

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    execute: (params: CallParams<KeeperArgs['obj']['execute(uint64)uint64'] | KeeperArgs['tuple']['execute(uint64)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(KeeperParamsFactory.execute(params))
    },

    /**
     * Makes a call to the Keeper smart contract using the `opt_in_asset(pay,uint64,uint64)uint64` ABI method.
     *
    * Let the app account hold `asset`, so an upkeep can escrow a bonus.
    Permissionless, but tied to an upkeep that actually names the asset: an app that anyone could opt in to anything would accrete junk holdings for good, since there is no opt-out. The deposit is not refundable — reference-counting it would cost a box per asset and more code than the 0.1 ALGO it would ever return.

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    optInAsset: (params: CallParams<KeeperArgs['obj']['opt_in_asset(pay,uint64,uint64)uint64'] | KeeperArgs['tuple']['opt_in_asset(pay,uint64,uint64)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(KeeperParamsFactory.optInAsset(params))
    },

    /**
     * Makes a call to the Keeper smart contract using the `top_up_asset(uint64,axfer)uint64` ABI method.
     *
    * Add ASA to an upkeep's bonus escrow; returns the new asset balance.
    Separate from `register` because an asset transfer cannot be an optional member of a transaction group: folding it in would make every ALGO-only registration carry a zero-amount transfer of an asset it does not use.

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    topUpAsset: (params: CallParams<KeeperArgs['obj']['top_up_asset(uint64,axfer)uint64'] | KeeperArgs['tuple']['top_up_asset(uint64,axfer)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(KeeperParamsFactory.topUpAsset(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Gets available update methods
     */
    update: {
      /**
       * Updates an existing instance of the Keeper smart contract using the `update()void` ABI method.
       *
      * Replace the programs. Creator only, and only before `freeze`.
      This exists because being unable to fix a bug is expensive while nobody depends on the deployment yet. Two earlier deployments were abandoned rather than repaired, which stranded box minimum balance and made every creator cancel and re-register by hand.
      It is also a real power: while `frozen` is 0, the creator can change the rules after funds are escrowed, and no statement of intent removes that. So it is temporary by construction, readable on-chain, and given up before the network asks anyone to rely on it.

       *
       * @param params The params for the smart contract call
       * @returns The update transaction
       */
      update: (params: CallParams<KeeperArgs['obj']['update()void'] | KeeperArgs['tuple']['update()void']> & AppClientCompilationParams = {args: []}) => {
        return this.appClient.createTransaction.update(KeeperParamsFactory.update.update(params))
      },

    },

    /**
     * Makes a clear_state call to an existing instance of the Keeper smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the Keeper smart contract using the `freeze()void` ABI method.
     *
    * Give up the ability to update, permanently. Creator only.
    One way. Nothing sets `frozen` back to 0, and after this the only call that could add such a path is an update, which is now refused. From here the contract is exactly as immutable as one deployed with no update path at all, and `verify_build` proves which programs it is stuck with.

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    freeze: (params: CallParams<KeeperArgs['obj']['freeze()void'] | KeeperArgs['tuple']['freeze()void']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(KeeperParamsFactory.freeze(params))
    },

    /**
     * Makes a call to the Keeper smart contract using the `register(pay,pay,uint64,byte[][],uint64,uint64,uint64,uint64,uint64,uint64)uint64` ABI method.
     *
    * Register an upkeep; returns its id.
    `call_args` is every app arg of the call, in order. `policy` is CATCH_UP or SKIP_AHEAD. `fee_cap` is the most this upkeep will ever pay for one execution in ALGO; zero means the fee never escalates. `fee_asset` and `asset_fee` add an ASA bonus on top of the ALGO fee; zero means ALGO only.

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    register: (params: CallParams<KeeperArgs['obj']['register(pay,pay,uint64,byte[][],uint64,uint64,uint64,uint64,uint64,uint64)uint64'] | KeeperArgs['tuple']['register(pay,pay,uint64,byte[][],uint64,uint64,uint64,uint64,uint64,uint64)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(KeeperParamsFactory.register(params))
    },

    /**
     * Makes a call to the Keeper smart contract using the `top_up(uint64,pay)uint64` ABI method.
     *
     * Add ALGO to an upkeep's escrow; returns the new balance.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    topUp: (params: CallParams<KeeperArgs['obj']['top_up(uint64,pay)uint64'] | KeeperArgs['tuple']['top_up(uint64,pay)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(KeeperParamsFactory.topUp(params))
    },

    /**
     * Makes a call to the Keeper smart contract using the `cancel(uint64)uint64` ABI method.
     *
    * Cancel an upkeep (creator only); refunds escrow and box MBR.
    Deleting the box releases its minimum balance, so the creator gets back the remaining escrow *and* the MBR it paid at registration — nothing is stranded in the app account. Returns the refunded amount.

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    cancel: (params: CallParams<KeeperArgs['obj']['cancel(uint64)uint64'] | KeeperArgs['tuple']['cancel(uint64)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(KeeperParamsFactory.cancel(params))
    },

    /**
     * Makes a call to the Keeper smart contract using the `execute(uint64)uint64` ABI method.
     *
    * Execute a due upkeep (permissionless); pays the caller its fee.
    Returns the round the upkeep is next due.

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    execute: (params: CallParams<KeeperArgs['obj']['execute(uint64)uint64'] | KeeperArgs['tuple']['execute(uint64)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(KeeperParamsFactory.execute(params))
    },

    /**
     * Makes a call to the Keeper smart contract using the `opt_in_asset(pay,uint64,uint64)uint64` ABI method.
     *
    * Let the app account hold `asset`, so an upkeep can escrow a bonus.
    Permissionless, but tied to an upkeep that actually names the asset: an app that anyone could opt in to anything would accrete junk holdings for good, since there is no opt-out. The deposit is not refundable — reference-counting it would cost a box per asset and more code than the 0.1 ALGO it would ever return.

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    optInAsset: (params: CallParams<KeeperArgs['obj']['opt_in_asset(pay,uint64,uint64)uint64'] | KeeperArgs['tuple']['opt_in_asset(pay,uint64,uint64)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(KeeperParamsFactory.optInAsset(params))
    },

    /**
     * Makes a call to the Keeper smart contract using the `top_up_asset(uint64,axfer)uint64` ABI method.
     *
    * Add ASA to an upkeep's bonus escrow; returns the new asset balance.
    Separate from `register` because an asset transfer cannot be an optional member of a transaction group: folding it in would make every ALGO-only registration carry a zero-amount transfer of an asset it does not use.

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    topUpAsset: (params: CallParams<KeeperArgs['obj']['top_up_asset(uint64,axfer)uint64'] | KeeperArgs['tuple']['top_up_asset(uint64,axfer)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(KeeperParamsFactory.topUpAsset(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Gets available update methods
     */
    update: {
      /**
       * Updates an existing instance of the Keeper smart contract using the `update()void` ABI method.
       *
      * Replace the programs. Creator only, and only before `freeze`.
      This exists because being unable to fix a bug is expensive while nobody depends on the deployment yet. Two earlier deployments were abandoned rather than repaired, which stranded box minimum balance and made every creator cancel and re-register by hand.
      It is also a real power: while `frozen` is 0, the creator can change the rules after funds are escrowed, and no statement of intent removes that. So it is temporary by construction, readable on-chain, and given up before the network asks anyone to rely on it.

       *
       * @param params The params for the smart contract call
       * @returns The update result
       */
      update: async (params: CallParams<KeeperArgs['obj']['update()void'] | KeeperArgs['tuple']['update()void']> & AppClientCompilationParams & SendParams = {args: []}) => {
        const result = await this.appClient.send.update(KeeperParamsFactory.update.update(params))
        return {...result, return: result.return as unknown as (undefined | KeeperReturns['update()void'])}
      },

    },

    /**
     * Makes a clear_state call to an existing instance of the Keeper smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the Keeper smart contract using the `freeze()void` ABI method.
     *
    * Give up the ability to update, permanently. Creator only.
    One way. Nothing sets `frozen` back to 0, and after this the only call that could add such a path is an update, which is now refused. From here the contract is exactly as immutable as one deployed with no update path at all, and `verify_build` proves which programs it is stuck with.

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    freeze: async (params: CallParams<KeeperArgs['obj']['freeze()void'] | KeeperArgs['tuple']['freeze()void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(KeeperParamsFactory.freeze(params))
      return {...result, return: result.return as unknown as (undefined | KeeperReturns['freeze()void'])}
    },

    /**
     * Makes a call to the Keeper smart contract using the `register(pay,pay,uint64,byte[][],uint64,uint64,uint64,uint64,uint64,uint64)uint64` ABI method.
     *
    * Register an upkeep; returns its id.
    `call_args` is every app arg of the call, in order. `policy` is CATCH_UP or SKIP_AHEAD. `fee_cap` is the most this upkeep will ever pay for one execution in ALGO; zero means the fee never escalates. `fee_asset` and `asset_fee` add an ASA bonus on top of the ALGO fee; zero means ALGO only.

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    register: async (params: CallParams<KeeperArgs['obj']['register(pay,pay,uint64,byte[][],uint64,uint64,uint64,uint64,uint64,uint64)uint64'] | KeeperArgs['tuple']['register(pay,pay,uint64,byte[][],uint64,uint64,uint64,uint64,uint64,uint64)uint64']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(KeeperParamsFactory.register(params))
      return {...result, return: result.return as unknown as (undefined | KeeperReturns['register(pay,pay,uint64,byte[][],uint64,uint64,uint64,uint64,uint64,uint64)uint64'])}
    },

    /**
     * Makes a call to the Keeper smart contract using the `top_up(uint64,pay)uint64` ABI method.
     *
     * Add ALGO to an upkeep's escrow; returns the new balance.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    topUp: async (params: CallParams<KeeperArgs['obj']['top_up(uint64,pay)uint64'] | KeeperArgs['tuple']['top_up(uint64,pay)uint64']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(KeeperParamsFactory.topUp(params))
      return {...result, return: result.return as unknown as (undefined | KeeperReturns['top_up(uint64,pay)uint64'])}
    },

    /**
     * Makes a call to the Keeper smart contract using the `cancel(uint64)uint64` ABI method.
     *
    * Cancel an upkeep (creator only); refunds escrow and box MBR.
    Deleting the box releases its minimum balance, so the creator gets back the remaining escrow *and* the MBR it paid at registration — nothing is stranded in the app account. Returns the refunded amount.

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    cancel: async (params: CallParams<KeeperArgs['obj']['cancel(uint64)uint64'] | KeeperArgs['tuple']['cancel(uint64)uint64']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(KeeperParamsFactory.cancel(params))
      return {...result, return: result.return as unknown as (undefined | KeeperReturns['cancel(uint64)uint64'])}
    },

    /**
     * Makes a call to the Keeper smart contract using the `execute(uint64)uint64` ABI method.
     *
    * Execute a due upkeep (permissionless); pays the caller its fee.
    Returns the round the upkeep is next due.

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    execute: async (params: CallParams<KeeperArgs['obj']['execute(uint64)uint64'] | KeeperArgs['tuple']['execute(uint64)uint64']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(KeeperParamsFactory.execute(params))
      return {...result, return: result.return as unknown as (undefined | KeeperReturns['execute(uint64)uint64'])}
    },

    /**
     * Makes a call to the Keeper smart contract using the `opt_in_asset(pay,uint64,uint64)uint64` ABI method.
     *
    * Let the app account hold `asset`, so an upkeep can escrow a bonus.
    Permissionless, but tied to an upkeep that actually names the asset: an app that anyone could opt in to anything would accrete junk holdings for good, since there is no opt-out. The deposit is not refundable — reference-counting it would cost a box per asset and more code than the 0.1 ALGO it would ever return.

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    optInAsset: async (params: CallParams<KeeperArgs['obj']['opt_in_asset(pay,uint64,uint64)uint64'] | KeeperArgs['tuple']['opt_in_asset(pay,uint64,uint64)uint64']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(KeeperParamsFactory.optInAsset(params))
      return {...result, return: result.return as unknown as (undefined | KeeperReturns['opt_in_asset(pay,uint64,uint64)uint64'])}
    },

    /**
     * Makes a call to the Keeper smart contract using the `top_up_asset(uint64,axfer)uint64` ABI method.
     *
    * Add ASA to an upkeep's bonus escrow; returns the new asset balance.
    Separate from `register` because an asset transfer cannot be an optional member of a transaction group: folding it in would make every ALGO-only registration carry a zero-amount transfer of an asset it does not use.

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    topUpAsset: async (params: CallParams<KeeperArgs['obj']['top_up_asset(uint64,axfer)uint64'] | KeeperArgs['tuple']['top_up_asset(uint64,axfer)uint64']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(KeeperParamsFactory.topUpAsset(params))
      return {...result, return: result.return as unknown as (undefined | KeeperReturns['top_up_asset(uint64,axfer)uint64'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new KeeperClient(this.appClient.clone(params))
  }

  /**
   * Methods to access state for the current Keeper app
   */
  state = {
    /**
     * Methods to access global state for the current Keeper app
     */
    global: {
      /**
       * Get all current keyed values from global state
       */
      getAll: async (): Promise<Partial<Expand<GlobalKeysState>>> => {
        const result = await this.appClient.state.global.getAll()
        return {
          nextUpkeepId: result.next_upkeep_id,
          frozen: result.frozen,
        }
      },
      /**
       * Get the current value of the next_upkeep_id key in global state
       */
      nextUpkeepId: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("next_upkeep_id")) as bigint | undefined },
      /**
       * Get the current value of the frozen key in global state
       */
      frozen: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("frozen")) as bigint | undefined },
    },
  }

  public newGroup(): KeeperComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a freeze()void method call against the Keeper contract
       */
      freeze(params: CallParams<KeeperArgs['obj']['freeze()void'] | KeeperArgs['tuple']['freeze()void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.freeze(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a register(pay,pay,uint64,byte[][],uint64,uint64,uint64,uint64,uint64,uint64)uint64 method call against the Keeper contract
       */
      register(params: CallParams<KeeperArgs['obj']['register(pay,pay,uint64,byte[][],uint64,uint64,uint64,uint64,uint64,uint64)uint64'] | KeeperArgs['tuple']['register(pay,pay,uint64,byte[][],uint64,uint64,uint64,uint64,uint64,uint64)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.register(params)))
        resultMappers.push((v) => client.decodeReturnValue('register(pay,pay,uint64,byte[][],uint64,uint64,uint64,uint64,uint64,uint64)uint64', v))
        return this
      },
      /**
       * Add a top_up(uint64,pay)uint64 method call against the Keeper contract
       */
      topUp(params: CallParams<KeeperArgs['obj']['top_up(uint64,pay)uint64'] | KeeperArgs['tuple']['top_up(uint64,pay)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.topUp(params)))
        resultMappers.push((v) => client.decodeReturnValue('top_up(uint64,pay)uint64', v))
        return this
      },
      /**
       * Add a cancel(uint64)uint64 method call against the Keeper contract
       */
      cancel(params: CallParams<KeeperArgs['obj']['cancel(uint64)uint64'] | KeeperArgs['tuple']['cancel(uint64)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.cancel(params)))
        resultMappers.push((v) => client.decodeReturnValue('cancel(uint64)uint64', v))
        return this
      },
      /**
       * Add a execute(uint64)uint64 method call against the Keeper contract
       */
      execute(params: CallParams<KeeperArgs['obj']['execute(uint64)uint64'] | KeeperArgs['tuple']['execute(uint64)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.execute(params)))
        resultMappers.push((v) => client.decodeReturnValue('execute(uint64)uint64', v))
        return this
      },
      /**
       * Add a opt_in_asset(pay,uint64,uint64)uint64 method call against the Keeper contract
       */
      optInAsset(params: CallParams<KeeperArgs['obj']['opt_in_asset(pay,uint64,uint64)uint64'] | KeeperArgs['tuple']['opt_in_asset(pay,uint64,uint64)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.optInAsset(params)))
        resultMappers.push((v) => client.decodeReturnValue('opt_in_asset(pay,uint64,uint64)uint64', v))
        return this
      },
      /**
       * Add a top_up_asset(uint64,axfer)uint64 method call against the Keeper contract
       */
      topUpAsset(params: CallParams<KeeperArgs['obj']['top_up_asset(uint64,axfer)uint64'] | KeeperArgs['tuple']['top_up_asset(uint64,axfer)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.topUpAsset(params)))
        resultMappers.push((v) => client.decodeReturnValue('top_up_asset(uint64,axfer)uint64', v))
        return this
      },
      get update() {
        return {
          update: (params: CallParams<KeeperArgs['obj']['update()void'] | KeeperArgs['tuple']['update()void']> & AppClientCompilationParams) => {
            promiseChain = promiseChain.then(async () => composer.addAppUpdateMethodCall(await client.params.update.update(params)))
            resultMappers.push(undefined)
            return this
          },
        }
      },
      /**
       * Add a clear state call to the Keeper contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as KeeperComposer
  }
}
export type KeeperComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the freeze()void ABI method.
   *
  * Give up the ability to update, permanently. Creator only.
  One way. Nothing sets `frozen` back to 0, and after this the only call that could add such a path is an update, which is now refused. From here the contract is exactly as immutable as one deployed with no update path at all, and `verify_build` proves which programs it is stuck with.

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  freeze(params?: CallParams<KeeperArgs['obj']['freeze()void'] | KeeperArgs['tuple']['freeze()void']>): KeeperComposer<[...TReturns, KeeperReturns['freeze()void'] | undefined]>

  /**
   * Calls the register(pay,pay,uint64,byte[][],uint64,uint64,uint64,uint64,uint64,uint64)uint64 ABI method.
   *
  * Register an upkeep; returns its id.
  `call_args` is every app arg of the call, in order. `policy` is CATCH_UP or SKIP_AHEAD. `fee_cap` is the most this upkeep will ever pay for one execution in ALGO; zero means the fee never escalates. `fee_asset` and `asset_fee` add an ASA bonus on top of the ALGO fee; zero means ALGO only.

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  register(params?: CallParams<KeeperArgs['obj']['register(pay,pay,uint64,byte[][],uint64,uint64,uint64,uint64,uint64,uint64)uint64'] | KeeperArgs['tuple']['register(pay,pay,uint64,byte[][],uint64,uint64,uint64,uint64,uint64,uint64)uint64']>): KeeperComposer<[...TReturns, KeeperReturns['register(pay,pay,uint64,byte[][],uint64,uint64,uint64,uint64,uint64,uint64)uint64'] | undefined]>

  /**
   * Calls the top_up(uint64,pay)uint64 ABI method.
   *
   * Add ALGO to an upkeep's escrow; returns the new balance.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  topUp(params?: CallParams<KeeperArgs['obj']['top_up(uint64,pay)uint64'] | KeeperArgs['tuple']['top_up(uint64,pay)uint64']>): KeeperComposer<[...TReturns, KeeperReturns['top_up(uint64,pay)uint64'] | undefined]>

  /**
   * Calls the cancel(uint64)uint64 ABI method.
   *
  * Cancel an upkeep (creator only); refunds escrow and box MBR.
  Deleting the box releases its minimum balance, so the creator gets back the remaining escrow *and* the MBR it paid at registration — nothing is stranded in the app account. Returns the refunded amount.

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  cancel(params?: CallParams<KeeperArgs['obj']['cancel(uint64)uint64'] | KeeperArgs['tuple']['cancel(uint64)uint64']>): KeeperComposer<[...TReturns, KeeperReturns['cancel(uint64)uint64'] | undefined]>

  /**
   * Calls the execute(uint64)uint64 ABI method.
   *
  * Execute a due upkeep (permissionless); pays the caller its fee.
  Returns the round the upkeep is next due.

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  execute(params?: CallParams<KeeperArgs['obj']['execute(uint64)uint64'] | KeeperArgs['tuple']['execute(uint64)uint64']>): KeeperComposer<[...TReturns, KeeperReturns['execute(uint64)uint64'] | undefined]>

  /**
   * Calls the opt_in_asset(pay,uint64,uint64)uint64 ABI method.
   *
  * Let the app account hold `asset`, so an upkeep can escrow a bonus.
  Permissionless, but tied to an upkeep that actually names the asset: an app that anyone could opt in to anything would accrete junk holdings for good, since there is no opt-out. The deposit is not refundable — reference-counting it would cost a box per asset and more code than the 0.1 ALGO it would ever return.

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  optInAsset(params?: CallParams<KeeperArgs['obj']['opt_in_asset(pay,uint64,uint64)uint64'] | KeeperArgs['tuple']['opt_in_asset(pay,uint64,uint64)uint64']>): KeeperComposer<[...TReturns, KeeperReturns['opt_in_asset(pay,uint64,uint64)uint64'] | undefined]>

  /**
   * Calls the top_up_asset(uint64,axfer)uint64 ABI method.
   *
  * Add ASA to an upkeep's bonus escrow; returns the new asset balance.
  Separate from `register` because an asset transfer cannot be an optional member of a transaction group: folding it in would make every ALGO-only registration carry a zero-amount transfer of an asset it does not use.

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  topUpAsset(params?: CallParams<KeeperArgs['obj']['top_up_asset(uint64,axfer)uint64'] | KeeperArgs['tuple']['top_up_asset(uint64,axfer)uint64']>): KeeperComposer<[...TReturns, KeeperReturns['top_up_asset(uint64,axfer)uint64'] | undefined]>

  /**
   * Gets available update methods
   */
  readonly update: {
    /**
     * Updates an existing instance of the Keeper smart contract using the update()void ABI method.
     *
     * @param args The arguments for the smart contract call
     * @param params Any additional parameters for the call
     * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
     */
    update(params?: CallParams<KeeperArgs['obj']['update()void'] | KeeperArgs['tuple']['update()void']>): KeeperComposer<[...TReturns, KeeperReturns['update()void'] | undefined]>
  }

  /**
   * Makes a clear_state call to an existing instance of the Keeper smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): KeeperComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): KeeperComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<KeeperComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<KeeperComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<KeeperComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<KeeperComposerResults<TReturns>>
}
export type KeeperComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

