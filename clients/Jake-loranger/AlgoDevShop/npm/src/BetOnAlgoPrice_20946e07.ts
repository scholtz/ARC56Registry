/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"BetOnAlgoPrice","desc":"","methods":[{"name":"createApplication","desc":"Create the application","args":[{"name":"price","type":"uint64","desc":"The target price of ALGO"},{"name":"date","type":"uint64","desc":"The target date (timestamp)"},{"name":"condition","type":"string","desc":"The condition (Over or Under)"},{"name":"amount","type":"uint64","desc":"The amount to bet (in ALGO)"}],"returns":{"type":"void"},"actions":{"create":["NoOp"],"call":[]}},{"name":"optIn","desc":"Opt-in to the contract (per user)","args":[],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]}},{"name":"placeBet","desc":"Place a bet on the contract","args":[{"name":"amount","type":"uint64","desc":"The amount to wager in ALGO"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]}},{"name":"settleBet","desc":"Settle the bet based on whether the user won or lost","args":[{"name":"isWinner","type":"bool","desc":"Boolean indicating if the sender is the winner"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]}},{"name":"deleteApplication","desc":"Method to delete the application\nThis method transfers any remaining funds back to the creator","args":[],"returns":{"type":"void"},"actions":{"create":[],"call":["DeleteApplication"]}}],"arcs":[4,56],"structs":{},"state":{"schema":{"global":{"bytes":2,"ints":3},"local":{"bytes":0,"ints":0}},"keys":{"global":{"price":{"key":"cHJpY2U=","keyType":"AVMBytes","valueType":"uint64"},"targetDate":{"key":"dGFyZ2V0RGF0ZQ==","keyType":"AVMBytes","valueType":"uint64"},"condition":{"key":"Y29uZGl0aW9u","keyType":"AVMBytes","valueType":"string"},"betAmount":{"key":"YmV0QW1vdW50","keyType":"AVMBytes","valueType":"uint64"},"settled":{"key":"c2V0dGxlZA==","keyType":"AVMBytes","valueType":"bool"}},"local":{},"box":{}},"maps":{"global":{},"local":{},"box":{}}},"bareActions":{"create":[],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[62],"errorMessage":"The requested action is not implemented in this contract. Are you using the correct OnComplete? Did you set your app ID?","teal":25},{"pc":[168],"errorMessage":"Creator cannot opt-in","teal":127},{"pc":[184],"errorMessage":"Insufficient balance to opt-in","teal":145},{"pc":[207],"errorMessage":"Creator cannot place a bet","teal":176},{"pc":[214],"errorMessage":"The bet has already been settled","teal":188},{"pc":[220],"errorMessage":"Bet amount must match the expected wager","teal":198},{"pc":[229],"errorMessage":"Insufficient balance to place the bet","teal":209},{"pc":[254],"errorMessage":"argument 0 (isWinner) for settleBet must be a bool","teal":248},{"pc":[271],"errorMessage":"The bet has already been settled","teal":275},{"pc":[354],"errorMessage":"Only the creator can delete the application","teal":392},{"pc":[400],"errorMessage":"this contract does not implement the given ABI method for create NoOp","teal":439},{"pc":[430],"errorMessage":"this contract does not implement the given ABI method for call NoOp","teal":449},{"pc":[444],"errorMessage":"this contract does not implement the given ABI method for call DeleteApplication","teal":457}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}}} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


/**
 * The argument types for the BetOnAlgoPrice contract
 */
export type BetOnAlgoPriceArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'createApplication(uint64,uint64,string,uint64)void': {
      /**
       * The target price of ALGO
       */
      price: bigint | number
      /**
       * The target date (timestamp)
       */
      date: bigint | number
      /**
       * The condition (Over or Under)
       */
      condition: string
      /**
       * The amount to bet (in ALGO)
       */
      amount: bigint | number
    }
    'optIn()void': Record<string, never>
    'placeBet(uint64)void': {
      /**
       * The amount to wager in ALGO
       */
      amount: bigint | number
    }
    'settleBet(bool)void': {
      /**
       * Boolean indicating if the sender is the winner
       */
      isWinner: boolean
    }
    'deleteApplication()void': Record<string, never>
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'createApplication(uint64,uint64,string,uint64)void': [price: bigint | number, date: bigint | number, condition: string, amount: bigint | number]
    'optIn()void': []
    'placeBet(uint64)void': [amount: bigint | number]
    'settleBet(bool)void': [isWinner: boolean]
    'deleteApplication()void': []
  }
}

/**
 * The return type for each method
 */
export type BetOnAlgoPriceReturns = {
  'createApplication(uint64,uint64,string,uint64)void': void
  'optIn()void': void
  'placeBet(uint64)void': void
  'settleBet(bool)void': void
  'deleteApplication()void': void
}

/**
 * Defines the types of available calls and state of the BetOnAlgoPrice smart contract.
 */
export type BetOnAlgoPriceTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'createApplication(uint64,uint64,string,uint64)void' | 'createApplication', {
      argsObj: BetOnAlgoPriceArgs['obj']['createApplication(uint64,uint64,string,uint64)void']
      argsTuple: BetOnAlgoPriceArgs['tuple']['createApplication(uint64,uint64,string,uint64)void']
      returns: BetOnAlgoPriceReturns['createApplication(uint64,uint64,string,uint64)void']
    }>
    & Record<'optIn()void' | 'optIn', {
      argsObj: BetOnAlgoPriceArgs['obj']['optIn()void']
      argsTuple: BetOnAlgoPriceArgs['tuple']['optIn()void']
      returns: BetOnAlgoPriceReturns['optIn()void']
    }>
    & Record<'placeBet(uint64)void' | 'placeBet', {
      argsObj: BetOnAlgoPriceArgs['obj']['placeBet(uint64)void']
      argsTuple: BetOnAlgoPriceArgs['tuple']['placeBet(uint64)void']
      returns: BetOnAlgoPriceReturns['placeBet(uint64)void']
    }>
    & Record<'settleBet(bool)void' | 'settleBet', {
      argsObj: BetOnAlgoPriceArgs['obj']['settleBet(bool)void']
      argsTuple: BetOnAlgoPriceArgs['tuple']['settleBet(bool)void']
      returns: BetOnAlgoPriceReturns['settleBet(bool)void']
    }>
    & Record<'deleteApplication()void' | 'deleteApplication', {
      argsObj: BetOnAlgoPriceArgs['obj']['deleteApplication()void']
      argsTuple: BetOnAlgoPriceArgs['tuple']['deleteApplication()void']
      returns: BetOnAlgoPriceReturns['deleteApplication()void']
    }>
  /**
   * Defines the shape of the state of the application.
   */
  state: {
    global: {
      keys: {
        price: bigint
        targetDate: bigint
        condition: string
        betAmount: bigint
        settled: boolean
      }
      maps: {}
    }
  }
}

/**
 * Defines the possible abi call signatures.
 */
export type BetOnAlgoPriceSignatures = keyof BetOnAlgoPriceTypes['methods']
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the BetOnAlgoPrice smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends BetOnAlgoPriceSignatures> = BetOnAlgoPriceTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the BetOnAlgoPrice smart contract to the method's return type
 */
export type MethodReturn<TSignature extends BetOnAlgoPriceSignatures> = BetOnAlgoPriceTypes['methods'][TSignature]['returns']

/**
 * Defines the shape of the keyed global state of the application.
 */
export type GlobalKeysState = BetOnAlgoPriceTypes['state']['global']['keys']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the BetOnAlgoPrice smart contract
 */
export abstract class BetOnAlgoPriceParamsFactory {
  /**
   * Constructs a no op call for the optIn()void ABI method
   *
   * Opt-in to the contract (per user)
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static optIn(params: CallParams<BetOnAlgoPriceArgs['obj']['optIn()void'] | BetOnAlgoPriceArgs['tuple']['optIn()void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'optIn()void' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the placeBet(uint64)void ABI method
   *
   * Place a bet on the contract
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static placeBet(params: CallParams<BetOnAlgoPriceArgs['obj']['placeBet(uint64)void'] | BetOnAlgoPriceArgs['tuple']['placeBet(uint64)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'placeBet(uint64)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.amount],
    }
  }
  /**
   * Constructs a no op call for the settleBet(bool)void ABI method
   *
   * Settle the bet based on whether the user won or lost
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static settleBet(params: CallParams<BetOnAlgoPriceArgs['obj']['settleBet(bool)void'] | BetOnAlgoPriceArgs['tuple']['settleBet(bool)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'settleBet(bool)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.isWinner],
    }
  }
}

/**
 * A client to make calls to the BetOnAlgoPrice smart contract
 */
export class BetOnAlgoPriceClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `BetOnAlgoPriceClient`
   *
   * @param appClient An `AppClient` instance which has been created with the BetOnAlgoPrice app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `BetOnAlgoPriceClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Returns a new `BetOnAlgoPriceClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<BetOnAlgoPriceClient> {
    return new BetOnAlgoPriceClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `BetOnAlgoPriceClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<BetOnAlgoPriceClient> {
    return new BetOnAlgoPriceClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Makes a clear_state call to an existing instance of the BetOnAlgoPrice smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the BetOnAlgoPrice smart contract using the `optIn()void` ABI method.
     *
     * Opt-in to the contract (per user)
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    optIn: (params: CallParams<BetOnAlgoPriceArgs['obj']['optIn()void'] | BetOnAlgoPriceArgs['tuple']['optIn()void']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(BetOnAlgoPriceParamsFactory.optIn(params))
    },

    /**
     * Makes a call to the BetOnAlgoPrice smart contract using the `placeBet(uint64)void` ABI method.
     *
     * Place a bet on the contract
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    placeBet: (params: CallParams<BetOnAlgoPriceArgs['obj']['placeBet(uint64)void'] | BetOnAlgoPriceArgs['tuple']['placeBet(uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(BetOnAlgoPriceParamsFactory.placeBet(params))
    },

    /**
     * Makes a call to the BetOnAlgoPrice smart contract using the `settleBet(bool)void` ABI method.
     *
     * Settle the bet based on whether the user won or lost
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    settleBet: (params: CallParams<BetOnAlgoPriceArgs['obj']['settleBet(bool)void'] | BetOnAlgoPriceArgs['tuple']['settleBet(bool)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(BetOnAlgoPriceParamsFactory.settleBet(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Makes a clear_state call to an existing instance of the BetOnAlgoPrice smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the BetOnAlgoPrice smart contract using the `optIn()void` ABI method.
     *
     * Opt-in to the contract (per user)
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    optIn: (params: CallParams<BetOnAlgoPriceArgs['obj']['optIn()void'] | BetOnAlgoPriceArgs['tuple']['optIn()void']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(BetOnAlgoPriceParamsFactory.optIn(params))
    },

    /**
     * Makes a call to the BetOnAlgoPrice smart contract using the `placeBet(uint64)void` ABI method.
     *
     * Place a bet on the contract
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    placeBet: (params: CallParams<BetOnAlgoPriceArgs['obj']['placeBet(uint64)void'] | BetOnAlgoPriceArgs['tuple']['placeBet(uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(BetOnAlgoPriceParamsFactory.placeBet(params))
    },

    /**
     * Makes a call to the BetOnAlgoPrice smart contract using the `settleBet(bool)void` ABI method.
     *
     * Settle the bet based on whether the user won or lost
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    settleBet: (params: CallParams<BetOnAlgoPriceArgs['obj']['settleBet(bool)void'] | BetOnAlgoPriceArgs['tuple']['settleBet(bool)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(BetOnAlgoPriceParamsFactory.settleBet(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Makes a clear_state call to an existing instance of the BetOnAlgoPrice smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the BetOnAlgoPrice smart contract using the `optIn()void` ABI method.
     *
     * Opt-in to the contract (per user)
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    optIn: async (params: CallParams<BetOnAlgoPriceArgs['obj']['optIn()void'] | BetOnAlgoPriceArgs['tuple']['optIn()void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(BetOnAlgoPriceParamsFactory.optIn(params))
      return {...result, return: result.return as unknown as (undefined | BetOnAlgoPriceReturns['optIn()void'])}
    },

    /**
     * Makes a call to the BetOnAlgoPrice smart contract using the `placeBet(uint64)void` ABI method.
     *
     * Place a bet on the contract
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    placeBet: async (params: CallParams<BetOnAlgoPriceArgs['obj']['placeBet(uint64)void'] | BetOnAlgoPriceArgs['tuple']['placeBet(uint64)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(BetOnAlgoPriceParamsFactory.placeBet(params))
      return {...result, return: result.return as unknown as (undefined | BetOnAlgoPriceReturns['placeBet(uint64)void'])}
    },

    /**
     * Makes a call to the BetOnAlgoPrice smart contract using the `settleBet(bool)void` ABI method.
     *
     * Settle the bet based on whether the user won or lost
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    settleBet: async (params: CallParams<BetOnAlgoPriceArgs['obj']['settleBet(bool)void'] | BetOnAlgoPriceArgs['tuple']['settleBet(bool)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(BetOnAlgoPriceParamsFactory.settleBet(params))
      return {...result, return: result.return as unknown as (undefined | BetOnAlgoPriceReturns['settleBet(bool)void'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new BetOnAlgoPriceClient(this.appClient.clone(params))
  }

  /**
   * Methods to access state for the current BetOnAlgoPrice app
   */
  state = {
    /**
     * Methods to access global state for the current BetOnAlgoPrice app
     */
    global: {
      /**
       * Get all current keyed values from global state
       */
      getAll: async (): Promise<Partial<Expand<GlobalKeysState>>> => {
        const result = await this.appClient.state.global.getAll()
        return {
          price: result.price,
          targetDate: result.targetDate,
          condition: result.condition,
          betAmount: result.betAmount,
          settled: result.settled,
        }
      },
      /**
       * Get the current value of the price key in global state
       */
      price: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("price")) as bigint | undefined },
      /**
       * Get the current value of the targetDate key in global state
       */
      targetDate: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("targetDate")) as bigint | undefined },
      /**
       * Get the current value of the condition key in global state
       */
      condition: async (): Promise<string | undefined> => { return (await this.appClient.state.global.getValue("condition")) as string | undefined },
      /**
       * Get the current value of the betAmount key in global state
       */
      betAmount: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("betAmount")) as bigint | undefined },
      /**
       * Get the current value of the settled key in global state
       */
      settled: async (): Promise<boolean | undefined> => { return (await this.appClient.state.global.getValue("settled")) as boolean | undefined },
    },
  }

  public newGroup(): BetOnAlgoPriceComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a optIn()void method call against the BetOnAlgoPrice contract
       */
      optIn(params: CallParams<BetOnAlgoPriceArgs['obj']['optIn()void'] | BetOnAlgoPriceArgs['tuple']['optIn()void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.optIn(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a placeBet(uint64)void method call against the BetOnAlgoPrice contract
       */
      placeBet(params: CallParams<BetOnAlgoPriceArgs['obj']['placeBet(uint64)void'] | BetOnAlgoPriceArgs['tuple']['placeBet(uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.placeBet(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a settleBet(bool)void method call against the BetOnAlgoPrice contract
       */
      settleBet(params: CallParams<BetOnAlgoPriceArgs['obj']['settleBet(bool)void'] | BetOnAlgoPriceArgs['tuple']['settleBet(bool)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.settleBet(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a clear state call to the BetOnAlgoPrice contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as BetOnAlgoPriceComposer
  }
}
export type BetOnAlgoPriceComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the optIn()void ABI method.
   *
   * Opt-in to the contract (per user)
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  optIn(params?: CallParams<BetOnAlgoPriceArgs['obj']['optIn()void'] | BetOnAlgoPriceArgs['tuple']['optIn()void']>): BetOnAlgoPriceComposer<[...TReturns, BetOnAlgoPriceReturns['optIn()void'] | undefined]>

  /**
   * Calls the placeBet(uint64)void ABI method.
   *
   * Place a bet on the contract
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  placeBet(params?: CallParams<BetOnAlgoPriceArgs['obj']['placeBet(uint64)void'] | BetOnAlgoPriceArgs['tuple']['placeBet(uint64)void']>): BetOnAlgoPriceComposer<[...TReturns, BetOnAlgoPriceReturns['placeBet(uint64)void'] | undefined]>

  /**
   * Calls the settleBet(bool)void ABI method.
   *
   * Settle the bet based on whether the user won or lost
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  settleBet(params?: CallParams<BetOnAlgoPriceArgs['obj']['settleBet(bool)void'] | BetOnAlgoPriceArgs['tuple']['settleBet(bool)void']>): BetOnAlgoPriceComposer<[...TReturns, BetOnAlgoPriceReturns['settleBet(bool)void'] | undefined]>

  /**
   * Makes a clear_state call to an existing instance of the BetOnAlgoPrice smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): BetOnAlgoPriceComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): BetOnAlgoPriceComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<BetOnAlgoPriceComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<BetOnAlgoPriceComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<BetOnAlgoPriceComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<BetOnAlgoPriceComposerResults<TReturns>>
}
export type BetOnAlgoPriceComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

