/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"AlgoHealx","structs":{},"methods":[{"name":"create_application","args":[{"type":"address","name":"owner_addr"},{"type":"address","name":"government_addr"}],"returns":{"type":"void"},"actions":{"create":["NoOp"],"call":[]},"readonly":false,"events":[],"recommendations":{}},{"name":"add_manufacturer","args":[{"type":"address","name":"manufacturer_addr"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}},{"name":"register_batch","args":[{"type":"string","name":"batch_details"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}},{"name":"get_batch_details","args":[{"type":"uint64","name":"index"}],"returns":{"type":"string"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}}],"arcs":[22,28],"networks":{},"state":{"schema":{"global":{"ints":2,"bytes":2},"local":{"ints":0,"bytes":0}},"keys":{"global":{"owner":{"keyType":"AVMString","valueType":"address","key":"b3duZXI="},"government_address":{"keyType":"AVMString","valueType":"address","key":"Z292ZXJubWVudF9hZGRyZXNz"},"batch_count":{"keyType":"AVMString","valueType":"AVMUint64","key":"YmF0Y2hfY291bnQ="},"manufacturer_count":{"keyType":"AVMString","valueType":"AVMUint64","key":"bWFudWZhY3R1cmVyX2NvdW50"}},"local":{},"box":{}},"maps":{"global":{},"local":{},"box":{"manufacturers":{"keyType":"uint64","valueType":"address","prefix":"bWFudV8="},"batches":{"keyType":"uint64","valueType":"AVMString","prefix":"YmF0Y2hf"}}}},"bareActions":{"create":[],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[300],"errorMessage":"Batch limit reached"},{"pc":[349],"errorMessage":"Invalid index"},{"pc":[291],"errorMessage":"Not whitelisted"},{"pc":[72],"errorMessage":"OnCompletion must be NoOp"},{"pc":[195],"errorMessage":"Unauthorized"},{"pc":[187],"errorMessage":"Whitelist full"},{"pc":[295,345],"errorMessage":"check self.batch_count exists"},{"pc":[182,232],"errorMessage":"check self.manufacturer_count exists"},{"pc":[193],"errorMessage":"check self.owner exists"},{"pc":[224],"errorMessage":"invalid number of bytes for (len+utf8[])"},{"pc":[340],"errorMessage":"invalid number of bytes for uint64"},{"pc":[128,136,178],"errorMessage":"invalid number of bytes for uint8[32]"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"events":[]} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


/**
 * The argument types for the AlgoHealx contract
 */
export type AlgoHealxArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'create_application(address,address)void': {
      ownerAddr: string
      governmentAddr: string
    }
    'add_manufacturer(address)void': {
      manufacturerAddr: string
    }
    'register_batch(string)void': {
      batchDetails: string
    }
    'get_batch_details(uint64)string': {
      index: bigint | number
    }
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'create_application(address,address)void': [ownerAddr: string, governmentAddr: string]
    'add_manufacturer(address)void': [manufacturerAddr: string]
    'register_batch(string)void': [batchDetails: string]
    'get_batch_details(uint64)string': [index: bigint | number]
  }
}

/**
 * The return type for each method
 */
export type AlgoHealxReturns = {
  'create_application(address,address)void': void
  'add_manufacturer(address)void': void
  'register_batch(string)void': void
  'get_batch_details(uint64)string': string
}

/**
 * Defines the types of available calls and state of the AlgoHealx smart contract.
 */
export type AlgoHealxTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'create_application(address,address)void' | 'create_application', {
      argsObj: AlgoHealxArgs['obj']['create_application(address,address)void']
      argsTuple: AlgoHealxArgs['tuple']['create_application(address,address)void']
      returns: AlgoHealxReturns['create_application(address,address)void']
    }>
    & Record<'add_manufacturer(address)void' | 'add_manufacturer', {
      argsObj: AlgoHealxArgs['obj']['add_manufacturer(address)void']
      argsTuple: AlgoHealxArgs['tuple']['add_manufacturer(address)void']
      returns: AlgoHealxReturns['add_manufacturer(address)void']
    }>
    & Record<'register_batch(string)void' | 'register_batch', {
      argsObj: AlgoHealxArgs['obj']['register_batch(string)void']
      argsTuple: AlgoHealxArgs['tuple']['register_batch(string)void']
      returns: AlgoHealxReturns['register_batch(string)void']
    }>
    & Record<'get_batch_details(uint64)string' | 'get_batch_details', {
      argsObj: AlgoHealxArgs['obj']['get_batch_details(uint64)string']
      argsTuple: AlgoHealxArgs['tuple']['get_batch_details(uint64)string']
      returns: AlgoHealxReturns['get_batch_details(uint64)string']
    }>
  /**
   * Defines the shape of the state of the application.
   */
  state: {
    global: {
      keys: {
        owner: string
        governmentAddress: string
        batchCount: bigint
        manufacturerCount: bigint
      }
      maps: {}
    }
    box: {
      keys: {}
      maps: {
        manufacturers: Map<bigint | number, string>
        batches: Map<bigint | number, string>
      }
    }
  }
}

/**
 * Defines the possible abi call signatures.
 */
export type AlgoHealxSignatures = keyof AlgoHealxTypes['methods']
/**
 * Defines the possible abi call signatures for methods that return a non-void value.
 */
export type AlgoHealxNonVoidMethodSignatures = keyof AlgoHealxTypes['methods'] extends infer T ? T extends keyof AlgoHealxTypes['methods'] ? MethodReturn<T> extends void ? never : T  : never : never
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the AlgoHealx smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends AlgoHealxSignatures> = AlgoHealxTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the AlgoHealx smart contract to the method's return type
 */
export type MethodReturn<TSignature extends AlgoHealxSignatures> = AlgoHealxTypes['methods'][TSignature]['returns']

/**
 * Defines the shape of the keyed global state of the application.
 */
export type GlobalKeysState = AlgoHealxTypes['state']['global']['keys']

/**
 * Defines the shape of the keyed box state of the application.
 */
export type BoxKeysState = AlgoHealxTypes['state']['box']['keys']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the AlgoHealx smart contract
 */
export abstract class AlgoHealxParamsFactory {
  /**
   * Constructs a no op call for the add_manufacturer(address)void ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static addManufacturer(params: CallParams<AlgoHealxArgs['obj']['add_manufacturer(address)void'] | AlgoHealxArgs['tuple']['add_manufacturer(address)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'add_manufacturer(address)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.manufacturerAddr],
    }
  }
  /**
   * Constructs a no op call for the register_batch(string)void ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static registerBatch(params: CallParams<AlgoHealxArgs['obj']['register_batch(string)void'] | AlgoHealxArgs['tuple']['register_batch(string)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'register_batch(string)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.batchDetails],
    }
  }
  /**
   * Constructs a no op call for the get_batch_details(uint64)string ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static getBatchDetails(params: CallParams<AlgoHealxArgs['obj']['get_batch_details(uint64)string'] | AlgoHealxArgs['tuple']['get_batch_details(uint64)string']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'get_batch_details(uint64)string' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.index],
    }
  }
}

/**
 * A client to make calls to the AlgoHealx smart contract
 */
export class AlgoHealxClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `AlgoHealxClient`
   *
   * @param appClient An `AppClient` instance which has been created with the AlgoHealx app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `AlgoHealxClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Checks for decode errors on the given return value and maps the return value to the return type for the given method
   * @returns The typed return value or undefined if there was no value
   */
  decodeReturnValue<TSignature extends AlgoHealxNonVoidMethodSignatures>(method: TSignature, returnValue: ABIReturn | undefined) {
    return returnValue !== undefined ? getArc56ReturnValue<MethodReturn<TSignature>>(returnValue, this.appClient.getABIMethod(method), APP_SPEC.structs) : undefined
  }

  /**
   * Returns a new `AlgoHealxClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<AlgoHealxClient> {
    return new AlgoHealxClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `AlgoHealxClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<AlgoHealxClient> {
    return new AlgoHealxClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Makes a clear_state call to an existing instance of the AlgoHealx smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the AlgoHealx smart contract using the `add_manufacturer(address)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    addManufacturer: (params: CallParams<AlgoHealxArgs['obj']['add_manufacturer(address)void'] | AlgoHealxArgs['tuple']['add_manufacturer(address)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(AlgoHealxParamsFactory.addManufacturer(params))
    },

    /**
     * Makes a call to the AlgoHealx smart contract using the `register_batch(string)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    registerBatch: (params: CallParams<AlgoHealxArgs['obj']['register_batch(string)void'] | AlgoHealxArgs['tuple']['register_batch(string)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(AlgoHealxParamsFactory.registerBatch(params))
    },

    /**
     * Makes a call to the AlgoHealx smart contract using the `get_batch_details(uint64)string` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    getBatchDetails: (params: CallParams<AlgoHealxArgs['obj']['get_batch_details(uint64)string'] | AlgoHealxArgs['tuple']['get_batch_details(uint64)string']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(AlgoHealxParamsFactory.getBatchDetails(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Makes a clear_state call to an existing instance of the AlgoHealx smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the AlgoHealx smart contract using the `add_manufacturer(address)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    addManufacturer: (params: CallParams<AlgoHealxArgs['obj']['add_manufacturer(address)void'] | AlgoHealxArgs['tuple']['add_manufacturer(address)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(AlgoHealxParamsFactory.addManufacturer(params))
    },

    /**
     * Makes a call to the AlgoHealx smart contract using the `register_batch(string)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    registerBatch: (params: CallParams<AlgoHealxArgs['obj']['register_batch(string)void'] | AlgoHealxArgs['tuple']['register_batch(string)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(AlgoHealxParamsFactory.registerBatch(params))
    },

    /**
     * Makes a call to the AlgoHealx smart contract using the `get_batch_details(uint64)string` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    getBatchDetails: (params: CallParams<AlgoHealxArgs['obj']['get_batch_details(uint64)string'] | AlgoHealxArgs['tuple']['get_batch_details(uint64)string']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(AlgoHealxParamsFactory.getBatchDetails(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Makes a clear_state call to an existing instance of the AlgoHealx smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the AlgoHealx smart contract using the `add_manufacturer(address)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    addManufacturer: async (params: CallParams<AlgoHealxArgs['obj']['add_manufacturer(address)void'] | AlgoHealxArgs['tuple']['add_manufacturer(address)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(AlgoHealxParamsFactory.addManufacturer(params))
      return {...result, return: result.return as unknown as (undefined | AlgoHealxReturns['add_manufacturer(address)void'])}
    },

    /**
     * Makes a call to the AlgoHealx smart contract using the `register_batch(string)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    registerBatch: async (params: CallParams<AlgoHealxArgs['obj']['register_batch(string)void'] | AlgoHealxArgs['tuple']['register_batch(string)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(AlgoHealxParamsFactory.registerBatch(params))
      return {...result, return: result.return as unknown as (undefined | AlgoHealxReturns['register_batch(string)void'])}
    },

    /**
     * Makes a call to the AlgoHealx smart contract using the `get_batch_details(uint64)string` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    getBatchDetails: async (params: CallParams<AlgoHealxArgs['obj']['get_batch_details(uint64)string'] | AlgoHealxArgs['tuple']['get_batch_details(uint64)string']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(AlgoHealxParamsFactory.getBatchDetails(params))
      return {...result, return: result.return as unknown as (undefined | AlgoHealxReturns['get_batch_details(uint64)string'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new AlgoHealxClient(this.appClient.clone(params))
  }

  /**
   * Makes a readonly (simulated) call to the AlgoHealx smart contract using the `get_batch_details(uint64)string` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async getBatchDetails(params: CallParams<AlgoHealxArgs['obj']['get_batch_details(uint64)string'] | AlgoHealxArgs['tuple']['get_batch_details(uint64)string']>) {
    const result = await this.appClient.send.call(AlgoHealxParamsFactory.getBatchDetails(params))
    return result.return as unknown as AlgoHealxReturns['get_batch_details(uint64)string']
  }

  /**
   * Methods to access state for the current AlgoHealx app
   */
  state = {
    /**
     * Methods to access global state for the current AlgoHealx app
     */
    global: {
      /**
       * Get all current keyed values from global state
       */
      getAll: async (): Promise<Partial<Expand<GlobalKeysState>>> => {
        const result = await this.appClient.state.global.getAll()
        return {
          owner: result.owner,
          governmentAddress: result.government_address,
          batchCount: result.batch_count,
          manufacturerCount: result.manufacturer_count,
        }
      },
      /**
       * Get the current value of the owner key in global state
       */
      owner: async (): Promise<string | undefined> => { return (await this.appClient.state.global.getValue("owner")) as string | undefined },
      /**
       * Get the current value of the government_address key in global state
       */
      governmentAddress: async (): Promise<string | undefined> => { return (await this.appClient.state.global.getValue("government_address")) as string | undefined },
      /**
       * Get the current value of the batch_count key in global state
       */
      batchCount: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("batch_count")) as bigint | undefined },
      /**
       * Get the current value of the manufacturer_count key in global state
       */
      manufacturerCount: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("manufacturer_count")) as bigint | undefined },
    },
    /**
     * Methods to access box state for the current AlgoHealx app
     */
    box: {
      /**
       * Get all current keyed values from box state
       */
      getAll: async (): Promise<Partial<Expand<BoxKeysState>>> => {
        const result = await this.appClient.state.box.getAll()
        return {
        }
      },
      /**
       * Get values from the manufacturers map in box state
       */
      manufacturers: {
        /**
         * Get all current values of the manufacturers map in box state
         */
        getMap: async (): Promise<Map<bigint, string>> => { return (await this.appClient.state.box.getMap("manufacturers")) as Map<bigint, string> },
        /**
         * Get a current value of the manufacturers map by key from box state
         */
        value: async (key: bigint | number): Promise<string | undefined> => { return await this.appClient.state.box.getMapValue("manufacturers", key) as string | undefined },
      },
      /**
       * Get values from the batches map in box state
       */
      batches: {
        /**
         * Get all current values of the batches map in box state
         */
        getMap: async (): Promise<Map<bigint, string>> => { return (await this.appClient.state.box.getMap("batches")) as Map<bigint, string> },
        /**
         * Get a current value of the batches map by key from box state
         */
        value: async (key: bigint | number): Promise<string | undefined> => { return await this.appClient.state.box.getMapValue("batches", key) as string | undefined },
      },
    },
  }

  public newGroup(): AlgoHealxComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a add_manufacturer(address)void method call against the AlgoHealx contract
       */
      addManufacturer(params: CallParams<AlgoHealxArgs['obj']['add_manufacturer(address)void'] | AlgoHealxArgs['tuple']['add_manufacturer(address)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.addManufacturer(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a register_batch(string)void method call against the AlgoHealx contract
       */
      registerBatch(params: CallParams<AlgoHealxArgs['obj']['register_batch(string)void'] | AlgoHealxArgs['tuple']['register_batch(string)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.registerBatch(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a get_batch_details(uint64)string method call against the AlgoHealx contract
       */
      getBatchDetails(params: CallParams<AlgoHealxArgs['obj']['get_batch_details(uint64)string'] | AlgoHealxArgs['tuple']['get_batch_details(uint64)string']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.getBatchDetails(params)))
        resultMappers.push((v) => client.decodeReturnValue('get_batch_details(uint64)string', v))
        return this
      },
      /**
       * Add a clear state call to the AlgoHealx contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as AlgoHealxComposer
  }
}
export type AlgoHealxComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the add_manufacturer(address)void ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  addManufacturer(params?: CallParams<AlgoHealxArgs['obj']['add_manufacturer(address)void'] | AlgoHealxArgs['tuple']['add_manufacturer(address)void']>): AlgoHealxComposer<[...TReturns, AlgoHealxReturns['add_manufacturer(address)void'] | undefined]>

  /**
   * Calls the register_batch(string)void ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  registerBatch(params?: CallParams<AlgoHealxArgs['obj']['register_batch(string)void'] | AlgoHealxArgs['tuple']['register_batch(string)void']>): AlgoHealxComposer<[...TReturns, AlgoHealxReturns['register_batch(string)void'] | undefined]>

  /**
   * Calls the get_batch_details(uint64)string ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  getBatchDetails(params?: CallParams<AlgoHealxArgs['obj']['get_batch_details(uint64)string'] | AlgoHealxArgs['tuple']['get_batch_details(uint64)string']>): AlgoHealxComposer<[...TReturns, AlgoHealxReturns['get_batch_details(uint64)string'] | undefined]>

  /**
   * Makes a clear_state call to an existing instance of the AlgoHealx smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): AlgoHealxComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): AlgoHealxComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<AlgoHealxComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<AlgoHealxComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<AlgoHealxComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<AlgoHealxComposerResults<TReturns>>
}
export type AlgoHealxComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

