/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"LootBoxCommitReveal","structs":{},"methods":[{"name":"createApplication","args":[{"type":"address","name":"treasury"},{"type":"uint64","name":"price"},{"type":"uint64","name":"beaconApp"},{"type":"uint64","name":"beaconCadence"}],"returns":{"type":"void"},"actions":{"create":["NoOp"],"call":[]},"readonly":false,"events":[],"recommendations":{}},{"name":"configure","args":[{"type":"address","name":"treasury"},{"type":"uint64","name":"price"},{"type":"uint64","name":"beaconApp"},{"type":"uint64","name":"beaconCadence"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}},{"name":"commit","args":[{"type":"pay","name":"payment"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[{"name":"Committed","args":[{"type":"address","name":"account"},{"type":"uint64","name":"target"}]}],"recommendations":{}},{"name":"reveal","args":[],"returns":{"type":"uint64"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[{"name":"Revealed","args":[{"type":"address","name":"account"},{"type":"uint64","name":"target"},{"type":"uint64","name":"value"}]}],"recommendations":{}},{"name":"reclaim","args":[{"type":"address","name":"account"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}},{"name":"withdraw","args":[{"type":"uint64","name":"amount"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}}],"arcs":[22,28],"networks":{},"state":{"schema":{"global":{"ints":3,"bytes":1},"local":{"ints":0,"bytes":0}},"keys":{"global":{"treasuryAddress":{"keyType":"AVMString","valueType":"address","key":"dHJlYXN1cnk="},"cratePrice":{"keyType":"AVMString","valueType":"AVMUint64","key":"cHJpY2U="},"beaconApp":{"keyType":"AVMString","valueType":"AVMUint64","key":"YmVhY29u"},"beaconCadence":{"keyType":"AVMString","valueType":"AVMUint64","key":"Y2FkZW5jZQ=="}},"local":{},"box":{}},"maps":{"global":{},"local":{},"box":{"commitTarget":{"keyType":"address","valueType":"uint64","prefix":"Yw=="}}}},"bareActions":{"create":[],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[246],"errorMessage":"Active commit exists — reveal or reclaim first"},{"pc":[459],"errorMessage":"Beacon app id is required"},{"pc":[462],"errorMessage":"Beacon cadence must be positive"},{"pc":[298],"errorMessage":"Beacon round not reached yet"},{"pc":[361],"errorMessage":"Bytes has valid prefix"},{"pc":[308],"errorMessage":"Commit expired — call reclaim() and recommit"},{"pc":[415],"errorMessage":"Commit has not expired yet"},{"pc":[456],"errorMessage":"Crate price must be positive"},{"pc":[288],"errorMessage":"No active commit to reveal"},{"pc":[402],"errorMessage":"No commit to reclaim"},{"pc":[49],"errorMessage":"OnCompletion must be NoOp"},{"pc":[192],"errorMessage":"Only the creator can configure"},{"pc":[434],"errorMessage":"Only the creator can withdraw"},{"pc":[229],"errorMessage":"Payment is below the crate price"},{"pc":[219],"errorMessage":"Payment must go to the treasury"},{"pc":[236],"errorMessage":"Payment sender must match the caller"},{"pc":[217,227,252,317],"errorMessage":"check GlobalState exists"},{"pc":[119,159,394],"errorMessage":"invalid number of bytes for arc4.static_array<arc4.uint8, 32>"},{"pc":[127,136,145,167,176,185,427],"errorMessage":"invalid number of bytes for arc4.uint64"},{"pc":[207],"errorMessage":"transaction type is pay"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"events":[{"name":"Committed","args":[{"type":"address","name":"account"},{"type":"uint64","name":"target"}]},{"name":"Revealed","args":[{"type":"address","name":"account"},{"type":"uint64","name":"target"},{"type":"uint64","name":"value"}]}]} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


/**
 * The argument types for the LootBoxCommitReveal contract
 */
export type LootBoxCommitRevealArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'createApplication(address,uint64,uint64,uint64)void': {
      treasury: string
      price: bigint | number
      beaconApp: bigint | number
      beaconCadence: bigint | number
    }
    'configure(address,uint64,uint64,uint64)void': {
      treasury: string
      price: bigint | number
      beaconApp: bigint | number
      beaconCadence: bigint | number
    }
    'commit(pay)void': {
      payment: AppMethodCallTransactionArgument
    }
    'reveal()uint64': Record<string, never>
    'reclaim(address)void': {
      account: string
    }
    'withdraw(uint64)void': {
      amount: bigint | number
    }
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'createApplication(address,uint64,uint64,uint64)void': [treasury: string, price: bigint | number, beaconApp: bigint | number, beaconCadence: bigint | number]
    'configure(address,uint64,uint64,uint64)void': [treasury: string, price: bigint | number, beaconApp: bigint | number, beaconCadence: bigint | number]
    'commit(pay)void': [payment: AppMethodCallTransactionArgument]
    'reveal()uint64': []
    'reclaim(address)void': [account: string]
    'withdraw(uint64)void': [amount: bigint | number]
  }
}

/**
 * The return type for each method
 */
export type LootBoxCommitRevealReturns = {
  'createApplication(address,uint64,uint64,uint64)void': void
  'configure(address,uint64,uint64,uint64)void': void
  'commit(pay)void': void
  'reveal()uint64': bigint
  'reclaim(address)void': void
  'withdraw(uint64)void': void
}

/**
 * Defines the types of available calls and state of the LootBoxCommitReveal smart contract.
 */
export type LootBoxCommitRevealTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'createApplication(address,uint64,uint64,uint64)void' | 'createApplication', {
      argsObj: LootBoxCommitRevealArgs['obj']['createApplication(address,uint64,uint64,uint64)void']
      argsTuple: LootBoxCommitRevealArgs['tuple']['createApplication(address,uint64,uint64,uint64)void']
      returns: LootBoxCommitRevealReturns['createApplication(address,uint64,uint64,uint64)void']
    }>
    & Record<'configure(address,uint64,uint64,uint64)void' | 'configure', {
      argsObj: LootBoxCommitRevealArgs['obj']['configure(address,uint64,uint64,uint64)void']
      argsTuple: LootBoxCommitRevealArgs['tuple']['configure(address,uint64,uint64,uint64)void']
      returns: LootBoxCommitRevealReturns['configure(address,uint64,uint64,uint64)void']
    }>
    & Record<'commit(pay)void' | 'commit', {
      argsObj: LootBoxCommitRevealArgs['obj']['commit(pay)void']
      argsTuple: LootBoxCommitRevealArgs['tuple']['commit(pay)void']
      returns: LootBoxCommitRevealReturns['commit(pay)void']
    }>
    & Record<'reveal()uint64' | 'reveal', {
      argsObj: LootBoxCommitRevealArgs['obj']['reveal()uint64']
      argsTuple: LootBoxCommitRevealArgs['tuple']['reveal()uint64']
      returns: LootBoxCommitRevealReturns['reveal()uint64']
    }>
    & Record<'reclaim(address)void' | 'reclaim', {
      argsObj: LootBoxCommitRevealArgs['obj']['reclaim(address)void']
      argsTuple: LootBoxCommitRevealArgs['tuple']['reclaim(address)void']
      returns: LootBoxCommitRevealReturns['reclaim(address)void']
    }>
    & Record<'withdraw(uint64)void' | 'withdraw', {
      argsObj: LootBoxCommitRevealArgs['obj']['withdraw(uint64)void']
      argsTuple: LootBoxCommitRevealArgs['tuple']['withdraw(uint64)void']
      returns: LootBoxCommitRevealReturns['withdraw(uint64)void']
    }>
  /**
   * Defines the shape of the state of the application.
   */
  state: {
    global: {
      keys: {
        treasuryAddress: string
        cratePrice: bigint
        beaconApp: bigint
        beaconCadence: bigint
      }
      maps: {}
    }
    box: {
      keys: {}
      maps: {
        commitTarget: Map<string, bigint>
      }
    }
  }
}

/**
 * Defines the possible abi call signatures.
 */
export type LootBoxCommitRevealSignatures = keyof LootBoxCommitRevealTypes['methods']
/**
 * Defines the possible abi call signatures for methods that return a non-void value.
 */
export type LootBoxCommitRevealNonVoidMethodSignatures = keyof LootBoxCommitRevealTypes['methods'] extends infer T ? T extends keyof LootBoxCommitRevealTypes['methods'] ? MethodReturn<T> extends void ? never : T  : never : never
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the LootBoxCommitReveal smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends LootBoxCommitRevealSignatures> = LootBoxCommitRevealTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the LootBoxCommitReveal smart contract to the method's return type
 */
export type MethodReturn<TSignature extends LootBoxCommitRevealSignatures> = LootBoxCommitRevealTypes['methods'][TSignature]['returns']

/**
 * Defines the shape of the keyed global state of the application.
 */
export type GlobalKeysState = LootBoxCommitRevealTypes['state']['global']['keys']

/**
 * Defines the shape of the keyed box state of the application.
 */
export type BoxKeysState = LootBoxCommitRevealTypes['state']['box']['keys']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the LootBoxCommitReveal smart contract
 */
export abstract class LootBoxCommitRevealParamsFactory {
  /**
   * Constructs a no op call for the configure(address,uint64,uint64,uint64)void ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static configure(params: CallParams<LootBoxCommitRevealArgs['obj']['configure(address,uint64,uint64,uint64)void'] | LootBoxCommitRevealArgs['tuple']['configure(address,uint64,uint64,uint64)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'configure(address,uint64,uint64,uint64)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.treasury, params.args.price, params.args.beaconApp, params.args.beaconCadence],
    }
  }
  /**
   * Constructs a no op call for the commit(pay)void ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static commit(params: CallParams<LootBoxCommitRevealArgs['obj']['commit(pay)void'] | LootBoxCommitRevealArgs['tuple']['commit(pay)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'commit(pay)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.payment],
    }
  }
  /**
   * Constructs a no op call for the reveal()uint64 ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static reveal(params: CallParams<LootBoxCommitRevealArgs['obj']['reveal()uint64'] | LootBoxCommitRevealArgs['tuple']['reveal()uint64']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'reveal()uint64' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the reclaim(address)void ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static reclaim(params: CallParams<LootBoxCommitRevealArgs['obj']['reclaim(address)void'] | LootBoxCommitRevealArgs['tuple']['reclaim(address)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'reclaim(address)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.account],
    }
  }
  /**
   * Constructs a no op call for the withdraw(uint64)void ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static withdraw(params: CallParams<LootBoxCommitRevealArgs['obj']['withdraw(uint64)void'] | LootBoxCommitRevealArgs['tuple']['withdraw(uint64)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'withdraw(uint64)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.amount],
    }
  }
}

/**
 * A client to make calls to the LootBoxCommitReveal smart contract
 */
export class LootBoxCommitRevealClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `LootBoxCommitRevealClient`
   *
   * @param appClient An `AppClient` instance which has been created with the LootBoxCommitReveal app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `LootBoxCommitRevealClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Checks for decode errors on the given return value and maps the return value to the return type for the given method
   * @returns The typed return value or undefined if there was no value
   */
  decodeReturnValue<TSignature extends LootBoxCommitRevealNonVoidMethodSignatures>(method: TSignature, returnValue: ABIReturn | undefined) {
    return returnValue !== undefined ? getArc56ReturnValue<MethodReturn<TSignature>>(returnValue, this.appClient.getABIMethod(method), APP_SPEC.structs) : undefined
  }

  /**
   * Returns a new `LootBoxCommitRevealClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<LootBoxCommitRevealClient> {
    return new LootBoxCommitRevealClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `LootBoxCommitRevealClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<LootBoxCommitRevealClient> {
    return new LootBoxCommitRevealClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Makes a clear_state call to an existing instance of the LootBoxCommitReveal smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the LootBoxCommitReveal smart contract using the `configure(address,uint64,uint64,uint64)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    configure: (params: CallParams<LootBoxCommitRevealArgs['obj']['configure(address,uint64,uint64,uint64)void'] | LootBoxCommitRevealArgs['tuple']['configure(address,uint64,uint64,uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(LootBoxCommitRevealParamsFactory.configure(params))
    },

    /**
     * Makes a call to the LootBoxCommitReveal smart contract using the `commit(pay)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    commit: (params: CallParams<LootBoxCommitRevealArgs['obj']['commit(pay)void'] | LootBoxCommitRevealArgs['tuple']['commit(pay)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(LootBoxCommitRevealParamsFactory.commit(params))
    },

    /**
     * Makes a call to the LootBoxCommitReveal smart contract using the `reveal()uint64` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    reveal: (params: CallParams<LootBoxCommitRevealArgs['obj']['reveal()uint64'] | LootBoxCommitRevealArgs['tuple']['reveal()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(LootBoxCommitRevealParamsFactory.reveal(params))
    },

    /**
     * Makes a call to the LootBoxCommitReveal smart contract using the `reclaim(address)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    reclaim: (params: CallParams<LootBoxCommitRevealArgs['obj']['reclaim(address)void'] | LootBoxCommitRevealArgs['tuple']['reclaim(address)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(LootBoxCommitRevealParamsFactory.reclaim(params))
    },

    /**
     * Makes a call to the LootBoxCommitReveal smart contract using the `withdraw(uint64)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    withdraw: (params: CallParams<LootBoxCommitRevealArgs['obj']['withdraw(uint64)void'] | LootBoxCommitRevealArgs['tuple']['withdraw(uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(LootBoxCommitRevealParamsFactory.withdraw(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Makes a clear_state call to an existing instance of the LootBoxCommitReveal smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the LootBoxCommitReveal smart contract using the `configure(address,uint64,uint64,uint64)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    configure: (params: CallParams<LootBoxCommitRevealArgs['obj']['configure(address,uint64,uint64,uint64)void'] | LootBoxCommitRevealArgs['tuple']['configure(address,uint64,uint64,uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(LootBoxCommitRevealParamsFactory.configure(params))
    },

    /**
     * Makes a call to the LootBoxCommitReveal smart contract using the `commit(pay)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    commit: (params: CallParams<LootBoxCommitRevealArgs['obj']['commit(pay)void'] | LootBoxCommitRevealArgs['tuple']['commit(pay)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(LootBoxCommitRevealParamsFactory.commit(params))
    },

    /**
     * Makes a call to the LootBoxCommitReveal smart contract using the `reveal()uint64` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    reveal: (params: CallParams<LootBoxCommitRevealArgs['obj']['reveal()uint64'] | LootBoxCommitRevealArgs['tuple']['reveal()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(LootBoxCommitRevealParamsFactory.reveal(params))
    },

    /**
     * Makes a call to the LootBoxCommitReveal smart contract using the `reclaim(address)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    reclaim: (params: CallParams<LootBoxCommitRevealArgs['obj']['reclaim(address)void'] | LootBoxCommitRevealArgs['tuple']['reclaim(address)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(LootBoxCommitRevealParamsFactory.reclaim(params))
    },

    /**
     * Makes a call to the LootBoxCommitReveal smart contract using the `withdraw(uint64)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    withdraw: (params: CallParams<LootBoxCommitRevealArgs['obj']['withdraw(uint64)void'] | LootBoxCommitRevealArgs['tuple']['withdraw(uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(LootBoxCommitRevealParamsFactory.withdraw(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Makes a clear_state call to an existing instance of the LootBoxCommitReveal smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the LootBoxCommitReveal smart contract using the `configure(address,uint64,uint64,uint64)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    configure: async (params: CallParams<LootBoxCommitRevealArgs['obj']['configure(address,uint64,uint64,uint64)void'] | LootBoxCommitRevealArgs['tuple']['configure(address,uint64,uint64,uint64)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(LootBoxCommitRevealParamsFactory.configure(params))
      return {...result, return: result.return as unknown as (undefined | LootBoxCommitRevealReturns['configure(address,uint64,uint64,uint64)void'])}
    },

    /**
     * Makes a call to the LootBoxCommitReveal smart contract using the `commit(pay)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    commit: async (params: CallParams<LootBoxCommitRevealArgs['obj']['commit(pay)void'] | LootBoxCommitRevealArgs['tuple']['commit(pay)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(LootBoxCommitRevealParamsFactory.commit(params))
      return {...result, return: result.return as unknown as (undefined | LootBoxCommitRevealReturns['commit(pay)void'])}
    },

    /**
     * Makes a call to the LootBoxCommitReveal smart contract using the `reveal()uint64` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    reveal: async (params: CallParams<LootBoxCommitRevealArgs['obj']['reveal()uint64'] | LootBoxCommitRevealArgs['tuple']['reveal()uint64']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(LootBoxCommitRevealParamsFactory.reveal(params))
      return {...result, return: result.return as unknown as (undefined | LootBoxCommitRevealReturns['reveal()uint64'])}
    },

    /**
     * Makes a call to the LootBoxCommitReveal smart contract using the `reclaim(address)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    reclaim: async (params: CallParams<LootBoxCommitRevealArgs['obj']['reclaim(address)void'] | LootBoxCommitRevealArgs['tuple']['reclaim(address)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(LootBoxCommitRevealParamsFactory.reclaim(params))
      return {...result, return: result.return as unknown as (undefined | LootBoxCommitRevealReturns['reclaim(address)void'])}
    },

    /**
     * Makes a call to the LootBoxCommitReveal smart contract using the `withdraw(uint64)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    withdraw: async (params: CallParams<LootBoxCommitRevealArgs['obj']['withdraw(uint64)void'] | LootBoxCommitRevealArgs['tuple']['withdraw(uint64)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(LootBoxCommitRevealParamsFactory.withdraw(params))
      return {...result, return: result.return as unknown as (undefined | LootBoxCommitRevealReturns['withdraw(uint64)void'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new LootBoxCommitRevealClient(this.appClient.clone(params))
  }

  /**
   * Methods to access state for the current LootBoxCommitReveal app
   */
  state = {
    /**
     * Methods to access global state for the current LootBoxCommitReveal app
     */
    global: {
      /**
       * Get all current keyed values from global state
       */
      getAll: async (): Promise<Partial<Expand<GlobalKeysState>>> => {
        const result = await this.appClient.state.global.getAll()
        return {
          treasuryAddress: result.treasuryAddress,
          cratePrice: result.cratePrice,
          beaconApp: result.beaconApp,
          beaconCadence: result.beaconCadence,
        }
      },
      /**
       * Get the current value of the treasuryAddress key in global state
       */
      treasuryAddress: async (): Promise<string | undefined> => { return (await this.appClient.state.global.getValue("treasuryAddress")) as string | undefined },
      /**
       * Get the current value of the cratePrice key in global state
       */
      cratePrice: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("cratePrice")) as bigint | undefined },
      /**
       * Get the current value of the beaconApp key in global state
       */
      beaconApp: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("beaconApp")) as bigint | undefined },
      /**
       * Get the current value of the beaconCadence key in global state
       */
      beaconCadence: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("beaconCadence")) as bigint | undefined },
    },
    /**
     * Methods to access box state for the current LootBoxCommitReveal app
     */
    box: {
      /**
       * Get all current keyed values from box state
       */
      getAll: async (): Promise<Partial<Expand<BoxKeysState>>> => {
        const result = await this.appClient.state.box.getAll()
        return {
        }
      },
      /**
       * Get values from the commitTarget map in box state
       */
      commitTarget: {
        /**
         * Get all current values of the commitTarget map in box state
         */
        getMap: async (): Promise<Map<string, bigint>> => { return (await this.appClient.state.box.getMap("commitTarget")) as Map<string, bigint> },
        /**
         * Get a current value of the commitTarget map by key from box state
         */
        value: async (key: string): Promise<bigint | undefined> => { return await this.appClient.state.box.getMapValue("commitTarget", key) as bigint | undefined },
      },
    },
  }

  public newGroup(): LootBoxCommitRevealComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a configure(address,uint64,uint64,uint64)void method call against the LootBoxCommitReveal contract
       */
      configure(params: CallParams<LootBoxCommitRevealArgs['obj']['configure(address,uint64,uint64,uint64)void'] | LootBoxCommitRevealArgs['tuple']['configure(address,uint64,uint64,uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.configure(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a commit(pay)void method call against the LootBoxCommitReveal contract
       */
      commit(params: CallParams<LootBoxCommitRevealArgs['obj']['commit(pay)void'] | LootBoxCommitRevealArgs['tuple']['commit(pay)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.commit(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a reveal()uint64 method call against the LootBoxCommitReveal contract
       */
      reveal(params: CallParams<LootBoxCommitRevealArgs['obj']['reveal()uint64'] | LootBoxCommitRevealArgs['tuple']['reveal()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.reveal(params)))
        resultMappers.push((v) => client.decodeReturnValue('reveal()uint64', v))
        return this
      },
      /**
       * Add a reclaim(address)void method call against the LootBoxCommitReveal contract
       */
      reclaim(params: CallParams<LootBoxCommitRevealArgs['obj']['reclaim(address)void'] | LootBoxCommitRevealArgs['tuple']['reclaim(address)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.reclaim(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a withdraw(uint64)void method call against the LootBoxCommitReveal contract
       */
      withdraw(params: CallParams<LootBoxCommitRevealArgs['obj']['withdraw(uint64)void'] | LootBoxCommitRevealArgs['tuple']['withdraw(uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.withdraw(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a clear state call to the LootBoxCommitReveal contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as LootBoxCommitRevealComposer
  }
}
export type LootBoxCommitRevealComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the configure(address,uint64,uint64,uint64)void ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  configure(params?: CallParams<LootBoxCommitRevealArgs['obj']['configure(address,uint64,uint64,uint64)void'] | LootBoxCommitRevealArgs['tuple']['configure(address,uint64,uint64,uint64)void']>): LootBoxCommitRevealComposer<[...TReturns, LootBoxCommitRevealReturns['configure(address,uint64,uint64,uint64)void'] | undefined]>

  /**
   * Calls the commit(pay)void ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  commit(params?: CallParams<LootBoxCommitRevealArgs['obj']['commit(pay)void'] | LootBoxCommitRevealArgs['tuple']['commit(pay)void']>): LootBoxCommitRevealComposer<[...TReturns, LootBoxCommitRevealReturns['commit(pay)void'] | undefined]>

  /**
   * Calls the reveal()uint64 ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  reveal(params?: CallParams<LootBoxCommitRevealArgs['obj']['reveal()uint64'] | LootBoxCommitRevealArgs['tuple']['reveal()uint64']>): LootBoxCommitRevealComposer<[...TReturns, LootBoxCommitRevealReturns['reveal()uint64'] | undefined]>

  /**
   * Calls the reclaim(address)void ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  reclaim(params?: CallParams<LootBoxCommitRevealArgs['obj']['reclaim(address)void'] | LootBoxCommitRevealArgs['tuple']['reclaim(address)void']>): LootBoxCommitRevealComposer<[...TReturns, LootBoxCommitRevealReturns['reclaim(address)void'] | undefined]>

  /**
   * Calls the withdraw(uint64)void ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  withdraw(params?: CallParams<LootBoxCommitRevealArgs['obj']['withdraw(uint64)void'] | LootBoxCommitRevealArgs['tuple']['withdraw(uint64)void']>): LootBoxCommitRevealComposer<[...TReturns, LootBoxCommitRevealReturns['withdraw(uint64)void'] | undefined]>

  /**
   * Makes a clear_state call to an existing instance of the LootBoxCommitReveal smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): LootBoxCommitRevealComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): LootBoxCommitRevealComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<LootBoxCommitRevealComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<LootBoxCommitRevealComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<LootBoxCommitRevealComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<LootBoxCommitRevealComposerResults<TReturns>>
}
export type LootBoxCommitRevealComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

