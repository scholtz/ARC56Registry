/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"HandshakeContract","structs":{"HandshakeRecord":[{"name":"handshakeId","type":"uint64"},{"name":"initiator","type":"address"},{"name":"receiver","type":"address"},{"name":"timestamp","type":"uint64"},{"name":"note","type":"string"}]},"methods":[{"name":"create_handshake","args":[{"type":"address","name":"other_user"},{"type":"string","name":"note"}],"returns":{"type":"uint64"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}},{"name":"update_handshake_note","args":[{"type":"uint64","name":"handshake_id"},{"type":"string","name":"new_note"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Update the note of an existing handshake (only by participants)","events":[],"recommendations":{}},{"name":"get_counter","args":[],"returns":{"type":"uint64"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}},{"name":"get_handshake","args":[{"type":"uint64","name":"handshake_id"}],"returns":{"type":"(uint64,address,address,uint64,string)","struct":"HandshakeRecord"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}},{"name":"get_user_handshakes","args":[{"type":"address","name":"user"}],"returns":{"type":"uint64[]"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"NEW: Get all handshake IDs for a specific user","events":[],"recommendations":{}}],"arcs":[22,28],"networks":{},"state":{"schema":{"global":{"ints":1,"bytes":0},"local":{"ints":0,"bytes":0}},"keys":{"global":{"counter":{"keyType":"AVMString","valueType":"AVMUint64","key":"Y291bnRlcg=="}},"local":{},"box":{}},"maps":{"global":{},"local":{},"box":{"handshakes":{"keyType":"uint64","valueType":"HandshakeRecord","prefix":"aGFuZHNoYWtlcw=="},"user_handshakes":{"keyType":"address","valueType":"uint64[]","prefix":"dXNlcl9oYW5kc2hha2Vz"}}}},"bareActions":{"create":["NoOp"],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[392,404,418,423,428],"errorMessage":"Index access is out of bounds"},{"pc":[114,133,152,168,186],"errorMessage":"OnCompletion is not NoOp"},{"pc":[213],"errorMessage":"can only call when creating"},{"pc":[117,136,155,171,189],"errorMessage":"can only call when not creating"},{"pc":[231,462],"errorMessage":"check self.counter exists"},{"pc":[391,476],"errorMessage":"check self.handshakes entry exists"},{"pc":[283,318,495],"errorMessage":"check self.user_handshakes entry exists"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"events":[]} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


// Type definitions for ARC-56 structs

export type HandshakeRecord = {
  handshakeId: bigint,
  initiator: string,
  receiver: string,
  timestamp: bigint,
  note: string
}


/**
 * Converts the ABI tuple representation of a HandshakeRecord to the struct representation
 */
export function HandshakeRecordFromTuple(abiTuple: [bigint, string, string, bigint, string]) {
  return getABIStructFromABITuple(abiTuple, APP_SPEC.structs.HandshakeRecord, APP_SPEC.structs) as HandshakeRecord
}

/**
 * The argument types for the HandshakeContract contract
 */
export type HandshakeContractArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'create_handshake(address,string)uint64': {
      otherUser: string
      note: string
    }
    'update_handshake_note(uint64,string)void': {
      handshakeId: bigint | number
      newNote: string
    }
    'get_counter()uint64': Record<string, never>
    'get_handshake(uint64)(uint64,address,address,uint64,string)': {
      handshakeId: bigint | number
    }
    'get_user_handshakes(address)uint64[]': {
      user: string
    }
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'create_handshake(address,string)uint64': [otherUser: string, note: string]
    'update_handshake_note(uint64,string)void': [handshakeId: bigint | number, newNote: string]
    'get_counter()uint64': []
    'get_handshake(uint64)(uint64,address,address,uint64,string)': [handshakeId: bigint | number]
    'get_user_handshakes(address)uint64[]': [user: string]
  }
}

/**
 * The return type for each method
 */
export type HandshakeContractReturns = {
  'create_handshake(address,string)uint64': bigint
  'update_handshake_note(uint64,string)void': void
  'get_counter()uint64': bigint
  'get_handshake(uint64)(uint64,address,address,uint64,string)': HandshakeRecord
  'get_user_handshakes(address)uint64[]': bigint[]
}

/**
 * Defines the types of available calls and state of the HandshakeContract smart contract.
 */
export type HandshakeContractTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'create_handshake(address,string)uint64' | 'create_handshake', {
      argsObj: HandshakeContractArgs['obj']['create_handshake(address,string)uint64']
      argsTuple: HandshakeContractArgs['tuple']['create_handshake(address,string)uint64']
      returns: HandshakeContractReturns['create_handshake(address,string)uint64']
    }>
    & Record<'update_handshake_note(uint64,string)void' | 'update_handshake_note', {
      argsObj: HandshakeContractArgs['obj']['update_handshake_note(uint64,string)void']
      argsTuple: HandshakeContractArgs['tuple']['update_handshake_note(uint64,string)void']
      returns: HandshakeContractReturns['update_handshake_note(uint64,string)void']
    }>
    & Record<'get_counter()uint64' | 'get_counter', {
      argsObj: HandshakeContractArgs['obj']['get_counter()uint64']
      argsTuple: HandshakeContractArgs['tuple']['get_counter()uint64']
      returns: HandshakeContractReturns['get_counter()uint64']
    }>
    & Record<'get_handshake(uint64)(uint64,address,address,uint64,string)' | 'get_handshake', {
      argsObj: HandshakeContractArgs['obj']['get_handshake(uint64)(uint64,address,address,uint64,string)']
      argsTuple: HandshakeContractArgs['tuple']['get_handshake(uint64)(uint64,address,address,uint64,string)']
      returns: HandshakeContractReturns['get_handshake(uint64)(uint64,address,address,uint64,string)']
    }>
    & Record<'get_user_handshakes(address)uint64[]' | 'get_user_handshakes', {
      argsObj: HandshakeContractArgs['obj']['get_user_handshakes(address)uint64[]']
      argsTuple: HandshakeContractArgs['tuple']['get_user_handshakes(address)uint64[]']
      returns: HandshakeContractReturns['get_user_handshakes(address)uint64[]']
    }>
  /**
   * Defines the shape of the state of the application.
   */
  state: {
    global: {
      keys: {
        counter: bigint
      }
      maps: {}
    }
    box: {
      keys: {}
      maps: {
        handshakes: Map<bigint | number, HandshakeRecord>
        userHandshakes: Map<string, bigint[]>
      }
    }
  }
}

/**
 * Defines the possible abi call signatures.
 */
export type HandshakeContractSignatures = keyof HandshakeContractTypes['methods']
/**
 * Defines the possible abi call signatures for methods that return a non-void value.
 */
export type HandshakeContractNonVoidMethodSignatures = keyof HandshakeContractTypes['methods'] extends infer T ? T extends keyof HandshakeContractTypes['methods'] ? MethodReturn<T> extends void ? never : T  : never : never
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the HandshakeContract smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends HandshakeContractSignatures> = HandshakeContractTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the HandshakeContract smart contract to the method's return type
 */
export type MethodReturn<TSignature extends HandshakeContractSignatures> = HandshakeContractTypes['methods'][TSignature]['returns']

/**
 * Defines the shape of the keyed global state of the application.
 */
export type GlobalKeysState = HandshakeContractTypes['state']['global']['keys']

/**
 * Defines the shape of the keyed box state of the application.
 */
export type BoxKeysState = HandshakeContractTypes['state']['box']['keys']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the HandshakeContract smart contract
 */
export abstract class HandshakeContractParamsFactory {
  /**
   * Constructs a no op call for the create_handshake(address,string)uint64 ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static createHandshake(params: CallParams<HandshakeContractArgs['obj']['create_handshake(address,string)uint64'] | HandshakeContractArgs['tuple']['create_handshake(address,string)uint64']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'create_handshake(address,string)uint64' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.otherUser, params.args.note],
    }
  }
  /**
   * Constructs a no op call for the update_handshake_note(uint64,string)void ABI method
   *
   * Update the note of an existing handshake (only by participants)
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static updateHandshakeNote(params: CallParams<HandshakeContractArgs['obj']['update_handshake_note(uint64,string)void'] | HandshakeContractArgs['tuple']['update_handshake_note(uint64,string)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'update_handshake_note(uint64,string)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.handshakeId, params.args.newNote],
    }
  }
  /**
   * Constructs a no op call for the get_counter()uint64 ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static getCounter(params: CallParams<HandshakeContractArgs['obj']['get_counter()uint64'] | HandshakeContractArgs['tuple']['get_counter()uint64']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'get_counter()uint64' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the get_handshake(uint64)(uint64,address,address,uint64,string) ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static getHandshake(params: CallParams<HandshakeContractArgs['obj']['get_handshake(uint64)(uint64,address,address,uint64,string)'] | HandshakeContractArgs['tuple']['get_handshake(uint64)(uint64,address,address,uint64,string)']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'get_handshake(uint64)(uint64,address,address,uint64,string)' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.handshakeId],
    }
  }
  /**
   * Constructs a no op call for the get_user_handshakes(address)uint64[] ABI method
   *
   * NEW: Get all handshake IDs for a specific user
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static getUserHandshakes(params: CallParams<HandshakeContractArgs['obj']['get_user_handshakes(address)uint64[]'] | HandshakeContractArgs['tuple']['get_user_handshakes(address)uint64[]']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'get_user_handshakes(address)uint64[]' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.user],
    }
  }
}

/**
 * A client to make calls to the HandshakeContract smart contract
 */
export class HandshakeContractClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `HandshakeContractClient`
   *
   * @param appClient An `AppClient` instance which has been created with the HandshakeContract app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `HandshakeContractClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Checks for decode errors on the given return value and maps the return value to the return type for the given method
   * @returns The typed return value or undefined if there was no value
   */
  decodeReturnValue<TSignature extends HandshakeContractNonVoidMethodSignatures>(method: TSignature, returnValue: ABIReturn | undefined) {
    return returnValue !== undefined ? getArc56ReturnValue<MethodReturn<TSignature>>(returnValue, this.appClient.getABIMethod(method), APP_SPEC.structs) : undefined
  }

  /**
   * Returns a new `HandshakeContractClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<HandshakeContractClient> {
    return new HandshakeContractClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `HandshakeContractClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<HandshakeContractClient> {
    return new HandshakeContractClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Makes a clear_state call to an existing instance of the HandshakeContract smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the HandshakeContract smart contract using the `create_handshake(address,string)uint64` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    createHandshake: (params: CallParams<HandshakeContractArgs['obj']['create_handshake(address,string)uint64'] | HandshakeContractArgs['tuple']['create_handshake(address,string)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(HandshakeContractParamsFactory.createHandshake(params))
    },

    /**
     * Makes a call to the HandshakeContract smart contract using the `update_handshake_note(uint64,string)void` ABI method.
     *
     * Update the note of an existing handshake (only by participants)
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    updateHandshakeNote: (params: CallParams<HandshakeContractArgs['obj']['update_handshake_note(uint64,string)void'] | HandshakeContractArgs['tuple']['update_handshake_note(uint64,string)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(HandshakeContractParamsFactory.updateHandshakeNote(params))
    },

    /**
     * Makes a call to the HandshakeContract smart contract using the `get_counter()uint64` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    getCounter: (params: CallParams<HandshakeContractArgs['obj']['get_counter()uint64'] | HandshakeContractArgs['tuple']['get_counter()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(HandshakeContractParamsFactory.getCounter(params))
    },

    /**
     * Makes a call to the HandshakeContract smart contract using the `get_handshake(uint64)(uint64,address,address,uint64,string)` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    getHandshake: (params: CallParams<HandshakeContractArgs['obj']['get_handshake(uint64)(uint64,address,address,uint64,string)'] | HandshakeContractArgs['tuple']['get_handshake(uint64)(uint64,address,address,uint64,string)']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(HandshakeContractParamsFactory.getHandshake(params))
    },

    /**
     * Makes a call to the HandshakeContract smart contract using the `get_user_handshakes(address)uint64[]` ABI method.
     *
     * NEW: Get all handshake IDs for a specific user
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    getUserHandshakes: (params: CallParams<HandshakeContractArgs['obj']['get_user_handshakes(address)uint64[]'] | HandshakeContractArgs['tuple']['get_user_handshakes(address)uint64[]']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(HandshakeContractParamsFactory.getUserHandshakes(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Makes a clear_state call to an existing instance of the HandshakeContract smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the HandshakeContract smart contract using the `create_handshake(address,string)uint64` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    createHandshake: (params: CallParams<HandshakeContractArgs['obj']['create_handshake(address,string)uint64'] | HandshakeContractArgs['tuple']['create_handshake(address,string)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(HandshakeContractParamsFactory.createHandshake(params))
    },

    /**
     * Makes a call to the HandshakeContract smart contract using the `update_handshake_note(uint64,string)void` ABI method.
     *
     * Update the note of an existing handshake (only by participants)
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    updateHandshakeNote: (params: CallParams<HandshakeContractArgs['obj']['update_handshake_note(uint64,string)void'] | HandshakeContractArgs['tuple']['update_handshake_note(uint64,string)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(HandshakeContractParamsFactory.updateHandshakeNote(params))
    },

    /**
     * Makes a call to the HandshakeContract smart contract using the `get_counter()uint64` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    getCounter: (params: CallParams<HandshakeContractArgs['obj']['get_counter()uint64'] | HandshakeContractArgs['tuple']['get_counter()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(HandshakeContractParamsFactory.getCounter(params))
    },

    /**
     * Makes a call to the HandshakeContract smart contract using the `get_handshake(uint64)(uint64,address,address,uint64,string)` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    getHandshake: (params: CallParams<HandshakeContractArgs['obj']['get_handshake(uint64)(uint64,address,address,uint64,string)'] | HandshakeContractArgs['tuple']['get_handshake(uint64)(uint64,address,address,uint64,string)']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(HandshakeContractParamsFactory.getHandshake(params))
    },

    /**
     * Makes a call to the HandshakeContract smart contract using the `get_user_handshakes(address)uint64[]` ABI method.
     *
     * NEW: Get all handshake IDs for a specific user
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    getUserHandshakes: (params: CallParams<HandshakeContractArgs['obj']['get_user_handshakes(address)uint64[]'] | HandshakeContractArgs['tuple']['get_user_handshakes(address)uint64[]']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(HandshakeContractParamsFactory.getUserHandshakes(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Makes a clear_state call to an existing instance of the HandshakeContract smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the HandshakeContract smart contract using the `create_handshake(address,string)uint64` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    createHandshake: async (params: CallParams<HandshakeContractArgs['obj']['create_handshake(address,string)uint64'] | HandshakeContractArgs['tuple']['create_handshake(address,string)uint64']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(HandshakeContractParamsFactory.createHandshake(params))
      return {...result, return: result.return as unknown as (undefined | HandshakeContractReturns['create_handshake(address,string)uint64'])}
    },

    /**
     * Makes a call to the HandshakeContract smart contract using the `update_handshake_note(uint64,string)void` ABI method.
     *
     * Update the note of an existing handshake (only by participants)
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    updateHandshakeNote: async (params: CallParams<HandshakeContractArgs['obj']['update_handshake_note(uint64,string)void'] | HandshakeContractArgs['tuple']['update_handshake_note(uint64,string)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(HandshakeContractParamsFactory.updateHandshakeNote(params))
      return {...result, return: result.return as unknown as (undefined | HandshakeContractReturns['update_handshake_note(uint64,string)void'])}
    },

    /**
     * Makes a call to the HandshakeContract smart contract using the `get_counter()uint64` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    getCounter: async (params: CallParams<HandshakeContractArgs['obj']['get_counter()uint64'] | HandshakeContractArgs['tuple']['get_counter()uint64']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(HandshakeContractParamsFactory.getCounter(params))
      return {...result, return: result.return as unknown as (undefined | HandshakeContractReturns['get_counter()uint64'])}
    },

    /**
     * Makes a call to the HandshakeContract smart contract using the `get_handshake(uint64)(uint64,address,address,uint64,string)` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    getHandshake: async (params: CallParams<HandshakeContractArgs['obj']['get_handshake(uint64)(uint64,address,address,uint64,string)'] | HandshakeContractArgs['tuple']['get_handshake(uint64)(uint64,address,address,uint64,string)']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(HandshakeContractParamsFactory.getHandshake(params))
      return {...result, return: result.return as unknown as (undefined | HandshakeContractReturns['get_handshake(uint64)(uint64,address,address,uint64,string)'])}
    },

    /**
     * Makes a call to the HandshakeContract smart contract using the `get_user_handshakes(address)uint64[]` ABI method.
     *
     * NEW: Get all handshake IDs for a specific user
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    getUserHandshakes: async (params: CallParams<HandshakeContractArgs['obj']['get_user_handshakes(address)uint64[]'] | HandshakeContractArgs['tuple']['get_user_handshakes(address)uint64[]']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(HandshakeContractParamsFactory.getUserHandshakes(params))
      return {...result, return: result.return as unknown as (undefined | HandshakeContractReturns['get_user_handshakes(address)uint64[]'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new HandshakeContractClient(this.appClient.clone(params))
  }

  /**
   * Methods to access state for the current HandshakeContract app
   */
  state = {
    /**
     * Methods to access global state for the current HandshakeContract app
     */
    global: {
      /**
       * Get all current keyed values from global state
       */
      getAll: async (): Promise<Partial<Expand<GlobalKeysState>>> => {
        const result = await this.appClient.state.global.getAll()
        return {
          counter: result.counter,
        }
      },
      /**
       * Get the current value of the counter key in global state
       */
      counter: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("counter")) as bigint | undefined },
    },
    /**
     * Methods to access box state for the current HandshakeContract app
     */
    box: {
      /**
       * Get all current keyed values from box state
       */
      getAll: async (): Promise<Partial<Expand<BoxKeysState>>> => {
        const result = await this.appClient.state.box.getAll()
        return {
        }
      },
      /**
       * Get values from the handshakes map in box state
       */
      handshakes: {
        /**
         * Get all current values of the handshakes map in box state
         */
        getMap: async (): Promise<Map<bigint, HandshakeRecord>> => { return (await this.appClient.state.box.getMap("handshakes")) as Map<bigint, HandshakeRecord> },
        /**
         * Get a current value of the handshakes map by key from box state
         */
        value: async (key: bigint | number): Promise<HandshakeRecord | undefined> => { return await this.appClient.state.box.getMapValue("handshakes", key) as HandshakeRecord | undefined },
      },
      /**
       * Get values from the user_handshakes map in box state
       */
      userHandshakes: {
        /**
         * Get all current values of the user_handshakes map in box state
         */
        getMap: async (): Promise<Map<string, bigint[]>> => { return (await this.appClient.state.box.getMap("user_handshakes")) as Map<string, bigint[]> },
        /**
         * Get a current value of the user_handshakes map by key from box state
         */
        value: async (key: string): Promise<bigint[] | undefined> => { return await this.appClient.state.box.getMapValue("user_handshakes", key) as bigint[] | undefined },
      },
    },
  }

  public newGroup(): HandshakeContractComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a create_handshake(address,string)uint64 method call against the HandshakeContract contract
       */
      createHandshake(params: CallParams<HandshakeContractArgs['obj']['create_handshake(address,string)uint64'] | HandshakeContractArgs['tuple']['create_handshake(address,string)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.createHandshake(params)))
        resultMappers.push((v) => client.decodeReturnValue('create_handshake(address,string)uint64', v))
        return this
      },
      /**
       * Add a update_handshake_note(uint64,string)void method call against the HandshakeContract contract
       */
      updateHandshakeNote(params: CallParams<HandshakeContractArgs['obj']['update_handshake_note(uint64,string)void'] | HandshakeContractArgs['tuple']['update_handshake_note(uint64,string)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.updateHandshakeNote(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a get_counter()uint64 method call against the HandshakeContract contract
       */
      getCounter(params: CallParams<HandshakeContractArgs['obj']['get_counter()uint64'] | HandshakeContractArgs['tuple']['get_counter()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.getCounter(params)))
        resultMappers.push((v) => client.decodeReturnValue('get_counter()uint64', v))
        return this
      },
      /**
       * Add a get_handshake(uint64)(uint64,address,address,uint64,string) method call against the HandshakeContract contract
       */
      getHandshake(params: CallParams<HandshakeContractArgs['obj']['get_handshake(uint64)(uint64,address,address,uint64,string)'] | HandshakeContractArgs['tuple']['get_handshake(uint64)(uint64,address,address,uint64,string)']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.getHandshake(params)))
        resultMappers.push((v) => client.decodeReturnValue('get_handshake(uint64)(uint64,address,address,uint64,string)', v))
        return this
      },
      /**
       * Add a get_user_handshakes(address)uint64[] method call against the HandshakeContract contract
       */
      getUserHandshakes(params: CallParams<HandshakeContractArgs['obj']['get_user_handshakes(address)uint64[]'] | HandshakeContractArgs['tuple']['get_user_handshakes(address)uint64[]']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.getUserHandshakes(params)))
        resultMappers.push((v) => client.decodeReturnValue('get_user_handshakes(address)uint64[]', v))
        return this
      },
      /**
       * Add a clear state call to the HandshakeContract contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as HandshakeContractComposer
  }
}
export type HandshakeContractComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the create_handshake(address,string)uint64 ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  createHandshake(params?: CallParams<HandshakeContractArgs['obj']['create_handshake(address,string)uint64'] | HandshakeContractArgs['tuple']['create_handshake(address,string)uint64']>): HandshakeContractComposer<[...TReturns, HandshakeContractReturns['create_handshake(address,string)uint64'] | undefined]>

  /**
   * Calls the update_handshake_note(uint64,string)void ABI method.
   *
   * Update the note of an existing handshake (only by participants)
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  updateHandshakeNote(params?: CallParams<HandshakeContractArgs['obj']['update_handshake_note(uint64,string)void'] | HandshakeContractArgs['tuple']['update_handshake_note(uint64,string)void']>): HandshakeContractComposer<[...TReturns, HandshakeContractReturns['update_handshake_note(uint64,string)void'] | undefined]>

  /**
   * Calls the get_counter()uint64 ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  getCounter(params?: CallParams<HandshakeContractArgs['obj']['get_counter()uint64'] | HandshakeContractArgs['tuple']['get_counter()uint64']>): HandshakeContractComposer<[...TReturns, HandshakeContractReturns['get_counter()uint64'] | undefined]>

  /**
   * Calls the get_handshake(uint64)(uint64,address,address,uint64,string) ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  getHandshake(params?: CallParams<HandshakeContractArgs['obj']['get_handshake(uint64)(uint64,address,address,uint64,string)'] | HandshakeContractArgs['tuple']['get_handshake(uint64)(uint64,address,address,uint64,string)']>): HandshakeContractComposer<[...TReturns, HandshakeContractReturns['get_handshake(uint64)(uint64,address,address,uint64,string)'] | undefined]>

  /**
   * Calls the get_user_handshakes(address)uint64[] ABI method.
   *
   * NEW: Get all handshake IDs for a specific user
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  getUserHandshakes(params?: CallParams<HandshakeContractArgs['obj']['get_user_handshakes(address)uint64[]'] | HandshakeContractArgs['tuple']['get_user_handshakes(address)uint64[]']>): HandshakeContractComposer<[...TReturns, HandshakeContractReturns['get_user_handshakes(address)uint64[]'] | undefined]>

  /**
   * Makes a clear_state call to an existing instance of the HandshakeContract smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): HandshakeContractComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): HandshakeContractComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<HandshakeContractComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<HandshakeContractComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<HandshakeContractComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<HandshakeContractComposerResults<TReturns>>
}
export type HandshakeContractComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

