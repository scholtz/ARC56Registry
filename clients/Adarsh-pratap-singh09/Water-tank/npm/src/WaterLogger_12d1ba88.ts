/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"WaterLogger","structs":{},"methods":[{"name":"logIntake","args":[{"type":"uint64","name":"amount"}],"returns":{"type":"uint64"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}},{"name":"resetIntake","args":[],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}}],"arcs":[22,28],"networks":{},"state":{"schema":{"global":{"ints":1,"bytes":0},"local":{"ints":0,"bytes":0}},"keys":{"global":{"totalIntake":{"keyType":"AVMString","valueType":"AVMUint64","key":"dG90YWxJbnRha2U="}},"local":{},"box":{}},"maps":{"global":{},"local":{},"box":{}}},"bareActions":{"create":["NoOp"],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[58,70],"errorMessage":"OnCompletion is not NoOp"},{"pc":[101],"errorMessage":"can only call when creating"},{"pc":[61,73],"errorMessage":"can only call when not creating"},{"pc":[110,120],"errorMessage":"check GlobalState exists"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"events":[]} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


/**
 * The argument types for the WaterLogger contract
 */
export type WaterLoggerArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'logIntake(uint64)uint64': {
      amount: bigint | number
    }
    'resetIntake()void': Record<string, never>
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'logIntake(uint64)uint64': [amount: bigint | number]
    'resetIntake()void': []
  }
}

/**
 * The return type for each method
 */
export type WaterLoggerReturns = {
  'logIntake(uint64)uint64': bigint
  'resetIntake()void': void
}

/**
 * Defines the types of available calls and state of the WaterLogger smart contract.
 */
export type WaterLoggerTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'logIntake(uint64)uint64' | 'logIntake', {
      argsObj: WaterLoggerArgs['obj']['logIntake(uint64)uint64']
      argsTuple: WaterLoggerArgs['tuple']['logIntake(uint64)uint64']
      returns: WaterLoggerReturns['logIntake(uint64)uint64']
    }>
    & Record<'resetIntake()void' | 'resetIntake', {
      argsObj: WaterLoggerArgs['obj']['resetIntake()void']
      argsTuple: WaterLoggerArgs['tuple']['resetIntake()void']
      returns: WaterLoggerReturns['resetIntake()void']
    }>
  /**
   * Defines the shape of the state of the application.
   */
  state: {
    global: {
      keys: {
        totalIntake: bigint
      }
      maps: {}
    }
  }
}

/**
 * Defines the possible abi call signatures.
 */
export type WaterLoggerSignatures = keyof WaterLoggerTypes['methods']
/**
 * Defines the possible abi call signatures for methods that return a non-void value.
 */
export type WaterLoggerNonVoidMethodSignatures = keyof WaterLoggerTypes['methods'] extends infer T ? T extends keyof WaterLoggerTypes['methods'] ? MethodReturn<T> extends void ? never : T  : never : never
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the WaterLogger smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends WaterLoggerSignatures> = WaterLoggerTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the WaterLogger smart contract to the method's return type
 */
export type MethodReturn<TSignature extends WaterLoggerSignatures> = WaterLoggerTypes['methods'][TSignature]['returns']

/**
 * Defines the shape of the keyed global state of the application.
 */
export type GlobalKeysState = WaterLoggerTypes['state']['global']['keys']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the WaterLogger smart contract
 */
export abstract class WaterLoggerParamsFactory {
  /**
   * Constructs a no op call for the logIntake(uint64)uint64 ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static logIntake(params: CallParams<WaterLoggerArgs['obj']['logIntake(uint64)uint64'] | WaterLoggerArgs['tuple']['logIntake(uint64)uint64']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'logIntake(uint64)uint64' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.amount],
    }
  }
  /**
   * Constructs a no op call for the resetIntake()void ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static resetIntake(params: CallParams<WaterLoggerArgs['obj']['resetIntake()void'] | WaterLoggerArgs['tuple']['resetIntake()void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'resetIntake()void' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
}

/**
 * A client to make calls to the WaterLogger smart contract
 */
export class WaterLoggerClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `WaterLoggerClient`
   *
   * @param appClient An `AppClient` instance which has been created with the WaterLogger app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `WaterLoggerClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Checks for decode errors on the given return value and maps the return value to the return type for the given method
   * @returns The typed return value or undefined if there was no value
   */
  decodeReturnValue<TSignature extends WaterLoggerNonVoidMethodSignatures>(method: TSignature, returnValue: ABIReturn | undefined) {
    return returnValue !== undefined ? getArc56ReturnValue<MethodReturn<TSignature>>(returnValue, this.appClient.getABIMethod(method), APP_SPEC.structs) : undefined
  }

  /**
   * Returns a new `WaterLoggerClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<WaterLoggerClient> {
    return new WaterLoggerClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `WaterLoggerClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<WaterLoggerClient> {
    return new WaterLoggerClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Makes a clear_state call to an existing instance of the WaterLogger smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the WaterLogger smart contract using the `logIntake(uint64)uint64` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    logIntake: (params: CallParams<WaterLoggerArgs['obj']['logIntake(uint64)uint64'] | WaterLoggerArgs['tuple']['logIntake(uint64)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(WaterLoggerParamsFactory.logIntake(params))
    },

    /**
     * Makes a call to the WaterLogger smart contract using the `resetIntake()void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    resetIntake: (params: CallParams<WaterLoggerArgs['obj']['resetIntake()void'] | WaterLoggerArgs['tuple']['resetIntake()void']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(WaterLoggerParamsFactory.resetIntake(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Makes a clear_state call to an existing instance of the WaterLogger smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the WaterLogger smart contract using the `logIntake(uint64)uint64` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    logIntake: (params: CallParams<WaterLoggerArgs['obj']['logIntake(uint64)uint64'] | WaterLoggerArgs['tuple']['logIntake(uint64)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(WaterLoggerParamsFactory.logIntake(params))
    },

    /**
     * Makes a call to the WaterLogger smart contract using the `resetIntake()void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    resetIntake: (params: CallParams<WaterLoggerArgs['obj']['resetIntake()void'] | WaterLoggerArgs['tuple']['resetIntake()void']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(WaterLoggerParamsFactory.resetIntake(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Makes a clear_state call to an existing instance of the WaterLogger smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the WaterLogger smart contract using the `logIntake(uint64)uint64` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    logIntake: async (params: CallParams<WaterLoggerArgs['obj']['logIntake(uint64)uint64'] | WaterLoggerArgs['tuple']['logIntake(uint64)uint64']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(WaterLoggerParamsFactory.logIntake(params))
      return {...result, return: result.return as unknown as (undefined | WaterLoggerReturns['logIntake(uint64)uint64'])}
    },

    /**
     * Makes a call to the WaterLogger smart contract using the `resetIntake()void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    resetIntake: async (params: CallParams<WaterLoggerArgs['obj']['resetIntake()void'] | WaterLoggerArgs['tuple']['resetIntake()void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(WaterLoggerParamsFactory.resetIntake(params))
      return {...result, return: result.return as unknown as (undefined | WaterLoggerReturns['resetIntake()void'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new WaterLoggerClient(this.appClient.clone(params))
  }

  /**
   * Methods to access state for the current WaterLogger app
   */
  state = {
    /**
     * Methods to access global state for the current WaterLogger app
     */
    global: {
      /**
       * Get all current keyed values from global state
       */
      getAll: async (): Promise<Partial<Expand<GlobalKeysState>>> => {
        const result = await this.appClient.state.global.getAll()
        return {
          totalIntake: result.totalIntake,
        }
      },
      /**
       * Get the current value of the totalIntake key in global state
       */
      totalIntake: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("totalIntake")) as bigint | undefined },
    },
  }

  public newGroup(): WaterLoggerComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a logIntake(uint64)uint64 method call against the WaterLogger contract
       */
      logIntake(params: CallParams<WaterLoggerArgs['obj']['logIntake(uint64)uint64'] | WaterLoggerArgs['tuple']['logIntake(uint64)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.logIntake(params)))
        resultMappers.push((v) => client.decodeReturnValue('logIntake(uint64)uint64', v))
        return this
      },
      /**
       * Add a resetIntake()void method call against the WaterLogger contract
       */
      resetIntake(params: CallParams<WaterLoggerArgs['obj']['resetIntake()void'] | WaterLoggerArgs['tuple']['resetIntake()void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.resetIntake(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a clear state call to the WaterLogger contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as WaterLoggerComposer
  }
}
export type WaterLoggerComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the logIntake(uint64)uint64 ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  logIntake(params?: CallParams<WaterLoggerArgs['obj']['logIntake(uint64)uint64'] | WaterLoggerArgs['tuple']['logIntake(uint64)uint64']>): WaterLoggerComposer<[...TReturns, WaterLoggerReturns['logIntake(uint64)uint64'] | undefined]>

  /**
   * Calls the resetIntake()void ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  resetIntake(params?: CallParams<WaterLoggerArgs['obj']['resetIntake()void'] | WaterLoggerArgs['tuple']['resetIntake()void']>): WaterLoggerComposer<[...TReturns, WaterLoggerReturns['resetIntake()void'] | undefined]>

  /**
   * Makes a clear_state call to an existing instance of the WaterLogger smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): WaterLoggerComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): WaterLoggerComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<WaterLoggerComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<WaterLoggerComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<WaterLoggerComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<WaterLoggerComposerResults<TReturns>>
}
export type WaterLoggerComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

