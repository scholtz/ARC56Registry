/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"CampusVotingDapp","structs":{},"methods":[{"name":"create_poll","args":[{"type":"string","name":"question"},{"type":"string","name":"option_a"},{"type":"string","name":"option_b"},{"type":"string","name":"option_c"},{"type":"string","name":"option_d"},{"type":"uint64","name":"num_options"},{"type":"uint64","name":"duration_secs"},{"type":"uint64","name":"token_id"}],"returns":{"type":"uint64"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Create a new poll. Only callable by the app creator.\nReturns the poll end timestamp.","events":[],"recommendations":{}},{"name":"vote","args":[{"type":"uint64","name":"option"}],"returns":{"type":"string"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Cast a vote. Enforces:\n- Poll must be active - Voter must have opted in (has local state) - Voter must not have already voted - Option must be valid - If token_id > 0, sender must hold that ASA","events":[],"recommendations":{}},{"name":"get_results","args":[],"returns":{"type":"(uint64,uint64,uint64,uint64)"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"desc":"Return vote counts for all 4 options.","events":[],"recommendations":{}},{"name":"get_poll_info","args":[],"returns":{"type":"(string,string,string,string,string,uint64,uint64,uint64,uint64)"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"desc":"Return full poll info:\n(question, opt_a, opt_b, opt_c, opt_d, num_options, end_time, token_id, total_votes)","events":[],"recommendations":{}},{"name":"get_voter_status","args":[{"type":"address","name":"voter"}],"returns":{"type":"uint64"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"desc":"Return the option a voter chose (0 = hasn't voted).","events":[],"recommendations":{}},{"name":"close_poll","args":[],"returns":{"type":"string"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Creator can manually close the poll before the deadline.","events":[],"recommendations":{}}],"arcs":[22,28],"desc":"\n    Campus Voting DApp Smart Contract\n    \n    Supports:\n    - Poll creation with up to 4 options and a deadline\n    - One-person-one-vote enforcement via local state\n    - Optional token-gating (require holding a specific ASA)\n    - Transparent, tamper-proof vote counting\n    - Public read methods for results & poll info\n    ","networks":{},"state":{"schema":{"global":{"ints":9,"bytes":6},"local":{"ints":1,"bytes":0}},"keys":{"global":{"question":{"keyType":"AVMString","valueType":"AVMString","key":"cXVlc3Rpb24="},"option_a":{"keyType":"AVMString","valueType":"AVMString","key":"b3B0aW9uX2E="},"option_b":{"keyType":"AVMString","valueType":"AVMString","key":"b3B0aW9uX2I="},"option_c":{"keyType":"AVMString","valueType":"AVMString","key":"b3B0aW9uX2M="},"option_d":{"keyType":"AVMString","valueType":"AVMString","key":"b3B0aW9uX2Q="},"num_options":{"keyType":"AVMString","valueType":"AVMUint64","key":"bnVtX29wdGlvbnM="},"votes_a":{"keyType":"AVMString","valueType":"AVMUint64","key":"dm90ZXNfYQ=="},"votes_b":{"keyType":"AVMString","valueType":"AVMUint64","key":"dm90ZXNfYg=="},"votes_c":{"keyType":"AVMString","valueType":"AVMUint64","key":"dm90ZXNfYw=="},"votes_d":{"keyType":"AVMString","valueType":"AVMUint64","key":"dm90ZXNfZA=="},"end_time":{"keyType":"AVMString","valueType":"AVMUint64","key":"ZW5kX3RpbWU="},"creator":{"keyType":"AVMString","valueType":"address","key":"Y3JlYXRvcg=="},"token_id":{"keyType":"AVMString","valueType":"AVMUint64","key":"dG9rZW5faWQ="},"total_votes":{"keyType":"AVMString","valueType":"AVMUint64","key":"dG90YWxfdm90ZXM="},"poll_active":{"keyType":"AVMString","valueType":"AVMUint64","key":"cG9sbF9hY3RpdmU="}},"local":{"has_voted":{"keyType":"AVMString","valueType":"AVMUint64","key":"aHY=","desc":"Option voted (0=none)"}},"box":{}},"maps":{"global":{},"local":{},"box":{}}},"bareActions":{"create":["NoOp"],"call":["OptIn"]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[559],"errorMessage":"Already voted"},{"pc":[432],"errorMessage":"Duration must be positive"},{"pc":[578],"errorMessage":"Insufficient campus token balance"},{"pc":[550],"errorMessage":"Invalid option number"},{"pc":[429],"errorMessage":"Maximum 4 options allowed"},{"pc":[577],"errorMessage":"Must hold campus token to vote"},{"pc":[423],"errorMessage":"Need at least 2 options"},{"pc":[532],"errorMessage":"No active poll"},{"pc":[949],"errorMessage":"Only creator can close poll"},{"pc":[418],"errorMessage":"Only creator can create polls"},{"pc":[543],"errorMessage":"Option must be 1-4"},{"pc":[541],"errorMessage":"Poll has ended"},{"pc":[506,539,815],"errorMessage":"check self.end_time exists"},{"pc":[557],"errorMessage":"check self.has_voted exists for account"},{"pc":[548,809],"errorMessage":"check self.num_options exists"},{"pc":[757],"errorMessage":"check self.option_a exists"},{"pc":[770],"errorMessage":"check self.option_b exists"},{"pc":[783],"errorMessage":"check self.option_c exists"},{"pc":[796],"errorMessage":"check self.option_d exists"},{"pc":[529],"errorMessage":"check self.poll_active exists"},{"pc":[744],"errorMessage":"check self.question exists"},{"pc":[564,574,821],"errorMessage":"check self.token_id exists"},{"pc":[607,827],"errorMessage":"check self.total_votes exists"},{"pc":[597,705],"errorMessage":"check self.votes_a exists"},{"pc":[660,710],"errorMessage":"check self.votes_b exists"},{"pc":[679,715],"errorMessage":"check self.votes_c exists"},{"pc":[692,721],"errorMessage":"check self.votes_d exists"},{"pc":[311,327,343,359,375],"errorMessage":"invalid array length header"},{"pc":[318,334,350,366,382],"errorMessage":"invalid number of bytes for arc4.dynamic_array<arc4.uint8>"},{"pc":[927],"errorMessage":"invalid number of bytes for arc4.static_array<arc4.uint8, 32>"},{"pc":[393,402,411,522],"errorMessage":"invalid number of bytes for arc4.uint64"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"events":[]} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


/**
 * The argument types for the CampusVotingDapp contract
 */
export type CampusVotingDappArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'create_poll(string,string,string,string,string,uint64,uint64,uint64)uint64': {
      question: string
      optionA: string
      optionB: string
      optionC: string
      optionD: string
      numOptions: bigint | number
      durationSecs: bigint | number
      tokenId: bigint | number
    }
    'vote(uint64)string': {
      option: bigint | number
    }
    'get_results()(uint64,uint64,uint64,uint64)': Record<string, never>
    'get_poll_info()(string,string,string,string,string,uint64,uint64,uint64,uint64)': Record<string, never>
    'get_voter_status(address)uint64': {
      voter: string
    }
    'close_poll()string': Record<string, never>
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'create_poll(string,string,string,string,string,uint64,uint64,uint64)uint64': [question: string, optionA: string, optionB: string, optionC: string, optionD: string, numOptions: bigint | number, durationSecs: bigint | number, tokenId: bigint | number]
    'vote(uint64)string': [option: bigint | number]
    'get_results()(uint64,uint64,uint64,uint64)': []
    'get_poll_info()(string,string,string,string,string,uint64,uint64,uint64,uint64)': []
    'get_voter_status(address)uint64': [voter: string]
    'close_poll()string': []
  }
}

/**
 * The return type for each method
 */
export type CampusVotingDappReturns = {
  'create_poll(string,string,string,string,string,uint64,uint64,uint64)uint64': bigint
  'vote(uint64)string': string
  'get_results()(uint64,uint64,uint64,uint64)': [bigint, bigint, bigint, bigint]
  'get_poll_info()(string,string,string,string,string,uint64,uint64,uint64,uint64)': [string, string, string, string, string, bigint, bigint, bigint, bigint]
  'get_voter_status(address)uint64': bigint
  'close_poll()string': string
}

/**
 * Defines the types of available calls and state of the CampusVotingDapp smart contract.
 */
export type CampusVotingDappTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'create_poll(string,string,string,string,string,uint64,uint64,uint64)uint64' | 'create_poll', {
      argsObj: CampusVotingDappArgs['obj']['create_poll(string,string,string,string,string,uint64,uint64,uint64)uint64']
      argsTuple: CampusVotingDappArgs['tuple']['create_poll(string,string,string,string,string,uint64,uint64,uint64)uint64']
      returns: CampusVotingDappReturns['create_poll(string,string,string,string,string,uint64,uint64,uint64)uint64']
    }>
    & Record<'vote(uint64)string' | 'vote', {
      argsObj: CampusVotingDappArgs['obj']['vote(uint64)string']
      argsTuple: CampusVotingDappArgs['tuple']['vote(uint64)string']
      returns: CampusVotingDappReturns['vote(uint64)string']
    }>
    & Record<'get_results()(uint64,uint64,uint64,uint64)' | 'get_results', {
      argsObj: CampusVotingDappArgs['obj']['get_results()(uint64,uint64,uint64,uint64)']
      argsTuple: CampusVotingDappArgs['tuple']['get_results()(uint64,uint64,uint64,uint64)']
      returns: CampusVotingDappReturns['get_results()(uint64,uint64,uint64,uint64)']
    }>
    & Record<'get_poll_info()(string,string,string,string,string,uint64,uint64,uint64,uint64)' | 'get_poll_info', {
      argsObj: CampusVotingDappArgs['obj']['get_poll_info()(string,string,string,string,string,uint64,uint64,uint64,uint64)']
      argsTuple: CampusVotingDappArgs['tuple']['get_poll_info()(string,string,string,string,string,uint64,uint64,uint64,uint64)']
      returns: CampusVotingDappReturns['get_poll_info()(string,string,string,string,string,uint64,uint64,uint64,uint64)']
    }>
    & Record<'get_voter_status(address)uint64' | 'get_voter_status', {
      argsObj: CampusVotingDappArgs['obj']['get_voter_status(address)uint64']
      argsTuple: CampusVotingDappArgs['tuple']['get_voter_status(address)uint64']
      returns: CampusVotingDappReturns['get_voter_status(address)uint64']
    }>
    & Record<'close_poll()string' | 'close_poll', {
      argsObj: CampusVotingDappArgs['obj']['close_poll()string']
      argsTuple: CampusVotingDappArgs['tuple']['close_poll()string']
      returns: CampusVotingDappReturns['close_poll()string']
    }>
  /**
   * Defines the shape of the state of the application.
   */
  state: {
    global: {
      keys: {
        question: string
        optionA: string
        optionB: string
        optionC: string
        optionD: string
        numOptions: bigint
        votesA: bigint
        votesB: bigint
        votesC: bigint
        votesD: bigint
        endTime: bigint
        creator: string
        tokenId: bigint
        totalVotes: bigint
        pollActive: bigint
      }
      maps: {}
    }
    local: {
      keys: {
        /**
         * Option voted (0=none)
         */
        hasVoted: bigint
      }
      maps: {}
    }
  }
}

/**
 * Defines the possible abi call signatures.
 */
export type CampusVotingDappSignatures = keyof CampusVotingDappTypes['methods']
/**
 * Defines the possible abi call signatures for methods that return a non-void value.
 */
export type CampusVotingDappNonVoidMethodSignatures = keyof CampusVotingDappTypes['methods'] extends infer T ? T extends keyof CampusVotingDappTypes['methods'] ? MethodReturn<T> extends void ? never : T  : never : never
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the CampusVotingDapp smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends CampusVotingDappSignatures> = CampusVotingDappTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the CampusVotingDapp smart contract to the method's return type
 */
export type MethodReturn<TSignature extends CampusVotingDappSignatures> = CampusVotingDappTypes['methods'][TSignature]['returns']

/**
 * Defines the shape of the keyed global state of the application.
 */
export type GlobalKeysState = CampusVotingDappTypes['state']['global']['keys']

/**
 * Defines the shape of the keyed local state of the application.
 */
export type LocalKeysState = CampusVotingDappTypes['state']['local']['keys']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the CampusVotingDapp smart contract
 */
export abstract class CampusVotingDappParamsFactory {
  /**
   * Constructs a no op call for the create_poll(string,string,string,string,string,uint64,uint64,uint64)uint64 ABI method
   *
  * Create a new poll. Only callable by the app creator.
  Returns the poll end timestamp.

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static createPoll(params: CallParams<CampusVotingDappArgs['obj']['create_poll(string,string,string,string,string,uint64,uint64,uint64)uint64'] | CampusVotingDappArgs['tuple']['create_poll(string,string,string,string,string,uint64,uint64,uint64)uint64']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'create_poll(string,string,string,string,string,uint64,uint64,uint64)uint64' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.question, params.args.optionA, params.args.optionB, params.args.optionC, params.args.optionD, params.args.numOptions, params.args.durationSecs, params.args.tokenId],
    }
  }
  /**
   * Constructs a no op call for the vote(uint64)string ABI method
   *
  * Cast a vote. Enforces:
  - Poll must be active - Voter must have opted in (has local state) - Voter must not have already voted - Option must be valid - If token_id > 0, sender must hold that ASA

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static vote(params: CallParams<CampusVotingDappArgs['obj']['vote(uint64)string'] | CampusVotingDappArgs['tuple']['vote(uint64)string']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'vote(uint64)string' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.option],
    }
  }
  /**
   * Constructs a no op call for the get_results()(uint64,uint64,uint64,uint64) ABI method
   *
   * Return vote counts for all 4 options.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static getResults(params: CallParams<CampusVotingDappArgs['obj']['get_results()(uint64,uint64,uint64,uint64)'] | CampusVotingDappArgs['tuple']['get_results()(uint64,uint64,uint64,uint64)']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'get_results()(uint64,uint64,uint64,uint64)' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the get_poll_info()(string,string,string,string,string,uint64,uint64,uint64,uint64) ABI method
   *
  * Return full poll info:
  (question, opt_a, opt_b, opt_c, opt_d, num_options, end_time, token_id, total_votes)

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static getPollInfo(params: CallParams<CampusVotingDappArgs['obj']['get_poll_info()(string,string,string,string,string,uint64,uint64,uint64,uint64)'] | CampusVotingDappArgs['tuple']['get_poll_info()(string,string,string,string,string,uint64,uint64,uint64,uint64)']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'get_poll_info()(string,string,string,string,string,uint64,uint64,uint64,uint64)' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the get_voter_status(address)uint64 ABI method
   *
   * Return the option a voter chose (0 = hasn't voted).
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static getVoterStatus(params: CallParams<CampusVotingDappArgs['obj']['get_voter_status(address)uint64'] | CampusVotingDappArgs['tuple']['get_voter_status(address)uint64']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'get_voter_status(address)uint64' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.voter],
    }
  }
  /**
   * Constructs a no op call for the close_poll()string ABI method
   *
   * Creator can manually close the poll before the deadline.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static closePoll(params: CallParams<CampusVotingDappArgs['obj']['close_poll()string'] | CampusVotingDappArgs['tuple']['close_poll()string']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'close_poll()string' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
}

/**
 * A client to make calls to the CampusVotingDapp smart contract
 */
export class CampusVotingDappClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `CampusVotingDappClient`
   *
   * @param appClient An `AppClient` instance which has been created with the CampusVotingDapp app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `CampusVotingDappClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Checks for decode errors on the given return value and maps the return value to the return type for the given method
   * @returns The typed return value or undefined if there was no value
   */
  decodeReturnValue<TSignature extends CampusVotingDappNonVoidMethodSignatures>(method: TSignature, returnValue: ABIReturn | undefined) {
    return returnValue !== undefined ? getArc56ReturnValue<MethodReturn<TSignature>>(returnValue, this.appClient.getABIMethod(method), APP_SPEC.structs) : undefined
  }

  /**
   * Returns a new `CampusVotingDappClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<CampusVotingDappClient> {
    return new CampusVotingDappClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `CampusVotingDappClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<CampusVotingDappClient> {
    return new CampusVotingDappClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Gets available optIn methods
     */
    optIn: {
      /**
       * Opts the user into an existing instance of the CampusVotingDapp smart contract using a bare call.
       *
       * @param params The params for the bare (raw) call
       * @returns The optIn result
       */
      bare: (params?: Expand<AppClientBareCallParams>) => {
        return this.appClient.params.bare.optIn(params)
      },
    },

    /**
     * Makes a clear_state call to an existing instance of the CampusVotingDapp smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the CampusVotingDapp smart contract using the `create_poll(string,string,string,string,string,uint64,uint64,uint64)uint64` ABI method.
     *
    * Create a new poll. Only callable by the app creator.
    Returns the poll end timestamp.

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    createPoll: (params: CallParams<CampusVotingDappArgs['obj']['create_poll(string,string,string,string,string,uint64,uint64,uint64)uint64'] | CampusVotingDappArgs['tuple']['create_poll(string,string,string,string,string,uint64,uint64,uint64)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(CampusVotingDappParamsFactory.createPoll(params))
    },

    /**
     * Makes a call to the CampusVotingDapp smart contract using the `vote(uint64)string` ABI method.
     *
    * Cast a vote. Enforces:
    - Poll must be active - Voter must have opted in (has local state) - Voter must not have already voted - Option must be valid - If token_id > 0, sender must hold that ASA

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    vote: (params: CallParams<CampusVotingDappArgs['obj']['vote(uint64)string'] | CampusVotingDappArgs['tuple']['vote(uint64)string']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(CampusVotingDappParamsFactory.vote(params))
    },

    /**
     * Makes a call to the CampusVotingDapp smart contract using the `get_results()(uint64,uint64,uint64,uint64)` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * Return vote counts for all 4 options.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    getResults: (params: CallParams<CampusVotingDappArgs['obj']['get_results()(uint64,uint64,uint64,uint64)'] | CampusVotingDappArgs['tuple']['get_results()(uint64,uint64,uint64,uint64)']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(CampusVotingDappParamsFactory.getResults(params))
    },

    /**
     * Makes a call to the CampusVotingDapp smart contract using the `get_poll_info()(string,string,string,string,string,uint64,uint64,uint64,uint64)` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
    * Return full poll info:
    (question, opt_a, opt_b, opt_c, opt_d, num_options, end_time, token_id, total_votes)

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    getPollInfo: (params: CallParams<CampusVotingDappArgs['obj']['get_poll_info()(string,string,string,string,string,uint64,uint64,uint64,uint64)'] | CampusVotingDappArgs['tuple']['get_poll_info()(string,string,string,string,string,uint64,uint64,uint64,uint64)']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(CampusVotingDappParamsFactory.getPollInfo(params))
    },

    /**
     * Makes a call to the CampusVotingDapp smart contract using the `get_voter_status(address)uint64` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * Return the option a voter chose (0 = hasn't voted).
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    getVoterStatus: (params: CallParams<CampusVotingDappArgs['obj']['get_voter_status(address)uint64'] | CampusVotingDappArgs['tuple']['get_voter_status(address)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(CampusVotingDappParamsFactory.getVoterStatus(params))
    },

    /**
     * Makes a call to the CampusVotingDapp smart contract using the `close_poll()string` ABI method.
     *
     * Creator can manually close the poll before the deadline.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    closePoll: (params: CallParams<CampusVotingDappArgs['obj']['close_poll()string'] | CampusVotingDappArgs['tuple']['close_poll()string']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(CampusVotingDappParamsFactory.closePoll(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Gets available optIn methods
     */
    optIn: {
      /**
       * Opts the user into an existing instance of the CampusVotingDapp smart contract using a bare call.
       *
       * @param params The params for the bare (raw) call
       * @returns The optIn result
       */
      bare: (params?: Expand<AppClientBareCallParams>) => {
        return this.appClient.createTransaction.bare.optIn(params)
      },
    },

    /**
     * Makes a clear_state call to an existing instance of the CampusVotingDapp smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the CampusVotingDapp smart contract using the `create_poll(string,string,string,string,string,uint64,uint64,uint64)uint64` ABI method.
     *
    * Create a new poll. Only callable by the app creator.
    Returns the poll end timestamp.

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    createPoll: (params: CallParams<CampusVotingDappArgs['obj']['create_poll(string,string,string,string,string,uint64,uint64,uint64)uint64'] | CampusVotingDappArgs['tuple']['create_poll(string,string,string,string,string,uint64,uint64,uint64)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(CampusVotingDappParamsFactory.createPoll(params))
    },

    /**
     * Makes a call to the CampusVotingDapp smart contract using the `vote(uint64)string` ABI method.
     *
    * Cast a vote. Enforces:
    - Poll must be active - Voter must have opted in (has local state) - Voter must not have already voted - Option must be valid - If token_id > 0, sender must hold that ASA

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    vote: (params: CallParams<CampusVotingDappArgs['obj']['vote(uint64)string'] | CampusVotingDappArgs['tuple']['vote(uint64)string']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(CampusVotingDappParamsFactory.vote(params))
    },

    /**
     * Makes a call to the CampusVotingDapp smart contract using the `get_results()(uint64,uint64,uint64,uint64)` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * Return vote counts for all 4 options.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    getResults: (params: CallParams<CampusVotingDappArgs['obj']['get_results()(uint64,uint64,uint64,uint64)'] | CampusVotingDappArgs['tuple']['get_results()(uint64,uint64,uint64,uint64)']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(CampusVotingDappParamsFactory.getResults(params))
    },

    /**
     * Makes a call to the CampusVotingDapp smart contract using the `get_poll_info()(string,string,string,string,string,uint64,uint64,uint64,uint64)` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
    * Return full poll info:
    (question, opt_a, opt_b, opt_c, opt_d, num_options, end_time, token_id, total_votes)

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    getPollInfo: (params: CallParams<CampusVotingDappArgs['obj']['get_poll_info()(string,string,string,string,string,uint64,uint64,uint64,uint64)'] | CampusVotingDappArgs['tuple']['get_poll_info()(string,string,string,string,string,uint64,uint64,uint64,uint64)']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(CampusVotingDappParamsFactory.getPollInfo(params))
    },

    /**
     * Makes a call to the CampusVotingDapp smart contract using the `get_voter_status(address)uint64` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * Return the option a voter chose (0 = hasn't voted).
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    getVoterStatus: (params: CallParams<CampusVotingDappArgs['obj']['get_voter_status(address)uint64'] | CampusVotingDappArgs['tuple']['get_voter_status(address)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(CampusVotingDappParamsFactory.getVoterStatus(params))
    },

    /**
     * Makes a call to the CampusVotingDapp smart contract using the `close_poll()string` ABI method.
     *
     * Creator can manually close the poll before the deadline.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    closePoll: (params: CallParams<CampusVotingDappArgs['obj']['close_poll()string'] | CampusVotingDappArgs['tuple']['close_poll()string']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(CampusVotingDappParamsFactory.closePoll(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Gets available optIn methods
     */
    optIn: {
      /**
       * Opts the user into an existing instance of the CampusVotingDapp smart contract using a bare call.
       *
       * @param params The params for the bare (raw) call
       * @returns The optIn result
       */
      bare: (params?: Expand<AppClientBareCallParams & SendParams>) => {
        return this.appClient.send.bare.optIn(params)
      },
    },

    /**
     * Makes a clear_state call to an existing instance of the CampusVotingDapp smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the CampusVotingDapp smart contract using the `create_poll(string,string,string,string,string,uint64,uint64,uint64)uint64` ABI method.
     *
    * Create a new poll. Only callable by the app creator.
    Returns the poll end timestamp.

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    createPoll: async (params: CallParams<CampusVotingDappArgs['obj']['create_poll(string,string,string,string,string,uint64,uint64,uint64)uint64'] | CampusVotingDappArgs['tuple']['create_poll(string,string,string,string,string,uint64,uint64,uint64)uint64']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(CampusVotingDappParamsFactory.createPoll(params))
      return {...result, return: result.return as unknown as (undefined | CampusVotingDappReturns['create_poll(string,string,string,string,string,uint64,uint64,uint64)uint64'])}
    },

    /**
     * Makes a call to the CampusVotingDapp smart contract using the `vote(uint64)string` ABI method.
     *
    * Cast a vote. Enforces:
    - Poll must be active - Voter must have opted in (has local state) - Voter must not have already voted - Option must be valid - If token_id > 0, sender must hold that ASA

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    vote: async (params: CallParams<CampusVotingDappArgs['obj']['vote(uint64)string'] | CampusVotingDappArgs['tuple']['vote(uint64)string']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(CampusVotingDappParamsFactory.vote(params))
      return {...result, return: result.return as unknown as (undefined | CampusVotingDappReturns['vote(uint64)string'])}
    },

    /**
     * Makes a call to the CampusVotingDapp smart contract using the `get_results()(uint64,uint64,uint64,uint64)` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * Return vote counts for all 4 options.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    getResults: async (params: CallParams<CampusVotingDappArgs['obj']['get_results()(uint64,uint64,uint64,uint64)'] | CampusVotingDappArgs['tuple']['get_results()(uint64,uint64,uint64,uint64)']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(CampusVotingDappParamsFactory.getResults(params))
      return {...result, return: result.return as unknown as (undefined | CampusVotingDappReturns['get_results()(uint64,uint64,uint64,uint64)'])}
    },

    /**
     * Makes a call to the CampusVotingDapp smart contract using the `get_poll_info()(string,string,string,string,string,uint64,uint64,uint64,uint64)` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
    * Return full poll info:
    (question, opt_a, opt_b, opt_c, opt_d, num_options, end_time, token_id, total_votes)

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    getPollInfo: async (params: CallParams<CampusVotingDappArgs['obj']['get_poll_info()(string,string,string,string,string,uint64,uint64,uint64,uint64)'] | CampusVotingDappArgs['tuple']['get_poll_info()(string,string,string,string,string,uint64,uint64,uint64,uint64)']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(CampusVotingDappParamsFactory.getPollInfo(params))
      return {...result, return: result.return as unknown as (undefined | CampusVotingDappReturns['get_poll_info()(string,string,string,string,string,uint64,uint64,uint64,uint64)'])}
    },

    /**
     * Makes a call to the CampusVotingDapp smart contract using the `get_voter_status(address)uint64` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * Return the option a voter chose (0 = hasn't voted).
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    getVoterStatus: async (params: CallParams<CampusVotingDappArgs['obj']['get_voter_status(address)uint64'] | CampusVotingDappArgs['tuple']['get_voter_status(address)uint64']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(CampusVotingDappParamsFactory.getVoterStatus(params))
      return {...result, return: result.return as unknown as (undefined | CampusVotingDappReturns['get_voter_status(address)uint64'])}
    },

    /**
     * Makes a call to the CampusVotingDapp smart contract using the `close_poll()string` ABI method.
     *
     * Creator can manually close the poll before the deadline.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    closePoll: async (params: CallParams<CampusVotingDappArgs['obj']['close_poll()string'] | CampusVotingDappArgs['tuple']['close_poll()string']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(CampusVotingDappParamsFactory.closePoll(params))
      return {...result, return: result.return as unknown as (undefined | CampusVotingDappReturns['close_poll()string'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new CampusVotingDappClient(this.appClient.clone(params))
  }

  /**
   * Makes a readonly (simulated) call to the CampusVotingDapp smart contract using the `get_results()(uint64,uint64,uint64,uint64)` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * Return vote counts for all 4 options.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async getResults(params: CallParams<CampusVotingDappArgs['obj']['get_results()(uint64,uint64,uint64,uint64)'] | CampusVotingDappArgs['tuple']['get_results()(uint64,uint64,uint64,uint64)']> = {args: []}) {
    const result = await this.appClient.send.call(CampusVotingDappParamsFactory.getResults(params))
    return result.return as unknown as CampusVotingDappReturns['get_results()(uint64,uint64,uint64,uint64)']
  }

  /**
   * Makes a readonly (simulated) call to the CampusVotingDapp smart contract using the `get_poll_info()(string,string,string,string,string,uint64,uint64,uint64,uint64)` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
  * Return full poll info:
  (question, opt_a, opt_b, opt_c, opt_d, num_options, end_time, token_id, total_votes)

   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async getPollInfo(params: CallParams<CampusVotingDappArgs['obj']['get_poll_info()(string,string,string,string,string,uint64,uint64,uint64,uint64)'] | CampusVotingDappArgs['tuple']['get_poll_info()(string,string,string,string,string,uint64,uint64,uint64,uint64)']> = {args: []}) {
    const result = await this.appClient.send.call(CampusVotingDappParamsFactory.getPollInfo(params))
    return result.return as unknown as CampusVotingDappReturns['get_poll_info()(string,string,string,string,string,uint64,uint64,uint64,uint64)']
  }

  /**
   * Makes a readonly (simulated) call to the CampusVotingDapp smart contract using the `get_voter_status(address)uint64` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * Return the option a voter chose (0 = hasn't voted).
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async getVoterStatus(params: CallParams<CampusVotingDappArgs['obj']['get_voter_status(address)uint64'] | CampusVotingDappArgs['tuple']['get_voter_status(address)uint64']>) {
    const result = await this.appClient.send.call(CampusVotingDappParamsFactory.getVoterStatus(params))
    return result.return as unknown as CampusVotingDappReturns['get_voter_status(address)uint64']
  }

  /**
   * Methods to access state for the current CampusVotingDapp app
   */
  state = {
    /**
     * Methods to access global state for the current CampusVotingDapp app
     */
    global: {
      /**
       * Get all current keyed values from global state
       */
      getAll: async (): Promise<Partial<Expand<GlobalKeysState>>> => {
        const result = await this.appClient.state.global.getAll()
        return {
          question: result.question,
          optionA: result.option_a,
          optionB: result.option_b,
          optionC: result.option_c,
          optionD: result.option_d,
          numOptions: result.num_options,
          votesA: result.votes_a,
          votesB: result.votes_b,
          votesC: result.votes_c,
          votesD: result.votes_d,
          endTime: result.end_time,
          creator: result.creator,
          tokenId: result.token_id,
          totalVotes: result.total_votes,
          pollActive: result.poll_active,
        }
      },
      /**
       * Get the current value of the question key in global state
       */
      question: async (): Promise<string | undefined> => { return (await this.appClient.state.global.getValue("question")) as string | undefined },
      /**
       * Get the current value of the option_a key in global state
       */
      optionA: async (): Promise<string | undefined> => { return (await this.appClient.state.global.getValue("option_a")) as string | undefined },
      /**
       * Get the current value of the option_b key in global state
       */
      optionB: async (): Promise<string | undefined> => { return (await this.appClient.state.global.getValue("option_b")) as string | undefined },
      /**
       * Get the current value of the option_c key in global state
       */
      optionC: async (): Promise<string | undefined> => { return (await this.appClient.state.global.getValue("option_c")) as string | undefined },
      /**
       * Get the current value of the option_d key in global state
       */
      optionD: async (): Promise<string | undefined> => { return (await this.appClient.state.global.getValue("option_d")) as string | undefined },
      /**
       * Get the current value of the num_options key in global state
       */
      numOptions: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("num_options")) as bigint | undefined },
      /**
       * Get the current value of the votes_a key in global state
       */
      votesA: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("votes_a")) as bigint | undefined },
      /**
       * Get the current value of the votes_b key in global state
       */
      votesB: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("votes_b")) as bigint | undefined },
      /**
       * Get the current value of the votes_c key in global state
       */
      votesC: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("votes_c")) as bigint | undefined },
      /**
       * Get the current value of the votes_d key in global state
       */
      votesD: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("votes_d")) as bigint | undefined },
      /**
       * Get the current value of the end_time key in global state
       */
      endTime: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("end_time")) as bigint | undefined },
      /**
       * Get the current value of the creator key in global state
       */
      creator: async (): Promise<string | undefined> => { return (await this.appClient.state.global.getValue("creator")) as string | undefined },
      /**
       * Get the current value of the token_id key in global state
       */
      tokenId: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("token_id")) as bigint | undefined },
      /**
       * Get the current value of the total_votes key in global state
       */
      totalVotes: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("total_votes")) as bigint | undefined },
      /**
       * Get the current value of the poll_active key in global state
       */
      pollActive: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("poll_active")) as bigint | undefined },
    },
    /**
     * Methods to access local state for the current CampusVotingDapp app
     */
    local: (address: string | Address) => {
      const encodedAddress = typeof address === 'string' ? address : encodeAddress(address.publicKey)
      return {
        /**
         * Get all current keyed values from local state
         */
        getAll: async (): Promise<Partial<Expand<LocalKeysState>>> => {
          const result = await this.appClient.state.local(encodedAddress).getAll()
          return {
            hasVoted: result.has_voted,
          }
        },
        /**
         * Get the current value of the has_voted key in local state
         */
        hasVoted: async (): Promise<bigint | undefined> => { return (await this.appClient.state.local(encodedAddress).getValue("has_voted")) as bigint | undefined },
      }
    },
  }

  public newGroup(): CampusVotingDappComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a create_poll(string,string,string,string,string,uint64,uint64,uint64)uint64 method call against the CampusVotingDapp contract
       */
      createPoll(params: CallParams<CampusVotingDappArgs['obj']['create_poll(string,string,string,string,string,uint64,uint64,uint64)uint64'] | CampusVotingDappArgs['tuple']['create_poll(string,string,string,string,string,uint64,uint64,uint64)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.createPoll(params)))
        resultMappers.push((v) => client.decodeReturnValue('create_poll(string,string,string,string,string,uint64,uint64,uint64)uint64', v))
        return this
      },
      /**
       * Add a vote(uint64)string method call against the CampusVotingDapp contract
       */
      vote(params: CallParams<CampusVotingDappArgs['obj']['vote(uint64)string'] | CampusVotingDappArgs['tuple']['vote(uint64)string']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.vote(params)))
        resultMappers.push((v) => client.decodeReturnValue('vote(uint64)string', v))
        return this
      },
      /**
       * Add a get_results()(uint64,uint64,uint64,uint64) method call against the CampusVotingDapp contract
       */
      getResults(params: CallParams<CampusVotingDappArgs['obj']['get_results()(uint64,uint64,uint64,uint64)'] | CampusVotingDappArgs['tuple']['get_results()(uint64,uint64,uint64,uint64)']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.getResults(params)))
        resultMappers.push((v) => client.decodeReturnValue('get_results()(uint64,uint64,uint64,uint64)', v))
        return this
      },
      /**
       * Add a get_poll_info()(string,string,string,string,string,uint64,uint64,uint64,uint64) method call against the CampusVotingDapp contract
       */
      getPollInfo(params: CallParams<CampusVotingDappArgs['obj']['get_poll_info()(string,string,string,string,string,uint64,uint64,uint64,uint64)'] | CampusVotingDappArgs['tuple']['get_poll_info()(string,string,string,string,string,uint64,uint64,uint64,uint64)']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.getPollInfo(params)))
        resultMappers.push((v) => client.decodeReturnValue('get_poll_info()(string,string,string,string,string,uint64,uint64,uint64,uint64)', v))
        return this
      },
      /**
       * Add a get_voter_status(address)uint64 method call against the CampusVotingDapp contract
       */
      getVoterStatus(params: CallParams<CampusVotingDappArgs['obj']['get_voter_status(address)uint64'] | CampusVotingDappArgs['tuple']['get_voter_status(address)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.getVoterStatus(params)))
        resultMappers.push((v) => client.decodeReturnValue('get_voter_status(address)uint64', v))
        return this
      },
      /**
       * Add a close_poll()string method call against the CampusVotingDapp contract
       */
      closePoll(params: CallParams<CampusVotingDappArgs['obj']['close_poll()string'] | CampusVotingDappArgs['tuple']['close_poll()string']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.closePoll(params)))
        resultMappers.push((v) => client.decodeReturnValue('close_poll()string', v))
        return this
      },
      get optIn() {
        return {
          bare: (params?: AppClientBareCallParams ) => {
            promiseChain = promiseChain.then(() => composer.addAppCall(client.params.optIn.bare(params)))
            return this
          },
        }
      },
      /**
       * Add a clear state call to the CampusVotingDapp contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as CampusVotingDappComposer
  }
}
export type CampusVotingDappComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the create_poll(string,string,string,string,string,uint64,uint64,uint64)uint64 ABI method.
   *
  * Create a new poll. Only callable by the app creator.
  Returns the poll end timestamp.

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  createPoll(params?: CallParams<CampusVotingDappArgs['obj']['create_poll(string,string,string,string,string,uint64,uint64,uint64)uint64'] | CampusVotingDappArgs['tuple']['create_poll(string,string,string,string,string,uint64,uint64,uint64)uint64']>): CampusVotingDappComposer<[...TReturns, CampusVotingDappReturns['create_poll(string,string,string,string,string,uint64,uint64,uint64)uint64'] | undefined]>

  /**
   * Calls the vote(uint64)string ABI method.
   *
  * Cast a vote. Enforces:
  - Poll must be active - Voter must have opted in (has local state) - Voter must not have already voted - Option must be valid - If token_id > 0, sender must hold that ASA

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  vote(params?: CallParams<CampusVotingDappArgs['obj']['vote(uint64)string'] | CampusVotingDappArgs['tuple']['vote(uint64)string']>): CampusVotingDappComposer<[...TReturns, CampusVotingDappReturns['vote(uint64)string'] | undefined]>

  /**
   * Calls the get_results()(uint64,uint64,uint64,uint64) ABI method.
   *
   * Return vote counts for all 4 options.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  getResults(params?: CallParams<CampusVotingDappArgs['obj']['get_results()(uint64,uint64,uint64,uint64)'] | CampusVotingDappArgs['tuple']['get_results()(uint64,uint64,uint64,uint64)']>): CampusVotingDappComposer<[...TReturns, CampusVotingDappReturns['get_results()(uint64,uint64,uint64,uint64)'] | undefined]>

  /**
   * Calls the get_poll_info()(string,string,string,string,string,uint64,uint64,uint64,uint64) ABI method.
   *
  * Return full poll info:
  (question, opt_a, opt_b, opt_c, opt_d, num_options, end_time, token_id, total_votes)

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  getPollInfo(params?: CallParams<CampusVotingDappArgs['obj']['get_poll_info()(string,string,string,string,string,uint64,uint64,uint64,uint64)'] | CampusVotingDappArgs['tuple']['get_poll_info()(string,string,string,string,string,uint64,uint64,uint64,uint64)']>): CampusVotingDappComposer<[...TReturns, CampusVotingDappReturns['get_poll_info()(string,string,string,string,string,uint64,uint64,uint64,uint64)'] | undefined]>

  /**
   * Calls the get_voter_status(address)uint64 ABI method.
   *
   * Return the option a voter chose (0 = hasn't voted).
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  getVoterStatus(params?: CallParams<CampusVotingDappArgs['obj']['get_voter_status(address)uint64'] | CampusVotingDappArgs['tuple']['get_voter_status(address)uint64']>): CampusVotingDappComposer<[...TReturns, CampusVotingDappReturns['get_voter_status(address)uint64'] | undefined]>

  /**
   * Calls the close_poll()string ABI method.
   *
   * Creator can manually close the poll before the deadline.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  closePoll(params?: CallParams<CampusVotingDappArgs['obj']['close_poll()string'] | CampusVotingDappArgs['tuple']['close_poll()string']>): CampusVotingDappComposer<[...TReturns, CampusVotingDappReturns['close_poll()string'] | undefined]>

  /**
   * Gets available optIn methods
   */
  readonly optIn: {
    /**
     * Opts the user into an existing instance of the CampusVotingDapp smart contract using a bare call.
     *
     * @param args The arguments for the bare call
     * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
     */
    bare(params?: AppClientBareCallParams ): CampusVotingDappComposer<[...TReturns, undefined]>
  }

  /**
   * Makes a clear_state call to an existing instance of the CampusVotingDapp smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): CampusVotingDappComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): CampusVotingDappComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<CampusVotingDappComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<CampusVotingDappComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<CampusVotingDappComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<CampusVotingDappComposerResults<TReturns>>
}
export type CampusVotingDappComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

