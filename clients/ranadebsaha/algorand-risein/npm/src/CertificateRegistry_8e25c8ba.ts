/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"CertificateRegistry","structs":{},"methods":[{"name":"register_certificate","args":[{"type":"byte[]","name":"cert_hash","desc":"The SHA-256 hash of the certificate file."}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Registers a new certificate hash and associates it with the transaction sender.","events":[],"recommendations":{}},{"name":"verify_certificate","args":[{"type":"byte[]","name":"cert_hash","desc":"The SHA-256 hash of the certificate to verify."}],"returns":{"type":"byte[]","desc":"The owner's address as Bytes if found, otherwise an empty Bytes string."},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Verifies if a given certificate hash is registered and returns the owner's address.","events":[],"recommendations":{}},{"name":"transfer_certificate","args":[{"type":"byte[]","name":"cert_hash","desc":"The hash of the certificate to transfer."},{"type":"byte[]","name":"new_owner","desc":"The Algorand address of the new owner."}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Transfers ownership of a certificate to a new address.\nThis can only be called by the current owner of the certificate.","events":[],"recommendations":{}}],"arcs":[22,28],"desc":"\n    A smart contract for registering and verifying certificate hashes on the Algorand blockchain.\n    It uses Algorand's Box storage to map a certificate's hash to its owner's address.\n    ","networks":{},"state":{"schema":{"global":{"ints":0,"bytes":0},"local":{"ints":0,"bytes":0}},"keys":{"global":{},"local":{},"box":{}},"maps":{"global":{},"local":{},"box":{"certificates":{"keyType":"AVMBytes","valueType":"AVMBytes","prefix":"Y2VydGlmaWNhdGVz"}}}},"bareActions":{"create":["NoOp"],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[159],"errorMessage":"Certificate hash is already registered."},{"pc":[197],"errorMessage":"Certificate not found or not registered."},{"pc":[59,83,118],"errorMessage":"OnCompletion is not NoOp"},{"pc":[201],"errorMessage":"Permission denied: Only the current owner can transfer."},{"pc":[141],"errorMessage":"can only call when creating"},{"pc":[62,86,121],"errorMessage":"can only call when not creating"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"events":[]} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


/**
 * The argument types for the CertificateRegistry contract
 */
export type CertificateRegistryArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'register_certificate(byte[])void': {
      /**
       * The SHA-256 hash of the certificate file.
       */
      certHash: Uint8Array
    }
    'verify_certificate(byte[])byte[]': {
      /**
       * The SHA-256 hash of the certificate to verify.
       */
      certHash: Uint8Array
    }
    'transfer_certificate(byte[],byte[])void': {
      /**
       * The hash of the certificate to transfer.
       */
      certHash: Uint8Array
      /**
       * The Algorand address of the new owner.
       */
      newOwner: Uint8Array
    }
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'register_certificate(byte[])void': [certHash: Uint8Array]
    'verify_certificate(byte[])byte[]': [certHash: Uint8Array]
    'transfer_certificate(byte[],byte[])void': [certHash: Uint8Array, newOwner: Uint8Array]
  }
}

/**
 * The return type for each method
 */
export type CertificateRegistryReturns = {
  'register_certificate(byte[])void': void
  'verify_certificate(byte[])byte[]': Uint8Array
  'transfer_certificate(byte[],byte[])void': void
}

/**
 * Defines the types of available calls and state of the CertificateRegistry smart contract.
 */
export type CertificateRegistryTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'register_certificate(byte[])void' | 'register_certificate', {
      argsObj: CertificateRegistryArgs['obj']['register_certificate(byte[])void']
      argsTuple: CertificateRegistryArgs['tuple']['register_certificate(byte[])void']
      returns: CertificateRegistryReturns['register_certificate(byte[])void']
    }>
    & Record<'verify_certificate(byte[])byte[]' | 'verify_certificate', {
      argsObj: CertificateRegistryArgs['obj']['verify_certificate(byte[])byte[]']
      argsTuple: CertificateRegistryArgs['tuple']['verify_certificate(byte[])byte[]']
      /**
       * The owner's address as Bytes if found, otherwise an empty Bytes string.
       */
      returns: CertificateRegistryReturns['verify_certificate(byte[])byte[]']
    }>
    & Record<'transfer_certificate(byte[],byte[])void' | 'transfer_certificate', {
      argsObj: CertificateRegistryArgs['obj']['transfer_certificate(byte[],byte[])void']
      argsTuple: CertificateRegistryArgs['tuple']['transfer_certificate(byte[],byte[])void']
      returns: CertificateRegistryReturns['transfer_certificate(byte[],byte[])void']
    }>
  /**
   * Defines the shape of the state of the application.
   */
  state: {
    box: {
      keys: {}
      maps: {
        certificates: Map<Uint8Array | string, Uint8Array>
      }
    }
  }
}

/**
 * Defines the possible abi call signatures.
 */
export type CertificateRegistrySignatures = keyof CertificateRegistryTypes['methods']
/**
 * Defines the possible abi call signatures for methods that return a non-void value.
 */
export type CertificateRegistryNonVoidMethodSignatures = keyof CertificateRegistryTypes['methods'] extends infer T ? T extends keyof CertificateRegistryTypes['methods'] ? MethodReturn<T> extends void ? never : T  : never : never
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the CertificateRegistry smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends CertificateRegistrySignatures> = CertificateRegistryTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the CertificateRegistry smart contract to the method's return type
 */
export type MethodReturn<TSignature extends CertificateRegistrySignatures> = CertificateRegistryTypes['methods'][TSignature]['returns']

/**
 * Defines the shape of the keyed box state of the application.
 */
export type BoxKeysState = CertificateRegistryTypes['state']['box']['keys']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the CertificateRegistry smart contract
 */
export abstract class CertificateRegistryParamsFactory {
  /**
   * Constructs a no op call for the register_certificate(byte[])void ABI method
   *
   * Registers a new certificate hash and associates it with the transaction sender.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static registerCertificate(params: CallParams<CertificateRegistryArgs['obj']['register_certificate(byte[])void'] | CertificateRegistryArgs['tuple']['register_certificate(byte[])void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'register_certificate(byte[])void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.certHash],
    }
  }
  /**
   * Constructs a no op call for the verify_certificate(byte[])byte[] ABI method
   *
   * Verifies if a given certificate hash is registered and returns the owner's address.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static verifyCertificate(params: CallParams<CertificateRegistryArgs['obj']['verify_certificate(byte[])byte[]'] | CertificateRegistryArgs['tuple']['verify_certificate(byte[])byte[]']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'verify_certificate(byte[])byte[]' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.certHash],
    }
  }
  /**
   * Constructs a no op call for the transfer_certificate(byte[],byte[])void ABI method
   *
  * Transfers ownership of a certificate to a new address.
  This can only be called by the current owner of the certificate.

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static transferCertificate(params: CallParams<CertificateRegistryArgs['obj']['transfer_certificate(byte[],byte[])void'] | CertificateRegistryArgs['tuple']['transfer_certificate(byte[],byte[])void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'transfer_certificate(byte[],byte[])void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.certHash, params.args.newOwner],
    }
  }
}

/**
 * A client to make calls to the CertificateRegistry smart contract
 */
export class CertificateRegistryClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `CertificateRegistryClient`
   *
   * @param appClient An `AppClient` instance which has been created with the CertificateRegistry app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `CertificateRegistryClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Checks for decode errors on the given return value and maps the return value to the return type for the given method
   * @returns The typed return value or undefined if there was no value
   */
  decodeReturnValue<TSignature extends CertificateRegistryNonVoidMethodSignatures>(method: TSignature, returnValue: ABIReturn | undefined) {
    return returnValue !== undefined ? getArc56ReturnValue<MethodReturn<TSignature>>(returnValue, this.appClient.getABIMethod(method), APP_SPEC.structs) : undefined
  }

  /**
   * Returns a new `CertificateRegistryClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<CertificateRegistryClient> {
    return new CertificateRegistryClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `CertificateRegistryClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<CertificateRegistryClient> {
    return new CertificateRegistryClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Makes a clear_state call to an existing instance of the CertificateRegistry smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the CertificateRegistry smart contract using the `register_certificate(byte[])void` ABI method.
     *
     * Registers a new certificate hash and associates it with the transaction sender.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    registerCertificate: (params: CallParams<CertificateRegistryArgs['obj']['register_certificate(byte[])void'] | CertificateRegistryArgs['tuple']['register_certificate(byte[])void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(CertificateRegistryParamsFactory.registerCertificate(params))
    },

    /**
     * Makes a call to the CertificateRegistry smart contract using the `verify_certificate(byte[])byte[]` ABI method.
     *
     * Verifies if a given certificate hash is registered and returns the owner's address.
     *
     * @param params The params for the smart contract call
     * @returns The call params: The owner's address as Bytes if found, otherwise an empty Bytes string.
     */
    verifyCertificate: (params: CallParams<CertificateRegistryArgs['obj']['verify_certificate(byte[])byte[]'] | CertificateRegistryArgs['tuple']['verify_certificate(byte[])byte[]']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(CertificateRegistryParamsFactory.verifyCertificate(params))
    },

    /**
     * Makes a call to the CertificateRegistry smart contract using the `transfer_certificate(byte[],byte[])void` ABI method.
     *
    * Transfers ownership of a certificate to a new address.
    This can only be called by the current owner of the certificate.

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    transferCertificate: (params: CallParams<CertificateRegistryArgs['obj']['transfer_certificate(byte[],byte[])void'] | CertificateRegistryArgs['tuple']['transfer_certificate(byte[],byte[])void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(CertificateRegistryParamsFactory.transferCertificate(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Makes a clear_state call to an existing instance of the CertificateRegistry smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the CertificateRegistry smart contract using the `register_certificate(byte[])void` ABI method.
     *
     * Registers a new certificate hash and associates it with the transaction sender.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    registerCertificate: (params: CallParams<CertificateRegistryArgs['obj']['register_certificate(byte[])void'] | CertificateRegistryArgs['tuple']['register_certificate(byte[])void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(CertificateRegistryParamsFactory.registerCertificate(params))
    },

    /**
     * Makes a call to the CertificateRegistry smart contract using the `verify_certificate(byte[])byte[]` ABI method.
     *
     * Verifies if a given certificate hash is registered and returns the owner's address.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction: The owner's address as Bytes if found, otherwise an empty Bytes string.
     */
    verifyCertificate: (params: CallParams<CertificateRegistryArgs['obj']['verify_certificate(byte[])byte[]'] | CertificateRegistryArgs['tuple']['verify_certificate(byte[])byte[]']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(CertificateRegistryParamsFactory.verifyCertificate(params))
    },

    /**
     * Makes a call to the CertificateRegistry smart contract using the `transfer_certificate(byte[],byte[])void` ABI method.
     *
    * Transfers ownership of a certificate to a new address.
    This can only be called by the current owner of the certificate.

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    transferCertificate: (params: CallParams<CertificateRegistryArgs['obj']['transfer_certificate(byte[],byte[])void'] | CertificateRegistryArgs['tuple']['transfer_certificate(byte[],byte[])void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(CertificateRegistryParamsFactory.transferCertificate(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Makes a clear_state call to an existing instance of the CertificateRegistry smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the CertificateRegistry smart contract using the `register_certificate(byte[])void` ABI method.
     *
     * Registers a new certificate hash and associates it with the transaction sender.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    registerCertificate: async (params: CallParams<CertificateRegistryArgs['obj']['register_certificate(byte[])void'] | CertificateRegistryArgs['tuple']['register_certificate(byte[])void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(CertificateRegistryParamsFactory.registerCertificate(params))
      return {...result, return: result.return as unknown as (undefined | CertificateRegistryReturns['register_certificate(byte[])void'])}
    },

    /**
     * Makes a call to the CertificateRegistry smart contract using the `verify_certificate(byte[])byte[]` ABI method.
     *
     * Verifies if a given certificate hash is registered and returns the owner's address.
     *
     * @param params The params for the smart contract call
     * @returns The call result: The owner's address as Bytes if found, otherwise an empty Bytes string.
     */
    verifyCertificate: async (params: CallParams<CertificateRegistryArgs['obj']['verify_certificate(byte[])byte[]'] | CertificateRegistryArgs['tuple']['verify_certificate(byte[])byte[]']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(CertificateRegistryParamsFactory.verifyCertificate(params))
      return {...result, return: result.return as unknown as (undefined | CertificateRegistryReturns['verify_certificate(byte[])byte[]'])}
    },

    /**
     * Makes a call to the CertificateRegistry smart contract using the `transfer_certificate(byte[],byte[])void` ABI method.
     *
    * Transfers ownership of a certificate to a new address.
    This can only be called by the current owner of the certificate.

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    transferCertificate: async (params: CallParams<CertificateRegistryArgs['obj']['transfer_certificate(byte[],byte[])void'] | CertificateRegistryArgs['tuple']['transfer_certificate(byte[],byte[])void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(CertificateRegistryParamsFactory.transferCertificate(params))
      return {...result, return: result.return as unknown as (undefined | CertificateRegistryReturns['transfer_certificate(byte[],byte[])void'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new CertificateRegistryClient(this.appClient.clone(params))
  }

  /**
   * Methods to access state for the current CertificateRegistry app
   */
  state = {
    /**
     * Methods to access box state for the current CertificateRegistry app
     */
    box: {
      /**
       * Get all current keyed values from box state
       */
      getAll: async (): Promise<Partial<Expand<BoxKeysState>>> => {
        const result = await this.appClient.state.box.getAll()
        return {
        }
      },
      /**
       * Get values from the certificates map in box state
       */
      certificates: {
        /**
         * Get all current values of the certificates map in box state
         */
        getMap: async (): Promise<Map<Uint8Array, Uint8Array>> => { return (await this.appClient.state.box.getMap("certificates")) as Map<Uint8Array, Uint8Array> },
        /**
         * Get a current value of the certificates map by key from box state
         */
        value: async (key: Uint8Array | string): Promise<Uint8Array | undefined> => { return await this.appClient.state.box.getMapValue("certificates", key) as Uint8Array | undefined },
      },
    },
  }

  public newGroup(): CertificateRegistryComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a register_certificate(byte[])void method call against the CertificateRegistry contract
       */
      registerCertificate(params: CallParams<CertificateRegistryArgs['obj']['register_certificate(byte[])void'] | CertificateRegistryArgs['tuple']['register_certificate(byte[])void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.registerCertificate(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a verify_certificate(byte[])byte[] method call against the CertificateRegistry contract
       */
      verifyCertificate(params: CallParams<CertificateRegistryArgs['obj']['verify_certificate(byte[])byte[]'] | CertificateRegistryArgs['tuple']['verify_certificate(byte[])byte[]']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.verifyCertificate(params)))
        resultMappers.push((v) => client.decodeReturnValue('verify_certificate(byte[])byte[]', v))
        return this
      },
      /**
       * Add a transfer_certificate(byte[],byte[])void method call against the CertificateRegistry contract
       */
      transferCertificate(params: CallParams<CertificateRegistryArgs['obj']['transfer_certificate(byte[],byte[])void'] | CertificateRegistryArgs['tuple']['transfer_certificate(byte[],byte[])void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.transferCertificate(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a clear state call to the CertificateRegistry contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as CertificateRegistryComposer
  }
}
export type CertificateRegistryComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the register_certificate(byte[])void ABI method.
   *
   * Registers a new certificate hash and associates it with the transaction sender.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  registerCertificate(params?: CallParams<CertificateRegistryArgs['obj']['register_certificate(byte[])void'] | CertificateRegistryArgs['tuple']['register_certificate(byte[])void']>): CertificateRegistryComposer<[...TReturns, CertificateRegistryReturns['register_certificate(byte[])void'] | undefined]>

  /**
   * Calls the verify_certificate(byte[])byte[] ABI method.
   *
   * Verifies if a given certificate hash is registered and returns the owner's address.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  verifyCertificate(params?: CallParams<CertificateRegistryArgs['obj']['verify_certificate(byte[])byte[]'] | CertificateRegistryArgs['tuple']['verify_certificate(byte[])byte[]']>): CertificateRegistryComposer<[...TReturns, CertificateRegistryReturns['verify_certificate(byte[])byte[]'] | undefined]>

  /**
   * Calls the transfer_certificate(byte[],byte[])void ABI method.
   *
  * Transfers ownership of a certificate to a new address.
  This can only be called by the current owner of the certificate.

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  transferCertificate(params?: CallParams<CertificateRegistryArgs['obj']['transfer_certificate(byte[],byte[])void'] | CertificateRegistryArgs['tuple']['transfer_certificate(byte[],byte[])void']>): CertificateRegistryComposer<[...TReturns, CertificateRegistryReturns['transfer_certificate(byte[],byte[])void'] | undefined]>

  /**
   * Makes a clear_state call to an existing instance of the CertificateRegistry smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): CertificateRegistryComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): CertificateRegistryComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<CertificateRegistryComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<CertificateRegistryComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<CertificateRegistryComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<CertificateRegistryComposerResults<TReturns>>
}
export type CertificateRegistryComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

