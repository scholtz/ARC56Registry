/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"MedicineLendingPlatform","structs":{},"methods":[{"name":"register_medicine","args":[{"type":"string","name":"name"},{"type":"uint64","name":"expiry"},{"type":"uint64","name":"quantity"}],"returns":{"type":"bool"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Registers or updates a medicine.\nIf the medicine already exists, increases its quantity. Sets the lender to the transaction sender.","events":[],"recommendations":{}},{"name":"request_medicine","args":[{"type":"string","name":"name"}],"returns":{"type":"bool"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Records a request for a medicine if available and not expired.","events":[],"recommendations":{}},{"name":"approve_request","args":[{"type":"string","name":"name"}],"returns":{"type":"bool"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Approves a medicine request.\nOnly the lender (registrant) may approve. On approval, decreases quantity and records the medicine record.","events":[],"recommendations":{}},{"name":"return_medicine","args":[{"type":"string","name":"name"}],"returns":{"type":"bool"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Processes the return of a medicine by increasing its quantity.","events":[],"recommendations":{}},{"name":"is_expired","args":[{"type":"string","name":"name"}],"returns":{"type":"bool"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Checks if a medicine is expired (i.e. current round > expiry).","events":[],"recommendations":{}}],"arcs":[22,28],"networks":{},"state":{"schema":{"global":{"ints":0,"bytes":3},"local":{"ints":0,"bytes":0}},"keys":{"global":{"medicines":{"keyType":"AVMString","valueType":"(string,uint64,uint64,address)[]","key":"bWVkaWNpbmVz"},"medicine_requests":{"keyType":"AVMString","valueType":"(string,address)[]","key":"bWVkaWNpbmVfcmVxdWVzdHM="},"medicine_records":{"keyType":"AVMString","valueType":"(string,address,uint64)[]","key":"bWVkaWNpbmVfcmVjb3Jkcw=="}},"local":{},"box":{}},"maps":{"global":{},"local":{},"box":{}}},"bareActions":{"create":["NoOp"],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[437,570,604,615,734,768,834,868,879,996,1085,1119],"errorMessage":"Index access is out of bounds"},{"pc":[148,167,186,205,224],"errorMessage":"OnCompletion is not NoOp"},{"pc":[254],"errorMessage":"can only call when creating"},{"pc":[151,170,189,208,227],"errorMessage":"can only call when not creating"},{"pc":[901],"errorMessage":"check self.medicine_records exists"},{"pc":[639,791],"errorMessage":"check self.medicine_requests exists"},{"pc":[392,407,490,525,540,689,704,951,966,1040,1055],"errorMessage":"check self.medicines exists"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"events":[]} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


/**
 * The argument types for the MedicineLendingPlatform contract
 */
export type MedicineLendingPlatformArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'register_medicine(string,uint64,uint64)bool': {
      name: string
      expiry: bigint | number
      quantity: bigint | number
    }
    'request_medicine(string)bool': {
      name: string
    }
    'approve_request(string)bool': {
      name: string
    }
    'return_medicine(string)bool': {
      name: string
    }
    'is_expired(string)bool': {
      name: string
    }
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'register_medicine(string,uint64,uint64)bool': [name: string, expiry: bigint | number, quantity: bigint | number]
    'request_medicine(string)bool': [name: string]
    'approve_request(string)bool': [name: string]
    'return_medicine(string)bool': [name: string]
    'is_expired(string)bool': [name: string]
  }
}

/**
 * The return type for each method
 */
export type MedicineLendingPlatformReturns = {
  'register_medicine(string,uint64,uint64)bool': boolean
  'request_medicine(string)bool': boolean
  'approve_request(string)bool': boolean
  'return_medicine(string)bool': boolean
  'is_expired(string)bool': boolean
}

/**
 * Defines the types of available calls and state of the MedicineLendingPlatform smart contract.
 */
export type MedicineLendingPlatformTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'register_medicine(string,uint64,uint64)bool' | 'register_medicine', {
      argsObj: MedicineLendingPlatformArgs['obj']['register_medicine(string,uint64,uint64)bool']
      argsTuple: MedicineLendingPlatformArgs['tuple']['register_medicine(string,uint64,uint64)bool']
      returns: MedicineLendingPlatformReturns['register_medicine(string,uint64,uint64)bool']
    }>
    & Record<'request_medicine(string)bool' | 'request_medicine', {
      argsObj: MedicineLendingPlatformArgs['obj']['request_medicine(string)bool']
      argsTuple: MedicineLendingPlatformArgs['tuple']['request_medicine(string)bool']
      returns: MedicineLendingPlatformReturns['request_medicine(string)bool']
    }>
    & Record<'approve_request(string)bool' | 'approve_request', {
      argsObj: MedicineLendingPlatformArgs['obj']['approve_request(string)bool']
      argsTuple: MedicineLendingPlatformArgs['tuple']['approve_request(string)bool']
      returns: MedicineLendingPlatformReturns['approve_request(string)bool']
    }>
    & Record<'return_medicine(string)bool' | 'return_medicine', {
      argsObj: MedicineLendingPlatformArgs['obj']['return_medicine(string)bool']
      argsTuple: MedicineLendingPlatformArgs['tuple']['return_medicine(string)bool']
      returns: MedicineLendingPlatformReturns['return_medicine(string)bool']
    }>
    & Record<'is_expired(string)bool' | 'is_expired', {
      argsObj: MedicineLendingPlatformArgs['obj']['is_expired(string)bool']
      argsTuple: MedicineLendingPlatformArgs['tuple']['is_expired(string)bool']
      returns: MedicineLendingPlatformReturns['is_expired(string)bool']
    }>
  /**
   * Defines the shape of the state of the application.
   */
  state: {
    global: {
      keys: {
        medicines: [string, bigint, bigint, string][]
        medicineRequests: [string, string][]
        medicineRecords: [string, string, bigint][]
      }
      maps: {}
    }
  }
}

/**
 * Defines the possible abi call signatures.
 */
export type MedicineLendingPlatformSignatures = keyof MedicineLendingPlatformTypes['methods']
/**
 * Defines the possible abi call signatures for methods that return a non-void value.
 */
export type MedicineLendingPlatformNonVoidMethodSignatures = keyof MedicineLendingPlatformTypes['methods'] extends infer T ? T extends keyof MedicineLendingPlatformTypes['methods'] ? MethodReturn<T> extends void ? never : T  : never : never
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the MedicineLendingPlatform smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends MedicineLendingPlatformSignatures> = MedicineLendingPlatformTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the MedicineLendingPlatform smart contract to the method's return type
 */
export type MethodReturn<TSignature extends MedicineLendingPlatformSignatures> = MedicineLendingPlatformTypes['methods'][TSignature]['returns']

/**
 * Defines the shape of the keyed global state of the application.
 */
export type GlobalKeysState = MedicineLendingPlatformTypes['state']['global']['keys']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the MedicineLendingPlatform smart contract
 */
export abstract class MedicineLendingPlatformParamsFactory {
  /**
   * Constructs a no op call for the register_medicine(string,uint64,uint64)bool ABI method
   *
  * Registers or updates a medicine.
  If the medicine already exists, increases its quantity. Sets the lender to the transaction sender.

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static registerMedicine(params: CallParams<MedicineLendingPlatformArgs['obj']['register_medicine(string,uint64,uint64)bool'] | MedicineLendingPlatformArgs['tuple']['register_medicine(string,uint64,uint64)bool']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'register_medicine(string,uint64,uint64)bool' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.name, params.args.expiry, params.args.quantity],
    }
  }
  /**
   * Constructs a no op call for the request_medicine(string)bool ABI method
   *
   * Records a request for a medicine if available and not expired.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static requestMedicine(params: CallParams<MedicineLendingPlatformArgs['obj']['request_medicine(string)bool'] | MedicineLendingPlatformArgs['tuple']['request_medicine(string)bool']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'request_medicine(string)bool' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.name],
    }
  }
  /**
   * Constructs a no op call for the approve_request(string)bool ABI method
   *
  * Approves a medicine request.
  Only the lender (registrant) may approve. On approval, decreases quantity and records the medicine record.

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static approveRequest(params: CallParams<MedicineLendingPlatformArgs['obj']['approve_request(string)bool'] | MedicineLendingPlatformArgs['tuple']['approve_request(string)bool']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'approve_request(string)bool' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.name],
    }
  }
  /**
   * Constructs a no op call for the return_medicine(string)bool ABI method
   *
   * Processes the return of a medicine by increasing its quantity.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static returnMedicine(params: CallParams<MedicineLendingPlatformArgs['obj']['return_medicine(string)bool'] | MedicineLendingPlatformArgs['tuple']['return_medicine(string)bool']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'return_medicine(string)bool' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.name],
    }
  }
  /**
   * Constructs a no op call for the is_expired(string)bool ABI method
   *
   * Checks if a medicine is expired (i.e. current round > expiry).
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static isExpired(params: CallParams<MedicineLendingPlatformArgs['obj']['is_expired(string)bool'] | MedicineLendingPlatformArgs['tuple']['is_expired(string)bool']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'is_expired(string)bool' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.name],
    }
  }
}

/**
 * A client to make calls to the MedicineLendingPlatform smart contract
 */
export class MedicineLendingPlatformClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `MedicineLendingPlatformClient`
   *
   * @param appClient An `AppClient` instance which has been created with the MedicineLendingPlatform app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `MedicineLendingPlatformClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Checks for decode errors on the given return value and maps the return value to the return type for the given method
   * @returns The typed return value or undefined if there was no value
   */
  decodeReturnValue<TSignature extends MedicineLendingPlatformNonVoidMethodSignatures>(method: TSignature, returnValue: ABIReturn | undefined) {
    return returnValue !== undefined ? getArc56ReturnValue<MethodReturn<TSignature>>(returnValue, this.appClient.getABIMethod(method), APP_SPEC.structs) : undefined
  }

  /**
   * Returns a new `MedicineLendingPlatformClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<MedicineLendingPlatformClient> {
    return new MedicineLendingPlatformClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `MedicineLendingPlatformClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<MedicineLendingPlatformClient> {
    return new MedicineLendingPlatformClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Makes a clear_state call to an existing instance of the MedicineLendingPlatform smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the MedicineLendingPlatform smart contract using the `register_medicine(string,uint64,uint64)bool` ABI method.
     *
    * Registers or updates a medicine.
    If the medicine already exists, increases its quantity. Sets the lender to the transaction sender.

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    registerMedicine: (params: CallParams<MedicineLendingPlatformArgs['obj']['register_medicine(string,uint64,uint64)bool'] | MedicineLendingPlatformArgs['tuple']['register_medicine(string,uint64,uint64)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(MedicineLendingPlatformParamsFactory.registerMedicine(params))
    },

    /**
     * Makes a call to the MedicineLendingPlatform smart contract using the `request_medicine(string)bool` ABI method.
     *
     * Records a request for a medicine if available and not expired.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    requestMedicine: (params: CallParams<MedicineLendingPlatformArgs['obj']['request_medicine(string)bool'] | MedicineLendingPlatformArgs['tuple']['request_medicine(string)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(MedicineLendingPlatformParamsFactory.requestMedicine(params))
    },

    /**
     * Makes a call to the MedicineLendingPlatform smart contract using the `approve_request(string)bool` ABI method.
     *
    * Approves a medicine request.
    Only the lender (registrant) may approve. On approval, decreases quantity and records the medicine record.

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    approveRequest: (params: CallParams<MedicineLendingPlatformArgs['obj']['approve_request(string)bool'] | MedicineLendingPlatformArgs['tuple']['approve_request(string)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(MedicineLendingPlatformParamsFactory.approveRequest(params))
    },

    /**
     * Makes a call to the MedicineLendingPlatform smart contract using the `return_medicine(string)bool` ABI method.
     *
     * Processes the return of a medicine by increasing its quantity.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    returnMedicine: (params: CallParams<MedicineLendingPlatformArgs['obj']['return_medicine(string)bool'] | MedicineLendingPlatformArgs['tuple']['return_medicine(string)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(MedicineLendingPlatformParamsFactory.returnMedicine(params))
    },

    /**
     * Makes a call to the MedicineLendingPlatform smart contract using the `is_expired(string)bool` ABI method.
     *
     * Checks if a medicine is expired (i.e. current round > expiry).
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    isExpired: (params: CallParams<MedicineLendingPlatformArgs['obj']['is_expired(string)bool'] | MedicineLendingPlatformArgs['tuple']['is_expired(string)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(MedicineLendingPlatformParamsFactory.isExpired(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Makes a clear_state call to an existing instance of the MedicineLendingPlatform smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the MedicineLendingPlatform smart contract using the `register_medicine(string,uint64,uint64)bool` ABI method.
     *
    * Registers or updates a medicine.
    If the medicine already exists, increases its quantity. Sets the lender to the transaction sender.

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    registerMedicine: (params: CallParams<MedicineLendingPlatformArgs['obj']['register_medicine(string,uint64,uint64)bool'] | MedicineLendingPlatformArgs['tuple']['register_medicine(string,uint64,uint64)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(MedicineLendingPlatformParamsFactory.registerMedicine(params))
    },

    /**
     * Makes a call to the MedicineLendingPlatform smart contract using the `request_medicine(string)bool` ABI method.
     *
     * Records a request for a medicine if available and not expired.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    requestMedicine: (params: CallParams<MedicineLendingPlatformArgs['obj']['request_medicine(string)bool'] | MedicineLendingPlatformArgs['tuple']['request_medicine(string)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(MedicineLendingPlatformParamsFactory.requestMedicine(params))
    },

    /**
     * Makes a call to the MedicineLendingPlatform smart contract using the `approve_request(string)bool` ABI method.
     *
    * Approves a medicine request.
    Only the lender (registrant) may approve. On approval, decreases quantity and records the medicine record.

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    approveRequest: (params: CallParams<MedicineLendingPlatformArgs['obj']['approve_request(string)bool'] | MedicineLendingPlatformArgs['tuple']['approve_request(string)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(MedicineLendingPlatformParamsFactory.approveRequest(params))
    },

    /**
     * Makes a call to the MedicineLendingPlatform smart contract using the `return_medicine(string)bool` ABI method.
     *
     * Processes the return of a medicine by increasing its quantity.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    returnMedicine: (params: CallParams<MedicineLendingPlatformArgs['obj']['return_medicine(string)bool'] | MedicineLendingPlatformArgs['tuple']['return_medicine(string)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(MedicineLendingPlatformParamsFactory.returnMedicine(params))
    },

    /**
     * Makes a call to the MedicineLendingPlatform smart contract using the `is_expired(string)bool` ABI method.
     *
     * Checks if a medicine is expired (i.e. current round > expiry).
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    isExpired: (params: CallParams<MedicineLendingPlatformArgs['obj']['is_expired(string)bool'] | MedicineLendingPlatformArgs['tuple']['is_expired(string)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(MedicineLendingPlatformParamsFactory.isExpired(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Makes a clear_state call to an existing instance of the MedicineLendingPlatform smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the MedicineLendingPlatform smart contract using the `register_medicine(string,uint64,uint64)bool` ABI method.
     *
    * Registers or updates a medicine.
    If the medicine already exists, increases its quantity. Sets the lender to the transaction sender.

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    registerMedicine: async (params: CallParams<MedicineLendingPlatformArgs['obj']['register_medicine(string,uint64,uint64)bool'] | MedicineLendingPlatformArgs['tuple']['register_medicine(string,uint64,uint64)bool']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(MedicineLendingPlatformParamsFactory.registerMedicine(params))
      return {...result, return: result.return as unknown as (undefined | MedicineLendingPlatformReturns['register_medicine(string,uint64,uint64)bool'])}
    },

    /**
     * Makes a call to the MedicineLendingPlatform smart contract using the `request_medicine(string)bool` ABI method.
     *
     * Records a request for a medicine if available and not expired.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    requestMedicine: async (params: CallParams<MedicineLendingPlatformArgs['obj']['request_medicine(string)bool'] | MedicineLendingPlatformArgs['tuple']['request_medicine(string)bool']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(MedicineLendingPlatformParamsFactory.requestMedicine(params))
      return {...result, return: result.return as unknown as (undefined | MedicineLendingPlatformReturns['request_medicine(string)bool'])}
    },

    /**
     * Makes a call to the MedicineLendingPlatform smart contract using the `approve_request(string)bool` ABI method.
     *
    * Approves a medicine request.
    Only the lender (registrant) may approve. On approval, decreases quantity and records the medicine record.

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    approveRequest: async (params: CallParams<MedicineLendingPlatformArgs['obj']['approve_request(string)bool'] | MedicineLendingPlatformArgs['tuple']['approve_request(string)bool']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(MedicineLendingPlatformParamsFactory.approveRequest(params))
      return {...result, return: result.return as unknown as (undefined | MedicineLendingPlatformReturns['approve_request(string)bool'])}
    },

    /**
     * Makes a call to the MedicineLendingPlatform smart contract using the `return_medicine(string)bool` ABI method.
     *
     * Processes the return of a medicine by increasing its quantity.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    returnMedicine: async (params: CallParams<MedicineLendingPlatformArgs['obj']['return_medicine(string)bool'] | MedicineLendingPlatformArgs['tuple']['return_medicine(string)bool']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(MedicineLendingPlatformParamsFactory.returnMedicine(params))
      return {...result, return: result.return as unknown as (undefined | MedicineLendingPlatformReturns['return_medicine(string)bool'])}
    },

    /**
     * Makes a call to the MedicineLendingPlatform smart contract using the `is_expired(string)bool` ABI method.
     *
     * Checks if a medicine is expired (i.e. current round > expiry).
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    isExpired: async (params: CallParams<MedicineLendingPlatformArgs['obj']['is_expired(string)bool'] | MedicineLendingPlatformArgs['tuple']['is_expired(string)bool']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(MedicineLendingPlatformParamsFactory.isExpired(params))
      return {...result, return: result.return as unknown as (undefined | MedicineLendingPlatformReturns['is_expired(string)bool'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new MedicineLendingPlatformClient(this.appClient.clone(params))
  }

  /**
   * Methods to access state for the current MedicineLendingPlatform app
   */
  state = {
    /**
     * Methods to access global state for the current MedicineLendingPlatform app
     */
    global: {
      /**
       * Get all current keyed values from global state
       */
      getAll: async (): Promise<Partial<Expand<GlobalKeysState>>> => {
        const result = await this.appClient.state.global.getAll()
        return {
          medicines: result.medicines,
          medicineRequests: result.medicine_requests,
          medicineRecords: result.medicine_records,
        }
      },
      /**
       * Get the current value of the medicines key in global state
       */
      medicines: async (): Promise<[string, bigint, bigint, string][] | undefined> => { return (await this.appClient.state.global.getValue("medicines")) as [string, bigint, bigint, string][] | undefined },
      /**
       * Get the current value of the medicine_requests key in global state
       */
      medicineRequests: async (): Promise<[string, string][] | undefined> => { return (await this.appClient.state.global.getValue("medicine_requests")) as [string, string][] | undefined },
      /**
       * Get the current value of the medicine_records key in global state
       */
      medicineRecords: async (): Promise<[string, string, bigint][] | undefined> => { return (await this.appClient.state.global.getValue("medicine_records")) as [string, string, bigint][] | undefined },
    },
  }

  public newGroup(): MedicineLendingPlatformComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a register_medicine(string,uint64,uint64)bool method call against the MedicineLendingPlatform contract
       */
      registerMedicine(params: CallParams<MedicineLendingPlatformArgs['obj']['register_medicine(string,uint64,uint64)bool'] | MedicineLendingPlatformArgs['tuple']['register_medicine(string,uint64,uint64)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.registerMedicine(params)))
        resultMappers.push((v) => client.decodeReturnValue('register_medicine(string,uint64,uint64)bool', v))
        return this
      },
      /**
       * Add a request_medicine(string)bool method call against the MedicineLendingPlatform contract
       */
      requestMedicine(params: CallParams<MedicineLendingPlatformArgs['obj']['request_medicine(string)bool'] | MedicineLendingPlatformArgs['tuple']['request_medicine(string)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.requestMedicine(params)))
        resultMappers.push((v) => client.decodeReturnValue('request_medicine(string)bool', v))
        return this
      },
      /**
       * Add a approve_request(string)bool method call against the MedicineLendingPlatform contract
       */
      approveRequest(params: CallParams<MedicineLendingPlatformArgs['obj']['approve_request(string)bool'] | MedicineLendingPlatformArgs['tuple']['approve_request(string)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.approveRequest(params)))
        resultMappers.push((v) => client.decodeReturnValue('approve_request(string)bool', v))
        return this
      },
      /**
       * Add a return_medicine(string)bool method call against the MedicineLendingPlatform contract
       */
      returnMedicine(params: CallParams<MedicineLendingPlatformArgs['obj']['return_medicine(string)bool'] | MedicineLendingPlatformArgs['tuple']['return_medicine(string)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.returnMedicine(params)))
        resultMappers.push((v) => client.decodeReturnValue('return_medicine(string)bool', v))
        return this
      },
      /**
       * Add a is_expired(string)bool method call against the MedicineLendingPlatform contract
       */
      isExpired(params: CallParams<MedicineLendingPlatformArgs['obj']['is_expired(string)bool'] | MedicineLendingPlatformArgs['tuple']['is_expired(string)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.isExpired(params)))
        resultMappers.push((v) => client.decodeReturnValue('is_expired(string)bool', v))
        return this
      },
      /**
       * Add a clear state call to the MedicineLendingPlatform contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as MedicineLendingPlatformComposer
  }
}
export type MedicineLendingPlatformComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the register_medicine(string,uint64,uint64)bool ABI method.
   *
  * Registers or updates a medicine.
  If the medicine already exists, increases its quantity. Sets the lender to the transaction sender.

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  registerMedicine(params?: CallParams<MedicineLendingPlatformArgs['obj']['register_medicine(string,uint64,uint64)bool'] | MedicineLendingPlatformArgs['tuple']['register_medicine(string,uint64,uint64)bool']>): MedicineLendingPlatformComposer<[...TReturns, MedicineLendingPlatformReturns['register_medicine(string,uint64,uint64)bool'] | undefined]>

  /**
   * Calls the request_medicine(string)bool ABI method.
   *
   * Records a request for a medicine if available and not expired.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  requestMedicine(params?: CallParams<MedicineLendingPlatformArgs['obj']['request_medicine(string)bool'] | MedicineLendingPlatformArgs['tuple']['request_medicine(string)bool']>): MedicineLendingPlatformComposer<[...TReturns, MedicineLendingPlatformReturns['request_medicine(string)bool'] | undefined]>

  /**
   * Calls the approve_request(string)bool ABI method.
   *
  * Approves a medicine request.
  Only the lender (registrant) may approve. On approval, decreases quantity and records the medicine record.

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  approveRequest(params?: CallParams<MedicineLendingPlatformArgs['obj']['approve_request(string)bool'] | MedicineLendingPlatformArgs['tuple']['approve_request(string)bool']>): MedicineLendingPlatformComposer<[...TReturns, MedicineLendingPlatformReturns['approve_request(string)bool'] | undefined]>

  /**
   * Calls the return_medicine(string)bool ABI method.
   *
   * Processes the return of a medicine by increasing its quantity.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  returnMedicine(params?: CallParams<MedicineLendingPlatformArgs['obj']['return_medicine(string)bool'] | MedicineLendingPlatformArgs['tuple']['return_medicine(string)bool']>): MedicineLendingPlatformComposer<[...TReturns, MedicineLendingPlatformReturns['return_medicine(string)bool'] | undefined]>

  /**
   * Calls the is_expired(string)bool ABI method.
   *
   * Checks if a medicine is expired (i.e. current round > expiry).
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  isExpired(params?: CallParams<MedicineLendingPlatformArgs['obj']['is_expired(string)bool'] | MedicineLendingPlatformArgs['tuple']['is_expired(string)bool']>): MedicineLendingPlatformComposer<[...TReturns, MedicineLendingPlatformReturns['is_expired(string)bool'] | undefined]>

  /**
   * Makes a clear_state call to an existing instance of the MedicineLendingPlatform smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): MedicineLendingPlatformComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): MedicineLendingPlatformComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<MedicineLendingPlatformComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<MedicineLendingPlatformComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<MedicineLendingPlatformComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<MedicineLendingPlatformComposerResults<TReturns>>
}
export type MedicineLendingPlatformComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

