/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"FactChecker","structs":{},"methods":[{"name":"submit_text_for_analysis","args":[{"type":"string","name":"text"}],"returns":{"type":"uint64"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Step 1: Submit text and get submission ID","events":[],"recommendations":{}},{"name":"extract_claims_count","args":[{"type":"string","name":"text"}],"returns":{"type":"uint64"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Step 1a: Count potential claims by counting sentences","events":[],"recommendations":{}},{"name":"get_text_hash","args":[{"type":"string","name":"text"}],"returns":{"type":"byte[]"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Generate SHA256 hash for text verification","events":[],"recommendations":{}},{"name":"get_total_submissions","args":[],"returns":{"type":"uint64"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Get total number of text submissions","events":[],"recommendations":{}},{"name":"get_total_claims","args":[],"returns":{"type":"uint64"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Get total claims extracted across all submissions","events":[],"recommendations":{}},{"name":"process_step1_complete","args":[{"type":"string","name":"text"}],"returns":{"type":"string"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Complete Step 1: Submit text and extract claims","events":[],"recommendations":{}},{"name":"reset_all_stats","args":[],"returns":{"type":"string"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Reset all counters for testing","events":[],"recommendations":{}},{"name":"get_contract_info","args":[],"returns":{"type":"string"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Get basic contract information","events":[],"recommendations":{}}],"arcs":[22,28],"networks":{},"state":{"schema":{"global":{"ints":2,"bytes":0},"local":{"ints":0,"bytes":0}},"keys":{"global":{"total_submissions":{"keyType":"AVMString","valueType":"AVMUint64","key":"dG90YWxfc3VibWlzc2lvbnM="},"total_claims_extracted":{"keyType":"AVMString","valueType":"AVMUint64","key":"dG90YWxfY2xhaW1zX2V4dHJhY3RlZA=="}},"local":{},"box":{}},"maps":{"global":{},"local":{},"box":{}}},"bareActions":{"create":["NoOp"],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[137,198,222,252,269,286,316,339],"errorMessage":"OnCompletion is not NoOp"},{"pc":[367],"errorMessage":"can only call when creating"},{"pc":[140,201,225,255,272,289,319,342],"errorMessage":"can only call when not creating"},{"pc":[436,464],"errorMessage":"check self.total_claims_extracted exists"},{"pc":[376,459],"errorMessage":"check self.total_submissions exists"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"events":[]} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


/**
 * The argument types for the FactChecker contract
 */
export type FactCheckerArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'submit_text_for_analysis(string)uint64': {
      text: string
    }
    'extract_claims_count(string)uint64': {
      text: string
    }
    'get_text_hash(string)byte[]': {
      text: string
    }
    'get_total_submissions()uint64': Record<string, never>
    'get_total_claims()uint64': Record<string, never>
    'process_step1_complete(string)string': {
      text: string
    }
    'reset_all_stats()string': Record<string, never>
    'get_contract_info()string': Record<string, never>
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'submit_text_for_analysis(string)uint64': [text: string]
    'extract_claims_count(string)uint64': [text: string]
    'get_text_hash(string)byte[]': [text: string]
    'get_total_submissions()uint64': []
    'get_total_claims()uint64': []
    'process_step1_complete(string)string': [text: string]
    'reset_all_stats()string': []
    'get_contract_info()string': []
  }
}

/**
 * The return type for each method
 */
export type FactCheckerReturns = {
  'submit_text_for_analysis(string)uint64': bigint
  'extract_claims_count(string)uint64': bigint
  'get_text_hash(string)byte[]': Uint8Array
  'get_total_submissions()uint64': bigint
  'get_total_claims()uint64': bigint
  'process_step1_complete(string)string': string
  'reset_all_stats()string': string
  'get_contract_info()string': string
}

/**
 * Defines the types of available calls and state of the FactChecker smart contract.
 */
export type FactCheckerTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'submit_text_for_analysis(string)uint64' | 'submit_text_for_analysis', {
      argsObj: FactCheckerArgs['obj']['submit_text_for_analysis(string)uint64']
      argsTuple: FactCheckerArgs['tuple']['submit_text_for_analysis(string)uint64']
      returns: FactCheckerReturns['submit_text_for_analysis(string)uint64']
    }>
    & Record<'extract_claims_count(string)uint64' | 'extract_claims_count', {
      argsObj: FactCheckerArgs['obj']['extract_claims_count(string)uint64']
      argsTuple: FactCheckerArgs['tuple']['extract_claims_count(string)uint64']
      returns: FactCheckerReturns['extract_claims_count(string)uint64']
    }>
    & Record<'get_text_hash(string)byte[]' | 'get_text_hash', {
      argsObj: FactCheckerArgs['obj']['get_text_hash(string)byte[]']
      argsTuple: FactCheckerArgs['tuple']['get_text_hash(string)byte[]']
      returns: FactCheckerReturns['get_text_hash(string)byte[]']
    }>
    & Record<'get_total_submissions()uint64' | 'get_total_submissions', {
      argsObj: FactCheckerArgs['obj']['get_total_submissions()uint64']
      argsTuple: FactCheckerArgs['tuple']['get_total_submissions()uint64']
      returns: FactCheckerReturns['get_total_submissions()uint64']
    }>
    & Record<'get_total_claims()uint64' | 'get_total_claims', {
      argsObj: FactCheckerArgs['obj']['get_total_claims()uint64']
      argsTuple: FactCheckerArgs['tuple']['get_total_claims()uint64']
      returns: FactCheckerReturns['get_total_claims()uint64']
    }>
    & Record<'process_step1_complete(string)string' | 'process_step1_complete', {
      argsObj: FactCheckerArgs['obj']['process_step1_complete(string)string']
      argsTuple: FactCheckerArgs['tuple']['process_step1_complete(string)string']
      returns: FactCheckerReturns['process_step1_complete(string)string']
    }>
    & Record<'reset_all_stats()string' | 'reset_all_stats', {
      argsObj: FactCheckerArgs['obj']['reset_all_stats()string']
      argsTuple: FactCheckerArgs['tuple']['reset_all_stats()string']
      returns: FactCheckerReturns['reset_all_stats()string']
    }>
    & Record<'get_contract_info()string' | 'get_contract_info', {
      argsObj: FactCheckerArgs['obj']['get_contract_info()string']
      argsTuple: FactCheckerArgs['tuple']['get_contract_info()string']
      returns: FactCheckerReturns['get_contract_info()string']
    }>
  /**
   * Defines the shape of the state of the application.
   */
  state: {
    global: {
      keys: {
        totalSubmissions: bigint
        totalClaimsExtracted: bigint
      }
      maps: {}
    }
  }
}

/**
 * Defines the possible abi call signatures.
 */
export type FactCheckerSignatures = keyof FactCheckerTypes['methods']
/**
 * Defines the possible abi call signatures for methods that return a non-void value.
 */
export type FactCheckerNonVoidMethodSignatures = keyof FactCheckerTypes['methods'] extends infer T ? T extends keyof FactCheckerTypes['methods'] ? MethodReturn<T> extends void ? never : T  : never : never
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the FactChecker smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends FactCheckerSignatures> = FactCheckerTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the FactChecker smart contract to the method's return type
 */
export type MethodReturn<TSignature extends FactCheckerSignatures> = FactCheckerTypes['methods'][TSignature]['returns']

/**
 * Defines the shape of the keyed global state of the application.
 */
export type GlobalKeysState = FactCheckerTypes['state']['global']['keys']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the FactChecker smart contract
 */
export abstract class FactCheckerParamsFactory {
  /**
   * Constructs a no op call for the submit_text_for_analysis(string)uint64 ABI method
   *
   * Step 1: Submit text and get submission ID
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static submitTextForAnalysis(params: CallParams<FactCheckerArgs['obj']['submit_text_for_analysis(string)uint64'] | FactCheckerArgs['tuple']['submit_text_for_analysis(string)uint64']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'submit_text_for_analysis(string)uint64' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.text],
    }
  }
  /**
   * Constructs a no op call for the extract_claims_count(string)uint64 ABI method
   *
   * Step 1a: Count potential claims by counting sentences
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static extractClaimsCount(params: CallParams<FactCheckerArgs['obj']['extract_claims_count(string)uint64'] | FactCheckerArgs['tuple']['extract_claims_count(string)uint64']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'extract_claims_count(string)uint64' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.text],
    }
  }
  /**
   * Constructs a no op call for the get_text_hash(string)byte[] ABI method
   *
   * Generate SHA256 hash for text verification
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static getTextHash(params: CallParams<FactCheckerArgs['obj']['get_text_hash(string)byte[]'] | FactCheckerArgs['tuple']['get_text_hash(string)byte[]']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'get_text_hash(string)byte[]' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.text],
    }
  }
  /**
   * Constructs a no op call for the get_total_submissions()uint64 ABI method
   *
   * Get total number of text submissions
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static getTotalSubmissions(params: CallParams<FactCheckerArgs['obj']['get_total_submissions()uint64'] | FactCheckerArgs['tuple']['get_total_submissions()uint64']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'get_total_submissions()uint64' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the get_total_claims()uint64 ABI method
   *
   * Get total claims extracted across all submissions
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static getTotalClaims(params: CallParams<FactCheckerArgs['obj']['get_total_claims()uint64'] | FactCheckerArgs['tuple']['get_total_claims()uint64']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'get_total_claims()uint64' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the process_step1_complete(string)string ABI method
   *
   * Complete Step 1: Submit text and extract claims
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static processStep1Complete(params: CallParams<FactCheckerArgs['obj']['process_step1_complete(string)string'] | FactCheckerArgs['tuple']['process_step1_complete(string)string']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'process_step1_complete(string)string' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.text],
    }
  }
  /**
   * Constructs a no op call for the reset_all_stats()string ABI method
   *
   * Reset all counters for testing
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static resetAllStats(params: CallParams<FactCheckerArgs['obj']['reset_all_stats()string'] | FactCheckerArgs['tuple']['reset_all_stats()string']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'reset_all_stats()string' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the get_contract_info()string ABI method
   *
   * Get basic contract information
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static getContractInfo(params: CallParams<FactCheckerArgs['obj']['get_contract_info()string'] | FactCheckerArgs['tuple']['get_contract_info()string']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'get_contract_info()string' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
}

/**
 * A client to make calls to the FactChecker smart contract
 */
export class FactCheckerClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `FactCheckerClient`
   *
   * @param appClient An `AppClient` instance which has been created with the FactChecker app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `FactCheckerClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Checks for decode errors on the given return value and maps the return value to the return type for the given method
   * @returns The typed return value or undefined if there was no value
   */
  decodeReturnValue<TSignature extends FactCheckerNonVoidMethodSignatures>(method: TSignature, returnValue: ABIReturn | undefined) {
    return returnValue !== undefined ? getArc56ReturnValue<MethodReturn<TSignature>>(returnValue, this.appClient.getABIMethod(method), APP_SPEC.structs) : undefined
  }

  /**
   * Returns a new `FactCheckerClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<FactCheckerClient> {
    return new FactCheckerClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `FactCheckerClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<FactCheckerClient> {
    return new FactCheckerClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Makes a clear_state call to an existing instance of the FactChecker smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the FactChecker smart contract using the `submit_text_for_analysis(string)uint64` ABI method.
     *
     * Step 1: Submit text and get submission ID
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    submitTextForAnalysis: (params: CallParams<FactCheckerArgs['obj']['submit_text_for_analysis(string)uint64'] | FactCheckerArgs['tuple']['submit_text_for_analysis(string)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(FactCheckerParamsFactory.submitTextForAnalysis(params))
    },

    /**
     * Makes a call to the FactChecker smart contract using the `extract_claims_count(string)uint64` ABI method.
     *
     * Step 1a: Count potential claims by counting sentences
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    extractClaimsCount: (params: CallParams<FactCheckerArgs['obj']['extract_claims_count(string)uint64'] | FactCheckerArgs['tuple']['extract_claims_count(string)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(FactCheckerParamsFactory.extractClaimsCount(params))
    },

    /**
     * Makes a call to the FactChecker smart contract using the `get_text_hash(string)byte[]` ABI method.
     *
     * Generate SHA256 hash for text verification
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    getTextHash: (params: CallParams<FactCheckerArgs['obj']['get_text_hash(string)byte[]'] | FactCheckerArgs['tuple']['get_text_hash(string)byte[]']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(FactCheckerParamsFactory.getTextHash(params))
    },

    /**
     * Makes a call to the FactChecker smart contract using the `get_total_submissions()uint64` ABI method.
     *
     * Get total number of text submissions
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    getTotalSubmissions: (params: CallParams<FactCheckerArgs['obj']['get_total_submissions()uint64'] | FactCheckerArgs['tuple']['get_total_submissions()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(FactCheckerParamsFactory.getTotalSubmissions(params))
    },

    /**
     * Makes a call to the FactChecker smart contract using the `get_total_claims()uint64` ABI method.
     *
     * Get total claims extracted across all submissions
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    getTotalClaims: (params: CallParams<FactCheckerArgs['obj']['get_total_claims()uint64'] | FactCheckerArgs['tuple']['get_total_claims()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(FactCheckerParamsFactory.getTotalClaims(params))
    },

    /**
     * Makes a call to the FactChecker smart contract using the `process_step1_complete(string)string` ABI method.
     *
     * Complete Step 1: Submit text and extract claims
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    processStep1Complete: (params: CallParams<FactCheckerArgs['obj']['process_step1_complete(string)string'] | FactCheckerArgs['tuple']['process_step1_complete(string)string']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(FactCheckerParamsFactory.processStep1Complete(params))
    },

    /**
     * Makes a call to the FactChecker smart contract using the `reset_all_stats()string` ABI method.
     *
     * Reset all counters for testing
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    resetAllStats: (params: CallParams<FactCheckerArgs['obj']['reset_all_stats()string'] | FactCheckerArgs['tuple']['reset_all_stats()string']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(FactCheckerParamsFactory.resetAllStats(params))
    },

    /**
     * Makes a call to the FactChecker smart contract using the `get_contract_info()string` ABI method.
     *
     * Get basic contract information
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    getContractInfo: (params: CallParams<FactCheckerArgs['obj']['get_contract_info()string'] | FactCheckerArgs['tuple']['get_contract_info()string']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(FactCheckerParamsFactory.getContractInfo(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Makes a clear_state call to an existing instance of the FactChecker smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the FactChecker smart contract using the `submit_text_for_analysis(string)uint64` ABI method.
     *
     * Step 1: Submit text and get submission ID
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    submitTextForAnalysis: (params: CallParams<FactCheckerArgs['obj']['submit_text_for_analysis(string)uint64'] | FactCheckerArgs['tuple']['submit_text_for_analysis(string)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(FactCheckerParamsFactory.submitTextForAnalysis(params))
    },

    /**
     * Makes a call to the FactChecker smart contract using the `extract_claims_count(string)uint64` ABI method.
     *
     * Step 1a: Count potential claims by counting sentences
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    extractClaimsCount: (params: CallParams<FactCheckerArgs['obj']['extract_claims_count(string)uint64'] | FactCheckerArgs['tuple']['extract_claims_count(string)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(FactCheckerParamsFactory.extractClaimsCount(params))
    },

    /**
     * Makes a call to the FactChecker smart contract using the `get_text_hash(string)byte[]` ABI method.
     *
     * Generate SHA256 hash for text verification
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    getTextHash: (params: CallParams<FactCheckerArgs['obj']['get_text_hash(string)byte[]'] | FactCheckerArgs['tuple']['get_text_hash(string)byte[]']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(FactCheckerParamsFactory.getTextHash(params))
    },

    /**
     * Makes a call to the FactChecker smart contract using the `get_total_submissions()uint64` ABI method.
     *
     * Get total number of text submissions
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    getTotalSubmissions: (params: CallParams<FactCheckerArgs['obj']['get_total_submissions()uint64'] | FactCheckerArgs['tuple']['get_total_submissions()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(FactCheckerParamsFactory.getTotalSubmissions(params))
    },

    /**
     * Makes a call to the FactChecker smart contract using the `get_total_claims()uint64` ABI method.
     *
     * Get total claims extracted across all submissions
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    getTotalClaims: (params: CallParams<FactCheckerArgs['obj']['get_total_claims()uint64'] | FactCheckerArgs['tuple']['get_total_claims()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(FactCheckerParamsFactory.getTotalClaims(params))
    },

    /**
     * Makes a call to the FactChecker smart contract using the `process_step1_complete(string)string` ABI method.
     *
     * Complete Step 1: Submit text and extract claims
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    processStep1Complete: (params: CallParams<FactCheckerArgs['obj']['process_step1_complete(string)string'] | FactCheckerArgs['tuple']['process_step1_complete(string)string']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(FactCheckerParamsFactory.processStep1Complete(params))
    },

    /**
     * Makes a call to the FactChecker smart contract using the `reset_all_stats()string` ABI method.
     *
     * Reset all counters for testing
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    resetAllStats: (params: CallParams<FactCheckerArgs['obj']['reset_all_stats()string'] | FactCheckerArgs['tuple']['reset_all_stats()string']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(FactCheckerParamsFactory.resetAllStats(params))
    },

    /**
     * Makes a call to the FactChecker smart contract using the `get_contract_info()string` ABI method.
     *
     * Get basic contract information
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    getContractInfo: (params: CallParams<FactCheckerArgs['obj']['get_contract_info()string'] | FactCheckerArgs['tuple']['get_contract_info()string']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(FactCheckerParamsFactory.getContractInfo(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Makes a clear_state call to an existing instance of the FactChecker smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the FactChecker smart contract using the `submit_text_for_analysis(string)uint64` ABI method.
     *
     * Step 1: Submit text and get submission ID
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    submitTextForAnalysis: async (params: CallParams<FactCheckerArgs['obj']['submit_text_for_analysis(string)uint64'] | FactCheckerArgs['tuple']['submit_text_for_analysis(string)uint64']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(FactCheckerParamsFactory.submitTextForAnalysis(params))
      return {...result, return: result.return as unknown as (undefined | FactCheckerReturns['submit_text_for_analysis(string)uint64'])}
    },

    /**
     * Makes a call to the FactChecker smart contract using the `extract_claims_count(string)uint64` ABI method.
     *
     * Step 1a: Count potential claims by counting sentences
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    extractClaimsCount: async (params: CallParams<FactCheckerArgs['obj']['extract_claims_count(string)uint64'] | FactCheckerArgs['tuple']['extract_claims_count(string)uint64']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(FactCheckerParamsFactory.extractClaimsCount(params))
      return {...result, return: result.return as unknown as (undefined | FactCheckerReturns['extract_claims_count(string)uint64'])}
    },

    /**
     * Makes a call to the FactChecker smart contract using the `get_text_hash(string)byte[]` ABI method.
     *
     * Generate SHA256 hash for text verification
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    getTextHash: async (params: CallParams<FactCheckerArgs['obj']['get_text_hash(string)byte[]'] | FactCheckerArgs['tuple']['get_text_hash(string)byte[]']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(FactCheckerParamsFactory.getTextHash(params))
      return {...result, return: result.return as unknown as (undefined | FactCheckerReturns['get_text_hash(string)byte[]'])}
    },

    /**
     * Makes a call to the FactChecker smart contract using the `get_total_submissions()uint64` ABI method.
     *
     * Get total number of text submissions
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    getTotalSubmissions: async (params: CallParams<FactCheckerArgs['obj']['get_total_submissions()uint64'] | FactCheckerArgs['tuple']['get_total_submissions()uint64']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(FactCheckerParamsFactory.getTotalSubmissions(params))
      return {...result, return: result.return as unknown as (undefined | FactCheckerReturns['get_total_submissions()uint64'])}
    },

    /**
     * Makes a call to the FactChecker smart contract using the `get_total_claims()uint64` ABI method.
     *
     * Get total claims extracted across all submissions
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    getTotalClaims: async (params: CallParams<FactCheckerArgs['obj']['get_total_claims()uint64'] | FactCheckerArgs['tuple']['get_total_claims()uint64']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(FactCheckerParamsFactory.getTotalClaims(params))
      return {...result, return: result.return as unknown as (undefined | FactCheckerReturns['get_total_claims()uint64'])}
    },

    /**
     * Makes a call to the FactChecker smart contract using the `process_step1_complete(string)string` ABI method.
     *
     * Complete Step 1: Submit text and extract claims
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    processStep1Complete: async (params: CallParams<FactCheckerArgs['obj']['process_step1_complete(string)string'] | FactCheckerArgs['tuple']['process_step1_complete(string)string']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(FactCheckerParamsFactory.processStep1Complete(params))
      return {...result, return: result.return as unknown as (undefined | FactCheckerReturns['process_step1_complete(string)string'])}
    },

    /**
     * Makes a call to the FactChecker smart contract using the `reset_all_stats()string` ABI method.
     *
     * Reset all counters for testing
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    resetAllStats: async (params: CallParams<FactCheckerArgs['obj']['reset_all_stats()string'] | FactCheckerArgs['tuple']['reset_all_stats()string']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(FactCheckerParamsFactory.resetAllStats(params))
      return {...result, return: result.return as unknown as (undefined | FactCheckerReturns['reset_all_stats()string'])}
    },

    /**
     * Makes a call to the FactChecker smart contract using the `get_contract_info()string` ABI method.
     *
     * Get basic contract information
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    getContractInfo: async (params: CallParams<FactCheckerArgs['obj']['get_contract_info()string'] | FactCheckerArgs['tuple']['get_contract_info()string']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(FactCheckerParamsFactory.getContractInfo(params))
      return {...result, return: result.return as unknown as (undefined | FactCheckerReturns['get_contract_info()string'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new FactCheckerClient(this.appClient.clone(params))
  }

  /**
   * Methods to access state for the current FactChecker app
   */
  state = {
    /**
     * Methods to access global state for the current FactChecker app
     */
    global: {
      /**
       * Get all current keyed values from global state
       */
      getAll: async (): Promise<Partial<Expand<GlobalKeysState>>> => {
        const result = await this.appClient.state.global.getAll()
        return {
          totalSubmissions: result.total_submissions,
          totalClaimsExtracted: result.total_claims_extracted,
        }
      },
      /**
       * Get the current value of the total_submissions key in global state
       */
      totalSubmissions: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("total_submissions")) as bigint | undefined },
      /**
       * Get the current value of the total_claims_extracted key in global state
       */
      totalClaimsExtracted: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("total_claims_extracted")) as bigint | undefined },
    },
  }

  public newGroup(): FactCheckerComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a submit_text_for_analysis(string)uint64 method call against the FactChecker contract
       */
      submitTextForAnalysis(params: CallParams<FactCheckerArgs['obj']['submit_text_for_analysis(string)uint64'] | FactCheckerArgs['tuple']['submit_text_for_analysis(string)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.submitTextForAnalysis(params)))
        resultMappers.push((v) => client.decodeReturnValue('submit_text_for_analysis(string)uint64', v))
        return this
      },
      /**
       * Add a extract_claims_count(string)uint64 method call against the FactChecker contract
       */
      extractClaimsCount(params: CallParams<FactCheckerArgs['obj']['extract_claims_count(string)uint64'] | FactCheckerArgs['tuple']['extract_claims_count(string)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.extractClaimsCount(params)))
        resultMappers.push((v) => client.decodeReturnValue('extract_claims_count(string)uint64', v))
        return this
      },
      /**
       * Add a get_text_hash(string)byte[] method call against the FactChecker contract
       */
      getTextHash(params: CallParams<FactCheckerArgs['obj']['get_text_hash(string)byte[]'] | FactCheckerArgs['tuple']['get_text_hash(string)byte[]']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.getTextHash(params)))
        resultMappers.push((v) => client.decodeReturnValue('get_text_hash(string)byte[]', v))
        return this
      },
      /**
       * Add a get_total_submissions()uint64 method call against the FactChecker contract
       */
      getTotalSubmissions(params: CallParams<FactCheckerArgs['obj']['get_total_submissions()uint64'] | FactCheckerArgs['tuple']['get_total_submissions()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.getTotalSubmissions(params)))
        resultMappers.push((v) => client.decodeReturnValue('get_total_submissions()uint64', v))
        return this
      },
      /**
       * Add a get_total_claims()uint64 method call against the FactChecker contract
       */
      getTotalClaims(params: CallParams<FactCheckerArgs['obj']['get_total_claims()uint64'] | FactCheckerArgs['tuple']['get_total_claims()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.getTotalClaims(params)))
        resultMappers.push((v) => client.decodeReturnValue('get_total_claims()uint64', v))
        return this
      },
      /**
       * Add a process_step1_complete(string)string method call against the FactChecker contract
       */
      processStep1Complete(params: CallParams<FactCheckerArgs['obj']['process_step1_complete(string)string'] | FactCheckerArgs['tuple']['process_step1_complete(string)string']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.processStep1Complete(params)))
        resultMappers.push((v) => client.decodeReturnValue('process_step1_complete(string)string', v))
        return this
      },
      /**
       * Add a reset_all_stats()string method call against the FactChecker contract
       */
      resetAllStats(params: CallParams<FactCheckerArgs['obj']['reset_all_stats()string'] | FactCheckerArgs['tuple']['reset_all_stats()string']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.resetAllStats(params)))
        resultMappers.push((v) => client.decodeReturnValue('reset_all_stats()string', v))
        return this
      },
      /**
       * Add a get_contract_info()string method call against the FactChecker contract
       */
      getContractInfo(params: CallParams<FactCheckerArgs['obj']['get_contract_info()string'] | FactCheckerArgs['tuple']['get_contract_info()string']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.getContractInfo(params)))
        resultMappers.push((v) => client.decodeReturnValue('get_contract_info()string', v))
        return this
      },
      /**
       * Add a clear state call to the FactChecker contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as FactCheckerComposer
  }
}
export type FactCheckerComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the submit_text_for_analysis(string)uint64 ABI method.
   *
   * Step 1: Submit text and get submission ID
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  submitTextForAnalysis(params?: CallParams<FactCheckerArgs['obj']['submit_text_for_analysis(string)uint64'] | FactCheckerArgs['tuple']['submit_text_for_analysis(string)uint64']>): FactCheckerComposer<[...TReturns, FactCheckerReturns['submit_text_for_analysis(string)uint64'] | undefined]>

  /**
   * Calls the extract_claims_count(string)uint64 ABI method.
   *
   * Step 1a: Count potential claims by counting sentences
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  extractClaimsCount(params?: CallParams<FactCheckerArgs['obj']['extract_claims_count(string)uint64'] | FactCheckerArgs['tuple']['extract_claims_count(string)uint64']>): FactCheckerComposer<[...TReturns, FactCheckerReturns['extract_claims_count(string)uint64'] | undefined]>

  /**
   * Calls the get_text_hash(string)byte[] ABI method.
   *
   * Generate SHA256 hash for text verification
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  getTextHash(params?: CallParams<FactCheckerArgs['obj']['get_text_hash(string)byte[]'] | FactCheckerArgs['tuple']['get_text_hash(string)byte[]']>): FactCheckerComposer<[...TReturns, FactCheckerReturns['get_text_hash(string)byte[]'] | undefined]>

  /**
   * Calls the get_total_submissions()uint64 ABI method.
   *
   * Get total number of text submissions
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  getTotalSubmissions(params?: CallParams<FactCheckerArgs['obj']['get_total_submissions()uint64'] | FactCheckerArgs['tuple']['get_total_submissions()uint64']>): FactCheckerComposer<[...TReturns, FactCheckerReturns['get_total_submissions()uint64'] | undefined]>

  /**
   * Calls the get_total_claims()uint64 ABI method.
   *
   * Get total claims extracted across all submissions
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  getTotalClaims(params?: CallParams<FactCheckerArgs['obj']['get_total_claims()uint64'] | FactCheckerArgs['tuple']['get_total_claims()uint64']>): FactCheckerComposer<[...TReturns, FactCheckerReturns['get_total_claims()uint64'] | undefined]>

  /**
   * Calls the process_step1_complete(string)string ABI method.
   *
   * Complete Step 1: Submit text and extract claims
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  processStep1Complete(params?: CallParams<FactCheckerArgs['obj']['process_step1_complete(string)string'] | FactCheckerArgs['tuple']['process_step1_complete(string)string']>): FactCheckerComposer<[...TReturns, FactCheckerReturns['process_step1_complete(string)string'] | undefined]>

  /**
   * Calls the reset_all_stats()string ABI method.
   *
   * Reset all counters for testing
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  resetAllStats(params?: CallParams<FactCheckerArgs['obj']['reset_all_stats()string'] | FactCheckerArgs['tuple']['reset_all_stats()string']>): FactCheckerComposer<[...TReturns, FactCheckerReturns['reset_all_stats()string'] | undefined]>

  /**
   * Calls the get_contract_info()string ABI method.
   *
   * Get basic contract information
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  getContractInfo(params?: CallParams<FactCheckerArgs['obj']['get_contract_info()string'] | FactCheckerArgs['tuple']['get_contract_info()string']>): FactCheckerComposer<[...TReturns, FactCheckerReturns['get_contract_info()string'] | undefined]>

  /**
   * Makes a clear_state call to an existing instance of the FactChecker smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): FactCheckerComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): FactCheckerComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<FactCheckerComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<FactCheckerComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<FactCheckerComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<FactCheckerComposerResults<TReturns>>
}
export type FactCheckerComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

