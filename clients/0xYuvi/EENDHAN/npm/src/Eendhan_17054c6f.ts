/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"Eendhan","structs":{},"methods":[{"name":"create","args":[{"type":"address","name":"treasury"},{"type":"address","name":"creator"}],"returns":{"type":"void"},"actions":{"create":["NoOp"],"call":[]},"readonly":false,"desc":"Initialize contract with treasury and creator addresses.","events":[],"recommendations":{}},{"name":"opt_in_to_usdc","args":[],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Opt-in the application account to USDC.\nStrictly gated to creator address only.","events":[],"recommendations":{}},{"name":"process_payment_split","args":[{"type":"axfer","name":"payment"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Verify incoming USDC payment and split: 5% treasury, 95% creator.\nPayment must be sent to this contract's address in the same group.","events":[],"recommendations":{}},{"name":"update_treasury","args":[{"type":"address","name":"new_treasury"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Update the treasury address. Only creator can call.","events":[],"recommendations":{}},{"name":"get_treasury","args":[],"returns":{"type":"address"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Return the current treasury address.","events":[],"recommendations":{}},{"name":"get_creator","args":[],"returns":{"type":"address"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Return the current creator address.","events":[],"recommendations":{}}],"arcs":[22,28],"desc":"\n    AlgoGate AI - x402-powered AI API Gateway Contract.\n    Handles USDC payment verification and payout splits.\n    ","networks":{},"state":{"schema":{"global":{"ints":1,"bytes":2},"local":{"ints":0,"bytes":0}},"keys":{"global":{"treasury_address":{"keyType":"AVMString","valueType":"address","key":"dHJlYXN1cnlfYWRkcmVzcw=="},"creator_address":{"keyType":"AVMString","valueType":"address","key":"Y3JlYXRvcl9hZGRyZXNz"},"usdc_asset":{"keyType":"AVMString","valueType":"AVMUint64","key":"dXNkY19hc3NldA=="}},"local":{},"box":{}},"maps":{"global":{},"local":{},"box":{}}},"bareActions":{"create":[],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[188],"errorMessage":"Creator not set"},{"pc":[228],"errorMessage":"Only USDC accepted"},{"pc":[183],"errorMessage":"Only creator can opt in to assets"},{"pc":[328],"errorMessage":"Only creator can update treasury"},{"pc":[239],"errorMessage":"Payment amount must be positive"},{"pc":[235],"errorMessage":"Payment must be sent to contract"},{"pc":[178,295,326,347],"errorMessage":"check self.creator_address exists"},{"pc":[267,337],"errorMessage":"check self.treasury_address exists"},{"pc":[193,263,291],"errorMessage":"check self.usdc_asset exists"},{"pc":[151,159,320],"errorMessage":"invalid number of bytes for arc4.static_array<arc4.uint8, 32>"},{"pc":[221],"errorMessage":"transaction type is axfer"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"events":[]} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


/**
 * The argument types for the Eendhan contract
 */
export type EendhanArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'create(address,address)void': {
      treasury: string
      creator: string
    }
    'opt_in_to_usdc()void': Record<string, never>
    'process_payment_split(axfer)void': {
      payment: AppMethodCallTransactionArgument
    }
    'update_treasury(address)void': {
      newTreasury: string
    }
    'get_treasury()address': Record<string, never>
    'get_creator()address': Record<string, never>
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'create(address,address)void': [treasury: string, creator: string]
    'opt_in_to_usdc()void': []
    'process_payment_split(axfer)void': [payment: AppMethodCallTransactionArgument]
    'update_treasury(address)void': [newTreasury: string]
    'get_treasury()address': []
    'get_creator()address': []
  }
}

/**
 * The return type for each method
 */
export type EendhanReturns = {
  'create(address,address)void': void
  'opt_in_to_usdc()void': void
  'process_payment_split(axfer)void': void
  'update_treasury(address)void': void
  'get_treasury()address': string
  'get_creator()address': string
}

/**
 * Defines the types of available calls and state of the Eendhan smart contract.
 */
export type EendhanTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'create(address,address)void' | 'create', {
      argsObj: EendhanArgs['obj']['create(address,address)void']
      argsTuple: EendhanArgs['tuple']['create(address,address)void']
      returns: EendhanReturns['create(address,address)void']
    }>
    & Record<'opt_in_to_usdc()void' | 'opt_in_to_usdc', {
      argsObj: EendhanArgs['obj']['opt_in_to_usdc()void']
      argsTuple: EendhanArgs['tuple']['opt_in_to_usdc()void']
      returns: EendhanReturns['opt_in_to_usdc()void']
    }>
    & Record<'process_payment_split(axfer)void' | 'process_payment_split', {
      argsObj: EendhanArgs['obj']['process_payment_split(axfer)void']
      argsTuple: EendhanArgs['tuple']['process_payment_split(axfer)void']
      returns: EendhanReturns['process_payment_split(axfer)void']
    }>
    & Record<'update_treasury(address)void' | 'update_treasury', {
      argsObj: EendhanArgs['obj']['update_treasury(address)void']
      argsTuple: EendhanArgs['tuple']['update_treasury(address)void']
      returns: EendhanReturns['update_treasury(address)void']
    }>
    & Record<'get_treasury()address' | 'get_treasury', {
      argsObj: EendhanArgs['obj']['get_treasury()address']
      argsTuple: EendhanArgs['tuple']['get_treasury()address']
      returns: EendhanReturns['get_treasury()address']
    }>
    & Record<'get_creator()address' | 'get_creator', {
      argsObj: EendhanArgs['obj']['get_creator()address']
      argsTuple: EendhanArgs['tuple']['get_creator()address']
      returns: EendhanReturns['get_creator()address']
    }>
  /**
   * Defines the shape of the state of the application.
   */
  state: {
    global: {
      keys: {
        treasuryAddress: string
        creatorAddress: string
        usdcAsset: bigint
      }
      maps: {}
    }
  }
}

/**
 * Defines the possible abi call signatures.
 */
export type EendhanSignatures = keyof EendhanTypes['methods']
/**
 * Defines the possible abi call signatures for methods that return a non-void value.
 */
export type EendhanNonVoidMethodSignatures = keyof EendhanTypes['methods'] extends infer T ? T extends keyof EendhanTypes['methods'] ? MethodReturn<T> extends void ? never : T  : never : never
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the Eendhan smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends EendhanSignatures> = EendhanTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the Eendhan smart contract to the method's return type
 */
export type MethodReturn<TSignature extends EendhanSignatures> = EendhanTypes['methods'][TSignature]['returns']

/**
 * Defines the shape of the keyed global state of the application.
 */
export type GlobalKeysState = EendhanTypes['state']['global']['keys']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the Eendhan smart contract
 */
export abstract class EendhanParamsFactory {
  /**
   * Constructs a no op call for the opt_in_to_usdc()void ABI method
   *
  * Opt-in the application account to USDC.
  Strictly gated to creator address only.

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static optInToUsdc(params: CallParams<EendhanArgs['obj']['opt_in_to_usdc()void'] | EendhanArgs['tuple']['opt_in_to_usdc()void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'opt_in_to_usdc()void' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the process_payment_split(axfer)void ABI method
   *
  * Verify incoming USDC payment and split: 5% treasury, 95% creator.
  Payment must be sent to this contract's address in the same group.

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static processPaymentSplit(params: CallParams<EendhanArgs['obj']['process_payment_split(axfer)void'] | EendhanArgs['tuple']['process_payment_split(axfer)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'process_payment_split(axfer)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.payment],
    }
  }
  /**
   * Constructs a no op call for the update_treasury(address)void ABI method
   *
   * Update the treasury address. Only creator can call.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static updateTreasury(params: CallParams<EendhanArgs['obj']['update_treasury(address)void'] | EendhanArgs['tuple']['update_treasury(address)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'update_treasury(address)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.newTreasury],
    }
  }
  /**
   * Constructs a no op call for the get_treasury()address ABI method
   *
   * Return the current treasury address.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static getTreasury(params: CallParams<EendhanArgs['obj']['get_treasury()address'] | EendhanArgs['tuple']['get_treasury()address']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'get_treasury()address' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the get_creator()address ABI method
   *
   * Return the current creator address.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static getCreator(params: CallParams<EendhanArgs['obj']['get_creator()address'] | EendhanArgs['tuple']['get_creator()address']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'get_creator()address' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
}

/**
 * A client to make calls to the Eendhan smart contract
 */
export class EendhanClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `EendhanClient`
   *
   * @param appClient An `AppClient` instance which has been created with the Eendhan app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `EendhanClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Checks for decode errors on the given return value and maps the return value to the return type for the given method
   * @returns The typed return value or undefined if there was no value
   */
  decodeReturnValue<TSignature extends EendhanNonVoidMethodSignatures>(method: TSignature, returnValue: ABIReturn | undefined) {
    return returnValue !== undefined ? getArc56ReturnValue<MethodReturn<TSignature>>(returnValue, this.appClient.getABIMethod(method), APP_SPEC.structs) : undefined
  }

  /**
   * Returns a new `EendhanClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<EendhanClient> {
    return new EendhanClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `EendhanClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<EendhanClient> {
    return new EendhanClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Makes a clear_state call to an existing instance of the Eendhan smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the Eendhan smart contract using the `opt_in_to_usdc()void` ABI method.
     *
    * Opt-in the application account to USDC.
    Strictly gated to creator address only.

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    optInToUsdc: (params: CallParams<EendhanArgs['obj']['opt_in_to_usdc()void'] | EendhanArgs['tuple']['opt_in_to_usdc()void']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(EendhanParamsFactory.optInToUsdc(params))
    },

    /**
     * Makes a call to the Eendhan smart contract using the `process_payment_split(axfer)void` ABI method.
     *
    * Verify incoming USDC payment and split: 5% treasury, 95% creator.
    Payment must be sent to this contract's address in the same group.

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    processPaymentSplit: (params: CallParams<EendhanArgs['obj']['process_payment_split(axfer)void'] | EendhanArgs['tuple']['process_payment_split(axfer)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(EendhanParamsFactory.processPaymentSplit(params))
    },

    /**
     * Makes a call to the Eendhan smart contract using the `update_treasury(address)void` ABI method.
     *
     * Update the treasury address. Only creator can call.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    updateTreasury: (params: CallParams<EendhanArgs['obj']['update_treasury(address)void'] | EendhanArgs['tuple']['update_treasury(address)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(EendhanParamsFactory.updateTreasury(params))
    },

    /**
     * Makes a call to the Eendhan smart contract using the `get_treasury()address` ABI method.
     *
     * Return the current treasury address.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    getTreasury: (params: CallParams<EendhanArgs['obj']['get_treasury()address'] | EendhanArgs['tuple']['get_treasury()address']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(EendhanParamsFactory.getTreasury(params))
    },

    /**
     * Makes a call to the Eendhan smart contract using the `get_creator()address` ABI method.
     *
     * Return the current creator address.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    getCreator: (params: CallParams<EendhanArgs['obj']['get_creator()address'] | EendhanArgs['tuple']['get_creator()address']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(EendhanParamsFactory.getCreator(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Makes a clear_state call to an existing instance of the Eendhan smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the Eendhan smart contract using the `opt_in_to_usdc()void` ABI method.
     *
    * Opt-in the application account to USDC.
    Strictly gated to creator address only.

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    optInToUsdc: (params: CallParams<EendhanArgs['obj']['opt_in_to_usdc()void'] | EendhanArgs['tuple']['opt_in_to_usdc()void']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(EendhanParamsFactory.optInToUsdc(params))
    },

    /**
     * Makes a call to the Eendhan smart contract using the `process_payment_split(axfer)void` ABI method.
     *
    * Verify incoming USDC payment and split: 5% treasury, 95% creator.
    Payment must be sent to this contract's address in the same group.

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    processPaymentSplit: (params: CallParams<EendhanArgs['obj']['process_payment_split(axfer)void'] | EendhanArgs['tuple']['process_payment_split(axfer)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(EendhanParamsFactory.processPaymentSplit(params))
    },

    /**
     * Makes a call to the Eendhan smart contract using the `update_treasury(address)void` ABI method.
     *
     * Update the treasury address. Only creator can call.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    updateTreasury: (params: CallParams<EendhanArgs['obj']['update_treasury(address)void'] | EendhanArgs['tuple']['update_treasury(address)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(EendhanParamsFactory.updateTreasury(params))
    },

    /**
     * Makes a call to the Eendhan smart contract using the `get_treasury()address` ABI method.
     *
     * Return the current treasury address.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    getTreasury: (params: CallParams<EendhanArgs['obj']['get_treasury()address'] | EendhanArgs['tuple']['get_treasury()address']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(EendhanParamsFactory.getTreasury(params))
    },

    /**
     * Makes a call to the Eendhan smart contract using the `get_creator()address` ABI method.
     *
     * Return the current creator address.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    getCreator: (params: CallParams<EendhanArgs['obj']['get_creator()address'] | EendhanArgs['tuple']['get_creator()address']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(EendhanParamsFactory.getCreator(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Makes a clear_state call to an existing instance of the Eendhan smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the Eendhan smart contract using the `opt_in_to_usdc()void` ABI method.
     *
    * Opt-in the application account to USDC.
    Strictly gated to creator address only.

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    optInToUsdc: async (params: CallParams<EendhanArgs['obj']['opt_in_to_usdc()void'] | EendhanArgs['tuple']['opt_in_to_usdc()void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(EendhanParamsFactory.optInToUsdc(params))
      return {...result, return: result.return as unknown as (undefined | EendhanReturns['opt_in_to_usdc()void'])}
    },

    /**
     * Makes a call to the Eendhan smart contract using the `process_payment_split(axfer)void` ABI method.
     *
    * Verify incoming USDC payment and split: 5% treasury, 95% creator.
    Payment must be sent to this contract's address in the same group.

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    processPaymentSplit: async (params: CallParams<EendhanArgs['obj']['process_payment_split(axfer)void'] | EendhanArgs['tuple']['process_payment_split(axfer)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(EendhanParamsFactory.processPaymentSplit(params))
      return {...result, return: result.return as unknown as (undefined | EendhanReturns['process_payment_split(axfer)void'])}
    },

    /**
     * Makes a call to the Eendhan smart contract using the `update_treasury(address)void` ABI method.
     *
     * Update the treasury address. Only creator can call.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    updateTreasury: async (params: CallParams<EendhanArgs['obj']['update_treasury(address)void'] | EendhanArgs['tuple']['update_treasury(address)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(EendhanParamsFactory.updateTreasury(params))
      return {...result, return: result.return as unknown as (undefined | EendhanReturns['update_treasury(address)void'])}
    },

    /**
     * Makes a call to the Eendhan smart contract using the `get_treasury()address` ABI method.
     *
     * Return the current treasury address.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    getTreasury: async (params: CallParams<EendhanArgs['obj']['get_treasury()address'] | EendhanArgs['tuple']['get_treasury()address']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(EendhanParamsFactory.getTreasury(params))
      return {...result, return: result.return as unknown as (undefined | EendhanReturns['get_treasury()address'])}
    },

    /**
     * Makes a call to the Eendhan smart contract using the `get_creator()address` ABI method.
     *
     * Return the current creator address.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    getCreator: async (params: CallParams<EendhanArgs['obj']['get_creator()address'] | EendhanArgs['tuple']['get_creator()address']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(EendhanParamsFactory.getCreator(params))
      return {...result, return: result.return as unknown as (undefined | EendhanReturns['get_creator()address'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new EendhanClient(this.appClient.clone(params))
  }

  /**
   * Methods to access state for the current Eendhan app
   */
  state = {
    /**
     * Methods to access global state for the current Eendhan app
     */
    global: {
      /**
       * Get all current keyed values from global state
       */
      getAll: async (): Promise<Partial<Expand<GlobalKeysState>>> => {
        const result = await this.appClient.state.global.getAll()
        return {
          treasuryAddress: result.treasury_address,
          creatorAddress: result.creator_address,
          usdcAsset: result.usdc_asset,
        }
      },
      /**
       * Get the current value of the treasury_address key in global state
       */
      treasuryAddress: async (): Promise<string | undefined> => { return (await this.appClient.state.global.getValue("treasury_address")) as string | undefined },
      /**
       * Get the current value of the creator_address key in global state
       */
      creatorAddress: async (): Promise<string | undefined> => { return (await this.appClient.state.global.getValue("creator_address")) as string | undefined },
      /**
       * Get the current value of the usdc_asset key in global state
       */
      usdcAsset: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("usdc_asset")) as bigint | undefined },
    },
  }

  public newGroup(): EendhanComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a opt_in_to_usdc()void method call against the Eendhan contract
       */
      optInToUsdc(params: CallParams<EendhanArgs['obj']['opt_in_to_usdc()void'] | EendhanArgs['tuple']['opt_in_to_usdc()void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.optInToUsdc(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a process_payment_split(axfer)void method call against the Eendhan contract
       */
      processPaymentSplit(params: CallParams<EendhanArgs['obj']['process_payment_split(axfer)void'] | EendhanArgs['tuple']['process_payment_split(axfer)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.processPaymentSplit(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a update_treasury(address)void method call against the Eendhan contract
       */
      updateTreasury(params: CallParams<EendhanArgs['obj']['update_treasury(address)void'] | EendhanArgs['tuple']['update_treasury(address)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.updateTreasury(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a get_treasury()address method call against the Eendhan contract
       */
      getTreasury(params: CallParams<EendhanArgs['obj']['get_treasury()address'] | EendhanArgs['tuple']['get_treasury()address']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.getTreasury(params)))
        resultMappers.push((v) => client.decodeReturnValue('get_treasury()address', v))
        return this
      },
      /**
       * Add a get_creator()address method call against the Eendhan contract
       */
      getCreator(params: CallParams<EendhanArgs['obj']['get_creator()address'] | EendhanArgs['tuple']['get_creator()address']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.getCreator(params)))
        resultMappers.push((v) => client.decodeReturnValue('get_creator()address', v))
        return this
      },
      /**
       * Add a clear state call to the Eendhan contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as EendhanComposer
  }
}
export type EendhanComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the opt_in_to_usdc()void ABI method.
   *
  * Opt-in the application account to USDC.
  Strictly gated to creator address only.

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  optInToUsdc(params?: CallParams<EendhanArgs['obj']['opt_in_to_usdc()void'] | EendhanArgs['tuple']['opt_in_to_usdc()void']>): EendhanComposer<[...TReturns, EendhanReturns['opt_in_to_usdc()void'] | undefined]>

  /**
   * Calls the process_payment_split(axfer)void ABI method.
   *
  * Verify incoming USDC payment and split: 5% treasury, 95% creator.
  Payment must be sent to this contract's address in the same group.

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  processPaymentSplit(params?: CallParams<EendhanArgs['obj']['process_payment_split(axfer)void'] | EendhanArgs['tuple']['process_payment_split(axfer)void']>): EendhanComposer<[...TReturns, EendhanReturns['process_payment_split(axfer)void'] | undefined]>

  /**
   * Calls the update_treasury(address)void ABI method.
   *
   * Update the treasury address. Only creator can call.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  updateTreasury(params?: CallParams<EendhanArgs['obj']['update_treasury(address)void'] | EendhanArgs['tuple']['update_treasury(address)void']>): EendhanComposer<[...TReturns, EendhanReturns['update_treasury(address)void'] | undefined]>

  /**
   * Calls the get_treasury()address ABI method.
   *
   * Return the current treasury address.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  getTreasury(params?: CallParams<EendhanArgs['obj']['get_treasury()address'] | EendhanArgs['tuple']['get_treasury()address']>): EendhanComposer<[...TReturns, EendhanReturns['get_treasury()address'] | undefined]>

  /**
   * Calls the get_creator()address ABI method.
   *
   * Return the current creator address.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  getCreator(params?: CallParams<EendhanArgs['obj']['get_creator()address'] | EendhanArgs['tuple']['get_creator()address']>): EendhanComposer<[...TReturns, EendhanReturns['get_creator()address'] | undefined]>

  /**
   * Makes a clear_state call to an existing instance of the Eendhan smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): EendhanComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): EendhanComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<EendhanComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<EendhanComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<EendhanComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<EendhanComposerResults<TReturns>>
}
export type EendhanComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

