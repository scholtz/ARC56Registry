/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"TrustAnchor","structs":{},"methods":[{"name":"register_anchor","args":[{"type":"byte[]","name":"trait_id"},{"type":"byte[]","name":"commitment"}],"returns":{"type":"bool"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}},{"name":"get_commitment","args":[{"type":"byte[]","name":"user_address"}],"returns":{"type":"byte[]"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}},{"name":"verify","args":[{"type":"uint64","name":"threshold"},{"type":"byte[]","name":"proof_data"}],"returns":{"type":"bool"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}}],"arcs":[22,28],"networks":{},"state":{"schema":{"global":{"ints":0,"bytes":0},"local":{"ints":0,"bytes":0}},"keys":{"global":{},"local":{},"box":{}},"maps":{"global":{},"local":{},"box":{"anchors":{"keyType":"byte[]","valueType":"byte[]","prefix":"YW5jaG9yXw=="}}}},"bareActions":{"create":["NoOp"],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[127],"errorMessage":"check self.anchors entry exists"},{"pc":[76,89,115,152],"errorMessage":"invalid array length header"},{"pc":[83,96,122,158],"errorMessage":"invalid number of bytes for arc4.dynamic_array<arc4.uint8>"},{"pc":[146],"errorMessage":"invalid number of bytes for arc4.uint64"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"events":[]} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


/**
 * The argument types for the TrustAnchor contract
 */
export type TrustAnchorArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'register_anchor(byte[],byte[])bool': {
      traitId: Uint8Array
      commitment: Uint8Array
    }
    'get_commitment(byte[])byte[]': {
      userAddress: Uint8Array
    }
    'verify(uint64,byte[])bool': {
      threshold: bigint | number
      proofData: Uint8Array
    }
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'register_anchor(byte[],byte[])bool': [traitId: Uint8Array, commitment: Uint8Array]
    'get_commitment(byte[])byte[]': [userAddress: Uint8Array]
    'verify(uint64,byte[])bool': [threshold: bigint | number, proofData: Uint8Array]
  }
}

/**
 * The return type for each method
 */
export type TrustAnchorReturns = {
  'register_anchor(byte[],byte[])bool': boolean
  'get_commitment(byte[])byte[]': Uint8Array
  'verify(uint64,byte[])bool': boolean
}

/**
 * Defines the types of available calls and state of the TrustAnchor smart contract.
 */
export type TrustAnchorTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'register_anchor(byte[],byte[])bool' | 'register_anchor', {
      argsObj: TrustAnchorArgs['obj']['register_anchor(byte[],byte[])bool']
      argsTuple: TrustAnchorArgs['tuple']['register_anchor(byte[],byte[])bool']
      returns: TrustAnchorReturns['register_anchor(byte[],byte[])bool']
    }>
    & Record<'get_commitment(byte[])byte[]' | 'get_commitment', {
      argsObj: TrustAnchorArgs['obj']['get_commitment(byte[])byte[]']
      argsTuple: TrustAnchorArgs['tuple']['get_commitment(byte[])byte[]']
      returns: TrustAnchorReturns['get_commitment(byte[])byte[]']
    }>
    & Record<'verify(uint64,byte[])bool' | 'verify', {
      argsObj: TrustAnchorArgs['obj']['verify(uint64,byte[])bool']
      argsTuple: TrustAnchorArgs['tuple']['verify(uint64,byte[])bool']
      returns: TrustAnchorReturns['verify(uint64,byte[])bool']
    }>
  /**
   * Defines the shape of the state of the application.
   */
  state: {
    box: {
      keys: {}
      maps: {
        anchors: Map<Uint8Array, Uint8Array>
      }
    }
  }
}

/**
 * Defines the possible abi call signatures.
 */
export type TrustAnchorSignatures = keyof TrustAnchorTypes['methods']
/**
 * Defines the possible abi call signatures for methods that return a non-void value.
 */
export type TrustAnchorNonVoidMethodSignatures = keyof TrustAnchorTypes['methods'] extends infer T ? T extends keyof TrustAnchorTypes['methods'] ? MethodReturn<T> extends void ? never : T  : never : never
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the TrustAnchor smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends TrustAnchorSignatures> = TrustAnchorTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the TrustAnchor smart contract to the method's return type
 */
export type MethodReturn<TSignature extends TrustAnchorSignatures> = TrustAnchorTypes['methods'][TSignature]['returns']

/**
 * Defines the shape of the keyed box state of the application.
 */
export type BoxKeysState = TrustAnchorTypes['state']['box']['keys']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the TrustAnchor smart contract
 */
export abstract class TrustAnchorParamsFactory {
  /**
   * Constructs a no op call for the register_anchor(byte[],byte[])bool ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static registerAnchor(params: CallParams<TrustAnchorArgs['obj']['register_anchor(byte[],byte[])bool'] | TrustAnchorArgs['tuple']['register_anchor(byte[],byte[])bool']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'register_anchor(byte[],byte[])bool' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.traitId, params.args.commitment],
    }
  }
  /**
   * Constructs a no op call for the get_commitment(byte[])byte[] ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static getCommitment(params: CallParams<TrustAnchorArgs['obj']['get_commitment(byte[])byte[]'] | TrustAnchorArgs['tuple']['get_commitment(byte[])byte[]']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'get_commitment(byte[])byte[]' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.userAddress],
    }
  }
  /**
   * Constructs a no op call for the verify(uint64,byte[])bool ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static verify(params: CallParams<TrustAnchorArgs['obj']['verify(uint64,byte[])bool'] | TrustAnchorArgs['tuple']['verify(uint64,byte[])bool']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'verify(uint64,byte[])bool' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.threshold, params.args.proofData],
    }
  }
}

/**
 * A client to make calls to the TrustAnchor smart contract
 */
export class TrustAnchorClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `TrustAnchorClient`
   *
   * @param appClient An `AppClient` instance which has been created with the TrustAnchor app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `TrustAnchorClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Checks for decode errors on the given return value and maps the return value to the return type for the given method
   * @returns The typed return value or undefined if there was no value
   */
  decodeReturnValue<TSignature extends TrustAnchorNonVoidMethodSignatures>(method: TSignature, returnValue: ABIReturn | undefined) {
    return returnValue !== undefined ? getArc56ReturnValue<MethodReturn<TSignature>>(returnValue, this.appClient.getABIMethod(method), APP_SPEC.structs) : undefined
  }

  /**
   * Returns a new `TrustAnchorClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<TrustAnchorClient> {
    return new TrustAnchorClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `TrustAnchorClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<TrustAnchorClient> {
    return new TrustAnchorClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Makes a clear_state call to an existing instance of the TrustAnchor smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the TrustAnchor smart contract using the `register_anchor(byte[],byte[])bool` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    registerAnchor: (params: CallParams<TrustAnchorArgs['obj']['register_anchor(byte[],byte[])bool'] | TrustAnchorArgs['tuple']['register_anchor(byte[],byte[])bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(TrustAnchorParamsFactory.registerAnchor(params))
    },

    /**
     * Makes a call to the TrustAnchor smart contract using the `get_commitment(byte[])byte[]` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    getCommitment: (params: CallParams<TrustAnchorArgs['obj']['get_commitment(byte[])byte[]'] | TrustAnchorArgs['tuple']['get_commitment(byte[])byte[]']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(TrustAnchorParamsFactory.getCommitment(params))
    },

    /**
     * Makes a call to the TrustAnchor smart contract using the `verify(uint64,byte[])bool` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    verify: (params: CallParams<TrustAnchorArgs['obj']['verify(uint64,byte[])bool'] | TrustAnchorArgs['tuple']['verify(uint64,byte[])bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(TrustAnchorParamsFactory.verify(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Makes a clear_state call to an existing instance of the TrustAnchor smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the TrustAnchor smart contract using the `register_anchor(byte[],byte[])bool` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    registerAnchor: (params: CallParams<TrustAnchorArgs['obj']['register_anchor(byte[],byte[])bool'] | TrustAnchorArgs['tuple']['register_anchor(byte[],byte[])bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(TrustAnchorParamsFactory.registerAnchor(params))
    },

    /**
     * Makes a call to the TrustAnchor smart contract using the `get_commitment(byte[])byte[]` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    getCommitment: (params: CallParams<TrustAnchorArgs['obj']['get_commitment(byte[])byte[]'] | TrustAnchorArgs['tuple']['get_commitment(byte[])byte[]']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(TrustAnchorParamsFactory.getCommitment(params))
    },

    /**
     * Makes a call to the TrustAnchor smart contract using the `verify(uint64,byte[])bool` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    verify: (params: CallParams<TrustAnchorArgs['obj']['verify(uint64,byte[])bool'] | TrustAnchorArgs['tuple']['verify(uint64,byte[])bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(TrustAnchorParamsFactory.verify(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Makes a clear_state call to an existing instance of the TrustAnchor smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the TrustAnchor smart contract using the `register_anchor(byte[],byte[])bool` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    registerAnchor: async (params: CallParams<TrustAnchorArgs['obj']['register_anchor(byte[],byte[])bool'] | TrustAnchorArgs['tuple']['register_anchor(byte[],byte[])bool']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(TrustAnchorParamsFactory.registerAnchor(params))
      return {...result, return: result.return as unknown as (undefined | TrustAnchorReturns['register_anchor(byte[],byte[])bool'])}
    },

    /**
     * Makes a call to the TrustAnchor smart contract using the `get_commitment(byte[])byte[]` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    getCommitment: async (params: CallParams<TrustAnchorArgs['obj']['get_commitment(byte[])byte[]'] | TrustAnchorArgs['tuple']['get_commitment(byte[])byte[]']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(TrustAnchorParamsFactory.getCommitment(params))
      return {...result, return: result.return as unknown as (undefined | TrustAnchorReturns['get_commitment(byte[])byte[]'])}
    },

    /**
     * Makes a call to the TrustAnchor smart contract using the `verify(uint64,byte[])bool` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    verify: async (params: CallParams<TrustAnchorArgs['obj']['verify(uint64,byte[])bool'] | TrustAnchorArgs['tuple']['verify(uint64,byte[])bool']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(TrustAnchorParamsFactory.verify(params))
      return {...result, return: result.return as unknown as (undefined | TrustAnchorReturns['verify(uint64,byte[])bool'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new TrustAnchorClient(this.appClient.clone(params))
  }

  /**
   * Methods to access state for the current TrustAnchor app
   */
  state = {
    /**
     * Methods to access box state for the current TrustAnchor app
     */
    box: {
      /**
       * Get all current keyed values from box state
       */
      getAll: async (): Promise<Partial<Expand<BoxKeysState>>> => {
        const result = await this.appClient.state.box.getAll()
        return {
        }
      },
      /**
       * Get values from the anchors map in box state
       */
      anchors: {
        /**
         * Get all current values of the anchors map in box state
         */
        getMap: async (): Promise<Map<Uint8Array, Uint8Array>> => { return (await this.appClient.state.box.getMap("anchors")) as Map<Uint8Array, Uint8Array> },
        /**
         * Get a current value of the anchors map by key from box state
         */
        value: async (key: Uint8Array): Promise<Uint8Array | undefined> => { return await this.appClient.state.box.getMapValue("anchors", key) as Uint8Array | undefined },
      },
    },
  }

  public newGroup(): TrustAnchorComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a register_anchor(byte[],byte[])bool method call against the TrustAnchor contract
       */
      registerAnchor(params: CallParams<TrustAnchorArgs['obj']['register_anchor(byte[],byte[])bool'] | TrustAnchorArgs['tuple']['register_anchor(byte[],byte[])bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.registerAnchor(params)))
        resultMappers.push((v) => client.decodeReturnValue('register_anchor(byte[],byte[])bool', v))
        return this
      },
      /**
       * Add a get_commitment(byte[])byte[] method call against the TrustAnchor contract
       */
      getCommitment(params: CallParams<TrustAnchorArgs['obj']['get_commitment(byte[])byte[]'] | TrustAnchorArgs['tuple']['get_commitment(byte[])byte[]']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.getCommitment(params)))
        resultMappers.push((v) => client.decodeReturnValue('get_commitment(byte[])byte[]', v))
        return this
      },
      /**
       * Add a verify(uint64,byte[])bool method call against the TrustAnchor contract
       */
      verify(params: CallParams<TrustAnchorArgs['obj']['verify(uint64,byte[])bool'] | TrustAnchorArgs['tuple']['verify(uint64,byte[])bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.verify(params)))
        resultMappers.push((v) => client.decodeReturnValue('verify(uint64,byte[])bool', v))
        return this
      },
      /**
       * Add a clear state call to the TrustAnchor contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as TrustAnchorComposer
  }
}
export type TrustAnchorComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the register_anchor(byte[],byte[])bool ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  registerAnchor(params?: CallParams<TrustAnchorArgs['obj']['register_anchor(byte[],byte[])bool'] | TrustAnchorArgs['tuple']['register_anchor(byte[],byte[])bool']>): TrustAnchorComposer<[...TReturns, TrustAnchorReturns['register_anchor(byte[],byte[])bool'] | undefined]>

  /**
   * Calls the get_commitment(byte[])byte[] ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  getCommitment(params?: CallParams<TrustAnchorArgs['obj']['get_commitment(byte[])byte[]'] | TrustAnchorArgs['tuple']['get_commitment(byte[])byte[]']>): TrustAnchorComposer<[...TReturns, TrustAnchorReturns['get_commitment(byte[])byte[]'] | undefined]>

  /**
   * Calls the verify(uint64,byte[])bool ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  verify(params?: CallParams<TrustAnchorArgs['obj']['verify(uint64,byte[])bool'] | TrustAnchorArgs['tuple']['verify(uint64,byte[])bool']>): TrustAnchorComposer<[...TReturns, TrustAnchorReturns['verify(uint64,byte[])bool'] | undefined]>

  /**
   * Makes a clear_state call to an existing instance of the TrustAnchor smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): TrustAnchorComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): TrustAnchorComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<TrustAnchorComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<TrustAnchorComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<TrustAnchorComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<TrustAnchorComposerResults<TReturns>>
}
export type TrustAnchorComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

