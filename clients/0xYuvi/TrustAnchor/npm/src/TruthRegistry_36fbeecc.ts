/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"TruthRegistry","structs":{"ZKProofStatus":[{"name":"proofId","type":"byte[]"},{"name":"threshold","type":"uint64"},{"name":"proofHash","type":"uint8[32]"},{"name":"isVerified","type":"bool"},{"name":"submittedAtRound","type":"uint64"}]},"methods":[{"name":"create","args":[{"type":"uint64","name":"identity_registry_app_id"}],"returns":{"type":"void"},"actions":{"create":["NoOp"],"call":[]},"readonly":false,"events":[],"recommendations":{}},{"name":"set_verifier","args":[{"type":"uint64","name":"verifier_app_id"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}},{"name":"register_anchor","args":[{"type":"byte[]","name":"trait_id"},{"type":"byte[]","name":"commitment"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}},{"name":"verify_zk_claim","args":[{"type":"byte[]","name":"trait_id"},{"type":"byte[]","name":"proof_id"},{"type":"uint64","name":"threshold"},{"type":"uint8[32]","name":"proof_hash"}],"returns":{"type":"bool"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Verify and settle a ZK claim.\nThe claim is verified off-chain by the institution and then anchored here.","events":[],"recommendations":{}},{"name":"get_proof_status","args":[{"type":"byte[]","name":"proof_id"}],"returns":{"type":"(byte[],uint64,uint8[32],bool,uint64)","struct":"ZKProofStatus"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"is_proof_verified","args":[{"type":"byte[]","name":"proof_id"}],"returns":{"type":"bool"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"get_commitment","args":[{"type":"byte[]","name":"trait_id"}],"returns":{"type":"byte[]"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"is_trait_registered","args":[{"type":"byte[]","name":"trait_id"}],"returns":{"type":"bool"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"get_anchor_count","args":[],"returns":{"type":"uint64"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}}],"arcs":[22,28],"networks":{},"state":{"schema":{"global":{"ints":3,"bytes":0},"local":{"ints":0,"bytes":0}},"keys":{"global":{"identity_registry_app_id":{"keyType":"AVMString","valueType":"AVMUint64","key":"aWRlbnRpdHlfcmVnaXN0cnlfYXBwX2lk"},"anchor_count":{"keyType":"AVMString","valueType":"AVMUint64","key":"YW5jaG9yX2NvdW50"},"verifier_app_id":{"keyType":"AVMString","valueType":"AVMUint64","key":"dmVyaWZpZXJfYXBwX2lk"}},"local":{},"box":{}},"maps":{"global":{},"local":{},"box":{"anchors":{"keyType":"byte[]","valueType":"byte[]","prefix":"YW5jaG9yXw=="},"proofs":{"keyType":"byte[]","valueType":"ZKProofStatus","prefix":"cHJvb2Zf"}}}},"bareActions":{"create":[],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[258,333],"errorMessage":"Caller must be a registered institution"},{"pc":[219],"errorMessage":"Only creator can set verifier"},{"pc":[405],"errorMessage":"Proof not found"},{"pc":[268],"errorMessage":"Trait ID already registered"},{"pc":[341,480],"errorMessage":"Trait ID not registered"},{"pc":[569],"errorMessage":"application log value is not the result of an ABI return"},{"pc":[277,522],"errorMessage":"check self.anchor_count exists"},{"pc":[538],"errorMessage":"check self.identity_registry_app_id exists"},{"pc":[449],"errorMessage":"index out of bounds"},{"pc":[232,245,290,303,389,419,465,494],"errorMessage":"invalid array length header"},{"pc":[577],"errorMessage":"invalid number of bytes for arc4.bool"},{"pc":[239,252,297,310,396,426,472,501],"errorMessage":"invalid number of bytes for arc4.dynamic_array<arc4.uint8>"},{"pc":[327],"errorMessage":"invalid number of bytes for arc4.static_array<arc4.uint8, 32>"},{"pc":[195,213,318],"errorMessage":"invalid number of bytes for arc4.uint64"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"events":[]} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


// Type definitions for ARC-56 structs

export type ZkProofStatus = {
  proofId: Uint8Array,
  threshold: bigint,
  proofHash: [number, number, number, number, number, number, number, number, number, number, number, number, number, number, number, number, number, number, number, number, number, number, number, number, number, number, number, number, number, number, number, number],
  isVerified: boolean,
  submittedAtRound: bigint
}


/**
 * Converts the ABI tuple representation of a ZKProofStatus to the struct representation
 */
export function ZkProofStatusFromTuple(abiTuple: [Uint8Array, bigint, [number, number, number, number, number, number, number, number, number, number, number, number, number, number, number, number, number, number, number, number, number, number, number, number, number, number, number, number, number, number, number, number], boolean, bigint]) {
  return getABIStructFromABITuple(abiTuple, APP_SPEC.structs.ZKProofStatus, APP_SPEC.structs) as ZkProofStatus
}

/**
 * The argument types for the TruthRegistry contract
 */
export type TruthRegistryArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'create(uint64)void': {
      identityRegistryAppId: bigint | number
    }
    'set_verifier(uint64)void': {
      verifierAppId: bigint | number
    }
    'register_anchor(byte[],byte[])void': {
      traitId: Uint8Array
      commitment: Uint8Array
    }
    'verify_zk_claim(byte[],byte[],uint64,uint8[32])bool': {
      traitId: Uint8Array
      proofId: Uint8Array
      threshold: bigint | number
      proofHash: [bigint | number, bigint | number, bigint | number, bigint | number, bigint | number, bigint | number, bigint | number, bigint | number, bigint | number, bigint | number, bigint | number, bigint | number, bigint | number, bigint | number, bigint | number, bigint | number, bigint | number, bigint | number, bigint | number, bigint | number, bigint | number, bigint | number, bigint | number, bigint | number, bigint | number, bigint | number, bigint | number, bigint | number, bigint | number, bigint | number, bigint | number, bigint | number]
    }
    'get_proof_status(byte[])(byte[],uint64,uint8[32],bool,uint64)': {
      proofId: Uint8Array
    }
    'is_proof_verified(byte[])bool': {
      proofId: Uint8Array
    }
    'get_commitment(byte[])byte[]': {
      traitId: Uint8Array
    }
    'is_trait_registered(byte[])bool': {
      traitId: Uint8Array
    }
    'get_anchor_count()uint64': Record<string, never>
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'create(uint64)void': [identityRegistryAppId: bigint | number]
    'set_verifier(uint64)void': [verifierAppId: bigint | number]
    'register_anchor(byte[],byte[])void': [traitId: Uint8Array, commitment: Uint8Array]
    'verify_zk_claim(byte[],byte[],uint64,uint8[32])bool': [traitId: Uint8Array, proofId: Uint8Array, threshold: bigint | number, proofHash: [bigint | number, bigint | number, bigint | number, bigint | number, bigint | number, bigint | number, bigint | number, bigint | number, bigint | number, bigint | number, bigint | number, bigint | number, bigint | number, bigint | number, bigint | number, bigint | number, bigint | number, bigint | number, bigint | number, bigint | number, bigint | number, bigint | number, bigint | number, bigint | number, bigint | number, bigint | number, bigint | number, bigint | number, bigint | number, bigint | number, bigint | number, bigint | number]]
    'get_proof_status(byte[])(byte[],uint64,uint8[32],bool,uint64)': [proofId: Uint8Array]
    'is_proof_verified(byte[])bool': [proofId: Uint8Array]
    'get_commitment(byte[])byte[]': [traitId: Uint8Array]
    'is_trait_registered(byte[])bool': [traitId: Uint8Array]
    'get_anchor_count()uint64': []
  }
}

/**
 * The return type for each method
 */
export type TruthRegistryReturns = {
  'create(uint64)void': void
  'set_verifier(uint64)void': void
  'register_anchor(byte[],byte[])void': void
  'verify_zk_claim(byte[],byte[],uint64,uint8[32])bool': boolean
  'get_proof_status(byte[])(byte[],uint64,uint8[32],bool,uint64)': ZkProofStatus
  'is_proof_verified(byte[])bool': boolean
  'get_commitment(byte[])byte[]': Uint8Array
  'is_trait_registered(byte[])bool': boolean
  'get_anchor_count()uint64': bigint
}

/**
 * Defines the types of available calls and state of the TruthRegistry smart contract.
 */
export type TruthRegistryTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'create(uint64)void' | 'create', {
      argsObj: TruthRegistryArgs['obj']['create(uint64)void']
      argsTuple: TruthRegistryArgs['tuple']['create(uint64)void']
      returns: TruthRegistryReturns['create(uint64)void']
    }>
    & Record<'set_verifier(uint64)void' | 'set_verifier', {
      argsObj: TruthRegistryArgs['obj']['set_verifier(uint64)void']
      argsTuple: TruthRegistryArgs['tuple']['set_verifier(uint64)void']
      returns: TruthRegistryReturns['set_verifier(uint64)void']
    }>
    & Record<'register_anchor(byte[],byte[])void' | 'register_anchor', {
      argsObj: TruthRegistryArgs['obj']['register_anchor(byte[],byte[])void']
      argsTuple: TruthRegistryArgs['tuple']['register_anchor(byte[],byte[])void']
      returns: TruthRegistryReturns['register_anchor(byte[],byte[])void']
    }>
    & Record<'verify_zk_claim(byte[],byte[],uint64,uint8[32])bool' | 'verify_zk_claim', {
      argsObj: TruthRegistryArgs['obj']['verify_zk_claim(byte[],byte[],uint64,uint8[32])bool']
      argsTuple: TruthRegistryArgs['tuple']['verify_zk_claim(byte[],byte[],uint64,uint8[32])bool']
      returns: TruthRegistryReturns['verify_zk_claim(byte[],byte[],uint64,uint8[32])bool']
    }>
    & Record<'get_proof_status(byte[])(byte[],uint64,uint8[32],bool,uint64)' | 'get_proof_status', {
      argsObj: TruthRegistryArgs['obj']['get_proof_status(byte[])(byte[],uint64,uint8[32],bool,uint64)']
      argsTuple: TruthRegistryArgs['tuple']['get_proof_status(byte[])(byte[],uint64,uint8[32],bool,uint64)']
      returns: TruthRegistryReturns['get_proof_status(byte[])(byte[],uint64,uint8[32],bool,uint64)']
    }>
    & Record<'is_proof_verified(byte[])bool' | 'is_proof_verified', {
      argsObj: TruthRegistryArgs['obj']['is_proof_verified(byte[])bool']
      argsTuple: TruthRegistryArgs['tuple']['is_proof_verified(byte[])bool']
      returns: TruthRegistryReturns['is_proof_verified(byte[])bool']
    }>
    & Record<'get_commitment(byte[])byte[]' | 'get_commitment', {
      argsObj: TruthRegistryArgs['obj']['get_commitment(byte[])byte[]']
      argsTuple: TruthRegistryArgs['tuple']['get_commitment(byte[])byte[]']
      returns: TruthRegistryReturns['get_commitment(byte[])byte[]']
    }>
    & Record<'is_trait_registered(byte[])bool' | 'is_trait_registered', {
      argsObj: TruthRegistryArgs['obj']['is_trait_registered(byte[])bool']
      argsTuple: TruthRegistryArgs['tuple']['is_trait_registered(byte[])bool']
      returns: TruthRegistryReturns['is_trait_registered(byte[])bool']
    }>
    & Record<'get_anchor_count()uint64' | 'get_anchor_count', {
      argsObj: TruthRegistryArgs['obj']['get_anchor_count()uint64']
      argsTuple: TruthRegistryArgs['tuple']['get_anchor_count()uint64']
      returns: TruthRegistryReturns['get_anchor_count()uint64']
    }>
  /**
   * Defines the shape of the state of the application.
   */
  state: {
    global: {
      keys: {
        identityRegistryAppId: bigint
        anchorCount: bigint
        verifierAppId: bigint
      }
      maps: {}
    }
    box: {
      keys: {}
      maps: {
        anchors: Map<Uint8Array, Uint8Array>
        proofs: Map<Uint8Array, ZkProofStatus>
      }
    }
  }
}

/**
 * Defines the possible abi call signatures.
 */
export type TruthRegistrySignatures = keyof TruthRegistryTypes['methods']
/**
 * Defines the possible abi call signatures for methods that return a non-void value.
 */
export type TruthRegistryNonVoidMethodSignatures = keyof TruthRegistryTypes['methods'] extends infer T ? T extends keyof TruthRegistryTypes['methods'] ? MethodReturn<T> extends void ? never : T  : never : never
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the TruthRegistry smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends TruthRegistrySignatures> = TruthRegistryTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the TruthRegistry smart contract to the method's return type
 */
export type MethodReturn<TSignature extends TruthRegistrySignatures> = TruthRegistryTypes['methods'][TSignature]['returns']

/**
 * Defines the shape of the keyed global state of the application.
 */
export type GlobalKeysState = TruthRegistryTypes['state']['global']['keys']

/**
 * Defines the shape of the keyed box state of the application.
 */
export type BoxKeysState = TruthRegistryTypes['state']['box']['keys']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the TruthRegistry smart contract
 */
export abstract class TruthRegistryParamsFactory {
  /**
   * Constructs a no op call for the set_verifier(uint64)void ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static setVerifier(params: CallParams<TruthRegistryArgs['obj']['set_verifier(uint64)void'] | TruthRegistryArgs['tuple']['set_verifier(uint64)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'set_verifier(uint64)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.verifierAppId],
    }
  }
  /**
   * Constructs a no op call for the register_anchor(byte[],byte[])void ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static registerAnchor(params: CallParams<TruthRegistryArgs['obj']['register_anchor(byte[],byte[])void'] | TruthRegistryArgs['tuple']['register_anchor(byte[],byte[])void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'register_anchor(byte[],byte[])void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.traitId, params.args.commitment],
    }
  }
  /**
   * Constructs a no op call for the verify_zk_claim(byte[],byte[],uint64,uint8[32])bool ABI method
   *
  * Verify and settle a ZK claim.
  The claim is verified off-chain by the institution and then anchored here.

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static verifyZkClaim(params: CallParams<TruthRegistryArgs['obj']['verify_zk_claim(byte[],byte[],uint64,uint8[32])bool'] | TruthRegistryArgs['tuple']['verify_zk_claim(byte[],byte[],uint64,uint8[32])bool']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'verify_zk_claim(byte[],byte[],uint64,uint8[32])bool' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.traitId, params.args.proofId, params.args.threshold, params.args.proofHash],
    }
  }
  /**
   * Constructs a no op call for the get_proof_status(byte[])(byte[],uint64,uint8[32],bool,uint64) ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static getProofStatus(params: CallParams<TruthRegistryArgs['obj']['get_proof_status(byte[])(byte[],uint64,uint8[32],bool,uint64)'] | TruthRegistryArgs['tuple']['get_proof_status(byte[])(byte[],uint64,uint8[32],bool,uint64)']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'get_proof_status(byte[])(byte[],uint64,uint8[32],bool,uint64)' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.proofId],
    }
  }
  /**
   * Constructs a no op call for the is_proof_verified(byte[])bool ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static isProofVerified(params: CallParams<TruthRegistryArgs['obj']['is_proof_verified(byte[])bool'] | TruthRegistryArgs['tuple']['is_proof_verified(byte[])bool']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'is_proof_verified(byte[])bool' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.proofId],
    }
  }
  /**
   * Constructs a no op call for the get_commitment(byte[])byte[] ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static getCommitment(params: CallParams<TruthRegistryArgs['obj']['get_commitment(byte[])byte[]'] | TruthRegistryArgs['tuple']['get_commitment(byte[])byte[]']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'get_commitment(byte[])byte[]' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.traitId],
    }
  }
  /**
   * Constructs a no op call for the is_trait_registered(byte[])bool ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static isTraitRegistered(params: CallParams<TruthRegistryArgs['obj']['is_trait_registered(byte[])bool'] | TruthRegistryArgs['tuple']['is_trait_registered(byte[])bool']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'is_trait_registered(byte[])bool' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.traitId],
    }
  }
  /**
   * Constructs a no op call for the get_anchor_count()uint64 ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static getAnchorCount(params: CallParams<TruthRegistryArgs['obj']['get_anchor_count()uint64'] | TruthRegistryArgs['tuple']['get_anchor_count()uint64']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'get_anchor_count()uint64' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
}

/**
 * A client to make calls to the TruthRegistry smart contract
 */
export class TruthRegistryClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `TruthRegistryClient`
   *
   * @param appClient An `AppClient` instance which has been created with the TruthRegistry app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `TruthRegistryClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Checks for decode errors on the given return value and maps the return value to the return type for the given method
   * @returns The typed return value or undefined if there was no value
   */
  decodeReturnValue<TSignature extends TruthRegistryNonVoidMethodSignatures>(method: TSignature, returnValue: ABIReturn | undefined) {
    return returnValue !== undefined ? getArc56ReturnValue<MethodReturn<TSignature>>(returnValue, this.appClient.getABIMethod(method), APP_SPEC.structs) : undefined
  }

  /**
   * Returns a new `TruthRegistryClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<TruthRegistryClient> {
    return new TruthRegistryClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `TruthRegistryClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<TruthRegistryClient> {
    return new TruthRegistryClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Makes a clear_state call to an existing instance of the TruthRegistry smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the TruthRegistry smart contract using the `set_verifier(uint64)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    setVerifier: (params: CallParams<TruthRegistryArgs['obj']['set_verifier(uint64)void'] | TruthRegistryArgs['tuple']['set_verifier(uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(TruthRegistryParamsFactory.setVerifier(params))
    },

    /**
     * Makes a call to the TruthRegistry smart contract using the `register_anchor(byte[],byte[])void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    registerAnchor: (params: CallParams<TruthRegistryArgs['obj']['register_anchor(byte[],byte[])void'] | TruthRegistryArgs['tuple']['register_anchor(byte[],byte[])void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(TruthRegistryParamsFactory.registerAnchor(params))
    },

    /**
     * Makes a call to the TruthRegistry smart contract using the `verify_zk_claim(byte[],byte[],uint64,uint8[32])bool` ABI method.
     *
    * Verify and settle a ZK claim.
    The claim is verified off-chain by the institution and then anchored here.

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    verifyZkClaim: (params: CallParams<TruthRegistryArgs['obj']['verify_zk_claim(byte[],byte[],uint64,uint8[32])bool'] | TruthRegistryArgs['tuple']['verify_zk_claim(byte[],byte[],uint64,uint8[32])bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(TruthRegistryParamsFactory.verifyZkClaim(params))
    },

    /**
     * Makes a call to the TruthRegistry smart contract using the `get_proof_status(byte[])(byte[],uint64,uint8[32],bool,uint64)` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    getProofStatus: (params: CallParams<TruthRegistryArgs['obj']['get_proof_status(byte[])(byte[],uint64,uint8[32],bool,uint64)'] | TruthRegistryArgs['tuple']['get_proof_status(byte[])(byte[],uint64,uint8[32],bool,uint64)']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(TruthRegistryParamsFactory.getProofStatus(params))
    },

    /**
     * Makes a call to the TruthRegistry smart contract using the `is_proof_verified(byte[])bool` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    isProofVerified: (params: CallParams<TruthRegistryArgs['obj']['is_proof_verified(byte[])bool'] | TruthRegistryArgs['tuple']['is_proof_verified(byte[])bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(TruthRegistryParamsFactory.isProofVerified(params))
    },

    /**
     * Makes a call to the TruthRegistry smart contract using the `get_commitment(byte[])byte[]` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    getCommitment: (params: CallParams<TruthRegistryArgs['obj']['get_commitment(byte[])byte[]'] | TruthRegistryArgs['tuple']['get_commitment(byte[])byte[]']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(TruthRegistryParamsFactory.getCommitment(params))
    },

    /**
     * Makes a call to the TruthRegistry smart contract using the `is_trait_registered(byte[])bool` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    isTraitRegistered: (params: CallParams<TruthRegistryArgs['obj']['is_trait_registered(byte[])bool'] | TruthRegistryArgs['tuple']['is_trait_registered(byte[])bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(TruthRegistryParamsFactory.isTraitRegistered(params))
    },

    /**
     * Makes a call to the TruthRegistry smart contract using the `get_anchor_count()uint64` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    getAnchorCount: (params: CallParams<TruthRegistryArgs['obj']['get_anchor_count()uint64'] | TruthRegistryArgs['tuple']['get_anchor_count()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(TruthRegistryParamsFactory.getAnchorCount(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Makes a clear_state call to an existing instance of the TruthRegistry smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the TruthRegistry smart contract using the `set_verifier(uint64)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    setVerifier: (params: CallParams<TruthRegistryArgs['obj']['set_verifier(uint64)void'] | TruthRegistryArgs['tuple']['set_verifier(uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(TruthRegistryParamsFactory.setVerifier(params))
    },

    /**
     * Makes a call to the TruthRegistry smart contract using the `register_anchor(byte[],byte[])void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    registerAnchor: (params: CallParams<TruthRegistryArgs['obj']['register_anchor(byte[],byte[])void'] | TruthRegistryArgs['tuple']['register_anchor(byte[],byte[])void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(TruthRegistryParamsFactory.registerAnchor(params))
    },

    /**
     * Makes a call to the TruthRegistry smart contract using the `verify_zk_claim(byte[],byte[],uint64,uint8[32])bool` ABI method.
     *
    * Verify and settle a ZK claim.
    The claim is verified off-chain by the institution and then anchored here.

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    verifyZkClaim: (params: CallParams<TruthRegistryArgs['obj']['verify_zk_claim(byte[],byte[],uint64,uint8[32])bool'] | TruthRegistryArgs['tuple']['verify_zk_claim(byte[],byte[],uint64,uint8[32])bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(TruthRegistryParamsFactory.verifyZkClaim(params))
    },

    /**
     * Makes a call to the TruthRegistry smart contract using the `get_proof_status(byte[])(byte[],uint64,uint8[32],bool,uint64)` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    getProofStatus: (params: CallParams<TruthRegistryArgs['obj']['get_proof_status(byte[])(byte[],uint64,uint8[32],bool,uint64)'] | TruthRegistryArgs['tuple']['get_proof_status(byte[])(byte[],uint64,uint8[32],bool,uint64)']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(TruthRegistryParamsFactory.getProofStatus(params))
    },

    /**
     * Makes a call to the TruthRegistry smart contract using the `is_proof_verified(byte[])bool` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    isProofVerified: (params: CallParams<TruthRegistryArgs['obj']['is_proof_verified(byte[])bool'] | TruthRegistryArgs['tuple']['is_proof_verified(byte[])bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(TruthRegistryParamsFactory.isProofVerified(params))
    },

    /**
     * Makes a call to the TruthRegistry smart contract using the `get_commitment(byte[])byte[]` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    getCommitment: (params: CallParams<TruthRegistryArgs['obj']['get_commitment(byte[])byte[]'] | TruthRegistryArgs['tuple']['get_commitment(byte[])byte[]']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(TruthRegistryParamsFactory.getCommitment(params))
    },

    /**
     * Makes a call to the TruthRegistry smart contract using the `is_trait_registered(byte[])bool` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    isTraitRegistered: (params: CallParams<TruthRegistryArgs['obj']['is_trait_registered(byte[])bool'] | TruthRegistryArgs['tuple']['is_trait_registered(byte[])bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(TruthRegistryParamsFactory.isTraitRegistered(params))
    },

    /**
     * Makes a call to the TruthRegistry smart contract using the `get_anchor_count()uint64` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    getAnchorCount: (params: CallParams<TruthRegistryArgs['obj']['get_anchor_count()uint64'] | TruthRegistryArgs['tuple']['get_anchor_count()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(TruthRegistryParamsFactory.getAnchorCount(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Makes a clear_state call to an existing instance of the TruthRegistry smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the TruthRegistry smart contract using the `set_verifier(uint64)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    setVerifier: async (params: CallParams<TruthRegistryArgs['obj']['set_verifier(uint64)void'] | TruthRegistryArgs['tuple']['set_verifier(uint64)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(TruthRegistryParamsFactory.setVerifier(params))
      return {...result, return: result.return as unknown as (undefined | TruthRegistryReturns['set_verifier(uint64)void'])}
    },

    /**
     * Makes a call to the TruthRegistry smart contract using the `register_anchor(byte[],byte[])void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    registerAnchor: async (params: CallParams<TruthRegistryArgs['obj']['register_anchor(byte[],byte[])void'] | TruthRegistryArgs['tuple']['register_anchor(byte[],byte[])void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(TruthRegistryParamsFactory.registerAnchor(params))
      return {...result, return: result.return as unknown as (undefined | TruthRegistryReturns['register_anchor(byte[],byte[])void'])}
    },

    /**
     * Makes a call to the TruthRegistry smart contract using the `verify_zk_claim(byte[],byte[],uint64,uint8[32])bool` ABI method.
     *
    * Verify and settle a ZK claim.
    The claim is verified off-chain by the institution and then anchored here.

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    verifyZkClaim: async (params: CallParams<TruthRegistryArgs['obj']['verify_zk_claim(byte[],byte[],uint64,uint8[32])bool'] | TruthRegistryArgs['tuple']['verify_zk_claim(byte[],byte[],uint64,uint8[32])bool']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(TruthRegistryParamsFactory.verifyZkClaim(params))
      return {...result, return: result.return as unknown as (undefined | TruthRegistryReturns['verify_zk_claim(byte[],byte[],uint64,uint8[32])bool'])}
    },

    /**
     * Makes a call to the TruthRegistry smart contract using the `get_proof_status(byte[])(byte[],uint64,uint8[32],bool,uint64)` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    getProofStatus: async (params: CallParams<TruthRegistryArgs['obj']['get_proof_status(byte[])(byte[],uint64,uint8[32],bool,uint64)'] | TruthRegistryArgs['tuple']['get_proof_status(byte[])(byte[],uint64,uint8[32],bool,uint64)']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(TruthRegistryParamsFactory.getProofStatus(params))
      return {...result, return: result.return as unknown as (undefined | TruthRegistryReturns['get_proof_status(byte[])(byte[],uint64,uint8[32],bool,uint64)'])}
    },

    /**
     * Makes a call to the TruthRegistry smart contract using the `is_proof_verified(byte[])bool` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    isProofVerified: async (params: CallParams<TruthRegistryArgs['obj']['is_proof_verified(byte[])bool'] | TruthRegistryArgs['tuple']['is_proof_verified(byte[])bool']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(TruthRegistryParamsFactory.isProofVerified(params))
      return {...result, return: result.return as unknown as (undefined | TruthRegistryReturns['is_proof_verified(byte[])bool'])}
    },

    /**
     * Makes a call to the TruthRegistry smart contract using the `get_commitment(byte[])byte[]` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    getCommitment: async (params: CallParams<TruthRegistryArgs['obj']['get_commitment(byte[])byte[]'] | TruthRegistryArgs['tuple']['get_commitment(byte[])byte[]']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(TruthRegistryParamsFactory.getCommitment(params))
      return {...result, return: result.return as unknown as (undefined | TruthRegistryReturns['get_commitment(byte[])byte[]'])}
    },

    /**
     * Makes a call to the TruthRegistry smart contract using the `is_trait_registered(byte[])bool` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    isTraitRegistered: async (params: CallParams<TruthRegistryArgs['obj']['is_trait_registered(byte[])bool'] | TruthRegistryArgs['tuple']['is_trait_registered(byte[])bool']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(TruthRegistryParamsFactory.isTraitRegistered(params))
      return {...result, return: result.return as unknown as (undefined | TruthRegistryReturns['is_trait_registered(byte[])bool'])}
    },

    /**
     * Makes a call to the TruthRegistry smart contract using the `get_anchor_count()uint64` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    getAnchorCount: async (params: CallParams<TruthRegistryArgs['obj']['get_anchor_count()uint64'] | TruthRegistryArgs['tuple']['get_anchor_count()uint64']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(TruthRegistryParamsFactory.getAnchorCount(params))
      return {...result, return: result.return as unknown as (undefined | TruthRegistryReturns['get_anchor_count()uint64'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new TruthRegistryClient(this.appClient.clone(params))
  }

  /**
   * Makes a readonly (simulated) call to the TruthRegistry smart contract using the `get_proof_status(byte[])(byte[],uint64,uint8[32],bool,uint64)` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async getProofStatus(params: CallParams<TruthRegistryArgs['obj']['get_proof_status(byte[])(byte[],uint64,uint8[32],bool,uint64)'] | TruthRegistryArgs['tuple']['get_proof_status(byte[])(byte[],uint64,uint8[32],bool,uint64)']>) {
    const result = await this.appClient.send.call(TruthRegistryParamsFactory.getProofStatus(params))
    return result.return as unknown as TruthRegistryReturns['get_proof_status(byte[])(byte[],uint64,uint8[32],bool,uint64)']
  }

  /**
   * Makes a readonly (simulated) call to the TruthRegistry smart contract using the `is_proof_verified(byte[])bool` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async isProofVerified(params: CallParams<TruthRegistryArgs['obj']['is_proof_verified(byte[])bool'] | TruthRegistryArgs['tuple']['is_proof_verified(byte[])bool']>) {
    const result = await this.appClient.send.call(TruthRegistryParamsFactory.isProofVerified(params))
    return result.return as unknown as TruthRegistryReturns['is_proof_verified(byte[])bool']
  }

  /**
   * Makes a readonly (simulated) call to the TruthRegistry smart contract using the `get_commitment(byte[])byte[]` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async getCommitment(params: CallParams<TruthRegistryArgs['obj']['get_commitment(byte[])byte[]'] | TruthRegistryArgs['tuple']['get_commitment(byte[])byte[]']>) {
    const result = await this.appClient.send.call(TruthRegistryParamsFactory.getCommitment(params))
    return result.return as unknown as TruthRegistryReturns['get_commitment(byte[])byte[]']
  }

  /**
   * Makes a readonly (simulated) call to the TruthRegistry smart contract using the `is_trait_registered(byte[])bool` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async isTraitRegistered(params: CallParams<TruthRegistryArgs['obj']['is_trait_registered(byte[])bool'] | TruthRegistryArgs['tuple']['is_trait_registered(byte[])bool']>) {
    const result = await this.appClient.send.call(TruthRegistryParamsFactory.isTraitRegistered(params))
    return result.return as unknown as TruthRegistryReturns['is_trait_registered(byte[])bool']
  }

  /**
   * Makes a readonly (simulated) call to the TruthRegistry smart contract using the `get_anchor_count()uint64` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async getAnchorCount(params: CallParams<TruthRegistryArgs['obj']['get_anchor_count()uint64'] | TruthRegistryArgs['tuple']['get_anchor_count()uint64']> = {args: []}) {
    const result = await this.appClient.send.call(TruthRegistryParamsFactory.getAnchorCount(params))
    return result.return as unknown as TruthRegistryReturns['get_anchor_count()uint64']
  }

  /**
   * Methods to access state for the current TruthRegistry app
   */
  state = {
    /**
     * Methods to access global state for the current TruthRegistry app
     */
    global: {
      /**
       * Get all current keyed values from global state
       */
      getAll: async (): Promise<Partial<Expand<GlobalKeysState>>> => {
        const result = await this.appClient.state.global.getAll()
        return {
          identityRegistryAppId: result.identity_registry_app_id,
          anchorCount: result.anchor_count,
          verifierAppId: result.verifier_app_id,
        }
      },
      /**
       * Get the current value of the identity_registry_app_id key in global state
       */
      identityRegistryAppId: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("identity_registry_app_id")) as bigint | undefined },
      /**
       * Get the current value of the anchor_count key in global state
       */
      anchorCount: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("anchor_count")) as bigint | undefined },
      /**
       * Get the current value of the verifier_app_id key in global state
       */
      verifierAppId: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("verifier_app_id")) as bigint | undefined },
    },
    /**
     * Methods to access box state for the current TruthRegistry app
     */
    box: {
      /**
       * Get all current keyed values from box state
       */
      getAll: async (): Promise<Partial<Expand<BoxKeysState>>> => {
        const result = await this.appClient.state.box.getAll()
        return {
        }
      },
      /**
       * Get values from the anchors map in box state
       */
      anchors: {
        /**
         * Get all current values of the anchors map in box state
         */
        getMap: async (): Promise<Map<Uint8Array, Uint8Array>> => { return (await this.appClient.state.box.getMap("anchors")) as Map<Uint8Array, Uint8Array> },
        /**
         * Get a current value of the anchors map by key from box state
         */
        value: async (key: Uint8Array): Promise<Uint8Array | undefined> => { return await this.appClient.state.box.getMapValue("anchors", key) as Uint8Array | undefined },
      },
      /**
       * Get values from the proofs map in box state
       */
      proofs: {
        /**
         * Get all current values of the proofs map in box state
         */
        getMap: async (): Promise<Map<Uint8Array, ZkProofStatus>> => { return (await this.appClient.state.box.getMap("proofs")) as Map<Uint8Array, ZkProofStatus> },
        /**
         * Get a current value of the proofs map by key from box state
         */
        value: async (key: Uint8Array): Promise<ZkProofStatus | undefined> => { return await this.appClient.state.box.getMapValue("proofs", key) as ZkProofStatus | undefined },
      },
    },
  }

  public newGroup(): TruthRegistryComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a set_verifier(uint64)void method call against the TruthRegistry contract
       */
      setVerifier(params: CallParams<TruthRegistryArgs['obj']['set_verifier(uint64)void'] | TruthRegistryArgs['tuple']['set_verifier(uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.setVerifier(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a register_anchor(byte[],byte[])void method call against the TruthRegistry contract
       */
      registerAnchor(params: CallParams<TruthRegistryArgs['obj']['register_anchor(byte[],byte[])void'] | TruthRegistryArgs['tuple']['register_anchor(byte[],byte[])void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.registerAnchor(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a verify_zk_claim(byte[],byte[],uint64,uint8[32])bool method call against the TruthRegistry contract
       */
      verifyZkClaim(params: CallParams<TruthRegistryArgs['obj']['verify_zk_claim(byte[],byte[],uint64,uint8[32])bool'] | TruthRegistryArgs['tuple']['verify_zk_claim(byte[],byte[],uint64,uint8[32])bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.verifyZkClaim(params)))
        resultMappers.push((v) => client.decodeReturnValue('verify_zk_claim(byte[],byte[],uint64,uint8[32])bool', v))
        return this
      },
      /**
       * Add a get_proof_status(byte[])(byte[],uint64,uint8[32],bool,uint64) method call against the TruthRegistry contract
       */
      getProofStatus(params: CallParams<TruthRegistryArgs['obj']['get_proof_status(byte[])(byte[],uint64,uint8[32],bool,uint64)'] | TruthRegistryArgs['tuple']['get_proof_status(byte[])(byte[],uint64,uint8[32],bool,uint64)']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.getProofStatus(params)))
        resultMappers.push((v) => client.decodeReturnValue('get_proof_status(byte[])(byte[],uint64,uint8[32],bool,uint64)', v))
        return this
      },
      /**
       * Add a is_proof_verified(byte[])bool method call against the TruthRegistry contract
       */
      isProofVerified(params: CallParams<TruthRegistryArgs['obj']['is_proof_verified(byte[])bool'] | TruthRegistryArgs['tuple']['is_proof_verified(byte[])bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.isProofVerified(params)))
        resultMappers.push((v) => client.decodeReturnValue('is_proof_verified(byte[])bool', v))
        return this
      },
      /**
       * Add a get_commitment(byte[])byte[] method call against the TruthRegistry contract
       */
      getCommitment(params: CallParams<TruthRegistryArgs['obj']['get_commitment(byte[])byte[]'] | TruthRegistryArgs['tuple']['get_commitment(byte[])byte[]']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.getCommitment(params)))
        resultMappers.push((v) => client.decodeReturnValue('get_commitment(byte[])byte[]', v))
        return this
      },
      /**
       * Add a is_trait_registered(byte[])bool method call against the TruthRegistry contract
       */
      isTraitRegistered(params: CallParams<TruthRegistryArgs['obj']['is_trait_registered(byte[])bool'] | TruthRegistryArgs['tuple']['is_trait_registered(byte[])bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.isTraitRegistered(params)))
        resultMappers.push((v) => client.decodeReturnValue('is_trait_registered(byte[])bool', v))
        return this
      },
      /**
       * Add a get_anchor_count()uint64 method call against the TruthRegistry contract
       */
      getAnchorCount(params: CallParams<TruthRegistryArgs['obj']['get_anchor_count()uint64'] | TruthRegistryArgs['tuple']['get_anchor_count()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.getAnchorCount(params)))
        resultMappers.push((v) => client.decodeReturnValue('get_anchor_count()uint64', v))
        return this
      },
      /**
       * Add a clear state call to the TruthRegistry contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as TruthRegistryComposer
  }
}
export type TruthRegistryComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the set_verifier(uint64)void ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  setVerifier(params?: CallParams<TruthRegistryArgs['obj']['set_verifier(uint64)void'] | TruthRegistryArgs['tuple']['set_verifier(uint64)void']>): TruthRegistryComposer<[...TReturns, TruthRegistryReturns['set_verifier(uint64)void'] | undefined]>

  /**
   * Calls the register_anchor(byte[],byte[])void ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  registerAnchor(params?: CallParams<TruthRegistryArgs['obj']['register_anchor(byte[],byte[])void'] | TruthRegistryArgs['tuple']['register_anchor(byte[],byte[])void']>): TruthRegistryComposer<[...TReturns, TruthRegistryReturns['register_anchor(byte[],byte[])void'] | undefined]>

  /**
   * Calls the verify_zk_claim(byte[],byte[],uint64,uint8[32])bool ABI method.
   *
  * Verify and settle a ZK claim.
  The claim is verified off-chain by the institution and then anchored here.

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  verifyZkClaim(params?: CallParams<TruthRegistryArgs['obj']['verify_zk_claim(byte[],byte[],uint64,uint8[32])bool'] | TruthRegistryArgs['tuple']['verify_zk_claim(byte[],byte[],uint64,uint8[32])bool']>): TruthRegistryComposer<[...TReturns, TruthRegistryReturns['verify_zk_claim(byte[],byte[],uint64,uint8[32])bool'] | undefined]>

  /**
   * Calls the get_proof_status(byte[])(byte[],uint64,uint8[32],bool,uint64) ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  getProofStatus(params?: CallParams<TruthRegistryArgs['obj']['get_proof_status(byte[])(byte[],uint64,uint8[32],bool,uint64)'] | TruthRegistryArgs['tuple']['get_proof_status(byte[])(byte[],uint64,uint8[32],bool,uint64)']>): TruthRegistryComposer<[...TReturns, TruthRegistryReturns['get_proof_status(byte[])(byte[],uint64,uint8[32],bool,uint64)'] | undefined]>

  /**
   * Calls the is_proof_verified(byte[])bool ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  isProofVerified(params?: CallParams<TruthRegistryArgs['obj']['is_proof_verified(byte[])bool'] | TruthRegistryArgs['tuple']['is_proof_verified(byte[])bool']>): TruthRegistryComposer<[...TReturns, TruthRegistryReturns['is_proof_verified(byte[])bool'] | undefined]>

  /**
   * Calls the get_commitment(byte[])byte[] ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  getCommitment(params?: CallParams<TruthRegistryArgs['obj']['get_commitment(byte[])byte[]'] | TruthRegistryArgs['tuple']['get_commitment(byte[])byte[]']>): TruthRegistryComposer<[...TReturns, TruthRegistryReturns['get_commitment(byte[])byte[]'] | undefined]>

  /**
   * Calls the is_trait_registered(byte[])bool ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  isTraitRegistered(params?: CallParams<TruthRegistryArgs['obj']['is_trait_registered(byte[])bool'] | TruthRegistryArgs['tuple']['is_trait_registered(byte[])bool']>): TruthRegistryComposer<[...TReturns, TruthRegistryReturns['is_trait_registered(byte[])bool'] | undefined]>

  /**
   * Calls the get_anchor_count()uint64 ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  getAnchorCount(params?: CallParams<TruthRegistryArgs['obj']['get_anchor_count()uint64'] | TruthRegistryArgs['tuple']['get_anchor_count()uint64']>): TruthRegistryComposer<[...TReturns, TruthRegistryReturns['get_anchor_count()uint64'] | undefined]>

  /**
   * Makes a clear_state call to an existing instance of the TruthRegistry smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): TruthRegistryComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): TruthRegistryComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<TruthRegistryComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<TruthRegistryComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<TruthRegistryComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<TruthRegistryComposerResults<TReturns>>
}
export type TruthRegistryComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

