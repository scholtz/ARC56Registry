/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"DocumentVerification","structs":{},"methods":[{"name":"register_document","args":[{"type":"string","name":"document_hash"},{"type":"string","name":"candidate_address"},{"type":"string","name":"org_name"}],"returns":{"type":"bool"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Register a document on-chain.\nStores: document_hash -> candidate_address|issuer|timestamp|org_name","events":[],"recommendations":{}},{"name":"verify_document","args":[{"type":"string","name":"document_hash"},{"type":"string","name":"candidate_address"}],"returns":{"type":"string"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"desc":"Verify that this document belongs to this candidate.\nReturns: VALID | NOT_FOUND | REVOKED | MISMATCH","events":[],"recommendations":{}},{"name":"get_document_details","args":[{"type":"string","name":"document_hash"}],"returns":{"type":"byte[]"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"desc":"Return raw packed bytes — parse off-chain.","events":[],"recommendations":{}},{"name":"document_exists","args":[{"type":"string","name":"document_hash"}],"returns":{"type":"bool"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"revoke_document","args":[{"type":"string","name":"document_hash"},{"type":"string","name":"reason"}],"returns":{"type":"bool"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Only the original issuer can revoke.","events":[],"recommendations":{}},{"name":"is_revoked","args":[{"type":"string","name":"document_hash"}],"returns":{"type":"bool"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}}],"arcs":[22,28],"desc":"\n    Document Verification Contract\n    Stores: document_hash -> candidate_address\n    Any org can register docs; anyone can verify.\n    ","networks":{},"state":{"schema":{"global":{"ints":0,"bytes":0},"local":{"ints":0,"bytes":0}},"keys":{"global":{},"local":{},"box":{}},"maps":{"global":{},"local":{},"box":{"documents":{"keyType":"AVMBytes","valueType":"AVMBytes","prefix":"ZG9j"},"revoked":{"keyType":"AVMBytes","valueType":"AVMBytes","prefix":"cmV2"}}}},"bareActions":{"create":["NoOp"],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[453],"errorMessage":"Already revoked"},{"pc":[161],"errorMessage":"Document already registered"},{"pc":[359,444],"errorMessage":"Document not found"},{"pc":[170],"errorMessage":"Document was previously revoked"},{"pc":[115,128,141,208,224,343,381,414,427,482],"errorMessage":"invalid array length header"},{"pc":[122,135,148,215,230,350,388,421,434,489],"errorMessage":"invalid number of bytes for arc4.dynamic_array<arc4.uint8>"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"events":[]} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


/**
 * The argument types for the DocumentVerification contract
 */
export type DocumentVerificationArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'register_document(string,string,string)bool': {
      documentHash: string
      candidateAddress: string
      orgName: string
    }
    'verify_document(string,string)string': {
      documentHash: string
      candidateAddress: string
    }
    'get_document_details(string)byte[]': {
      documentHash: string
    }
    'document_exists(string)bool': {
      documentHash: string
    }
    'revoke_document(string,string)bool': {
      documentHash: string
      reason: string
    }
    'is_revoked(string)bool': {
      documentHash: string
    }
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'register_document(string,string,string)bool': [documentHash: string, candidateAddress: string, orgName: string]
    'verify_document(string,string)string': [documentHash: string, candidateAddress: string]
    'get_document_details(string)byte[]': [documentHash: string]
    'document_exists(string)bool': [documentHash: string]
    'revoke_document(string,string)bool': [documentHash: string, reason: string]
    'is_revoked(string)bool': [documentHash: string]
  }
}

/**
 * The return type for each method
 */
export type DocumentVerificationReturns = {
  'register_document(string,string,string)bool': boolean
  'verify_document(string,string)string': string
  'get_document_details(string)byte[]': Uint8Array
  'document_exists(string)bool': boolean
  'revoke_document(string,string)bool': boolean
  'is_revoked(string)bool': boolean
}

/**
 * Defines the types of available calls and state of the DocumentVerification smart contract.
 */
export type DocumentVerificationTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'register_document(string,string,string)bool' | 'register_document', {
      argsObj: DocumentVerificationArgs['obj']['register_document(string,string,string)bool']
      argsTuple: DocumentVerificationArgs['tuple']['register_document(string,string,string)bool']
      returns: DocumentVerificationReturns['register_document(string,string,string)bool']
    }>
    & Record<'verify_document(string,string)string' | 'verify_document', {
      argsObj: DocumentVerificationArgs['obj']['verify_document(string,string)string']
      argsTuple: DocumentVerificationArgs['tuple']['verify_document(string,string)string']
      returns: DocumentVerificationReturns['verify_document(string,string)string']
    }>
    & Record<'get_document_details(string)byte[]' | 'get_document_details', {
      argsObj: DocumentVerificationArgs['obj']['get_document_details(string)byte[]']
      argsTuple: DocumentVerificationArgs['tuple']['get_document_details(string)byte[]']
      returns: DocumentVerificationReturns['get_document_details(string)byte[]']
    }>
    & Record<'document_exists(string)bool' | 'document_exists', {
      argsObj: DocumentVerificationArgs['obj']['document_exists(string)bool']
      argsTuple: DocumentVerificationArgs['tuple']['document_exists(string)bool']
      returns: DocumentVerificationReturns['document_exists(string)bool']
    }>
    & Record<'revoke_document(string,string)bool' | 'revoke_document', {
      argsObj: DocumentVerificationArgs['obj']['revoke_document(string,string)bool']
      argsTuple: DocumentVerificationArgs['tuple']['revoke_document(string,string)bool']
      returns: DocumentVerificationReturns['revoke_document(string,string)bool']
    }>
    & Record<'is_revoked(string)bool' | 'is_revoked', {
      argsObj: DocumentVerificationArgs['obj']['is_revoked(string)bool']
      argsTuple: DocumentVerificationArgs['tuple']['is_revoked(string)bool']
      returns: DocumentVerificationReturns['is_revoked(string)bool']
    }>
  /**
   * Defines the shape of the state of the application.
   */
  state: {
    box: {
      keys: {}
      maps: {
        documents: Map<Uint8Array | string, Uint8Array>
        revoked: Map<Uint8Array | string, Uint8Array>
      }
    }
  }
}

/**
 * Defines the possible abi call signatures.
 */
export type DocumentVerificationSignatures = keyof DocumentVerificationTypes['methods']
/**
 * Defines the possible abi call signatures for methods that return a non-void value.
 */
export type DocumentVerificationNonVoidMethodSignatures = keyof DocumentVerificationTypes['methods'] extends infer T ? T extends keyof DocumentVerificationTypes['methods'] ? MethodReturn<T> extends void ? never : T  : never : never
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the DocumentVerification smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends DocumentVerificationSignatures> = DocumentVerificationTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the DocumentVerification smart contract to the method's return type
 */
export type MethodReturn<TSignature extends DocumentVerificationSignatures> = DocumentVerificationTypes['methods'][TSignature]['returns']

/**
 * Defines the shape of the keyed box state of the application.
 */
export type BoxKeysState = DocumentVerificationTypes['state']['box']['keys']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the DocumentVerification smart contract
 */
export abstract class DocumentVerificationParamsFactory {
  /**
   * Constructs a no op call for the register_document(string,string,string)bool ABI method
   *
  * Register a document on-chain.
  Stores: document_hash -> candidate_address|issuer|timestamp|org_name

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static registerDocument(params: CallParams<DocumentVerificationArgs['obj']['register_document(string,string,string)bool'] | DocumentVerificationArgs['tuple']['register_document(string,string,string)bool']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'register_document(string,string,string)bool' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.documentHash, params.args.candidateAddress, params.args.orgName],
    }
  }
  /**
   * Constructs a no op call for the verify_document(string,string)string ABI method
   *
  * Verify that this document belongs to this candidate.
  Returns: VALID | NOT_FOUND | REVOKED | MISMATCH

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static verifyDocument(params: CallParams<DocumentVerificationArgs['obj']['verify_document(string,string)string'] | DocumentVerificationArgs['tuple']['verify_document(string,string)string']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'verify_document(string,string)string' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.documentHash, params.args.candidateAddress],
    }
  }
  /**
   * Constructs a no op call for the get_document_details(string)byte[] ABI method
   *
   * Return raw packed bytes — parse off-chain.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static getDocumentDetails(params: CallParams<DocumentVerificationArgs['obj']['get_document_details(string)byte[]'] | DocumentVerificationArgs['tuple']['get_document_details(string)byte[]']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'get_document_details(string)byte[]' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.documentHash],
    }
  }
  /**
   * Constructs a no op call for the document_exists(string)bool ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static documentExists(params: CallParams<DocumentVerificationArgs['obj']['document_exists(string)bool'] | DocumentVerificationArgs['tuple']['document_exists(string)bool']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'document_exists(string)bool' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.documentHash],
    }
  }
  /**
   * Constructs a no op call for the revoke_document(string,string)bool ABI method
   *
   * Only the original issuer can revoke.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static revokeDocument(params: CallParams<DocumentVerificationArgs['obj']['revoke_document(string,string)bool'] | DocumentVerificationArgs['tuple']['revoke_document(string,string)bool']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'revoke_document(string,string)bool' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.documentHash, params.args.reason],
    }
  }
  /**
   * Constructs a no op call for the is_revoked(string)bool ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static isRevoked(params: CallParams<DocumentVerificationArgs['obj']['is_revoked(string)bool'] | DocumentVerificationArgs['tuple']['is_revoked(string)bool']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'is_revoked(string)bool' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.documentHash],
    }
  }
}

/**
 * A client to make calls to the DocumentVerification smart contract
 */
export class DocumentVerificationClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `DocumentVerificationClient`
   *
   * @param appClient An `AppClient` instance which has been created with the DocumentVerification app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `DocumentVerificationClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Checks for decode errors on the given return value and maps the return value to the return type for the given method
   * @returns The typed return value or undefined if there was no value
   */
  decodeReturnValue<TSignature extends DocumentVerificationNonVoidMethodSignatures>(method: TSignature, returnValue: ABIReturn | undefined) {
    return returnValue !== undefined ? getArc56ReturnValue<MethodReturn<TSignature>>(returnValue, this.appClient.getABIMethod(method), APP_SPEC.structs) : undefined
  }

  /**
   * Returns a new `DocumentVerificationClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<DocumentVerificationClient> {
    return new DocumentVerificationClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `DocumentVerificationClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<DocumentVerificationClient> {
    return new DocumentVerificationClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Makes a clear_state call to an existing instance of the DocumentVerification smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the DocumentVerification smart contract using the `register_document(string,string,string)bool` ABI method.
     *
    * Register a document on-chain.
    Stores: document_hash -> candidate_address|issuer|timestamp|org_name

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    registerDocument: (params: CallParams<DocumentVerificationArgs['obj']['register_document(string,string,string)bool'] | DocumentVerificationArgs['tuple']['register_document(string,string,string)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(DocumentVerificationParamsFactory.registerDocument(params))
    },

    /**
     * Makes a call to the DocumentVerification smart contract using the `verify_document(string,string)string` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
    * Verify that this document belongs to this candidate.
    Returns: VALID | NOT_FOUND | REVOKED | MISMATCH

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    verifyDocument: (params: CallParams<DocumentVerificationArgs['obj']['verify_document(string,string)string'] | DocumentVerificationArgs['tuple']['verify_document(string,string)string']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(DocumentVerificationParamsFactory.verifyDocument(params))
    },

    /**
     * Makes a call to the DocumentVerification smart contract using the `get_document_details(string)byte[]` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * Return raw packed bytes — parse off-chain.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    getDocumentDetails: (params: CallParams<DocumentVerificationArgs['obj']['get_document_details(string)byte[]'] | DocumentVerificationArgs['tuple']['get_document_details(string)byte[]']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(DocumentVerificationParamsFactory.getDocumentDetails(params))
    },

    /**
     * Makes a call to the DocumentVerification smart contract using the `document_exists(string)bool` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    documentExists: (params: CallParams<DocumentVerificationArgs['obj']['document_exists(string)bool'] | DocumentVerificationArgs['tuple']['document_exists(string)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(DocumentVerificationParamsFactory.documentExists(params))
    },

    /**
     * Makes a call to the DocumentVerification smart contract using the `revoke_document(string,string)bool` ABI method.
     *
     * Only the original issuer can revoke.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    revokeDocument: (params: CallParams<DocumentVerificationArgs['obj']['revoke_document(string,string)bool'] | DocumentVerificationArgs['tuple']['revoke_document(string,string)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(DocumentVerificationParamsFactory.revokeDocument(params))
    },

    /**
     * Makes a call to the DocumentVerification smart contract using the `is_revoked(string)bool` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    isRevoked: (params: CallParams<DocumentVerificationArgs['obj']['is_revoked(string)bool'] | DocumentVerificationArgs['tuple']['is_revoked(string)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(DocumentVerificationParamsFactory.isRevoked(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Makes a clear_state call to an existing instance of the DocumentVerification smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the DocumentVerification smart contract using the `register_document(string,string,string)bool` ABI method.
     *
    * Register a document on-chain.
    Stores: document_hash -> candidate_address|issuer|timestamp|org_name

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    registerDocument: (params: CallParams<DocumentVerificationArgs['obj']['register_document(string,string,string)bool'] | DocumentVerificationArgs['tuple']['register_document(string,string,string)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(DocumentVerificationParamsFactory.registerDocument(params))
    },

    /**
     * Makes a call to the DocumentVerification smart contract using the `verify_document(string,string)string` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
    * Verify that this document belongs to this candidate.
    Returns: VALID | NOT_FOUND | REVOKED | MISMATCH

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    verifyDocument: (params: CallParams<DocumentVerificationArgs['obj']['verify_document(string,string)string'] | DocumentVerificationArgs['tuple']['verify_document(string,string)string']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(DocumentVerificationParamsFactory.verifyDocument(params))
    },

    /**
     * Makes a call to the DocumentVerification smart contract using the `get_document_details(string)byte[]` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * Return raw packed bytes — parse off-chain.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    getDocumentDetails: (params: CallParams<DocumentVerificationArgs['obj']['get_document_details(string)byte[]'] | DocumentVerificationArgs['tuple']['get_document_details(string)byte[]']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(DocumentVerificationParamsFactory.getDocumentDetails(params))
    },

    /**
     * Makes a call to the DocumentVerification smart contract using the `document_exists(string)bool` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    documentExists: (params: CallParams<DocumentVerificationArgs['obj']['document_exists(string)bool'] | DocumentVerificationArgs['tuple']['document_exists(string)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(DocumentVerificationParamsFactory.documentExists(params))
    },

    /**
     * Makes a call to the DocumentVerification smart contract using the `revoke_document(string,string)bool` ABI method.
     *
     * Only the original issuer can revoke.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    revokeDocument: (params: CallParams<DocumentVerificationArgs['obj']['revoke_document(string,string)bool'] | DocumentVerificationArgs['tuple']['revoke_document(string,string)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(DocumentVerificationParamsFactory.revokeDocument(params))
    },

    /**
     * Makes a call to the DocumentVerification smart contract using the `is_revoked(string)bool` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    isRevoked: (params: CallParams<DocumentVerificationArgs['obj']['is_revoked(string)bool'] | DocumentVerificationArgs['tuple']['is_revoked(string)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(DocumentVerificationParamsFactory.isRevoked(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Makes a clear_state call to an existing instance of the DocumentVerification smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the DocumentVerification smart contract using the `register_document(string,string,string)bool` ABI method.
     *
    * Register a document on-chain.
    Stores: document_hash -> candidate_address|issuer|timestamp|org_name

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    registerDocument: async (params: CallParams<DocumentVerificationArgs['obj']['register_document(string,string,string)bool'] | DocumentVerificationArgs['tuple']['register_document(string,string,string)bool']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(DocumentVerificationParamsFactory.registerDocument(params))
      return {...result, return: result.return as unknown as (undefined | DocumentVerificationReturns['register_document(string,string,string)bool'])}
    },

    /**
     * Makes a call to the DocumentVerification smart contract using the `verify_document(string,string)string` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
    * Verify that this document belongs to this candidate.
    Returns: VALID | NOT_FOUND | REVOKED | MISMATCH

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    verifyDocument: async (params: CallParams<DocumentVerificationArgs['obj']['verify_document(string,string)string'] | DocumentVerificationArgs['tuple']['verify_document(string,string)string']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(DocumentVerificationParamsFactory.verifyDocument(params))
      return {...result, return: result.return as unknown as (undefined | DocumentVerificationReturns['verify_document(string,string)string'])}
    },

    /**
     * Makes a call to the DocumentVerification smart contract using the `get_document_details(string)byte[]` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * Return raw packed bytes — parse off-chain.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    getDocumentDetails: async (params: CallParams<DocumentVerificationArgs['obj']['get_document_details(string)byte[]'] | DocumentVerificationArgs['tuple']['get_document_details(string)byte[]']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(DocumentVerificationParamsFactory.getDocumentDetails(params))
      return {...result, return: result.return as unknown as (undefined | DocumentVerificationReturns['get_document_details(string)byte[]'])}
    },

    /**
     * Makes a call to the DocumentVerification smart contract using the `document_exists(string)bool` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    documentExists: async (params: CallParams<DocumentVerificationArgs['obj']['document_exists(string)bool'] | DocumentVerificationArgs['tuple']['document_exists(string)bool']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(DocumentVerificationParamsFactory.documentExists(params))
      return {...result, return: result.return as unknown as (undefined | DocumentVerificationReturns['document_exists(string)bool'])}
    },

    /**
     * Makes a call to the DocumentVerification smart contract using the `revoke_document(string,string)bool` ABI method.
     *
     * Only the original issuer can revoke.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    revokeDocument: async (params: CallParams<DocumentVerificationArgs['obj']['revoke_document(string,string)bool'] | DocumentVerificationArgs['tuple']['revoke_document(string,string)bool']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(DocumentVerificationParamsFactory.revokeDocument(params))
      return {...result, return: result.return as unknown as (undefined | DocumentVerificationReturns['revoke_document(string,string)bool'])}
    },

    /**
     * Makes a call to the DocumentVerification smart contract using the `is_revoked(string)bool` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    isRevoked: async (params: CallParams<DocumentVerificationArgs['obj']['is_revoked(string)bool'] | DocumentVerificationArgs['tuple']['is_revoked(string)bool']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(DocumentVerificationParamsFactory.isRevoked(params))
      return {...result, return: result.return as unknown as (undefined | DocumentVerificationReturns['is_revoked(string)bool'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new DocumentVerificationClient(this.appClient.clone(params))
  }

  /**
   * Makes a readonly (simulated) call to the DocumentVerification smart contract using the `verify_document(string,string)string` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
  * Verify that this document belongs to this candidate.
  Returns: VALID | NOT_FOUND | REVOKED | MISMATCH

   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async verifyDocument(params: CallParams<DocumentVerificationArgs['obj']['verify_document(string,string)string'] | DocumentVerificationArgs['tuple']['verify_document(string,string)string']>) {
    const result = await this.appClient.send.call(DocumentVerificationParamsFactory.verifyDocument(params))
    return result.return as unknown as DocumentVerificationReturns['verify_document(string,string)string']
  }

  /**
   * Makes a readonly (simulated) call to the DocumentVerification smart contract using the `get_document_details(string)byte[]` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * Return raw packed bytes — parse off-chain.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async getDocumentDetails(params: CallParams<DocumentVerificationArgs['obj']['get_document_details(string)byte[]'] | DocumentVerificationArgs['tuple']['get_document_details(string)byte[]']>) {
    const result = await this.appClient.send.call(DocumentVerificationParamsFactory.getDocumentDetails(params))
    return result.return as unknown as DocumentVerificationReturns['get_document_details(string)byte[]']
  }

  /**
   * Makes a readonly (simulated) call to the DocumentVerification smart contract using the `document_exists(string)bool` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async documentExists(params: CallParams<DocumentVerificationArgs['obj']['document_exists(string)bool'] | DocumentVerificationArgs['tuple']['document_exists(string)bool']>) {
    const result = await this.appClient.send.call(DocumentVerificationParamsFactory.documentExists(params))
    return result.return as unknown as DocumentVerificationReturns['document_exists(string)bool']
  }

  /**
   * Makes a readonly (simulated) call to the DocumentVerification smart contract using the `is_revoked(string)bool` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async isRevoked(params: CallParams<DocumentVerificationArgs['obj']['is_revoked(string)bool'] | DocumentVerificationArgs['tuple']['is_revoked(string)bool']>) {
    const result = await this.appClient.send.call(DocumentVerificationParamsFactory.isRevoked(params))
    return result.return as unknown as DocumentVerificationReturns['is_revoked(string)bool']
  }

  /**
   * Methods to access state for the current DocumentVerification app
   */
  state = {
    /**
     * Methods to access box state for the current DocumentVerification app
     */
    box: {
      /**
       * Get all current keyed values from box state
       */
      getAll: async (): Promise<Partial<Expand<BoxKeysState>>> => {
        const result = await this.appClient.state.box.getAll()
        return {
        }
      },
      /**
       * Get values from the documents map in box state
       */
      documents: {
        /**
         * Get all current values of the documents map in box state
         */
        getMap: async (): Promise<Map<Uint8Array, Uint8Array>> => { return (await this.appClient.state.box.getMap("documents")) as Map<Uint8Array, Uint8Array> },
        /**
         * Get a current value of the documents map by key from box state
         */
        value: async (key: Uint8Array | string): Promise<Uint8Array | undefined> => { return await this.appClient.state.box.getMapValue("documents", key) as Uint8Array | undefined },
      },
      /**
       * Get values from the revoked map in box state
       */
      revoked: {
        /**
         * Get all current values of the revoked map in box state
         */
        getMap: async (): Promise<Map<Uint8Array, Uint8Array>> => { return (await this.appClient.state.box.getMap("revoked")) as Map<Uint8Array, Uint8Array> },
        /**
         * Get a current value of the revoked map by key from box state
         */
        value: async (key: Uint8Array | string): Promise<Uint8Array | undefined> => { return await this.appClient.state.box.getMapValue("revoked", key) as Uint8Array | undefined },
      },
    },
  }

  public newGroup(): DocumentVerificationComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a register_document(string,string,string)bool method call against the DocumentVerification contract
       */
      registerDocument(params: CallParams<DocumentVerificationArgs['obj']['register_document(string,string,string)bool'] | DocumentVerificationArgs['tuple']['register_document(string,string,string)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.registerDocument(params)))
        resultMappers.push((v) => client.decodeReturnValue('register_document(string,string,string)bool', v))
        return this
      },
      /**
       * Add a verify_document(string,string)string method call against the DocumentVerification contract
       */
      verifyDocument(params: CallParams<DocumentVerificationArgs['obj']['verify_document(string,string)string'] | DocumentVerificationArgs['tuple']['verify_document(string,string)string']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.verifyDocument(params)))
        resultMappers.push((v) => client.decodeReturnValue('verify_document(string,string)string', v))
        return this
      },
      /**
       * Add a get_document_details(string)byte[] method call against the DocumentVerification contract
       */
      getDocumentDetails(params: CallParams<DocumentVerificationArgs['obj']['get_document_details(string)byte[]'] | DocumentVerificationArgs['tuple']['get_document_details(string)byte[]']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.getDocumentDetails(params)))
        resultMappers.push((v) => client.decodeReturnValue('get_document_details(string)byte[]', v))
        return this
      },
      /**
       * Add a document_exists(string)bool method call against the DocumentVerification contract
       */
      documentExists(params: CallParams<DocumentVerificationArgs['obj']['document_exists(string)bool'] | DocumentVerificationArgs['tuple']['document_exists(string)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.documentExists(params)))
        resultMappers.push((v) => client.decodeReturnValue('document_exists(string)bool', v))
        return this
      },
      /**
       * Add a revoke_document(string,string)bool method call against the DocumentVerification contract
       */
      revokeDocument(params: CallParams<DocumentVerificationArgs['obj']['revoke_document(string,string)bool'] | DocumentVerificationArgs['tuple']['revoke_document(string,string)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.revokeDocument(params)))
        resultMappers.push((v) => client.decodeReturnValue('revoke_document(string,string)bool', v))
        return this
      },
      /**
       * Add a is_revoked(string)bool method call against the DocumentVerification contract
       */
      isRevoked(params: CallParams<DocumentVerificationArgs['obj']['is_revoked(string)bool'] | DocumentVerificationArgs['tuple']['is_revoked(string)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.isRevoked(params)))
        resultMappers.push((v) => client.decodeReturnValue('is_revoked(string)bool', v))
        return this
      },
      /**
       * Add a clear state call to the DocumentVerification contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as DocumentVerificationComposer
  }
}
export type DocumentVerificationComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the register_document(string,string,string)bool ABI method.
   *
  * Register a document on-chain.
  Stores: document_hash -> candidate_address|issuer|timestamp|org_name

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  registerDocument(params?: CallParams<DocumentVerificationArgs['obj']['register_document(string,string,string)bool'] | DocumentVerificationArgs['tuple']['register_document(string,string,string)bool']>): DocumentVerificationComposer<[...TReturns, DocumentVerificationReturns['register_document(string,string,string)bool'] | undefined]>

  /**
   * Calls the verify_document(string,string)string ABI method.
   *
  * Verify that this document belongs to this candidate.
  Returns: VALID | NOT_FOUND | REVOKED | MISMATCH

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  verifyDocument(params?: CallParams<DocumentVerificationArgs['obj']['verify_document(string,string)string'] | DocumentVerificationArgs['tuple']['verify_document(string,string)string']>): DocumentVerificationComposer<[...TReturns, DocumentVerificationReturns['verify_document(string,string)string'] | undefined]>

  /**
   * Calls the get_document_details(string)byte[] ABI method.
   *
   * Return raw packed bytes — parse off-chain.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  getDocumentDetails(params?: CallParams<DocumentVerificationArgs['obj']['get_document_details(string)byte[]'] | DocumentVerificationArgs['tuple']['get_document_details(string)byte[]']>): DocumentVerificationComposer<[...TReturns, DocumentVerificationReturns['get_document_details(string)byte[]'] | undefined]>

  /**
   * Calls the document_exists(string)bool ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  documentExists(params?: CallParams<DocumentVerificationArgs['obj']['document_exists(string)bool'] | DocumentVerificationArgs['tuple']['document_exists(string)bool']>): DocumentVerificationComposer<[...TReturns, DocumentVerificationReturns['document_exists(string)bool'] | undefined]>

  /**
   * Calls the revoke_document(string,string)bool ABI method.
   *
   * Only the original issuer can revoke.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  revokeDocument(params?: CallParams<DocumentVerificationArgs['obj']['revoke_document(string,string)bool'] | DocumentVerificationArgs['tuple']['revoke_document(string,string)bool']>): DocumentVerificationComposer<[...TReturns, DocumentVerificationReturns['revoke_document(string,string)bool'] | undefined]>

  /**
   * Calls the is_revoked(string)bool ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  isRevoked(params?: CallParams<DocumentVerificationArgs['obj']['is_revoked(string)bool'] | DocumentVerificationArgs['tuple']['is_revoked(string)bool']>): DocumentVerificationComposer<[...TReturns, DocumentVerificationReturns['is_revoked(string)bool'] | undefined]>

  /**
   * Makes a clear_state call to an existing instance of the DocumentVerification smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): DocumentVerificationComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): DocumentVerificationComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<DocumentVerificationComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<DocumentVerificationComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<DocumentVerificationComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<DocumentVerificationComposerResults<TReturns>>
}
export type DocumentVerificationComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

