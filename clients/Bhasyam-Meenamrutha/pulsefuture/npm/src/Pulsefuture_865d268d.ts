/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"Pulsefuture","structs":{"Bill":[{"name":"userAddress","type":"address"},{"name":"receiverAddress","type":"address"},{"name":"amount","type":"uint64"},{"name":"dueDate","type":"uint64"},{"name":"status","type":"uint8"},{"name":"description","type":"string"}]},"methods":[{"name":"get_locked_funds","args":[{"type":"address","name":"user_address"}],"returns":{"type":"uint64"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Get the locked funds for a single user","events":[],"recommendations":{}},{"name":"lock_funds","args":[{"type":"address","name":"user_address"},{"type":"uint64","name":"amount"}],"returns":{"type":"string"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Lock funds for a user (store the locked amount per address)","events":[],"recommendations":{}},{"name":"hello","args":[{"type":"string","name":"name"}],"returns":{"type":"string"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}},{"name":"store_bill","args":[{"type":"account","name":"user_address"},{"type":"string","name":"description"},{"type":"uint64","name":"amount"},{"type":"account","name":"payee_address"},{"type":"uint64","name":"due_date"}],"returns":{"type":"uint64"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Store a bill for a user (multiple bills per user)","events":[],"recommendations":{}},{"name":"mark_bill_paid","args":[{"type":"uint64","name":"bill_id"}],"returns":{"type":"string"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Mark a bill as paid (just a stub for now)","events":[],"recommendations":{}},{"name":"get_bill_details","args":[{"type":"uint64","name":"bill_id"}],"returns":{"type":"(address,address,uint64,uint64,uint8,string)","struct":"Bill"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Get specific bill details for a user and bill ID","events":[],"recommendations":{}}],"arcs":[22,28],"networks":{},"state":{"schema":{"global":{"ints":1,"bytes":0},"local":{"ints":0,"bytes":0}},"keys":{"global":{"total_bills_count":{"keyType":"AVMString","valueType":"AVMUint64","key":"dG90YWxfYmlsbHNfY291bnQ="}},"local":{},"box":{}},"maps":{"global":{},"local":{},"box":{"bills":{"keyType":"uint64","valueType":"Bill","prefix":"YmlsbHM="},"locked_funds":{"keyType":"address","valueType":"uint64","prefix":"bG9ja2VkX2Z1bmRz"}}}},"bareActions":{"create":["NoOp"],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[326],"errorMessage":"Amount must be greater than zero"},{"pc":[480,542],"errorMessage":"Bill ID does not exist"},{"pc":[125,145,173,216,246,268],"errorMessage":"OnCompletion is not NoOp"},{"pc":[292],"errorMessage":"can only call when creating"},{"pc":[128,148,176,219,249,271],"errorMessage":"can only call when not creating"},{"pc":[483,544],"errorMessage":"check self.bills entry exists"},{"pc":[313,342],"errorMessage":"check self.locked_funds entry exists"},{"pc":[410],"errorMessage":"check self.total_bills_count exists"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"events":[]} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


// Type definitions for ARC-56 structs

export type Bill = {
  userAddress: string,
  receiverAddress: string,
  amount: bigint,
  dueDate: bigint,
  status: number,
  description: string
}


/**
 * Converts the ABI tuple representation of a Bill to the struct representation
 */
export function BillFromTuple(abiTuple: [string, string, bigint, bigint, number, string]) {
  return getABIStructFromABITuple(abiTuple, APP_SPEC.structs.Bill, APP_SPEC.structs) as Bill
}

/**
 * The argument types for the Pulsefuture contract
 */
export type PulsefutureArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'get_locked_funds(address)uint64': {
      userAddress: string
    }
    'lock_funds(address,uint64)string': {
      userAddress: string
      amount: bigint | number
    }
    'hello(string)string': {
      name: string
    }
    'store_bill(account,string,uint64,account,uint64)uint64': {
      userAddress: Uint8Array | string
      description: string
      amount: bigint | number
      payeeAddress: Uint8Array | string
      dueDate: bigint | number
    }
    'mark_bill_paid(uint64)string': {
      billId: bigint | number
    }
    'get_bill_details(uint64)(address,address,uint64,uint64,uint8,string)': {
      billId: bigint | number
    }
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'get_locked_funds(address)uint64': [userAddress: string]
    'lock_funds(address,uint64)string': [userAddress: string, amount: bigint | number]
    'hello(string)string': [name: string]
    'store_bill(account,string,uint64,account,uint64)uint64': [userAddress: Uint8Array | string, description: string, amount: bigint | number, payeeAddress: Uint8Array | string, dueDate: bigint | number]
    'mark_bill_paid(uint64)string': [billId: bigint | number]
    'get_bill_details(uint64)(address,address,uint64,uint64,uint8,string)': [billId: bigint | number]
  }
}

/**
 * The return type for each method
 */
export type PulsefutureReturns = {
  'get_locked_funds(address)uint64': bigint
  'lock_funds(address,uint64)string': string
  'hello(string)string': string
  'store_bill(account,string,uint64,account,uint64)uint64': bigint
  'mark_bill_paid(uint64)string': string
  'get_bill_details(uint64)(address,address,uint64,uint64,uint8,string)': Bill
}

/**
 * Defines the types of available calls and state of the Pulsefuture smart contract.
 */
export type PulsefutureTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'get_locked_funds(address)uint64' | 'get_locked_funds', {
      argsObj: PulsefutureArgs['obj']['get_locked_funds(address)uint64']
      argsTuple: PulsefutureArgs['tuple']['get_locked_funds(address)uint64']
      returns: PulsefutureReturns['get_locked_funds(address)uint64']
    }>
    & Record<'lock_funds(address,uint64)string' | 'lock_funds', {
      argsObj: PulsefutureArgs['obj']['lock_funds(address,uint64)string']
      argsTuple: PulsefutureArgs['tuple']['lock_funds(address,uint64)string']
      returns: PulsefutureReturns['lock_funds(address,uint64)string']
    }>
    & Record<'hello(string)string' | 'hello', {
      argsObj: PulsefutureArgs['obj']['hello(string)string']
      argsTuple: PulsefutureArgs['tuple']['hello(string)string']
      returns: PulsefutureReturns['hello(string)string']
    }>
    & Record<'store_bill(account,string,uint64,account,uint64)uint64' | 'store_bill', {
      argsObj: PulsefutureArgs['obj']['store_bill(account,string,uint64,account,uint64)uint64']
      argsTuple: PulsefutureArgs['tuple']['store_bill(account,string,uint64,account,uint64)uint64']
      returns: PulsefutureReturns['store_bill(account,string,uint64,account,uint64)uint64']
    }>
    & Record<'mark_bill_paid(uint64)string' | 'mark_bill_paid', {
      argsObj: PulsefutureArgs['obj']['mark_bill_paid(uint64)string']
      argsTuple: PulsefutureArgs['tuple']['mark_bill_paid(uint64)string']
      returns: PulsefutureReturns['mark_bill_paid(uint64)string']
    }>
    & Record<'get_bill_details(uint64)(address,address,uint64,uint64,uint8,string)' | 'get_bill_details', {
      argsObj: PulsefutureArgs['obj']['get_bill_details(uint64)(address,address,uint64,uint64,uint8,string)']
      argsTuple: PulsefutureArgs['tuple']['get_bill_details(uint64)(address,address,uint64,uint64,uint8,string)']
      returns: PulsefutureReturns['get_bill_details(uint64)(address,address,uint64,uint64,uint8,string)']
    }>
  /**
   * Defines the shape of the state of the application.
   */
  state: {
    global: {
      keys: {
        totalBillsCount: bigint
      }
      maps: {}
    }
    box: {
      keys: {}
      maps: {
        bills: Map<bigint | number, Bill>
        lockedFunds: Map<string, bigint>
      }
    }
  }
}

/**
 * Defines the possible abi call signatures.
 */
export type PulsefutureSignatures = keyof PulsefutureTypes['methods']
/**
 * Defines the possible abi call signatures for methods that return a non-void value.
 */
export type PulsefutureNonVoidMethodSignatures = keyof PulsefutureTypes['methods'] extends infer T ? T extends keyof PulsefutureTypes['methods'] ? MethodReturn<T> extends void ? never : T  : never : never
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the Pulsefuture smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends PulsefutureSignatures> = PulsefutureTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the Pulsefuture smart contract to the method's return type
 */
export type MethodReturn<TSignature extends PulsefutureSignatures> = PulsefutureTypes['methods'][TSignature]['returns']

/**
 * Defines the shape of the keyed global state of the application.
 */
export type GlobalKeysState = PulsefutureTypes['state']['global']['keys']

/**
 * Defines the shape of the keyed box state of the application.
 */
export type BoxKeysState = PulsefutureTypes['state']['box']['keys']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the Pulsefuture smart contract
 */
export abstract class PulsefutureParamsFactory {
  /**
   * Constructs a no op call for the get_locked_funds(address)uint64 ABI method
   *
   * Get the locked funds for a single user
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static getLockedFunds(params: CallParams<PulsefutureArgs['obj']['get_locked_funds(address)uint64'] | PulsefutureArgs['tuple']['get_locked_funds(address)uint64']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'get_locked_funds(address)uint64' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.userAddress],
    }
  }
  /**
   * Constructs a no op call for the lock_funds(address,uint64)string ABI method
   *
   * Lock funds for a user (store the locked amount per address)
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static lockFunds(params: CallParams<PulsefutureArgs['obj']['lock_funds(address,uint64)string'] | PulsefutureArgs['tuple']['lock_funds(address,uint64)string']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'lock_funds(address,uint64)string' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.userAddress, params.args.amount],
    }
  }
  /**
   * Constructs a no op call for the hello(string)string ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static hello(params: CallParams<PulsefutureArgs['obj']['hello(string)string'] | PulsefutureArgs['tuple']['hello(string)string']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'hello(string)string' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.name],
    }
  }
  /**
   * Constructs a no op call for the store_bill(account,string,uint64,account,uint64)uint64 ABI method
   *
   * Store a bill for a user (multiple bills per user)
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static storeBill(params: CallParams<PulsefutureArgs['obj']['store_bill(account,string,uint64,account,uint64)uint64'] | PulsefutureArgs['tuple']['store_bill(account,string,uint64,account,uint64)uint64']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'store_bill(account,string,uint64,account,uint64)uint64' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.userAddress, params.args.description, params.args.amount, params.args.payeeAddress, params.args.dueDate],
    }
  }
  /**
   * Constructs a no op call for the mark_bill_paid(uint64)string ABI method
   *
   * Mark a bill as paid (just a stub for now)
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static markBillPaid(params: CallParams<PulsefutureArgs['obj']['mark_bill_paid(uint64)string'] | PulsefutureArgs['tuple']['mark_bill_paid(uint64)string']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'mark_bill_paid(uint64)string' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.billId],
    }
  }
  /**
   * Constructs a no op call for the get_bill_details(uint64)(address,address,uint64,uint64,uint8,string) ABI method
   *
   * Get specific bill details for a user and bill ID
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static getBillDetails(params: CallParams<PulsefutureArgs['obj']['get_bill_details(uint64)(address,address,uint64,uint64,uint8,string)'] | PulsefutureArgs['tuple']['get_bill_details(uint64)(address,address,uint64,uint64,uint8,string)']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'get_bill_details(uint64)(address,address,uint64,uint64,uint8,string)' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.billId],
    }
  }
}

/**
 * A client to make calls to the Pulsefuture smart contract
 */
export class PulsefutureClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `PulsefutureClient`
   *
   * @param appClient An `AppClient` instance which has been created with the Pulsefuture app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `PulsefutureClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Checks for decode errors on the given return value and maps the return value to the return type for the given method
   * @returns The typed return value or undefined if there was no value
   */
  decodeReturnValue<TSignature extends PulsefutureNonVoidMethodSignatures>(method: TSignature, returnValue: ABIReturn | undefined) {
    return returnValue !== undefined ? getArc56ReturnValue<MethodReturn<TSignature>>(returnValue, this.appClient.getABIMethod(method), APP_SPEC.structs) : undefined
  }

  /**
   * Returns a new `PulsefutureClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<PulsefutureClient> {
    return new PulsefutureClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `PulsefutureClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<PulsefutureClient> {
    return new PulsefutureClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Makes a clear_state call to an existing instance of the Pulsefuture smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the Pulsefuture smart contract using the `get_locked_funds(address)uint64` ABI method.
     *
     * Get the locked funds for a single user
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    getLockedFunds: (params: CallParams<PulsefutureArgs['obj']['get_locked_funds(address)uint64'] | PulsefutureArgs['tuple']['get_locked_funds(address)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(PulsefutureParamsFactory.getLockedFunds(params))
    },

    /**
     * Makes a call to the Pulsefuture smart contract using the `lock_funds(address,uint64)string` ABI method.
     *
     * Lock funds for a user (store the locked amount per address)
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    lockFunds: (params: CallParams<PulsefutureArgs['obj']['lock_funds(address,uint64)string'] | PulsefutureArgs['tuple']['lock_funds(address,uint64)string']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(PulsefutureParamsFactory.lockFunds(params))
    },

    /**
     * Makes a call to the Pulsefuture smart contract using the `hello(string)string` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    hello: (params: CallParams<PulsefutureArgs['obj']['hello(string)string'] | PulsefutureArgs['tuple']['hello(string)string']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(PulsefutureParamsFactory.hello(params))
    },

    /**
     * Makes a call to the Pulsefuture smart contract using the `store_bill(account,string,uint64,account,uint64)uint64` ABI method.
     *
     * Store a bill for a user (multiple bills per user)
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    storeBill: (params: CallParams<PulsefutureArgs['obj']['store_bill(account,string,uint64,account,uint64)uint64'] | PulsefutureArgs['tuple']['store_bill(account,string,uint64,account,uint64)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(PulsefutureParamsFactory.storeBill(params))
    },

    /**
     * Makes a call to the Pulsefuture smart contract using the `mark_bill_paid(uint64)string` ABI method.
     *
     * Mark a bill as paid (just a stub for now)
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    markBillPaid: (params: CallParams<PulsefutureArgs['obj']['mark_bill_paid(uint64)string'] | PulsefutureArgs['tuple']['mark_bill_paid(uint64)string']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(PulsefutureParamsFactory.markBillPaid(params))
    },

    /**
     * Makes a call to the Pulsefuture smart contract using the `get_bill_details(uint64)(address,address,uint64,uint64,uint8,string)` ABI method.
     *
     * Get specific bill details for a user and bill ID
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    getBillDetails: (params: CallParams<PulsefutureArgs['obj']['get_bill_details(uint64)(address,address,uint64,uint64,uint8,string)'] | PulsefutureArgs['tuple']['get_bill_details(uint64)(address,address,uint64,uint64,uint8,string)']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(PulsefutureParamsFactory.getBillDetails(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Makes a clear_state call to an existing instance of the Pulsefuture smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the Pulsefuture smart contract using the `get_locked_funds(address)uint64` ABI method.
     *
     * Get the locked funds for a single user
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    getLockedFunds: (params: CallParams<PulsefutureArgs['obj']['get_locked_funds(address)uint64'] | PulsefutureArgs['tuple']['get_locked_funds(address)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(PulsefutureParamsFactory.getLockedFunds(params))
    },

    /**
     * Makes a call to the Pulsefuture smart contract using the `lock_funds(address,uint64)string` ABI method.
     *
     * Lock funds for a user (store the locked amount per address)
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    lockFunds: (params: CallParams<PulsefutureArgs['obj']['lock_funds(address,uint64)string'] | PulsefutureArgs['tuple']['lock_funds(address,uint64)string']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(PulsefutureParamsFactory.lockFunds(params))
    },

    /**
     * Makes a call to the Pulsefuture smart contract using the `hello(string)string` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    hello: (params: CallParams<PulsefutureArgs['obj']['hello(string)string'] | PulsefutureArgs['tuple']['hello(string)string']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(PulsefutureParamsFactory.hello(params))
    },

    /**
     * Makes a call to the Pulsefuture smart contract using the `store_bill(account,string,uint64,account,uint64)uint64` ABI method.
     *
     * Store a bill for a user (multiple bills per user)
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    storeBill: (params: CallParams<PulsefutureArgs['obj']['store_bill(account,string,uint64,account,uint64)uint64'] | PulsefutureArgs['tuple']['store_bill(account,string,uint64,account,uint64)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(PulsefutureParamsFactory.storeBill(params))
    },

    /**
     * Makes a call to the Pulsefuture smart contract using the `mark_bill_paid(uint64)string` ABI method.
     *
     * Mark a bill as paid (just a stub for now)
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    markBillPaid: (params: CallParams<PulsefutureArgs['obj']['mark_bill_paid(uint64)string'] | PulsefutureArgs['tuple']['mark_bill_paid(uint64)string']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(PulsefutureParamsFactory.markBillPaid(params))
    },

    /**
     * Makes a call to the Pulsefuture smart contract using the `get_bill_details(uint64)(address,address,uint64,uint64,uint8,string)` ABI method.
     *
     * Get specific bill details for a user and bill ID
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    getBillDetails: (params: CallParams<PulsefutureArgs['obj']['get_bill_details(uint64)(address,address,uint64,uint64,uint8,string)'] | PulsefutureArgs['tuple']['get_bill_details(uint64)(address,address,uint64,uint64,uint8,string)']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(PulsefutureParamsFactory.getBillDetails(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Makes a clear_state call to an existing instance of the Pulsefuture smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the Pulsefuture smart contract using the `get_locked_funds(address)uint64` ABI method.
     *
     * Get the locked funds for a single user
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    getLockedFunds: async (params: CallParams<PulsefutureArgs['obj']['get_locked_funds(address)uint64'] | PulsefutureArgs['tuple']['get_locked_funds(address)uint64']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(PulsefutureParamsFactory.getLockedFunds(params))
      return {...result, return: result.return as unknown as (undefined | PulsefutureReturns['get_locked_funds(address)uint64'])}
    },

    /**
     * Makes a call to the Pulsefuture smart contract using the `lock_funds(address,uint64)string` ABI method.
     *
     * Lock funds for a user (store the locked amount per address)
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    lockFunds: async (params: CallParams<PulsefutureArgs['obj']['lock_funds(address,uint64)string'] | PulsefutureArgs['tuple']['lock_funds(address,uint64)string']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(PulsefutureParamsFactory.lockFunds(params))
      return {...result, return: result.return as unknown as (undefined | PulsefutureReturns['lock_funds(address,uint64)string'])}
    },

    /**
     * Makes a call to the Pulsefuture smart contract using the `hello(string)string` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    hello: async (params: CallParams<PulsefutureArgs['obj']['hello(string)string'] | PulsefutureArgs['tuple']['hello(string)string']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(PulsefutureParamsFactory.hello(params))
      return {...result, return: result.return as unknown as (undefined | PulsefutureReturns['hello(string)string'])}
    },

    /**
     * Makes a call to the Pulsefuture smart contract using the `store_bill(account,string,uint64,account,uint64)uint64` ABI method.
     *
     * Store a bill for a user (multiple bills per user)
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    storeBill: async (params: CallParams<PulsefutureArgs['obj']['store_bill(account,string,uint64,account,uint64)uint64'] | PulsefutureArgs['tuple']['store_bill(account,string,uint64,account,uint64)uint64']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(PulsefutureParamsFactory.storeBill(params))
      return {...result, return: result.return as unknown as (undefined | PulsefutureReturns['store_bill(account,string,uint64,account,uint64)uint64'])}
    },

    /**
     * Makes a call to the Pulsefuture smart contract using the `mark_bill_paid(uint64)string` ABI method.
     *
     * Mark a bill as paid (just a stub for now)
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    markBillPaid: async (params: CallParams<PulsefutureArgs['obj']['mark_bill_paid(uint64)string'] | PulsefutureArgs['tuple']['mark_bill_paid(uint64)string']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(PulsefutureParamsFactory.markBillPaid(params))
      return {...result, return: result.return as unknown as (undefined | PulsefutureReturns['mark_bill_paid(uint64)string'])}
    },

    /**
     * Makes a call to the Pulsefuture smart contract using the `get_bill_details(uint64)(address,address,uint64,uint64,uint8,string)` ABI method.
     *
     * Get specific bill details for a user and bill ID
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    getBillDetails: async (params: CallParams<PulsefutureArgs['obj']['get_bill_details(uint64)(address,address,uint64,uint64,uint8,string)'] | PulsefutureArgs['tuple']['get_bill_details(uint64)(address,address,uint64,uint64,uint8,string)']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(PulsefutureParamsFactory.getBillDetails(params))
      return {...result, return: result.return as unknown as (undefined | PulsefutureReturns['get_bill_details(uint64)(address,address,uint64,uint64,uint8,string)'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new PulsefutureClient(this.appClient.clone(params))
  }

  /**
   * Methods to access state for the current Pulsefuture app
   */
  state = {
    /**
     * Methods to access global state for the current Pulsefuture app
     */
    global: {
      /**
       * Get all current keyed values from global state
       */
      getAll: async (): Promise<Partial<Expand<GlobalKeysState>>> => {
        const result = await this.appClient.state.global.getAll()
        return {
          totalBillsCount: result.total_bills_count,
        }
      },
      /**
       * Get the current value of the total_bills_count key in global state
       */
      totalBillsCount: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("total_bills_count")) as bigint | undefined },
    },
    /**
     * Methods to access box state for the current Pulsefuture app
     */
    box: {
      /**
       * Get all current keyed values from box state
       */
      getAll: async (): Promise<Partial<Expand<BoxKeysState>>> => {
        const result = await this.appClient.state.box.getAll()
        return {
        }
      },
      /**
       * Get values from the bills map in box state
       */
      bills: {
        /**
         * Get all current values of the bills map in box state
         */
        getMap: async (): Promise<Map<bigint, Bill>> => { return (await this.appClient.state.box.getMap("bills")) as Map<bigint, Bill> },
        /**
         * Get a current value of the bills map by key from box state
         */
        value: async (key: bigint | number): Promise<Bill | undefined> => { return await this.appClient.state.box.getMapValue("bills", key) as Bill | undefined },
      },
      /**
       * Get values from the locked_funds map in box state
       */
      lockedFunds: {
        /**
         * Get all current values of the locked_funds map in box state
         */
        getMap: async (): Promise<Map<string, bigint>> => { return (await this.appClient.state.box.getMap("locked_funds")) as Map<string, bigint> },
        /**
         * Get a current value of the locked_funds map by key from box state
         */
        value: async (key: string): Promise<bigint | undefined> => { return await this.appClient.state.box.getMapValue("locked_funds", key) as bigint | undefined },
      },
    },
  }

  public newGroup(): PulsefutureComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a get_locked_funds(address)uint64 method call against the Pulsefuture contract
       */
      getLockedFunds(params: CallParams<PulsefutureArgs['obj']['get_locked_funds(address)uint64'] | PulsefutureArgs['tuple']['get_locked_funds(address)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.getLockedFunds(params)))
        resultMappers.push((v) => client.decodeReturnValue('get_locked_funds(address)uint64', v))
        return this
      },
      /**
       * Add a lock_funds(address,uint64)string method call against the Pulsefuture contract
       */
      lockFunds(params: CallParams<PulsefutureArgs['obj']['lock_funds(address,uint64)string'] | PulsefutureArgs['tuple']['lock_funds(address,uint64)string']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.lockFunds(params)))
        resultMappers.push((v) => client.decodeReturnValue('lock_funds(address,uint64)string', v))
        return this
      },
      /**
       * Add a hello(string)string method call against the Pulsefuture contract
       */
      hello(params: CallParams<PulsefutureArgs['obj']['hello(string)string'] | PulsefutureArgs['tuple']['hello(string)string']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.hello(params)))
        resultMappers.push((v) => client.decodeReturnValue('hello(string)string', v))
        return this
      },
      /**
       * Add a store_bill(account,string,uint64,account,uint64)uint64 method call against the Pulsefuture contract
       */
      storeBill(params: CallParams<PulsefutureArgs['obj']['store_bill(account,string,uint64,account,uint64)uint64'] | PulsefutureArgs['tuple']['store_bill(account,string,uint64,account,uint64)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.storeBill(params)))
        resultMappers.push((v) => client.decodeReturnValue('store_bill(account,string,uint64,account,uint64)uint64', v))
        return this
      },
      /**
       * Add a mark_bill_paid(uint64)string method call against the Pulsefuture contract
       */
      markBillPaid(params: CallParams<PulsefutureArgs['obj']['mark_bill_paid(uint64)string'] | PulsefutureArgs['tuple']['mark_bill_paid(uint64)string']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.markBillPaid(params)))
        resultMappers.push((v) => client.decodeReturnValue('mark_bill_paid(uint64)string', v))
        return this
      },
      /**
       * Add a get_bill_details(uint64)(address,address,uint64,uint64,uint8,string) method call against the Pulsefuture contract
       */
      getBillDetails(params: CallParams<PulsefutureArgs['obj']['get_bill_details(uint64)(address,address,uint64,uint64,uint8,string)'] | PulsefutureArgs['tuple']['get_bill_details(uint64)(address,address,uint64,uint64,uint8,string)']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.getBillDetails(params)))
        resultMappers.push((v) => client.decodeReturnValue('get_bill_details(uint64)(address,address,uint64,uint64,uint8,string)', v))
        return this
      },
      /**
       * Add a clear state call to the Pulsefuture contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as PulsefutureComposer
  }
}
export type PulsefutureComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the get_locked_funds(address)uint64 ABI method.
   *
   * Get the locked funds for a single user
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  getLockedFunds(params?: CallParams<PulsefutureArgs['obj']['get_locked_funds(address)uint64'] | PulsefutureArgs['tuple']['get_locked_funds(address)uint64']>): PulsefutureComposer<[...TReturns, PulsefutureReturns['get_locked_funds(address)uint64'] | undefined]>

  /**
   * Calls the lock_funds(address,uint64)string ABI method.
   *
   * Lock funds for a user (store the locked amount per address)
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  lockFunds(params?: CallParams<PulsefutureArgs['obj']['lock_funds(address,uint64)string'] | PulsefutureArgs['tuple']['lock_funds(address,uint64)string']>): PulsefutureComposer<[...TReturns, PulsefutureReturns['lock_funds(address,uint64)string'] | undefined]>

  /**
   * Calls the hello(string)string ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  hello(params?: CallParams<PulsefutureArgs['obj']['hello(string)string'] | PulsefutureArgs['tuple']['hello(string)string']>): PulsefutureComposer<[...TReturns, PulsefutureReturns['hello(string)string'] | undefined]>

  /**
   * Calls the store_bill(account,string,uint64,account,uint64)uint64 ABI method.
   *
   * Store a bill for a user (multiple bills per user)
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  storeBill(params?: CallParams<PulsefutureArgs['obj']['store_bill(account,string,uint64,account,uint64)uint64'] | PulsefutureArgs['tuple']['store_bill(account,string,uint64,account,uint64)uint64']>): PulsefutureComposer<[...TReturns, PulsefutureReturns['store_bill(account,string,uint64,account,uint64)uint64'] | undefined]>

  /**
   * Calls the mark_bill_paid(uint64)string ABI method.
   *
   * Mark a bill as paid (just a stub for now)
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  markBillPaid(params?: CallParams<PulsefutureArgs['obj']['mark_bill_paid(uint64)string'] | PulsefutureArgs['tuple']['mark_bill_paid(uint64)string']>): PulsefutureComposer<[...TReturns, PulsefutureReturns['mark_bill_paid(uint64)string'] | undefined]>

  /**
   * Calls the get_bill_details(uint64)(address,address,uint64,uint64,uint8,string) ABI method.
   *
   * Get specific bill details for a user and bill ID
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  getBillDetails(params?: CallParams<PulsefutureArgs['obj']['get_bill_details(uint64)(address,address,uint64,uint64,uint8,string)'] | PulsefutureArgs['tuple']['get_bill_details(uint64)(address,address,uint64,uint64,uint8,string)']>): PulsefutureComposer<[...TReturns, PulsefutureReturns['get_bill_details(uint64)(address,address,uint64,uint64,uint8,string)'] | undefined]>

  /**
   * Makes a clear_state call to an existing instance of the Pulsefuture smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): PulsefutureComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): PulsefutureComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<PulsefutureComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<PulsefutureComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<PulsefutureComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<PulsefutureComposerResults<TReturns>>
}
export type PulsefutureComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

