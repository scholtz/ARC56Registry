/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"ERC4626Wrapper__Helper1","structs":{},"methods":[{"name":"FixedPointMathLib.fullMulDivUp","args":[{"type":"uint256","name":"x"},{"type":"uint256","name":"y"},{"type":"uint256","name":"d"}],"returns":{"type":"uint256"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"desc":"@dev Calculates `floor(x * y / d)` with full precision, rounded up.\n Throws if result overflows a uint256 or when `d` is zero.\n Credit to Uniswap-v3-core under MIT license:\n https://github.com/Uniswap/v3-core/blob/main/contracts/libraries/FullMath.sol","events":[],"recommendations":{}},{"name":"SafeTransferLib.safeTransferFrom","args":[{"type":"address","name":"token"},{"type":"address","name":"from"},{"type":"address","name":"to"},{"type":"uint256","name":"amount"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"@dev Sends `amount` of ERC20 `token` from `from` to `to`.\n Reverts upon failure.\n The `from` account must have at least `amount` approved for\n the current contract to manage.","events":[],"recommendations":{}},{"name":"SafeTransferLib.safeTransfer","args":[{"type":"address","name":"token"},{"type":"address","name":"to"},{"type":"uint256","name":"amount"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"@dev Sends `amount` of ERC20 `token` from the current contract to `to`.\n Reverts upon failure.","events":[],"recommendations":{}},{"name":"SafeTransferLib.balanceOf","args":[{"type":"address","name":"token"},{"type":"address","name":"account"}],"returns":{"type":"uint256"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"@dev Returns the amount of ERC20 `token` owned by `account`.\n Returns zero if the `token` does not exist.","events":[],"recommendations":{}},{"name":"__init__","args":[{"type":"uint64","name":"o"},{"type":"uint64","name":"p"},{"type":"byte[]","name":"s"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}}],"arcs":[22,28],"desc":"Helper contract 1 for ERC4626Wrapper","networks":{},"state":{"schema":{"global":{"ints":2,"bytes":1},"local":{"ints":0,"bytes":0}},"keys":{"global":{"o":{"keyType":"AVMString","valueType":"AVMUint64","key":"bw=="},"p":{"keyType":"AVMString","valueType":"AVMUint64","key":"cA=="},"s":{"keyType":"AVMString","valueType":"AVMBytes","key":"cw=="}},"local":{},"box":{}},"maps":{"global":{},"local":{},"box":{}}},"bareActions":{"create":["NoOp"],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[1007],"errorMessage":"helper: already initialized"},{"pc":[634,741,829,907],"errorMessage":"helper: must be called in group"},{"pc":[648,755,843,921],"errorMessage":"helper: sender mismatch"},{"pc":[641,748,836,914],"errorMessage":"helper: unauthorized caller"},{"pc":[663,770,858,936],"errorMessage":"helper: wrong prev chunk"},{"pc":[675,782,870,948],"errorMessage":"helper: wrong prev method"},{"pc":[993],"errorMessage":"invalid array length header"},{"pc":[1000],"errorMessage":"invalid number of bytes for arc4.dynamic_array<arc4.uint8>"},{"pc":[707,715,723,803,811,889,897],"errorMessage":"invalid number of bytes for arc4.static_array<arc4.uint8, 32>"},{"pc":[608,616,624,731,819],"errorMessage":"invalid number of bytes for arc4.uint256"},{"pc":[976,986],"errorMessage":"invalid number of bytes for arc4.uint64"},{"pc":[689,957],"errorMessage":"overflow"},{"pc":[241,523],"errorMessage":"revert"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"events":[]} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


/**
 * The argument types for the Erc4626WrapperHelper1 contract
 */
export type Erc4626WrapperHelper1Args = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'FixedPointMathLib.fullMulDivUp(uint256,uint256,uint256)uint256': {
      x: bigint | number
      y: bigint | number
      d: bigint | number
    }
    'SafeTransferLib.safeTransferFrom(address,address,address,uint256)void': {
      token: string
      from: string
      to: string
      amount: bigint | number
    }
    'SafeTransferLib.safeTransfer(address,address,uint256)void': {
      token: string
      to: string
      amount: bigint | number
    }
    'SafeTransferLib.balanceOf(address,address)uint256': {
      token: string
      account: string
    }
    '__init__(uint64,uint64,byte[])void': {
      o: bigint | number
      p: bigint | number
      s: Uint8Array
    }
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'FixedPointMathLib.fullMulDivUp(uint256,uint256,uint256)uint256': [x: bigint | number, y: bigint | number, d: bigint | number]
    'SafeTransferLib.safeTransferFrom(address,address,address,uint256)void': [token: string, from: string, to: string, amount: bigint | number]
    'SafeTransferLib.safeTransfer(address,address,uint256)void': [token: string, to: string, amount: bigint | number]
    'SafeTransferLib.balanceOf(address,address)uint256': [token: string, account: string]
    '__init__(uint64,uint64,byte[])void': [o: bigint | number, p: bigint | number, s: Uint8Array]
  }
}

/**
 * The return type for each method
 */
export type Erc4626WrapperHelper1Returns = {
  'FixedPointMathLib.fullMulDivUp(uint256,uint256,uint256)uint256': bigint
  'SafeTransferLib.safeTransferFrom(address,address,address,uint256)void': void
  'SafeTransferLib.safeTransfer(address,address,uint256)void': void
  'SafeTransferLib.balanceOf(address,address)uint256': bigint
  '__init__(uint64,uint64,byte[])void': void
}

/**
 * Defines the types of available calls and state of the Erc4626WrapperHelper1 smart contract.
 */
export type Erc4626WrapperHelper1Types = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'FixedPointMathLib.fullMulDivUp(uint256,uint256,uint256)uint256' | 'FixedPointMathLib.fullMulDivUp', {
      argsObj: Erc4626WrapperHelper1Args['obj']['FixedPointMathLib.fullMulDivUp(uint256,uint256,uint256)uint256']
      argsTuple: Erc4626WrapperHelper1Args['tuple']['FixedPointMathLib.fullMulDivUp(uint256,uint256,uint256)uint256']
      returns: Erc4626WrapperHelper1Returns['FixedPointMathLib.fullMulDivUp(uint256,uint256,uint256)uint256']
    }>
    & Record<'SafeTransferLib.safeTransferFrom(address,address,address,uint256)void' | 'SafeTransferLib.safeTransferFrom', {
      argsObj: Erc4626WrapperHelper1Args['obj']['SafeTransferLib.safeTransferFrom(address,address,address,uint256)void']
      argsTuple: Erc4626WrapperHelper1Args['tuple']['SafeTransferLib.safeTransferFrom(address,address,address,uint256)void']
      returns: Erc4626WrapperHelper1Returns['SafeTransferLib.safeTransferFrom(address,address,address,uint256)void']
    }>
    & Record<'SafeTransferLib.safeTransfer(address,address,uint256)void' | 'SafeTransferLib.safeTransfer', {
      argsObj: Erc4626WrapperHelper1Args['obj']['SafeTransferLib.safeTransfer(address,address,uint256)void']
      argsTuple: Erc4626WrapperHelper1Args['tuple']['SafeTransferLib.safeTransfer(address,address,uint256)void']
      returns: Erc4626WrapperHelper1Returns['SafeTransferLib.safeTransfer(address,address,uint256)void']
    }>
    & Record<'SafeTransferLib.balanceOf(address,address)uint256' | 'SafeTransferLib.balanceOf', {
      argsObj: Erc4626WrapperHelper1Args['obj']['SafeTransferLib.balanceOf(address,address)uint256']
      argsTuple: Erc4626WrapperHelper1Args['tuple']['SafeTransferLib.balanceOf(address,address)uint256']
      returns: Erc4626WrapperHelper1Returns['SafeTransferLib.balanceOf(address,address)uint256']
    }>
    & Record<'__init__(uint64,uint64,byte[])void' | '__init__', {
      argsObj: Erc4626WrapperHelper1Args['obj']['__init__(uint64,uint64,byte[])void']
      argsTuple: Erc4626WrapperHelper1Args['tuple']['__init__(uint64,uint64,byte[])void']
      returns: Erc4626WrapperHelper1Returns['__init__(uint64,uint64,byte[])void']
    }>
  /**
   * Defines the shape of the state of the application.
   */
  state: {
    global: {
      keys: {
        o: bigint
        p: bigint
        s: BinaryState
      }
      maps: {}
    }
  }
}

/**
 * Defines the possible abi call signatures.
 */
export type Erc4626WrapperHelper1Signatures = keyof Erc4626WrapperHelper1Types['methods']
/**
 * Defines the possible abi call signatures for methods that return a non-void value.
 */
export type Erc4626WrapperHelper1NonVoidMethodSignatures = keyof Erc4626WrapperHelper1Types['methods'] extends infer T ? T extends keyof Erc4626WrapperHelper1Types['methods'] ? MethodReturn<T> extends void ? never : T  : never : never
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the Erc4626WrapperHelper1 smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends Erc4626WrapperHelper1Signatures> = Erc4626WrapperHelper1Types['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the Erc4626WrapperHelper1 smart contract to the method's return type
 */
export type MethodReturn<TSignature extends Erc4626WrapperHelper1Signatures> = Erc4626WrapperHelper1Types['methods'][TSignature]['returns']

/**
 * Defines the shape of the keyed global state of the application.
 */
export type GlobalKeysState = Erc4626WrapperHelper1Types['state']['global']['keys']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the Erc4626WrapperHelper1 smart contract
 */
export abstract class Erc4626WrapperHelper1ParamsFactory {
  /**
   * Constructs a no op call for the FixedPointMathLib.fullMulDivUp(uint256,uint256,uint256)uint256 ABI method
   *
  * @dev Calculates `floor(x * y / d)` with full precision, rounded up.
  Throws if result overflows a uint256 or when `d` is zero.
  Credit to Uniswap-v3-core under MIT license:
  https://github.com/Uniswap/v3-core/blob/main/contracts/libraries/FullMath.sol

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static fixedPointMathLibFullMulDivUp(params: CallParams<Erc4626WrapperHelper1Args['obj']['FixedPointMathLib.fullMulDivUp(uint256,uint256,uint256)uint256'] | Erc4626WrapperHelper1Args['tuple']['FixedPointMathLib.fullMulDivUp(uint256,uint256,uint256)uint256']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'FixedPointMathLib.fullMulDivUp(uint256,uint256,uint256)uint256' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.x, params.args.y, params.args.d],
    }
  }
  /**
   * Constructs a no op call for the SafeTransferLib.safeTransferFrom(address,address,address,uint256)void ABI method
   *
  * @dev Sends `amount` of ERC20 `token` from `from` to `to`.
  Reverts upon failure.
  The `from` account must have at least `amount` approved for
  the current contract to manage.

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static safeTransferLibSafeTransferFrom(params: CallParams<Erc4626WrapperHelper1Args['obj']['SafeTransferLib.safeTransferFrom(address,address,address,uint256)void'] | Erc4626WrapperHelper1Args['tuple']['SafeTransferLib.safeTransferFrom(address,address,address,uint256)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'SafeTransferLib.safeTransferFrom(address,address,address,uint256)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.token, params.args.from, params.args.to, params.args.amount],
    }
  }
  /**
   * Constructs a no op call for the SafeTransferLib.safeTransfer(address,address,uint256)void ABI method
   *
  * @dev Sends `amount` of ERC20 `token` from the current contract to `to`.
  Reverts upon failure.

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static safeTransferLibSafeTransfer(params: CallParams<Erc4626WrapperHelper1Args['obj']['SafeTransferLib.safeTransfer(address,address,uint256)void'] | Erc4626WrapperHelper1Args['tuple']['SafeTransferLib.safeTransfer(address,address,uint256)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'SafeTransferLib.safeTransfer(address,address,uint256)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.token, params.args.to, params.args.amount],
    }
  }
  /**
   * Constructs a no op call for the SafeTransferLib.balanceOf(address,address)uint256 ABI method
   *
  * @dev Returns the amount of ERC20 `token` owned by `account`.
  Returns zero if the `token` does not exist.

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static safeTransferLibBalanceOf(params: CallParams<Erc4626WrapperHelper1Args['obj']['SafeTransferLib.balanceOf(address,address)uint256'] | Erc4626WrapperHelper1Args['tuple']['SafeTransferLib.balanceOf(address,address)uint256']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'SafeTransferLib.balanceOf(address,address)uint256' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.token, params.args.account],
    }
  }
  /**
   * Constructs a no op call for the __init__(uint64,uint64,byte[])void ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static __init(params: CallParams<Erc4626WrapperHelper1Args['obj']['__init__(uint64,uint64,byte[])void'] | Erc4626WrapperHelper1Args['tuple']['__init__(uint64,uint64,byte[])void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: '__init__(uint64,uint64,byte[])void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.o, params.args.p, params.args.s],
    }
  }
}

/**
 * A client to make calls to the ERC4626Wrapper__Helper1 smart contract
 */
export class Erc4626WrapperHelper1Client {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `Erc4626WrapperHelper1Client`
   *
   * @param appClient An `AppClient` instance which has been created with the Erc4626WrapperHelper1 app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `Erc4626WrapperHelper1Client`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Checks for decode errors on the given return value and maps the return value to the return type for the given method
   * @returns The typed return value or undefined if there was no value
   */
  decodeReturnValue<TSignature extends Erc4626WrapperHelper1NonVoidMethodSignatures>(method: TSignature, returnValue: ABIReturn | undefined) {
    return returnValue !== undefined ? getArc56ReturnValue<MethodReturn<TSignature>>(returnValue, this.appClient.getABIMethod(method), APP_SPEC.structs) : undefined
  }

  /**
   * Returns a new `Erc4626WrapperHelper1Client` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<Erc4626WrapperHelper1Client> {
    return new Erc4626WrapperHelper1Client(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `Erc4626WrapperHelper1Client` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<Erc4626WrapperHelper1Client> {
    return new Erc4626WrapperHelper1Client(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Makes a clear_state call to an existing instance of the ERC4626Wrapper__Helper1 smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the ERC4626Wrapper__Helper1 smart contract using the `FixedPointMathLib.fullMulDivUp(uint256,uint256,uint256)uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
    * @dev Calculates `floor(x * y / d)` with full precision, rounded up.
    Throws if result overflows a uint256 or when `d` is zero.
    Credit to Uniswap-v3-core under MIT license:
    https://github.com/Uniswap/v3-core/blob/main/contracts/libraries/FullMath.sol

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    fixedPointMathLibFullMulDivUp: (params: CallParams<Erc4626WrapperHelper1Args['obj']['FixedPointMathLib.fullMulDivUp(uint256,uint256,uint256)uint256'] | Erc4626WrapperHelper1Args['tuple']['FixedPointMathLib.fullMulDivUp(uint256,uint256,uint256)uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(Erc4626WrapperHelper1ParamsFactory.fixedPointMathLibFullMulDivUp(params))
    },

    /**
     * Makes a call to the ERC4626Wrapper__Helper1 smart contract using the `SafeTransferLib.safeTransferFrom(address,address,address,uint256)void` ABI method.
     *
    * @dev Sends `amount` of ERC20 `token` from `from` to `to`.
    Reverts upon failure.
    The `from` account must have at least `amount` approved for
    the current contract to manage.

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    safeTransferLibSafeTransferFrom: (params: CallParams<Erc4626WrapperHelper1Args['obj']['SafeTransferLib.safeTransferFrom(address,address,address,uint256)void'] | Erc4626WrapperHelper1Args['tuple']['SafeTransferLib.safeTransferFrom(address,address,address,uint256)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(Erc4626WrapperHelper1ParamsFactory.safeTransferLibSafeTransferFrom(params))
    },

    /**
     * Makes a call to the ERC4626Wrapper__Helper1 smart contract using the `SafeTransferLib.safeTransfer(address,address,uint256)void` ABI method.
     *
    * @dev Sends `amount` of ERC20 `token` from the current contract to `to`.
    Reverts upon failure.

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    safeTransferLibSafeTransfer: (params: CallParams<Erc4626WrapperHelper1Args['obj']['SafeTransferLib.safeTransfer(address,address,uint256)void'] | Erc4626WrapperHelper1Args['tuple']['SafeTransferLib.safeTransfer(address,address,uint256)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(Erc4626WrapperHelper1ParamsFactory.safeTransferLibSafeTransfer(params))
    },

    /**
     * Makes a call to the ERC4626Wrapper__Helper1 smart contract using the `SafeTransferLib.balanceOf(address,address)uint256` ABI method.
     *
    * @dev Returns the amount of ERC20 `token` owned by `account`.
    Returns zero if the `token` does not exist.

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    safeTransferLibBalanceOf: (params: CallParams<Erc4626WrapperHelper1Args['obj']['SafeTransferLib.balanceOf(address,address)uint256'] | Erc4626WrapperHelper1Args['tuple']['SafeTransferLib.balanceOf(address,address)uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(Erc4626WrapperHelper1ParamsFactory.safeTransferLibBalanceOf(params))
    },

    /**
     * Makes a call to the ERC4626Wrapper__Helper1 smart contract using the `__init__(uint64,uint64,byte[])void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    __init: (params: CallParams<Erc4626WrapperHelper1Args['obj']['__init__(uint64,uint64,byte[])void'] | Erc4626WrapperHelper1Args['tuple']['__init__(uint64,uint64,byte[])void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(Erc4626WrapperHelper1ParamsFactory.__init(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Makes a clear_state call to an existing instance of the ERC4626Wrapper__Helper1 smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the ERC4626Wrapper__Helper1 smart contract using the `FixedPointMathLib.fullMulDivUp(uint256,uint256,uint256)uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
    * @dev Calculates `floor(x * y / d)` with full precision, rounded up.
    Throws if result overflows a uint256 or when `d` is zero.
    Credit to Uniswap-v3-core under MIT license:
    https://github.com/Uniswap/v3-core/blob/main/contracts/libraries/FullMath.sol

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    fixedPointMathLibFullMulDivUp: (params: CallParams<Erc4626WrapperHelper1Args['obj']['FixedPointMathLib.fullMulDivUp(uint256,uint256,uint256)uint256'] | Erc4626WrapperHelper1Args['tuple']['FixedPointMathLib.fullMulDivUp(uint256,uint256,uint256)uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(Erc4626WrapperHelper1ParamsFactory.fixedPointMathLibFullMulDivUp(params))
    },

    /**
     * Makes a call to the ERC4626Wrapper__Helper1 smart contract using the `SafeTransferLib.safeTransferFrom(address,address,address,uint256)void` ABI method.
     *
    * @dev Sends `amount` of ERC20 `token` from `from` to `to`.
    Reverts upon failure.
    The `from` account must have at least `amount` approved for
    the current contract to manage.

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    safeTransferLibSafeTransferFrom: (params: CallParams<Erc4626WrapperHelper1Args['obj']['SafeTransferLib.safeTransferFrom(address,address,address,uint256)void'] | Erc4626WrapperHelper1Args['tuple']['SafeTransferLib.safeTransferFrom(address,address,address,uint256)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(Erc4626WrapperHelper1ParamsFactory.safeTransferLibSafeTransferFrom(params))
    },

    /**
     * Makes a call to the ERC4626Wrapper__Helper1 smart contract using the `SafeTransferLib.safeTransfer(address,address,uint256)void` ABI method.
     *
    * @dev Sends `amount` of ERC20 `token` from the current contract to `to`.
    Reverts upon failure.

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    safeTransferLibSafeTransfer: (params: CallParams<Erc4626WrapperHelper1Args['obj']['SafeTransferLib.safeTransfer(address,address,uint256)void'] | Erc4626WrapperHelper1Args['tuple']['SafeTransferLib.safeTransfer(address,address,uint256)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(Erc4626WrapperHelper1ParamsFactory.safeTransferLibSafeTransfer(params))
    },

    /**
     * Makes a call to the ERC4626Wrapper__Helper1 smart contract using the `SafeTransferLib.balanceOf(address,address)uint256` ABI method.
     *
    * @dev Returns the amount of ERC20 `token` owned by `account`.
    Returns zero if the `token` does not exist.

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    safeTransferLibBalanceOf: (params: CallParams<Erc4626WrapperHelper1Args['obj']['SafeTransferLib.balanceOf(address,address)uint256'] | Erc4626WrapperHelper1Args['tuple']['SafeTransferLib.balanceOf(address,address)uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(Erc4626WrapperHelper1ParamsFactory.safeTransferLibBalanceOf(params))
    },

    /**
     * Makes a call to the ERC4626Wrapper__Helper1 smart contract using the `__init__(uint64,uint64,byte[])void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    __init: (params: CallParams<Erc4626WrapperHelper1Args['obj']['__init__(uint64,uint64,byte[])void'] | Erc4626WrapperHelper1Args['tuple']['__init__(uint64,uint64,byte[])void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(Erc4626WrapperHelper1ParamsFactory.__init(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Makes a clear_state call to an existing instance of the ERC4626Wrapper__Helper1 smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the ERC4626Wrapper__Helper1 smart contract using the `FixedPointMathLib.fullMulDivUp(uint256,uint256,uint256)uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
    * @dev Calculates `floor(x * y / d)` with full precision, rounded up.
    Throws if result overflows a uint256 or when `d` is zero.
    Credit to Uniswap-v3-core under MIT license:
    https://github.com/Uniswap/v3-core/blob/main/contracts/libraries/FullMath.sol

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    fixedPointMathLibFullMulDivUp: async (params: CallParams<Erc4626WrapperHelper1Args['obj']['FixedPointMathLib.fullMulDivUp(uint256,uint256,uint256)uint256'] | Erc4626WrapperHelper1Args['tuple']['FixedPointMathLib.fullMulDivUp(uint256,uint256,uint256)uint256']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(Erc4626WrapperHelper1ParamsFactory.fixedPointMathLibFullMulDivUp(params))
      return {...result, return: result.return as unknown as (undefined | Erc4626WrapperHelper1Returns['FixedPointMathLib.fullMulDivUp(uint256,uint256,uint256)uint256'])}
    },

    /**
     * Makes a call to the ERC4626Wrapper__Helper1 smart contract using the `SafeTransferLib.safeTransferFrom(address,address,address,uint256)void` ABI method.
     *
    * @dev Sends `amount` of ERC20 `token` from `from` to `to`.
    Reverts upon failure.
    The `from` account must have at least `amount` approved for
    the current contract to manage.

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    safeTransferLibSafeTransferFrom: async (params: CallParams<Erc4626WrapperHelper1Args['obj']['SafeTransferLib.safeTransferFrom(address,address,address,uint256)void'] | Erc4626WrapperHelper1Args['tuple']['SafeTransferLib.safeTransferFrom(address,address,address,uint256)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(Erc4626WrapperHelper1ParamsFactory.safeTransferLibSafeTransferFrom(params))
      return {...result, return: result.return as unknown as (undefined | Erc4626WrapperHelper1Returns['SafeTransferLib.safeTransferFrom(address,address,address,uint256)void'])}
    },

    /**
     * Makes a call to the ERC4626Wrapper__Helper1 smart contract using the `SafeTransferLib.safeTransfer(address,address,uint256)void` ABI method.
     *
    * @dev Sends `amount` of ERC20 `token` from the current contract to `to`.
    Reverts upon failure.

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    safeTransferLibSafeTransfer: async (params: CallParams<Erc4626WrapperHelper1Args['obj']['SafeTransferLib.safeTransfer(address,address,uint256)void'] | Erc4626WrapperHelper1Args['tuple']['SafeTransferLib.safeTransfer(address,address,uint256)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(Erc4626WrapperHelper1ParamsFactory.safeTransferLibSafeTransfer(params))
      return {...result, return: result.return as unknown as (undefined | Erc4626WrapperHelper1Returns['SafeTransferLib.safeTransfer(address,address,uint256)void'])}
    },

    /**
     * Makes a call to the ERC4626Wrapper__Helper1 smart contract using the `SafeTransferLib.balanceOf(address,address)uint256` ABI method.
     *
    * @dev Returns the amount of ERC20 `token` owned by `account`.
    Returns zero if the `token` does not exist.

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    safeTransferLibBalanceOf: async (params: CallParams<Erc4626WrapperHelper1Args['obj']['SafeTransferLib.balanceOf(address,address)uint256'] | Erc4626WrapperHelper1Args['tuple']['SafeTransferLib.balanceOf(address,address)uint256']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(Erc4626WrapperHelper1ParamsFactory.safeTransferLibBalanceOf(params))
      return {...result, return: result.return as unknown as (undefined | Erc4626WrapperHelper1Returns['SafeTransferLib.balanceOf(address,address)uint256'])}
    },

    /**
     * Makes a call to the ERC4626Wrapper__Helper1 smart contract using the `__init__(uint64,uint64,byte[])void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    __init: async (params: CallParams<Erc4626WrapperHelper1Args['obj']['__init__(uint64,uint64,byte[])void'] | Erc4626WrapperHelper1Args['tuple']['__init__(uint64,uint64,byte[])void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(Erc4626WrapperHelper1ParamsFactory.__init(params))
      return {...result, return: result.return as unknown as (undefined | Erc4626WrapperHelper1Returns['__init__(uint64,uint64,byte[])void'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new Erc4626WrapperHelper1Client(this.appClient.clone(params))
  }

  /**
   * Makes a readonly (simulated) call to the ERC4626Wrapper__Helper1 smart contract using the `FixedPointMathLib.fullMulDivUp(uint256,uint256,uint256)uint256` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
  * @dev Calculates `floor(x * y / d)` with full precision, rounded up.
  Throws if result overflows a uint256 or when `d` is zero.
  Credit to Uniswap-v3-core under MIT license:
  https://github.com/Uniswap/v3-core/blob/main/contracts/libraries/FullMath.sol

   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async fixedPointMathLibFullMulDivUp(params: CallParams<Erc4626WrapperHelper1Args['obj']['FixedPointMathLib.fullMulDivUp(uint256,uint256,uint256)uint256'] | Erc4626WrapperHelper1Args['tuple']['FixedPointMathLib.fullMulDivUp(uint256,uint256,uint256)uint256']>) {
    const result = await this.appClient.send.call(Erc4626WrapperHelper1ParamsFactory.fixedPointMathLibFullMulDivUp(params))
    return result.return as unknown as Erc4626WrapperHelper1Returns['FixedPointMathLib.fullMulDivUp(uint256,uint256,uint256)uint256']
  }

  /**
   * Methods to access state for the current ERC4626Wrapper__Helper1 app
   */
  state = {
    /**
     * Methods to access global state for the current ERC4626Wrapper__Helper1 app
     */
    global: {
      /**
       * Get all current keyed values from global state
       */
      getAll: async (): Promise<Partial<Expand<GlobalKeysState>>> => {
        const result = await this.appClient.state.global.getAll()
        return {
          o: result.o,
          p: result.p,
          s: new BinaryStateValue(result.s),
        }
      },
      /**
       * Get the current value of the o key in global state
       */
      o: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("o")) as bigint | undefined },
      /**
       * Get the current value of the p key in global state
       */
      p: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("p")) as bigint | undefined },
      /**
       * Get the current value of the s key in global state
       */
      s: async (): Promise<BinaryState> => { return new BinaryStateValue((await this.appClient.state.global.getValue("s")) as Uint8Array | undefined) },
    },
  }

  public newGroup(): Erc4626WrapperHelper1Composer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a FixedPointMathLib.fullMulDivUp(uint256,uint256,uint256)uint256 method call against the ERC4626Wrapper__Helper1 contract
       */
      fixedPointMathLibFullMulDivUp(params: CallParams<Erc4626WrapperHelper1Args['obj']['FixedPointMathLib.fullMulDivUp(uint256,uint256,uint256)uint256'] | Erc4626WrapperHelper1Args['tuple']['FixedPointMathLib.fullMulDivUp(uint256,uint256,uint256)uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.fixedPointMathLibFullMulDivUp(params)))
        resultMappers.push((v) => client.decodeReturnValue('FixedPointMathLib.fullMulDivUp(uint256,uint256,uint256)uint256', v))
        return this
      },
      /**
       * Add a SafeTransferLib.safeTransferFrom(address,address,address,uint256)void method call against the ERC4626Wrapper__Helper1 contract
       */
      safeTransferLibSafeTransferFrom(params: CallParams<Erc4626WrapperHelper1Args['obj']['SafeTransferLib.safeTransferFrom(address,address,address,uint256)void'] | Erc4626WrapperHelper1Args['tuple']['SafeTransferLib.safeTransferFrom(address,address,address,uint256)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.safeTransferLibSafeTransferFrom(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a SafeTransferLib.safeTransfer(address,address,uint256)void method call against the ERC4626Wrapper__Helper1 contract
       */
      safeTransferLibSafeTransfer(params: CallParams<Erc4626WrapperHelper1Args['obj']['SafeTransferLib.safeTransfer(address,address,uint256)void'] | Erc4626WrapperHelper1Args['tuple']['SafeTransferLib.safeTransfer(address,address,uint256)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.safeTransferLibSafeTransfer(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a SafeTransferLib.balanceOf(address,address)uint256 method call against the ERC4626Wrapper__Helper1 contract
       */
      safeTransferLibBalanceOf(params: CallParams<Erc4626WrapperHelper1Args['obj']['SafeTransferLib.balanceOf(address,address)uint256'] | Erc4626WrapperHelper1Args['tuple']['SafeTransferLib.balanceOf(address,address)uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.safeTransferLibBalanceOf(params)))
        resultMappers.push((v) => client.decodeReturnValue('SafeTransferLib.balanceOf(address,address)uint256', v))
        return this
      },
      /**
       * Add a __init__(uint64,uint64,byte[])void method call against the ERC4626Wrapper__Helper1 contract
       */
      __init(params: CallParams<Erc4626WrapperHelper1Args['obj']['__init__(uint64,uint64,byte[])void'] | Erc4626WrapperHelper1Args['tuple']['__init__(uint64,uint64,byte[])void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.__init(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a clear state call to the ERC4626Wrapper__Helper1 contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as Erc4626WrapperHelper1Composer
  }
}
export type Erc4626WrapperHelper1Composer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the FixedPointMathLib.fullMulDivUp(uint256,uint256,uint256)uint256 ABI method.
   *
  * @dev Calculates `floor(x * y / d)` with full precision, rounded up.
  Throws if result overflows a uint256 or when `d` is zero.
  Credit to Uniswap-v3-core under MIT license:
  https://github.com/Uniswap/v3-core/blob/main/contracts/libraries/FullMath.sol

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  fixedPointMathLibFullMulDivUp(params?: CallParams<Erc4626WrapperHelper1Args['obj']['FixedPointMathLib.fullMulDivUp(uint256,uint256,uint256)uint256'] | Erc4626WrapperHelper1Args['tuple']['FixedPointMathLib.fullMulDivUp(uint256,uint256,uint256)uint256']>): Erc4626WrapperHelper1Composer<[...TReturns, Erc4626WrapperHelper1Returns['FixedPointMathLib.fullMulDivUp(uint256,uint256,uint256)uint256'] | undefined]>

  /**
   * Calls the SafeTransferLib.safeTransferFrom(address,address,address,uint256)void ABI method.
   *
  * @dev Sends `amount` of ERC20 `token` from `from` to `to`.
  Reverts upon failure.
  The `from` account must have at least `amount` approved for
  the current contract to manage.

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  safeTransferLibSafeTransferFrom(params?: CallParams<Erc4626WrapperHelper1Args['obj']['SafeTransferLib.safeTransferFrom(address,address,address,uint256)void'] | Erc4626WrapperHelper1Args['tuple']['SafeTransferLib.safeTransferFrom(address,address,address,uint256)void']>): Erc4626WrapperHelper1Composer<[...TReturns, Erc4626WrapperHelper1Returns['SafeTransferLib.safeTransferFrom(address,address,address,uint256)void'] | undefined]>

  /**
   * Calls the SafeTransferLib.safeTransfer(address,address,uint256)void ABI method.
   *
  * @dev Sends `amount` of ERC20 `token` from the current contract to `to`.
  Reverts upon failure.

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  safeTransferLibSafeTransfer(params?: CallParams<Erc4626WrapperHelper1Args['obj']['SafeTransferLib.safeTransfer(address,address,uint256)void'] | Erc4626WrapperHelper1Args['tuple']['SafeTransferLib.safeTransfer(address,address,uint256)void']>): Erc4626WrapperHelper1Composer<[...TReturns, Erc4626WrapperHelper1Returns['SafeTransferLib.safeTransfer(address,address,uint256)void'] | undefined]>

  /**
   * Calls the SafeTransferLib.balanceOf(address,address)uint256 ABI method.
   *
  * @dev Returns the amount of ERC20 `token` owned by `account`.
  Returns zero if the `token` does not exist.

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  safeTransferLibBalanceOf(params?: CallParams<Erc4626WrapperHelper1Args['obj']['SafeTransferLib.balanceOf(address,address)uint256'] | Erc4626WrapperHelper1Args['tuple']['SafeTransferLib.balanceOf(address,address)uint256']>): Erc4626WrapperHelper1Composer<[...TReturns, Erc4626WrapperHelper1Returns['SafeTransferLib.balanceOf(address,address)uint256'] | undefined]>

  /**
   * Calls the __init__(uint64,uint64,byte[])void ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  __init(params?: CallParams<Erc4626WrapperHelper1Args['obj']['__init__(uint64,uint64,byte[])void'] | Erc4626WrapperHelper1Args['tuple']['__init__(uint64,uint64,byte[])void']>): Erc4626WrapperHelper1Composer<[...TReturns, Erc4626WrapperHelper1Returns['__init__(uint64,uint64,byte[])void'] | undefined]>

  /**
   * Makes a clear_state call to an existing instance of the ERC4626Wrapper__Helper1 smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): Erc4626WrapperHelper1Composer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): Erc4626WrapperHelper1Composer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<Erc4626WrapperHelper1ComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<Erc4626WrapperHelper1ComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<Erc4626WrapperHelper1ComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<Erc4626WrapperHelper1ComposerResults<TReturns>>
}
export type Erc4626WrapperHelper1ComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

