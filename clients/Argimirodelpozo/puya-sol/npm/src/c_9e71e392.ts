/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"c","structs":{"Struct":[{"name":"a","type":"uint256"},{"name":"data","type":"byte[]"},{"name":"b","type":"uint256"}]},"methods":[{"name":"set","args":[{"type":"uint256","name":"__arc4__a"},{"type":"byte[]","name":"_data"},{"type":"uint256","name":"__arc4__b"}],"returns":{"type":"bool"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}},{"name":"copy","args":[],"returns":{"type":"bool"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}},{"name":"del","args":[],"returns":{"type":"bool"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}},{"name":"test","args":[{"type":"uint256","name":"__arc4_i"}],"returns":{"type":"byte[1]"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}}],"arcs":[22,28],"networks":{},"state":{"schema":{"global":{"ints":0,"bytes":0},"local":{"ints":0,"bytes":0}},"keys":{"global":{},"local":{},"box":{"data1":{"keyType":"AVMString","valueType":"Struct","key":"ZGF0YTE="},"data2":{"keyType":"AVMString","valueType":"Struct","key":"ZGF0YTI="}}},"maps":{"global":{},"local":{},"box":{}}},"bareActions":{"create":[],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[345],"errorMessage":"invalid array length header"},{"pc":[352],"errorMessage":"invalid number of bytes for arc4.dynamic_array<arc4.uint8>"},{"pc":[204,336,362],"errorMessage":"invalid number of bytes for arc4.uint256"},{"pc":[217,273,300,375],"errorMessage":"not payable"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"events":[]} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


// Type definitions for ARC-56 structs

export type Struct = {
  a: bigint,
  data: Uint8Array,
  b: bigint
}


/**
 * Converts the ABI tuple representation of a Struct to the struct representation
 */
export function StructFromTuple(abiTuple: [bigint, Uint8Array, bigint]) {
  return getABIStructFromABITuple(abiTuple, APP_SPEC.structs.Struct, APP_SPEC.structs) as Struct
}

/**
 * The argument types for the C contract
 */
export type CArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'set(uint256,byte[],uint256)bool': {
      __arc4A: bigint | number
      _data: Uint8Array
      __arc4B: bigint | number
    }
    'copy()bool': Record<string, never>
    'del()bool': Record<string, never>
    'test(uint256)byte[1]': {
      __arc4I: bigint | number
    }
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'set(uint256,byte[],uint256)bool': [__arc4A: bigint | number, _data: Uint8Array, __arc4B: bigint | number]
    'copy()bool': []
    'del()bool': []
    'test(uint256)byte[1]': [__arc4I: bigint | number]
  }
}

/**
 * The return type for each method
 */
export type CReturns = {
  'set(uint256,byte[],uint256)bool': boolean
  'copy()bool': boolean
  'del()bool': boolean
  'test(uint256)byte[1]': Uint8Array
}

/**
 * Defines the types of available calls and state of the C smart contract.
 */
export type CTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'set(uint256,byte[],uint256)bool' | 'set', {
      argsObj: CArgs['obj']['set(uint256,byte[],uint256)bool']
      argsTuple: CArgs['tuple']['set(uint256,byte[],uint256)bool']
      returns: CReturns['set(uint256,byte[],uint256)bool']
    }>
    & Record<'copy()bool' | 'copy', {
      argsObj: CArgs['obj']['copy()bool']
      argsTuple: CArgs['tuple']['copy()bool']
      returns: CReturns['copy()bool']
    }>
    & Record<'del()bool' | 'del', {
      argsObj: CArgs['obj']['del()bool']
      argsTuple: CArgs['tuple']['del()bool']
      returns: CReturns['del()bool']
    }>
    & Record<'test(uint256)byte[1]' | 'test', {
      argsObj: CArgs['obj']['test(uint256)byte[1]']
      argsTuple: CArgs['tuple']['test(uint256)byte[1]']
      returns: CReturns['test(uint256)byte[1]']
    }>
  /**
   * Defines the shape of the state of the application.
   */
  state: {
    box: {
      keys: {
        data1: Struct
        data2: Struct
      }
      maps: {}
    }
  }
}

/**
 * Defines the possible abi call signatures.
 */
export type CSignatures = keyof CTypes['methods']
/**
 * Defines the possible abi call signatures for methods that return a non-void value.
 */
export type CNonVoidMethodSignatures = keyof CTypes['methods'] extends infer T ? T extends keyof CTypes['methods'] ? MethodReturn<T> extends void ? never : T  : never : never
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the C smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends CSignatures> = CTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the C smart contract to the method's return type
 */
export type MethodReturn<TSignature extends CSignatures> = CTypes['methods'][TSignature]['returns']

/**
 * Defines the shape of the keyed box state of the application.
 */
export type BoxKeysState = CTypes['state']['box']['keys']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the C smart contract
 */
export abstract class CParamsFactory {
  /**
   * Constructs a no op call for the set(uint256,byte[],uint256)bool ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static set(params: CallParams<CArgs['obj']['set(uint256,byte[],uint256)bool'] | CArgs['tuple']['set(uint256,byte[],uint256)bool']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'set(uint256,byte[],uint256)bool' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.__arc4A, params.args._data, params.args.__arc4B],
    }
  }
  /**
   * Constructs a no op call for the copy()bool ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static copy(params: CallParams<CArgs['obj']['copy()bool'] | CArgs['tuple']['copy()bool']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'copy()bool' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the del()bool ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static del(params: CallParams<CArgs['obj']['del()bool'] | CArgs['tuple']['del()bool']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'del()bool' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the test(uint256)byte[1] ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static test(params: CallParams<CArgs['obj']['test(uint256)byte[1]'] | CArgs['tuple']['test(uint256)byte[1]']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'test(uint256)byte[1]' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.__arc4I],
    }
  }
}

/**
 * A client to make calls to the c smart contract
 */
export class CClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `CClient`
   *
   * @param appClient An `AppClient` instance which has been created with the C app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `CClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Checks for decode errors on the given return value and maps the return value to the return type for the given method
   * @returns The typed return value or undefined if there was no value
   */
  decodeReturnValue<TSignature extends CNonVoidMethodSignatures>(method: TSignature, returnValue: ABIReturn | undefined) {
    return returnValue !== undefined ? getArc56ReturnValue<MethodReturn<TSignature>>(returnValue, this.appClient.getABIMethod(method), APP_SPEC.structs) : undefined
  }

  /**
   * Returns a new `CClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<CClient> {
    return new CClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `CClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<CClient> {
    return new CClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Makes a clear_state call to an existing instance of the c smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the c smart contract using the `set(uint256,byte[],uint256)bool` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    set: (params: CallParams<CArgs['obj']['set(uint256,byte[],uint256)bool'] | CArgs['tuple']['set(uint256,byte[],uint256)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(CParamsFactory.set(params))
    },

    /**
     * Makes a call to the c smart contract using the `copy()bool` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    copy: (params: CallParams<CArgs['obj']['copy()bool'] | CArgs['tuple']['copy()bool']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(CParamsFactory.copy(params))
    },

    /**
     * Makes a call to the c smart contract using the `del()bool` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    del: (params: CallParams<CArgs['obj']['del()bool'] | CArgs['tuple']['del()bool']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(CParamsFactory.del(params))
    },

    /**
     * Makes a call to the c smart contract using the `test(uint256)byte[1]` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    test: (params: CallParams<CArgs['obj']['test(uint256)byte[1]'] | CArgs['tuple']['test(uint256)byte[1]']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(CParamsFactory.test(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Makes a clear_state call to an existing instance of the c smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the c smart contract using the `set(uint256,byte[],uint256)bool` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    set: (params: CallParams<CArgs['obj']['set(uint256,byte[],uint256)bool'] | CArgs['tuple']['set(uint256,byte[],uint256)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(CParamsFactory.set(params))
    },

    /**
     * Makes a call to the c smart contract using the `copy()bool` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    copy: (params: CallParams<CArgs['obj']['copy()bool'] | CArgs['tuple']['copy()bool']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(CParamsFactory.copy(params))
    },

    /**
     * Makes a call to the c smart contract using the `del()bool` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    del: (params: CallParams<CArgs['obj']['del()bool'] | CArgs['tuple']['del()bool']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(CParamsFactory.del(params))
    },

    /**
     * Makes a call to the c smart contract using the `test(uint256)byte[1]` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    test: (params: CallParams<CArgs['obj']['test(uint256)byte[1]'] | CArgs['tuple']['test(uint256)byte[1]']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(CParamsFactory.test(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Makes a clear_state call to an existing instance of the c smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the c smart contract using the `set(uint256,byte[],uint256)bool` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    set: async (params: CallParams<CArgs['obj']['set(uint256,byte[],uint256)bool'] | CArgs['tuple']['set(uint256,byte[],uint256)bool']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(CParamsFactory.set(params))
      return {...result, return: result.return as unknown as (undefined | CReturns['set(uint256,byte[],uint256)bool'])}
    },

    /**
     * Makes a call to the c smart contract using the `copy()bool` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    copy: async (params: CallParams<CArgs['obj']['copy()bool'] | CArgs['tuple']['copy()bool']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(CParamsFactory.copy(params))
      return {...result, return: result.return as unknown as (undefined | CReturns['copy()bool'])}
    },

    /**
     * Makes a call to the c smart contract using the `del()bool` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    del: async (params: CallParams<CArgs['obj']['del()bool'] | CArgs['tuple']['del()bool']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(CParamsFactory.del(params))
      return {...result, return: result.return as unknown as (undefined | CReturns['del()bool'])}
    },

    /**
     * Makes a call to the c smart contract using the `test(uint256)byte[1]` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    test: async (params: CallParams<CArgs['obj']['test(uint256)byte[1]'] | CArgs['tuple']['test(uint256)byte[1]']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(CParamsFactory.test(params))
      return {...result, return: result.return as unknown as (undefined | CReturns['test(uint256)byte[1]'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new CClient(this.appClient.clone(params))
  }

  /**
   * Methods to access state for the current c app
   */
  state = {
    /**
     * Methods to access box state for the current c app
     */
    box: {
      /**
       * Get all current keyed values from box state
       */
      getAll: async (): Promise<Partial<Expand<BoxKeysState>>> => {
        const result = await this.appClient.state.box.getAll()
        return {
          data1: result.data1,
          data2: result.data2,
        }
      },
      /**
       * Get the current value of the data1 key in box state
       */
      data1: async (): Promise<Struct | undefined> => { return (await this.appClient.state.box.getValue("data1")) as Struct | undefined },
      /**
       * Get the current value of the data2 key in box state
       */
      data2: async (): Promise<Struct | undefined> => { return (await this.appClient.state.box.getValue("data2")) as Struct | undefined },
    },
  }

  public newGroup(): CComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a set(uint256,byte[],uint256)bool method call against the c contract
       */
      set(params: CallParams<CArgs['obj']['set(uint256,byte[],uint256)bool'] | CArgs['tuple']['set(uint256,byte[],uint256)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.set(params)))
        resultMappers.push((v) => client.decodeReturnValue('set(uint256,byte[],uint256)bool', v))
        return this
      },
      /**
       * Add a copy()bool method call against the c contract
       */
      copy(params: CallParams<CArgs['obj']['copy()bool'] | CArgs['tuple']['copy()bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.copy(params)))
        resultMappers.push((v) => client.decodeReturnValue('copy()bool', v))
        return this
      },
      /**
       * Add a del()bool method call against the c contract
       */
      del(params: CallParams<CArgs['obj']['del()bool'] | CArgs['tuple']['del()bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.del(params)))
        resultMappers.push((v) => client.decodeReturnValue('del()bool', v))
        return this
      },
      /**
       * Add a test(uint256)byte[1] method call against the c contract
       */
      test(params: CallParams<CArgs['obj']['test(uint256)byte[1]'] | CArgs['tuple']['test(uint256)byte[1]']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.test(params)))
        resultMappers.push((v) => client.decodeReturnValue('test(uint256)byte[1]', v))
        return this
      },
      /**
       * Add a clear state call to the c contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as CComposer
  }
}
export type CComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the set(uint256,byte[],uint256)bool ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  set(params?: CallParams<CArgs['obj']['set(uint256,byte[],uint256)bool'] | CArgs['tuple']['set(uint256,byte[],uint256)bool']>): CComposer<[...TReturns, CReturns['set(uint256,byte[],uint256)bool'] | undefined]>

  /**
   * Calls the copy()bool ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  copy(params?: CallParams<CArgs['obj']['copy()bool'] | CArgs['tuple']['copy()bool']>): CComposer<[...TReturns, CReturns['copy()bool'] | undefined]>

  /**
   * Calls the del()bool ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  del(params?: CallParams<CArgs['obj']['del()bool'] | CArgs['tuple']['del()bool']>): CComposer<[...TReturns, CReturns['del()bool'] | undefined]>

  /**
   * Calls the test(uint256)byte[1] ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  test(params?: CallParams<CArgs['obj']['test(uint256)byte[1]'] | CArgs['tuple']['test(uint256)byte[1]']>): CComposer<[...TReturns, CReturns['test(uint256)byte[1]'] | undefined]>

  /**
   * Makes a clear_state call to an existing instance of the c smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): CComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): CComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<CComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<CComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<CComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<CComposerResults<TReturns>>
}
export type CComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

