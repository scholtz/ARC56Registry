/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"DateTimeWrapper","structs":{"epochDayToDateReturn":[{"name":"year","type":"uint256"},{"name":"month","type":"uint256"},{"name":"day","type":"uint256"}],"timestampToDateReturn":[{"name":"year","type":"uint256"},{"name":"month","type":"uint256"},{"name":"day","type":"uint256"}]},"methods":[{"name":"dateToEpochDay","args":[{"type":"uint256","name":"year"},{"type":"uint256","name":"month"},{"type":"uint256","name":"day"}],"returns":{"type":"uint256"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"epochDayToDate","args":[{"type":"uint256","name":"epochDay"}],"returns":{"type":"(uint256,uint256,uint256)","struct":"epochDayToDateReturn"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"dateToTimestamp","args":[{"type":"uint256","name":"year"},{"type":"uint256","name":"month"},{"type":"uint256","name":"day"}],"returns":{"type":"uint256"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"timestampToDate","args":[{"type":"uint256","name":"timestamp"}],"returns":{"type":"(uint256,uint256,uint256)","struct":"timestampToDateReturn"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"isLeapYear","args":[{"type":"uint256","name":"year"}],"returns":{"type":"bool"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"daysInMonth","args":[{"type":"uint256","name":"year"},{"type":"uint256","name":"month"}],"returns":{"type":"uint256"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"weekday","args":[{"type":"uint256","name":"timestamp"}],"returns":{"type":"uint256"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}}],"arcs":[22,28],"networks":{},"state":{"schema":{"global":{"ints":0,"bytes":0},"local":{"ints":0,"bytes":0}},"keys":{"global":{},"local":{},"box":{}},"maps":{"global":{},"local":{},"box":{}}},"bareActions":{"create":[],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[805,813,821,851,908,916,924,954,1011,1040,1048,1078],"errorMessage":"invalid number of bytes for arc4.uint256"},{"pc":[834,865,878,890,937,968,981,993,1061,1091],"errorMessage":"overflow"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"events":[]} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


// Type definitions for ARC-56 structs

export type EpochDayToDateReturn = {
  year: bigint,
  month: bigint,
  day: bigint
}


/**
 * Converts the ABI tuple representation of a epochDayToDateReturn to the struct representation
 */
export function EpochDayToDateReturnFromTuple(abiTuple: [bigint, bigint, bigint]) {
  return getABIStructFromABITuple(abiTuple, APP_SPEC.structs.epochDayToDateReturn, APP_SPEC.structs) as EpochDayToDateReturn
}

export type TimestampToDateReturn = {
  year: bigint,
  month: bigint,
  day: bigint
}


/**
 * Converts the ABI tuple representation of a timestampToDateReturn to the struct representation
 */
export function TimestampToDateReturnFromTuple(abiTuple: [bigint, bigint, bigint]) {
  return getABIStructFromABITuple(abiTuple, APP_SPEC.structs.timestampToDateReturn, APP_SPEC.structs) as TimestampToDateReturn
}

/**
 * The argument types for the DateTimeWrapper contract
 */
export type DateTimeWrapperArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'dateToEpochDay(uint256,uint256,uint256)uint256': {
      year: bigint | number
      month: bigint | number
      day: bigint | number
    }
    'epochDayToDate(uint256)(uint256,uint256,uint256)': {
      epochDay: bigint | number
    }
    'dateToTimestamp(uint256,uint256,uint256)uint256': {
      year: bigint | number
      month: bigint | number
      day: bigint | number
    }
    'timestampToDate(uint256)(uint256,uint256,uint256)': {
      timestamp: bigint | number
    }
    'isLeapYear(uint256)bool': {
      year: bigint | number
    }
    'daysInMonth(uint256,uint256)uint256': {
      year: bigint | number
      month: bigint | number
    }
    'weekday(uint256)uint256': {
      timestamp: bigint | number
    }
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'dateToEpochDay(uint256,uint256,uint256)uint256': [year: bigint | number, month: bigint | number, day: bigint | number]
    'epochDayToDate(uint256)(uint256,uint256,uint256)': [epochDay: bigint | number]
    'dateToTimestamp(uint256,uint256,uint256)uint256': [year: bigint | number, month: bigint | number, day: bigint | number]
    'timestampToDate(uint256)(uint256,uint256,uint256)': [timestamp: bigint | number]
    'isLeapYear(uint256)bool': [year: bigint | number]
    'daysInMonth(uint256,uint256)uint256': [year: bigint | number, month: bigint | number]
    'weekday(uint256)uint256': [timestamp: bigint | number]
  }
}

/**
 * The return type for each method
 */
export type DateTimeWrapperReturns = {
  'dateToEpochDay(uint256,uint256,uint256)uint256': bigint
  'epochDayToDate(uint256)(uint256,uint256,uint256)': EpochDayToDateReturn
  'dateToTimestamp(uint256,uint256,uint256)uint256': bigint
  'timestampToDate(uint256)(uint256,uint256,uint256)': TimestampToDateReturn
  'isLeapYear(uint256)bool': boolean
  'daysInMonth(uint256,uint256)uint256': bigint
  'weekday(uint256)uint256': bigint
}

/**
 * Defines the types of available calls and state of the DateTimeWrapper smart contract.
 */
export type DateTimeWrapperTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'dateToEpochDay(uint256,uint256,uint256)uint256' | 'dateToEpochDay', {
      argsObj: DateTimeWrapperArgs['obj']['dateToEpochDay(uint256,uint256,uint256)uint256']
      argsTuple: DateTimeWrapperArgs['tuple']['dateToEpochDay(uint256,uint256,uint256)uint256']
      returns: DateTimeWrapperReturns['dateToEpochDay(uint256,uint256,uint256)uint256']
    }>
    & Record<'epochDayToDate(uint256)(uint256,uint256,uint256)' | 'epochDayToDate', {
      argsObj: DateTimeWrapperArgs['obj']['epochDayToDate(uint256)(uint256,uint256,uint256)']
      argsTuple: DateTimeWrapperArgs['tuple']['epochDayToDate(uint256)(uint256,uint256,uint256)']
      returns: DateTimeWrapperReturns['epochDayToDate(uint256)(uint256,uint256,uint256)']
    }>
    & Record<'dateToTimestamp(uint256,uint256,uint256)uint256' | 'dateToTimestamp', {
      argsObj: DateTimeWrapperArgs['obj']['dateToTimestamp(uint256,uint256,uint256)uint256']
      argsTuple: DateTimeWrapperArgs['tuple']['dateToTimestamp(uint256,uint256,uint256)uint256']
      returns: DateTimeWrapperReturns['dateToTimestamp(uint256,uint256,uint256)uint256']
    }>
    & Record<'timestampToDate(uint256)(uint256,uint256,uint256)' | 'timestampToDate', {
      argsObj: DateTimeWrapperArgs['obj']['timestampToDate(uint256)(uint256,uint256,uint256)']
      argsTuple: DateTimeWrapperArgs['tuple']['timestampToDate(uint256)(uint256,uint256,uint256)']
      returns: DateTimeWrapperReturns['timestampToDate(uint256)(uint256,uint256,uint256)']
    }>
    & Record<'isLeapYear(uint256)bool' | 'isLeapYear', {
      argsObj: DateTimeWrapperArgs['obj']['isLeapYear(uint256)bool']
      argsTuple: DateTimeWrapperArgs['tuple']['isLeapYear(uint256)bool']
      returns: DateTimeWrapperReturns['isLeapYear(uint256)bool']
    }>
    & Record<'daysInMonth(uint256,uint256)uint256' | 'daysInMonth', {
      argsObj: DateTimeWrapperArgs['obj']['daysInMonth(uint256,uint256)uint256']
      argsTuple: DateTimeWrapperArgs['tuple']['daysInMonth(uint256,uint256)uint256']
      returns: DateTimeWrapperReturns['daysInMonth(uint256,uint256)uint256']
    }>
    & Record<'weekday(uint256)uint256' | 'weekday', {
      argsObj: DateTimeWrapperArgs['obj']['weekday(uint256)uint256']
      argsTuple: DateTimeWrapperArgs['tuple']['weekday(uint256)uint256']
      returns: DateTimeWrapperReturns['weekday(uint256)uint256']
    }>
}

/**
 * Defines the possible abi call signatures.
 */
export type DateTimeWrapperSignatures = keyof DateTimeWrapperTypes['methods']
/**
 * Defines the possible abi call signatures for methods that return a non-void value.
 */
export type DateTimeWrapperNonVoidMethodSignatures = keyof DateTimeWrapperTypes['methods'] extends infer T ? T extends keyof DateTimeWrapperTypes['methods'] ? MethodReturn<T> extends void ? never : T  : never : never
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the DateTimeWrapper smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends DateTimeWrapperSignatures> = DateTimeWrapperTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the DateTimeWrapper smart contract to the method's return type
 */
export type MethodReturn<TSignature extends DateTimeWrapperSignatures> = DateTimeWrapperTypes['methods'][TSignature]['returns']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the DateTimeWrapper smart contract
 */
export abstract class DateTimeWrapperParamsFactory {
  /**
   * Constructs a no op call for the dateToEpochDay(uint256,uint256,uint256)uint256 ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static dateToEpochDay(params: CallParams<DateTimeWrapperArgs['obj']['dateToEpochDay(uint256,uint256,uint256)uint256'] | DateTimeWrapperArgs['tuple']['dateToEpochDay(uint256,uint256,uint256)uint256']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'dateToEpochDay(uint256,uint256,uint256)uint256' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.year, params.args.month, params.args.day],
    }
  }
  /**
   * Constructs a no op call for the epochDayToDate(uint256)(uint256,uint256,uint256) ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static epochDayToDate(params: CallParams<DateTimeWrapperArgs['obj']['epochDayToDate(uint256)(uint256,uint256,uint256)'] | DateTimeWrapperArgs['tuple']['epochDayToDate(uint256)(uint256,uint256,uint256)']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'epochDayToDate(uint256)(uint256,uint256,uint256)' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.epochDay],
    }
  }
  /**
   * Constructs a no op call for the dateToTimestamp(uint256,uint256,uint256)uint256 ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static dateToTimestamp(params: CallParams<DateTimeWrapperArgs['obj']['dateToTimestamp(uint256,uint256,uint256)uint256'] | DateTimeWrapperArgs['tuple']['dateToTimestamp(uint256,uint256,uint256)uint256']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'dateToTimestamp(uint256,uint256,uint256)uint256' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.year, params.args.month, params.args.day],
    }
  }
  /**
   * Constructs a no op call for the timestampToDate(uint256)(uint256,uint256,uint256) ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static timestampToDate(params: CallParams<DateTimeWrapperArgs['obj']['timestampToDate(uint256)(uint256,uint256,uint256)'] | DateTimeWrapperArgs['tuple']['timestampToDate(uint256)(uint256,uint256,uint256)']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'timestampToDate(uint256)(uint256,uint256,uint256)' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.timestamp],
    }
  }
  /**
   * Constructs a no op call for the isLeapYear(uint256)bool ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static isLeapYear(params: CallParams<DateTimeWrapperArgs['obj']['isLeapYear(uint256)bool'] | DateTimeWrapperArgs['tuple']['isLeapYear(uint256)bool']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'isLeapYear(uint256)bool' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.year],
    }
  }
  /**
   * Constructs a no op call for the daysInMonth(uint256,uint256)uint256 ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static daysInMonth(params: CallParams<DateTimeWrapperArgs['obj']['daysInMonth(uint256,uint256)uint256'] | DateTimeWrapperArgs['tuple']['daysInMonth(uint256,uint256)uint256']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'daysInMonth(uint256,uint256)uint256' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.year, params.args.month],
    }
  }
  /**
   * Constructs a no op call for the weekday(uint256)uint256 ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static weekday(params: CallParams<DateTimeWrapperArgs['obj']['weekday(uint256)uint256'] | DateTimeWrapperArgs['tuple']['weekday(uint256)uint256']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'weekday(uint256)uint256' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.timestamp],
    }
  }
}

/**
 * A client to make calls to the DateTimeWrapper smart contract
 */
export class DateTimeWrapperClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `DateTimeWrapperClient`
   *
   * @param appClient An `AppClient` instance which has been created with the DateTimeWrapper app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `DateTimeWrapperClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Checks for decode errors on the given return value and maps the return value to the return type for the given method
   * @returns The typed return value or undefined if there was no value
   */
  decodeReturnValue<TSignature extends DateTimeWrapperNonVoidMethodSignatures>(method: TSignature, returnValue: ABIReturn | undefined) {
    return returnValue !== undefined ? getArc56ReturnValue<MethodReturn<TSignature>>(returnValue, this.appClient.getABIMethod(method), APP_SPEC.structs) : undefined
  }

  /**
   * Returns a new `DateTimeWrapperClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<DateTimeWrapperClient> {
    return new DateTimeWrapperClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `DateTimeWrapperClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<DateTimeWrapperClient> {
    return new DateTimeWrapperClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Makes a clear_state call to an existing instance of the DateTimeWrapper smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the DateTimeWrapper smart contract using the `dateToEpochDay(uint256,uint256,uint256)uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    dateToEpochDay: (params: CallParams<DateTimeWrapperArgs['obj']['dateToEpochDay(uint256,uint256,uint256)uint256'] | DateTimeWrapperArgs['tuple']['dateToEpochDay(uint256,uint256,uint256)uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(DateTimeWrapperParamsFactory.dateToEpochDay(params))
    },

    /**
     * Makes a call to the DateTimeWrapper smart contract using the `epochDayToDate(uint256)(uint256,uint256,uint256)` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    epochDayToDate: (params: CallParams<DateTimeWrapperArgs['obj']['epochDayToDate(uint256)(uint256,uint256,uint256)'] | DateTimeWrapperArgs['tuple']['epochDayToDate(uint256)(uint256,uint256,uint256)']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(DateTimeWrapperParamsFactory.epochDayToDate(params))
    },

    /**
     * Makes a call to the DateTimeWrapper smart contract using the `dateToTimestamp(uint256,uint256,uint256)uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    dateToTimestamp: (params: CallParams<DateTimeWrapperArgs['obj']['dateToTimestamp(uint256,uint256,uint256)uint256'] | DateTimeWrapperArgs['tuple']['dateToTimestamp(uint256,uint256,uint256)uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(DateTimeWrapperParamsFactory.dateToTimestamp(params))
    },

    /**
     * Makes a call to the DateTimeWrapper smart contract using the `timestampToDate(uint256)(uint256,uint256,uint256)` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    timestampToDate: (params: CallParams<DateTimeWrapperArgs['obj']['timestampToDate(uint256)(uint256,uint256,uint256)'] | DateTimeWrapperArgs['tuple']['timestampToDate(uint256)(uint256,uint256,uint256)']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(DateTimeWrapperParamsFactory.timestampToDate(params))
    },

    /**
     * Makes a call to the DateTimeWrapper smart contract using the `isLeapYear(uint256)bool` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    isLeapYear: (params: CallParams<DateTimeWrapperArgs['obj']['isLeapYear(uint256)bool'] | DateTimeWrapperArgs['tuple']['isLeapYear(uint256)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(DateTimeWrapperParamsFactory.isLeapYear(params))
    },

    /**
     * Makes a call to the DateTimeWrapper smart contract using the `daysInMonth(uint256,uint256)uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    daysInMonth: (params: CallParams<DateTimeWrapperArgs['obj']['daysInMonth(uint256,uint256)uint256'] | DateTimeWrapperArgs['tuple']['daysInMonth(uint256,uint256)uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(DateTimeWrapperParamsFactory.daysInMonth(params))
    },

    /**
     * Makes a call to the DateTimeWrapper smart contract using the `weekday(uint256)uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    weekday: (params: CallParams<DateTimeWrapperArgs['obj']['weekday(uint256)uint256'] | DateTimeWrapperArgs['tuple']['weekday(uint256)uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(DateTimeWrapperParamsFactory.weekday(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Makes a clear_state call to an existing instance of the DateTimeWrapper smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the DateTimeWrapper smart contract using the `dateToEpochDay(uint256,uint256,uint256)uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    dateToEpochDay: (params: CallParams<DateTimeWrapperArgs['obj']['dateToEpochDay(uint256,uint256,uint256)uint256'] | DateTimeWrapperArgs['tuple']['dateToEpochDay(uint256,uint256,uint256)uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(DateTimeWrapperParamsFactory.dateToEpochDay(params))
    },

    /**
     * Makes a call to the DateTimeWrapper smart contract using the `epochDayToDate(uint256)(uint256,uint256,uint256)` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    epochDayToDate: (params: CallParams<DateTimeWrapperArgs['obj']['epochDayToDate(uint256)(uint256,uint256,uint256)'] | DateTimeWrapperArgs['tuple']['epochDayToDate(uint256)(uint256,uint256,uint256)']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(DateTimeWrapperParamsFactory.epochDayToDate(params))
    },

    /**
     * Makes a call to the DateTimeWrapper smart contract using the `dateToTimestamp(uint256,uint256,uint256)uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    dateToTimestamp: (params: CallParams<DateTimeWrapperArgs['obj']['dateToTimestamp(uint256,uint256,uint256)uint256'] | DateTimeWrapperArgs['tuple']['dateToTimestamp(uint256,uint256,uint256)uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(DateTimeWrapperParamsFactory.dateToTimestamp(params))
    },

    /**
     * Makes a call to the DateTimeWrapper smart contract using the `timestampToDate(uint256)(uint256,uint256,uint256)` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    timestampToDate: (params: CallParams<DateTimeWrapperArgs['obj']['timestampToDate(uint256)(uint256,uint256,uint256)'] | DateTimeWrapperArgs['tuple']['timestampToDate(uint256)(uint256,uint256,uint256)']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(DateTimeWrapperParamsFactory.timestampToDate(params))
    },

    /**
     * Makes a call to the DateTimeWrapper smart contract using the `isLeapYear(uint256)bool` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    isLeapYear: (params: CallParams<DateTimeWrapperArgs['obj']['isLeapYear(uint256)bool'] | DateTimeWrapperArgs['tuple']['isLeapYear(uint256)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(DateTimeWrapperParamsFactory.isLeapYear(params))
    },

    /**
     * Makes a call to the DateTimeWrapper smart contract using the `daysInMonth(uint256,uint256)uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    daysInMonth: (params: CallParams<DateTimeWrapperArgs['obj']['daysInMonth(uint256,uint256)uint256'] | DateTimeWrapperArgs['tuple']['daysInMonth(uint256,uint256)uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(DateTimeWrapperParamsFactory.daysInMonth(params))
    },

    /**
     * Makes a call to the DateTimeWrapper smart contract using the `weekday(uint256)uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    weekday: (params: CallParams<DateTimeWrapperArgs['obj']['weekday(uint256)uint256'] | DateTimeWrapperArgs['tuple']['weekday(uint256)uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(DateTimeWrapperParamsFactory.weekday(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Makes a clear_state call to an existing instance of the DateTimeWrapper smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the DateTimeWrapper smart contract using the `dateToEpochDay(uint256,uint256,uint256)uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    dateToEpochDay: async (params: CallParams<DateTimeWrapperArgs['obj']['dateToEpochDay(uint256,uint256,uint256)uint256'] | DateTimeWrapperArgs['tuple']['dateToEpochDay(uint256,uint256,uint256)uint256']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(DateTimeWrapperParamsFactory.dateToEpochDay(params))
      return {...result, return: result.return as unknown as (undefined | DateTimeWrapperReturns['dateToEpochDay(uint256,uint256,uint256)uint256'])}
    },

    /**
     * Makes a call to the DateTimeWrapper smart contract using the `epochDayToDate(uint256)(uint256,uint256,uint256)` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    epochDayToDate: async (params: CallParams<DateTimeWrapperArgs['obj']['epochDayToDate(uint256)(uint256,uint256,uint256)'] | DateTimeWrapperArgs['tuple']['epochDayToDate(uint256)(uint256,uint256,uint256)']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(DateTimeWrapperParamsFactory.epochDayToDate(params))
      return {...result, return: result.return as unknown as (undefined | DateTimeWrapperReturns['epochDayToDate(uint256)(uint256,uint256,uint256)'])}
    },

    /**
     * Makes a call to the DateTimeWrapper smart contract using the `dateToTimestamp(uint256,uint256,uint256)uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    dateToTimestamp: async (params: CallParams<DateTimeWrapperArgs['obj']['dateToTimestamp(uint256,uint256,uint256)uint256'] | DateTimeWrapperArgs['tuple']['dateToTimestamp(uint256,uint256,uint256)uint256']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(DateTimeWrapperParamsFactory.dateToTimestamp(params))
      return {...result, return: result.return as unknown as (undefined | DateTimeWrapperReturns['dateToTimestamp(uint256,uint256,uint256)uint256'])}
    },

    /**
     * Makes a call to the DateTimeWrapper smart contract using the `timestampToDate(uint256)(uint256,uint256,uint256)` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    timestampToDate: async (params: CallParams<DateTimeWrapperArgs['obj']['timestampToDate(uint256)(uint256,uint256,uint256)'] | DateTimeWrapperArgs['tuple']['timestampToDate(uint256)(uint256,uint256,uint256)']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(DateTimeWrapperParamsFactory.timestampToDate(params))
      return {...result, return: result.return as unknown as (undefined | DateTimeWrapperReturns['timestampToDate(uint256)(uint256,uint256,uint256)'])}
    },

    /**
     * Makes a call to the DateTimeWrapper smart contract using the `isLeapYear(uint256)bool` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    isLeapYear: async (params: CallParams<DateTimeWrapperArgs['obj']['isLeapYear(uint256)bool'] | DateTimeWrapperArgs['tuple']['isLeapYear(uint256)bool']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(DateTimeWrapperParamsFactory.isLeapYear(params))
      return {...result, return: result.return as unknown as (undefined | DateTimeWrapperReturns['isLeapYear(uint256)bool'])}
    },

    /**
     * Makes a call to the DateTimeWrapper smart contract using the `daysInMonth(uint256,uint256)uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    daysInMonth: async (params: CallParams<DateTimeWrapperArgs['obj']['daysInMonth(uint256,uint256)uint256'] | DateTimeWrapperArgs['tuple']['daysInMonth(uint256,uint256)uint256']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(DateTimeWrapperParamsFactory.daysInMonth(params))
      return {...result, return: result.return as unknown as (undefined | DateTimeWrapperReturns['daysInMonth(uint256,uint256)uint256'])}
    },

    /**
     * Makes a call to the DateTimeWrapper smart contract using the `weekday(uint256)uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    weekday: async (params: CallParams<DateTimeWrapperArgs['obj']['weekday(uint256)uint256'] | DateTimeWrapperArgs['tuple']['weekday(uint256)uint256']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(DateTimeWrapperParamsFactory.weekday(params))
      return {...result, return: result.return as unknown as (undefined | DateTimeWrapperReturns['weekday(uint256)uint256'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new DateTimeWrapperClient(this.appClient.clone(params))
  }

  /**
   * Makes a readonly (simulated) call to the DateTimeWrapper smart contract using the `dateToEpochDay(uint256,uint256,uint256)uint256` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async dateToEpochDay(params: CallParams<DateTimeWrapperArgs['obj']['dateToEpochDay(uint256,uint256,uint256)uint256'] | DateTimeWrapperArgs['tuple']['dateToEpochDay(uint256,uint256,uint256)uint256']>) {
    const result = await this.appClient.send.call(DateTimeWrapperParamsFactory.dateToEpochDay(params))
    return result.return as unknown as DateTimeWrapperReturns['dateToEpochDay(uint256,uint256,uint256)uint256']
  }

  /**
   * Makes a readonly (simulated) call to the DateTimeWrapper smart contract using the `epochDayToDate(uint256)(uint256,uint256,uint256)` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async epochDayToDate(params: CallParams<DateTimeWrapperArgs['obj']['epochDayToDate(uint256)(uint256,uint256,uint256)'] | DateTimeWrapperArgs['tuple']['epochDayToDate(uint256)(uint256,uint256,uint256)']>) {
    const result = await this.appClient.send.call(DateTimeWrapperParamsFactory.epochDayToDate(params))
    return result.return as unknown as DateTimeWrapperReturns['epochDayToDate(uint256)(uint256,uint256,uint256)']
  }

  /**
   * Makes a readonly (simulated) call to the DateTimeWrapper smart contract using the `dateToTimestamp(uint256,uint256,uint256)uint256` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async dateToTimestamp(params: CallParams<DateTimeWrapperArgs['obj']['dateToTimestamp(uint256,uint256,uint256)uint256'] | DateTimeWrapperArgs['tuple']['dateToTimestamp(uint256,uint256,uint256)uint256']>) {
    const result = await this.appClient.send.call(DateTimeWrapperParamsFactory.dateToTimestamp(params))
    return result.return as unknown as DateTimeWrapperReturns['dateToTimestamp(uint256,uint256,uint256)uint256']
  }

  /**
   * Makes a readonly (simulated) call to the DateTimeWrapper smart contract using the `timestampToDate(uint256)(uint256,uint256,uint256)` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async timestampToDate(params: CallParams<DateTimeWrapperArgs['obj']['timestampToDate(uint256)(uint256,uint256,uint256)'] | DateTimeWrapperArgs['tuple']['timestampToDate(uint256)(uint256,uint256,uint256)']>) {
    const result = await this.appClient.send.call(DateTimeWrapperParamsFactory.timestampToDate(params))
    return result.return as unknown as DateTimeWrapperReturns['timestampToDate(uint256)(uint256,uint256,uint256)']
  }

  /**
   * Makes a readonly (simulated) call to the DateTimeWrapper smart contract using the `isLeapYear(uint256)bool` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async isLeapYear(params: CallParams<DateTimeWrapperArgs['obj']['isLeapYear(uint256)bool'] | DateTimeWrapperArgs['tuple']['isLeapYear(uint256)bool']>) {
    const result = await this.appClient.send.call(DateTimeWrapperParamsFactory.isLeapYear(params))
    return result.return as unknown as DateTimeWrapperReturns['isLeapYear(uint256)bool']
  }

  /**
   * Makes a readonly (simulated) call to the DateTimeWrapper smart contract using the `daysInMonth(uint256,uint256)uint256` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async daysInMonth(params: CallParams<DateTimeWrapperArgs['obj']['daysInMonth(uint256,uint256)uint256'] | DateTimeWrapperArgs['tuple']['daysInMonth(uint256,uint256)uint256']>) {
    const result = await this.appClient.send.call(DateTimeWrapperParamsFactory.daysInMonth(params))
    return result.return as unknown as DateTimeWrapperReturns['daysInMonth(uint256,uint256)uint256']
  }

  /**
   * Makes a readonly (simulated) call to the DateTimeWrapper smart contract using the `weekday(uint256)uint256` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async weekday(params: CallParams<DateTimeWrapperArgs['obj']['weekday(uint256)uint256'] | DateTimeWrapperArgs['tuple']['weekday(uint256)uint256']>) {
    const result = await this.appClient.send.call(DateTimeWrapperParamsFactory.weekday(params))
    return result.return as unknown as DateTimeWrapperReturns['weekday(uint256)uint256']
  }

  /**
   * Methods to access state for the current DateTimeWrapper app
   */
  state = {
  }

  public newGroup(): DateTimeWrapperComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a dateToEpochDay(uint256,uint256,uint256)uint256 method call against the DateTimeWrapper contract
       */
      dateToEpochDay(params: CallParams<DateTimeWrapperArgs['obj']['dateToEpochDay(uint256,uint256,uint256)uint256'] | DateTimeWrapperArgs['tuple']['dateToEpochDay(uint256,uint256,uint256)uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.dateToEpochDay(params)))
        resultMappers.push((v) => client.decodeReturnValue('dateToEpochDay(uint256,uint256,uint256)uint256', v))
        return this
      },
      /**
       * Add a epochDayToDate(uint256)(uint256,uint256,uint256) method call against the DateTimeWrapper contract
       */
      epochDayToDate(params: CallParams<DateTimeWrapperArgs['obj']['epochDayToDate(uint256)(uint256,uint256,uint256)'] | DateTimeWrapperArgs['tuple']['epochDayToDate(uint256)(uint256,uint256,uint256)']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.epochDayToDate(params)))
        resultMappers.push((v) => client.decodeReturnValue('epochDayToDate(uint256)(uint256,uint256,uint256)', v))
        return this
      },
      /**
       * Add a dateToTimestamp(uint256,uint256,uint256)uint256 method call against the DateTimeWrapper contract
       */
      dateToTimestamp(params: CallParams<DateTimeWrapperArgs['obj']['dateToTimestamp(uint256,uint256,uint256)uint256'] | DateTimeWrapperArgs['tuple']['dateToTimestamp(uint256,uint256,uint256)uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.dateToTimestamp(params)))
        resultMappers.push((v) => client.decodeReturnValue('dateToTimestamp(uint256,uint256,uint256)uint256', v))
        return this
      },
      /**
       * Add a timestampToDate(uint256)(uint256,uint256,uint256) method call against the DateTimeWrapper contract
       */
      timestampToDate(params: CallParams<DateTimeWrapperArgs['obj']['timestampToDate(uint256)(uint256,uint256,uint256)'] | DateTimeWrapperArgs['tuple']['timestampToDate(uint256)(uint256,uint256,uint256)']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.timestampToDate(params)))
        resultMappers.push((v) => client.decodeReturnValue('timestampToDate(uint256)(uint256,uint256,uint256)', v))
        return this
      },
      /**
       * Add a isLeapYear(uint256)bool method call against the DateTimeWrapper contract
       */
      isLeapYear(params: CallParams<DateTimeWrapperArgs['obj']['isLeapYear(uint256)bool'] | DateTimeWrapperArgs['tuple']['isLeapYear(uint256)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.isLeapYear(params)))
        resultMappers.push((v) => client.decodeReturnValue('isLeapYear(uint256)bool', v))
        return this
      },
      /**
       * Add a daysInMonth(uint256,uint256)uint256 method call against the DateTimeWrapper contract
       */
      daysInMonth(params: CallParams<DateTimeWrapperArgs['obj']['daysInMonth(uint256,uint256)uint256'] | DateTimeWrapperArgs['tuple']['daysInMonth(uint256,uint256)uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.daysInMonth(params)))
        resultMappers.push((v) => client.decodeReturnValue('daysInMonth(uint256,uint256)uint256', v))
        return this
      },
      /**
       * Add a weekday(uint256)uint256 method call against the DateTimeWrapper contract
       */
      weekday(params: CallParams<DateTimeWrapperArgs['obj']['weekday(uint256)uint256'] | DateTimeWrapperArgs['tuple']['weekday(uint256)uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.weekday(params)))
        resultMappers.push((v) => client.decodeReturnValue('weekday(uint256)uint256', v))
        return this
      },
      /**
       * Add a clear state call to the DateTimeWrapper contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as DateTimeWrapperComposer
  }
}
export type DateTimeWrapperComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the dateToEpochDay(uint256,uint256,uint256)uint256 ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  dateToEpochDay(params?: CallParams<DateTimeWrapperArgs['obj']['dateToEpochDay(uint256,uint256,uint256)uint256'] | DateTimeWrapperArgs['tuple']['dateToEpochDay(uint256,uint256,uint256)uint256']>): DateTimeWrapperComposer<[...TReturns, DateTimeWrapperReturns['dateToEpochDay(uint256,uint256,uint256)uint256'] | undefined]>

  /**
   * Calls the epochDayToDate(uint256)(uint256,uint256,uint256) ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  epochDayToDate(params?: CallParams<DateTimeWrapperArgs['obj']['epochDayToDate(uint256)(uint256,uint256,uint256)'] | DateTimeWrapperArgs['tuple']['epochDayToDate(uint256)(uint256,uint256,uint256)']>): DateTimeWrapperComposer<[...TReturns, DateTimeWrapperReturns['epochDayToDate(uint256)(uint256,uint256,uint256)'] | undefined]>

  /**
   * Calls the dateToTimestamp(uint256,uint256,uint256)uint256 ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  dateToTimestamp(params?: CallParams<DateTimeWrapperArgs['obj']['dateToTimestamp(uint256,uint256,uint256)uint256'] | DateTimeWrapperArgs['tuple']['dateToTimestamp(uint256,uint256,uint256)uint256']>): DateTimeWrapperComposer<[...TReturns, DateTimeWrapperReturns['dateToTimestamp(uint256,uint256,uint256)uint256'] | undefined]>

  /**
   * Calls the timestampToDate(uint256)(uint256,uint256,uint256) ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  timestampToDate(params?: CallParams<DateTimeWrapperArgs['obj']['timestampToDate(uint256)(uint256,uint256,uint256)'] | DateTimeWrapperArgs['tuple']['timestampToDate(uint256)(uint256,uint256,uint256)']>): DateTimeWrapperComposer<[...TReturns, DateTimeWrapperReturns['timestampToDate(uint256)(uint256,uint256,uint256)'] | undefined]>

  /**
   * Calls the isLeapYear(uint256)bool ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  isLeapYear(params?: CallParams<DateTimeWrapperArgs['obj']['isLeapYear(uint256)bool'] | DateTimeWrapperArgs['tuple']['isLeapYear(uint256)bool']>): DateTimeWrapperComposer<[...TReturns, DateTimeWrapperReturns['isLeapYear(uint256)bool'] | undefined]>

  /**
   * Calls the daysInMonth(uint256,uint256)uint256 ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  daysInMonth(params?: CallParams<DateTimeWrapperArgs['obj']['daysInMonth(uint256,uint256)uint256'] | DateTimeWrapperArgs['tuple']['daysInMonth(uint256,uint256)uint256']>): DateTimeWrapperComposer<[...TReturns, DateTimeWrapperReturns['daysInMonth(uint256,uint256)uint256'] | undefined]>

  /**
   * Calls the weekday(uint256)uint256 ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  weekday(params?: CallParams<DateTimeWrapperArgs['obj']['weekday(uint256)uint256'] | DateTimeWrapperArgs['tuple']['weekday(uint256)uint256']>): DateTimeWrapperComposer<[...TReturns, DateTimeWrapperReturns['weekday(uint256)uint256'] | undefined]>

  /**
   * Makes a clear_state call to an existing instance of the DateTimeWrapper smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): DateTimeWrapperComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): DateTimeWrapperComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<DateTimeWrapperComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<DateTimeWrapperComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<DateTimeWrapperComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<DateTimeWrapperComposerResults<TReturns>>
}
export type DateTimeWrapperComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

