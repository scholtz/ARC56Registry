/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"ShortStringsTest","structs":{},"methods":[{"name":"toShortString","args":[{"type":"string","name":"str"}],"returns":{"type":"byte[32]"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"toString","args":[{"type":"byte[32]","name":"sstr"}],"returns":{"type":"string"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"byteLength","args":[{"type":"byte[32]","name":"sstr"}],"returns":{"type":"uint512"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"roundTrip","args":[{"type":"string","name":"str"}],"returns":{"type":"string"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}}],"arcs":[22,28],"networks":{},"state":{"schema":{"global":{"ints":0,"bytes":0},"local":{"ints":0,"bytes":0}},"keys":{"global":{},"local":{},"box":{}},"maps":{"global":{},"local":{},"box":{}}},"bareActions":{"create":[],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[79],"errorMessage":"StringTooLong"},{"pc":[286,365],"errorMessage":"invalid array length header"},{"pc":[294,373],"errorMessage":"invalid number of bytes for arc4.dynamic_array<arc4.uint8>"},{"pc":[314,339],"errorMessage":"invalid number of bytes for arc4.static_array<arc4.uint8, 32>"},{"pc":[349],"errorMessage":"overflow"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"events":[]} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


/**
 * The argument types for the ShortStringsTest contract
 */
export type ShortStringsTestArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'toShortString(string)byte[32]': {
      str: string
    }
    'toString(byte[32])string': {
      sstr: Uint8Array
    }
    'byteLength(byte[32])uint512': {
      sstr: Uint8Array
    }
    'roundTrip(string)string': {
      str: string
    }
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'toShortString(string)byte[32]': [str: string]
    'toString(byte[32])string': [sstr: Uint8Array]
    'byteLength(byte[32])uint512': [sstr: Uint8Array]
    'roundTrip(string)string': [str: string]
  }
}

/**
 * The return type for each method
 */
export type ShortStringsTestReturns = {
  'toShortString(string)byte[32]': Uint8Array
  'toString(byte[32])string': string
  'byteLength(byte[32])uint512': bigint
  'roundTrip(string)string': string
}

/**
 * Defines the types of available calls and state of the ShortStringsTest smart contract.
 */
export type ShortStringsTestTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'toShortString(string)byte[32]' | 'toShortString', {
      argsObj: ShortStringsTestArgs['obj']['toShortString(string)byte[32]']
      argsTuple: ShortStringsTestArgs['tuple']['toShortString(string)byte[32]']
      returns: ShortStringsTestReturns['toShortString(string)byte[32]']
    }>
    & Record<'toString(byte[32])string' | 'toString', {
      argsObj: ShortStringsTestArgs['obj']['toString(byte[32])string']
      argsTuple: ShortStringsTestArgs['tuple']['toString(byte[32])string']
      returns: ShortStringsTestReturns['toString(byte[32])string']
    }>
    & Record<'byteLength(byte[32])uint512' | 'byteLength', {
      argsObj: ShortStringsTestArgs['obj']['byteLength(byte[32])uint512']
      argsTuple: ShortStringsTestArgs['tuple']['byteLength(byte[32])uint512']
      returns: ShortStringsTestReturns['byteLength(byte[32])uint512']
    }>
    & Record<'roundTrip(string)string' | 'roundTrip', {
      argsObj: ShortStringsTestArgs['obj']['roundTrip(string)string']
      argsTuple: ShortStringsTestArgs['tuple']['roundTrip(string)string']
      returns: ShortStringsTestReturns['roundTrip(string)string']
    }>
}

/**
 * Defines the possible abi call signatures.
 */
export type ShortStringsTestSignatures = keyof ShortStringsTestTypes['methods']
/**
 * Defines the possible abi call signatures for methods that return a non-void value.
 */
export type ShortStringsTestNonVoidMethodSignatures = keyof ShortStringsTestTypes['methods'] extends infer T ? T extends keyof ShortStringsTestTypes['methods'] ? MethodReturn<T> extends void ? never : T  : never : never
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the ShortStringsTest smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends ShortStringsTestSignatures> = ShortStringsTestTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the ShortStringsTest smart contract to the method's return type
 */
export type MethodReturn<TSignature extends ShortStringsTestSignatures> = ShortStringsTestTypes['methods'][TSignature]['returns']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the ShortStringsTest smart contract
 */
export abstract class ShortStringsTestParamsFactory {
  /**
   * Constructs a no op call for the toShortString(string)byte[32] ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static toShortString(params: CallParams<ShortStringsTestArgs['obj']['toShortString(string)byte[32]'] | ShortStringsTestArgs['tuple']['toShortString(string)byte[32]']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'toShortString(string)byte[32]' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.str],
    }
  }
  /**
   * Constructs a no op call for the toString(byte[32])string ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static toString(params: CallParams<ShortStringsTestArgs['obj']['toString(byte[32])string'] | ShortStringsTestArgs['tuple']['toString(byte[32])string']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'toString(byte[32])string' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.sstr],
    }
  }
  /**
   * Constructs a no op call for the byteLength(byte[32])uint512 ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static byteLength(params: CallParams<ShortStringsTestArgs['obj']['byteLength(byte[32])uint512'] | ShortStringsTestArgs['tuple']['byteLength(byte[32])uint512']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'byteLength(byte[32])uint512' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.sstr],
    }
  }
  /**
   * Constructs a no op call for the roundTrip(string)string ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static roundTrip(params: CallParams<ShortStringsTestArgs['obj']['roundTrip(string)string'] | ShortStringsTestArgs['tuple']['roundTrip(string)string']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'roundTrip(string)string' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.str],
    }
  }
}

/**
 * A client to make calls to the ShortStringsTest smart contract
 */
export class ShortStringsTestClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `ShortStringsTestClient`
   *
   * @param appClient An `AppClient` instance which has been created with the ShortStringsTest app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `ShortStringsTestClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Checks for decode errors on the given return value and maps the return value to the return type for the given method
   * @returns The typed return value or undefined if there was no value
   */
  decodeReturnValue<TSignature extends ShortStringsTestNonVoidMethodSignatures>(method: TSignature, returnValue: ABIReturn | undefined) {
    return returnValue !== undefined ? getArc56ReturnValue<MethodReturn<TSignature>>(returnValue, this.appClient.getABIMethod(method), APP_SPEC.structs) : undefined
  }

  /**
   * Returns a new `ShortStringsTestClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<ShortStringsTestClient> {
    return new ShortStringsTestClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `ShortStringsTestClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<ShortStringsTestClient> {
    return new ShortStringsTestClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Makes a clear_state call to an existing instance of the ShortStringsTest smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the ShortStringsTest smart contract using the `toShortString(string)byte[32]` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    toShortString: (params: CallParams<ShortStringsTestArgs['obj']['toShortString(string)byte[32]'] | ShortStringsTestArgs['tuple']['toShortString(string)byte[32]']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(ShortStringsTestParamsFactory.toShortString(params))
    },

    /**
     * Makes a call to the ShortStringsTest smart contract using the `toString(byte[32])string` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    toString: (params: CallParams<ShortStringsTestArgs['obj']['toString(byte[32])string'] | ShortStringsTestArgs['tuple']['toString(byte[32])string']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(ShortStringsTestParamsFactory.toString(params))
    },

    /**
     * Makes a call to the ShortStringsTest smart contract using the `byteLength(byte[32])uint512` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    byteLength: (params: CallParams<ShortStringsTestArgs['obj']['byteLength(byte[32])uint512'] | ShortStringsTestArgs['tuple']['byteLength(byte[32])uint512']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(ShortStringsTestParamsFactory.byteLength(params))
    },

    /**
     * Makes a call to the ShortStringsTest smart contract using the `roundTrip(string)string` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    roundTrip: (params: CallParams<ShortStringsTestArgs['obj']['roundTrip(string)string'] | ShortStringsTestArgs['tuple']['roundTrip(string)string']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(ShortStringsTestParamsFactory.roundTrip(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Makes a clear_state call to an existing instance of the ShortStringsTest smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the ShortStringsTest smart contract using the `toShortString(string)byte[32]` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    toShortString: (params: CallParams<ShortStringsTestArgs['obj']['toShortString(string)byte[32]'] | ShortStringsTestArgs['tuple']['toShortString(string)byte[32]']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(ShortStringsTestParamsFactory.toShortString(params))
    },

    /**
     * Makes a call to the ShortStringsTest smart contract using the `toString(byte[32])string` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    toString: (params: CallParams<ShortStringsTestArgs['obj']['toString(byte[32])string'] | ShortStringsTestArgs['tuple']['toString(byte[32])string']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(ShortStringsTestParamsFactory.toString(params))
    },

    /**
     * Makes a call to the ShortStringsTest smart contract using the `byteLength(byte[32])uint512` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    byteLength: (params: CallParams<ShortStringsTestArgs['obj']['byteLength(byte[32])uint512'] | ShortStringsTestArgs['tuple']['byteLength(byte[32])uint512']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(ShortStringsTestParamsFactory.byteLength(params))
    },

    /**
     * Makes a call to the ShortStringsTest smart contract using the `roundTrip(string)string` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    roundTrip: (params: CallParams<ShortStringsTestArgs['obj']['roundTrip(string)string'] | ShortStringsTestArgs['tuple']['roundTrip(string)string']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(ShortStringsTestParamsFactory.roundTrip(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Makes a clear_state call to an existing instance of the ShortStringsTest smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the ShortStringsTest smart contract using the `toShortString(string)byte[32]` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    toShortString: async (params: CallParams<ShortStringsTestArgs['obj']['toShortString(string)byte[32]'] | ShortStringsTestArgs['tuple']['toShortString(string)byte[32]']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(ShortStringsTestParamsFactory.toShortString(params))
      return {...result, return: result.return as unknown as (undefined | ShortStringsTestReturns['toShortString(string)byte[32]'])}
    },

    /**
     * Makes a call to the ShortStringsTest smart contract using the `toString(byte[32])string` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    toString: async (params: CallParams<ShortStringsTestArgs['obj']['toString(byte[32])string'] | ShortStringsTestArgs['tuple']['toString(byte[32])string']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(ShortStringsTestParamsFactory.toString(params))
      return {...result, return: result.return as unknown as (undefined | ShortStringsTestReturns['toString(byte[32])string'])}
    },

    /**
     * Makes a call to the ShortStringsTest smart contract using the `byteLength(byte[32])uint512` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    byteLength: async (params: CallParams<ShortStringsTestArgs['obj']['byteLength(byte[32])uint512'] | ShortStringsTestArgs['tuple']['byteLength(byte[32])uint512']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(ShortStringsTestParamsFactory.byteLength(params))
      return {...result, return: result.return as unknown as (undefined | ShortStringsTestReturns['byteLength(byte[32])uint512'])}
    },

    /**
     * Makes a call to the ShortStringsTest smart contract using the `roundTrip(string)string` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    roundTrip: async (params: CallParams<ShortStringsTestArgs['obj']['roundTrip(string)string'] | ShortStringsTestArgs['tuple']['roundTrip(string)string']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(ShortStringsTestParamsFactory.roundTrip(params))
      return {...result, return: result.return as unknown as (undefined | ShortStringsTestReturns['roundTrip(string)string'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new ShortStringsTestClient(this.appClient.clone(params))
  }

  /**
   * Makes a readonly (simulated) call to the ShortStringsTest smart contract using the `toShortString(string)byte[32]` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async toShortString(params: CallParams<ShortStringsTestArgs['obj']['toShortString(string)byte[32]'] | ShortStringsTestArgs['tuple']['toShortString(string)byte[32]']>) {
    const result = await this.appClient.send.call(ShortStringsTestParamsFactory.toShortString(params))
    return result.return as unknown as ShortStringsTestReturns['toShortString(string)byte[32]']
  }

  /**
   * Makes a readonly (simulated) call to the ShortStringsTest smart contract using the `toString(byte[32])string` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async toString(params: CallParams<ShortStringsTestArgs['obj']['toString(byte[32])string'] | ShortStringsTestArgs['tuple']['toString(byte[32])string']>) {
    const result = await this.appClient.send.call(ShortStringsTestParamsFactory.toString(params))
    return result.return as unknown as ShortStringsTestReturns['toString(byte[32])string']
  }

  /**
   * Makes a readonly (simulated) call to the ShortStringsTest smart contract using the `byteLength(byte[32])uint512` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async byteLength(params: CallParams<ShortStringsTestArgs['obj']['byteLength(byte[32])uint512'] | ShortStringsTestArgs['tuple']['byteLength(byte[32])uint512']>) {
    const result = await this.appClient.send.call(ShortStringsTestParamsFactory.byteLength(params))
    return result.return as unknown as ShortStringsTestReturns['byteLength(byte[32])uint512']
  }

  /**
   * Makes a readonly (simulated) call to the ShortStringsTest smart contract using the `roundTrip(string)string` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async roundTrip(params: CallParams<ShortStringsTestArgs['obj']['roundTrip(string)string'] | ShortStringsTestArgs['tuple']['roundTrip(string)string']>) {
    const result = await this.appClient.send.call(ShortStringsTestParamsFactory.roundTrip(params))
    return result.return as unknown as ShortStringsTestReturns['roundTrip(string)string']
  }

  /**
   * Methods to access state for the current ShortStringsTest app
   */
  state = {
  }

  public newGroup(): ShortStringsTestComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a toShortString(string)byte[32] method call against the ShortStringsTest contract
       */
      toShortString(params: CallParams<ShortStringsTestArgs['obj']['toShortString(string)byte[32]'] | ShortStringsTestArgs['tuple']['toShortString(string)byte[32]']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.toShortString(params)))
        resultMappers.push((v) => client.decodeReturnValue('toShortString(string)byte[32]', v))
        return this
      },
      /**
       * Add a toString(byte[32])string method call against the ShortStringsTest contract
       */
      toString(params: CallParams<ShortStringsTestArgs['obj']['toString(byte[32])string'] | ShortStringsTestArgs['tuple']['toString(byte[32])string']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.toString(params)))
        resultMappers.push((v) => client.decodeReturnValue('toString(byte[32])string', v))
        return this
      },
      /**
       * Add a byteLength(byte[32])uint512 method call against the ShortStringsTest contract
       */
      byteLength(params: CallParams<ShortStringsTestArgs['obj']['byteLength(byte[32])uint512'] | ShortStringsTestArgs['tuple']['byteLength(byte[32])uint512']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.byteLength(params)))
        resultMappers.push((v) => client.decodeReturnValue('byteLength(byte[32])uint512', v))
        return this
      },
      /**
       * Add a roundTrip(string)string method call against the ShortStringsTest contract
       */
      roundTrip(params: CallParams<ShortStringsTestArgs['obj']['roundTrip(string)string'] | ShortStringsTestArgs['tuple']['roundTrip(string)string']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.roundTrip(params)))
        resultMappers.push((v) => client.decodeReturnValue('roundTrip(string)string', v))
        return this
      },
      /**
       * Add a clear state call to the ShortStringsTest contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as ShortStringsTestComposer
  }
}
export type ShortStringsTestComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the toShortString(string)byte[32] ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  toShortString(params?: CallParams<ShortStringsTestArgs['obj']['toShortString(string)byte[32]'] | ShortStringsTestArgs['tuple']['toShortString(string)byte[32]']>): ShortStringsTestComposer<[...TReturns, ShortStringsTestReturns['toShortString(string)byte[32]'] | undefined]>

  /**
   * Calls the toString(byte[32])string ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  toString(params?: CallParams<ShortStringsTestArgs['obj']['toString(byte[32])string'] | ShortStringsTestArgs['tuple']['toString(byte[32])string']>): ShortStringsTestComposer<[...TReturns, ShortStringsTestReturns['toString(byte[32])string'] | undefined]>

  /**
   * Calls the byteLength(byte[32])uint512 ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  byteLength(params?: CallParams<ShortStringsTestArgs['obj']['byteLength(byte[32])uint512'] | ShortStringsTestArgs['tuple']['byteLength(byte[32])uint512']>): ShortStringsTestComposer<[...TReturns, ShortStringsTestReturns['byteLength(byte[32])uint512'] | undefined]>

  /**
   * Calls the roundTrip(string)string ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  roundTrip(params?: CallParams<ShortStringsTestArgs['obj']['roundTrip(string)string'] | ShortStringsTestArgs['tuple']['roundTrip(string)string']>): ShortStringsTestComposer<[...TReturns, ShortStringsTestReturns['roundTrip(string)string'] | undefined]>

  /**
   * Makes a clear_state call to an existing instance of the ShortStringsTest smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): ShortStringsTestComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): ShortStringsTestComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<ShortStringsTestComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<ShortStringsTestComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<ShortStringsTestComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<ShortStringsTestComposerResults<TReturns>>
}
export type ShortStringsTestComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

