/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"LotteryTest","structs":{},"methods":[{"name":"manager","args":[],"returns":{"type":"address"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"ticketPrice","args":[],"returns":{"type":"uint512"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"playerCount","args":[],"returns":{"type":"uint512"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"prizePool","args":[],"returns":{"type":"uint512"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"isActive","args":[],"returns":{"type":"bool"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"lastWinner","args":[],"returns":{"type":"address"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"roundNumber","args":[],"returns":{"type":"uint512"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"setTicketPrice","args":[{"type":"uint512","name":"price"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}},{"name":"enter","args":[{"type":"address","name":"player"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}},{"name":"getPlayer","args":[{"type":"uint512","name":"index"}],"returns":{"type":"address"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"pickWinner","args":[{"type":"uint512","name":"seed"}],"returns":{"type":"address"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}},{"name":"toggleActive","args":[],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}}],"arcs":[22,28],"desc":" @dev Simple lottery using deterministic seed for testing.\n Players enter with tickets, manager picks winner using seed.","networks":{},"state":{"schema":{"global":{"ints":1,"bytes":6},"local":{"ints":0,"bytes":0}},"keys":{"global":{"_manager":{"keyType":"AVMString","valueType":"address","key":"X21hbmFnZXI="},"_ticketPrice":{"keyType":"AVMString","valueType":"AVMBytes","key":"X3RpY2tldFByaWNl"},"_playerCount":{"keyType":"AVMString","valueType":"AVMBytes","key":"X3BsYXllckNvdW50"},"_prizePool":{"keyType":"AVMString","valueType":"AVMBytes","key":"X3ByaXplUG9vbA=="},"_isActive":{"keyType":"AVMString","valueType":"AVMUint64","key":"X2lzQWN0aXZl"},"_lastWinner":{"keyType":"AVMString","valueType":"address","key":"X2xhc3RXaW5uZXI="},"_roundNumber":{"keyType":"AVMString","valueType":"AVMBytes","key":"X3JvdW5kTnVtYmVy"}},"local":{},"box":{}},"maps":{"global":{},"local":{},"box":{"_players":{"keyType":"AVMBytes","valueType":"address","prefix":"X3BsYXllcnM="}}}},"bareActions":{"create":[],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[449],"errorMessage":"Lottery not active"},{"pc":[554],"errorMessage":"No players"},{"pc":[421,545,624],"errorMessage":"Not manager"},{"pc":[429],"errorMessage":"Round in progress"},{"pc":[362,448,628],"errorMessage":"check _isActive exists"},{"pc":[380],"errorMessage":"check _lastWinner exists"},{"pc":[296,419,543,622],"errorMessage":"check _manager exists"},{"pc":[325,425,453,549],"errorMessage":"check _playerCount exists"},{"pc":[344,482],"errorMessage":"check _prizePool exists"},{"pc":[391,603],"errorMessage":"check _roundNumber exists"},{"pc":[307,487],"errorMessage":"check _ticketPrice exists"},{"pc":[444],"errorMessage":"invalid number of bytes for arc4.static_array<arc4.uint8, 32>"},{"pc":[413,502,537],"errorMessage":"invalid number of bytes for arc4.uint512"},{"pc":[312,330,349,396],"errorMessage":"overflow"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"events":[]} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


/**
 * The argument types for the LotteryTest contract
 */
export type LotteryTestArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'manager()address': Record<string, never>
    'ticketPrice()uint512': Record<string, never>
    'playerCount()uint512': Record<string, never>
    'prizePool()uint512': Record<string, never>
    'isActive()bool': Record<string, never>
    'lastWinner()address': Record<string, never>
    'roundNumber()uint512': Record<string, never>
    'setTicketPrice(uint512)void': {
      price: bigint | number
    }
    'enter(address)void': {
      player: string
    }
    'getPlayer(uint512)address': {
      index: bigint | number
    }
    'pickWinner(uint512)address': {
      seed: bigint | number
    }
    'toggleActive()void': Record<string, never>
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'manager()address': []
    'ticketPrice()uint512': []
    'playerCount()uint512': []
    'prizePool()uint512': []
    'isActive()bool': []
    'lastWinner()address': []
    'roundNumber()uint512': []
    'setTicketPrice(uint512)void': [price: bigint | number]
    'enter(address)void': [player: string]
    'getPlayer(uint512)address': [index: bigint | number]
    'pickWinner(uint512)address': [seed: bigint | number]
    'toggleActive()void': []
  }
}

/**
 * The return type for each method
 */
export type LotteryTestReturns = {
  'manager()address': string
  'ticketPrice()uint512': bigint
  'playerCount()uint512': bigint
  'prizePool()uint512': bigint
  'isActive()bool': boolean
  'lastWinner()address': string
  'roundNumber()uint512': bigint
  'setTicketPrice(uint512)void': void
  'enter(address)void': void
  'getPlayer(uint512)address': string
  'pickWinner(uint512)address': string
  'toggleActive()void': void
}

/**
 * Defines the types of available calls and state of the LotteryTest smart contract.
 */
export type LotteryTestTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'manager()address' | 'manager', {
      argsObj: LotteryTestArgs['obj']['manager()address']
      argsTuple: LotteryTestArgs['tuple']['manager()address']
      returns: LotteryTestReturns['manager()address']
    }>
    & Record<'ticketPrice()uint512' | 'ticketPrice', {
      argsObj: LotteryTestArgs['obj']['ticketPrice()uint512']
      argsTuple: LotteryTestArgs['tuple']['ticketPrice()uint512']
      returns: LotteryTestReturns['ticketPrice()uint512']
    }>
    & Record<'playerCount()uint512' | 'playerCount', {
      argsObj: LotteryTestArgs['obj']['playerCount()uint512']
      argsTuple: LotteryTestArgs['tuple']['playerCount()uint512']
      returns: LotteryTestReturns['playerCount()uint512']
    }>
    & Record<'prizePool()uint512' | 'prizePool', {
      argsObj: LotteryTestArgs['obj']['prizePool()uint512']
      argsTuple: LotteryTestArgs['tuple']['prizePool()uint512']
      returns: LotteryTestReturns['prizePool()uint512']
    }>
    & Record<'isActive()bool' | 'isActive', {
      argsObj: LotteryTestArgs['obj']['isActive()bool']
      argsTuple: LotteryTestArgs['tuple']['isActive()bool']
      returns: LotteryTestReturns['isActive()bool']
    }>
    & Record<'lastWinner()address' | 'lastWinner', {
      argsObj: LotteryTestArgs['obj']['lastWinner()address']
      argsTuple: LotteryTestArgs['tuple']['lastWinner()address']
      returns: LotteryTestReturns['lastWinner()address']
    }>
    & Record<'roundNumber()uint512' | 'roundNumber', {
      argsObj: LotteryTestArgs['obj']['roundNumber()uint512']
      argsTuple: LotteryTestArgs['tuple']['roundNumber()uint512']
      returns: LotteryTestReturns['roundNumber()uint512']
    }>
    & Record<'setTicketPrice(uint512)void' | 'setTicketPrice', {
      argsObj: LotteryTestArgs['obj']['setTicketPrice(uint512)void']
      argsTuple: LotteryTestArgs['tuple']['setTicketPrice(uint512)void']
      returns: LotteryTestReturns['setTicketPrice(uint512)void']
    }>
    & Record<'enter(address)void' | 'enter', {
      argsObj: LotteryTestArgs['obj']['enter(address)void']
      argsTuple: LotteryTestArgs['tuple']['enter(address)void']
      returns: LotteryTestReturns['enter(address)void']
    }>
    & Record<'getPlayer(uint512)address' | 'getPlayer', {
      argsObj: LotteryTestArgs['obj']['getPlayer(uint512)address']
      argsTuple: LotteryTestArgs['tuple']['getPlayer(uint512)address']
      returns: LotteryTestReturns['getPlayer(uint512)address']
    }>
    & Record<'pickWinner(uint512)address' | 'pickWinner', {
      argsObj: LotteryTestArgs['obj']['pickWinner(uint512)address']
      argsTuple: LotteryTestArgs['tuple']['pickWinner(uint512)address']
      returns: LotteryTestReturns['pickWinner(uint512)address']
    }>
    & Record<'toggleActive()void' | 'toggleActive', {
      argsObj: LotteryTestArgs['obj']['toggleActive()void']
      argsTuple: LotteryTestArgs['tuple']['toggleActive()void']
      returns: LotteryTestReturns['toggleActive()void']
    }>
  /**
   * Defines the shape of the state of the application.
   */
  state: {
    global: {
      keys: {
        _manager: string
        _ticketPrice: BinaryState
        _playerCount: BinaryState
        _prizePool: BinaryState
        _isActive: bigint
        _lastWinner: string
        _roundNumber: BinaryState
      }
      maps: {}
    }
    box: {
      keys: {}
      maps: {
        _players: Map<Uint8Array | string, string>
      }
    }
  }
}

/**
 * Defines the possible abi call signatures.
 */
export type LotteryTestSignatures = keyof LotteryTestTypes['methods']
/**
 * Defines the possible abi call signatures for methods that return a non-void value.
 */
export type LotteryTestNonVoidMethodSignatures = keyof LotteryTestTypes['methods'] extends infer T ? T extends keyof LotteryTestTypes['methods'] ? MethodReturn<T> extends void ? never : T  : never : never
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the LotteryTest smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends LotteryTestSignatures> = LotteryTestTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the LotteryTest smart contract to the method's return type
 */
export type MethodReturn<TSignature extends LotteryTestSignatures> = LotteryTestTypes['methods'][TSignature]['returns']

/**
 * Defines the shape of the keyed global state of the application.
 */
export type GlobalKeysState = LotteryTestTypes['state']['global']['keys']

/**
 * Defines the shape of the keyed box state of the application.
 */
export type BoxKeysState = LotteryTestTypes['state']['box']['keys']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the LotteryTest smart contract
 */
export abstract class LotteryTestParamsFactory {
  /**
   * Constructs a no op call for the manager()address ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static manager(params: CallParams<LotteryTestArgs['obj']['manager()address'] | LotteryTestArgs['tuple']['manager()address']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'manager()address' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the ticketPrice()uint512 ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static ticketPrice(params: CallParams<LotteryTestArgs['obj']['ticketPrice()uint512'] | LotteryTestArgs['tuple']['ticketPrice()uint512']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'ticketPrice()uint512' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the playerCount()uint512 ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static playerCount(params: CallParams<LotteryTestArgs['obj']['playerCount()uint512'] | LotteryTestArgs['tuple']['playerCount()uint512']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'playerCount()uint512' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the prizePool()uint512 ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static prizePool(params: CallParams<LotteryTestArgs['obj']['prizePool()uint512'] | LotteryTestArgs['tuple']['prizePool()uint512']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'prizePool()uint512' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the isActive()bool ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static isActive(params: CallParams<LotteryTestArgs['obj']['isActive()bool'] | LotteryTestArgs['tuple']['isActive()bool']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'isActive()bool' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the lastWinner()address ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static lastWinner(params: CallParams<LotteryTestArgs['obj']['lastWinner()address'] | LotteryTestArgs['tuple']['lastWinner()address']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'lastWinner()address' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the roundNumber()uint512 ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static roundNumber(params: CallParams<LotteryTestArgs['obj']['roundNumber()uint512'] | LotteryTestArgs['tuple']['roundNumber()uint512']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'roundNumber()uint512' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the setTicketPrice(uint512)void ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static setTicketPrice(params: CallParams<LotteryTestArgs['obj']['setTicketPrice(uint512)void'] | LotteryTestArgs['tuple']['setTicketPrice(uint512)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'setTicketPrice(uint512)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.price],
    }
  }
  /**
   * Constructs a no op call for the enter(address)void ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static enter(params: CallParams<LotteryTestArgs['obj']['enter(address)void'] | LotteryTestArgs['tuple']['enter(address)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'enter(address)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.player],
    }
  }
  /**
   * Constructs a no op call for the getPlayer(uint512)address ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static getPlayer(params: CallParams<LotteryTestArgs['obj']['getPlayer(uint512)address'] | LotteryTestArgs['tuple']['getPlayer(uint512)address']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'getPlayer(uint512)address' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.index],
    }
  }
  /**
   * Constructs a no op call for the pickWinner(uint512)address ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static pickWinner(params: CallParams<LotteryTestArgs['obj']['pickWinner(uint512)address'] | LotteryTestArgs['tuple']['pickWinner(uint512)address']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'pickWinner(uint512)address' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.seed],
    }
  }
  /**
   * Constructs a no op call for the toggleActive()void ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static toggleActive(params: CallParams<LotteryTestArgs['obj']['toggleActive()void'] | LotteryTestArgs['tuple']['toggleActive()void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'toggleActive()void' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
}

/**
 * A client to make calls to the LotteryTest smart contract
 */
export class LotteryTestClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `LotteryTestClient`
   *
   * @param appClient An `AppClient` instance which has been created with the LotteryTest app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `LotteryTestClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Checks for decode errors on the given return value and maps the return value to the return type for the given method
   * @returns The typed return value or undefined if there was no value
   */
  decodeReturnValue<TSignature extends LotteryTestNonVoidMethodSignatures>(method: TSignature, returnValue: ABIReturn | undefined) {
    return returnValue !== undefined ? getArc56ReturnValue<MethodReturn<TSignature>>(returnValue, this.appClient.getABIMethod(method), APP_SPEC.structs) : undefined
  }

  /**
   * Returns a new `LotteryTestClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<LotteryTestClient> {
    return new LotteryTestClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `LotteryTestClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<LotteryTestClient> {
    return new LotteryTestClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Makes a clear_state call to an existing instance of the LotteryTest smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the LotteryTest smart contract using the `manager()address` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    manager: (params: CallParams<LotteryTestArgs['obj']['manager()address'] | LotteryTestArgs['tuple']['manager()address']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(LotteryTestParamsFactory.manager(params))
    },

    /**
     * Makes a call to the LotteryTest smart contract using the `ticketPrice()uint512` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    ticketPrice: (params: CallParams<LotteryTestArgs['obj']['ticketPrice()uint512'] | LotteryTestArgs['tuple']['ticketPrice()uint512']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(LotteryTestParamsFactory.ticketPrice(params))
    },

    /**
     * Makes a call to the LotteryTest smart contract using the `playerCount()uint512` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    playerCount: (params: CallParams<LotteryTestArgs['obj']['playerCount()uint512'] | LotteryTestArgs['tuple']['playerCount()uint512']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(LotteryTestParamsFactory.playerCount(params))
    },

    /**
     * Makes a call to the LotteryTest smart contract using the `prizePool()uint512` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    prizePool: (params: CallParams<LotteryTestArgs['obj']['prizePool()uint512'] | LotteryTestArgs['tuple']['prizePool()uint512']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(LotteryTestParamsFactory.prizePool(params))
    },

    /**
     * Makes a call to the LotteryTest smart contract using the `isActive()bool` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    isActive: (params: CallParams<LotteryTestArgs['obj']['isActive()bool'] | LotteryTestArgs['tuple']['isActive()bool']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(LotteryTestParamsFactory.isActive(params))
    },

    /**
     * Makes a call to the LotteryTest smart contract using the `lastWinner()address` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    lastWinner: (params: CallParams<LotteryTestArgs['obj']['lastWinner()address'] | LotteryTestArgs['tuple']['lastWinner()address']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(LotteryTestParamsFactory.lastWinner(params))
    },

    /**
     * Makes a call to the LotteryTest smart contract using the `roundNumber()uint512` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    roundNumber: (params: CallParams<LotteryTestArgs['obj']['roundNumber()uint512'] | LotteryTestArgs['tuple']['roundNumber()uint512']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(LotteryTestParamsFactory.roundNumber(params))
    },

    /**
     * Makes a call to the LotteryTest smart contract using the `setTicketPrice(uint512)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    setTicketPrice: (params: CallParams<LotteryTestArgs['obj']['setTicketPrice(uint512)void'] | LotteryTestArgs['tuple']['setTicketPrice(uint512)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(LotteryTestParamsFactory.setTicketPrice(params))
    },

    /**
     * Makes a call to the LotteryTest smart contract using the `enter(address)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    enter: (params: CallParams<LotteryTestArgs['obj']['enter(address)void'] | LotteryTestArgs['tuple']['enter(address)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(LotteryTestParamsFactory.enter(params))
    },

    /**
     * Makes a call to the LotteryTest smart contract using the `getPlayer(uint512)address` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    getPlayer: (params: CallParams<LotteryTestArgs['obj']['getPlayer(uint512)address'] | LotteryTestArgs['tuple']['getPlayer(uint512)address']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(LotteryTestParamsFactory.getPlayer(params))
    },

    /**
     * Makes a call to the LotteryTest smart contract using the `pickWinner(uint512)address` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    pickWinner: (params: CallParams<LotteryTestArgs['obj']['pickWinner(uint512)address'] | LotteryTestArgs['tuple']['pickWinner(uint512)address']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(LotteryTestParamsFactory.pickWinner(params))
    },

    /**
     * Makes a call to the LotteryTest smart contract using the `toggleActive()void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    toggleActive: (params: CallParams<LotteryTestArgs['obj']['toggleActive()void'] | LotteryTestArgs['tuple']['toggleActive()void']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(LotteryTestParamsFactory.toggleActive(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Makes a clear_state call to an existing instance of the LotteryTest smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the LotteryTest smart contract using the `manager()address` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    manager: (params: CallParams<LotteryTestArgs['obj']['manager()address'] | LotteryTestArgs['tuple']['manager()address']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(LotteryTestParamsFactory.manager(params))
    },

    /**
     * Makes a call to the LotteryTest smart contract using the `ticketPrice()uint512` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    ticketPrice: (params: CallParams<LotteryTestArgs['obj']['ticketPrice()uint512'] | LotteryTestArgs['tuple']['ticketPrice()uint512']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(LotteryTestParamsFactory.ticketPrice(params))
    },

    /**
     * Makes a call to the LotteryTest smart contract using the `playerCount()uint512` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    playerCount: (params: CallParams<LotteryTestArgs['obj']['playerCount()uint512'] | LotteryTestArgs['tuple']['playerCount()uint512']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(LotteryTestParamsFactory.playerCount(params))
    },

    /**
     * Makes a call to the LotteryTest smart contract using the `prizePool()uint512` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    prizePool: (params: CallParams<LotteryTestArgs['obj']['prizePool()uint512'] | LotteryTestArgs['tuple']['prizePool()uint512']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(LotteryTestParamsFactory.prizePool(params))
    },

    /**
     * Makes a call to the LotteryTest smart contract using the `isActive()bool` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    isActive: (params: CallParams<LotteryTestArgs['obj']['isActive()bool'] | LotteryTestArgs['tuple']['isActive()bool']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(LotteryTestParamsFactory.isActive(params))
    },

    /**
     * Makes a call to the LotteryTest smart contract using the `lastWinner()address` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    lastWinner: (params: CallParams<LotteryTestArgs['obj']['lastWinner()address'] | LotteryTestArgs['tuple']['lastWinner()address']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(LotteryTestParamsFactory.lastWinner(params))
    },

    /**
     * Makes a call to the LotteryTest smart contract using the `roundNumber()uint512` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    roundNumber: (params: CallParams<LotteryTestArgs['obj']['roundNumber()uint512'] | LotteryTestArgs['tuple']['roundNumber()uint512']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(LotteryTestParamsFactory.roundNumber(params))
    },

    /**
     * Makes a call to the LotteryTest smart contract using the `setTicketPrice(uint512)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    setTicketPrice: (params: CallParams<LotteryTestArgs['obj']['setTicketPrice(uint512)void'] | LotteryTestArgs['tuple']['setTicketPrice(uint512)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(LotteryTestParamsFactory.setTicketPrice(params))
    },

    /**
     * Makes a call to the LotteryTest smart contract using the `enter(address)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    enter: (params: CallParams<LotteryTestArgs['obj']['enter(address)void'] | LotteryTestArgs['tuple']['enter(address)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(LotteryTestParamsFactory.enter(params))
    },

    /**
     * Makes a call to the LotteryTest smart contract using the `getPlayer(uint512)address` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    getPlayer: (params: CallParams<LotteryTestArgs['obj']['getPlayer(uint512)address'] | LotteryTestArgs['tuple']['getPlayer(uint512)address']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(LotteryTestParamsFactory.getPlayer(params))
    },

    /**
     * Makes a call to the LotteryTest smart contract using the `pickWinner(uint512)address` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    pickWinner: (params: CallParams<LotteryTestArgs['obj']['pickWinner(uint512)address'] | LotteryTestArgs['tuple']['pickWinner(uint512)address']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(LotteryTestParamsFactory.pickWinner(params))
    },

    /**
     * Makes a call to the LotteryTest smart contract using the `toggleActive()void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    toggleActive: (params: CallParams<LotteryTestArgs['obj']['toggleActive()void'] | LotteryTestArgs['tuple']['toggleActive()void']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(LotteryTestParamsFactory.toggleActive(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Makes a clear_state call to an existing instance of the LotteryTest smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the LotteryTest smart contract using the `manager()address` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    manager: async (params: CallParams<LotteryTestArgs['obj']['manager()address'] | LotteryTestArgs['tuple']['manager()address']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(LotteryTestParamsFactory.manager(params))
      return {...result, return: result.return as unknown as (undefined | LotteryTestReturns['manager()address'])}
    },

    /**
     * Makes a call to the LotteryTest smart contract using the `ticketPrice()uint512` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    ticketPrice: async (params: CallParams<LotteryTestArgs['obj']['ticketPrice()uint512'] | LotteryTestArgs['tuple']['ticketPrice()uint512']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(LotteryTestParamsFactory.ticketPrice(params))
      return {...result, return: result.return as unknown as (undefined | LotteryTestReturns['ticketPrice()uint512'])}
    },

    /**
     * Makes a call to the LotteryTest smart contract using the `playerCount()uint512` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    playerCount: async (params: CallParams<LotteryTestArgs['obj']['playerCount()uint512'] | LotteryTestArgs['tuple']['playerCount()uint512']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(LotteryTestParamsFactory.playerCount(params))
      return {...result, return: result.return as unknown as (undefined | LotteryTestReturns['playerCount()uint512'])}
    },

    /**
     * Makes a call to the LotteryTest smart contract using the `prizePool()uint512` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    prizePool: async (params: CallParams<LotteryTestArgs['obj']['prizePool()uint512'] | LotteryTestArgs['tuple']['prizePool()uint512']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(LotteryTestParamsFactory.prizePool(params))
      return {...result, return: result.return as unknown as (undefined | LotteryTestReturns['prizePool()uint512'])}
    },

    /**
     * Makes a call to the LotteryTest smart contract using the `isActive()bool` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    isActive: async (params: CallParams<LotteryTestArgs['obj']['isActive()bool'] | LotteryTestArgs['tuple']['isActive()bool']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(LotteryTestParamsFactory.isActive(params))
      return {...result, return: result.return as unknown as (undefined | LotteryTestReturns['isActive()bool'])}
    },

    /**
     * Makes a call to the LotteryTest smart contract using the `lastWinner()address` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    lastWinner: async (params: CallParams<LotteryTestArgs['obj']['lastWinner()address'] | LotteryTestArgs['tuple']['lastWinner()address']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(LotteryTestParamsFactory.lastWinner(params))
      return {...result, return: result.return as unknown as (undefined | LotteryTestReturns['lastWinner()address'])}
    },

    /**
     * Makes a call to the LotteryTest smart contract using the `roundNumber()uint512` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    roundNumber: async (params: CallParams<LotteryTestArgs['obj']['roundNumber()uint512'] | LotteryTestArgs['tuple']['roundNumber()uint512']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(LotteryTestParamsFactory.roundNumber(params))
      return {...result, return: result.return as unknown as (undefined | LotteryTestReturns['roundNumber()uint512'])}
    },

    /**
     * Makes a call to the LotteryTest smart contract using the `setTicketPrice(uint512)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    setTicketPrice: async (params: CallParams<LotteryTestArgs['obj']['setTicketPrice(uint512)void'] | LotteryTestArgs['tuple']['setTicketPrice(uint512)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(LotteryTestParamsFactory.setTicketPrice(params))
      return {...result, return: result.return as unknown as (undefined | LotteryTestReturns['setTicketPrice(uint512)void'])}
    },

    /**
     * Makes a call to the LotteryTest smart contract using the `enter(address)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    enter: async (params: CallParams<LotteryTestArgs['obj']['enter(address)void'] | LotteryTestArgs['tuple']['enter(address)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(LotteryTestParamsFactory.enter(params))
      return {...result, return: result.return as unknown as (undefined | LotteryTestReturns['enter(address)void'])}
    },

    /**
     * Makes a call to the LotteryTest smart contract using the `getPlayer(uint512)address` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    getPlayer: async (params: CallParams<LotteryTestArgs['obj']['getPlayer(uint512)address'] | LotteryTestArgs['tuple']['getPlayer(uint512)address']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(LotteryTestParamsFactory.getPlayer(params))
      return {...result, return: result.return as unknown as (undefined | LotteryTestReturns['getPlayer(uint512)address'])}
    },

    /**
     * Makes a call to the LotteryTest smart contract using the `pickWinner(uint512)address` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    pickWinner: async (params: CallParams<LotteryTestArgs['obj']['pickWinner(uint512)address'] | LotteryTestArgs['tuple']['pickWinner(uint512)address']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(LotteryTestParamsFactory.pickWinner(params))
      return {...result, return: result.return as unknown as (undefined | LotteryTestReturns['pickWinner(uint512)address'])}
    },

    /**
     * Makes a call to the LotteryTest smart contract using the `toggleActive()void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    toggleActive: async (params: CallParams<LotteryTestArgs['obj']['toggleActive()void'] | LotteryTestArgs['tuple']['toggleActive()void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(LotteryTestParamsFactory.toggleActive(params))
      return {...result, return: result.return as unknown as (undefined | LotteryTestReturns['toggleActive()void'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new LotteryTestClient(this.appClient.clone(params))
  }

  /**
   * Makes a readonly (simulated) call to the LotteryTest smart contract using the `manager()address` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async manager(params: CallParams<LotteryTestArgs['obj']['manager()address'] | LotteryTestArgs['tuple']['manager()address']> = {args: []}) {
    const result = await this.appClient.send.call(LotteryTestParamsFactory.manager(params))
    return result.return as unknown as LotteryTestReturns['manager()address']
  }

  /**
   * Makes a readonly (simulated) call to the LotteryTest smart contract using the `ticketPrice()uint512` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async ticketPrice(params: CallParams<LotteryTestArgs['obj']['ticketPrice()uint512'] | LotteryTestArgs['tuple']['ticketPrice()uint512']> = {args: []}) {
    const result = await this.appClient.send.call(LotteryTestParamsFactory.ticketPrice(params))
    return result.return as unknown as LotteryTestReturns['ticketPrice()uint512']
  }

  /**
   * Makes a readonly (simulated) call to the LotteryTest smart contract using the `playerCount()uint512` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async playerCount(params: CallParams<LotteryTestArgs['obj']['playerCount()uint512'] | LotteryTestArgs['tuple']['playerCount()uint512']> = {args: []}) {
    const result = await this.appClient.send.call(LotteryTestParamsFactory.playerCount(params))
    return result.return as unknown as LotteryTestReturns['playerCount()uint512']
  }

  /**
   * Makes a readonly (simulated) call to the LotteryTest smart contract using the `prizePool()uint512` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async prizePool(params: CallParams<LotteryTestArgs['obj']['prizePool()uint512'] | LotteryTestArgs['tuple']['prizePool()uint512']> = {args: []}) {
    const result = await this.appClient.send.call(LotteryTestParamsFactory.prizePool(params))
    return result.return as unknown as LotteryTestReturns['prizePool()uint512']
  }

  /**
   * Makes a readonly (simulated) call to the LotteryTest smart contract using the `isActive()bool` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async isActive(params: CallParams<LotteryTestArgs['obj']['isActive()bool'] | LotteryTestArgs['tuple']['isActive()bool']> = {args: []}) {
    const result = await this.appClient.send.call(LotteryTestParamsFactory.isActive(params))
    return result.return as unknown as LotteryTestReturns['isActive()bool']
  }

  /**
   * Makes a readonly (simulated) call to the LotteryTest smart contract using the `lastWinner()address` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async lastWinner(params: CallParams<LotteryTestArgs['obj']['lastWinner()address'] | LotteryTestArgs['tuple']['lastWinner()address']> = {args: []}) {
    const result = await this.appClient.send.call(LotteryTestParamsFactory.lastWinner(params))
    return result.return as unknown as LotteryTestReturns['lastWinner()address']
  }

  /**
   * Makes a readonly (simulated) call to the LotteryTest smart contract using the `roundNumber()uint512` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async roundNumber(params: CallParams<LotteryTestArgs['obj']['roundNumber()uint512'] | LotteryTestArgs['tuple']['roundNumber()uint512']> = {args: []}) {
    const result = await this.appClient.send.call(LotteryTestParamsFactory.roundNumber(params))
    return result.return as unknown as LotteryTestReturns['roundNumber()uint512']
  }

  /**
   * Makes a readonly (simulated) call to the LotteryTest smart contract using the `getPlayer(uint512)address` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async getPlayer(params: CallParams<LotteryTestArgs['obj']['getPlayer(uint512)address'] | LotteryTestArgs['tuple']['getPlayer(uint512)address']>) {
    const result = await this.appClient.send.call(LotteryTestParamsFactory.getPlayer(params))
    return result.return as unknown as LotteryTestReturns['getPlayer(uint512)address']
  }

  /**
   * Methods to access state for the current LotteryTest app
   */
  state = {
    /**
     * Methods to access global state for the current LotteryTest app
     */
    global: {
      /**
       * Get all current keyed values from global state
       */
      getAll: async (): Promise<Partial<Expand<GlobalKeysState>>> => {
        const result = await this.appClient.state.global.getAll()
        return {
          _manager: result._manager,
          _ticketPrice: new BinaryStateValue(result._ticketPrice),
          _playerCount: new BinaryStateValue(result._playerCount),
          _prizePool: new BinaryStateValue(result._prizePool),
          _isActive: result._isActive,
          _lastWinner: result._lastWinner,
          _roundNumber: new BinaryStateValue(result._roundNumber),
        }
      },
      /**
       * Get the current value of the _manager key in global state
       */
      _manager: async (): Promise<string | undefined> => { return (await this.appClient.state.global.getValue("_manager")) as string | undefined },
      /**
       * Get the current value of the _ticketPrice key in global state
       */
      _ticketPrice: async (): Promise<BinaryState> => { return new BinaryStateValue((await this.appClient.state.global.getValue("_ticketPrice")) as Uint8Array | undefined) },
      /**
       * Get the current value of the _playerCount key in global state
       */
      _playerCount: async (): Promise<BinaryState> => { return new BinaryStateValue((await this.appClient.state.global.getValue("_playerCount")) as Uint8Array | undefined) },
      /**
       * Get the current value of the _prizePool key in global state
       */
      _prizePool: async (): Promise<BinaryState> => { return new BinaryStateValue((await this.appClient.state.global.getValue("_prizePool")) as Uint8Array | undefined) },
      /**
       * Get the current value of the _isActive key in global state
       */
      _isActive: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("_isActive")) as bigint | undefined },
      /**
       * Get the current value of the _lastWinner key in global state
       */
      _lastWinner: async (): Promise<string | undefined> => { return (await this.appClient.state.global.getValue("_lastWinner")) as string | undefined },
      /**
       * Get the current value of the _roundNumber key in global state
       */
      _roundNumber: async (): Promise<BinaryState> => { return new BinaryStateValue((await this.appClient.state.global.getValue("_roundNumber")) as Uint8Array | undefined) },
    },
    /**
     * Methods to access box state for the current LotteryTest app
     */
    box: {
      /**
       * Get all current keyed values from box state
       */
      getAll: async (): Promise<Partial<Expand<BoxKeysState>>> => {
        const result = await this.appClient.state.box.getAll()
        return {
        }
      },
      /**
       * Get values from the _players map in box state
       */
      _players: {
        /**
         * Get all current values of the _players map in box state
         */
        getMap: async (): Promise<Map<Uint8Array, string>> => { return (await this.appClient.state.box.getMap("_players")) as Map<Uint8Array, string> },
        /**
         * Get a current value of the _players map by key from box state
         */
        value: async (key: Uint8Array | string): Promise<string | undefined> => { return await this.appClient.state.box.getMapValue("_players", key) as string | undefined },
      },
    },
  }

  public newGroup(): LotteryTestComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a manager()address method call against the LotteryTest contract
       */
      manager(params: CallParams<LotteryTestArgs['obj']['manager()address'] | LotteryTestArgs['tuple']['manager()address']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.manager(params)))
        resultMappers.push((v) => client.decodeReturnValue('manager()address', v))
        return this
      },
      /**
       * Add a ticketPrice()uint512 method call against the LotteryTest contract
       */
      ticketPrice(params: CallParams<LotteryTestArgs['obj']['ticketPrice()uint512'] | LotteryTestArgs['tuple']['ticketPrice()uint512']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.ticketPrice(params)))
        resultMappers.push((v) => client.decodeReturnValue('ticketPrice()uint512', v))
        return this
      },
      /**
       * Add a playerCount()uint512 method call against the LotteryTest contract
       */
      playerCount(params: CallParams<LotteryTestArgs['obj']['playerCount()uint512'] | LotteryTestArgs['tuple']['playerCount()uint512']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.playerCount(params)))
        resultMappers.push((v) => client.decodeReturnValue('playerCount()uint512', v))
        return this
      },
      /**
       * Add a prizePool()uint512 method call against the LotteryTest contract
       */
      prizePool(params: CallParams<LotteryTestArgs['obj']['prizePool()uint512'] | LotteryTestArgs['tuple']['prizePool()uint512']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.prizePool(params)))
        resultMappers.push((v) => client.decodeReturnValue('prizePool()uint512', v))
        return this
      },
      /**
       * Add a isActive()bool method call against the LotteryTest contract
       */
      isActive(params: CallParams<LotteryTestArgs['obj']['isActive()bool'] | LotteryTestArgs['tuple']['isActive()bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.isActive(params)))
        resultMappers.push((v) => client.decodeReturnValue('isActive()bool', v))
        return this
      },
      /**
       * Add a lastWinner()address method call against the LotteryTest contract
       */
      lastWinner(params: CallParams<LotteryTestArgs['obj']['lastWinner()address'] | LotteryTestArgs['tuple']['lastWinner()address']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.lastWinner(params)))
        resultMappers.push((v) => client.decodeReturnValue('lastWinner()address', v))
        return this
      },
      /**
       * Add a roundNumber()uint512 method call against the LotteryTest contract
       */
      roundNumber(params: CallParams<LotteryTestArgs['obj']['roundNumber()uint512'] | LotteryTestArgs['tuple']['roundNumber()uint512']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.roundNumber(params)))
        resultMappers.push((v) => client.decodeReturnValue('roundNumber()uint512', v))
        return this
      },
      /**
       * Add a setTicketPrice(uint512)void method call against the LotteryTest contract
       */
      setTicketPrice(params: CallParams<LotteryTestArgs['obj']['setTicketPrice(uint512)void'] | LotteryTestArgs['tuple']['setTicketPrice(uint512)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.setTicketPrice(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a enter(address)void method call against the LotteryTest contract
       */
      enter(params: CallParams<LotteryTestArgs['obj']['enter(address)void'] | LotteryTestArgs['tuple']['enter(address)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.enter(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a getPlayer(uint512)address method call against the LotteryTest contract
       */
      getPlayer(params: CallParams<LotteryTestArgs['obj']['getPlayer(uint512)address'] | LotteryTestArgs['tuple']['getPlayer(uint512)address']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.getPlayer(params)))
        resultMappers.push((v) => client.decodeReturnValue('getPlayer(uint512)address', v))
        return this
      },
      /**
       * Add a pickWinner(uint512)address method call against the LotteryTest contract
       */
      pickWinner(params: CallParams<LotteryTestArgs['obj']['pickWinner(uint512)address'] | LotteryTestArgs['tuple']['pickWinner(uint512)address']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.pickWinner(params)))
        resultMappers.push((v) => client.decodeReturnValue('pickWinner(uint512)address', v))
        return this
      },
      /**
       * Add a toggleActive()void method call against the LotteryTest contract
       */
      toggleActive(params: CallParams<LotteryTestArgs['obj']['toggleActive()void'] | LotteryTestArgs['tuple']['toggleActive()void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.toggleActive(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a clear state call to the LotteryTest contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as LotteryTestComposer
  }
}
export type LotteryTestComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the manager()address ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  manager(params?: CallParams<LotteryTestArgs['obj']['manager()address'] | LotteryTestArgs['tuple']['manager()address']>): LotteryTestComposer<[...TReturns, LotteryTestReturns['manager()address'] | undefined]>

  /**
   * Calls the ticketPrice()uint512 ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  ticketPrice(params?: CallParams<LotteryTestArgs['obj']['ticketPrice()uint512'] | LotteryTestArgs['tuple']['ticketPrice()uint512']>): LotteryTestComposer<[...TReturns, LotteryTestReturns['ticketPrice()uint512'] | undefined]>

  /**
   * Calls the playerCount()uint512 ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  playerCount(params?: CallParams<LotteryTestArgs['obj']['playerCount()uint512'] | LotteryTestArgs['tuple']['playerCount()uint512']>): LotteryTestComposer<[...TReturns, LotteryTestReturns['playerCount()uint512'] | undefined]>

  /**
   * Calls the prizePool()uint512 ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  prizePool(params?: CallParams<LotteryTestArgs['obj']['prizePool()uint512'] | LotteryTestArgs['tuple']['prizePool()uint512']>): LotteryTestComposer<[...TReturns, LotteryTestReturns['prizePool()uint512'] | undefined]>

  /**
   * Calls the isActive()bool ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  isActive(params?: CallParams<LotteryTestArgs['obj']['isActive()bool'] | LotteryTestArgs['tuple']['isActive()bool']>): LotteryTestComposer<[...TReturns, LotteryTestReturns['isActive()bool'] | undefined]>

  /**
   * Calls the lastWinner()address ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  lastWinner(params?: CallParams<LotteryTestArgs['obj']['lastWinner()address'] | LotteryTestArgs['tuple']['lastWinner()address']>): LotteryTestComposer<[...TReturns, LotteryTestReturns['lastWinner()address'] | undefined]>

  /**
   * Calls the roundNumber()uint512 ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  roundNumber(params?: CallParams<LotteryTestArgs['obj']['roundNumber()uint512'] | LotteryTestArgs['tuple']['roundNumber()uint512']>): LotteryTestComposer<[...TReturns, LotteryTestReturns['roundNumber()uint512'] | undefined]>

  /**
   * Calls the setTicketPrice(uint512)void ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  setTicketPrice(params?: CallParams<LotteryTestArgs['obj']['setTicketPrice(uint512)void'] | LotteryTestArgs['tuple']['setTicketPrice(uint512)void']>): LotteryTestComposer<[...TReturns, LotteryTestReturns['setTicketPrice(uint512)void'] | undefined]>

  /**
   * Calls the enter(address)void ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  enter(params?: CallParams<LotteryTestArgs['obj']['enter(address)void'] | LotteryTestArgs['tuple']['enter(address)void']>): LotteryTestComposer<[...TReturns, LotteryTestReturns['enter(address)void'] | undefined]>

  /**
   * Calls the getPlayer(uint512)address ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  getPlayer(params?: CallParams<LotteryTestArgs['obj']['getPlayer(uint512)address'] | LotteryTestArgs['tuple']['getPlayer(uint512)address']>): LotteryTestComposer<[...TReturns, LotteryTestReturns['getPlayer(uint512)address'] | undefined]>

  /**
   * Calls the pickWinner(uint512)address ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  pickWinner(params?: CallParams<LotteryTestArgs['obj']['pickWinner(uint512)address'] | LotteryTestArgs['tuple']['pickWinner(uint512)address']>): LotteryTestComposer<[...TReturns, LotteryTestReturns['pickWinner(uint512)address'] | undefined]>

  /**
   * Calls the toggleActive()void ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  toggleActive(params?: CallParams<LotteryTestArgs['obj']['toggleActive()void'] | LotteryTestArgs['tuple']['toggleActive()void']>): LotteryTestComposer<[...TReturns, LotteryTestReturns['toggleActive()void'] | undefined]>

  /**
   * Makes a clear_state call to an existing instance of the LotteryTest smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): LotteryTestComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): LotteryTestComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<LotteryTestComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<LotteryTestComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<LotteryTestComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<LotteryTestComposerResults<TReturns>>
}
export type LotteryTestComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

