/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"test","structs":{},"methods":[{"name":"x","args":[],"returns":{"type":"uint512"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"y","args":[],"returns":{"type":"uint512"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"q","args":[],"returns":{"type":"uint512"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}}],"arcs":[22,28],"networks":{},"state":{"schema":{"global":{"ints":3,"bytes":0},"local":{"ints":0,"bytes":0}},"keys":{"global":{"x":{"keyType":"AVMString","valueType":"AVMUint64","key":"eA=="},"y":{"keyType":"AVMString","valueType":"AVMUint64","key":"eQ=="},"q":{"keyType":"AVMString","valueType":"AVMUint64","key":"cQ=="}},"local":{},"box":{}},"maps":{"global":{},"local":{},"box":{}}},"bareActions":{"create":[],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[212,228,289],"errorMessage":"check q exists"},{"pc":[386,400,428],"errorMessage":"check x exists"},{"pc":[317,331,359],"errorMessage":"check y exists"},{"pc":[208,313,382],"errorMessage":"not payable"},{"pc":[275,345,414],"errorMessage":"overflow"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"events":[]} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


/**
 * The argument types for the Test contract
 */
export type TestArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'x()uint512': Record<string, never>
    'y()uint512': Record<string, never>
    'q()uint512': Record<string, never>
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'x()uint512': []
    'y()uint512': []
    'q()uint512': []
  }
}

/**
 * The return type for each method
 */
export type TestReturns = {
  'x()uint512': bigint
  'y()uint512': bigint
  'q()uint512': bigint
}

/**
 * Defines the types of available calls and state of the Test smart contract.
 */
export type TestTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'x()uint512' | 'x', {
      argsObj: TestArgs['obj']['x()uint512']
      argsTuple: TestArgs['tuple']['x()uint512']
      returns: TestReturns['x()uint512']
    }>
    & Record<'y()uint512' | 'y', {
      argsObj: TestArgs['obj']['y()uint512']
      argsTuple: TestArgs['tuple']['y()uint512']
      returns: TestReturns['y()uint512']
    }>
    & Record<'q()uint512' | 'q', {
      argsObj: TestArgs['obj']['q()uint512']
      argsTuple: TestArgs['tuple']['q()uint512']
      returns: TestReturns['q()uint512']
    }>
  /**
   * Defines the shape of the state of the application.
   */
  state: {
    global: {
      keys: {
        x: bigint
        y: bigint
        q: bigint
      }
      maps: {}
    }
  }
}

/**
 * Defines the possible abi call signatures.
 */
export type TestSignatures = keyof TestTypes['methods']
/**
 * Defines the possible abi call signatures for methods that return a non-void value.
 */
export type TestNonVoidMethodSignatures = keyof TestTypes['methods'] extends infer T ? T extends keyof TestTypes['methods'] ? MethodReturn<T> extends void ? never : T  : never : never
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the Test smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends TestSignatures> = TestTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the Test smart contract to the method's return type
 */
export type MethodReturn<TSignature extends TestSignatures> = TestTypes['methods'][TSignature]['returns']

/**
 * Defines the shape of the keyed global state of the application.
 */
export type GlobalKeysState = TestTypes['state']['global']['keys']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the Test smart contract
 */
export abstract class TestParamsFactory {
  /**
   * Constructs a no op call for the x()uint512 ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static x(params: CallParams<TestArgs['obj']['x()uint512'] | TestArgs['tuple']['x()uint512']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'x()uint512' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the y()uint512 ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static y(params: CallParams<TestArgs['obj']['y()uint512'] | TestArgs['tuple']['y()uint512']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'y()uint512' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the q()uint512 ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static q(params: CallParams<TestArgs['obj']['q()uint512'] | TestArgs['tuple']['q()uint512']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'q()uint512' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
}

/**
 * A client to make calls to the test smart contract
 */
export class TestClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `TestClient`
   *
   * @param appClient An `AppClient` instance which has been created with the Test app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `TestClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Checks for decode errors on the given return value and maps the return value to the return type for the given method
   * @returns The typed return value or undefined if there was no value
   */
  decodeReturnValue<TSignature extends TestNonVoidMethodSignatures>(method: TSignature, returnValue: ABIReturn | undefined) {
    return returnValue !== undefined ? getArc56ReturnValue<MethodReturn<TSignature>>(returnValue, this.appClient.getABIMethod(method), APP_SPEC.structs) : undefined
  }

  /**
   * Returns a new `TestClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<TestClient> {
    return new TestClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `TestClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<TestClient> {
    return new TestClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Makes a clear_state call to an existing instance of the test smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the test smart contract using the `x()uint512` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    x: (params: CallParams<TestArgs['obj']['x()uint512'] | TestArgs['tuple']['x()uint512']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(TestParamsFactory.x(params))
    },

    /**
     * Makes a call to the test smart contract using the `y()uint512` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    y: (params: CallParams<TestArgs['obj']['y()uint512'] | TestArgs['tuple']['y()uint512']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(TestParamsFactory.y(params))
    },

    /**
     * Makes a call to the test smart contract using the `q()uint512` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    q: (params: CallParams<TestArgs['obj']['q()uint512'] | TestArgs['tuple']['q()uint512']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(TestParamsFactory.q(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Makes a clear_state call to an existing instance of the test smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the test smart contract using the `x()uint512` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    x: (params: CallParams<TestArgs['obj']['x()uint512'] | TestArgs['tuple']['x()uint512']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(TestParamsFactory.x(params))
    },

    /**
     * Makes a call to the test smart contract using the `y()uint512` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    y: (params: CallParams<TestArgs['obj']['y()uint512'] | TestArgs['tuple']['y()uint512']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(TestParamsFactory.y(params))
    },

    /**
     * Makes a call to the test smart contract using the `q()uint512` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    q: (params: CallParams<TestArgs['obj']['q()uint512'] | TestArgs['tuple']['q()uint512']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(TestParamsFactory.q(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Makes a clear_state call to an existing instance of the test smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the test smart contract using the `x()uint512` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    x: async (params: CallParams<TestArgs['obj']['x()uint512'] | TestArgs['tuple']['x()uint512']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(TestParamsFactory.x(params))
      return {...result, return: result.return as unknown as (undefined | TestReturns['x()uint512'])}
    },

    /**
     * Makes a call to the test smart contract using the `y()uint512` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    y: async (params: CallParams<TestArgs['obj']['y()uint512'] | TestArgs['tuple']['y()uint512']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(TestParamsFactory.y(params))
      return {...result, return: result.return as unknown as (undefined | TestReturns['y()uint512'])}
    },

    /**
     * Makes a call to the test smart contract using the `q()uint512` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    q: async (params: CallParams<TestArgs['obj']['q()uint512'] | TestArgs['tuple']['q()uint512']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(TestParamsFactory.q(params))
      return {...result, return: result.return as unknown as (undefined | TestReturns['q()uint512'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new TestClient(this.appClient.clone(params))
  }

  /**
   * Makes a readonly (simulated) call to the test smart contract using the `x()uint512` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async x(params: CallParams<TestArgs['obj']['x()uint512'] | TestArgs['tuple']['x()uint512']> = {args: []}) {
    const result = await this.appClient.send.call(TestParamsFactory.x(params))
    return result.return as unknown as TestReturns['x()uint512']
  }

  /**
   * Makes a readonly (simulated) call to the test smart contract using the `y()uint512` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async y(params: CallParams<TestArgs['obj']['y()uint512'] | TestArgs['tuple']['y()uint512']> = {args: []}) {
    const result = await this.appClient.send.call(TestParamsFactory.y(params))
    return result.return as unknown as TestReturns['y()uint512']
  }

  /**
   * Makes a readonly (simulated) call to the test smart contract using the `q()uint512` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async q(params: CallParams<TestArgs['obj']['q()uint512'] | TestArgs['tuple']['q()uint512']> = {args: []}) {
    const result = await this.appClient.send.call(TestParamsFactory.q(params))
    return result.return as unknown as TestReturns['q()uint512']
  }

  /**
   * Methods to access state for the current test app
   */
  state = {
    /**
     * Methods to access global state for the current test app
     */
    global: {
      /**
       * Get all current keyed values from global state
       */
      getAll: async (): Promise<Partial<Expand<GlobalKeysState>>> => {
        const result = await this.appClient.state.global.getAll()
        return {
          x: result.x,
          y: result.y,
          q: result.q,
        }
      },
      /**
       * Get the current value of the x key in global state
       */
      x: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("x")) as bigint | undefined },
      /**
       * Get the current value of the y key in global state
       */
      y: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("y")) as bigint | undefined },
      /**
       * Get the current value of the q key in global state
       */
      q: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("q")) as bigint | undefined },
    },
  }

  public newGroup(): TestComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a x()uint512 method call against the test contract
       */
      x(params: CallParams<TestArgs['obj']['x()uint512'] | TestArgs['tuple']['x()uint512']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.x(params)))
        resultMappers.push((v) => client.decodeReturnValue('x()uint512', v))
        return this
      },
      /**
       * Add a y()uint512 method call against the test contract
       */
      y(params: CallParams<TestArgs['obj']['y()uint512'] | TestArgs['tuple']['y()uint512']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.y(params)))
        resultMappers.push((v) => client.decodeReturnValue('y()uint512', v))
        return this
      },
      /**
       * Add a q()uint512 method call against the test contract
       */
      q(params: CallParams<TestArgs['obj']['q()uint512'] | TestArgs['tuple']['q()uint512']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.q(params)))
        resultMappers.push((v) => client.decodeReturnValue('q()uint512', v))
        return this
      },
      /**
       * Add a clear state call to the test contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as TestComposer
  }
}
export type TestComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the x()uint512 ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  x(params?: CallParams<TestArgs['obj']['x()uint512'] | TestArgs['tuple']['x()uint512']>): TestComposer<[...TReturns, TestReturns['x()uint512'] | undefined]>

  /**
   * Calls the y()uint512 ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  y(params?: CallParams<TestArgs['obj']['y()uint512'] | TestArgs['tuple']['y()uint512']>): TestComposer<[...TReturns, TestReturns['y()uint512'] | undefined]>

  /**
   * Calls the q()uint512 ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  q(params?: CallParams<TestArgs['obj']['q()uint512'] | TestArgs['tuple']['q()uint512']>): TestComposer<[...TReturns, TestReturns['q()uint512'] | undefined]>

  /**
   * Makes a clear_state call to an existing instance of the test smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): TestComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): TestComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<TestComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<TestComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<TestComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<TestComposerResults<TReturns>>
}
export type TestComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

