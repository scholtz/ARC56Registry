/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"EscrowTest","structs":{},"methods":[{"name":"owner","args":[],"returns":{"type":"address"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"depositsOf","args":[{"type":"address","name":"payee"}],"returns":{"type":"uint512"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"totalDeposits","args":[],"returns":{"type":"uint512"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"deposit","args":[{"type":"address","name":"payee"},{"type":"uint512","name":"amount"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}},{"name":"withdraw","args":[{"type":"address","name":"payee"},{"type":"uint512","name":"amount"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}}],"arcs":[22,28],"desc":" @dev Simplified Escrow pattern from OpenZeppelin.\n Tracks deposits per address and allows withdrawal by owner.","networks":{},"state":{"schema":{"global":{"ints":0,"bytes":2},"local":{"ints":0,"bytes":0}},"keys":{"global":{"_owner":{"keyType":"AVMString","valueType":"address","key":"X293bmVy"},"_totalDeposits":{"keyType":"AVMString","valueType":"AVMBytes","key":"X3RvdGFsRGVwb3NpdHM="}},"local":{},"box":{}},"maps":{"global":{},"local":{},"box":{"_deposits":{"keyType":"AVMBytes","valueType":"AVMBytes","prefix":"X2RlcG9zaXRz"}}}},"bareActions":{"create":[],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[276],"errorMessage":"Escrow: caller is not the owner"},{"pc":[293],"errorMessage":"Escrow: insufficient deposit"},{"pc":[150,274],"errorMessage":"check _owner exists"},{"pc":[192,246,305],"errorMessage":"check _totalDeposits exists"},{"pc":[164,214,260],"errorMessage":"invalid number of bytes for arc4.static_array<arc4.uint8, 32>"},{"pc":[222,268],"errorMessage":"invalid number of bytes for arc4.uint512"},{"pc":[179,197],"errorMessage":"overflow"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"events":[]} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


/**
 * The argument types for the EscrowTest contract
 */
export type EscrowTestArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'owner()address': Record<string, never>
    'depositsOf(address)uint512': {
      payee: string
    }
    'totalDeposits()uint512': Record<string, never>
    'deposit(address,uint512)void': {
      payee: string
      amount: bigint | number
    }
    'withdraw(address,uint512)void': {
      payee: string
      amount: bigint | number
    }
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'owner()address': []
    'depositsOf(address)uint512': [payee: string]
    'totalDeposits()uint512': []
    'deposit(address,uint512)void': [payee: string, amount: bigint | number]
    'withdraw(address,uint512)void': [payee: string, amount: bigint | number]
  }
}

/**
 * The return type for each method
 */
export type EscrowTestReturns = {
  'owner()address': string
  'depositsOf(address)uint512': bigint
  'totalDeposits()uint512': bigint
  'deposit(address,uint512)void': void
  'withdraw(address,uint512)void': void
}

/**
 * Defines the types of available calls and state of the EscrowTest smart contract.
 */
export type EscrowTestTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'owner()address' | 'owner', {
      argsObj: EscrowTestArgs['obj']['owner()address']
      argsTuple: EscrowTestArgs['tuple']['owner()address']
      returns: EscrowTestReturns['owner()address']
    }>
    & Record<'depositsOf(address)uint512' | 'depositsOf', {
      argsObj: EscrowTestArgs['obj']['depositsOf(address)uint512']
      argsTuple: EscrowTestArgs['tuple']['depositsOf(address)uint512']
      returns: EscrowTestReturns['depositsOf(address)uint512']
    }>
    & Record<'totalDeposits()uint512' | 'totalDeposits', {
      argsObj: EscrowTestArgs['obj']['totalDeposits()uint512']
      argsTuple: EscrowTestArgs['tuple']['totalDeposits()uint512']
      returns: EscrowTestReturns['totalDeposits()uint512']
    }>
    & Record<'deposit(address,uint512)void' | 'deposit', {
      argsObj: EscrowTestArgs['obj']['deposit(address,uint512)void']
      argsTuple: EscrowTestArgs['tuple']['deposit(address,uint512)void']
      returns: EscrowTestReturns['deposit(address,uint512)void']
    }>
    & Record<'withdraw(address,uint512)void' | 'withdraw', {
      argsObj: EscrowTestArgs['obj']['withdraw(address,uint512)void']
      argsTuple: EscrowTestArgs['tuple']['withdraw(address,uint512)void']
      returns: EscrowTestReturns['withdraw(address,uint512)void']
    }>
  /**
   * Defines the shape of the state of the application.
   */
  state: {
    global: {
      keys: {
        _owner: string
        _totalDeposits: BinaryState
      }
      maps: {}
    }
    box: {
      keys: {}
      maps: {
        _deposits: Map<Uint8Array | string, Uint8Array>
      }
    }
  }
}

/**
 * Defines the possible abi call signatures.
 */
export type EscrowTestSignatures = keyof EscrowTestTypes['methods']
/**
 * Defines the possible abi call signatures for methods that return a non-void value.
 */
export type EscrowTestNonVoidMethodSignatures = keyof EscrowTestTypes['methods'] extends infer T ? T extends keyof EscrowTestTypes['methods'] ? MethodReturn<T> extends void ? never : T  : never : never
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the EscrowTest smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends EscrowTestSignatures> = EscrowTestTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the EscrowTest smart contract to the method's return type
 */
export type MethodReturn<TSignature extends EscrowTestSignatures> = EscrowTestTypes['methods'][TSignature]['returns']

/**
 * Defines the shape of the keyed global state of the application.
 */
export type GlobalKeysState = EscrowTestTypes['state']['global']['keys']

/**
 * Defines the shape of the keyed box state of the application.
 */
export type BoxKeysState = EscrowTestTypes['state']['box']['keys']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the EscrowTest smart contract
 */
export abstract class EscrowTestParamsFactory {
  /**
   * Constructs a no op call for the owner()address ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static owner(params: CallParams<EscrowTestArgs['obj']['owner()address'] | EscrowTestArgs['tuple']['owner()address']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'owner()address' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the depositsOf(address)uint512 ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static depositsOf(params: CallParams<EscrowTestArgs['obj']['depositsOf(address)uint512'] | EscrowTestArgs['tuple']['depositsOf(address)uint512']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'depositsOf(address)uint512' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.payee],
    }
  }
  /**
   * Constructs a no op call for the totalDeposits()uint512 ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static totalDeposits(params: CallParams<EscrowTestArgs['obj']['totalDeposits()uint512'] | EscrowTestArgs['tuple']['totalDeposits()uint512']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'totalDeposits()uint512' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the deposit(address,uint512)void ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static deposit(params: CallParams<EscrowTestArgs['obj']['deposit(address,uint512)void'] | EscrowTestArgs['tuple']['deposit(address,uint512)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'deposit(address,uint512)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.payee, params.args.amount],
    }
  }
  /**
   * Constructs a no op call for the withdraw(address,uint512)void ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static withdraw(params: CallParams<EscrowTestArgs['obj']['withdraw(address,uint512)void'] | EscrowTestArgs['tuple']['withdraw(address,uint512)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'withdraw(address,uint512)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.payee, params.args.amount],
    }
  }
}

/**
 * A client to make calls to the EscrowTest smart contract
 */
export class EscrowTestClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `EscrowTestClient`
   *
   * @param appClient An `AppClient` instance which has been created with the EscrowTest app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `EscrowTestClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Checks for decode errors on the given return value and maps the return value to the return type for the given method
   * @returns The typed return value or undefined if there was no value
   */
  decodeReturnValue<TSignature extends EscrowTestNonVoidMethodSignatures>(method: TSignature, returnValue: ABIReturn | undefined) {
    return returnValue !== undefined ? getArc56ReturnValue<MethodReturn<TSignature>>(returnValue, this.appClient.getABIMethod(method), APP_SPEC.structs) : undefined
  }

  /**
   * Returns a new `EscrowTestClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<EscrowTestClient> {
    return new EscrowTestClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `EscrowTestClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<EscrowTestClient> {
    return new EscrowTestClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Makes a clear_state call to an existing instance of the EscrowTest smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the EscrowTest smart contract using the `owner()address` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    owner: (params: CallParams<EscrowTestArgs['obj']['owner()address'] | EscrowTestArgs['tuple']['owner()address']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(EscrowTestParamsFactory.owner(params))
    },

    /**
     * Makes a call to the EscrowTest smart contract using the `depositsOf(address)uint512` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    depositsOf: (params: CallParams<EscrowTestArgs['obj']['depositsOf(address)uint512'] | EscrowTestArgs['tuple']['depositsOf(address)uint512']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(EscrowTestParamsFactory.depositsOf(params))
    },

    /**
     * Makes a call to the EscrowTest smart contract using the `totalDeposits()uint512` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    totalDeposits: (params: CallParams<EscrowTestArgs['obj']['totalDeposits()uint512'] | EscrowTestArgs['tuple']['totalDeposits()uint512']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(EscrowTestParamsFactory.totalDeposits(params))
    },

    /**
     * Makes a call to the EscrowTest smart contract using the `deposit(address,uint512)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    deposit: (params: CallParams<EscrowTestArgs['obj']['deposit(address,uint512)void'] | EscrowTestArgs['tuple']['deposit(address,uint512)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(EscrowTestParamsFactory.deposit(params))
    },

    /**
     * Makes a call to the EscrowTest smart contract using the `withdraw(address,uint512)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    withdraw: (params: CallParams<EscrowTestArgs['obj']['withdraw(address,uint512)void'] | EscrowTestArgs['tuple']['withdraw(address,uint512)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(EscrowTestParamsFactory.withdraw(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Makes a clear_state call to an existing instance of the EscrowTest smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the EscrowTest smart contract using the `owner()address` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    owner: (params: CallParams<EscrowTestArgs['obj']['owner()address'] | EscrowTestArgs['tuple']['owner()address']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(EscrowTestParamsFactory.owner(params))
    },

    /**
     * Makes a call to the EscrowTest smart contract using the `depositsOf(address)uint512` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    depositsOf: (params: CallParams<EscrowTestArgs['obj']['depositsOf(address)uint512'] | EscrowTestArgs['tuple']['depositsOf(address)uint512']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(EscrowTestParamsFactory.depositsOf(params))
    },

    /**
     * Makes a call to the EscrowTest smart contract using the `totalDeposits()uint512` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    totalDeposits: (params: CallParams<EscrowTestArgs['obj']['totalDeposits()uint512'] | EscrowTestArgs['tuple']['totalDeposits()uint512']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(EscrowTestParamsFactory.totalDeposits(params))
    },

    /**
     * Makes a call to the EscrowTest smart contract using the `deposit(address,uint512)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    deposit: (params: CallParams<EscrowTestArgs['obj']['deposit(address,uint512)void'] | EscrowTestArgs['tuple']['deposit(address,uint512)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(EscrowTestParamsFactory.deposit(params))
    },

    /**
     * Makes a call to the EscrowTest smart contract using the `withdraw(address,uint512)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    withdraw: (params: CallParams<EscrowTestArgs['obj']['withdraw(address,uint512)void'] | EscrowTestArgs['tuple']['withdraw(address,uint512)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(EscrowTestParamsFactory.withdraw(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Makes a clear_state call to an existing instance of the EscrowTest smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the EscrowTest smart contract using the `owner()address` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    owner: async (params: CallParams<EscrowTestArgs['obj']['owner()address'] | EscrowTestArgs['tuple']['owner()address']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(EscrowTestParamsFactory.owner(params))
      return {...result, return: result.return as unknown as (undefined | EscrowTestReturns['owner()address'])}
    },

    /**
     * Makes a call to the EscrowTest smart contract using the `depositsOf(address)uint512` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    depositsOf: async (params: CallParams<EscrowTestArgs['obj']['depositsOf(address)uint512'] | EscrowTestArgs['tuple']['depositsOf(address)uint512']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(EscrowTestParamsFactory.depositsOf(params))
      return {...result, return: result.return as unknown as (undefined | EscrowTestReturns['depositsOf(address)uint512'])}
    },

    /**
     * Makes a call to the EscrowTest smart contract using the `totalDeposits()uint512` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    totalDeposits: async (params: CallParams<EscrowTestArgs['obj']['totalDeposits()uint512'] | EscrowTestArgs['tuple']['totalDeposits()uint512']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(EscrowTestParamsFactory.totalDeposits(params))
      return {...result, return: result.return as unknown as (undefined | EscrowTestReturns['totalDeposits()uint512'])}
    },

    /**
     * Makes a call to the EscrowTest smart contract using the `deposit(address,uint512)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    deposit: async (params: CallParams<EscrowTestArgs['obj']['deposit(address,uint512)void'] | EscrowTestArgs['tuple']['deposit(address,uint512)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(EscrowTestParamsFactory.deposit(params))
      return {...result, return: result.return as unknown as (undefined | EscrowTestReturns['deposit(address,uint512)void'])}
    },

    /**
     * Makes a call to the EscrowTest smart contract using the `withdraw(address,uint512)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    withdraw: async (params: CallParams<EscrowTestArgs['obj']['withdraw(address,uint512)void'] | EscrowTestArgs['tuple']['withdraw(address,uint512)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(EscrowTestParamsFactory.withdraw(params))
      return {...result, return: result.return as unknown as (undefined | EscrowTestReturns['withdraw(address,uint512)void'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new EscrowTestClient(this.appClient.clone(params))
  }

  /**
   * Makes a readonly (simulated) call to the EscrowTest smart contract using the `owner()address` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async owner(params: CallParams<EscrowTestArgs['obj']['owner()address'] | EscrowTestArgs['tuple']['owner()address']> = {args: []}) {
    const result = await this.appClient.send.call(EscrowTestParamsFactory.owner(params))
    return result.return as unknown as EscrowTestReturns['owner()address']
  }

  /**
   * Makes a readonly (simulated) call to the EscrowTest smart contract using the `depositsOf(address)uint512` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async depositsOf(params: CallParams<EscrowTestArgs['obj']['depositsOf(address)uint512'] | EscrowTestArgs['tuple']['depositsOf(address)uint512']>) {
    const result = await this.appClient.send.call(EscrowTestParamsFactory.depositsOf(params))
    return result.return as unknown as EscrowTestReturns['depositsOf(address)uint512']
  }

  /**
   * Makes a readonly (simulated) call to the EscrowTest smart contract using the `totalDeposits()uint512` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async totalDeposits(params: CallParams<EscrowTestArgs['obj']['totalDeposits()uint512'] | EscrowTestArgs['tuple']['totalDeposits()uint512']> = {args: []}) {
    const result = await this.appClient.send.call(EscrowTestParamsFactory.totalDeposits(params))
    return result.return as unknown as EscrowTestReturns['totalDeposits()uint512']
  }

  /**
   * Methods to access state for the current EscrowTest app
   */
  state = {
    /**
     * Methods to access global state for the current EscrowTest app
     */
    global: {
      /**
       * Get all current keyed values from global state
       */
      getAll: async (): Promise<Partial<Expand<GlobalKeysState>>> => {
        const result = await this.appClient.state.global.getAll()
        return {
          _owner: result._owner,
          _totalDeposits: new BinaryStateValue(result._totalDeposits),
        }
      },
      /**
       * Get the current value of the _owner key in global state
       */
      _owner: async (): Promise<string | undefined> => { return (await this.appClient.state.global.getValue("_owner")) as string | undefined },
      /**
       * Get the current value of the _totalDeposits key in global state
       */
      _totalDeposits: async (): Promise<BinaryState> => { return new BinaryStateValue((await this.appClient.state.global.getValue("_totalDeposits")) as Uint8Array | undefined) },
    },
    /**
     * Methods to access box state for the current EscrowTest app
     */
    box: {
      /**
       * Get all current keyed values from box state
       */
      getAll: async (): Promise<Partial<Expand<BoxKeysState>>> => {
        const result = await this.appClient.state.box.getAll()
        return {
        }
      },
      /**
       * Get values from the _deposits map in box state
       */
      _deposits: {
        /**
         * Get all current values of the _deposits map in box state
         */
        getMap: async (): Promise<Map<Uint8Array, Uint8Array>> => { return (await this.appClient.state.box.getMap("_deposits")) as Map<Uint8Array, Uint8Array> },
        /**
         * Get a current value of the _deposits map by key from box state
         */
        value: async (key: Uint8Array | string): Promise<Uint8Array | undefined> => { return await this.appClient.state.box.getMapValue("_deposits", key) as Uint8Array | undefined },
      },
    },
  }

  public newGroup(): EscrowTestComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a owner()address method call against the EscrowTest contract
       */
      owner(params: CallParams<EscrowTestArgs['obj']['owner()address'] | EscrowTestArgs['tuple']['owner()address']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.owner(params)))
        resultMappers.push((v) => client.decodeReturnValue('owner()address', v))
        return this
      },
      /**
       * Add a depositsOf(address)uint512 method call against the EscrowTest contract
       */
      depositsOf(params: CallParams<EscrowTestArgs['obj']['depositsOf(address)uint512'] | EscrowTestArgs['tuple']['depositsOf(address)uint512']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.depositsOf(params)))
        resultMappers.push((v) => client.decodeReturnValue('depositsOf(address)uint512', v))
        return this
      },
      /**
       * Add a totalDeposits()uint512 method call against the EscrowTest contract
       */
      totalDeposits(params: CallParams<EscrowTestArgs['obj']['totalDeposits()uint512'] | EscrowTestArgs['tuple']['totalDeposits()uint512']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.totalDeposits(params)))
        resultMappers.push((v) => client.decodeReturnValue('totalDeposits()uint512', v))
        return this
      },
      /**
       * Add a deposit(address,uint512)void method call against the EscrowTest contract
       */
      deposit(params: CallParams<EscrowTestArgs['obj']['deposit(address,uint512)void'] | EscrowTestArgs['tuple']['deposit(address,uint512)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.deposit(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a withdraw(address,uint512)void method call against the EscrowTest contract
       */
      withdraw(params: CallParams<EscrowTestArgs['obj']['withdraw(address,uint512)void'] | EscrowTestArgs['tuple']['withdraw(address,uint512)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.withdraw(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a clear state call to the EscrowTest contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as EscrowTestComposer
  }
}
export type EscrowTestComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the owner()address ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  owner(params?: CallParams<EscrowTestArgs['obj']['owner()address'] | EscrowTestArgs['tuple']['owner()address']>): EscrowTestComposer<[...TReturns, EscrowTestReturns['owner()address'] | undefined]>

  /**
   * Calls the depositsOf(address)uint512 ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  depositsOf(params?: CallParams<EscrowTestArgs['obj']['depositsOf(address)uint512'] | EscrowTestArgs['tuple']['depositsOf(address)uint512']>): EscrowTestComposer<[...TReturns, EscrowTestReturns['depositsOf(address)uint512'] | undefined]>

  /**
   * Calls the totalDeposits()uint512 ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  totalDeposits(params?: CallParams<EscrowTestArgs['obj']['totalDeposits()uint512'] | EscrowTestArgs['tuple']['totalDeposits()uint512']>): EscrowTestComposer<[...TReturns, EscrowTestReturns['totalDeposits()uint512'] | undefined]>

  /**
   * Calls the deposit(address,uint512)void ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  deposit(params?: CallParams<EscrowTestArgs['obj']['deposit(address,uint512)void'] | EscrowTestArgs['tuple']['deposit(address,uint512)void']>): EscrowTestComposer<[...TReturns, EscrowTestReturns['deposit(address,uint512)void'] | undefined]>

  /**
   * Calls the withdraw(address,uint512)void ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  withdraw(params?: CallParams<EscrowTestArgs['obj']['withdraw(address,uint512)void'] | EscrowTestArgs['tuple']['withdraw(address,uint512)void']>): EscrowTestComposer<[...TReturns, EscrowTestReturns['withdraw(address,uint512)void'] | undefined]>

  /**
   * Makes a clear_state call to an existing instance of the EscrowTest smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): EscrowTestComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): EscrowTestComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<EscrowTestComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<EscrowTestComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<EscrowTestComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<EscrowTestComposerResults<TReturns>>
}
export type EscrowTestComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

