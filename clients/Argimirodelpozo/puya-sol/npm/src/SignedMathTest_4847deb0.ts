/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"SignedMathTest","structs":{},"methods":[{"name":"testMax","args":[{"type":"uint512","name":"a"},{"type":"uint512","name":"b"}],"returns":{"type":"uint512"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"testMin","args":[{"type":"uint512","name":"a"},{"type":"uint512","name":"b"}],"returns":{"type":"uint512"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"testAverage","args":[{"type":"uint512","name":"a"},{"type":"uint512","name":"b"}],"returns":{"type":"uint512"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"testAbs","args":[{"type":"uint512","name":"n"}],"returns":{"type":"uint512"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}}],"arcs":[22,28],"networks":{},"state":{"schema":{"global":{"ints":0,"bytes":0},"local":{"ints":0,"bytes":0}},"keys":{"global":{},"local":{},"box":{}},"maps":{"global":{},"local":{},"box":{}}},"bareActions":{"create":[],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[103,114,185,196,266,274,384],"errorMessage":"invalid number of bytes for arc4.uint512"},{"pc":[137,219,366,433],"errorMessage":"overflow"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"events":[]} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


/**
 * The argument types for the SignedMathTest contract
 */
export type SignedMathTestArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'testMax(uint512,uint512)uint512': {
      a: bigint | number
      b: bigint | number
    }
    'testMin(uint512,uint512)uint512': {
      a: bigint | number
      b: bigint | number
    }
    'testAverage(uint512,uint512)uint512': {
      a: bigint | number
      b: bigint | number
    }
    'testAbs(uint512)uint512': {
      n: bigint | number
    }
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'testMax(uint512,uint512)uint512': [a: bigint | number, b: bigint | number]
    'testMin(uint512,uint512)uint512': [a: bigint | number, b: bigint | number]
    'testAverage(uint512,uint512)uint512': [a: bigint | number, b: bigint | number]
    'testAbs(uint512)uint512': [n: bigint | number]
  }
}

/**
 * The return type for each method
 */
export type SignedMathTestReturns = {
  'testMax(uint512,uint512)uint512': bigint
  'testMin(uint512,uint512)uint512': bigint
  'testAverage(uint512,uint512)uint512': bigint
  'testAbs(uint512)uint512': bigint
}

/**
 * Defines the types of available calls and state of the SignedMathTest smart contract.
 */
export type SignedMathTestTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'testMax(uint512,uint512)uint512' | 'testMax', {
      argsObj: SignedMathTestArgs['obj']['testMax(uint512,uint512)uint512']
      argsTuple: SignedMathTestArgs['tuple']['testMax(uint512,uint512)uint512']
      returns: SignedMathTestReturns['testMax(uint512,uint512)uint512']
    }>
    & Record<'testMin(uint512,uint512)uint512' | 'testMin', {
      argsObj: SignedMathTestArgs['obj']['testMin(uint512,uint512)uint512']
      argsTuple: SignedMathTestArgs['tuple']['testMin(uint512,uint512)uint512']
      returns: SignedMathTestReturns['testMin(uint512,uint512)uint512']
    }>
    & Record<'testAverage(uint512,uint512)uint512' | 'testAverage', {
      argsObj: SignedMathTestArgs['obj']['testAverage(uint512,uint512)uint512']
      argsTuple: SignedMathTestArgs['tuple']['testAverage(uint512,uint512)uint512']
      returns: SignedMathTestReturns['testAverage(uint512,uint512)uint512']
    }>
    & Record<'testAbs(uint512)uint512' | 'testAbs', {
      argsObj: SignedMathTestArgs['obj']['testAbs(uint512)uint512']
      argsTuple: SignedMathTestArgs['tuple']['testAbs(uint512)uint512']
      returns: SignedMathTestReturns['testAbs(uint512)uint512']
    }>
}

/**
 * Defines the possible abi call signatures.
 */
export type SignedMathTestSignatures = keyof SignedMathTestTypes['methods']
/**
 * Defines the possible abi call signatures for methods that return a non-void value.
 */
export type SignedMathTestNonVoidMethodSignatures = keyof SignedMathTestTypes['methods'] extends infer T ? T extends keyof SignedMathTestTypes['methods'] ? MethodReturn<T> extends void ? never : T  : never : never
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the SignedMathTest smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends SignedMathTestSignatures> = SignedMathTestTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the SignedMathTest smart contract to the method's return type
 */
export type MethodReturn<TSignature extends SignedMathTestSignatures> = SignedMathTestTypes['methods'][TSignature]['returns']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the SignedMathTest smart contract
 */
export abstract class SignedMathTestParamsFactory {
  /**
   * Constructs a no op call for the testMax(uint512,uint512)uint512 ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static testMax(params: CallParams<SignedMathTestArgs['obj']['testMax(uint512,uint512)uint512'] | SignedMathTestArgs['tuple']['testMax(uint512,uint512)uint512']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'testMax(uint512,uint512)uint512' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.a, params.args.b],
    }
  }
  /**
   * Constructs a no op call for the testMin(uint512,uint512)uint512 ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static testMin(params: CallParams<SignedMathTestArgs['obj']['testMin(uint512,uint512)uint512'] | SignedMathTestArgs['tuple']['testMin(uint512,uint512)uint512']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'testMin(uint512,uint512)uint512' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.a, params.args.b],
    }
  }
  /**
   * Constructs a no op call for the testAverage(uint512,uint512)uint512 ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static testAverage(params: CallParams<SignedMathTestArgs['obj']['testAverage(uint512,uint512)uint512'] | SignedMathTestArgs['tuple']['testAverage(uint512,uint512)uint512']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'testAverage(uint512,uint512)uint512' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.a, params.args.b],
    }
  }
  /**
   * Constructs a no op call for the testAbs(uint512)uint512 ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static testAbs(params: CallParams<SignedMathTestArgs['obj']['testAbs(uint512)uint512'] | SignedMathTestArgs['tuple']['testAbs(uint512)uint512']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'testAbs(uint512)uint512' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.n],
    }
  }
}

/**
 * A client to make calls to the SignedMathTest smart contract
 */
export class SignedMathTestClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `SignedMathTestClient`
   *
   * @param appClient An `AppClient` instance which has been created with the SignedMathTest app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `SignedMathTestClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Checks for decode errors on the given return value and maps the return value to the return type for the given method
   * @returns The typed return value or undefined if there was no value
   */
  decodeReturnValue<TSignature extends SignedMathTestNonVoidMethodSignatures>(method: TSignature, returnValue: ABIReturn | undefined) {
    return returnValue !== undefined ? getArc56ReturnValue<MethodReturn<TSignature>>(returnValue, this.appClient.getABIMethod(method), APP_SPEC.structs) : undefined
  }

  /**
   * Returns a new `SignedMathTestClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<SignedMathTestClient> {
    return new SignedMathTestClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `SignedMathTestClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<SignedMathTestClient> {
    return new SignedMathTestClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Makes a clear_state call to an existing instance of the SignedMathTest smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the SignedMathTest smart contract using the `testMax(uint512,uint512)uint512` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    testMax: (params: CallParams<SignedMathTestArgs['obj']['testMax(uint512,uint512)uint512'] | SignedMathTestArgs['tuple']['testMax(uint512,uint512)uint512']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(SignedMathTestParamsFactory.testMax(params))
    },

    /**
     * Makes a call to the SignedMathTest smart contract using the `testMin(uint512,uint512)uint512` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    testMin: (params: CallParams<SignedMathTestArgs['obj']['testMin(uint512,uint512)uint512'] | SignedMathTestArgs['tuple']['testMin(uint512,uint512)uint512']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(SignedMathTestParamsFactory.testMin(params))
    },

    /**
     * Makes a call to the SignedMathTest smart contract using the `testAverage(uint512,uint512)uint512` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    testAverage: (params: CallParams<SignedMathTestArgs['obj']['testAverage(uint512,uint512)uint512'] | SignedMathTestArgs['tuple']['testAverage(uint512,uint512)uint512']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(SignedMathTestParamsFactory.testAverage(params))
    },

    /**
     * Makes a call to the SignedMathTest smart contract using the `testAbs(uint512)uint512` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    testAbs: (params: CallParams<SignedMathTestArgs['obj']['testAbs(uint512)uint512'] | SignedMathTestArgs['tuple']['testAbs(uint512)uint512']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(SignedMathTestParamsFactory.testAbs(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Makes a clear_state call to an existing instance of the SignedMathTest smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the SignedMathTest smart contract using the `testMax(uint512,uint512)uint512` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    testMax: (params: CallParams<SignedMathTestArgs['obj']['testMax(uint512,uint512)uint512'] | SignedMathTestArgs['tuple']['testMax(uint512,uint512)uint512']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(SignedMathTestParamsFactory.testMax(params))
    },

    /**
     * Makes a call to the SignedMathTest smart contract using the `testMin(uint512,uint512)uint512` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    testMin: (params: CallParams<SignedMathTestArgs['obj']['testMin(uint512,uint512)uint512'] | SignedMathTestArgs['tuple']['testMin(uint512,uint512)uint512']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(SignedMathTestParamsFactory.testMin(params))
    },

    /**
     * Makes a call to the SignedMathTest smart contract using the `testAverage(uint512,uint512)uint512` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    testAverage: (params: CallParams<SignedMathTestArgs['obj']['testAverage(uint512,uint512)uint512'] | SignedMathTestArgs['tuple']['testAverage(uint512,uint512)uint512']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(SignedMathTestParamsFactory.testAverage(params))
    },

    /**
     * Makes a call to the SignedMathTest smart contract using the `testAbs(uint512)uint512` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    testAbs: (params: CallParams<SignedMathTestArgs['obj']['testAbs(uint512)uint512'] | SignedMathTestArgs['tuple']['testAbs(uint512)uint512']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(SignedMathTestParamsFactory.testAbs(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Makes a clear_state call to an existing instance of the SignedMathTest smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the SignedMathTest smart contract using the `testMax(uint512,uint512)uint512` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    testMax: async (params: CallParams<SignedMathTestArgs['obj']['testMax(uint512,uint512)uint512'] | SignedMathTestArgs['tuple']['testMax(uint512,uint512)uint512']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(SignedMathTestParamsFactory.testMax(params))
      return {...result, return: result.return as unknown as (undefined | SignedMathTestReturns['testMax(uint512,uint512)uint512'])}
    },

    /**
     * Makes a call to the SignedMathTest smart contract using the `testMin(uint512,uint512)uint512` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    testMin: async (params: CallParams<SignedMathTestArgs['obj']['testMin(uint512,uint512)uint512'] | SignedMathTestArgs['tuple']['testMin(uint512,uint512)uint512']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(SignedMathTestParamsFactory.testMin(params))
      return {...result, return: result.return as unknown as (undefined | SignedMathTestReturns['testMin(uint512,uint512)uint512'])}
    },

    /**
     * Makes a call to the SignedMathTest smart contract using the `testAverage(uint512,uint512)uint512` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    testAverage: async (params: CallParams<SignedMathTestArgs['obj']['testAverage(uint512,uint512)uint512'] | SignedMathTestArgs['tuple']['testAverage(uint512,uint512)uint512']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(SignedMathTestParamsFactory.testAverage(params))
      return {...result, return: result.return as unknown as (undefined | SignedMathTestReturns['testAverage(uint512,uint512)uint512'])}
    },

    /**
     * Makes a call to the SignedMathTest smart contract using the `testAbs(uint512)uint512` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    testAbs: async (params: CallParams<SignedMathTestArgs['obj']['testAbs(uint512)uint512'] | SignedMathTestArgs['tuple']['testAbs(uint512)uint512']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(SignedMathTestParamsFactory.testAbs(params))
      return {...result, return: result.return as unknown as (undefined | SignedMathTestReturns['testAbs(uint512)uint512'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new SignedMathTestClient(this.appClient.clone(params))
  }

  /**
   * Makes a readonly (simulated) call to the SignedMathTest smart contract using the `testMax(uint512,uint512)uint512` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async testMax(params: CallParams<SignedMathTestArgs['obj']['testMax(uint512,uint512)uint512'] | SignedMathTestArgs['tuple']['testMax(uint512,uint512)uint512']>) {
    const result = await this.appClient.send.call(SignedMathTestParamsFactory.testMax(params))
    return result.return as unknown as SignedMathTestReturns['testMax(uint512,uint512)uint512']
  }

  /**
   * Makes a readonly (simulated) call to the SignedMathTest smart contract using the `testMin(uint512,uint512)uint512` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async testMin(params: CallParams<SignedMathTestArgs['obj']['testMin(uint512,uint512)uint512'] | SignedMathTestArgs['tuple']['testMin(uint512,uint512)uint512']>) {
    const result = await this.appClient.send.call(SignedMathTestParamsFactory.testMin(params))
    return result.return as unknown as SignedMathTestReturns['testMin(uint512,uint512)uint512']
  }

  /**
   * Makes a readonly (simulated) call to the SignedMathTest smart contract using the `testAverage(uint512,uint512)uint512` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async testAverage(params: CallParams<SignedMathTestArgs['obj']['testAverage(uint512,uint512)uint512'] | SignedMathTestArgs['tuple']['testAverage(uint512,uint512)uint512']>) {
    const result = await this.appClient.send.call(SignedMathTestParamsFactory.testAverage(params))
    return result.return as unknown as SignedMathTestReturns['testAverage(uint512,uint512)uint512']
  }

  /**
   * Makes a readonly (simulated) call to the SignedMathTest smart contract using the `testAbs(uint512)uint512` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async testAbs(params: CallParams<SignedMathTestArgs['obj']['testAbs(uint512)uint512'] | SignedMathTestArgs['tuple']['testAbs(uint512)uint512']>) {
    const result = await this.appClient.send.call(SignedMathTestParamsFactory.testAbs(params))
    return result.return as unknown as SignedMathTestReturns['testAbs(uint512)uint512']
  }

  /**
   * Methods to access state for the current SignedMathTest app
   */
  state = {
  }

  public newGroup(): SignedMathTestComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a testMax(uint512,uint512)uint512 method call against the SignedMathTest contract
       */
      testMax(params: CallParams<SignedMathTestArgs['obj']['testMax(uint512,uint512)uint512'] | SignedMathTestArgs['tuple']['testMax(uint512,uint512)uint512']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.testMax(params)))
        resultMappers.push((v) => client.decodeReturnValue('testMax(uint512,uint512)uint512', v))
        return this
      },
      /**
       * Add a testMin(uint512,uint512)uint512 method call against the SignedMathTest contract
       */
      testMin(params: CallParams<SignedMathTestArgs['obj']['testMin(uint512,uint512)uint512'] | SignedMathTestArgs['tuple']['testMin(uint512,uint512)uint512']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.testMin(params)))
        resultMappers.push((v) => client.decodeReturnValue('testMin(uint512,uint512)uint512', v))
        return this
      },
      /**
       * Add a testAverage(uint512,uint512)uint512 method call against the SignedMathTest contract
       */
      testAverage(params: CallParams<SignedMathTestArgs['obj']['testAverage(uint512,uint512)uint512'] | SignedMathTestArgs['tuple']['testAverage(uint512,uint512)uint512']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.testAverage(params)))
        resultMappers.push((v) => client.decodeReturnValue('testAverage(uint512,uint512)uint512', v))
        return this
      },
      /**
       * Add a testAbs(uint512)uint512 method call against the SignedMathTest contract
       */
      testAbs(params: CallParams<SignedMathTestArgs['obj']['testAbs(uint512)uint512'] | SignedMathTestArgs['tuple']['testAbs(uint512)uint512']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.testAbs(params)))
        resultMappers.push((v) => client.decodeReturnValue('testAbs(uint512)uint512', v))
        return this
      },
      /**
       * Add a clear state call to the SignedMathTest contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as SignedMathTestComposer
  }
}
export type SignedMathTestComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the testMax(uint512,uint512)uint512 ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  testMax(params?: CallParams<SignedMathTestArgs['obj']['testMax(uint512,uint512)uint512'] | SignedMathTestArgs['tuple']['testMax(uint512,uint512)uint512']>): SignedMathTestComposer<[...TReturns, SignedMathTestReturns['testMax(uint512,uint512)uint512'] | undefined]>

  /**
   * Calls the testMin(uint512,uint512)uint512 ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  testMin(params?: CallParams<SignedMathTestArgs['obj']['testMin(uint512,uint512)uint512'] | SignedMathTestArgs['tuple']['testMin(uint512,uint512)uint512']>): SignedMathTestComposer<[...TReturns, SignedMathTestReturns['testMin(uint512,uint512)uint512'] | undefined]>

  /**
   * Calls the testAverage(uint512,uint512)uint512 ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  testAverage(params?: CallParams<SignedMathTestArgs['obj']['testAverage(uint512,uint512)uint512'] | SignedMathTestArgs['tuple']['testAverage(uint512,uint512)uint512']>): SignedMathTestComposer<[...TReturns, SignedMathTestReturns['testAverage(uint512,uint512)uint512'] | undefined]>

  /**
   * Calls the testAbs(uint512)uint512 ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  testAbs(params?: CallParams<SignedMathTestArgs['obj']['testAbs(uint512)uint512'] | SignedMathTestArgs['tuple']['testAbs(uint512)uint512']>): SignedMathTestComposer<[...TReturns, SignedMathTestReturns['testAbs(uint512)uint512'] | undefined]>

  /**
   * Makes a clear_state call to an existing instance of the SignedMathTest smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): SignedMathTestComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): SignedMathTestComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<SignedMathTestComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<SignedMathTestComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<SignedMathTestComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<SignedMathTestComposerResults<TReturns>>
}
export type SignedMathTestComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

