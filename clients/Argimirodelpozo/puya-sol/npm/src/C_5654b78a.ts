/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"C","structs":{},"methods":[{"name":"mcopy_to_right_overlap","args":[],"returns":{"type":"byte[]"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"mcopy_to_left_overlap","args":[],"returns":{"type":"byte[]"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"mcopy_in_place","args":[],"returns":{"type":"byte[]"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"mcopy_to_right_no_overlap","args":[],"returns":{"type":"byte[]"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"mcopy_to_left_no_overlap","args":[],"returns":{"type":"byte[]"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}}],"arcs":[22,28],"networks":{},"state":{"schema":{"global":{"ints":0,"bytes":0},"local":{"ints":0,"bytes":0}},"keys":{"global":{},"local":{},"box":{}},"maps":{"global":{},"local":{},"box":{}}},"bareActions":{"create":[],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[263,304,345,388,429],"errorMessage":"not payable"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"events":[]} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


/**
 * The argument types for the C contract
 */
export type CArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'mcopy_to_right_overlap()byte[]': Record<string, never>
    'mcopy_to_left_overlap()byte[]': Record<string, never>
    'mcopy_in_place()byte[]': Record<string, never>
    'mcopy_to_right_no_overlap()byte[]': Record<string, never>
    'mcopy_to_left_no_overlap()byte[]': Record<string, never>
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'mcopy_to_right_overlap()byte[]': []
    'mcopy_to_left_overlap()byte[]': []
    'mcopy_in_place()byte[]': []
    'mcopy_to_right_no_overlap()byte[]': []
    'mcopy_to_left_no_overlap()byte[]': []
  }
}

/**
 * The return type for each method
 */
export type CReturns = {
  'mcopy_to_right_overlap()byte[]': Uint8Array
  'mcopy_to_left_overlap()byte[]': Uint8Array
  'mcopy_in_place()byte[]': Uint8Array
  'mcopy_to_right_no_overlap()byte[]': Uint8Array
  'mcopy_to_left_no_overlap()byte[]': Uint8Array
}

/**
 * Defines the types of available calls and state of the C smart contract.
 */
export type CTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'mcopy_to_right_overlap()byte[]' | 'mcopy_to_right_overlap', {
      argsObj: CArgs['obj']['mcopy_to_right_overlap()byte[]']
      argsTuple: CArgs['tuple']['mcopy_to_right_overlap()byte[]']
      returns: CReturns['mcopy_to_right_overlap()byte[]']
    }>
    & Record<'mcopy_to_left_overlap()byte[]' | 'mcopy_to_left_overlap', {
      argsObj: CArgs['obj']['mcopy_to_left_overlap()byte[]']
      argsTuple: CArgs['tuple']['mcopy_to_left_overlap()byte[]']
      returns: CReturns['mcopy_to_left_overlap()byte[]']
    }>
    & Record<'mcopy_in_place()byte[]' | 'mcopy_in_place', {
      argsObj: CArgs['obj']['mcopy_in_place()byte[]']
      argsTuple: CArgs['tuple']['mcopy_in_place()byte[]']
      returns: CReturns['mcopy_in_place()byte[]']
    }>
    & Record<'mcopy_to_right_no_overlap()byte[]' | 'mcopy_to_right_no_overlap', {
      argsObj: CArgs['obj']['mcopy_to_right_no_overlap()byte[]']
      argsTuple: CArgs['tuple']['mcopy_to_right_no_overlap()byte[]']
      returns: CReturns['mcopy_to_right_no_overlap()byte[]']
    }>
    & Record<'mcopy_to_left_no_overlap()byte[]' | 'mcopy_to_left_no_overlap', {
      argsObj: CArgs['obj']['mcopy_to_left_no_overlap()byte[]']
      argsTuple: CArgs['tuple']['mcopy_to_left_no_overlap()byte[]']
      returns: CReturns['mcopy_to_left_no_overlap()byte[]']
    }>
}

/**
 * Defines the possible abi call signatures.
 */
export type CSignatures = keyof CTypes['methods']
/**
 * Defines the possible abi call signatures for methods that return a non-void value.
 */
export type CNonVoidMethodSignatures = keyof CTypes['methods'] extends infer T ? T extends keyof CTypes['methods'] ? MethodReturn<T> extends void ? never : T  : never : never
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the C smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends CSignatures> = CTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the C smart contract to the method's return type
 */
export type MethodReturn<TSignature extends CSignatures> = CTypes['methods'][TSignature]['returns']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the C smart contract
 */
export abstract class CParamsFactory {
  /**
   * Constructs a no op call for the mcopy_to_right_overlap()byte[] ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static mcopyToRightOverlap(params: CallParams<CArgs['obj']['mcopy_to_right_overlap()byte[]'] | CArgs['tuple']['mcopy_to_right_overlap()byte[]']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'mcopy_to_right_overlap()byte[]' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the mcopy_to_left_overlap()byte[] ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static mcopyToLeftOverlap(params: CallParams<CArgs['obj']['mcopy_to_left_overlap()byte[]'] | CArgs['tuple']['mcopy_to_left_overlap()byte[]']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'mcopy_to_left_overlap()byte[]' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the mcopy_in_place()byte[] ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static mcopyInPlace(params: CallParams<CArgs['obj']['mcopy_in_place()byte[]'] | CArgs['tuple']['mcopy_in_place()byte[]']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'mcopy_in_place()byte[]' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the mcopy_to_right_no_overlap()byte[] ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static mcopyToRightNoOverlap(params: CallParams<CArgs['obj']['mcopy_to_right_no_overlap()byte[]'] | CArgs['tuple']['mcopy_to_right_no_overlap()byte[]']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'mcopy_to_right_no_overlap()byte[]' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the mcopy_to_left_no_overlap()byte[] ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static mcopyToLeftNoOverlap(params: CallParams<CArgs['obj']['mcopy_to_left_no_overlap()byte[]'] | CArgs['tuple']['mcopy_to_left_no_overlap()byte[]']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'mcopy_to_left_no_overlap()byte[]' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
}

/**
 * A client to make calls to the C smart contract
 */
export class CClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `CClient`
   *
   * @param appClient An `AppClient` instance which has been created with the C app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `CClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Checks for decode errors on the given return value and maps the return value to the return type for the given method
   * @returns The typed return value or undefined if there was no value
   */
  decodeReturnValue<TSignature extends CNonVoidMethodSignatures>(method: TSignature, returnValue: ABIReturn | undefined) {
    return returnValue !== undefined ? getArc56ReturnValue<MethodReturn<TSignature>>(returnValue, this.appClient.getABIMethod(method), APP_SPEC.structs) : undefined
  }

  /**
   * Returns a new `CClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<CClient> {
    return new CClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `CClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<CClient> {
    return new CClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Makes a clear_state call to an existing instance of the C smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the C smart contract using the `mcopy_to_right_overlap()byte[]` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    mcopyToRightOverlap: (params: CallParams<CArgs['obj']['mcopy_to_right_overlap()byte[]'] | CArgs['tuple']['mcopy_to_right_overlap()byte[]']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(CParamsFactory.mcopyToRightOverlap(params))
    },

    /**
     * Makes a call to the C smart contract using the `mcopy_to_left_overlap()byte[]` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    mcopyToLeftOverlap: (params: CallParams<CArgs['obj']['mcopy_to_left_overlap()byte[]'] | CArgs['tuple']['mcopy_to_left_overlap()byte[]']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(CParamsFactory.mcopyToLeftOverlap(params))
    },

    /**
     * Makes a call to the C smart contract using the `mcopy_in_place()byte[]` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    mcopyInPlace: (params: CallParams<CArgs['obj']['mcopy_in_place()byte[]'] | CArgs['tuple']['mcopy_in_place()byte[]']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(CParamsFactory.mcopyInPlace(params))
    },

    /**
     * Makes a call to the C smart contract using the `mcopy_to_right_no_overlap()byte[]` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    mcopyToRightNoOverlap: (params: CallParams<CArgs['obj']['mcopy_to_right_no_overlap()byte[]'] | CArgs['tuple']['mcopy_to_right_no_overlap()byte[]']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(CParamsFactory.mcopyToRightNoOverlap(params))
    },

    /**
     * Makes a call to the C smart contract using the `mcopy_to_left_no_overlap()byte[]` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    mcopyToLeftNoOverlap: (params: CallParams<CArgs['obj']['mcopy_to_left_no_overlap()byte[]'] | CArgs['tuple']['mcopy_to_left_no_overlap()byte[]']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(CParamsFactory.mcopyToLeftNoOverlap(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Makes a clear_state call to an existing instance of the C smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the C smart contract using the `mcopy_to_right_overlap()byte[]` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    mcopyToRightOverlap: (params: CallParams<CArgs['obj']['mcopy_to_right_overlap()byte[]'] | CArgs['tuple']['mcopy_to_right_overlap()byte[]']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(CParamsFactory.mcopyToRightOverlap(params))
    },

    /**
     * Makes a call to the C smart contract using the `mcopy_to_left_overlap()byte[]` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    mcopyToLeftOverlap: (params: CallParams<CArgs['obj']['mcopy_to_left_overlap()byte[]'] | CArgs['tuple']['mcopy_to_left_overlap()byte[]']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(CParamsFactory.mcopyToLeftOverlap(params))
    },

    /**
     * Makes a call to the C smart contract using the `mcopy_in_place()byte[]` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    mcopyInPlace: (params: CallParams<CArgs['obj']['mcopy_in_place()byte[]'] | CArgs['tuple']['mcopy_in_place()byte[]']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(CParamsFactory.mcopyInPlace(params))
    },

    /**
     * Makes a call to the C smart contract using the `mcopy_to_right_no_overlap()byte[]` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    mcopyToRightNoOverlap: (params: CallParams<CArgs['obj']['mcopy_to_right_no_overlap()byte[]'] | CArgs['tuple']['mcopy_to_right_no_overlap()byte[]']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(CParamsFactory.mcopyToRightNoOverlap(params))
    },

    /**
     * Makes a call to the C smart contract using the `mcopy_to_left_no_overlap()byte[]` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    mcopyToLeftNoOverlap: (params: CallParams<CArgs['obj']['mcopy_to_left_no_overlap()byte[]'] | CArgs['tuple']['mcopy_to_left_no_overlap()byte[]']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(CParamsFactory.mcopyToLeftNoOverlap(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Makes a clear_state call to an existing instance of the C smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the C smart contract using the `mcopy_to_right_overlap()byte[]` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    mcopyToRightOverlap: async (params: CallParams<CArgs['obj']['mcopy_to_right_overlap()byte[]'] | CArgs['tuple']['mcopy_to_right_overlap()byte[]']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(CParamsFactory.mcopyToRightOverlap(params))
      return {...result, return: result.return as unknown as (undefined | CReturns['mcopy_to_right_overlap()byte[]'])}
    },

    /**
     * Makes a call to the C smart contract using the `mcopy_to_left_overlap()byte[]` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    mcopyToLeftOverlap: async (params: CallParams<CArgs['obj']['mcopy_to_left_overlap()byte[]'] | CArgs['tuple']['mcopy_to_left_overlap()byte[]']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(CParamsFactory.mcopyToLeftOverlap(params))
      return {...result, return: result.return as unknown as (undefined | CReturns['mcopy_to_left_overlap()byte[]'])}
    },

    /**
     * Makes a call to the C smart contract using the `mcopy_in_place()byte[]` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    mcopyInPlace: async (params: CallParams<CArgs['obj']['mcopy_in_place()byte[]'] | CArgs['tuple']['mcopy_in_place()byte[]']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(CParamsFactory.mcopyInPlace(params))
      return {...result, return: result.return as unknown as (undefined | CReturns['mcopy_in_place()byte[]'])}
    },

    /**
     * Makes a call to the C smart contract using the `mcopy_to_right_no_overlap()byte[]` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    mcopyToRightNoOverlap: async (params: CallParams<CArgs['obj']['mcopy_to_right_no_overlap()byte[]'] | CArgs['tuple']['mcopy_to_right_no_overlap()byte[]']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(CParamsFactory.mcopyToRightNoOverlap(params))
      return {...result, return: result.return as unknown as (undefined | CReturns['mcopy_to_right_no_overlap()byte[]'])}
    },

    /**
     * Makes a call to the C smart contract using the `mcopy_to_left_no_overlap()byte[]` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    mcopyToLeftNoOverlap: async (params: CallParams<CArgs['obj']['mcopy_to_left_no_overlap()byte[]'] | CArgs['tuple']['mcopy_to_left_no_overlap()byte[]']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(CParamsFactory.mcopyToLeftNoOverlap(params))
      return {...result, return: result.return as unknown as (undefined | CReturns['mcopy_to_left_no_overlap()byte[]'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new CClient(this.appClient.clone(params))
  }

  /**
   * Makes a readonly (simulated) call to the C smart contract using the `mcopy_to_right_overlap()byte[]` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async mcopyToRightOverlap(params: CallParams<CArgs['obj']['mcopy_to_right_overlap()byte[]'] | CArgs['tuple']['mcopy_to_right_overlap()byte[]']> = {args: []}) {
    const result = await this.appClient.send.call(CParamsFactory.mcopyToRightOverlap(params))
    return result.return as unknown as CReturns['mcopy_to_right_overlap()byte[]']
  }

  /**
   * Makes a readonly (simulated) call to the C smart contract using the `mcopy_to_left_overlap()byte[]` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async mcopyToLeftOverlap(params: CallParams<CArgs['obj']['mcopy_to_left_overlap()byte[]'] | CArgs['tuple']['mcopy_to_left_overlap()byte[]']> = {args: []}) {
    const result = await this.appClient.send.call(CParamsFactory.mcopyToLeftOverlap(params))
    return result.return as unknown as CReturns['mcopy_to_left_overlap()byte[]']
  }

  /**
   * Makes a readonly (simulated) call to the C smart contract using the `mcopy_in_place()byte[]` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async mcopyInPlace(params: CallParams<CArgs['obj']['mcopy_in_place()byte[]'] | CArgs['tuple']['mcopy_in_place()byte[]']> = {args: []}) {
    const result = await this.appClient.send.call(CParamsFactory.mcopyInPlace(params))
    return result.return as unknown as CReturns['mcopy_in_place()byte[]']
  }

  /**
   * Makes a readonly (simulated) call to the C smart contract using the `mcopy_to_right_no_overlap()byte[]` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async mcopyToRightNoOverlap(params: CallParams<CArgs['obj']['mcopy_to_right_no_overlap()byte[]'] | CArgs['tuple']['mcopy_to_right_no_overlap()byte[]']> = {args: []}) {
    const result = await this.appClient.send.call(CParamsFactory.mcopyToRightNoOverlap(params))
    return result.return as unknown as CReturns['mcopy_to_right_no_overlap()byte[]']
  }

  /**
   * Makes a readonly (simulated) call to the C smart contract using the `mcopy_to_left_no_overlap()byte[]` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async mcopyToLeftNoOverlap(params: CallParams<CArgs['obj']['mcopy_to_left_no_overlap()byte[]'] | CArgs['tuple']['mcopy_to_left_no_overlap()byte[]']> = {args: []}) {
    const result = await this.appClient.send.call(CParamsFactory.mcopyToLeftNoOverlap(params))
    return result.return as unknown as CReturns['mcopy_to_left_no_overlap()byte[]']
  }

  /**
   * Methods to access state for the current C app
   */
  state = {
  }

  public newGroup(): CComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a mcopy_to_right_overlap()byte[] method call against the C contract
       */
      mcopyToRightOverlap(params: CallParams<CArgs['obj']['mcopy_to_right_overlap()byte[]'] | CArgs['tuple']['mcopy_to_right_overlap()byte[]']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.mcopyToRightOverlap(params)))
        resultMappers.push((v) => client.decodeReturnValue('mcopy_to_right_overlap()byte[]', v))
        return this
      },
      /**
       * Add a mcopy_to_left_overlap()byte[] method call against the C contract
       */
      mcopyToLeftOverlap(params: CallParams<CArgs['obj']['mcopy_to_left_overlap()byte[]'] | CArgs['tuple']['mcopy_to_left_overlap()byte[]']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.mcopyToLeftOverlap(params)))
        resultMappers.push((v) => client.decodeReturnValue('mcopy_to_left_overlap()byte[]', v))
        return this
      },
      /**
       * Add a mcopy_in_place()byte[] method call against the C contract
       */
      mcopyInPlace(params: CallParams<CArgs['obj']['mcopy_in_place()byte[]'] | CArgs['tuple']['mcopy_in_place()byte[]']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.mcopyInPlace(params)))
        resultMappers.push((v) => client.decodeReturnValue('mcopy_in_place()byte[]', v))
        return this
      },
      /**
       * Add a mcopy_to_right_no_overlap()byte[] method call against the C contract
       */
      mcopyToRightNoOverlap(params: CallParams<CArgs['obj']['mcopy_to_right_no_overlap()byte[]'] | CArgs['tuple']['mcopy_to_right_no_overlap()byte[]']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.mcopyToRightNoOverlap(params)))
        resultMappers.push((v) => client.decodeReturnValue('mcopy_to_right_no_overlap()byte[]', v))
        return this
      },
      /**
       * Add a mcopy_to_left_no_overlap()byte[] method call against the C contract
       */
      mcopyToLeftNoOverlap(params: CallParams<CArgs['obj']['mcopy_to_left_no_overlap()byte[]'] | CArgs['tuple']['mcopy_to_left_no_overlap()byte[]']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.mcopyToLeftNoOverlap(params)))
        resultMappers.push((v) => client.decodeReturnValue('mcopy_to_left_no_overlap()byte[]', v))
        return this
      },
      /**
       * Add a clear state call to the C contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as CComposer
  }
}
export type CComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the mcopy_to_right_overlap()byte[] ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  mcopyToRightOverlap(params?: CallParams<CArgs['obj']['mcopy_to_right_overlap()byte[]'] | CArgs['tuple']['mcopy_to_right_overlap()byte[]']>): CComposer<[...TReturns, CReturns['mcopy_to_right_overlap()byte[]'] | undefined]>

  /**
   * Calls the mcopy_to_left_overlap()byte[] ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  mcopyToLeftOverlap(params?: CallParams<CArgs['obj']['mcopy_to_left_overlap()byte[]'] | CArgs['tuple']['mcopy_to_left_overlap()byte[]']>): CComposer<[...TReturns, CReturns['mcopy_to_left_overlap()byte[]'] | undefined]>

  /**
   * Calls the mcopy_in_place()byte[] ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  mcopyInPlace(params?: CallParams<CArgs['obj']['mcopy_in_place()byte[]'] | CArgs['tuple']['mcopy_in_place()byte[]']>): CComposer<[...TReturns, CReturns['mcopy_in_place()byte[]'] | undefined]>

  /**
   * Calls the mcopy_to_right_no_overlap()byte[] ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  mcopyToRightNoOverlap(params?: CallParams<CArgs['obj']['mcopy_to_right_no_overlap()byte[]'] | CArgs['tuple']['mcopy_to_right_no_overlap()byte[]']>): CComposer<[...TReturns, CReturns['mcopy_to_right_no_overlap()byte[]'] | undefined]>

  /**
   * Calls the mcopy_to_left_no_overlap()byte[] ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  mcopyToLeftNoOverlap(params?: CallParams<CArgs['obj']['mcopy_to_left_no_overlap()byte[]'] | CArgs['tuple']['mcopy_to_left_no_overlap()byte[]']>): CComposer<[...TReturns, CReturns['mcopy_to_left_no_overlap()byte[]'] | undefined]>

  /**
   * Makes a clear_state call to an existing instance of the C smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): CComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): CComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<CComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<CComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<CComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<CComposerResults<TReturns>>
}
export type CComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

