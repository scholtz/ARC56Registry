/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"Derived","structs":{},"methods":[{"name":"setData","args":[{"type":"uint256","name":"__arc4_base"},{"type":"uint256","name":"__arc4_derived"}],"returns":{"type":"bool"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}},{"name":"getViaDerived","args":[],"returns":{"type":"(uint256,uint256)"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}},{"name":"getViaBase","args":[],"returns":{"type":"uint256"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}}],"arcs":[22,28],"networks":{},"state":{"schema":{"global":{"ints":0,"bytes":2},"local":{"ints":0,"bytes":0}},"keys":{"global":{"dataDerived":{"keyType":"AVMString","valueType":"AVMBytes","key":"ZGF0YURlcml2ZWQ="},"dataBase":{"keyType":"AVMString","valueType":"AVMBytes","key":"ZGF0YUJhc2U="}},"local":{},"box":{}},"maps":{"global":{},"local":{},"box":{}}},"bareActions":{"create":[],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[151,186],"errorMessage":"check dataBase exists"},{"pc":[190],"errorMessage":"check dataDerived exists"},{"pc":[233,243],"errorMessage":"invalid number of bytes for arc4.uint256"},{"pc":[147,182,256],"errorMessage":"not payable"},{"pc":[156,196,209],"errorMessage":"overflow"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"events":[]} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


/**
 * The argument types for the Derived contract
 */
export type DerivedArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'setData(uint256,uint256)bool': {
      __arc4Base: bigint | number
      __arc4Derived: bigint | number
    }
    'getViaDerived()(uint256,uint256)': Record<string, never>
    'getViaBase()uint256': Record<string, never>
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'setData(uint256,uint256)bool': [__arc4Base: bigint | number, __arc4Derived: bigint | number]
    'getViaDerived()(uint256,uint256)': []
    'getViaBase()uint256': []
  }
}

/**
 * The return type for each method
 */
export type DerivedReturns = {
  'setData(uint256,uint256)bool': boolean
  'getViaDerived()(uint256,uint256)': [bigint, bigint]
  'getViaBase()uint256': bigint
}

/**
 * Defines the types of available calls and state of the Derived smart contract.
 */
export type DerivedTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'setData(uint256,uint256)bool' | 'setData', {
      argsObj: DerivedArgs['obj']['setData(uint256,uint256)bool']
      argsTuple: DerivedArgs['tuple']['setData(uint256,uint256)bool']
      returns: DerivedReturns['setData(uint256,uint256)bool']
    }>
    & Record<'getViaDerived()(uint256,uint256)' | 'getViaDerived', {
      argsObj: DerivedArgs['obj']['getViaDerived()(uint256,uint256)']
      argsTuple: DerivedArgs['tuple']['getViaDerived()(uint256,uint256)']
      returns: DerivedReturns['getViaDerived()(uint256,uint256)']
    }>
    & Record<'getViaBase()uint256' | 'getViaBase', {
      argsObj: DerivedArgs['obj']['getViaBase()uint256']
      argsTuple: DerivedArgs['tuple']['getViaBase()uint256']
      returns: DerivedReturns['getViaBase()uint256']
    }>
  /**
   * Defines the shape of the state of the application.
   */
  state: {
    global: {
      keys: {
        dataDerived: BinaryState
        dataBase: BinaryState
      }
      maps: {}
    }
  }
}

/**
 * Defines the possible abi call signatures.
 */
export type DerivedSignatures = keyof DerivedTypes['methods']
/**
 * Defines the possible abi call signatures for methods that return a non-void value.
 */
export type DerivedNonVoidMethodSignatures = keyof DerivedTypes['methods'] extends infer T ? T extends keyof DerivedTypes['methods'] ? MethodReturn<T> extends void ? never : T  : never : never
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the Derived smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends DerivedSignatures> = DerivedTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the Derived smart contract to the method's return type
 */
export type MethodReturn<TSignature extends DerivedSignatures> = DerivedTypes['methods'][TSignature]['returns']

/**
 * Defines the shape of the keyed global state of the application.
 */
export type GlobalKeysState = DerivedTypes['state']['global']['keys']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the Derived smart contract
 */
export abstract class DerivedParamsFactory {
  /**
   * Constructs a no op call for the setData(uint256,uint256)bool ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static setData(params: CallParams<DerivedArgs['obj']['setData(uint256,uint256)bool'] | DerivedArgs['tuple']['setData(uint256,uint256)bool']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'setData(uint256,uint256)bool' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.__arc4Base, params.args.__arc4Derived],
    }
  }
  /**
   * Constructs a no op call for the getViaDerived()(uint256,uint256) ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static getViaDerived(params: CallParams<DerivedArgs['obj']['getViaDerived()(uint256,uint256)'] | DerivedArgs['tuple']['getViaDerived()(uint256,uint256)']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'getViaDerived()(uint256,uint256)' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the getViaBase()uint256 ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static getViaBase(params: CallParams<DerivedArgs['obj']['getViaBase()uint256'] | DerivedArgs['tuple']['getViaBase()uint256']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'getViaBase()uint256' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
}

/**
 * A client to make calls to the Derived smart contract
 */
export class DerivedClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `DerivedClient`
   *
   * @param appClient An `AppClient` instance which has been created with the Derived app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `DerivedClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Checks for decode errors on the given return value and maps the return value to the return type for the given method
   * @returns The typed return value or undefined if there was no value
   */
  decodeReturnValue<TSignature extends DerivedNonVoidMethodSignatures>(method: TSignature, returnValue: ABIReturn | undefined) {
    return returnValue !== undefined ? getArc56ReturnValue<MethodReturn<TSignature>>(returnValue, this.appClient.getABIMethod(method), APP_SPEC.structs) : undefined
  }

  /**
   * Returns a new `DerivedClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<DerivedClient> {
    return new DerivedClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `DerivedClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<DerivedClient> {
    return new DerivedClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Makes a clear_state call to an existing instance of the Derived smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the Derived smart contract using the `setData(uint256,uint256)bool` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    setData: (params: CallParams<DerivedArgs['obj']['setData(uint256,uint256)bool'] | DerivedArgs['tuple']['setData(uint256,uint256)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(DerivedParamsFactory.setData(params))
    },

    /**
     * Makes a call to the Derived smart contract using the `getViaDerived()(uint256,uint256)` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    getViaDerived: (params: CallParams<DerivedArgs['obj']['getViaDerived()(uint256,uint256)'] | DerivedArgs['tuple']['getViaDerived()(uint256,uint256)']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(DerivedParamsFactory.getViaDerived(params))
    },

    /**
     * Makes a call to the Derived smart contract using the `getViaBase()uint256` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    getViaBase: (params: CallParams<DerivedArgs['obj']['getViaBase()uint256'] | DerivedArgs['tuple']['getViaBase()uint256']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(DerivedParamsFactory.getViaBase(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Makes a clear_state call to an existing instance of the Derived smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the Derived smart contract using the `setData(uint256,uint256)bool` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    setData: (params: CallParams<DerivedArgs['obj']['setData(uint256,uint256)bool'] | DerivedArgs['tuple']['setData(uint256,uint256)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(DerivedParamsFactory.setData(params))
    },

    /**
     * Makes a call to the Derived smart contract using the `getViaDerived()(uint256,uint256)` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    getViaDerived: (params: CallParams<DerivedArgs['obj']['getViaDerived()(uint256,uint256)'] | DerivedArgs['tuple']['getViaDerived()(uint256,uint256)']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(DerivedParamsFactory.getViaDerived(params))
    },

    /**
     * Makes a call to the Derived smart contract using the `getViaBase()uint256` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    getViaBase: (params: CallParams<DerivedArgs['obj']['getViaBase()uint256'] | DerivedArgs['tuple']['getViaBase()uint256']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(DerivedParamsFactory.getViaBase(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Makes a clear_state call to an existing instance of the Derived smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the Derived smart contract using the `setData(uint256,uint256)bool` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    setData: async (params: CallParams<DerivedArgs['obj']['setData(uint256,uint256)bool'] | DerivedArgs['tuple']['setData(uint256,uint256)bool']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(DerivedParamsFactory.setData(params))
      return {...result, return: result.return as unknown as (undefined | DerivedReturns['setData(uint256,uint256)bool'])}
    },

    /**
     * Makes a call to the Derived smart contract using the `getViaDerived()(uint256,uint256)` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    getViaDerived: async (params: CallParams<DerivedArgs['obj']['getViaDerived()(uint256,uint256)'] | DerivedArgs['tuple']['getViaDerived()(uint256,uint256)']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(DerivedParamsFactory.getViaDerived(params))
      return {...result, return: result.return as unknown as (undefined | DerivedReturns['getViaDerived()(uint256,uint256)'])}
    },

    /**
     * Makes a call to the Derived smart contract using the `getViaBase()uint256` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    getViaBase: async (params: CallParams<DerivedArgs['obj']['getViaBase()uint256'] | DerivedArgs['tuple']['getViaBase()uint256']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(DerivedParamsFactory.getViaBase(params))
      return {...result, return: result.return as unknown as (undefined | DerivedReturns['getViaBase()uint256'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new DerivedClient(this.appClient.clone(params))
  }

  /**
   * Methods to access state for the current Derived app
   */
  state = {
    /**
     * Methods to access global state for the current Derived app
     */
    global: {
      /**
       * Get all current keyed values from global state
       */
      getAll: async (): Promise<Partial<Expand<GlobalKeysState>>> => {
        const result = await this.appClient.state.global.getAll()
        return {
          dataDerived: new BinaryStateValue(result.dataDerived),
          dataBase: new BinaryStateValue(result.dataBase),
        }
      },
      /**
       * Get the current value of the dataDerived key in global state
       */
      dataDerived: async (): Promise<BinaryState> => { return new BinaryStateValue((await this.appClient.state.global.getValue("dataDerived")) as Uint8Array | undefined) },
      /**
       * Get the current value of the dataBase key in global state
       */
      dataBase: async (): Promise<BinaryState> => { return new BinaryStateValue((await this.appClient.state.global.getValue("dataBase")) as Uint8Array | undefined) },
    },
  }

  public newGroup(): DerivedComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a setData(uint256,uint256)bool method call against the Derived contract
       */
      setData(params: CallParams<DerivedArgs['obj']['setData(uint256,uint256)bool'] | DerivedArgs['tuple']['setData(uint256,uint256)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.setData(params)))
        resultMappers.push((v) => client.decodeReturnValue('setData(uint256,uint256)bool', v))
        return this
      },
      /**
       * Add a getViaDerived()(uint256,uint256) method call against the Derived contract
       */
      getViaDerived(params: CallParams<DerivedArgs['obj']['getViaDerived()(uint256,uint256)'] | DerivedArgs['tuple']['getViaDerived()(uint256,uint256)']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.getViaDerived(params)))
        resultMappers.push((v) => client.decodeReturnValue('getViaDerived()(uint256,uint256)', v))
        return this
      },
      /**
       * Add a getViaBase()uint256 method call against the Derived contract
       */
      getViaBase(params: CallParams<DerivedArgs['obj']['getViaBase()uint256'] | DerivedArgs['tuple']['getViaBase()uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.getViaBase(params)))
        resultMappers.push((v) => client.decodeReturnValue('getViaBase()uint256', v))
        return this
      },
      /**
       * Add a clear state call to the Derived contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as DerivedComposer
  }
}
export type DerivedComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the setData(uint256,uint256)bool ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  setData(params?: CallParams<DerivedArgs['obj']['setData(uint256,uint256)bool'] | DerivedArgs['tuple']['setData(uint256,uint256)bool']>): DerivedComposer<[...TReturns, DerivedReturns['setData(uint256,uint256)bool'] | undefined]>

  /**
   * Calls the getViaDerived()(uint256,uint256) ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  getViaDerived(params?: CallParams<DerivedArgs['obj']['getViaDerived()(uint256,uint256)'] | DerivedArgs['tuple']['getViaDerived()(uint256,uint256)']>): DerivedComposer<[...TReturns, DerivedReturns['getViaDerived()(uint256,uint256)'] | undefined]>

  /**
   * Calls the getViaBase()uint256 ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  getViaBase(params?: CallParams<DerivedArgs['obj']['getViaBase()uint256'] | DerivedArgs['tuple']['getViaBase()uint256']>): DerivedComposer<[...TReturns, DerivedReturns['getViaBase()uint256'] | undefined]>

  /**
   * Makes a clear_state call to an existing instance of the Derived smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): DerivedComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): DerivedComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<DerivedComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<DerivedComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<DerivedComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<DerivedComposerResults<TReturns>>
}
export type DerivedComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

