/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"TimelockTest","structs":{},"methods":[{"name":"initOperation","args":[{"type":"uint512","name":"operationId"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}},{"name":"schedule","args":[{"type":"uint512","name":"target"},{"type":"uint512","name":"value"},{"type":"uint512","name":"delay"},{"type":"uint512","name":"currentTime"}],"returns":{"type":"uint512"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":" @dev Schedule an operation with a delay. Only admin.\n currentTime is passed as a parameter (AVM has no block.timestamp equivalent).\n Returns the assigned operationId.","events":[],"recommendations":{}},{"name":"execute","args":[{"type":"uint512","name":"operationId"},{"type":"uint512","name":"currentTime"}],"returns":{"type":"bool"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":" @dev Execute a scheduled operation. Only admin.\n Requires the operation is not yet executed and the delay has passed.","events":[],"recommendations":{}},{"name":"cancel","args":[{"type":"uint512","name":"operationId"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":" @dev Cancel a pending operation. Only admin.\n Sets the timestamp to 0, effectively cancelling it.","events":[],"recommendations":{}},{"name":"isOperationReady","args":[{"type":"uint512","name":"operationId"},{"type":"uint512","name":"currentTime"}],"returns":{"type":"bool"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"desc":" @dev Returns true if the operation is ready to execute.","events":[],"recommendations":{}},{"name":"isOperationDone","args":[{"type":"uint512","name":"operationId"}],"returns":{"type":"bool"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"desc":" @dev Returns true if the operation has been executed.","events":[],"recommendations":{}},{"name":"getTimestamp","args":[{"type":"uint512","name":"operationId"}],"returns":{"type":"uint512"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"desc":" @dev Returns the scheduled timestamp for an operation.","events":[],"recommendations":{}},{"name":"getAdmin","args":[],"returns":{"type":"address"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"desc":" @dev Returns the admin address.","events":[],"recommendations":{}},{"name":"getMinDelay","args":[],"returns":{"type":"uint512"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"desc":" @dev Returns the minimum delay.","events":[],"recommendations":{}},{"name":"setMinDelay","args":[{"type":"uint512","name":"newDelay"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":" @dev Updates the minimum delay. Only admin.","events":[],"recommendations":{}}],"arcs":[22,28],"desc":" @dev Test wrapper for Timelock with minDelay=10.","networks":{},"state":{"schema":{"global":{"ints":0,"bytes":3},"local":{"ints":0,"bytes":0}},"keys":{"global":{"_admin":{"keyType":"AVMString","valueType":"address","key":"X2FkbWlu"},"_minDelay":{"keyType":"AVMString","valueType":"AVMBytes","key":"X21pbkRlbGF5"},"_operationCount":{"keyType":"AVMString","valueType":"AVMBytes","key":"X29wZXJhdGlvbkNvdW50"}},"local":{},"box":{}},"maps":{"global":{},"local":{},"box":{"_opTimestamp":{"keyType":"AVMBytes","valueType":"AVMBytes","prefix":"X29wVGltZXN0YW1w"},"_opExecuted":{"keyType":"AVMBytes","valueType":"uint64","prefix":"X29wRXhlY3V0ZWQ="},"_opTarget":{"keyType":"AVMBytes","valueType":"AVMBytes","prefix":"X29wVGFyZ2V0"},"_opValue":{"keyType":"AVMBytes","valueType":"AVMBytes","prefix":"X29wVmFsdWU="}}}},"bareActions":{"create":[],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[506,590],"errorMessage":"Timelock: already executed"},{"pc":[343,478,563,843],"errorMessage":"Timelock: caller is not admin"},{"pc":[351],"errorMessage":"Timelock: insufficient delay"},{"pc":[525],"errorMessage":"Timelock: not yet ready"},{"pc":[521,606],"errorMessage":"Timelock: operation not scheduled"},{"pc":[341,476,561,801,841],"errorMessage":"check _admin exists"},{"pc":[347,812,847],"errorMessage":"check _minDelay exists"},{"pc":[356],"errorMessage":"check _operationCount exists"},{"pc":[249,311,319,327,335,462,470,555,630,640,718,763,835],"errorMessage":"invalid number of bytes for arc4.uint512"},{"pc":[446,789,817],"errorMessage":"overflow"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"events":[]} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


/**
 * The argument types for the TimelockTest contract
 */
export type TimelockTestArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'initOperation(uint512)void': {
      operationId: bigint | number
    }
    'schedule(uint512,uint512,uint512,uint512)uint512': {
      target: bigint | number
      value: bigint | number
      delay: bigint | number
      currentTime: bigint | number
    }
    'execute(uint512,uint512)bool': {
      operationId: bigint | number
      currentTime: bigint | number
    }
    'cancel(uint512)void': {
      operationId: bigint | number
    }
    'isOperationReady(uint512,uint512)bool': {
      operationId: bigint | number
      currentTime: bigint | number
    }
    'isOperationDone(uint512)bool': {
      operationId: bigint | number
    }
    'getTimestamp(uint512)uint512': {
      operationId: bigint | number
    }
    'getAdmin()address': Record<string, never>
    'getMinDelay()uint512': Record<string, never>
    'setMinDelay(uint512)void': {
      newDelay: bigint | number
    }
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'initOperation(uint512)void': [operationId: bigint | number]
    'schedule(uint512,uint512,uint512,uint512)uint512': [target: bigint | number, value: bigint | number, delay: bigint | number, currentTime: bigint | number]
    'execute(uint512,uint512)bool': [operationId: bigint | number, currentTime: bigint | number]
    'cancel(uint512)void': [operationId: bigint | number]
    'isOperationReady(uint512,uint512)bool': [operationId: bigint | number, currentTime: bigint | number]
    'isOperationDone(uint512)bool': [operationId: bigint | number]
    'getTimestamp(uint512)uint512': [operationId: bigint | number]
    'getAdmin()address': []
    'getMinDelay()uint512': []
    'setMinDelay(uint512)void': [newDelay: bigint | number]
  }
}

/**
 * The return type for each method
 */
export type TimelockTestReturns = {
  'initOperation(uint512)void': void
  'schedule(uint512,uint512,uint512,uint512)uint512': bigint
  'execute(uint512,uint512)bool': boolean
  'cancel(uint512)void': void
  'isOperationReady(uint512,uint512)bool': boolean
  'isOperationDone(uint512)bool': boolean
  'getTimestamp(uint512)uint512': bigint
  'getAdmin()address': string
  'getMinDelay()uint512': bigint
  'setMinDelay(uint512)void': void
}

/**
 * Defines the types of available calls and state of the TimelockTest smart contract.
 */
export type TimelockTestTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'initOperation(uint512)void' | 'initOperation', {
      argsObj: TimelockTestArgs['obj']['initOperation(uint512)void']
      argsTuple: TimelockTestArgs['tuple']['initOperation(uint512)void']
      returns: TimelockTestReturns['initOperation(uint512)void']
    }>
    & Record<'schedule(uint512,uint512,uint512,uint512)uint512' | 'schedule', {
      argsObj: TimelockTestArgs['obj']['schedule(uint512,uint512,uint512,uint512)uint512']
      argsTuple: TimelockTestArgs['tuple']['schedule(uint512,uint512,uint512,uint512)uint512']
      returns: TimelockTestReturns['schedule(uint512,uint512,uint512,uint512)uint512']
    }>
    & Record<'execute(uint512,uint512)bool' | 'execute', {
      argsObj: TimelockTestArgs['obj']['execute(uint512,uint512)bool']
      argsTuple: TimelockTestArgs['tuple']['execute(uint512,uint512)bool']
      returns: TimelockTestReturns['execute(uint512,uint512)bool']
    }>
    & Record<'cancel(uint512)void' | 'cancel', {
      argsObj: TimelockTestArgs['obj']['cancel(uint512)void']
      argsTuple: TimelockTestArgs['tuple']['cancel(uint512)void']
      returns: TimelockTestReturns['cancel(uint512)void']
    }>
    & Record<'isOperationReady(uint512,uint512)bool' | 'isOperationReady', {
      argsObj: TimelockTestArgs['obj']['isOperationReady(uint512,uint512)bool']
      argsTuple: TimelockTestArgs['tuple']['isOperationReady(uint512,uint512)bool']
      returns: TimelockTestReturns['isOperationReady(uint512,uint512)bool']
    }>
    & Record<'isOperationDone(uint512)bool' | 'isOperationDone', {
      argsObj: TimelockTestArgs['obj']['isOperationDone(uint512)bool']
      argsTuple: TimelockTestArgs['tuple']['isOperationDone(uint512)bool']
      returns: TimelockTestReturns['isOperationDone(uint512)bool']
    }>
    & Record<'getTimestamp(uint512)uint512' | 'getTimestamp', {
      argsObj: TimelockTestArgs['obj']['getTimestamp(uint512)uint512']
      argsTuple: TimelockTestArgs['tuple']['getTimestamp(uint512)uint512']
      returns: TimelockTestReturns['getTimestamp(uint512)uint512']
    }>
    & Record<'getAdmin()address' | 'getAdmin', {
      argsObj: TimelockTestArgs['obj']['getAdmin()address']
      argsTuple: TimelockTestArgs['tuple']['getAdmin()address']
      returns: TimelockTestReturns['getAdmin()address']
    }>
    & Record<'getMinDelay()uint512' | 'getMinDelay', {
      argsObj: TimelockTestArgs['obj']['getMinDelay()uint512']
      argsTuple: TimelockTestArgs['tuple']['getMinDelay()uint512']
      returns: TimelockTestReturns['getMinDelay()uint512']
    }>
    & Record<'setMinDelay(uint512)void' | 'setMinDelay', {
      argsObj: TimelockTestArgs['obj']['setMinDelay(uint512)void']
      argsTuple: TimelockTestArgs['tuple']['setMinDelay(uint512)void']
      returns: TimelockTestReturns['setMinDelay(uint512)void']
    }>
  /**
   * Defines the shape of the state of the application.
   */
  state: {
    global: {
      keys: {
        _admin: string
        _minDelay: BinaryState
        _operationCount: BinaryState
      }
      maps: {}
    }
    box: {
      keys: {}
      maps: {
        _opTimestamp: Map<Uint8Array | string, Uint8Array>
        _opExecuted: Map<Uint8Array | string, bigint>
        _opTarget: Map<Uint8Array | string, Uint8Array>
        _opValue: Map<Uint8Array | string, Uint8Array>
      }
    }
  }
}

/**
 * Defines the possible abi call signatures.
 */
export type TimelockTestSignatures = keyof TimelockTestTypes['methods']
/**
 * Defines the possible abi call signatures for methods that return a non-void value.
 */
export type TimelockTestNonVoidMethodSignatures = keyof TimelockTestTypes['methods'] extends infer T ? T extends keyof TimelockTestTypes['methods'] ? MethodReturn<T> extends void ? never : T  : never : never
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the TimelockTest smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends TimelockTestSignatures> = TimelockTestTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the TimelockTest smart contract to the method's return type
 */
export type MethodReturn<TSignature extends TimelockTestSignatures> = TimelockTestTypes['methods'][TSignature]['returns']

/**
 * Defines the shape of the keyed global state of the application.
 */
export type GlobalKeysState = TimelockTestTypes['state']['global']['keys']

/**
 * Defines the shape of the keyed box state of the application.
 */
export type BoxKeysState = TimelockTestTypes['state']['box']['keys']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the TimelockTest smart contract
 */
export abstract class TimelockTestParamsFactory {
  /**
   * Constructs a no op call for the initOperation(uint512)void ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static initOperation(params: CallParams<TimelockTestArgs['obj']['initOperation(uint512)void'] | TimelockTestArgs['tuple']['initOperation(uint512)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'initOperation(uint512)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.operationId],
    }
  }
  /**
   * Constructs a no op call for the schedule(uint512,uint512,uint512,uint512)uint512 ABI method
   *
  *  @dev Schedule an operation with a delay. Only admin.
  currentTime is passed as a parameter (AVM has no block.timestamp equivalent).
  Returns the assigned operationId.

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static schedule(params: CallParams<TimelockTestArgs['obj']['schedule(uint512,uint512,uint512,uint512)uint512'] | TimelockTestArgs['tuple']['schedule(uint512,uint512,uint512,uint512)uint512']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'schedule(uint512,uint512,uint512,uint512)uint512' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.target, params.args.value, params.args.delay, params.args.currentTime],
    }
  }
  /**
   * Constructs a no op call for the execute(uint512,uint512)bool ABI method
   *
  *  @dev Execute a scheduled operation. Only admin.
  Requires the operation is not yet executed and the delay has passed.

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static execute(params: CallParams<TimelockTestArgs['obj']['execute(uint512,uint512)bool'] | TimelockTestArgs['tuple']['execute(uint512,uint512)bool']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'execute(uint512,uint512)bool' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.operationId, params.args.currentTime],
    }
  }
  /**
   * Constructs a no op call for the cancel(uint512)void ABI method
   *
  *  @dev Cancel a pending operation. Only admin.
  Sets the timestamp to 0, effectively cancelling it.

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static cancel(params: CallParams<TimelockTestArgs['obj']['cancel(uint512)void'] | TimelockTestArgs['tuple']['cancel(uint512)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'cancel(uint512)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.operationId],
    }
  }
  /**
   * Constructs a no op call for the isOperationReady(uint512,uint512)bool ABI method
   *
   *  @dev Returns true if the operation is ready to execute.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static isOperationReady(params: CallParams<TimelockTestArgs['obj']['isOperationReady(uint512,uint512)bool'] | TimelockTestArgs['tuple']['isOperationReady(uint512,uint512)bool']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'isOperationReady(uint512,uint512)bool' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.operationId, params.args.currentTime],
    }
  }
  /**
   * Constructs a no op call for the isOperationDone(uint512)bool ABI method
   *
   *  @dev Returns true if the operation has been executed.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static isOperationDone(params: CallParams<TimelockTestArgs['obj']['isOperationDone(uint512)bool'] | TimelockTestArgs['tuple']['isOperationDone(uint512)bool']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'isOperationDone(uint512)bool' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.operationId],
    }
  }
  /**
   * Constructs a no op call for the getTimestamp(uint512)uint512 ABI method
   *
   *  @dev Returns the scheduled timestamp for an operation.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static getTimestamp(params: CallParams<TimelockTestArgs['obj']['getTimestamp(uint512)uint512'] | TimelockTestArgs['tuple']['getTimestamp(uint512)uint512']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'getTimestamp(uint512)uint512' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.operationId],
    }
  }
  /**
   * Constructs a no op call for the getAdmin()address ABI method
   *
   *  @dev Returns the admin address.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static getAdmin(params: CallParams<TimelockTestArgs['obj']['getAdmin()address'] | TimelockTestArgs['tuple']['getAdmin()address']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'getAdmin()address' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the getMinDelay()uint512 ABI method
   *
   *  @dev Returns the minimum delay.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static getMinDelay(params: CallParams<TimelockTestArgs['obj']['getMinDelay()uint512'] | TimelockTestArgs['tuple']['getMinDelay()uint512']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'getMinDelay()uint512' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the setMinDelay(uint512)void ABI method
   *
   *  @dev Updates the minimum delay. Only admin.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static setMinDelay(params: CallParams<TimelockTestArgs['obj']['setMinDelay(uint512)void'] | TimelockTestArgs['tuple']['setMinDelay(uint512)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'setMinDelay(uint512)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.newDelay],
    }
  }
}

/**
 * A client to make calls to the TimelockTest smart contract
 */
export class TimelockTestClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `TimelockTestClient`
   *
   * @param appClient An `AppClient` instance which has been created with the TimelockTest app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `TimelockTestClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Checks for decode errors on the given return value and maps the return value to the return type for the given method
   * @returns The typed return value or undefined if there was no value
   */
  decodeReturnValue<TSignature extends TimelockTestNonVoidMethodSignatures>(method: TSignature, returnValue: ABIReturn | undefined) {
    return returnValue !== undefined ? getArc56ReturnValue<MethodReturn<TSignature>>(returnValue, this.appClient.getABIMethod(method), APP_SPEC.structs) : undefined
  }

  /**
   * Returns a new `TimelockTestClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<TimelockTestClient> {
    return new TimelockTestClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `TimelockTestClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<TimelockTestClient> {
    return new TimelockTestClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Makes a clear_state call to an existing instance of the TimelockTest smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the TimelockTest smart contract using the `initOperation(uint512)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    initOperation: (params: CallParams<TimelockTestArgs['obj']['initOperation(uint512)void'] | TimelockTestArgs['tuple']['initOperation(uint512)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(TimelockTestParamsFactory.initOperation(params))
    },

    /**
     * Makes a call to the TimelockTest smart contract using the `schedule(uint512,uint512,uint512,uint512)uint512` ABI method.
     *
    *  @dev Schedule an operation with a delay. Only admin.
    currentTime is passed as a parameter (AVM has no block.timestamp equivalent).
    Returns the assigned operationId.

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    schedule: (params: CallParams<TimelockTestArgs['obj']['schedule(uint512,uint512,uint512,uint512)uint512'] | TimelockTestArgs['tuple']['schedule(uint512,uint512,uint512,uint512)uint512']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(TimelockTestParamsFactory.schedule(params))
    },

    /**
     * Makes a call to the TimelockTest smart contract using the `execute(uint512,uint512)bool` ABI method.
     *
    *  @dev Execute a scheduled operation. Only admin.
    Requires the operation is not yet executed and the delay has passed.

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    execute: (params: CallParams<TimelockTestArgs['obj']['execute(uint512,uint512)bool'] | TimelockTestArgs['tuple']['execute(uint512,uint512)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(TimelockTestParamsFactory.execute(params))
    },

    /**
     * Makes a call to the TimelockTest smart contract using the `cancel(uint512)void` ABI method.
     *
    *  @dev Cancel a pending operation. Only admin.
    Sets the timestamp to 0, effectively cancelling it.

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    cancel: (params: CallParams<TimelockTestArgs['obj']['cancel(uint512)void'] | TimelockTestArgs['tuple']['cancel(uint512)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(TimelockTestParamsFactory.cancel(params))
    },

    /**
     * Makes a call to the TimelockTest smart contract using the `isOperationReady(uint512,uint512)bool` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     *  @dev Returns true if the operation is ready to execute.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    isOperationReady: (params: CallParams<TimelockTestArgs['obj']['isOperationReady(uint512,uint512)bool'] | TimelockTestArgs['tuple']['isOperationReady(uint512,uint512)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(TimelockTestParamsFactory.isOperationReady(params))
    },

    /**
     * Makes a call to the TimelockTest smart contract using the `isOperationDone(uint512)bool` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     *  @dev Returns true if the operation has been executed.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    isOperationDone: (params: CallParams<TimelockTestArgs['obj']['isOperationDone(uint512)bool'] | TimelockTestArgs['tuple']['isOperationDone(uint512)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(TimelockTestParamsFactory.isOperationDone(params))
    },

    /**
     * Makes a call to the TimelockTest smart contract using the `getTimestamp(uint512)uint512` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     *  @dev Returns the scheduled timestamp for an operation.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    getTimestamp: (params: CallParams<TimelockTestArgs['obj']['getTimestamp(uint512)uint512'] | TimelockTestArgs['tuple']['getTimestamp(uint512)uint512']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(TimelockTestParamsFactory.getTimestamp(params))
    },

    /**
     * Makes a call to the TimelockTest smart contract using the `getAdmin()address` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     *  @dev Returns the admin address.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    getAdmin: (params: CallParams<TimelockTestArgs['obj']['getAdmin()address'] | TimelockTestArgs['tuple']['getAdmin()address']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(TimelockTestParamsFactory.getAdmin(params))
    },

    /**
     * Makes a call to the TimelockTest smart contract using the `getMinDelay()uint512` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     *  @dev Returns the minimum delay.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    getMinDelay: (params: CallParams<TimelockTestArgs['obj']['getMinDelay()uint512'] | TimelockTestArgs['tuple']['getMinDelay()uint512']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(TimelockTestParamsFactory.getMinDelay(params))
    },

    /**
     * Makes a call to the TimelockTest smart contract using the `setMinDelay(uint512)void` ABI method.
     *
     *  @dev Updates the minimum delay. Only admin.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    setMinDelay: (params: CallParams<TimelockTestArgs['obj']['setMinDelay(uint512)void'] | TimelockTestArgs['tuple']['setMinDelay(uint512)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(TimelockTestParamsFactory.setMinDelay(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Makes a clear_state call to an existing instance of the TimelockTest smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the TimelockTest smart contract using the `initOperation(uint512)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    initOperation: (params: CallParams<TimelockTestArgs['obj']['initOperation(uint512)void'] | TimelockTestArgs['tuple']['initOperation(uint512)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(TimelockTestParamsFactory.initOperation(params))
    },

    /**
     * Makes a call to the TimelockTest smart contract using the `schedule(uint512,uint512,uint512,uint512)uint512` ABI method.
     *
    *  @dev Schedule an operation with a delay. Only admin.
    currentTime is passed as a parameter (AVM has no block.timestamp equivalent).
    Returns the assigned operationId.

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    schedule: (params: CallParams<TimelockTestArgs['obj']['schedule(uint512,uint512,uint512,uint512)uint512'] | TimelockTestArgs['tuple']['schedule(uint512,uint512,uint512,uint512)uint512']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(TimelockTestParamsFactory.schedule(params))
    },

    /**
     * Makes a call to the TimelockTest smart contract using the `execute(uint512,uint512)bool` ABI method.
     *
    *  @dev Execute a scheduled operation. Only admin.
    Requires the operation is not yet executed and the delay has passed.

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    execute: (params: CallParams<TimelockTestArgs['obj']['execute(uint512,uint512)bool'] | TimelockTestArgs['tuple']['execute(uint512,uint512)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(TimelockTestParamsFactory.execute(params))
    },

    /**
     * Makes a call to the TimelockTest smart contract using the `cancel(uint512)void` ABI method.
     *
    *  @dev Cancel a pending operation. Only admin.
    Sets the timestamp to 0, effectively cancelling it.

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    cancel: (params: CallParams<TimelockTestArgs['obj']['cancel(uint512)void'] | TimelockTestArgs['tuple']['cancel(uint512)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(TimelockTestParamsFactory.cancel(params))
    },

    /**
     * Makes a call to the TimelockTest smart contract using the `isOperationReady(uint512,uint512)bool` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     *  @dev Returns true if the operation is ready to execute.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    isOperationReady: (params: CallParams<TimelockTestArgs['obj']['isOperationReady(uint512,uint512)bool'] | TimelockTestArgs['tuple']['isOperationReady(uint512,uint512)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(TimelockTestParamsFactory.isOperationReady(params))
    },

    /**
     * Makes a call to the TimelockTest smart contract using the `isOperationDone(uint512)bool` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     *  @dev Returns true if the operation has been executed.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    isOperationDone: (params: CallParams<TimelockTestArgs['obj']['isOperationDone(uint512)bool'] | TimelockTestArgs['tuple']['isOperationDone(uint512)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(TimelockTestParamsFactory.isOperationDone(params))
    },

    /**
     * Makes a call to the TimelockTest smart contract using the `getTimestamp(uint512)uint512` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     *  @dev Returns the scheduled timestamp for an operation.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    getTimestamp: (params: CallParams<TimelockTestArgs['obj']['getTimestamp(uint512)uint512'] | TimelockTestArgs['tuple']['getTimestamp(uint512)uint512']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(TimelockTestParamsFactory.getTimestamp(params))
    },

    /**
     * Makes a call to the TimelockTest smart contract using the `getAdmin()address` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     *  @dev Returns the admin address.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    getAdmin: (params: CallParams<TimelockTestArgs['obj']['getAdmin()address'] | TimelockTestArgs['tuple']['getAdmin()address']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(TimelockTestParamsFactory.getAdmin(params))
    },

    /**
     * Makes a call to the TimelockTest smart contract using the `getMinDelay()uint512` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     *  @dev Returns the minimum delay.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    getMinDelay: (params: CallParams<TimelockTestArgs['obj']['getMinDelay()uint512'] | TimelockTestArgs['tuple']['getMinDelay()uint512']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(TimelockTestParamsFactory.getMinDelay(params))
    },

    /**
     * Makes a call to the TimelockTest smart contract using the `setMinDelay(uint512)void` ABI method.
     *
     *  @dev Updates the minimum delay. Only admin.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    setMinDelay: (params: CallParams<TimelockTestArgs['obj']['setMinDelay(uint512)void'] | TimelockTestArgs['tuple']['setMinDelay(uint512)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(TimelockTestParamsFactory.setMinDelay(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Makes a clear_state call to an existing instance of the TimelockTest smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the TimelockTest smart contract using the `initOperation(uint512)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    initOperation: async (params: CallParams<TimelockTestArgs['obj']['initOperation(uint512)void'] | TimelockTestArgs['tuple']['initOperation(uint512)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(TimelockTestParamsFactory.initOperation(params))
      return {...result, return: result.return as unknown as (undefined | TimelockTestReturns['initOperation(uint512)void'])}
    },

    /**
     * Makes a call to the TimelockTest smart contract using the `schedule(uint512,uint512,uint512,uint512)uint512` ABI method.
     *
    *  @dev Schedule an operation with a delay. Only admin.
    currentTime is passed as a parameter (AVM has no block.timestamp equivalent).
    Returns the assigned operationId.

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    schedule: async (params: CallParams<TimelockTestArgs['obj']['schedule(uint512,uint512,uint512,uint512)uint512'] | TimelockTestArgs['tuple']['schedule(uint512,uint512,uint512,uint512)uint512']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(TimelockTestParamsFactory.schedule(params))
      return {...result, return: result.return as unknown as (undefined | TimelockTestReturns['schedule(uint512,uint512,uint512,uint512)uint512'])}
    },

    /**
     * Makes a call to the TimelockTest smart contract using the `execute(uint512,uint512)bool` ABI method.
     *
    *  @dev Execute a scheduled operation. Only admin.
    Requires the operation is not yet executed and the delay has passed.

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    execute: async (params: CallParams<TimelockTestArgs['obj']['execute(uint512,uint512)bool'] | TimelockTestArgs['tuple']['execute(uint512,uint512)bool']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(TimelockTestParamsFactory.execute(params))
      return {...result, return: result.return as unknown as (undefined | TimelockTestReturns['execute(uint512,uint512)bool'])}
    },

    /**
     * Makes a call to the TimelockTest smart contract using the `cancel(uint512)void` ABI method.
     *
    *  @dev Cancel a pending operation. Only admin.
    Sets the timestamp to 0, effectively cancelling it.

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    cancel: async (params: CallParams<TimelockTestArgs['obj']['cancel(uint512)void'] | TimelockTestArgs['tuple']['cancel(uint512)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(TimelockTestParamsFactory.cancel(params))
      return {...result, return: result.return as unknown as (undefined | TimelockTestReturns['cancel(uint512)void'])}
    },

    /**
     * Makes a call to the TimelockTest smart contract using the `isOperationReady(uint512,uint512)bool` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     *  @dev Returns true if the operation is ready to execute.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    isOperationReady: async (params: CallParams<TimelockTestArgs['obj']['isOperationReady(uint512,uint512)bool'] | TimelockTestArgs['tuple']['isOperationReady(uint512,uint512)bool']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(TimelockTestParamsFactory.isOperationReady(params))
      return {...result, return: result.return as unknown as (undefined | TimelockTestReturns['isOperationReady(uint512,uint512)bool'])}
    },

    /**
     * Makes a call to the TimelockTest smart contract using the `isOperationDone(uint512)bool` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     *  @dev Returns true if the operation has been executed.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    isOperationDone: async (params: CallParams<TimelockTestArgs['obj']['isOperationDone(uint512)bool'] | TimelockTestArgs['tuple']['isOperationDone(uint512)bool']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(TimelockTestParamsFactory.isOperationDone(params))
      return {...result, return: result.return as unknown as (undefined | TimelockTestReturns['isOperationDone(uint512)bool'])}
    },

    /**
     * Makes a call to the TimelockTest smart contract using the `getTimestamp(uint512)uint512` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     *  @dev Returns the scheduled timestamp for an operation.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    getTimestamp: async (params: CallParams<TimelockTestArgs['obj']['getTimestamp(uint512)uint512'] | TimelockTestArgs['tuple']['getTimestamp(uint512)uint512']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(TimelockTestParamsFactory.getTimestamp(params))
      return {...result, return: result.return as unknown as (undefined | TimelockTestReturns['getTimestamp(uint512)uint512'])}
    },

    /**
     * Makes a call to the TimelockTest smart contract using the `getAdmin()address` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     *  @dev Returns the admin address.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    getAdmin: async (params: CallParams<TimelockTestArgs['obj']['getAdmin()address'] | TimelockTestArgs['tuple']['getAdmin()address']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(TimelockTestParamsFactory.getAdmin(params))
      return {...result, return: result.return as unknown as (undefined | TimelockTestReturns['getAdmin()address'])}
    },

    /**
     * Makes a call to the TimelockTest smart contract using the `getMinDelay()uint512` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     *  @dev Returns the minimum delay.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    getMinDelay: async (params: CallParams<TimelockTestArgs['obj']['getMinDelay()uint512'] | TimelockTestArgs['tuple']['getMinDelay()uint512']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(TimelockTestParamsFactory.getMinDelay(params))
      return {...result, return: result.return as unknown as (undefined | TimelockTestReturns['getMinDelay()uint512'])}
    },

    /**
     * Makes a call to the TimelockTest smart contract using the `setMinDelay(uint512)void` ABI method.
     *
     *  @dev Updates the minimum delay. Only admin.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    setMinDelay: async (params: CallParams<TimelockTestArgs['obj']['setMinDelay(uint512)void'] | TimelockTestArgs['tuple']['setMinDelay(uint512)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(TimelockTestParamsFactory.setMinDelay(params))
      return {...result, return: result.return as unknown as (undefined | TimelockTestReturns['setMinDelay(uint512)void'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new TimelockTestClient(this.appClient.clone(params))
  }

  /**
   * Makes a readonly (simulated) call to the TimelockTest smart contract using the `isOperationReady(uint512,uint512)bool` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   *  @dev Returns true if the operation is ready to execute.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async isOperationReady(params: CallParams<TimelockTestArgs['obj']['isOperationReady(uint512,uint512)bool'] | TimelockTestArgs['tuple']['isOperationReady(uint512,uint512)bool']>) {
    const result = await this.appClient.send.call(TimelockTestParamsFactory.isOperationReady(params))
    return result.return as unknown as TimelockTestReturns['isOperationReady(uint512,uint512)bool']
  }

  /**
   * Makes a readonly (simulated) call to the TimelockTest smart contract using the `isOperationDone(uint512)bool` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   *  @dev Returns true if the operation has been executed.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async isOperationDone(params: CallParams<TimelockTestArgs['obj']['isOperationDone(uint512)bool'] | TimelockTestArgs['tuple']['isOperationDone(uint512)bool']>) {
    const result = await this.appClient.send.call(TimelockTestParamsFactory.isOperationDone(params))
    return result.return as unknown as TimelockTestReturns['isOperationDone(uint512)bool']
  }

  /**
   * Makes a readonly (simulated) call to the TimelockTest smart contract using the `getTimestamp(uint512)uint512` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   *  @dev Returns the scheduled timestamp for an operation.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async getTimestamp(params: CallParams<TimelockTestArgs['obj']['getTimestamp(uint512)uint512'] | TimelockTestArgs['tuple']['getTimestamp(uint512)uint512']>) {
    const result = await this.appClient.send.call(TimelockTestParamsFactory.getTimestamp(params))
    return result.return as unknown as TimelockTestReturns['getTimestamp(uint512)uint512']
  }

  /**
   * Makes a readonly (simulated) call to the TimelockTest smart contract using the `getAdmin()address` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   *  @dev Returns the admin address.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async getAdmin(params: CallParams<TimelockTestArgs['obj']['getAdmin()address'] | TimelockTestArgs['tuple']['getAdmin()address']> = {args: []}) {
    const result = await this.appClient.send.call(TimelockTestParamsFactory.getAdmin(params))
    return result.return as unknown as TimelockTestReturns['getAdmin()address']
  }

  /**
   * Makes a readonly (simulated) call to the TimelockTest smart contract using the `getMinDelay()uint512` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   *  @dev Returns the minimum delay.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async getMinDelay(params: CallParams<TimelockTestArgs['obj']['getMinDelay()uint512'] | TimelockTestArgs['tuple']['getMinDelay()uint512']> = {args: []}) {
    const result = await this.appClient.send.call(TimelockTestParamsFactory.getMinDelay(params))
    return result.return as unknown as TimelockTestReturns['getMinDelay()uint512']
  }

  /**
   * Methods to access state for the current TimelockTest app
   */
  state = {
    /**
     * Methods to access global state for the current TimelockTest app
     */
    global: {
      /**
       * Get all current keyed values from global state
       */
      getAll: async (): Promise<Partial<Expand<GlobalKeysState>>> => {
        const result = await this.appClient.state.global.getAll()
        return {
          _admin: result._admin,
          _minDelay: new BinaryStateValue(result._minDelay),
          _operationCount: new BinaryStateValue(result._operationCount),
        }
      },
      /**
       * Get the current value of the _admin key in global state
       */
      _admin: async (): Promise<string | undefined> => { return (await this.appClient.state.global.getValue("_admin")) as string | undefined },
      /**
       * Get the current value of the _minDelay key in global state
       */
      _minDelay: async (): Promise<BinaryState> => { return new BinaryStateValue((await this.appClient.state.global.getValue("_minDelay")) as Uint8Array | undefined) },
      /**
       * Get the current value of the _operationCount key in global state
       */
      _operationCount: async (): Promise<BinaryState> => { return new BinaryStateValue((await this.appClient.state.global.getValue("_operationCount")) as Uint8Array | undefined) },
    },
    /**
     * Methods to access box state for the current TimelockTest app
     */
    box: {
      /**
       * Get all current keyed values from box state
       */
      getAll: async (): Promise<Partial<Expand<BoxKeysState>>> => {
        const result = await this.appClient.state.box.getAll()
        return {
        }
      },
      /**
       * Get values from the _opTimestamp map in box state
       */
      _opTimestamp: {
        /**
         * Get all current values of the _opTimestamp map in box state
         */
        getMap: async (): Promise<Map<Uint8Array, Uint8Array>> => { return (await this.appClient.state.box.getMap("_opTimestamp")) as Map<Uint8Array, Uint8Array> },
        /**
         * Get a current value of the _opTimestamp map by key from box state
         */
        value: async (key: Uint8Array | string): Promise<Uint8Array | undefined> => { return await this.appClient.state.box.getMapValue("_opTimestamp", key) as Uint8Array | undefined },
      },
      /**
       * Get values from the _opExecuted map in box state
       */
      _opExecuted: {
        /**
         * Get all current values of the _opExecuted map in box state
         */
        getMap: async (): Promise<Map<Uint8Array, bigint>> => { return (await this.appClient.state.box.getMap("_opExecuted")) as Map<Uint8Array, bigint> },
        /**
         * Get a current value of the _opExecuted map by key from box state
         */
        value: async (key: Uint8Array | string): Promise<bigint | undefined> => { return await this.appClient.state.box.getMapValue("_opExecuted", key) as bigint | undefined },
      },
      /**
       * Get values from the _opTarget map in box state
       */
      _opTarget: {
        /**
         * Get all current values of the _opTarget map in box state
         */
        getMap: async (): Promise<Map<Uint8Array, Uint8Array>> => { return (await this.appClient.state.box.getMap("_opTarget")) as Map<Uint8Array, Uint8Array> },
        /**
         * Get a current value of the _opTarget map by key from box state
         */
        value: async (key: Uint8Array | string): Promise<Uint8Array | undefined> => { return await this.appClient.state.box.getMapValue("_opTarget", key) as Uint8Array | undefined },
      },
      /**
       * Get values from the _opValue map in box state
       */
      _opValue: {
        /**
         * Get all current values of the _opValue map in box state
         */
        getMap: async (): Promise<Map<Uint8Array, Uint8Array>> => { return (await this.appClient.state.box.getMap("_opValue")) as Map<Uint8Array, Uint8Array> },
        /**
         * Get a current value of the _opValue map by key from box state
         */
        value: async (key: Uint8Array | string): Promise<Uint8Array | undefined> => { return await this.appClient.state.box.getMapValue("_opValue", key) as Uint8Array | undefined },
      },
    },
  }

  public newGroup(): TimelockTestComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a initOperation(uint512)void method call against the TimelockTest contract
       */
      initOperation(params: CallParams<TimelockTestArgs['obj']['initOperation(uint512)void'] | TimelockTestArgs['tuple']['initOperation(uint512)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.initOperation(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a schedule(uint512,uint512,uint512,uint512)uint512 method call against the TimelockTest contract
       */
      schedule(params: CallParams<TimelockTestArgs['obj']['schedule(uint512,uint512,uint512,uint512)uint512'] | TimelockTestArgs['tuple']['schedule(uint512,uint512,uint512,uint512)uint512']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.schedule(params)))
        resultMappers.push((v) => client.decodeReturnValue('schedule(uint512,uint512,uint512,uint512)uint512', v))
        return this
      },
      /**
       * Add a execute(uint512,uint512)bool method call against the TimelockTest contract
       */
      execute(params: CallParams<TimelockTestArgs['obj']['execute(uint512,uint512)bool'] | TimelockTestArgs['tuple']['execute(uint512,uint512)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.execute(params)))
        resultMappers.push((v) => client.decodeReturnValue('execute(uint512,uint512)bool', v))
        return this
      },
      /**
       * Add a cancel(uint512)void method call against the TimelockTest contract
       */
      cancel(params: CallParams<TimelockTestArgs['obj']['cancel(uint512)void'] | TimelockTestArgs['tuple']['cancel(uint512)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.cancel(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a isOperationReady(uint512,uint512)bool method call against the TimelockTest contract
       */
      isOperationReady(params: CallParams<TimelockTestArgs['obj']['isOperationReady(uint512,uint512)bool'] | TimelockTestArgs['tuple']['isOperationReady(uint512,uint512)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.isOperationReady(params)))
        resultMappers.push((v) => client.decodeReturnValue('isOperationReady(uint512,uint512)bool', v))
        return this
      },
      /**
       * Add a isOperationDone(uint512)bool method call against the TimelockTest contract
       */
      isOperationDone(params: CallParams<TimelockTestArgs['obj']['isOperationDone(uint512)bool'] | TimelockTestArgs['tuple']['isOperationDone(uint512)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.isOperationDone(params)))
        resultMappers.push((v) => client.decodeReturnValue('isOperationDone(uint512)bool', v))
        return this
      },
      /**
       * Add a getTimestamp(uint512)uint512 method call against the TimelockTest contract
       */
      getTimestamp(params: CallParams<TimelockTestArgs['obj']['getTimestamp(uint512)uint512'] | TimelockTestArgs['tuple']['getTimestamp(uint512)uint512']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.getTimestamp(params)))
        resultMappers.push((v) => client.decodeReturnValue('getTimestamp(uint512)uint512', v))
        return this
      },
      /**
       * Add a getAdmin()address method call against the TimelockTest contract
       */
      getAdmin(params: CallParams<TimelockTestArgs['obj']['getAdmin()address'] | TimelockTestArgs['tuple']['getAdmin()address']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.getAdmin(params)))
        resultMappers.push((v) => client.decodeReturnValue('getAdmin()address', v))
        return this
      },
      /**
       * Add a getMinDelay()uint512 method call against the TimelockTest contract
       */
      getMinDelay(params: CallParams<TimelockTestArgs['obj']['getMinDelay()uint512'] | TimelockTestArgs['tuple']['getMinDelay()uint512']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.getMinDelay(params)))
        resultMappers.push((v) => client.decodeReturnValue('getMinDelay()uint512', v))
        return this
      },
      /**
       * Add a setMinDelay(uint512)void method call against the TimelockTest contract
       */
      setMinDelay(params: CallParams<TimelockTestArgs['obj']['setMinDelay(uint512)void'] | TimelockTestArgs['tuple']['setMinDelay(uint512)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.setMinDelay(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a clear state call to the TimelockTest contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as TimelockTestComposer
  }
}
export type TimelockTestComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the initOperation(uint512)void ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  initOperation(params?: CallParams<TimelockTestArgs['obj']['initOperation(uint512)void'] | TimelockTestArgs['tuple']['initOperation(uint512)void']>): TimelockTestComposer<[...TReturns, TimelockTestReturns['initOperation(uint512)void'] | undefined]>

  /**
   * Calls the schedule(uint512,uint512,uint512,uint512)uint512 ABI method.
   *
  *  @dev Schedule an operation with a delay. Only admin.
  currentTime is passed as a parameter (AVM has no block.timestamp equivalent).
  Returns the assigned operationId.

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  schedule(params?: CallParams<TimelockTestArgs['obj']['schedule(uint512,uint512,uint512,uint512)uint512'] | TimelockTestArgs['tuple']['schedule(uint512,uint512,uint512,uint512)uint512']>): TimelockTestComposer<[...TReturns, TimelockTestReturns['schedule(uint512,uint512,uint512,uint512)uint512'] | undefined]>

  /**
   * Calls the execute(uint512,uint512)bool ABI method.
   *
  *  @dev Execute a scheduled operation. Only admin.
  Requires the operation is not yet executed and the delay has passed.

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  execute(params?: CallParams<TimelockTestArgs['obj']['execute(uint512,uint512)bool'] | TimelockTestArgs['tuple']['execute(uint512,uint512)bool']>): TimelockTestComposer<[...TReturns, TimelockTestReturns['execute(uint512,uint512)bool'] | undefined]>

  /**
   * Calls the cancel(uint512)void ABI method.
   *
  *  @dev Cancel a pending operation. Only admin.
  Sets the timestamp to 0, effectively cancelling it.

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  cancel(params?: CallParams<TimelockTestArgs['obj']['cancel(uint512)void'] | TimelockTestArgs['tuple']['cancel(uint512)void']>): TimelockTestComposer<[...TReturns, TimelockTestReturns['cancel(uint512)void'] | undefined]>

  /**
   * Calls the isOperationReady(uint512,uint512)bool ABI method.
   *
   *  @dev Returns true if the operation is ready to execute.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  isOperationReady(params?: CallParams<TimelockTestArgs['obj']['isOperationReady(uint512,uint512)bool'] | TimelockTestArgs['tuple']['isOperationReady(uint512,uint512)bool']>): TimelockTestComposer<[...TReturns, TimelockTestReturns['isOperationReady(uint512,uint512)bool'] | undefined]>

  /**
   * Calls the isOperationDone(uint512)bool ABI method.
   *
   *  @dev Returns true if the operation has been executed.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  isOperationDone(params?: CallParams<TimelockTestArgs['obj']['isOperationDone(uint512)bool'] | TimelockTestArgs['tuple']['isOperationDone(uint512)bool']>): TimelockTestComposer<[...TReturns, TimelockTestReturns['isOperationDone(uint512)bool'] | undefined]>

  /**
   * Calls the getTimestamp(uint512)uint512 ABI method.
   *
   *  @dev Returns the scheduled timestamp for an operation.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  getTimestamp(params?: CallParams<TimelockTestArgs['obj']['getTimestamp(uint512)uint512'] | TimelockTestArgs['tuple']['getTimestamp(uint512)uint512']>): TimelockTestComposer<[...TReturns, TimelockTestReturns['getTimestamp(uint512)uint512'] | undefined]>

  /**
   * Calls the getAdmin()address ABI method.
   *
   *  @dev Returns the admin address.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  getAdmin(params?: CallParams<TimelockTestArgs['obj']['getAdmin()address'] | TimelockTestArgs['tuple']['getAdmin()address']>): TimelockTestComposer<[...TReturns, TimelockTestReturns['getAdmin()address'] | undefined]>

  /**
   * Calls the getMinDelay()uint512 ABI method.
   *
   *  @dev Returns the minimum delay.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  getMinDelay(params?: CallParams<TimelockTestArgs['obj']['getMinDelay()uint512'] | TimelockTestArgs['tuple']['getMinDelay()uint512']>): TimelockTestComposer<[...TReturns, TimelockTestReturns['getMinDelay()uint512'] | undefined]>

  /**
   * Calls the setMinDelay(uint512)void ABI method.
   *
   *  @dev Updates the minimum delay. Only admin.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  setMinDelay(params?: CallParams<TimelockTestArgs['obj']['setMinDelay(uint512)void'] | TimelockTestArgs['tuple']['setMinDelay(uint512)void']>): TimelockTestComposer<[...TReturns, TimelockTestReturns['setMinDelay(uint512)void'] | undefined]>

  /**
   * Makes a clear_state call to an existing instance of the TimelockTest smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): TimelockTestComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): TimelockTestComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<TimelockTestComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<TimelockTestComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<TimelockTestComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<TimelockTestComposerResults<TReturns>>
}
export type TimelockTestComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

