/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"EIP712Wrapper","structs":{"eip712DomainReturn":[{"name":"fields","type":"byte[1]"},{"name":"name","type":"string"},{"name":"version","type":"string"},{"name":"chainId","type":"uint256"},{"name":"verifyingContract","type":"address"},{"name":"salt","type":"byte[32]"},{"name":"extensions","type":"uint256[]"}]},"methods":[{"name":"getDomainSeparator","args":[],"returns":{"type":"byte[32]"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"eip712Domain","args":[],"returns":{"type":"(byte[1],string,string,uint256,address,byte[32],uint256[])","struct":"eip712DomainReturn"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"desc":"@dev See: https://eips.ethereum.org/EIPS/eip-5267","events":[],"recommendations":{}}],"arcs":[22,28],"networks":{},"state":{"schema":{"global":{"ints":0,"bytes":5},"local":{"ints":0,"bytes":0}},"keys":{"global":{"_cachedThis":{"keyType":"AVMString","valueType":"AVMBytes","key":"X2NhY2hlZFRoaXM="},"_cachedChainId":{"keyType":"AVMString","valueType":"AVMBytes","key":"X2NhY2hlZENoYWluSWQ="},"_cachedNameHash":{"keyType":"AVMString","valueType":"AVMBytes","key":"X2NhY2hlZE5hbWVIYXNo"},"_cachedVersionHash":{"keyType":"AVMString","valueType":"AVMBytes","key":"X2NhY2hlZFZlcnNpb25IYXNo"},"_cachedDomainSeparator":{"keyType":"AVMString","valueType":"AVMBytes","key":"X2NhY2hlZERvbWFpblNlcGFyYXRvcg=="}},"local":{},"box":{}},"maps":{"global":{},"local":{},"box":{}}},"bareActions":{"create":[],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[403],"errorMessage":"check _cachedChainId exists"},{"pc":[399],"errorMessage":"check _cachedDomainSeparator exists"},{"pc":[612],"errorMessage":"check _cachedNameHash exists"},{"pc":[407],"errorMessage":"check _cachedThis exists"},{"pc":[617],"errorMessage":"check _cachedVersionHash exists"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"events":[]} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


// Type definitions for ARC-56 structs

export type Eip712DomainReturn = {
  fields: Uint8Array,
  name: string,
  version: string,
  chainId: bigint,
  verifyingContract: string,
  salt: Uint8Array,
  extensions: bigint[]
}


/**
 * Converts the ABI tuple representation of a eip712DomainReturn to the struct representation
 */
export function Eip712DomainReturnFromTuple(abiTuple: [Uint8Array, string, string, bigint, string, Uint8Array, bigint[]]) {
  return getABIStructFromABITuple(abiTuple, APP_SPEC.structs.eip712DomainReturn, APP_SPEC.structs) as Eip712DomainReturn
}

/**
 * The argument types for the Eip712Wrapper contract
 */
export type Eip712WrapperArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'getDomainSeparator()byte[32]': Record<string, never>
    'eip712Domain()(byte[1],string,string,uint256,address,byte[32],uint256[])': Record<string, never>
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'getDomainSeparator()byte[32]': []
    'eip712Domain()(byte[1],string,string,uint256,address,byte[32],uint256[])': []
  }
}

/**
 * The return type for each method
 */
export type Eip712WrapperReturns = {
  'getDomainSeparator()byte[32]': Uint8Array
  'eip712Domain()(byte[1],string,string,uint256,address,byte[32],uint256[])': Eip712DomainReturn
}

/**
 * Defines the types of available calls and state of the Eip712Wrapper smart contract.
 */
export type Eip712WrapperTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'getDomainSeparator()byte[32]' | 'getDomainSeparator', {
      argsObj: Eip712WrapperArgs['obj']['getDomainSeparator()byte[32]']
      argsTuple: Eip712WrapperArgs['tuple']['getDomainSeparator()byte[32]']
      returns: Eip712WrapperReturns['getDomainSeparator()byte[32]']
    }>
    & Record<'eip712Domain()(byte[1],string,string,uint256,address,byte[32],uint256[])' | 'eip712Domain', {
      argsObj: Eip712WrapperArgs['obj']['eip712Domain()(byte[1],string,string,uint256,address,byte[32],uint256[])']
      argsTuple: Eip712WrapperArgs['tuple']['eip712Domain()(byte[1],string,string,uint256,address,byte[32],uint256[])']
      returns: Eip712WrapperReturns['eip712Domain()(byte[1],string,string,uint256,address,byte[32],uint256[])']
    }>
  /**
   * Defines the shape of the state of the application.
   */
  state: {
    global: {
      keys: {
        _cachedThis: BinaryState
        _cachedChainId: BinaryState
        _cachedNameHash: BinaryState
        _cachedVersionHash: BinaryState
        _cachedDomainSeparator: BinaryState
      }
      maps: {}
    }
  }
}

/**
 * Defines the possible abi call signatures.
 */
export type Eip712WrapperSignatures = keyof Eip712WrapperTypes['methods']
/**
 * Defines the possible abi call signatures for methods that return a non-void value.
 */
export type Eip712WrapperNonVoidMethodSignatures = keyof Eip712WrapperTypes['methods'] extends infer T ? T extends keyof Eip712WrapperTypes['methods'] ? MethodReturn<T> extends void ? never : T  : never : never
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the Eip712Wrapper smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends Eip712WrapperSignatures> = Eip712WrapperTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the Eip712Wrapper smart contract to the method's return type
 */
export type MethodReturn<TSignature extends Eip712WrapperSignatures> = Eip712WrapperTypes['methods'][TSignature]['returns']

/**
 * Defines the shape of the keyed global state of the application.
 */
export type GlobalKeysState = Eip712WrapperTypes['state']['global']['keys']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the Eip712Wrapper smart contract
 */
export abstract class Eip712WrapperParamsFactory {
  /**
   * Constructs a no op call for the getDomainSeparator()byte[32] ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static getDomainSeparator(params: CallParams<Eip712WrapperArgs['obj']['getDomainSeparator()byte[32]'] | Eip712WrapperArgs['tuple']['getDomainSeparator()byte[32]']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'getDomainSeparator()byte[32]' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the eip712Domain()(byte[1],string,string,uint256,address,byte[32],uint256[]) ABI method
   *
   * @dev See: https://eips.ethereum.org/EIPS/eip-5267
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static eip712Domain(params: CallParams<Eip712WrapperArgs['obj']['eip712Domain()(byte[1],string,string,uint256,address,byte[32],uint256[])'] | Eip712WrapperArgs['tuple']['eip712Domain()(byte[1],string,string,uint256,address,byte[32],uint256[])']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'eip712Domain()(byte[1],string,string,uint256,address,byte[32],uint256[])' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
}

/**
 * A client to make calls to the EIP712Wrapper smart contract
 */
export class Eip712WrapperClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `Eip712WrapperClient`
   *
   * @param appClient An `AppClient` instance which has been created with the Eip712Wrapper app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `Eip712WrapperClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Checks for decode errors on the given return value and maps the return value to the return type for the given method
   * @returns The typed return value or undefined if there was no value
   */
  decodeReturnValue<TSignature extends Eip712WrapperNonVoidMethodSignatures>(method: TSignature, returnValue: ABIReturn | undefined) {
    return returnValue !== undefined ? getArc56ReturnValue<MethodReturn<TSignature>>(returnValue, this.appClient.getABIMethod(method), APP_SPEC.structs) : undefined
  }

  /**
   * Returns a new `Eip712WrapperClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<Eip712WrapperClient> {
    return new Eip712WrapperClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `Eip712WrapperClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<Eip712WrapperClient> {
    return new Eip712WrapperClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Makes a clear_state call to an existing instance of the EIP712Wrapper smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the EIP712Wrapper smart contract using the `getDomainSeparator()byte[32]` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    getDomainSeparator: (params: CallParams<Eip712WrapperArgs['obj']['getDomainSeparator()byte[32]'] | Eip712WrapperArgs['tuple']['getDomainSeparator()byte[32]']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(Eip712WrapperParamsFactory.getDomainSeparator(params))
    },

    /**
     * Makes a call to the EIP712Wrapper smart contract using the `eip712Domain()(byte[1],string,string,uint256,address,byte[32],uint256[])` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @dev See: https://eips.ethereum.org/EIPS/eip-5267
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    eip712Domain: (params: CallParams<Eip712WrapperArgs['obj']['eip712Domain()(byte[1],string,string,uint256,address,byte[32],uint256[])'] | Eip712WrapperArgs['tuple']['eip712Domain()(byte[1],string,string,uint256,address,byte[32],uint256[])']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(Eip712WrapperParamsFactory.eip712Domain(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Makes a clear_state call to an existing instance of the EIP712Wrapper smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the EIP712Wrapper smart contract using the `getDomainSeparator()byte[32]` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    getDomainSeparator: (params: CallParams<Eip712WrapperArgs['obj']['getDomainSeparator()byte[32]'] | Eip712WrapperArgs['tuple']['getDomainSeparator()byte[32]']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(Eip712WrapperParamsFactory.getDomainSeparator(params))
    },

    /**
     * Makes a call to the EIP712Wrapper smart contract using the `eip712Domain()(byte[1],string,string,uint256,address,byte[32],uint256[])` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @dev See: https://eips.ethereum.org/EIPS/eip-5267
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    eip712Domain: (params: CallParams<Eip712WrapperArgs['obj']['eip712Domain()(byte[1],string,string,uint256,address,byte[32],uint256[])'] | Eip712WrapperArgs['tuple']['eip712Domain()(byte[1],string,string,uint256,address,byte[32],uint256[])']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(Eip712WrapperParamsFactory.eip712Domain(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Makes a clear_state call to an existing instance of the EIP712Wrapper smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the EIP712Wrapper smart contract using the `getDomainSeparator()byte[32]` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    getDomainSeparator: async (params: CallParams<Eip712WrapperArgs['obj']['getDomainSeparator()byte[32]'] | Eip712WrapperArgs['tuple']['getDomainSeparator()byte[32]']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(Eip712WrapperParamsFactory.getDomainSeparator(params))
      return {...result, return: result.return as unknown as (undefined | Eip712WrapperReturns['getDomainSeparator()byte[32]'])}
    },

    /**
     * Makes a call to the EIP712Wrapper smart contract using the `eip712Domain()(byte[1],string,string,uint256,address,byte[32],uint256[])` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @dev See: https://eips.ethereum.org/EIPS/eip-5267
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    eip712Domain: async (params: CallParams<Eip712WrapperArgs['obj']['eip712Domain()(byte[1],string,string,uint256,address,byte[32],uint256[])'] | Eip712WrapperArgs['tuple']['eip712Domain()(byte[1],string,string,uint256,address,byte[32],uint256[])']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(Eip712WrapperParamsFactory.eip712Domain(params))
      return {...result, return: result.return as unknown as (undefined | Eip712WrapperReturns['eip712Domain()(byte[1],string,string,uint256,address,byte[32],uint256[])'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new Eip712WrapperClient(this.appClient.clone(params))
  }

  /**
   * Makes a readonly (simulated) call to the EIP712Wrapper smart contract using the `getDomainSeparator()byte[32]` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async getDomainSeparator(params: CallParams<Eip712WrapperArgs['obj']['getDomainSeparator()byte[32]'] | Eip712WrapperArgs['tuple']['getDomainSeparator()byte[32]']> = {args: []}) {
    const result = await this.appClient.send.call(Eip712WrapperParamsFactory.getDomainSeparator(params))
    return result.return as unknown as Eip712WrapperReturns['getDomainSeparator()byte[32]']
  }

  /**
   * Makes a readonly (simulated) call to the EIP712Wrapper smart contract using the `eip712Domain()(byte[1],string,string,uint256,address,byte[32],uint256[])` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @dev See: https://eips.ethereum.org/EIPS/eip-5267
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async eip712Domain(params: CallParams<Eip712WrapperArgs['obj']['eip712Domain()(byte[1],string,string,uint256,address,byte[32],uint256[])'] | Eip712WrapperArgs['tuple']['eip712Domain()(byte[1],string,string,uint256,address,byte[32],uint256[])']> = {args: []}) {
    const result = await this.appClient.send.call(Eip712WrapperParamsFactory.eip712Domain(params))
    return result.return as unknown as Eip712WrapperReturns['eip712Domain()(byte[1],string,string,uint256,address,byte[32],uint256[])']
  }

  /**
   * Methods to access state for the current EIP712Wrapper app
   */
  state = {
    /**
     * Methods to access global state for the current EIP712Wrapper app
     */
    global: {
      /**
       * Get all current keyed values from global state
       */
      getAll: async (): Promise<Partial<Expand<GlobalKeysState>>> => {
        const result = await this.appClient.state.global.getAll()
        return {
          _cachedThis: new BinaryStateValue(result._cachedThis),
          _cachedChainId: new BinaryStateValue(result._cachedChainId),
          _cachedNameHash: new BinaryStateValue(result._cachedNameHash),
          _cachedVersionHash: new BinaryStateValue(result._cachedVersionHash),
          _cachedDomainSeparator: new BinaryStateValue(result._cachedDomainSeparator),
        }
      },
      /**
       * Get the current value of the _cachedThis key in global state
       */
      _cachedThis: async (): Promise<BinaryState> => { return new BinaryStateValue((await this.appClient.state.global.getValue("_cachedThis")) as Uint8Array | undefined) },
      /**
       * Get the current value of the _cachedChainId key in global state
       */
      _cachedChainId: async (): Promise<BinaryState> => { return new BinaryStateValue((await this.appClient.state.global.getValue("_cachedChainId")) as Uint8Array | undefined) },
      /**
       * Get the current value of the _cachedNameHash key in global state
       */
      _cachedNameHash: async (): Promise<BinaryState> => { return new BinaryStateValue((await this.appClient.state.global.getValue("_cachedNameHash")) as Uint8Array | undefined) },
      /**
       * Get the current value of the _cachedVersionHash key in global state
       */
      _cachedVersionHash: async (): Promise<BinaryState> => { return new BinaryStateValue((await this.appClient.state.global.getValue("_cachedVersionHash")) as Uint8Array | undefined) },
      /**
       * Get the current value of the _cachedDomainSeparator key in global state
       */
      _cachedDomainSeparator: async (): Promise<BinaryState> => { return new BinaryStateValue((await this.appClient.state.global.getValue("_cachedDomainSeparator")) as Uint8Array | undefined) },
    },
  }

  public newGroup(): Eip712WrapperComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a getDomainSeparator()byte[32] method call against the EIP712Wrapper contract
       */
      getDomainSeparator(params: CallParams<Eip712WrapperArgs['obj']['getDomainSeparator()byte[32]'] | Eip712WrapperArgs['tuple']['getDomainSeparator()byte[32]']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.getDomainSeparator(params)))
        resultMappers.push((v) => client.decodeReturnValue('getDomainSeparator()byte[32]', v))
        return this
      },
      /**
       * Add a eip712Domain()(byte[1],string,string,uint256,address,byte[32],uint256[]) method call against the EIP712Wrapper contract
       */
      eip712Domain(params: CallParams<Eip712WrapperArgs['obj']['eip712Domain()(byte[1],string,string,uint256,address,byte[32],uint256[])'] | Eip712WrapperArgs['tuple']['eip712Domain()(byte[1],string,string,uint256,address,byte[32],uint256[])']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.eip712Domain(params)))
        resultMappers.push((v) => client.decodeReturnValue('eip712Domain()(byte[1],string,string,uint256,address,byte[32],uint256[])', v))
        return this
      },
      /**
       * Add a clear state call to the EIP712Wrapper contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as Eip712WrapperComposer
  }
}
export type Eip712WrapperComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the getDomainSeparator()byte[32] ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  getDomainSeparator(params?: CallParams<Eip712WrapperArgs['obj']['getDomainSeparator()byte[32]'] | Eip712WrapperArgs['tuple']['getDomainSeparator()byte[32]']>): Eip712WrapperComposer<[...TReturns, Eip712WrapperReturns['getDomainSeparator()byte[32]'] | undefined]>

  /**
   * Calls the eip712Domain()(byte[1],string,string,uint256,address,byte[32],uint256[]) ABI method.
   *
   * @dev See: https://eips.ethereum.org/EIPS/eip-5267
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  eip712Domain(params?: CallParams<Eip712WrapperArgs['obj']['eip712Domain()(byte[1],string,string,uint256,address,byte[32],uint256[])'] | Eip712WrapperArgs['tuple']['eip712Domain()(byte[1],string,string,uint256,address,byte[32],uint256[])']>): Eip712WrapperComposer<[...TReturns, Eip712WrapperReturns['eip712Domain()(byte[1],string,string,uint256,address,byte[32],uint256[])'] | undefined]>

  /**
   * Makes a clear_state call to an existing instance of the EIP712Wrapper smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): Eip712WrapperComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): Eip712WrapperComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<Eip712WrapperComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<Eip712WrapperComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<Eip712WrapperComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<Eip712WrapperComposerResults<TReturns>>
}
export type Eip712WrapperComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

