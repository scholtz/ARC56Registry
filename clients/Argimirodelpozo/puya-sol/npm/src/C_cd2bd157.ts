/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"C","structs":{"wSlotOffsetReturn":[{"name":"s","type":"uint512"},{"name":"o","type":"uint512"}],"xSlotOffsetReturn":[{"name":"s","type":"uint512"},{"name":"o","type":"uint512"}],"ySlotOffsetReturn":[{"name":"s","type":"uint512"},{"name":"o","type":"uint512"}],"zSlotOffsetReturn":[{"name":"s","type":"uint512"},{"name":"o","type":"uint512"}]},"methods":[{"name":"xSlotOffset","args":[],"returns":{"type":"(uint512,uint512)","struct":"xSlotOffsetReturn"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}},{"name":"ySlotOffset","args":[],"returns":{"type":"(uint512,uint512)","struct":"ySlotOffsetReturn"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}},{"name":"wSlotOffset","args":[],"returns":{"type":"(uint512,uint512)","struct":"wSlotOffsetReturn"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}},{"name":"zSlotOffset","args":[],"returns":{"type":"(uint512,uint512)","struct":"zSlotOffsetReturn"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}}],"arcs":[22,28],"networks":{},"state":{"schema":{"global":{"ints":1,"bytes":3},"local":{"ints":0,"bytes":0}},"keys":{"global":{"w":{"keyType":"AVMString","valueType":"AVMUint64","key":"dw=="},"z":{"keyType":"AVMString","valueType":"AVMBytes","key":"eg=="},"x":{"keyType":"AVMString","valueType":"AVMBytes","key":"eA=="},"y":{"keyType":"AVMString","valueType":"AVMBytes","key":"eQ=="}},"local":{},"box":{}},"maps":{"global":{},"local":{},"box":{}}},"bareActions":{"create":[],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[139,298,457,616],"errorMessage":"not payable"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"events":[]} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


// Type definitions for ARC-56 structs

export type WSlotOffsetReturn = {
  s: bigint,
  o: bigint
}


/**
 * Converts the ABI tuple representation of a wSlotOffsetReturn to the struct representation
 */
export function WSlotOffsetReturnFromTuple(abiTuple: [bigint, bigint]) {
  return getABIStructFromABITuple(abiTuple, APP_SPEC.structs.wSlotOffsetReturn, APP_SPEC.structs) as WSlotOffsetReturn
}

export type XSlotOffsetReturn = {
  s: bigint,
  o: bigint
}


/**
 * Converts the ABI tuple representation of a xSlotOffsetReturn to the struct representation
 */
export function XSlotOffsetReturnFromTuple(abiTuple: [bigint, bigint]) {
  return getABIStructFromABITuple(abiTuple, APP_SPEC.structs.xSlotOffsetReturn, APP_SPEC.structs) as XSlotOffsetReturn
}

export type YSlotOffsetReturn = {
  s: bigint,
  o: bigint
}


/**
 * Converts the ABI tuple representation of a ySlotOffsetReturn to the struct representation
 */
export function YSlotOffsetReturnFromTuple(abiTuple: [bigint, bigint]) {
  return getABIStructFromABITuple(abiTuple, APP_SPEC.structs.ySlotOffsetReturn, APP_SPEC.structs) as YSlotOffsetReturn
}

export type ZSlotOffsetReturn = {
  s: bigint,
  o: bigint
}


/**
 * Converts the ABI tuple representation of a zSlotOffsetReturn to the struct representation
 */
export function ZSlotOffsetReturnFromTuple(abiTuple: [bigint, bigint]) {
  return getABIStructFromABITuple(abiTuple, APP_SPEC.structs.zSlotOffsetReturn, APP_SPEC.structs) as ZSlotOffsetReturn
}

/**
 * The argument types for the C contract
 */
export type CArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'xSlotOffset()(uint512,uint512)': Record<string, never>
    'ySlotOffset()(uint512,uint512)': Record<string, never>
    'wSlotOffset()(uint512,uint512)': Record<string, never>
    'zSlotOffset()(uint512,uint512)': Record<string, never>
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'xSlotOffset()(uint512,uint512)': []
    'ySlotOffset()(uint512,uint512)': []
    'wSlotOffset()(uint512,uint512)': []
    'zSlotOffset()(uint512,uint512)': []
  }
}

/**
 * The return type for each method
 */
export type CReturns = {
  'xSlotOffset()(uint512,uint512)': XSlotOffsetReturn
  'ySlotOffset()(uint512,uint512)': YSlotOffsetReturn
  'wSlotOffset()(uint512,uint512)': WSlotOffsetReturn
  'zSlotOffset()(uint512,uint512)': ZSlotOffsetReturn
}

/**
 * Defines the types of available calls and state of the C smart contract.
 */
export type CTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'xSlotOffset()(uint512,uint512)' | 'xSlotOffset', {
      argsObj: CArgs['obj']['xSlotOffset()(uint512,uint512)']
      argsTuple: CArgs['tuple']['xSlotOffset()(uint512,uint512)']
      returns: CReturns['xSlotOffset()(uint512,uint512)']
    }>
    & Record<'ySlotOffset()(uint512,uint512)' | 'ySlotOffset', {
      argsObj: CArgs['obj']['ySlotOffset()(uint512,uint512)']
      argsTuple: CArgs['tuple']['ySlotOffset()(uint512,uint512)']
      returns: CReturns['ySlotOffset()(uint512,uint512)']
    }>
    & Record<'wSlotOffset()(uint512,uint512)' | 'wSlotOffset', {
      argsObj: CArgs['obj']['wSlotOffset()(uint512,uint512)']
      argsTuple: CArgs['tuple']['wSlotOffset()(uint512,uint512)']
      returns: CReturns['wSlotOffset()(uint512,uint512)']
    }>
    & Record<'zSlotOffset()(uint512,uint512)' | 'zSlotOffset', {
      argsObj: CArgs['obj']['zSlotOffset()(uint512,uint512)']
      argsTuple: CArgs['tuple']['zSlotOffset()(uint512,uint512)']
      returns: CReturns['zSlotOffset()(uint512,uint512)']
    }>
  /**
   * Defines the shape of the state of the application.
   */
  state: {
    global: {
      keys: {
        w: bigint
        z: BinaryState
        x: BinaryState
        y: BinaryState
      }
      maps: {}
    }
  }
}

/**
 * Defines the possible abi call signatures.
 */
export type CSignatures = keyof CTypes['methods']
/**
 * Defines the possible abi call signatures for methods that return a non-void value.
 */
export type CNonVoidMethodSignatures = keyof CTypes['methods'] extends infer T ? T extends keyof CTypes['methods'] ? MethodReturn<T> extends void ? never : T  : never : never
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the C smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends CSignatures> = CTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the C smart contract to the method's return type
 */
export type MethodReturn<TSignature extends CSignatures> = CTypes['methods'][TSignature]['returns']

/**
 * Defines the shape of the keyed global state of the application.
 */
export type GlobalKeysState = CTypes['state']['global']['keys']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the C smart contract
 */
export abstract class CParamsFactory {
  /**
   * Constructs a no op call for the xSlotOffset()(uint512,uint512) ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static xSlotOffset(params: CallParams<CArgs['obj']['xSlotOffset()(uint512,uint512)'] | CArgs['tuple']['xSlotOffset()(uint512,uint512)']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'xSlotOffset()(uint512,uint512)' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the ySlotOffset()(uint512,uint512) ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static ySlotOffset(params: CallParams<CArgs['obj']['ySlotOffset()(uint512,uint512)'] | CArgs['tuple']['ySlotOffset()(uint512,uint512)']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'ySlotOffset()(uint512,uint512)' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the wSlotOffset()(uint512,uint512) ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static wSlotOffset(params: CallParams<CArgs['obj']['wSlotOffset()(uint512,uint512)'] | CArgs['tuple']['wSlotOffset()(uint512,uint512)']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'wSlotOffset()(uint512,uint512)' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the zSlotOffset()(uint512,uint512) ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static zSlotOffset(params: CallParams<CArgs['obj']['zSlotOffset()(uint512,uint512)'] | CArgs['tuple']['zSlotOffset()(uint512,uint512)']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'zSlotOffset()(uint512,uint512)' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
}

/**
 * A client to make calls to the C smart contract
 */
export class CClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `CClient`
   *
   * @param appClient An `AppClient` instance which has been created with the C app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `CClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Checks for decode errors on the given return value and maps the return value to the return type for the given method
   * @returns The typed return value or undefined if there was no value
   */
  decodeReturnValue<TSignature extends CNonVoidMethodSignatures>(method: TSignature, returnValue: ABIReturn | undefined) {
    return returnValue !== undefined ? getArc56ReturnValue<MethodReturn<TSignature>>(returnValue, this.appClient.getABIMethod(method), APP_SPEC.structs) : undefined
  }

  /**
   * Returns a new `CClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<CClient> {
    return new CClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `CClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<CClient> {
    return new CClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Makes a clear_state call to an existing instance of the C smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the C smart contract using the `xSlotOffset()(uint512,uint512)` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    xSlotOffset: (params: CallParams<CArgs['obj']['xSlotOffset()(uint512,uint512)'] | CArgs['tuple']['xSlotOffset()(uint512,uint512)']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(CParamsFactory.xSlotOffset(params))
    },

    /**
     * Makes a call to the C smart contract using the `ySlotOffset()(uint512,uint512)` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    ySlotOffset: (params: CallParams<CArgs['obj']['ySlotOffset()(uint512,uint512)'] | CArgs['tuple']['ySlotOffset()(uint512,uint512)']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(CParamsFactory.ySlotOffset(params))
    },

    /**
     * Makes a call to the C smart contract using the `wSlotOffset()(uint512,uint512)` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    wSlotOffset: (params: CallParams<CArgs['obj']['wSlotOffset()(uint512,uint512)'] | CArgs['tuple']['wSlotOffset()(uint512,uint512)']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(CParamsFactory.wSlotOffset(params))
    },

    /**
     * Makes a call to the C smart contract using the `zSlotOffset()(uint512,uint512)` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    zSlotOffset: (params: CallParams<CArgs['obj']['zSlotOffset()(uint512,uint512)'] | CArgs['tuple']['zSlotOffset()(uint512,uint512)']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(CParamsFactory.zSlotOffset(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Makes a clear_state call to an existing instance of the C smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the C smart contract using the `xSlotOffset()(uint512,uint512)` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    xSlotOffset: (params: CallParams<CArgs['obj']['xSlotOffset()(uint512,uint512)'] | CArgs['tuple']['xSlotOffset()(uint512,uint512)']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(CParamsFactory.xSlotOffset(params))
    },

    /**
     * Makes a call to the C smart contract using the `ySlotOffset()(uint512,uint512)` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    ySlotOffset: (params: CallParams<CArgs['obj']['ySlotOffset()(uint512,uint512)'] | CArgs['tuple']['ySlotOffset()(uint512,uint512)']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(CParamsFactory.ySlotOffset(params))
    },

    /**
     * Makes a call to the C smart contract using the `wSlotOffset()(uint512,uint512)` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    wSlotOffset: (params: CallParams<CArgs['obj']['wSlotOffset()(uint512,uint512)'] | CArgs['tuple']['wSlotOffset()(uint512,uint512)']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(CParamsFactory.wSlotOffset(params))
    },

    /**
     * Makes a call to the C smart contract using the `zSlotOffset()(uint512,uint512)` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    zSlotOffset: (params: CallParams<CArgs['obj']['zSlotOffset()(uint512,uint512)'] | CArgs['tuple']['zSlotOffset()(uint512,uint512)']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(CParamsFactory.zSlotOffset(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Makes a clear_state call to an existing instance of the C smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the C smart contract using the `xSlotOffset()(uint512,uint512)` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    xSlotOffset: async (params: CallParams<CArgs['obj']['xSlotOffset()(uint512,uint512)'] | CArgs['tuple']['xSlotOffset()(uint512,uint512)']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(CParamsFactory.xSlotOffset(params))
      return {...result, return: result.return as unknown as (undefined | CReturns['xSlotOffset()(uint512,uint512)'])}
    },

    /**
     * Makes a call to the C smart contract using the `ySlotOffset()(uint512,uint512)` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    ySlotOffset: async (params: CallParams<CArgs['obj']['ySlotOffset()(uint512,uint512)'] | CArgs['tuple']['ySlotOffset()(uint512,uint512)']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(CParamsFactory.ySlotOffset(params))
      return {...result, return: result.return as unknown as (undefined | CReturns['ySlotOffset()(uint512,uint512)'])}
    },

    /**
     * Makes a call to the C smart contract using the `wSlotOffset()(uint512,uint512)` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    wSlotOffset: async (params: CallParams<CArgs['obj']['wSlotOffset()(uint512,uint512)'] | CArgs['tuple']['wSlotOffset()(uint512,uint512)']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(CParamsFactory.wSlotOffset(params))
      return {...result, return: result.return as unknown as (undefined | CReturns['wSlotOffset()(uint512,uint512)'])}
    },

    /**
     * Makes a call to the C smart contract using the `zSlotOffset()(uint512,uint512)` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    zSlotOffset: async (params: CallParams<CArgs['obj']['zSlotOffset()(uint512,uint512)'] | CArgs['tuple']['zSlotOffset()(uint512,uint512)']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(CParamsFactory.zSlotOffset(params))
      return {...result, return: result.return as unknown as (undefined | CReturns['zSlotOffset()(uint512,uint512)'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new CClient(this.appClient.clone(params))
  }

  /**
   * Methods to access state for the current C app
   */
  state = {
    /**
     * Methods to access global state for the current C app
     */
    global: {
      /**
       * Get all current keyed values from global state
       */
      getAll: async (): Promise<Partial<Expand<GlobalKeysState>>> => {
        const result = await this.appClient.state.global.getAll()
        return {
          w: result.w,
          z: new BinaryStateValue(result.z),
          x: new BinaryStateValue(result.x),
          y: new BinaryStateValue(result.y),
        }
      },
      /**
       * Get the current value of the w key in global state
       */
      w: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("w")) as bigint | undefined },
      /**
       * Get the current value of the z key in global state
       */
      z: async (): Promise<BinaryState> => { return new BinaryStateValue((await this.appClient.state.global.getValue("z")) as Uint8Array | undefined) },
      /**
       * Get the current value of the x key in global state
       */
      x: async (): Promise<BinaryState> => { return new BinaryStateValue((await this.appClient.state.global.getValue("x")) as Uint8Array | undefined) },
      /**
       * Get the current value of the y key in global state
       */
      y: async (): Promise<BinaryState> => { return new BinaryStateValue((await this.appClient.state.global.getValue("y")) as Uint8Array | undefined) },
    },
  }

  public newGroup(): CComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a xSlotOffset()(uint512,uint512) method call against the C contract
       */
      xSlotOffset(params: CallParams<CArgs['obj']['xSlotOffset()(uint512,uint512)'] | CArgs['tuple']['xSlotOffset()(uint512,uint512)']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.xSlotOffset(params)))
        resultMappers.push((v) => client.decodeReturnValue('xSlotOffset()(uint512,uint512)', v))
        return this
      },
      /**
       * Add a ySlotOffset()(uint512,uint512) method call against the C contract
       */
      ySlotOffset(params: CallParams<CArgs['obj']['ySlotOffset()(uint512,uint512)'] | CArgs['tuple']['ySlotOffset()(uint512,uint512)']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.ySlotOffset(params)))
        resultMappers.push((v) => client.decodeReturnValue('ySlotOffset()(uint512,uint512)', v))
        return this
      },
      /**
       * Add a wSlotOffset()(uint512,uint512) method call against the C contract
       */
      wSlotOffset(params: CallParams<CArgs['obj']['wSlotOffset()(uint512,uint512)'] | CArgs['tuple']['wSlotOffset()(uint512,uint512)']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.wSlotOffset(params)))
        resultMappers.push((v) => client.decodeReturnValue('wSlotOffset()(uint512,uint512)', v))
        return this
      },
      /**
       * Add a zSlotOffset()(uint512,uint512) method call against the C contract
       */
      zSlotOffset(params: CallParams<CArgs['obj']['zSlotOffset()(uint512,uint512)'] | CArgs['tuple']['zSlotOffset()(uint512,uint512)']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.zSlotOffset(params)))
        resultMappers.push((v) => client.decodeReturnValue('zSlotOffset()(uint512,uint512)', v))
        return this
      },
      /**
       * Add a clear state call to the C contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as CComposer
  }
}
export type CComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the xSlotOffset()(uint512,uint512) ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  xSlotOffset(params?: CallParams<CArgs['obj']['xSlotOffset()(uint512,uint512)'] | CArgs['tuple']['xSlotOffset()(uint512,uint512)']>): CComposer<[...TReturns, CReturns['xSlotOffset()(uint512,uint512)'] | undefined]>

  /**
   * Calls the ySlotOffset()(uint512,uint512) ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  ySlotOffset(params?: CallParams<CArgs['obj']['ySlotOffset()(uint512,uint512)'] | CArgs['tuple']['ySlotOffset()(uint512,uint512)']>): CComposer<[...TReturns, CReturns['ySlotOffset()(uint512,uint512)'] | undefined]>

  /**
   * Calls the wSlotOffset()(uint512,uint512) ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  wSlotOffset(params?: CallParams<CArgs['obj']['wSlotOffset()(uint512,uint512)'] | CArgs['tuple']['wSlotOffset()(uint512,uint512)']>): CComposer<[...TReturns, CReturns['wSlotOffset()(uint512,uint512)'] | undefined]>

  /**
   * Calls the zSlotOffset()(uint512,uint512) ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  zSlotOffset(params?: CallParams<CArgs['obj']['zSlotOffset()(uint512,uint512)'] | CArgs['tuple']['zSlotOffset()(uint512,uint512)']>): CComposer<[...TReturns, CReturns['zSlotOffset()(uint512,uint512)'] | undefined]>

  /**
   * Makes a clear_state call to an existing instance of the C smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): CComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): CComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<CComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<CComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<CComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<CComposerResults<TReturns>>
}
export type CComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

