/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"TokenB","structs":{},"methods":[{"name":"__postInit","args":[],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}},{"name":"mint","args":[{"type":"address","name":"to"},{"type":"uint256","name":"__arc4_amount"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}},{"name":"balanceOf","args":[{"type":"address","name":"who"}],"returns":{"type":"uint256"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"balances","args":[{"type":"address","name":"key0"}],"returns":{"type":"uint256"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"totalMinted","args":[],"returns":{"type":"uint256"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}}],"arcs":[22,28],"networks":{},"state":{"schema":{"global":{"ints":1,"bytes":1},"local":{"ints":0,"bytes":0}},"keys":{"global":{"totalMinted":{"keyType":"AVMString","valueType":"AVMBytes","key":"dG90YWxNaW50ZWQ="},"__ctor_pending":{"keyType":"AVMString","valueType":"AVMUint64","key":"X19jdG9yX3BlbmRpbmc="}},"local":{},"box":{}},"maps":{"global":{},"local":{},"box":{"balances":{"keyType":"AVMBytes","valueType":"AVMBytes","prefix":"YmFsYW5jZXM="}}}},"bareActions":{"create":[],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[161],"errorMessage":"__postInit already called"},{"pc":[230,357],"errorMessage":"check totalMinted exists"},{"pc":[182,248,298],"errorMessage":"invalid number of bytes for arc4.static_array<arc4.uint8, 32>"},{"pc":[190],"errorMessage":"invalid number of bytes for arc4.uint256"},{"pc":[203,261,311,353],"errorMessage":"not payable"},{"pc":[276,326,362],"errorMessage":"overflow"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"events":[]} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


/**
 * The argument types for the TokenB contract
 */
export type TokenBArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    '__postInit()void': Record<string, never>
    'mint(address,uint256)void': {
      to: string
      __arc4Amount: bigint | number
    }
    'balanceOf(address)uint256': {
      who: string
    }
    'balances(address)uint256': {
      key0: string
    }
    'totalMinted()uint256': Record<string, never>
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    '__postInit()void': []
    'mint(address,uint256)void': [to: string, __arc4Amount: bigint | number]
    'balanceOf(address)uint256': [who: string]
    'balances(address)uint256': [key0: string]
    'totalMinted()uint256': []
  }
}

/**
 * The return type for each method
 */
export type TokenBReturns = {
  '__postInit()void': void
  'mint(address,uint256)void': void
  'balanceOf(address)uint256': bigint
  'balances(address)uint256': bigint
  'totalMinted()uint256': bigint
}

/**
 * Defines the types of available calls and state of the TokenB smart contract.
 */
export type TokenBTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'__postInit()void' | '__postInit', {
      argsObj: TokenBArgs['obj']['__postInit()void']
      argsTuple: TokenBArgs['tuple']['__postInit()void']
      returns: TokenBReturns['__postInit()void']
    }>
    & Record<'mint(address,uint256)void' | 'mint', {
      argsObj: TokenBArgs['obj']['mint(address,uint256)void']
      argsTuple: TokenBArgs['tuple']['mint(address,uint256)void']
      returns: TokenBReturns['mint(address,uint256)void']
    }>
    & Record<'balanceOf(address)uint256' | 'balanceOf', {
      argsObj: TokenBArgs['obj']['balanceOf(address)uint256']
      argsTuple: TokenBArgs['tuple']['balanceOf(address)uint256']
      returns: TokenBReturns['balanceOf(address)uint256']
    }>
    & Record<'balances(address)uint256' | 'balances', {
      argsObj: TokenBArgs['obj']['balances(address)uint256']
      argsTuple: TokenBArgs['tuple']['balances(address)uint256']
      returns: TokenBReturns['balances(address)uint256']
    }>
    & Record<'totalMinted()uint256' | 'totalMinted', {
      argsObj: TokenBArgs['obj']['totalMinted()uint256']
      argsTuple: TokenBArgs['tuple']['totalMinted()uint256']
      returns: TokenBReturns['totalMinted()uint256']
    }>
  /**
   * Defines the shape of the state of the application.
   */
  state: {
    global: {
      keys: {
        totalMinted: BinaryState
        __ctorPending: bigint
      }
      maps: {}
    }
    box: {
      keys: {}
      maps: {
        balances: Map<Uint8Array | string, Uint8Array>
      }
    }
  }
}

/**
 * Defines the possible abi call signatures.
 */
export type TokenBSignatures = keyof TokenBTypes['methods']
/**
 * Defines the possible abi call signatures for methods that return a non-void value.
 */
export type TokenBNonVoidMethodSignatures = keyof TokenBTypes['methods'] extends infer T ? T extends keyof TokenBTypes['methods'] ? MethodReturn<T> extends void ? never : T  : never : never
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the TokenB smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends TokenBSignatures> = TokenBTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the TokenB smart contract to the method's return type
 */
export type MethodReturn<TSignature extends TokenBSignatures> = TokenBTypes['methods'][TSignature]['returns']

/**
 * Defines the shape of the keyed global state of the application.
 */
export type GlobalKeysState = TokenBTypes['state']['global']['keys']

/**
 * Defines the shape of the keyed box state of the application.
 */
export type BoxKeysState = TokenBTypes['state']['box']['keys']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the TokenB smart contract
 */
export abstract class TokenBParamsFactory {
  /**
   * Constructs a no op call for the __postInit()void ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static __postInit(params: CallParams<TokenBArgs['obj']['__postInit()void'] | TokenBArgs['tuple']['__postInit()void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: '__postInit()void' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the mint(address,uint256)void ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static mint(params: CallParams<TokenBArgs['obj']['mint(address,uint256)void'] | TokenBArgs['tuple']['mint(address,uint256)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'mint(address,uint256)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.to, params.args.__arc4Amount],
    }
  }
  /**
   * Constructs a no op call for the balanceOf(address)uint256 ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static balanceOf(params: CallParams<TokenBArgs['obj']['balanceOf(address)uint256'] | TokenBArgs['tuple']['balanceOf(address)uint256']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'balanceOf(address)uint256' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.who],
    }
  }
  /**
   * Constructs a no op call for the balances(address)uint256 ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static balances(params: CallParams<TokenBArgs['obj']['balances(address)uint256'] | TokenBArgs['tuple']['balances(address)uint256']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'balances(address)uint256' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.key0],
    }
  }
  /**
   * Constructs a no op call for the totalMinted()uint256 ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static totalMinted(params: CallParams<TokenBArgs['obj']['totalMinted()uint256'] | TokenBArgs['tuple']['totalMinted()uint256']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'totalMinted()uint256' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
}

/**
 * A client to make calls to the TokenB smart contract
 */
export class TokenBClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `TokenBClient`
   *
   * @param appClient An `AppClient` instance which has been created with the TokenB app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `TokenBClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Checks for decode errors on the given return value and maps the return value to the return type for the given method
   * @returns The typed return value or undefined if there was no value
   */
  decodeReturnValue<TSignature extends TokenBNonVoidMethodSignatures>(method: TSignature, returnValue: ABIReturn | undefined) {
    return returnValue !== undefined ? getArc56ReturnValue<MethodReturn<TSignature>>(returnValue, this.appClient.getABIMethod(method), APP_SPEC.structs) : undefined
  }

  /**
   * Returns a new `TokenBClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<TokenBClient> {
    return new TokenBClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `TokenBClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<TokenBClient> {
    return new TokenBClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Makes a clear_state call to an existing instance of the TokenB smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the TokenB smart contract using the `__postInit()void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    __postInit: (params: CallParams<TokenBArgs['obj']['__postInit()void'] | TokenBArgs['tuple']['__postInit()void']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(TokenBParamsFactory.__postInit(params))
    },

    /**
     * Makes a call to the TokenB smart contract using the `mint(address,uint256)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    mint: (params: CallParams<TokenBArgs['obj']['mint(address,uint256)void'] | TokenBArgs['tuple']['mint(address,uint256)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(TokenBParamsFactory.mint(params))
    },

    /**
     * Makes a call to the TokenB smart contract using the `balanceOf(address)uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    balanceOf: (params: CallParams<TokenBArgs['obj']['balanceOf(address)uint256'] | TokenBArgs['tuple']['balanceOf(address)uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(TokenBParamsFactory.balanceOf(params))
    },

    /**
     * Makes a call to the TokenB smart contract using the `balances(address)uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    balances: (params: CallParams<TokenBArgs['obj']['balances(address)uint256'] | TokenBArgs['tuple']['balances(address)uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(TokenBParamsFactory.balances(params))
    },

    /**
     * Makes a call to the TokenB smart contract using the `totalMinted()uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    totalMinted: (params: CallParams<TokenBArgs['obj']['totalMinted()uint256'] | TokenBArgs['tuple']['totalMinted()uint256']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(TokenBParamsFactory.totalMinted(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Makes a clear_state call to an existing instance of the TokenB smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the TokenB smart contract using the `__postInit()void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    __postInit: (params: CallParams<TokenBArgs['obj']['__postInit()void'] | TokenBArgs['tuple']['__postInit()void']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(TokenBParamsFactory.__postInit(params))
    },

    /**
     * Makes a call to the TokenB smart contract using the `mint(address,uint256)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    mint: (params: CallParams<TokenBArgs['obj']['mint(address,uint256)void'] | TokenBArgs['tuple']['mint(address,uint256)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(TokenBParamsFactory.mint(params))
    },

    /**
     * Makes a call to the TokenB smart contract using the `balanceOf(address)uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    balanceOf: (params: CallParams<TokenBArgs['obj']['balanceOf(address)uint256'] | TokenBArgs['tuple']['balanceOf(address)uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(TokenBParamsFactory.balanceOf(params))
    },

    /**
     * Makes a call to the TokenB smart contract using the `balances(address)uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    balances: (params: CallParams<TokenBArgs['obj']['balances(address)uint256'] | TokenBArgs['tuple']['balances(address)uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(TokenBParamsFactory.balances(params))
    },

    /**
     * Makes a call to the TokenB smart contract using the `totalMinted()uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    totalMinted: (params: CallParams<TokenBArgs['obj']['totalMinted()uint256'] | TokenBArgs['tuple']['totalMinted()uint256']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(TokenBParamsFactory.totalMinted(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Makes a clear_state call to an existing instance of the TokenB smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the TokenB smart contract using the `__postInit()void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    __postInit: async (params: CallParams<TokenBArgs['obj']['__postInit()void'] | TokenBArgs['tuple']['__postInit()void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(TokenBParamsFactory.__postInit(params))
      return {...result, return: result.return as unknown as (undefined | TokenBReturns['__postInit()void'])}
    },

    /**
     * Makes a call to the TokenB smart contract using the `mint(address,uint256)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    mint: async (params: CallParams<TokenBArgs['obj']['mint(address,uint256)void'] | TokenBArgs['tuple']['mint(address,uint256)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(TokenBParamsFactory.mint(params))
      return {...result, return: result.return as unknown as (undefined | TokenBReturns['mint(address,uint256)void'])}
    },

    /**
     * Makes a call to the TokenB smart contract using the `balanceOf(address)uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    balanceOf: async (params: CallParams<TokenBArgs['obj']['balanceOf(address)uint256'] | TokenBArgs['tuple']['balanceOf(address)uint256']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(TokenBParamsFactory.balanceOf(params))
      return {...result, return: result.return as unknown as (undefined | TokenBReturns['balanceOf(address)uint256'])}
    },

    /**
     * Makes a call to the TokenB smart contract using the `balances(address)uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    balances: async (params: CallParams<TokenBArgs['obj']['balances(address)uint256'] | TokenBArgs['tuple']['balances(address)uint256']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(TokenBParamsFactory.balances(params))
      return {...result, return: result.return as unknown as (undefined | TokenBReturns['balances(address)uint256'])}
    },

    /**
     * Makes a call to the TokenB smart contract using the `totalMinted()uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    totalMinted: async (params: CallParams<TokenBArgs['obj']['totalMinted()uint256'] | TokenBArgs['tuple']['totalMinted()uint256']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(TokenBParamsFactory.totalMinted(params))
      return {...result, return: result.return as unknown as (undefined | TokenBReturns['totalMinted()uint256'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new TokenBClient(this.appClient.clone(params))
  }

  /**
   * Makes a readonly (simulated) call to the TokenB smart contract using the `balanceOf(address)uint256` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async balanceOf(params: CallParams<TokenBArgs['obj']['balanceOf(address)uint256'] | TokenBArgs['tuple']['balanceOf(address)uint256']>) {
    const result = await this.appClient.send.call(TokenBParamsFactory.balanceOf(params))
    return result.return as unknown as TokenBReturns['balanceOf(address)uint256']
  }

  /**
   * Makes a readonly (simulated) call to the TokenB smart contract using the `balances(address)uint256` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async balances(params: CallParams<TokenBArgs['obj']['balances(address)uint256'] | TokenBArgs['tuple']['balances(address)uint256']>) {
    const result = await this.appClient.send.call(TokenBParamsFactory.balances(params))
    return result.return as unknown as TokenBReturns['balances(address)uint256']
  }

  /**
   * Makes a readonly (simulated) call to the TokenB smart contract using the `totalMinted()uint256` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async totalMinted(params: CallParams<TokenBArgs['obj']['totalMinted()uint256'] | TokenBArgs['tuple']['totalMinted()uint256']> = {args: []}) {
    const result = await this.appClient.send.call(TokenBParamsFactory.totalMinted(params))
    return result.return as unknown as TokenBReturns['totalMinted()uint256']
  }

  /**
   * Methods to access state for the current TokenB app
   */
  state = {
    /**
     * Methods to access global state for the current TokenB app
     */
    global: {
      /**
       * Get all current keyed values from global state
       */
      getAll: async (): Promise<Partial<Expand<GlobalKeysState>>> => {
        const result = await this.appClient.state.global.getAll()
        return {
          totalMinted: new BinaryStateValue(result.totalMinted),
          __ctorPending: result.__ctor_pending,
        }
      },
      /**
       * Get the current value of the totalMinted key in global state
       */
      totalMinted: async (): Promise<BinaryState> => { return new BinaryStateValue((await this.appClient.state.global.getValue("totalMinted")) as Uint8Array | undefined) },
      /**
       * Get the current value of the __ctor_pending key in global state
       */
      __ctorPending: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("__ctor_pending")) as bigint | undefined },
    },
    /**
     * Methods to access box state for the current TokenB app
     */
    box: {
      /**
       * Get all current keyed values from box state
       */
      getAll: async (): Promise<Partial<Expand<BoxKeysState>>> => {
        const result = await this.appClient.state.box.getAll()
        return {
        }
      },
      /**
       * Get values from the balances map in box state
       */
      balances: {
        /**
         * Get all current values of the balances map in box state
         */
        getMap: async (): Promise<Map<Uint8Array, Uint8Array>> => { return (await this.appClient.state.box.getMap("balances")) as Map<Uint8Array, Uint8Array> },
        /**
         * Get a current value of the balances map by key from box state
         */
        value: async (key: Uint8Array | string): Promise<Uint8Array | undefined> => { return await this.appClient.state.box.getMapValue("balances", key) as Uint8Array | undefined },
      },
    },
  }

  public newGroup(): TokenBComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a __postInit()void method call against the TokenB contract
       */
      __postInit(params: CallParams<TokenBArgs['obj']['__postInit()void'] | TokenBArgs['tuple']['__postInit()void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.__postInit(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a mint(address,uint256)void method call against the TokenB contract
       */
      mint(params: CallParams<TokenBArgs['obj']['mint(address,uint256)void'] | TokenBArgs['tuple']['mint(address,uint256)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.mint(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a balanceOf(address)uint256 method call against the TokenB contract
       */
      balanceOf(params: CallParams<TokenBArgs['obj']['balanceOf(address)uint256'] | TokenBArgs['tuple']['balanceOf(address)uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.balanceOf(params)))
        resultMappers.push((v) => client.decodeReturnValue('balanceOf(address)uint256', v))
        return this
      },
      /**
       * Add a balances(address)uint256 method call against the TokenB contract
       */
      balances(params: CallParams<TokenBArgs['obj']['balances(address)uint256'] | TokenBArgs['tuple']['balances(address)uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.balances(params)))
        resultMappers.push((v) => client.decodeReturnValue('balances(address)uint256', v))
        return this
      },
      /**
       * Add a totalMinted()uint256 method call against the TokenB contract
       */
      totalMinted(params: CallParams<TokenBArgs['obj']['totalMinted()uint256'] | TokenBArgs['tuple']['totalMinted()uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.totalMinted(params)))
        resultMappers.push((v) => client.decodeReturnValue('totalMinted()uint256', v))
        return this
      },
      /**
       * Add a clear state call to the TokenB contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as TokenBComposer
  }
}
export type TokenBComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the __postInit()void ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  __postInit(params?: CallParams<TokenBArgs['obj']['__postInit()void'] | TokenBArgs['tuple']['__postInit()void']>): TokenBComposer<[...TReturns, TokenBReturns['__postInit()void'] | undefined]>

  /**
   * Calls the mint(address,uint256)void ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  mint(params?: CallParams<TokenBArgs['obj']['mint(address,uint256)void'] | TokenBArgs['tuple']['mint(address,uint256)void']>): TokenBComposer<[...TReturns, TokenBReturns['mint(address,uint256)void'] | undefined]>

  /**
   * Calls the balanceOf(address)uint256 ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  balanceOf(params?: CallParams<TokenBArgs['obj']['balanceOf(address)uint256'] | TokenBArgs['tuple']['balanceOf(address)uint256']>): TokenBComposer<[...TReturns, TokenBReturns['balanceOf(address)uint256'] | undefined]>

  /**
   * Calls the balances(address)uint256 ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  balances(params?: CallParams<TokenBArgs['obj']['balances(address)uint256'] | TokenBArgs['tuple']['balances(address)uint256']>): TokenBComposer<[...TReturns, TokenBReturns['balances(address)uint256'] | undefined]>

  /**
   * Calls the totalMinted()uint256 ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  totalMinted(params?: CallParams<TokenBArgs['obj']['totalMinted()uint256'] | TokenBArgs['tuple']['totalMinted()uint256']>): TokenBComposer<[...TReturns, TokenBReturns['totalMinted()uint256'] | undefined]>

  /**
   * Makes a clear_state call to an existing instance of the TokenB smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): TokenBComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): TokenBComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<TokenBComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<TokenBComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<TokenBComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<TokenBComposerResults<TReturns>>
}
export type TokenBComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

