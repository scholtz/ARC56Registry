/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"Airdrop","structs":{},"methods":[{"name":"setMerkleRoot","args":[{"type":"byte[32]","name":"newMerkleRoot"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":" @notice Sets the Merkle root for claim validation.\n @dev Only callable by the contract owner.\n @param newMerkleRoot The new Merkle root.","events":[],"recommendations":{}},{"name":"openRegistration","args":[],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":" @notice Opens the registration phase for users.\n @dev Only callable by the contract owner.","events":[],"recommendations":{}},{"name":"closeRegistration","args":[],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":" @notice Closes the registration phase.\n @dev Only callable by the contract owner.","events":[],"recommendations":{}},{"name":"openClaim","args":[],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":" @notice Opens the claim phase, allowing registered users to claim tokens.\n @dev Only callable by the contract owner.","events":[],"recommendations":{}},{"name":"closeClaim","args":[],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":" @notice Closes the claim phase.\n @dev Only callable by the contract owner.","events":[],"recommendations":{}},{"name":"getScope","args":[],"returns":{"type":"uint512"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"desc":" @notice Retrieves the expected proof scope.\n @return The scope value used for registration verification.","events":[],"recommendations":{}},{"name":"setConfigId","args":[{"type":"byte[32]","name":"configId"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":" @notice Sets the verification config ID\n @dev Only callable by the contract owner\n @param configId The verification config ID to set","events":[],"recommendations":{}},{"name":"getConfigId","args":[{"type":"byte[32]","name":"destinationChainId"},{"type":"byte[32]","name":"userIdentifier"},{"type":"byte[]","name":"userDefinedData"}],"returns":{"type":"byte[32]"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"desc":" @notice Generates a configId for the user\n @dev Override of the SelfVerificationRoot virtual function\n @param destinationChainId The destination chain ID\n @param userIdentifier The user identifier\n @param userDefinedData The user defined data\n @return The stored verification config ID","events":[],"recommendations":{}},{"name":"isRegistered","args":[{"type":"address","name":"registeredAddress"}],"returns":{"type":"bool"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"desc":" @notice Checks if a given address is registered.\n @param registeredAddress The address to check.\n @return True if the address is registered, false otherwise.","events":[],"recommendations":{}},{"name":"claim","args":[{"type":"uint512","name":"index"},{"type":"uint512","name":"amount"},{"type":"uint8[32][]","name":"__arc4_merkleProof"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":" @notice Allows a registered user to claim their tokens.\n @dev Reverts if registration is still open, if claiming is disabled, if already claimed,\n      or if the sender is not registered. Also validates the claim using a Merkle proof.\n @param index The index of the claim in the Merkle tree.\n @param amount The amount of tokens to be claimed.\n @param merkleProof The Merkle proof verifying the claim.","events":[],"recommendations":{}},{"name":"owner","args":[],"returns":{"type":"address"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"desc":" @dev Returns the address of the current owner.","events":[],"recommendations":{}},{"name":"renounceOwnership","args":[],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":" @dev Leaves the contract without owner. It will not be possible to call\n `onlyOwner` functions. Can only be called by the current owner.\n NOTE: Renouncing ownership will leave the contract without an owner,\n thereby disabling any functionality that is only available to the owner.","events":[],"recommendations":{}},{"name":"transferOwnership","args":[{"type":"address","name":"newOwner"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":" @dev Transfers ownership of the contract to a new account (`newOwner`).\n Can only be called by the current owner.","events":[],"recommendations":{}},{"name":"scope","args":[],"returns":{"type":"uint512"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"desc":" @notice Returns the current scope value\n @dev Public view function to access the current scope setting\n @return The scope value that proofs must match","events":[],"recommendations":{}},{"name":"verifySelfProof","args":[{"type":"byte[]","name":"proofPayload"},{"type":"byte[]","name":"userContextData"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":" @notice Verifies a self-proof using the bytes-based interface\n @dev Parses relayer data format and validates against contract settings before calling hub V2\n @param proofPayload Packed data from relayer in format: | 32 bytes attestationId | proof data |\n @param userContextData User-defined data in format: | 32 bytes destChainId | 32 bytes userIdentifier | data |\n @custom:data-format proofPayload = | 32 bytes attestationId | proofData |\n @custom:data-format userContextData = | 32 bytes destChainId | 32 bytes userIdentifier | data |\n @custom:data-format hubData = | 1 bytes contract version | 31 bytes buffer | 32 bytes scope | 32 bytes attestationId | proofData |","events":[],"recommendations":{}},{"name":"onVerificationSuccess","args":[{"type":"byte[]","name":"output"},{"type":"byte[]","name":"userData"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":" @notice Callback function called upon successful verification by the hub contract\n @dev Only callable by the identity verification hub V2 contract for security\n @param output The verification output data containing disclosed identity information\n @param userData The user-defined data passed through the verification process\n @custom:security Only the authorized hub contract can call this function\n @custom:flow This function decodes the output and calls the customizable verification hook","events":[],"recommendations":{}}],"arcs":[22,28],"desc":" @title Airdrop V2 (Experimental)\n @notice This contract manages an airdrop campaign by verifying user registrations with zero-knowledge proofs\n         supporting both E-Passport and EU ID Card attestations, and distributing ERC20 tokens.\n         It is provided for testing and demonstration purposes only.\n         **WARNING:** This contract has not been audited and is NOT intended for production use.\n @dev Inherits from SelfVerificationRoot V2 for registration logic and Ownable for administrative control.","networks":{},"state":{"schema":{"global":{"ints":2,"bytes":6},"local":{"ints":0,"bytes":0}},"keys":{"global":{"token":{"keyType":"AVMString","valueType":"address","key":"dG9rZW4="},"merkleRoot":{"keyType":"AVMString","valueType":"AVMBytes","key":"bWVya2xlUm9vdA=="},"isRegistrationOpen":{"keyType":"AVMString","valueType":"AVMUint64","key":"aXNSZWdpc3RyYXRpb25PcGVu"},"isClaimOpen":{"keyType":"AVMString","valueType":"AVMUint64","key":"aXNDbGFpbU9wZW4="},"verificationConfigId":{"keyType":"AVMString","valueType":"AVMBytes","key":"dmVyaWZpY2F0aW9uQ29uZmlnSWQ="},"_owner":{"keyType":"AVMString","valueType":"address","key":"X293bmVy"},"_scope":{"keyType":"AVMString","valueType":"AVMBytes","key":"X3Njb3Bl"},"_identityVerificationHubV2":{"keyType":"AVMString","valueType":"address","key":"X2lkZW50aXR5VmVyaWZpY2F0aW9uSHViVjI="}},"local":{},"box":{}},"maps":{"global":{},"local":{},"box":{"claimed":{"keyType":"AVMBytes","valueType":"uint64","prefix":"Y2xhaW1lZA=="},"_nullifierToUserIdentifier":{"keyType":"AVMBytes","valueType":"AVMBytes","prefix":"X251bGxpZmllclRvVXNlcklkZW50aWZpZXI="},"_registeredUserIdentifiers":{"keyType":"AVMBytes","valueType":"uint64","prefix":"X3JlZ2lzdGVyZWRVc2VySWRlbnRpZmllcnM="}}}},"bareActions":{"create":[],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[980],"errorMessage":"AlreadyClaimed"},{"pc":[963],"errorMessage":"ClaimNotOpen"},{"pc":[1232,1239],"errorMessage":"InvalidDataFormat"},{"pc":[1094],"errorMessage":"InvalidProof"},{"pc":[1432],"errorMessage":"InvalidUserIdentifier"},{"pc":[996],"errorMessage":"NotRegistered"},{"pc":[301,1167],"errorMessage":"OwnableInvalidOwner"},{"pc":[1477],"errorMessage":"OwnableUnauthorizedAccount"},{"pc":[1421],"errorMessage":"RegisteredNullifier"},{"pc":[957],"errorMessage":"RegistrationNotClosed"},{"pc":[1372],"errorMessage":"RegistrationNotOpen"},{"pc":[1113],"errorMessage":"SafeERC20FailedOperation"},{"pc":[1366],"errorMessage":"UnauthorizedCaller"},{"pc":[1449],"errorMessage":"UserIdentifierAlreadyRegistered"},{"pc":[1300,1363],"errorMessage":"check _identityVerificationHubV2 exists"},{"pc":[1135,1472,1487],"errorMessage":"check _owner exists"},{"pc":[801,1176,1250],"errorMessage":"check _scope exists"},{"pc":[962],"errorMessage":"check isClaimOpen exists"},{"pc":[955,1371],"errorMessage":"check isRegistrationOpen exists"},{"pc":[1019],"errorMessage":"check merkleRoot exists"},{"pc":[1111],"errorMessage":"check token exists"},{"pc":[866,1244],"errorMessage":"check verificationConfigId exists"},{"pc":[1053],"errorMessage":"index access is out of bounds"},{"pc":[855,936,1199,1215,1337,1350],"errorMessage":"invalid array length header"},{"pc":[945],"errorMessage":"invalid number of bytes for arc4.dynamic_array<arc4.static_array<arc4.uint8, 32>>"},{"pc":[861,1206,1222,1344,1356],"errorMessage":"invalid number of bytes for arc4.dynamic_array<arc4.uint8>"},{"pc":[714,826,842,849,881,1159],"errorMessage":"invalid number of bytes for arc4.static_array<arc4.uint8, 32>"},{"pc":[918,930],"errorMessage":"invalid number of bytes for arc4.uint512"},{"pc":[807,1182],"errorMessage":"overflow"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"events":[]} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


/**
 * The argument types for the Airdrop contract
 */
export type AirdropArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'setMerkleRoot(byte[32])void': {
      newMerkleRoot: Uint8Array
    }
    'openRegistration()void': Record<string, never>
    'closeRegistration()void': Record<string, never>
    'openClaim()void': Record<string, never>
    'closeClaim()void': Record<string, never>
    'getScope()uint512': Record<string, never>
    'setConfigId(byte[32])void': {
      configId: Uint8Array
    }
    'getConfigId(byte[32],byte[32],byte[])byte[32]': {
      destinationChainId: Uint8Array
      userIdentifier: Uint8Array
      userDefinedData: Uint8Array
    }
    'isRegistered(address)bool': {
      registeredAddress: string
    }
    'claim(uint512,uint512,uint8[32][])void': {
      index: bigint | number
      amount: bigint | number
      __arc4MerkleProof: [bigint | number, bigint | number, bigint | number, bigint | number, bigint | number, bigint | number, bigint | number, bigint | number, bigint | number, bigint | number, bigint | number, bigint | number, bigint | number, bigint | number, bigint | number, bigint | number, bigint | number, bigint | number, bigint | number, bigint | number, bigint | number, bigint | number, bigint | number, bigint | number, bigint | number, bigint | number, bigint | number, bigint | number, bigint | number, bigint | number, bigint | number, bigint | number][]
    }
    'owner()address': Record<string, never>
    'renounceOwnership()void': Record<string, never>
    'transferOwnership(address)void': {
      newOwner: string
    }
    'scope()uint512': Record<string, never>
    'verifySelfProof(byte[],byte[])void': {
      proofPayload: Uint8Array
      userContextData: Uint8Array
    }
    'onVerificationSuccess(byte[],byte[])void': {
      output: Uint8Array
      userData: Uint8Array
    }
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'setMerkleRoot(byte[32])void': [newMerkleRoot: Uint8Array]
    'openRegistration()void': []
    'closeRegistration()void': []
    'openClaim()void': []
    'closeClaim()void': []
    'getScope()uint512': []
    'setConfigId(byte[32])void': [configId: Uint8Array]
    'getConfigId(byte[32],byte[32],byte[])byte[32]': [destinationChainId: Uint8Array, userIdentifier: Uint8Array, userDefinedData: Uint8Array]
    'isRegistered(address)bool': [registeredAddress: string]
    'claim(uint512,uint512,uint8[32][])void': [index: bigint | number, amount: bigint | number, __arc4MerkleProof: [bigint | number, bigint | number, bigint | number, bigint | number, bigint | number, bigint | number, bigint | number, bigint | number, bigint | number, bigint | number, bigint | number, bigint | number, bigint | number, bigint | number, bigint | number, bigint | number, bigint | number, bigint | number, bigint | number, bigint | number, bigint | number, bigint | number, bigint | number, bigint | number, bigint | number, bigint | number, bigint | number, bigint | number, bigint | number, bigint | number, bigint | number, bigint | number][]]
    'owner()address': []
    'renounceOwnership()void': []
    'transferOwnership(address)void': [newOwner: string]
    'scope()uint512': []
    'verifySelfProof(byte[],byte[])void': [proofPayload: Uint8Array, userContextData: Uint8Array]
    'onVerificationSuccess(byte[],byte[])void': [output: Uint8Array, userData: Uint8Array]
  }
}

/**
 * The return type for each method
 */
export type AirdropReturns = {
  'setMerkleRoot(byte[32])void': void
  'openRegistration()void': void
  'closeRegistration()void': void
  'openClaim()void': void
  'closeClaim()void': void
  'getScope()uint512': bigint
  'setConfigId(byte[32])void': void
  'getConfigId(byte[32],byte[32],byte[])byte[32]': Uint8Array
  'isRegistered(address)bool': boolean
  'claim(uint512,uint512,uint8[32][])void': void
  'owner()address': string
  'renounceOwnership()void': void
  'transferOwnership(address)void': void
  'scope()uint512': bigint
  'verifySelfProof(byte[],byte[])void': void
  'onVerificationSuccess(byte[],byte[])void': void
}

/**
 * Defines the types of available calls and state of the Airdrop smart contract.
 */
export type AirdropTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'setMerkleRoot(byte[32])void' | 'setMerkleRoot', {
      argsObj: AirdropArgs['obj']['setMerkleRoot(byte[32])void']
      argsTuple: AirdropArgs['tuple']['setMerkleRoot(byte[32])void']
      returns: AirdropReturns['setMerkleRoot(byte[32])void']
    }>
    & Record<'openRegistration()void' | 'openRegistration', {
      argsObj: AirdropArgs['obj']['openRegistration()void']
      argsTuple: AirdropArgs['tuple']['openRegistration()void']
      returns: AirdropReturns['openRegistration()void']
    }>
    & Record<'closeRegistration()void' | 'closeRegistration', {
      argsObj: AirdropArgs['obj']['closeRegistration()void']
      argsTuple: AirdropArgs['tuple']['closeRegistration()void']
      returns: AirdropReturns['closeRegistration()void']
    }>
    & Record<'openClaim()void' | 'openClaim', {
      argsObj: AirdropArgs['obj']['openClaim()void']
      argsTuple: AirdropArgs['tuple']['openClaim()void']
      returns: AirdropReturns['openClaim()void']
    }>
    & Record<'closeClaim()void' | 'closeClaim', {
      argsObj: AirdropArgs['obj']['closeClaim()void']
      argsTuple: AirdropArgs['tuple']['closeClaim()void']
      returns: AirdropReturns['closeClaim()void']
    }>
    & Record<'getScope()uint512' | 'getScope', {
      argsObj: AirdropArgs['obj']['getScope()uint512']
      argsTuple: AirdropArgs['tuple']['getScope()uint512']
      returns: AirdropReturns['getScope()uint512']
    }>
    & Record<'setConfigId(byte[32])void' | 'setConfigId', {
      argsObj: AirdropArgs['obj']['setConfigId(byte[32])void']
      argsTuple: AirdropArgs['tuple']['setConfigId(byte[32])void']
      returns: AirdropReturns['setConfigId(byte[32])void']
    }>
    & Record<'getConfigId(byte[32],byte[32],byte[])byte[32]' | 'getConfigId', {
      argsObj: AirdropArgs['obj']['getConfigId(byte[32],byte[32],byte[])byte[32]']
      argsTuple: AirdropArgs['tuple']['getConfigId(byte[32],byte[32],byte[])byte[32]']
      returns: AirdropReturns['getConfigId(byte[32],byte[32],byte[])byte[32]']
    }>
    & Record<'isRegistered(address)bool' | 'isRegistered', {
      argsObj: AirdropArgs['obj']['isRegistered(address)bool']
      argsTuple: AirdropArgs['tuple']['isRegistered(address)bool']
      returns: AirdropReturns['isRegistered(address)bool']
    }>
    & Record<'claim(uint512,uint512,uint8[32][])void' | 'claim', {
      argsObj: AirdropArgs['obj']['claim(uint512,uint512,uint8[32][])void']
      argsTuple: AirdropArgs['tuple']['claim(uint512,uint512,uint8[32][])void']
      returns: AirdropReturns['claim(uint512,uint512,uint8[32][])void']
    }>
    & Record<'owner()address' | 'owner', {
      argsObj: AirdropArgs['obj']['owner()address']
      argsTuple: AirdropArgs['tuple']['owner()address']
      returns: AirdropReturns['owner()address']
    }>
    & Record<'renounceOwnership()void' | 'renounceOwnership', {
      argsObj: AirdropArgs['obj']['renounceOwnership()void']
      argsTuple: AirdropArgs['tuple']['renounceOwnership()void']
      returns: AirdropReturns['renounceOwnership()void']
    }>
    & Record<'transferOwnership(address)void' | 'transferOwnership', {
      argsObj: AirdropArgs['obj']['transferOwnership(address)void']
      argsTuple: AirdropArgs['tuple']['transferOwnership(address)void']
      returns: AirdropReturns['transferOwnership(address)void']
    }>
    & Record<'scope()uint512' | 'scope', {
      argsObj: AirdropArgs['obj']['scope()uint512']
      argsTuple: AirdropArgs['tuple']['scope()uint512']
      returns: AirdropReturns['scope()uint512']
    }>
    & Record<'verifySelfProof(byte[],byte[])void' | 'verifySelfProof', {
      argsObj: AirdropArgs['obj']['verifySelfProof(byte[],byte[])void']
      argsTuple: AirdropArgs['tuple']['verifySelfProof(byte[],byte[])void']
      returns: AirdropReturns['verifySelfProof(byte[],byte[])void']
    }>
    & Record<'onVerificationSuccess(byte[],byte[])void' | 'onVerificationSuccess', {
      argsObj: AirdropArgs['obj']['onVerificationSuccess(byte[],byte[])void']
      argsTuple: AirdropArgs['tuple']['onVerificationSuccess(byte[],byte[])void']
      returns: AirdropReturns['onVerificationSuccess(byte[],byte[])void']
    }>
  /**
   * Defines the shape of the state of the application.
   */
  state: {
    global: {
      keys: {
        token: string
        merkleRoot: BinaryState
        isRegistrationOpen: bigint
        isClaimOpen: bigint
        verificationConfigId: BinaryState
        _owner: string
        _scope: BinaryState
        _identityVerificationHubV2: string
      }
      maps: {}
    }
    box: {
      keys: {}
      maps: {
        claimed: Map<Uint8Array | string, bigint>
        _nullifierToUserIdentifier: Map<Uint8Array | string, Uint8Array>
        _registeredUserIdentifiers: Map<Uint8Array | string, bigint>
      }
    }
  }
}

/**
 * Defines the possible abi call signatures.
 */
export type AirdropSignatures = keyof AirdropTypes['methods']
/**
 * Defines the possible abi call signatures for methods that return a non-void value.
 */
export type AirdropNonVoidMethodSignatures = keyof AirdropTypes['methods'] extends infer T ? T extends keyof AirdropTypes['methods'] ? MethodReturn<T> extends void ? never : T  : never : never
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the Airdrop smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends AirdropSignatures> = AirdropTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the Airdrop smart contract to the method's return type
 */
export type MethodReturn<TSignature extends AirdropSignatures> = AirdropTypes['methods'][TSignature]['returns']

/**
 * Defines the shape of the keyed global state of the application.
 */
export type GlobalKeysState = AirdropTypes['state']['global']['keys']

/**
 * Defines the shape of the keyed box state of the application.
 */
export type BoxKeysState = AirdropTypes['state']['box']['keys']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the Airdrop smart contract
 */
export abstract class AirdropParamsFactory {
  /**
   * Constructs a no op call for the setMerkleRoot(byte[32])void ABI method
   *
  *  @notice Sets the Merkle root for claim validation.
  @dev Only callable by the contract owner.
  @param newMerkleRoot The new Merkle root.

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static setMerkleRoot(params: CallParams<AirdropArgs['obj']['setMerkleRoot(byte[32])void'] | AirdropArgs['tuple']['setMerkleRoot(byte[32])void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'setMerkleRoot(byte[32])void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.newMerkleRoot],
    }
  }
  /**
   * Constructs a no op call for the openRegistration()void ABI method
   *
  *  @notice Opens the registration phase for users.
  @dev Only callable by the contract owner.

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static openRegistration(params: CallParams<AirdropArgs['obj']['openRegistration()void'] | AirdropArgs['tuple']['openRegistration()void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'openRegistration()void' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the closeRegistration()void ABI method
   *
  *  @notice Closes the registration phase.
  @dev Only callable by the contract owner.

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static closeRegistration(params: CallParams<AirdropArgs['obj']['closeRegistration()void'] | AirdropArgs['tuple']['closeRegistration()void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'closeRegistration()void' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the openClaim()void ABI method
   *
  *  @notice Opens the claim phase, allowing registered users to claim tokens.
  @dev Only callable by the contract owner.

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static openClaim(params: CallParams<AirdropArgs['obj']['openClaim()void'] | AirdropArgs['tuple']['openClaim()void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'openClaim()void' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the closeClaim()void ABI method
   *
  *  @notice Closes the claim phase.
  @dev Only callable by the contract owner.

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static closeClaim(params: CallParams<AirdropArgs['obj']['closeClaim()void'] | AirdropArgs['tuple']['closeClaim()void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'closeClaim()void' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the getScope()uint512 ABI method
   *
  *  @notice Retrieves the expected proof scope.
  @return The scope value used for registration verification.

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static getScope(params: CallParams<AirdropArgs['obj']['getScope()uint512'] | AirdropArgs['tuple']['getScope()uint512']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'getScope()uint512' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the setConfigId(byte[32])void ABI method
   *
  *  @notice Sets the verification config ID
  @dev Only callable by the contract owner
  @param configId The verification config ID to set

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static setConfigId(params: CallParams<AirdropArgs['obj']['setConfigId(byte[32])void'] | AirdropArgs['tuple']['setConfigId(byte[32])void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'setConfigId(byte[32])void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.configId],
    }
  }
  /**
   * Constructs a no op call for the getConfigId(byte[32],byte[32],byte[])byte[32] ABI method
   *
  *  @notice Generates a configId for the user
  @dev Override of the SelfVerificationRoot virtual function
  @param destinationChainId The destination chain ID
  @param userIdentifier The user identifier
  @param userDefinedData The user defined data
  @return The stored verification config ID

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static getConfigId(params: CallParams<AirdropArgs['obj']['getConfigId(byte[32],byte[32],byte[])byte[32]'] | AirdropArgs['tuple']['getConfigId(byte[32],byte[32],byte[])byte[32]']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'getConfigId(byte[32],byte[32],byte[])byte[32]' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.destinationChainId, params.args.userIdentifier, params.args.userDefinedData],
    }
  }
  /**
   * Constructs a no op call for the isRegistered(address)bool ABI method
   *
  *  @notice Checks if a given address is registered.
  @param registeredAddress The address to check.
  @return True if the address is registered, false otherwise.

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static isRegistered(params: CallParams<AirdropArgs['obj']['isRegistered(address)bool'] | AirdropArgs['tuple']['isRegistered(address)bool']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'isRegistered(address)bool' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.registeredAddress],
    }
  }
  /**
   * Constructs a no op call for the claim(uint512,uint512,uint8[32][])void ABI method
   *
  *  @notice Allows a registered user to claim their tokens.
  @dev Reverts if registration is still open, if claiming is disabled, if already claimed,
       or if the sender is not registered. Also validates the claim using a Merkle proof.
  @param index The index of the claim in the Merkle tree.
  @param amount The amount of tokens to be claimed.
  @param merkleProof The Merkle proof verifying the claim.

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static claim(params: CallParams<AirdropArgs['obj']['claim(uint512,uint512,uint8[32][])void'] | AirdropArgs['tuple']['claim(uint512,uint512,uint8[32][])void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'claim(uint512,uint512,uint8[32][])void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.index, params.args.amount, params.args.__arc4MerkleProof],
    }
  }
  /**
   * Constructs a no op call for the owner()address ABI method
   *
   *  @dev Returns the address of the current owner.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static owner(params: CallParams<AirdropArgs['obj']['owner()address'] | AirdropArgs['tuple']['owner()address']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'owner()address' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the renounceOwnership()void ABI method
   *
  *  @dev Leaves the contract without owner. It will not be possible to call
  `onlyOwner` functions. Can only be called by the current owner.
  NOTE: Renouncing ownership will leave the contract without an owner,
  thereby disabling any functionality that is only available to the owner.

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static renounceOwnership(params: CallParams<AirdropArgs['obj']['renounceOwnership()void'] | AirdropArgs['tuple']['renounceOwnership()void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'renounceOwnership()void' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the transferOwnership(address)void ABI method
   *
  *  @dev Transfers ownership of the contract to a new account (`newOwner`).
  Can only be called by the current owner.

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static transferOwnership(params: CallParams<AirdropArgs['obj']['transferOwnership(address)void'] | AirdropArgs['tuple']['transferOwnership(address)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'transferOwnership(address)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.newOwner],
    }
  }
  /**
   * Constructs a no op call for the scope()uint512 ABI method
   *
  *  @notice Returns the current scope value
  @dev Public view function to access the current scope setting
  @return The scope value that proofs must match

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static scope(params: CallParams<AirdropArgs['obj']['scope()uint512'] | AirdropArgs['tuple']['scope()uint512']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'scope()uint512' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the verifySelfProof(byte[],byte[])void ABI method
   *
  *  @notice Verifies a self-proof using the bytes-based interface
  @dev Parses relayer data format and validates against contract settings before calling hub V2
  @param proofPayload Packed data from relayer in format: | 32 bytes attestationId | proof data |
  @param userContextData User-defined data in format: | 32 bytes destChainId | 32 bytes userIdentifier | data |
  @custom:data-format proofPayload = | 32 bytes attestationId | proofData |
  @custom:data-format userContextData = | 32 bytes destChainId | 32 bytes userIdentifier | data |
  @custom:data-format hubData = | 1 bytes contract version | 31 bytes buffer | 32 bytes scope | 32 bytes attestationId | proofData |

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static verifySelfProof(params: CallParams<AirdropArgs['obj']['verifySelfProof(byte[],byte[])void'] | AirdropArgs['tuple']['verifySelfProof(byte[],byte[])void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'verifySelfProof(byte[],byte[])void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.proofPayload, params.args.userContextData],
    }
  }
  /**
   * Constructs a no op call for the onVerificationSuccess(byte[],byte[])void ABI method
   *
  *  @notice Callback function called upon successful verification by the hub contract
  @dev Only callable by the identity verification hub V2 contract for security
  @param output The verification output data containing disclosed identity information
  @param userData The user-defined data passed through the verification process
  @custom:security Only the authorized hub contract can call this function
  @custom:flow This function decodes the output and calls the customizable verification hook

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static onVerificationSuccess(params: CallParams<AirdropArgs['obj']['onVerificationSuccess(byte[],byte[])void'] | AirdropArgs['tuple']['onVerificationSuccess(byte[],byte[])void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'onVerificationSuccess(byte[],byte[])void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.output, params.args.userData],
    }
  }
}

/**
 * A client to make calls to the Airdrop smart contract
 */
export class AirdropClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `AirdropClient`
   *
   * @param appClient An `AppClient` instance which has been created with the Airdrop app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `AirdropClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Checks for decode errors on the given return value and maps the return value to the return type for the given method
   * @returns The typed return value or undefined if there was no value
   */
  decodeReturnValue<TSignature extends AirdropNonVoidMethodSignatures>(method: TSignature, returnValue: ABIReturn | undefined) {
    return returnValue !== undefined ? getArc56ReturnValue<MethodReturn<TSignature>>(returnValue, this.appClient.getABIMethod(method), APP_SPEC.structs) : undefined
  }

  /**
   * Returns a new `AirdropClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<AirdropClient> {
    return new AirdropClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `AirdropClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<AirdropClient> {
    return new AirdropClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Makes a clear_state call to an existing instance of the Airdrop smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the Airdrop smart contract using the `setMerkleRoot(byte[32])void` ABI method.
     *
    *  @notice Sets the Merkle root for claim validation.
    @dev Only callable by the contract owner.
    @param newMerkleRoot The new Merkle root.

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    setMerkleRoot: (params: CallParams<AirdropArgs['obj']['setMerkleRoot(byte[32])void'] | AirdropArgs['tuple']['setMerkleRoot(byte[32])void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(AirdropParamsFactory.setMerkleRoot(params))
    },

    /**
     * Makes a call to the Airdrop smart contract using the `openRegistration()void` ABI method.
     *
    *  @notice Opens the registration phase for users.
    @dev Only callable by the contract owner.

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    openRegistration: (params: CallParams<AirdropArgs['obj']['openRegistration()void'] | AirdropArgs['tuple']['openRegistration()void']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(AirdropParamsFactory.openRegistration(params))
    },

    /**
     * Makes a call to the Airdrop smart contract using the `closeRegistration()void` ABI method.
     *
    *  @notice Closes the registration phase.
    @dev Only callable by the contract owner.

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    closeRegistration: (params: CallParams<AirdropArgs['obj']['closeRegistration()void'] | AirdropArgs['tuple']['closeRegistration()void']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(AirdropParamsFactory.closeRegistration(params))
    },

    /**
     * Makes a call to the Airdrop smart contract using the `openClaim()void` ABI method.
     *
    *  @notice Opens the claim phase, allowing registered users to claim tokens.
    @dev Only callable by the contract owner.

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    openClaim: (params: CallParams<AirdropArgs['obj']['openClaim()void'] | AirdropArgs['tuple']['openClaim()void']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(AirdropParamsFactory.openClaim(params))
    },

    /**
     * Makes a call to the Airdrop smart contract using the `closeClaim()void` ABI method.
     *
    *  @notice Closes the claim phase.
    @dev Only callable by the contract owner.

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    closeClaim: (params: CallParams<AirdropArgs['obj']['closeClaim()void'] | AirdropArgs['tuple']['closeClaim()void']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(AirdropParamsFactory.closeClaim(params))
    },

    /**
     * Makes a call to the Airdrop smart contract using the `getScope()uint512` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
    *  @notice Retrieves the expected proof scope.
    @return The scope value used for registration verification.

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    getScope: (params: CallParams<AirdropArgs['obj']['getScope()uint512'] | AirdropArgs['tuple']['getScope()uint512']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(AirdropParamsFactory.getScope(params))
    },

    /**
     * Makes a call to the Airdrop smart contract using the `setConfigId(byte[32])void` ABI method.
     *
    *  @notice Sets the verification config ID
    @dev Only callable by the contract owner
    @param configId The verification config ID to set

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    setConfigId: (params: CallParams<AirdropArgs['obj']['setConfigId(byte[32])void'] | AirdropArgs['tuple']['setConfigId(byte[32])void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(AirdropParamsFactory.setConfigId(params))
    },

    /**
     * Makes a call to the Airdrop smart contract using the `getConfigId(byte[32],byte[32],byte[])byte[32]` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
    *  @notice Generates a configId for the user
    @dev Override of the SelfVerificationRoot virtual function
    @param destinationChainId The destination chain ID
    @param userIdentifier The user identifier
    @param userDefinedData The user defined data
    @return The stored verification config ID

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    getConfigId: (params: CallParams<AirdropArgs['obj']['getConfigId(byte[32],byte[32],byte[])byte[32]'] | AirdropArgs['tuple']['getConfigId(byte[32],byte[32],byte[])byte[32]']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(AirdropParamsFactory.getConfigId(params))
    },

    /**
     * Makes a call to the Airdrop smart contract using the `isRegistered(address)bool` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
    *  @notice Checks if a given address is registered.
    @param registeredAddress The address to check.
    @return True if the address is registered, false otherwise.

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    isRegistered: (params: CallParams<AirdropArgs['obj']['isRegistered(address)bool'] | AirdropArgs['tuple']['isRegistered(address)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(AirdropParamsFactory.isRegistered(params))
    },

    /**
     * Makes a call to the Airdrop smart contract using the `claim(uint512,uint512,uint8[32][])void` ABI method.
     *
    *  @notice Allows a registered user to claim their tokens.
    @dev Reverts if registration is still open, if claiming is disabled, if already claimed,
         or if the sender is not registered. Also validates the claim using a Merkle proof.
    @param index The index of the claim in the Merkle tree.
    @param amount The amount of tokens to be claimed.
    @param merkleProof The Merkle proof verifying the claim.

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    claim: (params: CallParams<AirdropArgs['obj']['claim(uint512,uint512,uint8[32][])void'] | AirdropArgs['tuple']['claim(uint512,uint512,uint8[32][])void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(AirdropParamsFactory.claim(params))
    },

    /**
     * Makes a call to the Airdrop smart contract using the `owner()address` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     *  @dev Returns the address of the current owner.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    owner: (params: CallParams<AirdropArgs['obj']['owner()address'] | AirdropArgs['tuple']['owner()address']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(AirdropParamsFactory.owner(params))
    },

    /**
     * Makes a call to the Airdrop smart contract using the `renounceOwnership()void` ABI method.
     *
    *  @dev Leaves the contract without owner. It will not be possible to call
    `onlyOwner` functions. Can only be called by the current owner.
    NOTE: Renouncing ownership will leave the contract without an owner,
    thereby disabling any functionality that is only available to the owner.

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    renounceOwnership: (params: CallParams<AirdropArgs['obj']['renounceOwnership()void'] | AirdropArgs['tuple']['renounceOwnership()void']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(AirdropParamsFactory.renounceOwnership(params))
    },

    /**
     * Makes a call to the Airdrop smart contract using the `transferOwnership(address)void` ABI method.
     *
    *  @dev Transfers ownership of the contract to a new account (`newOwner`).
    Can only be called by the current owner.

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    transferOwnership: (params: CallParams<AirdropArgs['obj']['transferOwnership(address)void'] | AirdropArgs['tuple']['transferOwnership(address)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(AirdropParamsFactory.transferOwnership(params))
    },

    /**
     * Makes a call to the Airdrop smart contract using the `scope()uint512` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
    *  @notice Returns the current scope value
    @dev Public view function to access the current scope setting
    @return The scope value that proofs must match

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    scope: (params: CallParams<AirdropArgs['obj']['scope()uint512'] | AirdropArgs['tuple']['scope()uint512']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(AirdropParamsFactory.scope(params))
    },

    /**
     * Makes a call to the Airdrop smart contract using the `verifySelfProof(byte[],byte[])void` ABI method.
     *
    *  @notice Verifies a self-proof using the bytes-based interface
    @dev Parses relayer data format and validates against contract settings before calling hub V2
    @param proofPayload Packed data from relayer in format: | 32 bytes attestationId | proof data |
    @param userContextData User-defined data in format: | 32 bytes destChainId | 32 bytes userIdentifier | data |
    @custom:data-format proofPayload = | 32 bytes attestationId | proofData |
    @custom:data-format userContextData = | 32 bytes destChainId | 32 bytes userIdentifier | data |
    @custom:data-format hubData = | 1 bytes contract version | 31 bytes buffer | 32 bytes scope | 32 bytes attestationId | proofData |

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    verifySelfProof: (params: CallParams<AirdropArgs['obj']['verifySelfProof(byte[],byte[])void'] | AirdropArgs['tuple']['verifySelfProof(byte[],byte[])void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(AirdropParamsFactory.verifySelfProof(params))
    },

    /**
     * Makes a call to the Airdrop smart contract using the `onVerificationSuccess(byte[],byte[])void` ABI method.
     *
    *  @notice Callback function called upon successful verification by the hub contract
    @dev Only callable by the identity verification hub V2 contract for security
    @param output The verification output data containing disclosed identity information
    @param userData The user-defined data passed through the verification process
    @custom:security Only the authorized hub contract can call this function
    @custom:flow This function decodes the output and calls the customizable verification hook

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    onVerificationSuccess: (params: CallParams<AirdropArgs['obj']['onVerificationSuccess(byte[],byte[])void'] | AirdropArgs['tuple']['onVerificationSuccess(byte[],byte[])void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(AirdropParamsFactory.onVerificationSuccess(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Makes a clear_state call to an existing instance of the Airdrop smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the Airdrop smart contract using the `setMerkleRoot(byte[32])void` ABI method.
     *
    *  @notice Sets the Merkle root for claim validation.
    @dev Only callable by the contract owner.
    @param newMerkleRoot The new Merkle root.

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    setMerkleRoot: (params: CallParams<AirdropArgs['obj']['setMerkleRoot(byte[32])void'] | AirdropArgs['tuple']['setMerkleRoot(byte[32])void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(AirdropParamsFactory.setMerkleRoot(params))
    },

    /**
     * Makes a call to the Airdrop smart contract using the `openRegistration()void` ABI method.
     *
    *  @notice Opens the registration phase for users.
    @dev Only callable by the contract owner.

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    openRegistration: (params: CallParams<AirdropArgs['obj']['openRegistration()void'] | AirdropArgs['tuple']['openRegistration()void']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(AirdropParamsFactory.openRegistration(params))
    },

    /**
     * Makes a call to the Airdrop smart contract using the `closeRegistration()void` ABI method.
     *
    *  @notice Closes the registration phase.
    @dev Only callable by the contract owner.

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    closeRegistration: (params: CallParams<AirdropArgs['obj']['closeRegistration()void'] | AirdropArgs['tuple']['closeRegistration()void']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(AirdropParamsFactory.closeRegistration(params))
    },

    /**
     * Makes a call to the Airdrop smart contract using the `openClaim()void` ABI method.
     *
    *  @notice Opens the claim phase, allowing registered users to claim tokens.
    @dev Only callable by the contract owner.

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    openClaim: (params: CallParams<AirdropArgs['obj']['openClaim()void'] | AirdropArgs['tuple']['openClaim()void']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(AirdropParamsFactory.openClaim(params))
    },

    /**
     * Makes a call to the Airdrop smart contract using the `closeClaim()void` ABI method.
     *
    *  @notice Closes the claim phase.
    @dev Only callable by the contract owner.

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    closeClaim: (params: CallParams<AirdropArgs['obj']['closeClaim()void'] | AirdropArgs['tuple']['closeClaim()void']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(AirdropParamsFactory.closeClaim(params))
    },

    /**
     * Makes a call to the Airdrop smart contract using the `getScope()uint512` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
    *  @notice Retrieves the expected proof scope.
    @return The scope value used for registration verification.

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    getScope: (params: CallParams<AirdropArgs['obj']['getScope()uint512'] | AirdropArgs['tuple']['getScope()uint512']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(AirdropParamsFactory.getScope(params))
    },

    /**
     * Makes a call to the Airdrop smart contract using the `setConfigId(byte[32])void` ABI method.
     *
    *  @notice Sets the verification config ID
    @dev Only callable by the contract owner
    @param configId The verification config ID to set

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    setConfigId: (params: CallParams<AirdropArgs['obj']['setConfigId(byte[32])void'] | AirdropArgs['tuple']['setConfigId(byte[32])void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(AirdropParamsFactory.setConfigId(params))
    },

    /**
     * Makes a call to the Airdrop smart contract using the `getConfigId(byte[32],byte[32],byte[])byte[32]` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
    *  @notice Generates a configId for the user
    @dev Override of the SelfVerificationRoot virtual function
    @param destinationChainId The destination chain ID
    @param userIdentifier The user identifier
    @param userDefinedData The user defined data
    @return The stored verification config ID

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    getConfigId: (params: CallParams<AirdropArgs['obj']['getConfigId(byte[32],byte[32],byte[])byte[32]'] | AirdropArgs['tuple']['getConfigId(byte[32],byte[32],byte[])byte[32]']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(AirdropParamsFactory.getConfigId(params))
    },

    /**
     * Makes a call to the Airdrop smart contract using the `isRegistered(address)bool` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
    *  @notice Checks if a given address is registered.
    @param registeredAddress The address to check.
    @return True if the address is registered, false otherwise.

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    isRegistered: (params: CallParams<AirdropArgs['obj']['isRegistered(address)bool'] | AirdropArgs['tuple']['isRegistered(address)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(AirdropParamsFactory.isRegistered(params))
    },

    /**
     * Makes a call to the Airdrop smart contract using the `claim(uint512,uint512,uint8[32][])void` ABI method.
     *
    *  @notice Allows a registered user to claim their tokens.
    @dev Reverts if registration is still open, if claiming is disabled, if already claimed,
         or if the sender is not registered. Also validates the claim using a Merkle proof.
    @param index The index of the claim in the Merkle tree.
    @param amount The amount of tokens to be claimed.
    @param merkleProof The Merkle proof verifying the claim.

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    claim: (params: CallParams<AirdropArgs['obj']['claim(uint512,uint512,uint8[32][])void'] | AirdropArgs['tuple']['claim(uint512,uint512,uint8[32][])void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(AirdropParamsFactory.claim(params))
    },

    /**
     * Makes a call to the Airdrop smart contract using the `owner()address` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     *  @dev Returns the address of the current owner.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    owner: (params: CallParams<AirdropArgs['obj']['owner()address'] | AirdropArgs['tuple']['owner()address']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(AirdropParamsFactory.owner(params))
    },

    /**
     * Makes a call to the Airdrop smart contract using the `renounceOwnership()void` ABI method.
     *
    *  @dev Leaves the contract without owner. It will not be possible to call
    `onlyOwner` functions. Can only be called by the current owner.
    NOTE: Renouncing ownership will leave the contract without an owner,
    thereby disabling any functionality that is only available to the owner.

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    renounceOwnership: (params: CallParams<AirdropArgs['obj']['renounceOwnership()void'] | AirdropArgs['tuple']['renounceOwnership()void']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(AirdropParamsFactory.renounceOwnership(params))
    },

    /**
     * Makes a call to the Airdrop smart contract using the `transferOwnership(address)void` ABI method.
     *
    *  @dev Transfers ownership of the contract to a new account (`newOwner`).
    Can only be called by the current owner.

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    transferOwnership: (params: CallParams<AirdropArgs['obj']['transferOwnership(address)void'] | AirdropArgs['tuple']['transferOwnership(address)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(AirdropParamsFactory.transferOwnership(params))
    },

    /**
     * Makes a call to the Airdrop smart contract using the `scope()uint512` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
    *  @notice Returns the current scope value
    @dev Public view function to access the current scope setting
    @return The scope value that proofs must match

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    scope: (params: CallParams<AirdropArgs['obj']['scope()uint512'] | AirdropArgs['tuple']['scope()uint512']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(AirdropParamsFactory.scope(params))
    },

    /**
     * Makes a call to the Airdrop smart contract using the `verifySelfProof(byte[],byte[])void` ABI method.
     *
    *  @notice Verifies a self-proof using the bytes-based interface
    @dev Parses relayer data format and validates against contract settings before calling hub V2
    @param proofPayload Packed data from relayer in format: | 32 bytes attestationId | proof data |
    @param userContextData User-defined data in format: | 32 bytes destChainId | 32 bytes userIdentifier | data |
    @custom:data-format proofPayload = | 32 bytes attestationId | proofData |
    @custom:data-format userContextData = | 32 bytes destChainId | 32 bytes userIdentifier | data |
    @custom:data-format hubData = | 1 bytes contract version | 31 bytes buffer | 32 bytes scope | 32 bytes attestationId | proofData |

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    verifySelfProof: (params: CallParams<AirdropArgs['obj']['verifySelfProof(byte[],byte[])void'] | AirdropArgs['tuple']['verifySelfProof(byte[],byte[])void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(AirdropParamsFactory.verifySelfProof(params))
    },

    /**
     * Makes a call to the Airdrop smart contract using the `onVerificationSuccess(byte[],byte[])void` ABI method.
     *
    *  @notice Callback function called upon successful verification by the hub contract
    @dev Only callable by the identity verification hub V2 contract for security
    @param output The verification output data containing disclosed identity information
    @param userData The user-defined data passed through the verification process
    @custom:security Only the authorized hub contract can call this function
    @custom:flow This function decodes the output and calls the customizable verification hook

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    onVerificationSuccess: (params: CallParams<AirdropArgs['obj']['onVerificationSuccess(byte[],byte[])void'] | AirdropArgs['tuple']['onVerificationSuccess(byte[],byte[])void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(AirdropParamsFactory.onVerificationSuccess(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Makes a clear_state call to an existing instance of the Airdrop smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the Airdrop smart contract using the `setMerkleRoot(byte[32])void` ABI method.
     *
    *  @notice Sets the Merkle root for claim validation.
    @dev Only callable by the contract owner.
    @param newMerkleRoot The new Merkle root.

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    setMerkleRoot: async (params: CallParams<AirdropArgs['obj']['setMerkleRoot(byte[32])void'] | AirdropArgs['tuple']['setMerkleRoot(byte[32])void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(AirdropParamsFactory.setMerkleRoot(params))
      return {...result, return: result.return as unknown as (undefined | AirdropReturns['setMerkleRoot(byte[32])void'])}
    },

    /**
     * Makes a call to the Airdrop smart contract using the `openRegistration()void` ABI method.
     *
    *  @notice Opens the registration phase for users.
    @dev Only callable by the contract owner.

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    openRegistration: async (params: CallParams<AirdropArgs['obj']['openRegistration()void'] | AirdropArgs['tuple']['openRegistration()void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(AirdropParamsFactory.openRegistration(params))
      return {...result, return: result.return as unknown as (undefined | AirdropReturns['openRegistration()void'])}
    },

    /**
     * Makes a call to the Airdrop smart contract using the `closeRegistration()void` ABI method.
     *
    *  @notice Closes the registration phase.
    @dev Only callable by the contract owner.

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    closeRegistration: async (params: CallParams<AirdropArgs['obj']['closeRegistration()void'] | AirdropArgs['tuple']['closeRegistration()void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(AirdropParamsFactory.closeRegistration(params))
      return {...result, return: result.return as unknown as (undefined | AirdropReturns['closeRegistration()void'])}
    },

    /**
     * Makes a call to the Airdrop smart contract using the `openClaim()void` ABI method.
     *
    *  @notice Opens the claim phase, allowing registered users to claim tokens.
    @dev Only callable by the contract owner.

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    openClaim: async (params: CallParams<AirdropArgs['obj']['openClaim()void'] | AirdropArgs['tuple']['openClaim()void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(AirdropParamsFactory.openClaim(params))
      return {...result, return: result.return as unknown as (undefined | AirdropReturns['openClaim()void'])}
    },

    /**
     * Makes a call to the Airdrop smart contract using the `closeClaim()void` ABI method.
     *
    *  @notice Closes the claim phase.
    @dev Only callable by the contract owner.

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    closeClaim: async (params: CallParams<AirdropArgs['obj']['closeClaim()void'] | AirdropArgs['tuple']['closeClaim()void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(AirdropParamsFactory.closeClaim(params))
      return {...result, return: result.return as unknown as (undefined | AirdropReturns['closeClaim()void'])}
    },

    /**
     * Makes a call to the Airdrop smart contract using the `getScope()uint512` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
    *  @notice Retrieves the expected proof scope.
    @return The scope value used for registration verification.

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    getScope: async (params: CallParams<AirdropArgs['obj']['getScope()uint512'] | AirdropArgs['tuple']['getScope()uint512']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(AirdropParamsFactory.getScope(params))
      return {...result, return: result.return as unknown as (undefined | AirdropReturns['getScope()uint512'])}
    },

    /**
     * Makes a call to the Airdrop smart contract using the `setConfigId(byte[32])void` ABI method.
     *
    *  @notice Sets the verification config ID
    @dev Only callable by the contract owner
    @param configId The verification config ID to set

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    setConfigId: async (params: CallParams<AirdropArgs['obj']['setConfigId(byte[32])void'] | AirdropArgs['tuple']['setConfigId(byte[32])void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(AirdropParamsFactory.setConfigId(params))
      return {...result, return: result.return as unknown as (undefined | AirdropReturns['setConfigId(byte[32])void'])}
    },

    /**
     * Makes a call to the Airdrop smart contract using the `getConfigId(byte[32],byte[32],byte[])byte[32]` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
    *  @notice Generates a configId for the user
    @dev Override of the SelfVerificationRoot virtual function
    @param destinationChainId The destination chain ID
    @param userIdentifier The user identifier
    @param userDefinedData The user defined data
    @return The stored verification config ID

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    getConfigId: async (params: CallParams<AirdropArgs['obj']['getConfigId(byte[32],byte[32],byte[])byte[32]'] | AirdropArgs['tuple']['getConfigId(byte[32],byte[32],byte[])byte[32]']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(AirdropParamsFactory.getConfigId(params))
      return {...result, return: result.return as unknown as (undefined | AirdropReturns['getConfigId(byte[32],byte[32],byte[])byte[32]'])}
    },

    /**
     * Makes a call to the Airdrop smart contract using the `isRegistered(address)bool` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
    *  @notice Checks if a given address is registered.
    @param registeredAddress The address to check.
    @return True if the address is registered, false otherwise.

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    isRegistered: async (params: CallParams<AirdropArgs['obj']['isRegistered(address)bool'] | AirdropArgs['tuple']['isRegistered(address)bool']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(AirdropParamsFactory.isRegistered(params))
      return {...result, return: result.return as unknown as (undefined | AirdropReturns['isRegistered(address)bool'])}
    },

    /**
     * Makes a call to the Airdrop smart contract using the `claim(uint512,uint512,uint8[32][])void` ABI method.
     *
    *  @notice Allows a registered user to claim their tokens.
    @dev Reverts if registration is still open, if claiming is disabled, if already claimed,
         or if the sender is not registered. Also validates the claim using a Merkle proof.
    @param index The index of the claim in the Merkle tree.
    @param amount The amount of tokens to be claimed.
    @param merkleProof The Merkle proof verifying the claim.

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    claim: async (params: CallParams<AirdropArgs['obj']['claim(uint512,uint512,uint8[32][])void'] | AirdropArgs['tuple']['claim(uint512,uint512,uint8[32][])void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(AirdropParamsFactory.claim(params))
      return {...result, return: result.return as unknown as (undefined | AirdropReturns['claim(uint512,uint512,uint8[32][])void'])}
    },

    /**
     * Makes a call to the Airdrop smart contract using the `owner()address` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     *  @dev Returns the address of the current owner.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    owner: async (params: CallParams<AirdropArgs['obj']['owner()address'] | AirdropArgs['tuple']['owner()address']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(AirdropParamsFactory.owner(params))
      return {...result, return: result.return as unknown as (undefined | AirdropReturns['owner()address'])}
    },

    /**
     * Makes a call to the Airdrop smart contract using the `renounceOwnership()void` ABI method.
     *
    *  @dev Leaves the contract without owner. It will not be possible to call
    `onlyOwner` functions. Can only be called by the current owner.
    NOTE: Renouncing ownership will leave the contract without an owner,
    thereby disabling any functionality that is only available to the owner.

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    renounceOwnership: async (params: CallParams<AirdropArgs['obj']['renounceOwnership()void'] | AirdropArgs['tuple']['renounceOwnership()void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(AirdropParamsFactory.renounceOwnership(params))
      return {...result, return: result.return as unknown as (undefined | AirdropReturns['renounceOwnership()void'])}
    },

    /**
     * Makes a call to the Airdrop smart contract using the `transferOwnership(address)void` ABI method.
     *
    *  @dev Transfers ownership of the contract to a new account (`newOwner`).
    Can only be called by the current owner.

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    transferOwnership: async (params: CallParams<AirdropArgs['obj']['transferOwnership(address)void'] | AirdropArgs['tuple']['transferOwnership(address)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(AirdropParamsFactory.transferOwnership(params))
      return {...result, return: result.return as unknown as (undefined | AirdropReturns['transferOwnership(address)void'])}
    },

    /**
     * Makes a call to the Airdrop smart contract using the `scope()uint512` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
    *  @notice Returns the current scope value
    @dev Public view function to access the current scope setting
    @return The scope value that proofs must match

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    scope: async (params: CallParams<AirdropArgs['obj']['scope()uint512'] | AirdropArgs['tuple']['scope()uint512']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(AirdropParamsFactory.scope(params))
      return {...result, return: result.return as unknown as (undefined | AirdropReturns['scope()uint512'])}
    },

    /**
     * Makes a call to the Airdrop smart contract using the `verifySelfProof(byte[],byte[])void` ABI method.
     *
    *  @notice Verifies a self-proof using the bytes-based interface
    @dev Parses relayer data format and validates against contract settings before calling hub V2
    @param proofPayload Packed data from relayer in format: | 32 bytes attestationId | proof data |
    @param userContextData User-defined data in format: | 32 bytes destChainId | 32 bytes userIdentifier | data |
    @custom:data-format proofPayload = | 32 bytes attestationId | proofData |
    @custom:data-format userContextData = | 32 bytes destChainId | 32 bytes userIdentifier | data |
    @custom:data-format hubData = | 1 bytes contract version | 31 bytes buffer | 32 bytes scope | 32 bytes attestationId | proofData |

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    verifySelfProof: async (params: CallParams<AirdropArgs['obj']['verifySelfProof(byte[],byte[])void'] | AirdropArgs['tuple']['verifySelfProof(byte[],byte[])void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(AirdropParamsFactory.verifySelfProof(params))
      return {...result, return: result.return as unknown as (undefined | AirdropReturns['verifySelfProof(byte[],byte[])void'])}
    },

    /**
     * Makes a call to the Airdrop smart contract using the `onVerificationSuccess(byte[],byte[])void` ABI method.
     *
    *  @notice Callback function called upon successful verification by the hub contract
    @dev Only callable by the identity verification hub V2 contract for security
    @param output The verification output data containing disclosed identity information
    @param userData The user-defined data passed through the verification process
    @custom:security Only the authorized hub contract can call this function
    @custom:flow This function decodes the output and calls the customizable verification hook

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    onVerificationSuccess: async (params: CallParams<AirdropArgs['obj']['onVerificationSuccess(byte[],byte[])void'] | AirdropArgs['tuple']['onVerificationSuccess(byte[],byte[])void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(AirdropParamsFactory.onVerificationSuccess(params))
      return {...result, return: result.return as unknown as (undefined | AirdropReturns['onVerificationSuccess(byte[],byte[])void'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new AirdropClient(this.appClient.clone(params))
  }

  /**
   * Makes a readonly (simulated) call to the Airdrop smart contract using the `getScope()uint512` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
  *  @notice Retrieves the expected proof scope.
  @return The scope value used for registration verification.

   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async getScope(params: CallParams<AirdropArgs['obj']['getScope()uint512'] | AirdropArgs['tuple']['getScope()uint512']> = {args: []}) {
    const result = await this.appClient.send.call(AirdropParamsFactory.getScope(params))
    return result.return as unknown as AirdropReturns['getScope()uint512']
  }

  /**
   * Makes a readonly (simulated) call to the Airdrop smart contract using the `getConfigId(byte[32],byte[32],byte[])byte[32]` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
  *  @notice Generates a configId for the user
  @dev Override of the SelfVerificationRoot virtual function
  @param destinationChainId The destination chain ID
  @param userIdentifier The user identifier
  @param userDefinedData The user defined data
  @return The stored verification config ID

   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async getConfigId(params: CallParams<AirdropArgs['obj']['getConfigId(byte[32],byte[32],byte[])byte[32]'] | AirdropArgs['tuple']['getConfigId(byte[32],byte[32],byte[])byte[32]']>) {
    const result = await this.appClient.send.call(AirdropParamsFactory.getConfigId(params))
    return result.return as unknown as AirdropReturns['getConfigId(byte[32],byte[32],byte[])byte[32]']
  }

  /**
   * Makes a readonly (simulated) call to the Airdrop smart contract using the `isRegistered(address)bool` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
  *  @notice Checks if a given address is registered.
  @param registeredAddress The address to check.
  @return True if the address is registered, false otherwise.

   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async isRegistered(params: CallParams<AirdropArgs['obj']['isRegistered(address)bool'] | AirdropArgs['tuple']['isRegistered(address)bool']>) {
    const result = await this.appClient.send.call(AirdropParamsFactory.isRegistered(params))
    return result.return as unknown as AirdropReturns['isRegistered(address)bool']
  }

  /**
   * Makes a readonly (simulated) call to the Airdrop smart contract using the `owner()address` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   *  @dev Returns the address of the current owner.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async owner(params: CallParams<AirdropArgs['obj']['owner()address'] | AirdropArgs['tuple']['owner()address']> = {args: []}) {
    const result = await this.appClient.send.call(AirdropParamsFactory.owner(params))
    return result.return as unknown as AirdropReturns['owner()address']
  }

  /**
   * Makes a readonly (simulated) call to the Airdrop smart contract using the `scope()uint512` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
  *  @notice Returns the current scope value
  @dev Public view function to access the current scope setting
  @return The scope value that proofs must match

   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async scope(params: CallParams<AirdropArgs['obj']['scope()uint512'] | AirdropArgs['tuple']['scope()uint512']> = {args: []}) {
    const result = await this.appClient.send.call(AirdropParamsFactory.scope(params))
    return result.return as unknown as AirdropReturns['scope()uint512']
  }

  /**
   * Methods to access state for the current Airdrop app
   */
  state = {
    /**
     * Methods to access global state for the current Airdrop app
     */
    global: {
      /**
       * Get all current keyed values from global state
       */
      getAll: async (): Promise<Partial<Expand<GlobalKeysState>>> => {
        const result = await this.appClient.state.global.getAll()
        return {
          token: result.token,
          merkleRoot: new BinaryStateValue(result.merkleRoot),
          isRegistrationOpen: result.isRegistrationOpen,
          isClaimOpen: result.isClaimOpen,
          verificationConfigId: new BinaryStateValue(result.verificationConfigId),
          _owner: result._owner,
          _scope: new BinaryStateValue(result._scope),
          _identityVerificationHubV2: result._identityVerificationHubV2,
        }
      },
      /**
       * Get the current value of the token key in global state
       */
      token: async (): Promise<string | undefined> => { return (await this.appClient.state.global.getValue("token")) as string | undefined },
      /**
       * Get the current value of the merkleRoot key in global state
       */
      merkleRoot: async (): Promise<BinaryState> => { return new BinaryStateValue((await this.appClient.state.global.getValue("merkleRoot")) as Uint8Array | undefined) },
      /**
       * Get the current value of the isRegistrationOpen key in global state
       */
      isRegistrationOpen: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("isRegistrationOpen")) as bigint | undefined },
      /**
       * Get the current value of the isClaimOpen key in global state
       */
      isClaimOpen: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("isClaimOpen")) as bigint | undefined },
      /**
       * Get the current value of the verificationConfigId key in global state
       */
      verificationConfigId: async (): Promise<BinaryState> => { return new BinaryStateValue((await this.appClient.state.global.getValue("verificationConfigId")) as Uint8Array | undefined) },
      /**
       * Get the current value of the _owner key in global state
       */
      _owner: async (): Promise<string | undefined> => { return (await this.appClient.state.global.getValue("_owner")) as string | undefined },
      /**
       * Get the current value of the _scope key in global state
       */
      _scope: async (): Promise<BinaryState> => { return new BinaryStateValue((await this.appClient.state.global.getValue("_scope")) as Uint8Array | undefined) },
      /**
       * Get the current value of the _identityVerificationHubV2 key in global state
       */
      _identityVerificationHubV2: async (): Promise<string | undefined> => { return (await this.appClient.state.global.getValue("_identityVerificationHubV2")) as string | undefined },
    },
    /**
     * Methods to access box state for the current Airdrop app
     */
    box: {
      /**
       * Get all current keyed values from box state
       */
      getAll: async (): Promise<Partial<Expand<BoxKeysState>>> => {
        const result = await this.appClient.state.box.getAll()
        return {
        }
      },
      /**
       * Get values from the claimed map in box state
       */
      claimed: {
        /**
         * Get all current values of the claimed map in box state
         */
        getMap: async (): Promise<Map<Uint8Array, bigint>> => { return (await this.appClient.state.box.getMap("claimed")) as Map<Uint8Array, bigint> },
        /**
         * Get a current value of the claimed map by key from box state
         */
        value: async (key: Uint8Array | string): Promise<bigint | undefined> => { return await this.appClient.state.box.getMapValue("claimed", key) as bigint | undefined },
      },
      /**
       * Get values from the _nullifierToUserIdentifier map in box state
       */
      _nullifierToUserIdentifier: {
        /**
         * Get all current values of the _nullifierToUserIdentifier map in box state
         */
        getMap: async (): Promise<Map<Uint8Array, Uint8Array>> => { return (await this.appClient.state.box.getMap("_nullifierToUserIdentifier")) as Map<Uint8Array, Uint8Array> },
        /**
         * Get a current value of the _nullifierToUserIdentifier map by key from box state
         */
        value: async (key: Uint8Array | string): Promise<Uint8Array | undefined> => { return await this.appClient.state.box.getMapValue("_nullifierToUserIdentifier", key) as Uint8Array | undefined },
      },
      /**
       * Get values from the _registeredUserIdentifiers map in box state
       */
      _registeredUserIdentifiers: {
        /**
         * Get all current values of the _registeredUserIdentifiers map in box state
         */
        getMap: async (): Promise<Map<Uint8Array, bigint>> => { return (await this.appClient.state.box.getMap("_registeredUserIdentifiers")) as Map<Uint8Array, bigint> },
        /**
         * Get a current value of the _registeredUserIdentifiers map by key from box state
         */
        value: async (key: Uint8Array | string): Promise<bigint | undefined> => { return await this.appClient.state.box.getMapValue("_registeredUserIdentifiers", key) as bigint | undefined },
      },
    },
  }

  public newGroup(): AirdropComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a setMerkleRoot(byte[32])void method call against the Airdrop contract
       */
      setMerkleRoot(params: CallParams<AirdropArgs['obj']['setMerkleRoot(byte[32])void'] | AirdropArgs['tuple']['setMerkleRoot(byte[32])void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.setMerkleRoot(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a openRegistration()void method call against the Airdrop contract
       */
      openRegistration(params: CallParams<AirdropArgs['obj']['openRegistration()void'] | AirdropArgs['tuple']['openRegistration()void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.openRegistration(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a closeRegistration()void method call against the Airdrop contract
       */
      closeRegistration(params: CallParams<AirdropArgs['obj']['closeRegistration()void'] | AirdropArgs['tuple']['closeRegistration()void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.closeRegistration(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a openClaim()void method call against the Airdrop contract
       */
      openClaim(params: CallParams<AirdropArgs['obj']['openClaim()void'] | AirdropArgs['tuple']['openClaim()void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.openClaim(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a closeClaim()void method call against the Airdrop contract
       */
      closeClaim(params: CallParams<AirdropArgs['obj']['closeClaim()void'] | AirdropArgs['tuple']['closeClaim()void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.closeClaim(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a getScope()uint512 method call against the Airdrop contract
       */
      getScope(params: CallParams<AirdropArgs['obj']['getScope()uint512'] | AirdropArgs['tuple']['getScope()uint512']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.getScope(params)))
        resultMappers.push((v) => client.decodeReturnValue('getScope()uint512', v))
        return this
      },
      /**
       * Add a setConfigId(byte[32])void method call against the Airdrop contract
       */
      setConfigId(params: CallParams<AirdropArgs['obj']['setConfigId(byte[32])void'] | AirdropArgs['tuple']['setConfigId(byte[32])void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.setConfigId(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a getConfigId(byte[32],byte[32],byte[])byte[32] method call against the Airdrop contract
       */
      getConfigId(params: CallParams<AirdropArgs['obj']['getConfigId(byte[32],byte[32],byte[])byte[32]'] | AirdropArgs['tuple']['getConfigId(byte[32],byte[32],byte[])byte[32]']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.getConfigId(params)))
        resultMappers.push((v) => client.decodeReturnValue('getConfigId(byte[32],byte[32],byte[])byte[32]', v))
        return this
      },
      /**
       * Add a isRegistered(address)bool method call against the Airdrop contract
       */
      isRegistered(params: CallParams<AirdropArgs['obj']['isRegistered(address)bool'] | AirdropArgs['tuple']['isRegistered(address)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.isRegistered(params)))
        resultMappers.push((v) => client.decodeReturnValue('isRegistered(address)bool', v))
        return this
      },
      /**
       * Add a claim(uint512,uint512,uint8[32][])void method call against the Airdrop contract
       */
      claim(params: CallParams<AirdropArgs['obj']['claim(uint512,uint512,uint8[32][])void'] | AirdropArgs['tuple']['claim(uint512,uint512,uint8[32][])void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.claim(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a owner()address method call against the Airdrop contract
       */
      owner(params: CallParams<AirdropArgs['obj']['owner()address'] | AirdropArgs['tuple']['owner()address']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.owner(params)))
        resultMappers.push((v) => client.decodeReturnValue('owner()address', v))
        return this
      },
      /**
       * Add a renounceOwnership()void method call against the Airdrop contract
       */
      renounceOwnership(params: CallParams<AirdropArgs['obj']['renounceOwnership()void'] | AirdropArgs['tuple']['renounceOwnership()void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.renounceOwnership(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a transferOwnership(address)void method call against the Airdrop contract
       */
      transferOwnership(params: CallParams<AirdropArgs['obj']['transferOwnership(address)void'] | AirdropArgs['tuple']['transferOwnership(address)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.transferOwnership(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a scope()uint512 method call against the Airdrop contract
       */
      scope(params: CallParams<AirdropArgs['obj']['scope()uint512'] | AirdropArgs['tuple']['scope()uint512']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.scope(params)))
        resultMappers.push((v) => client.decodeReturnValue('scope()uint512', v))
        return this
      },
      /**
       * Add a verifySelfProof(byte[],byte[])void method call against the Airdrop contract
       */
      verifySelfProof(params: CallParams<AirdropArgs['obj']['verifySelfProof(byte[],byte[])void'] | AirdropArgs['tuple']['verifySelfProof(byte[],byte[])void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.verifySelfProof(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a onVerificationSuccess(byte[],byte[])void method call against the Airdrop contract
       */
      onVerificationSuccess(params: CallParams<AirdropArgs['obj']['onVerificationSuccess(byte[],byte[])void'] | AirdropArgs['tuple']['onVerificationSuccess(byte[],byte[])void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.onVerificationSuccess(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a clear state call to the Airdrop contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as AirdropComposer
  }
}
export type AirdropComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the setMerkleRoot(byte[32])void ABI method.
   *
  *  @notice Sets the Merkle root for claim validation.
  @dev Only callable by the contract owner.
  @param newMerkleRoot The new Merkle root.

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  setMerkleRoot(params?: CallParams<AirdropArgs['obj']['setMerkleRoot(byte[32])void'] | AirdropArgs['tuple']['setMerkleRoot(byte[32])void']>): AirdropComposer<[...TReturns, AirdropReturns['setMerkleRoot(byte[32])void'] | undefined]>

  /**
   * Calls the openRegistration()void ABI method.
   *
  *  @notice Opens the registration phase for users.
  @dev Only callable by the contract owner.

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  openRegistration(params?: CallParams<AirdropArgs['obj']['openRegistration()void'] | AirdropArgs['tuple']['openRegistration()void']>): AirdropComposer<[...TReturns, AirdropReturns['openRegistration()void'] | undefined]>

  /**
   * Calls the closeRegistration()void ABI method.
   *
  *  @notice Closes the registration phase.
  @dev Only callable by the contract owner.

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  closeRegistration(params?: CallParams<AirdropArgs['obj']['closeRegistration()void'] | AirdropArgs['tuple']['closeRegistration()void']>): AirdropComposer<[...TReturns, AirdropReturns['closeRegistration()void'] | undefined]>

  /**
   * Calls the openClaim()void ABI method.
   *
  *  @notice Opens the claim phase, allowing registered users to claim tokens.
  @dev Only callable by the contract owner.

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  openClaim(params?: CallParams<AirdropArgs['obj']['openClaim()void'] | AirdropArgs['tuple']['openClaim()void']>): AirdropComposer<[...TReturns, AirdropReturns['openClaim()void'] | undefined]>

  /**
   * Calls the closeClaim()void ABI method.
   *
  *  @notice Closes the claim phase.
  @dev Only callable by the contract owner.

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  closeClaim(params?: CallParams<AirdropArgs['obj']['closeClaim()void'] | AirdropArgs['tuple']['closeClaim()void']>): AirdropComposer<[...TReturns, AirdropReturns['closeClaim()void'] | undefined]>

  /**
   * Calls the getScope()uint512 ABI method.
   *
  *  @notice Retrieves the expected proof scope.
  @return The scope value used for registration verification.

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  getScope(params?: CallParams<AirdropArgs['obj']['getScope()uint512'] | AirdropArgs['tuple']['getScope()uint512']>): AirdropComposer<[...TReturns, AirdropReturns['getScope()uint512'] | undefined]>

  /**
   * Calls the setConfigId(byte[32])void ABI method.
   *
  *  @notice Sets the verification config ID
  @dev Only callable by the contract owner
  @param configId The verification config ID to set

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  setConfigId(params?: CallParams<AirdropArgs['obj']['setConfigId(byte[32])void'] | AirdropArgs['tuple']['setConfigId(byte[32])void']>): AirdropComposer<[...TReturns, AirdropReturns['setConfigId(byte[32])void'] | undefined]>

  /**
   * Calls the getConfigId(byte[32],byte[32],byte[])byte[32] ABI method.
   *
  *  @notice Generates a configId for the user
  @dev Override of the SelfVerificationRoot virtual function
  @param destinationChainId The destination chain ID
  @param userIdentifier The user identifier
  @param userDefinedData The user defined data
  @return The stored verification config ID

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  getConfigId(params?: CallParams<AirdropArgs['obj']['getConfigId(byte[32],byte[32],byte[])byte[32]'] | AirdropArgs['tuple']['getConfigId(byte[32],byte[32],byte[])byte[32]']>): AirdropComposer<[...TReturns, AirdropReturns['getConfigId(byte[32],byte[32],byte[])byte[32]'] | undefined]>

  /**
   * Calls the isRegistered(address)bool ABI method.
   *
  *  @notice Checks if a given address is registered.
  @param registeredAddress The address to check.
  @return True if the address is registered, false otherwise.

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  isRegistered(params?: CallParams<AirdropArgs['obj']['isRegistered(address)bool'] | AirdropArgs['tuple']['isRegistered(address)bool']>): AirdropComposer<[...TReturns, AirdropReturns['isRegistered(address)bool'] | undefined]>

  /**
   * Calls the claim(uint512,uint512,uint8[32][])void ABI method.
   *
  *  @notice Allows a registered user to claim their tokens.
  @dev Reverts if registration is still open, if claiming is disabled, if already claimed,
       or if the sender is not registered. Also validates the claim using a Merkle proof.
  @param index The index of the claim in the Merkle tree.
  @param amount The amount of tokens to be claimed.
  @param merkleProof The Merkle proof verifying the claim.

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  claim(params?: CallParams<AirdropArgs['obj']['claim(uint512,uint512,uint8[32][])void'] | AirdropArgs['tuple']['claim(uint512,uint512,uint8[32][])void']>): AirdropComposer<[...TReturns, AirdropReturns['claim(uint512,uint512,uint8[32][])void'] | undefined]>

  /**
   * Calls the owner()address ABI method.
   *
   *  @dev Returns the address of the current owner.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  owner(params?: CallParams<AirdropArgs['obj']['owner()address'] | AirdropArgs['tuple']['owner()address']>): AirdropComposer<[...TReturns, AirdropReturns['owner()address'] | undefined]>

  /**
   * Calls the renounceOwnership()void ABI method.
   *
  *  @dev Leaves the contract without owner. It will not be possible to call
  `onlyOwner` functions. Can only be called by the current owner.
  NOTE: Renouncing ownership will leave the contract without an owner,
  thereby disabling any functionality that is only available to the owner.

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  renounceOwnership(params?: CallParams<AirdropArgs['obj']['renounceOwnership()void'] | AirdropArgs['tuple']['renounceOwnership()void']>): AirdropComposer<[...TReturns, AirdropReturns['renounceOwnership()void'] | undefined]>

  /**
   * Calls the transferOwnership(address)void ABI method.
   *
  *  @dev Transfers ownership of the contract to a new account (`newOwner`).
  Can only be called by the current owner.

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  transferOwnership(params?: CallParams<AirdropArgs['obj']['transferOwnership(address)void'] | AirdropArgs['tuple']['transferOwnership(address)void']>): AirdropComposer<[...TReturns, AirdropReturns['transferOwnership(address)void'] | undefined]>

  /**
   * Calls the scope()uint512 ABI method.
   *
  *  @notice Returns the current scope value
  @dev Public view function to access the current scope setting
  @return The scope value that proofs must match

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  scope(params?: CallParams<AirdropArgs['obj']['scope()uint512'] | AirdropArgs['tuple']['scope()uint512']>): AirdropComposer<[...TReturns, AirdropReturns['scope()uint512'] | undefined]>

  /**
   * Calls the verifySelfProof(byte[],byte[])void ABI method.
   *
  *  @notice Verifies a self-proof using the bytes-based interface
  @dev Parses relayer data format and validates against contract settings before calling hub V2
  @param proofPayload Packed data from relayer in format: | 32 bytes attestationId | proof data |
  @param userContextData User-defined data in format: | 32 bytes destChainId | 32 bytes userIdentifier | data |
  @custom:data-format proofPayload = | 32 bytes attestationId | proofData |
  @custom:data-format userContextData = | 32 bytes destChainId | 32 bytes userIdentifier | data |
  @custom:data-format hubData = | 1 bytes contract version | 31 bytes buffer | 32 bytes scope | 32 bytes attestationId | proofData |

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  verifySelfProof(params?: CallParams<AirdropArgs['obj']['verifySelfProof(byte[],byte[])void'] | AirdropArgs['tuple']['verifySelfProof(byte[],byte[])void']>): AirdropComposer<[...TReturns, AirdropReturns['verifySelfProof(byte[],byte[])void'] | undefined]>

  /**
   * Calls the onVerificationSuccess(byte[],byte[])void ABI method.
   *
  *  @notice Callback function called upon successful verification by the hub contract
  @dev Only callable by the identity verification hub V2 contract for security
  @param output The verification output data containing disclosed identity information
  @param userData The user-defined data passed through the verification process
  @custom:security Only the authorized hub contract can call this function
  @custom:flow This function decodes the output and calls the customizable verification hook

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  onVerificationSuccess(params?: CallParams<AirdropArgs['obj']['onVerificationSuccess(byte[],byte[])void'] | AirdropArgs['tuple']['onVerificationSuccess(byte[],byte[])void']>): AirdropComposer<[...TReturns, AirdropReturns['onVerificationSuccess(byte[],byte[])void'] | undefined]>

  /**
   * Makes a clear_state call to an existing instance of the Airdrop smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): AirdropComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): AirdropComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<AirdropComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<AirdropComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<AirdropComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<AirdropComposerResults<TReturns>>
}
export type AirdropComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

