/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"CTokenStorage","structs":{"BorrowSnapshot":[{"name":"principal","type":"uint256"},{"name":"interestIndex","type":"uint256"}]},"methods":[{"name":"name","args":[],"returns":{"type":"string"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"symbol","args":[],"returns":{"type":"string"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"decimals","args":[],"returns":{"type":"uint64"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"admin","args":[],"returns":{"type":"address"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"pendingAdmin","args":[],"returns":{"type":"address"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"comptroller","args":[],"returns":{"type":"address"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"interestRateModel","args":[],"returns":{"type":"address"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"reserveFactorMantissa","args":[],"returns":{"type":"uint256"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"accrualBlockNumber","args":[],"returns":{"type":"uint256"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"borrowIndex","args":[],"returns":{"type":"uint256"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"totalBorrows","args":[],"returns":{"type":"uint256"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"totalReserves","args":[],"returns":{"type":"uint256"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"totalSupply","args":[],"returns":{"type":"uint256"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}}],"arcs":[22,28],"networks":{},"state":{"schema":{"global":{"ints":2,"bytes":13},"local":{"ints":0,"bytes":0}},"keys":{"global":{"_notEntered":{"keyType":"AVMString","valueType":"AVMUint64","key":"X25vdEVudGVyZWQ="},"name":{"keyType":"AVMString","valueType":"AVMString","key":"bmFtZQ=="},"symbol":{"keyType":"AVMString","valueType":"AVMString","key":"c3ltYm9s"},"decimals":{"keyType":"AVMString","valueType":"AVMUint64","key":"ZGVjaW1hbHM="},"admin":{"keyType":"AVMString","valueType":"address","key":"YWRtaW4="},"pendingAdmin":{"keyType":"AVMString","valueType":"address","key":"cGVuZGluZ0FkbWlu"},"comptroller":{"keyType":"AVMString","valueType":"address","key":"Y29tcHRyb2xsZXI="},"interestRateModel":{"keyType":"AVMString","valueType":"address","key":"aW50ZXJlc3RSYXRlTW9kZWw="},"initialExchangeRateMantissa":{"keyType":"AVMString","valueType":"AVMBytes","key":"aW5pdGlhbEV4Y2hhbmdlUmF0ZU1hbnRpc3Nh"},"reserveFactorMantissa":{"keyType":"AVMString","valueType":"AVMBytes","key":"cmVzZXJ2ZUZhY3Rvck1hbnRpc3Nh"},"accrualBlockNumber":{"keyType":"AVMString","valueType":"AVMBytes","key":"YWNjcnVhbEJsb2NrTnVtYmVy"},"borrowIndex":{"keyType":"AVMString","valueType":"AVMBytes","key":"Ym9ycm93SW5kZXg="},"totalBorrows":{"keyType":"AVMString","valueType":"AVMBytes","key":"dG90YWxCb3Jyb3dz"},"totalReserves":{"keyType":"AVMString","valueType":"AVMBytes","key":"dG90YWxSZXNlcnZlcw=="},"totalSupply":{"keyType":"AVMString","valueType":"AVMBytes","key":"dG90YWxTdXBwbHk="}},"local":{},"box":{}},"maps":{"global":{},"local":{},"box":{"accountTokens":{"keyType":"AVMBytes","valueType":"AVMBytes","prefix":"YWNjb3VudFRva2Vucw=="},"transferAllowances":{"keyType":"AVMBytes","valueType":"AVMBytes","prefix":"dHJhbnNmZXJBbGxvd2FuY2Vz"},"accountBorrows":{"keyType":"AVMBytes","valueType":"BorrowSnapshot","prefix":"YWNjb3VudEJvcnJvd3M="}}}},"bareActions":{"create":[],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[535],"errorMessage":"check accrualBlockNumber exists"},{"pc":[472],"errorMessage":"check admin exists"},{"pc":[554],"errorMessage":"check borrowIndex exists"},{"pc":[494],"errorMessage":"check comptroller exists"},{"pc":[460],"errorMessage":"check decimals exists"},{"pc":[505],"errorMessage":"check interestRateModel exists"},{"pc":[422],"errorMessage":"check name exists"},{"pc":[483],"errorMessage":"check pendingAdmin exists"},{"pc":[516],"errorMessage":"check reserveFactorMantissa exists"},{"pc":[441],"errorMessage":"check symbol exists"},{"pc":[573],"errorMessage":"check totalBorrows exists"},{"pc":[592],"errorMessage":"check totalReserves exists"},{"pc":[611],"errorMessage":"check totalSupply exists"},{"pc":[521,540,559,578,597,616],"errorMessage":"overflow"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"events":[]} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


// Type definitions for ARC-56 structs

export type BorrowSnapshot = {
  principal: bigint,
  interestIndex: bigint
}


/**
 * Converts the ABI tuple representation of a BorrowSnapshot to the struct representation
 */
export function BorrowSnapshotFromTuple(abiTuple: [bigint, bigint]) {
  return getABIStructFromABITuple(abiTuple, APP_SPEC.structs.BorrowSnapshot, APP_SPEC.structs) as BorrowSnapshot
}

/**
 * The argument types for the CTokenStorage contract
 */
export type CTokenStorageArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'name()string': Record<string, never>
    'symbol()string': Record<string, never>
    'decimals()uint64': Record<string, never>
    'admin()address': Record<string, never>
    'pendingAdmin()address': Record<string, never>
    'comptroller()address': Record<string, never>
    'interestRateModel()address': Record<string, never>
    'reserveFactorMantissa()uint256': Record<string, never>
    'accrualBlockNumber()uint256': Record<string, never>
    'borrowIndex()uint256': Record<string, never>
    'totalBorrows()uint256': Record<string, never>
    'totalReserves()uint256': Record<string, never>
    'totalSupply()uint256': Record<string, never>
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'name()string': []
    'symbol()string': []
    'decimals()uint64': []
    'admin()address': []
    'pendingAdmin()address': []
    'comptroller()address': []
    'interestRateModel()address': []
    'reserveFactorMantissa()uint256': []
    'accrualBlockNumber()uint256': []
    'borrowIndex()uint256': []
    'totalBorrows()uint256': []
    'totalReserves()uint256': []
    'totalSupply()uint256': []
  }
}

/**
 * The return type for each method
 */
export type CTokenStorageReturns = {
  'name()string': string
  'symbol()string': string
  'decimals()uint64': bigint
  'admin()address': string
  'pendingAdmin()address': string
  'comptroller()address': string
  'interestRateModel()address': string
  'reserveFactorMantissa()uint256': bigint
  'accrualBlockNumber()uint256': bigint
  'borrowIndex()uint256': bigint
  'totalBorrows()uint256': bigint
  'totalReserves()uint256': bigint
  'totalSupply()uint256': bigint
}

/**
 * Defines the types of available calls and state of the CTokenStorage smart contract.
 */
export type CTokenStorageTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'name()string' | 'name', {
      argsObj: CTokenStorageArgs['obj']['name()string']
      argsTuple: CTokenStorageArgs['tuple']['name()string']
      returns: CTokenStorageReturns['name()string']
    }>
    & Record<'symbol()string' | 'symbol', {
      argsObj: CTokenStorageArgs['obj']['symbol()string']
      argsTuple: CTokenStorageArgs['tuple']['symbol()string']
      returns: CTokenStorageReturns['symbol()string']
    }>
    & Record<'decimals()uint64' | 'decimals', {
      argsObj: CTokenStorageArgs['obj']['decimals()uint64']
      argsTuple: CTokenStorageArgs['tuple']['decimals()uint64']
      returns: CTokenStorageReturns['decimals()uint64']
    }>
    & Record<'admin()address' | 'admin', {
      argsObj: CTokenStorageArgs['obj']['admin()address']
      argsTuple: CTokenStorageArgs['tuple']['admin()address']
      returns: CTokenStorageReturns['admin()address']
    }>
    & Record<'pendingAdmin()address' | 'pendingAdmin', {
      argsObj: CTokenStorageArgs['obj']['pendingAdmin()address']
      argsTuple: CTokenStorageArgs['tuple']['pendingAdmin()address']
      returns: CTokenStorageReturns['pendingAdmin()address']
    }>
    & Record<'comptroller()address' | 'comptroller', {
      argsObj: CTokenStorageArgs['obj']['comptroller()address']
      argsTuple: CTokenStorageArgs['tuple']['comptroller()address']
      returns: CTokenStorageReturns['comptroller()address']
    }>
    & Record<'interestRateModel()address' | 'interestRateModel', {
      argsObj: CTokenStorageArgs['obj']['interestRateModel()address']
      argsTuple: CTokenStorageArgs['tuple']['interestRateModel()address']
      returns: CTokenStorageReturns['interestRateModel()address']
    }>
    & Record<'reserveFactorMantissa()uint256' | 'reserveFactorMantissa', {
      argsObj: CTokenStorageArgs['obj']['reserveFactorMantissa()uint256']
      argsTuple: CTokenStorageArgs['tuple']['reserveFactorMantissa()uint256']
      returns: CTokenStorageReturns['reserveFactorMantissa()uint256']
    }>
    & Record<'accrualBlockNumber()uint256' | 'accrualBlockNumber', {
      argsObj: CTokenStorageArgs['obj']['accrualBlockNumber()uint256']
      argsTuple: CTokenStorageArgs['tuple']['accrualBlockNumber()uint256']
      returns: CTokenStorageReturns['accrualBlockNumber()uint256']
    }>
    & Record<'borrowIndex()uint256' | 'borrowIndex', {
      argsObj: CTokenStorageArgs['obj']['borrowIndex()uint256']
      argsTuple: CTokenStorageArgs['tuple']['borrowIndex()uint256']
      returns: CTokenStorageReturns['borrowIndex()uint256']
    }>
    & Record<'totalBorrows()uint256' | 'totalBorrows', {
      argsObj: CTokenStorageArgs['obj']['totalBorrows()uint256']
      argsTuple: CTokenStorageArgs['tuple']['totalBorrows()uint256']
      returns: CTokenStorageReturns['totalBorrows()uint256']
    }>
    & Record<'totalReserves()uint256' | 'totalReserves', {
      argsObj: CTokenStorageArgs['obj']['totalReserves()uint256']
      argsTuple: CTokenStorageArgs['tuple']['totalReserves()uint256']
      returns: CTokenStorageReturns['totalReserves()uint256']
    }>
    & Record<'totalSupply()uint256' | 'totalSupply', {
      argsObj: CTokenStorageArgs['obj']['totalSupply()uint256']
      argsTuple: CTokenStorageArgs['tuple']['totalSupply()uint256']
      returns: CTokenStorageReturns['totalSupply()uint256']
    }>
  /**
   * Defines the shape of the state of the application.
   */
  state: {
    global: {
      keys: {
        _notEntered: bigint
        name: string
        symbol: string
        decimals: bigint
        admin: string
        pendingAdmin: string
        comptroller: string
        interestRateModel: string
        initialExchangeRateMantissa: BinaryState
        reserveFactorMantissa: BinaryState
        accrualBlockNumber: BinaryState
        borrowIndex: BinaryState
        totalBorrows: BinaryState
        totalReserves: BinaryState
        totalSupply: BinaryState
      }
      maps: {}
    }
    box: {
      keys: {}
      maps: {
        accountTokens: Map<Uint8Array | string, Uint8Array>
        transferAllowances: Map<Uint8Array | string, Uint8Array>
        accountBorrows: Map<Uint8Array | string, BorrowSnapshot>
      }
    }
  }
}

/**
 * Defines the possible abi call signatures.
 */
export type CTokenStorageSignatures = keyof CTokenStorageTypes['methods']
/**
 * Defines the possible abi call signatures for methods that return a non-void value.
 */
export type CTokenStorageNonVoidMethodSignatures = keyof CTokenStorageTypes['methods'] extends infer T ? T extends keyof CTokenStorageTypes['methods'] ? MethodReturn<T> extends void ? never : T  : never : never
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the CTokenStorage smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends CTokenStorageSignatures> = CTokenStorageTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the CTokenStorage smart contract to the method's return type
 */
export type MethodReturn<TSignature extends CTokenStorageSignatures> = CTokenStorageTypes['methods'][TSignature]['returns']

/**
 * Defines the shape of the keyed global state of the application.
 */
export type GlobalKeysState = CTokenStorageTypes['state']['global']['keys']

/**
 * Defines the shape of the keyed box state of the application.
 */
export type BoxKeysState = CTokenStorageTypes['state']['box']['keys']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the CTokenStorage smart contract
 */
export abstract class CTokenStorageParamsFactory {
  /**
   * Constructs a no op call for the name()string ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static name(params: CallParams<CTokenStorageArgs['obj']['name()string'] | CTokenStorageArgs['tuple']['name()string']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'name()string' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the symbol()string ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static symbol(params: CallParams<CTokenStorageArgs['obj']['symbol()string'] | CTokenStorageArgs['tuple']['symbol()string']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'symbol()string' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the decimals()uint64 ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static decimals(params: CallParams<CTokenStorageArgs['obj']['decimals()uint64'] | CTokenStorageArgs['tuple']['decimals()uint64']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'decimals()uint64' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the admin()address ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static admin(params: CallParams<CTokenStorageArgs['obj']['admin()address'] | CTokenStorageArgs['tuple']['admin()address']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'admin()address' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the pendingAdmin()address ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static pendingAdmin(params: CallParams<CTokenStorageArgs['obj']['pendingAdmin()address'] | CTokenStorageArgs['tuple']['pendingAdmin()address']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'pendingAdmin()address' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the comptroller()address ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static comptroller(params: CallParams<CTokenStorageArgs['obj']['comptroller()address'] | CTokenStorageArgs['tuple']['comptroller()address']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'comptroller()address' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the interestRateModel()address ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static interestRateModel(params: CallParams<CTokenStorageArgs['obj']['interestRateModel()address'] | CTokenStorageArgs['tuple']['interestRateModel()address']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'interestRateModel()address' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the reserveFactorMantissa()uint256 ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static reserveFactorMantissa(params: CallParams<CTokenStorageArgs['obj']['reserveFactorMantissa()uint256'] | CTokenStorageArgs['tuple']['reserveFactorMantissa()uint256']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'reserveFactorMantissa()uint256' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the accrualBlockNumber()uint256 ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static accrualBlockNumber(params: CallParams<CTokenStorageArgs['obj']['accrualBlockNumber()uint256'] | CTokenStorageArgs['tuple']['accrualBlockNumber()uint256']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'accrualBlockNumber()uint256' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the borrowIndex()uint256 ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static borrowIndex(params: CallParams<CTokenStorageArgs['obj']['borrowIndex()uint256'] | CTokenStorageArgs['tuple']['borrowIndex()uint256']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'borrowIndex()uint256' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the totalBorrows()uint256 ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static totalBorrows(params: CallParams<CTokenStorageArgs['obj']['totalBorrows()uint256'] | CTokenStorageArgs['tuple']['totalBorrows()uint256']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'totalBorrows()uint256' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the totalReserves()uint256 ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static totalReserves(params: CallParams<CTokenStorageArgs['obj']['totalReserves()uint256'] | CTokenStorageArgs['tuple']['totalReserves()uint256']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'totalReserves()uint256' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the totalSupply()uint256 ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static totalSupply(params: CallParams<CTokenStorageArgs['obj']['totalSupply()uint256'] | CTokenStorageArgs['tuple']['totalSupply()uint256']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'totalSupply()uint256' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
}

/**
 * A client to make calls to the CTokenStorage smart contract
 */
export class CTokenStorageClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `CTokenStorageClient`
   *
   * @param appClient An `AppClient` instance which has been created with the CTokenStorage app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `CTokenStorageClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Checks for decode errors on the given return value and maps the return value to the return type for the given method
   * @returns The typed return value or undefined if there was no value
   */
  decodeReturnValue<TSignature extends CTokenStorageNonVoidMethodSignatures>(method: TSignature, returnValue: ABIReturn | undefined) {
    return returnValue !== undefined ? getArc56ReturnValue<MethodReturn<TSignature>>(returnValue, this.appClient.getABIMethod(method), APP_SPEC.structs) : undefined
  }

  /**
   * Returns a new `CTokenStorageClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<CTokenStorageClient> {
    return new CTokenStorageClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `CTokenStorageClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<CTokenStorageClient> {
    return new CTokenStorageClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Makes a clear_state call to an existing instance of the CTokenStorage smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the CTokenStorage smart contract using the `name()string` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    name: (params: CallParams<CTokenStorageArgs['obj']['name()string'] | CTokenStorageArgs['tuple']['name()string']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(CTokenStorageParamsFactory.name(params))
    },

    /**
     * Makes a call to the CTokenStorage smart contract using the `symbol()string` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    symbol: (params: CallParams<CTokenStorageArgs['obj']['symbol()string'] | CTokenStorageArgs['tuple']['symbol()string']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(CTokenStorageParamsFactory.symbol(params))
    },

    /**
     * Makes a call to the CTokenStorage smart contract using the `decimals()uint64` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    decimals: (params: CallParams<CTokenStorageArgs['obj']['decimals()uint64'] | CTokenStorageArgs['tuple']['decimals()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(CTokenStorageParamsFactory.decimals(params))
    },

    /**
     * Makes a call to the CTokenStorage smart contract using the `admin()address` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    admin: (params: CallParams<CTokenStorageArgs['obj']['admin()address'] | CTokenStorageArgs['tuple']['admin()address']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(CTokenStorageParamsFactory.admin(params))
    },

    /**
     * Makes a call to the CTokenStorage smart contract using the `pendingAdmin()address` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    pendingAdmin: (params: CallParams<CTokenStorageArgs['obj']['pendingAdmin()address'] | CTokenStorageArgs['tuple']['pendingAdmin()address']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(CTokenStorageParamsFactory.pendingAdmin(params))
    },

    /**
     * Makes a call to the CTokenStorage smart contract using the `comptroller()address` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    comptroller: (params: CallParams<CTokenStorageArgs['obj']['comptroller()address'] | CTokenStorageArgs['tuple']['comptroller()address']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(CTokenStorageParamsFactory.comptroller(params))
    },

    /**
     * Makes a call to the CTokenStorage smart contract using the `interestRateModel()address` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    interestRateModel: (params: CallParams<CTokenStorageArgs['obj']['interestRateModel()address'] | CTokenStorageArgs['tuple']['interestRateModel()address']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(CTokenStorageParamsFactory.interestRateModel(params))
    },

    /**
     * Makes a call to the CTokenStorage smart contract using the `reserveFactorMantissa()uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    reserveFactorMantissa: (params: CallParams<CTokenStorageArgs['obj']['reserveFactorMantissa()uint256'] | CTokenStorageArgs['tuple']['reserveFactorMantissa()uint256']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(CTokenStorageParamsFactory.reserveFactorMantissa(params))
    },

    /**
     * Makes a call to the CTokenStorage smart contract using the `accrualBlockNumber()uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    accrualBlockNumber: (params: CallParams<CTokenStorageArgs['obj']['accrualBlockNumber()uint256'] | CTokenStorageArgs['tuple']['accrualBlockNumber()uint256']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(CTokenStorageParamsFactory.accrualBlockNumber(params))
    },

    /**
     * Makes a call to the CTokenStorage smart contract using the `borrowIndex()uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    borrowIndex: (params: CallParams<CTokenStorageArgs['obj']['borrowIndex()uint256'] | CTokenStorageArgs['tuple']['borrowIndex()uint256']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(CTokenStorageParamsFactory.borrowIndex(params))
    },

    /**
     * Makes a call to the CTokenStorage smart contract using the `totalBorrows()uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    totalBorrows: (params: CallParams<CTokenStorageArgs['obj']['totalBorrows()uint256'] | CTokenStorageArgs['tuple']['totalBorrows()uint256']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(CTokenStorageParamsFactory.totalBorrows(params))
    },

    /**
     * Makes a call to the CTokenStorage smart contract using the `totalReserves()uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    totalReserves: (params: CallParams<CTokenStorageArgs['obj']['totalReserves()uint256'] | CTokenStorageArgs['tuple']['totalReserves()uint256']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(CTokenStorageParamsFactory.totalReserves(params))
    },

    /**
     * Makes a call to the CTokenStorage smart contract using the `totalSupply()uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    totalSupply: (params: CallParams<CTokenStorageArgs['obj']['totalSupply()uint256'] | CTokenStorageArgs['tuple']['totalSupply()uint256']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(CTokenStorageParamsFactory.totalSupply(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Makes a clear_state call to an existing instance of the CTokenStorage smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the CTokenStorage smart contract using the `name()string` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    name: (params: CallParams<CTokenStorageArgs['obj']['name()string'] | CTokenStorageArgs['tuple']['name()string']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(CTokenStorageParamsFactory.name(params))
    },

    /**
     * Makes a call to the CTokenStorage smart contract using the `symbol()string` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    symbol: (params: CallParams<CTokenStorageArgs['obj']['symbol()string'] | CTokenStorageArgs['tuple']['symbol()string']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(CTokenStorageParamsFactory.symbol(params))
    },

    /**
     * Makes a call to the CTokenStorage smart contract using the `decimals()uint64` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    decimals: (params: CallParams<CTokenStorageArgs['obj']['decimals()uint64'] | CTokenStorageArgs['tuple']['decimals()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(CTokenStorageParamsFactory.decimals(params))
    },

    /**
     * Makes a call to the CTokenStorage smart contract using the `admin()address` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    admin: (params: CallParams<CTokenStorageArgs['obj']['admin()address'] | CTokenStorageArgs['tuple']['admin()address']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(CTokenStorageParamsFactory.admin(params))
    },

    /**
     * Makes a call to the CTokenStorage smart contract using the `pendingAdmin()address` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    pendingAdmin: (params: CallParams<CTokenStorageArgs['obj']['pendingAdmin()address'] | CTokenStorageArgs['tuple']['pendingAdmin()address']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(CTokenStorageParamsFactory.pendingAdmin(params))
    },

    /**
     * Makes a call to the CTokenStorage smart contract using the `comptroller()address` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    comptroller: (params: CallParams<CTokenStorageArgs['obj']['comptroller()address'] | CTokenStorageArgs['tuple']['comptroller()address']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(CTokenStorageParamsFactory.comptroller(params))
    },

    /**
     * Makes a call to the CTokenStorage smart contract using the `interestRateModel()address` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    interestRateModel: (params: CallParams<CTokenStorageArgs['obj']['interestRateModel()address'] | CTokenStorageArgs['tuple']['interestRateModel()address']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(CTokenStorageParamsFactory.interestRateModel(params))
    },

    /**
     * Makes a call to the CTokenStorage smart contract using the `reserveFactorMantissa()uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    reserveFactorMantissa: (params: CallParams<CTokenStorageArgs['obj']['reserveFactorMantissa()uint256'] | CTokenStorageArgs['tuple']['reserveFactorMantissa()uint256']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(CTokenStorageParamsFactory.reserveFactorMantissa(params))
    },

    /**
     * Makes a call to the CTokenStorage smart contract using the `accrualBlockNumber()uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    accrualBlockNumber: (params: CallParams<CTokenStorageArgs['obj']['accrualBlockNumber()uint256'] | CTokenStorageArgs['tuple']['accrualBlockNumber()uint256']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(CTokenStorageParamsFactory.accrualBlockNumber(params))
    },

    /**
     * Makes a call to the CTokenStorage smart contract using the `borrowIndex()uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    borrowIndex: (params: CallParams<CTokenStorageArgs['obj']['borrowIndex()uint256'] | CTokenStorageArgs['tuple']['borrowIndex()uint256']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(CTokenStorageParamsFactory.borrowIndex(params))
    },

    /**
     * Makes a call to the CTokenStorage smart contract using the `totalBorrows()uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    totalBorrows: (params: CallParams<CTokenStorageArgs['obj']['totalBorrows()uint256'] | CTokenStorageArgs['tuple']['totalBorrows()uint256']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(CTokenStorageParamsFactory.totalBorrows(params))
    },

    /**
     * Makes a call to the CTokenStorage smart contract using the `totalReserves()uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    totalReserves: (params: CallParams<CTokenStorageArgs['obj']['totalReserves()uint256'] | CTokenStorageArgs['tuple']['totalReserves()uint256']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(CTokenStorageParamsFactory.totalReserves(params))
    },

    /**
     * Makes a call to the CTokenStorage smart contract using the `totalSupply()uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    totalSupply: (params: CallParams<CTokenStorageArgs['obj']['totalSupply()uint256'] | CTokenStorageArgs['tuple']['totalSupply()uint256']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(CTokenStorageParamsFactory.totalSupply(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Makes a clear_state call to an existing instance of the CTokenStorage smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the CTokenStorage smart contract using the `name()string` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    name: async (params: CallParams<CTokenStorageArgs['obj']['name()string'] | CTokenStorageArgs['tuple']['name()string']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(CTokenStorageParamsFactory.name(params))
      return {...result, return: result.return as unknown as (undefined | CTokenStorageReturns['name()string'])}
    },

    /**
     * Makes a call to the CTokenStorage smart contract using the `symbol()string` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    symbol: async (params: CallParams<CTokenStorageArgs['obj']['symbol()string'] | CTokenStorageArgs['tuple']['symbol()string']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(CTokenStorageParamsFactory.symbol(params))
      return {...result, return: result.return as unknown as (undefined | CTokenStorageReturns['symbol()string'])}
    },

    /**
     * Makes a call to the CTokenStorage smart contract using the `decimals()uint64` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    decimals: async (params: CallParams<CTokenStorageArgs['obj']['decimals()uint64'] | CTokenStorageArgs['tuple']['decimals()uint64']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(CTokenStorageParamsFactory.decimals(params))
      return {...result, return: result.return as unknown as (undefined | CTokenStorageReturns['decimals()uint64'])}
    },

    /**
     * Makes a call to the CTokenStorage smart contract using the `admin()address` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    admin: async (params: CallParams<CTokenStorageArgs['obj']['admin()address'] | CTokenStorageArgs['tuple']['admin()address']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(CTokenStorageParamsFactory.admin(params))
      return {...result, return: result.return as unknown as (undefined | CTokenStorageReturns['admin()address'])}
    },

    /**
     * Makes a call to the CTokenStorage smart contract using the `pendingAdmin()address` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    pendingAdmin: async (params: CallParams<CTokenStorageArgs['obj']['pendingAdmin()address'] | CTokenStorageArgs['tuple']['pendingAdmin()address']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(CTokenStorageParamsFactory.pendingAdmin(params))
      return {...result, return: result.return as unknown as (undefined | CTokenStorageReturns['pendingAdmin()address'])}
    },

    /**
     * Makes a call to the CTokenStorage smart contract using the `comptroller()address` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    comptroller: async (params: CallParams<CTokenStorageArgs['obj']['comptroller()address'] | CTokenStorageArgs['tuple']['comptroller()address']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(CTokenStorageParamsFactory.comptroller(params))
      return {...result, return: result.return as unknown as (undefined | CTokenStorageReturns['comptroller()address'])}
    },

    /**
     * Makes a call to the CTokenStorage smart contract using the `interestRateModel()address` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    interestRateModel: async (params: CallParams<CTokenStorageArgs['obj']['interestRateModel()address'] | CTokenStorageArgs['tuple']['interestRateModel()address']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(CTokenStorageParamsFactory.interestRateModel(params))
      return {...result, return: result.return as unknown as (undefined | CTokenStorageReturns['interestRateModel()address'])}
    },

    /**
     * Makes a call to the CTokenStorage smart contract using the `reserveFactorMantissa()uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    reserveFactorMantissa: async (params: CallParams<CTokenStorageArgs['obj']['reserveFactorMantissa()uint256'] | CTokenStorageArgs['tuple']['reserveFactorMantissa()uint256']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(CTokenStorageParamsFactory.reserveFactorMantissa(params))
      return {...result, return: result.return as unknown as (undefined | CTokenStorageReturns['reserveFactorMantissa()uint256'])}
    },

    /**
     * Makes a call to the CTokenStorage smart contract using the `accrualBlockNumber()uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    accrualBlockNumber: async (params: CallParams<CTokenStorageArgs['obj']['accrualBlockNumber()uint256'] | CTokenStorageArgs['tuple']['accrualBlockNumber()uint256']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(CTokenStorageParamsFactory.accrualBlockNumber(params))
      return {...result, return: result.return as unknown as (undefined | CTokenStorageReturns['accrualBlockNumber()uint256'])}
    },

    /**
     * Makes a call to the CTokenStorage smart contract using the `borrowIndex()uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    borrowIndex: async (params: CallParams<CTokenStorageArgs['obj']['borrowIndex()uint256'] | CTokenStorageArgs['tuple']['borrowIndex()uint256']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(CTokenStorageParamsFactory.borrowIndex(params))
      return {...result, return: result.return as unknown as (undefined | CTokenStorageReturns['borrowIndex()uint256'])}
    },

    /**
     * Makes a call to the CTokenStorage smart contract using the `totalBorrows()uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    totalBorrows: async (params: CallParams<CTokenStorageArgs['obj']['totalBorrows()uint256'] | CTokenStorageArgs['tuple']['totalBorrows()uint256']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(CTokenStorageParamsFactory.totalBorrows(params))
      return {...result, return: result.return as unknown as (undefined | CTokenStorageReturns['totalBorrows()uint256'])}
    },

    /**
     * Makes a call to the CTokenStorage smart contract using the `totalReserves()uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    totalReserves: async (params: CallParams<CTokenStorageArgs['obj']['totalReserves()uint256'] | CTokenStorageArgs['tuple']['totalReserves()uint256']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(CTokenStorageParamsFactory.totalReserves(params))
      return {...result, return: result.return as unknown as (undefined | CTokenStorageReturns['totalReserves()uint256'])}
    },

    /**
     * Makes a call to the CTokenStorage smart contract using the `totalSupply()uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    totalSupply: async (params: CallParams<CTokenStorageArgs['obj']['totalSupply()uint256'] | CTokenStorageArgs['tuple']['totalSupply()uint256']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(CTokenStorageParamsFactory.totalSupply(params))
      return {...result, return: result.return as unknown as (undefined | CTokenStorageReturns['totalSupply()uint256'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new CTokenStorageClient(this.appClient.clone(params))
  }

  /**
   * Makes a readonly (simulated) call to the CTokenStorage smart contract using the `name()string` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async name(params: CallParams<CTokenStorageArgs['obj']['name()string'] | CTokenStorageArgs['tuple']['name()string']> = {args: []}) {
    const result = await this.appClient.send.call(CTokenStorageParamsFactory.name(params))
    return result.return as unknown as CTokenStorageReturns['name()string']
  }

  /**
   * Makes a readonly (simulated) call to the CTokenStorage smart contract using the `symbol()string` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async symbol(params: CallParams<CTokenStorageArgs['obj']['symbol()string'] | CTokenStorageArgs['tuple']['symbol()string']> = {args: []}) {
    const result = await this.appClient.send.call(CTokenStorageParamsFactory.symbol(params))
    return result.return as unknown as CTokenStorageReturns['symbol()string']
  }

  /**
   * Makes a readonly (simulated) call to the CTokenStorage smart contract using the `decimals()uint64` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async decimals(params: CallParams<CTokenStorageArgs['obj']['decimals()uint64'] | CTokenStorageArgs['tuple']['decimals()uint64']> = {args: []}) {
    const result = await this.appClient.send.call(CTokenStorageParamsFactory.decimals(params))
    return result.return as unknown as CTokenStorageReturns['decimals()uint64']
  }

  /**
   * Makes a readonly (simulated) call to the CTokenStorage smart contract using the `admin()address` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async admin(params: CallParams<CTokenStorageArgs['obj']['admin()address'] | CTokenStorageArgs['tuple']['admin()address']> = {args: []}) {
    const result = await this.appClient.send.call(CTokenStorageParamsFactory.admin(params))
    return result.return as unknown as CTokenStorageReturns['admin()address']
  }

  /**
   * Makes a readonly (simulated) call to the CTokenStorage smart contract using the `pendingAdmin()address` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async pendingAdmin(params: CallParams<CTokenStorageArgs['obj']['pendingAdmin()address'] | CTokenStorageArgs['tuple']['pendingAdmin()address']> = {args: []}) {
    const result = await this.appClient.send.call(CTokenStorageParamsFactory.pendingAdmin(params))
    return result.return as unknown as CTokenStorageReturns['pendingAdmin()address']
  }

  /**
   * Makes a readonly (simulated) call to the CTokenStorage smart contract using the `comptroller()address` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async comptroller(params: CallParams<CTokenStorageArgs['obj']['comptroller()address'] | CTokenStorageArgs['tuple']['comptroller()address']> = {args: []}) {
    const result = await this.appClient.send.call(CTokenStorageParamsFactory.comptroller(params))
    return result.return as unknown as CTokenStorageReturns['comptroller()address']
  }

  /**
   * Makes a readonly (simulated) call to the CTokenStorage smart contract using the `interestRateModel()address` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async interestRateModel(params: CallParams<CTokenStorageArgs['obj']['interestRateModel()address'] | CTokenStorageArgs['tuple']['interestRateModel()address']> = {args: []}) {
    const result = await this.appClient.send.call(CTokenStorageParamsFactory.interestRateModel(params))
    return result.return as unknown as CTokenStorageReturns['interestRateModel()address']
  }

  /**
   * Makes a readonly (simulated) call to the CTokenStorage smart contract using the `reserveFactorMantissa()uint256` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async reserveFactorMantissa(params: CallParams<CTokenStorageArgs['obj']['reserveFactorMantissa()uint256'] | CTokenStorageArgs['tuple']['reserveFactorMantissa()uint256']> = {args: []}) {
    const result = await this.appClient.send.call(CTokenStorageParamsFactory.reserveFactorMantissa(params))
    return result.return as unknown as CTokenStorageReturns['reserveFactorMantissa()uint256']
  }

  /**
   * Makes a readonly (simulated) call to the CTokenStorage smart contract using the `accrualBlockNumber()uint256` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async accrualBlockNumber(params: CallParams<CTokenStorageArgs['obj']['accrualBlockNumber()uint256'] | CTokenStorageArgs['tuple']['accrualBlockNumber()uint256']> = {args: []}) {
    const result = await this.appClient.send.call(CTokenStorageParamsFactory.accrualBlockNumber(params))
    return result.return as unknown as CTokenStorageReturns['accrualBlockNumber()uint256']
  }

  /**
   * Makes a readonly (simulated) call to the CTokenStorage smart contract using the `borrowIndex()uint256` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async borrowIndex(params: CallParams<CTokenStorageArgs['obj']['borrowIndex()uint256'] | CTokenStorageArgs['tuple']['borrowIndex()uint256']> = {args: []}) {
    const result = await this.appClient.send.call(CTokenStorageParamsFactory.borrowIndex(params))
    return result.return as unknown as CTokenStorageReturns['borrowIndex()uint256']
  }

  /**
   * Makes a readonly (simulated) call to the CTokenStorage smart contract using the `totalBorrows()uint256` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async totalBorrows(params: CallParams<CTokenStorageArgs['obj']['totalBorrows()uint256'] | CTokenStorageArgs['tuple']['totalBorrows()uint256']> = {args: []}) {
    const result = await this.appClient.send.call(CTokenStorageParamsFactory.totalBorrows(params))
    return result.return as unknown as CTokenStorageReturns['totalBorrows()uint256']
  }

  /**
   * Makes a readonly (simulated) call to the CTokenStorage smart contract using the `totalReserves()uint256` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async totalReserves(params: CallParams<CTokenStorageArgs['obj']['totalReserves()uint256'] | CTokenStorageArgs['tuple']['totalReserves()uint256']> = {args: []}) {
    const result = await this.appClient.send.call(CTokenStorageParamsFactory.totalReserves(params))
    return result.return as unknown as CTokenStorageReturns['totalReserves()uint256']
  }

  /**
   * Makes a readonly (simulated) call to the CTokenStorage smart contract using the `totalSupply()uint256` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async totalSupply(params: CallParams<CTokenStorageArgs['obj']['totalSupply()uint256'] | CTokenStorageArgs['tuple']['totalSupply()uint256']> = {args: []}) {
    const result = await this.appClient.send.call(CTokenStorageParamsFactory.totalSupply(params))
    return result.return as unknown as CTokenStorageReturns['totalSupply()uint256']
  }

  /**
   * Methods to access state for the current CTokenStorage app
   */
  state = {
    /**
     * Methods to access global state for the current CTokenStorage app
     */
    global: {
      /**
       * Get all current keyed values from global state
       */
      getAll: async (): Promise<Partial<Expand<GlobalKeysState>>> => {
        const result = await this.appClient.state.global.getAll()
        return {
          _notEntered: result._notEntered,
          name: result.name,
          symbol: result.symbol,
          decimals: result.decimals,
          admin: result.admin,
          pendingAdmin: result.pendingAdmin,
          comptroller: result.comptroller,
          interestRateModel: result.interestRateModel,
          initialExchangeRateMantissa: new BinaryStateValue(result.initialExchangeRateMantissa),
          reserveFactorMantissa: new BinaryStateValue(result.reserveFactorMantissa),
          accrualBlockNumber: new BinaryStateValue(result.accrualBlockNumber),
          borrowIndex: new BinaryStateValue(result.borrowIndex),
          totalBorrows: new BinaryStateValue(result.totalBorrows),
          totalReserves: new BinaryStateValue(result.totalReserves),
          totalSupply: new BinaryStateValue(result.totalSupply),
        }
      },
      /**
       * Get the current value of the _notEntered key in global state
       */
      _notEntered: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("_notEntered")) as bigint | undefined },
      /**
       * Get the current value of the name key in global state
       */
      name: async (): Promise<string | undefined> => { return (await this.appClient.state.global.getValue("name")) as string | undefined },
      /**
       * Get the current value of the symbol key in global state
       */
      symbol: async (): Promise<string | undefined> => { return (await this.appClient.state.global.getValue("symbol")) as string | undefined },
      /**
       * Get the current value of the decimals key in global state
       */
      decimals: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("decimals")) as bigint | undefined },
      /**
       * Get the current value of the admin key in global state
       */
      admin: async (): Promise<string | undefined> => { return (await this.appClient.state.global.getValue("admin")) as string | undefined },
      /**
       * Get the current value of the pendingAdmin key in global state
       */
      pendingAdmin: async (): Promise<string | undefined> => { return (await this.appClient.state.global.getValue("pendingAdmin")) as string | undefined },
      /**
       * Get the current value of the comptroller key in global state
       */
      comptroller: async (): Promise<string | undefined> => { return (await this.appClient.state.global.getValue("comptroller")) as string | undefined },
      /**
       * Get the current value of the interestRateModel key in global state
       */
      interestRateModel: async (): Promise<string | undefined> => { return (await this.appClient.state.global.getValue("interestRateModel")) as string | undefined },
      /**
       * Get the current value of the initialExchangeRateMantissa key in global state
       */
      initialExchangeRateMantissa: async (): Promise<BinaryState> => { return new BinaryStateValue((await this.appClient.state.global.getValue("initialExchangeRateMantissa")) as Uint8Array | undefined) },
      /**
       * Get the current value of the reserveFactorMantissa key in global state
       */
      reserveFactorMantissa: async (): Promise<BinaryState> => { return new BinaryStateValue((await this.appClient.state.global.getValue("reserveFactorMantissa")) as Uint8Array | undefined) },
      /**
       * Get the current value of the accrualBlockNumber key in global state
       */
      accrualBlockNumber: async (): Promise<BinaryState> => { return new BinaryStateValue((await this.appClient.state.global.getValue("accrualBlockNumber")) as Uint8Array | undefined) },
      /**
       * Get the current value of the borrowIndex key in global state
       */
      borrowIndex: async (): Promise<BinaryState> => { return new BinaryStateValue((await this.appClient.state.global.getValue("borrowIndex")) as Uint8Array | undefined) },
      /**
       * Get the current value of the totalBorrows key in global state
       */
      totalBorrows: async (): Promise<BinaryState> => { return new BinaryStateValue((await this.appClient.state.global.getValue("totalBorrows")) as Uint8Array | undefined) },
      /**
       * Get the current value of the totalReserves key in global state
       */
      totalReserves: async (): Promise<BinaryState> => { return new BinaryStateValue((await this.appClient.state.global.getValue("totalReserves")) as Uint8Array | undefined) },
      /**
       * Get the current value of the totalSupply key in global state
       */
      totalSupply: async (): Promise<BinaryState> => { return new BinaryStateValue((await this.appClient.state.global.getValue("totalSupply")) as Uint8Array | undefined) },
    },
    /**
     * Methods to access box state for the current CTokenStorage app
     */
    box: {
      /**
       * Get all current keyed values from box state
       */
      getAll: async (): Promise<Partial<Expand<BoxKeysState>>> => {
        const result = await this.appClient.state.box.getAll()
        return {
        }
      },
      /**
       * Get values from the accountTokens map in box state
       */
      accountTokens: {
        /**
         * Get all current values of the accountTokens map in box state
         */
        getMap: async (): Promise<Map<Uint8Array, Uint8Array>> => { return (await this.appClient.state.box.getMap("accountTokens")) as Map<Uint8Array, Uint8Array> },
        /**
         * Get a current value of the accountTokens map by key from box state
         */
        value: async (key: Uint8Array | string): Promise<Uint8Array | undefined> => { return await this.appClient.state.box.getMapValue("accountTokens", key) as Uint8Array | undefined },
      },
      /**
       * Get values from the transferAllowances map in box state
       */
      transferAllowances: {
        /**
         * Get all current values of the transferAllowances map in box state
         */
        getMap: async (): Promise<Map<Uint8Array, Uint8Array>> => { return (await this.appClient.state.box.getMap("transferAllowances")) as Map<Uint8Array, Uint8Array> },
        /**
         * Get a current value of the transferAllowances map by key from box state
         */
        value: async (key: Uint8Array | string): Promise<Uint8Array | undefined> => { return await this.appClient.state.box.getMapValue("transferAllowances", key) as Uint8Array | undefined },
      },
      /**
       * Get values from the accountBorrows map in box state
       */
      accountBorrows: {
        /**
         * Get all current values of the accountBorrows map in box state
         */
        getMap: async (): Promise<Map<Uint8Array, BorrowSnapshot>> => { return (await this.appClient.state.box.getMap("accountBorrows")) as Map<Uint8Array, BorrowSnapshot> },
        /**
         * Get a current value of the accountBorrows map by key from box state
         */
        value: async (key: Uint8Array | string): Promise<BorrowSnapshot | undefined> => { return await this.appClient.state.box.getMapValue("accountBorrows", key) as BorrowSnapshot | undefined },
      },
    },
  }

  public newGroup(): CTokenStorageComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a name()string method call against the CTokenStorage contract
       */
      name(params: CallParams<CTokenStorageArgs['obj']['name()string'] | CTokenStorageArgs['tuple']['name()string']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.name(params)))
        resultMappers.push((v) => client.decodeReturnValue('name()string', v))
        return this
      },
      /**
       * Add a symbol()string method call against the CTokenStorage contract
       */
      symbol(params: CallParams<CTokenStorageArgs['obj']['symbol()string'] | CTokenStorageArgs['tuple']['symbol()string']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.symbol(params)))
        resultMappers.push((v) => client.decodeReturnValue('symbol()string', v))
        return this
      },
      /**
       * Add a decimals()uint64 method call against the CTokenStorage contract
       */
      decimals(params: CallParams<CTokenStorageArgs['obj']['decimals()uint64'] | CTokenStorageArgs['tuple']['decimals()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.decimals(params)))
        resultMappers.push((v) => client.decodeReturnValue('decimals()uint64', v))
        return this
      },
      /**
       * Add a admin()address method call against the CTokenStorage contract
       */
      admin(params: CallParams<CTokenStorageArgs['obj']['admin()address'] | CTokenStorageArgs['tuple']['admin()address']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.admin(params)))
        resultMappers.push((v) => client.decodeReturnValue('admin()address', v))
        return this
      },
      /**
       * Add a pendingAdmin()address method call against the CTokenStorage contract
       */
      pendingAdmin(params: CallParams<CTokenStorageArgs['obj']['pendingAdmin()address'] | CTokenStorageArgs['tuple']['pendingAdmin()address']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.pendingAdmin(params)))
        resultMappers.push((v) => client.decodeReturnValue('pendingAdmin()address', v))
        return this
      },
      /**
       * Add a comptroller()address method call against the CTokenStorage contract
       */
      comptroller(params: CallParams<CTokenStorageArgs['obj']['comptroller()address'] | CTokenStorageArgs['tuple']['comptroller()address']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.comptroller(params)))
        resultMappers.push((v) => client.decodeReturnValue('comptroller()address', v))
        return this
      },
      /**
       * Add a interestRateModel()address method call against the CTokenStorage contract
       */
      interestRateModel(params: CallParams<CTokenStorageArgs['obj']['interestRateModel()address'] | CTokenStorageArgs['tuple']['interestRateModel()address']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.interestRateModel(params)))
        resultMappers.push((v) => client.decodeReturnValue('interestRateModel()address', v))
        return this
      },
      /**
       * Add a reserveFactorMantissa()uint256 method call against the CTokenStorage contract
       */
      reserveFactorMantissa(params: CallParams<CTokenStorageArgs['obj']['reserveFactorMantissa()uint256'] | CTokenStorageArgs['tuple']['reserveFactorMantissa()uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.reserveFactorMantissa(params)))
        resultMappers.push((v) => client.decodeReturnValue('reserveFactorMantissa()uint256', v))
        return this
      },
      /**
       * Add a accrualBlockNumber()uint256 method call against the CTokenStorage contract
       */
      accrualBlockNumber(params: CallParams<CTokenStorageArgs['obj']['accrualBlockNumber()uint256'] | CTokenStorageArgs['tuple']['accrualBlockNumber()uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.accrualBlockNumber(params)))
        resultMappers.push((v) => client.decodeReturnValue('accrualBlockNumber()uint256', v))
        return this
      },
      /**
       * Add a borrowIndex()uint256 method call against the CTokenStorage contract
       */
      borrowIndex(params: CallParams<CTokenStorageArgs['obj']['borrowIndex()uint256'] | CTokenStorageArgs['tuple']['borrowIndex()uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.borrowIndex(params)))
        resultMappers.push((v) => client.decodeReturnValue('borrowIndex()uint256', v))
        return this
      },
      /**
       * Add a totalBorrows()uint256 method call against the CTokenStorage contract
       */
      totalBorrows(params: CallParams<CTokenStorageArgs['obj']['totalBorrows()uint256'] | CTokenStorageArgs['tuple']['totalBorrows()uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.totalBorrows(params)))
        resultMappers.push((v) => client.decodeReturnValue('totalBorrows()uint256', v))
        return this
      },
      /**
       * Add a totalReserves()uint256 method call against the CTokenStorage contract
       */
      totalReserves(params: CallParams<CTokenStorageArgs['obj']['totalReserves()uint256'] | CTokenStorageArgs['tuple']['totalReserves()uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.totalReserves(params)))
        resultMappers.push((v) => client.decodeReturnValue('totalReserves()uint256', v))
        return this
      },
      /**
       * Add a totalSupply()uint256 method call against the CTokenStorage contract
       */
      totalSupply(params: CallParams<CTokenStorageArgs['obj']['totalSupply()uint256'] | CTokenStorageArgs['tuple']['totalSupply()uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.totalSupply(params)))
        resultMappers.push((v) => client.decodeReturnValue('totalSupply()uint256', v))
        return this
      },
      /**
       * Add a clear state call to the CTokenStorage contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as CTokenStorageComposer
  }
}
export type CTokenStorageComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the name()string ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  name(params?: CallParams<CTokenStorageArgs['obj']['name()string'] | CTokenStorageArgs['tuple']['name()string']>): CTokenStorageComposer<[...TReturns, CTokenStorageReturns['name()string'] | undefined]>

  /**
   * Calls the symbol()string ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  symbol(params?: CallParams<CTokenStorageArgs['obj']['symbol()string'] | CTokenStorageArgs['tuple']['symbol()string']>): CTokenStorageComposer<[...TReturns, CTokenStorageReturns['symbol()string'] | undefined]>

  /**
   * Calls the decimals()uint64 ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  decimals(params?: CallParams<CTokenStorageArgs['obj']['decimals()uint64'] | CTokenStorageArgs['tuple']['decimals()uint64']>): CTokenStorageComposer<[...TReturns, CTokenStorageReturns['decimals()uint64'] | undefined]>

  /**
   * Calls the admin()address ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  admin(params?: CallParams<CTokenStorageArgs['obj']['admin()address'] | CTokenStorageArgs['tuple']['admin()address']>): CTokenStorageComposer<[...TReturns, CTokenStorageReturns['admin()address'] | undefined]>

  /**
   * Calls the pendingAdmin()address ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  pendingAdmin(params?: CallParams<CTokenStorageArgs['obj']['pendingAdmin()address'] | CTokenStorageArgs['tuple']['pendingAdmin()address']>): CTokenStorageComposer<[...TReturns, CTokenStorageReturns['pendingAdmin()address'] | undefined]>

  /**
   * Calls the comptroller()address ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  comptroller(params?: CallParams<CTokenStorageArgs['obj']['comptroller()address'] | CTokenStorageArgs['tuple']['comptroller()address']>): CTokenStorageComposer<[...TReturns, CTokenStorageReturns['comptroller()address'] | undefined]>

  /**
   * Calls the interestRateModel()address ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  interestRateModel(params?: CallParams<CTokenStorageArgs['obj']['interestRateModel()address'] | CTokenStorageArgs['tuple']['interestRateModel()address']>): CTokenStorageComposer<[...TReturns, CTokenStorageReturns['interestRateModel()address'] | undefined]>

  /**
   * Calls the reserveFactorMantissa()uint256 ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  reserveFactorMantissa(params?: CallParams<CTokenStorageArgs['obj']['reserveFactorMantissa()uint256'] | CTokenStorageArgs['tuple']['reserveFactorMantissa()uint256']>): CTokenStorageComposer<[...TReturns, CTokenStorageReturns['reserveFactorMantissa()uint256'] | undefined]>

  /**
   * Calls the accrualBlockNumber()uint256 ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  accrualBlockNumber(params?: CallParams<CTokenStorageArgs['obj']['accrualBlockNumber()uint256'] | CTokenStorageArgs['tuple']['accrualBlockNumber()uint256']>): CTokenStorageComposer<[...TReturns, CTokenStorageReturns['accrualBlockNumber()uint256'] | undefined]>

  /**
   * Calls the borrowIndex()uint256 ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  borrowIndex(params?: CallParams<CTokenStorageArgs['obj']['borrowIndex()uint256'] | CTokenStorageArgs['tuple']['borrowIndex()uint256']>): CTokenStorageComposer<[...TReturns, CTokenStorageReturns['borrowIndex()uint256'] | undefined]>

  /**
   * Calls the totalBorrows()uint256 ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  totalBorrows(params?: CallParams<CTokenStorageArgs['obj']['totalBorrows()uint256'] | CTokenStorageArgs['tuple']['totalBorrows()uint256']>): CTokenStorageComposer<[...TReturns, CTokenStorageReturns['totalBorrows()uint256'] | undefined]>

  /**
   * Calls the totalReserves()uint256 ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  totalReserves(params?: CallParams<CTokenStorageArgs['obj']['totalReserves()uint256'] | CTokenStorageArgs['tuple']['totalReserves()uint256']>): CTokenStorageComposer<[...TReturns, CTokenStorageReturns['totalReserves()uint256'] | undefined]>

  /**
   * Calls the totalSupply()uint256 ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  totalSupply(params?: CallParams<CTokenStorageArgs['obj']['totalSupply()uint256'] | CTokenStorageArgs['tuple']['totalSupply()uint256']>): CTokenStorageComposer<[...TReturns, CTokenStorageReturns['totalSupply()uint256'] | undefined]>

  /**
   * Makes a clear_state call to an existing instance of the CTokenStorage smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): CTokenStorageComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): CTokenStorageComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<CTokenStorageComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<CTokenStorageComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<CTokenStorageComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<CTokenStorageComposerResults<TReturns>>
}
export type CTokenStorageComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

