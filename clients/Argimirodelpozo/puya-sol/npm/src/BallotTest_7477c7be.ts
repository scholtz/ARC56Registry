/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"BallotTest","structs":{"tuple":[{"name":"winId","type":"uint512"},{"name":"winVotes","type":"uint512"}]},"methods":[{"name":"chairman","args":[],"returns":{"type":"address"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"proposalCount","args":[],"returns":{"type":"uint512"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"canVote","args":[{"type":"address","name":"voter"}],"returns":{"type":"bool"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"hasVoted","args":[{"type":"address","name":"voter"}],"returns":{"type":"bool"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"proposalVotes","args":[{"type":"uint512","name":"proposalId"}],"returns":{"type":"uint512"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"grantVotingRight","args":[{"type":"address","name":"voter"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}},{"name":"vote","args":[{"type":"address","name":"voter"},{"type":"uint512","name":"proposalId"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}},{"name":"winningProposal","args":[],"returns":{"type":"(uint512,uint512)","struct":"tuple"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}}],"arcs":[22,28],"desc":" @dev Simple ballot/voting contract.\n Chairman grants voting rights, voters can vote on proposals (by index).","networks":{},"state":{"schema":{"global":{"ints":0,"bytes":2},"local":{"ints":0,"bytes":0}},"keys":{"global":{"_chairman":{"keyType":"AVMString","valueType":"address","key":"X2NoYWlybWFu"},"_proposalCount":{"keyType":"AVMString","valueType":"AVMBytes","key":"X3Byb3Bvc2FsQ291bnQ="}},"local":{},"box":{}},"maps":{"global":{},"local":{},"box":{"_hasVoted":{"keyType":"AVMBytes","valueType":"uint64","prefix":"X2hhc1ZvdGVk"},"_canVote":{"keyType":"AVMBytes","valueType":"uint64","prefix":"X2NhblZvdGU="},"_proposalVotes":{"keyType":"AVMBytes","valueType":"AVMBytes","prefix":"X3Byb3Bvc2FsVm90ZXM="}}}},"bareActions":{"create":[],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[370,426],"errorMessage":"Already voted"},{"pc":[434],"errorMessage":"Invalid proposal"},{"pc":[411],"errorMessage":"No voting right"},{"pc":[354],"errorMessage":"Not chairman"},{"pc":[204,352],"errorMessage":"check _chairman exists"},{"pc":[214,430,477],"errorMessage":"check _proposalCount exists"},{"pc":[236,270,346,387],"errorMessage":"invalid number of bytes for arc4.static_array<arc4.uint8, 32>"},{"pc":[304,395],"errorMessage":"invalid number of bytes for arc4.uint512"},{"pc":[219,331,537,550],"errorMessage":"overflow"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"events":[]} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


// Type definitions for ARC-56 structs

export type Tuple = {
  winId: bigint,
  winVotes: bigint
}


/**
 * Converts the ABI tuple representation of a tuple to the struct representation
 */
export function TupleFromTuple(abiTuple: [bigint, bigint]) {
  return getABIStructFromABITuple(abiTuple, APP_SPEC.structs.tuple, APP_SPEC.structs) as Tuple
}

/**
 * The argument types for the BallotTest contract
 */
export type BallotTestArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'chairman()address': Record<string, never>
    'proposalCount()uint512': Record<string, never>
    'canVote(address)bool': {
      voter: string
    }
    'hasVoted(address)bool': {
      voter: string
    }
    'proposalVotes(uint512)uint512': {
      proposalId: bigint | number
    }
    'grantVotingRight(address)void': {
      voter: string
    }
    'vote(address,uint512)void': {
      voter: string
      proposalId: bigint | number
    }
    'winningProposal()(uint512,uint512)': Record<string, never>
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'chairman()address': []
    'proposalCount()uint512': []
    'canVote(address)bool': [voter: string]
    'hasVoted(address)bool': [voter: string]
    'proposalVotes(uint512)uint512': [proposalId: bigint | number]
    'grantVotingRight(address)void': [voter: string]
    'vote(address,uint512)void': [voter: string, proposalId: bigint | number]
    'winningProposal()(uint512,uint512)': []
  }
}

/**
 * The return type for each method
 */
export type BallotTestReturns = {
  'chairman()address': string
  'proposalCount()uint512': bigint
  'canVote(address)bool': boolean
  'hasVoted(address)bool': boolean
  'proposalVotes(uint512)uint512': bigint
  'grantVotingRight(address)void': void
  'vote(address,uint512)void': void
  'winningProposal()(uint512,uint512)': Tuple
}

/**
 * Defines the types of available calls and state of the BallotTest smart contract.
 */
export type BallotTestTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'chairman()address' | 'chairman', {
      argsObj: BallotTestArgs['obj']['chairman()address']
      argsTuple: BallotTestArgs['tuple']['chairman()address']
      returns: BallotTestReturns['chairman()address']
    }>
    & Record<'proposalCount()uint512' | 'proposalCount', {
      argsObj: BallotTestArgs['obj']['proposalCount()uint512']
      argsTuple: BallotTestArgs['tuple']['proposalCount()uint512']
      returns: BallotTestReturns['proposalCount()uint512']
    }>
    & Record<'canVote(address)bool' | 'canVote', {
      argsObj: BallotTestArgs['obj']['canVote(address)bool']
      argsTuple: BallotTestArgs['tuple']['canVote(address)bool']
      returns: BallotTestReturns['canVote(address)bool']
    }>
    & Record<'hasVoted(address)bool' | 'hasVoted', {
      argsObj: BallotTestArgs['obj']['hasVoted(address)bool']
      argsTuple: BallotTestArgs['tuple']['hasVoted(address)bool']
      returns: BallotTestReturns['hasVoted(address)bool']
    }>
    & Record<'proposalVotes(uint512)uint512' | 'proposalVotes', {
      argsObj: BallotTestArgs['obj']['proposalVotes(uint512)uint512']
      argsTuple: BallotTestArgs['tuple']['proposalVotes(uint512)uint512']
      returns: BallotTestReturns['proposalVotes(uint512)uint512']
    }>
    & Record<'grantVotingRight(address)void' | 'grantVotingRight', {
      argsObj: BallotTestArgs['obj']['grantVotingRight(address)void']
      argsTuple: BallotTestArgs['tuple']['grantVotingRight(address)void']
      returns: BallotTestReturns['grantVotingRight(address)void']
    }>
    & Record<'vote(address,uint512)void' | 'vote', {
      argsObj: BallotTestArgs['obj']['vote(address,uint512)void']
      argsTuple: BallotTestArgs['tuple']['vote(address,uint512)void']
      returns: BallotTestReturns['vote(address,uint512)void']
    }>
    & Record<'winningProposal()(uint512,uint512)' | 'winningProposal', {
      argsObj: BallotTestArgs['obj']['winningProposal()(uint512,uint512)']
      argsTuple: BallotTestArgs['tuple']['winningProposal()(uint512,uint512)']
      returns: BallotTestReturns['winningProposal()(uint512,uint512)']
    }>
  /**
   * Defines the shape of the state of the application.
   */
  state: {
    global: {
      keys: {
        _chairman: string
        _proposalCount: BinaryState
      }
      maps: {}
    }
    box: {
      keys: {}
      maps: {
        _hasVoted: Map<Uint8Array | string, bigint>
        _canVote: Map<Uint8Array | string, bigint>
        _proposalVotes: Map<Uint8Array | string, Uint8Array>
      }
    }
  }
}

/**
 * Defines the possible abi call signatures.
 */
export type BallotTestSignatures = keyof BallotTestTypes['methods']
/**
 * Defines the possible abi call signatures for methods that return a non-void value.
 */
export type BallotTestNonVoidMethodSignatures = keyof BallotTestTypes['methods'] extends infer T ? T extends keyof BallotTestTypes['methods'] ? MethodReturn<T> extends void ? never : T  : never : never
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the BallotTest smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends BallotTestSignatures> = BallotTestTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the BallotTest smart contract to the method's return type
 */
export type MethodReturn<TSignature extends BallotTestSignatures> = BallotTestTypes['methods'][TSignature]['returns']

/**
 * Defines the shape of the keyed global state of the application.
 */
export type GlobalKeysState = BallotTestTypes['state']['global']['keys']

/**
 * Defines the shape of the keyed box state of the application.
 */
export type BoxKeysState = BallotTestTypes['state']['box']['keys']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the BallotTest smart contract
 */
export abstract class BallotTestParamsFactory {
  /**
   * Constructs a no op call for the chairman()address ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static chairman(params: CallParams<BallotTestArgs['obj']['chairman()address'] | BallotTestArgs['tuple']['chairman()address']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'chairman()address' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the proposalCount()uint512 ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static proposalCount(params: CallParams<BallotTestArgs['obj']['proposalCount()uint512'] | BallotTestArgs['tuple']['proposalCount()uint512']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'proposalCount()uint512' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the canVote(address)bool ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static canVote(params: CallParams<BallotTestArgs['obj']['canVote(address)bool'] | BallotTestArgs['tuple']['canVote(address)bool']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'canVote(address)bool' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.voter],
    }
  }
  /**
   * Constructs a no op call for the hasVoted(address)bool ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static hasVoted(params: CallParams<BallotTestArgs['obj']['hasVoted(address)bool'] | BallotTestArgs['tuple']['hasVoted(address)bool']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'hasVoted(address)bool' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.voter],
    }
  }
  /**
   * Constructs a no op call for the proposalVotes(uint512)uint512 ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static proposalVotes(params: CallParams<BallotTestArgs['obj']['proposalVotes(uint512)uint512'] | BallotTestArgs['tuple']['proposalVotes(uint512)uint512']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'proposalVotes(uint512)uint512' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.proposalId],
    }
  }
  /**
   * Constructs a no op call for the grantVotingRight(address)void ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static grantVotingRight(params: CallParams<BallotTestArgs['obj']['grantVotingRight(address)void'] | BallotTestArgs['tuple']['grantVotingRight(address)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'grantVotingRight(address)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.voter],
    }
  }
  /**
   * Constructs a no op call for the vote(address,uint512)void ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static vote(params: CallParams<BallotTestArgs['obj']['vote(address,uint512)void'] | BallotTestArgs['tuple']['vote(address,uint512)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'vote(address,uint512)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.voter, params.args.proposalId],
    }
  }
  /**
   * Constructs a no op call for the winningProposal()(uint512,uint512) ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static winningProposal(params: CallParams<BallotTestArgs['obj']['winningProposal()(uint512,uint512)'] | BallotTestArgs['tuple']['winningProposal()(uint512,uint512)']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'winningProposal()(uint512,uint512)' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
}

/**
 * A client to make calls to the BallotTest smart contract
 */
export class BallotTestClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `BallotTestClient`
   *
   * @param appClient An `AppClient` instance which has been created with the BallotTest app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `BallotTestClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Checks for decode errors on the given return value and maps the return value to the return type for the given method
   * @returns The typed return value or undefined if there was no value
   */
  decodeReturnValue<TSignature extends BallotTestNonVoidMethodSignatures>(method: TSignature, returnValue: ABIReturn | undefined) {
    return returnValue !== undefined ? getArc56ReturnValue<MethodReturn<TSignature>>(returnValue, this.appClient.getABIMethod(method), APP_SPEC.structs) : undefined
  }

  /**
   * Returns a new `BallotTestClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<BallotTestClient> {
    return new BallotTestClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `BallotTestClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<BallotTestClient> {
    return new BallotTestClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Makes a clear_state call to an existing instance of the BallotTest smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the BallotTest smart contract using the `chairman()address` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    chairman: (params: CallParams<BallotTestArgs['obj']['chairman()address'] | BallotTestArgs['tuple']['chairman()address']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(BallotTestParamsFactory.chairman(params))
    },

    /**
     * Makes a call to the BallotTest smart contract using the `proposalCount()uint512` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    proposalCount: (params: CallParams<BallotTestArgs['obj']['proposalCount()uint512'] | BallotTestArgs['tuple']['proposalCount()uint512']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(BallotTestParamsFactory.proposalCount(params))
    },

    /**
     * Makes a call to the BallotTest smart contract using the `canVote(address)bool` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    canVote: (params: CallParams<BallotTestArgs['obj']['canVote(address)bool'] | BallotTestArgs['tuple']['canVote(address)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(BallotTestParamsFactory.canVote(params))
    },

    /**
     * Makes a call to the BallotTest smart contract using the `hasVoted(address)bool` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    hasVoted: (params: CallParams<BallotTestArgs['obj']['hasVoted(address)bool'] | BallotTestArgs['tuple']['hasVoted(address)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(BallotTestParamsFactory.hasVoted(params))
    },

    /**
     * Makes a call to the BallotTest smart contract using the `proposalVotes(uint512)uint512` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    proposalVotes: (params: CallParams<BallotTestArgs['obj']['proposalVotes(uint512)uint512'] | BallotTestArgs['tuple']['proposalVotes(uint512)uint512']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(BallotTestParamsFactory.proposalVotes(params))
    },

    /**
     * Makes a call to the BallotTest smart contract using the `grantVotingRight(address)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    grantVotingRight: (params: CallParams<BallotTestArgs['obj']['grantVotingRight(address)void'] | BallotTestArgs['tuple']['grantVotingRight(address)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(BallotTestParamsFactory.grantVotingRight(params))
    },

    /**
     * Makes a call to the BallotTest smart contract using the `vote(address,uint512)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    vote: (params: CallParams<BallotTestArgs['obj']['vote(address,uint512)void'] | BallotTestArgs['tuple']['vote(address,uint512)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(BallotTestParamsFactory.vote(params))
    },

    /**
     * Makes a call to the BallotTest smart contract using the `winningProposal()(uint512,uint512)` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    winningProposal: (params: CallParams<BallotTestArgs['obj']['winningProposal()(uint512,uint512)'] | BallotTestArgs['tuple']['winningProposal()(uint512,uint512)']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(BallotTestParamsFactory.winningProposal(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Makes a clear_state call to an existing instance of the BallotTest smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the BallotTest smart contract using the `chairman()address` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    chairman: (params: CallParams<BallotTestArgs['obj']['chairman()address'] | BallotTestArgs['tuple']['chairman()address']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(BallotTestParamsFactory.chairman(params))
    },

    /**
     * Makes a call to the BallotTest smart contract using the `proposalCount()uint512` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    proposalCount: (params: CallParams<BallotTestArgs['obj']['proposalCount()uint512'] | BallotTestArgs['tuple']['proposalCount()uint512']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(BallotTestParamsFactory.proposalCount(params))
    },

    /**
     * Makes a call to the BallotTest smart contract using the `canVote(address)bool` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    canVote: (params: CallParams<BallotTestArgs['obj']['canVote(address)bool'] | BallotTestArgs['tuple']['canVote(address)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(BallotTestParamsFactory.canVote(params))
    },

    /**
     * Makes a call to the BallotTest smart contract using the `hasVoted(address)bool` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    hasVoted: (params: CallParams<BallotTestArgs['obj']['hasVoted(address)bool'] | BallotTestArgs['tuple']['hasVoted(address)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(BallotTestParamsFactory.hasVoted(params))
    },

    /**
     * Makes a call to the BallotTest smart contract using the `proposalVotes(uint512)uint512` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    proposalVotes: (params: CallParams<BallotTestArgs['obj']['proposalVotes(uint512)uint512'] | BallotTestArgs['tuple']['proposalVotes(uint512)uint512']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(BallotTestParamsFactory.proposalVotes(params))
    },

    /**
     * Makes a call to the BallotTest smart contract using the `grantVotingRight(address)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    grantVotingRight: (params: CallParams<BallotTestArgs['obj']['grantVotingRight(address)void'] | BallotTestArgs['tuple']['grantVotingRight(address)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(BallotTestParamsFactory.grantVotingRight(params))
    },

    /**
     * Makes a call to the BallotTest smart contract using the `vote(address,uint512)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    vote: (params: CallParams<BallotTestArgs['obj']['vote(address,uint512)void'] | BallotTestArgs['tuple']['vote(address,uint512)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(BallotTestParamsFactory.vote(params))
    },

    /**
     * Makes a call to the BallotTest smart contract using the `winningProposal()(uint512,uint512)` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    winningProposal: (params: CallParams<BallotTestArgs['obj']['winningProposal()(uint512,uint512)'] | BallotTestArgs['tuple']['winningProposal()(uint512,uint512)']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(BallotTestParamsFactory.winningProposal(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Makes a clear_state call to an existing instance of the BallotTest smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the BallotTest smart contract using the `chairman()address` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    chairman: async (params: CallParams<BallotTestArgs['obj']['chairman()address'] | BallotTestArgs['tuple']['chairman()address']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(BallotTestParamsFactory.chairman(params))
      return {...result, return: result.return as unknown as (undefined | BallotTestReturns['chairman()address'])}
    },

    /**
     * Makes a call to the BallotTest smart contract using the `proposalCount()uint512` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    proposalCount: async (params: CallParams<BallotTestArgs['obj']['proposalCount()uint512'] | BallotTestArgs['tuple']['proposalCount()uint512']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(BallotTestParamsFactory.proposalCount(params))
      return {...result, return: result.return as unknown as (undefined | BallotTestReturns['proposalCount()uint512'])}
    },

    /**
     * Makes a call to the BallotTest smart contract using the `canVote(address)bool` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    canVote: async (params: CallParams<BallotTestArgs['obj']['canVote(address)bool'] | BallotTestArgs['tuple']['canVote(address)bool']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(BallotTestParamsFactory.canVote(params))
      return {...result, return: result.return as unknown as (undefined | BallotTestReturns['canVote(address)bool'])}
    },

    /**
     * Makes a call to the BallotTest smart contract using the `hasVoted(address)bool` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    hasVoted: async (params: CallParams<BallotTestArgs['obj']['hasVoted(address)bool'] | BallotTestArgs['tuple']['hasVoted(address)bool']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(BallotTestParamsFactory.hasVoted(params))
      return {...result, return: result.return as unknown as (undefined | BallotTestReturns['hasVoted(address)bool'])}
    },

    /**
     * Makes a call to the BallotTest smart contract using the `proposalVotes(uint512)uint512` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    proposalVotes: async (params: CallParams<BallotTestArgs['obj']['proposalVotes(uint512)uint512'] | BallotTestArgs['tuple']['proposalVotes(uint512)uint512']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(BallotTestParamsFactory.proposalVotes(params))
      return {...result, return: result.return as unknown as (undefined | BallotTestReturns['proposalVotes(uint512)uint512'])}
    },

    /**
     * Makes a call to the BallotTest smart contract using the `grantVotingRight(address)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    grantVotingRight: async (params: CallParams<BallotTestArgs['obj']['grantVotingRight(address)void'] | BallotTestArgs['tuple']['grantVotingRight(address)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(BallotTestParamsFactory.grantVotingRight(params))
      return {...result, return: result.return as unknown as (undefined | BallotTestReturns['grantVotingRight(address)void'])}
    },

    /**
     * Makes a call to the BallotTest smart contract using the `vote(address,uint512)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    vote: async (params: CallParams<BallotTestArgs['obj']['vote(address,uint512)void'] | BallotTestArgs['tuple']['vote(address,uint512)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(BallotTestParamsFactory.vote(params))
      return {...result, return: result.return as unknown as (undefined | BallotTestReturns['vote(address,uint512)void'])}
    },

    /**
     * Makes a call to the BallotTest smart contract using the `winningProposal()(uint512,uint512)` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    winningProposal: async (params: CallParams<BallotTestArgs['obj']['winningProposal()(uint512,uint512)'] | BallotTestArgs['tuple']['winningProposal()(uint512,uint512)']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(BallotTestParamsFactory.winningProposal(params))
      return {...result, return: result.return as unknown as (undefined | BallotTestReturns['winningProposal()(uint512,uint512)'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new BallotTestClient(this.appClient.clone(params))
  }

  /**
   * Makes a readonly (simulated) call to the BallotTest smart contract using the `chairman()address` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async chairman(params: CallParams<BallotTestArgs['obj']['chairman()address'] | BallotTestArgs['tuple']['chairman()address']> = {args: []}) {
    const result = await this.appClient.send.call(BallotTestParamsFactory.chairman(params))
    return result.return as unknown as BallotTestReturns['chairman()address']
  }

  /**
   * Makes a readonly (simulated) call to the BallotTest smart contract using the `proposalCount()uint512` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async proposalCount(params: CallParams<BallotTestArgs['obj']['proposalCount()uint512'] | BallotTestArgs['tuple']['proposalCount()uint512']> = {args: []}) {
    const result = await this.appClient.send.call(BallotTestParamsFactory.proposalCount(params))
    return result.return as unknown as BallotTestReturns['proposalCount()uint512']
  }

  /**
   * Makes a readonly (simulated) call to the BallotTest smart contract using the `canVote(address)bool` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async canVote(params: CallParams<BallotTestArgs['obj']['canVote(address)bool'] | BallotTestArgs['tuple']['canVote(address)bool']>) {
    const result = await this.appClient.send.call(BallotTestParamsFactory.canVote(params))
    return result.return as unknown as BallotTestReturns['canVote(address)bool']
  }

  /**
   * Makes a readonly (simulated) call to the BallotTest smart contract using the `hasVoted(address)bool` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async hasVoted(params: CallParams<BallotTestArgs['obj']['hasVoted(address)bool'] | BallotTestArgs['tuple']['hasVoted(address)bool']>) {
    const result = await this.appClient.send.call(BallotTestParamsFactory.hasVoted(params))
    return result.return as unknown as BallotTestReturns['hasVoted(address)bool']
  }

  /**
   * Makes a readonly (simulated) call to the BallotTest smart contract using the `proposalVotes(uint512)uint512` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async proposalVotes(params: CallParams<BallotTestArgs['obj']['proposalVotes(uint512)uint512'] | BallotTestArgs['tuple']['proposalVotes(uint512)uint512']>) {
    const result = await this.appClient.send.call(BallotTestParamsFactory.proposalVotes(params))
    return result.return as unknown as BallotTestReturns['proposalVotes(uint512)uint512']
  }

  /**
   * Makes a readonly (simulated) call to the BallotTest smart contract using the `winningProposal()(uint512,uint512)` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async winningProposal(params: CallParams<BallotTestArgs['obj']['winningProposal()(uint512,uint512)'] | BallotTestArgs['tuple']['winningProposal()(uint512,uint512)']> = {args: []}) {
    const result = await this.appClient.send.call(BallotTestParamsFactory.winningProposal(params))
    return result.return as unknown as BallotTestReturns['winningProposal()(uint512,uint512)']
  }

  /**
   * Methods to access state for the current BallotTest app
   */
  state = {
    /**
     * Methods to access global state for the current BallotTest app
     */
    global: {
      /**
       * Get all current keyed values from global state
       */
      getAll: async (): Promise<Partial<Expand<GlobalKeysState>>> => {
        const result = await this.appClient.state.global.getAll()
        return {
          _chairman: result._chairman,
          _proposalCount: new BinaryStateValue(result._proposalCount),
        }
      },
      /**
       * Get the current value of the _chairman key in global state
       */
      _chairman: async (): Promise<string | undefined> => { return (await this.appClient.state.global.getValue("_chairman")) as string | undefined },
      /**
       * Get the current value of the _proposalCount key in global state
       */
      _proposalCount: async (): Promise<BinaryState> => { return new BinaryStateValue((await this.appClient.state.global.getValue("_proposalCount")) as Uint8Array | undefined) },
    },
    /**
     * Methods to access box state for the current BallotTest app
     */
    box: {
      /**
       * Get all current keyed values from box state
       */
      getAll: async (): Promise<Partial<Expand<BoxKeysState>>> => {
        const result = await this.appClient.state.box.getAll()
        return {
        }
      },
      /**
       * Get values from the _hasVoted map in box state
       */
      _hasVoted: {
        /**
         * Get all current values of the _hasVoted map in box state
         */
        getMap: async (): Promise<Map<Uint8Array, bigint>> => { return (await this.appClient.state.box.getMap("_hasVoted")) as Map<Uint8Array, bigint> },
        /**
         * Get a current value of the _hasVoted map by key from box state
         */
        value: async (key: Uint8Array | string): Promise<bigint | undefined> => { return await this.appClient.state.box.getMapValue("_hasVoted", key) as bigint | undefined },
      },
      /**
       * Get values from the _canVote map in box state
       */
      _canVote: {
        /**
         * Get all current values of the _canVote map in box state
         */
        getMap: async (): Promise<Map<Uint8Array, bigint>> => { return (await this.appClient.state.box.getMap("_canVote")) as Map<Uint8Array, bigint> },
        /**
         * Get a current value of the _canVote map by key from box state
         */
        value: async (key: Uint8Array | string): Promise<bigint | undefined> => { return await this.appClient.state.box.getMapValue("_canVote", key) as bigint | undefined },
      },
      /**
       * Get values from the _proposalVotes map in box state
       */
      _proposalVotes: {
        /**
         * Get all current values of the _proposalVotes map in box state
         */
        getMap: async (): Promise<Map<Uint8Array, Uint8Array>> => { return (await this.appClient.state.box.getMap("_proposalVotes")) as Map<Uint8Array, Uint8Array> },
        /**
         * Get a current value of the _proposalVotes map by key from box state
         */
        value: async (key: Uint8Array | string): Promise<Uint8Array | undefined> => { return await this.appClient.state.box.getMapValue("_proposalVotes", key) as Uint8Array | undefined },
      },
    },
  }

  public newGroup(): BallotTestComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a chairman()address method call against the BallotTest contract
       */
      chairman(params: CallParams<BallotTestArgs['obj']['chairman()address'] | BallotTestArgs['tuple']['chairman()address']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.chairman(params)))
        resultMappers.push((v) => client.decodeReturnValue('chairman()address', v))
        return this
      },
      /**
       * Add a proposalCount()uint512 method call against the BallotTest contract
       */
      proposalCount(params: CallParams<BallotTestArgs['obj']['proposalCount()uint512'] | BallotTestArgs['tuple']['proposalCount()uint512']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.proposalCount(params)))
        resultMappers.push((v) => client.decodeReturnValue('proposalCount()uint512', v))
        return this
      },
      /**
       * Add a canVote(address)bool method call against the BallotTest contract
       */
      canVote(params: CallParams<BallotTestArgs['obj']['canVote(address)bool'] | BallotTestArgs['tuple']['canVote(address)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.canVote(params)))
        resultMappers.push((v) => client.decodeReturnValue('canVote(address)bool', v))
        return this
      },
      /**
       * Add a hasVoted(address)bool method call against the BallotTest contract
       */
      hasVoted(params: CallParams<BallotTestArgs['obj']['hasVoted(address)bool'] | BallotTestArgs['tuple']['hasVoted(address)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.hasVoted(params)))
        resultMappers.push((v) => client.decodeReturnValue('hasVoted(address)bool', v))
        return this
      },
      /**
       * Add a proposalVotes(uint512)uint512 method call against the BallotTest contract
       */
      proposalVotes(params: CallParams<BallotTestArgs['obj']['proposalVotes(uint512)uint512'] | BallotTestArgs['tuple']['proposalVotes(uint512)uint512']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.proposalVotes(params)))
        resultMappers.push((v) => client.decodeReturnValue('proposalVotes(uint512)uint512', v))
        return this
      },
      /**
       * Add a grantVotingRight(address)void method call against the BallotTest contract
       */
      grantVotingRight(params: CallParams<BallotTestArgs['obj']['grantVotingRight(address)void'] | BallotTestArgs['tuple']['grantVotingRight(address)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.grantVotingRight(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a vote(address,uint512)void method call against the BallotTest contract
       */
      vote(params: CallParams<BallotTestArgs['obj']['vote(address,uint512)void'] | BallotTestArgs['tuple']['vote(address,uint512)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.vote(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a winningProposal()(uint512,uint512) method call against the BallotTest contract
       */
      winningProposal(params: CallParams<BallotTestArgs['obj']['winningProposal()(uint512,uint512)'] | BallotTestArgs['tuple']['winningProposal()(uint512,uint512)']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.winningProposal(params)))
        resultMappers.push((v) => client.decodeReturnValue('winningProposal()(uint512,uint512)', v))
        return this
      },
      /**
       * Add a clear state call to the BallotTest contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as BallotTestComposer
  }
}
export type BallotTestComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the chairman()address ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  chairman(params?: CallParams<BallotTestArgs['obj']['chairman()address'] | BallotTestArgs['tuple']['chairman()address']>): BallotTestComposer<[...TReturns, BallotTestReturns['chairman()address'] | undefined]>

  /**
   * Calls the proposalCount()uint512 ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  proposalCount(params?: CallParams<BallotTestArgs['obj']['proposalCount()uint512'] | BallotTestArgs['tuple']['proposalCount()uint512']>): BallotTestComposer<[...TReturns, BallotTestReturns['proposalCount()uint512'] | undefined]>

  /**
   * Calls the canVote(address)bool ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  canVote(params?: CallParams<BallotTestArgs['obj']['canVote(address)bool'] | BallotTestArgs['tuple']['canVote(address)bool']>): BallotTestComposer<[...TReturns, BallotTestReturns['canVote(address)bool'] | undefined]>

  /**
   * Calls the hasVoted(address)bool ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  hasVoted(params?: CallParams<BallotTestArgs['obj']['hasVoted(address)bool'] | BallotTestArgs['tuple']['hasVoted(address)bool']>): BallotTestComposer<[...TReturns, BallotTestReturns['hasVoted(address)bool'] | undefined]>

  /**
   * Calls the proposalVotes(uint512)uint512 ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  proposalVotes(params?: CallParams<BallotTestArgs['obj']['proposalVotes(uint512)uint512'] | BallotTestArgs['tuple']['proposalVotes(uint512)uint512']>): BallotTestComposer<[...TReturns, BallotTestReturns['proposalVotes(uint512)uint512'] | undefined]>

  /**
   * Calls the grantVotingRight(address)void ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  grantVotingRight(params?: CallParams<BallotTestArgs['obj']['grantVotingRight(address)void'] | BallotTestArgs['tuple']['grantVotingRight(address)void']>): BallotTestComposer<[...TReturns, BallotTestReturns['grantVotingRight(address)void'] | undefined]>

  /**
   * Calls the vote(address,uint512)void ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  vote(params?: CallParams<BallotTestArgs['obj']['vote(address,uint512)void'] | BallotTestArgs['tuple']['vote(address,uint512)void']>): BallotTestComposer<[...TReturns, BallotTestReturns['vote(address,uint512)void'] | undefined]>

  /**
   * Calls the winningProposal()(uint512,uint512) ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  winningProposal(params?: CallParams<BallotTestArgs['obj']['winningProposal()(uint512,uint512)'] | BallotTestArgs['tuple']['winningProposal()(uint512,uint512)']>): BallotTestComposer<[...TReturns, BallotTestReturns['winningProposal()(uint512,uint512)'] | undefined]>

  /**
   * Makes a clear_state call to an existing instance of the BallotTest smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): BallotTestComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): BallotTestComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<BallotTestComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<BallotTestComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<BallotTestComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<BallotTestComposerResults<TReturns>>
}
export type BallotTestComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

