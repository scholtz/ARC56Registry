/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"EnumerableMapTest","structs":{"tuple":[{"name":"exists","type":"bool"},{"name":"value","type":"uint512"}]},"methods":[{"name":"set","args":[{"type":"uint512","name":"key"},{"type":"uint512","name":"value"}],"returns":{"type":"bool"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}},{"name":"remove","args":[{"type":"uint512","name":"key"}],"returns":{"type":"bool"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}},{"name":"contains","args":[{"type":"uint512","name":"key"}],"returns":{"type":"bool"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"length","args":[],"returns":{"type":"uint512"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"at","args":[{"type":"uint512","name":"index"}],"returns":{"type":"(uint512,uint512)","struct":"tuple"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"get","args":[{"type":"uint512","name":"key"}],"returns":{"type":"uint512"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"tryGet","args":[{"type":"uint512","name":"key"}],"returns":{"type":"(bool,uint512)","struct":"tuple"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}}],"arcs":[22,28],"networks":{},"state":{"schema":{"global":{"ints":0,"bytes":1},"local":{"ints":0,"bytes":0}},"keys":{"global":{"_length":{"keyType":"AVMString","valueType":"AVMBytes","key":"X2xlbmd0aA=="}},"local":{},"box":{}},"maps":{"global":{},"local":{},"box":{"_values":{"keyType":"AVMBytes","valueType":"AVMBytes","prefix":"X3ZhbHVlcw=="},"_indexOf":{"keyType":"AVMBytes","valueType":"AVMBytes","prefix":"X2luZGV4T2Y="},"_keyAtIndex":{"keyType":"AVMBytes","valueType":"AVMBytes","prefix":"X2tleUF0SW5kZXg="}}}},"bareActions":{"create":[],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[188,301,374,430,463],"errorMessage":"check _length exists"},{"pc":[471],"errorMessage":"index out of bounds"},{"pc":[135,143,250,392,453,550,605],"errorMessage":"invalid number of bytes for arc4.uint512"},{"pc":[576],"errorMessage":"key not found"},{"pc":[435,517,527,590,652],"errorMessage":"overflow"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"events":[]} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


// Type definitions for ARC-56 structs

export type Tuple = {
  exists: boolean,
  value: bigint
}


/**
 * Converts the ABI tuple representation of a tuple to the struct representation
 */
export function TupleFromTuple(abiTuple: [boolean, bigint]) {
  return getABIStructFromABITuple(abiTuple, APP_SPEC.structs.tuple, APP_SPEC.structs) as Tuple
}

/**
 * The argument types for the EnumerableMapTest contract
 */
export type EnumerableMapTestArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'set(uint512,uint512)bool': {
      key: bigint | number
      value: bigint | number
    }
    'remove(uint512)bool': {
      key: bigint | number
    }
    'contains(uint512)bool': {
      key: bigint | number
    }
    'length()uint512': Record<string, never>
    'at(uint512)(uint512,uint512)': {
      index: bigint | number
    }
    'get(uint512)uint512': {
      key: bigint | number
    }
    'tryGet(uint512)(bool,uint512)': {
      key: bigint | number
    }
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'set(uint512,uint512)bool': [key: bigint | number, value: bigint | number]
    'remove(uint512)bool': [key: bigint | number]
    'contains(uint512)bool': [key: bigint | number]
    'length()uint512': []
    'at(uint512)(uint512,uint512)': [index: bigint | number]
    'get(uint512)uint512': [key: bigint | number]
    'tryGet(uint512)(bool,uint512)': [key: bigint | number]
  }
}

/**
 * The return type for each method
 */
export type EnumerableMapTestReturns = {
  'set(uint512,uint512)bool': boolean
  'remove(uint512)bool': boolean
  'contains(uint512)bool': boolean
  'length()uint512': bigint
  'at(uint512)(uint512,uint512)': Tuple
  'get(uint512)uint512': bigint
  'tryGet(uint512)(bool,uint512)': Tuple
}

/**
 * Defines the types of available calls and state of the EnumerableMapTest smart contract.
 */
export type EnumerableMapTestTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'set(uint512,uint512)bool' | 'set', {
      argsObj: EnumerableMapTestArgs['obj']['set(uint512,uint512)bool']
      argsTuple: EnumerableMapTestArgs['tuple']['set(uint512,uint512)bool']
      returns: EnumerableMapTestReturns['set(uint512,uint512)bool']
    }>
    & Record<'remove(uint512)bool' | 'remove', {
      argsObj: EnumerableMapTestArgs['obj']['remove(uint512)bool']
      argsTuple: EnumerableMapTestArgs['tuple']['remove(uint512)bool']
      returns: EnumerableMapTestReturns['remove(uint512)bool']
    }>
    & Record<'contains(uint512)bool' | 'contains', {
      argsObj: EnumerableMapTestArgs['obj']['contains(uint512)bool']
      argsTuple: EnumerableMapTestArgs['tuple']['contains(uint512)bool']
      returns: EnumerableMapTestReturns['contains(uint512)bool']
    }>
    & Record<'length()uint512' | 'length', {
      argsObj: EnumerableMapTestArgs['obj']['length()uint512']
      argsTuple: EnumerableMapTestArgs['tuple']['length()uint512']
      returns: EnumerableMapTestReturns['length()uint512']
    }>
    & Record<'at(uint512)(uint512,uint512)' | 'at', {
      argsObj: EnumerableMapTestArgs['obj']['at(uint512)(uint512,uint512)']
      argsTuple: EnumerableMapTestArgs['tuple']['at(uint512)(uint512,uint512)']
      returns: EnumerableMapTestReturns['at(uint512)(uint512,uint512)']
    }>
    & Record<'get(uint512)uint512' | 'get', {
      argsObj: EnumerableMapTestArgs['obj']['get(uint512)uint512']
      argsTuple: EnumerableMapTestArgs['tuple']['get(uint512)uint512']
      returns: EnumerableMapTestReturns['get(uint512)uint512']
    }>
    & Record<'tryGet(uint512)(bool,uint512)' | 'tryGet', {
      argsObj: EnumerableMapTestArgs['obj']['tryGet(uint512)(bool,uint512)']
      argsTuple: EnumerableMapTestArgs['tuple']['tryGet(uint512)(bool,uint512)']
      returns: EnumerableMapTestReturns['tryGet(uint512)(bool,uint512)']
    }>
  /**
   * Defines the shape of the state of the application.
   */
  state: {
    global: {
      keys: {
        _length: BinaryState
      }
      maps: {}
    }
    box: {
      keys: {}
      maps: {
        _values: Map<Uint8Array | string, Uint8Array>
        _indexOf: Map<Uint8Array | string, Uint8Array>
        _keyAtIndex: Map<Uint8Array | string, Uint8Array>
      }
    }
  }
}

/**
 * Defines the possible abi call signatures.
 */
export type EnumerableMapTestSignatures = keyof EnumerableMapTestTypes['methods']
/**
 * Defines the possible abi call signatures for methods that return a non-void value.
 */
export type EnumerableMapTestNonVoidMethodSignatures = keyof EnumerableMapTestTypes['methods'] extends infer T ? T extends keyof EnumerableMapTestTypes['methods'] ? MethodReturn<T> extends void ? never : T  : never : never
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the EnumerableMapTest smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends EnumerableMapTestSignatures> = EnumerableMapTestTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the EnumerableMapTest smart contract to the method's return type
 */
export type MethodReturn<TSignature extends EnumerableMapTestSignatures> = EnumerableMapTestTypes['methods'][TSignature]['returns']

/**
 * Defines the shape of the keyed global state of the application.
 */
export type GlobalKeysState = EnumerableMapTestTypes['state']['global']['keys']

/**
 * Defines the shape of the keyed box state of the application.
 */
export type BoxKeysState = EnumerableMapTestTypes['state']['box']['keys']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the EnumerableMapTest smart contract
 */
export abstract class EnumerableMapTestParamsFactory {
  /**
   * Constructs a no op call for the set(uint512,uint512)bool ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static set(params: CallParams<EnumerableMapTestArgs['obj']['set(uint512,uint512)bool'] | EnumerableMapTestArgs['tuple']['set(uint512,uint512)bool']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'set(uint512,uint512)bool' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.key, params.args.value],
    }
  }
  /**
   * Constructs a no op call for the remove(uint512)bool ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static remove(params: CallParams<EnumerableMapTestArgs['obj']['remove(uint512)bool'] | EnumerableMapTestArgs['tuple']['remove(uint512)bool']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'remove(uint512)bool' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.key],
    }
  }
  /**
   * Constructs a no op call for the contains(uint512)bool ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static contains(params: CallParams<EnumerableMapTestArgs['obj']['contains(uint512)bool'] | EnumerableMapTestArgs['tuple']['contains(uint512)bool']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'contains(uint512)bool' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.key],
    }
  }
  /**
   * Constructs a no op call for the length()uint512 ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static length(params: CallParams<EnumerableMapTestArgs['obj']['length()uint512'] | EnumerableMapTestArgs['tuple']['length()uint512']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'length()uint512' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the at(uint512)(uint512,uint512) ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static at(params: CallParams<EnumerableMapTestArgs['obj']['at(uint512)(uint512,uint512)'] | EnumerableMapTestArgs['tuple']['at(uint512)(uint512,uint512)']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'at(uint512)(uint512,uint512)' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.index],
    }
  }
  /**
   * Constructs a no op call for the get(uint512)uint512 ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static get(params: CallParams<EnumerableMapTestArgs['obj']['get(uint512)uint512'] | EnumerableMapTestArgs['tuple']['get(uint512)uint512']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'get(uint512)uint512' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.key],
    }
  }
  /**
   * Constructs a no op call for the tryGet(uint512)(bool,uint512) ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static tryGet(params: CallParams<EnumerableMapTestArgs['obj']['tryGet(uint512)(bool,uint512)'] | EnumerableMapTestArgs['tuple']['tryGet(uint512)(bool,uint512)']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'tryGet(uint512)(bool,uint512)' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.key],
    }
  }
}

/**
 * A client to make calls to the EnumerableMapTest smart contract
 */
export class EnumerableMapTestClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `EnumerableMapTestClient`
   *
   * @param appClient An `AppClient` instance which has been created with the EnumerableMapTest app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `EnumerableMapTestClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Checks for decode errors on the given return value and maps the return value to the return type for the given method
   * @returns The typed return value or undefined if there was no value
   */
  decodeReturnValue<TSignature extends EnumerableMapTestNonVoidMethodSignatures>(method: TSignature, returnValue: ABIReturn | undefined) {
    return returnValue !== undefined ? getArc56ReturnValue<MethodReturn<TSignature>>(returnValue, this.appClient.getABIMethod(method), APP_SPEC.structs) : undefined
  }

  /**
   * Returns a new `EnumerableMapTestClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<EnumerableMapTestClient> {
    return new EnumerableMapTestClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `EnumerableMapTestClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<EnumerableMapTestClient> {
    return new EnumerableMapTestClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Makes a clear_state call to an existing instance of the EnumerableMapTest smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the EnumerableMapTest smart contract using the `set(uint512,uint512)bool` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    set: (params: CallParams<EnumerableMapTestArgs['obj']['set(uint512,uint512)bool'] | EnumerableMapTestArgs['tuple']['set(uint512,uint512)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(EnumerableMapTestParamsFactory.set(params))
    },

    /**
     * Makes a call to the EnumerableMapTest smart contract using the `remove(uint512)bool` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    remove: (params: CallParams<EnumerableMapTestArgs['obj']['remove(uint512)bool'] | EnumerableMapTestArgs['tuple']['remove(uint512)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(EnumerableMapTestParamsFactory.remove(params))
    },

    /**
     * Makes a call to the EnumerableMapTest smart contract using the `contains(uint512)bool` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    contains: (params: CallParams<EnumerableMapTestArgs['obj']['contains(uint512)bool'] | EnumerableMapTestArgs['tuple']['contains(uint512)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(EnumerableMapTestParamsFactory.contains(params))
    },

    /**
     * Makes a call to the EnumerableMapTest smart contract using the `length()uint512` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    length: (params: CallParams<EnumerableMapTestArgs['obj']['length()uint512'] | EnumerableMapTestArgs['tuple']['length()uint512']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(EnumerableMapTestParamsFactory.length(params))
    },

    /**
     * Makes a call to the EnumerableMapTest smart contract using the `at(uint512)(uint512,uint512)` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    at: (params: CallParams<EnumerableMapTestArgs['obj']['at(uint512)(uint512,uint512)'] | EnumerableMapTestArgs['tuple']['at(uint512)(uint512,uint512)']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(EnumerableMapTestParamsFactory.at(params))
    },

    /**
     * Makes a call to the EnumerableMapTest smart contract using the `get(uint512)uint512` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    get: (params: CallParams<EnumerableMapTestArgs['obj']['get(uint512)uint512'] | EnumerableMapTestArgs['tuple']['get(uint512)uint512']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(EnumerableMapTestParamsFactory.get(params))
    },

    /**
     * Makes a call to the EnumerableMapTest smart contract using the `tryGet(uint512)(bool,uint512)` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    tryGet: (params: CallParams<EnumerableMapTestArgs['obj']['tryGet(uint512)(bool,uint512)'] | EnumerableMapTestArgs['tuple']['tryGet(uint512)(bool,uint512)']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(EnumerableMapTestParamsFactory.tryGet(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Makes a clear_state call to an existing instance of the EnumerableMapTest smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the EnumerableMapTest smart contract using the `set(uint512,uint512)bool` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    set: (params: CallParams<EnumerableMapTestArgs['obj']['set(uint512,uint512)bool'] | EnumerableMapTestArgs['tuple']['set(uint512,uint512)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(EnumerableMapTestParamsFactory.set(params))
    },

    /**
     * Makes a call to the EnumerableMapTest smart contract using the `remove(uint512)bool` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    remove: (params: CallParams<EnumerableMapTestArgs['obj']['remove(uint512)bool'] | EnumerableMapTestArgs['tuple']['remove(uint512)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(EnumerableMapTestParamsFactory.remove(params))
    },

    /**
     * Makes a call to the EnumerableMapTest smart contract using the `contains(uint512)bool` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    contains: (params: CallParams<EnumerableMapTestArgs['obj']['contains(uint512)bool'] | EnumerableMapTestArgs['tuple']['contains(uint512)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(EnumerableMapTestParamsFactory.contains(params))
    },

    /**
     * Makes a call to the EnumerableMapTest smart contract using the `length()uint512` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    length: (params: CallParams<EnumerableMapTestArgs['obj']['length()uint512'] | EnumerableMapTestArgs['tuple']['length()uint512']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(EnumerableMapTestParamsFactory.length(params))
    },

    /**
     * Makes a call to the EnumerableMapTest smart contract using the `at(uint512)(uint512,uint512)` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    at: (params: CallParams<EnumerableMapTestArgs['obj']['at(uint512)(uint512,uint512)'] | EnumerableMapTestArgs['tuple']['at(uint512)(uint512,uint512)']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(EnumerableMapTestParamsFactory.at(params))
    },

    /**
     * Makes a call to the EnumerableMapTest smart contract using the `get(uint512)uint512` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    get: (params: CallParams<EnumerableMapTestArgs['obj']['get(uint512)uint512'] | EnumerableMapTestArgs['tuple']['get(uint512)uint512']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(EnumerableMapTestParamsFactory.get(params))
    },

    /**
     * Makes a call to the EnumerableMapTest smart contract using the `tryGet(uint512)(bool,uint512)` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    tryGet: (params: CallParams<EnumerableMapTestArgs['obj']['tryGet(uint512)(bool,uint512)'] | EnumerableMapTestArgs['tuple']['tryGet(uint512)(bool,uint512)']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(EnumerableMapTestParamsFactory.tryGet(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Makes a clear_state call to an existing instance of the EnumerableMapTest smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the EnumerableMapTest smart contract using the `set(uint512,uint512)bool` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    set: async (params: CallParams<EnumerableMapTestArgs['obj']['set(uint512,uint512)bool'] | EnumerableMapTestArgs['tuple']['set(uint512,uint512)bool']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(EnumerableMapTestParamsFactory.set(params))
      return {...result, return: result.return as unknown as (undefined | EnumerableMapTestReturns['set(uint512,uint512)bool'])}
    },

    /**
     * Makes a call to the EnumerableMapTest smart contract using the `remove(uint512)bool` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    remove: async (params: CallParams<EnumerableMapTestArgs['obj']['remove(uint512)bool'] | EnumerableMapTestArgs['tuple']['remove(uint512)bool']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(EnumerableMapTestParamsFactory.remove(params))
      return {...result, return: result.return as unknown as (undefined | EnumerableMapTestReturns['remove(uint512)bool'])}
    },

    /**
     * Makes a call to the EnumerableMapTest smart contract using the `contains(uint512)bool` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    contains: async (params: CallParams<EnumerableMapTestArgs['obj']['contains(uint512)bool'] | EnumerableMapTestArgs['tuple']['contains(uint512)bool']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(EnumerableMapTestParamsFactory.contains(params))
      return {...result, return: result.return as unknown as (undefined | EnumerableMapTestReturns['contains(uint512)bool'])}
    },

    /**
     * Makes a call to the EnumerableMapTest smart contract using the `length()uint512` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    length: async (params: CallParams<EnumerableMapTestArgs['obj']['length()uint512'] | EnumerableMapTestArgs['tuple']['length()uint512']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(EnumerableMapTestParamsFactory.length(params))
      return {...result, return: result.return as unknown as (undefined | EnumerableMapTestReturns['length()uint512'])}
    },

    /**
     * Makes a call to the EnumerableMapTest smart contract using the `at(uint512)(uint512,uint512)` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    at: async (params: CallParams<EnumerableMapTestArgs['obj']['at(uint512)(uint512,uint512)'] | EnumerableMapTestArgs['tuple']['at(uint512)(uint512,uint512)']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(EnumerableMapTestParamsFactory.at(params))
      return {...result, return: result.return as unknown as (undefined | EnumerableMapTestReturns['at(uint512)(uint512,uint512)'])}
    },

    /**
     * Makes a call to the EnumerableMapTest smart contract using the `get(uint512)uint512` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    get: async (params: CallParams<EnumerableMapTestArgs['obj']['get(uint512)uint512'] | EnumerableMapTestArgs['tuple']['get(uint512)uint512']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(EnumerableMapTestParamsFactory.get(params))
      return {...result, return: result.return as unknown as (undefined | EnumerableMapTestReturns['get(uint512)uint512'])}
    },

    /**
     * Makes a call to the EnumerableMapTest smart contract using the `tryGet(uint512)(bool,uint512)` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    tryGet: async (params: CallParams<EnumerableMapTestArgs['obj']['tryGet(uint512)(bool,uint512)'] | EnumerableMapTestArgs['tuple']['tryGet(uint512)(bool,uint512)']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(EnumerableMapTestParamsFactory.tryGet(params))
      return {...result, return: result.return as unknown as (undefined | EnumerableMapTestReturns['tryGet(uint512)(bool,uint512)'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new EnumerableMapTestClient(this.appClient.clone(params))
  }

  /**
   * Makes a readonly (simulated) call to the EnumerableMapTest smart contract using the `contains(uint512)bool` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async contains(params: CallParams<EnumerableMapTestArgs['obj']['contains(uint512)bool'] | EnumerableMapTestArgs['tuple']['contains(uint512)bool']>) {
    const result = await this.appClient.send.call(EnumerableMapTestParamsFactory.contains(params))
    return result.return as unknown as EnumerableMapTestReturns['contains(uint512)bool']
  }

  /**
   * Makes a readonly (simulated) call to the EnumerableMapTest smart contract using the `length()uint512` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async length(params: CallParams<EnumerableMapTestArgs['obj']['length()uint512'] | EnumerableMapTestArgs['tuple']['length()uint512']> = {args: []}) {
    const result = await this.appClient.send.call(EnumerableMapTestParamsFactory.length(params))
    return result.return as unknown as EnumerableMapTestReturns['length()uint512']
  }

  /**
   * Makes a readonly (simulated) call to the EnumerableMapTest smart contract using the `at(uint512)(uint512,uint512)` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async at(params: CallParams<EnumerableMapTestArgs['obj']['at(uint512)(uint512,uint512)'] | EnumerableMapTestArgs['tuple']['at(uint512)(uint512,uint512)']>) {
    const result = await this.appClient.send.call(EnumerableMapTestParamsFactory.at(params))
    return result.return as unknown as EnumerableMapTestReturns['at(uint512)(uint512,uint512)']
  }

  /**
   * Makes a readonly (simulated) call to the EnumerableMapTest smart contract using the `get(uint512)uint512` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async get(params: CallParams<EnumerableMapTestArgs['obj']['get(uint512)uint512'] | EnumerableMapTestArgs['tuple']['get(uint512)uint512']>) {
    const result = await this.appClient.send.call(EnumerableMapTestParamsFactory.get(params))
    return result.return as unknown as EnumerableMapTestReturns['get(uint512)uint512']
  }

  /**
   * Makes a readonly (simulated) call to the EnumerableMapTest smart contract using the `tryGet(uint512)(bool,uint512)` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async tryGet(params: CallParams<EnumerableMapTestArgs['obj']['tryGet(uint512)(bool,uint512)'] | EnumerableMapTestArgs['tuple']['tryGet(uint512)(bool,uint512)']>) {
    const result = await this.appClient.send.call(EnumerableMapTestParamsFactory.tryGet(params))
    return result.return as unknown as EnumerableMapTestReturns['tryGet(uint512)(bool,uint512)']
  }

  /**
   * Methods to access state for the current EnumerableMapTest app
   */
  state = {
    /**
     * Methods to access global state for the current EnumerableMapTest app
     */
    global: {
      /**
       * Get all current keyed values from global state
       */
      getAll: async (): Promise<Partial<Expand<GlobalKeysState>>> => {
        const result = await this.appClient.state.global.getAll()
        return {
          _length: new BinaryStateValue(result._length),
        }
      },
      /**
       * Get the current value of the _length key in global state
       */
      _length: async (): Promise<BinaryState> => { return new BinaryStateValue((await this.appClient.state.global.getValue("_length")) as Uint8Array | undefined) },
    },
    /**
     * Methods to access box state for the current EnumerableMapTest app
     */
    box: {
      /**
       * Get all current keyed values from box state
       */
      getAll: async (): Promise<Partial<Expand<BoxKeysState>>> => {
        const result = await this.appClient.state.box.getAll()
        return {
        }
      },
      /**
       * Get values from the _values map in box state
       */
      _values: {
        /**
         * Get all current values of the _values map in box state
         */
        getMap: async (): Promise<Map<Uint8Array, Uint8Array>> => { return (await this.appClient.state.box.getMap("_values")) as Map<Uint8Array, Uint8Array> },
        /**
         * Get a current value of the _values map by key from box state
         */
        value: async (key: Uint8Array | string): Promise<Uint8Array | undefined> => { return await this.appClient.state.box.getMapValue("_values", key) as Uint8Array | undefined },
      },
      /**
       * Get values from the _indexOf map in box state
       */
      _indexOf: {
        /**
         * Get all current values of the _indexOf map in box state
         */
        getMap: async (): Promise<Map<Uint8Array, Uint8Array>> => { return (await this.appClient.state.box.getMap("_indexOf")) as Map<Uint8Array, Uint8Array> },
        /**
         * Get a current value of the _indexOf map by key from box state
         */
        value: async (key: Uint8Array | string): Promise<Uint8Array | undefined> => { return await this.appClient.state.box.getMapValue("_indexOf", key) as Uint8Array | undefined },
      },
      /**
       * Get values from the _keyAtIndex map in box state
       */
      _keyAtIndex: {
        /**
         * Get all current values of the _keyAtIndex map in box state
         */
        getMap: async (): Promise<Map<Uint8Array, Uint8Array>> => { return (await this.appClient.state.box.getMap("_keyAtIndex")) as Map<Uint8Array, Uint8Array> },
        /**
         * Get a current value of the _keyAtIndex map by key from box state
         */
        value: async (key: Uint8Array | string): Promise<Uint8Array | undefined> => { return await this.appClient.state.box.getMapValue("_keyAtIndex", key) as Uint8Array | undefined },
      },
    },
  }

  public newGroup(): EnumerableMapTestComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a set(uint512,uint512)bool method call against the EnumerableMapTest contract
       */
      set(params: CallParams<EnumerableMapTestArgs['obj']['set(uint512,uint512)bool'] | EnumerableMapTestArgs['tuple']['set(uint512,uint512)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.set(params)))
        resultMappers.push((v) => client.decodeReturnValue('set(uint512,uint512)bool', v))
        return this
      },
      /**
       * Add a remove(uint512)bool method call against the EnumerableMapTest contract
       */
      remove(params: CallParams<EnumerableMapTestArgs['obj']['remove(uint512)bool'] | EnumerableMapTestArgs['tuple']['remove(uint512)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.remove(params)))
        resultMappers.push((v) => client.decodeReturnValue('remove(uint512)bool', v))
        return this
      },
      /**
       * Add a contains(uint512)bool method call against the EnumerableMapTest contract
       */
      contains(params: CallParams<EnumerableMapTestArgs['obj']['contains(uint512)bool'] | EnumerableMapTestArgs['tuple']['contains(uint512)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.contains(params)))
        resultMappers.push((v) => client.decodeReturnValue('contains(uint512)bool', v))
        return this
      },
      /**
       * Add a length()uint512 method call against the EnumerableMapTest contract
       */
      length(params: CallParams<EnumerableMapTestArgs['obj']['length()uint512'] | EnumerableMapTestArgs['tuple']['length()uint512']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.length(params)))
        resultMappers.push((v) => client.decodeReturnValue('length()uint512', v))
        return this
      },
      /**
       * Add a at(uint512)(uint512,uint512) method call against the EnumerableMapTest contract
       */
      at(params: CallParams<EnumerableMapTestArgs['obj']['at(uint512)(uint512,uint512)'] | EnumerableMapTestArgs['tuple']['at(uint512)(uint512,uint512)']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.at(params)))
        resultMappers.push((v) => client.decodeReturnValue('at(uint512)(uint512,uint512)', v))
        return this
      },
      /**
       * Add a get(uint512)uint512 method call against the EnumerableMapTest contract
       */
      get(params: CallParams<EnumerableMapTestArgs['obj']['get(uint512)uint512'] | EnumerableMapTestArgs['tuple']['get(uint512)uint512']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.get(params)))
        resultMappers.push((v) => client.decodeReturnValue('get(uint512)uint512', v))
        return this
      },
      /**
       * Add a tryGet(uint512)(bool,uint512) method call against the EnumerableMapTest contract
       */
      tryGet(params: CallParams<EnumerableMapTestArgs['obj']['tryGet(uint512)(bool,uint512)'] | EnumerableMapTestArgs['tuple']['tryGet(uint512)(bool,uint512)']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.tryGet(params)))
        resultMappers.push((v) => client.decodeReturnValue('tryGet(uint512)(bool,uint512)', v))
        return this
      },
      /**
       * Add a clear state call to the EnumerableMapTest contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as EnumerableMapTestComposer
  }
}
export type EnumerableMapTestComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the set(uint512,uint512)bool ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  set(params?: CallParams<EnumerableMapTestArgs['obj']['set(uint512,uint512)bool'] | EnumerableMapTestArgs['tuple']['set(uint512,uint512)bool']>): EnumerableMapTestComposer<[...TReturns, EnumerableMapTestReturns['set(uint512,uint512)bool'] | undefined]>

  /**
   * Calls the remove(uint512)bool ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  remove(params?: CallParams<EnumerableMapTestArgs['obj']['remove(uint512)bool'] | EnumerableMapTestArgs['tuple']['remove(uint512)bool']>): EnumerableMapTestComposer<[...TReturns, EnumerableMapTestReturns['remove(uint512)bool'] | undefined]>

  /**
   * Calls the contains(uint512)bool ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  contains(params?: CallParams<EnumerableMapTestArgs['obj']['contains(uint512)bool'] | EnumerableMapTestArgs['tuple']['contains(uint512)bool']>): EnumerableMapTestComposer<[...TReturns, EnumerableMapTestReturns['contains(uint512)bool'] | undefined]>

  /**
   * Calls the length()uint512 ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  length(params?: CallParams<EnumerableMapTestArgs['obj']['length()uint512'] | EnumerableMapTestArgs['tuple']['length()uint512']>): EnumerableMapTestComposer<[...TReturns, EnumerableMapTestReturns['length()uint512'] | undefined]>

  /**
   * Calls the at(uint512)(uint512,uint512) ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  at(params?: CallParams<EnumerableMapTestArgs['obj']['at(uint512)(uint512,uint512)'] | EnumerableMapTestArgs['tuple']['at(uint512)(uint512,uint512)']>): EnumerableMapTestComposer<[...TReturns, EnumerableMapTestReturns['at(uint512)(uint512,uint512)'] | undefined]>

  /**
   * Calls the get(uint512)uint512 ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  get(params?: CallParams<EnumerableMapTestArgs['obj']['get(uint512)uint512'] | EnumerableMapTestArgs['tuple']['get(uint512)uint512']>): EnumerableMapTestComposer<[...TReturns, EnumerableMapTestReturns['get(uint512)uint512'] | undefined]>

  /**
   * Calls the tryGet(uint512)(bool,uint512) ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  tryGet(params?: CallParams<EnumerableMapTestArgs['obj']['tryGet(uint512)(bool,uint512)'] | EnumerableMapTestArgs['tuple']['tryGet(uint512)(bool,uint512)']>): EnumerableMapTestComposer<[...TReturns, EnumerableMapTestReturns['tryGet(uint512)(bool,uint512)'] | undefined]>

  /**
   * Makes a clear_state call to an existing instance of the EnumerableMapTest smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): EnumerableMapTestComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): EnumerableMapTestComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<EnumerableMapTestComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<EnumerableMapTestComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<EnumerableMapTestComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<EnumerableMapTestComposerResults<TReturns>>
}
export type EnumerableMapTestComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

