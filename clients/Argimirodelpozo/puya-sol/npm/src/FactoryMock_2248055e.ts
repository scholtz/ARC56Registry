/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"FactoryMock","structs":{},"methods":[{"name":"setFeeTo","args":[{"type":"address","name":"_feeTo"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}},{"name":"setMigrator","args":[{"type":"address","name":"_migrator"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}},{"name":"feeTo","args":[],"returns":{"type":"address"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"migrator","args":[],"returns":{"type":"address"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}}],"arcs":[22,28],"desc":"@title Minimal Factory mock for SushiSwap V2 Pair testing.\n Provides feeTo() and migrator() that the Pair reads during mint/burn.","networks":{},"state":{"schema":{"global":{"ints":0,"bytes":2},"local":{"ints":0,"bytes":0}},"keys":{"global":{"feeTo":{"keyType":"AVMString","valueType":"address","key":"ZmVlVG8="},"migrator":{"keyType":"AVMString","valueType":"address","key":"bWlncmF0b3I="}},"local":{},"box":{}},"maps":{"global":{},"local":{},"box":{}}},"bareActions":{"create":[],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[146],"errorMessage":"check feeTo exists"},{"pc":[156],"errorMessage":"check migrator exists"},{"pc":[124,137],"errorMessage":"invalid number of bytes for arc4.static_array<arc4.uint8, 32>"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"events":[]} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


/**
 * The argument types for the FactoryMock contract
 */
export type FactoryMockArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'setFeeTo(address)void': {
      _feeTo: string
    }
    'setMigrator(address)void': {
      _migrator: string
    }
    'feeTo()address': Record<string, never>
    'migrator()address': Record<string, never>
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'setFeeTo(address)void': [_feeTo: string]
    'setMigrator(address)void': [_migrator: string]
    'feeTo()address': []
    'migrator()address': []
  }
}

/**
 * The return type for each method
 */
export type FactoryMockReturns = {
  'setFeeTo(address)void': void
  'setMigrator(address)void': void
  'feeTo()address': string
  'migrator()address': string
}

/**
 * Defines the types of available calls and state of the FactoryMock smart contract.
 */
export type FactoryMockTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'setFeeTo(address)void' | 'setFeeTo', {
      argsObj: FactoryMockArgs['obj']['setFeeTo(address)void']
      argsTuple: FactoryMockArgs['tuple']['setFeeTo(address)void']
      returns: FactoryMockReturns['setFeeTo(address)void']
    }>
    & Record<'setMigrator(address)void' | 'setMigrator', {
      argsObj: FactoryMockArgs['obj']['setMigrator(address)void']
      argsTuple: FactoryMockArgs['tuple']['setMigrator(address)void']
      returns: FactoryMockReturns['setMigrator(address)void']
    }>
    & Record<'feeTo()address' | 'feeTo', {
      argsObj: FactoryMockArgs['obj']['feeTo()address']
      argsTuple: FactoryMockArgs['tuple']['feeTo()address']
      returns: FactoryMockReturns['feeTo()address']
    }>
    & Record<'migrator()address' | 'migrator', {
      argsObj: FactoryMockArgs['obj']['migrator()address']
      argsTuple: FactoryMockArgs['tuple']['migrator()address']
      returns: FactoryMockReturns['migrator()address']
    }>
  /**
   * Defines the shape of the state of the application.
   */
  state: {
    global: {
      keys: {
        feeTo: string
        migrator: string
      }
      maps: {}
    }
  }
}

/**
 * Defines the possible abi call signatures.
 */
export type FactoryMockSignatures = keyof FactoryMockTypes['methods']
/**
 * Defines the possible abi call signatures for methods that return a non-void value.
 */
export type FactoryMockNonVoidMethodSignatures = keyof FactoryMockTypes['methods'] extends infer T ? T extends keyof FactoryMockTypes['methods'] ? MethodReturn<T> extends void ? never : T  : never : never
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the FactoryMock smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends FactoryMockSignatures> = FactoryMockTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the FactoryMock smart contract to the method's return type
 */
export type MethodReturn<TSignature extends FactoryMockSignatures> = FactoryMockTypes['methods'][TSignature]['returns']

/**
 * Defines the shape of the keyed global state of the application.
 */
export type GlobalKeysState = FactoryMockTypes['state']['global']['keys']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the FactoryMock smart contract
 */
export abstract class FactoryMockParamsFactory {
  /**
   * Constructs a no op call for the setFeeTo(address)void ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static setFeeTo(params: CallParams<FactoryMockArgs['obj']['setFeeTo(address)void'] | FactoryMockArgs['tuple']['setFeeTo(address)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'setFeeTo(address)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args._feeTo],
    }
  }
  /**
   * Constructs a no op call for the setMigrator(address)void ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static setMigrator(params: CallParams<FactoryMockArgs['obj']['setMigrator(address)void'] | FactoryMockArgs['tuple']['setMigrator(address)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'setMigrator(address)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args._migrator],
    }
  }
  /**
   * Constructs a no op call for the feeTo()address ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static feeTo(params: CallParams<FactoryMockArgs['obj']['feeTo()address'] | FactoryMockArgs['tuple']['feeTo()address']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'feeTo()address' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the migrator()address ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static migrator(params: CallParams<FactoryMockArgs['obj']['migrator()address'] | FactoryMockArgs['tuple']['migrator()address']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'migrator()address' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
}

/**
 * A client to make calls to the FactoryMock smart contract
 */
export class FactoryMockClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `FactoryMockClient`
   *
   * @param appClient An `AppClient` instance which has been created with the FactoryMock app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `FactoryMockClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Checks for decode errors on the given return value and maps the return value to the return type for the given method
   * @returns The typed return value or undefined if there was no value
   */
  decodeReturnValue<TSignature extends FactoryMockNonVoidMethodSignatures>(method: TSignature, returnValue: ABIReturn | undefined) {
    return returnValue !== undefined ? getArc56ReturnValue<MethodReturn<TSignature>>(returnValue, this.appClient.getABIMethod(method), APP_SPEC.structs) : undefined
  }

  /**
   * Returns a new `FactoryMockClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<FactoryMockClient> {
    return new FactoryMockClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `FactoryMockClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<FactoryMockClient> {
    return new FactoryMockClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Makes a clear_state call to an existing instance of the FactoryMock smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the FactoryMock smart contract using the `setFeeTo(address)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    setFeeTo: (params: CallParams<FactoryMockArgs['obj']['setFeeTo(address)void'] | FactoryMockArgs['tuple']['setFeeTo(address)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(FactoryMockParamsFactory.setFeeTo(params))
    },

    /**
     * Makes a call to the FactoryMock smart contract using the `setMigrator(address)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    setMigrator: (params: CallParams<FactoryMockArgs['obj']['setMigrator(address)void'] | FactoryMockArgs['tuple']['setMigrator(address)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(FactoryMockParamsFactory.setMigrator(params))
    },

    /**
     * Makes a call to the FactoryMock smart contract using the `feeTo()address` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    feeTo: (params: CallParams<FactoryMockArgs['obj']['feeTo()address'] | FactoryMockArgs['tuple']['feeTo()address']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(FactoryMockParamsFactory.feeTo(params))
    },

    /**
     * Makes a call to the FactoryMock smart contract using the `migrator()address` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    migrator: (params: CallParams<FactoryMockArgs['obj']['migrator()address'] | FactoryMockArgs['tuple']['migrator()address']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(FactoryMockParamsFactory.migrator(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Makes a clear_state call to an existing instance of the FactoryMock smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the FactoryMock smart contract using the `setFeeTo(address)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    setFeeTo: (params: CallParams<FactoryMockArgs['obj']['setFeeTo(address)void'] | FactoryMockArgs['tuple']['setFeeTo(address)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(FactoryMockParamsFactory.setFeeTo(params))
    },

    /**
     * Makes a call to the FactoryMock smart contract using the `setMigrator(address)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    setMigrator: (params: CallParams<FactoryMockArgs['obj']['setMigrator(address)void'] | FactoryMockArgs['tuple']['setMigrator(address)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(FactoryMockParamsFactory.setMigrator(params))
    },

    /**
     * Makes a call to the FactoryMock smart contract using the `feeTo()address` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    feeTo: (params: CallParams<FactoryMockArgs['obj']['feeTo()address'] | FactoryMockArgs['tuple']['feeTo()address']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(FactoryMockParamsFactory.feeTo(params))
    },

    /**
     * Makes a call to the FactoryMock smart contract using the `migrator()address` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    migrator: (params: CallParams<FactoryMockArgs['obj']['migrator()address'] | FactoryMockArgs['tuple']['migrator()address']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(FactoryMockParamsFactory.migrator(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Makes a clear_state call to an existing instance of the FactoryMock smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the FactoryMock smart contract using the `setFeeTo(address)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    setFeeTo: async (params: CallParams<FactoryMockArgs['obj']['setFeeTo(address)void'] | FactoryMockArgs['tuple']['setFeeTo(address)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(FactoryMockParamsFactory.setFeeTo(params))
      return {...result, return: result.return as unknown as (undefined | FactoryMockReturns['setFeeTo(address)void'])}
    },

    /**
     * Makes a call to the FactoryMock smart contract using the `setMigrator(address)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    setMigrator: async (params: CallParams<FactoryMockArgs['obj']['setMigrator(address)void'] | FactoryMockArgs['tuple']['setMigrator(address)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(FactoryMockParamsFactory.setMigrator(params))
      return {...result, return: result.return as unknown as (undefined | FactoryMockReturns['setMigrator(address)void'])}
    },

    /**
     * Makes a call to the FactoryMock smart contract using the `feeTo()address` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    feeTo: async (params: CallParams<FactoryMockArgs['obj']['feeTo()address'] | FactoryMockArgs['tuple']['feeTo()address']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(FactoryMockParamsFactory.feeTo(params))
      return {...result, return: result.return as unknown as (undefined | FactoryMockReturns['feeTo()address'])}
    },

    /**
     * Makes a call to the FactoryMock smart contract using the `migrator()address` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    migrator: async (params: CallParams<FactoryMockArgs['obj']['migrator()address'] | FactoryMockArgs['tuple']['migrator()address']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(FactoryMockParamsFactory.migrator(params))
      return {...result, return: result.return as unknown as (undefined | FactoryMockReturns['migrator()address'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new FactoryMockClient(this.appClient.clone(params))
  }

  /**
   * Makes a readonly (simulated) call to the FactoryMock smart contract using the `feeTo()address` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async feeTo(params: CallParams<FactoryMockArgs['obj']['feeTo()address'] | FactoryMockArgs['tuple']['feeTo()address']> = {args: []}) {
    const result = await this.appClient.send.call(FactoryMockParamsFactory.feeTo(params))
    return result.return as unknown as FactoryMockReturns['feeTo()address']
  }

  /**
   * Makes a readonly (simulated) call to the FactoryMock smart contract using the `migrator()address` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async migrator(params: CallParams<FactoryMockArgs['obj']['migrator()address'] | FactoryMockArgs['tuple']['migrator()address']> = {args: []}) {
    const result = await this.appClient.send.call(FactoryMockParamsFactory.migrator(params))
    return result.return as unknown as FactoryMockReturns['migrator()address']
  }

  /**
   * Methods to access state for the current FactoryMock app
   */
  state = {
    /**
     * Methods to access global state for the current FactoryMock app
     */
    global: {
      /**
       * Get all current keyed values from global state
       */
      getAll: async (): Promise<Partial<Expand<GlobalKeysState>>> => {
        const result = await this.appClient.state.global.getAll()
        return {
          feeTo: result.feeTo,
          migrator: result.migrator,
        }
      },
      /**
       * Get the current value of the feeTo key in global state
       */
      feeTo: async (): Promise<string | undefined> => { return (await this.appClient.state.global.getValue("feeTo")) as string | undefined },
      /**
       * Get the current value of the migrator key in global state
       */
      migrator: async (): Promise<string | undefined> => { return (await this.appClient.state.global.getValue("migrator")) as string | undefined },
    },
  }

  public newGroup(): FactoryMockComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a setFeeTo(address)void method call against the FactoryMock contract
       */
      setFeeTo(params: CallParams<FactoryMockArgs['obj']['setFeeTo(address)void'] | FactoryMockArgs['tuple']['setFeeTo(address)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.setFeeTo(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a setMigrator(address)void method call against the FactoryMock contract
       */
      setMigrator(params: CallParams<FactoryMockArgs['obj']['setMigrator(address)void'] | FactoryMockArgs['tuple']['setMigrator(address)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.setMigrator(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a feeTo()address method call against the FactoryMock contract
       */
      feeTo(params: CallParams<FactoryMockArgs['obj']['feeTo()address'] | FactoryMockArgs['tuple']['feeTo()address']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.feeTo(params)))
        resultMappers.push((v) => client.decodeReturnValue('feeTo()address', v))
        return this
      },
      /**
       * Add a migrator()address method call against the FactoryMock contract
       */
      migrator(params: CallParams<FactoryMockArgs['obj']['migrator()address'] | FactoryMockArgs['tuple']['migrator()address']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.migrator(params)))
        resultMappers.push((v) => client.decodeReturnValue('migrator()address', v))
        return this
      },
      /**
       * Add a clear state call to the FactoryMock contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as FactoryMockComposer
  }
}
export type FactoryMockComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the setFeeTo(address)void ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  setFeeTo(params?: CallParams<FactoryMockArgs['obj']['setFeeTo(address)void'] | FactoryMockArgs['tuple']['setFeeTo(address)void']>): FactoryMockComposer<[...TReturns, FactoryMockReturns['setFeeTo(address)void'] | undefined]>

  /**
   * Calls the setMigrator(address)void ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  setMigrator(params?: CallParams<FactoryMockArgs['obj']['setMigrator(address)void'] | FactoryMockArgs['tuple']['setMigrator(address)void']>): FactoryMockComposer<[...TReturns, FactoryMockReturns['setMigrator(address)void'] | undefined]>

  /**
   * Calls the feeTo()address ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  feeTo(params?: CallParams<FactoryMockArgs['obj']['feeTo()address'] | FactoryMockArgs['tuple']['feeTo()address']>): FactoryMockComposer<[...TReturns, FactoryMockReturns['feeTo()address'] | undefined]>

  /**
   * Calls the migrator()address ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  migrator(params?: CallParams<FactoryMockArgs['obj']['migrator()address'] | FactoryMockArgs['tuple']['migrator()address']>): FactoryMockComposer<[...TReturns, FactoryMockReturns['migrator()address'] | undefined]>

  /**
   * Makes a clear_state call to an existing instance of the FactoryMock smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): FactoryMockComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): FactoryMockComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<FactoryMockComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<FactoryMockComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<FactoryMockComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<FactoryMockComposerResults<TReturns>>
}
export type FactoryMockComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

