/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"PausableTest","structs":{},"methods":[{"name":"pause","args":[],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}},{"name":"unpause","args":[],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}},{"name":"doSomething","args":[],"returns":{"type":"uint512"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}},{"name":"paused","args":[],"returns":{"type":"bool"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}}],"arcs":[22,28],"networks":{},"state":{"schema":{"global":{"ints":1,"bytes":0},"local":{"ints":0,"bytes":0}},"keys":{"global":{"_paused":{"keyType":"AVMString","valueType":"AVMUint64","key":"X3BhdXNlZA=="}},"local":{},"box":{}},"maps":{"global":{},"local":{},"box":{}}},"bareActions":{"create":[],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[73,114],"errorMessage":"EnforcedPause"},{"pc":[93],"errorMessage":"ExpectedPause"},{"pc":[71,92,112,191],"errorMessage":"check _paused exists"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"events":[]} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


/**
 * The argument types for the PausableTest contract
 */
export type PausableTestArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'pause()void': Record<string, never>
    'unpause()void': Record<string, never>
    'doSomething()uint512': Record<string, never>
    'paused()bool': Record<string, never>
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'pause()void': []
    'unpause()void': []
    'doSomething()uint512': []
    'paused()bool': []
  }
}

/**
 * The return type for each method
 */
export type PausableTestReturns = {
  'pause()void': void
  'unpause()void': void
  'doSomething()uint512': bigint
  'paused()bool': boolean
}

/**
 * Defines the types of available calls and state of the PausableTest smart contract.
 */
export type PausableTestTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'pause()void' | 'pause', {
      argsObj: PausableTestArgs['obj']['pause()void']
      argsTuple: PausableTestArgs['tuple']['pause()void']
      returns: PausableTestReturns['pause()void']
    }>
    & Record<'unpause()void' | 'unpause', {
      argsObj: PausableTestArgs['obj']['unpause()void']
      argsTuple: PausableTestArgs['tuple']['unpause()void']
      returns: PausableTestReturns['unpause()void']
    }>
    & Record<'doSomething()uint512' | 'doSomething', {
      argsObj: PausableTestArgs['obj']['doSomething()uint512']
      argsTuple: PausableTestArgs['tuple']['doSomething()uint512']
      returns: PausableTestReturns['doSomething()uint512']
    }>
    & Record<'paused()bool' | 'paused', {
      argsObj: PausableTestArgs['obj']['paused()bool']
      argsTuple: PausableTestArgs['tuple']['paused()bool']
      returns: PausableTestReturns['paused()bool']
    }>
  /**
   * Defines the shape of the state of the application.
   */
  state: {
    global: {
      keys: {
        _paused: bigint
      }
      maps: {}
    }
  }
}

/**
 * Defines the possible abi call signatures.
 */
export type PausableTestSignatures = keyof PausableTestTypes['methods']
/**
 * Defines the possible abi call signatures for methods that return a non-void value.
 */
export type PausableTestNonVoidMethodSignatures = keyof PausableTestTypes['methods'] extends infer T ? T extends keyof PausableTestTypes['methods'] ? MethodReturn<T> extends void ? never : T  : never : never
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the PausableTest smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends PausableTestSignatures> = PausableTestTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the PausableTest smart contract to the method's return type
 */
export type MethodReturn<TSignature extends PausableTestSignatures> = PausableTestTypes['methods'][TSignature]['returns']

/**
 * Defines the shape of the keyed global state of the application.
 */
export type GlobalKeysState = PausableTestTypes['state']['global']['keys']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the PausableTest smart contract
 */
export abstract class PausableTestParamsFactory {
  /**
   * Constructs a no op call for the pause()void ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static pause(params: CallParams<PausableTestArgs['obj']['pause()void'] | PausableTestArgs['tuple']['pause()void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'pause()void' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the unpause()void ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static unpause(params: CallParams<PausableTestArgs['obj']['unpause()void'] | PausableTestArgs['tuple']['unpause()void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'unpause()void' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the doSomething()uint512 ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static doSomething(params: CallParams<PausableTestArgs['obj']['doSomething()uint512'] | PausableTestArgs['tuple']['doSomething()uint512']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'doSomething()uint512' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the paused()bool ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static paused(params: CallParams<PausableTestArgs['obj']['paused()bool'] | PausableTestArgs['tuple']['paused()bool']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'paused()bool' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
}

/**
 * A client to make calls to the PausableTest smart contract
 */
export class PausableTestClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `PausableTestClient`
   *
   * @param appClient An `AppClient` instance which has been created with the PausableTest app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `PausableTestClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Checks for decode errors on the given return value and maps the return value to the return type for the given method
   * @returns The typed return value or undefined if there was no value
   */
  decodeReturnValue<TSignature extends PausableTestNonVoidMethodSignatures>(method: TSignature, returnValue: ABIReturn | undefined) {
    return returnValue !== undefined ? getArc56ReturnValue<MethodReturn<TSignature>>(returnValue, this.appClient.getABIMethod(method), APP_SPEC.structs) : undefined
  }

  /**
   * Returns a new `PausableTestClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<PausableTestClient> {
    return new PausableTestClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `PausableTestClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<PausableTestClient> {
    return new PausableTestClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Makes a clear_state call to an existing instance of the PausableTest smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the PausableTest smart contract using the `pause()void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    pause: (params: CallParams<PausableTestArgs['obj']['pause()void'] | PausableTestArgs['tuple']['pause()void']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(PausableTestParamsFactory.pause(params))
    },

    /**
     * Makes a call to the PausableTest smart contract using the `unpause()void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    unpause: (params: CallParams<PausableTestArgs['obj']['unpause()void'] | PausableTestArgs['tuple']['unpause()void']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(PausableTestParamsFactory.unpause(params))
    },

    /**
     * Makes a call to the PausableTest smart contract using the `doSomething()uint512` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    doSomething: (params: CallParams<PausableTestArgs['obj']['doSomething()uint512'] | PausableTestArgs['tuple']['doSomething()uint512']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(PausableTestParamsFactory.doSomething(params))
    },

    /**
     * Makes a call to the PausableTest smart contract using the `paused()bool` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    paused: (params: CallParams<PausableTestArgs['obj']['paused()bool'] | PausableTestArgs['tuple']['paused()bool']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(PausableTestParamsFactory.paused(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Makes a clear_state call to an existing instance of the PausableTest smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the PausableTest smart contract using the `pause()void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    pause: (params: CallParams<PausableTestArgs['obj']['pause()void'] | PausableTestArgs['tuple']['pause()void']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(PausableTestParamsFactory.pause(params))
    },

    /**
     * Makes a call to the PausableTest smart contract using the `unpause()void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    unpause: (params: CallParams<PausableTestArgs['obj']['unpause()void'] | PausableTestArgs['tuple']['unpause()void']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(PausableTestParamsFactory.unpause(params))
    },

    /**
     * Makes a call to the PausableTest smart contract using the `doSomething()uint512` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    doSomething: (params: CallParams<PausableTestArgs['obj']['doSomething()uint512'] | PausableTestArgs['tuple']['doSomething()uint512']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(PausableTestParamsFactory.doSomething(params))
    },

    /**
     * Makes a call to the PausableTest smart contract using the `paused()bool` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    paused: (params: CallParams<PausableTestArgs['obj']['paused()bool'] | PausableTestArgs['tuple']['paused()bool']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(PausableTestParamsFactory.paused(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Makes a clear_state call to an existing instance of the PausableTest smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the PausableTest smart contract using the `pause()void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    pause: async (params: CallParams<PausableTestArgs['obj']['pause()void'] | PausableTestArgs['tuple']['pause()void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(PausableTestParamsFactory.pause(params))
      return {...result, return: result.return as unknown as (undefined | PausableTestReturns['pause()void'])}
    },

    /**
     * Makes a call to the PausableTest smart contract using the `unpause()void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    unpause: async (params: CallParams<PausableTestArgs['obj']['unpause()void'] | PausableTestArgs['tuple']['unpause()void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(PausableTestParamsFactory.unpause(params))
      return {...result, return: result.return as unknown as (undefined | PausableTestReturns['unpause()void'])}
    },

    /**
     * Makes a call to the PausableTest smart contract using the `doSomething()uint512` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    doSomething: async (params: CallParams<PausableTestArgs['obj']['doSomething()uint512'] | PausableTestArgs['tuple']['doSomething()uint512']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(PausableTestParamsFactory.doSomething(params))
      return {...result, return: result.return as unknown as (undefined | PausableTestReturns['doSomething()uint512'])}
    },

    /**
     * Makes a call to the PausableTest smart contract using the `paused()bool` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    paused: async (params: CallParams<PausableTestArgs['obj']['paused()bool'] | PausableTestArgs['tuple']['paused()bool']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(PausableTestParamsFactory.paused(params))
      return {...result, return: result.return as unknown as (undefined | PausableTestReturns['paused()bool'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new PausableTestClient(this.appClient.clone(params))
  }

  /**
   * Makes a readonly (simulated) call to the PausableTest smart contract using the `paused()bool` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async paused(params: CallParams<PausableTestArgs['obj']['paused()bool'] | PausableTestArgs['tuple']['paused()bool']> = {args: []}) {
    const result = await this.appClient.send.call(PausableTestParamsFactory.paused(params))
    return result.return as unknown as PausableTestReturns['paused()bool']
  }

  /**
   * Methods to access state for the current PausableTest app
   */
  state = {
    /**
     * Methods to access global state for the current PausableTest app
     */
    global: {
      /**
       * Get all current keyed values from global state
       */
      getAll: async (): Promise<Partial<Expand<GlobalKeysState>>> => {
        const result = await this.appClient.state.global.getAll()
        return {
          _paused: result._paused,
        }
      },
      /**
       * Get the current value of the _paused key in global state
       */
      _paused: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("_paused")) as bigint | undefined },
    },
  }

  public newGroup(): PausableTestComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a pause()void method call against the PausableTest contract
       */
      pause(params: CallParams<PausableTestArgs['obj']['pause()void'] | PausableTestArgs['tuple']['pause()void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.pause(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a unpause()void method call against the PausableTest contract
       */
      unpause(params: CallParams<PausableTestArgs['obj']['unpause()void'] | PausableTestArgs['tuple']['unpause()void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.unpause(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a doSomething()uint512 method call against the PausableTest contract
       */
      doSomething(params: CallParams<PausableTestArgs['obj']['doSomething()uint512'] | PausableTestArgs['tuple']['doSomething()uint512']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.doSomething(params)))
        resultMappers.push((v) => client.decodeReturnValue('doSomething()uint512', v))
        return this
      },
      /**
       * Add a paused()bool method call against the PausableTest contract
       */
      paused(params: CallParams<PausableTestArgs['obj']['paused()bool'] | PausableTestArgs['tuple']['paused()bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.paused(params)))
        resultMappers.push((v) => client.decodeReturnValue('paused()bool', v))
        return this
      },
      /**
       * Add a clear state call to the PausableTest contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as PausableTestComposer
  }
}
export type PausableTestComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the pause()void ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  pause(params?: CallParams<PausableTestArgs['obj']['pause()void'] | PausableTestArgs['tuple']['pause()void']>): PausableTestComposer<[...TReturns, PausableTestReturns['pause()void'] | undefined]>

  /**
   * Calls the unpause()void ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  unpause(params?: CallParams<PausableTestArgs['obj']['unpause()void'] | PausableTestArgs['tuple']['unpause()void']>): PausableTestComposer<[...TReturns, PausableTestReturns['unpause()void'] | undefined]>

  /**
   * Calls the doSomething()uint512 ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  doSomething(params?: CallParams<PausableTestArgs['obj']['doSomething()uint512'] | PausableTestArgs['tuple']['doSomething()uint512']>): PausableTestComposer<[...TReturns, PausableTestReturns['doSomething()uint512'] | undefined]>

  /**
   * Calls the paused()bool ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  paused(params?: CallParams<PausableTestArgs['obj']['paused()bool'] | PausableTestArgs['tuple']['paused()bool']>): PausableTestComposer<[...TReturns, PausableTestReturns['paused()bool'] | undefined]>

  /**
   * Makes a clear_state call to an existing instance of the PausableTest smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): PausableTestComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): PausableTestComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<PausableTestComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<PausableTestComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<PausableTestComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<PausableTestComposerResults<TReturns>>
}
export type PausableTestComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

