/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"Governor","structs":{},"methods":[{"name":"propose","args":[{"type":"string","name":"description"}],"returns":{"type":"uint512"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Create a new proposal. Proposer must have enough voting power.","events":[],"recommendations":{}},{"name":"castVote","args":[{"type":"uint512","name":"proposalId"},{"type":"uint64","name":"support"}],"returns":{"type":"uint512"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Cast a vote. Support: 0=Against, 1=For, 2=Abstain.","events":[],"recommendations":{}},{"name":"execute","args":[{"type":"uint512","name":"proposalId"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Execute a succeeded proposal.","events":[],"recommendations":{}},{"name":"cancel","args":[{"type":"uint512","name":"proposalId"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Cancel a proposal. Only proposer or admin can cancel.","events":[],"recommendations":{}},{"name":"getProposal","args":[{"type":"uint512","name":"proposalId"}],"returns":{"type":"(address,uint512,uint512,uint512,bool,bool)"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"getReceipt","args":[{"type":"uint512","name":"proposalId"},{"type":"address","name":"voter"}],"returns":{"type":"(bool,uint512,uint512)"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"getProposalCount","args":[],"returns":{"type":"uint512"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}}],"arcs":[22,28],"networks":{},"state":{"schema":{"global":{"ints":0,"bytes":5},"local":{"ints":0,"bytes":0}},"keys":{"global":{"token":{"keyType":"AVMString","valueType":"address","key":"dG9rZW4="},"admin":{"keyType":"AVMString","valueType":"address","key":"YWRtaW4="},"proposalCount":{"keyType":"AVMString","valueType":"AVMBytes","key":"cHJvcG9zYWxDb3VudA=="},"quorumVotes":{"keyType":"AVMString","valueType":"AVMBytes","key":"cXVvcnVtVm90ZXM="},"proposalThreshold":{"keyType":"AVMString","valueType":"AVMBytes","key":"cHJvcG9zYWxUaHJlc2hvbGQ="}},"local":{},"box":{}},"maps":{"global":{},"local":{},"box":{"_proposalProposer":{"keyType":"AVMBytes","valueType":"address","prefix":"X3Byb3Bvc2FsUHJvcG9zZXI="},"_proposalForVotes":{"keyType":"AVMBytes","valueType":"AVMBytes","prefix":"X3Byb3Bvc2FsRm9yVm90ZXM="},"_proposalAgainstVotes":{"keyType":"AVMBytes","valueType":"AVMBytes","prefix":"X3Byb3Bvc2FsQWdhaW5zdFZvdGVz"},"_proposalAbstainVotes":{"keyType":"AVMBytes","valueType":"AVMBytes","prefix":"X3Byb3Bvc2FsQWJzdGFpblZvdGVz"},"_proposalExecuted":{"keyType":"AVMBytes","valueType":"uint64","prefix":"X3Byb3Bvc2FsRXhlY3V0ZWQ="},"_proposalCancelled":{"keyType":"AVMBytes","valueType":"uint64","prefix":"X3Byb3Bvc2FsQ2FuY2VsbGVk"},"_receiptHasVoted":{"keyType":"AVMBytes","valueType":"uint64","prefix":"X3JlY2VpcHRIYXNWb3RlZA=="},"_receiptSupport":{"keyType":"AVMBytes","valueType":"AVMBytes","prefix":"X3JlY2VpcHRTdXBwb3J0"},"_receiptVotes":{"keyType":"AVMBytes","valueType":"AVMBytes","prefix":"X3JlY2VpcHRWb3Rlcw=="}}}},"bareActions":{"create":[],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[996],"errorMessage":"already cancelled"},{"pc":[850,978],"errorMessage":"already executed"},{"pc":[590],"errorMessage":"already voted"},{"pc":[445],"errorMessage":"below proposal threshold"},{"pc":[1020],"errorMessage":"check admin exists"},{"pc":[449,540,814,943,1300],"errorMessage":"check proposalCount exists"},{"pc":[443],"errorMessage":"check proposalThreshold exists"},{"pc":[905],"errorMessage":"check quorumVotes exists"},{"pc":[411,597],"errorMessage":"check token exists"},{"pc":[394],"errorMessage":"invalid array length header"},{"pc":[401],"errorMessage":"invalid number of bytes for arc4.dynamic_array<arc4.uint8>"},{"pc":[1211],"errorMessage":"invalid number of bytes for arc4.static_array<arc4.uint8, 32>"},{"pc":[516,804,933,1057,1202],"errorMessage":"invalid number of bytes for arc4.uint512"},{"pc":[525],"errorMessage":"invalid number of bytes for arc4.uint64"},{"pc":[548,822,951],"errorMessage":"invalid proposal"},{"pc":[554],"errorMessage":"invalid vote type"},{"pc":[632],"errorMessage":"no voting power"},{"pc":[1026],"errorMessage":"not proposer or admin"},{"pc":[497,727,1143,1158,1170,1273,1285,1305],"errorMessage":"overflow"},{"pc":[865],"errorMessage":"proposal cancelled"},{"pc":[889],"errorMessage":"proposal not succeeded"},{"pc":[907],"errorMessage":"quorum not reached"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"events":[]} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


/**
 * The argument types for the Governor contract
 */
export type GovernorArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'propose(string)uint512': {
      description: string
    }
    'castVote(uint512,uint64)uint512': {
      proposalId: bigint | number
      support: bigint | number
    }
    'execute(uint512)void': {
      proposalId: bigint | number
    }
    'cancel(uint512)void': {
      proposalId: bigint | number
    }
    'getProposal(uint512)(address,uint512,uint512,uint512,bool,bool)': {
      proposalId: bigint | number
    }
    'getReceipt(uint512,address)(bool,uint512,uint512)': {
      proposalId: bigint | number
      voter: string
    }
    'getProposalCount()uint512': Record<string, never>
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'propose(string)uint512': [description: string]
    'castVote(uint512,uint64)uint512': [proposalId: bigint | number, support: bigint | number]
    'execute(uint512)void': [proposalId: bigint | number]
    'cancel(uint512)void': [proposalId: bigint | number]
    'getProposal(uint512)(address,uint512,uint512,uint512,bool,bool)': [proposalId: bigint | number]
    'getReceipt(uint512,address)(bool,uint512,uint512)': [proposalId: bigint | number, voter: string]
    'getProposalCount()uint512': []
  }
}

/**
 * The return type for each method
 */
export type GovernorReturns = {
  'propose(string)uint512': bigint
  'castVote(uint512,uint64)uint512': bigint
  'execute(uint512)void': void
  'cancel(uint512)void': void
  'getProposal(uint512)(address,uint512,uint512,uint512,bool,bool)': [string, bigint, bigint, bigint, boolean, boolean]
  'getReceipt(uint512,address)(bool,uint512,uint512)': [boolean, bigint, bigint]
  'getProposalCount()uint512': bigint
}

/**
 * Defines the types of available calls and state of the Governor smart contract.
 */
export type GovernorTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'propose(string)uint512' | 'propose', {
      argsObj: GovernorArgs['obj']['propose(string)uint512']
      argsTuple: GovernorArgs['tuple']['propose(string)uint512']
      returns: GovernorReturns['propose(string)uint512']
    }>
    & Record<'castVote(uint512,uint64)uint512' | 'castVote', {
      argsObj: GovernorArgs['obj']['castVote(uint512,uint64)uint512']
      argsTuple: GovernorArgs['tuple']['castVote(uint512,uint64)uint512']
      returns: GovernorReturns['castVote(uint512,uint64)uint512']
    }>
    & Record<'execute(uint512)void' | 'execute', {
      argsObj: GovernorArgs['obj']['execute(uint512)void']
      argsTuple: GovernorArgs['tuple']['execute(uint512)void']
      returns: GovernorReturns['execute(uint512)void']
    }>
    & Record<'cancel(uint512)void' | 'cancel', {
      argsObj: GovernorArgs['obj']['cancel(uint512)void']
      argsTuple: GovernorArgs['tuple']['cancel(uint512)void']
      returns: GovernorReturns['cancel(uint512)void']
    }>
    & Record<'getProposal(uint512)(address,uint512,uint512,uint512,bool,bool)' | 'getProposal', {
      argsObj: GovernorArgs['obj']['getProposal(uint512)(address,uint512,uint512,uint512,bool,bool)']
      argsTuple: GovernorArgs['tuple']['getProposal(uint512)(address,uint512,uint512,uint512,bool,bool)']
      returns: GovernorReturns['getProposal(uint512)(address,uint512,uint512,uint512,bool,bool)']
    }>
    & Record<'getReceipt(uint512,address)(bool,uint512,uint512)' | 'getReceipt', {
      argsObj: GovernorArgs['obj']['getReceipt(uint512,address)(bool,uint512,uint512)']
      argsTuple: GovernorArgs['tuple']['getReceipt(uint512,address)(bool,uint512,uint512)']
      returns: GovernorReturns['getReceipt(uint512,address)(bool,uint512,uint512)']
    }>
    & Record<'getProposalCount()uint512' | 'getProposalCount', {
      argsObj: GovernorArgs['obj']['getProposalCount()uint512']
      argsTuple: GovernorArgs['tuple']['getProposalCount()uint512']
      returns: GovernorReturns['getProposalCount()uint512']
    }>
  /**
   * Defines the shape of the state of the application.
   */
  state: {
    global: {
      keys: {
        token: string
        admin: string
        proposalCount: BinaryState
        quorumVotes: BinaryState
        proposalThreshold: BinaryState
      }
      maps: {}
    }
    box: {
      keys: {}
      maps: {
        _proposalProposer: Map<Uint8Array | string, string>
        _proposalForVotes: Map<Uint8Array | string, Uint8Array>
        _proposalAgainstVotes: Map<Uint8Array | string, Uint8Array>
        _proposalAbstainVotes: Map<Uint8Array | string, Uint8Array>
        _proposalExecuted: Map<Uint8Array | string, bigint>
        _proposalCancelled: Map<Uint8Array | string, bigint>
        _receiptHasVoted: Map<Uint8Array | string, bigint>
        _receiptSupport: Map<Uint8Array | string, Uint8Array>
        _receiptVotes: Map<Uint8Array | string, Uint8Array>
      }
    }
  }
}

/**
 * Defines the possible abi call signatures.
 */
export type GovernorSignatures = keyof GovernorTypes['methods']
/**
 * Defines the possible abi call signatures for methods that return a non-void value.
 */
export type GovernorNonVoidMethodSignatures = keyof GovernorTypes['methods'] extends infer T ? T extends keyof GovernorTypes['methods'] ? MethodReturn<T> extends void ? never : T  : never : never
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the Governor smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends GovernorSignatures> = GovernorTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the Governor smart contract to the method's return type
 */
export type MethodReturn<TSignature extends GovernorSignatures> = GovernorTypes['methods'][TSignature]['returns']

/**
 * Defines the shape of the keyed global state of the application.
 */
export type GlobalKeysState = GovernorTypes['state']['global']['keys']

/**
 * Defines the shape of the keyed box state of the application.
 */
export type BoxKeysState = GovernorTypes['state']['box']['keys']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the Governor smart contract
 */
export abstract class GovernorParamsFactory {
  /**
   * Constructs a no op call for the propose(string)uint512 ABI method
   *
   * Create a new proposal. Proposer must have enough voting power.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static propose(params: CallParams<GovernorArgs['obj']['propose(string)uint512'] | GovernorArgs['tuple']['propose(string)uint512']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'propose(string)uint512' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.description],
    }
  }
  /**
   * Constructs a no op call for the castVote(uint512,uint64)uint512 ABI method
   *
   * Cast a vote. Support: 0=Against, 1=For, 2=Abstain.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static castVote(params: CallParams<GovernorArgs['obj']['castVote(uint512,uint64)uint512'] | GovernorArgs['tuple']['castVote(uint512,uint64)uint512']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'castVote(uint512,uint64)uint512' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.proposalId, params.args.support],
    }
  }
  /**
   * Constructs a no op call for the execute(uint512)void ABI method
   *
   * Execute a succeeded proposal.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static execute(params: CallParams<GovernorArgs['obj']['execute(uint512)void'] | GovernorArgs['tuple']['execute(uint512)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'execute(uint512)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.proposalId],
    }
  }
  /**
   * Constructs a no op call for the cancel(uint512)void ABI method
   *
   * Cancel a proposal. Only proposer or admin can cancel.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static cancel(params: CallParams<GovernorArgs['obj']['cancel(uint512)void'] | GovernorArgs['tuple']['cancel(uint512)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'cancel(uint512)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.proposalId],
    }
  }
  /**
   * Constructs a no op call for the getProposal(uint512)(address,uint512,uint512,uint512,bool,bool) ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static getProposal(params: CallParams<GovernorArgs['obj']['getProposal(uint512)(address,uint512,uint512,uint512,bool,bool)'] | GovernorArgs['tuple']['getProposal(uint512)(address,uint512,uint512,uint512,bool,bool)']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'getProposal(uint512)(address,uint512,uint512,uint512,bool,bool)' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.proposalId],
    }
  }
  /**
   * Constructs a no op call for the getReceipt(uint512,address)(bool,uint512,uint512) ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static getReceipt(params: CallParams<GovernorArgs['obj']['getReceipt(uint512,address)(bool,uint512,uint512)'] | GovernorArgs['tuple']['getReceipt(uint512,address)(bool,uint512,uint512)']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'getReceipt(uint512,address)(bool,uint512,uint512)' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.proposalId, params.args.voter],
    }
  }
  /**
   * Constructs a no op call for the getProposalCount()uint512 ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static getProposalCount(params: CallParams<GovernorArgs['obj']['getProposalCount()uint512'] | GovernorArgs['tuple']['getProposalCount()uint512']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'getProposalCount()uint512' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
}

/**
 * A client to make calls to the Governor smart contract
 */
export class GovernorClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `GovernorClient`
   *
   * @param appClient An `AppClient` instance which has been created with the Governor app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `GovernorClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Checks for decode errors on the given return value and maps the return value to the return type for the given method
   * @returns The typed return value or undefined if there was no value
   */
  decodeReturnValue<TSignature extends GovernorNonVoidMethodSignatures>(method: TSignature, returnValue: ABIReturn | undefined) {
    return returnValue !== undefined ? getArc56ReturnValue<MethodReturn<TSignature>>(returnValue, this.appClient.getABIMethod(method), APP_SPEC.structs) : undefined
  }

  /**
   * Returns a new `GovernorClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<GovernorClient> {
    return new GovernorClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `GovernorClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<GovernorClient> {
    return new GovernorClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Makes a clear_state call to an existing instance of the Governor smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the Governor smart contract using the `propose(string)uint512` ABI method.
     *
     * Create a new proposal. Proposer must have enough voting power.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    propose: (params: CallParams<GovernorArgs['obj']['propose(string)uint512'] | GovernorArgs['tuple']['propose(string)uint512']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(GovernorParamsFactory.propose(params))
    },

    /**
     * Makes a call to the Governor smart contract using the `castVote(uint512,uint64)uint512` ABI method.
     *
     * Cast a vote. Support: 0=Against, 1=For, 2=Abstain.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    castVote: (params: CallParams<GovernorArgs['obj']['castVote(uint512,uint64)uint512'] | GovernorArgs['tuple']['castVote(uint512,uint64)uint512']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(GovernorParamsFactory.castVote(params))
    },

    /**
     * Makes a call to the Governor smart contract using the `execute(uint512)void` ABI method.
     *
     * Execute a succeeded proposal.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    execute: (params: CallParams<GovernorArgs['obj']['execute(uint512)void'] | GovernorArgs['tuple']['execute(uint512)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(GovernorParamsFactory.execute(params))
    },

    /**
     * Makes a call to the Governor smart contract using the `cancel(uint512)void` ABI method.
     *
     * Cancel a proposal. Only proposer or admin can cancel.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    cancel: (params: CallParams<GovernorArgs['obj']['cancel(uint512)void'] | GovernorArgs['tuple']['cancel(uint512)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(GovernorParamsFactory.cancel(params))
    },

    /**
     * Makes a call to the Governor smart contract using the `getProposal(uint512)(address,uint512,uint512,uint512,bool,bool)` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    getProposal: (params: CallParams<GovernorArgs['obj']['getProposal(uint512)(address,uint512,uint512,uint512,bool,bool)'] | GovernorArgs['tuple']['getProposal(uint512)(address,uint512,uint512,uint512,bool,bool)']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(GovernorParamsFactory.getProposal(params))
    },

    /**
     * Makes a call to the Governor smart contract using the `getReceipt(uint512,address)(bool,uint512,uint512)` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    getReceipt: (params: CallParams<GovernorArgs['obj']['getReceipt(uint512,address)(bool,uint512,uint512)'] | GovernorArgs['tuple']['getReceipt(uint512,address)(bool,uint512,uint512)']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(GovernorParamsFactory.getReceipt(params))
    },

    /**
     * Makes a call to the Governor smart contract using the `getProposalCount()uint512` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    getProposalCount: (params: CallParams<GovernorArgs['obj']['getProposalCount()uint512'] | GovernorArgs['tuple']['getProposalCount()uint512']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(GovernorParamsFactory.getProposalCount(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Makes a clear_state call to an existing instance of the Governor smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the Governor smart contract using the `propose(string)uint512` ABI method.
     *
     * Create a new proposal. Proposer must have enough voting power.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    propose: (params: CallParams<GovernorArgs['obj']['propose(string)uint512'] | GovernorArgs['tuple']['propose(string)uint512']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(GovernorParamsFactory.propose(params))
    },

    /**
     * Makes a call to the Governor smart contract using the `castVote(uint512,uint64)uint512` ABI method.
     *
     * Cast a vote. Support: 0=Against, 1=For, 2=Abstain.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    castVote: (params: CallParams<GovernorArgs['obj']['castVote(uint512,uint64)uint512'] | GovernorArgs['tuple']['castVote(uint512,uint64)uint512']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(GovernorParamsFactory.castVote(params))
    },

    /**
     * Makes a call to the Governor smart contract using the `execute(uint512)void` ABI method.
     *
     * Execute a succeeded proposal.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    execute: (params: CallParams<GovernorArgs['obj']['execute(uint512)void'] | GovernorArgs['tuple']['execute(uint512)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(GovernorParamsFactory.execute(params))
    },

    /**
     * Makes a call to the Governor smart contract using the `cancel(uint512)void` ABI method.
     *
     * Cancel a proposal. Only proposer or admin can cancel.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    cancel: (params: CallParams<GovernorArgs['obj']['cancel(uint512)void'] | GovernorArgs['tuple']['cancel(uint512)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(GovernorParamsFactory.cancel(params))
    },

    /**
     * Makes a call to the Governor smart contract using the `getProposal(uint512)(address,uint512,uint512,uint512,bool,bool)` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    getProposal: (params: CallParams<GovernorArgs['obj']['getProposal(uint512)(address,uint512,uint512,uint512,bool,bool)'] | GovernorArgs['tuple']['getProposal(uint512)(address,uint512,uint512,uint512,bool,bool)']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(GovernorParamsFactory.getProposal(params))
    },

    /**
     * Makes a call to the Governor smart contract using the `getReceipt(uint512,address)(bool,uint512,uint512)` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    getReceipt: (params: CallParams<GovernorArgs['obj']['getReceipt(uint512,address)(bool,uint512,uint512)'] | GovernorArgs['tuple']['getReceipt(uint512,address)(bool,uint512,uint512)']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(GovernorParamsFactory.getReceipt(params))
    },

    /**
     * Makes a call to the Governor smart contract using the `getProposalCount()uint512` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    getProposalCount: (params: CallParams<GovernorArgs['obj']['getProposalCount()uint512'] | GovernorArgs['tuple']['getProposalCount()uint512']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(GovernorParamsFactory.getProposalCount(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Makes a clear_state call to an existing instance of the Governor smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the Governor smart contract using the `propose(string)uint512` ABI method.
     *
     * Create a new proposal. Proposer must have enough voting power.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    propose: async (params: CallParams<GovernorArgs['obj']['propose(string)uint512'] | GovernorArgs['tuple']['propose(string)uint512']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(GovernorParamsFactory.propose(params))
      return {...result, return: result.return as unknown as (undefined | GovernorReturns['propose(string)uint512'])}
    },

    /**
     * Makes a call to the Governor smart contract using the `castVote(uint512,uint64)uint512` ABI method.
     *
     * Cast a vote. Support: 0=Against, 1=For, 2=Abstain.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    castVote: async (params: CallParams<GovernorArgs['obj']['castVote(uint512,uint64)uint512'] | GovernorArgs['tuple']['castVote(uint512,uint64)uint512']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(GovernorParamsFactory.castVote(params))
      return {...result, return: result.return as unknown as (undefined | GovernorReturns['castVote(uint512,uint64)uint512'])}
    },

    /**
     * Makes a call to the Governor smart contract using the `execute(uint512)void` ABI method.
     *
     * Execute a succeeded proposal.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    execute: async (params: CallParams<GovernorArgs['obj']['execute(uint512)void'] | GovernorArgs['tuple']['execute(uint512)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(GovernorParamsFactory.execute(params))
      return {...result, return: result.return as unknown as (undefined | GovernorReturns['execute(uint512)void'])}
    },

    /**
     * Makes a call to the Governor smart contract using the `cancel(uint512)void` ABI method.
     *
     * Cancel a proposal. Only proposer or admin can cancel.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    cancel: async (params: CallParams<GovernorArgs['obj']['cancel(uint512)void'] | GovernorArgs['tuple']['cancel(uint512)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(GovernorParamsFactory.cancel(params))
      return {...result, return: result.return as unknown as (undefined | GovernorReturns['cancel(uint512)void'])}
    },

    /**
     * Makes a call to the Governor smart contract using the `getProposal(uint512)(address,uint512,uint512,uint512,bool,bool)` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    getProposal: async (params: CallParams<GovernorArgs['obj']['getProposal(uint512)(address,uint512,uint512,uint512,bool,bool)'] | GovernorArgs['tuple']['getProposal(uint512)(address,uint512,uint512,uint512,bool,bool)']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(GovernorParamsFactory.getProposal(params))
      return {...result, return: result.return as unknown as (undefined | GovernorReturns['getProposal(uint512)(address,uint512,uint512,uint512,bool,bool)'])}
    },

    /**
     * Makes a call to the Governor smart contract using the `getReceipt(uint512,address)(bool,uint512,uint512)` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    getReceipt: async (params: CallParams<GovernorArgs['obj']['getReceipt(uint512,address)(bool,uint512,uint512)'] | GovernorArgs['tuple']['getReceipt(uint512,address)(bool,uint512,uint512)']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(GovernorParamsFactory.getReceipt(params))
      return {...result, return: result.return as unknown as (undefined | GovernorReturns['getReceipt(uint512,address)(bool,uint512,uint512)'])}
    },

    /**
     * Makes a call to the Governor smart contract using the `getProposalCount()uint512` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    getProposalCount: async (params: CallParams<GovernorArgs['obj']['getProposalCount()uint512'] | GovernorArgs['tuple']['getProposalCount()uint512']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(GovernorParamsFactory.getProposalCount(params))
      return {...result, return: result.return as unknown as (undefined | GovernorReturns['getProposalCount()uint512'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new GovernorClient(this.appClient.clone(params))
  }

  /**
   * Makes a readonly (simulated) call to the Governor smart contract using the `getProposal(uint512)(address,uint512,uint512,uint512,bool,bool)` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async getProposal(params: CallParams<GovernorArgs['obj']['getProposal(uint512)(address,uint512,uint512,uint512,bool,bool)'] | GovernorArgs['tuple']['getProposal(uint512)(address,uint512,uint512,uint512,bool,bool)']>) {
    const result = await this.appClient.send.call(GovernorParamsFactory.getProposal(params))
    return result.return as unknown as GovernorReturns['getProposal(uint512)(address,uint512,uint512,uint512,bool,bool)']
  }

  /**
   * Makes a readonly (simulated) call to the Governor smart contract using the `getReceipt(uint512,address)(bool,uint512,uint512)` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async getReceipt(params: CallParams<GovernorArgs['obj']['getReceipt(uint512,address)(bool,uint512,uint512)'] | GovernorArgs['tuple']['getReceipt(uint512,address)(bool,uint512,uint512)']>) {
    const result = await this.appClient.send.call(GovernorParamsFactory.getReceipt(params))
    return result.return as unknown as GovernorReturns['getReceipt(uint512,address)(bool,uint512,uint512)']
  }

  /**
   * Makes a readonly (simulated) call to the Governor smart contract using the `getProposalCount()uint512` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async getProposalCount(params: CallParams<GovernorArgs['obj']['getProposalCount()uint512'] | GovernorArgs['tuple']['getProposalCount()uint512']> = {args: []}) {
    const result = await this.appClient.send.call(GovernorParamsFactory.getProposalCount(params))
    return result.return as unknown as GovernorReturns['getProposalCount()uint512']
  }

  /**
   * Methods to access state for the current Governor app
   */
  state = {
    /**
     * Methods to access global state for the current Governor app
     */
    global: {
      /**
       * Get all current keyed values from global state
       */
      getAll: async (): Promise<Partial<Expand<GlobalKeysState>>> => {
        const result = await this.appClient.state.global.getAll()
        return {
          token: result.token,
          admin: result.admin,
          proposalCount: new BinaryStateValue(result.proposalCount),
          quorumVotes: new BinaryStateValue(result.quorumVotes),
          proposalThreshold: new BinaryStateValue(result.proposalThreshold),
        }
      },
      /**
       * Get the current value of the token key in global state
       */
      token: async (): Promise<string | undefined> => { return (await this.appClient.state.global.getValue("token")) as string | undefined },
      /**
       * Get the current value of the admin key in global state
       */
      admin: async (): Promise<string | undefined> => { return (await this.appClient.state.global.getValue("admin")) as string | undefined },
      /**
       * Get the current value of the proposalCount key in global state
       */
      proposalCount: async (): Promise<BinaryState> => { return new BinaryStateValue((await this.appClient.state.global.getValue("proposalCount")) as Uint8Array | undefined) },
      /**
       * Get the current value of the quorumVotes key in global state
       */
      quorumVotes: async (): Promise<BinaryState> => { return new BinaryStateValue((await this.appClient.state.global.getValue("quorumVotes")) as Uint8Array | undefined) },
      /**
       * Get the current value of the proposalThreshold key in global state
       */
      proposalThreshold: async (): Promise<BinaryState> => { return new BinaryStateValue((await this.appClient.state.global.getValue("proposalThreshold")) as Uint8Array | undefined) },
    },
    /**
     * Methods to access box state for the current Governor app
     */
    box: {
      /**
       * Get all current keyed values from box state
       */
      getAll: async (): Promise<Partial<Expand<BoxKeysState>>> => {
        const result = await this.appClient.state.box.getAll()
        return {
        }
      },
      /**
       * Get values from the _proposalProposer map in box state
       */
      _proposalProposer: {
        /**
         * Get all current values of the _proposalProposer map in box state
         */
        getMap: async (): Promise<Map<Uint8Array, string>> => { return (await this.appClient.state.box.getMap("_proposalProposer")) as Map<Uint8Array, string> },
        /**
         * Get a current value of the _proposalProposer map by key from box state
         */
        value: async (key: Uint8Array | string): Promise<string | undefined> => { return await this.appClient.state.box.getMapValue("_proposalProposer", key) as string | undefined },
      },
      /**
       * Get values from the _proposalForVotes map in box state
       */
      _proposalForVotes: {
        /**
         * Get all current values of the _proposalForVotes map in box state
         */
        getMap: async (): Promise<Map<Uint8Array, Uint8Array>> => { return (await this.appClient.state.box.getMap("_proposalForVotes")) as Map<Uint8Array, Uint8Array> },
        /**
         * Get a current value of the _proposalForVotes map by key from box state
         */
        value: async (key: Uint8Array | string): Promise<Uint8Array | undefined> => { return await this.appClient.state.box.getMapValue("_proposalForVotes", key) as Uint8Array | undefined },
      },
      /**
       * Get values from the _proposalAgainstVotes map in box state
       */
      _proposalAgainstVotes: {
        /**
         * Get all current values of the _proposalAgainstVotes map in box state
         */
        getMap: async (): Promise<Map<Uint8Array, Uint8Array>> => { return (await this.appClient.state.box.getMap("_proposalAgainstVotes")) as Map<Uint8Array, Uint8Array> },
        /**
         * Get a current value of the _proposalAgainstVotes map by key from box state
         */
        value: async (key: Uint8Array | string): Promise<Uint8Array | undefined> => { return await this.appClient.state.box.getMapValue("_proposalAgainstVotes", key) as Uint8Array | undefined },
      },
      /**
       * Get values from the _proposalAbstainVotes map in box state
       */
      _proposalAbstainVotes: {
        /**
         * Get all current values of the _proposalAbstainVotes map in box state
         */
        getMap: async (): Promise<Map<Uint8Array, Uint8Array>> => { return (await this.appClient.state.box.getMap("_proposalAbstainVotes")) as Map<Uint8Array, Uint8Array> },
        /**
         * Get a current value of the _proposalAbstainVotes map by key from box state
         */
        value: async (key: Uint8Array | string): Promise<Uint8Array | undefined> => { return await this.appClient.state.box.getMapValue("_proposalAbstainVotes", key) as Uint8Array | undefined },
      },
      /**
       * Get values from the _proposalExecuted map in box state
       */
      _proposalExecuted: {
        /**
         * Get all current values of the _proposalExecuted map in box state
         */
        getMap: async (): Promise<Map<Uint8Array, bigint>> => { return (await this.appClient.state.box.getMap("_proposalExecuted")) as Map<Uint8Array, bigint> },
        /**
         * Get a current value of the _proposalExecuted map by key from box state
         */
        value: async (key: Uint8Array | string): Promise<bigint | undefined> => { return await this.appClient.state.box.getMapValue("_proposalExecuted", key) as bigint | undefined },
      },
      /**
       * Get values from the _proposalCancelled map in box state
       */
      _proposalCancelled: {
        /**
         * Get all current values of the _proposalCancelled map in box state
         */
        getMap: async (): Promise<Map<Uint8Array, bigint>> => { return (await this.appClient.state.box.getMap("_proposalCancelled")) as Map<Uint8Array, bigint> },
        /**
         * Get a current value of the _proposalCancelled map by key from box state
         */
        value: async (key: Uint8Array | string): Promise<bigint | undefined> => { return await this.appClient.state.box.getMapValue("_proposalCancelled", key) as bigint | undefined },
      },
      /**
       * Get values from the _receiptHasVoted map in box state
       */
      _receiptHasVoted: {
        /**
         * Get all current values of the _receiptHasVoted map in box state
         */
        getMap: async (): Promise<Map<Uint8Array, bigint>> => { return (await this.appClient.state.box.getMap("_receiptHasVoted")) as Map<Uint8Array, bigint> },
        /**
         * Get a current value of the _receiptHasVoted map by key from box state
         */
        value: async (key: Uint8Array | string): Promise<bigint | undefined> => { return await this.appClient.state.box.getMapValue("_receiptHasVoted", key) as bigint | undefined },
      },
      /**
       * Get values from the _receiptSupport map in box state
       */
      _receiptSupport: {
        /**
         * Get all current values of the _receiptSupport map in box state
         */
        getMap: async (): Promise<Map<Uint8Array, Uint8Array>> => { return (await this.appClient.state.box.getMap("_receiptSupport")) as Map<Uint8Array, Uint8Array> },
        /**
         * Get a current value of the _receiptSupport map by key from box state
         */
        value: async (key: Uint8Array | string): Promise<Uint8Array | undefined> => { return await this.appClient.state.box.getMapValue("_receiptSupport", key) as Uint8Array | undefined },
      },
      /**
       * Get values from the _receiptVotes map in box state
       */
      _receiptVotes: {
        /**
         * Get all current values of the _receiptVotes map in box state
         */
        getMap: async (): Promise<Map<Uint8Array, Uint8Array>> => { return (await this.appClient.state.box.getMap("_receiptVotes")) as Map<Uint8Array, Uint8Array> },
        /**
         * Get a current value of the _receiptVotes map by key from box state
         */
        value: async (key: Uint8Array | string): Promise<Uint8Array | undefined> => { return await this.appClient.state.box.getMapValue("_receiptVotes", key) as Uint8Array | undefined },
      },
    },
  }

  public newGroup(): GovernorComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a propose(string)uint512 method call against the Governor contract
       */
      propose(params: CallParams<GovernorArgs['obj']['propose(string)uint512'] | GovernorArgs['tuple']['propose(string)uint512']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.propose(params)))
        resultMappers.push((v) => client.decodeReturnValue('propose(string)uint512', v))
        return this
      },
      /**
       * Add a castVote(uint512,uint64)uint512 method call against the Governor contract
       */
      castVote(params: CallParams<GovernorArgs['obj']['castVote(uint512,uint64)uint512'] | GovernorArgs['tuple']['castVote(uint512,uint64)uint512']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.castVote(params)))
        resultMappers.push((v) => client.decodeReturnValue('castVote(uint512,uint64)uint512', v))
        return this
      },
      /**
       * Add a execute(uint512)void method call against the Governor contract
       */
      execute(params: CallParams<GovernorArgs['obj']['execute(uint512)void'] | GovernorArgs['tuple']['execute(uint512)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.execute(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a cancel(uint512)void method call against the Governor contract
       */
      cancel(params: CallParams<GovernorArgs['obj']['cancel(uint512)void'] | GovernorArgs['tuple']['cancel(uint512)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.cancel(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a getProposal(uint512)(address,uint512,uint512,uint512,bool,bool) method call against the Governor contract
       */
      getProposal(params: CallParams<GovernorArgs['obj']['getProposal(uint512)(address,uint512,uint512,uint512,bool,bool)'] | GovernorArgs['tuple']['getProposal(uint512)(address,uint512,uint512,uint512,bool,bool)']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.getProposal(params)))
        resultMappers.push((v) => client.decodeReturnValue('getProposal(uint512)(address,uint512,uint512,uint512,bool,bool)', v))
        return this
      },
      /**
       * Add a getReceipt(uint512,address)(bool,uint512,uint512) method call against the Governor contract
       */
      getReceipt(params: CallParams<GovernorArgs['obj']['getReceipt(uint512,address)(bool,uint512,uint512)'] | GovernorArgs['tuple']['getReceipt(uint512,address)(bool,uint512,uint512)']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.getReceipt(params)))
        resultMappers.push((v) => client.decodeReturnValue('getReceipt(uint512,address)(bool,uint512,uint512)', v))
        return this
      },
      /**
       * Add a getProposalCount()uint512 method call against the Governor contract
       */
      getProposalCount(params: CallParams<GovernorArgs['obj']['getProposalCount()uint512'] | GovernorArgs['tuple']['getProposalCount()uint512']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.getProposalCount(params)))
        resultMappers.push((v) => client.decodeReturnValue('getProposalCount()uint512', v))
        return this
      },
      /**
       * Add a clear state call to the Governor contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as GovernorComposer
  }
}
export type GovernorComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the propose(string)uint512 ABI method.
   *
   * Create a new proposal. Proposer must have enough voting power.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  propose(params?: CallParams<GovernorArgs['obj']['propose(string)uint512'] | GovernorArgs['tuple']['propose(string)uint512']>): GovernorComposer<[...TReturns, GovernorReturns['propose(string)uint512'] | undefined]>

  /**
   * Calls the castVote(uint512,uint64)uint512 ABI method.
   *
   * Cast a vote. Support: 0=Against, 1=For, 2=Abstain.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  castVote(params?: CallParams<GovernorArgs['obj']['castVote(uint512,uint64)uint512'] | GovernorArgs['tuple']['castVote(uint512,uint64)uint512']>): GovernorComposer<[...TReturns, GovernorReturns['castVote(uint512,uint64)uint512'] | undefined]>

  /**
   * Calls the execute(uint512)void ABI method.
   *
   * Execute a succeeded proposal.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  execute(params?: CallParams<GovernorArgs['obj']['execute(uint512)void'] | GovernorArgs['tuple']['execute(uint512)void']>): GovernorComposer<[...TReturns, GovernorReturns['execute(uint512)void'] | undefined]>

  /**
   * Calls the cancel(uint512)void ABI method.
   *
   * Cancel a proposal. Only proposer or admin can cancel.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  cancel(params?: CallParams<GovernorArgs['obj']['cancel(uint512)void'] | GovernorArgs['tuple']['cancel(uint512)void']>): GovernorComposer<[...TReturns, GovernorReturns['cancel(uint512)void'] | undefined]>

  /**
   * Calls the getProposal(uint512)(address,uint512,uint512,uint512,bool,bool) ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  getProposal(params?: CallParams<GovernorArgs['obj']['getProposal(uint512)(address,uint512,uint512,uint512,bool,bool)'] | GovernorArgs['tuple']['getProposal(uint512)(address,uint512,uint512,uint512,bool,bool)']>): GovernorComposer<[...TReturns, GovernorReturns['getProposal(uint512)(address,uint512,uint512,uint512,bool,bool)'] | undefined]>

  /**
   * Calls the getReceipt(uint512,address)(bool,uint512,uint512) ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  getReceipt(params?: CallParams<GovernorArgs['obj']['getReceipt(uint512,address)(bool,uint512,uint512)'] | GovernorArgs['tuple']['getReceipt(uint512,address)(bool,uint512,uint512)']>): GovernorComposer<[...TReturns, GovernorReturns['getReceipt(uint512,address)(bool,uint512,uint512)'] | undefined]>

  /**
   * Calls the getProposalCount()uint512 ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  getProposalCount(params?: CallParams<GovernorArgs['obj']['getProposalCount()uint512'] | GovernorArgs['tuple']['getProposalCount()uint512']>): GovernorComposer<[...TReturns, GovernorReturns['getProposalCount()uint512'] | undefined]>

  /**
   * Makes a clear_state call to an existing instance of the Governor smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): GovernorComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): GovernorComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<GovernorComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<GovernorComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<GovernorComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<GovernorComposerResults<TReturns>>
}
export type GovernorComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

