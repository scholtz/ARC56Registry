/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"Create2Test","structs":{},"methods":[{"name":"computeAddress","args":[{"type":"byte[32]","name":"salt"},{"type":"byte[32]","name":"bytecodeHash"},{"type":"address","name":"deployer"}],"returns":{"type":"address"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"desc":" @dev Returns the address where a contract will be stored if deployed via\n CREATE2 from a contract at `deployer` with `salt` and `bytecodeHash`.","events":[],"recommendations":{}},{"name":"computeBytecodeHash","args":[{"type":"byte[]","name":"bytecode"}],"returns":{"type":"byte[32]"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"desc":" @dev Returns the keccak256 hash of creation bytecode.","events":[],"recommendations":{}},{"name":"verifyDeterministic","args":[{"type":"byte[32]","name":"salt1"},{"type":"byte[32]","name":"salt2"},{"type":"byte[32]","name":"bytecodeHash"},{"type":"address","name":"deployer"}],"returns":{"type":"bool"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"desc":" @dev Verify that the same inputs always produce the same address.","events":[],"recommendations":{}}],"arcs":[22,28],"desc":" @dev Demonstrates CREATE2 address computation (deterministic deployment).\n On Algorand this is just the hash computation, not actual deployment.","networks":{},"state":{"schema":{"global":{"ints":0,"bytes":0},"local":{"ints":0,"bytes":0}},"keys":{"global":{},"local":{},"box":{}},"maps":{"global":{},"local":{},"box":{}}},"bareActions":{"create":[],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[119],"errorMessage":"invalid array length header"},{"pc":[127],"errorMessage":"invalid number of bytes for arc4.dynamic_array<arc4.uint8>"},{"pc":[63,71,79,145,153,161,169],"errorMessage":"invalid number of bytes for arc4.static_array<arc4.uint8, 32>"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"events":[]} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


/**
 * The argument types for the Create2Test contract
 */
export type Create2TestArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'computeAddress(byte[32],byte[32],address)address': {
      salt: Uint8Array
      bytecodeHash: Uint8Array
      deployer: string
    }
    'computeBytecodeHash(byte[])byte[32]': {
      bytecode: Uint8Array
    }
    'verifyDeterministic(byte[32],byte[32],byte[32],address)bool': {
      salt1: Uint8Array
      salt2: Uint8Array
      bytecodeHash: Uint8Array
      deployer: string
    }
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'computeAddress(byte[32],byte[32],address)address': [salt: Uint8Array, bytecodeHash: Uint8Array, deployer: string]
    'computeBytecodeHash(byte[])byte[32]': [bytecode: Uint8Array]
    'verifyDeterministic(byte[32],byte[32],byte[32],address)bool': [salt1: Uint8Array, salt2: Uint8Array, bytecodeHash: Uint8Array, deployer: string]
  }
}

/**
 * The return type for each method
 */
export type Create2TestReturns = {
  'computeAddress(byte[32],byte[32],address)address': string
  'computeBytecodeHash(byte[])byte[32]': Uint8Array
  'verifyDeterministic(byte[32],byte[32],byte[32],address)bool': boolean
}

/**
 * Defines the types of available calls and state of the Create2Test smart contract.
 */
export type Create2TestTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'computeAddress(byte[32],byte[32],address)address' | 'computeAddress', {
      argsObj: Create2TestArgs['obj']['computeAddress(byte[32],byte[32],address)address']
      argsTuple: Create2TestArgs['tuple']['computeAddress(byte[32],byte[32],address)address']
      returns: Create2TestReturns['computeAddress(byte[32],byte[32],address)address']
    }>
    & Record<'computeBytecodeHash(byte[])byte[32]' | 'computeBytecodeHash', {
      argsObj: Create2TestArgs['obj']['computeBytecodeHash(byte[])byte[32]']
      argsTuple: Create2TestArgs['tuple']['computeBytecodeHash(byte[])byte[32]']
      returns: Create2TestReturns['computeBytecodeHash(byte[])byte[32]']
    }>
    & Record<'verifyDeterministic(byte[32],byte[32],byte[32],address)bool' | 'verifyDeterministic', {
      argsObj: Create2TestArgs['obj']['verifyDeterministic(byte[32],byte[32],byte[32],address)bool']
      argsTuple: Create2TestArgs['tuple']['verifyDeterministic(byte[32],byte[32],byte[32],address)bool']
      returns: Create2TestReturns['verifyDeterministic(byte[32],byte[32],byte[32],address)bool']
    }>
}

/**
 * Defines the possible abi call signatures.
 */
export type Create2TestSignatures = keyof Create2TestTypes['methods']
/**
 * Defines the possible abi call signatures for methods that return a non-void value.
 */
export type Create2TestNonVoidMethodSignatures = keyof Create2TestTypes['methods'] extends infer T ? T extends keyof Create2TestTypes['methods'] ? MethodReturn<T> extends void ? never : T  : never : never
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the Create2Test smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends Create2TestSignatures> = Create2TestTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the Create2Test smart contract to the method's return type
 */
export type MethodReturn<TSignature extends Create2TestSignatures> = Create2TestTypes['methods'][TSignature]['returns']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the Create2Test smart contract
 */
export abstract class Create2TestParamsFactory {
  /**
   * Constructs a no op call for the computeAddress(byte[32],byte[32],address)address ABI method
   *
  *  @dev Returns the address where a contract will be stored if deployed via
  CREATE2 from a contract at `deployer` with `salt` and `bytecodeHash`.

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static computeAddress(params: CallParams<Create2TestArgs['obj']['computeAddress(byte[32],byte[32],address)address'] | Create2TestArgs['tuple']['computeAddress(byte[32],byte[32],address)address']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'computeAddress(byte[32],byte[32],address)address' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.salt, params.args.bytecodeHash, params.args.deployer],
    }
  }
  /**
   * Constructs a no op call for the computeBytecodeHash(byte[])byte[32] ABI method
   *
   *  @dev Returns the keccak256 hash of creation bytecode.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static computeBytecodeHash(params: CallParams<Create2TestArgs['obj']['computeBytecodeHash(byte[])byte[32]'] | Create2TestArgs['tuple']['computeBytecodeHash(byte[])byte[32]']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'computeBytecodeHash(byte[])byte[32]' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.bytecode],
    }
  }
  /**
   * Constructs a no op call for the verifyDeterministic(byte[32],byte[32],byte[32],address)bool ABI method
   *
   *  @dev Verify that the same inputs always produce the same address.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static verifyDeterministic(params: CallParams<Create2TestArgs['obj']['verifyDeterministic(byte[32],byte[32],byte[32],address)bool'] | Create2TestArgs['tuple']['verifyDeterministic(byte[32],byte[32],byte[32],address)bool']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'verifyDeterministic(byte[32],byte[32],byte[32],address)bool' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.salt1, params.args.salt2, params.args.bytecodeHash, params.args.deployer],
    }
  }
}

/**
 * A client to make calls to the Create2Test smart contract
 */
export class Create2TestClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `Create2TestClient`
   *
   * @param appClient An `AppClient` instance which has been created with the Create2Test app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `Create2TestClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Checks for decode errors on the given return value and maps the return value to the return type for the given method
   * @returns The typed return value or undefined if there was no value
   */
  decodeReturnValue<TSignature extends Create2TestNonVoidMethodSignatures>(method: TSignature, returnValue: ABIReturn | undefined) {
    return returnValue !== undefined ? getArc56ReturnValue<MethodReturn<TSignature>>(returnValue, this.appClient.getABIMethod(method), APP_SPEC.structs) : undefined
  }

  /**
   * Returns a new `Create2TestClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<Create2TestClient> {
    return new Create2TestClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `Create2TestClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<Create2TestClient> {
    return new Create2TestClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Makes a clear_state call to an existing instance of the Create2Test smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the Create2Test smart contract using the `computeAddress(byte[32],byte[32],address)address` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
    *  @dev Returns the address where a contract will be stored if deployed via
    CREATE2 from a contract at `deployer` with `salt` and `bytecodeHash`.

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    computeAddress: (params: CallParams<Create2TestArgs['obj']['computeAddress(byte[32],byte[32],address)address'] | Create2TestArgs['tuple']['computeAddress(byte[32],byte[32],address)address']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(Create2TestParamsFactory.computeAddress(params))
    },

    /**
     * Makes a call to the Create2Test smart contract using the `computeBytecodeHash(byte[])byte[32]` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     *  @dev Returns the keccak256 hash of creation bytecode.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    computeBytecodeHash: (params: CallParams<Create2TestArgs['obj']['computeBytecodeHash(byte[])byte[32]'] | Create2TestArgs['tuple']['computeBytecodeHash(byte[])byte[32]']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(Create2TestParamsFactory.computeBytecodeHash(params))
    },

    /**
     * Makes a call to the Create2Test smart contract using the `verifyDeterministic(byte[32],byte[32],byte[32],address)bool` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     *  @dev Verify that the same inputs always produce the same address.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    verifyDeterministic: (params: CallParams<Create2TestArgs['obj']['verifyDeterministic(byte[32],byte[32],byte[32],address)bool'] | Create2TestArgs['tuple']['verifyDeterministic(byte[32],byte[32],byte[32],address)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(Create2TestParamsFactory.verifyDeterministic(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Makes a clear_state call to an existing instance of the Create2Test smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the Create2Test smart contract using the `computeAddress(byte[32],byte[32],address)address` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
    *  @dev Returns the address where a contract will be stored if deployed via
    CREATE2 from a contract at `deployer` with `salt` and `bytecodeHash`.

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    computeAddress: (params: CallParams<Create2TestArgs['obj']['computeAddress(byte[32],byte[32],address)address'] | Create2TestArgs['tuple']['computeAddress(byte[32],byte[32],address)address']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(Create2TestParamsFactory.computeAddress(params))
    },

    /**
     * Makes a call to the Create2Test smart contract using the `computeBytecodeHash(byte[])byte[32]` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     *  @dev Returns the keccak256 hash of creation bytecode.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    computeBytecodeHash: (params: CallParams<Create2TestArgs['obj']['computeBytecodeHash(byte[])byte[32]'] | Create2TestArgs['tuple']['computeBytecodeHash(byte[])byte[32]']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(Create2TestParamsFactory.computeBytecodeHash(params))
    },

    /**
     * Makes a call to the Create2Test smart contract using the `verifyDeterministic(byte[32],byte[32],byte[32],address)bool` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     *  @dev Verify that the same inputs always produce the same address.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    verifyDeterministic: (params: CallParams<Create2TestArgs['obj']['verifyDeterministic(byte[32],byte[32],byte[32],address)bool'] | Create2TestArgs['tuple']['verifyDeterministic(byte[32],byte[32],byte[32],address)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(Create2TestParamsFactory.verifyDeterministic(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Makes a clear_state call to an existing instance of the Create2Test smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the Create2Test smart contract using the `computeAddress(byte[32],byte[32],address)address` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
    *  @dev Returns the address where a contract will be stored if deployed via
    CREATE2 from a contract at `deployer` with `salt` and `bytecodeHash`.

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    computeAddress: async (params: CallParams<Create2TestArgs['obj']['computeAddress(byte[32],byte[32],address)address'] | Create2TestArgs['tuple']['computeAddress(byte[32],byte[32],address)address']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(Create2TestParamsFactory.computeAddress(params))
      return {...result, return: result.return as unknown as (undefined | Create2TestReturns['computeAddress(byte[32],byte[32],address)address'])}
    },

    /**
     * Makes a call to the Create2Test smart contract using the `computeBytecodeHash(byte[])byte[32]` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     *  @dev Returns the keccak256 hash of creation bytecode.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    computeBytecodeHash: async (params: CallParams<Create2TestArgs['obj']['computeBytecodeHash(byte[])byte[32]'] | Create2TestArgs['tuple']['computeBytecodeHash(byte[])byte[32]']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(Create2TestParamsFactory.computeBytecodeHash(params))
      return {...result, return: result.return as unknown as (undefined | Create2TestReturns['computeBytecodeHash(byte[])byte[32]'])}
    },

    /**
     * Makes a call to the Create2Test smart contract using the `verifyDeterministic(byte[32],byte[32],byte[32],address)bool` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     *  @dev Verify that the same inputs always produce the same address.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    verifyDeterministic: async (params: CallParams<Create2TestArgs['obj']['verifyDeterministic(byte[32],byte[32],byte[32],address)bool'] | Create2TestArgs['tuple']['verifyDeterministic(byte[32],byte[32],byte[32],address)bool']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(Create2TestParamsFactory.verifyDeterministic(params))
      return {...result, return: result.return as unknown as (undefined | Create2TestReturns['verifyDeterministic(byte[32],byte[32],byte[32],address)bool'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new Create2TestClient(this.appClient.clone(params))
  }

  /**
   * Makes a readonly (simulated) call to the Create2Test smart contract using the `computeAddress(byte[32],byte[32],address)address` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
  *  @dev Returns the address where a contract will be stored if deployed via
  CREATE2 from a contract at `deployer` with `salt` and `bytecodeHash`.

   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async computeAddress(params: CallParams<Create2TestArgs['obj']['computeAddress(byte[32],byte[32],address)address'] | Create2TestArgs['tuple']['computeAddress(byte[32],byte[32],address)address']>) {
    const result = await this.appClient.send.call(Create2TestParamsFactory.computeAddress(params))
    return result.return as unknown as Create2TestReturns['computeAddress(byte[32],byte[32],address)address']
  }

  /**
   * Makes a readonly (simulated) call to the Create2Test smart contract using the `computeBytecodeHash(byte[])byte[32]` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   *  @dev Returns the keccak256 hash of creation bytecode.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async computeBytecodeHash(params: CallParams<Create2TestArgs['obj']['computeBytecodeHash(byte[])byte[32]'] | Create2TestArgs['tuple']['computeBytecodeHash(byte[])byte[32]']>) {
    const result = await this.appClient.send.call(Create2TestParamsFactory.computeBytecodeHash(params))
    return result.return as unknown as Create2TestReturns['computeBytecodeHash(byte[])byte[32]']
  }

  /**
   * Makes a readonly (simulated) call to the Create2Test smart contract using the `verifyDeterministic(byte[32],byte[32],byte[32],address)bool` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   *  @dev Verify that the same inputs always produce the same address.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async verifyDeterministic(params: CallParams<Create2TestArgs['obj']['verifyDeterministic(byte[32],byte[32],byte[32],address)bool'] | Create2TestArgs['tuple']['verifyDeterministic(byte[32],byte[32],byte[32],address)bool']>) {
    const result = await this.appClient.send.call(Create2TestParamsFactory.verifyDeterministic(params))
    return result.return as unknown as Create2TestReturns['verifyDeterministic(byte[32],byte[32],byte[32],address)bool']
  }

  /**
   * Methods to access state for the current Create2Test app
   */
  state = {
  }

  public newGroup(): Create2TestComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a computeAddress(byte[32],byte[32],address)address method call against the Create2Test contract
       */
      computeAddress(params: CallParams<Create2TestArgs['obj']['computeAddress(byte[32],byte[32],address)address'] | Create2TestArgs['tuple']['computeAddress(byte[32],byte[32],address)address']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.computeAddress(params)))
        resultMappers.push((v) => client.decodeReturnValue('computeAddress(byte[32],byte[32],address)address', v))
        return this
      },
      /**
       * Add a computeBytecodeHash(byte[])byte[32] method call against the Create2Test contract
       */
      computeBytecodeHash(params: CallParams<Create2TestArgs['obj']['computeBytecodeHash(byte[])byte[32]'] | Create2TestArgs['tuple']['computeBytecodeHash(byte[])byte[32]']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.computeBytecodeHash(params)))
        resultMappers.push((v) => client.decodeReturnValue('computeBytecodeHash(byte[])byte[32]', v))
        return this
      },
      /**
       * Add a verifyDeterministic(byte[32],byte[32],byte[32],address)bool method call against the Create2Test contract
       */
      verifyDeterministic(params: CallParams<Create2TestArgs['obj']['verifyDeterministic(byte[32],byte[32],byte[32],address)bool'] | Create2TestArgs['tuple']['verifyDeterministic(byte[32],byte[32],byte[32],address)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.verifyDeterministic(params)))
        resultMappers.push((v) => client.decodeReturnValue('verifyDeterministic(byte[32],byte[32],byte[32],address)bool', v))
        return this
      },
      /**
       * Add a clear state call to the Create2Test contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as Create2TestComposer
  }
}
export type Create2TestComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the computeAddress(byte[32],byte[32],address)address ABI method.
   *
  *  @dev Returns the address where a contract will be stored if deployed via
  CREATE2 from a contract at `deployer` with `salt` and `bytecodeHash`.

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  computeAddress(params?: CallParams<Create2TestArgs['obj']['computeAddress(byte[32],byte[32],address)address'] | Create2TestArgs['tuple']['computeAddress(byte[32],byte[32],address)address']>): Create2TestComposer<[...TReturns, Create2TestReturns['computeAddress(byte[32],byte[32],address)address'] | undefined]>

  /**
   * Calls the computeBytecodeHash(byte[])byte[32] ABI method.
   *
   *  @dev Returns the keccak256 hash of creation bytecode.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  computeBytecodeHash(params?: CallParams<Create2TestArgs['obj']['computeBytecodeHash(byte[])byte[32]'] | Create2TestArgs['tuple']['computeBytecodeHash(byte[])byte[32]']>): Create2TestComposer<[...TReturns, Create2TestReturns['computeBytecodeHash(byte[])byte[32]'] | undefined]>

  /**
   * Calls the verifyDeterministic(byte[32],byte[32],byte[32],address)bool ABI method.
   *
   *  @dev Verify that the same inputs always produce the same address.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  verifyDeterministic(params?: CallParams<Create2TestArgs['obj']['verifyDeterministic(byte[32],byte[32],byte[32],address)bool'] | Create2TestArgs['tuple']['verifyDeterministic(byte[32],byte[32],byte[32],address)bool']>): Create2TestComposer<[...TReturns, Create2TestReturns['verifyDeterministic(byte[32],byte[32],byte[32],address)bool'] | undefined]>

  /**
   * Makes a clear_state call to an existing instance of the Create2Test smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): Create2TestComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): Create2TestComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<Create2TestComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<Create2TestComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<Create2TestComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<Create2TestComposerResults<TReturns>>
}
export type Create2TestComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

