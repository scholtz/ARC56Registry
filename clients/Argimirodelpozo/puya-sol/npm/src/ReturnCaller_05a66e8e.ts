/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"ReturnCaller","structs":{},"methods":[{"name":"callGetNumber","args":[],"returns":{"type":"uint256"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Calls getNumber() via .call and decodes the uint256 return","events":[],"recommendations":{}},{"name":"callAdd","args":[{"type":"uint256","name":"__arc4_a"},{"type":"uint256","name":"__arc4_b"}],"returns":{"type":"uint256"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Calls add(a, b) via .call and decodes the uint256 return","events":[],"recommendations":{}},{"name":"callIsEven","args":[{"type":"uint256","name":"__arc4_n"}],"returns":{"type":"bool"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Calls isEven(n) via .call and decodes the bool return","events":[],"recommendations":{}},{"name":"target","args":[],"returns":{"type":"address"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}}],"arcs":[22,28],"networks":{},"state":{"schema":{"global":{"ints":0,"bytes":1},"local":{"ints":0,"bytes":0}},"keys":{"global":{"target":{"keyType":"AVMString","valueType":"address","key":"dGFyZ2V0"}},"local":{},"box":{}},"maps":{"global":{},"local":{},"box":{}}},"bareActions":{"create":[],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[142,234,312,379],"errorMessage":"check target exists"},{"pc":[191,199,286],"errorMessage":"invalid number of bytes for arc4.uint256"},{"pc":[137,212,299,375],"errorMessage":"not payable"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"events":[]} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


/**
 * The argument types for the ReturnCaller contract
 */
export type ReturnCallerArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'callGetNumber()uint256': Record<string, never>
    'callAdd(uint256,uint256)uint256': {
      __arc4A: bigint | number
      __arc4B: bigint | number
    }
    'callIsEven(uint256)bool': {
      __arc4N: bigint | number
    }
    'target()address': Record<string, never>
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'callGetNumber()uint256': []
    'callAdd(uint256,uint256)uint256': [__arc4A: bigint | number, __arc4B: bigint | number]
    'callIsEven(uint256)bool': [__arc4N: bigint | number]
    'target()address': []
  }
}

/**
 * The return type for each method
 */
export type ReturnCallerReturns = {
  'callGetNumber()uint256': bigint
  'callAdd(uint256,uint256)uint256': bigint
  'callIsEven(uint256)bool': boolean
  'target()address': string
}

/**
 * Defines the types of available calls and state of the ReturnCaller smart contract.
 */
export type ReturnCallerTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'callGetNumber()uint256' | 'callGetNumber', {
      argsObj: ReturnCallerArgs['obj']['callGetNumber()uint256']
      argsTuple: ReturnCallerArgs['tuple']['callGetNumber()uint256']
      returns: ReturnCallerReturns['callGetNumber()uint256']
    }>
    & Record<'callAdd(uint256,uint256)uint256' | 'callAdd', {
      argsObj: ReturnCallerArgs['obj']['callAdd(uint256,uint256)uint256']
      argsTuple: ReturnCallerArgs['tuple']['callAdd(uint256,uint256)uint256']
      returns: ReturnCallerReturns['callAdd(uint256,uint256)uint256']
    }>
    & Record<'callIsEven(uint256)bool' | 'callIsEven', {
      argsObj: ReturnCallerArgs['obj']['callIsEven(uint256)bool']
      argsTuple: ReturnCallerArgs['tuple']['callIsEven(uint256)bool']
      returns: ReturnCallerReturns['callIsEven(uint256)bool']
    }>
    & Record<'target()address' | 'target', {
      argsObj: ReturnCallerArgs['obj']['target()address']
      argsTuple: ReturnCallerArgs['tuple']['target()address']
      returns: ReturnCallerReturns['target()address']
    }>
  /**
   * Defines the shape of the state of the application.
   */
  state: {
    global: {
      keys: {
        target: string
      }
      maps: {}
    }
  }
}

/**
 * Defines the possible abi call signatures.
 */
export type ReturnCallerSignatures = keyof ReturnCallerTypes['methods']
/**
 * Defines the possible abi call signatures for methods that return a non-void value.
 */
export type ReturnCallerNonVoidMethodSignatures = keyof ReturnCallerTypes['methods'] extends infer T ? T extends keyof ReturnCallerTypes['methods'] ? MethodReturn<T> extends void ? never : T  : never : never
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the ReturnCaller smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends ReturnCallerSignatures> = ReturnCallerTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the ReturnCaller smart contract to the method's return type
 */
export type MethodReturn<TSignature extends ReturnCallerSignatures> = ReturnCallerTypes['methods'][TSignature]['returns']

/**
 * Defines the shape of the keyed global state of the application.
 */
export type GlobalKeysState = ReturnCallerTypes['state']['global']['keys']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the ReturnCaller smart contract
 */
export abstract class ReturnCallerParamsFactory {
  /**
   * Constructs a no op call for the callGetNumber()uint256 ABI method
   *
   * Calls getNumber() via .call and decodes the uint256 return
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static callGetNumber(params: CallParams<ReturnCallerArgs['obj']['callGetNumber()uint256'] | ReturnCallerArgs['tuple']['callGetNumber()uint256']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'callGetNumber()uint256' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the callAdd(uint256,uint256)uint256 ABI method
   *
   * Calls add(a, b) via .call and decodes the uint256 return
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static callAdd(params: CallParams<ReturnCallerArgs['obj']['callAdd(uint256,uint256)uint256'] | ReturnCallerArgs['tuple']['callAdd(uint256,uint256)uint256']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'callAdd(uint256,uint256)uint256' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.__arc4A, params.args.__arc4B],
    }
  }
  /**
   * Constructs a no op call for the callIsEven(uint256)bool ABI method
   *
   * Calls isEven(n) via .call and decodes the bool return
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static callIsEven(params: CallParams<ReturnCallerArgs['obj']['callIsEven(uint256)bool'] | ReturnCallerArgs['tuple']['callIsEven(uint256)bool']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'callIsEven(uint256)bool' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.__arc4N],
    }
  }
  /**
   * Constructs a no op call for the target()address ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static target(params: CallParams<ReturnCallerArgs['obj']['target()address'] | ReturnCallerArgs['tuple']['target()address']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'target()address' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
}

/**
 * A client to make calls to the ReturnCaller smart contract
 */
export class ReturnCallerClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `ReturnCallerClient`
   *
   * @param appClient An `AppClient` instance which has been created with the ReturnCaller app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `ReturnCallerClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Checks for decode errors on the given return value and maps the return value to the return type for the given method
   * @returns The typed return value or undefined if there was no value
   */
  decodeReturnValue<TSignature extends ReturnCallerNonVoidMethodSignatures>(method: TSignature, returnValue: ABIReturn | undefined) {
    return returnValue !== undefined ? getArc56ReturnValue<MethodReturn<TSignature>>(returnValue, this.appClient.getABIMethod(method), APP_SPEC.structs) : undefined
  }

  /**
   * Returns a new `ReturnCallerClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<ReturnCallerClient> {
    return new ReturnCallerClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `ReturnCallerClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<ReturnCallerClient> {
    return new ReturnCallerClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Makes a clear_state call to an existing instance of the ReturnCaller smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the ReturnCaller smart contract using the `callGetNumber()uint256` ABI method.
     *
     * Calls getNumber() via .call and decodes the uint256 return
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    callGetNumber: (params: CallParams<ReturnCallerArgs['obj']['callGetNumber()uint256'] | ReturnCallerArgs['tuple']['callGetNumber()uint256']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(ReturnCallerParamsFactory.callGetNumber(params))
    },

    /**
     * Makes a call to the ReturnCaller smart contract using the `callAdd(uint256,uint256)uint256` ABI method.
     *
     * Calls add(a, b) via .call and decodes the uint256 return
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    callAdd: (params: CallParams<ReturnCallerArgs['obj']['callAdd(uint256,uint256)uint256'] | ReturnCallerArgs['tuple']['callAdd(uint256,uint256)uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(ReturnCallerParamsFactory.callAdd(params))
    },

    /**
     * Makes a call to the ReturnCaller smart contract using the `callIsEven(uint256)bool` ABI method.
     *
     * Calls isEven(n) via .call and decodes the bool return
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    callIsEven: (params: CallParams<ReturnCallerArgs['obj']['callIsEven(uint256)bool'] | ReturnCallerArgs['tuple']['callIsEven(uint256)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(ReturnCallerParamsFactory.callIsEven(params))
    },

    /**
     * Makes a call to the ReturnCaller smart contract using the `target()address` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    target: (params: CallParams<ReturnCallerArgs['obj']['target()address'] | ReturnCallerArgs['tuple']['target()address']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(ReturnCallerParamsFactory.target(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Makes a clear_state call to an existing instance of the ReturnCaller smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the ReturnCaller smart contract using the `callGetNumber()uint256` ABI method.
     *
     * Calls getNumber() via .call and decodes the uint256 return
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    callGetNumber: (params: CallParams<ReturnCallerArgs['obj']['callGetNumber()uint256'] | ReturnCallerArgs['tuple']['callGetNumber()uint256']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(ReturnCallerParamsFactory.callGetNumber(params))
    },

    /**
     * Makes a call to the ReturnCaller smart contract using the `callAdd(uint256,uint256)uint256` ABI method.
     *
     * Calls add(a, b) via .call and decodes the uint256 return
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    callAdd: (params: CallParams<ReturnCallerArgs['obj']['callAdd(uint256,uint256)uint256'] | ReturnCallerArgs['tuple']['callAdd(uint256,uint256)uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(ReturnCallerParamsFactory.callAdd(params))
    },

    /**
     * Makes a call to the ReturnCaller smart contract using the `callIsEven(uint256)bool` ABI method.
     *
     * Calls isEven(n) via .call and decodes the bool return
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    callIsEven: (params: CallParams<ReturnCallerArgs['obj']['callIsEven(uint256)bool'] | ReturnCallerArgs['tuple']['callIsEven(uint256)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(ReturnCallerParamsFactory.callIsEven(params))
    },

    /**
     * Makes a call to the ReturnCaller smart contract using the `target()address` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    target: (params: CallParams<ReturnCallerArgs['obj']['target()address'] | ReturnCallerArgs['tuple']['target()address']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(ReturnCallerParamsFactory.target(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Makes a clear_state call to an existing instance of the ReturnCaller smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the ReturnCaller smart contract using the `callGetNumber()uint256` ABI method.
     *
     * Calls getNumber() via .call and decodes the uint256 return
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    callGetNumber: async (params: CallParams<ReturnCallerArgs['obj']['callGetNumber()uint256'] | ReturnCallerArgs['tuple']['callGetNumber()uint256']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(ReturnCallerParamsFactory.callGetNumber(params))
      return {...result, return: result.return as unknown as (undefined | ReturnCallerReturns['callGetNumber()uint256'])}
    },

    /**
     * Makes a call to the ReturnCaller smart contract using the `callAdd(uint256,uint256)uint256` ABI method.
     *
     * Calls add(a, b) via .call and decodes the uint256 return
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    callAdd: async (params: CallParams<ReturnCallerArgs['obj']['callAdd(uint256,uint256)uint256'] | ReturnCallerArgs['tuple']['callAdd(uint256,uint256)uint256']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(ReturnCallerParamsFactory.callAdd(params))
      return {...result, return: result.return as unknown as (undefined | ReturnCallerReturns['callAdd(uint256,uint256)uint256'])}
    },

    /**
     * Makes a call to the ReturnCaller smart contract using the `callIsEven(uint256)bool` ABI method.
     *
     * Calls isEven(n) via .call and decodes the bool return
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    callIsEven: async (params: CallParams<ReturnCallerArgs['obj']['callIsEven(uint256)bool'] | ReturnCallerArgs['tuple']['callIsEven(uint256)bool']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(ReturnCallerParamsFactory.callIsEven(params))
      return {...result, return: result.return as unknown as (undefined | ReturnCallerReturns['callIsEven(uint256)bool'])}
    },

    /**
     * Makes a call to the ReturnCaller smart contract using the `target()address` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    target: async (params: CallParams<ReturnCallerArgs['obj']['target()address'] | ReturnCallerArgs['tuple']['target()address']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(ReturnCallerParamsFactory.target(params))
      return {...result, return: result.return as unknown as (undefined | ReturnCallerReturns['target()address'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new ReturnCallerClient(this.appClient.clone(params))
  }

  /**
   * Makes a readonly (simulated) call to the ReturnCaller smart contract using the `target()address` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async target(params: CallParams<ReturnCallerArgs['obj']['target()address'] | ReturnCallerArgs['tuple']['target()address']> = {args: []}) {
    const result = await this.appClient.send.call(ReturnCallerParamsFactory.target(params))
    return result.return as unknown as ReturnCallerReturns['target()address']
  }

  /**
   * Methods to access state for the current ReturnCaller app
   */
  state = {
    /**
     * Methods to access global state for the current ReturnCaller app
     */
    global: {
      /**
       * Get all current keyed values from global state
       */
      getAll: async (): Promise<Partial<Expand<GlobalKeysState>>> => {
        const result = await this.appClient.state.global.getAll()
        return {
          target: result.target,
        }
      },
      /**
       * Get the current value of the target key in global state
       */
      target: async (): Promise<string | undefined> => { return (await this.appClient.state.global.getValue("target")) as string | undefined },
    },
  }

  public newGroup(): ReturnCallerComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a callGetNumber()uint256 method call against the ReturnCaller contract
       */
      callGetNumber(params: CallParams<ReturnCallerArgs['obj']['callGetNumber()uint256'] | ReturnCallerArgs['tuple']['callGetNumber()uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.callGetNumber(params)))
        resultMappers.push((v) => client.decodeReturnValue('callGetNumber()uint256', v))
        return this
      },
      /**
       * Add a callAdd(uint256,uint256)uint256 method call against the ReturnCaller contract
       */
      callAdd(params: CallParams<ReturnCallerArgs['obj']['callAdd(uint256,uint256)uint256'] | ReturnCallerArgs['tuple']['callAdd(uint256,uint256)uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.callAdd(params)))
        resultMappers.push((v) => client.decodeReturnValue('callAdd(uint256,uint256)uint256', v))
        return this
      },
      /**
       * Add a callIsEven(uint256)bool method call against the ReturnCaller contract
       */
      callIsEven(params: CallParams<ReturnCallerArgs['obj']['callIsEven(uint256)bool'] | ReturnCallerArgs['tuple']['callIsEven(uint256)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.callIsEven(params)))
        resultMappers.push((v) => client.decodeReturnValue('callIsEven(uint256)bool', v))
        return this
      },
      /**
       * Add a target()address method call against the ReturnCaller contract
       */
      target(params: CallParams<ReturnCallerArgs['obj']['target()address'] | ReturnCallerArgs['tuple']['target()address']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.target(params)))
        resultMappers.push((v) => client.decodeReturnValue('target()address', v))
        return this
      },
      /**
       * Add a clear state call to the ReturnCaller contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as ReturnCallerComposer
  }
}
export type ReturnCallerComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the callGetNumber()uint256 ABI method.
   *
   * Calls getNumber() via .call and decodes the uint256 return
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  callGetNumber(params?: CallParams<ReturnCallerArgs['obj']['callGetNumber()uint256'] | ReturnCallerArgs['tuple']['callGetNumber()uint256']>): ReturnCallerComposer<[...TReturns, ReturnCallerReturns['callGetNumber()uint256'] | undefined]>

  /**
   * Calls the callAdd(uint256,uint256)uint256 ABI method.
   *
   * Calls add(a, b) via .call and decodes the uint256 return
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  callAdd(params?: CallParams<ReturnCallerArgs['obj']['callAdd(uint256,uint256)uint256'] | ReturnCallerArgs['tuple']['callAdd(uint256,uint256)uint256']>): ReturnCallerComposer<[...TReturns, ReturnCallerReturns['callAdd(uint256,uint256)uint256'] | undefined]>

  /**
   * Calls the callIsEven(uint256)bool ABI method.
   *
   * Calls isEven(n) via .call and decodes the bool return
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  callIsEven(params?: CallParams<ReturnCallerArgs['obj']['callIsEven(uint256)bool'] | ReturnCallerArgs['tuple']['callIsEven(uint256)bool']>): ReturnCallerComposer<[...TReturns, ReturnCallerReturns['callIsEven(uint256)bool'] | undefined]>

  /**
   * Calls the target()address ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  target(params?: CallParams<ReturnCallerArgs['obj']['target()address'] | ReturnCallerArgs['tuple']['target()address']>): ReturnCallerComposer<[...TReturns, ReturnCallerReturns['target()address'] | undefined]>

  /**
   * Makes a clear_state call to an existing instance of the ReturnCaller smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): ReturnCallerComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): ReturnCallerComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<ReturnCallerComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<ReturnCallerComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<ReturnCallerComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<ReturnCallerComposerResults<TReturns>>
}
export type ReturnCallerComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

