/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"Ballot","structs":{},"methods":[{"name":"__postInit","args":[],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}},{"name":"getVoteCount","args":[{"type":"address","name":"addr"}],"returns":{"type":"uint256"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}},{"name":"grantVoteRight","args":[{"type":"address","name":"addr"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}},{"name":"vote","args":[{"type":"address","name":"voter"},{"type":"address","name":"vote"}],"returns":{"type":"bool"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}}],"arcs":[22,28],"networks":{},"state":{"schema":{"global":{"ints":1,"bytes":0},"local":{"ints":0,"bytes":0}},"keys":{"global":{"__ctor_pending":{"keyType":"AVMString","valueType":"AVMUint64","key":"X19jdG9yX3BlbmRpbmc="}},"local":{},"box":{}},"maps":{"global":{},"local":{},"box":{"canVote":{"keyType":"AVMBytes","valueType":"uint64","prefix":"Y2FuVm90ZQ=="},"voteCount":{"keyType":"AVMBytes","valueType":"AVMBytes","prefix":"dm90ZUNvdW50"},"voted":{"keyType":"AVMBytes","valueType":"uint64","prefix":"dm90ZWQ="}}}},"bareActions":{"create":[],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[373],"errorMessage":"__postInit already called"},{"pc":[167,177,288,326],"errorMessage":"invalid number of bytes for arc4.static_array<arc4.uint8, 32>"},{"pc":[190,301,339],"errorMessage":"not payable"},{"pc":[356],"errorMessage":"overflow"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"events":[]} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


/**
 * The argument types for the Ballot contract
 */
export type BallotArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    '__postInit()void': Record<string, never>
    'getVoteCount(address)uint256': {
      addr: string
    }
    'grantVoteRight(address)void': {
      addr: string
    }
    'vote(address,address)bool': {
      voter: string
      vote: string
    }
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    '__postInit()void': []
    'getVoteCount(address)uint256': [addr: string]
    'grantVoteRight(address)void': [addr: string]
    'vote(address,address)bool': [voter: string, vote: string]
  }
}

/**
 * The return type for each method
 */
export type BallotReturns = {
  '__postInit()void': void
  'getVoteCount(address)uint256': bigint
  'grantVoteRight(address)void': void
  'vote(address,address)bool': boolean
}

/**
 * Defines the types of available calls and state of the Ballot smart contract.
 */
export type BallotTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'__postInit()void' | '__postInit', {
      argsObj: BallotArgs['obj']['__postInit()void']
      argsTuple: BallotArgs['tuple']['__postInit()void']
      returns: BallotReturns['__postInit()void']
    }>
    & Record<'getVoteCount(address)uint256' | 'getVoteCount', {
      argsObj: BallotArgs['obj']['getVoteCount(address)uint256']
      argsTuple: BallotArgs['tuple']['getVoteCount(address)uint256']
      returns: BallotReturns['getVoteCount(address)uint256']
    }>
    & Record<'grantVoteRight(address)void' | 'grantVoteRight', {
      argsObj: BallotArgs['obj']['grantVoteRight(address)void']
      argsTuple: BallotArgs['tuple']['grantVoteRight(address)void']
      returns: BallotReturns['grantVoteRight(address)void']
    }>
    & Record<'vote(address,address)bool' | 'vote', {
      argsObj: BallotArgs['obj']['vote(address,address)bool']
      argsTuple: BallotArgs['tuple']['vote(address,address)bool']
      returns: BallotReturns['vote(address,address)bool']
    }>
  /**
   * Defines the shape of the state of the application.
   */
  state: {
    global: {
      keys: {
        __ctorPending: bigint
      }
      maps: {}
    }
    box: {
      keys: {}
      maps: {
        canVote: Map<Uint8Array | string, bigint>
        voteCount: Map<Uint8Array | string, Uint8Array>
        voted: Map<Uint8Array | string, bigint>
      }
    }
  }
}

/**
 * Defines the possible abi call signatures.
 */
export type BallotSignatures = keyof BallotTypes['methods']
/**
 * Defines the possible abi call signatures for methods that return a non-void value.
 */
export type BallotNonVoidMethodSignatures = keyof BallotTypes['methods'] extends infer T ? T extends keyof BallotTypes['methods'] ? MethodReturn<T> extends void ? never : T  : never : never
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the Ballot smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends BallotSignatures> = BallotTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the Ballot smart contract to the method's return type
 */
export type MethodReturn<TSignature extends BallotSignatures> = BallotTypes['methods'][TSignature]['returns']

/**
 * Defines the shape of the keyed global state of the application.
 */
export type GlobalKeysState = BallotTypes['state']['global']['keys']

/**
 * Defines the shape of the keyed box state of the application.
 */
export type BoxKeysState = BallotTypes['state']['box']['keys']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the Ballot smart contract
 */
export abstract class BallotParamsFactory {
  /**
   * Constructs a no op call for the __postInit()void ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static __postInit(params: CallParams<BallotArgs['obj']['__postInit()void'] | BallotArgs['tuple']['__postInit()void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: '__postInit()void' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the getVoteCount(address)uint256 ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static getVoteCount(params: CallParams<BallotArgs['obj']['getVoteCount(address)uint256'] | BallotArgs['tuple']['getVoteCount(address)uint256']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'getVoteCount(address)uint256' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.addr],
    }
  }
  /**
   * Constructs a no op call for the grantVoteRight(address)void ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static grantVoteRight(params: CallParams<BallotArgs['obj']['grantVoteRight(address)void'] | BallotArgs['tuple']['grantVoteRight(address)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'grantVoteRight(address)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.addr],
    }
  }
  /**
   * Constructs a no op call for the vote(address,address)bool ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static vote(params: CallParams<BallotArgs['obj']['vote(address,address)bool'] | BallotArgs['tuple']['vote(address,address)bool']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'vote(address,address)bool' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.voter, params.args.vote],
    }
  }
}

/**
 * A client to make calls to the Ballot smart contract
 */
export class BallotClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `BallotClient`
   *
   * @param appClient An `AppClient` instance which has been created with the Ballot app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `BallotClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Checks for decode errors on the given return value and maps the return value to the return type for the given method
   * @returns The typed return value or undefined if there was no value
   */
  decodeReturnValue<TSignature extends BallotNonVoidMethodSignatures>(method: TSignature, returnValue: ABIReturn | undefined) {
    return returnValue !== undefined ? getArc56ReturnValue<MethodReturn<TSignature>>(returnValue, this.appClient.getABIMethod(method), APP_SPEC.structs) : undefined
  }

  /**
   * Returns a new `BallotClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<BallotClient> {
    return new BallotClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `BallotClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<BallotClient> {
    return new BallotClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Makes a clear_state call to an existing instance of the Ballot smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the Ballot smart contract using the `__postInit()void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    __postInit: (params: CallParams<BallotArgs['obj']['__postInit()void'] | BallotArgs['tuple']['__postInit()void']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(BallotParamsFactory.__postInit(params))
    },

    /**
     * Makes a call to the Ballot smart contract using the `getVoteCount(address)uint256` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    getVoteCount: (params: CallParams<BallotArgs['obj']['getVoteCount(address)uint256'] | BallotArgs['tuple']['getVoteCount(address)uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(BallotParamsFactory.getVoteCount(params))
    },

    /**
     * Makes a call to the Ballot smart contract using the `grantVoteRight(address)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    grantVoteRight: (params: CallParams<BallotArgs['obj']['grantVoteRight(address)void'] | BallotArgs['tuple']['grantVoteRight(address)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(BallotParamsFactory.grantVoteRight(params))
    },

    /**
     * Makes a call to the Ballot smart contract using the `vote(address,address)bool` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    vote: (params: CallParams<BallotArgs['obj']['vote(address,address)bool'] | BallotArgs['tuple']['vote(address,address)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(BallotParamsFactory.vote(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Makes a clear_state call to an existing instance of the Ballot smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the Ballot smart contract using the `__postInit()void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    __postInit: (params: CallParams<BallotArgs['obj']['__postInit()void'] | BallotArgs['tuple']['__postInit()void']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(BallotParamsFactory.__postInit(params))
    },

    /**
     * Makes a call to the Ballot smart contract using the `getVoteCount(address)uint256` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    getVoteCount: (params: CallParams<BallotArgs['obj']['getVoteCount(address)uint256'] | BallotArgs['tuple']['getVoteCount(address)uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(BallotParamsFactory.getVoteCount(params))
    },

    /**
     * Makes a call to the Ballot smart contract using the `grantVoteRight(address)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    grantVoteRight: (params: CallParams<BallotArgs['obj']['grantVoteRight(address)void'] | BallotArgs['tuple']['grantVoteRight(address)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(BallotParamsFactory.grantVoteRight(params))
    },

    /**
     * Makes a call to the Ballot smart contract using the `vote(address,address)bool` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    vote: (params: CallParams<BallotArgs['obj']['vote(address,address)bool'] | BallotArgs['tuple']['vote(address,address)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(BallotParamsFactory.vote(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Makes a clear_state call to an existing instance of the Ballot smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the Ballot smart contract using the `__postInit()void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    __postInit: async (params: CallParams<BallotArgs['obj']['__postInit()void'] | BallotArgs['tuple']['__postInit()void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(BallotParamsFactory.__postInit(params))
      return {...result, return: result.return as unknown as (undefined | BallotReturns['__postInit()void'])}
    },

    /**
     * Makes a call to the Ballot smart contract using the `getVoteCount(address)uint256` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    getVoteCount: async (params: CallParams<BallotArgs['obj']['getVoteCount(address)uint256'] | BallotArgs['tuple']['getVoteCount(address)uint256']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(BallotParamsFactory.getVoteCount(params))
      return {...result, return: result.return as unknown as (undefined | BallotReturns['getVoteCount(address)uint256'])}
    },

    /**
     * Makes a call to the Ballot smart contract using the `grantVoteRight(address)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    grantVoteRight: async (params: CallParams<BallotArgs['obj']['grantVoteRight(address)void'] | BallotArgs['tuple']['grantVoteRight(address)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(BallotParamsFactory.grantVoteRight(params))
      return {...result, return: result.return as unknown as (undefined | BallotReturns['grantVoteRight(address)void'])}
    },

    /**
     * Makes a call to the Ballot smart contract using the `vote(address,address)bool` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    vote: async (params: CallParams<BallotArgs['obj']['vote(address,address)bool'] | BallotArgs['tuple']['vote(address,address)bool']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(BallotParamsFactory.vote(params))
      return {...result, return: result.return as unknown as (undefined | BallotReturns['vote(address,address)bool'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new BallotClient(this.appClient.clone(params))
  }

  /**
   * Methods to access state for the current Ballot app
   */
  state = {
    /**
     * Methods to access global state for the current Ballot app
     */
    global: {
      /**
       * Get all current keyed values from global state
       */
      getAll: async (): Promise<Partial<Expand<GlobalKeysState>>> => {
        const result = await this.appClient.state.global.getAll()
        return {
          __ctorPending: result.__ctor_pending,
        }
      },
      /**
       * Get the current value of the __ctor_pending key in global state
       */
      __ctorPending: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("__ctor_pending")) as bigint | undefined },
    },
    /**
     * Methods to access box state for the current Ballot app
     */
    box: {
      /**
       * Get all current keyed values from box state
       */
      getAll: async (): Promise<Partial<Expand<BoxKeysState>>> => {
        const result = await this.appClient.state.box.getAll()
        return {
        }
      },
      /**
       * Get values from the canVote map in box state
       */
      canVote: {
        /**
         * Get all current values of the canVote map in box state
         */
        getMap: async (): Promise<Map<Uint8Array, bigint>> => { return (await this.appClient.state.box.getMap("canVote")) as Map<Uint8Array, bigint> },
        /**
         * Get a current value of the canVote map by key from box state
         */
        value: async (key: Uint8Array | string): Promise<bigint | undefined> => { return await this.appClient.state.box.getMapValue("canVote", key) as bigint | undefined },
      },
      /**
       * Get values from the voteCount map in box state
       */
      voteCount: {
        /**
         * Get all current values of the voteCount map in box state
         */
        getMap: async (): Promise<Map<Uint8Array, Uint8Array>> => { return (await this.appClient.state.box.getMap("voteCount")) as Map<Uint8Array, Uint8Array> },
        /**
         * Get a current value of the voteCount map by key from box state
         */
        value: async (key: Uint8Array | string): Promise<Uint8Array | undefined> => { return await this.appClient.state.box.getMapValue("voteCount", key) as Uint8Array | undefined },
      },
      /**
       * Get values from the voted map in box state
       */
      voted: {
        /**
         * Get all current values of the voted map in box state
         */
        getMap: async (): Promise<Map<Uint8Array, bigint>> => { return (await this.appClient.state.box.getMap("voted")) as Map<Uint8Array, bigint> },
        /**
         * Get a current value of the voted map by key from box state
         */
        value: async (key: Uint8Array | string): Promise<bigint | undefined> => { return await this.appClient.state.box.getMapValue("voted", key) as bigint | undefined },
      },
    },
  }

  public newGroup(): BallotComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a __postInit()void method call against the Ballot contract
       */
      __postInit(params: CallParams<BallotArgs['obj']['__postInit()void'] | BallotArgs['tuple']['__postInit()void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.__postInit(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a getVoteCount(address)uint256 method call against the Ballot contract
       */
      getVoteCount(params: CallParams<BallotArgs['obj']['getVoteCount(address)uint256'] | BallotArgs['tuple']['getVoteCount(address)uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.getVoteCount(params)))
        resultMappers.push((v) => client.decodeReturnValue('getVoteCount(address)uint256', v))
        return this
      },
      /**
       * Add a grantVoteRight(address)void method call against the Ballot contract
       */
      grantVoteRight(params: CallParams<BallotArgs['obj']['grantVoteRight(address)void'] | BallotArgs['tuple']['grantVoteRight(address)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.grantVoteRight(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a vote(address,address)bool method call against the Ballot contract
       */
      vote(params: CallParams<BallotArgs['obj']['vote(address,address)bool'] | BallotArgs['tuple']['vote(address,address)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.vote(params)))
        resultMappers.push((v) => client.decodeReturnValue('vote(address,address)bool', v))
        return this
      },
      /**
       * Add a clear state call to the Ballot contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as BallotComposer
  }
}
export type BallotComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the __postInit()void ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  __postInit(params?: CallParams<BallotArgs['obj']['__postInit()void'] | BallotArgs['tuple']['__postInit()void']>): BallotComposer<[...TReturns, BallotReturns['__postInit()void'] | undefined]>

  /**
   * Calls the getVoteCount(address)uint256 ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  getVoteCount(params?: CallParams<BallotArgs['obj']['getVoteCount(address)uint256'] | BallotArgs['tuple']['getVoteCount(address)uint256']>): BallotComposer<[...TReturns, BallotReturns['getVoteCount(address)uint256'] | undefined]>

  /**
   * Calls the grantVoteRight(address)void ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  grantVoteRight(params?: CallParams<BallotArgs['obj']['grantVoteRight(address)void'] | BallotArgs['tuple']['grantVoteRight(address)void']>): BallotComposer<[...TReturns, BallotReturns['grantVoteRight(address)void'] | undefined]>

  /**
   * Calls the vote(address,address)bool ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  vote(params?: CallParams<BallotArgs['obj']['vote(address,address)bool'] | BallotArgs['tuple']['vote(address,address)bool']>): BallotComposer<[...TReturns, BallotReturns['vote(address,address)bool'] | undefined]>

  /**
   * Makes a clear_state call to an existing instance of the Ballot smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): BallotComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): BallotComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<BallotComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<BallotComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<BallotComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<BallotComposerResults<TReturns>>
}
export type BallotComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

