/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"VerifierMock","structs":{},"methods":[{"name":"verifyProof","args":[{"type":"byte[]","name":"_param0"},{"type":"uint256[6]","name":"__arc4__param1"}],"returns":{"type":"bool"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}}],"arcs":[22,28],"desc":"@dev A mock verifier that always returns true for testing purposes.","networks":{},"state":{"schema":{"global":{"ints":0,"bytes":0},"local":{"ints":0,"bytes":0}},"keys":{"global":{},"local":{},"box":{}},"maps":{"global":{},"local":{},"box":{}}},"bareActions":{"create":[],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[39],"errorMessage":"invalid array length header"},{"pc":[46],"errorMessage":"invalid number of bytes for arc4.dynamic_array<arc4.uint8>"},{"pc":[55],"errorMessage":"invalid number of bytes for arc4.static_array<arc4.uint256, 6>"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"events":[]} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


/**
 * The argument types for the VerifierMock contract
 */
export type VerifierMockArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'verifyProof(byte[],uint256[6])bool': {
      _param0: Uint8Array
      __arc4Param1: [bigint | number, bigint | number, bigint | number, bigint | number, bigint | number, bigint | number]
    }
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'verifyProof(byte[],uint256[6])bool': [_param0: Uint8Array, __arc4Param1: [bigint | number, bigint | number, bigint | number, bigint | number, bigint | number, bigint | number]]
  }
}

/**
 * The return type for each method
 */
export type VerifierMockReturns = {
  'verifyProof(byte[],uint256[6])bool': boolean
}

/**
 * Defines the types of available calls and state of the VerifierMock smart contract.
 */
export type VerifierMockTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'verifyProof(byte[],uint256[6])bool' | 'verifyProof', {
      argsObj: VerifierMockArgs['obj']['verifyProof(byte[],uint256[6])bool']
      argsTuple: VerifierMockArgs['tuple']['verifyProof(byte[],uint256[6])bool']
      returns: VerifierMockReturns['verifyProof(byte[],uint256[6])bool']
    }>
}

/**
 * Defines the possible abi call signatures.
 */
export type VerifierMockSignatures = keyof VerifierMockTypes['methods']
/**
 * Defines the possible abi call signatures for methods that return a non-void value.
 */
export type VerifierMockNonVoidMethodSignatures = keyof VerifierMockTypes['methods'] extends infer T ? T extends keyof VerifierMockTypes['methods'] ? MethodReturn<T> extends void ? never : T  : never : never
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the VerifierMock smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends VerifierMockSignatures> = VerifierMockTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the VerifierMock smart contract to the method's return type
 */
export type MethodReturn<TSignature extends VerifierMockSignatures> = VerifierMockTypes['methods'][TSignature]['returns']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the VerifierMock smart contract
 */
export abstract class VerifierMockParamsFactory {
  /**
   * Constructs a no op call for the verifyProof(byte[],uint256[6])bool ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static verifyProof(params: CallParams<VerifierMockArgs['obj']['verifyProof(byte[],uint256[6])bool'] | VerifierMockArgs['tuple']['verifyProof(byte[],uint256[6])bool']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'verifyProof(byte[],uint256[6])bool' as const,
      args: Array.isArray(params.args) ? params.args : [params.args._param0, params.args.__arc4Param1],
    }
  }
}

/**
 * A client to make calls to the VerifierMock smart contract
 */
export class VerifierMockClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `VerifierMockClient`
   *
   * @param appClient An `AppClient` instance which has been created with the VerifierMock app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `VerifierMockClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Checks for decode errors on the given return value and maps the return value to the return type for the given method
   * @returns The typed return value or undefined if there was no value
   */
  decodeReturnValue<TSignature extends VerifierMockNonVoidMethodSignatures>(method: TSignature, returnValue: ABIReturn | undefined) {
    return returnValue !== undefined ? getArc56ReturnValue<MethodReturn<TSignature>>(returnValue, this.appClient.getABIMethod(method), APP_SPEC.structs) : undefined
  }

  /**
   * Returns a new `VerifierMockClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<VerifierMockClient> {
    return new VerifierMockClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `VerifierMockClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<VerifierMockClient> {
    return new VerifierMockClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Makes a clear_state call to an existing instance of the VerifierMock smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the VerifierMock smart contract using the `verifyProof(byte[],uint256[6])bool` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    verifyProof: (params: CallParams<VerifierMockArgs['obj']['verifyProof(byte[],uint256[6])bool'] | VerifierMockArgs['tuple']['verifyProof(byte[],uint256[6])bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(VerifierMockParamsFactory.verifyProof(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Makes a clear_state call to an existing instance of the VerifierMock smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the VerifierMock smart contract using the `verifyProof(byte[],uint256[6])bool` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    verifyProof: (params: CallParams<VerifierMockArgs['obj']['verifyProof(byte[],uint256[6])bool'] | VerifierMockArgs['tuple']['verifyProof(byte[],uint256[6])bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(VerifierMockParamsFactory.verifyProof(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Makes a clear_state call to an existing instance of the VerifierMock smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the VerifierMock smart contract using the `verifyProof(byte[],uint256[6])bool` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    verifyProof: async (params: CallParams<VerifierMockArgs['obj']['verifyProof(byte[],uint256[6])bool'] | VerifierMockArgs['tuple']['verifyProof(byte[],uint256[6])bool']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(VerifierMockParamsFactory.verifyProof(params))
      return {...result, return: result.return as unknown as (undefined | VerifierMockReturns['verifyProof(byte[],uint256[6])bool'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new VerifierMockClient(this.appClient.clone(params))
  }

  /**
   * Makes a readonly (simulated) call to the VerifierMock smart contract using the `verifyProof(byte[],uint256[6])bool` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async verifyProof(params: CallParams<VerifierMockArgs['obj']['verifyProof(byte[],uint256[6])bool'] | VerifierMockArgs['tuple']['verifyProof(byte[],uint256[6])bool']>) {
    const result = await this.appClient.send.call(VerifierMockParamsFactory.verifyProof(params))
    return result.return as unknown as VerifierMockReturns['verifyProof(byte[],uint256[6])bool']
  }

  /**
   * Methods to access state for the current VerifierMock app
   */
  state = {
  }

  public newGroup(): VerifierMockComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a verifyProof(byte[],uint256[6])bool method call against the VerifierMock contract
       */
      verifyProof(params: CallParams<VerifierMockArgs['obj']['verifyProof(byte[],uint256[6])bool'] | VerifierMockArgs['tuple']['verifyProof(byte[],uint256[6])bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.verifyProof(params)))
        resultMappers.push((v) => client.decodeReturnValue('verifyProof(byte[],uint256[6])bool', v))
        return this
      },
      /**
       * Add a clear state call to the VerifierMock contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as VerifierMockComposer
  }
}
export type VerifierMockComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the verifyProof(byte[],uint256[6])bool ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  verifyProof(params?: CallParams<VerifierMockArgs['obj']['verifyProof(byte[],uint256[6])bool'] | VerifierMockArgs['tuple']['verifyProof(byte[],uint256[6])bool']>): VerifierMockComposer<[...TReturns, VerifierMockReturns['verifyProof(byte[],uint256[6])bool'] | undefined]>

  /**
   * Makes a clear_state call to an existing instance of the VerifierMock smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): VerifierMockComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): VerifierMockComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<VerifierMockComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<VerifierMockComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<VerifierMockComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<VerifierMockComposerResults<TReturns>>
}
export type VerifierMockComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

