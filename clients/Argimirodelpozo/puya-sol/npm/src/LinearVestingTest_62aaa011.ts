/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"LinearVestingTest","structs":{},"methods":[{"name":"createVesting","args":[{"type":"address","name":"beneficiary"},{"type":"uint512","name":"amount"},{"type":"uint512","name":"start"},{"type":"uint512","name":"dur"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}},{"name":"vestedAmount","args":[{"type":"address","name":"beneficiary"},{"type":"uint512","name":"currentTime"}],"returns":{"type":"uint512"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"releasableAmount","args":[{"type":"address","name":"beneficiary"},{"type":"uint512","name":"currentTime"}],"returns":{"type":"uint512"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"release","args":[{"type":"address","name":"beneficiary"},{"type":"uint512","name":"currentTime"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}},{"name":"allocation","args":[{"type":"address","name":"beneficiary"}],"returns":{"type":"uint512"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"released","args":[{"type":"address","name":"beneficiary"}],"returns":{"type":"uint512"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"startTime","args":[{"type":"address","name":"beneficiary"}],"returns":{"type":"uint512"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"duration","args":[{"type":"address","name":"beneficiary"}],"returns":{"type":"uint512"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}}],"arcs":[22,28],"networks":{},"state":{"schema":{"global":{"ints":0,"bytes":0},"local":{"ints":0,"bytes":0}},"keys":{"global":{},"local":{},"box":{}},"maps":{"global":{},"local":{},"box":{"_totalAllocation":{"keyType":"AVMBytes","valueType":"AVMBytes","prefix":"X3RvdGFsQWxsb2NhdGlvbg=="},"_released":{"keyType":"AVMBytes","valueType":"AVMBytes","prefix":"X3JlbGVhc2Vk"},"_startTime":{"keyType":"AVMBytes","valueType":"AVMBytes","prefix":"X3N0YXJ0VGltZQ=="},"_duration":{"keyType":"AVMBytes","valueType":"AVMBytes","prefix":"X2R1cmF0aW9u"}}}},"bareActions":{"create":[],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[373],"errorMessage":"NothingToRelease"},{"pc":[178],"errorMessage":"ZeroDuration"},{"pc":[148,246,279,341,409,440,471,503],"errorMessage":"invalid number of bytes for arc4.static_array<arc4.uint8, 32>"},{"pc":[156,164,172,254,287,349],"errorMessage":"invalid number of bytes for arc4.uint512"},{"pc":[262,319,423,454,486,518],"errorMessage":"overflow"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"events":[]} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


/**
 * The argument types for the LinearVestingTest contract
 */
export type LinearVestingTestArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'createVesting(address,uint512,uint512,uint512)void': {
      beneficiary: string
      amount: bigint | number
      start: bigint | number
      dur: bigint | number
    }
    'vestedAmount(address,uint512)uint512': {
      beneficiary: string
      currentTime: bigint | number
    }
    'releasableAmount(address,uint512)uint512': {
      beneficiary: string
      currentTime: bigint | number
    }
    'release(address,uint512)void': {
      beneficiary: string
      currentTime: bigint | number
    }
    'allocation(address)uint512': {
      beneficiary: string
    }
    'released(address)uint512': {
      beneficiary: string
    }
    'startTime(address)uint512': {
      beneficiary: string
    }
    'duration(address)uint512': {
      beneficiary: string
    }
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'createVesting(address,uint512,uint512,uint512)void': [beneficiary: string, amount: bigint | number, start: bigint | number, dur: bigint | number]
    'vestedAmount(address,uint512)uint512': [beneficiary: string, currentTime: bigint | number]
    'releasableAmount(address,uint512)uint512': [beneficiary: string, currentTime: bigint | number]
    'release(address,uint512)void': [beneficiary: string, currentTime: bigint | number]
    'allocation(address)uint512': [beneficiary: string]
    'released(address)uint512': [beneficiary: string]
    'startTime(address)uint512': [beneficiary: string]
    'duration(address)uint512': [beneficiary: string]
  }
}

/**
 * The return type for each method
 */
export type LinearVestingTestReturns = {
  'createVesting(address,uint512,uint512,uint512)void': void
  'vestedAmount(address,uint512)uint512': bigint
  'releasableAmount(address,uint512)uint512': bigint
  'release(address,uint512)void': void
  'allocation(address)uint512': bigint
  'released(address)uint512': bigint
  'startTime(address)uint512': bigint
  'duration(address)uint512': bigint
}

/**
 * Defines the types of available calls and state of the LinearVestingTest smart contract.
 */
export type LinearVestingTestTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'createVesting(address,uint512,uint512,uint512)void' | 'createVesting', {
      argsObj: LinearVestingTestArgs['obj']['createVesting(address,uint512,uint512,uint512)void']
      argsTuple: LinearVestingTestArgs['tuple']['createVesting(address,uint512,uint512,uint512)void']
      returns: LinearVestingTestReturns['createVesting(address,uint512,uint512,uint512)void']
    }>
    & Record<'vestedAmount(address,uint512)uint512' | 'vestedAmount', {
      argsObj: LinearVestingTestArgs['obj']['vestedAmount(address,uint512)uint512']
      argsTuple: LinearVestingTestArgs['tuple']['vestedAmount(address,uint512)uint512']
      returns: LinearVestingTestReturns['vestedAmount(address,uint512)uint512']
    }>
    & Record<'releasableAmount(address,uint512)uint512' | 'releasableAmount', {
      argsObj: LinearVestingTestArgs['obj']['releasableAmount(address,uint512)uint512']
      argsTuple: LinearVestingTestArgs['tuple']['releasableAmount(address,uint512)uint512']
      returns: LinearVestingTestReturns['releasableAmount(address,uint512)uint512']
    }>
    & Record<'release(address,uint512)void' | 'release', {
      argsObj: LinearVestingTestArgs['obj']['release(address,uint512)void']
      argsTuple: LinearVestingTestArgs['tuple']['release(address,uint512)void']
      returns: LinearVestingTestReturns['release(address,uint512)void']
    }>
    & Record<'allocation(address)uint512' | 'allocation', {
      argsObj: LinearVestingTestArgs['obj']['allocation(address)uint512']
      argsTuple: LinearVestingTestArgs['tuple']['allocation(address)uint512']
      returns: LinearVestingTestReturns['allocation(address)uint512']
    }>
    & Record<'released(address)uint512' | 'released', {
      argsObj: LinearVestingTestArgs['obj']['released(address)uint512']
      argsTuple: LinearVestingTestArgs['tuple']['released(address)uint512']
      returns: LinearVestingTestReturns['released(address)uint512']
    }>
    & Record<'startTime(address)uint512' | 'startTime', {
      argsObj: LinearVestingTestArgs['obj']['startTime(address)uint512']
      argsTuple: LinearVestingTestArgs['tuple']['startTime(address)uint512']
      returns: LinearVestingTestReturns['startTime(address)uint512']
    }>
    & Record<'duration(address)uint512' | 'duration', {
      argsObj: LinearVestingTestArgs['obj']['duration(address)uint512']
      argsTuple: LinearVestingTestArgs['tuple']['duration(address)uint512']
      returns: LinearVestingTestReturns['duration(address)uint512']
    }>
  /**
   * Defines the shape of the state of the application.
   */
  state: {
    box: {
      keys: {}
      maps: {
        _totalAllocation: Map<Uint8Array | string, Uint8Array>
        _released: Map<Uint8Array | string, Uint8Array>
        _startTime: Map<Uint8Array | string, Uint8Array>
        _duration: Map<Uint8Array | string, Uint8Array>
      }
    }
  }
}

/**
 * Defines the possible abi call signatures.
 */
export type LinearVestingTestSignatures = keyof LinearVestingTestTypes['methods']
/**
 * Defines the possible abi call signatures for methods that return a non-void value.
 */
export type LinearVestingTestNonVoidMethodSignatures = keyof LinearVestingTestTypes['methods'] extends infer T ? T extends keyof LinearVestingTestTypes['methods'] ? MethodReturn<T> extends void ? never : T  : never : never
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the LinearVestingTest smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends LinearVestingTestSignatures> = LinearVestingTestTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the LinearVestingTest smart contract to the method's return type
 */
export type MethodReturn<TSignature extends LinearVestingTestSignatures> = LinearVestingTestTypes['methods'][TSignature]['returns']

/**
 * Defines the shape of the keyed box state of the application.
 */
export type BoxKeysState = LinearVestingTestTypes['state']['box']['keys']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the LinearVestingTest smart contract
 */
export abstract class LinearVestingTestParamsFactory {
  /**
   * Constructs a no op call for the createVesting(address,uint512,uint512,uint512)void ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static createVesting(params: CallParams<LinearVestingTestArgs['obj']['createVesting(address,uint512,uint512,uint512)void'] | LinearVestingTestArgs['tuple']['createVesting(address,uint512,uint512,uint512)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'createVesting(address,uint512,uint512,uint512)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.beneficiary, params.args.amount, params.args.start, params.args.dur],
    }
  }
  /**
   * Constructs a no op call for the vestedAmount(address,uint512)uint512 ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static vestedAmount(params: CallParams<LinearVestingTestArgs['obj']['vestedAmount(address,uint512)uint512'] | LinearVestingTestArgs['tuple']['vestedAmount(address,uint512)uint512']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'vestedAmount(address,uint512)uint512' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.beneficiary, params.args.currentTime],
    }
  }
  /**
   * Constructs a no op call for the releasableAmount(address,uint512)uint512 ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static releasableAmount(params: CallParams<LinearVestingTestArgs['obj']['releasableAmount(address,uint512)uint512'] | LinearVestingTestArgs['tuple']['releasableAmount(address,uint512)uint512']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'releasableAmount(address,uint512)uint512' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.beneficiary, params.args.currentTime],
    }
  }
  /**
   * Constructs a no op call for the release(address,uint512)void ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static release(params: CallParams<LinearVestingTestArgs['obj']['release(address,uint512)void'] | LinearVestingTestArgs['tuple']['release(address,uint512)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'release(address,uint512)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.beneficiary, params.args.currentTime],
    }
  }
  /**
   * Constructs a no op call for the allocation(address)uint512 ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static allocation(params: CallParams<LinearVestingTestArgs['obj']['allocation(address)uint512'] | LinearVestingTestArgs['tuple']['allocation(address)uint512']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'allocation(address)uint512' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.beneficiary],
    }
  }
  /**
   * Constructs a no op call for the released(address)uint512 ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static released(params: CallParams<LinearVestingTestArgs['obj']['released(address)uint512'] | LinearVestingTestArgs['tuple']['released(address)uint512']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'released(address)uint512' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.beneficiary],
    }
  }
  /**
   * Constructs a no op call for the startTime(address)uint512 ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static startTime(params: CallParams<LinearVestingTestArgs['obj']['startTime(address)uint512'] | LinearVestingTestArgs['tuple']['startTime(address)uint512']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'startTime(address)uint512' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.beneficiary],
    }
  }
  /**
   * Constructs a no op call for the duration(address)uint512 ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static duration(params: CallParams<LinearVestingTestArgs['obj']['duration(address)uint512'] | LinearVestingTestArgs['tuple']['duration(address)uint512']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'duration(address)uint512' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.beneficiary],
    }
  }
}

/**
 * A client to make calls to the LinearVestingTest smart contract
 */
export class LinearVestingTestClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `LinearVestingTestClient`
   *
   * @param appClient An `AppClient` instance which has been created with the LinearVestingTest app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `LinearVestingTestClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Checks for decode errors on the given return value and maps the return value to the return type for the given method
   * @returns The typed return value or undefined if there was no value
   */
  decodeReturnValue<TSignature extends LinearVestingTestNonVoidMethodSignatures>(method: TSignature, returnValue: ABIReturn | undefined) {
    return returnValue !== undefined ? getArc56ReturnValue<MethodReturn<TSignature>>(returnValue, this.appClient.getABIMethod(method), APP_SPEC.structs) : undefined
  }

  /**
   * Returns a new `LinearVestingTestClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<LinearVestingTestClient> {
    return new LinearVestingTestClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `LinearVestingTestClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<LinearVestingTestClient> {
    return new LinearVestingTestClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Makes a clear_state call to an existing instance of the LinearVestingTest smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the LinearVestingTest smart contract using the `createVesting(address,uint512,uint512,uint512)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    createVesting: (params: CallParams<LinearVestingTestArgs['obj']['createVesting(address,uint512,uint512,uint512)void'] | LinearVestingTestArgs['tuple']['createVesting(address,uint512,uint512,uint512)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(LinearVestingTestParamsFactory.createVesting(params))
    },

    /**
     * Makes a call to the LinearVestingTest smart contract using the `vestedAmount(address,uint512)uint512` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    vestedAmount: (params: CallParams<LinearVestingTestArgs['obj']['vestedAmount(address,uint512)uint512'] | LinearVestingTestArgs['tuple']['vestedAmount(address,uint512)uint512']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(LinearVestingTestParamsFactory.vestedAmount(params))
    },

    /**
     * Makes a call to the LinearVestingTest smart contract using the `releasableAmount(address,uint512)uint512` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    releasableAmount: (params: CallParams<LinearVestingTestArgs['obj']['releasableAmount(address,uint512)uint512'] | LinearVestingTestArgs['tuple']['releasableAmount(address,uint512)uint512']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(LinearVestingTestParamsFactory.releasableAmount(params))
    },

    /**
     * Makes a call to the LinearVestingTest smart contract using the `release(address,uint512)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    release: (params: CallParams<LinearVestingTestArgs['obj']['release(address,uint512)void'] | LinearVestingTestArgs['tuple']['release(address,uint512)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(LinearVestingTestParamsFactory.release(params))
    },

    /**
     * Makes a call to the LinearVestingTest smart contract using the `allocation(address)uint512` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    allocation: (params: CallParams<LinearVestingTestArgs['obj']['allocation(address)uint512'] | LinearVestingTestArgs['tuple']['allocation(address)uint512']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(LinearVestingTestParamsFactory.allocation(params))
    },

    /**
     * Makes a call to the LinearVestingTest smart contract using the `released(address)uint512` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    released: (params: CallParams<LinearVestingTestArgs['obj']['released(address)uint512'] | LinearVestingTestArgs['tuple']['released(address)uint512']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(LinearVestingTestParamsFactory.released(params))
    },

    /**
     * Makes a call to the LinearVestingTest smart contract using the `startTime(address)uint512` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    startTime: (params: CallParams<LinearVestingTestArgs['obj']['startTime(address)uint512'] | LinearVestingTestArgs['tuple']['startTime(address)uint512']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(LinearVestingTestParamsFactory.startTime(params))
    },

    /**
     * Makes a call to the LinearVestingTest smart contract using the `duration(address)uint512` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    duration: (params: CallParams<LinearVestingTestArgs['obj']['duration(address)uint512'] | LinearVestingTestArgs['tuple']['duration(address)uint512']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(LinearVestingTestParamsFactory.duration(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Makes a clear_state call to an existing instance of the LinearVestingTest smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the LinearVestingTest smart contract using the `createVesting(address,uint512,uint512,uint512)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    createVesting: (params: CallParams<LinearVestingTestArgs['obj']['createVesting(address,uint512,uint512,uint512)void'] | LinearVestingTestArgs['tuple']['createVesting(address,uint512,uint512,uint512)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(LinearVestingTestParamsFactory.createVesting(params))
    },

    /**
     * Makes a call to the LinearVestingTest smart contract using the `vestedAmount(address,uint512)uint512` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    vestedAmount: (params: CallParams<LinearVestingTestArgs['obj']['vestedAmount(address,uint512)uint512'] | LinearVestingTestArgs['tuple']['vestedAmount(address,uint512)uint512']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(LinearVestingTestParamsFactory.vestedAmount(params))
    },

    /**
     * Makes a call to the LinearVestingTest smart contract using the `releasableAmount(address,uint512)uint512` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    releasableAmount: (params: CallParams<LinearVestingTestArgs['obj']['releasableAmount(address,uint512)uint512'] | LinearVestingTestArgs['tuple']['releasableAmount(address,uint512)uint512']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(LinearVestingTestParamsFactory.releasableAmount(params))
    },

    /**
     * Makes a call to the LinearVestingTest smart contract using the `release(address,uint512)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    release: (params: CallParams<LinearVestingTestArgs['obj']['release(address,uint512)void'] | LinearVestingTestArgs['tuple']['release(address,uint512)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(LinearVestingTestParamsFactory.release(params))
    },

    /**
     * Makes a call to the LinearVestingTest smart contract using the `allocation(address)uint512` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    allocation: (params: CallParams<LinearVestingTestArgs['obj']['allocation(address)uint512'] | LinearVestingTestArgs['tuple']['allocation(address)uint512']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(LinearVestingTestParamsFactory.allocation(params))
    },

    /**
     * Makes a call to the LinearVestingTest smart contract using the `released(address)uint512` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    released: (params: CallParams<LinearVestingTestArgs['obj']['released(address)uint512'] | LinearVestingTestArgs['tuple']['released(address)uint512']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(LinearVestingTestParamsFactory.released(params))
    },

    /**
     * Makes a call to the LinearVestingTest smart contract using the `startTime(address)uint512` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    startTime: (params: CallParams<LinearVestingTestArgs['obj']['startTime(address)uint512'] | LinearVestingTestArgs['tuple']['startTime(address)uint512']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(LinearVestingTestParamsFactory.startTime(params))
    },

    /**
     * Makes a call to the LinearVestingTest smart contract using the `duration(address)uint512` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    duration: (params: CallParams<LinearVestingTestArgs['obj']['duration(address)uint512'] | LinearVestingTestArgs['tuple']['duration(address)uint512']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(LinearVestingTestParamsFactory.duration(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Makes a clear_state call to an existing instance of the LinearVestingTest smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the LinearVestingTest smart contract using the `createVesting(address,uint512,uint512,uint512)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    createVesting: async (params: CallParams<LinearVestingTestArgs['obj']['createVesting(address,uint512,uint512,uint512)void'] | LinearVestingTestArgs['tuple']['createVesting(address,uint512,uint512,uint512)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(LinearVestingTestParamsFactory.createVesting(params))
      return {...result, return: result.return as unknown as (undefined | LinearVestingTestReturns['createVesting(address,uint512,uint512,uint512)void'])}
    },

    /**
     * Makes a call to the LinearVestingTest smart contract using the `vestedAmount(address,uint512)uint512` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    vestedAmount: async (params: CallParams<LinearVestingTestArgs['obj']['vestedAmount(address,uint512)uint512'] | LinearVestingTestArgs['tuple']['vestedAmount(address,uint512)uint512']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(LinearVestingTestParamsFactory.vestedAmount(params))
      return {...result, return: result.return as unknown as (undefined | LinearVestingTestReturns['vestedAmount(address,uint512)uint512'])}
    },

    /**
     * Makes a call to the LinearVestingTest smart contract using the `releasableAmount(address,uint512)uint512` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    releasableAmount: async (params: CallParams<LinearVestingTestArgs['obj']['releasableAmount(address,uint512)uint512'] | LinearVestingTestArgs['tuple']['releasableAmount(address,uint512)uint512']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(LinearVestingTestParamsFactory.releasableAmount(params))
      return {...result, return: result.return as unknown as (undefined | LinearVestingTestReturns['releasableAmount(address,uint512)uint512'])}
    },

    /**
     * Makes a call to the LinearVestingTest smart contract using the `release(address,uint512)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    release: async (params: CallParams<LinearVestingTestArgs['obj']['release(address,uint512)void'] | LinearVestingTestArgs['tuple']['release(address,uint512)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(LinearVestingTestParamsFactory.release(params))
      return {...result, return: result.return as unknown as (undefined | LinearVestingTestReturns['release(address,uint512)void'])}
    },

    /**
     * Makes a call to the LinearVestingTest smart contract using the `allocation(address)uint512` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    allocation: async (params: CallParams<LinearVestingTestArgs['obj']['allocation(address)uint512'] | LinearVestingTestArgs['tuple']['allocation(address)uint512']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(LinearVestingTestParamsFactory.allocation(params))
      return {...result, return: result.return as unknown as (undefined | LinearVestingTestReturns['allocation(address)uint512'])}
    },

    /**
     * Makes a call to the LinearVestingTest smart contract using the `released(address)uint512` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    released: async (params: CallParams<LinearVestingTestArgs['obj']['released(address)uint512'] | LinearVestingTestArgs['tuple']['released(address)uint512']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(LinearVestingTestParamsFactory.released(params))
      return {...result, return: result.return as unknown as (undefined | LinearVestingTestReturns['released(address)uint512'])}
    },

    /**
     * Makes a call to the LinearVestingTest smart contract using the `startTime(address)uint512` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    startTime: async (params: CallParams<LinearVestingTestArgs['obj']['startTime(address)uint512'] | LinearVestingTestArgs['tuple']['startTime(address)uint512']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(LinearVestingTestParamsFactory.startTime(params))
      return {...result, return: result.return as unknown as (undefined | LinearVestingTestReturns['startTime(address)uint512'])}
    },

    /**
     * Makes a call to the LinearVestingTest smart contract using the `duration(address)uint512` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    duration: async (params: CallParams<LinearVestingTestArgs['obj']['duration(address)uint512'] | LinearVestingTestArgs['tuple']['duration(address)uint512']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(LinearVestingTestParamsFactory.duration(params))
      return {...result, return: result.return as unknown as (undefined | LinearVestingTestReturns['duration(address)uint512'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new LinearVestingTestClient(this.appClient.clone(params))
  }

  /**
   * Makes a readonly (simulated) call to the LinearVestingTest smart contract using the `vestedAmount(address,uint512)uint512` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async vestedAmount(params: CallParams<LinearVestingTestArgs['obj']['vestedAmount(address,uint512)uint512'] | LinearVestingTestArgs['tuple']['vestedAmount(address,uint512)uint512']>) {
    const result = await this.appClient.send.call(LinearVestingTestParamsFactory.vestedAmount(params))
    return result.return as unknown as LinearVestingTestReturns['vestedAmount(address,uint512)uint512']
  }

  /**
   * Makes a readonly (simulated) call to the LinearVestingTest smart contract using the `releasableAmount(address,uint512)uint512` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async releasableAmount(params: CallParams<LinearVestingTestArgs['obj']['releasableAmount(address,uint512)uint512'] | LinearVestingTestArgs['tuple']['releasableAmount(address,uint512)uint512']>) {
    const result = await this.appClient.send.call(LinearVestingTestParamsFactory.releasableAmount(params))
    return result.return as unknown as LinearVestingTestReturns['releasableAmount(address,uint512)uint512']
  }

  /**
   * Makes a readonly (simulated) call to the LinearVestingTest smart contract using the `allocation(address)uint512` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async allocation(params: CallParams<LinearVestingTestArgs['obj']['allocation(address)uint512'] | LinearVestingTestArgs['tuple']['allocation(address)uint512']>) {
    const result = await this.appClient.send.call(LinearVestingTestParamsFactory.allocation(params))
    return result.return as unknown as LinearVestingTestReturns['allocation(address)uint512']
  }

  /**
   * Makes a readonly (simulated) call to the LinearVestingTest smart contract using the `released(address)uint512` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async released(params: CallParams<LinearVestingTestArgs['obj']['released(address)uint512'] | LinearVestingTestArgs['tuple']['released(address)uint512']>) {
    const result = await this.appClient.send.call(LinearVestingTestParamsFactory.released(params))
    return result.return as unknown as LinearVestingTestReturns['released(address)uint512']
  }

  /**
   * Makes a readonly (simulated) call to the LinearVestingTest smart contract using the `startTime(address)uint512` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async startTime(params: CallParams<LinearVestingTestArgs['obj']['startTime(address)uint512'] | LinearVestingTestArgs['tuple']['startTime(address)uint512']>) {
    const result = await this.appClient.send.call(LinearVestingTestParamsFactory.startTime(params))
    return result.return as unknown as LinearVestingTestReturns['startTime(address)uint512']
  }

  /**
   * Makes a readonly (simulated) call to the LinearVestingTest smart contract using the `duration(address)uint512` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async duration(params: CallParams<LinearVestingTestArgs['obj']['duration(address)uint512'] | LinearVestingTestArgs['tuple']['duration(address)uint512']>) {
    const result = await this.appClient.send.call(LinearVestingTestParamsFactory.duration(params))
    return result.return as unknown as LinearVestingTestReturns['duration(address)uint512']
  }

  /**
   * Methods to access state for the current LinearVestingTest app
   */
  state = {
    /**
     * Methods to access box state for the current LinearVestingTest app
     */
    box: {
      /**
       * Get all current keyed values from box state
       */
      getAll: async (): Promise<Partial<Expand<BoxKeysState>>> => {
        const result = await this.appClient.state.box.getAll()
        return {
        }
      },
      /**
       * Get values from the _totalAllocation map in box state
       */
      _totalAllocation: {
        /**
         * Get all current values of the _totalAllocation map in box state
         */
        getMap: async (): Promise<Map<Uint8Array, Uint8Array>> => { return (await this.appClient.state.box.getMap("_totalAllocation")) as Map<Uint8Array, Uint8Array> },
        /**
         * Get a current value of the _totalAllocation map by key from box state
         */
        value: async (key: Uint8Array | string): Promise<Uint8Array | undefined> => { return await this.appClient.state.box.getMapValue("_totalAllocation", key) as Uint8Array | undefined },
      },
      /**
       * Get values from the _released map in box state
       */
      _released: {
        /**
         * Get all current values of the _released map in box state
         */
        getMap: async (): Promise<Map<Uint8Array, Uint8Array>> => { return (await this.appClient.state.box.getMap("_released")) as Map<Uint8Array, Uint8Array> },
        /**
         * Get a current value of the _released map by key from box state
         */
        value: async (key: Uint8Array | string): Promise<Uint8Array | undefined> => { return await this.appClient.state.box.getMapValue("_released", key) as Uint8Array | undefined },
      },
      /**
       * Get values from the _startTime map in box state
       */
      _startTime: {
        /**
         * Get all current values of the _startTime map in box state
         */
        getMap: async (): Promise<Map<Uint8Array, Uint8Array>> => { return (await this.appClient.state.box.getMap("_startTime")) as Map<Uint8Array, Uint8Array> },
        /**
         * Get a current value of the _startTime map by key from box state
         */
        value: async (key: Uint8Array | string): Promise<Uint8Array | undefined> => { return await this.appClient.state.box.getMapValue("_startTime", key) as Uint8Array | undefined },
      },
      /**
       * Get values from the _duration map in box state
       */
      _duration: {
        /**
         * Get all current values of the _duration map in box state
         */
        getMap: async (): Promise<Map<Uint8Array, Uint8Array>> => { return (await this.appClient.state.box.getMap("_duration")) as Map<Uint8Array, Uint8Array> },
        /**
         * Get a current value of the _duration map by key from box state
         */
        value: async (key: Uint8Array | string): Promise<Uint8Array | undefined> => { return await this.appClient.state.box.getMapValue("_duration", key) as Uint8Array | undefined },
      },
    },
  }

  public newGroup(): LinearVestingTestComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a createVesting(address,uint512,uint512,uint512)void method call against the LinearVestingTest contract
       */
      createVesting(params: CallParams<LinearVestingTestArgs['obj']['createVesting(address,uint512,uint512,uint512)void'] | LinearVestingTestArgs['tuple']['createVesting(address,uint512,uint512,uint512)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.createVesting(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a vestedAmount(address,uint512)uint512 method call against the LinearVestingTest contract
       */
      vestedAmount(params: CallParams<LinearVestingTestArgs['obj']['vestedAmount(address,uint512)uint512'] | LinearVestingTestArgs['tuple']['vestedAmount(address,uint512)uint512']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.vestedAmount(params)))
        resultMappers.push((v) => client.decodeReturnValue('vestedAmount(address,uint512)uint512', v))
        return this
      },
      /**
       * Add a releasableAmount(address,uint512)uint512 method call against the LinearVestingTest contract
       */
      releasableAmount(params: CallParams<LinearVestingTestArgs['obj']['releasableAmount(address,uint512)uint512'] | LinearVestingTestArgs['tuple']['releasableAmount(address,uint512)uint512']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.releasableAmount(params)))
        resultMappers.push((v) => client.decodeReturnValue('releasableAmount(address,uint512)uint512', v))
        return this
      },
      /**
       * Add a release(address,uint512)void method call against the LinearVestingTest contract
       */
      release(params: CallParams<LinearVestingTestArgs['obj']['release(address,uint512)void'] | LinearVestingTestArgs['tuple']['release(address,uint512)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.release(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a allocation(address)uint512 method call against the LinearVestingTest contract
       */
      allocation(params: CallParams<LinearVestingTestArgs['obj']['allocation(address)uint512'] | LinearVestingTestArgs['tuple']['allocation(address)uint512']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.allocation(params)))
        resultMappers.push((v) => client.decodeReturnValue('allocation(address)uint512', v))
        return this
      },
      /**
       * Add a released(address)uint512 method call against the LinearVestingTest contract
       */
      released(params: CallParams<LinearVestingTestArgs['obj']['released(address)uint512'] | LinearVestingTestArgs['tuple']['released(address)uint512']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.released(params)))
        resultMappers.push((v) => client.decodeReturnValue('released(address)uint512', v))
        return this
      },
      /**
       * Add a startTime(address)uint512 method call against the LinearVestingTest contract
       */
      startTime(params: CallParams<LinearVestingTestArgs['obj']['startTime(address)uint512'] | LinearVestingTestArgs['tuple']['startTime(address)uint512']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.startTime(params)))
        resultMappers.push((v) => client.decodeReturnValue('startTime(address)uint512', v))
        return this
      },
      /**
       * Add a duration(address)uint512 method call against the LinearVestingTest contract
       */
      duration(params: CallParams<LinearVestingTestArgs['obj']['duration(address)uint512'] | LinearVestingTestArgs['tuple']['duration(address)uint512']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.duration(params)))
        resultMappers.push((v) => client.decodeReturnValue('duration(address)uint512', v))
        return this
      },
      /**
       * Add a clear state call to the LinearVestingTest contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as LinearVestingTestComposer
  }
}
export type LinearVestingTestComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the createVesting(address,uint512,uint512,uint512)void ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  createVesting(params?: CallParams<LinearVestingTestArgs['obj']['createVesting(address,uint512,uint512,uint512)void'] | LinearVestingTestArgs['tuple']['createVesting(address,uint512,uint512,uint512)void']>): LinearVestingTestComposer<[...TReturns, LinearVestingTestReturns['createVesting(address,uint512,uint512,uint512)void'] | undefined]>

  /**
   * Calls the vestedAmount(address,uint512)uint512 ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  vestedAmount(params?: CallParams<LinearVestingTestArgs['obj']['vestedAmount(address,uint512)uint512'] | LinearVestingTestArgs['tuple']['vestedAmount(address,uint512)uint512']>): LinearVestingTestComposer<[...TReturns, LinearVestingTestReturns['vestedAmount(address,uint512)uint512'] | undefined]>

  /**
   * Calls the releasableAmount(address,uint512)uint512 ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  releasableAmount(params?: CallParams<LinearVestingTestArgs['obj']['releasableAmount(address,uint512)uint512'] | LinearVestingTestArgs['tuple']['releasableAmount(address,uint512)uint512']>): LinearVestingTestComposer<[...TReturns, LinearVestingTestReturns['releasableAmount(address,uint512)uint512'] | undefined]>

  /**
   * Calls the release(address,uint512)void ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  release(params?: CallParams<LinearVestingTestArgs['obj']['release(address,uint512)void'] | LinearVestingTestArgs['tuple']['release(address,uint512)void']>): LinearVestingTestComposer<[...TReturns, LinearVestingTestReturns['release(address,uint512)void'] | undefined]>

  /**
   * Calls the allocation(address)uint512 ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  allocation(params?: CallParams<LinearVestingTestArgs['obj']['allocation(address)uint512'] | LinearVestingTestArgs['tuple']['allocation(address)uint512']>): LinearVestingTestComposer<[...TReturns, LinearVestingTestReturns['allocation(address)uint512'] | undefined]>

  /**
   * Calls the released(address)uint512 ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  released(params?: CallParams<LinearVestingTestArgs['obj']['released(address)uint512'] | LinearVestingTestArgs['tuple']['released(address)uint512']>): LinearVestingTestComposer<[...TReturns, LinearVestingTestReturns['released(address)uint512'] | undefined]>

  /**
   * Calls the startTime(address)uint512 ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  startTime(params?: CallParams<LinearVestingTestArgs['obj']['startTime(address)uint512'] | LinearVestingTestArgs['tuple']['startTime(address)uint512']>): LinearVestingTestComposer<[...TReturns, LinearVestingTestReturns['startTime(address)uint512'] | undefined]>

  /**
   * Calls the duration(address)uint512 ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  duration(params?: CallParams<LinearVestingTestArgs['obj']['duration(address)uint512'] | LinearVestingTestArgs['tuple']['duration(address)uint512']>): LinearVestingTestComposer<[...TReturns, LinearVestingTestReturns['duration(address)uint512'] | undefined]>

  /**
   * Makes a clear_state call to an existing instance of the LinearVestingTest smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): LinearVestingTestComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): LinearVestingTestComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<LinearVestingTestComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<LinearVestingTestComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<LinearVestingTestComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<LinearVestingTestComposerResults<TReturns>>
}
export type LinearVestingTestComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

