/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"RewardPoolTest","structs":{},"methods":[{"name":"initRecipient","args":[{"type":"uint512","name":"recipientId"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}},{"name":"getAdmin","args":[],"returns":{"type":"address"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"getRecipientCount","args":[],"returns":{"type":"uint512"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"getTotalDistributed","args":[],"returns":{"type":"uint512"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"getTotalClaimed","args":[],"returns":{"type":"uint512"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"getRecipientHash","args":[{"type":"uint512","name":"recipientId"}],"returns":{"type":"uint512"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"getPendingReward","args":[{"type":"uint512","name":"recipientId"}],"returns":{"type":"uint512"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"getClaimedReward","args":[{"type":"uint512","name":"recipientId"}],"returns":{"type":"uint512"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"addRecipient","args":[{"type":"uint512","name":"recipientHash"}],"returns":{"type":"uint512"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}},{"name":"distributeReward","args":[{"type":"uint512","name":"recipientId"},{"type":"uint512","name":"amount"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}},{"name":"claimReward","args":[{"type":"uint512","name":"recipientId"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}}],"arcs":[22,28],"networks":{},"state":{"schema":{"global":{"ints":0,"bytes":4},"local":{"ints":0,"bytes":0}},"keys":{"global":{"_admin":{"keyType":"AVMString","valueType":"address","key":"X2FkbWlu"},"_recipientCount":{"keyType":"AVMString","valueType":"AVMBytes","key":"X3JlY2lwaWVudENvdW50"},"_totalDistributed":{"keyType":"AVMString","valueType":"AVMBytes","key":"X3RvdGFsRGlzdHJpYnV0ZWQ="},"_totalClaimed":{"keyType":"AVMString","valueType":"AVMBytes","key":"X3RvdGFsQ2xhaW1lZA=="}},"local":{},"box":{}},"maps":{"global":{},"local":{},"box":{"_recipientHash":{"keyType":"AVMBytes","valueType":"AVMBytes","prefix":"X3JlY2lwaWVudEhhc2g="},"_pendingReward":{"keyType":"AVMBytes","valueType":"AVMBytes","prefix":"X3BlbmRpbmdSZXdhcmQ="},"_claimedReward":{"keyType":"AVMBytes","valueType":"AVMBytes","prefix":"X2NsYWltZWRSZXdhcmQ="}}}},"bareActions":{"create":[],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[705],"errorMessage":"No pending reward"},{"pc":[630],"errorMessage":"Not admin"},{"pc":[338,628],"errorMessage":"check _admin exists"},{"pc":[348,537],"errorMessage":"check _recipientCount exists"},{"pc":[385,737],"errorMessage":"check _totalClaimed exists"},{"pc":[366,664],"errorMessage":"check _totalDistributed exists"},{"pc":[286,407,449,491,533,613,621,678],"errorMessage":"invalid number of bytes for arc4.uint512"},{"pc":[353,371,390,434,476,518,598],"errorMessage":"overflow"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"events":[]} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


/**
 * The argument types for the RewardPoolTest contract
 */
export type RewardPoolTestArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'initRecipient(uint512)void': {
      recipientId: bigint | number
    }
    'getAdmin()address': Record<string, never>
    'getRecipientCount()uint512': Record<string, never>
    'getTotalDistributed()uint512': Record<string, never>
    'getTotalClaimed()uint512': Record<string, never>
    'getRecipientHash(uint512)uint512': {
      recipientId: bigint | number
    }
    'getPendingReward(uint512)uint512': {
      recipientId: bigint | number
    }
    'getClaimedReward(uint512)uint512': {
      recipientId: bigint | number
    }
    'addRecipient(uint512)uint512': {
      recipientHash: bigint | number
    }
    'distributeReward(uint512,uint512)void': {
      recipientId: bigint | number
      amount: bigint | number
    }
    'claimReward(uint512)void': {
      recipientId: bigint | number
    }
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'initRecipient(uint512)void': [recipientId: bigint | number]
    'getAdmin()address': []
    'getRecipientCount()uint512': []
    'getTotalDistributed()uint512': []
    'getTotalClaimed()uint512': []
    'getRecipientHash(uint512)uint512': [recipientId: bigint | number]
    'getPendingReward(uint512)uint512': [recipientId: bigint | number]
    'getClaimedReward(uint512)uint512': [recipientId: bigint | number]
    'addRecipient(uint512)uint512': [recipientHash: bigint | number]
    'distributeReward(uint512,uint512)void': [recipientId: bigint | number, amount: bigint | number]
    'claimReward(uint512)void': [recipientId: bigint | number]
  }
}

/**
 * The return type for each method
 */
export type RewardPoolTestReturns = {
  'initRecipient(uint512)void': void
  'getAdmin()address': string
  'getRecipientCount()uint512': bigint
  'getTotalDistributed()uint512': bigint
  'getTotalClaimed()uint512': bigint
  'getRecipientHash(uint512)uint512': bigint
  'getPendingReward(uint512)uint512': bigint
  'getClaimedReward(uint512)uint512': bigint
  'addRecipient(uint512)uint512': bigint
  'distributeReward(uint512,uint512)void': void
  'claimReward(uint512)void': void
}

/**
 * Defines the types of available calls and state of the RewardPoolTest smart contract.
 */
export type RewardPoolTestTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'initRecipient(uint512)void' | 'initRecipient', {
      argsObj: RewardPoolTestArgs['obj']['initRecipient(uint512)void']
      argsTuple: RewardPoolTestArgs['tuple']['initRecipient(uint512)void']
      returns: RewardPoolTestReturns['initRecipient(uint512)void']
    }>
    & Record<'getAdmin()address' | 'getAdmin', {
      argsObj: RewardPoolTestArgs['obj']['getAdmin()address']
      argsTuple: RewardPoolTestArgs['tuple']['getAdmin()address']
      returns: RewardPoolTestReturns['getAdmin()address']
    }>
    & Record<'getRecipientCount()uint512' | 'getRecipientCount', {
      argsObj: RewardPoolTestArgs['obj']['getRecipientCount()uint512']
      argsTuple: RewardPoolTestArgs['tuple']['getRecipientCount()uint512']
      returns: RewardPoolTestReturns['getRecipientCount()uint512']
    }>
    & Record<'getTotalDistributed()uint512' | 'getTotalDistributed', {
      argsObj: RewardPoolTestArgs['obj']['getTotalDistributed()uint512']
      argsTuple: RewardPoolTestArgs['tuple']['getTotalDistributed()uint512']
      returns: RewardPoolTestReturns['getTotalDistributed()uint512']
    }>
    & Record<'getTotalClaimed()uint512' | 'getTotalClaimed', {
      argsObj: RewardPoolTestArgs['obj']['getTotalClaimed()uint512']
      argsTuple: RewardPoolTestArgs['tuple']['getTotalClaimed()uint512']
      returns: RewardPoolTestReturns['getTotalClaimed()uint512']
    }>
    & Record<'getRecipientHash(uint512)uint512' | 'getRecipientHash', {
      argsObj: RewardPoolTestArgs['obj']['getRecipientHash(uint512)uint512']
      argsTuple: RewardPoolTestArgs['tuple']['getRecipientHash(uint512)uint512']
      returns: RewardPoolTestReturns['getRecipientHash(uint512)uint512']
    }>
    & Record<'getPendingReward(uint512)uint512' | 'getPendingReward', {
      argsObj: RewardPoolTestArgs['obj']['getPendingReward(uint512)uint512']
      argsTuple: RewardPoolTestArgs['tuple']['getPendingReward(uint512)uint512']
      returns: RewardPoolTestReturns['getPendingReward(uint512)uint512']
    }>
    & Record<'getClaimedReward(uint512)uint512' | 'getClaimedReward', {
      argsObj: RewardPoolTestArgs['obj']['getClaimedReward(uint512)uint512']
      argsTuple: RewardPoolTestArgs['tuple']['getClaimedReward(uint512)uint512']
      returns: RewardPoolTestReturns['getClaimedReward(uint512)uint512']
    }>
    & Record<'addRecipient(uint512)uint512' | 'addRecipient', {
      argsObj: RewardPoolTestArgs['obj']['addRecipient(uint512)uint512']
      argsTuple: RewardPoolTestArgs['tuple']['addRecipient(uint512)uint512']
      returns: RewardPoolTestReturns['addRecipient(uint512)uint512']
    }>
    & Record<'distributeReward(uint512,uint512)void' | 'distributeReward', {
      argsObj: RewardPoolTestArgs['obj']['distributeReward(uint512,uint512)void']
      argsTuple: RewardPoolTestArgs['tuple']['distributeReward(uint512,uint512)void']
      returns: RewardPoolTestReturns['distributeReward(uint512,uint512)void']
    }>
    & Record<'claimReward(uint512)void' | 'claimReward', {
      argsObj: RewardPoolTestArgs['obj']['claimReward(uint512)void']
      argsTuple: RewardPoolTestArgs['tuple']['claimReward(uint512)void']
      returns: RewardPoolTestReturns['claimReward(uint512)void']
    }>
  /**
   * Defines the shape of the state of the application.
   */
  state: {
    global: {
      keys: {
        _admin: string
        _recipientCount: BinaryState
        _totalDistributed: BinaryState
        _totalClaimed: BinaryState
      }
      maps: {}
    }
    box: {
      keys: {}
      maps: {
        _recipientHash: Map<Uint8Array | string, Uint8Array>
        _pendingReward: Map<Uint8Array | string, Uint8Array>
        _claimedReward: Map<Uint8Array | string, Uint8Array>
      }
    }
  }
}

/**
 * Defines the possible abi call signatures.
 */
export type RewardPoolTestSignatures = keyof RewardPoolTestTypes['methods']
/**
 * Defines the possible abi call signatures for methods that return a non-void value.
 */
export type RewardPoolTestNonVoidMethodSignatures = keyof RewardPoolTestTypes['methods'] extends infer T ? T extends keyof RewardPoolTestTypes['methods'] ? MethodReturn<T> extends void ? never : T  : never : never
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the RewardPoolTest smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends RewardPoolTestSignatures> = RewardPoolTestTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the RewardPoolTest smart contract to the method's return type
 */
export type MethodReturn<TSignature extends RewardPoolTestSignatures> = RewardPoolTestTypes['methods'][TSignature]['returns']

/**
 * Defines the shape of the keyed global state of the application.
 */
export type GlobalKeysState = RewardPoolTestTypes['state']['global']['keys']

/**
 * Defines the shape of the keyed box state of the application.
 */
export type BoxKeysState = RewardPoolTestTypes['state']['box']['keys']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the RewardPoolTest smart contract
 */
export abstract class RewardPoolTestParamsFactory {
  /**
   * Constructs a no op call for the initRecipient(uint512)void ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static initRecipient(params: CallParams<RewardPoolTestArgs['obj']['initRecipient(uint512)void'] | RewardPoolTestArgs['tuple']['initRecipient(uint512)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'initRecipient(uint512)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.recipientId],
    }
  }
  /**
   * Constructs a no op call for the getAdmin()address ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static getAdmin(params: CallParams<RewardPoolTestArgs['obj']['getAdmin()address'] | RewardPoolTestArgs['tuple']['getAdmin()address']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'getAdmin()address' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the getRecipientCount()uint512 ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static getRecipientCount(params: CallParams<RewardPoolTestArgs['obj']['getRecipientCount()uint512'] | RewardPoolTestArgs['tuple']['getRecipientCount()uint512']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'getRecipientCount()uint512' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the getTotalDistributed()uint512 ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static getTotalDistributed(params: CallParams<RewardPoolTestArgs['obj']['getTotalDistributed()uint512'] | RewardPoolTestArgs['tuple']['getTotalDistributed()uint512']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'getTotalDistributed()uint512' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the getTotalClaimed()uint512 ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static getTotalClaimed(params: CallParams<RewardPoolTestArgs['obj']['getTotalClaimed()uint512'] | RewardPoolTestArgs['tuple']['getTotalClaimed()uint512']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'getTotalClaimed()uint512' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the getRecipientHash(uint512)uint512 ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static getRecipientHash(params: CallParams<RewardPoolTestArgs['obj']['getRecipientHash(uint512)uint512'] | RewardPoolTestArgs['tuple']['getRecipientHash(uint512)uint512']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'getRecipientHash(uint512)uint512' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.recipientId],
    }
  }
  /**
   * Constructs a no op call for the getPendingReward(uint512)uint512 ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static getPendingReward(params: CallParams<RewardPoolTestArgs['obj']['getPendingReward(uint512)uint512'] | RewardPoolTestArgs['tuple']['getPendingReward(uint512)uint512']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'getPendingReward(uint512)uint512' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.recipientId],
    }
  }
  /**
   * Constructs a no op call for the getClaimedReward(uint512)uint512 ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static getClaimedReward(params: CallParams<RewardPoolTestArgs['obj']['getClaimedReward(uint512)uint512'] | RewardPoolTestArgs['tuple']['getClaimedReward(uint512)uint512']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'getClaimedReward(uint512)uint512' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.recipientId],
    }
  }
  /**
   * Constructs a no op call for the addRecipient(uint512)uint512 ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static addRecipient(params: CallParams<RewardPoolTestArgs['obj']['addRecipient(uint512)uint512'] | RewardPoolTestArgs['tuple']['addRecipient(uint512)uint512']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'addRecipient(uint512)uint512' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.recipientHash],
    }
  }
  /**
   * Constructs a no op call for the distributeReward(uint512,uint512)void ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static distributeReward(params: CallParams<RewardPoolTestArgs['obj']['distributeReward(uint512,uint512)void'] | RewardPoolTestArgs['tuple']['distributeReward(uint512,uint512)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'distributeReward(uint512,uint512)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.recipientId, params.args.amount],
    }
  }
  /**
   * Constructs a no op call for the claimReward(uint512)void ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static claimReward(params: CallParams<RewardPoolTestArgs['obj']['claimReward(uint512)void'] | RewardPoolTestArgs['tuple']['claimReward(uint512)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'claimReward(uint512)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.recipientId],
    }
  }
}

/**
 * A client to make calls to the RewardPoolTest smart contract
 */
export class RewardPoolTestClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `RewardPoolTestClient`
   *
   * @param appClient An `AppClient` instance which has been created with the RewardPoolTest app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `RewardPoolTestClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Checks for decode errors on the given return value and maps the return value to the return type for the given method
   * @returns The typed return value or undefined if there was no value
   */
  decodeReturnValue<TSignature extends RewardPoolTestNonVoidMethodSignatures>(method: TSignature, returnValue: ABIReturn | undefined) {
    return returnValue !== undefined ? getArc56ReturnValue<MethodReturn<TSignature>>(returnValue, this.appClient.getABIMethod(method), APP_SPEC.structs) : undefined
  }

  /**
   * Returns a new `RewardPoolTestClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<RewardPoolTestClient> {
    return new RewardPoolTestClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `RewardPoolTestClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<RewardPoolTestClient> {
    return new RewardPoolTestClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Makes a clear_state call to an existing instance of the RewardPoolTest smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the RewardPoolTest smart contract using the `initRecipient(uint512)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    initRecipient: (params: CallParams<RewardPoolTestArgs['obj']['initRecipient(uint512)void'] | RewardPoolTestArgs['tuple']['initRecipient(uint512)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(RewardPoolTestParamsFactory.initRecipient(params))
    },

    /**
     * Makes a call to the RewardPoolTest smart contract using the `getAdmin()address` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    getAdmin: (params: CallParams<RewardPoolTestArgs['obj']['getAdmin()address'] | RewardPoolTestArgs['tuple']['getAdmin()address']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(RewardPoolTestParamsFactory.getAdmin(params))
    },

    /**
     * Makes a call to the RewardPoolTest smart contract using the `getRecipientCount()uint512` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    getRecipientCount: (params: CallParams<RewardPoolTestArgs['obj']['getRecipientCount()uint512'] | RewardPoolTestArgs['tuple']['getRecipientCount()uint512']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(RewardPoolTestParamsFactory.getRecipientCount(params))
    },

    /**
     * Makes a call to the RewardPoolTest smart contract using the `getTotalDistributed()uint512` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    getTotalDistributed: (params: CallParams<RewardPoolTestArgs['obj']['getTotalDistributed()uint512'] | RewardPoolTestArgs['tuple']['getTotalDistributed()uint512']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(RewardPoolTestParamsFactory.getTotalDistributed(params))
    },

    /**
     * Makes a call to the RewardPoolTest smart contract using the `getTotalClaimed()uint512` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    getTotalClaimed: (params: CallParams<RewardPoolTestArgs['obj']['getTotalClaimed()uint512'] | RewardPoolTestArgs['tuple']['getTotalClaimed()uint512']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(RewardPoolTestParamsFactory.getTotalClaimed(params))
    },

    /**
     * Makes a call to the RewardPoolTest smart contract using the `getRecipientHash(uint512)uint512` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    getRecipientHash: (params: CallParams<RewardPoolTestArgs['obj']['getRecipientHash(uint512)uint512'] | RewardPoolTestArgs['tuple']['getRecipientHash(uint512)uint512']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(RewardPoolTestParamsFactory.getRecipientHash(params))
    },

    /**
     * Makes a call to the RewardPoolTest smart contract using the `getPendingReward(uint512)uint512` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    getPendingReward: (params: CallParams<RewardPoolTestArgs['obj']['getPendingReward(uint512)uint512'] | RewardPoolTestArgs['tuple']['getPendingReward(uint512)uint512']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(RewardPoolTestParamsFactory.getPendingReward(params))
    },

    /**
     * Makes a call to the RewardPoolTest smart contract using the `getClaimedReward(uint512)uint512` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    getClaimedReward: (params: CallParams<RewardPoolTestArgs['obj']['getClaimedReward(uint512)uint512'] | RewardPoolTestArgs['tuple']['getClaimedReward(uint512)uint512']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(RewardPoolTestParamsFactory.getClaimedReward(params))
    },

    /**
     * Makes a call to the RewardPoolTest smart contract using the `addRecipient(uint512)uint512` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    addRecipient: (params: CallParams<RewardPoolTestArgs['obj']['addRecipient(uint512)uint512'] | RewardPoolTestArgs['tuple']['addRecipient(uint512)uint512']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(RewardPoolTestParamsFactory.addRecipient(params))
    },

    /**
     * Makes a call to the RewardPoolTest smart contract using the `distributeReward(uint512,uint512)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    distributeReward: (params: CallParams<RewardPoolTestArgs['obj']['distributeReward(uint512,uint512)void'] | RewardPoolTestArgs['tuple']['distributeReward(uint512,uint512)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(RewardPoolTestParamsFactory.distributeReward(params))
    },

    /**
     * Makes a call to the RewardPoolTest smart contract using the `claimReward(uint512)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    claimReward: (params: CallParams<RewardPoolTestArgs['obj']['claimReward(uint512)void'] | RewardPoolTestArgs['tuple']['claimReward(uint512)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(RewardPoolTestParamsFactory.claimReward(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Makes a clear_state call to an existing instance of the RewardPoolTest smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the RewardPoolTest smart contract using the `initRecipient(uint512)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    initRecipient: (params: CallParams<RewardPoolTestArgs['obj']['initRecipient(uint512)void'] | RewardPoolTestArgs['tuple']['initRecipient(uint512)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(RewardPoolTestParamsFactory.initRecipient(params))
    },

    /**
     * Makes a call to the RewardPoolTest smart contract using the `getAdmin()address` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    getAdmin: (params: CallParams<RewardPoolTestArgs['obj']['getAdmin()address'] | RewardPoolTestArgs['tuple']['getAdmin()address']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(RewardPoolTestParamsFactory.getAdmin(params))
    },

    /**
     * Makes a call to the RewardPoolTest smart contract using the `getRecipientCount()uint512` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    getRecipientCount: (params: CallParams<RewardPoolTestArgs['obj']['getRecipientCount()uint512'] | RewardPoolTestArgs['tuple']['getRecipientCount()uint512']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(RewardPoolTestParamsFactory.getRecipientCount(params))
    },

    /**
     * Makes a call to the RewardPoolTest smart contract using the `getTotalDistributed()uint512` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    getTotalDistributed: (params: CallParams<RewardPoolTestArgs['obj']['getTotalDistributed()uint512'] | RewardPoolTestArgs['tuple']['getTotalDistributed()uint512']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(RewardPoolTestParamsFactory.getTotalDistributed(params))
    },

    /**
     * Makes a call to the RewardPoolTest smart contract using the `getTotalClaimed()uint512` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    getTotalClaimed: (params: CallParams<RewardPoolTestArgs['obj']['getTotalClaimed()uint512'] | RewardPoolTestArgs['tuple']['getTotalClaimed()uint512']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(RewardPoolTestParamsFactory.getTotalClaimed(params))
    },

    /**
     * Makes a call to the RewardPoolTest smart contract using the `getRecipientHash(uint512)uint512` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    getRecipientHash: (params: CallParams<RewardPoolTestArgs['obj']['getRecipientHash(uint512)uint512'] | RewardPoolTestArgs['tuple']['getRecipientHash(uint512)uint512']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(RewardPoolTestParamsFactory.getRecipientHash(params))
    },

    /**
     * Makes a call to the RewardPoolTest smart contract using the `getPendingReward(uint512)uint512` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    getPendingReward: (params: CallParams<RewardPoolTestArgs['obj']['getPendingReward(uint512)uint512'] | RewardPoolTestArgs['tuple']['getPendingReward(uint512)uint512']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(RewardPoolTestParamsFactory.getPendingReward(params))
    },

    /**
     * Makes a call to the RewardPoolTest smart contract using the `getClaimedReward(uint512)uint512` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    getClaimedReward: (params: CallParams<RewardPoolTestArgs['obj']['getClaimedReward(uint512)uint512'] | RewardPoolTestArgs['tuple']['getClaimedReward(uint512)uint512']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(RewardPoolTestParamsFactory.getClaimedReward(params))
    },

    /**
     * Makes a call to the RewardPoolTest smart contract using the `addRecipient(uint512)uint512` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    addRecipient: (params: CallParams<RewardPoolTestArgs['obj']['addRecipient(uint512)uint512'] | RewardPoolTestArgs['tuple']['addRecipient(uint512)uint512']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(RewardPoolTestParamsFactory.addRecipient(params))
    },

    /**
     * Makes a call to the RewardPoolTest smart contract using the `distributeReward(uint512,uint512)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    distributeReward: (params: CallParams<RewardPoolTestArgs['obj']['distributeReward(uint512,uint512)void'] | RewardPoolTestArgs['tuple']['distributeReward(uint512,uint512)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(RewardPoolTestParamsFactory.distributeReward(params))
    },

    /**
     * Makes a call to the RewardPoolTest smart contract using the `claimReward(uint512)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    claimReward: (params: CallParams<RewardPoolTestArgs['obj']['claimReward(uint512)void'] | RewardPoolTestArgs['tuple']['claimReward(uint512)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(RewardPoolTestParamsFactory.claimReward(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Makes a clear_state call to an existing instance of the RewardPoolTest smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the RewardPoolTest smart contract using the `initRecipient(uint512)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    initRecipient: async (params: CallParams<RewardPoolTestArgs['obj']['initRecipient(uint512)void'] | RewardPoolTestArgs['tuple']['initRecipient(uint512)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(RewardPoolTestParamsFactory.initRecipient(params))
      return {...result, return: result.return as unknown as (undefined | RewardPoolTestReturns['initRecipient(uint512)void'])}
    },

    /**
     * Makes a call to the RewardPoolTest smart contract using the `getAdmin()address` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    getAdmin: async (params: CallParams<RewardPoolTestArgs['obj']['getAdmin()address'] | RewardPoolTestArgs['tuple']['getAdmin()address']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(RewardPoolTestParamsFactory.getAdmin(params))
      return {...result, return: result.return as unknown as (undefined | RewardPoolTestReturns['getAdmin()address'])}
    },

    /**
     * Makes a call to the RewardPoolTest smart contract using the `getRecipientCount()uint512` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    getRecipientCount: async (params: CallParams<RewardPoolTestArgs['obj']['getRecipientCount()uint512'] | RewardPoolTestArgs['tuple']['getRecipientCount()uint512']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(RewardPoolTestParamsFactory.getRecipientCount(params))
      return {...result, return: result.return as unknown as (undefined | RewardPoolTestReturns['getRecipientCount()uint512'])}
    },

    /**
     * Makes a call to the RewardPoolTest smart contract using the `getTotalDistributed()uint512` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    getTotalDistributed: async (params: CallParams<RewardPoolTestArgs['obj']['getTotalDistributed()uint512'] | RewardPoolTestArgs['tuple']['getTotalDistributed()uint512']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(RewardPoolTestParamsFactory.getTotalDistributed(params))
      return {...result, return: result.return as unknown as (undefined | RewardPoolTestReturns['getTotalDistributed()uint512'])}
    },

    /**
     * Makes a call to the RewardPoolTest smart contract using the `getTotalClaimed()uint512` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    getTotalClaimed: async (params: CallParams<RewardPoolTestArgs['obj']['getTotalClaimed()uint512'] | RewardPoolTestArgs['tuple']['getTotalClaimed()uint512']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(RewardPoolTestParamsFactory.getTotalClaimed(params))
      return {...result, return: result.return as unknown as (undefined | RewardPoolTestReturns['getTotalClaimed()uint512'])}
    },

    /**
     * Makes a call to the RewardPoolTest smart contract using the `getRecipientHash(uint512)uint512` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    getRecipientHash: async (params: CallParams<RewardPoolTestArgs['obj']['getRecipientHash(uint512)uint512'] | RewardPoolTestArgs['tuple']['getRecipientHash(uint512)uint512']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(RewardPoolTestParamsFactory.getRecipientHash(params))
      return {...result, return: result.return as unknown as (undefined | RewardPoolTestReturns['getRecipientHash(uint512)uint512'])}
    },

    /**
     * Makes a call to the RewardPoolTest smart contract using the `getPendingReward(uint512)uint512` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    getPendingReward: async (params: CallParams<RewardPoolTestArgs['obj']['getPendingReward(uint512)uint512'] | RewardPoolTestArgs['tuple']['getPendingReward(uint512)uint512']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(RewardPoolTestParamsFactory.getPendingReward(params))
      return {...result, return: result.return as unknown as (undefined | RewardPoolTestReturns['getPendingReward(uint512)uint512'])}
    },

    /**
     * Makes a call to the RewardPoolTest smart contract using the `getClaimedReward(uint512)uint512` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    getClaimedReward: async (params: CallParams<RewardPoolTestArgs['obj']['getClaimedReward(uint512)uint512'] | RewardPoolTestArgs['tuple']['getClaimedReward(uint512)uint512']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(RewardPoolTestParamsFactory.getClaimedReward(params))
      return {...result, return: result.return as unknown as (undefined | RewardPoolTestReturns['getClaimedReward(uint512)uint512'])}
    },

    /**
     * Makes a call to the RewardPoolTest smart contract using the `addRecipient(uint512)uint512` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    addRecipient: async (params: CallParams<RewardPoolTestArgs['obj']['addRecipient(uint512)uint512'] | RewardPoolTestArgs['tuple']['addRecipient(uint512)uint512']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(RewardPoolTestParamsFactory.addRecipient(params))
      return {...result, return: result.return as unknown as (undefined | RewardPoolTestReturns['addRecipient(uint512)uint512'])}
    },

    /**
     * Makes a call to the RewardPoolTest smart contract using the `distributeReward(uint512,uint512)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    distributeReward: async (params: CallParams<RewardPoolTestArgs['obj']['distributeReward(uint512,uint512)void'] | RewardPoolTestArgs['tuple']['distributeReward(uint512,uint512)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(RewardPoolTestParamsFactory.distributeReward(params))
      return {...result, return: result.return as unknown as (undefined | RewardPoolTestReturns['distributeReward(uint512,uint512)void'])}
    },

    /**
     * Makes a call to the RewardPoolTest smart contract using the `claimReward(uint512)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    claimReward: async (params: CallParams<RewardPoolTestArgs['obj']['claimReward(uint512)void'] | RewardPoolTestArgs['tuple']['claimReward(uint512)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(RewardPoolTestParamsFactory.claimReward(params))
      return {...result, return: result.return as unknown as (undefined | RewardPoolTestReturns['claimReward(uint512)void'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new RewardPoolTestClient(this.appClient.clone(params))
  }

  /**
   * Makes a readonly (simulated) call to the RewardPoolTest smart contract using the `getAdmin()address` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async getAdmin(params: CallParams<RewardPoolTestArgs['obj']['getAdmin()address'] | RewardPoolTestArgs['tuple']['getAdmin()address']> = {args: []}) {
    const result = await this.appClient.send.call(RewardPoolTestParamsFactory.getAdmin(params))
    return result.return as unknown as RewardPoolTestReturns['getAdmin()address']
  }

  /**
   * Makes a readonly (simulated) call to the RewardPoolTest smart contract using the `getRecipientCount()uint512` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async getRecipientCount(params: CallParams<RewardPoolTestArgs['obj']['getRecipientCount()uint512'] | RewardPoolTestArgs['tuple']['getRecipientCount()uint512']> = {args: []}) {
    const result = await this.appClient.send.call(RewardPoolTestParamsFactory.getRecipientCount(params))
    return result.return as unknown as RewardPoolTestReturns['getRecipientCount()uint512']
  }

  /**
   * Makes a readonly (simulated) call to the RewardPoolTest smart contract using the `getTotalDistributed()uint512` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async getTotalDistributed(params: CallParams<RewardPoolTestArgs['obj']['getTotalDistributed()uint512'] | RewardPoolTestArgs['tuple']['getTotalDistributed()uint512']> = {args: []}) {
    const result = await this.appClient.send.call(RewardPoolTestParamsFactory.getTotalDistributed(params))
    return result.return as unknown as RewardPoolTestReturns['getTotalDistributed()uint512']
  }

  /**
   * Makes a readonly (simulated) call to the RewardPoolTest smart contract using the `getTotalClaimed()uint512` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async getTotalClaimed(params: CallParams<RewardPoolTestArgs['obj']['getTotalClaimed()uint512'] | RewardPoolTestArgs['tuple']['getTotalClaimed()uint512']> = {args: []}) {
    const result = await this.appClient.send.call(RewardPoolTestParamsFactory.getTotalClaimed(params))
    return result.return as unknown as RewardPoolTestReturns['getTotalClaimed()uint512']
  }

  /**
   * Makes a readonly (simulated) call to the RewardPoolTest smart contract using the `getRecipientHash(uint512)uint512` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async getRecipientHash(params: CallParams<RewardPoolTestArgs['obj']['getRecipientHash(uint512)uint512'] | RewardPoolTestArgs['tuple']['getRecipientHash(uint512)uint512']>) {
    const result = await this.appClient.send.call(RewardPoolTestParamsFactory.getRecipientHash(params))
    return result.return as unknown as RewardPoolTestReturns['getRecipientHash(uint512)uint512']
  }

  /**
   * Makes a readonly (simulated) call to the RewardPoolTest smart contract using the `getPendingReward(uint512)uint512` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async getPendingReward(params: CallParams<RewardPoolTestArgs['obj']['getPendingReward(uint512)uint512'] | RewardPoolTestArgs['tuple']['getPendingReward(uint512)uint512']>) {
    const result = await this.appClient.send.call(RewardPoolTestParamsFactory.getPendingReward(params))
    return result.return as unknown as RewardPoolTestReturns['getPendingReward(uint512)uint512']
  }

  /**
   * Makes a readonly (simulated) call to the RewardPoolTest smart contract using the `getClaimedReward(uint512)uint512` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async getClaimedReward(params: CallParams<RewardPoolTestArgs['obj']['getClaimedReward(uint512)uint512'] | RewardPoolTestArgs['tuple']['getClaimedReward(uint512)uint512']>) {
    const result = await this.appClient.send.call(RewardPoolTestParamsFactory.getClaimedReward(params))
    return result.return as unknown as RewardPoolTestReturns['getClaimedReward(uint512)uint512']
  }

  /**
   * Methods to access state for the current RewardPoolTest app
   */
  state = {
    /**
     * Methods to access global state for the current RewardPoolTest app
     */
    global: {
      /**
       * Get all current keyed values from global state
       */
      getAll: async (): Promise<Partial<Expand<GlobalKeysState>>> => {
        const result = await this.appClient.state.global.getAll()
        return {
          _admin: result._admin,
          _recipientCount: new BinaryStateValue(result._recipientCount),
          _totalDistributed: new BinaryStateValue(result._totalDistributed),
          _totalClaimed: new BinaryStateValue(result._totalClaimed),
        }
      },
      /**
       * Get the current value of the _admin key in global state
       */
      _admin: async (): Promise<string | undefined> => { return (await this.appClient.state.global.getValue("_admin")) as string | undefined },
      /**
       * Get the current value of the _recipientCount key in global state
       */
      _recipientCount: async (): Promise<BinaryState> => { return new BinaryStateValue((await this.appClient.state.global.getValue("_recipientCount")) as Uint8Array | undefined) },
      /**
       * Get the current value of the _totalDistributed key in global state
       */
      _totalDistributed: async (): Promise<BinaryState> => { return new BinaryStateValue((await this.appClient.state.global.getValue("_totalDistributed")) as Uint8Array | undefined) },
      /**
       * Get the current value of the _totalClaimed key in global state
       */
      _totalClaimed: async (): Promise<BinaryState> => { return new BinaryStateValue((await this.appClient.state.global.getValue("_totalClaimed")) as Uint8Array | undefined) },
    },
    /**
     * Methods to access box state for the current RewardPoolTest app
     */
    box: {
      /**
       * Get all current keyed values from box state
       */
      getAll: async (): Promise<Partial<Expand<BoxKeysState>>> => {
        const result = await this.appClient.state.box.getAll()
        return {
        }
      },
      /**
       * Get values from the _recipientHash map in box state
       */
      _recipientHash: {
        /**
         * Get all current values of the _recipientHash map in box state
         */
        getMap: async (): Promise<Map<Uint8Array, Uint8Array>> => { return (await this.appClient.state.box.getMap("_recipientHash")) as Map<Uint8Array, Uint8Array> },
        /**
         * Get a current value of the _recipientHash map by key from box state
         */
        value: async (key: Uint8Array | string): Promise<Uint8Array | undefined> => { return await this.appClient.state.box.getMapValue("_recipientHash", key) as Uint8Array | undefined },
      },
      /**
       * Get values from the _pendingReward map in box state
       */
      _pendingReward: {
        /**
         * Get all current values of the _pendingReward map in box state
         */
        getMap: async (): Promise<Map<Uint8Array, Uint8Array>> => { return (await this.appClient.state.box.getMap("_pendingReward")) as Map<Uint8Array, Uint8Array> },
        /**
         * Get a current value of the _pendingReward map by key from box state
         */
        value: async (key: Uint8Array | string): Promise<Uint8Array | undefined> => { return await this.appClient.state.box.getMapValue("_pendingReward", key) as Uint8Array | undefined },
      },
      /**
       * Get values from the _claimedReward map in box state
       */
      _claimedReward: {
        /**
         * Get all current values of the _claimedReward map in box state
         */
        getMap: async (): Promise<Map<Uint8Array, Uint8Array>> => { return (await this.appClient.state.box.getMap("_claimedReward")) as Map<Uint8Array, Uint8Array> },
        /**
         * Get a current value of the _claimedReward map by key from box state
         */
        value: async (key: Uint8Array | string): Promise<Uint8Array | undefined> => { return await this.appClient.state.box.getMapValue("_claimedReward", key) as Uint8Array | undefined },
      },
    },
  }

  public newGroup(): RewardPoolTestComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a initRecipient(uint512)void method call against the RewardPoolTest contract
       */
      initRecipient(params: CallParams<RewardPoolTestArgs['obj']['initRecipient(uint512)void'] | RewardPoolTestArgs['tuple']['initRecipient(uint512)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.initRecipient(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a getAdmin()address method call against the RewardPoolTest contract
       */
      getAdmin(params: CallParams<RewardPoolTestArgs['obj']['getAdmin()address'] | RewardPoolTestArgs['tuple']['getAdmin()address']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.getAdmin(params)))
        resultMappers.push((v) => client.decodeReturnValue('getAdmin()address', v))
        return this
      },
      /**
       * Add a getRecipientCount()uint512 method call against the RewardPoolTest contract
       */
      getRecipientCount(params: CallParams<RewardPoolTestArgs['obj']['getRecipientCount()uint512'] | RewardPoolTestArgs['tuple']['getRecipientCount()uint512']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.getRecipientCount(params)))
        resultMappers.push((v) => client.decodeReturnValue('getRecipientCount()uint512', v))
        return this
      },
      /**
       * Add a getTotalDistributed()uint512 method call against the RewardPoolTest contract
       */
      getTotalDistributed(params: CallParams<RewardPoolTestArgs['obj']['getTotalDistributed()uint512'] | RewardPoolTestArgs['tuple']['getTotalDistributed()uint512']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.getTotalDistributed(params)))
        resultMappers.push((v) => client.decodeReturnValue('getTotalDistributed()uint512', v))
        return this
      },
      /**
       * Add a getTotalClaimed()uint512 method call against the RewardPoolTest contract
       */
      getTotalClaimed(params: CallParams<RewardPoolTestArgs['obj']['getTotalClaimed()uint512'] | RewardPoolTestArgs['tuple']['getTotalClaimed()uint512']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.getTotalClaimed(params)))
        resultMappers.push((v) => client.decodeReturnValue('getTotalClaimed()uint512', v))
        return this
      },
      /**
       * Add a getRecipientHash(uint512)uint512 method call against the RewardPoolTest contract
       */
      getRecipientHash(params: CallParams<RewardPoolTestArgs['obj']['getRecipientHash(uint512)uint512'] | RewardPoolTestArgs['tuple']['getRecipientHash(uint512)uint512']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.getRecipientHash(params)))
        resultMappers.push((v) => client.decodeReturnValue('getRecipientHash(uint512)uint512', v))
        return this
      },
      /**
       * Add a getPendingReward(uint512)uint512 method call against the RewardPoolTest contract
       */
      getPendingReward(params: CallParams<RewardPoolTestArgs['obj']['getPendingReward(uint512)uint512'] | RewardPoolTestArgs['tuple']['getPendingReward(uint512)uint512']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.getPendingReward(params)))
        resultMappers.push((v) => client.decodeReturnValue('getPendingReward(uint512)uint512', v))
        return this
      },
      /**
       * Add a getClaimedReward(uint512)uint512 method call against the RewardPoolTest contract
       */
      getClaimedReward(params: CallParams<RewardPoolTestArgs['obj']['getClaimedReward(uint512)uint512'] | RewardPoolTestArgs['tuple']['getClaimedReward(uint512)uint512']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.getClaimedReward(params)))
        resultMappers.push((v) => client.decodeReturnValue('getClaimedReward(uint512)uint512', v))
        return this
      },
      /**
       * Add a addRecipient(uint512)uint512 method call against the RewardPoolTest contract
       */
      addRecipient(params: CallParams<RewardPoolTestArgs['obj']['addRecipient(uint512)uint512'] | RewardPoolTestArgs['tuple']['addRecipient(uint512)uint512']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.addRecipient(params)))
        resultMappers.push((v) => client.decodeReturnValue('addRecipient(uint512)uint512', v))
        return this
      },
      /**
       * Add a distributeReward(uint512,uint512)void method call against the RewardPoolTest contract
       */
      distributeReward(params: CallParams<RewardPoolTestArgs['obj']['distributeReward(uint512,uint512)void'] | RewardPoolTestArgs['tuple']['distributeReward(uint512,uint512)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.distributeReward(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a claimReward(uint512)void method call against the RewardPoolTest contract
       */
      claimReward(params: CallParams<RewardPoolTestArgs['obj']['claimReward(uint512)void'] | RewardPoolTestArgs['tuple']['claimReward(uint512)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.claimReward(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a clear state call to the RewardPoolTest contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as RewardPoolTestComposer
  }
}
export type RewardPoolTestComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the initRecipient(uint512)void ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  initRecipient(params?: CallParams<RewardPoolTestArgs['obj']['initRecipient(uint512)void'] | RewardPoolTestArgs['tuple']['initRecipient(uint512)void']>): RewardPoolTestComposer<[...TReturns, RewardPoolTestReturns['initRecipient(uint512)void'] | undefined]>

  /**
   * Calls the getAdmin()address ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  getAdmin(params?: CallParams<RewardPoolTestArgs['obj']['getAdmin()address'] | RewardPoolTestArgs['tuple']['getAdmin()address']>): RewardPoolTestComposer<[...TReturns, RewardPoolTestReturns['getAdmin()address'] | undefined]>

  /**
   * Calls the getRecipientCount()uint512 ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  getRecipientCount(params?: CallParams<RewardPoolTestArgs['obj']['getRecipientCount()uint512'] | RewardPoolTestArgs['tuple']['getRecipientCount()uint512']>): RewardPoolTestComposer<[...TReturns, RewardPoolTestReturns['getRecipientCount()uint512'] | undefined]>

  /**
   * Calls the getTotalDistributed()uint512 ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  getTotalDistributed(params?: CallParams<RewardPoolTestArgs['obj']['getTotalDistributed()uint512'] | RewardPoolTestArgs['tuple']['getTotalDistributed()uint512']>): RewardPoolTestComposer<[...TReturns, RewardPoolTestReturns['getTotalDistributed()uint512'] | undefined]>

  /**
   * Calls the getTotalClaimed()uint512 ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  getTotalClaimed(params?: CallParams<RewardPoolTestArgs['obj']['getTotalClaimed()uint512'] | RewardPoolTestArgs['tuple']['getTotalClaimed()uint512']>): RewardPoolTestComposer<[...TReturns, RewardPoolTestReturns['getTotalClaimed()uint512'] | undefined]>

  /**
   * Calls the getRecipientHash(uint512)uint512 ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  getRecipientHash(params?: CallParams<RewardPoolTestArgs['obj']['getRecipientHash(uint512)uint512'] | RewardPoolTestArgs['tuple']['getRecipientHash(uint512)uint512']>): RewardPoolTestComposer<[...TReturns, RewardPoolTestReturns['getRecipientHash(uint512)uint512'] | undefined]>

  /**
   * Calls the getPendingReward(uint512)uint512 ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  getPendingReward(params?: CallParams<RewardPoolTestArgs['obj']['getPendingReward(uint512)uint512'] | RewardPoolTestArgs['tuple']['getPendingReward(uint512)uint512']>): RewardPoolTestComposer<[...TReturns, RewardPoolTestReturns['getPendingReward(uint512)uint512'] | undefined]>

  /**
   * Calls the getClaimedReward(uint512)uint512 ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  getClaimedReward(params?: CallParams<RewardPoolTestArgs['obj']['getClaimedReward(uint512)uint512'] | RewardPoolTestArgs['tuple']['getClaimedReward(uint512)uint512']>): RewardPoolTestComposer<[...TReturns, RewardPoolTestReturns['getClaimedReward(uint512)uint512'] | undefined]>

  /**
   * Calls the addRecipient(uint512)uint512 ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  addRecipient(params?: CallParams<RewardPoolTestArgs['obj']['addRecipient(uint512)uint512'] | RewardPoolTestArgs['tuple']['addRecipient(uint512)uint512']>): RewardPoolTestComposer<[...TReturns, RewardPoolTestReturns['addRecipient(uint512)uint512'] | undefined]>

  /**
   * Calls the distributeReward(uint512,uint512)void ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  distributeReward(params?: CallParams<RewardPoolTestArgs['obj']['distributeReward(uint512,uint512)void'] | RewardPoolTestArgs['tuple']['distributeReward(uint512,uint512)void']>): RewardPoolTestComposer<[...TReturns, RewardPoolTestReturns['distributeReward(uint512,uint512)void'] | undefined]>

  /**
   * Calls the claimReward(uint512)void ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  claimReward(params?: CallParams<RewardPoolTestArgs['obj']['claimReward(uint512)void'] | RewardPoolTestArgs['tuple']['claimReward(uint512)void']>): RewardPoolTestComposer<[...TReturns, RewardPoolTestReturns['claimReward(uint512)void'] | undefined]>

  /**
   * Makes a clear_state call to an existing instance of the RewardPoolTest smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): RewardPoolTestComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): RewardPoolTestComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<RewardPoolTestComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<RewardPoolTestComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<RewardPoolTestComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<RewardPoolTestComposerResults<TReturns>>
}
export type RewardPoolTestComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

