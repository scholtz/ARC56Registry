/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"VotingTest","structs":{"Proposal":[{"name":"forVotes","type":"uint512"},{"name":"againstVotes","type":"uint512"},{"name":"deadline","type":"uint512"},{"name":"executed","type":"uint8"}]},"methods":[{"name":"createProposal","args":[{"type":"uint512","name":"deadline"}],"returns":{"type":"uint512"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}},{"name":"vote","args":[{"type":"uint512","name":"proposalId"},{"type":"bool","name":"support"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}},{"name":"getForVotes","args":[{"type":"uint512","name":"proposalId"}],"returns":{"type":"uint512"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"getAgainstVotes","args":[{"type":"uint512","name":"proposalId"}],"returns":{"type":"uint512"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"isExecuted","args":[{"type":"uint512","name":"proposalId"}],"returns":{"type":"bool"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"execute","args":[{"type":"uint512","name":"proposalId"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}}],"arcs":[22,28],"networks":{},"state":{"schema":{"global":{"ints":0,"bytes":2},"local":{"ints":0,"bytes":0}},"keys":{"global":{"proposalCount":{"keyType":"AVMString","valueType":"AVMBytes","key":"cHJvcG9zYWxDb3VudA=="},"admin":{"keyType":"AVMString","valueType":"address","key":"YWRtaW4="}},"local":{},"box":{}},"maps":{"global":{},"local":{},"box":{"proposals":{"keyType":"AVMBytes","valueType":"Proposal","prefix":"cHJvcG9zYWxz"},"hasVoted":{"keyType":"AVMBytes","valueType":"uint64","prefix":"aGFzVm90ZWQ="}}}},"bareActions":{"create":[],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[582],"errorMessage":"AlreadyVoted"},{"pc":[854],"errorMessage":"NotAdmin"},{"pc":[884],"errorMessage":"ProposalNotSucceeded"},{"pc":[851],"errorMessage":"check admin exists"},{"pc":[332],"errorMessage":"check proposalCount exists"},{"pc":[542],"errorMessage":"invalid number of bytes for arc4.bool"},{"pc":[328,534,720,759,798,844],"errorMessage":"invalid number of bytes for arc4.uint512"},{"pc":[347,519,634,682],"errorMessage":"overflow"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"events":[]} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


// Type definitions for ARC-56 structs

export type Proposal = {
  forVotes: bigint,
  againstVotes: bigint,
  deadline: bigint,
  executed: number
}


/**
 * Converts the ABI tuple representation of a Proposal to the struct representation
 */
export function ProposalFromTuple(abiTuple: [bigint, bigint, bigint, number]) {
  return getABIStructFromABITuple(abiTuple, APP_SPEC.structs.Proposal, APP_SPEC.structs) as Proposal
}

/**
 * The argument types for the VotingTest contract
 */
export type VotingTestArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'createProposal(uint512)uint512': {
      deadline: bigint | number
    }
    'vote(uint512,bool)void': {
      proposalId: bigint | number
      support: boolean
    }
    'getForVotes(uint512)uint512': {
      proposalId: bigint | number
    }
    'getAgainstVotes(uint512)uint512': {
      proposalId: bigint | number
    }
    'isExecuted(uint512)bool': {
      proposalId: bigint | number
    }
    'execute(uint512)void': {
      proposalId: bigint | number
    }
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'createProposal(uint512)uint512': [deadline: bigint | number]
    'vote(uint512,bool)void': [proposalId: bigint | number, support: boolean]
    'getForVotes(uint512)uint512': [proposalId: bigint | number]
    'getAgainstVotes(uint512)uint512': [proposalId: bigint | number]
    'isExecuted(uint512)bool': [proposalId: bigint | number]
    'execute(uint512)void': [proposalId: bigint | number]
  }
}

/**
 * The return type for each method
 */
export type VotingTestReturns = {
  'createProposal(uint512)uint512': bigint
  'vote(uint512,bool)void': void
  'getForVotes(uint512)uint512': bigint
  'getAgainstVotes(uint512)uint512': bigint
  'isExecuted(uint512)bool': boolean
  'execute(uint512)void': void
}

/**
 * Defines the types of available calls and state of the VotingTest smart contract.
 */
export type VotingTestTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'createProposal(uint512)uint512' | 'createProposal', {
      argsObj: VotingTestArgs['obj']['createProposal(uint512)uint512']
      argsTuple: VotingTestArgs['tuple']['createProposal(uint512)uint512']
      returns: VotingTestReturns['createProposal(uint512)uint512']
    }>
    & Record<'vote(uint512,bool)void' | 'vote', {
      argsObj: VotingTestArgs['obj']['vote(uint512,bool)void']
      argsTuple: VotingTestArgs['tuple']['vote(uint512,bool)void']
      returns: VotingTestReturns['vote(uint512,bool)void']
    }>
    & Record<'getForVotes(uint512)uint512' | 'getForVotes', {
      argsObj: VotingTestArgs['obj']['getForVotes(uint512)uint512']
      argsTuple: VotingTestArgs['tuple']['getForVotes(uint512)uint512']
      returns: VotingTestReturns['getForVotes(uint512)uint512']
    }>
    & Record<'getAgainstVotes(uint512)uint512' | 'getAgainstVotes', {
      argsObj: VotingTestArgs['obj']['getAgainstVotes(uint512)uint512']
      argsTuple: VotingTestArgs['tuple']['getAgainstVotes(uint512)uint512']
      returns: VotingTestReturns['getAgainstVotes(uint512)uint512']
    }>
    & Record<'isExecuted(uint512)bool' | 'isExecuted', {
      argsObj: VotingTestArgs['obj']['isExecuted(uint512)bool']
      argsTuple: VotingTestArgs['tuple']['isExecuted(uint512)bool']
      returns: VotingTestReturns['isExecuted(uint512)bool']
    }>
    & Record<'execute(uint512)void' | 'execute', {
      argsObj: VotingTestArgs['obj']['execute(uint512)void']
      argsTuple: VotingTestArgs['tuple']['execute(uint512)void']
      returns: VotingTestReturns['execute(uint512)void']
    }>
  /**
   * Defines the shape of the state of the application.
   */
  state: {
    global: {
      keys: {
        proposalCount: BinaryState
        admin: string
      }
      maps: {}
    }
    box: {
      keys: {}
      maps: {
        proposals: Map<Uint8Array | string, Proposal>
        hasVoted: Map<Uint8Array | string, bigint>
      }
    }
  }
}

/**
 * Defines the possible abi call signatures.
 */
export type VotingTestSignatures = keyof VotingTestTypes['methods']
/**
 * Defines the possible abi call signatures for methods that return a non-void value.
 */
export type VotingTestNonVoidMethodSignatures = keyof VotingTestTypes['methods'] extends infer T ? T extends keyof VotingTestTypes['methods'] ? MethodReturn<T> extends void ? never : T  : never : never
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the VotingTest smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends VotingTestSignatures> = VotingTestTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the VotingTest smart contract to the method's return type
 */
export type MethodReturn<TSignature extends VotingTestSignatures> = VotingTestTypes['methods'][TSignature]['returns']

/**
 * Defines the shape of the keyed global state of the application.
 */
export type GlobalKeysState = VotingTestTypes['state']['global']['keys']

/**
 * Defines the shape of the keyed box state of the application.
 */
export type BoxKeysState = VotingTestTypes['state']['box']['keys']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the VotingTest smart contract
 */
export abstract class VotingTestParamsFactory {
  /**
   * Constructs a no op call for the createProposal(uint512)uint512 ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static createProposal(params: CallParams<VotingTestArgs['obj']['createProposal(uint512)uint512'] | VotingTestArgs['tuple']['createProposal(uint512)uint512']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'createProposal(uint512)uint512' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.deadline],
    }
  }
  /**
   * Constructs a no op call for the vote(uint512,bool)void ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static vote(params: CallParams<VotingTestArgs['obj']['vote(uint512,bool)void'] | VotingTestArgs['tuple']['vote(uint512,bool)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'vote(uint512,bool)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.proposalId, params.args.support],
    }
  }
  /**
   * Constructs a no op call for the getForVotes(uint512)uint512 ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static getForVotes(params: CallParams<VotingTestArgs['obj']['getForVotes(uint512)uint512'] | VotingTestArgs['tuple']['getForVotes(uint512)uint512']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'getForVotes(uint512)uint512' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.proposalId],
    }
  }
  /**
   * Constructs a no op call for the getAgainstVotes(uint512)uint512 ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static getAgainstVotes(params: CallParams<VotingTestArgs['obj']['getAgainstVotes(uint512)uint512'] | VotingTestArgs['tuple']['getAgainstVotes(uint512)uint512']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'getAgainstVotes(uint512)uint512' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.proposalId],
    }
  }
  /**
   * Constructs a no op call for the isExecuted(uint512)bool ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static isExecuted(params: CallParams<VotingTestArgs['obj']['isExecuted(uint512)bool'] | VotingTestArgs['tuple']['isExecuted(uint512)bool']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'isExecuted(uint512)bool' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.proposalId],
    }
  }
  /**
   * Constructs a no op call for the execute(uint512)void ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static execute(params: CallParams<VotingTestArgs['obj']['execute(uint512)void'] | VotingTestArgs['tuple']['execute(uint512)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'execute(uint512)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.proposalId],
    }
  }
}

/**
 * A client to make calls to the VotingTest smart contract
 */
export class VotingTestClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `VotingTestClient`
   *
   * @param appClient An `AppClient` instance which has been created with the VotingTest app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `VotingTestClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Checks for decode errors on the given return value and maps the return value to the return type for the given method
   * @returns The typed return value or undefined if there was no value
   */
  decodeReturnValue<TSignature extends VotingTestNonVoidMethodSignatures>(method: TSignature, returnValue: ABIReturn | undefined) {
    return returnValue !== undefined ? getArc56ReturnValue<MethodReturn<TSignature>>(returnValue, this.appClient.getABIMethod(method), APP_SPEC.structs) : undefined
  }

  /**
   * Returns a new `VotingTestClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<VotingTestClient> {
    return new VotingTestClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `VotingTestClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<VotingTestClient> {
    return new VotingTestClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Makes a clear_state call to an existing instance of the VotingTest smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the VotingTest smart contract using the `createProposal(uint512)uint512` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    createProposal: (params: CallParams<VotingTestArgs['obj']['createProposal(uint512)uint512'] | VotingTestArgs['tuple']['createProposal(uint512)uint512']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(VotingTestParamsFactory.createProposal(params))
    },

    /**
     * Makes a call to the VotingTest smart contract using the `vote(uint512,bool)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    vote: (params: CallParams<VotingTestArgs['obj']['vote(uint512,bool)void'] | VotingTestArgs['tuple']['vote(uint512,bool)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(VotingTestParamsFactory.vote(params))
    },

    /**
     * Makes a call to the VotingTest smart contract using the `getForVotes(uint512)uint512` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    getForVotes: (params: CallParams<VotingTestArgs['obj']['getForVotes(uint512)uint512'] | VotingTestArgs['tuple']['getForVotes(uint512)uint512']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(VotingTestParamsFactory.getForVotes(params))
    },

    /**
     * Makes a call to the VotingTest smart contract using the `getAgainstVotes(uint512)uint512` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    getAgainstVotes: (params: CallParams<VotingTestArgs['obj']['getAgainstVotes(uint512)uint512'] | VotingTestArgs['tuple']['getAgainstVotes(uint512)uint512']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(VotingTestParamsFactory.getAgainstVotes(params))
    },

    /**
     * Makes a call to the VotingTest smart contract using the `isExecuted(uint512)bool` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    isExecuted: (params: CallParams<VotingTestArgs['obj']['isExecuted(uint512)bool'] | VotingTestArgs['tuple']['isExecuted(uint512)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(VotingTestParamsFactory.isExecuted(params))
    },

    /**
     * Makes a call to the VotingTest smart contract using the `execute(uint512)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    execute: (params: CallParams<VotingTestArgs['obj']['execute(uint512)void'] | VotingTestArgs['tuple']['execute(uint512)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(VotingTestParamsFactory.execute(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Makes a clear_state call to an existing instance of the VotingTest smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the VotingTest smart contract using the `createProposal(uint512)uint512` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    createProposal: (params: CallParams<VotingTestArgs['obj']['createProposal(uint512)uint512'] | VotingTestArgs['tuple']['createProposal(uint512)uint512']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(VotingTestParamsFactory.createProposal(params))
    },

    /**
     * Makes a call to the VotingTest smart contract using the `vote(uint512,bool)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    vote: (params: CallParams<VotingTestArgs['obj']['vote(uint512,bool)void'] | VotingTestArgs['tuple']['vote(uint512,bool)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(VotingTestParamsFactory.vote(params))
    },

    /**
     * Makes a call to the VotingTest smart contract using the `getForVotes(uint512)uint512` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    getForVotes: (params: CallParams<VotingTestArgs['obj']['getForVotes(uint512)uint512'] | VotingTestArgs['tuple']['getForVotes(uint512)uint512']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(VotingTestParamsFactory.getForVotes(params))
    },

    /**
     * Makes a call to the VotingTest smart contract using the `getAgainstVotes(uint512)uint512` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    getAgainstVotes: (params: CallParams<VotingTestArgs['obj']['getAgainstVotes(uint512)uint512'] | VotingTestArgs['tuple']['getAgainstVotes(uint512)uint512']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(VotingTestParamsFactory.getAgainstVotes(params))
    },

    /**
     * Makes a call to the VotingTest smart contract using the `isExecuted(uint512)bool` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    isExecuted: (params: CallParams<VotingTestArgs['obj']['isExecuted(uint512)bool'] | VotingTestArgs['tuple']['isExecuted(uint512)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(VotingTestParamsFactory.isExecuted(params))
    },

    /**
     * Makes a call to the VotingTest smart contract using the `execute(uint512)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    execute: (params: CallParams<VotingTestArgs['obj']['execute(uint512)void'] | VotingTestArgs['tuple']['execute(uint512)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(VotingTestParamsFactory.execute(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Makes a clear_state call to an existing instance of the VotingTest smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the VotingTest smart contract using the `createProposal(uint512)uint512` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    createProposal: async (params: CallParams<VotingTestArgs['obj']['createProposal(uint512)uint512'] | VotingTestArgs['tuple']['createProposal(uint512)uint512']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(VotingTestParamsFactory.createProposal(params))
      return {...result, return: result.return as unknown as (undefined | VotingTestReturns['createProposal(uint512)uint512'])}
    },

    /**
     * Makes a call to the VotingTest smart contract using the `vote(uint512,bool)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    vote: async (params: CallParams<VotingTestArgs['obj']['vote(uint512,bool)void'] | VotingTestArgs['tuple']['vote(uint512,bool)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(VotingTestParamsFactory.vote(params))
      return {...result, return: result.return as unknown as (undefined | VotingTestReturns['vote(uint512,bool)void'])}
    },

    /**
     * Makes a call to the VotingTest smart contract using the `getForVotes(uint512)uint512` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    getForVotes: async (params: CallParams<VotingTestArgs['obj']['getForVotes(uint512)uint512'] | VotingTestArgs['tuple']['getForVotes(uint512)uint512']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(VotingTestParamsFactory.getForVotes(params))
      return {...result, return: result.return as unknown as (undefined | VotingTestReturns['getForVotes(uint512)uint512'])}
    },

    /**
     * Makes a call to the VotingTest smart contract using the `getAgainstVotes(uint512)uint512` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    getAgainstVotes: async (params: CallParams<VotingTestArgs['obj']['getAgainstVotes(uint512)uint512'] | VotingTestArgs['tuple']['getAgainstVotes(uint512)uint512']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(VotingTestParamsFactory.getAgainstVotes(params))
      return {...result, return: result.return as unknown as (undefined | VotingTestReturns['getAgainstVotes(uint512)uint512'])}
    },

    /**
     * Makes a call to the VotingTest smart contract using the `isExecuted(uint512)bool` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    isExecuted: async (params: CallParams<VotingTestArgs['obj']['isExecuted(uint512)bool'] | VotingTestArgs['tuple']['isExecuted(uint512)bool']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(VotingTestParamsFactory.isExecuted(params))
      return {...result, return: result.return as unknown as (undefined | VotingTestReturns['isExecuted(uint512)bool'])}
    },

    /**
     * Makes a call to the VotingTest smart contract using the `execute(uint512)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    execute: async (params: CallParams<VotingTestArgs['obj']['execute(uint512)void'] | VotingTestArgs['tuple']['execute(uint512)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(VotingTestParamsFactory.execute(params))
      return {...result, return: result.return as unknown as (undefined | VotingTestReturns['execute(uint512)void'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new VotingTestClient(this.appClient.clone(params))
  }

  /**
   * Makes a readonly (simulated) call to the VotingTest smart contract using the `getForVotes(uint512)uint512` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async getForVotes(params: CallParams<VotingTestArgs['obj']['getForVotes(uint512)uint512'] | VotingTestArgs['tuple']['getForVotes(uint512)uint512']>) {
    const result = await this.appClient.send.call(VotingTestParamsFactory.getForVotes(params))
    return result.return as unknown as VotingTestReturns['getForVotes(uint512)uint512']
  }

  /**
   * Makes a readonly (simulated) call to the VotingTest smart contract using the `getAgainstVotes(uint512)uint512` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async getAgainstVotes(params: CallParams<VotingTestArgs['obj']['getAgainstVotes(uint512)uint512'] | VotingTestArgs['tuple']['getAgainstVotes(uint512)uint512']>) {
    const result = await this.appClient.send.call(VotingTestParamsFactory.getAgainstVotes(params))
    return result.return as unknown as VotingTestReturns['getAgainstVotes(uint512)uint512']
  }

  /**
   * Makes a readonly (simulated) call to the VotingTest smart contract using the `isExecuted(uint512)bool` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async isExecuted(params: CallParams<VotingTestArgs['obj']['isExecuted(uint512)bool'] | VotingTestArgs['tuple']['isExecuted(uint512)bool']>) {
    const result = await this.appClient.send.call(VotingTestParamsFactory.isExecuted(params))
    return result.return as unknown as VotingTestReturns['isExecuted(uint512)bool']
  }

  /**
   * Methods to access state for the current VotingTest app
   */
  state = {
    /**
     * Methods to access global state for the current VotingTest app
     */
    global: {
      /**
       * Get all current keyed values from global state
       */
      getAll: async (): Promise<Partial<Expand<GlobalKeysState>>> => {
        const result = await this.appClient.state.global.getAll()
        return {
          proposalCount: new BinaryStateValue(result.proposalCount),
          admin: result.admin,
        }
      },
      /**
       * Get the current value of the proposalCount key in global state
       */
      proposalCount: async (): Promise<BinaryState> => { return new BinaryStateValue((await this.appClient.state.global.getValue("proposalCount")) as Uint8Array | undefined) },
      /**
       * Get the current value of the admin key in global state
       */
      admin: async (): Promise<string | undefined> => { return (await this.appClient.state.global.getValue("admin")) as string | undefined },
    },
    /**
     * Methods to access box state for the current VotingTest app
     */
    box: {
      /**
       * Get all current keyed values from box state
       */
      getAll: async (): Promise<Partial<Expand<BoxKeysState>>> => {
        const result = await this.appClient.state.box.getAll()
        return {
        }
      },
      /**
       * Get values from the proposals map in box state
       */
      proposals: {
        /**
         * Get all current values of the proposals map in box state
         */
        getMap: async (): Promise<Map<Uint8Array, Proposal>> => { return (await this.appClient.state.box.getMap("proposals")) as Map<Uint8Array, Proposal> },
        /**
         * Get a current value of the proposals map by key from box state
         */
        value: async (key: Uint8Array | string): Promise<Proposal | undefined> => { return await this.appClient.state.box.getMapValue("proposals", key) as Proposal | undefined },
      },
      /**
       * Get values from the hasVoted map in box state
       */
      hasVoted: {
        /**
         * Get all current values of the hasVoted map in box state
         */
        getMap: async (): Promise<Map<Uint8Array, bigint>> => { return (await this.appClient.state.box.getMap("hasVoted")) as Map<Uint8Array, bigint> },
        /**
         * Get a current value of the hasVoted map by key from box state
         */
        value: async (key: Uint8Array | string): Promise<bigint | undefined> => { return await this.appClient.state.box.getMapValue("hasVoted", key) as bigint | undefined },
      },
    },
  }

  public newGroup(): VotingTestComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a createProposal(uint512)uint512 method call against the VotingTest contract
       */
      createProposal(params: CallParams<VotingTestArgs['obj']['createProposal(uint512)uint512'] | VotingTestArgs['tuple']['createProposal(uint512)uint512']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.createProposal(params)))
        resultMappers.push((v) => client.decodeReturnValue('createProposal(uint512)uint512', v))
        return this
      },
      /**
       * Add a vote(uint512,bool)void method call against the VotingTest contract
       */
      vote(params: CallParams<VotingTestArgs['obj']['vote(uint512,bool)void'] | VotingTestArgs['tuple']['vote(uint512,bool)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.vote(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a getForVotes(uint512)uint512 method call against the VotingTest contract
       */
      getForVotes(params: CallParams<VotingTestArgs['obj']['getForVotes(uint512)uint512'] | VotingTestArgs['tuple']['getForVotes(uint512)uint512']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.getForVotes(params)))
        resultMappers.push((v) => client.decodeReturnValue('getForVotes(uint512)uint512', v))
        return this
      },
      /**
       * Add a getAgainstVotes(uint512)uint512 method call against the VotingTest contract
       */
      getAgainstVotes(params: CallParams<VotingTestArgs['obj']['getAgainstVotes(uint512)uint512'] | VotingTestArgs['tuple']['getAgainstVotes(uint512)uint512']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.getAgainstVotes(params)))
        resultMappers.push((v) => client.decodeReturnValue('getAgainstVotes(uint512)uint512', v))
        return this
      },
      /**
       * Add a isExecuted(uint512)bool method call against the VotingTest contract
       */
      isExecuted(params: CallParams<VotingTestArgs['obj']['isExecuted(uint512)bool'] | VotingTestArgs['tuple']['isExecuted(uint512)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.isExecuted(params)))
        resultMappers.push((v) => client.decodeReturnValue('isExecuted(uint512)bool', v))
        return this
      },
      /**
       * Add a execute(uint512)void method call against the VotingTest contract
       */
      execute(params: CallParams<VotingTestArgs['obj']['execute(uint512)void'] | VotingTestArgs['tuple']['execute(uint512)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.execute(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a clear state call to the VotingTest contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as VotingTestComposer
  }
}
export type VotingTestComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the createProposal(uint512)uint512 ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  createProposal(params?: CallParams<VotingTestArgs['obj']['createProposal(uint512)uint512'] | VotingTestArgs['tuple']['createProposal(uint512)uint512']>): VotingTestComposer<[...TReturns, VotingTestReturns['createProposal(uint512)uint512'] | undefined]>

  /**
   * Calls the vote(uint512,bool)void ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  vote(params?: CallParams<VotingTestArgs['obj']['vote(uint512,bool)void'] | VotingTestArgs['tuple']['vote(uint512,bool)void']>): VotingTestComposer<[...TReturns, VotingTestReturns['vote(uint512,bool)void'] | undefined]>

  /**
   * Calls the getForVotes(uint512)uint512 ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  getForVotes(params?: CallParams<VotingTestArgs['obj']['getForVotes(uint512)uint512'] | VotingTestArgs['tuple']['getForVotes(uint512)uint512']>): VotingTestComposer<[...TReturns, VotingTestReturns['getForVotes(uint512)uint512'] | undefined]>

  /**
   * Calls the getAgainstVotes(uint512)uint512 ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  getAgainstVotes(params?: CallParams<VotingTestArgs['obj']['getAgainstVotes(uint512)uint512'] | VotingTestArgs['tuple']['getAgainstVotes(uint512)uint512']>): VotingTestComposer<[...TReturns, VotingTestReturns['getAgainstVotes(uint512)uint512'] | undefined]>

  /**
   * Calls the isExecuted(uint512)bool ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  isExecuted(params?: CallParams<VotingTestArgs['obj']['isExecuted(uint512)bool'] | VotingTestArgs['tuple']['isExecuted(uint512)bool']>): VotingTestComposer<[...TReturns, VotingTestReturns['isExecuted(uint512)bool'] | undefined]>

  /**
   * Calls the execute(uint512)void ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  execute(params?: CallParams<VotingTestArgs['obj']['execute(uint512)void'] | VotingTestArgs['tuple']['execute(uint512)void']>): VotingTestComposer<[...TReturns, VotingTestReturns['execute(uint512)void'] | undefined]>

  /**
   * Makes a clear_state call to an existing instance of the VotingTest smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): VotingTestComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): VotingTestComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<VotingTestComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<VotingTestComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<VotingTestComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<VotingTestComposerResults<TReturns>>
}
export type VotingTestComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

