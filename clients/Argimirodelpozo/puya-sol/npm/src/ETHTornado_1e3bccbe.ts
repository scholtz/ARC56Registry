/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"ETHTornado","structs":{},"methods":[{"name":"__postInit","args":[{"type":"address","name":"_verifier"},{"type":"address","name":"_hasher"},{"type":"uint256","name":"_denomination"},{"type":"uint64","name":"_merkleTreeHeight"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}},{"name":"verifier","args":[],"returns":{"type":"address"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"denomination","args":[],"returns":{"type":"uint256"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"nullifierHashes","args":[{"type":"byte[32]","name":"key0"}],"returns":{"type":"bool"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"commitments","args":[{"type":"byte[32]","name":"key0"}],"returns":{"type":"bool"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"FIELD_SIZE","args":[],"returns":{"type":"uint256"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"ZERO_VALUE","args":[],"returns":{"type":"uint256"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"hasher","args":[],"returns":{"type":"address"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"levels","args":[],"returns":{"type":"uint64"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"filledSubtrees","args":[{"type":"uint256","name":"key0"}],"returns":{"type":"byte[32]"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"roots","args":[{"type":"uint256","name":"key0"}],"returns":{"type":"byte[32]"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"ROOT_HISTORY_SIZE","args":[],"returns":{"type":"uint64"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"currentRootIndex","args":[],"returns":{"type":"uint64"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"nextIndex","args":[],"returns":{"type":"uint64"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"deposit","args":[{"type":"byte[32]","name":"_commitment"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"@dev Deposit funds into the contract. The caller must send (for ETH) or approve (for ERC20) value equal to or `denomination` of this instance.\n@param _commitment the note commitment, which is PedersenHash(nullifier + secret)","events":[{"name":"Deposit","args":[{"type":"uint8[32]","name":"commitment"},{"type":"uint64","name":"leafIndex"},{"type":"uint64","name":"timestamp"}]}],"recommendations":{}},{"name":"withdraw","args":[{"type":"byte[]","name":"_proof"},{"type":"byte[32]","name":"_root"},{"type":"byte[32]","name":"_nullifierHash"},{"type":"address","name":"_recipient"},{"type":"address","name":"_relayer"},{"type":"uint256","name":"_fee"},{"type":"uint256","name":"_refund"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"@dev Withdraw a deposit from the contract. `proof` is a zkSNARK proof data, and input is an array of circuit public inputs\n`input` array consists of:\n- merkle root of all deposits in the contract\n- hash of unique deposit nullifier to prevent double spends\n- the recipient of funds\n- optional fee that goes to the transaction sender (usually a relay)","events":[{"name":"Withdrawal","args":[{"type":"uint8[32]","name":"to"},{"type":"uint8[32]","name":"nullifierHash"},{"type":"uint8[32]","name":"relayer"},{"type":"uint256","name":"fee"}]}],"recommendations":{}},{"name":"isSpent","args":[{"type":"byte[32]","name":"_nullifierHash"}],"returns":{"type":"bool"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"desc":"@dev whether a note is already spent ","events":[],"recommendations":{}},{"name":"hashLeftRight","args":[{"type":"address","name":"_hasher"},{"type":"byte[32]","name":"_left"},{"type":"byte[32]","name":"_right"}],"returns":{"type":"byte[32]"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"desc":"@dev Hash 2 tree leaves, returns MiMC(_left, _right)","events":[],"recommendations":{}},{"name":"isKnownRoot","args":[{"type":"byte[32]","name":"_root"}],"returns":{"type":"bool"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"desc":"@dev Whether the root is present in the root history","events":[],"recommendations":{}},{"name":"getLastRoot","args":[],"returns":{"type":"byte[32]"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"desc":"@dev Returns the last root","events":[],"recommendations":{}},{"name":"zeros","args":[{"type":"uint256","name":"i"}],"returns":{"type":"byte[32]"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"desc":"@dev provides Zero (Empty) elements for a MiMC MerkleTree. Up to 32 levels","events":[],"recommendations":{}}],"arcs":[22,28],"networks":{},"state":{"schema":{"global":{"ints":4,"bytes":4},"local":{"ints":0,"bytes":0}},"keys":{"global":{"verifier":{"keyType":"AVMString","valueType":"address","key":"dmVyaWZpZXI="},"denomination":{"keyType":"AVMString","valueType":"AVMBytes","key":"ZGVub21pbmF0aW9u"},"_status":{"keyType":"AVMString","valueType":"AVMBytes","key":"X3N0YXR1cw=="},"hasher":{"keyType":"AVMString","valueType":"address","key":"aGFzaGVy"},"levels":{"keyType":"AVMString","valueType":"AVMUint64","key":"bGV2ZWxz"},"currentRootIndex":{"keyType":"AVMString","valueType":"AVMUint64","key":"Y3VycmVudFJvb3RJbmRleA=="},"nextIndex":{"keyType":"AVMString","valueType":"AVMUint64","key":"bmV4dEluZGV4"},"__ctor_pending":{"keyType":"AVMString","valueType":"AVMUint64","key":"X19jdG9yX3BlbmRpbmc="}},"local":{},"box":{}},"maps":{"global":{},"local":{},"box":{"nullifierHashes":{"keyType":"AVMBytes","valueType":"uint64","prefix":"bnVsbGlmaWVySGFzaGVz"},"commitments":{"keyType":"AVMBytes","valueType":"uint64","prefix":"Y29tbWl0bWVudHM="},"filledSubtrees":{"keyType":"AVMBytes","valueType":"AVMBytes","prefix":"ZmlsbGVkU3VidHJlZXM="},"roots":{"keyType":"AVMBytes","valueType":"AVMBytes","prefix":"cm9vdHM="}}}},"bareActions":{"create":[],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[1396],"errorMessage":"Cannot find your merkle root"},{"pc":[1372],"errorMessage":"Fee exceeds transfer value"},{"pc":[3375],"errorMessage":"Index out of bounds"},{"pc":[1523],"errorMessage":"Invalid withdraw proof"},{"pc":[1066],"errorMessage":"Merkle tree is full. No more leaves can be added"},{"pc":[1531],"errorMessage":"Message value is supposed to be zero for ETH instance"},{"pc":[1244],"errorMessage":"Please send `mixDenomination` ETH along with transaction"},{"pc":[1014,1358],"errorMessage":"ReentrancyGuard: reentrant call"},{"pc":[1534],"errorMessage":"Refund value is supposed to be zero for ETH instance"},{"pc":[1038],"errorMessage":"The commitment has been submitted"},{"pc":[1390],"errorMessage":"The note has been already spent"},{"pc":[651],"errorMessage":"__postInit already called"},{"pc":[1793],"errorMessage":"_left should be inside the field"},{"pc":[657],"errorMessage":"_levels should be greater than zero"},{"pc":[661],"errorMessage":"_levels should be less than 32"},{"pc":[1799],"errorMessage":"_right should be inside the field"},{"pc":[1010,1354],"errorMessage":"check _status exists"},{"pc":[977,1179,1743,1944],"errorMessage":"check currentRootIndex exists"},{"pc":[801,1242,1368,1539],"errorMessage":"check denomination exists"},{"pc":[886,1122],"errorMessage":"check hasher exists"},{"pc":[897,1054,1074],"errorMessage":"check levels exists"},{"pc":[989,1049],"errorMessage":"check nextIndex exists"},{"pc":[790,1486],"errorMessage":"check verifier exists"},{"pc":[774],"errorMessage":"denomination should be greater than 0"},{"pc":[1277],"errorMessage":"invalid array length header"},{"pc":[1284],"errorMessage":"invalid number of bytes for arc4.dynamic_array<arc4.uint8>"},{"pc":[616,624,823,856,1006,1295,1306,1317,1328,1655,1688,1696,1704,1721],"errorMessage":"invalid number of bytes for arc4.static_array<arc4.uint8, 32>"},{"pc":[634,912,946,1342,1350,1775],"errorMessage":"invalid number of bytes for arc4.uint256"},{"pc":[643],"errorMessage":"invalid number of bytes for arc4.uint64"},{"pc":[718,806,1062,1186,1223,1616],"errorMessage":"overflow"},{"pc":[1544],"errorMessage":"underflow"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"events":[{"name":"Deposit","args":[{"type":"uint8[32]","name":"commitment"},{"type":"uint64","name":"leafIndex"},{"type":"uint64","name":"timestamp"}]},{"name":"Withdrawal","args":[{"type":"uint8[32]","name":"to"},{"type":"uint8[32]","name":"nullifierHash"},{"type":"uint8[32]","name":"relayer"},{"type":"uint256","name":"fee"}]}]} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


/**
 * The argument types for the EthTornado contract
 */
export type EthTornadoArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    '__postInit(address,address,uint256,uint64)void': {
      _verifier: string
      _hasher: string
      _denomination: bigint | number
      _merkleTreeHeight: bigint | number
    }
    'verifier()address': Record<string, never>
    'denomination()uint256': Record<string, never>
    'nullifierHashes(byte[32])bool': {
      key0: Uint8Array
    }
    'commitments(byte[32])bool': {
      key0: Uint8Array
    }
    'FIELD_SIZE()uint256': Record<string, never>
    'ZERO_VALUE()uint256': Record<string, never>
    'hasher()address': Record<string, never>
    'levels()uint64': Record<string, never>
    'filledSubtrees(uint256)byte[32]': {
      key0: bigint | number
    }
    'roots(uint256)byte[32]': {
      key0: bigint | number
    }
    'ROOT_HISTORY_SIZE()uint64': Record<string, never>
    'currentRootIndex()uint64': Record<string, never>
    'nextIndex()uint64': Record<string, never>
    'deposit(byte[32])void': {
      _commitment: Uint8Array
    }
    'withdraw(byte[],byte[32],byte[32],address,address,uint256,uint256)void': {
      _proof: Uint8Array
      _root: Uint8Array
      _nullifierHash: Uint8Array
      _recipient: string
      _relayer: string
      _fee: bigint | number
      _refund: bigint | number
    }
    'isSpent(byte[32])bool': {
      _nullifierHash: Uint8Array
    }
    'hashLeftRight(address,byte[32],byte[32])byte[32]': {
      _hasher: string
      _left: Uint8Array
      _right: Uint8Array
    }
    'isKnownRoot(byte[32])bool': {
      _root: Uint8Array
    }
    'getLastRoot()byte[32]': Record<string, never>
    'zeros(uint256)byte[32]': {
      i: bigint | number
    }
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    '__postInit(address,address,uint256,uint64)void': [_verifier: string, _hasher: string, _denomination: bigint | number, _merkleTreeHeight: bigint | number]
    'verifier()address': []
    'denomination()uint256': []
    'nullifierHashes(byte[32])bool': [key0: Uint8Array]
    'commitments(byte[32])bool': [key0: Uint8Array]
    'FIELD_SIZE()uint256': []
    'ZERO_VALUE()uint256': []
    'hasher()address': []
    'levels()uint64': []
    'filledSubtrees(uint256)byte[32]': [key0: bigint | number]
    'roots(uint256)byte[32]': [key0: bigint | number]
    'ROOT_HISTORY_SIZE()uint64': []
    'currentRootIndex()uint64': []
    'nextIndex()uint64': []
    'deposit(byte[32])void': [_commitment: Uint8Array]
    'withdraw(byte[],byte[32],byte[32],address,address,uint256,uint256)void': [_proof: Uint8Array, _root: Uint8Array, _nullifierHash: Uint8Array, _recipient: string, _relayer: string, _fee: bigint | number, _refund: bigint | number]
    'isSpent(byte[32])bool': [_nullifierHash: Uint8Array]
    'hashLeftRight(address,byte[32],byte[32])byte[32]': [_hasher: string, _left: Uint8Array, _right: Uint8Array]
    'isKnownRoot(byte[32])bool': [_root: Uint8Array]
    'getLastRoot()byte[32]': []
    'zeros(uint256)byte[32]': [i: bigint | number]
  }
}

/**
 * The return type for each method
 */
export type EthTornadoReturns = {
  '__postInit(address,address,uint256,uint64)void': void
  'verifier()address': string
  'denomination()uint256': bigint
  'nullifierHashes(byte[32])bool': boolean
  'commitments(byte[32])bool': boolean
  'FIELD_SIZE()uint256': bigint
  'ZERO_VALUE()uint256': bigint
  'hasher()address': string
  'levels()uint64': bigint
  'filledSubtrees(uint256)byte[32]': Uint8Array
  'roots(uint256)byte[32]': Uint8Array
  'ROOT_HISTORY_SIZE()uint64': bigint
  'currentRootIndex()uint64': bigint
  'nextIndex()uint64': bigint
  'deposit(byte[32])void': void
  'withdraw(byte[],byte[32],byte[32],address,address,uint256,uint256)void': void
  'isSpent(byte[32])bool': boolean
  'hashLeftRight(address,byte[32],byte[32])byte[32]': Uint8Array
  'isKnownRoot(byte[32])bool': boolean
  'getLastRoot()byte[32]': Uint8Array
  'zeros(uint256)byte[32]': Uint8Array
}

/**
 * Defines the types of available calls and state of the EthTornado smart contract.
 */
export type EthTornadoTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'__postInit(address,address,uint256,uint64)void' | '__postInit', {
      argsObj: EthTornadoArgs['obj']['__postInit(address,address,uint256,uint64)void']
      argsTuple: EthTornadoArgs['tuple']['__postInit(address,address,uint256,uint64)void']
      returns: EthTornadoReturns['__postInit(address,address,uint256,uint64)void']
    }>
    & Record<'verifier()address' | 'verifier', {
      argsObj: EthTornadoArgs['obj']['verifier()address']
      argsTuple: EthTornadoArgs['tuple']['verifier()address']
      returns: EthTornadoReturns['verifier()address']
    }>
    & Record<'denomination()uint256' | 'denomination', {
      argsObj: EthTornadoArgs['obj']['denomination()uint256']
      argsTuple: EthTornadoArgs['tuple']['denomination()uint256']
      returns: EthTornadoReturns['denomination()uint256']
    }>
    & Record<'nullifierHashes(byte[32])bool' | 'nullifierHashes', {
      argsObj: EthTornadoArgs['obj']['nullifierHashes(byte[32])bool']
      argsTuple: EthTornadoArgs['tuple']['nullifierHashes(byte[32])bool']
      returns: EthTornadoReturns['nullifierHashes(byte[32])bool']
    }>
    & Record<'commitments(byte[32])bool' | 'commitments', {
      argsObj: EthTornadoArgs['obj']['commitments(byte[32])bool']
      argsTuple: EthTornadoArgs['tuple']['commitments(byte[32])bool']
      returns: EthTornadoReturns['commitments(byte[32])bool']
    }>
    & Record<'FIELD_SIZE()uint256' | 'FIELD_SIZE', {
      argsObj: EthTornadoArgs['obj']['FIELD_SIZE()uint256']
      argsTuple: EthTornadoArgs['tuple']['FIELD_SIZE()uint256']
      returns: EthTornadoReturns['FIELD_SIZE()uint256']
    }>
    & Record<'ZERO_VALUE()uint256' | 'ZERO_VALUE', {
      argsObj: EthTornadoArgs['obj']['ZERO_VALUE()uint256']
      argsTuple: EthTornadoArgs['tuple']['ZERO_VALUE()uint256']
      returns: EthTornadoReturns['ZERO_VALUE()uint256']
    }>
    & Record<'hasher()address' | 'hasher', {
      argsObj: EthTornadoArgs['obj']['hasher()address']
      argsTuple: EthTornadoArgs['tuple']['hasher()address']
      returns: EthTornadoReturns['hasher()address']
    }>
    & Record<'levels()uint64' | 'levels', {
      argsObj: EthTornadoArgs['obj']['levels()uint64']
      argsTuple: EthTornadoArgs['tuple']['levels()uint64']
      returns: EthTornadoReturns['levels()uint64']
    }>
    & Record<'filledSubtrees(uint256)byte[32]' | 'filledSubtrees', {
      argsObj: EthTornadoArgs['obj']['filledSubtrees(uint256)byte[32]']
      argsTuple: EthTornadoArgs['tuple']['filledSubtrees(uint256)byte[32]']
      returns: EthTornadoReturns['filledSubtrees(uint256)byte[32]']
    }>
    & Record<'roots(uint256)byte[32]' | 'roots', {
      argsObj: EthTornadoArgs['obj']['roots(uint256)byte[32]']
      argsTuple: EthTornadoArgs['tuple']['roots(uint256)byte[32]']
      returns: EthTornadoReturns['roots(uint256)byte[32]']
    }>
    & Record<'ROOT_HISTORY_SIZE()uint64' | 'ROOT_HISTORY_SIZE', {
      argsObj: EthTornadoArgs['obj']['ROOT_HISTORY_SIZE()uint64']
      argsTuple: EthTornadoArgs['tuple']['ROOT_HISTORY_SIZE()uint64']
      returns: EthTornadoReturns['ROOT_HISTORY_SIZE()uint64']
    }>
    & Record<'currentRootIndex()uint64' | 'currentRootIndex', {
      argsObj: EthTornadoArgs['obj']['currentRootIndex()uint64']
      argsTuple: EthTornadoArgs['tuple']['currentRootIndex()uint64']
      returns: EthTornadoReturns['currentRootIndex()uint64']
    }>
    & Record<'nextIndex()uint64' | 'nextIndex', {
      argsObj: EthTornadoArgs['obj']['nextIndex()uint64']
      argsTuple: EthTornadoArgs['tuple']['nextIndex()uint64']
      returns: EthTornadoReturns['nextIndex()uint64']
    }>
    & Record<'deposit(byte[32])void' | 'deposit', {
      argsObj: EthTornadoArgs['obj']['deposit(byte[32])void']
      argsTuple: EthTornadoArgs['tuple']['deposit(byte[32])void']
      returns: EthTornadoReturns['deposit(byte[32])void']
    }>
    & Record<'withdraw(byte[],byte[32],byte[32],address,address,uint256,uint256)void' | 'withdraw', {
      argsObj: EthTornadoArgs['obj']['withdraw(byte[],byte[32],byte[32],address,address,uint256,uint256)void']
      argsTuple: EthTornadoArgs['tuple']['withdraw(byte[],byte[32],byte[32],address,address,uint256,uint256)void']
      returns: EthTornadoReturns['withdraw(byte[],byte[32],byte[32],address,address,uint256,uint256)void']
    }>
    & Record<'isSpent(byte[32])bool' | 'isSpent', {
      argsObj: EthTornadoArgs['obj']['isSpent(byte[32])bool']
      argsTuple: EthTornadoArgs['tuple']['isSpent(byte[32])bool']
      returns: EthTornadoReturns['isSpent(byte[32])bool']
    }>
    & Record<'hashLeftRight(address,byte[32],byte[32])byte[32]' | 'hashLeftRight', {
      argsObj: EthTornadoArgs['obj']['hashLeftRight(address,byte[32],byte[32])byte[32]']
      argsTuple: EthTornadoArgs['tuple']['hashLeftRight(address,byte[32],byte[32])byte[32]']
      returns: EthTornadoReturns['hashLeftRight(address,byte[32],byte[32])byte[32]']
    }>
    & Record<'isKnownRoot(byte[32])bool' | 'isKnownRoot', {
      argsObj: EthTornadoArgs['obj']['isKnownRoot(byte[32])bool']
      argsTuple: EthTornadoArgs['tuple']['isKnownRoot(byte[32])bool']
      returns: EthTornadoReturns['isKnownRoot(byte[32])bool']
    }>
    & Record<'getLastRoot()byte[32]' | 'getLastRoot', {
      argsObj: EthTornadoArgs['obj']['getLastRoot()byte[32]']
      argsTuple: EthTornadoArgs['tuple']['getLastRoot()byte[32]']
      returns: EthTornadoReturns['getLastRoot()byte[32]']
    }>
    & Record<'zeros(uint256)byte[32]' | 'zeros', {
      argsObj: EthTornadoArgs['obj']['zeros(uint256)byte[32]']
      argsTuple: EthTornadoArgs['tuple']['zeros(uint256)byte[32]']
      returns: EthTornadoReturns['zeros(uint256)byte[32]']
    }>
  /**
   * Defines the shape of the state of the application.
   */
  state: {
    global: {
      keys: {
        verifier: string
        denomination: BinaryState
        _status: BinaryState
        hasher: string
        levels: bigint
        currentRootIndex: bigint
        nextIndex: bigint
        __ctorPending: bigint
      }
      maps: {}
    }
    box: {
      keys: {}
      maps: {
        nullifierHashes: Map<Uint8Array | string, bigint>
        commitments: Map<Uint8Array | string, bigint>
        filledSubtrees: Map<Uint8Array | string, Uint8Array>
        roots: Map<Uint8Array | string, Uint8Array>
      }
    }
  }
}

/**
 * Defines the possible abi call signatures.
 */
export type EthTornadoSignatures = keyof EthTornadoTypes['methods']
/**
 * Defines the possible abi call signatures for methods that return a non-void value.
 */
export type EthTornadoNonVoidMethodSignatures = keyof EthTornadoTypes['methods'] extends infer T ? T extends keyof EthTornadoTypes['methods'] ? MethodReturn<T> extends void ? never : T  : never : never
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the EthTornado smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends EthTornadoSignatures> = EthTornadoTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the EthTornado smart contract to the method's return type
 */
export type MethodReturn<TSignature extends EthTornadoSignatures> = EthTornadoTypes['methods'][TSignature]['returns']

/**
 * Defines the shape of the keyed global state of the application.
 */
export type GlobalKeysState = EthTornadoTypes['state']['global']['keys']

/**
 * Defines the shape of the keyed box state of the application.
 */
export type BoxKeysState = EthTornadoTypes['state']['box']['keys']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the EthTornado smart contract
 */
export abstract class EthTornadoParamsFactory {
  /**
   * Constructs a no op call for the __postInit(address,address,uint256,uint64)void ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static __postInit(params: CallParams<EthTornadoArgs['obj']['__postInit(address,address,uint256,uint64)void'] | EthTornadoArgs['tuple']['__postInit(address,address,uint256,uint64)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: '__postInit(address,address,uint256,uint64)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args._verifier, params.args._hasher, params.args._denomination, params.args._merkleTreeHeight],
    }
  }
  /**
   * Constructs a no op call for the verifier()address ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static verifier(params: CallParams<EthTornadoArgs['obj']['verifier()address'] | EthTornadoArgs['tuple']['verifier()address']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'verifier()address' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the denomination()uint256 ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static denomination(params: CallParams<EthTornadoArgs['obj']['denomination()uint256'] | EthTornadoArgs['tuple']['denomination()uint256']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'denomination()uint256' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the nullifierHashes(byte[32])bool ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static nullifierHashes(params: CallParams<EthTornadoArgs['obj']['nullifierHashes(byte[32])bool'] | EthTornadoArgs['tuple']['nullifierHashes(byte[32])bool']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'nullifierHashes(byte[32])bool' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.key0],
    }
  }
  /**
   * Constructs a no op call for the commitments(byte[32])bool ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static commitments(params: CallParams<EthTornadoArgs['obj']['commitments(byte[32])bool'] | EthTornadoArgs['tuple']['commitments(byte[32])bool']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'commitments(byte[32])bool' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.key0],
    }
  }
  /**
   * Constructs a no op call for the FIELD_SIZE()uint256 ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static fieldSize(params: CallParams<EthTornadoArgs['obj']['FIELD_SIZE()uint256'] | EthTornadoArgs['tuple']['FIELD_SIZE()uint256']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'FIELD_SIZE()uint256' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the ZERO_VALUE()uint256 ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static zeroValue(params: CallParams<EthTornadoArgs['obj']['ZERO_VALUE()uint256'] | EthTornadoArgs['tuple']['ZERO_VALUE()uint256']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'ZERO_VALUE()uint256' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the hasher()address ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static hasher(params: CallParams<EthTornadoArgs['obj']['hasher()address'] | EthTornadoArgs['tuple']['hasher()address']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'hasher()address' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the levels()uint64 ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static levels(params: CallParams<EthTornadoArgs['obj']['levels()uint64'] | EthTornadoArgs['tuple']['levels()uint64']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'levels()uint64' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the filledSubtrees(uint256)byte[32] ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static filledSubtrees(params: CallParams<EthTornadoArgs['obj']['filledSubtrees(uint256)byte[32]'] | EthTornadoArgs['tuple']['filledSubtrees(uint256)byte[32]']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'filledSubtrees(uint256)byte[32]' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.key0],
    }
  }
  /**
   * Constructs a no op call for the roots(uint256)byte[32] ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static roots(params: CallParams<EthTornadoArgs['obj']['roots(uint256)byte[32]'] | EthTornadoArgs['tuple']['roots(uint256)byte[32]']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'roots(uint256)byte[32]' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.key0],
    }
  }
  /**
   * Constructs a no op call for the ROOT_HISTORY_SIZE()uint64 ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static rootHistorySize(params: CallParams<EthTornadoArgs['obj']['ROOT_HISTORY_SIZE()uint64'] | EthTornadoArgs['tuple']['ROOT_HISTORY_SIZE()uint64']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'ROOT_HISTORY_SIZE()uint64' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the currentRootIndex()uint64 ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static currentRootIndex(params: CallParams<EthTornadoArgs['obj']['currentRootIndex()uint64'] | EthTornadoArgs['tuple']['currentRootIndex()uint64']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'currentRootIndex()uint64' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the nextIndex()uint64 ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static nextIndex(params: CallParams<EthTornadoArgs['obj']['nextIndex()uint64'] | EthTornadoArgs['tuple']['nextIndex()uint64']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'nextIndex()uint64' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the deposit(byte[32])void ABI method
   *
  * @dev Deposit funds into the contract. The caller must send (for ETH) or approve (for ERC20) value equal to or `denomination` of this instance.
  @param _commitment the note commitment, which is PedersenHash(nullifier + secret)

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static deposit(params: CallParams<EthTornadoArgs['obj']['deposit(byte[32])void'] | EthTornadoArgs['tuple']['deposit(byte[32])void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'deposit(byte[32])void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args._commitment],
    }
  }
  /**
   * Constructs a no op call for the withdraw(byte[],byte[32],byte[32],address,address,uint256,uint256)void ABI method
   *
  * @dev Withdraw a deposit from the contract. `proof` is a zkSNARK proof data, and input is an array of circuit public inputs
  `input` array consists of:
  - merkle root of all deposits in the contract
  - hash of unique deposit nullifier to prevent double spends
  - the recipient of funds
  - optional fee that goes to the transaction sender (usually a relay)

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static withdraw(params: CallParams<EthTornadoArgs['obj']['withdraw(byte[],byte[32],byte[32],address,address,uint256,uint256)void'] | EthTornadoArgs['tuple']['withdraw(byte[],byte[32],byte[32],address,address,uint256,uint256)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'withdraw(byte[],byte[32],byte[32],address,address,uint256,uint256)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args._proof, params.args._root, params.args._nullifierHash, params.args._recipient, params.args._relayer, params.args._fee, params.args._refund],
    }
  }
  /**
   * Constructs a no op call for the isSpent(byte[32])bool ABI method
   *
   * @dev whether a note is already spent 
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static isSpent(params: CallParams<EthTornadoArgs['obj']['isSpent(byte[32])bool'] | EthTornadoArgs['tuple']['isSpent(byte[32])bool']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'isSpent(byte[32])bool' as const,
      args: Array.isArray(params.args) ? params.args : [params.args._nullifierHash],
    }
  }
  /**
   * Constructs a no op call for the hashLeftRight(address,byte[32],byte[32])byte[32] ABI method
   *
   * @dev Hash 2 tree leaves, returns MiMC(_left, _right)
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static hashLeftRight(params: CallParams<EthTornadoArgs['obj']['hashLeftRight(address,byte[32],byte[32])byte[32]'] | EthTornadoArgs['tuple']['hashLeftRight(address,byte[32],byte[32])byte[32]']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'hashLeftRight(address,byte[32],byte[32])byte[32]' as const,
      args: Array.isArray(params.args) ? params.args : [params.args._hasher, params.args._left, params.args._right],
    }
  }
  /**
   * Constructs a no op call for the isKnownRoot(byte[32])bool ABI method
   *
   * @dev Whether the root is present in the root history
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static isKnownRoot(params: CallParams<EthTornadoArgs['obj']['isKnownRoot(byte[32])bool'] | EthTornadoArgs['tuple']['isKnownRoot(byte[32])bool']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'isKnownRoot(byte[32])bool' as const,
      args: Array.isArray(params.args) ? params.args : [params.args._root],
    }
  }
  /**
   * Constructs a no op call for the getLastRoot()byte[32] ABI method
   *
   * @dev Returns the last root
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static getLastRoot(params: CallParams<EthTornadoArgs['obj']['getLastRoot()byte[32]'] | EthTornadoArgs['tuple']['getLastRoot()byte[32]']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'getLastRoot()byte[32]' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the zeros(uint256)byte[32] ABI method
   *
   * @dev provides Zero (Empty) elements for a MiMC MerkleTree. Up to 32 levels
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static zeros(params: CallParams<EthTornadoArgs['obj']['zeros(uint256)byte[32]'] | EthTornadoArgs['tuple']['zeros(uint256)byte[32]']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'zeros(uint256)byte[32]' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.i],
    }
  }
}

/**
 * A client to make calls to the ETHTornado smart contract
 */
export class EthTornadoClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `EthTornadoClient`
   *
   * @param appClient An `AppClient` instance which has been created with the EthTornado app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `EthTornadoClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Checks for decode errors on the given return value and maps the return value to the return type for the given method
   * @returns The typed return value or undefined if there was no value
   */
  decodeReturnValue<TSignature extends EthTornadoNonVoidMethodSignatures>(method: TSignature, returnValue: ABIReturn | undefined) {
    return returnValue !== undefined ? getArc56ReturnValue<MethodReturn<TSignature>>(returnValue, this.appClient.getABIMethod(method), APP_SPEC.structs) : undefined
  }

  /**
   * Returns a new `EthTornadoClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<EthTornadoClient> {
    return new EthTornadoClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `EthTornadoClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<EthTornadoClient> {
    return new EthTornadoClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Makes a clear_state call to an existing instance of the ETHTornado smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the ETHTornado smart contract using the `__postInit(address,address,uint256,uint64)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    __postInit: (params: CallParams<EthTornadoArgs['obj']['__postInit(address,address,uint256,uint64)void'] | EthTornadoArgs['tuple']['__postInit(address,address,uint256,uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(EthTornadoParamsFactory.__postInit(params))
    },

    /**
     * Makes a call to the ETHTornado smart contract using the `verifier()address` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    verifier: (params: CallParams<EthTornadoArgs['obj']['verifier()address'] | EthTornadoArgs['tuple']['verifier()address']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(EthTornadoParamsFactory.verifier(params))
    },

    /**
     * Makes a call to the ETHTornado smart contract using the `denomination()uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    denomination: (params: CallParams<EthTornadoArgs['obj']['denomination()uint256'] | EthTornadoArgs['tuple']['denomination()uint256']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(EthTornadoParamsFactory.denomination(params))
    },

    /**
     * Makes a call to the ETHTornado smart contract using the `nullifierHashes(byte[32])bool` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    nullifierHashes: (params: CallParams<EthTornadoArgs['obj']['nullifierHashes(byte[32])bool'] | EthTornadoArgs['tuple']['nullifierHashes(byte[32])bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(EthTornadoParamsFactory.nullifierHashes(params))
    },

    /**
     * Makes a call to the ETHTornado smart contract using the `commitments(byte[32])bool` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    commitments: (params: CallParams<EthTornadoArgs['obj']['commitments(byte[32])bool'] | EthTornadoArgs['tuple']['commitments(byte[32])bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(EthTornadoParamsFactory.commitments(params))
    },

    /**
     * Makes a call to the ETHTornado smart contract using the `FIELD_SIZE()uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    fieldSize: (params: CallParams<EthTornadoArgs['obj']['FIELD_SIZE()uint256'] | EthTornadoArgs['tuple']['FIELD_SIZE()uint256']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(EthTornadoParamsFactory.fieldSize(params))
    },

    /**
     * Makes a call to the ETHTornado smart contract using the `ZERO_VALUE()uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    zeroValue: (params: CallParams<EthTornadoArgs['obj']['ZERO_VALUE()uint256'] | EthTornadoArgs['tuple']['ZERO_VALUE()uint256']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(EthTornadoParamsFactory.zeroValue(params))
    },

    /**
     * Makes a call to the ETHTornado smart contract using the `hasher()address` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    hasher: (params: CallParams<EthTornadoArgs['obj']['hasher()address'] | EthTornadoArgs['tuple']['hasher()address']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(EthTornadoParamsFactory.hasher(params))
    },

    /**
     * Makes a call to the ETHTornado smart contract using the `levels()uint64` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    levels: (params: CallParams<EthTornadoArgs['obj']['levels()uint64'] | EthTornadoArgs['tuple']['levels()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(EthTornadoParamsFactory.levels(params))
    },

    /**
     * Makes a call to the ETHTornado smart contract using the `filledSubtrees(uint256)byte[32]` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    filledSubtrees: (params: CallParams<EthTornadoArgs['obj']['filledSubtrees(uint256)byte[32]'] | EthTornadoArgs['tuple']['filledSubtrees(uint256)byte[32]']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(EthTornadoParamsFactory.filledSubtrees(params))
    },

    /**
     * Makes a call to the ETHTornado smart contract using the `roots(uint256)byte[32]` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    roots: (params: CallParams<EthTornadoArgs['obj']['roots(uint256)byte[32]'] | EthTornadoArgs['tuple']['roots(uint256)byte[32]']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(EthTornadoParamsFactory.roots(params))
    },

    /**
     * Makes a call to the ETHTornado smart contract using the `ROOT_HISTORY_SIZE()uint64` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    rootHistorySize: (params: CallParams<EthTornadoArgs['obj']['ROOT_HISTORY_SIZE()uint64'] | EthTornadoArgs['tuple']['ROOT_HISTORY_SIZE()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(EthTornadoParamsFactory.rootHistorySize(params))
    },

    /**
     * Makes a call to the ETHTornado smart contract using the `currentRootIndex()uint64` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    currentRootIndex: (params: CallParams<EthTornadoArgs['obj']['currentRootIndex()uint64'] | EthTornadoArgs['tuple']['currentRootIndex()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(EthTornadoParamsFactory.currentRootIndex(params))
    },

    /**
     * Makes a call to the ETHTornado smart contract using the `nextIndex()uint64` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    nextIndex: (params: CallParams<EthTornadoArgs['obj']['nextIndex()uint64'] | EthTornadoArgs['tuple']['nextIndex()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(EthTornadoParamsFactory.nextIndex(params))
    },

    /**
     * Makes a call to the ETHTornado smart contract using the `deposit(byte[32])void` ABI method.
     *
    * @dev Deposit funds into the contract. The caller must send (for ETH) or approve (for ERC20) value equal to or `denomination` of this instance.
    @param _commitment the note commitment, which is PedersenHash(nullifier + secret)

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    deposit: (params: CallParams<EthTornadoArgs['obj']['deposit(byte[32])void'] | EthTornadoArgs['tuple']['deposit(byte[32])void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(EthTornadoParamsFactory.deposit(params))
    },

    /**
     * Makes a call to the ETHTornado smart contract using the `withdraw(byte[],byte[32],byte[32],address,address,uint256,uint256)void` ABI method.
     *
    * @dev Withdraw a deposit from the contract. `proof` is a zkSNARK proof data, and input is an array of circuit public inputs
    `input` array consists of:
    - merkle root of all deposits in the contract
    - hash of unique deposit nullifier to prevent double spends
    - the recipient of funds
    - optional fee that goes to the transaction sender (usually a relay)

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    withdraw: (params: CallParams<EthTornadoArgs['obj']['withdraw(byte[],byte[32],byte[32],address,address,uint256,uint256)void'] | EthTornadoArgs['tuple']['withdraw(byte[],byte[32],byte[32],address,address,uint256,uint256)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(EthTornadoParamsFactory.withdraw(params))
    },

    /**
     * Makes a call to the ETHTornado smart contract using the `isSpent(byte[32])bool` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @dev whether a note is already spent 
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    isSpent: (params: CallParams<EthTornadoArgs['obj']['isSpent(byte[32])bool'] | EthTornadoArgs['tuple']['isSpent(byte[32])bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(EthTornadoParamsFactory.isSpent(params))
    },

    /**
     * Makes a call to the ETHTornado smart contract using the `hashLeftRight(address,byte[32],byte[32])byte[32]` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @dev Hash 2 tree leaves, returns MiMC(_left, _right)
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    hashLeftRight: (params: CallParams<EthTornadoArgs['obj']['hashLeftRight(address,byte[32],byte[32])byte[32]'] | EthTornadoArgs['tuple']['hashLeftRight(address,byte[32],byte[32])byte[32]']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(EthTornadoParamsFactory.hashLeftRight(params))
    },

    /**
     * Makes a call to the ETHTornado smart contract using the `isKnownRoot(byte[32])bool` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @dev Whether the root is present in the root history
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    isKnownRoot: (params: CallParams<EthTornadoArgs['obj']['isKnownRoot(byte[32])bool'] | EthTornadoArgs['tuple']['isKnownRoot(byte[32])bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(EthTornadoParamsFactory.isKnownRoot(params))
    },

    /**
     * Makes a call to the ETHTornado smart contract using the `getLastRoot()byte[32]` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @dev Returns the last root
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    getLastRoot: (params: CallParams<EthTornadoArgs['obj']['getLastRoot()byte[32]'] | EthTornadoArgs['tuple']['getLastRoot()byte[32]']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(EthTornadoParamsFactory.getLastRoot(params))
    },

    /**
     * Makes a call to the ETHTornado smart contract using the `zeros(uint256)byte[32]` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @dev provides Zero (Empty) elements for a MiMC MerkleTree. Up to 32 levels
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    zeros: (params: CallParams<EthTornadoArgs['obj']['zeros(uint256)byte[32]'] | EthTornadoArgs['tuple']['zeros(uint256)byte[32]']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(EthTornadoParamsFactory.zeros(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Makes a clear_state call to an existing instance of the ETHTornado smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the ETHTornado smart contract using the `__postInit(address,address,uint256,uint64)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    __postInit: (params: CallParams<EthTornadoArgs['obj']['__postInit(address,address,uint256,uint64)void'] | EthTornadoArgs['tuple']['__postInit(address,address,uint256,uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(EthTornadoParamsFactory.__postInit(params))
    },

    /**
     * Makes a call to the ETHTornado smart contract using the `verifier()address` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    verifier: (params: CallParams<EthTornadoArgs['obj']['verifier()address'] | EthTornadoArgs['tuple']['verifier()address']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(EthTornadoParamsFactory.verifier(params))
    },

    /**
     * Makes a call to the ETHTornado smart contract using the `denomination()uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    denomination: (params: CallParams<EthTornadoArgs['obj']['denomination()uint256'] | EthTornadoArgs['tuple']['denomination()uint256']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(EthTornadoParamsFactory.denomination(params))
    },

    /**
     * Makes a call to the ETHTornado smart contract using the `nullifierHashes(byte[32])bool` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    nullifierHashes: (params: CallParams<EthTornadoArgs['obj']['nullifierHashes(byte[32])bool'] | EthTornadoArgs['tuple']['nullifierHashes(byte[32])bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(EthTornadoParamsFactory.nullifierHashes(params))
    },

    /**
     * Makes a call to the ETHTornado smart contract using the `commitments(byte[32])bool` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    commitments: (params: CallParams<EthTornadoArgs['obj']['commitments(byte[32])bool'] | EthTornadoArgs['tuple']['commitments(byte[32])bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(EthTornadoParamsFactory.commitments(params))
    },

    /**
     * Makes a call to the ETHTornado smart contract using the `FIELD_SIZE()uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    fieldSize: (params: CallParams<EthTornadoArgs['obj']['FIELD_SIZE()uint256'] | EthTornadoArgs['tuple']['FIELD_SIZE()uint256']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(EthTornadoParamsFactory.fieldSize(params))
    },

    /**
     * Makes a call to the ETHTornado smart contract using the `ZERO_VALUE()uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    zeroValue: (params: CallParams<EthTornadoArgs['obj']['ZERO_VALUE()uint256'] | EthTornadoArgs['tuple']['ZERO_VALUE()uint256']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(EthTornadoParamsFactory.zeroValue(params))
    },

    /**
     * Makes a call to the ETHTornado smart contract using the `hasher()address` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    hasher: (params: CallParams<EthTornadoArgs['obj']['hasher()address'] | EthTornadoArgs['tuple']['hasher()address']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(EthTornadoParamsFactory.hasher(params))
    },

    /**
     * Makes a call to the ETHTornado smart contract using the `levels()uint64` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    levels: (params: CallParams<EthTornadoArgs['obj']['levels()uint64'] | EthTornadoArgs['tuple']['levels()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(EthTornadoParamsFactory.levels(params))
    },

    /**
     * Makes a call to the ETHTornado smart contract using the `filledSubtrees(uint256)byte[32]` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    filledSubtrees: (params: CallParams<EthTornadoArgs['obj']['filledSubtrees(uint256)byte[32]'] | EthTornadoArgs['tuple']['filledSubtrees(uint256)byte[32]']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(EthTornadoParamsFactory.filledSubtrees(params))
    },

    /**
     * Makes a call to the ETHTornado smart contract using the `roots(uint256)byte[32]` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    roots: (params: CallParams<EthTornadoArgs['obj']['roots(uint256)byte[32]'] | EthTornadoArgs['tuple']['roots(uint256)byte[32]']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(EthTornadoParamsFactory.roots(params))
    },

    /**
     * Makes a call to the ETHTornado smart contract using the `ROOT_HISTORY_SIZE()uint64` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    rootHistorySize: (params: CallParams<EthTornadoArgs['obj']['ROOT_HISTORY_SIZE()uint64'] | EthTornadoArgs['tuple']['ROOT_HISTORY_SIZE()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(EthTornadoParamsFactory.rootHistorySize(params))
    },

    /**
     * Makes a call to the ETHTornado smart contract using the `currentRootIndex()uint64` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    currentRootIndex: (params: CallParams<EthTornadoArgs['obj']['currentRootIndex()uint64'] | EthTornadoArgs['tuple']['currentRootIndex()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(EthTornadoParamsFactory.currentRootIndex(params))
    },

    /**
     * Makes a call to the ETHTornado smart contract using the `nextIndex()uint64` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    nextIndex: (params: CallParams<EthTornadoArgs['obj']['nextIndex()uint64'] | EthTornadoArgs['tuple']['nextIndex()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(EthTornadoParamsFactory.nextIndex(params))
    },

    /**
     * Makes a call to the ETHTornado smart contract using the `deposit(byte[32])void` ABI method.
     *
    * @dev Deposit funds into the contract. The caller must send (for ETH) or approve (for ERC20) value equal to or `denomination` of this instance.
    @param _commitment the note commitment, which is PedersenHash(nullifier + secret)

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    deposit: (params: CallParams<EthTornadoArgs['obj']['deposit(byte[32])void'] | EthTornadoArgs['tuple']['deposit(byte[32])void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(EthTornadoParamsFactory.deposit(params))
    },

    /**
     * Makes a call to the ETHTornado smart contract using the `withdraw(byte[],byte[32],byte[32],address,address,uint256,uint256)void` ABI method.
     *
    * @dev Withdraw a deposit from the contract. `proof` is a zkSNARK proof data, and input is an array of circuit public inputs
    `input` array consists of:
    - merkle root of all deposits in the contract
    - hash of unique deposit nullifier to prevent double spends
    - the recipient of funds
    - optional fee that goes to the transaction sender (usually a relay)

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    withdraw: (params: CallParams<EthTornadoArgs['obj']['withdraw(byte[],byte[32],byte[32],address,address,uint256,uint256)void'] | EthTornadoArgs['tuple']['withdraw(byte[],byte[32],byte[32],address,address,uint256,uint256)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(EthTornadoParamsFactory.withdraw(params))
    },

    /**
     * Makes a call to the ETHTornado smart contract using the `isSpent(byte[32])bool` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @dev whether a note is already spent 
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    isSpent: (params: CallParams<EthTornadoArgs['obj']['isSpent(byte[32])bool'] | EthTornadoArgs['tuple']['isSpent(byte[32])bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(EthTornadoParamsFactory.isSpent(params))
    },

    /**
     * Makes a call to the ETHTornado smart contract using the `hashLeftRight(address,byte[32],byte[32])byte[32]` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @dev Hash 2 tree leaves, returns MiMC(_left, _right)
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    hashLeftRight: (params: CallParams<EthTornadoArgs['obj']['hashLeftRight(address,byte[32],byte[32])byte[32]'] | EthTornadoArgs['tuple']['hashLeftRight(address,byte[32],byte[32])byte[32]']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(EthTornadoParamsFactory.hashLeftRight(params))
    },

    /**
     * Makes a call to the ETHTornado smart contract using the `isKnownRoot(byte[32])bool` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @dev Whether the root is present in the root history
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    isKnownRoot: (params: CallParams<EthTornadoArgs['obj']['isKnownRoot(byte[32])bool'] | EthTornadoArgs['tuple']['isKnownRoot(byte[32])bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(EthTornadoParamsFactory.isKnownRoot(params))
    },

    /**
     * Makes a call to the ETHTornado smart contract using the `getLastRoot()byte[32]` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @dev Returns the last root
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    getLastRoot: (params: CallParams<EthTornadoArgs['obj']['getLastRoot()byte[32]'] | EthTornadoArgs['tuple']['getLastRoot()byte[32]']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(EthTornadoParamsFactory.getLastRoot(params))
    },

    /**
     * Makes a call to the ETHTornado smart contract using the `zeros(uint256)byte[32]` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @dev provides Zero (Empty) elements for a MiMC MerkleTree. Up to 32 levels
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    zeros: (params: CallParams<EthTornadoArgs['obj']['zeros(uint256)byte[32]'] | EthTornadoArgs['tuple']['zeros(uint256)byte[32]']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(EthTornadoParamsFactory.zeros(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Makes a clear_state call to an existing instance of the ETHTornado smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the ETHTornado smart contract using the `__postInit(address,address,uint256,uint64)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    __postInit: async (params: CallParams<EthTornadoArgs['obj']['__postInit(address,address,uint256,uint64)void'] | EthTornadoArgs['tuple']['__postInit(address,address,uint256,uint64)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(EthTornadoParamsFactory.__postInit(params))
      return {...result, return: result.return as unknown as (undefined | EthTornadoReturns['__postInit(address,address,uint256,uint64)void'])}
    },

    /**
     * Makes a call to the ETHTornado smart contract using the `verifier()address` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    verifier: async (params: CallParams<EthTornadoArgs['obj']['verifier()address'] | EthTornadoArgs['tuple']['verifier()address']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(EthTornadoParamsFactory.verifier(params))
      return {...result, return: result.return as unknown as (undefined | EthTornadoReturns['verifier()address'])}
    },

    /**
     * Makes a call to the ETHTornado smart contract using the `denomination()uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    denomination: async (params: CallParams<EthTornadoArgs['obj']['denomination()uint256'] | EthTornadoArgs['tuple']['denomination()uint256']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(EthTornadoParamsFactory.denomination(params))
      return {...result, return: result.return as unknown as (undefined | EthTornadoReturns['denomination()uint256'])}
    },

    /**
     * Makes a call to the ETHTornado smart contract using the `nullifierHashes(byte[32])bool` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    nullifierHashes: async (params: CallParams<EthTornadoArgs['obj']['nullifierHashes(byte[32])bool'] | EthTornadoArgs['tuple']['nullifierHashes(byte[32])bool']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(EthTornadoParamsFactory.nullifierHashes(params))
      return {...result, return: result.return as unknown as (undefined | EthTornadoReturns['nullifierHashes(byte[32])bool'])}
    },

    /**
     * Makes a call to the ETHTornado smart contract using the `commitments(byte[32])bool` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    commitments: async (params: CallParams<EthTornadoArgs['obj']['commitments(byte[32])bool'] | EthTornadoArgs['tuple']['commitments(byte[32])bool']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(EthTornadoParamsFactory.commitments(params))
      return {...result, return: result.return as unknown as (undefined | EthTornadoReturns['commitments(byte[32])bool'])}
    },

    /**
     * Makes a call to the ETHTornado smart contract using the `FIELD_SIZE()uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    fieldSize: async (params: CallParams<EthTornadoArgs['obj']['FIELD_SIZE()uint256'] | EthTornadoArgs['tuple']['FIELD_SIZE()uint256']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(EthTornadoParamsFactory.fieldSize(params))
      return {...result, return: result.return as unknown as (undefined | EthTornadoReturns['FIELD_SIZE()uint256'])}
    },

    /**
     * Makes a call to the ETHTornado smart contract using the `ZERO_VALUE()uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    zeroValue: async (params: CallParams<EthTornadoArgs['obj']['ZERO_VALUE()uint256'] | EthTornadoArgs['tuple']['ZERO_VALUE()uint256']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(EthTornadoParamsFactory.zeroValue(params))
      return {...result, return: result.return as unknown as (undefined | EthTornadoReturns['ZERO_VALUE()uint256'])}
    },

    /**
     * Makes a call to the ETHTornado smart contract using the `hasher()address` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    hasher: async (params: CallParams<EthTornadoArgs['obj']['hasher()address'] | EthTornadoArgs['tuple']['hasher()address']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(EthTornadoParamsFactory.hasher(params))
      return {...result, return: result.return as unknown as (undefined | EthTornadoReturns['hasher()address'])}
    },

    /**
     * Makes a call to the ETHTornado smart contract using the `levels()uint64` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    levels: async (params: CallParams<EthTornadoArgs['obj']['levels()uint64'] | EthTornadoArgs['tuple']['levels()uint64']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(EthTornadoParamsFactory.levels(params))
      return {...result, return: result.return as unknown as (undefined | EthTornadoReturns['levels()uint64'])}
    },

    /**
     * Makes a call to the ETHTornado smart contract using the `filledSubtrees(uint256)byte[32]` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    filledSubtrees: async (params: CallParams<EthTornadoArgs['obj']['filledSubtrees(uint256)byte[32]'] | EthTornadoArgs['tuple']['filledSubtrees(uint256)byte[32]']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(EthTornadoParamsFactory.filledSubtrees(params))
      return {...result, return: result.return as unknown as (undefined | EthTornadoReturns['filledSubtrees(uint256)byte[32]'])}
    },

    /**
     * Makes a call to the ETHTornado smart contract using the `roots(uint256)byte[32]` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    roots: async (params: CallParams<EthTornadoArgs['obj']['roots(uint256)byte[32]'] | EthTornadoArgs['tuple']['roots(uint256)byte[32]']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(EthTornadoParamsFactory.roots(params))
      return {...result, return: result.return as unknown as (undefined | EthTornadoReturns['roots(uint256)byte[32]'])}
    },

    /**
     * Makes a call to the ETHTornado smart contract using the `ROOT_HISTORY_SIZE()uint64` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    rootHistorySize: async (params: CallParams<EthTornadoArgs['obj']['ROOT_HISTORY_SIZE()uint64'] | EthTornadoArgs['tuple']['ROOT_HISTORY_SIZE()uint64']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(EthTornadoParamsFactory.rootHistorySize(params))
      return {...result, return: result.return as unknown as (undefined | EthTornadoReturns['ROOT_HISTORY_SIZE()uint64'])}
    },

    /**
     * Makes a call to the ETHTornado smart contract using the `currentRootIndex()uint64` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    currentRootIndex: async (params: CallParams<EthTornadoArgs['obj']['currentRootIndex()uint64'] | EthTornadoArgs['tuple']['currentRootIndex()uint64']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(EthTornadoParamsFactory.currentRootIndex(params))
      return {...result, return: result.return as unknown as (undefined | EthTornadoReturns['currentRootIndex()uint64'])}
    },

    /**
     * Makes a call to the ETHTornado smart contract using the `nextIndex()uint64` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    nextIndex: async (params: CallParams<EthTornadoArgs['obj']['nextIndex()uint64'] | EthTornadoArgs['tuple']['nextIndex()uint64']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(EthTornadoParamsFactory.nextIndex(params))
      return {...result, return: result.return as unknown as (undefined | EthTornadoReturns['nextIndex()uint64'])}
    },

    /**
     * Makes a call to the ETHTornado smart contract using the `deposit(byte[32])void` ABI method.
     *
    * @dev Deposit funds into the contract. The caller must send (for ETH) or approve (for ERC20) value equal to or `denomination` of this instance.
    @param _commitment the note commitment, which is PedersenHash(nullifier + secret)

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    deposit: async (params: CallParams<EthTornadoArgs['obj']['deposit(byte[32])void'] | EthTornadoArgs['tuple']['deposit(byte[32])void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(EthTornadoParamsFactory.deposit(params))
      return {...result, return: result.return as unknown as (undefined | EthTornadoReturns['deposit(byte[32])void'])}
    },

    /**
     * Makes a call to the ETHTornado smart contract using the `withdraw(byte[],byte[32],byte[32],address,address,uint256,uint256)void` ABI method.
     *
    * @dev Withdraw a deposit from the contract. `proof` is a zkSNARK proof data, and input is an array of circuit public inputs
    `input` array consists of:
    - merkle root of all deposits in the contract
    - hash of unique deposit nullifier to prevent double spends
    - the recipient of funds
    - optional fee that goes to the transaction sender (usually a relay)

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    withdraw: async (params: CallParams<EthTornadoArgs['obj']['withdraw(byte[],byte[32],byte[32],address,address,uint256,uint256)void'] | EthTornadoArgs['tuple']['withdraw(byte[],byte[32],byte[32],address,address,uint256,uint256)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(EthTornadoParamsFactory.withdraw(params))
      return {...result, return: result.return as unknown as (undefined | EthTornadoReturns['withdraw(byte[],byte[32],byte[32],address,address,uint256,uint256)void'])}
    },

    /**
     * Makes a call to the ETHTornado smart contract using the `isSpent(byte[32])bool` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @dev whether a note is already spent 
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    isSpent: async (params: CallParams<EthTornadoArgs['obj']['isSpent(byte[32])bool'] | EthTornadoArgs['tuple']['isSpent(byte[32])bool']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(EthTornadoParamsFactory.isSpent(params))
      return {...result, return: result.return as unknown as (undefined | EthTornadoReturns['isSpent(byte[32])bool'])}
    },

    /**
     * Makes a call to the ETHTornado smart contract using the `hashLeftRight(address,byte[32],byte[32])byte[32]` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @dev Hash 2 tree leaves, returns MiMC(_left, _right)
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    hashLeftRight: async (params: CallParams<EthTornadoArgs['obj']['hashLeftRight(address,byte[32],byte[32])byte[32]'] | EthTornadoArgs['tuple']['hashLeftRight(address,byte[32],byte[32])byte[32]']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(EthTornadoParamsFactory.hashLeftRight(params))
      return {...result, return: result.return as unknown as (undefined | EthTornadoReturns['hashLeftRight(address,byte[32],byte[32])byte[32]'])}
    },

    /**
     * Makes a call to the ETHTornado smart contract using the `isKnownRoot(byte[32])bool` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @dev Whether the root is present in the root history
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    isKnownRoot: async (params: CallParams<EthTornadoArgs['obj']['isKnownRoot(byte[32])bool'] | EthTornadoArgs['tuple']['isKnownRoot(byte[32])bool']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(EthTornadoParamsFactory.isKnownRoot(params))
      return {...result, return: result.return as unknown as (undefined | EthTornadoReturns['isKnownRoot(byte[32])bool'])}
    },

    /**
     * Makes a call to the ETHTornado smart contract using the `getLastRoot()byte[32]` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @dev Returns the last root
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    getLastRoot: async (params: CallParams<EthTornadoArgs['obj']['getLastRoot()byte[32]'] | EthTornadoArgs['tuple']['getLastRoot()byte[32]']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(EthTornadoParamsFactory.getLastRoot(params))
      return {...result, return: result.return as unknown as (undefined | EthTornadoReturns['getLastRoot()byte[32]'])}
    },

    /**
     * Makes a call to the ETHTornado smart contract using the `zeros(uint256)byte[32]` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @dev provides Zero (Empty) elements for a MiMC MerkleTree. Up to 32 levels
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    zeros: async (params: CallParams<EthTornadoArgs['obj']['zeros(uint256)byte[32]'] | EthTornadoArgs['tuple']['zeros(uint256)byte[32]']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(EthTornadoParamsFactory.zeros(params))
      return {...result, return: result.return as unknown as (undefined | EthTornadoReturns['zeros(uint256)byte[32]'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new EthTornadoClient(this.appClient.clone(params))
  }

  /**
   * Makes a readonly (simulated) call to the ETHTornado smart contract using the `verifier()address` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async verifier(params: CallParams<EthTornadoArgs['obj']['verifier()address'] | EthTornadoArgs['tuple']['verifier()address']> = {args: []}) {
    const result = await this.appClient.send.call(EthTornadoParamsFactory.verifier(params))
    return result.return as unknown as EthTornadoReturns['verifier()address']
  }

  /**
   * Makes a readonly (simulated) call to the ETHTornado smart contract using the `denomination()uint256` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async denomination(params: CallParams<EthTornadoArgs['obj']['denomination()uint256'] | EthTornadoArgs['tuple']['denomination()uint256']> = {args: []}) {
    const result = await this.appClient.send.call(EthTornadoParamsFactory.denomination(params))
    return result.return as unknown as EthTornadoReturns['denomination()uint256']
  }

  /**
   * Makes a readonly (simulated) call to the ETHTornado smart contract using the `nullifierHashes(byte[32])bool` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async nullifierHashes(params: CallParams<EthTornadoArgs['obj']['nullifierHashes(byte[32])bool'] | EthTornadoArgs['tuple']['nullifierHashes(byte[32])bool']>) {
    const result = await this.appClient.send.call(EthTornadoParamsFactory.nullifierHashes(params))
    return result.return as unknown as EthTornadoReturns['nullifierHashes(byte[32])bool']
  }

  /**
   * Makes a readonly (simulated) call to the ETHTornado smart contract using the `commitments(byte[32])bool` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async commitments(params: CallParams<EthTornadoArgs['obj']['commitments(byte[32])bool'] | EthTornadoArgs['tuple']['commitments(byte[32])bool']>) {
    const result = await this.appClient.send.call(EthTornadoParamsFactory.commitments(params))
    return result.return as unknown as EthTornadoReturns['commitments(byte[32])bool']
  }

  /**
   * Makes a readonly (simulated) call to the ETHTornado smart contract using the `FIELD_SIZE()uint256` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async fieldSize(params: CallParams<EthTornadoArgs['obj']['FIELD_SIZE()uint256'] | EthTornadoArgs['tuple']['FIELD_SIZE()uint256']> = {args: []}) {
    const result = await this.appClient.send.call(EthTornadoParamsFactory.fieldSize(params))
    return result.return as unknown as EthTornadoReturns['FIELD_SIZE()uint256']
  }

  /**
   * Makes a readonly (simulated) call to the ETHTornado smart contract using the `ZERO_VALUE()uint256` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async zeroValue(params: CallParams<EthTornadoArgs['obj']['ZERO_VALUE()uint256'] | EthTornadoArgs['tuple']['ZERO_VALUE()uint256']> = {args: []}) {
    const result = await this.appClient.send.call(EthTornadoParamsFactory.zeroValue(params))
    return result.return as unknown as EthTornadoReturns['ZERO_VALUE()uint256']
  }

  /**
   * Makes a readonly (simulated) call to the ETHTornado smart contract using the `hasher()address` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async hasher(params: CallParams<EthTornadoArgs['obj']['hasher()address'] | EthTornadoArgs['tuple']['hasher()address']> = {args: []}) {
    const result = await this.appClient.send.call(EthTornadoParamsFactory.hasher(params))
    return result.return as unknown as EthTornadoReturns['hasher()address']
  }

  /**
   * Makes a readonly (simulated) call to the ETHTornado smart contract using the `levels()uint64` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async levels(params: CallParams<EthTornadoArgs['obj']['levels()uint64'] | EthTornadoArgs['tuple']['levels()uint64']> = {args: []}) {
    const result = await this.appClient.send.call(EthTornadoParamsFactory.levels(params))
    return result.return as unknown as EthTornadoReturns['levels()uint64']
  }

  /**
   * Makes a readonly (simulated) call to the ETHTornado smart contract using the `filledSubtrees(uint256)byte[32]` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async filledSubtrees(params: CallParams<EthTornadoArgs['obj']['filledSubtrees(uint256)byte[32]'] | EthTornadoArgs['tuple']['filledSubtrees(uint256)byte[32]']>) {
    const result = await this.appClient.send.call(EthTornadoParamsFactory.filledSubtrees(params))
    return result.return as unknown as EthTornadoReturns['filledSubtrees(uint256)byte[32]']
  }

  /**
   * Makes a readonly (simulated) call to the ETHTornado smart contract using the `roots(uint256)byte[32]` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async roots(params: CallParams<EthTornadoArgs['obj']['roots(uint256)byte[32]'] | EthTornadoArgs['tuple']['roots(uint256)byte[32]']>) {
    const result = await this.appClient.send.call(EthTornadoParamsFactory.roots(params))
    return result.return as unknown as EthTornadoReturns['roots(uint256)byte[32]']
  }

  /**
   * Makes a readonly (simulated) call to the ETHTornado smart contract using the `ROOT_HISTORY_SIZE()uint64` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async rootHistorySize(params: CallParams<EthTornadoArgs['obj']['ROOT_HISTORY_SIZE()uint64'] | EthTornadoArgs['tuple']['ROOT_HISTORY_SIZE()uint64']> = {args: []}) {
    const result = await this.appClient.send.call(EthTornadoParamsFactory.rootHistorySize(params))
    return result.return as unknown as EthTornadoReturns['ROOT_HISTORY_SIZE()uint64']
  }

  /**
   * Makes a readonly (simulated) call to the ETHTornado smart contract using the `currentRootIndex()uint64` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async currentRootIndex(params: CallParams<EthTornadoArgs['obj']['currentRootIndex()uint64'] | EthTornadoArgs['tuple']['currentRootIndex()uint64']> = {args: []}) {
    const result = await this.appClient.send.call(EthTornadoParamsFactory.currentRootIndex(params))
    return result.return as unknown as EthTornadoReturns['currentRootIndex()uint64']
  }

  /**
   * Makes a readonly (simulated) call to the ETHTornado smart contract using the `nextIndex()uint64` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async nextIndex(params: CallParams<EthTornadoArgs['obj']['nextIndex()uint64'] | EthTornadoArgs['tuple']['nextIndex()uint64']> = {args: []}) {
    const result = await this.appClient.send.call(EthTornadoParamsFactory.nextIndex(params))
    return result.return as unknown as EthTornadoReturns['nextIndex()uint64']
  }

  /**
   * Makes a readonly (simulated) call to the ETHTornado smart contract using the `isSpent(byte[32])bool` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @dev whether a note is already spent 
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async isSpent(params: CallParams<EthTornadoArgs['obj']['isSpent(byte[32])bool'] | EthTornadoArgs['tuple']['isSpent(byte[32])bool']>) {
    const result = await this.appClient.send.call(EthTornadoParamsFactory.isSpent(params))
    return result.return as unknown as EthTornadoReturns['isSpent(byte[32])bool']
  }

  /**
   * Makes a readonly (simulated) call to the ETHTornado smart contract using the `hashLeftRight(address,byte[32],byte[32])byte[32]` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @dev Hash 2 tree leaves, returns MiMC(_left, _right)
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async hashLeftRight(params: CallParams<EthTornadoArgs['obj']['hashLeftRight(address,byte[32],byte[32])byte[32]'] | EthTornadoArgs['tuple']['hashLeftRight(address,byte[32],byte[32])byte[32]']>) {
    const result = await this.appClient.send.call(EthTornadoParamsFactory.hashLeftRight(params))
    return result.return as unknown as EthTornadoReturns['hashLeftRight(address,byte[32],byte[32])byte[32]']
  }

  /**
   * Makes a readonly (simulated) call to the ETHTornado smart contract using the `isKnownRoot(byte[32])bool` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @dev Whether the root is present in the root history
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async isKnownRoot(params: CallParams<EthTornadoArgs['obj']['isKnownRoot(byte[32])bool'] | EthTornadoArgs['tuple']['isKnownRoot(byte[32])bool']>) {
    const result = await this.appClient.send.call(EthTornadoParamsFactory.isKnownRoot(params))
    return result.return as unknown as EthTornadoReturns['isKnownRoot(byte[32])bool']
  }

  /**
   * Makes a readonly (simulated) call to the ETHTornado smart contract using the `getLastRoot()byte[32]` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @dev Returns the last root
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async getLastRoot(params: CallParams<EthTornadoArgs['obj']['getLastRoot()byte[32]'] | EthTornadoArgs['tuple']['getLastRoot()byte[32]']> = {args: []}) {
    const result = await this.appClient.send.call(EthTornadoParamsFactory.getLastRoot(params))
    return result.return as unknown as EthTornadoReturns['getLastRoot()byte[32]']
  }

  /**
   * Makes a readonly (simulated) call to the ETHTornado smart contract using the `zeros(uint256)byte[32]` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @dev provides Zero (Empty) elements for a MiMC MerkleTree. Up to 32 levels
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async zeros(params: CallParams<EthTornadoArgs['obj']['zeros(uint256)byte[32]'] | EthTornadoArgs['tuple']['zeros(uint256)byte[32]']>) {
    const result = await this.appClient.send.call(EthTornadoParamsFactory.zeros(params))
    return result.return as unknown as EthTornadoReturns['zeros(uint256)byte[32]']
  }

  /**
   * Methods to access state for the current ETHTornado app
   */
  state = {
    /**
     * Methods to access global state for the current ETHTornado app
     */
    global: {
      /**
       * Get all current keyed values from global state
       */
      getAll: async (): Promise<Partial<Expand<GlobalKeysState>>> => {
        const result = await this.appClient.state.global.getAll()
        return {
          verifier: result.verifier,
          denomination: new BinaryStateValue(result.denomination),
          _status: new BinaryStateValue(result._status),
          hasher: result.hasher,
          levels: result.levels,
          currentRootIndex: result.currentRootIndex,
          nextIndex: result.nextIndex,
          __ctorPending: result.__ctor_pending,
        }
      },
      /**
       * Get the current value of the verifier key in global state
       */
      verifier: async (): Promise<string | undefined> => { return (await this.appClient.state.global.getValue("verifier")) as string | undefined },
      /**
       * Get the current value of the denomination key in global state
       */
      denomination: async (): Promise<BinaryState> => { return new BinaryStateValue((await this.appClient.state.global.getValue("denomination")) as Uint8Array | undefined) },
      /**
       * Get the current value of the _status key in global state
       */
      _status: async (): Promise<BinaryState> => { return new BinaryStateValue((await this.appClient.state.global.getValue("_status")) as Uint8Array | undefined) },
      /**
       * Get the current value of the hasher key in global state
       */
      hasher: async (): Promise<string | undefined> => { return (await this.appClient.state.global.getValue("hasher")) as string | undefined },
      /**
       * Get the current value of the levels key in global state
       */
      levels: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("levels")) as bigint | undefined },
      /**
       * Get the current value of the currentRootIndex key in global state
       */
      currentRootIndex: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("currentRootIndex")) as bigint | undefined },
      /**
       * Get the current value of the nextIndex key in global state
       */
      nextIndex: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("nextIndex")) as bigint | undefined },
      /**
       * Get the current value of the __ctor_pending key in global state
       */
      __ctorPending: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("__ctor_pending")) as bigint | undefined },
    },
    /**
     * Methods to access box state for the current ETHTornado app
     */
    box: {
      /**
       * Get all current keyed values from box state
       */
      getAll: async (): Promise<Partial<Expand<BoxKeysState>>> => {
        const result = await this.appClient.state.box.getAll()
        return {
        }
      },
      /**
       * Get values from the nullifierHashes map in box state
       */
      nullifierHashes: {
        /**
         * Get all current values of the nullifierHashes map in box state
         */
        getMap: async (): Promise<Map<Uint8Array, bigint>> => { return (await this.appClient.state.box.getMap("nullifierHashes")) as Map<Uint8Array, bigint> },
        /**
         * Get a current value of the nullifierHashes map by key from box state
         */
        value: async (key: Uint8Array | string): Promise<bigint | undefined> => { return await this.appClient.state.box.getMapValue("nullifierHashes", key) as bigint | undefined },
      },
      /**
       * Get values from the commitments map in box state
       */
      commitments: {
        /**
         * Get all current values of the commitments map in box state
         */
        getMap: async (): Promise<Map<Uint8Array, bigint>> => { return (await this.appClient.state.box.getMap("commitments")) as Map<Uint8Array, bigint> },
        /**
         * Get a current value of the commitments map by key from box state
         */
        value: async (key: Uint8Array | string): Promise<bigint | undefined> => { return await this.appClient.state.box.getMapValue("commitments", key) as bigint | undefined },
      },
      /**
       * Get values from the filledSubtrees map in box state
       */
      filledSubtrees: {
        /**
         * Get all current values of the filledSubtrees map in box state
         */
        getMap: async (): Promise<Map<Uint8Array, Uint8Array>> => { return (await this.appClient.state.box.getMap("filledSubtrees")) as Map<Uint8Array, Uint8Array> },
        /**
         * Get a current value of the filledSubtrees map by key from box state
         */
        value: async (key: Uint8Array | string): Promise<Uint8Array | undefined> => { return await this.appClient.state.box.getMapValue("filledSubtrees", key) as Uint8Array | undefined },
      },
      /**
       * Get values from the roots map in box state
       */
      roots: {
        /**
         * Get all current values of the roots map in box state
         */
        getMap: async (): Promise<Map<Uint8Array, Uint8Array>> => { return (await this.appClient.state.box.getMap("roots")) as Map<Uint8Array, Uint8Array> },
        /**
         * Get a current value of the roots map by key from box state
         */
        value: async (key: Uint8Array | string): Promise<Uint8Array | undefined> => { return await this.appClient.state.box.getMapValue("roots", key) as Uint8Array | undefined },
      },
    },
  }

  public newGroup(): EthTornadoComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a __postInit(address,address,uint256,uint64)void method call against the ETHTornado contract
       */
      __postInit(params: CallParams<EthTornadoArgs['obj']['__postInit(address,address,uint256,uint64)void'] | EthTornadoArgs['tuple']['__postInit(address,address,uint256,uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.__postInit(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a verifier()address method call against the ETHTornado contract
       */
      verifier(params: CallParams<EthTornadoArgs['obj']['verifier()address'] | EthTornadoArgs['tuple']['verifier()address']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.verifier(params)))
        resultMappers.push((v) => client.decodeReturnValue('verifier()address', v))
        return this
      },
      /**
       * Add a denomination()uint256 method call against the ETHTornado contract
       */
      denomination(params: CallParams<EthTornadoArgs['obj']['denomination()uint256'] | EthTornadoArgs['tuple']['denomination()uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.denomination(params)))
        resultMappers.push((v) => client.decodeReturnValue('denomination()uint256', v))
        return this
      },
      /**
       * Add a nullifierHashes(byte[32])bool method call against the ETHTornado contract
       */
      nullifierHashes(params: CallParams<EthTornadoArgs['obj']['nullifierHashes(byte[32])bool'] | EthTornadoArgs['tuple']['nullifierHashes(byte[32])bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.nullifierHashes(params)))
        resultMappers.push((v) => client.decodeReturnValue('nullifierHashes(byte[32])bool', v))
        return this
      },
      /**
       * Add a commitments(byte[32])bool method call against the ETHTornado contract
       */
      commitments(params: CallParams<EthTornadoArgs['obj']['commitments(byte[32])bool'] | EthTornadoArgs['tuple']['commitments(byte[32])bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.commitments(params)))
        resultMappers.push((v) => client.decodeReturnValue('commitments(byte[32])bool', v))
        return this
      },
      /**
       * Add a FIELD_SIZE()uint256 method call against the ETHTornado contract
       */
      fieldSize(params: CallParams<EthTornadoArgs['obj']['FIELD_SIZE()uint256'] | EthTornadoArgs['tuple']['FIELD_SIZE()uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.fieldSize(params)))
        resultMappers.push((v) => client.decodeReturnValue('FIELD_SIZE()uint256', v))
        return this
      },
      /**
       * Add a ZERO_VALUE()uint256 method call against the ETHTornado contract
       */
      zeroValue(params: CallParams<EthTornadoArgs['obj']['ZERO_VALUE()uint256'] | EthTornadoArgs['tuple']['ZERO_VALUE()uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.zeroValue(params)))
        resultMappers.push((v) => client.decodeReturnValue('ZERO_VALUE()uint256', v))
        return this
      },
      /**
       * Add a hasher()address method call against the ETHTornado contract
       */
      hasher(params: CallParams<EthTornadoArgs['obj']['hasher()address'] | EthTornadoArgs['tuple']['hasher()address']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.hasher(params)))
        resultMappers.push((v) => client.decodeReturnValue('hasher()address', v))
        return this
      },
      /**
       * Add a levels()uint64 method call against the ETHTornado contract
       */
      levels(params: CallParams<EthTornadoArgs['obj']['levels()uint64'] | EthTornadoArgs['tuple']['levels()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.levels(params)))
        resultMappers.push((v) => client.decodeReturnValue('levels()uint64', v))
        return this
      },
      /**
       * Add a filledSubtrees(uint256)byte[32] method call against the ETHTornado contract
       */
      filledSubtrees(params: CallParams<EthTornadoArgs['obj']['filledSubtrees(uint256)byte[32]'] | EthTornadoArgs['tuple']['filledSubtrees(uint256)byte[32]']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.filledSubtrees(params)))
        resultMappers.push((v) => client.decodeReturnValue('filledSubtrees(uint256)byte[32]', v))
        return this
      },
      /**
       * Add a roots(uint256)byte[32] method call against the ETHTornado contract
       */
      roots(params: CallParams<EthTornadoArgs['obj']['roots(uint256)byte[32]'] | EthTornadoArgs['tuple']['roots(uint256)byte[32]']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.roots(params)))
        resultMappers.push((v) => client.decodeReturnValue('roots(uint256)byte[32]', v))
        return this
      },
      /**
       * Add a ROOT_HISTORY_SIZE()uint64 method call against the ETHTornado contract
       */
      rootHistorySize(params: CallParams<EthTornadoArgs['obj']['ROOT_HISTORY_SIZE()uint64'] | EthTornadoArgs['tuple']['ROOT_HISTORY_SIZE()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.rootHistorySize(params)))
        resultMappers.push((v) => client.decodeReturnValue('ROOT_HISTORY_SIZE()uint64', v))
        return this
      },
      /**
       * Add a currentRootIndex()uint64 method call against the ETHTornado contract
       */
      currentRootIndex(params: CallParams<EthTornadoArgs['obj']['currentRootIndex()uint64'] | EthTornadoArgs['tuple']['currentRootIndex()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.currentRootIndex(params)))
        resultMappers.push((v) => client.decodeReturnValue('currentRootIndex()uint64', v))
        return this
      },
      /**
       * Add a nextIndex()uint64 method call against the ETHTornado contract
       */
      nextIndex(params: CallParams<EthTornadoArgs['obj']['nextIndex()uint64'] | EthTornadoArgs['tuple']['nextIndex()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.nextIndex(params)))
        resultMappers.push((v) => client.decodeReturnValue('nextIndex()uint64', v))
        return this
      },
      /**
       * Add a deposit(byte[32])void method call against the ETHTornado contract
       */
      deposit(params: CallParams<EthTornadoArgs['obj']['deposit(byte[32])void'] | EthTornadoArgs['tuple']['deposit(byte[32])void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.deposit(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a withdraw(byte[],byte[32],byte[32],address,address,uint256,uint256)void method call against the ETHTornado contract
       */
      withdraw(params: CallParams<EthTornadoArgs['obj']['withdraw(byte[],byte[32],byte[32],address,address,uint256,uint256)void'] | EthTornadoArgs['tuple']['withdraw(byte[],byte[32],byte[32],address,address,uint256,uint256)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.withdraw(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a isSpent(byte[32])bool method call against the ETHTornado contract
       */
      isSpent(params: CallParams<EthTornadoArgs['obj']['isSpent(byte[32])bool'] | EthTornadoArgs['tuple']['isSpent(byte[32])bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.isSpent(params)))
        resultMappers.push((v) => client.decodeReturnValue('isSpent(byte[32])bool', v))
        return this
      },
      /**
       * Add a hashLeftRight(address,byte[32],byte[32])byte[32] method call against the ETHTornado contract
       */
      hashLeftRight(params: CallParams<EthTornadoArgs['obj']['hashLeftRight(address,byte[32],byte[32])byte[32]'] | EthTornadoArgs['tuple']['hashLeftRight(address,byte[32],byte[32])byte[32]']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.hashLeftRight(params)))
        resultMappers.push((v) => client.decodeReturnValue('hashLeftRight(address,byte[32],byte[32])byte[32]', v))
        return this
      },
      /**
       * Add a isKnownRoot(byte[32])bool method call against the ETHTornado contract
       */
      isKnownRoot(params: CallParams<EthTornadoArgs['obj']['isKnownRoot(byte[32])bool'] | EthTornadoArgs['tuple']['isKnownRoot(byte[32])bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.isKnownRoot(params)))
        resultMappers.push((v) => client.decodeReturnValue('isKnownRoot(byte[32])bool', v))
        return this
      },
      /**
       * Add a getLastRoot()byte[32] method call against the ETHTornado contract
       */
      getLastRoot(params: CallParams<EthTornadoArgs['obj']['getLastRoot()byte[32]'] | EthTornadoArgs['tuple']['getLastRoot()byte[32]']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.getLastRoot(params)))
        resultMappers.push((v) => client.decodeReturnValue('getLastRoot()byte[32]', v))
        return this
      },
      /**
       * Add a zeros(uint256)byte[32] method call against the ETHTornado contract
       */
      zeros(params: CallParams<EthTornadoArgs['obj']['zeros(uint256)byte[32]'] | EthTornadoArgs['tuple']['zeros(uint256)byte[32]']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.zeros(params)))
        resultMappers.push((v) => client.decodeReturnValue('zeros(uint256)byte[32]', v))
        return this
      },
      /**
       * Add a clear state call to the ETHTornado contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as EthTornadoComposer
  }
}
export type EthTornadoComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the __postInit(address,address,uint256,uint64)void ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  __postInit(params?: CallParams<EthTornadoArgs['obj']['__postInit(address,address,uint256,uint64)void'] | EthTornadoArgs['tuple']['__postInit(address,address,uint256,uint64)void']>): EthTornadoComposer<[...TReturns, EthTornadoReturns['__postInit(address,address,uint256,uint64)void'] | undefined]>

  /**
   * Calls the verifier()address ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  verifier(params?: CallParams<EthTornadoArgs['obj']['verifier()address'] | EthTornadoArgs['tuple']['verifier()address']>): EthTornadoComposer<[...TReturns, EthTornadoReturns['verifier()address'] | undefined]>

  /**
   * Calls the denomination()uint256 ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  denomination(params?: CallParams<EthTornadoArgs['obj']['denomination()uint256'] | EthTornadoArgs['tuple']['denomination()uint256']>): EthTornadoComposer<[...TReturns, EthTornadoReturns['denomination()uint256'] | undefined]>

  /**
   * Calls the nullifierHashes(byte[32])bool ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  nullifierHashes(params?: CallParams<EthTornadoArgs['obj']['nullifierHashes(byte[32])bool'] | EthTornadoArgs['tuple']['nullifierHashes(byte[32])bool']>): EthTornadoComposer<[...TReturns, EthTornadoReturns['nullifierHashes(byte[32])bool'] | undefined]>

  /**
   * Calls the commitments(byte[32])bool ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  commitments(params?: CallParams<EthTornadoArgs['obj']['commitments(byte[32])bool'] | EthTornadoArgs['tuple']['commitments(byte[32])bool']>): EthTornadoComposer<[...TReturns, EthTornadoReturns['commitments(byte[32])bool'] | undefined]>

  /**
   * Calls the FIELD_SIZE()uint256 ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  fieldSize(params?: CallParams<EthTornadoArgs['obj']['FIELD_SIZE()uint256'] | EthTornadoArgs['tuple']['FIELD_SIZE()uint256']>): EthTornadoComposer<[...TReturns, EthTornadoReturns['FIELD_SIZE()uint256'] | undefined]>

  /**
   * Calls the ZERO_VALUE()uint256 ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  zeroValue(params?: CallParams<EthTornadoArgs['obj']['ZERO_VALUE()uint256'] | EthTornadoArgs['tuple']['ZERO_VALUE()uint256']>): EthTornadoComposer<[...TReturns, EthTornadoReturns['ZERO_VALUE()uint256'] | undefined]>

  /**
   * Calls the hasher()address ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  hasher(params?: CallParams<EthTornadoArgs['obj']['hasher()address'] | EthTornadoArgs['tuple']['hasher()address']>): EthTornadoComposer<[...TReturns, EthTornadoReturns['hasher()address'] | undefined]>

  /**
   * Calls the levels()uint64 ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  levels(params?: CallParams<EthTornadoArgs['obj']['levels()uint64'] | EthTornadoArgs['tuple']['levels()uint64']>): EthTornadoComposer<[...TReturns, EthTornadoReturns['levels()uint64'] | undefined]>

  /**
   * Calls the filledSubtrees(uint256)byte[32] ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  filledSubtrees(params?: CallParams<EthTornadoArgs['obj']['filledSubtrees(uint256)byte[32]'] | EthTornadoArgs['tuple']['filledSubtrees(uint256)byte[32]']>): EthTornadoComposer<[...TReturns, EthTornadoReturns['filledSubtrees(uint256)byte[32]'] | undefined]>

  /**
   * Calls the roots(uint256)byte[32] ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  roots(params?: CallParams<EthTornadoArgs['obj']['roots(uint256)byte[32]'] | EthTornadoArgs['tuple']['roots(uint256)byte[32]']>): EthTornadoComposer<[...TReturns, EthTornadoReturns['roots(uint256)byte[32]'] | undefined]>

  /**
   * Calls the ROOT_HISTORY_SIZE()uint64 ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  rootHistorySize(params?: CallParams<EthTornadoArgs['obj']['ROOT_HISTORY_SIZE()uint64'] | EthTornadoArgs['tuple']['ROOT_HISTORY_SIZE()uint64']>): EthTornadoComposer<[...TReturns, EthTornadoReturns['ROOT_HISTORY_SIZE()uint64'] | undefined]>

  /**
   * Calls the currentRootIndex()uint64 ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  currentRootIndex(params?: CallParams<EthTornadoArgs['obj']['currentRootIndex()uint64'] | EthTornadoArgs['tuple']['currentRootIndex()uint64']>): EthTornadoComposer<[...TReturns, EthTornadoReturns['currentRootIndex()uint64'] | undefined]>

  /**
   * Calls the nextIndex()uint64 ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  nextIndex(params?: CallParams<EthTornadoArgs['obj']['nextIndex()uint64'] | EthTornadoArgs['tuple']['nextIndex()uint64']>): EthTornadoComposer<[...TReturns, EthTornadoReturns['nextIndex()uint64'] | undefined]>

  /**
   * Calls the deposit(byte[32])void ABI method.
   *
  * @dev Deposit funds into the contract. The caller must send (for ETH) or approve (for ERC20) value equal to or `denomination` of this instance.
  @param _commitment the note commitment, which is PedersenHash(nullifier + secret)

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  deposit(params?: CallParams<EthTornadoArgs['obj']['deposit(byte[32])void'] | EthTornadoArgs['tuple']['deposit(byte[32])void']>): EthTornadoComposer<[...TReturns, EthTornadoReturns['deposit(byte[32])void'] | undefined]>

  /**
   * Calls the withdraw(byte[],byte[32],byte[32],address,address,uint256,uint256)void ABI method.
   *
  * @dev Withdraw a deposit from the contract. `proof` is a zkSNARK proof data, and input is an array of circuit public inputs
  `input` array consists of:
  - merkle root of all deposits in the contract
  - hash of unique deposit nullifier to prevent double spends
  - the recipient of funds
  - optional fee that goes to the transaction sender (usually a relay)

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  withdraw(params?: CallParams<EthTornadoArgs['obj']['withdraw(byte[],byte[32],byte[32],address,address,uint256,uint256)void'] | EthTornadoArgs['tuple']['withdraw(byte[],byte[32],byte[32],address,address,uint256,uint256)void']>): EthTornadoComposer<[...TReturns, EthTornadoReturns['withdraw(byte[],byte[32],byte[32],address,address,uint256,uint256)void'] | undefined]>

  /**
   * Calls the isSpent(byte[32])bool ABI method.
   *
   * @dev whether a note is already spent 
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  isSpent(params?: CallParams<EthTornadoArgs['obj']['isSpent(byte[32])bool'] | EthTornadoArgs['tuple']['isSpent(byte[32])bool']>): EthTornadoComposer<[...TReturns, EthTornadoReturns['isSpent(byte[32])bool'] | undefined]>

  /**
   * Calls the hashLeftRight(address,byte[32],byte[32])byte[32] ABI method.
   *
   * @dev Hash 2 tree leaves, returns MiMC(_left, _right)
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  hashLeftRight(params?: CallParams<EthTornadoArgs['obj']['hashLeftRight(address,byte[32],byte[32])byte[32]'] | EthTornadoArgs['tuple']['hashLeftRight(address,byte[32],byte[32])byte[32]']>): EthTornadoComposer<[...TReturns, EthTornadoReturns['hashLeftRight(address,byte[32],byte[32])byte[32]'] | undefined]>

  /**
   * Calls the isKnownRoot(byte[32])bool ABI method.
   *
   * @dev Whether the root is present in the root history
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  isKnownRoot(params?: CallParams<EthTornadoArgs['obj']['isKnownRoot(byte[32])bool'] | EthTornadoArgs['tuple']['isKnownRoot(byte[32])bool']>): EthTornadoComposer<[...TReturns, EthTornadoReturns['isKnownRoot(byte[32])bool'] | undefined]>

  /**
   * Calls the getLastRoot()byte[32] ABI method.
   *
   * @dev Returns the last root
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  getLastRoot(params?: CallParams<EthTornadoArgs['obj']['getLastRoot()byte[32]'] | EthTornadoArgs['tuple']['getLastRoot()byte[32]']>): EthTornadoComposer<[...TReturns, EthTornadoReturns['getLastRoot()byte[32]'] | undefined]>

  /**
   * Calls the zeros(uint256)byte[32] ABI method.
   *
   * @dev provides Zero (Empty) elements for a MiMC MerkleTree. Up to 32 levels
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  zeros(params?: CallParams<EthTornadoArgs['obj']['zeros(uint256)byte[32]'] | EthTornadoArgs['tuple']['zeros(uint256)byte[32]']>): EthTornadoComposer<[...TReturns, EthTornadoReturns['zeros(uint256)byte[32]'] | undefined]>

  /**
   * Makes a clear_state call to an existing instance of the ETHTornado smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): EthTornadoComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): EthTornadoComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<EthTornadoComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<EthTornadoComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<EthTornadoComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<EthTornadoComposerResults<TReturns>>
}
export type EthTornadoComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

