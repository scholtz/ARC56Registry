/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"MinHeapWrapper","structs":{"Heap":[{"name":"data","type":"uint256[]"}]},"methods":[{"name":"push","args":[{"type":"uint256","name":"value"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}},{"name":"pop","args":[],"returns":{"type":"uint256"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}},{"name":"root","args":[],"returns":{"type":"uint256"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"length","args":[],"returns":{"type":"uint256"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}}],"arcs":[22,28],"networks":{},"state":{"schema":{"global":{"ints":0,"bytes":1},"local":{"ints":0,"bytes":0}},"keys":{"global":{"heap":{"keyType":"AVMString","valueType":"Heap","key":"aGVhcA=="}},"local":{},"box":{}},"maps":{"global":{},"local":{},"box":{}}},"bareActions":{"create":[],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[513,524,547,570],"errorMessage":"check heap exists"},{"pc":[509],"errorMessage":"invalid number of bytes for arc4.uint256"},{"pc":[533,556,579],"errorMessage":"overflow"},{"pc":[146],"errorMessage":"revert"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"events":[]} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


// Type definitions for ARC-56 structs

export type Heap = {
  data: bigint[]
}


/**
 * Converts the ABI tuple representation of a Heap to the struct representation
 */
export function HeapFromTuple(abiTuple: [bigint[]]) {
  return getABIStructFromABITuple(abiTuple, APP_SPEC.structs.Heap, APP_SPEC.structs) as Heap
}

/**
 * The argument types for the MinHeapWrapper contract
 */
export type MinHeapWrapperArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'push(uint256)void': {
      value: bigint | number
    }
    'pop()uint256': Record<string, never>
    'root()uint256': Record<string, never>
    'length()uint256': Record<string, never>
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'push(uint256)void': [value: bigint | number]
    'pop()uint256': []
    'root()uint256': []
    'length()uint256': []
  }
}

/**
 * The return type for each method
 */
export type MinHeapWrapperReturns = {
  'push(uint256)void': void
  'pop()uint256': bigint
  'root()uint256': bigint
  'length()uint256': bigint
}

/**
 * Defines the types of available calls and state of the MinHeapWrapper smart contract.
 */
export type MinHeapWrapperTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'push(uint256)void' | 'push', {
      argsObj: MinHeapWrapperArgs['obj']['push(uint256)void']
      argsTuple: MinHeapWrapperArgs['tuple']['push(uint256)void']
      returns: MinHeapWrapperReturns['push(uint256)void']
    }>
    & Record<'pop()uint256' | 'pop', {
      argsObj: MinHeapWrapperArgs['obj']['pop()uint256']
      argsTuple: MinHeapWrapperArgs['tuple']['pop()uint256']
      returns: MinHeapWrapperReturns['pop()uint256']
    }>
    & Record<'root()uint256' | 'root', {
      argsObj: MinHeapWrapperArgs['obj']['root()uint256']
      argsTuple: MinHeapWrapperArgs['tuple']['root()uint256']
      returns: MinHeapWrapperReturns['root()uint256']
    }>
    & Record<'length()uint256' | 'length', {
      argsObj: MinHeapWrapperArgs['obj']['length()uint256']
      argsTuple: MinHeapWrapperArgs['tuple']['length()uint256']
      returns: MinHeapWrapperReturns['length()uint256']
    }>
  /**
   * Defines the shape of the state of the application.
   */
  state: {
    global: {
      keys: {
        heap: Heap
      }
      maps: {}
    }
  }
}

/**
 * Defines the possible abi call signatures.
 */
export type MinHeapWrapperSignatures = keyof MinHeapWrapperTypes['methods']
/**
 * Defines the possible abi call signatures for methods that return a non-void value.
 */
export type MinHeapWrapperNonVoidMethodSignatures = keyof MinHeapWrapperTypes['methods'] extends infer T ? T extends keyof MinHeapWrapperTypes['methods'] ? MethodReturn<T> extends void ? never : T  : never : never
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the MinHeapWrapper smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends MinHeapWrapperSignatures> = MinHeapWrapperTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the MinHeapWrapper smart contract to the method's return type
 */
export type MethodReturn<TSignature extends MinHeapWrapperSignatures> = MinHeapWrapperTypes['methods'][TSignature]['returns']

/**
 * Defines the shape of the keyed global state of the application.
 */
export type GlobalKeysState = MinHeapWrapperTypes['state']['global']['keys']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the MinHeapWrapper smart contract
 */
export abstract class MinHeapWrapperParamsFactory {
  /**
   * Constructs a no op call for the push(uint256)void ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static push(params: CallParams<MinHeapWrapperArgs['obj']['push(uint256)void'] | MinHeapWrapperArgs['tuple']['push(uint256)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'push(uint256)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.value],
    }
  }
  /**
   * Constructs a no op call for the pop()uint256 ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static pop(params: CallParams<MinHeapWrapperArgs['obj']['pop()uint256'] | MinHeapWrapperArgs['tuple']['pop()uint256']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'pop()uint256' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the root()uint256 ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static root(params: CallParams<MinHeapWrapperArgs['obj']['root()uint256'] | MinHeapWrapperArgs['tuple']['root()uint256']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'root()uint256' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the length()uint256 ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static length(params: CallParams<MinHeapWrapperArgs['obj']['length()uint256'] | MinHeapWrapperArgs['tuple']['length()uint256']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'length()uint256' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
}

/**
 * A client to make calls to the MinHeapWrapper smart contract
 */
export class MinHeapWrapperClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `MinHeapWrapperClient`
   *
   * @param appClient An `AppClient` instance which has been created with the MinHeapWrapper app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `MinHeapWrapperClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Checks for decode errors on the given return value and maps the return value to the return type for the given method
   * @returns The typed return value or undefined if there was no value
   */
  decodeReturnValue<TSignature extends MinHeapWrapperNonVoidMethodSignatures>(method: TSignature, returnValue: ABIReturn | undefined) {
    return returnValue !== undefined ? getArc56ReturnValue<MethodReturn<TSignature>>(returnValue, this.appClient.getABIMethod(method), APP_SPEC.structs) : undefined
  }

  /**
   * Returns a new `MinHeapWrapperClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<MinHeapWrapperClient> {
    return new MinHeapWrapperClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `MinHeapWrapperClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<MinHeapWrapperClient> {
    return new MinHeapWrapperClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Makes a clear_state call to an existing instance of the MinHeapWrapper smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the MinHeapWrapper smart contract using the `push(uint256)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    push: (params: CallParams<MinHeapWrapperArgs['obj']['push(uint256)void'] | MinHeapWrapperArgs['tuple']['push(uint256)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(MinHeapWrapperParamsFactory.push(params))
    },

    /**
     * Makes a call to the MinHeapWrapper smart contract using the `pop()uint256` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    pop: (params: CallParams<MinHeapWrapperArgs['obj']['pop()uint256'] | MinHeapWrapperArgs['tuple']['pop()uint256']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(MinHeapWrapperParamsFactory.pop(params))
    },

    /**
     * Makes a call to the MinHeapWrapper smart contract using the `root()uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    root: (params: CallParams<MinHeapWrapperArgs['obj']['root()uint256'] | MinHeapWrapperArgs['tuple']['root()uint256']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(MinHeapWrapperParamsFactory.root(params))
    },

    /**
     * Makes a call to the MinHeapWrapper smart contract using the `length()uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    length: (params: CallParams<MinHeapWrapperArgs['obj']['length()uint256'] | MinHeapWrapperArgs['tuple']['length()uint256']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(MinHeapWrapperParamsFactory.length(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Makes a clear_state call to an existing instance of the MinHeapWrapper smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the MinHeapWrapper smart contract using the `push(uint256)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    push: (params: CallParams<MinHeapWrapperArgs['obj']['push(uint256)void'] | MinHeapWrapperArgs['tuple']['push(uint256)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(MinHeapWrapperParamsFactory.push(params))
    },

    /**
     * Makes a call to the MinHeapWrapper smart contract using the `pop()uint256` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    pop: (params: CallParams<MinHeapWrapperArgs['obj']['pop()uint256'] | MinHeapWrapperArgs['tuple']['pop()uint256']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(MinHeapWrapperParamsFactory.pop(params))
    },

    /**
     * Makes a call to the MinHeapWrapper smart contract using the `root()uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    root: (params: CallParams<MinHeapWrapperArgs['obj']['root()uint256'] | MinHeapWrapperArgs['tuple']['root()uint256']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(MinHeapWrapperParamsFactory.root(params))
    },

    /**
     * Makes a call to the MinHeapWrapper smart contract using the `length()uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    length: (params: CallParams<MinHeapWrapperArgs['obj']['length()uint256'] | MinHeapWrapperArgs['tuple']['length()uint256']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(MinHeapWrapperParamsFactory.length(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Makes a clear_state call to an existing instance of the MinHeapWrapper smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the MinHeapWrapper smart contract using the `push(uint256)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    push: async (params: CallParams<MinHeapWrapperArgs['obj']['push(uint256)void'] | MinHeapWrapperArgs['tuple']['push(uint256)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(MinHeapWrapperParamsFactory.push(params))
      return {...result, return: result.return as unknown as (undefined | MinHeapWrapperReturns['push(uint256)void'])}
    },

    /**
     * Makes a call to the MinHeapWrapper smart contract using the `pop()uint256` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    pop: async (params: CallParams<MinHeapWrapperArgs['obj']['pop()uint256'] | MinHeapWrapperArgs['tuple']['pop()uint256']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(MinHeapWrapperParamsFactory.pop(params))
      return {...result, return: result.return as unknown as (undefined | MinHeapWrapperReturns['pop()uint256'])}
    },

    /**
     * Makes a call to the MinHeapWrapper smart contract using the `root()uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    root: async (params: CallParams<MinHeapWrapperArgs['obj']['root()uint256'] | MinHeapWrapperArgs['tuple']['root()uint256']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(MinHeapWrapperParamsFactory.root(params))
      return {...result, return: result.return as unknown as (undefined | MinHeapWrapperReturns['root()uint256'])}
    },

    /**
     * Makes a call to the MinHeapWrapper smart contract using the `length()uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    length: async (params: CallParams<MinHeapWrapperArgs['obj']['length()uint256'] | MinHeapWrapperArgs['tuple']['length()uint256']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(MinHeapWrapperParamsFactory.length(params))
      return {...result, return: result.return as unknown as (undefined | MinHeapWrapperReturns['length()uint256'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new MinHeapWrapperClient(this.appClient.clone(params))
  }

  /**
   * Makes a readonly (simulated) call to the MinHeapWrapper smart contract using the `root()uint256` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async root(params: CallParams<MinHeapWrapperArgs['obj']['root()uint256'] | MinHeapWrapperArgs['tuple']['root()uint256']> = {args: []}) {
    const result = await this.appClient.send.call(MinHeapWrapperParamsFactory.root(params))
    return result.return as unknown as MinHeapWrapperReturns['root()uint256']
  }

  /**
   * Makes a readonly (simulated) call to the MinHeapWrapper smart contract using the `length()uint256` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async length(params: CallParams<MinHeapWrapperArgs['obj']['length()uint256'] | MinHeapWrapperArgs['tuple']['length()uint256']> = {args: []}) {
    const result = await this.appClient.send.call(MinHeapWrapperParamsFactory.length(params))
    return result.return as unknown as MinHeapWrapperReturns['length()uint256']
  }

  /**
   * Methods to access state for the current MinHeapWrapper app
   */
  state = {
    /**
     * Methods to access global state for the current MinHeapWrapper app
     */
    global: {
      /**
       * Get all current keyed values from global state
       */
      getAll: async (): Promise<Partial<Expand<GlobalKeysState>>> => {
        const result = await this.appClient.state.global.getAll()
        return {
          heap: result.heap,
        }
      },
      /**
       * Get the current value of the heap key in global state
       */
      heap: async (): Promise<Heap | undefined> => { return (await this.appClient.state.global.getValue("heap")) as Heap | undefined },
    },
  }

  public newGroup(): MinHeapWrapperComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a push(uint256)void method call against the MinHeapWrapper contract
       */
      push(params: CallParams<MinHeapWrapperArgs['obj']['push(uint256)void'] | MinHeapWrapperArgs['tuple']['push(uint256)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.push(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a pop()uint256 method call against the MinHeapWrapper contract
       */
      pop(params: CallParams<MinHeapWrapperArgs['obj']['pop()uint256'] | MinHeapWrapperArgs['tuple']['pop()uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.pop(params)))
        resultMappers.push((v) => client.decodeReturnValue('pop()uint256', v))
        return this
      },
      /**
       * Add a root()uint256 method call against the MinHeapWrapper contract
       */
      root(params: CallParams<MinHeapWrapperArgs['obj']['root()uint256'] | MinHeapWrapperArgs['tuple']['root()uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.root(params)))
        resultMappers.push((v) => client.decodeReturnValue('root()uint256', v))
        return this
      },
      /**
       * Add a length()uint256 method call against the MinHeapWrapper contract
       */
      length(params: CallParams<MinHeapWrapperArgs['obj']['length()uint256'] | MinHeapWrapperArgs['tuple']['length()uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.length(params)))
        resultMappers.push((v) => client.decodeReturnValue('length()uint256', v))
        return this
      },
      /**
       * Add a clear state call to the MinHeapWrapper contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as MinHeapWrapperComposer
  }
}
export type MinHeapWrapperComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the push(uint256)void ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  push(params?: CallParams<MinHeapWrapperArgs['obj']['push(uint256)void'] | MinHeapWrapperArgs['tuple']['push(uint256)void']>): MinHeapWrapperComposer<[...TReturns, MinHeapWrapperReturns['push(uint256)void'] | undefined]>

  /**
   * Calls the pop()uint256 ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  pop(params?: CallParams<MinHeapWrapperArgs['obj']['pop()uint256'] | MinHeapWrapperArgs['tuple']['pop()uint256']>): MinHeapWrapperComposer<[...TReturns, MinHeapWrapperReturns['pop()uint256'] | undefined]>

  /**
   * Calls the root()uint256 ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  root(params?: CallParams<MinHeapWrapperArgs['obj']['root()uint256'] | MinHeapWrapperArgs['tuple']['root()uint256']>): MinHeapWrapperComposer<[...TReturns, MinHeapWrapperReturns['root()uint256'] | undefined]>

  /**
   * Calls the length()uint256 ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  length(params?: CallParams<MinHeapWrapperArgs['obj']['length()uint256'] | MinHeapWrapperArgs['tuple']['length()uint256']>): MinHeapWrapperComposer<[...TReturns, MinHeapWrapperReturns['length()uint256'] | undefined]>

  /**
   * Makes a clear_state call to an existing instance of the MinHeapWrapper smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): MinHeapWrapperComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): MinHeapWrapperComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<MinHeapWrapperComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<MinHeapWrapperComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<MinHeapWrapperComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<MinHeapWrapperComposerResults<TReturns>>
}
export type MinHeapWrapperComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

