/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"CrowdfundingTest","structs":{},"methods":[{"name":"creator","args":[],"returns":{"type":"address"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"goal","args":[],"returns":{"type":"uint512"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"deadline","args":[],"returns":{"type":"uint512"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"totalPledged","args":[],"returns":{"type":"uint512"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"claimed","args":[],"returns":{"type":"bool"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"backerCount","args":[],"returns":{"type":"uint512"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"pledgeOf","args":[{"type":"address","name":"backer"}],"returns":{"type":"uint512"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"isGoalReached","args":[],"returns":{"type":"bool"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"pledge","args":[{"type":"address","name":"backer"},{"type":"uint512","name":"amount"},{"type":"uint512","name":"currentTime"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}},{"name":"unpledge","args":[{"type":"address","name":"backer"},{"type":"uint512","name":"amount"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}},{"name":"claim","args":[],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}},{"name":"refund","args":[{"type":"address","name":"backer"}],"returns":{"type":"uint512"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}}],"arcs":[22,28],"desc":" @dev Crowdfunding contract with goal, deadline, and refund support.","networks":{},"state":{"schema":{"global":{"ints":1,"bytes":5},"local":{"ints":0,"bytes":0}},"keys":{"global":{"_creator":{"keyType":"AVMString","valueType":"address","key":"X2NyZWF0b3I="},"_goal":{"keyType":"AVMString","valueType":"AVMBytes","key":"X2dvYWw="},"_deadline":{"keyType":"AVMString","valueType":"AVMBytes","key":"X2RlYWRsaW5l"},"_totalPledged":{"keyType":"AVMString","valueType":"AVMBytes","key":"X3RvdGFsUGxlZGdlZA=="},"_claimed":{"keyType":"AVMString","valueType":"AVMUint64","key":"X2NsYWltZWQ="},"_backerCount":{"keyType":"AVMString","valueType":"AVMBytes","key":"X2JhY2tlckNvdW50"}},"local":{},"box":{}},"maps":{"global":{},"local":{},"box":{"_pledges":{"keyType":"AVMBytes","valueType":"AVMBytes","prefix":"X3BsZWRnZXM="}}}},"bareActions":{"create":[],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[473,569,635,656],"errorMessage":"Already claimed"},{"pc":[466],"errorMessage":"Amount must be > 0"},{"pc":[459],"errorMessage":"Campaign ended"},{"pc":[628],"errorMessage":"Goal not reached"},{"pc":[562],"errorMessage":"Insufficient pledge"},{"pc":[617],"errorMessage":"Not creator"},{"pc":[356,495,600,699],"errorMessage":"check _backerCount exists"},{"pc":[339,471,567,633,654],"errorMessage":"check _claimed exists"},{"pc":[272,615],"errorMessage":"check _creator exists"},{"pc":[302,457],"errorMessage":"check _deadline exists"},{"pc":[283,411,626],"errorMessage":"check _goal exists"},{"pc":[320,406,523,584,621,689],"errorMessage":"check _totalPledged exists"},{"pc":[378,433,537,649],"errorMessage":"invalid number of bytes for arc4.static_array<arc4.uint8, 32>"},{"pc":[444,452,545],"errorMessage":"invalid number of bytes for arc4.uint512"},{"pc":[288,307,325,361,393,712],"errorMessage":"overflow"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"events":[]} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


/**
 * The argument types for the CrowdfundingTest contract
 */
export type CrowdfundingTestArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'creator()address': Record<string, never>
    'goal()uint512': Record<string, never>
    'deadline()uint512': Record<string, never>
    'totalPledged()uint512': Record<string, never>
    'claimed()bool': Record<string, never>
    'backerCount()uint512': Record<string, never>
    'pledgeOf(address)uint512': {
      backer: string
    }
    'isGoalReached()bool': Record<string, never>
    'pledge(address,uint512,uint512)void': {
      backer: string
      amount: bigint | number
      currentTime: bigint | number
    }
    'unpledge(address,uint512)void': {
      backer: string
      amount: bigint | number
    }
    'claim()void': Record<string, never>
    'refund(address)uint512': {
      backer: string
    }
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'creator()address': []
    'goal()uint512': []
    'deadline()uint512': []
    'totalPledged()uint512': []
    'claimed()bool': []
    'backerCount()uint512': []
    'pledgeOf(address)uint512': [backer: string]
    'isGoalReached()bool': []
    'pledge(address,uint512,uint512)void': [backer: string, amount: bigint | number, currentTime: bigint | number]
    'unpledge(address,uint512)void': [backer: string, amount: bigint | number]
    'claim()void': []
    'refund(address)uint512': [backer: string]
  }
}

/**
 * The return type for each method
 */
export type CrowdfundingTestReturns = {
  'creator()address': string
  'goal()uint512': bigint
  'deadline()uint512': bigint
  'totalPledged()uint512': bigint
  'claimed()bool': boolean
  'backerCount()uint512': bigint
  'pledgeOf(address)uint512': bigint
  'isGoalReached()bool': boolean
  'pledge(address,uint512,uint512)void': void
  'unpledge(address,uint512)void': void
  'claim()void': void
  'refund(address)uint512': bigint
}

/**
 * Defines the types of available calls and state of the CrowdfundingTest smart contract.
 */
export type CrowdfundingTestTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'creator()address' | 'creator', {
      argsObj: CrowdfundingTestArgs['obj']['creator()address']
      argsTuple: CrowdfundingTestArgs['tuple']['creator()address']
      returns: CrowdfundingTestReturns['creator()address']
    }>
    & Record<'goal()uint512' | 'goal', {
      argsObj: CrowdfundingTestArgs['obj']['goal()uint512']
      argsTuple: CrowdfundingTestArgs['tuple']['goal()uint512']
      returns: CrowdfundingTestReturns['goal()uint512']
    }>
    & Record<'deadline()uint512' | 'deadline', {
      argsObj: CrowdfundingTestArgs['obj']['deadline()uint512']
      argsTuple: CrowdfundingTestArgs['tuple']['deadline()uint512']
      returns: CrowdfundingTestReturns['deadline()uint512']
    }>
    & Record<'totalPledged()uint512' | 'totalPledged', {
      argsObj: CrowdfundingTestArgs['obj']['totalPledged()uint512']
      argsTuple: CrowdfundingTestArgs['tuple']['totalPledged()uint512']
      returns: CrowdfundingTestReturns['totalPledged()uint512']
    }>
    & Record<'claimed()bool' | 'claimed', {
      argsObj: CrowdfundingTestArgs['obj']['claimed()bool']
      argsTuple: CrowdfundingTestArgs['tuple']['claimed()bool']
      returns: CrowdfundingTestReturns['claimed()bool']
    }>
    & Record<'backerCount()uint512' | 'backerCount', {
      argsObj: CrowdfundingTestArgs['obj']['backerCount()uint512']
      argsTuple: CrowdfundingTestArgs['tuple']['backerCount()uint512']
      returns: CrowdfundingTestReturns['backerCount()uint512']
    }>
    & Record<'pledgeOf(address)uint512' | 'pledgeOf', {
      argsObj: CrowdfundingTestArgs['obj']['pledgeOf(address)uint512']
      argsTuple: CrowdfundingTestArgs['tuple']['pledgeOf(address)uint512']
      returns: CrowdfundingTestReturns['pledgeOf(address)uint512']
    }>
    & Record<'isGoalReached()bool' | 'isGoalReached', {
      argsObj: CrowdfundingTestArgs['obj']['isGoalReached()bool']
      argsTuple: CrowdfundingTestArgs['tuple']['isGoalReached()bool']
      returns: CrowdfundingTestReturns['isGoalReached()bool']
    }>
    & Record<'pledge(address,uint512,uint512)void' | 'pledge', {
      argsObj: CrowdfundingTestArgs['obj']['pledge(address,uint512,uint512)void']
      argsTuple: CrowdfundingTestArgs['tuple']['pledge(address,uint512,uint512)void']
      returns: CrowdfundingTestReturns['pledge(address,uint512,uint512)void']
    }>
    & Record<'unpledge(address,uint512)void' | 'unpledge', {
      argsObj: CrowdfundingTestArgs['obj']['unpledge(address,uint512)void']
      argsTuple: CrowdfundingTestArgs['tuple']['unpledge(address,uint512)void']
      returns: CrowdfundingTestReturns['unpledge(address,uint512)void']
    }>
    & Record<'claim()void' | 'claim', {
      argsObj: CrowdfundingTestArgs['obj']['claim()void']
      argsTuple: CrowdfundingTestArgs['tuple']['claim()void']
      returns: CrowdfundingTestReturns['claim()void']
    }>
    & Record<'refund(address)uint512' | 'refund', {
      argsObj: CrowdfundingTestArgs['obj']['refund(address)uint512']
      argsTuple: CrowdfundingTestArgs['tuple']['refund(address)uint512']
      returns: CrowdfundingTestReturns['refund(address)uint512']
    }>
  /**
   * Defines the shape of the state of the application.
   */
  state: {
    global: {
      keys: {
        _creator: string
        _goal: BinaryState
        _deadline: BinaryState
        _totalPledged: BinaryState
        _claimed: bigint
        _backerCount: BinaryState
      }
      maps: {}
    }
    box: {
      keys: {}
      maps: {
        _pledges: Map<Uint8Array | string, Uint8Array>
      }
    }
  }
}

/**
 * Defines the possible abi call signatures.
 */
export type CrowdfundingTestSignatures = keyof CrowdfundingTestTypes['methods']
/**
 * Defines the possible abi call signatures for methods that return a non-void value.
 */
export type CrowdfundingTestNonVoidMethodSignatures = keyof CrowdfundingTestTypes['methods'] extends infer T ? T extends keyof CrowdfundingTestTypes['methods'] ? MethodReturn<T> extends void ? never : T  : never : never
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the CrowdfundingTest smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends CrowdfundingTestSignatures> = CrowdfundingTestTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the CrowdfundingTest smart contract to the method's return type
 */
export type MethodReturn<TSignature extends CrowdfundingTestSignatures> = CrowdfundingTestTypes['methods'][TSignature]['returns']

/**
 * Defines the shape of the keyed global state of the application.
 */
export type GlobalKeysState = CrowdfundingTestTypes['state']['global']['keys']

/**
 * Defines the shape of the keyed box state of the application.
 */
export type BoxKeysState = CrowdfundingTestTypes['state']['box']['keys']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the CrowdfundingTest smart contract
 */
export abstract class CrowdfundingTestParamsFactory {
  /**
   * Constructs a no op call for the creator()address ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static creator(params: CallParams<CrowdfundingTestArgs['obj']['creator()address'] | CrowdfundingTestArgs['tuple']['creator()address']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'creator()address' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the goal()uint512 ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static goal(params: CallParams<CrowdfundingTestArgs['obj']['goal()uint512'] | CrowdfundingTestArgs['tuple']['goal()uint512']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'goal()uint512' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the deadline()uint512 ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static deadline(params: CallParams<CrowdfundingTestArgs['obj']['deadline()uint512'] | CrowdfundingTestArgs['tuple']['deadline()uint512']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'deadline()uint512' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the totalPledged()uint512 ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static totalPledged(params: CallParams<CrowdfundingTestArgs['obj']['totalPledged()uint512'] | CrowdfundingTestArgs['tuple']['totalPledged()uint512']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'totalPledged()uint512' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the claimed()bool ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static claimed(params: CallParams<CrowdfundingTestArgs['obj']['claimed()bool'] | CrowdfundingTestArgs['tuple']['claimed()bool']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'claimed()bool' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the backerCount()uint512 ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static backerCount(params: CallParams<CrowdfundingTestArgs['obj']['backerCount()uint512'] | CrowdfundingTestArgs['tuple']['backerCount()uint512']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'backerCount()uint512' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the pledgeOf(address)uint512 ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static pledgeOf(params: CallParams<CrowdfundingTestArgs['obj']['pledgeOf(address)uint512'] | CrowdfundingTestArgs['tuple']['pledgeOf(address)uint512']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'pledgeOf(address)uint512' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.backer],
    }
  }
  /**
   * Constructs a no op call for the isGoalReached()bool ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static isGoalReached(params: CallParams<CrowdfundingTestArgs['obj']['isGoalReached()bool'] | CrowdfundingTestArgs['tuple']['isGoalReached()bool']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'isGoalReached()bool' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the pledge(address,uint512,uint512)void ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static pledge(params: CallParams<CrowdfundingTestArgs['obj']['pledge(address,uint512,uint512)void'] | CrowdfundingTestArgs['tuple']['pledge(address,uint512,uint512)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'pledge(address,uint512,uint512)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.backer, params.args.amount, params.args.currentTime],
    }
  }
  /**
   * Constructs a no op call for the unpledge(address,uint512)void ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static unpledge(params: CallParams<CrowdfundingTestArgs['obj']['unpledge(address,uint512)void'] | CrowdfundingTestArgs['tuple']['unpledge(address,uint512)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'unpledge(address,uint512)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.backer, params.args.amount],
    }
  }
  /**
   * Constructs a no op call for the claim()void ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static claim(params: CallParams<CrowdfundingTestArgs['obj']['claim()void'] | CrowdfundingTestArgs['tuple']['claim()void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'claim()void' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the refund(address)uint512 ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static refund(params: CallParams<CrowdfundingTestArgs['obj']['refund(address)uint512'] | CrowdfundingTestArgs['tuple']['refund(address)uint512']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'refund(address)uint512' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.backer],
    }
  }
}

/**
 * A client to make calls to the CrowdfundingTest smart contract
 */
export class CrowdfundingTestClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `CrowdfundingTestClient`
   *
   * @param appClient An `AppClient` instance which has been created with the CrowdfundingTest app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `CrowdfundingTestClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Checks for decode errors on the given return value and maps the return value to the return type for the given method
   * @returns The typed return value or undefined if there was no value
   */
  decodeReturnValue<TSignature extends CrowdfundingTestNonVoidMethodSignatures>(method: TSignature, returnValue: ABIReturn | undefined) {
    return returnValue !== undefined ? getArc56ReturnValue<MethodReturn<TSignature>>(returnValue, this.appClient.getABIMethod(method), APP_SPEC.structs) : undefined
  }

  /**
   * Returns a new `CrowdfundingTestClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<CrowdfundingTestClient> {
    return new CrowdfundingTestClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `CrowdfundingTestClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<CrowdfundingTestClient> {
    return new CrowdfundingTestClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Makes a clear_state call to an existing instance of the CrowdfundingTest smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the CrowdfundingTest smart contract using the `creator()address` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    creator: (params: CallParams<CrowdfundingTestArgs['obj']['creator()address'] | CrowdfundingTestArgs['tuple']['creator()address']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(CrowdfundingTestParamsFactory.creator(params))
    },

    /**
     * Makes a call to the CrowdfundingTest smart contract using the `goal()uint512` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    goal: (params: CallParams<CrowdfundingTestArgs['obj']['goal()uint512'] | CrowdfundingTestArgs['tuple']['goal()uint512']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(CrowdfundingTestParamsFactory.goal(params))
    },

    /**
     * Makes a call to the CrowdfundingTest smart contract using the `deadline()uint512` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    deadline: (params: CallParams<CrowdfundingTestArgs['obj']['deadline()uint512'] | CrowdfundingTestArgs['tuple']['deadline()uint512']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(CrowdfundingTestParamsFactory.deadline(params))
    },

    /**
     * Makes a call to the CrowdfundingTest smart contract using the `totalPledged()uint512` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    totalPledged: (params: CallParams<CrowdfundingTestArgs['obj']['totalPledged()uint512'] | CrowdfundingTestArgs['tuple']['totalPledged()uint512']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(CrowdfundingTestParamsFactory.totalPledged(params))
    },

    /**
     * Makes a call to the CrowdfundingTest smart contract using the `claimed()bool` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    claimed: (params: CallParams<CrowdfundingTestArgs['obj']['claimed()bool'] | CrowdfundingTestArgs['tuple']['claimed()bool']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(CrowdfundingTestParamsFactory.claimed(params))
    },

    /**
     * Makes a call to the CrowdfundingTest smart contract using the `backerCount()uint512` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    backerCount: (params: CallParams<CrowdfundingTestArgs['obj']['backerCount()uint512'] | CrowdfundingTestArgs['tuple']['backerCount()uint512']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(CrowdfundingTestParamsFactory.backerCount(params))
    },

    /**
     * Makes a call to the CrowdfundingTest smart contract using the `pledgeOf(address)uint512` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    pledgeOf: (params: CallParams<CrowdfundingTestArgs['obj']['pledgeOf(address)uint512'] | CrowdfundingTestArgs['tuple']['pledgeOf(address)uint512']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(CrowdfundingTestParamsFactory.pledgeOf(params))
    },

    /**
     * Makes a call to the CrowdfundingTest smart contract using the `isGoalReached()bool` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    isGoalReached: (params: CallParams<CrowdfundingTestArgs['obj']['isGoalReached()bool'] | CrowdfundingTestArgs['tuple']['isGoalReached()bool']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(CrowdfundingTestParamsFactory.isGoalReached(params))
    },

    /**
     * Makes a call to the CrowdfundingTest smart contract using the `pledge(address,uint512,uint512)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    pledge: (params: CallParams<CrowdfundingTestArgs['obj']['pledge(address,uint512,uint512)void'] | CrowdfundingTestArgs['tuple']['pledge(address,uint512,uint512)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(CrowdfundingTestParamsFactory.pledge(params))
    },

    /**
     * Makes a call to the CrowdfundingTest smart contract using the `unpledge(address,uint512)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    unpledge: (params: CallParams<CrowdfundingTestArgs['obj']['unpledge(address,uint512)void'] | CrowdfundingTestArgs['tuple']['unpledge(address,uint512)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(CrowdfundingTestParamsFactory.unpledge(params))
    },

    /**
     * Makes a call to the CrowdfundingTest smart contract using the `claim()void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    claim: (params: CallParams<CrowdfundingTestArgs['obj']['claim()void'] | CrowdfundingTestArgs['tuple']['claim()void']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(CrowdfundingTestParamsFactory.claim(params))
    },

    /**
     * Makes a call to the CrowdfundingTest smart contract using the `refund(address)uint512` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    refund: (params: CallParams<CrowdfundingTestArgs['obj']['refund(address)uint512'] | CrowdfundingTestArgs['tuple']['refund(address)uint512']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(CrowdfundingTestParamsFactory.refund(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Makes a clear_state call to an existing instance of the CrowdfundingTest smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the CrowdfundingTest smart contract using the `creator()address` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    creator: (params: CallParams<CrowdfundingTestArgs['obj']['creator()address'] | CrowdfundingTestArgs['tuple']['creator()address']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(CrowdfundingTestParamsFactory.creator(params))
    },

    /**
     * Makes a call to the CrowdfundingTest smart contract using the `goal()uint512` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    goal: (params: CallParams<CrowdfundingTestArgs['obj']['goal()uint512'] | CrowdfundingTestArgs['tuple']['goal()uint512']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(CrowdfundingTestParamsFactory.goal(params))
    },

    /**
     * Makes a call to the CrowdfundingTest smart contract using the `deadline()uint512` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    deadline: (params: CallParams<CrowdfundingTestArgs['obj']['deadline()uint512'] | CrowdfundingTestArgs['tuple']['deadline()uint512']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(CrowdfundingTestParamsFactory.deadline(params))
    },

    /**
     * Makes a call to the CrowdfundingTest smart contract using the `totalPledged()uint512` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    totalPledged: (params: CallParams<CrowdfundingTestArgs['obj']['totalPledged()uint512'] | CrowdfundingTestArgs['tuple']['totalPledged()uint512']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(CrowdfundingTestParamsFactory.totalPledged(params))
    },

    /**
     * Makes a call to the CrowdfundingTest smart contract using the `claimed()bool` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    claimed: (params: CallParams<CrowdfundingTestArgs['obj']['claimed()bool'] | CrowdfundingTestArgs['tuple']['claimed()bool']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(CrowdfundingTestParamsFactory.claimed(params))
    },

    /**
     * Makes a call to the CrowdfundingTest smart contract using the `backerCount()uint512` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    backerCount: (params: CallParams<CrowdfundingTestArgs['obj']['backerCount()uint512'] | CrowdfundingTestArgs['tuple']['backerCount()uint512']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(CrowdfundingTestParamsFactory.backerCount(params))
    },

    /**
     * Makes a call to the CrowdfundingTest smart contract using the `pledgeOf(address)uint512` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    pledgeOf: (params: CallParams<CrowdfundingTestArgs['obj']['pledgeOf(address)uint512'] | CrowdfundingTestArgs['tuple']['pledgeOf(address)uint512']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(CrowdfundingTestParamsFactory.pledgeOf(params))
    },

    /**
     * Makes a call to the CrowdfundingTest smart contract using the `isGoalReached()bool` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    isGoalReached: (params: CallParams<CrowdfundingTestArgs['obj']['isGoalReached()bool'] | CrowdfundingTestArgs['tuple']['isGoalReached()bool']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(CrowdfundingTestParamsFactory.isGoalReached(params))
    },

    /**
     * Makes a call to the CrowdfundingTest smart contract using the `pledge(address,uint512,uint512)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    pledge: (params: CallParams<CrowdfundingTestArgs['obj']['pledge(address,uint512,uint512)void'] | CrowdfundingTestArgs['tuple']['pledge(address,uint512,uint512)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(CrowdfundingTestParamsFactory.pledge(params))
    },

    /**
     * Makes a call to the CrowdfundingTest smart contract using the `unpledge(address,uint512)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    unpledge: (params: CallParams<CrowdfundingTestArgs['obj']['unpledge(address,uint512)void'] | CrowdfundingTestArgs['tuple']['unpledge(address,uint512)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(CrowdfundingTestParamsFactory.unpledge(params))
    },

    /**
     * Makes a call to the CrowdfundingTest smart contract using the `claim()void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    claim: (params: CallParams<CrowdfundingTestArgs['obj']['claim()void'] | CrowdfundingTestArgs['tuple']['claim()void']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(CrowdfundingTestParamsFactory.claim(params))
    },

    /**
     * Makes a call to the CrowdfundingTest smart contract using the `refund(address)uint512` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    refund: (params: CallParams<CrowdfundingTestArgs['obj']['refund(address)uint512'] | CrowdfundingTestArgs['tuple']['refund(address)uint512']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(CrowdfundingTestParamsFactory.refund(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Makes a clear_state call to an existing instance of the CrowdfundingTest smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the CrowdfundingTest smart contract using the `creator()address` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    creator: async (params: CallParams<CrowdfundingTestArgs['obj']['creator()address'] | CrowdfundingTestArgs['tuple']['creator()address']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(CrowdfundingTestParamsFactory.creator(params))
      return {...result, return: result.return as unknown as (undefined | CrowdfundingTestReturns['creator()address'])}
    },

    /**
     * Makes a call to the CrowdfundingTest smart contract using the `goal()uint512` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    goal: async (params: CallParams<CrowdfundingTestArgs['obj']['goal()uint512'] | CrowdfundingTestArgs['tuple']['goal()uint512']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(CrowdfundingTestParamsFactory.goal(params))
      return {...result, return: result.return as unknown as (undefined | CrowdfundingTestReturns['goal()uint512'])}
    },

    /**
     * Makes a call to the CrowdfundingTest smart contract using the `deadline()uint512` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    deadline: async (params: CallParams<CrowdfundingTestArgs['obj']['deadline()uint512'] | CrowdfundingTestArgs['tuple']['deadline()uint512']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(CrowdfundingTestParamsFactory.deadline(params))
      return {...result, return: result.return as unknown as (undefined | CrowdfundingTestReturns['deadline()uint512'])}
    },

    /**
     * Makes a call to the CrowdfundingTest smart contract using the `totalPledged()uint512` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    totalPledged: async (params: CallParams<CrowdfundingTestArgs['obj']['totalPledged()uint512'] | CrowdfundingTestArgs['tuple']['totalPledged()uint512']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(CrowdfundingTestParamsFactory.totalPledged(params))
      return {...result, return: result.return as unknown as (undefined | CrowdfundingTestReturns['totalPledged()uint512'])}
    },

    /**
     * Makes a call to the CrowdfundingTest smart contract using the `claimed()bool` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    claimed: async (params: CallParams<CrowdfundingTestArgs['obj']['claimed()bool'] | CrowdfundingTestArgs['tuple']['claimed()bool']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(CrowdfundingTestParamsFactory.claimed(params))
      return {...result, return: result.return as unknown as (undefined | CrowdfundingTestReturns['claimed()bool'])}
    },

    /**
     * Makes a call to the CrowdfundingTest smart contract using the `backerCount()uint512` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    backerCount: async (params: CallParams<CrowdfundingTestArgs['obj']['backerCount()uint512'] | CrowdfundingTestArgs['tuple']['backerCount()uint512']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(CrowdfundingTestParamsFactory.backerCount(params))
      return {...result, return: result.return as unknown as (undefined | CrowdfundingTestReturns['backerCount()uint512'])}
    },

    /**
     * Makes a call to the CrowdfundingTest smart contract using the `pledgeOf(address)uint512` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    pledgeOf: async (params: CallParams<CrowdfundingTestArgs['obj']['pledgeOf(address)uint512'] | CrowdfundingTestArgs['tuple']['pledgeOf(address)uint512']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(CrowdfundingTestParamsFactory.pledgeOf(params))
      return {...result, return: result.return as unknown as (undefined | CrowdfundingTestReturns['pledgeOf(address)uint512'])}
    },

    /**
     * Makes a call to the CrowdfundingTest smart contract using the `isGoalReached()bool` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    isGoalReached: async (params: CallParams<CrowdfundingTestArgs['obj']['isGoalReached()bool'] | CrowdfundingTestArgs['tuple']['isGoalReached()bool']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(CrowdfundingTestParamsFactory.isGoalReached(params))
      return {...result, return: result.return as unknown as (undefined | CrowdfundingTestReturns['isGoalReached()bool'])}
    },

    /**
     * Makes a call to the CrowdfundingTest smart contract using the `pledge(address,uint512,uint512)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    pledge: async (params: CallParams<CrowdfundingTestArgs['obj']['pledge(address,uint512,uint512)void'] | CrowdfundingTestArgs['tuple']['pledge(address,uint512,uint512)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(CrowdfundingTestParamsFactory.pledge(params))
      return {...result, return: result.return as unknown as (undefined | CrowdfundingTestReturns['pledge(address,uint512,uint512)void'])}
    },

    /**
     * Makes a call to the CrowdfundingTest smart contract using the `unpledge(address,uint512)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    unpledge: async (params: CallParams<CrowdfundingTestArgs['obj']['unpledge(address,uint512)void'] | CrowdfundingTestArgs['tuple']['unpledge(address,uint512)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(CrowdfundingTestParamsFactory.unpledge(params))
      return {...result, return: result.return as unknown as (undefined | CrowdfundingTestReturns['unpledge(address,uint512)void'])}
    },

    /**
     * Makes a call to the CrowdfundingTest smart contract using the `claim()void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    claim: async (params: CallParams<CrowdfundingTestArgs['obj']['claim()void'] | CrowdfundingTestArgs['tuple']['claim()void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(CrowdfundingTestParamsFactory.claim(params))
      return {...result, return: result.return as unknown as (undefined | CrowdfundingTestReturns['claim()void'])}
    },

    /**
     * Makes a call to the CrowdfundingTest smart contract using the `refund(address)uint512` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    refund: async (params: CallParams<CrowdfundingTestArgs['obj']['refund(address)uint512'] | CrowdfundingTestArgs['tuple']['refund(address)uint512']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(CrowdfundingTestParamsFactory.refund(params))
      return {...result, return: result.return as unknown as (undefined | CrowdfundingTestReturns['refund(address)uint512'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new CrowdfundingTestClient(this.appClient.clone(params))
  }

  /**
   * Makes a readonly (simulated) call to the CrowdfundingTest smart contract using the `creator()address` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async creator(params: CallParams<CrowdfundingTestArgs['obj']['creator()address'] | CrowdfundingTestArgs['tuple']['creator()address']> = {args: []}) {
    const result = await this.appClient.send.call(CrowdfundingTestParamsFactory.creator(params))
    return result.return as unknown as CrowdfundingTestReturns['creator()address']
  }

  /**
   * Makes a readonly (simulated) call to the CrowdfundingTest smart contract using the `goal()uint512` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async goal(params: CallParams<CrowdfundingTestArgs['obj']['goal()uint512'] | CrowdfundingTestArgs['tuple']['goal()uint512']> = {args: []}) {
    const result = await this.appClient.send.call(CrowdfundingTestParamsFactory.goal(params))
    return result.return as unknown as CrowdfundingTestReturns['goal()uint512']
  }

  /**
   * Makes a readonly (simulated) call to the CrowdfundingTest smart contract using the `deadline()uint512` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async deadline(params: CallParams<CrowdfundingTestArgs['obj']['deadline()uint512'] | CrowdfundingTestArgs['tuple']['deadline()uint512']> = {args: []}) {
    const result = await this.appClient.send.call(CrowdfundingTestParamsFactory.deadline(params))
    return result.return as unknown as CrowdfundingTestReturns['deadline()uint512']
  }

  /**
   * Makes a readonly (simulated) call to the CrowdfundingTest smart contract using the `totalPledged()uint512` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async totalPledged(params: CallParams<CrowdfundingTestArgs['obj']['totalPledged()uint512'] | CrowdfundingTestArgs['tuple']['totalPledged()uint512']> = {args: []}) {
    const result = await this.appClient.send.call(CrowdfundingTestParamsFactory.totalPledged(params))
    return result.return as unknown as CrowdfundingTestReturns['totalPledged()uint512']
  }

  /**
   * Makes a readonly (simulated) call to the CrowdfundingTest smart contract using the `claimed()bool` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async claimed(params: CallParams<CrowdfundingTestArgs['obj']['claimed()bool'] | CrowdfundingTestArgs['tuple']['claimed()bool']> = {args: []}) {
    const result = await this.appClient.send.call(CrowdfundingTestParamsFactory.claimed(params))
    return result.return as unknown as CrowdfundingTestReturns['claimed()bool']
  }

  /**
   * Makes a readonly (simulated) call to the CrowdfundingTest smart contract using the `backerCount()uint512` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async backerCount(params: CallParams<CrowdfundingTestArgs['obj']['backerCount()uint512'] | CrowdfundingTestArgs['tuple']['backerCount()uint512']> = {args: []}) {
    const result = await this.appClient.send.call(CrowdfundingTestParamsFactory.backerCount(params))
    return result.return as unknown as CrowdfundingTestReturns['backerCount()uint512']
  }

  /**
   * Makes a readonly (simulated) call to the CrowdfundingTest smart contract using the `pledgeOf(address)uint512` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async pledgeOf(params: CallParams<CrowdfundingTestArgs['obj']['pledgeOf(address)uint512'] | CrowdfundingTestArgs['tuple']['pledgeOf(address)uint512']>) {
    const result = await this.appClient.send.call(CrowdfundingTestParamsFactory.pledgeOf(params))
    return result.return as unknown as CrowdfundingTestReturns['pledgeOf(address)uint512']
  }

  /**
   * Makes a readonly (simulated) call to the CrowdfundingTest smart contract using the `isGoalReached()bool` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async isGoalReached(params: CallParams<CrowdfundingTestArgs['obj']['isGoalReached()bool'] | CrowdfundingTestArgs['tuple']['isGoalReached()bool']> = {args: []}) {
    const result = await this.appClient.send.call(CrowdfundingTestParamsFactory.isGoalReached(params))
    return result.return as unknown as CrowdfundingTestReturns['isGoalReached()bool']
  }

  /**
   * Methods to access state for the current CrowdfundingTest app
   */
  state = {
    /**
     * Methods to access global state for the current CrowdfundingTest app
     */
    global: {
      /**
       * Get all current keyed values from global state
       */
      getAll: async (): Promise<Partial<Expand<GlobalKeysState>>> => {
        const result = await this.appClient.state.global.getAll()
        return {
          _creator: result._creator,
          _goal: new BinaryStateValue(result._goal),
          _deadline: new BinaryStateValue(result._deadline),
          _totalPledged: new BinaryStateValue(result._totalPledged),
          _claimed: result._claimed,
          _backerCount: new BinaryStateValue(result._backerCount),
        }
      },
      /**
       * Get the current value of the _creator key in global state
       */
      _creator: async (): Promise<string | undefined> => { return (await this.appClient.state.global.getValue("_creator")) as string | undefined },
      /**
       * Get the current value of the _goal key in global state
       */
      _goal: async (): Promise<BinaryState> => { return new BinaryStateValue((await this.appClient.state.global.getValue("_goal")) as Uint8Array | undefined) },
      /**
       * Get the current value of the _deadline key in global state
       */
      _deadline: async (): Promise<BinaryState> => { return new BinaryStateValue((await this.appClient.state.global.getValue("_deadline")) as Uint8Array | undefined) },
      /**
       * Get the current value of the _totalPledged key in global state
       */
      _totalPledged: async (): Promise<BinaryState> => { return new BinaryStateValue((await this.appClient.state.global.getValue("_totalPledged")) as Uint8Array | undefined) },
      /**
       * Get the current value of the _claimed key in global state
       */
      _claimed: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("_claimed")) as bigint | undefined },
      /**
       * Get the current value of the _backerCount key in global state
       */
      _backerCount: async (): Promise<BinaryState> => { return new BinaryStateValue((await this.appClient.state.global.getValue("_backerCount")) as Uint8Array | undefined) },
    },
    /**
     * Methods to access box state for the current CrowdfundingTest app
     */
    box: {
      /**
       * Get all current keyed values from box state
       */
      getAll: async (): Promise<Partial<Expand<BoxKeysState>>> => {
        const result = await this.appClient.state.box.getAll()
        return {
        }
      },
      /**
       * Get values from the _pledges map in box state
       */
      _pledges: {
        /**
         * Get all current values of the _pledges map in box state
         */
        getMap: async (): Promise<Map<Uint8Array, Uint8Array>> => { return (await this.appClient.state.box.getMap("_pledges")) as Map<Uint8Array, Uint8Array> },
        /**
         * Get a current value of the _pledges map by key from box state
         */
        value: async (key: Uint8Array | string): Promise<Uint8Array | undefined> => { return await this.appClient.state.box.getMapValue("_pledges", key) as Uint8Array | undefined },
      },
    },
  }

  public newGroup(): CrowdfundingTestComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a creator()address method call against the CrowdfundingTest contract
       */
      creator(params: CallParams<CrowdfundingTestArgs['obj']['creator()address'] | CrowdfundingTestArgs['tuple']['creator()address']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.creator(params)))
        resultMappers.push((v) => client.decodeReturnValue('creator()address', v))
        return this
      },
      /**
       * Add a goal()uint512 method call against the CrowdfundingTest contract
       */
      goal(params: CallParams<CrowdfundingTestArgs['obj']['goal()uint512'] | CrowdfundingTestArgs['tuple']['goal()uint512']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.goal(params)))
        resultMappers.push((v) => client.decodeReturnValue('goal()uint512', v))
        return this
      },
      /**
       * Add a deadline()uint512 method call against the CrowdfundingTest contract
       */
      deadline(params: CallParams<CrowdfundingTestArgs['obj']['deadline()uint512'] | CrowdfundingTestArgs['tuple']['deadline()uint512']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.deadline(params)))
        resultMappers.push((v) => client.decodeReturnValue('deadline()uint512', v))
        return this
      },
      /**
       * Add a totalPledged()uint512 method call against the CrowdfundingTest contract
       */
      totalPledged(params: CallParams<CrowdfundingTestArgs['obj']['totalPledged()uint512'] | CrowdfundingTestArgs['tuple']['totalPledged()uint512']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.totalPledged(params)))
        resultMappers.push((v) => client.decodeReturnValue('totalPledged()uint512', v))
        return this
      },
      /**
       * Add a claimed()bool method call against the CrowdfundingTest contract
       */
      claimed(params: CallParams<CrowdfundingTestArgs['obj']['claimed()bool'] | CrowdfundingTestArgs['tuple']['claimed()bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.claimed(params)))
        resultMappers.push((v) => client.decodeReturnValue('claimed()bool', v))
        return this
      },
      /**
       * Add a backerCount()uint512 method call against the CrowdfundingTest contract
       */
      backerCount(params: CallParams<CrowdfundingTestArgs['obj']['backerCount()uint512'] | CrowdfundingTestArgs['tuple']['backerCount()uint512']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.backerCount(params)))
        resultMappers.push((v) => client.decodeReturnValue('backerCount()uint512', v))
        return this
      },
      /**
       * Add a pledgeOf(address)uint512 method call against the CrowdfundingTest contract
       */
      pledgeOf(params: CallParams<CrowdfundingTestArgs['obj']['pledgeOf(address)uint512'] | CrowdfundingTestArgs['tuple']['pledgeOf(address)uint512']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.pledgeOf(params)))
        resultMappers.push((v) => client.decodeReturnValue('pledgeOf(address)uint512', v))
        return this
      },
      /**
       * Add a isGoalReached()bool method call against the CrowdfundingTest contract
       */
      isGoalReached(params: CallParams<CrowdfundingTestArgs['obj']['isGoalReached()bool'] | CrowdfundingTestArgs['tuple']['isGoalReached()bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.isGoalReached(params)))
        resultMappers.push((v) => client.decodeReturnValue('isGoalReached()bool', v))
        return this
      },
      /**
       * Add a pledge(address,uint512,uint512)void method call against the CrowdfundingTest contract
       */
      pledge(params: CallParams<CrowdfundingTestArgs['obj']['pledge(address,uint512,uint512)void'] | CrowdfundingTestArgs['tuple']['pledge(address,uint512,uint512)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.pledge(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a unpledge(address,uint512)void method call against the CrowdfundingTest contract
       */
      unpledge(params: CallParams<CrowdfundingTestArgs['obj']['unpledge(address,uint512)void'] | CrowdfundingTestArgs['tuple']['unpledge(address,uint512)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.unpledge(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a claim()void method call against the CrowdfundingTest contract
       */
      claim(params: CallParams<CrowdfundingTestArgs['obj']['claim()void'] | CrowdfundingTestArgs['tuple']['claim()void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.claim(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a refund(address)uint512 method call against the CrowdfundingTest contract
       */
      refund(params: CallParams<CrowdfundingTestArgs['obj']['refund(address)uint512'] | CrowdfundingTestArgs['tuple']['refund(address)uint512']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.refund(params)))
        resultMappers.push((v) => client.decodeReturnValue('refund(address)uint512', v))
        return this
      },
      /**
       * Add a clear state call to the CrowdfundingTest contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as CrowdfundingTestComposer
  }
}
export type CrowdfundingTestComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the creator()address ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  creator(params?: CallParams<CrowdfundingTestArgs['obj']['creator()address'] | CrowdfundingTestArgs['tuple']['creator()address']>): CrowdfundingTestComposer<[...TReturns, CrowdfundingTestReturns['creator()address'] | undefined]>

  /**
   * Calls the goal()uint512 ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  goal(params?: CallParams<CrowdfundingTestArgs['obj']['goal()uint512'] | CrowdfundingTestArgs['tuple']['goal()uint512']>): CrowdfundingTestComposer<[...TReturns, CrowdfundingTestReturns['goal()uint512'] | undefined]>

  /**
   * Calls the deadline()uint512 ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  deadline(params?: CallParams<CrowdfundingTestArgs['obj']['deadline()uint512'] | CrowdfundingTestArgs['tuple']['deadline()uint512']>): CrowdfundingTestComposer<[...TReturns, CrowdfundingTestReturns['deadline()uint512'] | undefined]>

  /**
   * Calls the totalPledged()uint512 ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  totalPledged(params?: CallParams<CrowdfundingTestArgs['obj']['totalPledged()uint512'] | CrowdfundingTestArgs['tuple']['totalPledged()uint512']>): CrowdfundingTestComposer<[...TReturns, CrowdfundingTestReturns['totalPledged()uint512'] | undefined]>

  /**
   * Calls the claimed()bool ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  claimed(params?: CallParams<CrowdfundingTestArgs['obj']['claimed()bool'] | CrowdfundingTestArgs['tuple']['claimed()bool']>): CrowdfundingTestComposer<[...TReturns, CrowdfundingTestReturns['claimed()bool'] | undefined]>

  /**
   * Calls the backerCount()uint512 ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  backerCount(params?: CallParams<CrowdfundingTestArgs['obj']['backerCount()uint512'] | CrowdfundingTestArgs['tuple']['backerCount()uint512']>): CrowdfundingTestComposer<[...TReturns, CrowdfundingTestReturns['backerCount()uint512'] | undefined]>

  /**
   * Calls the pledgeOf(address)uint512 ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  pledgeOf(params?: CallParams<CrowdfundingTestArgs['obj']['pledgeOf(address)uint512'] | CrowdfundingTestArgs['tuple']['pledgeOf(address)uint512']>): CrowdfundingTestComposer<[...TReturns, CrowdfundingTestReturns['pledgeOf(address)uint512'] | undefined]>

  /**
   * Calls the isGoalReached()bool ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  isGoalReached(params?: CallParams<CrowdfundingTestArgs['obj']['isGoalReached()bool'] | CrowdfundingTestArgs['tuple']['isGoalReached()bool']>): CrowdfundingTestComposer<[...TReturns, CrowdfundingTestReturns['isGoalReached()bool'] | undefined]>

  /**
   * Calls the pledge(address,uint512,uint512)void ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  pledge(params?: CallParams<CrowdfundingTestArgs['obj']['pledge(address,uint512,uint512)void'] | CrowdfundingTestArgs['tuple']['pledge(address,uint512,uint512)void']>): CrowdfundingTestComposer<[...TReturns, CrowdfundingTestReturns['pledge(address,uint512,uint512)void'] | undefined]>

  /**
   * Calls the unpledge(address,uint512)void ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  unpledge(params?: CallParams<CrowdfundingTestArgs['obj']['unpledge(address,uint512)void'] | CrowdfundingTestArgs['tuple']['unpledge(address,uint512)void']>): CrowdfundingTestComposer<[...TReturns, CrowdfundingTestReturns['unpledge(address,uint512)void'] | undefined]>

  /**
   * Calls the claim()void ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  claim(params?: CallParams<CrowdfundingTestArgs['obj']['claim()void'] | CrowdfundingTestArgs['tuple']['claim()void']>): CrowdfundingTestComposer<[...TReturns, CrowdfundingTestReturns['claim()void'] | undefined]>

  /**
   * Calls the refund(address)uint512 ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  refund(params?: CallParams<CrowdfundingTestArgs['obj']['refund(address)uint512'] | CrowdfundingTestArgs['tuple']['refund(address)uint512']>): CrowdfundingTestComposer<[...TReturns, CrowdfundingTestReturns['refund(address)uint512'] | undefined]>

  /**
   * Makes a clear_state call to an existing instance of the CrowdfundingTest smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): CrowdfundingTestComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): CrowdfundingTestComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<CrowdfundingTestComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<CrowdfundingTestComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<CrowdfundingTestComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<CrowdfundingTestComposerResults<TReturns>>
}
export type CrowdfundingTestComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

