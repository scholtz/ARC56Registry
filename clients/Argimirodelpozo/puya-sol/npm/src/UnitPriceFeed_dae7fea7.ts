/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"UnitPriceFeed","structs":{"getRoundDataReturn":[{"name":"roundId","type":"uint512"},{"name":"answer","type":"uint512"},{"name":"startedAt","type":"uint512"},{"name":"updatedAt","type":"uint512"},{"name":"answeredInRound","type":"uint512"}],"latestRoundDataReturn":[{"name":"roundId","type":"uint512"},{"name":"answer","type":"uint512"},{"name":"startedAt","type":"uint512"},{"name":"updatedAt","type":"uint512"},{"name":"answeredInRound","type":"uint512"}]},"methods":[{"name":"version","args":[],"returns":{"type":"uint256"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"desc":"@inheritdoc AggregatorV3Interface","events":[],"recommendations":{}},{"name":"getRoundData","args":[{"type":"uint80","name":"__arc4__roundId"}],"returns":{"type":"(uint512,uint512,uint512,uint512,uint512)","struct":"getRoundDataReturn"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"desc":"@inheritdoc AggregatorV3Interface","events":[],"recommendations":{}},{"name":"latestRoundData","args":[],"returns":{"type":"(uint512,uint512,uint512,uint512,uint512)","struct":"latestRoundDataReturn"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"desc":"@inheritdoc AggregatorV3Interface","events":[],"recommendations":{}},{"name":"decimals","args":[],"returns":{"type":"uint64"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"desc":"@inheritdoc AggregatorV3Interface","events":[],"recommendations":{}},{"name":"description","args":[],"returns":{"type":"string"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}}],"arcs":[22,28],"desc":"@title UnitPriceFeed contract\n @author Aave Labs\n @notice Price feed that returns the unit price (1), with decimals precision.\n @dev This price feed can be set for reserves that use the base currency as collateral.","networks":{},"state":{"schema":{"global":{"ints":1,"bytes":2},"local":{"ints":0,"bytes":0}},"keys":{"global":{"description":{"keyType":"AVMString","valueType":"AVMString","key":"ZGVzY3JpcHRpb24="},"DECIMALS":{"keyType":"AVMString","valueType":"AVMUint64","key":"REVDSU1BTFM="},"UNITS":{"keyType":"AVMString","valueType":"AVMBytes","key":"VU5JVFM="}},"local":{},"box":{}},"maps":{"global":{},"local":{},"box":{}}},"bareActions":{"create":[],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[647],"errorMessage":"SafeCastOverflowedUintToInt"},{"pc":[313],"errorMessage":"check DECIMALS exists"},{"pc":[351,467],"errorMessage":"check UNITS exists"},{"pc":[277],"errorMessage":"check description exists"},{"pc":[420],"errorMessage":"invalid number of bytes for arc4.uint80"},{"pc":[273,308,341,433,560],"errorMessage":"not payable"},{"pc":[363,376,487,500,511,522,533],"errorMessage":"overflow"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"events":[]} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


// Type definitions for ARC-56 structs

export type GetRoundDataReturn = {
  roundId: bigint,
  answer: bigint,
  startedAt: bigint,
  updatedAt: bigint,
  answeredInRound: bigint
}


/**
 * Converts the ABI tuple representation of a getRoundDataReturn to the struct representation
 */
export function GetRoundDataReturnFromTuple(abiTuple: [bigint, bigint, bigint, bigint, bigint]) {
  return getABIStructFromABITuple(abiTuple, APP_SPEC.structs.getRoundDataReturn, APP_SPEC.structs) as GetRoundDataReturn
}

export type LatestRoundDataReturn = {
  roundId: bigint,
  answer: bigint,
  startedAt: bigint,
  updatedAt: bigint,
  answeredInRound: bigint
}


/**
 * Converts the ABI tuple representation of a latestRoundDataReturn to the struct representation
 */
export function LatestRoundDataReturnFromTuple(abiTuple: [bigint, bigint, bigint, bigint, bigint]) {
  return getABIStructFromABITuple(abiTuple, APP_SPEC.structs.latestRoundDataReturn, APP_SPEC.structs) as LatestRoundDataReturn
}

/**
 * The argument types for the UnitPriceFeed contract
 */
export type UnitPriceFeedArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'version()uint256': Record<string, never>
    'getRoundData(uint80)(uint512,uint512,uint512,uint512,uint512)': {
      __arc4RoundId: bigint | number
    }
    'latestRoundData()(uint512,uint512,uint512,uint512,uint512)': Record<string, never>
    'decimals()uint64': Record<string, never>
    'description()string': Record<string, never>
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'version()uint256': []
    'getRoundData(uint80)(uint512,uint512,uint512,uint512,uint512)': [__arc4RoundId: bigint | number]
    'latestRoundData()(uint512,uint512,uint512,uint512,uint512)': []
    'decimals()uint64': []
    'description()string': []
  }
}

/**
 * The return type for each method
 */
export type UnitPriceFeedReturns = {
  'version()uint256': bigint
  'getRoundData(uint80)(uint512,uint512,uint512,uint512,uint512)': GetRoundDataReturn
  'latestRoundData()(uint512,uint512,uint512,uint512,uint512)': LatestRoundDataReturn
  'decimals()uint64': bigint
  'description()string': string
}

/**
 * Defines the types of available calls and state of the UnitPriceFeed smart contract.
 */
export type UnitPriceFeedTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'version()uint256' | 'version', {
      argsObj: UnitPriceFeedArgs['obj']['version()uint256']
      argsTuple: UnitPriceFeedArgs['tuple']['version()uint256']
      returns: UnitPriceFeedReturns['version()uint256']
    }>
    & Record<'getRoundData(uint80)(uint512,uint512,uint512,uint512,uint512)' | 'getRoundData', {
      argsObj: UnitPriceFeedArgs['obj']['getRoundData(uint80)(uint512,uint512,uint512,uint512,uint512)']
      argsTuple: UnitPriceFeedArgs['tuple']['getRoundData(uint80)(uint512,uint512,uint512,uint512,uint512)']
      returns: UnitPriceFeedReturns['getRoundData(uint80)(uint512,uint512,uint512,uint512,uint512)']
    }>
    & Record<'latestRoundData()(uint512,uint512,uint512,uint512,uint512)' | 'latestRoundData', {
      argsObj: UnitPriceFeedArgs['obj']['latestRoundData()(uint512,uint512,uint512,uint512,uint512)']
      argsTuple: UnitPriceFeedArgs['tuple']['latestRoundData()(uint512,uint512,uint512,uint512,uint512)']
      returns: UnitPriceFeedReturns['latestRoundData()(uint512,uint512,uint512,uint512,uint512)']
    }>
    & Record<'decimals()uint64' | 'decimals', {
      argsObj: UnitPriceFeedArgs['obj']['decimals()uint64']
      argsTuple: UnitPriceFeedArgs['tuple']['decimals()uint64']
      returns: UnitPriceFeedReturns['decimals()uint64']
    }>
    & Record<'description()string' | 'description', {
      argsObj: UnitPriceFeedArgs['obj']['description()string']
      argsTuple: UnitPriceFeedArgs['tuple']['description()string']
      returns: UnitPriceFeedReturns['description()string']
    }>
  /**
   * Defines the shape of the state of the application.
   */
  state: {
    global: {
      keys: {
        description: string
        decimals: bigint
        units: BinaryState
      }
      maps: {}
    }
  }
}

/**
 * Defines the possible abi call signatures.
 */
export type UnitPriceFeedSignatures = keyof UnitPriceFeedTypes['methods']
/**
 * Defines the possible abi call signatures for methods that return a non-void value.
 */
export type UnitPriceFeedNonVoidMethodSignatures = keyof UnitPriceFeedTypes['methods'] extends infer T ? T extends keyof UnitPriceFeedTypes['methods'] ? MethodReturn<T> extends void ? never : T  : never : never
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the UnitPriceFeed smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends UnitPriceFeedSignatures> = UnitPriceFeedTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the UnitPriceFeed smart contract to the method's return type
 */
export type MethodReturn<TSignature extends UnitPriceFeedSignatures> = UnitPriceFeedTypes['methods'][TSignature]['returns']

/**
 * Defines the shape of the keyed global state of the application.
 */
export type GlobalKeysState = UnitPriceFeedTypes['state']['global']['keys']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the UnitPriceFeed smart contract
 */
export abstract class UnitPriceFeedParamsFactory {
  /**
   * Constructs a no op call for the version()uint256 ABI method
   *
   * @inheritdoc AggregatorV3Interface
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static version(params: CallParams<UnitPriceFeedArgs['obj']['version()uint256'] | UnitPriceFeedArgs['tuple']['version()uint256']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'version()uint256' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the getRoundData(uint80)(uint512,uint512,uint512,uint512,uint512) ABI method
   *
   * @inheritdoc AggregatorV3Interface
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static getRoundData(params: CallParams<UnitPriceFeedArgs['obj']['getRoundData(uint80)(uint512,uint512,uint512,uint512,uint512)'] | UnitPriceFeedArgs['tuple']['getRoundData(uint80)(uint512,uint512,uint512,uint512,uint512)']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'getRoundData(uint80)(uint512,uint512,uint512,uint512,uint512)' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.__arc4RoundId],
    }
  }
  /**
   * Constructs a no op call for the latestRoundData()(uint512,uint512,uint512,uint512,uint512) ABI method
   *
   * @inheritdoc AggregatorV3Interface
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static latestRoundData(params: CallParams<UnitPriceFeedArgs['obj']['latestRoundData()(uint512,uint512,uint512,uint512,uint512)'] | UnitPriceFeedArgs['tuple']['latestRoundData()(uint512,uint512,uint512,uint512,uint512)']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'latestRoundData()(uint512,uint512,uint512,uint512,uint512)' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the decimals()uint64 ABI method
   *
   * @inheritdoc AggregatorV3Interface
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static decimals(params: CallParams<UnitPriceFeedArgs['obj']['decimals()uint64'] | UnitPriceFeedArgs['tuple']['decimals()uint64']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'decimals()uint64' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the description()string ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static description(params: CallParams<UnitPriceFeedArgs['obj']['description()string'] | UnitPriceFeedArgs['tuple']['description()string']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'description()string' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
}

/**
 * A client to make calls to the UnitPriceFeed smart contract
 */
export class UnitPriceFeedClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `UnitPriceFeedClient`
   *
   * @param appClient An `AppClient` instance which has been created with the UnitPriceFeed app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `UnitPriceFeedClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Checks for decode errors on the given return value and maps the return value to the return type for the given method
   * @returns The typed return value or undefined if there was no value
   */
  decodeReturnValue<TSignature extends UnitPriceFeedNonVoidMethodSignatures>(method: TSignature, returnValue: ABIReturn | undefined) {
    return returnValue !== undefined ? getArc56ReturnValue<MethodReturn<TSignature>>(returnValue, this.appClient.getABIMethod(method), APP_SPEC.structs) : undefined
  }

  /**
   * Returns a new `UnitPriceFeedClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<UnitPriceFeedClient> {
    return new UnitPriceFeedClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `UnitPriceFeedClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<UnitPriceFeedClient> {
    return new UnitPriceFeedClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Makes a clear_state call to an existing instance of the UnitPriceFeed smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the UnitPriceFeed smart contract using the `version()uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @inheritdoc AggregatorV3Interface
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    version: (params: CallParams<UnitPriceFeedArgs['obj']['version()uint256'] | UnitPriceFeedArgs['tuple']['version()uint256']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(UnitPriceFeedParamsFactory.version(params))
    },

    /**
     * Makes a call to the UnitPriceFeed smart contract using the `getRoundData(uint80)(uint512,uint512,uint512,uint512,uint512)` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @inheritdoc AggregatorV3Interface
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    getRoundData: (params: CallParams<UnitPriceFeedArgs['obj']['getRoundData(uint80)(uint512,uint512,uint512,uint512,uint512)'] | UnitPriceFeedArgs['tuple']['getRoundData(uint80)(uint512,uint512,uint512,uint512,uint512)']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(UnitPriceFeedParamsFactory.getRoundData(params))
    },

    /**
     * Makes a call to the UnitPriceFeed smart contract using the `latestRoundData()(uint512,uint512,uint512,uint512,uint512)` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @inheritdoc AggregatorV3Interface
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    latestRoundData: (params: CallParams<UnitPriceFeedArgs['obj']['latestRoundData()(uint512,uint512,uint512,uint512,uint512)'] | UnitPriceFeedArgs['tuple']['latestRoundData()(uint512,uint512,uint512,uint512,uint512)']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(UnitPriceFeedParamsFactory.latestRoundData(params))
    },

    /**
     * Makes a call to the UnitPriceFeed smart contract using the `decimals()uint64` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @inheritdoc AggregatorV3Interface
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    decimals: (params: CallParams<UnitPriceFeedArgs['obj']['decimals()uint64'] | UnitPriceFeedArgs['tuple']['decimals()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(UnitPriceFeedParamsFactory.decimals(params))
    },

    /**
     * Makes a call to the UnitPriceFeed smart contract using the `description()string` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    description: (params: CallParams<UnitPriceFeedArgs['obj']['description()string'] | UnitPriceFeedArgs['tuple']['description()string']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(UnitPriceFeedParamsFactory.description(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Makes a clear_state call to an existing instance of the UnitPriceFeed smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the UnitPriceFeed smart contract using the `version()uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @inheritdoc AggregatorV3Interface
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    version: (params: CallParams<UnitPriceFeedArgs['obj']['version()uint256'] | UnitPriceFeedArgs['tuple']['version()uint256']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(UnitPriceFeedParamsFactory.version(params))
    },

    /**
     * Makes a call to the UnitPriceFeed smart contract using the `getRoundData(uint80)(uint512,uint512,uint512,uint512,uint512)` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @inheritdoc AggregatorV3Interface
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    getRoundData: (params: CallParams<UnitPriceFeedArgs['obj']['getRoundData(uint80)(uint512,uint512,uint512,uint512,uint512)'] | UnitPriceFeedArgs['tuple']['getRoundData(uint80)(uint512,uint512,uint512,uint512,uint512)']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(UnitPriceFeedParamsFactory.getRoundData(params))
    },

    /**
     * Makes a call to the UnitPriceFeed smart contract using the `latestRoundData()(uint512,uint512,uint512,uint512,uint512)` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @inheritdoc AggregatorV3Interface
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    latestRoundData: (params: CallParams<UnitPriceFeedArgs['obj']['latestRoundData()(uint512,uint512,uint512,uint512,uint512)'] | UnitPriceFeedArgs['tuple']['latestRoundData()(uint512,uint512,uint512,uint512,uint512)']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(UnitPriceFeedParamsFactory.latestRoundData(params))
    },

    /**
     * Makes a call to the UnitPriceFeed smart contract using the `decimals()uint64` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @inheritdoc AggregatorV3Interface
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    decimals: (params: CallParams<UnitPriceFeedArgs['obj']['decimals()uint64'] | UnitPriceFeedArgs['tuple']['decimals()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(UnitPriceFeedParamsFactory.decimals(params))
    },

    /**
     * Makes a call to the UnitPriceFeed smart contract using the `description()string` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    description: (params: CallParams<UnitPriceFeedArgs['obj']['description()string'] | UnitPriceFeedArgs['tuple']['description()string']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(UnitPriceFeedParamsFactory.description(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Makes a clear_state call to an existing instance of the UnitPriceFeed smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the UnitPriceFeed smart contract using the `version()uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @inheritdoc AggregatorV3Interface
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    version: async (params: CallParams<UnitPriceFeedArgs['obj']['version()uint256'] | UnitPriceFeedArgs['tuple']['version()uint256']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(UnitPriceFeedParamsFactory.version(params))
      return {...result, return: result.return as unknown as (undefined | UnitPriceFeedReturns['version()uint256'])}
    },

    /**
     * Makes a call to the UnitPriceFeed smart contract using the `getRoundData(uint80)(uint512,uint512,uint512,uint512,uint512)` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @inheritdoc AggregatorV3Interface
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    getRoundData: async (params: CallParams<UnitPriceFeedArgs['obj']['getRoundData(uint80)(uint512,uint512,uint512,uint512,uint512)'] | UnitPriceFeedArgs['tuple']['getRoundData(uint80)(uint512,uint512,uint512,uint512,uint512)']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(UnitPriceFeedParamsFactory.getRoundData(params))
      return {...result, return: result.return as unknown as (undefined | UnitPriceFeedReturns['getRoundData(uint80)(uint512,uint512,uint512,uint512,uint512)'])}
    },

    /**
     * Makes a call to the UnitPriceFeed smart contract using the `latestRoundData()(uint512,uint512,uint512,uint512,uint512)` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @inheritdoc AggregatorV3Interface
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    latestRoundData: async (params: CallParams<UnitPriceFeedArgs['obj']['latestRoundData()(uint512,uint512,uint512,uint512,uint512)'] | UnitPriceFeedArgs['tuple']['latestRoundData()(uint512,uint512,uint512,uint512,uint512)']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(UnitPriceFeedParamsFactory.latestRoundData(params))
      return {...result, return: result.return as unknown as (undefined | UnitPriceFeedReturns['latestRoundData()(uint512,uint512,uint512,uint512,uint512)'])}
    },

    /**
     * Makes a call to the UnitPriceFeed smart contract using the `decimals()uint64` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @inheritdoc AggregatorV3Interface
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    decimals: async (params: CallParams<UnitPriceFeedArgs['obj']['decimals()uint64'] | UnitPriceFeedArgs['tuple']['decimals()uint64']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(UnitPriceFeedParamsFactory.decimals(params))
      return {...result, return: result.return as unknown as (undefined | UnitPriceFeedReturns['decimals()uint64'])}
    },

    /**
     * Makes a call to the UnitPriceFeed smart contract using the `description()string` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    description: async (params: CallParams<UnitPriceFeedArgs['obj']['description()string'] | UnitPriceFeedArgs['tuple']['description()string']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(UnitPriceFeedParamsFactory.description(params))
      return {...result, return: result.return as unknown as (undefined | UnitPriceFeedReturns['description()string'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new UnitPriceFeedClient(this.appClient.clone(params))
  }

  /**
   * Makes a readonly (simulated) call to the UnitPriceFeed smart contract using the `version()uint256` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @inheritdoc AggregatorV3Interface
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async version(params: CallParams<UnitPriceFeedArgs['obj']['version()uint256'] | UnitPriceFeedArgs['tuple']['version()uint256']> = {args: []}) {
    const result = await this.appClient.send.call(UnitPriceFeedParamsFactory.version(params))
    return result.return as unknown as UnitPriceFeedReturns['version()uint256']
  }

  /**
   * Makes a readonly (simulated) call to the UnitPriceFeed smart contract using the `getRoundData(uint80)(uint512,uint512,uint512,uint512,uint512)` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @inheritdoc AggregatorV3Interface
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async getRoundData(params: CallParams<UnitPriceFeedArgs['obj']['getRoundData(uint80)(uint512,uint512,uint512,uint512,uint512)'] | UnitPriceFeedArgs['tuple']['getRoundData(uint80)(uint512,uint512,uint512,uint512,uint512)']>) {
    const result = await this.appClient.send.call(UnitPriceFeedParamsFactory.getRoundData(params))
    return result.return as unknown as UnitPriceFeedReturns['getRoundData(uint80)(uint512,uint512,uint512,uint512,uint512)']
  }

  /**
   * Makes a readonly (simulated) call to the UnitPriceFeed smart contract using the `latestRoundData()(uint512,uint512,uint512,uint512,uint512)` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @inheritdoc AggregatorV3Interface
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async latestRoundData(params: CallParams<UnitPriceFeedArgs['obj']['latestRoundData()(uint512,uint512,uint512,uint512,uint512)'] | UnitPriceFeedArgs['tuple']['latestRoundData()(uint512,uint512,uint512,uint512,uint512)']> = {args: []}) {
    const result = await this.appClient.send.call(UnitPriceFeedParamsFactory.latestRoundData(params))
    return result.return as unknown as UnitPriceFeedReturns['latestRoundData()(uint512,uint512,uint512,uint512,uint512)']
  }

  /**
   * Makes a readonly (simulated) call to the UnitPriceFeed smart contract using the `decimals()uint64` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @inheritdoc AggregatorV3Interface
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async decimals(params: CallParams<UnitPriceFeedArgs['obj']['decimals()uint64'] | UnitPriceFeedArgs['tuple']['decimals()uint64']> = {args: []}) {
    const result = await this.appClient.send.call(UnitPriceFeedParamsFactory.decimals(params))
    return result.return as unknown as UnitPriceFeedReturns['decimals()uint64']
  }

  /**
   * Makes a readonly (simulated) call to the UnitPriceFeed smart contract using the `description()string` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async description(params: CallParams<UnitPriceFeedArgs['obj']['description()string'] | UnitPriceFeedArgs['tuple']['description()string']> = {args: []}) {
    const result = await this.appClient.send.call(UnitPriceFeedParamsFactory.description(params))
    return result.return as unknown as UnitPriceFeedReturns['description()string']
  }

  /**
   * Methods to access state for the current UnitPriceFeed app
   */
  state = {
    /**
     * Methods to access global state for the current UnitPriceFeed app
     */
    global: {
      /**
       * Get all current keyed values from global state
       */
      getAll: async (): Promise<Partial<Expand<GlobalKeysState>>> => {
        const result = await this.appClient.state.global.getAll()
        return {
          description: result.description,
          decimals: result.DECIMALS,
          units: new BinaryStateValue(result.UNITS),
        }
      },
      /**
       * Get the current value of the description key in global state
       */
      description: async (): Promise<string | undefined> => { return (await this.appClient.state.global.getValue("description")) as string | undefined },
      /**
       * Get the current value of the DECIMALS key in global state
       */
      decimals: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("DECIMALS")) as bigint | undefined },
      /**
       * Get the current value of the UNITS key in global state
       */
      units: async (): Promise<BinaryState> => { return new BinaryStateValue((await this.appClient.state.global.getValue("UNITS")) as Uint8Array | undefined) },
    },
  }

  public newGroup(): UnitPriceFeedComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a version()uint256 method call against the UnitPriceFeed contract
       */
      version(params: CallParams<UnitPriceFeedArgs['obj']['version()uint256'] | UnitPriceFeedArgs['tuple']['version()uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.version(params)))
        resultMappers.push((v) => client.decodeReturnValue('version()uint256', v))
        return this
      },
      /**
       * Add a getRoundData(uint80)(uint512,uint512,uint512,uint512,uint512) method call against the UnitPriceFeed contract
       */
      getRoundData(params: CallParams<UnitPriceFeedArgs['obj']['getRoundData(uint80)(uint512,uint512,uint512,uint512,uint512)'] | UnitPriceFeedArgs['tuple']['getRoundData(uint80)(uint512,uint512,uint512,uint512,uint512)']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.getRoundData(params)))
        resultMappers.push((v) => client.decodeReturnValue('getRoundData(uint80)(uint512,uint512,uint512,uint512,uint512)', v))
        return this
      },
      /**
       * Add a latestRoundData()(uint512,uint512,uint512,uint512,uint512) method call against the UnitPriceFeed contract
       */
      latestRoundData(params: CallParams<UnitPriceFeedArgs['obj']['latestRoundData()(uint512,uint512,uint512,uint512,uint512)'] | UnitPriceFeedArgs['tuple']['latestRoundData()(uint512,uint512,uint512,uint512,uint512)']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.latestRoundData(params)))
        resultMappers.push((v) => client.decodeReturnValue('latestRoundData()(uint512,uint512,uint512,uint512,uint512)', v))
        return this
      },
      /**
       * Add a decimals()uint64 method call against the UnitPriceFeed contract
       */
      decimals(params: CallParams<UnitPriceFeedArgs['obj']['decimals()uint64'] | UnitPriceFeedArgs['tuple']['decimals()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.decimals(params)))
        resultMappers.push((v) => client.decodeReturnValue('decimals()uint64', v))
        return this
      },
      /**
       * Add a description()string method call against the UnitPriceFeed contract
       */
      description(params: CallParams<UnitPriceFeedArgs['obj']['description()string'] | UnitPriceFeedArgs['tuple']['description()string']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.description(params)))
        resultMappers.push((v) => client.decodeReturnValue('description()string', v))
        return this
      },
      /**
       * Add a clear state call to the UnitPriceFeed contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as UnitPriceFeedComposer
  }
}
export type UnitPriceFeedComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the version()uint256 ABI method.
   *
   * @inheritdoc AggregatorV3Interface
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  version(params?: CallParams<UnitPriceFeedArgs['obj']['version()uint256'] | UnitPriceFeedArgs['tuple']['version()uint256']>): UnitPriceFeedComposer<[...TReturns, UnitPriceFeedReturns['version()uint256'] | undefined]>

  /**
   * Calls the getRoundData(uint80)(uint512,uint512,uint512,uint512,uint512) ABI method.
   *
   * @inheritdoc AggregatorV3Interface
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  getRoundData(params?: CallParams<UnitPriceFeedArgs['obj']['getRoundData(uint80)(uint512,uint512,uint512,uint512,uint512)'] | UnitPriceFeedArgs['tuple']['getRoundData(uint80)(uint512,uint512,uint512,uint512,uint512)']>): UnitPriceFeedComposer<[...TReturns, UnitPriceFeedReturns['getRoundData(uint80)(uint512,uint512,uint512,uint512,uint512)'] | undefined]>

  /**
   * Calls the latestRoundData()(uint512,uint512,uint512,uint512,uint512) ABI method.
   *
   * @inheritdoc AggregatorV3Interface
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  latestRoundData(params?: CallParams<UnitPriceFeedArgs['obj']['latestRoundData()(uint512,uint512,uint512,uint512,uint512)'] | UnitPriceFeedArgs['tuple']['latestRoundData()(uint512,uint512,uint512,uint512,uint512)']>): UnitPriceFeedComposer<[...TReturns, UnitPriceFeedReturns['latestRoundData()(uint512,uint512,uint512,uint512,uint512)'] | undefined]>

  /**
   * Calls the decimals()uint64 ABI method.
   *
   * @inheritdoc AggregatorV3Interface
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  decimals(params?: CallParams<UnitPriceFeedArgs['obj']['decimals()uint64'] | UnitPriceFeedArgs['tuple']['decimals()uint64']>): UnitPriceFeedComposer<[...TReturns, UnitPriceFeedReturns['decimals()uint64'] | undefined]>

  /**
   * Calls the description()string ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  description(params?: CallParams<UnitPriceFeedArgs['obj']['description()string'] | UnitPriceFeedArgs['tuple']['description()string']>): UnitPriceFeedComposer<[...TReturns, UnitPriceFeedReturns['description()string'] | undefined]>

  /**
   * Makes a clear_state call to an existing instance of the UnitPriceFeed smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): UnitPriceFeedComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): UnitPriceFeedComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<UnitPriceFeedComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<UnitPriceFeedComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<UnitPriceFeedComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<UnitPriceFeedComposerResults<TReturns>>
}
export type UnitPriceFeedComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

