/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"CertificateIssuerTest","structs":{},"methods":[{"name":"initCert","args":[{"type":"uint512","name":"certId"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}},{"name":"getAdmin","args":[],"returns":{"type":"address"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"getCertCount","args":[],"returns":{"type":"uint512"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"getRevokedCount","args":[],"returns":{"type":"uint512"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"getCertHash","args":[{"type":"uint512","name":"certId"}],"returns":{"type":"uint512"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"issueCertificate","args":[{"type":"uint512","name":"certHash"}],"returns":{"type":"uint512"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}},{"name":"verifyCertificate","args":[{"type":"uint512","name":"certId"}],"returns":{"type":"bool"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"revokeCertificate","args":[{"type":"uint512","name":"certId"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}}],"arcs":[22,28],"networks":{},"state":{"schema":{"global":{"ints":0,"bytes":3},"local":{"ints":0,"bytes":0}},"keys":{"global":{"_admin":{"keyType":"AVMString","valueType":"address","key":"X2FkbWlu"},"_certCount":{"keyType":"AVMString","valueType":"AVMBytes","key":"X2NlcnRDb3VudA=="},"_revokedCount":{"keyType":"AVMString","valueType":"AVMBytes","key":"X3Jldm9rZWRDb3VudA=="}},"local":{},"box":{}},"maps":{"global":{},"local":{},"box":{"_certHash":{"keyType":"AVMBytes","valueType":"AVMBytes","prefix":"X2NlcnRIYXNo"},"_certIssued":{"keyType":"AVMBytes","valueType":"uint64","prefix":"X2NlcnRJc3N1ZWQ="},"_certRevoked":{"keyType":"AVMBytes","valueType":"uint64","prefix":"X2NlcnRSZXZva2Vk"}}}},"bareActions":{"create":[],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[577],"errorMessage":"Certificate already revoked"},{"pc":[452,537],"errorMessage":"Certificate does not exist"},{"pc":[562],"errorMessage":"Certificate not issued"},{"pc":[372,529],"errorMessage":"Only admin"},{"pc":[271,370,527],"errorMessage":"check _admin exists"},{"pc":[281,376,448,533],"errorMessage":"check _certCount exists"},{"pc":[299,584],"errorMessage":"check _revokedCount exists"},{"pc":[222,321,364,444,521],"errorMessage":"invalid number of bytes for arc4.uint512"},{"pc":[286,304,349,429],"errorMessage":"overflow"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"events":[]} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


/**
 * The argument types for the CertificateIssuerTest contract
 */
export type CertificateIssuerTestArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'initCert(uint512)void': {
      certId: bigint | number
    }
    'getAdmin()address': Record<string, never>
    'getCertCount()uint512': Record<string, never>
    'getRevokedCount()uint512': Record<string, never>
    'getCertHash(uint512)uint512': {
      certId: bigint | number
    }
    'issueCertificate(uint512)uint512': {
      certHash: bigint | number
    }
    'verifyCertificate(uint512)bool': {
      certId: bigint | number
    }
    'revokeCertificate(uint512)void': {
      certId: bigint | number
    }
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'initCert(uint512)void': [certId: bigint | number]
    'getAdmin()address': []
    'getCertCount()uint512': []
    'getRevokedCount()uint512': []
    'getCertHash(uint512)uint512': [certId: bigint | number]
    'issueCertificate(uint512)uint512': [certHash: bigint | number]
    'verifyCertificate(uint512)bool': [certId: bigint | number]
    'revokeCertificate(uint512)void': [certId: bigint | number]
  }
}

/**
 * The return type for each method
 */
export type CertificateIssuerTestReturns = {
  'initCert(uint512)void': void
  'getAdmin()address': string
  'getCertCount()uint512': bigint
  'getRevokedCount()uint512': bigint
  'getCertHash(uint512)uint512': bigint
  'issueCertificate(uint512)uint512': bigint
  'verifyCertificate(uint512)bool': boolean
  'revokeCertificate(uint512)void': void
}

/**
 * Defines the types of available calls and state of the CertificateIssuerTest smart contract.
 */
export type CertificateIssuerTestTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'initCert(uint512)void' | 'initCert', {
      argsObj: CertificateIssuerTestArgs['obj']['initCert(uint512)void']
      argsTuple: CertificateIssuerTestArgs['tuple']['initCert(uint512)void']
      returns: CertificateIssuerTestReturns['initCert(uint512)void']
    }>
    & Record<'getAdmin()address' | 'getAdmin', {
      argsObj: CertificateIssuerTestArgs['obj']['getAdmin()address']
      argsTuple: CertificateIssuerTestArgs['tuple']['getAdmin()address']
      returns: CertificateIssuerTestReturns['getAdmin()address']
    }>
    & Record<'getCertCount()uint512' | 'getCertCount', {
      argsObj: CertificateIssuerTestArgs['obj']['getCertCount()uint512']
      argsTuple: CertificateIssuerTestArgs['tuple']['getCertCount()uint512']
      returns: CertificateIssuerTestReturns['getCertCount()uint512']
    }>
    & Record<'getRevokedCount()uint512' | 'getRevokedCount', {
      argsObj: CertificateIssuerTestArgs['obj']['getRevokedCount()uint512']
      argsTuple: CertificateIssuerTestArgs['tuple']['getRevokedCount()uint512']
      returns: CertificateIssuerTestReturns['getRevokedCount()uint512']
    }>
    & Record<'getCertHash(uint512)uint512' | 'getCertHash', {
      argsObj: CertificateIssuerTestArgs['obj']['getCertHash(uint512)uint512']
      argsTuple: CertificateIssuerTestArgs['tuple']['getCertHash(uint512)uint512']
      returns: CertificateIssuerTestReturns['getCertHash(uint512)uint512']
    }>
    & Record<'issueCertificate(uint512)uint512' | 'issueCertificate', {
      argsObj: CertificateIssuerTestArgs['obj']['issueCertificate(uint512)uint512']
      argsTuple: CertificateIssuerTestArgs['tuple']['issueCertificate(uint512)uint512']
      returns: CertificateIssuerTestReturns['issueCertificate(uint512)uint512']
    }>
    & Record<'verifyCertificate(uint512)bool' | 'verifyCertificate', {
      argsObj: CertificateIssuerTestArgs['obj']['verifyCertificate(uint512)bool']
      argsTuple: CertificateIssuerTestArgs['tuple']['verifyCertificate(uint512)bool']
      returns: CertificateIssuerTestReturns['verifyCertificate(uint512)bool']
    }>
    & Record<'revokeCertificate(uint512)void' | 'revokeCertificate', {
      argsObj: CertificateIssuerTestArgs['obj']['revokeCertificate(uint512)void']
      argsTuple: CertificateIssuerTestArgs['tuple']['revokeCertificate(uint512)void']
      returns: CertificateIssuerTestReturns['revokeCertificate(uint512)void']
    }>
  /**
   * Defines the shape of the state of the application.
   */
  state: {
    global: {
      keys: {
        _admin: string
        _certCount: BinaryState
        _revokedCount: BinaryState
      }
      maps: {}
    }
    box: {
      keys: {}
      maps: {
        _certHash: Map<Uint8Array | string, Uint8Array>
        _certIssued: Map<Uint8Array | string, bigint>
        _certRevoked: Map<Uint8Array | string, bigint>
      }
    }
  }
}

/**
 * Defines the possible abi call signatures.
 */
export type CertificateIssuerTestSignatures = keyof CertificateIssuerTestTypes['methods']
/**
 * Defines the possible abi call signatures for methods that return a non-void value.
 */
export type CertificateIssuerTestNonVoidMethodSignatures = keyof CertificateIssuerTestTypes['methods'] extends infer T ? T extends keyof CertificateIssuerTestTypes['methods'] ? MethodReturn<T> extends void ? never : T  : never : never
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the CertificateIssuerTest smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends CertificateIssuerTestSignatures> = CertificateIssuerTestTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the CertificateIssuerTest smart contract to the method's return type
 */
export type MethodReturn<TSignature extends CertificateIssuerTestSignatures> = CertificateIssuerTestTypes['methods'][TSignature]['returns']

/**
 * Defines the shape of the keyed global state of the application.
 */
export type GlobalKeysState = CertificateIssuerTestTypes['state']['global']['keys']

/**
 * Defines the shape of the keyed box state of the application.
 */
export type BoxKeysState = CertificateIssuerTestTypes['state']['box']['keys']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the CertificateIssuerTest smart contract
 */
export abstract class CertificateIssuerTestParamsFactory {
  /**
   * Constructs a no op call for the initCert(uint512)void ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static initCert(params: CallParams<CertificateIssuerTestArgs['obj']['initCert(uint512)void'] | CertificateIssuerTestArgs['tuple']['initCert(uint512)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'initCert(uint512)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.certId],
    }
  }
  /**
   * Constructs a no op call for the getAdmin()address ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static getAdmin(params: CallParams<CertificateIssuerTestArgs['obj']['getAdmin()address'] | CertificateIssuerTestArgs['tuple']['getAdmin()address']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'getAdmin()address' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the getCertCount()uint512 ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static getCertCount(params: CallParams<CertificateIssuerTestArgs['obj']['getCertCount()uint512'] | CertificateIssuerTestArgs['tuple']['getCertCount()uint512']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'getCertCount()uint512' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the getRevokedCount()uint512 ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static getRevokedCount(params: CallParams<CertificateIssuerTestArgs['obj']['getRevokedCount()uint512'] | CertificateIssuerTestArgs['tuple']['getRevokedCount()uint512']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'getRevokedCount()uint512' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the getCertHash(uint512)uint512 ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static getCertHash(params: CallParams<CertificateIssuerTestArgs['obj']['getCertHash(uint512)uint512'] | CertificateIssuerTestArgs['tuple']['getCertHash(uint512)uint512']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'getCertHash(uint512)uint512' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.certId],
    }
  }
  /**
   * Constructs a no op call for the issueCertificate(uint512)uint512 ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static issueCertificate(params: CallParams<CertificateIssuerTestArgs['obj']['issueCertificate(uint512)uint512'] | CertificateIssuerTestArgs['tuple']['issueCertificate(uint512)uint512']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'issueCertificate(uint512)uint512' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.certHash],
    }
  }
  /**
   * Constructs a no op call for the verifyCertificate(uint512)bool ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static verifyCertificate(params: CallParams<CertificateIssuerTestArgs['obj']['verifyCertificate(uint512)bool'] | CertificateIssuerTestArgs['tuple']['verifyCertificate(uint512)bool']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'verifyCertificate(uint512)bool' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.certId],
    }
  }
  /**
   * Constructs a no op call for the revokeCertificate(uint512)void ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static revokeCertificate(params: CallParams<CertificateIssuerTestArgs['obj']['revokeCertificate(uint512)void'] | CertificateIssuerTestArgs['tuple']['revokeCertificate(uint512)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'revokeCertificate(uint512)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.certId],
    }
  }
}

/**
 * A client to make calls to the CertificateIssuerTest smart contract
 */
export class CertificateIssuerTestClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `CertificateIssuerTestClient`
   *
   * @param appClient An `AppClient` instance which has been created with the CertificateIssuerTest app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `CertificateIssuerTestClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Checks for decode errors on the given return value and maps the return value to the return type for the given method
   * @returns The typed return value or undefined if there was no value
   */
  decodeReturnValue<TSignature extends CertificateIssuerTestNonVoidMethodSignatures>(method: TSignature, returnValue: ABIReturn | undefined) {
    return returnValue !== undefined ? getArc56ReturnValue<MethodReturn<TSignature>>(returnValue, this.appClient.getABIMethod(method), APP_SPEC.structs) : undefined
  }

  /**
   * Returns a new `CertificateIssuerTestClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<CertificateIssuerTestClient> {
    return new CertificateIssuerTestClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `CertificateIssuerTestClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<CertificateIssuerTestClient> {
    return new CertificateIssuerTestClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Makes a clear_state call to an existing instance of the CertificateIssuerTest smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the CertificateIssuerTest smart contract using the `initCert(uint512)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    initCert: (params: CallParams<CertificateIssuerTestArgs['obj']['initCert(uint512)void'] | CertificateIssuerTestArgs['tuple']['initCert(uint512)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(CertificateIssuerTestParamsFactory.initCert(params))
    },

    /**
     * Makes a call to the CertificateIssuerTest smart contract using the `getAdmin()address` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    getAdmin: (params: CallParams<CertificateIssuerTestArgs['obj']['getAdmin()address'] | CertificateIssuerTestArgs['tuple']['getAdmin()address']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(CertificateIssuerTestParamsFactory.getAdmin(params))
    },

    /**
     * Makes a call to the CertificateIssuerTest smart contract using the `getCertCount()uint512` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    getCertCount: (params: CallParams<CertificateIssuerTestArgs['obj']['getCertCount()uint512'] | CertificateIssuerTestArgs['tuple']['getCertCount()uint512']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(CertificateIssuerTestParamsFactory.getCertCount(params))
    },

    /**
     * Makes a call to the CertificateIssuerTest smart contract using the `getRevokedCount()uint512` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    getRevokedCount: (params: CallParams<CertificateIssuerTestArgs['obj']['getRevokedCount()uint512'] | CertificateIssuerTestArgs['tuple']['getRevokedCount()uint512']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(CertificateIssuerTestParamsFactory.getRevokedCount(params))
    },

    /**
     * Makes a call to the CertificateIssuerTest smart contract using the `getCertHash(uint512)uint512` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    getCertHash: (params: CallParams<CertificateIssuerTestArgs['obj']['getCertHash(uint512)uint512'] | CertificateIssuerTestArgs['tuple']['getCertHash(uint512)uint512']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(CertificateIssuerTestParamsFactory.getCertHash(params))
    },

    /**
     * Makes a call to the CertificateIssuerTest smart contract using the `issueCertificate(uint512)uint512` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    issueCertificate: (params: CallParams<CertificateIssuerTestArgs['obj']['issueCertificate(uint512)uint512'] | CertificateIssuerTestArgs['tuple']['issueCertificate(uint512)uint512']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(CertificateIssuerTestParamsFactory.issueCertificate(params))
    },

    /**
     * Makes a call to the CertificateIssuerTest smart contract using the `verifyCertificate(uint512)bool` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    verifyCertificate: (params: CallParams<CertificateIssuerTestArgs['obj']['verifyCertificate(uint512)bool'] | CertificateIssuerTestArgs['tuple']['verifyCertificate(uint512)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(CertificateIssuerTestParamsFactory.verifyCertificate(params))
    },

    /**
     * Makes a call to the CertificateIssuerTest smart contract using the `revokeCertificate(uint512)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    revokeCertificate: (params: CallParams<CertificateIssuerTestArgs['obj']['revokeCertificate(uint512)void'] | CertificateIssuerTestArgs['tuple']['revokeCertificate(uint512)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(CertificateIssuerTestParamsFactory.revokeCertificate(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Makes a clear_state call to an existing instance of the CertificateIssuerTest smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the CertificateIssuerTest smart contract using the `initCert(uint512)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    initCert: (params: CallParams<CertificateIssuerTestArgs['obj']['initCert(uint512)void'] | CertificateIssuerTestArgs['tuple']['initCert(uint512)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(CertificateIssuerTestParamsFactory.initCert(params))
    },

    /**
     * Makes a call to the CertificateIssuerTest smart contract using the `getAdmin()address` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    getAdmin: (params: CallParams<CertificateIssuerTestArgs['obj']['getAdmin()address'] | CertificateIssuerTestArgs['tuple']['getAdmin()address']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(CertificateIssuerTestParamsFactory.getAdmin(params))
    },

    /**
     * Makes a call to the CertificateIssuerTest smart contract using the `getCertCount()uint512` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    getCertCount: (params: CallParams<CertificateIssuerTestArgs['obj']['getCertCount()uint512'] | CertificateIssuerTestArgs['tuple']['getCertCount()uint512']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(CertificateIssuerTestParamsFactory.getCertCount(params))
    },

    /**
     * Makes a call to the CertificateIssuerTest smart contract using the `getRevokedCount()uint512` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    getRevokedCount: (params: CallParams<CertificateIssuerTestArgs['obj']['getRevokedCount()uint512'] | CertificateIssuerTestArgs['tuple']['getRevokedCount()uint512']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(CertificateIssuerTestParamsFactory.getRevokedCount(params))
    },

    /**
     * Makes a call to the CertificateIssuerTest smart contract using the `getCertHash(uint512)uint512` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    getCertHash: (params: CallParams<CertificateIssuerTestArgs['obj']['getCertHash(uint512)uint512'] | CertificateIssuerTestArgs['tuple']['getCertHash(uint512)uint512']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(CertificateIssuerTestParamsFactory.getCertHash(params))
    },

    /**
     * Makes a call to the CertificateIssuerTest smart contract using the `issueCertificate(uint512)uint512` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    issueCertificate: (params: CallParams<CertificateIssuerTestArgs['obj']['issueCertificate(uint512)uint512'] | CertificateIssuerTestArgs['tuple']['issueCertificate(uint512)uint512']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(CertificateIssuerTestParamsFactory.issueCertificate(params))
    },

    /**
     * Makes a call to the CertificateIssuerTest smart contract using the `verifyCertificate(uint512)bool` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    verifyCertificate: (params: CallParams<CertificateIssuerTestArgs['obj']['verifyCertificate(uint512)bool'] | CertificateIssuerTestArgs['tuple']['verifyCertificate(uint512)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(CertificateIssuerTestParamsFactory.verifyCertificate(params))
    },

    /**
     * Makes a call to the CertificateIssuerTest smart contract using the `revokeCertificate(uint512)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    revokeCertificate: (params: CallParams<CertificateIssuerTestArgs['obj']['revokeCertificate(uint512)void'] | CertificateIssuerTestArgs['tuple']['revokeCertificate(uint512)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(CertificateIssuerTestParamsFactory.revokeCertificate(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Makes a clear_state call to an existing instance of the CertificateIssuerTest smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the CertificateIssuerTest smart contract using the `initCert(uint512)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    initCert: async (params: CallParams<CertificateIssuerTestArgs['obj']['initCert(uint512)void'] | CertificateIssuerTestArgs['tuple']['initCert(uint512)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(CertificateIssuerTestParamsFactory.initCert(params))
      return {...result, return: result.return as unknown as (undefined | CertificateIssuerTestReturns['initCert(uint512)void'])}
    },

    /**
     * Makes a call to the CertificateIssuerTest smart contract using the `getAdmin()address` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    getAdmin: async (params: CallParams<CertificateIssuerTestArgs['obj']['getAdmin()address'] | CertificateIssuerTestArgs['tuple']['getAdmin()address']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(CertificateIssuerTestParamsFactory.getAdmin(params))
      return {...result, return: result.return as unknown as (undefined | CertificateIssuerTestReturns['getAdmin()address'])}
    },

    /**
     * Makes a call to the CertificateIssuerTest smart contract using the `getCertCount()uint512` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    getCertCount: async (params: CallParams<CertificateIssuerTestArgs['obj']['getCertCount()uint512'] | CertificateIssuerTestArgs['tuple']['getCertCount()uint512']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(CertificateIssuerTestParamsFactory.getCertCount(params))
      return {...result, return: result.return as unknown as (undefined | CertificateIssuerTestReturns['getCertCount()uint512'])}
    },

    /**
     * Makes a call to the CertificateIssuerTest smart contract using the `getRevokedCount()uint512` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    getRevokedCount: async (params: CallParams<CertificateIssuerTestArgs['obj']['getRevokedCount()uint512'] | CertificateIssuerTestArgs['tuple']['getRevokedCount()uint512']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(CertificateIssuerTestParamsFactory.getRevokedCount(params))
      return {...result, return: result.return as unknown as (undefined | CertificateIssuerTestReturns['getRevokedCount()uint512'])}
    },

    /**
     * Makes a call to the CertificateIssuerTest smart contract using the `getCertHash(uint512)uint512` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    getCertHash: async (params: CallParams<CertificateIssuerTestArgs['obj']['getCertHash(uint512)uint512'] | CertificateIssuerTestArgs['tuple']['getCertHash(uint512)uint512']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(CertificateIssuerTestParamsFactory.getCertHash(params))
      return {...result, return: result.return as unknown as (undefined | CertificateIssuerTestReturns['getCertHash(uint512)uint512'])}
    },

    /**
     * Makes a call to the CertificateIssuerTest smart contract using the `issueCertificate(uint512)uint512` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    issueCertificate: async (params: CallParams<CertificateIssuerTestArgs['obj']['issueCertificate(uint512)uint512'] | CertificateIssuerTestArgs['tuple']['issueCertificate(uint512)uint512']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(CertificateIssuerTestParamsFactory.issueCertificate(params))
      return {...result, return: result.return as unknown as (undefined | CertificateIssuerTestReturns['issueCertificate(uint512)uint512'])}
    },

    /**
     * Makes a call to the CertificateIssuerTest smart contract using the `verifyCertificate(uint512)bool` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    verifyCertificate: async (params: CallParams<CertificateIssuerTestArgs['obj']['verifyCertificate(uint512)bool'] | CertificateIssuerTestArgs['tuple']['verifyCertificate(uint512)bool']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(CertificateIssuerTestParamsFactory.verifyCertificate(params))
      return {...result, return: result.return as unknown as (undefined | CertificateIssuerTestReturns['verifyCertificate(uint512)bool'])}
    },

    /**
     * Makes a call to the CertificateIssuerTest smart contract using the `revokeCertificate(uint512)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    revokeCertificate: async (params: CallParams<CertificateIssuerTestArgs['obj']['revokeCertificate(uint512)void'] | CertificateIssuerTestArgs['tuple']['revokeCertificate(uint512)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(CertificateIssuerTestParamsFactory.revokeCertificate(params))
      return {...result, return: result.return as unknown as (undefined | CertificateIssuerTestReturns['revokeCertificate(uint512)void'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new CertificateIssuerTestClient(this.appClient.clone(params))
  }

  /**
   * Makes a readonly (simulated) call to the CertificateIssuerTest smart contract using the `getAdmin()address` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async getAdmin(params: CallParams<CertificateIssuerTestArgs['obj']['getAdmin()address'] | CertificateIssuerTestArgs['tuple']['getAdmin()address']> = {args: []}) {
    const result = await this.appClient.send.call(CertificateIssuerTestParamsFactory.getAdmin(params))
    return result.return as unknown as CertificateIssuerTestReturns['getAdmin()address']
  }

  /**
   * Makes a readonly (simulated) call to the CertificateIssuerTest smart contract using the `getCertCount()uint512` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async getCertCount(params: CallParams<CertificateIssuerTestArgs['obj']['getCertCount()uint512'] | CertificateIssuerTestArgs['tuple']['getCertCount()uint512']> = {args: []}) {
    const result = await this.appClient.send.call(CertificateIssuerTestParamsFactory.getCertCount(params))
    return result.return as unknown as CertificateIssuerTestReturns['getCertCount()uint512']
  }

  /**
   * Makes a readonly (simulated) call to the CertificateIssuerTest smart contract using the `getRevokedCount()uint512` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async getRevokedCount(params: CallParams<CertificateIssuerTestArgs['obj']['getRevokedCount()uint512'] | CertificateIssuerTestArgs['tuple']['getRevokedCount()uint512']> = {args: []}) {
    const result = await this.appClient.send.call(CertificateIssuerTestParamsFactory.getRevokedCount(params))
    return result.return as unknown as CertificateIssuerTestReturns['getRevokedCount()uint512']
  }

  /**
   * Makes a readonly (simulated) call to the CertificateIssuerTest smart contract using the `getCertHash(uint512)uint512` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async getCertHash(params: CallParams<CertificateIssuerTestArgs['obj']['getCertHash(uint512)uint512'] | CertificateIssuerTestArgs['tuple']['getCertHash(uint512)uint512']>) {
    const result = await this.appClient.send.call(CertificateIssuerTestParamsFactory.getCertHash(params))
    return result.return as unknown as CertificateIssuerTestReturns['getCertHash(uint512)uint512']
  }

  /**
   * Makes a readonly (simulated) call to the CertificateIssuerTest smart contract using the `verifyCertificate(uint512)bool` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async verifyCertificate(params: CallParams<CertificateIssuerTestArgs['obj']['verifyCertificate(uint512)bool'] | CertificateIssuerTestArgs['tuple']['verifyCertificate(uint512)bool']>) {
    const result = await this.appClient.send.call(CertificateIssuerTestParamsFactory.verifyCertificate(params))
    return result.return as unknown as CertificateIssuerTestReturns['verifyCertificate(uint512)bool']
  }

  /**
   * Methods to access state for the current CertificateIssuerTest app
   */
  state = {
    /**
     * Methods to access global state for the current CertificateIssuerTest app
     */
    global: {
      /**
       * Get all current keyed values from global state
       */
      getAll: async (): Promise<Partial<Expand<GlobalKeysState>>> => {
        const result = await this.appClient.state.global.getAll()
        return {
          _admin: result._admin,
          _certCount: new BinaryStateValue(result._certCount),
          _revokedCount: new BinaryStateValue(result._revokedCount),
        }
      },
      /**
       * Get the current value of the _admin key in global state
       */
      _admin: async (): Promise<string | undefined> => { return (await this.appClient.state.global.getValue("_admin")) as string | undefined },
      /**
       * Get the current value of the _certCount key in global state
       */
      _certCount: async (): Promise<BinaryState> => { return new BinaryStateValue((await this.appClient.state.global.getValue("_certCount")) as Uint8Array | undefined) },
      /**
       * Get the current value of the _revokedCount key in global state
       */
      _revokedCount: async (): Promise<BinaryState> => { return new BinaryStateValue((await this.appClient.state.global.getValue("_revokedCount")) as Uint8Array | undefined) },
    },
    /**
     * Methods to access box state for the current CertificateIssuerTest app
     */
    box: {
      /**
       * Get all current keyed values from box state
       */
      getAll: async (): Promise<Partial<Expand<BoxKeysState>>> => {
        const result = await this.appClient.state.box.getAll()
        return {
        }
      },
      /**
       * Get values from the _certHash map in box state
       */
      _certHash: {
        /**
         * Get all current values of the _certHash map in box state
         */
        getMap: async (): Promise<Map<Uint8Array, Uint8Array>> => { return (await this.appClient.state.box.getMap("_certHash")) as Map<Uint8Array, Uint8Array> },
        /**
         * Get a current value of the _certHash map by key from box state
         */
        value: async (key: Uint8Array | string): Promise<Uint8Array | undefined> => { return await this.appClient.state.box.getMapValue("_certHash", key) as Uint8Array | undefined },
      },
      /**
       * Get values from the _certIssued map in box state
       */
      _certIssued: {
        /**
         * Get all current values of the _certIssued map in box state
         */
        getMap: async (): Promise<Map<Uint8Array, bigint>> => { return (await this.appClient.state.box.getMap("_certIssued")) as Map<Uint8Array, bigint> },
        /**
         * Get a current value of the _certIssued map by key from box state
         */
        value: async (key: Uint8Array | string): Promise<bigint | undefined> => { return await this.appClient.state.box.getMapValue("_certIssued", key) as bigint | undefined },
      },
      /**
       * Get values from the _certRevoked map in box state
       */
      _certRevoked: {
        /**
         * Get all current values of the _certRevoked map in box state
         */
        getMap: async (): Promise<Map<Uint8Array, bigint>> => { return (await this.appClient.state.box.getMap("_certRevoked")) as Map<Uint8Array, bigint> },
        /**
         * Get a current value of the _certRevoked map by key from box state
         */
        value: async (key: Uint8Array | string): Promise<bigint | undefined> => { return await this.appClient.state.box.getMapValue("_certRevoked", key) as bigint | undefined },
      },
    },
  }

  public newGroup(): CertificateIssuerTestComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a initCert(uint512)void method call against the CertificateIssuerTest contract
       */
      initCert(params: CallParams<CertificateIssuerTestArgs['obj']['initCert(uint512)void'] | CertificateIssuerTestArgs['tuple']['initCert(uint512)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.initCert(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a getAdmin()address method call against the CertificateIssuerTest contract
       */
      getAdmin(params: CallParams<CertificateIssuerTestArgs['obj']['getAdmin()address'] | CertificateIssuerTestArgs['tuple']['getAdmin()address']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.getAdmin(params)))
        resultMappers.push((v) => client.decodeReturnValue('getAdmin()address', v))
        return this
      },
      /**
       * Add a getCertCount()uint512 method call against the CertificateIssuerTest contract
       */
      getCertCount(params: CallParams<CertificateIssuerTestArgs['obj']['getCertCount()uint512'] | CertificateIssuerTestArgs['tuple']['getCertCount()uint512']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.getCertCount(params)))
        resultMappers.push((v) => client.decodeReturnValue('getCertCount()uint512', v))
        return this
      },
      /**
       * Add a getRevokedCount()uint512 method call against the CertificateIssuerTest contract
       */
      getRevokedCount(params: CallParams<CertificateIssuerTestArgs['obj']['getRevokedCount()uint512'] | CertificateIssuerTestArgs['tuple']['getRevokedCount()uint512']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.getRevokedCount(params)))
        resultMappers.push((v) => client.decodeReturnValue('getRevokedCount()uint512', v))
        return this
      },
      /**
       * Add a getCertHash(uint512)uint512 method call against the CertificateIssuerTest contract
       */
      getCertHash(params: CallParams<CertificateIssuerTestArgs['obj']['getCertHash(uint512)uint512'] | CertificateIssuerTestArgs['tuple']['getCertHash(uint512)uint512']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.getCertHash(params)))
        resultMappers.push((v) => client.decodeReturnValue('getCertHash(uint512)uint512', v))
        return this
      },
      /**
       * Add a issueCertificate(uint512)uint512 method call against the CertificateIssuerTest contract
       */
      issueCertificate(params: CallParams<CertificateIssuerTestArgs['obj']['issueCertificate(uint512)uint512'] | CertificateIssuerTestArgs['tuple']['issueCertificate(uint512)uint512']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.issueCertificate(params)))
        resultMappers.push((v) => client.decodeReturnValue('issueCertificate(uint512)uint512', v))
        return this
      },
      /**
       * Add a verifyCertificate(uint512)bool method call against the CertificateIssuerTest contract
       */
      verifyCertificate(params: CallParams<CertificateIssuerTestArgs['obj']['verifyCertificate(uint512)bool'] | CertificateIssuerTestArgs['tuple']['verifyCertificate(uint512)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.verifyCertificate(params)))
        resultMappers.push((v) => client.decodeReturnValue('verifyCertificate(uint512)bool', v))
        return this
      },
      /**
       * Add a revokeCertificate(uint512)void method call against the CertificateIssuerTest contract
       */
      revokeCertificate(params: CallParams<CertificateIssuerTestArgs['obj']['revokeCertificate(uint512)void'] | CertificateIssuerTestArgs['tuple']['revokeCertificate(uint512)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.revokeCertificate(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a clear state call to the CertificateIssuerTest contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as CertificateIssuerTestComposer
  }
}
export type CertificateIssuerTestComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the initCert(uint512)void ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  initCert(params?: CallParams<CertificateIssuerTestArgs['obj']['initCert(uint512)void'] | CertificateIssuerTestArgs['tuple']['initCert(uint512)void']>): CertificateIssuerTestComposer<[...TReturns, CertificateIssuerTestReturns['initCert(uint512)void'] | undefined]>

  /**
   * Calls the getAdmin()address ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  getAdmin(params?: CallParams<CertificateIssuerTestArgs['obj']['getAdmin()address'] | CertificateIssuerTestArgs['tuple']['getAdmin()address']>): CertificateIssuerTestComposer<[...TReturns, CertificateIssuerTestReturns['getAdmin()address'] | undefined]>

  /**
   * Calls the getCertCount()uint512 ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  getCertCount(params?: CallParams<CertificateIssuerTestArgs['obj']['getCertCount()uint512'] | CertificateIssuerTestArgs['tuple']['getCertCount()uint512']>): CertificateIssuerTestComposer<[...TReturns, CertificateIssuerTestReturns['getCertCount()uint512'] | undefined]>

  /**
   * Calls the getRevokedCount()uint512 ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  getRevokedCount(params?: CallParams<CertificateIssuerTestArgs['obj']['getRevokedCount()uint512'] | CertificateIssuerTestArgs['tuple']['getRevokedCount()uint512']>): CertificateIssuerTestComposer<[...TReturns, CertificateIssuerTestReturns['getRevokedCount()uint512'] | undefined]>

  /**
   * Calls the getCertHash(uint512)uint512 ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  getCertHash(params?: CallParams<CertificateIssuerTestArgs['obj']['getCertHash(uint512)uint512'] | CertificateIssuerTestArgs['tuple']['getCertHash(uint512)uint512']>): CertificateIssuerTestComposer<[...TReturns, CertificateIssuerTestReturns['getCertHash(uint512)uint512'] | undefined]>

  /**
   * Calls the issueCertificate(uint512)uint512 ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  issueCertificate(params?: CallParams<CertificateIssuerTestArgs['obj']['issueCertificate(uint512)uint512'] | CertificateIssuerTestArgs['tuple']['issueCertificate(uint512)uint512']>): CertificateIssuerTestComposer<[...TReturns, CertificateIssuerTestReturns['issueCertificate(uint512)uint512'] | undefined]>

  /**
   * Calls the verifyCertificate(uint512)bool ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  verifyCertificate(params?: CallParams<CertificateIssuerTestArgs['obj']['verifyCertificate(uint512)bool'] | CertificateIssuerTestArgs['tuple']['verifyCertificate(uint512)bool']>): CertificateIssuerTestComposer<[...TReturns, CertificateIssuerTestReturns['verifyCertificate(uint512)bool'] | undefined]>

  /**
   * Calls the revokeCertificate(uint512)void ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  revokeCertificate(params?: CallParams<CertificateIssuerTestArgs['obj']['revokeCertificate(uint512)void'] | CertificateIssuerTestArgs['tuple']['revokeCertificate(uint512)void']>): CertificateIssuerTestComposer<[...TReturns, CertificateIssuerTestReturns['revokeCertificate(uint512)void'] | undefined]>

  /**
   * Makes a clear_state call to an existing instance of the CertificateIssuerTest smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): CertificateIssuerTestComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): CertificateIssuerTestComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<CertificateIssuerTestComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<CertificateIssuerTestComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<CertificateIssuerTestComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<CertificateIssuerTestComposerResults<TReturns>>
}
export type CertificateIssuerTestComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

