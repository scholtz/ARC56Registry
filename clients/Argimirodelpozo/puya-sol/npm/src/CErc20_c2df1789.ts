/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"CErc20","structs":{"BorrowSnapshot":[{"name":"principal","type":"uint256"},{"name":"interestIndex","type":"uint256"}]},"methods":[{"name":"initialize","args":[{"type":"address","name":"underlying_"},{"type":"address","name":"comptroller_"},{"type":"address","name":"interestRateModel_"},{"type":"uint256","name":"initialExchangeRateMantissa_"},{"type":"string","name":"name_"},{"type":"string","name":"symbol_"},{"type":"uint64","name":"decimals_"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":" @notice Initialize the new money market\n @param underlying_ The address of the underlying asset\n @param comptroller_ The address of the Comptroller\n @param interestRateModel_ The address of the interest rate model\n @param initialExchangeRateMantissa_ The initial exchange rate, scaled by 1e18\n @param name_ ERC-20 name of this token\n @param symbol_ ERC-20 symbol of this token\n @param decimals_ ERC-20 decimal precision of this token","events":[{"name":"NewComptroller","args":[{"type":"uint8[32]","name":"oldComptroller"},{"type":"uint8[32]","name":"newComptroller"}]},{"name":"NewMarketInterestRateModel","args":[{"type":"uint8[32]","name":"oldInterestRateModel"},{"type":"uint8[32]","name":"newInterestRateModel"}]}],"recommendations":{}},{"name":"mint","args":[{"type":"uint256","name":"mintAmount"}],"returns":{"type":"uint256"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":" @notice Sender supplies assets into the market and receives cTokens in exchange\n @dev Accrues interest whether or not the operation succeeds, unless reverted\n @param mintAmount The amount of the underlying asset to supply\n @return uint 0=success, otherwise a failure (see ErrorReporter.sol for details)","events":[{"name":"AccrueInterest","args":[{"type":"uint256","name":"cashPrior"},{"type":"uint256","name":"interestAccumulated"},{"type":"uint256","name":"borrowIndex"},{"type":"uint256","name":"totalBorrows"}]},{"name":"Mint","args":[{"type":"uint8[32]","name":"minter"},{"type":"uint256","name":"mintAmount"},{"type":"uint256","name":"mintTokens"}]},{"name":"Transfer","args":[{"type":"uint8[32]","name":"from"},{"type":"uint8[32]","name":"to"},{"type":"uint256","name":"amount"}]}],"recommendations":{}},{"name":"redeem","args":[{"type":"uint256","name":"redeemTokens"}],"returns":{"type":"uint256"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":" @notice Sender redeems cTokens in exchange for the underlying asset\n @dev Accrues interest whether or not the operation succeeds, unless reverted\n @param redeemTokens The number of cTokens to redeem into underlying\n @return uint 0=success, otherwise a failure (see ErrorReporter.sol for details)","events":[{"name":"AccrueInterest","args":[{"type":"uint256","name":"cashPrior"},{"type":"uint256","name":"interestAccumulated"},{"type":"uint256","name":"borrowIndex"},{"type":"uint256","name":"totalBorrows"}]},{"name":"Transfer","args":[{"type":"uint8[32]","name":"from"},{"type":"uint8[32]","name":"to"},{"type":"uint256","name":"amount"}]},{"name":"Redeem","args":[{"type":"uint8[32]","name":"redeemer"},{"type":"uint256","name":"redeemAmount"},{"type":"uint256","name":"redeemTokens"}]}],"recommendations":{}},{"name":"redeemUnderlying","args":[{"type":"uint256","name":"redeemAmount"}],"returns":{"type":"uint256"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":" @notice Sender redeems cTokens in exchange for a specified amount of underlying asset\n @dev Accrues interest whether or not the operation succeeds, unless reverted\n @param redeemAmount The amount of underlying to redeem\n @return uint 0=success, otherwise a failure (see ErrorReporter.sol for details)","events":[{"name":"AccrueInterest","args":[{"type":"uint256","name":"cashPrior"},{"type":"uint256","name":"interestAccumulated"},{"type":"uint256","name":"borrowIndex"},{"type":"uint256","name":"totalBorrows"}]},{"name":"Transfer","args":[{"type":"uint8[32]","name":"from"},{"type":"uint8[32]","name":"to"},{"type":"uint256","name":"amount"}]},{"name":"Redeem","args":[{"type":"uint8[32]","name":"redeemer"},{"type":"uint256","name":"redeemAmount"},{"type":"uint256","name":"redeemTokens"}]}],"recommendations":{}},{"name":"borrow","args":[{"type":"uint256","name":"borrowAmount"}],"returns":{"type":"uint256"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":" @notice Sender borrows assets from the protocol to their own address\n @param borrowAmount The amount of the underlying asset to borrow\n @return uint 0=success, otherwise a failure (see ErrorReporter.sol for details)","events":[{"name":"AccrueInterest","args":[{"type":"uint256","name":"cashPrior"},{"type":"uint256","name":"interestAccumulated"},{"type":"uint256","name":"borrowIndex"},{"type":"uint256","name":"totalBorrows"}]},{"name":"Borrow","args":[{"type":"uint8[32]","name":"borrower"},{"type":"uint256","name":"borrowAmount"},{"type":"uint256","name":"accountBorrows"},{"type":"uint256","name":"totalBorrows"}]}],"recommendations":{}},{"name":"repayBorrow","args":[{"type":"uint256","name":"repayAmount"}],"returns":{"type":"uint256"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":" @notice Sender repays their own borrow\n @param repayAmount The amount to repay, or -1 for the full outstanding amount\n @return uint 0=success, otherwise a failure (see ErrorReporter.sol for details)","events":[{"name":"AccrueInterest","args":[{"type":"uint256","name":"cashPrior"},{"type":"uint256","name":"interestAccumulated"},{"type":"uint256","name":"borrowIndex"},{"type":"uint256","name":"totalBorrows"}]},{"name":"RepayBorrow","args":[{"type":"uint8[32]","name":"payer"},{"type":"uint8[32]","name":"borrower"},{"type":"uint256","name":"repayAmount"},{"type":"uint256","name":"accountBorrows"},{"type":"uint256","name":"totalBorrows"}]}],"recommendations":{}},{"name":"repayBorrowBehalf","args":[{"type":"address","name":"borrower"},{"type":"uint256","name":"repayAmount"}],"returns":{"type":"uint256"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":" @notice Sender repays a borrow belonging to borrower\n @param borrower the account with the debt being payed off\n @param repayAmount The amount to repay, or -1 for the full outstanding amount\n @return uint 0=success, otherwise a failure (see ErrorReporter.sol for details)","events":[{"name":"AccrueInterest","args":[{"type":"uint256","name":"cashPrior"},{"type":"uint256","name":"interestAccumulated"},{"type":"uint256","name":"borrowIndex"},{"type":"uint256","name":"totalBorrows"}]},{"name":"RepayBorrow","args":[{"type":"uint8[32]","name":"payer"},{"type":"uint8[32]","name":"borrower"},{"type":"uint256","name":"repayAmount"},{"type":"uint256","name":"accountBorrows"},{"type":"uint256","name":"totalBorrows"}]}],"recommendations":{}},{"name":"liquidateBorrow","args":[{"type":"address","name":"borrower"},{"type":"uint256","name":"repayAmount"},{"type":"address","name":"cTokenCollateral"}],"returns":{"type":"uint256"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":" @notice The sender liquidates the borrowers collateral.\n  The collateral seized is transferred to the liquidator.\n @param borrower The borrower of this cToken to be liquidated\n @param repayAmount The amount of the underlying borrowed asset to repay\n @param cTokenCollateral The market in which to seize collateral from the borrower\n @return uint 0=success, otherwise a failure (see ErrorReporter.sol for details)","events":[{"name":"AccrueInterest","args":[{"type":"uint256","name":"cashPrior"},{"type":"uint256","name":"interestAccumulated"},{"type":"uint256","name":"borrowIndex"},{"type":"uint256","name":"totalBorrows"}]},{"name":"RepayBorrow","args":[{"type":"uint8[32]","name":"payer"},{"type":"uint8[32]","name":"borrower"},{"type":"uint256","name":"repayAmount"},{"type":"uint256","name":"accountBorrows"},{"type":"uint256","name":"totalBorrows"}]},{"name":"Transfer","args":[{"type":"uint8[32]","name":"from"},{"type":"uint8[32]","name":"to"},{"type":"uint256","name":"amount"}]},{"name":"ReservesAdded","args":[{"type":"uint8[32]","name":"benefactor"},{"type":"uint256","name":"addAmount"},{"type":"uint256","name":"newTotalReserves"}]},{"name":"LiquidateBorrow","args":[{"type":"uint8[32]","name":"liquidator"},{"type":"uint8[32]","name":"borrower"},{"type":"uint256","name":"repayAmount"},{"type":"uint8[32]","name":"cTokenCollateral"},{"type":"uint256","name":"seizeTokens"}]}],"recommendations":{}},{"name":"sweepToken","args":[{"type":"address","name":"token"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":" @notice A public function to sweep accidental ERC-20 transfers to this contract. Tokens are sent to admin (timelock)\n @param token The address of the ERC-20 token to sweep","events":[],"recommendations":{}},{"name":"_addReserves","args":[{"type":"uint256","name":"addAmount"}],"returns":{"type":"uint256"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":" @notice The sender adds to reserves.\n @param addAmount The amount fo underlying token to add as reserves\n @return uint 0=success, otherwise a failure (see ErrorReporter.sol for details)","events":[{"name":"AccrueInterest","args":[{"type":"uint256","name":"cashPrior"},{"type":"uint256","name":"interestAccumulated"},{"type":"uint256","name":"borrowIndex"},{"type":"uint256","name":"totalBorrows"}]},{"name":"ReservesAdded","args":[{"type":"uint8[32]","name":"benefactor"},{"type":"uint256","name":"addAmount"},{"type":"uint256","name":"newTotalReserves"}]}],"recommendations":{}},{"name":"_delegateCompLikeTo","args":[{"type":"address","name":"compLikeDelegatee"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":" @notice Admin call to delegate the votes of the COMP-like underlying\n @param compLikeDelegatee The address to delegate votes to\n @dev CTokens whose underlying are not CompLike should revert here","events":[],"recommendations":{}},{"name":"initialize","args":[{"type":"address","name":"comptroller_"},{"type":"address","name":"interestRateModel_"},{"type":"uint256","name":"initialExchangeRateMantissa_"},{"type":"string","name":"name_"},{"type":"string","name":"symbol_"},{"type":"uint64","name":"decimals_"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":" @notice Initialize the money market\n @param comptroller_ The address of the Comptroller\n @param interestRateModel_ The address of the interest rate model\n @param initialExchangeRateMantissa_ The initial exchange rate, scaled by 1e18\n @param name_ EIP-20 name of this token\n @param symbol_ EIP-20 symbol of this token\n @param decimals_ EIP-20 decimal precision of this token","events":[{"name":"NewComptroller","args":[{"type":"uint8[32]","name":"oldComptroller"},{"type":"uint8[32]","name":"newComptroller"}]},{"name":"NewMarketInterestRateModel","args":[{"type":"uint8[32]","name":"oldInterestRateModel"},{"type":"uint8[32]","name":"newInterestRateModel"}]}],"recommendations":{}},{"name":"transfer","args":[{"type":"address","name":"dst"},{"type":"uint256","name":"amount"}],"returns":{"type":"bool"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":" @notice Transfer `amount` tokens from `msg.sender` to `dst`\n @param dst The address of the destination account\n @param amount The number of tokens to transfer\n @return Whether or not the transfer succeeded","events":[{"name":"Transfer","args":[{"type":"uint8[32]","name":"from"},{"type":"uint8[32]","name":"to"},{"type":"uint256","name":"amount"}]}],"recommendations":{}},{"name":"transferFrom","args":[{"type":"address","name":"src"},{"type":"address","name":"dst"},{"type":"uint256","name":"amount"}],"returns":{"type":"bool"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":" @notice Transfer `amount` tokens from `src` to `dst`\n @param src The address of the source account\n @param dst The address of the destination account\n @param amount The number of tokens to transfer\n @return Whether or not the transfer succeeded","events":[{"name":"Transfer","args":[{"type":"uint8[32]","name":"from"},{"type":"uint8[32]","name":"to"},{"type":"uint256","name":"amount"}]}],"recommendations":{}},{"name":"approve","args":[{"type":"address","name":"spender"},{"type":"uint256","name":"amount"}],"returns":{"type":"bool"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":" @notice Approve `spender` to transfer up to `amount` from `src`\n @dev This will overwrite the approval amount for `spender`\n  and is subject to issues noted [here](https://eips.ethereum.org/EIPS/eip-20#approve)\n @param spender The address of the account which may transfer tokens\n @param amount The number of tokens that are approved (uint256.max means infinite)\n @return Whether or not the approval succeeded","events":[{"name":"Approval","args":[{"type":"uint8[32]","name":"owner"},{"type":"uint8[32]","name":"spender"},{"type":"uint256","name":"amount"}]}],"recommendations":{}},{"name":"allowance","args":[{"type":"address","name":"owner"},{"type":"address","name":"spender"}],"returns":{"type":"uint256"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"desc":" @notice Get the current allowance from `owner` for `spender`\n @param owner The address of the account which owns the tokens to be spent\n @param spender The address of the account which may transfer tokens\n @return The number of tokens allowed to be spent (-1 means infinite)","events":[],"recommendations":{}},{"name":"balanceOf","args":[{"type":"address","name":"owner"}],"returns":{"type":"uint256"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"desc":" @notice Get the token balance of the `owner`\n @param owner The address of the account to query\n @return The number of tokens owned by `owner`","events":[],"recommendations":{}},{"name":"balanceOfUnderlying","args":[{"type":"address","name":"owner"}],"returns":{"type":"uint256"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":" @notice Get the underlying balance of the `owner`\n @dev This also accrues interest in a transaction\n @param owner The address of the account to query\n @return The amount of underlying owned by `owner`","events":[{"name":"AccrueInterest","args":[{"type":"uint256","name":"cashPrior"},{"type":"uint256","name":"interestAccumulated"},{"type":"uint256","name":"borrowIndex"},{"type":"uint256","name":"totalBorrows"}]}],"recommendations":{}},{"name":"getAccountSnapshot","args":[{"type":"address","name":"account"}],"returns":{"type":"(uint256,uint256,uint256,uint256)"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"desc":" @notice Get a snapshot of the account's balances, and the cached exchange rate\n @dev This is used by comptroller to more efficiently perform liquidity checks.\n @param account Address of the account to snapshot\n @return (possible error, token balance, borrow balance, exchange rate mantissa)","events":[],"recommendations":{}},{"name":"borrowRatePerBlock","args":[],"returns":{"type":"uint256"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"desc":" @notice Returns the current per-block borrow interest rate for this cToken\n @return The borrow interest rate per block, scaled by 1e18","events":[],"recommendations":{}},{"name":"supplyRatePerBlock","args":[],"returns":{"type":"uint256"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"desc":" @notice Returns the current per-block supply interest rate for this cToken\n @return The supply interest rate per block, scaled by 1e18","events":[],"recommendations":{}},{"name":"totalBorrowsCurrent","args":[],"returns":{"type":"uint256"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":" @notice Returns the current total borrows plus accrued interest\n @return The total borrows with interest","events":[{"name":"AccrueInterest","args":[{"type":"uint256","name":"cashPrior"},{"type":"uint256","name":"interestAccumulated"},{"type":"uint256","name":"borrowIndex"},{"type":"uint256","name":"totalBorrows"}]}],"recommendations":{}},{"name":"borrowBalanceCurrent","args":[{"type":"address","name":"account"}],"returns":{"type":"uint256"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":" @notice Accrue interest to updated borrowIndex and then calculate account's borrow balance using the updated borrowIndex\n @param account The address whose balance should be calculated after updating borrowIndex\n @return The calculated balance","events":[{"name":"AccrueInterest","args":[{"type":"uint256","name":"cashPrior"},{"type":"uint256","name":"interestAccumulated"},{"type":"uint256","name":"borrowIndex"},{"type":"uint256","name":"totalBorrows"}]}],"recommendations":{}},{"name":"borrowBalanceStored","args":[{"type":"address","name":"account"}],"returns":{"type":"uint256"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"desc":" @notice Return the borrow balance of account based on stored data\n @param account The address whose balance should be calculated\n @return The calculated balance","events":[],"recommendations":{}},{"name":"exchangeRateCurrent","args":[],"returns":{"type":"uint256"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":" @notice Accrue interest then return the up-to-date exchange rate\n @return Calculated exchange rate scaled by 1e18","events":[{"name":"AccrueInterest","args":[{"type":"uint256","name":"cashPrior"},{"type":"uint256","name":"interestAccumulated"},{"type":"uint256","name":"borrowIndex"},{"type":"uint256","name":"totalBorrows"}]}],"recommendations":{}},{"name":"exchangeRateStored","args":[],"returns":{"type":"uint256"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"desc":" @notice Calculates the exchange rate from the underlying to the CToken\n @dev This function does not accrue interest before calculating the exchange rate\n @return Calculated exchange rate scaled by 1e18","events":[],"recommendations":{}},{"name":"getCash","args":[],"returns":{"type":"uint256"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"desc":" @notice Get cash balance of this cToken in the underlying asset\n @return The quantity of underlying asset owned by this contract","events":[],"recommendations":{}},{"name":"accrueInterest","args":[],"returns":{"type":"uint256"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":" @notice Applies accrued interest to total borrows and reserves\n @dev This calculates interest accrued from the last checkpointed block\n   up to the current block and writes new checkpoint to storage.","events":[{"name":"AccrueInterest","args":[{"type":"uint256","name":"cashPrior"},{"type":"uint256","name":"interestAccumulated"},{"type":"uint256","name":"borrowIndex"},{"type":"uint256","name":"totalBorrows"}]}],"recommendations":{}},{"name":"seize","args":[{"type":"address","name":"liquidator"},{"type":"address","name":"borrower"},{"type":"uint256","name":"seizeTokens"}],"returns":{"type":"uint256"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":" @notice Transfers collateral tokens (this market) to the liquidator.\n @dev Will fail unless called by another cToken during the process of liquidation.\n  Its absolutely critical to use msg.sender as the borrowed cToken and not a parameter.\n @param liquidator The account receiving seized collateral\n @param borrower The account having collateral seized\n @param seizeTokens The number of cTokens to seize\n @return uint 0=success, otherwise a failure (see ErrorReporter.sol for details)","events":[{"name":"Transfer","args":[{"type":"uint8[32]","name":"from"},{"type":"uint8[32]","name":"to"},{"type":"uint256","name":"amount"}]},{"name":"ReservesAdded","args":[{"type":"uint8[32]","name":"benefactor"},{"type":"uint256","name":"addAmount"},{"type":"uint256","name":"newTotalReserves"}]}],"recommendations":{}},{"name":"_setPendingAdmin","args":[{"type":"address","name":"newPendingAdmin"}],"returns":{"type":"uint256"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":" @notice Begins transfer of admin rights. The newPendingAdmin must call `_acceptAdmin` to finalize the transfer.\n @dev Admin function to begin change of admin. The newPendingAdmin must call `_acceptAdmin` to finalize the transfer.\n @param newPendingAdmin New pending admin.\n @return uint 0=success, otherwise a failure (see ErrorReporter.sol for details)","events":[{"name":"NewPendingAdmin","args":[{"type":"uint8[32]","name":"oldPendingAdmin"},{"type":"uint8[32]","name":"newPendingAdmin"}]}],"recommendations":{}},{"name":"_acceptAdmin","args":[],"returns":{"type":"uint256"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":" @notice Accepts transfer of admin rights. msg.sender must be pendingAdmin\n @dev Admin function for pending admin to accept role and update admin\n @return uint 0=success, otherwise a failure (see ErrorReporter.sol for details)","events":[{"name":"NewAdmin","args":[{"type":"uint8[32]","name":"oldAdmin"},{"type":"uint8[32]","name":"newAdmin"}]},{"name":"NewPendingAdmin","args":[{"type":"uint8[32]","name":"oldPendingAdmin"},{"type":"uint8[32]","name":"newPendingAdmin"}]}],"recommendations":{}},{"name":"_setComptroller","args":[{"type":"address","name":"newComptroller"}],"returns":{"type":"uint256"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":" @notice Sets a new comptroller for the market\n @dev Admin function to set a new comptroller\n @return uint 0=success, otherwise a failure (see ErrorReporter.sol for details)","events":[{"name":"NewComptroller","args":[{"type":"uint8[32]","name":"oldComptroller"},{"type":"uint8[32]","name":"newComptroller"}]}],"recommendations":{}},{"name":"_setReserveFactor","args":[{"type":"uint256","name":"newReserveFactorMantissa"}],"returns":{"type":"uint256"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":" @notice accrues interest and sets a new reserve factor for the protocol using _setReserveFactorFresh\n @dev Admin function to accrue interest and set a new reserve factor\n @return uint 0=success, otherwise a failure (see ErrorReporter.sol for details)","events":[{"name":"AccrueInterest","args":[{"type":"uint256","name":"cashPrior"},{"type":"uint256","name":"interestAccumulated"},{"type":"uint256","name":"borrowIndex"},{"type":"uint256","name":"totalBorrows"}]},{"name":"NewReserveFactor","args":[{"type":"uint256","name":"oldReserveFactorMantissa"},{"type":"uint256","name":"newReserveFactorMantissa"}]}],"recommendations":{}},{"name":"_reduceReserves","args":[{"type":"uint256","name":"reduceAmount"}],"returns":{"type":"uint256"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":" @notice Accrues interest and reduces reserves by transferring to admin\n @param reduceAmount Amount of reduction to reserves\n @return uint 0=success, otherwise a failure (see ErrorReporter.sol for details)","events":[{"name":"AccrueInterest","args":[{"type":"uint256","name":"cashPrior"},{"type":"uint256","name":"interestAccumulated"},{"type":"uint256","name":"borrowIndex"},{"type":"uint256","name":"totalBorrows"}]},{"name":"ReservesReduced","args":[{"type":"uint8[32]","name":"admin"},{"type":"uint256","name":"reduceAmount"},{"type":"uint256","name":"newTotalReserves"}]}],"recommendations":{}},{"name":"_setInterestRateModel","args":[{"type":"address","name":"newInterestRateModel"}],"returns":{"type":"uint256"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":" @notice accrues interest and updates the interest rate model using _setInterestRateModelFresh\n @dev Admin function to accrue interest and update the interest rate model\n @param newInterestRateModel the new interest rate model to use\n @return uint 0=success, otherwise a failure (see ErrorReporter.sol for details)","events":[{"name":"AccrueInterest","args":[{"type":"uint256","name":"cashPrior"},{"type":"uint256","name":"interestAccumulated"},{"type":"uint256","name":"borrowIndex"},{"type":"uint256","name":"totalBorrows"}]},{"name":"NewMarketInterestRateModel","args":[{"type":"uint8[32]","name":"oldInterestRateModel"},{"type":"uint8[32]","name":"newInterestRateModel"}]}],"recommendations":{}},{"name":"underlying","args":[],"returns":{"type":"address"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"name","args":[],"returns":{"type":"string"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"symbol","args":[],"returns":{"type":"string"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"decimals","args":[],"returns":{"type":"uint64"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"admin","args":[],"returns":{"type":"address"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"pendingAdmin","args":[],"returns":{"type":"address"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"comptroller","args":[],"returns":{"type":"address"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"interestRateModel","args":[],"returns":{"type":"address"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"reserveFactorMantissa","args":[],"returns":{"type":"uint256"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"accrualBlockNumber","args":[],"returns":{"type":"uint256"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"borrowIndex","args":[],"returns":{"type":"uint256"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"totalBorrows","args":[],"returns":{"type":"uint256"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"totalReserves","args":[],"returns":{"type":"uint256"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"totalSupply","args":[],"returns":{"type":"uint256"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}}],"arcs":[22,28],"desc":" @title Compound's CErc20 Contract\n @notice CTokens which wrap an EIP-20 underlying\n @author Compound","networks":{},"state":{"schema":{"global":{"ints":2,"bytes":14},"local":{"ints":0,"bytes":0}},"keys":{"global":{"underlying":{"keyType":"AVMString","valueType":"address","key":"dW5kZXJseWluZw=="},"_notEntered":{"keyType":"AVMString","valueType":"AVMUint64","key":"X25vdEVudGVyZWQ="},"name":{"keyType":"AVMString","valueType":"AVMString","key":"bmFtZQ=="},"symbol":{"keyType":"AVMString","valueType":"AVMString","key":"c3ltYm9s"},"decimals":{"keyType":"AVMString","valueType":"AVMUint64","key":"ZGVjaW1hbHM="},"admin":{"keyType":"AVMString","valueType":"address","key":"YWRtaW4="},"pendingAdmin":{"keyType":"AVMString","valueType":"address","key":"cGVuZGluZ0FkbWlu"},"comptroller":{"keyType":"AVMString","valueType":"address","key":"Y29tcHRyb2xsZXI="},"interestRateModel":{"keyType":"AVMString","valueType":"address","key":"aW50ZXJlc3RSYXRlTW9kZWw="},"initialExchangeRateMantissa":{"keyType":"AVMString","valueType":"AVMBytes","key":"aW5pdGlhbEV4Y2hhbmdlUmF0ZU1hbnRpc3Nh"},"reserveFactorMantissa":{"keyType":"AVMString","valueType":"AVMBytes","key":"cmVzZXJ2ZUZhY3Rvck1hbnRpc3Nh"},"accrualBlockNumber":{"keyType":"AVMString","valueType":"AVMBytes","key":"YWNjcnVhbEJsb2NrTnVtYmVy"},"borrowIndex":{"keyType":"AVMString","valueType":"AVMBytes","key":"Ym9ycm93SW5kZXg="},"totalBorrows":{"keyType":"AVMString","valueType":"AVMBytes","key":"dG90YWxCb3Jyb3dz"},"totalReserves":{"keyType":"AVMString","valueType":"AVMBytes","key":"dG90YWxSZXNlcnZlcw=="},"totalSupply":{"keyType":"AVMString","valueType":"AVMBytes","key":"dG90YWxTdXBwbHk="}},"local":{},"box":{}},"maps":{"global":{},"local":{},"box":{"accountTokens":{"keyType":"AVMBytes","valueType":"AVMBytes","prefix":"YWNjb3VudFRva2Vucw=="},"transferAllowances":{"keyType":"AVMBytes","valueType":"AVMBytes","prefix":"dHJhbnNmZXJBbGxvd2FuY2Vz"},"accountBorrows":{"keyType":"AVMBytes","valueType":"BorrowSnapshot","prefix":"YWNjb3VudEJvcnJvd3M="}}}},"bareActions":{"create":[],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[3503,3510],"errorMessage":"AcceptAdminPendingAdminCheck"},{"pc":[2366],"errorMessage":"AddReservesFactorFreshCheck"},{"pc":[1590],"errorMessage":"BorrowCashNotAvailable"},{"pc":[1571],"errorMessage":"BorrowComptrollerRejection"},{"pc":[1582],"errorMessage":"BorrowFreshnessCheck"},{"pc":[2267],"errorMessage":"CErc20::sweepToken: can not sweep underlying token"},{"pc":[2258],"errorMessage":"CErc20::sweepToken: only admin can sweep tokens"},{"pc":[2093],"errorMessage":"LIQUIDATE_COMPTROLLER_CALCULATE_AMOUNT_SEIZE_FAILED"},{"pc":[2122],"errorMessage":"LIQUIDATE_SEIZE_TOO_MUCH"},{"pc":[1876],"errorMessage":"LiquidateAccrueCollateralInterestFailed"},{"pc":[2015],"errorMessage":"LiquidateCloseAmountIsUintMax"},{"pc":[2008],"errorMessage":"LiquidateCloseAmountIsZero"},{"pc":[1995],"errorMessage":"LiquidateCollateralFreshnessCheck"},{"pc":[1954],"errorMessage":"LiquidateComptrollerRejection"},{"pc":[1965],"errorMessage":"LiquidateFreshnessCheck"},{"pc":[2002],"errorMessage":"LiquidateLiquidatorIsBorrower"},{"pc":[5570],"errorMessage":"LiquidateSeizeComptrollerRejection"},{"pc":[5577],"errorMessage":"LiquidateSeizeLiquidatorIsBorrower"},{"pc":[1286],"errorMessage":"MintComptrollerRejection"},{"pc":[1297],"errorMessage":"MintFreshnessCheck"},{"pc":[5078],"errorMessage":"RedeemComptrollerRejection"},{"pc":[5089],"errorMessage":"RedeemFreshnessCheck"},{"pc":[5100],"errorMessage":"RedeemTransferOutNotPossible"},{"pc":[3710],"errorMessage":"ReduceReservesAdminCheck"},{"pc":[3729],"errorMessage":"ReduceReservesCashNotAvailable"},{"pc":[3741],"errorMessage":"ReduceReservesCashValidation"},{"pc":[3721],"errorMessage":"ReduceReservesFreshCheck"},{"pc":[5338],"errorMessage":"RepayBorrowComptrollerRejection"},{"pc":[5349],"errorMessage":"RepayBorrowFreshnessCheck"},{"pc":[5816],"errorMessage":"SetComptrollerOwnerCheck"},{"pc":[5899],"errorMessage":"SetInterestRateModelFreshCheck"},{"pc":[5888],"errorMessage":"SetInterestRateModelOwnerCheck"},{"pc":[3468],"errorMessage":"SetPendingAdminOwnerCheck"},{"pc":[3612],"errorMessage":"SetReserveFactorAdminCheck"},{"pc":[3630],"errorMessage":"SetReserveFactorBoundsCheck"},{"pc":[3623],"errorMessage":"SetReserveFactorFreshCheck"},{"pc":[4202],"errorMessage":"TOKEN_TRANSFER_IN_FAILED"},{"pc":[4308],"errorMessage":"TOKEN_TRANSFER_OUT_FAILED"},{"pc":[4384],"errorMessage":"TransferComptrollerRejection"},{"pc":[4391],"errorMessage":"TransferNotAllowed"},{"pc":[4782],"errorMessage":"borrow rate is absurdly high"},{"pc":[1214,1423,1461,1499,1733,1778,1834,2347,2664,2721,3240,3282,3431,3591,3689,4587],"errorMessage":"check _notEntered exists"},{"pc":[1092,1291,1576,1959,2360,2562,3617,3715,3969,4671,5083,5343,5893],"errorMessage":"check accrualBlockNumber exists"},{"pc":[1085,2253,2445,2555,3465,3515,3530,3609,3707,3758,3768,3906,5813,5885],"errorMessage":"check admin exists"},{"pc":[1102,1643,2572,3988,4575,4698,5429],"errorMessage":"check borrowIndex exists"},{"pc":[1244,1529,1904,2043,3928,4339,5036,5217,5292,5520,5821],"errorMessage":"check comptroller exists"},{"pc":[3894],"errorMessage":"check decimals exists"},{"pc":[4624],"errorMessage":"check initialExchangeRateMantissa exists"},{"pc":[3066,3183,3939,4738,5904],"errorMessage":"check interestRateModel exists"},{"pc":[3856],"errorMessage":"check name exists"},{"pc":[3473,3497,3917],"errorMessage":"check pendingAdmin exists"},{"pc":[3168,3635,3950,4829],"errorMessage":"check reserveFactorMantissa exists"},{"pc":[3875],"errorMessage":"check symbol exists"},{"pc":[1603,3036,3138,3256,4007,4634,4688,5385],"errorMessage":"check totalBorrows exists"},{"pc":[2377,3051,3153,3734,4026,4642,4693,5652],"errorMessage":"check totalReserves exists"},{"pc":[1326,4045,4614,5105,5665],"errorMessage":"check totalSupply exists"},{"pc":[2263,2453,3845,4064,4104,4253],"errorMessage":"check underlying exists"},{"pc":[1120,2590],"errorMessage":"initial exchange rate must be greater than zero."},{"pc":[1040,1057,2510,2527],"errorMessage":"invalid array length header"},{"pc":[1048,1065,2518,2535],"errorMessage":"invalid number of bytes for arc4.dynamic_array<arc4.uint8>"},{"pc":[1010,1018,1026,1766,1811,1830,2246,2438,2488,2496,2652,2701,2709,2755,2818,2826,2859,2891,2946,3278,3318,3411,3419,3458,3561,3819],"errorMessage":"invalid number of bytes for arc4.static_array<arc4.uint8, 32>"},{"pc":[1034,1210,1419,1457,1495,1729,1774,1819,2343,2504,2660,2717,2764,3427,3587,3685],"errorMessage":"invalid number of bytes for arc4.uint256"},{"pc":[1077,2547],"errorMessage":"invalid number of bytes for arc4.uint64"},{"pc":[5855,5938],"errorMessage":"marker method returned false"},{"pc":[1109,2579],"errorMessage":"market may only be initialized once"},{"pc":[4975],"errorMessage":"one of redeemTokensIn or redeemAmountIn must be zero"},{"pc":[1087,2557],"errorMessage":"only admin may initialize the market"},{"pc":[2447],"errorMessage":"only the admin may set the comp-like delegate"},{"pc":[1305,1360,1370,1629,1648,1678,1689,2146,2391,2406,2785,2842,2874,2899,2931,2972,2987,2999,3104,3229,3261,3301,3326,3343,3360,3377,3394,3569,3644,3657,3774,3786,3831,3955,3974,3993,4012,4031,4050,4489,4800,4834,4896,4905,4916,4927,4983,5154,5178,5415,5434,5458,5469,5633,5728,5751,5774,5786,5979,6007,6038],"errorMessage":"overflow"},{"pc":[1215,1424,1462,1500,1734,1779,1835,2348,2665,2722,3241,3283,3432,3592,3690,4588],"errorMessage":"re-entered"},{"pc":[4197,4303],"errorMessage":"revert"},{"pc":[1128,2598],"errorMessage":"setting comptroller failed"},{"pc":[1150,2620],"errorMessage":"setting interest rate model failed"},{"pc":[2235],"errorMessage":"token seizure failed"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"events":[{"name":"NewComptroller","args":[{"type":"uint8[32]","name":"oldComptroller"},{"type":"uint8[32]","name":"newComptroller"}]},{"name":"NewMarketInterestRateModel","args":[{"type":"uint8[32]","name":"oldInterestRateModel"},{"type":"uint8[32]","name":"newInterestRateModel"}]},{"name":"AccrueInterest","args":[{"type":"uint256","name":"cashPrior"},{"type":"uint256","name":"interestAccumulated"},{"type":"uint256","name":"borrowIndex"},{"type":"uint256","name":"totalBorrows"}]},{"name":"Mint","args":[{"type":"uint8[32]","name":"minter"},{"type":"uint256","name":"mintAmount"},{"type":"uint256","name":"mintTokens"}]},{"name":"Transfer","args":[{"type":"uint8[32]","name":"from"},{"type":"uint8[32]","name":"to"},{"type":"uint256","name":"amount"}]},{"name":"Redeem","args":[{"type":"uint8[32]","name":"redeemer"},{"type":"uint256","name":"redeemAmount"},{"type":"uint256","name":"redeemTokens"}]},{"name":"Borrow","args":[{"type":"uint8[32]","name":"borrower"},{"type":"uint256","name":"borrowAmount"},{"type":"uint256","name":"accountBorrows"},{"type":"uint256","name":"totalBorrows"}]},{"name":"RepayBorrow","args":[{"type":"uint8[32]","name":"payer"},{"type":"uint8[32]","name":"borrower"},{"type":"uint256","name":"repayAmount"},{"type":"uint256","name":"accountBorrows"},{"type":"uint256","name":"totalBorrows"}]},{"name":"ReservesAdded","args":[{"type":"uint8[32]","name":"benefactor"},{"type":"uint256","name":"addAmount"},{"type":"uint256","name":"newTotalReserves"}]},{"name":"LiquidateBorrow","args":[{"type":"uint8[32]","name":"liquidator"},{"type":"uint8[32]","name":"borrower"},{"type":"uint256","name":"repayAmount"},{"type":"uint8[32]","name":"cTokenCollateral"},{"type":"uint256","name":"seizeTokens"}]},{"name":"Approval","args":[{"type":"uint8[32]","name":"owner"},{"type":"uint8[32]","name":"spender"},{"type":"uint256","name":"amount"}]},{"name":"NewPendingAdmin","args":[{"type":"uint8[32]","name":"oldPendingAdmin"},{"type":"uint8[32]","name":"newPendingAdmin"}]},{"name":"NewAdmin","args":[{"type":"uint8[32]","name":"oldAdmin"},{"type":"uint8[32]","name":"newAdmin"}]},{"name":"NewReserveFactor","args":[{"type":"uint256","name":"oldReserveFactorMantissa"},{"type":"uint256","name":"newReserveFactorMantissa"}]},{"name":"ReservesReduced","args":[{"type":"uint8[32]","name":"admin"},{"type":"uint256","name":"reduceAmount"},{"type":"uint256","name":"newTotalReserves"}]}]} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


// Type definitions for ARC-56 structs

export type BorrowSnapshot = {
  principal: bigint,
  interestIndex: bigint
}


/**
 * Converts the ABI tuple representation of a BorrowSnapshot to the struct representation
 */
export function BorrowSnapshotFromTuple(abiTuple: [bigint, bigint]) {
  return getABIStructFromABITuple(abiTuple, APP_SPEC.structs.BorrowSnapshot, APP_SPEC.structs) as BorrowSnapshot
}

/**
 * The argument types for the CErc20 contract
 */
export type CErc20Args = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'initialize(address,address,address,uint256,string,string,uint64)void': {
      underlying: string
      comptroller: string
      interestRateModel: string
      initialExchangeRateMantissa: bigint | number
      name: string
      symbol: string
      decimals: bigint | number
    }
    'mint(uint256)uint256': {
      mintAmount: bigint | number
    }
    'redeem(uint256)uint256': {
      redeemTokens: bigint | number
    }
    'redeemUnderlying(uint256)uint256': {
      redeemAmount: bigint | number
    }
    'borrow(uint256)uint256': {
      borrowAmount: bigint | number
    }
    'repayBorrow(uint256)uint256': {
      repayAmount: bigint | number
    }
    'repayBorrowBehalf(address,uint256)uint256': {
      borrower: string
      repayAmount: bigint | number
    }
    'liquidateBorrow(address,uint256,address)uint256': {
      borrower: string
      repayAmount: bigint | number
      cTokenCollateral: string
    }
    'sweepToken(address)void': {
      token: string
    }
    '_addReserves(uint256)uint256': {
      addAmount: bigint | number
    }
    '_delegateCompLikeTo(address)void': {
      compLikeDelegatee: string
    }
    'initialize(address,address,uint256,string,string,uint64)void': {
      comptroller: string
      interestRateModel: string
      initialExchangeRateMantissa: bigint | number
      name: string
      symbol: string
      decimals: bigint | number
    }
    'transfer(address,uint256)bool': {
      dst: string
      amount: bigint | number
    }
    'transferFrom(address,address,uint256)bool': {
      src: string
      dst: string
      amount: bigint | number
    }
    'approve(address,uint256)bool': {
      spender: string
      amount: bigint | number
    }
    'allowance(address,address)uint256': {
      owner: string
      spender: string
    }
    'balanceOf(address)uint256': {
      owner: string
    }
    'balanceOfUnderlying(address)uint256': {
      owner: string
    }
    'getAccountSnapshot(address)(uint256,uint256,uint256,uint256)': {
      account: string
    }
    'borrowRatePerBlock()uint256': Record<string, never>
    'supplyRatePerBlock()uint256': Record<string, never>
    'totalBorrowsCurrent()uint256': Record<string, never>
    'borrowBalanceCurrent(address)uint256': {
      account: string
    }
    'borrowBalanceStored(address)uint256': {
      account: string
    }
    'exchangeRateCurrent()uint256': Record<string, never>
    'exchangeRateStored()uint256': Record<string, never>
    'getCash()uint256': Record<string, never>
    'accrueInterest()uint256': Record<string, never>
    'seize(address,address,uint256)uint256': {
      liquidator: string
      borrower: string
      seizeTokens: bigint | number
    }
    '_setPendingAdmin(address)uint256': {
      newPendingAdmin: string
    }
    '_acceptAdmin()uint256': Record<string, never>
    '_setComptroller(address)uint256': {
      newComptroller: string
    }
    '_setReserveFactor(uint256)uint256': {
      newReserveFactorMantissa: bigint | number
    }
    '_reduceReserves(uint256)uint256': {
      reduceAmount: bigint | number
    }
    '_setInterestRateModel(address)uint256': {
      newInterestRateModel: string
    }
    'underlying()address': Record<string, never>
    'name()string': Record<string, never>
    'symbol()string': Record<string, never>
    'decimals()uint64': Record<string, never>
    'admin()address': Record<string, never>
    'pendingAdmin()address': Record<string, never>
    'comptroller()address': Record<string, never>
    'interestRateModel()address': Record<string, never>
    'reserveFactorMantissa()uint256': Record<string, never>
    'accrualBlockNumber()uint256': Record<string, never>
    'borrowIndex()uint256': Record<string, never>
    'totalBorrows()uint256': Record<string, never>
    'totalReserves()uint256': Record<string, never>
    'totalSupply()uint256': Record<string, never>
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'initialize(address,address,address,uint256,string,string,uint64)void': [underlying: string, comptroller: string, interestRateModel: string, initialExchangeRateMantissa: bigint | number, name: string, symbol: string, decimals: bigint | number]
    'mint(uint256)uint256': [mintAmount: bigint | number]
    'redeem(uint256)uint256': [redeemTokens: bigint | number]
    'redeemUnderlying(uint256)uint256': [redeemAmount: bigint | number]
    'borrow(uint256)uint256': [borrowAmount: bigint | number]
    'repayBorrow(uint256)uint256': [repayAmount: bigint | number]
    'repayBorrowBehalf(address,uint256)uint256': [borrower: string, repayAmount: bigint | number]
    'liquidateBorrow(address,uint256,address)uint256': [borrower: string, repayAmount: bigint | number, cTokenCollateral: string]
    'sweepToken(address)void': [token: string]
    '_addReserves(uint256)uint256': [addAmount: bigint | number]
    '_delegateCompLikeTo(address)void': [compLikeDelegatee: string]
    'initialize(address,address,uint256,string,string,uint64)void': [comptroller: string, interestRateModel: string, initialExchangeRateMantissa: bigint | number, name: string, symbol: string, decimals: bigint | number]
    'transfer(address,uint256)bool': [dst: string, amount: bigint | number]
    'transferFrom(address,address,uint256)bool': [src: string, dst: string, amount: bigint | number]
    'approve(address,uint256)bool': [spender: string, amount: bigint | number]
    'allowance(address,address)uint256': [owner: string, spender: string]
    'balanceOf(address)uint256': [owner: string]
    'balanceOfUnderlying(address)uint256': [owner: string]
    'getAccountSnapshot(address)(uint256,uint256,uint256,uint256)': [account: string]
    'borrowRatePerBlock()uint256': []
    'supplyRatePerBlock()uint256': []
    'totalBorrowsCurrent()uint256': []
    'borrowBalanceCurrent(address)uint256': [account: string]
    'borrowBalanceStored(address)uint256': [account: string]
    'exchangeRateCurrent()uint256': []
    'exchangeRateStored()uint256': []
    'getCash()uint256': []
    'accrueInterest()uint256': []
    'seize(address,address,uint256)uint256': [liquidator: string, borrower: string, seizeTokens: bigint | number]
    '_setPendingAdmin(address)uint256': [newPendingAdmin: string]
    '_acceptAdmin()uint256': []
    '_setComptroller(address)uint256': [newComptroller: string]
    '_setReserveFactor(uint256)uint256': [newReserveFactorMantissa: bigint | number]
    '_reduceReserves(uint256)uint256': [reduceAmount: bigint | number]
    '_setInterestRateModel(address)uint256': [newInterestRateModel: string]
    'underlying()address': []
    'name()string': []
    'symbol()string': []
    'decimals()uint64': []
    'admin()address': []
    'pendingAdmin()address': []
    'comptroller()address': []
    'interestRateModel()address': []
    'reserveFactorMantissa()uint256': []
    'accrualBlockNumber()uint256': []
    'borrowIndex()uint256': []
    'totalBorrows()uint256': []
    'totalReserves()uint256': []
    'totalSupply()uint256': []
  }
}

/**
 * The return type for each method
 */
export type CErc20Returns = {
  'initialize(address,address,address,uint256,string,string,uint64)void': void
  'mint(uint256)uint256': bigint
  'redeem(uint256)uint256': bigint
  'redeemUnderlying(uint256)uint256': bigint
  'borrow(uint256)uint256': bigint
  'repayBorrow(uint256)uint256': bigint
  'repayBorrowBehalf(address,uint256)uint256': bigint
  'liquidateBorrow(address,uint256,address)uint256': bigint
  'sweepToken(address)void': void
  '_addReserves(uint256)uint256': bigint
  '_delegateCompLikeTo(address)void': void
  'initialize(address,address,uint256,string,string,uint64)void': void
  'transfer(address,uint256)bool': boolean
  'transferFrom(address,address,uint256)bool': boolean
  'approve(address,uint256)bool': boolean
  'allowance(address,address)uint256': bigint
  'balanceOf(address)uint256': bigint
  'balanceOfUnderlying(address)uint256': bigint
  'getAccountSnapshot(address)(uint256,uint256,uint256,uint256)': [bigint, bigint, bigint, bigint]
  'borrowRatePerBlock()uint256': bigint
  'supplyRatePerBlock()uint256': bigint
  'totalBorrowsCurrent()uint256': bigint
  'borrowBalanceCurrent(address)uint256': bigint
  'borrowBalanceStored(address)uint256': bigint
  'exchangeRateCurrent()uint256': bigint
  'exchangeRateStored()uint256': bigint
  'getCash()uint256': bigint
  'accrueInterest()uint256': bigint
  'seize(address,address,uint256)uint256': bigint
  '_setPendingAdmin(address)uint256': bigint
  '_acceptAdmin()uint256': bigint
  '_setComptroller(address)uint256': bigint
  '_setReserveFactor(uint256)uint256': bigint
  '_reduceReserves(uint256)uint256': bigint
  '_setInterestRateModel(address)uint256': bigint
  'underlying()address': string
  'name()string': string
  'symbol()string': string
  'decimals()uint64': bigint
  'admin()address': string
  'pendingAdmin()address': string
  'comptroller()address': string
  'interestRateModel()address': string
  'reserveFactorMantissa()uint256': bigint
  'accrualBlockNumber()uint256': bigint
  'borrowIndex()uint256': bigint
  'totalBorrows()uint256': bigint
  'totalReserves()uint256': bigint
  'totalSupply()uint256': bigint
}

/**
 * Defines the types of available calls and state of the CErc20 smart contract.
 */
export type CErc20Types = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'initialize(address,address,address,uint256,string,string,uint64)void', {
      argsObj: CErc20Args['obj']['initialize(address,address,address,uint256,string,string,uint64)void']
      argsTuple: CErc20Args['tuple']['initialize(address,address,address,uint256,string,string,uint64)void']
      returns: CErc20Returns['initialize(address,address,address,uint256,string,string,uint64)void']
    }>
    & Record<'mint(uint256)uint256' | 'mint', {
      argsObj: CErc20Args['obj']['mint(uint256)uint256']
      argsTuple: CErc20Args['tuple']['mint(uint256)uint256']
      returns: CErc20Returns['mint(uint256)uint256']
    }>
    & Record<'redeem(uint256)uint256' | 'redeem', {
      argsObj: CErc20Args['obj']['redeem(uint256)uint256']
      argsTuple: CErc20Args['tuple']['redeem(uint256)uint256']
      returns: CErc20Returns['redeem(uint256)uint256']
    }>
    & Record<'redeemUnderlying(uint256)uint256' | 'redeemUnderlying', {
      argsObj: CErc20Args['obj']['redeemUnderlying(uint256)uint256']
      argsTuple: CErc20Args['tuple']['redeemUnderlying(uint256)uint256']
      returns: CErc20Returns['redeemUnderlying(uint256)uint256']
    }>
    & Record<'borrow(uint256)uint256' | 'borrow', {
      argsObj: CErc20Args['obj']['borrow(uint256)uint256']
      argsTuple: CErc20Args['tuple']['borrow(uint256)uint256']
      returns: CErc20Returns['borrow(uint256)uint256']
    }>
    & Record<'repayBorrow(uint256)uint256' | 'repayBorrow', {
      argsObj: CErc20Args['obj']['repayBorrow(uint256)uint256']
      argsTuple: CErc20Args['tuple']['repayBorrow(uint256)uint256']
      returns: CErc20Returns['repayBorrow(uint256)uint256']
    }>
    & Record<'repayBorrowBehalf(address,uint256)uint256' | 'repayBorrowBehalf', {
      argsObj: CErc20Args['obj']['repayBorrowBehalf(address,uint256)uint256']
      argsTuple: CErc20Args['tuple']['repayBorrowBehalf(address,uint256)uint256']
      returns: CErc20Returns['repayBorrowBehalf(address,uint256)uint256']
    }>
    & Record<'liquidateBorrow(address,uint256,address)uint256' | 'liquidateBorrow', {
      argsObj: CErc20Args['obj']['liquidateBorrow(address,uint256,address)uint256']
      argsTuple: CErc20Args['tuple']['liquidateBorrow(address,uint256,address)uint256']
      returns: CErc20Returns['liquidateBorrow(address,uint256,address)uint256']
    }>
    & Record<'sweepToken(address)void' | 'sweepToken', {
      argsObj: CErc20Args['obj']['sweepToken(address)void']
      argsTuple: CErc20Args['tuple']['sweepToken(address)void']
      returns: CErc20Returns['sweepToken(address)void']
    }>
    & Record<'_addReserves(uint256)uint256' | '_addReserves', {
      argsObj: CErc20Args['obj']['_addReserves(uint256)uint256']
      argsTuple: CErc20Args['tuple']['_addReserves(uint256)uint256']
      returns: CErc20Returns['_addReserves(uint256)uint256']
    }>
    & Record<'_delegateCompLikeTo(address)void' | '_delegateCompLikeTo', {
      argsObj: CErc20Args['obj']['_delegateCompLikeTo(address)void']
      argsTuple: CErc20Args['tuple']['_delegateCompLikeTo(address)void']
      returns: CErc20Returns['_delegateCompLikeTo(address)void']
    }>
    & Record<'initialize(address,address,uint256,string,string,uint64)void', {
      argsObj: CErc20Args['obj']['initialize(address,address,uint256,string,string,uint64)void']
      argsTuple: CErc20Args['tuple']['initialize(address,address,uint256,string,string,uint64)void']
      returns: CErc20Returns['initialize(address,address,uint256,string,string,uint64)void']
    }>
    & Record<'transfer(address,uint256)bool' | 'transfer', {
      argsObj: CErc20Args['obj']['transfer(address,uint256)bool']
      argsTuple: CErc20Args['tuple']['transfer(address,uint256)bool']
      returns: CErc20Returns['transfer(address,uint256)bool']
    }>
    & Record<'transferFrom(address,address,uint256)bool' | 'transferFrom', {
      argsObj: CErc20Args['obj']['transferFrom(address,address,uint256)bool']
      argsTuple: CErc20Args['tuple']['transferFrom(address,address,uint256)bool']
      returns: CErc20Returns['transferFrom(address,address,uint256)bool']
    }>
    & Record<'approve(address,uint256)bool' | 'approve', {
      argsObj: CErc20Args['obj']['approve(address,uint256)bool']
      argsTuple: CErc20Args['tuple']['approve(address,uint256)bool']
      returns: CErc20Returns['approve(address,uint256)bool']
    }>
    & Record<'allowance(address,address)uint256' | 'allowance', {
      argsObj: CErc20Args['obj']['allowance(address,address)uint256']
      argsTuple: CErc20Args['tuple']['allowance(address,address)uint256']
      returns: CErc20Returns['allowance(address,address)uint256']
    }>
    & Record<'balanceOf(address)uint256' | 'balanceOf', {
      argsObj: CErc20Args['obj']['balanceOf(address)uint256']
      argsTuple: CErc20Args['tuple']['balanceOf(address)uint256']
      returns: CErc20Returns['balanceOf(address)uint256']
    }>
    & Record<'balanceOfUnderlying(address)uint256' | 'balanceOfUnderlying', {
      argsObj: CErc20Args['obj']['balanceOfUnderlying(address)uint256']
      argsTuple: CErc20Args['tuple']['balanceOfUnderlying(address)uint256']
      returns: CErc20Returns['balanceOfUnderlying(address)uint256']
    }>
    & Record<'getAccountSnapshot(address)(uint256,uint256,uint256,uint256)' | 'getAccountSnapshot', {
      argsObj: CErc20Args['obj']['getAccountSnapshot(address)(uint256,uint256,uint256,uint256)']
      argsTuple: CErc20Args['tuple']['getAccountSnapshot(address)(uint256,uint256,uint256,uint256)']
      returns: CErc20Returns['getAccountSnapshot(address)(uint256,uint256,uint256,uint256)']
    }>
    & Record<'borrowRatePerBlock()uint256' | 'borrowRatePerBlock', {
      argsObj: CErc20Args['obj']['borrowRatePerBlock()uint256']
      argsTuple: CErc20Args['tuple']['borrowRatePerBlock()uint256']
      returns: CErc20Returns['borrowRatePerBlock()uint256']
    }>
    & Record<'supplyRatePerBlock()uint256' | 'supplyRatePerBlock', {
      argsObj: CErc20Args['obj']['supplyRatePerBlock()uint256']
      argsTuple: CErc20Args['tuple']['supplyRatePerBlock()uint256']
      returns: CErc20Returns['supplyRatePerBlock()uint256']
    }>
    & Record<'totalBorrowsCurrent()uint256' | 'totalBorrowsCurrent', {
      argsObj: CErc20Args['obj']['totalBorrowsCurrent()uint256']
      argsTuple: CErc20Args['tuple']['totalBorrowsCurrent()uint256']
      returns: CErc20Returns['totalBorrowsCurrent()uint256']
    }>
    & Record<'borrowBalanceCurrent(address)uint256' | 'borrowBalanceCurrent', {
      argsObj: CErc20Args['obj']['borrowBalanceCurrent(address)uint256']
      argsTuple: CErc20Args['tuple']['borrowBalanceCurrent(address)uint256']
      returns: CErc20Returns['borrowBalanceCurrent(address)uint256']
    }>
    & Record<'borrowBalanceStored(address)uint256' | 'borrowBalanceStored', {
      argsObj: CErc20Args['obj']['borrowBalanceStored(address)uint256']
      argsTuple: CErc20Args['tuple']['borrowBalanceStored(address)uint256']
      returns: CErc20Returns['borrowBalanceStored(address)uint256']
    }>
    & Record<'exchangeRateCurrent()uint256' | 'exchangeRateCurrent', {
      argsObj: CErc20Args['obj']['exchangeRateCurrent()uint256']
      argsTuple: CErc20Args['tuple']['exchangeRateCurrent()uint256']
      returns: CErc20Returns['exchangeRateCurrent()uint256']
    }>
    & Record<'exchangeRateStored()uint256' | 'exchangeRateStored', {
      argsObj: CErc20Args['obj']['exchangeRateStored()uint256']
      argsTuple: CErc20Args['tuple']['exchangeRateStored()uint256']
      returns: CErc20Returns['exchangeRateStored()uint256']
    }>
    & Record<'getCash()uint256' | 'getCash', {
      argsObj: CErc20Args['obj']['getCash()uint256']
      argsTuple: CErc20Args['tuple']['getCash()uint256']
      returns: CErc20Returns['getCash()uint256']
    }>
    & Record<'accrueInterest()uint256' | 'accrueInterest', {
      argsObj: CErc20Args['obj']['accrueInterest()uint256']
      argsTuple: CErc20Args['tuple']['accrueInterest()uint256']
      returns: CErc20Returns['accrueInterest()uint256']
    }>
    & Record<'seize(address,address,uint256)uint256' | 'seize', {
      argsObj: CErc20Args['obj']['seize(address,address,uint256)uint256']
      argsTuple: CErc20Args['tuple']['seize(address,address,uint256)uint256']
      returns: CErc20Returns['seize(address,address,uint256)uint256']
    }>
    & Record<'_setPendingAdmin(address)uint256' | '_setPendingAdmin', {
      argsObj: CErc20Args['obj']['_setPendingAdmin(address)uint256']
      argsTuple: CErc20Args['tuple']['_setPendingAdmin(address)uint256']
      returns: CErc20Returns['_setPendingAdmin(address)uint256']
    }>
    & Record<'_acceptAdmin()uint256' | '_acceptAdmin', {
      argsObj: CErc20Args['obj']['_acceptAdmin()uint256']
      argsTuple: CErc20Args['tuple']['_acceptAdmin()uint256']
      returns: CErc20Returns['_acceptAdmin()uint256']
    }>
    & Record<'_setComptroller(address)uint256' | '_setComptroller', {
      argsObj: CErc20Args['obj']['_setComptroller(address)uint256']
      argsTuple: CErc20Args['tuple']['_setComptroller(address)uint256']
      returns: CErc20Returns['_setComptroller(address)uint256']
    }>
    & Record<'_setReserveFactor(uint256)uint256' | '_setReserveFactor', {
      argsObj: CErc20Args['obj']['_setReserveFactor(uint256)uint256']
      argsTuple: CErc20Args['tuple']['_setReserveFactor(uint256)uint256']
      returns: CErc20Returns['_setReserveFactor(uint256)uint256']
    }>
    & Record<'_reduceReserves(uint256)uint256' | '_reduceReserves', {
      argsObj: CErc20Args['obj']['_reduceReserves(uint256)uint256']
      argsTuple: CErc20Args['tuple']['_reduceReserves(uint256)uint256']
      returns: CErc20Returns['_reduceReserves(uint256)uint256']
    }>
    & Record<'_setInterestRateModel(address)uint256' | '_setInterestRateModel', {
      argsObj: CErc20Args['obj']['_setInterestRateModel(address)uint256']
      argsTuple: CErc20Args['tuple']['_setInterestRateModel(address)uint256']
      returns: CErc20Returns['_setInterestRateModel(address)uint256']
    }>
    & Record<'underlying()address' | 'underlying', {
      argsObj: CErc20Args['obj']['underlying()address']
      argsTuple: CErc20Args['tuple']['underlying()address']
      returns: CErc20Returns['underlying()address']
    }>
    & Record<'name()string' | 'name', {
      argsObj: CErc20Args['obj']['name()string']
      argsTuple: CErc20Args['tuple']['name()string']
      returns: CErc20Returns['name()string']
    }>
    & Record<'symbol()string' | 'symbol', {
      argsObj: CErc20Args['obj']['symbol()string']
      argsTuple: CErc20Args['tuple']['symbol()string']
      returns: CErc20Returns['symbol()string']
    }>
    & Record<'decimals()uint64' | 'decimals', {
      argsObj: CErc20Args['obj']['decimals()uint64']
      argsTuple: CErc20Args['tuple']['decimals()uint64']
      returns: CErc20Returns['decimals()uint64']
    }>
    & Record<'admin()address' | 'admin', {
      argsObj: CErc20Args['obj']['admin()address']
      argsTuple: CErc20Args['tuple']['admin()address']
      returns: CErc20Returns['admin()address']
    }>
    & Record<'pendingAdmin()address' | 'pendingAdmin', {
      argsObj: CErc20Args['obj']['pendingAdmin()address']
      argsTuple: CErc20Args['tuple']['pendingAdmin()address']
      returns: CErc20Returns['pendingAdmin()address']
    }>
    & Record<'comptroller()address' | 'comptroller', {
      argsObj: CErc20Args['obj']['comptroller()address']
      argsTuple: CErc20Args['tuple']['comptroller()address']
      returns: CErc20Returns['comptroller()address']
    }>
    & Record<'interestRateModel()address' | 'interestRateModel', {
      argsObj: CErc20Args['obj']['interestRateModel()address']
      argsTuple: CErc20Args['tuple']['interestRateModel()address']
      returns: CErc20Returns['interestRateModel()address']
    }>
    & Record<'reserveFactorMantissa()uint256' | 'reserveFactorMantissa', {
      argsObj: CErc20Args['obj']['reserveFactorMantissa()uint256']
      argsTuple: CErc20Args['tuple']['reserveFactorMantissa()uint256']
      returns: CErc20Returns['reserveFactorMantissa()uint256']
    }>
    & Record<'accrualBlockNumber()uint256' | 'accrualBlockNumber', {
      argsObj: CErc20Args['obj']['accrualBlockNumber()uint256']
      argsTuple: CErc20Args['tuple']['accrualBlockNumber()uint256']
      returns: CErc20Returns['accrualBlockNumber()uint256']
    }>
    & Record<'borrowIndex()uint256' | 'borrowIndex', {
      argsObj: CErc20Args['obj']['borrowIndex()uint256']
      argsTuple: CErc20Args['tuple']['borrowIndex()uint256']
      returns: CErc20Returns['borrowIndex()uint256']
    }>
    & Record<'totalBorrows()uint256' | 'totalBorrows', {
      argsObj: CErc20Args['obj']['totalBorrows()uint256']
      argsTuple: CErc20Args['tuple']['totalBorrows()uint256']
      returns: CErc20Returns['totalBorrows()uint256']
    }>
    & Record<'totalReserves()uint256' | 'totalReserves', {
      argsObj: CErc20Args['obj']['totalReserves()uint256']
      argsTuple: CErc20Args['tuple']['totalReserves()uint256']
      returns: CErc20Returns['totalReserves()uint256']
    }>
    & Record<'totalSupply()uint256' | 'totalSupply', {
      argsObj: CErc20Args['obj']['totalSupply()uint256']
      argsTuple: CErc20Args['tuple']['totalSupply()uint256']
      returns: CErc20Returns['totalSupply()uint256']
    }>
  /**
   * Defines the shape of the state of the application.
   */
  state: {
    global: {
      keys: {
        underlying: string
        _notEntered: bigint
        name: string
        symbol: string
        decimals: bigint
        admin: string
        pendingAdmin: string
        comptroller: string
        interestRateModel: string
        initialExchangeRateMantissa: BinaryState
        reserveFactorMantissa: BinaryState
        accrualBlockNumber: BinaryState
        borrowIndex: BinaryState
        totalBorrows: BinaryState
        totalReserves: BinaryState
        totalSupply: BinaryState
      }
      maps: {}
    }
    box: {
      keys: {}
      maps: {
        accountTokens: Map<Uint8Array | string, Uint8Array>
        transferAllowances: Map<Uint8Array | string, Uint8Array>
        accountBorrows: Map<Uint8Array | string, BorrowSnapshot>
      }
    }
  }
}

/**
 * Defines the possible abi call signatures.
 */
export type CErc20Signatures = keyof CErc20Types['methods']
/**
 * Defines the possible abi call signatures for methods that return a non-void value.
 */
export type CErc20NonVoidMethodSignatures = keyof CErc20Types['methods'] extends infer T ? T extends keyof CErc20Types['methods'] ? MethodReturn<T> extends void ? never : T  : never : never
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the CErc20 smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends CErc20Signatures> = CErc20Types['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the CErc20 smart contract to the method's return type
 */
export type MethodReturn<TSignature extends CErc20Signatures> = CErc20Types['methods'][TSignature]['returns']

/**
 * Defines the shape of the keyed global state of the application.
 */
export type GlobalKeysState = CErc20Types['state']['global']['keys']

/**
 * Defines the shape of the keyed box state of the application.
 */
export type BoxKeysState = CErc20Types['state']['box']['keys']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the CErc20 smart contract
 */
export abstract class CErc20ParamsFactory {
  /**
   * Constructs a no op call for the initialize(address,address,address,uint256,string,string,uint64)void ABI method
   *
  *  @notice Initialize the new money market
  @param underlying_ The address of the underlying asset
  @param comptroller_ The address of the Comptroller
  @param interestRateModel_ The address of the interest rate model
  @param initialExchangeRateMantissa_ The initial exchange rate, scaled by 1e18
  @param name_ ERC-20 name of this token
  @param symbol_ ERC-20 symbol of this token
  @param decimals_ ERC-20 decimal precision of this token

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static initializeAddressAddressAddressUint256StringStringUint64Void(params: CallParams<CErc20Args['obj']['initialize(address,address,address,uint256,string,string,uint64)void'] | CErc20Args['tuple']['initialize(address,address,address,uint256,string,string,uint64)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'initialize(address,address,address,uint256,string,string,uint64)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.underlying, params.args.comptroller, params.args.interestRateModel, params.args.initialExchangeRateMantissa, params.args.name, params.args.symbol, params.args.decimals],
    }
  }
  /**
   * Constructs a no op call for the mint(uint256)uint256 ABI method
   *
  *  @notice Sender supplies assets into the market and receives cTokens in exchange
  @dev Accrues interest whether or not the operation succeeds, unless reverted
  @param mintAmount The amount of the underlying asset to supply
  @return uint 0=success, otherwise a failure (see ErrorReporter.sol for details)

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static mint(params: CallParams<CErc20Args['obj']['mint(uint256)uint256'] | CErc20Args['tuple']['mint(uint256)uint256']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'mint(uint256)uint256' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.mintAmount],
    }
  }
  /**
   * Constructs a no op call for the redeem(uint256)uint256 ABI method
   *
  *  @notice Sender redeems cTokens in exchange for the underlying asset
  @dev Accrues interest whether or not the operation succeeds, unless reverted
  @param redeemTokens The number of cTokens to redeem into underlying
  @return uint 0=success, otherwise a failure (see ErrorReporter.sol for details)

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static redeem(params: CallParams<CErc20Args['obj']['redeem(uint256)uint256'] | CErc20Args['tuple']['redeem(uint256)uint256']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'redeem(uint256)uint256' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.redeemTokens],
    }
  }
  /**
   * Constructs a no op call for the redeemUnderlying(uint256)uint256 ABI method
   *
  *  @notice Sender redeems cTokens in exchange for a specified amount of underlying asset
  @dev Accrues interest whether or not the operation succeeds, unless reverted
  @param redeemAmount The amount of underlying to redeem
  @return uint 0=success, otherwise a failure (see ErrorReporter.sol for details)

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static redeemUnderlying(params: CallParams<CErc20Args['obj']['redeemUnderlying(uint256)uint256'] | CErc20Args['tuple']['redeemUnderlying(uint256)uint256']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'redeemUnderlying(uint256)uint256' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.redeemAmount],
    }
  }
  /**
   * Constructs a no op call for the borrow(uint256)uint256 ABI method
   *
  *  @notice Sender borrows assets from the protocol to their own address
  @param borrowAmount The amount of the underlying asset to borrow
  @return uint 0=success, otherwise a failure (see ErrorReporter.sol for details)

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static borrow(params: CallParams<CErc20Args['obj']['borrow(uint256)uint256'] | CErc20Args['tuple']['borrow(uint256)uint256']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'borrow(uint256)uint256' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.borrowAmount],
    }
  }
  /**
   * Constructs a no op call for the repayBorrow(uint256)uint256 ABI method
   *
  *  @notice Sender repays their own borrow
  @param repayAmount The amount to repay, or -1 for the full outstanding amount
  @return uint 0=success, otherwise a failure (see ErrorReporter.sol for details)

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static repayBorrow(params: CallParams<CErc20Args['obj']['repayBorrow(uint256)uint256'] | CErc20Args['tuple']['repayBorrow(uint256)uint256']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'repayBorrow(uint256)uint256' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.repayAmount],
    }
  }
  /**
   * Constructs a no op call for the repayBorrowBehalf(address,uint256)uint256 ABI method
   *
  *  @notice Sender repays a borrow belonging to borrower
  @param borrower the account with the debt being payed off
  @param repayAmount The amount to repay, or -1 for the full outstanding amount
  @return uint 0=success, otherwise a failure (see ErrorReporter.sol for details)

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static repayBorrowBehalf(params: CallParams<CErc20Args['obj']['repayBorrowBehalf(address,uint256)uint256'] | CErc20Args['tuple']['repayBorrowBehalf(address,uint256)uint256']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'repayBorrowBehalf(address,uint256)uint256' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.borrower, params.args.repayAmount],
    }
  }
  /**
   * Constructs a no op call for the liquidateBorrow(address,uint256,address)uint256 ABI method
   *
  *  @notice The sender liquidates the borrowers collateral.
   The collateral seized is transferred to the liquidator.
  @param borrower The borrower of this cToken to be liquidated
  @param repayAmount The amount of the underlying borrowed asset to repay
  @param cTokenCollateral The market in which to seize collateral from the borrower
  @return uint 0=success, otherwise a failure (see ErrorReporter.sol for details)

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static liquidateBorrow(params: CallParams<CErc20Args['obj']['liquidateBorrow(address,uint256,address)uint256'] | CErc20Args['tuple']['liquidateBorrow(address,uint256,address)uint256']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'liquidateBorrow(address,uint256,address)uint256' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.borrower, params.args.repayAmount, params.args.cTokenCollateral],
    }
  }
  /**
   * Constructs a no op call for the sweepToken(address)void ABI method
   *
  *  @notice A public function to sweep accidental ERC-20 transfers to this contract. Tokens are sent to admin (timelock)
  @param token The address of the ERC-20 token to sweep

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static sweepToken(params: CallParams<CErc20Args['obj']['sweepToken(address)void'] | CErc20Args['tuple']['sweepToken(address)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'sweepToken(address)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.token],
    }
  }
  /**
   * Constructs a no op call for the _addReserves(uint256)uint256 ABI method
   *
  *  @notice The sender adds to reserves.
  @param addAmount The amount fo underlying token to add as reserves
  @return uint 0=success, otherwise a failure (see ErrorReporter.sol for details)

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static _addReserves(params: CallParams<CErc20Args['obj']['_addReserves(uint256)uint256'] | CErc20Args['tuple']['_addReserves(uint256)uint256']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: '_addReserves(uint256)uint256' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.addAmount],
    }
  }
  /**
   * Constructs a no op call for the _delegateCompLikeTo(address)void ABI method
   *
  *  @notice Admin call to delegate the votes of the COMP-like underlying
  @param compLikeDelegatee The address to delegate votes to
  @dev CTokens whose underlying are not CompLike should revert here

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static _delegateCompLikeTo(params: CallParams<CErc20Args['obj']['_delegateCompLikeTo(address)void'] | CErc20Args['tuple']['_delegateCompLikeTo(address)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: '_delegateCompLikeTo(address)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.compLikeDelegatee],
    }
  }
  /**
   * Constructs a no op call for the initialize(address,address,uint256,string,string,uint64)void ABI method
   *
  *  @notice Initialize the money market
  @param comptroller_ The address of the Comptroller
  @param interestRateModel_ The address of the interest rate model
  @param initialExchangeRateMantissa_ The initial exchange rate, scaled by 1e18
  @param name_ EIP-20 name of this token
  @param symbol_ EIP-20 symbol of this token
  @param decimals_ EIP-20 decimal precision of this token

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static initializeAddressAddressUint256StringStringUint64Void(params: CallParams<CErc20Args['obj']['initialize(address,address,uint256,string,string,uint64)void'] | CErc20Args['tuple']['initialize(address,address,uint256,string,string,uint64)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'initialize(address,address,uint256,string,string,uint64)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.comptroller, params.args.interestRateModel, params.args.initialExchangeRateMantissa, params.args.name, params.args.symbol, params.args.decimals],
    }
  }
  /**
   * Constructs a no op call for the transfer(address,uint256)bool ABI method
   *
  *  @notice Transfer `amount` tokens from `msg.sender` to `dst`
  @param dst The address of the destination account
  @param amount The number of tokens to transfer
  @return Whether or not the transfer succeeded

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static transfer(params: CallParams<CErc20Args['obj']['transfer(address,uint256)bool'] | CErc20Args['tuple']['transfer(address,uint256)bool']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'transfer(address,uint256)bool' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.dst, params.args.amount],
    }
  }
  /**
   * Constructs a no op call for the transferFrom(address,address,uint256)bool ABI method
   *
  *  @notice Transfer `amount` tokens from `src` to `dst`
  @param src The address of the source account
  @param dst The address of the destination account
  @param amount The number of tokens to transfer
  @return Whether or not the transfer succeeded

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static transferFrom(params: CallParams<CErc20Args['obj']['transferFrom(address,address,uint256)bool'] | CErc20Args['tuple']['transferFrom(address,address,uint256)bool']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'transferFrom(address,address,uint256)bool' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.src, params.args.dst, params.args.amount],
    }
  }
  /**
   * Constructs a no op call for the approve(address,uint256)bool ABI method
   *
  *  @notice Approve `spender` to transfer up to `amount` from `src`
  @dev This will overwrite the approval amount for `spender`
   and is subject to issues noted [here](https://eips.ethereum.org/EIPS/eip-20#approve)
  @param spender The address of the account which may transfer tokens
  @param amount The number of tokens that are approved (uint256.max means infinite)
  @return Whether or not the approval succeeded

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static approve(params: CallParams<CErc20Args['obj']['approve(address,uint256)bool'] | CErc20Args['tuple']['approve(address,uint256)bool']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'approve(address,uint256)bool' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.spender, params.args.amount],
    }
  }
  /**
   * Constructs a no op call for the allowance(address,address)uint256 ABI method
   *
  *  @notice Get the current allowance from `owner` for `spender`
  @param owner The address of the account which owns the tokens to be spent
  @param spender The address of the account which may transfer tokens
  @return The number of tokens allowed to be spent (-1 means infinite)

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static allowance(params: CallParams<CErc20Args['obj']['allowance(address,address)uint256'] | CErc20Args['tuple']['allowance(address,address)uint256']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'allowance(address,address)uint256' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.owner, params.args.spender],
    }
  }
  /**
   * Constructs a no op call for the balanceOf(address)uint256 ABI method
   *
  *  @notice Get the token balance of the `owner`
  @param owner The address of the account to query
  @return The number of tokens owned by `owner`

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static balanceOf(params: CallParams<CErc20Args['obj']['balanceOf(address)uint256'] | CErc20Args['tuple']['balanceOf(address)uint256']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'balanceOf(address)uint256' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.owner],
    }
  }
  /**
   * Constructs a no op call for the balanceOfUnderlying(address)uint256 ABI method
   *
  *  @notice Get the underlying balance of the `owner`
  @dev This also accrues interest in a transaction
  @param owner The address of the account to query
  @return The amount of underlying owned by `owner`

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static balanceOfUnderlying(params: CallParams<CErc20Args['obj']['balanceOfUnderlying(address)uint256'] | CErc20Args['tuple']['balanceOfUnderlying(address)uint256']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'balanceOfUnderlying(address)uint256' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.owner],
    }
  }
  /**
   * Constructs a no op call for the getAccountSnapshot(address)(uint256,uint256,uint256,uint256) ABI method
   *
  *  @notice Get a snapshot of the account's balances, and the cached exchange rate
  @dev This is used by comptroller to more efficiently perform liquidity checks.
  @param account Address of the account to snapshot
  @return (possible error, token balance, borrow balance, exchange rate mantissa)

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static getAccountSnapshot(params: CallParams<CErc20Args['obj']['getAccountSnapshot(address)(uint256,uint256,uint256,uint256)'] | CErc20Args['tuple']['getAccountSnapshot(address)(uint256,uint256,uint256,uint256)']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'getAccountSnapshot(address)(uint256,uint256,uint256,uint256)' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.account],
    }
  }
  /**
   * Constructs a no op call for the borrowRatePerBlock()uint256 ABI method
   *
  *  @notice Returns the current per-block borrow interest rate for this cToken
  @return The borrow interest rate per block, scaled by 1e18

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static borrowRatePerBlock(params: CallParams<CErc20Args['obj']['borrowRatePerBlock()uint256'] | CErc20Args['tuple']['borrowRatePerBlock()uint256']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'borrowRatePerBlock()uint256' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the supplyRatePerBlock()uint256 ABI method
   *
  *  @notice Returns the current per-block supply interest rate for this cToken
  @return The supply interest rate per block, scaled by 1e18

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static supplyRatePerBlock(params: CallParams<CErc20Args['obj']['supplyRatePerBlock()uint256'] | CErc20Args['tuple']['supplyRatePerBlock()uint256']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'supplyRatePerBlock()uint256' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the totalBorrowsCurrent()uint256 ABI method
   *
  *  @notice Returns the current total borrows plus accrued interest
  @return The total borrows with interest

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static totalBorrowsCurrent(params: CallParams<CErc20Args['obj']['totalBorrowsCurrent()uint256'] | CErc20Args['tuple']['totalBorrowsCurrent()uint256']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'totalBorrowsCurrent()uint256' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the borrowBalanceCurrent(address)uint256 ABI method
   *
  *  @notice Accrue interest to updated borrowIndex and then calculate account's borrow balance using the updated borrowIndex
  @param account The address whose balance should be calculated after updating borrowIndex
  @return The calculated balance

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static borrowBalanceCurrent(params: CallParams<CErc20Args['obj']['borrowBalanceCurrent(address)uint256'] | CErc20Args['tuple']['borrowBalanceCurrent(address)uint256']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'borrowBalanceCurrent(address)uint256' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.account],
    }
  }
  /**
   * Constructs a no op call for the borrowBalanceStored(address)uint256 ABI method
   *
  *  @notice Return the borrow balance of account based on stored data
  @param account The address whose balance should be calculated
  @return The calculated balance

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static borrowBalanceStored(params: CallParams<CErc20Args['obj']['borrowBalanceStored(address)uint256'] | CErc20Args['tuple']['borrowBalanceStored(address)uint256']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'borrowBalanceStored(address)uint256' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.account],
    }
  }
  /**
   * Constructs a no op call for the exchangeRateCurrent()uint256 ABI method
   *
  *  @notice Accrue interest then return the up-to-date exchange rate
  @return Calculated exchange rate scaled by 1e18

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static exchangeRateCurrent(params: CallParams<CErc20Args['obj']['exchangeRateCurrent()uint256'] | CErc20Args['tuple']['exchangeRateCurrent()uint256']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'exchangeRateCurrent()uint256' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the exchangeRateStored()uint256 ABI method
   *
  *  @notice Calculates the exchange rate from the underlying to the CToken
  @dev This function does not accrue interest before calculating the exchange rate
  @return Calculated exchange rate scaled by 1e18

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static exchangeRateStored(params: CallParams<CErc20Args['obj']['exchangeRateStored()uint256'] | CErc20Args['tuple']['exchangeRateStored()uint256']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'exchangeRateStored()uint256' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the getCash()uint256 ABI method
   *
  *  @notice Get cash balance of this cToken in the underlying asset
  @return The quantity of underlying asset owned by this contract

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static getCash(params: CallParams<CErc20Args['obj']['getCash()uint256'] | CErc20Args['tuple']['getCash()uint256']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'getCash()uint256' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the accrueInterest()uint256 ABI method
   *
  *  @notice Applies accrued interest to total borrows and reserves
  @dev This calculates interest accrued from the last checkpointed block
    up to the current block and writes new checkpoint to storage.

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static accrueInterest(params: CallParams<CErc20Args['obj']['accrueInterest()uint256'] | CErc20Args['tuple']['accrueInterest()uint256']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'accrueInterest()uint256' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the seize(address,address,uint256)uint256 ABI method
   *
  *  @notice Transfers collateral tokens (this market) to the liquidator.
  @dev Will fail unless called by another cToken during the process of liquidation.
   Its absolutely critical to use msg.sender as the borrowed cToken and not a parameter.
  @param liquidator The account receiving seized collateral
  @param borrower The account having collateral seized
  @param seizeTokens The number of cTokens to seize
  @return uint 0=success, otherwise a failure (see ErrorReporter.sol for details)

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static seize(params: CallParams<CErc20Args['obj']['seize(address,address,uint256)uint256'] | CErc20Args['tuple']['seize(address,address,uint256)uint256']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'seize(address,address,uint256)uint256' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.liquidator, params.args.borrower, params.args.seizeTokens],
    }
  }
  /**
   * Constructs a no op call for the _setPendingAdmin(address)uint256 ABI method
   *
  *  @notice Begins transfer of admin rights. The newPendingAdmin must call `_acceptAdmin` to finalize the transfer.
  @dev Admin function to begin change of admin. The newPendingAdmin must call `_acceptAdmin` to finalize the transfer.
  @param newPendingAdmin New pending admin.
  @return uint 0=success, otherwise a failure (see ErrorReporter.sol for details)

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static _setPendingAdmin(params: CallParams<CErc20Args['obj']['_setPendingAdmin(address)uint256'] | CErc20Args['tuple']['_setPendingAdmin(address)uint256']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: '_setPendingAdmin(address)uint256' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.newPendingAdmin],
    }
  }
  /**
   * Constructs a no op call for the _acceptAdmin()uint256 ABI method
   *
  *  @notice Accepts transfer of admin rights. msg.sender must be pendingAdmin
  @dev Admin function for pending admin to accept role and update admin
  @return uint 0=success, otherwise a failure (see ErrorReporter.sol for details)

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static _acceptAdmin(params: CallParams<CErc20Args['obj']['_acceptAdmin()uint256'] | CErc20Args['tuple']['_acceptAdmin()uint256']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: '_acceptAdmin()uint256' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the _setComptroller(address)uint256 ABI method
   *
  *  @notice Sets a new comptroller for the market
  @dev Admin function to set a new comptroller
  @return uint 0=success, otherwise a failure (see ErrorReporter.sol for details)

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static _setComptroller(params: CallParams<CErc20Args['obj']['_setComptroller(address)uint256'] | CErc20Args['tuple']['_setComptroller(address)uint256']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: '_setComptroller(address)uint256' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.newComptroller],
    }
  }
  /**
   * Constructs a no op call for the _setReserveFactor(uint256)uint256 ABI method
   *
  *  @notice accrues interest and sets a new reserve factor for the protocol using _setReserveFactorFresh
  @dev Admin function to accrue interest and set a new reserve factor
  @return uint 0=success, otherwise a failure (see ErrorReporter.sol for details)

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static _setReserveFactor(params: CallParams<CErc20Args['obj']['_setReserveFactor(uint256)uint256'] | CErc20Args['tuple']['_setReserveFactor(uint256)uint256']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: '_setReserveFactor(uint256)uint256' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.newReserveFactorMantissa],
    }
  }
  /**
   * Constructs a no op call for the _reduceReserves(uint256)uint256 ABI method
   *
  *  @notice Accrues interest and reduces reserves by transferring to admin
  @param reduceAmount Amount of reduction to reserves
  @return uint 0=success, otherwise a failure (see ErrorReporter.sol for details)

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static _reduceReserves(params: CallParams<CErc20Args['obj']['_reduceReserves(uint256)uint256'] | CErc20Args['tuple']['_reduceReserves(uint256)uint256']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: '_reduceReserves(uint256)uint256' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.reduceAmount],
    }
  }
  /**
   * Constructs a no op call for the _setInterestRateModel(address)uint256 ABI method
   *
  *  @notice accrues interest and updates the interest rate model using _setInterestRateModelFresh
  @dev Admin function to accrue interest and update the interest rate model
  @param newInterestRateModel the new interest rate model to use
  @return uint 0=success, otherwise a failure (see ErrorReporter.sol for details)

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static _setInterestRateModel(params: CallParams<CErc20Args['obj']['_setInterestRateModel(address)uint256'] | CErc20Args['tuple']['_setInterestRateModel(address)uint256']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: '_setInterestRateModel(address)uint256' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.newInterestRateModel],
    }
  }
  /**
   * Constructs a no op call for the underlying()address ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static underlying(params: CallParams<CErc20Args['obj']['underlying()address'] | CErc20Args['tuple']['underlying()address']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'underlying()address' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the name()string ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static name(params: CallParams<CErc20Args['obj']['name()string'] | CErc20Args['tuple']['name()string']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'name()string' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the symbol()string ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static symbol(params: CallParams<CErc20Args['obj']['symbol()string'] | CErc20Args['tuple']['symbol()string']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'symbol()string' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the decimals()uint64 ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static decimals(params: CallParams<CErc20Args['obj']['decimals()uint64'] | CErc20Args['tuple']['decimals()uint64']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'decimals()uint64' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the admin()address ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static admin(params: CallParams<CErc20Args['obj']['admin()address'] | CErc20Args['tuple']['admin()address']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'admin()address' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the pendingAdmin()address ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static pendingAdmin(params: CallParams<CErc20Args['obj']['pendingAdmin()address'] | CErc20Args['tuple']['pendingAdmin()address']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'pendingAdmin()address' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the comptroller()address ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static comptroller(params: CallParams<CErc20Args['obj']['comptroller()address'] | CErc20Args['tuple']['comptroller()address']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'comptroller()address' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the interestRateModel()address ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static interestRateModel(params: CallParams<CErc20Args['obj']['interestRateModel()address'] | CErc20Args['tuple']['interestRateModel()address']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'interestRateModel()address' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the reserveFactorMantissa()uint256 ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static reserveFactorMantissa(params: CallParams<CErc20Args['obj']['reserveFactorMantissa()uint256'] | CErc20Args['tuple']['reserveFactorMantissa()uint256']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'reserveFactorMantissa()uint256' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the accrualBlockNumber()uint256 ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static accrualBlockNumber(params: CallParams<CErc20Args['obj']['accrualBlockNumber()uint256'] | CErc20Args['tuple']['accrualBlockNumber()uint256']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'accrualBlockNumber()uint256' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the borrowIndex()uint256 ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static borrowIndex(params: CallParams<CErc20Args['obj']['borrowIndex()uint256'] | CErc20Args['tuple']['borrowIndex()uint256']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'borrowIndex()uint256' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the totalBorrows()uint256 ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static totalBorrows(params: CallParams<CErc20Args['obj']['totalBorrows()uint256'] | CErc20Args['tuple']['totalBorrows()uint256']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'totalBorrows()uint256' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the totalReserves()uint256 ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static totalReserves(params: CallParams<CErc20Args['obj']['totalReserves()uint256'] | CErc20Args['tuple']['totalReserves()uint256']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'totalReserves()uint256' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the totalSupply()uint256 ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static totalSupply(params: CallParams<CErc20Args['obj']['totalSupply()uint256'] | CErc20Args['tuple']['totalSupply()uint256']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'totalSupply()uint256' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
}

/**
 * A client to make calls to the CErc20 smart contract
 */
export class CErc20Client {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `CErc20Client`
   *
   * @param appClient An `AppClient` instance which has been created with the CErc20 app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `CErc20Client`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Checks for decode errors on the given return value and maps the return value to the return type for the given method
   * @returns The typed return value or undefined if there was no value
   */
  decodeReturnValue<TSignature extends CErc20NonVoidMethodSignatures>(method: TSignature, returnValue: ABIReturn | undefined) {
    return returnValue !== undefined ? getArc56ReturnValue<MethodReturn<TSignature>>(returnValue, this.appClient.getABIMethod(method), APP_SPEC.structs) : undefined
  }

  /**
   * Returns a new `CErc20Client` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<CErc20Client> {
    return new CErc20Client(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `CErc20Client` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<CErc20Client> {
    return new CErc20Client(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Makes a clear_state call to an existing instance of the CErc20 smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the CErc20 smart contract using the `initialize(address,address,address,uint256,string,string,uint64)void` ABI method.
     *
    *  @notice Initialize the new money market
    @param underlying_ The address of the underlying asset
    @param comptroller_ The address of the Comptroller
    @param interestRateModel_ The address of the interest rate model
    @param initialExchangeRateMantissa_ The initial exchange rate, scaled by 1e18
    @param name_ ERC-20 name of this token
    @param symbol_ ERC-20 symbol of this token
    @param decimals_ ERC-20 decimal precision of this token

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    initializeAddressAddressAddressUint256StringStringUint64Void: (params: CallParams<CErc20Args['obj']['initialize(address,address,address,uint256,string,string,uint64)void'] | CErc20Args['tuple']['initialize(address,address,address,uint256,string,string,uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(CErc20ParamsFactory.initializeAddressAddressAddressUint256StringStringUint64Void(params))
    },

    /**
     * Makes a call to the CErc20 smart contract using the `mint(uint256)uint256` ABI method.
     *
    *  @notice Sender supplies assets into the market and receives cTokens in exchange
    @dev Accrues interest whether or not the operation succeeds, unless reverted
    @param mintAmount The amount of the underlying asset to supply
    @return uint 0=success, otherwise a failure (see ErrorReporter.sol for details)

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    mint: (params: CallParams<CErc20Args['obj']['mint(uint256)uint256'] | CErc20Args['tuple']['mint(uint256)uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(CErc20ParamsFactory.mint(params))
    },

    /**
     * Makes a call to the CErc20 smart contract using the `redeem(uint256)uint256` ABI method.
     *
    *  @notice Sender redeems cTokens in exchange for the underlying asset
    @dev Accrues interest whether or not the operation succeeds, unless reverted
    @param redeemTokens The number of cTokens to redeem into underlying
    @return uint 0=success, otherwise a failure (see ErrorReporter.sol for details)

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    redeem: (params: CallParams<CErc20Args['obj']['redeem(uint256)uint256'] | CErc20Args['tuple']['redeem(uint256)uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(CErc20ParamsFactory.redeem(params))
    },

    /**
     * Makes a call to the CErc20 smart contract using the `redeemUnderlying(uint256)uint256` ABI method.
     *
    *  @notice Sender redeems cTokens in exchange for a specified amount of underlying asset
    @dev Accrues interest whether or not the operation succeeds, unless reverted
    @param redeemAmount The amount of underlying to redeem
    @return uint 0=success, otherwise a failure (see ErrorReporter.sol for details)

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    redeemUnderlying: (params: CallParams<CErc20Args['obj']['redeemUnderlying(uint256)uint256'] | CErc20Args['tuple']['redeemUnderlying(uint256)uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(CErc20ParamsFactory.redeemUnderlying(params))
    },

    /**
     * Makes a call to the CErc20 smart contract using the `borrow(uint256)uint256` ABI method.
     *
    *  @notice Sender borrows assets from the protocol to their own address
    @param borrowAmount The amount of the underlying asset to borrow
    @return uint 0=success, otherwise a failure (see ErrorReporter.sol for details)

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    borrow: (params: CallParams<CErc20Args['obj']['borrow(uint256)uint256'] | CErc20Args['tuple']['borrow(uint256)uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(CErc20ParamsFactory.borrow(params))
    },

    /**
     * Makes a call to the CErc20 smart contract using the `repayBorrow(uint256)uint256` ABI method.
     *
    *  @notice Sender repays their own borrow
    @param repayAmount The amount to repay, or -1 for the full outstanding amount
    @return uint 0=success, otherwise a failure (see ErrorReporter.sol for details)

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    repayBorrow: (params: CallParams<CErc20Args['obj']['repayBorrow(uint256)uint256'] | CErc20Args['tuple']['repayBorrow(uint256)uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(CErc20ParamsFactory.repayBorrow(params))
    },

    /**
     * Makes a call to the CErc20 smart contract using the `repayBorrowBehalf(address,uint256)uint256` ABI method.
     *
    *  @notice Sender repays a borrow belonging to borrower
    @param borrower the account with the debt being payed off
    @param repayAmount The amount to repay, or -1 for the full outstanding amount
    @return uint 0=success, otherwise a failure (see ErrorReporter.sol for details)

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    repayBorrowBehalf: (params: CallParams<CErc20Args['obj']['repayBorrowBehalf(address,uint256)uint256'] | CErc20Args['tuple']['repayBorrowBehalf(address,uint256)uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(CErc20ParamsFactory.repayBorrowBehalf(params))
    },

    /**
     * Makes a call to the CErc20 smart contract using the `liquidateBorrow(address,uint256,address)uint256` ABI method.
     *
    *  @notice The sender liquidates the borrowers collateral.
     The collateral seized is transferred to the liquidator.
    @param borrower The borrower of this cToken to be liquidated
    @param repayAmount The amount of the underlying borrowed asset to repay
    @param cTokenCollateral The market in which to seize collateral from the borrower
    @return uint 0=success, otherwise a failure (see ErrorReporter.sol for details)

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    liquidateBorrow: (params: CallParams<CErc20Args['obj']['liquidateBorrow(address,uint256,address)uint256'] | CErc20Args['tuple']['liquidateBorrow(address,uint256,address)uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(CErc20ParamsFactory.liquidateBorrow(params))
    },

    /**
     * Makes a call to the CErc20 smart contract using the `sweepToken(address)void` ABI method.
     *
    *  @notice A public function to sweep accidental ERC-20 transfers to this contract. Tokens are sent to admin (timelock)
    @param token The address of the ERC-20 token to sweep

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    sweepToken: (params: CallParams<CErc20Args['obj']['sweepToken(address)void'] | CErc20Args['tuple']['sweepToken(address)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(CErc20ParamsFactory.sweepToken(params))
    },

    /**
     * Makes a call to the CErc20 smart contract using the `_addReserves(uint256)uint256` ABI method.
     *
    *  @notice The sender adds to reserves.
    @param addAmount The amount fo underlying token to add as reserves
    @return uint 0=success, otherwise a failure (see ErrorReporter.sol for details)

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    _addReserves: (params: CallParams<CErc20Args['obj']['_addReserves(uint256)uint256'] | CErc20Args['tuple']['_addReserves(uint256)uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(CErc20ParamsFactory._addReserves(params))
    },

    /**
     * Makes a call to the CErc20 smart contract using the `_delegateCompLikeTo(address)void` ABI method.
     *
    *  @notice Admin call to delegate the votes of the COMP-like underlying
    @param compLikeDelegatee The address to delegate votes to
    @dev CTokens whose underlying are not CompLike should revert here

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    _delegateCompLikeTo: (params: CallParams<CErc20Args['obj']['_delegateCompLikeTo(address)void'] | CErc20Args['tuple']['_delegateCompLikeTo(address)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(CErc20ParamsFactory._delegateCompLikeTo(params))
    },

    /**
     * Makes a call to the CErc20 smart contract using the `initialize(address,address,uint256,string,string,uint64)void` ABI method.
     *
    *  @notice Initialize the money market
    @param comptroller_ The address of the Comptroller
    @param interestRateModel_ The address of the interest rate model
    @param initialExchangeRateMantissa_ The initial exchange rate, scaled by 1e18
    @param name_ EIP-20 name of this token
    @param symbol_ EIP-20 symbol of this token
    @param decimals_ EIP-20 decimal precision of this token

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    initializeAddressAddressUint256StringStringUint64Void: (params: CallParams<CErc20Args['obj']['initialize(address,address,uint256,string,string,uint64)void'] | CErc20Args['tuple']['initialize(address,address,uint256,string,string,uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(CErc20ParamsFactory.initializeAddressAddressUint256StringStringUint64Void(params))
    },

    /**
     * Makes a call to the CErc20 smart contract using the `transfer(address,uint256)bool` ABI method.
     *
    *  @notice Transfer `amount` tokens from `msg.sender` to `dst`
    @param dst The address of the destination account
    @param amount The number of tokens to transfer
    @return Whether or not the transfer succeeded

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    transfer: (params: CallParams<CErc20Args['obj']['transfer(address,uint256)bool'] | CErc20Args['tuple']['transfer(address,uint256)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(CErc20ParamsFactory.transfer(params))
    },

    /**
     * Makes a call to the CErc20 smart contract using the `transferFrom(address,address,uint256)bool` ABI method.
     *
    *  @notice Transfer `amount` tokens from `src` to `dst`
    @param src The address of the source account
    @param dst The address of the destination account
    @param amount The number of tokens to transfer
    @return Whether or not the transfer succeeded

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    transferFrom: (params: CallParams<CErc20Args['obj']['transferFrom(address,address,uint256)bool'] | CErc20Args['tuple']['transferFrom(address,address,uint256)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(CErc20ParamsFactory.transferFrom(params))
    },

    /**
     * Makes a call to the CErc20 smart contract using the `approve(address,uint256)bool` ABI method.
     *
    *  @notice Approve `spender` to transfer up to `amount` from `src`
    @dev This will overwrite the approval amount for `spender`
     and is subject to issues noted [here](https://eips.ethereum.org/EIPS/eip-20#approve)
    @param spender The address of the account which may transfer tokens
    @param amount The number of tokens that are approved (uint256.max means infinite)
    @return Whether or not the approval succeeded

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    approve: (params: CallParams<CErc20Args['obj']['approve(address,uint256)bool'] | CErc20Args['tuple']['approve(address,uint256)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(CErc20ParamsFactory.approve(params))
    },

    /**
     * Makes a call to the CErc20 smart contract using the `allowance(address,address)uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
    *  @notice Get the current allowance from `owner` for `spender`
    @param owner The address of the account which owns the tokens to be spent
    @param spender The address of the account which may transfer tokens
    @return The number of tokens allowed to be spent (-1 means infinite)

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    allowance: (params: CallParams<CErc20Args['obj']['allowance(address,address)uint256'] | CErc20Args['tuple']['allowance(address,address)uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(CErc20ParamsFactory.allowance(params))
    },

    /**
     * Makes a call to the CErc20 smart contract using the `balanceOf(address)uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
    *  @notice Get the token balance of the `owner`
    @param owner The address of the account to query
    @return The number of tokens owned by `owner`

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    balanceOf: (params: CallParams<CErc20Args['obj']['balanceOf(address)uint256'] | CErc20Args['tuple']['balanceOf(address)uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(CErc20ParamsFactory.balanceOf(params))
    },

    /**
     * Makes a call to the CErc20 smart contract using the `balanceOfUnderlying(address)uint256` ABI method.
     *
    *  @notice Get the underlying balance of the `owner`
    @dev This also accrues interest in a transaction
    @param owner The address of the account to query
    @return The amount of underlying owned by `owner`

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    balanceOfUnderlying: (params: CallParams<CErc20Args['obj']['balanceOfUnderlying(address)uint256'] | CErc20Args['tuple']['balanceOfUnderlying(address)uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(CErc20ParamsFactory.balanceOfUnderlying(params))
    },

    /**
     * Makes a call to the CErc20 smart contract using the `getAccountSnapshot(address)(uint256,uint256,uint256,uint256)` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
    *  @notice Get a snapshot of the account's balances, and the cached exchange rate
    @dev This is used by comptroller to more efficiently perform liquidity checks.
    @param account Address of the account to snapshot
    @return (possible error, token balance, borrow balance, exchange rate mantissa)

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    getAccountSnapshot: (params: CallParams<CErc20Args['obj']['getAccountSnapshot(address)(uint256,uint256,uint256,uint256)'] | CErc20Args['tuple']['getAccountSnapshot(address)(uint256,uint256,uint256,uint256)']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(CErc20ParamsFactory.getAccountSnapshot(params))
    },

    /**
     * Makes a call to the CErc20 smart contract using the `borrowRatePerBlock()uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
    *  @notice Returns the current per-block borrow interest rate for this cToken
    @return The borrow interest rate per block, scaled by 1e18

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    borrowRatePerBlock: (params: CallParams<CErc20Args['obj']['borrowRatePerBlock()uint256'] | CErc20Args['tuple']['borrowRatePerBlock()uint256']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(CErc20ParamsFactory.borrowRatePerBlock(params))
    },

    /**
     * Makes a call to the CErc20 smart contract using the `supplyRatePerBlock()uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
    *  @notice Returns the current per-block supply interest rate for this cToken
    @return The supply interest rate per block, scaled by 1e18

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    supplyRatePerBlock: (params: CallParams<CErc20Args['obj']['supplyRatePerBlock()uint256'] | CErc20Args['tuple']['supplyRatePerBlock()uint256']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(CErc20ParamsFactory.supplyRatePerBlock(params))
    },

    /**
     * Makes a call to the CErc20 smart contract using the `totalBorrowsCurrent()uint256` ABI method.
     *
    *  @notice Returns the current total borrows plus accrued interest
    @return The total borrows with interest

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    totalBorrowsCurrent: (params: CallParams<CErc20Args['obj']['totalBorrowsCurrent()uint256'] | CErc20Args['tuple']['totalBorrowsCurrent()uint256']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(CErc20ParamsFactory.totalBorrowsCurrent(params))
    },

    /**
     * Makes a call to the CErc20 smart contract using the `borrowBalanceCurrent(address)uint256` ABI method.
     *
    *  @notice Accrue interest to updated borrowIndex and then calculate account's borrow balance using the updated borrowIndex
    @param account The address whose balance should be calculated after updating borrowIndex
    @return The calculated balance

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    borrowBalanceCurrent: (params: CallParams<CErc20Args['obj']['borrowBalanceCurrent(address)uint256'] | CErc20Args['tuple']['borrowBalanceCurrent(address)uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(CErc20ParamsFactory.borrowBalanceCurrent(params))
    },

    /**
     * Makes a call to the CErc20 smart contract using the `borrowBalanceStored(address)uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
    *  @notice Return the borrow balance of account based on stored data
    @param account The address whose balance should be calculated
    @return The calculated balance

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    borrowBalanceStored: (params: CallParams<CErc20Args['obj']['borrowBalanceStored(address)uint256'] | CErc20Args['tuple']['borrowBalanceStored(address)uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(CErc20ParamsFactory.borrowBalanceStored(params))
    },

    /**
     * Makes a call to the CErc20 smart contract using the `exchangeRateCurrent()uint256` ABI method.
     *
    *  @notice Accrue interest then return the up-to-date exchange rate
    @return Calculated exchange rate scaled by 1e18

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    exchangeRateCurrent: (params: CallParams<CErc20Args['obj']['exchangeRateCurrent()uint256'] | CErc20Args['tuple']['exchangeRateCurrent()uint256']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(CErc20ParamsFactory.exchangeRateCurrent(params))
    },

    /**
     * Makes a call to the CErc20 smart contract using the `exchangeRateStored()uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
    *  @notice Calculates the exchange rate from the underlying to the CToken
    @dev This function does not accrue interest before calculating the exchange rate
    @return Calculated exchange rate scaled by 1e18

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    exchangeRateStored: (params: CallParams<CErc20Args['obj']['exchangeRateStored()uint256'] | CErc20Args['tuple']['exchangeRateStored()uint256']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(CErc20ParamsFactory.exchangeRateStored(params))
    },

    /**
     * Makes a call to the CErc20 smart contract using the `getCash()uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
    *  @notice Get cash balance of this cToken in the underlying asset
    @return The quantity of underlying asset owned by this contract

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    getCash: (params: CallParams<CErc20Args['obj']['getCash()uint256'] | CErc20Args['tuple']['getCash()uint256']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(CErc20ParamsFactory.getCash(params))
    },

    /**
     * Makes a call to the CErc20 smart contract using the `accrueInterest()uint256` ABI method.
     *
    *  @notice Applies accrued interest to total borrows and reserves
    @dev This calculates interest accrued from the last checkpointed block
      up to the current block and writes new checkpoint to storage.

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    accrueInterest: (params: CallParams<CErc20Args['obj']['accrueInterest()uint256'] | CErc20Args['tuple']['accrueInterest()uint256']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(CErc20ParamsFactory.accrueInterest(params))
    },

    /**
     * Makes a call to the CErc20 smart contract using the `seize(address,address,uint256)uint256` ABI method.
     *
    *  @notice Transfers collateral tokens (this market) to the liquidator.
    @dev Will fail unless called by another cToken during the process of liquidation.
     Its absolutely critical to use msg.sender as the borrowed cToken and not a parameter.
    @param liquidator The account receiving seized collateral
    @param borrower The account having collateral seized
    @param seizeTokens The number of cTokens to seize
    @return uint 0=success, otherwise a failure (see ErrorReporter.sol for details)

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    seize: (params: CallParams<CErc20Args['obj']['seize(address,address,uint256)uint256'] | CErc20Args['tuple']['seize(address,address,uint256)uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(CErc20ParamsFactory.seize(params))
    },

    /**
     * Makes a call to the CErc20 smart contract using the `_setPendingAdmin(address)uint256` ABI method.
     *
    *  @notice Begins transfer of admin rights. The newPendingAdmin must call `_acceptAdmin` to finalize the transfer.
    @dev Admin function to begin change of admin. The newPendingAdmin must call `_acceptAdmin` to finalize the transfer.
    @param newPendingAdmin New pending admin.
    @return uint 0=success, otherwise a failure (see ErrorReporter.sol for details)

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    _setPendingAdmin: (params: CallParams<CErc20Args['obj']['_setPendingAdmin(address)uint256'] | CErc20Args['tuple']['_setPendingAdmin(address)uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(CErc20ParamsFactory._setPendingAdmin(params))
    },

    /**
     * Makes a call to the CErc20 smart contract using the `_acceptAdmin()uint256` ABI method.
     *
    *  @notice Accepts transfer of admin rights. msg.sender must be pendingAdmin
    @dev Admin function for pending admin to accept role and update admin
    @return uint 0=success, otherwise a failure (see ErrorReporter.sol for details)

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    _acceptAdmin: (params: CallParams<CErc20Args['obj']['_acceptAdmin()uint256'] | CErc20Args['tuple']['_acceptAdmin()uint256']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(CErc20ParamsFactory._acceptAdmin(params))
    },

    /**
     * Makes a call to the CErc20 smart contract using the `_setComptroller(address)uint256` ABI method.
     *
    *  @notice Sets a new comptroller for the market
    @dev Admin function to set a new comptroller
    @return uint 0=success, otherwise a failure (see ErrorReporter.sol for details)

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    _setComptroller: (params: CallParams<CErc20Args['obj']['_setComptroller(address)uint256'] | CErc20Args['tuple']['_setComptroller(address)uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(CErc20ParamsFactory._setComptroller(params))
    },

    /**
     * Makes a call to the CErc20 smart contract using the `_setReserveFactor(uint256)uint256` ABI method.
     *
    *  @notice accrues interest and sets a new reserve factor for the protocol using _setReserveFactorFresh
    @dev Admin function to accrue interest and set a new reserve factor
    @return uint 0=success, otherwise a failure (see ErrorReporter.sol for details)

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    _setReserveFactor: (params: CallParams<CErc20Args['obj']['_setReserveFactor(uint256)uint256'] | CErc20Args['tuple']['_setReserveFactor(uint256)uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(CErc20ParamsFactory._setReserveFactor(params))
    },

    /**
     * Makes a call to the CErc20 smart contract using the `_reduceReserves(uint256)uint256` ABI method.
     *
    *  @notice Accrues interest and reduces reserves by transferring to admin
    @param reduceAmount Amount of reduction to reserves
    @return uint 0=success, otherwise a failure (see ErrorReporter.sol for details)

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    _reduceReserves: (params: CallParams<CErc20Args['obj']['_reduceReserves(uint256)uint256'] | CErc20Args['tuple']['_reduceReserves(uint256)uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(CErc20ParamsFactory._reduceReserves(params))
    },

    /**
     * Makes a call to the CErc20 smart contract using the `_setInterestRateModel(address)uint256` ABI method.
     *
    *  @notice accrues interest and updates the interest rate model using _setInterestRateModelFresh
    @dev Admin function to accrue interest and update the interest rate model
    @param newInterestRateModel the new interest rate model to use
    @return uint 0=success, otherwise a failure (see ErrorReporter.sol for details)

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    _setInterestRateModel: (params: CallParams<CErc20Args['obj']['_setInterestRateModel(address)uint256'] | CErc20Args['tuple']['_setInterestRateModel(address)uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(CErc20ParamsFactory._setInterestRateModel(params))
    },

    /**
     * Makes a call to the CErc20 smart contract using the `underlying()address` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    underlying: (params: CallParams<CErc20Args['obj']['underlying()address'] | CErc20Args['tuple']['underlying()address']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(CErc20ParamsFactory.underlying(params))
    },

    /**
     * Makes a call to the CErc20 smart contract using the `name()string` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    name: (params: CallParams<CErc20Args['obj']['name()string'] | CErc20Args['tuple']['name()string']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(CErc20ParamsFactory.name(params))
    },

    /**
     * Makes a call to the CErc20 smart contract using the `symbol()string` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    symbol: (params: CallParams<CErc20Args['obj']['symbol()string'] | CErc20Args['tuple']['symbol()string']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(CErc20ParamsFactory.symbol(params))
    },

    /**
     * Makes a call to the CErc20 smart contract using the `decimals()uint64` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    decimals: (params: CallParams<CErc20Args['obj']['decimals()uint64'] | CErc20Args['tuple']['decimals()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(CErc20ParamsFactory.decimals(params))
    },

    /**
     * Makes a call to the CErc20 smart contract using the `admin()address` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    admin: (params: CallParams<CErc20Args['obj']['admin()address'] | CErc20Args['tuple']['admin()address']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(CErc20ParamsFactory.admin(params))
    },

    /**
     * Makes a call to the CErc20 smart contract using the `pendingAdmin()address` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    pendingAdmin: (params: CallParams<CErc20Args['obj']['pendingAdmin()address'] | CErc20Args['tuple']['pendingAdmin()address']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(CErc20ParamsFactory.pendingAdmin(params))
    },

    /**
     * Makes a call to the CErc20 smart contract using the `comptroller()address` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    comptroller: (params: CallParams<CErc20Args['obj']['comptroller()address'] | CErc20Args['tuple']['comptroller()address']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(CErc20ParamsFactory.comptroller(params))
    },

    /**
     * Makes a call to the CErc20 smart contract using the `interestRateModel()address` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    interestRateModel: (params: CallParams<CErc20Args['obj']['interestRateModel()address'] | CErc20Args['tuple']['interestRateModel()address']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(CErc20ParamsFactory.interestRateModel(params))
    },

    /**
     * Makes a call to the CErc20 smart contract using the `reserveFactorMantissa()uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    reserveFactorMantissa: (params: CallParams<CErc20Args['obj']['reserveFactorMantissa()uint256'] | CErc20Args['tuple']['reserveFactorMantissa()uint256']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(CErc20ParamsFactory.reserveFactorMantissa(params))
    },

    /**
     * Makes a call to the CErc20 smart contract using the `accrualBlockNumber()uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    accrualBlockNumber: (params: CallParams<CErc20Args['obj']['accrualBlockNumber()uint256'] | CErc20Args['tuple']['accrualBlockNumber()uint256']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(CErc20ParamsFactory.accrualBlockNumber(params))
    },

    /**
     * Makes a call to the CErc20 smart contract using the `borrowIndex()uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    borrowIndex: (params: CallParams<CErc20Args['obj']['borrowIndex()uint256'] | CErc20Args['tuple']['borrowIndex()uint256']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(CErc20ParamsFactory.borrowIndex(params))
    },

    /**
     * Makes a call to the CErc20 smart contract using the `totalBorrows()uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    totalBorrows: (params: CallParams<CErc20Args['obj']['totalBorrows()uint256'] | CErc20Args['tuple']['totalBorrows()uint256']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(CErc20ParamsFactory.totalBorrows(params))
    },

    /**
     * Makes a call to the CErc20 smart contract using the `totalReserves()uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    totalReserves: (params: CallParams<CErc20Args['obj']['totalReserves()uint256'] | CErc20Args['tuple']['totalReserves()uint256']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(CErc20ParamsFactory.totalReserves(params))
    },

    /**
     * Makes a call to the CErc20 smart contract using the `totalSupply()uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    totalSupply: (params: CallParams<CErc20Args['obj']['totalSupply()uint256'] | CErc20Args['tuple']['totalSupply()uint256']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(CErc20ParamsFactory.totalSupply(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Makes a clear_state call to an existing instance of the CErc20 smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the CErc20 smart contract using the `initialize(address,address,address,uint256,string,string,uint64)void` ABI method.
     *
    *  @notice Initialize the new money market
    @param underlying_ The address of the underlying asset
    @param comptroller_ The address of the Comptroller
    @param interestRateModel_ The address of the interest rate model
    @param initialExchangeRateMantissa_ The initial exchange rate, scaled by 1e18
    @param name_ ERC-20 name of this token
    @param symbol_ ERC-20 symbol of this token
    @param decimals_ ERC-20 decimal precision of this token

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    initializeAddressAddressAddressUint256StringStringUint64Void: (params: CallParams<CErc20Args['obj']['initialize(address,address,address,uint256,string,string,uint64)void'] | CErc20Args['tuple']['initialize(address,address,address,uint256,string,string,uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(CErc20ParamsFactory.initializeAddressAddressAddressUint256StringStringUint64Void(params))
    },

    /**
     * Makes a call to the CErc20 smart contract using the `mint(uint256)uint256` ABI method.
     *
    *  @notice Sender supplies assets into the market and receives cTokens in exchange
    @dev Accrues interest whether or not the operation succeeds, unless reverted
    @param mintAmount The amount of the underlying asset to supply
    @return uint 0=success, otherwise a failure (see ErrorReporter.sol for details)

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    mint: (params: CallParams<CErc20Args['obj']['mint(uint256)uint256'] | CErc20Args['tuple']['mint(uint256)uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(CErc20ParamsFactory.mint(params))
    },

    /**
     * Makes a call to the CErc20 smart contract using the `redeem(uint256)uint256` ABI method.
     *
    *  @notice Sender redeems cTokens in exchange for the underlying asset
    @dev Accrues interest whether or not the operation succeeds, unless reverted
    @param redeemTokens The number of cTokens to redeem into underlying
    @return uint 0=success, otherwise a failure (see ErrorReporter.sol for details)

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    redeem: (params: CallParams<CErc20Args['obj']['redeem(uint256)uint256'] | CErc20Args['tuple']['redeem(uint256)uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(CErc20ParamsFactory.redeem(params))
    },

    /**
     * Makes a call to the CErc20 smart contract using the `redeemUnderlying(uint256)uint256` ABI method.
     *
    *  @notice Sender redeems cTokens in exchange for a specified amount of underlying asset
    @dev Accrues interest whether or not the operation succeeds, unless reverted
    @param redeemAmount The amount of underlying to redeem
    @return uint 0=success, otherwise a failure (see ErrorReporter.sol for details)

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    redeemUnderlying: (params: CallParams<CErc20Args['obj']['redeemUnderlying(uint256)uint256'] | CErc20Args['tuple']['redeemUnderlying(uint256)uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(CErc20ParamsFactory.redeemUnderlying(params))
    },

    /**
     * Makes a call to the CErc20 smart contract using the `borrow(uint256)uint256` ABI method.
     *
    *  @notice Sender borrows assets from the protocol to their own address
    @param borrowAmount The amount of the underlying asset to borrow
    @return uint 0=success, otherwise a failure (see ErrorReporter.sol for details)

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    borrow: (params: CallParams<CErc20Args['obj']['borrow(uint256)uint256'] | CErc20Args['tuple']['borrow(uint256)uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(CErc20ParamsFactory.borrow(params))
    },

    /**
     * Makes a call to the CErc20 smart contract using the `repayBorrow(uint256)uint256` ABI method.
     *
    *  @notice Sender repays their own borrow
    @param repayAmount The amount to repay, or -1 for the full outstanding amount
    @return uint 0=success, otherwise a failure (see ErrorReporter.sol for details)

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    repayBorrow: (params: CallParams<CErc20Args['obj']['repayBorrow(uint256)uint256'] | CErc20Args['tuple']['repayBorrow(uint256)uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(CErc20ParamsFactory.repayBorrow(params))
    },

    /**
     * Makes a call to the CErc20 smart contract using the `repayBorrowBehalf(address,uint256)uint256` ABI method.
     *
    *  @notice Sender repays a borrow belonging to borrower
    @param borrower the account with the debt being payed off
    @param repayAmount The amount to repay, or -1 for the full outstanding amount
    @return uint 0=success, otherwise a failure (see ErrorReporter.sol for details)

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    repayBorrowBehalf: (params: CallParams<CErc20Args['obj']['repayBorrowBehalf(address,uint256)uint256'] | CErc20Args['tuple']['repayBorrowBehalf(address,uint256)uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(CErc20ParamsFactory.repayBorrowBehalf(params))
    },

    /**
     * Makes a call to the CErc20 smart contract using the `liquidateBorrow(address,uint256,address)uint256` ABI method.
     *
    *  @notice The sender liquidates the borrowers collateral.
     The collateral seized is transferred to the liquidator.
    @param borrower The borrower of this cToken to be liquidated
    @param repayAmount The amount of the underlying borrowed asset to repay
    @param cTokenCollateral The market in which to seize collateral from the borrower
    @return uint 0=success, otherwise a failure (see ErrorReporter.sol for details)

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    liquidateBorrow: (params: CallParams<CErc20Args['obj']['liquidateBorrow(address,uint256,address)uint256'] | CErc20Args['tuple']['liquidateBorrow(address,uint256,address)uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(CErc20ParamsFactory.liquidateBorrow(params))
    },

    /**
     * Makes a call to the CErc20 smart contract using the `sweepToken(address)void` ABI method.
     *
    *  @notice A public function to sweep accidental ERC-20 transfers to this contract. Tokens are sent to admin (timelock)
    @param token The address of the ERC-20 token to sweep

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    sweepToken: (params: CallParams<CErc20Args['obj']['sweepToken(address)void'] | CErc20Args['tuple']['sweepToken(address)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(CErc20ParamsFactory.sweepToken(params))
    },

    /**
     * Makes a call to the CErc20 smart contract using the `_addReserves(uint256)uint256` ABI method.
     *
    *  @notice The sender adds to reserves.
    @param addAmount The amount fo underlying token to add as reserves
    @return uint 0=success, otherwise a failure (see ErrorReporter.sol for details)

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    _addReserves: (params: CallParams<CErc20Args['obj']['_addReserves(uint256)uint256'] | CErc20Args['tuple']['_addReserves(uint256)uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(CErc20ParamsFactory._addReserves(params))
    },

    /**
     * Makes a call to the CErc20 smart contract using the `_delegateCompLikeTo(address)void` ABI method.
     *
    *  @notice Admin call to delegate the votes of the COMP-like underlying
    @param compLikeDelegatee The address to delegate votes to
    @dev CTokens whose underlying are not CompLike should revert here

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    _delegateCompLikeTo: (params: CallParams<CErc20Args['obj']['_delegateCompLikeTo(address)void'] | CErc20Args['tuple']['_delegateCompLikeTo(address)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(CErc20ParamsFactory._delegateCompLikeTo(params))
    },

    /**
     * Makes a call to the CErc20 smart contract using the `initialize(address,address,uint256,string,string,uint64)void` ABI method.
     *
    *  @notice Initialize the money market
    @param comptroller_ The address of the Comptroller
    @param interestRateModel_ The address of the interest rate model
    @param initialExchangeRateMantissa_ The initial exchange rate, scaled by 1e18
    @param name_ EIP-20 name of this token
    @param symbol_ EIP-20 symbol of this token
    @param decimals_ EIP-20 decimal precision of this token

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    initializeAddressAddressUint256StringStringUint64Void: (params: CallParams<CErc20Args['obj']['initialize(address,address,uint256,string,string,uint64)void'] | CErc20Args['tuple']['initialize(address,address,uint256,string,string,uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(CErc20ParamsFactory.initializeAddressAddressUint256StringStringUint64Void(params))
    },

    /**
     * Makes a call to the CErc20 smart contract using the `transfer(address,uint256)bool` ABI method.
     *
    *  @notice Transfer `amount` tokens from `msg.sender` to `dst`
    @param dst The address of the destination account
    @param amount The number of tokens to transfer
    @return Whether or not the transfer succeeded

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    transfer: (params: CallParams<CErc20Args['obj']['transfer(address,uint256)bool'] | CErc20Args['tuple']['transfer(address,uint256)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(CErc20ParamsFactory.transfer(params))
    },

    /**
     * Makes a call to the CErc20 smart contract using the `transferFrom(address,address,uint256)bool` ABI method.
     *
    *  @notice Transfer `amount` tokens from `src` to `dst`
    @param src The address of the source account
    @param dst The address of the destination account
    @param amount The number of tokens to transfer
    @return Whether or not the transfer succeeded

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    transferFrom: (params: CallParams<CErc20Args['obj']['transferFrom(address,address,uint256)bool'] | CErc20Args['tuple']['transferFrom(address,address,uint256)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(CErc20ParamsFactory.transferFrom(params))
    },

    /**
     * Makes a call to the CErc20 smart contract using the `approve(address,uint256)bool` ABI method.
     *
    *  @notice Approve `spender` to transfer up to `amount` from `src`
    @dev This will overwrite the approval amount for `spender`
     and is subject to issues noted [here](https://eips.ethereum.org/EIPS/eip-20#approve)
    @param spender The address of the account which may transfer tokens
    @param amount The number of tokens that are approved (uint256.max means infinite)
    @return Whether or not the approval succeeded

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    approve: (params: CallParams<CErc20Args['obj']['approve(address,uint256)bool'] | CErc20Args['tuple']['approve(address,uint256)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(CErc20ParamsFactory.approve(params))
    },

    /**
     * Makes a call to the CErc20 smart contract using the `allowance(address,address)uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
    *  @notice Get the current allowance from `owner` for `spender`
    @param owner The address of the account which owns the tokens to be spent
    @param spender The address of the account which may transfer tokens
    @return The number of tokens allowed to be spent (-1 means infinite)

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    allowance: (params: CallParams<CErc20Args['obj']['allowance(address,address)uint256'] | CErc20Args['tuple']['allowance(address,address)uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(CErc20ParamsFactory.allowance(params))
    },

    /**
     * Makes a call to the CErc20 smart contract using the `balanceOf(address)uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
    *  @notice Get the token balance of the `owner`
    @param owner The address of the account to query
    @return The number of tokens owned by `owner`

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    balanceOf: (params: CallParams<CErc20Args['obj']['balanceOf(address)uint256'] | CErc20Args['tuple']['balanceOf(address)uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(CErc20ParamsFactory.balanceOf(params))
    },

    /**
     * Makes a call to the CErc20 smart contract using the `balanceOfUnderlying(address)uint256` ABI method.
     *
    *  @notice Get the underlying balance of the `owner`
    @dev This also accrues interest in a transaction
    @param owner The address of the account to query
    @return The amount of underlying owned by `owner`

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    balanceOfUnderlying: (params: CallParams<CErc20Args['obj']['balanceOfUnderlying(address)uint256'] | CErc20Args['tuple']['balanceOfUnderlying(address)uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(CErc20ParamsFactory.balanceOfUnderlying(params))
    },

    /**
     * Makes a call to the CErc20 smart contract using the `getAccountSnapshot(address)(uint256,uint256,uint256,uint256)` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
    *  @notice Get a snapshot of the account's balances, and the cached exchange rate
    @dev This is used by comptroller to more efficiently perform liquidity checks.
    @param account Address of the account to snapshot
    @return (possible error, token balance, borrow balance, exchange rate mantissa)

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    getAccountSnapshot: (params: CallParams<CErc20Args['obj']['getAccountSnapshot(address)(uint256,uint256,uint256,uint256)'] | CErc20Args['tuple']['getAccountSnapshot(address)(uint256,uint256,uint256,uint256)']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(CErc20ParamsFactory.getAccountSnapshot(params))
    },

    /**
     * Makes a call to the CErc20 smart contract using the `borrowRatePerBlock()uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
    *  @notice Returns the current per-block borrow interest rate for this cToken
    @return The borrow interest rate per block, scaled by 1e18

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    borrowRatePerBlock: (params: CallParams<CErc20Args['obj']['borrowRatePerBlock()uint256'] | CErc20Args['tuple']['borrowRatePerBlock()uint256']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(CErc20ParamsFactory.borrowRatePerBlock(params))
    },

    /**
     * Makes a call to the CErc20 smart contract using the `supplyRatePerBlock()uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
    *  @notice Returns the current per-block supply interest rate for this cToken
    @return The supply interest rate per block, scaled by 1e18

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    supplyRatePerBlock: (params: CallParams<CErc20Args['obj']['supplyRatePerBlock()uint256'] | CErc20Args['tuple']['supplyRatePerBlock()uint256']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(CErc20ParamsFactory.supplyRatePerBlock(params))
    },

    /**
     * Makes a call to the CErc20 smart contract using the `totalBorrowsCurrent()uint256` ABI method.
     *
    *  @notice Returns the current total borrows plus accrued interest
    @return The total borrows with interest

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    totalBorrowsCurrent: (params: CallParams<CErc20Args['obj']['totalBorrowsCurrent()uint256'] | CErc20Args['tuple']['totalBorrowsCurrent()uint256']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(CErc20ParamsFactory.totalBorrowsCurrent(params))
    },

    /**
     * Makes a call to the CErc20 smart contract using the `borrowBalanceCurrent(address)uint256` ABI method.
     *
    *  @notice Accrue interest to updated borrowIndex and then calculate account's borrow balance using the updated borrowIndex
    @param account The address whose balance should be calculated after updating borrowIndex
    @return The calculated balance

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    borrowBalanceCurrent: (params: CallParams<CErc20Args['obj']['borrowBalanceCurrent(address)uint256'] | CErc20Args['tuple']['borrowBalanceCurrent(address)uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(CErc20ParamsFactory.borrowBalanceCurrent(params))
    },

    /**
     * Makes a call to the CErc20 smart contract using the `borrowBalanceStored(address)uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
    *  @notice Return the borrow balance of account based on stored data
    @param account The address whose balance should be calculated
    @return The calculated balance

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    borrowBalanceStored: (params: CallParams<CErc20Args['obj']['borrowBalanceStored(address)uint256'] | CErc20Args['tuple']['borrowBalanceStored(address)uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(CErc20ParamsFactory.borrowBalanceStored(params))
    },

    /**
     * Makes a call to the CErc20 smart contract using the `exchangeRateCurrent()uint256` ABI method.
     *
    *  @notice Accrue interest then return the up-to-date exchange rate
    @return Calculated exchange rate scaled by 1e18

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    exchangeRateCurrent: (params: CallParams<CErc20Args['obj']['exchangeRateCurrent()uint256'] | CErc20Args['tuple']['exchangeRateCurrent()uint256']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(CErc20ParamsFactory.exchangeRateCurrent(params))
    },

    /**
     * Makes a call to the CErc20 smart contract using the `exchangeRateStored()uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
    *  @notice Calculates the exchange rate from the underlying to the CToken
    @dev This function does not accrue interest before calculating the exchange rate
    @return Calculated exchange rate scaled by 1e18

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    exchangeRateStored: (params: CallParams<CErc20Args['obj']['exchangeRateStored()uint256'] | CErc20Args['tuple']['exchangeRateStored()uint256']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(CErc20ParamsFactory.exchangeRateStored(params))
    },

    /**
     * Makes a call to the CErc20 smart contract using the `getCash()uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
    *  @notice Get cash balance of this cToken in the underlying asset
    @return The quantity of underlying asset owned by this contract

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    getCash: (params: CallParams<CErc20Args['obj']['getCash()uint256'] | CErc20Args['tuple']['getCash()uint256']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(CErc20ParamsFactory.getCash(params))
    },

    /**
     * Makes a call to the CErc20 smart contract using the `accrueInterest()uint256` ABI method.
     *
    *  @notice Applies accrued interest to total borrows and reserves
    @dev This calculates interest accrued from the last checkpointed block
      up to the current block and writes new checkpoint to storage.

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    accrueInterest: (params: CallParams<CErc20Args['obj']['accrueInterest()uint256'] | CErc20Args['tuple']['accrueInterest()uint256']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(CErc20ParamsFactory.accrueInterest(params))
    },

    /**
     * Makes a call to the CErc20 smart contract using the `seize(address,address,uint256)uint256` ABI method.
     *
    *  @notice Transfers collateral tokens (this market) to the liquidator.
    @dev Will fail unless called by another cToken during the process of liquidation.
     Its absolutely critical to use msg.sender as the borrowed cToken and not a parameter.
    @param liquidator The account receiving seized collateral
    @param borrower The account having collateral seized
    @param seizeTokens The number of cTokens to seize
    @return uint 0=success, otherwise a failure (see ErrorReporter.sol for details)

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    seize: (params: CallParams<CErc20Args['obj']['seize(address,address,uint256)uint256'] | CErc20Args['tuple']['seize(address,address,uint256)uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(CErc20ParamsFactory.seize(params))
    },

    /**
     * Makes a call to the CErc20 smart contract using the `_setPendingAdmin(address)uint256` ABI method.
     *
    *  @notice Begins transfer of admin rights. The newPendingAdmin must call `_acceptAdmin` to finalize the transfer.
    @dev Admin function to begin change of admin. The newPendingAdmin must call `_acceptAdmin` to finalize the transfer.
    @param newPendingAdmin New pending admin.
    @return uint 0=success, otherwise a failure (see ErrorReporter.sol for details)

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    _setPendingAdmin: (params: CallParams<CErc20Args['obj']['_setPendingAdmin(address)uint256'] | CErc20Args['tuple']['_setPendingAdmin(address)uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(CErc20ParamsFactory._setPendingAdmin(params))
    },

    /**
     * Makes a call to the CErc20 smart contract using the `_acceptAdmin()uint256` ABI method.
     *
    *  @notice Accepts transfer of admin rights. msg.sender must be pendingAdmin
    @dev Admin function for pending admin to accept role and update admin
    @return uint 0=success, otherwise a failure (see ErrorReporter.sol for details)

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    _acceptAdmin: (params: CallParams<CErc20Args['obj']['_acceptAdmin()uint256'] | CErc20Args['tuple']['_acceptAdmin()uint256']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(CErc20ParamsFactory._acceptAdmin(params))
    },

    /**
     * Makes a call to the CErc20 smart contract using the `_setComptroller(address)uint256` ABI method.
     *
    *  @notice Sets a new comptroller for the market
    @dev Admin function to set a new comptroller
    @return uint 0=success, otherwise a failure (see ErrorReporter.sol for details)

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    _setComptroller: (params: CallParams<CErc20Args['obj']['_setComptroller(address)uint256'] | CErc20Args['tuple']['_setComptroller(address)uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(CErc20ParamsFactory._setComptroller(params))
    },

    /**
     * Makes a call to the CErc20 smart contract using the `_setReserveFactor(uint256)uint256` ABI method.
     *
    *  @notice accrues interest and sets a new reserve factor for the protocol using _setReserveFactorFresh
    @dev Admin function to accrue interest and set a new reserve factor
    @return uint 0=success, otherwise a failure (see ErrorReporter.sol for details)

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    _setReserveFactor: (params: CallParams<CErc20Args['obj']['_setReserveFactor(uint256)uint256'] | CErc20Args['tuple']['_setReserveFactor(uint256)uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(CErc20ParamsFactory._setReserveFactor(params))
    },

    /**
     * Makes a call to the CErc20 smart contract using the `_reduceReserves(uint256)uint256` ABI method.
     *
    *  @notice Accrues interest and reduces reserves by transferring to admin
    @param reduceAmount Amount of reduction to reserves
    @return uint 0=success, otherwise a failure (see ErrorReporter.sol for details)

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    _reduceReserves: (params: CallParams<CErc20Args['obj']['_reduceReserves(uint256)uint256'] | CErc20Args['tuple']['_reduceReserves(uint256)uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(CErc20ParamsFactory._reduceReserves(params))
    },

    /**
     * Makes a call to the CErc20 smart contract using the `_setInterestRateModel(address)uint256` ABI method.
     *
    *  @notice accrues interest and updates the interest rate model using _setInterestRateModelFresh
    @dev Admin function to accrue interest and update the interest rate model
    @param newInterestRateModel the new interest rate model to use
    @return uint 0=success, otherwise a failure (see ErrorReporter.sol for details)

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    _setInterestRateModel: (params: CallParams<CErc20Args['obj']['_setInterestRateModel(address)uint256'] | CErc20Args['tuple']['_setInterestRateModel(address)uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(CErc20ParamsFactory._setInterestRateModel(params))
    },

    /**
     * Makes a call to the CErc20 smart contract using the `underlying()address` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    underlying: (params: CallParams<CErc20Args['obj']['underlying()address'] | CErc20Args['tuple']['underlying()address']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(CErc20ParamsFactory.underlying(params))
    },

    /**
     * Makes a call to the CErc20 smart contract using the `name()string` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    name: (params: CallParams<CErc20Args['obj']['name()string'] | CErc20Args['tuple']['name()string']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(CErc20ParamsFactory.name(params))
    },

    /**
     * Makes a call to the CErc20 smart contract using the `symbol()string` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    symbol: (params: CallParams<CErc20Args['obj']['symbol()string'] | CErc20Args['tuple']['symbol()string']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(CErc20ParamsFactory.symbol(params))
    },

    /**
     * Makes a call to the CErc20 smart contract using the `decimals()uint64` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    decimals: (params: CallParams<CErc20Args['obj']['decimals()uint64'] | CErc20Args['tuple']['decimals()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(CErc20ParamsFactory.decimals(params))
    },

    /**
     * Makes a call to the CErc20 smart contract using the `admin()address` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    admin: (params: CallParams<CErc20Args['obj']['admin()address'] | CErc20Args['tuple']['admin()address']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(CErc20ParamsFactory.admin(params))
    },

    /**
     * Makes a call to the CErc20 smart contract using the `pendingAdmin()address` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    pendingAdmin: (params: CallParams<CErc20Args['obj']['pendingAdmin()address'] | CErc20Args['tuple']['pendingAdmin()address']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(CErc20ParamsFactory.pendingAdmin(params))
    },

    /**
     * Makes a call to the CErc20 smart contract using the `comptroller()address` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    comptroller: (params: CallParams<CErc20Args['obj']['comptroller()address'] | CErc20Args['tuple']['comptroller()address']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(CErc20ParamsFactory.comptroller(params))
    },

    /**
     * Makes a call to the CErc20 smart contract using the `interestRateModel()address` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    interestRateModel: (params: CallParams<CErc20Args['obj']['interestRateModel()address'] | CErc20Args['tuple']['interestRateModel()address']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(CErc20ParamsFactory.interestRateModel(params))
    },

    /**
     * Makes a call to the CErc20 smart contract using the `reserveFactorMantissa()uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    reserveFactorMantissa: (params: CallParams<CErc20Args['obj']['reserveFactorMantissa()uint256'] | CErc20Args['tuple']['reserveFactorMantissa()uint256']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(CErc20ParamsFactory.reserveFactorMantissa(params))
    },

    /**
     * Makes a call to the CErc20 smart contract using the `accrualBlockNumber()uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    accrualBlockNumber: (params: CallParams<CErc20Args['obj']['accrualBlockNumber()uint256'] | CErc20Args['tuple']['accrualBlockNumber()uint256']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(CErc20ParamsFactory.accrualBlockNumber(params))
    },

    /**
     * Makes a call to the CErc20 smart contract using the `borrowIndex()uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    borrowIndex: (params: CallParams<CErc20Args['obj']['borrowIndex()uint256'] | CErc20Args['tuple']['borrowIndex()uint256']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(CErc20ParamsFactory.borrowIndex(params))
    },

    /**
     * Makes a call to the CErc20 smart contract using the `totalBorrows()uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    totalBorrows: (params: CallParams<CErc20Args['obj']['totalBorrows()uint256'] | CErc20Args['tuple']['totalBorrows()uint256']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(CErc20ParamsFactory.totalBorrows(params))
    },

    /**
     * Makes a call to the CErc20 smart contract using the `totalReserves()uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    totalReserves: (params: CallParams<CErc20Args['obj']['totalReserves()uint256'] | CErc20Args['tuple']['totalReserves()uint256']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(CErc20ParamsFactory.totalReserves(params))
    },

    /**
     * Makes a call to the CErc20 smart contract using the `totalSupply()uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    totalSupply: (params: CallParams<CErc20Args['obj']['totalSupply()uint256'] | CErc20Args['tuple']['totalSupply()uint256']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(CErc20ParamsFactory.totalSupply(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Makes a clear_state call to an existing instance of the CErc20 smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the CErc20 smart contract using the `initialize(address,address,address,uint256,string,string,uint64)void` ABI method.
     *
    *  @notice Initialize the new money market
    @param underlying_ The address of the underlying asset
    @param comptroller_ The address of the Comptroller
    @param interestRateModel_ The address of the interest rate model
    @param initialExchangeRateMantissa_ The initial exchange rate, scaled by 1e18
    @param name_ ERC-20 name of this token
    @param symbol_ ERC-20 symbol of this token
    @param decimals_ ERC-20 decimal precision of this token

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    initializeAddressAddressAddressUint256StringStringUint64Void: async (params: CallParams<CErc20Args['obj']['initialize(address,address,address,uint256,string,string,uint64)void'] | CErc20Args['tuple']['initialize(address,address,address,uint256,string,string,uint64)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(CErc20ParamsFactory.initializeAddressAddressAddressUint256StringStringUint64Void(params))
      return {...result, return: result.return as unknown as (undefined | CErc20Returns['initialize(address,address,address,uint256,string,string,uint64)void'])}
    },

    /**
     * Makes a call to the CErc20 smart contract using the `mint(uint256)uint256` ABI method.
     *
    *  @notice Sender supplies assets into the market and receives cTokens in exchange
    @dev Accrues interest whether or not the operation succeeds, unless reverted
    @param mintAmount The amount of the underlying asset to supply
    @return uint 0=success, otherwise a failure (see ErrorReporter.sol for details)

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    mint: async (params: CallParams<CErc20Args['obj']['mint(uint256)uint256'] | CErc20Args['tuple']['mint(uint256)uint256']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(CErc20ParamsFactory.mint(params))
      return {...result, return: result.return as unknown as (undefined | CErc20Returns['mint(uint256)uint256'])}
    },

    /**
     * Makes a call to the CErc20 smart contract using the `redeem(uint256)uint256` ABI method.
     *
    *  @notice Sender redeems cTokens in exchange for the underlying asset
    @dev Accrues interest whether or not the operation succeeds, unless reverted
    @param redeemTokens The number of cTokens to redeem into underlying
    @return uint 0=success, otherwise a failure (see ErrorReporter.sol for details)

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    redeem: async (params: CallParams<CErc20Args['obj']['redeem(uint256)uint256'] | CErc20Args['tuple']['redeem(uint256)uint256']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(CErc20ParamsFactory.redeem(params))
      return {...result, return: result.return as unknown as (undefined | CErc20Returns['redeem(uint256)uint256'])}
    },

    /**
     * Makes a call to the CErc20 smart contract using the `redeemUnderlying(uint256)uint256` ABI method.
     *
    *  @notice Sender redeems cTokens in exchange for a specified amount of underlying asset
    @dev Accrues interest whether or not the operation succeeds, unless reverted
    @param redeemAmount The amount of underlying to redeem
    @return uint 0=success, otherwise a failure (see ErrorReporter.sol for details)

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    redeemUnderlying: async (params: CallParams<CErc20Args['obj']['redeemUnderlying(uint256)uint256'] | CErc20Args['tuple']['redeemUnderlying(uint256)uint256']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(CErc20ParamsFactory.redeemUnderlying(params))
      return {...result, return: result.return as unknown as (undefined | CErc20Returns['redeemUnderlying(uint256)uint256'])}
    },

    /**
     * Makes a call to the CErc20 smart contract using the `borrow(uint256)uint256` ABI method.
     *
    *  @notice Sender borrows assets from the protocol to their own address
    @param borrowAmount The amount of the underlying asset to borrow
    @return uint 0=success, otherwise a failure (see ErrorReporter.sol for details)

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    borrow: async (params: CallParams<CErc20Args['obj']['borrow(uint256)uint256'] | CErc20Args['tuple']['borrow(uint256)uint256']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(CErc20ParamsFactory.borrow(params))
      return {...result, return: result.return as unknown as (undefined | CErc20Returns['borrow(uint256)uint256'])}
    },

    /**
     * Makes a call to the CErc20 smart contract using the `repayBorrow(uint256)uint256` ABI method.
     *
    *  @notice Sender repays their own borrow
    @param repayAmount The amount to repay, or -1 for the full outstanding amount
    @return uint 0=success, otherwise a failure (see ErrorReporter.sol for details)

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    repayBorrow: async (params: CallParams<CErc20Args['obj']['repayBorrow(uint256)uint256'] | CErc20Args['tuple']['repayBorrow(uint256)uint256']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(CErc20ParamsFactory.repayBorrow(params))
      return {...result, return: result.return as unknown as (undefined | CErc20Returns['repayBorrow(uint256)uint256'])}
    },

    /**
     * Makes a call to the CErc20 smart contract using the `repayBorrowBehalf(address,uint256)uint256` ABI method.
     *
    *  @notice Sender repays a borrow belonging to borrower
    @param borrower the account with the debt being payed off
    @param repayAmount The amount to repay, or -1 for the full outstanding amount
    @return uint 0=success, otherwise a failure (see ErrorReporter.sol for details)

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    repayBorrowBehalf: async (params: CallParams<CErc20Args['obj']['repayBorrowBehalf(address,uint256)uint256'] | CErc20Args['tuple']['repayBorrowBehalf(address,uint256)uint256']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(CErc20ParamsFactory.repayBorrowBehalf(params))
      return {...result, return: result.return as unknown as (undefined | CErc20Returns['repayBorrowBehalf(address,uint256)uint256'])}
    },

    /**
     * Makes a call to the CErc20 smart contract using the `liquidateBorrow(address,uint256,address)uint256` ABI method.
     *
    *  @notice The sender liquidates the borrowers collateral.
     The collateral seized is transferred to the liquidator.
    @param borrower The borrower of this cToken to be liquidated
    @param repayAmount The amount of the underlying borrowed asset to repay
    @param cTokenCollateral The market in which to seize collateral from the borrower
    @return uint 0=success, otherwise a failure (see ErrorReporter.sol for details)

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    liquidateBorrow: async (params: CallParams<CErc20Args['obj']['liquidateBorrow(address,uint256,address)uint256'] | CErc20Args['tuple']['liquidateBorrow(address,uint256,address)uint256']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(CErc20ParamsFactory.liquidateBorrow(params))
      return {...result, return: result.return as unknown as (undefined | CErc20Returns['liquidateBorrow(address,uint256,address)uint256'])}
    },

    /**
     * Makes a call to the CErc20 smart contract using the `sweepToken(address)void` ABI method.
     *
    *  @notice A public function to sweep accidental ERC-20 transfers to this contract. Tokens are sent to admin (timelock)
    @param token The address of the ERC-20 token to sweep

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    sweepToken: async (params: CallParams<CErc20Args['obj']['sweepToken(address)void'] | CErc20Args['tuple']['sweepToken(address)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(CErc20ParamsFactory.sweepToken(params))
      return {...result, return: result.return as unknown as (undefined | CErc20Returns['sweepToken(address)void'])}
    },

    /**
     * Makes a call to the CErc20 smart contract using the `_addReserves(uint256)uint256` ABI method.
     *
    *  @notice The sender adds to reserves.
    @param addAmount The amount fo underlying token to add as reserves
    @return uint 0=success, otherwise a failure (see ErrorReporter.sol for details)

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    _addReserves: async (params: CallParams<CErc20Args['obj']['_addReserves(uint256)uint256'] | CErc20Args['tuple']['_addReserves(uint256)uint256']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(CErc20ParamsFactory._addReserves(params))
      return {...result, return: result.return as unknown as (undefined | CErc20Returns['_addReserves(uint256)uint256'])}
    },

    /**
     * Makes a call to the CErc20 smart contract using the `_delegateCompLikeTo(address)void` ABI method.
     *
    *  @notice Admin call to delegate the votes of the COMP-like underlying
    @param compLikeDelegatee The address to delegate votes to
    @dev CTokens whose underlying are not CompLike should revert here

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    _delegateCompLikeTo: async (params: CallParams<CErc20Args['obj']['_delegateCompLikeTo(address)void'] | CErc20Args['tuple']['_delegateCompLikeTo(address)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(CErc20ParamsFactory._delegateCompLikeTo(params))
      return {...result, return: result.return as unknown as (undefined | CErc20Returns['_delegateCompLikeTo(address)void'])}
    },

    /**
     * Makes a call to the CErc20 smart contract using the `initialize(address,address,uint256,string,string,uint64)void` ABI method.
     *
    *  @notice Initialize the money market
    @param comptroller_ The address of the Comptroller
    @param interestRateModel_ The address of the interest rate model
    @param initialExchangeRateMantissa_ The initial exchange rate, scaled by 1e18
    @param name_ EIP-20 name of this token
    @param symbol_ EIP-20 symbol of this token
    @param decimals_ EIP-20 decimal precision of this token

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    initializeAddressAddressUint256StringStringUint64Void: async (params: CallParams<CErc20Args['obj']['initialize(address,address,uint256,string,string,uint64)void'] | CErc20Args['tuple']['initialize(address,address,uint256,string,string,uint64)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(CErc20ParamsFactory.initializeAddressAddressUint256StringStringUint64Void(params))
      return {...result, return: result.return as unknown as (undefined | CErc20Returns['initialize(address,address,uint256,string,string,uint64)void'])}
    },

    /**
     * Makes a call to the CErc20 smart contract using the `transfer(address,uint256)bool` ABI method.
     *
    *  @notice Transfer `amount` tokens from `msg.sender` to `dst`
    @param dst The address of the destination account
    @param amount The number of tokens to transfer
    @return Whether or not the transfer succeeded

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    transfer: async (params: CallParams<CErc20Args['obj']['transfer(address,uint256)bool'] | CErc20Args['tuple']['transfer(address,uint256)bool']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(CErc20ParamsFactory.transfer(params))
      return {...result, return: result.return as unknown as (undefined | CErc20Returns['transfer(address,uint256)bool'])}
    },

    /**
     * Makes a call to the CErc20 smart contract using the `transferFrom(address,address,uint256)bool` ABI method.
     *
    *  @notice Transfer `amount` tokens from `src` to `dst`
    @param src The address of the source account
    @param dst The address of the destination account
    @param amount The number of tokens to transfer
    @return Whether or not the transfer succeeded

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    transferFrom: async (params: CallParams<CErc20Args['obj']['transferFrom(address,address,uint256)bool'] | CErc20Args['tuple']['transferFrom(address,address,uint256)bool']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(CErc20ParamsFactory.transferFrom(params))
      return {...result, return: result.return as unknown as (undefined | CErc20Returns['transferFrom(address,address,uint256)bool'])}
    },

    /**
     * Makes a call to the CErc20 smart contract using the `approve(address,uint256)bool` ABI method.
     *
    *  @notice Approve `spender` to transfer up to `amount` from `src`
    @dev This will overwrite the approval amount for `spender`
     and is subject to issues noted [here](https://eips.ethereum.org/EIPS/eip-20#approve)
    @param spender The address of the account which may transfer tokens
    @param amount The number of tokens that are approved (uint256.max means infinite)
    @return Whether or not the approval succeeded

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    approve: async (params: CallParams<CErc20Args['obj']['approve(address,uint256)bool'] | CErc20Args['tuple']['approve(address,uint256)bool']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(CErc20ParamsFactory.approve(params))
      return {...result, return: result.return as unknown as (undefined | CErc20Returns['approve(address,uint256)bool'])}
    },

    /**
     * Makes a call to the CErc20 smart contract using the `allowance(address,address)uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
    *  @notice Get the current allowance from `owner` for `spender`
    @param owner The address of the account which owns the tokens to be spent
    @param spender The address of the account which may transfer tokens
    @return The number of tokens allowed to be spent (-1 means infinite)

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    allowance: async (params: CallParams<CErc20Args['obj']['allowance(address,address)uint256'] | CErc20Args['tuple']['allowance(address,address)uint256']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(CErc20ParamsFactory.allowance(params))
      return {...result, return: result.return as unknown as (undefined | CErc20Returns['allowance(address,address)uint256'])}
    },

    /**
     * Makes a call to the CErc20 smart contract using the `balanceOf(address)uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
    *  @notice Get the token balance of the `owner`
    @param owner The address of the account to query
    @return The number of tokens owned by `owner`

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    balanceOf: async (params: CallParams<CErc20Args['obj']['balanceOf(address)uint256'] | CErc20Args['tuple']['balanceOf(address)uint256']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(CErc20ParamsFactory.balanceOf(params))
      return {...result, return: result.return as unknown as (undefined | CErc20Returns['balanceOf(address)uint256'])}
    },

    /**
     * Makes a call to the CErc20 smart contract using the `balanceOfUnderlying(address)uint256` ABI method.
     *
    *  @notice Get the underlying balance of the `owner`
    @dev This also accrues interest in a transaction
    @param owner The address of the account to query
    @return The amount of underlying owned by `owner`

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    balanceOfUnderlying: async (params: CallParams<CErc20Args['obj']['balanceOfUnderlying(address)uint256'] | CErc20Args['tuple']['balanceOfUnderlying(address)uint256']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(CErc20ParamsFactory.balanceOfUnderlying(params))
      return {...result, return: result.return as unknown as (undefined | CErc20Returns['balanceOfUnderlying(address)uint256'])}
    },

    /**
     * Makes a call to the CErc20 smart contract using the `getAccountSnapshot(address)(uint256,uint256,uint256,uint256)` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
    *  @notice Get a snapshot of the account's balances, and the cached exchange rate
    @dev This is used by comptroller to more efficiently perform liquidity checks.
    @param account Address of the account to snapshot
    @return (possible error, token balance, borrow balance, exchange rate mantissa)

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    getAccountSnapshot: async (params: CallParams<CErc20Args['obj']['getAccountSnapshot(address)(uint256,uint256,uint256,uint256)'] | CErc20Args['tuple']['getAccountSnapshot(address)(uint256,uint256,uint256,uint256)']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(CErc20ParamsFactory.getAccountSnapshot(params))
      return {...result, return: result.return as unknown as (undefined | CErc20Returns['getAccountSnapshot(address)(uint256,uint256,uint256,uint256)'])}
    },

    /**
     * Makes a call to the CErc20 smart contract using the `borrowRatePerBlock()uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
    *  @notice Returns the current per-block borrow interest rate for this cToken
    @return The borrow interest rate per block, scaled by 1e18

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    borrowRatePerBlock: async (params: CallParams<CErc20Args['obj']['borrowRatePerBlock()uint256'] | CErc20Args['tuple']['borrowRatePerBlock()uint256']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(CErc20ParamsFactory.borrowRatePerBlock(params))
      return {...result, return: result.return as unknown as (undefined | CErc20Returns['borrowRatePerBlock()uint256'])}
    },

    /**
     * Makes a call to the CErc20 smart contract using the `supplyRatePerBlock()uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
    *  @notice Returns the current per-block supply interest rate for this cToken
    @return The supply interest rate per block, scaled by 1e18

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    supplyRatePerBlock: async (params: CallParams<CErc20Args['obj']['supplyRatePerBlock()uint256'] | CErc20Args['tuple']['supplyRatePerBlock()uint256']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(CErc20ParamsFactory.supplyRatePerBlock(params))
      return {...result, return: result.return as unknown as (undefined | CErc20Returns['supplyRatePerBlock()uint256'])}
    },

    /**
     * Makes a call to the CErc20 smart contract using the `totalBorrowsCurrent()uint256` ABI method.
     *
    *  @notice Returns the current total borrows plus accrued interest
    @return The total borrows with interest

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    totalBorrowsCurrent: async (params: CallParams<CErc20Args['obj']['totalBorrowsCurrent()uint256'] | CErc20Args['tuple']['totalBorrowsCurrent()uint256']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(CErc20ParamsFactory.totalBorrowsCurrent(params))
      return {...result, return: result.return as unknown as (undefined | CErc20Returns['totalBorrowsCurrent()uint256'])}
    },

    /**
     * Makes a call to the CErc20 smart contract using the `borrowBalanceCurrent(address)uint256` ABI method.
     *
    *  @notice Accrue interest to updated borrowIndex and then calculate account's borrow balance using the updated borrowIndex
    @param account The address whose balance should be calculated after updating borrowIndex
    @return The calculated balance

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    borrowBalanceCurrent: async (params: CallParams<CErc20Args['obj']['borrowBalanceCurrent(address)uint256'] | CErc20Args['tuple']['borrowBalanceCurrent(address)uint256']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(CErc20ParamsFactory.borrowBalanceCurrent(params))
      return {...result, return: result.return as unknown as (undefined | CErc20Returns['borrowBalanceCurrent(address)uint256'])}
    },

    /**
     * Makes a call to the CErc20 smart contract using the `borrowBalanceStored(address)uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
    *  @notice Return the borrow balance of account based on stored data
    @param account The address whose balance should be calculated
    @return The calculated balance

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    borrowBalanceStored: async (params: CallParams<CErc20Args['obj']['borrowBalanceStored(address)uint256'] | CErc20Args['tuple']['borrowBalanceStored(address)uint256']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(CErc20ParamsFactory.borrowBalanceStored(params))
      return {...result, return: result.return as unknown as (undefined | CErc20Returns['borrowBalanceStored(address)uint256'])}
    },

    /**
     * Makes a call to the CErc20 smart contract using the `exchangeRateCurrent()uint256` ABI method.
     *
    *  @notice Accrue interest then return the up-to-date exchange rate
    @return Calculated exchange rate scaled by 1e18

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    exchangeRateCurrent: async (params: CallParams<CErc20Args['obj']['exchangeRateCurrent()uint256'] | CErc20Args['tuple']['exchangeRateCurrent()uint256']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(CErc20ParamsFactory.exchangeRateCurrent(params))
      return {...result, return: result.return as unknown as (undefined | CErc20Returns['exchangeRateCurrent()uint256'])}
    },

    /**
     * Makes a call to the CErc20 smart contract using the `exchangeRateStored()uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
    *  @notice Calculates the exchange rate from the underlying to the CToken
    @dev This function does not accrue interest before calculating the exchange rate
    @return Calculated exchange rate scaled by 1e18

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    exchangeRateStored: async (params: CallParams<CErc20Args['obj']['exchangeRateStored()uint256'] | CErc20Args['tuple']['exchangeRateStored()uint256']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(CErc20ParamsFactory.exchangeRateStored(params))
      return {...result, return: result.return as unknown as (undefined | CErc20Returns['exchangeRateStored()uint256'])}
    },

    /**
     * Makes a call to the CErc20 smart contract using the `getCash()uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
    *  @notice Get cash balance of this cToken in the underlying asset
    @return The quantity of underlying asset owned by this contract

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    getCash: async (params: CallParams<CErc20Args['obj']['getCash()uint256'] | CErc20Args['tuple']['getCash()uint256']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(CErc20ParamsFactory.getCash(params))
      return {...result, return: result.return as unknown as (undefined | CErc20Returns['getCash()uint256'])}
    },

    /**
     * Makes a call to the CErc20 smart contract using the `accrueInterest()uint256` ABI method.
     *
    *  @notice Applies accrued interest to total borrows and reserves
    @dev This calculates interest accrued from the last checkpointed block
      up to the current block and writes new checkpoint to storage.

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    accrueInterest: async (params: CallParams<CErc20Args['obj']['accrueInterest()uint256'] | CErc20Args['tuple']['accrueInterest()uint256']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(CErc20ParamsFactory.accrueInterest(params))
      return {...result, return: result.return as unknown as (undefined | CErc20Returns['accrueInterest()uint256'])}
    },

    /**
     * Makes a call to the CErc20 smart contract using the `seize(address,address,uint256)uint256` ABI method.
     *
    *  @notice Transfers collateral tokens (this market) to the liquidator.
    @dev Will fail unless called by another cToken during the process of liquidation.
     Its absolutely critical to use msg.sender as the borrowed cToken and not a parameter.
    @param liquidator The account receiving seized collateral
    @param borrower The account having collateral seized
    @param seizeTokens The number of cTokens to seize
    @return uint 0=success, otherwise a failure (see ErrorReporter.sol for details)

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    seize: async (params: CallParams<CErc20Args['obj']['seize(address,address,uint256)uint256'] | CErc20Args['tuple']['seize(address,address,uint256)uint256']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(CErc20ParamsFactory.seize(params))
      return {...result, return: result.return as unknown as (undefined | CErc20Returns['seize(address,address,uint256)uint256'])}
    },

    /**
     * Makes a call to the CErc20 smart contract using the `_setPendingAdmin(address)uint256` ABI method.
     *
    *  @notice Begins transfer of admin rights. The newPendingAdmin must call `_acceptAdmin` to finalize the transfer.
    @dev Admin function to begin change of admin. The newPendingAdmin must call `_acceptAdmin` to finalize the transfer.
    @param newPendingAdmin New pending admin.
    @return uint 0=success, otherwise a failure (see ErrorReporter.sol for details)

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    _setPendingAdmin: async (params: CallParams<CErc20Args['obj']['_setPendingAdmin(address)uint256'] | CErc20Args['tuple']['_setPendingAdmin(address)uint256']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(CErc20ParamsFactory._setPendingAdmin(params))
      return {...result, return: result.return as unknown as (undefined | CErc20Returns['_setPendingAdmin(address)uint256'])}
    },

    /**
     * Makes a call to the CErc20 smart contract using the `_acceptAdmin()uint256` ABI method.
     *
    *  @notice Accepts transfer of admin rights. msg.sender must be pendingAdmin
    @dev Admin function for pending admin to accept role and update admin
    @return uint 0=success, otherwise a failure (see ErrorReporter.sol for details)

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    _acceptAdmin: async (params: CallParams<CErc20Args['obj']['_acceptAdmin()uint256'] | CErc20Args['tuple']['_acceptAdmin()uint256']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(CErc20ParamsFactory._acceptAdmin(params))
      return {...result, return: result.return as unknown as (undefined | CErc20Returns['_acceptAdmin()uint256'])}
    },

    /**
     * Makes a call to the CErc20 smart contract using the `_setComptroller(address)uint256` ABI method.
     *
    *  @notice Sets a new comptroller for the market
    @dev Admin function to set a new comptroller
    @return uint 0=success, otherwise a failure (see ErrorReporter.sol for details)

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    _setComptroller: async (params: CallParams<CErc20Args['obj']['_setComptroller(address)uint256'] | CErc20Args['tuple']['_setComptroller(address)uint256']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(CErc20ParamsFactory._setComptroller(params))
      return {...result, return: result.return as unknown as (undefined | CErc20Returns['_setComptroller(address)uint256'])}
    },

    /**
     * Makes a call to the CErc20 smart contract using the `_setReserveFactor(uint256)uint256` ABI method.
     *
    *  @notice accrues interest and sets a new reserve factor for the protocol using _setReserveFactorFresh
    @dev Admin function to accrue interest and set a new reserve factor
    @return uint 0=success, otherwise a failure (see ErrorReporter.sol for details)

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    _setReserveFactor: async (params: CallParams<CErc20Args['obj']['_setReserveFactor(uint256)uint256'] | CErc20Args['tuple']['_setReserveFactor(uint256)uint256']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(CErc20ParamsFactory._setReserveFactor(params))
      return {...result, return: result.return as unknown as (undefined | CErc20Returns['_setReserveFactor(uint256)uint256'])}
    },

    /**
     * Makes a call to the CErc20 smart contract using the `_reduceReserves(uint256)uint256` ABI method.
     *
    *  @notice Accrues interest and reduces reserves by transferring to admin
    @param reduceAmount Amount of reduction to reserves
    @return uint 0=success, otherwise a failure (see ErrorReporter.sol for details)

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    _reduceReserves: async (params: CallParams<CErc20Args['obj']['_reduceReserves(uint256)uint256'] | CErc20Args['tuple']['_reduceReserves(uint256)uint256']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(CErc20ParamsFactory._reduceReserves(params))
      return {...result, return: result.return as unknown as (undefined | CErc20Returns['_reduceReserves(uint256)uint256'])}
    },

    /**
     * Makes a call to the CErc20 smart contract using the `_setInterestRateModel(address)uint256` ABI method.
     *
    *  @notice accrues interest and updates the interest rate model using _setInterestRateModelFresh
    @dev Admin function to accrue interest and update the interest rate model
    @param newInterestRateModel the new interest rate model to use
    @return uint 0=success, otherwise a failure (see ErrorReporter.sol for details)

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    _setInterestRateModel: async (params: CallParams<CErc20Args['obj']['_setInterestRateModel(address)uint256'] | CErc20Args['tuple']['_setInterestRateModel(address)uint256']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(CErc20ParamsFactory._setInterestRateModel(params))
      return {...result, return: result.return as unknown as (undefined | CErc20Returns['_setInterestRateModel(address)uint256'])}
    },

    /**
     * Makes a call to the CErc20 smart contract using the `underlying()address` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    underlying: async (params: CallParams<CErc20Args['obj']['underlying()address'] | CErc20Args['tuple']['underlying()address']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(CErc20ParamsFactory.underlying(params))
      return {...result, return: result.return as unknown as (undefined | CErc20Returns['underlying()address'])}
    },

    /**
     * Makes a call to the CErc20 smart contract using the `name()string` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    name: async (params: CallParams<CErc20Args['obj']['name()string'] | CErc20Args['tuple']['name()string']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(CErc20ParamsFactory.name(params))
      return {...result, return: result.return as unknown as (undefined | CErc20Returns['name()string'])}
    },

    /**
     * Makes a call to the CErc20 smart contract using the `symbol()string` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    symbol: async (params: CallParams<CErc20Args['obj']['symbol()string'] | CErc20Args['tuple']['symbol()string']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(CErc20ParamsFactory.symbol(params))
      return {...result, return: result.return as unknown as (undefined | CErc20Returns['symbol()string'])}
    },

    /**
     * Makes a call to the CErc20 smart contract using the `decimals()uint64` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    decimals: async (params: CallParams<CErc20Args['obj']['decimals()uint64'] | CErc20Args['tuple']['decimals()uint64']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(CErc20ParamsFactory.decimals(params))
      return {...result, return: result.return as unknown as (undefined | CErc20Returns['decimals()uint64'])}
    },

    /**
     * Makes a call to the CErc20 smart contract using the `admin()address` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    admin: async (params: CallParams<CErc20Args['obj']['admin()address'] | CErc20Args['tuple']['admin()address']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(CErc20ParamsFactory.admin(params))
      return {...result, return: result.return as unknown as (undefined | CErc20Returns['admin()address'])}
    },

    /**
     * Makes a call to the CErc20 smart contract using the `pendingAdmin()address` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    pendingAdmin: async (params: CallParams<CErc20Args['obj']['pendingAdmin()address'] | CErc20Args['tuple']['pendingAdmin()address']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(CErc20ParamsFactory.pendingAdmin(params))
      return {...result, return: result.return as unknown as (undefined | CErc20Returns['pendingAdmin()address'])}
    },

    /**
     * Makes a call to the CErc20 smart contract using the `comptroller()address` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    comptroller: async (params: CallParams<CErc20Args['obj']['comptroller()address'] | CErc20Args['tuple']['comptroller()address']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(CErc20ParamsFactory.comptroller(params))
      return {...result, return: result.return as unknown as (undefined | CErc20Returns['comptroller()address'])}
    },

    /**
     * Makes a call to the CErc20 smart contract using the `interestRateModel()address` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    interestRateModel: async (params: CallParams<CErc20Args['obj']['interestRateModel()address'] | CErc20Args['tuple']['interestRateModel()address']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(CErc20ParamsFactory.interestRateModel(params))
      return {...result, return: result.return as unknown as (undefined | CErc20Returns['interestRateModel()address'])}
    },

    /**
     * Makes a call to the CErc20 smart contract using the `reserveFactorMantissa()uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    reserveFactorMantissa: async (params: CallParams<CErc20Args['obj']['reserveFactorMantissa()uint256'] | CErc20Args['tuple']['reserveFactorMantissa()uint256']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(CErc20ParamsFactory.reserveFactorMantissa(params))
      return {...result, return: result.return as unknown as (undefined | CErc20Returns['reserveFactorMantissa()uint256'])}
    },

    /**
     * Makes a call to the CErc20 smart contract using the `accrualBlockNumber()uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    accrualBlockNumber: async (params: CallParams<CErc20Args['obj']['accrualBlockNumber()uint256'] | CErc20Args['tuple']['accrualBlockNumber()uint256']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(CErc20ParamsFactory.accrualBlockNumber(params))
      return {...result, return: result.return as unknown as (undefined | CErc20Returns['accrualBlockNumber()uint256'])}
    },

    /**
     * Makes a call to the CErc20 smart contract using the `borrowIndex()uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    borrowIndex: async (params: CallParams<CErc20Args['obj']['borrowIndex()uint256'] | CErc20Args['tuple']['borrowIndex()uint256']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(CErc20ParamsFactory.borrowIndex(params))
      return {...result, return: result.return as unknown as (undefined | CErc20Returns['borrowIndex()uint256'])}
    },

    /**
     * Makes a call to the CErc20 smart contract using the `totalBorrows()uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    totalBorrows: async (params: CallParams<CErc20Args['obj']['totalBorrows()uint256'] | CErc20Args['tuple']['totalBorrows()uint256']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(CErc20ParamsFactory.totalBorrows(params))
      return {...result, return: result.return as unknown as (undefined | CErc20Returns['totalBorrows()uint256'])}
    },

    /**
     * Makes a call to the CErc20 smart contract using the `totalReserves()uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    totalReserves: async (params: CallParams<CErc20Args['obj']['totalReserves()uint256'] | CErc20Args['tuple']['totalReserves()uint256']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(CErc20ParamsFactory.totalReserves(params))
      return {...result, return: result.return as unknown as (undefined | CErc20Returns['totalReserves()uint256'])}
    },

    /**
     * Makes a call to the CErc20 smart contract using the `totalSupply()uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    totalSupply: async (params: CallParams<CErc20Args['obj']['totalSupply()uint256'] | CErc20Args['tuple']['totalSupply()uint256']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(CErc20ParamsFactory.totalSupply(params))
      return {...result, return: result.return as unknown as (undefined | CErc20Returns['totalSupply()uint256'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new CErc20Client(this.appClient.clone(params))
  }

  /**
   * Makes a readonly (simulated) call to the CErc20 smart contract using the `allowance(address,address)uint256` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
  *  @notice Get the current allowance from `owner` for `spender`
  @param owner The address of the account which owns the tokens to be spent
  @param spender The address of the account which may transfer tokens
  @return The number of tokens allowed to be spent (-1 means infinite)

   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async allowance(params: CallParams<CErc20Args['obj']['allowance(address,address)uint256'] | CErc20Args['tuple']['allowance(address,address)uint256']>) {
    const result = await this.appClient.send.call(CErc20ParamsFactory.allowance(params))
    return result.return as unknown as CErc20Returns['allowance(address,address)uint256']
  }

  /**
   * Makes a readonly (simulated) call to the CErc20 smart contract using the `balanceOf(address)uint256` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
  *  @notice Get the token balance of the `owner`
  @param owner The address of the account to query
  @return The number of tokens owned by `owner`

   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async balanceOf(params: CallParams<CErc20Args['obj']['balanceOf(address)uint256'] | CErc20Args['tuple']['balanceOf(address)uint256']>) {
    const result = await this.appClient.send.call(CErc20ParamsFactory.balanceOf(params))
    return result.return as unknown as CErc20Returns['balanceOf(address)uint256']
  }

  /**
   * Makes a readonly (simulated) call to the CErc20 smart contract using the `getAccountSnapshot(address)(uint256,uint256,uint256,uint256)` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
  *  @notice Get a snapshot of the account's balances, and the cached exchange rate
  @dev This is used by comptroller to more efficiently perform liquidity checks.
  @param account Address of the account to snapshot
  @return (possible error, token balance, borrow balance, exchange rate mantissa)

   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async getAccountSnapshot(params: CallParams<CErc20Args['obj']['getAccountSnapshot(address)(uint256,uint256,uint256,uint256)'] | CErc20Args['tuple']['getAccountSnapshot(address)(uint256,uint256,uint256,uint256)']>) {
    const result = await this.appClient.send.call(CErc20ParamsFactory.getAccountSnapshot(params))
    return result.return as unknown as CErc20Returns['getAccountSnapshot(address)(uint256,uint256,uint256,uint256)']
  }

  /**
   * Makes a readonly (simulated) call to the CErc20 smart contract using the `borrowRatePerBlock()uint256` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
  *  @notice Returns the current per-block borrow interest rate for this cToken
  @return The borrow interest rate per block, scaled by 1e18

   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async borrowRatePerBlock(params: CallParams<CErc20Args['obj']['borrowRatePerBlock()uint256'] | CErc20Args['tuple']['borrowRatePerBlock()uint256']> = {args: []}) {
    const result = await this.appClient.send.call(CErc20ParamsFactory.borrowRatePerBlock(params))
    return result.return as unknown as CErc20Returns['borrowRatePerBlock()uint256']
  }

  /**
   * Makes a readonly (simulated) call to the CErc20 smart contract using the `supplyRatePerBlock()uint256` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
  *  @notice Returns the current per-block supply interest rate for this cToken
  @return The supply interest rate per block, scaled by 1e18

   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async supplyRatePerBlock(params: CallParams<CErc20Args['obj']['supplyRatePerBlock()uint256'] | CErc20Args['tuple']['supplyRatePerBlock()uint256']> = {args: []}) {
    const result = await this.appClient.send.call(CErc20ParamsFactory.supplyRatePerBlock(params))
    return result.return as unknown as CErc20Returns['supplyRatePerBlock()uint256']
  }

  /**
   * Makes a readonly (simulated) call to the CErc20 smart contract using the `borrowBalanceStored(address)uint256` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
  *  @notice Return the borrow balance of account based on stored data
  @param account The address whose balance should be calculated
  @return The calculated balance

   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async borrowBalanceStored(params: CallParams<CErc20Args['obj']['borrowBalanceStored(address)uint256'] | CErc20Args['tuple']['borrowBalanceStored(address)uint256']>) {
    const result = await this.appClient.send.call(CErc20ParamsFactory.borrowBalanceStored(params))
    return result.return as unknown as CErc20Returns['borrowBalanceStored(address)uint256']
  }

  /**
   * Makes a readonly (simulated) call to the CErc20 smart contract using the `exchangeRateStored()uint256` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
  *  @notice Calculates the exchange rate from the underlying to the CToken
  @dev This function does not accrue interest before calculating the exchange rate
  @return Calculated exchange rate scaled by 1e18

   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async exchangeRateStored(params: CallParams<CErc20Args['obj']['exchangeRateStored()uint256'] | CErc20Args['tuple']['exchangeRateStored()uint256']> = {args: []}) {
    const result = await this.appClient.send.call(CErc20ParamsFactory.exchangeRateStored(params))
    return result.return as unknown as CErc20Returns['exchangeRateStored()uint256']
  }

  /**
   * Makes a readonly (simulated) call to the CErc20 smart contract using the `getCash()uint256` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
  *  @notice Get cash balance of this cToken in the underlying asset
  @return The quantity of underlying asset owned by this contract

   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async getCash(params: CallParams<CErc20Args['obj']['getCash()uint256'] | CErc20Args['tuple']['getCash()uint256']> = {args: []}) {
    const result = await this.appClient.send.call(CErc20ParamsFactory.getCash(params))
    return result.return as unknown as CErc20Returns['getCash()uint256']
  }

  /**
   * Makes a readonly (simulated) call to the CErc20 smart contract using the `underlying()address` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async underlying(params: CallParams<CErc20Args['obj']['underlying()address'] | CErc20Args['tuple']['underlying()address']> = {args: []}) {
    const result = await this.appClient.send.call(CErc20ParamsFactory.underlying(params))
    return result.return as unknown as CErc20Returns['underlying()address']
  }

  /**
   * Makes a readonly (simulated) call to the CErc20 smart contract using the `name()string` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async name(params: CallParams<CErc20Args['obj']['name()string'] | CErc20Args['tuple']['name()string']> = {args: []}) {
    const result = await this.appClient.send.call(CErc20ParamsFactory.name(params))
    return result.return as unknown as CErc20Returns['name()string']
  }

  /**
   * Makes a readonly (simulated) call to the CErc20 smart contract using the `symbol()string` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async symbol(params: CallParams<CErc20Args['obj']['symbol()string'] | CErc20Args['tuple']['symbol()string']> = {args: []}) {
    const result = await this.appClient.send.call(CErc20ParamsFactory.symbol(params))
    return result.return as unknown as CErc20Returns['symbol()string']
  }

  /**
   * Makes a readonly (simulated) call to the CErc20 smart contract using the `decimals()uint64` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async decimals(params: CallParams<CErc20Args['obj']['decimals()uint64'] | CErc20Args['tuple']['decimals()uint64']> = {args: []}) {
    const result = await this.appClient.send.call(CErc20ParamsFactory.decimals(params))
    return result.return as unknown as CErc20Returns['decimals()uint64']
  }

  /**
   * Makes a readonly (simulated) call to the CErc20 smart contract using the `admin()address` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async admin(params: CallParams<CErc20Args['obj']['admin()address'] | CErc20Args['tuple']['admin()address']> = {args: []}) {
    const result = await this.appClient.send.call(CErc20ParamsFactory.admin(params))
    return result.return as unknown as CErc20Returns['admin()address']
  }

  /**
   * Makes a readonly (simulated) call to the CErc20 smart contract using the `pendingAdmin()address` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async pendingAdmin(params: CallParams<CErc20Args['obj']['pendingAdmin()address'] | CErc20Args['tuple']['pendingAdmin()address']> = {args: []}) {
    const result = await this.appClient.send.call(CErc20ParamsFactory.pendingAdmin(params))
    return result.return as unknown as CErc20Returns['pendingAdmin()address']
  }

  /**
   * Makes a readonly (simulated) call to the CErc20 smart contract using the `comptroller()address` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async comptroller(params: CallParams<CErc20Args['obj']['comptroller()address'] | CErc20Args['tuple']['comptroller()address']> = {args: []}) {
    const result = await this.appClient.send.call(CErc20ParamsFactory.comptroller(params))
    return result.return as unknown as CErc20Returns['comptroller()address']
  }

  /**
   * Makes a readonly (simulated) call to the CErc20 smart contract using the `interestRateModel()address` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async interestRateModel(params: CallParams<CErc20Args['obj']['interestRateModel()address'] | CErc20Args['tuple']['interestRateModel()address']> = {args: []}) {
    const result = await this.appClient.send.call(CErc20ParamsFactory.interestRateModel(params))
    return result.return as unknown as CErc20Returns['interestRateModel()address']
  }

  /**
   * Makes a readonly (simulated) call to the CErc20 smart contract using the `reserveFactorMantissa()uint256` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async reserveFactorMantissa(params: CallParams<CErc20Args['obj']['reserveFactorMantissa()uint256'] | CErc20Args['tuple']['reserveFactorMantissa()uint256']> = {args: []}) {
    const result = await this.appClient.send.call(CErc20ParamsFactory.reserveFactorMantissa(params))
    return result.return as unknown as CErc20Returns['reserveFactorMantissa()uint256']
  }

  /**
   * Makes a readonly (simulated) call to the CErc20 smart contract using the `accrualBlockNumber()uint256` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async accrualBlockNumber(params: CallParams<CErc20Args['obj']['accrualBlockNumber()uint256'] | CErc20Args['tuple']['accrualBlockNumber()uint256']> = {args: []}) {
    const result = await this.appClient.send.call(CErc20ParamsFactory.accrualBlockNumber(params))
    return result.return as unknown as CErc20Returns['accrualBlockNumber()uint256']
  }

  /**
   * Makes a readonly (simulated) call to the CErc20 smart contract using the `borrowIndex()uint256` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async borrowIndex(params: CallParams<CErc20Args['obj']['borrowIndex()uint256'] | CErc20Args['tuple']['borrowIndex()uint256']> = {args: []}) {
    const result = await this.appClient.send.call(CErc20ParamsFactory.borrowIndex(params))
    return result.return as unknown as CErc20Returns['borrowIndex()uint256']
  }

  /**
   * Makes a readonly (simulated) call to the CErc20 smart contract using the `totalBorrows()uint256` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async totalBorrows(params: CallParams<CErc20Args['obj']['totalBorrows()uint256'] | CErc20Args['tuple']['totalBorrows()uint256']> = {args: []}) {
    const result = await this.appClient.send.call(CErc20ParamsFactory.totalBorrows(params))
    return result.return as unknown as CErc20Returns['totalBorrows()uint256']
  }

  /**
   * Makes a readonly (simulated) call to the CErc20 smart contract using the `totalReserves()uint256` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async totalReserves(params: CallParams<CErc20Args['obj']['totalReserves()uint256'] | CErc20Args['tuple']['totalReserves()uint256']> = {args: []}) {
    const result = await this.appClient.send.call(CErc20ParamsFactory.totalReserves(params))
    return result.return as unknown as CErc20Returns['totalReserves()uint256']
  }

  /**
   * Makes a readonly (simulated) call to the CErc20 smart contract using the `totalSupply()uint256` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async totalSupply(params: CallParams<CErc20Args['obj']['totalSupply()uint256'] | CErc20Args['tuple']['totalSupply()uint256']> = {args: []}) {
    const result = await this.appClient.send.call(CErc20ParamsFactory.totalSupply(params))
    return result.return as unknown as CErc20Returns['totalSupply()uint256']
  }

  /**
   * Methods to access state for the current CErc20 app
   */
  state = {
    /**
     * Methods to access global state for the current CErc20 app
     */
    global: {
      /**
       * Get all current keyed values from global state
       */
      getAll: async (): Promise<Partial<Expand<GlobalKeysState>>> => {
        const result = await this.appClient.state.global.getAll()
        return {
          underlying: result.underlying,
          _notEntered: result._notEntered,
          name: result.name,
          symbol: result.symbol,
          decimals: result.decimals,
          admin: result.admin,
          pendingAdmin: result.pendingAdmin,
          comptroller: result.comptroller,
          interestRateModel: result.interestRateModel,
          initialExchangeRateMantissa: new BinaryStateValue(result.initialExchangeRateMantissa),
          reserveFactorMantissa: new BinaryStateValue(result.reserveFactorMantissa),
          accrualBlockNumber: new BinaryStateValue(result.accrualBlockNumber),
          borrowIndex: new BinaryStateValue(result.borrowIndex),
          totalBorrows: new BinaryStateValue(result.totalBorrows),
          totalReserves: new BinaryStateValue(result.totalReserves),
          totalSupply: new BinaryStateValue(result.totalSupply),
        }
      },
      /**
       * Get the current value of the underlying key in global state
       */
      underlying: async (): Promise<string | undefined> => { return (await this.appClient.state.global.getValue("underlying")) as string | undefined },
      /**
       * Get the current value of the _notEntered key in global state
       */
      _notEntered: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("_notEntered")) as bigint | undefined },
      /**
       * Get the current value of the name key in global state
       */
      name: async (): Promise<string | undefined> => { return (await this.appClient.state.global.getValue("name")) as string | undefined },
      /**
       * Get the current value of the symbol key in global state
       */
      symbol: async (): Promise<string | undefined> => { return (await this.appClient.state.global.getValue("symbol")) as string | undefined },
      /**
       * Get the current value of the decimals key in global state
       */
      decimals: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("decimals")) as bigint | undefined },
      /**
       * Get the current value of the admin key in global state
       */
      admin: async (): Promise<string | undefined> => { return (await this.appClient.state.global.getValue("admin")) as string | undefined },
      /**
       * Get the current value of the pendingAdmin key in global state
       */
      pendingAdmin: async (): Promise<string | undefined> => { return (await this.appClient.state.global.getValue("pendingAdmin")) as string | undefined },
      /**
       * Get the current value of the comptroller key in global state
       */
      comptroller: async (): Promise<string | undefined> => { return (await this.appClient.state.global.getValue("comptroller")) as string | undefined },
      /**
       * Get the current value of the interestRateModel key in global state
       */
      interestRateModel: async (): Promise<string | undefined> => { return (await this.appClient.state.global.getValue("interestRateModel")) as string | undefined },
      /**
       * Get the current value of the initialExchangeRateMantissa key in global state
       */
      initialExchangeRateMantissa: async (): Promise<BinaryState> => { return new BinaryStateValue((await this.appClient.state.global.getValue("initialExchangeRateMantissa")) as Uint8Array | undefined) },
      /**
       * Get the current value of the reserveFactorMantissa key in global state
       */
      reserveFactorMantissa: async (): Promise<BinaryState> => { return new BinaryStateValue((await this.appClient.state.global.getValue("reserveFactorMantissa")) as Uint8Array | undefined) },
      /**
       * Get the current value of the accrualBlockNumber key in global state
       */
      accrualBlockNumber: async (): Promise<BinaryState> => { return new BinaryStateValue((await this.appClient.state.global.getValue("accrualBlockNumber")) as Uint8Array | undefined) },
      /**
       * Get the current value of the borrowIndex key in global state
       */
      borrowIndex: async (): Promise<BinaryState> => { return new BinaryStateValue((await this.appClient.state.global.getValue("borrowIndex")) as Uint8Array | undefined) },
      /**
       * Get the current value of the totalBorrows key in global state
       */
      totalBorrows: async (): Promise<BinaryState> => { return new BinaryStateValue((await this.appClient.state.global.getValue("totalBorrows")) as Uint8Array | undefined) },
      /**
       * Get the current value of the totalReserves key in global state
       */
      totalReserves: async (): Promise<BinaryState> => { return new BinaryStateValue((await this.appClient.state.global.getValue("totalReserves")) as Uint8Array | undefined) },
      /**
       * Get the current value of the totalSupply key in global state
       */
      totalSupply: async (): Promise<BinaryState> => { return new BinaryStateValue((await this.appClient.state.global.getValue("totalSupply")) as Uint8Array | undefined) },
    },
    /**
     * Methods to access box state for the current CErc20 app
     */
    box: {
      /**
       * Get all current keyed values from box state
       */
      getAll: async (): Promise<Partial<Expand<BoxKeysState>>> => {
        const result = await this.appClient.state.box.getAll()
        return {
        }
      },
      /**
       * Get values from the accountTokens map in box state
       */
      accountTokens: {
        /**
         * Get all current values of the accountTokens map in box state
         */
        getMap: async (): Promise<Map<Uint8Array, Uint8Array>> => { return (await this.appClient.state.box.getMap("accountTokens")) as Map<Uint8Array, Uint8Array> },
        /**
         * Get a current value of the accountTokens map by key from box state
         */
        value: async (key: Uint8Array | string): Promise<Uint8Array | undefined> => { return await this.appClient.state.box.getMapValue("accountTokens", key) as Uint8Array | undefined },
      },
      /**
       * Get values from the transferAllowances map in box state
       */
      transferAllowances: {
        /**
         * Get all current values of the transferAllowances map in box state
         */
        getMap: async (): Promise<Map<Uint8Array, Uint8Array>> => { return (await this.appClient.state.box.getMap("transferAllowances")) as Map<Uint8Array, Uint8Array> },
        /**
         * Get a current value of the transferAllowances map by key from box state
         */
        value: async (key: Uint8Array | string): Promise<Uint8Array | undefined> => { return await this.appClient.state.box.getMapValue("transferAllowances", key) as Uint8Array | undefined },
      },
      /**
       * Get values from the accountBorrows map in box state
       */
      accountBorrows: {
        /**
         * Get all current values of the accountBorrows map in box state
         */
        getMap: async (): Promise<Map<Uint8Array, BorrowSnapshot>> => { return (await this.appClient.state.box.getMap("accountBorrows")) as Map<Uint8Array, BorrowSnapshot> },
        /**
         * Get a current value of the accountBorrows map by key from box state
         */
        value: async (key: Uint8Array | string): Promise<BorrowSnapshot | undefined> => { return await this.appClient.state.box.getMapValue("accountBorrows", key) as BorrowSnapshot | undefined },
      },
    },
  }

  public newGroup(): CErc20Composer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a initialize(address,address,address,uint256,string,string,uint64)void method call against the CErc20 contract
       */
      initializeAddressAddressAddressUint256StringStringUint64Void(params: CallParams<CErc20Args['obj']['initialize(address,address,address,uint256,string,string,uint64)void'] | CErc20Args['tuple']['initialize(address,address,address,uint256,string,string,uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.initializeAddressAddressAddressUint256StringStringUint64Void(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a mint(uint256)uint256 method call against the CErc20 contract
       */
      mint(params: CallParams<CErc20Args['obj']['mint(uint256)uint256'] | CErc20Args['tuple']['mint(uint256)uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.mint(params)))
        resultMappers.push((v) => client.decodeReturnValue('mint(uint256)uint256', v))
        return this
      },
      /**
       * Add a redeem(uint256)uint256 method call against the CErc20 contract
       */
      redeem(params: CallParams<CErc20Args['obj']['redeem(uint256)uint256'] | CErc20Args['tuple']['redeem(uint256)uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.redeem(params)))
        resultMappers.push((v) => client.decodeReturnValue('redeem(uint256)uint256', v))
        return this
      },
      /**
       * Add a redeemUnderlying(uint256)uint256 method call against the CErc20 contract
       */
      redeemUnderlying(params: CallParams<CErc20Args['obj']['redeemUnderlying(uint256)uint256'] | CErc20Args['tuple']['redeemUnderlying(uint256)uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.redeemUnderlying(params)))
        resultMappers.push((v) => client.decodeReturnValue('redeemUnderlying(uint256)uint256', v))
        return this
      },
      /**
       * Add a borrow(uint256)uint256 method call against the CErc20 contract
       */
      borrow(params: CallParams<CErc20Args['obj']['borrow(uint256)uint256'] | CErc20Args['tuple']['borrow(uint256)uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.borrow(params)))
        resultMappers.push((v) => client.decodeReturnValue('borrow(uint256)uint256', v))
        return this
      },
      /**
       * Add a repayBorrow(uint256)uint256 method call against the CErc20 contract
       */
      repayBorrow(params: CallParams<CErc20Args['obj']['repayBorrow(uint256)uint256'] | CErc20Args['tuple']['repayBorrow(uint256)uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.repayBorrow(params)))
        resultMappers.push((v) => client.decodeReturnValue('repayBorrow(uint256)uint256', v))
        return this
      },
      /**
       * Add a repayBorrowBehalf(address,uint256)uint256 method call against the CErc20 contract
       */
      repayBorrowBehalf(params: CallParams<CErc20Args['obj']['repayBorrowBehalf(address,uint256)uint256'] | CErc20Args['tuple']['repayBorrowBehalf(address,uint256)uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.repayBorrowBehalf(params)))
        resultMappers.push((v) => client.decodeReturnValue('repayBorrowBehalf(address,uint256)uint256', v))
        return this
      },
      /**
       * Add a liquidateBorrow(address,uint256,address)uint256 method call against the CErc20 contract
       */
      liquidateBorrow(params: CallParams<CErc20Args['obj']['liquidateBorrow(address,uint256,address)uint256'] | CErc20Args['tuple']['liquidateBorrow(address,uint256,address)uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.liquidateBorrow(params)))
        resultMappers.push((v) => client.decodeReturnValue('liquidateBorrow(address,uint256,address)uint256', v))
        return this
      },
      /**
       * Add a sweepToken(address)void method call against the CErc20 contract
       */
      sweepToken(params: CallParams<CErc20Args['obj']['sweepToken(address)void'] | CErc20Args['tuple']['sweepToken(address)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.sweepToken(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a _addReserves(uint256)uint256 method call against the CErc20 contract
       */
      _addReserves(params: CallParams<CErc20Args['obj']['_addReserves(uint256)uint256'] | CErc20Args['tuple']['_addReserves(uint256)uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params._addReserves(params)))
        resultMappers.push((v) => client.decodeReturnValue('_addReserves(uint256)uint256', v))
        return this
      },
      /**
       * Add a _delegateCompLikeTo(address)void method call against the CErc20 contract
       */
      _delegateCompLikeTo(params: CallParams<CErc20Args['obj']['_delegateCompLikeTo(address)void'] | CErc20Args['tuple']['_delegateCompLikeTo(address)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params._delegateCompLikeTo(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a initialize(address,address,uint256,string,string,uint64)void method call against the CErc20 contract
       */
      initializeAddressAddressUint256StringStringUint64Void(params: CallParams<CErc20Args['obj']['initialize(address,address,uint256,string,string,uint64)void'] | CErc20Args['tuple']['initialize(address,address,uint256,string,string,uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.initializeAddressAddressUint256StringStringUint64Void(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a transfer(address,uint256)bool method call against the CErc20 contract
       */
      transfer(params: CallParams<CErc20Args['obj']['transfer(address,uint256)bool'] | CErc20Args['tuple']['transfer(address,uint256)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.transfer(params)))
        resultMappers.push((v) => client.decodeReturnValue('transfer(address,uint256)bool', v))
        return this
      },
      /**
       * Add a transferFrom(address,address,uint256)bool method call against the CErc20 contract
       */
      transferFrom(params: CallParams<CErc20Args['obj']['transferFrom(address,address,uint256)bool'] | CErc20Args['tuple']['transferFrom(address,address,uint256)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.transferFrom(params)))
        resultMappers.push((v) => client.decodeReturnValue('transferFrom(address,address,uint256)bool', v))
        return this
      },
      /**
       * Add a approve(address,uint256)bool method call against the CErc20 contract
       */
      approve(params: CallParams<CErc20Args['obj']['approve(address,uint256)bool'] | CErc20Args['tuple']['approve(address,uint256)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.approve(params)))
        resultMappers.push((v) => client.decodeReturnValue('approve(address,uint256)bool', v))
        return this
      },
      /**
       * Add a allowance(address,address)uint256 method call against the CErc20 contract
       */
      allowance(params: CallParams<CErc20Args['obj']['allowance(address,address)uint256'] | CErc20Args['tuple']['allowance(address,address)uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.allowance(params)))
        resultMappers.push((v) => client.decodeReturnValue('allowance(address,address)uint256', v))
        return this
      },
      /**
       * Add a balanceOf(address)uint256 method call against the CErc20 contract
       */
      balanceOf(params: CallParams<CErc20Args['obj']['balanceOf(address)uint256'] | CErc20Args['tuple']['balanceOf(address)uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.balanceOf(params)))
        resultMappers.push((v) => client.decodeReturnValue('balanceOf(address)uint256', v))
        return this
      },
      /**
       * Add a balanceOfUnderlying(address)uint256 method call against the CErc20 contract
       */
      balanceOfUnderlying(params: CallParams<CErc20Args['obj']['balanceOfUnderlying(address)uint256'] | CErc20Args['tuple']['balanceOfUnderlying(address)uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.balanceOfUnderlying(params)))
        resultMappers.push((v) => client.decodeReturnValue('balanceOfUnderlying(address)uint256', v))
        return this
      },
      /**
       * Add a getAccountSnapshot(address)(uint256,uint256,uint256,uint256) method call against the CErc20 contract
       */
      getAccountSnapshot(params: CallParams<CErc20Args['obj']['getAccountSnapshot(address)(uint256,uint256,uint256,uint256)'] | CErc20Args['tuple']['getAccountSnapshot(address)(uint256,uint256,uint256,uint256)']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.getAccountSnapshot(params)))
        resultMappers.push((v) => client.decodeReturnValue('getAccountSnapshot(address)(uint256,uint256,uint256,uint256)', v))
        return this
      },
      /**
       * Add a borrowRatePerBlock()uint256 method call against the CErc20 contract
       */
      borrowRatePerBlock(params: CallParams<CErc20Args['obj']['borrowRatePerBlock()uint256'] | CErc20Args['tuple']['borrowRatePerBlock()uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.borrowRatePerBlock(params)))
        resultMappers.push((v) => client.decodeReturnValue('borrowRatePerBlock()uint256', v))
        return this
      },
      /**
       * Add a supplyRatePerBlock()uint256 method call against the CErc20 contract
       */
      supplyRatePerBlock(params: CallParams<CErc20Args['obj']['supplyRatePerBlock()uint256'] | CErc20Args['tuple']['supplyRatePerBlock()uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.supplyRatePerBlock(params)))
        resultMappers.push((v) => client.decodeReturnValue('supplyRatePerBlock()uint256', v))
        return this
      },
      /**
       * Add a totalBorrowsCurrent()uint256 method call against the CErc20 contract
       */
      totalBorrowsCurrent(params: CallParams<CErc20Args['obj']['totalBorrowsCurrent()uint256'] | CErc20Args['tuple']['totalBorrowsCurrent()uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.totalBorrowsCurrent(params)))
        resultMappers.push((v) => client.decodeReturnValue('totalBorrowsCurrent()uint256', v))
        return this
      },
      /**
       * Add a borrowBalanceCurrent(address)uint256 method call against the CErc20 contract
       */
      borrowBalanceCurrent(params: CallParams<CErc20Args['obj']['borrowBalanceCurrent(address)uint256'] | CErc20Args['tuple']['borrowBalanceCurrent(address)uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.borrowBalanceCurrent(params)))
        resultMappers.push((v) => client.decodeReturnValue('borrowBalanceCurrent(address)uint256', v))
        return this
      },
      /**
       * Add a borrowBalanceStored(address)uint256 method call against the CErc20 contract
       */
      borrowBalanceStored(params: CallParams<CErc20Args['obj']['borrowBalanceStored(address)uint256'] | CErc20Args['tuple']['borrowBalanceStored(address)uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.borrowBalanceStored(params)))
        resultMappers.push((v) => client.decodeReturnValue('borrowBalanceStored(address)uint256', v))
        return this
      },
      /**
       * Add a exchangeRateCurrent()uint256 method call against the CErc20 contract
       */
      exchangeRateCurrent(params: CallParams<CErc20Args['obj']['exchangeRateCurrent()uint256'] | CErc20Args['tuple']['exchangeRateCurrent()uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.exchangeRateCurrent(params)))
        resultMappers.push((v) => client.decodeReturnValue('exchangeRateCurrent()uint256', v))
        return this
      },
      /**
       * Add a exchangeRateStored()uint256 method call against the CErc20 contract
       */
      exchangeRateStored(params: CallParams<CErc20Args['obj']['exchangeRateStored()uint256'] | CErc20Args['tuple']['exchangeRateStored()uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.exchangeRateStored(params)))
        resultMappers.push((v) => client.decodeReturnValue('exchangeRateStored()uint256', v))
        return this
      },
      /**
       * Add a getCash()uint256 method call against the CErc20 contract
       */
      getCash(params: CallParams<CErc20Args['obj']['getCash()uint256'] | CErc20Args['tuple']['getCash()uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.getCash(params)))
        resultMappers.push((v) => client.decodeReturnValue('getCash()uint256', v))
        return this
      },
      /**
       * Add a accrueInterest()uint256 method call against the CErc20 contract
       */
      accrueInterest(params: CallParams<CErc20Args['obj']['accrueInterest()uint256'] | CErc20Args['tuple']['accrueInterest()uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.accrueInterest(params)))
        resultMappers.push((v) => client.decodeReturnValue('accrueInterest()uint256', v))
        return this
      },
      /**
       * Add a seize(address,address,uint256)uint256 method call against the CErc20 contract
       */
      seize(params: CallParams<CErc20Args['obj']['seize(address,address,uint256)uint256'] | CErc20Args['tuple']['seize(address,address,uint256)uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.seize(params)))
        resultMappers.push((v) => client.decodeReturnValue('seize(address,address,uint256)uint256', v))
        return this
      },
      /**
       * Add a _setPendingAdmin(address)uint256 method call against the CErc20 contract
       */
      _setPendingAdmin(params: CallParams<CErc20Args['obj']['_setPendingAdmin(address)uint256'] | CErc20Args['tuple']['_setPendingAdmin(address)uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params._setPendingAdmin(params)))
        resultMappers.push((v) => client.decodeReturnValue('_setPendingAdmin(address)uint256', v))
        return this
      },
      /**
       * Add a _acceptAdmin()uint256 method call against the CErc20 contract
       */
      _acceptAdmin(params: CallParams<CErc20Args['obj']['_acceptAdmin()uint256'] | CErc20Args['tuple']['_acceptAdmin()uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params._acceptAdmin(params)))
        resultMappers.push((v) => client.decodeReturnValue('_acceptAdmin()uint256', v))
        return this
      },
      /**
       * Add a _setComptroller(address)uint256 method call against the CErc20 contract
       */
      _setComptroller(params: CallParams<CErc20Args['obj']['_setComptroller(address)uint256'] | CErc20Args['tuple']['_setComptroller(address)uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params._setComptroller(params)))
        resultMappers.push((v) => client.decodeReturnValue('_setComptroller(address)uint256', v))
        return this
      },
      /**
       * Add a _setReserveFactor(uint256)uint256 method call against the CErc20 contract
       */
      _setReserveFactor(params: CallParams<CErc20Args['obj']['_setReserveFactor(uint256)uint256'] | CErc20Args['tuple']['_setReserveFactor(uint256)uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params._setReserveFactor(params)))
        resultMappers.push((v) => client.decodeReturnValue('_setReserveFactor(uint256)uint256', v))
        return this
      },
      /**
       * Add a _reduceReserves(uint256)uint256 method call against the CErc20 contract
       */
      _reduceReserves(params: CallParams<CErc20Args['obj']['_reduceReserves(uint256)uint256'] | CErc20Args['tuple']['_reduceReserves(uint256)uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params._reduceReserves(params)))
        resultMappers.push((v) => client.decodeReturnValue('_reduceReserves(uint256)uint256', v))
        return this
      },
      /**
       * Add a _setInterestRateModel(address)uint256 method call against the CErc20 contract
       */
      _setInterestRateModel(params: CallParams<CErc20Args['obj']['_setInterestRateModel(address)uint256'] | CErc20Args['tuple']['_setInterestRateModel(address)uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params._setInterestRateModel(params)))
        resultMappers.push((v) => client.decodeReturnValue('_setInterestRateModel(address)uint256', v))
        return this
      },
      /**
       * Add a underlying()address method call against the CErc20 contract
       */
      underlying(params: CallParams<CErc20Args['obj']['underlying()address'] | CErc20Args['tuple']['underlying()address']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.underlying(params)))
        resultMappers.push((v) => client.decodeReturnValue('underlying()address', v))
        return this
      },
      /**
       * Add a name()string method call against the CErc20 contract
       */
      name(params: CallParams<CErc20Args['obj']['name()string'] | CErc20Args['tuple']['name()string']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.name(params)))
        resultMappers.push((v) => client.decodeReturnValue('name()string', v))
        return this
      },
      /**
       * Add a symbol()string method call against the CErc20 contract
       */
      symbol(params: CallParams<CErc20Args['obj']['symbol()string'] | CErc20Args['tuple']['symbol()string']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.symbol(params)))
        resultMappers.push((v) => client.decodeReturnValue('symbol()string', v))
        return this
      },
      /**
       * Add a decimals()uint64 method call against the CErc20 contract
       */
      decimals(params: CallParams<CErc20Args['obj']['decimals()uint64'] | CErc20Args['tuple']['decimals()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.decimals(params)))
        resultMappers.push((v) => client.decodeReturnValue('decimals()uint64', v))
        return this
      },
      /**
       * Add a admin()address method call against the CErc20 contract
       */
      admin(params: CallParams<CErc20Args['obj']['admin()address'] | CErc20Args['tuple']['admin()address']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.admin(params)))
        resultMappers.push((v) => client.decodeReturnValue('admin()address', v))
        return this
      },
      /**
       * Add a pendingAdmin()address method call against the CErc20 contract
       */
      pendingAdmin(params: CallParams<CErc20Args['obj']['pendingAdmin()address'] | CErc20Args['tuple']['pendingAdmin()address']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.pendingAdmin(params)))
        resultMappers.push((v) => client.decodeReturnValue('pendingAdmin()address', v))
        return this
      },
      /**
       * Add a comptroller()address method call against the CErc20 contract
       */
      comptroller(params: CallParams<CErc20Args['obj']['comptroller()address'] | CErc20Args['tuple']['comptroller()address']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.comptroller(params)))
        resultMappers.push((v) => client.decodeReturnValue('comptroller()address', v))
        return this
      },
      /**
       * Add a interestRateModel()address method call against the CErc20 contract
       */
      interestRateModel(params: CallParams<CErc20Args['obj']['interestRateModel()address'] | CErc20Args['tuple']['interestRateModel()address']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.interestRateModel(params)))
        resultMappers.push((v) => client.decodeReturnValue('interestRateModel()address', v))
        return this
      },
      /**
       * Add a reserveFactorMantissa()uint256 method call against the CErc20 contract
       */
      reserveFactorMantissa(params: CallParams<CErc20Args['obj']['reserveFactorMantissa()uint256'] | CErc20Args['tuple']['reserveFactorMantissa()uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.reserveFactorMantissa(params)))
        resultMappers.push((v) => client.decodeReturnValue('reserveFactorMantissa()uint256', v))
        return this
      },
      /**
       * Add a accrualBlockNumber()uint256 method call against the CErc20 contract
       */
      accrualBlockNumber(params: CallParams<CErc20Args['obj']['accrualBlockNumber()uint256'] | CErc20Args['tuple']['accrualBlockNumber()uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.accrualBlockNumber(params)))
        resultMappers.push((v) => client.decodeReturnValue('accrualBlockNumber()uint256', v))
        return this
      },
      /**
       * Add a borrowIndex()uint256 method call against the CErc20 contract
       */
      borrowIndex(params: CallParams<CErc20Args['obj']['borrowIndex()uint256'] | CErc20Args['tuple']['borrowIndex()uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.borrowIndex(params)))
        resultMappers.push((v) => client.decodeReturnValue('borrowIndex()uint256', v))
        return this
      },
      /**
       * Add a totalBorrows()uint256 method call against the CErc20 contract
       */
      totalBorrows(params: CallParams<CErc20Args['obj']['totalBorrows()uint256'] | CErc20Args['tuple']['totalBorrows()uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.totalBorrows(params)))
        resultMappers.push((v) => client.decodeReturnValue('totalBorrows()uint256', v))
        return this
      },
      /**
       * Add a totalReserves()uint256 method call against the CErc20 contract
       */
      totalReserves(params: CallParams<CErc20Args['obj']['totalReserves()uint256'] | CErc20Args['tuple']['totalReserves()uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.totalReserves(params)))
        resultMappers.push((v) => client.decodeReturnValue('totalReserves()uint256', v))
        return this
      },
      /**
       * Add a totalSupply()uint256 method call against the CErc20 contract
       */
      totalSupply(params: CallParams<CErc20Args['obj']['totalSupply()uint256'] | CErc20Args['tuple']['totalSupply()uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.totalSupply(params)))
        resultMappers.push((v) => client.decodeReturnValue('totalSupply()uint256', v))
        return this
      },
      /**
       * Add a clear state call to the CErc20 contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as CErc20Composer
  }
}
export type CErc20Composer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the initialize(address,address,address,uint256,string,string,uint64)void ABI method.
   *
  *  @notice Initialize the new money market
  @param underlying_ The address of the underlying asset
  @param comptroller_ The address of the Comptroller
  @param interestRateModel_ The address of the interest rate model
  @param initialExchangeRateMantissa_ The initial exchange rate, scaled by 1e18
  @param name_ ERC-20 name of this token
  @param symbol_ ERC-20 symbol of this token
  @param decimals_ ERC-20 decimal precision of this token

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  initializeAddressAddressAddressUint256StringStringUint64Void(params?: CallParams<CErc20Args['obj']['initialize(address,address,address,uint256,string,string,uint64)void'] | CErc20Args['tuple']['initialize(address,address,address,uint256,string,string,uint64)void']>): CErc20Composer<[...TReturns, CErc20Returns['initialize(address,address,address,uint256,string,string,uint64)void'] | undefined]>

  /**
   * Calls the mint(uint256)uint256 ABI method.
   *
  *  @notice Sender supplies assets into the market and receives cTokens in exchange
  @dev Accrues interest whether or not the operation succeeds, unless reverted
  @param mintAmount The amount of the underlying asset to supply
  @return uint 0=success, otherwise a failure (see ErrorReporter.sol for details)

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  mint(params?: CallParams<CErc20Args['obj']['mint(uint256)uint256'] | CErc20Args['tuple']['mint(uint256)uint256']>): CErc20Composer<[...TReturns, CErc20Returns['mint(uint256)uint256'] | undefined]>

  /**
   * Calls the redeem(uint256)uint256 ABI method.
   *
  *  @notice Sender redeems cTokens in exchange for the underlying asset
  @dev Accrues interest whether or not the operation succeeds, unless reverted
  @param redeemTokens The number of cTokens to redeem into underlying
  @return uint 0=success, otherwise a failure (see ErrorReporter.sol for details)

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  redeem(params?: CallParams<CErc20Args['obj']['redeem(uint256)uint256'] | CErc20Args['tuple']['redeem(uint256)uint256']>): CErc20Composer<[...TReturns, CErc20Returns['redeem(uint256)uint256'] | undefined]>

  /**
   * Calls the redeemUnderlying(uint256)uint256 ABI method.
   *
  *  @notice Sender redeems cTokens in exchange for a specified amount of underlying asset
  @dev Accrues interest whether or not the operation succeeds, unless reverted
  @param redeemAmount The amount of underlying to redeem
  @return uint 0=success, otherwise a failure (see ErrorReporter.sol for details)

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  redeemUnderlying(params?: CallParams<CErc20Args['obj']['redeemUnderlying(uint256)uint256'] | CErc20Args['tuple']['redeemUnderlying(uint256)uint256']>): CErc20Composer<[...TReturns, CErc20Returns['redeemUnderlying(uint256)uint256'] | undefined]>

  /**
   * Calls the borrow(uint256)uint256 ABI method.
   *
  *  @notice Sender borrows assets from the protocol to their own address
  @param borrowAmount The amount of the underlying asset to borrow
  @return uint 0=success, otherwise a failure (see ErrorReporter.sol for details)

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  borrow(params?: CallParams<CErc20Args['obj']['borrow(uint256)uint256'] | CErc20Args['tuple']['borrow(uint256)uint256']>): CErc20Composer<[...TReturns, CErc20Returns['borrow(uint256)uint256'] | undefined]>

  /**
   * Calls the repayBorrow(uint256)uint256 ABI method.
   *
  *  @notice Sender repays their own borrow
  @param repayAmount The amount to repay, or -1 for the full outstanding amount
  @return uint 0=success, otherwise a failure (see ErrorReporter.sol for details)

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  repayBorrow(params?: CallParams<CErc20Args['obj']['repayBorrow(uint256)uint256'] | CErc20Args['tuple']['repayBorrow(uint256)uint256']>): CErc20Composer<[...TReturns, CErc20Returns['repayBorrow(uint256)uint256'] | undefined]>

  /**
   * Calls the repayBorrowBehalf(address,uint256)uint256 ABI method.
   *
  *  @notice Sender repays a borrow belonging to borrower
  @param borrower the account with the debt being payed off
  @param repayAmount The amount to repay, or -1 for the full outstanding amount
  @return uint 0=success, otherwise a failure (see ErrorReporter.sol for details)

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  repayBorrowBehalf(params?: CallParams<CErc20Args['obj']['repayBorrowBehalf(address,uint256)uint256'] | CErc20Args['tuple']['repayBorrowBehalf(address,uint256)uint256']>): CErc20Composer<[...TReturns, CErc20Returns['repayBorrowBehalf(address,uint256)uint256'] | undefined]>

  /**
   * Calls the liquidateBorrow(address,uint256,address)uint256 ABI method.
   *
  *  @notice The sender liquidates the borrowers collateral.
   The collateral seized is transferred to the liquidator.
  @param borrower The borrower of this cToken to be liquidated
  @param repayAmount The amount of the underlying borrowed asset to repay
  @param cTokenCollateral The market in which to seize collateral from the borrower
  @return uint 0=success, otherwise a failure (see ErrorReporter.sol for details)

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  liquidateBorrow(params?: CallParams<CErc20Args['obj']['liquidateBorrow(address,uint256,address)uint256'] | CErc20Args['tuple']['liquidateBorrow(address,uint256,address)uint256']>): CErc20Composer<[...TReturns, CErc20Returns['liquidateBorrow(address,uint256,address)uint256'] | undefined]>

  /**
   * Calls the sweepToken(address)void ABI method.
   *
  *  @notice A public function to sweep accidental ERC-20 transfers to this contract. Tokens are sent to admin (timelock)
  @param token The address of the ERC-20 token to sweep

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  sweepToken(params?: CallParams<CErc20Args['obj']['sweepToken(address)void'] | CErc20Args['tuple']['sweepToken(address)void']>): CErc20Composer<[...TReturns, CErc20Returns['sweepToken(address)void'] | undefined]>

  /**
   * Calls the _addReserves(uint256)uint256 ABI method.
   *
  *  @notice The sender adds to reserves.
  @param addAmount The amount fo underlying token to add as reserves
  @return uint 0=success, otherwise a failure (see ErrorReporter.sol for details)

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  _addReserves(params?: CallParams<CErc20Args['obj']['_addReserves(uint256)uint256'] | CErc20Args['tuple']['_addReserves(uint256)uint256']>): CErc20Composer<[...TReturns, CErc20Returns['_addReserves(uint256)uint256'] | undefined]>

  /**
   * Calls the _delegateCompLikeTo(address)void ABI method.
   *
  *  @notice Admin call to delegate the votes of the COMP-like underlying
  @param compLikeDelegatee The address to delegate votes to
  @dev CTokens whose underlying are not CompLike should revert here

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  _delegateCompLikeTo(params?: CallParams<CErc20Args['obj']['_delegateCompLikeTo(address)void'] | CErc20Args['tuple']['_delegateCompLikeTo(address)void']>): CErc20Composer<[...TReturns, CErc20Returns['_delegateCompLikeTo(address)void'] | undefined]>

  /**
   * Calls the initialize(address,address,uint256,string,string,uint64)void ABI method.
   *
  *  @notice Initialize the money market
  @param comptroller_ The address of the Comptroller
  @param interestRateModel_ The address of the interest rate model
  @param initialExchangeRateMantissa_ The initial exchange rate, scaled by 1e18
  @param name_ EIP-20 name of this token
  @param symbol_ EIP-20 symbol of this token
  @param decimals_ EIP-20 decimal precision of this token

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  initializeAddressAddressUint256StringStringUint64Void(params?: CallParams<CErc20Args['obj']['initialize(address,address,uint256,string,string,uint64)void'] | CErc20Args['tuple']['initialize(address,address,uint256,string,string,uint64)void']>): CErc20Composer<[...TReturns, CErc20Returns['initialize(address,address,uint256,string,string,uint64)void'] | undefined]>

  /**
   * Calls the transfer(address,uint256)bool ABI method.
   *
  *  @notice Transfer `amount` tokens from `msg.sender` to `dst`
  @param dst The address of the destination account
  @param amount The number of tokens to transfer
  @return Whether or not the transfer succeeded

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  transfer(params?: CallParams<CErc20Args['obj']['transfer(address,uint256)bool'] | CErc20Args['tuple']['transfer(address,uint256)bool']>): CErc20Composer<[...TReturns, CErc20Returns['transfer(address,uint256)bool'] | undefined]>

  /**
   * Calls the transferFrom(address,address,uint256)bool ABI method.
   *
  *  @notice Transfer `amount` tokens from `src` to `dst`
  @param src The address of the source account
  @param dst The address of the destination account
  @param amount The number of tokens to transfer
  @return Whether or not the transfer succeeded

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  transferFrom(params?: CallParams<CErc20Args['obj']['transferFrom(address,address,uint256)bool'] | CErc20Args['tuple']['transferFrom(address,address,uint256)bool']>): CErc20Composer<[...TReturns, CErc20Returns['transferFrom(address,address,uint256)bool'] | undefined]>

  /**
   * Calls the approve(address,uint256)bool ABI method.
   *
  *  @notice Approve `spender` to transfer up to `amount` from `src`
  @dev This will overwrite the approval amount for `spender`
   and is subject to issues noted [here](https://eips.ethereum.org/EIPS/eip-20#approve)
  @param spender The address of the account which may transfer tokens
  @param amount The number of tokens that are approved (uint256.max means infinite)
  @return Whether or not the approval succeeded

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  approve(params?: CallParams<CErc20Args['obj']['approve(address,uint256)bool'] | CErc20Args['tuple']['approve(address,uint256)bool']>): CErc20Composer<[...TReturns, CErc20Returns['approve(address,uint256)bool'] | undefined]>

  /**
   * Calls the allowance(address,address)uint256 ABI method.
   *
  *  @notice Get the current allowance from `owner` for `spender`
  @param owner The address of the account which owns the tokens to be spent
  @param spender The address of the account which may transfer tokens
  @return The number of tokens allowed to be spent (-1 means infinite)

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  allowance(params?: CallParams<CErc20Args['obj']['allowance(address,address)uint256'] | CErc20Args['tuple']['allowance(address,address)uint256']>): CErc20Composer<[...TReturns, CErc20Returns['allowance(address,address)uint256'] | undefined]>

  /**
   * Calls the balanceOf(address)uint256 ABI method.
   *
  *  @notice Get the token balance of the `owner`
  @param owner The address of the account to query
  @return The number of tokens owned by `owner`

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  balanceOf(params?: CallParams<CErc20Args['obj']['balanceOf(address)uint256'] | CErc20Args['tuple']['balanceOf(address)uint256']>): CErc20Composer<[...TReturns, CErc20Returns['balanceOf(address)uint256'] | undefined]>

  /**
   * Calls the balanceOfUnderlying(address)uint256 ABI method.
   *
  *  @notice Get the underlying balance of the `owner`
  @dev This also accrues interest in a transaction
  @param owner The address of the account to query
  @return The amount of underlying owned by `owner`

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  balanceOfUnderlying(params?: CallParams<CErc20Args['obj']['balanceOfUnderlying(address)uint256'] | CErc20Args['tuple']['balanceOfUnderlying(address)uint256']>): CErc20Composer<[...TReturns, CErc20Returns['balanceOfUnderlying(address)uint256'] | undefined]>

  /**
   * Calls the getAccountSnapshot(address)(uint256,uint256,uint256,uint256) ABI method.
   *
  *  @notice Get a snapshot of the account's balances, and the cached exchange rate
  @dev This is used by comptroller to more efficiently perform liquidity checks.
  @param account Address of the account to snapshot
  @return (possible error, token balance, borrow balance, exchange rate mantissa)

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  getAccountSnapshot(params?: CallParams<CErc20Args['obj']['getAccountSnapshot(address)(uint256,uint256,uint256,uint256)'] | CErc20Args['tuple']['getAccountSnapshot(address)(uint256,uint256,uint256,uint256)']>): CErc20Composer<[...TReturns, CErc20Returns['getAccountSnapshot(address)(uint256,uint256,uint256,uint256)'] | undefined]>

  /**
   * Calls the borrowRatePerBlock()uint256 ABI method.
   *
  *  @notice Returns the current per-block borrow interest rate for this cToken
  @return The borrow interest rate per block, scaled by 1e18

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  borrowRatePerBlock(params?: CallParams<CErc20Args['obj']['borrowRatePerBlock()uint256'] | CErc20Args['tuple']['borrowRatePerBlock()uint256']>): CErc20Composer<[...TReturns, CErc20Returns['borrowRatePerBlock()uint256'] | undefined]>

  /**
   * Calls the supplyRatePerBlock()uint256 ABI method.
   *
  *  @notice Returns the current per-block supply interest rate for this cToken
  @return The supply interest rate per block, scaled by 1e18

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  supplyRatePerBlock(params?: CallParams<CErc20Args['obj']['supplyRatePerBlock()uint256'] | CErc20Args['tuple']['supplyRatePerBlock()uint256']>): CErc20Composer<[...TReturns, CErc20Returns['supplyRatePerBlock()uint256'] | undefined]>

  /**
   * Calls the totalBorrowsCurrent()uint256 ABI method.
   *
  *  @notice Returns the current total borrows plus accrued interest
  @return The total borrows with interest

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  totalBorrowsCurrent(params?: CallParams<CErc20Args['obj']['totalBorrowsCurrent()uint256'] | CErc20Args['tuple']['totalBorrowsCurrent()uint256']>): CErc20Composer<[...TReturns, CErc20Returns['totalBorrowsCurrent()uint256'] | undefined]>

  /**
   * Calls the borrowBalanceCurrent(address)uint256 ABI method.
   *
  *  @notice Accrue interest to updated borrowIndex and then calculate account's borrow balance using the updated borrowIndex
  @param account The address whose balance should be calculated after updating borrowIndex
  @return The calculated balance

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  borrowBalanceCurrent(params?: CallParams<CErc20Args['obj']['borrowBalanceCurrent(address)uint256'] | CErc20Args['tuple']['borrowBalanceCurrent(address)uint256']>): CErc20Composer<[...TReturns, CErc20Returns['borrowBalanceCurrent(address)uint256'] | undefined]>

  /**
   * Calls the borrowBalanceStored(address)uint256 ABI method.
   *
  *  @notice Return the borrow balance of account based on stored data
  @param account The address whose balance should be calculated
  @return The calculated balance

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  borrowBalanceStored(params?: CallParams<CErc20Args['obj']['borrowBalanceStored(address)uint256'] | CErc20Args['tuple']['borrowBalanceStored(address)uint256']>): CErc20Composer<[...TReturns, CErc20Returns['borrowBalanceStored(address)uint256'] | undefined]>

  /**
   * Calls the exchangeRateCurrent()uint256 ABI method.
   *
  *  @notice Accrue interest then return the up-to-date exchange rate
  @return Calculated exchange rate scaled by 1e18

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  exchangeRateCurrent(params?: CallParams<CErc20Args['obj']['exchangeRateCurrent()uint256'] | CErc20Args['tuple']['exchangeRateCurrent()uint256']>): CErc20Composer<[...TReturns, CErc20Returns['exchangeRateCurrent()uint256'] | undefined]>

  /**
   * Calls the exchangeRateStored()uint256 ABI method.
   *
  *  @notice Calculates the exchange rate from the underlying to the CToken
  @dev This function does not accrue interest before calculating the exchange rate
  @return Calculated exchange rate scaled by 1e18

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  exchangeRateStored(params?: CallParams<CErc20Args['obj']['exchangeRateStored()uint256'] | CErc20Args['tuple']['exchangeRateStored()uint256']>): CErc20Composer<[...TReturns, CErc20Returns['exchangeRateStored()uint256'] | undefined]>

  /**
   * Calls the getCash()uint256 ABI method.
   *
  *  @notice Get cash balance of this cToken in the underlying asset
  @return The quantity of underlying asset owned by this contract

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  getCash(params?: CallParams<CErc20Args['obj']['getCash()uint256'] | CErc20Args['tuple']['getCash()uint256']>): CErc20Composer<[...TReturns, CErc20Returns['getCash()uint256'] | undefined]>

  /**
   * Calls the accrueInterest()uint256 ABI method.
   *
  *  @notice Applies accrued interest to total borrows and reserves
  @dev This calculates interest accrued from the last checkpointed block
    up to the current block and writes new checkpoint to storage.

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  accrueInterest(params?: CallParams<CErc20Args['obj']['accrueInterest()uint256'] | CErc20Args['tuple']['accrueInterest()uint256']>): CErc20Composer<[...TReturns, CErc20Returns['accrueInterest()uint256'] | undefined]>

  /**
   * Calls the seize(address,address,uint256)uint256 ABI method.
   *
  *  @notice Transfers collateral tokens (this market) to the liquidator.
  @dev Will fail unless called by another cToken during the process of liquidation.
   Its absolutely critical to use msg.sender as the borrowed cToken and not a parameter.
  @param liquidator The account receiving seized collateral
  @param borrower The account having collateral seized
  @param seizeTokens The number of cTokens to seize
  @return uint 0=success, otherwise a failure (see ErrorReporter.sol for details)

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  seize(params?: CallParams<CErc20Args['obj']['seize(address,address,uint256)uint256'] | CErc20Args['tuple']['seize(address,address,uint256)uint256']>): CErc20Composer<[...TReturns, CErc20Returns['seize(address,address,uint256)uint256'] | undefined]>

  /**
   * Calls the _setPendingAdmin(address)uint256 ABI method.
   *
  *  @notice Begins transfer of admin rights. The newPendingAdmin must call `_acceptAdmin` to finalize the transfer.
  @dev Admin function to begin change of admin. The newPendingAdmin must call `_acceptAdmin` to finalize the transfer.
  @param newPendingAdmin New pending admin.
  @return uint 0=success, otherwise a failure (see ErrorReporter.sol for details)

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  _setPendingAdmin(params?: CallParams<CErc20Args['obj']['_setPendingAdmin(address)uint256'] | CErc20Args['tuple']['_setPendingAdmin(address)uint256']>): CErc20Composer<[...TReturns, CErc20Returns['_setPendingAdmin(address)uint256'] | undefined]>

  /**
   * Calls the _acceptAdmin()uint256 ABI method.
   *
  *  @notice Accepts transfer of admin rights. msg.sender must be pendingAdmin
  @dev Admin function for pending admin to accept role and update admin
  @return uint 0=success, otherwise a failure (see ErrorReporter.sol for details)

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  _acceptAdmin(params?: CallParams<CErc20Args['obj']['_acceptAdmin()uint256'] | CErc20Args['tuple']['_acceptAdmin()uint256']>): CErc20Composer<[...TReturns, CErc20Returns['_acceptAdmin()uint256'] | undefined]>

  /**
   * Calls the _setComptroller(address)uint256 ABI method.
   *
  *  @notice Sets a new comptroller for the market
  @dev Admin function to set a new comptroller
  @return uint 0=success, otherwise a failure (see ErrorReporter.sol for details)

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  _setComptroller(params?: CallParams<CErc20Args['obj']['_setComptroller(address)uint256'] | CErc20Args['tuple']['_setComptroller(address)uint256']>): CErc20Composer<[...TReturns, CErc20Returns['_setComptroller(address)uint256'] | undefined]>

  /**
   * Calls the _setReserveFactor(uint256)uint256 ABI method.
   *
  *  @notice accrues interest and sets a new reserve factor for the protocol using _setReserveFactorFresh
  @dev Admin function to accrue interest and set a new reserve factor
  @return uint 0=success, otherwise a failure (see ErrorReporter.sol for details)

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  _setReserveFactor(params?: CallParams<CErc20Args['obj']['_setReserveFactor(uint256)uint256'] | CErc20Args['tuple']['_setReserveFactor(uint256)uint256']>): CErc20Composer<[...TReturns, CErc20Returns['_setReserveFactor(uint256)uint256'] | undefined]>

  /**
   * Calls the _reduceReserves(uint256)uint256 ABI method.
   *
  *  @notice Accrues interest and reduces reserves by transferring to admin
  @param reduceAmount Amount of reduction to reserves
  @return uint 0=success, otherwise a failure (see ErrorReporter.sol for details)

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  _reduceReserves(params?: CallParams<CErc20Args['obj']['_reduceReserves(uint256)uint256'] | CErc20Args['tuple']['_reduceReserves(uint256)uint256']>): CErc20Composer<[...TReturns, CErc20Returns['_reduceReserves(uint256)uint256'] | undefined]>

  /**
   * Calls the _setInterestRateModel(address)uint256 ABI method.
   *
  *  @notice accrues interest and updates the interest rate model using _setInterestRateModelFresh
  @dev Admin function to accrue interest and update the interest rate model
  @param newInterestRateModel the new interest rate model to use
  @return uint 0=success, otherwise a failure (see ErrorReporter.sol for details)

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  _setInterestRateModel(params?: CallParams<CErc20Args['obj']['_setInterestRateModel(address)uint256'] | CErc20Args['tuple']['_setInterestRateModel(address)uint256']>): CErc20Composer<[...TReturns, CErc20Returns['_setInterestRateModel(address)uint256'] | undefined]>

  /**
   * Calls the underlying()address ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  underlying(params?: CallParams<CErc20Args['obj']['underlying()address'] | CErc20Args['tuple']['underlying()address']>): CErc20Composer<[...TReturns, CErc20Returns['underlying()address'] | undefined]>

  /**
   * Calls the name()string ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  name(params?: CallParams<CErc20Args['obj']['name()string'] | CErc20Args['tuple']['name()string']>): CErc20Composer<[...TReturns, CErc20Returns['name()string'] | undefined]>

  /**
   * Calls the symbol()string ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  symbol(params?: CallParams<CErc20Args['obj']['symbol()string'] | CErc20Args['tuple']['symbol()string']>): CErc20Composer<[...TReturns, CErc20Returns['symbol()string'] | undefined]>

  /**
   * Calls the decimals()uint64 ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  decimals(params?: CallParams<CErc20Args['obj']['decimals()uint64'] | CErc20Args['tuple']['decimals()uint64']>): CErc20Composer<[...TReturns, CErc20Returns['decimals()uint64'] | undefined]>

  /**
   * Calls the admin()address ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  admin(params?: CallParams<CErc20Args['obj']['admin()address'] | CErc20Args['tuple']['admin()address']>): CErc20Composer<[...TReturns, CErc20Returns['admin()address'] | undefined]>

  /**
   * Calls the pendingAdmin()address ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  pendingAdmin(params?: CallParams<CErc20Args['obj']['pendingAdmin()address'] | CErc20Args['tuple']['pendingAdmin()address']>): CErc20Composer<[...TReturns, CErc20Returns['pendingAdmin()address'] | undefined]>

  /**
   * Calls the comptroller()address ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  comptroller(params?: CallParams<CErc20Args['obj']['comptroller()address'] | CErc20Args['tuple']['comptroller()address']>): CErc20Composer<[...TReturns, CErc20Returns['comptroller()address'] | undefined]>

  /**
   * Calls the interestRateModel()address ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  interestRateModel(params?: CallParams<CErc20Args['obj']['interestRateModel()address'] | CErc20Args['tuple']['interestRateModel()address']>): CErc20Composer<[...TReturns, CErc20Returns['interestRateModel()address'] | undefined]>

  /**
   * Calls the reserveFactorMantissa()uint256 ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  reserveFactorMantissa(params?: CallParams<CErc20Args['obj']['reserveFactorMantissa()uint256'] | CErc20Args['tuple']['reserveFactorMantissa()uint256']>): CErc20Composer<[...TReturns, CErc20Returns['reserveFactorMantissa()uint256'] | undefined]>

  /**
   * Calls the accrualBlockNumber()uint256 ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  accrualBlockNumber(params?: CallParams<CErc20Args['obj']['accrualBlockNumber()uint256'] | CErc20Args['tuple']['accrualBlockNumber()uint256']>): CErc20Composer<[...TReturns, CErc20Returns['accrualBlockNumber()uint256'] | undefined]>

  /**
   * Calls the borrowIndex()uint256 ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  borrowIndex(params?: CallParams<CErc20Args['obj']['borrowIndex()uint256'] | CErc20Args['tuple']['borrowIndex()uint256']>): CErc20Composer<[...TReturns, CErc20Returns['borrowIndex()uint256'] | undefined]>

  /**
   * Calls the totalBorrows()uint256 ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  totalBorrows(params?: CallParams<CErc20Args['obj']['totalBorrows()uint256'] | CErc20Args['tuple']['totalBorrows()uint256']>): CErc20Composer<[...TReturns, CErc20Returns['totalBorrows()uint256'] | undefined]>

  /**
   * Calls the totalReserves()uint256 ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  totalReserves(params?: CallParams<CErc20Args['obj']['totalReserves()uint256'] | CErc20Args['tuple']['totalReserves()uint256']>): CErc20Composer<[...TReturns, CErc20Returns['totalReserves()uint256'] | undefined]>

  /**
   * Calls the totalSupply()uint256 ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  totalSupply(params?: CallParams<CErc20Args['obj']['totalSupply()uint256'] | CErc20Args['tuple']['totalSupply()uint256']>): CErc20Composer<[...TReturns, CErc20Returns['totalSupply()uint256'] | undefined]>

  /**
   * Makes a clear_state call to an existing instance of the CErc20 smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): CErc20Composer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): CErc20Composer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<CErc20ComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<CErc20ComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<CErc20ComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<CErc20ComposerResults<TReturns>>
}
export type CErc20ComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

