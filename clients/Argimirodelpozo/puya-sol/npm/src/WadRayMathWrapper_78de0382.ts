/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"WadRayMathWrapper","structs":{},"methods":[{"name":"WAD_DECIMALS","args":[],"returns":{"type":"uint256"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"WAD","args":[],"returns":{"type":"uint256"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"RAY","args":[],"returns":{"type":"uint256"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"PERCENTAGE_FACTOR","args":[],"returns":{"type":"uint256"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"wadMulDown","args":[{"type":"uint256","name":"__arc4_a"},{"type":"uint256","name":"__arc4_b"}],"returns":{"type":"uint256"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"wadMulUp","args":[{"type":"uint256","name":"__arc4_a"},{"type":"uint256","name":"__arc4_b"}],"returns":{"type":"uint256"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"wadDivDown","args":[{"type":"uint256","name":"__arc4_a"},{"type":"uint256","name":"__arc4_b"}],"returns":{"type":"uint256"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"wadDivUp","args":[{"type":"uint256","name":"__arc4_a"},{"type":"uint256","name":"__arc4_b"}],"returns":{"type":"uint256"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"rayMulDown","args":[{"type":"uint256","name":"__arc4_a"},{"type":"uint256","name":"__arc4_b"}],"returns":{"type":"uint256"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"rayMulUp","args":[{"type":"uint256","name":"__arc4_a"},{"type":"uint256","name":"__arc4_b"}],"returns":{"type":"uint256"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"rayDivDown","args":[{"type":"uint256","name":"__arc4_a"},{"type":"uint256","name":"__arc4_b"}],"returns":{"type":"uint256"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"rayDivUp","args":[{"type":"uint256","name":"__arc4_a"},{"type":"uint256","name":"__arc4_b"}],"returns":{"type":"uint256"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"toWad","args":[{"type":"uint256","name":"__arc4_a"}],"returns":{"type":"uint256"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"toRay","args":[{"type":"uint256","name":"__arc4_a"}],"returns":{"type":"uint256"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"fromWadDown","args":[{"type":"uint256","name":"__arc4_a"}],"returns":{"type":"uint256"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"fromRayUp","args":[{"type":"uint256","name":"__arc4_a"}],"returns":{"type":"uint256"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"bpsToWad","args":[{"type":"uint256","name":"__arc4_a"}],"returns":{"type":"uint256"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"bpsToRay","args":[{"type":"uint256","name":"__arc4_a"}],"returns":{"type":"uint256"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"roundRayUp","args":[{"type":"uint256","name":"__arc4_a"}],"returns":{"type":"uint256"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}}],"arcs":[22,28],"networks":{},"state":{"schema":{"global":{"ints":0,"bytes":0},"local":{"ints":0,"bytes":0}},"keys":{"global":{},"local":{},"box":{}},"maps":{"global":{},"local":{},"box":{}}},"bareActions":{"create":[],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[386,432,478,524,570,616,662,708,718,763,773,818,828,873,883,928,938,983,993,1038,1048,1093,1103],"errorMessage":"invalid number of bytes for arc4.uint256"},{"pc":[399,445,491,537,583,629,675,731,786,841,896,951,1006,1061,1116,1151,1209,1267,1325],"errorMessage":"not payable"},{"pc":[409,455,501,547,593,639,685,740,795,850,905,960,1015,1070,1125],"errorMessage":"overflow"},{"pc":[1412,1475,1542,1601,1704,1767,1834,1893,1976,2002,2071,2097,2142],"errorMessage":"revert"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"events":[]} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


/**
 * The argument types for the WadRayMathWrapper contract
 */
export type WadRayMathWrapperArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'WAD_DECIMALS()uint256': Record<string, never>
    'WAD()uint256': Record<string, never>
    'RAY()uint256': Record<string, never>
    'PERCENTAGE_FACTOR()uint256': Record<string, never>
    'wadMulDown(uint256,uint256)uint256': {
      __arc4A: bigint | number
      __arc4B: bigint | number
    }
    'wadMulUp(uint256,uint256)uint256': {
      __arc4A: bigint | number
      __arc4B: bigint | number
    }
    'wadDivDown(uint256,uint256)uint256': {
      __arc4A: bigint | number
      __arc4B: bigint | number
    }
    'wadDivUp(uint256,uint256)uint256': {
      __arc4A: bigint | number
      __arc4B: bigint | number
    }
    'rayMulDown(uint256,uint256)uint256': {
      __arc4A: bigint | number
      __arc4B: bigint | number
    }
    'rayMulUp(uint256,uint256)uint256': {
      __arc4A: bigint | number
      __arc4B: bigint | number
    }
    'rayDivDown(uint256,uint256)uint256': {
      __arc4A: bigint | number
      __arc4B: bigint | number
    }
    'rayDivUp(uint256,uint256)uint256': {
      __arc4A: bigint | number
      __arc4B: bigint | number
    }
    'toWad(uint256)uint256': {
      __arc4A: bigint | number
    }
    'toRay(uint256)uint256': {
      __arc4A: bigint | number
    }
    'fromWadDown(uint256)uint256': {
      __arc4A: bigint | number
    }
    'fromRayUp(uint256)uint256': {
      __arc4A: bigint | number
    }
    'bpsToWad(uint256)uint256': {
      __arc4A: bigint | number
    }
    'bpsToRay(uint256)uint256': {
      __arc4A: bigint | number
    }
    'roundRayUp(uint256)uint256': {
      __arc4A: bigint | number
    }
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'WAD_DECIMALS()uint256': []
    'WAD()uint256': []
    'RAY()uint256': []
    'PERCENTAGE_FACTOR()uint256': []
    'wadMulDown(uint256,uint256)uint256': [__arc4A: bigint | number, __arc4B: bigint | number]
    'wadMulUp(uint256,uint256)uint256': [__arc4A: bigint | number, __arc4B: bigint | number]
    'wadDivDown(uint256,uint256)uint256': [__arc4A: bigint | number, __arc4B: bigint | number]
    'wadDivUp(uint256,uint256)uint256': [__arc4A: bigint | number, __arc4B: bigint | number]
    'rayMulDown(uint256,uint256)uint256': [__arc4A: bigint | number, __arc4B: bigint | number]
    'rayMulUp(uint256,uint256)uint256': [__arc4A: bigint | number, __arc4B: bigint | number]
    'rayDivDown(uint256,uint256)uint256': [__arc4A: bigint | number, __arc4B: bigint | number]
    'rayDivUp(uint256,uint256)uint256': [__arc4A: bigint | number, __arc4B: bigint | number]
    'toWad(uint256)uint256': [__arc4A: bigint | number]
    'toRay(uint256)uint256': [__arc4A: bigint | number]
    'fromWadDown(uint256)uint256': [__arc4A: bigint | number]
    'fromRayUp(uint256)uint256': [__arc4A: bigint | number]
    'bpsToWad(uint256)uint256': [__arc4A: bigint | number]
    'bpsToRay(uint256)uint256': [__arc4A: bigint | number]
    'roundRayUp(uint256)uint256': [__arc4A: bigint | number]
  }
}

/**
 * The return type for each method
 */
export type WadRayMathWrapperReturns = {
  'WAD_DECIMALS()uint256': bigint
  'WAD()uint256': bigint
  'RAY()uint256': bigint
  'PERCENTAGE_FACTOR()uint256': bigint
  'wadMulDown(uint256,uint256)uint256': bigint
  'wadMulUp(uint256,uint256)uint256': bigint
  'wadDivDown(uint256,uint256)uint256': bigint
  'wadDivUp(uint256,uint256)uint256': bigint
  'rayMulDown(uint256,uint256)uint256': bigint
  'rayMulUp(uint256,uint256)uint256': bigint
  'rayDivDown(uint256,uint256)uint256': bigint
  'rayDivUp(uint256,uint256)uint256': bigint
  'toWad(uint256)uint256': bigint
  'toRay(uint256)uint256': bigint
  'fromWadDown(uint256)uint256': bigint
  'fromRayUp(uint256)uint256': bigint
  'bpsToWad(uint256)uint256': bigint
  'bpsToRay(uint256)uint256': bigint
  'roundRayUp(uint256)uint256': bigint
}

/**
 * Defines the types of available calls and state of the WadRayMathWrapper smart contract.
 */
export type WadRayMathWrapperTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'WAD_DECIMALS()uint256' | 'WAD_DECIMALS', {
      argsObj: WadRayMathWrapperArgs['obj']['WAD_DECIMALS()uint256']
      argsTuple: WadRayMathWrapperArgs['tuple']['WAD_DECIMALS()uint256']
      returns: WadRayMathWrapperReturns['WAD_DECIMALS()uint256']
    }>
    & Record<'WAD()uint256' | 'WAD', {
      argsObj: WadRayMathWrapperArgs['obj']['WAD()uint256']
      argsTuple: WadRayMathWrapperArgs['tuple']['WAD()uint256']
      returns: WadRayMathWrapperReturns['WAD()uint256']
    }>
    & Record<'RAY()uint256' | 'RAY', {
      argsObj: WadRayMathWrapperArgs['obj']['RAY()uint256']
      argsTuple: WadRayMathWrapperArgs['tuple']['RAY()uint256']
      returns: WadRayMathWrapperReturns['RAY()uint256']
    }>
    & Record<'PERCENTAGE_FACTOR()uint256' | 'PERCENTAGE_FACTOR', {
      argsObj: WadRayMathWrapperArgs['obj']['PERCENTAGE_FACTOR()uint256']
      argsTuple: WadRayMathWrapperArgs['tuple']['PERCENTAGE_FACTOR()uint256']
      returns: WadRayMathWrapperReturns['PERCENTAGE_FACTOR()uint256']
    }>
    & Record<'wadMulDown(uint256,uint256)uint256' | 'wadMulDown', {
      argsObj: WadRayMathWrapperArgs['obj']['wadMulDown(uint256,uint256)uint256']
      argsTuple: WadRayMathWrapperArgs['tuple']['wadMulDown(uint256,uint256)uint256']
      returns: WadRayMathWrapperReturns['wadMulDown(uint256,uint256)uint256']
    }>
    & Record<'wadMulUp(uint256,uint256)uint256' | 'wadMulUp', {
      argsObj: WadRayMathWrapperArgs['obj']['wadMulUp(uint256,uint256)uint256']
      argsTuple: WadRayMathWrapperArgs['tuple']['wadMulUp(uint256,uint256)uint256']
      returns: WadRayMathWrapperReturns['wadMulUp(uint256,uint256)uint256']
    }>
    & Record<'wadDivDown(uint256,uint256)uint256' | 'wadDivDown', {
      argsObj: WadRayMathWrapperArgs['obj']['wadDivDown(uint256,uint256)uint256']
      argsTuple: WadRayMathWrapperArgs['tuple']['wadDivDown(uint256,uint256)uint256']
      returns: WadRayMathWrapperReturns['wadDivDown(uint256,uint256)uint256']
    }>
    & Record<'wadDivUp(uint256,uint256)uint256' | 'wadDivUp', {
      argsObj: WadRayMathWrapperArgs['obj']['wadDivUp(uint256,uint256)uint256']
      argsTuple: WadRayMathWrapperArgs['tuple']['wadDivUp(uint256,uint256)uint256']
      returns: WadRayMathWrapperReturns['wadDivUp(uint256,uint256)uint256']
    }>
    & Record<'rayMulDown(uint256,uint256)uint256' | 'rayMulDown', {
      argsObj: WadRayMathWrapperArgs['obj']['rayMulDown(uint256,uint256)uint256']
      argsTuple: WadRayMathWrapperArgs['tuple']['rayMulDown(uint256,uint256)uint256']
      returns: WadRayMathWrapperReturns['rayMulDown(uint256,uint256)uint256']
    }>
    & Record<'rayMulUp(uint256,uint256)uint256' | 'rayMulUp', {
      argsObj: WadRayMathWrapperArgs['obj']['rayMulUp(uint256,uint256)uint256']
      argsTuple: WadRayMathWrapperArgs['tuple']['rayMulUp(uint256,uint256)uint256']
      returns: WadRayMathWrapperReturns['rayMulUp(uint256,uint256)uint256']
    }>
    & Record<'rayDivDown(uint256,uint256)uint256' | 'rayDivDown', {
      argsObj: WadRayMathWrapperArgs['obj']['rayDivDown(uint256,uint256)uint256']
      argsTuple: WadRayMathWrapperArgs['tuple']['rayDivDown(uint256,uint256)uint256']
      returns: WadRayMathWrapperReturns['rayDivDown(uint256,uint256)uint256']
    }>
    & Record<'rayDivUp(uint256,uint256)uint256' | 'rayDivUp', {
      argsObj: WadRayMathWrapperArgs['obj']['rayDivUp(uint256,uint256)uint256']
      argsTuple: WadRayMathWrapperArgs['tuple']['rayDivUp(uint256,uint256)uint256']
      returns: WadRayMathWrapperReturns['rayDivUp(uint256,uint256)uint256']
    }>
    & Record<'toWad(uint256)uint256' | 'toWad', {
      argsObj: WadRayMathWrapperArgs['obj']['toWad(uint256)uint256']
      argsTuple: WadRayMathWrapperArgs['tuple']['toWad(uint256)uint256']
      returns: WadRayMathWrapperReturns['toWad(uint256)uint256']
    }>
    & Record<'toRay(uint256)uint256' | 'toRay', {
      argsObj: WadRayMathWrapperArgs['obj']['toRay(uint256)uint256']
      argsTuple: WadRayMathWrapperArgs['tuple']['toRay(uint256)uint256']
      returns: WadRayMathWrapperReturns['toRay(uint256)uint256']
    }>
    & Record<'fromWadDown(uint256)uint256' | 'fromWadDown', {
      argsObj: WadRayMathWrapperArgs['obj']['fromWadDown(uint256)uint256']
      argsTuple: WadRayMathWrapperArgs['tuple']['fromWadDown(uint256)uint256']
      returns: WadRayMathWrapperReturns['fromWadDown(uint256)uint256']
    }>
    & Record<'fromRayUp(uint256)uint256' | 'fromRayUp', {
      argsObj: WadRayMathWrapperArgs['obj']['fromRayUp(uint256)uint256']
      argsTuple: WadRayMathWrapperArgs['tuple']['fromRayUp(uint256)uint256']
      returns: WadRayMathWrapperReturns['fromRayUp(uint256)uint256']
    }>
    & Record<'bpsToWad(uint256)uint256' | 'bpsToWad', {
      argsObj: WadRayMathWrapperArgs['obj']['bpsToWad(uint256)uint256']
      argsTuple: WadRayMathWrapperArgs['tuple']['bpsToWad(uint256)uint256']
      returns: WadRayMathWrapperReturns['bpsToWad(uint256)uint256']
    }>
    & Record<'bpsToRay(uint256)uint256' | 'bpsToRay', {
      argsObj: WadRayMathWrapperArgs['obj']['bpsToRay(uint256)uint256']
      argsTuple: WadRayMathWrapperArgs['tuple']['bpsToRay(uint256)uint256']
      returns: WadRayMathWrapperReturns['bpsToRay(uint256)uint256']
    }>
    & Record<'roundRayUp(uint256)uint256' | 'roundRayUp', {
      argsObj: WadRayMathWrapperArgs['obj']['roundRayUp(uint256)uint256']
      argsTuple: WadRayMathWrapperArgs['tuple']['roundRayUp(uint256)uint256']
      returns: WadRayMathWrapperReturns['roundRayUp(uint256)uint256']
    }>
}

/**
 * Defines the possible abi call signatures.
 */
export type WadRayMathWrapperSignatures = keyof WadRayMathWrapperTypes['methods']
/**
 * Defines the possible abi call signatures for methods that return a non-void value.
 */
export type WadRayMathWrapperNonVoidMethodSignatures = keyof WadRayMathWrapperTypes['methods'] extends infer T ? T extends keyof WadRayMathWrapperTypes['methods'] ? MethodReturn<T> extends void ? never : T  : never : never
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the WadRayMathWrapper smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends WadRayMathWrapperSignatures> = WadRayMathWrapperTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the WadRayMathWrapper smart contract to the method's return type
 */
export type MethodReturn<TSignature extends WadRayMathWrapperSignatures> = WadRayMathWrapperTypes['methods'][TSignature]['returns']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the WadRayMathWrapper smart contract
 */
export abstract class WadRayMathWrapperParamsFactory {
  /**
   * Constructs a no op call for the WAD_DECIMALS()uint256 ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static wadDecimals(params: CallParams<WadRayMathWrapperArgs['obj']['WAD_DECIMALS()uint256'] | WadRayMathWrapperArgs['tuple']['WAD_DECIMALS()uint256']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'WAD_DECIMALS()uint256' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the WAD()uint256 ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static wad(params: CallParams<WadRayMathWrapperArgs['obj']['WAD()uint256'] | WadRayMathWrapperArgs['tuple']['WAD()uint256']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'WAD()uint256' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the RAY()uint256 ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static ray(params: CallParams<WadRayMathWrapperArgs['obj']['RAY()uint256'] | WadRayMathWrapperArgs['tuple']['RAY()uint256']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'RAY()uint256' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the PERCENTAGE_FACTOR()uint256 ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static percentageFactor(params: CallParams<WadRayMathWrapperArgs['obj']['PERCENTAGE_FACTOR()uint256'] | WadRayMathWrapperArgs['tuple']['PERCENTAGE_FACTOR()uint256']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'PERCENTAGE_FACTOR()uint256' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the wadMulDown(uint256,uint256)uint256 ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static wadMulDown(params: CallParams<WadRayMathWrapperArgs['obj']['wadMulDown(uint256,uint256)uint256'] | WadRayMathWrapperArgs['tuple']['wadMulDown(uint256,uint256)uint256']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'wadMulDown(uint256,uint256)uint256' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.__arc4A, params.args.__arc4B],
    }
  }
  /**
   * Constructs a no op call for the wadMulUp(uint256,uint256)uint256 ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static wadMulUp(params: CallParams<WadRayMathWrapperArgs['obj']['wadMulUp(uint256,uint256)uint256'] | WadRayMathWrapperArgs['tuple']['wadMulUp(uint256,uint256)uint256']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'wadMulUp(uint256,uint256)uint256' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.__arc4A, params.args.__arc4B],
    }
  }
  /**
   * Constructs a no op call for the wadDivDown(uint256,uint256)uint256 ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static wadDivDown(params: CallParams<WadRayMathWrapperArgs['obj']['wadDivDown(uint256,uint256)uint256'] | WadRayMathWrapperArgs['tuple']['wadDivDown(uint256,uint256)uint256']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'wadDivDown(uint256,uint256)uint256' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.__arc4A, params.args.__arc4B],
    }
  }
  /**
   * Constructs a no op call for the wadDivUp(uint256,uint256)uint256 ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static wadDivUp(params: CallParams<WadRayMathWrapperArgs['obj']['wadDivUp(uint256,uint256)uint256'] | WadRayMathWrapperArgs['tuple']['wadDivUp(uint256,uint256)uint256']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'wadDivUp(uint256,uint256)uint256' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.__arc4A, params.args.__arc4B],
    }
  }
  /**
   * Constructs a no op call for the rayMulDown(uint256,uint256)uint256 ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static rayMulDown(params: CallParams<WadRayMathWrapperArgs['obj']['rayMulDown(uint256,uint256)uint256'] | WadRayMathWrapperArgs['tuple']['rayMulDown(uint256,uint256)uint256']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'rayMulDown(uint256,uint256)uint256' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.__arc4A, params.args.__arc4B],
    }
  }
  /**
   * Constructs a no op call for the rayMulUp(uint256,uint256)uint256 ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static rayMulUp(params: CallParams<WadRayMathWrapperArgs['obj']['rayMulUp(uint256,uint256)uint256'] | WadRayMathWrapperArgs['tuple']['rayMulUp(uint256,uint256)uint256']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'rayMulUp(uint256,uint256)uint256' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.__arc4A, params.args.__arc4B],
    }
  }
  /**
   * Constructs a no op call for the rayDivDown(uint256,uint256)uint256 ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static rayDivDown(params: CallParams<WadRayMathWrapperArgs['obj']['rayDivDown(uint256,uint256)uint256'] | WadRayMathWrapperArgs['tuple']['rayDivDown(uint256,uint256)uint256']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'rayDivDown(uint256,uint256)uint256' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.__arc4A, params.args.__arc4B],
    }
  }
  /**
   * Constructs a no op call for the rayDivUp(uint256,uint256)uint256 ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static rayDivUp(params: CallParams<WadRayMathWrapperArgs['obj']['rayDivUp(uint256,uint256)uint256'] | WadRayMathWrapperArgs['tuple']['rayDivUp(uint256,uint256)uint256']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'rayDivUp(uint256,uint256)uint256' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.__arc4A, params.args.__arc4B],
    }
  }
  /**
   * Constructs a no op call for the toWad(uint256)uint256 ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static toWad(params: CallParams<WadRayMathWrapperArgs['obj']['toWad(uint256)uint256'] | WadRayMathWrapperArgs['tuple']['toWad(uint256)uint256']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'toWad(uint256)uint256' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.__arc4A],
    }
  }
  /**
   * Constructs a no op call for the toRay(uint256)uint256 ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static toRay(params: CallParams<WadRayMathWrapperArgs['obj']['toRay(uint256)uint256'] | WadRayMathWrapperArgs['tuple']['toRay(uint256)uint256']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'toRay(uint256)uint256' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.__arc4A],
    }
  }
  /**
   * Constructs a no op call for the fromWadDown(uint256)uint256 ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static fromWadDown(params: CallParams<WadRayMathWrapperArgs['obj']['fromWadDown(uint256)uint256'] | WadRayMathWrapperArgs['tuple']['fromWadDown(uint256)uint256']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'fromWadDown(uint256)uint256' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.__arc4A],
    }
  }
  /**
   * Constructs a no op call for the fromRayUp(uint256)uint256 ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static fromRayUp(params: CallParams<WadRayMathWrapperArgs['obj']['fromRayUp(uint256)uint256'] | WadRayMathWrapperArgs['tuple']['fromRayUp(uint256)uint256']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'fromRayUp(uint256)uint256' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.__arc4A],
    }
  }
  /**
   * Constructs a no op call for the bpsToWad(uint256)uint256 ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static bpsToWad(params: CallParams<WadRayMathWrapperArgs['obj']['bpsToWad(uint256)uint256'] | WadRayMathWrapperArgs['tuple']['bpsToWad(uint256)uint256']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'bpsToWad(uint256)uint256' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.__arc4A],
    }
  }
  /**
   * Constructs a no op call for the bpsToRay(uint256)uint256 ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static bpsToRay(params: CallParams<WadRayMathWrapperArgs['obj']['bpsToRay(uint256)uint256'] | WadRayMathWrapperArgs['tuple']['bpsToRay(uint256)uint256']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'bpsToRay(uint256)uint256' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.__arc4A],
    }
  }
  /**
   * Constructs a no op call for the roundRayUp(uint256)uint256 ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static roundRayUp(params: CallParams<WadRayMathWrapperArgs['obj']['roundRayUp(uint256)uint256'] | WadRayMathWrapperArgs['tuple']['roundRayUp(uint256)uint256']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'roundRayUp(uint256)uint256' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.__arc4A],
    }
  }
}

/**
 * A client to make calls to the WadRayMathWrapper smart contract
 */
export class WadRayMathWrapperClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `WadRayMathWrapperClient`
   *
   * @param appClient An `AppClient` instance which has been created with the WadRayMathWrapper app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `WadRayMathWrapperClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Checks for decode errors on the given return value and maps the return value to the return type for the given method
   * @returns The typed return value or undefined if there was no value
   */
  decodeReturnValue<TSignature extends WadRayMathWrapperNonVoidMethodSignatures>(method: TSignature, returnValue: ABIReturn | undefined) {
    return returnValue !== undefined ? getArc56ReturnValue<MethodReturn<TSignature>>(returnValue, this.appClient.getABIMethod(method), APP_SPEC.structs) : undefined
  }

  /**
   * Returns a new `WadRayMathWrapperClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<WadRayMathWrapperClient> {
    return new WadRayMathWrapperClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `WadRayMathWrapperClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<WadRayMathWrapperClient> {
    return new WadRayMathWrapperClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Makes a clear_state call to an existing instance of the WadRayMathWrapper smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the WadRayMathWrapper smart contract using the `WAD_DECIMALS()uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    wadDecimals: (params: CallParams<WadRayMathWrapperArgs['obj']['WAD_DECIMALS()uint256'] | WadRayMathWrapperArgs['tuple']['WAD_DECIMALS()uint256']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(WadRayMathWrapperParamsFactory.wadDecimals(params))
    },

    /**
     * Makes a call to the WadRayMathWrapper smart contract using the `WAD()uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    wad: (params: CallParams<WadRayMathWrapperArgs['obj']['WAD()uint256'] | WadRayMathWrapperArgs['tuple']['WAD()uint256']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(WadRayMathWrapperParamsFactory.wad(params))
    },

    /**
     * Makes a call to the WadRayMathWrapper smart contract using the `RAY()uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    ray: (params: CallParams<WadRayMathWrapperArgs['obj']['RAY()uint256'] | WadRayMathWrapperArgs['tuple']['RAY()uint256']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(WadRayMathWrapperParamsFactory.ray(params))
    },

    /**
     * Makes a call to the WadRayMathWrapper smart contract using the `PERCENTAGE_FACTOR()uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    percentageFactor: (params: CallParams<WadRayMathWrapperArgs['obj']['PERCENTAGE_FACTOR()uint256'] | WadRayMathWrapperArgs['tuple']['PERCENTAGE_FACTOR()uint256']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(WadRayMathWrapperParamsFactory.percentageFactor(params))
    },

    /**
     * Makes a call to the WadRayMathWrapper smart contract using the `wadMulDown(uint256,uint256)uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    wadMulDown: (params: CallParams<WadRayMathWrapperArgs['obj']['wadMulDown(uint256,uint256)uint256'] | WadRayMathWrapperArgs['tuple']['wadMulDown(uint256,uint256)uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(WadRayMathWrapperParamsFactory.wadMulDown(params))
    },

    /**
     * Makes a call to the WadRayMathWrapper smart contract using the `wadMulUp(uint256,uint256)uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    wadMulUp: (params: CallParams<WadRayMathWrapperArgs['obj']['wadMulUp(uint256,uint256)uint256'] | WadRayMathWrapperArgs['tuple']['wadMulUp(uint256,uint256)uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(WadRayMathWrapperParamsFactory.wadMulUp(params))
    },

    /**
     * Makes a call to the WadRayMathWrapper smart contract using the `wadDivDown(uint256,uint256)uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    wadDivDown: (params: CallParams<WadRayMathWrapperArgs['obj']['wadDivDown(uint256,uint256)uint256'] | WadRayMathWrapperArgs['tuple']['wadDivDown(uint256,uint256)uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(WadRayMathWrapperParamsFactory.wadDivDown(params))
    },

    /**
     * Makes a call to the WadRayMathWrapper smart contract using the `wadDivUp(uint256,uint256)uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    wadDivUp: (params: CallParams<WadRayMathWrapperArgs['obj']['wadDivUp(uint256,uint256)uint256'] | WadRayMathWrapperArgs['tuple']['wadDivUp(uint256,uint256)uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(WadRayMathWrapperParamsFactory.wadDivUp(params))
    },

    /**
     * Makes a call to the WadRayMathWrapper smart contract using the `rayMulDown(uint256,uint256)uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    rayMulDown: (params: CallParams<WadRayMathWrapperArgs['obj']['rayMulDown(uint256,uint256)uint256'] | WadRayMathWrapperArgs['tuple']['rayMulDown(uint256,uint256)uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(WadRayMathWrapperParamsFactory.rayMulDown(params))
    },

    /**
     * Makes a call to the WadRayMathWrapper smart contract using the `rayMulUp(uint256,uint256)uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    rayMulUp: (params: CallParams<WadRayMathWrapperArgs['obj']['rayMulUp(uint256,uint256)uint256'] | WadRayMathWrapperArgs['tuple']['rayMulUp(uint256,uint256)uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(WadRayMathWrapperParamsFactory.rayMulUp(params))
    },

    /**
     * Makes a call to the WadRayMathWrapper smart contract using the `rayDivDown(uint256,uint256)uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    rayDivDown: (params: CallParams<WadRayMathWrapperArgs['obj']['rayDivDown(uint256,uint256)uint256'] | WadRayMathWrapperArgs['tuple']['rayDivDown(uint256,uint256)uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(WadRayMathWrapperParamsFactory.rayDivDown(params))
    },

    /**
     * Makes a call to the WadRayMathWrapper smart contract using the `rayDivUp(uint256,uint256)uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    rayDivUp: (params: CallParams<WadRayMathWrapperArgs['obj']['rayDivUp(uint256,uint256)uint256'] | WadRayMathWrapperArgs['tuple']['rayDivUp(uint256,uint256)uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(WadRayMathWrapperParamsFactory.rayDivUp(params))
    },

    /**
     * Makes a call to the WadRayMathWrapper smart contract using the `toWad(uint256)uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    toWad: (params: CallParams<WadRayMathWrapperArgs['obj']['toWad(uint256)uint256'] | WadRayMathWrapperArgs['tuple']['toWad(uint256)uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(WadRayMathWrapperParamsFactory.toWad(params))
    },

    /**
     * Makes a call to the WadRayMathWrapper smart contract using the `toRay(uint256)uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    toRay: (params: CallParams<WadRayMathWrapperArgs['obj']['toRay(uint256)uint256'] | WadRayMathWrapperArgs['tuple']['toRay(uint256)uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(WadRayMathWrapperParamsFactory.toRay(params))
    },

    /**
     * Makes a call to the WadRayMathWrapper smart contract using the `fromWadDown(uint256)uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    fromWadDown: (params: CallParams<WadRayMathWrapperArgs['obj']['fromWadDown(uint256)uint256'] | WadRayMathWrapperArgs['tuple']['fromWadDown(uint256)uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(WadRayMathWrapperParamsFactory.fromWadDown(params))
    },

    /**
     * Makes a call to the WadRayMathWrapper smart contract using the `fromRayUp(uint256)uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    fromRayUp: (params: CallParams<WadRayMathWrapperArgs['obj']['fromRayUp(uint256)uint256'] | WadRayMathWrapperArgs['tuple']['fromRayUp(uint256)uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(WadRayMathWrapperParamsFactory.fromRayUp(params))
    },

    /**
     * Makes a call to the WadRayMathWrapper smart contract using the `bpsToWad(uint256)uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    bpsToWad: (params: CallParams<WadRayMathWrapperArgs['obj']['bpsToWad(uint256)uint256'] | WadRayMathWrapperArgs['tuple']['bpsToWad(uint256)uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(WadRayMathWrapperParamsFactory.bpsToWad(params))
    },

    /**
     * Makes a call to the WadRayMathWrapper smart contract using the `bpsToRay(uint256)uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    bpsToRay: (params: CallParams<WadRayMathWrapperArgs['obj']['bpsToRay(uint256)uint256'] | WadRayMathWrapperArgs['tuple']['bpsToRay(uint256)uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(WadRayMathWrapperParamsFactory.bpsToRay(params))
    },

    /**
     * Makes a call to the WadRayMathWrapper smart contract using the `roundRayUp(uint256)uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    roundRayUp: (params: CallParams<WadRayMathWrapperArgs['obj']['roundRayUp(uint256)uint256'] | WadRayMathWrapperArgs['tuple']['roundRayUp(uint256)uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(WadRayMathWrapperParamsFactory.roundRayUp(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Makes a clear_state call to an existing instance of the WadRayMathWrapper smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the WadRayMathWrapper smart contract using the `WAD_DECIMALS()uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    wadDecimals: (params: CallParams<WadRayMathWrapperArgs['obj']['WAD_DECIMALS()uint256'] | WadRayMathWrapperArgs['tuple']['WAD_DECIMALS()uint256']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(WadRayMathWrapperParamsFactory.wadDecimals(params))
    },

    /**
     * Makes a call to the WadRayMathWrapper smart contract using the `WAD()uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    wad: (params: CallParams<WadRayMathWrapperArgs['obj']['WAD()uint256'] | WadRayMathWrapperArgs['tuple']['WAD()uint256']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(WadRayMathWrapperParamsFactory.wad(params))
    },

    /**
     * Makes a call to the WadRayMathWrapper smart contract using the `RAY()uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    ray: (params: CallParams<WadRayMathWrapperArgs['obj']['RAY()uint256'] | WadRayMathWrapperArgs['tuple']['RAY()uint256']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(WadRayMathWrapperParamsFactory.ray(params))
    },

    /**
     * Makes a call to the WadRayMathWrapper smart contract using the `PERCENTAGE_FACTOR()uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    percentageFactor: (params: CallParams<WadRayMathWrapperArgs['obj']['PERCENTAGE_FACTOR()uint256'] | WadRayMathWrapperArgs['tuple']['PERCENTAGE_FACTOR()uint256']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(WadRayMathWrapperParamsFactory.percentageFactor(params))
    },

    /**
     * Makes a call to the WadRayMathWrapper smart contract using the `wadMulDown(uint256,uint256)uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    wadMulDown: (params: CallParams<WadRayMathWrapperArgs['obj']['wadMulDown(uint256,uint256)uint256'] | WadRayMathWrapperArgs['tuple']['wadMulDown(uint256,uint256)uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(WadRayMathWrapperParamsFactory.wadMulDown(params))
    },

    /**
     * Makes a call to the WadRayMathWrapper smart contract using the `wadMulUp(uint256,uint256)uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    wadMulUp: (params: CallParams<WadRayMathWrapperArgs['obj']['wadMulUp(uint256,uint256)uint256'] | WadRayMathWrapperArgs['tuple']['wadMulUp(uint256,uint256)uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(WadRayMathWrapperParamsFactory.wadMulUp(params))
    },

    /**
     * Makes a call to the WadRayMathWrapper smart contract using the `wadDivDown(uint256,uint256)uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    wadDivDown: (params: CallParams<WadRayMathWrapperArgs['obj']['wadDivDown(uint256,uint256)uint256'] | WadRayMathWrapperArgs['tuple']['wadDivDown(uint256,uint256)uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(WadRayMathWrapperParamsFactory.wadDivDown(params))
    },

    /**
     * Makes a call to the WadRayMathWrapper smart contract using the `wadDivUp(uint256,uint256)uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    wadDivUp: (params: CallParams<WadRayMathWrapperArgs['obj']['wadDivUp(uint256,uint256)uint256'] | WadRayMathWrapperArgs['tuple']['wadDivUp(uint256,uint256)uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(WadRayMathWrapperParamsFactory.wadDivUp(params))
    },

    /**
     * Makes a call to the WadRayMathWrapper smart contract using the `rayMulDown(uint256,uint256)uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    rayMulDown: (params: CallParams<WadRayMathWrapperArgs['obj']['rayMulDown(uint256,uint256)uint256'] | WadRayMathWrapperArgs['tuple']['rayMulDown(uint256,uint256)uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(WadRayMathWrapperParamsFactory.rayMulDown(params))
    },

    /**
     * Makes a call to the WadRayMathWrapper smart contract using the `rayMulUp(uint256,uint256)uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    rayMulUp: (params: CallParams<WadRayMathWrapperArgs['obj']['rayMulUp(uint256,uint256)uint256'] | WadRayMathWrapperArgs['tuple']['rayMulUp(uint256,uint256)uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(WadRayMathWrapperParamsFactory.rayMulUp(params))
    },

    /**
     * Makes a call to the WadRayMathWrapper smart contract using the `rayDivDown(uint256,uint256)uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    rayDivDown: (params: CallParams<WadRayMathWrapperArgs['obj']['rayDivDown(uint256,uint256)uint256'] | WadRayMathWrapperArgs['tuple']['rayDivDown(uint256,uint256)uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(WadRayMathWrapperParamsFactory.rayDivDown(params))
    },

    /**
     * Makes a call to the WadRayMathWrapper smart contract using the `rayDivUp(uint256,uint256)uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    rayDivUp: (params: CallParams<WadRayMathWrapperArgs['obj']['rayDivUp(uint256,uint256)uint256'] | WadRayMathWrapperArgs['tuple']['rayDivUp(uint256,uint256)uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(WadRayMathWrapperParamsFactory.rayDivUp(params))
    },

    /**
     * Makes a call to the WadRayMathWrapper smart contract using the `toWad(uint256)uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    toWad: (params: CallParams<WadRayMathWrapperArgs['obj']['toWad(uint256)uint256'] | WadRayMathWrapperArgs['tuple']['toWad(uint256)uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(WadRayMathWrapperParamsFactory.toWad(params))
    },

    /**
     * Makes a call to the WadRayMathWrapper smart contract using the `toRay(uint256)uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    toRay: (params: CallParams<WadRayMathWrapperArgs['obj']['toRay(uint256)uint256'] | WadRayMathWrapperArgs['tuple']['toRay(uint256)uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(WadRayMathWrapperParamsFactory.toRay(params))
    },

    /**
     * Makes a call to the WadRayMathWrapper smart contract using the `fromWadDown(uint256)uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    fromWadDown: (params: CallParams<WadRayMathWrapperArgs['obj']['fromWadDown(uint256)uint256'] | WadRayMathWrapperArgs['tuple']['fromWadDown(uint256)uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(WadRayMathWrapperParamsFactory.fromWadDown(params))
    },

    /**
     * Makes a call to the WadRayMathWrapper smart contract using the `fromRayUp(uint256)uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    fromRayUp: (params: CallParams<WadRayMathWrapperArgs['obj']['fromRayUp(uint256)uint256'] | WadRayMathWrapperArgs['tuple']['fromRayUp(uint256)uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(WadRayMathWrapperParamsFactory.fromRayUp(params))
    },

    /**
     * Makes a call to the WadRayMathWrapper smart contract using the `bpsToWad(uint256)uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    bpsToWad: (params: CallParams<WadRayMathWrapperArgs['obj']['bpsToWad(uint256)uint256'] | WadRayMathWrapperArgs['tuple']['bpsToWad(uint256)uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(WadRayMathWrapperParamsFactory.bpsToWad(params))
    },

    /**
     * Makes a call to the WadRayMathWrapper smart contract using the `bpsToRay(uint256)uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    bpsToRay: (params: CallParams<WadRayMathWrapperArgs['obj']['bpsToRay(uint256)uint256'] | WadRayMathWrapperArgs['tuple']['bpsToRay(uint256)uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(WadRayMathWrapperParamsFactory.bpsToRay(params))
    },

    /**
     * Makes a call to the WadRayMathWrapper smart contract using the `roundRayUp(uint256)uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    roundRayUp: (params: CallParams<WadRayMathWrapperArgs['obj']['roundRayUp(uint256)uint256'] | WadRayMathWrapperArgs['tuple']['roundRayUp(uint256)uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(WadRayMathWrapperParamsFactory.roundRayUp(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Makes a clear_state call to an existing instance of the WadRayMathWrapper smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the WadRayMathWrapper smart contract using the `WAD_DECIMALS()uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    wadDecimals: async (params: CallParams<WadRayMathWrapperArgs['obj']['WAD_DECIMALS()uint256'] | WadRayMathWrapperArgs['tuple']['WAD_DECIMALS()uint256']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(WadRayMathWrapperParamsFactory.wadDecimals(params))
      return {...result, return: result.return as unknown as (undefined | WadRayMathWrapperReturns['WAD_DECIMALS()uint256'])}
    },

    /**
     * Makes a call to the WadRayMathWrapper smart contract using the `WAD()uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    wad: async (params: CallParams<WadRayMathWrapperArgs['obj']['WAD()uint256'] | WadRayMathWrapperArgs['tuple']['WAD()uint256']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(WadRayMathWrapperParamsFactory.wad(params))
      return {...result, return: result.return as unknown as (undefined | WadRayMathWrapperReturns['WAD()uint256'])}
    },

    /**
     * Makes a call to the WadRayMathWrapper smart contract using the `RAY()uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    ray: async (params: CallParams<WadRayMathWrapperArgs['obj']['RAY()uint256'] | WadRayMathWrapperArgs['tuple']['RAY()uint256']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(WadRayMathWrapperParamsFactory.ray(params))
      return {...result, return: result.return as unknown as (undefined | WadRayMathWrapperReturns['RAY()uint256'])}
    },

    /**
     * Makes a call to the WadRayMathWrapper smart contract using the `PERCENTAGE_FACTOR()uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    percentageFactor: async (params: CallParams<WadRayMathWrapperArgs['obj']['PERCENTAGE_FACTOR()uint256'] | WadRayMathWrapperArgs['tuple']['PERCENTAGE_FACTOR()uint256']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(WadRayMathWrapperParamsFactory.percentageFactor(params))
      return {...result, return: result.return as unknown as (undefined | WadRayMathWrapperReturns['PERCENTAGE_FACTOR()uint256'])}
    },

    /**
     * Makes a call to the WadRayMathWrapper smart contract using the `wadMulDown(uint256,uint256)uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    wadMulDown: async (params: CallParams<WadRayMathWrapperArgs['obj']['wadMulDown(uint256,uint256)uint256'] | WadRayMathWrapperArgs['tuple']['wadMulDown(uint256,uint256)uint256']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(WadRayMathWrapperParamsFactory.wadMulDown(params))
      return {...result, return: result.return as unknown as (undefined | WadRayMathWrapperReturns['wadMulDown(uint256,uint256)uint256'])}
    },

    /**
     * Makes a call to the WadRayMathWrapper smart contract using the `wadMulUp(uint256,uint256)uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    wadMulUp: async (params: CallParams<WadRayMathWrapperArgs['obj']['wadMulUp(uint256,uint256)uint256'] | WadRayMathWrapperArgs['tuple']['wadMulUp(uint256,uint256)uint256']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(WadRayMathWrapperParamsFactory.wadMulUp(params))
      return {...result, return: result.return as unknown as (undefined | WadRayMathWrapperReturns['wadMulUp(uint256,uint256)uint256'])}
    },

    /**
     * Makes a call to the WadRayMathWrapper smart contract using the `wadDivDown(uint256,uint256)uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    wadDivDown: async (params: CallParams<WadRayMathWrapperArgs['obj']['wadDivDown(uint256,uint256)uint256'] | WadRayMathWrapperArgs['tuple']['wadDivDown(uint256,uint256)uint256']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(WadRayMathWrapperParamsFactory.wadDivDown(params))
      return {...result, return: result.return as unknown as (undefined | WadRayMathWrapperReturns['wadDivDown(uint256,uint256)uint256'])}
    },

    /**
     * Makes a call to the WadRayMathWrapper smart contract using the `wadDivUp(uint256,uint256)uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    wadDivUp: async (params: CallParams<WadRayMathWrapperArgs['obj']['wadDivUp(uint256,uint256)uint256'] | WadRayMathWrapperArgs['tuple']['wadDivUp(uint256,uint256)uint256']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(WadRayMathWrapperParamsFactory.wadDivUp(params))
      return {...result, return: result.return as unknown as (undefined | WadRayMathWrapperReturns['wadDivUp(uint256,uint256)uint256'])}
    },

    /**
     * Makes a call to the WadRayMathWrapper smart contract using the `rayMulDown(uint256,uint256)uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    rayMulDown: async (params: CallParams<WadRayMathWrapperArgs['obj']['rayMulDown(uint256,uint256)uint256'] | WadRayMathWrapperArgs['tuple']['rayMulDown(uint256,uint256)uint256']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(WadRayMathWrapperParamsFactory.rayMulDown(params))
      return {...result, return: result.return as unknown as (undefined | WadRayMathWrapperReturns['rayMulDown(uint256,uint256)uint256'])}
    },

    /**
     * Makes a call to the WadRayMathWrapper smart contract using the `rayMulUp(uint256,uint256)uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    rayMulUp: async (params: CallParams<WadRayMathWrapperArgs['obj']['rayMulUp(uint256,uint256)uint256'] | WadRayMathWrapperArgs['tuple']['rayMulUp(uint256,uint256)uint256']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(WadRayMathWrapperParamsFactory.rayMulUp(params))
      return {...result, return: result.return as unknown as (undefined | WadRayMathWrapperReturns['rayMulUp(uint256,uint256)uint256'])}
    },

    /**
     * Makes a call to the WadRayMathWrapper smart contract using the `rayDivDown(uint256,uint256)uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    rayDivDown: async (params: CallParams<WadRayMathWrapperArgs['obj']['rayDivDown(uint256,uint256)uint256'] | WadRayMathWrapperArgs['tuple']['rayDivDown(uint256,uint256)uint256']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(WadRayMathWrapperParamsFactory.rayDivDown(params))
      return {...result, return: result.return as unknown as (undefined | WadRayMathWrapperReturns['rayDivDown(uint256,uint256)uint256'])}
    },

    /**
     * Makes a call to the WadRayMathWrapper smart contract using the `rayDivUp(uint256,uint256)uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    rayDivUp: async (params: CallParams<WadRayMathWrapperArgs['obj']['rayDivUp(uint256,uint256)uint256'] | WadRayMathWrapperArgs['tuple']['rayDivUp(uint256,uint256)uint256']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(WadRayMathWrapperParamsFactory.rayDivUp(params))
      return {...result, return: result.return as unknown as (undefined | WadRayMathWrapperReturns['rayDivUp(uint256,uint256)uint256'])}
    },

    /**
     * Makes a call to the WadRayMathWrapper smart contract using the `toWad(uint256)uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    toWad: async (params: CallParams<WadRayMathWrapperArgs['obj']['toWad(uint256)uint256'] | WadRayMathWrapperArgs['tuple']['toWad(uint256)uint256']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(WadRayMathWrapperParamsFactory.toWad(params))
      return {...result, return: result.return as unknown as (undefined | WadRayMathWrapperReturns['toWad(uint256)uint256'])}
    },

    /**
     * Makes a call to the WadRayMathWrapper smart contract using the `toRay(uint256)uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    toRay: async (params: CallParams<WadRayMathWrapperArgs['obj']['toRay(uint256)uint256'] | WadRayMathWrapperArgs['tuple']['toRay(uint256)uint256']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(WadRayMathWrapperParamsFactory.toRay(params))
      return {...result, return: result.return as unknown as (undefined | WadRayMathWrapperReturns['toRay(uint256)uint256'])}
    },

    /**
     * Makes a call to the WadRayMathWrapper smart contract using the `fromWadDown(uint256)uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    fromWadDown: async (params: CallParams<WadRayMathWrapperArgs['obj']['fromWadDown(uint256)uint256'] | WadRayMathWrapperArgs['tuple']['fromWadDown(uint256)uint256']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(WadRayMathWrapperParamsFactory.fromWadDown(params))
      return {...result, return: result.return as unknown as (undefined | WadRayMathWrapperReturns['fromWadDown(uint256)uint256'])}
    },

    /**
     * Makes a call to the WadRayMathWrapper smart contract using the `fromRayUp(uint256)uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    fromRayUp: async (params: CallParams<WadRayMathWrapperArgs['obj']['fromRayUp(uint256)uint256'] | WadRayMathWrapperArgs['tuple']['fromRayUp(uint256)uint256']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(WadRayMathWrapperParamsFactory.fromRayUp(params))
      return {...result, return: result.return as unknown as (undefined | WadRayMathWrapperReturns['fromRayUp(uint256)uint256'])}
    },

    /**
     * Makes a call to the WadRayMathWrapper smart contract using the `bpsToWad(uint256)uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    bpsToWad: async (params: CallParams<WadRayMathWrapperArgs['obj']['bpsToWad(uint256)uint256'] | WadRayMathWrapperArgs['tuple']['bpsToWad(uint256)uint256']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(WadRayMathWrapperParamsFactory.bpsToWad(params))
      return {...result, return: result.return as unknown as (undefined | WadRayMathWrapperReturns['bpsToWad(uint256)uint256'])}
    },

    /**
     * Makes a call to the WadRayMathWrapper smart contract using the `bpsToRay(uint256)uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    bpsToRay: async (params: CallParams<WadRayMathWrapperArgs['obj']['bpsToRay(uint256)uint256'] | WadRayMathWrapperArgs['tuple']['bpsToRay(uint256)uint256']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(WadRayMathWrapperParamsFactory.bpsToRay(params))
      return {...result, return: result.return as unknown as (undefined | WadRayMathWrapperReturns['bpsToRay(uint256)uint256'])}
    },

    /**
     * Makes a call to the WadRayMathWrapper smart contract using the `roundRayUp(uint256)uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    roundRayUp: async (params: CallParams<WadRayMathWrapperArgs['obj']['roundRayUp(uint256)uint256'] | WadRayMathWrapperArgs['tuple']['roundRayUp(uint256)uint256']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(WadRayMathWrapperParamsFactory.roundRayUp(params))
      return {...result, return: result.return as unknown as (undefined | WadRayMathWrapperReturns['roundRayUp(uint256)uint256'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new WadRayMathWrapperClient(this.appClient.clone(params))
  }

  /**
   * Makes a readonly (simulated) call to the WadRayMathWrapper smart contract using the `WAD_DECIMALS()uint256` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async wadDecimals(params: CallParams<WadRayMathWrapperArgs['obj']['WAD_DECIMALS()uint256'] | WadRayMathWrapperArgs['tuple']['WAD_DECIMALS()uint256']> = {args: []}) {
    const result = await this.appClient.send.call(WadRayMathWrapperParamsFactory.wadDecimals(params))
    return result.return as unknown as WadRayMathWrapperReturns['WAD_DECIMALS()uint256']
  }

  /**
   * Makes a readonly (simulated) call to the WadRayMathWrapper smart contract using the `WAD()uint256` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async wad(params: CallParams<WadRayMathWrapperArgs['obj']['WAD()uint256'] | WadRayMathWrapperArgs['tuple']['WAD()uint256']> = {args: []}) {
    const result = await this.appClient.send.call(WadRayMathWrapperParamsFactory.wad(params))
    return result.return as unknown as WadRayMathWrapperReturns['WAD()uint256']
  }

  /**
   * Makes a readonly (simulated) call to the WadRayMathWrapper smart contract using the `RAY()uint256` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async ray(params: CallParams<WadRayMathWrapperArgs['obj']['RAY()uint256'] | WadRayMathWrapperArgs['tuple']['RAY()uint256']> = {args: []}) {
    const result = await this.appClient.send.call(WadRayMathWrapperParamsFactory.ray(params))
    return result.return as unknown as WadRayMathWrapperReturns['RAY()uint256']
  }

  /**
   * Makes a readonly (simulated) call to the WadRayMathWrapper smart contract using the `PERCENTAGE_FACTOR()uint256` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async percentageFactor(params: CallParams<WadRayMathWrapperArgs['obj']['PERCENTAGE_FACTOR()uint256'] | WadRayMathWrapperArgs['tuple']['PERCENTAGE_FACTOR()uint256']> = {args: []}) {
    const result = await this.appClient.send.call(WadRayMathWrapperParamsFactory.percentageFactor(params))
    return result.return as unknown as WadRayMathWrapperReturns['PERCENTAGE_FACTOR()uint256']
  }

  /**
   * Makes a readonly (simulated) call to the WadRayMathWrapper smart contract using the `wadMulDown(uint256,uint256)uint256` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async wadMulDown(params: CallParams<WadRayMathWrapperArgs['obj']['wadMulDown(uint256,uint256)uint256'] | WadRayMathWrapperArgs['tuple']['wadMulDown(uint256,uint256)uint256']>) {
    const result = await this.appClient.send.call(WadRayMathWrapperParamsFactory.wadMulDown(params))
    return result.return as unknown as WadRayMathWrapperReturns['wadMulDown(uint256,uint256)uint256']
  }

  /**
   * Makes a readonly (simulated) call to the WadRayMathWrapper smart contract using the `wadMulUp(uint256,uint256)uint256` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async wadMulUp(params: CallParams<WadRayMathWrapperArgs['obj']['wadMulUp(uint256,uint256)uint256'] | WadRayMathWrapperArgs['tuple']['wadMulUp(uint256,uint256)uint256']>) {
    const result = await this.appClient.send.call(WadRayMathWrapperParamsFactory.wadMulUp(params))
    return result.return as unknown as WadRayMathWrapperReturns['wadMulUp(uint256,uint256)uint256']
  }

  /**
   * Makes a readonly (simulated) call to the WadRayMathWrapper smart contract using the `wadDivDown(uint256,uint256)uint256` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async wadDivDown(params: CallParams<WadRayMathWrapperArgs['obj']['wadDivDown(uint256,uint256)uint256'] | WadRayMathWrapperArgs['tuple']['wadDivDown(uint256,uint256)uint256']>) {
    const result = await this.appClient.send.call(WadRayMathWrapperParamsFactory.wadDivDown(params))
    return result.return as unknown as WadRayMathWrapperReturns['wadDivDown(uint256,uint256)uint256']
  }

  /**
   * Makes a readonly (simulated) call to the WadRayMathWrapper smart contract using the `wadDivUp(uint256,uint256)uint256` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async wadDivUp(params: CallParams<WadRayMathWrapperArgs['obj']['wadDivUp(uint256,uint256)uint256'] | WadRayMathWrapperArgs['tuple']['wadDivUp(uint256,uint256)uint256']>) {
    const result = await this.appClient.send.call(WadRayMathWrapperParamsFactory.wadDivUp(params))
    return result.return as unknown as WadRayMathWrapperReturns['wadDivUp(uint256,uint256)uint256']
  }

  /**
   * Makes a readonly (simulated) call to the WadRayMathWrapper smart contract using the `rayMulDown(uint256,uint256)uint256` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async rayMulDown(params: CallParams<WadRayMathWrapperArgs['obj']['rayMulDown(uint256,uint256)uint256'] | WadRayMathWrapperArgs['tuple']['rayMulDown(uint256,uint256)uint256']>) {
    const result = await this.appClient.send.call(WadRayMathWrapperParamsFactory.rayMulDown(params))
    return result.return as unknown as WadRayMathWrapperReturns['rayMulDown(uint256,uint256)uint256']
  }

  /**
   * Makes a readonly (simulated) call to the WadRayMathWrapper smart contract using the `rayMulUp(uint256,uint256)uint256` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async rayMulUp(params: CallParams<WadRayMathWrapperArgs['obj']['rayMulUp(uint256,uint256)uint256'] | WadRayMathWrapperArgs['tuple']['rayMulUp(uint256,uint256)uint256']>) {
    const result = await this.appClient.send.call(WadRayMathWrapperParamsFactory.rayMulUp(params))
    return result.return as unknown as WadRayMathWrapperReturns['rayMulUp(uint256,uint256)uint256']
  }

  /**
   * Makes a readonly (simulated) call to the WadRayMathWrapper smart contract using the `rayDivDown(uint256,uint256)uint256` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async rayDivDown(params: CallParams<WadRayMathWrapperArgs['obj']['rayDivDown(uint256,uint256)uint256'] | WadRayMathWrapperArgs['tuple']['rayDivDown(uint256,uint256)uint256']>) {
    const result = await this.appClient.send.call(WadRayMathWrapperParamsFactory.rayDivDown(params))
    return result.return as unknown as WadRayMathWrapperReturns['rayDivDown(uint256,uint256)uint256']
  }

  /**
   * Makes a readonly (simulated) call to the WadRayMathWrapper smart contract using the `rayDivUp(uint256,uint256)uint256` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async rayDivUp(params: CallParams<WadRayMathWrapperArgs['obj']['rayDivUp(uint256,uint256)uint256'] | WadRayMathWrapperArgs['tuple']['rayDivUp(uint256,uint256)uint256']>) {
    const result = await this.appClient.send.call(WadRayMathWrapperParamsFactory.rayDivUp(params))
    return result.return as unknown as WadRayMathWrapperReturns['rayDivUp(uint256,uint256)uint256']
  }

  /**
   * Makes a readonly (simulated) call to the WadRayMathWrapper smart contract using the `toWad(uint256)uint256` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async toWad(params: CallParams<WadRayMathWrapperArgs['obj']['toWad(uint256)uint256'] | WadRayMathWrapperArgs['tuple']['toWad(uint256)uint256']>) {
    const result = await this.appClient.send.call(WadRayMathWrapperParamsFactory.toWad(params))
    return result.return as unknown as WadRayMathWrapperReturns['toWad(uint256)uint256']
  }

  /**
   * Makes a readonly (simulated) call to the WadRayMathWrapper smart contract using the `toRay(uint256)uint256` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async toRay(params: CallParams<WadRayMathWrapperArgs['obj']['toRay(uint256)uint256'] | WadRayMathWrapperArgs['tuple']['toRay(uint256)uint256']>) {
    const result = await this.appClient.send.call(WadRayMathWrapperParamsFactory.toRay(params))
    return result.return as unknown as WadRayMathWrapperReturns['toRay(uint256)uint256']
  }

  /**
   * Makes a readonly (simulated) call to the WadRayMathWrapper smart contract using the `fromWadDown(uint256)uint256` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async fromWadDown(params: CallParams<WadRayMathWrapperArgs['obj']['fromWadDown(uint256)uint256'] | WadRayMathWrapperArgs['tuple']['fromWadDown(uint256)uint256']>) {
    const result = await this.appClient.send.call(WadRayMathWrapperParamsFactory.fromWadDown(params))
    return result.return as unknown as WadRayMathWrapperReturns['fromWadDown(uint256)uint256']
  }

  /**
   * Makes a readonly (simulated) call to the WadRayMathWrapper smart contract using the `fromRayUp(uint256)uint256` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async fromRayUp(params: CallParams<WadRayMathWrapperArgs['obj']['fromRayUp(uint256)uint256'] | WadRayMathWrapperArgs['tuple']['fromRayUp(uint256)uint256']>) {
    const result = await this.appClient.send.call(WadRayMathWrapperParamsFactory.fromRayUp(params))
    return result.return as unknown as WadRayMathWrapperReturns['fromRayUp(uint256)uint256']
  }

  /**
   * Makes a readonly (simulated) call to the WadRayMathWrapper smart contract using the `bpsToWad(uint256)uint256` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async bpsToWad(params: CallParams<WadRayMathWrapperArgs['obj']['bpsToWad(uint256)uint256'] | WadRayMathWrapperArgs['tuple']['bpsToWad(uint256)uint256']>) {
    const result = await this.appClient.send.call(WadRayMathWrapperParamsFactory.bpsToWad(params))
    return result.return as unknown as WadRayMathWrapperReturns['bpsToWad(uint256)uint256']
  }

  /**
   * Makes a readonly (simulated) call to the WadRayMathWrapper smart contract using the `bpsToRay(uint256)uint256` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async bpsToRay(params: CallParams<WadRayMathWrapperArgs['obj']['bpsToRay(uint256)uint256'] | WadRayMathWrapperArgs['tuple']['bpsToRay(uint256)uint256']>) {
    const result = await this.appClient.send.call(WadRayMathWrapperParamsFactory.bpsToRay(params))
    return result.return as unknown as WadRayMathWrapperReturns['bpsToRay(uint256)uint256']
  }

  /**
   * Makes a readonly (simulated) call to the WadRayMathWrapper smart contract using the `roundRayUp(uint256)uint256` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async roundRayUp(params: CallParams<WadRayMathWrapperArgs['obj']['roundRayUp(uint256)uint256'] | WadRayMathWrapperArgs['tuple']['roundRayUp(uint256)uint256']>) {
    const result = await this.appClient.send.call(WadRayMathWrapperParamsFactory.roundRayUp(params))
    return result.return as unknown as WadRayMathWrapperReturns['roundRayUp(uint256)uint256']
  }

  /**
   * Methods to access state for the current WadRayMathWrapper app
   */
  state = {
  }

  public newGroup(): WadRayMathWrapperComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a WAD_DECIMALS()uint256 method call against the WadRayMathWrapper contract
       */
      wadDecimals(params: CallParams<WadRayMathWrapperArgs['obj']['WAD_DECIMALS()uint256'] | WadRayMathWrapperArgs['tuple']['WAD_DECIMALS()uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.wadDecimals(params)))
        resultMappers.push((v) => client.decodeReturnValue('WAD_DECIMALS()uint256', v))
        return this
      },
      /**
       * Add a WAD()uint256 method call against the WadRayMathWrapper contract
       */
      wad(params: CallParams<WadRayMathWrapperArgs['obj']['WAD()uint256'] | WadRayMathWrapperArgs['tuple']['WAD()uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.wad(params)))
        resultMappers.push((v) => client.decodeReturnValue('WAD()uint256', v))
        return this
      },
      /**
       * Add a RAY()uint256 method call against the WadRayMathWrapper contract
       */
      ray(params: CallParams<WadRayMathWrapperArgs['obj']['RAY()uint256'] | WadRayMathWrapperArgs['tuple']['RAY()uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.ray(params)))
        resultMappers.push((v) => client.decodeReturnValue('RAY()uint256', v))
        return this
      },
      /**
       * Add a PERCENTAGE_FACTOR()uint256 method call against the WadRayMathWrapper contract
       */
      percentageFactor(params: CallParams<WadRayMathWrapperArgs['obj']['PERCENTAGE_FACTOR()uint256'] | WadRayMathWrapperArgs['tuple']['PERCENTAGE_FACTOR()uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.percentageFactor(params)))
        resultMappers.push((v) => client.decodeReturnValue('PERCENTAGE_FACTOR()uint256', v))
        return this
      },
      /**
       * Add a wadMulDown(uint256,uint256)uint256 method call against the WadRayMathWrapper contract
       */
      wadMulDown(params: CallParams<WadRayMathWrapperArgs['obj']['wadMulDown(uint256,uint256)uint256'] | WadRayMathWrapperArgs['tuple']['wadMulDown(uint256,uint256)uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.wadMulDown(params)))
        resultMappers.push((v) => client.decodeReturnValue('wadMulDown(uint256,uint256)uint256', v))
        return this
      },
      /**
       * Add a wadMulUp(uint256,uint256)uint256 method call against the WadRayMathWrapper contract
       */
      wadMulUp(params: CallParams<WadRayMathWrapperArgs['obj']['wadMulUp(uint256,uint256)uint256'] | WadRayMathWrapperArgs['tuple']['wadMulUp(uint256,uint256)uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.wadMulUp(params)))
        resultMappers.push((v) => client.decodeReturnValue('wadMulUp(uint256,uint256)uint256', v))
        return this
      },
      /**
       * Add a wadDivDown(uint256,uint256)uint256 method call against the WadRayMathWrapper contract
       */
      wadDivDown(params: CallParams<WadRayMathWrapperArgs['obj']['wadDivDown(uint256,uint256)uint256'] | WadRayMathWrapperArgs['tuple']['wadDivDown(uint256,uint256)uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.wadDivDown(params)))
        resultMappers.push((v) => client.decodeReturnValue('wadDivDown(uint256,uint256)uint256', v))
        return this
      },
      /**
       * Add a wadDivUp(uint256,uint256)uint256 method call against the WadRayMathWrapper contract
       */
      wadDivUp(params: CallParams<WadRayMathWrapperArgs['obj']['wadDivUp(uint256,uint256)uint256'] | WadRayMathWrapperArgs['tuple']['wadDivUp(uint256,uint256)uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.wadDivUp(params)))
        resultMappers.push((v) => client.decodeReturnValue('wadDivUp(uint256,uint256)uint256', v))
        return this
      },
      /**
       * Add a rayMulDown(uint256,uint256)uint256 method call against the WadRayMathWrapper contract
       */
      rayMulDown(params: CallParams<WadRayMathWrapperArgs['obj']['rayMulDown(uint256,uint256)uint256'] | WadRayMathWrapperArgs['tuple']['rayMulDown(uint256,uint256)uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.rayMulDown(params)))
        resultMappers.push((v) => client.decodeReturnValue('rayMulDown(uint256,uint256)uint256', v))
        return this
      },
      /**
       * Add a rayMulUp(uint256,uint256)uint256 method call against the WadRayMathWrapper contract
       */
      rayMulUp(params: CallParams<WadRayMathWrapperArgs['obj']['rayMulUp(uint256,uint256)uint256'] | WadRayMathWrapperArgs['tuple']['rayMulUp(uint256,uint256)uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.rayMulUp(params)))
        resultMappers.push((v) => client.decodeReturnValue('rayMulUp(uint256,uint256)uint256', v))
        return this
      },
      /**
       * Add a rayDivDown(uint256,uint256)uint256 method call against the WadRayMathWrapper contract
       */
      rayDivDown(params: CallParams<WadRayMathWrapperArgs['obj']['rayDivDown(uint256,uint256)uint256'] | WadRayMathWrapperArgs['tuple']['rayDivDown(uint256,uint256)uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.rayDivDown(params)))
        resultMappers.push((v) => client.decodeReturnValue('rayDivDown(uint256,uint256)uint256', v))
        return this
      },
      /**
       * Add a rayDivUp(uint256,uint256)uint256 method call against the WadRayMathWrapper contract
       */
      rayDivUp(params: CallParams<WadRayMathWrapperArgs['obj']['rayDivUp(uint256,uint256)uint256'] | WadRayMathWrapperArgs['tuple']['rayDivUp(uint256,uint256)uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.rayDivUp(params)))
        resultMappers.push((v) => client.decodeReturnValue('rayDivUp(uint256,uint256)uint256', v))
        return this
      },
      /**
       * Add a toWad(uint256)uint256 method call against the WadRayMathWrapper contract
       */
      toWad(params: CallParams<WadRayMathWrapperArgs['obj']['toWad(uint256)uint256'] | WadRayMathWrapperArgs['tuple']['toWad(uint256)uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.toWad(params)))
        resultMappers.push((v) => client.decodeReturnValue('toWad(uint256)uint256', v))
        return this
      },
      /**
       * Add a toRay(uint256)uint256 method call against the WadRayMathWrapper contract
       */
      toRay(params: CallParams<WadRayMathWrapperArgs['obj']['toRay(uint256)uint256'] | WadRayMathWrapperArgs['tuple']['toRay(uint256)uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.toRay(params)))
        resultMappers.push((v) => client.decodeReturnValue('toRay(uint256)uint256', v))
        return this
      },
      /**
       * Add a fromWadDown(uint256)uint256 method call against the WadRayMathWrapper contract
       */
      fromWadDown(params: CallParams<WadRayMathWrapperArgs['obj']['fromWadDown(uint256)uint256'] | WadRayMathWrapperArgs['tuple']['fromWadDown(uint256)uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.fromWadDown(params)))
        resultMappers.push((v) => client.decodeReturnValue('fromWadDown(uint256)uint256', v))
        return this
      },
      /**
       * Add a fromRayUp(uint256)uint256 method call against the WadRayMathWrapper contract
       */
      fromRayUp(params: CallParams<WadRayMathWrapperArgs['obj']['fromRayUp(uint256)uint256'] | WadRayMathWrapperArgs['tuple']['fromRayUp(uint256)uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.fromRayUp(params)))
        resultMappers.push((v) => client.decodeReturnValue('fromRayUp(uint256)uint256', v))
        return this
      },
      /**
       * Add a bpsToWad(uint256)uint256 method call against the WadRayMathWrapper contract
       */
      bpsToWad(params: CallParams<WadRayMathWrapperArgs['obj']['bpsToWad(uint256)uint256'] | WadRayMathWrapperArgs['tuple']['bpsToWad(uint256)uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.bpsToWad(params)))
        resultMappers.push((v) => client.decodeReturnValue('bpsToWad(uint256)uint256', v))
        return this
      },
      /**
       * Add a bpsToRay(uint256)uint256 method call against the WadRayMathWrapper contract
       */
      bpsToRay(params: CallParams<WadRayMathWrapperArgs['obj']['bpsToRay(uint256)uint256'] | WadRayMathWrapperArgs['tuple']['bpsToRay(uint256)uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.bpsToRay(params)))
        resultMappers.push((v) => client.decodeReturnValue('bpsToRay(uint256)uint256', v))
        return this
      },
      /**
       * Add a roundRayUp(uint256)uint256 method call against the WadRayMathWrapper contract
       */
      roundRayUp(params: CallParams<WadRayMathWrapperArgs['obj']['roundRayUp(uint256)uint256'] | WadRayMathWrapperArgs['tuple']['roundRayUp(uint256)uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.roundRayUp(params)))
        resultMappers.push((v) => client.decodeReturnValue('roundRayUp(uint256)uint256', v))
        return this
      },
      /**
       * Add a clear state call to the WadRayMathWrapper contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as WadRayMathWrapperComposer
  }
}
export type WadRayMathWrapperComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the WAD_DECIMALS()uint256 ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  wadDecimals(params?: CallParams<WadRayMathWrapperArgs['obj']['WAD_DECIMALS()uint256'] | WadRayMathWrapperArgs['tuple']['WAD_DECIMALS()uint256']>): WadRayMathWrapperComposer<[...TReturns, WadRayMathWrapperReturns['WAD_DECIMALS()uint256'] | undefined]>

  /**
   * Calls the WAD()uint256 ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  wad(params?: CallParams<WadRayMathWrapperArgs['obj']['WAD()uint256'] | WadRayMathWrapperArgs['tuple']['WAD()uint256']>): WadRayMathWrapperComposer<[...TReturns, WadRayMathWrapperReturns['WAD()uint256'] | undefined]>

  /**
   * Calls the RAY()uint256 ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  ray(params?: CallParams<WadRayMathWrapperArgs['obj']['RAY()uint256'] | WadRayMathWrapperArgs['tuple']['RAY()uint256']>): WadRayMathWrapperComposer<[...TReturns, WadRayMathWrapperReturns['RAY()uint256'] | undefined]>

  /**
   * Calls the PERCENTAGE_FACTOR()uint256 ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  percentageFactor(params?: CallParams<WadRayMathWrapperArgs['obj']['PERCENTAGE_FACTOR()uint256'] | WadRayMathWrapperArgs['tuple']['PERCENTAGE_FACTOR()uint256']>): WadRayMathWrapperComposer<[...TReturns, WadRayMathWrapperReturns['PERCENTAGE_FACTOR()uint256'] | undefined]>

  /**
   * Calls the wadMulDown(uint256,uint256)uint256 ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  wadMulDown(params?: CallParams<WadRayMathWrapperArgs['obj']['wadMulDown(uint256,uint256)uint256'] | WadRayMathWrapperArgs['tuple']['wadMulDown(uint256,uint256)uint256']>): WadRayMathWrapperComposer<[...TReturns, WadRayMathWrapperReturns['wadMulDown(uint256,uint256)uint256'] | undefined]>

  /**
   * Calls the wadMulUp(uint256,uint256)uint256 ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  wadMulUp(params?: CallParams<WadRayMathWrapperArgs['obj']['wadMulUp(uint256,uint256)uint256'] | WadRayMathWrapperArgs['tuple']['wadMulUp(uint256,uint256)uint256']>): WadRayMathWrapperComposer<[...TReturns, WadRayMathWrapperReturns['wadMulUp(uint256,uint256)uint256'] | undefined]>

  /**
   * Calls the wadDivDown(uint256,uint256)uint256 ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  wadDivDown(params?: CallParams<WadRayMathWrapperArgs['obj']['wadDivDown(uint256,uint256)uint256'] | WadRayMathWrapperArgs['tuple']['wadDivDown(uint256,uint256)uint256']>): WadRayMathWrapperComposer<[...TReturns, WadRayMathWrapperReturns['wadDivDown(uint256,uint256)uint256'] | undefined]>

  /**
   * Calls the wadDivUp(uint256,uint256)uint256 ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  wadDivUp(params?: CallParams<WadRayMathWrapperArgs['obj']['wadDivUp(uint256,uint256)uint256'] | WadRayMathWrapperArgs['tuple']['wadDivUp(uint256,uint256)uint256']>): WadRayMathWrapperComposer<[...TReturns, WadRayMathWrapperReturns['wadDivUp(uint256,uint256)uint256'] | undefined]>

  /**
   * Calls the rayMulDown(uint256,uint256)uint256 ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  rayMulDown(params?: CallParams<WadRayMathWrapperArgs['obj']['rayMulDown(uint256,uint256)uint256'] | WadRayMathWrapperArgs['tuple']['rayMulDown(uint256,uint256)uint256']>): WadRayMathWrapperComposer<[...TReturns, WadRayMathWrapperReturns['rayMulDown(uint256,uint256)uint256'] | undefined]>

  /**
   * Calls the rayMulUp(uint256,uint256)uint256 ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  rayMulUp(params?: CallParams<WadRayMathWrapperArgs['obj']['rayMulUp(uint256,uint256)uint256'] | WadRayMathWrapperArgs['tuple']['rayMulUp(uint256,uint256)uint256']>): WadRayMathWrapperComposer<[...TReturns, WadRayMathWrapperReturns['rayMulUp(uint256,uint256)uint256'] | undefined]>

  /**
   * Calls the rayDivDown(uint256,uint256)uint256 ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  rayDivDown(params?: CallParams<WadRayMathWrapperArgs['obj']['rayDivDown(uint256,uint256)uint256'] | WadRayMathWrapperArgs['tuple']['rayDivDown(uint256,uint256)uint256']>): WadRayMathWrapperComposer<[...TReturns, WadRayMathWrapperReturns['rayDivDown(uint256,uint256)uint256'] | undefined]>

  /**
   * Calls the rayDivUp(uint256,uint256)uint256 ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  rayDivUp(params?: CallParams<WadRayMathWrapperArgs['obj']['rayDivUp(uint256,uint256)uint256'] | WadRayMathWrapperArgs['tuple']['rayDivUp(uint256,uint256)uint256']>): WadRayMathWrapperComposer<[...TReturns, WadRayMathWrapperReturns['rayDivUp(uint256,uint256)uint256'] | undefined]>

  /**
   * Calls the toWad(uint256)uint256 ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  toWad(params?: CallParams<WadRayMathWrapperArgs['obj']['toWad(uint256)uint256'] | WadRayMathWrapperArgs['tuple']['toWad(uint256)uint256']>): WadRayMathWrapperComposer<[...TReturns, WadRayMathWrapperReturns['toWad(uint256)uint256'] | undefined]>

  /**
   * Calls the toRay(uint256)uint256 ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  toRay(params?: CallParams<WadRayMathWrapperArgs['obj']['toRay(uint256)uint256'] | WadRayMathWrapperArgs['tuple']['toRay(uint256)uint256']>): WadRayMathWrapperComposer<[...TReturns, WadRayMathWrapperReturns['toRay(uint256)uint256'] | undefined]>

  /**
   * Calls the fromWadDown(uint256)uint256 ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  fromWadDown(params?: CallParams<WadRayMathWrapperArgs['obj']['fromWadDown(uint256)uint256'] | WadRayMathWrapperArgs['tuple']['fromWadDown(uint256)uint256']>): WadRayMathWrapperComposer<[...TReturns, WadRayMathWrapperReturns['fromWadDown(uint256)uint256'] | undefined]>

  /**
   * Calls the fromRayUp(uint256)uint256 ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  fromRayUp(params?: CallParams<WadRayMathWrapperArgs['obj']['fromRayUp(uint256)uint256'] | WadRayMathWrapperArgs['tuple']['fromRayUp(uint256)uint256']>): WadRayMathWrapperComposer<[...TReturns, WadRayMathWrapperReturns['fromRayUp(uint256)uint256'] | undefined]>

  /**
   * Calls the bpsToWad(uint256)uint256 ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  bpsToWad(params?: CallParams<WadRayMathWrapperArgs['obj']['bpsToWad(uint256)uint256'] | WadRayMathWrapperArgs['tuple']['bpsToWad(uint256)uint256']>): WadRayMathWrapperComposer<[...TReturns, WadRayMathWrapperReturns['bpsToWad(uint256)uint256'] | undefined]>

  /**
   * Calls the bpsToRay(uint256)uint256 ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  bpsToRay(params?: CallParams<WadRayMathWrapperArgs['obj']['bpsToRay(uint256)uint256'] | WadRayMathWrapperArgs['tuple']['bpsToRay(uint256)uint256']>): WadRayMathWrapperComposer<[...TReturns, WadRayMathWrapperReturns['bpsToRay(uint256)uint256'] | undefined]>

  /**
   * Calls the roundRayUp(uint256)uint256 ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  roundRayUp(params?: CallParams<WadRayMathWrapperArgs['obj']['roundRayUp(uint256)uint256'] | WadRayMathWrapperArgs['tuple']['roundRayUp(uint256)uint256']>): WadRayMathWrapperComposer<[...TReturns, WadRayMathWrapperReturns['roundRayUp(uint256)uint256'] | undefined]>

  /**
   * Makes a clear_state call to an existing instance of the WadRayMathWrapper smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): WadRayMathWrapperComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): WadRayMathWrapperComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<WadRayMathWrapperComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<WadRayMathWrapperComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<WadRayMathWrapperComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<WadRayMathWrapperComposerResults<TReturns>>
}
export type WadRayMathWrapperComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

