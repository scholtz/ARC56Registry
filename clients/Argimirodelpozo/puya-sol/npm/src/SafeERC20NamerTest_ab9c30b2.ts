/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"SafeERC20NamerTest","structs":{},"methods":[{"name":"tokenSymbol","args":[{"type":"address","name":"token"}],"returns":{"type":"string"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"tokenName","args":[{"type":"address","name":"token"}],"returns":{"type":"string"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}}],"arcs":[22,28],"networks":{},"state":{"schema":{"global":{"ints":0,"bytes":0},"local":{"ints":0,"bytes":0}},"keys":{"global":{},"local":{},"box":{}},"maps":{"global":{},"local":{},"box":{}}},"bareActions":{"create":[],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[48,73],"errorMessage":"invalid number of bytes for arc4.static_array<arc4.uint8, 32>"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"events":[]} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


/**
 * The argument types for the SafeErc20NamerTest contract
 */
export type SafeErc20NamerTestArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'tokenSymbol(address)string': {
      token: string
    }
    'tokenName(address)string': {
      token: string
    }
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'tokenSymbol(address)string': [token: string]
    'tokenName(address)string': [token: string]
  }
}

/**
 * The return type for each method
 */
export type SafeErc20NamerTestReturns = {
  'tokenSymbol(address)string': string
  'tokenName(address)string': string
}

/**
 * Defines the types of available calls and state of the SafeErc20NamerTest smart contract.
 */
export type SafeErc20NamerTestTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'tokenSymbol(address)string' | 'tokenSymbol', {
      argsObj: SafeErc20NamerTestArgs['obj']['tokenSymbol(address)string']
      argsTuple: SafeErc20NamerTestArgs['tuple']['tokenSymbol(address)string']
      returns: SafeErc20NamerTestReturns['tokenSymbol(address)string']
    }>
    & Record<'tokenName(address)string' | 'tokenName', {
      argsObj: SafeErc20NamerTestArgs['obj']['tokenName(address)string']
      argsTuple: SafeErc20NamerTestArgs['tuple']['tokenName(address)string']
      returns: SafeErc20NamerTestReturns['tokenName(address)string']
    }>
}

/**
 * Defines the possible abi call signatures.
 */
export type SafeErc20NamerTestSignatures = keyof SafeErc20NamerTestTypes['methods']
/**
 * Defines the possible abi call signatures for methods that return a non-void value.
 */
export type SafeErc20NamerTestNonVoidMethodSignatures = keyof SafeErc20NamerTestTypes['methods'] extends infer T ? T extends keyof SafeErc20NamerTestTypes['methods'] ? MethodReturn<T> extends void ? never : T  : never : never
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the SafeErc20NamerTest smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends SafeErc20NamerTestSignatures> = SafeErc20NamerTestTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the SafeErc20NamerTest smart contract to the method's return type
 */
export type MethodReturn<TSignature extends SafeErc20NamerTestSignatures> = SafeErc20NamerTestTypes['methods'][TSignature]['returns']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the SafeErc20NamerTest smart contract
 */
export abstract class SafeErc20NamerTestParamsFactory {
  /**
   * Constructs a no op call for the tokenSymbol(address)string ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static tokenSymbol(params: CallParams<SafeErc20NamerTestArgs['obj']['tokenSymbol(address)string'] | SafeErc20NamerTestArgs['tuple']['tokenSymbol(address)string']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'tokenSymbol(address)string' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.token],
    }
  }
  /**
   * Constructs a no op call for the tokenName(address)string ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static tokenName(params: CallParams<SafeErc20NamerTestArgs['obj']['tokenName(address)string'] | SafeErc20NamerTestArgs['tuple']['tokenName(address)string']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'tokenName(address)string' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.token],
    }
  }
}

/**
 * A client to make calls to the SafeERC20NamerTest smart contract
 */
export class SafeErc20NamerTestClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `SafeErc20NamerTestClient`
   *
   * @param appClient An `AppClient` instance which has been created with the SafeErc20NamerTest app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `SafeErc20NamerTestClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Checks for decode errors on the given return value and maps the return value to the return type for the given method
   * @returns The typed return value or undefined if there was no value
   */
  decodeReturnValue<TSignature extends SafeErc20NamerTestNonVoidMethodSignatures>(method: TSignature, returnValue: ABIReturn | undefined) {
    return returnValue !== undefined ? getArc56ReturnValue<MethodReturn<TSignature>>(returnValue, this.appClient.getABIMethod(method), APP_SPEC.structs) : undefined
  }

  /**
   * Returns a new `SafeErc20NamerTestClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<SafeErc20NamerTestClient> {
    return new SafeErc20NamerTestClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `SafeErc20NamerTestClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<SafeErc20NamerTestClient> {
    return new SafeErc20NamerTestClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Makes a clear_state call to an existing instance of the SafeERC20NamerTest smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the SafeERC20NamerTest smart contract using the `tokenSymbol(address)string` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    tokenSymbol: (params: CallParams<SafeErc20NamerTestArgs['obj']['tokenSymbol(address)string'] | SafeErc20NamerTestArgs['tuple']['tokenSymbol(address)string']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(SafeErc20NamerTestParamsFactory.tokenSymbol(params))
    },

    /**
     * Makes a call to the SafeERC20NamerTest smart contract using the `tokenName(address)string` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    tokenName: (params: CallParams<SafeErc20NamerTestArgs['obj']['tokenName(address)string'] | SafeErc20NamerTestArgs['tuple']['tokenName(address)string']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(SafeErc20NamerTestParamsFactory.tokenName(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Makes a clear_state call to an existing instance of the SafeERC20NamerTest smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the SafeERC20NamerTest smart contract using the `tokenSymbol(address)string` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    tokenSymbol: (params: CallParams<SafeErc20NamerTestArgs['obj']['tokenSymbol(address)string'] | SafeErc20NamerTestArgs['tuple']['tokenSymbol(address)string']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(SafeErc20NamerTestParamsFactory.tokenSymbol(params))
    },

    /**
     * Makes a call to the SafeERC20NamerTest smart contract using the `tokenName(address)string` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    tokenName: (params: CallParams<SafeErc20NamerTestArgs['obj']['tokenName(address)string'] | SafeErc20NamerTestArgs['tuple']['tokenName(address)string']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(SafeErc20NamerTestParamsFactory.tokenName(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Makes a clear_state call to an existing instance of the SafeERC20NamerTest smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the SafeERC20NamerTest smart contract using the `tokenSymbol(address)string` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    tokenSymbol: async (params: CallParams<SafeErc20NamerTestArgs['obj']['tokenSymbol(address)string'] | SafeErc20NamerTestArgs['tuple']['tokenSymbol(address)string']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(SafeErc20NamerTestParamsFactory.tokenSymbol(params))
      return {...result, return: result.return as unknown as (undefined | SafeErc20NamerTestReturns['tokenSymbol(address)string'])}
    },

    /**
     * Makes a call to the SafeERC20NamerTest smart contract using the `tokenName(address)string` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    tokenName: async (params: CallParams<SafeErc20NamerTestArgs['obj']['tokenName(address)string'] | SafeErc20NamerTestArgs['tuple']['tokenName(address)string']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(SafeErc20NamerTestParamsFactory.tokenName(params))
      return {...result, return: result.return as unknown as (undefined | SafeErc20NamerTestReturns['tokenName(address)string'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new SafeErc20NamerTestClient(this.appClient.clone(params))
  }

  /**
   * Makes a readonly (simulated) call to the SafeERC20NamerTest smart contract using the `tokenSymbol(address)string` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async tokenSymbol(params: CallParams<SafeErc20NamerTestArgs['obj']['tokenSymbol(address)string'] | SafeErc20NamerTestArgs['tuple']['tokenSymbol(address)string']>) {
    const result = await this.appClient.send.call(SafeErc20NamerTestParamsFactory.tokenSymbol(params))
    return result.return as unknown as SafeErc20NamerTestReturns['tokenSymbol(address)string']
  }

  /**
   * Makes a readonly (simulated) call to the SafeERC20NamerTest smart contract using the `tokenName(address)string` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async tokenName(params: CallParams<SafeErc20NamerTestArgs['obj']['tokenName(address)string'] | SafeErc20NamerTestArgs['tuple']['tokenName(address)string']>) {
    const result = await this.appClient.send.call(SafeErc20NamerTestParamsFactory.tokenName(params))
    return result.return as unknown as SafeErc20NamerTestReturns['tokenName(address)string']
  }

  /**
   * Methods to access state for the current SafeERC20NamerTest app
   */
  state = {
  }

  public newGroup(): SafeErc20NamerTestComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a tokenSymbol(address)string method call against the SafeERC20NamerTest contract
       */
      tokenSymbol(params: CallParams<SafeErc20NamerTestArgs['obj']['tokenSymbol(address)string'] | SafeErc20NamerTestArgs['tuple']['tokenSymbol(address)string']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.tokenSymbol(params)))
        resultMappers.push((v) => client.decodeReturnValue('tokenSymbol(address)string', v))
        return this
      },
      /**
       * Add a tokenName(address)string method call against the SafeERC20NamerTest contract
       */
      tokenName(params: CallParams<SafeErc20NamerTestArgs['obj']['tokenName(address)string'] | SafeErc20NamerTestArgs['tuple']['tokenName(address)string']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.tokenName(params)))
        resultMappers.push((v) => client.decodeReturnValue('tokenName(address)string', v))
        return this
      },
      /**
       * Add a clear state call to the SafeERC20NamerTest contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as SafeErc20NamerTestComposer
  }
}
export type SafeErc20NamerTestComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the tokenSymbol(address)string ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  tokenSymbol(params?: CallParams<SafeErc20NamerTestArgs['obj']['tokenSymbol(address)string'] | SafeErc20NamerTestArgs['tuple']['tokenSymbol(address)string']>): SafeErc20NamerTestComposer<[...TReturns, SafeErc20NamerTestReturns['tokenSymbol(address)string'] | undefined]>

  /**
   * Calls the tokenName(address)string ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  tokenName(params?: CallParams<SafeErc20NamerTestArgs['obj']['tokenName(address)string'] | SafeErc20NamerTestArgs['tuple']['tokenName(address)string']>): SafeErc20NamerTestComposer<[...TReturns, SafeErc20NamerTestReturns['tokenName(address)string'] | undefined]>

  /**
   * Makes a clear_state call to an existing instance of the SafeERC20NamerTest smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): SafeErc20NamerTestComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): SafeErc20NamerTestComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<SafeErc20NamerTestComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<SafeErc20NamerTestComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<SafeErc20NamerTestComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<SafeErc20NamerTestComposerResults<TReturns>>
}
export type SafeErc20NamerTestComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

