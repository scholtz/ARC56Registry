/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"UD60x18Wrapper__Helper3","structs":{},"methods":[{"name":"ln","args":[{"type":"uint256","name":"x"}],"returns":{"type":"uint256"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"desc":"@notice Calculates the natural logarithm of x using the following formula:\n $$\n ln{x} = log_2{x} / log_2{e}\n $$\n @dev Notes:\n - Refer to the notes in {log2}.\n - The precision isn't sufficiently fine-grained to return exactly `UNIT` when the input is `E`.\n Requirements:\n - Refer to the requirements in {log2}.\n @param x The UD60x18 number for which to calculate the natural logarithm.\n @return result The natural logarithm as a UD60x18 number.\n @custom:smtchecker abstract-function-nondet","events":[],"recommendations":{}},{"name":"div","args":[{"type":"uint256","name":"x"},{"type":"uint256","name":"y"}],"returns":{"type":"uint256"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"desc":"@notice Divides two UD60x18 numbers, returning a new UD60x18 number.\n @dev Uses {Common.mulDiv} to enable overflow-safe multiplication and division.\n Notes:\n - Refer to the notes in {Common.mulDiv}.\n Requirements:\n - Refer to the requirements in {Common.mulDiv}.\n @param x The numerator as a UD60x18 number.\n @param y The denominator as a UD60x18 number.\n @return result The quotient as a UD60x18 number.\n @custom:smtchecker abstract-function-nondet","events":[],"recommendations":{}},{"name":"log2","args":[{"type":"uint256","name":"x"}],"returns":{"type":"uint256"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"desc":"@notice Calculates the binary logarithm of x using the iterative approximation algorithm:\n $$\n log_2{x} = n + log_2{y}, \\text{ where } y = x*2^{-n}, \\ y \\in [1, 2)\n $$\n For $0 \\leq x \\lt 1$, the input is inverted:\n $$\n log_2{x} = -log_2{\\frac{1}{x}}\n $$\n @dev See https://en.wikipedia.org/wiki/Binary_logarithm#Iterative_approximation\n Notes:\n - Due to the lossy precision of the iterative approximation, the results are not perfectly accurate to the last decimal.\n Requirements:\n - x ≥ UNIT\n @param x The UD60x18 number for which to calculate the binary logarithm.\n @return result The binary logarithm as a UD60x18 number.\n @custom:smtchecker abstract-function-nondet","events":[],"recommendations":{}},{"name":"msb","args":[{"type":"uint256","name":"x"}],"returns":{"type":"uint256"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"desc":"@notice Finds the zero-based index of the first 1 in the binary representation of x.\n @dev See the note on \"msb\" in this Wikipedia article: https://en.wikipedia.org/wiki/Find_first_set\n The implementation satisfies these properties:\n $$\n \\begin{cases}\n   x = 0 \\implies \\text{msb}(x) = 0 \\\\\n   x > 0 \\implies x \\gg \\text{msb}(x) = 1\n \\end{cases}\n $$\n Each step below is equivalent to this high-level code:\n ```solidity\n if (x >= 2 ** 128) {\n     x >>= 128;\n     result += 128;\n }\n ```\n Where 128 is replaced with each respective power of two factor. The Yul instructions used below are:\n - \"gt\" is \"greater than\"\n - \"or\" is the OR bitwise operator\n - \"shl\" is \"shift left\"\n - \"shr\" is \"shift right\"\n See the full high-level implementation here:\n https://gist.github.com/PaulRBerg/f932f8693f2733e30c4d479e8e980948\n @param x The uint256 number for which to find the index of the most significant bit.\n @return result The index of the most significant bit as a uint256.\n @custom:smtchecker abstract-function-nondet","events":[],"recommendations":{}},{"name":"mul","args":[{"type":"uint256","name":"x"},{"type":"uint256","name":"y"}],"returns":{"type":"uint256"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"desc":"@notice Multiplies two UD60x18 numbers together, returning a new UD60x18 number.\n @dev Uses {Common.mulDiv} to enable overflow-safe multiplication and division.\n Notes:\n - Refer to the notes in {Common.mulDiv}.\n Requirements:\n - Refer to the requirements in {Common.mulDiv}.\n @dev See the documentation in {Common.mulDiv18}.\n @param x The multiplicand as a UD60x18 number.\n @param y The multiplier as a UD60x18 number.\n @return result The product as a UD60x18 number.\n @custom:smtchecker abstract-function-nondet","events":[],"recommendations":{}},{"name":"unwrap","args":[{"type":"uint256","name":"x"}],"returns":{"type":"uint256"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"desc":"@notice Unwraps a UD60x18 number into uint256.","events":[],"recommendations":{}},{"name":"wrap","args":[{"type":"uint256","name":"x"}],"returns":{"type":"uint256"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"desc":"@notice Wraps a uint256 number into the UD60x18 value type.","events":[],"recommendations":{}},{"name":"__init__","args":[{"type":"uint64","name":"o"},{"type":"uint64","name":"p"},{"type":"byte[]","name":"s"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}}],"arcs":[22,28],"desc":"Helper contract 3 for UD60x18Wrapper","networks":{},"state":{"schema":{"global":{"ints":2,"bytes":1},"local":{"ints":0,"bytes":0}},"keys":{"global":{"o":{"keyType":"AVMString","valueType":"AVMUint64","key":"bw=="},"p":{"keyType":"AVMString","valueType":"AVMUint64","key":"cA=="},"s":{"keyType":"AVMString","valueType":"AVMBytes","key":"cw=="}},"local":{},"box":{}},"maps":{"global":{},"local":{},"box":{}}},"bareActions":{"create":["NoOp"],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[1347],"errorMessage":"PRBMath_MulDiv18_Overflow"},{"pc":[1045],"errorMessage":"PRBMath_MulDiv_Overflow"},{"pc":[249],"errorMessage":"PRBMath_UD60x18_Log_InputTooSmall"},{"pc":[2178],"errorMessage":"helper: already initialized"},{"pc":[1574,1663,1744,1825,1914,1995,2076],"errorMessage":"helper: must be called in group"},{"pc":[1588,1677,1758,1839,1928,2009,2090],"errorMessage":"helper: sender mismatch"},{"pc":[1581,1670,1751,1832,1921,2002,2083],"errorMessage":"helper: unauthorized caller"},{"pc":[1603,1692,1773,1854,1943,2024,2105],"errorMessage":"helper: wrong prev chunk"},{"pc":[1615,1704,1785,1866,1955,2036,2117],"errorMessage":"helper: wrong prev method"},{"pc":[2164],"errorMessage":"invalid array length header"},{"pc":[2171],"errorMessage":"invalid number of bytes for arc4.dynamic_array<arc4.uint8>"},{"pc":[1564,1645,1653,1734,1815,1896,1904,1985,2066],"errorMessage":"invalid number of bytes for arc4.uint256"},{"pc":[2147,2156],"errorMessage":"invalid number of bytes for arc4.uint64"},{"pc":[1627,1716,1797,1878,1967,2048,2129],"errorMessage":"overflow"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"events":[]} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


/**
 * The argument types for the Ud60x18WrapperHelper3 contract
 */
export type Ud60x18WrapperHelper3Args = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'ln(uint256)uint256': {
      x: bigint | number
    }
    'div(uint256,uint256)uint256': {
      x: bigint | number
      y: bigint | number
    }
    'log2(uint256)uint256': {
      x: bigint | number
    }
    'msb(uint256)uint256': {
      x: bigint | number
    }
    'mul(uint256,uint256)uint256': {
      x: bigint | number
      y: bigint | number
    }
    'unwrap(uint256)uint256': {
      x: bigint | number
    }
    'wrap(uint256)uint256': {
      x: bigint | number
    }
    '__init__(uint64,uint64,byte[])void': {
      o: bigint | number
      p: bigint | number
      s: Uint8Array
    }
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'ln(uint256)uint256': [x: bigint | number]
    'div(uint256,uint256)uint256': [x: bigint | number, y: bigint | number]
    'log2(uint256)uint256': [x: bigint | number]
    'msb(uint256)uint256': [x: bigint | number]
    'mul(uint256,uint256)uint256': [x: bigint | number, y: bigint | number]
    'unwrap(uint256)uint256': [x: bigint | number]
    'wrap(uint256)uint256': [x: bigint | number]
    '__init__(uint64,uint64,byte[])void': [o: bigint | number, p: bigint | number, s: Uint8Array]
  }
}

/**
 * The return type for each method
 */
export type Ud60x18WrapperHelper3Returns = {
  'ln(uint256)uint256': bigint
  'div(uint256,uint256)uint256': bigint
  'log2(uint256)uint256': bigint
  'msb(uint256)uint256': bigint
  'mul(uint256,uint256)uint256': bigint
  'unwrap(uint256)uint256': bigint
  'wrap(uint256)uint256': bigint
  '__init__(uint64,uint64,byte[])void': void
}

/**
 * Defines the types of available calls and state of the Ud60x18WrapperHelper3 smart contract.
 */
export type Ud60x18WrapperHelper3Types = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'ln(uint256)uint256' | 'ln', {
      argsObj: Ud60x18WrapperHelper3Args['obj']['ln(uint256)uint256']
      argsTuple: Ud60x18WrapperHelper3Args['tuple']['ln(uint256)uint256']
      returns: Ud60x18WrapperHelper3Returns['ln(uint256)uint256']
    }>
    & Record<'div(uint256,uint256)uint256' | 'div', {
      argsObj: Ud60x18WrapperHelper3Args['obj']['div(uint256,uint256)uint256']
      argsTuple: Ud60x18WrapperHelper3Args['tuple']['div(uint256,uint256)uint256']
      returns: Ud60x18WrapperHelper3Returns['div(uint256,uint256)uint256']
    }>
    & Record<'log2(uint256)uint256' | 'log2', {
      argsObj: Ud60x18WrapperHelper3Args['obj']['log2(uint256)uint256']
      argsTuple: Ud60x18WrapperHelper3Args['tuple']['log2(uint256)uint256']
      returns: Ud60x18WrapperHelper3Returns['log2(uint256)uint256']
    }>
    & Record<'msb(uint256)uint256' | 'msb', {
      argsObj: Ud60x18WrapperHelper3Args['obj']['msb(uint256)uint256']
      argsTuple: Ud60x18WrapperHelper3Args['tuple']['msb(uint256)uint256']
      returns: Ud60x18WrapperHelper3Returns['msb(uint256)uint256']
    }>
    & Record<'mul(uint256,uint256)uint256' | 'mul', {
      argsObj: Ud60x18WrapperHelper3Args['obj']['mul(uint256,uint256)uint256']
      argsTuple: Ud60x18WrapperHelper3Args['tuple']['mul(uint256,uint256)uint256']
      returns: Ud60x18WrapperHelper3Returns['mul(uint256,uint256)uint256']
    }>
    & Record<'unwrap(uint256)uint256' | 'unwrap', {
      argsObj: Ud60x18WrapperHelper3Args['obj']['unwrap(uint256)uint256']
      argsTuple: Ud60x18WrapperHelper3Args['tuple']['unwrap(uint256)uint256']
      returns: Ud60x18WrapperHelper3Returns['unwrap(uint256)uint256']
    }>
    & Record<'wrap(uint256)uint256' | 'wrap', {
      argsObj: Ud60x18WrapperHelper3Args['obj']['wrap(uint256)uint256']
      argsTuple: Ud60x18WrapperHelper3Args['tuple']['wrap(uint256)uint256']
      returns: Ud60x18WrapperHelper3Returns['wrap(uint256)uint256']
    }>
    & Record<'__init__(uint64,uint64,byte[])void' | '__init__', {
      argsObj: Ud60x18WrapperHelper3Args['obj']['__init__(uint64,uint64,byte[])void']
      argsTuple: Ud60x18WrapperHelper3Args['tuple']['__init__(uint64,uint64,byte[])void']
      returns: Ud60x18WrapperHelper3Returns['__init__(uint64,uint64,byte[])void']
    }>
  /**
   * Defines the shape of the state of the application.
   */
  state: {
    global: {
      keys: {
        o: bigint
        p: bigint
        s: BinaryState
      }
      maps: {}
    }
  }
}

/**
 * Defines the possible abi call signatures.
 */
export type Ud60x18WrapperHelper3Signatures = keyof Ud60x18WrapperHelper3Types['methods']
/**
 * Defines the possible abi call signatures for methods that return a non-void value.
 */
export type Ud60x18WrapperHelper3NonVoidMethodSignatures = keyof Ud60x18WrapperHelper3Types['methods'] extends infer T ? T extends keyof Ud60x18WrapperHelper3Types['methods'] ? MethodReturn<T> extends void ? never : T  : never : never
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the Ud60x18WrapperHelper3 smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends Ud60x18WrapperHelper3Signatures> = Ud60x18WrapperHelper3Types['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the Ud60x18WrapperHelper3 smart contract to the method's return type
 */
export type MethodReturn<TSignature extends Ud60x18WrapperHelper3Signatures> = Ud60x18WrapperHelper3Types['methods'][TSignature]['returns']

/**
 * Defines the shape of the keyed global state of the application.
 */
export type GlobalKeysState = Ud60x18WrapperHelper3Types['state']['global']['keys']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the Ud60x18WrapperHelper3 smart contract
 */
export abstract class Ud60x18WrapperHelper3ParamsFactory {
  /**
   * Constructs a no op call for the ln(uint256)uint256 ABI method
   *
  * @notice Calculates the natural logarithm of x using the following formula:
  $$
  ln{x} = log_2{x} / log_2{e}
  $$
  @dev Notes:
  - Refer to the notes in {log2}.
  - The precision isn't sufficiently fine-grained to return exactly `UNIT` when the input is `E`.
  Requirements:
  - Refer to the requirements in {log2}.
  @param x The UD60x18 number for which to calculate the natural logarithm.
  @return result The natural logarithm as a UD60x18 number.
  @custom:smtchecker abstract-function-nondet

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static ln(params: CallParams<Ud60x18WrapperHelper3Args['obj']['ln(uint256)uint256'] | Ud60x18WrapperHelper3Args['tuple']['ln(uint256)uint256']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'ln(uint256)uint256' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.x],
    }
  }
  /**
   * Constructs a no op call for the div(uint256,uint256)uint256 ABI method
   *
  * @notice Divides two UD60x18 numbers, returning a new UD60x18 number.
  @dev Uses {Common.mulDiv} to enable overflow-safe multiplication and division.
  Notes:
  - Refer to the notes in {Common.mulDiv}.
  Requirements:
  - Refer to the requirements in {Common.mulDiv}.
  @param x The numerator as a UD60x18 number.
  @param y The denominator as a UD60x18 number.
  @return result The quotient as a UD60x18 number.
  @custom:smtchecker abstract-function-nondet

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static div(params: CallParams<Ud60x18WrapperHelper3Args['obj']['div(uint256,uint256)uint256'] | Ud60x18WrapperHelper3Args['tuple']['div(uint256,uint256)uint256']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'div(uint256,uint256)uint256' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.x, params.args.y],
    }
  }
  /**
   * Constructs a no op call for the log2(uint256)uint256 ABI method
   *
  * @notice Calculates the binary logarithm of x using the iterative approximation algorithm:
  $$
  log_2{x} = n + log_2{y}, \text{ where } y = x*2^{-n}, \ y \in [1, 2)
  $$
  For $0 \leq x \lt 1$, the input is inverted:
  $$
  log_2{x} = -log_2{\frac{1}{x}}
  $$
  @dev See https://en.wikipedia.org/wiki/Binary_logarithm#Iterative_approximation
  Notes:
  - Due to the lossy precision of the iterative approximation, the results are not perfectly accurate to the last decimal.
  Requirements:
  - x ≥ UNIT
  @param x The UD60x18 number for which to calculate the binary logarithm.
  @return result The binary logarithm as a UD60x18 number.
  @custom:smtchecker abstract-function-nondet

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static log2(params: CallParams<Ud60x18WrapperHelper3Args['obj']['log2(uint256)uint256'] | Ud60x18WrapperHelper3Args['tuple']['log2(uint256)uint256']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'log2(uint256)uint256' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.x],
    }
  }
  /**
   * Constructs a no op call for the msb(uint256)uint256 ABI method
   *
  * @notice Finds the zero-based index of the first 1 in the binary representation of x.
  @dev See the note on "msb" in this Wikipedia article: https://en.wikipedia.org/wiki/Find_first_set
  The implementation satisfies these properties:
  $$
  \begin{cases}
    x = 0 \implies \text{msb}(x) = 0 \\
    x > 0 \implies x \gg \text{msb}(x) = 1
  \end{cases}
  $$
  Each step below is equivalent to this high-level code:
  ```solidity
  if (x >= 2 ** 128) {
      x >>= 128;
      result += 128;
  }
  ```
  Where 128 is replaced with each respective power of two factor. The Yul instructions used below are:
  - "gt" is "greater than"
  - "or" is the OR bitwise operator
  - "shl" is "shift left"
  - "shr" is "shift right"
  See the full high-level implementation here:
  https://gist.github.com/PaulRBerg/f932f8693f2733e30c4d479e8e980948
  @param x The uint256 number for which to find the index of the most significant bit.
  @return result The index of the most significant bit as a uint256.
  @custom:smtchecker abstract-function-nondet

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static msb(params: CallParams<Ud60x18WrapperHelper3Args['obj']['msb(uint256)uint256'] | Ud60x18WrapperHelper3Args['tuple']['msb(uint256)uint256']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'msb(uint256)uint256' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.x],
    }
  }
  /**
   * Constructs a no op call for the mul(uint256,uint256)uint256 ABI method
   *
  * @notice Multiplies two UD60x18 numbers together, returning a new UD60x18 number.
  @dev Uses {Common.mulDiv} to enable overflow-safe multiplication and division.
  Notes:
  - Refer to the notes in {Common.mulDiv}.
  Requirements:
  - Refer to the requirements in {Common.mulDiv}.
  @dev See the documentation in {Common.mulDiv18}.
  @param x The multiplicand as a UD60x18 number.
  @param y The multiplier as a UD60x18 number.
  @return result The product as a UD60x18 number.
  @custom:smtchecker abstract-function-nondet

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static mul(params: CallParams<Ud60x18WrapperHelper3Args['obj']['mul(uint256,uint256)uint256'] | Ud60x18WrapperHelper3Args['tuple']['mul(uint256,uint256)uint256']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'mul(uint256,uint256)uint256' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.x, params.args.y],
    }
  }
  /**
   * Constructs a no op call for the unwrap(uint256)uint256 ABI method
   *
   * @notice Unwraps a UD60x18 number into uint256.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static unwrap(params: CallParams<Ud60x18WrapperHelper3Args['obj']['unwrap(uint256)uint256'] | Ud60x18WrapperHelper3Args['tuple']['unwrap(uint256)uint256']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'unwrap(uint256)uint256' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.x],
    }
  }
  /**
   * Constructs a no op call for the wrap(uint256)uint256 ABI method
   *
   * @notice Wraps a uint256 number into the UD60x18 value type.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static wrap(params: CallParams<Ud60x18WrapperHelper3Args['obj']['wrap(uint256)uint256'] | Ud60x18WrapperHelper3Args['tuple']['wrap(uint256)uint256']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'wrap(uint256)uint256' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.x],
    }
  }
  /**
   * Constructs a no op call for the __init__(uint64,uint64,byte[])void ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static __init(params: CallParams<Ud60x18WrapperHelper3Args['obj']['__init__(uint64,uint64,byte[])void'] | Ud60x18WrapperHelper3Args['tuple']['__init__(uint64,uint64,byte[])void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: '__init__(uint64,uint64,byte[])void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.o, params.args.p, params.args.s],
    }
  }
}

/**
 * A client to make calls to the UD60x18Wrapper__Helper3 smart contract
 */
export class Ud60x18WrapperHelper3Client {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `Ud60x18WrapperHelper3Client`
   *
   * @param appClient An `AppClient` instance which has been created with the Ud60x18WrapperHelper3 app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `Ud60x18WrapperHelper3Client`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Checks for decode errors on the given return value and maps the return value to the return type for the given method
   * @returns The typed return value or undefined if there was no value
   */
  decodeReturnValue<TSignature extends Ud60x18WrapperHelper3NonVoidMethodSignatures>(method: TSignature, returnValue: ABIReturn | undefined) {
    return returnValue !== undefined ? getArc56ReturnValue<MethodReturn<TSignature>>(returnValue, this.appClient.getABIMethod(method), APP_SPEC.structs) : undefined
  }

  /**
   * Returns a new `Ud60x18WrapperHelper3Client` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<Ud60x18WrapperHelper3Client> {
    return new Ud60x18WrapperHelper3Client(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `Ud60x18WrapperHelper3Client` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<Ud60x18WrapperHelper3Client> {
    return new Ud60x18WrapperHelper3Client(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Makes a clear_state call to an existing instance of the UD60x18Wrapper__Helper3 smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the UD60x18Wrapper__Helper3 smart contract using the `ln(uint256)uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
    * @notice Calculates the natural logarithm of x using the following formula:
    $$
    ln{x} = log_2{x} / log_2{e}
    $$
    @dev Notes:
    - Refer to the notes in {log2}.
    - The precision isn't sufficiently fine-grained to return exactly `UNIT` when the input is `E`.
    Requirements:
    - Refer to the requirements in {log2}.
    @param x The UD60x18 number for which to calculate the natural logarithm.
    @return result The natural logarithm as a UD60x18 number.
    @custom:smtchecker abstract-function-nondet

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    ln: (params: CallParams<Ud60x18WrapperHelper3Args['obj']['ln(uint256)uint256'] | Ud60x18WrapperHelper3Args['tuple']['ln(uint256)uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(Ud60x18WrapperHelper3ParamsFactory.ln(params))
    },

    /**
     * Makes a call to the UD60x18Wrapper__Helper3 smart contract using the `div(uint256,uint256)uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
    * @notice Divides two UD60x18 numbers, returning a new UD60x18 number.
    @dev Uses {Common.mulDiv} to enable overflow-safe multiplication and division.
    Notes:
    - Refer to the notes in {Common.mulDiv}.
    Requirements:
    - Refer to the requirements in {Common.mulDiv}.
    @param x The numerator as a UD60x18 number.
    @param y The denominator as a UD60x18 number.
    @return result The quotient as a UD60x18 number.
    @custom:smtchecker abstract-function-nondet

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    div: (params: CallParams<Ud60x18WrapperHelper3Args['obj']['div(uint256,uint256)uint256'] | Ud60x18WrapperHelper3Args['tuple']['div(uint256,uint256)uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(Ud60x18WrapperHelper3ParamsFactory.div(params))
    },

    /**
     * Makes a call to the UD60x18Wrapper__Helper3 smart contract using the `log2(uint256)uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
    * @notice Calculates the binary logarithm of x using the iterative approximation algorithm:
    $$
    log_2{x} = n + log_2{y}, \text{ where } y = x*2^{-n}, \ y \in [1, 2)
    $$
    For $0 \leq x \lt 1$, the input is inverted:
    $$
    log_2{x} = -log_2{\frac{1}{x}}
    $$
    @dev See https://en.wikipedia.org/wiki/Binary_logarithm#Iterative_approximation
    Notes:
    - Due to the lossy precision of the iterative approximation, the results are not perfectly accurate to the last decimal.
    Requirements:
    - x ≥ UNIT
    @param x The UD60x18 number for which to calculate the binary logarithm.
    @return result The binary logarithm as a UD60x18 number.
    @custom:smtchecker abstract-function-nondet

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    log2: (params: CallParams<Ud60x18WrapperHelper3Args['obj']['log2(uint256)uint256'] | Ud60x18WrapperHelper3Args['tuple']['log2(uint256)uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(Ud60x18WrapperHelper3ParamsFactory.log2(params))
    },

    /**
     * Makes a call to the UD60x18Wrapper__Helper3 smart contract using the `msb(uint256)uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
    * @notice Finds the zero-based index of the first 1 in the binary representation of x.
    @dev See the note on "msb" in this Wikipedia article: https://en.wikipedia.org/wiki/Find_first_set
    The implementation satisfies these properties:
    $$
    \begin{cases}
      x = 0 \implies \text{msb}(x) = 0 \\
      x > 0 \implies x \gg \text{msb}(x) = 1
    \end{cases}
    $$
    Each step below is equivalent to this high-level code:
    ```solidity
    if (x >= 2 ** 128) {
        x >>= 128;
        result += 128;
    }
    ```
    Where 128 is replaced with each respective power of two factor. The Yul instructions used below are:
    - "gt" is "greater than"
    - "or" is the OR bitwise operator
    - "shl" is "shift left"
    - "shr" is "shift right"
    See the full high-level implementation here:
    https://gist.github.com/PaulRBerg/f932f8693f2733e30c4d479e8e980948
    @param x The uint256 number for which to find the index of the most significant bit.
    @return result The index of the most significant bit as a uint256.
    @custom:smtchecker abstract-function-nondet

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    msb: (params: CallParams<Ud60x18WrapperHelper3Args['obj']['msb(uint256)uint256'] | Ud60x18WrapperHelper3Args['tuple']['msb(uint256)uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(Ud60x18WrapperHelper3ParamsFactory.msb(params))
    },

    /**
     * Makes a call to the UD60x18Wrapper__Helper3 smart contract using the `mul(uint256,uint256)uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
    * @notice Multiplies two UD60x18 numbers together, returning a new UD60x18 number.
    @dev Uses {Common.mulDiv} to enable overflow-safe multiplication and division.
    Notes:
    - Refer to the notes in {Common.mulDiv}.
    Requirements:
    - Refer to the requirements in {Common.mulDiv}.
    @dev See the documentation in {Common.mulDiv18}.
    @param x The multiplicand as a UD60x18 number.
    @param y The multiplier as a UD60x18 number.
    @return result The product as a UD60x18 number.
    @custom:smtchecker abstract-function-nondet

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    mul: (params: CallParams<Ud60x18WrapperHelper3Args['obj']['mul(uint256,uint256)uint256'] | Ud60x18WrapperHelper3Args['tuple']['mul(uint256,uint256)uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(Ud60x18WrapperHelper3ParamsFactory.mul(params))
    },

    /**
     * Makes a call to the UD60x18Wrapper__Helper3 smart contract using the `unwrap(uint256)uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @notice Unwraps a UD60x18 number into uint256.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    unwrap: (params: CallParams<Ud60x18WrapperHelper3Args['obj']['unwrap(uint256)uint256'] | Ud60x18WrapperHelper3Args['tuple']['unwrap(uint256)uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(Ud60x18WrapperHelper3ParamsFactory.unwrap(params))
    },

    /**
     * Makes a call to the UD60x18Wrapper__Helper3 smart contract using the `wrap(uint256)uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @notice Wraps a uint256 number into the UD60x18 value type.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    wrap: (params: CallParams<Ud60x18WrapperHelper3Args['obj']['wrap(uint256)uint256'] | Ud60x18WrapperHelper3Args['tuple']['wrap(uint256)uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(Ud60x18WrapperHelper3ParamsFactory.wrap(params))
    },

    /**
     * Makes a call to the UD60x18Wrapper__Helper3 smart contract using the `__init__(uint64,uint64,byte[])void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    __init: (params: CallParams<Ud60x18WrapperHelper3Args['obj']['__init__(uint64,uint64,byte[])void'] | Ud60x18WrapperHelper3Args['tuple']['__init__(uint64,uint64,byte[])void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(Ud60x18WrapperHelper3ParamsFactory.__init(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Makes a clear_state call to an existing instance of the UD60x18Wrapper__Helper3 smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the UD60x18Wrapper__Helper3 smart contract using the `ln(uint256)uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
    * @notice Calculates the natural logarithm of x using the following formula:
    $$
    ln{x} = log_2{x} / log_2{e}
    $$
    @dev Notes:
    - Refer to the notes in {log2}.
    - The precision isn't sufficiently fine-grained to return exactly `UNIT` when the input is `E`.
    Requirements:
    - Refer to the requirements in {log2}.
    @param x The UD60x18 number for which to calculate the natural logarithm.
    @return result The natural logarithm as a UD60x18 number.
    @custom:smtchecker abstract-function-nondet

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    ln: (params: CallParams<Ud60x18WrapperHelper3Args['obj']['ln(uint256)uint256'] | Ud60x18WrapperHelper3Args['tuple']['ln(uint256)uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(Ud60x18WrapperHelper3ParamsFactory.ln(params))
    },

    /**
     * Makes a call to the UD60x18Wrapper__Helper3 smart contract using the `div(uint256,uint256)uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
    * @notice Divides two UD60x18 numbers, returning a new UD60x18 number.
    @dev Uses {Common.mulDiv} to enable overflow-safe multiplication and division.
    Notes:
    - Refer to the notes in {Common.mulDiv}.
    Requirements:
    - Refer to the requirements in {Common.mulDiv}.
    @param x The numerator as a UD60x18 number.
    @param y The denominator as a UD60x18 number.
    @return result The quotient as a UD60x18 number.
    @custom:smtchecker abstract-function-nondet

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    div: (params: CallParams<Ud60x18WrapperHelper3Args['obj']['div(uint256,uint256)uint256'] | Ud60x18WrapperHelper3Args['tuple']['div(uint256,uint256)uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(Ud60x18WrapperHelper3ParamsFactory.div(params))
    },

    /**
     * Makes a call to the UD60x18Wrapper__Helper3 smart contract using the `log2(uint256)uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
    * @notice Calculates the binary logarithm of x using the iterative approximation algorithm:
    $$
    log_2{x} = n + log_2{y}, \text{ where } y = x*2^{-n}, \ y \in [1, 2)
    $$
    For $0 \leq x \lt 1$, the input is inverted:
    $$
    log_2{x} = -log_2{\frac{1}{x}}
    $$
    @dev See https://en.wikipedia.org/wiki/Binary_logarithm#Iterative_approximation
    Notes:
    - Due to the lossy precision of the iterative approximation, the results are not perfectly accurate to the last decimal.
    Requirements:
    - x ≥ UNIT
    @param x The UD60x18 number for which to calculate the binary logarithm.
    @return result The binary logarithm as a UD60x18 number.
    @custom:smtchecker abstract-function-nondet

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    log2: (params: CallParams<Ud60x18WrapperHelper3Args['obj']['log2(uint256)uint256'] | Ud60x18WrapperHelper3Args['tuple']['log2(uint256)uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(Ud60x18WrapperHelper3ParamsFactory.log2(params))
    },

    /**
     * Makes a call to the UD60x18Wrapper__Helper3 smart contract using the `msb(uint256)uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
    * @notice Finds the zero-based index of the first 1 in the binary representation of x.
    @dev See the note on "msb" in this Wikipedia article: https://en.wikipedia.org/wiki/Find_first_set
    The implementation satisfies these properties:
    $$
    \begin{cases}
      x = 0 \implies \text{msb}(x) = 0 \\
      x > 0 \implies x \gg \text{msb}(x) = 1
    \end{cases}
    $$
    Each step below is equivalent to this high-level code:
    ```solidity
    if (x >= 2 ** 128) {
        x >>= 128;
        result += 128;
    }
    ```
    Where 128 is replaced with each respective power of two factor. The Yul instructions used below are:
    - "gt" is "greater than"
    - "or" is the OR bitwise operator
    - "shl" is "shift left"
    - "shr" is "shift right"
    See the full high-level implementation here:
    https://gist.github.com/PaulRBerg/f932f8693f2733e30c4d479e8e980948
    @param x The uint256 number for which to find the index of the most significant bit.
    @return result The index of the most significant bit as a uint256.
    @custom:smtchecker abstract-function-nondet

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    msb: (params: CallParams<Ud60x18WrapperHelper3Args['obj']['msb(uint256)uint256'] | Ud60x18WrapperHelper3Args['tuple']['msb(uint256)uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(Ud60x18WrapperHelper3ParamsFactory.msb(params))
    },

    /**
     * Makes a call to the UD60x18Wrapper__Helper3 smart contract using the `mul(uint256,uint256)uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
    * @notice Multiplies two UD60x18 numbers together, returning a new UD60x18 number.
    @dev Uses {Common.mulDiv} to enable overflow-safe multiplication and division.
    Notes:
    - Refer to the notes in {Common.mulDiv}.
    Requirements:
    - Refer to the requirements in {Common.mulDiv}.
    @dev See the documentation in {Common.mulDiv18}.
    @param x The multiplicand as a UD60x18 number.
    @param y The multiplier as a UD60x18 number.
    @return result The product as a UD60x18 number.
    @custom:smtchecker abstract-function-nondet

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    mul: (params: CallParams<Ud60x18WrapperHelper3Args['obj']['mul(uint256,uint256)uint256'] | Ud60x18WrapperHelper3Args['tuple']['mul(uint256,uint256)uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(Ud60x18WrapperHelper3ParamsFactory.mul(params))
    },

    /**
     * Makes a call to the UD60x18Wrapper__Helper3 smart contract using the `unwrap(uint256)uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @notice Unwraps a UD60x18 number into uint256.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    unwrap: (params: CallParams<Ud60x18WrapperHelper3Args['obj']['unwrap(uint256)uint256'] | Ud60x18WrapperHelper3Args['tuple']['unwrap(uint256)uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(Ud60x18WrapperHelper3ParamsFactory.unwrap(params))
    },

    /**
     * Makes a call to the UD60x18Wrapper__Helper3 smart contract using the `wrap(uint256)uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @notice Wraps a uint256 number into the UD60x18 value type.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    wrap: (params: CallParams<Ud60x18WrapperHelper3Args['obj']['wrap(uint256)uint256'] | Ud60x18WrapperHelper3Args['tuple']['wrap(uint256)uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(Ud60x18WrapperHelper3ParamsFactory.wrap(params))
    },

    /**
     * Makes a call to the UD60x18Wrapper__Helper3 smart contract using the `__init__(uint64,uint64,byte[])void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    __init: (params: CallParams<Ud60x18WrapperHelper3Args['obj']['__init__(uint64,uint64,byte[])void'] | Ud60x18WrapperHelper3Args['tuple']['__init__(uint64,uint64,byte[])void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(Ud60x18WrapperHelper3ParamsFactory.__init(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Makes a clear_state call to an existing instance of the UD60x18Wrapper__Helper3 smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the UD60x18Wrapper__Helper3 smart contract using the `ln(uint256)uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
    * @notice Calculates the natural logarithm of x using the following formula:
    $$
    ln{x} = log_2{x} / log_2{e}
    $$
    @dev Notes:
    - Refer to the notes in {log2}.
    - The precision isn't sufficiently fine-grained to return exactly `UNIT` when the input is `E`.
    Requirements:
    - Refer to the requirements in {log2}.
    @param x The UD60x18 number for which to calculate the natural logarithm.
    @return result The natural logarithm as a UD60x18 number.
    @custom:smtchecker abstract-function-nondet

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    ln: async (params: CallParams<Ud60x18WrapperHelper3Args['obj']['ln(uint256)uint256'] | Ud60x18WrapperHelper3Args['tuple']['ln(uint256)uint256']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(Ud60x18WrapperHelper3ParamsFactory.ln(params))
      return {...result, return: result.return as unknown as (undefined | Ud60x18WrapperHelper3Returns['ln(uint256)uint256'])}
    },

    /**
     * Makes a call to the UD60x18Wrapper__Helper3 smart contract using the `div(uint256,uint256)uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
    * @notice Divides two UD60x18 numbers, returning a new UD60x18 number.
    @dev Uses {Common.mulDiv} to enable overflow-safe multiplication and division.
    Notes:
    - Refer to the notes in {Common.mulDiv}.
    Requirements:
    - Refer to the requirements in {Common.mulDiv}.
    @param x The numerator as a UD60x18 number.
    @param y The denominator as a UD60x18 number.
    @return result The quotient as a UD60x18 number.
    @custom:smtchecker abstract-function-nondet

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    div: async (params: CallParams<Ud60x18WrapperHelper3Args['obj']['div(uint256,uint256)uint256'] | Ud60x18WrapperHelper3Args['tuple']['div(uint256,uint256)uint256']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(Ud60x18WrapperHelper3ParamsFactory.div(params))
      return {...result, return: result.return as unknown as (undefined | Ud60x18WrapperHelper3Returns['div(uint256,uint256)uint256'])}
    },

    /**
     * Makes a call to the UD60x18Wrapper__Helper3 smart contract using the `log2(uint256)uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
    * @notice Calculates the binary logarithm of x using the iterative approximation algorithm:
    $$
    log_2{x} = n + log_2{y}, \text{ where } y = x*2^{-n}, \ y \in [1, 2)
    $$
    For $0 \leq x \lt 1$, the input is inverted:
    $$
    log_2{x} = -log_2{\frac{1}{x}}
    $$
    @dev See https://en.wikipedia.org/wiki/Binary_logarithm#Iterative_approximation
    Notes:
    - Due to the lossy precision of the iterative approximation, the results are not perfectly accurate to the last decimal.
    Requirements:
    - x ≥ UNIT
    @param x The UD60x18 number for which to calculate the binary logarithm.
    @return result The binary logarithm as a UD60x18 number.
    @custom:smtchecker abstract-function-nondet

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    log2: async (params: CallParams<Ud60x18WrapperHelper3Args['obj']['log2(uint256)uint256'] | Ud60x18WrapperHelper3Args['tuple']['log2(uint256)uint256']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(Ud60x18WrapperHelper3ParamsFactory.log2(params))
      return {...result, return: result.return as unknown as (undefined | Ud60x18WrapperHelper3Returns['log2(uint256)uint256'])}
    },

    /**
     * Makes a call to the UD60x18Wrapper__Helper3 smart contract using the `msb(uint256)uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
    * @notice Finds the zero-based index of the first 1 in the binary representation of x.
    @dev See the note on "msb" in this Wikipedia article: https://en.wikipedia.org/wiki/Find_first_set
    The implementation satisfies these properties:
    $$
    \begin{cases}
      x = 0 \implies \text{msb}(x) = 0 \\
      x > 0 \implies x \gg \text{msb}(x) = 1
    \end{cases}
    $$
    Each step below is equivalent to this high-level code:
    ```solidity
    if (x >= 2 ** 128) {
        x >>= 128;
        result += 128;
    }
    ```
    Where 128 is replaced with each respective power of two factor. The Yul instructions used below are:
    - "gt" is "greater than"
    - "or" is the OR bitwise operator
    - "shl" is "shift left"
    - "shr" is "shift right"
    See the full high-level implementation here:
    https://gist.github.com/PaulRBerg/f932f8693f2733e30c4d479e8e980948
    @param x The uint256 number for which to find the index of the most significant bit.
    @return result The index of the most significant bit as a uint256.
    @custom:smtchecker abstract-function-nondet

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    msb: async (params: CallParams<Ud60x18WrapperHelper3Args['obj']['msb(uint256)uint256'] | Ud60x18WrapperHelper3Args['tuple']['msb(uint256)uint256']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(Ud60x18WrapperHelper3ParamsFactory.msb(params))
      return {...result, return: result.return as unknown as (undefined | Ud60x18WrapperHelper3Returns['msb(uint256)uint256'])}
    },

    /**
     * Makes a call to the UD60x18Wrapper__Helper3 smart contract using the `mul(uint256,uint256)uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
    * @notice Multiplies two UD60x18 numbers together, returning a new UD60x18 number.
    @dev Uses {Common.mulDiv} to enable overflow-safe multiplication and division.
    Notes:
    - Refer to the notes in {Common.mulDiv}.
    Requirements:
    - Refer to the requirements in {Common.mulDiv}.
    @dev See the documentation in {Common.mulDiv18}.
    @param x The multiplicand as a UD60x18 number.
    @param y The multiplier as a UD60x18 number.
    @return result The product as a UD60x18 number.
    @custom:smtchecker abstract-function-nondet

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    mul: async (params: CallParams<Ud60x18WrapperHelper3Args['obj']['mul(uint256,uint256)uint256'] | Ud60x18WrapperHelper3Args['tuple']['mul(uint256,uint256)uint256']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(Ud60x18WrapperHelper3ParamsFactory.mul(params))
      return {...result, return: result.return as unknown as (undefined | Ud60x18WrapperHelper3Returns['mul(uint256,uint256)uint256'])}
    },

    /**
     * Makes a call to the UD60x18Wrapper__Helper3 smart contract using the `unwrap(uint256)uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @notice Unwraps a UD60x18 number into uint256.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    unwrap: async (params: CallParams<Ud60x18WrapperHelper3Args['obj']['unwrap(uint256)uint256'] | Ud60x18WrapperHelper3Args['tuple']['unwrap(uint256)uint256']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(Ud60x18WrapperHelper3ParamsFactory.unwrap(params))
      return {...result, return: result.return as unknown as (undefined | Ud60x18WrapperHelper3Returns['unwrap(uint256)uint256'])}
    },

    /**
     * Makes a call to the UD60x18Wrapper__Helper3 smart contract using the `wrap(uint256)uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @notice Wraps a uint256 number into the UD60x18 value type.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    wrap: async (params: CallParams<Ud60x18WrapperHelper3Args['obj']['wrap(uint256)uint256'] | Ud60x18WrapperHelper3Args['tuple']['wrap(uint256)uint256']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(Ud60x18WrapperHelper3ParamsFactory.wrap(params))
      return {...result, return: result.return as unknown as (undefined | Ud60x18WrapperHelper3Returns['wrap(uint256)uint256'])}
    },

    /**
     * Makes a call to the UD60x18Wrapper__Helper3 smart contract using the `__init__(uint64,uint64,byte[])void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    __init: async (params: CallParams<Ud60x18WrapperHelper3Args['obj']['__init__(uint64,uint64,byte[])void'] | Ud60x18WrapperHelper3Args['tuple']['__init__(uint64,uint64,byte[])void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(Ud60x18WrapperHelper3ParamsFactory.__init(params))
      return {...result, return: result.return as unknown as (undefined | Ud60x18WrapperHelper3Returns['__init__(uint64,uint64,byte[])void'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new Ud60x18WrapperHelper3Client(this.appClient.clone(params))
  }

  /**
   * Makes a readonly (simulated) call to the UD60x18Wrapper__Helper3 smart contract using the `ln(uint256)uint256` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
  * @notice Calculates the natural logarithm of x using the following formula:
  $$
  ln{x} = log_2{x} / log_2{e}
  $$
  @dev Notes:
  - Refer to the notes in {log2}.
  - The precision isn't sufficiently fine-grained to return exactly `UNIT` when the input is `E`.
  Requirements:
  - Refer to the requirements in {log2}.
  @param x The UD60x18 number for which to calculate the natural logarithm.
  @return result The natural logarithm as a UD60x18 number.
  @custom:smtchecker abstract-function-nondet

   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async ln(params: CallParams<Ud60x18WrapperHelper3Args['obj']['ln(uint256)uint256'] | Ud60x18WrapperHelper3Args['tuple']['ln(uint256)uint256']>) {
    const result = await this.appClient.send.call(Ud60x18WrapperHelper3ParamsFactory.ln(params))
    return result.return as unknown as Ud60x18WrapperHelper3Returns['ln(uint256)uint256']
  }

  /**
   * Makes a readonly (simulated) call to the UD60x18Wrapper__Helper3 smart contract using the `div(uint256,uint256)uint256` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
  * @notice Divides two UD60x18 numbers, returning a new UD60x18 number.
  @dev Uses {Common.mulDiv} to enable overflow-safe multiplication and division.
  Notes:
  - Refer to the notes in {Common.mulDiv}.
  Requirements:
  - Refer to the requirements in {Common.mulDiv}.
  @param x The numerator as a UD60x18 number.
  @param y The denominator as a UD60x18 number.
  @return result The quotient as a UD60x18 number.
  @custom:smtchecker abstract-function-nondet

   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async div(params: CallParams<Ud60x18WrapperHelper3Args['obj']['div(uint256,uint256)uint256'] | Ud60x18WrapperHelper3Args['tuple']['div(uint256,uint256)uint256']>) {
    const result = await this.appClient.send.call(Ud60x18WrapperHelper3ParamsFactory.div(params))
    return result.return as unknown as Ud60x18WrapperHelper3Returns['div(uint256,uint256)uint256']
  }

  /**
   * Makes a readonly (simulated) call to the UD60x18Wrapper__Helper3 smart contract using the `log2(uint256)uint256` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
  * @notice Calculates the binary logarithm of x using the iterative approximation algorithm:
  $$
  log_2{x} = n + log_2{y}, \text{ where } y = x*2^{-n}, \ y \in [1, 2)
  $$
  For $0 \leq x \lt 1$, the input is inverted:
  $$
  log_2{x} = -log_2{\frac{1}{x}}
  $$
  @dev See https://en.wikipedia.org/wiki/Binary_logarithm#Iterative_approximation
  Notes:
  - Due to the lossy precision of the iterative approximation, the results are not perfectly accurate to the last decimal.
  Requirements:
  - x ≥ UNIT
  @param x The UD60x18 number for which to calculate the binary logarithm.
  @return result The binary logarithm as a UD60x18 number.
  @custom:smtchecker abstract-function-nondet

   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async log2(params: CallParams<Ud60x18WrapperHelper3Args['obj']['log2(uint256)uint256'] | Ud60x18WrapperHelper3Args['tuple']['log2(uint256)uint256']>) {
    const result = await this.appClient.send.call(Ud60x18WrapperHelper3ParamsFactory.log2(params))
    return result.return as unknown as Ud60x18WrapperHelper3Returns['log2(uint256)uint256']
  }

  /**
   * Makes a readonly (simulated) call to the UD60x18Wrapper__Helper3 smart contract using the `msb(uint256)uint256` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
  * @notice Finds the zero-based index of the first 1 in the binary representation of x.
  @dev See the note on "msb" in this Wikipedia article: https://en.wikipedia.org/wiki/Find_first_set
  The implementation satisfies these properties:
  $$
  \begin{cases}
    x = 0 \implies \text{msb}(x) = 0 \\
    x > 0 \implies x \gg \text{msb}(x) = 1
  \end{cases}
  $$
  Each step below is equivalent to this high-level code:
  ```solidity
  if (x >= 2 ** 128) {
      x >>= 128;
      result += 128;
  }
  ```
  Where 128 is replaced with each respective power of two factor. The Yul instructions used below are:
  - "gt" is "greater than"
  - "or" is the OR bitwise operator
  - "shl" is "shift left"
  - "shr" is "shift right"
  See the full high-level implementation here:
  https://gist.github.com/PaulRBerg/f932f8693f2733e30c4d479e8e980948
  @param x The uint256 number for which to find the index of the most significant bit.
  @return result The index of the most significant bit as a uint256.
  @custom:smtchecker abstract-function-nondet

   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async msb(params: CallParams<Ud60x18WrapperHelper3Args['obj']['msb(uint256)uint256'] | Ud60x18WrapperHelper3Args['tuple']['msb(uint256)uint256']>) {
    const result = await this.appClient.send.call(Ud60x18WrapperHelper3ParamsFactory.msb(params))
    return result.return as unknown as Ud60x18WrapperHelper3Returns['msb(uint256)uint256']
  }

  /**
   * Makes a readonly (simulated) call to the UD60x18Wrapper__Helper3 smart contract using the `mul(uint256,uint256)uint256` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
  * @notice Multiplies two UD60x18 numbers together, returning a new UD60x18 number.
  @dev Uses {Common.mulDiv} to enable overflow-safe multiplication and division.
  Notes:
  - Refer to the notes in {Common.mulDiv}.
  Requirements:
  - Refer to the requirements in {Common.mulDiv}.
  @dev See the documentation in {Common.mulDiv18}.
  @param x The multiplicand as a UD60x18 number.
  @param y The multiplier as a UD60x18 number.
  @return result The product as a UD60x18 number.
  @custom:smtchecker abstract-function-nondet

   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async mul(params: CallParams<Ud60x18WrapperHelper3Args['obj']['mul(uint256,uint256)uint256'] | Ud60x18WrapperHelper3Args['tuple']['mul(uint256,uint256)uint256']>) {
    const result = await this.appClient.send.call(Ud60x18WrapperHelper3ParamsFactory.mul(params))
    return result.return as unknown as Ud60x18WrapperHelper3Returns['mul(uint256,uint256)uint256']
  }

  /**
   * Makes a readonly (simulated) call to the UD60x18Wrapper__Helper3 smart contract using the `unwrap(uint256)uint256` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @notice Unwraps a UD60x18 number into uint256.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async unwrap(params: CallParams<Ud60x18WrapperHelper3Args['obj']['unwrap(uint256)uint256'] | Ud60x18WrapperHelper3Args['tuple']['unwrap(uint256)uint256']>) {
    const result = await this.appClient.send.call(Ud60x18WrapperHelper3ParamsFactory.unwrap(params))
    return result.return as unknown as Ud60x18WrapperHelper3Returns['unwrap(uint256)uint256']
  }

  /**
   * Makes a readonly (simulated) call to the UD60x18Wrapper__Helper3 smart contract using the `wrap(uint256)uint256` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @notice Wraps a uint256 number into the UD60x18 value type.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async wrap(params: CallParams<Ud60x18WrapperHelper3Args['obj']['wrap(uint256)uint256'] | Ud60x18WrapperHelper3Args['tuple']['wrap(uint256)uint256']>) {
    const result = await this.appClient.send.call(Ud60x18WrapperHelper3ParamsFactory.wrap(params))
    return result.return as unknown as Ud60x18WrapperHelper3Returns['wrap(uint256)uint256']
  }

  /**
   * Methods to access state for the current UD60x18Wrapper__Helper3 app
   */
  state = {
    /**
     * Methods to access global state for the current UD60x18Wrapper__Helper3 app
     */
    global: {
      /**
       * Get all current keyed values from global state
       */
      getAll: async (): Promise<Partial<Expand<GlobalKeysState>>> => {
        const result = await this.appClient.state.global.getAll()
        return {
          o: result.o,
          p: result.p,
          s: new BinaryStateValue(result.s),
        }
      },
      /**
       * Get the current value of the o key in global state
       */
      o: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("o")) as bigint | undefined },
      /**
       * Get the current value of the p key in global state
       */
      p: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("p")) as bigint | undefined },
      /**
       * Get the current value of the s key in global state
       */
      s: async (): Promise<BinaryState> => { return new BinaryStateValue((await this.appClient.state.global.getValue("s")) as Uint8Array | undefined) },
    },
  }

  public newGroup(): Ud60x18WrapperHelper3Composer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a ln(uint256)uint256 method call against the UD60x18Wrapper__Helper3 contract
       */
      ln(params: CallParams<Ud60x18WrapperHelper3Args['obj']['ln(uint256)uint256'] | Ud60x18WrapperHelper3Args['tuple']['ln(uint256)uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.ln(params)))
        resultMappers.push((v) => client.decodeReturnValue('ln(uint256)uint256', v))
        return this
      },
      /**
       * Add a div(uint256,uint256)uint256 method call against the UD60x18Wrapper__Helper3 contract
       */
      div(params: CallParams<Ud60x18WrapperHelper3Args['obj']['div(uint256,uint256)uint256'] | Ud60x18WrapperHelper3Args['tuple']['div(uint256,uint256)uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.div(params)))
        resultMappers.push((v) => client.decodeReturnValue('div(uint256,uint256)uint256', v))
        return this
      },
      /**
       * Add a log2(uint256)uint256 method call against the UD60x18Wrapper__Helper3 contract
       */
      log2(params: CallParams<Ud60x18WrapperHelper3Args['obj']['log2(uint256)uint256'] | Ud60x18WrapperHelper3Args['tuple']['log2(uint256)uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.log2(params)))
        resultMappers.push((v) => client.decodeReturnValue('log2(uint256)uint256', v))
        return this
      },
      /**
       * Add a msb(uint256)uint256 method call against the UD60x18Wrapper__Helper3 contract
       */
      msb(params: CallParams<Ud60x18WrapperHelper3Args['obj']['msb(uint256)uint256'] | Ud60x18WrapperHelper3Args['tuple']['msb(uint256)uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.msb(params)))
        resultMappers.push((v) => client.decodeReturnValue('msb(uint256)uint256', v))
        return this
      },
      /**
       * Add a mul(uint256,uint256)uint256 method call against the UD60x18Wrapper__Helper3 contract
       */
      mul(params: CallParams<Ud60x18WrapperHelper3Args['obj']['mul(uint256,uint256)uint256'] | Ud60x18WrapperHelper3Args['tuple']['mul(uint256,uint256)uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.mul(params)))
        resultMappers.push((v) => client.decodeReturnValue('mul(uint256,uint256)uint256', v))
        return this
      },
      /**
       * Add a unwrap(uint256)uint256 method call against the UD60x18Wrapper__Helper3 contract
       */
      unwrap(params: CallParams<Ud60x18WrapperHelper3Args['obj']['unwrap(uint256)uint256'] | Ud60x18WrapperHelper3Args['tuple']['unwrap(uint256)uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.unwrap(params)))
        resultMappers.push((v) => client.decodeReturnValue('unwrap(uint256)uint256', v))
        return this
      },
      /**
       * Add a wrap(uint256)uint256 method call against the UD60x18Wrapper__Helper3 contract
       */
      wrap(params: CallParams<Ud60x18WrapperHelper3Args['obj']['wrap(uint256)uint256'] | Ud60x18WrapperHelper3Args['tuple']['wrap(uint256)uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.wrap(params)))
        resultMappers.push((v) => client.decodeReturnValue('wrap(uint256)uint256', v))
        return this
      },
      /**
       * Add a __init__(uint64,uint64,byte[])void method call against the UD60x18Wrapper__Helper3 contract
       */
      __init(params: CallParams<Ud60x18WrapperHelper3Args['obj']['__init__(uint64,uint64,byte[])void'] | Ud60x18WrapperHelper3Args['tuple']['__init__(uint64,uint64,byte[])void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.__init(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a clear state call to the UD60x18Wrapper__Helper3 contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as Ud60x18WrapperHelper3Composer
  }
}
export type Ud60x18WrapperHelper3Composer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the ln(uint256)uint256 ABI method.
   *
  * @notice Calculates the natural logarithm of x using the following formula:
  $$
  ln{x} = log_2{x} / log_2{e}
  $$
  @dev Notes:
  - Refer to the notes in {log2}.
  - The precision isn't sufficiently fine-grained to return exactly `UNIT` when the input is `E`.
  Requirements:
  - Refer to the requirements in {log2}.
  @param x The UD60x18 number for which to calculate the natural logarithm.
  @return result The natural logarithm as a UD60x18 number.
  @custom:smtchecker abstract-function-nondet

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  ln(params?: CallParams<Ud60x18WrapperHelper3Args['obj']['ln(uint256)uint256'] | Ud60x18WrapperHelper3Args['tuple']['ln(uint256)uint256']>): Ud60x18WrapperHelper3Composer<[...TReturns, Ud60x18WrapperHelper3Returns['ln(uint256)uint256'] | undefined]>

  /**
   * Calls the div(uint256,uint256)uint256 ABI method.
   *
  * @notice Divides two UD60x18 numbers, returning a new UD60x18 number.
  @dev Uses {Common.mulDiv} to enable overflow-safe multiplication and division.
  Notes:
  - Refer to the notes in {Common.mulDiv}.
  Requirements:
  - Refer to the requirements in {Common.mulDiv}.
  @param x The numerator as a UD60x18 number.
  @param y The denominator as a UD60x18 number.
  @return result The quotient as a UD60x18 number.
  @custom:smtchecker abstract-function-nondet

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  div(params?: CallParams<Ud60x18WrapperHelper3Args['obj']['div(uint256,uint256)uint256'] | Ud60x18WrapperHelper3Args['tuple']['div(uint256,uint256)uint256']>): Ud60x18WrapperHelper3Composer<[...TReturns, Ud60x18WrapperHelper3Returns['div(uint256,uint256)uint256'] | undefined]>

  /**
   * Calls the log2(uint256)uint256 ABI method.
   *
  * @notice Calculates the binary logarithm of x using the iterative approximation algorithm:
  $$
  log_2{x} = n + log_2{y}, \text{ where } y = x*2^{-n}, \ y \in [1, 2)
  $$
  For $0 \leq x \lt 1$, the input is inverted:
  $$
  log_2{x} = -log_2{\frac{1}{x}}
  $$
  @dev See https://en.wikipedia.org/wiki/Binary_logarithm#Iterative_approximation
  Notes:
  - Due to the lossy precision of the iterative approximation, the results are not perfectly accurate to the last decimal.
  Requirements:
  - x ≥ UNIT
  @param x The UD60x18 number for which to calculate the binary logarithm.
  @return result The binary logarithm as a UD60x18 number.
  @custom:smtchecker abstract-function-nondet

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  log2(params?: CallParams<Ud60x18WrapperHelper3Args['obj']['log2(uint256)uint256'] | Ud60x18WrapperHelper3Args['tuple']['log2(uint256)uint256']>): Ud60x18WrapperHelper3Composer<[...TReturns, Ud60x18WrapperHelper3Returns['log2(uint256)uint256'] | undefined]>

  /**
   * Calls the msb(uint256)uint256 ABI method.
   *
  * @notice Finds the zero-based index of the first 1 in the binary representation of x.
  @dev See the note on "msb" in this Wikipedia article: https://en.wikipedia.org/wiki/Find_first_set
  The implementation satisfies these properties:
  $$
  \begin{cases}
    x = 0 \implies \text{msb}(x) = 0 \\
    x > 0 \implies x \gg \text{msb}(x) = 1
  \end{cases}
  $$
  Each step below is equivalent to this high-level code:
  ```solidity
  if (x >= 2 ** 128) {
      x >>= 128;
      result += 128;
  }
  ```
  Where 128 is replaced with each respective power of two factor. The Yul instructions used below are:
  - "gt" is "greater than"
  - "or" is the OR bitwise operator
  - "shl" is "shift left"
  - "shr" is "shift right"
  See the full high-level implementation here:
  https://gist.github.com/PaulRBerg/f932f8693f2733e30c4d479e8e980948
  @param x The uint256 number for which to find the index of the most significant bit.
  @return result The index of the most significant bit as a uint256.
  @custom:smtchecker abstract-function-nondet

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  msb(params?: CallParams<Ud60x18WrapperHelper3Args['obj']['msb(uint256)uint256'] | Ud60x18WrapperHelper3Args['tuple']['msb(uint256)uint256']>): Ud60x18WrapperHelper3Composer<[...TReturns, Ud60x18WrapperHelper3Returns['msb(uint256)uint256'] | undefined]>

  /**
   * Calls the mul(uint256,uint256)uint256 ABI method.
   *
  * @notice Multiplies two UD60x18 numbers together, returning a new UD60x18 number.
  @dev Uses {Common.mulDiv} to enable overflow-safe multiplication and division.
  Notes:
  - Refer to the notes in {Common.mulDiv}.
  Requirements:
  - Refer to the requirements in {Common.mulDiv}.
  @dev See the documentation in {Common.mulDiv18}.
  @param x The multiplicand as a UD60x18 number.
  @param y The multiplier as a UD60x18 number.
  @return result The product as a UD60x18 number.
  @custom:smtchecker abstract-function-nondet

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  mul(params?: CallParams<Ud60x18WrapperHelper3Args['obj']['mul(uint256,uint256)uint256'] | Ud60x18WrapperHelper3Args['tuple']['mul(uint256,uint256)uint256']>): Ud60x18WrapperHelper3Composer<[...TReturns, Ud60x18WrapperHelper3Returns['mul(uint256,uint256)uint256'] | undefined]>

  /**
   * Calls the unwrap(uint256)uint256 ABI method.
   *
   * @notice Unwraps a UD60x18 number into uint256.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  unwrap(params?: CallParams<Ud60x18WrapperHelper3Args['obj']['unwrap(uint256)uint256'] | Ud60x18WrapperHelper3Args['tuple']['unwrap(uint256)uint256']>): Ud60x18WrapperHelper3Composer<[...TReturns, Ud60x18WrapperHelper3Returns['unwrap(uint256)uint256'] | undefined]>

  /**
   * Calls the wrap(uint256)uint256 ABI method.
   *
   * @notice Wraps a uint256 number into the UD60x18 value type.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  wrap(params?: CallParams<Ud60x18WrapperHelper3Args['obj']['wrap(uint256)uint256'] | Ud60x18WrapperHelper3Args['tuple']['wrap(uint256)uint256']>): Ud60x18WrapperHelper3Composer<[...TReturns, Ud60x18WrapperHelper3Returns['wrap(uint256)uint256'] | undefined]>

  /**
   * Calls the __init__(uint64,uint64,byte[])void ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  __init(params?: CallParams<Ud60x18WrapperHelper3Args['obj']['__init__(uint64,uint64,byte[])void'] | Ud60x18WrapperHelper3Args['tuple']['__init__(uint64,uint64,byte[])void']>): Ud60x18WrapperHelper3Composer<[...TReturns, Ud60x18WrapperHelper3Returns['__init__(uint64,uint64,byte[])void'] | undefined]>

  /**
   * Makes a clear_state call to an existing instance of the UD60x18Wrapper__Helper3 smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): Ud60x18WrapperHelper3Composer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): Ud60x18WrapperHelper3Composer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<Ud60x18WrapperHelper3ComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<Ud60x18WrapperHelper3ComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<Ud60x18WrapperHelper3ComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<Ud60x18WrapperHelper3ComposerResults<TReturns>>
}
export type Ud60x18WrapperHelper3ComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

