/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"PoolManager__Helper20","structs":{},"methods":[{"name":"FullMath.mulDiv","args":[{"type":"uint256","name":"a"},{"type":"uint256","name":"b"},{"type":"uint256","name":"denominator"}],"returns":{"type":"uint256"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"desc":"@notice Calculates floor(a×b÷denominator) with full precision. Throws if result overflows a uint256 or denominator == 0\n @param a The multiplicand\n @param b The multiplier\n @param denominator The divisor\n @return result The 256-bit result\n @dev Credit to Remco Bloemen under MIT license https://xn--2-umb.com/21/muldiv","events":[],"recommendations":{}},{"name":"UnsafeMath.simpleMulDiv","args":[{"type":"uint256","name":"a"},{"type":"uint256","name":"b"},{"type":"uint256","name":"denominator"}],"returns":{"type":"uint256"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"desc":"@notice Calculates floor(a×b÷denominator)\n @dev division by 0 will return 0, and should be checked externally\n @param a The multiplicand\n @param b The multiplier\n @param denominator The divisor\n @return result The 256-bit result, floor(a×b÷denominator)","events":[],"recommendations":{}},{"name":"__init__","args":[{"type":"uint64","name":"o"},{"type":"uint64","name":"p"},{"type":"byte[]","name":"s"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}}],"arcs":[22,28],"desc":"Helper contract 20 for PoolManager","networks":{},"state":{"schema":{"global":{"ints":2,"bytes":1},"local":{"ints":0,"bytes":0}},"keys":{"global":{"o":{"keyType":"AVMString","valueType":"AVMUint64","key":"bw=="},"p":{"keyType":"AVMString","valueType":"AVMUint64","key":"cA=="},"s":{"keyType":"AVMString","valueType":"AVMBytes","key":"cw=="}},"local":{},"box":{}},"maps":{"global":{},"local":{},"box":{}}},"bareActions":{"create":["NoOp"],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[747],"errorMessage":"helper: already initialized"},{"pc":[533,637],"errorMessage":"helper: must be called in group"},{"pc":[547,651],"errorMessage":"helper: sender mismatch"},{"pc":[540,644],"errorMessage":"helper: unauthorized caller"},{"pc":[564,668],"errorMessage":"helper: wrong prev chunk"},{"pc":[576,680],"errorMessage":"helper: wrong prev method"},{"pc":[733],"errorMessage":"invalid array length header"},{"pc":[740],"errorMessage":"invalid number of bytes for arc4.dynamic_array<arc4.uint8>"},{"pc":[507,515,523,611,619,627],"errorMessage":"invalid number of bytes for arc4.uint256"},{"pc":[716,726],"errorMessage":"invalid number of bytes for arc4.uint64"},{"pc":[593,697],"errorMessage":"overflow"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"events":[]} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


/**
 * The argument types for the PoolManagerHelper20 contract
 */
export type PoolManagerHelper20Args = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'FullMath.mulDiv(uint256,uint256,uint256)uint256': {
      a: bigint | number
      b: bigint | number
      denominator: bigint | number
    }
    'UnsafeMath.simpleMulDiv(uint256,uint256,uint256)uint256': {
      a: bigint | number
      b: bigint | number
      denominator: bigint | number
    }
    '__init__(uint64,uint64,byte[])void': {
      o: bigint | number
      p: bigint | number
      s: Uint8Array
    }
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'FullMath.mulDiv(uint256,uint256,uint256)uint256': [a: bigint | number, b: bigint | number, denominator: bigint | number]
    'UnsafeMath.simpleMulDiv(uint256,uint256,uint256)uint256': [a: bigint | number, b: bigint | number, denominator: bigint | number]
    '__init__(uint64,uint64,byte[])void': [o: bigint | number, p: bigint | number, s: Uint8Array]
  }
}

/**
 * The return type for each method
 */
export type PoolManagerHelper20Returns = {
  'FullMath.mulDiv(uint256,uint256,uint256)uint256': bigint
  'UnsafeMath.simpleMulDiv(uint256,uint256,uint256)uint256': bigint
  '__init__(uint64,uint64,byte[])void': void
}

/**
 * Defines the types of available calls and state of the PoolManagerHelper20 smart contract.
 */
export type PoolManagerHelper20Types = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'FullMath.mulDiv(uint256,uint256,uint256)uint256' | 'FullMath.mulDiv', {
      argsObj: PoolManagerHelper20Args['obj']['FullMath.mulDiv(uint256,uint256,uint256)uint256']
      argsTuple: PoolManagerHelper20Args['tuple']['FullMath.mulDiv(uint256,uint256,uint256)uint256']
      returns: PoolManagerHelper20Returns['FullMath.mulDiv(uint256,uint256,uint256)uint256']
    }>
    & Record<'UnsafeMath.simpleMulDiv(uint256,uint256,uint256)uint256' | 'UnsafeMath.simpleMulDiv', {
      argsObj: PoolManagerHelper20Args['obj']['UnsafeMath.simpleMulDiv(uint256,uint256,uint256)uint256']
      argsTuple: PoolManagerHelper20Args['tuple']['UnsafeMath.simpleMulDiv(uint256,uint256,uint256)uint256']
      returns: PoolManagerHelper20Returns['UnsafeMath.simpleMulDiv(uint256,uint256,uint256)uint256']
    }>
    & Record<'__init__(uint64,uint64,byte[])void' | '__init__', {
      argsObj: PoolManagerHelper20Args['obj']['__init__(uint64,uint64,byte[])void']
      argsTuple: PoolManagerHelper20Args['tuple']['__init__(uint64,uint64,byte[])void']
      returns: PoolManagerHelper20Returns['__init__(uint64,uint64,byte[])void']
    }>
  /**
   * Defines the shape of the state of the application.
   */
  state: {
    global: {
      keys: {
        o: bigint
        p: bigint
        s: BinaryState
      }
      maps: {}
    }
  }
}

/**
 * Defines the possible abi call signatures.
 */
export type PoolManagerHelper20Signatures = keyof PoolManagerHelper20Types['methods']
/**
 * Defines the possible abi call signatures for methods that return a non-void value.
 */
export type PoolManagerHelper20NonVoidMethodSignatures = keyof PoolManagerHelper20Types['methods'] extends infer T ? T extends keyof PoolManagerHelper20Types['methods'] ? MethodReturn<T> extends void ? never : T  : never : never
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the PoolManagerHelper20 smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends PoolManagerHelper20Signatures> = PoolManagerHelper20Types['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the PoolManagerHelper20 smart contract to the method's return type
 */
export type MethodReturn<TSignature extends PoolManagerHelper20Signatures> = PoolManagerHelper20Types['methods'][TSignature]['returns']

/**
 * Defines the shape of the keyed global state of the application.
 */
export type GlobalKeysState = PoolManagerHelper20Types['state']['global']['keys']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the PoolManagerHelper20 smart contract
 */
export abstract class PoolManagerHelper20ParamsFactory {
  /**
   * Constructs a no op call for the FullMath.mulDiv(uint256,uint256,uint256)uint256 ABI method
   *
  * @notice Calculates floor(a×b÷denominator) with full precision. Throws if result overflows a uint256 or denominator == 0
  @param a The multiplicand
  @param b The multiplier
  @param denominator The divisor
  @return result The 256-bit result
  @dev Credit to Remco Bloemen under MIT license https://xn--2-umb.com/21/muldiv

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static fullMathMulDiv(params: CallParams<PoolManagerHelper20Args['obj']['FullMath.mulDiv(uint256,uint256,uint256)uint256'] | PoolManagerHelper20Args['tuple']['FullMath.mulDiv(uint256,uint256,uint256)uint256']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'FullMath.mulDiv(uint256,uint256,uint256)uint256' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.a, params.args.b, params.args.denominator],
    }
  }
  /**
   * Constructs a no op call for the UnsafeMath.simpleMulDiv(uint256,uint256,uint256)uint256 ABI method
   *
  * @notice Calculates floor(a×b÷denominator)
  @dev division by 0 will return 0, and should be checked externally
  @param a The multiplicand
  @param b The multiplier
  @param denominator The divisor
  @return result The 256-bit result, floor(a×b÷denominator)

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static unsafeMathSimpleMulDiv(params: CallParams<PoolManagerHelper20Args['obj']['UnsafeMath.simpleMulDiv(uint256,uint256,uint256)uint256'] | PoolManagerHelper20Args['tuple']['UnsafeMath.simpleMulDiv(uint256,uint256,uint256)uint256']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'UnsafeMath.simpleMulDiv(uint256,uint256,uint256)uint256' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.a, params.args.b, params.args.denominator],
    }
  }
  /**
   * Constructs a no op call for the __init__(uint64,uint64,byte[])void ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static __init(params: CallParams<PoolManagerHelper20Args['obj']['__init__(uint64,uint64,byte[])void'] | PoolManagerHelper20Args['tuple']['__init__(uint64,uint64,byte[])void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: '__init__(uint64,uint64,byte[])void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.o, params.args.p, params.args.s],
    }
  }
}

/**
 * A client to make calls to the PoolManager__Helper20 smart contract
 */
export class PoolManagerHelper20Client {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `PoolManagerHelper20Client`
   *
   * @param appClient An `AppClient` instance which has been created with the PoolManagerHelper20 app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `PoolManagerHelper20Client`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Checks for decode errors on the given return value and maps the return value to the return type for the given method
   * @returns The typed return value or undefined if there was no value
   */
  decodeReturnValue<TSignature extends PoolManagerHelper20NonVoidMethodSignatures>(method: TSignature, returnValue: ABIReturn | undefined) {
    return returnValue !== undefined ? getArc56ReturnValue<MethodReturn<TSignature>>(returnValue, this.appClient.getABIMethod(method), APP_SPEC.structs) : undefined
  }

  /**
   * Returns a new `PoolManagerHelper20Client` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<PoolManagerHelper20Client> {
    return new PoolManagerHelper20Client(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `PoolManagerHelper20Client` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<PoolManagerHelper20Client> {
    return new PoolManagerHelper20Client(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Makes a clear_state call to an existing instance of the PoolManager__Helper20 smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the PoolManager__Helper20 smart contract using the `FullMath.mulDiv(uint256,uint256,uint256)uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
    * @notice Calculates floor(a×b÷denominator) with full precision. Throws if result overflows a uint256 or denominator == 0
    @param a The multiplicand
    @param b The multiplier
    @param denominator The divisor
    @return result The 256-bit result
    @dev Credit to Remco Bloemen under MIT license https://xn--2-umb.com/21/muldiv

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    fullMathMulDiv: (params: CallParams<PoolManagerHelper20Args['obj']['FullMath.mulDiv(uint256,uint256,uint256)uint256'] | PoolManagerHelper20Args['tuple']['FullMath.mulDiv(uint256,uint256,uint256)uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(PoolManagerHelper20ParamsFactory.fullMathMulDiv(params))
    },

    /**
     * Makes a call to the PoolManager__Helper20 smart contract using the `UnsafeMath.simpleMulDiv(uint256,uint256,uint256)uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
    * @notice Calculates floor(a×b÷denominator)
    @dev division by 0 will return 0, and should be checked externally
    @param a The multiplicand
    @param b The multiplier
    @param denominator The divisor
    @return result The 256-bit result, floor(a×b÷denominator)

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    unsafeMathSimpleMulDiv: (params: CallParams<PoolManagerHelper20Args['obj']['UnsafeMath.simpleMulDiv(uint256,uint256,uint256)uint256'] | PoolManagerHelper20Args['tuple']['UnsafeMath.simpleMulDiv(uint256,uint256,uint256)uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(PoolManagerHelper20ParamsFactory.unsafeMathSimpleMulDiv(params))
    },

    /**
     * Makes a call to the PoolManager__Helper20 smart contract using the `__init__(uint64,uint64,byte[])void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    __init: (params: CallParams<PoolManagerHelper20Args['obj']['__init__(uint64,uint64,byte[])void'] | PoolManagerHelper20Args['tuple']['__init__(uint64,uint64,byte[])void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(PoolManagerHelper20ParamsFactory.__init(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Makes a clear_state call to an existing instance of the PoolManager__Helper20 smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the PoolManager__Helper20 smart contract using the `FullMath.mulDiv(uint256,uint256,uint256)uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
    * @notice Calculates floor(a×b÷denominator) with full precision. Throws if result overflows a uint256 or denominator == 0
    @param a The multiplicand
    @param b The multiplier
    @param denominator The divisor
    @return result The 256-bit result
    @dev Credit to Remco Bloemen under MIT license https://xn--2-umb.com/21/muldiv

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    fullMathMulDiv: (params: CallParams<PoolManagerHelper20Args['obj']['FullMath.mulDiv(uint256,uint256,uint256)uint256'] | PoolManagerHelper20Args['tuple']['FullMath.mulDiv(uint256,uint256,uint256)uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(PoolManagerHelper20ParamsFactory.fullMathMulDiv(params))
    },

    /**
     * Makes a call to the PoolManager__Helper20 smart contract using the `UnsafeMath.simpleMulDiv(uint256,uint256,uint256)uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
    * @notice Calculates floor(a×b÷denominator)
    @dev division by 0 will return 0, and should be checked externally
    @param a The multiplicand
    @param b The multiplier
    @param denominator The divisor
    @return result The 256-bit result, floor(a×b÷denominator)

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    unsafeMathSimpleMulDiv: (params: CallParams<PoolManagerHelper20Args['obj']['UnsafeMath.simpleMulDiv(uint256,uint256,uint256)uint256'] | PoolManagerHelper20Args['tuple']['UnsafeMath.simpleMulDiv(uint256,uint256,uint256)uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(PoolManagerHelper20ParamsFactory.unsafeMathSimpleMulDiv(params))
    },

    /**
     * Makes a call to the PoolManager__Helper20 smart contract using the `__init__(uint64,uint64,byte[])void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    __init: (params: CallParams<PoolManagerHelper20Args['obj']['__init__(uint64,uint64,byte[])void'] | PoolManagerHelper20Args['tuple']['__init__(uint64,uint64,byte[])void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(PoolManagerHelper20ParamsFactory.__init(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Makes a clear_state call to an existing instance of the PoolManager__Helper20 smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the PoolManager__Helper20 smart contract using the `FullMath.mulDiv(uint256,uint256,uint256)uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
    * @notice Calculates floor(a×b÷denominator) with full precision. Throws if result overflows a uint256 or denominator == 0
    @param a The multiplicand
    @param b The multiplier
    @param denominator The divisor
    @return result The 256-bit result
    @dev Credit to Remco Bloemen under MIT license https://xn--2-umb.com/21/muldiv

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    fullMathMulDiv: async (params: CallParams<PoolManagerHelper20Args['obj']['FullMath.mulDiv(uint256,uint256,uint256)uint256'] | PoolManagerHelper20Args['tuple']['FullMath.mulDiv(uint256,uint256,uint256)uint256']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(PoolManagerHelper20ParamsFactory.fullMathMulDiv(params))
      return {...result, return: result.return as unknown as (undefined | PoolManagerHelper20Returns['FullMath.mulDiv(uint256,uint256,uint256)uint256'])}
    },

    /**
     * Makes a call to the PoolManager__Helper20 smart contract using the `UnsafeMath.simpleMulDiv(uint256,uint256,uint256)uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
    * @notice Calculates floor(a×b÷denominator)
    @dev division by 0 will return 0, and should be checked externally
    @param a The multiplicand
    @param b The multiplier
    @param denominator The divisor
    @return result The 256-bit result, floor(a×b÷denominator)

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    unsafeMathSimpleMulDiv: async (params: CallParams<PoolManagerHelper20Args['obj']['UnsafeMath.simpleMulDiv(uint256,uint256,uint256)uint256'] | PoolManagerHelper20Args['tuple']['UnsafeMath.simpleMulDiv(uint256,uint256,uint256)uint256']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(PoolManagerHelper20ParamsFactory.unsafeMathSimpleMulDiv(params))
      return {...result, return: result.return as unknown as (undefined | PoolManagerHelper20Returns['UnsafeMath.simpleMulDiv(uint256,uint256,uint256)uint256'])}
    },

    /**
     * Makes a call to the PoolManager__Helper20 smart contract using the `__init__(uint64,uint64,byte[])void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    __init: async (params: CallParams<PoolManagerHelper20Args['obj']['__init__(uint64,uint64,byte[])void'] | PoolManagerHelper20Args['tuple']['__init__(uint64,uint64,byte[])void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(PoolManagerHelper20ParamsFactory.__init(params))
      return {...result, return: result.return as unknown as (undefined | PoolManagerHelper20Returns['__init__(uint64,uint64,byte[])void'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new PoolManagerHelper20Client(this.appClient.clone(params))
  }

  /**
   * Makes a readonly (simulated) call to the PoolManager__Helper20 smart contract using the `FullMath.mulDiv(uint256,uint256,uint256)uint256` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
  * @notice Calculates floor(a×b÷denominator) with full precision. Throws if result overflows a uint256 or denominator == 0
  @param a The multiplicand
  @param b The multiplier
  @param denominator The divisor
  @return result The 256-bit result
  @dev Credit to Remco Bloemen under MIT license https://xn--2-umb.com/21/muldiv

   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async fullMathMulDiv(params: CallParams<PoolManagerHelper20Args['obj']['FullMath.mulDiv(uint256,uint256,uint256)uint256'] | PoolManagerHelper20Args['tuple']['FullMath.mulDiv(uint256,uint256,uint256)uint256']>) {
    const result = await this.appClient.send.call(PoolManagerHelper20ParamsFactory.fullMathMulDiv(params))
    return result.return as unknown as PoolManagerHelper20Returns['FullMath.mulDiv(uint256,uint256,uint256)uint256']
  }

  /**
   * Makes a readonly (simulated) call to the PoolManager__Helper20 smart contract using the `UnsafeMath.simpleMulDiv(uint256,uint256,uint256)uint256` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
  * @notice Calculates floor(a×b÷denominator)
  @dev division by 0 will return 0, and should be checked externally
  @param a The multiplicand
  @param b The multiplier
  @param denominator The divisor
  @return result The 256-bit result, floor(a×b÷denominator)

   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async unsafeMathSimpleMulDiv(params: CallParams<PoolManagerHelper20Args['obj']['UnsafeMath.simpleMulDiv(uint256,uint256,uint256)uint256'] | PoolManagerHelper20Args['tuple']['UnsafeMath.simpleMulDiv(uint256,uint256,uint256)uint256']>) {
    const result = await this.appClient.send.call(PoolManagerHelper20ParamsFactory.unsafeMathSimpleMulDiv(params))
    return result.return as unknown as PoolManagerHelper20Returns['UnsafeMath.simpleMulDiv(uint256,uint256,uint256)uint256']
  }

  /**
   * Methods to access state for the current PoolManager__Helper20 app
   */
  state = {
    /**
     * Methods to access global state for the current PoolManager__Helper20 app
     */
    global: {
      /**
       * Get all current keyed values from global state
       */
      getAll: async (): Promise<Partial<Expand<GlobalKeysState>>> => {
        const result = await this.appClient.state.global.getAll()
        return {
          o: result.o,
          p: result.p,
          s: new BinaryStateValue(result.s),
        }
      },
      /**
       * Get the current value of the o key in global state
       */
      o: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("o")) as bigint | undefined },
      /**
       * Get the current value of the p key in global state
       */
      p: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("p")) as bigint | undefined },
      /**
       * Get the current value of the s key in global state
       */
      s: async (): Promise<BinaryState> => { return new BinaryStateValue((await this.appClient.state.global.getValue("s")) as Uint8Array | undefined) },
    },
  }

  public newGroup(): PoolManagerHelper20Composer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a FullMath.mulDiv(uint256,uint256,uint256)uint256 method call against the PoolManager__Helper20 contract
       */
      fullMathMulDiv(params: CallParams<PoolManagerHelper20Args['obj']['FullMath.mulDiv(uint256,uint256,uint256)uint256'] | PoolManagerHelper20Args['tuple']['FullMath.mulDiv(uint256,uint256,uint256)uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.fullMathMulDiv(params)))
        resultMappers.push((v) => client.decodeReturnValue('FullMath.mulDiv(uint256,uint256,uint256)uint256', v))
        return this
      },
      /**
       * Add a UnsafeMath.simpleMulDiv(uint256,uint256,uint256)uint256 method call against the PoolManager__Helper20 contract
       */
      unsafeMathSimpleMulDiv(params: CallParams<PoolManagerHelper20Args['obj']['UnsafeMath.simpleMulDiv(uint256,uint256,uint256)uint256'] | PoolManagerHelper20Args['tuple']['UnsafeMath.simpleMulDiv(uint256,uint256,uint256)uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.unsafeMathSimpleMulDiv(params)))
        resultMappers.push((v) => client.decodeReturnValue('UnsafeMath.simpleMulDiv(uint256,uint256,uint256)uint256', v))
        return this
      },
      /**
       * Add a __init__(uint64,uint64,byte[])void method call against the PoolManager__Helper20 contract
       */
      __init(params: CallParams<PoolManagerHelper20Args['obj']['__init__(uint64,uint64,byte[])void'] | PoolManagerHelper20Args['tuple']['__init__(uint64,uint64,byte[])void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.__init(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a clear state call to the PoolManager__Helper20 contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as PoolManagerHelper20Composer
  }
}
export type PoolManagerHelper20Composer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the FullMath.mulDiv(uint256,uint256,uint256)uint256 ABI method.
   *
  * @notice Calculates floor(a×b÷denominator) with full precision. Throws if result overflows a uint256 or denominator == 0
  @param a The multiplicand
  @param b The multiplier
  @param denominator The divisor
  @return result The 256-bit result
  @dev Credit to Remco Bloemen under MIT license https://xn--2-umb.com/21/muldiv

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  fullMathMulDiv(params?: CallParams<PoolManagerHelper20Args['obj']['FullMath.mulDiv(uint256,uint256,uint256)uint256'] | PoolManagerHelper20Args['tuple']['FullMath.mulDiv(uint256,uint256,uint256)uint256']>): PoolManagerHelper20Composer<[...TReturns, PoolManagerHelper20Returns['FullMath.mulDiv(uint256,uint256,uint256)uint256'] | undefined]>

  /**
   * Calls the UnsafeMath.simpleMulDiv(uint256,uint256,uint256)uint256 ABI method.
   *
  * @notice Calculates floor(a×b÷denominator)
  @dev division by 0 will return 0, and should be checked externally
  @param a The multiplicand
  @param b The multiplier
  @param denominator The divisor
  @return result The 256-bit result, floor(a×b÷denominator)

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  unsafeMathSimpleMulDiv(params?: CallParams<PoolManagerHelper20Args['obj']['UnsafeMath.simpleMulDiv(uint256,uint256,uint256)uint256'] | PoolManagerHelper20Args['tuple']['UnsafeMath.simpleMulDiv(uint256,uint256,uint256)uint256']>): PoolManagerHelper20Composer<[...TReturns, PoolManagerHelper20Returns['UnsafeMath.simpleMulDiv(uint256,uint256,uint256)uint256'] | undefined]>

  /**
   * Calls the __init__(uint64,uint64,byte[])void ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  __init(params?: CallParams<PoolManagerHelper20Args['obj']['__init__(uint64,uint64,byte[])void'] | PoolManagerHelper20Args['tuple']['__init__(uint64,uint64,byte[])void']>): PoolManagerHelper20Composer<[...TReturns, PoolManagerHelper20Returns['__init__(uint64,uint64,byte[])void'] | undefined]>

  /**
   * Makes a clear_state call to an existing instance of the PoolManager__Helper20 smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): PoolManagerHelper20Composer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): PoolManagerHelper20Composer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<PoolManagerHelper20ComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<PoolManagerHelper20ComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<PoolManagerHelper20ComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<PoolManagerHelper20ComposerResults<TReturns>>
}
export type PoolManagerHelper20ComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

