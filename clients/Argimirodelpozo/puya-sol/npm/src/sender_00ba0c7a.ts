/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"sender","structs":{},"methods":[{"name":"__postInit","args":[],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}},{"name":"__fallback","args":[],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}},{"name":"forward","args":[],"returns":{"type":"bool"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}},{"name":"clear","args":[],"returns":{"type":"bool"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}},{"name":"val","args":[],"returns":{"type":"uint256"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}}],"arcs":[22,28],"networks":{},"state":{"schema":{"global":{"ints":1,"bytes":1},"local":{"ints":0,"bytes":0}},"keys":{"global":{"rec":{"keyType":"AVMString","valueType":"address","key":"cmVj"},"__ctor_pending":{"keyType":"AVMString","valueType":"AVMUint64","key":"X19jdG9yX3BlbmRpbmc="}},"local":{},"box":{"savedData":{"keyType":"AVMString","valueType":"AVMBytes","key":"c2F2ZWREYXRh"}}},"maps":{"global":{},"local":{},"box":{}}},"bareActions":{"create":[],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[428],"errorMessage":"__postInit already called"},{"pc":[222,371],"errorMessage":"check rec exists"},{"pc":[217,292,324,515],"errorMessage":"not payable"},{"pc":[256],"errorMessage":"overflow"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"events":[]} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


/**
 * The argument types for the Sender contract
 */
export type SenderArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    '__postInit()void': Record<string, never>
    '__fallback()void': Record<string, never>
    'forward()bool': Record<string, never>
    'clear()bool': Record<string, never>
    'val()uint256': Record<string, never>
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    '__postInit()void': []
    '__fallback()void': []
    'forward()bool': []
    'clear()bool': []
    'val()uint256': []
  }
}

/**
 * The return type for each method
 */
export type SenderReturns = {
  '__postInit()void': void
  '__fallback()void': void
  'forward()bool': boolean
  'clear()bool': boolean
  'val()uint256': bigint
}

/**
 * Defines the types of available calls and state of the Sender smart contract.
 */
export type SenderTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'__postInit()void' | '__postInit', {
      argsObj: SenderArgs['obj']['__postInit()void']
      argsTuple: SenderArgs['tuple']['__postInit()void']
      returns: SenderReturns['__postInit()void']
    }>
    & Record<'__fallback()void' | '__fallback', {
      argsObj: SenderArgs['obj']['__fallback()void']
      argsTuple: SenderArgs['tuple']['__fallback()void']
      returns: SenderReturns['__fallback()void']
    }>
    & Record<'forward()bool' | 'forward', {
      argsObj: SenderArgs['obj']['forward()bool']
      argsTuple: SenderArgs['tuple']['forward()bool']
      returns: SenderReturns['forward()bool']
    }>
    & Record<'clear()bool' | 'clear', {
      argsObj: SenderArgs['obj']['clear()bool']
      argsTuple: SenderArgs['tuple']['clear()bool']
      returns: SenderReturns['clear()bool']
    }>
    & Record<'val()uint256' | 'val', {
      argsObj: SenderArgs['obj']['val()uint256']
      argsTuple: SenderArgs['tuple']['val()uint256']
      returns: SenderReturns['val()uint256']
    }>
  /**
   * Defines the shape of the state of the application.
   */
  state: {
    global: {
      keys: {
        rec: string
        __ctorPending: bigint
      }
      maps: {}
    }
    box: {
      keys: {
        savedData: BinaryState
      }
      maps: {}
    }
  }
}

/**
 * Defines the possible abi call signatures.
 */
export type SenderSignatures = keyof SenderTypes['methods']
/**
 * Defines the possible abi call signatures for methods that return a non-void value.
 */
export type SenderNonVoidMethodSignatures = keyof SenderTypes['methods'] extends infer T ? T extends keyof SenderTypes['methods'] ? MethodReturn<T> extends void ? never : T  : never : never
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the Sender smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends SenderSignatures> = SenderTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the Sender smart contract to the method's return type
 */
export type MethodReturn<TSignature extends SenderSignatures> = SenderTypes['methods'][TSignature]['returns']

/**
 * Defines the shape of the keyed global state of the application.
 */
export type GlobalKeysState = SenderTypes['state']['global']['keys']

/**
 * Defines the shape of the keyed box state of the application.
 */
export type BoxKeysState = SenderTypes['state']['box']['keys']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the Sender smart contract
 */
export abstract class SenderParamsFactory {
  /**
   * Constructs a no op call for the __postInit()void ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static __postInit(params: CallParams<SenderArgs['obj']['__postInit()void'] | SenderArgs['tuple']['__postInit()void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: '__postInit()void' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the __fallback()void ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static __fallback(params: CallParams<SenderArgs['obj']['__fallback()void'] | SenderArgs['tuple']['__fallback()void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: '__fallback()void' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the forward()bool ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static forward(params: CallParams<SenderArgs['obj']['forward()bool'] | SenderArgs['tuple']['forward()bool']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'forward()bool' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the clear()bool ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static clear(params: CallParams<SenderArgs['obj']['clear()bool'] | SenderArgs['tuple']['clear()bool']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'clear()bool' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the val()uint256 ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static val(params: CallParams<SenderArgs['obj']['val()uint256'] | SenderArgs['tuple']['val()uint256']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'val()uint256' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
}

/**
 * A client to make calls to the sender smart contract
 */
export class SenderClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `SenderClient`
   *
   * @param appClient An `AppClient` instance which has been created with the Sender app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `SenderClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Checks for decode errors on the given return value and maps the return value to the return type for the given method
   * @returns The typed return value or undefined if there was no value
   */
  decodeReturnValue<TSignature extends SenderNonVoidMethodSignatures>(method: TSignature, returnValue: ABIReturn | undefined) {
    return returnValue !== undefined ? getArc56ReturnValue<MethodReturn<TSignature>>(returnValue, this.appClient.getABIMethod(method), APP_SPEC.structs) : undefined
  }

  /**
   * Returns a new `SenderClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<SenderClient> {
    return new SenderClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `SenderClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<SenderClient> {
    return new SenderClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Makes a clear_state call to an existing instance of the sender smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the sender smart contract using the `__postInit()void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    __postInit: (params: CallParams<SenderArgs['obj']['__postInit()void'] | SenderArgs['tuple']['__postInit()void']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(SenderParamsFactory.__postInit(params))
    },

    /**
     * Makes a call to the sender smart contract using the `__fallback()void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    __fallback: (params: CallParams<SenderArgs['obj']['__fallback()void'] | SenderArgs['tuple']['__fallback()void']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(SenderParamsFactory.__fallback(params))
    },

    /**
     * Makes a call to the sender smart contract using the `forward()bool` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    forward: (params: CallParams<SenderArgs['obj']['forward()bool'] | SenderArgs['tuple']['forward()bool']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(SenderParamsFactory.forward(params))
    },

    /**
     * Makes a call to the sender smart contract using the `clear()bool` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    clear: (params: CallParams<SenderArgs['obj']['clear()bool'] | SenderArgs['tuple']['clear()bool']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(SenderParamsFactory.clear(params))
    },

    /**
     * Makes a call to the sender smart contract using the `val()uint256` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    val: (params: CallParams<SenderArgs['obj']['val()uint256'] | SenderArgs['tuple']['val()uint256']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(SenderParamsFactory.val(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Makes a clear_state call to an existing instance of the sender smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the sender smart contract using the `__postInit()void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    __postInit: (params: CallParams<SenderArgs['obj']['__postInit()void'] | SenderArgs['tuple']['__postInit()void']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(SenderParamsFactory.__postInit(params))
    },

    /**
     * Makes a call to the sender smart contract using the `__fallback()void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    __fallback: (params: CallParams<SenderArgs['obj']['__fallback()void'] | SenderArgs['tuple']['__fallback()void']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(SenderParamsFactory.__fallback(params))
    },

    /**
     * Makes a call to the sender smart contract using the `forward()bool` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    forward: (params: CallParams<SenderArgs['obj']['forward()bool'] | SenderArgs['tuple']['forward()bool']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(SenderParamsFactory.forward(params))
    },

    /**
     * Makes a call to the sender smart contract using the `clear()bool` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    clear: (params: CallParams<SenderArgs['obj']['clear()bool'] | SenderArgs['tuple']['clear()bool']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(SenderParamsFactory.clear(params))
    },

    /**
     * Makes a call to the sender smart contract using the `val()uint256` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    val: (params: CallParams<SenderArgs['obj']['val()uint256'] | SenderArgs['tuple']['val()uint256']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(SenderParamsFactory.val(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Makes a clear_state call to an existing instance of the sender smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the sender smart contract using the `__postInit()void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    __postInit: async (params: CallParams<SenderArgs['obj']['__postInit()void'] | SenderArgs['tuple']['__postInit()void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(SenderParamsFactory.__postInit(params))
      return {...result, return: result.return as unknown as (undefined | SenderReturns['__postInit()void'])}
    },

    /**
     * Makes a call to the sender smart contract using the `__fallback()void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    __fallback: async (params: CallParams<SenderArgs['obj']['__fallback()void'] | SenderArgs['tuple']['__fallback()void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(SenderParamsFactory.__fallback(params))
      return {...result, return: result.return as unknown as (undefined | SenderReturns['__fallback()void'])}
    },

    /**
     * Makes a call to the sender smart contract using the `forward()bool` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    forward: async (params: CallParams<SenderArgs['obj']['forward()bool'] | SenderArgs['tuple']['forward()bool']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(SenderParamsFactory.forward(params))
      return {...result, return: result.return as unknown as (undefined | SenderReturns['forward()bool'])}
    },

    /**
     * Makes a call to the sender smart contract using the `clear()bool` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    clear: async (params: CallParams<SenderArgs['obj']['clear()bool'] | SenderArgs['tuple']['clear()bool']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(SenderParamsFactory.clear(params))
      return {...result, return: result.return as unknown as (undefined | SenderReturns['clear()bool'])}
    },

    /**
     * Makes a call to the sender smart contract using the `val()uint256` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    val: async (params: CallParams<SenderArgs['obj']['val()uint256'] | SenderArgs['tuple']['val()uint256']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(SenderParamsFactory.val(params))
      return {...result, return: result.return as unknown as (undefined | SenderReturns['val()uint256'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new SenderClient(this.appClient.clone(params))
  }

  /**
   * Methods to access state for the current sender app
   */
  state = {
    /**
     * Methods to access global state for the current sender app
     */
    global: {
      /**
       * Get all current keyed values from global state
       */
      getAll: async (): Promise<Partial<Expand<GlobalKeysState>>> => {
        const result = await this.appClient.state.global.getAll()
        return {
          rec: result.rec,
          __ctorPending: result.__ctor_pending,
        }
      },
      /**
       * Get the current value of the rec key in global state
       */
      rec: async (): Promise<string | undefined> => { return (await this.appClient.state.global.getValue("rec")) as string | undefined },
      /**
       * Get the current value of the __ctor_pending key in global state
       */
      __ctorPending: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("__ctor_pending")) as bigint | undefined },
    },
    /**
     * Methods to access box state for the current sender app
     */
    box: {
      /**
       * Get all current keyed values from box state
       */
      getAll: async (): Promise<Partial<Expand<BoxKeysState>>> => {
        const result = await this.appClient.state.box.getAll()
        return {
          savedData: new BinaryStateValue(result.savedData),
        }
      },
      /**
       * Get the current value of the savedData key in box state
       */
      savedData: async (): Promise<BinaryState> => { return new BinaryStateValue((await this.appClient.state.box.getValue("savedData")) as Uint8Array | undefined) },
    },
  }

  public newGroup(): SenderComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a __postInit()void method call against the sender contract
       */
      __postInit(params: CallParams<SenderArgs['obj']['__postInit()void'] | SenderArgs['tuple']['__postInit()void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.__postInit(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a __fallback()void method call against the sender contract
       */
      __fallback(params: CallParams<SenderArgs['obj']['__fallback()void'] | SenderArgs['tuple']['__fallback()void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.__fallback(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a forward()bool method call against the sender contract
       */
      forward(params: CallParams<SenderArgs['obj']['forward()bool'] | SenderArgs['tuple']['forward()bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.forward(params)))
        resultMappers.push((v) => client.decodeReturnValue('forward()bool', v))
        return this
      },
      /**
       * Add a clear()bool method call against the sender contract
       */
      clear(params: CallParams<SenderArgs['obj']['clear()bool'] | SenderArgs['tuple']['clear()bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.clear(params)))
        resultMappers.push((v) => client.decodeReturnValue('clear()bool', v))
        return this
      },
      /**
       * Add a val()uint256 method call against the sender contract
       */
      val(params: CallParams<SenderArgs['obj']['val()uint256'] | SenderArgs['tuple']['val()uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.val(params)))
        resultMappers.push((v) => client.decodeReturnValue('val()uint256', v))
        return this
      },
      /**
       * Add a clear state call to the sender contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as SenderComposer
  }
}
export type SenderComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the __postInit()void ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  __postInit(params?: CallParams<SenderArgs['obj']['__postInit()void'] | SenderArgs['tuple']['__postInit()void']>): SenderComposer<[...TReturns, SenderReturns['__postInit()void'] | undefined]>

  /**
   * Calls the __fallback()void ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  __fallback(params?: CallParams<SenderArgs['obj']['__fallback()void'] | SenderArgs['tuple']['__fallback()void']>): SenderComposer<[...TReturns, SenderReturns['__fallback()void'] | undefined]>

  /**
   * Calls the forward()bool ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  forward(params?: CallParams<SenderArgs['obj']['forward()bool'] | SenderArgs['tuple']['forward()bool']>): SenderComposer<[...TReturns, SenderReturns['forward()bool'] | undefined]>

  /**
   * Calls the clear()bool ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clear(params?: CallParams<SenderArgs['obj']['clear()bool'] | SenderArgs['tuple']['clear()bool']>): SenderComposer<[...TReturns, SenderReturns['clear()bool'] | undefined]>

  /**
   * Calls the val()uint256 ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  val(params?: CallParams<SenderArgs['obj']['val()uint256'] | SenderArgs['tuple']['val()uint256']>): SenderComposer<[...TReturns, SenderReturns['val()uint256'] | undefined]>

  /**
   * Makes a clear_state call to an existing instance of the sender smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): SenderComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): SenderComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<SenderComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<SenderComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<SenderComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<SenderComposerResults<TReturns>>
}
export type SenderComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

