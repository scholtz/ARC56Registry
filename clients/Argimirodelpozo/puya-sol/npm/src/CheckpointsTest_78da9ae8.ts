/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"CheckpointsTest","structs":{"tuple":[{"name":"exists","type":"bool"},{"name":"key","type":"uint512"},{"name":"value","type":"uint512"}]},"methods":[{"name":"push","args":[{"type":"uint512","name":"key"},{"type":"uint512","name":"value"}],"returns":{"type":"(uint512,uint512)"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"@dev Pushes a (key, value) pair. Keys must be non-decreasing.\n Returns (oldValue, newValue).","events":[],"recommendations":{}},{"name":"latest","args":[],"returns":{"type":"uint512"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"desc":"@dev Returns the latest stored value.","events":[],"recommendations":{}},{"name":"latestCheckpoint","args":[],"returns":{"type":"(bool,uint512,uint512)","struct":"tuple"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"desc":"@dev Returns whether a checkpoint exists, and if so, the latest key and value.","events":[],"recommendations":{}},{"name":"getAtKey","args":[{"type":"uint512","name":"key"}],"returns":{"type":"uint512"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"desc":"@dev Returns the value stored at a given key.","events":[],"recommendations":{}},{"name":"hasCheckpoint","args":[],"returns":{"type":"bool"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"desc":"@dev Returns whether any checkpoint has been pushed.","events":[],"recommendations":{}}],"arcs":[22,28],"desc":"@dev Simplified Checkpoints pattern using mappings.\n Real OZ uses a sorted array + binary search for gas-efficient historical lookups.\n We use a mapping since AVM box storage doesn't benefit from the same gas optimizations.","networks":{},"state":{"schema":{"global":{"ints":1,"bytes":2},"local":{"ints":0,"bytes":0}},"keys":{"global":{"_latestKey":{"keyType":"AVMString","valueType":"AVMBytes","key":"X2xhdGVzdEtleQ=="},"_latestValue":{"keyType":"AVMString","valueType":"AVMBytes","key":"X2xhdGVzdFZhbHVl"},"_hasCheckpoint":{"keyType":"AVMString","valueType":"AVMUint64","key":"X2hhc0NoZWNrcG9pbnQ="}},"local":{},"box":{}},"maps":{"global":{},"local":{},"box":{"_values":{"keyType":"AVMBytes","valueType":"AVMBytes","prefix":"X3ZhbHVlcw=="}}}},"bareActions":{"create":[],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[163],"errorMessage":"Checkpoint: decreasing keys"},{"pc":[152,251,346],"errorMessage":"check _hasCheckpoint exists"},{"pc":[159,255],"errorMessage":"check _latestKey exists"},{"pc":[148,233,259],"errorMessage":"check _latestValue exists"},{"pc":[135,144,307],"errorMessage":"invalid number of bytes for arc4.uint512"},{"pc":[210,218,238,272,289,335],"errorMessage":"overflow"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"events":[]} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


// Type definitions for ARC-56 structs

export type Tuple = {
  exists: boolean,
  key: bigint,
  value: bigint
}


/**
 * Converts the ABI tuple representation of a tuple to the struct representation
 */
export function TupleFromTuple(abiTuple: [boolean, bigint, bigint]) {
  return getABIStructFromABITuple(abiTuple, APP_SPEC.structs.tuple, APP_SPEC.structs) as Tuple
}

/**
 * The argument types for the CheckpointsTest contract
 */
export type CheckpointsTestArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'push(uint512,uint512)(uint512,uint512)': {
      key: bigint | number
      value: bigint | number
    }
    'latest()uint512': Record<string, never>
    'latestCheckpoint()(bool,uint512,uint512)': Record<string, never>
    'getAtKey(uint512)uint512': {
      key: bigint | number
    }
    'hasCheckpoint()bool': Record<string, never>
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'push(uint512,uint512)(uint512,uint512)': [key: bigint | number, value: bigint | number]
    'latest()uint512': []
    'latestCheckpoint()(bool,uint512,uint512)': []
    'getAtKey(uint512)uint512': [key: bigint | number]
    'hasCheckpoint()bool': []
  }
}

/**
 * The return type for each method
 */
export type CheckpointsTestReturns = {
  'push(uint512,uint512)(uint512,uint512)': [bigint, bigint]
  'latest()uint512': bigint
  'latestCheckpoint()(bool,uint512,uint512)': Tuple
  'getAtKey(uint512)uint512': bigint
  'hasCheckpoint()bool': boolean
}

/**
 * Defines the types of available calls and state of the CheckpointsTest smart contract.
 */
export type CheckpointsTestTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'push(uint512,uint512)(uint512,uint512)' | 'push', {
      argsObj: CheckpointsTestArgs['obj']['push(uint512,uint512)(uint512,uint512)']
      argsTuple: CheckpointsTestArgs['tuple']['push(uint512,uint512)(uint512,uint512)']
      returns: CheckpointsTestReturns['push(uint512,uint512)(uint512,uint512)']
    }>
    & Record<'latest()uint512' | 'latest', {
      argsObj: CheckpointsTestArgs['obj']['latest()uint512']
      argsTuple: CheckpointsTestArgs['tuple']['latest()uint512']
      returns: CheckpointsTestReturns['latest()uint512']
    }>
    & Record<'latestCheckpoint()(bool,uint512,uint512)' | 'latestCheckpoint', {
      argsObj: CheckpointsTestArgs['obj']['latestCheckpoint()(bool,uint512,uint512)']
      argsTuple: CheckpointsTestArgs['tuple']['latestCheckpoint()(bool,uint512,uint512)']
      returns: CheckpointsTestReturns['latestCheckpoint()(bool,uint512,uint512)']
    }>
    & Record<'getAtKey(uint512)uint512' | 'getAtKey', {
      argsObj: CheckpointsTestArgs['obj']['getAtKey(uint512)uint512']
      argsTuple: CheckpointsTestArgs['tuple']['getAtKey(uint512)uint512']
      returns: CheckpointsTestReturns['getAtKey(uint512)uint512']
    }>
    & Record<'hasCheckpoint()bool' | 'hasCheckpoint', {
      argsObj: CheckpointsTestArgs['obj']['hasCheckpoint()bool']
      argsTuple: CheckpointsTestArgs['tuple']['hasCheckpoint()bool']
      returns: CheckpointsTestReturns['hasCheckpoint()bool']
    }>
  /**
   * Defines the shape of the state of the application.
   */
  state: {
    global: {
      keys: {
        _latestKey: BinaryState
        _latestValue: BinaryState
        _hasCheckpoint: bigint
      }
      maps: {}
    }
    box: {
      keys: {}
      maps: {
        _values: Map<Uint8Array | string, Uint8Array>
      }
    }
  }
}

/**
 * Defines the possible abi call signatures.
 */
export type CheckpointsTestSignatures = keyof CheckpointsTestTypes['methods']
/**
 * Defines the possible abi call signatures for methods that return a non-void value.
 */
export type CheckpointsTestNonVoidMethodSignatures = keyof CheckpointsTestTypes['methods'] extends infer T ? T extends keyof CheckpointsTestTypes['methods'] ? MethodReturn<T> extends void ? never : T  : never : never
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the CheckpointsTest smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends CheckpointsTestSignatures> = CheckpointsTestTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the CheckpointsTest smart contract to the method's return type
 */
export type MethodReturn<TSignature extends CheckpointsTestSignatures> = CheckpointsTestTypes['methods'][TSignature]['returns']

/**
 * Defines the shape of the keyed global state of the application.
 */
export type GlobalKeysState = CheckpointsTestTypes['state']['global']['keys']

/**
 * Defines the shape of the keyed box state of the application.
 */
export type BoxKeysState = CheckpointsTestTypes['state']['box']['keys']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the CheckpointsTest smart contract
 */
export abstract class CheckpointsTestParamsFactory {
  /**
   * Constructs a no op call for the push(uint512,uint512)(uint512,uint512) ABI method
   *
  * @dev Pushes a (key, value) pair. Keys must be non-decreasing.
  Returns (oldValue, newValue).

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static push(params: CallParams<CheckpointsTestArgs['obj']['push(uint512,uint512)(uint512,uint512)'] | CheckpointsTestArgs['tuple']['push(uint512,uint512)(uint512,uint512)']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'push(uint512,uint512)(uint512,uint512)' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.key, params.args.value],
    }
  }
  /**
   * Constructs a no op call for the latest()uint512 ABI method
   *
   * @dev Returns the latest stored value.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static latest(params: CallParams<CheckpointsTestArgs['obj']['latest()uint512'] | CheckpointsTestArgs['tuple']['latest()uint512']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'latest()uint512' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the latestCheckpoint()(bool,uint512,uint512) ABI method
   *
   * @dev Returns whether a checkpoint exists, and if so, the latest key and value.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static latestCheckpoint(params: CallParams<CheckpointsTestArgs['obj']['latestCheckpoint()(bool,uint512,uint512)'] | CheckpointsTestArgs['tuple']['latestCheckpoint()(bool,uint512,uint512)']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'latestCheckpoint()(bool,uint512,uint512)' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the getAtKey(uint512)uint512 ABI method
   *
   * @dev Returns the value stored at a given key.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static getAtKey(params: CallParams<CheckpointsTestArgs['obj']['getAtKey(uint512)uint512'] | CheckpointsTestArgs['tuple']['getAtKey(uint512)uint512']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'getAtKey(uint512)uint512' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.key],
    }
  }
  /**
   * Constructs a no op call for the hasCheckpoint()bool ABI method
   *
   * @dev Returns whether any checkpoint has been pushed.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static hasCheckpoint(params: CallParams<CheckpointsTestArgs['obj']['hasCheckpoint()bool'] | CheckpointsTestArgs['tuple']['hasCheckpoint()bool']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'hasCheckpoint()bool' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
}

/**
 * A client to make calls to the CheckpointsTest smart contract
 */
export class CheckpointsTestClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `CheckpointsTestClient`
   *
   * @param appClient An `AppClient` instance which has been created with the CheckpointsTest app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `CheckpointsTestClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Checks for decode errors on the given return value and maps the return value to the return type for the given method
   * @returns The typed return value or undefined if there was no value
   */
  decodeReturnValue<TSignature extends CheckpointsTestNonVoidMethodSignatures>(method: TSignature, returnValue: ABIReturn | undefined) {
    return returnValue !== undefined ? getArc56ReturnValue<MethodReturn<TSignature>>(returnValue, this.appClient.getABIMethod(method), APP_SPEC.structs) : undefined
  }

  /**
   * Returns a new `CheckpointsTestClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<CheckpointsTestClient> {
    return new CheckpointsTestClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `CheckpointsTestClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<CheckpointsTestClient> {
    return new CheckpointsTestClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Makes a clear_state call to an existing instance of the CheckpointsTest smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the CheckpointsTest smart contract using the `push(uint512,uint512)(uint512,uint512)` ABI method.
     *
    * @dev Pushes a (key, value) pair. Keys must be non-decreasing.
    Returns (oldValue, newValue).

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    push: (params: CallParams<CheckpointsTestArgs['obj']['push(uint512,uint512)(uint512,uint512)'] | CheckpointsTestArgs['tuple']['push(uint512,uint512)(uint512,uint512)']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(CheckpointsTestParamsFactory.push(params))
    },

    /**
     * Makes a call to the CheckpointsTest smart contract using the `latest()uint512` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @dev Returns the latest stored value.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    latest: (params: CallParams<CheckpointsTestArgs['obj']['latest()uint512'] | CheckpointsTestArgs['tuple']['latest()uint512']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(CheckpointsTestParamsFactory.latest(params))
    },

    /**
     * Makes a call to the CheckpointsTest smart contract using the `latestCheckpoint()(bool,uint512,uint512)` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @dev Returns whether a checkpoint exists, and if so, the latest key and value.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    latestCheckpoint: (params: CallParams<CheckpointsTestArgs['obj']['latestCheckpoint()(bool,uint512,uint512)'] | CheckpointsTestArgs['tuple']['latestCheckpoint()(bool,uint512,uint512)']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(CheckpointsTestParamsFactory.latestCheckpoint(params))
    },

    /**
     * Makes a call to the CheckpointsTest smart contract using the `getAtKey(uint512)uint512` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @dev Returns the value stored at a given key.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    getAtKey: (params: CallParams<CheckpointsTestArgs['obj']['getAtKey(uint512)uint512'] | CheckpointsTestArgs['tuple']['getAtKey(uint512)uint512']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(CheckpointsTestParamsFactory.getAtKey(params))
    },

    /**
     * Makes a call to the CheckpointsTest smart contract using the `hasCheckpoint()bool` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @dev Returns whether any checkpoint has been pushed.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    hasCheckpoint: (params: CallParams<CheckpointsTestArgs['obj']['hasCheckpoint()bool'] | CheckpointsTestArgs['tuple']['hasCheckpoint()bool']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(CheckpointsTestParamsFactory.hasCheckpoint(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Makes a clear_state call to an existing instance of the CheckpointsTest smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the CheckpointsTest smart contract using the `push(uint512,uint512)(uint512,uint512)` ABI method.
     *
    * @dev Pushes a (key, value) pair. Keys must be non-decreasing.
    Returns (oldValue, newValue).

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    push: (params: CallParams<CheckpointsTestArgs['obj']['push(uint512,uint512)(uint512,uint512)'] | CheckpointsTestArgs['tuple']['push(uint512,uint512)(uint512,uint512)']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(CheckpointsTestParamsFactory.push(params))
    },

    /**
     * Makes a call to the CheckpointsTest smart contract using the `latest()uint512` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @dev Returns the latest stored value.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    latest: (params: CallParams<CheckpointsTestArgs['obj']['latest()uint512'] | CheckpointsTestArgs['tuple']['latest()uint512']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(CheckpointsTestParamsFactory.latest(params))
    },

    /**
     * Makes a call to the CheckpointsTest smart contract using the `latestCheckpoint()(bool,uint512,uint512)` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @dev Returns whether a checkpoint exists, and if so, the latest key and value.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    latestCheckpoint: (params: CallParams<CheckpointsTestArgs['obj']['latestCheckpoint()(bool,uint512,uint512)'] | CheckpointsTestArgs['tuple']['latestCheckpoint()(bool,uint512,uint512)']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(CheckpointsTestParamsFactory.latestCheckpoint(params))
    },

    /**
     * Makes a call to the CheckpointsTest smart contract using the `getAtKey(uint512)uint512` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @dev Returns the value stored at a given key.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    getAtKey: (params: CallParams<CheckpointsTestArgs['obj']['getAtKey(uint512)uint512'] | CheckpointsTestArgs['tuple']['getAtKey(uint512)uint512']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(CheckpointsTestParamsFactory.getAtKey(params))
    },

    /**
     * Makes a call to the CheckpointsTest smart contract using the `hasCheckpoint()bool` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @dev Returns whether any checkpoint has been pushed.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    hasCheckpoint: (params: CallParams<CheckpointsTestArgs['obj']['hasCheckpoint()bool'] | CheckpointsTestArgs['tuple']['hasCheckpoint()bool']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(CheckpointsTestParamsFactory.hasCheckpoint(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Makes a clear_state call to an existing instance of the CheckpointsTest smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the CheckpointsTest smart contract using the `push(uint512,uint512)(uint512,uint512)` ABI method.
     *
    * @dev Pushes a (key, value) pair. Keys must be non-decreasing.
    Returns (oldValue, newValue).

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    push: async (params: CallParams<CheckpointsTestArgs['obj']['push(uint512,uint512)(uint512,uint512)'] | CheckpointsTestArgs['tuple']['push(uint512,uint512)(uint512,uint512)']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(CheckpointsTestParamsFactory.push(params))
      return {...result, return: result.return as unknown as (undefined | CheckpointsTestReturns['push(uint512,uint512)(uint512,uint512)'])}
    },

    /**
     * Makes a call to the CheckpointsTest smart contract using the `latest()uint512` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @dev Returns the latest stored value.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    latest: async (params: CallParams<CheckpointsTestArgs['obj']['latest()uint512'] | CheckpointsTestArgs['tuple']['latest()uint512']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(CheckpointsTestParamsFactory.latest(params))
      return {...result, return: result.return as unknown as (undefined | CheckpointsTestReturns['latest()uint512'])}
    },

    /**
     * Makes a call to the CheckpointsTest smart contract using the `latestCheckpoint()(bool,uint512,uint512)` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @dev Returns whether a checkpoint exists, and if so, the latest key and value.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    latestCheckpoint: async (params: CallParams<CheckpointsTestArgs['obj']['latestCheckpoint()(bool,uint512,uint512)'] | CheckpointsTestArgs['tuple']['latestCheckpoint()(bool,uint512,uint512)']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(CheckpointsTestParamsFactory.latestCheckpoint(params))
      return {...result, return: result.return as unknown as (undefined | CheckpointsTestReturns['latestCheckpoint()(bool,uint512,uint512)'])}
    },

    /**
     * Makes a call to the CheckpointsTest smart contract using the `getAtKey(uint512)uint512` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @dev Returns the value stored at a given key.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    getAtKey: async (params: CallParams<CheckpointsTestArgs['obj']['getAtKey(uint512)uint512'] | CheckpointsTestArgs['tuple']['getAtKey(uint512)uint512']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(CheckpointsTestParamsFactory.getAtKey(params))
      return {...result, return: result.return as unknown as (undefined | CheckpointsTestReturns['getAtKey(uint512)uint512'])}
    },

    /**
     * Makes a call to the CheckpointsTest smart contract using the `hasCheckpoint()bool` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @dev Returns whether any checkpoint has been pushed.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    hasCheckpoint: async (params: CallParams<CheckpointsTestArgs['obj']['hasCheckpoint()bool'] | CheckpointsTestArgs['tuple']['hasCheckpoint()bool']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(CheckpointsTestParamsFactory.hasCheckpoint(params))
      return {...result, return: result.return as unknown as (undefined | CheckpointsTestReturns['hasCheckpoint()bool'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new CheckpointsTestClient(this.appClient.clone(params))
  }

  /**
   * Makes a readonly (simulated) call to the CheckpointsTest smart contract using the `latest()uint512` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @dev Returns the latest stored value.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async latest(params: CallParams<CheckpointsTestArgs['obj']['latest()uint512'] | CheckpointsTestArgs['tuple']['latest()uint512']> = {args: []}) {
    const result = await this.appClient.send.call(CheckpointsTestParamsFactory.latest(params))
    return result.return as unknown as CheckpointsTestReturns['latest()uint512']
  }

  /**
   * Makes a readonly (simulated) call to the CheckpointsTest smart contract using the `latestCheckpoint()(bool,uint512,uint512)` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @dev Returns whether a checkpoint exists, and if so, the latest key and value.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async latestCheckpoint(params: CallParams<CheckpointsTestArgs['obj']['latestCheckpoint()(bool,uint512,uint512)'] | CheckpointsTestArgs['tuple']['latestCheckpoint()(bool,uint512,uint512)']> = {args: []}) {
    const result = await this.appClient.send.call(CheckpointsTestParamsFactory.latestCheckpoint(params))
    return result.return as unknown as CheckpointsTestReturns['latestCheckpoint()(bool,uint512,uint512)']
  }

  /**
   * Makes a readonly (simulated) call to the CheckpointsTest smart contract using the `getAtKey(uint512)uint512` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @dev Returns the value stored at a given key.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async getAtKey(params: CallParams<CheckpointsTestArgs['obj']['getAtKey(uint512)uint512'] | CheckpointsTestArgs['tuple']['getAtKey(uint512)uint512']>) {
    const result = await this.appClient.send.call(CheckpointsTestParamsFactory.getAtKey(params))
    return result.return as unknown as CheckpointsTestReturns['getAtKey(uint512)uint512']
  }

  /**
   * Makes a readonly (simulated) call to the CheckpointsTest smart contract using the `hasCheckpoint()bool` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @dev Returns whether any checkpoint has been pushed.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async hasCheckpoint(params: CallParams<CheckpointsTestArgs['obj']['hasCheckpoint()bool'] | CheckpointsTestArgs['tuple']['hasCheckpoint()bool']> = {args: []}) {
    const result = await this.appClient.send.call(CheckpointsTestParamsFactory.hasCheckpoint(params))
    return result.return as unknown as CheckpointsTestReturns['hasCheckpoint()bool']
  }

  /**
   * Methods to access state for the current CheckpointsTest app
   */
  state = {
    /**
     * Methods to access global state for the current CheckpointsTest app
     */
    global: {
      /**
       * Get all current keyed values from global state
       */
      getAll: async (): Promise<Partial<Expand<GlobalKeysState>>> => {
        const result = await this.appClient.state.global.getAll()
        return {
          _latestKey: new BinaryStateValue(result._latestKey),
          _latestValue: new BinaryStateValue(result._latestValue),
          _hasCheckpoint: result._hasCheckpoint,
        }
      },
      /**
       * Get the current value of the _latestKey key in global state
       */
      _latestKey: async (): Promise<BinaryState> => { return new BinaryStateValue((await this.appClient.state.global.getValue("_latestKey")) as Uint8Array | undefined) },
      /**
       * Get the current value of the _latestValue key in global state
       */
      _latestValue: async (): Promise<BinaryState> => { return new BinaryStateValue((await this.appClient.state.global.getValue("_latestValue")) as Uint8Array | undefined) },
      /**
       * Get the current value of the _hasCheckpoint key in global state
       */
      _hasCheckpoint: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("_hasCheckpoint")) as bigint | undefined },
    },
    /**
     * Methods to access box state for the current CheckpointsTest app
     */
    box: {
      /**
       * Get all current keyed values from box state
       */
      getAll: async (): Promise<Partial<Expand<BoxKeysState>>> => {
        const result = await this.appClient.state.box.getAll()
        return {
        }
      },
      /**
       * Get values from the _values map in box state
       */
      _values: {
        /**
         * Get all current values of the _values map in box state
         */
        getMap: async (): Promise<Map<Uint8Array, Uint8Array>> => { return (await this.appClient.state.box.getMap("_values")) as Map<Uint8Array, Uint8Array> },
        /**
         * Get a current value of the _values map by key from box state
         */
        value: async (key: Uint8Array | string): Promise<Uint8Array | undefined> => { return await this.appClient.state.box.getMapValue("_values", key) as Uint8Array | undefined },
      },
    },
  }

  public newGroup(): CheckpointsTestComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a push(uint512,uint512)(uint512,uint512) method call against the CheckpointsTest contract
       */
      push(params: CallParams<CheckpointsTestArgs['obj']['push(uint512,uint512)(uint512,uint512)'] | CheckpointsTestArgs['tuple']['push(uint512,uint512)(uint512,uint512)']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.push(params)))
        resultMappers.push((v) => client.decodeReturnValue('push(uint512,uint512)(uint512,uint512)', v))
        return this
      },
      /**
       * Add a latest()uint512 method call against the CheckpointsTest contract
       */
      latest(params: CallParams<CheckpointsTestArgs['obj']['latest()uint512'] | CheckpointsTestArgs['tuple']['latest()uint512']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.latest(params)))
        resultMappers.push((v) => client.decodeReturnValue('latest()uint512', v))
        return this
      },
      /**
       * Add a latestCheckpoint()(bool,uint512,uint512) method call against the CheckpointsTest contract
       */
      latestCheckpoint(params: CallParams<CheckpointsTestArgs['obj']['latestCheckpoint()(bool,uint512,uint512)'] | CheckpointsTestArgs['tuple']['latestCheckpoint()(bool,uint512,uint512)']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.latestCheckpoint(params)))
        resultMappers.push((v) => client.decodeReturnValue('latestCheckpoint()(bool,uint512,uint512)', v))
        return this
      },
      /**
       * Add a getAtKey(uint512)uint512 method call against the CheckpointsTest contract
       */
      getAtKey(params: CallParams<CheckpointsTestArgs['obj']['getAtKey(uint512)uint512'] | CheckpointsTestArgs['tuple']['getAtKey(uint512)uint512']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.getAtKey(params)))
        resultMappers.push((v) => client.decodeReturnValue('getAtKey(uint512)uint512', v))
        return this
      },
      /**
       * Add a hasCheckpoint()bool method call against the CheckpointsTest contract
       */
      hasCheckpoint(params: CallParams<CheckpointsTestArgs['obj']['hasCheckpoint()bool'] | CheckpointsTestArgs['tuple']['hasCheckpoint()bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.hasCheckpoint(params)))
        resultMappers.push((v) => client.decodeReturnValue('hasCheckpoint()bool', v))
        return this
      },
      /**
       * Add a clear state call to the CheckpointsTest contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as CheckpointsTestComposer
  }
}
export type CheckpointsTestComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the push(uint512,uint512)(uint512,uint512) ABI method.
   *
  * @dev Pushes a (key, value) pair. Keys must be non-decreasing.
  Returns (oldValue, newValue).

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  push(params?: CallParams<CheckpointsTestArgs['obj']['push(uint512,uint512)(uint512,uint512)'] | CheckpointsTestArgs['tuple']['push(uint512,uint512)(uint512,uint512)']>): CheckpointsTestComposer<[...TReturns, CheckpointsTestReturns['push(uint512,uint512)(uint512,uint512)'] | undefined]>

  /**
   * Calls the latest()uint512 ABI method.
   *
   * @dev Returns the latest stored value.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  latest(params?: CallParams<CheckpointsTestArgs['obj']['latest()uint512'] | CheckpointsTestArgs['tuple']['latest()uint512']>): CheckpointsTestComposer<[...TReturns, CheckpointsTestReturns['latest()uint512'] | undefined]>

  /**
   * Calls the latestCheckpoint()(bool,uint512,uint512) ABI method.
   *
   * @dev Returns whether a checkpoint exists, and if so, the latest key and value.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  latestCheckpoint(params?: CallParams<CheckpointsTestArgs['obj']['latestCheckpoint()(bool,uint512,uint512)'] | CheckpointsTestArgs['tuple']['latestCheckpoint()(bool,uint512,uint512)']>): CheckpointsTestComposer<[...TReturns, CheckpointsTestReturns['latestCheckpoint()(bool,uint512,uint512)'] | undefined]>

  /**
   * Calls the getAtKey(uint512)uint512 ABI method.
   *
   * @dev Returns the value stored at a given key.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  getAtKey(params?: CallParams<CheckpointsTestArgs['obj']['getAtKey(uint512)uint512'] | CheckpointsTestArgs['tuple']['getAtKey(uint512)uint512']>): CheckpointsTestComposer<[...TReturns, CheckpointsTestReturns['getAtKey(uint512)uint512'] | undefined]>

  /**
   * Calls the hasCheckpoint()bool ABI method.
   *
   * @dev Returns whether any checkpoint has been pushed.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  hasCheckpoint(params?: CallParams<CheckpointsTestArgs['obj']['hasCheckpoint()bool'] | CheckpointsTestArgs['tuple']['hasCheckpoint()bool']>): CheckpointsTestComposer<[...TReturns, CheckpointsTestReturns['hasCheckpoint()bool'] | undefined]>

  /**
   * Makes a clear_state call to an existing instance of the CheckpointsTest smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): CheckpointsTestComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): CheckpointsTestComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<CheckpointsTestComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<CheckpointsTestComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<CheckpointsTestComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<CheckpointsTestComposerResults<TReturns>>
}
export type CheckpointsTestComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

