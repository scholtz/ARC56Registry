/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"WETH9","structs":{},"methods":[{"name":"deposit","args":[{"type":"uint512","name":"wad"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}},{"name":"withdraw","args":[{"type":"uint512","name":"wad"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}},{"name":"totalSupply","args":[],"returns":{"type":"uint512"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"approve","args":[{"type":"address","name":"guy"},{"type":"uint512","name":"wad"}],"returns":{"type":"bool"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}},{"name":"transfer","args":[{"type":"address","name":"dst"},{"type":"uint512","name":"wad"}],"returns":{"type":"bool"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}},{"name":"transferFrom","args":[{"type":"address","name":"src"},{"type":"address","name":"dst"},{"type":"uint512","name":"wad"}],"returns":{"type":"bool"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}},{"name":"getBalanceOf","args":[{"type":"address","name":"account"}],"returns":{"type":"uint512"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"getAllowance","args":[{"type":"address","name":"src"},{"type":"address","name":"guy"}],"returns":{"type":"uint512"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}}],"arcs":[22,28],"networks":{},"state":{"schema":{"global":{"ints":0,"bytes":1},"local":{"ints":0,"bytes":0}},"keys":{"global":{"_totalSupply":{"keyType":"AVMString","valueType":"AVMBytes","key":"X3RvdGFsU3VwcGx5"}},"local":{},"box":{}},"maps":{"global":{},"local":{},"box":{"balanceOf":{"keyType":"AVMBytes","valueType":"AVMBytes","prefix":"YmFsYW5jZU9m"},"allowance":{"keyType":"AVMBytes","valueType":"AVMBytes","prefix":"YWxsb3dhbmNl"}}}},"bareActions":{"create":[],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[568],"errorMessage":"WETH9: insufficient allowance"},{"pc":[205,490],"errorMessage":"WETH9: insufficient balance"},{"pc":[165,234,255],"errorMessage":"check _totalSupply exists"},{"pc":[278,328,365,373,406,438,446],"errorMessage":"invalid number of bytes for arc4.static_array<arc4.uint8, 32>"},{"pc":[136,190,286,336,381],"errorMessage":"invalid number of bytes for arc4.uint512"},{"pc":[260,420,461],"errorMessage":"overflow"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"events":[]} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


/**
 * The argument types for the Weth9 contract
 */
export type Weth9Args = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'deposit(uint512)void': {
      wad: bigint | number
    }
    'withdraw(uint512)void': {
      wad: bigint | number
    }
    'totalSupply()uint512': Record<string, never>
    'approve(address,uint512)bool': {
      guy: string
      wad: bigint | number
    }
    'transfer(address,uint512)bool': {
      dst: string
      wad: bigint | number
    }
    'transferFrom(address,address,uint512)bool': {
      src: string
      dst: string
      wad: bigint | number
    }
    'getBalanceOf(address)uint512': {
      account: string
    }
    'getAllowance(address,address)uint512': {
      src: string
      guy: string
    }
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'deposit(uint512)void': [wad: bigint | number]
    'withdraw(uint512)void': [wad: bigint | number]
    'totalSupply()uint512': []
    'approve(address,uint512)bool': [guy: string, wad: bigint | number]
    'transfer(address,uint512)bool': [dst: string, wad: bigint | number]
    'transferFrom(address,address,uint512)bool': [src: string, dst: string, wad: bigint | number]
    'getBalanceOf(address)uint512': [account: string]
    'getAllowance(address,address)uint512': [src: string, guy: string]
  }
}

/**
 * The return type for each method
 */
export type Weth9Returns = {
  'deposit(uint512)void': void
  'withdraw(uint512)void': void
  'totalSupply()uint512': bigint
  'approve(address,uint512)bool': boolean
  'transfer(address,uint512)bool': boolean
  'transferFrom(address,address,uint512)bool': boolean
  'getBalanceOf(address)uint512': bigint
  'getAllowance(address,address)uint512': bigint
}

/**
 * Defines the types of available calls and state of the Weth9 smart contract.
 */
export type Weth9Types = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'deposit(uint512)void' | 'deposit', {
      argsObj: Weth9Args['obj']['deposit(uint512)void']
      argsTuple: Weth9Args['tuple']['deposit(uint512)void']
      returns: Weth9Returns['deposit(uint512)void']
    }>
    & Record<'withdraw(uint512)void' | 'withdraw', {
      argsObj: Weth9Args['obj']['withdraw(uint512)void']
      argsTuple: Weth9Args['tuple']['withdraw(uint512)void']
      returns: Weth9Returns['withdraw(uint512)void']
    }>
    & Record<'totalSupply()uint512' | 'totalSupply', {
      argsObj: Weth9Args['obj']['totalSupply()uint512']
      argsTuple: Weth9Args['tuple']['totalSupply()uint512']
      returns: Weth9Returns['totalSupply()uint512']
    }>
    & Record<'approve(address,uint512)bool' | 'approve', {
      argsObj: Weth9Args['obj']['approve(address,uint512)bool']
      argsTuple: Weth9Args['tuple']['approve(address,uint512)bool']
      returns: Weth9Returns['approve(address,uint512)bool']
    }>
    & Record<'transfer(address,uint512)bool' | 'transfer', {
      argsObj: Weth9Args['obj']['transfer(address,uint512)bool']
      argsTuple: Weth9Args['tuple']['transfer(address,uint512)bool']
      returns: Weth9Returns['transfer(address,uint512)bool']
    }>
    & Record<'transferFrom(address,address,uint512)bool' | 'transferFrom', {
      argsObj: Weth9Args['obj']['transferFrom(address,address,uint512)bool']
      argsTuple: Weth9Args['tuple']['transferFrom(address,address,uint512)bool']
      returns: Weth9Returns['transferFrom(address,address,uint512)bool']
    }>
    & Record<'getBalanceOf(address)uint512' | 'getBalanceOf', {
      argsObj: Weth9Args['obj']['getBalanceOf(address)uint512']
      argsTuple: Weth9Args['tuple']['getBalanceOf(address)uint512']
      returns: Weth9Returns['getBalanceOf(address)uint512']
    }>
    & Record<'getAllowance(address,address)uint512' | 'getAllowance', {
      argsObj: Weth9Args['obj']['getAllowance(address,address)uint512']
      argsTuple: Weth9Args['tuple']['getAllowance(address,address)uint512']
      returns: Weth9Returns['getAllowance(address,address)uint512']
    }>
  /**
   * Defines the shape of the state of the application.
   */
  state: {
    global: {
      keys: {
        _totalSupply: BinaryState
      }
      maps: {}
    }
    box: {
      keys: {}
      maps: {
        balanceOf: Map<Uint8Array | string, Uint8Array>
        allowance: Map<Uint8Array | string, Uint8Array>
      }
    }
  }
}

/**
 * Defines the possible abi call signatures.
 */
export type Weth9Signatures = keyof Weth9Types['methods']
/**
 * Defines the possible abi call signatures for methods that return a non-void value.
 */
export type Weth9NonVoidMethodSignatures = keyof Weth9Types['methods'] extends infer T ? T extends keyof Weth9Types['methods'] ? MethodReturn<T> extends void ? never : T  : never : never
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the Weth9 smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends Weth9Signatures> = Weth9Types['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the Weth9 smart contract to the method's return type
 */
export type MethodReturn<TSignature extends Weth9Signatures> = Weth9Types['methods'][TSignature]['returns']

/**
 * Defines the shape of the keyed global state of the application.
 */
export type GlobalKeysState = Weth9Types['state']['global']['keys']

/**
 * Defines the shape of the keyed box state of the application.
 */
export type BoxKeysState = Weth9Types['state']['box']['keys']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the Weth9 smart contract
 */
export abstract class Weth9ParamsFactory {
  /**
   * Constructs a no op call for the deposit(uint512)void ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static deposit(params: CallParams<Weth9Args['obj']['deposit(uint512)void'] | Weth9Args['tuple']['deposit(uint512)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'deposit(uint512)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.wad],
    }
  }
  /**
   * Constructs a no op call for the withdraw(uint512)void ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static withdraw(params: CallParams<Weth9Args['obj']['withdraw(uint512)void'] | Weth9Args['tuple']['withdraw(uint512)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'withdraw(uint512)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.wad],
    }
  }
  /**
   * Constructs a no op call for the totalSupply()uint512 ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static totalSupply(params: CallParams<Weth9Args['obj']['totalSupply()uint512'] | Weth9Args['tuple']['totalSupply()uint512']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'totalSupply()uint512' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the approve(address,uint512)bool ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static approve(params: CallParams<Weth9Args['obj']['approve(address,uint512)bool'] | Weth9Args['tuple']['approve(address,uint512)bool']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'approve(address,uint512)bool' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.guy, params.args.wad],
    }
  }
  /**
   * Constructs a no op call for the transfer(address,uint512)bool ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static transfer(params: CallParams<Weth9Args['obj']['transfer(address,uint512)bool'] | Weth9Args['tuple']['transfer(address,uint512)bool']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'transfer(address,uint512)bool' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.dst, params.args.wad],
    }
  }
  /**
   * Constructs a no op call for the transferFrom(address,address,uint512)bool ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static transferFrom(params: CallParams<Weth9Args['obj']['transferFrom(address,address,uint512)bool'] | Weth9Args['tuple']['transferFrom(address,address,uint512)bool']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'transferFrom(address,address,uint512)bool' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.src, params.args.dst, params.args.wad],
    }
  }
  /**
   * Constructs a no op call for the getBalanceOf(address)uint512 ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static getBalanceOf(params: CallParams<Weth9Args['obj']['getBalanceOf(address)uint512'] | Weth9Args['tuple']['getBalanceOf(address)uint512']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'getBalanceOf(address)uint512' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.account],
    }
  }
  /**
   * Constructs a no op call for the getAllowance(address,address)uint512 ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static getAllowance(params: CallParams<Weth9Args['obj']['getAllowance(address,address)uint512'] | Weth9Args['tuple']['getAllowance(address,address)uint512']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'getAllowance(address,address)uint512' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.src, params.args.guy],
    }
  }
}

/**
 * A client to make calls to the WETH9 smart contract
 */
export class Weth9Client {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `Weth9Client`
   *
   * @param appClient An `AppClient` instance which has been created with the Weth9 app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `Weth9Client`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Checks for decode errors on the given return value and maps the return value to the return type for the given method
   * @returns The typed return value or undefined if there was no value
   */
  decodeReturnValue<TSignature extends Weth9NonVoidMethodSignatures>(method: TSignature, returnValue: ABIReturn | undefined) {
    return returnValue !== undefined ? getArc56ReturnValue<MethodReturn<TSignature>>(returnValue, this.appClient.getABIMethod(method), APP_SPEC.structs) : undefined
  }

  /**
   * Returns a new `Weth9Client` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<Weth9Client> {
    return new Weth9Client(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `Weth9Client` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<Weth9Client> {
    return new Weth9Client(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Makes a clear_state call to an existing instance of the WETH9 smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the WETH9 smart contract using the `deposit(uint512)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    deposit: (params: CallParams<Weth9Args['obj']['deposit(uint512)void'] | Weth9Args['tuple']['deposit(uint512)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(Weth9ParamsFactory.deposit(params))
    },

    /**
     * Makes a call to the WETH9 smart contract using the `withdraw(uint512)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    withdraw: (params: CallParams<Weth9Args['obj']['withdraw(uint512)void'] | Weth9Args['tuple']['withdraw(uint512)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(Weth9ParamsFactory.withdraw(params))
    },

    /**
     * Makes a call to the WETH9 smart contract using the `totalSupply()uint512` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    totalSupply: (params: CallParams<Weth9Args['obj']['totalSupply()uint512'] | Weth9Args['tuple']['totalSupply()uint512']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(Weth9ParamsFactory.totalSupply(params))
    },

    /**
     * Makes a call to the WETH9 smart contract using the `approve(address,uint512)bool` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    approve: (params: CallParams<Weth9Args['obj']['approve(address,uint512)bool'] | Weth9Args['tuple']['approve(address,uint512)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(Weth9ParamsFactory.approve(params))
    },

    /**
     * Makes a call to the WETH9 smart contract using the `transfer(address,uint512)bool` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    transfer: (params: CallParams<Weth9Args['obj']['transfer(address,uint512)bool'] | Weth9Args['tuple']['transfer(address,uint512)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(Weth9ParamsFactory.transfer(params))
    },

    /**
     * Makes a call to the WETH9 smart contract using the `transferFrom(address,address,uint512)bool` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    transferFrom: (params: CallParams<Weth9Args['obj']['transferFrom(address,address,uint512)bool'] | Weth9Args['tuple']['transferFrom(address,address,uint512)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(Weth9ParamsFactory.transferFrom(params))
    },

    /**
     * Makes a call to the WETH9 smart contract using the `getBalanceOf(address)uint512` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    getBalanceOf: (params: CallParams<Weth9Args['obj']['getBalanceOf(address)uint512'] | Weth9Args['tuple']['getBalanceOf(address)uint512']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(Weth9ParamsFactory.getBalanceOf(params))
    },

    /**
     * Makes a call to the WETH9 smart contract using the `getAllowance(address,address)uint512` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    getAllowance: (params: CallParams<Weth9Args['obj']['getAllowance(address,address)uint512'] | Weth9Args['tuple']['getAllowance(address,address)uint512']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(Weth9ParamsFactory.getAllowance(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Makes a clear_state call to an existing instance of the WETH9 smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the WETH9 smart contract using the `deposit(uint512)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    deposit: (params: CallParams<Weth9Args['obj']['deposit(uint512)void'] | Weth9Args['tuple']['deposit(uint512)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(Weth9ParamsFactory.deposit(params))
    },

    /**
     * Makes a call to the WETH9 smart contract using the `withdraw(uint512)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    withdraw: (params: CallParams<Weth9Args['obj']['withdraw(uint512)void'] | Weth9Args['tuple']['withdraw(uint512)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(Weth9ParamsFactory.withdraw(params))
    },

    /**
     * Makes a call to the WETH9 smart contract using the `totalSupply()uint512` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    totalSupply: (params: CallParams<Weth9Args['obj']['totalSupply()uint512'] | Weth9Args['tuple']['totalSupply()uint512']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(Weth9ParamsFactory.totalSupply(params))
    },

    /**
     * Makes a call to the WETH9 smart contract using the `approve(address,uint512)bool` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    approve: (params: CallParams<Weth9Args['obj']['approve(address,uint512)bool'] | Weth9Args['tuple']['approve(address,uint512)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(Weth9ParamsFactory.approve(params))
    },

    /**
     * Makes a call to the WETH9 smart contract using the `transfer(address,uint512)bool` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    transfer: (params: CallParams<Weth9Args['obj']['transfer(address,uint512)bool'] | Weth9Args['tuple']['transfer(address,uint512)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(Weth9ParamsFactory.transfer(params))
    },

    /**
     * Makes a call to the WETH9 smart contract using the `transferFrom(address,address,uint512)bool` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    transferFrom: (params: CallParams<Weth9Args['obj']['transferFrom(address,address,uint512)bool'] | Weth9Args['tuple']['transferFrom(address,address,uint512)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(Weth9ParamsFactory.transferFrom(params))
    },

    /**
     * Makes a call to the WETH9 smart contract using the `getBalanceOf(address)uint512` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    getBalanceOf: (params: CallParams<Weth9Args['obj']['getBalanceOf(address)uint512'] | Weth9Args['tuple']['getBalanceOf(address)uint512']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(Weth9ParamsFactory.getBalanceOf(params))
    },

    /**
     * Makes a call to the WETH9 smart contract using the `getAllowance(address,address)uint512` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    getAllowance: (params: CallParams<Weth9Args['obj']['getAllowance(address,address)uint512'] | Weth9Args['tuple']['getAllowance(address,address)uint512']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(Weth9ParamsFactory.getAllowance(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Makes a clear_state call to an existing instance of the WETH9 smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the WETH9 smart contract using the `deposit(uint512)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    deposit: async (params: CallParams<Weth9Args['obj']['deposit(uint512)void'] | Weth9Args['tuple']['deposit(uint512)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(Weth9ParamsFactory.deposit(params))
      return {...result, return: result.return as unknown as (undefined | Weth9Returns['deposit(uint512)void'])}
    },

    /**
     * Makes a call to the WETH9 smart contract using the `withdraw(uint512)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    withdraw: async (params: CallParams<Weth9Args['obj']['withdraw(uint512)void'] | Weth9Args['tuple']['withdraw(uint512)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(Weth9ParamsFactory.withdraw(params))
      return {...result, return: result.return as unknown as (undefined | Weth9Returns['withdraw(uint512)void'])}
    },

    /**
     * Makes a call to the WETH9 smart contract using the `totalSupply()uint512` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    totalSupply: async (params: CallParams<Weth9Args['obj']['totalSupply()uint512'] | Weth9Args['tuple']['totalSupply()uint512']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(Weth9ParamsFactory.totalSupply(params))
      return {...result, return: result.return as unknown as (undefined | Weth9Returns['totalSupply()uint512'])}
    },

    /**
     * Makes a call to the WETH9 smart contract using the `approve(address,uint512)bool` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    approve: async (params: CallParams<Weth9Args['obj']['approve(address,uint512)bool'] | Weth9Args['tuple']['approve(address,uint512)bool']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(Weth9ParamsFactory.approve(params))
      return {...result, return: result.return as unknown as (undefined | Weth9Returns['approve(address,uint512)bool'])}
    },

    /**
     * Makes a call to the WETH9 smart contract using the `transfer(address,uint512)bool` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    transfer: async (params: CallParams<Weth9Args['obj']['transfer(address,uint512)bool'] | Weth9Args['tuple']['transfer(address,uint512)bool']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(Weth9ParamsFactory.transfer(params))
      return {...result, return: result.return as unknown as (undefined | Weth9Returns['transfer(address,uint512)bool'])}
    },

    /**
     * Makes a call to the WETH9 smart contract using the `transferFrom(address,address,uint512)bool` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    transferFrom: async (params: CallParams<Weth9Args['obj']['transferFrom(address,address,uint512)bool'] | Weth9Args['tuple']['transferFrom(address,address,uint512)bool']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(Weth9ParamsFactory.transferFrom(params))
      return {...result, return: result.return as unknown as (undefined | Weth9Returns['transferFrom(address,address,uint512)bool'])}
    },

    /**
     * Makes a call to the WETH9 smart contract using the `getBalanceOf(address)uint512` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    getBalanceOf: async (params: CallParams<Weth9Args['obj']['getBalanceOf(address)uint512'] | Weth9Args['tuple']['getBalanceOf(address)uint512']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(Weth9ParamsFactory.getBalanceOf(params))
      return {...result, return: result.return as unknown as (undefined | Weth9Returns['getBalanceOf(address)uint512'])}
    },

    /**
     * Makes a call to the WETH9 smart contract using the `getAllowance(address,address)uint512` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    getAllowance: async (params: CallParams<Weth9Args['obj']['getAllowance(address,address)uint512'] | Weth9Args['tuple']['getAllowance(address,address)uint512']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(Weth9ParamsFactory.getAllowance(params))
      return {...result, return: result.return as unknown as (undefined | Weth9Returns['getAllowance(address,address)uint512'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new Weth9Client(this.appClient.clone(params))
  }

  /**
   * Makes a readonly (simulated) call to the WETH9 smart contract using the `totalSupply()uint512` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async totalSupply(params: CallParams<Weth9Args['obj']['totalSupply()uint512'] | Weth9Args['tuple']['totalSupply()uint512']> = {args: []}) {
    const result = await this.appClient.send.call(Weth9ParamsFactory.totalSupply(params))
    return result.return as unknown as Weth9Returns['totalSupply()uint512']
  }

  /**
   * Makes a readonly (simulated) call to the WETH9 smart contract using the `getBalanceOf(address)uint512` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async getBalanceOf(params: CallParams<Weth9Args['obj']['getBalanceOf(address)uint512'] | Weth9Args['tuple']['getBalanceOf(address)uint512']>) {
    const result = await this.appClient.send.call(Weth9ParamsFactory.getBalanceOf(params))
    return result.return as unknown as Weth9Returns['getBalanceOf(address)uint512']
  }

  /**
   * Makes a readonly (simulated) call to the WETH9 smart contract using the `getAllowance(address,address)uint512` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async getAllowance(params: CallParams<Weth9Args['obj']['getAllowance(address,address)uint512'] | Weth9Args['tuple']['getAllowance(address,address)uint512']>) {
    const result = await this.appClient.send.call(Weth9ParamsFactory.getAllowance(params))
    return result.return as unknown as Weth9Returns['getAllowance(address,address)uint512']
  }

  /**
   * Methods to access state for the current WETH9 app
   */
  state = {
    /**
     * Methods to access global state for the current WETH9 app
     */
    global: {
      /**
       * Get all current keyed values from global state
       */
      getAll: async (): Promise<Partial<Expand<GlobalKeysState>>> => {
        const result = await this.appClient.state.global.getAll()
        return {
          _totalSupply: new BinaryStateValue(result._totalSupply),
        }
      },
      /**
       * Get the current value of the _totalSupply key in global state
       */
      _totalSupply: async (): Promise<BinaryState> => { return new BinaryStateValue((await this.appClient.state.global.getValue("_totalSupply")) as Uint8Array | undefined) },
    },
    /**
     * Methods to access box state for the current WETH9 app
     */
    box: {
      /**
       * Get all current keyed values from box state
       */
      getAll: async (): Promise<Partial<Expand<BoxKeysState>>> => {
        const result = await this.appClient.state.box.getAll()
        return {
        }
      },
      /**
       * Get values from the balanceOf map in box state
       */
      balanceOf: {
        /**
         * Get all current values of the balanceOf map in box state
         */
        getMap: async (): Promise<Map<Uint8Array, Uint8Array>> => { return (await this.appClient.state.box.getMap("balanceOf")) as Map<Uint8Array, Uint8Array> },
        /**
         * Get a current value of the balanceOf map by key from box state
         */
        value: async (key: Uint8Array | string): Promise<Uint8Array | undefined> => { return await this.appClient.state.box.getMapValue("balanceOf", key) as Uint8Array | undefined },
      },
      /**
       * Get values from the allowance map in box state
       */
      allowance: {
        /**
         * Get all current values of the allowance map in box state
         */
        getMap: async (): Promise<Map<Uint8Array, Uint8Array>> => { return (await this.appClient.state.box.getMap("allowance")) as Map<Uint8Array, Uint8Array> },
        /**
         * Get a current value of the allowance map by key from box state
         */
        value: async (key: Uint8Array | string): Promise<Uint8Array | undefined> => { return await this.appClient.state.box.getMapValue("allowance", key) as Uint8Array | undefined },
      },
    },
  }

  public newGroup(): Weth9Composer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a deposit(uint512)void method call against the WETH9 contract
       */
      deposit(params: CallParams<Weth9Args['obj']['deposit(uint512)void'] | Weth9Args['tuple']['deposit(uint512)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.deposit(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a withdraw(uint512)void method call against the WETH9 contract
       */
      withdraw(params: CallParams<Weth9Args['obj']['withdraw(uint512)void'] | Weth9Args['tuple']['withdraw(uint512)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.withdraw(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a totalSupply()uint512 method call against the WETH9 contract
       */
      totalSupply(params: CallParams<Weth9Args['obj']['totalSupply()uint512'] | Weth9Args['tuple']['totalSupply()uint512']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.totalSupply(params)))
        resultMappers.push((v) => client.decodeReturnValue('totalSupply()uint512', v))
        return this
      },
      /**
       * Add a approve(address,uint512)bool method call against the WETH9 contract
       */
      approve(params: CallParams<Weth9Args['obj']['approve(address,uint512)bool'] | Weth9Args['tuple']['approve(address,uint512)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.approve(params)))
        resultMappers.push((v) => client.decodeReturnValue('approve(address,uint512)bool', v))
        return this
      },
      /**
       * Add a transfer(address,uint512)bool method call against the WETH9 contract
       */
      transfer(params: CallParams<Weth9Args['obj']['transfer(address,uint512)bool'] | Weth9Args['tuple']['transfer(address,uint512)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.transfer(params)))
        resultMappers.push((v) => client.decodeReturnValue('transfer(address,uint512)bool', v))
        return this
      },
      /**
       * Add a transferFrom(address,address,uint512)bool method call against the WETH9 contract
       */
      transferFrom(params: CallParams<Weth9Args['obj']['transferFrom(address,address,uint512)bool'] | Weth9Args['tuple']['transferFrom(address,address,uint512)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.transferFrom(params)))
        resultMappers.push((v) => client.decodeReturnValue('transferFrom(address,address,uint512)bool', v))
        return this
      },
      /**
       * Add a getBalanceOf(address)uint512 method call against the WETH9 contract
       */
      getBalanceOf(params: CallParams<Weth9Args['obj']['getBalanceOf(address)uint512'] | Weth9Args['tuple']['getBalanceOf(address)uint512']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.getBalanceOf(params)))
        resultMappers.push((v) => client.decodeReturnValue('getBalanceOf(address)uint512', v))
        return this
      },
      /**
       * Add a getAllowance(address,address)uint512 method call against the WETH9 contract
       */
      getAllowance(params: CallParams<Weth9Args['obj']['getAllowance(address,address)uint512'] | Weth9Args['tuple']['getAllowance(address,address)uint512']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.getAllowance(params)))
        resultMappers.push((v) => client.decodeReturnValue('getAllowance(address,address)uint512', v))
        return this
      },
      /**
       * Add a clear state call to the WETH9 contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as Weth9Composer
  }
}
export type Weth9Composer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the deposit(uint512)void ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  deposit(params?: CallParams<Weth9Args['obj']['deposit(uint512)void'] | Weth9Args['tuple']['deposit(uint512)void']>): Weth9Composer<[...TReturns, Weth9Returns['deposit(uint512)void'] | undefined]>

  /**
   * Calls the withdraw(uint512)void ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  withdraw(params?: CallParams<Weth9Args['obj']['withdraw(uint512)void'] | Weth9Args['tuple']['withdraw(uint512)void']>): Weth9Composer<[...TReturns, Weth9Returns['withdraw(uint512)void'] | undefined]>

  /**
   * Calls the totalSupply()uint512 ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  totalSupply(params?: CallParams<Weth9Args['obj']['totalSupply()uint512'] | Weth9Args['tuple']['totalSupply()uint512']>): Weth9Composer<[...TReturns, Weth9Returns['totalSupply()uint512'] | undefined]>

  /**
   * Calls the approve(address,uint512)bool ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  approve(params?: CallParams<Weth9Args['obj']['approve(address,uint512)bool'] | Weth9Args['tuple']['approve(address,uint512)bool']>): Weth9Composer<[...TReturns, Weth9Returns['approve(address,uint512)bool'] | undefined]>

  /**
   * Calls the transfer(address,uint512)bool ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  transfer(params?: CallParams<Weth9Args['obj']['transfer(address,uint512)bool'] | Weth9Args['tuple']['transfer(address,uint512)bool']>): Weth9Composer<[...TReturns, Weth9Returns['transfer(address,uint512)bool'] | undefined]>

  /**
   * Calls the transferFrom(address,address,uint512)bool ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  transferFrom(params?: CallParams<Weth9Args['obj']['transferFrom(address,address,uint512)bool'] | Weth9Args['tuple']['transferFrom(address,address,uint512)bool']>): Weth9Composer<[...TReturns, Weth9Returns['transferFrom(address,address,uint512)bool'] | undefined]>

  /**
   * Calls the getBalanceOf(address)uint512 ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  getBalanceOf(params?: CallParams<Weth9Args['obj']['getBalanceOf(address)uint512'] | Weth9Args['tuple']['getBalanceOf(address)uint512']>): Weth9Composer<[...TReturns, Weth9Returns['getBalanceOf(address)uint512'] | undefined]>

  /**
   * Calls the getAllowance(address,address)uint512 ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  getAllowance(params?: CallParams<Weth9Args['obj']['getAllowance(address,address)uint512'] | Weth9Args['tuple']['getAllowance(address,address)uint512']>): Weth9Composer<[...TReturns, Weth9Returns['getAllowance(address,address)uint512'] | undefined]>

  /**
   * Makes a clear_state call to an existing instance of the WETH9 smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): Weth9Composer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): Weth9Composer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<Weth9ComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<Weth9ComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<Weth9ComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<Weth9ComposerResults<TReturns>>
}
export type Weth9ComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

