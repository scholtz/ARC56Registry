/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"C","structs":{},"methods":[{"name":"storageVar","args":[],"returns":{"type":"bool"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"constStorageVar","args":[],"returns":{"type":"bool"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"memoryVar","args":[],"returns":{"type":"bool"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"calldataParam","args":[{"type":"string","name":"calldataVarStr"}],"returns":{"type":"bool"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"calldataSlice","args":[{"type":"byte[]","name":"namespaceID"}],"returns":{"type":"bool"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"literalParam","args":[],"returns":{"type":"bool"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}}],"arcs":[22,28],"networks":{},"state":{"schema":{"global":{"ints":0,"bytes":1},"local":{"ints":0,"bytes":0}},"keys":{"global":{"storageVarStr":{"keyType":"AVMString","valueType":"AVMString","key":"c3RvcmFnZVZhclN0cg=="}},"local":{},"box":{}},"maps":{"global":{},"local":{},"box":{}}},"bareActions":{"create":[],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[544],"errorMessage":"check storageVarStr exists"},{"pc":[289,361],"errorMessage":"invalid array length header"},{"pc":[296,368],"errorMessage":"invalid number of bytes for arc4.dynamic_array<arc4.uint8>"},{"pc":[260,314,386,434,470,537],"errorMessage":"not payable"},{"pc":[584],"errorMessage":"underflow"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"events":[]} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


/**
 * The argument types for the C contract
 */
export type CArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'storageVar()bool': Record<string, never>
    'constStorageVar()bool': Record<string, never>
    'memoryVar()bool': Record<string, never>
    'calldataParam(string)bool': {
      calldataVarStr: string
    }
    'calldataSlice(byte[])bool': {
      namespaceId: Uint8Array
    }
    'literalParam()bool': Record<string, never>
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'storageVar()bool': []
    'constStorageVar()bool': []
    'memoryVar()bool': []
    'calldataParam(string)bool': [calldataVarStr: string]
    'calldataSlice(byte[])bool': [namespaceId: Uint8Array]
    'literalParam()bool': []
  }
}

/**
 * The return type for each method
 */
export type CReturns = {
  'storageVar()bool': boolean
  'constStorageVar()bool': boolean
  'memoryVar()bool': boolean
  'calldataParam(string)bool': boolean
  'calldataSlice(byte[])bool': boolean
  'literalParam()bool': boolean
}

/**
 * Defines the types of available calls and state of the C smart contract.
 */
export type CTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'storageVar()bool' | 'storageVar', {
      argsObj: CArgs['obj']['storageVar()bool']
      argsTuple: CArgs['tuple']['storageVar()bool']
      returns: CReturns['storageVar()bool']
    }>
    & Record<'constStorageVar()bool' | 'constStorageVar', {
      argsObj: CArgs['obj']['constStorageVar()bool']
      argsTuple: CArgs['tuple']['constStorageVar()bool']
      returns: CReturns['constStorageVar()bool']
    }>
    & Record<'memoryVar()bool' | 'memoryVar', {
      argsObj: CArgs['obj']['memoryVar()bool']
      argsTuple: CArgs['tuple']['memoryVar()bool']
      returns: CReturns['memoryVar()bool']
    }>
    & Record<'calldataParam(string)bool' | 'calldataParam', {
      argsObj: CArgs['obj']['calldataParam(string)bool']
      argsTuple: CArgs['tuple']['calldataParam(string)bool']
      returns: CReturns['calldataParam(string)bool']
    }>
    & Record<'calldataSlice(byte[])bool' | 'calldataSlice', {
      argsObj: CArgs['obj']['calldataSlice(byte[])bool']
      argsTuple: CArgs['tuple']['calldataSlice(byte[])bool']
      returns: CReturns['calldataSlice(byte[])bool']
    }>
    & Record<'literalParam()bool' | 'literalParam', {
      argsObj: CArgs['obj']['literalParam()bool']
      argsTuple: CArgs['tuple']['literalParam()bool']
      returns: CReturns['literalParam()bool']
    }>
  /**
   * Defines the shape of the state of the application.
   */
  state: {
    global: {
      keys: {
        storageVarStr: string
      }
      maps: {}
    }
  }
}

/**
 * Defines the possible abi call signatures.
 */
export type CSignatures = keyof CTypes['methods']
/**
 * Defines the possible abi call signatures for methods that return a non-void value.
 */
export type CNonVoidMethodSignatures = keyof CTypes['methods'] extends infer T ? T extends keyof CTypes['methods'] ? MethodReturn<T> extends void ? never : T  : never : never
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the C smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends CSignatures> = CTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the C smart contract to the method's return type
 */
export type MethodReturn<TSignature extends CSignatures> = CTypes['methods'][TSignature]['returns']

/**
 * Defines the shape of the keyed global state of the application.
 */
export type GlobalKeysState = CTypes['state']['global']['keys']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the C smart contract
 */
export abstract class CParamsFactory {
  /**
   * Constructs a no op call for the storageVar()bool ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static storageVar(params: CallParams<CArgs['obj']['storageVar()bool'] | CArgs['tuple']['storageVar()bool']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'storageVar()bool' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the constStorageVar()bool ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static constStorageVar(params: CallParams<CArgs['obj']['constStorageVar()bool'] | CArgs['tuple']['constStorageVar()bool']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'constStorageVar()bool' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the memoryVar()bool ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static memoryVar(params: CallParams<CArgs['obj']['memoryVar()bool'] | CArgs['tuple']['memoryVar()bool']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'memoryVar()bool' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the calldataParam(string)bool ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static calldataParam(params: CallParams<CArgs['obj']['calldataParam(string)bool'] | CArgs['tuple']['calldataParam(string)bool']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'calldataParam(string)bool' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.calldataVarStr],
    }
  }
  /**
   * Constructs a no op call for the calldataSlice(byte[])bool ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static calldataSlice(params: CallParams<CArgs['obj']['calldataSlice(byte[])bool'] | CArgs['tuple']['calldataSlice(byte[])bool']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'calldataSlice(byte[])bool' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.namespaceId],
    }
  }
  /**
   * Constructs a no op call for the literalParam()bool ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static literalParam(params: CallParams<CArgs['obj']['literalParam()bool'] | CArgs['tuple']['literalParam()bool']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'literalParam()bool' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
}

/**
 * A client to make calls to the C smart contract
 */
export class CClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `CClient`
   *
   * @param appClient An `AppClient` instance which has been created with the C app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `CClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Checks for decode errors on the given return value and maps the return value to the return type for the given method
   * @returns The typed return value or undefined if there was no value
   */
  decodeReturnValue<TSignature extends CNonVoidMethodSignatures>(method: TSignature, returnValue: ABIReturn | undefined) {
    return returnValue !== undefined ? getArc56ReturnValue<MethodReturn<TSignature>>(returnValue, this.appClient.getABIMethod(method), APP_SPEC.structs) : undefined
  }

  /**
   * Returns a new `CClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<CClient> {
    return new CClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `CClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<CClient> {
    return new CClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Makes a clear_state call to an existing instance of the C smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the C smart contract using the `storageVar()bool` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    storageVar: (params: CallParams<CArgs['obj']['storageVar()bool'] | CArgs['tuple']['storageVar()bool']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(CParamsFactory.storageVar(params))
    },

    /**
     * Makes a call to the C smart contract using the `constStorageVar()bool` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    constStorageVar: (params: CallParams<CArgs['obj']['constStorageVar()bool'] | CArgs['tuple']['constStorageVar()bool']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(CParamsFactory.constStorageVar(params))
    },

    /**
     * Makes a call to the C smart contract using the `memoryVar()bool` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    memoryVar: (params: CallParams<CArgs['obj']['memoryVar()bool'] | CArgs['tuple']['memoryVar()bool']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(CParamsFactory.memoryVar(params))
    },

    /**
     * Makes a call to the C smart contract using the `calldataParam(string)bool` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    calldataParam: (params: CallParams<CArgs['obj']['calldataParam(string)bool'] | CArgs['tuple']['calldataParam(string)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(CParamsFactory.calldataParam(params))
    },

    /**
     * Makes a call to the C smart contract using the `calldataSlice(byte[])bool` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    calldataSlice: (params: CallParams<CArgs['obj']['calldataSlice(byte[])bool'] | CArgs['tuple']['calldataSlice(byte[])bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(CParamsFactory.calldataSlice(params))
    },

    /**
     * Makes a call to the C smart contract using the `literalParam()bool` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    literalParam: (params: CallParams<CArgs['obj']['literalParam()bool'] | CArgs['tuple']['literalParam()bool']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(CParamsFactory.literalParam(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Makes a clear_state call to an existing instance of the C smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the C smart contract using the `storageVar()bool` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    storageVar: (params: CallParams<CArgs['obj']['storageVar()bool'] | CArgs['tuple']['storageVar()bool']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(CParamsFactory.storageVar(params))
    },

    /**
     * Makes a call to the C smart contract using the `constStorageVar()bool` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    constStorageVar: (params: CallParams<CArgs['obj']['constStorageVar()bool'] | CArgs['tuple']['constStorageVar()bool']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(CParamsFactory.constStorageVar(params))
    },

    /**
     * Makes a call to the C smart contract using the `memoryVar()bool` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    memoryVar: (params: CallParams<CArgs['obj']['memoryVar()bool'] | CArgs['tuple']['memoryVar()bool']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(CParamsFactory.memoryVar(params))
    },

    /**
     * Makes a call to the C smart contract using the `calldataParam(string)bool` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    calldataParam: (params: CallParams<CArgs['obj']['calldataParam(string)bool'] | CArgs['tuple']['calldataParam(string)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(CParamsFactory.calldataParam(params))
    },

    /**
     * Makes a call to the C smart contract using the `calldataSlice(byte[])bool` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    calldataSlice: (params: CallParams<CArgs['obj']['calldataSlice(byte[])bool'] | CArgs['tuple']['calldataSlice(byte[])bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(CParamsFactory.calldataSlice(params))
    },

    /**
     * Makes a call to the C smart contract using the `literalParam()bool` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    literalParam: (params: CallParams<CArgs['obj']['literalParam()bool'] | CArgs['tuple']['literalParam()bool']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(CParamsFactory.literalParam(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Makes a clear_state call to an existing instance of the C smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the C smart contract using the `storageVar()bool` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    storageVar: async (params: CallParams<CArgs['obj']['storageVar()bool'] | CArgs['tuple']['storageVar()bool']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(CParamsFactory.storageVar(params))
      return {...result, return: result.return as unknown as (undefined | CReturns['storageVar()bool'])}
    },

    /**
     * Makes a call to the C smart contract using the `constStorageVar()bool` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    constStorageVar: async (params: CallParams<CArgs['obj']['constStorageVar()bool'] | CArgs['tuple']['constStorageVar()bool']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(CParamsFactory.constStorageVar(params))
      return {...result, return: result.return as unknown as (undefined | CReturns['constStorageVar()bool'])}
    },

    /**
     * Makes a call to the C smart contract using the `memoryVar()bool` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    memoryVar: async (params: CallParams<CArgs['obj']['memoryVar()bool'] | CArgs['tuple']['memoryVar()bool']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(CParamsFactory.memoryVar(params))
      return {...result, return: result.return as unknown as (undefined | CReturns['memoryVar()bool'])}
    },

    /**
     * Makes a call to the C smart contract using the `calldataParam(string)bool` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    calldataParam: async (params: CallParams<CArgs['obj']['calldataParam(string)bool'] | CArgs['tuple']['calldataParam(string)bool']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(CParamsFactory.calldataParam(params))
      return {...result, return: result.return as unknown as (undefined | CReturns['calldataParam(string)bool'])}
    },

    /**
     * Makes a call to the C smart contract using the `calldataSlice(byte[])bool` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    calldataSlice: async (params: CallParams<CArgs['obj']['calldataSlice(byte[])bool'] | CArgs['tuple']['calldataSlice(byte[])bool']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(CParamsFactory.calldataSlice(params))
      return {...result, return: result.return as unknown as (undefined | CReturns['calldataSlice(byte[])bool'])}
    },

    /**
     * Makes a call to the C smart contract using the `literalParam()bool` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    literalParam: async (params: CallParams<CArgs['obj']['literalParam()bool'] | CArgs['tuple']['literalParam()bool']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(CParamsFactory.literalParam(params))
      return {...result, return: result.return as unknown as (undefined | CReturns['literalParam()bool'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new CClient(this.appClient.clone(params))
  }

  /**
   * Makes a readonly (simulated) call to the C smart contract using the `storageVar()bool` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async storageVar(params: CallParams<CArgs['obj']['storageVar()bool'] | CArgs['tuple']['storageVar()bool']> = {args: []}) {
    const result = await this.appClient.send.call(CParamsFactory.storageVar(params))
    return result.return as unknown as CReturns['storageVar()bool']
  }

  /**
   * Makes a readonly (simulated) call to the C smart contract using the `constStorageVar()bool` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async constStorageVar(params: CallParams<CArgs['obj']['constStorageVar()bool'] | CArgs['tuple']['constStorageVar()bool']> = {args: []}) {
    const result = await this.appClient.send.call(CParamsFactory.constStorageVar(params))
    return result.return as unknown as CReturns['constStorageVar()bool']
  }

  /**
   * Makes a readonly (simulated) call to the C smart contract using the `memoryVar()bool` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async memoryVar(params: CallParams<CArgs['obj']['memoryVar()bool'] | CArgs['tuple']['memoryVar()bool']> = {args: []}) {
    const result = await this.appClient.send.call(CParamsFactory.memoryVar(params))
    return result.return as unknown as CReturns['memoryVar()bool']
  }

  /**
   * Makes a readonly (simulated) call to the C smart contract using the `calldataParam(string)bool` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async calldataParam(params: CallParams<CArgs['obj']['calldataParam(string)bool'] | CArgs['tuple']['calldataParam(string)bool']>) {
    const result = await this.appClient.send.call(CParamsFactory.calldataParam(params))
    return result.return as unknown as CReturns['calldataParam(string)bool']
  }

  /**
   * Makes a readonly (simulated) call to the C smart contract using the `calldataSlice(byte[])bool` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async calldataSlice(params: CallParams<CArgs['obj']['calldataSlice(byte[])bool'] | CArgs['tuple']['calldataSlice(byte[])bool']>) {
    const result = await this.appClient.send.call(CParamsFactory.calldataSlice(params))
    return result.return as unknown as CReturns['calldataSlice(byte[])bool']
  }

  /**
   * Makes a readonly (simulated) call to the C smart contract using the `literalParam()bool` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async literalParam(params: CallParams<CArgs['obj']['literalParam()bool'] | CArgs['tuple']['literalParam()bool']> = {args: []}) {
    const result = await this.appClient.send.call(CParamsFactory.literalParam(params))
    return result.return as unknown as CReturns['literalParam()bool']
  }

  /**
   * Methods to access state for the current C app
   */
  state = {
    /**
     * Methods to access global state for the current C app
     */
    global: {
      /**
       * Get all current keyed values from global state
       */
      getAll: async (): Promise<Partial<Expand<GlobalKeysState>>> => {
        const result = await this.appClient.state.global.getAll()
        return {
          storageVarStr: result.storageVarStr,
        }
      },
      /**
       * Get the current value of the storageVarStr key in global state
       */
      storageVarStr: async (): Promise<string | undefined> => { return (await this.appClient.state.global.getValue("storageVarStr")) as string | undefined },
    },
  }

  public newGroup(): CComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a storageVar()bool method call against the C contract
       */
      storageVar(params: CallParams<CArgs['obj']['storageVar()bool'] | CArgs['tuple']['storageVar()bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.storageVar(params)))
        resultMappers.push((v) => client.decodeReturnValue('storageVar()bool', v))
        return this
      },
      /**
       * Add a constStorageVar()bool method call against the C contract
       */
      constStorageVar(params: CallParams<CArgs['obj']['constStorageVar()bool'] | CArgs['tuple']['constStorageVar()bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.constStorageVar(params)))
        resultMappers.push((v) => client.decodeReturnValue('constStorageVar()bool', v))
        return this
      },
      /**
       * Add a memoryVar()bool method call against the C contract
       */
      memoryVar(params: CallParams<CArgs['obj']['memoryVar()bool'] | CArgs['tuple']['memoryVar()bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.memoryVar(params)))
        resultMappers.push((v) => client.decodeReturnValue('memoryVar()bool', v))
        return this
      },
      /**
       * Add a calldataParam(string)bool method call against the C contract
       */
      calldataParam(params: CallParams<CArgs['obj']['calldataParam(string)bool'] | CArgs['tuple']['calldataParam(string)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.calldataParam(params)))
        resultMappers.push((v) => client.decodeReturnValue('calldataParam(string)bool', v))
        return this
      },
      /**
       * Add a calldataSlice(byte[])bool method call against the C contract
       */
      calldataSlice(params: CallParams<CArgs['obj']['calldataSlice(byte[])bool'] | CArgs['tuple']['calldataSlice(byte[])bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.calldataSlice(params)))
        resultMappers.push((v) => client.decodeReturnValue('calldataSlice(byte[])bool', v))
        return this
      },
      /**
       * Add a literalParam()bool method call against the C contract
       */
      literalParam(params: CallParams<CArgs['obj']['literalParam()bool'] | CArgs['tuple']['literalParam()bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.literalParam(params)))
        resultMappers.push((v) => client.decodeReturnValue('literalParam()bool', v))
        return this
      },
      /**
       * Add a clear state call to the C contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as CComposer
  }
}
export type CComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the storageVar()bool ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  storageVar(params?: CallParams<CArgs['obj']['storageVar()bool'] | CArgs['tuple']['storageVar()bool']>): CComposer<[...TReturns, CReturns['storageVar()bool'] | undefined]>

  /**
   * Calls the constStorageVar()bool ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  constStorageVar(params?: CallParams<CArgs['obj']['constStorageVar()bool'] | CArgs['tuple']['constStorageVar()bool']>): CComposer<[...TReturns, CReturns['constStorageVar()bool'] | undefined]>

  /**
   * Calls the memoryVar()bool ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  memoryVar(params?: CallParams<CArgs['obj']['memoryVar()bool'] | CArgs['tuple']['memoryVar()bool']>): CComposer<[...TReturns, CReturns['memoryVar()bool'] | undefined]>

  /**
   * Calls the calldataParam(string)bool ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  calldataParam(params?: CallParams<CArgs['obj']['calldataParam(string)bool'] | CArgs['tuple']['calldataParam(string)bool']>): CComposer<[...TReturns, CReturns['calldataParam(string)bool'] | undefined]>

  /**
   * Calls the calldataSlice(byte[])bool ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  calldataSlice(params?: CallParams<CArgs['obj']['calldataSlice(byte[])bool'] | CArgs['tuple']['calldataSlice(byte[])bool']>): CComposer<[...TReturns, CReturns['calldataSlice(byte[])bool'] | undefined]>

  /**
   * Calls the literalParam()bool ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  literalParam(params?: CallParams<CArgs['obj']['literalParam()bool'] | CArgs['tuple']['literalParam()bool']>): CComposer<[...TReturns, CReturns['literalParam()bool'] | undefined]>

  /**
   * Makes a clear_state call to an existing instance of the C smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): CComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): CComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<CComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<CComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<CComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<CComposerResults<TReturns>>
}
export type CComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

