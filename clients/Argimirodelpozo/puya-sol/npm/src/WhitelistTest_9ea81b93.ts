/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"WhitelistTest","structs":{},"methods":[{"name":"admin","args":[],"returns":{"type":"address"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"isWhitelisted","args":[{"type":"address","name":"account"}],"returns":{"type":"bool"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"whitelistCount","args":[],"returns":{"type":"uint512"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"addToWhitelist","args":[{"type":"address","name":"account"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}},{"name":"removeFromWhitelist","args":[{"type":"address","name":"account"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}},{"name":"batchAdd","args":[{"type":"address","name":"a1"},{"type":"address","name":"a2"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}}],"arcs":[22,28],"desc":" @dev Whitelist access control pattern.\n Demonstrates mapping-based permission management.","networks":{},"state":{"schema":{"global":{"ints":0,"bytes":2},"local":{"ints":0,"bytes":0}},"keys":{"global":{"_admin":{"keyType":"AVMString","valueType":"address","key":"X2FkbWlu"},"_count":{"keyType":"AVMString","valueType":"AVMBytes","key":"X2NvdW50"}},"local":{},"box":{}},"maps":{"global":{},"local":{},"box":{"_whitelisted":{"keyType":"AVMBytes","valueType":"uint64","prefix":"X3doaXRlbGlzdGVk"}}}},"bareActions":{"create":[],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[240],"errorMessage":"Whitelist: already whitelisted"},{"pc":[225,271,327],"errorMessage":"Whitelist: not admin"},{"pc":[285],"errorMessage":"Whitelist: not whitelisted"},{"pc":[152,223,269,325],"errorMessage":"check _admin exists"},{"pc":[195,247,292,353,389],"errorMessage":"check _count exists"},{"pc":[166,217,263,309,319],"errorMessage":"invalid number of bytes for arc4.static_array<arc4.uint8, 32>"},{"pc":[200],"errorMessage":"overflow"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"events":[]} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


/**
 * The argument types for the WhitelistTest contract
 */
export type WhitelistTestArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'admin()address': Record<string, never>
    'isWhitelisted(address)bool': {
      account: string
    }
    'whitelistCount()uint512': Record<string, never>
    'addToWhitelist(address)void': {
      account: string
    }
    'removeFromWhitelist(address)void': {
      account: string
    }
    'batchAdd(address,address)void': {
      a1: string
      a2: string
    }
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'admin()address': []
    'isWhitelisted(address)bool': [account: string]
    'whitelistCount()uint512': []
    'addToWhitelist(address)void': [account: string]
    'removeFromWhitelist(address)void': [account: string]
    'batchAdd(address,address)void': [a1: string, a2: string]
  }
}

/**
 * The return type for each method
 */
export type WhitelistTestReturns = {
  'admin()address': string
  'isWhitelisted(address)bool': boolean
  'whitelistCount()uint512': bigint
  'addToWhitelist(address)void': void
  'removeFromWhitelist(address)void': void
  'batchAdd(address,address)void': void
}

/**
 * Defines the types of available calls and state of the WhitelistTest smart contract.
 */
export type WhitelistTestTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'admin()address' | 'admin', {
      argsObj: WhitelistTestArgs['obj']['admin()address']
      argsTuple: WhitelistTestArgs['tuple']['admin()address']
      returns: WhitelistTestReturns['admin()address']
    }>
    & Record<'isWhitelisted(address)bool' | 'isWhitelisted', {
      argsObj: WhitelistTestArgs['obj']['isWhitelisted(address)bool']
      argsTuple: WhitelistTestArgs['tuple']['isWhitelisted(address)bool']
      returns: WhitelistTestReturns['isWhitelisted(address)bool']
    }>
    & Record<'whitelistCount()uint512' | 'whitelistCount', {
      argsObj: WhitelistTestArgs['obj']['whitelistCount()uint512']
      argsTuple: WhitelistTestArgs['tuple']['whitelistCount()uint512']
      returns: WhitelistTestReturns['whitelistCount()uint512']
    }>
    & Record<'addToWhitelist(address)void' | 'addToWhitelist', {
      argsObj: WhitelistTestArgs['obj']['addToWhitelist(address)void']
      argsTuple: WhitelistTestArgs['tuple']['addToWhitelist(address)void']
      returns: WhitelistTestReturns['addToWhitelist(address)void']
    }>
    & Record<'removeFromWhitelist(address)void' | 'removeFromWhitelist', {
      argsObj: WhitelistTestArgs['obj']['removeFromWhitelist(address)void']
      argsTuple: WhitelistTestArgs['tuple']['removeFromWhitelist(address)void']
      returns: WhitelistTestReturns['removeFromWhitelist(address)void']
    }>
    & Record<'batchAdd(address,address)void' | 'batchAdd', {
      argsObj: WhitelistTestArgs['obj']['batchAdd(address,address)void']
      argsTuple: WhitelistTestArgs['tuple']['batchAdd(address,address)void']
      returns: WhitelistTestReturns['batchAdd(address,address)void']
    }>
  /**
   * Defines the shape of the state of the application.
   */
  state: {
    global: {
      keys: {
        _admin: string
        _count: BinaryState
      }
      maps: {}
    }
    box: {
      keys: {}
      maps: {
        _whitelisted: Map<Uint8Array | string, bigint>
      }
    }
  }
}

/**
 * Defines the possible abi call signatures.
 */
export type WhitelistTestSignatures = keyof WhitelistTestTypes['methods']
/**
 * Defines the possible abi call signatures for methods that return a non-void value.
 */
export type WhitelistTestNonVoidMethodSignatures = keyof WhitelistTestTypes['methods'] extends infer T ? T extends keyof WhitelistTestTypes['methods'] ? MethodReturn<T> extends void ? never : T  : never : never
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the WhitelistTest smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends WhitelistTestSignatures> = WhitelistTestTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the WhitelistTest smart contract to the method's return type
 */
export type MethodReturn<TSignature extends WhitelistTestSignatures> = WhitelistTestTypes['methods'][TSignature]['returns']

/**
 * Defines the shape of the keyed global state of the application.
 */
export type GlobalKeysState = WhitelistTestTypes['state']['global']['keys']

/**
 * Defines the shape of the keyed box state of the application.
 */
export type BoxKeysState = WhitelistTestTypes['state']['box']['keys']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the WhitelistTest smart contract
 */
export abstract class WhitelistTestParamsFactory {
  /**
   * Constructs a no op call for the admin()address ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static admin(params: CallParams<WhitelistTestArgs['obj']['admin()address'] | WhitelistTestArgs['tuple']['admin()address']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'admin()address' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the isWhitelisted(address)bool ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static isWhitelisted(params: CallParams<WhitelistTestArgs['obj']['isWhitelisted(address)bool'] | WhitelistTestArgs['tuple']['isWhitelisted(address)bool']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'isWhitelisted(address)bool' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.account],
    }
  }
  /**
   * Constructs a no op call for the whitelistCount()uint512 ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static whitelistCount(params: CallParams<WhitelistTestArgs['obj']['whitelistCount()uint512'] | WhitelistTestArgs['tuple']['whitelistCount()uint512']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'whitelistCount()uint512' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the addToWhitelist(address)void ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static addToWhitelist(params: CallParams<WhitelistTestArgs['obj']['addToWhitelist(address)void'] | WhitelistTestArgs['tuple']['addToWhitelist(address)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'addToWhitelist(address)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.account],
    }
  }
  /**
   * Constructs a no op call for the removeFromWhitelist(address)void ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static removeFromWhitelist(params: CallParams<WhitelistTestArgs['obj']['removeFromWhitelist(address)void'] | WhitelistTestArgs['tuple']['removeFromWhitelist(address)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'removeFromWhitelist(address)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.account],
    }
  }
  /**
   * Constructs a no op call for the batchAdd(address,address)void ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static batchAdd(params: CallParams<WhitelistTestArgs['obj']['batchAdd(address,address)void'] | WhitelistTestArgs['tuple']['batchAdd(address,address)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'batchAdd(address,address)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.a1, params.args.a2],
    }
  }
}

/**
 * A client to make calls to the WhitelistTest smart contract
 */
export class WhitelistTestClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `WhitelistTestClient`
   *
   * @param appClient An `AppClient` instance which has been created with the WhitelistTest app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `WhitelistTestClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Checks for decode errors on the given return value and maps the return value to the return type for the given method
   * @returns The typed return value or undefined if there was no value
   */
  decodeReturnValue<TSignature extends WhitelistTestNonVoidMethodSignatures>(method: TSignature, returnValue: ABIReturn | undefined) {
    return returnValue !== undefined ? getArc56ReturnValue<MethodReturn<TSignature>>(returnValue, this.appClient.getABIMethod(method), APP_SPEC.structs) : undefined
  }

  /**
   * Returns a new `WhitelistTestClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<WhitelistTestClient> {
    return new WhitelistTestClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `WhitelistTestClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<WhitelistTestClient> {
    return new WhitelistTestClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Makes a clear_state call to an existing instance of the WhitelistTest smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the WhitelistTest smart contract using the `admin()address` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    admin: (params: CallParams<WhitelistTestArgs['obj']['admin()address'] | WhitelistTestArgs['tuple']['admin()address']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(WhitelistTestParamsFactory.admin(params))
    },

    /**
     * Makes a call to the WhitelistTest smart contract using the `isWhitelisted(address)bool` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    isWhitelisted: (params: CallParams<WhitelistTestArgs['obj']['isWhitelisted(address)bool'] | WhitelistTestArgs['tuple']['isWhitelisted(address)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(WhitelistTestParamsFactory.isWhitelisted(params))
    },

    /**
     * Makes a call to the WhitelistTest smart contract using the `whitelistCount()uint512` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    whitelistCount: (params: CallParams<WhitelistTestArgs['obj']['whitelistCount()uint512'] | WhitelistTestArgs['tuple']['whitelistCount()uint512']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(WhitelistTestParamsFactory.whitelistCount(params))
    },

    /**
     * Makes a call to the WhitelistTest smart contract using the `addToWhitelist(address)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    addToWhitelist: (params: CallParams<WhitelistTestArgs['obj']['addToWhitelist(address)void'] | WhitelistTestArgs['tuple']['addToWhitelist(address)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(WhitelistTestParamsFactory.addToWhitelist(params))
    },

    /**
     * Makes a call to the WhitelistTest smart contract using the `removeFromWhitelist(address)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    removeFromWhitelist: (params: CallParams<WhitelistTestArgs['obj']['removeFromWhitelist(address)void'] | WhitelistTestArgs['tuple']['removeFromWhitelist(address)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(WhitelistTestParamsFactory.removeFromWhitelist(params))
    },

    /**
     * Makes a call to the WhitelistTest smart contract using the `batchAdd(address,address)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    batchAdd: (params: CallParams<WhitelistTestArgs['obj']['batchAdd(address,address)void'] | WhitelistTestArgs['tuple']['batchAdd(address,address)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(WhitelistTestParamsFactory.batchAdd(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Makes a clear_state call to an existing instance of the WhitelistTest smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the WhitelistTest smart contract using the `admin()address` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    admin: (params: CallParams<WhitelistTestArgs['obj']['admin()address'] | WhitelistTestArgs['tuple']['admin()address']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(WhitelistTestParamsFactory.admin(params))
    },

    /**
     * Makes a call to the WhitelistTest smart contract using the `isWhitelisted(address)bool` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    isWhitelisted: (params: CallParams<WhitelistTestArgs['obj']['isWhitelisted(address)bool'] | WhitelistTestArgs['tuple']['isWhitelisted(address)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(WhitelistTestParamsFactory.isWhitelisted(params))
    },

    /**
     * Makes a call to the WhitelistTest smart contract using the `whitelistCount()uint512` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    whitelistCount: (params: CallParams<WhitelistTestArgs['obj']['whitelistCount()uint512'] | WhitelistTestArgs['tuple']['whitelistCount()uint512']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(WhitelistTestParamsFactory.whitelistCount(params))
    },

    /**
     * Makes a call to the WhitelistTest smart contract using the `addToWhitelist(address)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    addToWhitelist: (params: CallParams<WhitelistTestArgs['obj']['addToWhitelist(address)void'] | WhitelistTestArgs['tuple']['addToWhitelist(address)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(WhitelistTestParamsFactory.addToWhitelist(params))
    },

    /**
     * Makes a call to the WhitelistTest smart contract using the `removeFromWhitelist(address)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    removeFromWhitelist: (params: CallParams<WhitelistTestArgs['obj']['removeFromWhitelist(address)void'] | WhitelistTestArgs['tuple']['removeFromWhitelist(address)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(WhitelistTestParamsFactory.removeFromWhitelist(params))
    },

    /**
     * Makes a call to the WhitelistTest smart contract using the `batchAdd(address,address)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    batchAdd: (params: CallParams<WhitelistTestArgs['obj']['batchAdd(address,address)void'] | WhitelistTestArgs['tuple']['batchAdd(address,address)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(WhitelistTestParamsFactory.batchAdd(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Makes a clear_state call to an existing instance of the WhitelistTest smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the WhitelistTest smart contract using the `admin()address` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    admin: async (params: CallParams<WhitelistTestArgs['obj']['admin()address'] | WhitelistTestArgs['tuple']['admin()address']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(WhitelistTestParamsFactory.admin(params))
      return {...result, return: result.return as unknown as (undefined | WhitelistTestReturns['admin()address'])}
    },

    /**
     * Makes a call to the WhitelistTest smart contract using the `isWhitelisted(address)bool` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    isWhitelisted: async (params: CallParams<WhitelistTestArgs['obj']['isWhitelisted(address)bool'] | WhitelistTestArgs['tuple']['isWhitelisted(address)bool']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(WhitelistTestParamsFactory.isWhitelisted(params))
      return {...result, return: result.return as unknown as (undefined | WhitelistTestReturns['isWhitelisted(address)bool'])}
    },

    /**
     * Makes a call to the WhitelistTest smart contract using the `whitelistCount()uint512` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    whitelistCount: async (params: CallParams<WhitelistTestArgs['obj']['whitelistCount()uint512'] | WhitelistTestArgs['tuple']['whitelistCount()uint512']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(WhitelistTestParamsFactory.whitelistCount(params))
      return {...result, return: result.return as unknown as (undefined | WhitelistTestReturns['whitelistCount()uint512'])}
    },

    /**
     * Makes a call to the WhitelistTest smart contract using the `addToWhitelist(address)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    addToWhitelist: async (params: CallParams<WhitelistTestArgs['obj']['addToWhitelist(address)void'] | WhitelistTestArgs['tuple']['addToWhitelist(address)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(WhitelistTestParamsFactory.addToWhitelist(params))
      return {...result, return: result.return as unknown as (undefined | WhitelistTestReturns['addToWhitelist(address)void'])}
    },

    /**
     * Makes a call to the WhitelistTest smart contract using the `removeFromWhitelist(address)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    removeFromWhitelist: async (params: CallParams<WhitelistTestArgs['obj']['removeFromWhitelist(address)void'] | WhitelistTestArgs['tuple']['removeFromWhitelist(address)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(WhitelistTestParamsFactory.removeFromWhitelist(params))
      return {...result, return: result.return as unknown as (undefined | WhitelistTestReturns['removeFromWhitelist(address)void'])}
    },

    /**
     * Makes a call to the WhitelistTest smart contract using the `batchAdd(address,address)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    batchAdd: async (params: CallParams<WhitelistTestArgs['obj']['batchAdd(address,address)void'] | WhitelistTestArgs['tuple']['batchAdd(address,address)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(WhitelistTestParamsFactory.batchAdd(params))
      return {...result, return: result.return as unknown as (undefined | WhitelistTestReturns['batchAdd(address,address)void'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new WhitelistTestClient(this.appClient.clone(params))
  }

  /**
   * Makes a readonly (simulated) call to the WhitelistTest smart contract using the `admin()address` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async admin(params: CallParams<WhitelistTestArgs['obj']['admin()address'] | WhitelistTestArgs['tuple']['admin()address']> = {args: []}) {
    const result = await this.appClient.send.call(WhitelistTestParamsFactory.admin(params))
    return result.return as unknown as WhitelistTestReturns['admin()address']
  }

  /**
   * Makes a readonly (simulated) call to the WhitelistTest smart contract using the `isWhitelisted(address)bool` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async isWhitelisted(params: CallParams<WhitelistTestArgs['obj']['isWhitelisted(address)bool'] | WhitelistTestArgs['tuple']['isWhitelisted(address)bool']>) {
    const result = await this.appClient.send.call(WhitelistTestParamsFactory.isWhitelisted(params))
    return result.return as unknown as WhitelistTestReturns['isWhitelisted(address)bool']
  }

  /**
   * Makes a readonly (simulated) call to the WhitelistTest smart contract using the `whitelistCount()uint512` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async whitelistCount(params: CallParams<WhitelistTestArgs['obj']['whitelistCount()uint512'] | WhitelistTestArgs['tuple']['whitelistCount()uint512']> = {args: []}) {
    const result = await this.appClient.send.call(WhitelistTestParamsFactory.whitelistCount(params))
    return result.return as unknown as WhitelistTestReturns['whitelistCount()uint512']
  }

  /**
   * Methods to access state for the current WhitelistTest app
   */
  state = {
    /**
     * Methods to access global state for the current WhitelistTest app
     */
    global: {
      /**
       * Get all current keyed values from global state
       */
      getAll: async (): Promise<Partial<Expand<GlobalKeysState>>> => {
        const result = await this.appClient.state.global.getAll()
        return {
          _admin: result._admin,
          _count: new BinaryStateValue(result._count),
        }
      },
      /**
       * Get the current value of the _admin key in global state
       */
      _admin: async (): Promise<string | undefined> => { return (await this.appClient.state.global.getValue("_admin")) as string | undefined },
      /**
       * Get the current value of the _count key in global state
       */
      _count: async (): Promise<BinaryState> => { return new BinaryStateValue((await this.appClient.state.global.getValue("_count")) as Uint8Array | undefined) },
    },
    /**
     * Methods to access box state for the current WhitelistTest app
     */
    box: {
      /**
       * Get all current keyed values from box state
       */
      getAll: async (): Promise<Partial<Expand<BoxKeysState>>> => {
        const result = await this.appClient.state.box.getAll()
        return {
        }
      },
      /**
       * Get values from the _whitelisted map in box state
       */
      _whitelisted: {
        /**
         * Get all current values of the _whitelisted map in box state
         */
        getMap: async (): Promise<Map<Uint8Array, bigint>> => { return (await this.appClient.state.box.getMap("_whitelisted")) as Map<Uint8Array, bigint> },
        /**
         * Get a current value of the _whitelisted map by key from box state
         */
        value: async (key: Uint8Array | string): Promise<bigint | undefined> => { return await this.appClient.state.box.getMapValue("_whitelisted", key) as bigint | undefined },
      },
    },
  }

  public newGroup(): WhitelistTestComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a admin()address method call against the WhitelistTest contract
       */
      admin(params: CallParams<WhitelistTestArgs['obj']['admin()address'] | WhitelistTestArgs['tuple']['admin()address']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.admin(params)))
        resultMappers.push((v) => client.decodeReturnValue('admin()address', v))
        return this
      },
      /**
       * Add a isWhitelisted(address)bool method call against the WhitelistTest contract
       */
      isWhitelisted(params: CallParams<WhitelistTestArgs['obj']['isWhitelisted(address)bool'] | WhitelistTestArgs['tuple']['isWhitelisted(address)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.isWhitelisted(params)))
        resultMappers.push((v) => client.decodeReturnValue('isWhitelisted(address)bool', v))
        return this
      },
      /**
       * Add a whitelistCount()uint512 method call against the WhitelistTest contract
       */
      whitelistCount(params: CallParams<WhitelistTestArgs['obj']['whitelistCount()uint512'] | WhitelistTestArgs['tuple']['whitelistCount()uint512']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.whitelistCount(params)))
        resultMappers.push((v) => client.decodeReturnValue('whitelistCount()uint512', v))
        return this
      },
      /**
       * Add a addToWhitelist(address)void method call against the WhitelistTest contract
       */
      addToWhitelist(params: CallParams<WhitelistTestArgs['obj']['addToWhitelist(address)void'] | WhitelistTestArgs['tuple']['addToWhitelist(address)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.addToWhitelist(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a removeFromWhitelist(address)void method call against the WhitelistTest contract
       */
      removeFromWhitelist(params: CallParams<WhitelistTestArgs['obj']['removeFromWhitelist(address)void'] | WhitelistTestArgs['tuple']['removeFromWhitelist(address)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.removeFromWhitelist(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a batchAdd(address,address)void method call against the WhitelistTest contract
       */
      batchAdd(params: CallParams<WhitelistTestArgs['obj']['batchAdd(address,address)void'] | WhitelistTestArgs['tuple']['batchAdd(address,address)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.batchAdd(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a clear state call to the WhitelistTest contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as WhitelistTestComposer
  }
}
export type WhitelistTestComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the admin()address ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  admin(params?: CallParams<WhitelistTestArgs['obj']['admin()address'] | WhitelistTestArgs['tuple']['admin()address']>): WhitelistTestComposer<[...TReturns, WhitelistTestReturns['admin()address'] | undefined]>

  /**
   * Calls the isWhitelisted(address)bool ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  isWhitelisted(params?: CallParams<WhitelistTestArgs['obj']['isWhitelisted(address)bool'] | WhitelistTestArgs['tuple']['isWhitelisted(address)bool']>): WhitelistTestComposer<[...TReturns, WhitelistTestReturns['isWhitelisted(address)bool'] | undefined]>

  /**
   * Calls the whitelistCount()uint512 ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  whitelistCount(params?: CallParams<WhitelistTestArgs['obj']['whitelistCount()uint512'] | WhitelistTestArgs['tuple']['whitelistCount()uint512']>): WhitelistTestComposer<[...TReturns, WhitelistTestReturns['whitelistCount()uint512'] | undefined]>

  /**
   * Calls the addToWhitelist(address)void ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  addToWhitelist(params?: CallParams<WhitelistTestArgs['obj']['addToWhitelist(address)void'] | WhitelistTestArgs['tuple']['addToWhitelist(address)void']>): WhitelistTestComposer<[...TReturns, WhitelistTestReturns['addToWhitelist(address)void'] | undefined]>

  /**
   * Calls the removeFromWhitelist(address)void ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  removeFromWhitelist(params?: CallParams<WhitelistTestArgs['obj']['removeFromWhitelist(address)void'] | WhitelistTestArgs['tuple']['removeFromWhitelist(address)void']>): WhitelistTestComposer<[...TReturns, WhitelistTestReturns['removeFromWhitelist(address)void'] | undefined]>

  /**
   * Calls the batchAdd(address,address)void ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  batchAdd(params?: CallParams<WhitelistTestArgs['obj']['batchAdd(address,address)void'] | WhitelistTestArgs['tuple']['batchAdd(address,address)void']>): WhitelistTestComposer<[...TReturns, WhitelistTestReturns['batchAdd(address,address)void'] | undefined]>

  /**
   * Makes a clear_state call to an existing instance of the WhitelistTest smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): WhitelistTestComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): WhitelistTestComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<WhitelistTestComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<WhitelistTestComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<WhitelistTestComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<WhitelistTestComposerResults<TReturns>>
}
export type WhitelistTestComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

