/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"LibZipWrapper__Helper4","structs":{},"methods":[{"name":"LibZip.flzDecompress","args":[{"type":"byte[]","name":"data"}],"returns":{"type":"byte[]"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"desc":"@dev Returns the decompressed `data`.","events":[],"recommendations":{}},{"name":"__init__","args":[{"type":"uint64","name":"o"},{"type":"uint64","name":"p"},{"type":"byte[]","name":"s"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}}],"arcs":[22,28],"desc":"Helper contract 4 for LibZipWrapper","networks":{},"state":{"schema":{"global":{"ints":2,"bytes":1},"local":{"ints":0,"bytes":0}},"keys":{"global":{"o":{"keyType":"AVMString","valueType":"AVMUint64","key":"bw=="},"p":{"keyType":"AVMString","valueType":"AVMUint64","key":"cA=="},"s":{"keyType":"AVMString","valueType":"AVMBytes","key":"cw=="}},"local":{},"box":{}},"maps":{"global":{},"local":{},"box":{}}},"bareActions":{"create":["NoOp"],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[546],"errorMessage":"helper: already initialized"},{"pc":[440],"errorMessage":"helper: must be called in group"},{"pc":[454],"errorMessage":"helper: sender mismatch"},{"pc":[447],"errorMessage":"helper: unauthorized caller"},{"pc":[471],"errorMessage":"helper: wrong prev chunk"},{"pc":[484],"errorMessage":"helper: wrong prev method"},{"pc":[418,531],"errorMessage":"invalid array length header"},{"pc":[426,539],"errorMessage":"invalid number of bytes for arc4.dynamic_array<arc4.uint8>"},{"pc":[515,524],"errorMessage":"invalid number of bytes for arc4.uint64"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"events":[]} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


/**
 * The argument types for the LibZipWrapperHelper4 contract
 */
export type LibZipWrapperHelper4Args = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'LibZip.flzDecompress(byte[])byte[]': {
      data: Uint8Array
    }
    '__init__(uint64,uint64,byte[])void': {
      o: bigint | number
      p: bigint | number
      s: Uint8Array
    }
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'LibZip.flzDecompress(byte[])byte[]': [data: Uint8Array]
    '__init__(uint64,uint64,byte[])void': [o: bigint | number, p: bigint | number, s: Uint8Array]
  }
}

/**
 * The return type for each method
 */
export type LibZipWrapperHelper4Returns = {
  'LibZip.flzDecompress(byte[])byte[]': Uint8Array
  '__init__(uint64,uint64,byte[])void': void
}

/**
 * Defines the types of available calls and state of the LibZipWrapperHelper4 smart contract.
 */
export type LibZipWrapperHelper4Types = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'LibZip.flzDecompress(byte[])byte[]' | 'LibZip.flzDecompress', {
      argsObj: LibZipWrapperHelper4Args['obj']['LibZip.flzDecompress(byte[])byte[]']
      argsTuple: LibZipWrapperHelper4Args['tuple']['LibZip.flzDecompress(byte[])byte[]']
      returns: LibZipWrapperHelper4Returns['LibZip.flzDecompress(byte[])byte[]']
    }>
    & Record<'__init__(uint64,uint64,byte[])void' | '__init__', {
      argsObj: LibZipWrapperHelper4Args['obj']['__init__(uint64,uint64,byte[])void']
      argsTuple: LibZipWrapperHelper4Args['tuple']['__init__(uint64,uint64,byte[])void']
      returns: LibZipWrapperHelper4Returns['__init__(uint64,uint64,byte[])void']
    }>
  /**
   * Defines the shape of the state of the application.
   */
  state: {
    global: {
      keys: {
        o: bigint
        p: bigint
        s: BinaryState
      }
      maps: {}
    }
  }
}

/**
 * Defines the possible abi call signatures.
 */
export type LibZipWrapperHelper4Signatures = keyof LibZipWrapperHelper4Types['methods']
/**
 * Defines the possible abi call signatures for methods that return a non-void value.
 */
export type LibZipWrapperHelper4NonVoidMethodSignatures = keyof LibZipWrapperHelper4Types['methods'] extends infer T ? T extends keyof LibZipWrapperHelper4Types['methods'] ? MethodReturn<T> extends void ? never : T  : never : never
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the LibZipWrapperHelper4 smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends LibZipWrapperHelper4Signatures> = LibZipWrapperHelper4Types['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the LibZipWrapperHelper4 smart contract to the method's return type
 */
export type MethodReturn<TSignature extends LibZipWrapperHelper4Signatures> = LibZipWrapperHelper4Types['methods'][TSignature]['returns']

/**
 * Defines the shape of the keyed global state of the application.
 */
export type GlobalKeysState = LibZipWrapperHelper4Types['state']['global']['keys']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the LibZipWrapperHelper4 smart contract
 */
export abstract class LibZipWrapperHelper4ParamsFactory {
  /**
   * Constructs a no op call for the LibZip.flzDecompress(byte[])byte[] ABI method
   *
   * @dev Returns the decompressed `data`.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static libZipFlzDecompress(params: CallParams<LibZipWrapperHelper4Args['obj']['LibZip.flzDecompress(byte[])byte[]'] | LibZipWrapperHelper4Args['tuple']['LibZip.flzDecompress(byte[])byte[]']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'LibZip.flzDecompress(byte[])byte[]' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.data],
    }
  }
  /**
   * Constructs a no op call for the __init__(uint64,uint64,byte[])void ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static __init(params: CallParams<LibZipWrapperHelper4Args['obj']['__init__(uint64,uint64,byte[])void'] | LibZipWrapperHelper4Args['tuple']['__init__(uint64,uint64,byte[])void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: '__init__(uint64,uint64,byte[])void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.o, params.args.p, params.args.s],
    }
  }
}

/**
 * A client to make calls to the LibZipWrapper__Helper4 smart contract
 */
export class LibZipWrapperHelper4Client {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `LibZipWrapperHelper4Client`
   *
   * @param appClient An `AppClient` instance which has been created with the LibZipWrapperHelper4 app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `LibZipWrapperHelper4Client`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Checks for decode errors on the given return value and maps the return value to the return type for the given method
   * @returns The typed return value or undefined if there was no value
   */
  decodeReturnValue<TSignature extends LibZipWrapperHelper4NonVoidMethodSignatures>(method: TSignature, returnValue: ABIReturn | undefined) {
    return returnValue !== undefined ? getArc56ReturnValue<MethodReturn<TSignature>>(returnValue, this.appClient.getABIMethod(method), APP_SPEC.structs) : undefined
  }

  /**
   * Returns a new `LibZipWrapperHelper4Client` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<LibZipWrapperHelper4Client> {
    return new LibZipWrapperHelper4Client(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `LibZipWrapperHelper4Client` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<LibZipWrapperHelper4Client> {
    return new LibZipWrapperHelper4Client(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Makes a clear_state call to an existing instance of the LibZipWrapper__Helper4 smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the LibZipWrapper__Helper4 smart contract using the `LibZip.flzDecompress(byte[])byte[]` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @dev Returns the decompressed `data`.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    libZipFlzDecompress: (params: CallParams<LibZipWrapperHelper4Args['obj']['LibZip.flzDecompress(byte[])byte[]'] | LibZipWrapperHelper4Args['tuple']['LibZip.flzDecompress(byte[])byte[]']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(LibZipWrapperHelper4ParamsFactory.libZipFlzDecompress(params))
    },

    /**
     * Makes a call to the LibZipWrapper__Helper4 smart contract using the `__init__(uint64,uint64,byte[])void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    __init: (params: CallParams<LibZipWrapperHelper4Args['obj']['__init__(uint64,uint64,byte[])void'] | LibZipWrapperHelper4Args['tuple']['__init__(uint64,uint64,byte[])void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(LibZipWrapperHelper4ParamsFactory.__init(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Makes a clear_state call to an existing instance of the LibZipWrapper__Helper4 smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the LibZipWrapper__Helper4 smart contract using the `LibZip.flzDecompress(byte[])byte[]` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @dev Returns the decompressed `data`.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    libZipFlzDecompress: (params: CallParams<LibZipWrapperHelper4Args['obj']['LibZip.flzDecompress(byte[])byte[]'] | LibZipWrapperHelper4Args['tuple']['LibZip.flzDecompress(byte[])byte[]']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(LibZipWrapperHelper4ParamsFactory.libZipFlzDecompress(params))
    },

    /**
     * Makes a call to the LibZipWrapper__Helper4 smart contract using the `__init__(uint64,uint64,byte[])void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    __init: (params: CallParams<LibZipWrapperHelper4Args['obj']['__init__(uint64,uint64,byte[])void'] | LibZipWrapperHelper4Args['tuple']['__init__(uint64,uint64,byte[])void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(LibZipWrapperHelper4ParamsFactory.__init(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Makes a clear_state call to an existing instance of the LibZipWrapper__Helper4 smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the LibZipWrapper__Helper4 smart contract using the `LibZip.flzDecompress(byte[])byte[]` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @dev Returns the decompressed `data`.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    libZipFlzDecompress: async (params: CallParams<LibZipWrapperHelper4Args['obj']['LibZip.flzDecompress(byte[])byte[]'] | LibZipWrapperHelper4Args['tuple']['LibZip.flzDecompress(byte[])byte[]']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(LibZipWrapperHelper4ParamsFactory.libZipFlzDecompress(params))
      return {...result, return: result.return as unknown as (undefined | LibZipWrapperHelper4Returns['LibZip.flzDecompress(byte[])byte[]'])}
    },

    /**
     * Makes a call to the LibZipWrapper__Helper4 smart contract using the `__init__(uint64,uint64,byte[])void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    __init: async (params: CallParams<LibZipWrapperHelper4Args['obj']['__init__(uint64,uint64,byte[])void'] | LibZipWrapperHelper4Args['tuple']['__init__(uint64,uint64,byte[])void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(LibZipWrapperHelper4ParamsFactory.__init(params))
      return {...result, return: result.return as unknown as (undefined | LibZipWrapperHelper4Returns['__init__(uint64,uint64,byte[])void'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new LibZipWrapperHelper4Client(this.appClient.clone(params))
  }

  /**
   * Makes a readonly (simulated) call to the LibZipWrapper__Helper4 smart contract using the `LibZip.flzDecompress(byte[])byte[]` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @dev Returns the decompressed `data`.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async libZipFlzDecompress(params: CallParams<LibZipWrapperHelper4Args['obj']['LibZip.flzDecompress(byte[])byte[]'] | LibZipWrapperHelper4Args['tuple']['LibZip.flzDecompress(byte[])byte[]']>) {
    const result = await this.appClient.send.call(LibZipWrapperHelper4ParamsFactory.libZipFlzDecompress(params))
    return result.return as unknown as LibZipWrapperHelper4Returns['LibZip.flzDecompress(byte[])byte[]']
  }

  /**
   * Methods to access state for the current LibZipWrapper__Helper4 app
   */
  state = {
    /**
     * Methods to access global state for the current LibZipWrapper__Helper4 app
     */
    global: {
      /**
       * Get all current keyed values from global state
       */
      getAll: async (): Promise<Partial<Expand<GlobalKeysState>>> => {
        const result = await this.appClient.state.global.getAll()
        return {
          o: result.o,
          p: result.p,
          s: new BinaryStateValue(result.s),
        }
      },
      /**
       * Get the current value of the o key in global state
       */
      o: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("o")) as bigint | undefined },
      /**
       * Get the current value of the p key in global state
       */
      p: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("p")) as bigint | undefined },
      /**
       * Get the current value of the s key in global state
       */
      s: async (): Promise<BinaryState> => { return new BinaryStateValue((await this.appClient.state.global.getValue("s")) as Uint8Array | undefined) },
    },
  }

  public newGroup(): LibZipWrapperHelper4Composer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a LibZip.flzDecompress(byte[])byte[] method call against the LibZipWrapper__Helper4 contract
       */
      libZipFlzDecompress(params: CallParams<LibZipWrapperHelper4Args['obj']['LibZip.flzDecompress(byte[])byte[]'] | LibZipWrapperHelper4Args['tuple']['LibZip.flzDecompress(byte[])byte[]']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.libZipFlzDecompress(params)))
        resultMappers.push((v) => client.decodeReturnValue('LibZip.flzDecompress(byte[])byte[]', v))
        return this
      },
      /**
       * Add a __init__(uint64,uint64,byte[])void method call against the LibZipWrapper__Helper4 contract
       */
      __init(params: CallParams<LibZipWrapperHelper4Args['obj']['__init__(uint64,uint64,byte[])void'] | LibZipWrapperHelper4Args['tuple']['__init__(uint64,uint64,byte[])void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.__init(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a clear state call to the LibZipWrapper__Helper4 contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as LibZipWrapperHelper4Composer
  }
}
export type LibZipWrapperHelper4Composer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the LibZip.flzDecompress(byte[])byte[] ABI method.
   *
   * @dev Returns the decompressed `data`.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  libZipFlzDecompress(params?: CallParams<LibZipWrapperHelper4Args['obj']['LibZip.flzDecompress(byte[])byte[]'] | LibZipWrapperHelper4Args['tuple']['LibZip.flzDecompress(byte[])byte[]']>): LibZipWrapperHelper4Composer<[...TReturns, LibZipWrapperHelper4Returns['LibZip.flzDecompress(byte[])byte[]'] | undefined]>

  /**
   * Calls the __init__(uint64,uint64,byte[])void ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  __init(params?: CallParams<LibZipWrapperHelper4Args['obj']['__init__(uint64,uint64,byte[])void'] | LibZipWrapperHelper4Args['tuple']['__init__(uint64,uint64,byte[])void']>): LibZipWrapperHelper4Composer<[...TReturns, LibZipWrapperHelper4Returns['__init__(uint64,uint64,byte[])void'] | undefined]>

  /**
   * Makes a clear_state call to an existing instance of the LibZipWrapper__Helper4 smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): LibZipWrapperHelper4Composer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): LibZipWrapperHelper4Composer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<LibZipWrapperHelper4ComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<LibZipWrapperHelper4ComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<LibZipWrapperHelper4ComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<LibZipWrapperHelper4ComposerResults<TReturns>>
}
export type LibZipWrapperHelper4ComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

