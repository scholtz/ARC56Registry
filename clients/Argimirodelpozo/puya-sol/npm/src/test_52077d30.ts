/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"test","structs":{},"methods":[{"name":"uintMinA","args":[],"returns":{"type":"bool"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"uintMinB","args":[],"returns":{"type":"bool"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"uintMinC","args":[],"returns":{"type":"bool"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"uintMinD","args":[],"returns":{"type":"bool"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"uintMaxA","args":[],"returns":{"type":"bool"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"uintMaxB","args":[],"returns":{"type":"bool"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"uintMaxC","args":[],"returns":{"type":"bool"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"uintMaxD","args":[],"returns":{"type":"bool"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}}],"arcs":[22,28],"networks":{},"state":{"schema":{"global":{"ints":0,"bytes":0},"local":{"ints":0,"bytes":0}},"keys":{"global":{},"local":{},"box":{}},"maps":{"global":{},"local":{},"box":{}}},"bareActions":{"create":[],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[144],"errorMessage":"not payable"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"events":[]} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


/**
 * The argument types for the Test contract
 */
export type TestArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'uintMinA()bool': Record<string, never>
    'uintMinB()bool': Record<string, never>
    'uintMinC()bool': Record<string, never>
    'uintMinD()bool': Record<string, never>
    'uintMaxA()bool': Record<string, never>
    'uintMaxB()bool': Record<string, never>
    'uintMaxC()bool': Record<string, never>
    'uintMaxD()bool': Record<string, never>
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'uintMinA()bool': []
    'uintMinB()bool': []
    'uintMinC()bool': []
    'uintMinD()bool': []
    'uintMaxA()bool': []
    'uintMaxB()bool': []
    'uintMaxC()bool': []
    'uintMaxD()bool': []
  }
}

/**
 * The return type for each method
 */
export type TestReturns = {
  'uintMinA()bool': boolean
  'uintMinB()bool': boolean
  'uintMinC()bool': boolean
  'uintMinD()bool': boolean
  'uintMaxA()bool': boolean
  'uintMaxB()bool': boolean
  'uintMaxC()bool': boolean
  'uintMaxD()bool': boolean
}

/**
 * Defines the types of available calls and state of the Test smart contract.
 */
export type TestTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'uintMinA()bool' | 'uintMinA', {
      argsObj: TestArgs['obj']['uintMinA()bool']
      argsTuple: TestArgs['tuple']['uintMinA()bool']
      returns: TestReturns['uintMinA()bool']
    }>
    & Record<'uintMinB()bool' | 'uintMinB', {
      argsObj: TestArgs['obj']['uintMinB()bool']
      argsTuple: TestArgs['tuple']['uintMinB()bool']
      returns: TestReturns['uintMinB()bool']
    }>
    & Record<'uintMinC()bool' | 'uintMinC', {
      argsObj: TestArgs['obj']['uintMinC()bool']
      argsTuple: TestArgs['tuple']['uintMinC()bool']
      returns: TestReturns['uintMinC()bool']
    }>
    & Record<'uintMinD()bool' | 'uintMinD', {
      argsObj: TestArgs['obj']['uintMinD()bool']
      argsTuple: TestArgs['tuple']['uintMinD()bool']
      returns: TestReturns['uintMinD()bool']
    }>
    & Record<'uintMaxA()bool' | 'uintMaxA', {
      argsObj: TestArgs['obj']['uintMaxA()bool']
      argsTuple: TestArgs['tuple']['uintMaxA()bool']
      returns: TestReturns['uintMaxA()bool']
    }>
    & Record<'uintMaxB()bool' | 'uintMaxB', {
      argsObj: TestArgs['obj']['uintMaxB()bool']
      argsTuple: TestArgs['tuple']['uintMaxB()bool']
      returns: TestReturns['uintMaxB()bool']
    }>
    & Record<'uintMaxC()bool' | 'uintMaxC', {
      argsObj: TestArgs['obj']['uintMaxC()bool']
      argsTuple: TestArgs['tuple']['uintMaxC()bool']
      returns: TestReturns['uintMaxC()bool']
    }>
    & Record<'uintMaxD()bool' | 'uintMaxD', {
      argsObj: TestArgs['obj']['uintMaxD()bool']
      argsTuple: TestArgs['tuple']['uintMaxD()bool']
      returns: TestReturns['uintMaxD()bool']
    }>
}

/**
 * Defines the possible abi call signatures.
 */
export type TestSignatures = keyof TestTypes['methods']
/**
 * Defines the possible abi call signatures for methods that return a non-void value.
 */
export type TestNonVoidMethodSignatures = keyof TestTypes['methods'] extends infer T ? T extends keyof TestTypes['methods'] ? MethodReturn<T> extends void ? never : T  : never : never
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the Test smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends TestSignatures> = TestTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the Test smart contract to the method's return type
 */
export type MethodReturn<TSignature extends TestSignatures> = TestTypes['methods'][TSignature]['returns']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the Test smart contract
 */
export abstract class TestParamsFactory {
  /**
   * Constructs a no op call for the uintMinA()bool ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static uintMinA(params: CallParams<TestArgs['obj']['uintMinA()bool'] | TestArgs['tuple']['uintMinA()bool']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'uintMinA()bool' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the uintMinB()bool ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static uintMinB(params: CallParams<TestArgs['obj']['uintMinB()bool'] | TestArgs['tuple']['uintMinB()bool']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'uintMinB()bool' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the uintMinC()bool ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static uintMinC(params: CallParams<TestArgs['obj']['uintMinC()bool'] | TestArgs['tuple']['uintMinC()bool']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'uintMinC()bool' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the uintMinD()bool ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static uintMinD(params: CallParams<TestArgs['obj']['uintMinD()bool'] | TestArgs['tuple']['uintMinD()bool']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'uintMinD()bool' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the uintMaxA()bool ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static uintMaxA(params: CallParams<TestArgs['obj']['uintMaxA()bool'] | TestArgs['tuple']['uintMaxA()bool']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'uintMaxA()bool' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the uintMaxB()bool ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static uintMaxB(params: CallParams<TestArgs['obj']['uintMaxB()bool'] | TestArgs['tuple']['uintMaxB()bool']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'uintMaxB()bool' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the uintMaxC()bool ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static uintMaxC(params: CallParams<TestArgs['obj']['uintMaxC()bool'] | TestArgs['tuple']['uintMaxC()bool']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'uintMaxC()bool' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the uintMaxD()bool ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static uintMaxD(params: CallParams<TestArgs['obj']['uintMaxD()bool'] | TestArgs['tuple']['uintMaxD()bool']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'uintMaxD()bool' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
}

/**
 * A client to make calls to the test smart contract
 */
export class TestClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `TestClient`
   *
   * @param appClient An `AppClient` instance which has been created with the Test app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `TestClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Checks for decode errors on the given return value and maps the return value to the return type for the given method
   * @returns The typed return value or undefined if there was no value
   */
  decodeReturnValue<TSignature extends TestNonVoidMethodSignatures>(method: TSignature, returnValue: ABIReturn | undefined) {
    return returnValue !== undefined ? getArc56ReturnValue<MethodReturn<TSignature>>(returnValue, this.appClient.getABIMethod(method), APP_SPEC.structs) : undefined
  }

  /**
   * Returns a new `TestClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<TestClient> {
    return new TestClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `TestClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<TestClient> {
    return new TestClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Makes a clear_state call to an existing instance of the test smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the test smart contract using the `uintMinA()bool` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    uintMinA: (params: CallParams<TestArgs['obj']['uintMinA()bool'] | TestArgs['tuple']['uintMinA()bool']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(TestParamsFactory.uintMinA(params))
    },

    /**
     * Makes a call to the test smart contract using the `uintMinB()bool` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    uintMinB: (params: CallParams<TestArgs['obj']['uintMinB()bool'] | TestArgs['tuple']['uintMinB()bool']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(TestParamsFactory.uintMinB(params))
    },

    /**
     * Makes a call to the test smart contract using the `uintMinC()bool` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    uintMinC: (params: CallParams<TestArgs['obj']['uintMinC()bool'] | TestArgs['tuple']['uintMinC()bool']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(TestParamsFactory.uintMinC(params))
    },

    /**
     * Makes a call to the test smart contract using the `uintMinD()bool` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    uintMinD: (params: CallParams<TestArgs['obj']['uintMinD()bool'] | TestArgs['tuple']['uintMinD()bool']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(TestParamsFactory.uintMinD(params))
    },

    /**
     * Makes a call to the test smart contract using the `uintMaxA()bool` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    uintMaxA: (params: CallParams<TestArgs['obj']['uintMaxA()bool'] | TestArgs['tuple']['uintMaxA()bool']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(TestParamsFactory.uintMaxA(params))
    },

    /**
     * Makes a call to the test smart contract using the `uintMaxB()bool` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    uintMaxB: (params: CallParams<TestArgs['obj']['uintMaxB()bool'] | TestArgs['tuple']['uintMaxB()bool']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(TestParamsFactory.uintMaxB(params))
    },

    /**
     * Makes a call to the test smart contract using the `uintMaxC()bool` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    uintMaxC: (params: CallParams<TestArgs['obj']['uintMaxC()bool'] | TestArgs['tuple']['uintMaxC()bool']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(TestParamsFactory.uintMaxC(params))
    },

    /**
     * Makes a call to the test smart contract using the `uintMaxD()bool` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    uintMaxD: (params: CallParams<TestArgs['obj']['uintMaxD()bool'] | TestArgs['tuple']['uintMaxD()bool']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(TestParamsFactory.uintMaxD(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Makes a clear_state call to an existing instance of the test smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the test smart contract using the `uintMinA()bool` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    uintMinA: (params: CallParams<TestArgs['obj']['uintMinA()bool'] | TestArgs['tuple']['uintMinA()bool']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(TestParamsFactory.uintMinA(params))
    },

    /**
     * Makes a call to the test smart contract using the `uintMinB()bool` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    uintMinB: (params: CallParams<TestArgs['obj']['uintMinB()bool'] | TestArgs['tuple']['uintMinB()bool']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(TestParamsFactory.uintMinB(params))
    },

    /**
     * Makes a call to the test smart contract using the `uintMinC()bool` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    uintMinC: (params: CallParams<TestArgs['obj']['uintMinC()bool'] | TestArgs['tuple']['uintMinC()bool']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(TestParamsFactory.uintMinC(params))
    },

    /**
     * Makes a call to the test smart contract using the `uintMinD()bool` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    uintMinD: (params: CallParams<TestArgs['obj']['uintMinD()bool'] | TestArgs['tuple']['uintMinD()bool']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(TestParamsFactory.uintMinD(params))
    },

    /**
     * Makes a call to the test smart contract using the `uintMaxA()bool` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    uintMaxA: (params: CallParams<TestArgs['obj']['uintMaxA()bool'] | TestArgs['tuple']['uintMaxA()bool']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(TestParamsFactory.uintMaxA(params))
    },

    /**
     * Makes a call to the test smart contract using the `uintMaxB()bool` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    uintMaxB: (params: CallParams<TestArgs['obj']['uintMaxB()bool'] | TestArgs['tuple']['uintMaxB()bool']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(TestParamsFactory.uintMaxB(params))
    },

    /**
     * Makes a call to the test smart contract using the `uintMaxC()bool` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    uintMaxC: (params: CallParams<TestArgs['obj']['uintMaxC()bool'] | TestArgs['tuple']['uintMaxC()bool']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(TestParamsFactory.uintMaxC(params))
    },

    /**
     * Makes a call to the test smart contract using the `uintMaxD()bool` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    uintMaxD: (params: CallParams<TestArgs['obj']['uintMaxD()bool'] | TestArgs['tuple']['uintMaxD()bool']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(TestParamsFactory.uintMaxD(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Makes a clear_state call to an existing instance of the test smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the test smart contract using the `uintMinA()bool` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    uintMinA: async (params: CallParams<TestArgs['obj']['uintMinA()bool'] | TestArgs['tuple']['uintMinA()bool']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(TestParamsFactory.uintMinA(params))
      return {...result, return: result.return as unknown as (undefined | TestReturns['uintMinA()bool'])}
    },

    /**
     * Makes a call to the test smart contract using the `uintMinB()bool` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    uintMinB: async (params: CallParams<TestArgs['obj']['uintMinB()bool'] | TestArgs['tuple']['uintMinB()bool']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(TestParamsFactory.uintMinB(params))
      return {...result, return: result.return as unknown as (undefined | TestReturns['uintMinB()bool'])}
    },

    /**
     * Makes a call to the test smart contract using the `uintMinC()bool` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    uintMinC: async (params: CallParams<TestArgs['obj']['uintMinC()bool'] | TestArgs['tuple']['uintMinC()bool']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(TestParamsFactory.uintMinC(params))
      return {...result, return: result.return as unknown as (undefined | TestReturns['uintMinC()bool'])}
    },

    /**
     * Makes a call to the test smart contract using the `uintMinD()bool` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    uintMinD: async (params: CallParams<TestArgs['obj']['uintMinD()bool'] | TestArgs['tuple']['uintMinD()bool']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(TestParamsFactory.uintMinD(params))
      return {...result, return: result.return as unknown as (undefined | TestReturns['uintMinD()bool'])}
    },

    /**
     * Makes a call to the test smart contract using the `uintMaxA()bool` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    uintMaxA: async (params: CallParams<TestArgs['obj']['uintMaxA()bool'] | TestArgs['tuple']['uintMaxA()bool']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(TestParamsFactory.uintMaxA(params))
      return {...result, return: result.return as unknown as (undefined | TestReturns['uintMaxA()bool'])}
    },

    /**
     * Makes a call to the test smart contract using the `uintMaxB()bool` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    uintMaxB: async (params: CallParams<TestArgs['obj']['uintMaxB()bool'] | TestArgs['tuple']['uintMaxB()bool']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(TestParamsFactory.uintMaxB(params))
      return {...result, return: result.return as unknown as (undefined | TestReturns['uintMaxB()bool'])}
    },

    /**
     * Makes a call to the test smart contract using the `uintMaxC()bool` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    uintMaxC: async (params: CallParams<TestArgs['obj']['uintMaxC()bool'] | TestArgs['tuple']['uintMaxC()bool']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(TestParamsFactory.uintMaxC(params))
      return {...result, return: result.return as unknown as (undefined | TestReturns['uintMaxC()bool'])}
    },

    /**
     * Makes a call to the test smart contract using the `uintMaxD()bool` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    uintMaxD: async (params: CallParams<TestArgs['obj']['uintMaxD()bool'] | TestArgs['tuple']['uintMaxD()bool']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(TestParamsFactory.uintMaxD(params))
      return {...result, return: result.return as unknown as (undefined | TestReturns['uintMaxD()bool'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new TestClient(this.appClient.clone(params))
  }

  /**
   * Makes a readonly (simulated) call to the test smart contract using the `uintMinA()bool` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async uintMinA(params: CallParams<TestArgs['obj']['uintMinA()bool'] | TestArgs['tuple']['uintMinA()bool']> = {args: []}) {
    const result = await this.appClient.send.call(TestParamsFactory.uintMinA(params))
    return result.return as unknown as TestReturns['uintMinA()bool']
  }

  /**
   * Makes a readonly (simulated) call to the test smart contract using the `uintMinB()bool` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async uintMinB(params: CallParams<TestArgs['obj']['uintMinB()bool'] | TestArgs['tuple']['uintMinB()bool']> = {args: []}) {
    const result = await this.appClient.send.call(TestParamsFactory.uintMinB(params))
    return result.return as unknown as TestReturns['uintMinB()bool']
  }

  /**
   * Makes a readonly (simulated) call to the test smart contract using the `uintMinC()bool` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async uintMinC(params: CallParams<TestArgs['obj']['uintMinC()bool'] | TestArgs['tuple']['uintMinC()bool']> = {args: []}) {
    const result = await this.appClient.send.call(TestParamsFactory.uintMinC(params))
    return result.return as unknown as TestReturns['uintMinC()bool']
  }

  /**
   * Makes a readonly (simulated) call to the test smart contract using the `uintMinD()bool` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async uintMinD(params: CallParams<TestArgs['obj']['uintMinD()bool'] | TestArgs['tuple']['uintMinD()bool']> = {args: []}) {
    const result = await this.appClient.send.call(TestParamsFactory.uintMinD(params))
    return result.return as unknown as TestReturns['uintMinD()bool']
  }

  /**
   * Makes a readonly (simulated) call to the test smart contract using the `uintMaxA()bool` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async uintMaxA(params: CallParams<TestArgs['obj']['uintMaxA()bool'] | TestArgs['tuple']['uintMaxA()bool']> = {args: []}) {
    const result = await this.appClient.send.call(TestParamsFactory.uintMaxA(params))
    return result.return as unknown as TestReturns['uintMaxA()bool']
  }

  /**
   * Makes a readonly (simulated) call to the test smart contract using the `uintMaxB()bool` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async uintMaxB(params: CallParams<TestArgs['obj']['uintMaxB()bool'] | TestArgs['tuple']['uintMaxB()bool']> = {args: []}) {
    const result = await this.appClient.send.call(TestParamsFactory.uintMaxB(params))
    return result.return as unknown as TestReturns['uintMaxB()bool']
  }

  /**
   * Makes a readonly (simulated) call to the test smart contract using the `uintMaxC()bool` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async uintMaxC(params: CallParams<TestArgs['obj']['uintMaxC()bool'] | TestArgs['tuple']['uintMaxC()bool']> = {args: []}) {
    const result = await this.appClient.send.call(TestParamsFactory.uintMaxC(params))
    return result.return as unknown as TestReturns['uintMaxC()bool']
  }

  /**
   * Makes a readonly (simulated) call to the test smart contract using the `uintMaxD()bool` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async uintMaxD(params: CallParams<TestArgs['obj']['uintMaxD()bool'] | TestArgs['tuple']['uintMaxD()bool']> = {args: []}) {
    const result = await this.appClient.send.call(TestParamsFactory.uintMaxD(params))
    return result.return as unknown as TestReturns['uintMaxD()bool']
  }

  /**
   * Methods to access state for the current test app
   */
  state = {
  }

  public newGroup(): TestComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a uintMinA()bool method call against the test contract
       */
      uintMinA(params: CallParams<TestArgs['obj']['uintMinA()bool'] | TestArgs['tuple']['uintMinA()bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.uintMinA(params)))
        resultMappers.push((v) => client.decodeReturnValue('uintMinA()bool', v))
        return this
      },
      /**
       * Add a uintMinB()bool method call against the test contract
       */
      uintMinB(params: CallParams<TestArgs['obj']['uintMinB()bool'] | TestArgs['tuple']['uintMinB()bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.uintMinB(params)))
        resultMappers.push((v) => client.decodeReturnValue('uintMinB()bool', v))
        return this
      },
      /**
       * Add a uintMinC()bool method call against the test contract
       */
      uintMinC(params: CallParams<TestArgs['obj']['uintMinC()bool'] | TestArgs['tuple']['uintMinC()bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.uintMinC(params)))
        resultMappers.push((v) => client.decodeReturnValue('uintMinC()bool', v))
        return this
      },
      /**
       * Add a uintMinD()bool method call against the test contract
       */
      uintMinD(params: CallParams<TestArgs['obj']['uintMinD()bool'] | TestArgs['tuple']['uintMinD()bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.uintMinD(params)))
        resultMappers.push((v) => client.decodeReturnValue('uintMinD()bool', v))
        return this
      },
      /**
       * Add a uintMaxA()bool method call against the test contract
       */
      uintMaxA(params: CallParams<TestArgs['obj']['uintMaxA()bool'] | TestArgs['tuple']['uintMaxA()bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.uintMaxA(params)))
        resultMappers.push((v) => client.decodeReturnValue('uintMaxA()bool', v))
        return this
      },
      /**
       * Add a uintMaxB()bool method call against the test contract
       */
      uintMaxB(params: CallParams<TestArgs['obj']['uintMaxB()bool'] | TestArgs['tuple']['uintMaxB()bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.uintMaxB(params)))
        resultMappers.push((v) => client.decodeReturnValue('uintMaxB()bool', v))
        return this
      },
      /**
       * Add a uintMaxC()bool method call against the test contract
       */
      uintMaxC(params: CallParams<TestArgs['obj']['uintMaxC()bool'] | TestArgs['tuple']['uintMaxC()bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.uintMaxC(params)))
        resultMappers.push((v) => client.decodeReturnValue('uintMaxC()bool', v))
        return this
      },
      /**
       * Add a uintMaxD()bool method call against the test contract
       */
      uintMaxD(params: CallParams<TestArgs['obj']['uintMaxD()bool'] | TestArgs['tuple']['uintMaxD()bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.uintMaxD(params)))
        resultMappers.push((v) => client.decodeReturnValue('uintMaxD()bool', v))
        return this
      },
      /**
       * Add a clear state call to the test contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as TestComposer
  }
}
export type TestComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the uintMinA()bool ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  uintMinA(params?: CallParams<TestArgs['obj']['uintMinA()bool'] | TestArgs['tuple']['uintMinA()bool']>): TestComposer<[...TReturns, TestReturns['uintMinA()bool'] | undefined]>

  /**
   * Calls the uintMinB()bool ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  uintMinB(params?: CallParams<TestArgs['obj']['uintMinB()bool'] | TestArgs['tuple']['uintMinB()bool']>): TestComposer<[...TReturns, TestReturns['uintMinB()bool'] | undefined]>

  /**
   * Calls the uintMinC()bool ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  uintMinC(params?: CallParams<TestArgs['obj']['uintMinC()bool'] | TestArgs['tuple']['uintMinC()bool']>): TestComposer<[...TReturns, TestReturns['uintMinC()bool'] | undefined]>

  /**
   * Calls the uintMinD()bool ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  uintMinD(params?: CallParams<TestArgs['obj']['uintMinD()bool'] | TestArgs['tuple']['uintMinD()bool']>): TestComposer<[...TReturns, TestReturns['uintMinD()bool'] | undefined]>

  /**
   * Calls the uintMaxA()bool ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  uintMaxA(params?: CallParams<TestArgs['obj']['uintMaxA()bool'] | TestArgs['tuple']['uintMaxA()bool']>): TestComposer<[...TReturns, TestReturns['uintMaxA()bool'] | undefined]>

  /**
   * Calls the uintMaxB()bool ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  uintMaxB(params?: CallParams<TestArgs['obj']['uintMaxB()bool'] | TestArgs['tuple']['uintMaxB()bool']>): TestComposer<[...TReturns, TestReturns['uintMaxB()bool'] | undefined]>

  /**
   * Calls the uintMaxC()bool ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  uintMaxC(params?: CallParams<TestArgs['obj']['uintMaxC()bool'] | TestArgs['tuple']['uintMaxC()bool']>): TestComposer<[...TReturns, TestReturns['uintMaxC()bool'] | undefined]>

  /**
   * Calls the uintMaxD()bool ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  uintMaxD(params?: CallParams<TestArgs['obj']['uintMaxD()bool'] | TestArgs['tuple']['uintMaxD()bool']>): TestComposer<[...TReturns, TestReturns['uintMaxD()bool'] | undefined]>

  /**
   * Makes a clear_state call to an existing instance of the test smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): TestComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): TestComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<TestComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<TestComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<TestComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<TestComposerResults<TReturns>>
}
export type TestComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

