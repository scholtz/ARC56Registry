/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"ERC20Custom","structs":{},"methods":[{"name":"__postInit","args":[],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}},{"name":"totalSupply","args":[],"returns":{"type":"uint256"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"desc":" @dev See {IERC20-totalSupply}.","events":[],"recommendations":{}},{"name":"balanceOf","args":[{"type":"address","name":"account"}],"returns":{"type":"uint256"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"desc":" @dev See {IERC20-balanceOf}.","events":[],"recommendations":{}},{"name":"transfer","args":[{"type":"address","name":"recipient"},{"type":"uint256","name":"__arc4_amount"}],"returns":{"type":"bool"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":" @dev See {IERC20-transfer}.\n Requirements:\n - `recipient` cannot be the zero address.\n - the caller must have a balance of at least `amount`.","events":[{"name":"Transfer","args":[{"type":"uint8[32]","name":"from"},{"type":"uint8[32]","name":"to"},{"type":"uint256","name":"value"}]}],"recommendations":{}},{"name":"allowance","args":[{"type":"address","name":"owner"},{"type":"address","name":"spender"}],"returns":{"type":"uint256"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"desc":" @dev See {IERC20-allowance}.","events":[],"recommendations":{}},{"name":"approve","args":[{"type":"address","name":"spender"},{"type":"uint256","name":"__arc4_amount"}],"returns":{"type":"bool"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":" @dev See {IERC20-approve}.\n Requirements:\n - `spender` cannot be the zero address.approve(address spender, uint256 amount)","events":[{"name":"Approval","args":[{"type":"uint8[32]","name":"owner"},{"type":"uint8[32]","name":"spender"},{"type":"uint256","name":"value"}]}],"recommendations":{}},{"name":"transferFrom","args":[{"type":"address","name":"sender"},{"type":"address","name":"recipient"},{"type":"uint256","name":"__arc4_amount"}],"returns":{"type":"bool"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":" @dev See {IERC20-transferFrom}.\n Emits an {Approval} event indicating the updated allowance. This is not\n required by the EIP. See the note at the beginning of {ERC20};\n Requirements:\n - `sender` and `recipient` cannot be the zero address.\n - `sender` must have a balance of at least `amount`.\n - the caller must have allowance for `sender`'s tokens of at least\n `amount`.","events":[{"name":"Transfer","args":[{"type":"uint8[32]","name":"from"},{"type":"uint8[32]","name":"to"},{"type":"uint256","name":"value"}]},{"name":"Approval","args":[{"type":"uint8[32]","name":"owner"},{"type":"uint8[32]","name":"spender"},{"type":"uint256","name":"value"}]}],"recommendations":{}},{"name":"increaseAllowance","args":[{"type":"address","name":"spender"},{"type":"uint256","name":"__arc4_addedValue"}],"returns":{"type":"bool"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":" @dev Atomically increases the allowance granted to `spender` by the caller.\n This is an alternative to {approve} that can be used as a mitigation for\n problems described in {IERC20-approve}.\n Emits an {Approval} event indicating the updated allowance.\n Requirements:\n - `spender` cannot be the zero address.","events":[{"name":"Approval","args":[{"type":"uint8[32]","name":"owner"},{"type":"uint8[32]","name":"spender"},{"type":"uint256","name":"value"}]}],"recommendations":{}},{"name":"decreaseAllowance","args":[{"type":"address","name":"spender"},{"type":"uint256","name":"__arc4_subtractedValue"}],"returns":{"type":"bool"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":" @dev Atomically decreases the allowance granted to `spender` by the caller.\n This is an alternative to {approve} that can be used as a mitigation for\n problems described in {IERC20-approve}.\n Emits an {Approval} event indicating the updated allowance.\n Requirements:\n - `spender` cannot be the zero address.\n - `spender` must have allowance for the caller of at least\n `subtractedValue`.","events":[{"name":"Approval","args":[{"type":"uint8[32]","name":"owner"},{"type":"uint8[32]","name":"spender"},{"type":"uint256","name":"value"}]}],"recommendations":{}},{"name":"burn","args":[{"type":"uint256","name":"__arc4_amount"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":" @dev Destroys `amount` tokens from the caller.\n See {ERC20-_burn}.","events":[{"name":"Transfer","args":[{"type":"uint8[32]","name":"from"},{"type":"uint8[32]","name":"to"},{"type":"uint256","name":"value"}]}],"recommendations":{}},{"name":"burnFrom","args":[{"type":"address","name":"account"},{"type":"uint256","name":"__arc4_amount"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":" @dev Destroys `amount` tokens from `account`, deducting from the caller's\n allowance.\n See {ERC20-_burn} and {ERC20-allowance}.\n Requirements:\n - the caller must have allowance for `accounts`'s tokens of at least\n `amount`.","events":[{"name":"Approval","args":[{"type":"uint8[32]","name":"owner"},{"type":"uint8[32]","name":"spender"},{"type":"uint256","name":"value"}]},{"name":"Transfer","args":[{"type":"uint8[32]","name":"from"},{"type":"uint8[32]","name":"to"},{"type":"uint256","name":"value"}]}],"recommendations":{}}],"arcs":[22,28],"desc":" @dev Implementation of the {IERC20} interface.\n This implementation is agnostic to the way tokens are created. This means\n that a supply mechanism has to be added in a derived contract using {_mint}.\n For a generic mechanism see {ERC20Mintable}.\n TIP: For a detailed writeup see our guide\n https://forum.zeppelin.solutions/t/how-to-implement-erc20-supply-mechanisms/226[How\n to implement supply mechanisms].\n We have followed general OpenZeppelin guidelines: functions revert instead\n of returning `false` on failure. This behavior is nonetheless conventional\n and does not conflict with the expectations of ERC20 applications.\n Additionally, an {Approval} event is emitted on calls to {transferFrom}.\n This allows applications to reconstruct the allowance for all accounts just\n by listening to said events. Other implementations of the EIP may not emit\n these events, as it isn't required by the specification.\n Finally, the non-standard {decreaseAllowance} and {increaseAllowance}\n functions have been added to mitigate the well-known issues around setting\n allowances. See {IERC20-approve}.","networks":{},"state":{"schema":{"global":{"ints":1,"bytes":1},"local":{"ints":0,"bytes":0}},"keys":{"global":{"_totalSupply":{"keyType":"AVMString","valueType":"AVMBytes","key":"X3RvdGFsU3VwcGx5"},"__ctor_pending":{"keyType":"AVMString","valueType":"AVMUint64","key":"X19jdG9yX3BlbmRpbmc="}},"local":{},"box":{}},"maps":{"global":{},"local":{},"box":{"_balances":{"keyType":"AVMBytes","valueType":"AVMBytes","prefix":"X2JhbGFuY2Vz"},"_allowances":{"keyType":"AVMBytes","valueType":"AVMBytes","prefix":"X2FsbG93YW5jZXM="}}}},"bareActions":{"create":[],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[1312],"errorMessage":"ERC20: approve from the zero address"},{"pc":[1317],"errorMessage":"ERC20: approve to the zero address"},{"pc":[1209],"errorMessage":"ERC20: burn from the zero address"},{"pc":[1088],"errorMessage":"ERC20: transfer from the zero address"},{"pc":[1093],"errorMessage":"ERC20: transfer to the zero address"},{"pc":[307],"errorMessage":"SafeMath: addition overflow"},{"pc":[382],"errorMessage":"__postInit already called"},{"pc":[360],"errorMessage":"assertion failed"},{"pc":[418,1272],"errorMessage":"check _totalSupply exists"},{"pc":[445,495,542,550,568,615,623,740,804,941],"errorMessage":"invalid number of bytes for arc4.static_array<arc4.uint8, 32>"},{"pc":[503,576,631,748,812,907,949],"errorMessage":"invalid number of bytes for arc4.uint256"},{"pc":[413,458,516,589,644,761,825,920,962,1053],"errorMessage":"not payable"},{"pc":[423,473,1072,1183,1287,1339],"errorMessage":"overflow"},{"pc":[366],"errorMessage":"underflow"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"events":[{"name":"Transfer","args":[{"type":"uint8[32]","name":"from"},{"type":"uint8[32]","name":"to"},{"type":"uint256","name":"value"}]},{"name":"Approval","args":[{"type":"uint8[32]","name":"owner"},{"type":"uint8[32]","name":"spender"},{"type":"uint256","name":"value"}]}]} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


/**
 * The argument types for the Erc20Custom contract
 */
export type Erc20CustomArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    '__postInit()void': Record<string, never>
    'totalSupply()uint256': Record<string, never>
    'balanceOf(address)uint256': {
      account: string
    }
    'transfer(address,uint256)bool': {
      recipient: string
      __arc4Amount: bigint | number
    }
    'allowance(address,address)uint256': {
      owner: string
      spender: string
    }
    'approve(address,uint256)bool': {
      spender: string
      __arc4Amount: bigint | number
    }
    'transferFrom(address,address,uint256)bool': {
      sender: string
      recipient: string
      __arc4Amount: bigint | number
    }
    'increaseAllowance(address,uint256)bool': {
      spender: string
      __arc4AddedValue: bigint | number
    }
    'decreaseAllowance(address,uint256)bool': {
      spender: string
      __arc4SubtractedValue: bigint | number
    }
    'burn(uint256)void': {
      __arc4Amount: bigint | number
    }
    'burnFrom(address,uint256)void': {
      account: string
      __arc4Amount: bigint | number
    }
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    '__postInit()void': []
    'totalSupply()uint256': []
    'balanceOf(address)uint256': [account: string]
    'transfer(address,uint256)bool': [recipient: string, __arc4Amount: bigint | number]
    'allowance(address,address)uint256': [owner: string, spender: string]
    'approve(address,uint256)bool': [spender: string, __arc4Amount: bigint | number]
    'transferFrom(address,address,uint256)bool': [sender: string, recipient: string, __arc4Amount: bigint | number]
    'increaseAllowance(address,uint256)bool': [spender: string, __arc4AddedValue: bigint | number]
    'decreaseAllowance(address,uint256)bool': [spender: string, __arc4SubtractedValue: bigint | number]
    'burn(uint256)void': [__arc4Amount: bigint | number]
    'burnFrom(address,uint256)void': [account: string, __arc4Amount: bigint | number]
  }
}

/**
 * The return type for each method
 */
export type Erc20CustomReturns = {
  '__postInit()void': void
  'totalSupply()uint256': bigint
  'balanceOf(address)uint256': bigint
  'transfer(address,uint256)bool': boolean
  'allowance(address,address)uint256': bigint
  'approve(address,uint256)bool': boolean
  'transferFrom(address,address,uint256)bool': boolean
  'increaseAllowance(address,uint256)bool': boolean
  'decreaseAllowance(address,uint256)bool': boolean
  'burn(uint256)void': void
  'burnFrom(address,uint256)void': void
}

/**
 * Defines the types of available calls and state of the Erc20Custom smart contract.
 */
export type Erc20CustomTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'__postInit()void' | '__postInit', {
      argsObj: Erc20CustomArgs['obj']['__postInit()void']
      argsTuple: Erc20CustomArgs['tuple']['__postInit()void']
      returns: Erc20CustomReturns['__postInit()void']
    }>
    & Record<'totalSupply()uint256' | 'totalSupply', {
      argsObj: Erc20CustomArgs['obj']['totalSupply()uint256']
      argsTuple: Erc20CustomArgs['tuple']['totalSupply()uint256']
      returns: Erc20CustomReturns['totalSupply()uint256']
    }>
    & Record<'balanceOf(address)uint256' | 'balanceOf', {
      argsObj: Erc20CustomArgs['obj']['balanceOf(address)uint256']
      argsTuple: Erc20CustomArgs['tuple']['balanceOf(address)uint256']
      returns: Erc20CustomReturns['balanceOf(address)uint256']
    }>
    & Record<'transfer(address,uint256)bool' | 'transfer', {
      argsObj: Erc20CustomArgs['obj']['transfer(address,uint256)bool']
      argsTuple: Erc20CustomArgs['tuple']['transfer(address,uint256)bool']
      returns: Erc20CustomReturns['transfer(address,uint256)bool']
    }>
    & Record<'allowance(address,address)uint256' | 'allowance', {
      argsObj: Erc20CustomArgs['obj']['allowance(address,address)uint256']
      argsTuple: Erc20CustomArgs['tuple']['allowance(address,address)uint256']
      returns: Erc20CustomReturns['allowance(address,address)uint256']
    }>
    & Record<'approve(address,uint256)bool' | 'approve', {
      argsObj: Erc20CustomArgs['obj']['approve(address,uint256)bool']
      argsTuple: Erc20CustomArgs['tuple']['approve(address,uint256)bool']
      returns: Erc20CustomReturns['approve(address,uint256)bool']
    }>
    & Record<'transferFrom(address,address,uint256)bool' | 'transferFrom', {
      argsObj: Erc20CustomArgs['obj']['transferFrom(address,address,uint256)bool']
      argsTuple: Erc20CustomArgs['tuple']['transferFrom(address,address,uint256)bool']
      returns: Erc20CustomReturns['transferFrom(address,address,uint256)bool']
    }>
    & Record<'increaseAllowance(address,uint256)bool' | 'increaseAllowance', {
      argsObj: Erc20CustomArgs['obj']['increaseAllowance(address,uint256)bool']
      argsTuple: Erc20CustomArgs['tuple']['increaseAllowance(address,uint256)bool']
      returns: Erc20CustomReturns['increaseAllowance(address,uint256)bool']
    }>
    & Record<'decreaseAllowance(address,uint256)bool' | 'decreaseAllowance', {
      argsObj: Erc20CustomArgs['obj']['decreaseAllowance(address,uint256)bool']
      argsTuple: Erc20CustomArgs['tuple']['decreaseAllowance(address,uint256)bool']
      returns: Erc20CustomReturns['decreaseAllowance(address,uint256)bool']
    }>
    & Record<'burn(uint256)void' | 'burn', {
      argsObj: Erc20CustomArgs['obj']['burn(uint256)void']
      argsTuple: Erc20CustomArgs['tuple']['burn(uint256)void']
      returns: Erc20CustomReturns['burn(uint256)void']
    }>
    & Record<'burnFrom(address,uint256)void' | 'burnFrom', {
      argsObj: Erc20CustomArgs['obj']['burnFrom(address,uint256)void']
      argsTuple: Erc20CustomArgs['tuple']['burnFrom(address,uint256)void']
      returns: Erc20CustomReturns['burnFrom(address,uint256)void']
    }>
  /**
   * Defines the shape of the state of the application.
   */
  state: {
    global: {
      keys: {
        _totalSupply: BinaryState
        __ctorPending: bigint
      }
      maps: {}
    }
    box: {
      keys: {}
      maps: {
        _balances: Map<Uint8Array | string, Uint8Array>
        _allowances: Map<Uint8Array | string, Uint8Array>
      }
    }
  }
}

/**
 * Defines the possible abi call signatures.
 */
export type Erc20CustomSignatures = keyof Erc20CustomTypes['methods']
/**
 * Defines the possible abi call signatures for methods that return a non-void value.
 */
export type Erc20CustomNonVoidMethodSignatures = keyof Erc20CustomTypes['methods'] extends infer T ? T extends keyof Erc20CustomTypes['methods'] ? MethodReturn<T> extends void ? never : T  : never : never
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the Erc20Custom smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends Erc20CustomSignatures> = Erc20CustomTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the Erc20Custom smart contract to the method's return type
 */
export type MethodReturn<TSignature extends Erc20CustomSignatures> = Erc20CustomTypes['methods'][TSignature]['returns']

/**
 * Defines the shape of the keyed global state of the application.
 */
export type GlobalKeysState = Erc20CustomTypes['state']['global']['keys']

/**
 * Defines the shape of the keyed box state of the application.
 */
export type BoxKeysState = Erc20CustomTypes['state']['box']['keys']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the Erc20Custom smart contract
 */
export abstract class Erc20CustomParamsFactory {
  /**
   * Constructs a no op call for the __postInit()void ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static __postInit(params: CallParams<Erc20CustomArgs['obj']['__postInit()void'] | Erc20CustomArgs['tuple']['__postInit()void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: '__postInit()void' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the totalSupply()uint256 ABI method
   *
   *  @dev See {IERC20-totalSupply}.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static totalSupply(params: CallParams<Erc20CustomArgs['obj']['totalSupply()uint256'] | Erc20CustomArgs['tuple']['totalSupply()uint256']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'totalSupply()uint256' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the balanceOf(address)uint256 ABI method
   *
   *  @dev See {IERC20-balanceOf}.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static balanceOf(params: CallParams<Erc20CustomArgs['obj']['balanceOf(address)uint256'] | Erc20CustomArgs['tuple']['balanceOf(address)uint256']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'balanceOf(address)uint256' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.account],
    }
  }
  /**
   * Constructs a no op call for the transfer(address,uint256)bool ABI method
   *
  *  @dev See {IERC20-transfer}.
  Requirements:
  - `recipient` cannot be the zero address.
  - the caller must have a balance of at least `amount`.

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static transfer(params: CallParams<Erc20CustomArgs['obj']['transfer(address,uint256)bool'] | Erc20CustomArgs['tuple']['transfer(address,uint256)bool']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'transfer(address,uint256)bool' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.recipient, params.args.__arc4Amount],
    }
  }
  /**
   * Constructs a no op call for the allowance(address,address)uint256 ABI method
   *
   *  @dev See {IERC20-allowance}.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static allowance(params: CallParams<Erc20CustomArgs['obj']['allowance(address,address)uint256'] | Erc20CustomArgs['tuple']['allowance(address,address)uint256']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'allowance(address,address)uint256' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.owner, params.args.spender],
    }
  }
  /**
   * Constructs a no op call for the approve(address,uint256)bool ABI method
   *
  *  @dev See {IERC20-approve}.
  Requirements:
  - `spender` cannot be the zero address.approve(address spender, uint256 amount)

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static approve(params: CallParams<Erc20CustomArgs['obj']['approve(address,uint256)bool'] | Erc20CustomArgs['tuple']['approve(address,uint256)bool']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'approve(address,uint256)bool' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.spender, params.args.__arc4Amount],
    }
  }
  /**
   * Constructs a no op call for the transferFrom(address,address,uint256)bool ABI method
   *
  *  @dev See {IERC20-transferFrom}.
  Emits an {Approval} event indicating the updated allowance. This is not
  required by the EIP. See the note at the beginning of {ERC20};
  Requirements:
  - `sender` and `recipient` cannot be the zero address.
  - `sender` must have a balance of at least `amount`.
  - the caller must have allowance for `sender`'s tokens of at least
  `amount`.

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static transferFrom(params: CallParams<Erc20CustomArgs['obj']['transferFrom(address,address,uint256)bool'] | Erc20CustomArgs['tuple']['transferFrom(address,address,uint256)bool']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'transferFrom(address,address,uint256)bool' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.sender, params.args.recipient, params.args.__arc4Amount],
    }
  }
  /**
   * Constructs a no op call for the increaseAllowance(address,uint256)bool ABI method
   *
  *  @dev Atomically increases the allowance granted to `spender` by the caller.
  This is an alternative to {approve} that can be used as a mitigation for
  problems described in {IERC20-approve}.
  Emits an {Approval} event indicating the updated allowance.
  Requirements:
  - `spender` cannot be the zero address.

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static increaseAllowance(params: CallParams<Erc20CustomArgs['obj']['increaseAllowance(address,uint256)bool'] | Erc20CustomArgs['tuple']['increaseAllowance(address,uint256)bool']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'increaseAllowance(address,uint256)bool' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.spender, params.args.__arc4AddedValue],
    }
  }
  /**
   * Constructs a no op call for the decreaseAllowance(address,uint256)bool ABI method
   *
  *  @dev Atomically decreases the allowance granted to `spender` by the caller.
  This is an alternative to {approve} that can be used as a mitigation for
  problems described in {IERC20-approve}.
  Emits an {Approval} event indicating the updated allowance.
  Requirements:
  - `spender` cannot be the zero address.
  - `spender` must have allowance for the caller of at least
  `subtractedValue`.

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static decreaseAllowance(params: CallParams<Erc20CustomArgs['obj']['decreaseAllowance(address,uint256)bool'] | Erc20CustomArgs['tuple']['decreaseAllowance(address,uint256)bool']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'decreaseAllowance(address,uint256)bool' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.spender, params.args.__arc4SubtractedValue],
    }
  }
  /**
   * Constructs a no op call for the burn(uint256)void ABI method
   *
  *  @dev Destroys `amount` tokens from the caller.
  See {ERC20-_burn}.

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static burn(params: CallParams<Erc20CustomArgs['obj']['burn(uint256)void'] | Erc20CustomArgs['tuple']['burn(uint256)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'burn(uint256)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.__arc4Amount],
    }
  }
  /**
   * Constructs a no op call for the burnFrom(address,uint256)void ABI method
   *
  *  @dev Destroys `amount` tokens from `account`, deducting from the caller's
  allowance.
  See {ERC20-_burn} and {ERC20-allowance}.
  Requirements:
  - the caller must have allowance for `accounts`'s tokens of at least
  `amount`.

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static burnFrom(params: CallParams<Erc20CustomArgs['obj']['burnFrom(address,uint256)void'] | Erc20CustomArgs['tuple']['burnFrom(address,uint256)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'burnFrom(address,uint256)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.account, params.args.__arc4Amount],
    }
  }
}

/**
 * A client to make calls to the ERC20Custom smart contract
 */
export class Erc20CustomClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `Erc20CustomClient`
   *
   * @param appClient An `AppClient` instance which has been created with the Erc20Custom app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `Erc20CustomClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Checks for decode errors on the given return value and maps the return value to the return type for the given method
   * @returns The typed return value or undefined if there was no value
   */
  decodeReturnValue<TSignature extends Erc20CustomNonVoidMethodSignatures>(method: TSignature, returnValue: ABIReturn | undefined) {
    return returnValue !== undefined ? getArc56ReturnValue<MethodReturn<TSignature>>(returnValue, this.appClient.getABIMethod(method), APP_SPEC.structs) : undefined
  }

  /**
   * Returns a new `Erc20CustomClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<Erc20CustomClient> {
    return new Erc20CustomClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `Erc20CustomClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<Erc20CustomClient> {
    return new Erc20CustomClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Makes a clear_state call to an existing instance of the ERC20Custom smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the ERC20Custom smart contract using the `__postInit()void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    __postInit: (params: CallParams<Erc20CustomArgs['obj']['__postInit()void'] | Erc20CustomArgs['tuple']['__postInit()void']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(Erc20CustomParamsFactory.__postInit(params))
    },

    /**
     * Makes a call to the ERC20Custom smart contract using the `totalSupply()uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     *  @dev See {IERC20-totalSupply}.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    totalSupply: (params: CallParams<Erc20CustomArgs['obj']['totalSupply()uint256'] | Erc20CustomArgs['tuple']['totalSupply()uint256']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(Erc20CustomParamsFactory.totalSupply(params))
    },

    /**
     * Makes a call to the ERC20Custom smart contract using the `balanceOf(address)uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     *  @dev See {IERC20-balanceOf}.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    balanceOf: (params: CallParams<Erc20CustomArgs['obj']['balanceOf(address)uint256'] | Erc20CustomArgs['tuple']['balanceOf(address)uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(Erc20CustomParamsFactory.balanceOf(params))
    },

    /**
     * Makes a call to the ERC20Custom smart contract using the `transfer(address,uint256)bool` ABI method.
     *
    *  @dev See {IERC20-transfer}.
    Requirements:
    - `recipient` cannot be the zero address.
    - the caller must have a balance of at least `amount`.

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    transfer: (params: CallParams<Erc20CustomArgs['obj']['transfer(address,uint256)bool'] | Erc20CustomArgs['tuple']['transfer(address,uint256)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(Erc20CustomParamsFactory.transfer(params))
    },

    /**
     * Makes a call to the ERC20Custom smart contract using the `allowance(address,address)uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     *  @dev See {IERC20-allowance}.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    allowance: (params: CallParams<Erc20CustomArgs['obj']['allowance(address,address)uint256'] | Erc20CustomArgs['tuple']['allowance(address,address)uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(Erc20CustomParamsFactory.allowance(params))
    },

    /**
     * Makes a call to the ERC20Custom smart contract using the `approve(address,uint256)bool` ABI method.
     *
    *  @dev See {IERC20-approve}.
    Requirements:
    - `spender` cannot be the zero address.approve(address spender, uint256 amount)

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    approve: (params: CallParams<Erc20CustomArgs['obj']['approve(address,uint256)bool'] | Erc20CustomArgs['tuple']['approve(address,uint256)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(Erc20CustomParamsFactory.approve(params))
    },

    /**
     * Makes a call to the ERC20Custom smart contract using the `transferFrom(address,address,uint256)bool` ABI method.
     *
    *  @dev See {IERC20-transferFrom}.
    Emits an {Approval} event indicating the updated allowance. This is not
    required by the EIP. See the note at the beginning of {ERC20};
    Requirements:
    - `sender` and `recipient` cannot be the zero address.
    - `sender` must have a balance of at least `amount`.
    - the caller must have allowance for `sender`'s tokens of at least
    `amount`.

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    transferFrom: (params: CallParams<Erc20CustomArgs['obj']['transferFrom(address,address,uint256)bool'] | Erc20CustomArgs['tuple']['transferFrom(address,address,uint256)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(Erc20CustomParamsFactory.transferFrom(params))
    },

    /**
     * Makes a call to the ERC20Custom smart contract using the `increaseAllowance(address,uint256)bool` ABI method.
     *
    *  @dev Atomically increases the allowance granted to `spender` by the caller.
    This is an alternative to {approve} that can be used as a mitigation for
    problems described in {IERC20-approve}.
    Emits an {Approval} event indicating the updated allowance.
    Requirements:
    - `spender` cannot be the zero address.

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    increaseAllowance: (params: CallParams<Erc20CustomArgs['obj']['increaseAllowance(address,uint256)bool'] | Erc20CustomArgs['tuple']['increaseAllowance(address,uint256)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(Erc20CustomParamsFactory.increaseAllowance(params))
    },

    /**
     * Makes a call to the ERC20Custom smart contract using the `decreaseAllowance(address,uint256)bool` ABI method.
     *
    *  @dev Atomically decreases the allowance granted to `spender` by the caller.
    This is an alternative to {approve} that can be used as a mitigation for
    problems described in {IERC20-approve}.
    Emits an {Approval} event indicating the updated allowance.
    Requirements:
    - `spender` cannot be the zero address.
    - `spender` must have allowance for the caller of at least
    `subtractedValue`.

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    decreaseAllowance: (params: CallParams<Erc20CustomArgs['obj']['decreaseAllowance(address,uint256)bool'] | Erc20CustomArgs['tuple']['decreaseAllowance(address,uint256)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(Erc20CustomParamsFactory.decreaseAllowance(params))
    },

    /**
     * Makes a call to the ERC20Custom smart contract using the `burn(uint256)void` ABI method.
     *
    *  @dev Destroys `amount` tokens from the caller.
    See {ERC20-_burn}.

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    burn: (params: CallParams<Erc20CustomArgs['obj']['burn(uint256)void'] | Erc20CustomArgs['tuple']['burn(uint256)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(Erc20CustomParamsFactory.burn(params))
    },

    /**
     * Makes a call to the ERC20Custom smart contract using the `burnFrom(address,uint256)void` ABI method.
     *
    *  @dev Destroys `amount` tokens from `account`, deducting from the caller's
    allowance.
    See {ERC20-_burn} and {ERC20-allowance}.
    Requirements:
    - the caller must have allowance for `accounts`'s tokens of at least
    `amount`.

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    burnFrom: (params: CallParams<Erc20CustomArgs['obj']['burnFrom(address,uint256)void'] | Erc20CustomArgs['tuple']['burnFrom(address,uint256)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(Erc20CustomParamsFactory.burnFrom(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Makes a clear_state call to an existing instance of the ERC20Custom smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the ERC20Custom smart contract using the `__postInit()void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    __postInit: (params: CallParams<Erc20CustomArgs['obj']['__postInit()void'] | Erc20CustomArgs['tuple']['__postInit()void']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(Erc20CustomParamsFactory.__postInit(params))
    },

    /**
     * Makes a call to the ERC20Custom smart contract using the `totalSupply()uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     *  @dev See {IERC20-totalSupply}.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    totalSupply: (params: CallParams<Erc20CustomArgs['obj']['totalSupply()uint256'] | Erc20CustomArgs['tuple']['totalSupply()uint256']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(Erc20CustomParamsFactory.totalSupply(params))
    },

    /**
     * Makes a call to the ERC20Custom smart contract using the `balanceOf(address)uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     *  @dev See {IERC20-balanceOf}.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    balanceOf: (params: CallParams<Erc20CustomArgs['obj']['balanceOf(address)uint256'] | Erc20CustomArgs['tuple']['balanceOf(address)uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(Erc20CustomParamsFactory.balanceOf(params))
    },

    /**
     * Makes a call to the ERC20Custom smart contract using the `transfer(address,uint256)bool` ABI method.
     *
    *  @dev See {IERC20-transfer}.
    Requirements:
    - `recipient` cannot be the zero address.
    - the caller must have a balance of at least `amount`.

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    transfer: (params: CallParams<Erc20CustomArgs['obj']['transfer(address,uint256)bool'] | Erc20CustomArgs['tuple']['transfer(address,uint256)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(Erc20CustomParamsFactory.transfer(params))
    },

    /**
     * Makes a call to the ERC20Custom smart contract using the `allowance(address,address)uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     *  @dev See {IERC20-allowance}.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    allowance: (params: CallParams<Erc20CustomArgs['obj']['allowance(address,address)uint256'] | Erc20CustomArgs['tuple']['allowance(address,address)uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(Erc20CustomParamsFactory.allowance(params))
    },

    /**
     * Makes a call to the ERC20Custom smart contract using the `approve(address,uint256)bool` ABI method.
     *
    *  @dev See {IERC20-approve}.
    Requirements:
    - `spender` cannot be the zero address.approve(address spender, uint256 amount)

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    approve: (params: CallParams<Erc20CustomArgs['obj']['approve(address,uint256)bool'] | Erc20CustomArgs['tuple']['approve(address,uint256)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(Erc20CustomParamsFactory.approve(params))
    },

    /**
     * Makes a call to the ERC20Custom smart contract using the `transferFrom(address,address,uint256)bool` ABI method.
     *
    *  @dev See {IERC20-transferFrom}.
    Emits an {Approval} event indicating the updated allowance. This is not
    required by the EIP. See the note at the beginning of {ERC20};
    Requirements:
    - `sender` and `recipient` cannot be the zero address.
    - `sender` must have a balance of at least `amount`.
    - the caller must have allowance for `sender`'s tokens of at least
    `amount`.

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    transferFrom: (params: CallParams<Erc20CustomArgs['obj']['transferFrom(address,address,uint256)bool'] | Erc20CustomArgs['tuple']['transferFrom(address,address,uint256)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(Erc20CustomParamsFactory.transferFrom(params))
    },

    /**
     * Makes a call to the ERC20Custom smart contract using the `increaseAllowance(address,uint256)bool` ABI method.
     *
    *  @dev Atomically increases the allowance granted to `spender` by the caller.
    This is an alternative to {approve} that can be used as a mitigation for
    problems described in {IERC20-approve}.
    Emits an {Approval} event indicating the updated allowance.
    Requirements:
    - `spender` cannot be the zero address.

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    increaseAllowance: (params: CallParams<Erc20CustomArgs['obj']['increaseAllowance(address,uint256)bool'] | Erc20CustomArgs['tuple']['increaseAllowance(address,uint256)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(Erc20CustomParamsFactory.increaseAllowance(params))
    },

    /**
     * Makes a call to the ERC20Custom smart contract using the `decreaseAllowance(address,uint256)bool` ABI method.
     *
    *  @dev Atomically decreases the allowance granted to `spender` by the caller.
    This is an alternative to {approve} that can be used as a mitigation for
    problems described in {IERC20-approve}.
    Emits an {Approval} event indicating the updated allowance.
    Requirements:
    - `spender` cannot be the zero address.
    - `spender` must have allowance for the caller of at least
    `subtractedValue`.

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    decreaseAllowance: (params: CallParams<Erc20CustomArgs['obj']['decreaseAllowance(address,uint256)bool'] | Erc20CustomArgs['tuple']['decreaseAllowance(address,uint256)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(Erc20CustomParamsFactory.decreaseAllowance(params))
    },

    /**
     * Makes a call to the ERC20Custom smart contract using the `burn(uint256)void` ABI method.
     *
    *  @dev Destroys `amount` tokens from the caller.
    See {ERC20-_burn}.

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    burn: (params: CallParams<Erc20CustomArgs['obj']['burn(uint256)void'] | Erc20CustomArgs['tuple']['burn(uint256)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(Erc20CustomParamsFactory.burn(params))
    },

    /**
     * Makes a call to the ERC20Custom smart contract using the `burnFrom(address,uint256)void` ABI method.
     *
    *  @dev Destroys `amount` tokens from `account`, deducting from the caller's
    allowance.
    See {ERC20-_burn} and {ERC20-allowance}.
    Requirements:
    - the caller must have allowance for `accounts`'s tokens of at least
    `amount`.

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    burnFrom: (params: CallParams<Erc20CustomArgs['obj']['burnFrom(address,uint256)void'] | Erc20CustomArgs['tuple']['burnFrom(address,uint256)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(Erc20CustomParamsFactory.burnFrom(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Makes a clear_state call to an existing instance of the ERC20Custom smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the ERC20Custom smart contract using the `__postInit()void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    __postInit: async (params: CallParams<Erc20CustomArgs['obj']['__postInit()void'] | Erc20CustomArgs['tuple']['__postInit()void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(Erc20CustomParamsFactory.__postInit(params))
      return {...result, return: result.return as unknown as (undefined | Erc20CustomReturns['__postInit()void'])}
    },

    /**
     * Makes a call to the ERC20Custom smart contract using the `totalSupply()uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     *  @dev See {IERC20-totalSupply}.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    totalSupply: async (params: CallParams<Erc20CustomArgs['obj']['totalSupply()uint256'] | Erc20CustomArgs['tuple']['totalSupply()uint256']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(Erc20CustomParamsFactory.totalSupply(params))
      return {...result, return: result.return as unknown as (undefined | Erc20CustomReturns['totalSupply()uint256'])}
    },

    /**
     * Makes a call to the ERC20Custom smart contract using the `balanceOf(address)uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     *  @dev See {IERC20-balanceOf}.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    balanceOf: async (params: CallParams<Erc20CustomArgs['obj']['balanceOf(address)uint256'] | Erc20CustomArgs['tuple']['balanceOf(address)uint256']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(Erc20CustomParamsFactory.balanceOf(params))
      return {...result, return: result.return as unknown as (undefined | Erc20CustomReturns['balanceOf(address)uint256'])}
    },

    /**
     * Makes a call to the ERC20Custom smart contract using the `transfer(address,uint256)bool` ABI method.
     *
    *  @dev See {IERC20-transfer}.
    Requirements:
    - `recipient` cannot be the zero address.
    - the caller must have a balance of at least `amount`.

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    transfer: async (params: CallParams<Erc20CustomArgs['obj']['transfer(address,uint256)bool'] | Erc20CustomArgs['tuple']['transfer(address,uint256)bool']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(Erc20CustomParamsFactory.transfer(params))
      return {...result, return: result.return as unknown as (undefined | Erc20CustomReturns['transfer(address,uint256)bool'])}
    },

    /**
     * Makes a call to the ERC20Custom smart contract using the `allowance(address,address)uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     *  @dev See {IERC20-allowance}.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    allowance: async (params: CallParams<Erc20CustomArgs['obj']['allowance(address,address)uint256'] | Erc20CustomArgs['tuple']['allowance(address,address)uint256']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(Erc20CustomParamsFactory.allowance(params))
      return {...result, return: result.return as unknown as (undefined | Erc20CustomReturns['allowance(address,address)uint256'])}
    },

    /**
     * Makes a call to the ERC20Custom smart contract using the `approve(address,uint256)bool` ABI method.
     *
    *  @dev See {IERC20-approve}.
    Requirements:
    - `spender` cannot be the zero address.approve(address spender, uint256 amount)

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    approve: async (params: CallParams<Erc20CustomArgs['obj']['approve(address,uint256)bool'] | Erc20CustomArgs['tuple']['approve(address,uint256)bool']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(Erc20CustomParamsFactory.approve(params))
      return {...result, return: result.return as unknown as (undefined | Erc20CustomReturns['approve(address,uint256)bool'])}
    },

    /**
     * Makes a call to the ERC20Custom smart contract using the `transferFrom(address,address,uint256)bool` ABI method.
     *
    *  @dev See {IERC20-transferFrom}.
    Emits an {Approval} event indicating the updated allowance. This is not
    required by the EIP. See the note at the beginning of {ERC20};
    Requirements:
    - `sender` and `recipient` cannot be the zero address.
    - `sender` must have a balance of at least `amount`.
    - the caller must have allowance for `sender`'s tokens of at least
    `amount`.

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    transferFrom: async (params: CallParams<Erc20CustomArgs['obj']['transferFrom(address,address,uint256)bool'] | Erc20CustomArgs['tuple']['transferFrom(address,address,uint256)bool']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(Erc20CustomParamsFactory.transferFrom(params))
      return {...result, return: result.return as unknown as (undefined | Erc20CustomReturns['transferFrom(address,address,uint256)bool'])}
    },

    /**
     * Makes a call to the ERC20Custom smart contract using the `increaseAllowance(address,uint256)bool` ABI method.
     *
    *  @dev Atomically increases the allowance granted to `spender` by the caller.
    This is an alternative to {approve} that can be used as a mitigation for
    problems described in {IERC20-approve}.
    Emits an {Approval} event indicating the updated allowance.
    Requirements:
    - `spender` cannot be the zero address.

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    increaseAllowance: async (params: CallParams<Erc20CustomArgs['obj']['increaseAllowance(address,uint256)bool'] | Erc20CustomArgs['tuple']['increaseAllowance(address,uint256)bool']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(Erc20CustomParamsFactory.increaseAllowance(params))
      return {...result, return: result.return as unknown as (undefined | Erc20CustomReturns['increaseAllowance(address,uint256)bool'])}
    },

    /**
     * Makes a call to the ERC20Custom smart contract using the `decreaseAllowance(address,uint256)bool` ABI method.
     *
    *  @dev Atomically decreases the allowance granted to `spender` by the caller.
    This is an alternative to {approve} that can be used as a mitigation for
    problems described in {IERC20-approve}.
    Emits an {Approval} event indicating the updated allowance.
    Requirements:
    - `spender` cannot be the zero address.
    - `spender` must have allowance for the caller of at least
    `subtractedValue`.

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    decreaseAllowance: async (params: CallParams<Erc20CustomArgs['obj']['decreaseAllowance(address,uint256)bool'] | Erc20CustomArgs['tuple']['decreaseAllowance(address,uint256)bool']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(Erc20CustomParamsFactory.decreaseAllowance(params))
      return {...result, return: result.return as unknown as (undefined | Erc20CustomReturns['decreaseAllowance(address,uint256)bool'])}
    },

    /**
     * Makes a call to the ERC20Custom smart contract using the `burn(uint256)void` ABI method.
     *
    *  @dev Destroys `amount` tokens from the caller.
    See {ERC20-_burn}.

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    burn: async (params: CallParams<Erc20CustomArgs['obj']['burn(uint256)void'] | Erc20CustomArgs['tuple']['burn(uint256)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(Erc20CustomParamsFactory.burn(params))
      return {...result, return: result.return as unknown as (undefined | Erc20CustomReturns['burn(uint256)void'])}
    },

    /**
     * Makes a call to the ERC20Custom smart contract using the `burnFrom(address,uint256)void` ABI method.
     *
    *  @dev Destroys `amount` tokens from `account`, deducting from the caller's
    allowance.
    See {ERC20-_burn} and {ERC20-allowance}.
    Requirements:
    - the caller must have allowance for `accounts`'s tokens of at least
    `amount`.

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    burnFrom: async (params: CallParams<Erc20CustomArgs['obj']['burnFrom(address,uint256)void'] | Erc20CustomArgs['tuple']['burnFrom(address,uint256)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(Erc20CustomParamsFactory.burnFrom(params))
      return {...result, return: result.return as unknown as (undefined | Erc20CustomReturns['burnFrom(address,uint256)void'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new Erc20CustomClient(this.appClient.clone(params))
  }

  /**
   * Makes a readonly (simulated) call to the ERC20Custom smart contract using the `totalSupply()uint256` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   *  @dev See {IERC20-totalSupply}.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async totalSupply(params: CallParams<Erc20CustomArgs['obj']['totalSupply()uint256'] | Erc20CustomArgs['tuple']['totalSupply()uint256']> = {args: []}) {
    const result = await this.appClient.send.call(Erc20CustomParamsFactory.totalSupply(params))
    return result.return as unknown as Erc20CustomReturns['totalSupply()uint256']
  }

  /**
   * Makes a readonly (simulated) call to the ERC20Custom smart contract using the `balanceOf(address)uint256` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   *  @dev See {IERC20-balanceOf}.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async balanceOf(params: CallParams<Erc20CustomArgs['obj']['balanceOf(address)uint256'] | Erc20CustomArgs['tuple']['balanceOf(address)uint256']>) {
    const result = await this.appClient.send.call(Erc20CustomParamsFactory.balanceOf(params))
    return result.return as unknown as Erc20CustomReturns['balanceOf(address)uint256']
  }

  /**
   * Makes a readonly (simulated) call to the ERC20Custom smart contract using the `allowance(address,address)uint256` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   *  @dev See {IERC20-allowance}.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async allowance(params: CallParams<Erc20CustomArgs['obj']['allowance(address,address)uint256'] | Erc20CustomArgs['tuple']['allowance(address,address)uint256']>) {
    const result = await this.appClient.send.call(Erc20CustomParamsFactory.allowance(params))
    return result.return as unknown as Erc20CustomReturns['allowance(address,address)uint256']
  }

  /**
   * Methods to access state for the current ERC20Custom app
   */
  state = {
    /**
     * Methods to access global state for the current ERC20Custom app
     */
    global: {
      /**
       * Get all current keyed values from global state
       */
      getAll: async (): Promise<Partial<Expand<GlobalKeysState>>> => {
        const result = await this.appClient.state.global.getAll()
        return {
          _totalSupply: new BinaryStateValue(result._totalSupply),
          __ctorPending: result.__ctor_pending,
        }
      },
      /**
       * Get the current value of the _totalSupply key in global state
       */
      _totalSupply: async (): Promise<BinaryState> => { return new BinaryStateValue((await this.appClient.state.global.getValue("_totalSupply")) as Uint8Array | undefined) },
      /**
       * Get the current value of the __ctor_pending key in global state
       */
      __ctorPending: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("__ctor_pending")) as bigint | undefined },
    },
    /**
     * Methods to access box state for the current ERC20Custom app
     */
    box: {
      /**
       * Get all current keyed values from box state
       */
      getAll: async (): Promise<Partial<Expand<BoxKeysState>>> => {
        const result = await this.appClient.state.box.getAll()
        return {
        }
      },
      /**
       * Get values from the _balances map in box state
       */
      _balances: {
        /**
         * Get all current values of the _balances map in box state
         */
        getMap: async (): Promise<Map<Uint8Array, Uint8Array>> => { return (await this.appClient.state.box.getMap("_balances")) as Map<Uint8Array, Uint8Array> },
        /**
         * Get a current value of the _balances map by key from box state
         */
        value: async (key: Uint8Array | string): Promise<Uint8Array | undefined> => { return await this.appClient.state.box.getMapValue("_balances", key) as Uint8Array | undefined },
      },
      /**
       * Get values from the _allowances map in box state
       */
      _allowances: {
        /**
         * Get all current values of the _allowances map in box state
         */
        getMap: async (): Promise<Map<Uint8Array, Uint8Array>> => { return (await this.appClient.state.box.getMap("_allowances")) as Map<Uint8Array, Uint8Array> },
        /**
         * Get a current value of the _allowances map by key from box state
         */
        value: async (key: Uint8Array | string): Promise<Uint8Array | undefined> => { return await this.appClient.state.box.getMapValue("_allowances", key) as Uint8Array | undefined },
      },
    },
  }

  public newGroup(): Erc20CustomComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a __postInit()void method call against the ERC20Custom contract
       */
      __postInit(params: CallParams<Erc20CustomArgs['obj']['__postInit()void'] | Erc20CustomArgs['tuple']['__postInit()void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.__postInit(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a totalSupply()uint256 method call against the ERC20Custom contract
       */
      totalSupply(params: CallParams<Erc20CustomArgs['obj']['totalSupply()uint256'] | Erc20CustomArgs['tuple']['totalSupply()uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.totalSupply(params)))
        resultMappers.push((v) => client.decodeReturnValue('totalSupply()uint256', v))
        return this
      },
      /**
       * Add a balanceOf(address)uint256 method call against the ERC20Custom contract
       */
      balanceOf(params: CallParams<Erc20CustomArgs['obj']['balanceOf(address)uint256'] | Erc20CustomArgs['tuple']['balanceOf(address)uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.balanceOf(params)))
        resultMappers.push((v) => client.decodeReturnValue('balanceOf(address)uint256', v))
        return this
      },
      /**
       * Add a transfer(address,uint256)bool method call against the ERC20Custom contract
       */
      transfer(params: CallParams<Erc20CustomArgs['obj']['transfer(address,uint256)bool'] | Erc20CustomArgs['tuple']['transfer(address,uint256)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.transfer(params)))
        resultMappers.push((v) => client.decodeReturnValue('transfer(address,uint256)bool', v))
        return this
      },
      /**
       * Add a allowance(address,address)uint256 method call against the ERC20Custom contract
       */
      allowance(params: CallParams<Erc20CustomArgs['obj']['allowance(address,address)uint256'] | Erc20CustomArgs['tuple']['allowance(address,address)uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.allowance(params)))
        resultMappers.push((v) => client.decodeReturnValue('allowance(address,address)uint256', v))
        return this
      },
      /**
       * Add a approve(address,uint256)bool method call against the ERC20Custom contract
       */
      approve(params: CallParams<Erc20CustomArgs['obj']['approve(address,uint256)bool'] | Erc20CustomArgs['tuple']['approve(address,uint256)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.approve(params)))
        resultMappers.push((v) => client.decodeReturnValue('approve(address,uint256)bool', v))
        return this
      },
      /**
       * Add a transferFrom(address,address,uint256)bool method call against the ERC20Custom contract
       */
      transferFrom(params: CallParams<Erc20CustomArgs['obj']['transferFrom(address,address,uint256)bool'] | Erc20CustomArgs['tuple']['transferFrom(address,address,uint256)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.transferFrom(params)))
        resultMappers.push((v) => client.decodeReturnValue('transferFrom(address,address,uint256)bool', v))
        return this
      },
      /**
       * Add a increaseAllowance(address,uint256)bool method call against the ERC20Custom contract
       */
      increaseAllowance(params: CallParams<Erc20CustomArgs['obj']['increaseAllowance(address,uint256)bool'] | Erc20CustomArgs['tuple']['increaseAllowance(address,uint256)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.increaseAllowance(params)))
        resultMappers.push((v) => client.decodeReturnValue('increaseAllowance(address,uint256)bool', v))
        return this
      },
      /**
       * Add a decreaseAllowance(address,uint256)bool method call against the ERC20Custom contract
       */
      decreaseAllowance(params: CallParams<Erc20CustomArgs['obj']['decreaseAllowance(address,uint256)bool'] | Erc20CustomArgs['tuple']['decreaseAllowance(address,uint256)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.decreaseAllowance(params)))
        resultMappers.push((v) => client.decodeReturnValue('decreaseAllowance(address,uint256)bool', v))
        return this
      },
      /**
       * Add a burn(uint256)void method call against the ERC20Custom contract
       */
      burn(params: CallParams<Erc20CustomArgs['obj']['burn(uint256)void'] | Erc20CustomArgs['tuple']['burn(uint256)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.burn(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a burnFrom(address,uint256)void method call against the ERC20Custom contract
       */
      burnFrom(params: CallParams<Erc20CustomArgs['obj']['burnFrom(address,uint256)void'] | Erc20CustomArgs['tuple']['burnFrom(address,uint256)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.burnFrom(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a clear state call to the ERC20Custom contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as Erc20CustomComposer
  }
}
export type Erc20CustomComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the __postInit()void ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  __postInit(params?: CallParams<Erc20CustomArgs['obj']['__postInit()void'] | Erc20CustomArgs['tuple']['__postInit()void']>): Erc20CustomComposer<[...TReturns, Erc20CustomReturns['__postInit()void'] | undefined]>

  /**
   * Calls the totalSupply()uint256 ABI method.
   *
   *  @dev See {IERC20-totalSupply}.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  totalSupply(params?: CallParams<Erc20CustomArgs['obj']['totalSupply()uint256'] | Erc20CustomArgs['tuple']['totalSupply()uint256']>): Erc20CustomComposer<[...TReturns, Erc20CustomReturns['totalSupply()uint256'] | undefined]>

  /**
   * Calls the balanceOf(address)uint256 ABI method.
   *
   *  @dev See {IERC20-balanceOf}.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  balanceOf(params?: CallParams<Erc20CustomArgs['obj']['balanceOf(address)uint256'] | Erc20CustomArgs['tuple']['balanceOf(address)uint256']>): Erc20CustomComposer<[...TReturns, Erc20CustomReturns['balanceOf(address)uint256'] | undefined]>

  /**
   * Calls the transfer(address,uint256)bool ABI method.
   *
  *  @dev See {IERC20-transfer}.
  Requirements:
  - `recipient` cannot be the zero address.
  - the caller must have a balance of at least `amount`.

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  transfer(params?: CallParams<Erc20CustomArgs['obj']['transfer(address,uint256)bool'] | Erc20CustomArgs['tuple']['transfer(address,uint256)bool']>): Erc20CustomComposer<[...TReturns, Erc20CustomReturns['transfer(address,uint256)bool'] | undefined]>

  /**
   * Calls the allowance(address,address)uint256 ABI method.
   *
   *  @dev See {IERC20-allowance}.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  allowance(params?: CallParams<Erc20CustomArgs['obj']['allowance(address,address)uint256'] | Erc20CustomArgs['tuple']['allowance(address,address)uint256']>): Erc20CustomComposer<[...TReturns, Erc20CustomReturns['allowance(address,address)uint256'] | undefined]>

  /**
   * Calls the approve(address,uint256)bool ABI method.
   *
  *  @dev See {IERC20-approve}.
  Requirements:
  - `spender` cannot be the zero address.approve(address spender, uint256 amount)

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  approve(params?: CallParams<Erc20CustomArgs['obj']['approve(address,uint256)bool'] | Erc20CustomArgs['tuple']['approve(address,uint256)bool']>): Erc20CustomComposer<[...TReturns, Erc20CustomReturns['approve(address,uint256)bool'] | undefined]>

  /**
   * Calls the transferFrom(address,address,uint256)bool ABI method.
   *
  *  @dev See {IERC20-transferFrom}.
  Emits an {Approval} event indicating the updated allowance. This is not
  required by the EIP. See the note at the beginning of {ERC20};
  Requirements:
  - `sender` and `recipient` cannot be the zero address.
  - `sender` must have a balance of at least `amount`.
  - the caller must have allowance for `sender`'s tokens of at least
  `amount`.

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  transferFrom(params?: CallParams<Erc20CustomArgs['obj']['transferFrom(address,address,uint256)bool'] | Erc20CustomArgs['tuple']['transferFrom(address,address,uint256)bool']>): Erc20CustomComposer<[...TReturns, Erc20CustomReturns['transferFrom(address,address,uint256)bool'] | undefined]>

  /**
   * Calls the increaseAllowance(address,uint256)bool ABI method.
   *
  *  @dev Atomically increases the allowance granted to `spender` by the caller.
  This is an alternative to {approve} that can be used as a mitigation for
  problems described in {IERC20-approve}.
  Emits an {Approval} event indicating the updated allowance.
  Requirements:
  - `spender` cannot be the zero address.

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  increaseAllowance(params?: CallParams<Erc20CustomArgs['obj']['increaseAllowance(address,uint256)bool'] | Erc20CustomArgs['tuple']['increaseAllowance(address,uint256)bool']>): Erc20CustomComposer<[...TReturns, Erc20CustomReturns['increaseAllowance(address,uint256)bool'] | undefined]>

  /**
   * Calls the decreaseAllowance(address,uint256)bool ABI method.
   *
  *  @dev Atomically decreases the allowance granted to `spender` by the caller.
  This is an alternative to {approve} that can be used as a mitigation for
  problems described in {IERC20-approve}.
  Emits an {Approval} event indicating the updated allowance.
  Requirements:
  - `spender` cannot be the zero address.
  - `spender` must have allowance for the caller of at least
  `subtractedValue`.

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  decreaseAllowance(params?: CallParams<Erc20CustomArgs['obj']['decreaseAllowance(address,uint256)bool'] | Erc20CustomArgs['tuple']['decreaseAllowance(address,uint256)bool']>): Erc20CustomComposer<[...TReturns, Erc20CustomReturns['decreaseAllowance(address,uint256)bool'] | undefined]>

  /**
   * Calls the burn(uint256)void ABI method.
   *
  *  @dev Destroys `amount` tokens from the caller.
  See {ERC20-_burn}.

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  burn(params?: CallParams<Erc20CustomArgs['obj']['burn(uint256)void'] | Erc20CustomArgs['tuple']['burn(uint256)void']>): Erc20CustomComposer<[...TReturns, Erc20CustomReturns['burn(uint256)void'] | undefined]>

  /**
   * Calls the burnFrom(address,uint256)void ABI method.
   *
  *  @dev Destroys `amount` tokens from `account`, deducting from the caller's
  allowance.
  See {ERC20-_burn} and {ERC20-allowance}.
  Requirements:
  - the caller must have allowance for `accounts`'s tokens of at least
  `amount`.

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  burnFrom(params?: CallParams<Erc20CustomArgs['obj']['burnFrom(address,uint256)void'] | Erc20CustomArgs['tuple']['burnFrom(address,uint256)void']>): Erc20CustomComposer<[...TReturns, Erc20CustomReturns['burnFrom(address,uint256)void'] | undefined]>

  /**
   * Makes a clear_state call to an existing instance of the ERC20Custom smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): Erc20CustomComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): Erc20CustomComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<Erc20CustomComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<Erc20CustomComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<Erc20CustomComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<Erc20CustomComposerResults<TReturns>>
}
export type Erc20CustomComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

