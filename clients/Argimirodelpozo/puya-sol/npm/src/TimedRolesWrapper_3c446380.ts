/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"TimedRolesWrapper","structs":{"checkTimedRoleReturn":[{"name":"isActive","type":"bool"},{"name":"start","type":"uint64"},{"name":"expires","type":"uint64"}],"timedRoleActiveReturn":[{"name":"isActive","type":"bool"},{"name":"start","type":"uint64"},{"name":"expires","type":"uint64"}]},"methods":[{"name":"owner","args":[],"returns":{"type":"address"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"checkTimedRole","args":[{"type":"address","name":"holder"},{"type":"uint256","name":"role"}],"returns":{"type":"(bool,uint64,uint64)","struct":"checkTimedRoleReturn"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"setTimedRole","args":[{"type":"address","name":"holder"},{"type":"uint256","name":"timedRole"},{"type":"uint64","name":"start"},{"type":"uint64","name":"expires"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"@dev Sets the active time range of `timedRole` of `holder` to [`start`, `expires`].\n The `timedRole` is active when `start <= block.timestamp && block.timestamp <= expires`.","events":[],"recommendations":{}},{"name":"timedRoleActive","args":[{"type":"address","name":"holder"},{"type":"uint256","name":"timedRole"}],"returns":{"type":"(bool,uint64,uint64)","struct":"timedRoleActiveReturn"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"desc":"@dev Returns whether the `timedRole` is active for `holder` and the active time range.","events":[],"recommendations":{}}],"arcs":[22,28],"networks":{},"state":{"schema":{"global":{"ints":0,"bytes":1},"local":{"ints":0,"bytes":0}},"keys":{"global":{"_owner":{"keyType":"AVMString","valueType":"address","key":"X293bmVy"}},"local":{},"box":{}},"maps":{"global":{},"local":{},"box":{}}},"bareActions":{"create":[],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[81],"errorMessage":"check _owner exists"},{"pc":[95,131,161],"errorMessage":"invalid number of bytes for arc4.static_array<arc4.uint8, 32>"},{"pc":[103,138,169],"errorMessage":"invalid number of bytes for arc4.uint256"},{"pc":[145,152],"errorMessage":"invalid number of bytes for arc4.uint64"},{"pc":[153],"errorMessage":"revert"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"events":[]} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


// Type definitions for ARC-56 structs

export type CheckTimedRoleReturn = {
  isActive: boolean,
  start: bigint,
  expires: bigint
}


/**
 * Converts the ABI tuple representation of a checkTimedRoleReturn to the struct representation
 */
export function CheckTimedRoleReturnFromTuple(abiTuple: [boolean, bigint, bigint]) {
  return getABIStructFromABITuple(abiTuple, APP_SPEC.structs.checkTimedRoleReturn, APP_SPEC.structs) as CheckTimedRoleReturn
}

export type TimedRoleActiveReturn = {
  isActive: boolean,
  start: bigint,
  expires: bigint
}


/**
 * Converts the ABI tuple representation of a timedRoleActiveReturn to the struct representation
 */
export function TimedRoleActiveReturnFromTuple(abiTuple: [boolean, bigint, bigint]) {
  return getABIStructFromABITuple(abiTuple, APP_SPEC.structs.timedRoleActiveReturn, APP_SPEC.structs) as TimedRoleActiveReturn
}

/**
 * The argument types for the TimedRolesWrapper contract
 */
export type TimedRolesWrapperArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'owner()address': Record<string, never>
    'checkTimedRole(address,uint256)(bool,uint64,uint64)': {
      holder: string
      role: bigint | number
    }
    'setTimedRole(address,uint256,uint64,uint64)void': {
      holder: string
      timedRole: bigint | number
      start: bigint | number
      expires: bigint | number
    }
    'timedRoleActive(address,uint256)(bool,uint64,uint64)': {
      holder: string
      timedRole: bigint | number
    }
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'owner()address': []
    'checkTimedRole(address,uint256)(bool,uint64,uint64)': [holder: string, role: bigint | number]
    'setTimedRole(address,uint256,uint64,uint64)void': [holder: string, timedRole: bigint | number, start: bigint | number, expires: bigint | number]
    'timedRoleActive(address,uint256)(bool,uint64,uint64)': [holder: string, timedRole: bigint | number]
  }
}

/**
 * The return type for each method
 */
export type TimedRolesWrapperReturns = {
  'owner()address': string
  'checkTimedRole(address,uint256)(bool,uint64,uint64)': CheckTimedRoleReturn
  'setTimedRole(address,uint256,uint64,uint64)void': void
  'timedRoleActive(address,uint256)(bool,uint64,uint64)': TimedRoleActiveReturn
}

/**
 * Defines the types of available calls and state of the TimedRolesWrapper smart contract.
 */
export type TimedRolesWrapperTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'owner()address' | 'owner', {
      argsObj: TimedRolesWrapperArgs['obj']['owner()address']
      argsTuple: TimedRolesWrapperArgs['tuple']['owner()address']
      returns: TimedRolesWrapperReturns['owner()address']
    }>
    & Record<'checkTimedRole(address,uint256)(bool,uint64,uint64)' | 'checkTimedRole', {
      argsObj: TimedRolesWrapperArgs['obj']['checkTimedRole(address,uint256)(bool,uint64,uint64)']
      argsTuple: TimedRolesWrapperArgs['tuple']['checkTimedRole(address,uint256)(bool,uint64,uint64)']
      returns: TimedRolesWrapperReturns['checkTimedRole(address,uint256)(bool,uint64,uint64)']
    }>
    & Record<'setTimedRole(address,uint256,uint64,uint64)void' | 'setTimedRole', {
      argsObj: TimedRolesWrapperArgs['obj']['setTimedRole(address,uint256,uint64,uint64)void']
      argsTuple: TimedRolesWrapperArgs['tuple']['setTimedRole(address,uint256,uint64,uint64)void']
      returns: TimedRolesWrapperReturns['setTimedRole(address,uint256,uint64,uint64)void']
    }>
    & Record<'timedRoleActive(address,uint256)(bool,uint64,uint64)' | 'timedRoleActive', {
      argsObj: TimedRolesWrapperArgs['obj']['timedRoleActive(address,uint256)(bool,uint64,uint64)']
      argsTuple: TimedRolesWrapperArgs['tuple']['timedRoleActive(address,uint256)(bool,uint64,uint64)']
      returns: TimedRolesWrapperReturns['timedRoleActive(address,uint256)(bool,uint64,uint64)']
    }>
  /**
   * Defines the shape of the state of the application.
   */
  state: {
    global: {
      keys: {
        _owner: string
      }
      maps: {}
    }
  }
}

/**
 * Defines the possible abi call signatures.
 */
export type TimedRolesWrapperSignatures = keyof TimedRolesWrapperTypes['methods']
/**
 * Defines the possible abi call signatures for methods that return a non-void value.
 */
export type TimedRolesWrapperNonVoidMethodSignatures = keyof TimedRolesWrapperTypes['methods'] extends infer T ? T extends keyof TimedRolesWrapperTypes['methods'] ? MethodReturn<T> extends void ? never : T  : never : never
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the TimedRolesWrapper smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends TimedRolesWrapperSignatures> = TimedRolesWrapperTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the TimedRolesWrapper smart contract to the method's return type
 */
export type MethodReturn<TSignature extends TimedRolesWrapperSignatures> = TimedRolesWrapperTypes['methods'][TSignature]['returns']

/**
 * Defines the shape of the keyed global state of the application.
 */
export type GlobalKeysState = TimedRolesWrapperTypes['state']['global']['keys']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the TimedRolesWrapper smart contract
 */
export abstract class TimedRolesWrapperParamsFactory {
  /**
   * Constructs a no op call for the owner()address ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static owner(params: CallParams<TimedRolesWrapperArgs['obj']['owner()address'] | TimedRolesWrapperArgs['tuple']['owner()address']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'owner()address' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the checkTimedRole(address,uint256)(bool,uint64,uint64) ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static checkTimedRole(params: CallParams<TimedRolesWrapperArgs['obj']['checkTimedRole(address,uint256)(bool,uint64,uint64)'] | TimedRolesWrapperArgs['tuple']['checkTimedRole(address,uint256)(bool,uint64,uint64)']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'checkTimedRole(address,uint256)(bool,uint64,uint64)' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.holder, params.args.role],
    }
  }
  /**
   * Constructs a no op call for the setTimedRole(address,uint256,uint64,uint64)void ABI method
   *
  * @dev Sets the active time range of `timedRole` of `holder` to [`start`, `expires`].
  The `timedRole` is active when `start <= block.timestamp && block.timestamp <= expires`.

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static setTimedRole(params: CallParams<TimedRolesWrapperArgs['obj']['setTimedRole(address,uint256,uint64,uint64)void'] | TimedRolesWrapperArgs['tuple']['setTimedRole(address,uint256,uint64,uint64)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'setTimedRole(address,uint256,uint64,uint64)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.holder, params.args.timedRole, params.args.start, params.args.expires],
    }
  }
  /**
   * Constructs a no op call for the timedRoleActive(address,uint256)(bool,uint64,uint64) ABI method
   *
   * @dev Returns whether the `timedRole` is active for `holder` and the active time range.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static timedRoleActive(params: CallParams<TimedRolesWrapperArgs['obj']['timedRoleActive(address,uint256)(bool,uint64,uint64)'] | TimedRolesWrapperArgs['tuple']['timedRoleActive(address,uint256)(bool,uint64,uint64)']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'timedRoleActive(address,uint256)(bool,uint64,uint64)' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.holder, params.args.timedRole],
    }
  }
}

/**
 * A client to make calls to the TimedRolesWrapper smart contract
 */
export class TimedRolesWrapperClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `TimedRolesWrapperClient`
   *
   * @param appClient An `AppClient` instance which has been created with the TimedRolesWrapper app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `TimedRolesWrapperClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Checks for decode errors on the given return value and maps the return value to the return type for the given method
   * @returns The typed return value or undefined if there was no value
   */
  decodeReturnValue<TSignature extends TimedRolesWrapperNonVoidMethodSignatures>(method: TSignature, returnValue: ABIReturn | undefined) {
    return returnValue !== undefined ? getArc56ReturnValue<MethodReturn<TSignature>>(returnValue, this.appClient.getABIMethod(method), APP_SPEC.structs) : undefined
  }

  /**
   * Returns a new `TimedRolesWrapperClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<TimedRolesWrapperClient> {
    return new TimedRolesWrapperClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `TimedRolesWrapperClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<TimedRolesWrapperClient> {
    return new TimedRolesWrapperClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Makes a clear_state call to an existing instance of the TimedRolesWrapper smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the TimedRolesWrapper smart contract using the `owner()address` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    owner: (params: CallParams<TimedRolesWrapperArgs['obj']['owner()address'] | TimedRolesWrapperArgs['tuple']['owner()address']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(TimedRolesWrapperParamsFactory.owner(params))
    },

    /**
     * Makes a call to the TimedRolesWrapper smart contract using the `checkTimedRole(address,uint256)(bool,uint64,uint64)` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    checkTimedRole: (params: CallParams<TimedRolesWrapperArgs['obj']['checkTimedRole(address,uint256)(bool,uint64,uint64)'] | TimedRolesWrapperArgs['tuple']['checkTimedRole(address,uint256)(bool,uint64,uint64)']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(TimedRolesWrapperParamsFactory.checkTimedRole(params))
    },

    /**
     * Makes a call to the TimedRolesWrapper smart contract using the `setTimedRole(address,uint256,uint64,uint64)void` ABI method.
     *
    * @dev Sets the active time range of `timedRole` of `holder` to [`start`, `expires`].
    The `timedRole` is active when `start <= block.timestamp && block.timestamp <= expires`.

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    setTimedRole: (params: CallParams<TimedRolesWrapperArgs['obj']['setTimedRole(address,uint256,uint64,uint64)void'] | TimedRolesWrapperArgs['tuple']['setTimedRole(address,uint256,uint64,uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(TimedRolesWrapperParamsFactory.setTimedRole(params))
    },

    /**
     * Makes a call to the TimedRolesWrapper smart contract using the `timedRoleActive(address,uint256)(bool,uint64,uint64)` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @dev Returns whether the `timedRole` is active for `holder` and the active time range.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    timedRoleActive: (params: CallParams<TimedRolesWrapperArgs['obj']['timedRoleActive(address,uint256)(bool,uint64,uint64)'] | TimedRolesWrapperArgs['tuple']['timedRoleActive(address,uint256)(bool,uint64,uint64)']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(TimedRolesWrapperParamsFactory.timedRoleActive(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Makes a clear_state call to an existing instance of the TimedRolesWrapper smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the TimedRolesWrapper smart contract using the `owner()address` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    owner: (params: CallParams<TimedRolesWrapperArgs['obj']['owner()address'] | TimedRolesWrapperArgs['tuple']['owner()address']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(TimedRolesWrapperParamsFactory.owner(params))
    },

    /**
     * Makes a call to the TimedRolesWrapper smart contract using the `checkTimedRole(address,uint256)(bool,uint64,uint64)` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    checkTimedRole: (params: CallParams<TimedRolesWrapperArgs['obj']['checkTimedRole(address,uint256)(bool,uint64,uint64)'] | TimedRolesWrapperArgs['tuple']['checkTimedRole(address,uint256)(bool,uint64,uint64)']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(TimedRolesWrapperParamsFactory.checkTimedRole(params))
    },

    /**
     * Makes a call to the TimedRolesWrapper smart contract using the `setTimedRole(address,uint256,uint64,uint64)void` ABI method.
     *
    * @dev Sets the active time range of `timedRole` of `holder` to [`start`, `expires`].
    The `timedRole` is active when `start <= block.timestamp && block.timestamp <= expires`.

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    setTimedRole: (params: CallParams<TimedRolesWrapperArgs['obj']['setTimedRole(address,uint256,uint64,uint64)void'] | TimedRolesWrapperArgs['tuple']['setTimedRole(address,uint256,uint64,uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(TimedRolesWrapperParamsFactory.setTimedRole(params))
    },

    /**
     * Makes a call to the TimedRolesWrapper smart contract using the `timedRoleActive(address,uint256)(bool,uint64,uint64)` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @dev Returns whether the `timedRole` is active for `holder` and the active time range.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    timedRoleActive: (params: CallParams<TimedRolesWrapperArgs['obj']['timedRoleActive(address,uint256)(bool,uint64,uint64)'] | TimedRolesWrapperArgs['tuple']['timedRoleActive(address,uint256)(bool,uint64,uint64)']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(TimedRolesWrapperParamsFactory.timedRoleActive(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Makes a clear_state call to an existing instance of the TimedRolesWrapper smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the TimedRolesWrapper smart contract using the `owner()address` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    owner: async (params: CallParams<TimedRolesWrapperArgs['obj']['owner()address'] | TimedRolesWrapperArgs['tuple']['owner()address']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(TimedRolesWrapperParamsFactory.owner(params))
      return {...result, return: result.return as unknown as (undefined | TimedRolesWrapperReturns['owner()address'])}
    },

    /**
     * Makes a call to the TimedRolesWrapper smart contract using the `checkTimedRole(address,uint256)(bool,uint64,uint64)` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    checkTimedRole: async (params: CallParams<TimedRolesWrapperArgs['obj']['checkTimedRole(address,uint256)(bool,uint64,uint64)'] | TimedRolesWrapperArgs['tuple']['checkTimedRole(address,uint256)(bool,uint64,uint64)']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(TimedRolesWrapperParamsFactory.checkTimedRole(params))
      return {...result, return: result.return as unknown as (undefined | TimedRolesWrapperReturns['checkTimedRole(address,uint256)(bool,uint64,uint64)'])}
    },

    /**
     * Makes a call to the TimedRolesWrapper smart contract using the `setTimedRole(address,uint256,uint64,uint64)void` ABI method.
     *
    * @dev Sets the active time range of `timedRole` of `holder` to [`start`, `expires`].
    The `timedRole` is active when `start <= block.timestamp && block.timestamp <= expires`.

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    setTimedRole: async (params: CallParams<TimedRolesWrapperArgs['obj']['setTimedRole(address,uint256,uint64,uint64)void'] | TimedRolesWrapperArgs['tuple']['setTimedRole(address,uint256,uint64,uint64)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(TimedRolesWrapperParamsFactory.setTimedRole(params))
      return {...result, return: result.return as unknown as (undefined | TimedRolesWrapperReturns['setTimedRole(address,uint256,uint64,uint64)void'])}
    },

    /**
     * Makes a call to the TimedRolesWrapper smart contract using the `timedRoleActive(address,uint256)(bool,uint64,uint64)` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @dev Returns whether the `timedRole` is active for `holder` and the active time range.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    timedRoleActive: async (params: CallParams<TimedRolesWrapperArgs['obj']['timedRoleActive(address,uint256)(bool,uint64,uint64)'] | TimedRolesWrapperArgs['tuple']['timedRoleActive(address,uint256)(bool,uint64,uint64)']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(TimedRolesWrapperParamsFactory.timedRoleActive(params))
      return {...result, return: result.return as unknown as (undefined | TimedRolesWrapperReturns['timedRoleActive(address,uint256)(bool,uint64,uint64)'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new TimedRolesWrapperClient(this.appClient.clone(params))
  }

  /**
   * Makes a readonly (simulated) call to the TimedRolesWrapper smart contract using the `owner()address` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async owner(params: CallParams<TimedRolesWrapperArgs['obj']['owner()address'] | TimedRolesWrapperArgs['tuple']['owner()address']> = {args: []}) {
    const result = await this.appClient.send.call(TimedRolesWrapperParamsFactory.owner(params))
    return result.return as unknown as TimedRolesWrapperReturns['owner()address']
  }

  /**
   * Makes a readonly (simulated) call to the TimedRolesWrapper smart contract using the `checkTimedRole(address,uint256)(bool,uint64,uint64)` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async checkTimedRole(params: CallParams<TimedRolesWrapperArgs['obj']['checkTimedRole(address,uint256)(bool,uint64,uint64)'] | TimedRolesWrapperArgs['tuple']['checkTimedRole(address,uint256)(bool,uint64,uint64)']>) {
    const result = await this.appClient.send.call(TimedRolesWrapperParamsFactory.checkTimedRole(params))
    return result.return as unknown as TimedRolesWrapperReturns['checkTimedRole(address,uint256)(bool,uint64,uint64)']
  }

  /**
   * Makes a readonly (simulated) call to the TimedRolesWrapper smart contract using the `timedRoleActive(address,uint256)(bool,uint64,uint64)` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @dev Returns whether the `timedRole` is active for `holder` and the active time range.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async timedRoleActive(params: CallParams<TimedRolesWrapperArgs['obj']['timedRoleActive(address,uint256)(bool,uint64,uint64)'] | TimedRolesWrapperArgs['tuple']['timedRoleActive(address,uint256)(bool,uint64,uint64)']>) {
    const result = await this.appClient.send.call(TimedRolesWrapperParamsFactory.timedRoleActive(params))
    return result.return as unknown as TimedRolesWrapperReturns['timedRoleActive(address,uint256)(bool,uint64,uint64)']
  }

  /**
   * Methods to access state for the current TimedRolesWrapper app
   */
  state = {
    /**
     * Methods to access global state for the current TimedRolesWrapper app
     */
    global: {
      /**
       * Get all current keyed values from global state
       */
      getAll: async (): Promise<Partial<Expand<GlobalKeysState>>> => {
        const result = await this.appClient.state.global.getAll()
        return {
          _owner: result._owner,
        }
      },
      /**
       * Get the current value of the _owner key in global state
       */
      _owner: async (): Promise<string | undefined> => { return (await this.appClient.state.global.getValue("_owner")) as string | undefined },
    },
  }

  public newGroup(): TimedRolesWrapperComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a owner()address method call against the TimedRolesWrapper contract
       */
      owner(params: CallParams<TimedRolesWrapperArgs['obj']['owner()address'] | TimedRolesWrapperArgs['tuple']['owner()address']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.owner(params)))
        resultMappers.push((v) => client.decodeReturnValue('owner()address', v))
        return this
      },
      /**
       * Add a checkTimedRole(address,uint256)(bool,uint64,uint64) method call against the TimedRolesWrapper contract
       */
      checkTimedRole(params: CallParams<TimedRolesWrapperArgs['obj']['checkTimedRole(address,uint256)(bool,uint64,uint64)'] | TimedRolesWrapperArgs['tuple']['checkTimedRole(address,uint256)(bool,uint64,uint64)']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.checkTimedRole(params)))
        resultMappers.push((v) => client.decodeReturnValue('checkTimedRole(address,uint256)(bool,uint64,uint64)', v))
        return this
      },
      /**
       * Add a setTimedRole(address,uint256,uint64,uint64)void method call against the TimedRolesWrapper contract
       */
      setTimedRole(params: CallParams<TimedRolesWrapperArgs['obj']['setTimedRole(address,uint256,uint64,uint64)void'] | TimedRolesWrapperArgs['tuple']['setTimedRole(address,uint256,uint64,uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.setTimedRole(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a timedRoleActive(address,uint256)(bool,uint64,uint64) method call against the TimedRolesWrapper contract
       */
      timedRoleActive(params: CallParams<TimedRolesWrapperArgs['obj']['timedRoleActive(address,uint256)(bool,uint64,uint64)'] | TimedRolesWrapperArgs['tuple']['timedRoleActive(address,uint256)(bool,uint64,uint64)']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.timedRoleActive(params)))
        resultMappers.push((v) => client.decodeReturnValue('timedRoleActive(address,uint256)(bool,uint64,uint64)', v))
        return this
      },
      /**
       * Add a clear state call to the TimedRolesWrapper contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as TimedRolesWrapperComposer
  }
}
export type TimedRolesWrapperComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the owner()address ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  owner(params?: CallParams<TimedRolesWrapperArgs['obj']['owner()address'] | TimedRolesWrapperArgs['tuple']['owner()address']>): TimedRolesWrapperComposer<[...TReturns, TimedRolesWrapperReturns['owner()address'] | undefined]>

  /**
   * Calls the checkTimedRole(address,uint256)(bool,uint64,uint64) ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  checkTimedRole(params?: CallParams<TimedRolesWrapperArgs['obj']['checkTimedRole(address,uint256)(bool,uint64,uint64)'] | TimedRolesWrapperArgs['tuple']['checkTimedRole(address,uint256)(bool,uint64,uint64)']>): TimedRolesWrapperComposer<[...TReturns, TimedRolesWrapperReturns['checkTimedRole(address,uint256)(bool,uint64,uint64)'] | undefined]>

  /**
   * Calls the setTimedRole(address,uint256,uint64,uint64)void ABI method.
   *
  * @dev Sets the active time range of `timedRole` of `holder` to [`start`, `expires`].
  The `timedRole` is active when `start <= block.timestamp && block.timestamp <= expires`.

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  setTimedRole(params?: CallParams<TimedRolesWrapperArgs['obj']['setTimedRole(address,uint256,uint64,uint64)void'] | TimedRolesWrapperArgs['tuple']['setTimedRole(address,uint256,uint64,uint64)void']>): TimedRolesWrapperComposer<[...TReturns, TimedRolesWrapperReturns['setTimedRole(address,uint256,uint64,uint64)void'] | undefined]>

  /**
   * Calls the timedRoleActive(address,uint256)(bool,uint64,uint64) ABI method.
   *
   * @dev Returns whether the `timedRole` is active for `holder` and the active time range.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  timedRoleActive(params?: CallParams<TimedRolesWrapperArgs['obj']['timedRoleActive(address,uint256)(bool,uint64,uint64)'] | TimedRolesWrapperArgs['tuple']['timedRoleActive(address,uint256)(bool,uint64,uint64)']>): TimedRolesWrapperComposer<[...TReturns, TimedRolesWrapperReturns['timedRoleActive(address,uint256)(bool,uint64,uint64)'] | undefined]>

  /**
   * Makes a clear_state call to an existing instance of the TimedRolesWrapper smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): TimedRolesWrapperComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): TimedRolesWrapperComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<TimedRolesWrapperComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<TimedRolesWrapperComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<TimedRolesWrapperComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<TimedRolesWrapperComposerResults<TReturns>>
}
export type TimedRolesWrapperComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

