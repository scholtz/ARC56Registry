/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"helper","structs":{},"methods":[{"name":"getBalance","args":[],"returns":{"type":"uint256"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}},{"name":"setFlag","args":[],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}},{"name":"getFlag","args":[],"returns":{"type":"bool"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}}],"arcs":[22,28],"networks":{},"state":{"schema":{"global":{"ints":1,"bytes":0},"local":{"ints":0,"bytes":0}},"keys":{"global":{"flag":{"keyType":"AVMString","valueType":"AVMUint64","key":"ZmxhZw=="}},"local":{},"box":{}},"maps":{"global":{},"local":{},"box":{}}},"bareActions":{"create":[],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[128],"errorMessage":"check flag exists"},{"pc":[124,158],"errorMessage":"not payable"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"events":[]} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


/**
 * The argument types for the Helper contract
 */
export type HelperArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'getBalance()uint256': Record<string, never>
    'setFlag()void': Record<string, never>
    'getFlag()bool': Record<string, never>
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'getBalance()uint256': []
    'setFlag()void': []
    'getFlag()bool': []
  }
}

/**
 * The return type for each method
 */
export type HelperReturns = {
  'getBalance()uint256': bigint
  'setFlag()void': void
  'getFlag()bool': boolean
}

/**
 * Defines the types of available calls and state of the Helper smart contract.
 */
export type HelperTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'getBalance()uint256' | 'getBalance', {
      argsObj: HelperArgs['obj']['getBalance()uint256']
      argsTuple: HelperArgs['tuple']['getBalance()uint256']
      returns: HelperReturns['getBalance()uint256']
    }>
    & Record<'setFlag()void' | 'setFlag', {
      argsObj: HelperArgs['obj']['setFlag()void']
      argsTuple: HelperArgs['tuple']['setFlag()void']
      returns: HelperReturns['setFlag()void']
    }>
    & Record<'getFlag()bool' | 'getFlag', {
      argsObj: HelperArgs['obj']['getFlag()bool']
      argsTuple: HelperArgs['tuple']['getFlag()bool']
      returns: HelperReturns['getFlag()bool']
    }>
  /**
   * Defines the shape of the state of the application.
   */
  state: {
    global: {
      keys: {
        flag: bigint
      }
      maps: {}
    }
  }
}

/**
 * Defines the possible abi call signatures.
 */
export type HelperSignatures = keyof HelperTypes['methods']
/**
 * Defines the possible abi call signatures for methods that return a non-void value.
 */
export type HelperNonVoidMethodSignatures = keyof HelperTypes['methods'] extends infer T ? T extends keyof HelperTypes['methods'] ? MethodReturn<T> extends void ? never : T  : never : never
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the Helper smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends HelperSignatures> = HelperTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the Helper smart contract to the method's return type
 */
export type MethodReturn<TSignature extends HelperSignatures> = HelperTypes['methods'][TSignature]['returns']

/**
 * Defines the shape of the keyed global state of the application.
 */
export type GlobalKeysState = HelperTypes['state']['global']['keys']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the Helper smart contract
 */
export abstract class HelperParamsFactory {
  /**
   * Constructs a no op call for the getBalance()uint256 ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static getBalance(params: CallParams<HelperArgs['obj']['getBalance()uint256'] | HelperArgs['tuple']['getBalance()uint256']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'getBalance()uint256' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the setFlag()void ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static setFlag(params: CallParams<HelperArgs['obj']['setFlag()void'] | HelperArgs['tuple']['setFlag()void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'setFlag()void' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the getFlag()bool ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static getFlag(params: CallParams<HelperArgs['obj']['getFlag()bool'] | HelperArgs['tuple']['getFlag()bool']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'getFlag()bool' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
}

/**
 * A client to make calls to the helper smart contract
 */
export class HelperClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `HelperClient`
   *
   * @param appClient An `AppClient` instance which has been created with the Helper app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `HelperClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Checks for decode errors on the given return value and maps the return value to the return type for the given method
   * @returns The typed return value or undefined if there was no value
   */
  decodeReturnValue<TSignature extends HelperNonVoidMethodSignatures>(method: TSignature, returnValue: ABIReturn | undefined) {
    return returnValue !== undefined ? getArc56ReturnValue<MethodReturn<TSignature>>(returnValue, this.appClient.getABIMethod(method), APP_SPEC.structs) : undefined
  }

  /**
   * Returns a new `HelperClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<HelperClient> {
    return new HelperClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `HelperClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<HelperClient> {
    return new HelperClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Makes a clear_state call to an existing instance of the helper smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the helper smart contract using the `getBalance()uint256` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    getBalance: (params: CallParams<HelperArgs['obj']['getBalance()uint256'] | HelperArgs['tuple']['getBalance()uint256']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(HelperParamsFactory.getBalance(params))
    },

    /**
     * Makes a call to the helper smart contract using the `setFlag()void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    setFlag: (params: CallParams<HelperArgs['obj']['setFlag()void'] | HelperArgs['tuple']['setFlag()void']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(HelperParamsFactory.setFlag(params))
    },

    /**
     * Makes a call to the helper smart contract using the `getFlag()bool` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    getFlag: (params: CallParams<HelperArgs['obj']['getFlag()bool'] | HelperArgs['tuple']['getFlag()bool']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(HelperParamsFactory.getFlag(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Makes a clear_state call to an existing instance of the helper smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the helper smart contract using the `getBalance()uint256` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    getBalance: (params: CallParams<HelperArgs['obj']['getBalance()uint256'] | HelperArgs['tuple']['getBalance()uint256']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(HelperParamsFactory.getBalance(params))
    },

    /**
     * Makes a call to the helper smart contract using the `setFlag()void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    setFlag: (params: CallParams<HelperArgs['obj']['setFlag()void'] | HelperArgs['tuple']['setFlag()void']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(HelperParamsFactory.setFlag(params))
    },

    /**
     * Makes a call to the helper smart contract using the `getFlag()bool` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    getFlag: (params: CallParams<HelperArgs['obj']['getFlag()bool'] | HelperArgs['tuple']['getFlag()bool']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(HelperParamsFactory.getFlag(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Makes a clear_state call to an existing instance of the helper smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the helper smart contract using the `getBalance()uint256` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    getBalance: async (params: CallParams<HelperArgs['obj']['getBalance()uint256'] | HelperArgs['tuple']['getBalance()uint256']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(HelperParamsFactory.getBalance(params))
      return {...result, return: result.return as unknown as (undefined | HelperReturns['getBalance()uint256'])}
    },

    /**
     * Makes a call to the helper smart contract using the `setFlag()void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    setFlag: async (params: CallParams<HelperArgs['obj']['setFlag()void'] | HelperArgs['tuple']['setFlag()void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(HelperParamsFactory.setFlag(params))
      return {...result, return: result.return as unknown as (undefined | HelperReturns['setFlag()void'])}
    },

    /**
     * Makes a call to the helper smart contract using the `getFlag()bool` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    getFlag: async (params: CallParams<HelperArgs['obj']['getFlag()bool'] | HelperArgs['tuple']['getFlag()bool']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(HelperParamsFactory.getFlag(params))
      return {...result, return: result.return as unknown as (undefined | HelperReturns['getFlag()bool'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new HelperClient(this.appClient.clone(params))
  }

  /**
   * Methods to access state for the current helper app
   */
  state = {
    /**
     * Methods to access global state for the current helper app
     */
    global: {
      /**
       * Get all current keyed values from global state
       */
      getAll: async (): Promise<Partial<Expand<GlobalKeysState>>> => {
        const result = await this.appClient.state.global.getAll()
        return {
          flag: result.flag,
        }
      },
      /**
       * Get the current value of the flag key in global state
       */
      flag: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("flag")) as bigint | undefined },
    },
  }

  public newGroup(): HelperComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a getBalance()uint256 method call against the helper contract
       */
      getBalance(params: CallParams<HelperArgs['obj']['getBalance()uint256'] | HelperArgs['tuple']['getBalance()uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.getBalance(params)))
        resultMappers.push((v) => client.decodeReturnValue('getBalance()uint256', v))
        return this
      },
      /**
       * Add a setFlag()void method call against the helper contract
       */
      setFlag(params: CallParams<HelperArgs['obj']['setFlag()void'] | HelperArgs['tuple']['setFlag()void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.setFlag(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a getFlag()bool method call against the helper contract
       */
      getFlag(params: CallParams<HelperArgs['obj']['getFlag()bool'] | HelperArgs['tuple']['getFlag()bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.getFlag(params)))
        resultMappers.push((v) => client.decodeReturnValue('getFlag()bool', v))
        return this
      },
      /**
       * Add a clear state call to the helper contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as HelperComposer
  }
}
export type HelperComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the getBalance()uint256 ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  getBalance(params?: CallParams<HelperArgs['obj']['getBalance()uint256'] | HelperArgs['tuple']['getBalance()uint256']>): HelperComposer<[...TReturns, HelperReturns['getBalance()uint256'] | undefined]>

  /**
   * Calls the setFlag()void ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  setFlag(params?: CallParams<HelperArgs['obj']['setFlag()void'] | HelperArgs['tuple']['setFlag()void']>): HelperComposer<[...TReturns, HelperReturns['setFlag()void'] | undefined]>

  /**
   * Calls the getFlag()bool ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  getFlag(params?: CallParams<HelperArgs['obj']['getFlag()bool'] | HelperArgs['tuple']['getFlag()bool']>): HelperComposer<[...TReturns, HelperReturns['getFlag()bool'] | undefined]>

  /**
   * Makes a clear_state call to an existing instance of the helper smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): HelperComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): HelperComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<HelperComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<HelperComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<HelperComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<HelperComposerResults<TReturns>>
}
export type HelperComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

