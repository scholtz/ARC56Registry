/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"AaveOracle","structs":{},"methods":[{"name":"__postInit","args":[{"type":"uint64","name":"decimals_"},{"type":"string","name":"description_"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}},{"name":"setSpoke","args":[{"type":"address","name":"spoke"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"@inheritdoc IAaveOracle","events":[{"name":"SetSpoke","args":[{"type":"uint8[32]","name":"spoke"}]}],"recommendations":{}},{"name":"setReserveSource","args":[{"type":"uint256","name":"__arc4_reserveId"},{"type":"address","name":"source"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"@inheritdoc IAaveOracle","events":[{"name":"UpdateReserveSource","args":[{"type":"uint256","name":"reserveId"},{"type":"uint8[32]","name":"source"}]}],"recommendations":{}},{"name":"getReservePrice","args":[{"type":"uint256","name":"__arc4_reserveId"}],"returns":{"type":"uint256"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"desc":"@inheritdoc IPriceOracle","events":[],"recommendations":{}},{"name":"getReservesPrices","args":[{"type":"uint256[]","name":"reserveIds"}],"returns":{"type":"uint256[]"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"desc":"@inheritdoc IAaveOracle","events":[],"recommendations":{}},{"name":"getReserveSource","args":[{"type":"uint256","name":"__arc4_reserveId"}],"returns":{"type":"address"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"desc":"@inheritdoc IAaveOracle","events":[],"recommendations":{}},{"name":"DECIMALS","args":[],"returns":{"type":"uint64"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"DESCRIPTION","args":[],"returns":{"type":"string"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"SPOKE","args":[],"returns":{"type":"address"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}}],"arcs":[22,28],"desc":"@title AaveOracle\n @author Aave Labs\n @notice Provides reserve prices.\n @dev Oracles are spoke-specific, due to the usage of reserve id as index of the `_sources` mapping.","networks":{},"state":{"schema":{"global":{"ints":2,"bytes":3},"local":{"ints":0,"bytes":0}},"keys":{"global":{"DECIMALS":{"keyType":"AVMString","valueType":"AVMUint64","key":"REVDSU1BTFM="},"DESCRIPTION":{"keyType":"AVMString","valueType":"AVMString","key":"REVTQ1JJUFRJT04="},"SPOKE":{"keyType":"AVMString","valueType":"address","key":"U1BPS0U="},"DEPLOYER":{"keyType":"AVMString","valueType":"address","key":"REVQTE9ZRVI="},"__ctor_pending":{"keyType":"AVMString","valueType":"AVMUint64","key":"X19jdG9yX3BlbmRpbmc="}},"local":{},"box":{}},"maps":{"global":{},"local":{},"box":{"_sources":{"keyType":"AVMBytes","valueType":"address","prefix":"X3NvdXJjZXM="}}}},"bareActions":{"create":[],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[839],"errorMessage":"InvalidAddress"},{"pc":[1000],"errorMessage":"InvalidPrice"},{"pc":[962],"errorMessage":"InvalidSource"},{"pc":[733],"errorMessage":"InvalidSourceDecimals"},{"pc":[833],"errorMessage":"OnlyDeployer"},{"pc":[693],"errorMessage":"OnlySpoke"},{"pc":[846],"errorMessage":"SpokeAlreadySet"},{"pc":[899],"errorMessage":"__postInit already called"},{"pc":[363,731],"errorMessage":"check DECIMALS exists"},{"pc":[811,827],"errorMessage":"check DEPLOYER exists"},{"pc":[328],"errorMessage":"check DESCRIPTION exists"},{"pc":[300,672,687,843],"errorMessage":"check SPOKE exists"},{"pc":[545,565],"errorMessage":"index access is out of bounds"},{"pc":[437,885],"errorMessage":"invalid array length header"},{"pc":[448],"errorMessage":"invalid number of bytes for arc4.dynamic_array<arc4.uint256>"},{"pc":[892],"errorMessage":"invalid number of bytes for arc4.dynamic_array<arc4.uint8>"},{"pc":[653,791],"errorMessage":"invalid number of bytes for arc4.static_array<arc4.uint8, 32>"},{"pc":[384,597,643],"errorMessage":"invalid number of bytes for arc4.uint256"},{"pc":[878],"errorMessage":"invalid number of bytes for arc4.uint64"},{"pc":[296,323,359,397,461,610,666,804],"errorMessage":"not payable"},{"pc":[553,620],"errorMessage":"overflow"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"events":[{"name":"SetSpoke","args":[{"type":"uint8[32]","name":"spoke"}]},{"name":"UpdateReserveSource","args":[{"type":"uint256","name":"reserveId"},{"type":"uint8[32]","name":"source"}]}]} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


/**
 * The argument types for the AaveOracle contract
 */
export type AaveOracleArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    '__postInit(uint64,string)void': {
      decimals: bigint | number
      description: string
    }
    'setSpoke(address)void': {
      spoke: string
    }
    'setReserveSource(uint256,address)void': {
      __arc4ReserveId: bigint | number
      source: string
    }
    'getReservePrice(uint256)uint256': {
      __arc4ReserveId: bigint | number
    }
    'getReservesPrices(uint256[])uint256[]': {
      reserveIds: bigint[] | number[]
    }
    'getReserveSource(uint256)address': {
      __arc4ReserveId: bigint | number
    }
    'DECIMALS()uint64': Record<string, never>
    'DESCRIPTION()string': Record<string, never>
    'SPOKE()address': Record<string, never>
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    '__postInit(uint64,string)void': [decimals: bigint | number, description: string]
    'setSpoke(address)void': [spoke: string]
    'setReserveSource(uint256,address)void': [__arc4ReserveId: bigint | number, source: string]
    'getReservePrice(uint256)uint256': [__arc4ReserveId: bigint | number]
    'getReservesPrices(uint256[])uint256[]': [reserveIds: bigint[] | number[]]
    'getReserveSource(uint256)address': [__arc4ReserveId: bigint | number]
    'DECIMALS()uint64': []
    'DESCRIPTION()string': []
    'SPOKE()address': []
  }
}

/**
 * The return type for each method
 */
export type AaveOracleReturns = {
  '__postInit(uint64,string)void': void
  'setSpoke(address)void': void
  'setReserveSource(uint256,address)void': void
  'getReservePrice(uint256)uint256': bigint
  'getReservesPrices(uint256[])uint256[]': bigint[]
  'getReserveSource(uint256)address': string
  'DECIMALS()uint64': bigint
  'DESCRIPTION()string': string
  'SPOKE()address': string
}

/**
 * Defines the types of available calls and state of the AaveOracle smart contract.
 */
export type AaveOracleTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'__postInit(uint64,string)void' | '__postInit', {
      argsObj: AaveOracleArgs['obj']['__postInit(uint64,string)void']
      argsTuple: AaveOracleArgs['tuple']['__postInit(uint64,string)void']
      returns: AaveOracleReturns['__postInit(uint64,string)void']
    }>
    & Record<'setSpoke(address)void' | 'setSpoke', {
      argsObj: AaveOracleArgs['obj']['setSpoke(address)void']
      argsTuple: AaveOracleArgs['tuple']['setSpoke(address)void']
      returns: AaveOracleReturns['setSpoke(address)void']
    }>
    & Record<'setReserveSource(uint256,address)void' | 'setReserveSource', {
      argsObj: AaveOracleArgs['obj']['setReserveSource(uint256,address)void']
      argsTuple: AaveOracleArgs['tuple']['setReserveSource(uint256,address)void']
      returns: AaveOracleReturns['setReserveSource(uint256,address)void']
    }>
    & Record<'getReservePrice(uint256)uint256' | 'getReservePrice', {
      argsObj: AaveOracleArgs['obj']['getReservePrice(uint256)uint256']
      argsTuple: AaveOracleArgs['tuple']['getReservePrice(uint256)uint256']
      returns: AaveOracleReturns['getReservePrice(uint256)uint256']
    }>
    & Record<'getReservesPrices(uint256[])uint256[]' | 'getReservesPrices', {
      argsObj: AaveOracleArgs['obj']['getReservesPrices(uint256[])uint256[]']
      argsTuple: AaveOracleArgs['tuple']['getReservesPrices(uint256[])uint256[]']
      returns: AaveOracleReturns['getReservesPrices(uint256[])uint256[]']
    }>
    & Record<'getReserveSource(uint256)address' | 'getReserveSource', {
      argsObj: AaveOracleArgs['obj']['getReserveSource(uint256)address']
      argsTuple: AaveOracleArgs['tuple']['getReserveSource(uint256)address']
      returns: AaveOracleReturns['getReserveSource(uint256)address']
    }>
    & Record<'DECIMALS()uint64' | 'DECIMALS', {
      argsObj: AaveOracleArgs['obj']['DECIMALS()uint64']
      argsTuple: AaveOracleArgs['tuple']['DECIMALS()uint64']
      returns: AaveOracleReturns['DECIMALS()uint64']
    }>
    & Record<'DESCRIPTION()string' | 'DESCRIPTION', {
      argsObj: AaveOracleArgs['obj']['DESCRIPTION()string']
      argsTuple: AaveOracleArgs['tuple']['DESCRIPTION()string']
      returns: AaveOracleReturns['DESCRIPTION()string']
    }>
    & Record<'SPOKE()address' | 'SPOKE', {
      argsObj: AaveOracleArgs['obj']['SPOKE()address']
      argsTuple: AaveOracleArgs['tuple']['SPOKE()address']
      returns: AaveOracleReturns['SPOKE()address']
    }>
  /**
   * Defines the shape of the state of the application.
   */
  state: {
    global: {
      keys: {
        decimals: bigint
        description: string
        spoke: string
        deployer: string
        __ctorPending: bigint
      }
      maps: {}
    }
    box: {
      keys: {}
      maps: {
        _sources: Map<Uint8Array | string, string>
      }
    }
  }
}

/**
 * Defines the possible abi call signatures.
 */
export type AaveOracleSignatures = keyof AaveOracleTypes['methods']
/**
 * Defines the possible abi call signatures for methods that return a non-void value.
 */
export type AaveOracleNonVoidMethodSignatures = keyof AaveOracleTypes['methods'] extends infer T ? T extends keyof AaveOracleTypes['methods'] ? MethodReturn<T> extends void ? never : T  : never : never
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the AaveOracle smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends AaveOracleSignatures> = AaveOracleTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the AaveOracle smart contract to the method's return type
 */
export type MethodReturn<TSignature extends AaveOracleSignatures> = AaveOracleTypes['methods'][TSignature]['returns']

/**
 * Defines the shape of the keyed global state of the application.
 */
export type GlobalKeysState = AaveOracleTypes['state']['global']['keys']

/**
 * Defines the shape of the keyed box state of the application.
 */
export type BoxKeysState = AaveOracleTypes['state']['box']['keys']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the AaveOracle smart contract
 */
export abstract class AaveOracleParamsFactory {
  /**
   * Constructs a no op call for the __postInit(uint64,string)void ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static __postInit(params: CallParams<AaveOracleArgs['obj']['__postInit(uint64,string)void'] | AaveOracleArgs['tuple']['__postInit(uint64,string)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: '__postInit(uint64,string)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.decimals, params.args.description],
    }
  }
  /**
   * Constructs a no op call for the setSpoke(address)void ABI method
   *
   * @inheritdoc IAaveOracle
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static setSpoke(params: CallParams<AaveOracleArgs['obj']['setSpoke(address)void'] | AaveOracleArgs['tuple']['setSpoke(address)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'setSpoke(address)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.spoke],
    }
  }
  /**
   * Constructs a no op call for the setReserveSource(uint256,address)void ABI method
   *
   * @inheritdoc IAaveOracle
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static setReserveSource(params: CallParams<AaveOracleArgs['obj']['setReserveSource(uint256,address)void'] | AaveOracleArgs['tuple']['setReserveSource(uint256,address)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'setReserveSource(uint256,address)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.__arc4ReserveId, params.args.source],
    }
  }
  /**
   * Constructs a no op call for the getReservePrice(uint256)uint256 ABI method
   *
   * @inheritdoc IPriceOracle
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static getReservePrice(params: CallParams<AaveOracleArgs['obj']['getReservePrice(uint256)uint256'] | AaveOracleArgs['tuple']['getReservePrice(uint256)uint256']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'getReservePrice(uint256)uint256' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.__arc4ReserveId],
    }
  }
  /**
   * Constructs a no op call for the getReservesPrices(uint256[])uint256[] ABI method
   *
   * @inheritdoc IAaveOracle
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static getReservesPrices(params: CallParams<AaveOracleArgs['obj']['getReservesPrices(uint256[])uint256[]'] | AaveOracleArgs['tuple']['getReservesPrices(uint256[])uint256[]']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'getReservesPrices(uint256[])uint256[]' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.reserveIds],
    }
  }
  /**
   * Constructs a no op call for the getReserveSource(uint256)address ABI method
   *
   * @inheritdoc IAaveOracle
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static getReserveSource(params: CallParams<AaveOracleArgs['obj']['getReserveSource(uint256)address'] | AaveOracleArgs['tuple']['getReserveSource(uint256)address']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'getReserveSource(uint256)address' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.__arc4ReserveId],
    }
  }
  /**
   * Constructs a no op call for the DECIMALS()uint64 ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static decimals(params: CallParams<AaveOracleArgs['obj']['DECIMALS()uint64'] | AaveOracleArgs['tuple']['DECIMALS()uint64']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'DECIMALS()uint64' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the DESCRIPTION()string ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static description(params: CallParams<AaveOracleArgs['obj']['DESCRIPTION()string'] | AaveOracleArgs['tuple']['DESCRIPTION()string']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'DESCRIPTION()string' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the SPOKE()address ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static spoke(params: CallParams<AaveOracleArgs['obj']['SPOKE()address'] | AaveOracleArgs['tuple']['SPOKE()address']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'SPOKE()address' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
}

/**
 * A client to make calls to the AaveOracle smart contract
 */
export class AaveOracleClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `AaveOracleClient`
   *
   * @param appClient An `AppClient` instance which has been created with the AaveOracle app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `AaveOracleClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Checks for decode errors on the given return value and maps the return value to the return type for the given method
   * @returns The typed return value or undefined if there was no value
   */
  decodeReturnValue<TSignature extends AaveOracleNonVoidMethodSignatures>(method: TSignature, returnValue: ABIReturn | undefined) {
    return returnValue !== undefined ? getArc56ReturnValue<MethodReturn<TSignature>>(returnValue, this.appClient.getABIMethod(method), APP_SPEC.structs) : undefined
  }

  /**
   * Returns a new `AaveOracleClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<AaveOracleClient> {
    return new AaveOracleClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `AaveOracleClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<AaveOracleClient> {
    return new AaveOracleClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Makes a clear_state call to an existing instance of the AaveOracle smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the AaveOracle smart contract using the `__postInit(uint64,string)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    __postInit: (params: CallParams<AaveOracleArgs['obj']['__postInit(uint64,string)void'] | AaveOracleArgs['tuple']['__postInit(uint64,string)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(AaveOracleParamsFactory.__postInit(params))
    },

    /**
     * Makes a call to the AaveOracle smart contract using the `setSpoke(address)void` ABI method.
     *
     * @inheritdoc IAaveOracle
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    setSpoke: (params: CallParams<AaveOracleArgs['obj']['setSpoke(address)void'] | AaveOracleArgs['tuple']['setSpoke(address)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(AaveOracleParamsFactory.setSpoke(params))
    },

    /**
     * Makes a call to the AaveOracle smart contract using the `setReserveSource(uint256,address)void` ABI method.
     *
     * @inheritdoc IAaveOracle
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    setReserveSource: (params: CallParams<AaveOracleArgs['obj']['setReserveSource(uint256,address)void'] | AaveOracleArgs['tuple']['setReserveSource(uint256,address)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(AaveOracleParamsFactory.setReserveSource(params))
    },

    /**
     * Makes a call to the AaveOracle smart contract using the `getReservePrice(uint256)uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @inheritdoc IPriceOracle
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    getReservePrice: (params: CallParams<AaveOracleArgs['obj']['getReservePrice(uint256)uint256'] | AaveOracleArgs['tuple']['getReservePrice(uint256)uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(AaveOracleParamsFactory.getReservePrice(params))
    },

    /**
     * Makes a call to the AaveOracle smart contract using the `getReservesPrices(uint256[])uint256[]` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @inheritdoc IAaveOracle
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    getReservesPrices: (params: CallParams<AaveOracleArgs['obj']['getReservesPrices(uint256[])uint256[]'] | AaveOracleArgs['tuple']['getReservesPrices(uint256[])uint256[]']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(AaveOracleParamsFactory.getReservesPrices(params))
    },

    /**
     * Makes a call to the AaveOracle smart contract using the `getReserveSource(uint256)address` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @inheritdoc IAaveOracle
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    getReserveSource: (params: CallParams<AaveOracleArgs['obj']['getReserveSource(uint256)address'] | AaveOracleArgs['tuple']['getReserveSource(uint256)address']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(AaveOracleParamsFactory.getReserveSource(params))
    },

    /**
     * Makes a call to the AaveOracle smart contract using the `DECIMALS()uint64` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    decimals: (params: CallParams<AaveOracleArgs['obj']['DECIMALS()uint64'] | AaveOracleArgs['tuple']['DECIMALS()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(AaveOracleParamsFactory.decimals(params))
    },

    /**
     * Makes a call to the AaveOracle smart contract using the `DESCRIPTION()string` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    description: (params: CallParams<AaveOracleArgs['obj']['DESCRIPTION()string'] | AaveOracleArgs['tuple']['DESCRIPTION()string']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(AaveOracleParamsFactory.description(params))
    },

    /**
     * Makes a call to the AaveOracle smart contract using the `SPOKE()address` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    spoke: (params: CallParams<AaveOracleArgs['obj']['SPOKE()address'] | AaveOracleArgs['tuple']['SPOKE()address']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(AaveOracleParamsFactory.spoke(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Makes a clear_state call to an existing instance of the AaveOracle smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the AaveOracle smart contract using the `__postInit(uint64,string)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    __postInit: (params: CallParams<AaveOracleArgs['obj']['__postInit(uint64,string)void'] | AaveOracleArgs['tuple']['__postInit(uint64,string)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(AaveOracleParamsFactory.__postInit(params))
    },

    /**
     * Makes a call to the AaveOracle smart contract using the `setSpoke(address)void` ABI method.
     *
     * @inheritdoc IAaveOracle
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    setSpoke: (params: CallParams<AaveOracleArgs['obj']['setSpoke(address)void'] | AaveOracleArgs['tuple']['setSpoke(address)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(AaveOracleParamsFactory.setSpoke(params))
    },

    /**
     * Makes a call to the AaveOracle smart contract using the `setReserveSource(uint256,address)void` ABI method.
     *
     * @inheritdoc IAaveOracle
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    setReserveSource: (params: CallParams<AaveOracleArgs['obj']['setReserveSource(uint256,address)void'] | AaveOracleArgs['tuple']['setReserveSource(uint256,address)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(AaveOracleParamsFactory.setReserveSource(params))
    },

    /**
     * Makes a call to the AaveOracle smart contract using the `getReservePrice(uint256)uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @inheritdoc IPriceOracle
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    getReservePrice: (params: CallParams<AaveOracleArgs['obj']['getReservePrice(uint256)uint256'] | AaveOracleArgs['tuple']['getReservePrice(uint256)uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(AaveOracleParamsFactory.getReservePrice(params))
    },

    /**
     * Makes a call to the AaveOracle smart contract using the `getReservesPrices(uint256[])uint256[]` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @inheritdoc IAaveOracle
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    getReservesPrices: (params: CallParams<AaveOracleArgs['obj']['getReservesPrices(uint256[])uint256[]'] | AaveOracleArgs['tuple']['getReservesPrices(uint256[])uint256[]']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(AaveOracleParamsFactory.getReservesPrices(params))
    },

    /**
     * Makes a call to the AaveOracle smart contract using the `getReserveSource(uint256)address` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @inheritdoc IAaveOracle
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    getReserveSource: (params: CallParams<AaveOracleArgs['obj']['getReserveSource(uint256)address'] | AaveOracleArgs['tuple']['getReserveSource(uint256)address']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(AaveOracleParamsFactory.getReserveSource(params))
    },

    /**
     * Makes a call to the AaveOracle smart contract using the `DECIMALS()uint64` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    decimals: (params: CallParams<AaveOracleArgs['obj']['DECIMALS()uint64'] | AaveOracleArgs['tuple']['DECIMALS()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(AaveOracleParamsFactory.decimals(params))
    },

    /**
     * Makes a call to the AaveOracle smart contract using the `DESCRIPTION()string` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    description: (params: CallParams<AaveOracleArgs['obj']['DESCRIPTION()string'] | AaveOracleArgs['tuple']['DESCRIPTION()string']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(AaveOracleParamsFactory.description(params))
    },

    /**
     * Makes a call to the AaveOracle smart contract using the `SPOKE()address` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    spoke: (params: CallParams<AaveOracleArgs['obj']['SPOKE()address'] | AaveOracleArgs['tuple']['SPOKE()address']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(AaveOracleParamsFactory.spoke(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Makes a clear_state call to an existing instance of the AaveOracle smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the AaveOracle smart contract using the `__postInit(uint64,string)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    __postInit: async (params: CallParams<AaveOracleArgs['obj']['__postInit(uint64,string)void'] | AaveOracleArgs['tuple']['__postInit(uint64,string)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(AaveOracleParamsFactory.__postInit(params))
      return {...result, return: result.return as unknown as (undefined | AaveOracleReturns['__postInit(uint64,string)void'])}
    },

    /**
     * Makes a call to the AaveOracle smart contract using the `setSpoke(address)void` ABI method.
     *
     * @inheritdoc IAaveOracle
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    setSpoke: async (params: CallParams<AaveOracleArgs['obj']['setSpoke(address)void'] | AaveOracleArgs['tuple']['setSpoke(address)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(AaveOracleParamsFactory.setSpoke(params))
      return {...result, return: result.return as unknown as (undefined | AaveOracleReturns['setSpoke(address)void'])}
    },

    /**
     * Makes a call to the AaveOracle smart contract using the `setReserveSource(uint256,address)void` ABI method.
     *
     * @inheritdoc IAaveOracle
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    setReserveSource: async (params: CallParams<AaveOracleArgs['obj']['setReserveSource(uint256,address)void'] | AaveOracleArgs['tuple']['setReserveSource(uint256,address)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(AaveOracleParamsFactory.setReserveSource(params))
      return {...result, return: result.return as unknown as (undefined | AaveOracleReturns['setReserveSource(uint256,address)void'])}
    },

    /**
     * Makes a call to the AaveOracle smart contract using the `getReservePrice(uint256)uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @inheritdoc IPriceOracle
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    getReservePrice: async (params: CallParams<AaveOracleArgs['obj']['getReservePrice(uint256)uint256'] | AaveOracleArgs['tuple']['getReservePrice(uint256)uint256']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(AaveOracleParamsFactory.getReservePrice(params))
      return {...result, return: result.return as unknown as (undefined | AaveOracleReturns['getReservePrice(uint256)uint256'])}
    },

    /**
     * Makes a call to the AaveOracle smart contract using the `getReservesPrices(uint256[])uint256[]` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @inheritdoc IAaveOracle
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    getReservesPrices: async (params: CallParams<AaveOracleArgs['obj']['getReservesPrices(uint256[])uint256[]'] | AaveOracleArgs['tuple']['getReservesPrices(uint256[])uint256[]']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(AaveOracleParamsFactory.getReservesPrices(params))
      return {...result, return: result.return as unknown as (undefined | AaveOracleReturns['getReservesPrices(uint256[])uint256[]'])}
    },

    /**
     * Makes a call to the AaveOracle smart contract using the `getReserveSource(uint256)address` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @inheritdoc IAaveOracle
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    getReserveSource: async (params: CallParams<AaveOracleArgs['obj']['getReserveSource(uint256)address'] | AaveOracleArgs['tuple']['getReserveSource(uint256)address']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(AaveOracleParamsFactory.getReserveSource(params))
      return {...result, return: result.return as unknown as (undefined | AaveOracleReturns['getReserveSource(uint256)address'])}
    },

    /**
     * Makes a call to the AaveOracle smart contract using the `DECIMALS()uint64` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    decimals: async (params: CallParams<AaveOracleArgs['obj']['DECIMALS()uint64'] | AaveOracleArgs['tuple']['DECIMALS()uint64']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(AaveOracleParamsFactory.decimals(params))
      return {...result, return: result.return as unknown as (undefined | AaveOracleReturns['DECIMALS()uint64'])}
    },

    /**
     * Makes a call to the AaveOracle smart contract using the `DESCRIPTION()string` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    description: async (params: CallParams<AaveOracleArgs['obj']['DESCRIPTION()string'] | AaveOracleArgs['tuple']['DESCRIPTION()string']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(AaveOracleParamsFactory.description(params))
      return {...result, return: result.return as unknown as (undefined | AaveOracleReturns['DESCRIPTION()string'])}
    },

    /**
     * Makes a call to the AaveOracle smart contract using the `SPOKE()address` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    spoke: async (params: CallParams<AaveOracleArgs['obj']['SPOKE()address'] | AaveOracleArgs['tuple']['SPOKE()address']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(AaveOracleParamsFactory.spoke(params))
      return {...result, return: result.return as unknown as (undefined | AaveOracleReturns['SPOKE()address'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new AaveOracleClient(this.appClient.clone(params))
  }

  /**
   * Makes a readonly (simulated) call to the AaveOracle smart contract using the `getReservePrice(uint256)uint256` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @inheritdoc IPriceOracle
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async getReservePrice(params: CallParams<AaveOracleArgs['obj']['getReservePrice(uint256)uint256'] | AaveOracleArgs['tuple']['getReservePrice(uint256)uint256']>) {
    const result = await this.appClient.send.call(AaveOracleParamsFactory.getReservePrice(params))
    return result.return as unknown as AaveOracleReturns['getReservePrice(uint256)uint256']
  }

  /**
   * Makes a readonly (simulated) call to the AaveOracle smart contract using the `getReservesPrices(uint256[])uint256[]` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @inheritdoc IAaveOracle
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async getReservesPrices(params: CallParams<AaveOracleArgs['obj']['getReservesPrices(uint256[])uint256[]'] | AaveOracleArgs['tuple']['getReservesPrices(uint256[])uint256[]']>) {
    const result = await this.appClient.send.call(AaveOracleParamsFactory.getReservesPrices(params))
    return result.return as unknown as AaveOracleReturns['getReservesPrices(uint256[])uint256[]']
  }

  /**
   * Makes a readonly (simulated) call to the AaveOracle smart contract using the `getReserveSource(uint256)address` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @inheritdoc IAaveOracle
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async getReserveSource(params: CallParams<AaveOracleArgs['obj']['getReserveSource(uint256)address'] | AaveOracleArgs['tuple']['getReserveSource(uint256)address']>) {
    const result = await this.appClient.send.call(AaveOracleParamsFactory.getReserveSource(params))
    return result.return as unknown as AaveOracleReturns['getReserveSource(uint256)address']
  }

  /**
   * Makes a readonly (simulated) call to the AaveOracle smart contract using the `DECIMALS()uint64` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async decimals(params: CallParams<AaveOracleArgs['obj']['DECIMALS()uint64'] | AaveOracleArgs['tuple']['DECIMALS()uint64']> = {args: []}) {
    const result = await this.appClient.send.call(AaveOracleParamsFactory.decimals(params))
    return result.return as unknown as AaveOracleReturns['DECIMALS()uint64']
  }

  /**
   * Makes a readonly (simulated) call to the AaveOracle smart contract using the `DESCRIPTION()string` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async description(params: CallParams<AaveOracleArgs['obj']['DESCRIPTION()string'] | AaveOracleArgs['tuple']['DESCRIPTION()string']> = {args: []}) {
    const result = await this.appClient.send.call(AaveOracleParamsFactory.description(params))
    return result.return as unknown as AaveOracleReturns['DESCRIPTION()string']
  }

  /**
   * Makes a readonly (simulated) call to the AaveOracle smart contract using the `SPOKE()address` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async spoke(params: CallParams<AaveOracleArgs['obj']['SPOKE()address'] | AaveOracleArgs['tuple']['SPOKE()address']> = {args: []}) {
    const result = await this.appClient.send.call(AaveOracleParamsFactory.spoke(params))
    return result.return as unknown as AaveOracleReturns['SPOKE()address']
  }

  /**
   * Methods to access state for the current AaveOracle app
   */
  state = {
    /**
     * Methods to access global state for the current AaveOracle app
     */
    global: {
      /**
       * Get all current keyed values from global state
       */
      getAll: async (): Promise<Partial<Expand<GlobalKeysState>>> => {
        const result = await this.appClient.state.global.getAll()
        return {
          decimals: result.DECIMALS,
          description: result.DESCRIPTION,
          spoke: result.SPOKE,
          deployer: result.DEPLOYER,
          __ctorPending: result.__ctor_pending,
        }
      },
      /**
       * Get the current value of the DECIMALS key in global state
       */
      decimals: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("DECIMALS")) as bigint | undefined },
      /**
       * Get the current value of the DESCRIPTION key in global state
       */
      description: async (): Promise<string | undefined> => { return (await this.appClient.state.global.getValue("DESCRIPTION")) as string | undefined },
      /**
       * Get the current value of the SPOKE key in global state
       */
      spoke: async (): Promise<string | undefined> => { return (await this.appClient.state.global.getValue("SPOKE")) as string | undefined },
      /**
       * Get the current value of the DEPLOYER key in global state
       */
      deployer: async (): Promise<string | undefined> => { return (await this.appClient.state.global.getValue("DEPLOYER")) as string | undefined },
      /**
       * Get the current value of the __ctor_pending key in global state
       */
      __ctorPending: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("__ctor_pending")) as bigint | undefined },
    },
    /**
     * Methods to access box state for the current AaveOracle app
     */
    box: {
      /**
       * Get all current keyed values from box state
       */
      getAll: async (): Promise<Partial<Expand<BoxKeysState>>> => {
        const result = await this.appClient.state.box.getAll()
        return {
        }
      },
      /**
       * Get values from the _sources map in box state
       */
      _sources: {
        /**
         * Get all current values of the _sources map in box state
         */
        getMap: async (): Promise<Map<Uint8Array, string>> => { return (await this.appClient.state.box.getMap("_sources")) as Map<Uint8Array, string> },
        /**
         * Get a current value of the _sources map by key from box state
         */
        value: async (key: Uint8Array | string): Promise<string | undefined> => { return await this.appClient.state.box.getMapValue("_sources", key) as string | undefined },
      },
    },
  }

  public newGroup(): AaveOracleComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a __postInit(uint64,string)void method call against the AaveOracle contract
       */
      __postInit(params: CallParams<AaveOracleArgs['obj']['__postInit(uint64,string)void'] | AaveOracleArgs['tuple']['__postInit(uint64,string)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.__postInit(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a setSpoke(address)void method call against the AaveOracle contract
       */
      setSpoke(params: CallParams<AaveOracleArgs['obj']['setSpoke(address)void'] | AaveOracleArgs['tuple']['setSpoke(address)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.setSpoke(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a setReserveSource(uint256,address)void method call against the AaveOracle contract
       */
      setReserveSource(params: CallParams<AaveOracleArgs['obj']['setReserveSource(uint256,address)void'] | AaveOracleArgs['tuple']['setReserveSource(uint256,address)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.setReserveSource(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a getReservePrice(uint256)uint256 method call against the AaveOracle contract
       */
      getReservePrice(params: CallParams<AaveOracleArgs['obj']['getReservePrice(uint256)uint256'] | AaveOracleArgs['tuple']['getReservePrice(uint256)uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.getReservePrice(params)))
        resultMappers.push((v) => client.decodeReturnValue('getReservePrice(uint256)uint256', v))
        return this
      },
      /**
       * Add a getReservesPrices(uint256[])uint256[] method call against the AaveOracle contract
       */
      getReservesPrices(params: CallParams<AaveOracleArgs['obj']['getReservesPrices(uint256[])uint256[]'] | AaveOracleArgs['tuple']['getReservesPrices(uint256[])uint256[]']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.getReservesPrices(params)))
        resultMappers.push((v) => client.decodeReturnValue('getReservesPrices(uint256[])uint256[]', v))
        return this
      },
      /**
       * Add a getReserveSource(uint256)address method call against the AaveOracle contract
       */
      getReserveSource(params: CallParams<AaveOracleArgs['obj']['getReserveSource(uint256)address'] | AaveOracleArgs['tuple']['getReserveSource(uint256)address']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.getReserveSource(params)))
        resultMappers.push((v) => client.decodeReturnValue('getReserveSource(uint256)address', v))
        return this
      },
      /**
       * Add a DECIMALS()uint64 method call against the AaveOracle contract
       */
      decimals(params: CallParams<AaveOracleArgs['obj']['DECIMALS()uint64'] | AaveOracleArgs['tuple']['DECIMALS()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.decimals(params)))
        resultMappers.push((v) => client.decodeReturnValue('DECIMALS()uint64', v))
        return this
      },
      /**
       * Add a DESCRIPTION()string method call against the AaveOracle contract
       */
      description(params: CallParams<AaveOracleArgs['obj']['DESCRIPTION()string'] | AaveOracleArgs['tuple']['DESCRIPTION()string']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.description(params)))
        resultMappers.push((v) => client.decodeReturnValue('DESCRIPTION()string', v))
        return this
      },
      /**
       * Add a SPOKE()address method call against the AaveOracle contract
       */
      spoke(params: CallParams<AaveOracleArgs['obj']['SPOKE()address'] | AaveOracleArgs['tuple']['SPOKE()address']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.spoke(params)))
        resultMappers.push((v) => client.decodeReturnValue('SPOKE()address', v))
        return this
      },
      /**
       * Add a clear state call to the AaveOracle contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as AaveOracleComposer
  }
}
export type AaveOracleComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the __postInit(uint64,string)void ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  __postInit(params?: CallParams<AaveOracleArgs['obj']['__postInit(uint64,string)void'] | AaveOracleArgs['tuple']['__postInit(uint64,string)void']>): AaveOracleComposer<[...TReturns, AaveOracleReturns['__postInit(uint64,string)void'] | undefined]>

  /**
   * Calls the setSpoke(address)void ABI method.
   *
   * @inheritdoc IAaveOracle
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  setSpoke(params?: CallParams<AaveOracleArgs['obj']['setSpoke(address)void'] | AaveOracleArgs['tuple']['setSpoke(address)void']>): AaveOracleComposer<[...TReturns, AaveOracleReturns['setSpoke(address)void'] | undefined]>

  /**
   * Calls the setReserveSource(uint256,address)void ABI method.
   *
   * @inheritdoc IAaveOracle
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  setReserveSource(params?: CallParams<AaveOracleArgs['obj']['setReserveSource(uint256,address)void'] | AaveOracleArgs['tuple']['setReserveSource(uint256,address)void']>): AaveOracleComposer<[...TReturns, AaveOracleReturns['setReserveSource(uint256,address)void'] | undefined]>

  /**
   * Calls the getReservePrice(uint256)uint256 ABI method.
   *
   * @inheritdoc IPriceOracle
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  getReservePrice(params?: CallParams<AaveOracleArgs['obj']['getReservePrice(uint256)uint256'] | AaveOracleArgs['tuple']['getReservePrice(uint256)uint256']>): AaveOracleComposer<[...TReturns, AaveOracleReturns['getReservePrice(uint256)uint256'] | undefined]>

  /**
   * Calls the getReservesPrices(uint256[])uint256[] ABI method.
   *
   * @inheritdoc IAaveOracle
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  getReservesPrices(params?: CallParams<AaveOracleArgs['obj']['getReservesPrices(uint256[])uint256[]'] | AaveOracleArgs['tuple']['getReservesPrices(uint256[])uint256[]']>): AaveOracleComposer<[...TReturns, AaveOracleReturns['getReservesPrices(uint256[])uint256[]'] | undefined]>

  /**
   * Calls the getReserveSource(uint256)address ABI method.
   *
   * @inheritdoc IAaveOracle
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  getReserveSource(params?: CallParams<AaveOracleArgs['obj']['getReserveSource(uint256)address'] | AaveOracleArgs['tuple']['getReserveSource(uint256)address']>): AaveOracleComposer<[...TReturns, AaveOracleReturns['getReserveSource(uint256)address'] | undefined]>

  /**
   * Calls the DECIMALS()uint64 ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  decimals(params?: CallParams<AaveOracleArgs['obj']['DECIMALS()uint64'] | AaveOracleArgs['tuple']['DECIMALS()uint64']>): AaveOracleComposer<[...TReturns, AaveOracleReturns['DECIMALS()uint64'] | undefined]>

  /**
   * Calls the DESCRIPTION()string ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  description(params?: CallParams<AaveOracleArgs['obj']['DESCRIPTION()string'] | AaveOracleArgs['tuple']['DESCRIPTION()string']>): AaveOracleComposer<[...TReturns, AaveOracleReturns['DESCRIPTION()string'] | undefined]>

  /**
   * Calls the SPOKE()address ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  spoke(params?: CallParams<AaveOracleArgs['obj']['SPOKE()address'] | AaveOracleArgs['tuple']['SPOKE()address']>): AaveOracleComposer<[...TReturns, AaveOracleReturns['SPOKE()address'] | undefined]>

  /**
   * Makes a clear_state call to an existing instance of the AaveOracle smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): AaveOracleComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): AaveOracleComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<AaveOracleComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<AaveOracleComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<AaveOracleComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<AaveOracleComposerResults<TReturns>>
}
export type AaveOracleComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

