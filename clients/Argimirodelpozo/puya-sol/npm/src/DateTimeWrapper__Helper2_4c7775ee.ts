/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"DateTimeWrapper__Helper2","structs":{},"methods":[{"name":"DateTimeLib.dateToTimestamp","args":[{"type":"uint256","name":"year"},{"type":"uint256","name":"month"},{"type":"uint256","name":"day"}],"returns":{"type":"uint256"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"desc":"@dev Returns the unix timestamp from (`year`,`month`,`day`).\n Note: Inputs outside the supported ranges result in undefined behavior.\n Use {isSupportedDate} to check if the inputs are supported.","events":[],"recommendations":{}},{"name":"DateTimeLib.dateToEpochDay","args":[{"type":"uint256","name":"year"},{"type":"uint256","name":"month"},{"type":"uint256","name":"day"}],"returns":{"type":"uint256"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"desc":"@dev Returns the number of days since 1970-01-01 from (`year`,`month`,`day`).\n See: https://howardhinnant.github.io/date_algorithms.html\n Note: Inputs outside the supported ranges result in undefined behavior.\n Use {isSupportedDate} to check if the inputs are supported.","events":[],"recommendations":{}},{"name":"DateTimeLib.daysInMonth","args":[{"type":"uint256","name":"year"},{"type":"uint256","name":"month"}],"returns":{"type":"uint256"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"desc":"@dev Returns number of days in given `month` of `year`.","events":[],"recommendations":{}},{"name":"DateTimeLib.isLeapYear","args":[{"type":"uint256","name":"year"}],"returns":{"type":"bool"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"desc":"@dev Returns if the `year` is leap.","events":[],"recommendations":{}},{"name":"__init__","args":[{"type":"uint64","name":"o"},{"type":"uint64","name":"p"},{"type":"byte[]","name":"s"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}}],"arcs":[22,28],"desc":"Helper contract 2 for DateTimeWrapper","networks":{},"state":{"schema":{"global":{"ints":2,"bytes":1},"local":{"ints":0,"bytes":0}},"keys":{"global":{"o":{"keyType":"AVMString","valueType":"AVMUint64","key":"bw=="},"p":{"keyType":"AVMString","valueType":"AVMUint64","key":"cA=="},"s":{"keyType":"AVMString","valueType":"AVMBytes","key":"cw=="}},"local":{},"box":{}},"maps":{"global":{},"local":{},"box":{}}},"bareActions":{"create":["NoOp"],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[845],"errorMessage":"helper: already initialized"},{"pc":[478,577,668,746],"errorMessage":"helper: must be called in group"},{"pc":[492,591,682,760],"errorMessage":"helper: sender mismatch"},{"pc":[485,584,675,753],"errorMessage":"helper: unauthorized caller"},{"pc":[507,606,697,775],"errorMessage":"helper: wrong prev chunk"},{"pc":[519,618,709,787],"errorMessage":"helper: wrong prev method"},{"pc":[831],"errorMessage":"invalid array length header"},{"pc":[838],"errorMessage":"invalid number of bytes for arc4.dynamic_array<arc4.uint8>"},{"pc":[452,460,468,551,559,567,650,658,736],"errorMessage":"invalid number of bytes for arc4.uint256"},{"pc":[814,823],"errorMessage":"invalid number of bytes for arc4.uint64"},{"pc":[533,632,718],"errorMessage":"overflow"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"events":[]} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


/**
 * The argument types for the DateTimeWrapperHelper2 contract
 */
export type DateTimeWrapperHelper2Args = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'DateTimeLib.dateToTimestamp(uint256,uint256,uint256)uint256': {
      year: bigint | number
      month: bigint | number
      day: bigint | number
    }
    'DateTimeLib.dateToEpochDay(uint256,uint256,uint256)uint256': {
      year: bigint | number
      month: bigint | number
      day: bigint | number
    }
    'DateTimeLib.daysInMonth(uint256,uint256)uint256': {
      year: bigint | number
      month: bigint | number
    }
    'DateTimeLib.isLeapYear(uint256)bool': {
      year: bigint | number
    }
    '__init__(uint64,uint64,byte[])void': {
      o: bigint | number
      p: bigint | number
      s: Uint8Array
    }
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'DateTimeLib.dateToTimestamp(uint256,uint256,uint256)uint256': [year: bigint | number, month: bigint | number, day: bigint | number]
    'DateTimeLib.dateToEpochDay(uint256,uint256,uint256)uint256': [year: bigint | number, month: bigint | number, day: bigint | number]
    'DateTimeLib.daysInMonth(uint256,uint256)uint256': [year: bigint | number, month: bigint | number]
    'DateTimeLib.isLeapYear(uint256)bool': [year: bigint | number]
    '__init__(uint64,uint64,byte[])void': [o: bigint | number, p: bigint | number, s: Uint8Array]
  }
}

/**
 * The return type for each method
 */
export type DateTimeWrapperHelper2Returns = {
  'DateTimeLib.dateToTimestamp(uint256,uint256,uint256)uint256': bigint
  'DateTimeLib.dateToEpochDay(uint256,uint256,uint256)uint256': bigint
  'DateTimeLib.daysInMonth(uint256,uint256)uint256': bigint
  'DateTimeLib.isLeapYear(uint256)bool': boolean
  '__init__(uint64,uint64,byte[])void': void
}

/**
 * Defines the types of available calls and state of the DateTimeWrapperHelper2 smart contract.
 */
export type DateTimeWrapperHelper2Types = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'DateTimeLib.dateToTimestamp(uint256,uint256,uint256)uint256' | 'DateTimeLib.dateToTimestamp', {
      argsObj: DateTimeWrapperHelper2Args['obj']['DateTimeLib.dateToTimestamp(uint256,uint256,uint256)uint256']
      argsTuple: DateTimeWrapperHelper2Args['tuple']['DateTimeLib.dateToTimestamp(uint256,uint256,uint256)uint256']
      returns: DateTimeWrapperHelper2Returns['DateTimeLib.dateToTimestamp(uint256,uint256,uint256)uint256']
    }>
    & Record<'DateTimeLib.dateToEpochDay(uint256,uint256,uint256)uint256' | 'DateTimeLib.dateToEpochDay', {
      argsObj: DateTimeWrapperHelper2Args['obj']['DateTimeLib.dateToEpochDay(uint256,uint256,uint256)uint256']
      argsTuple: DateTimeWrapperHelper2Args['tuple']['DateTimeLib.dateToEpochDay(uint256,uint256,uint256)uint256']
      returns: DateTimeWrapperHelper2Returns['DateTimeLib.dateToEpochDay(uint256,uint256,uint256)uint256']
    }>
    & Record<'DateTimeLib.daysInMonth(uint256,uint256)uint256' | 'DateTimeLib.daysInMonth', {
      argsObj: DateTimeWrapperHelper2Args['obj']['DateTimeLib.daysInMonth(uint256,uint256)uint256']
      argsTuple: DateTimeWrapperHelper2Args['tuple']['DateTimeLib.daysInMonth(uint256,uint256)uint256']
      returns: DateTimeWrapperHelper2Returns['DateTimeLib.daysInMonth(uint256,uint256)uint256']
    }>
    & Record<'DateTimeLib.isLeapYear(uint256)bool' | 'DateTimeLib.isLeapYear', {
      argsObj: DateTimeWrapperHelper2Args['obj']['DateTimeLib.isLeapYear(uint256)bool']
      argsTuple: DateTimeWrapperHelper2Args['tuple']['DateTimeLib.isLeapYear(uint256)bool']
      returns: DateTimeWrapperHelper2Returns['DateTimeLib.isLeapYear(uint256)bool']
    }>
    & Record<'__init__(uint64,uint64,byte[])void' | '__init__', {
      argsObj: DateTimeWrapperHelper2Args['obj']['__init__(uint64,uint64,byte[])void']
      argsTuple: DateTimeWrapperHelper2Args['tuple']['__init__(uint64,uint64,byte[])void']
      returns: DateTimeWrapperHelper2Returns['__init__(uint64,uint64,byte[])void']
    }>
  /**
   * Defines the shape of the state of the application.
   */
  state: {
    global: {
      keys: {
        o: bigint
        p: bigint
        s: BinaryState
      }
      maps: {}
    }
  }
}

/**
 * Defines the possible abi call signatures.
 */
export type DateTimeWrapperHelper2Signatures = keyof DateTimeWrapperHelper2Types['methods']
/**
 * Defines the possible abi call signatures for methods that return a non-void value.
 */
export type DateTimeWrapperHelper2NonVoidMethodSignatures = keyof DateTimeWrapperHelper2Types['methods'] extends infer T ? T extends keyof DateTimeWrapperHelper2Types['methods'] ? MethodReturn<T> extends void ? never : T  : never : never
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the DateTimeWrapperHelper2 smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends DateTimeWrapperHelper2Signatures> = DateTimeWrapperHelper2Types['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the DateTimeWrapperHelper2 smart contract to the method's return type
 */
export type MethodReturn<TSignature extends DateTimeWrapperHelper2Signatures> = DateTimeWrapperHelper2Types['methods'][TSignature]['returns']

/**
 * Defines the shape of the keyed global state of the application.
 */
export type GlobalKeysState = DateTimeWrapperHelper2Types['state']['global']['keys']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the DateTimeWrapperHelper2 smart contract
 */
export abstract class DateTimeWrapperHelper2ParamsFactory {
  /**
   * Constructs a no op call for the DateTimeLib.dateToTimestamp(uint256,uint256,uint256)uint256 ABI method
   *
  * @dev Returns the unix timestamp from (`year`,`month`,`day`).
  Note: Inputs outside the supported ranges result in undefined behavior.
  Use {isSupportedDate} to check if the inputs are supported.

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static dateTimeLibDateToTimestamp(params: CallParams<DateTimeWrapperHelper2Args['obj']['DateTimeLib.dateToTimestamp(uint256,uint256,uint256)uint256'] | DateTimeWrapperHelper2Args['tuple']['DateTimeLib.dateToTimestamp(uint256,uint256,uint256)uint256']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'DateTimeLib.dateToTimestamp(uint256,uint256,uint256)uint256' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.year, params.args.month, params.args.day],
    }
  }
  /**
   * Constructs a no op call for the DateTimeLib.dateToEpochDay(uint256,uint256,uint256)uint256 ABI method
   *
  * @dev Returns the number of days since 1970-01-01 from (`year`,`month`,`day`).
  See: https://howardhinnant.github.io/date_algorithms.html
  Note: Inputs outside the supported ranges result in undefined behavior.
  Use {isSupportedDate} to check if the inputs are supported.

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static dateTimeLibDateToEpochDay(params: CallParams<DateTimeWrapperHelper2Args['obj']['DateTimeLib.dateToEpochDay(uint256,uint256,uint256)uint256'] | DateTimeWrapperHelper2Args['tuple']['DateTimeLib.dateToEpochDay(uint256,uint256,uint256)uint256']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'DateTimeLib.dateToEpochDay(uint256,uint256,uint256)uint256' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.year, params.args.month, params.args.day],
    }
  }
  /**
   * Constructs a no op call for the DateTimeLib.daysInMonth(uint256,uint256)uint256 ABI method
   *
   * @dev Returns number of days in given `month` of `year`.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static dateTimeLibDaysInMonth(params: CallParams<DateTimeWrapperHelper2Args['obj']['DateTimeLib.daysInMonth(uint256,uint256)uint256'] | DateTimeWrapperHelper2Args['tuple']['DateTimeLib.daysInMonth(uint256,uint256)uint256']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'DateTimeLib.daysInMonth(uint256,uint256)uint256' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.year, params.args.month],
    }
  }
  /**
   * Constructs a no op call for the DateTimeLib.isLeapYear(uint256)bool ABI method
   *
   * @dev Returns if the `year` is leap.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static dateTimeLibIsLeapYear(params: CallParams<DateTimeWrapperHelper2Args['obj']['DateTimeLib.isLeapYear(uint256)bool'] | DateTimeWrapperHelper2Args['tuple']['DateTimeLib.isLeapYear(uint256)bool']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'DateTimeLib.isLeapYear(uint256)bool' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.year],
    }
  }
  /**
   * Constructs a no op call for the __init__(uint64,uint64,byte[])void ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static __init(params: CallParams<DateTimeWrapperHelper2Args['obj']['__init__(uint64,uint64,byte[])void'] | DateTimeWrapperHelper2Args['tuple']['__init__(uint64,uint64,byte[])void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: '__init__(uint64,uint64,byte[])void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.o, params.args.p, params.args.s],
    }
  }
}

/**
 * A client to make calls to the DateTimeWrapper__Helper2 smart contract
 */
export class DateTimeWrapperHelper2Client {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `DateTimeWrapperHelper2Client`
   *
   * @param appClient An `AppClient` instance which has been created with the DateTimeWrapperHelper2 app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `DateTimeWrapperHelper2Client`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Checks for decode errors on the given return value and maps the return value to the return type for the given method
   * @returns The typed return value or undefined if there was no value
   */
  decodeReturnValue<TSignature extends DateTimeWrapperHelper2NonVoidMethodSignatures>(method: TSignature, returnValue: ABIReturn | undefined) {
    return returnValue !== undefined ? getArc56ReturnValue<MethodReturn<TSignature>>(returnValue, this.appClient.getABIMethod(method), APP_SPEC.structs) : undefined
  }

  /**
   * Returns a new `DateTimeWrapperHelper2Client` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<DateTimeWrapperHelper2Client> {
    return new DateTimeWrapperHelper2Client(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `DateTimeWrapperHelper2Client` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<DateTimeWrapperHelper2Client> {
    return new DateTimeWrapperHelper2Client(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Makes a clear_state call to an existing instance of the DateTimeWrapper__Helper2 smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the DateTimeWrapper__Helper2 smart contract using the `DateTimeLib.dateToTimestamp(uint256,uint256,uint256)uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
    * @dev Returns the unix timestamp from (`year`,`month`,`day`).
    Note: Inputs outside the supported ranges result in undefined behavior.
    Use {isSupportedDate} to check if the inputs are supported.

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    dateTimeLibDateToTimestamp: (params: CallParams<DateTimeWrapperHelper2Args['obj']['DateTimeLib.dateToTimestamp(uint256,uint256,uint256)uint256'] | DateTimeWrapperHelper2Args['tuple']['DateTimeLib.dateToTimestamp(uint256,uint256,uint256)uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(DateTimeWrapperHelper2ParamsFactory.dateTimeLibDateToTimestamp(params))
    },

    /**
     * Makes a call to the DateTimeWrapper__Helper2 smart contract using the `DateTimeLib.dateToEpochDay(uint256,uint256,uint256)uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
    * @dev Returns the number of days since 1970-01-01 from (`year`,`month`,`day`).
    See: https://howardhinnant.github.io/date_algorithms.html
    Note: Inputs outside the supported ranges result in undefined behavior.
    Use {isSupportedDate} to check if the inputs are supported.

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    dateTimeLibDateToEpochDay: (params: CallParams<DateTimeWrapperHelper2Args['obj']['DateTimeLib.dateToEpochDay(uint256,uint256,uint256)uint256'] | DateTimeWrapperHelper2Args['tuple']['DateTimeLib.dateToEpochDay(uint256,uint256,uint256)uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(DateTimeWrapperHelper2ParamsFactory.dateTimeLibDateToEpochDay(params))
    },

    /**
     * Makes a call to the DateTimeWrapper__Helper2 smart contract using the `DateTimeLib.daysInMonth(uint256,uint256)uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @dev Returns number of days in given `month` of `year`.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    dateTimeLibDaysInMonth: (params: CallParams<DateTimeWrapperHelper2Args['obj']['DateTimeLib.daysInMonth(uint256,uint256)uint256'] | DateTimeWrapperHelper2Args['tuple']['DateTimeLib.daysInMonth(uint256,uint256)uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(DateTimeWrapperHelper2ParamsFactory.dateTimeLibDaysInMonth(params))
    },

    /**
     * Makes a call to the DateTimeWrapper__Helper2 smart contract using the `DateTimeLib.isLeapYear(uint256)bool` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @dev Returns if the `year` is leap.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    dateTimeLibIsLeapYear: (params: CallParams<DateTimeWrapperHelper2Args['obj']['DateTimeLib.isLeapYear(uint256)bool'] | DateTimeWrapperHelper2Args['tuple']['DateTimeLib.isLeapYear(uint256)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(DateTimeWrapperHelper2ParamsFactory.dateTimeLibIsLeapYear(params))
    },

    /**
     * Makes a call to the DateTimeWrapper__Helper2 smart contract using the `__init__(uint64,uint64,byte[])void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    __init: (params: CallParams<DateTimeWrapperHelper2Args['obj']['__init__(uint64,uint64,byte[])void'] | DateTimeWrapperHelper2Args['tuple']['__init__(uint64,uint64,byte[])void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(DateTimeWrapperHelper2ParamsFactory.__init(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Makes a clear_state call to an existing instance of the DateTimeWrapper__Helper2 smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the DateTimeWrapper__Helper2 smart contract using the `DateTimeLib.dateToTimestamp(uint256,uint256,uint256)uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
    * @dev Returns the unix timestamp from (`year`,`month`,`day`).
    Note: Inputs outside the supported ranges result in undefined behavior.
    Use {isSupportedDate} to check if the inputs are supported.

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    dateTimeLibDateToTimestamp: (params: CallParams<DateTimeWrapperHelper2Args['obj']['DateTimeLib.dateToTimestamp(uint256,uint256,uint256)uint256'] | DateTimeWrapperHelper2Args['tuple']['DateTimeLib.dateToTimestamp(uint256,uint256,uint256)uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(DateTimeWrapperHelper2ParamsFactory.dateTimeLibDateToTimestamp(params))
    },

    /**
     * Makes a call to the DateTimeWrapper__Helper2 smart contract using the `DateTimeLib.dateToEpochDay(uint256,uint256,uint256)uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
    * @dev Returns the number of days since 1970-01-01 from (`year`,`month`,`day`).
    See: https://howardhinnant.github.io/date_algorithms.html
    Note: Inputs outside the supported ranges result in undefined behavior.
    Use {isSupportedDate} to check if the inputs are supported.

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    dateTimeLibDateToEpochDay: (params: CallParams<DateTimeWrapperHelper2Args['obj']['DateTimeLib.dateToEpochDay(uint256,uint256,uint256)uint256'] | DateTimeWrapperHelper2Args['tuple']['DateTimeLib.dateToEpochDay(uint256,uint256,uint256)uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(DateTimeWrapperHelper2ParamsFactory.dateTimeLibDateToEpochDay(params))
    },

    /**
     * Makes a call to the DateTimeWrapper__Helper2 smart contract using the `DateTimeLib.daysInMonth(uint256,uint256)uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @dev Returns number of days in given `month` of `year`.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    dateTimeLibDaysInMonth: (params: CallParams<DateTimeWrapperHelper2Args['obj']['DateTimeLib.daysInMonth(uint256,uint256)uint256'] | DateTimeWrapperHelper2Args['tuple']['DateTimeLib.daysInMonth(uint256,uint256)uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(DateTimeWrapperHelper2ParamsFactory.dateTimeLibDaysInMonth(params))
    },

    /**
     * Makes a call to the DateTimeWrapper__Helper2 smart contract using the `DateTimeLib.isLeapYear(uint256)bool` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @dev Returns if the `year` is leap.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    dateTimeLibIsLeapYear: (params: CallParams<DateTimeWrapperHelper2Args['obj']['DateTimeLib.isLeapYear(uint256)bool'] | DateTimeWrapperHelper2Args['tuple']['DateTimeLib.isLeapYear(uint256)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(DateTimeWrapperHelper2ParamsFactory.dateTimeLibIsLeapYear(params))
    },

    /**
     * Makes a call to the DateTimeWrapper__Helper2 smart contract using the `__init__(uint64,uint64,byte[])void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    __init: (params: CallParams<DateTimeWrapperHelper2Args['obj']['__init__(uint64,uint64,byte[])void'] | DateTimeWrapperHelper2Args['tuple']['__init__(uint64,uint64,byte[])void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(DateTimeWrapperHelper2ParamsFactory.__init(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Makes a clear_state call to an existing instance of the DateTimeWrapper__Helper2 smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the DateTimeWrapper__Helper2 smart contract using the `DateTimeLib.dateToTimestamp(uint256,uint256,uint256)uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
    * @dev Returns the unix timestamp from (`year`,`month`,`day`).
    Note: Inputs outside the supported ranges result in undefined behavior.
    Use {isSupportedDate} to check if the inputs are supported.

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    dateTimeLibDateToTimestamp: async (params: CallParams<DateTimeWrapperHelper2Args['obj']['DateTimeLib.dateToTimestamp(uint256,uint256,uint256)uint256'] | DateTimeWrapperHelper2Args['tuple']['DateTimeLib.dateToTimestamp(uint256,uint256,uint256)uint256']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(DateTimeWrapperHelper2ParamsFactory.dateTimeLibDateToTimestamp(params))
      return {...result, return: result.return as unknown as (undefined | DateTimeWrapperHelper2Returns['DateTimeLib.dateToTimestamp(uint256,uint256,uint256)uint256'])}
    },

    /**
     * Makes a call to the DateTimeWrapper__Helper2 smart contract using the `DateTimeLib.dateToEpochDay(uint256,uint256,uint256)uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
    * @dev Returns the number of days since 1970-01-01 from (`year`,`month`,`day`).
    See: https://howardhinnant.github.io/date_algorithms.html
    Note: Inputs outside the supported ranges result in undefined behavior.
    Use {isSupportedDate} to check if the inputs are supported.

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    dateTimeLibDateToEpochDay: async (params: CallParams<DateTimeWrapperHelper2Args['obj']['DateTimeLib.dateToEpochDay(uint256,uint256,uint256)uint256'] | DateTimeWrapperHelper2Args['tuple']['DateTimeLib.dateToEpochDay(uint256,uint256,uint256)uint256']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(DateTimeWrapperHelper2ParamsFactory.dateTimeLibDateToEpochDay(params))
      return {...result, return: result.return as unknown as (undefined | DateTimeWrapperHelper2Returns['DateTimeLib.dateToEpochDay(uint256,uint256,uint256)uint256'])}
    },

    /**
     * Makes a call to the DateTimeWrapper__Helper2 smart contract using the `DateTimeLib.daysInMonth(uint256,uint256)uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @dev Returns number of days in given `month` of `year`.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    dateTimeLibDaysInMonth: async (params: CallParams<DateTimeWrapperHelper2Args['obj']['DateTimeLib.daysInMonth(uint256,uint256)uint256'] | DateTimeWrapperHelper2Args['tuple']['DateTimeLib.daysInMonth(uint256,uint256)uint256']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(DateTimeWrapperHelper2ParamsFactory.dateTimeLibDaysInMonth(params))
      return {...result, return: result.return as unknown as (undefined | DateTimeWrapperHelper2Returns['DateTimeLib.daysInMonth(uint256,uint256)uint256'])}
    },

    /**
     * Makes a call to the DateTimeWrapper__Helper2 smart contract using the `DateTimeLib.isLeapYear(uint256)bool` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @dev Returns if the `year` is leap.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    dateTimeLibIsLeapYear: async (params: CallParams<DateTimeWrapperHelper2Args['obj']['DateTimeLib.isLeapYear(uint256)bool'] | DateTimeWrapperHelper2Args['tuple']['DateTimeLib.isLeapYear(uint256)bool']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(DateTimeWrapperHelper2ParamsFactory.dateTimeLibIsLeapYear(params))
      return {...result, return: result.return as unknown as (undefined | DateTimeWrapperHelper2Returns['DateTimeLib.isLeapYear(uint256)bool'])}
    },

    /**
     * Makes a call to the DateTimeWrapper__Helper2 smart contract using the `__init__(uint64,uint64,byte[])void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    __init: async (params: CallParams<DateTimeWrapperHelper2Args['obj']['__init__(uint64,uint64,byte[])void'] | DateTimeWrapperHelper2Args['tuple']['__init__(uint64,uint64,byte[])void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(DateTimeWrapperHelper2ParamsFactory.__init(params))
      return {...result, return: result.return as unknown as (undefined | DateTimeWrapperHelper2Returns['__init__(uint64,uint64,byte[])void'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new DateTimeWrapperHelper2Client(this.appClient.clone(params))
  }

  /**
   * Makes a readonly (simulated) call to the DateTimeWrapper__Helper2 smart contract using the `DateTimeLib.dateToTimestamp(uint256,uint256,uint256)uint256` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
  * @dev Returns the unix timestamp from (`year`,`month`,`day`).
  Note: Inputs outside the supported ranges result in undefined behavior.
  Use {isSupportedDate} to check if the inputs are supported.

   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async dateTimeLibDateToTimestamp(params: CallParams<DateTimeWrapperHelper2Args['obj']['DateTimeLib.dateToTimestamp(uint256,uint256,uint256)uint256'] | DateTimeWrapperHelper2Args['tuple']['DateTimeLib.dateToTimestamp(uint256,uint256,uint256)uint256']>) {
    const result = await this.appClient.send.call(DateTimeWrapperHelper2ParamsFactory.dateTimeLibDateToTimestamp(params))
    return result.return as unknown as DateTimeWrapperHelper2Returns['DateTimeLib.dateToTimestamp(uint256,uint256,uint256)uint256']
  }

  /**
   * Makes a readonly (simulated) call to the DateTimeWrapper__Helper2 smart contract using the `DateTimeLib.dateToEpochDay(uint256,uint256,uint256)uint256` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
  * @dev Returns the number of days since 1970-01-01 from (`year`,`month`,`day`).
  See: https://howardhinnant.github.io/date_algorithms.html
  Note: Inputs outside the supported ranges result in undefined behavior.
  Use {isSupportedDate} to check if the inputs are supported.

   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async dateTimeLibDateToEpochDay(params: CallParams<DateTimeWrapperHelper2Args['obj']['DateTimeLib.dateToEpochDay(uint256,uint256,uint256)uint256'] | DateTimeWrapperHelper2Args['tuple']['DateTimeLib.dateToEpochDay(uint256,uint256,uint256)uint256']>) {
    const result = await this.appClient.send.call(DateTimeWrapperHelper2ParamsFactory.dateTimeLibDateToEpochDay(params))
    return result.return as unknown as DateTimeWrapperHelper2Returns['DateTimeLib.dateToEpochDay(uint256,uint256,uint256)uint256']
  }

  /**
   * Makes a readonly (simulated) call to the DateTimeWrapper__Helper2 smart contract using the `DateTimeLib.daysInMonth(uint256,uint256)uint256` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @dev Returns number of days in given `month` of `year`.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async dateTimeLibDaysInMonth(params: CallParams<DateTimeWrapperHelper2Args['obj']['DateTimeLib.daysInMonth(uint256,uint256)uint256'] | DateTimeWrapperHelper2Args['tuple']['DateTimeLib.daysInMonth(uint256,uint256)uint256']>) {
    const result = await this.appClient.send.call(DateTimeWrapperHelper2ParamsFactory.dateTimeLibDaysInMonth(params))
    return result.return as unknown as DateTimeWrapperHelper2Returns['DateTimeLib.daysInMonth(uint256,uint256)uint256']
  }

  /**
   * Makes a readonly (simulated) call to the DateTimeWrapper__Helper2 smart contract using the `DateTimeLib.isLeapYear(uint256)bool` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @dev Returns if the `year` is leap.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async dateTimeLibIsLeapYear(params: CallParams<DateTimeWrapperHelper2Args['obj']['DateTimeLib.isLeapYear(uint256)bool'] | DateTimeWrapperHelper2Args['tuple']['DateTimeLib.isLeapYear(uint256)bool']>) {
    const result = await this.appClient.send.call(DateTimeWrapperHelper2ParamsFactory.dateTimeLibIsLeapYear(params))
    return result.return as unknown as DateTimeWrapperHelper2Returns['DateTimeLib.isLeapYear(uint256)bool']
  }

  /**
   * Methods to access state for the current DateTimeWrapper__Helper2 app
   */
  state = {
    /**
     * Methods to access global state for the current DateTimeWrapper__Helper2 app
     */
    global: {
      /**
       * Get all current keyed values from global state
       */
      getAll: async (): Promise<Partial<Expand<GlobalKeysState>>> => {
        const result = await this.appClient.state.global.getAll()
        return {
          o: result.o,
          p: result.p,
          s: new BinaryStateValue(result.s),
        }
      },
      /**
       * Get the current value of the o key in global state
       */
      o: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("o")) as bigint | undefined },
      /**
       * Get the current value of the p key in global state
       */
      p: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("p")) as bigint | undefined },
      /**
       * Get the current value of the s key in global state
       */
      s: async (): Promise<BinaryState> => { return new BinaryStateValue((await this.appClient.state.global.getValue("s")) as Uint8Array | undefined) },
    },
  }

  public newGroup(): DateTimeWrapperHelper2Composer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a DateTimeLib.dateToTimestamp(uint256,uint256,uint256)uint256 method call against the DateTimeWrapper__Helper2 contract
       */
      dateTimeLibDateToTimestamp(params: CallParams<DateTimeWrapperHelper2Args['obj']['DateTimeLib.dateToTimestamp(uint256,uint256,uint256)uint256'] | DateTimeWrapperHelper2Args['tuple']['DateTimeLib.dateToTimestamp(uint256,uint256,uint256)uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.dateTimeLibDateToTimestamp(params)))
        resultMappers.push((v) => client.decodeReturnValue('DateTimeLib.dateToTimestamp(uint256,uint256,uint256)uint256', v))
        return this
      },
      /**
       * Add a DateTimeLib.dateToEpochDay(uint256,uint256,uint256)uint256 method call against the DateTimeWrapper__Helper2 contract
       */
      dateTimeLibDateToEpochDay(params: CallParams<DateTimeWrapperHelper2Args['obj']['DateTimeLib.dateToEpochDay(uint256,uint256,uint256)uint256'] | DateTimeWrapperHelper2Args['tuple']['DateTimeLib.dateToEpochDay(uint256,uint256,uint256)uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.dateTimeLibDateToEpochDay(params)))
        resultMappers.push((v) => client.decodeReturnValue('DateTimeLib.dateToEpochDay(uint256,uint256,uint256)uint256', v))
        return this
      },
      /**
       * Add a DateTimeLib.daysInMonth(uint256,uint256)uint256 method call against the DateTimeWrapper__Helper2 contract
       */
      dateTimeLibDaysInMonth(params: CallParams<DateTimeWrapperHelper2Args['obj']['DateTimeLib.daysInMonth(uint256,uint256)uint256'] | DateTimeWrapperHelper2Args['tuple']['DateTimeLib.daysInMonth(uint256,uint256)uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.dateTimeLibDaysInMonth(params)))
        resultMappers.push((v) => client.decodeReturnValue('DateTimeLib.daysInMonth(uint256,uint256)uint256', v))
        return this
      },
      /**
       * Add a DateTimeLib.isLeapYear(uint256)bool method call against the DateTimeWrapper__Helper2 contract
       */
      dateTimeLibIsLeapYear(params: CallParams<DateTimeWrapperHelper2Args['obj']['DateTimeLib.isLeapYear(uint256)bool'] | DateTimeWrapperHelper2Args['tuple']['DateTimeLib.isLeapYear(uint256)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.dateTimeLibIsLeapYear(params)))
        resultMappers.push((v) => client.decodeReturnValue('DateTimeLib.isLeapYear(uint256)bool', v))
        return this
      },
      /**
       * Add a __init__(uint64,uint64,byte[])void method call against the DateTimeWrapper__Helper2 contract
       */
      __init(params: CallParams<DateTimeWrapperHelper2Args['obj']['__init__(uint64,uint64,byte[])void'] | DateTimeWrapperHelper2Args['tuple']['__init__(uint64,uint64,byte[])void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.__init(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a clear state call to the DateTimeWrapper__Helper2 contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as DateTimeWrapperHelper2Composer
  }
}
export type DateTimeWrapperHelper2Composer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the DateTimeLib.dateToTimestamp(uint256,uint256,uint256)uint256 ABI method.
   *
  * @dev Returns the unix timestamp from (`year`,`month`,`day`).
  Note: Inputs outside the supported ranges result in undefined behavior.
  Use {isSupportedDate} to check if the inputs are supported.

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  dateTimeLibDateToTimestamp(params?: CallParams<DateTimeWrapperHelper2Args['obj']['DateTimeLib.dateToTimestamp(uint256,uint256,uint256)uint256'] | DateTimeWrapperHelper2Args['tuple']['DateTimeLib.dateToTimestamp(uint256,uint256,uint256)uint256']>): DateTimeWrapperHelper2Composer<[...TReturns, DateTimeWrapperHelper2Returns['DateTimeLib.dateToTimestamp(uint256,uint256,uint256)uint256'] | undefined]>

  /**
   * Calls the DateTimeLib.dateToEpochDay(uint256,uint256,uint256)uint256 ABI method.
   *
  * @dev Returns the number of days since 1970-01-01 from (`year`,`month`,`day`).
  See: https://howardhinnant.github.io/date_algorithms.html
  Note: Inputs outside the supported ranges result in undefined behavior.
  Use {isSupportedDate} to check if the inputs are supported.

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  dateTimeLibDateToEpochDay(params?: CallParams<DateTimeWrapperHelper2Args['obj']['DateTimeLib.dateToEpochDay(uint256,uint256,uint256)uint256'] | DateTimeWrapperHelper2Args['tuple']['DateTimeLib.dateToEpochDay(uint256,uint256,uint256)uint256']>): DateTimeWrapperHelper2Composer<[...TReturns, DateTimeWrapperHelper2Returns['DateTimeLib.dateToEpochDay(uint256,uint256,uint256)uint256'] | undefined]>

  /**
   * Calls the DateTimeLib.daysInMonth(uint256,uint256)uint256 ABI method.
   *
   * @dev Returns number of days in given `month` of `year`.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  dateTimeLibDaysInMonth(params?: CallParams<DateTimeWrapperHelper2Args['obj']['DateTimeLib.daysInMonth(uint256,uint256)uint256'] | DateTimeWrapperHelper2Args['tuple']['DateTimeLib.daysInMonth(uint256,uint256)uint256']>): DateTimeWrapperHelper2Composer<[...TReturns, DateTimeWrapperHelper2Returns['DateTimeLib.daysInMonth(uint256,uint256)uint256'] | undefined]>

  /**
   * Calls the DateTimeLib.isLeapYear(uint256)bool ABI method.
   *
   * @dev Returns if the `year` is leap.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  dateTimeLibIsLeapYear(params?: CallParams<DateTimeWrapperHelper2Args['obj']['DateTimeLib.isLeapYear(uint256)bool'] | DateTimeWrapperHelper2Args['tuple']['DateTimeLib.isLeapYear(uint256)bool']>): DateTimeWrapperHelper2Composer<[...TReturns, DateTimeWrapperHelper2Returns['DateTimeLib.isLeapYear(uint256)bool'] | undefined]>

  /**
   * Calls the __init__(uint64,uint64,byte[])void ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  __init(params?: CallParams<DateTimeWrapperHelper2Args['obj']['__init__(uint64,uint64,byte[])void'] | DateTimeWrapperHelper2Args['tuple']['__init__(uint64,uint64,byte[])void']>): DateTimeWrapperHelper2Composer<[...TReturns, DateTimeWrapperHelper2Returns['__init__(uint64,uint64,byte[])void'] | undefined]>

  /**
   * Makes a clear_state call to an existing instance of the DateTimeWrapper__Helper2 smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): DateTimeWrapperHelper2Composer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): DateTimeWrapperHelper2Composer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<DateTimeWrapperHelper2ComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<DateTimeWrapperHelper2ComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<DateTimeWrapperHelper2ComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<DateTimeWrapperHelper2ComposerResults<TReturns>>
}
export type DateTimeWrapperHelper2ComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

