/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"TokenStakingV2","structs":{},"methods":[{"name":"stake","args":[{"type":"uint512","name":"amount"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}},{"name":"unstake","args":[{"type":"uint512","name":"amount"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}},{"name":"claimRewards","args":[],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}},{"name":"setRewardRate","args":[{"type":"uint512","name":"newRate"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}},{"name":"setLockDuration","args":[{"type":"uint512","name":"duration"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}},{"name":"stakedBalance","args":[{"type":"address","name":"staker"}],"returns":{"type":"uint512"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"lockUntil","args":[{"type":"address","name":"staker"}],"returns":{"type":"uint512"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"pendingRewards","args":[{"type":"address","name":"staker"}],"returns":{"type":"uint512"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"totalStaked","args":[],"returns":{"type":"uint512"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"totalRewardsPaid","args":[],"returns":{"type":"uint512"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"rewardRate","args":[],"returns":{"type":"uint512"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"lockDuration","args":[],"returns":{"type":"uint512"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"stakerCount","args":[],"returns":{"type":"uint512"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"owner","args":[],"returns":{"type":"address"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"isLocked","args":[{"type":"address","name":"staker"}],"returns":{"type":"bool"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}}],"arcs":[22,28],"networks":{},"state":{"schema":{"global":{"ints":0,"bytes":6},"local":{"ints":0,"bytes":0}},"keys":{"global":{"_owner":{"keyType":"AVMString","valueType":"address","key":"X293bmVy"},"_totalStaked":{"keyType":"AVMString","valueType":"AVMBytes","key":"X3RvdGFsU3Rha2Vk"},"_rewardRatePerSecond":{"keyType":"AVMString","valueType":"AVMBytes","key":"X3Jld2FyZFJhdGVQZXJTZWNvbmQ="},"_totalRewardsPaid":{"keyType":"AVMString","valueType":"AVMBytes","key":"X3RvdGFsUmV3YXJkc1BhaWQ="},"_stakerCount":{"keyType":"AVMString","valueType":"AVMBytes","key":"X3N0YWtlckNvdW50"},"_lockDuration":{"keyType":"AVMString","valueType":"AVMBytes","key":"X2xvY2tEdXJhdGlvbg=="}},"local":{},"box":{}},"maps":{"global":{},"local":{},"box":{"_stakedBalances":{"keyType":"AVMBytes","valueType":"AVMBytes","prefix":"X3N0YWtlZEJhbGFuY2Vz"},"_lockUntil":{"keyType":"AVMBytes","valueType":"AVMBytes","prefix":"X2xvY2tVbnRpbA=="},"_lastRewardTime":{"keyType":"AVMBytes","valueType":"AVMBytes","prefix":"X2xhc3RSZXdhcmRUaW1l"},"_pendingRewards":{"keyType":"AVMBytes","valueType":"AVMBytes","prefix":"X3BlbmRpbmdSZXdhcmRz"}}}},"bareActions":{"create":[],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[523],"errorMessage":"StakingInsufficientStake"},{"pc":[541],"errorMessage":"StakingLocked"},{"pc":[634],"errorMessage":"StakingNoRewards"},{"pc":[687,726],"errorMessage":"StakingNotOwner"},{"pc":[376,506],"errorMessage":"StakingZeroAmount"},{"pc":[456,960],"errorMessage":"check _lockDuration exists"},{"pc":[684,723,998],"errorMessage":"check _owner exists"},{"pc":[691,869,941,1100],"errorMessage":"check _rewardRatePerSecond exists"},{"pc":[401,592,979],"errorMessage":"check _stakerCount exists"},{"pc":[651,923],"errorMessage":"check _totalRewardsPaid exists"},{"pc":[441,565,904],"errorMessage":"check _totalStaked exists"},{"pc":[740,771,804,1012],"errorMessage":"invalid number of bytes for arc4.static_array<arc4.uint8, 32>"},{"pc":[368,500,677,716],"errorMessage":"invalid number of bytes for arc4.uint512"},{"pc":[754,786,890,909,928,946,965,984],"errorMessage":"overflow"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"events":[]} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


/**
 * The argument types for the TokenStakingV2 contract
 */
export type TokenStakingV2Args = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'stake(uint512)void': {
      amount: bigint | number
    }
    'unstake(uint512)void': {
      amount: bigint | number
    }
    'claimRewards()void': Record<string, never>
    'setRewardRate(uint512)void': {
      newRate: bigint | number
    }
    'setLockDuration(uint512)void': {
      duration: bigint | number
    }
    'stakedBalance(address)uint512': {
      staker: string
    }
    'lockUntil(address)uint512': {
      staker: string
    }
    'pendingRewards(address)uint512': {
      staker: string
    }
    'totalStaked()uint512': Record<string, never>
    'totalRewardsPaid()uint512': Record<string, never>
    'rewardRate()uint512': Record<string, never>
    'lockDuration()uint512': Record<string, never>
    'stakerCount()uint512': Record<string, never>
    'owner()address': Record<string, never>
    'isLocked(address)bool': {
      staker: string
    }
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'stake(uint512)void': [amount: bigint | number]
    'unstake(uint512)void': [amount: bigint | number]
    'claimRewards()void': []
    'setRewardRate(uint512)void': [newRate: bigint | number]
    'setLockDuration(uint512)void': [duration: bigint | number]
    'stakedBalance(address)uint512': [staker: string]
    'lockUntil(address)uint512': [staker: string]
    'pendingRewards(address)uint512': [staker: string]
    'totalStaked()uint512': []
    'totalRewardsPaid()uint512': []
    'rewardRate()uint512': []
    'lockDuration()uint512': []
    'stakerCount()uint512': []
    'owner()address': []
    'isLocked(address)bool': [staker: string]
  }
}

/**
 * The return type for each method
 */
export type TokenStakingV2Returns = {
  'stake(uint512)void': void
  'unstake(uint512)void': void
  'claimRewards()void': void
  'setRewardRate(uint512)void': void
  'setLockDuration(uint512)void': void
  'stakedBalance(address)uint512': bigint
  'lockUntil(address)uint512': bigint
  'pendingRewards(address)uint512': bigint
  'totalStaked()uint512': bigint
  'totalRewardsPaid()uint512': bigint
  'rewardRate()uint512': bigint
  'lockDuration()uint512': bigint
  'stakerCount()uint512': bigint
  'owner()address': string
  'isLocked(address)bool': boolean
}

/**
 * Defines the types of available calls and state of the TokenStakingV2 smart contract.
 */
export type TokenStakingV2Types = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'stake(uint512)void' | 'stake', {
      argsObj: TokenStakingV2Args['obj']['stake(uint512)void']
      argsTuple: TokenStakingV2Args['tuple']['stake(uint512)void']
      returns: TokenStakingV2Returns['stake(uint512)void']
    }>
    & Record<'unstake(uint512)void' | 'unstake', {
      argsObj: TokenStakingV2Args['obj']['unstake(uint512)void']
      argsTuple: TokenStakingV2Args['tuple']['unstake(uint512)void']
      returns: TokenStakingV2Returns['unstake(uint512)void']
    }>
    & Record<'claimRewards()void' | 'claimRewards', {
      argsObj: TokenStakingV2Args['obj']['claimRewards()void']
      argsTuple: TokenStakingV2Args['tuple']['claimRewards()void']
      returns: TokenStakingV2Returns['claimRewards()void']
    }>
    & Record<'setRewardRate(uint512)void' | 'setRewardRate', {
      argsObj: TokenStakingV2Args['obj']['setRewardRate(uint512)void']
      argsTuple: TokenStakingV2Args['tuple']['setRewardRate(uint512)void']
      returns: TokenStakingV2Returns['setRewardRate(uint512)void']
    }>
    & Record<'setLockDuration(uint512)void' | 'setLockDuration', {
      argsObj: TokenStakingV2Args['obj']['setLockDuration(uint512)void']
      argsTuple: TokenStakingV2Args['tuple']['setLockDuration(uint512)void']
      returns: TokenStakingV2Returns['setLockDuration(uint512)void']
    }>
    & Record<'stakedBalance(address)uint512' | 'stakedBalance', {
      argsObj: TokenStakingV2Args['obj']['stakedBalance(address)uint512']
      argsTuple: TokenStakingV2Args['tuple']['stakedBalance(address)uint512']
      returns: TokenStakingV2Returns['stakedBalance(address)uint512']
    }>
    & Record<'lockUntil(address)uint512' | 'lockUntil', {
      argsObj: TokenStakingV2Args['obj']['lockUntil(address)uint512']
      argsTuple: TokenStakingV2Args['tuple']['lockUntil(address)uint512']
      returns: TokenStakingV2Returns['lockUntil(address)uint512']
    }>
    & Record<'pendingRewards(address)uint512' | 'pendingRewards', {
      argsObj: TokenStakingV2Args['obj']['pendingRewards(address)uint512']
      argsTuple: TokenStakingV2Args['tuple']['pendingRewards(address)uint512']
      returns: TokenStakingV2Returns['pendingRewards(address)uint512']
    }>
    & Record<'totalStaked()uint512' | 'totalStaked', {
      argsObj: TokenStakingV2Args['obj']['totalStaked()uint512']
      argsTuple: TokenStakingV2Args['tuple']['totalStaked()uint512']
      returns: TokenStakingV2Returns['totalStaked()uint512']
    }>
    & Record<'totalRewardsPaid()uint512' | 'totalRewardsPaid', {
      argsObj: TokenStakingV2Args['obj']['totalRewardsPaid()uint512']
      argsTuple: TokenStakingV2Args['tuple']['totalRewardsPaid()uint512']
      returns: TokenStakingV2Returns['totalRewardsPaid()uint512']
    }>
    & Record<'rewardRate()uint512' | 'rewardRate', {
      argsObj: TokenStakingV2Args['obj']['rewardRate()uint512']
      argsTuple: TokenStakingV2Args['tuple']['rewardRate()uint512']
      returns: TokenStakingV2Returns['rewardRate()uint512']
    }>
    & Record<'lockDuration()uint512' | 'lockDuration', {
      argsObj: TokenStakingV2Args['obj']['lockDuration()uint512']
      argsTuple: TokenStakingV2Args['tuple']['lockDuration()uint512']
      returns: TokenStakingV2Returns['lockDuration()uint512']
    }>
    & Record<'stakerCount()uint512' | 'stakerCount', {
      argsObj: TokenStakingV2Args['obj']['stakerCount()uint512']
      argsTuple: TokenStakingV2Args['tuple']['stakerCount()uint512']
      returns: TokenStakingV2Returns['stakerCount()uint512']
    }>
    & Record<'owner()address' | 'owner', {
      argsObj: TokenStakingV2Args['obj']['owner()address']
      argsTuple: TokenStakingV2Args['tuple']['owner()address']
      returns: TokenStakingV2Returns['owner()address']
    }>
    & Record<'isLocked(address)bool' | 'isLocked', {
      argsObj: TokenStakingV2Args['obj']['isLocked(address)bool']
      argsTuple: TokenStakingV2Args['tuple']['isLocked(address)bool']
      returns: TokenStakingV2Returns['isLocked(address)bool']
    }>
  /**
   * Defines the shape of the state of the application.
   */
  state: {
    global: {
      keys: {
        _owner: string
        _totalStaked: BinaryState
        _rewardRatePerSecond: BinaryState
        _totalRewardsPaid: BinaryState
        _stakerCount: BinaryState
        _lockDuration: BinaryState
      }
      maps: {}
    }
    box: {
      keys: {}
      maps: {
        _stakedBalances: Map<Uint8Array | string, Uint8Array>
        _lockUntil: Map<Uint8Array | string, Uint8Array>
        _lastRewardTime: Map<Uint8Array | string, Uint8Array>
        _pendingRewards: Map<Uint8Array | string, Uint8Array>
      }
    }
  }
}

/**
 * Defines the possible abi call signatures.
 */
export type TokenStakingV2Signatures = keyof TokenStakingV2Types['methods']
/**
 * Defines the possible abi call signatures for methods that return a non-void value.
 */
export type TokenStakingV2NonVoidMethodSignatures = keyof TokenStakingV2Types['methods'] extends infer T ? T extends keyof TokenStakingV2Types['methods'] ? MethodReturn<T> extends void ? never : T  : never : never
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the TokenStakingV2 smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends TokenStakingV2Signatures> = TokenStakingV2Types['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the TokenStakingV2 smart contract to the method's return type
 */
export type MethodReturn<TSignature extends TokenStakingV2Signatures> = TokenStakingV2Types['methods'][TSignature]['returns']

/**
 * Defines the shape of the keyed global state of the application.
 */
export type GlobalKeysState = TokenStakingV2Types['state']['global']['keys']

/**
 * Defines the shape of the keyed box state of the application.
 */
export type BoxKeysState = TokenStakingV2Types['state']['box']['keys']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the TokenStakingV2 smart contract
 */
export abstract class TokenStakingV2ParamsFactory {
  /**
   * Constructs a no op call for the stake(uint512)void ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static stake(params: CallParams<TokenStakingV2Args['obj']['stake(uint512)void'] | TokenStakingV2Args['tuple']['stake(uint512)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'stake(uint512)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.amount],
    }
  }
  /**
   * Constructs a no op call for the unstake(uint512)void ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static unstake(params: CallParams<TokenStakingV2Args['obj']['unstake(uint512)void'] | TokenStakingV2Args['tuple']['unstake(uint512)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'unstake(uint512)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.amount],
    }
  }
  /**
   * Constructs a no op call for the claimRewards()void ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static claimRewards(params: CallParams<TokenStakingV2Args['obj']['claimRewards()void'] | TokenStakingV2Args['tuple']['claimRewards()void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'claimRewards()void' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the setRewardRate(uint512)void ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static setRewardRate(params: CallParams<TokenStakingV2Args['obj']['setRewardRate(uint512)void'] | TokenStakingV2Args['tuple']['setRewardRate(uint512)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'setRewardRate(uint512)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.newRate],
    }
  }
  /**
   * Constructs a no op call for the setLockDuration(uint512)void ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static setLockDuration(params: CallParams<TokenStakingV2Args['obj']['setLockDuration(uint512)void'] | TokenStakingV2Args['tuple']['setLockDuration(uint512)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'setLockDuration(uint512)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.duration],
    }
  }
  /**
   * Constructs a no op call for the stakedBalance(address)uint512 ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static stakedBalance(params: CallParams<TokenStakingV2Args['obj']['stakedBalance(address)uint512'] | TokenStakingV2Args['tuple']['stakedBalance(address)uint512']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'stakedBalance(address)uint512' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.staker],
    }
  }
  /**
   * Constructs a no op call for the lockUntil(address)uint512 ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static lockUntil(params: CallParams<TokenStakingV2Args['obj']['lockUntil(address)uint512'] | TokenStakingV2Args['tuple']['lockUntil(address)uint512']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'lockUntil(address)uint512' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.staker],
    }
  }
  /**
   * Constructs a no op call for the pendingRewards(address)uint512 ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static pendingRewards(params: CallParams<TokenStakingV2Args['obj']['pendingRewards(address)uint512'] | TokenStakingV2Args['tuple']['pendingRewards(address)uint512']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'pendingRewards(address)uint512' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.staker],
    }
  }
  /**
   * Constructs a no op call for the totalStaked()uint512 ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static totalStaked(params: CallParams<TokenStakingV2Args['obj']['totalStaked()uint512'] | TokenStakingV2Args['tuple']['totalStaked()uint512']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'totalStaked()uint512' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the totalRewardsPaid()uint512 ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static totalRewardsPaid(params: CallParams<TokenStakingV2Args['obj']['totalRewardsPaid()uint512'] | TokenStakingV2Args['tuple']['totalRewardsPaid()uint512']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'totalRewardsPaid()uint512' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the rewardRate()uint512 ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static rewardRate(params: CallParams<TokenStakingV2Args['obj']['rewardRate()uint512'] | TokenStakingV2Args['tuple']['rewardRate()uint512']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'rewardRate()uint512' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the lockDuration()uint512 ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static lockDuration(params: CallParams<TokenStakingV2Args['obj']['lockDuration()uint512'] | TokenStakingV2Args['tuple']['lockDuration()uint512']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'lockDuration()uint512' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the stakerCount()uint512 ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static stakerCount(params: CallParams<TokenStakingV2Args['obj']['stakerCount()uint512'] | TokenStakingV2Args['tuple']['stakerCount()uint512']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'stakerCount()uint512' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the owner()address ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static owner(params: CallParams<TokenStakingV2Args['obj']['owner()address'] | TokenStakingV2Args['tuple']['owner()address']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'owner()address' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the isLocked(address)bool ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static isLocked(params: CallParams<TokenStakingV2Args['obj']['isLocked(address)bool'] | TokenStakingV2Args['tuple']['isLocked(address)bool']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'isLocked(address)bool' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.staker],
    }
  }
}

/**
 * A client to make calls to the TokenStakingV2 smart contract
 */
export class TokenStakingV2Client {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `TokenStakingV2Client`
   *
   * @param appClient An `AppClient` instance which has been created with the TokenStakingV2 app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `TokenStakingV2Client`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Checks for decode errors on the given return value and maps the return value to the return type for the given method
   * @returns The typed return value or undefined if there was no value
   */
  decodeReturnValue<TSignature extends TokenStakingV2NonVoidMethodSignatures>(method: TSignature, returnValue: ABIReturn | undefined) {
    return returnValue !== undefined ? getArc56ReturnValue<MethodReturn<TSignature>>(returnValue, this.appClient.getABIMethod(method), APP_SPEC.structs) : undefined
  }

  /**
   * Returns a new `TokenStakingV2Client` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<TokenStakingV2Client> {
    return new TokenStakingV2Client(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `TokenStakingV2Client` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<TokenStakingV2Client> {
    return new TokenStakingV2Client(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Makes a clear_state call to an existing instance of the TokenStakingV2 smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the TokenStakingV2 smart contract using the `stake(uint512)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    stake: (params: CallParams<TokenStakingV2Args['obj']['stake(uint512)void'] | TokenStakingV2Args['tuple']['stake(uint512)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(TokenStakingV2ParamsFactory.stake(params))
    },

    /**
     * Makes a call to the TokenStakingV2 smart contract using the `unstake(uint512)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    unstake: (params: CallParams<TokenStakingV2Args['obj']['unstake(uint512)void'] | TokenStakingV2Args['tuple']['unstake(uint512)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(TokenStakingV2ParamsFactory.unstake(params))
    },

    /**
     * Makes a call to the TokenStakingV2 smart contract using the `claimRewards()void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    claimRewards: (params: CallParams<TokenStakingV2Args['obj']['claimRewards()void'] | TokenStakingV2Args['tuple']['claimRewards()void']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(TokenStakingV2ParamsFactory.claimRewards(params))
    },

    /**
     * Makes a call to the TokenStakingV2 smart contract using the `setRewardRate(uint512)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    setRewardRate: (params: CallParams<TokenStakingV2Args['obj']['setRewardRate(uint512)void'] | TokenStakingV2Args['tuple']['setRewardRate(uint512)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(TokenStakingV2ParamsFactory.setRewardRate(params))
    },

    /**
     * Makes a call to the TokenStakingV2 smart contract using the `setLockDuration(uint512)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    setLockDuration: (params: CallParams<TokenStakingV2Args['obj']['setLockDuration(uint512)void'] | TokenStakingV2Args['tuple']['setLockDuration(uint512)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(TokenStakingV2ParamsFactory.setLockDuration(params))
    },

    /**
     * Makes a call to the TokenStakingV2 smart contract using the `stakedBalance(address)uint512` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    stakedBalance: (params: CallParams<TokenStakingV2Args['obj']['stakedBalance(address)uint512'] | TokenStakingV2Args['tuple']['stakedBalance(address)uint512']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(TokenStakingV2ParamsFactory.stakedBalance(params))
    },

    /**
     * Makes a call to the TokenStakingV2 smart contract using the `lockUntil(address)uint512` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    lockUntil: (params: CallParams<TokenStakingV2Args['obj']['lockUntil(address)uint512'] | TokenStakingV2Args['tuple']['lockUntil(address)uint512']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(TokenStakingV2ParamsFactory.lockUntil(params))
    },

    /**
     * Makes a call to the TokenStakingV2 smart contract using the `pendingRewards(address)uint512` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    pendingRewards: (params: CallParams<TokenStakingV2Args['obj']['pendingRewards(address)uint512'] | TokenStakingV2Args['tuple']['pendingRewards(address)uint512']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(TokenStakingV2ParamsFactory.pendingRewards(params))
    },

    /**
     * Makes a call to the TokenStakingV2 smart contract using the `totalStaked()uint512` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    totalStaked: (params: CallParams<TokenStakingV2Args['obj']['totalStaked()uint512'] | TokenStakingV2Args['tuple']['totalStaked()uint512']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(TokenStakingV2ParamsFactory.totalStaked(params))
    },

    /**
     * Makes a call to the TokenStakingV2 smart contract using the `totalRewardsPaid()uint512` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    totalRewardsPaid: (params: CallParams<TokenStakingV2Args['obj']['totalRewardsPaid()uint512'] | TokenStakingV2Args['tuple']['totalRewardsPaid()uint512']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(TokenStakingV2ParamsFactory.totalRewardsPaid(params))
    },

    /**
     * Makes a call to the TokenStakingV2 smart contract using the `rewardRate()uint512` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    rewardRate: (params: CallParams<TokenStakingV2Args['obj']['rewardRate()uint512'] | TokenStakingV2Args['tuple']['rewardRate()uint512']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(TokenStakingV2ParamsFactory.rewardRate(params))
    },

    /**
     * Makes a call to the TokenStakingV2 smart contract using the `lockDuration()uint512` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    lockDuration: (params: CallParams<TokenStakingV2Args['obj']['lockDuration()uint512'] | TokenStakingV2Args['tuple']['lockDuration()uint512']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(TokenStakingV2ParamsFactory.lockDuration(params))
    },

    /**
     * Makes a call to the TokenStakingV2 smart contract using the `stakerCount()uint512` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    stakerCount: (params: CallParams<TokenStakingV2Args['obj']['stakerCount()uint512'] | TokenStakingV2Args['tuple']['stakerCount()uint512']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(TokenStakingV2ParamsFactory.stakerCount(params))
    },

    /**
     * Makes a call to the TokenStakingV2 smart contract using the `owner()address` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    owner: (params: CallParams<TokenStakingV2Args['obj']['owner()address'] | TokenStakingV2Args['tuple']['owner()address']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(TokenStakingV2ParamsFactory.owner(params))
    },

    /**
     * Makes a call to the TokenStakingV2 smart contract using the `isLocked(address)bool` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    isLocked: (params: CallParams<TokenStakingV2Args['obj']['isLocked(address)bool'] | TokenStakingV2Args['tuple']['isLocked(address)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(TokenStakingV2ParamsFactory.isLocked(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Makes a clear_state call to an existing instance of the TokenStakingV2 smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the TokenStakingV2 smart contract using the `stake(uint512)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    stake: (params: CallParams<TokenStakingV2Args['obj']['stake(uint512)void'] | TokenStakingV2Args['tuple']['stake(uint512)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(TokenStakingV2ParamsFactory.stake(params))
    },

    /**
     * Makes a call to the TokenStakingV2 smart contract using the `unstake(uint512)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    unstake: (params: CallParams<TokenStakingV2Args['obj']['unstake(uint512)void'] | TokenStakingV2Args['tuple']['unstake(uint512)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(TokenStakingV2ParamsFactory.unstake(params))
    },

    /**
     * Makes a call to the TokenStakingV2 smart contract using the `claimRewards()void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    claimRewards: (params: CallParams<TokenStakingV2Args['obj']['claimRewards()void'] | TokenStakingV2Args['tuple']['claimRewards()void']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(TokenStakingV2ParamsFactory.claimRewards(params))
    },

    /**
     * Makes a call to the TokenStakingV2 smart contract using the `setRewardRate(uint512)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    setRewardRate: (params: CallParams<TokenStakingV2Args['obj']['setRewardRate(uint512)void'] | TokenStakingV2Args['tuple']['setRewardRate(uint512)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(TokenStakingV2ParamsFactory.setRewardRate(params))
    },

    /**
     * Makes a call to the TokenStakingV2 smart contract using the `setLockDuration(uint512)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    setLockDuration: (params: CallParams<TokenStakingV2Args['obj']['setLockDuration(uint512)void'] | TokenStakingV2Args['tuple']['setLockDuration(uint512)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(TokenStakingV2ParamsFactory.setLockDuration(params))
    },

    /**
     * Makes a call to the TokenStakingV2 smart contract using the `stakedBalance(address)uint512` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    stakedBalance: (params: CallParams<TokenStakingV2Args['obj']['stakedBalance(address)uint512'] | TokenStakingV2Args['tuple']['stakedBalance(address)uint512']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(TokenStakingV2ParamsFactory.stakedBalance(params))
    },

    /**
     * Makes a call to the TokenStakingV2 smart contract using the `lockUntil(address)uint512` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    lockUntil: (params: CallParams<TokenStakingV2Args['obj']['lockUntil(address)uint512'] | TokenStakingV2Args['tuple']['lockUntil(address)uint512']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(TokenStakingV2ParamsFactory.lockUntil(params))
    },

    /**
     * Makes a call to the TokenStakingV2 smart contract using the `pendingRewards(address)uint512` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    pendingRewards: (params: CallParams<TokenStakingV2Args['obj']['pendingRewards(address)uint512'] | TokenStakingV2Args['tuple']['pendingRewards(address)uint512']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(TokenStakingV2ParamsFactory.pendingRewards(params))
    },

    /**
     * Makes a call to the TokenStakingV2 smart contract using the `totalStaked()uint512` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    totalStaked: (params: CallParams<TokenStakingV2Args['obj']['totalStaked()uint512'] | TokenStakingV2Args['tuple']['totalStaked()uint512']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(TokenStakingV2ParamsFactory.totalStaked(params))
    },

    /**
     * Makes a call to the TokenStakingV2 smart contract using the `totalRewardsPaid()uint512` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    totalRewardsPaid: (params: CallParams<TokenStakingV2Args['obj']['totalRewardsPaid()uint512'] | TokenStakingV2Args['tuple']['totalRewardsPaid()uint512']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(TokenStakingV2ParamsFactory.totalRewardsPaid(params))
    },

    /**
     * Makes a call to the TokenStakingV2 smart contract using the `rewardRate()uint512` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    rewardRate: (params: CallParams<TokenStakingV2Args['obj']['rewardRate()uint512'] | TokenStakingV2Args['tuple']['rewardRate()uint512']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(TokenStakingV2ParamsFactory.rewardRate(params))
    },

    /**
     * Makes a call to the TokenStakingV2 smart contract using the `lockDuration()uint512` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    lockDuration: (params: CallParams<TokenStakingV2Args['obj']['lockDuration()uint512'] | TokenStakingV2Args['tuple']['lockDuration()uint512']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(TokenStakingV2ParamsFactory.lockDuration(params))
    },

    /**
     * Makes a call to the TokenStakingV2 smart contract using the `stakerCount()uint512` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    stakerCount: (params: CallParams<TokenStakingV2Args['obj']['stakerCount()uint512'] | TokenStakingV2Args['tuple']['stakerCount()uint512']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(TokenStakingV2ParamsFactory.stakerCount(params))
    },

    /**
     * Makes a call to the TokenStakingV2 smart contract using the `owner()address` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    owner: (params: CallParams<TokenStakingV2Args['obj']['owner()address'] | TokenStakingV2Args['tuple']['owner()address']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(TokenStakingV2ParamsFactory.owner(params))
    },

    /**
     * Makes a call to the TokenStakingV2 smart contract using the `isLocked(address)bool` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    isLocked: (params: CallParams<TokenStakingV2Args['obj']['isLocked(address)bool'] | TokenStakingV2Args['tuple']['isLocked(address)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(TokenStakingV2ParamsFactory.isLocked(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Makes a clear_state call to an existing instance of the TokenStakingV2 smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the TokenStakingV2 smart contract using the `stake(uint512)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    stake: async (params: CallParams<TokenStakingV2Args['obj']['stake(uint512)void'] | TokenStakingV2Args['tuple']['stake(uint512)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(TokenStakingV2ParamsFactory.stake(params))
      return {...result, return: result.return as unknown as (undefined | TokenStakingV2Returns['stake(uint512)void'])}
    },

    /**
     * Makes a call to the TokenStakingV2 smart contract using the `unstake(uint512)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    unstake: async (params: CallParams<TokenStakingV2Args['obj']['unstake(uint512)void'] | TokenStakingV2Args['tuple']['unstake(uint512)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(TokenStakingV2ParamsFactory.unstake(params))
      return {...result, return: result.return as unknown as (undefined | TokenStakingV2Returns['unstake(uint512)void'])}
    },

    /**
     * Makes a call to the TokenStakingV2 smart contract using the `claimRewards()void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    claimRewards: async (params: CallParams<TokenStakingV2Args['obj']['claimRewards()void'] | TokenStakingV2Args['tuple']['claimRewards()void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(TokenStakingV2ParamsFactory.claimRewards(params))
      return {...result, return: result.return as unknown as (undefined | TokenStakingV2Returns['claimRewards()void'])}
    },

    /**
     * Makes a call to the TokenStakingV2 smart contract using the `setRewardRate(uint512)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    setRewardRate: async (params: CallParams<TokenStakingV2Args['obj']['setRewardRate(uint512)void'] | TokenStakingV2Args['tuple']['setRewardRate(uint512)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(TokenStakingV2ParamsFactory.setRewardRate(params))
      return {...result, return: result.return as unknown as (undefined | TokenStakingV2Returns['setRewardRate(uint512)void'])}
    },

    /**
     * Makes a call to the TokenStakingV2 smart contract using the `setLockDuration(uint512)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    setLockDuration: async (params: CallParams<TokenStakingV2Args['obj']['setLockDuration(uint512)void'] | TokenStakingV2Args['tuple']['setLockDuration(uint512)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(TokenStakingV2ParamsFactory.setLockDuration(params))
      return {...result, return: result.return as unknown as (undefined | TokenStakingV2Returns['setLockDuration(uint512)void'])}
    },

    /**
     * Makes a call to the TokenStakingV2 smart contract using the `stakedBalance(address)uint512` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    stakedBalance: async (params: CallParams<TokenStakingV2Args['obj']['stakedBalance(address)uint512'] | TokenStakingV2Args['tuple']['stakedBalance(address)uint512']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(TokenStakingV2ParamsFactory.stakedBalance(params))
      return {...result, return: result.return as unknown as (undefined | TokenStakingV2Returns['stakedBalance(address)uint512'])}
    },

    /**
     * Makes a call to the TokenStakingV2 smart contract using the `lockUntil(address)uint512` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    lockUntil: async (params: CallParams<TokenStakingV2Args['obj']['lockUntil(address)uint512'] | TokenStakingV2Args['tuple']['lockUntil(address)uint512']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(TokenStakingV2ParamsFactory.lockUntil(params))
      return {...result, return: result.return as unknown as (undefined | TokenStakingV2Returns['lockUntil(address)uint512'])}
    },

    /**
     * Makes a call to the TokenStakingV2 smart contract using the `pendingRewards(address)uint512` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    pendingRewards: async (params: CallParams<TokenStakingV2Args['obj']['pendingRewards(address)uint512'] | TokenStakingV2Args['tuple']['pendingRewards(address)uint512']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(TokenStakingV2ParamsFactory.pendingRewards(params))
      return {...result, return: result.return as unknown as (undefined | TokenStakingV2Returns['pendingRewards(address)uint512'])}
    },

    /**
     * Makes a call to the TokenStakingV2 smart contract using the `totalStaked()uint512` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    totalStaked: async (params: CallParams<TokenStakingV2Args['obj']['totalStaked()uint512'] | TokenStakingV2Args['tuple']['totalStaked()uint512']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(TokenStakingV2ParamsFactory.totalStaked(params))
      return {...result, return: result.return as unknown as (undefined | TokenStakingV2Returns['totalStaked()uint512'])}
    },

    /**
     * Makes a call to the TokenStakingV2 smart contract using the `totalRewardsPaid()uint512` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    totalRewardsPaid: async (params: CallParams<TokenStakingV2Args['obj']['totalRewardsPaid()uint512'] | TokenStakingV2Args['tuple']['totalRewardsPaid()uint512']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(TokenStakingV2ParamsFactory.totalRewardsPaid(params))
      return {...result, return: result.return as unknown as (undefined | TokenStakingV2Returns['totalRewardsPaid()uint512'])}
    },

    /**
     * Makes a call to the TokenStakingV2 smart contract using the `rewardRate()uint512` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    rewardRate: async (params: CallParams<TokenStakingV2Args['obj']['rewardRate()uint512'] | TokenStakingV2Args['tuple']['rewardRate()uint512']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(TokenStakingV2ParamsFactory.rewardRate(params))
      return {...result, return: result.return as unknown as (undefined | TokenStakingV2Returns['rewardRate()uint512'])}
    },

    /**
     * Makes a call to the TokenStakingV2 smart contract using the `lockDuration()uint512` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    lockDuration: async (params: CallParams<TokenStakingV2Args['obj']['lockDuration()uint512'] | TokenStakingV2Args['tuple']['lockDuration()uint512']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(TokenStakingV2ParamsFactory.lockDuration(params))
      return {...result, return: result.return as unknown as (undefined | TokenStakingV2Returns['lockDuration()uint512'])}
    },

    /**
     * Makes a call to the TokenStakingV2 smart contract using the `stakerCount()uint512` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    stakerCount: async (params: CallParams<TokenStakingV2Args['obj']['stakerCount()uint512'] | TokenStakingV2Args['tuple']['stakerCount()uint512']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(TokenStakingV2ParamsFactory.stakerCount(params))
      return {...result, return: result.return as unknown as (undefined | TokenStakingV2Returns['stakerCount()uint512'])}
    },

    /**
     * Makes a call to the TokenStakingV2 smart contract using the `owner()address` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    owner: async (params: CallParams<TokenStakingV2Args['obj']['owner()address'] | TokenStakingV2Args['tuple']['owner()address']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(TokenStakingV2ParamsFactory.owner(params))
      return {...result, return: result.return as unknown as (undefined | TokenStakingV2Returns['owner()address'])}
    },

    /**
     * Makes a call to the TokenStakingV2 smart contract using the `isLocked(address)bool` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    isLocked: async (params: CallParams<TokenStakingV2Args['obj']['isLocked(address)bool'] | TokenStakingV2Args['tuple']['isLocked(address)bool']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(TokenStakingV2ParamsFactory.isLocked(params))
      return {...result, return: result.return as unknown as (undefined | TokenStakingV2Returns['isLocked(address)bool'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new TokenStakingV2Client(this.appClient.clone(params))
  }

  /**
   * Makes a readonly (simulated) call to the TokenStakingV2 smart contract using the `stakedBalance(address)uint512` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async stakedBalance(params: CallParams<TokenStakingV2Args['obj']['stakedBalance(address)uint512'] | TokenStakingV2Args['tuple']['stakedBalance(address)uint512']>) {
    const result = await this.appClient.send.call(TokenStakingV2ParamsFactory.stakedBalance(params))
    return result.return as unknown as TokenStakingV2Returns['stakedBalance(address)uint512']
  }

  /**
   * Makes a readonly (simulated) call to the TokenStakingV2 smart contract using the `lockUntil(address)uint512` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async lockUntil(params: CallParams<TokenStakingV2Args['obj']['lockUntil(address)uint512'] | TokenStakingV2Args['tuple']['lockUntil(address)uint512']>) {
    const result = await this.appClient.send.call(TokenStakingV2ParamsFactory.lockUntil(params))
    return result.return as unknown as TokenStakingV2Returns['lockUntil(address)uint512']
  }

  /**
   * Makes a readonly (simulated) call to the TokenStakingV2 smart contract using the `pendingRewards(address)uint512` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async pendingRewards(params: CallParams<TokenStakingV2Args['obj']['pendingRewards(address)uint512'] | TokenStakingV2Args['tuple']['pendingRewards(address)uint512']>) {
    const result = await this.appClient.send.call(TokenStakingV2ParamsFactory.pendingRewards(params))
    return result.return as unknown as TokenStakingV2Returns['pendingRewards(address)uint512']
  }

  /**
   * Makes a readonly (simulated) call to the TokenStakingV2 smart contract using the `totalStaked()uint512` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async totalStaked(params: CallParams<TokenStakingV2Args['obj']['totalStaked()uint512'] | TokenStakingV2Args['tuple']['totalStaked()uint512']> = {args: []}) {
    const result = await this.appClient.send.call(TokenStakingV2ParamsFactory.totalStaked(params))
    return result.return as unknown as TokenStakingV2Returns['totalStaked()uint512']
  }

  /**
   * Makes a readonly (simulated) call to the TokenStakingV2 smart contract using the `totalRewardsPaid()uint512` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async totalRewardsPaid(params: CallParams<TokenStakingV2Args['obj']['totalRewardsPaid()uint512'] | TokenStakingV2Args['tuple']['totalRewardsPaid()uint512']> = {args: []}) {
    const result = await this.appClient.send.call(TokenStakingV2ParamsFactory.totalRewardsPaid(params))
    return result.return as unknown as TokenStakingV2Returns['totalRewardsPaid()uint512']
  }

  /**
   * Makes a readonly (simulated) call to the TokenStakingV2 smart contract using the `rewardRate()uint512` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async rewardRate(params: CallParams<TokenStakingV2Args['obj']['rewardRate()uint512'] | TokenStakingV2Args['tuple']['rewardRate()uint512']> = {args: []}) {
    const result = await this.appClient.send.call(TokenStakingV2ParamsFactory.rewardRate(params))
    return result.return as unknown as TokenStakingV2Returns['rewardRate()uint512']
  }

  /**
   * Makes a readonly (simulated) call to the TokenStakingV2 smart contract using the `lockDuration()uint512` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async lockDuration(params: CallParams<TokenStakingV2Args['obj']['lockDuration()uint512'] | TokenStakingV2Args['tuple']['lockDuration()uint512']> = {args: []}) {
    const result = await this.appClient.send.call(TokenStakingV2ParamsFactory.lockDuration(params))
    return result.return as unknown as TokenStakingV2Returns['lockDuration()uint512']
  }

  /**
   * Makes a readonly (simulated) call to the TokenStakingV2 smart contract using the `stakerCount()uint512` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async stakerCount(params: CallParams<TokenStakingV2Args['obj']['stakerCount()uint512'] | TokenStakingV2Args['tuple']['stakerCount()uint512']> = {args: []}) {
    const result = await this.appClient.send.call(TokenStakingV2ParamsFactory.stakerCount(params))
    return result.return as unknown as TokenStakingV2Returns['stakerCount()uint512']
  }

  /**
   * Makes a readonly (simulated) call to the TokenStakingV2 smart contract using the `owner()address` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async owner(params: CallParams<TokenStakingV2Args['obj']['owner()address'] | TokenStakingV2Args['tuple']['owner()address']> = {args: []}) {
    const result = await this.appClient.send.call(TokenStakingV2ParamsFactory.owner(params))
    return result.return as unknown as TokenStakingV2Returns['owner()address']
  }

  /**
   * Makes a readonly (simulated) call to the TokenStakingV2 smart contract using the `isLocked(address)bool` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async isLocked(params: CallParams<TokenStakingV2Args['obj']['isLocked(address)bool'] | TokenStakingV2Args['tuple']['isLocked(address)bool']>) {
    const result = await this.appClient.send.call(TokenStakingV2ParamsFactory.isLocked(params))
    return result.return as unknown as TokenStakingV2Returns['isLocked(address)bool']
  }

  /**
   * Methods to access state for the current TokenStakingV2 app
   */
  state = {
    /**
     * Methods to access global state for the current TokenStakingV2 app
     */
    global: {
      /**
       * Get all current keyed values from global state
       */
      getAll: async (): Promise<Partial<Expand<GlobalKeysState>>> => {
        const result = await this.appClient.state.global.getAll()
        return {
          _owner: result._owner,
          _totalStaked: new BinaryStateValue(result._totalStaked),
          _rewardRatePerSecond: new BinaryStateValue(result._rewardRatePerSecond),
          _totalRewardsPaid: new BinaryStateValue(result._totalRewardsPaid),
          _stakerCount: new BinaryStateValue(result._stakerCount),
          _lockDuration: new BinaryStateValue(result._lockDuration),
        }
      },
      /**
       * Get the current value of the _owner key in global state
       */
      _owner: async (): Promise<string | undefined> => { return (await this.appClient.state.global.getValue("_owner")) as string | undefined },
      /**
       * Get the current value of the _totalStaked key in global state
       */
      _totalStaked: async (): Promise<BinaryState> => { return new BinaryStateValue((await this.appClient.state.global.getValue("_totalStaked")) as Uint8Array | undefined) },
      /**
       * Get the current value of the _rewardRatePerSecond key in global state
       */
      _rewardRatePerSecond: async (): Promise<BinaryState> => { return new BinaryStateValue((await this.appClient.state.global.getValue("_rewardRatePerSecond")) as Uint8Array | undefined) },
      /**
       * Get the current value of the _totalRewardsPaid key in global state
       */
      _totalRewardsPaid: async (): Promise<BinaryState> => { return new BinaryStateValue((await this.appClient.state.global.getValue("_totalRewardsPaid")) as Uint8Array | undefined) },
      /**
       * Get the current value of the _stakerCount key in global state
       */
      _stakerCount: async (): Promise<BinaryState> => { return new BinaryStateValue((await this.appClient.state.global.getValue("_stakerCount")) as Uint8Array | undefined) },
      /**
       * Get the current value of the _lockDuration key in global state
       */
      _lockDuration: async (): Promise<BinaryState> => { return new BinaryStateValue((await this.appClient.state.global.getValue("_lockDuration")) as Uint8Array | undefined) },
    },
    /**
     * Methods to access box state for the current TokenStakingV2 app
     */
    box: {
      /**
       * Get all current keyed values from box state
       */
      getAll: async (): Promise<Partial<Expand<BoxKeysState>>> => {
        const result = await this.appClient.state.box.getAll()
        return {
        }
      },
      /**
       * Get values from the _stakedBalances map in box state
       */
      _stakedBalances: {
        /**
         * Get all current values of the _stakedBalances map in box state
         */
        getMap: async (): Promise<Map<Uint8Array, Uint8Array>> => { return (await this.appClient.state.box.getMap("_stakedBalances")) as Map<Uint8Array, Uint8Array> },
        /**
         * Get a current value of the _stakedBalances map by key from box state
         */
        value: async (key: Uint8Array | string): Promise<Uint8Array | undefined> => { return await this.appClient.state.box.getMapValue("_stakedBalances", key) as Uint8Array | undefined },
      },
      /**
       * Get values from the _lockUntil map in box state
       */
      _lockUntil: {
        /**
         * Get all current values of the _lockUntil map in box state
         */
        getMap: async (): Promise<Map<Uint8Array, Uint8Array>> => { return (await this.appClient.state.box.getMap("_lockUntil")) as Map<Uint8Array, Uint8Array> },
        /**
         * Get a current value of the _lockUntil map by key from box state
         */
        value: async (key: Uint8Array | string): Promise<Uint8Array | undefined> => { return await this.appClient.state.box.getMapValue("_lockUntil", key) as Uint8Array | undefined },
      },
      /**
       * Get values from the _lastRewardTime map in box state
       */
      _lastRewardTime: {
        /**
         * Get all current values of the _lastRewardTime map in box state
         */
        getMap: async (): Promise<Map<Uint8Array, Uint8Array>> => { return (await this.appClient.state.box.getMap("_lastRewardTime")) as Map<Uint8Array, Uint8Array> },
        /**
         * Get a current value of the _lastRewardTime map by key from box state
         */
        value: async (key: Uint8Array | string): Promise<Uint8Array | undefined> => { return await this.appClient.state.box.getMapValue("_lastRewardTime", key) as Uint8Array | undefined },
      },
      /**
       * Get values from the _pendingRewards map in box state
       */
      _pendingRewards: {
        /**
         * Get all current values of the _pendingRewards map in box state
         */
        getMap: async (): Promise<Map<Uint8Array, Uint8Array>> => { return (await this.appClient.state.box.getMap("_pendingRewards")) as Map<Uint8Array, Uint8Array> },
        /**
         * Get a current value of the _pendingRewards map by key from box state
         */
        value: async (key: Uint8Array | string): Promise<Uint8Array | undefined> => { return await this.appClient.state.box.getMapValue("_pendingRewards", key) as Uint8Array | undefined },
      },
    },
  }

  public newGroup(): TokenStakingV2Composer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a stake(uint512)void method call against the TokenStakingV2 contract
       */
      stake(params: CallParams<TokenStakingV2Args['obj']['stake(uint512)void'] | TokenStakingV2Args['tuple']['stake(uint512)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.stake(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a unstake(uint512)void method call against the TokenStakingV2 contract
       */
      unstake(params: CallParams<TokenStakingV2Args['obj']['unstake(uint512)void'] | TokenStakingV2Args['tuple']['unstake(uint512)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.unstake(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a claimRewards()void method call against the TokenStakingV2 contract
       */
      claimRewards(params: CallParams<TokenStakingV2Args['obj']['claimRewards()void'] | TokenStakingV2Args['tuple']['claimRewards()void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.claimRewards(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a setRewardRate(uint512)void method call against the TokenStakingV2 contract
       */
      setRewardRate(params: CallParams<TokenStakingV2Args['obj']['setRewardRate(uint512)void'] | TokenStakingV2Args['tuple']['setRewardRate(uint512)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.setRewardRate(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a setLockDuration(uint512)void method call against the TokenStakingV2 contract
       */
      setLockDuration(params: CallParams<TokenStakingV2Args['obj']['setLockDuration(uint512)void'] | TokenStakingV2Args['tuple']['setLockDuration(uint512)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.setLockDuration(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a stakedBalance(address)uint512 method call against the TokenStakingV2 contract
       */
      stakedBalance(params: CallParams<TokenStakingV2Args['obj']['stakedBalance(address)uint512'] | TokenStakingV2Args['tuple']['stakedBalance(address)uint512']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.stakedBalance(params)))
        resultMappers.push((v) => client.decodeReturnValue('stakedBalance(address)uint512', v))
        return this
      },
      /**
       * Add a lockUntil(address)uint512 method call against the TokenStakingV2 contract
       */
      lockUntil(params: CallParams<TokenStakingV2Args['obj']['lockUntil(address)uint512'] | TokenStakingV2Args['tuple']['lockUntil(address)uint512']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.lockUntil(params)))
        resultMappers.push((v) => client.decodeReturnValue('lockUntil(address)uint512', v))
        return this
      },
      /**
       * Add a pendingRewards(address)uint512 method call against the TokenStakingV2 contract
       */
      pendingRewards(params: CallParams<TokenStakingV2Args['obj']['pendingRewards(address)uint512'] | TokenStakingV2Args['tuple']['pendingRewards(address)uint512']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.pendingRewards(params)))
        resultMappers.push((v) => client.decodeReturnValue('pendingRewards(address)uint512', v))
        return this
      },
      /**
       * Add a totalStaked()uint512 method call against the TokenStakingV2 contract
       */
      totalStaked(params: CallParams<TokenStakingV2Args['obj']['totalStaked()uint512'] | TokenStakingV2Args['tuple']['totalStaked()uint512']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.totalStaked(params)))
        resultMappers.push((v) => client.decodeReturnValue('totalStaked()uint512', v))
        return this
      },
      /**
       * Add a totalRewardsPaid()uint512 method call against the TokenStakingV2 contract
       */
      totalRewardsPaid(params: CallParams<TokenStakingV2Args['obj']['totalRewardsPaid()uint512'] | TokenStakingV2Args['tuple']['totalRewardsPaid()uint512']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.totalRewardsPaid(params)))
        resultMappers.push((v) => client.decodeReturnValue('totalRewardsPaid()uint512', v))
        return this
      },
      /**
       * Add a rewardRate()uint512 method call against the TokenStakingV2 contract
       */
      rewardRate(params: CallParams<TokenStakingV2Args['obj']['rewardRate()uint512'] | TokenStakingV2Args['tuple']['rewardRate()uint512']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.rewardRate(params)))
        resultMappers.push((v) => client.decodeReturnValue('rewardRate()uint512', v))
        return this
      },
      /**
       * Add a lockDuration()uint512 method call against the TokenStakingV2 contract
       */
      lockDuration(params: CallParams<TokenStakingV2Args['obj']['lockDuration()uint512'] | TokenStakingV2Args['tuple']['lockDuration()uint512']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.lockDuration(params)))
        resultMappers.push((v) => client.decodeReturnValue('lockDuration()uint512', v))
        return this
      },
      /**
       * Add a stakerCount()uint512 method call against the TokenStakingV2 contract
       */
      stakerCount(params: CallParams<TokenStakingV2Args['obj']['stakerCount()uint512'] | TokenStakingV2Args['tuple']['stakerCount()uint512']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.stakerCount(params)))
        resultMappers.push((v) => client.decodeReturnValue('stakerCount()uint512', v))
        return this
      },
      /**
       * Add a owner()address method call against the TokenStakingV2 contract
       */
      owner(params: CallParams<TokenStakingV2Args['obj']['owner()address'] | TokenStakingV2Args['tuple']['owner()address']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.owner(params)))
        resultMappers.push((v) => client.decodeReturnValue('owner()address', v))
        return this
      },
      /**
       * Add a isLocked(address)bool method call against the TokenStakingV2 contract
       */
      isLocked(params: CallParams<TokenStakingV2Args['obj']['isLocked(address)bool'] | TokenStakingV2Args['tuple']['isLocked(address)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.isLocked(params)))
        resultMappers.push((v) => client.decodeReturnValue('isLocked(address)bool', v))
        return this
      },
      /**
       * Add a clear state call to the TokenStakingV2 contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as TokenStakingV2Composer
  }
}
export type TokenStakingV2Composer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the stake(uint512)void ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  stake(params?: CallParams<TokenStakingV2Args['obj']['stake(uint512)void'] | TokenStakingV2Args['tuple']['stake(uint512)void']>): TokenStakingV2Composer<[...TReturns, TokenStakingV2Returns['stake(uint512)void'] | undefined]>

  /**
   * Calls the unstake(uint512)void ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  unstake(params?: CallParams<TokenStakingV2Args['obj']['unstake(uint512)void'] | TokenStakingV2Args['tuple']['unstake(uint512)void']>): TokenStakingV2Composer<[...TReturns, TokenStakingV2Returns['unstake(uint512)void'] | undefined]>

  /**
   * Calls the claimRewards()void ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  claimRewards(params?: CallParams<TokenStakingV2Args['obj']['claimRewards()void'] | TokenStakingV2Args['tuple']['claimRewards()void']>): TokenStakingV2Composer<[...TReturns, TokenStakingV2Returns['claimRewards()void'] | undefined]>

  /**
   * Calls the setRewardRate(uint512)void ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  setRewardRate(params?: CallParams<TokenStakingV2Args['obj']['setRewardRate(uint512)void'] | TokenStakingV2Args['tuple']['setRewardRate(uint512)void']>): TokenStakingV2Composer<[...TReturns, TokenStakingV2Returns['setRewardRate(uint512)void'] | undefined]>

  /**
   * Calls the setLockDuration(uint512)void ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  setLockDuration(params?: CallParams<TokenStakingV2Args['obj']['setLockDuration(uint512)void'] | TokenStakingV2Args['tuple']['setLockDuration(uint512)void']>): TokenStakingV2Composer<[...TReturns, TokenStakingV2Returns['setLockDuration(uint512)void'] | undefined]>

  /**
   * Calls the stakedBalance(address)uint512 ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  stakedBalance(params?: CallParams<TokenStakingV2Args['obj']['stakedBalance(address)uint512'] | TokenStakingV2Args['tuple']['stakedBalance(address)uint512']>): TokenStakingV2Composer<[...TReturns, TokenStakingV2Returns['stakedBalance(address)uint512'] | undefined]>

  /**
   * Calls the lockUntil(address)uint512 ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  lockUntil(params?: CallParams<TokenStakingV2Args['obj']['lockUntil(address)uint512'] | TokenStakingV2Args['tuple']['lockUntil(address)uint512']>): TokenStakingV2Composer<[...TReturns, TokenStakingV2Returns['lockUntil(address)uint512'] | undefined]>

  /**
   * Calls the pendingRewards(address)uint512 ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  pendingRewards(params?: CallParams<TokenStakingV2Args['obj']['pendingRewards(address)uint512'] | TokenStakingV2Args['tuple']['pendingRewards(address)uint512']>): TokenStakingV2Composer<[...TReturns, TokenStakingV2Returns['pendingRewards(address)uint512'] | undefined]>

  /**
   * Calls the totalStaked()uint512 ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  totalStaked(params?: CallParams<TokenStakingV2Args['obj']['totalStaked()uint512'] | TokenStakingV2Args['tuple']['totalStaked()uint512']>): TokenStakingV2Composer<[...TReturns, TokenStakingV2Returns['totalStaked()uint512'] | undefined]>

  /**
   * Calls the totalRewardsPaid()uint512 ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  totalRewardsPaid(params?: CallParams<TokenStakingV2Args['obj']['totalRewardsPaid()uint512'] | TokenStakingV2Args['tuple']['totalRewardsPaid()uint512']>): TokenStakingV2Composer<[...TReturns, TokenStakingV2Returns['totalRewardsPaid()uint512'] | undefined]>

  /**
   * Calls the rewardRate()uint512 ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  rewardRate(params?: CallParams<TokenStakingV2Args['obj']['rewardRate()uint512'] | TokenStakingV2Args['tuple']['rewardRate()uint512']>): TokenStakingV2Composer<[...TReturns, TokenStakingV2Returns['rewardRate()uint512'] | undefined]>

  /**
   * Calls the lockDuration()uint512 ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  lockDuration(params?: CallParams<TokenStakingV2Args['obj']['lockDuration()uint512'] | TokenStakingV2Args['tuple']['lockDuration()uint512']>): TokenStakingV2Composer<[...TReturns, TokenStakingV2Returns['lockDuration()uint512'] | undefined]>

  /**
   * Calls the stakerCount()uint512 ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  stakerCount(params?: CallParams<TokenStakingV2Args['obj']['stakerCount()uint512'] | TokenStakingV2Args['tuple']['stakerCount()uint512']>): TokenStakingV2Composer<[...TReturns, TokenStakingV2Returns['stakerCount()uint512'] | undefined]>

  /**
   * Calls the owner()address ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  owner(params?: CallParams<TokenStakingV2Args['obj']['owner()address'] | TokenStakingV2Args['tuple']['owner()address']>): TokenStakingV2Composer<[...TReturns, TokenStakingV2Returns['owner()address'] | undefined]>

  /**
   * Calls the isLocked(address)bool ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  isLocked(params?: CallParams<TokenStakingV2Args['obj']['isLocked(address)bool'] | TokenStakingV2Args['tuple']['isLocked(address)bool']>): TokenStakingV2Composer<[...TReturns, TokenStakingV2Returns['isLocked(address)bool'] | undefined]>

  /**
   * Makes a clear_state call to an existing instance of the TokenStakingV2 smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): TokenStakingV2Composer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): TokenStakingV2Composer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<TokenStakingV2ComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<TokenStakingV2ComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<TokenStakingV2ComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<TokenStakingV2ComposerResults<TReturns>>
}
export type TokenStakingV2ComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

