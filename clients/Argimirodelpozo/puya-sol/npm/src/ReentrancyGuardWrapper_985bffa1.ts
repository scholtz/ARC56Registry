/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"ReentrancyGuardWrapper","structs":{},"methods":[{"name":"increment","args":[],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}},{"name":"getCounter","args":[],"returns":{"type":"uint256"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"counter","args":[],"returns":{"type":"uint256"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}}],"arcs":[22,28],"networks":{},"state":{"schema":{"global":{"ints":0,"bytes":1},"local":{"ints":0,"bytes":0}},"keys":{"global":{"counter":{"keyType":"AVMString","valueType":"AVMBytes","key":"Y291bnRlcg=="}},"local":{},"box":{}},"maps":{"global":{},"local":{},"box":{}}},"bareActions":{"create":[],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[77,89,107],"errorMessage":"check counter exists"},{"pc":[94,112],"errorMessage":"overflow"},{"pc":[73],"errorMessage":"revert"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"events":[]} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


/**
 * The argument types for the ReentrancyGuardWrapper contract
 */
export type ReentrancyGuardWrapperArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'increment()void': Record<string, never>
    'getCounter()uint256': Record<string, never>
    'counter()uint256': Record<string, never>
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'increment()void': []
    'getCounter()uint256': []
    'counter()uint256': []
  }
}

/**
 * The return type for each method
 */
export type ReentrancyGuardWrapperReturns = {
  'increment()void': void
  'getCounter()uint256': bigint
  'counter()uint256': bigint
}

/**
 * Defines the types of available calls and state of the ReentrancyGuardWrapper smart contract.
 */
export type ReentrancyGuardWrapperTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'increment()void' | 'increment', {
      argsObj: ReentrancyGuardWrapperArgs['obj']['increment()void']
      argsTuple: ReentrancyGuardWrapperArgs['tuple']['increment()void']
      returns: ReentrancyGuardWrapperReturns['increment()void']
    }>
    & Record<'getCounter()uint256' | 'getCounter', {
      argsObj: ReentrancyGuardWrapperArgs['obj']['getCounter()uint256']
      argsTuple: ReentrancyGuardWrapperArgs['tuple']['getCounter()uint256']
      returns: ReentrancyGuardWrapperReturns['getCounter()uint256']
    }>
    & Record<'counter()uint256' | 'counter', {
      argsObj: ReentrancyGuardWrapperArgs['obj']['counter()uint256']
      argsTuple: ReentrancyGuardWrapperArgs['tuple']['counter()uint256']
      returns: ReentrancyGuardWrapperReturns['counter()uint256']
    }>
  /**
   * Defines the shape of the state of the application.
   */
  state: {
    global: {
      keys: {
        counter: BinaryState
      }
      maps: {}
    }
  }
}

/**
 * Defines the possible abi call signatures.
 */
export type ReentrancyGuardWrapperSignatures = keyof ReentrancyGuardWrapperTypes['methods']
/**
 * Defines the possible abi call signatures for methods that return a non-void value.
 */
export type ReentrancyGuardWrapperNonVoidMethodSignatures = keyof ReentrancyGuardWrapperTypes['methods'] extends infer T ? T extends keyof ReentrancyGuardWrapperTypes['methods'] ? MethodReturn<T> extends void ? never : T  : never : never
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the ReentrancyGuardWrapper smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends ReentrancyGuardWrapperSignatures> = ReentrancyGuardWrapperTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the ReentrancyGuardWrapper smart contract to the method's return type
 */
export type MethodReturn<TSignature extends ReentrancyGuardWrapperSignatures> = ReentrancyGuardWrapperTypes['methods'][TSignature]['returns']

/**
 * Defines the shape of the keyed global state of the application.
 */
export type GlobalKeysState = ReentrancyGuardWrapperTypes['state']['global']['keys']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the ReentrancyGuardWrapper smart contract
 */
export abstract class ReentrancyGuardWrapperParamsFactory {
  /**
   * Constructs a no op call for the increment()void ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static increment(params: CallParams<ReentrancyGuardWrapperArgs['obj']['increment()void'] | ReentrancyGuardWrapperArgs['tuple']['increment()void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'increment()void' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the getCounter()uint256 ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static getCounter(params: CallParams<ReentrancyGuardWrapperArgs['obj']['getCounter()uint256'] | ReentrancyGuardWrapperArgs['tuple']['getCounter()uint256']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'getCounter()uint256' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the counter()uint256 ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static counter(params: CallParams<ReentrancyGuardWrapperArgs['obj']['counter()uint256'] | ReentrancyGuardWrapperArgs['tuple']['counter()uint256']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'counter()uint256' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
}

/**
 * A client to make calls to the ReentrancyGuardWrapper smart contract
 */
export class ReentrancyGuardWrapperClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `ReentrancyGuardWrapperClient`
   *
   * @param appClient An `AppClient` instance which has been created with the ReentrancyGuardWrapper app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `ReentrancyGuardWrapperClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Checks for decode errors on the given return value and maps the return value to the return type for the given method
   * @returns The typed return value or undefined if there was no value
   */
  decodeReturnValue<TSignature extends ReentrancyGuardWrapperNonVoidMethodSignatures>(method: TSignature, returnValue: ABIReturn | undefined) {
    return returnValue !== undefined ? getArc56ReturnValue<MethodReturn<TSignature>>(returnValue, this.appClient.getABIMethod(method), APP_SPEC.structs) : undefined
  }

  /**
   * Returns a new `ReentrancyGuardWrapperClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<ReentrancyGuardWrapperClient> {
    return new ReentrancyGuardWrapperClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `ReentrancyGuardWrapperClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<ReentrancyGuardWrapperClient> {
    return new ReentrancyGuardWrapperClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Makes a clear_state call to an existing instance of the ReentrancyGuardWrapper smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the ReentrancyGuardWrapper smart contract using the `increment()void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    increment: (params: CallParams<ReentrancyGuardWrapperArgs['obj']['increment()void'] | ReentrancyGuardWrapperArgs['tuple']['increment()void']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(ReentrancyGuardWrapperParamsFactory.increment(params))
    },

    /**
     * Makes a call to the ReentrancyGuardWrapper smart contract using the `getCounter()uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    getCounter: (params: CallParams<ReentrancyGuardWrapperArgs['obj']['getCounter()uint256'] | ReentrancyGuardWrapperArgs['tuple']['getCounter()uint256']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(ReentrancyGuardWrapperParamsFactory.getCounter(params))
    },

    /**
     * Makes a call to the ReentrancyGuardWrapper smart contract using the `counter()uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    counter: (params: CallParams<ReentrancyGuardWrapperArgs['obj']['counter()uint256'] | ReentrancyGuardWrapperArgs['tuple']['counter()uint256']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(ReentrancyGuardWrapperParamsFactory.counter(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Makes a clear_state call to an existing instance of the ReentrancyGuardWrapper smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the ReentrancyGuardWrapper smart contract using the `increment()void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    increment: (params: CallParams<ReentrancyGuardWrapperArgs['obj']['increment()void'] | ReentrancyGuardWrapperArgs['tuple']['increment()void']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(ReentrancyGuardWrapperParamsFactory.increment(params))
    },

    /**
     * Makes a call to the ReentrancyGuardWrapper smart contract using the `getCounter()uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    getCounter: (params: CallParams<ReentrancyGuardWrapperArgs['obj']['getCounter()uint256'] | ReentrancyGuardWrapperArgs['tuple']['getCounter()uint256']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(ReentrancyGuardWrapperParamsFactory.getCounter(params))
    },

    /**
     * Makes a call to the ReentrancyGuardWrapper smart contract using the `counter()uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    counter: (params: CallParams<ReentrancyGuardWrapperArgs['obj']['counter()uint256'] | ReentrancyGuardWrapperArgs['tuple']['counter()uint256']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(ReentrancyGuardWrapperParamsFactory.counter(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Makes a clear_state call to an existing instance of the ReentrancyGuardWrapper smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the ReentrancyGuardWrapper smart contract using the `increment()void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    increment: async (params: CallParams<ReentrancyGuardWrapperArgs['obj']['increment()void'] | ReentrancyGuardWrapperArgs['tuple']['increment()void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(ReentrancyGuardWrapperParamsFactory.increment(params))
      return {...result, return: result.return as unknown as (undefined | ReentrancyGuardWrapperReturns['increment()void'])}
    },

    /**
     * Makes a call to the ReentrancyGuardWrapper smart contract using the `getCounter()uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    getCounter: async (params: CallParams<ReentrancyGuardWrapperArgs['obj']['getCounter()uint256'] | ReentrancyGuardWrapperArgs['tuple']['getCounter()uint256']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(ReentrancyGuardWrapperParamsFactory.getCounter(params))
      return {...result, return: result.return as unknown as (undefined | ReentrancyGuardWrapperReturns['getCounter()uint256'])}
    },

    /**
     * Makes a call to the ReentrancyGuardWrapper smart contract using the `counter()uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    counter: async (params: CallParams<ReentrancyGuardWrapperArgs['obj']['counter()uint256'] | ReentrancyGuardWrapperArgs['tuple']['counter()uint256']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(ReentrancyGuardWrapperParamsFactory.counter(params))
      return {...result, return: result.return as unknown as (undefined | ReentrancyGuardWrapperReturns['counter()uint256'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new ReentrancyGuardWrapperClient(this.appClient.clone(params))
  }

  /**
   * Makes a readonly (simulated) call to the ReentrancyGuardWrapper smart contract using the `getCounter()uint256` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async getCounter(params: CallParams<ReentrancyGuardWrapperArgs['obj']['getCounter()uint256'] | ReentrancyGuardWrapperArgs['tuple']['getCounter()uint256']> = {args: []}) {
    const result = await this.appClient.send.call(ReentrancyGuardWrapperParamsFactory.getCounter(params))
    return result.return as unknown as ReentrancyGuardWrapperReturns['getCounter()uint256']
  }

  /**
   * Makes a readonly (simulated) call to the ReentrancyGuardWrapper smart contract using the `counter()uint256` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async counter(params: CallParams<ReentrancyGuardWrapperArgs['obj']['counter()uint256'] | ReentrancyGuardWrapperArgs['tuple']['counter()uint256']> = {args: []}) {
    const result = await this.appClient.send.call(ReentrancyGuardWrapperParamsFactory.counter(params))
    return result.return as unknown as ReentrancyGuardWrapperReturns['counter()uint256']
  }

  /**
   * Methods to access state for the current ReentrancyGuardWrapper app
   */
  state = {
    /**
     * Methods to access global state for the current ReentrancyGuardWrapper app
     */
    global: {
      /**
       * Get all current keyed values from global state
       */
      getAll: async (): Promise<Partial<Expand<GlobalKeysState>>> => {
        const result = await this.appClient.state.global.getAll()
        return {
          counter: new BinaryStateValue(result.counter),
        }
      },
      /**
       * Get the current value of the counter key in global state
       */
      counter: async (): Promise<BinaryState> => { return new BinaryStateValue((await this.appClient.state.global.getValue("counter")) as Uint8Array | undefined) },
    },
  }

  public newGroup(): ReentrancyGuardWrapperComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a increment()void method call against the ReentrancyGuardWrapper contract
       */
      increment(params: CallParams<ReentrancyGuardWrapperArgs['obj']['increment()void'] | ReentrancyGuardWrapperArgs['tuple']['increment()void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.increment(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a getCounter()uint256 method call against the ReentrancyGuardWrapper contract
       */
      getCounter(params: CallParams<ReentrancyGuardWrapperArgs['obj']['getCounter()uint256'] | ReentrancyGuardWrapperArgs['tuple']['getCounter()uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.getCounter(params)))
        resultMappers.push((v) => client.decodeReturnValue('getCounter()uint256', v))
        return this
      },
      /**
       * Add a counter()uint256 method call against the ReentrancyGuardWrapper contract
       */
      counter(params: CallParams<ReentrancyGuardWrapperArgs['obj']['counter()uint256'] | ReentrancyGuardWrapperArgs['tuple']['counter()uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.counter(params)))
        resultMappers.push((v) => client.decodeReturnValue('counter()uint256', v))
        return this
      },
      /**
       * Add a clear state call to the ReentrancyGuardWrapper contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as ReentrancyGuardWrapperComposer
  }
}
export type ReentrancyGuardWrapperComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the increment()void ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  increment(params?: CallParams<ReentrancyGuardWrapperArgs['obj']['increment()void'] | ReentrancyGuardWrapperArgs['tuple']['increment()void']>): ReentrancyGuardWrapperComposer<[...TReturns, ReentrancyGuardWrapperReturns['increment()void'] | undefined]>

  /**
   * Calls the getCounter()uint256 ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  getCounter(params?: CallParams<ReentrancyGuardWrapperArgs['obj']['getCounter()uint256'] | ReentrancyGuardWrapperArgs['tuple']['getCounter()uint256']>): ReentrancyGuardWrapperComposer<[...TReturns, ReentrancyGuardWrapperReturns['getCounter()uint256'] | undefined]>

  /**
   * Calls the counter()uint256 ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  counter(params?: CallParams<ReentrancyGuardWrapperArgs['obj']['counter()uint256'] | ReentrancyGuardWrapperArgs['tuple']['counter()uint256']>): ReentrancyGuardWrapperComposer<[...TReturns, ReentrancyGuardWrapperReturns['counter()uint256'] | undefined]>

  /**
   * Makes a clear_state call to an existing instance of the ReentrancyGuardWrapper smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): ReentrancyGuardWrapperComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): ReentrancyGuardWrapperComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<ReentrancyGuardWrapperComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<ReentrancyGuardWrapperComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<ReentrancyGuardWrapperComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<ReentrancyGuardWrapperComposerResults<TReturns>>
}
export type ReentrancyGuardWrapperComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

