/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"C","structs":{},"methods":[{"name":"a","args":[],"returns":{"type":"uint64"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"b","args":[],"returns":{"type":"uint64"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"c","args":[],"returns":{"type":"uint128"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"d","args":[],"returns":{"type":"uint256"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"e","args":[],"returns":{"type":"byte[1]"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"f","args":[],"returns":{"type":"byte[20]"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"g","args":[],"returns":{"type":"byte[32]"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"h","args":[],"returns":{"type":"bool"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"i","args":[],"returns":{"type":"address"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}}],"arcs":[22,28],"networks":{},"state":{"schema":{"global":{"ints":3,"bytes":6},"local":{"ints":0,"bytes":0}},"keys":{"global":{"a":{"keyType":"AVMString","valueType":"AVMUint64","key":"YQ=="},"b":{"keyType":"AVMString","valueType":"AVMUint64","key":"Yg=="},"c":{"keyType":"AVMString","valueType":"AVMBytes","key":"Yw=="},"d":{"keyType":"AVMString","valueType":"AVMBytes","key":"ZA=="},"e":{"keyType":"AVMString","valueType":"AVMBytes","key":"ZQ=="},"f":{"keyType":"AVMString","valueType":"AVMBytes","key":"Zg=="},"g":{"keyType":"AVMString","valueType":"AVMBytes","key":"Zw=="},"h":{"keyType":"AVMString","valueType":"AVMUint64","key":"aA=="},"i":{"keyType":"AVMString","valueType":"address","key":"aQ=="}},"local":{},"box":{}},"maps":{"global":{},"local":{},"box":{}}},"bareActions":{"create":[],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[651],"errorMessage":"check a exists"},{"pc":[623],"errorMessage":"check b exists"},{"pc":[588],"errorMessage":"check c exists"},{"pc":[553],"errorMessage":"check d exists"},{"pc":[525],"errorMessage":"check e exists"},{"pc":[497],"errorMessage":"check f exists"},{"pc":[469],"errorMessage":"check g exists"},{"pc":[434],"errorMessage":"check h exists"},{"pc":[406],"errorMessage":"check i exists"},{"pc":[401,429,464,492,520,548,584,619,647],"errorMessage":"not payable"},{"pc":[558,593],"errorMessage":"overflow"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"events":[]} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


/**
 * The argument types for the C contract
 */
export type CArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'a()uint64': Record<string, never>
    'b()uint64': Record<string, never>
    'c()uint128': Record<string, never>
    'd()uint256': Record<string, never>
    'e()byte[1]': Record<string, never>
    'f()byte[20]': Record<string, never>
    'g()byte[32]': Record<string, never>
    'h()bool': Record<string, never>
    'i()address': Record<string, never>
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'a()uint64': []
    'b()uint64': []
    'c()uint128': []
    'd()uint256': []
    'e()byte[1]': []
    'f()byte[20]': []
    'g()byte[32]': []
    'h()bool': []
    'i()address': []
  }
}

/**
 * The return type for each method
 */
export type CReturns = {
  'a()uint64': bigint
  'b()uint64': bigint
  'c()uint128': bigint
  'd()uint256': bigint
  'e()byte[1]': Uint8Array
  'f()byte[20]': Uint8Array
  'g()byte[32]': Uint8Array
  'h()bool': boolean
  'i()address': string
}

/**
 * Defines the types of available calls and state of the C smart contract.
 */
export type CTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'a()uint64' | 'a', {
      argsObj: CArgs['obj']['a()uint64']
      argsTuple: CArgs['tuple']['a()uint64']
      returns: CReturns['a()uint64']
    }>
    & Record<'b()uint64' | 'b', {
      argsObj: CArgs['obj']['b()uint64']
      argsTuple: CArgs['tuple']['b()uint64']
      returns: CReturns['b()uint64']
    }>
    & Record<'c()uint128' | 'c', {
      argsObj: CArgs['obj']['c()uint128']
      argsTuple: CArgs['tuple']['c()uint128']
      returns: CReturns['c()uint128']
    }>
    & Record<'d()uint256' | 'd', {
      argsObj: CArgs['obj']['d()uint256']
      argsTuple: CArgs['tuple']['d()uint256']
      returns: CReturns['d()uint256']
    }>
    & Record<'e()byte[1]' | 'e', {
      argsObj: CArgs['obj']['e()byte[1]']
      argsTuple: CArgs['tuple']['e()byte[1]']
      returns: CReturns['e()byte[1]']
    }>
    & Record<'f()byte[20]' | 'f', {
      argsObj: CArgs['obj']['f()byte[20]']
      argsTuple: CArgs['tuple']['f()byte[20]']
      returns: CReturns['f()byte[20]']
    }>
    & Record<'g()byte[32]' | 'g', {
      argsObj: CArgs['obj']['g()byte[32]']
      argsTuple: CArgs['tuple']['g()byte[32]']
      returns: CReturns['g()byte[32]']
    }>
    & Record<'h()bool' | 'h', {
      argsObj: CArgs['obj']['h()bool']
      argsTuple: CArgs['tuple']['h()bool']
      returns: CReturns['h()bool']
    }>
    & Record<'i()address' | 'i', {
      argsObj: CArgs['obj']['i()address']
      argsTuple: CArgs['tuple']['i()address']
      returns: CReturns['i()address']
    }>
  /**
   * Defines the shape of the state of the application.
   */
  state: {
    global: {
      keys: {
        a: bigint
        b: bigint
        c: BinaryState
        d: BinaryState
        e: BinaryState
        f: BinaryState
        g: BinaryState
        h: bigint
        i: string
      }
      maps: {}
    }
  }
}

/**
 * Defines the possible abi call signatures.
 */
export type CSignatures = keyof CTypes['methods']
/**
 * Defines the possible abi call signatures for methods that return a non-void value.
 */
export type CNonVoidMethodSignatures = keyof CTypes['methods'] extends infer T ? T extends keyof CTypes['methods'] ? MethodReturn<T> extends void ? never : T  : never : never
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the C smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends CSignatures> = CTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the C smart contract to the method's return type
 */
export type MethodReturn<TSignature extends CSignatures> = CTypes['methods'][TSignature]['returns']

/**
 * Defines the shape of the keyed global state of the application.
 */
export type GlobalKeysState = CTypes['state']['global']['keys']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the C smart contract
 */
export abstract class CParamsFactory {
  /**
   * Constructs a no op call for the a()uint64 ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static a(params: CallParams<CArgs['obj']['a()uint64'] | CArgs['tuple']['a()uint64']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'a()uint64' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the b()uint64 ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static b(params: CallParams<CArgs['obj']['b()uint64'] | CArgs['tuple']['b()uint64']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'b()uint64' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the c()uint128 ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static c(params: CallParams<CArgs['obj']['c()uint128'] | CArgs['tuple']['c()uint128']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'c()uint128' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the d()uint256 ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static d(params: CallParams<CArgs['obj']['d()uint256'] | CArgs['tuple']['d()uint256']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'd()uint256' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the e()byte[1] ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static e(params: CallParams<CArgs['obj']['e()byte[1]'] | CArgs['tuple']['e()byte[1]']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'e()byte[1]' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the f()byte[20] ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static f(params: CallParams<CArgs['obj']['f()byte[20]'] | CArgs['tuple']['f()byte[20]']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'f()byte[20]' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the g()byte[32] ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static g(params: CallParams<CArgs['obj']['g()byte[32]'] | CArgs['tuple']['g()byte[32]']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'g()byte[32]' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the h()bool ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static h(params: CallParams<CArgs['obj']['h()bool'] | CArgs['tuple']['h()bool']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'h()bool' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the i()address ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static i(params: CallParams<CArgs['obj']['i()address'] | CArgs['tuple']['i()address']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'i()address' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
}

/**
 * A client to make calls to the C smart contract
 */
export class CClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `CClient`
   *
   * @param appClient An `AppClient` instance which has been created with the C app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `CClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Checks for decode errors on the given return value and maps the return value to the return type for the given method
   * @returns The typed return value or undefined if there was no value
   */
  decodeReturnValue<TSignature extends CNonVoidMethodSignatures>(method: TSignature, returnValue: ABIReturn | undefined) {
    return returnValue !== undefined ? getArc56ReturnValue<MethodReturn<TSignature>>(returnValue, this.appClient.getABIMethod(method), APP_SPEC.structs) : undefined
  }

  /**
   * Returns a new `CClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<CClient> {
    return new CClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `CClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<CClient> {
    return new CClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Makes a clear_state call to an existing instance of the C smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the C smart contract using the `a()uint64` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    a: (params: CallParams<CArgs['obj']['a()uint64'] | CArgs['tuple']['a()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(CParamsFactory.a(params))
    },

    /**
     * Makes a call to the C smart contract using the `b()uint64` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    b: (params: CallParams<CArgs['obj']['b()uint64'] | CArgs['tuple']['b()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(CParamsFactory.b(params))
    },

    /**
     * Makes a call to the C smart contract using the `c()uint128` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    c: (params: CallParams<CArgs['obj']['c()uint128'] | CArgs['tuple']['c()uint128']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(CParamsFactory.c(params))
    },

    /**
     * Makes a call to the C smart contract using the `d()uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    d: (params: CallParams<CArgs['obj']['d()uint256'] | CArgs['tuple']['d()uint256']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(CParamsFactory.d(params))
    },

    /**
     * Makes a call to the C smart contract using the `e()byte[1]` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    e: (params: CallParams<CArgs['obj']['e()byte[1]'] | CArgs['tuple']['e()byte[1]']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(CParamsFactory.e(params))
    },

    /**
     * Makes a call to the C smart contract using the `f()byte[20]` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    f: (params: CallParams<CArgs['obj']['f()byte[20]'] | CArgs['tuple']['f()byte[20]']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(CParamsFactory.f(params))
    },

    /**
     * Makes a call to the C smart contract using the `g()byte[32]` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    g: (params: CallParams<CArgs['obj']['g()byte[32]'] | CArgs['tuple']['g()byte[32]']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(CParamsFactory.g(params))
    },

    /**
     * Makes a call to the C smart contract using the `h()bool` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    h: (params: CallParams<CArgs['obj']['h()bool'] | CArgs['tuple']['h()bool']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(CParamsFactory.h(params))
    },

    /**
     * Makes a call to the C smart contract using the `i()address` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    i: (params: CallParams<CArgs['obj']['i()address'] | CArgs['tuple']['i()address']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(CParamsFactory.i(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Makes a clear_state call to an existing instance of the C smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the C smart contract using the `a()uint64` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    a: (params: CallParams<CArgs['obj']['a()uint64'] | CArgs['tuple']['a()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(CParamsFactory.a(params))
    },

    /**
     * Makes a call to the C smart contract using the `b()uint64` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    b: (params: CallParams<CArgs['obj']['b()uint64'] | CArgs['tuple']['b()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(CParamsFactory.b(params))
    },

    /**
     * Makes a call to the C smart contract using the `c()uint128` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    c: (params: CallParams<CArgs['obj']['c()uint128'] | CArgs['tuple']['c()uint128']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(CParamsFactory.c(params))
    },

    /**
     * Makes a call to the C smart contract using the `d()uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    d: (params: CallParams<CArgs['obj']['d()uint256'] | CArgs['tuple']['d()uint256']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(CParamsFactory.d(params))
    },

    /**
     * Makes a call to the C smart contract using the `e()byte[1]` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    e: (params: CallParams<CArgs['obj']['e()byte[1]'] | CArgs['tuple']['e()byte[1]']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(CParamsFactory.e(params))
    },

    /**
     * Makes a call to the C smart contract using the `f()byte[20]` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    f: (params: CallParams<CArgs['obj']['f()byte[20]'] | CArgs['tuple']['f()byte[20]']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(CParamsFactory.f(params))
    },

    /**
     * Makes a call to the C smart contract using the `g()byte[32]` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    g: (params: CallParams<CArgs['obj']['g()byte[32]'] | CArgs['tuple']['g()byte[32]']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(CParamsFactory.g(params))
    },

    /**
     * Makes a call to the C smart contract using the `h()bool` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    h: (params: CallParams<CArgs['obj']['h()bool'] | CArgs['tuple']['h()bool']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(CParamsFactory.h(params))
    },

    /**
     * Makes a call to the C smart contract using the `i()address` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    i: (params: CallParams<CArgs['obj']['i()address'] | CArgs['tuple']['i()address']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(CParamsFactory.i(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Makes a clear_state call to an existing instance of the C smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the C smart contract using the `a()uint64` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    a: async (params: CallParams<CArgs['obj']['a()uint64'] | CArgs['tuple']['a()uint64']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(CParamsFactory.a(params))
      return {...result, return: result.return as unknown as (undefined | CReturns['a()uint64'])}
    },

    /**
     * Makes a call to the C smart contract using the `b()uint64` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    b: async (params: CallParams<CArgs['obj']['b()uint64'] | CArgs['tuple']['b()uint64']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(CParamsFactory.b(params))
      return {...result, return: result.return as unknown as (undefined | CReturns['b()uint64'])}
    },

    /**
     * Makes a call to the C smart contract using the `c()uint128` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    c: async (params: CallParams<CArgs['obj']['c()uint128'] | CArgs['tuple']['c()uint128']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(CParamsFactory.c(params))
      return {...result, return: result.return as unknown as (undefined | CReturns['c()uint128'])}
    },

    /**
     * Makes a call to the C smart contract using the `d()uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    d: async (params: CallParams<CArgs['obj']['d()uint256'] | CArgs['tuple']['d()uint256']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(CParamsFactory.d(params))
      return {...result, return: result.return as unknown as (undefined | CReturns['d()uint256'])}
    },

    /**
     * Makes a call to the C smart contract using the `e()byte[1]` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    e: async (params: CallParams<CArgs['obj']['e()byte[1]'] | CArgs['tuple']['e()byte[1]']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(CParamsFactory.e(params))
      return {...result, return: result.return as unknown as (undefined | CReturns['e()byte[1]'])}
    },

    /**
     * Makes a call to the C smart contract using the `f()byte[20]` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    f: async (params: CallParams<CArgs['obj']['f()byte[20]'] | CArgs['tuple']['f()byte[20]']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(CParamsFactory.f(params))
      return {...result, return: result.return as unknown as (undefined | CReturns['f()byte[20]'])}
    },

    /**
     * Makes a call to the C smart contract using the `g()byte[32]` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    g: async (params: CallParams<CArgs['obj']['g()byte[32]'] | CArgs['tuple']['g()byte[32]']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(CParamsFactory.g(params))
      return {...result, return: result.return as unknown as (undefined | CReturns['g()byte[32]'])}
    },

    /**
     * Makes a call to the C smart contract using the `h()bool` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    h: async (params: CallParams<CArgs['obj']['h()bool'] | CArgs['tuple']['h()bool']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(CParamsFactory.h(params))
      return {...result, return: result.return as unknown as (undefined | CReturns['h()bool'])}
    },

    /**
     * Makes a call to the C smart contract using the `i()address` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    i: async (params: CallParams<CArgs['obj']['i()address'] | CArgs['tuple']['i()address']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(CParamsFactory.i(params))
      return {...result, return: result.return as unknown as (undefined | CReturns['i()address'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new CClient(this.appClient.clone(params))
  }

  /**
   * Makes a readonly (simulated) call to the C smart contract using the `a()uint64` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async a(params: CallParams<CArgs['obj']['a()uint64'] | CArgs['tuple']['a()uint64']> = {args: []}) {
    const result = await this.appClient.send.call(CParamsFactory.a(params))
    return result.return as unknown as CReturns['a()uint64']
  }

  /**
   * Makes a readonly (simulated) call to the C smart contract using the `b()uint64` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async b(params: CallParams<CArgs['obj']['b()uint64'] | CArgs['tuple']['b()uint64']> = {args: []}) {
    const result = await this.appClient.send.call(CParamsFactory.b(params))
    return result.return as unknown as CReturns['b()uint64']
  }

  /**
   * Makes a readonly (simulated) call to the C smart contract using the `c()uint128` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async c(params: CallParams<CArgs['obj']['c()uint128'] | CArgs['tuple']['c()uint128']> = {args: []}) {
    const result = await this.appClient.send.call(CParamsFactory.c(params))
    return result.return as unknown as CReturns['c()uint128']
  }

  /**
   * Makes a readonly (simulated) call to the C smart contract using the `d()uint256` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async d(params: CallParams<CArgs['obj']['d()uint256'] | CArgs['tuple']['d()uint256']> = {args: []}) {
    const result = await this.appClient.send.call(CParamsFactory.d(params))
    return result.return as unknown as CReturns['d()uint256']
  }

  /**
   * Makes a readonly (simulated) call to the C smart contract using the `e()byte[1]` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async e(params: CallParams<CArgs['obj']['e()byte[1]'] | CArgs['tuple']['e()byte[1]']> = {args: []}) {
    const result = await this.appClient.send.call(CParamsFactory.e(params))
    return result.return as unknown as CReturns['e()byte[1]']
  }

  /**
   * Makes a readonly (simulated) call to the C smart contract using the `f()byte[20]` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async f(params: CallParams<CArgs['obj']['f()byte[20]'] | CArgs['tuple']['f()byte[20]']> = {args: []}) {
    const result = await this.appClient.send.call(CParamsFactory.f(params))
    return result.return as unknown as CReturns['f()byte[20]']
  }

  /**
   * Makes a readonly (simulated) call to the C smart contract using the `g()byte[32]` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async g(params: CallParams<CArgs['obj']['g()byte[32]'] | CArgs['tuple']['g()byte[32]']> = {args: []}) {
    const result = await this.appClient.send.call(CParamsFactory.g(params))
    return result.return as unknown as CReturns['g()byte[32]']
  }

  /**
   * Makes a readonly (simulated) call to the C smart contract using the `h()bool` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async h(params: CallParams<CArgs['obj']['h()bool'] | CArgs['tuple']['h()bool']> = {args: []}) {
    const result = await this.appClient.send.call(CParamsFactory.h(params))
    return result.return as unknown as CReturns['h()bool']
  }

  /**
   * Makes a readonly (simulated) call to the C smart contract using the `i()address` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async i(params: CallParams<CArgs['obj']['i()address'] | CArgs['tuple']['i()address']> = {args: []}) {
    const result = await this.appClient.send.call(CParamsFactory.i(params))
    return result.return as unknown as CReturns['i()address']
  }

  /**
   * Methods to access state for the current C app
   */
  state = {
    /**
     * Methods to access global state for the current C app
     */
    global: {
      /**
       * Get all current keyed values from global state
       */
      getAll: async (): Promise<Partial<Expand<GlobalKeysState>>> => {
        const result = await this.appClient.state.global.getAll()
        return {
          a: result.a,
          b: result.b,
          c: new BinaryStateValue(result.c),
          d: new BinaryStateValue(result.d),
          e: new BinaryStateValue(result.e),
          f: new BinaryStateValue(result.f),
          g: new BinaryStateValue(result.g),
          h: result.h,
          i: result.i,
        }
      },
      /**
       * Get the current value of the a key in global state
       */
      a: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("a")) as bigint | undefined },
      /**
       * Get the current value of the b key in global state
       */
      b: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("b")) as bigint | undefined },
      /**
       * Get the current value of the c key in global state
       */
      c: async (): Promise<BinaryState> => { return new BinaryStateValue((await this.appClient.state.global.getValue("c")) as Uint8Array | undefined) },
      /**
       * Get the current value of the d key in global state
       */
      d: async (): Promise<BinaryState> => { return new BinaryStateValue((await this.appClient.state.global.getValue("d")) as Uint8Array | undefined) },
      /**
       * Get the current value of the e key in global state
       */
      e: async (): Promise<BinaryState> => { return new BinaryStateValue((await this.appClient.state.global.getValue("e")) as Uint8Array | undefined) },
      /**
       * Get the current value of the f key in global state
       */
      f: async (): Promise<BinaryState> => { return new BinaryStateValue((await this.appClient.state.global.getValue("f")) as Uint8Array | undefined) },
      /**
       * Get the current value of the g key in global state
       */
      g: async (): Promise<BinaryState> => { return new BinaryStateValue((await this.appClient.state.global.getValue("g")) as Uint8Array | undefined) },
      /**
       * Get the current value of the h key in global state
       */
      h: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("h")) as bigint | undefined },
      /**
       * Get the current value of the i key in global state
       */
      i: async (): Promise<string | undefined> => { return (await this.appClient.state.global.getValue("i")) as string | undefined },
    },
  }

  public newGroup(): CComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a a()uint64 method call against the C contract
       */
      a(params: CallParams<CArgs['obj']['a()uint64'] | CArgs['tuple']['a()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.a(params)))
        resultMappers.push((v) => client.decodeReturnValue('a()uint64', v))
        return this
      },
      /**
       * Add a b()uint64 method call against the C contract
       */
      b(params: CallParams<CArgs['obj']['b()uint64'] | CArgs['tuple']['b()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.b(params)))
        resultMappers.push((v) => client.decodeReturnValue('b()uint64', v))
        return this
      },
      /**
       * Add a c()uint128 method call against the C contract
       */
      c(params: CallParams<CArgs['obj']['c()uint128'] | CArgs['tuple']['c()uint128']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.c(params)))
        resultMappers.push((v) => client.decodeReturnValue('c()uint128', v))
        return this
      },
      /**
       * Add a d()uint256 method call against the C contract
       */
      d(params: CallParams<CArgs['obj']['d()uint256'] | CArgs['tuple']['d()uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.d(params)))
        resultMappers.push((v) => client.decodeReturnValue('d()uint256', v))
        return this
      },
      /**
       * Add a e()byte[1] method call against the C contract
       */
      e(params: CallParams<CArgs['obj']['e()byte[1]'] | CArgs['tuple']['e()byte[1]']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.e(params)))
        resultMappers.push((v) => client.decodeReturnValue('e()byte[1]', v))
        return this
      },
      /**
       * Add a f()byte[20] method call against the C contract
       */
      f(params: CallParams<CArgs['obj']['f()byte[20]'] | CArgs['tuple']['f()byte[20]']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.f(params)))
        resultMappers.push((v) => client.decodeReturnValue('f()byte[20]', v))
        return this
      },
      /**
       * Add a g()byte[32] method call against the C contract
       */
      g(params: CallParams<CArgs['obj']['g()byte[32]'] | CArgs['tuple']['g()byte[32]']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.g(params)))
        resultMappers.push((v) => client.decodeReturnValue('g()byte[32]', v))
        return this
      },
      /**
       * Add a h()bool method call against the C contract
       */
      h(params: CallParams<CArgs['obj']['h()bool'] | CArgs['tuple']['h()bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.h(params)))
        resultMappers.push((v) => client.decodeReturnValue('h()bool', v))
        return this
      },
      /**
       * Add a i()address method call against the C contract
       */
      i(params: CallParams<CArgs['obj']['i()address'] | CArgs['tuple']['i()address']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.i(params)))
        resultMappers.push((v) => client.decodeReturnValue('i()address', v))
        return this
      },
      /**
       * Add a clear state call to the C contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as CComposer
  }
}
export type CComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the a()uint64 ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  a(params?: CallParams<CArgs['obj']['a()uint64'] | CArgs['tuple']['a()uint64']>): CComposer<[...TReturns, CReturns['a()uint64'] | undefined]>

  /**
   * Calls the b()uint64 ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  b(params?: CallParams<CArgs['obj']['b()uint64'] | CArgs['tuple']['b()uint64']>): CComposer<[...TReturns, CReturns['b()uint64'] | undefined]>

  /**
   * Calls the c()uint128 ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  c(params?: CallParams<CArgs['obj']['c()uint128'] | CArgs['tuple']['c()uint128']>): CComposer<[...TReturns, CReturns['c()uint128'] | undefined]>

  /**
   * Calls the d()uint256 ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  d(params?: CallParams<CArgs['obj']['d()uint256'] | CArgs['tuple']['d()uint256']>): CComposer<[...TReturns, CReturns['d()uint256'] | undefined]>

  /**
   * Calls the e()byte[1] ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  e(params?: CallParams<CArgs['obj']['e()byte[1]'] | CArgs['tuple']['e()byte[1]']>): CComposer<[...TReturns, CReturns['e()byte[1]'] | undefined]>

  /**
   * Calls the f()byte[20] ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  f(params?: CallParams<CArgs['obj']['f()byte[20]'] | CArgs['tuple']['f()byte[20]']>): CComposer<[...TReturns, CReturns['f()byte[20]'] | undefined]>

  /**
   * Calls the g()byte[32] ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  g(params?: CallParams<CArgs['obj']['g()byte[32]'] | CArgs['tuple']['g()byte[32]']>): CComposer<[...TReturns, CReturns['g()byte[32]'] | undefined]>

  /**
   * Calls the h()bool ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  h(params?: CallParams<CArgs['obj']['h()bool'] | CArgs['tuple']['h()bool']>): CComposer<[...TReturns, CReturns['h()bool'] | undefined]>

  /**
   * Calls the i()address ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  i(params?: CallParams<CArgs['obj']['i()address'] | CArgs['tuple']['i()address']>): CComposer<[...TReturns, CReturns['i()address'] | undefined]>

  /**
   * Makes a clear_state call to an existing instance of the C smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): CComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): CComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<CComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<CComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<CComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<CComposerResults<TReturns>>
}
export type CComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

