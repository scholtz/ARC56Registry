/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"C","structs":{},"methods":[{"name":"parentFun","args":[],"returns":{"type":"uint256"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}},{"name":"subAFun","args":[],"returns":{"type":"uint256"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}},{"name":"subBFun","args":[],"returns":{"type":"uint256"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}},{"name":"convertParent","args":[],"returns":{"type":"uint256"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}},{"name":"convertSubA","args":[],"returns":{"type":"(uint256,uint256)"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}},{"name":"convertSubB","args":[],"returns":{"type":"(uint256,uint256)"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}}],"arcs":[22,28],"networks":{},"state":{"schema":{"global":{"ints":0,"bytes":0},"local":{"ints":0,"bytes":0}},"keys":{"global":{},"local":{},"box":{}},"maps":{"global":{},"local":{},"box":{}}},"bareActions":{"create":[],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[138,187,236,297,349,401],"errorMessage":"not payable"},{"pc":[146,160,195,209,244],"errorMessage":"overflow"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"events":[]} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


/**
 * The argument types for the C contract
 */
export type CArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'parentFun()uint256': Record<string, never>
    'subAFun()uint256': Record<string, never>
    'subBFun()uint256': Record<string, never>
    'convertParent()uint256': Record<string, never>
    'convertSubA()(uint256,uint256)': Record<string, never>
    'convertSubB()(uint256,uint256)': Record<string, never>
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'parentFun()uint256': []
    'subAFun()uint256': []
    'subBFun()uint256': []
    'convertParent()uint256': []
    'convertSubA()(uint256,uint256)': []
    'convertSubB()(uint256,uint256)': []
  }
}

/**
 * The return type for each method
 */
export type CReturns = {
  'parentFun()uint256': bigint
  'subAFun()uint256': bigint
  'subBFun()uint256': bigint
  'convertParent()uint256': bigint
  'convertSubA()(uint256,uint256)': [bigint, bigint]
  'convertSubB()(uint256,uint256)': [bigint, bigint]
}

/**
 * Defines the types of available calls and state of the C smart contract.
 */
export type CTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'parentFun()uint256' | 'parentFun', {
      argsObj: CArgs['obj']['parentFun()uint256']
      argsTuple: CArgs['tuple']['parentFun()uint256']
      returns: CReturns['parentFun()uint256']
    }>
    & Record<'subAFun()uint256' | 'subAFun', {
      argsObj: CArgs['obj']['subAFun()uint256']
      argsTuple: CArgs['tuple']['subAFun()uint256']
      returns: CReturns['subAFun()uint256']
    }>
    & Record<'subBFun()uint256' | 'subBFun', {
      argsObj: CArgs['obj']['subBFun()uint256']
      argsTuple: CArgs['tuple']['subBFun()uint256']
      returns: CReturns['subBFun()uint256']
    }>
    & Record<'convertParent()uint256' | 'convertParent', {
      argsObj: CArgs['obj']['convertParent()uint256']
      argsTuple: CArgs['tuple']['convertParent()uint256']
      returns: CReturns['convertParent()uint256']
    }>
    & Record<'convertSubA()(uint256,uint256)' | 'convertSubA', {
      argsObj: CArgs['obj']['convertSubA()(uint256,uint256)']
      argsTuple: CArgs['tuple']['convertSubA()(uint256,uint256)']
      returns: CReturns['convertSubA()(uint256,uint256)']
    }>
    & Record<'convertSubB()(uint256,uint256)' | 'convertSubB', {
      argsObj: CArgs['obj']['convertSubB()(uint256,uint256)']
      argsTuple: CArgs['tuple']['convertSubB()(uint256,uint256)']
      returns: CReturns['convertSubB()(uint256,uint256)']
    }>
}

/**
 * Defines the possible abi call signatures.
 */
export type CSignatures = keyof CTypes['methods']
/**
 * Defines the possible abi call signatures for methods that return a non-void value.
 */
export type CNonVoidMethodSignatures = keyof CTypes['methods'] extends infer T ? T extends keyof CTypes['methods'] ? MethodReturn<T> extends void ? never : T  : never : never
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the C smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends CSignatures> = CTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the C smart contract to the method's return type
 */
export type MethodReturn<TSignature extends CSignatures> = CTypes['methods'][TSignature]['returns']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the C smart contract
 */
export abstract class CParamsFactory {
  /**
   * Constructs a no op call for the parentFun()uint256 ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static parentFun(params: CallParams<CArgs['obj']['parentFun()uint256'] | CArgs['tuple']['parentFun()uint256']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'parentFun()uint256' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the subAFun()uint256 ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static subAFun(params: CallParams<CArgs['obj']['subAFun()uint256'] | CArgs['tuple']['subAFun()uint256']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'subAFun()uint256' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the subBFun()uint256 ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static subBFun(params: CallParams<CArgs['obj']['subBFun()uint256'] | CArgs['tuple']['subBFun()uint256']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'subBFun()uint256' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the convertParent()uint256 ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static convertParent(params: CallParams<CArgs['obj']['convertParent()uint256'] | CArgs['tuple']['convertParent()uint256']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'convertParent()uint256' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the convertSubA()(uint256,uint256) ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static convertSubA(params: CallParams<CArgs['obj']['convertSubA()(uint256,uint256)'] | CArgs['tuple']['convertSubA()(uint256,uint256)']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'convertSubA()(uint256,uint256)' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the convertSubB()(uint256,uint256) ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static convertSubB(params: CallParams<CArgs['obj']['convertSubB()(uint256,uint256)'] | CArgs['tuple']['convertSubB()(uint256,uint256)']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'convertSubB()(uint256,uint256)' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
}

/**
 * A client to make calls to the C smart contract
 */
export class CClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `CClient`
   *
   * @param appClient An `AppClient` instance which has been created with the C app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `CClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Checks for decode errors on the given return value and maps the return value to the return type for the given method
   * @returns The typed return value or undefined if there was no value
   */
  decodeReturnValue<TSignature extends CNonVoidMethodSignatures>(method: TSignature, returnValue: ABIReturn | undefined) {
    return returnValue !== undefined ? getArc56ReturnValue<MethodReturn<TSignature>>(returnValue, this.appClient.getABIMethod(method), APP_SPEC.structs) : undefined
  }

  /**
   * Returns a new `CClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<CClient> {
    return new CClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `CClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<CClient> {
    return new CClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Makes a clear_state call to an existing instance of the C smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the C smart contract using the `parentFun()uint256` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    parentFun: (params: CallParams<CArgs['obj']['parentFun()uint256'] | CArgs['tuple']['parentFun()uint256']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(CParamsFactory.parentFun(params))
    },

    /**
     * Makes a call to the C smart contract using the `subAFun()uint256` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    subAFun: (params: CallParams<CArgs['obj']['subAFun()uint256'] | CArgs['tuple']['subAFun()uint256']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(CParamsFactory.subAFun(params))
    },

    /**
     * Makes a call to the C smart contract using the `subBFun()uint256` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    subBFun: (params: CallParams<CArgs['obj']['subBFun()uint256'] | CArgs['tuple']['subBFun()uint256']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(CParamsFactory.subBFun(params))
    },

    /**
     * Makes a call to the C smart contract using the `convertParent()uint256` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    convertParent: (params: CallParams<CArgs['obj']['convertParent()uint256'] | CArgs['tuple']['convertParent()uint256']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(CParamsFactory.convertParent(params))
    },

    /**
     * Makes a call to the C smart contract using the `convertSubA()(uint256,uint256)` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    convertSubA: (params: CallParams<CArgs['obj']['convertSubA()(uint256,uint256)'] | CArgs['tuple']['convertSubA()(uint256,uint256)']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(CParamsFactory.convertSubA(params))
    },

    /**
     * Makes a call to the C smart contract using the `convertSubB()(uint256,uint256)` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    convertSubB: (params: CallParams<CArgs['obj']['convertSubB()(uint256,uint256)'] | CArgs['tuple']['convertSubB()(uint256,uint256)']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(CParamsFactory.convertSubB(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Makes a clear_state call to an existing instance of the C smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the C smart contract using the `parentFun()uint256` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    parentFun: (params: CallParams<CArgs['obj']['parentFun()uint256'] | CArgs['tuple']['parentFun()uint256']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(CParamsFactory.parentFun(params))
    },

    /**
     * Makes a call to the C smart contract using the `subAFun()uint256` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    subAFun: (params: CallParams<CArgs['obj']['subAFun()uint256'] | CArgs['tuple']['subAFun()uint256']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(CParamsFactory.subAFun(params))
    },

    /**
     * Makes a call to the C smart contract using the `subBFun()uint256` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    subBFun: (params: CallParams<CArgs['obj']['subBFun()uint256'] | CArgs['tuple']['subBFun()uint256']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(CParamsFactory.subBFun(params))
    },

    /**
     * Makes a call to the C smart contract using the `convertParent()uint256` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    convertParent: (params: CallParams<CArgs['obj']['convertParent()uint256'] | CArgs['tuple']['convertParent()uint256']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(CParamsFactory.convertParent(params))
    },

    /**
     * Makes a call to the C smart contract using the `convertSubA()(uint256,uint256)` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    convertSubA: (params: CallParams<CArgs['obj']['convertSubA()(uint256,uint256)'] | CArgs['tuple']['convertSubA()(uint256,uint256)']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(CParamsFactory.convertSubA(params))
    },

    /**
     * Makes a call to the C smart contract using the `convertSubB()(uint256,uint256)` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    convertSubB: (params: CallParams<CArgs['obj']['convertSubB()(uint256,uint256)'] | CArgs['tuple']['convertSubB()(uint256,uint256)']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(CParamsFactory.convertSubB(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Makes a clear_state call to an existing instance of the C smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the C smart contract using the `parentFun()uint256` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    parentFun: async (params: CallParams<CArgs['obj']['parentFun()uint256'] | CArgs['tuple']['parentFun()uint256']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(CParamsFactory.parentFun(params))
      return {...result, return: result.return as unknown as (undefined | CReturns['parentFun()uint256'])}
    },

    /**
     * Makes a call to the C smart contract using the `subAFun()uint256` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    subAFun: async (params: CallParams<CArgs['obj']['subAFun()uint256'] | CArgs['tuple']['subAFun()uint256']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(CParamsFactory.subAFun(params))
      return {...result, return: result.return as unknown as (undefined | CReturns['subAFun()uint256'])}
    },

    /**
     * Makes a call to the C smart contract using the `subBFun()uint256` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    subBFun: async (params: CallParams<CArgs['obj']['subBFun()uint256'] | CArgs['tuple']['subBFun()uint256']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(CParamsFactory.subBFun(params))
      return {...result, return: result.return as unknown as (undefined | CReturns['subBFun()uint256'])}
    },

    /**
     * Makes a call to the C smart contract using the `convertParent()uint256` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    convertParent: async (params: CallParams<CArgs['obj']['convertParent()uint256'] | CArgs['tuple']['convertParent()uint256']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(CParamsFactory.convertParent(params))
      return {...result, return: result.return as unknown as (undefined | CReturns['convertParent()uint256'])}
    },

    /**
     * Makes a call to the C smart contract using the `convertSubA()(uint256,uint256)` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    convertSubA: async (params: CallParams<CArgs['obj']['convertSubA()(uint256,uint256)'] | CArgs['tuple']['convertSubA()(uint256,uint256)']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(CParamsFactory.convertSubA(params))
      return {...result, return: result.return as unknown as (undefined | CReturns['convertSubA()(uint256,uint256)'])}
    },

    /**
     * Makes a call to the C smart contract using the `convertSubB()(uint256,uint256)` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    convertSubB: async (params: CallParams<CArgs['obj']['convertSubB()(uint256,uint256)'] | CArgs['tuple']['convertSubB()(uint256,uint256)']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(CParamsFactory.convertSubB(params))
      return {...result, return: result.return as unknown as (undefined | CReturns['convertSubB()(uint256,uint256)'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new CClient(this.appClient.clone(params))
  }

  /**
   * Methods to access state for the current C app
   */
  state = {
  }

  public newGroup(): CComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a parentFun()uint256 method call against the C contract
       */
      parentFun(params: CallParams<CArgs['obj']['parentFun()uint256'] | CArgs['tuple']['parentFun()uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.parentFun(params)))
        resultMappers.push((v) => client.decodeReturnValue('parentFun()uint256', v))
        return this
      },
      /**
       * Add a subAFun()uint256 method call against the C contract
       */
      subAFun(params: CallParams<CArgs['obj']['subAFun()uint256'] | CArgs['tuple']['subAFun()uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.subAFun(params)))
        resultMappers.push((v) => client.decodeReturnValue('subAFun()uint256', v))
        return this
      },
      /**
       * Add a subBFun()uint256 method call against the C contract
       */
      subBFun(params: CallParams<CArgs['obj']['subBFun()uint256'] | CArgs['tuple']['subBFun()uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.subBFun(params)))
        resultMappers.push((v) => client.decodeReturnValue('subBFun()uint256', v))
        return this
      },
      /**
       * Add a convertParent()uint256 method call against the C contract
       */
      convertParent(params: CallParams<CArgs['obj']['convertParent()uint256'] | CArgs['tuple']['convertParent()uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.convertParent(params)))
        resultMappers.push((v) => client.decodeReturnValue('convertParent()uint256', v))
        return this
      },
      /**
       * Add a convertSubA()(uint256,uint256) method call against the C contract
       */
      convertSubA(params: CallParams<CArgs['obj']['convertSubA()(uint256,uint256)'] | CArgs['tuple']['convertSubA()(uint256,uint256)']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.convertSubA(params)))
        resultMappers.push((v) => client.decodeReturnValue('convertSubA()(uint256,uint256)', v))
        return this
      },
      /**
       * Add a convertSubB()(uint256,uint256) method call against the C contract
       */
      convertSubB(params: CallParams<CArgs['obj']['convertSubB()(uint256,uint256)'] | CArgs['tuple']['convertSubB()(uint256,uint256)']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.convertSubB(params)))
        resultMappers.push((v) => client.decodeReturnValue('convertSubB()(uint256,uint256)', v))
        return this
      },
      /**
       * Add a clear state call to the C contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as CComposer
  }
}
export type CComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the parentFun()uint256 ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  parentFun(params?: CallParams<CArgs['obj']['parentFun()uint256'] | CArgs['tuple']['parentFun()uint256']>): CComposer<[...TReturns, CReturns['parentFun()uint256'] | undefined]>

  /**
   * Calls the subAFun()uint256 ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  subAFun(params?: CallParams<CArgs['obj']['subAFun()uint256'] | CArgs['tuple']['subAFun()uint256']>): CComposer<[...TReturns, CReturns['subAFun()uint256'] | undefined]>

  /**
   * Calls the subBFun()uint256 ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  subBFun(params?: CallParams<CArgs['obj']['subBFun()uint256'] | CArgs['tuple']['subBFun()uint256']>): CComposer<[...TReturns, CReturns['subBFun()uint256'] | undefined]>

  /**
   * Calls the convertParent()uint256 ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  convertParent(params?: CallParams<CArgs['obj']['convertParent()uint256'] | CArgs['tuple']['convertParent()uint256']>): CComposer<[...TReturns, CReturns['convertParent()uint256'] | undefined]>

  /**
   * Calls the convertSubA()(uint256,uint256) ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  convertSubA(params?: CallParams<CArgs['obj']['convertSubA()(uint256,uint256)'] | CArgs['tuple']['convertSubA()(uint256,uint256)']>): CComposer<[...TReturns, CReturns['convertSubA()(uint256,uint256)'] | undefined]>

  /**
   * Calls the convertSubB()(uint256,uint256) ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  convertSubB(params?: CallParams<CArgs['obj']['convertSubB()(uint256,uint256)'] | CArgs['tuple']['convertSubB()(uint256,uint256)']>): CComposer<[...TReturns, CReturns['convertSubB()(uint256,uint256)'] | undefined]>

  /**
   * Makes a clear_state call to an existing instance of the C smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): CComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): CComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<CComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<CComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<CComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<CComposerResults<TReturns>>
}
export type CComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

