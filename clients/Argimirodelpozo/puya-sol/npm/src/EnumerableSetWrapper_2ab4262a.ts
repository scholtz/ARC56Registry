/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"EnumerableSetWrapper","structs":{"Uint256Set":[{"name":"_spacer","type":"uint256"}]},"methods":[{"name":"add","args":[{"type":"uint256","name":"value"}],"returns":{"type":"bool"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}},{"name":"remove","args":[{"type":"uint256","name":"value"}],"returns":{"type":"bool"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}},{"name":"contains","args":[{"type":"uint256","name":"value"}],"returns":{"type":"bool"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"length","args":[],"returns":{"type":"uint256"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"at","args":[{"type":"uint256","name":"index"}],"returns":{"type":"uint256"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}}],"arcs":[22,28],"networks":{},"state":{"schema":{"global":{"ints":0,"bytes":1},"local":{"ints":0,"bytes":0}},"keys":{"global":{"set":{"keyType":"AVMString","valueType":"Uint256Set","key":"c2V0"}},"local":{},"box":{}},"maps":{"global":{},"local":{},"box":{}}},"bareActions":{"create":[],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[377],"errorMessage":"IndexOutOfBounds"},{"pc":[459,488,517,538,568],"errorMessage":"check set exists"},{"pc":[455,484,513,564],"errorMessage":"invalid number of bytes for arc4.uint256"},{"pc":[547,578],"errorMessage":"overflow"},{"pc":[140,230,281],"errorMessage":"revert"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"events":[]} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


// Type definitions for ARC-56 structs

export type Uint256Set = {
  _spacer: bigint
}


/**
 * Converts the ABI tuple representation of a Uint256Set to the struct representation
 */
export function Uint256SetFromTuple(abiTuple: [bigint]) {
  return getABIStructFromABITuple(abiTuple, APP_SPEC.structs.Uint256Set, APP_SPEC.structs) as Uint256Set
}

/**
 * The argument types for the EnumerableSetWrapper contract
 */
export type EnumerableSetWrapperArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'add(uint256)bool': {
      value: bigint | number
    }
    'remove(uint256)bool': {
      value: bigint | number
    }
    'contains(uint256)bool': {
      value: bigint | number
    }
    'length()uint256': Record<string, never>
    'at(uint256)uint256': {
      index: bigint | number
    }
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'add(uint256)bool': [value: bigint | number]
    'remove(uint256)bool': [value: bigint | number]
    'contains(uint256)bool': [value: bigint | number]
    'length()uint256': []
    'at(uint256)uint256': [index: bigint | number]
  }
}

/**
 * The return type for each method
 */
export type EnumerableSetWrapperReturns = {
  'add(uint256)bool': boolean
  'remove(uint256)bool': boolean
  'contains(uint256)bool': boolean
  'length()uint256': bigint
  'at(uint256)uint256': bigint
}

/**
 * Defines the types of available calls and state of the EnumerableSetWrapper smart contract.
 */
export type EnumerableSetWrapperTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'add(uint256)bool' | 'add', {
      argsObj: EnumerableSetWrapperArgs['obj']['add(uint256)bool']
      argsTuple: EnumerableSetWrapperArgs['tuple']['add(uint256)bool']
      returns: EnumerableSetWrapperReturns['add(uint256)bool']
    }>
    & Record<'remove(uint256)bool' | 'remove', {
      argsObj: EnumerableSetWrapperArgs['obj']['remove(uint256)bool']
      argsTuple: EnumerableSetWrapperArgs['tuple']['remove(uint256)bool']
      returns: EnumerableSetWrapperReturns['remove(uint256)bool']
    }>
    & Record<'contains(uint256)bool' | 'contains', {
      argsObj: EnumerableSetWrapperArgs['obj']['contains(uint256)bool']
      argsTuple: EnumerableSetWrapperArgs['tuple']['contains(uint256)bool']
      returns: EnumerableSetWrapperReturns['contains(uint256)bool']
    }>
    & Record<'length()uint256' | 'length', {
      argsObj: EnumerableSetWrapperArgs['obj']['length()uint256']
      argsTuple: EnumerableSetWrapperArgs['tuple']['length()uint256']
      returns: EnumerableSetWrapperReturns['length()uint256']
    }>
    & Record<'at(uint256)uint256' | 'at', {
      argsObj: EnumerableSetWrapperArgs['obj']['at(uint256)uint256']
      argsTuple: EnumerableSetWrapperArgs['tuple']['at(uint256)uint256']
      returns: EnumerableSetWrapperReturns['at(uint256)uint256']
    }>
  /**
   * Defines the shape of the state of the application.
   */
  state: {
    global: {
      keys: {
        set: Uint256Set
      }
      maps: {}
    }
  }
}

/**
 * Defines the possible abi call signatures.
 */
export type EnumerableSetWrapperSignatures = keyof EnumerableSetWrapperTypes['methods']
/**
 * Defines the possible abi call signatures for methods that return a non-void value.
 */
export type EnumerableSetWrapperNonVoidMethodSignatures = keyof EnumerableSetWrapperTypes['methods'] extends infer T ? T extends keyof EnumerableSetWrapperTypes['methods'] ? MethodReturn<T> extends void ? never : T  : never : never
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the EnumerableSetWrapper smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends EnumerableSetWrapperSignatures> = EnumerableSetWrapperTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the EnumerableSetWrapper smart contract to the method's return type
 */
export type MethodReturn<TSignature extends EnumerableSetWrapperSignatures> = EnumerableSetWrapperTypes['methods'][TSignature]['returns']

/**
 * Defines the shape of the keyed global state of the application.
 */
export type GlobalKeysState = EnumerableSetWrapperTypes['state']['global']['keys']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the EnumerableSetWrapper smart contract
 */
export abstract class EnumerableSetWrapperParamsFactory {
  /**
   * Constructs a no op call for the add(uint256)bool ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static add(params: CallParams<EnumerableSetWrapperArgs['obj']['add(uint256)bool'] | EnumerableSetWrapperArgs['tuple']['add(uint256)bool']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'add(uint256)bool' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.value],
    }
  }
  /**
   * Constructs a no op call for the remove(uint256)bool ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static remove(params: CallParams<EnumerableSetWrapperArgs['obj']['remove(uint256)bool'] | EnumerableSetWrapperArgs['tuple']['remove(uint256)bool']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'remove(uint256)bool' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.value],
    }
  }
  /**
   * Constructs a no op call for the contains(uint256)bool ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static contains(params: CallParams<EnumerableSetWrapperArgs['obj']['contains(uint256)bool'] | EnumerableSetWrapperArgs['tuple']['contains(uint256)bool']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'contains(uint256)bool' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.value],
    }
  }
  /**
   * Constructs a no op call for the length()uint256 ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static length(params: CallParams<EnumerableSetWrapperArgs['obj']['length()uint256'] | EnumerableSetWrapperArgs['tuple']['length()uint256']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'length()uint256' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the at(uint256)uint256 ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static at(params: CallParams<EnumerableSetWrapperArgs['obj']['at(uint256)uint256'] | EnumerableSetWrapperArgs['tuple']['at(uint256)uint256']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'at(uint256)uint256' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.index],
    }
  }
}

/**
 * A client to make calls to the EnumerableSetWrapper smart contract
 */
export class EnumerableSetWrapperClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `EnumerableSetWrapperClient`
   *
   * @param appClient An `AppClient` instance which has been created with the EnumerableSetWrapper app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `EnumerableSetWrapperClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Checks for decode errors on the given return value and maps the return value to the return type for the given method
   * @returns The typed return value or undefined if there was no value
   */
  decodeReturnValue<TSignature extends EnumerableSetWrapperNonVoidMethodSignatures>(method: TSignature, returnValue: ABIReturn | undefined) {
    return returnValue !== undefined ? getArc56ReturnValue<MethodReturn<TSignature>>(returnValue, this.appClient.getABIMethod(method), APP_SPEC.structs) : undefined
  }

  /**
   * Returns a new `EnumerableSetWrapperClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<EnumerableSetWrapperClient> {
    return new EnumerableSetWrapperClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `EnumerableSetWrapperClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<EnumerableSetWrapperClient> {
    return new EnumerableSetWrapperClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Makes a clear_state call to an existing instance of the EnumerableSetWrapper smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the EnumerableSetWrapper smart contract using the `add(uint256)bool` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    add: (params: CallParams<EnumerableSetWrapperArgs['obj']['add(uint256)bool'] | EnumerableSetWrapperArgs['tuple']['add(uint256)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(EnumerableSetWrapperParamsFactory.add(params))
    },

    /**
     * Makes a call to the EnumerableSetWrapper smart contract using the `remove(uint256)bool` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    remove: (params: CallParams<EnumerableSetWrapperArgs['obj']['remove(uint256)bool'] | EnumerableSetWrapperArgs['tuple']['remove(uint256)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(EnumerableSetWrapperParamsFactory.remove(params))
    },

    /**
     * Makes a call to the EnumerableSetWrapper smart contract using the `contains(uint256)bool` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    contains: (params: CallParams<EnumerableSetWrapperArgs['obj']['contains(uint256)bool'] | EnumerableSetWrapperArgs['tuple']['contains(uint256)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(EnumerableSetWrapperParamsFactory.contains(params))
    },

    /**
     * Makes a call to the EnumerableSetWrapper smart contract using the `length()uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    length: (params: CallParams<EnumerableSetWrapperArgs['obj']['length()uint256'] | EnumerableSetWrapperArgs['tuple']['length()uint256']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(EnumerableSetWrapperParamsFactory.length(params))
    },

    /**
     * Makes a call to the EnumerableSetWrapper smart contract using the `at(uint256)uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    at: (params: CallParams<EnumerableSetWrapperArgs['obj']['at(uint256)uint256'] | EnumerableSetWrapperArgs['tuple']['at(uint256)uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(EnumerableSetWrapperParamsFactory.at(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Makes a clear_state call to an existing instance of the EnumerableSetWrapper smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the EnumerableSetWrapper smart contract using the `add(uint256)bool` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    add: (params: CallParams<EnumerableSetWrapperArgs['obj']['add(uint256)bool'] | EnumerableSetWrapperArgs['tuple']['add(uint256)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(EnumerableSetWrapperParamsFactory.add(params))
    },

    /**
     * Makes a call to the EnumerableSetWrapper smart contract using the `remove(uint256)bool` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    remove: (params: CallParams<EnumerableSetWrapperArgs['obj']['remove(uint256)bool'] | EnumerableSetWrapperArgs['tuple']['remove(uint256)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(EnumerableSetWrapperParamsFactory.remove(params))
    },

    /**
     * Makes a call to the EnumerableSetWrapper smart contract using the `contains(uint256)bool` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    contains: (params: CallParams<EnumerableSetWrapperArgs['obj']['contains(uint256)bool'] | EnumerableSetWrapperArgs['tuple']['contains(uint256)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(EnumerableSetWrapperParamsFactory.contains(params))
    },

    /**
     * Makes a call to the EnumerableSetWrapper smart contract using the `length()uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    length: (params: CallParams<EnumerableSetWrapperArgs['obj']['length()uint256'] | EnumerableSetWrapperArgs['tuple']['length()uint256']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(EnumerableSetWrapperParamsFactory.length(params))
    },

    /**
     * Makes a call to the EnumerableSetWrapper smart contract using the `at(uint256)uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    at: (params: CallParams<EnumerableSetWrapperArgs['obj']['at(uint256)uint256'] | EnumerableSetWrapperArgs['tuple']['at(uint256)uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(EnumerableSetWrapperParamsFactory.at(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Makes a clear_state call to an existing instance of the EnumerableSetWrapper smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the EnumerableSetWrapper smart contract using the `add(uint256)bool` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    add: async (params: CallParams<EnumerableSetWrapperArgs['obj']['add(uint256)bool'] | EnumerableSetWrapperArgs['tuple']['add(uint256)bool']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(EnumerableSetWrapperParamsFactory.add(params))
      return {...result, return: result.return as unknown as (undefined | EnumerableSetWrapperReturns['add(uint256)bool'])}
    },

    /**
     * Makes a call to the EnumerableSetWrapper smart contract using the `remove(uint256)bool` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    remove: async (params: CallParams<EnumerableSetWrapperArgs['obj']['remove(uint256)bool'] | EnumerableSetWrapperArgs['tuple']['remove(uint256)bool']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(EnumerableSetWrapperParamsFactory.remove(params))
      return {...result, return: result.return as unknown as (undefined | EnumerableSetWrapperReturns['remove(uint256)bool'])}
    },

    /**
     * Makes a call to the EnumerableSetWrapper smart contract using the `contains(uint256)bool` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    contains: async (params: CallParams<EnumerableSetWrapperArgs['obj']['contains(uint256)bool'] | EnumerableSetWrapperArgs['tuple']['contains(uint256)bool']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(EnumerableSetWrapperParamsFactory.contains(params))
      return {...result, return: result.return as unknown as (undefined | EnumerableSetWrapperReturns['contains(uint256)bool'])}
    },

    /**
     * Makes a call to the EnumerableSetWrapper smart contract using the `length()uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    length: async (params: CallParams<EnumerableSetWrapperArgs['obj']['length()uint256'] | EnumerableSetWrapperArgs['tuple']['length()uint256']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(EnumerableSetWrapperParamsFactory.length(params))
      return {...result, return: result.return as unknown as (undefined | EnumerableSetWrapperReturns['length()uint256'])}
    },

    /**
     * Makes a call to the EnumerableSetWrapper smart contract using the `at(uint256)uint256` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    at: async (params: CallParams<EnumerableSetWrapperArgs['obj']['at(uint256)uint256'] | EnumerableSetWrapperArgs['tuple']['at(uint256)uint256']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(EnumerableSetWrapperParamsFactory.at(params))
      return {...result, return: result.return as unknown as (undefined | EnumerableSetWrapperReturns['at(uint256)uint256'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new EnumerableSetWrapperClient(this.appClient.clone(params))
  }

  /**
   * Makes a readonly (simulated) call to the EnumerableSetWrapper smart contract using the `contains(uint256)bool` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async contains(params: CallParams<EnumerableSetWrapperArgs['obj']['contains(uint256)bool'] | EnumerableSetWrapperArgs['tuple']['contains(uint256)bool']>) {
    const result = await this.appClient.send.call(EnumerableSetWrapperParamsFactory.contains(params))
    return result.return as unknown as EnumerableSetWrapperReturns['contains(uint256)bool']
  }

  /**
   * Makes a readonly (simulated) call to the EnumerableSetWrapper smart contract using the `length()uint256` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async length(params: CallParams<EnumerableSetWrapperArgs['obj']['length()uint256'] | EnumerableSetWrapperArgs['tuple']['length()uint256']> = {args: []}) {
    const result = await this.appClient.send.call(EnumerableSetWrapperParamsFactory.length(params))
    return result.return as unknown as EnumerableSetWrapperReturns['length()uint256']
  }

  /**
   * Makes a readonly (simulated) call to the EnumerableSetWrapper smart contract using the `at(uint256)uint256` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async at(params: CallParams<EnumerableSetWrapperArgs['obj']['at(uint256)uint256'] | EnumerableSetWrapperArgs['tuple']['at(uint256)uint256']>) {
    const result = await this.appClient.send.call(EnumerableSetWrapperParamsFactory.at(params))
    return result.return as unknown as EnumerableSetWrapperReturns['at(uint256)uint256']
  }

  /**
   * Methods to access state for the current EnumerableSetWrapper app
   */
  state = {
    /**
     * Methods to access global state for the current EnumerableSetWrapper app
     */
    global: {
      /**
       * Get all current keyed values from global state
       */
      getAll: async (): Promise<Partial<Expand<GlobalKeysState>>> => {
        const result = await this.appClient.state.global.getAll()
        return {
          set: result.set,
        }
      },
      /**
       * Get the current value of the set key in global state
       */
      set: async (): Promise<Uint256Set | undefined> => { return (await this.appClient.state.global.getValue("set")) as Uint256Set | undefined },
    },
  }

  public newGroup(): EnumerableSetWrapperComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a add(uint256)bool method call against the EnumerableSetWrapper contract
       */
      add(params: CallParams<EnumerableSetWrapperArgs['obj']['add(uint256)bool'] | EnumerableSetWrapperArgs['tuple']['add(uint256)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.add(params)))
        resultMappers.push((v) => client.decodeReturnValue('add(uint256)bool', v))
        return this
      },
      /**
       * Add a remove(uint256)bool method call against the EnumerableSetWrapper contract
       */
      remove(params: CallParams<EnumerableSetWrapperArgs['obj']['remove(uint256)bool'] | EnumerableSetWrapperArgs['tuple']['remove(uint256)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.remove(params)))
        resultMappers.push((v) => client.decodeReturnValue('remove(uint256)bool', v))
        return this
      },
      /**
       * Add a contains(uint256)bool method call against the EnumerableSetWrapper contract
       */
      contains(params: CallParams<EnumerableSetWrapperArgs['obj']['contains(uint256)bool'] | EnumerableSetWrapperArgs['tuple']['contains(uint256)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.contains(params)))
        resultMappers.push((v) => client.decodeReturnValue('contains(uint256)bool', v))
        return this
      },
      /**
       * Add a length()uint256 method call against the EnumerableSetWrapper contract
       */
      length(params: CallParams<EnumerableSetWrapperArgs['obj']['length()uint256'] | EnumerableSetWrapperArgs['tuple']['length()uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.length(params)))
        resultMappers.push((v) => client.decodeReturnValue('length()uint256', v))
        return this
      },
      /**
       * Add a at(uint256)uint256 method call against the EnumerableSetWrapper contract
       */
      at(params: CallParams<EnumerableSetWrapperArgs['obj']['at(uint256)uint256'] | EnumerableSetWrapperArgs['tuple']['at(uint256)uint256']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.at(params)))
        resultMappers.push((v) => client.decodeReturnValue('at(uint256)uint256', v))
        return this
      },
      /**
       * Add a clear state call to the EnumerableSetWrapper contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as EnumerableSetWrapperComposer
  }
}
export type EnumerableSetWrapperComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the add(uint256)bool ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  add(params?: CallParams<EnumerableSetWrapperArgs['obj']['add(uint256)bool'] | EnumerableSetWrapperArgs['tuple']['add(uint256)bool']>): EnumerableSetWrapperComposer<[...TReturns, EnumerableSetWrapperReturns['add(uint256)bool'] | undefined]>

  /**
   * Calls the remove(uint256)bool ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  remove(params?: CallParams<EnumerableSetWrapperArgs['obj']['remove(uint256)bool'] | EnumerableSetWrapperArgs['tuple']['remove(uint256)bool']>): EnumerableSetWrapperComposer<[...TReturns, EnumerableSetWrapperReturns['remove(uint256)bool'] | undefined]>

  /**
   * Calls the contains(uint256)bool ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  contains(params?: CallParams<EnumerableSetWrapperArgs['obj']['contains(uint256)bool'] | EnumerableSetWrapperArgs['tuple']['contains(uint256)bool']>): EnumerableSetWrapperComposer<[...TReturns, EnumerableSetWrapperReturns['contains(uint256)bool'] | undefined]>

  /**
   * Calls the length()uint256 ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  length(params?: CallParams<EnumerableSetWrapperArgs['obj']['length()uint256'] | EnumerableSetWrapperArgs['tuple']['length()uint256']>): EnumerableSetWrapperComposer<[...TReturns, EnumerableSetWrapperReturns['length()uint256'] | undefined]>

  /**
   * Calls the at(uint256)uint256 ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  at(params?: CallParams<EnumerableSetWrapperArgs['obj']['at(uint256)uint256'] | EnumerableSetWrapperArgs['tuple']['at(uint256)uint256']>): EnumerableSetWrapperComposer<[...TReturns, EnumerableSetWrapperReturns['at(uint256)uint256'] | undefined]>

  /**
   * Makes a clear_state call to an existing instance of the EnumerableSetWrapper smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): EnumerableSetWrapperComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): EnumerableSetWrapperComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<EnumerableSetWrapperComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<EnumerableSetWrapperComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<EnumerableSetWrapperComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<EnumerableSetWrapperComposerResults<TReturns>>
}
export type EnumerableSetWrapperComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

