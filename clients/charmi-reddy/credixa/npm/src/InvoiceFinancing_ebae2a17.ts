/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"InvoiceFinancing","structs":{},"methods":[{"name":"create_invoice","args":[{"type":"uint64","name":"amount"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}},{"name":"request_financing","args":[],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}},{"name":"fund_invoice","args":[{"type":"pay","name":"payment"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}},{"name":"settle_invoice","args":[{"type":"pay","name":"payment"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}}],"arcs":[22,28],"networks":{},"state":{"schema":{"global":{"ints":1,"bytes":2},"local":{"ints":0,"bytes":0}},"keys":{"global":{"owner":{"keyType":"AVMString","valueType":"address","key":"b3duZXI="},"amount":{"keyType":"AVMString","valueType":"AVMUint64","key":"YW1vdW50"},"status":{"keyType":"AVMString","valueType":"AVMString","key":"c3RhdHVz"}},"local":{},"box":{}},"maps":{"global":{},"local":{},"box":{}}},"bareActions":{"create":["NoOp"],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[228],"errorMessage":"Invoice must be Funded to be settled"},{"pc":[159],"errorMessage":"Invoice must be in Created state"},{"pc":[183],"errorMessage":"Invoice not available for funding"},{"pc":[152],"errorMessage":"Only owner can request financing"},{"pc":[200,245],"errorMessage":"Payment amount must match invoice amount"},{"pc":[192],"errorMessage":"Payment must be to the current owner"},{"pc":[237],"errorMessage":"Payment must be to the current owner (investor)"},{"pc":[198,243],"errorMessage":"check self.amount exists"},{"pc":[150,190,235],"errorMessage":"check self.owner exists"},{"pc":[156,179,224],"errorMessage":"check self.status exists"},{"pc":[131],"errorMessage":"invalid number of bytes for arc4.uint64"},{"pc":[175,220],"errorMessage":"transaction type is pay"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"events":[]} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


/**
 * The argument types for the InvoiceFinancing contract
 */
export type InvoiceFinancingArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'create_invoice(uint64)void': {
      amount: bigint | number
    }
    'request_financing()void': Record<string, never>
    'fund_invoice(pay)void': {
      payment: AppMethodCallTransactionArgument
    }
    'settle_invoice(pay)void': {
      payment: AppMethodCallTransactionArgument
    }
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'create_invoice(uint64)void': [amount: bigint | number]
    'request_financing()void': []
    'fund_invoice(pay)void': [payment: AppMethodCallTransactionArgument]
    'settle_invoice(pay)void': [payment: AppMethodCallTransactionArgument]
  }
}

/**
 * The return type for each method
 */
export type InvoiceFinancingReturns = {
  'create_invoice(uint64)void': void
  'request_financing()void': void
  'fund_invoice(pay)void': void
  'settle_invoice(pay)void': void
}

/**
 * Defines the types of available calls and state of the InvoiceFinancing smart contract.
 */
export type InvoiceFinancingTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'create_invoice(uint64)void' | 'create_invoice', {
      argsObj: InvoiceFinancingArgs['obj']['create_invoice(uint64)void']
      argsTuple: InvoiceFinancingArgs['tuple']['create_invoice(uint64)void']
      returns: InvoiceFinancingReturns['create_invoice(uint64)void']
    }>
    & Record<'request_financing()void' | 'request_financing', {
      argsObj: InvoiceFinancingArgs['obj']['request_financing()void']
      argsTuple: InvoiceFinancingArgs['tuple']['request_financing()void']
      returns: InvoiceFinancingReturns['request_financing()void']
    }>
    & Record<'fund_invoice(pay)void' | 'fund_invoice', {
      argsObj: InvoiceFinancingArgs['obj']['fund_invoice(pay)void']
      argsTuple: InvoiceFinancingArgs['tuple']['fund_invoice(pay)void']
      returns: InvoiceFinancingReturns['fund_invoice(pay)void']
    }>
    & Record<'settle_invoice(pay)void' | 'settle_invoice', {
      argsObj: InvoiceFinancingArgs['obj']['settle_invoice(pay)void']
      argsTuple: InvoiceFinancingArgs['tuple']['settle_invoice(pay)void']
      returns: InvoiceFinancingReturns['settle_invoice(pay)void']
    }>
  /**
   * Defines the shape of the state of the application.
   */
  state: {
    global: {
      keys: {
        owner: string
        amount: bigint
        status: string
      }
      maps: {}
    }
  }
}

/**
 * Defines the possible abi call signatures.
 */
export type InvoiceFinancingSignatures = keyof InvoiceFinancingTypes['methods']
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the InvoiceFinancing smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends InvoiceFinancingSignatures> = InvoiceFinancingTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the InvoiceFinancing smart contract to the method's return type
 */
export type MethodReturn<TSignature extends InvoiceFinancingSignatures> = InvoiceFinancingTypes['methods'][TSignature]['returns']

/**
 * Defines the shape of the keyed global state of the application.
 */
export type GlobalKeysState = InvoiceFinancingTypes['state']['global']['keys']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the InvoiceFinancing smart contract
 */
export abstract class InvoiceFinancingParamsFactory {
  /**
   * Constructs a no op call for the create_invoice(uint64)void ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static createInvoice(params: CallParams<InvoiceFinancingArgs['obj']['create_invoice(uint64)void'] | InvoiceFinancingArgs['tuple']['create_invoice(uint64)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'create_invoice(uint64)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.amount],
    }
  }
  /**
   * Constructs a no op call for the request_financing()void ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static requestFinancing(params: CallParams<InvoiceFinancingArgs['obj']['request_financing()void'] | InvoiceFinancingArgs['tuple']['request_financing()void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'request_financing()void' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the fund_invoice(pay)void ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static fundInvoice(params: CallParams<InvoiceFinancingArgs['obj']['fund_invoice(pay)void'] | InvoiceFinancingArgs['tuple']['fund_invoice(pay)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'fund_invoice(pay)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.payment],
    }
  }
  /**
   * Constructs a no op call for the settle_invoice(pay)void ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static settleInvoice(params: CallParams<InvoiceFinancingArgs['obj']['settle_invoice(pay)void'] | InvoiceFinancingArgs['tuple']['settle_invoice(pay)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'settle_invoice(pay)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.payment],
    }
  }
}

/**
 * A client to make calls to the InvoiceFinancing smart contract
 */
export class InvoiceFinancingClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `InvoiceFinancingClient`
   *
   * @param appClient An `AppClient` instance which has been created with the InvoiceFinancing app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `InvoiceFinancingClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Returns a new `InvoiceFinancingClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<InvoiceFinancingClient> {
    return new InvoiceFinancingClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `InvoiceFinancingClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<InvoiceFinancingClient> {
    return new InvoiceFinancingClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Makes a clear_state call to an existing instance of the InvoiceFinancing smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the InvoiceFinancing smart contract using the `create_invoice(uint64)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    createInvoice: (params: CallParams<InvoiceFinancingArgs['obj']['create_invoice(uint64)void'] | InvoiceFinancingArgs['tuple']['create_invoice(uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(InvoiceFinancingParamsFactory.createInvoice(params))
    },

    /**
     * Makes a call to the InvoiceFinancing smart contract using the `request_financing()void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    requestFinancing: (params: CallParams<InvoiceFinancingArgs['obj']['request_financing()void'] | InvoiceFinancingArgs['tuple']['request_financing()void']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(InvoiceFinancingParamsFactory.requestFinancing(params))
    },

    /**
     * Makes a call to the InvoiceFinancing smart contract using the `fund_invoice(pay)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    fundInvoice: (params: CallParams<InvoiceFinancingArgs['obj']['fund_invoice(pay)void'] | InvoiceFinancingArgs['tuple']['fund_invoice(pay)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(InvoiceFinancingParamsFactory.fundInvoice(params))
    },

    /**
     * Makes a call to the InvoiceFinancing smart contract using the `settle_invoice(pay)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    settleInvoice: (params: CallParams<InvoiceFinancingArgs['obj']['settle_invoice(pay)void'] | InvoiceFinancingArgs['tuple']['settle_invoice(pay)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(InvoiceFinancingParamsFactory.settleInvoice(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Makes a clear_state call to an existing instance of the InvoiceFinancing smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the InvoiceFinancing smart contract using the `create_invoice(uint64)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    createInvoice: (params: CallParams<InvoiceFinancingArgs['obj']['create_invoice(uint64)void'] | InvoiceFinancingArgs['tuple']['create_invoice(uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(InvoiceFinancingParamsFactory.createInvoice(params))
    },

    /**
     * Makes a call to the InvoiceFinancing smart contract using the `request_financing()void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    requestFinancing: (params: CallParams<InvoiceFinancingArgs['obj']['request_financing()void'] | InvoiceFinancingArgs['tuple']['request_financing()void']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(InvoiceFinancingParamsFactory.requestFinancing(params))
    },

    /**
     * Makes a call to the InvoiceFinancing smart contract using the `fund_invoice(pay)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    fundInvoice: (params: CallParams<InvoiceFinancingArgs['obj']['fund_invoice(pay)void'] | InvoiceFinancingArgs['tuple']['fund_invoice(pay)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(InvoiceFinancingParamsFactory.fundInvoice(params))
    },

    /**
     * Makes a call to the InvoiceFinancing smart contract using the `settle_invoice(pay)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    settleInvoice: (params: CallParams<InvoiceFinancingArgs['obj']['settle_invoice(pay)void'] | InvoiceFinancingArgs['tuple']['settle_invoice(pay)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(InvoiceFinancingParamsFactory.settleInvoice(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Makes a clear_state call to an existing instance of the InvoiceFinancing smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the InvoiceFinancing smart contract using the `create_invoice(uint64)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    createInvoice: async (params: CallParams<InvoiceFinancingArgs['obj']['create_invoice(uint64)void'] | InvoiceFinancingArgs['tuple']['create_invoice(uint64)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(InvoiceFinancingParamsFactory.createInvoice(params))
      return {...result, return: result.return as unknown as (undefined | InvoiceFinancingReturns['create_invoice(uint64)void'])}
    },

    /**
     * Makes a call to the InvoiceFinancing smart contract using the `request_financing()void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    requestFinancing: async (params: CallParams<InvoiceFinancingArgs['obj']['request_financing()void'] | InvoiceFinancingArgs['tuple']['request_financing()void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(InvoiceFinancingParamsFactory.requestFinancing(params))
      return {...result, return: result.return as unknown as (undefined | InvoiceFinancingReturns['request_financing()void'])}
    },

    /**
     * Makes a call to the InvoiceFinancing smart contract using the `fund_invoice(pay)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    fundInvoice: async (params: CallParams<InvoiceFinancingArgs['obj']['fund_invoice(pay)void'] | InvoiceFinancingArgs['tuple']['fund_invoice(pay)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(InvoiceFinancingParamsFactory.fundInvoice(params))
      return {...result, return: result.return as unknown as (undefined | InvoiceFinancingReturns['fund_invoice(pay)void'])}
    },

    /**
     * Makes a call to the InvoiceFinancing smart contract using the `settle_invoice(pay)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    settleInvoice: async (params: CallParams<InvoiceFinancingArgs['obj']['settle_invoice(pay)void'] | InvoiceFinancingArgs['tuple']['settle_invoice(pay)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(InvoiceFinancingParamsFactory.settleInvoice(params))
      return {...result, return: result.return as unknown as (undefined | InvoiceFinancingReturns['settle_invoice(pay)void'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new InvoiceFinancingClient(this.appClient.clone(params))
  }

  /**
   * Methods to access state for the current InvoiceFinancing app
   */
  state = {
    /**
     * Methods to access global state for the current InvoiceFinancing app
     */
    global: {
      /**
       * Get all current keyed values from global state
       */
      getAll: async (): Promise<Partial<Expand<GlobalKeysState>>> => {
        const result = await this.appClient.state.global.getAll()
        return {
          owner: result.owner,
          amount: result.amount,
          status: result.status,
        }
      },
      /**
       * Get the current value of the owner key in global state
       */
      owner: async (): Promise<string | undefined> => { return (await this.appClient.state.global.getValue("owner")) as string | undefined },
      /**
       * Get the current value of the amount key in global state
       */
      amount: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("amount")) as bigint | undefined },
      /**
       * Get the current value of the status key in global state
       */
      status: async (): Promise<string | undefined> => { return (await this.appClient.state.global.getValue("status")) as string | undefined },
    },
  }

  public newGroup(): InvoiceFinancingComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a create_invoice(uint64)void method call against the InvoiceFinancing contract
       */
      createInvoice(params: CallParams<InvoiceFinancingArgs['obj']['create_invoice(uint64)void'] | InvoiceFinancingArgs['tuple']['create_invoice(uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.createInvoice(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a request_financing()void method call against the InvoiceFinancing contract
       */
      requestFinancing(params: CallParams<InvoiceFinancingArgs['obj']['request_financing()void'] | InvoiceFinancingArgs['tuple']['request_financing()void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.requestFinancing(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a fund_invoice(pay)void method call against the InvoiceFinancing contract
       */
      fundInvoice(params: CallParams<InvoiceFinancingArgs['obj']['fund_invoice(pay)void'] | InvoiceFinancingArgs['tuple']['fund_invoice(pay)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.fundInvoice(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a settle_invoice(pay)void method call against the InvoiceFinancing contract
       */
      settleInvoice(params: CallParams<InvoiceFinancingArgs['obj']['settle_invoice(pay)void'] | InvoiceFinancingArgs['tuple']['settle_invoice(pay)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.settleInvoice(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a clear state call to the InvoiceFinancing contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as InvoiceFinancingComposer
  }
}
export type InvoiceFinancingComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the create_invoice(uint64)void ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  createInvoice(params?: CallParams<InvoiceFinancingArgs['obj']['create_invoice(uint64)void'] | InvoiceFinancingArgs['tuple']['create_invoice(uint64)void']>): InvoiceFinancingComposer<[...TReturns, InvoiceFinancingReturns['create_invoice(uint64)void'] | undefined]>

  /**
   * Calls the request_financing()void ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  requestFinancing(params?: CallParams<InvoiceFinancingArgs['obj']['request_financing()void'] | InvoiceFinancingArgs['tuple']['request_financing()void']>): InvoiceFinancingComposer<[...TReturns, InvoiceFinancingReturns['request_financing()void'] | undefined]>

  /**
   * Calls the fund_invoice(pay)void ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  fundInvoice(params?: CallParams<InvoiceFinancingArgs['obj']['fund_invoice(pay)void'] | InvoiceFinancingArgs['tuple']['fund_invoice(pay)void']>): InvoiceFinancingComposer<[...TReturns, InvoiceFinancingReturns['fund_invoice(pay)void'] | undefined]>

  /**
   * Calls the settle_invoice(pay)void ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  settleInvoice(params?: CallParams<InvoiceFinancingArgs['obj']['settle_invoice(pay)void'] | InvoiceFinancingArgs['tuple']['settle_invoice(pay)void']>): InvoiceFinancingComposer<[...TReturns, InvoiceFinancingReturns['settle_invoice(pay)void'] | undefined]>

  /**
   * Makes a clear_state call to an existing instance of the InvoiceFinancing smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): InvoiceFinancingComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): InvoiceFinancingComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<InvoiceFinancingComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<InvoiceFinancingComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<InvoiceFinancingComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<InvoiceFinancingComposerResults<TReturns>>
}
export type InvoiceFinancingComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

