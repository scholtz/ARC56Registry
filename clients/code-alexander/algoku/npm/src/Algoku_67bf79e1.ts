/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"Algoku","structs":{},"methods":[{"name":"is_valid_solution","args":[{"type":"byte[11]","name":"puzzle","desc":"81-bit bitmask (11 bytes, big-endian bit order) marking which cells were given as clues."},{"type":"byte[81]","name":"solution","desc":"The sudoku solution as 81 bytes, each cell holding a value in 1-9."}],"returns":{"type":"bool","desc":"True if the solution is a valid sudoku and 17 <= popcount(puzzle) <= 40, else False."},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Checks whether the solution is valid and the puzzle mask has a plausible clue count.","events":[],"recommendations":{}},{"name":"mint","args":[{"type":"byte[11]","name":"puzzle","desc":"81-bit bitmask marking which cells were given as clues."},{"type":"byte[81]","name":"solution","desc":"The sudoku solution as 81 bytes, each cell holding 1-9."},{"type":"pay","name":"mbr_payment","desc":"Payment covering the box + ASA MBR (~0.1185 ALGO)."}],"returns":{"type":"uint64","desc":"The created ASA's asset ID."},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Mints an NFT for a valid, unique sudoku solution.\nEach unique solution can only be minted once — uniqueness is enforced by a box keyed on sha256(solution). The minted NFT encodes (solution || mask) in its URL field and records the solver address in its reserve field; claim() later transfers to the address stored there.","events":[],"recommendations":{}},{"name":"claim","args":[{"type":"uint64","name":"asset"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Transfers a minted NFT to the address recorded in its reserve field.\nThe reserve field is set at mint time to the solver's address, so this method is self-authorizing — it always sends the asset to its intended recipient regardless of who calls it. Caller must ensure the recipient has already opted in to the asset.","events":[],"recommendations":{}}],"arcs":[22,28],"desc":"A smart contract for validating sudoku solutions and minting NFTs.","networks":{},"state":{"schema":{"global":{"ints":0,"bytes":0},"local":{"ints":0,"bytes":0}},"keys":{"global":{},"local":{},"box":{}},"maps":{"global":{},"local":{},"box":{}}},"bareActions":{"create":["NoOp"],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[290],"errorMessage":"asset exists"},{"pc":[131,163],"errorMessage":"invalid number of bytes for arc4.static_array<arc4.uint8, 11>"},{"pc":[139,171],"errorMessage":"invalid number of bytes for arc4.static_array<arc4.uint8, 81>"},{"pc":[284],"errorMessage":"invalid number of bytes for arc4.uint64"},{"pc":[199],"errorMessage":"invalid solve"},{"pc":[194],"errorMessage":"mbr payment must be addressed to the app"},{"pc":[207],"errorMessage":"puzzle already solved"},{"pc":[181],"errorMessage":"transaction type is pay"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"events":[]} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


/**
 * The argument types for the Algoku contract
 */
export type AlgokuArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'is_valid_solution(byte[11],byte[81])bool': {
      /**
       * 81-bit bitmask (11 bytes, big-endian bit order) marking which cells were given as clues.
       */
      puzzle: Uint8Array
      /**
       * The sudoku solution as 81 bytes, each cell holding a value in 1-9.
       */
      solution: Uint8Array
    }
    'mint(byte[11],byte[81],pay)uint64': {
      /**
       * 81-bit bitmask marking which cells were given as clues.
       */
      puzzle: Uint8Array
      /**
       * The sudoku solution as 81 bytes, each cell holding 1-9.
       */
      solution: Uint8Array
      /**
       * Payment covering the box + ASA MBR (~0.1185 ALGO).
       */
      mbrPayment: AppMethodCallTransactionArgument
    }
    'claim(uint64)void': {
      asset: bigint | number
    }
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'is_valid_solution(byte[11],byte[81])bool': [puzzle: Uint8Array, solution: Uint8Array]
    'mint(byte[11],byte[81],pay)uint64': [puzzle: Uint8Array, solution: Uint8Array, mbrPayment: AppMethodCallTransactionArgument]
    'claim(uint64)void': [asset: bigint | number]
  }
}

/**
 * The return type for each method
 */
export type AlgokuReturns = {
  'is_valid_solution(byte[11],byte[81])bool': boolean
  'mint(byte[11],byte[81],pay)uint64': bigint
  'claim(uint64)void': void
}

/**
 * Defines the types of available calls and state of the Algoku smart contract.
 */
export type AlgokuTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'is_valid_solution(byte[11],byte[81])bool' | 'is_valid_solution', {
      argsObj: AlgokuArgs['obj']['is_valid_solution(byte[11],byte[81])bool']
      argsTuple: AlgokuArgs['tuple']['is_valid_solution(byte[11],byte[81])bool']
      /**
       * True if the solution is a valid sudoku and 17 <= popcount(puzzle) <= 40, else False.
       */
      returns: AlgokuReturns['is_valid_solution(byte[11],byte[81])bool']
    }>
    & Record<'mint(byte[11],byte[81],pay)uint64' | 'mint', {
      argsObj: AlgokuArgs['obj']['mint(byte[11],byte[81],pay)uint64']
      argsTuple: AlgokuArgs['tuple']['mint(byte[11],byte[81],pay)uint64']
      /**
       * The created ASA's asset ID.
       */
      returns: AlgokuReturns['mint(byte[11],byte[81],pay)uint64']
    }>
    & Record<'claim(uint64)void' | 'claim', {
      argsObj: AlgokuArgs['obj']['claim(uint64)void']
      argsTuple: AlgokuArgs['tuple']['claim(uint64)void']
      returns: AlgokuReturns['claim(uint64)void']
    }>
}

/**
 * Defines the possible abi call signatures.
 */
export type AlgokuSignatures = keyof AlgokuTypes['methods']
/**
 * Defines the possible abi call signatures for methods that return a non-void value.
 */
export type AlgokuNonVoidMethodSignatures = keyof AlgokuTypes['methods'] extends infer T ? T extends keyof AlgokuTypes['methods'] ? MethodReturn<T> extends void ? never : T  : never : never
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the Algoku smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends AlgokuSignatures> = AlgokuTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the Algoku smart contract to the method's return type
 */
export type MethodReturn<TSignature extends AlgokuSignatures> = AlgokuTypes['methods'][TSignature]['returns']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the Algoku smart contract
 */
export abstract class AlgokuParamsFactory {
  /**
   * Constructs a no op call for the is_valid_solution(byte[11],byte[81])bool ABI method
   *
   * Checks whether the solution is valid and the puzzle mask has a plausible clue count.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static isValidSolution(params: CallParams<AlgokuArgs['obj']['is_valid_solution(byte[11],byte[81])bool'] | AlgokuArgs['tuple']['is_valid_solution(byte[11],byte[81])bool']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'is_valid_solution(byte[11],byte[81])bool' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.puzzle, params.args.solution],
    }
  }
  /**
   * Constructs a no op call for the mint(byte[11],byte[81],pay)uint64 ABI method
   *
  * Mints an NFT for a valid, unique sudoku solution.
  Each unique solution can only be minted once — uniqueness is enforced by a box keyed on sha256(solution). The minted NFT encodes (solution || mask) in its URL field and records the solver address in its reserve field; claim() later transfers to the address stored there.

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static mint(params: CallParams<AlgokuArgs['obj']['mint(byte[11],byte[81],pay)uint64'] | AlgokuArgs['tuple']['mint(byte[11],byte[81],pay)uint64']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'mint(byte[11],byte[81],pay)uint64' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.puzzle, params.args.solution, params.args.mbrPayment],
    }
  }
  /**
   * Constructs a no op call for the claim(uint64)void ABI method
   *
  * Transfers a minted NFT to the address recorded in its reserve field.
  The reserve field is set at mint time to the solver's address, so this method is self-authorizing — it always sends the asset to its intended recipient regardless of who calls it. Caller must ensure the recipient has already opted in to the asset.

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static claim(params: CallParams<AlgokuArgs['obj']['claim(uint64)void'] | AlgokuArgs['tuple']['claim(uint64)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'claim(uint64)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.asset],
    }
  }
}

/**
 * A client to make calls to the Algoku smart contract
 */
export class AlgokuClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `AlgokuClient`
   *
   * @param appClient An `AppClient` instance which has been created with the Algoku app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `AlgokuClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Checks for decode errors on the given return value and maps the return value to the return type for the given method
   * @returns The typed return value or undefined if there was no value
   */
  decodeReturnValue<TSignature extends AlgokuNonVoidMethodSignatures>(method: TSignature, returnValue: ABIReturn | undefined) {
    return returnValue !== undefined ? getArc56ReturnValue<MethodReturn<TSignature>>(returnValue, this.appClient.getABIMethod(method), APP_SPEC.structs) : undefined
  }

  /**
   * Returns a new `AlgokuClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<AlgokuClient> {
    return new AlgokuClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `AlgokuClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<AlgokuClient> {
    return new AlgokuClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Makes a clear_state call to an existing instance of the Algoku smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the Algoku smart contract using the `is_valid_solution(byte[11],byte[81])bool` ABI method.
     *
     * Checks whether the solution is valid and the puzzle mask has a plausible clue count.
     *
     * @param params The params for the smart contract call
     * @returns The call params: True if the solution is a valid sudoku and 17 <= popcount(puzzle) <= 40, else False.
     */
    isValidSolution: (params: CallParams<AlgokuArgs['obj']['is_valid_solution(byte[11],byte[81])bool'] | AlgokuArgs['tuple']['is_valid_solution(byte[11],byte[81])bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(AlgokuParamsFactory.isValidSolution(params))
    },

    /**
     * Makes a call to the Algoku smart contract using the `mint(byte[11],byte[81],pay)uint64` ABI method.
     *
    * Mints an NFT for a valid, unique sudoku solution.
    Each unique solution can only be minted once — uniqueness is enforced by a box keyed on sha256(solution). The minted NFT encodes (solution || mask) in its URL field and records the solver address in its reserve field; claim() later transfers to the address stored there.

     *
     * @param params The params for the smart contract call
     * @returns The call params: The created ASA's asset ID.
     */
    mint: (params: CallParams<AlgokuArgs['obj']['mint(byte[11],byte[81],pay)uint64'] | AlgokuArgs['tuple']['mint(byte[11],byte[81],pay)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(AlgokuParamsFactory.mint(params))
    },

    /**
     * Makes a call to the Algoku smart contract using the `claim(uint64)void` ABI method.
     *
    * Transfers a minted NFT to the address recorded in its reserve field.
    The reserve field is set at mint time to the solver's address, so this method is self-authorizing — it always sends the asset to its intended recipient regardless of who calls it. Caller must ensure the recipient has already opted in to the asset.

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    claim: (params: CallParams<AlgokuArgs['obj']['claim(uint64)void'] | AlgokuArgs['tuple']['claim(uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(AlgokuParamsFactory.claim(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Makes a clear_state call to an existing instance of the Algoku smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the Algoku smart contract using the `is_valid_solution(byte[11],byte[81])bool` ABI method.
     *
     * Checks whether the solution is valid and the puzzle mask has a plausible clue count.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction: True if the solution is a valid sudoku and 17 <= popcount(puzzle) <= 40, else False.
     */
    isValidSolution: (params: CallParams<AlgokuArgs['obj']['is_valid_solution(byte[11],byte[81])bool'] | AlgokuArgs['tuple']['is_valid_solution(byte[11],byte[81])bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(AlgokuParamsFactory.isValidSolution(params))
    },

    /**
     * Makes a call to the Algoku smart contract using the `mint(byte[11],byte[81],pay)uint64` ABI method.
     *
    * Mints an NFT for a valid, unique sudoku solution.
    Each unique solution can only be minted once — uniqueness is enforced by a box keyed on sha256(solution). The minted NFT encodes (solution || mask) in its URL field and records the solver address in its reserve field; claim() later transfers to the address stored there.

     *
     * @param params The params for the smart contract call
     * @returns The call transaction: The created ASA's asset ID.
     */
    mint: (params: CallParams<AlgokuArgs['obj']['mint(byte[11],byte[81],pay)uint64'] | AlgokuArgs['tuple']['mint(byte[11],byte[81],pay)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(AlgokuParamsFactory.mint(params))
    },

    /**
     * Makes a call to the Algoku smart contract using the `claim(uint64)void` ABI method.
     *
    * Transfers a minted NFT to the address recorded in its reserve field.
    The reserve field is set at mint time to the solver's address, so this method is self-authorizing — it always sends the asset to its intended recipient regardless of who calls it. Caller must ensure the recipient has already opted in to the asset.

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    claim: (params: CallParams<AlgokuArgs['obj']['claim(uint64)void'] | AlgokuArgs['tuple']['claim(uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(AlgokuParamsFactory.claim(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Makes a clear_state call to an existing instance of the Algoku smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the Algoku smart contract using the `is_valid_solution(byte[11],byte[81])bool` ABI method.
     *
     * Checks whether the solution is valid and the puzzle mask has a plausible clue count.
     *
     * @param params The params for the smart contract call
     * @returns The call result: True if the solution is a valid sudoku and 17 <= popcount(puzzle) <= 40, else False.
     */
    isValidSolution: async (params: CallParams<AlgokuArgs['obj']['is_valid_solution(byte[11],byte[81])bool'] | AlgokuArgs['tuple']['is_valid_solution(byte[11],byte[81])bool']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(AlgokuParamsFactory.isValidSolution(params))
      return {...result, return: result.return as unknown as (undefined | AlgokuReturns['is_valid_solution(byte[11],byte[81])bool'])}
    },

    /**
     * Makes a call to the Algoku smart contract using the `mint(byte[11],byte[81],pay)uint64` ABI method.
     *
    * Mints an NFT for a valid, unique sudoku solution.
    Each unique solution can only be minted once — uniqueness is enforced by a box keyed on sha256(solution). The minted NFT encodes (solution || mask) in its URL field and records the solver address in its reserve field; claim() later transfers to the address stored there.

     *
     * @param params The params for the smart contract call
     * @returns The call result: The created ASA's asset ID.
     */
    mint: async (params: CallParams<AlgokuArgs['obj']['mint(byte[11],byte[81],pay)uint64'] | AlgokuArgs['tuple']['mint(byte[11],byte[81],pay)uint64']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(AlgokuParamsFactory.mint(params))
      return {...result, return: result.return as unknown as (undefined | AlgokuReturns['mint(byte[11],byte[81],pay)uint64'])}
    },

    /**
     * Makes a call to the Algoku smart contract using the `claim(uint64)void` ABI method.
     *
    * Transfers a minted NFT to the address recorded in its reserve field.
    The reserve field is set at mint time to the solver's address, so this method is self-authorizing — it always sends the asset to its intended recipient regardless of who calls it. Caller must ensure the recipient has already opted in to the asset.

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    claim: async (params: CallParams<AlgokuArgs['obj']['claim(uint64)void'] | AlgokuArgs['tuple']['claim(uint64)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(AlgokuParamsFactory.claim(params))
      return {...result, return: result.return as unknown as (undefined | AlgokuReturns['claim(uint64)void'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new AlgokuClient(this.appClient.clone(params))
  }

  /**
   * Methods to access state for the current Algoku app
   */
  state = {
  }

  public newGroup(): AlgokuComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a is_valid_solution(byte[11],byte[81])bool method call against the Algoku contract
       */
      isValidSolution(params: CallParams<AlgokuArgs['obj']['is_valid_solution(byte[11],byte[81])bool'] | AlgokuArgs['tuple']['is_valid_solution(byte[11],byte[81])bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.isValidSolution(params)))
        resultMappers.push((v) => client.decodeReturnValue('is_valid_solution(byte[11],byte[81])bool', v))
        return this
      },
      /**
       * Add a mint(byte[11],byte[81],pay)uint64 method call against the Algoku contract
       */
      mint(params: CallParams<AlgokuArgs['obj']['mint(byte[11],byte[81],pay)uint64'] | AlgokuArgs['tuple']['mint(byte[11],byte[81],pay)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.mint(params)))
        resultMappers.push((v) => client.decodeReturnValue('mint(byte[11],byte[81],pay)uint64', v))
        return this
      },
      /**
       * Add a claim(uint64)void method call against the Algoku contract
       */
      claim(params: CallParams<AlgokuArgs['obj']['claim(uint64)void'] | AlgokuArgs['tuple']['claim(uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.claim(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a clear state call to the Algoku contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as AlgokuComposer
  }
}
export type AlgokuComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the is_valid_solution(byte[11],byte[81])bool ABI method.
   *
   * Checks whether the solution is valid and the puzzle mask has a plausible clue count.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  isValidSolution(params?: CallParams<AlgokuArgs['obj']['is_valid_solution(byte[11],byte[81])bool'] | AlgokuArgs['tuple']['is_valid_solution(byte[11],byte[81])bool']>): AlgokuComposer<[...TReturns, AlgokuReturns['is_valid_solution(byte[11],byte[81])bool'] | undefined]>

  /**
   * Calls the mint(byte[11],byte[81],pay)uint64 ABI method.
   *
  * Mints an NFT for a valid, unique sudoku solution.
  Each unique solution can only be minted once — uniqueness is enforced by a box keyed on sha256(solution). The minted NFT encodes (solution || mask) in its URL field and records the solver address in its reserve field; claim() later transfers to the address stored there.

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  mint(params?: CallParams<AlgokuArgs['obj']['mint(byte[11],byte[81],pay)uint64'] | AlgokuArgs['tuple']['mint(byte[11],byte[81],pay)uint64']>): AlgokuComposer<[...TReturns, AlgokuReturns['mint(byte[11],byte[81],pay)uint64'] | undefined]>

  /**
   * Calls the claim(uint64)void ABI method.
   *
  * Transfers a minted NFT to the address recorded in its reserve field.
  The reserve field is set at mint time to the solver's address, so this method is self-authorizing — it always sends the asset to its intended recipient regardless of who calls it. Caller must ensure the recipient has already opted in to the asset.

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  claim(params?: CallParams<AlgokuArgs['obj']['claim(uint64)void'] | AlgokuArgs['tuple']['claim(uint64)void']>): AlgokuComposer<[...TReturns, AlgokuReturns['claim(uint64)void'] | undefined]>

  /**
   * Makes a clear_state call to an existing instance of the Algoku smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): AlgokuComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): AlgokuComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<AlgokuComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<AlgokuComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<AlgokuComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<AlgokuComposerResults<TReturns>>
}
export type AlgokuComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

