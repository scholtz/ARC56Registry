/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"VolunteerRegistry","structs":{"Volunteer":[{"name":"cid","type":"string"},{"name":"active","type":"bool"}]},"methods":[{"name":"add_volunteer","args":[{"type":"byte[32]","name":"hash_id"},{"type":"string","name":"cid"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[{"name":"VolunteerAdded","args":[{"type":"byte[32]","name":"field1"},{"type":"string","name":"field2"}]}],"recommendations":{}},{"name":"update_status","args":[{"type":"byte[32]","name":"hash_id"},{"type":"bool","name":"active"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[{"name":"StatusUpdated","args":[{"type":"byte[32]","name":"field1"},{"type":"bool","name":"field2"}]}],"recommendations":{}},{"name":"get_volunteer","args":[{"type":"byte[32]","name":"hash_id"}],"returns":{"type":"(string,bool)","struct":"Volunteer"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}}],"arcs":[22,28],"networks":{},"state":{"schema":{"global":{"ints":0,"bytes":0},"local":{"ints":0,"bytes":0}},"keys":{"global":{},"local":{},"box":{}},"maps":{"global":{},"local":{},"box":{"volunteers":{"keyType":"byte[32]","valueType":"Volunteer","prefix":"dm9sXw=="}}}},"bareActions":{"create":["NoOp"],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[180],"errorMessage":"check self.volunteers entry exists"},{"pc":[76],"errorMessage":"invalid array length header"},{"pc":[135],"errorMessage":"invalid number of bytes for arc4.bool"},{"pc":[83],"errorMessage":"invalid number of bytes for arc4.dynamic_array<arc4.uint8>"},{"pc":[70,127,175],"errorMessage":"invalid number of bytes for arc4.static_array<arc4.uint8, 32>"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"events":[{"name":"VolunteerAdded","args":[{"type":"byte[32]","name":"field1"},{"type":"string","name":"field2"}]},{"name":"StatusUpdated","args":[{"type":"byte[32]","name":"field1"},{"type":"bool","name":"field2"}]}]} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


// Type definitions for ARC-56 structs

export type Volunteer = {
  cid: string,
  active: boolean
}


/**
 * Converts the ABI tuple representation of a Volunteer to the struct representation
 */
export function VolunteerFromTuple(abiTuple: [string, boolean]) {
  return getABIStructFromABITuple(abiTuple, APP_SPEC.structs.Volunteer, APP_SPEC.structs) as Volunteer
}

/**
 * The argument types for the VolunteerRegistry contract
 */
export type VolunteerRegistryArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'add_volunteer(byte[32],string)void': {
      hashId: Uint8Array
      cid: string
    }
    'update_status(byte[32],bool)void': {
      hashId: Uint8Array
      active: boolean
    }
    'get_volunteer(byte[32])(string,bool)': {
      hashId: Uint8Array
    }
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'add_volunteer(byte[32],string)void': [hashId: Uint8Array, cid: string]
    'update_status(byte[32],bool)void': [hashId: Uint8Array, active: boolean]
    'get_volunteer(byte[32])(string,bool)': [hashId: Uint8Array]
  }
}

/**
 * The return type for each method
 */
export type VolunteerRegistryReturns = {
  'add_volunteer(byte[32],string)void': void
  'update_status(byte[32],bool)void': void
  'get_volunteer(byte[32])(string,bool)': Volunteer
}

/**
 * Defines the types of available calls and state of the VolunteerRegistry smart contract.
 */
export type VolunteerRegistryTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'add_volunteer(byte[32],string)void' | 'add_volunteer', {
      argsObj: VolunteerRegistryArgs['obj']['add_volunteer(byte[32],string)void']
      argsTuple: VolunteerRegistryArgs['tuple']['add_volunteer(byte[32],string)void']
      returns: VolunteerRegistryReturns['add_volunteer(byte[32],string)void']
    }>
    & Record<'update_status(byte[32],bool)void' | 'update_status', {
      argsObj: VolunteerRegistryArgs['obj']['update_status(byte[32],bool)void']
      argsTuple: VolunteerRegistryArgs['tuple']['update_status(byte[32],bool)void']
      returns: VolunteerRegistryReturns['update_status(byte[32],bool)void']
    }>
    & Record<'get_volunteer(byte[32])(string,bool)' | 'get_volunteer', {
      argsObj: VolunteerRegistryArgs['obj']['get_volunteer(byte[32])(string,bool)']
      argsTuple: VolunteerRegistryArgs['tuple']['get_volunteer(byte[32])(string,bool)']
      returns: VolunteerRegistryReturns['get_volunteer(byte[32])(string,bool)']
    }>
  /**
   * Defines the shape of the state of the application.
   */
  state: {
    box: {
      keys: {}
      maps: {
        volunteers: Map<Uint8Array, Volunteer>
      }
    }
  }
}

/**
 * Defines the possible abi call signatures.
 */
export type VolunteerRegistrySignatures = keyof VolunteerRegistryTypes['methods']
/**
 * Defines the possible abi call signatures for methods that return a non-void value.
 */
export type VolunteerRegistryNonVoidMethodSignatures = keyof VolunteerRegistryTypes['methods'] extends infer T ? T extends keyof VolunteerRegistryTypes['methods'] ? MethodReturn<T> extends void ? never : T  : never : never
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the VolunteerRegistry smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends VolunteerRegistrySignatures> = VolunteerRegistryTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the VolunteerRegistry smart contract to the method's return type
 */
export type MethodReturn<TSignature extends VolunteerRegistrySignatures> = VolunteerRegistryTypes['methods'][TSignature]['returns']

/**
 * Defines the shape of the keyed box state of the application.
 */
export type BoxKeysState = VolunteerRegistryTypes['state']['box']['keys']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the VolunteerRegistry smart contract
 */
export abstract class VolunteerRegistryParamsFactory {
  /**
   * Constructs a no op call for the add_volunteer(byte[32],string)void ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static addVolunteer(params: CallParams<VolunteerRegistryArgs['obj']['add_volunteer(byte[32],string)void'] | VolunteerRegistryArgs['tuple']['add_volunteer(byte[32],string)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'add_volunteer(byte[32],string)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.hashId, params.args.cid],
    }
  }
  /**
   * Constructs a no op call for the update_status(byte[32],bool)void ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static updateStatus(params: CallParams<VolunteerRegistryArgs['obj']['update_status(byte[32],bool)void'] | VolunteerRegistryArgs['tuple']['update_status(byte[32],bool)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'update_status(byte[32],bool)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.hashId, params.args.active],
    }
  }
  /**
   * Constructs a no op call for the get_volunteer(byte[32])(string,bool) ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static getVolunteer(params: CallParams<VolunteerRegistryArgs['obj']['get_volunteer(byte[32])(string,bool)'] | VolunteerRegistryArgs['tuple']['get_volunteer(byte[32])(string,bool)']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'get_volunteer(byte[32])(string,bool)' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.hashId],
    }
  }
}

/**
 * A client to make calls to the VolunteerRegistry smart contract
 */
export class VolunteerRegistryClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `VolunteerRegistryClient`
   *
   * @param appClient An `AppClient` instance which has been created with the VolunteerRegistry app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `VolunteerRegistryClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Checks for decode errors on the given return value and maps the return value to the return type for the given method
   * @returns The typed return value or undefined if there was no value
   */
  decodeReturnValue<TSignature extends VolunteerRegistryNonVoidMethodSignatures>(method: TSignature, returnValue: ABIReturn | undefined) {
    return returnValue !== undefined ? getArc56ReturnValue<MethodReturn<TSignature>>(returnValue, this.appClient.getABIMethod(method), APP_SPEC.structs) : undefined
  }

  /**
   * Returns a new `VolunteerRegistryClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<VolunteerRegistryClient> {
    return new VolunteerRegistryClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `VolunteerRegistryClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<VolunteerRegistryClient> {
    return new VolunteerRegistryClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Makes a clear_state call to an existing instance of the VolunteerRegistry smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the VolunteerRegistry smart contract using the `add_volunteer(byte[32],string)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    addVolunteer: (params: CallParams<VolunteerRegistryArgs['obj']['add_volunteer(byte[32],string)void'] | VolunteerRegistryArgs['tuple']['add_volunteer(byte[32],string)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(VolunteerRegistryParamsFactory.addVolunteer(params))
    },

    /**
     * Makes a call to the VolunteerRegistry smart contract using the `update_status(byte[32],bool)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    updateStatus: (params: CallParams<VolunteerRegistryArgs['obj']['update_status(byte[32],bool)void'] | VolunteerRegistryArgs['tuple']['update_status(byte[32],bool)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(VolunteerRegistryParamsFactory.updateStatus(params))
    },

    /**
     * Makes a call to the VolunteerRegistry smart contract using the `get_volunteer(byte[32])(string,bool)` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    getVolunteer: (params: CallParams<VolunteerRegistryArgs['obj']['get_volunteer(byte[32])(string,bool)'] | VolunteerRegistryArgs['tuple']['get_volunteer(byte[32])(string,bool)']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(VolunteerRegistryParamsFactory.getVolunteer(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Makes a clear_state call to an existing instance of the VolunteerRegistry smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the VolunteerRegistry smart contract using the `add_volunteer(byte[32],string)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    addVolunteer: (params: CallParams<VolunteerRegistryArgs['obj']['add_volunteer(byte[32],string)void'] | VolunteerRegistryArgs['tuple']['add_volunteer(byte[32],string)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(VolunteerRegistryParamsFactory.addVolunteer(params))
    },

    /**
     * Makes a call to the VolunteerRegistry smart contract using the `update_status(byte[32],bool)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    updateStatus: (params: CallParams<VolunteerRegistryArgs['obj']['update_status(byte[32],bool)void'] | VolunteerRegistryArgs['tuple']['update_status(byte[32],bool)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(VolunteerRegistryParamsFactory.updateStatus(params))
    },

    /**
     * Makes a call to the VolunteerRegistry smart contract using the `get_volunteer(byte[32])(string,bool)` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    getVolunteer: (params: CallParams<VolunteerRegistryArgs['obj']['get_volunteer(byte[32])(string,bool)'] | VolunteerRegistryArgs['tuple']['get_volunteer(byte[32])(string,bool)']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(VolunteerRegistryParamsFactory.getVolunteer(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Makes a clear_state call to an existing instance of the VolunteerRegistry smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the VolunteerRegistry smart contract using the `add_volunteer(byte[32],string)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    addVolunteer: async (params: CallParams<VolunteerRegistryArgs['obj']['add_volunteer(byte[32],string)void'] | VolunteerRegistryArgs['tuple']['add_volunteer(byte[32],string)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(VolunteerRegistryParamsFactory.addVolunteer(params))
      return {...result, return: result.return as unknown as (undefined | VolunteerRegistryReturns['add_volunteer(byte[32],string)void'])}
    },

    /**
     * Makes a call to the VolunteerRegistry smart contract using the `update_status(byte[32],bool)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    updateStatus: async (params: CallParams<VolunteerRegistryArgs['obj']['update_status(byte[32],bool)void'] | VolunteerRegistryArgs['tuple']['update_status(byte[32],bool)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(VolunteerRegistryParamsFactory.updateStatus(params))
      return {...result, return: result.return as unknown as (undefined | VolunteerRegistryReturns['update_status(byte[32],bool)void'])}
    },

    /**
     * Makes a call to the VolunteerRegistry smart contract using the `get_volunteer(byte[32])(string,bool)` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    getVolunteer: async (params: CallParams<VolunteerRegistryArgs['obj']['get_volunteer(byte[32])(string,bool)'] | VolunteerRegistryArgs['tuple']['get_volunteer(byte[32])(string,bool)']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(VolunteerRegistryParamsFactory.getVolunteer(params))
      return {...result, return: result.return as unknown as (undefined | VolunteerRegistryReturns['get_volunteer(byte[32])(string,bool)'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new VolunteerRegistryClient(this.appClient.clone(params))
  }

  /**
   * Makes a readonly (simulated) call to the VolunteerRegistry smart contract using the `get_volunteer(byte[32])(string,bool)` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async getVolunteer(params: CallParams<VolunteerRegistryArgs['obj']['get_volunteer(byte[32])(string,bool)'] | VolunteerRegistryArgs['tuple']['get_volunteer(byte[32])(string,bool)']>) {
    const result = await this.appClient.send.call(VolunteerRegistryParamsFactory.getVolunteer(params))
    return result.return as unknown as VolunteerRegistryReturns['get_volunteer(byte[32])(string,bool)']
  }

  /**
   * Methods to access state for the current VolunteerRegistry app
   */
  state = {
    /**
     * Methods to access box state for the current VolunteerRegistry app
     */
    box: {
      /**
       * Get all current keyed values from box state
       */
      getAll: async (): Promise<Partial<Expand<BoxKeysState>>> => {
        const result = await this.appClient.state.box.getAll()
        return {
        }
      },
      /**
       * Get values from the volunteers map in box state
       */
      volunteers: {
        /**
         * Get all current values of the volunteers map in box state
         */
        getMap: async (): Promise<Map<Uint8Array, Volunteer>> => { return (await this.appClient.state.box.getMap("volunteers")) as Map<Uint8Array, Volunteer> },
        /**
         * Get a current value of the volunteers map by key from box state
         */
        value: async (key: Uint8Array): Promise<Volunteer | undefined> => { return await this.appClient.state.box.getMapValue("volunteers", key) as Volunteer | undefined },
      },
    },
  }

  public newGroup(): VolunteerRegistryComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a add_volunteer(byte[32],string)void method call against the VolunteerRegistry contract
       */
      addVolunteer(params: CallParams<VolunteerRegistryArgs['obj']['add_volunteer(byte[32],string)void'] | VolunteerRegistryArgs['tuple']['add_volunteer(byte[32],string)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.addVolunteer(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a update_status(byte[32],bool)void method call against the VolunteerRegistry contract
       */
      updateStatus(params: CallParams<VolunteerRegistryArgs['obj']['update_status(byte[32],bool)void'] | VolunteerRegistryArgs['tuple']['update_status(byte[32],bool)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.updateStatus(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a get_volunteer(byte[32])(string,bool) method call against the VolunteerRegistry contract
       */
      getVolunteer(params: CallParams<VolunteerRegistryArgs['obj']['get_volunteer(byte[32])(string,bool)'] | VolunteerRegistryArgs['tuple']['get_volunteer(byte[32])(string,bool)']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.getVolunteer(params)))
        resultMappers.push((v) => client.decodeReturnValue('get_volunteer(byte[32])(string,bool)', v))
        return this
      },
      /**
       * Add a clear state call to the VolunteerRegistry contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as VolunteerRegistryComposer
  }
}
export type VolunteerRegistryComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the add_volunteer(byte[32],string)void ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  addVolunteer(params?: CallParams<VolunteerRegistryArgs['obj']['add_volunteer(byte[32],string)void'] | VolunteerRegistryArgs['tuple']['add_volunteer(byte[32],string)void']>): VolunteerRegistryComposer<[...TReturns, VolunteerRegistryReturns['add_volunteer(byte[32],string)void'] | undefined]>

  /**
   * Calls the update_status(byte[32],bool)void ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  updateStatus(params?: CallParams<VolunteerRegistryArgs['obj']['update_status(byte[32],bool)void'] | VolunteerRegistryArgs['tuple']['update_status(byte[32],bool)void']>): VolunteerRegistryComposer<[...TReturns, VolunteerRegistryReturns['update_status(byte[32],bool)void'] | undefined]>

  /**
   * Calls the get_volunteer(byte[32])(string,bool) ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  getVolunteer(params?: CallParams<VolunteerRegistryArgs['obj']['get_volunteer(byte[32])(string,bool)'] | VolunteerRegistryArgs['tuple']['get_volunteer(byte[32])(string,bool)']>): VolunteerRegistryComposer<[...TReturns, VolunteerRegistryReturns['get_volunteer(byte[32])(string,bool)'] | undefined]>

  /**
   * Makes a clear_state call to an existing instance of the VolunteerRegistry smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): VolunteerRegistryComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): VolunteerRegistryComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<VolunteerRegistryComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<VolunteerRegistryComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<VolunteerRegistryComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<VolunteerRegistryComposerResults<TReturns>>
}
export type VolunteerRegistryComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

