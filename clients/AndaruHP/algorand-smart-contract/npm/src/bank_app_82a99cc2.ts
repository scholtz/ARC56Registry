/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"bank_app","structs":{},"methods":[{"name":"deposit","args":[{"type":"pay","name":"mbrPay"}],"returns":{"type":"uint64"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}},{"name":"withdraw","args":[{"type":"uint64","name":"amount"}],"returns":{"type":"uint64"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}},{"name":"getMyBalance","args":[],"returns":{"type":"uint64"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}}],"arcs":[22,28],"networks":{},"state":{"schema":{"global":{"ints":2,"bytes":0},"local":{"ints":0,"bytes":0}},"keys":{"global":{"total_deposited":{"keyType":"AVMString","valueType":"AVMUint64","key":"dG90YWxfZGVwb3NpdGVk"},"total_users":{"keyType":"AVMString","valueType":"AVMUint64","key":"dG90YWxfdXNlcnM="}},"local":{},"box":{}},"maps":{"global":{},"local":{},"box":{"user_balances":{"keyType":"address","valueType":"uint64","prefix":"dXNlcl9iYWxhbmNlcw=="}}}},"bareActions":{"create":["NoOp"],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[239],"errorMessage":"Can't withdraw more than deposited"},{"pc":[139],"errorMessage":"Invalid receiver"},{"pc":[235],"errorMessage":"No Deposit Found"},{"pc":[73],"errorMessage":"OnCompletion must be NoOp"},{"pc":[113],"errorMessage":"OnCompletion must be NoOp && can only call when creating"},{"pc":[132],"errorMessage":"Sender mismatch"},{"pc":[296],"errorMessage":"You've not been deposited before"},{"pc":[143],"errorMessage":"Zero Amount"},{"pc":[185,261],"errorMessage":"check self.total_deposited exists"},{"pc":[159],"errorMessage":"check self.total_users exists"},{"pc":[175,197,274],"errorMessage":"check self.user_balances entry exists"},{"pc":[225],"errorMessage":"invalid number of bytes for arc4.uint64"},{"pc":[125],"errorMessage":"transaction type is pay"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"events":[]} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


/**
 * The argument types for the BankApp contract
 */
export type BankAppArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'deposit(pay)uint64': {
      mbrPay: AppMethodCallTransactionArgument
    }
    'withdraw(uint64)uint64': {
      amount: bigint | number
    }
    'getMyBalance()uint64': Record<string, never>
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'deposit(pay)uint64': [mbrPay: AppMethodCallTransactionArgument]
    'withdraw(uint64)uint64': [amount: bigint | number]
    'getMyBalance()uint64': []
  }
}

/**
 * The return type for each method
 */
export type BankAppReturns = {
  'deposit(pay)uint64': bigint
  'withdraw(uint64)uint64': bigint
  'getMyBalance()uint64': bigint
}

/**
 * Defines the types of available calls and state of the BankApp smart contract.
 */
export type BankAppTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'deposit(pay)uint64' | 'deposit', {
      argsObj: BankAppArgs['obj']['deposit(pay)uint64']
      argsTuple: BankAppArgs['tuple']['deposit(pay)uint64']
      returns: BankAppReturns['deposit(pay)uint64']
    }>
    & Record<'withdraw(uint64)uint64' | 'withdraw', {
      argsObj: BankAppArgs['obj']['withdraw(uint64)uint64']
      argsTuple: BankAppArgs['tuple']['withdraw(uint64)uint64']
      returns: BankAppReturns['withdraw(uint64)uint64']
    }>
    & Record<'getMyBalance()uint64' | 'getMyBalance', {
      argsObj: BankAppArgs['obj']['getMyBalance()uint64']
      argsTuple: BankAppArgs['tuple']['getMyBalance()uint64']
      returns: BankAppReturns['getMyBalance()uint64']
    }>
  /**
   * Defines the shape of the state of the application.
   */
  state: {
    global: {
      keys: {
        totalDeposited: bigint
        totalUsers: bigint
      }
      maps: {}
    }
    box: {
      keys: {}
      maps: {
        userBalances: Map<string, bigint>
      }
    }
  }
}

/**
 * Defines the possible abi call signatures.
 */
export type BankAppSignatures = keyof BankAppTypes['methods']
/**
 * Defines the possible abi call signatures for methods that return a non-void value.
 */
export type BankAppNonVoidMethodSignatures = keyof BankAppTypes['methods'] extends infer T ? T extends keyof BankAppTypes['methods'] ? MethodReturn<T> extends void ? never : T  : never : never
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the BankApp smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends BankAppSignatures> = BankAppTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the BankApp smart contract to the method's return type
 */
export type MethodReturn<TSignature extends BankAppSignatures> = BankAppTypes['methods'][TSignature]['returns']

/**
 * Defines the shape of the keyed global state of the application.
 */
export type GlobalKeysState = BankAppTypes['state']['global']['keys']

/**
 * Defines the shape of the keyed box state of the application.
 */
export type BoxKeysState = BankAppTypes['state']['box']['keys']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the BankApp smart contract
 */
export abstract class BankAppParamsFactory {
  /**
   * Constructs a no op call for the deposit(pay)uint64 ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static deposit(params: CallParams<BankAppArgs['obj']['deposit(pay)uint64'] | BankAppArgs['tuple']['deposit(pay)uint64']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'deposit(pay)uint64' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.mbrPay],
    }
  }
  /**
   * Constructs a no op call for the withdraw(uint64)uint64 ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static withdraw(params: CallParams<BankAppArgs['obj']['withdraw(uint64)uint64'] | BankAppArgs['tuple']['withdraw(uint64)uint64']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'withdraw(uint64)uint64' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.amount],
    }
  }
  /**
   * Constructs a no op call for the getMyBalance()uint64 ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static getMyBalance(params: CallParams<BankAppArgs['obj']['getMyBalance()uint64'] | BankAppArgs['tuple']['getMyBalance()uint64']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'getMyBalance()uint64' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
}

/**
 * A client to make calls to the bank_app smart contract
 */
export class BankAppClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `BankAppClient`
   *
   * @param appClient An `AppClient` instance which has been created with the BankApp app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `BankAppClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Checks for decode errors on the given return value and maps the return value to the return type for the given method
   * @returns The typed return value or undefined if there was no value
   */
  decodeReturnValue<TSignature extends BankAppNonVoidMethodSignatures>(method: TSignature, returnValue: ABIReturn | undefined) {
    return returnValue !== undefined ? getArc56ReturnValue<MethodReturn<TSignature>>(returnValue, this.appClient.getABIMethod(method), APP_SPEC.structs) : undefined
  }

  /**
   * Returns a new `BankAppClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<BankAppClient> {
    return new BankAppClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `BankAppClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<BankAppClient> {
    return new BankAppClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Makes a clear_state call to an existing instance of the bank_app smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the bank_app smart contract using the `deposit(pay)uint64` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    deposit: (params: CallParams<BankAppArgs['obj']['deposit(pay)uint64'] | BankAppArgs['tuple']['deposit(pay)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(BankAppParamsFactory.deposit(params))
    },

    /**
     * Makes a call to the bank_app smart contract using the `withdraw(uint64)uint64` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    withdraw: (params: CallParams<BankAppArgs['obj']['withdraw(uint64)uint64'] | BankAppArgs['tuple']['withdraw(uint64)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(BankAppParamsFactory.withdraw(params))
    },

    /**
     * Makes a call to the bank_app smart contract using the `getMyBalance()uint64` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    getMyBalance: (params: CallParams<BankAppArgs['obj']['getMyBalance()uint64'] | BankAppArgs['tuple']['getMyBalance()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(BankAppParamsFactory.getMyBalance(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Makes a clear_state call to an existing instance of the bank_app smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the bank_app smart contract using the `deposit(pay)uint64` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    deposit: (params: CallParams<BankAppArgs['obj']['deposit(pay)uint64'] | BankAppArgs['tuple']['deposit(pay)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(BankAppParamsFactory.deposit(params))
    },

    /**
     * Makes a call to the bank_app smart contract using the `withdraw(uint64)uint64` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    withdraw: (params: CallParams<BankAppArgs['obj']['withdraw(uint64)uint64'] | BankAppArgs['tuple']['withdraw(uint64)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(BankAppParamsFactory.withdraw(params))
    },

    /**
     * Makes a call to the bank_app smart contract using the `getMyBalance()uint64` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    getMyBalance: (params: CallParams<BankAppArgs['obj']['getMyBalance()uint64'] | BankAppArgs['tuple']['getMyBalance()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(BankAppParamsFactory.getMyBalance(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Makes a clear_state call to an existing instance of the bank_app smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the bank_app smart contract using the `deposit(pay)uint64` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    deposit: async (params: CallParams<BankAppArgs['obj']['deposit(pay)uint64'] | BankAppArgs['tuple']['deposit(pay)uint64']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(BankAppParamsFactory.deposit(params))
      return {...result, return: result.return as unknown as (undefined | BankAppReturns['deposit(pay)uint64'])}
    },

    /**
     * Makes a call to the bank_app smart contract using the `withdraw(uint64)uint64` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    withdraw: async (params: CallParams<BankAppArgs['obj']['withdraw(uint64)uint64'] | BankAppArgs['tuple']['withdraw(uint64)uint64']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(BankAppParamsFactory.withdraw(params))
      return {...result, return: result.return as unknown as (undefined | BankAppReturns['withdraw(uint64)uint64'])}
    },

    /**
     * Makes a call to the bank_app smart contract using the `getMyBalance()uint64` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    getMyBalance: async (params: CallParams<BankAppArgs['obj']['getMyBalance()uint64'] | BankAppArgs['tuple']['getMyBalance()uint64']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(BankAppParamsFactory.getMyBalance(params))
      return {...result, return: result.return as unknown as (undefined | BankAppReturns['getMyBalance()uint64'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new BankAppClient(this.appClient.clone(params))
  }

  /**
   * Methods to access state for the current bank_app app
   */
  state = {
    /**
     * Methods to access global state for the current bank_app app
     */
    global: {
      /**
       * Get all current keyed values from global state
       */
      getAll: async (): Promise<Partial<Expand<GlobalKeysState>>> => {
        const result = await this.appClient.state.global.getAll()
        return {
          totalDeposited: result.total_deposited,
          totalUsers: result.total_users,
        }
      },
      /**
       * Get the current value of the total_deposited key in global state
       */
      totalDeposited: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("total_deposited")) as bigint | undefined },
      /**
       * Get the current value of the total_users key in global state
       */
      totalUsers: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("total_users")) as bigint | undefined },
    },
    /**
     * Methods to access box state for the current bank_app app
     */
    box: {
      /**
       * Get all current keyed values from box state
       */
      getAll: async (): Promise<Partial<Expand<BoxKeysState>>> => {
        const result = await this.appClient.state.box.getAll()
        return {
        }
      },
      /**
       * Get values from the user_balances map in box state
       */
      userBalances: {
        /**
         * Get all current values of the user_balances map in box state
         */
        getMap: async (): Promise<Map<string, bigint>> => { return (await this.appClient.state.box.getMap("user_balances")) as Map<string, bigint> },
        /**
         * Get a current value of the user_balances map by key from box state
         */
        value: async (key: string): Promise<bigint | undefined> => { return await this.appClient.state.box.getMapValue("user_balances", key) as bigint | undefined },
      },
    },
  }

  public newGroup(): BankAppComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a deposit(pay)uint64 method call against the bank_app contract
       */
      deposit(params: CallParams<BankAppArgs['obj']['deposit(pay)uint64'] | BankAppArgs['tuple']['deposit(pay)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.deposit(params)))
        resultMappers.push((v) => client.decodeReturnValue('deposit(pay)uint64', v))
        return this
      },
      /**
       * Add a withdraw(uint64)uint64 method call against the bank_app contract
       */
      withdraw(params: CallParams<BankAppArgs['obj']['withdraw(uint64)uint64'] | BankAppArgs['tuple']['withdraw(uint64)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.withdraw(params)))
        resultMappers.push((v) => client.decodeReturnValue('withdraw(uint64)uint64', v))
        return this
      },
      /**
       * Add a getMyBalance()uint64 method call against the bank_app contract
       */
      getMyBalance(params: CallParams<BankAppArgs['obj']['getMyBalance()uint64'] | BankAppArgs['tuple']['getMyBalance()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.getMyBalance(params)))
        resultMappers.push((v) => client.decodeReturnValue('getMyBalance()uint64', v))
        return this
      },
      /**
       * Add a clear state call to the bank_app contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as BankAppComposer
  }
}
export type BankAppComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the deposit(pay)uint64 ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  deposit(params?: CallParams<BankAppArgs['obj']['deposit(pay)uint64'] | BankAppArgs['tuple']['deposit(pay)uint64']>): BankAppComposer<[...TReturns, BankAppReturns['deposit(pay)uint64'] | undefined]>

  /**
   * Calls the withdraw(uint64)uint64 ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  withdraw(params?: CallParams<BankAppArgs['obj']['withdraw(uint64)uint64'] | BankAppArgs['tuple']['withdraw(uint64)uint64']>): BankAppComposer<[...TReturns, BankAppReturns['withdraw(uint64)uint64'] | undefined]>

  /**
   * Calls the getMyBalance()uint64 ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  getMyBalance(params?: CallParams<BankAppArgs['obj']['getMyBalance()uint64'] | BankAppArgs['tuple']['getMyBalance()uint64']>): BankAppComposer<[...TReturns, BankAppReturns['getMyBalance()uint64'] | undefined]>

  /**
   * Makes a clear_state call to an existing instance of the bank_app smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): BankAppComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): BankAppComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<BankAppComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<BankAppComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<BankAppComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<BankAppComposerResults<TReturns>>
}
export type BankAppComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

