/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"KYCRegistry","structs":{"KYCRecord":[{"name":"kycTier","type":"uint8"},{"name":"role","type":"uint8"},{"name":"verifiedAt","type":"uint64"},{"name":"kycHash","type":"byte[32]"}]},"methods":[{"name":"create","args":[{"type":"address","name":"admin"},{"type":"address","name":"oracle"}],"returns":{"type":"void"},"actions":{"create":["NoOp"],"call":[]},"readonly":false,"desc":"Initialize the contract. Called once at deployment.","events":[],"recommendations":{}},{"name":"register","args":[{"type":"address","name":"address"},{"type":"uint8","name":"role"},{"type":"byte[32]","name":"kyc_hash"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Register or update a KYC record. Oracle-only.\nThe caller must include a payment transaction covering the box MBR if this is a new registration (box doesn't exist yet).","events":[],"recommendations":{}},{"name":"update_tier","args":[{"type":"address","name":"address"},{"type":"uint8","name":"new_tier"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Upgrade KYC tier (e.g., basic → enhanced). Oracle-only.","events":[],"recommendations":{}},{"name":"revoke","args":[{"type":"address","name":"address"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Revoke KYC for an address. Admin-only.","events":[],"recommendations":{}},{"name":"set_oracle","args":[{"type":"address","name":"new_oracle"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Update the oracle address. Admin-only.","events":[],"recommendations":{}},{"name":"is_verified","args":[{"type":"address","name":"address"}],"returns":{"type":"bool"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"desc":"Check if an address has a valid KYC record.","events":[],"recommendations":{}},{"name":"get_record","args":[{"type":"address","name":"address"}],"returns":{"type":"(uint8,uint8,uint64,byte[32])","struct":"KYCRecord"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"desc":"Get the full KYC record for an address. Reverts if not found.","events":[],"recommendations":{}},{"name":"get_role","args":[{"type":"address","name":"address"}],"returns":{"type":"uint8"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"desc":"Get the role for an address. Returns 0 if not registered.","events":[],"recommendations":{}},{"name":"get_stats","args":[],"returns":{"type":"uint64"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"desc":"Return the total number of verified accounts.","events":[],"recommendations":{}}],"arcs":[22,28],"desc":"\n    On-chain KYC status registry.\n\n    Access control:\n        - register / update_tier: oracle only\n        - revoke: admin only\n        - is_verified / get_record: anyone (read-only)\n    ","networks":{},"state":{"schema":{"global":{"ints":1,"bytes":2},"local":{"ints":0,"bytes":0}},"keys":{"global":{"admin":{"keyType":"AVMString","valueType":"address","key":"YWRtaW4="},"oracle":{"keyType":"AVMString","valueType":"address","key":"b3JhY2xl"},"total_verified":{"keyType":"AVMString","valueType":"AVMUint64","key":"dG90YWxfdmVyaWZpZWQ="}},"local":{},"box":{}},"maps":{"global":{},"local":{},"box":{"records":{"keyType":"address","valueType":"KYCRecord","prefix":"a3ljXw=="}}}},"bareActions":{"create":[],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[287,318,396],"errorMessage":"address not registered"},{"pc":[308,346],"errorMessage":"check self.admin exists"},{"pc":[194,276],"errorMessage":"check self.oracle exists"},{"pc":[425],"errorMessage":"check self.records entry exists"},{"pc":[243,324,444],"errorMessage":"check self.total_verified exists"},{"pc":[291],"errorMessage":"index out of bounds"},{"pc":[140,148,169,188,262,301,339,361,388,412],"errorMessage":"invalid number of bytes for arc4.static_array<arc4.uint8, 32>"},{"pc":[178,270],"errorMessage":"invalid number of bytes for arc4.uint8"},{"pc":[210],"errorMessage":"invalid role"},{"pc":[310],"errorMessage":"only admin can revoke"},{"pc":[348],"errorMessage":"only admin can set oracle"},{"pc":[196],"errorMessage":"only oracle can register"},{"pc":[278],"errorMessage":"only oracle can update tier"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"events":[]} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


// Type definitions for ARC-56 structs

export type KycRecord = {
  kycTier: number,
  role: number,
  verifiedAt: bigint,
  kycHash: Uint8Array
}


/**
 * Converts the ABI tuple representation of a KYCRecord to the struct representation
 */
export function KycRecordFromTuple(abiTuple: [number, number, bigint, Uint8Array]) {
  return getABIStructFromABITuple(abiTuple, APP_SPEC.structs.KYCRecord, APP_SPEC.structs) as KycRecord
}

/**
 * The argument types for the KycRegistry contract
 */
export type KycRegistryArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'create(address,address)void': {
      admin: string
      oracle: string
    }
    'register(address,uint8,byte[32])void': {
      address: string
      role: bigint | number
      kycHash: Uint8Array
    }
    'update_tier(address,uint8)void': {
      address: string
      newTier: bigint | number
    }
    'revoke(address)void': {
      address: string
    }
    'set_oracle(address)void': {
      newOracle: string
    }
    'is_verified(address)bool': {
      address: string
    }
    'get_record(address)(uint8,uint8,uint64,byte[32])': {
      address: string
    }
    'get_role(address)uint8': {
      address: string
    }
    'get_stats()uint64': Record<string, never>
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'create(address,address)void': [admin: string, oracle: string]
    'register(address,uint8,byte[32])void': [address: string, role: bigint | number, kycHash: Uint8Array]
    'update_tier(address,uint8)void': [address: string, newTier: bigint | number]
    'revoke(address)void': [address: string]
    'set_oracle(address)void': [newOracle: string]
    'is_verified(address)bool': [address: string]
    'get_record(address)(uint8,uint8,uint64,byte[32])': [address: string]
    'get_role(address)uint8': [address: string]
    'get_stats()uint64': []
  }
}

/**
 * The return type for each method
 */
export type KycRegistryReturns = {
  'create(address,address)void': void
  'register(address,uint8,byte[32])void': void
  'update_tier(address,uint8)void': void
  'revoke(address)void': void
  'set_oracle(address)void': void
  'is_verified(address)bool': boolean
  'get_record(address)(uint8,uint8,uint64,byte[32])': KycRecord
  'get_role(address)uint8': number
  'get_stats()uint64': bigint
}

/**
 * Defines the types of available calls and state of the KycRegistry smart contract.
 */
export type KycRegistryTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'create(address,address)void' | 'create', {
      argsObj: KycRegistryArgs['obj']['create(address,address)void']
      argsTuple: KycRegistryArgs['tuple']['create(address,address)void']
      returns: KycRegistryReturns['create(address,address)void']
    }>
    & Record<'register(address,uint8,byte[32])void' | 'register', {
      argsObj: KycRegistryArgs['obj']['register(address,uint8,byte[32])void']
      argsTuple: KycRegistryArgs['tuple']['register(address,uint8,byte[32])void']
      returns: KycRegistryReturns['register(address,uint8,byte[32])void']
    }>
    & Record<'update_tier(address,uint8)void' | 'update_tier', {
      argsObj: KycRegistryArgs['obj']['update_tier(address,uint8)void']
      argsTuple: KycRegistryArgs['tuple']['update_tier(address,uint8)void']
      returns: KycRegistryReturns['update_tier(address,uint8)void']
    }>
    & Record<'revoke(address)void' | 'revoke', {
      argsObj: KycRegistryArgs['obj']['revoke(address)void']
      argsTuple: KycRegistryArgs['tuple']['revoke(address)void']
      returns: KycRegistryReturns['revoke(address)void']
    }>
    & Record<'set_oracle(address)void' | 'set_oracle', {
      argsObj: KycRegistryArgs['obj']['set_oracle(address)void']
      argsTuple: KycRegistryArgs['tuple']['set_oracle(address)void']
      returns: KycRegistryReturns['set_oracle(address)void']
    }>
    & Record<'is_verified(address)bool' | 'is_verified', {
      argsObj: KycRegistryArgs['obj']['is_verified(address)bool']
      argsTuple: KycRegistryArgs['tuple']['is_verified(address)bool']
      returns: KycRegistryReturns['is_verified(address)bool']
    }>
    & Record<'get_record(address)(uint8,uint8,uint64,byte[32])' | 'get_record', {
      argsObj: KycRegistryArgs['obj']['get_record(address)(uint8,uint8,uint64,byte[32])']
      argsTuple: KycRegistryArgs['tuple']['get_record(address)(uint8,uint8,uint64,byte[32])']
      returns: KycRegistryReturns['get_record(address)(uint8,uint8,uint64,byte[32])']
    }>
    & Record<'get_role(address)uint8' | 'get_role', {
      argsObj: KycRegistryArgs['obj']['get_role(address)uint8']
      argsTuple: KycRegistryArgs['tuple']['get_role(address)uint8']
      returns: KycRegistryReturns['get_role(address)uint8']
    }>
    & Record<'get_stats()uint64' | 'get_stats', {
      argsObj: KycRegistryArgs['obj']['get_stats()uint64']
      argsTuple: KycRegistryArgs['tuple']['get_stats()uint64']
      returns: KycRegistryReturns['get_stats()uint64']
    }>
  /**
   * Defines the shape of the state of the application.
   */
  state: {
    global: {
      keys: {
        admin: string
        oracle: string
        totalVerified: bigint
      }
      maps: {}
    }
    box: {
      keys: {}
      maps: {
        records: Map<string, KycRecord>
      }
    }
  }
}

/**
 * Defines the possible abi call signatures.
 */
export type KycRegistrySignatures = keyof KycRegistryTypes['methods']
/**
 * Defines the possible abi call signatures for methods that return a non-void value.
 */
export type KycRegistryNonVoidMethodSignatures = keyof KycRegistryTypes['methods'] extends infer T ? T extends keyof KycRegistryTypes['methods'] ? MethodReturn<T> extends void ? never : T  : never : never
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the KycRegistry smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends KycRegistrySignatures> = KycRegistryTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the KycRegistry smart contract to the method's return type
 */
export type MethodReturn<TSignature extends KycRegistrySignatures> = KycRegistryTypes['methods'][TSignature]['returns']

/**
 * Defines the shape of the keyed global state of the application.
 */
export type GlobalKeysState = KycRegistryTypes['state']['global']['keys']

/**
 * Defines the shape of the keyed box state of the application.
 */
export type BoxKeysState = KycRegistryTypes['state']['box']['keys']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the KycRegistry smart contract
 */
export abstract class KycRegistryParamsFactory {
  /**
   * Constructs a no op call for the register(address,uint8,byte[32])void ABI method
   *
  * Register or update a KYC record. Oracle-only.
  The caller must include a payment transaction covering the box MBR if this is a new registration (box doesn't exist yet).

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static register(params: CallParams<KycRegistryArgs['obj']['register(address,uint8,byte[32])void'] | KycRegistryArgs['tuple']['register(address,uint8,byte[32])void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'register(address,uint8,byte[32])void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.address, params.args.role, params.args.kycHash],
    }
  }
  /**
   * Constructs a no op call for the update_tier(address,uint8)void ABI method
   *
   * Upgrade KYC tier (e.g., basic → enhanced). Oracle-only.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static updateTier(params: CallParams<KycRegistryArgs['obj']['update_tier(address,uint8)void'] | KycRegistryArgs['tuple']['update_tier(address,uint8)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'update_tier(address,uint8)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.address, params.args.newTier],
    }
  }
  /**
   * Constructs a no op call for the revoke(address)void ABI method
   *
   * Revoke KYC for an address. Admin-only.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static revoke(params: CallParams<KycRegistryArgs['obj']['revoke(address)void'] | KycRegistryArgs['tuple']['revoke(address)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'revoke(address)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.address],
    }
  }
  /**
   * Constructs a no op call for the set_oracle(address)void ABI method
   *
   * Update the oracle address. Admin-only.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static setOracle(params: CallParams<KycRegistryArgs['obj']['set_oracle(address)void'] | KycRegistryArgs['tuple']['set_oracle(address)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'set_oracle(address)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.newOracle],
    }
  }
  /**
   * Constructs a no op call for the is_verified(address)bool ABI method
   *
   * Check if an address has a valid KYC record.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static isVerified(params: CallParams<KycRegistryArgs['obj']['is_verified(address)bool'] | KycRegistryArgs['tuple']['is_verified(address)bool']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'is_verified(address)bool' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.address],
    }
  }
  /**
   * Constructs a no op call for the get_record(address)(uint8,uint8,uint64,byte[32]) ABI method
   *
   * Get the full KYC record for an address. Reverts if not found.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static getRecord(params: CallParams<KycRegistryArgs['obj']['get_record(address)(uint8,uint8,uint64,byte[32])'] | KycRegistryArgs['tuple']['get_record(address)(uint8,uint8,uint64,byte[32])']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'get_record(address)(uint8,uint8,uint64,byte[32])' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.address],
    }
  }
  /**
   * Constructs a no op call for the get_role(address)uint8 ABI method
   *
   * Get the role for an address. Returns 0 if not registered.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static getRole(params: CallParams<KycRegistryArgs['obj']['get_role(address)uint8'] | KycRegistryArgs['tuple']['get_role(address)uint8']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'get_role(address)uint8' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.address],
    }
  }
  /**
   * Constructs a no op call for the get_stats()uint64 ABI method
   *
   * Return the total number of verified accounts.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static getStats(params: CallParams<KycRegistryArgs['obj']['get_stats()uint64'] | KycRegistryArgs['tuple']['get_stats()uint64']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'get_stats()uint64' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
}

/**
 * A client to make calls to the KYCRegistry smart contract
 */
export class KycRegistryClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `KycRegistryClient`
   *
   * @param appClient An `AppClient` instance which has been created with the KycRegistry app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `KycRegistryClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Checks for decode errors on the given return value and maps the return value to the return type for the given method
   * @returns The typed return value or undefined if there was no value
   */
  decodeReturnValue<TSignature extends KycRegistryNonVoidMethodSignatures>(method: TSignature, returnValue: ABIReturn | undefined) {
    return returnValue !== undefined ? getArc56ReturnValue<MethodReturn<TSignature>>(returnValue, this.appClient.getABIMethod(method), APP_SPEC.structs) : undefined
  }

  /**
   * Returns a new `KycRegistryClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<KycRegistryClient> {
    return new KycRegistryClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `KycRegistryClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<KycRegistryClient> {
    return new KycRegistryClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Makes a clear_state call to an existing instance of the KYCRegistry smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the KYCRegistry smart contract using the `register(address,uint8,byte[32])void` ABI method.
     *
    * Register or update a KYC record. Oracle-only.
    The caller must include a payment transaction covering the box MBR if this is a new registration (box doesn't exist yet).

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    register: (params: CallParams<KycRegistryArgs['obj']['register(address,uint8,byte[32])void'] | KycRegistryArgs['tuple']['register(address,uint8,byte[32])void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(KycRegistryParamsFactory.register(params))
    },

    /**
     * Makes a call to the KYCRegistry smart contract using the `update_tier(address,uint8)void` ABI method.
     *
     * Upgrade KYC tier (e.g., basic → enhanced). Oracle-only.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    updateTier: (params: CallParams<KycRegistryArgs['obj']['update_tier(address,uint8)void'] | KycRegistryArgs['tuple']['update_tier(address,uint8)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(KycRegistryParamsFactory.updateTier(params))
    },

    /**
     * Makes a call to the KYCRegistry smart contract using the `revoke(address)void` ABI method.
     *
     * Revoke KYC for an address. Admin-only.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    revoke: (params: CallParams<KycRegistryArgs['obj']['revoke(address)void'] | KycRegistryArgs['tuple']['revoke(address)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(KycRegistryParamsFactory.revoke(params))
    },

    /**
     * Makes a call to the KYCRegistry smart contract using the `set_oracle(address)void` ABI method.
     *
     * Update the oracle address. Admin-only.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    setOracle: (params: CallParams<KycRegistryArgs['obj']['set_oracle(address)void'] | KycRegistryArgs['tuple']['set_oracle(address)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(KycRegistryParamsFactory.setOracle(params))
    },

    /**
     * Makes a call to the KYCRegistry smart contract using the `is_verified(address)bool` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * Check if an address has a valid KYC record.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    isVerified: (params: CallParams<KycRegistryArgs['obj']['is_verified(address)bool'] | KycRegistryArgs['tuple']['is_verified(address)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(KycRegistryParamsFactory.isVerified(params))
    },

    /**
     * Makes a call to the KYCRegistry smart contract using the `get_record(address)(uint8,uint8,uint64,byte[32])` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * Get the full KYC record for an address. Reverts if not found.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    getRecord: (params: CallParams<KycRegistryArgs['obj']['get_record(address)(uint8,uint8,uint64,byte[32])'] | KycRegistryArgs['tuple']['get_record(address)(uint8,uint8,uint64,byte[32])']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(KycRegistryParamsFactory.getRecord(params))
    },

    /**
     * Makes a call to the KYCRegistry smart contract using the `get_role(address)uint8` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * Get the role for an address. Returns 0 if not registered.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    getRole: (params: CallParams<KycRegistryArgs['obj']['get_role(address)uint8'] | KycRegistryArgs['tuple']['get_role(address)uint8']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(KycRegistryParamsFactory.getRole(params))
    },

    /**
     * Makes a call to the KYCRegistry smart contract using the `get_stats()uint64` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * Return the total number of verified accounts.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    getStats: (params: CallParams<KycRegistryArgs['obj']['get_stats()uint64'] | KycRegistryArgs['tuple']['get_stats()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(KycRegistryParamsFactory.getStats(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Makes a clear_state call to an existing instance of the KYCRegistry smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the KYCRegistry smart contract using the `register(address,uint8,byte[32])void` ABI method.
     *
    * Register or update a KYC record. Oracle-only.
    The caller must include a payment transaction covering the box MBR if this is a new registration (box doesn't exist yet).

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    register: (params: CallParams<KycRegistryArgs['obj']['register(address,uint8,byte[32])void'] | KycRegistryArgs['tuple']['register(address,uint8,byte[32])void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(KycRegistryParamsFactory.register(params))
    },

    /**
     * Makes a call to the KYCRegistry smart contract using the `update_tier(address,uint8)void` ABI method.
     *
     * Upgrade KYC tier (e.g., basic → enhanced). Oracle-only.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    updateTier: (params: CallParams<KycRegistryArgs['obj']['update_tier(address,uint8)void'] | KycRegistryArgs['tuple']['update_tier(address,uint8)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(KycRegistryParamsFactory.updateTier(params))
    },

    /**
     * Makes a call to the KYCRegistry smart contract using the `revoke(address)void` ABI method.
     *
     * Revoke KYC for an address. Admin-only.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    revoke: (params: CallParams<KycRegistryArgs['obj']['revoke(address)void'] | KycRegistryArgs['tuple']['revoke(address)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(KycRegistryParamsFactory.revoke(params))
    },

    /**
     * Makes a call to the KYCRegistry smart contract using the `set_oracle(address)void` ABI method.
     *
     * Update the oracle address. Admin-only.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    setOracle: (params: CallParams<KycRegistryArgs['obj']['set_oracle(address)void'] | KycRegistryArgs['tuple']['set_oracle(address)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(KycRegistryParamsFactory.setOracle(params))
    },

    /**
     * Makes a call to the KYCRegistry smart contract using the `is_verified(address)bool` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * Check if an address has a valid KYC record.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    isVerified: (params: CallParams<KycRegistryArgs['obj']['is_verified(address)bool'] | KycRegistryArgs['tuple']['is_verified(address)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(KycRegistryParamsFactory.isVerified(params))
    },

    /**
     * Makes a call to the KYCRegistry smart contract using the `get_record(address)(uint8,uint8,uint64,byte[32])` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * Get the full KYC record for an address. Reverts if not found.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    getRecord: (params: CallParams<KycRegistryArgs['obj']['get_record(address)(uint8,uint8,uint64,byte[32])'] | KycRegistryArgs['tuple']['get_record(address)(uint8,uint8,uint64,byte[32])']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(KycRegistryParamsFactory.getRecord(params))
    },

    /**
     * Makes a call to the KYCRegistry smart contract using the `get_role(address)uint8` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * Get the role for an address. Returns 0 if not registered.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    getRole: (params: CallParams<KycRegistryArgs['obj']['get_role(address)uint8'] | KycRegistryArgs['tuple']['get_role(address)uint8']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(KycRegistryParamsFactory.getRole(params))
    },

    /**
     * Makes a call to the KYCRegistry smart contract using the `get_stats()uint64` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * Return the total number of verified accounts.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    getStats: (params: CallParams<KycRegistryArgs['obj']['get_stats()uint64'] | KycRegistryArgs['tuple']['get_stats()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(KycRegistryParamsFactory.getStats(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Makes a clear_state call to an existing instance of the KYCRegistry smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the KYCRegistry smart contract using the `register(address,uint8,byte[32])void` ABI method.
     *
    * Register or update a KYC record. Oracle-only.
    The caller must include a payment transaction covering the box MBR if this is a new registration (box doesn't exist yet).

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    register: async (params: CallParams<KycRegistryArgs['obj']['register(address,uint8,byte[32])void'] | KycRegistryArgs['tuple']['register(address,uint8,byte[32])void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(KycRegistryParamsFactory.register(params))
      return {...result, return: result.return as unknown as (undefined | KycRegistryReturns['register(address,uint8,byte[32])void'])}
    },

    /**
     * Makes a call to the KYCRegistry smart contract using the `update_tier(address,uint8)void` ABI method.
     *
     * Upgrade KYC tier (e.g., basic → enhanced). Oracle-only.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    updateTier: async (params: CallParams<KycRegistryArgs['obj']['update_tier(address,uint8)void'] | KycRegistryArgs['tuple']['update_tier(address,uint8)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(KycRegistryParamsFactory.updateTier(params))
      return {...result, return: result.return as unknown as (undefined | KycRegistryReturns['update_tier(address,uint8)void'])}
    },

    /**
     * Makes a call to the KYCRegistry smart contract using the `revoke(address)void` ABI method.
     *
     * Revoke KYC for an address. Admin-only.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    revoke: async (params: CallParams<KycRegistryArgs['obj']['revoke(address)void'] | KycRegistryArgs['tuple']['revoke(address)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(KycRegistryParamsFactory.revoke(params))
      return {...result, return: result.return as unknown as (undefined | KycRegistryReturns['revoke(address)void'])}
    },

    /**
     * Makes a call to the KYCRegistry smart contract using the `set_oracle(address)void` ABI method.
     *
     * Update the oracle address. Admin-only.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    setOracle: async (params: CallParams<KycRegistryArgs['obj']['set_oracle(address)void'] | KycRegistryArgs['tuple']['set_oracle(address)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(KycRegistryParamsFactory.setOracle(params))
      return {...result, return: result.return as unknown as (undefined | KycRegistryReturns['set_oracle(address)void'])}
    },

    /**
     * Makes a call to the KYCRegistry smart contract using the `is_verified(address)bool` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * Check if an address has a valid KYC record.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    isVerified: async (params: CallParams<KycRegistryArgs['obj']['is_verified(address)bool'] | KycRegistryArgs['tuple']['is_verified(address)bool']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(KycRegistryParamsFactory.isVerified(params))
      return {...result, return: result.return as unknown as (undefined | KycRegistryReturns['is_verified(address)bool'])}
    },

    /**
     * Makes a call to the KYCRegistry smart contract using the `get_record(address)(uint8,uint8,uint64,byte[32])` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * Get the full KYC record for an address. Reverts if not found.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    getRecord: async (params: CallParams<KycRegistryArgs['obj']['get_record(address)(uint8,uint8,uint64,byte[32])'] | KycRegistryArgs['tuple']['get_record(address)(uint8,uint8,uint64,byte[32])']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(KycRegistryParamsFactory.getRecord(params))
      return {...result, return: result.return as unknown as (undefined | KycRegistryReturns['get_record(address)(uint8,uint8,uint64,byte[32])'])}
    },

    /**
     * Makes a call to the KYCRegistry smart contract using the `get_role(address)uint8` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * Get the role for an address. Returns 0 if not registered.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    getRole: async (params: CallParams<KycRegistryArgs['obj']['get_role(address)uint8'] | KycRegistryArgs['tuple']['get_role(address)uint8']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(KycRegistryParamsFactory.getRole(params))
      return {...result, return: result.return as unknown as (undefined | KycRegistryReturns['get_role(address)uint8'])}
    },

    /**
     * Makes a call to the KYCRegistry smart contract using the `get_stats()uint64` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * Return the total number of verified accounts.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    getStats: async (params: CallParams<KycRegistryArgs['obj']['get_stats()uint64'] | KycRegistryArgs['tuple']['get_stats()uint64']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(KycRegistryParamsFactory.getStats(params))
      return {...result, return: result.return as unknown as (undefined | KycRegistryReturns['get_stats()uint64'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new KycRegistryClient(this.appClient.clone(params))
  }

  /**
   * Makes a readonly (simulated) call to the KYCRegistry smart contract using the `is_verified(address)bool` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * Check if an address has a valid KYC record.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async isVerified(params: CallParams<KycRegistryArgs['obj']['is_verified(address)bool'] | KycRegistryArgs['tuple']['is_verified(address)bool']>) {
    const result = await this.appClient.send.call(KycRegistryParamsFactory.isVerified(params))
    return result.return as unknown as KycRegistryReturns['is_verified(address)bool']
  }

  /**
   * Makes a readonly (simulated) call to the KYCRegistry smart contract using the `get_record(address)(uint8,uint8,uint64,byte[32])` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * Get the full KYC record for an address. Reverts if not found.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async getRecord(params: CallParams<KycRegistryArgs['obj']['get_record(address)(uint8,uint8,uint64,byte[32])'] | KycRegistryArgs['tuple']['get_record(address)(uint8,uint8,uint64,byte[32])']>) {
    const result = await this.appClient.send.call(KycRegistryParamsFactory.getRecord(params))
    return result.return as unknown as KycRegistryReturns['get_record(address)(uint8,uint8,uint64,byte[32])']
  }

  /**
   * Makes a readonly (simulated) call to the KYCRegistry smart contract using the `get_role(address)uint8` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * Get the role for an address. Returns 0 if not registered.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async getRole(params: CallParams<KycRegistryArgs['obj']['get_role(address)uint8'] | KycRegistryArgs['tuple']['get_role(address)uint8']>) {
    const result = await this.appClient.send.call(KycRegistryParamsFactory.getRole(params))
    return result.return as unknown as KycRegistryReturns['get_role(address)uint8']
  }

  /**
   * Makes a readonly (simulated) call to the KYCRegistry smart contract using the `get_stats()uint64` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * Return the total number of verified accounts.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async getStats(params: CallParams<KycRegistryArgs['obj']['get_stats()uint64'] | KycRegistryArgs['tuple']['get_stats()uint64']> = {args: []}) {
    const result = await this.appClient.send.call(KycRegistryParamsFactory.getStats(params))
    return result.return as unknown as KycRegistryReturns['get_stats()uint64']
  }

  /**
   * Methods to access state for the current KYCRegistry app
   */
  state = {
    /**
     * Methods to access global state for the current KYCRegistry app
     */
    global: {
      /**
       * Get all current keyed values from global state
       */
      getAll: async (): Promise<Partial<Expand<GlobalKeysState>>> => {
        const result = await this.appClient.state.global.getAll()
        return {
          admin: result.admin,
          oracle: result.oracle,
          totalVerified: result.total_verified,
        }
      },
      /**
       * Get the current value of the admin key in global state
       */
      admin: async (): Promise<string | undefined> => { return (await this.appClient.state.global.getValue("admin")) as string | undefined },
      /**
       * Get the current value of the oracle key in global state
       */
      oracle: async (): Promise<string | undefined> => { return (await this.appClient.state.global.getValue("oracle")) as string | undefined },
      /**
       * Get the current value of the total_verified key in global state
       */
      totalVerified: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("total_verified")) as bigint | undefined },
    },
    /**
     * Methods to access box state for the current KYCRegistry app
     */
    box: {
      /**
       * Get all current keyed values from box state
       */
      getAll: async (): Promise<Partial<Expand<BoxKeysState>>> => {
        const result = await this.appClient.state.box.getAll()
        return {
        }
      },
      /**
       * Get values from the records map in box state
       */
      records: {
        /**
         * Get all current values of the records map in box state
         */
        getMap: async (): Promise<Map<string, KycRecord>> => { return (await this.appClient.state.box.getMap("records")) as Map<string, KycRecord> },
        /**
         * Get a current value of the records map by key from box state
         */
        value: async (key: string): Promise<KycRecord | undefined> => { return await this.appClient.state.box.getMapValue("records", key) as KycRecord | undefined },
      },
    },
  }

  public newGroup(): KycRegistryComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a register(address,uint8,byte[32])void method call against the KYCRegistry contract
       */
      register(params: CallParams<KycRegistryArgs['obj']['register(address,uint8,byte[32])void'] | KycRegistryArgs['tuple']['register(address,uint8,byte[32])void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.register(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a update_tier(address,uint8)void method call against the KYCRegistry contract
       */
      updateTier(params: CallParams<KycRegistryArgs['obj']['update_tier(address,uint8)void'] | KycRegistryArgs['tuple']['update_tier(address,uint8)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.updateTier(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a revoke(address)void method call against the KYCRegistry contract
       */
      revoke(params: CallParams<KycRegistryArgs['obj']['revoke(address)void'] | KycRegistryArgs['tuple']['revoke(address)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.revoke(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a set_oracle(address)void method call against the KYCRegistry contract
       */
      setOracle(params: CallParams<KycRegistryArgs['obj']['set_oracle(address)void'] | KycRegistryArgs['tuple']['set_oracle(address)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.setOracle(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a is_verified(address)bool method call against the KYCRegistry contract
       */
      isVerified(params: CallParams<KycRegistryArgs['obj']['is_verified(address)bool'] | KycRegistryArgs['tuple']['is_verified(address)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.isVerified(params)))
        resultMappers.push((v) => client.decodeReturnValue('is_verified(address)bool', v))
        return this
      },
      /**
       * Add a get_record(address)(uint8,uint8,uint64,byte[32]) method call against the KYCRegistry contract
       */
      getRecord(params: CallParams<KycRegistryArgs['obj']['get_record(address)(uint8,uint8,uint64,byte[32])'] | KycRegistryArgs['tuple']['get_record(address)(uint8,uint8,uint64,byte[32])']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.getRecord(params)))
        resultMappers.push((v) => client.decodeReturnValue('get_record(address)(uint8,uint8,uint64,byte[32])', v))
        return this
      },
      /**
       * Add a get_role(address)uint8 method call against the KYCRegistry contract
       */
      getRole(params: CallParams<KycRegistryArgs['obj']['get_role(address)uint8'] | KycRegistryArgs['tuple']['get_role(address)uint8']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.getRole(params)))
        resultMappers.push((v) => client.decodeReturnValue('get_role(address)uint8', v))
        return this
      },
      /**
       * Add a get_stats()uint64 method call against the KYCRegistry contract
       */
      getStats(params: CallParams<KycRegistryArgs['obj']['get_stats()uint64'] | KycRegistryArgs['tuple']['get_stats()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.getStats(params)))
        resultMappers.push((v) => client.decodeReturnValue('get_stats()uint64', v))
        return this
      },
      /**
       * Add a clear state call to the KYCRegistry contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as KycRegistryComposer
  }
}
export type KycRegistryComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the register(address,uint8,byte[32])void ABI method.
   *
  * Register or update a KYC record. Oracle-only.
  The caller must include a payment transaction covering the box MBR if this is a new registration (box doesn't exist yet).

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  register(params?: CallParams<KycRegistryArgs['obj']['register(address,uint8,byte[32])void'] | KycRegistryArgs['tuple']['register(address,uint8,byte[32])void']>): KycRegistryComposer<[...TReturns, KycRegistryReturns['register(address,uint8,byte[32])void'] | undefined]>

  /**
   * Calls the update_tier(address,uint8)void ABI method.
   *
   * Upgrade KYC tier (e.g., basic → enhanced). Oracle-only.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  updateTier(params?: CallParams<KycRegistryArgs['obj']['update_tier(address,uint8)void'] | KycRegistryArgs['tuple']['update_tier(address,uint8)void']>): KycRegistryComposer<[...TReturns, KycRegistryReturns['update_tier(address,uint8)void'] | undefined]>

  /**
   * Calls the revoke(address)void ABI method.
   *
   * Revoke KYC for an address. Admin-only.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  revoke(params?: CallParams<KycRegistryArgs['obj']['revoke(address)void'] | KycRegistryArgs['tuple']['revoke(address)void']>): KycRegistryComposer<[...TReturns, KycRegistryReturns['revoke(address)void'] | undefined]>

  /**
   * Calls the set_oracle(address)void ABI method.
   *
   * Update the oracle address. Admin-only.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  setOracle(params?: CallParams<KycRegistryArgs['obj']['set_oracle(address)void'] | KycRegistryArgs['tuple']['set_oracle(address)void']>): KycRegistryComposer<[...TReturns, KycRegistryReturns['set_oracle(address)void'] | undefined]>

  /**
   * Calls the is_verified(address)bool ABI method.
   *
   * Check if an address has a valid KYC record.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  isVerified(params?: CallParams<KycRegistryArgs['obj']['is_verified(address)bool'] | KycRegistryArgs['tuple']['is_verified(address)bool']>): KycRegistryComposer<[...TReturns, KycRegistryReturns['is_verified(address)bool'] | undefined]>

  /**
   * Calls the get_record(address)(uint8,uint8,uint64,byte[32]) ABI method.
   *
   * Get the full KYC record for an address. Reverts if not found.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  getRecord(params?: CallParams<KycRegistryArgs['obj']['get_record(address)(uint8,uint8,uint64,byte[32])'] | KycRegistryArgs['tuple']['get_record(address)(uint8,uint8,uint64,byte[32])']>): KycRegistryComposer<[...TReturns, KycRegistryReturns['get_record(address)(uint8,uint8,uint64,byte[32])'] | undefined]>

  /**
   * Calls the get_role(address)uint8 ABI method.
   *
   * Get the role for an address. Returns 0 if not registered.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  getRole(params?: CallParams<KycRegistryArgs['obj']['get_role(address)uint8'] | KycRegistryArgs['tuple']['get_role(address)uint8']>): KycRegistryComposer<[...TReturns, KycRegistryReturns['get_role(address)uint8'] | undefined]>

  /**
   * Calls the get_stats()uint64 ABI method.
   *
   * Return the total number of verified accounts.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  getStats(params?: CallParams<KycRegistryArgs['obj']['get_stats()uint64'] | KycRegistryArgs['tuple']['get_stats()uint64']>): KycRegistryComposer<[...TReturns, KycRegistryReturns['get_stats()uint64'] | undefined]>

  /**
   * Makes a clear_state call to an existing instance of the KYCRegistry smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): KycRegistryComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): KycRegistryComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<KycRegistryComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<KycRegistryComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<KycRegistryComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<KycRegistryComposerResults<TReturns>>
}
export type KycRegistryComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

