/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"RantaiAdil","structs":{"Contributor":[{"name":"address","type":"address"},{"name":"sharePercentage","type":"uint64"},{"name":"paid","type":"bool"}]},"methods":[{"name":"hello","args":[{"type":"string","name":"name"}],"returns":{"type":"string"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}},{"name":"setup_project","args":[{"type":"address","name":"manager","desc":"Address of the project manager"},{"type":"address","name":"client","desc":"Address of the client who will pay"}],"returns":{"type":"string","desc":"Success message"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Initialize the project with manager and client addresses.\nCan only be called once.","events":[],"recommendations":{}},{"name":"add_contributor","args":[{"type":"address","name":"contributor_address","desc":"Address of the contributor"},{"type":"uint64","name":"share_percentage","desc":"Their share (0-100)"}],"returns":{"type":"string","desc":"Success message"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Add a contributor with their share percentage.\nOnly manager can call this before locking.","events":[],"recommendations":{}},{"name":"lock_contributors","args":[],"returns":{"type":"string","desc":"Success message"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Lock the contributors list to prevent modifications.\nOnly manager can call this. Validates that total shares equal 100%.","events":[],"recommendations":{}},{"name":"fund_project","args":[{"type":"pay","name":"payment","desc":"Payment transaction from client"}],"returns":{"type":"string","desc":"Success message"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Client funds the project by sending payment to contract.\nCan only be called after contributors are locked.","events":[],"recommendations":{}},{"name":"complete_and_payout","args":[],"returns":{"type":"string","desc":"Success message"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Mark project as complete and distribute funds to all contributors.\nOnly manager or client can call this.","events":[],"recommendations":{}},{"name":"get_project_status","args":[],"returns":{"type":"string","desc":"Status message"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Get current project status.","events":[],"recommendations":{}},{"name":"get_contributor_count","args":[],"returns":{"type":"uint64","desc":"Number of contributors"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Get the number of contributors.","events":[],"recommendations":{}}],"arcs":[22,28],"networks":{},"state":{"schema":{"global":{"ints":4,"bytes":2},"local":{"ints":0,"bytes":0}},"keys":{"global":{"manager":{"keyType":"AVMString","valueType":"address","key":"bWFuYWdlcg=="},"client":{"keyType":"AVMString","valueType":"address","key":"Y2xpZW50"},"total_funded":{"keyType":"AVMString","valueType":"AVMUint64","key":"dG90YWxfZnVuZGVk"},"is_locked":{"keyType":"AVMString","valueType":"AVMUint64","key":"aXNfbG9ja2Vk"},"is_completed":{"keyType":"AVMString","valueType":"AVMUint64","key":"aXNfY29tcGxldGVk"},"contributor_count":{"keyType":"AVMString","valueType":"AVMUint64","key":"Y29udHJpYnV0b3JfY291bnQ="}},"local":{},"box":{}},"maps":{"global":{},"local":{},"box":{"contributors":{"keyType":"uint64","valueType":"Contributor","prefix":"Y29udHJpYnV0b3Jz"}}}},"bareActions":{"create":["NoOp"],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[451],"errorMessage":"Already locked"},{"pc":[352],"errorMessage":"Contributors list is locked"},{"pc":[579],"errorMessage":"Contributors must be locked first"},{"pc":[363],"errorMessage":"Invalid percentage"},{"pc":[602],"errorMessage":"Minimum funding is 1 ALGO"},{"pc":[460],"errorMessage":"No contributors added"},{"pc":[131],"errorMessage":"OnCompletion must be NoOp"},{"pc":[206],"errorMessage":"OnCompletion must be NoOp && can only call when creating"},{"pc":[574],"errorMessage":"Only client can fund"},{"pc":[346],"errorMessage":"Only manager can add contributors"},{"pc":[445],"errorMessage":"Only manager can lock contributors"},{"pc":[677],"errorMessage":"Only manager or client can complete"},{"pc":[593],"errorMessage":"Payment must go to contract"},{"pc":[586,689],"errorMessage":"Project already completed"},{"pc":[275],"errorMessage":"Project already initialized"},{"pc":[682],"errorMessage":"Project not funded yet"},{"pc":[497],"errorMessage":"Shares must sum to 100%"},{"pc":[570,669],"errorMessage":"check self.client exists"},{"pc":[377,459,696,1023],"errorMessage":"check self.contributor_count exists"},{"pc":[478,722],"errorMessage":"check self.contributors entry exists"},{"pc":[584,687,941],"errorMessage":"check self.is_completed exists"},{"pc":[350,449,578,846],"errorMessage":"check self.is_locked exists"},{"pc":[271,342,441,658],"errorMessage":"check self.manager exists"},{"pc":[606,681,733,901],"errorMessage":"check self.total_funded exists"},{"pc":[214],"errorMessage":"invalid array length header"},{"pc":[222],"errorMessage":"invalid number of bytes for arc4.dynamic_array<arc4.uint8>"},{"pc":[259,267,327],"errorMessage":"invalid number of bytes for arc4.static_array<arc4.uint8, 32>"},{"pc":[336],"errorMessage":"invalid number of bytes for arc4.uint64"},{"pc":[565],"errorMessage":"transaction type is pay"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"events":[]} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


// Type definitions for ARC-56 structs

export type Contributor = {
  address: string,
  sharePercentage: bigint,
  paid: boolean
}


/**
 * Converts the ABI tuple representation of a Contributor to the struct representation
 */
export function ContributorFromTuple(abiTuple: [string, bigint, boolean]) {
  return getABIStructFromABITuple(abiTuple, APP_SPEC.structs.Contributor, APP_SPEC.structs) as Contributor
}

/**
 * The argument types for the RantaiAdil contract
 */
export type RantaiAdilArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'hello(string)string': {
      name: string
    }
    'setup_project(address,address)string': {
      /**
       * Address of the project manager
       */
      manager: string
      /**
       * Address of the client who will pay
       */
      client: string
    }
    'add_contributor(address,uint64)string': {
      /**
       * Address of the contributor
       */
      contributorAddress: string
      /**
       * Their share (0-100)
       */
      sharePercentage: bigint | number
    }
    'lock_contributors()string': Record<string, never>
    'fund_project(pay)string': {
      /**
       * Payment transaction from client
       */
      payment: AppMethodCallTransactionArgument
    }
    'complete_and_payout()string': Record<string, never>
    'get_project_status()string': Record<string, never>
    'get_contributor_count()uint64': Record<string, never>
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'hello(string)string': [name: string]
    'setup_project(address,address)string': [manager: string, client: string]
    'add_contributor(address,uint64)string': [contributorAddress: string, sharePercentage: bigint | number]
    'lock_contributors()string': []
    'fund_project(pay)string': [payment: AppMethodCallTransactionArgument]
    'complete_and_payout()string': []
    'get_project_status()string': []
    'get_contributor_count()uint64': []
  }
}

/**
 * The return type for each method
 */
export type RantaiAdilReturns = {
  'hello(string)string': string
  'setup_project(address,address)string': string
  'add_contributor(address,uint64)string': string
  'lock_contributors()string': string
  'fund_project(pay)string': string
  'complete_and_payout()string': string
  'get_project_status()string': string
  'get_contributor_count()uint64': bigint
}

/**
 * Defines the types of available calls and state of the RantaiAdil smart contract.
 */
export type RantaiAdilTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'hello(string)string' | 'hello', {
      argsObj: RantaiAdilArgs['obj']['hello(string)string']
      argsTuple: RantaiAdilArgs['tuple']['hello(string)string']
      returns: RantaiAdilReturns['hello(string)string']
    }>
    & Record<'setup_project(address,address)string' | 'setup_project', {
      argsObj: RantaiAdilArgs['obj']['setup_project(address,address)string']
      argsTuple: RantaiAdilArgs['tuple']['setup_project(address,address)string']
      /**
       * Success message
       */
      returns: RantaiAdilReturns['setup_project(address,address)string']
    }>
    & Record<'add_contributor(address,uint64)string' | 'add_contributor', {
      argsObj: RantaiAdilArgs['obj']['add_contributor(address,uint64)string']
      argsTuple: RantaiAdilArgs['tuple']['add_contributor(address,uint64)string']
      /**
       * Success message
       */
      returns: RantaiAdilReturns['add_contributor(address,uint64)string']
    }>
    & Record<'lock_contributors()string' | 'lock_contributors', {
      argsObj: RantaiAdilArgs['obj']['lock_contributors()string']
      argsTuple: RantaiAdilArgs['tuple']['lock_contributors()string']
      /**
       * Success message
       */
      returns: RantaiAdilReturns['lock_contributors()string']
    }>
    & Record<'fund_project(pay)string' | 'fund_project', {
      argsObj: RantaiAdilArgs['obj']['fund_project(pay)string']
      argsTuple: RantaiAdilArgs['tuple']['fund_project(pay)string']
      /**
       * Success message
       */
      returns: RantaiAdilReturns['fund_project(pay)string']
    }>
    & Record<'complete_and_payout()string' | 'complete_and_payout', {
      argsObj: RantaiAdilArgs['obj']['complete_and_payout()string']
      argsTuple: RantaiAdilArgs['tuple']['complete_and_payout()string']
      /**
       * Success message
       */
      returns: RantaiAdilReturns['complete_and_payout()string']
    }>
    & Record<'get_project_status()string' | 'get_project_status', {
      argsObj: RantaiAdilArgs['obj']['get_project_status()string']
      argsTuple: RantaiAdilArgs['tuple']['get_project_status()string']
      /**
       * Status message
       */
      returns: RantaiAdilReturns['get_project_status()string']
    }>
    & Record<'get_contributor_count()uint64' | 'get_contributor_count', {
      argsObj: RantaiAdilArgs['obj']['get_contributor_count()uint64']
      argsTuple: RantaiAdilArgs['tuple']['get_contributor_count()uint64']
      /**
       * Number of contributors
       */
      returns: RantaiAdilReturns['get_contributor_count()uint64']
    }>
  /**
   * Defines the shape of the state of the application.
   */
  state: {
    global: {
      keys: {
        manager: string
        client: string
        totalFunded: bigint
        isLocked: bigint
        isCompleted: bigint
        contributorCount: bigint
      }
      maps: {}
    }
    box: {
      keys: {}
      maps: {
        contributors: Map<bigint | number, Contributor>
      }
    }
  }
}

/**
 * Defines the possible abi call signatures.
 */
export type RantaiAdilSignatures = keyof RantaiAdilTypes['methods']
/**
 * Defines the possible abi call signatures for methods that return a non-void value.
 */
export type RantaiAdilNonVoidMethodSignatures = keyof RantaiAdilTypes['methods'] extends infer T ? T extends keyof RantaiAdilTypes['methods'] ? MethodReturn<T> extends void ? never : T  : never : never
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the RantaiAdil smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends RantaiAdilSignatures> = RantaiAdilTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the RantaiAdil smart contract to the method's return type
 */
export type MethodReturn<TSignature extends RantaiAdilSignatures> = RantaiAdilTypes['methods'][TSignature]['returns']

/**
 * Defines the shape of the keyed global state of the application.
 */
export type GlobalKeysState = RantaiAdilTypes['state']['global']['keys']

/**
 * Defines the shape of the keyed box state of the application.
 */
export type BoxKeysState = RantaiAdilTypes['state']['box']['keys']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the RantaiAdil smart contract
 */
export abstract class RantaiAdilParamsFactory {
  /**
   * Constructs a no op call for the hello(string)string ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static hello(params: CallParams<RantaiAdilArgs['obj']['hello(string)string'] | RantaiAdilArgs['tuple']['hello(string)string']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'hello(string)string' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.name],
    }
  }
  /**
   * Constructs a no op call for the setup_project(address,address)string ABI method
   *
  * Initialize the project with manager and client addresses.
  Can only be called once.

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static setupProject(params: CallParams<RantaiAdilArgs['obj']['setup_project(address,address)string'] | RantaiAdilArgs['tuple']['setup_project(address,address)string']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'setup_project(address,address)string' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.manager, params.args.client],
    }
  }
  /**
   * Constructs a no op call for the add_contributor(address,uint64)string ABI method
   *
  * Add a contributor with their share percentage.
  Only manager can call this before locking.

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static addContributor(params: CallParams<RantaiAdilArgs['obj']['add_contributor(address,uint64)string'] | RantaiAdilArgs['tuple']['add_contributor(address,uint64)string']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'add_contributor(address,uint64)string' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.contributorAddress, params.args.sharePercentage],
    }
  }
  /**
   * Constructs a no op call for the lock_contributors()string ABI method
   *
  * Lock the contributors list to prevent modifications.
  Only manager can call this. Validates that total shares equal 100%.

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static lockContributors(params: CallParams<RantaiAdilArgs['obj']['lock_contributors()string'] | RantaiAdilArgs['tuple']['lock_contributors()string']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'lock_contributors()string' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the fund_project(pay)string ABI method
   *
  * Client funds the project by sending payment to contract.
  Can only be called after contributors are locked.

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static fundProject(params: CallParams<RantaiAdilArgs['obj']['fund_project(pay)string'] | RantaiAdilArgs['tuple']['fund_project(pay)string']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'fund_project(pay)string' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.payment],
    }
  }
  /**
   * Constructs a no op call for the complete_and_payout()string ABI method
   *
  * Mark project as complete and distribute funds to all contributors.
  Only manager or client can call this.

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static completeAndPayout(params: CallParams<RantaiAdilArgs['obj']['complete_and_payout()string'] | RantaiAdilArgs['tuple']['complete_and_payout()string']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'complete_and_payout()string' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the get_project_status()string ABI method
   *
   * Get current project status.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static getProjectStatus(params: CallParams<RantaiAdilArgs['obj']['get_project_status()string'] | RantaiAdilArgs['tuple']['get_project_status()string']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'get_project_status()string' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the get_contributor_count()uint64 ABI method
   *
   * Get the number of contributors.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static getContributorCount(params: CallParams<RantaiAdilArgs['obj']['get_contributor_count()uint64'] | RantaiAdilArgs['tuple']['get_contributor_count()uint64']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'get_contributor_count()uint64' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
}

/**
 * A client to make calls to the RantaiAdil smart contract
 */
export class RantaiAdilClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `RantaiAdilClient`
   *
   * @param appClient An `AppClient` instance which has been created with the RantaiAdil app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `RantaiAdilClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Checks for decode errors on the given return value and maps the return value to the return type for the given method
   * @returns The typed return value or undefined if there was no value
   */
  decodeReturnValue<TSignature extends RantaiAdilNonVoidMethodSignatures>(method: TSignature, returnValue: ABIReturn | undefined) {
    return returnValue !== undefined ? getArc56ReturnValue<MethodReturn<TSignature>>(returnValue, this.appClient.getABIMethod(method), APP_SPEC.structs) : undefined
  }

  /**
   * Returns a new `RantaiAdilClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<RantaiAdilClient> {
    return new RantaiAdilClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `RantaiAdilClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<RantaiAdilClient> {
    return new RantaiAdilClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Makes a clear_state call to an existing instance of the RantaiAdil smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the RantaiAdil smart contract using the `hello(string)string` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    hello: (params: CallParams<RantaiAdilArgs['obj']['hello(string)string'] | RantaiAdilArgs['tuple']['hello(string)string']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(RantaiAdilParamsFactory.hello(params))
    },

    /**
     * Makes a call to the RantaiAdil smart contract using the `setup_project(address,address)string` ABI method.
     *
    * Initialize the project with manager and client addresses.
    Can only be called once.

     *
     * @param params The params for the smart contract call
     * @returns The call params: Success message
     */
    setupProject: (params: CallParams<RantaiAdilArgs['obj']['setup_project(address,address)string'] | RantaiAdilArgs['tuple']['setup_project(address,address)string']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(RantaiAdilParamsFactory.setupProject(params))
    },

    /**
     * Makes a call to the RantaiAdil smart contract using the `add_contributor(address,uint64)string` ABI method.
     *
    * Add a contributor with their share percentage.
    Only manager can call this before locking.

     *
     * @param params The params for the smart contract call
     * @returns The call params: Success message
     */
    addContributor: (params: CallParams<RantaiAdilArgs['obj']['add_contributor(address,uint64)string'] | RantaiAdilArgs['tuple']['add_contributor(address,uint64)string']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(RantaiAdilParamsFactory.addContributor(params))
    },

    /**
     * Makes a call to the RantaiAdil smart contract using the `lock_contributors()string` ABI method.
     *
    * Lock the contributors list to prevent modifications.
    Only manager can call this. Validates that total shares equal 100%.

     *
     * @param params The params for the smart contract call
     * @returns The call params: Success message
     */
    lockContributors: (params: CallParams<RantaiAdilArgs['obj']['lock_contributors()string'] | RantaiAdilArgs['tuple']['lock_contributors()string']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(RantaiAdilParamsFactory.lockContributors(params))
    },

    /**
     * Makes a call to the RantaiAdil smart contract using the `fund_project(pay)string` ABI method.
     *
    * Client funds the project by sending payment to contract.
    Can only be called after contributors are locked.

     *
     * @param params The params for the smart contract call
     * @returns The call params: Success message
     */
    fundProject: (params: CallParams<RantaiAdilArgs['obj']['fund_project(pay)string'] | RantaiAdilArgs['tuple']['fund_project(pay)string']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(RantaiAdilParamsFactory.fundProject(params))
    },

    /**
     * Makes a call to the RantaiAdil smart contract using the `complete_and_payout()string` ABI method.
     *
    * Mark project as complete and distribute funds to all contributors.
    Only manager or client can call this.

     *
     * @param params The params for the smart contract call
     * @returns The call params: Success message
     */
    completeAndPayout: (params: CallParams<RantaiAdilArgs['obj']['complete_and_payout()string'] | RantaiAdilArgs['tuple']['complete_and_payout()string']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(RantaiAdilParamsFactory.completeAndPayout(params))
    },

    /**
     * Makes a call to the RantaiAdil smart contract using the `get_project_status()string` ABI method.
     *
     * Get current project status.
     *
     * @param params The params for the smart contract call
     * @returns The call params: Status message
     */
    getProjectStatus: (params: CallParams<RantaiAdilArgs['obj']['get_project_status()string'] | RantaiAdilArgs['tuple']['get_project_status()string']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(RantaiAdilParamsFactory.getProjectStatus(params))
    },

    /**
     * Makes a call to the RantaiAdil smart contract using the `get_contributor_count()uint64` ABI method.
     *
     * Get the number of contributors.
     *
     * @param params The params for the smart contract call
     * @returns The call params: Number of contributors
     */
    getContributorCount: (params: CallParams<RantaiAdilArgs['obj']['get_contributor_count()uint64'] | RantaiAdilArgs['tuple']['get_contributor_count()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(RantaiAdilParamsFactory.getContributorCount(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Makes a clear_state call to an existing instance of the RantaiAdil smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the RantaiAdil smart contract using the `hello(string)string` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    hello: (params: CallParams<RantaiAdilArgs['obj']['hello(string)string'] | RantaiAdilArgs['tuple']['hello(string)string']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(RantaiAdilParamsFactory.hello(params))
    },

    /**
     * Makes a call to the RantaiAdil smart contract using the `setup_project(address,address)string` ABI method.
     *
    * Initialize the project with manager and client addresses.
    Can only be called once.

     *
     * @param params The params for the smart contract call
     * @returns The call transaction: Success message
     */
    setupProject: (params: CallParams<RantaiAdilArgs['obj']['setup_project(address,address)string'] | RantaiAdilArgs['tuple']['setup_project(address,address)string']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(RantaiAdilParamsFactory.setupProject(params))
    },

    /**
     * Makes a call to the RantaiAdil smart contract using the `add_contributor(address,uint64)string` ABI method.
     *
    * Add a contributor with their share percentage.
    Only manager can call this before locking.

     *
     * @param params The params for the smart contract call
     * @returns The call transaction: Success message
     */
    addContributor: (params: CallParams<RantaiAdilArgs['obj']['add_contributor(address,uint64)string'] | RantaiAdilArgs['tuple']['add_contributor(address,uint64)string']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(RantaiAdilParamsFactory.addContributor(params))
    },

    /**
     * Makes a call to the RantaiAdil smart contract using the `lock_contributors()string` ABI method.
     *
    * Lock the contributors list to prevent modifications.
    Only manager can call this. Validates that total shares equal 100%.

     *
     * @param params The params for the smart contract call
     * @returns The call transaction: Success message
     */
    lockContributors: (params: CallParams<RantaiAdilArgs['obj']['lock_contributors()string'] | RantaiAdilArgs['tuple']['lock_contributors()string']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(RantaiAdilParamsFactory.lockContributors(params))
    },

    /**
     * Makes a call to the RantaiAdil smart contract using the `fund_project(pay)string` ABI method.
     *
    * Client funds the project by sending payment to contract.
    Can only be called after contributors are locked.

     *
     * @param params The params for the smart contract call
     * @returns The call transaction: Success message
     */
    fundProject: (params: CallParams<RantaiAdilArgs['obj']['fund_project(pay)string'] | RantaiAdilArgs['tuple']['fund_project(pay)string']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(RantaiAdilParamsFactory.fundProject(params))
    },

    /**
     * Makes a call to the RantaiAdil smart contract using the `complete_and_payout()string` ABI method.
     *
    * Mark project as complete and distribute funds to all contributors.
    Only manager or client can call this.

     *
     * @param params The params for the smart contract call
     * @returns The call transaction: Success message
     */
    completeAndPayout: (params: CallParams<RantaiAdilArgs['obj']['complete_and_payout()string'] | RantaiAdilArgs['tuple']['complete_and_payout()string']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(RantaiAdilParamsFactory.completeAndPayout(params))
    },

    /**
     * Makes a call to the RantaiAdil smart contract using the `get_project_status()string` ABI method.
     *
     * Get current project status.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction: Status message
     */
    getProjectStatus: (params: CallParams<RantaiAdilArgs['obj']['get_project_status()string'] | RantaiAdilArgs['tuple']['get_project_status()string']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(RantaiAdilParamsFactory.getProjectStatus(params))
    },

    /**
     * Makes a call to the RantaiAdil smart contract using the `get_contributor_count()uint64` ABI method.
     *
     * Get the number of contributors.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction: Number of contributors
     */
    getContributorCount: (params: CallParams<RantaiAdilArgs['obj']['get_contributor_count()uint64'] | RantaiAdilArgs['tuple']['get_contributor_count()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(RantaiAdilParamsFactory.getContributorCount(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Makes a clear_state call to an existing instance of the RantaiAdil smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the RantaiAdil smart contract using the `hello(string)string` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    hello: async (params: CallParams<RantaiAdilArgs['obj']['hello(string)string'] | RantaiAdilArgs['tuple']['hello(string)string']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(RantaiAdilParamsFactory.hello(params))
      return {...result, return: result.return as unknown as (undefined | RantaiAdilReturns['hello(string)string'])}
    },

    /**
     * Makes a call to the RantaiAdil smart contract using the `setup_project(address,address)string` ABI method.
     *
    * Initialize the project with manager and client addresses.
    Can only be called once.

     *
     * @param params The params for the smart contract call
     * @returns The call result: Success message
     */
    setupProject: async (params: CallParams<RantaiAdilArgs['obj']['setup_project(address,address)string'] | RantaiAdilArgs['tuple']['setup_project(address,address)string']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(RantaiAdilParamsFactory.setupProject(params))
      return {...result, return: result.return as unknown as (undefined | RantaiAdilReturns['setup_project(address,address)string'])}
    },

    /**
     * Makes a call to the RantaiAdil smart contract using the `add_contributor(address,uint64)string` ABI method.
     *
    * Add a contributor with their share percentage.
    Only manager can call this before locking.

     *
     * @param params The params for the smart contract call
     * @returns The call result: Success message
     */
    addContributor: async (params: CallParams<RantaiAdilArgs['obj']['add_contributor(address,uint64)string'] | RantaiAdilArgs['tuple']['add_contributor(address,uint64)string']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(RantaiAdilParamsFactory.addContributor(params))
      return {...result, return: result.return as unknown as (undefined | RantaiAdilReturns['add_contributor(address,uint64)string'])}
    },

    /**
     * Makes a call to the RantaiAdil smart contract using the `lock_contributors()string` ABI method.
     *
    * Lock the contributors list to prevent modifications.
    Only manager can call this. Validates that total shares equal 100%.

     *
     * @param params The params for the smart contract call
     * @returns The call result: Success message
     */
    lockContributors: async (params: CallParams<RantaiAdilArgs['obj']['lock_contributors()string'] | RantaiAdilArgs['tuple']['lock_contributors()string']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(RantaiAdilParamsFactory.lockContributors(params))
      return {...result, return: result.return as unknown as (undefined | RantaiAdilReturns['lock_contributors()string'])}
    },

    /**
     * Makes a call to the RantaiAdil smart contract using the `fund_project(pay)string` ABI method.
     *
    * Client funds the project by sending payment to contract.
    Can only be called after contributors are locked.

     *
     * @param params The params for the smart contract call
     * @returns The call result: Success message
     */
    fundProject: async (params: CallParams<RantaiAdilArgs['obj']['fund_project(pay)string'] | RantaiAdilArgs['tuple']['fund_project(pay)string']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(RantaiAdilParamsFactory.fundProject(params))
      return {...result, return: result.return as unknown as (undefined | RantaiAdilReturns['fund_project(pay)string'])}
    },

    /**
     * Makes a call to the RantaiAdil smart contract using the `complete_and_payout()string` ABI method.
     *
    * Mark project as complete and distribute funds to all contributors.
    Only manager or client can call this.

     *
     * @param params The params for the smart contract call
     * @returns The call result: Success message
     */
    completeAndPayout: async (params: CallParams<RantaiAdilArgs['obj']['complete_and_payout()string'] | RantaiAdilArgs['tuple']['complete_and_payout()string']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(RantaiAdilParamsFactory.completeAndPayout(params))
      return {...result, return: result.return as unknown as (undefined | RantaiAdilReturns['complete_and_payout()string'])}
    },

    /**
     * Makes a call to the RantaiAdil smart contract using the `get_project_status()string` ABI method.
     *
     * Get current project status.
     *
     * @param params The params for the smart contract call
     * @returns The call result: Status message
     */
    getProjectStatus: async (params: CallParams<RantaiAdilArgs['obj']['get_project_status()string'] | RantaiAdilArgs['tuple']['get_project_status()string']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(RantaiAdilParamsFactory.getProjectStatus(params))
      return {...result, return: result.return as unknown as (undefined | RantaiAdilReturns['get_project_status()string'])}
    },

    /**
     * Makes a call to the RantaiAdil smart contract using the `get_contributor_count()uint64` ABI method.
     *
     * Get the number of contributors.
     *
     * @param params The params for the smart contract call
     * @returns The call result: Number of contributors
     */
    getContributorCount: async (params: CallParams<RantaiAdilArgs['obj']['get_contributor_count()uint64'] | RantaiAdilArgs['tuple']['get_contributor_count()uint64']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(RantaiAdilParamsFactory.getContributorCount(params))
      return {...result, return: result.return as unknown as (undefined | RantaiAdilReturns['get_contributor_count()uint64'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new RantaiAdilClient(this.appClient.clone(params))
  }

  /**
   * Methods to access state for the current RantaiAdil app
   */
  state = {
    /**
     * Methods to access global state for the current RantaiAdil app
     */
    global: {
      /**
       * Get all current keyed values from global state
       */
      getAll: async (): Promise<Partial<Expand<GlobalKeysState>>> => {
        const result = await this.appClient.state.global.getAll()
        return {
          manager: result.manager,
          client: result.client,
          totalFunded: result.total_funded,
          isLocked: result.is_locked,
          isCompleted: result.is_completed,
          contributorCount: result.contributor_count,
        }
      },
      /**
       * Get the current value of the manager key in global state
       */
      manager: async (): Promise<string | undefined> => { return (await this.appClient.state.global.getValue("manager")) as string | undefined },
      /**
       * Get the current value of the client key in global state
       */
      client: async (): Promise<string | undefined> => { return (await this.appClient.state.global.getValue("client")) as string | undefined },
      /**
       * Get the current value of the total_funded key in global state
       */
      totalFunded: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("total_funded")) as bigint | undefined },
      /**
       * Get the current value of the is_locked key in global state
       */
      isLocked: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("is_locked")) as bigint | undefined },
      /**
       * Get the current value of the is_completed key in global state
       */
      isCompleted: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("is_completed")) as bigint | undefined },
      /**
       * Get the current value of the contributor_count key in global state
       */
      contributorCount: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("contributor_count")) as bigint | undefined },
    },
    /**
     * Methods to access box state for the current RantaiAdil app
     */
    box: {
      /**
       * Get all current keyed values from box state
       */
      getAll: async (): Promise<Partial<Expand<BoxKeysState>>> => {
        const result = await this.appClient.state.box.getAll()
        return {
        }
      },
      /**
       * Get values from the contributors map in box state
       */
      contributors: {
        /**
         * Get all current values of the contributors map in box state
         */
        getMap: async (): Promise<Map<bigint, Contributor>> => { return (await this.appClient.state.box.getMap("contributors")) as Map<bigint, Contributor> },
        /**
         * Get a current value of the contributors map by key from box state
         */
        value: async (key: bigint | number): Promise<Contributor | undefined> => { return await this.appClient.state.box.getMapValue("contributors", key) as Contributor | undefined },
      },
    },
  }

  public newGroup(): RantaiAdilComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a hello(string)string method call against the RantaiAdil contract
       */
      hello(params: CallParams<RantaiAdilArgs['obj']['hello(string)string'] | RantaiAdilArgs['tuple']['hello(string)string']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.hello(params)))
        resultMappers.push((v) => client.decodeReturnValue('hello(string)string', v))
        return this
      },
      /**
       * Add a setup_project(address,address)string method call against the RantaiAdil contract
       */
      setupProject(params: CallParams<RantaiAdilArgs['obj']['setup_project(address,address)string'] | RantaiAdilArgs['tuple']['setup_project(address,address)string']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.setupProject(params)))
        resultMappers.push((v) => client.decodeReturnValue('setup_project(address,address)string', v))
        return this
      },
      /**
       * Add a add_contributor(address,uint64)string method call against the RantaiAdil contract
       */
      addContributor(params: CallParams<RantaiAdilArgs['obj']['add_contributor(address,uint64)string'] | RantaiAdilArgs['tuple']['add_contributor(address,uint64)string']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.addContributor(params)))
        resultMappers.push((v) => client.decodeReturnValue('add_contributor(address,uint64)string', v))
        return this
      },
      /**
       * Add a lock_contributors()string method call against the RantaiAdil contract
       */
      lockContributors(params: CallParams<RantaiAdilArgs['obj']['lock_contributors()string'] | RantaiAdilArgs['tuple']['lock_contributors()string']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.lockContributors(params)))
        resultMappers.push((v) => client.decodeReturnValue('lock_contributors()string', v))
        return this
      },
      /**
       * Add a fund_project(pay)string method call against the RantaiAdil contract
       */
      fundProject(params: CallParams<RantaiAdilArgs['obj']['fund_project(pay)string'] | RantaiAdilArgs['tuple']['fund_project(pay)string']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.fundProject(params)))
        resultMappers.push((v) => client.decodeReturnValue('fund_project(pay)string', v))
        return this
      },
      /**
       * Add a complete_and_payout()string method call against the RantaiAdil contract
       */
      completeAndPayout(params: CallParams<RantaiAdilArgs['obj']['complete_and_payout()string'] | RantaiAdilArgs['tuple']['complete_and_payout()string']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.completeAndPayout(params)))
        resultMappers.push((v) => client.decodeReturnValue('complete_and_payout()string', v))
        return this
      },
      /**
       * Add a get_project_status()string method call against the RantaiAdil contract
       */
      getProjectStatus(params: CallParams<RantaiAdilArgs['obj']['get_project_status()string'] | RantaiAdilArgs['tuple']['get_project_status()string']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.getProjectStatus(params)))
        resultMappers.push((v) => client.decodeReturnValue('get_project_status()string', v))
        return this
      },
      /**
       * Add a get_contributor_count()uint64 method call against the RantaiAdil contract
       */
      getContributorCount(params: CallParams<RantaiAdilArgs['obj']['get_contributor_count()uint64'] | RantaiAdilArgs['tuple']['get_contributor_count()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.getContributorCount(params)))
        resultMappers.push((v) => client.decodeReturnValue('get_contributor_count()uint64', v))
        return this
      },
      /**
       * Add a clear state call to the RantaiAdil contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as RantaiAdilComposer
  }
}
export type RantaiAdilComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the hello(string)string ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  hello(params?: CallParams<RantaiAdilArgs['obj']['hello(string)string'] | RantaiAdilArgs['tuple']['hello(string)string']>): RantaiAdilComposer<[...TReturns, RantaiAdilReturns['hello(string)string'] | undefined]>

  /**
   * Calls the setup_project(address,address)string ABI method.
   *
  * Initialize the project with manager and client addresses.
  Can only be called once.

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  setupProject(params?: CallParams<RantaiAdilArgs['obj']['setup_project(address,address)string'] | RantaiAdilArgs['tuple']['setup_project(address,address)string']>): RantaiAdilComposer<[...TReturns, RantaiAdilReturns['setup_project(address,address)string'] | undefined]>

  /**
   * Calls the add_contributor(address,uint64)string ABI method.
   *
  * Add a contributor with their share percentage.
  Only manager can call this before locking.

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  addContributor(params?: CallParams<RantaiAdilArgs['obj']['add_contributor(address,uint64)string'] | RantaiAdilArgs['tuple']['add_contributor(address,uint64)string']>): RantaiAdilComposer<[...TReturns, RantaiAdilReturns['add_contributor(address,uint64)string'] | undefined]>

  /**
   * Calls the lock_contributors()string ABI method.
   *
  * Lock the contributors list to prevent modifications.
  Only manager can call this. Validates that total shares equal 100%.

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  lockContributors(params?: CallParams<RantaiAdilArgs['obj']['lock_contributors()string'] | RantaiAdilArgs['tuple']['lock_contributors()string']>): RantaiAdilComposer<[...TReturns, RantaiAdilReturns['lock_contributors()string'] | undefined]>

  /**
   * Calls the fund_project(pay)string ABI method.
   *
  * Client funds the project by sending payment to contract.
  Can only be called after contributors are locked.

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  fundProject(params?: CallParams<RantaiAdilArgs['obj']['fund_project(pay)string'] | RantaiAdilArgs['tuple']['fund_project(pay)string']>): RantaiAdilComposer<[...TReturns, RantaiAdilReturns['fund_project(pay)string'] | undefined]>

  /**
   * Calls the complete_and_payout()string ABI method.
   *
  * Mark project as complete and distribute funds to all contributors.
  Only manager or client can call this.

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  completeAndPayout(params?: CallParams<RantaiAdilArgs['obj']['complete_and_payout()string'] | RantaiAdilArgs['tuple']['complete_and_payout()string']>): RantaiAdilComposer<[...TReturns, RantaiAdilReturns['complete_and_payout()string'] | undefined]>

  /**
   * Calls the get_project_status()string ABI method.
   *
   * Get current project status.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  getProjectStatus(params?: CallParams<RantaiAdilArgs['obj']['get_project_status()string'] | RantaiAdilArgs['tuple']['get_project_status()string']>): RantaiAdilComposer<[...TReturns, RantaiAdilReturns['get_project_status()string'] | undefined]>

  /**
   * Calls the get_contributor_count()uint64 ABI method.
   *
   * Get the number of contributors.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  getContributorCount(params?: CallParams<RantaiAdilArgs['obj']['get_contributor_count()uint64'] | RantaiAdilArgs['tuple']['get_contributor_count()uint64']>): RantaiAdilComposer<[...TReturns, RantaiAdilReturns['get_contributor_count()uint64'] | undefined]>

  /**
   * Makes a clear_state call to an existing instance of the RantaiAdil smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): RantaiAdilComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): RantaiAdilComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<RantaiAdilComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<RantaiAdilComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<RantaiAdilComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<RantaiAdilComposerResults<TReturns>>
}
export type RantaiAdilComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

