/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"PetitionMaster","structs":{"MasterPetitionDetails":[{"name":"petitionAppId","type":"uint64"},{"name":"petitionTitle","type":"byte[]"},{"name":"petitionCreator","type":"address"},{"name":"creationTime","type":"uint64"}]},"methods":[{"name":"update","args":[],"returns":{"type":"void"},"actions":{"create":[],"call":["UpdateApplication"]},"readonly":false,"events":[],"recommendations":{}},{"name":"assign_template_app","args":[{"type":"application","name":"template_app"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}},{"name":"prime_initial_petition","args":[{"type":"pay","name":"mbr_payment"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}},{"name":"create_petition","args":[{"type":"byte[]","name":"petition_title"},{"type":"byte[]","name":"petition_text"},{"type":"pay","name":"mbr_payment"}],"returns":{"type":"uint64"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}},{"name":"additional_box_characters_option","args":[{"type":"byte[]","name":"additional_text"},{"type":"pay","name":"mbr_payment"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}},{"name":"prime_petition","args":[{"type":"application","name":"petition_app_id"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}},{"name":"sign_petition","args":[{"type":"application","name":"petition_app"},{"type":"pay","name":"mbr_payment"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}},{"name":"add_comment","args":[{"type":"application","name":"petition_app"},{"type":"byte[]","name":"text"},{"type":"pay","name":"mbr_payment"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}}],"arcs":[22,28],"networks":{},"state":{"schema":{"global":{"ints":4,"bytes":1},"local":{"ints":0,"bytes":0}},"keys":{"global":{"petition_template_app":{"keyType":"AVMString","valueType":"AVMUint64","key":"cGV0aXRpb25fdGVtcGxhdGVfYXBw"},"petition_uid":{"keyType":"AVMString","valueType":"uint64","key":"cGV0aXRpb25fdWlk"},"template_app_assigned":{"keyType":"AVMString","valueType":"AVMUint64","key":"dGVtcGxhdGVfYXBwX2Fzc2lnbmVk"},"previous_child_petition_app":{"keyType":"AVMString","valueType":"AVMUint64","key":"cHJldmlvdXNfY2hpbGRfcGV0aXRpb25fYXBw"},"next_child_petition_app":{"keyType":"AVMString","valueType":"AVMUint64","key":"bmV4dF9jaGlsZF9wZXRpdGlvbl9hcHA="}},"local":{},"box":{}},"maps":{"global":{},"local":{},"box":{"petition_details":{"keyType":"uint64","valueType":"MasterPetitionDetails","prefix":""}}}},"bareActions":{"create":["NoOp"],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[237,268,296,314,342,375,397],"errorMessage":"OnCompletion is not NoOp"},{"pc":[416],"errorMessage":"OnCompletion is not UpdateApplication"},{"pc":[490,501,624,724],"errorMessage":"account funded"},{"pc":[563,636,762,892,896,962,1031,1054],"errorMessage":"application exists"},{"pc":[618,825,1017,1109],"errorMessage":"application log value is not the result of an ABI return"},{"pc":[433],"errorMessage":"can only call when creating"},{"pc":[240,271,299,317,345,378,400,419],"errorMessage":"can only call when not creating"},{"pc":[554],"errorMessage":"check self.next_child_petition_app exists"},{"pc":[888],"errorMessage":"check self.petition_template_app exists"},{"pc":[543,714],"errorMessage":"check self.petition_uid exists"},{"pc":[758],"errorMessage":"check self.previous_child_petition_app exists"},{"pc":[528],"errorMessage":"check self.template_app_assigned exists"},{"pc":[837,863],"errorMessage":"transaction type is appl"},{"pc":[259,287,333,361,388],"errorMessage":"transaction type is pay"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"events":[]} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


// Type definitions for ARC-56 structs

export type MasterPetitionDetails = {
  petitionAppId: bigint,
  petitionTitle: Uint8Array,
  petitionCreator: string,
  creationTime: bigint
}


/**
 * Converts the ABI tuple representation of a MasterPetitionDetails to the struct representation
 */
export function MasterPetitionDetailsFromTuple(abiTuple: [bigint, Uint8Array, string, bigint]) {
  return getABIStructFromABITuple(abiTuple, APP_SPEC.structs.MasterPetitionDetails, APP_SPEC.structs) as MasterPetitionDetails
}

/**
 * The argument types for the PetitionMaster contract
 */
export type PetitionMasterArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'update()void': Record<string, never>
    'assign_template_app(application)void': {
      templateApp: bigint
    }
    'prime_initial_petition(pay)void': {
      mbrPayment: AppMethodCallTransactionArgument
    }
    'create_petition(byte[],byte[],pay)uint64': {
      petitionTitle: Uint8Array
      petitionText: Uint8Array
      mbrPayment: AppMethodCallTransactionArgument
    }
    'additional_box_characters_option(byte[],pay)void': {
      additionalText: Uint8Array
      mbrPayment: AppMethodCallTransactionArgument
    }
    'prime_petition(application)void': {
      petitionAppId: bigint
    }
    'sign_petition(application,pay)void': {
      petitionApp: bigint
      mbrPayment: AppMethodCallTransactionArgument
    }
    'add_comment(application,byte[],pay)void': {
      petitionApp: bigint
      text: Uint8Array
      mbrPayment: AppMethodCallTransactionArgument
    }
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'update()void': []
    'assign_template_app(application)void': [templateApp: bigint]
    'prime_initial_petition(pay)void': [mbrPayment: AppMethodCallTransactionArgument]
    'create_petition(byte[],byte[],pay)uint64': [petitionTitle: Uint8Array, petitionText: Uint8Array, mbrPayment: AppMethodCallTransactionArgument]
    'additional_box_characters_option(byte[],pay)void': [additionalText: Uint8Array, mbrPayment: AppMethodCallTransactionArgument]
    'prime_petition(application)void': [petitionAppId: bigint]
    'sign_petition(application,pay)void': [petitionApp: bigint, mbrPayment: AppMethodCallTransactionArgument]
    'add_comment(application,byte[],pay)void': [petitionApp: bigint, text: Uint8Array, mbrPayment: AppMethodCallTransactionArgument]
  }
}

/**
 * The return type for each method
 */
export type PetitionMasterReturns = {
  'update()void': void
  'assign_template_app(application)void': void
  'prime_initial_petition(pay)void': void
  'create_petition(byte[],byte[],pay)uint64': bigint
  'additional_box_characters_option(byte[],pay)void': void
  'prime_petition(application)void': void
  'sign_petition(application,pay)void': void
  'add_comment(application,byte[],pay)void': void
}

/**
 * Defines the types of available calls and state of the PetitionMaster smart contract.
 */
export type PetitionMasterTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'update()void' | 'update', {
      argsObj: PetitionMasterArgs['obj']['update()void']
      argsTuple: PetitionMasterArgs['tuple']['update()void']
      returns: PetitionMasterReturns['update()void']
    }>
    & Record<'assign_template_app(application)void' | 'assign_template_app', {
      argsObj: PetitionMasterArgs['obj']['assign_template_app(application)void']
      argsTuple: PetitionMasterArgs['tuple']['assign_template_app(application)void']
      returns: PetitionMasterReturns['assign_template_app(application)void']
    }>
    & Record<'prime_initial_petition(pay)void' | 'prime_initial_petition', {
      argsObj: PetitionMasterArgs['obj']['prime_initial_petition(pay)void']
      argsTuple: PetitionMasterArgs['tuple']['prime_initial_petition(pay)void']
      returns: PetitionMasterReturns['prime_initial_petition(pay)void']
    }>
    & Record<'create_petition(byte[],byte[],pay)uint64' | 'create_petition', {
      argsObj: PetitionMasterArgs['obj']['create_petition(byte[],byte[],pay)uint64']
      argsTuple: PetitionMasterArgs['tuple']['create_petition(byte[],byte[],pay)uint64']
      returns: PetitionMasterReturns['create_petition(byte[],byte[],pay)uint64']
    }>
    & Record<'additional_box_characters_option(byte[],pay)void' | 'additional_box_characters_option', {
      argsObj: PetitionMasterArgs['obj']['additional_box_characters_option(byte[],pay)void']
      argsTuple: PetitionMasterArgs['tuple']['additional_box_characters_option(byte[],pay)void']
      returns: PetitionMasterReturns['additional_box_characters_option(byte[],pay)void']
    }>
    & Record<'prime_petition(application)void' | 'prime_petition', {
      argsObj: PetitionMasterArgs['obj']['prime_petition(application)void']
      argsTuple: PetitionMasterArgs['tuple']['prime_petition(application)void']
      returns: PetitionMasterReturns['prime_petition(application)void']
    }>
    & Record<'sign_petition(application,pay)void' | 'sign_petition', {
      argsObj: PetitionMasterArgs['obj']['sign_petition(application,pay)void']
      argsTuple: PetitionMasterArgs['tuple']['sign_petition(application,pay)void']
      returns: PetitionMasterReturns['sign_petition(application,pay)void']
    }>
    & Record<'add_comment(application,byte[],pay)void' | 'add_comment', {
      argsObj: PetitionMasterArgs['obj']['add_comment(application,byte[],pay)void']
      argsTuple: PetitionMasterArgs['tuple']['add_comment(application,byte[],pay)void']
      returns: PetitionMasterReturns['add_comment(application,byte[],pay)void']
    }>
  /**
   * Defines the shape of the state of the application.
   */
  state: {
    global: {
      keys: {
        petitionTemplateApp: bigint
        petitionUid: bigint
        templateAppAssigned: bigint
        previousChildPetitionApp: bigint
        nextChildPetitionApp: bigint
      }
      maps: {}
    }
    box: {
      keys: {}
      maps: {
        petitionDetails: Map<bigint | number, MasterPetitionDetails>
      }
    }
  }
}

/**
 * Defines the possible abi call signatures.
 */
export type PetitionMasterSignatures = keyof PetitionMasterTypes['methods']
/**
 * Defines the possible abi call signatures for methods that return a non-void value.
 */
export type PetitionMasterNonVoidMethodSignatures = keyof PetitionMasterTypes['methods'] extends infer T ? T extends keyof PetitionMasterTypes['methods'] ? MethodReturn<T> extends void ? never : T  : never : never
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the PetitionMaster smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends PetitionMasterSignatures> = PetitionMasterTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the PetitionMaster smart contract to the method's return type
 */
export type MethodReturn<TSignature extends PetitionMasterSignatures> = PetitionMasterTypes['methods'][TSignature]['returns']

/**
 * Defines the shape of the keyed global state of the application.
 */
export type GlobalKeysState = PetitionMasterTypes['state']['global']['keys']

/**
 * Defines the shape of the keyed box state of the application.
 */
export type BoxKeysState = PetitionMasterTypes['state']['box']['keys']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the PetitionMaster smart contract
 */
export abstract class PetitionMasterParamsFactory {
  /**
   * Constructs a no op call for the assign_template_app(application)void ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static assignTemplateApp(params: CallParams<PetitionMasterArgs['obj']['assign_template_app(application)void'] | PetitionMasterArgs['tuple']['assign_template_app(application)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'assign_template_app(application)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.templateApp],
    }
  }
  /**
   * Constructs a no op call for the prime_initial_petition(pay)void ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static primeInitialPetition(params: CallParams<PetitionMasterArgs['obj']['prime_initial_petition(pay)void'] | PetitionMasterArgs['tuple']['prime_initial_petition(pay)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'prime_initial_petition(pay)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.mbrPayment],
    }
  }
  /**
   * Constructs a no op call for the create_petition(byte[],byte[],pay)uint64 ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static createPetition(params: CallParams<PetitionMasterArgs['obj']['create_petition(byte[],byte[],pay)uint64'] | PetitionMasterArgs['tuple']['create_petition(byte[],byte[],pay)uint64']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'create_petition(byte[],byte[],pay)uint64' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.petitionTitle, params.args.petitionText, params.args.mbrPayment],
    }
  }
  /**
   * Constructs a no op call for the additional_box_characters_option(byte[],pay)void ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static additionalBoxCharactersOption(params: CallParams<PetitionMasterArgs['obj']['additional_box_characters_option(byte[],pay)void'] | PetitionMasterArgs['tuple']['additional_box_characters_option(byte[],pay)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'additional_box_characters_option(byte[],pay)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.additionalText, params.args.mbrPayment],
    }
  }
  /**
   * Constructs a no op call for the prime_petition(application)void ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static primePetition(params: CallParams<PetitionMasterArgs['obj']['prime_petition(application)void'] | PetitionMasterArgs['tuple']['prime_petition(application)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'prime_petition(application)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.petitionAppId],
    }
  }
  /**
   * Constructs a no op call for the sign_petition(application,pay)void ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static signPetition(params: CallParams<PetitionMasterArgs['obj']['sign_petition(application,pay)void'] | PetitionMasterArgs['tuple']['sign_petition(application,pay)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'sign_petition(application,pay)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.petitionApp, params.args.mbrPayment],
    }
  }
  /**
   * Constructs a no op call for the add_comment(application,byte[],pay)void ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static addComment(params: CallParams<PetitionMasterArgs['obj']['add_comment(application,byte[],pay)void'] | PetitionMasterArgs['tuple']['add_comment(application,byte[],pay)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'add_comment(application,byte[],pay)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.petitionApp, params.args.text, params.args.mbrPayment],
    }
  }
}

/**
 * A client to make calls to the PetitionMaster smart contract
 */
export class PetitionMasterClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `PetitionMasterClient`
   *
   * @param appClient An `AppClient` instance which has been created with the PetitionMaster app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `PetitionMasterClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Checks for decode errors on the given return value and maps the return value to the return type for the given method
   * @returns The typed return value or undefined if there was no value
   */
  decodeReturnValue<TSignature extends PetitionMasterNonVoidMethodSignatures>(method: TSignature, returnValue: ABIReturn | undefined) {
    return returnValue !== undefined ? getArc56ReturnValue<MethodReturn<TSignature>>(returnValue, this.appClient.getABIMethod(method), APP_SPEC.structs) : undefined
  }

  /**
   * Returns a new `PetitionMasterClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<PetitionMasterClient> {
    return new PetitionMasterClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `PetitionMasterClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<PetitionMasterClient> {
    return new PetitionMasterClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Makes a clear_state call to an existing instance of the PetitionMaster smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the PetitionMaster smart contract using the `assign_template_app(application)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    assignTemplateApp: (params: CallParams<PetitionMasterArgs['obj']['assign_template_app(application)void'] | PetitionMasterArgs['tuple']['assign_template_app(application)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(PetitionMasterParamsFactory.assignTemplateApp(params))
    },

    /**
     * Makes a call to the PetitionMaster smart contract using the `prime_initial_petition(pay)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    primeInitialPetition: (params: CallParams<PetitionMasterArgs['obj']['prime_initial_petition(pay)void'] | PetitionMasterArgs['tuple']['prime_initial_petition(pay)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(PetitionMasterParamsFactory.primeInitialPetition(params))
    },

    /**
     * Makes a call to the PetitionMaster smart contract using the `create_petition(byte[],byte[],pay)uint64` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    createPetition: (params: CallParams<PetitionMasterArgs['obj']['create_petition(byte[],byte[],pay)uint64'] | PetitionMasterArgs['tuple']['create_petition(byte[],byte[],pay)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(PetitionMasterParamsFactory.createPetition(params))
    },

    /**
     * Makes a call to the PetitionMaster smart contract using the `additional_box_characters_option(byte[],pay)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    additionalBoxCharactersOption: (params: CallParams<PetitionMasterArgs['obj']['additional_box_characters_option(byte[],pay)void'] | PetitionMasterArgs['tuple']['additional_box_characters_option(byte[],pay)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(PetitionMasterParamsFactory.additionalBoxCharactersOption(params))
    },

    /**
     * Makes a call to the PetitionMaster smart contract using the `prime_petition(application)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    primePetition: (params: CallParams<PetitionMasterArgs['obj']['prime_petition(application)void'] | PetitionMasterArgs['tuple']['prime_petition(application)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(PetitionMasterParamsFactory.primePetition(params))
    },

    /**
     * Makes a call to the PetitionMaster smart contract using the `sign_petition(application,pay)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    signPetition: (params: CallParams<PetitionMasterArgs['obj']['sign_petition(application,pay)void'] | PetitionMasterArgs['tuple']['sign_petition(application,pay)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(PetitionMasterParamsFactory.signPetition(params))
    },

    /**
     * Makes a call to the PetitionMaster smart contract using the `add_comment(application,byte[],pay)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    addComment: (params: CallParams<PetitionMasterArgs['obj']['add_comment(application,byte[],pay)void'] | PetitionMasterArgs['tuple']['add_comment(application,byte[],pay)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(PetitionMasterParamsFactory.addComment(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Makes a clear_state call to an existing instance of the PetitionMaster smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the PetitionMaster smart contract using the `assign_template_app(application)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    assignTemplateApp: (params: CallParams<PetitionMasterArgs['obj']['assign_template_app(application)void'] | PetitionMasterArgs['tuple']['assign_template_app(application)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(PetitionMasterParamsFactory.assignTemplateApp(params))
    },

    /**
     * Makes a call to the PetitionMaster smart contract using the `prime_initial_petition(pay)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    primeInitialPetition: (params: CallParams<PetitionMasterArgs['obj']['prime_initial_petition(pay)void'] | PetitionMasterArgs['tuple']['prime_initial_petition(pay)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(PetitionMasterParamsFactory.primeInitialPetition(params))
    },

    /**
     * Makes a call to the PetitionMaster smart contract using the `create_petition(byte[],byte[],pay)uint64` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    createPetition: (params: CallParams<PetitionMasterArgs['obj']['create_petition(byte[],byte[],pay)uint64'] | PetitionMasterArgs['tuple']['create_petition(byte[],byte[],pay)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(PetitionMasterParamsFactory.createPetition(params))
    },

    /**
     * Makes a call to the PetitionMaster smart contract using the `additional_box_characters_option(byte[],pay)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    additionalBoxCharactersOption: (params: CallParams<PetitionMasterArgs['obj']['additional_box_characters_option(byte[],pay)void'] | PetitionMasterArgs['tuple']['additional_box_characters_option(byte[],pay)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(PetitionMasterParamsFactory.additionalBoxCharactersOption(params))
    },

    /**
     * Makes a call to the PetitionMaster smart contract using the `prime_petition(application)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    primePetition: (params: CallParams<PetitionMasterArgs['obj']['prime_petition(application)void'] | PetitionMasterArgs['tuple']['prime_petition(application)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(PetitionMasterParamsFactory.primePetition(params))
    },

    /**
     * Makes a call to the PetitionMaster smart contract using the `sign_petition(application,pay)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    signPetition: (params: CallParams<PetitionMasterArgs['obj']['sign_petition(application,pay)void'] | PetitionMasterArgs['tuple']['sign_petition(application,pay)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(PetitionMasterParamsFactory.signPetition(params))
    },

    /**
     * Makes a call to the PetitionMaster smart contract using the `add_comment(application,byte[],pay)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    addComment: (params: CallParams<PetitionMasterArgs['obj']['add_comment(application,byte[],pay)void'] | PetitionMasterArgs['tuple']['add_comment(application,byte[],pay)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(PetitionMasterParamsFactory.addComment(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Makes a clear_state call to an existing instance of the PetitionMaster smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the PetitionMaster smart contract using the `assign_template_app(application)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    assignTemplateApp: async (params: CallParams<PetitionMasterArgs['obj']['assign_template_app(application)void'] | PetitionMasterArgs['tuple']['assign_template_app(application)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(PetitionMasterParamsFactory.assignTemplateApp(params))
      return {...result, return: result.return as unknown as (undefined | PetitionMasterReturns['assign_template_app(application)void'])}
    },

    /**
     * Makes a call to the PetitionMaster smart contract using the `prime_initial_petition(pay)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    primeInitialPetition: async (params: CallParams<PetitionMasterArgs['obj']['prime_initial_petition(pay)void'] | PetitionMasterArgs['tuple']['prime_initial_petition(pay)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(PetitionMasterParamsFactory.primeInitialPetition(params))
      return {...result, return: result.return as unknown as (undefined | PetitionMasterReturns['prime_initial_petition(pay)void'])}
    },

    /**
     * Makes a call to the PetitionMaster smart contract using the `create_petition(byte[],byte[],pay)uint64` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    createPetition: async (params: CallParams<PetitionMasterArgs['obj']['create_petition(byte[],byte[],pay)uint64'] | PetitionMasterArgs['tuple']['create_petition(byte[],byte[],pay)uint64']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(PetitionMasterParamsFactory.createPetition(params))
      return {...result, return: result.return as unknown as (undefined | PetitionMasterReturns['create_petition(byte[],byte[],pay)uint64'])}
    },

    /**
     * Makes a call to the PetitionMaster smart contract using the `additional_box_characters_option(byte[],pay)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    additionalBoxCharactersOption: async (params: CallParams<PetitionMasterArgs['obj']['additional_box_characters_option(byte[],pay)void'] | PetitionMasterArgs['tuple']['additional_box_characters_option(byte[],pay)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(PetitionMasterParamsFactory.additionalBoxCharactersOption(params))
      return {...result, return: result.return as unknown as (undefined | PetitionMasterReturns['additional_box_characters_option(byte[],pay)void'])}
    },

    /**
     * Makes a call to the PetitionMaster smart contract using the `prime_petition(application)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    primePetition: async (params: CallParams<PetitionMasterArgs['obj']['prime_petition(application)void'] | PetitionMasterArgs['tuple']['prime_petition(application)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(PetitionMasterParamsFactory.primePetition(params))
      return {...result, return: result.return as unknown as (undefined | PetitionMasterReturns['prime_petition(application)void'])}
    },

    /**
     * Makes a call to the PetitionMaster smart contract using the `sign_petition(application,pay)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    signPetition: async (params: CallParams<PetitionMasterArgs['obj']['sign_petition(application,pay)void'] | PetitionMasterArgs['tuple']['sign_petition(application,pay)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(PetitionMasterParamsFactory.signPetition(params))
      return {...result, return: result.return as unknown as (undefined | PetitionMasterReturns['sign_petition(application,pay)void'])}
    },

    /**
     * Makes a call to the PetitionMaster smart contract using the `add_comment(application,byte[],pay)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    addComment: async (params: CallParams<PetitionMasterArgs['obj']['add_comment(application,byte[],pay)void'] | PetitionMasterArgs['tuple']['add_comment(application,byte[],pay)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(PetitionMasterParamsFactory.addComment(params))
      return {...result, return: result.return as unknown as (undefined | PetitionMasterReturns['add_comment(application,byte[],pay)void'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new PetitionMasterClient(this.appClient.clone(params))
  }

  /**
   * Methods to access state for the current PetitionMaster app
   */
  state = {
    /**
     * Methods to access global state for the current PetitionMaster app
     */
    global: {
      /**
       * Get all current keyed values from global state
       */
      getAll: async (): Promise<Partial<Expand<GlobalKeysState>>> => {
        const result = await this.appClient.state.global.getAll()
        return {
          petitionTemplateApp: result.petition_template_app,
          petitionUid: result.petition_uid,
          templateAppAssigned: result.template_app_assigned,
          previousChildPetitionApp: result.previous_child_petition_app,
          nextChildPetitionApp: result.next_child_petition_app,
        }
      },
      /**
       * Get the current value of the petition_template_app key in global state
       */
      petitionTemplateApp: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("petition_template_app")) as bigint | undefined },
      /**
       * Get the current value of the petition_uid key in global state
       */
      petitionUid: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("petition_uid")) as bigint | undefined },
      /**
       * Get the current value of the template_app_assigned key in global state
       */
      templateAppAssigned: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("template_app_assigned")) as bigint | undefined },
      /**
       * Get the current value of the previous_child_petition_app key in global state
       */
      previousChildPetitionApp: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("previous_child_petition_app")) as bigint | undefined },
      /**
       * Get the current value of the next_child_petition_app key in global state
       */
      nextChildPetitionApp: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("next_child_petition_app")) as bigint | undefined },
    },
    /**
     * Methods to access box state for the current PetitionMaster app
     */
    box: {
      /**
       * Get all current keyed values from box state
       */
      getAll: async (): Promise<Partial<Expand<BoxKeysState>>> => {
        const result = await this.appClient.state.box.getAll()
        return {
        }
      },
      /**
       * Get values from the petition_details map in box state
       */
      petitionDetails: {
        /**
         * Get all current values of the petition_details map in box state
         */
        getMap: async (): Promise<Map<bigint, MasterPetitionDetails>> => { return (await this.appClient.state.box.getMap("petition_details")) as Map<bigint, MasterPetitionDetails> },
        /**
         * Get a current value of the petition_details map by key from box state
         */
        value: async (key: bigint | number): Promise<MasterPetitionDetails | undefined> => { return await this.appClient.state.box.getMapValue("petition_details", key) as MasterPetitionDetails | undefined },
      },
    },
  }

  public newGroup(): PetitionMasterComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a assign_template_app(application)void method call against the PetitionMaster contract
       */
      assignTemplateApp(params: CallParams<PetitionMasterArgs['obj']['assign_template_app(application)void'] | PetitionMasterArgs['tuple']['assign_template_app(application)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.assignTemplateApp(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a prime_initial_petition(pay)void method call against the PetitionMaster contract
       */
      primeInitialPetition(params: CallParams<PetitionMasterArgs['obj']['prime_initial_petition(pay)void'] | PetitionMasterArgs['tuple']['prime_initial_petition(pay)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.primeInitialPetition(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a create_petition(byte[],byte[],pay)uint64 method call against the PetitionMaster contract
       */
      createPetition(params: CallParams<PetitionMasterArgs['obj']['create_petition(byte[],byte[],pay)uint64'] | PetitionMasterArgs['tuple']['create_petition(byte[],byte[],pay)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.createPetition(params)))
        resultMappers.push((v) => client.decodeReturnValue('create_petition(byte[],byte[],pay)uint64', v))
        return this
      },
      /**
       * Add a additional_box_characters_option(byte[],pay)void method call against the PetitionMaster contract
       */
      additionalBoxCharactersOption(params: CallParams<PetitionMasterArgs['obj']['additional_box_characters_option(byte[],pay)void'] | PetitionMasterArgs['tuple']['additional_box_characters_option(byte[],pay)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.additionalBoxCharactersOption(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a prime_petition(application)void method call against the PetitionMaster contract
       */
      primePetition(params: CallParams<PetitionMasterArgs['obj']['prime_petition(application)void'] | PetitionMasterArgs['tuple']['prime_petition(application)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.primePetition(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a sign_petition(application,pay)void method call against the PetitionMaster contract
       */
      signPetition(params: CallParams<PetitionMasterArgs['obj']['sign_petition(application,pay)void'] | PetitionMasterArgs['tuple']['sign_petition(application,pay)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.signPetition(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a add_comment(application,byte[],pay)void method call against the PetitionMaster contract
       */
      addComment(params: CallParams<PetitionMasterArgs['obj']['add_comment(application,byte[],pay)void'] | PetitionMasterArgs['tuple']['add_comment(application,byte[],pay)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.addComment(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a clear state call to the PetitionMaster contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as PetitionMasterComposer
  }
}
export type PetitionMasterComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the assign_template_app(application)void ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  assignTemplateApp(params?: CallParams<PetitionMasterArgs['obj']['assign_template_app(application)void'] | PetitionMasterArgs['tuple']['assign_template_app(application)void']>): PetitionMasterComposer<[...TReturns, PetitionMasterReturns['assign_template_app(application)void'] | undefined]>

  /**
   * Calls the prime_initial_petition(pay)void ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  primeInitialPetition(params?: CallParams<PetitionMasterArgs['obj']['prime_initial_petition(pay)void'] | PetitionMasterArgs['tuple']['prime_initial_petition(pay)void']>): PetitionMasterComposer<[...TReturns, PetitionMasterReturns['prime_initial_petition(pay)void'] | undefined]>

  /**
   * Calls the create_petition(byte[],byte[],pay)uint64 ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  createPetition(params?: CallParams<PetitionMasterArgs['obj']['create_petition(byte[],byte[],pay)uint64'] | PetitionMasterArgs['tuple']['create_petition(byte[],byte[],pay)uint64']>): PetitionMasterComposer<[...TReturns, PetitionMasterReturns['create_petition(byte[],byte[],pay)uint64'] | undefined]>

  /**
   * Calls the additional_box_characters_option(byte[],pay)void ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  additionalBoxCharactersOption(params?: CallParams<PetitionMasterArgs['obj']['additional_box_characters_option(byte[],pay)void'] | PetitionMasterArgs['tuple']['additional_box_characters_option(byte[],pay)void']>): PetitionMasterComposer<[...TReturns, PetitionMasterReturns['additional_box_characters_option(byte[],pay)void'] | undefined]>

  /**
   * Calls the prime_petition(application)void ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  primePetition(params?: CallParams<PetitionMasterArgs['obj']['prime_petition(application)void'] | PetitionMasterArgs['tuple']['prime_petition(application)void']>): PetitionMasterComposer<[...TReturns, PetitionMasterReturns['prime_petition(application)void'] | undefined]>

  /**
   * Calls the sign_petition(application,pay)void ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  signPetition(params?: CallParams<PetitionMasterArgs['obj']['sign_petition(application,pay)void'] | PetitionMasterArgs['tuple']['sign_petition(application,pay)void']>): PetitionMasterComposer<[...TReturns, PetitionMasterReturns['sign_petition(application,pay)void'] | undefined]>

  /**
   * Calls the add_comment(application,byte[],pay)void ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  addComment(params?: CallParams<PetitionMasterArgs['obj']['add_comment(application,byte[],pay)void'] | PetitionMasterArgs['tuple']['add_comment(application,byte[],pay)void']>): PetitionMasterComposer<[...TReturns, PetitionMasterReturns['add_comment(application,byte[],pay)void'] | undefined]>

  /**
   * Makes a clear_state call to an existing instance of the PetitionMaster smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): PetitionMasterComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): PetitionMasterComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<PetitionMasterComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<PetitionMasterComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<PetitionMasterComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<PetitionMasterComposerResults<TReturns>>
}
export type PetitionMasterComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

