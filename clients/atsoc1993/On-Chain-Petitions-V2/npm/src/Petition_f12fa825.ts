/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"Petition","structs":{"ChildPetitionDetails":[{"name":"petitionTitle","type":"byte[]"},{"name":"petitionText","type":"byte[]"}],"Comment":[{"name":"address","type":"address"},{"name":"time","type":"uint64"},{"name":"comment","type":"byte[]"}],"Signee":[{"name":"address","type":"address"}],"TimeSigned":[{"name":"timeSigned","type":"uint64"}]},"methods":[{"name":"update","args":[],"returns":{"type":"void"},"actions":{"create":[],"call":["UpdateApplication"]},"readonly":false,"events":[],"recommendations":{}},{"name":"assign_master_and_petition_creator","args":[{"type":"application","name":"petition_master"},{"type":"account","name":"petition_creator"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}},{"name":"create_petition","args":[{"type":"byte[]","name":"petition_title"},{"type":"byte[]","name":"petition_text"},{"type":"pay","name":"mbr_payment"}],"returns":{"type":"uint64"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}},{"name":"additional_box_characters_option","args":[{"type":"byte[]","name":"additional_text"},{"type":"pay","name":"mbr_payment"}],"returns":{"type":"uint64"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}},{"name":"prime_petition","args":[],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}},{"name":"sign_petition","args":[{"type":"address","name":"signer"},{"type":"pay","name":"mbr_payment"}],"returns":{"type":"uint64"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}},{"name":"add_comment","args":[{"type":"byte[]","name":"text"},{"type":"pay","name":"mbr_payment"}],"returns":{"type":"uint64"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}}],"arcs":[22,28],"networks":{},"state":{"schema":{"global":{"ints":4,"bytes":2},"local":{"ints":0,"bytes":0}},"keys":{"global":{"signees":{"keyType":"AVMString","valueType":"AVMUint64","key":"c2lnbmVlcw=="},"creation_time":{"keyType":"AVMString","valueType":"AVMUint64","key":"Y3JlYXRpb25fdGltZQ=="},"petition_master":{"keyType":"AVMString","valueType":"AVMUint64","key":"cGV0aXRpb25fbWFzdGVy"},"petition_creator":{"keyType":"AVMString","valueType":"address","key":"cGV0aXRpb25fY3JlYXRvcg=="},"comment_counter":{"keyType":"AVMString","valueType":"uint64","key":"Y29tbWVudF9jb3VudGVy"},"petition_primed":{"keyType":"AVMString","valueType":"AVMUint64","key":"cGV0aXRpb25fcHJpbWVk"}},"local":{},"box":{}},"maps":{"global":{},"local":{},"box":{"petition_details":{"keyType":"AVMString","valueType":"ChildPetitionDetails","prefix":""},"signatures":{"keyType":"Signee","valueType":"TimeSigned","prefix":""},"comments":{"keyType":"uint64","valueType":"Comment","prefix":""}}}},"bareActions":{"create":["NoOp"],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[205,235,265,277,310,343],"errorMessage":"OnCompletion is not NoOp"},{"pc":[368],"errorMessage":"OnCompletion is not UpdateApplication"},{"pc":[442,483,515,527,635,645,685,713],"errorMessage":"account funded"},{"pc":[591],"errorMessage":"application exists"},{"pc":[385],"errorMessage":"can only call when creating"},{"pc":[208,238,268,280,313,346,371],"errorMessage":"can only call when not creating"},{"pc":[703,729],"errorMessage":"check self.comment_counter exists"},{"pc":[588],"errorMessage":"check self.petition_master exists"},{"pc":[606,616,679],"errorMessage":"check self.petition_primed exists"},{"pc":[650],"errorMessage":"check self.signees exists"},{"pc":[221,251,296,329],"errorMessage":"transaction type is pay"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"events":[]} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


// Type definitions for ARC-56 structs

export type ChildPetitionDetails = {
  petitionTitle: Uint8Array,
  petitionText: Uint8Array
}


/**
 * Converts the ABI tuple representation of a ChildPetitionDetails to the struct representation
 */
export function ChildPetitionDetailsFromTuple(abiTuple: [Uint8Array, Uint8Array]) {
  return getABIStructFromABITuple(abiTuple, APP_SPEC.structs.ChildPetitionDetails, APP_SPEC.structs) as ChildPetitionDetails
}

export type Comment = {
  address: string,
  time: bigint,
  comment: Uint8Array
}


/**
 * Converts the ABI tuple representation of a Comment to the struct representation
 */
export function CommentFromTuple(abiTuple: [string, bigint, Uint8Array]) {
  return getABIStructFromABITuple(abiTuple, APP_SPEC.structs.Comment, APP_SPEC.structs) as Comment
}

export type Signee = {
  address: string
}


/**
 * Converts the ABI tuple representation of a Signee to the struct representation
 */
export function SigneeFromTuple(abiTuple: [string]) {
  return getABIStructFromABITuple(abiTuple, APP_SPEC.structs.Signee, APP_SPEC.structs) as Signee
}

export type TimeSigned = {
  timeSigned: bigint
}


/**
 * Converts the ABI tuple representation of a TimeSigned to the struct representation
 */
export function TimeSignedFromTuple(abiTuple: [bigint]) {
  return getABIStructFromABITuple(abiTuple, APP_SPEC.structs.TimeSigned, APP_SPEC.structs) as TimeSigned
}

/**
 * The argument types for the Petition contract
 */
export type PetitionArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'update()void': Record<string, never>
    'assign_master_and_petition_creator(application,account)void': {
      petitionMaster: bigint
      petitionCreator: Uint8Array | string
    }
    'create_petition(byte[],byte[],pay)uint64': {
      petitionTitle: Uint8Array
      petitionText: Uint8Array
      mbrPayment: AppMethodCallTransactionArgument
    }
    'additional_box_characters_option(byte[],pay)uint64': {
      additionalText: Uint8Array
      mbrPayment: AppMethodCallTransactionArgument
    }
    'prime_petition()void': Record<string, never>
    'sign_petition(address,pay)uint64': {
      signer: string
      mbrPayment: AppMethodCallTransactionArgument
    }
    'add_comment(byte[],pay)uint64': {
      text: Uint8Array
      mbrPayment: AppMethodCallTransactionArgument
    }
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'update()void': []
    'assign_master_and_petition_creator(application,account)void': [petitionMaster: bigint, petitionCreator: Uint8Array | string]
    'create_petition(byte[],byte[],pay)uint64': [petitionTitle: Uint8Array, petitionText: Uint8Array, mbrPayment: AppMethodCallTransactionArgument]
    'additional_box_characters_option(byte[],pay)uint64': [additionalText: Uint8Array, mbrPayment: AppMethodCallTransactionArgument]
    'prime_petition()void': []
    'sign_petition(address,pay)uint64': [signer: string, mbrPayment: AppMethodCallTransactionArgument]
    'add_comment(byte[],pay)uint64': [text: Uint8Array, mbrPayment: AppMethodCallTransactionArgument]
  }
}

/**
 * The return type for each method
 */
export type PetitionReturns = {
  'update()void': void
  'assign_master_and_petition_creator(application,account)void': void
  'create_petition(byte[],byte[],pay)uint64': bigint
  'additional_box_characters_option(byte[],pay)uint64': bigint
  'prime_petition()void': void
  'sign_petition(address,pay)uint64': bigint
  'add_comment(byte[],pay)uint64': bigint
}

/**
 * Defines the types of available calls and state of the Petition smart contract.
 */
export type PetitionTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'update()void' | 'update', {
      argsObj: PetitionArgs['obj']['update()void']
      argsTuple: PetitionArgs['tuple']['update()void']
      returns: PetitionReturns['update()void']
    }>
    & Record<'assign_master_and_petition_creator(application,account)void' | 'assign_master_and_petition_creator', {
      argsObj: PetitionArgs['obj']['assign_master_and_petition_creator(application,account)void']
      argsTuple: PetitionArgs['tuple']['assign_master_and_petition_creator(application,account)void']
      returns: PetitionReturns['assign_master_and_petition_creator(application,account)void']
    }>
    & Record<'create_petition(byte[],byte[],pay)uint64' | 'create_petition', {
      argsObj: PetitionArgs['obj']['create_petition(byte[],byte[],pay)uint64']
      argsTuple: PetitionArgs['tuple']['create_petition(byte[],byte[],pay)uint64']
      returns: PetitionReturns['create_petition(byte[],byte[],pay)uint64']
    }>
    & Record<'additional_box_characters_option(byte[],pay)uint64' | 'additional_box_characters_option', {
      argsObj: PetitionArgs['obj']['additional_box_characters_option(byte[],pay)uint64']
      argsTuple: PetitionArgs['tuple']['additional_box_characters_option(byte[],pay)uint64']
      returns: PetitionReturns['additional_box_characters_option(byte[],pay)uint64']
    }>
    & Record<'prime_petition()void' | 'prime_petition', {
      argsObj: PetitionArgs['obj']['prime_petition()void']
      argsTuple: PetitionArgs['tuple']['prime_petition()void']
      returns: PetitionReturns['prime_petition()void']
    }>
    & Record<'sign_petition(address,pay)uint64' | 'sign_petition', {
      argsObj: PetitionArgs['obj']['sign_petition(address,pay)uint64']
      argsTuple: PetitionArgs['tuple']['sign_petition(address,pay)uint64']
      returns: PetitionReturns['sign_petition(address,pay)uint64']
    }>
    & Record<'add_comment(byte[],pay)uint64' | 'add_comment', {
      argsObj: PetitionArgs['obj']['add_comment(byte[],pay)uint64']
      argsTuple: PetitionArgs['tuple']['add_comment(byte[],pay)uint64']
      returns: PetitionReturns['add_comment(byte[],pay)uint64']
    }>
  /**
   * Defines the shape of the state of the application.
   */
  state: {
    global: {
      keys: {
        signees: bigint
        creationTime: bigint
        petitionMaster: bigint
        petitionCreator: string
        commentCounter: bigint
        petitionPrimed: bigint
      }
      maps: {}
    }
    box: {
      keys: {}
      maps: {
        petitionDetails: Map<string, ChildPetitionDetails>
        signatures: Map<Signee, TimeSigned>
        comments: Map<bigint | number, Comment>
      }
    }
  }
}

/**
 * Defines the possible abi call signatures.
 */
export type PetitionSignatures = keyof PetitionTypes['methods']
/**
 * Defines the possible abi call signatures for methods that return a non-void value.
 */
export type PetitionNonVoidMethodSignatures = keyof PetitionTypes['methods'] extends infer T ? T extends keyof PetitionTypes['methods'] ? MethodReturn<T> extends void ? never : T  : never : never
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the Petition smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends PetitionSignatures> = PetitionTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the Petition smart contract to the method's return type
 */
export type MethodReturn<TSignature extends PetitionSignatures> = PetitionTypes['methods'][TSignature]['returns']

/**
 * Defines the shape of the keyed global state of the application.
 */
export type GlobalKeysState = PetitionTypes['state']['global']['keys']

/**
 * Defines the shape of the keyed box state of the application.
 */
export type BoxKeysState = PetitionTypes['state']['box']['keys']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the Petition smart contract
 */
export abstract class PetitionParamsFactory {
  /**
   * Constructs a no op call for the assign_master_and_petition_creator(application,account)void ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static assignMasterAndPetitionCreator(params: CallParams<PetitionArgs['obj']['assign_master_and_petition_creator(application,account)void'] | PetitionArgs['tuple']['assign_master_and_petition_creator(application,account)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'assign_master_and_petition_creator(application,account)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.petitionMaster, params.args.petitionCreator],
    }
  }
  /**
   * Constructs a no op call for the create_petition(byte[],byte[],pay)uint64 ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static createPetition(params: CallParams<PetitionArgs['obj']['create_petition(byte[],byte[],pay)uint64'] | PetitionArgs['tuple']['create_petition(byte[],byte[],pay)uint64']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'create_petition(byte[],byte[],pay)uint64' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.petitionTitle, params.args.petitionText, params.args.mbrPayment],
    }
  }
  /**
   * Constructs a no op call for the additional_box_characters_option(byte[],pay)uint64 ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static additionalBoxCharactersOption(params: CallParams<PetitionArgs['obj']['additional_box_characters_option(byte[],pay)uint64'] | PetitionArgs['tuple']['additional_box_characters_option(byte[],pay)uint64']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'additional_box_characters_option(byte[],pay)uint64' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.additionalText, params.args.mbrPayment],
    }
  }
  /**
   * Constructs a no op call for the prime_petition()void ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static primePetition(params: CallParams<PetitionArgs['obj']['prime_petition()void'] | PetitionArgs['tuple']['prime_petition()void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'prime_petition()void' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the sign_petition(address,pay)uint64 ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static signPetition(params: CallParams<PetitionArgs['obj']['sign_petition(address,pay)uint64'] | PetitionArgs['tuple']['sign_petition(address,pay)uint64']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'sign_petition(address,pay)uint64' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.signer, params.args.mbrPayment],
    }
  }
  /**
   * Constructs a no op call for the add_comment(byte[],pay)uint64 ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static addComment(params: CallParams<PetitionArgs['obj']['add_comment(byte[],pay)uint64'] | PetitionArgs['tuple']['add_comment(byte[],pay)uint64']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'add_comment(byte[],pay)uint64' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.text, params.args.mbrPayment],
    }
  }
}

/**
 * A client to make calls to the Petition smart contract
 */
export class PetitionClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `PetitionClient`
   *
   * @param appClient An `AppClient` instance which has been created with the Petition app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `PetitionClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Checks for decode errors on the given return value and maps the return value to the return type for the given method
   * @returns The typed return value or undefined if there was no value
   */
  decodeReturnValue<TSignature extends PetitionNonVoidMethodSignatures>(method: TSignature, returnValue: ABIReturn | undefined) {
    return returnValue !== undefined ? getArc56ReturnValue<MethodReturn<TSignature>>(returnValue, this.appClient.getABIMethod(method), APP_SPEC.structs) : undefined
  }

  /**
   * Returns a new `PetitionClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<PetitionClient> {
    return new PetitionClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `PetitionClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<PetitionClient> {
    return new PetitionClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Makes a clear_state call to an existing instance of the Petition smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the Petition smart contract using the `assign_master_and_petition_creator(application,account)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    assignMasterAndPetitionCreator: (params: CallParams<PetitionArgs['obj']['assign_master_and_petition_creator(application,account)void'] | PetitionArgs['tuple']['assign_master_and_petition_creator(application,account)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(PetitionParamsFactory.assignMasterAndPetitionCreator(params))
    },

    /**
     * Makes a call to the Petition smart contract using the `create_petition(byte[],byte[],pay)uint64` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    createPetition: (params: CallParams<PetitionArgs['obj']['create_petition(byte[],byte[],pay)uint64'] | PetitionArgs['tuple']['create_petition(byte[],byte[],pay)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(PetitionParamsFactory.createPetition(params))
    },

    /**
     * Makes a call to the Petition smart contract using the `additional_box_characters_option(byte[],pay)uint64` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    additionalBoxCharactersOption: (params: CallParams<PetitionArgs['obj']['additional_box_characters_option(byte[],pay)uint64'] | PetitionArgs['tuple']['additional_box_characters_option(byte[],pay)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(PetitionParamsFactory.additionalBoxCharactersOption(params))
    },

    /**
     * Makes a call to the Petition smart contract using the `prime_petition()void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    primePetition: (params: CallParams<PetitionArgs['obj']['prime_petition()void'] | PetitionArgs['tuple']['prime_petition()void']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(PetitionParamsFactory.primePetition(params))
    },

    /**
     * Makes a call to the Petition smart contract using the `sign_petition(address,pay)uint64` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    signPetition: (params: CallParams<PetitionArgs['obj']['sign_petition(address,pay)uint64'] | PetitionArgs['tuple']['sign_petition(address,pay)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(PetitionParamsFactory.signPetition(params))
    },

    /**
     * Makes a call to the Petition smart contract using the `add_comment(byte[],pay)uint64` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    addComment: (params: CallParams<PetitionArgs['obj']['add_comment(byte[],pay)uint64'] | PetitionArgs['tuple']['add_comment(byte[],pay)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(PetitionParamsFactory.addComment(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Makes a clear_state call to an existing instance of the Petition smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the Petition smart contract using the `assign_master_and_petition_creator(application,account)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    assignMasterAndPetitionCreator: (params: CallParams<PetitionArgs['obj']['assign_master_and_petition_creator(application,account)void'] | PetitionArgs['tuple']['assign_master_and_petition_creator(application,account)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(PetitionParamsFactory.assignMasterAndPetitionCreator(params))
    },

    /**
     * Makes a call to the Petition smart contract using the `create_petition(byte[],byte[],pay)uint64` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    createPetition: (params: CallParams<PetitionArgs['obj']['create_petition(byte[],byte[],pay)uint64'] | PetitionArgs['tuple']['create_petition(byte[],byte[],pay)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(PetitionParamsFactory.createPetition(params))
    },

    /**
     * Makes a call to the Petition smart contract using the `additional_box_characters_option(byte[],pay)uint64` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    additionalBoxCharactersOption: (params: CallParams<PetitionArgs['obj']['additional_box_characters_option(byte[],pay)uint64'] | PetitionArgs['tuple']['additional_box_characters_option(byte[],pay)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(PetitionParamsFactory.additionalBoxCharactersOption(params))
    },

    /**
     * Makes a call to the Petition smart contract using the `prime_petition()void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    primePetition: (params: CallParams<PetitionArgs['obj']['prime_petition()void'] | PetitionArgs['tuple']['prime_petition()void']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(PetitionParamsFactory.primePetition(params))
    },

    /**
     * Makes a call to the Petition smart contract using the `sign_petition(address,pay)uint64` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    signPetition: (params: CallParams<PetitionArgs['obj']['sign_petition(address,pay)uint64'] | PetitionArgs['tuple']['sign_petition(address,pay)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(PetitionParamsFactory.signPetition(params))
    },

    /**
     * Makes a call to the Petition smart contract using the `add_comment(byte[],pay)uint64` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    addComment: (params: CallParams<PetitionArgs['obj']['add_comment(byte[],pay)uint64'] | PetitionArgs['tuple']['add_comment(byte[],pay)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(PetitionParamsFactory.addComment(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Makes a clear_state call to an existing instance of the Petition smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the Petition smart contract using the `assign_master_and_petition_creator(application,account)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    assignMasterAndPetitionCreator: async (params: CallParams<PetitionArgs['obj']['assign_master_and_petition_creator(application,account)void'] | PetitionArgs['tuple']['assign_master_and_petition_creator(application,account)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(PetitionParamsFactory.assignMasterAndPetitionCreator(params))
      return {...result, return: result.return as unknown as (undefined | PetitionReturns['assign_master_and_petition_creator(application,account)void'])}
    },

    /**
     * Makes a call to the Petition smart contract using the `create_petition(byte[],byte[],pay)uint64` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    createPetition: async (params: CallParams<PetitionArgs['obj']['create_petition(byte[],byte[],pay)uint64'] | PetitionArgs['tuple']['create_petition(byte[],byte[],pay)uint64']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(PetitionParamsFactory.createPetition(params))
      return {...result, return: result.return as unknown as (undefined | PetitionReturns['create_petition(byte[],byte[],pay)uint64'])}
    },

    /**
     * Makes a call to the Petition smart contract using the `additional_box_characters_option(byte[],pay)uint64` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    additionalBoxCharactersOption: async (params: CallParams<PetitionArgs['obj']['additional_box_characters_option(byte[],pay)uint64'] | PetitionArgs['tuple']['additional_box_characters_option(byte[],pay)uint64']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(PetitionParamsFactory.additionalBoxCharactersOption(params))
      return {...result, return: result.return as unknown as (undefined | PetitionReturns['additional_box_characters_option(byte[],pay)uint64'])}
    },

    /**
     * Makes a call to the Petition smart contract using the `prime_petition()void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    primePetition: async (params: CallParams<PetitionArgs['obj']['prime_petition()void'] | PetitionArgs['tuple']['prime_petition()void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(PetitionParamsFactory.primePetition(params))
      return {...result, return: result.return as unknown as (undefined | PetitionReturns['prime_petition()void'])}
    },

    /**
     * Makes a call to the Petition smart contract using the `sign_petition(address,pay)uint64` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    signPetition: async (params: CallParams<PetitionArgs['obj']['sign_petition(address,pay)uint64'] | PetitionArgs['tuple']['sign_petition(address,pay)uint64']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(PetitionParamsFactory.signPetition(params))
      return {...result, return: result.return as unknown as (undefined | PetitionReturns['sign_petition(address,pay)uint64'])}
    },

    /**
     * Makes a call to the Petition smart contract using the `add_comment(byte[],pay)uint64` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    addComment: async (params: CallParams<PetitionArgs['obj']['add_comment(byte[],pay)uint64'] | PetitionArgs['tuple']['add_comment(byte[],pay)uint64']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(PetitionParamsFactory.addComment(params))
      return {...result, return: result.return as unknown as (undefined | PetitionReturns['add_comment(byte[],pay)uint64'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new PetitionClient(this.appClient.clone(params))
  }

  /**
   * Methods to access state for the current Petition app
   */
  state = {
    /**
     * Methods to access global state for the current Petition app
     */
    global: {
      /**
       * Get all current keyed values from global state
       */
      getAll: async (): Promise<Partial<Expand<GlobalKeysState>>> => {
        const result = await this.appClient.state.global.getAll()
        return {
          signees: result.signees,
          creationTime: result.creation_time,
          petitionMaster: result.petition_master,
          petitionCreator: result.petition_creator,
          commentCounter: result.comment_counter,
          petitionPrimed: result.petition_primed,
        }
      },
      /**
       * Get the current value of the signees key in global state
       */
      signees: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("signees")) as bigint | undefined },
      /**
       * Get the current value of the creation_time key in global state
       */
      creationTime: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("creation_time")) as bigint | undefined },
      /**
       * Get the current value of the petition_master key in global state
       */
      petitionMaster: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("petition_master")) as bigint | undefined },
      /**
       * Get the current value of the petition_creator key in global state
       */
      petitionCreator: async (): Promise<string | undefined> => { return (await this.appClient.state.global.getValue("petition_creator")) as string | undefined },
      /**
       * Get the current value of the comment_counter key in global state
       */
      commentCounter: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("comment_counter")) as bigint | undefined },
      /**
       * Get the current value of the petition_primed key in global state
       */
      petitionPrimed: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("petition_primed")) as bigint | undefined },
    },
    /**
     * Methods to access box state for the current Petition app
     */
    box: {
      /**
       * Get all current keyed values from box state
       */
      getAll: async (): Promise<Partial<Expand<BoxKeysState>>> => {
        const result = await this.appClient.state.box.getAll()
        return {
        }
      },
      /**
       * Get values from the petition_details map in box state
       */
      petitionDetails: {
        /**
         * Get all current values of the petition_details map in box state
         */
        getMap: async (): Promise<Map<string, ChildPetitionDetails>> => { return (await this.appClient.state.box.getMap("petition_details")) as Map<string, ChildPetitionDetails> },
        /**
         * Get a current value of the petition_details map by key from box state
         */
        value: async (key: string): Promise<ChildPetitionDetails | undefined> => { return await this.appClient.state.box.getMapValue("petition_details", key) as ChildPetitionDetails | undefined },
      },
      /**
       * Get values from the signatures map in box state
       */
      signatures: {
        /**
         * Get all current values of the signatures map in box state
         */
        getMap: async (): Promise<Map<Signee, TimeSigned>> => { return (await this.appClient.state.box.getMap("signatures")) as Map<Signee, TimeSigned> },
        /**
         * Get a current value of the signatures map by key from box state
         */
        value: async (key: Signee): Promise<TimeSigned | undefined> => { return await this.appClient.state.box.getMapValue("signatures", key) as TimeSigned | undefined },
      },
      /**
       * Get values from the comments map in box state
       */
      comments: {
        /**
         * Get all current values of the comments map in box state
         */
        getMap: async (): Promise<Map<bigint, Comment>> => { return (await this.appClient.state.box.getMap("comments")) as Map<bigint, Comment> },
        /**
         * Get a current value of the comments map by key from box state
         */
        value: async (key: bigint | number): Promise<Comment | undefined> => { return await this.appClient.state.box.getMapValue("comments", key) as Comment | undefined },
      },
    },
  }

  public newGroup(): PetitionComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a assign_master_and_petition_creator(application,account)void method call against the Petition contract
       */
      assignMasterAndPetitionCreator(params: CallParams<PetitionArgs['obj']['assign_master_and_petition_creator(application,account)void'] | PetitionArgs['tuple']['assign_master_and_petition_creator(application,account)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.assignMasterAndPetitionCreator(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a create_petition(byte[],byte[],pay)uint64 method call against the Petition contract
       */
      createPetition(params: CallParams<PetitionArgs['obj']['create_petition(byte[],byte[],pay)uint64'] | PetitionArgs['tuple']['create_petition(byte[],byte[],pay)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.createPetition(params)))
        resultMappers.push((v) => client.decodeReturnValue('create_petition(byte[],byte[],pay)uint64', v))
        return this
      },
      /**
       * Add a additional_box_characters_option(byte[],pay)uint64 method call against the Petition contract
       */
      additionalBoxCharactersOption(params: CallParams<PetitionArgs['obj']['additional_box_characters_option(byte[],pay)uint64'] | PetitionArgs['tuple']['additional_box_characters_option(byte[],pay)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.additionalBoxCharactersOption(params)))
        resultMappers.push((v) => client.decodeReturnValue('additional_box_characters_option(byte[],pay)uint64', v))
        return this
      },
      /**
       * Add a prime_petition()void method call against the Petition contract
       */
      primePetition(params: CallParams<PetitionArgs['obj']['prime_petition()void'] | PetitionArgs['tuple']['prime_petition()void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.primePetition(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a sign_petition(address,pay)uint64 method call against the Petition contract
       */
      signPetition(params: CallParams<PetitionArgs['obj']['sign_petition(address,pay)uint64'] | PetitionArgs['tuple']['sign_petition(address,pay)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.signPetition(params)))
        resultMappers.push((v) => client.decodeReturnValue('sign_petition(address,pay)uint64', v))
        return this
      },
      /**
       * Add a add_comment(byte[],pay)uint64 method call against the Petition contract
       */
      addComment(params: CallParams<PetitionArgs['obj']['add_comment(byte[],pay)uint64'] | PetitionArgs['tuple']['add_comment(byte[],pay)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.addComment(params)))
        resultMappers.push((v) => client.decodeReturnValue('add_comment(byte[],pay)uint64', v))
        return this
      },
      /**
       * Add a clear state call to the Petition contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as PetitionComposer
  }
}
export type PetitionComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the assign_master_and_petition_creator(application,account)void ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  assignMasterAndPetitionCreator(params?: CallParams<PetitionArgs['obj']['assign_master_and_petition_creator(application,account)void'] | PetitionArgs['tuple']['assign_master_and_petition_creator(application,account)void']>): PetitionComposer<[...TReturns, PetitionReturns['assign_master_and_petition_creator(application,account)void'] | undefined]>

  /**
   * Calls the create_petition(byte[],byte[],pay)uint64 ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  createPetition(params?: CallParams<PetitionArgs['obj']['create_petition(byte[],byte[],pay)uint64'] | PetitionArgs['tuple']['create_petition(byte[],byte[],pay)uint64']>): PetitionComposer<[...TReturns, PetitionReturns['create_petition(byte[],byte[],pay)uint64'] | undefined]>

  /**
   * Calls the additional_box_characters_option(byte[],pay)uint64 ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  additionalBoxCharactersOption(params?: CallParams<PetitionArgs['obj']['additional_box_characters_option(byte[],pay)uint64'] | PetitionArgs['tuple']['additional_box_characters_option(byte[],pay)uint64']>): PetitionComposer<[...TReturns, PetitionReturns['additional_box_characters_option(byte[],pay)uint64'] | undefined]>

  /**
   * Calls the prime_petition()void ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  primePetition(params?: CallParams<PetitionArgs['obj']['prime_petition()void'] | PetitionArgs['tuple']['prime_petition()void']>): PetitionComposer<[...TReturns, PetitionReturns['prime_petition()void'] | undefined]>

  /**
   * Calls the sign_petition(address,pay)uint64 ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  signPetition(params?: CallParams<PetitionArgs['obj']['sign_petition(address,pay)uint64'] | PetitionArgs['tuple']['sign_petition(address,pay)uint64']>): PetitionComposer<[...TReturns, PetitionReturns['sign_petition(address,pay)uint64'] | undefined]>

  /**
   * Calls the add_comment(byte[],pay)uint64 ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  addComment(params?: CallParams<PetitionArgs['obj']['add_comment(byte[],pay)uint64'] | PetitionArgs['tuple']['add_comment(byte[],pay)uint64']>): PetitionComposer<[...TReturns, PetitionReturns['add_comment(byte[],pay)uint64'] | undefined]>

  /**
   * Makes a clear_state call to an existing instance of the Petition smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): PetitionComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): PetitionComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<PetitionComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<PetitionComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<PetitionComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<PetitionComposerResults<TReturns>>
}
export type PetitionComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

