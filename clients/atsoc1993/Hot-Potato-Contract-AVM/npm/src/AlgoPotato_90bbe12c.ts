/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"AlgoPotato","structs":{"GameBoxName":[{"name":"player_1","type":"address"},{"name":"counter","type":"uint64"}],"GameBoxValue":[{"name":"player_1","type":"address"},{"name":"player_2","type":"address"},{"name":"player_1Round","type":"uint64"},{"name":"player_2Round","type":"uint64"},{"name":"vrfRound","type":"uint64"},{"name":"asset","type":"uint64"},{"name":"assetAmount","type":"uint64"}]},"methods":[{"name":"asset_opt_in","args":[{"type":"asset","name":"asset"},{"type":"txn","name":"mbr_payment"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Confirm the minimum balance requirement for the asset opt in of 100,000 Microalgo, or 0.1 Algo, was sent to the contract\n(Prevents potential usage of Algo funds reserves for games or excess mbr from box deletions)","events":[],"recommendations":{}},{"name":"create_game","args":[{"type":"txn","name":"asset_deposit"},{"type":"txn","name":"mbr_fee"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Initializes a game\n- Dynamically confirms the contract is the receiver of both the asset deposit and mbr fee - Gets the asset id and amount transferred from the asset deposit - Constructs a box name instance using the sender and current global counter - Constructs a box value template with only non-default values being player 1 address, asset (0 if algo), asset deposit amount, and player 1 round (current round) - Writes the box name and value","events":[],"recommendations":{}},{"name":"prime_game_vrf","args":[{"type":"(address,uint64)","struct":"GameBoxName","name":"game_box_name"},{"type":"txn","name":"asset_deposit"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Asserts the contract is the receiver of player 2's asset deposit\nGets the current game details Verifies that the asset deposit matches the game details, Updates the current game box with player 2's information Writes the updated information into box storage","events":[],"recommendations":{}},{"name":"play_game","args":[{"type":"(address,uint64)","struct":"GameBoxName","name":"game_box_name"}],"returns":{"type":"string"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Initializes the \"Hot Potato\" Game\n- Starts with an ensure budget requiring 14 inner txns - Gets the game information - Ensures a player 2 has joined and the game is ready - Gets the VRF Output % 240, see \"get_vrf_output_modulo\" for information on why the # 240 was chosen - Verifies both users are opted in or asset is Algorand which does not require opt in - If either of the users are not opted into the asset then the reward goes to the user that is still opted in - If both users are not opted in the first to opt in can claim the reward - If the VRF round has expired (1,512 rounds have passed since the VRF round selected) a new VRF round is created 9 rounds into the future - Hot Potato game starts, the contract sends Algo or Asset zero amount transactions to both players until the target modulo is reached - The player the 'Hot Potato' was on at the target modulo receives the reward - The reward is dispensed and the box is deleted to prevent state bloat - A little string is returned that states which player won","events":[],"recommendations":{}},{"name":"cancel_game","args":[{"type":"(address,uint64)","struct":"GameBoxName","name":"game_box_name"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Cancels a game if and only if there is no active player 2\nDispenses the asset deposit to player 1","events":[],"recommendations":{}}],"arcs":[22,28],"networks":{},"state":{"schema":{"global":{"ints":3,"bytes":0},"local":{"ints":0,"bytes":0}},"keys":{"global":{"games_played":{"keyType":"AVMString","valueType":"AVMUint64","key":"Z2FtZXNfcGxheWVk"},"counter":{"keyType":"AVMString","valueType":"AVMUint64","key":"Y291bnRlcg=="},"vrf_app_id":{"keyType":"AVMString","valueType":"AVMUint64","key":"dnJmX2FwcF9pZA=="}},"local":{},"box":{}},"maps":{"global":{},"local":{},"box":{"game_box":{"keyType":"GameBoxName","valueType":"GameBoxValue","prefix":""}}}},"bareActions":{"create":["NoOp"],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[1179],"errorMessage":"ARC4 prefix is valid"},{"pc":[504,510,523,555,565,635,667,673,1204,1209],"errorMessage":"Index access is out of bounds"},{"pc":[132,147,175,194,214],"errorMessage":"OnCompletion is not NoOp"},{"pc":[241],"errorMessage":"can only call when creating"},{"pc":[135,150,178,197,217],"errorMessage":"can only call when not creating"},{"pc":[391,402],"errorMessage":"check self.counter exists"},{"pc":[474,633,1202],"errorMessage":"check self.game_box entry exists"},{"pc":[757,858],"errorMessage":"check self.games_played exists"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"events":[]} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


// Type definitions for ARC-56 structs

export type GameBoxName = {
  player_1: string,
  counter: bigint
}


/**
 * Converts the ABI tuple representation of a GameBoxName to the struct representation
 */
export function GameBoxNameFromTuple(abiTuple: [string, bigint]) {
  return getABIStructFromABITuple(abiTuple, APP_SPEC.structs.GameBoxName, APP_SPEC.structs) as GameBoxName
}

export type GameBoxValue = {
  player_1: string,
  player_2: string,
  player_1Round: bigint,
  player_2Round: bigint,
  vrfRound: bigint,
  asset: bigint,
  assetAmount: bigint
}


/**
 * Converts the ABI tuple representation of a GameBoxValue to the struct representation
 */
export function GameBoxValueFromTuple(abiTuple: [string, string, bigint, bigint, bigint, bigint, bigint]) {
  return getABIStructFromABITuple(abiTuple, APP_SPEC.structs.GameBoxValue, APP_SPEC.structs) as GameBoxValue
}

/**
 * The argument types for the AlgoPotato contract
 */
export type AlgoPotatoArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'asset_opt_in(asset,txn)void': {
      asset: bigint
      mbrPayment: AppMethodCallTransactionArgument
    }
    'create_game(txn,txn)void': {
      assetDeposit?: AppMethodCallTransactionArgument
      mbrFee: AppMethodCallTransactionArgument
    }
    'prime_game_vrf((address,uint64),txn)void': {
      gameBoxName: GameBoxName
      assetDeposit: AppMethodCallTransactionArgument
    }
    'play_game((address,uint64))string': {
      gameBoxName: GameBoxName
    }
    'cancel_game((address,uint64))void': {
      gameBoxName: GameBoxName
    }
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'asset_opt_in(asset,txn)void': [asset: bigint, mbrPayment: AppMethodCallTransactionArgument]
    'create_game(txn,txn)void': [assetDeposit: AppMethodCallTransactionArgument | undefined, mbrFee: AppMethodCallTransactionArgument]
    'prime_game_vrf((address,uint64),txn)void': [gameBoxName: GameBoxName, assetDeposit: AppMethodCallTransactionArgument]
    'play_game((address,uint64))string': [gameBoxName: GameBoxName]
    'cancel_game((address,uint64))void': [gameBoxName: GameBoxName]
  }
}

/**
 * The return type for each method
 */
export type AlgoPotatoReturns = {
  'asset_opt_in(asset,txn)void': void
  'create_game(txn,txn)void': void
  'prime_game_vrf((address,uint64),txn)void': void
  'play_game((address,uint64))string': string
  'cancel_game((address,uint64))void': void
}

/**
 * Defines the types of available calls and state of the AlgoPotato smart contract.
 */
export type AlgoPotatoTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'asset_opt_in(asset,txn)void' | 'asset_opt_in', {
      argsObj: AlgoPotatoArgs['obj']['asset_opt_in(asset,txn)void']
      argsTuple: AlgoPotatoArgs['tuple']['asset_opt_in(asset,txn)void']
      returns: AlgoPotatoReturns['asset_opt_in(asset,txn)void']
    }>
    & Record<'create_game(txn,txn)void' | 'create_game', {
      argsObj: AlgoPotatoArgs['obj']['create_game(txn,txn)void']
      argsTuple: AlgoPotatoArgs['tuple']['create_game(txn,txn)void']
      returns: AlgoPotatoReturns['create_game(txn,txn)void']
    }>
    & Record<'prime_game_vrf((address,uint64),txn)void' | 'prime_game_vrf', {
      argsObj: AlgoPotatoArgs['obj']['prime_game_vrf((address,uint64),txn)void']
      argsTuple: AlgoPotatoArgs['tuple']['prime_game_vrf((address,uint64),txn)void']
      returns: AlgoPotatoReturns['prime_game_vrf((address,uint64),txn)void']
    }>
    & Record<'play_game((address,uint64))string' | 'play_game', {
      argsObj: AlgoPotatoArgs['obj']['play_game((address,uint64))string']
      argsTuple: AlgoPotatoArgs['tuple']['play_game((address,uint64))string']
      returns: AlgoPotatoReturns['play_game((address,uint64))string']
    }>
    & Record<'cancel_game((address,uint64))void' | 'cancel_game', {
      argsObj: AlgoPotatoArgs['obj']['cancel_game((address,uint64))void']
      argsTuple: AlgoPotatoArgs['tuple']['cancel_game((address,uint64))void']
      returns: AlgoPotatoReturns['cancel_game((address,uint64))void']
    }>
  /**
   * Defines the shape of the state of the application.
   */
  state: {
    global: {
      keys: {
        gamesPlayed: bigint
        counter: bigint
        vrfAppId: bigint
      }
      maps: {}
    }
    box: {
      keys: {}
      maps: {
        gameBox: Map<GameBoxName, GameBoxValue>
      }
    }
  }
}

/**
 * Defines the possible abi call signatures.
 */
export type AlgoPotatoSignatures = keyof AlgoPotatoTypes['methods']
/**
 * Defines the possible abi call signatures for methods that return a non-void value.
 */
export type AlgoPotatoNonVoidMethodSignatures = keyof AlgoPotatoTypes['methods'] extends infer T ? T extends keyof AlgoPotatoTypes['methods'] ? MethodReturn<T> extends void ? never : T  : never : never
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the AlgoPotato smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends AlgoPotatoSignatures> = AlgoPotatoTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the AlgoPotato smart contract to the method's return type
 */
export type MethodReturn<TSignature extends AlgoPotatoSignatures> = AlgoPotatoTypes['methods'][TSignature]['returns']

/**
 * Defines the shape of the keyed global state of the application.
 */
export type GlobalKeysState = AlgoPotatoTypes['state']['global']['keys']

/**
 * Defines the shape of the keyed box state of the application.
 */
export type BoxKeysState = AlgoPotatoTypes['state']['box']['keys']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the AlgoPotato smart contract
 */
export abstract class AlgoPotatoParamsFactory {
  /**
   * Constructs a no op call for the asset_opt_in(asset,txn)void ABI method
   *
  * Confirm the minimum balance requirement for the asset opt in of 100,000 Microalgo, or 0.1 Algo, was sent to the contract
  (Prevents potential usage of Algo funds reserves for games or excess mbr from box deletions)

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static assetOptIn(params: CallParams<AlgoPotatoArgs['obj']['asset_opt_in(asset,txn)void'] | AlgoPotatoArgs['tuple']['asset_opt_in(asset,txn)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'asset_opt_in(asset,txn)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.asset, params.args.mbrPayment],
    }
  }
  /**
   * Constructs a no op call for the create_game(txn,txn)void ABI method
   *
  * Initializes a game
  - Dynamically confirms the contract is the receiver of both the asset deposit and mbr fee - Gets the asset id and amount transferred from the asset deposit - Constructs a box name instance using the sender and current global counter - Constructs a box value template with only non-default values being player 1 address, asset (0 if algo), asset deposit amount, and player 1 round (current round) - Writes the box name and value

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static createGame(params: CallParams<AlgoPotatoArgs['obj']['create_game(txn,txn)void'] | AlgoPotatoArgs['tuple']['create_game(txn,txn)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'create_game(txn,txn)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.assetDeposit, params.args.mbrFee],
    }
  }
  /**
   * Constructs a no op call for the prime_game_vrf((address,uint64),txn)void ABI method
   *
  * Asserts the contract is the receiver of player 2's asset deposit
  Gets the current game details Verifies that the asset deposit matches the game details, Updates the current game box with player 2's information Writes the updated information into box storage

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static primeGameVrf(params: CallParams<AlgoPotatoArgs['obj']['prime_game_vrf((address,uint64),txn)void'] | AlgoPotatoArgs['tuple']['prime_game_vrf((address,uint64),txn)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'prime_game_vrf((address,uint64),txn)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.gameBoxName, params.args.assetDeposit],
    }
  }
  /**
   * Constructs a no op call for the play_game((address,uint64))string ABI method
   *
  * Initializes the "Hot Potato" Game
  - Starts with an ensure budget requiring 14 inner txns - Gets the game information - Ensures a player 2 has joined and the game is ready - Gets the VRF Output % 240, see "get_vrf_output_modulo" for information on why the # 240 was chosen - Verifies both users are opted in or asset is Algorand which does not require opt in - If either of the users are not opted into the asset then the reward goes to the user that is still opted in - If both users are not opted in the first to opt in can claim the reward - If the VRF round has expired (1,512 rounds have passed since the VRF round selected) a new VRF round is created 9 rounds into the future - Hot Potato game starts, the contract sends Algo or Asset zero amount transactions to both players until the target modulo is reached - The player the 'Hot Potato' was on at the target modulo receives the reward - The reward is dispensed and the box is deleted to prevent state bloat - A little string is returned that states which player won

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static playGame(params: CallParams<AlgoPotatoArgs['obj']['play_game((address,uint64))string'] | AlgoPotatoArgs['tuple']['play_game((address,uint64))string']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'play_game((address,uint64))string' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.gameBoxName],
    }
  }
  /**
   * Constructs a no op call for the cancel_game((address,uint64))void ABI method
   *
  * Cancels a game if and only if there is no active player 2
  Dispenses the asset deposit to player 1

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static cancelGame(params: CallParams<AlgoPotatoArgs['obj']['cancel_game((address,uint64))void'] | AlgoPotatoArgs['tuple']['cancel_game((address,uint64))void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'cancel_game((address,uint64))void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.gameBoxName],
    }
  }
}

/**
 * A client to make calls to the AlgoPotato smart contract
 */
export class AlgoPotatoClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `AlgoPotatoClient`
   *
   * @param appClient An `AppClient` instance which has been created with the AlgoPotato app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `AlgoPotatoClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Checks for decode errors on the given return value and maps the return value to the return type for the given method
   * @returns The typed return value or undefined if there was no value
   */
  decodeReturnValue<TSignature extends AlgoPotatoNonVoidMethodSignatures>(method: TSignature, returnValue: ABIReturn | undefined) {
    return returnValue !== undefined ? getArc56ReturnValue<MethodReturn<TSignature>>(returnValue, this.appClient.getABIMethod(method), APP_SPEC.structs) : undefined
  }

  /**
   * Returns a new `AlgoPotatoClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<AlgoPotatoClient> {
    return new AlgoPotatoClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `AlgoPotatoClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<AlgoPotatoClient> {
    return new AlgoPotatoClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Makes a clear_state call to an existing instance of the AlgoPotato smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the AlgoPotato smart contract using the `asset_opt_in(asset,txn)void` ABI method.
     *
    * Confirm the minimum balance requirement for the asset opt in of 100,000 Microalgo, or 0.1 Algo, was sent to the contract
    (Prevents potential usage of Algo funds reserves for games or excess mbr from box deletions)

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    assetOptIn: (params: CallParams<AlgoPotatoArgs['obj']['asset_opt_in(asset,txn)void'] | AlgoPotatoArgs['tuple']['asset_opt_in(asset,txn)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(AlgoPotatoParamsFactory.assetOptIn(params))
    },

    /**
     * Makes a call to the AlgoPotato smart contract using the `create_game(txn,txn)void` ABI method.
     *
    * Initializes a game
    - Dynamically confirms the contract is the receiver of both the asset deposit and mbr fee - Gets the asset id and amount transferred from the asset deposit - Constructs a box name instance using the sender and current global counter - Constructs a box value template with only non-default values being player 1 address, asset (0 if algo), asset deposit amount, and player 1 round (current round) - Writes the box name and value

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    createGame: (params: CallParams<AlgoPotatoArgs['obj']['create_game(txn,txn)void'] | AlgoPotatoArgs['tuple']['create_game(txn,txn)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(AlgoPotatoParamsFactory.createGame(params))
    },

    /**
     * Makes a call to the AlgoPotato smart contract using the `prime_game_vrf((address,uint64),txn)void` ABI method.
     *
    * Asserts the contract is the receiver of player 2's asset deposit
    Gets the current game details Verifies that the asset deposit matches the game details, Updates the current game box with player 2's information Writes the updated information into box storage

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    primeGameVrf: (params: CallParams<AlgoPotatoArgs['obj']['prime_game_vrf((address,uint64),txn)void'] | AlgoPotatoArgs['tuple']['prime_game_vrf((address,uint64),txn)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(AlgoPotatoParamsFactory.primeGameVrf(params))
    },

    /**
     * Makes a call to the AlgoPotato smart contract using the `play_game((address,uint64))string` ABI method.
     *
    * Initializes the "Hot Potato" Game
    - Starts with an ensure budget requiring 14 inner txns - Gets the game information - Ensures a player 2 has joined and the game is ready - Gets the VRF Output % 240, see "get_vrf_output_modulo" for information on why the # 240 was chosen - Verifies both users are opted in or asset is Algorand which does not require opt in - If either of the users are not opted into the asset then the reward goes to the user that is still opted in - If both users are not opted in the first to opt in can claim the reward - If the VRF round has expired (1,512 rounds have passed since the VRF round selected) a new VRF round is created 9 rounds into the future - Hot Potato game starts, the contract sends Algo or Asset zero amount transactions to both players until the target modulo is reached - The player the 'Hot Potato' was on at the target modulo receives the reward - The reward is dispensed and the box is deleted to prevent state bloat - A little string is returned that states which player won

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    playGame: (params: CallParams<AlgoPotatoArgs['obj']['play_game((address,uint64))string'] | AlgoPotatoArgs['tuple']['play_game((address,uint64))string']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(AlgoPotatoParamsFactory.playGame(params))
    },

    /**
     * Makes a call to the AlgoPotato smart contract using the `cancel_game((address,uint64))void` ABI method.
     *
    * Cancels a game if and only if there is no active player 2
    Dispenses the asset deposit to player 1

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    cancelGame: (params: CallParams<AlgoPotatoArgs['obj']['cancel_game((address,uint64))void'] | AlgoPotatoArgs['tuple']['cancel_game((address,uint64))void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(AlgoPotatoParamsFactory.cancelGame(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Makes a clear_state call to an existing instance of the AlgoPotato smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the AlgoPotato smart contract using the `asset_opt_in(asset,txn)void` ABI method.
     *
    * Confirm the minimum balance requirement for the asset opt in of 100,000 Microalgo, or 0.1 Algo, was sent to the contract
    (Prevents potential usage of Algo funds reserves for games or excess mbr from box deletions)

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    assetOptIn: (params: CallParams<AlgoPotatoArgs['obj']['asset_opt_in(asset,txn)void'] | AlgoPotatoArgs['tuple']['asset_opt_in(asset,txn)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(AlgoPotatoParamsFactory.assetOptIn(params))
    },

    /**
     * Makes a call to the AlgoPotato smart contract using the `create_game(txn,txn)void` ABI method.
     *
    * Initializes a game
    - Dynamically confirms the contract is the receiver of both the asset deposit and mbr fee - Gets the asset id and amount transferred from the asset deposit - Constructs a box name instance using the sender and current global counter - Constructs a box value template with only non-default values being player 1 address, asset (0 if algo), asset deposit amount, and player 1 round (current round) - Writes the box name and value

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    createGame: (params: CallParams<AlgoPotatoArgs['obj']['create_game(txn,txn)void'] | AlgoPotatoArgs['tuple']['create_game(txn,txn)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(AlgoPotatoParamsFactory.createGame(params))
    },

    /**
     * Makes a call to the AlgoPotato smart contract using the `prime_game_vrf((address,uint64),txn)void` ABI method.
     *
    * Asserts the contract is the receiver of player 2's asset deposit
    Gets the current game details Verifies that the asset deposit matches the game details, Updates the current game box with player 2's information Writes the updated information into box storage

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    primeGameVrf: (params: CallParams<AlgoPotatoArgs['obj']['prime_game_vrf((address,uint64),txn)void'] | AlgoPotatoArgs['tuple']['prime_game_vrf((address,uint64),txn)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(AlgoPotatoParamsFactory.primeGameVrf(params))
    },

    /**
     * Makes a call to the AlgoPotato smart contract using the `play_game((address,uint64))string` ABI method.
     *
    * Initializes the "Hot Potato" Game
    - Starts with an ensure budget requiring 14 inner txns - Gets the game information - Ensures a player 2 has joined and the game is ready - Gets the VRF Output % 240, see "get_vrf_output_modulo" for information on why the # 240 was chosen - Verifies both users are opted in or asset is Algorand which does not require opt in - If either of the users are not opted into the asset then the reward goes to the user that is still opted in - If both users are not opted in the first to opt in can claim the reward - If the VRF round has expired (1,512 rounds have passed since the VRF round selected) a new VRF round is created 9 rounds into the future - Hot Potato game starts, the contract sends Algo or Asset zero amount transactions to both players until the target modulo is reached - The player the 'Hot Potato' was on at the target modulo receives the reward - The reward is dispensed and the box is deleted to prevent state bloat - A little string is returned that states which player won

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    playGame: (params: CallParams<AlgoPotatoArgs['obj']['play_game((address,uint64))string'] | AlgoPotatoArgs['tuple']['play_game((address,uint64))string']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(AlgoPotatoParamsFactory.playGame(params))
    },

    /**
     * Makes a call to the AlgoPotato smart contract using the `cancel_game((address,uint64))void` ABI method.
     *
    * Cancels a game if and only if there is no active player 2
    Dispenses the asset deposit to player 1

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    cancelGame: (params: CallParams<AlgoPotatoArgs['obj']['cancel_game((address,uint64))void'] | AlgoPotatoArgs['tuple']['cancel_game((address,uint64))void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(AlgoPotatoParamsFactory.cancelGame(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Makes a clear_state call to an existing instance of the AlgoPotato smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the AlgoPotato smart contract using the `asset_opt_in(asset,txn)void` ABI method.
     *
    * Confirm the minimum balance requirement for the asset opt in of 100,000 Microalgo, or 0.1 Algo, was sent to the contract
    (Prevents potential usage of Algo funds reserves for games or excess mbr from box deletions)

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    assetOptIn: async (params: CallParams<AlgoPotatoArgs['obj']['asset_opt_in(asset,txn)void'] | AlgoPotatoArgs['tuple']['asset_opt_in(asset,txn)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(AlgoPotatoParamsFactory.assetOptIn(params))
      return {...result, return: result.return as unknown as (undefined | AlgoPotatoReturns['asset_opt_in(asset,txn)void'])}
    },

    /**
     * Makes a call to the AlgoPotato smart contract using the `create_game(txn,txn)void` ABI method.
     *
    * Initializes a game
    - Dynamically confirms the contract is the receiver of both the asset deposit and mbr fee - Gets the asset id and amount transferred from the asset deposit - Constructs a box name instance using the sender and current global counter - Constructs a box value template with only non-default values being player 1 address, asset (0 if algo), asset deposit amount, and player 1 round (current round) - Writes the box name and value

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    createGame: async (params: CallParams<AlgoPotatoArgs['obj']['create_game(txn,txn)void'] | AlgoPotatoArgs['tuple']['create_game(txn,txn)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(AlgoPotatoParamsFactory.createGame(params))
      return {...result, return: result.return as unknown as (undefined | AlgoPotatoReturns['create_game(txn,txn)void'])}
    },

    /**
     * Makes a call to the AlgoPotato smart contract using the `prime_game_vrf((address,uint64),txn)void` ABI method.
     *
    * Asserts the contract is the receiver of player 2's asset deposit
    Gets the current game details Verifies that the asset deposit matches the game details, Updates the current game box with player 2's information Writes the updated information into box storage

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    primeGameVrf: async (params: CallParams<AlgoPotatoArgs['obj']['prime_game_vrf((address,uint64),txn)void'] | AlgoPotatoArgs['tuple']['prime_game_vrf((address,uint64),txn)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(AlgoPotatoParamsFactory.primeGameVrf(params))
      return {...result, return: result.return as unknown as (undefined | AlgoPotatoReturns['prime_game_vrf((address,uint64),txn)void'])}
    },

    /**
     * Makes a call to the AlgoPotato smart contract using the `play_game((address,uint64))string` ABI method.
     *
    * Initializes the "Hot Potato" Game
    - Starts with an ensure budget requiring 14 inner txns - Gets the game information - Ensures a player 2 has joined and the game is ready - Gets the VRF Output % 240, see "get_vrf_output_modulo" for information on why the # 240 was chosen - Verifies both users are opted in or asset is Algorand which does not require opt in - If either of the users are not opted into the asset then the reward goes to the user that is still opted in - If both users are not opted in the first to opt in can claim the reward - If the VRF round has expired (1,512 rounds have passed since the VRF round selected) a new VRF round is created 9 rounds into the future - Hot Potato game starts, the contract sends Algo or Asset zero amount transactions to both players until the target modulo is reached - The player the 'Hot Potato' was on at the target modulo receives the reward - The reward is dispensed and the box is deleted to prevent state bloat - A little string is returned that states which player won

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    playGame: async (params: CallParams<AlgoPotatoArgs['obj']['play_game((address,uint64))string'] | AlgoPotatoArgs['tuple']['play_game((address,uint64))string']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(AlgoPotatoParamsFactory.playGame(params))
      return {...result, return: result.return as unknown as (undefined | AlgoPotatoReturns['play_game((address,uint64))string'])}
    },

    /**
     * Makes a call to the AlgoPotato smart contract using the `cancel_game((address,uint64))void` ABI method.
     *
    * Cancels a game if and only if there is no active player 2
    Dispenses the asset deposit to player 1

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    cancelGame: async (params: CallParams<AlgoPotatoArgs['obj']['cancel_game((address,uint64))void'] | AlgoPotatoArgs['tuple']['cancel_game((address,uint64))void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(AlgoPotatoParamsFactory.cancelGame(params))
      return {...result, return: result.return as unknown as (undefined | AlgoPotatoReturns['cancel_game((address,uint64))void'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new AlgoPotatoClient(this.appClient.clone(params))
  }

  /**
   * Methods to access state for the current AlgoPotato app
   */
  state = {
    /**
     * Methods to access global state for the current AlgoPotato app
     */
    global: {
      /**
       * Get all current keyed values from global state
       */
      getAll: async (): Promise<Partial<Expand<GlobalKeysState>>> => {
        const result = await this.appClient.state.global.getAll()
        return {
          gamesPlayed: result.games_played,
          counter: result.counter,
          vrfAppId: result.vrf_app_id,
        }
      },
      /**
       * Get the current value of the games_played key in global state
       */
      gamesPlayed: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("games_played")) as bigint | undefined },
      /**
       * Get the current value of the counter key in global state
       */
      counter: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("counter")) as bigint | undefined },
      /**
       * Get the current value of the vrf_app_id key in global state
       */
      vrfAppId: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("vrf_app_id")) as bigint | undefined },
    },
    /**
     * Methods to access box state for the current AlgoPotato app
     */
    box: {
      /**
       * Get all current keyed values from box state
       */
      getAll: async (): Promise<Partial<Expand<BoxKeysState>>> => {
        const result = await this.appClient.state.box.getAll()
        return {
        }
      },
      /**
       * Get values from the game_box map in box state
       */
      gameBox: {
        /**
         * Get all current values of the game_box map in box state
         */
        getMap: async (): Promise<Map<GameBoxName, GameBoxValue>> => { return (await this.appClient.state.box.getMap("game_box")) as Map<GameBoxName, GameBoxValue> },
        /**
         * Get a current value of the game_box map by key from box state
         */
        value: async (key: GameBoxName): Promise<GameBoxValue | undefined> => { return await this.appClient.state.box.getMapValue("game_box", key) as GameBoxValue | undefined },
      },
    },
  }

  public newGroup(): AlgoPotatoComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a asset_opt_in(asset,txn)void method call against the AlgoPotato contract
       */
      assetOptIn(params: CallParams<AlgoPotatoArgs['obj']['asset_opt_in(asset,txn)void'] | AlgoPotatoArgs['tuple']['asset_opt_in(asset,txn)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.assetOptIn(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a create_game(txn,txn)void method call against the AlgoPotato contract
       */
      createGame(params: CallParams<AlgoPotatoArgs['obj']['create_game(txn,txn)void'] | AlgoPotatoArgs['tuple']['create_game(txn,txn)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.createGame(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a prime_game_vrf((address,uint64),txn)void method call against the AlgoPotato contract
       */
      primeGameVrf(params: CallParams<AlgoPotatoArgs['obj']['prime_game_vrf((address,uint64),txn)void'] | AlgoPotatoArgs['tuple']['prime_game_vrf((address,uint64),txn)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.primeGameVrf(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a play_game((address,uint64))string method call against the AlgoPotato contract
       */
      playGame(params: CallParams<AlgoPotatoArgs['obj']['play_game((address,uint64))string'] | AlgoPotatoArgs['tuple']['play_game((address,uint64))string']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.playGame(params)))
        resultMappers.push((v) => client.decodeReturnValue('play_game((address,uint64))string', v))
        return this
      },
      /**
       * Add a cancel_game((address,uint64))void method call against the AlgoPotato contract
       */
      cancelGame(params: CallParams<AlgoPotatoArgs['obj']['cancel_game((address,uint64))void'] | AlgoPotatoArgs['tuple']['cancel_game((address,uint64))void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.cancelGame(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a clear state call to the AlgoPotato contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as AlgoPotatoComposer
  }
}
export type AlgoPotatoComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the asset_opt_in(asset,txn)void ABI method.
   *
  * Confirm the minimum balance requirement for the asset opt in of 100,000 Microalgo, or 0.1 Algo, was sent to the contract
  (Prevents potential usage of Algo funds reserves for games or excess mbr from box deletions)

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  assetOptIn(params?: CallParams<AlgoPotatoArgs['obj']['asset_opt_in(asset,txn)void'] | AlgoPotatoArgs['tuple']['asset_opt_in(asset,txn)void']>): AlgoPotatoComposer<[...TReturns, AlgoPotatoReturns['asset_opt_in(asset,txn)void'] | undefined]>

  /**
   * Calls the create_game(txn,txn)void ABI method.
   *
  * Initializes a game
  - Dynamically confirms the contract is the receiver of both the asset deposit and mbr fee - Gets the asset id and amount transferred from the asset deposit - Constructs a box name instance using the sender and current global counter - Constructs a box value template with only non-default values being player 1 address, asset (0 if algo), asset deposit amount, and player 1 round (current round) - Writes the box name and value

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  createGame(params?: CallParams<AlgoPotatoArgs['obj']['create_game(txn,txn)void'] | AlgoPotatoArgs['tuple']['create_game(txn,txn)void']>): AlgoPotatoComposer<[...TReturns, AlgoPotatoReturns['create_game(txn,txn)void'] | undefined]>

  /**
   * Calls the prime_game_vrf((address,uint64),txn)void ABI method.
   *
  * Asserts the contract is the receiver of player 2's asset deposit
  Gets the current game details Verifies that the asset deposit matches the game details, Updates the current game box with player 2's information Writes the updated information into box storage

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  primeGameVrf(params?: CallParams<AlgoPotatoArgs['obj']['prime_game_vrf((address,uint64),txn)void'] | AlgoPotatoArgs['tuple']['prime_game_vrf((address,uint64),txn)void']>): AlgoPotatoComposer<[...TReturns, AlgoPotatoReturns['prime_game_vrf((address,uint64),txn)void'] | undefined]>

  /**
   * Calls the play_game((address,uint64))string ABI method.
   *
  * Initializes the "Hot Potato" Game
  - Starts with an ensure budget requiring 14 inner txns - Gets the game information - Ensures a player 2 has joined and the game is ready - Gets the VRF Output % 240, see "get_vrf_output_modulo" for information on why the # 240 was chosen - Verifies both users are opted in or asset is Algorand which does not require opt in - If either of the users are not opted into the asset then the reward goes to the user that is still opted in - If both users are not opted in the first to opt in can claim the reward - If the VRF round has expired (1,512 rounds have passed since the VRF round selected) a new VRF round is created 9 rounds into the future - Hot Potato game starts, the contract sends Algo or Asset zero amount transactions to both players until the target modulo is reached - The player the 'Hot Potato' was on at the target modulo receives the reward - The reward is dispensed and the box is deleted to prevent state bloat - A little string is returned that states which player won

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  playGame(params?: CallParams<AlgoPotatoArgs['obj']['play_game((address,uint64))string'] | AlgoPotatoArgs['tuple']['play_game((address,uint64))string']>): AlgoPotatoComposer<[...TReturns, AlgoPotatoReturns['play_game((address,uint64))string'] | undefined]>

  /**
   * Calls the cancel_game((address,uint64))void ABI method.
   *
  * Cancels a game if and only if there is no active player 2
  Dispenses the asset deposit to player 1

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  cancelGame(params?: CallParams<AlgoPotatoArgs['obj']['cancel_game((address,uint64))void'] | AlgoPotatoArgs['tuple']['cancel_game((address,uint64))void']>): AlgoPotatoComposer<[...TReturns, AlgoPotatoReturns['cancel_game((address,uint64))void'] | undefined]>

  /**
   * Makes a clear_state call to an existing instance of the AlgoPotato smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): AlgoPotatoComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): AlgoPotatoComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<AlgoPotatoComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<AlgoPotatoComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<AlgoPotatoComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<AlgoPotatoComposerResults<TReturns>>
}
export type AlgoPotatoComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

