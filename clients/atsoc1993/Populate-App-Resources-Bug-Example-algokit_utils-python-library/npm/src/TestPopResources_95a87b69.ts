/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"TestPopResources","structs":{},"methods":[{"name":"populateTest","args":[{"type":"pay","name":"mbr_payment"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}},{"name":"arbitrary_app_call","args":[],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}}],"arcs":[22,28],"networks":{},"state":{"schema":{"global":{"ints":0,"bytes":0},"local":{"ints":0,"bytes":0}},"keys":{"global":{},"local":{},"box":{}},"maps":{"global":{},"local":{},"box":{}}},"bareActions":{"create":["NoOp"],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[46,55],"errorMessage":"OnCompletion is not NoOp"},{"pc":[92,180],"errorMessage":"account funded"},{"pc":[82],"errorMessage":"can only call when creating"},{"pc":[49,58],"errorMessage":"can only call when not creating"},{"pc":[68],"errorMessage":"transaction type is pay"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"events":[]} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


/**
 * The argument types for the TestPopResources contract
 */
export type TestPopResourcesArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'populateTest(pay)void': {
      mbrPayment: AppMethodCallTransactionArgument
    }
    'arbitrary_app_call()void': Record<string, never>
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'populateTest(pay)void': [mbrPayment: AppMethodCallTransactionArgument]
    'arbitrary_app_call()void': []
  }
}

/**
 * The return type for each method
 */
export type TestPopResourcesReturns = {
  'populateTest(pay)void': void
  'arbitrary_app_call()void': void
}

/**
 * Defines the types of available calls and state of the TestPopResources smart contract.
 */
export type TestPopResourcesTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'populateTest(pay)void' | 'populateTest', {
      argsObj: TestPopResourcesArgs['obj']['populateTest(pay)void']
      argsTuple: TestPopResourcesArgs['tuple']['populateTest(pay)void']
      returns: TestPopResourcesReturns['populateTest(pay)void']
    }>
    & Record<'arbitrary_app_call()void' | 'arbitrary_app_call', {
      argsObj: TestPopResourcesArgs['obj']['arbitrary_app_call()void']
      argsTuple: TestPopResourcesArgs['tuple']['arbitrary_app_call()void']
      returns: TestPopResourcesReturns['arbitrary_app_call()void']
    }>
}

/**
 * Defines the possible abi call signatures.
 */
export type TestPopResourcesSignatures = keyof TestPopResourcesTypes['methods']
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the TestPopResources smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends TestPopResourcesSignatures> = TestPopResourcesTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the TestPopResources smart contract to the method's return type
 */
export type MethodReturn<TSignature extends TestPopResourcesSignatures> = TestPopResourcesTypes['methods'][TSignature]['returns']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the TestPopResources smart contract
 */
export abstract class TestPopResourcesParamsFactory {
  /**
   * Constructs a no op call for the populateTest(pay)void ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static populateTest(params: CallParams<TestPopResourcesArgs['obj']['populateTest(pay)void'] | TestPopResourcesArgs['tuple']['populateTest(pay)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'populateTest(pay)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.mbrPayment],
    }
  }
  /**
   * Constructs a no op call for the arbitrary_app_call()void ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static arbitraryAppCall(params: CallParams<TestPopResourcesArgs['obj']['arbitrary_app_call()void'] | TestPopResourcesArgs['tuple']['arbitrary_app_call()void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'arbitrary_app_call()void' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
}

/**
 * A client to make calls to the TestPopResources smart contract
 */
export class TestPopResourcesClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `TestPopResourcesClient`
   *
   * @param appClient An `AppClient` instance which has been created with the TestPopResources app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `TestPopResourcesClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Returns a new `TestPopResourcesClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<TestPopResourcesClient> {
    return new TestPopResourcesClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `TestPopResourcesClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<TestPopResourcesClient> {
    return new TestPopResourcesClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Makes a clear_state call to an existing instance of the TestPopResources smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the TestPopResources smart contract using the `populateTest(pay)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    populateTest: (params: CallParams<TestPopResourcesArgs['obj']['populateTest(pay)void'] | TestPopResourcesArgs['tuple']['populateTest(pay)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(TestPopResourcesParamsFactory.populateTest(params))
    },

    /**
     * Makes a call to the TestPopResources smart contract using the `arbitrary_app_call()void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    arbitraryAppCall: (params: CallParams<TestPopResourcesArgs['obj']['arbitrary_app_call()void'] | TestPopResourcesArgs['tuple']['arbitrary_app_call()void']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(TestPopResourcesParamsFactory.arbitraryAppCall(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Makes a clear_state call to an existing instance of the TestPopResources smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the TestPopResources smart contract using the `populateTest(pay)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    populateTest: (params: CallParams<TestPopResourcesArgs['obj']['populateTest(pay)void'] | TestPopResourcesArgs['tuple']['populateTest(pay)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(TestPopResourcesParamsFactory.populateTest(params))
    },

    /**
     * Makes a call to the TestPopResources smart contract using the `arbitrary_app_call()void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    arbitraryAppCall: (params: CallParams<TestPopResourcesArgs['obj']['arbitrary_app_call()void'] | TestPopResourcesArgs['tuple']['arbitrary_app_call()void']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(TestPopResourcesParamsFactory.arbitraryAppCall(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Makes a clear_state call to an existing instance of the TestPopResources smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the TestPopResources smart contract using the `populateTest(pay)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    populateTest: async (params: CallParams<TestPopResourcesArgs['obj']['populateTest(pay)void'] | TestPopResourcesArgs['tuple']['populateTest(pay)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(TestPopResourcesParamsFactory.populateTest(params))
      return {...result, return: result.return as unknown as (undefined | TestPopResourcesReturns['populateTest(pay)void'])}
    },

    /**
     * Makes a call to the TestPopResources smart contract using the `arbitrary_app_call()void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    arbitraryAppCall: async (params: CallParams<TestPopResourcesArgs['obj']['arbitrary_app_call()void'] | TestPopResourcesArgs['tuple']['arbitrary_app_call()void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(TestPopResourcesParamsFactory.arbitraryAppCall(params))
      return {...result, return: result.return as unknown as (undefined | TestPopResourcesReturns['arbitrary_app_call()void'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new TestPopResourcesClient(this.appClient.clone(params))
  }

  /**
   * Methods to access state for the current TestPopResources app
   */
  state = {
  }

  public newGroup(): TestPopResourcesComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a populateTest(pay)void method call against the TestPopResources contract
       */
      populateTest(params: CallParams<TestPopResourcesArgs['obj']['populateTest(pay)void'] | TestPopResourcesArgs['tuple']['populateTest(pay)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.populateTest(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a arbitrary_app_call()void method call against the TestPopResources contract
       */
      arbitraryAppCall(params: CallParams<TestPopResourcesArgs['obj']['arbitrary_app_call()void'] | TestPopResourcesArgs['tuple']['arbitrary_app_call()void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.arbitraryAppCall(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a clear state call to the TestPopResources contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as TestPopResourcesComposer
  }
}
export type TestPopResourcesComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the populateTest(pay)void ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  populateTest(params?: CallParams<TestPopResourcesArgs['obj']['populateTest(pay)void'] | TestPopResourcesArgs['tuple']['populateTest(pay)void']>): TestPopResourcesComposer<[...TReturns, TestPopResourcesReturns['populateTest(pay)void'] | undefined]>

  /**
   * Calls the arbitrary_app_call()void ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  arbitraryAppCall(params?: CallParams<TestPopResourcesArgs['obj']['arbitrary_app_call()void'] | TestPopResourcesArgs['tuple']['arbitrary_app_call()void']>): TestPopResourcesComposer<[...TReturns, TestPopResourcesReturns['arbitrary_app_call()void'] | undefined]>

  /**
   * Makes a clear_state call to an existing instance of the TestPopResources smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): TestPopResourcesComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): TestPopResourcesComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<TestPopResourcesComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<TestPopResourcesComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<TestPopResourcesComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<TestPopResourcesComposerResults<TReturns>>
}
export type TestPopResourcesComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

