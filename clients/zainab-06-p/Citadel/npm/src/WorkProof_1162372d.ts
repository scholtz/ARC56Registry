/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"WorkProof","structs":{},"methods":[{"name":"create_work_contract","args":[{"type":"account","name":"contractor"},{"type":"account","name":"supervisor"},{"type":"account","name":"worker"},{"type":"uint64","name":"milestone_amount"},{"type":"byte[]","name":"milestone_name"},{"type":"pay","name":"pay_txn"}],"returns":{"type":"uint64"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Creates a single-milestone WorkProof agreement.\nThis method expects a grouped payment from `contractor` to the app address as escrow.","events":[],"recommendations":{}},{"name":"approve_milestone","args":[{"type":"byte[]","name":"metadata_url"},{"type":"byte[]","name":"metadata_hash"}],"returns":{"type":"uint64"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Supervisor approves the milestone.\nReleases ALGO from the app escrow to `worker` and mints/transfers a 1-of-1 credential ASA.","events":[],"recommendations":{}},{"name":"claim_credential","args":[],"returns":{"type":"uint64"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Worker claims the minted credential ASA.\nThe worker must be opted in to the ASA before this call.","events":[],"recommendations":{}}],"arcs":[22,28],"networks":{"testnet":{"appID":758015705}},"state":{"schema":{"global":{"ints":5,"bytes":4},"local":{"ints":0,"bytes":0}},"keys":{"global":{"contractor":{"keyType":"AVMString","valueType":"address","key":"Y29udHJhY3Rvcg=="},"supervisor":{"keyType":"AVMString","valueType":"address","key":"c3VwZXJ2aXNvcg=="},"worker":{"keyType":"AVMString","valueType":"address","key":"d29ya2Vy"},"milestone_amount":{"keyType":"AVMString","valueType":"AVMUint64","key":"bWlsZXN0b25lX2Ftb3VudA=="},"milestone_name":{"keyType":"AVMString","valueType":"AVMBytes","key":"bWlsZXN0b25lX25hbWU="},"milestone_paid":{"keyType":"AVMString","valueType":"AVMUint64","key":"bWlsZXN0b25lX3BhaWQ="},"escrow_funded":{"keyType":"AVMString","valueType":"AVMUint64","key":"ZXNjcm93X2Z1bmRlZA=="},"credential_asset_id":{"keyType":"AVMString","valueType":"AVMUint64","key":"Y3JlZGVudGlhbF9hc3NldF9pZA=="},"credential_claimed":{"keyType":"AVMString","valueType":"AVMUint64","key":"Y3JlZGVudGlhbF9jbGFpbWVk"}},"local":{},"box":{}},"maps":{"global":{},"local":{},"box":{}}},"bareActions":{"create":["NoOp"],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[539],"errorMessage":"Credential already claimed"},{"pc":[374],"errorMessage":"Escrow already funded"},{"pc":[364],"errorMessage":"Escrow amount must equal milestone amount"},{"pc":[421],"errorMessage":"Escrow not funded"},{"pc":[356],"errorMessage":"Escrow receiver must be the contract"},{"pc":[348],"errorMessage":"Escrow sender mismatch"},{"pc":[427],"errorMessage":"Milestone already paid"},{"pc":[367],"errorMessage":"Milestone amount must be > 0"},{"pc":[532],"errorMessage":"Milestone not approved yet"},{"pc":[544],"errorMessage":"Missing credential asset id"},{"pc":[220,238,268],"errorMessage":"OnCompletion is not NoOp"},{"pc":[340],"errorMessage":"Sender must be contractor"},{"pc":[436],"errorMessage":"Sender must be supervisor"},{"pc":[552],"errorMessage":"Sender must be worker"},{"pc":[329],"errorMessage":"can only call when creating"},{"pc":[223,241,271],"errorMessage":"can only call when not creating"},{"pc":[526,543,557,588],"errorMessage":"check self.credential_asset_id exists"},{"pc":[537],"errorMessage":"check self.credential_claimed exists"},{"pc":[372,420],"errorMessage":"check self.escrow_funded exists"},{"pc":[411,501],"errorMessage":"check self.milestone_amount exists"},{"pc":[442],"errorMessage":"check self.milestone_name exists"},{"pc":[425,531],"errorMessage":"check self.milestone_paid exists"},{"pc":[434],"errorMessage":"check self.supervisor exists"},{"pc":[497,550,563],"errorMessage":"check self.worker exists"},{"pc":[309],"errorMessage":"transaction type is pay"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"events":[]} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


/**
 * The argument types for the WorkProof contract
 */
export type WorkProofArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'create_work_contract(account,account,account,uint64,byte[],pay)uint64': {
      contractor: Uint8Array | string
      supervisor: Uint8Array | string
      worker: Uint8Array | string
      milestoneAmount: bigint | number
      milestoneName: Uint8Array
      payTxn: AppMethodCallTransactionArgument
    }
    'approve_milestone(byte[],byte[])uint64': {
      metadataUrl: Uint8Array
      metadataHash: Uint8Array
    }
    'claim_credential()uint64': Record<string, never>
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'create_work_contract(account,account,account,uint64,byte[],pay)uint64': [contractor: Uint8Array | string, supervisor: Uint8Array | string, worker: Uint8Array | string, milestoneAmount: bigint | number, milestoneName: Uint8Array, payTxn: AppMethodCallTransactionArgument]
    'approve_milestone(byte[],byte[])uint64': [metadataUrl: Uint8Array, metadataHash: Uint8Array]
    'claim_credential()uint64': []
  }
}

/**
 * The return type for each method
 */
export type WorkProofReturns = {
  'create_work_contract(account,account,account,uint64,byte[],pay)uint64': bigint
  'approve_milestone(byte[],byte[])uint64': bigint
  'claim_credential()uint64': bigint
}

/**
 * Defines the types of available calls and state of the WorkProof smart contract.
 */
export type WorkProofTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'create_work_contract(account,account,account,uint64,byte[],pay)uint64' | 'create_work_contract', {
      argsObj: WorkProofArgs['obj']['create_work_contract(account,account,account,uint64,byte[],pay)uint64']
      argsTuple: WorkProofArgs['tuple']['create_work_contract(account,account,account,uint64,byte[],pay)uint64']
      returns: WorkProofReturns['create_work_contract(account,account,account,uint64,byte[],pay)uint64']
    }>
    & Record<'approve_milestone(byte[],byte[])uint64' | 'approve_milestone', {
      argsObj: WorkProofArgs['obj']['approve_milestone(byte[],byte[])uint64']
      argsTuple: WorkProofArgs['tuple']['approve_milestone(byte[],byte[])uint64']
      returns: WorkProofReturns['approve_milestone(byte[],byte[])uint64']
    }>
    & Record<'claim_credential()uint64' | 'claim_credential', {
      argsObj: WorkProofArgs['obj']['claim_credential()uint64']
      argsTuple: WorkProofArgs['tuple']['claim_credential()uint64']
      returns: WorkProofReturns['claim_credential()uint64']
    }>
  /**
   * Defines the shape of the state of the application.
   */
  state: {
    global: {
      keys: {
        contractor: string
        supervisor: string
        worker: string
        milestoneAmount: bigint
        milestoneName: BinaryState
        milestonePaid: bigint
        escrowFunded: bigint
        credentialAssetId: bigint
        credentialClaimed: bigint
      }
      maps: {}
    }
  }
}

/**
 * Defines the possible abi call signatures.
 */
export type WorkProofSignatures = keyof WorkProofTypes['methods']
/**
 * Defines the possible abi call signatures for methods that return a non-void value.
 */
export type WorkProofNonVoidMethodSignatures = keyof WorkProofTypes['methods'] extends infer T ? T extends keyof WorkProofTypes['methods'] ? MethodReturn<T> extends void ? never : T  : never : never
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the WorkProof smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends WorkProofSignatures> = WorkProofTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the WorkProof smart contract to the method's return type
 */
export type MethodReturn<TSignature extends WorkProofSignatures> = WorkProofTypes['methods'][TSignature]['returns']

/**
 * Defines the shape of the keyed global state of the application.
 */
export type GlobalKeysState = WorkProofTypes['state']['global']['keys']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the WorkProof smart contract
 */
export abstract class WorkProofParamsFactory {
  /**
   * Constructs a no op call for the create_work_contract(account,account,account,uint64,byte[],pay)uint64 ABI method
   *
  * Creates a single-milestone WorkProof agreement.
  This method expects a grouped payment from `contractor` to the app address as escrow.

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static createWorkContract(params: CallParams<WorkProofArgs['obj']['create_work_contract(account,account,account,uint64,byte[],pay)uint64'] | WorkProofArgs['tuple']['create_work_contract(account,account,account,uint64,byte[],pay)uint64']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'create_work_contract(account,account,account,uint64,byte[],pay)uint64' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.contractor, params.args.supervisor, params.args.worker, params.args.milestoneAmount, params.args.milestoneName, params.args.payTxn],
    }
  }
  /**
   * Constructs a no op call for the approve_milestone(byte[],byte[])uint64 ABI method
   *
  * Supervisor approves the milestone.
  Releases ALGO from the app escrow to `worker` and mints/transfers a 1-of-1 credential ASA.

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static approveMilestone(params: CallParams<WorkProofArgs['obj']['approve_milestone(byte[],byte[])uint64'] | WorkProofArgs['tuple']['approve_milestone(byte[],byte[])uint64']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'approve_milestone(byte[],byte[])uint64' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.metadataUrl, params.args.metadataHash],
    }
  }
  /**
   * Constructs a no op call for the claim_credential()uint64 ABI method
   *
  * Worker claims the minted credential ASA.
  The worker must be opted in to the ASA before this call.

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static claimCredential(params: CallParams<WorkProofArgs['obj']['claim_credential()uint64'] | WorkProofArgs['tuple']['claim_credential()uint64']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'claim_credential()uint64' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
}

/**
 * A client to make calls to the WorkProof smart contract
 */
export class WorkProofClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `WorkProofClient`
   *
   * @param appClient An `AppClient` instance which has been created with the WorkProof app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `WorkProofClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Checks for decode errors on the given return value and maps the return value to the return type for the given method
   * @returns The typed return value or undefined if there was no value
   */
  decodeReturnValue<TSignature extends WorkProofNonVoidMethodSignatures>(method: TSignature, returnValue: ABIReturn | undefined) {
    return returnValue !== undefined ? getArc56ReturnValue<MethodReturn<TSignature>>(returnValue, this.appClient.getABIMethod(method), APP_SPEC.structs) : undefined
  }

  /**
   * Returns a new `WorkProofClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<WorkProofClient> {
    return new WorkProofClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `WorkProofClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<WorkProofClient> {
    return new WorkProofClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Makes a clear_state call to an existing instance of the WorkProof smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the WorkProof smart contract using the `create_work_contract(account,account,account,uint64,byte[],pay)uint64` ABI method.
     *
    * Creates a single-milestone WorkProof agreement.
    This method expects a grouped payment from `contractor` to the app address as escrow.

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    createWorkContract: (params: CallParams<WorkProofArgs['obj']['create_work_contract(account,account,account,uint64,byte[],pay)uint64'] | WorkProofArgs['tuple']['create_work_contract(account,account,account,uint64,byte[],pay)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(WorkProofParamsFactory.createWorkContract(params))
    },

    /**
     * Makes a call to the WorkProof smart contract using the `approve_milestone(byte[],byte[])uint64` ABI method.
     *
    * Supervisor approves the milestone.
    Releases ALGO from the app escrow to `worker` and mints/transfers a 1-of-1 credential ASA.

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    approveMilestone: (params: CallParams<WorkProofArgs['obj']['approve_milestone(byte[],byte[])uint64'] | WorkProofArgs['tuple']['approve_milestone(byte[],byte[])uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(WorkProofParamsFactory.approveMilestone(params))
    },

    /**
     * Makes a call to the WorkProof smart contract using the `claim_credential()uint64` ABI method.
     *
    * Worker claims the minted credential ASA.
    The worker must be opted in to the ASA before this call.

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    claimCredential: (params: CallParams<WorkProofArgs['obj']['claim_credential()uint64'] | WorkProofArgs['tuple']['claim_credential()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(WorkProofParamsFactory.claimCredential(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Makes a clear_state call to an existing instance of the WorkProof smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the WorkProof smart contract using the `create_work_contract(account,account,account,uint64,byte[],pay)uint64` ABI method.
     *
    * Creates a single-milestone WorkProof agreement.
    This method expects a grouped payment from `contractor` to the app address as escrow.

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    createWorkContract: (params: CallParams<WorkProofArgs['obj']['create_work_contract(account,account,account,uint64,byte[],pay)uint64'] | WorkProofArgs['tuple']['create_work_contract(account,account,account,uint64,byte[],pay)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(WorkProofParamsFactory.createWorkContract(params))
    },

    /**
     * Makes a call to the WorkProof smart contract using the `approve_milestone(byte[],byte[])uint64` ABI method.
     *
    * Supervisor approves the milestone.
    Releases ALGO from the app escrow to `worker` and mints/transfers a 1-of-1 credential ASA.

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    approveMilestone: (params: CallParams<WorkProofArgs['obj']['approve_milestone(byte[],byte[])uint64'] | WorkProofArgs['tuple']['approve_milestone(byte[],byte[])uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(WorkProofParamsFactory.approveMilestone(params))
    },

    /**
     * Makes a call to the WorkProof smart contract using the `claim_credential()uint64` ABI method.
     *
    * Worker claims the minted credential ASA.
    The worker must be opted in to the ASA before this call.

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    claimCredential: (params: CallParams<WorkProofArgs['obj']['claim_credential()uint64'] | WorkProofArgs['tuple']['claim_credential()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(WorkProofParamsFactory.claimCredential(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Makes a clear_state call to an existing instance of the WorkProof smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the WorkProof smart contract using the `create_work_contract(account,account,account,uint64,byte[],pay)uint64` ABI method.
     *
    * Creates a single-milestone WorkProof agreement.
    This method expects a grouped payment from `contractor` to the app address as escrow.

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    createWorkContract: async (params: CallParams<WorkProofArgs['obj']['create_work_contract(account,account,account,uint64,byte[],pay)uint64'] | WorkProofArgs['tuple']['create_work_contract(account,account,account,uint64,byte[],pay)uint64']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(WorkProofParamsFactory.createWorkContract(params))
      return {...result, return: result.return as unknown as (undefined | WorkProofReturns['create_work_contract(account,account,account,uint64,byte[],pay)uint64'])}
    },

    /**
     * Makes a call to the WorkProof smart contract using the `approve_milestone(byte[],byte[])uint64` ABI method.
     *
    * Supervisor approves the milestone.
    Releases ALGO from the app escrow to `worker` and mints/transfers a 1-of-1 credential ASA.

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    approveMilestone: async (params: CallParams<WorkProofArgs['obj']['approve_milestone(byte[],byte[])uint64'] | WorkProofArgs['tuple']['approve_milestone(byte[],byte[])uint64']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(WorkProofParamsFactory.approveMilestone(params))
      return {...result, return: result.return as unknown as (undefined | WorkProofReturns['approve_milestone(byte[],byte[])uint64'])}
    },

    /**
     * Makes a call to the WorkProof smart contract using the `claim_credential()uint64` ABI method.
     *
    * Worker claims the minted credential ASA.
    The worker must be opted in to the ASA before this call.

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    claimCredential: async (params: CallParams<WorkProofArgs['obj']['claim_credential()uint64'] | WorkProofArgs['tuple']['claim_credential()uint64']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(WorkProofParamsFactory.claimCredential(params))
      return {...result, return: result.return as unknown as (undefined | WorkProofReturns['claim_credential()uint64'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new WorkProofClient(this.appClient.clone(params))
  }

  /**
   * Methods to access state for the current WorkProof app
   */
  state = {
    /**
     * Methods to access global state for the current WorkProof app
     */
    global: {
      /**
       * Get all current keyed values from global state
       */
      getAll: async (): Promise<Partial<Expand<GlobalKeysState>>> => {
        const result = await this.appClient.state.global.getAll()
        return {
          contractor: result.contractor,
          supervisor: result.supervisor,
          worker: result.worker,
          milestoneAmount: result.milestone_amount,
          milestoneName: new BinaryStateValue(result.milestone_name),
          milestonePaid: result.milestone_paid,
          escrowFunded: result.escrow_funded,
          credentialAssetId: result.credential_asset_id,
          credentialClaimed: result.credential_claimed,
        }
      },
      /**
       * Get the current value of the contractor key in global state
       */
      contractor: async (): Promise<string | undefined> => { return (await this.appClient.state.global.getValue("contractor")) as string | undefined },
      /**
       * Get the current value of the supervisor key in global state
       */
      supervisor: async (): Promise<string | undefined> => { return (await this.appClient.state.global.getValue("supervisor")) as string | undefined },
      /**
       * Get the current value of the worker key in global state
       */
      worker: async (): Promise<string | undefined> => { return (await this.appClient.state.global.getValue("worker")) as string | undefined },
      /**
       * Get the current value of the milestone_amount key in global state
       */
      milestoneAmount: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("milestone_amount")) as bigint | undefined },
      /**
       * Get the current value of the milestone_name key in global state
       */
      milestoneName: async (): Promise<BinaryState> => { return new BinaryStateValue((await this.appClient.state.global.getValue("milestone_name")) as Uint8Array | undefined) },
      /**
       * Get the current value of the milestone_paid key in global state
       */
      milestonePaid: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("milestone_paid")) as bigint | undefined },
      /**
       * Get the current value of the escrow_funded key in global state
       */
      escrowFunded: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("escrow_funded")) as bigint | undefined },
      /**
       * Get the current value of the credential_asset_id key in global state
       */
      credentialAssetId: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("credential_asset_id")) as bigint | undefined },
      /**
       * Get the current value of the credential_claimed key in global state
       */
      credentialClaimed: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("credential_claimed")) as bigint | undefined },
    },
  }

  public newGroup(): WorkProofComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a create_work_contract(account,account,account,uint64,byte[],pay)uint64 method call against the WorkProof contract
       */
      createWorkContract(params: CallParams<WorkProofArgs['obj']['create_work_contract(account,account,account,uint64,byte[],pay)uint64'] | WorkProofArgs['tuple']['create_work_contract(account,account,account,uint64,byte[],pay)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.createWorkContract(params)))
        resultMappers.push((v) => client.decodeReturnValue('create_work_contract(account,account,account,uint64,byte[],pay)uint64', v))
        return this
      },
      /**
       * Add a approve_milestone(byte[],byte[])uint64 method call against the WorkProof contract
       */
      approveMilestone(params: CallParams<WorkProofArgs['obj']['approve_milestone(byte[],byte[])uint64'] | WorkProofArgs['tuple']['approve_milestone(byte[],byte[])uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.approveMilestone(params)))
        resultMappers.push((v) => client.decodeReturnValue('approve_milestone(byte[],byte[])uint64', v))
        return this
      },
      /**
       * Add a claim_credential()uint64 method call against the WorkProof contract
       */
      claimCredential(params: CallParams<WorkProofArgs['obj']['claim_credential()uint64'] | WorkProofArgs['tuple']['claim_credential()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.claimCredential(params)))
        resultMappers.push((v) => client.decodeReturnValue('claim_credential()uint64', v))
        return this
      },
      /**
       * Add a clear state call to the WorkProof contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as WorkProofComposer
  }
}
export type WorkProofComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the create_work_contract(account,account,account,uint64,byte[],pay)uint64 ABI method.
   *
  * Creates a single-milestone WorkProof agreement.
  This method expects a grouped payment from `contractor` to the app address as escrow.

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  createWorkContract(params?: CallParams<WorkProofArgs['obj']['create_work_contract(account,account,account,uint64,byte[],pay)uint64'] | WorkProofArgs['tuple']['create_work_contract(account,account,account,uint64,byte[],pay)uint64']>): WorkProofComposer<[...TReturns, WorkProofReturns['create_work_contract(account,account,account,uint64,byte[],pay)uint64'] | undefined]>

  /**
   * Calls the approve_milestone(byte[],byte[])uint64 ABI method.
   *
  * Supervisor approves the milestone.
  Releases ALGO from the app escrow to `worker` and mints/transfers a 1-of-1 credential ASA.

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  approveMilestone(params?: CallParams<WorkProofArgs['obj']['approve_milestone(byte[],byte[])uint64'] | WorkProofArgs['tuple']['approve_milestone(byte[],byte[])uint64']>): WorkProofComposer<[...TReturns, WorkProofReturns['approve_milestone(byte[],byte[])uint64'] | undefined]>

  /**
   * Calls the claim_credential()uint64 ABI method.
   *
  * Worker claims the minted credential ASA.
  The worker must be opted in to the ASA before this call.

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  claimCredential(params?: CallParams<WorkProofArgs['obj']['claim_credential()uint64'] | WorkProofArgs['tuple']['claim_credential()uint64']>): WorkProofComposer<[...TReturns, WorkProofReturns['claim_credential()uint64'] | undefined]>

  /**
   * Makes a clear_state call to an existing instance of the WorkProof smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): WorkProofComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): WorkProofComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<WorkProofComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<WorkProofComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<WorkProofComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<WorkProofComposerResults<TReturns>>
}
export type WorkProofComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

