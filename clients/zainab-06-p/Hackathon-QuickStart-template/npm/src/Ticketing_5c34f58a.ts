/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"Ticketing","structs":{},"methods":[{"name":"create_event","args":[{"type":"uint64","name":"price"},{"type":"uint64","name":"supply"},{"type":"uint64","name":"event_date"},{"type":"uint64","name":"sale_end_date"}],"returns":{"type":"uint64"},"actions":{"create":["NoOp"],"call":[]},"readonly":false,"desc":"Create a new ticketed event (called during contract creation)","events":[],"recommendations":{}},{"name":"buy_ticket","args":[{"type":"pay","name":"payment"}],"returns":{"type":"uint64"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Purchase an event ticket - Mints NFT to buyer\nReturns the NFT Asset ID","events":[],"recommendations":{}},{"name":"verify_entry","args":[{"type":"account","name":"ticket_holder"},{"type":"uint64","name":"ticket_asset_id"}],"returns":{"type":"bool"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Verify ticket at event entry (called by organizer/staff)\nChecks NFT ownership and check-in status","events":[],"recommendations":{}},{"name":"is_checked_in","args":[{"type":"uint64","name":"ticket_asset_id"}],"returns":{"type":"bool"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"desc":"Check if a ticket has been used for entry","events":[],"recommendations":{}},{"name":"toggle_sale","args":[],"returns":{"type":"bool"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Toggle ticket sales on/off (organizer only)","events":[],"recommendations":{}},{"name":"get_event_info","args":[],"returns":{"type":"(uint64,uint64,uint64,uint64,uint64,uint64,bool)"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Get event information","events":[],"recommendations":{}},{"name":"get_organizer","args":[],"returns":{"type":"address"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"desc":"Get event organizer address","events":[],"recommendations":{}}],"arcs":[22,28],"desc":"\n    CampusChain Ticketing Contract\n    NFT-based event tickets with anti-scalping and entry verification\n    Each deployment = one event (fully decentralized)\n    Features:\n    - Mints NFT ticket on purchase\n    - QR code verification at entry\n    - Prevents double entry\n    ","networks":{},"state":{"schema":{"global":{"ints":7,"bytes":1},"local":{"ints":0,"bytes":0}},"keys":{"global":{"ticket_price":{"keyType":"AVMString","valueType":"AVMUint64","key":"dGlja2V0X3ByaWNl"},"max_supply":{"keyType":"AVMString","valueType":"AVMUint64","key":"bWF4X3N1cHBseQ=="},"sold_count":{"keyType":"AVMString","valueType":"AVMUint64","key":"c29sZF9jb3VudA=="},"event_date":{"keyType":"AVMString","valueType":"AVMUint64","key":"ZXZlbnRfZGF0ZQ=="},"sale_end_date":{"keyType":"AVMString","valueType":"AVMUint64","key":"c2FsZV9lbmRfZGF0ZQ=="},"is_sale_active":{"keyType":"AVMString","valueType":"AVMUint64","key":"aXNfc2FsZV9hY3RpdmU="},"organizer":{"keyType":"AVMString","valueType":"address","key":"b3JnYW5pemVy"},"unique_buyers":{"keyType":"AVMString","valueType":"AVMUint64","key":"dW5pcXVlX2J1eWVycw=="}},"local":{},"box":{}},"maps":{"global":{},"local":{},"box":{}}},"bareActions":{"create":[],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[464],"errorMessage":"Event has passed"},{"pc":[638],"errorMessage":"Event verification period ended"},{"pc":[483],"errorMessage":"Insufficient payment"},{"pc":[662],"errorMessage":"Invalid ticket"},{"pc":[179,195,253,274,299,330,357],"errorMessage":"OnCompletion is not NoOp"},{"pc":[706],"errorMessage":"Only organizer can toggle"},{"pc":[624],"errorMessage":"Only organizer can verify"},{"pc":[435],"errorMessage":"Sale not active"},{"pc":[446],"errorMessage":"Sold out"},{"pc":[666],"errorMessage":"Ticket already used"},{"pc":[656],"errorMessage":"Ticket not owned by holder"},{"pc":[455],"errorMessage":"Ticket sales have ended"},{"pc":[361],"errorMessage":"can only call when creating"},{"pc":[182,198,256,277,302,333],"errorMessage":"can only call when not creating"},{"pc":[462,631,738],"errorMessage":"check self.event_date exists"},{"pc":[434,710,718,752],"errorMessage":"check self.is_sale_active exists"},{"pc":[444,729],"errorMessage":"check self.max_supply exists"},{"pc":[622,704,758],"errorMessage":"check self.organizer exists"},{"pc":[453,743],"errorMessage":"check self.sale_end_date exists"},{"pc":[439,487,733],"errorMessage":"check self.sold_count exists"},{"pc":[481,724],"errorMessage":"check self.ticket_price exists"},{"pc":[497,748],"errorMessage":"check self.unique_buyers exists"},{"pc":[343],"errorMessage":"transaction type is pay"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"events":[]} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


/**
 * The argument types for the Ticketing contract
 */
export type TicketingArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'create_event(uint64,uint64,uint64,uint64)uint64': {
      price: bigint | number
      supply: bigint | number
      eventDate: bigint | number
      saleEndDate: bigint | number
    }
    'buy_ticket(pay)uint64': {
      payment: AppMethodCallTransactionArgument
    }
    'verify_entry(account,uint64)bool': {
      ticketHolder: Uint8Array | string
      ticketAssetId: bigint | number
    }
    'is_checked_in(uint64)bool': {
      ticketAssetId: bigint | number
    }
    'toggle_sale()bool': Record<string, never>
    'get_event_info()(uint64,uint64,uint64,uint64,uint64,uint64,bool)': Record<string, never>
    'get_organizer()address': Record<string, never>
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'create_event(uint64,uint64,uint64,uint64)uint64': [price: bigint | number, supply: bigint | number, eventDate: bigint | number, saleEndDate: bigint | number]
    'buy_ticket(pay)uint64': [payment: AppMethodCallTransactionArgument]
    'verify_entry(account,uint64)bool': [ticketHolder: Uint8Array | string, ticketAssetId: bigint | number]
    'is_checked_in(uint64)bool': [ticketAssetId: bigint | number]
    'toggle_sale()bool': []
    'get_event_info()(uint64,uint64,uint64,uint64,uint64,uint64,bool)': []
    'get_organizer()address': []
  }
}

/**
 * The return type for each method
 */
export type TicketingReturns = {
  'create_event(uint64,uint64,uint64,uint64)uint64': bigint
  'buy_ticket(pay)uint64': bigint
  'verify_entry(account,uint64)bool': boolean
  'is_checked_in(uint64)bool': boolean
  'toggle_sale()bool': boolean
  'get_event_info()(uint64,uint64,uint64,uint64,uint64,uint64,bool)': [bigint, bigint, bigint, bigint, bigint, bigint, boolean]
  'get_organizer()address': string
}

/**
 * Defines the types of available calls and state of the Ticketing smart contract.
 */
export type TicketingTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'create_event(uint64,uint64,uint64,uint64)uint64' | 'create_event', {
      argsObj: TicketingArgs['obj']['create_event(uint64,uint64,uint64,uint64)uint64']
      argsTuple: TicketingArgs['tuple']['create_event(uint64,uint64,uint64,uint64)uint64']
      returns: TicketingReturns['create_event(uint64,uint64,uint64,uint64)uint64']
    }>
    & Record<'buy_ticket(pay)uint64' | 'buy_ticket', {
      argsObj: TicketingArgs['obj']['buy_ticket(pay)uint64']
      argsTuple: TicketingArgs['tuple']['buy_ticket(pay)uint64']
      returns: TicketingReturns['buy_ticket(pay)uint64']
    }>
    & Record<'verify_entry(account,uint64)bool' | 'verify_entry', {
      argsObj: TicketingArgs['obj']['verify_entry(account,uint64)bool']
      argsTuple: TicketingArgs['tuple']['verify_entry(account,uint64)bool']
      returns: TicketingReturns['verify_entry(account,uint64)bool']
    }>
    & Record<'is_checked_in(uint64)bool' | 'is_checked_in', {
      argsObj: TicketingArgs['obj']['is_checked_in(uint64)bool']
      argsTuple: TicketingArgs['tuple']['is_checked_in(uint64)bool']
      returns: TicketingReturns['is_checked_in(uint64)bool']
    }>
    & Record<'toggle_sale()bool' | 'toggle_sale', {
      argsObj: TicketingArgs['obj']['toggle_sale()bool']
      argsTuple: TicketingArgs['tuple']['toggle_sale()bool']
      returns: TicketingReturns['toggle_sale()bool']
    }>
    & Record<'get_event_info()(uint64,uint64,uint64,uint64,uint64,uint64,bool)' | 'get_event_info', {
      argsObj: TicketingArgs['obj']['get_event_info()(uint64,uint64,uint64,uint64,uint64,uint64,bool)']
      argsTuple: TicketingArgs['tuple']['get_event_info()(uint64,uint64,uint64,uint64,uint64,uint64,bool)']
      returns: TicketingReturns['get_event_info()(uint64,uint64,uint64,uint64,uint64,uint64,bool)']
    }>
    & Record<'get_organizer()address' | 'get_organizer', {
      argsObj: TicketingArgs['obj']['get_organizer()address']
      argsTuple: TicketingArgs['tuple']['get_organizer()address']
      returns: TicketingReturns['get_organizer()address']
    }>
  /**
   * Defines the shape of the state of the application.
   */
  state: {
    global: {
      keys: {
        ticketPrice: bigint
        maxSupply: bigint
        soldCount: bigint
        eventDate: bigint
        saleEndDate: bigint
        isSaleActive: bigint
        organizer: string
        uniqueBuyers: bigint
      }
      maps: {}
    }
  }
}

/**
 * Defines the possible abi call signatures.
 */
export type TicketingSignatures = keyof TicketingTypes['methods']
/**
 * Defines the possible abi call signatures for methods that return a non-void value.
 */
export type TicketingNonVoidMethodSignatures = keyof TicketingTypes['methods'] extends infer T ? T extends keyof TicketingTypes['methods'] ? MethodReturn<T> extends void ? never : T  : never : never
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the Ticketing smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends TicketingSignatures> = TicketingTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the Ticketing smart contract to the method's return type
 */
export type MethodReturn<TSignature extends TicketingSignatures> = TicketingTypes['methods'][TSignature]['returns']

/**
 * Defines the shape of the keyed global state of the application.
 */
export type GlobalKeysState = TicketingTypes['state']['global']['keys']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the Ticketing smart contract
 */
export abstract class TicketingParamsFactory {
  /**
   * Constructs a no op call for the buy_ticket(pay)uint64 ABI method
   *
  * Purchase an event ticket - Mints NFT to buyer
  Returns the NFT Asset ID

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static buyTicket(params: CallParams<TicketingArgs['obj']['buy_ticket(pay)uint64'] | TicketingArgs['tuple']['buy_ticket(pay)uint64']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'buy_ticket(pay)uint64' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.payment],
    }
  }
  /**
   * Constructs a no op call for the verify_entry(account,uint64)bool ABI method
   *
  * Verify ticket at event entry (called by organizer/staff)
  Checks NFT ownership and check-in status

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static verifyEntry(params: CallParams<TicketingArgs['obj']['verify_entry(account,uint64)bool'] | TicketingArgs['tuple']['verify_entry(account,uint64)bool']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'verify_entry(account,uint64)bool' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.ticketHolder, params.args.ticketAssetId],
    }
  }
  /**
   * Constructs a no op call for the is_checked_in(uint64)bool ABI method
   *
   * Check if a ticket has been used for entry
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static isCheckedIn(params: CallParams<TicketingArgs['obj']['is_checked_in(uint64)bool'] | TicketingArgs['tuple']['is_checked_in(uint64)bool']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'is_checked_in(uint64)bool' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.ticketAssetId],
    }
  }
  /**
   * Constructs a no op call for the toggle_sale()bool ABI method
   *
   * Toggle ticket sales on/off (organizer only)
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static toggleSale(params: CallParams<TicketingArgs['obj']['toggle_sale()bool'] | TicketingArgs['tuple']['toggle_sale()bool']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'toggle_sale()bool' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the get_event_info()(uint64,uint64,uint64,uint64,uint64,uint64,bool) ABI method
   *
   * Get event information
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static getEventInfo(params: CallParams<TicketingArgs['obj']['get_event_info()(uint64,uint64,uint64,uint64,uint64,uint64,bool)'] | TicketingArgs['tuple']['get_event_info()(uint64,uint64,uint64,uint64,uint64,uint64,bool)']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'get_event_info()(uint64,uint64,uint64,uint64,uint64,uint64,bool)' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the get_organizer()address ABI method
   *
   * Get event organizer address
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static getOrganizer(params: CallParams<TicketingArgs['obj']['get_organizer()address'] | TicketingArgs['tuple']['get_organizer()address']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'get_organizer()address' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
}

/**
 * A client to make calls to the Ticketing smart contract
 */
export class TicketingClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `TicketingClient`
   *
   * @param appClient An `AppClient` instance which has been created with the Ticketing app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `TicketingClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Checks for decode errors on the given return value and maps the return value to the return type for the given method
   * @returns The typed return value or undefined if there was no value
   */
  decodeReturnValue<TSignature extends TicketingNonVoidMethodSignatures>(method: TSignature, returnValue: ABIReturn | undefined) {
    return returnValue !== undefined ? getArc56ReturnValue<MethodReturn<TSignature>>(returnValue, this.appClient.getABIMethod(method), APP_SPEC.structs) : undefined
  }

  /**
   * Returns a new `TicketingClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<TicketingClient> {
    return new TicketingClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `TicketingClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<TicketingClient> {
    return new TicketingClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Makes a clear_state call to an existing instance of the Ticketing smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the Ticketing smart contract using the `buy_ticket(pay)uint64` ABI method.
     *
    * Purchase an event ticket - Mints NFT to buyer
    Returns the NFT Asset ID

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    buyTicket: (params: CallParams<TicketingArgs['obj']['buy_ticket(pay)uint64'] | TicketingArgs['tuple']['buy_ticket(pay)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(TicketingParamsFactory.buyTicket(params))
    },

    /**
     * Makes a call to the Ticketing smart contract using the `verify_entry(account,uint64)bool` ABI method.
     *
    * Verify ticket at event entry (called by organizer/staff)
    Checks NFT ownership and check-in status

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    verifyEntry: (params: CallParams<TicketingArgs['obj']['verify_entry(account,uint64)bool'] | TicketingArgs['tuple']['verify_entry(account,uint64)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(TicketingParamsFactory.verifyEntry(params))
    },

    /**
     * Makes a call to the Ticketing smart contract using the `is_checked_in(uint64)bool` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * Check if a ticket has been used for entry
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    isCheckedIn: (params: CallParams<TicketingArgs['obj']['is_checked_in(uint64)bool'] | TicketingArgs['tuple']['is_checked_in(uint64)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(TicketingParamsFactory.isCheckedIn(params))
    },

    /**
     * Makes a call to the Ticketing smart contract using the `toggle_sale()bool` ABI method.
     *
     * Toggle ticket sales on/off (organizer only)
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    toggleSale: (params: CallParams<TicketingArgs['obj']['toggle_sale()bool'] | TicketingArgs['tuple']['toggle_sale()bool']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(TicketingParamsFactory.toggleSale(params))
    },

    /**
     * Makes a call to the Ticketing smart contract using the `get_event_info()(uint64,uint64,uint64,uint64,uint64,uint64,bool)` ABI method.
     *
     * Get event information
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    getEventInfo: (params: CallParams<TicketingArgs['obj']['get_event_info()(uint64,uint64,uint64,uint64,uint64,uint64,bool)'] | TicketingArgs['tuple']['get_event_info()(uint64,uint64,uint64,uint64,uint64,uint64,bool)']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(TicketingParamsFactory.getEventInfo(params))
    },

    /**
     * Makes a call to the Ticketing smart contract using the `get_organizer()address` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * Get event organizer address
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    getOrganizer: (params: CallParams<TicketingArgs['obj']['get_organizer()address'] | TicketingArgs['tuple']['get_organizer()address']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(TicketingParamsFactory.getOrganizer(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Makes a clear_state call to an existing instance of the Ticketing smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the Ticketing smart contract using the `buy_ticket(pay)uint64` ABI method.
     *
    * Purchase an event ticket - Mints NFT to buyer
    Returns the NFT Asset ID

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    buyTicket: (params: CallParams<TicketingArgs['obj']['buy_ticket(pay)uint64'] | TicketingArgs['tuple']['buy_ticket(pay)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(TicketingParamsFactory.buyTicket(params))
    },

    /**
     * Makes a call to the Ticketing smart contract using the `verify_entry(account,uint64)bool` ABI method.
     *
    * Verify ticket at event entry (called by organizer/staff)
    Checks NFT ownership and check-in status

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    verifyEntry: (params: CallParams<TicketingArgs['obj']['verify_entry(account,uint64)bool'] | TicketingArgs['tuple']['verify_entry(account,uint64)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(TicketingParamsFactory.verifyEntry(params))
    },

    /**
     * Makes a call to the Ticketing smart contract using the `is_checked_in(uint64)bool` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * Check if a ticket has been used for entry
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    isCheckedIn: (params: CallParams<TicketingArgs['obj']['is_checked_in(uint64)bool'] | TicketingArgs['tuple']['is_checked_in(uint64)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(TicketingParamsFactory.isCheckedIn(params))
    },

    /**
     * Makes a call to the Ticketing smart contract using the `toggle_sale()bool` ABI method.
     *
     * Toggle ticket sales on/off (organizer only)
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    toggleSale: (params: CallParams<TicketingArgs['obj']['toggle_sale()bool'] | TicketingArgs['tuple']['toggle_sale()bool']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(TicketingParamsFactory.toggleSale(params))
    },

    /**
     * Makes a call to the Ticketing smart contract using the `get_event_info()(uint64,uint64,uint64,uint64,uint64,uint64,bool)` ABI method.
     *
     * Get event information
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    getEventInfo: (params: CallParams<TicketingArgs['obj']['get_event_info()(uint64,uint64,uint64,uint64,uint64,uint64,bool)'] | TicketingArgs['tuple']['get_event_info()(uint64,uint64,uint64,uint64,uint64,uint64,bool)']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(TicketingParamsFactory.getEventInfo(params))
    },

    /**
     * Makes a call to the Ticketing smart contract using the `get_organizer()address` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * Get event organizer address
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    getOrganizer: (params: CallParams<TicketingArgs['obj']['get_organizer()address'] | TicketingArgs['tuple']['get_organizer()address']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(TicketingParamsFactory.getOrganizer(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Makes a clear_state call to an existing instance of the Ticketing smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the Ticketing smart contract using the `buy_ticket(pay)uint64` ABI method.
     *
    * Purchase an event ticket - Mints NFT to buyer
    Returns the NFT Asset ID

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    buyTicket: async (params: CallParams<TicketingArgs['obj']['buy_ticket(pay)uint64'] | TicketingArgs['tuple']['buy_ticket(pay)uint64']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(TicketingParamsFactory.buyTicket(params))
      return {...result, return: result.return as unknown as (undefined | TicketingReturns['buy_ticket(pay)uint64'])}
    },

    /**
     * Makes a call to the Ticketing smart contract using the `verify_entry(account,uint64)bool` ABI method.
     *
    * Verify ticket at event entry (called by organizer/staff)
    Checks NFT ownership and check-in status

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    verifyEntry: async (params: CallParams<TicketingArgs['obj']['verify_entry(account,uint64)bool'] | TicketingArgs['tuple']['verify_entry(account,uint64)bool']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(TicketingParamsFactory.verifyEntry(params))
      return {...result, return: result.return as unknown as (undefined | TicketingReturns['verify_entry(account,uint64)bool'])}
    },

    /**
     * Makes a call to the Ticketing smart contract using the `is_checked_in(uint64)bool` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * Check if a ticket has been used for entry
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    isCheckedIn: async (params: CallParams<TicketingArgs['obj']['is_checked_in(uint64)bool'] | TicketingArgs['tuple']['is_checked_in(uint64)bool']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(TicketingParamsFactory.isCheckedIn(params))
      return {...result, return: result.return as unknown as (undefined | TicketingReturns['is_checked_in(uint64)bool'])}
    },

    /**
     * Makes a call to the Ticketing smart contract using the `toggle_sale()bool` ABI method.
     *
     * Toggle ticket sales on/off (organizer only)
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    toggleSale: async (params: CallParams<TicketingArgs['obj']['toggle_sale()bool'] | TicketingArgs['tuple']['toggle_sale()bool']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(TicketingParamsFactory.toggleSale(params))
      return {...result, return: result.return as unknown as (undefined | TicketingReturns['toggle_sale()bool'])}
    },

    /**
     * Makes a call to the Ticketing smart contract using the `get_event_info()(uint64,uint64,uint64,uint64,uint64,uint64,bool)` ABI method.
     *
     * Get event information
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    getEventInfo: async (params: CallParams<TicketingArgs['obj']['get_event_info()(uint64,uint64,uint64,uint64,uint64,uint64,bool)'] | TicketingArgs['tuple']['get_event_info()(uint64,uint64,uint64,uint64,uint64,uint64,bool)']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(TicketingParamsFactory.getEventInfo(params))
      return {...result, return: result.return as unknown as (undefined | TicketingReturns['get_event_info()(uint64,uint64,uint64,uint64,uint64,uint64,bool)'])}
    },

    /**
     * Makes a call to the Ticketing smart contract using the `get_organizer()address` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * Get event organizer address
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    getOrganizer: async (params: CallParams<TicketingArgs['obj']['get_organizer()address'] | TicketingArgs['tuple']['get_organizer()address']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(TicketingParamsFactory.getOrganizer(params))
      return {...result, return: result.return as unknown as (undefined | TicketingReturns['get_organizer()address'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new TicketingClient(this.appClient.clone(params))
  }

  /**
   * Makes a readonly (simulated) call to the Ticketing smart contract using the `is_checked_in(uint64)bool` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * Check if a ticket has been used for entry
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async isCheckedIn(params: CallParams<TicketingArgs['obj']['is_checked_in(uint64)bool'] | TicketingArgs['tuple']['is_checked_in(uint64)bool']>) {
    const result = await this.appClient.send.call(TicketingParamsFactory.isCheckedIn(params))
    return result.return as unknown as TicketingReturns['is_checked_in(uint64)bool']
  }

  /**
   * Makes a readonly (simulated) call to the Ticketing smart contract using the `get_organizer()address` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * Get event organizer address
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async getOrganizer(params: CallParams<TicketingArgs['obj']['get_organizer()address'] | TicketingArgs['tuple']['get_organizer()address']> = {args: []}) {
    const result = await this.appClient.send.call(TicketingParamsFactory.getOrganizer(params))
    return result.return as unknown as TicketingReturns['get_organizer()address']
  }

  /**
   * Methods to access state for the current Ticketing app
   */
  state = {
    /**
     * Methods to access global state for the current Ticketing app
     */
    global: {
      /**
       * Get all current keyed values from global state
       */
      getAll: async (): Promise<Partial<Expand<GlobalKeysState>>> => {
        const result = await this.appClient.state.global.getAll()
        return {
          ticketPrice: result.ticket_price,
          maxSupply: result.max_supply,
          soldCount: result.sold_count,
          eventDate: result.event_date,
          saleEndDate: result.sale_end_date,
          isSaleActive: result.is_sale_active,
          organizer: result.organizer,
          uniqueBuyers: result.unique_buyers,
        }
      },
      /**
       * Get the current value of the ticket_price key in global state
       */
      ticketPrice: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("ticket_price")) as bigint | undefined },
      /**
       * Get the current value of the max_supply key in global state
       */
      maxSupply: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("max_supply")) as bigint | undefined },
      /**
       * Get the current value of the sold_count key in global state
       */
      soldCount: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("sold_count")) as bigint | undefined },
      /**
       * Get the current value of the event_date key in global state
       */
      eventDate: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("event_date")) as bigint | undefined },
      /**
       * Get the current value of the sale_end_date key in global state
       */
      saleEndDate: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("sale_end_date")) as bigint | undefined },
      /**
       * Get the current value of the is_sale_active key in global state
       */
      isSaleActive: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("is_sale_active")) as bigint | undefined },
      /**
       * Get the current value of the organizer key in global state
       */
      organizer: async (): Promise<string | undefined> => { return (await this.appClient.state.global.getValue("organizer")) as string | undefined },
      /**
       * Get the current value of the unique_buyers key in global state
       */
      uniqueBuyers: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("unique_buyers")) as bigint | undefined },
    },
  }

  public newGroup(): TicketingComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a buy_ticket(pay)uint64 method call against the Ticketing contract
       */
      buyTicket(params: CallParams<TicketingArgs['obj']['buy_ticket(pay)uint64'] | TicketingArgs['tuple']['buy_ticket(pay)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.buyTicket(params)))
        resultMappers.push((v) => client.decodeReturnValue('buy_ticket(pay)uint64', v))
        return this
      },
      /**
       * Add a verify_entry(account,uint64)bool method call against the Ticketing contract
       */
      verifyEntry(params: CallParams<TicketingArgs['obj']['verify_entry(account,uint64)bool'] | TicketingArgs['tuple']['verify_entry(account,uint64)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.verifyEntry(params)))
        resultMappers.push((v) => client.decodeReturnValue('verify_entry(account,uint64)bool', v))
        return this
      },
      /**
       * Add a is_checked_in(uint64)bool method call against the Ticketing contract
       */
      isCheckedIn(params: CallParams<TicketingArgs['obj']['is_checked_in(uint64)bool'] | TicketingArgs['tuple']['is_checked_in(uint64)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.isCheckedIn(params)))
        resultMappers.push((v) => client.decodeReturnValue('is_checked_in(uint64)bool', v))
        return this
      },
      /**
       * Add a toggle_sale()bool method call against the Ticketing contract
       */
      toggleSale(params: CallParams<TicketingArgs['obj']['toggle_sale()bool'] | TicketingArgs['tuple']['toggle_sale()bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.toggleSale(params)))
        resultMappers.push((v) => client.decodeReturnValue('toggle_sale()bool', v))
        return this
      },
      /**
       * Add a get_event_info()(uint64,uint64,uint64,uint64,uint64,uint64,bool) method call against the Ticketing contract
       */
      getEventInfo(params: CallParams<TicketingArgs['obj']['get_event_info()(uint64,uint64,uint64,uint64,uint64,uint64,bool)'] | TicketingArgs['tuple']['get_event_info()(uint64,uint64,uint64,uint64,uint64,uint64,bool)']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.getEventInfo(params)))
        resultMappers.push((v) => client.decodeReturnValue('get_event_info()(uint64,uint64,uint64,uint64,uint64,uint64,bool)', v))
        return this
      },
      /**
       * Add a get_organizer()address method call against the Ticketing contract
       */
      getOrganizer(params: CallParams<TicketingArgs['obj']['get_organizer()address'] | TicketingArgs['tuple']['get_organizer()address']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.getOrganizer(params)))
        resultMappers.push((v) => client.decodeReturnValue('get_organizer()address', v))
        return this
      },
      /**
       * Add a clear state call to the Ticketing contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as TicketingComposer
  }
}
export type TicketingComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the buy_ticket(pay)uint64 ABI method.
   *
  * Purchase an event ticket - Mints NFT to buyer
  Returns the NFT Asset ID

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  buyTicket(params?: CallParams<TicketingArgs['obj']['buy_ticket(pay)uint64'] | TicketingArgs['tuple']['buy_ticket(pay)uint64']>): TicketingComposer<[...TReturns, TicketingReturns['buy_ticket(pay)uint64'] | undefined]>

  /**
   * Calls the verify_entry(account,uint64)bool ABI method.
   *
  * Verify ticket at event entry (called by organizer/staff)
  Checks NFT ownership and check-in status

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  verifyEntry(params?: CallParams<TicketingArgs['obj']['verify_entry(account,uint64)bool'] | TicketingArgs['tuple']['verify_entry(account,uint64)bool']>): TicketingComposer<[...TReturns, TicketingReturns['verify_entry(account,uint64)bool'] | undefined]>

  /**
   * Calls the is_checked_in(uint64)bool ABI method.
   *
   * Check if a ticket has been used for entry
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  isCheckedIn(params?: CallParams<TicketingArgs['obj']['is_checked_in(uint64)bool'] | TicketingArgs['tuple']['is_checked_in(uint64)bool']>): TicketingComposer<[...TReturns, TicketingReturns['is_checked_in(uint64)bool'] | undefined]>

  /**
   * Calls the toggle_sale()bool ABI method.
   *
   * Toggle ticket sales on/off (organizer only)
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  toggleSale(params?: CallParams<TicketingArgs['obj']['toggle_sale()bool'] | TicketingArgs['tuple']['toggle_sale()bool']>): TicketingComposer<[...TReturns, TicketingReturns['toggle_sale()bool'] | undefined]>

  /**
   * Calls the get_event_info()(uint64,uint64,uint64,uint64,uint64,uint64,bool) ABI method.
   *
   * Get event information
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  getEventInfo(params?: CallParams<TicketingArgs['obj']['get_event_info()(uint64,uint64,uint64,uint64,uint64,uint64,bool)'] | TicketingArgs['tuple']['get_event_info()(uint64,uint64,uint64,uint64,uint64,uint64,bool)']>): TicketingComposer<[...TReturns, TicketingReturns['get_event_info()(uint64,uint64,uint64,uint64,uint64,uint64,bool)'] | undefined]>

  /**
   * Calls the get_organizer()address ABI method.
   *
   * Get event organizer address
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  getOrganizer(params?: CallParams<TicketingArgs['obj']['get_organizer()address'] | TicketingArgs['tuple']['get_organizer()address']>): TicketingComposer<[...TReturns, TicketingReturns['get_organizer()address'] | undefined]>

  /**
   * Makes a clear_state call to an existing instance of the Ticketing smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): TicketingComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): TicketingComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<TicketingComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<TicketingComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<TicketingComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<TicketingComposerResults<TReturns>>
}
export type TicketingComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

