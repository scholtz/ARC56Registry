/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"CampusChainRegistry","structs":{},"methods":[{"name":"register_fundraiser","args":[{"type":"uint64","name":"app_id"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Register a new fundraising campaign app ID.\nCan only be called by the app creator (prevents spam).","events":[],"recommendations":{}},{"name":"register_ticketing","args":[{"type":"uint64","name":"app_id"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Register a new ticketing event app ID.\nCan only be called by the app creator (prevents spam).","events":[],"recommendations":{}},{"name":"get_fundraisers","args":[],"returns":{"type":"uint64[]"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"desc":"Get all registered fundraiser app IDs.\nFrontend calls this to discover all campaigns.","events":[],"recommendations":{}},{"name":"get_ticketing","args":[],"returns":{"type":"uint64[]"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"desc":"Get all registered ticketing event app IDs.\nFrontend calls this to discover all events.","events":[],"recommendations":{}},{"name":"get_total_counts","args":[],"returns":{"type":"(uint64,uint64)"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"desc":"Get total count of registered fundraisers and events.\nUseful for debugging and statistics.","events":[],"recommendations":{}}],"arcs":[22,28],"desc":"\n    Registry contract storing app IDs for fundraising campaigns and ticketing events.\n    Uses box storage to handle large lists (>128 items).\n    Boxes are auto-created on first registration - no initialization needed.\n    ","networks":{},"state":{"schema":{"global":{"ints":0,"bytes":0},"local":{"ints":0,"bytes":0}},"keys":{"global":{},"local":{},"box":{}},"maps":{"global":{},"local":{},"box":{}}},"bareActions":{"create":["NoOp"],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[257,361],"errorMessage":"App ID already registered"},{"pc":[244,348],"errorMessage":"Index access is out of bounds"},{"pc":[92,113,129,145,161],"errorMessage":"OnCompletion is not NoOp"},{"pc":[182],"errorMessage":"can only call when creating"},{"pc":[95,116,132,148,164],"errorMessage":"can only call when not creating"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"events":[]} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


/**
 * The argument types for the CampusChainRegistry contract
 */
export type CampusChainRegistryArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'register_fundraiser(uint64)void': {
      appId: bigint | number
    }
    'register_ticketing(uint64)void': {
      appId: bigint | number
    }
    'get_fundraisers()uint64[]': Record<string, never>
    'get_ticketing()uint64[]': Record<string, never>
    'get_total_counts()(uint64,uint64)': Record<string, never>
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'register_fundraiser(uint64)void': [appId: bigint | number]
    'register_ticketing(uint64)void': [appId: bigint | number]
    'get_fundraisers()uint64[]': []
    'get_ticketing()uint64[]': []
    'get_total_counts()(uint64,uint64)': []
  }
}

/**
 * The return type for each method
 */
export type CampusChainRegistryReturns = {
  'register_fundraiser(uint64)void': void
  'register_ticketing(uint64)void': void
  'get_fundraisers()uint64[]': bigint[]
  'get_ticketing()uint64[]': bigint[]
  'get_total_counts()(uint64,uint64)': [bigint, bigint]
}

/**
 * Defines the types of available calls and state of the CampusChainRegistry smart contract.
 */
export type CampusChainRegistryTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'register_fundraiser(uint64)void' | 'register_fundraiser', {
      argsObj: CampusChainRegistryArgs['obj']['register_fundraiser(uint64)void']
      argsTuple: CampusChainRegistryArgs['tuple']['register_fundraiser(uint64)void']
      returns: CampusChainRegistryReturns['register_fundraiser(uint64)void']
    }>
    & Record<'register_ticketing(uint64)void' | 'register_ticketing', {
      argsObj: CampusChainRegistryArgs['obj']['register_ticketing(uint64)void']
      argsTuple: CampusChainRegistryArgs['tuple']['register_ticketing(uint64)void']
      returns: CampusChainRegistryReturns['register_ticketing(uint64)void']
    }>
    & Record<'get_fundraisers()uint64[]' | 'get_fundraisers', {
      argsObj: CampusChainRegistryArgs['obj']['get_fundraisers()uint64[]']
      argsTuple: CampusChainRegistryArgs['tuple']['get_fundraisers()uint64[]']
      returns: CampusChainRegistryReturns['get_fundraisers()uint64[]']
    }>
    & Record<'get_ticketing()uint64[]' | 'get_ticketing', {
      argsObj: CampusChainRegistryArgs['obj']['get_ticketing()uint64[]']
      argsTuple: CampusChainRegistryArgs['tuple']['get_ticketing()uint64[]']
      returns: CampusChainRegistryReturns['get_ticketing()uint64[]']
    }>
    & Record<'get_total_counts()(uint64,uint64)' | 'get_total_counts', {
      argsObj: CampusChainRegistryArgs['obj']['get_total_counts()(uint64,uint64)']
      argsTuple: CampusChainRegistryArgs['tuple']['get_total_counts()(uint64,uint64)']
      returns: CampusChainRegistryReturns['get_total_counts()(uint64,uint64)']
    }>
}

/**
 * Defines the possible abi call signatures.
 */
export type CampusChainRegistrySignatures = keyof CampusChainRegistryTypes['methods']
/**
 * Defines the possible abi call signatures for methods that return a non-void value.
 */
export type CampusChainRegistryNonVoidMethodSignatures = keyof CampusChainRegistryTypes['methods'] extends infer T ? T extends keyof CampusChainRegistryTypes['methods'] ? MethodReturn<T> extends void ? never : T  : never : never
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the CampusChainRegistry smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends CampusChainRegistrySignatures> = CampusChainRegistryTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the CampusChainRegistry smart contract to the method's return type
 */
export type MethodReturn<TSignature extends CampusChainRegistrySignatures> = CampusChainRegistryTypes['methods'][TSignature]['returns']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the CampusChainRegistry smart contract
 */
export abstract class CampusChainRegistryParamsFactory {
  /**
   * Constructs a no op call for the register_fundraiser(uint64)void ABI method
   *
  * Register a new fundraising campaign app ID.
  Can only be called by the app creator (prevents spam).

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static registerFundraiser(params: CallParams<CampusChainRegistryArgs['obj']['register_fundraiser(uint64)void'] | CampusChainRegistryArgs['tuple']['register_fundraiser(uint64)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'register_fundraiser(uint64)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.appId],
    }
  }
  /**
   * Constructs a no op call for the register_ticketing(uint64)void ABI method
   *
  * Register a new ticketing event app ID.
  Can only be called by the app creator (prevents spam).

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static registerTicketing(params: CallParams<CampusChainRegistryArgs['obj']['register_ticketing(uint64)void'] | CampusChainRegistryArgs['tuple']['register_ticketing(uint64)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'register_ticketing(uint64)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.appId],
    }
  }
  /**
   * Constructs a no op call for the get_fundraisers()uint64[] ABI method
   *
  * Get all registered fundraiser app IDs.
  Frontend calls this to discover all campaigns.

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static getFundraisers(params: CallParams<CampusChainRegistryArgs['obj']['get_fundraisers()uint64[]'] | CampusChainRegistryArgs['tuple']['get_fundraisers()uint64[]']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'get_fundraisers()uint64[]' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the get_ticketing()uint64[] ABI method
   *
  * Get all registered ticketing event app IDs.
  Frontend calls this to discover all events.

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static getTicketing(params: CallParams<CampusChainRegistryArgs['obj']['get_ticketing()uint64[]'] | CampusChainRegistryArgs['tuple']['get_ticketing()uint64[]']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'get_ticketing()uint64[]' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the get_total_counts()(uint64,uint64) ABI method
   *
  * Get total count of registered fundraisers and events.
  Useful for debugging and statistics.

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static getTotalCounts(params: CallParams<CampusChainRegistryArgs['obj']['get_total_counts()(uint64,uint64)'] | CampusChainRegistryArgs['tuple']['get_total_counts()(uint64,uint64)']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'get_total_counts()(uint64,uint64)' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
}

/**
 * A client to make calls to the CampusChainRegistry smart contract
 */
export class CampusChainRegistryClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `CampusChainRegistryClient`
   *
   * @param appClient An `AppClient` instance which has been created with the CampusChainRegistry app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `CampusChainRegistryClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Checks for decode errors on the given return value and maps the return value to the return type for the given method
   * @returns The typed return value or undefined if there was no value
   */
  decodeReturnValue<TSignature extends CampusChainRegistryNonVoidMethodSignatures>(method: TSignature, returnValue: ABIReturn | undefined) {
    return returnValue !== undefined ? getArc56ReturnValue<MethodReturn<TSignature>>(returnValue, this.appClient.getABIMethod(method), APP_SPEC.structs) : undefined
  }

  /**
   * Returns a new `CampusChainRegistryClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<CampusChainRegistryClient> {
    return new CampusChainRegistryClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `CampusChainRegistryClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<CampusChainRegistryClient> {
    return new CampusChainRegistryClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Makes a clear_state call to an existing instance of the CampusChainRegistry smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the CampusChainRegistry smart contract using the `register_fundraiser(uint64)void` ABI method.
     *
    * Register a new fundraising campaign app ID.
    Can only be called by the app creator (prevents spam).

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    registerFundraiser: (params: CallParams<CampusChainRegistryArgs['obj']['register_fundraiser(uint64)void'] | CampusChainRegistryArgs['tuple']['register_fundraiser(uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(CampusChainRegistryParamsFactory.registerFundraiser(params))
    },

    /**
     * Makes a call to the CampusChainRegistry smart contract using the `register_ticketing(uint64)void` ABI method.
     *
    * Register a new ticketing event app ID.
    Can only be called by the app creator (prevents spam).

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    registerTicketing: (params: CallParams<CampusChainRegistryArgs['obj']['register_ticketing(uint64)void'] | CampusChainRegistryArgs['tuple']['register_ticketing(uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(CampusChainRegistryParamsFactory.registerTicketing(params))
    },

    /**
     * Makes a call to the CampusChainRegistry smart contract using the `get_fundraisers()uint64[]` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
    * Get all registered fundraiser app IDs.
    Frontend calls this to discover all campaigns.

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    getFundraisers: (params: CallParams<CampusChainRegistryArgs['obj']['get_fundraisers()uint64[]'] | CampusChainRegistryArgs['tuple']['get_fundraisers()uint64[]']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(CampusChainRegistryParamsFactory.getFundraisers(params))
    },

    /**
     * Makes a call to the CampusChainRegistry smart contract using the `get_ticketing()uint64[]` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
    * Get all registered ticketing event app IDs.
    Frontend calls this to discover all events.

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    getTicketing: (params: CallParams<CampusChainRegistryArgs['obj']['get_ticketing()uint64[]'] | CampusChainRegistryArgs['tuple']['get_ticketing()uint64[]']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(CampusChainRegistryParamsFactory.getTicketing(params))
    },

    /**
     * Makes a call to the CampusChainRegistry smart contract using the `get_total_counts()(uint64,uint64)` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
    * Get total count of registered fundraisers and events.
    Useful for debugging and statistics.

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    getTotalCounts: (params: CallParams<CampusChainRegistryArgs['obj']['get_total_counts()(uint64,uint64)'] | CampusChainRegistryArgs['tuple']['get_total_counts()(uint64,uint64)']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(CampusChainRegistryParamsFactory.getTotalCounts(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Makes a clear_state call to an existing instance of the CampusChainRegistry smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the CampusChainRegistry smart contract using the `register_fundraiser(uint64)void` ABI method.
     *
    * Register a new fundraising campaign app ID.
    Can only be called by the app creator (prevents spam).

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    registerFundraiser: (params: CallParams<CampusChainRegistryArgs['obj']['register_fundraiser(uint64)void'] | CampusChainRegistryArgs['tuple']['register_fundraiser(uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(CampusChainRegistryParamsFactory.registerFundraiser(params))
    },

    /**
     * Makes a call to the CampusChainRegistry smart contract using the `register_ticketing(uint64)void` ABI method.
     *
    * Register a new ticketing event app ID.
    Can only be called by the app creator (prevents spam).

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    registerTicketing: (params: CallParams<CampusChainRegistryArgs['obj']['register_ticketing(uint64)void'] | CampusChainRegistryArgs['tuple']['register_ticketing(uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(CampusChainRegistryParamsFactory.registerTicketing(params))
    },

    /**
     * Makes a call to the CampusChainRegistry smart contract using the `get_fundraisers()uint64[]` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
    * Get all registered fundraiser app IDs.
    Frontend calls this to discover all campaigns.

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    getFundraisers: (params: CallParams<CampusChainRegistryArgs['obj']['get_fundraisers()uint64[]'] | CampusChainRegistryArgs['tuple']['get_fundraisers()uint64[]']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(CampusChainRegistryParamsFactory.getFundraisers(params))
    },

    /**
     * Makes a call to the CampusChainRegistry smart contract using the `get_ticketing()uint64[]` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
    * Get all registered ticketing event app IDs.
    Frontend calls this to discover all events.

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    getTicketing: (params: CallParams<CampusChainRegistryArgs['obj']['get_ticketing()uint64[]'] | CampusChainRegistryArgs['tuple']['get_ticketing()uint64[]']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(CampusChainRegistryParamsFactory.getTicketing(params))
    },

    /**
     * Makes a call to the CampusChainRegistry smart contract using the `get_total_counts()(uint64,uint64)` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
    * Get total count of registered fundraisers and events.
    Useful for debugging and statistics.

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    getTotalCounts: (params: CallParams<CampusChainRegistryArgs['obj']['get_total_counts()(uint64,uint64)'] | CampusChainRegistryArgs['tuple']['get_total_counts()(uint64,uint64)']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(CampusChainRegistryParamsFactory.getTotalCounts(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Makes a clear_state call to an existing instance of the CampusChainRegistry smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the CampusChainRegistry smart contract using the `register_fundraiser(uint64)void` ABI method.
     *
    * Register a new fundraising campaign app ID.
    Can only be called by the app creator (prevents spam).

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    registerFundraiser: async (params: CallParams<CampusChainRegistryArgs['obj']['register_fundraiser(uint64)void'] | CampusChainRegistryArgs['tuple']['register_fundraiser(uint64)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(CampusChainRegistryParamsFactory.registerFundraiser(params))
      return {...result, return: result.return as unknown as (undefined | CampusChainRegistryReturns['register_fundraiser(uint64)void'])}
    },

    /**
     * Makes a call to the CampusChainRegistry smart contract using the `register_ticketing(uint64)void` ABI method.
     *
    * Register a new ticketing event app ID.
    Can only be called by the app creator (prevents spam).

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    registerTicketing: async (params: CallParams<CampusChainRegistryArgs['obj']['register_ticketing(uint64)void'] | CampusChainRegistryArgs['tuple']['register_ticketing(uint64)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(CampusChainRegistryParamsFactory.registerTicketing(params))
      return {...result, return: result.return as unknown as (undefined | CampusChainRegistryReturns['register_ticketing(uint64)void'])}
    },

    /**
     * Makes a call to the CampusChainRegistry smart contract using the `get_fundraisers()uint64[]` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
    * Get all registered fundraiser app IDs.
    Frontend calls this to discover all campaigns.

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    getFundraisers: async (params: CallParams<CampusChainRegistryArgs['obj']['get_fundraisers()uint64[]'] | CampusChainRegistryArgs['tuple']['get_fundraisers()uint64[]']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(CampusChainRegistryParamsFactory.getFundraisers(params))
      return {...result, return: result.return as unknown as (undefined | CampusChainRegistryReturns['get_fundraisers()uint64[]'])}
    },

    /**
     * Makes a call to the CampusChainRegistry smart contract using the `get_ticketing()uint64[]` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
    * Get all registered ticketing event app IDs.
    Frontend calls this to discover all events.

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    getTicketing: async (params: CallParams<CampusChainRegistryArgs['obj']['get_ticketing()uint64[]'] | CampusChainRegistryArgs['tuple']['get_ticketing()uint64[]']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(CampusChainRegistryParamsFactory.getTicketing(params))
      return {...result, return: result.return as unknown as (undefined | CampusChainRegistryReturns['get_ticketing()uint64[]'])}
    },

    /**
     * Makes a call to the CampusChainRegistry smart contract using the `get_total_counts()(uint64,uint64)` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
    * Get total count of registered fundraisers and events.
    Useful for debugging and statistics.

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    getTotalCounts: async (params: CallParams<CampusChainRegistryArgs['obj']['get_total_counts()(uint64,uint64)'] | CampusChainRegistryArgs['tuple']['get_total_counts()(uint64,uint64)']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(CampusChainRegistryParamsFactory.getTotalCounts(params))
      return {...result, return: result.return as unknown as (undefined | CampusChainRegistryReturns['get_total_counts()(uint64,uint64)'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new CampusChainRegistryClient(this.appClient.clone(params))
  }

  /**
   * Makes a readonly (simulated) call to the CampusChainRegistry smart contract using the `get_fundraisers()uint64[]` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
  * Get all registered fundraiser app IDs.
  Frontend calls this to discover all campaigns.

   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async getFundraisers(params: CallParams<CampusChainRegistryArgs['obj']['get_fundraisers()uint64[]'] | CampusChainRegistryArgs['tuple']['get_fundraisers()uint64[]']> = {args: []}) {
    const result = await this.appClient.send.call(CampusChainRegistryParamsFactory.getFundraisers(params))
    return result.return as unknown as CampusChainRegistryReturns['get_fundraisers()uint64[]']
  }

  /**
   * Makes a readonly (simulated) call to the CampusChainRegistry smart contract using the `get_ticketing()uint64[]` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
  * Get all registered ticketing event app IDs.
  Frontend calls this to discover all events.

   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async getTicketing(params: CallParams<CampusChainRegistryArgs['obj']['get_ticketing()uint64[]'] | CampusChainRegistryArgs['tuple']['get_ticketing()uint64[]']> = {args: []}) {
    const result = await this.appClient.send.call(CampusChainRegistryParamsFactory.getTicketing(params))
    return result.return as unknown as CampusChainRegistryReturns['get_ticketing()uint64[]']
  }

  /**
   * Makes a readonly (simulated) call to the CampusChainRegistry smart contract using the `get_total_counts()(uint64,uint64)` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
  * Get total count of registered fundraisers and events.
  Useful for debugging and statistics.

   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async getTotalCounts(params: CallParams<CampusChainRegistryArgs['obj']['get_total_counts()(uint64,uint64)'] | CampusChainRegistryArgs['tuple']['get_total_counts()(uint64,uint64)']> = {args: []}) {
    const result = await this.appClient.send.call(CampusChainRegistryParamsFactory.getTotalCounts(params))
    return result.return as unknown as CampusChainRegistryReturns['get_total_counts()(uint64,uint64)']
  }

  /**
   * Methods to access state for the current CampusChainRegistry app
   */
  state = {
  }

  public newGroup(): CampusChainRegistryComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a register_fundraiser(uint64)void method call against the CampusChainRegistry contract
       */
      registerFundraiser(params: CallParams<CampusChainRegistryArgs['obj']['register_fundraiser(uint64)void'] | CampusChainRegistryArgs['tuple']['register_fundraiser(uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.registerFundraiser(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a register_ticketing(uint64)void method call against the CampusChainRegistry contract
       */
      registerTicketing(params: CallParams<CampusChainRegistryArgs['obj']['register_ticketing(uint64)void'] | CampusChainRegistryArgs['tuple']['register_ticketing(uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.registerTicketing(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a get_fundraisers()uint64[] method call against the CampusChainRegistry contract
       */
      getFundraisers(params: CallParams<CampusChainRegistryArgs['obj']['get_fundraisers()uint64[]'] | CampusChainRegistryArgs['tuple']['get_fundraisers()uint64[]']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.getFundraisers(params)))
        resultMappers.push((v) => client.decodeReturnValue('get_fundraisers()uint64[]', v))
        return this
      },
      /**
       * Add a get_ticketing()uint64[] method call against the CampusChainRegistry contract
       */
      getTicketing(params: CallParams<CampusChainRegistryArgs['obj']['get_ticketing()uint64[]'] | CampusChainRegistryArgs['tuple']['get_ticketing()uint64[]']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.getTicketing(params)))
        resultMappers.push((v) => client.decodeReturnValue('get_ticketing()uint64[]', v))
        return this
      },
      /**
       * Add a get_total_counts()(uint64,uint64) method call against the CampusChainRegistry contract
       */
      getTotalCounts(params: CallParams<CampusChainRegistryArgs['obj']['get_total_counts()(uint64,uint64)'] | CampusChainRegistryArgs['tuple']['get_total_counts()(uint64,uint64)']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.getTotalCounts(params)))
        resultMappers.push((v) => client.decodeReturnValue('get_total_counts()(uint64,uint64)', v))
        return this
      },
      /**
       * Add a clear state call to the CampusChainRegistry contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as CampusChainRegistryComposer
  }
}
export type CampusChainRegistryComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the register_fundraiser(uint64)void ABI method.
   *
  * Register a new fundraising campaign app ID.
  Can only be called by the app creator (prevents spam).

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  registerFundraiser(params?: CallParams<CampusChainRegistryArgs['obj']['register_fundraiser(uint64)void'] | CampusChainRegistryArgs['tuple']['register_fundraiser(uint64)void']>): CampusChainRegistryComposer<[...TReturns, CampusChainRegistryReturns['register_fundraiser(uint64)void'] | undefined]>

  /**
   * Calls the register_ticketing(uint64)void ABI method.
   *
  * Register a new ticketing event app ID.
  Can only be called by the app creator (prevents spam).

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  registerTicketing(params?: CallParams<CampusChainRegistryArgs['obj']['register_ticketing(uint64)void'] | CampusChainRegistryArgs['tuple']['register_ticketing(uint64)void']>): CampusChainRegistryComposer<[...TReturns, CampusChainRegistryReturns['register_ticketing(uint64)void'] | undefined]>

  /**
   * Calls the get_fundraisers()uint64[] ABI method.
   *
  * Get all registered fundraiser app IDs.
  Frontend calls this to discover all campaigns.

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  getFundraisers(params?: CallParams<CampusChainRegistryArgs['obj']['get_fundraisers()uint64[]'] | CampusChainRegistryArgs['tuple']['get_fundraisers()uint64[]']>): CampusChainRegistryComposer<[...TReturns, CampusChainRegistryReturns['get_fundraisers()uint64[]'] | undefined]>

  /**
   * Calls the get_ticketing()uint64[] ABI method.
   *
  * Get all registered ticketing event app IDs.
  Frontend calls this to discover all events.

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  getTicketing(params?: CallParams<CampusChainRegistryArgs['obj']['get_ticketing()uint64[]'] | CampusChainRegistryArgs['tuple']['get_ticketing()uint64[]']>): CampusChainRegistryComposer<[...TReturns, CampusChainRegistryReturns['get_ticketing()uint64[]'] | undefined]>

  /**
   * Calls the get_total_counts()(uint64,uint64) ABI method.
   *
  * Get total count of registered fundraisers and events.
  Useful for debugging and statistics.

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  getTotalCounts(params?: CallParams<CampusChainRegistryArgs['obj']['get_total_counts()(uint64,uint64)'] | CampusChainRegistryArgs['tuple']['get_total_counts()(uint64,uint64)']>): CampusChainRegistryComposer<[...TReturns, CampusChainRegistryReturns['get_total_counts()(uint64,uint64)'] | undefined]>

  /**
   * Makes a clear_state call to an existing instance of the CampusChainRegistry smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): CampusChainRegistryComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): CampusChainRegistryComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<CampusChainRegistryComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<CampusChainRegistryComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<CampusChainRegistryComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<CampusChainRegistryComposerResults<TReturns>>
}
export type CampusChainRegistryComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

