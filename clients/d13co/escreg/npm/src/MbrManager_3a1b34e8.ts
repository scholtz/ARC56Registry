/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"MbrManager","structs":{},"methods":[{"name":"depositCredits","args":[{"type":"address","name":"creditor","desc":"account to credit"},{"type":"pay","name":"txn","desc":"payment transaction to contract. amount is the credit received"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"public method to deposit MBR credits for an account","events":[],"recommendations":{}},{"name":"withdrawCredits","args":[],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Withdraw all remaining MBR credits for sender. This will delete the user credit box, so all credits are withdrawn including the MBR locked for the box itself.","events":[],"recommendations":{}}],"arcs":[22,28],"networks":{},"state":{"schema":{"global":{"ints":0,"bytes":0},"local":{"ints":0,"bytes":0}},"keys":{"global":{},"local":{},"box":{}},"maps":{"global":{},"local":{},"box":{"userCredits":{"keyType":"address","valueType":"uint64","prefix":"Yw=="}}}},"bareActions":{"create":["NoOp"],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[133,200,268,310],"errorMessage":"Box must have value"},{"pc":[33],"errorMessage":"OnCompletion must be NoOp"},{"pc":[66],"errorMessage":"OnCompletion must be NoOp && can only call when creating"},{"pc":[145,165,291,322],"errorMessage":"account funded"},{"pc":[82],"errorMessage":"invalid number of bytes for arc4.static_array<arc4.uint8, 32>"},{"pc":[93],"errorMessage":"transaction type is pay"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"events":[]} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


/**
 * The argument types for the MbrManager contract
 */
export type MbrManagerArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'depositCredits(address,pay)void': {
      /**
       * account to credit
       */
      creditor: string
      /**
       * payment transaction to contract. amount is the credit received
       */
      txn: AppMethodCallTransactionArgument
    }
    'withdrawCredits()void': Record<string, never>
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'depositCredits(address,pay)void': [creditor: string, txn: AppMethodCallTransactionArgument]
    'withdrawCredits()void': []
  }
}

/**
 * The return type for each method
 */
export type MbrManagerReturns = {
  'depositCredits(address,pay)void': void
  'withdrawCredits()void': void
}

/**
 * Defines the types of available calls and state of the MbrManager smart contract.
 */
export type MbrManagerTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'depositCredits(address,pay)void' | 'depositCredits', {
      argsObj: MbrManagerArgs['obj']['depositCredits(address,pay)void']
      argsTuple: MbrManagerArgs['tuple']['depositCredits(address,pay)void']
      returns: MbrManagerReturns['depositCredits(address,pay)void']
    }>
    & Record<'withdrawCredits()void' | 'withdrawCredits', {
      argsObj: MbrManagerArgs['obj']['withdrawCredits()void']
      argsTuple: MbrManagerArgs['tuple']['withdrawCredits()void']
      returns: MbrManagerReturns['withdrawCredits()void']
    }>
  /**
   * Defines the shape of the state of the application.
   */
  state: {
    box: {
      keys: {}
      maps: {
        userCredits: Map<string, bigint>
      }
    }
  }
}

/**
 * Defines the possible abi call signatures.
 */
export type MbrManagerSignatures = keyof MbrManagerTypes['methods']
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the MbrManager smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends MbrManagerSignatures> = MbrManagerTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the MbrManager smart contract to the method's return type
 */
export type MethodReturn<TSignature extends MbrManagerSignatures> = MbrManagerTypes['methods'][TSignature]['returns']

/**
 * Defines the shape of the keyed box state of the application.
 */
export type BoxKeysState = MbrManagerTypes['state']['box']['keys']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the MbrManager smart contract
 */
export abstract class MbrManagerParamsFactory {
  /**
   * Constructs a no op call for the depositCredits(address,pay)void ABI method
   *
   * public method to deposit MBR credits for an account
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static depositCredits(params: CallParams<MbrManagerArgs['obj']['depositCredits(address,pay)void'] | MbrManagerArgs['tuple']['depositCredits(address,pay)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'depositCredits(address,pay)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.creditor, params.args.txn],
    }
  }
  /**
   * Constructs a no op call for the withdrawCredits()void ABI method
   *
   * Withdraw all remaining MBR credits for sender. This will delete the user credit box, so all credits are withdrawn including the MBR locked for the box itself.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static withdrawCredits(params: CallParams<MbrManagerArgs['obj']['withdrawCredits()void'] | MbrManagerArgs['tuple']['withdrawCredits()void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'withdrawCredits()void' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
}

/**
 * A client to make calls to the MbrManager smart contract
 */
export class MbrManagerClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `MbrManagerClient`
   *
   * @param appClient An `AppClient` instance which has been created with the MbrManager app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `MbrManagerClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Returns a new `MbrManagerClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<MbrManagerClient> {
    return new MbrManagerClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `MbrManagerClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<MbrManagerClient> {
    return new MbrManagerClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Makes a clear_state call to an existing instance of the MbrManager smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the MbrManager smart contract using the `depositCredits(address,pay)void` ABI method.
     *
     * public method to deposit MBR credits for an account
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    depositCredits: (params: CallParams<MbrManagerArgs['obj']['depositCredits(address,pay)void'] | MbrManagerArgs['tuple']['depositCredits(address,pay)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(MbrManagerParamsFactory.depositCredits(params))
    },

    /**
     * Makes a call to the MbrManager smart contract using the `withdrawCredits()void` ABI method.
     *
     * Withdraw all remaining MBR credits for sender. This will delete the user credit box, so all credits are withdrawn including the MBR locked for the box itself.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    withdrawCredits: (params: CallParams<MbrManagerArgs['obj']['withdrawCredits()void'] | MbrManagerArgs['tuple']['withdrawCredits()void']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(MbrManagerParamsFactory.withdrawCredits(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Makes a clear_state call to an existing instance of the MbrManager smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the MbrManager smart contract using the `depositCredits(address,pay)void` ABI method.
     *
     * public method to deposit MBR credits for an account
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    depositCredits: (params: CallParams<MbrManagerArgs['obj']['depositCredits(address,pay)void'] | MbrManagerArgs['tuple']['depositCredits(address,pay)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(MbrManagerParamsFactory.depositCredits(params))
    },

    /**
     * Makes a call to the MbrManager smart contract using the `withdrawCredits()void` ABI method.
     *
     * Withdraw all remaining MBR credits for sender. This will delete the user credit box, so all credits are withdrawn including the MBR locked for the box itself.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    withdrawCredits: (params: CallParams<MbrManagerArgs['obj']['withdrawCredits()void'] | MbrManagerArgs['tuple']['withdrawCredits()void']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(MbrManagerParamsFactory.withdrawCredits(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Makes a clear_state call to an existing instance of the MbrManager smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the MbrManager smart contract using the `depositCredits(address,pay)void` ABI method.
     *
     * public method to deposit MBR credits for an account
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    depositCredits: async (params: CallParams<MbrManagerArgs['obj']['depositCredits(address,pay)void'] | MbrManagerArgs['tuple']['depositCredits(address,pay)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(MbrManagerParamsFactory.depositCredits(params))
      return {...result, return: result.return as unknown as (undefined | MbrManagerReturns['depositCredits(address,pay)void'])}
    },

    /**
     * Makes a call to the MbrManager smart contract using the `withdrawCredits()void` ABI method.
     *
     * Withdraw all remaining MBR credits for sender. This will delete the user credit box, so all credits are withdrawn including the MBR locked for the box itself.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    withdrawCredits: async (params: CallParams<MbrManagerArgs['obj']['withdrawCredits()void'] | MbrManagerArgs['tuple']['withdrawCredits()void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(MbrManagerParamsFactory.withdrawCredits(params))
      return {...result, return: result.return as unknown as (undefined | MbrManagerReturns['withdrawCredits()void'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new MbrManagerClient(this.appClient.clone(params))
  }

  /**
   * Methods to access state for the current MbrManager app
   */
  state = {
    /**
     * Methods to access box state for the current MbrManager app
     */
    box: {
      /**
       * Get all current keyed values from box state
       */
      getAll: async (): Promise<Partial<Expand<BoxKeysState>>> => {
        const result = await this.appClient.state.box.getAll()
        return {
        }
      },
      /**
       * Get values from the userCredits map in box state
       */
      userCredits: {
        /**
         * Get all current values of the userCredits map in box state
         */
        getMap: async (): Promise<Map<string, bigint>> => { return (await this.appClient.state.box.getMap("userCredits")) as Map<string, bigint> },
        /**
         * Get a current value of the userCredits map by key from box state
         */
        value: async (key: string): Promise<bigint | undefined> => { return await this.appClient.state.box.getMapValue("userCredits", key) as bigint | undefined },
      },
    },
  }

  public newGroup(): MbrManagerComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a depositCredits(address,pay)void method call against the MbrManager contract
       */
      depositCredits(params: CallParams<MbrManagerArgs['obj']['depositCredits(address,pay)void'] | MbrManagerArgs['tuple']['depositCredits(address,pay)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.depositCredits(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a withdrawCredits()void method call against the MbrManager contract
       */
      withdrawCredits(params: CallParams<MbrManagerArgs['obj']['withdrawCredits()void'] | MbrManagerArgs['tuple']['withdrawCredits()void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.withdrawCredits(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a clear state call to the MbrManager contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as MbrManagerComposer
  }
}
export type MbrManagerComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the depositCredits(address,pay)void ABI method.
   *
   * public method to deposit MBR credits for an account
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  depositCredits(params?: CallParams<MbrManagerArgs['obj']['depositCredits(address,pay)void'] | MbrManagerArgs['tuple']['depositCredits(address,pay)void']>): MbrManagerComposer<[...TReturns, MbrManagerReturns['depositCredits(address,pay)void'] | undefined]>

  /**
   * Calls the withdrawCredits()void ABI method.
   *
   * Withdraw all remaining MBR credits for sender. This will delete the user credit box, so all credits are withdrawn including the MBR locked for the box itself.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  withdrawCredits(params?: CallParams<MbrManagerArgs['obj']['withdrawCredits()void'] | MbrManagerArgs['tuple']['withdrawCredits()void']>): MbrManagerComposer<[...TReturns, MbrManagerReturns['withdrawCredits()void'] | undefined]>

  /**
   * Makes a clear_state call to an existing instance of the MbrManager smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): MbrManagerComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): MbrManagerComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<MbrManagerComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<MbrManagerComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<MbrManagerComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<MbrManagerComposerResults<TReturns>>
}
export type MbrManagerComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

