/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"OracleAccountContract","structs":{"OracleAccount":[{"name":"accountId","type":"uint32"},{"name":"committeeOffsets","type":"(uint16,uint16)[]"}]},"methods":[{"name":"getAccount","args":[{"type":"address","name":"account","desc":"account to look up"}],"returns":{"type":"(uint32,(uint16,uint16)[])","struct":"OracleAccount","desc":"account ID or 0 if not found"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"desc":"Get account ID if exists, else return 0","events":[],"recommendations":{}},{"name":"logAccounts","args":[{"type":"address[]","name":"accounts","desc":"accounts to log"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"desc":"Log multiple accounts' IDs (or zero if not found)\nUsed to fetch account>ID quickly off-chain","events":[],"recommendations":{}},{"name":"increaseBudget","args":[{"type":"uint64","name":"itxns","desc":"Number of no-op itxns to perform"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Utility to increase opcode budget by performing $itxns no-op itxns","events":[],"recommendations":{}}],"arcs":[22,28],"networks":{},"state":{"schema":{"global":{"ints":1,"bytes":0},"local":{"ints":0,"bytes":0}},"keys":{"global":{"lastAccountId":{"keyType":"AVMString","valueType":"AVMUint64","key":"bGFzdEFjY291bnRJZA==","desc":"Last account numeric ID"}},"local":{},"box":{}},"maps":{"global":{},"local":{},"box":{"accounts":{"keyType":"address","valueType":"OracleAccount","desc":"Oracle accounts box map","prefix":"YQ=="}}}},"bareActions":{"create":["NoOp"],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[222],"errorMessage":"Box must have value"},{"pc":[43],"errorMessage":"OnCompletion must be NoOp"},{"pc":[83],"errorMessage":"OnCompletion must be NoOp && can only call when creating"},{"pc":[148],"errorMessage":"index access is out of bounds"},{"pc":[114],"errorMessage":"invalid array length header"},{"pc":[126],"errorMessage":"invalid number of bytes for arc4.dynamic_array<account>"},{"pc":[93],"errorMessage":"invalid number of bytes for arc4.static_array<arc4.uint8, 32>"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"events":[]} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


// Type definitions for ARC-56 structs

export type OracleAccount = {
  accountId: number,
  committeeOffsets: [number, number][]
}


/**
 * Converts the ABI tuple representation of a OracleAccount to the struct representation
 */
export function OracleAccountFromTuple(abiTuple: [number, [number, number][]]) {
  return getABIStructFromABITuple(abiTuple, APP_SPEC.structs.OracleAccount, APP_SPEC.structs) as OracleAccount
}

/**
 * The argument types for the OracleAccountContract contract
 */
export type OracleAccountContractArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'getAccount(address)(uint32,(uint16,uint16)[])': {
      /**
       * account to look up
       */
      account: string
    }
    'logAccounts(address[])void': {
      /**
       * accounts to log
       */
      accounts: string[]
    }
    'increaseBudget(uint64)void': {
      /**
       * Number of no-op itxns to perform
       */
      itxns: bigint | number
    }
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'getAccount(address)(uint32,(uint16,uint16)[])': [account: string]
    'logAccounts(address[])void': [accounts: string[]]
    'increaseBudget(uint64)void': [itxns: bigint | number]
  }
}

/**
 * The return type for each method
 */
export type OracleAccountContractReturns = {
  'getAccount(address)(uint32,(uint16,uint16)[])': OracleAccount
  'logAccounts(address[])void': void
  'increaseBudget(uint64)void': void
}

/**
 * Defines the types of available calls and state of the OracleAccountContract smart contract.
 */
export type OracleAccountContractTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'getAccount(address)(uint32,(uint16,uint16)[])' | 'getAccount', {
      argsObj: OracleAccountContractArgs['obj']['getAccount(address)(uint32,(uint16,uint16)[])']
      argsTuple: OracleAccountContractArgs['tuple']['getAccount(address)(uint32,(uint16,uint16)[])']
      /**
       * account ID or 0 if not found
       */
      returns: OracleAccountContractReturns['getAccount(address)(uint32,(uint16,uint16)[])']
    }>
    & Record<'logAccounts(address[])void' | 'logAccounts', {
      argsObj: OracleAccountContractArgs['obj']['logAccounts(address[])void']
      argsTuple: OracleAccountContractArgs['tuple']['logAccounts(address[])void']
      returns: OracleAccountContractReturns['logAccounts(address[])void']
    }>
    & Record<'increaseBudget(uint64)void' | 'increaseBudget', {
      argsObj: OracleAccountContractArgs['obj']['increaseBudget(uint64)void']
      argsTuple: OracleAccountContractArgs['tuple']['increaseBudget(uint64)void']
      returns: OracleAccountContractReturns['increaseBudget(uint64)void']
    }>
  /**
   * Defines the shape of the state of the application.
   */
  state: {
    global: {
      keys: {
        /**
         * Last account numeric ID
         */
        lastAccountId: bigint
      }
      maps: {}
    }
    box: {
      keys: {}
      maps: {
        /**
         * Oracle accounts box map
         */
        accounts: Map<string, OracleAccount>
      }
    }
  }
}

/**
 * Defines the possible abi call signatures.
 */
export type OracleAccountContractSignatures = keyof OracleAccountContractTypes['methods']
/**
 * Defines the possible abi call signatures for methods that return a non-void value.
 */
export type OracleAccountContractNonVoidMethodSignatures = keyof OracleAccountContractTypes['methods'] extends infer T ? T extends keyof OracleAccountContractTypes['methods'] ? MethodReturn<T> extends void ? never : T  : never : never
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the OracleAccountContract smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends OracleAccountContractSignatures> = OracleAccountContractTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the OracleAccountContract smart contract to the method's return type
 */
export type MethodReturn<TSignature extends OracleAccountContractSignatures> = OracleAccountContractTypes['methods'][TSignature]['returns']

/**
 * Defines the shape of the keyed global state of the application.
 */
export type GlobalKeysState = OracleAccountContractTypes['state']['global']['keys']

/**
 * Defines the shape of the keyed box state of the application.
 */
export type BoxKeysState = OracleAccountContractTypes['state']['box']['keys']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the OracleAccountContract smart contract
 */
export abstract class OracleAccountContractParamsFactory {
  /**
   * Constructs a no op call for the getAccount(address)(uint32,(uint16,uint16)[]) ABI method
   *
   * Get account ID if exists, else return 0
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static getAccount(params: CallParams<OracleAccountContractArgs['obj']['getAccount(address)(uint32,(uint16,uint16)[])'] | OracleAccountContractArgs['tuple']['getAccount(address)(uint32,(uint16,uint16)[])']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'getAccount(address)(uint32,(uint16,uint16)[])' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.account],
    }
  }
  /**
   * Constructs a no op call for the logAccounts(address[])void ABI method
   *
  * Log multiple accounts' IDs (or zero if not found)
  Used to fetch account>ID quickly off-chain

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static logAccounts(params: CallParams<OracleAccountContractArgs['obj']['logAccounts(address[])void'] | OracleAccountContractArgs['tuple']['logAccounts(address[])void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'logAccounts(address[])void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.accounts],
    }
  }
  /**
   * Constructs a no op call for the increaseBudget(uint64)void ABI method
   *
   * Utility to increase opcode budget by performing $itxns no-op itxns
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static increaseBudget(params: CallParams<OracleAccountContractArgs['obj']['increaseBudget(uint64)void'] | OracleAccountContractArgs['tuple']['increaseBudget(uint64)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'increaseBudget(uint64)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.itxns],
    }
  }
}

/**
 * A client to make calls to the OracleAccountContract smart contract
 */
export class OracleAccountContractClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `OracleAccountContractClient`
   *
   * @param appClient An `AppClient` instance which has been created with the OracleAccountContract app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `OracleAccountContractClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Checks for decode errors on the given return value and maps the return value to the return type for the given method
   * @returns The typed return value or undefined if there was no value
   */
  decodeReturnValue<TSignature extends OracleAccountContractNonVoidMethodSignatures>(method: TSignature, returnValue: ABIReturn | undefined) {
    return returnValue !== undefined ? getArc56ReturnValue<MethodReturn<TSignature>>(returnValue, this.appClient.getABIMethod(method), APP_SPEC.structs) : undefined
  }

  /**
   * Returns a new `OracleAccountContractClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<OracleAccountContractClient> {
    return new OracleAccountContractClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `OracleAccountContractClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<OracleAccountContractClient> {
    return new OracleAccountContractClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Makes a clear_state call to an existing instance of the OracleAccountContract smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the OracleAccountContract smart contract using the `getAccount(address)(uint32,(uint16,uint16)[])` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * Get account ID if exists, else return 0
     *
     * @param params The params for the smart contract call
     * @returns The call params: account ID or 0 if not found
     */
    getAccount: (params: CallParams<OracleAccountContractArgs['obj']['getAccount(address)(uint32,(uint16,uint16)[])'] | OracleAccountContractArgs['tuple']['getAccount(address)(uint32,(uint16,uint16)[])']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(OracleAccountContractParamsFactory.getAccount(params))
    },

    /**
     * Makes a call to the OracleAccountContract smart contract using the `logAccounts(address[])void` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
    * Log multiple accounts' IDs (or zero if not found)
    Used to fetch account>ID quickly off-chain

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    logAccounts: (params: CallParams<OracleAccountContractArgs['obj']['logAccounts(address[])void'] | OracleAccountContractArgs['tuple']['logAccounts(address[])void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(OracleAccountContractParamsFactory.logAccounts(params))
    },

    /**
     * Makes a call to the OracleAccountContract smart contract using the `increaseBudget(uint64)void` ABI method.
     *
     * Utility to increase opcode budget by performing $itxns no-op itxns
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    increaseBudget: (params: CallParams<OracleAccountContractArgs['obj']['increaseBudget(uint64)void'] | OracleAccountContractArgs['tuple']['increaseBudget(uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(OracleAccountContractParamsFactory.increaseBudget(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Makes a clear_state call to an existing instance of the OracleAccountContract smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the OracleAccountContract smart contract using the `getAccount(address)(uint32,(uint16,uint16)[])` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * Get account ID if exists, else return 0
     *
     * @param params The params for the smart contract call
     * @returns The call transaction: account ID or 0 if not found
     */
    getAccount: (params: CallParams<OracleAccountContractArgs['obj']['getAccount(address)(uint32,(uint16,uint16)[])'] | OracleAccountContractArgs['tuple']['getAccount(address)(uint32,(uint16,uint16)[])']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(OracleAccountContractParamsFactory.getAccount(params))
    },

    /**
     * Makes a call to the OracleAccountContract smart contract using the `logAccounts(address[])void` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
    * Log multiple accounts' IDs (or zero if not found)
    Used to fetch account>ID quickly off-chain

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    logAccounts: (params: CallParams<OracleAccountContractArgs['obj']['logAccounts(address[])void'] | OracleAccountContractArgs['tuple']['logAccounts(address[])void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(OracleAccountContractParamsFactory.logAccounts(params))
    },

    /**
     * Makes a call to the OracleAccountContract smart contract using the `increaseBudget(uint64)void` ABI method.
     *
     * Utility to increase opcode budget by performing $itxns no-op itxns
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    increaseBudget: (params: CallParams<OracleAccountContractArgs['obj']['increaseBudget(uint64)void'] | OracleAccountContractArgs['tuple']['increaseBudget(uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(OracleAccountContractParamsFactory.increaseBudget(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Makes a clear_state call to an existing instance of the OracleAccountContract smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the OracleAccountContract smart contract using the `getAccount(address)(uint32,(uint16,uint16)[])` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * Get account ID if exists, else return 0
     *
     * @param params The params for the smart contract call
     * @returns The call result: account ID or 0 if not found
     */
    getAccount: async (params: CallParams<OracleAccountContractArgs['obj']['getAccount(address)(uint32,(uint16,uint16)[])'] | OracleAccountContractArgs['tuple']['getAccount(address)(uint32,(uint16,uint16)[])']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(OracleAccountContractParamsFactory.getAccount(params))
      return {...result, return: result.return as unknown as (undefined | OracleAccountContractReturns['getAccount(address)(uint32,(uint16,uint16)[])'])}
    },

    /**
     * Makes a call to the OracleAccountContract smart contract using the `logAccounts(address[])void` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
    * Log multiple accounts' IDs (or zero if not found)
    Used to fetch account>ID quickly off-chain

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    logAccounts: async (params: CallParams<OracleAccountContractArgs['obj']['logAccounts(address[])void'] | OracleAccountContractArgs['tuple']['logAccounts(address[])void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(OracleAccountContractParamsFactory.logAccounts(params))
      return {...result, return: result.return as unknown as (undefined | OracleAccountContractReturns['logAccounts(address[])void'])}
    },

    /**
     * Makes a call to the OracleAccountContract smart contract using the `increaseBudget(uint64)void` ABI method.
     *
     * Utility to increase opcode budget by performing $itxns no-op itxns
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    increaseBudget: async (params: CallParams<OracleAccountContractArgs['obj']['increaseBudget(uint64)void'] | OracleAccountContractArgs['tuple']['increaseBudget(uint64)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(OracleAccountContractParamsFactory.increaseBudget(params))
      return {...result, return: result.return as unknown as (undefined | OracleAccountContractReturns['increaseBudget(uint64)void'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new OracleAccountContractClient(this.appClient.clone(params))
  }

  /**
   * Makes a readonly (simulated) call to the OracleAccountContract smart contract using the `getAccount(address)(uint32,(uint16,uint16)[])` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * Get account ID if exists, else return 0
   *
   * @param params The params for the smart contract call
   * @returns The call result: account ID or 0 if not found
   */
  async getAccount(params: CallParams<OracleAccountContractArgs['obj']['getAccount(address)(uint32,(uint16,uint16)[])'] | OracleAccountContractArgs['tuple']['getAccount(address)(uint32,(uint16,uint16)[])']>) {
    const result = await this.appClient.send.call(OracleAccountContractParamsFactory.getAccount(params))
    return result.return as unknown as OracleAccountContractReturns['getAccount(address)(uint32,(uint16,uint16)[])']
  }

  /**
   * Makes a readonly (simulated) call to the OracleAccountContract smart contract using the `logAccounts(address[])void` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
  * Log multiple accounts' IDs (or zero if not found)
  Used to fetch account>ID quickly off-chain

   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async logAccounts(params: CallParams<OracleAccountContractArgs['obj']['logAccounts(address[])void'] | OracleAccountContractArgs['tuple']['logAccounts(address[])void']>) {
    const result = await this.appClient.send.call(OracleAccountContractParamsFactory.logAccounts(params))
    return result.return as unknown as OracleAccountContractReturns['logAccounts(address[])void']
  }

  /**
   * Methods to access state for the current OracleAccountContract app
   */
  state = {
    /**
     * Methods to access global state for the current OracleAccountContract app
     */
    global: {
      /**
       * Get all current keyed values from global state
       */
      getAll: async (): Promise<Partial<Expand<GlobalKeysState>>> => {
        const result = await this.appClient.state.global.getAll()
        return {
          lastAccountId: result.lastAccountId,
        }
      },
      /**
       * Get the current value of the lastAccountId key in global state
       */
      lastAccountId: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("lastAccountId")) as bigint | undefined },
    },
    /**
     * Methods to access box state for the current OracleAccountContract app
     */
    box: {
      /**
       * Get all current keyed values from box state
       */
      getAll: async (): Promise<Partial<Expand<BoxKeysState>>> => {
        const result = await this.appClient.state.box.getAll()
        return {
        }
      },
      /**
       * Get values from the accounts map in box state
       */
      accounts: {
        /**
         * Get all current values of the accounts map in box state
         */
        getMap: async (): Promise<Map<string, OracleAccount>> => { return (await this.appClient.state.box.getMap("accounts")) as Map<string, OracleAccount> },
        /**
         * Get a current value of the accounts map by key from box state
         */
        value: async (key: string): Promise<OracleAccount | undefined> => { return await this.appClient.state.box.getMapValue("accounts", key) as OracleAccount | undefined },
      },
    },
  }

  public newGroup(): OracleAccountContractComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a getAccount(address)(uint32,(uint16,uint16)[]) method call against the OracleAccountContract contract
       */
      getAccount(params: CallParams<OracleAccountContractArgs['obj']['getAccount(address)(uint32,(uint16,uint16)[])'] | OracleAccountContractArgs['tuple']['getAccount(address)(uint32,(uint16,uint16)[])']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.getAccount(params)))
        resultMappers.push((v) => client.decodeReturnValue('getAccount(address)(uint32,(uint16,uint16)[])', v))
        return this
      },
      /**
       * Add a logAccounts(address[])void method call against the OracleAccountContract contract
       */
      logAccounts(params: CallParams<OracleAccountContractArgs['obj']['logAccounts(address[])void'] | OracleAccountContractArgs['tuple']['logAccounts(address[])void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.logAccounts(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a increaseBudget(uint64)void method call against the OracleAccountContract contract
       */
      increaseBudget(params: CallParams<OracleAccountContractArgs['obj']['increaseBudget(uint64)void'] | OracleAccountContractArgs['tuple']['increaseBudget(uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.increaseBudget(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a clear state call to the OracleAccountContract contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as OracleAccountContractComposer
  }
}
export type OracleAccountContractComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the getAccount(address)(uint32,(uint16,uint16)[]) ABI method.
   *
   * Get account ID if exists, else return 0
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  getAccount(params?: CallParams<OracleAccountContractArgs['obj']['getAccount(address)(uint32,(uint16,uint16)[])'] | OracleAccountContractArgs['tuple']['getAccount(address)(uint32,(uint16,uint16)[])']>): OracleAccountContractComposer<[...TReturns, OracleAccountContractReturns['getAccount(address)(uint32,(uint16,uint16)[])'] | undefined]>

  /**
   * Calls the logAccounts(address[])void ABI method.
   *
  * Log multiple accounts' IDs (or zero if not found)
  Used to fetch account>ID quickly off-chain

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  logAccounts(params?: CallParams<OracleAccountContractArgs['obj']['logAccounts(address[])void'] | OracleAccountContractArgs['tuple']['logAccounts(address[])void']>): OracleAccountContractComposer<[...TReturns, OracleAccountContractReturns['logAccounts(address[])void'] | undefined]>

  /**
   * Calls the increaseBudget(uint64)void ABI method.
   *
   * Utility to increase opcode budget by performing $itxns no-op itxns
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  increaseBudget(params?: CallParams<OracleAccountContractArgs['obj']['increaseBudget(uint64)void'] | OracleAccountContractArgs['tuple']['increaseBudget(uint64)void']>): OracleAccountContractComposer<[...TReturns, OracleAccountContractReturns['increaseBudget(uint64)void'] | undefined]>

  /**
   * Makes a clear_state call to an existing instance of the OracleAccountContract smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): OracleAccountContractComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): OracleAccountContractComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<OracleAccountContractComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<OracleAccountContractComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<OracleAccountContractComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<OracleAccountContractComposerResults<TReturns>>
}
export type OracleAccountContractComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

