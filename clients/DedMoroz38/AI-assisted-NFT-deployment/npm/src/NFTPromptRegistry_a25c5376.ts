/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"NFTPromptRegistry","structs":{},"methods":[{"name":"create_application","args":[],"returns":{"type":"void"},"actions":{"create":["NoOp"],"call":[]},"readonly":false,"desc":"Create the application and set the creator.","events":[],"recommendations":{}},{"name":"store_prompt","args":[{"type":"uint64","name":"asset_id"},{"type":"string","name":"prompt"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Store an AI prompt for a given NFT asset ID. Anyone can call this.","events":[],"recommendations":{}},{"name":"get_prompt","args":[{"type":"uint64","name":"asset_id"}],"returns":{"type":"string"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"desc":"Retrieve the AI prompt for a given NFT asset ID.","events":[],"recommendations":{}},{"name":"get_creator","args":[],"returns":{"type":"address"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"desc":"Get the creator address of the contract.","events":[],"recommendations":{}},{"name":"update_creator","args":[{"type":"address","name":"new_creator"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Transfer creator privileges to a new address.","events":[],"recommendations":{}}],"arcs":[22,28],"networks":{},"state":{"schema":{"global":{"ints":0,"bytes":2},"local":{"ints":0,"bytes":0}},"keys":{"global":{"creator":{"keyType":"AVMString","valueType":"address","key":"Y3JlYXRvcg=="},"prompt":{"keyType":"AVMString","valueType":"string","key":"cHJvbXB0"}},"local":{},"box":{}},"maps":{"global":{},"local":{},"box":{}}},"bareActions":{"create":[],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[40],"errorMessage":"OnCompletion must be NoOp"},{"pc":[171],"errorMessage":"Only creator can transfer ownership"},{"pc":[148,169],"errorMessage":"check self.creator exists"},{"pc":[138],"errorMessage":"check self.prompt exists"},{"pc":[122],"errorMessage":"invalid number of bytes for (len+utf8[])"},{"pc":[108,134],"errorMessage":"invalid number of bytes for uint64"},{"pc":[163],"errorMessage":"invalid number of bytes for uint8[32]"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"events":[]} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


/**
 * The argument types for the NftPromptRegistry contract
 */
export type NftPromptRegistryArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'create_application()void': Record<string, never>
    'store_prompt(uint64,string)void': {
      assetId: bigint | number
      prompt: string
    }
    'get_prompt(uint64)string': {
      assetId: bigint | number
    }
    'get_creator()address': Record<string, never>
    'update_creator(address)void': {
      newCreator: string
    }
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'create_application()void': []
    'store_prompt(uint64,string)void': [assetId: bigint | number, prompt: string]
    'get_prompt(uint64)string': [assetId: bigint | number]
    'get_creator()address': []
    'update_creator(address)void': [newCreator: string]
  }
}

/**
 * The return type for each method
 */
export type NftPromptRegistryReturns = {
  'create_application()void': void
  'store_prompt(uint64,string)void': void
  'get_prompt(uint64)string': string
  'get_creator()address': string
  'update_creator(address)void': void
}

/**
 * Defines the types of available calls and state of the NftPromptRegistry smart contract.
 */
export type NftPromptRegistryTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'create_application()void' | 'create_application', {
      argsObj: NftPromptRegistryArgs['obj']['create_application()void']
      argsTuple: NftPromptRegistryArgs['tuple']['create_application()void']
      returns: NftPromptRegistryReturns['create_application()void']
    }>
    & Record<'store_prompt(uint64,string)void' | 'store_prompt', {
      argsObj: NftPromptRegistryArgs['obj']['store_prompt(uint64,string)void']
      argsTuple: NftPromptRegistryArgs['tuple']['store_prompt(uint64,string)void']
      returns: NftPromptRegistryReturns['store_prompt(uint64,string)void']
    }>
    & Record<'get_prompt(uint64)string' | 'get_prompt', {
      argsObj: NftPromptRegistryArgs['obj']['get_prompt(uint64)string']
      argsTuple: NftPromptRegistryArgs['tuple']['get_prompt(uint64)string']
      returns: NftPromptRegistryReturns['get_prompt(uint64)string']
    }>
    & Record<'get_creator()address' | 'get_creator', {
      argsObj: NftPromptRegistryArgs['obj']['get_creator()address']
      argsTuple: NftPromptRegistryArgs['tuple']['get_creator()address']
      returns: NftPromptRegistryReturns['get_creator()address']
    }>
    & Record<'update_creator(address)void' | 'update_creator', {
      argsObj: NftPromptRegistryArgs['obj']['update_creator(address)void']
      argsTuple: NftPromptRegistryArgs['tuple']['update_creator(address)void']
      returns: NftPromptRegistryReturns['update_creator(address)void']
    }>
  /**
   * Defines the shape of the state of the application.
   */
  state: {
    global: {
      keys: {
        creator: string
        prompt: string
      }
      maps: {}
    }
  }
}

/**
 * Defines the possible abi call signatures.
 */
export type NftPromptRegistrySignatures = keyof NftPromptRegistryTypes['methods']
/**
 * Defines the possible abi call signatures for methods that return a non-void value.
 */
export type NftPromptRegistryNonVoidMethodSignatures = keyof NftPromptRegistryTypes['methods'] extends infer T ? T extends keyof NftPromptRegistryTypes['methods'] ? MethodReturn<T> extends void ? never : T  : never : never
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the NftPromptRegistry smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends NftPromptRegistrySignatures> = NftPromptRegistryTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the NftPromptRegistry smart contract to the method's return type
 */
export type MethodReturn<TSignature extends NftPromptRegistrySignatures> = NftPromptRegistryTypes['methods'][TSignature]['returns']

/**
 * Defines the shape of the keyed global state of the application.
 */
export type GlobalKeysState = NftPromptRegistryTypes['state']['global']['keys']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the NftPromptRegistry smart contract
 */
export abstract class NftPromptRegistryParamsFactory {
  /**
   * Constructs a no op call for the store_prompt(uint64,string)void ABI method
   *
   * Store an AI prompt for a given NFT asset ID. Anyone can call this.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static storePrompt(params: CallParams<NftPromptRegistryArgs['obj']['store_prompt(uint64,string)void'] | NftPromptRegistryArgs['tuple']['store_prompt(uint64,string)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'store_prompt(uint64,string)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.assetId, params.args.prompt],
    }
  }
  /**
   * Constructs a no op call for the get_prompt(uint64)string ABI method
   *
   * Retrieve the AI prompt for a given NFT asset ID.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static getPrompt(params: CallParams<NftPromptRegistryArgs['obj']['get_prompt(uint64)string'] | NftPromptRegistryArgs['tuple']['get_prompt(uint64)string']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'get_prompt(uint64)string' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.assetId],
    }
  }
  /**
   * Constructs a no op call for the get_creator()address ABI method
   *
   * Get the creator address of the contract.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static getCreator(params: CallParams<NftPromptRegistryArgs['obj']['get_creator()address'] | NftPromptRegistryArgs['tuple']['get_creator()address']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'get_creator()address' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the update_creator(address)void ABI method
   *
   * Transfer creator privileges to a new address.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static updateCreator(params: CallParams<NftPromptRegistryArgs['obj']['update_creator(address)void'] | NftPromptRegistryArgs['tuple']['update_creator(address)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'update_creator(address)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.newCreator],
    }
  }
}

/**
 * A client to make calls to the NFTPromptRegistry smart contract
 */
export class NftPromptRegistryClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `NftPromptRegistryClient`
   *
   * @param appClient An `AppClient` instance which has been created with the NftPromptRegistry app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `NftPromptRegistryClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Checks for decode errors on the given return value and maps the return value to the return type for the given method
   * @returns The typed return value or undefined if there was no value
   */
  decodeReturnValue<TSignature extends NftPromptRegistryNonVoidMethodSignatures>(method: TSignature, returnValue: ABIReturn | undefined) {
    return returnValue !== undefined ? getArc56ReturnValue<MethodReturn<TSignature>>(returnValue, this.appClient.getABIMethod(method), APP_SPEC.structs) : undefined
  }

  /**
   * Returns a new `NftPromptRegistryClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<NftPromptRegistryClient> {
    return new NftPromptRegistryClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `NftPromptRegistryClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<NftPromptRegistryClient> {
    return new NftPromptRegistryClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Makes a clear_state call to an existing instance of the NFTPromptRegistry smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the NFTPromptRegistry smart contract using the `store_prompt(uint64,string)void` ABI method.
     *
     * Store an AI prompt for a given NFT asset ID. Anyone can call this.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    storePrompt: (params: CallParams<NftPromptRegistryArgs['obj']['store_prompt(uint64,string)void'] | NftPromptRegistryArgs['tuple']['store_prompt(uint64,string)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(NftPromptRegistryParamsFactory.storePrompt(params))
    },

    /**
     * Makes a call to the NFTPromptRegistry smart contract using the `get_prompt(uint64)string` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * Retrieve the AI prompt for a given NFT asset ID.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    getPrompt: (params: CallParams<NftPromptRegistryArgs['obj']['get_prompt(uint64)string'] | NftPromptRegistryArgs['tuple']['get_prompt(uint64)string']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(NftPromptRegistryParamsFactory.getPrompt(params))
    },

    /**
     * Makes a call to the NFTPromptRegistry smart contract using the `get_creator()address` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * Get the creator address of the contract.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    getCreator: (params: CallParams<NftPromptRegistryArgs['obj']['get_creator()address'] | NftPromptRegistryArgs['tuple']['get_creator()address']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(NftPromptRegistryParamsFactory.getCreator(params))
    },

    /**
     * Makes a call to the NFTPromptRegistry smart contract using the `update_creator(address)void` ABI method.
     *
     * Transfer creator privileges to a new address.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    updateCreator: (params: CallParams<NftPromptRegistryArgs['obj']['update_creator(address)void'] | NftPromptRegistryArgs['tuple']['update_creator(address)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(NftPromptRegistryParamsFactory.updateCreator(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Makes a clear_state call to an existing instance of the NFTPromptRegistry smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the NFTPromptRegistry smart contract using the `store_prompt(uint64,string)void` ABI method.
     *
     * Store an AI prompt for a given NFT asset ID. Anyone can call this.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    storePrompt: (params: CallParams<NftPromptRegistryArgs['obj']['store_prompt(uint64,string)void'] | NftPromptRegistryArgs['tuple']['store_prompt(uint64,string)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(NftPromptRegistryParamsFactory.storePrompt(params))
    },

    /**
     * Makes a call to the NFTPromptRegistry smart contract using the `get_prompt(uint64)string` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * Retrieve the AI prompt for a given NFT asset ID.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    getPrompt: (params: CallParams<NftPromptRegistryArgs['obj']['get_prompt(uint64)string'] | NftPromptRegistryArgs['tuple']['get_prompt(uint64)string']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(NftPromptRegistryParamsFactory.getPrompt(params))
    },

    /**
     * Makes a call to the NFTPromptRegistry smart contract using the `get_creator()address` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * Get the creator address of the contract.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    getCreator: (params: CallParams<NftPromptRegistryArgs['obj']['get_creator()address'] | NftPromptRegistryArgs['tuple']['get_creator()address']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(NftPromptRegistryParamsFactory.getCreator(params))
    },

    /**
     * Makes a call to the NFTPromptRegistry smart contract using the `update_creator(address)void` ABI method.
     *
     * Transfer creator privileges to a new address.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    updateCreator: (params: CallParams<NftPromptRegistryArgs['obj']['update_creator(address)void'] | NftPromptRegistryArgs['tuple']['update_creator(address)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(NftPromptRegistryParamsFactory.updateCreator(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Makes a clear_state call to an existing instance of the NFTPromptRegistry smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the NFTPromptRegistry smart contract using the `store_prompt(uint64,string)void` ABI method.
     *
     * Store an AI prompt for a given NFT asset ID. Anyone can call this.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    storePrompt: async (params: CallParams<NftPromptRegistryArgs['obj']['store_prompt(uint64,string)void'] | NftPromptRegistryArgs['tuple']['store_prompt(uint64,string)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(NftPromptRegistryParamsFactory.storePrompt(params))
      return {...result, return: result.return as unknown as (undefined | NftPromptRegistryReturns['store_prompt(uint64,string)void'])}
    },

    /**
     * Makes a call to the NFTPromptRegistry smart contract using the `get_prompt(uint64)string` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * Retrieve the AI prompt for a given NFT asset ID.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    getPrompt: async (params: CallParams<NftPromptRegistryArgs['obj']['get_prompt(uint64)string'] | NftPromptRegistryArgs['tuple']['get_prompt(uint64)string']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(NftPromptRegistryParamsFactory.getPrompt(params))
      return {...result, return: result.return as unknown as (undefined | NftPromptRegistryReturns['get_prompt(uint64)string'])}
    },

    /**
     * Makes a call to the NFTPromptRegistry smart contract using the `get_creator()address` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * Get the creator address of the contract.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    getCreator: async (params: CallParams<NftPromptRegistryArgs['obj']['get_creator()address'] | NftPromptRegistryArgs['tuple']['get_creator()address']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(NftPromptRegistryParamsFactory.getCreator(params))
      return {...result, return: result.return as unknown as (undefined | NftPromptRegistryReturns['get_creator()address'])}
    },

    /**
     * Makes a call to the NFTPromptRegistry smart contract using the `update_creator(address)void` ABI method.
     *
     * Transfer creator privileges to a new address.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    updateCreator: async (params: CallParams<NftPromptRegistryArgs['obj']['update_creator(address)void'] | NftPromptRegistryArgs['tuple']['update_creator(address)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(NftPromptRegistryParamsFactory.updateCreator(params))
      return {...result, return: result.return as unknown as (undefined | NftPromptRegistryReturns['update_creator(address)void'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new NftPromptRegistryClient(this.appClient.clone(params))
  }

  /**
   * Makes a readonly (simulated) call to the NFTPromptRegistry smart contract using the `get_prompt(uint64)string` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * Retrieve the AI prompt for a given NFT asset ID.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async getPrompt(params: CallParams<NftPromptRegistryArgs['obj']['get_prompt(uint64)string'] | NftPromptRegistryArgs['tuple']['get_prompt(uint64)string']>) {
    const result = await this.appClient.send.call(NftPromptRegistryParamsFactory.getPrompt(params))
    return result.return as unknown as NftPromptRegistryReturns['get_prompt(uint64)string']
  }

  /**
   * Makes a readonly (simulated) call to the NFTPromptRegistry smart contract using the `get_creator()address` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * Get the creator address of the contract.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async getCreator(params: CallParams<NftPromptRegistryArgs['obj']['get_creator()address'] | NftPromptRegistryArgs['tuple']['get_creator()address']> = {args: []}) {
    const result = await this.appClient.send.call(NftPromptRegistryParamsFactory.getCreator(params))
    return result.return as unknown as NftPromptRegistryReturns['get_creator()address']
  }

  /**
   * Methods to access state for the current NFTPromptRegistry app
   */
  state = {
    /**
     * Methods to access global state for the current NFTPromptRegistry app
     */
    global: {
      /**
       * Get all current keyed values from global state
       */
      getAll: async (): Promise<Partial<Expand<GlobalKeysState>>> => {
        const result = await this.appClient.state.global.getAll()
        return {
          creator: result.creator,
          prompt: result.prompt,
        }
      },
      /**
       * Get the current value of the creator key in global state
       */
      creator: async (): Promise<string | undefined> => { return (await this.appClient.state.global.getValue("creator")) as string | undefined },
      /**
       * Get the current value of the prompt key in global state
       */
      prompt: async (): Promise<string | undefined> => { return (await this.appClient.state.global.getValue("prompt")) as string | undefined },
    },
  }

  public newGroup(): NftPromptRegistryComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a store_prompt(uint64,string)void method call against the NFTPromptRegistry contract
       */
      storePrompt(params: CallParams<NftPromptRegistryArgs['obj']['store_prompt(uint64,string)void'] | NftPromptRegistryArgs['tuple']['store_prompt(uint64,string)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.storePrompt(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a get_prompt(uint64)string method call against the NFTPromptRegistry contract
       */
      getPrompt(params: CallParams<NftPromptRegistryArgs['obj']['get_prompt(uint64)string'] | NftPromptRegistryArgs['tuple']['get_prompt(uint64)string']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.getPrompt(params)))
        resultMappers.push((v) => client.decodeReturnValue('get_prompt(uint64)string', v))
        return this
      },
      /**
       * Add a get_creator()address method call against the NFTPromptRegistry contract
       */
      getCreator(params: CallParams<NftPromptRegistryArgs['obj']['get_creator()address'] | NftPromptRegistryArgs['tuple']['get_creator()address']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.getCreator(params)))
        resultMappers.push((v) => client.decodeReturnValue('get_creator()address', v))
        return this
      },
      /**
       * Add a update_creator(address)void method call against the NFTPromptRegistry contract
       */
      updateCreator(params: CallParams<NftPromptRegistryArgs['obj']['update_creator(address)void'] | NftPromptRegistryArgs['tuple']['update_creator(address)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.updateCreator(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a clear state call to the NFTPromptRegistry contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as NftPromptRegistryComposer
  }
}
export type NftPromptRegistryComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the store_prompt(uint64,string)void ABI method.
   *
   * Store an AI prompt for a given NFT asset ID. Anyone can call this.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  storePrompt(params?: CallParams<NftPromptRegistryArgs['obj']['store_prompt(uint64,string)void'] | NftPromptRegistryArgs['tuple']['store_prompt(uint64,string)void']>): NftPromptRegistryComposer<[...TReturns, NftPromptRegistryReturns['store_prompt(uint64,string)void'] | undefined]>

  /**
   * Calls the get_prompt(uint64)string ABI method.
   *
   * Retrieve the AI prompt for a given NFT asset ID.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  getPrompt(params?: CallParams<NftPromptRegistryArgs['obj']['get_prompt(uint64)string'] | NftPromptRegistryArgs['tuple']['get_prompt(uint64)string']>): NftPromptRegistryComposer<[...TReturns, NftPromptRegistryReturns['get_prompt(uint64)string'] | undefined]>

  /**
   * Calls the get_creator()address ABI method.
   *
   * Get the creator address of the contract.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  getCreator(params?: CallParams<NftPromptRegistryArgs['obj']['get_creator()address'] | NftPromptRegistryArgs['tuple']['get_creator()address']>): NftPromptRegistryComposer<[...TReturns, NftPromptRegistryReturns['get_creator()address'] | undefined]>

  /**
   * Calls the update_creator(address)void ABI method.
   *
   * Transfer creator privileges to a new address.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  updateCreator(params?: CallParams<NftPromptRegistryArgs['obj']['update_creator(address)void'] | NftPromptRegistryArgs['tuple']['update_creator(address)void']>): NftPromptRegistryComposer<[...TReturns, NftPromptRegistryReturns['update_creator(address)void'] | undefined]>

  /**
   * Makes a clear_state call to an existing instance of the NFTPromptRegistry smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): NftPromptRegistryComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): NftPromptRegistryComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<NftPromptRegistryComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<NftPromptRegistryComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<NftPromptRegistryComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<NftPromptRegistryComposerResults<TReturns>>
}
export type NftPromptRegistryComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

