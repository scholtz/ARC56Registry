/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"VestingVault","structs":{},"methods":[{"name":"setupSchedule","args":[{"type":"uint64","name":"startTime"},{"type":"uint64","name":"amountPerStage"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}},{"name":"claim","args":[],"returns":{"type":"uint64"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}}],"arcs":[22,28],"desc":"And here’s our main contract class.\n\nIf you remember the Python example from earlier, this should feel familiar:\nwe’ve got a clean class, a couple of fields, a setup function, and a claim function.\n\nThe idea of this contract is simple:\nwe define a vesting schedule up front…\nand then later, the user calls `claim()` to withdraw any unlocked but unclaimed tokens.","networks":{},"state":{"schema":{"global":{"ints":0,"bytes":0},"local":{"ints":0,"bytes":0}},"keys":{"global":{},"local":{},"box":{"schedule":{"keyType":"AVMString","valueType":"(uint64,uint64,bool)[4]","key":"c2NoZWR1bGU=","desc":"Here's where we store the actual vesting schedule.\n\nWe’re using a Box because it lets us store larger or more complex data structures.\nAnd we’re using a FixedArray with exactly 4 elements.\n\nThis is perfect for a “static schedule” — we know the number of vesting steps,\nand we never push/splice or resize the array.\n\nThis also lines up with one of the course’s best practices:\n“Use StaticArray / FixedArray when the size is known.”"}}},"maps":{"global":{},"local":{},"box":{}}},"bareActions":{"create":["NoOp"],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[177],"errorMessage":"Box must have value"},{"pc":[28],"errorMessage":"OnCompletion must be NoOp"},{"pc":[61],"errorMessage":"OnCompletion must be NoOp && can only call when creating"},{"pc":[100,113,127,141,196,239],"errorMessage":"index access is out of bounds"},{"pc":[71,80],"errorMessage":"invalid number of bytes for arc4.uint64"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"events":[]} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


/**
 * The argument types for the VestingVault contract
 */
export type VestingVaultArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'setupSchedule(uint64,uint64)void': {
      startTime: bigint | number
      amountPerStage: bigint | number
    }
    'claim()uint64': Record<string, never>
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'setupSchedule(uint64,uint64)void': [startTime: bigint | number, amountPerStage: bigint | number]
    'claim()uint64': []
  }
}

/**
 * The return type for each method
 */
export type VestingVaultReturns = {
  'setupSchedule(uint64,uint64)void': void
  'claim()uint64': bigint
}

/**
 * Defines the types of available calls and state of the VestingVault smart contract.
 */
export type VestingVaultTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'setupSchedule(uint64,uint64)void' | 'setupSchedule', {
      argsObj: VestingVaultArgs['obj']['setupSchedule(uint64,uint64)void']
      argsTuple: VestingVaultArgs['tuple']['setupSchedule(uint64,uint64)void']
      returns: VestingVaultReturns['setupSchedule(uint64,uint64)void']
    }>
    & Record<'claim()uint64' | 'claim', {
      argsObj: VestingVaultArgs['obj']['claim()uint64']
      argsTuple: VestingVaultArgs['tuple']['claim()uint64']
      returns: VestingVaultReturns['claim()uint64']
    }>
  /**
   * Defines the shape of the state of the application.
   */
  state: {
    box: {
      keys: {
        /**
        * Here's where we store the actual vesting schedule.
        
        We’re using a Box because it lets us store larger or more complex data structures.
        And we’re using a FixedArray with exactly 4 elements.
        
        This is perfect for a “static schedule” — we know the number of vesting steps,
        and we never push/splice or resize the array.
        
        This also lines up with one of the course’s best practices:
        “Use StaticArray / FixedArray when the size is known.”

         */
        schedule: [[bigint, bigint, boolean], [bigint, bigint, boolean], [bigint, bigint, boolean], [bigint, bigint, boolean]]
      }
      maps: {}
    }
  }
}

/**
 * Defines the possible abi call signatures.
 */
export type VestingVaultSignatures = keyof VestingVaultTypes['methods']
/**
 * Defines the possible abi call signatures for methods that return a non-void value.
 */
export type VestingVaultNonVoidMethodSignatures = keyof VestingVaultTypes['methods'] extends infer T ? T extends keyof VestingVaultTypes['methods'] ? MethodReturn<T> extends void ? never : T  : never : never
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the VestingVault smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends VestingVaultSignatures> = VestingVaultTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the VestingVault smart contract to the method's return type
 */
export type MethodReturn<TSignature extends VestingVaultSignatures> = VestingVaultTypes['methods'][TSignature]['returns']

/**
 * Defines the shape of the keyed box state of the application.
 */
export type BoxKeysState = VestingVaultTypes['state']['box']['keys']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the VestingVault smart contract
 */
export abstract class VestingVaultParamsFactory {
  /**
   * Constructs a no op call for the setupSchedule(uint64,uint64)void ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static setupSchedule(params: CallParams<VestingVaultArgs['obj']['setupSchedule(uint64,uint64)void'] | VestingVaultArgs['tuple']['setupSchedule(uint64,uint64)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'setupSchedule(uint64,uint64)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.startTime, params.args.amountPerStage],
    }
  }
  /**
   * Constructs a no op call for the claim()uint64 ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static claim(params: CallParams<VestingVaultArgs['obj']['claim()uint64'] | VestingVaultArgs['tuple']['claim()uint64']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'claim()uint64' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
}

/**
 * A client to make calls to the VestingVault smart contract
 */
export class VestingVaultClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `VestingVaultClient`
   *
   * @param appClient An `AppClient` instance which has been created with the VestingVault app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `VestingVaultClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Checks for decode errors on the given return value and maps the return value to the return type for the given method
   * @returns The typed return value or undefined if there was no value
   */
  decodeReturnValue<TSignature extends VestingVaultNonVoidMethodSignatures>(method: TSignature, returnValue: ABIReturn | undefined) {
    return returnValue !== undefined ? getArc56ReturnValue<MethodReturn<TSignature>>(returnValue, this.appClient.getABIMethod(method), APP_SPEC.structs) : undefined
  }

  /**
   * Returns a new `VestingVaultClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<VestingVaultClient> {
    return new VestingVaultClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `VestingVaultClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<VestingVaultClient> {
    return new VestingVaultClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Makes a clear_state call to an existing instance of the VestingVault smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the VestingVault smart contract using the `setupSchedule(uint64,uint64)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    setupSchedule: (params: CallParams<VestingVaultArgs['obj']['setupSchedule(uint64,uint64)void'] | VestingVaultArgs['tuple']['setupSchedule(uint64,uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(VestingVaultParamsFactory.setupSchedule(params))
    },

    /**
     * Makes a call to the VestingVault smart contract using the `claim()uint64` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    claim: (params: CallParams<VestingVaultArgs['obj']['claim()uint64'] | VestingVaultArgs['tuple']['claim()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(VestingVaultParamsFactory.claim(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Makes a clear_state call to an existing instance of the VestingVault smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the VestingVault smart contract using the `setupSchedule(uint64,uint64)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    setupSchedule: (params: CallParams<VestingVaultArgs['obj']['setupSchedule(uint64,uint64)void'] | VestingVaultArgs['tuple']['setupSchedule(uint64,uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(VestingVaultParamsFactory.setupSchedule(params))
    },

    /**
     * Makes a call to the VestingVault smart contract using the `claim()uint64` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    claim: (params: CallParams<VestingVaultArgs['obj']['claim()uint64'] | VestingVaultArgs['tuple']['claim()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(VestingVaultParamsFactory.claim(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Makes a clear_state call to an existing instance of the VestingVault smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the VestingVault smart contract using the `setupSchedule(uint64,uint64)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    setupSchedule: async (params: CallParams<VestingVaultArgs['obj']['setupSchedule(uint64,uint64)void'] | VestingVaultArgs['tuple']['setupSchedule(uint64,uint64)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(VestingVaultParamsFactory.setupSchedule(params))
      return {...result, return: result.return as unknown as (undefined | VestingVaultReturns['setupSchedule(uint64,uint64)void'])}
    },

    /**
     * Makes a call to the VestingVault smart contract using the `claim()uint64` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    claim: async (params: CallParams<VestingVaultArgs['obj']['claim()uint64'] | VestingVaultArgs['tuple']['claim()uint64']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(VestingVaultParamsFactory.claim(params))
      return {...result, return: result.return as unknown as (undefined | VestingVaultReturns['claim()uint64'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new VestingVaultClient(this.appClient.clone(params))
  }

  /**
   * Methods to access state for the current VestingVault app
   */
  state = {
    /**
     * Methods to access box state for the current VestingVault app
     */
    box: {
      /**
       * Get all current keyed values from box state
       */
      getAll: async (): Promise<Partial<Expand<BoxKeysState>>> => {
        const result = await this.appClient.state.box.getAll()
        return {
          schedule: result.schedule,
        }
      },
      /**
       * Get the current value of the schedule key in box state
       */
      schedule: async (): Promise<[[bigint, bigint, boolean], [bigint, bigint, boolean], [bigint, bigint, boolean], [bigint, bigint, boolean]] | undefined> => { return (await this.appClient.state.box.getValue("schedule")) as [[bigint, bigint, boolean], [bigint, bigint, boolean], [bigint, bigint, boolean], [bigint, bigint, boolean]] | undefined },
    },
  }

  public newGroup(): VestingVaultComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a setupSchedule(uint64,uint64)void method call against the VestingVault contract
       */
      setupSchedule(params: CallParams<VestingVaultArgs['obj']['setupSchedule(uint64,uint64)void'] | VestingVaultArgs['tuple']['setupSchedule(uint64,uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.setupSchedule(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a claim()uint64 method call against the VestingVault contract
       */
      claim(params: CallParams<VestingVaultArgs['obj']['claim()uint64'] | VestingVaultArgs['tuple']['claim()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.claim(params)))
        resultMappers.push((v) => client.decodeReturnValue('claim()uint64', v))
        return this
      },
      /**
       * Add a clear state call to the VestingVault contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as VestingVaultComposer
  }
}
export type VestingVaultComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the setupSchedule(uint64,uint64)void ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  setupSchedule(params?: CallParams<VestingVaultArgs['obj']['setupSchedule(uint64,uint64)void'] | VestingVaultArgs['tuple']['setupSchedule(uint64,uint64)void']>): VestingVaultComposer<[...TReturns, VestingVaultReturns['setupSchedule(uint64,uint64)void'] | undefined]>

  /**
   * Calls the claim()uint64 ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  claim(params?: CallParams<VestingVaultArgs['obj']['claim()uint64'] | VestingVaultArgs['tuple']['claim()uint64']>): VestingVaultComposer<[...TReturns, VestingVaultReturns['claim()uint64'] | undefined]>

  /**
   * Makes a clear_state call to an existing instance of the VestingVault smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): VestingVaultComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): VestingVaultComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<VestingVaultComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<VestingVaultComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<VestingVaultComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<VestingVaultComposerResults<TReturns>>
}
export type VestingVaultComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

