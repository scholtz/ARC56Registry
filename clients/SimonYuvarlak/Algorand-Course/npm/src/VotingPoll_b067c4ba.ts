/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"VotingPoll","structs":{"VoteTally":[{"name":"yes","type":"uint64"},{"name":"no","type":"uint64"}]},"methods":[{"name":"create","args":[],"returns":{"type":"void"},"actions":{"create":["NoOp"],"call":[]},"readonly":false,"desc":"Creation entrypoint for the contract.\nThis runs exactly once when the application is created. Even though we already gave defaults in __init__, we explicitly set our global counters here to make the initialization logic very obvious.","events":[],"recommendations":{}},{"name":"opt_in","args":[],"returns":{"type":"void"},"actions":{"create":[],"call":["OptIn"]},"readonly":false,"desc":"Opt-in entrypoint.\nWhenever an account opts into this application and calls this method, we initialize its `has_voted` flag to 0 (meaning: they have not voted yet).","events":[],"recommendations":{}},{"name":"vote","args":[{"type":"string","name":"choice","desc":"\"yes\" or \"no\""}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Cast a vote for the given choice.","events":[],"recommendations":{}},{"name":"get_votes","args":[],"returns":{"type":"(uint64,uint64)","struct":"VoteTally","desc":"a NamedTuple (yes: UInt64, no: UInt64)"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Get the current vote tallies.","events":[],"recommendations":{}}],"arcs":[22,28],"desc":"\n    VotingPoll – a minimal, best‑practice Algorand Python contract.\n\n    This contract demonstrates:\n\n    1. EXPLICIT TYPES EVERYWHERE\n       - All on-chain state uses algopy.UInt64 / algopy.String,\n         never bare int/str.\n       - Method parameters and return values are typed.\n\n    2. FAMILIAR PYTHON CONSTRUCTS\n       - We still use __init__, NamedTuple, if/elif/else, and assert,\n         so the code reads like normal Python.\n\n    3. AVM RESTRICTIONS ARE RESPECTED\n       - The AVM doesn’t have a native bool type, so we model booleans\n         as UInt64(0) and UInt64(1).\n       - We use assert to handle invalid situations, which compiles\n         down to a safe AVM error.\n\n    4. STATIC TYPING PREVENTS ERRORS\n       - We declare state on self in __init__, so the compiler understands\n         each field’s type.\n       - Misusing APIs (for example, calling .get() on a UInt64 instead\n         of a LocalState) becomes a compile‑time error instead of a\n         hidden runtime bug.\n    ","networks":{},"state":{"schema":{"global":{"ints":2,"bytes":0},"local":{"ints":1,"bytes":0}},"keys":{"global":{"yes_votes":{"keyType":"AVMString","valueType":"AVMUint64","key":"eWVzX3ZvdGVz","desc":"Total number of 'yes' votes cast in this poll"},"no_votes":{"keyType":"AVMString","valueType":"AVMUint64","key":"bm9fdm90ZXM=","desc":"Total number of 'no' votes cast in this poll"}},"local":{"has_voted":{"keyType":"AVMString","valueType":"AVMUint64","key":"aGFzX3ZvdGVk","desc":"1 if this account has already voted in this poll, else 0"}},"box":{}},"maps":{"global":{},"local":{},"box":{}}},"bareActions":{"create":[],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[194],"errorMessage":"Invalid choice: must be 'yes' or 'no'"},{"pc":[63],"errorMessage":"OnCompletion must be NoOp"},{"pc":[112],"errorMessage":"OnCompletion must be OptIn && can only call when not creating"},{"pc":[162],"errorMessage":"You have already voted!"},{"pc":[198,214],"errorMessage":"check self.no_votes exists"},{"pc":[175,210],"errorMessage":"check self.yes_votes exists"},{"pc":[136],"errorMessage":"invalid array length header"},{"pc":[144],"errorMessage":"invalid number of bytes for arc4.dynamic_array<arc4.uint8>"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"events":[]} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


// Type definitions for ARC-56 structs

export type VoteTally = {
  yes: bigint,
  no: bigint
}


/**
 * Converts the ABI tuple representation of a VoteTally to the struct representation
 */
export function VoteTallyFromTuple(abiTuple: [bigint, bigint]) {
  return getABIStructFromABITuple(abiTuple, APP_SPEC.structs.VoteTally, APP_SPEC.structs) as VoteTally
}

/**
 * The argument types for the VotingPoll contract
 */
export type VotingPollArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'create()void': Record<string, never>
    'opt_in()void': Record<string, never>
    'vote(string)void': {
      /**
       * "yes" or "no"
       */
      choice: string
    }
    'get_votes()(uint64,uint64)': Record<string, never>
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'create()void': []
    'opt_in()void': []
    'vote(string)void': [choice: string]
    'get_votes()(uint64,uint64)': []
  }
}

/**
 * The return type for each method
 */
export type VotingPollReturns = {
  'create()void': void
  'opt_in()void': void
  'vote(string)void': void
  'get_votes()(uint64,uint64)': VoteTally
}

/**
 * Defines the types of available calls and state of the VotingPoll smart contract.
 */
export type VotingPollTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'create()void' | 'create', {
      argsObj: VotingPollArgs['obj']['create()void']
      argsTuple: VotingPollArgs['tuple']['create()void']
      returns: VotingPollReturns['create()void']
    }>
    & Record<'opt_in()void' | 'opt_in', {
      argsObj: VotingPollArgs['obj']['opt_in()void']
      argsTuple: VotingPollArgs['tuple']['opt_in()void']
      returns: VotingPollReturns['opt_in()void']
    }>
    & Record<'vote(string)void' | 'vote', {
      argsObj: VotingPollArgs['obj']['vote(string)void']
      argsTuple: VotingPollArgs['tuple']['vote(string)void']
      returns: VotingPollReturns['vote(string)void']
    }>
    & Record<'get_votes()(uint64,uint64)' | 'get_votes', {
      argsObj: VotingPollArgs['obj']['get_votes()(uint64,uint64)']
      argsTuple: VotingPollArgs['tuple']['get_votes()(uint64,uint64)']
      /**
       * a NamedTuple (yes: UInt64, no: UInt64)
       */
      returns: VotingPollReturns['get_votes()(uint64,uint64)']
    }>
  /**
   * Defines the shape of the state of the application.
   */
  state: {
    global: {
      keys: {
        /**
         * Total number of 'yes' votes cast in this poll
         */
        yesVotes: bigint
        /**
         * Total number of 'no' votes cast in this poll
         */
        noVotes: bigint
      }
      maps: {}
    }
    local: {
      keys: {
        /**
         * 1 if this account has already voted in this poll, else 0
         */
        hasVoted: bigint
      }
      maps: {}
    }
  }
}

/**
 * Defines the possible abi call signatures.
 */
export type VotingPollSignatures = keyof VotingPollTypes['methods']
/**
 * Defines the possible abi call signatures for methods that return a non-void value.
 */
export type VotingPollNonVoidMethodSignatures = keyof VotingPollTypes['methods'] extends infer T ? T extends keyof VotingPollTypes['methods'] ? MethodReturn<T> extends void ? never : T  : never : never
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the VotingPoll smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends VotingPollSignatures> = VotingPollTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the VotingPoll smart contract to the method's return type
 */
export type MethodReturn<TSignature extends VotingPollSignatures> = VotingPollTypes['methods'][TSignature]['returns']

/**
 * Defines the shape of the keyed global state of the application.
 */
export type GlobalKeysState = VotingPollTypes['state']['global']['keys']

/**
 * Defines the shape of the keyed local state of the application.
 */
export type LocalKeysState = VotingPollTypes['state']['local']['keys']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the VotingPoll smart contract
 */
export abstract class VotingPollParamsFactory {
  /**
   * Gets available optIn ABI call param factories
   */
  static get optIn() {
    return {
      /**
       * Constructs opt-in ABI call params for the VotingPoll smart contract using the opt_in()void ABI method
       *
       * @param params Parameters for the call
       * @returns An `AppClientMethodCallParams` object for the call
       */
      optIn(params: CallParams<VotingPollArgs['obj']['opt_in()void'] | VotingPollArgs['tuple']['opt_in()void']>): AppClientMethodCallParams {
        return {
          ...params,
          method: 'opt_in()void' as const,
          args: Array.isArray(params.args) ? params.args : [],
        }
      },
    }
  }

  /**
   * Constructs a no op call for the vote(string)void ABI method
   *
   * Cast a vote for the given choice.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static vote(params: CallParams<VotingPollArgs['obj']['vote(string)void'] | VotingPollArgs['tuple']['vote(string)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'vote(string)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.choice],
    }
  }
  /**
   * Constructs a no op call for the get_votes()(uint64,uint64) ABI method
   *
   * Get the current vote tallies.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static getVotes(params: CallParams<VotingPollArgs['obj']['get_votes()(uint64,uint64)'] | VotingPollArgs['tuple']['get_votes()(uint64,uint64)']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'get_votes()(uint64,uint64)' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
}

/**
 * A client to make calls to the VotingPoll smart contract
 */
export class VotingPollClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `VotingPollClient`
   *
   * @param appClient An `AppClient` instance which has been created with the VotingPoll app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `VotingPollClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Checks for decode errors on the given return value and maps the return value to the return type for the given method
   * @returns The typed return value or undefined if there was no value
   */
  decodeReturnValue<TSignature extends VotingPollNonVoidMethodSignatures>(method: TSignature, returnValue: ABIReturn | undefined) {
    return returnValue !== undefined ? getArc56ReturnValue<MethodReturn<TSignature>>(returnValue, this.appClient.getABIMethod(method), APP_SPEC.structs) : undefined
  }

  /**
   * Returns a new `VotingPollClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<VotingPollClient> {
    return new VotingPollClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `VotingPollClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<VotingPollClient> {
    return new VotingPollClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Gets available optIn methods
     */
    optIn: {
      /**
       * Opts the user into an existing instance of the VotingPoll smart contract using the `opt_in()void` ABI method.
       *
      * Opt-in entrypoint.
      Whenever an account opts into this application and calls this method, we initialize its `has_voted` flag to 0 (meaning: they have not voted yet).

       *
       * @param params The params for the smart contract call
       * @returns The optIn params
       */
      optIn: (params: CallParams<VotingPollArgs['obj']['opt_in()void'] | VotingPollArgs['tuple']['opt_in()void']> = {args: []}) => {
        return this.appClient.params.optIn(VotingPollParamsFactory.optIn.optIn(params))
      },

    },

    /**
     * Makes a clear_state call to an existing instance of the VotingPoll smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the VotingPoll smart contract using the `vote(string)void` ABI method.
     *
     * Cast a vote for the given choice.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    vote: (params: CallParams<VotingPollArgs['obj']['vote(string)void'] | VotingPollArgs['tuple']['vote(string)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(VotingPollParamsFactory.vote(params))
    },

    /**
     * Makes a call to the VotingPoll smart contract using the `get_votes()(uint64,uint64)` ABI method.
     *
     * Get the current vote tallies.
     *
     * @param params The params for the smart contract call
     * @returns The call params: a NamedTuple (yes: UInt64, no: UInt64)
     */
    getVotes: (params: CallParams<VotingPollArgs['obj']['get_votes()(uint64,uint64)'] | VotingPollArgs['tuple']['get_votes()(uint64,uint64)']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(VotingPollParamsFactory.getVotes(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Gets available optIn methods
     */
    optIn: {
      /**
       * Opts the user into an existing instance of the VotingPoll smart contract using the `opt_in()void` ABI method.
       *
      * Opt-in entrypoint.
      Whenever an account opts into this application and calls this method, we initialize its `has_voted` flag to 0 (meaning: they have not voted yet).

       *
       * @param params The params for the smart contract call
       * @returns The optIn transaction
       */
      optIn: (params: CallParams<VotingPollArgs['obj']['opt_in()void'] | VotingPollArgs['tuple']['opt_in()void']> = {args: []}) => {
        return this.appClient.createTransaction.optIn(VotingPollParamsFactory.optIn.optIn(params))
      },

    },

    /**
     * Makes a clear_state call to an existing instance of the VotingPoll smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the VotingPoll smart contract using the `vote(string)void` ABI method.
     *
     * Cast a vote for the given choice.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    vote: (params: CallParams<VotingPollArgs['obj']['vote(string)void'] | VotingPollArgs['tuple']['vote(string)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(VotingPollParamsFactory.vote(params))
    },

    /**
     * Makes a call to the VotingPoll smart contract using the `get_votes()(uint64,uint64)` ABI method.
     *
     * Get the current vote tallies.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction: a NamedTuple (yes: UInt64, no: UInt64)
     */
    getVotes: (params: CallParams<VotingPollArgs['obj']['get_votes()(uint64,uint64)'] | VotingPollArgs['tuple']['get_votes()(uint64,uint64)']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(VotingPollParamsFactory.getVotes(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Gets available optIn methods
     */
    optIn: {
      /**
       * Opts the user into an existing instance of the VotingPoll smart contract using the `opt_in()void` ABI method.
       *
      * Opt-in entrypoint.
      Whenever an account opts into this application and calls this method, we initialize its `has_voted` flag to 0 (meaning: they have not voted yet).

       *
       * @param params The params for the smart contract call
       * @returns The optIn result
       */
      optIn: async (params: CallParams<VotingPollArgs['obj']['opt_in()void'] | VotingPollArgs['tuple']['opt_in()void']> & SendParams = {args: []}) => {
        const result = await this.appClient.send.optIn(VotingPollParamsFactory.optIn.optIn(params))
        return {...result, return: result.return as unknown as (undefined | VotingPollReturns['opt_in()void'])}
      },

    },

    /**
     * Makes a clear_state call to an existing instance of the VotingPoll smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the VotingPoll smart contract using the `vote(string)void` ABI method.
     *
     * Cast a vote for the given choice.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    vote: async (params: CallParams<VotingPollArgs['obj']['vote(string)void'] | VotingPollArgs['tuple']['vote(string)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(VotingPollParamsFactory.vote(params))
      return {...result, return: result.return as unknown as (undefined | VotingPollReturns['vote(string)void'])}
    },

    /**
     * Makes a call to the VotingPoll smart contract using the `get_votes()(uint64,uint64)` ABI method.
     *
     * Get the current vote tallies.
     *
     * @param params The params for the smart contract call
     * @returns The call result: a NamedTuple (yes: UInt64, no: UInt64)
     */
    getVotes: async (params: CallParams<VotingPollArgs['obj']['get_votes()(uint64,uint64)'] | VotingPollArgs['tuple']['get_votes()(uint64,uint64)']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(VotingPollParamsFactory.getVotes(params))
      return {...result, return: result.return as unknown as (undefined | VotingPollReturns['get_votes()(uint64,uint64)'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new VotingPollClient(this.appClient.clone(params))
  }

  /**
   * Methods to access state for the current VotingPoll app
   */
  state = {
    /**
     * Methods to access global state for the current VotingPoll app
     */
    global: {
      /**
       * Get all current keyed values from global state
       */
      getAll: async (): Promise<Partial<Expand<GlobalKeysState>>> => {
        const result = await this.appClient.state.global.getAll()
        return {
          yesVotes: result.yes_votes,
          noVotes: result.no_votes,
        }
      },
      /**
       * Get the current value of the yes_votes key in global state
       */
      yesVotes: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("yes_votes")) as bigint | undefined },
      /**
       * Get the current value of the no_votes key in global state
       */
      noVotes: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("no_votes")) as bigint | undefined },
    },
    /**
     * Methods to access local state for the current VotingPoll app
     */
    local: (address: string | Address) => {
      const encodedAddress = typeof address === 'string' ? address : encodeAddress(address.publicKey)
      return {
        /**
         * Get all current keyed values from local state
         */
        getAll: async (): Promise<Partial<Expand<LocalKeysState>>> => {
          const result = await this.appClient.state.local(encodedAddress).getAll()
          return {
            hasVoted: result.has_voted,
          }
        },
        /**
         * Get the current value of the has_voted key in local state
         */
        hasVoted: async (): Promise<bigint | undefined> => { return (await this.appClient.state.local(encodedAddress).getValue("has_voted")) as bigint | undefined },
      }
    },
  }

  public newGroup(): VotingPollComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a vote(string)void method call against the VotingPoll contract
       */
      vote(params: CallParams<VotingPollArgs['obj']['vote(string)void'] | VotingPollArgs['tuple']['vote(string)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.vote(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a get_votes()(uint64,uint64) method call against the VotingPoll contract
       */
      getVotes(params: CallParams<VotingPollArgs['obj']['get_votes()(uint64,uint64)'] | VotingPollArgs['tuple']['get_votes()(uint64,uint64)']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.getVotes(params)))
        resultMappers.push((v) => client.decodeReturnValue('get_votes()(uint64,uint64)', v))
        return this
      },
      get optIn() {
        return {
          optIn: (params: CallParams<VotingPollArgs['obj']['opt_in()void'] | VotingPollArgs['tuple']['opt_in()void']>) => {
            promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.optIn.optIn(params)))
            resultMappers.push(undefined)
            return this
          },
        }
      },
      /**
       * Add a clear state call to the VotingPoll contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as VotingPollComposer
  }
}
export type VotingPollComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the vote(string)void ABI method.
   *
   * Cast a vote for the given choice.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  vote(params?: CallParams<VotingPollArgs['obj']['vote(string)void'] | VotingPollArgs['tuple']['vote(string)void']>): VotingPollComposer<[...TReturns, VotingPollReturns['vote(string)void'] | undefined]>

  /**
   * Calls the get_votes()(uint64,uint64) ABI method.
   *
   * Get the current vote tallies.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  getVotes(params?: CallParams<VotingPollArgs['obj']['get_votes()(uint64,uint64)'] | VotingPollArgs['tuple']['get_votes()(uint64,uint64)']>): VotingPollComposer<[...TReturns, VotingPollReturns['get_votes()(uint64,uint64)'] | undefined]>

  /**
   * Gets available optIn methods
   */
  readonly optIn: {
    /**
     * Opts the user into an existing instance of the VotingPoll smart contract using the opt_in()void ABI method.
     *
     * @param args The arguments for the smart contract call
     * @param params Any additional parameters for the call
     * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
     */
    optIn(params?: CallParams<VotingPollArgs['obj']['opt_in()void'] | VotingPollArgs['tuple']['opt_in()void']>): VotingPollComposer<[...TReturns, VotingPollReturns['opt_in()void'] | undefined]>
  }

  /**
   * Makes a clear_state call to an existing instance of the VotingPoll smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): VotingPollComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): VotingPollComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<VotingPollComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<VotingPollComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<VotingPollComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<VotingPollComposerResults<TReturns>>
}
export type VotingPollComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

