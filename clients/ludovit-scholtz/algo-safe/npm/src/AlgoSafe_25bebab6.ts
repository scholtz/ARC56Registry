/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"AlgoSafe","structs":{"Object78E87395":[{"name":"groupId","type":"uint64"},{"name":"account","type":"address"}],"Object9F556C53":[{"name":"proposalId","type":"uint64"},{"name":"account","type":"address"}],"ObjectAE8BF7D4":[{"name":"custodianGroupId","type":"uint64"},{"name":"assetId","type":"uint64"}],"AdminChange":[{"name":"changeType","type":"uint64"},{"name":"targetGroupId","type":"uint64"},{"name":"groupName","type":"string"},{"name":"memberAddr","type":"address"},{"name":"memberType","type":"uint64"},{"name":"memberLabel","type":"string"},{"name":"threshold","type":"uint64"},{"name":"adminPrivileges","type":"uint64"},{"name":"allowedActions","type":"uint64"},{"name":"limitAssetId","type":"uint64"},{"name":"dailyLimit","type":"uint64"},{"name":"monthlyLimit","type":"uint64"},{"name":"cooldownRounds","type":"uint64"},{"name":"activeFlag","type":"uint64"},{"name":"guardAmount","type":"uint64"}],"Approval":[{"name":"signer","type":"address"},{"name":"round","type":"uint64"}],"AssetGuard":[{"name":"createdRound","type":"uint64"},{"name":"lockedAmount","type":"uint64"}],"GroupSeed":[{"name":"name","type":"string"},{"name":"threshold","type":"uint64"},{"name":"adminPrivileges","type":"uint64"},{"name":"allowedActions","type":"uint64"},{"name":"limitAssetId","type":"uint64"},{"name":"dailyLimit","type":"uint64"},{"name":"monthlyLimit","type":"uint64"},{"name":"cooldownRounds","type":"uint64"},{"name":"groupType","type":"uint64"}],"Member":[{"name":"accountType","type":"uint64"},{"name":"label","type":"string"},{"name":"addr","type":"address"}],"Proposal":[{"name":"groupId","type":"uint64"},{"name":"status","type":"uint64"},{"name":"payloadType","type":"uint64"},{"name":"approvalsCount","type":"uint64"},{"name":"threshold","type":"uint64"},{"name":"expiryRound","type":"uint64"},{"name":"proposer","type":"address"},{"name":"numPayloads","type":"uint64"},{"name":"epochAtCreation","type":"uint64"},{"name":"payloadVersion","type":"uint64"},{"name":"totalTxns","type":"uint64"}],"RekeyedAddress":[{"name":"label","type":"string"},{"name":"addedRound","type":"uint64"}],"SignerGroup":[{"name":"name","type":"string"},{"name":"threshold","type":"uint64"},{"name":"memberCount","type":"uint64"},{"name":"adminPrivileges","type":"uint64"},{"name":"allowedActions","type":"uint64"},{"name":"limitAssetId","type":"uint64"},{"name":"dailyLimit","type":"uint64"},{"name":"dailyUsage","type":"uint64"},{"name":"dailyPeriodStart","type":"uint64"},{"name":"monthlyLimit","type":"uint64"},{"name":"monthlyUsage","type":"uint64"},{"name":"monthlyPeriodStart","type":"uint64"},{"name":"cooldownRounds","type":"uint64"},{"name":"lastExecutionRound","type":"uint64"},{"name":"membershipEpoch","type":"uint64"},{"name":"active","type":"uint64"},{"name":"groupType","type":"uint64"},{"name":"guardCount","type":"uint64"}]},"methods":[{"name":"createApplication","args":[{"type":"string","name":"name"},{"type":"uint64","name":"validatorAppId"}],"returns":{"type":"void"},"actions":{"create":["NoOp"],"call":[]},"readonly":false,"desc":"createApplication — called once on deployment. Initialises global state.\nThe creator is the only account that can bootstrap the safe.\n\nvalidatorAppId is pinned by BYTECODE HASH, not by trust in the deployer:\nthe approval program of the given app must hash to the compiled\nAlgoSafeTxnValidator (see validator-hash.generated.ts). Because that\nbytecode rejects UpdateApplication and DeleteApplication, the program\nbehind the ID is immutable, so this single check holds for the safe's\nwhole lifetime.","events":[{"name":"SafeCreated","args":[{"type":"string","name":"name"},{"type":"address","name":"creator"}]}],"recommendations":{}},{"name":"bootstrap","args":[{"type":"string","name":"groupName"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"bootstrap — simple one-call bootstrap for a fresh safe.\nCreates group 1 with full admin+action privileges and adds the creator as\nthe sole member. Incompatible with bootstrapGroup (the two paths cannot mix).","events":[{"name":"MemberAdded","args":[{"type":"uint64","name":"groupId"},{"type":"address","name":"member"},{"type":"uint64","name":"accountType"}]},{"name":"GroupCreated","args":[{"type":"uint64","name":"groupId"},{"type":"string","name":"name"},{"type":"uint64","name":"threshold"}]}],"recommendations":{}},{"name":"bootstrapGroup","args":[{"type":"(string,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64)","struct":"GroupSeed","name":"seed"},{"type":"(address,uint64,string)[]","name":"members"},{"type":"uint64","name":"ensureBudgetValue"}],"returns":{"type":"uint64"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"bootstrapGroup — clone-friendly bootstrap; may be called multiple times before\nfinalizeBootstrap(). Each call creates one group with its full member set.\nUsed by deployClonedSafe() to mirror the predecessor safe's governance structure\ninto a newly deployed contract before activating it.","events":[{"name":"MemberAdded","args":[{"type":"uint64","name":"groupId"},{"type":"address","name":"member"},{"type":"uint64","name":"accountType"}]},{"name":"GroupCreated","args":[{"type":"uint64","name":"groupId"},{"type":"string","name":"name"},{"type":"uint64","name":"threshold"}]}],"recommendations":{}},{"name":"bootstrapRekeyedAddress","args":[{"type":"address","name":"addr"},{"type":"string","name":"label"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"bootstrapRekeyedAddress — seed the rekeyed-address registry during cloning.\nOnly callable before finalizeBootstrap(); mirrors entries from the predecessor.","events":[{"name":"RekeyedAddressAdded","args":[{"type":"address","name":"addr"},{"type":"string","name":"label"}]}],"recommendations":{}},{"name":"finalizeBootstrap","args":[],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"finalizeBootstrap — activates a cloned safe seeded via bootstrapGroup.\nRequires at least one active group with PRIV_GROUP to prevent lockout.\nAfter this call _newProposal succeeds and normal governance begins.","events":[],"recommendations":{}},{"name":"proposeTransactionGroup","args":[{"type":"uint64","name":"groupId"},{"type":"(uint64,byte[])[]","name":"payload"},{"type":"uint64","name":"expiryRound"},{"type":"bool","name":"execute"},{"type":"uint64","name":"ensureBudgetValue"}],"returns":{"type":"uint64"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"proposeTransactionGroup — create and optionally immediately execute a transaction\nproposal. The caller must be a member of groupId.\n\nexecute=true skips the approval phase (valid only when threshold=1 and the\nproposer is the sole member, i.e. the proposal is instantly READY).\n\nThe payload is stored in chunk slot 1; further chunks can be appended via\nappendTransactionGroupPayload before the first independent approval.","events":[{"name":"ProposalCreated","args":[{"type":"uint64","name":"proposalId"},{"type":"uint64","name":"groupId"},{"type":"uint64","name":"payloadType"},{"type":"address","name":"proposer"}]},{"name":"ProposalApproved","args":[{"type":"uint64","name":"proposalId"},{"type":"address","name":"signer"},{"type":"uint64","name":"approvalsCount"}]},{"name":"CustodianGroupCreated","args":[{"type":"uint64","name":"groupId"},{"type":"string","name":"name"},{"type":"uint64","name":"threshold"}]},{"name":"GroupCreated","args":[{"type":"uint64","name":"groupId"},{"type":"string","name":"name"},{"type":"uint64","name":"threshold"}]},{"name":"MemberAdded","args":[{"type":"uint64","name":"groupId"},{"type":"address","name":"member"},{"type":"uint64","name":"accountType"}]},{"name":"MemberRemoved","args":[{"type":"uint64","name":"groupId"},{"type":"address","name":"member"}]},{"name":"RekeyedAddressAdded","args":[{"type":"address","name":"addr"},{"type":"string","name":"label"}]},{"name":"RekeyedAddressRemoved","args":[{"type":"address","name":"addr"}]},{"name":"GroupUpdated","args":[{"type":"uint64","name":"groupId"}]},{"name":"CustodianGroupDissolved","args":[{"type":"uint64","name":"groupId"}]},{"name":"AssetGuardSet","args":[{"type":"uint64","name":"custodianGroupId"},{"type":"uint64","name":"assetId"},{"type":"uint64","name":"lockedAmount"}]},{"name":"AssetGuardRemoved","args":[{"type":"uint64","name":"custodianGroupId"},{"type":"uint64","name":"assetId"}]},{"name":"SafePaused","args":[{"type":"uint64","name":"paused"}]},{"name":"ProposalExecuted","args":[{"type":"uint64","name":"proposalId"}]}],"recommendations":{}},{"name":"appendTransactionGroupPayload","args":[{"type":"uint64","name":"proposalId"},{"type":"uint64","name":"payloadIndex"},{"type":"(uint64,byte[])[]","name":"payload"},{"type":"uint64","name":"ensureBudgetValue"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"appendTransactionGroupPayload — add an additional payload chunk to an existing\ntransaction proposal. Only the original proposer may append, and only while\nno independent approver has signed yet (approvalsCount === 1 means only the\nauto-approval from _newProposal). Each append bumps payloadVersion so any\nsigner that has already approved sees \"payload changed since review\" on their\nnext call and must re-approve the new version.\n\nPrevents appending to expired proposals (blocks extending validity window).","events":[],"recommendations":{}},{"name":"proposeAdminChange","args":[{"type":"uint64","name":"groupId"},{"type":"(uint64,uint64,string,address,uint64,string,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64)","struct":"AdminChange","name":"change"},{"type":"uint64","name":"expiryRound"},{"type":"uint64","name":"ensureBudgetValue"}],"returns":{"type":"uint64"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"proposeAdminChange — create a governance change proposal.\n\nRouting:\n  ADM_DISSOLVE_CUSTODIAN — the proposing group must be the custodian group\n    itself. Admins cannot force-dissolve a custodian.\n  All other change types — the proposing group must hold the relevant admin\n    privilege (PRIV_GROUP or PRIV_POLICY via _assertPrivilegeForChange).\n\nPayload-level correctness (e.g. guard existence, target group type) is\nintentionally deferred to execution time in _applyAdminChange. This saves\n~700 bytes of compiled binary by not duplicating the check logic at proposal\ntime, at the cost of wasted box storage if an invalid proposal passes approval.\n\nThe cooldown cap is asserted here (before the box is written) so that a\nbad cooldown value cannot be stored and later cause an arithmetic overflow\nin _executeProposalInternal even if the group approves the proposal.","events":[{"name":"ProposalCreated","args":[{"type":"uint64","name":"proposalId"},{"type":"uint64","name":"groupId"},{"type":"uint64","name":"payloadType"},{"type":"address","name":"proposer"}]},{"name":"ProposalApproved","args":[{"type":"uint64","name":"proposalId"},{"type":"address","name":"signer"},{"type":"uint64","name":"approvalsCount"}]}],"recommendations":{}},{"name":"approveProposal","args":[{"type":"uint64","name":"proposalId"},{"type":"uint64","name":"expectedPayloadVersion"},{"type":"uint64","name":"ensureBudgetValue"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"approveProposal — add the caller's approval to an open proposal.\n\nexpectedPayloadVersion must match the proposal's live payloadVersion; this\nbinds the approval to the exact payload content the signer reviewed and\nprevents a bait-and-switch attack where the proposer appends a different\nchunk after other signers have already approved.\n\nAlso asserts that the group's membershipEpoch hasn't changed since proposal\ncreation — removing a compromised member invalidates all pending approvals.","events":[{"name":"ProposalApproved","args":[{"type":"uint64","name":"proposalId"},{"type":"address","name":"signer"},{"type":"uint64","name":"approvalsCount"}]}],"recommendations":{}},{"name":"executeProposal","args":[{"type":"uint64","name":"proposalId"},{"type":"uint64","name":"ensureBudgetValue"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"executeProposal — execute a READY proposal (threshold met, not expired).","events":[{"name":"CustodianGroupCreated","args":[{"type":"uint64","name":"groupId"},{"type":"string","name":"name"},{"type":"uint64","name":"threshold"}]},{"name":"GroupCreated","args":[{"type":"uint64","name":"groupId"},{"type":"string","name":"name"},{"type":"uint64","name":"threshold"}]},{"name":"MemberAdded","args":[{"type":"uint64","name":"groupId"},{"type":"address","name":"member"},{"type":"uint64","name":"accountType"}]},{"name":"MemberRemoved","args":[{"type":"uint64","name":"groupId"},{"type":"address","name":"member"}]},{"name":"RekeyedAddressAdded","args":[{"type":"address","name":"addr"},{"type":"string","name":"label"}]},{"name":"RekeyedAddressRemoved","args":[{"type":"address","name":"addr"}]},{"name":"GroupUpdated","args":[{"type":"uint64","name":"groupId"}]},{"name":"CustodianGroupDissolved","args":[{"type":"uint64","name":"groupId"}]},{"name":"AssetGuardSet","args":[{"type":"uint64","name":"custodianGroupId"},{"type":"uint64","name":"assetId"},{"type":"uint64","name":"lockedAmount"}]},{"name":"AssetGuardRemoved","args":[{"type":"uint64","name":"custodianGroupId"},{"type":"uint64","name":"assetId"}]},{"name":"SafePaused","args":[{"type":"uint64","name":"paused"}]},{"name":"ProposalExecuted","args":[{"type":"uint64","name":"proposalId"}]}],"recommendations":{}},{"name":"cancelProposal","args":[{"type":"uint64","name":"proposalId"},{"type":"uint64","name":"ensureBudgetValue"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"cancelProposal — cancel an open proposal. Either the original proposer\nor any current group member may cancel.","events":[{"name":"ProposalCancelled","args":[{"type":"uint64","name":"proposalId"}]}],"recommendations":{}},{"name":"pruneProposal","args":[{"type":"uint64","name":"proposalId"},{"type":"uint64","name":"ensureBudgetValue"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"pruneProposal — delete terminal proposal boxes to reclaim MBR.\nOnly callable on EXECUTED or CANCELLED proposals after their expiryRound.\nRemoves the proposal box, all payload chunk boxes, and the admin-change box.","events":[],"recommendations":{}}],"arcs":[22,28],"networks":{},"state":{"schema":{"global":{"ints":7,"bytes":3},"local":{"ints":0,"bytes":0}},"keys":{"global":{"name":{"keyType":"AVMString","valueType":"AVMString","key":"bmFtZQ=="},"creator":{"keyType":"AVMString","valueType":"address","key":"Y3JlYXRvcg=="},"bootstrapped":{"keyType":"AVMString","valueType":"AVMUint64","key":"Ym9vdA=="},"nextGroupId":{"keyType":"AVMString","valueType":"AVMUint64","key":"bmdpZA=="},"nextProposalId":{"keyType":"AVMString","valueType":"AVMUint64","key":"bnBpZA=="},"groupCount":{"keyType":"AVMString","valueType":"AVMUint64","key":"Z2NudA=="},"paused":{"keyType":"AVMString","valueType":"AVMUint64","key":"cGF1c2Vk"},"version":{"keyType":"AVMString","valueType":"AVMString","key":"dmVy"},"activePrivGroupCount":{"keyType":"AVMString","valueType":"AVMUint64","key":"YXBnYw=="},"validatorApp":{"keyType":"AVMString","valueType":"AVMUint64","key":"dmFwcA=="}},"local":{},"box":{}},"maps":{"global":{},"local":{},"box":{"groups":{"keyType":"uint64","valueType":"SignerGroup","prefix":"Zw=="},"members":{"keyType":"Object78E87395","valueType":"Member","prefix":"bQ=="},"proposals":{"keyType":"uint64","valueType":"Proposal","prefix":"cA=="},"approvals":{"keyType":"Object9F556C53","valueType":"Approval","prefix":"YQ=="},"transactionGroups":{"keyType":"uint64","valueType":"(uint64,byte[])[]","prefix":"dHhn"},"adminPayloads":{"keyType":"uint64","valueType":"AdminChange","prefix":"ZHA="},"rekeyedAddresses":{"keyType":"address","valueType":"RekeyedAddress","prefix":"cg=="},"assetGuards":{"keyType":"ObjectAE8BF7D4","valueType":"AssetGuard","prefix":"YWc="}}}},"bareActions":{"create":[],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[1115,1322,1593,1736,1927,2053,2691,2728,2911,3673,4945,5161,5411,5533,5691],"errorMessage":"Box must have value"},{"pc":[3335],"errorMessage":"Bytes has valid prefix"},{"pc":[126],"errorMessage":"OnCompletion must be NoOp"},{"pc":[831],"errorMessage":"address required"},{"pc":[5041],"errorMessage":"already a member"},{"pc":[1809],"errorMessage":"already approved"},{"pc":[488,767,825,900],"errorMessage":"already bootstrapped"},{"pc":[842,5238],"errorMessage":"already registered"},{"pc":[3202],"errorMessage":"app args total length exceeded"},{"pc":[3042],"errorMessage":"app update not supported"},{"pc":[3025],"errorMessage":"appId required"},{"pc":[3019],"errorMessage":"appl not allowed"},{"pc":[494],"errorMessage":"bootstrapGroup already used; cannot mix bootstrap paths"},{"pc":[1638],"errorMessage":"can only dissolve own custodian group"},{"pc":[4981],"errorMessage":"can only dissolve own group"},{"pc":[1382],"errorMessage":"cannot modify payload after independent approval"},{"pc":[5541],"errorMessage":"cannot set privileges on custodian groups"},{"pc":[479,486,492,758,765,816,823,891,898,904,1068,1282,2303,2580,2603,2792,3221,5605,5658,5763,5818,5909,5916,6190,6211,6560,6597,6635,6876,6935],"errorMessage":"check GlobalState exists"},{"pc":[1609],"errorMessage":"cooldown cap"},{"pc":[2273],"errorMessage":"cooldown too large"},{"pc":[2290],"errorMessage":"custodian groups cannot have admin privileges"},{"pc":[3525],"errorMessage":"daily limit exceeded"},{"pc":[2510],"errorMessage":"duplicate member"},{"pc":[1304],"errorMessage":"empty payload"},{"pc":[1059],"errorMessage":"empty tx group"},{"pc":[3450],"errorMessage":"exceeds guard allocation"},{"pc":[6199],"errorMessage":"expiry must be in the future"},{"pc":[6500],"errorMessage":"group admin required"},{"pc":[2829],"errorMessage":"group cooldown not elapsed"},{"pc":[1087,1599,2735],"errorMessage":"group disabled"},{"pc":[1824,2779],"errorMessage":"group membership changed since proposal creation"},{"pc":[2655],"errorMessage":"group not found"},{"pc":[6108],"errorMessage":"guard not found"},{"pc":[2470,2984,3163,3247,3746,3893,4036,4159,4230,4364,4515,4796],"errorMessage":"index access is out of bounds"},{"pc":[2249],"errorMessage":"invalid actions"},{"pc":[679,961,1206],"errorMessage":"invalid array encoding"},{"pc":[295,462,637,649,707,799,931,988,1123,1176,1233,1518,1540,2217,2967,3063,3082,3096,3110,3230,3729,3876,4019,4142,4208,4259,4302,4347,4498,4779,6445],"errorMessage":"invalid array length header"},{"pc":[1028],"errorMessage":"invalid number of bytes for arc4.bool"},{"pc":[302,469,806],"errorMessage":"invalid number of bytes for arc4.dynamic_array<arc4.uint8>"},{"pc":[728],"errorMessage":"invalid number of bytes for arc4.dynamic_array<smart_contracts/algo_safe/contract.algo.ts::MemberSeed>"},{"pc":[1009,1254],"errorMessage":"invalid number of bytes for arc4.dynamic_array<smart_contracts/algo_safe/contract.algo.ts::SafeTxn>"},{"pc":[793],"errorMessage":"invalid number of bytes for arc4.static_array<arc4.uint8, 32>"},{"pc":[3340],"errorMessage":"invalid number of bytes for arc4.tuple<arc4.uint64,arc4.uint64,arc4.uint64,arc4.static_array<arc4.uint8, 32>>"},{"pc":[313,736,923,1017,1040,1158,1168,1262,1492,1553,1562,1688,1697,1706,1845,1854,1885,1894,2011,2020],"errorMessage":"invalid number of bytes for arc4.uint64"},{"pc":[1545],"errorMessage":"invalid number of bytes for smart_contracts/algo_safe/contract.algo.ts::AdminChange"},{"pc":[642],"errorMessage":"invalid number of bytes for smart_contracts/algo_safe/contract.algo.ts::GroupSeed"},{"pc":[3038],"errorMessage":"invalid onCompletion"},{"pc":[2261],"errorMessage":"invalid privileges"},{"pc":[1301],"errorMessage":"invalid slot"},{"pc":[629],"errorMessage":"invalid tail pointer at index 0 of ((len+utf8[]),uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64)"},{"pc":[984,1229],"errorMessage":"invalid tail pointer at index 1 of (uint64,(len+uint8[]))"},{"pc":[1510],"errorMessage":"invalid tail pointer at index 2 of (uint64,uint64,(len+utf8[]),uint8[32],uint64,(len+utf8[]),uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64)"},{"pc":[703],"errorMessage":"invalid tail pointer at index 2 of (uint8[32],uint64,(len+utf8[]))"},{"pc":[1532],"errorMessage":"invalid tail pointer at index 5 of (uint64,uint64,(len+utf8[]),uint8[32],uint64,(len+utf8[]),uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64)"},{"pc":[969,1214],"errorMessage":"invalid tail pointer for (len+(uint64,(len+uint8[]))[])"},{"pc":[687],"errorMessage":"invalid tail pointer for (len+(uint8[32],uint64,(len+utf8[]))[])"},{"pc":[2237],"errorMessage":"invalid threshold"},{"pc":[624,698,979,1224,1504,1527],"errorMessage":"invalid tuple encoding"},{"pc":[2494,5024],"errorMessage":"member required"},{"pc":[5901],"errorMessage":"memberAddr must be the last member"},{"pc":[2219],"errorMessage":"members required"},{"pc":[3588],"errorMessage":"monthly limit exceeded"},{"pc":[5559,5738],"errorMessage":"must keep at least one active group admin"},{"pc":[905],"errorMessage":"need an active group admin"},{"pc":[6644],"errorMessage":"new group starts with one member"},{"pc":[3437],"errorMessage":"no guard for custodian+asset"},{"pc":[1794,2114,2666],"errorMessage":"not a group member"},{"pc":[5154],"errorMessage":"not a member"},{"pc":[1329],"errorMessage":"not a tx group"},{"pc":[1755],"errorMessage":"not approvable"},{"pc":[1973],"errorMessage":"not authorised to cancel"},{"pc":[6193],"errorMessage":"not bootstrapped"},{"pc":[1946],"errorMessage":"not cancellable"},{"pc":[2697],"errorMessage":"not ready to execute"},{"pc":[5319],"errorMessage":"not registered"},{"pc":[2086],"errorMessage":"not yet expired"},{"pc":[481,760,818,893],"errorMessage":"only creator can bootstrap"},{"pc":[1630],"errorMessage":"only custodian groups can propose self-dissolution"},{"pc":[1375],"errorMessage":"only proposer can append"},{"pc":[1775],"errorMessage":"payload changed since review"},{"pc":[6489],"errorMessage":"policy admin required"},{"pc":[1360,1767,2706],"errorMessage":"proposal expired"},{"pc":[1348],"errorMessage":"proposal not pending"},{"pc":[2074],"errorMessage":"proposal not terminal"},{"pc":[4971],"errorMessage":"proposing group must be custodian"},{"pc":[5872],"errorMessage":"remove all guards before dissolving"},{"pc":[5881],"errorMessage":"remove other members before dissolving"},{"pc":[1070,1284,2794],"errorMessage":"safe paused"},{"pc":[3029],"errorMessage":"self-call not allowed"},{"pc":[5972,6091],"errorMessage":"target must be a custodian group"},{"pc":[5372],"errorMessage":"threshold exceeds members"},{"pc":[5363],"errorMessage":"threshold must be at least 1"},{"pc":[2765],"errorMessage":"threshold not met"},{"pc":[3122],"errorMessage":"too many accounts"},{"pc":[3116],"errorMessage":"too many app args"},{"pc":[3127],"errorMessage":"too many foreign apps"},{"pc":[3131],"errorMessage":"too many foreign assets"},{"pc":[3138],"errorMessage":"too many references"},{"pc":[1063],"errorMessage":"too many txs"},{"pc":[1424],"errorMessage":"too many txs across chunks"},{"pc":[318],"errorMessage":"validator app not found"},{"pc":[355],"errorMessage":"validator bytecode mismatch"},{"pc":[5180],"errorMessage":"would drop below threshold"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"events":[{"name":"SafeCreated","args":[{"type":"string","name":"name"},{"type":"address","name":"creator"}]},{"name":"MemberAdded","args":[{"type":"uint64","name":"groupId"},{"type":"address","name":"member"},{"type":"uint64","name":"accountType"}]},{"name":"GroupCreated","args":[{"type":"uint64","name":"groupId"},{"type":"string","name":"name"},{"type":"uint64","name":"threshold"}]},{"name":"RekeyedAddressAdded","args":[{"type":"address","name":"addr"},{"type":"string","name":"label"}]},{"name":"ProposalCreated","args":[{"type":"uint64","name":"proposalId"},{"type":"uint64","name":"groupId"},{"type":"uint64","name":"payloadType"},{"type":"address","name":"proposer"}]},{"name":"ProposalApproved","args":[{"type":"uint64","name":"proposalId"},{"type":"address","name":"signer"},{"type":"uint64","name":"approvalsCount"}]},{"name":"CustodianGroupCreated","args":[{"type":"uint64","name":"groupId"},{"type":"string","name":"name"},{"type":"uint64","name":"threshold"}]},{"name":"MemberRemoved","args":[{"type":"uint64","name":"groupId"},{"type":"address","name":"member"}]},{"name":"RekeyedAddressRemoved","args":[{"type":"address","name":"addr"}]},{"name":"GroupUpdated","args":[{"type":"uint64","name":"groupId"}]},{"name":"CustodianGroupDissolved","args":[{"type":"uint64","name":"groupId"}]},{"name":"AssetGuardSet","args":[{"type":"uint64","name":"custodianGroupId"},{"type":"uint64","name":"assetId"},{"type":"uint64","name":"lockedAmount"}]},{"name":"AssetGuardRemoved","args":[{"type":"uint64","name":"custodianGroupId"},{"type":"uint64","name":"assetId"}]},{"name":"SafePaused","args":[{"type":"uint64","name":"paused"}]},{"name":"ProposalExecuted","args":[{"type":"uint64","name":"proposalId"}]},{"name":"ProposalCancelled","args":[{"type":"uint64","name":"proposalId"}]}]} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


// Type definitions for ARC-56 structs

export type Object78E87395 = {
  groupId: bigint,
  account: string
}


/**
 * Converts the ABI tuple representation of a Object78E87395 to the struct representation
 */
export function Object78E87395FromTuple(abiTuple: [bigint, string]) {
  return getABIStructFromABITuple(abiTuple, APP_SPEC.structs.Object78E87395, APP_SPEC.structs) as Object78E87395
}

export type Object9F556C53 = {
  proposalId: bigint,
  account: string
}


/**
 * Converts the ABI tuple representation of a Object9F556C53 to the struct representation
 */
export function Object9F556C53FromTuple(abiTuple: [bigint, string]) {
  return getABIStructFromABITuple(abiTuple, APP_SPEC.structs.Object9F556C53, APP_SPEC.structs) as Object9F556C53
}

export type ObjectAe8Bf7D4 = {
  custodianGroupId: bigint,
  assetId: bigint
}


/**
 * Converts the ABI tuple representation of a ObjectAE8BF7D4 to the struct representation
 */
export function ObjectAe8Bf7D4FromTuple(abiTuple: [bigint, bigint]) {
  return getABIStructFromABITuple(abiTuple, APP_SPEC.structs.ObjectAE8BF7D4, APP_SPEC.structs) as ObjectAe8Bf7D4
}

export type AdminChange = {
  changeType: bigint,
  targetGroupId: bigint,
  groupName: string,
  memberAddr: string,
  memberType: bigint,
  memberLabel: string,
  threshold: bigint,
  adminPrivileges: bigint,
  allowedActions: bigint,
  limitAssetId: bigint,
  dailyLimit: bigint,
  monthlyLimit: bigint,
  cooldownRounds: bigint,
  activeFlag: bigint,
  guardAmount: bigint
}


/**
 * Converts the ABI tuple representation of a AdminChange to the struct representation
 */
export function AdminChangeFromTuple(abiTuple: [bigint, bigint, string, string, bigint, string, bigint, bigint, bigint, bigint, bigint, bigint, bigint, bigint, bigint]) {
  return getABIStructFromABITuple(abiTuple, APP_SPEC.structs.AdminChange, APP_SPEC.structs) as AdminChange
}

export type Approval = {
  signer: string,
  round: bigint
}


/**
 * Converts the ABI tuple representation of a Approval to the struct representation
 */
export function ApprovalFromTuple(abiTuple: [string, bigint]) {
  return getABIStructFromABITuple(abiTuple, APP_SPEC.structs.Approval, APP_SPEC.structs) as Approval
}

export type AssetGuard = {
  createdRound: bigint,
  lockedAmount: bigint
}


/**
 * Converts the ABI tuple representation of a AssetGuard to the struct representation
 */
export function AssetGuardFromTuple(abiTuple: [bigint, bigint]) {
  return getABIStructFromABITuple(abiTuple, APP_SPEC.structs.AssetGuard, APP_SPEC.structs) as AssetGuard
}

export type GroupSeed = {
  name: string,
  threshold: bigint,
  adminPrivileges: bigint,
  allowedActions: bigint,
  limitAssetId: bigint,
  dailyLimit: bigint,
  monthlyLimit: bigint,
  cooldownRounds: bigint,
  groupType: bigint
}


/**
 * Converts the ABI tuple representation of a GroupSeed to the struct representation
 */
export function GroupSeedFromTuple(abiTuple: [string, bigint, bigint, bigint, bigint, bigint, bigint, bigint, bigint]) {
  return getABIStructFromABITuple(abiTuple, APP_SPEC.structs.GroupSeed, APP_SPEC.structs) as GroupSeed
}

export type Member = {
  accountType: bigint,
  label: string,
  addr: string
}


/**
 * Converts the ABI tuple representation of a Member to the struct representation
 */
export function MemberFromTuple(abiTuple: [bigint, string, string]) {
  return getABIStructFromABITuple(abiTuple, APP_SPEC.structs.Member, APP_SPEC.structs) as Member
}

export type Proposal = {
  groupId: bigint,
  status: bigint,
  payloadType: bigint,
  approvalsCount: bigint,
  threshold: bigint,
  expiryRound: bigint,
  proposer: string,
  numPayloads: bigint,
  epochAtCreation: bigint,
  payloadVersion: bigint,
  totalTxns: bigint
}


/**
 * Converts the ABI tuple representation of a Proposal to the struct representation
 */
export function ProposalFromTuple(abiTuple: [bigint, bigint, bigint, bigint, bigint, bigint, string, bigint, bigint, bigint, bigint]) {
  return getABIStructFromABITuple(abiTuple, APP_SPEC.structs.Proposal, APP_SPEC.structs) as Proposal
}

export type RekeyedAddress = {
  label: string,
  addedRound: bigint
}


/**
 * Converts the ABI tuple representation of a RekeyedAddress to the struct representation
 */
export function RekeyedAddressFromTuple(abiTuple: [string, bigint]) {
  return getABIStructFromABITuple(abiTuple, APP_SPEC.structs.RekeyedAddress, APP_SPEC.structs) as RekeyedAddress
}

export type SignerGroup = {
  name: string,
  threshold: bigint,
  memberCount: bigint,
  adminPrivileges: bigint,
  allowedActions: bigint,
  limitAssetId: bigint,
  dailyLimit: bigint,
  dailyUsage: bigint,
  dailyPeriodStart: bigint,
  monthlyLimit: bigint,
  monthlyUsage: bigint,
  monthlyPeriodStart: bigint,
  cooldownRounds: bigint,
  lastExecutionRound: bigint,
  membershipEpoch: bigint,
  active: bigint,
  groupType: bigint,
  guardCount: bigint
}


/**
 * Converts the ABI tuple representation of a SignerGroup to the struct representation
 */
export function SignerGroupFromTuple(abiTuple: [string, bigint, bigint, bigint, bigint, bigint, bigint, bigint, bigint, bigint, bigint, bigint, bigint, bigint, bigint, bigint, bigint, bigint]) {
  return getABIStructFromABITuple(abiTuple, APP_SPEC.structs.SignerGroup, APP_SPEC.structs) as SignerGroup
}

/**
 * The argument types for the AlgoSafe contract
 */
export type AlgoSafeArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'createApplication(string,uint64)void': {
      name: string
      validatorAppId: bigint | number
    }
    'bootstrap(string)void': {
      groupName: string
    }
    'bootstrapGroup((string,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64),(address,uint64,string)[],uint64)uint64': {
      seed: GroupSeed
      members: [string, bigint | number, string][]
      ensureBudgetValue: bigint | number
    }
    'bootstrapRekeyedAddress(address,string)void': {
      addr: string
      label: string
    }
    'finalizeBootstrap()void': Record<string, never>
    'proposeTransactionGroup(uint64,(uint64,byte[])[],uint64,bool,uint64)uint64': {
      groupId: bigint | number
      payload: [bigint | number, Uint8Array][]
      expiryRound: bigint | number
      execute: boolean
      ensureBudgetValue: bigint | number
    }
    'appendTransactionGroupPayload(uint64,uint64,(uint64,byte[])[],uint64)void': {
      proposalId: bigint | number
      payloadIndex: bigint | number
      payload: [bigint | number, Uint8Array][]
      ensureBudgetValue: bigint | number
    }
    'proposeAdminChange(uint64,(uint64,uint64,string,address,uint64,string,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64),uint64,uint64)uint64': {
      groupId: bigint | number
      change: AdminChange
      expiryRound: bigint | number
      ensureBudgetValue: bigint | number
    }
    'approveProposal(uint64,uint64,uint64)void': {
      proposalId: bigint | number
      expectedPayloadVersion: bigint | number
      ensureBudgetValue: bigint | number
    }
    'executeProposal(uint64,uint64)void': {
      proposalId: bigint | number
      ensureBudgetValue: bigint | number
    }
    'cancelProposal(uint64,uint64)void': {
      proposalId: bigint | number
      ensureBudgetValue: bigint | number
    }
    'pruneProposal(uint64,uint64)void': {
      proposalId: bigint | number
      ensureBudgetValue: bigint | number
    }
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'createApplication(string,uint64)void': [name: string, validatorAppId: bigint | number]
    'bootstrap(string)void': [groupName: string]
    'bootstrapGroup((string,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64),(address,uint64,string)[],uint64)uint64': [seed: GroupSeed, members: [string, bigint | number, string][], ensureBudgetValue: bigint | number]
    'bootstrapRekeyedAddress(address,string)void': [addr: string, label: string]
    'finalizeBootstrap()void': []
    'proposeTransactionGroup(uint64,(uint64,byte[])[],uint64,bool,uint64)uint64': [groupId: bigint | number, payload: [bigint | number, Uint8Array][], expiryRound: bigint | number, execute: boolean, ensureBudgetValue: bigint | number]
    'appendTransactionGroupPayload(uint64,uint64,(uint64,byte[])[],uint64)void': [proposalId: bigint | number, payloadIndex: bigint | number, payload: [bigint | number, Uint8Array][], ensureBudgetValue: bigint | number]
    'proposeAdminChange(uint64,(uint64,uint64,string,address,uint64,string,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64),uint64,uint64)uint64': [groupId: bigint | number, change: AdminChange, expiryRound: bigint | number, ensureBudgetValue: bigint | number]
    'approveProposal(uint64,uint64,uint64)void': [proposalId: bigint | number, expectedPayloadVersion: bigint | number, ensureBudgetValue: bigint | number]
    'executeProposal(uint64,uint64)void': [proposalId: bigint | number, ensureBudgetValue: bigint | number]
    'cancelProposal(uint64,uint64)void': [proposalId: bigint | number, ensureBudgetValue: bigint | number]
    'pruneProposal(uint64,uint64)void': [proposalId: bigint | number, ensureBudgetValue: bigint | number]
  }
}

/**
 * The return type for each method
 */
export type AlgoSafeReturns = {
  'createApplication(string,uint64)void': void
  'bootstrap(string)void': void
  'bootstrapGroup((string,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64),(address,uint64,string)[],uint64)uint64': bigint
  'bootstrapRekeyedAddress(address,string)void': void
  'finalizeBootstrap()void': void
  'proposeTransactionGroup(uint64,(uint64,byte[])[],uint64,bool,uint64)uint64': bigint
  'appendTransactionGroupPayload(uint64,uint64,(uint64,byte[])[],uint64)void': void
  'proposeAdminChange(uint64,(uint64,uint64,string,address,uint64,string,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64),uint64,uint64)uint64': bigint
  'approveProposal(uint64,uint64,uint64)void': void
  'executeProposal(uint64,uint64)void': void
  'cancelProposal(uint64,uint64)void': void
  'pruneProposal(uint64,uint64)void': void
}

/**
 * Defines the types of available calls and state of the AlgoSafe smart contract.
 */
export type AlgoSafeTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'createApplication(string,uint64)void' | 'createApplication', {
      argsObj: AlgoSafeArgs['obj']['createApplication(string,uint64)void']
      argsTuple: AlgoSafeArgs['tuple']['createApplication(string,uint64)void']
      returns: AlgoSafeReturns['createApplication(string,uint64)void']
    }>
    & Record<'bootstrap(string)void' | 'bootstrap', {
      argsObj: AlgoSafeArgs['obj']['bootstrap(string)void']
      argsTuple: AlgoSafeArgs['tuple']['bootstrap(string)void']
      returns: AlgoSafeReturns['bootstrap(string)void']
    }>
    & Record<'bootstrapGroup((string,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64),(address,uint64,string)[],uint64)uint64' | 'bootstrapGroup', {
      argsObj: AlgoSafeArgs['obj']['bootstrapGroup((string,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64),(address,uint64,string)[],uint64)uint64']
      argsTuple: AlgoSafeArgs['tuple']['bootstrapGroup((string,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64),(address,uint64,string)[],uint64)uint64']
      returns: AlgoSafeReturns['bootstrapGroup((string,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64),(address,uint64,string)[],uint64)uint64']
    }>
    & Record<'bootstrapRekeyedAddress(address,string)void' | 'bootstrapRekeyedAddress', {
      argsObj: AlgoSafeArgs['obj']['bootstrapRekeyedAddress(address,string)void']
      argsTuple: AlgoSafeArgs['tuple']['bootstrapRekeyedAddress(address,string)void']
      returns: AlgoSafeReturns['bootstrapRekeyedAddress(address,string)void']
    }>
    & Record<'finalizeBootstrap()void' | 'finalizeBootstrap', {
      argsObj: AlgoSafeArgs['obj']['finalizeBootstrap()void']
      argsTuple: AlgoSafeArgs['tuple']['finalizeBootstrap()void']
      returns: AlgoSafeReturns['finalizeBootstrap()void']
    }>
    & Record<'proposeTransactionGroup(uint64,(uint64,byte[])[],uint64,bool,uint64)uint64' | 'proposeTransactionGroup', {
      argsObj: AlgoSafeArgs['obj']['proposeTransactionGroup(uint64,(uint64,byte[])[],uint64,bool,uint64)uint64']
      argsTuple: AlgoSafeArgs['tuple']['proposeTransactionGroup(uint64,(uint64,byte[])[],uint64,bool,uint64)uint64']
      returns: AlgoSafeReturns['proposeTransactionGroup(uint64,(uint64,byte[])[],uint64,bool,uint64)uint64']
    }>
    & Record<'appendTransactionGroupPayload(uint64,uint64,(uint64,byte[])[],uint64)void' | 'appendTransactionGroupPayload', {
      argsObj: AlgoSafeArgs['obj']['appendTransactionGroupPayload(uint64,uint64,(uint64,byte[])[],uint64)void']
      argsTuple: AlgoSafeArgs['tuple']['appendTransactionGroupPayload(uint64,uint64,(uint64,byte[])[],uint64)void']
      returns: AlgoSafeReturns['appendTransactionGroupPayload(uint64,uint64,(uint64,byte[])[],uint64)void']
    }>
    & Record<'proposeAdminChange(uint64,(uint64,uint64,string,address,uint64,string,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64),uint64,uint64)uint64' | 'proposeAdminChange', {
      argsObj: AlgoSafeArgs['obj']['proposeAdminChange(uint64,(uint64,uint64,string,address,uint64,string,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64),uint64,uint64)uint64']
      argsTuple: AlgoSafeArgs['tuple']['proposeAdminChange(uint64,(uint64,uint64,string,address,uint64,string,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64),uint64,uint64)uint64']
      returns: AlgoSafeReturns['proposeAdminChange(uint64,(uint64,uint64,string,address,uint64,string,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64),uint64,uint64)uint64']
    }>
    & Record<'approveProposal(uint64,uint64,uint64)void' | 'approveProposal', {
      argsObj: AlgoSafeArgs['obj']['approveProposal(uint64,uint64,uint64)void']
      argsTuple: AlgoSafeArgs['tuple']['approveProposal(uint64,uint64,uint64)void']
      returns: AlgoSafeReturns['approveProposal(uint64,uint64,uint64)void']
    }>
    & Record<'executeProposal(uint64,uint64)void' | 'executeProposal', {
      argsObj: AlgoSafeArgs['obj']['executeProposal(uint64,uint64)void']
      argsTuple: AlgoSafeArgs['tuple']['executeProposal(uint64,uint64)void']
      returns: AlgoSafeReturns['executeProposal(uint64,uint64)void']
    }>
    & Record<'cancelProposal(uint64,uint64)void' | 'cancelProposal', {
      argsObj: AlgoSafeArgs['obj']['cancelProposal(uint64,uint64)void']
      argsTuple: AlgoSafeArgs['tuple']['cancelProposal(uint64,uint64)void']
      returns: AlgoSafeReturns['cancelProposal(uint64,uint64)void']
    }>
    & Record<'pruneProposal(uint64,uint64)void' | 'pruneProposal', {
      argsObj: AlgoSafeArgs['obj']['pruneProposal(uint64,uint64)void']
      argsTuple: AlgoSafeArgs['tuple']['pruneProposal(uint64,uint64)void']
      returns: AlgoSafeReturns['pruneProposal(uint64,uint64)void']
    }>
  /**
   * Defines the shape of the state of the application.
   */
  state: {
    global: {
      keys: {
        name: string
        creator: string
        bootstrapped: bigint
        nextGroupId: bigint
        nextProposalId: bigint
        groupCount: bigint
        paused: bigint
        version: string
        activePrivGroupCount: bigint
        validatorApp: bigint
      }
      maps: {}
    }
    box: {
      keys: {}
      maps: {
        groups: Map<bigint | number, SignerGroup>
        members: Map<Object78E87395, Member>
        proposals: Map<bigint | number, Proposal>
        approvals: Map<Object9F556C53, Approval>
        transactionGroups: Map<bigint | number, [bigint, Uint8Array][]>
        adminPayloads: Map<bigint | number, AdminChange>
        rekeyedAddresses: Map<string, RekeyedAddress>
        assetGuards: Map<ObjectAe8Bf7D4, AssetGuard>
      }
    }
  }
}

/**
 * Defines the possible abi call signatures.
 */
export type AlgoSafeSignatures = keyof AlgoSafeTypes['methods']
/**
 * Defines the possible abi call signatures for methods that return a non-void value.
 */
export type AlgoSafeNonVoidMethodSignatures = keyof AlgoSafeTypes['methods'] extends infer T ? T extends keyof AlgoSafeTypes['methods'] ? MethodReturn<T> extends void ? never : T  : never : never
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the AlgoSafe smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends AlgoSafeSignatures> = AlgoSafeTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the AlgoSafe smart contract to the method's return type
 */
export type MethodReturn<TSignature extends AlgoSafeSignatures> = AlgoSafeTypes['methods'][TSignature]['returns']

/**
 * Defines the shape of the keyed global state of the application.
 */
export type GlobalKeysState = AlgoSafeTypes['state']['global']['keys']

/**
 * Defines the shape of the keyed box state of the application.
 */
export type BoxKeysState = AlgoSafeTypes['state']['box']['keys']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the AlgoSafe smart contract
 */
export abstract class AlgoSafeParamsFactory {
  /**
   * Constructs a no op call for the bootstrap(string)void ABI method
   *
  * bootstrap — simple one-call bootstrap for a fresh safe.
  Creates group 1 with full admin+action privileges and adds the creator as
  the sole member. Incompatible with bootstrapGroup (the two paths cannot mix).

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static bootstrap(params: CallParams<AlgoSafeArgs['obj']['bootstrap(string)void'] | AlgoSafeArgs['tuple']['bootstrap(string)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'bootstrap(string)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.groupName],
    }
  }
  /**
   * Constructs a no op call for the bootstrapGroup((string,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64),(address,uint64,string)[],uint64)uint64 ABI method
   *
  * bootstrapGroup — clone-friendly bootstrap; may be called multiple times before
  finalizeBootstrap(). Each call creates one group with its full member set.
  Used by deployClonedSafe() to mirror the predecessor safe's governance structure
  into a newly deployed contract before activating it.

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static bootstrapGroup(params: CallParams<AlgoSafeArgs['obj']['bootstrapGroup((string,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64),(address,uint64,string)[],uint64)uint64'] | AlgoSafeArgs['tuple']['bootstrapGroup((string,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64),(address,uint64,string)[],uint64)uint64']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'bootstrapGroup((string,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64),(address,uint64,string)[],uint64)uint64' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.seed, params.args.members, params.args.ensureBudgetValue],
    }
  }
  /**
   * Constructs a no op call for the bootstrapRekeyedAddress(address,string)void ABI method
   *
  * bootstrapRekeyedAddress — seed the rekeyed-address registry during cloning.
  Only callable before finalizeBootstrap(); mirrors entries from the predecessor.

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static bootstrapRekeyedAddress(params: CallParams<AlgoSafeArgs['obj']['bootstrapRekeyedAddress(address,string)void'] | AlgoSafeArgs['tuple']['bootstrapRekeyedAddress(address,string)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'bootstrapRekeyedAddress(address,string)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.addr, params.args.label],
    }
  }
  /**
   * Constructs a no op call for the finalizeBootstrap()void ABI method
   *
  * finalizeBootstrap — activates a cloned safe seeded via bootstrapGroup.
  Requires at least one active group with PRIV_GROUP to prevent lockout.
  After this call _newProposal succeeds and normal governance begins.

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static finalizeBootstrap(params: CallParams<AlgoSafeArgs['obj']['finalizeBootstrap()void'] | AlgoSafeArgs['tuple']['finalizeBootstrap()void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'finalizeBootstrap()void' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the proposeTransactionGroup(uint64,(uint64,byte[])[],uint64,bool,uint64)uint64 ABI method
   *
  * proposeTransactionGroup — create and optionally immediately execute a transaction
  proposal. The caller must be a member of groupId.
  
  execute=true skips the approval phase (valid only when threshold=1 and the
  proposer is the sole member, i.e. the proposal is instantly READY).
  
  The payload is stored in chunk slot 1; further chunks can be appended via
  appendTransactionGroupPayload before the first independent approval.

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static proposeTransactionGroup(params: CallParams<AlgoSafeArgs['obj']['proposeTransactionGroup(uint64,(uint64,byte[])[],uint64,bool,uint64)uint64'] | AlgoSafeArgs['tuple']['proposeTransactionGroup(uint64,(uint64,byte[])[],uint64,bool,uint64)uint64']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'proposeTransactionGroup(uint64,(uint64,byte[])[],uint64,bool,uint64)uint64' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.groupId, params.args.payload, params.args.expiryRound, params.args.execute, params.args.ensureBudgetValue],
    }
  }
  /**
   * Constructs a no op call for the appendTransactionGroupPayload(uint64,uint64,(uint64,byte[])[],uint64)void ABI method
   *
  * appendTransactionGroupPayload — add an additional payload chunk to an existing
  transaction proposal. Only the original proposer may append, and only while
  no independent approver has signed yet (approvalsCount === 1 means only the
  auto-approval from _newProposal). Each append bumps payloadVersion so any
  signer that has already approved sees "payload changed since review" on their
  next call and must re-approve the new version.
  
  Prevents appending to expired proposals (blocks extending validity window).

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static appendTransactionGroupPayload(params: CallParams<AlgoSafeArgs['obj']['appendTransactionGroupPayload(uint64,uint64,(uint64,byte[])[],uint64)void'] | AlgoSafeArgs['tuple']['appendTransactionGroupPayload(uint64,uint64,(uint64,byte[])[],uint64)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'appendTransactionGroupPayload(uint64,uint64,(uint64,byte[])[],uint64)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.proposalId, params.args.payloadIndex, params.args.payload, params.args.ensureBudgetValue],
    }
  }
  /**
   * Constructs a no op call for the proposeAdminChange(uint64,(uint64,uint64,string,address,uint64,string,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64),uint64,uint64)uint64 ABI method
   *
  * proposeAdminChange — create a governance change proposal.
  
  Routing:
   ADM_DISSOLVE_CUSTODIAN — the proposing group must be the custodian group
     itself. Admins cannot force-dissolve a custodian.
   All other change types — the proposing group must hold the relevant admin
     privilege (PRIV_GROUP or PRIV_POLICY via _assertPrivilegeForChange).
  
  Payload-level correctness (e.g. guard existence, target group type) is
  intentionally deferred to execution time in _applyAdminChange. This saves
  ~700 bytes of compiled binary by not duplicating the check logic at proposal
  time, at the cost of wasted box storage if an invalid proposal passes approval.
  
  The cooldown cap is asserted here (before the box is written) so that a
  bad cooldown value cannot be stored and later cause an arithmetic overflow
  in _executeProposalInternal even if the group approves the proposal.

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static proposeAdminChange(params: CallParams<AlgoSafeArgs['obj']['proposeAdminChange(uint64,(uint64,uint64,string,address,uint64,string,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64),uint64,uint64)uint64'] | AlgoSafeArgs['tuple']['proposeAdminChange(uint64,(uint64,uint64,string,address,uint64,string,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64),uint64,uint64)uint64']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'proposeAdminChange(uint64,(uint64,uint64,string,address,uint64,string,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64),uint64,uint64)uint64' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.groupId, params.args.change, params.args.expiryRound, params.args.ensureBudgetValue],
    }
  }
  /**
   * Constructs a no op call for the approveProposal(uint64,uint64,uint64)void ABI method
   *
  * approveProposal — add the caller's approval to an open proposal.
  
  expectedPayloadVersion must match the proposal's live payloadVersion; this
  binds the approval to the exact payload content the signer reviewed and
  prevents a bait-and-switch attack where the proposer appends a different
  chunk after other signers have already approved.
  
  Also asserts that the group's membershipEpoch hasn't changed since proposal
  creation — removing a compromised member invalidates all pending approvals.

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static approveProposal(params: CallParams<AlgoSafeArgs['obj']['approveProposal(uint64,uint64,uint64)void'] | AlgoSafeArgs['tuple']['approveProposal(uint64,uint64,uint64)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'approveProposal(uint64,uint64,uint64)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.proposalId, params.args.expectedPayloadVersion, params.args.ensureBudgetValue],
    }
  }
  /**
   * Constructs a no op call for the executeProposal(uint64,uint64)void ABI method
   *
   * executeProposal — execute a READY proposal (threshold met, not expired).
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static executeProposal(params: CallParams<AlgoSafeArgs['obj']['executeProposal(uint64,uint64)void'] | AlgoSafeArgs['tuple']['executeProposal(uint64,uint64)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'executeProposal(uint64,uint64)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.proposalId, params.args.ensureBudgetValue],
    }
  }
  /**
   * Constructs a no op call for the cancelProposal(uint64,uint64)void ABI method
   *
  * cancelProposal — cancel an open proposal. Either the original proposer
  or any current group member may cancel.

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static cancelProposal(params: CallParams<AlgoSafeArgs['obj']['cancelProposal(uint64,uint64)void'] | AlgoSafeArgs['tuple']['cancelProposal(uint64,uint64)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'cancelProposal(uint64,uint64)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.proposalId, params.args.ensureBudgetValue],
    }
  }
  /**
   * Constructs a no op call for the pruneProposal(uint64,uint64)void ABI method
   *
  * pruneProposal — delete terminal proposal boxes to reclaim MBR.
  Only callable on EXECUTED or CANCELLED proposals after their expiryRound.
  Removes the proposal box, all payload chunk boxes, and the admin-change box.

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static pruneProposal(params: CallParams<AlgoSafeArgs['obj']['pruneProposal(uint64,uint64)void'] | AlgoSafeArgs['tuple']['pruneProposal(uint64,uint64)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'pruneProposal(uint64,uint64)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.proposalId, params.args.ensureBudgetValue],
    }
  }
}

/**
 * A client to make calls to the AlgoSafe smart contract
 */
export class AlgoSafeClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `AlgoSafeClient`
   *
   * @param appClient An `AppClient` instance which has been created with the AlgoSafe app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `AlgoSafeClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Checks for decode errors on the given return value and maps the return value to the return type for the given method
   * @returns The typed return value or undefined if there was no value
   */
  decodeReturnValue<TSignature extends AlgoSafeNonVoidMethodSignatures>(method: TSignature, returnValue: ABIReturn | undefined) {
    return returnValue !== undefined ? getArc56ReturnValue<MethodReturn<TSignature>>(returnValue, this.appClient.getABIMethod(method), APP_SPEC.structs) : undefined
  }

  /**
   * Returns a new `AlgoSafeClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<AlgoSafeClient> {
    return new AlgoSafeClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `AlgoSafeClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<AlgoSafeClient> {
    return new AlgoSafeClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Makes a clear_state call to an existing instance of the AlgoSafe smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the AlgoSafe smart contract using the `bootstrap(string)void` ABI method.
     *
    * bootstrap — simple one-call bootstrap for a fresh safe.
    Creates group 1 with full admin+action privileges and adds the creator as
    the sole member. Incompatible with bootstrapGroup (the two paths cannot mix).

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    bootstrap: (params: CallParams<AlgoSafeArgs['obj']['bootstrap(string)void'] | AlgoSafeArgs['tuple']['bootstrap(string)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(AlgoSafeParamsFactory.bootstrap(params))
    },

    /**
     * Makes a call to the AlgoSafe smart contract using the `bootstrapGroup((string,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64),(address,uint64,string)[],uint64)uint64` ABI method.
     *
    * bootstrapGroup — clone-friendly bootstrap; may be called multiple times before
    finalizeBootstrap(). Each call creates one group with its full member set.
    Used by deployClonedSafe() to mirror the predecessor safe's governance structure
    into a newly deployed contract before activating it.

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    bootstrapGroup: (params: CallParams<AlgoSafeArgs['obj']['bootstrapGroup((string,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64),(address,uint64,string)[],uint64)uint64'] | AlgoSafeArgs['tuple']['bootstrapGroup((string,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64),(address,uint64,string)[],uint64)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(AlgoSafeParamsFactory.bootstrapGroup(params))
    },

    /**
     * Makes a call to the AlgoSafe smart contract using the `bootstrapRekeyedAddress(address,string)void` ABI method.
     *
    * bootstrapRekeyedAddress — seed the rekeyed-address registry during cloning.
    Only callable before finalizeBootstrap(); mirrors entries from the predecessor.

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    bootstrapRekeyedAddress: (params: CallParams<AlgoSafeArgs['obj']['bootstrapRekeyedAddress(address,string)void'] | AlgoSafeArgs['tuple']['bootstrapRekeyedAddress(address,string)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(AlgoSafeParamsFactory.bootstrapRekeyedAddress(params))
    },

    /**
     * Makes a call to the AlgoSafe smart contract using the `finalizeBootstrap()void` ABI method.
     *
    * finalizeBootstrap — activates a cloned safe seeded via bootstrapGroup.
    Requires at least one active group with PRIV_GROUP to prevent lockout.
    After this call _newProposal succeeds and normal governance begins.

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    finalizeBootstrap: (params: CallParams<AlgoSafeArgs['obj']['finalizeBootstrap()void'] | AlgoSafeArgs['tuple']['finalizeBootstrap()void']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(AlgoSafeParamsFactory.finalizeBootstrap(params))
    },

    /**
     * Makes a call to the AlgoSafe smart contract using the `proposeTransactionGroup(uint64,(uint64,byte[])[],uint64,bool,uint64)uint64` ABI method.
     *
    * proposeTransactionGroup — create and optionally immediately execute a transaction
    proposal. The caller must be a member of groupId.
    
    execute=true skips the approval phase (valid only when threshold=1 and the
    proposer is the sole member, i.e. the proposal is instantly READY).
    
    The payload is stored in chunk slot 1; further chunks can be appended via
    appendTransactionGroupPayload before the first independent approval.

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    proposeTransactionGroup: (params: CallParams<AlgoSafeArgs['obj']['proposeTransactionGroup(uint64,(uint64,byte[])[],uint64,bool,uint64)uint64'] | AlgoSafeArgs['tuple']['proposeTransactionGroup(uint64,(uint64,byte[])[],uint64,bool,uint64)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(AlgoSafeParamsFactory.proposeTransactionGroup(params))
    },

    /**
     * Makes a call to the AlgoSafe smart contract using the `appendTransactionGroupPayload(uint64,uint64,(uint64,byte[])[],uint64)void` ABI method.
     *
    * appendTransactionGroupPayload — add an additional payload chunk to an existing
    transaction proposal. Only the original proposer may append, and only while
    no independent approver has signed yet (approvalsCount === 1 means only the
    auto-approval from _newProposal). Each append bumps payloadVersion so any
    signer that has already approved sees "payload changed since review" on their
    next call and must re-approve the new version.
    
    Prevents appending to expired proposals (blocks extending validity window).

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    appendTransactionGroupPayload: (params: CallParams<AlgoSafeArgs['obj']['appendTransactionGroupPayload(uint64,uint64,(uint64,byte[])[],uint64)void'] | AlgoSafeArgs['tuple']['appendTransactionGroupPayload(uint64,uint64,(uint64,byte[])[],uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(AlgoSafeParamsFactory.appendTransactionGroupPayload(params))
    },

    /**
     * Makes a call to the AlgoSafe smart contract using the `proposeAdminChange(uint64,(uint64,uint64,string,address,uint64,string,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64),uint64,uint64)uint64` ABI method.
     *
    * proposeAdminChange — create a governance change proposal.
    
    Routing:
     ADM_DISSOLVE_CUSTODIAN — the proposing group must be the custodian group
       itself. Admins cannot force-dissolve a custodian.
     All other change types — the proposing group must hold the relevant admin
       privilege (PRIV_GROUP or PRIV_POLICY via _assertPrivilegeForChange).
    
    Payload-level correctness (e.g. guard existence, target group type) is
    intentionally deferred to execution time in _applyAdminChange. This saves
    ~700 bytes of compiled binary by not duplicating the check logic at proposal
    time, at the cost of wasted box storage if an invalid proposal passes approval.
    
    The cooldown cap is asserted here (before the box is written) so that a
    bad cooldown value cannot be stored and later cause an arithmetic overflow
    in _executeProposalInternal even if the group approves the proposal.

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    proposeAdminChange: (params: CallParams<AlgoSafeArgs['obj']['proposeAdminChange(uint64,(uint64,uint64,string,address,uint64,string,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64),uint64,uint64)uint64'] | AlgoSafeArgs['tuple']['proposeAdminChange(uint64,(uint64,uint64,string,address,uint64,string,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64),uint64,uint64)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(AlgoSafeParamsFactory.proposeAdminChange(params))
    },

    /**
     * Makes a call to the AlgoSafe smart contract using the `approveProposal(uint64,uint64,uint64)void` ABI method.
     *
    * approveProposal — add the caller's approval to an open proposal.
    
    expectedPayloadVersion must match the proposal's live payloadVersion; this
    binds the approval to the exact payload content the signer reviewed and
    prevents a bait-and-switch attack where the proposer appends a different
    chunk after other signers have already approved.
    
    Also asserts that the group's membershipEpoch hasn't changed since proposal
    creation — removing a compromised member invalidates all pending approvals.

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    approveProposal: (params: CallParams<AlgoSafeArgs['obj']['approveProposal(uint64,uint64,uint64)void'] | AlgoSafeArgs['tuple']['approveProposal(uint64,uint64,uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(AlgoSafeParamsFactory.approveProposal(params))
    },

    /**
     * Makes a call to the AlgoSafe smart contract using the `executeProposal(uint64,uint64)void` ABI method.
     *
     * executeProposal — execute a READY proposal (threshold met, not expired).
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    executeProposal: (params: CallParams<AlgoSafeArgs['obj']['executeProposal(uint64,uint64)void'] | AlgoSafeArgs['tuple']['executeProposal(uint64,uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(AlgoSafeParamsFactory.executeProposal(params))
    },

    /**
     * Makes a call to the AlgoSafe smart contract using the `cancelProposal(uint64,uint64)void` ABI method.
     *
    * cancelProposal — cancel an open proposal. Either the original proposer
    or any current group member may cancel.

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    cancelProposal: (params: CallParams<AlgoSafeArgs['obj']['cancelProposal(uint64,uint64)void'] | AlgoSafeArgs['tuple']['cancelProposal(uint64,uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(AlgoSafeParamsFactory.cancelProposal(params))
    },

    /**
     * Makes a call to the AlgoSafe smart contract using the `pruneProposal(uint64,uint64)void` ABI method.
     *
    * pruneProposal — delete terminal proposal boxes to reclaim MBR.
    Only callable on EXECUTED or CANCELLED proposals after their expiryRound.
    Removes the proposal box, all payload chunk boxes, and the admin-change box.

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    pruneProposal: (params: CallParams<AlgoSafeArgs['obj']['pruneProposal(uint64,uint64)void'] | AlgoSafeArgs['tuple']['pruneProposal(uint64,uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(AlgoSafeParamsFactory.pruneProposal(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Makes a clear_state call to an existing instance of the AlgoSafe smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the AlgoSafe smart contract using the `bootstrap(string)void` ABI method.
     *
    * bootstrap — simple one-call bootstrap for a fresh safe.
    Creates group 1 with full admin+action privileges and adds the creator as
    the sole member. Incompatible with bootstrapGroup (the two paths cannot mix).

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    bootstrap: (params: CallParams<AlgoSafeArgs['obj']['bootstrap(string)void'] | AlgoSafeArgs['tuple']['bootstrap(string)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(AlgoSafeParamsFactory.bootstrap(params))
    },

    /**
     * Makes a call to the AlgoSafe smart contract using the `bootstrapGroup((string,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64),(address,uint64,string)[],uint64)uint64` ABI method.
     *
    * bootstrapGroup — clone-friendly bootstrap; may be called multiple times before
    finalizeBootstrap(). Each call creates one group with its full member set.
    Used by deployClonedSafe() to mirror the predecessor safe's governance structure
    into a newly deployed contract before activating it.

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    bootstrapGroup: (params: CallParams<AlgoSafeArgs['obj']['bootstrapGroup((string,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64),(address,uint64,string)[],uint64)uint64'] | AlgoSafeArgs['tuple']['bootstrapGroup((string,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64),(address,uint64,string)[],uint64)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(AlgoSafeParamsFactory.bootstrapGroup(params))
    },

    /**
     * Makes a call to the AlgoSafe smart contract using the `bootstrapRekeyedAddress(address,string)void` ABI method.
     *
    * bootstrapRekeyedAddress — seed the rekeyed-address registry during cloning.
    Only callable before finalizeBootstrap(); mirrors entries from the predecessor.

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    bootstrapRekeyedAddress: (params: CallParams<AlgoSafeArgs['obj']['bootstrapRekeyedAddress(address,string)void'] | AlgoSafeArgs['tuple']['bootstrapRekeyedAddress(address,string)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(AlgoSafeParamsFactory.bootstrapRekeyedAddress(params))
    },

    /**
     * Makes a call to the AlgoSafe smart contract using the `finalizeBootstrap()void` ABI method.
     *
    * finalizeBootstrap — activates a cloned safe seeded via bootstrapGroup.
    Requires at least one active group with PRIV_GROUP to prevent lockout.
    After this call _newProposal succeeds and normal governance begins.

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    finalizeBootstrap: (params: CallParams<AlgoSafeArgs['obj']['finalizeBootstrap()void'] | AlgoSafeArgs['tuple']['finalizeBootstrap()void']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(AlgoSafeParamsFactory.finalizeBootstrap(params))
    },

    /**
     * Makes a call to the AlgoSafe smart contract using the `proposeTransactionGroup(uint64,(uint64,byte[])[],uint64,bool,uint64)uint64` ABI method.
     *
    * proposeTransactionGroup — create and optionally immediately execute a transaction
    proposal. The caller must be a member of groupId.
    
    execute=true skips the approval phase (valid only when threshold=1 and the
    proposer is the sole member, i.e. the proposal is instantly READY).
    
    The payload is stored in chunk slot 1; further chunks can be appended via
    appendTransactionGroupPayload before the first independent approval.

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    proposeTransactionGroup: (params: CallParams<AlgoSafeArgs['obj']['proposeTransactionGroup(uint64,(uint64,byte[])[],uint64,bool,uint64)uint64'] | AlgoSafeArgs['tuple']['proposeTransactionGroup(uint64,(uint64,byte[])[],uint64,bool,uint64)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(AlgoSafeParamsFactory.proposeTransactionGroup(params))
    },

    /**
     * Makes a call to the AlgoSafe smart contract using the `appendTransactionGroupPayload(uint64,uint64,(uint64,byte[])[],uint64)void` ABI method.
     *
    * appendTransactionGroupPayload — add an additional payload chunk to an existing
    transaction proposal. Only the original proposer may append, and only while
    no independent approver has signed yet (approvalsCount === 1 means only the
    auto-approval from _newProposal). Each append bumps payloadVersion so any
    signer that has already approved sees "payload changed since review" on their
    next call and must re-approve the new version.
    
    Prevents appending to expired proposals (blocks extending validity window).

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    appendTransactionGroupPayload: (params: CallParams<AlgoSafeArgs['obj']['appendTransactionGroupPayload(uint64,uint64,(uint64,byte[])[],uint64)void'] | AlgoSafeArgs['tuple']['appendTransactionGroupPayload(uint64,uint64,(uint64,byte[])[],uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(AlgoSafeParamsFactory.appendTransactionGroupPayload(params))
    },

    /**
     * Makes a call to the AlgoSafe smart contract using the `proposeAdminChange(uint64,(uint64,uint64,string,address,uint64,string,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64),uint64,uint64)uint64` ABI method.
     *
    * proposeAdminChange — create a governance change proposal.
    
    Routing:
     ADM_DISSOLVE_CUSTODIAN — the proposing group must be the custodian group
       itself. Admins cannot force-dissolve a custodian.
     All other change types — the proposing group must hold the relevant admin
       privilege (PRIV_GROUP or PRIV_POLICY via _assertPrivilegeForChange).
    
    Payload-level correctness (e.g. guard existence, target group type) is
    intentionally deferred to execution time in _applyAdminChange. This saves
    ~700 bytes of compiled binary by not duplicating the check logic at proposal
    time, at the cost of wasted box storage if an invalid proposal passes approval.
    
    The cooldown cap is asserted here (before the box is written) so that a
    bad cooldown value cannot be stored and later cause an arithmetic overflow
    in _executeProposalInternal even if the group approves the proposal.

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    proposeAdminChange: (params: CallParams<AlgoSafeArgs['obj']['proposeAdminChange(uint64,(uint64,uint64,string,address,uint64,string,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64),uint64,uint64)uint64'] | AlgoSafeArgs['tuple']['proposeAdminChange(uint64,(uint64,uint64,string,address,uint64,string,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64),uint64,uint64)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(AlgoSafeParamsFactory.proposeAdminChange(params))
    },

    /**
     * Makes a call to the AlgoSafe smart contract using the `approveProposal(uint64,uint64,uint64)void` ABI method.
     *
    * approveProposal — add the caller's approval to an open proposal.
    
    expectedPayloadVersion must match the proposal's live payloadVersion; this
    binds the approval to the exact payload content the signer reviewed and
    prevents a bait-and-switch attack where the proposer appends a different
    chunk after other signers have already approved.
    
    Also asserts that the group's membershipEpoch hasn't changed since proposal
    creation — removing a compromised member invalidates all pending approvals.

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    approveProposal: (params: CallParams<AlgoSafeArgs['obj']['approveProposal(uint64,uint64,uint64)void'] | AlgoSafeArgs['tuple']['approveProposal(uint64,uint64,uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(AlgoSafeParamsFactory.approveProposal(params))
    },

    /**
     * Makes a call to the AlgoSafe smart contract using the `executeProposal(uint64,uint64)void` ABI method.
     *
     * executeProposal — execute a READY proposal (threshold met, not expired).
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    executeProposal: (params: CallParams<AlgoSafeArgs['obj']['executeProposal(uint64,uint64)void'] | AlgoSafeArgs['tuple']['executeProposal(uint64,uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(AlgoSafeParamsFactory.executeProposal(params))
    },

    /**
     * Makes a call to the AlgoSafe smart contract using the `cancelProposal(uint64,uint64)void` ABI method.
     *
    * cancelProposal — cancel an open proposal. Either the original proposer
    or any current group member may cancel.

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    cancelProposal: (params: CallParams<AlgoSafeArgs['obj']['cancelProposal(uint64,uint64)void'] | AlgoSafeArgs['tuple']['cancelProposal(uint64,uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(AlgoSafeParamsFactory.cancelProposal(params))
    },

    /**
     * Makes a call to the AlgoSafe smart contract using the `pruneProposal(uint64,uint64)void` ABI method.
     *
    * pruneProposal — delete terminal proposal boxes to reclaim MBR.
    Only callable on EXECUTED or CANCELLED proposals after their expiryRound.
    Removes the proposal box, all payload chunk boxes, and the admin-change box.

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    pruneProposal: (params: CallParams<AlgoSafeArgs['obj']['pruneProposal(uint64,uint64)void'] | AlgoSafeArgs['tuple']['pruneProposal(uint64,uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(AlgoSafeParamsFactory.pruneProposal(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Makes a clear_state call to an existing instance of the AlgoSafe smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the AlgoSafe smart contract using the `bootstrap(string)void` ABI method.
     *
    * bootstrap — simple one-call bootstrap for a fresh safe.
    Creates group 1 with full admin+action privileges and adds the creator as
    the sole member. Incompatible with bootstrapGroup (the two paths cannot mix).

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    bootstrap: async (params: CallParams<AlgoSafeArgs['obj']['bootstrap(string)void'] | AlgoSafeArgs['tuple']['bootstrap(string)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(AlgoSafeParamsFactory.bootstrap(params))
      return {...result, return: result.return as unknown as (undefined | AlgoSafeReturns['bootstrap(string)void'])}
    },

    /**
     * Makes a call to the AlgoSafe smart contract using the `bootstrapGroup((string,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64),(address,uint64,string)[],uint64)uint64` ABI method.
     *
    * bootstrapGroup — clone-friendly bootstrap; may be called multiple times before
    finalizeBootstrap(). Each call creates one group with its full member set.
    Used by deployClonedSafe() to mirror the predecessor safe's governance structure
    into a newly deployed contract before activating it.

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    bootstrapGroup: async (params: CallParams<AlgoSafeArgs['obj']['bootstrapGroup((string,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64),(address,uint64,string)[],uint64)uint64'] | AlgoSafeArgs['tuple']['bootstrapGroup((string,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64),(address,uint64,string)[],uint64)uint64']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(AlgoSafeParamsFactory.bootstrapGroup(params))
      return {...result, return: result.return as unknown as (undefined | AlgoSafeReturns['bootstrapGroup((string,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64),(address,uint64,string)[],uint64)uint64'])}
    },

    /**
     * Makes a call to the AlgoSafe smart contract using the `bootstrapRekeyedAddress(address,string)void` ABI method.
     *
    * bootstrapRekeyedAddress — seed the rekeyed-address registry during cloning.
    Only callable before finalizeBootstrap(); mirrors entries from the predecessor.

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    bootstrapRekeyedAddress: async (params: CallParams<AlgoSafeArgs['obj']['bootstrapRekeyedAddress(address,string)void'] | AlgoSafeArgs['tuple']['bootstrapRekeyedAddress(address,string)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(AlgoSafeParamsFactory.bootstrapRekeyedAddress(params))
      return {...result, return: result.return as unknown as (undefined | AlgoSafeReturns['bootstrapRekeyedAddress(address,string)void'])}
    },

    /**
     * Makes a call to the AlgoSafe smart contract using the `finalizeBootstrap()void` ABI method.
     *
    * finalizeBootstrap — activates a cloned safe seeded via bootstrapGroup.
    Requires at least one active group with PRIV_GROUP to prevent lockout.
    After this call _newProposal succeeds and normal governance begins.

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    finalizeBootstrap: async (params: CallParams<AlgoSafeArgs['obj']['finalizeBootstrap()void'] | AlgoSafeArgs['tuple']['finalizeBootstrap()void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(AlgoSafeParamsFactory.finalizeBootstrap(params))
      return {...result, return: result.return as unknown as (undefined | AlgoSafeReturns['finalizeBootstrap()void'])}
    },

    /**
     * Makes a call to the AlgoSafe smart contract using the `proposeTransactionGroup(uint64,(uint64,byte[])[],uint64,bool,uint64)uint64` ABI method.
     *
    * proposeTransactionGroup — create and optionally immediately execute a transaction
    proposal. The caller must be a member of groupId.
    
    execute=true skips the approval phase (valid only when threshold=1 and the
    proposer is the sole member, i.e. the proposal is instantly READY).
    
    The payload is stored in chunk slot 1; further chunks can be appended via
    appendTransactionGroupPayload before the first independent approval.

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    proposeTransactionGroup: async (params: CallParams<AlgoSafeArgs['obj']['proposeTransactionGroup(uint64,(uint64,byte[])[],uint64,bool,uint64)uint64'] | AlgoSafeArgs['tuple']['proposeTransactionGroup(uint64,(uint64,byte[])[],uint64,bool,uint64)uint64']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(AlgoSafeParamsFactory.proposeTransactionGroup(params))
      return {...result, return: result.return as unknown as (undefined | AlgoSafeReturns['proposeTransactionGroup(uint64,(uint64,byte[])[],uint64,bool,uint64)uint64'])}
    },

    /**
     * Makes a call to the AlgoSafe smart contract using the `appendTransactionGroupPayload(uint64,uint64,(uint64,byte[])[],uint64)void` ABI method.
     *
    * appendTransactionGroupPayload — add an additional payload chunk to an existing
    transaction proposal. Only the original proposer may append, and only while
    no independent approver has signed yet (approvalsCount === 1 means only the
    auto-approval from _newProposal). Each append bumps payloadVersion so any
    signer that has already approved sees "payload changed since review" on their
    next call and must re-approve the new version.
    
    Prevents appending to expired proposals (blocks extending validity window).

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    appendTransactionGroupPayload: async (params: CallParams<AlgoSafeArgs['obj']['appendTransactionGroupPayload(uint64,uint64,(uint64,byte[])[],uint64)void'] | AlgoSafeArgs['tuple']['appendTransactionGroupPayload(uint64,uint64,(uint64,byte[])[],uint64)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(AlgoSafeParamsFactory.appendTransactionGroupPayload(params))
      return {...result, return: result.return as unknown as (undefined | AlgoSafeReturns['appendTransactionGroupPayload(uint64,uint64,(uint64,byte[])[],uint64)void'])}
    },

    /**
     * Makes a call to the AlgoSafe smart contract using the `proposeAdminChange(uint64,(uint64,uint64,string,address,uint64,string,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64),uint64,uint64)uint64` ABI method.
     *
    * proposeAdminChange — create a governance change proposal.
    
    Routing:
     ADM_DISSOLVE_CUSTODIAN — the proposing group must be the custodian group
       itself. Admins cannot force-dissolve a custodian.
     All other change types — the proposing group must hold the relevant admin
       privilege (PRIV_GROUP or PRIV_POLICY via _assertPrivilegeForChange).
    
    Payload-level correctness (e.g. guard existence, target group type) is
    intentionally deferred to execution time in _applyAdminChange. This saves
    ~700 bytes of compiled binary by not duplicating the check logic at proposal
    time, at the cost of wasted box storage if an invalid proposal passes approval.
    
    The cooldown cap is asserted here (before the box is written) so that a
    bad cooldown value cannot be stored and later cause an arithmetic overflow
    in _executeProposalInternal even if the group approves the proposal.

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    proposeAdminChange: async (params: CallParams<AlgoSafeArgs['obj']['proposeAdminChange(uint64,(uint64,uint64,string,address,uint64,string,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64),uint64,uint64)uint64'] | AlgoSafeArgs['tuple']['proposeAdminChange(uint64,(uint64,uint64,string,address,uint64,string,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64),uint64,uint64)uint64']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(AlgoSafeParamsFactory.proposeAdminChange(params))
      return {...result, return: result.return as unknown as (undefined | AlgoSafeReturns['proposeAdminChange(uint64,(uint64,uint64,string,address,uint64,string,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64),uint64,uint64)uint64'])}
    },

    /**
     * Makes a call to the AlgoSafe smart contract using the `approveProposal(uint64,uint64,uint64)void` ABI method.
     *
    * approveProposal — add the caller's approval to an open proposal.
    
    expectedPayloadVersion must match the proposal's live payloadVersion; this
    binds the approval to the exact payload content the signer reviewed and
    prevents a bait-and-switch attack where the proposer appends a different
    chunk after other signers have already approved.
    
    Also asserts that the group's membershipEpoch hasn't changed since proposal
    creation — removing a compromised member invalidates all pending approvals.

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    approveProposal: async (params: CallParams<AlgoSafeArgs['obj']['approveProposal(uint64,uint64,uint64)void'] | AlgoSafeArgs['tuple']['approveProposal(uint64,uint64,uint64)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(AlgoSafeParamsFactory.approveProposal(params))
      return {...result, return: result.return as unknown as (undefined | AlgoSafeReturns['approveProposal(uint64,uint64,uint64)void'])}
    },

    /**
     * Makes a call to the AlgoSafe smart contract using the `executeProposal(uint64,uint64)void` ABI method.
     *
     * executeProposal — execute a READY proposal (threshold met, not expired).
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    executeProposal: async (params: CallParams<AlgoSafeArgs['obj']['executeProposal(uint64,uint64)void'] | AlgoSafeArgs['tuple']['executeProposal(uint64,uint64)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(AlgoSafeParamsFactory.executeProposal(params))
      return {...result, return: result.return as unknown as (undefined | AlgoSafeReturns['executeProposal(uint64,uint64)void'])}
    },

    /**
     * Makes a call to the AlgoSafe smart contract using the `cancelProposal(uint64,uint64)void` ABI method.
     *
    * cancelProposal — cancel an open proposal. Either the original proposer
    or any current group member may cancel.

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    cancelProposal: async (params: CallParams<AlgoSafeArgs['obj']['cancelProposal(uint64,uint64)void'] | AlgoSafeArgs['tuple']['cancelProposal(uint64,uint64)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(AlgoSafeParamsFactory.cancelProposal(params))
      return {...result, return: result.return as unknown as (undefined | AlgoSafeReturns['cancelProposal(uint64,uint64)void'])}
    },

    /**
     * Makes a call to the AlgoSafe smart contract using the `pruneProposal(uint64,uint64)void` ABI method.
     *
    * pruneProposal — delete terminal proposal boxes to reclaim MBR.
    Only callable on EXECUTED or CANCELLED proposals after their expiryRound.
    Removes the proposal box, all payload chunk boxes, and the admin-change box.

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    pruneProposal: async (params: CallParams<AlgoSafeArgs['obj']['pruneProposal(uint64,uint64)void'] | AlgoSafeArgs['tuple']['pruneProposal(uint64,uint64)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(AlgoSafeParamsFactory.pruneProposal(params))
      return {...result, return: result.return as unknown as (undefined | AlgoSafeReturns['pruneProposal(uint64,uint64)void'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new AlgoSafeClient(this.appClient.clone(params))
  }

  /**
   * Methods to access state for the current AlgoSafe app
   */
  state = {
    /**
     * Methods to access global state for the current AlgoSafe app
     */
    global: {
      /**
       * Get all current keyed values from global state
       */
      getAll: async (): Promise<Partial<Expand<GlobalKeysState>>> => {
        const result = await this.appClient.state.global.getAll()
        return {
          name: result.name,
          creator: result.creator,
          bootstrapped: result.bootstrapped,
          nextGroupId: result.nextGroupId,
          nextProposalId: result.nextProposalId,
          groupCount: result.groupCount,
          paused: result.paused,
          version: result.version,
          activePrivGroupCount: result.activePrivGroupCount,
          validatorApp: result.validatorApp,
        }
      },
      /**
       * Get the current value of the name key in global state
       */
      name: async (): Promise<string | undefined> => { return (await this.appClient.state.global.getValue("name")) as string | undefined },
      /**
       * Get the current value of the creator key in global state
       */
      creator: async (): Promise<string | undefined> => { return (await this.appClient.state.global.getValue("creator")) as string | undefined },
      /**
       * Get the current value of the bootstrapped key in global state
       */
      bootstrapped: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("bootstrapped")) as bigint | undefined },
      /**
       * Get the current value of the nextGroupId key in global state
       */
      nextGroupId: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("nextGroupId")) as bigint | undefined },
      /**
       * Get the current value of the nextProposalId key in global state
       */
      nextProposalId: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("nextProposalId")) as bigint | undefined },
      /**
       * Get the current value of the groupCount key in global state
       */
      groupCount: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("groupCount")) as bigint | undefined },
      /**
       * Get the current value of the paused key in global state
       */
      paused: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("paused")) as bigint | undefined },
      /**
       * Get the current value of the version key in global state
       */
      version: async (): Promise<string | undefined> => { return (await this.appClient.state.global.getValue("version")) as string | undefined },
      /**
       * Get the current value of the activePrivGroupCount key in global state
       */
      activePrivGroupCount: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("activePrivGroupCount")) as bigint | undefined },
      /**
       * Get the current value of the validatorApp key in global state
       */
      validatorApp: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("validatorApp")) as bigint | undefined },
    },
    /**
     * Methods to access box state for the current AlgoSafe app
     */
    box: {
      /**
       * Get all current keyed values from box state
       */
      getAll: async (): Promise<Partial<Expand<BoxKeysState>>> => {
        const result = await this.appClient.state.box.getAll()
        return {
        }
      },
      /**
       * Get values from the groups map in box state
       */
      groups: {
        /**
         * Get all current values of the groups map in box state
         */
        getMap: async (): Promise<Map<bigint, SignerGroup>> => { return (await this.appClient.state.box.getMap("groups")) as Map<bigint, SignerGroup> },
        /**
         * Get a current value of the groups map by key from box state
         */
        value: async (key: bigint | number): Promise<SignerGroup | undefined> => { return await this.appClient.state.box.getMapValue("groups", key) as SignerGroup | undefined },
      },
      /**
       * Get values from the members map in box state
       */
      members: {
        /**
         * Get all current values of the members map in box state
         */
        getMap: async (): Promise<Map<Object78E87395, Member>> => { return (await this.appClient.state.box.getMap("members")) as Map<Object78E87395, Member> },
        /**
         * Get a current value of the members map by key from box state
         */
        value: async (key: Object78E87395): Promise<Member | undefined> => { return await this.appClient.state.box.getMapValue("members", key) as Member | undefined },
      },
      /**
       * Get values from the proposals map in box state
       */
      proposals: {
        /**
         * Get all current values of the proposals map in box state
         */
        getMap: async (): Promise<Map<bigint, Proposal>> => { return (await this.appClient.state.box.getMap("proposals")) as Map<bigint, Proposal> },
        /**
         * Get a current value of the proposals map by key from box state
         */
        value: async (key: bigint | number): Promise<Proposal | undefined> => { return await this.appClient.state.box.getMapValue("proposals", key) as Proposal | undefined },
      },
      /**
       * Get values from the approvals map in box state
       */
      approvals: {
        /**
         * Get all current values of the approvals map in box state
         */
        getMap: async (): Promise<Map<Object9F556C53, Approval>> => { return (await this.appClient.state.box.getMap("approvals")) as Map<Object9F556C53, Approval> },
        /**
         * Get a current value of the approvals map by key from box state
         */
        value: async (key: Object9F556C53): Promise<Approval | undefined> => { return await this.appClient.state.box.getMapValue("approvals", key) as Approval | undefined },
      },
      /**
       * Get values from the transactionGroups map in box state
       */
      transactionGroups: {
        /**
         * Get all current values of the transactionGroups map in box state
         */
        getMap: async (): Promise<Map<bigint, [bigint, Uint8Array][]>> => { return (await this.appClient.state.box.getMap("transactionGroups")) as Map<bigint, [bigint, Uint8Array][]> },
        /**
         * Get a current value of the transactionGroups map by key from box state
         */
        value: async (key: bigint | number): Promise<[bigint, Uint8Array][] | undefined> => { return await this.appClient.state.box.getMapValue("transactionGroups", key) as [bigint, Uint8Array][] | undefined },
      },
      /**
       * Get values from the adminPayloads map in box state
       */
      adminPayloads: {
        /**
         * Get all current values of the adminPayloads map in box state
         */
        getMap: async (): Promise<Map<bigint, AdminChange>> => { return (await this.appClient.state.box.getMap("adminPayloads")) as Map<bigint, AdminChange> },
        /**
         * Get a current value of the adminPayloads map by key from box state
         */
        value: async (key: bigint | number): Promise<AdminChange | undefined> => { return await this.appClient.state.box.getMapValue("adminPayloads", key) as AdminChange | undefined },
      },
      /**
       * Get values from the rekeyedAddresses map in box state
       */
      rekeyedAddresses: {
        /**
         * Get all current values of the rekeyedAddresses map in box state
         */
        getMap: async (): Promise<Map<string, RekeyedAddress>> => { return (await this.appClient.state.box.getMap("rekeyedAddresses")) as Map<string, RekeyedAddress> },
        /**
         * Get a current value of the rekeyedAddresses map by key from box state
         */
        value: async (key: string): Promise<RekeyedAddress | undefined> => { return await this.appClient.state.box.getMapValue("rekeyedAddresses", key) as RekeyedAddress | undefined },
      },
      /**
       * Get values from the assetGuards map in box state
       */
      assetGuards: {
        /**
         * Get all current values of the assetGuards map in box state
         */
        getMap: async (): Promise<Map<ObjectAe8Bf7D4, AssetGuard>> => { return (await this.appClient.state.box.getMap("assetGuards")) as Map<ObjectAe8Bf7D4, AssetGuard> },
        /**
         * Get a current value of the assetGuards map by key from box state
         */
        value: async (key: ObjectAe8Bf7D4): Promise<AssetGuard | undefined> => { return await this.appClient.state.box.getMapValue("assetGuards", key) as AssetGuard | undefined },
      },
    },
  }

  public newGroup(): AlgoSafeComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a bootstrap(string)void method call against the AlgoSafe contract
       */
      bootstrap(params: CallParams<AlgoSafeArgs['obj']['bootstrap(string)void'] | AlgoSafeArgs['tuple']['bootstrap(string)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.bootstrap(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a bootstrapGroup((string,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64),(address,uint64,string)[],uint64)uint64 method call against the AlgoSafe contract
       */
      bootstrapGroup(params: CallParams<AlgoSafeArgs['obj']['bootstrapGroup((string,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64),(address,uint64,string)[],uint64)uint64'] | AlgoSafeArgs['tuple']['bootstrapGroup((string,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64),(address,uint64,string)[],uint64)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.bootstrapGroup(params)))
        resultMappers.push((v) => client.decodeReturnValue('bootstrapGroup((string,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64),(address,uint64,string)[],uint64)uint64', v))
        return this
      },
      /**
       * Add a bootstrapRekeyedAddress(address,string)void method call against the AlgoSafe contract
       */
      bootstrapRekeyedAddress(params: CallParams<AlgoSafeArgs['obj']['bootstrapRekeyedAddress(address,string)void'] | AlgoSafeArgs['tuple']['bootstrapRekeyedAddress(address,string)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.bootstrapRekeyedAddress(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a finalizeBootstrap()void method call against the AlgoSafe contract
       */
      finalizeBootstrap(params: CallParams<AlgoSafeArgs['obj']['finalizeBootstrap()void'] | AlgoSafeArgs['tuple']['finalizeBootstrap()void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.finalizeBootstrap(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a proposeTransactionGroup(uint64,(uint64,byte[])[],uint64,bool,uint64)uint64 method call against the AlgoSafe contract
       */
      proposeTransactionGroup(params: CallParams<AlgoSafeArgs['obj']['proposeTransactionGroup(uint64,(uint64,byte[])[],uint64,bool,uint64)uint64'] | AlgoSafeArgs['tuple']['proposeTransactionGroup(uint64,(uint64,byte[])[],uint64,bool,uint64)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.proposeTransactionGroup(params)))
        resultMappers.push((v) => client.decodeReturnValue('proposeTransactionGroup(uint64,(uint64,byte[])[],uint64,bool,uint64)uint64', v))
        return this
      },
      /**
       * Add a appendTransactionGroupPayload(uint64,uint64,(uint64,byte[])[],uint64)void method call against the AlgoSafe contract
       */
      appendTransactionGroupPayload(params: CallParams<AlgoSafeArgs['obj']['appendTransactionGroupPayload(uint64,uint64,(uint64,byte[])[],uint64)void'] | AlgoSafeArgs['tuple']['appendTransactionGroupPayload(uint64,uint64,(uint64,byte[])[],uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.appendTransactionGroupPayload(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a proposeAdminChange(uint64,(uint64,uint64,string,address,uint64,string,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64),uint64,uint64)uint64 method call against the AlgoSafe contract
       */
      proposeAdminChange(params: CallParams<AlgoSafeArgs['obj']['proposeAdminChange(uint64,(uint64,uint64,string,address,uint64,string,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64),uint64,uint64)uint64'] | AlgoSafeArgs['tuple']['proposeAdminChange(uint64,(uint64,uint64,string,address,uint64,string,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64),uint64,uint64)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.proposeAdminChange(params)))
        resultMappers.push((v) => client.decodeReturnValue('proposeAdminChange(uint64,(uint64,uint64,string,address,uint64,string,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64),uint64,uint64)uint64', v))
        return this
      },
      /**
       * Add a approveProposal(uint64,uint64,uint64)void method call against the AlgoSafe contract
       */
      approveProposal(params: CallParams<AlgoSafeArgs['obj']['approveProposal(uint64,uint64,uint64)void'] | AlgoSafeArgs['tuple']['approveProposal(uint64,uint64,uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.approveProposal(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a executeProposal(uint64,uint64)void method call against the AlgoSafe contract
       */
      executeProposal(params: CallParams<AlgoSafeArgs['obj']['executeProposal(uint64,uint64)void'] | AlgoSafeArgs['tuple']['executeProposal(uint64,uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.executeProposal(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a cancelProposal(uint64,uint64)void method call against the AlgoSafe contract
       */
      cancelProposal(params: CallParams<AlgoSafeArgs['obj']['cancelProposal(uint64,uint64)void'] | AlgoSafeArgs['tuple']['cancelProposal(uint64,uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.cancelProposal(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a pruneProposal(uint64,uint64)void method call against the AlgoSafe contract
       */
      pruneProposal(params: CallParams<AlgoSafeArgs['obj']['pruneProposal(uint64,uint64)void'] | AlgoSafeArgs['tuple']['pruneProposal(uint64,uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.pruneProposal(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a clear state call to the AlgoSafe contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as AlgoSafeComposer
  }
}
export type AlgoSafeComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the bootstrap(string)void ABI method.
   *
  * bootstrap — simple one-call bootstrap for a fresh safe.
  Creates group 1 with full admin+action privileges and adds the creator as
  the sole member. Incompatible with bootstrapGroup (the two paths cannot mix).

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  bootstrap(params?: CallParams<AlgoSafeArgs['obj']['bootstrap(string)void'] | AlgoSafeArgs['tuple']['bootstrap(string)void']>): AlgoSafeComposer<[...TReturns, AlgoSafeReturns['bootstrap(string)void'] | undefined]>

  /**
   * Calls the bootstrapGroup((string,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64),(address,uint64,string)[],uint64)uint64 ABI method.
   *
  * bootstrapGroup — clone-friendly bootstrap; may be called multiple times before
  finalizeBootstrap(). Each call creates one group with its full member set.
  Used by deployClonedSafe() to mirror the predecessor safe's governance structure
  into a newly deployed contract before activating it.

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  bootstrapGroup(params?: CallParams<AlgoSafeArgs['obj']['bootstrapGroup((string,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64),(address,uint64,string)[],uint64)uint64'] | AlgoSafeArgs['tuple']['bootstrapGroup((string,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64),(address,uint64,string)[],uint64)uint64']>): AlgoSafeComposer<[...TReturns, AlgoSafeReturns['bootstrapGroup((string,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64),(address,uint64,string)[],uint64)uint64'] | undefined]>

  /**
   * Calls the bootstrapRekeyedAddress(address,string)void ABI method.
   *
  * bootstrapRekeyedAddress — seed the rekeyed-address registry during cloning.
  Only callable before finalizeBootstrap(); mirrors entries from the predecessor.

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  bootstrapRekeyedAddress(params?: CallParams<AlgoSafeArgs['obj']['bootstrapRekeyedAddress(address,string)void'] | AlgoSafeArgs['tuple']['bootstrapRekeyedAddress(address,string)void']>): AlgoSafeComposer<[...TReturns, AlgoSafeReturns['bootstrapRekeyedAddress(address,string)void'] | undefined]>

  /**
   * Calls the finalizeBootstrap()void ABI method.
   *
  * finalizeBootstrap — activates a cloned safe seeded via bootstrapGroup.
  Requires at least one active group with PRIV_GROUP to prevent lockout.
  After this call _newProposal succeeds and normal governance begins.

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  finalizeBootstrap(params?: CallParams<AlgoSafeArgs['obj']['finalizeBootstrap()void'] | AlgoSafeArgs['tuple']['finalizeBootstrap()void']>): AlgoSafeComposer<[...TReturns, AlgoSafeReturns['finalizeBootstrap()void'] | undefined]>

  /**
   * Calls the proposeTransactionGroup(uint64,(uint64,byte[])[],uint64,bool,uint64)uint64 ABI method.
   *
  * proposeTransactionGroup — create and optionally immediately execute a transaction
  proposal. The caller must be a member of groupId.
  
  execute=true skips the approval phase (valid only when threshold=1 and the
  proposer is the sole member, i.e. the proposal is instantly READY).
  
  The payload is stored in chunk slot 1; further chunks can be appended via
  appendTransactionGroupPayload before the first independent approval.

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  proposeTransactionGroup(params?: CallParams<AlgoSafeArgs['obj']['proposeTransactionGroup(uint64,(uint64,byte[])[],uint64,bool,uint64)uint64'] | AlgoSafeArgs['tuple']['proposeTransactionGroup(uint64,(uint64,byte[])[],uint64,bool,uint64)uint64']>): AlgoSafeComposer<[...TReturns, AlgoSafeReturns['proposeTransactionGroup(uint64,(uint64,byte[])[],uint64,bool,uint64)uint64'] | undefined]>

  /**
   * Calls the appendTransactionGroupPayload(uint64,uint64,(uint64,byte[])[],uint64)void ABI method.
   *
  * appendTransactionGroupPayload — add an additional payload chunk to an existing
  transaction proposal. Only the original proposer may append, and only while
  no independent approver has signed yet (approvalsCount === 1 means only the
  auto-approval from _newProposal). Each append bumps payloadVersion so any
  signer that has already approved sees "payload changed since review" on their
  next call and must re-approve the new version.
  
  Prevents appending to expired proposals (blocks extending validity window).

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  appendTransactionGroupPayload(params?: CallParams<AlgoSafeArgs['obj']['appendTransactionGroupPayload(uint64,uint64,(uint64,byte[])[],uint64)void'] | AlgoSafeArgs['tuple']['appendTransactionGroupPayload(uint64,uint64,(uint64,byte[])[],uint64)void']>): AlgoSafeComposer<[...TReturns, AlgoSafeReturns['appendTransactionGroupPayload(uint64,uint64,(uint64,byte[])[],uint64)void'] | undefined]>

  /**
   * Calls the proposeAdminChange(uint64,(uint64,uint64,string,address,uint64,string,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64),uint64,uint64)uint64 ABI method.
   *
  * proposeAdminChange — create a governance change proposal.
  
  Routing:
   ADM_DISSOLVE_CUSTODIAN — the proposing group must be the custodian group
     itself. Admins cannot force-dissolve a custodian.
   All other change types — the proposing group must hold the relevant admin
     privilege (PRIV_GROUP or PRIV_POLICY via _assertPrivilegeForChange).
  
  Payload-level correctness (e.g. guard existence, target group type) is
  intentionally deferred to execution time in _applyAdminChange. This saves
  ~700 bytes of compiled binary by not duplicating the check logic at proposal
  time, at the cost of wasted box storage if an invalid proposal passes approval.
  
  The cooldown cap is asserted here (before the box is written) so that a
  bad cooldown value cannot be stored and later cause an arithmetic overflow
  in _executeProposalInternal even if the group approves the proposal.

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  proposeAdminChange(params?: CallParams<AlgoSafeArgs['obj']['proposeAdminChange(uint64,(uint64,uint64,string,address,uint64,string,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64),uint64,uint64)uint64'] | AlgoSafeArgs['tuple']['proposeAdminChange(uint64,(uint64,uint64,string,address,uint64,string,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64),uint64,uint64)uint64']>): AlgoSafeComposer<[...TReturns, AlgoSafeReturns['proposeAdminChange(uint64,(uint64,uint64,string,address,uint64,string,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64,uint64),uint64,uint64)uint64'] | undefined]>

  /**
   * Calls the approveProposal(uint64,uint64,uint64)void ABI method.
   *
  * approveProposal — add the caller's approval to an open proposal.
  
  expectedPayloadVersion must match the proposal's live payloadVersion; this
  binds the approval to the exact payload content the signer reviewed and
  prevents a bait-and-switch attack where the proposer appends a different
  chunk after other signers have already approved.
  
  Also asserts that the group's membershipEpoch hasn't changed since proposal
  creation — removing a compromised member invalidates all pending approvals.

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  approveProposal(params?: CallParams<AlgoSafeArgs['obj']['approveProposal(uint64,uint64,uint64)void'] | AlgoSafeArgs['tuple']['approveProposal(uint64,uint64,uint64)void']>): AlgoSafeComposer<[...TReturns, AlgoSafeReturns['approveProposal(uint64,uint64,uint64)void'] | undefined]>

  /**
   * Calls the executeProposal(uint64,uint64)void ABI method.
   *
   * executeProposal — execute a READY proposal (threshold met, not expired).
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  executeProposal(params?: CallParams<AlgoSafeArgs['obj']['executeProposal(uint64,uint64)void'] | AlgoSafeArgs['tuple']['executeProposal(uint64,uint64)void']>): AlgoSafeComposer<[...TReturns, AlgoSafeReturns['executeProposal(uint64,uint64)void'] | undefined]>

  /**
   * Calls the cancelProposal(uint64,uint64)void ABI method.
   *
  * cancelProposal — cancel an open proposal. Either the original proposer
  or any current group member may cancel.

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  cancelProposal(params?: CallParams<AlgoSafeArgs['obj']['cancelProposal(uint64,uint64)void'] | AlgoSafeArgs['tuple']['cancelProposal(uint64,uint64)void']>): AlgoSafeComposer<[...TReturns, AlgoSafeReturns['cancelProposal(uint64,uint64)void'] | undefined]>

  /**
   * Calls the pruneProposal(uint64,uint64)void ABI method.
   *
  * pruneProposal — delete terminal proposal boxes to reclaim MBR.
  Only callable on EXECUTED or CANCELLED proposals after their expiryRound.
  Removes the proposal box, all payload chunk boxes, and the admin-change box.

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  pruneProposal(params?: CallParams<AlgoSafeArgs['obj']['pruneProposal(uint64,uint64)void'] | AlgoSafeArgs['tuple']['pruneProposal(uint64,uint64)void']>): AlgoSafeComposer<[...TReturns, AlgoSafeReturns['pruneProposal(uint64,uint64)void'] | undefined]>

  /**
   * Makes a clear_state call to an existing instance of the AlgoSafe smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): AlgoSafeComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): AlgoSafeComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<AlgoSafeComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<AlgoSafeComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<AlgoSafeComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<AlgoSafeComposerResults<TReturns>>
}
export type AlgoSafeComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

