/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"StakingPool","structs":{"UserClaimState":[{"name":"lastEpoch","type":"uint64"},{"name":"lastCumulative","type":"uint64"}]},"methods":[{"name":"create","args":[{"type":"address","name":"sponsor"},{"type":"uint64","name":"rewardTokenId"},{"type":"uint64","name":"poolId"},{"type":"uint64","name":"fundingModel"},{"type":"uint64","name":"startDate"},{"type":"uint64","name":"endDate"},{"type":"address","name":"publisher"}],"returns":{"type":"void"},"actions":{"create":["NoOp"],"call":[]},"readonly":false,"events":[],"recommendations":{}},{"name":"optInAsset","args":[],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Contract opts into the reward token ASA.\nMust be called after creation and before funding.\nAdmin only.","events":[],"recommendations":{}},{"name":"fundPool","args":[{"type":"axfer","name":"axfer"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Deposit reward tokens into the pool.\nOne-time pools can only be funded once; rolling pools allow multiple deposits.\n\nExpects a grouped AssetTransfer from the sponsor preceding this call.","events":[],"recommendations":{}},{"name":"claimRewards","args":[{"type":"uint64","name":"epochId","desc":"- Epoch to claim from (must be published)"},{"type":"uint64","name":"cumulativeAmount","desc":"- Total rewards to date (from Merkle tree leaf)"},{"type":"byte[]","name":"proof","desc":"- Concatenated 32-byte Merkle proof elements"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Claim rewards using a Merkle proof.\n\nCUMULATIVE MODEL: Each epoch's Merkle tree stores total-to-date amounts.\nThe contract pays delta = (newCumulative - lastCumulative).\nUsers can skip epochs without losing rewards — a single claim catches up.\n\nTransaction group structure:\n  First claim:  [ Payment(boxMBR -> contract), AppCall(claimRewards) ]\n  Subsequent:   [ AppCall(claimRewards) ]","events":[],"recommendations":{}},{"name":"deleteBox","args":[],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Delete user's claim box and recover MBR.\nReturns (boxMBR - minTxnFee) to the caller.","events":[],"recommendations":{}},{"name":"setEpochRoot","args":[{"type":"uint64","name":"epochId"},{"type":"byte[]","name":"root"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Publish a new Merkle root for an epoch.\nCallable by admin or delegated publisher.\nRoots are immutable — cannot republish the same epoch.\nEpoch IDs must be strictly monotonically increasing.","events":[],"recommendations":{}},{"name":"togglePause","args":[{"type":"uint64","name":"state"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Emergency pause / unpause. Admin only.","events":[],"recommendations":{}},{"name":"toggleDeprecated","args":[{"type":"uint64","name":"state"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Deprecate pool (one-way). Admin only.\nOnce deprecated: no claims, no new roots, emergency withdrawal available.","events":[],"recommendations":{}},{"name":"emergencyWithdraw","args":[],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Emergency withdraw all remaining rewards to sponsor.\nPool must be deprecated first.","events":[],"recommendations":{}},{"name":"updateEndDate","args":[{"type":"uint64","name":"newEndDate"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Update pool end date (informational only). Admin only.","events":[],"recommendations":{}},{"name":"updatePublisher","args":[{"type":"address","name":"newPublisher"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Update the publisher address for automated epoch publishing. Admin only.","events":[],"recommendations":{}}],"arcs":[22,28],"networks":{},"state":{"schema":{"global":{"ints":9,"bytes":3},"local":{"ints":0,"bytes":0}},"keys":{"global":{"admin":{"keyType":"AVMString","valueType":"address","key":"YWRtaW4="},"sponsor":{"keyType":"AVMString","valueType":"address","key":"c3BvbnNvcg=="},"rewardTokenId":{"keyType":"AVMString","valueType":"AVMUint64","key":"cmV3YXJkX3Rva2Vu"},"poolId":{"keyType":"AVMString","valueType":"AVMUint64","key":"cG9vbF9pZA=="},"fundingModel":{"keyType":"AVMString","valueType":"AVMUint64","key":"ZnVuZGluZw=="},"poolStartDate":{"keyType":"AVMString","valueType":"AVMUint64","key":"c3RhcnRfZGF0ZQ=="},"poolEndDate":{"keyType":"AVMString","valueType":"AVMUint64","key":"ZW5kX2RhdGU="},"currentEpochId":{"keyType":"AVMString","valueType":"AVMUint64","key":"ZXBvY2hfaWQ="},"totalDeposited":{"keyType":"AVMString","valueType":"AVMUint64","key":"ZGVwb3NpdGVk"},"paused":{"keyType":"AVMString","valueType":"AVMUint64","key":"cGF1c2Vk"},"deprecated":{"keyType":"AVMString","valueType":"AVMUint64","key":"ZGVwcmVjYXRlZA=="},"publisher":{"keyType":"AVMString","valueType":"address","key":"cHVibGlzaGVy"}},"local":{},"box":{}},"maps":{"global":{},"local":{},"box":{"userClaims":{"keyType":"address","valueType":"UserClaimState","prefix":"dQ=="}}}},"bareActions":{"create":[],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[400,443,1073,1113,1155,1253,1275],"errorMessage":"Admin only"},{"pc":[490],"errorMessage":"Already funded"},{"pc":[475,582],"errorMessage":"Amount must be positive"},{"pc":[667],"errorMessage":"Box must have value"},{"pc":[1137],"errorMessage":"Cannot reactivate"},{"pc":[692],"errorMessage":"Cumulative cannot decrease"},{"pc":[1009],"errorMessage":"Epoch not increasing"},{"pc":[617],"errorMessage":"Epoch not published"},{"pc":[641],"errorMessage":"Epoch root not found"},{"pc":[862],"errorMessage":"Insufficient MBR"},{"pc":[802],"errorMessage":"Insufficient funds"},{"pc":[784],"errorMessage":"Invalid Merkle proof"},{"pc":[608,979],"errorMessage":"Invalid epoch"},{"pc":[333],"errorMessage":"Invalid funding model"},{"pc":[321],"errorMessage":"Invalid pool ID"},{"pc":[316,1280],"errorMessage":"Invalid publisher"},{"pc":[319],"errorMessage":"Invalid reward token"},{"pc":[645],"errorMessage":"Invalid root"},{"pc":[976],"errorMessage":"Invalid root size"},{"pc":[312],"errorMessage":"Invalid sponsor"},{"pc":[1084,1124],"errorMessage":"Invalid state"},{"pc":[856],"errorMessage":"MBR receiver mismatch"},{"pc":[849],"errorMessage":"MBR sender mismatch"},{"pc":[682],"errorMessage":"Must claim newer epoch"},{"pc":[1162],"errorMessage":"Must deprecate first"},{"pc":[880],"errorMessage":"No box to delete"},{"pc":[971],"errorMessage":"Not authorized"},{"pc":[700],"errorMessage":"Nothing to claim"},{"pc":[131],"errorMessage":"OnCompletion must be NoOp"},{"pc":[985],"errorMessage":"Pool deprecated"},{"pc":[595],"errorMessage":"Pool is deprecated"},{"pc":[589],"errorMessage":"Pool is paused"},{"pc":[872],"errorMessage":"Pool must be deprecated"},{"pc":[604],"errorMessage":"Pool not started"},{"pc":[629],"errorMessage":"Proof too deep"},{"pc":[1017],"errorMessage":"Root already published"},{"pc":[795,1180],"errorMessage":"Token not opted in"},{"pc":[471],"errorMessage":"assert target is match for conditions"},{"pc":[398,405,441,450,468,480,488,495,587,593,602,613,711,788,869,954,965,983,996,1071,1111,1128,1153,1159,1170,1189,1251,1273],"errorMessage":"check GlobalState exists"},{"pc":[535,937],"errorMessage":"invalid array length header"},{"pc":[543,945],"errorMessage":"invalid number of bytes for arc4.dynamic_array<arc4.uint8>"},{"pc":[237,306,1267],"errorMessage":"invalid number of bytes for arc4.static_array<arc4.uint8, 32>"},{"pc":[245,257,269,281,292,519,528,930,1063,1103,1244],"errorMessage":"invalid number of bytes for arc4.uint64"},{"pc":[435],"errorMessage":"transaction type is axfer"},{"pc":[842],"errorMessage":"transaction type is pay"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"events":[]} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


// Type definitions for ARC-56 structs

export type UserClaimState = {
  lastEpoch: bigint,
  lastCumulative: bigint
}


/**
 * Converts the ABI tuple representation of a UserClaimState to the struct representation
 */
export function UserClaimStateFromTuple(abiTuple: [bigint, bigint]) {
  return getABIStructFromABITuple(abiTuple, APP_SPEC.structs.UserClaimState, APP_SPEC.structs) as UserClaimState
}

/**
 * The argument types for the StakingPool contract
 */
export type StakingPoolArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'create(address,uint64,uint64,uint64,uint64,uint64,address)void': {
      sponsor: string
      rewardTokenId: bigint | number
      poolId: bigint | number
      fundingModel: bigint | number
      startDate: bigint | number
      endDate: bigint | number
      publisher: string
    }
    'optInAsset()void': Record<string, never>
    'fundPool(axfer)void': {
      axfer: AppMethodCallTransactionArgument
    }
    'claimRewards(uint64,uint64,byte[])void': {
      /**
       * - Epoch to claim from (must be published)
       */
      epochId: bigint | number
      /**
       * - Total rewards to date (from Merkle tree leaf)
       */
      cumulativeAmount: bigint | number
      /**
       * - Concatenated 32-byte Merkle proof elements
       */
      proof: Uint8Array
    }
    'deleteBox()void': Record<string, never>
    'setEpochRoot(uint64,byte[])void': {
      epochId: bigint | number
      root: Uint8Array
    }
    'togglePause(uint64)void': {
      state: bigint | number
    }
    'toggleDeprecated(uint64)void': {
      state: bigint | number
    }
    'emergencyWithdraw()void': Record<string, never>
    'updateEndDate(uint64)void': {
      newEndDate: bigint | number
    }
    'updatePublisher(address)void': {
      newPublisher: string
    }
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'create(address,uint64,uint64,uint64,uint64,uint64,address)void': [sponsor: string, rewardTokenId: bigint | number, poolId: bigint | number, fundingModel: bigint | number, startDate: bigint | number, endDate: bigint | number, publisher: string]
    'optInAsset()void': []
    'fundPool(axfer)void': [axfer: AppMethodCallTransactionArgument]
    'claimRewards(uint64,uint64,byte[])void': [epochId: bigint | number, cumulativeAmount: bigint | number, proof: Uint8Array]
    'deleteBox()void': []
    'setEpochRoot(uint64,byte[])void': [epochId: bigint | number, root: Uint8Array]
    'togglePause(uint64)void': [state: bigint | number]
    'toggleDeprecated(uint64)void': [state: bigint | number]
    'emergencyWithdraw()void': []
    'updateEndDate(uint64)void': [newEndDate: bigint | number]
    'updatePublisher(address)void': [newPublisher: string]
  }
}

/**
 * The return type for each method
 */
export type StakingPoolReturns = {
  'create(address,uint64,uint64,uint64,uint64,uint64,address)void': void
  'optInAsset()void': void
  'fundPool(axfer)void': void
  'claimRewards(uint64,uint64,byte[])void': void
  'deleteBox()void': void
  'setEpochRoot(uint64,byte[])void': void
  'togglePause(uint64)void': void
  'toggleDeprecated(uint64)void': void
  'emergencyWithdraw()void': void
  'updateEndDate(uint64)void': void
  'updatePublisher(address)void': void
}

/**
 * Defines the types of available calls and state of the StakingPool smart contract.
 */
export type StakingPoolTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'create(address,uint64,uint64,uint64,uint64,uint64,address)void' | 'create', {
      argsObj: StakingPoolArgs['obj']['create(address,uint64,uint64,uint64,uint64,uint64,address)void']
      argsTuple: StakingPoolArgs['tuple']['create(address,uint64,uint64,uint64,uint64,uint64,address)void']
      returns: StakingPoolReturns['create(address,uint64,uint64,uint64,uint64,uint64,address)void']
    }>
    & Record<'optInAsset()void' | 'optInAsset', {
      argsObj: StakingPoolArgs['obj']['optInAsset()void']
      argsTuple: StakingPoolArgs['tuple']['optInAsset()void']
      returns: StakingPoolReturns['optInAsset()void']
    }>
    & Record<'fundPool(axfer)void' | 'fundPool', {
      argsObj: StakingPoolArgs['obj']['fundPool(axfer)void']
      argsTuple: StakingPoolArgs['tuple']['fundPool(axfer)void']
      returns: StakingPoolReturns['fundPool(axfer)void']
    }>
    & Record<'claimRewards(uint64,uint64,byte[])void' | 'claimRewards', {
      argsObj: StakingPoolArgs['obj']['claimRewards(uint64,uint64,byte[])void']
      argsTuple: StakingPoolArgs['tuple']['claimRewards(uint64,uint64,byte[])void']
      returns: StakingPoolReturns['claimRewards(uint64,uint64,byte[])void']
    }>
    & Record<'deleteBox()void' | 'deleteBox', {
      argsObj: StakingPoolArgs['obj']['deleteBox()void']
      argsTuple: StakingPoolArgs['tuple']['deleteBox()void']
      returns: StakingPoolReturns['deleteBox()void']
    }>
    & Record<'setEpochRoot(uint64,byte[])void' | 'setEpochRoot', {
      argsObj: StakingPoolArgs['obj']['setEpochRoot(uint64,byte[])void']
      argsTuple: StakingPoolArgs['tuple']['setEpochRoot(uint64,byte[])void']
      returns: StakingPoolReturns['setEpochRoot(uint64,byte[])void']
    }>
    & Record<'togglePause(uint64)void' | 'togglePause', {
      argsObj: StakingPoolArgs['obj']['togglePause(uint64)void']
      argsTuple: StakingPoolArgs['tuple']['togglePause(uint64)void']
      returns: StakingPoolReturns['togglePause(uint64)void']
    }>
    & Record<'toggleDeprecated(uint64)void' | 'toggleDeprecated', {
      argsObj: StakingPoolArgs['obj']['toggleDeprecated(uint64)void']
      argsTuple: StakingPoolArgs['tuple']['toggleDeprecated(uint64)void']
      returns: StakingPoolReturns['toggleDeprecated(uint64)void']
    }>
    & Record<'emergencyWithdraw()void' | 'emergencyWithdraw', {
      argsObj: StakingPoolArgs['obj']['emergencyWithdraw()void']
      argsTuple: StakingPoolArgs['tuple']['emergencyWithdraw()void']
      returns: StakingPoolReturns['emergencyWithdraw()void']
    }>
    & Record<'updateEndDate(uint64)void' | 'updateEndDate', {
      argsObj: StakingPoolArgs['obj']['updateEndDate(uint64)void']
      argsTuple: StakingPoolArgs['tuple']['updateEndDate(uint64)void']
      returns: StakingPoolReturns['updateEndDate(uint64)void']
    }>
    & Record<'updatePublisher(address)void' | 'updatePublisher', {
      argsObj: StakingPoolArgs['obj']['updatePublisher(address)void']
      argsTuple: StakingPoolArgs['tuple']['updatePublisher(address)void']
      returns: StakingPoolReturns['updatePublisher(address)void']
    }>
  /**
   * Defines the shape of the state of the application.
   */
  state: {
    global: {
      keys: {
        admin: string
        sponsor: string
        rewardTokenId: bigint
        poolId: bigint
        fundingModel: bigint
        poolStartDate: bigint
        poolEndDate: bigint
        currentEpochId: bigint
        totalDeposited: bigint
        paused: bigint
        deprecated: bigint
        publisher: string
      }
      maps: {}
    }
    box: {
      keys: {}
      maps: {
        userClaims: Map<string, UserClaimState>
      }
    }
  }
}

/**
 * Defines the possible abi call signatures.
 */
export type StakingPoolSignatures = keyof StakingPoolTypes['methods']
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the StakingPool smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends StakingPoolSignatures> = StakingPoolTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the StakingPool smart contract to the method's return type
 */
export type MethodReturn<TSignature extends StakingPoolSignatures> = StakingPoolTypes['methods'][TSignature]['returns']

/**
 * Defines the shape of the keyed global state of the application.
 */
export type GlobalKeysState = StakingPoolTypes['state']['global']['keys']

/**
 * Defines the shape of the keyed box state of the application.
 */
export type BoxKeysState = StakingPoolTypes['state']['box']['keys']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the StakingPool smart contract
 */
export abstract class StakingPoolParamsFactory {
  /**
   * Constructs a no op call for the optInAsset()void ABI method
   *
  * Contract opts into the reward token ASA.
  Must be called after creation and before funding.
  Admin only.

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static optInAsset(params: CallParams<StakingPoolArgs['obj']['optInAsset()void'] | StakingPoolArgs['tuple']['optInAsset()void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'optInAsset()void' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the fundPool(axfer)void ABI method
   *
  * Deposit reward tokens into the pool.
  One-time pools can only be funded once; rolling pools allow multiple deposits.
  
  Expects a grouped AssetTransfer from the sponsor preceding this call.

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static fundPool(params: CallParams<StakingPoolArgs['obj']['fundPool(axfer)void'] | StakingPoolArgs['tuple']['fundPool(axfer)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'fundPool(axfer)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.axfer],
    }
  }
  /**
   * Constructs a no op call for the claimRewards(uint64,uint64,byte[])void ABI method
   *
  * Claim rewards using a Merkle proof.
  
  CUMULATIVE MODEL: Each epoch's Merkle tree stores total-to-date amounts.
  The contract pays delta = (newCumulative - lastCumulative).
  Users can skip epochs without losing rewards — a single claim catches up.
  
  Transaction group structure:
   First claim:  [ Payment(boxMBR -> contract), AppCall(claimRewards) ]
   Subsequent:   [ AppCall(claimRewards) ]

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static claimRewards(params: CallParams<StakingPoolArgs['obj']['claimRewards(uint64,uint64,byte[])void'] | StakingPoolArgs['tuple']['claimRewards(uint64,uint64,byte[])void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'claimRewards(uint64,uint64,byte[])void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.epochId, params.args.cumulativeAmount, params.args.proof],
    }
  }
  /**
   * Constructs a no op call for the deleteBox()void ABI method
   *
  * Delete user's claim box and recover MBR.
  Returns (boxMBR - minTxnFee) to the caller.

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static deleteBox(params: CallParams<StakingPoolArgs['obj']['deleteBox()void'] | StakingPoolArgs['tuple']['deleteBox()void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'deleteBox()void' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the setEpochRoot(uint64,byte[])void ABI method
   *
  * Publish a new Merkle root for an epoch.
  Callable by admin or delegated publisher.
  Roots are immutable — cannot republish the same epoch.
  Epoch IDs must be strictly monotonically increasing.

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static setEpochRoot(params: CallParams<StakingPoolArgs['obj']['setEpochRoot(uint64,byte[])void'] | StakingPoolArgs['tuple']['setEpochRoot(uint64,byte[])void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'setEpochRoot(uint64,byte[])void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.epochId, params.args.root],
    }
  }
  /**
   * Constructs a no op call for the togglePause(uint64)void ABI method
   *
   * Emergency pause / unpause. Admin only.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static togglePause(params: CallParams<StakingPoolArgs['obj']['togglePause(uint64)void'] | StakingPoolArgs['tuple']['togglePause(uint64)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'togglePause(uint64)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.state],
    }
  }
  /**
   * Constructs a no op call for the toggleDeprecated(uint64)void ABI method
   *
  * Deprecate pool (one-way). Admin only.
  Once deprecated: no claims, no new roots, emergency withdrawal available.

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static toggleDeprecated(params: CallParams<StakingPoolArgs['obj']['toggleDeprecated(uint64)void'] | StakingPoolArgs['tuple']['toggleDeprecated(uint64)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'toggleDeprecated(uint64)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.state],
    }
  }
  /**
   * Constructs a no op call for the emergencyWithdraw()void ABI method
   *
  * Emergency withdraw all remaining rewards to sponsor.
  Pool must be deprecated first.

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static emergencyWithdraw(params: CallParams<StakingPoolArgs['obj']['emergencyWithdraw()void'] | StakingPoolArgs['tuple']['emergencyWithdraw()void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'emergencyWithdraw()void' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the updateEndDate(uint64)void ABI method
   *
   * Update pool end date (informational only). Admin only.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static updateEndDate(params: CallParams<StakingPoolArgs['obj']['updateEndDate(uint64)void'] | StakingPoolArgs['tuple']['updateEndDate(uint64)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'updateEndDate(uint64)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.newEndDate],
    }
  }
  /**
   * Constructs a no op call for the updatePublisher(address)void ABI method
   *
   * Update the publisher address for automated epoch publishing. Admin only.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static updatePublisher(params: CallParams<StakingPoolArgs['obj']['updatePublisher(address)void'] | StakingPoolArgs['tuple']['updatePublisher(address)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'updatePublisher(address)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.newPublisher],
    }
  }
}

/**
 * A client to make calls to the StakingPool smart contract
 */
export class StakingPoolClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `StakingPoolClient`
   *
   * @param appClient An `AppClient` instance which has been created with the StakingPool app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `StakingPoolClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Returns a new `StakingPoolClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<StakingPoolClient> {
    return new StakingPoolClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `StakingPoolClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<StakingPoolClient> {
    return new StakingPoolClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Makes a clear_state call to an existing instance of the StakingPool smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the StakingPool smart contract using the `optInAsset()void` ABI method.
     *
    * Contract opts into the reward token ASA.
    Must be called after creation and before funding.
    Admin only.

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    optInAsset: (params: CallParams<StakingPoolArgs['obj']['optInAsset()void'] | StakingPoolArgs['tuple']['optInAsset()void']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(StakingPoolParamsFactory.optInAsset(params))
    },

    /**
     * Makes a call to the StakingPool smart contract using the `fundPool(axfer)void` ABI method.
     *
    * Deposit reward tokens into the pool.
    One-time pools can only be funded once; rolling pools allow multiple deposits.
    
    Expects a grouped AssetTransfer from the sponsor preceding this call.

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    fundPool: (params: CallParams<StakingPoolArgs['obj']['fundPool(axfer)void'] | StakingPoolArgs['tuple']['fundPool(axfer)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(StakingPoolParamsFactory.fundPool(params))
    },

    /**
     * Makes a call to the StakingPool smart contract using the `claimRewards(uint64,uint64,byte[])void` ABI method.
     *
    * Claim rewards using a Merkle proof.
    
    CUMULATIVE MODEL: Each epoch's Merkle tree stores total-to-date amounts.
    The contract pays delta = (newCumulative - lastCumulative).
    Users can skip epochs without losing rewards — a single claim catches up.
    
    Transaction group structure:
     First claim:  [ Payment(boxMBR -> contract), AppCall(claimRewards) ]
     Subsequent:   [ AppCall(claimRewards) ]

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    claimRewards: (params: CallParams<StakingPoolArgs['obj']['claimRewards(uint64,uint64,byte[])void'] | StakingPoolArgs['tuple']['claimRewards(uint64,uint64,byte[])void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(StakingPoolParamsFactory.claimRewards(params))
    },

    /**
     * Makes a call to the StakingPool smart contract using the `deleteBox()void` ABI method.
     *
    * Delete user's claim box and recover MBR.
    Returns (boxMBR - minTxnFee) to the caller.

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    deleteBox: (params: CallParams<StakingPoolArgs['obj']['deleteBox()void'] | StakingPoolArgs['tuple']['deleteBox()void']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(StakingPoolParamsFactory.deleteBox(params))
    },

    /**
     * Makes a call to the StakingPool smart contract using the `setEpochRoot(uint64,byte[])void` ABI method.
     *
    * Publish a new Merkle root for an epoch.
    Callable by admin or delegated publisher.
    Roots are immutable — cannot republish the same epoch.
    Epoch IDs must be strictly monotonically increasing.

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    setEpochRoot: (params: CallParams<StakingPoolArgs['obj']['setEpochRoot(uint64,byte[])void'] | StakingPoolArgs['tuple']['setEpochRoot(uint64,byte[])void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(StakingPoolParamsFactory.setEpochRoot(params))
    },

    /**
     * Makes a call to the StakingPool smart contract using the `togglePause(uint64)void` ABI method.
     *
     * Emergency pause / unpause. Admin only.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    togglePause: (params: CallParams<StakingPoolArgs['obj']['togglePause(uint64)void'] | StakingPoolArgs['tuple']['togglePause(uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(StakingPoolParamsFactory.togglePause(params))
    },

    /**
     * Makes a call to the StakingPool smart contract using the `toggleDeprecated(uint64)void` ABI method.
     *
    * Deprecate pool (one-way). Admin only.
    Once deprecated: no claims, no new roots, emergency withdrawal available.

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    toggleDeprecated: (params: CallParams<StakingPoolArgs['obj']['toggleDeprecated(uint64)void'] | StakingPoolArgs['tuple']['toggleDeprecated(uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(StakingPoolParamsFactory.toggleDeprecated(params))
    },

    /**
     * Makes a call to the StakingPool smart contract using the `emergencyWithdraw()void` ABI method.
     *
    * Emergency withdraw all remaining rewards to sponsor.
    Pool must be deprecated first.

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    emergencyWithdraw: (params: CallParams<StakingPoolArgs['obj']['emergencyWithdraw()void'] | StakingPoolArgs['tuple']['emergencyWithdraw()void']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(StakingPoolParamsFactory.emergencyWithdraw(params))
    },

    /**
     * Makes a call to the StakingPool smart contract using the `updateEndDate(uint64)void` ABI method.
     *
     * Update pool end date (informational only). Admin only.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    updateEndDate: (params: CallParams<StakingPoolArgs['obj']['updateEndDate(uint64)void'] | StakingPoolArgs['tuple']['updateEndDate(uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(StakingPoolParamsFactory.updateEndDate(params))
    },

    /**
     * Makes a call to the StakingPool smart contract using the `updatePublisher(address)void` ABI method.
     *
     * Update the publisher address for automated epoch publishing. Admin only.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    updatePublisher: (params: CallParams<StakingPoolArgs['obj']['updatePublisher(address)void'] | StakingPoolArgs['tuple']['updatePublisher(address)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(StakingPoolParamsFactory.updatePublisher(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Makes a clear_state call to an existing instance of the StakingPool smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the StakingPool smart contract using the `optInAsset()void` ABI method.
     *
    * Contract opts into the reward token ASA.
    Must be called after creation and before funding.
    Admin only.

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    optInAsset: (params: CallParams<StakingPoolArgs['obj']['optInAsset()void'] | StakingPoolArgs['tuple']['optInAsset()void']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(StakingPoolParamsFactory.optInAsset(params))
    },

    /**
     * Makes a call to the StakingPool smart contract using the `fundPool(axfer)void` ABI method.
     *
    * Deposit reward tokens into the pool.
    One-time pools can only be funded once; rolling pools allow multiple deposits.
    
    Expects a grouped AssetTransfer from the sponsor preceding this call.

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    fundPool: (params: CallParams<StakingPoolArgs['obj']['fundPool(axfer)void'] | StakingPoolArgs['tuple']['fundPool(axfer)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(StakingPoolParamsFactory.fundPool(params))
    },

    /**
     * Makes a call to the StakingPool smart contract using the `claimRewards(uint64,uint64,byte[])void` ABI method.
     *
    * Claim rewards using a Merkle proof.
    
    CUMULATIVE MODEL: Each epoch's Merkle tree stores total-to-date amounts.
    The contract pays delta = (newCumulative - lastCumulative).
    Users can skip epochs without losing rewards — a single claim catches up.
    
    Transaction group structure:
     First claim:  [ Payment(boxMBR -> contract), AppCall(claimRewards) ]
     Subsequent:   [ AppCall(claimRewards) ]

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    claimRewards: (params: CallParams<StakingPoolArgs['obj']['claimRewards(uint64,uint64,byte[])void'] | StakingPoolArgs['tuple']['claimRewards(uint64,uint64,byte[])void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(StakingPoolParamsFactory.claimRewards(params))
    },

    /**
     * Makes a call to the StakingPool smart contract using the `deleteBox()void` ABI method.
     *
    * Delete user's claim box and recover MBR.
    Returns (boxMBR - minTxnFee) to the caller.

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    deleteBox: (params: CallParams<StakingPoolArgs['obj']['deleteBox()void'] | StakingPoolArgs['tuple']['deleteBox()void']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(StakingPoolParamsFactory.deleteBox(params))
    },

    /**
     * Makes a call to the StakingPool smart contract using the `setEpochRoot(uint64,byte[])void` ABI method.
     *
    * Publish a new Merkle root for an epoch.
    Callable by admin or delegated publisher.
    Roots are immutable — cannot republish the same epoch.
    Epoch IDs must be strictly monotonically increasing.

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    setEpochRoot: (params: CallParams<StakingPoolArgs['obj']['setEpochRoot(uint64,byte[])void'] | StakingPoolArgs['tuple']['setEpochRoot(uint64,byte[])void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(StakingPoolParamsFactory.setEpochRoot(params))
    },

    /**
     * Makes a call to the StakingPool smart contract using the `togglePause(uint64)void` ABI method.
     *
     * Emergency pause / unpause. Admin only.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    togglePause: (params: CallParams<StakingPoolArgs['obj']['togglePause(uint64)void'] | StakingPoolArgs['tuple']['togglePause(uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(StakingPoolParamsFactory.togglePause(params))
    },

    /**
     * Makes a call to the StakingPool smart contract using the `toggleDeprecated(uint64)void` ABI method.
     *
    * Deprecate pool (one-way). Admin only.
    Once deprecated: no claims, no new roots, emergency withdrawal available.

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    toggleDeprecated: (params: CallParams<StakingPoolArgs['obj']['toggleDeprecated(uint64)void'] | StakingPoolArgs['tuple']['toggleDeprecated(uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(StakingPoolParamsFactory.toggleDeprecated(params))
    },

    /**
     * Makes a call to the StakingPool smart contract using the `emergencyWithdraw()void` ABI method.
     *
    * Emergency withdraw all remaining rewards to sponsor.
    Pool must be deprecated first.

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    emergencyWithdraw: (params: CallParams<StakingPoolArgs['obj']['emergencyWithdraw()void'] | StakingPoolArgs['tuple']['emergencyWithdraw()void']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(StakingPoolParamsFactory.emergencyWithdraw(params))
    },

    /**
     * Makes a call to the StakingPool smart contract using the `updateEndDate(uint64)void` ABI method.
     *
     * Update pool end date (informational only). Admin only.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    updateEndDate: (params: CallParams<StakingPoolArgs['obj']['updateEndDate(uint64)void'] | StakingPoolArgs['tuple']['updateEndDate(uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(StakingPoolParamsFactory.updateEndDate(params))
    },

    /**
     * Makes a call to the StakingPool smart contract using the `updatePublisher(address)void` ABI method.
     *
     * Update the publisher address for automated epoch publishing. Admin only.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    updatePublisher: (params: CallParams<StakingPoolArgs['obj']['updatePublisher(address)void'] | StakingPoolArgs['tuple']['updatePublisher(address)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(StakingPoolParamsFactory.updatePublisher(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Makes a clear_state call to an existing instance of the StakingPool smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the StakingPool smart contract using the `optInAsset()void` ABI method.
     *
    * Contract opts into the reward token ASA.
    Must be called after creation and before funding.
    Admin only.

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    optInAsset: async (params: CallParams<StakingPoolArgs['obj']['optInAsset()void'] | StakingPoolArgs['tuple']['optInAsset()void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(StakingPoolParamsFactory.optInAsset(params))
      return {...result, return: result.return as unknown as (undefined | StakingPoolReturns['optInAsset()void'])}
    },

    /**
     * Makes a call to the StakingPool smart contract using the `fundPool(axfer)void` ABI method.
     *
    * Deposit reward tokens into the pool.
    One-time pools can only be funded once; rolling pools allow multiple deposits.
    
    Expects a grouped AssetTransfer from the sponsor preceding this call.

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    fundPool: async (params: CallParams<StakingPoolArgs['obj']['fundPool(axfer)void'] | StakingPoolArgs['tuple']['fundPool(axfer)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(StakingPoolParamsFactory.fundPool(params))
      return {...result, return: result.return as unknown as (undefined | StakingPoolReturns['fundPool(axfer)void'])}
    },

    /**
     * Makes a call to the StakingPool smart contract using the `claimRewards(uint64,uint64,byte[])void` ABI method.
     *
    * Claim rewards using a Merkle proof.
    
    CUMULATIVE MODEL: Each epoch's Merkle tree stores total-to-date amounts.
    The contract pays delta = (newCumulative - lastCumulative).
    Users can skip epochs without losing rewards — a single claim catches up.
    
    Transaction group structure:
     First claim:  [ Payment(boxMBR -> contract), AppCall(claimRewards) ]
     Subsequent:   [ AppCall(claimRewards) ]

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    claimRewards: async (params: CallParams<StakingPoolArgs['obj']['claimRewards(uint64,uint64,byte[])void'] | StakingPoolArgs['tuple']['claimRewards(uint64,uint64,byte[])void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(StakingPoolParamsFactory.claimRewards(params))
      return {...result, return: result.return as unknown as (undefined | StakingPoolReturns['claimRewards(uint64,uint64,byte[])void'])}
    },

    /**
     * Makes a call to the StakingPool smart contract using the `deleteBox()void` ABI method.
     *
    * Delete user's claim box and recover MBR.
    Returns (boxMBR - minTxnFee) to the caller.

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    deleteBox: async (params: CallParams<StakingPoolArgs['obj']['deleteBox()void'] | StakingPoolArgs['tuple']['deleteBox()void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(StakingPoolParamsFactory.deleteBox(params))
      return {...result, return: result.return as unknown as (undefined | StakingPoolReturns['deleteBox()void'])}
    },

    /**
     * Makes a call to the StakingPool smart contract using the `setEpochRoot(uint64,byte[])void` ABI method.
     *
    * Publish a new Merkle root for an epoch.
    Callable by admin or delegated publisher.
    Roots are immutable — cannot republish the same epoch.
    Epoch IDs must be strictly monotonically increasing.

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    setEpochRoot: async (params: CallParams<StakingPoolArgs['obj']['setEpochRoot(uint64,byte[])void'] | StakingPoolArgs['tuple']['setEpochRoot(uint64,byte[])void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(StakingPoolParamsFactory.setEpochRoot(params))
      return {...result, return: result.return as unknown as (undefined | StakingPoolReturns['setEpochRoot(uint64,byte[])void'])}
    },

    /**
     * Makes a call to the StakingPool smart contract using the `togglePause(uint64)void` ABI method.
     *
     * Emergency pause / unpause. Admin only.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    togglePause: async (params: CallParams<StakingPoolArgs['obj']['togglePause(uint64)void'] | StakingPoolArgs['tuple']['togglePause(uint64)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(StakingPoolParamsFactory.togglePause(params))
      return {...result, return: result.return as unknown as (undefined | StakingPoolReturns['togglePause(uint64)void'])}
    },

    /**
     * Makes a call to the StakingPool smart contract using the `toggleDeprecated(uint64)void` ABI method.
     *
    * Deprecate pool (one-way). Admin only.
    Once deprecated: no claims, no new roots, emergency withdrawal available.

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    toggleDeprecated: async (params: CallParams<StakingPoolArgs['obj']['toggleDeprecated(uint64)void'] | StakingPoolArgs['tuple']['toggleDeprecated(uint64)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(StakingPoolParamsFactory.toggleDeprecated(params))
      return {...result, return: result.return as unknown as (undefined | StakingPoolReturns['toggleDeprecated(uint64)void'])}
    },

    /**
     * Makes a call to the StakingPool smart contract using the `emergencyWithdraw()void` ABI method.
     *
    * Emergency withdraw all remaining rewards to sponsor.
    Pool must be deprecated first.

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    emergencyWithdraw: async (params: CallParams<StakingPoolArgs['obj']['emergencyWithdraw()void'] | StakingPoolArgs['tuple']['emergencyWithdraw()void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(StakingPoolParamsFactory.emergencyWithdraw(params))
      return {...result, return: result.return as unknown as (undefined | StakingPoolReturns['emergencyWithdraw()void'])}
    },

    /**
     * Makes a call to the StakingPool smart contract using the `updateEndDate(uint64)void` ABI method.
     *
     * Update pool end date (informational only). Admin only.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    updateEndDate: async (params: CallParams<StakingPoolArgs['obj']['updateEndDate(uint64)void'] | StakingPoolArgs['tuple']['updateEndDate(uint64)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(StakingPoolParamsFactory.updateEndDate(params))
      return {...result, return: result.return as unknown as (undefined | StakingPoolReturns['updateEndDate(uint64)void'])}
    },

    /**
     * Makes a call to the StakingPool smart contract using the `updatePublisher(address)void` ABI method.
     *
     * Update the publisher address for automated epoch publishing. Admin only.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    updatePublisher: async (params: CallParams<StakingPoolArgs['obj']['updatePublisher(address)void'] | StakingPoolArgs['tuple']['updatePublisher(address)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(StakingPoolParamsFactory.updatePublisher(params))
      return {...result, return: result.return as unknown as (undefined | StakingPoolReturns['updatePublisher(address)void'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new StakingPoolClient(this.appClient.clone(params))
  }

  /**
   * Methods to access state for the current StakingPool app
   */
  state = {
    /**
     * Methods to access global state for the current StakingPool app
     */
    global: {
      /**
       * Get all current keyed values from global state
       */
      getAll: async (): Promise<Partial<Expand<GlobalKeysState>>> => {
        const result = await this.appClient.state.global.getAll()
        return {
          admin: result.admin,
          sponsor: result.sponsor,
          rewardTokenId: result.rewardTokenId,
          poolId: result.poolId,
          fundingModel: result.fundingModel,
          poolStartDate: result.poolStartDate,
          poolEndDate: result.poolEndDate,
          currentEpochId: result.currentEpochId,
          totalDeposited: result.totalDeposited,
          paused: result.paused,
          deprecated: result.deprecated,
          publisher: result.publisher,
        }
      },
      /**
       * Get the current value of the admin key in global state
       */
      admin: async (): Promise<string | undefined> => { return (await this.appClient.state.global.getValue("admin")) as string | undefined },
      /**
       * Get the current value of the sponsor key in global state
       */
      sponsor: async (): Promise<string | undefined> => { return (await this.appClient.state.global.getValue("sponsor")) as string | undefined },
      /**
       * Get the current value of the rewardTokenId key in global state
       */
      rewardTokenId: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("rewardTokenId")) as bigint | undefined },
      /**
       * Get the current value of the poolId key in global state
       */
      poolId: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("poolId")) as bigint | undefined },
      /**
       * Get the current value of the fundingModel key in global state
       */
      fundingModel: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("fundingModel")) as bigint | undefined },
      /**
       * Get the current value of the poolStartDate key in global state
       */
      poolStartDate: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("poolStartDate")) as bigint | undefined },
      /**
       * Get the current value of the poolEndDate key in global state
       */
      poolEndDate: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("poolEndDate")) as bigint | undefined },
      /**
       * Get the current value of the currentEpochId key in global state
       */
      currentEpochId: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("currentEpochId")) as bigint | undefined },
      /**
       * Get the current value of the totalDeposited key in global state
       */
      totalDeposited: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("totalDeposited")) as bigint | undefined },
      /**
       * Get the current value of the paused key in global state
       */
      paused: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("paused")) as bigint | undefined },
      /**
       * Get the current value of the deprecated key in global state
       */
      deprecated: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("deprecated")) as bigint | undefined },
      /**
       * Get the current value of the publisher key in global state
       */
      publisher: async (): Promise<string | undefined> => { return (await this.appClient.state.global.getValue("publisher")) as string | undefined },
    },
    /**
     * Methods to access box state for the current StakingPool app
     */
    box: {
      /**
       * Get all current keyed values from box state
       */
      getAll: async (): Promise<Partial<Expand<BoxKeysState>>> => {
        const result = await this.appClient.state.box.getAll()
        return {
        }
      },
      /**
       * Get values from the userClaims map in box state
       */
      userClaims: {
        /**
         * Get all current values of the userClaims map in box state
         */
        getMap: async (): Promise<Map<string, UserClaimState>> => { return (await this.appClient.state.box.getMap("userClaims")) as Map<string, UserClaimState> },
        /**
         * Get a current value of the userClaims map by key from box state
         */
        value: async (key: string): Promise<UserClaimState | undefined> => { return await this.appClient.state.box.getMapValue("userClaims", key) as UserClaimState | undefined },
      },
    },
  }

  public newGroup(): StakingPoolComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a optInAsset()void method call against the StakingPool contract
       */
      optInAsset(params: CallParams<StakingPoolArgs['obj']['optInAsset()void'] | StakingPoolArgs['tuple']['optInAsset()void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.optInAsset(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a fundPool(axfer)void method call against the StakingPool contract
       */
      fundPool(params: CallParams<StakingPoolArgs['obj']['fundPool(axfer)void'] | StakingPoolArgs['tuple']['fundPool(axfer)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.fundPool(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a claimRewards(uint64,uint64,byte[])void method call against the StakingPool contract
       */
      claimRewards(params: CallParams<StakingPoolArgs['obj']['claimRewards(uint64,uint64,byte[])void'] | StakingPoolArgs['tuple']['claimRewards(uint64,uint64,byte[])void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.claimRewards(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a deleteBox()void method call against the StakingPool contract
       */
      deleteBox(params: CallParams<StakingPoolArgs['obj']['deleteBox()void'] | StakingPoolArgs['tuple']['deleteBox()void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.deleteBox(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a setEpochRoot(uint64,byte[])void method call against the StakingPool contract
       */
      setEpochRoot(params: CallParams<StakingPoolArgs['obj']['setEpochRoot(uint64,byte[])void'] | StakingPoolArgs['tuple']['setEpochRoot(uint64,byte[])void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.setEpochRoot(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a togglePause(uint64)void method call against the StakingPool contract
       */
      togglePause(params: CallParams<StakingPoolArgs['obj']['togglePause(uint64)void'] | StakingPoolArgs['tuple']['togglePause(uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.togglePause(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a toggleDeprecated(uint64)void method call against the StakingPool contract
       */
      toggleDeprecated(params: CallParams<StakingPoolArgs['obj']['toggleDeprecated(uint64)void'] | StakingPoolArgs['tuple']['toggleDeprecated(uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.toggleDeprecated(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a emergencyWithdraw()void method call against the StakingPool contract
       */
      emergencyWithdraw(params: CallParams<StakingPoolArgs['obj']['emergencyWithdraw()void'] | StakingPoolArgs['tuple']['emergencyWithdraw()void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.emergencyWithdraw(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a updateEndDate(uint64)void method call against the StakingPool contract
       */
      updateEndDate(params: CallParams<StakingPoolArgs['obj']['updateEndDate(uint64)void'] | StakingPoolArgs['tuple']['updateEndDate(uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.updateEndDate(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a updatePublisher(address)void method call against the StakingPool contract
       */
      updatePublisher(params: CallParams<StakingPoolArgs['obj']['updatePublisher(address)void'] | StakingPoolArgs['tuple']['updatePublisher(address)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.updatePublisher(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a clear state call to the StakingPool contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as StakingPoolComposer
  }
}
export type StakingPoolComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the optInAsset()void ABI method.
   *
  * Contract opts into the reward token ASA.
  Must be called after creation and before funding.
  Admin only.

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  optInAsset(params?: CallParams<StakingPoolArgs['obj']['optInAsset()void'] | StakingPoolArgs['tuple']['optInAsset()void']>): StakingPoolComposer<[...TReturns, StakingPoolReturns['optInAsset()void'] | undefined]>

  /**
   * Calls the fundPool(axfer)void ABI method.
   *
  * Deposit reward tokens into the pool.
  One-time pools can only be funded once; rolling pools allow multiple deposits.
  
  Expects a grouped AssetTransfer from the sponsor preceding this call.

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  fundPool(params?: CallParams<StakingPoolArgs['obj']['fundPool(axfer)void'] | StakingPoolArgs['tuple']['fundPool(axfer)void']>): StakingPoolComposer<[...TReturns, StakingPoolReturns['fundPool(axfer)void'] | undefined]>

  /**
   * Calls the claimRewards(uint64,uint64,byte[])void ABI method.
   *
  * Claim rewards using a Merkle proof.
  
  CUMULATIVE MODEL: Each epoch's Merkle tree stores total-to-date amounts.
  The contract pays delta = (newCumulative - lastCumulative).
  Users can skip epochs without losing rewards — a single claim catches up.
  
  Transaction group structure:
   First claim:  [ Payment(boxMBR -> contract), AppCall(claimRewards) ]
   Subsequent:   [ AppCall(claimRewards) ]

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  claimRewards(params?: CallParams<StakingPoolArgs['obj']['claimRewards(uint64,uint64,byte[])void'] | StakingPoolArgs['tuple']['claimRewards(uint64,uint64,byte[])void']>): StakingPoolComposer<[...TReturns, StakingPoolReturns['claimRewards(uint64,uint64,byte[])void'] | undefined]>

  /**
   * Calls the deleteBox()void ABI method.
   *
  * Delete user's claim box and recover MBR.
  Returns (boxMBR - minTxnFee) to the caller.

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  deleteBox(params?: CallParams<StakingPoolArgs['obj']['deleteBox()void'] | StakingPoolArgs['tuple']['deleteBox()void']>): StakingPoolComposer<[...TReturns, StakingPoolReturns['deleteBox()void'] | undefined]>

  /**
   * Calls the setEpochRoot(uint64,byte[])void ABI method.
   *
  * Publish a new Merkle root for an epoch.
  Callable by admin or delegated publisher.
  Roots are immutable — cannot republish the same epoch.
  Epoch IDs must be strictly monotonically increasing.

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  setEpochRoot(params?: CallParams<StakingPoolArgs['obj']['setEpochRoot(uint64,byte[])void'] | StakingPoolArgs['tuple']['setEpochRoot(uint64,byte[])void']>): StakingPoolComposer<[...TReturns, StakingPoolReturns['setEpochRoot(uint64,byte[])void'] | undefined]>

  /**
   * Calls the togglePause(uint64)void ABI method.
   *
   * Emergency pause / unpause. Admin only.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  togglePause(params?: CallParams<StakingPoolArgs['obj']['togglePause(uint64)void'] | StakingPoolArgs['tuple']['togglePause(uint64)void']>): StakingPoolComposer<[...TReturns, StakingPoolReturns['togglePause(uint64)void'] | undefined]>

  /**
   * Calls the toggleDeprecated(uint64)void ABI method.
   *
  * Deprecate pool (one-way). Admin only.
  Once deprecated: no claims, no new roots, emergency withdrawal available.

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  toggleDeprecated(params?: CallParams<StakingPoolArgs['obj']['toggleDeprecated(uint64)void'] | StakingPoolArgs['tuple']['toggleDeprecated(uint64)void']>): StakingPoolComposer<[...TReturns, StakingPoolReturns['toggleDeprecated(uint64)void'] | undefined]>

  /**
   * Calls the emergencyWithdraw()void ABI method.
   *
  * Emergency withdraw all remaining rewards to sponsor.
  Pool must be deprecated first.

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  emergencyWithdraw(params?: CallParams<StakingPoolArgs['obj']['emergencyWithdraw()void'] | StakingPoolArgs['tuple']['emergencyWithdraw()void']>): StakingPoolComposer<[...TReturns, StakingPoolReturns['emergencyWithdraw()void'] | undefined]>

  /**
   * Calls the updateEndDate(uint64)void ABI method.
   *
   * Update pool end date (informational only). Admin only.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  updateEndDate(params?: CallParams<StakingPoolArgs['obj']['updateEndDate(uint64)void'] | StakingPoolArgs['tuple']['updateEndDate(uint64)void']>): StakingPoolComposer<[...TReturns, StakingPoolReturns['updateEndDate(uint64)void'] | undefined]>

  /**
   * Calls the updatePublisher(address)void ABI method.
   *
   * Update the publisher address for automated epoch publishing. Admin only.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  updatePublisher(params?: CallParams<StakingPoolArgs['obj']['updatePublisher(address)void'] | StakingPoolArgs['tuple']['updatePublisher(address)void']>): StakingPoolComposer<[...TReturns, StakingPoolReturns['updatePublisher(address)void'] | undefined]>

  /**
   * Makes a clear_state call to an existing instance of the StakingPool smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): StakingPoolComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): StakingPoolComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<StakingPoolComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<StakingPoolComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<StakingPoolComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<StakingPoolComposerResults<TReturns>>
}
export type StakingPoolComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

