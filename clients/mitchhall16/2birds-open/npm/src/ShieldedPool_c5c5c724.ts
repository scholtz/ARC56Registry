/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"ShieldedPool","desc":"","methods":[{"name":"createApplication","args":[{"name":"assetId","type":"uint64"}],"returns":{"type":"void"},"actions":{"create":["NoOp"],"call":[]}},{"name":"shield","desc":"Shield — deposit funds, create a shielded UTXO note.","args":[{"name":"commitment","type":"byte[]"},{"name":"amount","type":"uint64"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]}},{"name":"transfer","desc":"Transfer — consume input notes, create output notes (2-in/2-out).\nZK proof in LogicSig verifies membership, nullifiers, conservation, range proofs.","args":[{"name":"nullifierHash1","type":"byte[]"},{"name":"nullifierHash2","type":"byte[]"},{"name":"outputCommitment1","type":"byte[]"},{"name":"outputCommitment2","type":"byte[]"},{"name":"proof_root","type":"byte[]"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]}},{"name":"unshield","desc":"Unshield — withdraw funds by consuming a shielded note.","args":[{"name":"nullifierHash","type":"byte[]"},{"name":"recipient","type":"address"},{"name":"amount","type":"uint64"},{"name":"changeCommitment","type":"byte[]"},{"name":"proof_root","type":"byte[]"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]}},{"name":"optInToAsset","args":[],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]}}],"arcs":[4,56],"structs":{},"state":{"schema":{"global":{"bytes":1,"ints":3},"local":{"bytes":0,"ints":0}},"keys":{"global":{"currentRoot":{"key":"cm9vdA==","keyType":"AVMBytes","valueType":"AVMBytes"},"nextIndex":{"key":"bmV4dF9pZHg=","keyType":"AVMBytes","valueType":"uint64"},"assetId":{"key":"YXNzZXRfaWQ=","keyType":"AVMBytes","valueType":"uint64"},"rootHistoryIndex":{"key":"cmhp","keyType":"AVMBytes","valueType":"uint64"}},"local":{},"box":{}},"maps":{"global":{},"local":{},"box":{"treeFrontier":{"keyType":"uint64","valueType":"AVMBytes","prefix":"dHJlZQ=="},"nullifiers":{"keyType":"AVMBytes","valueType":"AVMBytes","prefix":"bnVsbA=="},"rootHistory":{"keyType":"uint64","valueType":"AVMBytes","prefix":"cm9vdA=="},"zeroHashes":{"keyType":"uint64","valueType":"AVMBytes","prefix":"emVybw=="}}}},"bareActions":{"create":[],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}}} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


/**
 * The argument types for the ShieldedPool contract
 */
export type ShieldedPoolArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'createApplication(uint64)void': {
      assetId: bigint | number
    }
    'shield(byte[],uint64)void': {
      commitment: Uint8Array
      amount: bigint | number
    }
    'transfer(byte[],byte[],byte[],byte[],byte[])void': {
      nullifierHash1: Uint8Array
      nullifierHash2: Uint8Array
      outputCommitment1: Uint8Array
      outputCommitment2: Uint8Array
      proofRoot: Uint8Array
    }
    'unshield(byte[],address,uint64,byte[],byte[])void': {
      nullifierHash: Uint8Array
      recipient: string
      amount: bigint | number
      changeCommitment: Uint8Array
      proofRoot: Uint8Array
    }
    'optInToAsset()void': Record<string, never>
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'createApplication(uint64)void': [assetId: bigint | number]
    'shield(byte[],uint64)void': [commitment: Uint8Array, amount: bigint | number]
    'transfer(byte[],byte[],byte[],byte[],byte[])void': [nullifierHash1: Uint8Array, nullifierHash2: Uint8Array, outputCommitment1: Uint8Array, outputCommitment2: Uint8Array, proofRoot: Uint8Array]
    'unshield(byte[],address,uint64,byte[],byte[])void': [nullifierHash: Uint8Array, recipient: string, amount: bigint | number, changeCommitment: Uint8Array, proofRoot: Uint8Array]
    'optInToAsset()void': []
  }
}

/**
 * The return type for each method
 */
export type ShieldedPoolReturns = {
  'createApplication(uint64)void': void
  'shield(byte[],uint64)void': void
  'transfer(byte[],byte[],byte[],byte[],byte[])void': void
  'unshield(byte[],address,uint64,byte[],byte[])void': void
  'optInToAsset()void': void
}

/**
 * Defines the types of available calls and state of the ShieldedPool smart contract.
 */
export type ShieldedPoolTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'createApplication(uint64)void' | 'createApplication', {
      argsObj: ShieldedPoolArgs['obj']['createApplication(uint64)void']
      argsTuple: ShieldedPoolArgs['tuple']['createApplication(uint64)void']
      returns: ShieldedPoolReturns['createApplication(uint64)void']
    }>
    & Record<'shield(byte[],uint64)void' | 'shield', {
      argsObj: ShieldedPoolArgs['obj']['shield(byte[],uint64)void']
      argsTuple: ShieldedPoolArgs['tuple']['shield(byte[],uint64)void']
      returns: ShieldedPoolReturns['shield(byte[],uint64)void']
    }>
    & Record<'transfer(byte[],byte[],byte[],byte[],byte[])void' | 'transfer', {
      argsObj: ShieldedPoolArgs['obj']['transfer(byte[],byte[],byte[],byte[],byte[])void']
      argsTuple: ShieldedPoolArgs['tuple']['transfer(byte[],byte[],byte[],byte[],byte[])void']
      returns: ShieldedPoolReturns['transfer(byte[],byte[],byte[],byte[],byte[])void']
    }>
    & Record<'unshield(byte[],address,uint64,byte[],byte[])void' | 'unshield', {
      argsObj: ShieldedPoolArgs['obj']['unshield(byte[],address,uint64,byte[],byte[])void']
      argsTuple: ShieldedPoolArgs['tuple']['unshield(byte[],address,uint64,byte[],byte[])void']
      returns: ShieldedPoolReturns['unshield(byte[],address,uint64,byte[],byte[])void']
    }>
    & Record<'optInToAsset()void' | 'optInToAsset', {
      argsObj: ShieldedPoolArgs['obj']['optInToAsset()void']
      argsTuple: ShieldedPoolArgs['tuple']['optInToAsset()void']
      returns: ShieldedPoolReturns['optInToAsset()void']
    }>
  /**
   * Defines the shape of the state of the application.
   */
  state: {
    global: {
      keys: {
        currentRoot: BinaryState
        nextIndex: bigint
        assetId: bigint
        rootHistoryIndex: bigint
      }
      maps: {}
    }
    box: {
      keys: {}
      maps: {
        treeFrontier: Map<bigint | number, Uint8Array>
        nullifiers: Map<Uint8Array | string, Uint8Array>
        rootHistory: Map<bigint | number, Uint8Array>
        zeroHashes: Map<bigint | number, Uint8Array>
      }
    }
  }
}

/**
 * Defines the possible abi call signatures.
 */
export type ShieldedPoolSignatures = keyof ShieldedPoolTypes['methods']
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the ShieldedPool smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends ShieldedPoolSignatures> = ShieldedPoolTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the ShieldedPool smart contract to the method's return type
 */
export type MethodReturn<TSignature extends ShieldedPoolSignatures> = ShieldedPoolTypes['methods'][TSignature]['returns']

/**
 * Defines the shape of the keyed global state of the application.
 */
export type GlobalKeysState = ShieldedPoolTypes['state']['global']['keys']

/**
 * Defines the shape of the keyed box state of the application.
 */
export type BoxKeysState = ShieldedPoolTypes['state']['box']['keys']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the ShieldedPool smart contract
 */
export abstract class ShieldedPoolParamsFactory {
  /**
   * Constructs a no op call for the shield(byte[],uint64)void ABI method
   *
   * Shield — deposit funds, create a shielded UTXO note.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static shield(params: CallParams<ShieldedPoolArgs['obj']['shield(byte[],uint64)void'] | ShieldedPoolArgs['tuple']['shield(byte[],uint64)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'shield(byte[],uint64)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.commitment, params.args.amount],
    }
  }
  /**
   * Constructs a no op call for the transfer(byte[],byte[],byte[],byte[],byte[])void ABI method
   *
  * Transfer — consume input notes, create output notes (2-in/2-out).
  ZK proof in LogicSig verifies membership, nullifiers, conservation, range proofs.

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static transfer(params: CallParams<ShieldedPoolArgs['obj']['transfer(byte[],byte[],byte[],byte[],byte[])void'] | ShieldedPoolArgs['tuple']['transfer(byte[],byte[],byte[],byte[],byte[])void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'transfer(byte[],byte[],byte[],byte[],byte[])void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.nullifierHash1, params.args.nullifierHash2, params.args.outputCommitment1, params.args.outputCommitment2, params.args.proofRoot],
    }
  }
  /**
   * Constructs a no op call for the unshield(byte[],address,uint64,byte[],byte[])void ABI method
   *
   * Unshield — withdraw funds by consuming a shielded note.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static unshield(params: CallParams<ShieldedPoolArgs['obj']['unshield(byte[],address,uint64,byte[],byte[])void'] | ShieldedPoolArgs['tuple']['unshield(byte[],address,uint64,byte[],byte[])void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'unshield(byte[],address,uint64,byte[],byte[])void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.nullifierHash, params.args.recipient, params.args.amount, params.args.changeCommitment, params.args.proofRoot],
    }
  }
  /**
   * Constructs a no op call for the optInToAsset()void ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static optInToAsset(params: CallParams<ShieldedPoolArgs['obj']['optInToAsset()void'] | ShieldedPoolArgs['tuple']['optInToAsset()void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'optInToAsset()void' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
}

/**
 * A client to make calls to the ShieldedPool smart contract
 */
export class ShieldedPoolClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `ShieldedPoolClient`
   *
   * @param appClient An `AppClient` instance which has been created with the ShieldedPool app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `ShieldedPoolClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Returns a new `ShieldedPoolClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<ShieldedPoolClient> {
    return new ShieldedPoolClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `ShieldedPoolClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<ShieldedPoolClient> {
    return new ShieldedPoolClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Makes a clear_state call to an existing instance of the ShieldedPool smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the ShieldedPool smart contract using the `shield(byte[],uint64)void` ABI method.
     *
     * Shield — deposit funds, create a shielded UTXO note.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    shield: (params: CallParams<ShieldedPoolArgs['obj']['shield(byte[],uint64)void'] | ShieldedPoolArgs['tuple']['shield(byte[],uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(ShieldedPoolParamsFactory.shield(params))
    },

    /**
     * Makes a call to the ShieldedPool smart contract using the `transfer(byte[],byte[],byte[],byte[],byte[])void` ABI method.
     *
    * Transfer — consume input notes, create output notes (2-in/2-out).
    ZK proof in LogicSig verifies membership, nullifiers, conservation, range proofs.

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    transfer: (params: CallParams<ShieldedPoolArgs['obj']['transfer(byte[],byte[],byte[],byte[],byte[])void'] | ShieldedPoolArgs['tuple']['transfer(byte[],byte[],byte[],byte[],byte[])void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(ShieldedPoolParamsFactory.transfer(params))
    },

    /**
     * Makes a call to the ShieldedPool smart contract using the `unshield(byte[],address,uint64,byte[],byte[])void` ABI method.
     *
     * Unshield — withdraw funds by consuming a shielded note.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    unshield: (params: CallParams<ShieldedPoolArgs['obj']['unshield(byte[],address,uint64,byte[],byte[])void'] | ShieldedPoolArgs['tuple']['unshield(byte[],address,uint64,byte[],byte[])void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(ShieldedPoolParamsFactory.unshield(params))
    },

    /**
     * Makes a call to the ShieldedPool smart contract using the `optInToAsset()void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    optInToAsset: (params: CallParams<ShieldedPoolArgs['obj']['optInToAsset()void'] | ShieldedPoolArgs['tuple']['optInToAsset()void']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(ShieldedPoolParamsFactory.optInToAsset(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Makes a clear_state call to an existing instance of the ShieldedPool smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the ShieldedPool smart contract using the `shield(byte[],uint64)void` ABI method.
     *
     * Shield — deposit funds, create a shielded UTXO note.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    shield: (params: CallParams<ShieldedPoolArgs['obj']['shield(byte[],uint64)void'] | ShieldedPoolArgs['tuple']['shield(byte[],uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(ShieldedPoolParamsFactory.shield(params))
    },

    /**
     * Makes a call to the ShieldedPool smart contract using the `transfer(byte[],byte[],byte[],byte[],byte[])void` ABI method.
     *
    * Transfer — consume input notes, create output notes (2-in/2-out).
    ZK proof in LogicSig verifies membership, nullifiers, conservation, range proofs.

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    transfer: (params: CallParams<ShieldedPoolArgs['obj']['transfer(byte[],byte[],byte[],byte[],byte[])void'] | ShieldedPoolArgs['tuple']['transfer(byte[],byte[],byte[],byte[],byte[])void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(ShieldedPoolParamsFactory.transfer(params))
    },

    /**
     * Makes a call to the ShieldedPool smart contract using the `unshield(byte[],address,uint64,byte[],byte[])void` ABI method.
     *
     * Unshield — withdraw funds by consuming a shielded note.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    unshield: (params: CallParams<ShieldedPoolArgs['obj']['unshield(byte[],address,uint64,byte[],byte[])void'] | ShieldedPoolArgs['tuple']['unshield(byte[],address,uint64,byte[],byte[])void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(ShieldedPoolParamsFactory.unshield(params))
    },

    /**
     * Makes a call to the ShieldedPool smart contract using the `optInToAsset()void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    optInToAsset: (params: CallParams<ShieldedPoolArgs['obj']['optInToAsset()void'] | ShieldedPoolArgs['tuple']['optInToAsset()void']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(ShieldedPoolParamsFactory.optInToAsset(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Makes a clear_state call to an existing instance of the ShieldedPool smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the ShieldedPool smart contract using the `shield(byte[],uint64)void` ABI method.
     *
     * Shield — deposit funds, create a shielded UTXO note.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    shield: async (params: CallParams<ShieldedPoolArgs['obj']['shield(byte[],uint64)void'] | ShieldedPoolArgs['tuple']['shield(byte[],uint64)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(ShieldedPoolParamsFactory.shield(params))
      return {...result, return: result.return as unknown as (undefined | ShieldedPoolReturns['shield(byte[],uint64)void'])}
    },

    /**
     * Makes a call to the ShieldedPool smart contract using the `transfer(byte[],byte[],byte[],byte[],byte[])void` ABI method.
     *
    * Transfer — consume input notes, create output notes (2-in/2-out).
    ZK proof in LogicSig verifies membership, nullifiers, conservation, range proofs.

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    transfer: async (params: CallParams<ShieldedPoolArgs['obj']['transfer(byte[],byte[],byte[],byte[],byte[])void'] | ShieldedPoolArgs['tuple']['transfer(byte[],byte[],byte[],byte[],byte[])void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(ShieldedPoolParamsFactory.transfer(params))
      return {...result, return: result.return as unknown as (undefined | ShieldedPoolReturns['transfer(byte[],byte[],byte[],byte[],byte[])void'])}
    },

    /**
     * Makes a call to the ShieldedPool smart contract using the `unshield(byte[],address,uint64,byte[],byte[])void` ABI method.
     *
     * Unshield — withdraw funds by consuming a shielded note.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    unshield: async (params: CallParams<ShieldedPoolArgs['obj']['unshield(byte[],address,uint64,byte[],byte[])void'] | ShieldedPoolArgs['tuple']['unshield(byte[],address,uint64,byte[],byte[])void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(ShieldedPoolParamsFactory.unshield(params))
      return {...result, return: result.return as unknown as (undefined | ShieldedPoolReturns['unshield(byte[],address,uint64,byte[],byte[])void'])}
    },

    /**
     * Makes a call to the ShieldedPool smart contract using the `optInToAsset()void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    optInToAsset: async (params: CallParams<ShieldedPoolArgs['obj']['optInToAsset()void'] | ShieldedPoolArgs['tuple']['optInToAsset()void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(ShieldedPoolParamsFactory.optInToAsset(params))
      return {...result, return: result.return as unknown as (undefined | ShieldedPoolReturns['optInToAsset()void'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new ShieldedPoolClient(this.appClient.clone(params))
  }

  /**
   * Methods to access state for the current ShieldedPool app
   */
  state = {
    /**
     * Methods to access global state for the current ShieldedPool app
     */
    global: {
      /**
       * Get all current keyed values from global state
       */
      getAll: async (): Promise<Partial<Expand<GlobalKeysState>>> => {
        const result = await this.appClient.state.global.getAll()
        return {
          currentRoot: new BinaryStateValue(result.currentRoot),
          nextIndex: result.nextIndex,
          assetId: result.assetId,
          rootHistoryIndex: result.rootHistoryIndex,
        }
      },
      /**
       * Get the current value of the currentRoot key in global state
       */
      currentRoot: async (): Promise<BinaryState> => { return new BinaryStateValue((await this.appClient.state.global.getValue("currentRoot")) as Uint8Array | undefined) },
      /**
       * Get the current value of the nextIndex key in global state
       */
      nextIndex: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("nextIndex")) as bigint | undefined },
      /**
       * Get the current value of the assetId key in global state
       */
      assetId: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("assetId")) as bigint | undefined },
      /**
       * Get the current value of the rootHistoryIndex key in global state
       */
      rootHistoryIndex: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("rootHistoryIndex")) as bigint | undefined },
    },
    /**
     * Methods to access box state for the current ShieldedPool app
     */
    box: {
      /**
       * Get all current keyed values from box state
       */
      getAll: async (): Promise<Partial<Expand<BoxKeysState>>> => {
        const result = await this.appClient.state.box.getAll()
        return {
        }
      },
      /**
       * Get values from the treeFrontier map in box state
       */
      treeFrontier: {
        /**
         * Get all current values of the treeFrontier map in box state
         */
        getMap: async (): Promise<Map<bigint, Uint8Array>> => { return (await this.appClient.state.box.getMap("treeFrontier")) as Map<bigint, Uint8Array> },
        /**
         * Get a current value of the treeFrontier map by key from box state
         */
        value: async (key: bigint | number): Promise<Uint8Array | undefined> => { return await this.appClient.state.box.getMapValue("treeFrontier", key) as Uint8Array | undefined },
      },
      /**
       * Get values from the nullifiers map in box state
       */
      nullifiers: {
        /**
         * Get all current values of the nullifiers map in box state
         */
        getMap: async (): Promise<Map<Uint8Array, Uint8Array>> => { return (await this.appClient.state.box.getMap("nullifiers")) as Map<Uint8Array, Uint8Array> },
        /**
         * Get a current value of the nullifiers map by key from box state
         */
        value: async (key: Uint8Array | string): Promise<Uint8Array | undefined> => { return await this.appClient.state.box.getMapValue("nullifiers", key) as Uint8Array | undefined },
      },
      /**
       * Get values from the rootHistory map in box state
       */
      rootHistory: {
        /**
         * Get all current values of the rootHistory map in box state
         */
        getMap: async (): Promise<Map<bigint, Uint8Array>> => { return (await this.appClient.state.box.getMap("rootHistory")) as Map<bigint, Uint8Array> },
        /**
         * Get a current value of the rootHistory map by key from box state
         */
        value: async (key: bigint | number): Promise<Uint8Array | undefined> => { return await this.appClient.state.box.getMapValue("rootHistory", key) as Uint8Array | undefined },
      },
      /**
       * Get values from the zeroHashes map in box state
       */
      zeroHashes: {
        /**
         * Get all current values of the zeroHashes map in box state
         */
        getMap: async (): Promise<Map<bigint, Uint8Array>> => { return (await this.appClient.state.box.getMap("zeroHashes")) as Map<bigint, Uint8Array> },
        /**
         * Get a current value of the zeroHashes map by key from box state
         */
        value: async (key: bigint | number): Promise<Uint8Array | undefined> => { return await this.appClient.state.box.getMapValue("zeroHashes", key) as Uint8Array | undefined },
      },
    },
  }

  public newGroup(): ShieldedPoolComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a shield(byte[],uint64)void method call against the ShieldedPool contract
       */
      shield(params: CallParams<ShieldedPoolArgs['obj']['shield(byte[],uint64)void'] | ShieldedPoolArgs['tuple']['shield(byte[],uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.shield(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a transfer(byte[],byte[],byte[],byte[],byte[])void method call against the ShieldedPool contract
       */
      transfer(params: CallParams<ShieldedPoolArgs['obj']['transfer(byte[],byte[],byte[],byte[],byte[])void'] | ShieldedPoolArgs['tuple']['transfer(byte[],byte[],byte[],byte[],byte[])void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.transfer(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a unshield(byte[],address,uint64,byte[],byte[])void method call against the ShieldedPool contract
       */
      unshield(params: CallParams<ShieldedPoolArgs['obj']['unshield(byte[],address,uint64,byte[],byte[])void'] | ShieldedPoolArgs['tuple']['unshield(byte[],address,uint64,byte[],byte[])void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.unshield(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a optInToAsset()void method call against the ShieldedPool contract
       */
      optInToAsset(params: CallParams<ShieldedPoolArgs['obj']['optInToAsset()void'] | ShieldedPoolArgs['tuple']['optInToAsset()void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.optInToAsset(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a clear state call to the ShieldedPool contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as ShieldedPoolComposer
  }
}
export type ShieldedPoolComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the shield(byte[],uint64)void ABI method.
   *
   * Shield — deposit funds, create a shielded UTXO note.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  shield(params?: CallParams<ShieldedPoolArgs['obj']['shield(byte[],uint64)void'] | ShieldedPoolArgs['tuple']['shield(byte[],uint64)void']>): ShieldedPoolComposer<[...TReturns, ShieldedPoolReturns['shield(byte[],uint64)void'] | undefined]>

  /**
   * Calls the transfer(byte[],byte[],byte[],byte[],byte[])void ABI method.
   *
  * Transfer — consume input notes, create output notes (2-in/2-out).
  ZK proof in LogicSig verifies membership, nullifiers, conservation, range proofs.

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  transfer(params?: CallParams<ShieldedPoolArgs['obj']['transfer(byte[],byte[],byte[],byte[],byte[])void'] | ShieldedPoolArgs['tuple']['transfer(byte[],byte[],byte[],byte[],byte[])void']>): ShieldedPoolComposer<[...TReturns, ShieldedPoolReturns['transfer(byte[],byte[],byte[],byte[],byte[])void'] | undefined]>

  /**
   * Calls the unshield(byte[],address,uint64,byte[],byte[])void ABI method.
   *
   * Unshield — withdraw funds by consuming a shielded note.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  unshield(params?: CallParams<ShieldedPoolArgs['obj']['unshield(byte[],address,uint64,byte[],byte[])void'] | ShieldedPoolArgs['tuple']['unshield(byte[],address,uint64,byte[],byte[])void']>): ShieldedPoolComposer<[...TReturns, ShieldedPoolReturns['unshield(byte[],address,uint64,byte[],byte[])void'] | undefined]>

  /**
   * Calls the optInToAsset()void ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  optInToAsset(params?: CallParams<ShieldedPoolArgs['obj']['optInToAsset()void'] | ShieldedPoolArgs['tuple']['optInToAsset()void']>): ShieldedPoolComposer<[...TReturns, ShieldedPoolReturns['optInToAsset()void'] | undefined]>

  /**
   * Makes a clear_state call to an existing instance of the ShieldedPool smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): ShieldedPoolComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): ShieldedPoolComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<ShieldedPoolComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<ShieldedPoolComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<ShieldedPoolComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<ShieldedPoolComposerResults<TReturns>>
}
export type ShieldedPoolComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

