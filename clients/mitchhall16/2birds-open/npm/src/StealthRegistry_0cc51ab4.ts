/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"StealthRegistry","desc":"","methods":[{"name":"createApplication","args":[],"returns":{"type":"void"},"actions":{"create":["NoOp"],"call":[]}},{"name":"register","desc":"Register a stealth meta-address (spending pub 64B + viewing pub 64B = 128B).","args":[{"name":"label","type":"byte[]"},{"name":"metaAddress","type":"byte[]"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]}},{"name":"announce","desc":"Publish a stealth payment announcement.\nData: ephemeral_pub (64B) + stealth_addr (32B) + view_tag (1B) + metadata","args":[{"name":"announcementData","type":"byte[]"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]}},{"name":"deregister","desc":"Remove a meta-address registration. Reclaims box MBR.","args":[{"name":"label","type":"byte[]"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]}}],"arcs":[4,56],"structs":{},"state":{"schema":{"global":{"bytes":0,"ints":1},"local":{"bytes":0,"ints":0}},"keys":{"global":{"announcementCount":{"key":"Y291bnQ=","keyType":"AVMBytes","valueType":"uint64"}},"local":{},"box":{}},"maps":{"global":{},"local":{},"box":{"metaAddresses":{"keyType":"AVMBytes","valueType":"AVMBytes","prefix":"bWV0YQ=="},"announcements":{"keyType":"uint64","valueType":"AVMBytes","prefix":"YW5u"}}}},"bareActions":{"create":[],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}}} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


/**
 * The argument types for the StealthRegistry contract
 */
export type StealthRegistryArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'createApplication()void': Record<string, never>
    'register(byte[],byte[])void': {
      label: Uint8Array
      metaAddress: Uint8Array
    }
    'announce(byte[])void': {
      announcementData: Uint8Array
    }
    'deregister(byte[])void': {
      label: Uint8Array
    }
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'createApplication()void': []
    'register(byte[],byte[])void': [label: Uint8Array, metaAddress: Uint8Array]
    'announce(byte[])void': [announcementData: Uint8Array]
    'deregister(byte[])void': [label: Uint8Array]
  }
}

/**
 * The return type for each method
 */
export type StealthRegistryReturns = {
  'createApplication()void': void
  'register(byte[],byte[])void': void
  'announce(byte[])void': void
  'deregister(byte[])void': void
}

/**
 * Defines the types of available calls and state of the StealthRegistry smart contract.
 */
export type StealthRegistryTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'createApplication()void' | 'createApplication', {
      argsObj: StealthRegistryArgs['obj']['createApplication()void']
      argsTuple: StealthRegistryArgs['tuple']['createApplication()void']
      returns: StealthRegistryReturns['createApplication()void']
    }>
    & Record<'register(byte[],byte[])void' | 'register', {
      argsObj: StealthRegistryArgs['obj']['register(byte[],byte[])void']
      argsTuple: StealthRegistryArgs['tuple']['register(byte[],byte[])void']
      returns: StealthRegistryReturns['register(byte[],byte[])void']
    }>
    & Record<'announce(byte[])void' | 'announce', {
      argsObj: StealthRegistryArgs['obj']['announce(byte[])void']
      argsTuple: StealthRegistryArgs['tuple']['announce(byte[])void']
      returns: StealthRegistryReturns['announce(byte[])void']
    }>
    & Record<'deregister(byte[])void' | 'deregister', {
      argsObj: StealthRegistryArgs['obj']['deregister(byte[])void']
      argsTuple: StealthRegistryArgs['tuple']['deregister(byte[])void']
      returns: StealthRegistryReturns['deregister(byte[])void']
    }>
  /**
   * Defines the shape of the state of the application.
   */
  state: {
    global: {
      keys: {
        announcementCount: bigint
      }
      maps: {}
    }
    box: {
      keys: {}
      maps: {
        metaAddresses: Map<Uint8Array | string, Uint8Array>
        announcements: Map<bigint | number, Uint8Array>
      }
    }
  }
}

/**
 * Defines the possible abi call signatures.
 */
export type StealthRegistrySignatures = keyof StealthRegistryTypes['methods']
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the StealthRegistry smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends StealthRegistrySignatures> = StealthRegistryTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the StealthRegistry smart contract to the method's return type
 */
export type MethodReturn<TSignature extends StealthRegistrySignatures> = StealthRegistryTypes['methods'][TSignature]['returns']

/**
 * Defines the shape of the keyed global state of the application.
 */
export type GlobalKeysState = StealthRegistryTypes['state']['global']['keys']

/**
 * Defines the shape of the keyed box state of the application.
 */
export type BoxKeysState = StealthRegistryTypes['state']['box']['keys']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the StealthRegistry smart contract
 */
export abstract class StealthRegistryParamsFactory {
  /**
   * Constructs a no op call for the register(byte[],byte[])void ABI method
   *
   * Register a stealth meta-address (spending pub 64B + viewing pub 64B = 128B).
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static register(params: CallParams<StealthRegistryArgs['obj']['register(byte[],byte[])void'] | StealthRegistryArgs['tuple']['register(byte[],byte[])void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'register(byte[],byte[])void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.label, params.args.metaAddress],
    }
  }
  /**
   * Constructs a no op call for the announce(byte[])void ABI method
   *
  * Publish a stealth payment announcement.
  Data: ephemeral_pub (64B) + stealth_addr (32B) + view_tag (1B) + metadata

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static announce(params: CallParams<StealthRegistryArgs['obj']['announce(byte[])void'] | StealthRegistryArgs['tuple']['announce(byte[])void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'announce(byte[])void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.announcementData],
    }
  }
  /**
   * Constructs a no op call for the deregister(byte[])void ABI method
   *
   * Remove a meta-address registration. Reclaims box MBR.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static deregister(params: CallParams<StealthRegistryArgs['obj']['deregister(byte[])void'] | StealthRegistryArgs['tuple']['deregister(byte[])void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'deregister(byte[])void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.label],
    }
  }
}

/**
 * A client to make calls to the StealthRegistry smart contract
 */
export class StealthRegistryClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `StealthRegistryClient`
   *
   * @param appClient An `AppClient` instance which has been created with the StealthRegistry app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `StealthRegistryClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Returns a new `StealthRegistryClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<StealthRegistryClient> {
    return new StealthRegistryClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `StealthRegistryClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<StealthRegistryClient> {
    return new StealthRegistryClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Makes a clear_state call to an existing instance of the StealthRegistry smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the StealthRegistry smart contract using the `register(byte[],byte[])void` ABI method.
     *
     * Register a stealth meta-address (spending pub 64B + viewing pub 64B = 128B).
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    register: (params: CallParams<StealthRegistryArgs['obj']['register(byte[],byte[])void'] | StealthRegistryArgs['tuple']['register(byte[],byte[])void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(StealthRegistryParamsFactory.register(params))
    },

    /**
     * Makes a call to the StealthRegistry smart contract using the `announce(byte[])void` ABI method.
     *
    * Publish a stealth payment announcement.
    Data: ephemeral_pub (64B) + stealth_addr (32B) + view_tag (1B) + metadata

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    announce: (params: CallParams<StealthRegistryArgs['obj']['announce(byte[])void'] | StealthRegistryArgs['tuple']['announce(byte[])void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(StealthRegistryParamsFactory.announce(params))
    },

    /**
     * Makes a call to the StealthRegistry smart contract using the `deregister(byte[])void` ABI method.
     *
     * Remove a meta-address registration. Reclaims box MBR.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    deregister: (params: CallParams<StealthRegistryArgs['obj']['deregister(byte[])void'] | StealthRegistryArgs['tuple']['deregister(byte[])void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(StealthRegistryParamsFactory.deregister(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Makes a clear_state call to an existing instance of the StealthRegistry smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the StealthRegistry smart contract using the `register(byte[],byte[])void` ABI method.
     *
     * Register a stealth meta-address (spending pub 64B + viewing pub 64B = 128B).
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    register: (params: CallParams<StealthRegistryArgs['obj']['register(byte[],byte[])void'] | StealthRegistryArgs['tuple']['register(byte[],byte[])void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(StealthRegistryParamsFactory.register(params))
    },

    /**
     * Makes a call to the StealthRegistry smart contract using the `announce(byte[])void` ABI method.
     *
    * Publish a stealth payment announcement.
    Data: ephemeral_pub (64B) + stealth_addr (32B) + view_tag (1B) + metadata

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    announce: (params: CallParams<StealthRegistryArgs['obj']['announce(byte[])void'] | StealthRegistryArgs['tuple']['announce(byte[])void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(StealthRegistryParamsFactory.announce(params))
    },

    /**
     * Makes a call to the StealthRegistry smart contract using the `deregister(byte[])void` ABI method.
     *
     * Remove a meta-address registration. Reclaims box MBR.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    deregister: (params: CallParams<StealthRegistryArgs['obj']['deregister(byte[])void'] | StealthRegistryArgs['tuple']['deregister(byte[])void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(StealthRegistryParamsFactory.deregister(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Makes a clear_state call to an existing instance of the StealthRegistry smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the StealthRegistry smart contract using the `register(byte[],byte[])void` ABI method.
     *
     * Register a stealth meta-address (spending pub 64B + viewing pub 64B = 128B).
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    register: async (params: CallParams<StealthRegistryArgs['obj']['register(byte[],byte[])void'] | StealthRegistryArgs['tuple']['register(byte[],byte[])void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(StealthRegistryParamsFactory.register(params))
      return {...result, return: result.return as unknown as (undefined | StealthRegistryReturns['register(byte[],byte[])void'])}
    },

    /**
     * Makes a call to the StealthRegistry smart contract using the `announce(byte[])void` ABI method.
     *
    * Publish a stealth payment announcement.
    Data: ephemeral_pub (64B) + stealth_addr (32B) + view_tag (1B) + metadata

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    announce: async (params: CallParams<StealthRegistryArgs['obj']['announce(byte[])void'] | StealthRegistryArgs['tuple']['announce(byte[])void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(StealthRegistryParamsFactory.announce(params))
      return {...result, return: result.return as unknown as (undefined | StealthRegistryReturns['announce(byte[])void'])}
    },

    /**
     * Makes a call to the StealthRegistry smart contract using the `deregister(byte[])void` ABI method.
     *
     * Remove a meta-address registration. Reclaims box MBR.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    deregister: async (params: CallParams<StealthRegistryArgs['obj']['deregister(byte[])void'] | StealthRegistryArgs['tuple']['deregister(byte[])void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(StealthRegistryParamsFactory.deregister(params))
      return {...result, return: result.return as unknown as (undefined | StealthRegistryReturns['deregister(byte[])void'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new StealthRegistryClient(this.appClient.clone(params))
  }

  /**
   * Methods to access state for the current StealthRegistry app
   */
  state = {
    /**
     * Methods to access global state for the current StealthRegistry app
     */
    global: {
      /**
       * Get all current keyed values from global state
       */
      getAll: async (): Promise<Partial<Expand<GlobalKeysState>>> => {
        const result = await this.appClient.state.global.getAll()
        return {
          announcementCount: result.announcementCount,
        }
      },
      /**
       * Get the current value of the announcementCount key in global state
       */
      announcementCount: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("announcementCount")) as bigint | undefined },
    },
    /**
     * Methods to access box state for the current StealthRegistry app
     */
    box: {
      /**
       * Get all current keyed values from box state
       */
      getAll: async (): Promise<Partial<Expand<BoxKeysState>>> => {
        const result = await this.appClient.state.box.getAll()
        return {
        }
      },
      /**
       * Get values from the metaAddresses map in box state
       */
      metaAddresses: {
        /**
         * Get all current values of the metaAddresses map in box state
         */
        getMap: async (): Promise<Map<Uint8Array, Uint8Array>> => { return (await this.appClient.state.box.getMap("metaAddresses")) as Map<Uint8Array, Uint8Array> },
        /**
         * Get a current value of the metaAddresses map by key from box state
         */
        value: async (key: Uint8Array | string): Promise<Uint8Array | undefined> => { return await this.appClient.state.box.getMapValue("metaAddresses", key) as Uint8Array | undefined },
      },
      /**
       * Get values from the announcements map in box state
       */
      announcements: {
        /**
         * Get all current values of the announcements map in box state
         */
        getMap: async (): Promise<Map<bigint, Uint8Array>> => { return (await this.appClient.state.box.getMap("announcements")) as Map<bigint, Uint8Array> },
        /**
         * Get a current value of the announcements map by key from box state
         */
        value: async (key: bigint | number): Promise<Uint8Array | undefined> => { return await this.appClient.state.box.getMapValue("announcements", key) as Uint8Array | undefined },
      },
    },
  }

  public newGroup(): StealthRegistryComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a register(byte[],byte[])void method call against the StealthRegistry contract
       */
      register(params: CallParams<StealthRegistryArgs['obj']['register(byte[],byte[])void'] | StealthRegistryArgs['tuple']['register(byte[],byte[])void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.register(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a announce(byte[])void method call against the StealthRegistry contract
       */
      announce(params: CallParams<StealthRegistryArgs['obj']['announce(byte[])void'] | StealthRegistryArgs['tuple']['announce(byte[])void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.announce(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a deregister(byte[])void method call against the StealthRegistry contract
       */
      deregister(params: CallParams<StealthRegistryArgs['obj']['deregister(byte[])void'] | StealthRegistryArgs['tuple']['deregister(byte[])void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.deregister(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a clear state call to the StealthRegistry contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as StealthRegistryComposer
  }
}
export type StealthRegistryComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the register(byte[],byte[])void ABI method.
   *
   * Register a stealth meta-address (spending pub 64B + viewing pub 64B = 128B).
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  register(params?: CallParams<StealthRegistryArgs['obj']['register(byte[],byte[])void'] | StealthRegistryArgs['tuple']['register(byte[],byte[])void']>): StealthRegistryComposer<[...TReturns, StealthRegistryReturns['register(byte[],byte[])void'] | undefined]>

  /**
   * Calls the announce(byte[])void ABI method.
   *
  * Publish a stealth payment announcement.
  Data: ephemeral_pub (64B) + stealth_addr (32B) + view_tag (1B) + metadata

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  announce(params?: CallParams<StealthRegistryArgs['obj']['announce(byte[])void'] | StealthRegistryArgs['tuple']['announce(byte[])void']>): StealthRegistryComposer<[...TReturns, StealthRegistryReturns['announce(byte[])void'] | undefined]>

  /**
   * Calls the deregister(byte[])void ABI method.
   *
   * Remove a meta-address registration. Reclaims box MBR.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  deregister(params?: CallParams<StealthRegistryArgs['obj']['deregister(byte[])void'] | StealthRegistryArgs['tuple']['deregister(byte[])void']>): StealthRegistryComposer<[...TReturns, StealthRegistryReturns['deregister(byte[])void'] | undefined]>

  /**
   * Makes a clear_state call to an existing instance of the StealthRegistry smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): StealthRegistryComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): StealthRegistryComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<StealthRegistryComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<StealthRegistryComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<StealthRegistryComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<StealthRegistryComposerResults<TReturns>>
}
export type StealthRegistryComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

