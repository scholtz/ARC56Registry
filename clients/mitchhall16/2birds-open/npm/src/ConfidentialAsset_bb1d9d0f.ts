/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"ConfidentialAsset","desc":"","methods":[{"name":"createApplication","args":[{"name":"assetId","type":"uint64"}],"returns":{"type":"void"},"actions":{"create":["NoOp"],"call":[]}},{"name":"shield","desc":"Shield — deposit public funds, create a Pedersen commitment.\nCommitment = amount * G + blinding * H (64-byte BN254 G1 point)","args":[{"name":"commitment","type":"byte[]"},{"name":"amount","type":"uint64"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]}},{"name":"confidentialTransfer","desc":"Confidential transfer — transfer between shielded balances with hidden amount.\nRange proofs verified by LogicSig in atomic group.","args":[{"name":"recipient","type":"address"},{"name":"senderNewCommitment","type":"byte[]"},{"name":"recipientNewCommitment","type":"byte[]"},{"name":"transferCommitment","type":"byte[]"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]}},{"name":"unshield","desc":"Unshield — withdraw from shielded balance to public.","args":[{"name":"amount","type":"uint64"},{"name":"newCommitment","type":"byte[]"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]}},{"name":"optInToAsset","args":[],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]}}],"arcs":[4,56],"structs":{},"state":{"schema":{"global":{"bytes":0,"ints":3},"local":{"bytes":0,"ints":0}},"keys":{"global":{"assetId":{"key":"YXNzZXRfaWQ=","keyType":"AVMBytes","valueType":"uint64"},"totalDeposited":{"key":"dG90YWxfZGVw","keyType":"AVMBytes","valueType":"uint64"},"totalWithdrawn":{"key":"dG90YWxfd2Q=","keyType":"AVMBytes","valueType":"uint64"}},"local":{},"box":{}},"maps":{"global":{},"local":{},"box":{"balances":{"keyType":"address","valueType":"AVMBytes","prefix":"YmFs"}}}},"bareActions":{"create":[],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}}} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


/**
 * The argument types for the ConfidentialAsset contract
 */
export type ConfidentialAssetArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'createApplication(uint64)void': {
      assetId: bigint | number
    }
    'shield(byte[],uint64)void': {
      commitment: Uint8Array
      amount: bigint | number
    }
    'confidentialTransfer(address,byte[],byte[],byte[])void': {
      recipient: string
      senderNewCommitment: Uint8Array
      recipientNewCommitment: Uint8Array
      transferCommitment: Uint8Array
    }
    'unshield(uint64,byte[])void': {
      amount: bigint | number
      newCommitment: Uint8Array
    }
    'optInToAsset()void': Record<string, never>
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'createApplication(uint64)void': [assetId: bigint | number]
    'shield(byte[],uint64)void': [commitment: Uint8Array, amount: bigint | number]
    'confidentialTransfer(address,byte[],byte[],byte[])void': [recipient: string, senderNewCommitment: Uint8Array, recipientNewCommitment: Uint8Array, transferCommitment: Uint8Array]
    'unshield(uint64,byte[])void': [amount: bigint | number, newCommitment: Uint8Array]
    'optInToAsset()void': []
  }
}

/**
 * The return type for each method
 */
export type ConfidentialAssetReturns = {
  'createApplication(uint64)void': void
  'shield(byte[],uint64)void': void
  'confidentialTransfer(address,byte[],byte[],byte[])void': void
  'unshield(uint64,byte[])void': void
  'optInToAsset()void': void
}

/**
 * Defines the types of available calls and state of the ConfidentialAsset smart contract.
 */
export type ConfidentialAssetTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'createApplication(uint64)void' | 'createApplication', {
      argsObj: ConfidentialAssetArgs['obj']['createApplication(uint64)void']
      argsTuple: ConfidentialAssetArgs['tuple']['createApplication(uint64)void']
      returns: ConfidentialAssetReturns['createApplication(uint64)void']
    }>
    & Record<'shield(byte[],uint64)void' | 'shield', {
      argsObj: ConfidentialAssetArgs['obj']['shield(byte[],uint64)void']
      argsTuple: ConfidentialAssetArgs['tuple']['shield(byte[],uint64)void']
      returns: ConfidentialAssetReturns['shield(byte[],uint64)void']
    }>
    & Record<'confidentialTransfer(address,byte[],byte[],byte[])void' | 'confidentialTransfer', {
      argsObj: ConfidentialAssetArgs['obj']['confidentialTransfer(address,byte[],byte[],byte[])void']
      argsTuple: ConfidentialAssetArgs['tuple']['confidentialTransfer(address,byte[],byte[],byte[])void']
      returns: ConfidentialAssetReturns['confidentialTransfer(address,byte[],byte[],byte[])void']
    }>
    & Record<'unshield(uint64,byte[])void' | 'unshield', {
      argsObj: ConfidentialAssetArgs['obj']['unshield(uint64,byte[])void']
      argsTuple: ConfidentialAssetArgs['tuple']['unshield(uint64,byte[])void']
      returns: ConfidentialAssetReturns['unshield(uint64,byte[])void']
    }>
    & Record<'optInToAsset()void' | 'optInToAsset', {
      argsObj: ConfidentialAssetArgs['obj']['optInToAsset()void']
      argsTuple: ConfidentialAssetArgs['tuple']['optInToAsset()void']
      returns: ConfidentialAssetReturns['optInToAsset()void']
    }>
  /**
   * Defines the shape of the state of the application.
   */
  state: {
    global: {
      keys: {
        assetId: bigint
        totalDeposited: bigint
        totalWithdrawn: bigint
      }
      maps: {}
    }
    box: {
      keys: {}
      maps: {
        balances: Map<string, Uint8Array>
      }
    }
  }
}

/**
 * Defines the possible abi call signatures.
 */
export type ConfidentialAssetSignatures = keyof ConfidentialAssetTypes['methods']
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the ConfidentialAsset smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends ConfidentialAssetSignatures> = ConfidentialAssetTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the ConfidentialAsset smart contract to the method's return type
 */
export type MethodReturn<TSignature extends ConfidentialAssetSignatures> = ConfidentialAssetTypes['methods'][TSignature]['returns']

/**
 * Defines the shape of the keyed global state of the application.
 */
export type GlobalKeysState = ConfidentialAssetTypes['state']['global']['keys']

/**
 * Defines the shape of the keyed box state of the application.
 */
export type BoxKeysState = ConfidentialAssetTypes['state']['box']['keys']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the ConfidentialAsset smart contract
 */
export abstract class ConfidentialAssetParamsFactory {
  /**
   * Constructs a no op call for the shield(byte[],uint64)void ABI method
   *
  * Shield — deposit public funds, create a Pedersen commitment.
  Commitment = amount * G + blinding * H (64-byte BN254 G1 point)

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static shield(params: CallParams<ConfidentialAssetArgs['obj']['shield(byte[],uint64)void'] | ConfidentialAssetArgs['tuple']['shield(byte[],uint64)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'shield(byte[],uint64)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.commitment, params.args.amount],
    }
  }
  /**
   * Constructs a no op call for the confidentialTransfer(address,byte[],byte[],byte[])void ABI method
   *
  * Confidential transfer — transfer between shielded balances with hidden amount.
  Range proofs verified by LogicSig in atomic group.

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static confidentialTransfer(params: CallParams<ConfidentialAssetArgs['obj']['confidentialTransfer(address,byte[],byte[],byte[])void'] | ConfidentialAssetArgs['tuple']['confidentialTransfer(address,byte[],byte[],byte[])void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'confidentialTransfer(address,byte[],byte[],byte[])void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.recipient, params.args.senderNewCommitment, params.args.recipientNewCommitment, params.args.transferCommitment],
    }
  }
  /**
   * Constructs a no op call for the unshield(uint64,byte[])void ABI method
   *
   * Unshield — withdraw from shielded balance to public.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static unshield(params: CallParams<ConfidentialAssetArgs['obj']['unshield(uint64,byte[])void'] | ConfidentialAssetArgs['tuple']['unshield(uint64,byte[])void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'unshield(uint64,byte[])void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.amount, params.args.newCommitment],
    }
  }
  /**
   * Constructs a no op call for the optInToAsset()void ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static optInToAsset(params: CallParams<ConfidentialAssetArgs['obj']['optInToAsset()void'] | ConfidentialAssetArgs['tuple']['optInToAsset()void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'optInToAsset()void' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
}

/**
 * A client to make calls to the ConfidentialAsset smart contract
 */
export class ConfidentialAssetClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `ConfidentialAssetClient`
   *
   * @param appClient An `AppClient` instance which has been created with the ConfidentialAsset app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `ConfidentialAssetClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Returns a new `ConfidentialAssetClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<ConfidentialAssetClient> {
    return new ConfidentialAssetClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `ConfidentialAssetClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<ConfidentialAssetClient> {
    return new ConfidentialAssetClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Makes a clear_state call to an existing instance of the ConfidentialAsset smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the ConfidentialAsset smart contract using the `shield(byte[],uint64)void` ABI method.
     *
    * Shield — deposit public funds, create a Pedersen commitment.
    Commitment = amount * G + blinding * H (64-byte BN254 G1 point)

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    shield: (params: CallParams<ConfidentialAssetArgs['obj']['shield(byte[],uint64)void'] | ConfidentialAssetArgs['tuple']['shield(byte[],uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(ConfidentialAssetParamsFactory.shield(params))
    },

    /**
     * Makes a call to the ConfidentialAsset smart contract using the `confidentialTransfer(address,byte[],byte[],byte[])void` ABI method.
     *
    * Confidential transfer — transfer between shielded balances with hidden amount.
    Range proofs verified by LogicSig in atomic group.

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    confidentialTransfer: (params: CallParams<ConfidentialAssetArgs['obj']['confidentialTransfer(address,byte[],byte[],byte[])void'] | ConfidentialAssetArgs['tuple']['confidentialTransfer(address,byte[],byte[],byte[])void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(ConfidentialAssetParamsFactory.confidentialTransfer(params))
    },

    /**
     * Makes a call to the ConfidentialAsset smart contract using the `unshield(uint64,byte[])void` ABI method.
     *
     * Unshield — withdraw from shielded balance to public.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    unshield: (params: CallParams<ConfidentialAssetArgs['obj']['unshield(uint64,byte[])void'] | ConfidentialAssetArgs['tuple']['unshield(uint64,byte[])void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(ConfidentialAssetParamsFactory.unshield(params))
    },

    /**
     * Makes a call to the ConfidentialAsset smart contract using the `optInToAsset()void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    optInToAsset: (params: CallParams<ConfidentialAssetArgs['obj']['optInToAsset()void'] | ConfidentialAssetArgs['tuple']['optInToAsset()void']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(ConfidentialAssetParamsFactory.optInToAsset(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Makes a clear_state call to an existing instance of the ConfidentialAsset smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the ConfidentialAsset smart contract using the `shield(byte[],uint64)void` ABI method.
     *
    * Shield — deposit public funds, create a Pedersen commitment.
    Commitment = amount * G + blinding * H (64-byte BN254 G1 point)

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    shield: (params: CallParams<ConfidentialAssetArgs['obj']['shield(byte[],uint64)void'] | ConfidentialAssetArgs['tuple']['shield(byte[],uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(ConfidentialAssetParamsFactory.shield(params))
    },

    /**
     * Makes a call to the ConfidentialAsset smart contract using the `confidentialTransfer(address,byte[],byte[],byte[])void` ABI method.
     *
    * Confidential transfer — transfer between shielded balances with hidden amount.
    Range proofs verified by LogicSig in atomic group.

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    confidentialTransfer: (params: CallParams<ConfidentialAssetArgs['obj']['confidentialTransfer(address,byte[],byte[],byte[])void'] | ConfidentialAssetArgs['tuple']['confidentialTransfer(address,byte[],byte[],byte[])void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(ConfidentialAssetParamsFactory.confidentialTransfer(params))
    },

    /**
     * Makes a call to the ConfidentialAsset smart contract using the `unshield(uint64,byte[])void` ABI method.
     *
     * Unshield — withdraw from shielded balance to public.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    unshield: (params: CallParams<ConfidentialAssetArgs['obj']['unshield(uint64,byte[])void'] | ConfidentialAssetArgs['tuple']['unshield(uint64,byte[])void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(ConfidentialAssetParamsFactory.unshield(params))
    },

    /**
     * Makes a call to the ConfidentialAsset smart contract using the `optInToAsset()void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    optInToAsset: (params: CallParams<ConfidentialAssetArgs['obj']['optInToAsset()void'] | ConfidentialAssetArgs['tuple']['optInToAsset()void']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(ConfidentialAssetParamsFactory.optInToAsset(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Makes a clear_state call to an existing instance of the ConfidentialAsset smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the ConfidentialAsset smart contract using the `shield(byte[],uint64)void` ABI method.
     *
    * Shield — deposit public funds, create a Pedersen commitment.
    Commitment = amount * G + blinding * H (64-byte BN254 G1 point)

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    shield: async (params: CallParams<ConfidentialAssetArgs['obj']['shield(byte[],uint64)void'] | ConfidentialAssetArgs['tuple']['shield(byte[],uint64)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(ConfidentialAssetParamsFactory.shield(params))
      return {...result, return: result.return as unknown as (undefined | ConfidentialAssetReturns['shield(byte[],uint64)void'])}
    },

    /**
     * Makes a call to the ConfidentialAsset smart contract using the `confidentialTransfer(address,byte[],byte[],byte[])void` ABI method.
     *
    * Confidential transfer — transfer between shielded balances with hidden amount.
    Range proofs verified by LogicSig in atomic group.

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    confidentialTransfer: async (params: CallParams<ConfidentialAssetArgs['obj']['confidentialTransfer(address,byte[],byte[],byte[])void'] | ConfidentialAssetArgs['tuple']['confidentialTransfer(address,byte[],byte[],byte[])void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(ConfidentialAssetParamsFactory.confidentialTransfer(params))
      return {...result, return: result.return as unknown as (undefined | ConfidentialAssetReturns['confidentialTransfer(address,byte[],byte[],byte[])void'])}
    },

    /**
     * Makes a call to the ConfidentialAsset smart contract using the `unshield(uint64,byte[])void` ABI method.
     *
     * Unshield — withdraw from shielded balance to public.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    unshield: async (params: CallParams<ConfidentialAssetArgs['obj']['unshield(uint64,byte[])void'] | ConfidentialAssetArgs['tuple']['unshield(uint64,byte[])void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(ConfidentialAssetParamsFactory.unshield(params))
      return {...result, return: result.return as unknown as (undefined | ConfidentialAssetReturns['unshield(uint64,byte[])void'])}
    },

    /**
     * Makes a call to the ConfidentialAsset smart contract using the `optInToAsset()void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    optInToAsset: async (params: CallParams<ConfidentialAssetArgs['obj']['optInToAsset()void'] | ConfidentialAssetArgs['tuple']['optInToAsset()void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(ConfidentialAssetParamsFactory.optInToAsset(params))
      return {...result, return: result.return as unknown as (undefined | ConfidentialAssetReturns['optInToAsset()void'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new ConfidentialAssetClient(this.appClient.clone(params))
  }

  /**
   * Methods to access state for the current ConfidentialAsset app
   */
  state = {
    /**
     * Methods to access global state for the current ConfidentialAsset app
     */
    global: {
      /**
       * Get all current keyed values from global state
       */
      getAll: async (): Promise<Partial<Expand<GlobalKeysState>>> => {
        const result = await this.appClient.state.global.getAll()
        return {
          assetId: result.assetId,
          totalDeposited: result.totalDeposited,
          totalWithdrawn: result.totalWithdrawn,
        }
      },
      /**
       * Get the current value of the assetId key in global state
       */
      assetId: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("assetId")) as bigint | undefined },
      /**
       * Get the current value of the totalDeposited key in global state
       */
      totalDeposited: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("totalDeposited")) as bigint | undefined },
      /**
       * Get the current value of the totalWithdrawn key in global state
       */
      totalWithdrawn: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("totalWithdrawn")) as bigint | undefined },
    },
    /**
     * Methods to access box state for the current ConfidentialAsset app
     */
    box: {
      /**
       * Get all current keyed values from box state
       */
      getAll: async (): Promise<Partial<Expand<BoxKeysState>>> => {
        const result = await this.appClient.state.box.getAll()
        return {
        }
      },
      /**
       * Get values from the balances map in box state
       */
      balances: {
        /**
         * Get all current values of the balances map in box state
         */
        getMap: async (): Promise<Map<string, Uint8Array>> => { return (await this.appClient.state.box.getMap("balances")) as Map<string, Uint8Array> },
        /**
         * Get a current value of the balances map by key from box state
         */
        value: async (key: string): Promise<Uint8Array | undefined> => { return await this.appClient.state.box.getMapValue("balances", key) as Uint8Array | undefined },
      },
    },
  }

  public newGroup(): ConfidentialAssetComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a shield(byte[],uint64)void method call against the ConfidentialAsset contract
       */
      shield(params: CallParams<ConfidentialAssetArgs['obj']['shield(byte[],uint64)void'] | ConfidentialAssetArgs['tuple']['shield(byte[],uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.shield(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a confidentialTransfer(address,byte[],byte[],byte[])void method call against the ConfidentialAsset contract
       */
      confidentialTransfer(params: CallParams<ConfidentialAssetArgs['obj']['confidentialTransfer(address,byte[],byte[],byte[])void'] | ConfidentialAssetArgs['tuple']['confidentialTransfer(address,byte[],byte[],byte[])void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.confidentialTransfer(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a unshield(uint64,byte[])void method call against the ConfidentialAsset contract
       */
      unshield(params: CallParams<ConfidentialAssetArgs['obj']['unshield(uint64,byte[])void'] | ConfidentialAssetArgs['tuple']['unshield(uint64,byte[])void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.unshield(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a optInToAsset()void method call against the ConfidentialAsset contract
       */
      optInToAsset(params: CallParams<ConfidentialAssetArgs['obj']['optInToAsset()void'] | ConfidentialAssetArgs['tuple']['optInToAsset()void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.optInToAsset(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a clear state call to the ConfidentialAsset contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as ConfidentialAssetComposer
  }
}
export type ConfidentialAssetComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the shield(byte[],uint64)void ABI method.
   *
  * Shield — deposit public funds, create a Pedersen commitment.
  Commitment = amount * G + blinding * H (64-byte BN254 G1 point)

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  shield(params?: CallParams<ConfidentialAssetArgs['obj']['shield(byte[],uint64)void'] | ConfidentialAssetArgs['tuple']['shield(byte[],uint64)void']>): ConfidentialAssetComposer<[...TReturns, ConfidentialAssetReturns['shield(byte[],uint64)void'] | undefined]>

  /**
   * Calls the confidentialTransfer(address,byte[],byte[],byte[])void ABI method.
   *
  * Confidential transfer — transfer between shielded balances with hidden amount.
  Range proofs verified by LogicSig in atomic group.

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  confidentialTransfer(params?: CallParams<ConfidentialAssetArgs['obj']['confidentialTransfer(address,byte[],byte[],byte[])void'] | ConfidentialAssetArgs['tuple']['confidentialTransfer(address,byte[],byte[],byte[])void']>): ConfidentialAssetComposer<[...TReturns, ConfidentialAssetReturns['confidentialTransfer(address,byte[],byte[],byte[])void'] | undefined]>

  /**
   * Calls the unshield(uint64,byte[])void ABI method.
   *
   * Unshield — withdraw from shielded balance to public.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  unshield(params?: CallParams<ConfidentialAssetArgs['obj']['unshield(uint64,byte[])void'] | ConfidentialAssetArgs['tuple']['unshield(uint64,byte[])void']>): ConfidentialAssetComposer<[...TReturns, ConfidentialAssetReturns['unshield(uint64,byte[])void'] | undefined]>

  /**
   * Calls the optInToAsset()void ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  optInToAsset(params?: CallParams<ConfidentialAssetArgs['obj']['optInToAsset()void'] | ConfidentialAssetArgs['tuple']['optInToAsset()void']>): ConfidentialAssetComposer<[...TReturns, ConfidentialAssetReturns['optInToAsset()void'] | undefined]>

  /**
   * Makes a clear_state call to an existing instance of the ConfidentialAsset smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): ConfidentialAssetComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): ConfidentialAssetComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<ConfidentialAssetComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<ConfidentialAssetComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<ConfidentialAssetComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<ConfidentialAssetComposerResults<TReturns>>
}
export type ConfidentialAssetComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

