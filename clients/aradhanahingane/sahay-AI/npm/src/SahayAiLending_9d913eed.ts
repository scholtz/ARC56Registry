/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"SahayAiLending","structs":{},"methods":[{"name":"set_fx_rate","args":[{"type":"uint64","name":"lockedInrPerUsdPaise"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}},{"name":"create_loan","args":[{"type":"string","name":"borrower"},{"type":"uint64","name":"loanAmount"},{"type":"uint64","name":"repaymentAmount"},{"type":"uint64","name":"repaymentPercentage"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}},{"name":"record_repayment","args":[{"type":"uint64","name":"amount"}],"returns":{"type":"uint64"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}}],"arcs":[22,28],"networks":{},"state":{"schema":{"global":{"ints":9,"bytes":3},"local":{"ints":0,"bytes":0}},"keys":{"global":{"borrower":{"keyType":"AVMString","valueType":"AVMString","key":"Ym9ycm93ZXI="},"borrowerAuth":{"keyType":"AVMString","valueType":"AVMBytes","key":"Ym9ycm93ZXJfYXV0aA=="},"lenderAuth":{"keyType":"AVMString","valueType":"AVMBytes","key":"bGVuZGVyX2F1dGg="},"usdcAssetId":{"keyType":"AVMString","valueType":"AVMUint64","key":"dXNkY19hc3NldF9pZA=="},"loanAmount":{"keyType":"AVMString","valueType":"AVMUint64","key":"bG9hbl9hbW91bnQ="},"repaymentAmount":{"keyType":"AVMString","valueType":"AVMUint64","key":"cmVwYXltZW50X2Ftb3VudA=="},"repaymentPercentage":{"keyType":"AVMString","valueType":"AVMUint64","key":"cmVwYXltZW50X3BlcmNlbnRhZ2U="},"lockedInrPerUsdPaise":{"keyType":"AVMString","valueType":"AVMUint64","key":"bG9ja2VkX2lucl9wZXJfdXNkX3BhaXNl"},"totalUsdcOwedMicro":{"keyType":"AVMString","valueType":"AVMUint64","key":"dG90YWxfdXNkY19vd2VkX21pY3Jv"},"totalUsdcRepaidMicro":{"keyType":"AVMString","valueType":"AVMUint64","key":"dG90YWxfdXNkY19yZXBhaWRfbWljcm8="},"loanClosed":{"keyType":"AVMString","valueType":"AVMUint64","key":"bG9hbl9jbG9zZWQ="},"repaidAmount":{"keyType":"AVMString","valueType":"AVMUint64","key":"cmVwYWlkX2Ftb3VudA=="}},"local":{},"box":{}},"maps":{"global":{},"local":{},"box":{}}},"bareActions":{"create":["NoOp"],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[265],"errorMessage":"OnCompletion must be NoOp"},{"pc":[305],"errorMessage":"OnCompletion must be NoOp && can only call when creating"},{"pc":[340],"errorMessage":"cannot change fx during active loan"},{"pc":[326,335,403,412,435,475,485,494,506,570,581,606,612,621,639,675,685,697,702,714,723,727,738],"errorMessage":"check GlobalState exists"},{"pc":[417],"errorMessage":"existing loan is still active"},{"pc":[471],"errorMessage":"funding transfer amount must be greater than zero"},{"pc":[572],"errorMessage":"funding transfer must use configured USDC asset"},{"pc":[343],"errorMessage":"fx rate must be greater than zero"},{"pc":[436],"errorMessage":"fx rate must be set before creating loan"},{"pc":[608],"errorMessage":"fx rate must be set before repayment"},{"pc":[362],"errorMessage":"invalid array length header"},{"pc":[369],"errorMessage":"invalid number of bytes for arc4.dynamic_array<arc4.uint8>"},{"pc":[315,380,389,398,598],"errorMessage":"invalid number of bytes for arc4.uint64"},{"pc":[421],"errorMessage":"loan amount must be greater than zero"},{"pc":[444],"errorMessage":"loan creation app call must follow the funding transfer"},{"pc":[441],"errorMessage":"loan creation must be grouped with a USDC transfer"},{"pc":[465],"errorMessage":"loan funding transfer must be sent by lender caller"},{"pc":[614],"errorMessage":"loan is already closed"},{"pc":[626],"errorMessage":"only borrower can record repayments"},{"pc":[583],"errorMessage":"only configured lender can create loans"},{"pc":[322],"errorMessage":"only creator can set fx rate"},{"pc":[425],"errorMessage":"repayment amount must be >= loan amount"},{"pc":[601],"errorMessage":"repayment amount must be greater than zero"},{"pc":[634],"errorMessage":"repayment app call must follow the repayment transfer"},{"pc":[706],"errorMessage":"repayment exceeds usdc owed amount"},{"pc":[631],"errorMessage":"repayment must be grouped with a USDC transfer"},{"pc":[430],"errorMessage":"repayment percentage must be <= 100"},{"pc":[693],"errorMessage":"repayment transfer amount must match computed due amount"},{"pc":[687],"errorMessage":"repayment transfer must use configured USDC asset"},{"pc":[677],"errorMessage":"repayment transfer receiver must be lender"},{"pc":[668],"errorMessage":"repayment transfer sender must be borrower"},{"pc":[458,661],"errorMessage":"transaction type is axfer"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"events":[]} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


/**
 * The argument types for the SahayAiLending contract
 */
export type SahayAiLendingArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'set_fx_rate(uint64)void': {
      lockedInrPerUsdPaise: bigint | number
    }
    'create_loan(string,uint64,uint64,uint64)void': {
      borrower: string
      loanAmount: bigint | number
      repaymentAmount: bigint | number
      repaymentPercentage: bigint | number
    }
    'record_repayment(uint64)uint64': {
      amount: bigint | number
    }
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'set_fx_rate(uint64)void': [lockedInrPerUsdPaise: bigint | number]
    'create_loan(string,uint64,uint64,uint64)void': [borrower: string, loanAmount: bigint | number, repaymentAmount: bigint | number, repaymentPercentage: bigint | number]
    'record_repayment(uint64)uint64': [amount: bigint | number]
  }
}

/**
 * The return type for each method
 */
export type SahayAiLendingReturns = {
  'set_fx_rate(uint64)void': void
  'create_loan(string,uint64,uint64,uint64)void': void
  'record_repayment(uint64)uint64': bigint
}

/**
 * Defines the types of available calls and state of the SahayAiLending smart contract.
 */
export type SahayAiLendingTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'set_fx_rate(uint64)void' | 'set_fx_rate', {
      argsObj: SahayAiLendingArgs['obj']['set_fx_rate(uint64)void']
      argsTuple: SahayAiLendingArgs['tuple']['set_fx_rate(uint64)void']
      returns: SahayAiLendingReturns['set_fx_rate(uint64)void']
    }>
    & Record<'create_loan(string,uint64,uint64,uint64)void' | 'create_loan', {
      argsObj: SahayAiLendingArgs['obj']['create_loan(string,uint64,uint64,uint64)void']
      argsTuple: SahayAiLendingArgs['tuple']['create_loan(string,uint64,uint64,uint64)void']
      returns: SahayAiLendingReturns['create_loan(string,uint64,uint64,uint64)void']
    }>
    & Record<'record_repayment(uint64)uint64' | 'record_repayment', {
      argsObj: SahayAiLendingArgs['obj']['record_repayment(uint64)uint64']
      argsTuple: SahayAiLendingArgs['tuple']['record_repayment(uint64)uint64']
      returns: SahayAiLendingReturns['record_repayment(uint64)uint64']
    }>
  /**
   * Defines the shape of the state of the application.
   */
  state: {
    global: {
      keys: {
        borrower: string
        borrowerAuth: BinaryState
        lenderAuth: BinaryState
        usdcAssetId: bigint
        loanAmount: bigint
        repaymentAmount: bigint
        repaymentPercentage: bigint
        lockedInrPerUsdPaise: bigint
        totalUsdcOwedMicro: bigint
        totalUsdcRepaidMicro: bigint
        loanClosed: bigint
        repaidAmount: bigint
      }
      maps: {}
    }
  }
}

/**
 * Defines the possible abi call signatures.
 */
export type SahayAiLendingSignatures = keyof SahayAiLendingTypes['methods']
/**
 * Defines the possible abi call signatures for methods that return a non-void value.
 */
export type SahayAiLendingNonVoidMethodSignatures = keyof SahayAiLendingTypes['methods'] extends infer T ? T extends keyof SahayAiLendingTypes['methods'] ? MethodReturn<T> extends void ? never : T  : never : never
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the SahayAiLending smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends SahayAiLendingSignatures> = SahayAiLendingTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the SahayAiLending smart contract to the method's return type
 */
export type MethodReturn<TSignature extends SahayAiLendingSignatures> = SahayAiLendingTypes['methods'][TSignature]['returns']

/**
 * Defines the shape of the keyed global state of the application.
 */
export type GlobalKeysState = SahayAiLendingTypes['state']['global']['keys']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the SahayAiLending smart contract
 */
export abstract class SahayAiLendingParamsFactory {
  /**
   * Constructs a no op call for the set_fx_rate(uint64)void ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static setFxRate(params: CallParams<SahayAiLendingArgs['obj']['set_fx_rate(uint64)void'] | SahayAiLendingArgs['tuple']['set_fx_rate(uint64)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'set_fx_rate(uint64)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.lockedInrPerUsdPaise],
    }
  }
  /**
   * Constructs a no op call for the create_loan(string,uint64,uint64,uint64)void ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static createLoan(params: CallParams<SahayAiLendingArgs['obj']['create_loan(string,uint64,uint64,uint64)void'] | SahayAiLendingArgs['tuple']['create_loan(string,uint64,uint64,uint64)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'create_loan(string,uint64,uint64,uint64)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.borrower, params.args.loanAmount, params.args.repaymentAmount, params.args.repaymentPercentage],
    }
  }
  /**
   * Constructs a no op call for the record_repayment(uint64)uint64 ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static recordRepayment(params: CallParams<SahayAiLendingArgs['obj']['record_repayment(uint64)uint64'] | SahayAiLendingArgs['tuple']['record_repayment(uint64)uint64']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'record_repayment(uint64)uint64' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.amount],
    }
  }
}

/**
 * A client to make calls to the SahayAiLending smart contract
 */
export class SahayAiLendingClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `SahayAiLendingClient`
   *
   * @param appClient An `AppClient` instance which has been created with the SahayAiLending app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `SahayAiLendingClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Checks for decode errors on the given return value and maps the return value to the return type for the given method
   * @returns The typed return value or undefined if there was no value
   */
  decodeReturnValue<TSignature extends SahayAiLendingNonVoidMethodSignatures>(method: TSignature, returnValue: ABIReturn | undefined) {
    return returnValue !== undefined ? getArc56ReturnValue<MethodReturn<TSignature>>(returnValue, this.appClient.getABIMethod(method), APP_SPEC.structs) : undefined
  }

  /**
   * Returns a new `SahayAiLendingClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<SahayAiLendingClient> {
    return new SahayAiLendingClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `SahayAiLendingClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<SahayAiLendingClient> {
    return new SahayAiLendingClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Makes a clear_state call to an existing instance of the SahayAiLending smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the SahayAiLending smart contract using the `set_fx_rate(uint64)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    setFxRate: (params: CallParams<SahayAiLendingArgs['obj']['set_fx_rate(uint64)void'] | SahayAiLendingArgs['tuple']['set_fx_rate(uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(SahayAiLendingParamsFactory.setFxRate(params))
    },

    /**
     * Makes a call to the SahayAiLending smart contract using the `create_loan(string,uint64,uint64,uint64)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    createLoan: (params: CallParams<SahayAiLendingArgs['obj']['create_loan(string,uint64,uint64,uint64)void'] | SahayAiLendingArgs['tuple']['create_loan(string,uint64,uint64,uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(SahayAiLendingParamsFactory.createLoan(params))
    },

    /**
     * Makes a call to the SahayAiLending smart contract using the `record_repayment(uint64)uint64` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    recordRepayment: (params: CallParams<SahayAiLendingArgs['obj']['record_repayment(uint64)uint64'] | SahayAiLendingArgs['tuple']['record_repayment(uint64)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(SahayAiLendingParamsFactory.recordRepayment(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Makes a clear_state call to an existing instance of the SahayAiLending smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the SahayAiLending smart contract using the `set_fx_rate(uint64)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    setFxRate: (params: CallParams<SahayAiLendingArgs['obj']['set_fx_rate(uint64)void'] | SahayAiLendingArgs['tuple']['set_fx_rate(uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(SahayAiLendingParamsFactory.setFxRate(params))
    },

    /**
     * Makes a call to the SahayAiLending smart contract using the `create_loan(string,uint64,uint64,uint64)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    createLoan: (params: CallParams<SahayAiLendingArgs['obj']['create_loan(string,uint64,uint64,uint64)void'] | SahayAiLendingArgs['tuple']['create_loan(string,uint64,uint64,uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(SahayAiLendingParamsFactory.createLoan(params))
    },

    /**
     * Makes a call to the SahayAiLending smart contract using the `record_repayment(uint64)uint64` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    recordRepayment: (params: CallParams<SahayAiLendingArgs['obj']['record_repayment(uint64)uint64'] | SahayAiLendingArgs['tuple']['record_repayment(uint64)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(SahayAiLendingParamsFactory.recordRepayment(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Makes a clear_state call to an existing instance of the SahayAiLending smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the SahayAiLending smart contract using the `set_fx_rate(uint64)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    setFxRate: async (params: CallParams<SahayAiLendingArgs['obj']['set_fx_rate(uint64)void'] | SahayAiLendingArgs['tuple']['set_fx_rate(uint64)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(SahayAiLendingParamsFactory.setFxRate(params))
      return {...result, return: result.return as unknown as (undefined | SahayAiLendingReturns['set_fx_rate(uint64)void'])}
    },

    /**
     * Makes a call to the SahayAiLending smart contract using the `create_loan(string,uint64,uint64,uint64)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    createLoan: async (params: CallParams<SahayAiLendingArgs['obj']['create_loan(string,uint64,uint64,uint64)void'] | SahayAiLendingArgs['tuple']['create_loan(string,uint64,uint64,uint64)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(SahayAiLendingParamsFactory.createLoan(params))
      return {...result, return: result.return as unknown as (undefined | SahayAiLendingReturns['create_loan(string,uint64,uint64,uint64)void'])}
    },

    /**
     * Makes a call to the SahayAiLending smart contract using the `record_repayment(uint64)uint64` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    recordRepayment: async (params: CallParams<SahayAiLendingArgs['obj']['record_repayment(uint64)uint64'] | SahayAiLendingArgs['tuple']['record_repayment(uint64)uint64']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(SahayAiLendingParamsFactory.recordRepayment(params))
      return {...result, return: result.return as unknown as (undefined | SahayAiLendingReturns['record_repayment(uint64)uint64'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new SahayAiLendingClient(this.appClient.clone(params))
  }

  /**
   * Methods to access state for the current SahayAiLending app
   */
  state = {
    /**
     * Methods to access global state for the current SahayAiLending app
     */
    global: {
      /**
       * Get all current keyed values from global state
       */
      getAll: async (): Promise<Partial<Expand<GlobalKeysState>>> => {
        const result = await this.appClient.state.global.getAll()
        return {
          borrower: result.borrower,
          borrowerAuth: new BinaryStateValue(result.borrowerAuth),
          lenderAuth: new BinaryStateValue(result.lenderAuth),
          usdcAssetId: result.usdcAssetId,
          loanAmount: result.loanAmount,
          repaymentAmount: result.repaymentAmount,
          repaymentPercentage: result.repaymentPercentage,
          lockedInrPerUsdPaise: result.lockedInrPerUsdPaise,
          totalUsdcOwedMicro: result.totalUsdcOwedMicro,
          totalUsdcRepaidMicro: result.totalUsdcRepaidMicro,
          loanClosed: result.loanClosed,
          repaidAmount: result.repaidAmount,
        }
      },
      /**
       * Get the current value of the borrower key in global state
       */
      borrower: async (): Promise<string | undefined> => { return (await this.appClient.state.global.getValue("borrower")) as string | undefined },
      /**
       * Get the current value of the borrowerAuth key in global state
       */
      borrowerAuth: async (): Promise<BinaryState> => { return new BinaryStateValue((await this.appClient.state.global.getValue("borrowerAuth")) as Uint8Array | undefined) },
      /**
       * Get the current value of the lenderAuth key in global state
       */
      lenderAuth: async (): Promise<BinaryState> => { return new BinaryStateValue((await this.appClient.state.global.getValue("lenderAuth")) as Uint8Array | undefined) },
      /**
       * Get the current value of the usdcAssetId key in global state
       */
      usdcAssetId: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("usdcAssetId")) as bigint | undefined },
      /**
       * Get the current value of the loanAmount key in global state
       */
      loanAmount: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("loanAmount")) as bigint | undefined },
      /**
       * Get the current value of the repaymentAmount key in global state
       */
      repaymentAmount: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("repaymentAmount")) as bigint | undefined },
      /**
       * Get the current value of the repaymentPercentage key in global state
       */
      repaymentPercentage: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("repaymentPercentage")) as bigint | undefined },
      /**
       * Get the current value of the lockedInrPerUsdPaise key in global state
       */
      lockedInrPerUsdPaise: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("lockedInrPerUsdPaise")) as bigint | undefined },
      /**
       * Get the current value of the totalUsdcOwedMicro key in global state
       */
      totalUsdcOwedMicro: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("totalUsdcOwedMicro")) as bigint | undefined },
      /**
       * Get the current value of the totalUsdcRepaidMicro key in global state
       */
      totalUsdcRepaidMicro: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("totalUsdcRepaidMicro")) as bigint | undefined },
      /**
       * Get the current value of the loanClosed key in global state
       */
      loanClosed: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("loanClosed")) as bigint | undefined },
      /**
       * Get the current value of the repaidAmount key in global state
       */
      repaidAmount: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("repaidAmount")) as bigint | undefined },
    },
  }

  public newGroup(): SahayAiLendingComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a set_fx_rate(uint64)void method call against the SahayAiLending contract
       */
      setFxRate(params: CallParams<SahayAiLendingArgs['obj']['set_fx_rate(uint64)void'] | SahayAiLendingArgs['tuple']['set_fx_rate(uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.setFxRate(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a create_loan(string,uint64,uint64,uint64)void method call against the SahayAiLending contract
       */
      createLoan(params: CallParams<SahayAiLendingArgs['obj']['create_loan(string,uint64,uint64,uint64)void'] | SahayAiLendingArgs['tuple']['create_loan(string,uint64,uint64,uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.createLoan(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a record_repayment(uint64)uint64 method call against the SahayAiLending contract
       */
      recordRepayment(params: CallParams<SahayAiLendingArgs['obj']['record_repayment(uint64)uint64'] | SahayAiLendingArgs['tuple']['record_repayment(uint64)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.recordRepayment(params)))
        resultMappers.push((v) => client.decodeReturnValue('record_repayment(uint64)uint64', v))
        return this
      },
      /**
       * Add a clear state call to the SahayAiLending contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as SahayAiLendingComposer
  }
}
export type SahayAiLendingComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the set_fx_rate(uint64)void ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  setFxRate(params?: CallParams<SahayAiLendingArgs['obj']['set_fx_rate(uint64)void'] | SahayAiLendingArgs['tuple']['set_fx_rate(uint64)void']>): SahayAiLendingComposer<[...TReturns, SahayAiLendingReturns['set_fx_rate(uint64)void'] | undefined]>

  /**
   * Calls the create_loan(string,uint64,uint64,uint64)void ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  createLoan(params?: CallParams<SahayAiLendingArgs['obj']['create_loan(string,uint64,uint64,uint64)void'] | SahayAiLendingArgs['tuple']['create_loan(string,uint64,uint64,uint64)void']>): SahayAiLendingComposer<[...TReturns, SahayAiLendingReturns['create_loan(string,uint64,uint64,uint64)void'] | undefined]>

  /**
   * Calls the record_repayment(uint64)uint64 ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  recordRepayment(params?: CallParams<SahayAiLendingArgs['obj']['record_repayment(uint64)uint64'] | SahayAiLendingArgs['tuple']['record_repayment(uint64)uint64']>): SahayAiLendingComposer<[...TReturns, SahayAiLendingReturns['record_repayment(uint64)uint64'] | undefined]>

  /**
   * Makes a clear_state call to an existing instance of the SahayAiLending smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): SahayAiLendingComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): SahayAiLendingComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<SahayAiLendingComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<SahayAiLendingComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<SahayAiLendingComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<SahayAiLendingComposerResults<TReturns>>
}
export type SahayAiLendingComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

