/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"EscrowContract","structs":{},"methods":[{"name":"create","args":[{"type":"address","name":"_buyer"},{"type":"address","name":"_supplier"},{"type":"uint64","name":"_amount"}],"returns":{"type":"void"},"actions":{"create":["NoOp"],"call":[]},"readonly":false,"desc":"Initialize the escrow with buyer, supplier, and agreed amount.","events":[],"recommendations":{}},{"name":"fund","args":[{"type":"pay","name":"payment"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Buyer funds the escrow with the agreed amount.","events":[],"recommendations":{}},{"name":"mark_verified","args":[],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Mark the delivery as verified. Only buyer can call this.","events":[],"recommendations":{}},{"name":"confirm_delivery","args":[],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Buyer or creator confirms delivery - releases funds to supplier.","events":[],"recommendations":{}},{"name":"get_funded","args":[],"returns":{"type":"bool"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"desc":"Returns whether escrow is funded.","events":[],"recommendations":{}},{"name":"get_released","args":[],"returns":{"type":"bool"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"desc":"Returns whether funds have been released.","events":[],"recommendations":{}},{"name":"get_verified","args":[],"returns":{"type":"bool"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"desc":"Returns whether delivery is verified.","events":[],"recommendations":{}}],"arcs":[22,28],"desc":"\n    ProcureAI Escrow Smart Contract\n    Holds funds from buyer and releases to supplier upon delivery confirmation.\n    ","networks":{},"state":{"schema":{"global":{"ints":4,"bytes":2},"local":{"ints":0,"bytes":0}},"keys":{"global":{"buyer":{"keyType":"AVMString","valueType":"address","key":"YnV5ZXI="},"supplier":{"keyType":"AVMString","valueType":"address","key":"c3VwcGxpZXI="},"amount":{"keyType":"AVMString","valueType":"AVMUint64","key":"YW1vdW50"},"is_funded":{"keyType":"AVMString","valueType":"AVMUint64","key":"aXNfZnVuZGVk"},"is_released":{"keyType":"AVMString","valueType":"AVMUint64","key":"aXNfcmVsZWFzZWQ="},"is_verified":{"keyType":"AVMString","valueType":"AVMUint64","key":"aXNfdmVyaWZpZWQ="}},"local":{},"box":{}},"maps":{"global":{},"local":{},"box":{}}},"bareActions":{"create":[],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[305],"errorMessage":"Delivery not verified"},{"pc":[240],"errorMessage":"Escrow already funded"},{"pc":[258],"errorMessage":"Escrow must be funded"},{"pc":[288],"errorMessage":"Escrow not funded yet"},{"pc":[311],"errorMessage":"Funds already released"},{"pc":[211],"errorMessage":"Only buyer can fund"},{"pc":[253],"errorMessage":"Only buyer can verify"},{"pc":[283],"errorMessage":"Only buyer or creator can confirm delivery"},{"pc":[234],"errorMessage":"Payment amount must match agreed amount"},{"pc":[218],"errorMessage":"Payment must come from buyer"},{"pc":[225],"errorMessage":"Payment must go to contract"},{"pc":[232,325],"errorMessage":"check self.amount exists"},{"pc":[209,251,269],"errorMessage":"check self.buyer exists"},{"pc":[238,257,287,350],"errorMessage":"check self.is_funded exists"},{"pc":[309,367],"errorMessage":"check self.is_released exists"},{"pc":[292,384],"errorMessage":"check self.is_verified exists"},{"pc":[320],"errorMessage":"check self.supplier exists"},{"pc":[151,159],"errorMessage":"invalid number of bytes for arc4.static_array<arc4.uint8, 32>"},{"pc":[168],"errorMessage":"invalid number of bytes for arc4.uint64"},{"pc":[203],"errorMessage":"transaction type is pay"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"events":[]} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


/**
 * The argument types for the EscrowContract contract
 */
export type EscrowContractArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'create(address,address,uint64)void': {
      _buyer: string
      _supplier: string
      _amount: bigint | number
    }
    'fund(pay)void': {
      payment: AppMethodCallTransactionArgument
    }
    'mark_verified()void': Record<string, never>
    'confirm_delivery()void': Record<string, never>
    'get_funded()bool': Record<string, never>
    'get_released()bool': Record<string, never>
    'get_verified()bool': Record<string, never>
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'create(address,address,uint64)void': [_buyer: string, _supplier: string, _amount: bigint | number]
    'fund(pay)void': [payment: AppMethodCallTransactionArgument]
    'mark_verified()void': []
    'confirm_delivery()void': []
    'get_funded()bool': []
    'get_released()bool': []
    'get_verified()bool': []
  }
}

/**
 * The return type for each method
 */
export type EscrowContractReturns = {
  'create(address,address,uint64)void': void
  'fund(pay)void': void
  'mark_verified()void': void
  'confirm_delivery()void': void
  'get_funded()bool': boolean
  'get_released()bool': boolean
  'get_verified()bool': boolean
}

/**
 * Defines the types of available calls and state of the EscrowContract smart contract.
 */
export type EscrowContractTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'create(address,address,uint64)void' | 'create', {
      argsObj: EscrowContractArgs['obj']['create(address,address,uint64)void']
      argsTuple: EscrowContractArgs['tuple']['create(address,address,uint64)void']
      returns: EscrowContractReturns['create(address,address,uint64)void']
    }>
    & Record<'fund(pay)void' | 'fund', {
      argsObj: EscrowContractArgs['obj']['fund(pay)void']
      argsTuple: EscrowContractArgs['tuple']['fund(pay)void']
      returns: EscrowContractReturns['fund(pay)void']
    }>
    & Record<'mark_verified()void' | 'mark_verified', {
      argsObj: EscrowContractArgs['obj']['mark_verified()void']
      argsTuple: EscrowContractArgs['tuple']['mark_verified()void']
      returns: EscrowContractReturns['mark_verified()void']
    }>
    & Record<'confirm_delivery()void' | 'confirm_delivery', {
      argsObj: EscrowContractArgs['obj']['confirm_delivery()void']
      argsTuple: EscrowContractArgs['tuple']['confirm_delivery()void']
      returns: EscrowContractReturns['confirm_delivery()void']
    }>
    & Record<'get_funded()bool' | 'get_funded', {
      argsObj: EscrowContractArgs['obj']['get_funded()bool']
      argsTuple: EscrowContractArgs['tuple']['get_funded()bool']
      returns: EscrowContractReturns['get_funded()bool']
    }>
    & Record<'get_released()bool' | 'get_released', {
      argsObj: EscrowContractArgs['obj']['get_released()bool']
      argsTuple: EscrowContractArgs['tuple']['get_released()bool']
      returns: EscrowContractReturns['get_released()bool']
    }>
    & Record<'get_verified()bool' | 'get_verified', {
      argsObj: EscrowContractArgs['obj']['get_verified()bool']
      argsTuple: EscrowContractArgs['tuple']['get_verified()bool']
      returns: EscrowContractReturns['get_verified()bool']
    }>
  /**
   * Defines the shape of the state of the application.
   */
  state: {
    global: {
      keys: {
        buyer: string
        supplier: string
        amount: bigint
        isFunded: bigint
        isReleased: bigint
        isVerified: bigint
      }
      maps: {}
    }
  }
}

/**
 * Defines the possible abi call signatures.
 */
export type EscrowContractSignatures = keyof EscrowContractTypes['methods']
/**
 * Defines the possible abi call signatures for methods that return a non-void value.
 */
export type EscrowContractNonVoidMethodSignatures = keyof EscrowContractTypes['methods'] extends infer T ? T extends keyof EscrowContractTypes['methods'] ? MethodReturn<T> extends void ? never : T  : never : never
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the EscrowContract smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends EscrowContractSignatures> = EscrowContractTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the EscrowContract smart contract to the method's return type
 */
export type MethodReturn<TSignature extends EscrowContractSignatures> = EscrowContractTypes['methods'][TSignature]['returns']

/**
 * Defines the shape of the keyed global state of the application.
 */
export type GlobalKeysState = EscrowContractTypes['state']['global']['keys']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the EscrowContract smart contract
 */
export abstract class EscrowContractParamsFactory {
  /**
   * Constructs a no op call for the fund(pay)void ABI method
   *
   * Buyer funds the escrow with the agreed amount.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static fund(params: CallParams<EscrowContractArgs['obj']['fund(pay)void'] | EscrowContractArgs['tuple']['fund(pay)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'fund(pay)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.payment],
    }
  }
  /**
   * Constructs a no op call for the mark_verified()void ABI method
   *
   * Mark the delivery as verified. Only buyer can call this.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static markVerified(params: CallParams<EscrowContractArgs['obj']['mark_verified()void'] | EscrowContractArgs['tuple']['mark_verified()void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'mark_verified()void' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the confirm_delivery()void ABI method
   *
   * Buyer or creator confirms delivery - releases funds to supplier.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static confirmDelivery(params: CallParams<EscrowContractArgs['obj']['confirm_delivery()void'] | EscrowContractArgs['tuple']['confirm_delivery()void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'confirm_delivery()void' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the get_funded()bool ABI method
   *
   * Returns whether escrow is funded.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static getFunded(params: CallParams<EscrowContractArgs['obj']['get_funded()bool'] | EscrowContractArgs['tuple']['get_funded()bool']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'get_funded()bool' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the get_released()bool ABI method
   *
   * Returns whether funds have been released.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static getReleased(params: CallParams<EscrowContractArgs['obj']['get_released()bool'] | EscrowContractArgs['tuple']['get_released()bool']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'get_released()bool' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the get_verified()bool ABI method
   *
   * Returns whether delivery is verified.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static getVerified(params: CallParams<EscrowContractArgs['obj']['get_verified()bool'] | EscrowContractArgs['tuple']['get_verified()bool']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'get_verified()bool' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
}

/**
 * A client to make calls to the EscrowContract smart contract
 */
export class EscrowContractClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `EscrowContractClient`
   *
   * @param appClient An `AppClient` instance which has been created with the EscrowContract app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `EscrowContractClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Checks for decode errors on the given return value and maps the return value to the return type for the given method
   * @returns The typed return value or undefined if there was no value
   */
  decodeReturnValue<TSignature extends EscrowContractNonVoidMethodSignatures>(method: TSignature, returnValue: ABIReturn | undefined) {
    return returnValue !== undefined ? getArc56ReturnValue<MethodReturn<TSignature>>(returnValue, this.appClient.getABIMethod(method), APP_SPEC.structs) : undefined
  }

  /**
   * Returns a new `EscrowContractClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<EscrowContractClient> {
    return new EscrowContractClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `EscrowContractClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<EscrowContractClient> {
    return new EscrowContractClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Makes a clear_state call to an existing instance of the EscrowContract smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the EscrowContract smart contract using the `fund(pay)void` ABI method.
     *
     * Buyer funds the escrow with the agreed amount.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    fund: (params: CallParams<EscrowContractArgs['obj']['fund(pay)void'] | EscrowContractArgs['tuple']['fund(pay)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(EscrowContractParamsFactory.fund(params))
    },

    /**
     * Makes a call to the EscrowContract smart contract using the `mark_verified()void` ABI method.
     *
     * Mark the delivery as verified. Only buyer can call this.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    markVerified: (params: CallParams<EscrowContractArgs['obj']['mark_verified()void'] | EscrowContractArgs['tuple']['mark_verified()void']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(EscrowContractParamsFactory.markVerified(params))
    },

    /**
     * Makes a call to the EscrowContract smart contract using the `confirm_delivery()void` ABI method.
     *
     * Buyer or creator confirms delivery - releases funds to supplier.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    confirmDelivery: (params: CallParams<EscrowContractArgs['obj']['confirm_delivery()void'] | EscrowContractArgs['tuple']['confirm_delivery()void']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(EscrowContractParamsFactory.confirmDelivery(params))
    },

    /**
     * Makes a call to the EscrowContract smart contract using the `get_funded()bool` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * Returns whether escrow is funded.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    getFunded: (params: CallParams<EscrowContractArgs['obj']['get_funded()bool'] | EscrowContractArgs['tuple']['get_funded()bool']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(EscrowContractParamsFactory.getFunded(params))
    },

    /**
     * Makes a call to the EscrowContract smart contract using the `get_released()bool` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * Returns whether funds have been released.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    getReleased: (params: CallParams<EscrowContractArgs['obj']['get_released()bool'] | EscrowContractArgs['tuple']['get_released()bool']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(EscrowContractParamsFactory.getReleased(params))
    },

    /**
     * Makes a call to the EscrowContract smart contract using the `get_verified()bool` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * Returns whether delivery is verified.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    getVerified: (params: CallParams<EscrowContractArgs['obj']['get_verified()bool'] | EscrowContractArgs['tuple']['get_verified()bool']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(EscrowContractParamsFactory.getVerified(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Makes a clear_state call to an existing instance of the EscrowContract smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the EscrowContract smart contract using the `fund(pay)void` ABI method.
     *
     * Buyer funds the escrow with the agreed amount.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    fund: (params: CallParams<EscrowContractArgs['obj']['fund(pay)void'] | EscrowContractArgs['tuple']['fund(pay)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(EscrowContractParamsFactory.fund(params))
    },

    /**
     * Makes a call to the EscrowContract smart contract using the `mark_verified()void` ABI method.
     *
     * Mark the delivery as verified. Only buyer can call this.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    markVerified: (params: CallParams<EscrowContractArgs['obj']['mark_verified()void'] | EscrowContractArgs['tuple']['mark_verified()void']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(EscrowContractParamsFactory.markVerified(params))
    },

    /**
     * Makes a call to the EscrowContract smart contract using the `confirm_delivery()void` ABI method.
     *
     * Buyer or creator confirms delivery - releases funds to supplier.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    confirmDelivery: (params: CallParams<EscrowContractArgs['obj']['confirm_delivery()void'] | EscrowContractArgs['tuple']['confirm_delivery()void']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(EscrowContractParamsFactory.confirmDelivery(params))
    },

    /**
     * Makes a call to the EscrowContract smart contract using the `get_funded()bool` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * Returns whether escrow is funded.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    getFunded: (params: CallParams<EscrowContractArgs['obj']['get_funded()bool'] | EscrowContractArgs['tuple']['get_funded()bool']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(EscrowContractParamsFactory.getFunded(params))
    },

    /**
     * Makes a call to the EscrowContract smart contract using the `get_released()bool` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * Returns whether funds have been released.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    getReleased: (params: CallParams<EscrowContractArgs['obj']['get_released()bool'] | EscrowContractArgs['tuple']['get_released()bool']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(EscrowContractParamsFactory.getReleased(params))
    },

    /**
     * Makes a call to the EscrowContract smart contract using the `get_verified()bool` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * Returns whether delivery is verified.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    getVerified: (params: CallParams<EscrowContractArgs['obj']['get_verified()bool'] | EscrowContractArgs['tuple']['get_verified()bool']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(EscrowContractParamsFactory.getVerified(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Makes a clear_state call to an existing instance of the EscrowContract smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the EscrowContract smart contract using the `fund(pay)void` ABI method.
     *
     * Buyer funds the escrow with the agreed amount.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    fund: async (params: CallParams<EscrowContractArgs['obj']['fund(pay)void'] | EscrowContractArgs['tuple']['fund(pay)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(EscrowContractParamsFactory.fund(params))
      return {...result, return: result.return as unknown as (undefined | EscrowContractReturns['fund(pay)void'])}
    },

    /**
     * Makes a call to the EscrowContract smart contract using the `mark_verified()void` ABI method.
     *
     * Mark the delivery as verified. Only buyer can call this.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    markVerified: async (params: CallParams<EscrowContractArgs['obj']['mark_verified()void'] | EscrowContractArgs['tuple']['mark_verified()void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(EscrowContractParamsFactory.markVerified(params))
      return {...result, return: result.return as unknown as (undefined | EscrowContractReturns['mark_verified()void'])}
    },

    /**
     * Makes a call to the EscrowContract smart contract using the `confirm_delivery()void` ABI method.
     *
     * Buyer or creator confirms delivery - releases funds to supplier.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    confirmDelivery: async (params: CallParams<EscrowContractArgs['obj']['confirm_delivery()void'] | EscrowContractArgs['tuple']['confirm_delivery()void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(EscrowContractParamsFactory.confirmDelivery(params))
      return {...result, return: result.return as unknown as (undefined | EscrowContractReturns['confirm_delivery()void'])}
    },

    /**
     * Makes a call to the EscrowContract smart contract using the `get_funded()bool` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * Returns whether escrow is funded.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    getFunded: async (params: CallParams<EscrowContractArgs['obj']['get_funded()bool'] | EscrowContractArgs['tuple']['get_funded()bool']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(EscrowContractParamsFactory.getFunded(params))
      return {...result, return: result.return as unknown as (undefined | EscrowContractReturns['get_funded()bool'])}
    },

    /**
     * Makes a call to the EscrowContract smart contract using the `get_released()bool` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * Returns whether funds have been released.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    getReleased: async (params: CallParams<EscrowContractArgs['obj']['get_released()bool'] | EscrowContractArgs['tuple']['get_released()bool']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(EscrowContractParamsFactory.getReleased(params))
      return {...result, return: result.return as unknown as (undefined | EscrowContractReturns['get_released()bool'])}
    },

    /**
     * Makes a call to the EscrowContract smart contract using the `get_verified()bool` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * Returns whether delivery is verified.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    getVerified: async (params: CallParams<EscrowContractArgs['obj']['get_verified()bool'] | EscrowContractArgs['tuple']['get_verified()bool']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(EscrowContractParamsFactory.getVerified(params))
      return {...result, return: result.return as unknown as (undefined | EscrowContractReturns['get_verified()bool'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new EscrowContractClient(this.appClient.clone(params))
  }

  /**
   * Makes a readonly (simulated) call to the EscrowContract smart contract using the `get_funded()bool` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * Returns whether escrow is funded.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async getFunded(params: CallParams<EscrowContractArgs['obj']['get_funded()bool'] | EscrowContractArgs['tuple']['get_funded()bool']> = {args: []}) {
    const result = await this.appClient.send.call(EscrowContractParamsFactory.getFunded(params))
    return result.return as unknown as EscrowContractReturns['get_funded()bool']
  }

  /**
   * Makes a readonly (simulated) call to the EscrowContract smart contract using the `get_released()bool` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * Returns whether funds have been released.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async getReleased(params: CallParams<EscrowContractArgs['obj']['get_released()bool'] | EscrowContractArgs['tuple']['get_released()bool']> = {args: []}) {
    const result = await this.appClient.send.call(EscrowContractParamsFactory.getReleased(params))
    return result.return as unknown as EscrowContractReturns['get_released()bool']
  }

  /**
   * Makes a readonly (simulated) call to the EscrowContract smart contract using the `get_verified()bool` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * Returns whether delivery is verified.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async getVerified(params: CallParams<EscrowContractArgs['obj']['get_verified()bool'] | EscrowContractArgs['tuple']['get_verified()bool']> = {args: []}) {
    const result = await this.appClient.send.call(EscrowContractParamsFactory.getVerified(params))
    return result.return as unknown as EscrowContractReturns['get_verified()bool']
  }

  /**
   * Methods to access state for the current EscrowContract app
   */
  state = {
    /**
     * Methods to access global state for the current EscrowContract app
     */
    global: {
      /**
       * Get all current keyed values from global state
       */
      getAll: async (): Promise<Partial<Expand<GlobalKeysState>>> => {
        const result = await this.appClient.state.global.getAll()
        return {
          buyer: result.buyer,
          supplier: result.supplier,
          amount: result.amount,
          isFunded: result.is_funded,
          isReleased: result.is_released,
          isVerified: result.is_verified,
        }
      },
      /**
       * Get the current value of the buyer key in global state
       */
      buyer: async (): Promise<string | undefined> => { return (await this.appClient.state.global.getValue("buyer")) as string | undefined },
      /**
       * Get the current value of the supplier key in global state
       */
      supplier: async (): Promise<string | undefined> => { return (await this.appClient.state.global.getValue("supplier")) as string | undefined },
      /**
       * Get the current value of the amount key in global state
       */
      amount: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("amount")) as bigint | undefined },
      /**
       * Get the current value of the is_funded key in global state
       */
      isFunded: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("is_funded")) as bigint | undefined },
      /**
       * Get the current value of the is_released key in global state
       */
      isReleased: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("is_released")) as bigint | undefined },
      /**
       * Get the current value of the is_verified key in global state
       */
      isVerified: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("is_verified")) as bigint | undefined },
    },
  }

  public newGroup(): EscrowContractComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a fund(pay)void method call against the EscrowContract contract
       */
      fund(params: CallParams<EscrowContractArgs['obj']['fund(pay)void'] | EscrowContractArgs['tuple']['fund(pay)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.fund(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a mark_verified()void method call against the EscrowContract contract
       */
      markVerified(params: CallParams<EscrowContractArgs['obj']['mark_verified()void'] | EscrowContractArgs['tuple']['mark_verified()void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.markVerified(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a confirm_delivery()void method call against the EscrowContract contract
       */
      confirmDelivery(params: CallParams<EscrowContractArgs['obj']['confirm_delivery()void'] | EscrowContractArgs['tuple']['confirm_delivery()void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.confirmDelivery(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a get_funded()bool method call against the EscrowContract contract
       */
      getFunded(params: CallParams<EscrowContractArgs['obj']['get_funded()bool'] | EscrowContractArgs['tuple']['get_funded()bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.getFunded(params)))
        resultMappers.push((v) => client.decodeReturnValue('get_funded()bool', v))
        return this
      },
      /**
       * Add a get_released()bool method call against the EscrowContract contract
       */
      getReleased(params: CallParams<EscrowContractArgs['obj']['get_released()bool'] | EscrowContractArgs['tuple']['get_released()bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.getReleased(params)))
        resultMappers.push((v) => client.decodeReturnValue('get_released()bool', v))
        return this
      },
      /**
       * Add a get_verified()bool method call against the EscrowContract contract
       */
      getVerified(params: CallParams<EscrowContractArgs['obj']['get_verified()bool'] | EscrowContractArgs['tuple']['get_verified()bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.getVerified(params)))
        resultMappers.push((v) => client.decodeReturnValue('get_verified()bool', v))
        return this
      },
      /**
       * Add a clear state call to the EscrowContract contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as EscrowContractComposer
  }
}
export type EscrowContractComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the fund(pay)void ABI method.
   *
   * Buyer funds the escrow with the agreed amount.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  fund(params?: CallParams<EscrowContractArgs['obj']['fund(pay)void'] | EscrowContractArgs['tuple']['fund(pay)void']>): EscrowContractComposer<[...TReturns, EscrowContractReturns['fund(pay)void'] | undefined]>

  /**
   * Calls the mark_verified()void ABI method.
   *
   * Mark the delivery as verified. Only buyer can call this.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  markVerified(params?: CallParams<EscrowContractArgs['obj']['mark_verified()void'] | EscrowContractArgs['tuple']['mark_verified()void']>): EscrowContractComposer<[...TReturns, EscrowContractReturns['mark_verified()void'] | undefined]>

  /**
   * Calls the confirm_delivery()void ABI method.
   *
   * Buyer or creator confirms delivery - releases funds to supplier.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  confirmDelivery(params?: CallParams<EscrowContractArgs['obj']['confirm_delivery()void'] | EscrowContractArgs['tuple']['confirm_delivery()void']>): EscrowContractComposer<[...TReturns, EscrowContractReturns['confirm_delivery()void'] | undefined]>

  /**
   * Calls the get_funded()bool ABI method.
   *
   * Returns whether escrow is funded.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  getFunded(params?: CallParams<EscrowContractArgs['obj']['get_funded()bool'] | EscrowContractArgs['tuple']['get_funded()bool']>): EscrowContractComposer<[...TReturns, EscrowContractReturns['get_funded()bool'] | undefined]>

  /**
   * Calls the get_released()bool ABI method.
   *
   * Returns whether funds have been released.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  getReleased(params?: CallParams<EscrowContractArgs['obj']['get_released()bool'] | EscrowContractArgs['tuple']['get_released()bool']>): EscrowContractComposer<[...TReturns, EscrowContractReturns['get_released()bool'] | undefined]>

  /**
   * Calls the get_verified()bool ABI method.
   *
   * Returns whether delivery is verified.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  getVerified(params?: CallParams<EscrowContractArgs['obj']['get_verified()bool'] | EscrowContractArgs['tuple']['get_verified()bool']>): EscrowContractComposer<[...TReturns, EscrowContractReturns['get_verified()bool'] | undefined]>

  /**
   * Makes a clear_state call to an existing instance of the EscrowContract smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): EscrowContractComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): EscrowContractComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<EscrowContractComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<EscrowContractComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<EscrowContractComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<EscrowContractComposerResults<TReturns>>
}
export type EscrowContractComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

