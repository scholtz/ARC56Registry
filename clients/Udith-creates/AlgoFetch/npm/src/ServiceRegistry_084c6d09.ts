/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"ServiceRegistry","structs":{"ServiceRecord":[{"name":"price","type":"uint64"},{"name":"provider","type":"address"}]},"methods":[{"name":"register_service","args":[{"type":"string","name":"id"},{"type":"uint64","name":"price"},{"type":"address","name":"provider"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Register a new service or update an existing one.","events":[],"recommendations":{}},{"name":"get_service","args":[{"type":"string","name":"id"}],"returns":{"type":"(uint64,address)","struct":"ServiceRecord"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"desc":"Fetch details of a specific service directly from the state registry.","events":[],"recommendations":{}}],"arcs":[22,28],"networks":{},"state":{"schema":{"global":{"ints":0,"bytes":0},"local":{"ints":0,"bytes":0}},"keys":{"global":{},"local":{},"box":{}},"maps":{"global":{},"local":{},"box":{"services":{"keyType":"AVMString","valueType":"ServiceRecord","prefix":"c2VydmljZXM="}}}},"bareActions":{"create":["NoOp"],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[96],"errorMessage":"Address length is 32 bytes"},{"pc":[129],"errorMessage":"Service not found in registry"},{"pc":[65,111],"errorMessage":"invalid array length header"},{"pc":[72,118],"errorMessage":"invalid number of bytes for arc4.dynamic_array<arc4.uint8>"},{"pc":[93],"errorMessage":"invalid number of bytes for arc4.static_array<arc4.uint8, 32>"},{"pc":[84],"errorMessage":"invalid number of bytes for arc4.uint64"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"events":[]} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


// Type definitions for ARC-56 structs

export type ServiceRecord = {
  price: bigint,
  provider: string
}


/**
 * Converts the ABI tuple representation of a ServiceRecord to the struct representation
 */
export function ServiceRecordFromTuple(abiTuple: [bigint, string]) {
  return getABIStructFromABITuple(abiTuple, APP_SPEC.structs.ServiceRecord, APP_SPEC.structs) as ServiceRecord
}

/**
 * The argument types for the ServiceRegistry contract
 */
export type ServiceRegistryArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'register_service(string,uint64,address)void': {
      id: string
      price: bigint | number
      provider: string
    }
    'get_service(string)(uint64,address)': {
      id: string
    }
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'register_service(string,uint64,address)void': [id: string, price: bigint | number, provider: string]
    'get_service(string)(uint64,address)': [id: string]
  }
}

/**
 * The return type for each method
 */
export type ServiceRegistryReturns = {
  'register_service(string,uint64,address)void': void
  'get_service(string)(uint64,address)': ServiceRecord
}

/**
 * Defines the types of available calls and state of the ServiceRegistry smart contract.
 */
export type ServiceRegistryTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'register_service(string,uint64,address)void' | 'register_service', {
      argsObj: ServiceRegistryArgs['obj']['register_service(string,uint64,address)void']
      argsTuple: ServiceRegistryArgs['tuple']['register_service(string,uint64,address)void']
      returns: ServiceRegistryReturns['register_service(string,uint64,address)void']
    }>
    & Record<'get_service(string)(uint64,address)' | 'get_service', {
      argsObj: ServiceRegistryArgs['obj']['get_service(string)(uint64,address)']
      argsTuple: ServiceRegistryArgs['tuple']['get_service(string)(uint64,address)']
      returns: ServiceRegistryReturns['get_service(string)(uint64,address)']
    }>
  /**
   * Defines the shape of the state of the application.
   */
  state: {
    box: {
      keys: {}
      maps: {
        services: Map<string, ServiceRecord>
      }
    }
  }
}

/**
 * Defines the possible abi call signatures.
 */
export type ServiceRegistrySignatures = keyof ServiceRegistryTypes['methods']
/**
 * Defines the possible abi call signatures for methods that return a non-void value.
 */
export type ServiceRegistryNonVoidMethodSignatures = keyof ServiceRegistryTypes['methods'] extends infer T ? T extends keyof ServiceRegistryTypes['methods'] ? MethodReturn<T> extends void ? never : T  : never : never
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the ServiceRegistry smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends ServiceRegistrySignatures> = ServiceRegistryTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the ServiceRegistry smart contract to the method's return type
 */
export type MethodReturn<TSignature extends ServiceRegistrySignatures> = ServiceRegistryTypes['methods'][TSignature]['returns']

/**
 * Defines the shape of the keyed box state of the application.
 */
export type BoxKeysState = ServiceRegistryTypes['state']['box']['keys']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the ServiceRegistry smart contract
 */
export abstract class ServiceRegistryParamsFactory {
  /**
   * Constructs a no op call for the register_service(string,uint64,address)void ABI method
   *
   * Register a new service or update an existing one.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static registerService(params: CallParams<ServiceRegistryArgs['obj']['register_service(string,uint64,address)void'] | ServiceRegistryArgs['tuple']['register_service(string,uint64,address)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'register_service(string,uint64,address)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.id, params.args.price, params.args.provider],
    }
  }
  /**
   * Constructs a no op call for the get_service(string)(uint64,address) ABI method
   *
   * Fetch details of a specific service directly from the state registry.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static getService(params: CallParams<ServiceRegistryArgs['obj']['get_service(string)(uint64,address)'] | ServiceRegistryArgs['tuple']['get_service(string)(uint64,address)']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'get_service(string)(uint64,address)' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.id],
    }
  }
}

/**
 * A client to make calls to the ServiceRegistry smart contract
 */
export class ServiceRegistryClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `ServiceRegistryClient`
   *
   * @param appClient An `AppClient` instance which has been created with the ServiceRegistry app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `ServiceRegistryClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Checks for decode errors on the given return value and maps the return value to the return type for the given method
   * @returns The typed return value or undefined if there was no value
   */
  decodeReturnValue<TSignature extends ServiceRegistryNonVoidMethodSignatures>(method: TSignature, returnValue: ABIReturn | undefined) {
    return returnValue !== undefined ? getArc56ReturnValue<MethodReturn<TSignature>>(returnValue, this.appClient.getABIMethod(method), APP_SPEC.structs) : undefined
  }

  /**
   * Returns a new `ServiceRegistryClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<ServiceRegistryClient> {
    return new ServiceRegistryClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `ServiceRegistryClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<ServiceRegistryClient> {
    return new ServiceRegistryClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Makes a clear_state call to an existing instance of the ServiceRegistry smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the ServiceRegistry smart contract using the `register_service(string,uint64,address)void` ABI method.
     *
     * Register a new service or update an existing one.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    registerService: (params: CallParams<ServiceRegistryArgs['obj']['register_service(string,uint64,address)void'] | ServiceRegistryArgs['tuple']['register_service(string,uint64,address)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(ServiceRegistryParamsFactory.registerService(params))
    },

    /**
     * Makes a call to the ServiceRegistry smart contract using the `get_service(string)(uint64,address)` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * Fetch details of a specific service directly from the state registry.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    getService: (params: CallParams<ServiceRegistryArgs['obj']['get_service(string)(uint64,address)'] | ServiceRegistryArgs['tuple']['get_service(string)(uint64,address)']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(ServiceRegistryParamsFactory.getService(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Makes a clear_state call to an existing instance of the ServiceRegistry smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the ServiceRegistry smart contract using the `register_service(string,uint64,address)void` ABI method.
     *
     * Register a new service or update an existing one.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    registerService: (params: CallParams<ServiceRegistryArgs['obj']['register_service(string,uint64,address)void'] | ServiceRegistryArgs['tuple']['register_service(string,uint64,address)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(ServiceRegistryParamsFactory.registerService(params))
    },

    /**
     * Makes a call to the ServiceRegistry smart contract using the `get_service(string)(uint64,address)` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * Fetch details of a specific service directly from the state registry.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    getService: (params: CallParams<ServiceRegistryArgs['obj']['get_service(string)(uint64,address)'] | ServiceRegistryArgs['tuple']['get_service(string)(uint64,address)']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(ServiceRegistryParamsFactory.getService(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Makes a clear_state call to an existing instance of the ServiceRegistry smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the ServiceRegistry smart contract using the `register_service(string,uint64,address)void` ABI method.
     *
     * Register a new service or update an existing one.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    registerService: async (params: CallParams<ServiceRegistryArgs['obj']['register_service(string,uint64,address)void'] | ServiceRegistryArgs['tuple']['register_service(string,uint64,address)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(ServiceRegistryParamsFactory.registerService(params))
      return {...result, return: result.return as unknown as (undefined | ServiceRegistryReturns['register_service(string,uint64,address)void'])}
    },

    /**
     * Makes a call to the ServiceRegistry smart contract using the `get_service(string)(uint64,address)` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * Fetch details of a specific service directly from the state registry.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    getService: async (params: CallParams<ServiceRegistryArgs['obj']['get_service(string)(uint64,address)'] | ServiceRegistryArgs['tuple']['get_service(string)(uint64,address)']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(ServiceRegistryParamsFactory.getService(params))
      return {...result, return: result.return as unknown as (undefined | ServiceRegistryReturns['get_service(string)(uint64,address)'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new ServiceRegistryClient(this.appClient.clone(params))
  }

  /**
   * Makes a readonly (simulated) call to the ServiceRegistry smart contract using the `get_service(string)(uint64,address)` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * Fetch details of a specific service directly from the state registry.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async getService(params: CallParams<ServiceRegistryArgs['obj']['get_service(string)(uint64,address)'] | ServiceRegistryArgs['tuple']['get_service(string)(uint64,address)']>) {
    const result = await this.appClient.send.call(ServiceRegistryParamsFactory.getService(params))
    return result.return as unknown as ServiceRegistryReturns['get_service(string)(uint64,address)']
  }

  /**
   * Methods to access state for the current ServiceRegistry app
   */
  state = {
    /**
     * Methods to access box state for the current ServiceRegistry app
     */
    box: {
      /**
       * Get all current keyed values from box state
       */
      getAll: async (): Promise<Partial<Expand<BoxKeysState>>> => {
        const result = await this.appClient.state.box.getAll()
        return {
        }
      },
      /**
       * Get values from the services map in box state
       */
      services: {
        /**
         * Get all current values of the services map in box state
         */
        getMap: async (): Promise<Map<string, ServiceRecord>> => { return (await this.appClient.state.box.getMap("services")) as Map<string, ServiceRecord> },
        /**
         * Get a current value of the services map by key from box state
         */
        value: async (key: string): Promise<ServiceRecord | undefined> => { return await this.appClient.state.box.getMapValue("services", key) as ServiceRecord | undefined },
      },
    },
  }

  public newGroup(): ServiceRegistryComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a register_service(string,uint64,address)void method call against the ServiceRegistry contract
       */
      registerService(params: CallParams<ServiceRegistryArgs['obj']['register_service(string,uint64,address)void'] | ServiceRegistryArgs['tuple']['register_service(string,uint64,address)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.registerService(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a get_service(string)(uint64,address) method call against the ServiceRegistry contract
       */
      getService(params: CallParams<ServiceRegistryArgs['obj']['get_service(string)(uint64,address)'] | ServiceRegistryArgs['tuple']['get_service(string)(uint64,address)']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.getService(params)))
        resultMappers.push((v) => client.decodeReturnValue('get_service(string)(uint64,address)', v))
        return this
      },
      /**
       * Add a clear state call to the ServiceRegistry contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as ServiceRegistryComposer
  }
}
export type ServiceRegistryComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the register_service(string,uint64,address)void ABI method.
   *
   * Register a new service or update an existing one.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  registerService(params?: CallParams<ServiceRegistryArgs['obj']['register_service(string,uint64,address)void'] | ServiceRegistryArgs['tuple']['register_service(string,uint64,address)void']>): ServiceRegistryComposer<[...TReturns, ServiceRegistryReturns['register_service(string,uint64,address)void'] | undefined]>

  /**
   * Calls the get_service(string)(uint64,address) ABI method.
   *
   * Fetch details of a specific service directly from the state registry.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  getService(params?: CallParams<ServiceRegistryArgs['obj']['get_service(string)(uint64,address)'] | ServiceRegistryArgs['tuple']['get_service(string)(uint64,address)']>): ServiceRegistryComposer<[...TReturns, ServiceRegistryReturns['get_service(string)(uint64,address)'] | undefined]>

  /**
   * Makes a clear_state call to an existing instance of the ServiceRegistry smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): ServiceRegistryComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): ServiceRegistryComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<ServiceRegistryComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<ServiceRegistryComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<ServiceRegistryComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<ServiceRegistryComposerResults<TReturns>>
}
export type ServiceRegistryComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

