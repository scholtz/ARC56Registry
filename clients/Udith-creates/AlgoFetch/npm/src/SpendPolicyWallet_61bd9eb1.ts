/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"SpendPolicyWallet","structs":{},"methods":[{"name":"create","args":[{"type":"uint64","name":"hard_cap"}],"returns":{"type":"void"},"actions":{"create":["NoOp"],"call":[]},"readonly":false,"desc":"Initialize the smart wallet and set the spend policy hard cap.","events":[],"recommendations":{}},{"name":"execute_payment","args":[{"type":"address","name":"recipient"},{"type":"uint64","name":"amount"},{"type":"byte[]","name":"note"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Execute a payment subject to ARC-58 spend policies.","events":[],"recommendations":{}},{"name":"reset_spend","args":[],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Reset the spend limit (manual trigger for this demo).","events":[],"recommendations":{}}],"arcs":[22,28],"networks":{},"state":{"schema":{"global":{"ints":2,"bytes":1},"local":{"ints":0,"bytes":0}},"keys":{"global":{"owner":{"keyType":"AVMString","valueType":"address","key":"b3duZXI=","desc":"Owner of the abstracted wallet"},"hard_cap":{"keyType":"AVMString","valueType":"AVMUint64","key":"aGFyZF9jYXA=","desc":"Maximum allowed spend limit"},"daily_spent":{"keyType":"AVMString","valueType":"AVMUint64","key":"c3BlbnQ=","desc":"Amount spent so far"}},"local":{},"box":{}},"maps":{"global":{},"local":{},"box":{}}},"bareActions":{"create":[],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[152],"errorMessage":"AVM Logic Error: Hard cap exceeded for the day"},{"pc":[179],"errorMessage":"Only owner can reset"},{"pc":[137],"errorMessage":"Only owner can spend"},{"pc":[141],"errorMessage":"check self.daily_spent exists"},{"pc":[148],"errorMessage":"check self.hard_cap exists"},{"pc":[135,177],"errorMessage":"check self.owner exists"},{"pc":[118],"errorMessage":"invalid array length header"},{"pc":[126],"errorMessage":"invalid number of bytes for arc4.dynamic_array<arc4.uint8>"},{"pc":[103],"errorMessage":"invalid number of bytes for arc4.static_array<arc4.uint8, 32>"},{"pc":[81,111],"errorMessage":"invalid number of bytes for arc4.uint64"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"events":[]} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


/**
 * The argument types for the SpendPolicyWallet contract
 */
export type SpendPolicyWalletArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'create(uint64)void': {
      hardCap: bigint | number
    }
    'execute_payment(address,uint64,byte[])void': {
      recipient: string
      amount: bigint | number
      note: Uint8Array
    }
    'reset_spend()void': Record<string, never>
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'create(uint64)void': [hardCap: bigint | number]
    'execute_payment(address,uint64,byte[])void': [recipient: string, amount: bigint | number, note: Uint8Array]
    'reset_spend()void': []
  }
}

/**
 * The return type for each method
 */
export type SpendPolicyWalletReturns = {
  'create(uint64)void': void
  'execute_payment(address,uint64,byte[])void': void
  'reset_spend()void': void
}

/**
 * Defines the types of available calls and state of the SpendPolicyWallet smart contract.
 */
export type SpendPolicyWalletTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'create(uint64)void' | 'create', {
      argsObj: SpendPolicyWalletArgs['obj']['create(uint64)void']
      argsTuple: SpendPolicyWalletArgs['tuple']['create(uint64)void']
      returns: SpendPolicyWalletReturns['create(uint64)void']
    }>
    & Record<'execute_payment(address,uint64,byte[])void' | 'execute_payment', {
      argsObj: SpendPolicyWalletArgs['obj']['execute_payment(address,uint64,byte[])void']
      argsTuple: SpendPolicyWalletArgs['tuple']['execute_payment(address,uint64,byte[])void']
      returns: SpendPolicyWalletReturns['execute_payment(address,uint64,byte[])void']
    }>
    & Record<'reset_spend()void' | 'reset_spend', {
      argsObj: SpendPolicyWalletArgs['obj']['reset_spend()void']
      argsTuple: SpendPolicyWalletArgs['tuple']['reset_spend()void']
      returns: SpendPolicyWalletReturns['reset_spend()void']
    }>
  /**
   * Defines the shape of the state of the application.
   */
  state: {
    global: {
      keys: {
        /**
         * Owner of the abstracted wallet
         */
        owner: string
        /**
         * Maximum allowed spend limit
         */
        hardCap: bigint
        /**
         * Amount spent so far
         */
        dailySpent: bigint
      }
      maps: {}
    }
  }
}

/**
 * Defines the possible abi call signatures.
 */
export type SpendPolicyWalletSignatures = keyof SpendPolicyWalletTypes['methods']
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the SpendPolicyWallet smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends SpendPolicyWalletSignatures> = SpendPolicyWalletTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the SpendPolicyWallet smart contract to the method's return type
 */
export type MethodReturn<TSignature extends SpendPolicyWalletSignatures> = SpendPolicyWalletTypes['methods'][TSignature]['returns']

/**
 * Defines the shape of the keyed global state of the application.
 */
export type GlobalKeysState = SpendPolicyWalletTypes['state']['global']['keys']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the SpendPolicyWallet smart contract
 */
export abstract class SpendPolicyWalletParamsFactory {
  /**
   * Constructs a no op call for the execute_payment(address,uint64,byte[])void ABI method
   *
   * Execute a payment subject to ARC-58 spend policies.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static executePayment(params: CallParams<SpendPolicyWalletArgs['obj']['execute_payment(address,uint64,byte[])void'] | SpendPolicyWalletArgs['tuple']['execute_payment(address,uint64,byte[])void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'execute_payment(address,uint64,byte[])void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.recipient, params.args.amount, params.args.note],
    }
  }
  /**
   * Constructs a no op call for the reset_spend()void ABI method
   *
   * Reset the spend limit (manual trigger for this demo).
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static resetSpend(params: CallParams<SpendPolicyWalletArgs['obj']['reset_spend()void'] | SpendPolicyWalletArgs['tuple']['reset_spend()void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'reset_spend()void' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
}

/**
 * A client to make calls to the SpendPolicyWallet smart contract
 */
export class SpendPolicyWalletClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `SpendPolicyWalletClient`
   *
   * @param appClient An `AppClient` instance which has been created with the SpendPolicyWallet app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `SpendPolicyWalletClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Returns a new `SpendPolicyWalletClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<SpendPolicyWalletClient> {
    return new SpendPolicyWalletClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `SpendPolicyWalletClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<SpendPolicyWalletClient> {
    return new SpendPolicyWalletClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Makes a clear_state call to an existing instance of the SpendPolicyWallet smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the SpendPolicyWallet smart contract using the `execute_payment(address,uint64,byte[])void` ABI method.
     *
     * Execute a payment subject to ARC-58 spend policies.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    executePayment: (params: CallParams<SpendPolicyWalletArgs['obj']['execute_payment(address,uint64,byte[])void'] | SpendPolicyWalletArgs['tuple']['execute_payment(address,uint64,byte[])void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(SpendPolicyWalletParamsFactory.executePayment(params))
    },

    /**
     * Makes a call to the SpendPolicyWallet smart contract using the `reset_spend()void` ABI method.
     *
     * Reset the spend limit (manual trigger for this demo).
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    resetSpend: (params: CallParams<SpendPolicyWalletArgs['obj']['reset_spend()void'] | SpendPolicyWalletArgs['tuple']['reset_spend()void']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(SpendPolicyWalletParamsFactory.resetSpend(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Makes a clear_state call to an existing instance of the SpendPolicyWallet smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the SpendPolicyWallet smart contract using the `execute_payment(address,uint64,byte[])void` ABI method.
     *
     * Execute a payment subject to ARC-58 spend policies.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    executePayment: (params: CallParams<SpendPolicyWalletArgs['obj']['execute_payment(address,uint64,byte[])void'] | SpendPolicyWalletArgs['tuple']['execute_payment(address,uint64,byte[])void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(SpendPolicyWalletParamsFactory.executePayment(params))
    },

    /**
     * Makes a call to the SpendPolicyWallet smart contract using the `reset_spend()void` ABI method.
     *
     * Reset the spend limit (manual trigger for this demo).
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    resetSpend: (params: CallParams<SpendPolicyWalletArgs['obj']['reset_spend()void'] | SpendPolicyWalletArgs['tuple']['reset_spend()void']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(SpendPolicyWalletParamsFactory.resetSpend(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Makes a clear_state call to an existing instance of the SpendPolicyWallet smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the SpendPolicyWallet smart contract using the `execute_payment(address,uint64,byte[])void` ABI method.
     *
     * Execute a payment subject to ARC-58 spend policies.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    executePayment: async (params: CallParams<SpendPolicyWalletArgs['obj']['execute_payment(address,uint64,byte[])void'] | SpendPolicyWalletArgs['tuple']['execute_payment(address,uint64,byte[])void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(SpendPolicyWalletParamsFactory.executePayment(params))
      return {...result, return: result.return as unknown as (undefined | SpendPolicyWalletReturns['execute_payment(address,uint64,byte[])void'])}
    },

    /**
     * Makes a call to the SpendPolicyWallet smart contract using the `reset_spend()void` ABI method.
     *
     * Reset the spend limit (manual trigger for this demo).
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    resetSpend: async (params: CallParams<SpendPolicyWalletArgs['obj']['reset_spend()void'] | SpendPolicyWalletArgs['tuple']['reset_spend()void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(SpendPolicyWalletParamsFactory.resetSpend(params))
      return {...result, return: result.return as unknown as (undefined | SpendPolicyWalletReturns['reset_spend()void'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new SpendPolicyWalletClient(this.appClient.clone(params))
  }

  /**
   * Methods to access state for the current SpendPolicyWallet app
   */
  state = {
    /**
     * Methods to access global state for the current SpendPolicyWallet app
     */
    global: {
      /**
       * Get all current keyed values from global state
       */
      getAll: async (): Promise<Partial<Expand<GlobalKeysState>>> => {
        const result = await this.appClient.state.global.getAll()
        return {
          owner: result.owner,
          hardCap: result.hard_cap,
          dailySpent: result.daily_spent,
        }
      },
      /**
       * Get the current value of the owner key in global state
       */
      owner: async (): Promise<string | undefined> => { return (await this.appClient.state.global.getValue("owner")) as string | undefined },
      /**
       * Get the current value of the hard_cap key in global state
       */
      hardCap: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("hard_cap")) as bigint | undefined },
      /**
       * Get the current value of the daily_spent key in global state
       */
      dailySpent: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("daily_spent")) as bigint | undefined },
    },
  }

  public newGroup(): SpendPolicyWalletComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a execute_payment(address,uint64,byte[])void method call against the SpendPolicyWallet contract
       */
      executePayment(params: CallParams<SpendPolicyWalletArgs['obj']['execute_payment(address,uint64,byte[])void'] | SpendPolicyWalletArgs['tuple']['execute_payment(address,uint64,byte[])void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.executePayment(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a reset_spend()void method call against the SpendPolicyWallet contract
       */
      resetSpend(params: CallParams<SpendPolicyWalletArgs['obj']['reset_spend()void'] | SpendPolicyWalletArgs['tuple']['reset_spend()void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.resetSpend(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a clear state call to the SpendPolicyWallet contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as SpendPolicyWalletComposer
  }
}
export type SpendPolicyWalletComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the execute_payment(address,uint64,byte[])void ABI method.
   *
   * Execute a payment subject to ARC-58 spend policies.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  executePayment(params?: CallParams<SpendPolicyWalletArgs['obj']['execute_payment(address,uint64,byte[])void'] | SpendPolicyWalletArgs['tuple']['execute_payment(address,uint64,byte[])void']>): SpendPolicyWalletComposer<[...TReturns, SpendPolicyWalletReturns['execute_payment(address,uint64,byte[])void'] | undefined]>

  /**
   * Calls the reset_spend()void ABI method.
   *
   * Reset the spend limit (manual trigger for this demo).
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  resetSpend(params?: CallParams<SpendPolicyWalletArgs['obj']['reset_spend()void'] | SpendPolicyWalletArgs['tuple']['reset_spend()void']>): SpendPolicyWalletComposer<[...TReturns, SpendPolicyWalletReturns['reset_spend()void'] | undefined]>

  /**
   * Makes a clear_state call to an existing instance of the SpendPolicyWallet smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): SpendPolicyWalletComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): SpendPolicyWalletComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<SpendPolicyWalletComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<SpendPolicyWalletComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<SpendPolicyWalletComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<SpendPolicyWalletComposerResults<TReturns>>
}
export type SpendPolicyWalletComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

