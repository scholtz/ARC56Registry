/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"CampusPay","structs":{},"methods":[{"name":"greet","args":[],"returns":{"type":"string"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}},{"name":"register_transaction","args":[{"type":"uint64","name":"amount"}],"returns":{"type":"uint64"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}},{"name":"get_stats","args":[],"returns":{"type":"(uint64,uint64)"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}},{"name":"pay_and_log","args":[{"type":"pay","name":"payment"}],"returns":{"type":"string"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}}],"arcs":[22,28],"networks":{},"state":{"schema":{"global":{"ints":2,"bytes":0},"local":{"ints":0,"bytes":0}},"keys":{"global":{"total_transactions":{"keyType":"AVMString","valueType":"AVMUint64","key":"dG90YWxfdHJhbnNhY3Rpb25z"},"total_volume":{"keyType":"AVMString","valueType":"AVMUint64","key":"dG90YWxfdm9sdW1l"}},"local":{},"box":{}},"maps":{"global":{},"local":{},"box":{}}},"bareActions":{"create":["NoOp"],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[256],"errorMessage":"Amount must be greater than 0"},{"pc":[199,216,227,260],"errorMessage":"check self.total_transactions exists"},{"pc":[208,231,269],"errorMessage":"check self.total_volume exists"},{"pc":[194],"errorMessage":"invalid number of bytes for arc4.uint64"},{"pc":[252],"errorMessage":"transaction type is pay"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"events":[]} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


/**
 * The argument types for the CampusPay contract
 */
export type CampusPayArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'greet()string': Record<string, never>
    'register_transaction(uint64)uint64': {
      amount: bigint | number
    }
    'get_stats()(uint64,uint64)': Record<string, never>
    'pay_and_log(pay)string': {
      payment: AppMethodCallTransactionArgument
    }
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'greet()string': []
    'register_transaction(uint64)uint64': [amount: bigint | number]
    'get_stats()(uint64,uint64)': []
    'pay_and_log(pay)string': [payment: AppMethodCallTransactionArgument]
  }
}

/**
 * The return type for each method
 */
export type CampusPayReturns = {
  'greet()string': string
  'register_transaction(uint64)uint64': bigint
  'get_stats()(uint64,uint64)': [bigint, bigint]
  'pay_and_log(pay)string': string
}

/**
 * Defines the types of available calls and state of the CampusPay smart contract.
 */
export type CampusPayTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'greet()string' | 'greet', {
      argsObj: CampusPayArgs['obj']['greet()string']
      argsTuple: CampusPayArgs['tuple']['greet()string']
      returns: CampusPayReturns['greet()string']
    }>
    & Record<'register_transaction(uint64)uint64' | 'register_transaction', {
      argsObj: CampusPayArgs['obj']['register_transaction(uint64)uint64']
      argsTuple: CampusPayArgs['tuple']['register_transaction(uint64)uint64']
      returns: CampusPayReturns['register_transaction(uint64)uint64']
    }>
    & Record<'get_stats()(uint64,uint64)' | 'get_stats', {
      argsObj: CampusPayArgs['obj']['get_stats()(uint64,uint64)']
      argsTuple: CampusPayArgs['tuple']['get_stats()(uint64,uint64)']
      returns: CampusPayReturns['get_stats()(uint64,uint64)']
    }>
    & Record<'pay_and_log(pay)string' | 'pay_and_log', {
      argsObj: CampusPayArgs['obj']['pay_and_log(pay)string']
      argsTuple: CampusPayArgs['tuple']['pay_and_log(pay)string']
      returns: CampusPayReturns['pay_and_log(pay)string']
    }>
  /**
   * Defines the shape of the state of the application.
   */
  state: {
    global: {
      keys: {
        totalTransactions: bigint
        totalVolume: bigint
      }
      maps: {}
    }
  }
}

/**
 * Defines the possible abi call signatures.
 */
export type CampusPaySignatures = keyof CampusPayTypes['methods']
/**
 * Defines the possible abi call signatures for methods that return a non-void value.
 */
export type CampusPayNonVoidMethodSignatures = keyof CampusPayTypes['methods'] extends infer T ? T extends keyof CampusPayTypes['methods'] ? MethodReturn<T> extends void ? never : T  : never : never
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the CampusPay smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends CampusPaySignatures> = CampusPayTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the CampusPay smart contract to the method's return type
 */
export type MethodReturn<TSignature extends CampusPaySignatures> = CampusPayTypes['methods'][TSignature]['returns']

/**
 * Defines the shape of the keyed global state of the application.
 */
export type GlobalKeysState = CampusPayTypes['state']['global']['keys']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the CampusPay smart contract
 */
export abstract class CampusPayParamsFactory {
  /**
   * Constructs a no op call for the greet()string ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static greet(params: CallParams<CampusPayArgs['obj']['greet()string'] | CampusPayArgs['tuple']['greet()string']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'greet()string' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the register_transaction(uint64)uint64 ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static registerTransaction(params: CallParams<CampusPayArgs['obj']['register_transaction(uint64)uint64'] | CampusPayArgs['tuple']['register_transaction(uint64)uint64']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'register_transaction(uint64)uint64' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.amount],
    }
  }
  /**
   * Constructs a no op call for the get_stats()(uint64,uint64) ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static getStats(params: CallParams<CampusPayArgs['obj']['get_stats()(uint64,uint64)'] | CampusPayArgs['tuple']['get_stats()(uint64,uint64)']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'get_stats()(uint64,uint64)' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the pay_and_log(pay)string ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static payAndLog(params: CallParams<CampusPayArgs['obj']['pay_and_log(pay)string'] | CampusPayArgs['tuple']['pay_and_log(pay)string']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'pay_and_log(pay)string' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.payment],
    }
  }
}

/**
 * A client to make calls to the CampusPay smart contract
 */
export class CampusPayClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `CampusPayClient`
   *
   * @param appClient An `AppClient` instance which has been created with the CampusPay app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `CampusPayClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Checks for decode errors on the given return value and maps the return value to the return type for the given method
   * @returns The typed return value or undefined if there was no value
   */
  decodeReturnValue<TSignature extends CampusPayNonVoidMethodSignatures>(method: TSignature, returnValue: ABIReturn | undefined) {
    return returnValue !== undefined ? getArc56ReturnValue<MethodReturn<TSignature>>(returnValue, this.appClient.getABIMethod(method), APP_SPEC.structs) : undefined
  }

  /**
   * Returns a new `CampusPayClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<CampusPayClient> {
    return new CampusPayClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `CampusPayClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<CampusPayClient> {
    return new CampusPayClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Makes a clear_state call to an existing instance of the CampusPay smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the CampusPay smart contract using the `greet()string` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    greet: (params: CallParams<CampusPayArgs['obj']['greet()string'] | CampusPayArgs['tuple']['greet()string']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(CampusPayParamsFactory.greet(params))
    },

    /**
     * Makes a call to the CampusPay smart contract using the `register_transaction(uint64)uint64` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    registerTransaction: (params: CallParams<CampusPayArgs['obj']['register_transaction(uint64)uint64'] | CampusPayArgs['tuple']['register_transaction(uint64)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(CampusPayParamsFactory.registerTransaction(params))
    },

    /**
     * Makes a call to the CampusPay smart contract using the `get_stats()(uint64,uint64)` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    getStats: (params: CallParams<CampusPayArgs['obj']['get_stats()(uint64,uint64)'] | CampusPayArgs['tuple']['get_stats()(uint64,uint64)']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(CampusPayParamsFactory.getStats(params))
    },

    /**
     * Makes a call to the CampusPay smart contract using the `pay_and_log(pay)string` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    payAndLog: (params: CallParams<CampusPayArgs['obj']['pay_and_log(pay)string'] | CampusPayArgs['tuple']['pay_and_log(pay)string']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(CampusPayParamsFactory.payAndLog(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Makes a clear_state call to an existing instance of the CampusPay smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the CampusPay smart contract using the `greet()string` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    greet: (params: CallParams<CampusPayArgs['obj']['greet()string'] | CampusPayArgs['tuple']['greet()string']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(CampusPayParamsFactory.greet(params))
    },

    /**
     * Makes a call to the CampusPay smart contract using the `register_transaction(uint64)uint64` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    registerTransaction: (params: CallParams<CampusPayArgs['obj']['register_transaction(uint64)uint64'] | CampusPayArgs['tuple']['register_transaction(uint64)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(CampusPayParamsFactory.registerTransaction(params))
    },

    /**
     * Makes a call to the CampusPay smart contract using the `get_stats()(uint64,uint64)` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    getStats: (params: CallParams<CampusPayArgs['obj']['get_stats()(uint64,uint64)'] | CampusPayArgs['tuple']['get_stats()(uint64,uint64)']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(CampusPayParamsFactory.getStats(params))
    },

    /**
     * Makes a call to the CampusPay smart contract using the `pay_and_log(pay)string` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    payAndLog: (params: CallParams<CampusPayArgs['obj']['pay_and_log(pay)string'] | CampusPayArgs['tuple']['pay_and_log(pay)string']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(CampusPayParamsFactory.payAndLog(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Makes a clear_state call to an existing instance of the CampusPay smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the CampusPay smart contract using the `greet()string` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    greet: async (params: CallParams<CampusPayArgs['obj']['greet()string'] | CampusPayArgs['tuple']['greet()string']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(CampusPayParamsFactory.greet(params))
      return {...result, return: result.return as unknown as (undefined | CampusPayReturns['greet()string'])}
    },

    /**
     * Makes a call to the CampusPay smart contract using the `register_transaction(uint64)uint64` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    registerTransaction: async (params: CallParams<CampusPayArgs['obj']['register_transaction(uint64)uint64'] | CampusPayArgs['tuple']['register_transaction(uint64)uint64']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(CampusPayParamsFactory.registerTransaction(params))
      return {...result, return: result.return as unknown as (undefined | CampusPayReturns['register_transaction(uint64)uint64'])}
    },

    /**
     * Makes a call to the CampusPay smart contract using the `get_stats()(uint64,uint64)` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    getStats: async (params: CallParams<CampusPayArgs['obj']['get_stats()(uint64,uint64)'] | CampusPayArgs['tuple']['get_stats()(uint64,uint64)']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(CampusPayParamsFactory.getStats(params))
      return {...result, return: result.return as unknown as (undefined | CampusPayReturns['get_stats()(uint64,uint64)'])}
    },

    /**
     * Makes a call to the CampusPay smart contract using the `pay_and_log(pay)string` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    payAndLog: async (params: CallParams<CampusPayArgs['obj']['pay_and_log(pay)string'] | CampusPayArgs['tuple']['pay_and_log(pay)string']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(CampusPayParamsFactory.payAndLog(params))
      return {...result, return: result.return as unknown as (undefined | CampusPayReturns['pay_and_log(pay)string'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new CampusPayClient(this.appClient.clone(params))
  }

  /**
   * Methods to access state for the current CampusPay app
   */
  state = {
    /**
     * Methods to access global state for the current CampusPay app
     */
    global: {
      /**
       * Get all current keyed values from global state
       */
      getAll: async (): Promise<Partial<Expand<GlobalKeysState>>> => {
        const result = await this.appClient.state.global.getAll()
        return {
          totalTransactions: result.total_transactions,
          totalVolume: result.total_volume,
        }
      },
      /**
       * Get the current value of the total_transactions key in global state
       */
      totalTransactions: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("total_transactions")) as bigint | undefined },
      /**
       * Get the current value of the total_volume key in global state
       */
      totalVolume: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("total_volume")) as bigint | undefined },
    },
  }

  public newGroup(): CampusPayComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a greet()string method call against the CampusPay contract
       */
      greet(params: CallParams<CampusPayArgs['obj']['greet()string'] | CampusPayArgs['tuple']['greet()string']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.greet(params)))
        resultMappers.push((v) => client.decodeReturnValue('greet()string', v))
        return this
      },
      /**
       * Add a register_transaction(uint64)uint64 method call against the CampusPay contract
       */
      registerTransaction(params: CallParams<CampusPayArgs['obj']['register_transaction(uint64)uint64'] | CampusPayArgs['tuple']['register_transaction(uint64)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.registerTransaction(params)))
        resultMappers.push((v) => client.decodeReturnValue('register_transaction(uint64)uint64', v))
        return this
      },
      /**
       * Add a get_stats()(uint64,uint64) method call against the CampusPay contract
       */
      getStats(params: CallParams<CampusPayArgs['obj']['get_stats()(uint64,uint64)'] | CampusPayArgs['tuple']['get_stats()(uint64,uint64)']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.getStats(params)))
        resultMappers.push((v) => client.decodeReturnValue('get_stats()(uint64,uint64)', v))
        return this
      },
      /**
       * Add a pay_and_log(pay)string method call against the CampusPay contract
       */
      payAndLog(params: CallParams<CampusPayArgs['obj']['pay_and_log(pay)string'] | CampusPayArgs['tuple']['pay_and_log(pay)string']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.payAndLog(params)))
        resultMappers.push((v) => client.decodeReturnValue('pay_and_log(pay)string', v))
        return this
      },
      /**
       * Add a clear state call to the CampusPay contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as CampusPayComposer
  }
}
export type CampusPayComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the greet()string ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  greet(params?: CallParams<CampusPayArgs['obj']['greet()string'] | CampusPayArgs['tuple']['greet()string']>): CampusPayComposer<[...TReturns, CampusPayReturns['greet()string'] | undefined]>

  /**
   * Calls the register_transaction(uint64)uint64 ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  registerTransaction(params?: CallParams<CampusPayArgs['obj']['register_transaction(uint64)uint64'] | CampusPayArgs['tuple']['register_transaction(uint64)uint64']>): CampusPayComposer<[...TReturns, CampusPayReturns['register_transaction(uint64)uint64'] | undefined]>

  /**
   * Calls the get_stats()(uint64,uint64) ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  getStats(params?: CallParams<CampusPayArgs['obj']['get_stats()(uint64,uint64)'] | CampusPayArgs['tuple']['get_stats()(uint64,uint64)']>): CampusPayComposer<[...TReturns, CampusPayReturns['get_stats()(uint64,uint64)'] | undefined]>

  /**
   * Calls the pay_and_log(pay)string ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  payAndLog(params?: CallParams<CampusPayArgs['obj']['pay_and_log(pay)string'] | CampusPayArgs['tuple']['pay_and_log(pay)string']>): CampusPayComposer<[...TReturns, CampusPayReturns['pay_and_log(pay)string'] | undefined]>

  /**
   * Makes a clear_state call to an existing instance of the CampusPay smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): CampusPayComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): CampusPayComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<CampusPayComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<CampusPayComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<CampusPayComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<CampusPayComposerResults<TReturns>>
}
export type CampusPayComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

