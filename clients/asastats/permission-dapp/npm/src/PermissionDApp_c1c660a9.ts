/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"PermissionDApp","structs":{},"methods":[{"name":"write_box","args":[{"type":"byte[]","name":"box_name","desc":"The name of the box (should be 32 bytes)"},{"type":"string","name":"value","desc":"The string value to write to the box"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Write a value to a box. Only the creator can call this method.","events":[],"recommendations":{}},{"name":"delete_box","args":[{"type":"byte[]","name":"box_name","desc":"The name of the box to delete (should be 32 bytes)"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Delete a box. Only the creator can call this method.","events":[],"recommendations":{}}],"arcs":[22,28],"desc":"\n    A permission-based dApp that allows only the creator to manage boxes.\n    ","networks":{"SGO1GKSzyE7IEPItTxCByw9x8FmnrCDexi9/cOUJOiI=":{"appID":750094865}},"state":{"schema":{"global":{"ints":0,"bytes":0},"local":{"ints":0,"bytes":0}},"keys":{"global":{},"local":{},"box":{}},"maps":{"global":{},"local":{},"box":{}}},"bareActions":{"create":["NoOp"],"call":["DeleteApplication","UpdateApplication"]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[142,185],"errorMessage":"Box name must be exactly 32 bytes"},{"pc":[15],"errorMessage":"OnCompletion must be NoOp"},{"pc":[66],"errorMessage":"Only creator can delete application"},{"pc":[176],"errorMessage":"Only creator can delete boxes"},{"pc":[77],"errorMessage":"Only creator can update application"},{"pc":[119],"errorMessage":"Only creator can write to boxes"},{"pc":[83],"errorMessage":"can only call when creating"},{"pc":[60,71],"errorMessage":"can only call when not creating"},{"pc":[91,105,162],"errorMessage":"invalid array length header"},{"pc":[99,113,170],"errorMessage":"invalid number of bytes for arc4.dynamic_array<arc4.uint8>"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"events":[]} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


/**
 * The argument types for the PermissionDApp contract
 */
export type PermissionDAppArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'write_box(byte[],string)void': {
      /**
       * The name of the box (should be 32 bytes)
       */
      boxName: Uint8Array
      /**
       * The string value to write to the box
       */
      value: string
    }
    'delete_box(byte[])void': {
      /**
       * The name of the box to delete (should be 32 bytes)
       */
      boxName: Uint8Array
    }
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'write_box(byte[],string)void': [boxName: Uint8Array, value: string]
    'delete_box(byte[])void': [boxName: Uint8Array]
  }
}

/**
 * The return type for each method
 */
export type PermissionDAppReturns = {
  'write_box(byte[],string)void': void
  'delete_box(byte[])void': void
}

/**
 * Defines the types of available calls and state of the PermissionDApp smart contract.
 */
export type PermissionDAppTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'write_box(byte[],string)void' | 'write_box', {
      argsObj: PermissionDAppArgs['obj']['write_box(byte[],string)void']
      argsTuple: PermissionDAppArgs['tuple']['write_box(byte[],string)void']
      returns: PermissionDAppReturns['write_box(byte[],string)void']
    }>
    & Record<'delete_box(byte[])void' | 'delete_box', {
      argsObj: PermissionDAppArgs['obj']['delete_box(byte[])void']
      argsTuple: PermissionDAppArgs['tuple']['delete_box(byte[])void']
      returns: PermissionDAppReturns['delete_box(byte[])void']
    }>
}

/**
 * Defines the possible abi call signatures.
 */
export type PermissionDAppSignatures = keyof PermissionDAppTypes['methods']
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the PermissionDApp smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends PermissionDAppSignatures> = PermissionDAppTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the PermissionDApp smart contract to the method's return type
 */
export type MethodReturn<TSignature extends PermissionDAppSignatures> = PermissionDAppTypes['methods'][TSignature]['returns']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the PermissionDApp smart contract
 */
export abstract class PermissionDAppParamsFactory {
  /**
   * Constructs a no op call for the write_box(byte[],string)void ABI method
   *
   * Write a value to a box. Only the creator can call this method.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static writeBox(params: CallParams<PermissionDAppArgs['obj']['write_box(byte[],string)void'] | PermissionDAppArgs['tuple']['write_box(byte[],string)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'write_box(byte[],string)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.boxName, params.args.value],
    }
  }
  /**
   * Constructs a no op call for the delete_box(byte[])void ABI method
   *
   * Delete a box. Only the creator can call this method.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static deleteBox(params: CallParams<PermissionDAppArgs['obj']['delete_box(byte[])void'] | PermissionDAppArgs['tuple']['delete_box(byte[])void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'delete_box(byte[])void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.boxName],
    }
  }
}

/**
 * A client to make calls to the PermissionDApp smart contract
 */
export class PermissionDAppClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `PermissionDAppClient`
   *
   * @param appClient An `AppClient` instance which has been created with the PermissionDApp app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `PermissionDAppClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Returns a new `PermissionDAppClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<PermissionDAppClient> {
    return new PermissionDAppClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `PermissionDAppClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<PermissionDAppClient> {
    return new PermissionDAppClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Makes a clear_state call to an existing instance of the PermissionDApp smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the PermissionDApp smart contract using the `write_box(byte[],string)void` ABI method.
     *
     * Write a value to a box. Only the creator can call this method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    writeBox: (params: CallParams<PermissionDAppArgs['obj']['write_box(byte[],string)void'] | PermissionDAppArgs['tuple']['write_box(byte[],string)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(PermissionDAppParamsFactory.writeBox(params))
    },

    /**
     * Makes a call to the PermissionDApp smart contract using the `delete_box(byte[])void` ABI method.
     *
     * Delete a box. Only the creator can call this method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    deleteBox: (params: CallParams<PermissionDAppArgs['obj']['delete_box(byte[])void'] | PermissionDAppArgs['tuple']['delete_box(byte[])void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(PermissionDAppParamsFactory.deleteBox(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Makes a clear_state call to an existing instance of the PermissionDApp smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the PermissionDApp smart contract using the `write_box(byte[],string)void` ABI method.
     *
     * Write a value to a box. Only the creator can call this method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    writeBox: (params: CallParams<PermissionDAppArgs['obj']['write_box(byte[],string)void'] | PermissionDAppArgs['tuple']['write_box(byte[],string)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(PermissionDAppParamsFactory.writeBox(params))
    },

    /**
     * Makes a call to the PermissionDApp smart contract using the `delete_box(byte[])void` ABI method.
     *
     * Delete a box. Only the creator can call this method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    deleteBox: (params: CallParams<PermissionDAppArgs['obj']['delete_box(byte[])void'] | PermissionDAppArgs['tuple']['delete_box(byte[])void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(PermissionDAppParamsFactory.deleteBox(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Makes a clear_state call to an existing instance of the PermissionDApp smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the PermissionDApp smart contract using the `write_box(byte[],string)void` ABI method.
     *
     * Write a value to a box. Only the creator can call this method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    writeBox: async (params: CallParams<PermissionDAppArgs['obj']['write_box(byte[],string)void'] | PermissionDAppArgs['tuple']['write_box(byte[],string)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(PermissionDAppParamsFactory.writeBox(params))
      return {...result, return: result.return as unknown as (undefined | PermissionDAppReturns['write_box(byte[],string)void'])}
    },

    /**
     * Makes a call to the PermissionDApp smart contract using the `delete_box(byte[])void` ABI method.
     *
     * Delete a box. Only the creator can call this method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    deleteBox: async (params: CallParams<PermissionDAppArgs['obj']['delete_box(byte[])void'] | PermissionDAppArgs['tuple']['delete_box(byte[])void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(PermissionDAppParamsFactory.deleteBox(params))
      return {...result, return: result.return as unknown as (undefined | PermissionDAppReturns['delete_box(byte[])void'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new PermissionDAppClient(this.appClient.clone(params))
  }

  /**
   * Methods to access state for the current PermissionDApp app
   */
  state = {
  }

  public newGroup(): PermissionDAppComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a write_box(byte[],string)void method call against the PermissionDApp contract
       */
      writeBox(params: CallParams<PermissionDAppArgs['obj']['write_box(byte[],string)void'] | PermissionDAppArgs['tuple']['write_box(byte[],string)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.writeBox(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a delete_box(byte[])void method call against the PermissionDApp contract
       */
      deleteBox(params: CallParams<PermissionDAppArgs['obj']['delete_box(byte[])void'] | PermissionDAppArgs['tuple']['delete_box(byte[])void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.deleteBox(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a clear state call to the PermissionDApp contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as PermissionDAppComposer
  }
}
export type PermissionDAppComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the write_box(byte[],string)void ABI method.
   *
   * Write a value to a box. Only the creator can call this method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  writeBox(params?: CallParams<PermissionDAppArgs['obj']['write_box(byte[],string)void'] | PermissionDAppArgs['tuple']['write_box(byte[],string)void']>): PermissionDAppComposer<[...TReturns, PermissionDAppReturns['write_box(byte[],string)void'] | undefined]>

  /**
   * Calls the delete_box(byte[])void ABI method.
   *
   * Delete a box. Only the creator can call this method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  deleteBox(params?: CallParams<PermissionDAppArgs['obj']['delete_box(byte[])void'] | PermissionDAppArgs['tuple']['delete_box(byte[])void']>): PermissionDAppComposer<[...TReturns, PermissionDAppReturns['delete_box(byte[])void'] | undefined]>

  /**
   * Makes a clear_state call to an existing instance of the PermissionDApp smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): PermissionDAppComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): PermissionDAppComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<PermissionDAppComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<PermissionDAppComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<PermissionDAppComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<PermissionDAppComposerResults<TReturns>>
}
export type PermissionDAppComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

