/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"Rewards","structs":{"Allocation":[{"name":"amount","type":"uint64"},{"name":"expiresAt","type":"uint64"}]},"methods":[{"name":"setup","args":[{"type":"uint64","name":"token","desc":"The ASA to be distributed."},{"type":"uint64","name":"claim_period_duration","desc":"The duration of the claim period in seconds."}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Sets up the contract with the token ID and the claim period duration.\nThis method can only be called by the admin and only once. It also makes the contract account opt-in to the specified ASA.","events":[],"recommendations":{}},{"name":"add_allocations","args":[{"type":"address[]","name":"addresses","desc":"An array of user addresses."},{"type":"uint64[]","name":"amounts","desc":"An array of corresponding allocation amounts."}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Adds or updates allocations for a batch of users.\nIf a user already has an allocation, the new amount is added to the existing one, and the expiration is reset.","events":[],"recommendations":{}},{"name":"claim","args":[],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Allows a user to claim their allocated tokens.\nThe user must opt-in to the ASA in a separate transaction within the same atomic group as the call to this method. The contract then transfers the allocated ASA amount to the user and removes their allocation to prevent re-claiming.","events":[],"recommendations":{}},{"name":"reclaim_allocation","args":[{"type":"address","name":"user_address","desc":"The address of the user whose allocation is to be reclaimed."}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Allows the admin to reclaim a user's allocation if it has expired.","events":[],"recommendations":{}}],"arcs":[22,28],"desc":"\n    A rewards smart contract for distributing an ASA (Algorand Standard Asset).\n\n    The contract is managed by an admin who can:\n    1. Fund the contract with the ASA.\n    2. Register user addresses with specific amounts to be claimed.\n\n    Users can:\n    1. Claim their allocated amount of the ASA.\n\n    The admin can also reclaim any remaining funds after a specified claim period ends.\n    ","networks":{"SGO1GKSzyE7IEPItTxCByw9x8FmnrCDexi9/cOUJOiI=":{"appID":750934138}},"state":{"schema":{"global":{"ints":2,"bytes":1},"local":{"ints":0,"bytes":0}},"keys":{"global":{"admin_address":{"keyType":"AVMString","valueType":"address","key":"YWRtaW5fYWRkcmVzcw=="},"token_id":{"keyType":"AVMString","valueType":"AVMUint64","key":"dG9rZW5faWQ="},"claim_period_duration":{"keyType":"AVMString","valueType":"AVMUint64","key":"Y2xhaW1fcGVyaW9kX2R1cmF0aW9u"}},"local":{},"box":{}},"maps":{"global":{},"local":{},"box":{"allocations":{"keyType":"address","valueType":"Allocation","prefix":"YWxsb2NhdGlvbnM="}}}},"bareActions":{"create":["NoOp"],"call":["DeleteApplication"]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[512],"errorMessage":"Claim period has ended"},{"pc":[588],"errorMessage":"Claim period has not ended for this user"},{"pc":[186],"errorMessage":"Contract already set up"},{"pc":[385],"errorMessage":"Incorrect ASA funding"},{"pc":[272],"errorMessage":"Input arrays must have the same length"},{"pc":[312],"errorMessage":"Missing funding transaction in group"},{"pc":[74],"errorMessage":"OnCompletion must be NoOp"},{"pc":[501],"errorMessage":"Sender has no allocation"},{"pc":[527],"errorMessage":"Sender has not opted-in to the asset"},{"pc":[136,180,270,568],"errorMessage":"Sender is not the admin"},{"pc":[577],"errorMessage":"User has no allocation"},{"pc":[142],"errorMessage":"can only call when creating"},{"pc":[128],"errorMessage":"can only call when not creating"},{"pc":[448],"errorMessage":"check Box exists"},{"pc":[134,178,268,563],"errorMessage":"check self.admin_address exists"},{"pc":[391],"errorMessage":"check self.claim_period_duration exists"},{"pc":[184,198,347,518,593],"errorMessage":"check self.token_id exists"},{"pc":[421],"errorMessage":"index access is out of bounds"},{"pc":[227,251],"errorMessage":"invalid array length header"},{"pc":[242],"errorMessage":"invalid number of bytes for arc4.dynamic_array<arc4.static_array<arc4.uint8, 32>>"},{"pc":[262],"errorMessage":"invalid number of bytes for arc4.dynamic_array<arc4.uint64>"},{"pc":[557],"errorMessage":"invalid number of bytes for arc4.static_array<arc4.uint8, 32>"},{"pc":[162,171],"errorMessage":"invalid number of bytes for arc4.uint64"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"events":[]} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


// Type definitions for ARC-56 structs

export type Allocation = {
  amount: bigint,
  expiresAt: bigint
}


/**
 * Converts the ABI tuple representation of a Allocation to the struct representation
 */
export function AllocationFromTuple(abiTuple: [bigint, bigint]) {
  return getABIStructFromABITuple(abiTuple, APP_SPEC.structs.Allocation, APP_SPEC.structs) as Allocation
}

/**
 * The argument types for the Rewards contract
 */
export type RewardsArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'setup(uint64,uint64)void': {
      /**
       * The ASA to be distributed.
       */
      token: bigint | number
      /**
       * The duration of the claim period in seconds.
       */
      claimPeriodDuration: bigint | number
    }
    'add_allocations(address[],uint64[])void': {
      /**
       * An array of user addresses.
       */
      addresses: string[]
      /**
       * An array of corresponding allocation amounts.
       */
      amounts: bigint[] | number[]
    }
    'claim()void': Record<string, never>
    'reclaim_allocation(address)void': {
      /**
       * The address of the user whose allocation is to be reclaimed.
       */
      userAddress: string
    }
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'setup(uint64,uint64)void': [token: bigint | number, claimPeriodDuration: bigint | number]
    'add_allocations(address[],uint64[])void': [addresses: string[], amounts: bigint[] | number[]]
    'claim()void': []
    'reclaim_allocation(address)void': [userAddress: string]
  }
}

/**
 * The return type for each method
 */
export type RewardsReturns = {
  'setup(uint64,uint64)void': void
  'add_allocations(address[],uint64[])void': void
  'claim()void': void
  'reclaim_allocation(address)void': void
}

/**
 * Defines the types of available calls and state of the Rewards smart contract.
 */
export type RewardsTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'setup(uint64,uint64)void' | 'setup', {
      argsObj: RewardsArgs['obj']['setup(uint64,uint64)void']
      argsTuple: RewardsArgs['tuple']['setup(uint64,uint64)void']
      returns: RewardsReturns['setup(uint64,uint64)void']
    }>
    & Record<'add_allocations(address[],uint64[])void' | 'add_allocations', {
      argsObj: RewardsArgs['obj']['add_allocations(address[],uint64[])void']
      argsTuple: RewardsArgs['tuple']['add_allocations(address[],uint64[])void']
      returns: RewardsReturns['add_allocations(address[],uint64[])void']
    }>
    & Record<'claim()void' | 'claim', {
      argsObj: RewardsArgs['obj']['claim()void']
      argsTuple: RewardsArgs['tuple']['claim()void']
      returns: RewardsReturns['claim()void']
    }>
    & Record<'reclaim_allocation(address)void' | 'reclaim_allocation', {
      argsObj: RewardsArgs['obj']['reclaim_allocation(address)void']
      argsTuple: RewardsArgs['tuple']['reclaim_allocation(address)void']
      returns: RewardsReturns['reclaim_allocation(address)void']
    }>
  /**
   * Defines the shape of the state of the application.
   */
  state: {
    global: {
      keys: {
        adminAddress: string
        tokenId: bigint
        claimPeriodDuration: bigint
      }
      maps: {}
    }
    box: {
      keys: {}
      maps: {
        allocations: Map<string, Allocation>
      }
    }
  }
}

/**
 * Defines the possible abi call signatures.
 */
export type RewardsSignatures = keyof RewardsTypes['methods']
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the Rewards smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends RewardsSignatures> = RewardsTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the Rewards smart contract to the method's return type
 */
export type MethodReturn<TSignature extends RewardsSignatures> = RewardsTypes['methods'][TSignature]['returns']

/**
 * Defines the shape of the keyed global state of the application.
 */
export type GlobalKeysState = RewardsTypes['state']['global']['keys']

/**
 * Defines the shape of the keyed box state of the application.
 */
export type BoxKeysState = RewardsTypes['state']['box']['keys']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the Rewards smart contract
 */
export abstract class RewardsParamsFactory {
  /**
   * Constructs a no op call for the setup(uint64,uint64)void ABI method
   *
  * Sets up the contract with the token ID and the claim period duration.
  This method can only be called by the admin and only once. It also makes the contract account opt-in to the specified ASA.

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static setup(params: CallParams<RewardsArgs['obj']['setup(uint64,uint64)void'] | RewardsArgs['tuple']['setup(uint64,uint64)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'setup(uint64,uint64)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.token, params.args.claimPeriodDuration],
    }
  }
  /**
   * Constructs a no op call for the add_allocations(address[],uint64[])void ABI method
   *
  * Adds or updates allocations for a batch of users.
  If a user already has an allocation, the new amount is added to the existing one, and the expiration is reset.

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static addAllocations(params: CallParams<RewardsArgs['obj']['add_allocations(address[],uint64[])void'] | RewardsArgs['tuple']['add_allocations(address[],uint64[])void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'add_allocations(address[],uint64[])void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.addresses, params.args.amounts],
    }
  }
  /**
   * Constructs a no op call for the claim()void ABI method
   *
  * Allows a user to claim their allocated tokens.
  The user must opt-in to the ASA in a separate transaction within the same atomic group as the call to this method. The contract then transfers the allocated ASA amount to the user and removes their allocation to prevent re-claiming.

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static claim(params: CallParams<RewardsArgs['obj']['claim()void'] | RewardsArgs['tuple']['claim()void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'claim()void' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the reclaim_allocation(address)void ABI method
   *
   * Allows the admin to reclaim a user's allocation if it has expired.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static reclaimAllocation(params: CallParams<RewardsArgs['obj']['reclaim_allocation(address)void'] | RewardsArgs['tuple']['reclaim_allocation(address)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'reclaim_allocation(address)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.userAddress],
    }
  }
}

/**
 * A client to make calls to the Rewards smart contract
 */
export class RewardsClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `RewardsClient`
   *
   * @param appClient An `AppClient` instance which has been created with the Rewards app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `RewardsClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Returns a new `RewardsClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<RewardsClient> {
    return new RewardsClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `RewardsClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<RewardsClient> {
    return new RewardsClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Makes a clear_state call to an existing instance of the Rewards smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the Rewards smart contract using the `setup(uint64,uint64)void` ABI method.
     *
    * Sets up the contract with the token ID and the claim period duration.
    This method can only be called by the admin and only once. It also makes the contract account opt-in to the specified ASA.

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    setup: (params: CallParams<RewardsArgs['obj']['setup(uint64,uint64)void'] | RewardsArgs['tuple']['setup(uint64,uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(RewardsParamsFactory.setup(params))
    },

    /**
     * Makes a call to the Rewards smart contract using the `add_allocations(address[],uint64[])void` ABI method.
     *
    * Adds or updates allocations for a batch of users.
    If a user already has an allocation, the new amount is added to the existing one, and the expiration is reset.

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    addAllocations: (params: CallParams<RewardsArgs['obj']['add_allocations(address[],uint64[])void'] | RewardsArgs['tuple']['add_allocations(address[],uint64[])void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(RewardsParamsFactory.addAllocations(params))
    },

    /**
     * Makes a call to the Rewards smart contract using the `claim()void` ABI method.
     *
    * Allows a user to claim their allocated tokens.
    The user must opt-in to the ASA in a separate transaction within the same atomic group as the call to this method. The contract then transfers the allocated ASA amount to the user and removes their allocation to prevent re-claiming.

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    claim: (params: CallParams<RewardsArgs['obj']['claim()void'] | RewardsArgs['tuple']['claim()void']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(RewardsParamsFactory.claim(params))
    },

    /**
     * Makes a call to the Rewards smart contract using the `reclaim_allocation(address)void` ABI method.
     *
     * Allows the admin to reclaim a user's allocation if it has expired.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    reclaimAllocation: (params: CallParams<RewardsArgs['obj']['reclaim_allocation(address)void'] | RewardsArgs['tuple']['reclaim_allocation(address)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(RewardsParamsFactory.reclaimAllocation(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Makes a clear_state call to an existing instance of the Rewards smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the Rewards smart contract using the `setup(uint64,uint64)void` ABI method.
     *
    * Sets up the contract with the token ID and the claim period duration.
    This method can only be called by the admin and only once. It also makes the contract account opt-in to the specified ASA.

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    setup: (params: CallParams<RewardsArgs['obj']['setup(uint64,uint64)void'] | RewardsArgs['tuple']['setup(uint64,uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(RewardsParamsFactory.setup(params))
    },

    /**
     * Makes a call to the Rewards smart contract using the `add_allocations(address[],uint64[])void` ABI method.
     *
    * Adds or updates allocations for a batch of users.
    If a user already has an allocation, the new amount is added to the existing one, and the expiration is reset.

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    addAllocations: (params: CallParams<RewardsArgs['obj']['add_allocations(address[],uint64[])void'] | RewardsArgs['tuple']['add_allocations(address[],uint64[])void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(RewardsParamsFactory.addAllocations(params))
    },

    /**
     * Makes a call to the Rewards smart contract using the `claim()void` ABI method.
     *
    * Allows a user to claim their allocated tokens.
    The user must opt-in to the ASA in a separate transaction within the same atomic group as the call to this method. The contract then transfers the allocated ASA amount to the user and removes their allocation to prevent re-claiming.

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    claim: (params: CallParams<RewardsArgs['obj']['claim()void'] | RewardsArgs['tuple']['claim()void']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(RewardsParamsFactory.claim(params))
    },

    /**
     * Makes a call to the Rewards smart contract using the `reclaim_allocation(address)void` ABI method.
     *
     * Allows the admin to reclaim a user's allocation if it has expired.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    reclaimAllocation: (params: CallParams<RewardsArgs['obj']['reclaim_allocation(address)void'] | RewardsArgs['tuple']['reclaim_allocation(address)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(RewardsParamsFactory.reclaimAllocation(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Makes a clear_state call to an existing instance of the Rewards smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the Rewards smart contract using the `setup(uint64,uint64)void` ABI method.
     *
    * Sets up the contract with the token ID and the claim period duration.
    This method can only be called by the admin and only once. It also makes the contract account opt-in to the specified ASA.

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    setup: async (params: CallParams<RewardsArgs['obj']['setup(uint64,uint64)void'] | RewardsArgs['tuple']['setup(uint64,uint64)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(RewardsParamsFactory.setup(params))
      return {...result, return: result.return as unknown as (undefined | RewardsReturns['setup(uint64,uint64)void'])}
    },

    /**
     * Makes a call to the Rewards smart contract using the `add_allocations(address[],uint64[])void` ABI method.
     *
    * Adds or updates allocations for a batch of users.
    If a user already has an allocation, the new amount is added to the existing one, and the expiration is reset.

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    addAllocations: async (params: CallParams<RewardsArgs['obj']['add_allocations(address[],uint64[])void'] | RewardsArgs['tuple']['add_allocations(address[],uint64[])void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(RewardsParamsFactory.addAllocations(params))
      return {...result, return: result.return as unknown as (undefined | RewardsReturns['add_allocations(address[],uint64[])void'])}
    },

    /**
     * Makes a call to the Rewards smart contract using the `claim()void` ABI method.
     *
    * Allows a user to claim their allocated tokens.
    The user must opt-in to the ASA in a separate transaction within the same atomic group as the call to this method. The contract then transfers the allocated ASA amount to the user and removes their allocation to prevent re-claiming.

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    claim: async (params: CallParams<RewardsArgs['obj']['claim()void'] | RewardsArgs['tuple']['claim()void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(RewardsParamsFactory.claim(params))
      return {...result, return: result.return as unknown as (undefined | RewardsReturns['claim()void'])}
    },

    /**
     * Makes a call to the Rewards smart contract using the `reclaim_allocation(address)void` ABI method.
     *
     * Allows the admin to reclaim a user's allocation if it has expired.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    reclaimAllocation: async (params: CallParams<RewardsArgs['obj']['reclaim_allocation(address)void'] | RewardsArgs['tuple']['reclaim_allocation(address)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(RewardsParamsFactory.reclaimAllocation(params))
      return {...result, return: result.return as unknown as (undefined | RewardsReturns['reclaim_allocation(address)void'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new RewardsClient(this.appClient.clone(params))
  }

  /**
   * Methods to access state for the current Rewards app
   */
  state = {
    /**
     * Methods to access global state for the current Rewards app
     */
    global: {
      /**
       * Get all current keyed values from global state
       */
      getAll: async (): Promise<Partial<Expand<GlobalKeysState>>> => {
        const result = await this.appClient.state.global.getAll()
        return {
          adminAddress: result.admin_address,
          tokenId: result.token_id,
          claimPeriodDuration: result.claim_period_duration,
        }
      },
      /**
       * Get the current value of the admin_address key in global state
       */
      adminAddress: async (): Promise<string | undefined> => { return (await this.appClient.state.global.getValue("admin_address")) as string | undefined },
      /**
       * Get the current value of the token_id key in global state
       */
      tokenId: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("token_id")) as bigint | undefined },
      /**
       * Get the current value of the claim_period_duration key in global state
       */
      claimPeriodDuration: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("claim_period_duration")) as bigint | undefined },
    },
    /**
     * Methods to access box state for the current Rewards app
     */
    box: {
      /**
       * Get all current keyed values from box state
       */
      getAll: async (): Promise<Partial<Expand<BoxKeysState>>> => {
        const result = await this.appClient.state.box.getAll()
        return {
        }
      },
      /**
       * Get values from the allocations map in box state
       */
      allocations: {
        /**
         * Get all current values of the allocations map in box state
         */
        getMap: async (): Promise<Map<string, Allocation>> => { return (await this.appClient.state.box.getMap("allocations")) as Map<string, Allocation> },
        /**
         * Get a current value of the allocations map by key from box state
         */
        value: async (key: string): Promise<Allocation | undefined> => { return await this.appClient.state.box.getMapValue("allocations", key) as Allocation | undefined },
      },
    },
  }

  public newGroup(): RewardsComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a setup(uint64,uint64)void method call against the Rewards contract
       */
      setup(params: CallParams<RewardsArgs['obj']['setup(uint64,uint64)void'] | RewardsArgs['tuple']['setup(uint64,uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.setup(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a add_allocations(address[],uint64[])void method call against the Rewards contract
       */
      addAllocations(params: CallParams<RewardsArgs['obj']['add_allocations(address[],uint64[])void'] | RewardsArgs['tuple']['add_allocations(address[],uint64[])void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.addAllocations(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a claim()void method call against the Rewards contract
       */
      claim(params: CallParams<RewardsArgs['obj']['claim()void'] | RewardsArgs['tuple']['claim()void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.claim(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a reclaim_allocation(address)void method call against the Rewards contract
       */
      reclaimAllocation(params: CallParams<RewardsArgs['obj']['reclaim_allocation(address)void'] | RewardsArgs['tuple']['reclaim_allocation(address)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.reclaimAllocation(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a clear state call to the Rewards contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as RewardsComposer
  }
}
export type RewardsComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the setup(uint64,uint64)void ABI method.
   *
  * Sets up the contract with the token ID and the claim period duration.
  This method can only be called by the admin and only once. It also makes the contract account opt-in to the specified ASA.

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  setup(params?: CallParams<RewardsArgs['obj']['setup(uint64,uint64)void'] | RewardsArgs['tuple']['setup(uint64,uint64)void']>): RewardsComposer<[...TReturns, RewardsReturns['setup(uint64,uint64)void'] | undefined]>

  /**
   * Calls the add_allocations(address[],uint64[])void ABI method.
   *
  * Adds or updates allocations for a batch of users.
  If a user already has an allocation, the new amount is added to the existing one, and the expiration is reset.

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  addAllocations(params?: CallParams<RewardsArgs['obj']['add_allocations(address[],uint64[])void'] | RewardsArgs['tuple']['add_allocations(address[],uint64[])void']>): RewardsComposer<[...TReturns, RewardsReturns['add_allocations(address[],uint64[])void'] | undefined]>

  /**
   * Calls the claim()void ABI method.
   *
  * Allows a user to claim their allocated tokens.
  The user must opt-in to the ASA in a separate transaction within the same atomic group as the call to this method. The contract then transfers the allocated ASA amount to the user and removes their allocation to prevent re-claiming.

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  claim(params?: CallParams<RewardsArgs['obj']['claim()void'] | RewardsArgs['tuple']['claim()void']>): RewardsComposer<[...TReturns, RewardsReturns['claim()void'] | undefined]>

  /**
   * Calls the reclaim_allocation(address)void ABI method.
   *
   * Allows the admin to reclaim a user's allocation if it has expired.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  reclaimAllocation(params?: CallParams<RewardsArgs['obj']['reclaim_allocation(address)void'] | RewardsArgs['tuple']['reclaim_allocation(address)void']>): RewardsComposer<[...TReturns, RewardsReturns['reclaim_allocation(address)void'] | undefined]>

  /**
   * Makes a clear_state call to an existing instance of the Rewards smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): RewardsComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): RewardsComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<RewardsComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<RewardsComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<RewardsComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<RewardsComposerResults<TReturns>>
}
export type RewardsComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

