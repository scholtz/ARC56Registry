/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"StateMutations","structs":{},"methods":[{"name":"append","args":[],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}},{"name":"modify","args":[],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}},{"name":"get","args":[],"returns":{"type":"(uint64,string)[]"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}}],"arcs":[22,28],"networks":{},"state":{"schema":{"global":{"ints":0,"bytes":3},"local":{"ints":0,"bytes":1}},"keys":{"global":{"no_proxy":{"keyType":"AVMString","valueType":"(uint64,string)[]","key":"bm9fcHJveHk="},"glob_assign":{"keyType":"AVMString","valueType":"(uint64,string)[]","key":"Z2xvYl9hc3NpZ24="},"glob":{"keyType":"AVMString","valueType":"(uint64,string)[]","key":"Z2xvYg=="}},"local":{"loc":{"keyType":"AVMString","valueType":"(uint64,string)[]","key":"bG9j"}},"box":{"box":{"keyType":"AVMString","valueType":"(uint64,string)[]","key":"Ym94"}}},"maps":{"global":{"global_map":{"keyType":"address","valueType":"(uint64,string)[]","prefix":"Z2xvYmFsX21hcA=="}},"local":{},"box":{"map":{"keyType":"address","valueType":"(uint64,string)[]","prefix":"bWFw"}}}},"bareActions":{"create":["NoOp"],"call":["OptIn"]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[562,719,827],"errorMessage":"check self.box exists"},{"pc":[533,666,817],"errorMessage":"check self.glob exists"},{"pc":[505,614,812],"errorMessage":"check self.glob_assign exists"},{"pc":[601,782,843],"errorMessage":"check self.global_map entry exists"},{"pc":[550,695,824],"errorMessage":"check self.loc exists for account"},{"pc":[581,750,834],"errorMessage":"check self.map entry exists"},{"pc":[519,640,807],"errorMessage":"check self.no_proxy exists"},{"pc":[867],"errorMessage":"expected box == no_proxy"},{"pc":[855],"errorMessage":"expected global == no_proxy"},{"pc":[849],"errorMessage":"expected global assign == no_proxy"},{"pc":[877],"errorMessage":"expected global_map == no_proxy"},{"pc":[861],"errorMessage":"expected local == no_proxy"},{"pc":[873],"errorMessage":"expected map == no_proxy"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"events":[]} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


/**
 * The argument types for the StateMutations contract
 */
export type StateMutationsArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'append()void': Record<string, never>
    'modify()void': Record<string, never>
    'get()(uint64,string)[]': Record<string, never>
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'append()void': []
    'modify()void': []
    'get()(uint64,string)[]': []
  }
}

/**
 * The return type for each method
 */
export type StateMutationsReturns = {
  'append()void': void
  'modify()void': void
  'get()(uint64,string)[]': [bigint, string][]
}

/**
 * Defines the types of available calls and state of the StateMutations smart contract.
 */
export type StateMutationsTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'append()void' | 'append', {
      argsObj: StateMutationsArgs['obj']['append()void']
      argsTuple: StateMutationsArgs['tuple']['append()void']
      returns: StateMutationsReturns['append()void']
    }>
    & Record<'modify()void' | 'modify', {
      argsObj: StateMutationsArgs['obj']['modify()void']
      argsTuple: StateMutationsArgs['tuple']['modify()void']
      returns: StateMutationsReturns['modify()void']
    }>
    & Record<'get()(uint64,string)[]' | 'get', {
      argsObj: StateMutationsArgs['obj']['get()(uint64,string)[]']
      argsTuple: StateMutationsArgs['tuple']['get()(uint64,string)[]']
      returns: StateMutationsReturns['get()(uint64,string)[]']
    }>
  /**
   * Defines the shape of the state of the application.
   */
  state: {
    global: {
      keys: {
        noProxy: [bigint, string][]
        globAssign: [bigint, string][]
        glob: [bigint, string][]
      }
      maps: {
        globalMap: Map<string, [bigint, string][]>
      }
    }
    local: {
      keys: {
        loc: [bigint, string][]
      }
      maps: {}
    }
    box: {
      keys: {
        box: [bigint, string][]
      }
      maps: {
        map: Map<string, [bigint, string][]>
      }
    }
  }
}

/**
 * Defines the possible abi call signatures.
 */
export type StateMutationsSignatures = keyof StateMutationsTypes['methods']
/**
 * Defines the possible abi call signatures for methods that return a non-void value.
 */
export type StateMutationsNonVoidMethodSignatures = keyof StateMutationsTypes['methods'] extends infer T ? T extends keyof StateMutationsTypes['methods'] ? MethodReturn<T> extends void ? never : T  : never : never
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the StateMutations smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends StateMutationsSignatures> = StateMutationsTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the StateMutations smart contract to the method's return type
 */
export type MethodReturn<TSignature extends StateMutationsSignatures> = StateMutationsTypes['methods'][TSignature]['returns']

/**
 * Defines the shape of the keyed global state of the application.
 */
export type GlobalKeysState = StateMutationsTypes['state']['global']['keys']

/**
 * Defines the shape of the keyed local state of the application.
 */
export type LocalKeysState = StateMutationsTypes['state']['local']['keys']

/**
 * Defines the shape of the keyed box state of the application.
 */
export type BoxKeysState = StateMutationsTypes['state']['box']['keys']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the StateMutations smart contract
 */
export abstract class StateMutationsParamsFactory {
  /**
   * Constructs a no op call for the append()void ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static append(params: CallParams<StateMutationsArgs['obj']['append()void'] | StateMutationsArgs['tuple']['append()void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'append()void' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the modify()void ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static modify(params: CallParams<StateMutationsArgs['obj']['modify()void'] | StateMutationsArgs['tuple']['modify()void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'modify()void' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the get()(uint64,string)[] ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static get(params: CallParams<StateMutationsArgs['obj']['get()(uint64,string)[]'] | StateMutationsArgs['tuple']['get()(uint64,string)[]']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'get()(uint64,string)[]' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
}

/**
 * A client to make calls to the StateMutations smart contract
 */
export class StateMutationsClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `StateMutationsClient`
   *
   * @param appClient An `AppClient` instance which has been created with the StateMutations app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `StateMutationsClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Checks for decode errors on the given return value and maps the return value to the return type for the given method
   * @returns The typed return value or undefined if there was no value
   */
  decodeReturnValue<TSignature extends StateMutationsNonVoidMethodSignatures>(method: TSignature, returnValue: ABIReturn | undefined) {
    return returnValue !== undefined ? getArc56ReturnValue<MethodReturn<TSignature>>(returnValue, this.appClient.getABIMethod(method), APP_SPEC.structs) : undefined
  }

  /**
   * Returns a new `StateMutationsClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<StateMutationsClient> {
    return new StateMutationsClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `StateMutationsClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<StateMutationsClient> {
    return new StateMutationsClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Gets available optIn methods
     */
    optIn: {
      /**
       * Opts the user into an existing instance of the StateMutations smart contract using a bare call.
       *
       * @param params The params for the bare (raw) call
       * @returns The optIn result
       */
      bare: (params?: Expand<AppClientBareCallParams>) => {
        return this.appClient.params.bare.optIn(params)
      },
    },

    /**
     * Makes a clear_state call to an existing instance of the StateMutations smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the StateMutations smart contract using the `append()void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    append: (params: CallParams<StateMutationsArgs['obj']['append()void'] | StateMutationsArgs['tuple']['append()void']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(StateMutationsParamsFactory.append(params))
    },

    /**
     * Makes a call to the StateMutations smart contract using the `modify()void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    modify: (params: CallParams<StateMutationsArgs['obj']['modify()void'] | StateMutationsArgs['tuple']['modify()void']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(StateMutationsParamsFactory.modify(params))
    },

    /**
     * Makes a call to the StateMutations smart contract using the `get()(uint64,string)[]` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    get: (params: CallParams<StateMutationsArgs['obj']['get()(uint64,string)[]'] | StateMutationsArgs['tuple']['get()(uint64,string)[]']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(StateMutationsParamsFactory.get(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Gets available optIn methods
     */
    optIn: {
      /**
       * Opts the user into an existing instance of the StateMutations smart contract using a bare call.
       *
       * @param params The params for the bare (raw) call
       * @returns The optIn result
       */
      bare: (params?: Expand<AppClientBareCallParams>) => {
        return this.appClient.createTransaction.bare.optIn(params)
      },
    },

    /**
     * Makes a clear_state call to an existing instance of the StateMutations smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the StateMutations smart contract using the `append()void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    append: (params: CallParams<StateMutationsArgs['obj']['append()void'] | StateMutationsArgs['tuple']['append()void']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(StateMutationsParamsFactory.append(params))
    },

    /**
     * Makes a call to the StateMutations smart contract using the `modify()void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    modify: (params: CallParams<StateMutationsArgs['obj']['modify()void'] | StateMutationsArgs['tuple']['modify()void']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(StateMutationsParamsFactory.modify(params))
    },

    /**
     * Makes a call to the StateMutations smart contract using the `get()(uint64,string)[]` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    get: (params: CallParams<StateMutationsArgs['obj']['get()(uint64,string)[]'] | StateMutationsArgs['tuple']['get()(uint64,string)[]']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(StateMutationsParamsFactory.get(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Gets available optIn methods
     */
    optIn: {
      /**
       * Opts the user into an existing instance of the StateMutations smart contract using a bare call.
       *
       * @param params The params for the bare (raw) call
       * @returns The optIn result
       */
      bare: (params?: Expand<AppClientBareCallParams & SendParams>) => {
        return this.appClient.send.bare.optIn(params)
      },
    },

    /**
     * Makes a clear_state call to an existing instance of the StateMutations smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the StateMutations smart contract using the `append()void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    append: async (params: CallParams<StateMutationsArgs['obj']['append()void'] | StateMutationsArgs['tuple']['append()void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(StateMutationsParamsFactory.append(params))
      return {...result, return: result.return as unknown as (undefined | StateMutationsReturns['append()void'])}
    },

    /**
     * Makes a call to the StateMutations smart contract using the `modify()void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    modify: async (params: CallParams<StateMutationsArgs['obj']['modify()void'] | StateMutationsArgs['tuple']['modify()void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(StateMutationsParamsFactory.modify(params))
      return {...result, return: result.return as unknown as (undefined | StateMutationsReturns['modify()void'])}
    },

    /**
     * Makes a call to the StateMutations smart contract using the `get()(uint64,string)[]` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    get: async (params: CallParams<StateMutationsArgs['obj']['get()(uint64,string)[]'] | StateMutationsArgs['tuple']['get()(uint64,string)[]']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(StateMutationsParamsFactory.get(params))
      return {...result, return: result.return as unknown as (undefined | StateMutationsReturns['get()(uint64,string)[]'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new StateMutationsClient(this.appClient.clone(params))
  }

  /**
   * Methods to access state for the current StateMutations app
   */
  state = {
    /**
     * Methods to access global state for the current StateMutations app
     */
    global: {
      /**
       * Get all current keyed values from global state
       */
      getAll: async (): Promise<Partial<Expand<GlobalKeysState>>> => {
        const result = await this.appClient.state.global.getAll()
        return {
          noProxy: result.no_proxy,
          globAssign: result.glob_assign,
          glob: result.glob,
        }
      },
      /**
       * Get the current value of the no_proxy key in global state
       */
      noProxy: async (): Promise<[bigint, string][] | undefined> => { return (await this.appClient.state.global.getValue("no_proxy")) as [bigint, string][] | undefined },
      /**
       * Get the current value of the glob_assign key in global state
       */
      globAssign: async (): Promise<[bigint, string][] | undefined> => { return (await this.appClient.state.global.getValue("glob_assign")) as [bigint, string][] | undefined },
      /**
       * Get the current value of the glob key in global state
       */
      glob: async (): Promise<[bigint, string][] | undefined> => { return (await this.appClient.state.global.getValue("glob")) as [bigint, string][] | undefined },
      /**
       * Get values from the global_map map in global state
       */
      globalMap: {
        /**
         * Get all current values of the global_map map in global state
         */
        getMap: async (): Promise<Map<string, [bigint, string][]>> => { return (await this.appClient.state.global.getMap("global_map")) as Map<string, [bigint, string][]> },
        /**
         * Get a current value of the global_map map by key from global state
         */
        value: async (key: string): Promise<[bigint, string][] | undefined> => { return await this.appClient.state.global.getMapValue("global_map", key) as [bigint, string][] | undefined },
      },
    },
    /**
     * Methods to access local state for the current StateMutations app
     */
    local: (address: string | Address) => {
      const encodedAddress = typeof address === 'string' ? address : encodeAddress(address.publicKey)
      return {
        /**
         * Get all current keyed values from local state
         */
        getAll: async (): Promise<Partial<Expand<LocalKeysState>>> => {
          const result = await this.appClient.state.local(encodedAddress).getAll()
          return {
            loc: result.loc,
          }
        },
        /**
         * Get the current value of the loc key in local state
         */
        loc: async (): Promise<[bigint, string][] | undefined> => { return (await this.appClient.state.local(encodedAddress).getValue("loc")) as [bigint, string][] | undefined },
      }
    },
    /**
     * Methods to access box state for the current StateMutations app
     */
    box: {
      /**
       * Get all current keyed values from box state
       */
      getAll: async (): Promise<Partial<Expand<BoxKeysState>>> => {
        const result = await this.appClient.state.box.getAll()
        return {
          box: result.box,
        }
      },
      /**
       * Get the current value of the box key in box state
       */
      box: async (): Promise<[bigint, string][] | undefined> => { return (await this.appClient.state.box.getValue("box")) as [bigint, string][] | undefined },
      /**
       * Get values from the map map in box state
       */
      map: {
        /**
         * Get all current values of the map map in box state
         */
        getMap: async (): Promise<Map<string, [bigint, string][]>> => { return (await this.appClient.state.box.getMap("map")) as Map<string, [bigint, string][]> },
        /**
         * Get a current value of the map map by key from box state
         */
        value: async (key: string): Promise<[bigint, string][] | undefined> => { return await this.appClient.state.box.getMapValue("map", key) as [bigint, string][] | undefined },
      },
    },
  }

  public newGroup(): StateMutationsComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a append()void method call against the StateMutations contract
       */
      append(params: CallParams<StateMutationsArgs['obj']['append()void'] | StateMutationsArgs['tuple']['append()void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.append(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a modify()void method call against the StateMutations contract
       */
      modify(params: CallParams<StateMutationsArgs['obj']['modify()void'] | StateMutationsArgs['tuple']['modify()void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.modify(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a get()(uint64,string)[] method call against the StateMutations contract
       */
      get(params: CallParams<StateMutationsArgs['obj']['get()(uint64,string)[]'] | StateMutationsArgs['tuple']['get()(uint64,string)[]']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.get(params)))
        resultMappers.push((v) => client.decodeReturnValue('get()(uint64,string)[]', v))
        return this
      },
      get optIn() {
        return {
          bare: (params?: AppClientBareCallParams ) => {
            promiseChain = promiseChain.then(() => composer.addAppCall(client.params.optIn.bare(params)))
            return this
          },
        }
      },
      /**
       * Add a clear state call to the StateMutations contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as StateMutationsComposer
  }
}
export type StateMutationsComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the append()void ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  append(params?: CallParams<StateMutationsArgs['obj']['append()void'] | StateMutationsArgs['tuple']['append()void']>): StateMutationsComposer<[...TReturns, StateMutationsReturns['append()void'] | undefined]>

  /**
   * Calls the modify()void ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  modify(params?: CallParams<StateMutationsArgs['obj']['modify()void'] | StateMutationsArgs['tuple']['modify()void']>): StateMutationsComposer<[...TReturns, StateMutationsReturns['modify()void'] | undefined]>

  /**
   * Calls the get()(uint64,string)[] ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  get(params?: CallParams<StateMutationsArgs['obj']['get()(uint64,string)[]'] | StateMutationsArgs['tuple']['get()(uint64,string)[]']>): StateMutationsComposer<[...TReturns, StateMutationsReturns['get()(uint64,string)[]'] | undefined]>

  /**
   * Gets available optIn methods
   */
  readonly optIn: {
    /**
     * Opts the user into an existing instance of the StateMutations smart contract using a bare call.
     *
     * @param args The arguments for the bare call
     * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
     */
    bare(params?: AppClientBareCallParams ): StateMutationsComposer<[...TReturns, undefined]>
  }

  /**
   * Makes a clear_state call to an existing instance of the StateMutations smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): StateMutationsComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): StateMutationsComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<StateMutationsComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<StateMutationsComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<StateMutationsComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<StateMutationsComposerResults<TReturns>>
}
export type StateMutationsComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

