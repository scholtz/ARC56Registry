/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"NestedTuplesStorage","structs":{"TupleWithMutable":[{"name":"arr","type":"uint64[]"},{"name":"bar","type":"uint64"}]},"methods":[{"name":"bootstrap","args":[],"returns":{"type":"void"},"actions":{"create":[],"call":["OptIn"]},"readonly":false,"events":[],"recommendations":{}},{"name":"mutate_tuple","args":[{"type":"uint64","name":"val"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}},{"name":"mutate_box","args":[{"type":"uint64","name":"val"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}},{"name":"mutate_global","args":[{"type":"uint64","name":"val"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}},{"name":"mutate_local","args":[{"type":"uint64","name":"val"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}}],"arcs":[22,28],"networks":{},"state":{"schema":{"global":{"ints":0,"bytes":2},"local":{"ints":0,"bytes":1}},"keys":{"global":{"glob":{"keyType":"AVMString","valueType":"TupleWithMutable","key":"Z2xvYg=="},"tup":{"keyType":"AVMString","valueType":"TupleWithMutable","key":"dHVw"}},"local":{"loc":{"keyType":"AVMString","valueType":"TupleWithMutable","key":"bG9j"}},"box":{"box":{"keyType":"AVMString","valueType":"TupleWithMutable","key":"Ym94"}}},"maps":{"global":{},"local":{},"box":{}}},"bareActions":{"create":["NoOp"],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[264],"errorMessage":"check self.box exists"},{"pc":[323],"errorMessage":"check self.glob exists"},{"pc":[383],"errorMessage":"check self.loc exists for account"},{"pc":[76,172,209],"errorMessage":"check self.tup exists"},{"pc":[205,261,319,375],"errorMessage":"invalid number of bytes for arc4.uint64"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"events":[]} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


// Type definitions for ARC-56 structs

export type TupleWithMutable = {
  arr: bigint[],
  bar: bigint
}


/**
 * Converts the ABI tuple representation of a TupleWithMutable to the struct representation
 */
export function TupleWithMutableFromTuple(abiTuple: [bigint[], bigint]) {
  return getABIStructFromABITuple(abiTuple, APP_SPEC.structs.TupleWithMutable, APP_SPEC.structs) as TupleWithMutable
}

/**
 * The argument types for the NestedTuplesStorage contract
 */
export type NestedTuplesStorageArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'bootstrap()void': Record<string, never>
    'mutate_tuple(uint64)void': {
      val: bigint | number
    }
    'mutate_box(uint64)void': {
      val: bigint | number
    }
    'mutate_global(uint64)void': {
      val: bigint | number
    }
    'mutate_local(uint64)void': {
      val: bigint | number
    }
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'bootstrap()void': []
    'mutate_tuple(uint64)void': [val: bigint | number]
    'mutate_box(uint64)void': [val: bigint | number]
    'mutate_global(uint64)void': [val: bigint | number]
    'mutate_local(uint64)void': [val: bigint | number]
  }
}

/**
 * The return type for each method
 */
export type NestedTuplesStorageReturns = {
  'bootstrap()void': void
  'mutate_tuple(uint64)void': void
  'mutate_box(uint64)void': void
  'mutate_global(uint64)void': void
  'mutate_local(uint64)void': void
}

/**
 * Defines the types of available calls and state of the NestedTuplesStorage smart contract.
 */
export type NestedTuplesStorageTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'bootstrap()void' | 'bootstrap', {
      argsObj: NestedTuplesStorageArgs['obj']['bootstrap()void']
      argsTuple: NestedTuplesStorageArgs['tuple']['bootstrap()void']
      returns: NestedTuplesStorageReturns['bootstrap()void']
    }>
    & Record<'mutate_tuple(uint64)void' | 'mutate_tuple', {
      argsObj: NestedTuplesStorageArgs['obj']['mutate_tuple(uint64)void']
      argsTuple: NestedTuplesStorageArgs['tuple']['mutate_tuple(uint64)void']
      returns: NestedTuplesStorageReturns['mutate_tuple(uint64)void']
    }>
    & Record<'mutate_box(uint64)void' | 'mutate_box', {
      argsObj: NestedTuplesStorageArgs['obj']['mutate_box(uint64)void']
      argsTuple: NestedTuplesStorageArgs['tuple']['mutate_box(uint64)void']
      returns: NestedTuplesStorageReturns['mutate_box(uint64)void']
    }>
    & Record<'mutate_global(uint64)void' | 'mutate_global', {
      argsObj: NestedTuplesStorageArgs['obj']['mutate_global(uint64)void']
      argsTuple: NestedTuplesStorageArgs['tuple']['mutate_global(uint64)void']
      returns: NestedTuplesStorageReturns['mutate_global(uint64)void']
    }>
    & Record<'mutate_local(uint64)void' | 'mutate_local', {
      argsObj: NestedTuplesStorageArgs['obj']['mutate_local(uint64)void']
      argsTuple: NestedTuplesStorageArgs['tuple']['mutate_local(uint64)void']
      returns: NestedTuplesStorageReturns['mutate_local(uint64)void']
    }>
  /**
   * Defines the shape of the state of the application.
   */
  state: {
    global: {
      keys: {
        glob: TupleWithMutable
        tup: TupleWithMutable
      }
      maps: {}
    }
    local: {
      keys: {
        loc: TupleWithMutable
      }
      maps: {}
    }
    box: {
      keys: {
        box: TupleWithMutable
      }
      maps: {}
    }
  }
}

/**
 * Defines the possible abi call signatures.
 */
export type NestedTuplesStorageSignatures = keyof NestedTuplesStorageTypes['methods']
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the NestedTuplesStorage smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends NestedTuplesStorageSignatures> = NestedTuplesStorageTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the NestedTuplesStorage smart contract to the method's return type
 */
export type MethodReturn<TSignature extends NestedTuplesStorageSignatures> = NestedTuplesStorageTypes['methods'][TSignature]['returns']

/**
 * Defines the shape of the keyed global state of the application.
 */
export type GlobalKeysState = NestedTuplesStorageTypes['state']['global']['keys']

/**
 * Defines the shape of the keyed local state of the application.
 */
export type LocalKeysState = NestedTuplesStorageTypes['state']['local']['keys']

/**
 * Defines the shape of the keyed box state of the application.
 */
export type BoxKeysState = NestedTuplesStorageTypes['state']['box']['keys']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the NestedTuplesStorage smart contract
 */
export abstract class NestedTuplesStorageParamsFactory {
  /**
   * Gets available optIn ABI call param factories
   */
  static get optIn() {
    return {
      /**
       * Constructs opt-in ABI call params for the NestedTuplesStorage smart contract using the bootstrap()void ABI method
       *
       * @param params Parameters for the call
       * @returns An `AppClientMethodCallParams` object for the call
       */
      bootstrap(params: CallParams<NestedTuplesStorageArgs['obj']['bootstrap()void'] | NestedTuplesStorageArgs['tuple']['bootstrap()void']>): AppClientMethodCallParams {
        return {
          ...params,
          method: 'bootstrap()void' as const,
          args: Array.isArray(params.args) ? params.args : [],
        }
      },
    }
  }

  /**
   * Constructs a no op call for the mutate_tuple(uint64)void ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static mutateTuple(params: CallParams<NestedTuplesStorageArgs['obj']['mutate_tuple(uint64)void'] | NestedTuplesStorageArgs['tuple']['mutate_tuple(uint64)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'mutate_tuple(uint64)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.val],
    }
  }
  /**
   * Constructs a no op call for the mutate_box(uint64)void ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static mutateBox(params: CallParams<NestedTuplesStorageArgs['obj']['mutate_box(uint64)void'] | NestedTuplesStorageArgs['tuple']['mutate_box(uint64)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'mutate_box(uint64)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.val],
    }
  }
  /**
   * Constructs a no op call for the mutate_global(uint64)void ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static mutateGlobal(params: CallParams<NestedTuplesStorageArgs['obj']['mutate_global(uint64)void'] | NestedTuplesStorageArgs['tuple']['mutate_global(uint64)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'mutate_global(uint64)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.val],
    }
  }
  /**
   * Constructs a no op call for the mutate_local(uint64)void ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static mutateLocal(params: CallParams<NestedTuplesStorageArgs['obj']['mutate_local(uint64)void'] | NestedTuplesStorageArgs['tuple']['mutate_local(uint64)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'mutate_local(uint64)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.val],
    }
  }
}

/**
 * A client to make calls to the NestedTuplesStorage smart contract
 */
export class NestedTuplesStorageClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `NestedTuplesStorageClient`
   *
   * @param appClient An `AppClient` instance which has been created with the NestedTuplesStorage app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `NestedTuplesStorageClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Returns a new `NestedTuplesStorageClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<NestedTuplesStorageClient> {
    return new NestedTuplesStorageClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `NestedTuplesStorageClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<NestedTuplesStorageClient> {
    return new NestedTuplesStorageClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Gets available optIn methods
     */
    optIn: {
      /**
       * Opts the user into an existing instance of the NestedTuplesStorage smart contract using the `bootstrap()void` ABI method.
       *
       * @param params The params for the smart contract call
       * @returns The optIn params
       */
      bootstrap: (params: CallParams<NestedTuplesStorageArgs['obj']['bootstrap()void'] | NestedTuplesStorageArgs['tuple']['bootstrap()void']> = {args: []}) => {
        return this.appClient.params.optIn(NestedTuplesStorageParamsFactory.optIn.bootstrap(params))
      },

    },

    /**
     * Makes a clear_state call to an existing instance of the NestedTuplesStorage smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the NestedTuplesStorage smart contract using the `mutate_tuple(uint64)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    mutateTuple: (params: CallParams<NestedTuplesStorageArgs['obj']['mutate_tuple(uint64)void'] | NestedTuplesStorageArgs['tuple']['mutate_tuple(uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(NestedTuplesStorageParamsFactory.mutateTuple(params))
    },

    /**
     * Makes a call to the NestedTuplesStorage smart contract using the `mutate_box(uint64)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    mutateBox: (params: CallParams<NestedTuplesStorageArgs['obj']['mutate_box(uint64)void'] | NestedTuplesStorageArgs['tuple']['mutate_box(uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(NestedTuplesStorageParamsFactory.mutateBox(params))
    },

    /**
     * Makes a call to the NestedTuplesStorage smart contract using the `mutate_global(uint64)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    mutateGlobal: (params: CallParams<NestedTuplesStorageArgs['obj']['mutate_global(uint64)void'] | NestedTuplesStorageArgs['tuple']['mutate_global(uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(NestedTuplesStorageParamsFactory.mutateGlobal(params))
    },

    /**
     * Makes a call to the NestedTuplesStorage smart contract using the `mutate_local(uint64)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    mutateLocal: (params: CallParams<NestedTuplesStorageArgs['obj']['mutate_local(uint64)void'] | NestedTuplesStorageArgs['tuple']['mutate_local(uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(NestedTuplesStorageParamsFactory.mutateLocal(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Gets available optIn methods
     */
    optIn: {
      /**
       * Opts the user into an existing instance of the NestedTuplesStorage smart contract using the `bootstrap()void` ABI method.
       *
       * @param params The params for the smart contract call
       * @returns The optIn transaction
       */
      bootstrap: (params: CallParams<NestedTuplesStorageArgs['obj']['bootstrap()void'] | NestedTuplesStorageArgs['tuple']['bootstrap()void']> = {args: []}) => {
        return this.appClient.createTransaction.optIn(NestedTuplesStorageParamsFactory.optIn.bootstrap(params))
      },

    },

    /**
     * Makes a clear_state call to an existing instance of the NestedTuplesStorage smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the NestedTuplesStorage smart contract using the `mutate_tuple(uint64)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    mutateTuple: (params: CallParams<NestedTuplesStorageArgs['obj']['mutate_tuple(uint64)void'] | NestedTuplesStorageArgs['tuple']['mutate_tuple(uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(NestedTuplesStorageParamsFactory.mutateTuple(params))
    },

    /**
     * Makes a call to the NestedTuplesStorage smart contract using the `mutate_box(uint64)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    mutateBox: (params: CallParams<NestedTuplesStorageArgs['obj']['mutate_box(uint64)void'] | NestedTuplesStorageArgs['tuple']['mutate_box(uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(NestedTuplesStorageParamsFactory.mutateBox(params))
    },

    /**
     * Makes a call to the NestedTuplesStorage smart contract using the `mutate_global(uint64)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    mutateGlobal: (params: CallParams<NestedTuplesStorageArgs['obj']['mutate_global(uint64)void'] | NestedTuplesStorageArgs['tuple']['mutate_global(uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(NestedTuplesStorageParamsFactory.mutateGlobal(params))
    },

    /**
     * Makes a call to the NestedTuplesStorage smart contract using the `mutate_local(uint64)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    mutateLocal: (params: CallParams<NestedTuplesStorageArgs['obj']['mutate_local(uint64)void'] | NestedTuplesStorageArgs['tuple']['mutate_local(uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(NestedTuplesStorageParamsFactory.mutateLocal(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Gets available optIn methods
     */
    optIn: {
      /**
       * Opts the user into an existing instance of the NestedTuplesStorage smart contract using the `bootstrap()void` ABI method.
       *
       * @param params The params for the smart contract call
       * @returns The optIn result
       */
      bootstrap: async (params: CallParams<NestedTuplesStorageArgs['obj']['bootstrap()void'] | NestedTuplesStorageArgs['tuple']['bootstrap()void']> & SendParams = {args: []}) => {
        const result = await this.appClient.send.optIn(NestedTuplesStorageParamsFactory.optIn.bootstrap(params))
        return {...result, return: result.return as unknown as (undefined | NestedTuplesStorageReturns['bootstrap()void'])}
      },

    },

    /**
     * Makes a clear_state call to an existing instance of the NestedTuplesStorage smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the NestedTuplesStorage smart contract using the `mutate_tuple(uint64)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    mutateTuple: async (params: CallParams<NestedTuplesStorageArgs['obj']['mutate_tuple(uint64)void'] | NestedTuplesStorageArgs['tuple']['mutate_tuple(uint64)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(NestedTuplesStorageParamsFactory.mutateTuple(params))
      return {...result, return: result.return as unknown as (undefined | NestedTuplesStorageReturns['mutate_tuple(uint64)void'])}
    },

    /**
     * Makes a call to the NestedTuplesStorage smart contract using the `mutate_box(uint64)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    mutateBox: async (params: CallParams<NestedTuplesStorageArgs['obj']['mutate_box(uint64)void'] | NestedTuplesStorageArgs['tuple']['mutate_box(uint64)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(NestedTuplesStorageParamsFactory.mutateBox(params))
      return {...result, return: result.return as unknown as (undefined | NestedTuplesStorageReturns['mutate_box(uint64)void'])}
    },

    /**
     * Makes a call to the NestedTuplesStorage smart contract using the `mutate_global(uint64)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    mutateGlobal: async (params: CallParams<NestedTuplesStorageArgs['obj']['mutate_global(uint64)void'] | NestedTuplesStorageArgs['tuple']['mutate_global(uint64)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(NestedTuplesStorageParamsFactory.mutateGlobal(params))
      return {...result, return: result.return as unknown as (undefined | NestedTuplesStorageReturns['mutate_global(uint64)void'])}
    },

    /**
     * Makes a call to the NestedTuplesStorage smart contract using the `mutate_local(uint64)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    mutateLocal: async (params: CallParams<NestedTuplesStorageArgs['obj']['mutate_local(uint64)void'] | NestedTuplesStorageArgs['tuple']['mutate_local(uint64)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(NestedTuplesStorageParamsFactory.mutateLocal(params))
      return {...result, return: result.return as unknown as (undefined | NestedTuplesStorageReturns['mutate_local(uint64)void'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new NestedTuplesStorageClient(this.appClient.clone(params))
  }

  /**
   * Methods to access state for the current NestedTuplesStorage app
   */
  state = {
    /**
     * Methods to access global state for the current NestedTuplesStorage app
     */
    global: {
      /**
       * Get all current keyed values from global state
       */
      getAll: async (): Promise<Partial<Expand<GlobalKeysState>>> => {
        const result = await this.appClient.state.global.getAll()
        return {
          glob: result.glob,
          tup: result.tup,
        }
      },
      /**
       * Get the current value of the glob key in global state
       */
      glob: async (): Promise<TupleWithMutable | undefined> => { return (await this.appClient.state.global.getValue("glob")) as TupleWithMutable | undefined },
      /**
       * Get the current value of the tup key in global state
       */
      tup: async (): Promise<TupleWithMutable | undefined> => { return (await this.appClient.state.global.getValue("tup")) as TupleWithMutable | undefined },
    },
    /**
     * Methods to access local state for the current NestedTuplesStorage app
     */
    local: (address: string | Address) => {
      const encodedAddress = typeof address === 'string' ? address : encodeAddress(address.publicKey)
      return {
        /**
         * Get all current keyed values from local state
         */
        getAll: async (): Promise<Partial<Expand<LocalKeysState>>> => {
          const result = await this.appClient.state.local(encodedAddress).getAll()
          return {
            loc: result.loc,
          }
        },
        /**
         * Get the current value of the loc key in local state
         */
        loc: async (): Promise<TupleWithMutable | undefined> => { return (await this.appClient.state.local(encodedAddress).getValue("loc")) as TupleWithMutable | undefined },
      }
    },
    /**
     * Methods to access box state for the current NestedTuplesStorage app
     */
    box: {
      /**
       * Get all current keyed values from box state
       */
      getAll: async (): Promise<Partial<Expand<BoxKeysState>>> => {
        const result = await this.appClient.state.box.getAll()
        return {
          box: result.box,
        }
      },
      /**
       * Get the current value of the box key in box state
       */
      box: async (): Promise<TupleWithMutable | undefined> => { return (await this.appClient.state.box.getValue("box")) as TupleWithMutable | undefined },
    },
  }

  public newGroup(): NestedTuplesStorageComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a mutate_tuple(uint64)void method call against the NestedTuplesStorage contract
       */
      mutateTuple(params: CallParams<NestedTuplesStorageArgs['obj']['mutate_tuple(uint64)void'] | NestedTuplesStorageArgs['tuple']['mutate_tuple(uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.mutateTuple(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a mutate_box(uint64)void method call against the NestedTuplesStorage contract
       */
      mutateBox(params: CallParams<NestedTuplesStorageArgs['obj']['mutate_box(uint64)void'] | NestedTuplesStorageArgs['tuple']['mutate_box(uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.mutateBox(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a mutate_global(uint64)void method call against the NestedTuplesStorage contract
       */
      mutateGlobal(params: CallParams<NestedTuplesStorageArgs['obj']['mutate_global(uint64)void'] | NestedTuplesStorageArgs['tuple']['mutate_global(uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.mutateGlobal(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a mutate_local(uint64)void method call against the NestedTuplesStorage contract
       */
      mutateLocal(params: CallParams<NestedTuplesStorageArgs['obj']['mutate_local(uint64)void'] | NestedTuplesStorageArgs['tuple']['mutate_local(uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.mutateLocal(params)))
        resultMappers.push(undefined)
        return this
      },
      get optIn() {
        return {
          bootstrap: (params: CallParams<NestedTuplesStorageArgs['obj']['bootstrap()void'] | NestedTuplesStorageArgs['tuple']['bootstrap()void']>) => {
            promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.optIn.bootstrap(params)))
            resultMappers.push(undefined)
            return this
          },
        }
      },
      /**
       * Add a clear state call to the NestedTuplesStorage contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as NestedTuplesStorageComposer
  }
}
export type NestedTuplesStorageComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the mutate_tuple(uint64)void ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  mutateTuple(params?: CallParams<NestedTuplesStorageArgs['obj']['mutate_tuple(uint64)void'] | NestedTuplesStorageArgs['tuple']['mutate_tuple(uint64)void']>): NestedTuplesStorageComposer<[...TReturns, NestedTuplesStorageReturns['mutate_tuple(uint64)void'] | undefined]>

  /**
   * Calls the mutate_box(uint64)void ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  mutateBox(params?: CallParams<NestedTuplesStorageArgs['obj']['mutate_box(uint64)void'] | NestedTuplesStorageArgs['tuple']['mutate_box(uint64)void']>): NestedTuplesStorageComposer<[...TReturns, NestedTuplesStorageReturns['mutate_box(uint64)void'] | undefined]>

  /**
   * Calls the mutate_global(uint64)void ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  mutateGlobal(params?: CallParams<NestedTuplesStorageArgs['obj']['mutate_global(uint64)void'] | NestedTuplesStorageArgs['tuple']['mutate_global(uint64)void']>): NestedTuplesStorageComposer<[...TReturns, NestedTuplesStorageReturns['mutate_global(uint64)void'] | undefined]>

  /**
   * Calls the mutate_local(uint64)void ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  mutateLocal(params?: CallParams<NestedTuplesStorageArgs['obj']['mutate_local(uint64)void'] | NestedTuplesStorageArgs['tuple']['mutate_local(uint64)void']>): NestedTuplesStorageComposer<[...TReturns, NestedTuplesStorageReturns['mutate_local(uint64)void'] | undefined]>

  /**
   * Gets available optIn methods
   */
  readonly optIn: {
    /**
     * Opts the user into an existing instance of the NestedTuplesStorage smart contract using the bootstrap()void ABI method.
     *
     * @param args The arguments for the smart contract call
     * @param params Any additional parameters for the call
     * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
     */
    bootstrap(params?: CallParams<NestedTuplesStorageArgs['obj']['bootstrap()void'] | NestedTuplesStorageArgs['tuple']['bootstrap()void']>): NestedTuplesStorageComposer<[...TReturns, NestedTuplesStorageReturns['bootstrap()void'] | undefined]>
  }

  /**
   * Makes a clear_state call to an existing instance of the NestedTuplesStorage smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): NestedTuplesStorageComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): NestedTuplesStorageComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<NestedTuplesStorageComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<NestedTuplesStorageComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<NestedTuplesStorageComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<NestedTuplesStorageComposerResults<TReturns>>
}
export type NestedTuplesStorageComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

