/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"MyContract","structs":{},"methods":[{"name":"create","args":[],"returns":{"type":"void"},"actions":{"create":["NoOp"],"call":[]},"readonly":false,"events":[],"recommendations":{}},{"name":"register","args":[{"type":"string","name":"name"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp","OptIn"]},"readonly":false,"events":[],"recommendations":{}},{"name":"say_hello","args":[],"returns":{"type":"string"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}},{"name":"calculate","args":[{"type":"uint64","name":"a"},{"type":"uint64","name":"b"}],"returns":{"type":"uint64"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}},{"name":"close_out","args":[],"returns":{"type":"void"},"actions":{"create":[],"call":["CloseOut"]},"readonly":false,"events":[],"recommendations":{}}],"arcs":[22,28],"networks":{},"state":{"schema":{"global":{"ints":1,"bytes":1},"local":{"ints":0,"bytes":1}},"keys":{"global":{"creator":{"keyType":"AVMString","valueType":"address","key":"Y3JlYXRvcg=="},"counter":{"keyType":"AVMString","valueType":"AVMUint64","key":"Y291bnRlcg=="}},"local":{"name":{"keyType":"AVMString","valueType":"string","key":"bmFtZQ=="}},"box":{}},"maps":{"global":{},"local":{},"box":{}}},"bareActions":{"create":[],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[155,190,227],"errorMessage":"You are banned, goodbye"},{"pc":[210,344],"errorMessage":"check self.counter exists"},{"pc":[179,267],"errorMessage":"invalid array length header"},{"pc":[185],"errorMessage":"invalid number of bytes for arc4.dynamic_array<arc4.uint8>"},{"pc":[316,324],"errorMessage":"invalid number of bytes for arc4.uint64"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[{"pc":[15],"errorMessage":"check self.counter exists"}],"pcOffsetMethod":"none"}},"events":[]} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


/**
 * The argument types for the MyContract contract
 */
export type MyContractArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'create()void': Record<string, never>
    'register(string)void': {
      name: string
    }
    'say_hello()string': Record<string, never>
    'calculate(uint64,uint64)uint64': {
      a: bigint | number
      b: bigint | number
    }
    'close_out()void': Record<string, never>
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'create()void': []
    'register(string)void': [name: string]
    'say_hello()string': []
    'calculate(uint64,uint64)uint64': [a: bigint | number, b: bigint | number]
    'close_out()void': []
  }
}

/**
 * The return type for each method
 */
export type MyContractReturns = {
  'create()void': void
  'register(string)void': void
  'say_hello()string': string
  'calculate(uint64,uint64)uint64': bigint
  'close_out()void': void
}

/**
 * Defines the types of available calls and state of the MyContract smart contract.
 */
export type MyContractTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'create()void' | 'create', {
      argsObj: MyContractArgs['obj']['create()void']
      argsTuple: MyContractArgs['tuple']['create()void']
      returns: MyContractReturns['create()void']
    }>
    & Record<'register(string)void' | 'register', {
      argsObj: MyContractArgs['obj']['register(string)void']
      argsTuple: MyContractArgs['tuple']['register(string)void']
      returns: MyContractReturns['register(string)void']
    }>
    & Record<'say_hello()string' | 'say_hello', {
      argsObj: MyContractArgs['obj']['say_hello()string']
      argsTuple: MyContractArgs['tuple']['say_hello()string']
      returns: MyContractReturns['say_hello()string']
    }>
    & Record<'calculate(uint64,uint64)uint64' | 'calculate', {
      argsObj: MyContractArgs['obj']['calculate(uint64,uint64)uint64']
      argsTuple: MyContractArgs['tuple']['calculate(uint64,uint64)uint64']
      returns: MyContractReturns['calculate(uint64,uint64)uint64']
    }>
    & Record<'close_out()void' | 'close_out', {
      argsObj: MyContractArgs['obj']['close_out()void']
      argsTuple: MyContractArgs['tuple']['close_out()void']
      returns: MyContractReturns['close_out()void']
    }>
  /**
   * Defines the shape of the state of the application.
   */
  state: {
    global: {
      keys: {
        creator: string
        counter: bigint
      }
      maps: {}
    }
    local: {
      keys: {
        name: string
      }
      maps: {}
    }
  }
}

/**
 * Defines the possible abi call signatures.
 */
export type MyContractSignatures = keyof MyContractTypes['methods']
/**
 * Defines the possible abi call signatures for methods that return a non-void value.
 */
export type MyContractNonVoidMethodSignatures = keyof MyContractTypes['methods'] extends infer T ? T extends keyof MyContractTypes['methods'] ? MethodReturn<T> extends void ? never : T  : never : never
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the MyContract smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends MyContractSignatures> = MyContractTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the MyContract smart contract to the method's return type
 */
export type MethodReturn<TSignature extends MyContractSignatures> = MyContractTypes['methods'][TSignature]['returns']

/**
 * Defines the shape of the keyed global state of the application.
 */
export type GlobalKeysState = MyContractTypes['state']['global']['keys']

/**
 * Defines the shape of the keyed local state of the application.
 */
export type LocalKeysState = MyContractTypes['state']['local']['keys']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the MyContract smart contract
 */
export abstract class MyContractParamsFactory {
  /**
   * Gets available optIn ABI call param factories
   */
  static get optIn() {
    return {
      /**
       * Constructs opt-in ABI call params for the MyContract smart contract using the register(string)void ABI method
       *
       * @param params Parameters for the call
       * @returns An `AppClientMethodCallParams` object for the call
       */
      register(params: CallParams<MyContractArgs['obj']['register(string)void'] | MyContractArgs['tuple']['register(string)void']>): AppClientMethodCallParams {
        return {
          ...params,
          method: 'register(string)void' as const,
          args: Array.isArray(params.args) ? params.args : [params.args.name],
        }
      },
    }
  }

  /**
   * Gets available closeOut ABI call param factories
   */
  static get closeOut() {
    return {
      /**
       * Constructs close out ABI call params for the MyContract smart contract using the close_out()void ABI method
       *
       * @param params Parameters for the call
       * @returns An `AppClientMethodCallParams` object for the call
       */
      closeOut(params: CallParams<MyContractArgs['obj']['close_out()void'] | MyContractArgs['tuple']['close_out()void']>): AppClientMethodCallParams {
        return {
          ...params,
          method: 'close_out()void' as const,
          args: Array.isArray(params.args) ? params.args : [],
        }
      },
    }
  }

  /**
   * Constructs a no op call for the register(string)void ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static register(params: CallParams<MyContractArgs['obj']['register(string)void'] | MyContractArgs['tuple']['register(string)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'register(string)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.name],
    }
  }
  /**
   * Constructs a no op call for the say_hello()string ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static sayHello(params: CallParams<MyContractArgs['obj']['say_hello()string'] | MyContractArgs['tuple']['say_hello()string']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'say_hello()string' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the calculate(uint64,uint64)uint64 ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static calculate(params: CallParams<MyContractArgs['obj']['calculate(uint64,uint64)uint64'] | MyContractArgs['tuple']['calculate(uint64,uint64)uint64']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'calculate(uint64,uint64)uint64' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.a, params.args.b],
    }
  }
}

/**
 * A client to make calls to the MyContract smart contract
 */
export class MyContractClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `MyContractClient`
   *
   * @param appClient An `AppClient` instance which has been created with the MyContract app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `MyContractClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Checks for decode errors on the given return value and maps the return value to the return type for the given method
   * @returns The typed return value or undefined if there was no value
   */
  decodeReturnValue<TSignature extends MyContractNonVoidMethodSignatures>(method: TSignature, returnValue: ABIReturn | undefined) {
    return returnValue !== undefined ? getArc56ReturnValue<MethodReturn<TSignature>>(returnValue, this.appClient.getABIMethod(method), APP_SPEC.structs) : undefined
  }

  /**
   * Returns a new `MyContractClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<MyContractClient> {
    return new MyContractClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `MyContractClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<MyContractClient> {
    return new MyContractClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Gets available optIn methods
     */
    optIn: {
      /**
       * Opts the user into an existing instance of the MyContract smart contract using the `register(string)void` ABI method.
       *
       * @param params The params for the smart contract call
       * @returns The optIn params
       */
      register: (params: CallParams<MyContractArgs['obj']['register(string)void'] | MyContractArgs['tuple']['register(string)void']>) => {
        return this.appClient.params.optIn(MyContractParamsFactory.optIn.register(params))
      },

    },

    /**
     * Gets available closeOut methods
     */
    closeOut: {
      /**
       * Makes a close out call to an existing instance of the MyContract smart contract using the `close_out()void` ABI method.
       *
       * @param params The params for the smart contract call
       * @returns The closeOut params
       */
      closeOut: (params: CallParams<MyContractArgs['obj']['close_out()void'] | MyContractArgs['tuple']['close_out()void']> = {args: []}) => {
        return this.appClient.params.closeOut(MyContractParamsFactory.closeOut.closeOut(params))
      },

    },

    /**
     * Makes a clear_state call to an existing instance of the MyContract smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the MyContract smart contract using the `register(string)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    register: (params: CallParams<MyContractArgs['obj']['register(string)void'] | MyContractArgs['tuple']['register(string)void']> & {onComplete?: OnApplicationComplete.NoOpOC | OnApplicationComplete.OptInOC}) => {
      return this.appClient.params.call(MyContractParamsFactory.register(params))
    },

    /**
     * Makes a call to the MyContract smart contract using the `say_hello()string` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    sayHello: (params: CallParams<MyContractArgs['obj']['say_hello()string'] | MyContractArgs['tuple']['say_hello()string']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(MyContractParamsFactory.sayHello(params))
    },

    /**
     * Makes a call to the MyContract smart contract using the `calculate(uint64,uint64)uint64` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    calculate: (params: CallParams<MyContractArgs['obj']['calculate(uint64,uint64)uint64'] | MyContractArgs['tuple']['calculate(uint64,uint64)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(MyContractParamsFactory.calculate(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Gets available optIn methods
     */
    optIn: {
      /**
       * Opts the user into an existing instance of the MyContract smart contract using the `register(string)void` ABI method.
       *
       * @param params The params for the smart contract call
       * @returns The optIn transaction
       */
      register: (params: CallParams<MyContractArgs['obj']['register(string)void'] | MyContractArgs['tuple']['register(string)void']>) => {
        return this.appClient.createTransaction.optIn(MyContractParamsFactory.optIn.register(params))
      },

    },

    /**
     * Gets available closeOut methods
     */
    closeOut: {
      /**
       * Makes a close out call to an existing instance of the MyContract smart contract using the `close_out()void` ABI method.
       *
       * @param params The params for the smart contract call
       * @returns The closeOut transaction
       */
      closeOut: (params: CallParams<MyContractArgs['obj']['close_out()void'] | MyContractArgs['tuple']['close_out()void']> = {args: []}) => {
        return this.appClient.createTransaction.closeOut(MyContractParamsFactory.closeOut.closeOut(params))
      },

    },

    /**
     * Makes a clear_state call to an existing instance of the MyContract smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the MyContract smart contract using the `register(string)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    register: (params: CallParams<MyContractArgs['obj']['register(string)void'] | MyContractArgs['tuple']['register(string)void']> & {onComplete?: OnApplicationComplete.NoOpOC | OnApplicationComplete.OptInOC}) => {
      return this.appClient.createTransaction.call(MyContractParamsFactory.register(params))
    },

    /**
     * Makes a call to the MyContract smart contract using the `say_hello()string` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    sayHello: (params: CallParams<MyContractArgs['obj']['say_hello()string'] | MyContractArgs['tuple']['say_hello()string']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(MyContractParamsFactory.sayHello(params))
    },

    /**
     * Makes a call to the MyContract smart contract using the `calculate(uint64,uint64)uint64` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    calculate: (params: CallParams<MyContractArgs['obj']['calculate(uint64,uint64)uint64'] | MyContractArgs['tuple']['calculate(uint64,uint64)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(MyContractParamsFactory.calculate(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Gets available optIn methods
     */
    optIn: {
      /**
       * Opts the user into an existing instance of the MyContract smart contract using the `register(string)void` ABI method.
       *
       * @param params The params for the smart contract call
       * @returns The optIn result
       */
      register: async (params: CallParams<MyContractArgs['obj']['register(string)void'] | MyContractArgs['tuple']['register(string)void']> & SendParams) => {
        const result = await this.appClient.send.optIn(MyContractParamsFactory.optIn.register(params))
        return {...result, return: result.return as unknown as (undefined | MyContractReturns['register(string)void'])}
      },

    },

    /**
     * Gets available closeOut methods
     */
    closeOut: {
      /**
       * Makes a close out call to an existing instance of the MyContract smart contract using the `close_out()void` ABI method.
       *
       * @param params The params for the smart contract call
       * @returns The closeOut result
       */
      closeOut: async (params: CallParams<MyContractArgs['obj']['close_out()void'] | MyContractArgs['tuple']['close_out()void']> & SendParams = {args: []}) => {
        const result = await this.appClient.send.closeOut(MyContractParamsFactory.closeOut.closeOut(params))
        return {...result, return: result.return as unknown as (undefined | MyContractReturns['close_out()void'])}
      },

    },

    /**
     * Makes a clear_state call to an existing instance of the MyContract smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the MyContract smart contract using the `register(string)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    register: async (params: CallParams<MyContractArgs['obj']['register(string)void'] | MyContractArgs['tuple']['register(string)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC | OnApplicationComplete.OptInOC}) => {
      const result = await this.appClient.send.call(MyContractParamsFactory.register(params))
      return {...result, return: result.return as unknown as (undefined | MyContractReturns['register(string)void'])}
    },

    /**
     * Makes a call to the MyContract smart contract using the `say_hello()string` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    sayHello: async (params: CallParams<MyContractArgs['obj']['say_hello()string'] | MyContractArgs['tuple']['say_hello()string']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(MyContractParamsFactory.sayHello(params))
      return {...result, return: result.return as unknown as (undefined | MyContractReturns['say_hello()string'])}
    },

    /**
     * Makes a call to the MyContract smart contract using the `calculate(uint64,uint64)uint64` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    calculate: async (params: CallParams<MyContractArgs['obj']['calculate(uint64,uint64)uint64'] | MyContractArgs['tuple']['calculate(uint64,uint64)uint64']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(MyContractParamsFactory.calculate(params))
      return {...result, return: result.return as unknown as (undefined | MyContractReturns['calculate(uint64,uint64)uint64'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new MyContractClient(this.appClient.clone(params))
  }

  /**
   * Methods to access state for the current MyContract app
   */
  state = {
    /**
     * Methods to access global state for the current MyContract app
     */
    global: {
      /**
       * Get all current keyed values from global state
       */
      getAll: async (): Promise<Partial<Expand<GlobalKeysState>>> => {
        const result = await this.appClient.state.global.getAll()
        return {
          creator: result.creator,
          counter: result.counter,
        }
      },
      /**
       * Get the current value of the creator key in global state
       */
      creator: async (): Promise<string | undefined> => { return (await this.appClient.state.global.getValue("creator")) as string | undefined },
      /**
       * Get the current value of the counter key in global state
       */
      counter: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("counter")) as bigint | undefined },
    },
    /**
     * Methods to access local state for the current MyContract app
     */
    local: (address: string | Address) => {
      const encodedAddress = typeof address === 'string' ? address : encodeAddress(address.publicKey)
      return {
        /**
         * Get all current keyed values from local state
         */
        getAll: async (): Promise<Partial<Expand<LocalKeysState>>> => {
          const result = await this.appClient.state.local(encodedAddress).getAll()
          return {
            name: result.name,
          }
        },
        /**
         * Get the current value of the name key in local state
         */
        name: async (): Promise<string | undefined> => { return (await this.appClient.state.local(encodedAddress).getValue("name")) as string | undefined },
      }
    },
  }

  public newGroup(): MyContractComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a register(string)void method call against the MyContract contract
       */
      register(params: CallParams<MyContractArgs['obj']['register(string)void'] | MyContractArgs['tuple']['register(string)void']> & {onComplete?: OnApplicationComplete.NoOpOC | OnApplicationComplete.OptInOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.register(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a say_hello()string method call against the MyContract contract
       */
      sayHello(params: CallParams<MyContractArgs['obj']['say_hello()string'] | MyContractArgs['tuple']['say_hello()string']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.sayHello(params)))
        resultMappers.push((v) => client.decodeReturnValue('say_hello()string', v))
        return this
      },
      /**
       * Add a calculate(uint64,uint64)uint64 method call against the MyContract contract
       */
      calculate(params: CallParams<MyContractArgs['obj']['calculate(uint64,uint64)uint64'] | MyContractArgs['tuple']['calculate(uint64,uint64)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.calculate(params)))
        resultMappers.push((v) => client.decodeReturnValue('calculate(uint64,uint64)uint64', v))
        return this
      },
      get optIn() {
        return {
          register: (params: CallParams<MyContractArgs['obj']['register(string)void'] | MyContractArgs['tuple']['register(string)void']>) => {
            promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.optIn.register(params)))
            resultMappers.push(undefined)
            return this
          },
        }
      },
      get closeOut() {
        return {
          closeOut: (params: CallParams<MyContractArgs['obj']['close_out()void'] | MyContractArgs['tuple']['close_out()void']>) => {
            promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.closeOut.closeOut(params)))
            resultMappers.push(undefined)
            return this
          },
        }
      },
      /**
       * Add a clear state call to the MyContract contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as MyContractComposer
  }
}
export type MyContractComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the register(string)void ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  register(params?: CallParams<MyContractArgs['obj']['register(string)void'] | MyContractArgs['tuple']['register(string)void']>): MyContractComposer<[...TReturns, MyContractReturns['register(string)void'] | undefined]>

  /**
   * Calls the say_hello()string ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  sayHello(params?: CallParams<MyContractArgs['obj']['say_hello()string'] | MyContractArgs['tuple']['say_hello()string']>): MyContractComposer<[...TReturns, MyContractReturns['say_hello()string'] | undefined]>

  /**
   * Calls the calculate(uint64,uint64)uint64 ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  calculate(params?: CallParams<MyContractArgs['obj']['calculate(uint64,uint64)uint64'] | MyContractArgs['tuple']['calculate(uint64,uint64)uint64']>): MyContractComposer<[...TReturns, MyContractReturns['calculate(uint64,uint64)uint64'] | undefined]>

  /**
   * Gets available optIn methods
   */
  readonly optIn: {
    /**
     * Opts the user into an existing instance of the MyContract smart contract using the register(string)void ABI method.
     *
     * @param args The arguments for the smart contract call
     * @param params Any additional parameters for the call
     * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
     */
    register(params?: CallParams<MyContractArgs['obj']['register(string)void'] | MyContractArgs['tuple']['register(string)void']>): MyContractComposer<[...TReturns, MyContractReturns['register(string)void'] | undefined]>
  }

  /**
   * Gets available closeOut methods
   */
  readonly closeOut: {
    /**
     * Makes a close out call to an existing instance of the MyContract smart contract using the close_out()void ABI method.
     *
     * @param args The arguments for the smart contract call
     * @param params Any additional parameters for the call
     * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
     */
    closeOut(params?: CallParams<MyContractArgs['obj']['close_out()void'] | MyContractArgs['tuple']['close_out()void']>): MyContractComposer<[...TReturns, MyContractReturns['close_out()void'] | undefined]>
  }

  /**
   * Makes a clear_state call to an existing instance of the MyContract smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): MyContractComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): MyContractComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<MyContractComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<MyContractComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<MyContractComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<MyContractComposerResults<TReturns>>
}
export type MyContractComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

