/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"BaseTwo","structs":{},"methods":[{"name":"methodTwo","args":[],"returns":{"type":"string"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}},{"name":"b2CantOverride","args":[],"returns":{"type":"string"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Because CommonBase implements this method, and MRO for polytype is depth first; this method\nshould not be accessible from MultiBases as the MRO should be `BaseOne => CommonBase => BaseTwo => CommonBase`\nand since CommonBase provides an implementation, this one should not be used","events":[],"recommendations":{}},{"name":"methodCommon","args":[],"returns":{"type":"string"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}}],"arcs":[22,28],"networks":{},"state":{"schema":{"global":{"ints":1,"bytes":1},"local":{"ints":0,"bytes":0}},"keys":{"global":{"stateTwo":{"keyType":"AVMString","valueType":"AVMString","key":"c3RhdGVUd28="},"stateCommon":{"keyType":"AVMString","valueType":"AVMUint64","key":"c3RhdGVDb21tb24="}},"local":{},"box":{}},"maps":{"global":{},"local":{},"box":{}}},"bareActions":{"create":["NoOp"],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"events":[]} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


/**
 * The argument types for the BaseTwo contract
 */
export type BaseTwoArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'methodTwo()string': Record<string, never>
    'b2CantOverride()string': Record<string, never>
    'methodCommon()string': Record<string, never>
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'methodTwo()string': []
    'b2CantOverride()string': []
    'methodCommon()string': []
  }
}

/**
 * The return type for each method
 */
export type BaseTwoReturns = {
  'methodTwo()string': string
  'b2CantOverride()string': string
  'methodCommon()string': string
}

/**
 * Defines the types of available calls and state of the BaseTwo smart contract.
 */
export type BaseTwoTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'methodTwo()string' | 'methodTwo', {
      argsObj: BaseTwoArgs['obj']['methodTwo()string']
      argsTuple: BaseTwoArgs['tuple']['methodTwo()string']
      returns: BaseTwoReturns['methodTwo()string']
    }>
    & Record<'b2CantOverride()string' | 'b2CantOverride', {
      argsObj: BaseTwoArgs['obj']['b2CantOverride()string']
      argsTuple: BaseTwoArgs['tuple']['b2CantOverride()string']
      returns: BaseTwoReturns['b2CantOverride()string']
    }>
    & Record<'methodCommon()string' | 'methodCommon', {
      argsObj: BaseTwoArgs['obj']['methodCommon()string']
      argsTuple: BaseTwoArgs['tuple']['methodCommon()string']
      returns: BaseTwoReturns['methodCommon()string']
    }>
  /**
   * Defines the shape of the state of the application.
   */
  state: {
    global: {
      keys: {
        stateTwo: string
        stateCommon: bigint
      }
      maps: {}
    }
  }
}

/**
 * Defines the possible abi call signatures.
 */
export type BaseTwoSignatures = keyof BaseTwoTypes['methods']
/**
 * Defines the possible abi call signatures for methods that return a non-void value.
 */
export type BaseTwoNonVoidMethodSignatures = keyof BaseTwoTypes['methods'] extends infer T ? T extends keyof BaseTwoTypes['methods'] ? MethodReturn<T> extends void ? never : T  : never : never
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the BaseTwo smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends BaseTwoSignatures> = BaseTwoTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the BaseTwo smart contract to the method's return type
 */
export type MethodReturn<TSignature extends BaseTwoSignatures> = BaseTwoTypes['methods'][TSignature]['returns']

/**
 * Defines the shape of the keyed global state of the application.
 */
export type GlobalKeysState = BaseTwoTypes['state']['global']['keys']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the BaseTwo smart contract
 */
export abstract class BaseTwoParamsFactory {
  /**
   * Constructs a no op call for the methodTwo()string ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static methodTwo(params: CallParams<BaseTwoArgs['obj']['methodTwo()string'] | BaseTwoArgs['tuple']['methodTwo()string']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'methodTwo()string' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the b2CantOverride()string ABI method
   *
  * Because CommonBase implements this method, and MRO for polytype is depth first; this method
  should not be accessible from MultiBases as the MRO should be `BaseOne => CommonBase => BaseTwo => CommonBase`
  and since CommonBase provides an implementation, this one should not be used

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static b2CantOverride(params: CallParams<BaseTwoArgs['obj']['b2CantOverride()string'] | BaseTwoArgs['tuple']['b2CantOverride()string']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'b2CantOverride()string' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the methodCommon()string ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static methodCommon(params: CallParams<BaseTwoArgs['obj']['methodCommon()string'] | BaseTwoArgs['tuple']['methodCommon()string']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'methodCommon()string' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
}

/**
 * A client to make calls to the BaseTwo smart contract
 */
export class BaseTwoClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `BaseTwoClient`
   *
   * @param appClient An `AppClient` instance which has been created with the BaseTwo app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `BaseTwoClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Checks for decode errors on the given return value and maps the return value to the return type for the given method
   * @returns The typed return value or undefined if there was no value
   */
  decodeReturnValue<TSignature extends BaseTwoNonVoidMethodSignatures>(method: TSignature, returnValue: ABIReturn | undefined) {
    return returnValue !== undefined ? getArc56ReturnValue<MethodReturn<TSignature>>(returnValue, this.appClient.getABIMethod(method), APP_SPEC.structs) : undefined
  }

  /**
   * Returns a new `BaseTwoClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<BaseTwoClient> {
    return new BaseTwoClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `BaseTwoClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<BaseTwoClient> {
    return new BaseTwoClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Makes a clear_state call to an existing instance of the BaseTwo smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the BaseTwo smart contract using the `methodTwo()string` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    methodTwo: (params: CallParams<BaseTwoArgs['obj']['methodTwo()string'] | BaseTwoArgs['tuple']['methodTwo()string']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(BaseTwoParamsFactory.methodTwo(params))
    },

    /**
     * Makes a call to the BaseTwo smart contract using the `b2CantOverride()string` ABI method.
     *
    * Because CommonBase implements this method, and MRO for polytype is depth first; this method
    should not be accessible from MultiBases as the MRO should be `BaseOne => CommonBase => BaseTwo => CommonBase`
    and since CommonBase provides an implementation, this one should not be used

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    b2CantOverride: (params: CallParams<BaseTwoArgs['obj']['b2CantOverride()string'] | BaseTwoArgs['tuple']['b2CantOverride()string']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(BaseTwoParamsFactory.b2CantOverride(params))
    },

    /**
     * Makes a call to the BaseTwo smart contract using the `methodCommon()string` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    methodCommon: (params: CallParams<BaseTwoArgs['obj']['methodCommon()string'] | BaseTwoArgs['tuple']['methodCommon()string']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(BaseTwoParamsFactory.methodCommon(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Makes a clear_state call to an existing instance of the BaseTwo smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the BaseTwo smart contract using the `methodTwo()string` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    methodTwo: (params: CallParams<BaseTwoArgs['obj']['methodTwo()string'] | BaseTwoArgs['tuple']['methodTwo()string']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(BaseTwoParamsFactory.methodTwo(params))
    },

    /**
     * Makes a call to the BaseTwo smart contract using the `b2CantOverride()string` ABI method.
     *
    * Because CommonBase implements this method, and MRO for polytype is depth first; this method
    should not be accessible from MultiBases as the MRO should be `BaseOne => CommonBase => BaseTwo => CommonBase`
    and since CommonBase provides an implementation, this one should not be used

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    b2CantOverride: (params: CallParams<BaseTwoArgs['obj']['b2CantOverride()string'] | BaseTwoArgs['tuple']['b2CantOverride()string']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(BaseTwoParamsFactory.b2CantOverride(params))
    },

    /**
     * Makes a call to the BaseTwo smart contract using the `methodCommon()string` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    methodCommon: (params: CallParams<BaseTwoArgs['obj']['methodCommon()string'] | BaseTwoArgs['tuple']['methodCommon()string']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(BaseTwoParamsFactory.methodCommon(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Makes a clear_state call to an existing instance of the BaseTwo smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the BaseTwo smart contract using the `methodTwo()string` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    methodTwo: async (params: CallParams<BaseTwoArgs['obj']['methodTwo()string'] | BaseTwoArgs['tuple']['methodTwo()string']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(BaseTwoParamsFactory.methodTwo(params))
      return {...result, return: result.return as unknown as (undefined | BaseTwoReturns['methodTwo()string'])}
    },

    /**
     * Makes a call to the BaseTwo smart contract using the `b2CantOverride()string` ABI method.
     *
    * Because CommonBase implements this method, and MRO for polytype is depth first; this method
    should not be accessible from MultiBases as the MRO should be `BaseOne => CommonBase => BaseTwo => CommonBase`
    and since CommonBase provides an implementation, this one should not be used

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    b2CantOverride: async (params: CallParams<BaseTwoArgs['obj']['b2CantOverride()string'] | BaseTwoArgs['tuple']['b2CantOverride()string']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(BaseTwoParamsFactory.b2CantOverride(params))
      return {...result, return: result.return as unknown as (undefined | BaseTwoReturns['b2CantOverride()string'])}
    },

    /**
     * Makes a call to the BaseTwo smart contract using the `methodCommon()string` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    methodCommon: async (params: CallParams<BaseTwoArgs['obj']['methodCommon()string'] | BaseTwoArgs['tuple']['methodCommon()string']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(BaseTwoParamsFactory.methodCommon(params))
      return {...result, return: result.return as unknown as (undefined | BaseTwoReturns['methodCommon()string'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new BaseTwoClient(this.appClient.clone(params))
  }

  /**
   * Methods to access state for the current BaseTwo app
   */
  state = {
    /**
     * Methods to access global state for the current BaseTwo app
     */
    global: {
      /**
       * Get all current keyed values from global state
       */
      getAll: async (): Promise<Partial<Expand<GlobalKeysState>>> => {
        const result = await this.appClient.state.global.getAll()
        return {
          stateTwo: result.stateTwo,
          stateCommon: result.stateCommon,
        }
      },
      /**
       * Get the current value of the stateTwo key in global state
       */
      stateTwo: async (): Promise<string | undefined> => { return (await this.appClient.state.global.getValue("stateTwo")) as string | undefined },
      /**
       * Get the current value of the stateCommon key in global state
       */
      stateCommon: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("stateCommon")) as bigint | undefined },
    },
  }

  public newGroup(): BaseTwoComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a methodTwo()string method call against the BaseTwo contract
       */
      methodTwo(params: CallParams<BaseTwoArgs['obj']['methodTwo()string'] | BaseTwoArgs['tuple']['methodTwo()string']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.methodTwo(params)))
        resultMappers.push((v) => client.decodeReturnValue('methodTwo()string', v))
        return this
      },
      /**
       * Add a b2CantOverride()string method call against the BaseTwo contract
       */
      b2CantOverride(params: CallParams<BaseTwoArgs['obj']['b2CantOverride()string'] | BaseTwoArgs['tuple']['b2CantOverride()string']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.b2CantOverride(params)))
        resultMappers.push((v) => client.decodeReturnValue('b2CantOverride()string', v))
        return this
      },
      /**
       * Add a methodCommon()string method call against the BaseTwo contract
       */
      methodCommon(params: CallParams<BaseTwoArgs['obj']['methodCommon()string'] | BaseTwoArgs['tuple']['methodCommon()string']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.methodCommon(params)))
        resultMappers.push((v) => client.decodeReturnValue('methodCommon()string', v))
        return this
      },
      /**
       * Add a clear state call to the BaseTwo contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as BaseTwoComposer
  }
}
export type BaseTwoComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the methodTwo()string ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  methodTwo(params?: CallParams<BaseTwoArgs['obj']['methodTwo()string'] | BaseTwoArgs['tuple']['methodTwo()string']>): BaseTwoComposer<[...TReturns, BaseTwoReturns['methodTwo()string'] | undefined]>

  /**
   * Calls the b2CantOverride()string ABI method.
   *
  * Because CommonBase implements this method, and MRO for polytype is depth first; this method
  should not be accessible from MultiBases as the MRO should be `BaseOne => CommonBase => BaseTwo => CommonBase`
  and since CommonBase provides an implementation, this one should not be used

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  b2CantOverride(params?: CallParams<BaseTwoArgs['obj']['b2CantOverride()string'] | BaseTwoArgs['tuple']['b2CantOverride()string']>): BaseTwoComposer<[...TReturns, BaseTwoReturns['b2CantOverride()string'] | undefined]>

  /**
   * Calls the methodCommon()string ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  methodCommon(params?: CallParams<BaseTwoArgs['obj']['methodCommon()string'] | BaseTwoArgs['tuple']['methodCommon()string']>): BaseTwoComposer<[...TReturns, BaseTwoReturns['methodCommon()string'] | undefined]>

  /**
   * Makes a clear_state call to an existing instance of the BaseTwo smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): BaseTwoComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): BaseTwoComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<BaseTwoComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<BaseTwoComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<BaseTwoComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<BaseTwoComposerResults<TReturns>>
}
export type BaseTwoComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

