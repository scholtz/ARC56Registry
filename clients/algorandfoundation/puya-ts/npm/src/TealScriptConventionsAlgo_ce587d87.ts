/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"TealScriptConventionsAlgo","structs":{},"methods":[{"name":"noMoreThanks","args":[{"type":"uint64","name":"arg"}],"returns":{"type":"uint64"},"actions":{"create":[],"call":["CloseOut"]},"readonly":false,"events":[],"recommendations":{}},{"name":"createApplication","args":[{"type":"byte[]","name":"value"}],"returns":{"type":"void"},"actions":{"create":["NoOp","DeleteApplication"],"call":[]},"readonly":false,"events":[],"recommendations":{}},{"name":"setLocal","args":[{"type":"string","name":"value"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}},{"name":"optInToApplication","args":[],"returns":{"type":"void"},"actions":{"create":[],"call":["OptIn"]},"readonly":false,"events":[],"recommendations":{}},{"name":"updateApplication","args":[],"returns":{"type":"void"},"actions":{"create":[],"call":["UpdateApplication"]},"readonly":false,"events":[],"recommendations":{}}],"arcs":[22,28],"networks":{},"state":{"schema":{"global":{"ints":0,"bytes":1},"local":{"ints":0,"bytes":1}},"keys":{"global":{"global":{"keyType":"AVMString","valueType":"AVMBytes","key":"Z2xvYmFs"}},"local":{"local":{"keyType":"AVMString","valueType":"AVMString","key":"bG9jYWw="}},"box":{}},"maps":{"global":{},"local":{},"box":{}}},"bareActions":{"create":[],"call":["DeleteApplication"]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[141,169],"errorMessage":"invalid array length header"},{"pc":[148,176],"errorMessage":"invalid number of bytes for arc4.dynamic_array<arc4.uint8>"},{"pc":[124],"errorMessage":"invalid number of bytes for arc4.uint64"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"events":[]} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


/**
 * The argument types for the TealScriptConventionsAlgo contract
 */
export type TealScriptConventionsAlgoArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'noMoreThanks(uint64)uint64': {
      arg: bigint | number
    }
    'createApplication(byte[])void': {
      value: Uint8Array
    }
    'setLocal(string)void': {
      value: string
    }
    'optInToApplication()void': Record<string, never>
    'updateApplication()void': Record<string, never>
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'noMoreThanks(uint64)uint64': [arg: bigint | number]
    'createApplication(byte[])void': [value: Uint8Array]
    'setLocal(string)void': [value: string]
    'optInToApplication()void': []
    'updateApplication()void': []
  }
}

/**
 * The return type for each method
 */
export type TealScriptConventionsAlgoReturns = {
  'noMoreThanks(uint64)uint64': bigint
  'createApplication(byte[])void': void
  'setLocal(string)void': void
  'optInToApplication()void': void
  'updateApplication()void': void
}

/**
 * Defines the types of available calls and state of the TealScriptConventionsAlgo smart contract.
 */
export type TealScriptConventionsAlgoTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'noMoreThanks(uint64)uint64' | 'noMoreThanks', {
      argsObj: TealScriptConventionsAlgoArgs['obj']['noMoreThanks(uint64)uint64']
      argsTuple: TealScriptConventionsAlgoArgs['tuple']['noMoreThanks(uint64)uint64']
      returns: TealScriptConventionsAlgoReturns['noMoreThanks(uint64)uint64']
    }>
    & Record<'createApplication(byte[])void' | 'createApplication', {
      argsObj: TealScriptConventionsAlgoArgs['obj']['createApplication(byte[])void']
      argsTuple: TealScriptConventionsAlgoArgs['tuple']['createApplication(byte[])void']
      returns: TealScriptConventionsAlgoReturns['createApplication(byte[])void']
    }>
    & Record<'setLocal(string)void' | 'setLocal', {
      argsObj: TealScriptConventionsAlgoArgs['obj']['setLocal(string)void']
      argsTuple: TealScriptConventionsAlgoArgs['tuple']['setLocal(string)void']
      returns: TealScriptConventionsAlgoReturns['setLocal(string)void']
    }>
    & Record<'optInToApplication()void' | 'optInToApplication', {
      argsObj: TealScriptConventionsAlgoArgs['obj']['optInToApplication()void']
      argsTuple: TealScriptConventionsAlgoArgs['tuple']['optInToApplication()void']
      returns: TealScriptConventionsAlgoReturns['optInToApplication()void']
    }>
    & Record<'updateApplication()void' | 'updateApplication', {
      argsObj: TealScriptConventionsAlgoArgs['obj']['updateApplication()void']
      argsTuple: TealScriptConventionsAlgoArgs['tuple']['updateApplication()void']
      returns: TealScriptConventionsAlgoReturns['updateApplication()void']
    }>
  /**
   * Defines the shape of the state of the application.
   */
  state: {
    global: {
      keys: {
        global: BinaryState
      }
      maps: {}
    }
    local: {
      keys: {
        local: string
      }
      maps: {}
    }
  }
}

/**
 * Defines the possible abi call signatures.
 */
export type TealScriptConventionsAlgoSignatures = keyof TealScriptConventionsAlgoTypes['methods']
/**
 * Defines the possible abi call signatures for methods that return a non-void value.
 */
export type TealScriptConventionsAlgoNonVoidMethodSignatures = keyof TealScriptConventionsAlgoTypes['methods'] extends infer T ? T extends keyof TealScriptConventionsAlgoTypes['methods'] ? MethodReturn<T> extends void ? never : T  : never : never
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the TealScriptConventionsAlgo smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends TealScriptConventionsAlgoSignatures> = TealScriptConventionsAlgoTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the TealScriptConventionsAlgo smart contract to the method's return type
 */
export type MethodReturn<TSignature extends TealScriptConventionsAlgoSignatures> = TealScriptConventionsAlgoTypes['methods'][TSignature]['returns']

/**
 * Defines the shape of the keyed global state of the application.
 */
export type GlobalKeysState = TealScriptConventionsAlgoTypes['state']['global']['keys']

/**
 * Defines the shape of the keyed local state of the application.
 */
export type LocalKeysState = TealScriptConventionsAlgoTypes['state']['local']['keys']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the TealScriptConventionsAlgo smart contract
 */
export abstract class TealScriptConventionsAlgoParamsFactory {
  /**
   * Gets available optIn ABI call param factories
   */
  static get optIn() {
    return {
      /**
       * Constructs opt-in ABI call params for the TealScriptConventionsAlgo smart contract using the optInToApplication()void ABI method
       *
       * @param params Parameters for the call
       * @returns An `AppClientMethodCallParams` object for the call
       */
      optInToApplication(params: CallParams<TealScriptConventionsAlgoArgs['obj']['optInToApplication()void'] | TealScriptConventionsAlgoArgs['tuple']['optInToApplication()void']>): AppClientMethodCallParams {
        return {
          ...params,
          method: 'optInToApplication()void' as const,
          args: Array.isArray(params.args) ? params.args : [],
        }
      },
    }
  }

  /**
   * Gets available closeOut ABI call param factories
   */
  static get closeOut() {
    return {
      /**
       * Constructs close out ABI call params for the TealScriptConventionsAlgo smart contract using the noMoreThanks(uint64)uint64 ABI method
       *
       * @param params Parameters for the call
       * @returns An `AppClientMethodCallParams` object for the call
       */
      noMoreThanks(params: CallParams<TealScriptConventionsAlgoArgs['obj']['noMoreThanks(uint64)uint64'] | TealScriptConventionsAlgoArgs['tuple']['noMoreThanks(uint64)uint64']>): AppClientMethodCallParams {
        return {
          ...params,
          method: 'noMoreThanks(uint64)uint64' as const,
          args: Array.isArray(params.args) ? params.args : [params.args.arg],
        }
      },
    }
  }

  /**
   * Constructs a no op call for the setLocal(string)void ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static setLocal(params: CallParams<TealScriptConventionsAlgoArgs['obj']['setLocal(string)void'] | TealScriptConventionsAlgoArgs['tuple']['setLocal(string)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'setLocal(string)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.value],
    }
  }
}

/**
 * A client to make calls to the TealScriptConventionsAlgo smart contract
 */
export class TealScriptConventionsAlgoClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `TealScriptConventionsAlgoClient`
   *
   * @param appClient An `AppClient` instance which has been created with the TealScriptConventionsAlgo app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `TealScriptConventionsAlgoClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Checks for decode errors on the given return value and maps the return value to the return type for the given method
   * @returns The typed return value or undefined if there was no value
   */
  decodeReturnValue<TSignature extends TealScriptConventionsAlgoNonVoidMethodSignatures>(method: TSignature, returnValue: ABIReturn | undefined) {
    return returnValue !== undefined ? getArc56ReturnValue<MethodReturn<TSignature>>(returnValue, this.appClient.getABIMethod(method), APP_SPEC.structs) : undefined
  }

  /**
   * Returns a new `TealScriptConventionsAlgoClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<TealScriptConventionsAlgoClient> {
    return new TealScriptConventionsAlgoClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `TealScriptConventionsAlgoClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<TealScriptConventionsAlgoClient> {
    return new TealScriptConventionsAlgoClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Gets available optIn methods
     */
    optIn: {
      /**
       * Opts the user into an existing instance of the TealScriptConventionsAlgo smart contract using the `optInToApplication()void` ABI method.
       *
       * @param params The params for the smart contract call
       * @returns The optIn params
       */
      optInToApplication: (params: CallParams<TealScriptConventionsAlgoArgs['obj']['optInToApplication()void'] | TealScriptConventionsAlgoArgs['tuple']['optInToApplication()void']> = {args: []}) => {
        return this.appClient.params.optIn(TealScriptConventionsAlgoParamsFactory.optIn.optInToApplication(params))
      },

    },

    /**
     * Gets available closeOut methods
     */
    closeOut: {
      /**
       * Makes a close out call to an existing instance of the TealScriptConventionsAlgo smart contract using the `noMoreThanks(uint64)uint64` ABI method.
       *
       * @param params The params for the smart contract call
       * @returns The closeOut params
       */
      noMoreThanks: (params: CallParams<TealScriptConventionsAlgoArgs['obj']['noMoreThanks(uint64)uint64'] | TealScriptConventionsAlgoArgs['tuple']['noMoreThanks(uint64)uint64']>) => {
        return this.appClient.params.closeOut(TealScriptConventionsAlgoParamsFactory.closeOut.noMoreThanks(params))
      },

    },

    /**
     * Makes a clear_state call to an existing instance of the TealScriptConventionsAlgo smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the TealScriptConventionsAlgo smart contract using the `setLocal(string)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    setLocal: (params: CallParams<TealScriptConventionsAlgoArgs['obj']['setLocal(string)void'] | TealScriptConventionsAlgoArgs['tuple']['setLocal(string)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(TealScriptConventionsAlgoParamsFactory.setLocal(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Gets available optIn methods
     */
    optIn: {
      /**
       * Opts the user into an existing instance of the TealScriptConventionsAlgo smart contract using the `optInToApplication()void` ABI method.
       *
       * @param params The params for the smart contract call
       * @returns The optIn transaction
       */
      optInToApplication: (params: CallParams<TealScriptConventionsAlgoArgs['obj']['optInToApplication()void'] | TealScriptConventionsAlgoArgs['tuple']['optInToApplication()void']> = {args: []}) => {
        return this.appClient.createTransaction.optIn(TealScriptConventionsAlgoParamsFactory.optIn.optInToApplication(params))
      },

    },

    /**
     * Gets available closeOut methods
     */
    closeOut: {
      /**
       * Makes a close out call to an existing instance of the TealScriptConventionsAlgo smart contract using the `noMoreThanks(uint64)uint64` ABI method.
       *
       * @param params The params for the smart contract call
       * @returns The closeOut transaction
       */
      noMoreThanks: (params: CallParams<TealScriptConventionsAlgoArgs['obj']['noMoreThanks(uint64)uint64'] | TealScriptConventionsAlgoArgs['tuple']['noMoreThanks(uint64)uint64']>) => {
        return this.appClient.createTransaction.closeOut(TealScriptConventionsAlgoParamsFactory.closeOut.noMoreThanks(params))
      },

    },

    /**
     * Makes a clear_state call to an existing instance of the TealScriptConventionsAlgo smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the TealScriptConventionsAlgo smart contract using the `setLocal(string)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    setLocal: (params: CallParams<TealScriptConventionsAlgoArgs['obj']['setLocal(string)void'] | TealScriptConventionsAlgoArgs['tuple']['setLocal(string)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(TealScriptConventionsAlgoParamsFactory.setLocal(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Gets available optIn methods
     */
    optIn: {
      /**
       * Opts the user into an existing instance of the TealScriptConventionsAlgo smart contract using the `optInToApplication()void` ABI method.
       *
       * @param params The params for the smart contract call
       * @returns The optIn result
       */
      optInToApplication: async (params: CallParams<TealScriptConventionsAlgoArgs['obj']['optInToApplication()void'] | TealScriptConventionsAlgoArgs['tuple']['optInToApplication()void']> & SendParams = {args: []}) => {
        const result = await this.appClient.send.optIn(TealScriptConventionsAlgoParamsFactory.optIn.optInToApplication(params))
        return {...result, return: result.return as unknown as (undefined | TealScriptConventionsAlgoReturns['optInToApplication()void'])}
      },

    },

    /**
     * Gets available closeOut methods
     */
    closeOut: {
      /**
       * Makes a close out call to an existing instance of the TealScriptConventionsAlgo smart contract using the `noMoreThanks(uint64)uint64` ABI method.
       *
       * @param params The params for the smart contract call
       * @returns The closeOut result
       */
      noMoreThanks: async (params: CallParams<TealScriptConventionsAlgoArgs['obj']['noMoreThanks(uint64)uint64'] | TealScriptConventionsAlgoArgs['tuple']['noMoreThanks(uint64)uint64']> & SendParams) => {
        const result = await this.appClient.send.closeOut(TealScriptConventionsAlgoParamsFactory.closeOut.noMoreThanks(params))
        return {...result, return: result.return as unknown as (undefined | TealScriptConventionsAlgoReturns['noMoreThanks(uint64)uint64'])}
      },

    },

    /**
     * Makes a clear_state call to an existing instance of the TealScriptConventionsAlgo smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the TealScriptConventionsAlgo smart contract using the `setLocal(string)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    setLocal: async (params: CallParams<TealScriptConventionsAlgoArgs['obj']['setLocal(string)void'] | TealScriptConventionsAlgoArgs['tuple']['setLocal(string)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(TealScriptConventionsAlgoParamsFactory.setLocal(params))
      return {...result, return: result.return as unknown as (undefined | TealScriptConventionsAlgoReturns['setLocal(string)void'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new TealScriptConventionsAlgoClient(this.appClient.clone(params))
  }

  /**
   * Methods to access state for the current TealScriptConventionsAlgo app
   */
  state = {
    /**
     * Methods to access global state for the current TealScriptConventionsAlgo app
     */
    global: {
      /**
       * Get all current keyed values from global state
       */
      getAll: async (): Promise<Partial<Expand<GlobalKeysState>>> => {
        const result = await this.appClient.state.global.getAll()
        return {
          global: new BinaryStateValue(result.global),
        }
      },
      /**
       * Get the current value of the global key in global state
       */
      global: async (): Promise<BinaryState> => { return new BinaryStateValue((await this.appClient.state.global.getValue("global")) as Uint8Array | undefined) },
    },
    /**
     * Methods to access local state for the current TealScriptConventionsAlgo app
     */
    local: (address: string | Address) => {
      const encodedAddress = typeof address === 'string' ? address : encodeAddress(address.publicKey)
      return {
        /**
         * Get all current keyed values from local state
         */
        getAll: async (): Promise<Partial<Expand<LocalKeysState>>> => {
          const result = await this.appClient.state.local(encodedAddress).getAll()
          return {
            local: result.local,
          }
        },
        /**
         * Get the current value of the local key in local state
         */
        local: async (): Promise<string | undefined> => { return (await this.appClient.state.local(encodedAddress).getValue("local")) as string | undefined },
      }
    },
  }

  public newGroup(): TealScriptConventionsAlgoComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a setLocal(string)void method call against the TealScriptConventionsAlgo contract
       */
      setLocal(params: CallParams<TealScriptConventionsAlgoArgs['obj']['setLocal(string)void'] | TealScriptConventionsAlgoArgs['tuple']['setLocal(string)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.setLocal(params)))
        resultMappers.push(undefined)
        return this
      },
      get optIn() {
        return {
          optInToApplication: (params: CallParams<TealScriptConventionsAlgoArgs['obj']['optInToApplication()void'] | TealScriptConventionsAlgoArgs['tuple']['optInToApplication()void']>) => {
            promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.optIn.optInToApplication(params)))
            resultMappers.push(undefined)
            return this
          },
        }
      },
      get closeOut() {
        return {
          noMoreThanks: (params: CallParams<TealScriptConventionsAlgoArgs['obj']['noMoreThanks(uint64)uint64'] | TealScriptConventionsAlgoArgs['tuple']['noMoreThanks(uint64)uint64']>) => {
            promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.closeOut.noMoreThanks(params)))
            resultMappers.push((v) => client.decodeReturnValue('noMoreThanks(uint64)uint64', v))
            return this
          },
        }
      },
      /**
       * Add a clear state call to the TealScriptConventionsAlgo contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as TealScriptConventionsAlgoComposer
  }
}
export type TealScriptConventionsAlgoComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the setLocal(string)void ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  setLocal(params?: CallParams<TealScriptConventionsAlgoArgs['obj']['setLocal(string)void'] | TealScriptConventionsAlgoArgs['tuple']['setLocal(string)void']>): TealScriptConventionsAlgoComposer<[...TReturns, TealScriptConventionsAlgoReturns['setLocal(string)void'] | undefined]>

  /**
   * Gets available optIn methods
   */
  readonly optIn: {
    /**
     * Opts the user into an existing instance of the TealScriptConventionsAlgo smart contract using the optInToApplication()void ABI method.
     *
     * @param args The arguments for the smart contract call
     * @param params Any additional parameters for the call
     * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
     */
    optInToApplication(params?: CallParams<TealScriptConventionsAlgoArgs['obj']['optInToApplication()void'] | TealScriptConventionsAlgoArgs['tuple']['optInToApplication()void']>): TealScriptConventionsAlgoComposer<[...TReturns, TealScriptConventionsAlgoReturns['optInToApplication()void'] | undefined]>
  }

  /**
   * Gets available closeOut methods
   */
  readonly closeOut: {
    /**
     * Makes a close out call to an existing instance of the TealScriptConventionsAlgo smart contract using the noMoreThanks(uint64)uint64 ABI method.
     *
     * @param args The arguments for the smart contract call
     * @param params Any additional parameters for the call
     * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
     */
    noMoreThanks(params?: CallParams<TealScriptConventionsAlgoArgs['obj']['noMoreThanks(uint64)uint64'] | TealScriptConventionsAlgoArgs['tuple']['noMoreThanks(uint64)uint64']>): TealScriptConventionsAlgoComposer<[...TReturns, TealScriptConventionsAlgoReturns['noMoreThanks(uint64)uint64'] | undefined]>
  }

  /**
   * Makes a clear_state call to an existing instance of the TealScriptConventionsAlgo smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): TealScriptConventionsAlgoComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): TealScriptConventionsAlgoComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<TealScriptConventionsAlgoComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<TealScriptConventionsAlgoComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<TealScriptConventionsAlgoComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<TealScriptConventionsAlgoComposerResults<TReturns>>
}
export type TealScriptConventionsAlgoComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

