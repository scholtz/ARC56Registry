/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"ArrayDestructuringAlgo","structs":{},"methods":[{"name":"testNested","args":[{"type":"(uint64,(uint512,uint512))","name":"arg"}],"returns":{"type":"(uint64,(uint512,uint512),uint64,uint512)"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}},{"name":"test","args":[],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}},{"name":"testLiteralDestructuring","args":[],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}},{"name":"produceFixed","args":[],"returns":{"type":"uint64[3]"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}}],"arcs":[22,28],"networks":{},"state":{"schema":{"global":{"ints":0,"bytes":0},"local":{"ints":0,"bytes":0}},"keys":{"global":{},"local":{},"box":{}},"maps":{"global":{},"local":{},"box":{}}},"bareActions":{"create":["NoOp"],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[107],"errorMessage":"invalid number of bytes for arc4.tuple<arc4.uint64,arc4.tuple<arc4.uint512,arc4.uint512>>"},{"pc":[130,143],"errorMessage":"overflow"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"events":[]} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


/**
 * The argument types for the ArrayDestructuringAlgo contract
 */
export type ArrayDestructuringAlgoArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'testNested((uint64,(uint512,uint512)))(uint64,(uint512,uint512),uint64,uint512)': {
      arg: [bigint | number, [bigint | number, bigint | number]]
    }
    'test()void': Record<string, never>
    'testLiteralDestructuring()void': Record<string, never>
    'produceFixed()uint64[3]': Record<string, never>
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'testNested((uint64,(uint512,uint512)))(uint64,(uint512,uint512),uint64,uint512)': [arg: [bigint | number, [bigint | number, bigint | number]]]
    'test()void': []
    'testLiteralDestructuring()void': []
    'produceFixed()uint64[3]': []
  }
}

/**
 * The return type for each method
 */
export type ArrayDestructuringAlgoReturns = {
  'testNested((uint64,(uint512,uint512)))(uint64,(uint512,uint512),uint64,uint512)': [bigint, [bigint, bigint], bigint, bigint]
  'test()void': void
  'testLiteralDestructuring()void': void
  'produceFixed()uint64[3]': [bigint, bigint, bigint]
}

/**
 * Defines the types of available calls and state of the ArrayDestructuringAlgo smart contract.
 */
export type ArrayDestructuringAlgoTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'testNested((uint64,(uint512,uint512)))(uint64,(uint512,uint512),uint64,uint512)' | 'testNested', {
      argsObj: ArrayDestructuringAlgoArgs['obj']['testNested((uint64,(uint512,uint512)))(uint64,(uint512,uint512),uint64,uint512)']
      argsTuple: ArrayDestructuringAlgoArgs['tuple']['testNested((uint64,(uint512,uint512)))(uint64,(uint512,uint512),uint64,uint512)']
      returns: ArrayDestructuringAlgoReturns['testNested((uint64,(uint512,uint512)))(uint64,(uint512,uint512),uint64,uint512)']
    }>
    & Record<'test()void' | 'test', {
      argsObj: ArrayDestructuringAlgoArgs['obj']['test()void']
      argsTuple: ArrayDestructuringAlgoArgs['tuple']['test()void']
      returns: ArrayDestructuringAlgoReturns['test()void']
    }>
    & Record<'testLiteralDestructuring()void' | 'testLiteralDestructuring', {
      argsObj: ArrayDestructuringAlgoArgs['obj']['testLiteralDestructuring()void']
      argsTuple: ArrayDestructuringAlgoArgs['tuple']['testLiteralDestructuring()void']
      returns: ArrayDestructuringAlgoReturns['testLiteralDestructuring()void']
    }>
    & Record<'produceFixed()uint64[3]' | 'produceFixed', {
      argsObj: ArrayDestructuringAlgoArgs['obj']['produceFixed()uint64[3]']
      argsTuple: ArrayDestructuringAlgoArgs['tuple']['produceFixed()uint64[3]']
      returns: ArrayDestructuringAlgoReturns['produceFixed()uint64[3]']
    }>
}

/**
 * Defines the possible abi call signatures.
 */
export type ArrayDestructuringAlgoSignatures = keyof ArrayDestructuringAlgoTypes['methods']
/**
 * Defines the possible abi call signatures for methods that return a non-void value.
 */
export type ArrayDestructuringAlgoNonVoidMethodSignatures = keyof ArrayDestructuringAlgoTypes['methods'] extends infer T ? T extends keyof ArrayDestructuringAlgoTypes['methods'] ? MethodReturn<T> extends void ? never : T  : never : never
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the ArrayDestructuringAlgo smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends ArrayDestructuringAlgoSignatures> = ArrayDestructuringAlgoTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the ArrayDestructuringAlgo smart contract to the method's return type
 */
export type MethodReturn<TSignature extends ArrayDestructuringAlgoSignatures> = ArrayDestructuringAlgoTypes['methods'][TSignature]['returns']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the ArrayDestructuringAlgo smart contract
 */
export abstract class ArrayDestructuringAlgoParamsFactory {
  /**
   * Constructs a no op call for the testNested((uint64,(uint512,uint512)))(uint64,(uint512,uint512),uint64,uint512) ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static testNested(params: CallParams<ArrayDestructuringAlgoArgs['obj']['testNested((uint64,(uint512,uint512)))(uint64,(uint512,uint512),uint64,uint512)'] | ArrayDestructuringAlgoArgs['tuple']['testNested((uint64,(uint512,uint512)))(uint64,(uint512,uint512),uint64,uint512)']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'testNested((uint64,(uint512,uint512)))(uint64,(uint512,uint512),uint64,uint512)' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.arg],
    }
  }
  /**
   * Constructs a no op call for the test()void ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static test(params: CallParams<ArrayDestructuringAlgoArgs['obj']['test()void'] | ArrayDestructuringAlgoArgs['tuple']['test()void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'test()void' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the testLiteralDestructuring()void ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static testLiteralDestructuring(params: CallParams<ArrayDestructuringAlgoArgs['obj']['testLiteralDestructuring()void'] | ArrayDestructuringAlgoArgs['tuple']['testLiteralDestructuring()void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'testLiteralDestructuring()void' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the produceFixed()uint64[3] ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static produceFixed(params: CallParams<ArrayDestructuringAlgoArgs['obj']['produceFixed()uint64[3]'] | ArrayDestructuringAlgoArgs['tuple']['produceFixed()uint64[3]']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'produceFixed()uint64[3]' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
}

/**
 * A client to make calls to the ArrayDestructuringAlgo smart contract
 */
export class ArrayDestructuringAlgoClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `ArrayDestructuringAlgoClient`
   *
   * @param appClient An `AppClient` instance which has been created with the ArrayDestructuringAlgo app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `ArrayDestructuringAlgoClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Checks for decode errors on the given return value and maps the return value to the return type for the given method
   * @returns The typed return value or undefined if there was no value
   */
  decodeReturnValue<TSignature extends ArrayDestructuringAlgoNonVoidMethodSignatures>(method: TSignature, returnValue: ABIReturn | undefined) {
    return returnValue !== undefined ? getArc56ReturnValue<MethodReturn<TSignature>>(returnValue, this.appClient.getABIMethod(method), APP_SPEC.structs) : undefined
  }

  /**
   * Returns a new `ArrayDestructuringAlgoClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<ArrayDestructuringAlgoClient> {
    return new ArrayDestructuringAlgoClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `ArrayDestructuringAlgoClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<ArrayDestructuringAlgoClient> {
    return new ArrayDestructuringAlgoClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Makes a clear_state call to an existing instance of the ArrayDestructuringAlgo smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the ArrayDestructuringAlgo smart contract using the `testNested((uint64,(uint512,uint512)))(uint64,(uint512,uint512),uint64,uint512)` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    testNested: (params: CallParams<ArrayDestructuringAlgoArgs['obj']['testNested((uint64,(uint512,uint512)))(uint64,(uint512,uint512),uint64,uint512)'] | ArrayDestructuringAlgoArgs['tuple']['testNested((uint64,(uint512,uint512)))(uint64,(uint512,uint512),uint64,uint512)']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(ArrayDestructuringAlgoParamsFactory.testNested(params))
    },

    /**
     * Makes a call to the ArrayDestructuringAlgo smart contract using the `test()void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    test: (params: CallParams<ArrayDestructuringAlgoArgs['obj']['test()void'] | ArrayDestructuringAlgoArgs['tuple']['test()void']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(ArrayDestructuringAlgoParamsFactory.test(params))
    },

    /**
     * Makes a call to the ArrayDestructuringAlgo smart contract using the `testLiteralDestructuring()void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    testLiteralDestructuring: (params: CallParams<ArrayDestructuringAlgoArgs['obj']['testLiteralDestructuring()void'] | ArrayDestructuringAlgoArgs['tuple']['testLiteralDestructuring()void']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(ArrayDestructuringAlgoParamsFactory.testLiteralDestructuring(params))
    },

    /**
     * Makes a call to the ArrayDestructuringAlgo smart contract using the `produceFixed()uint64[3]` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    produceFixed: (params: CallParams<ArrayDestructuringAlgoArgs['obj']['produceFixed()uint64[3]'] | ArrayDestructuringAlgoArgs['tuple']['produceFixed()uint64[3]']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(ArrayDestructuringAlgoParamsFactory.produceFixed(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Makes a clear_state call to an existing instance of the ArrayDestructuringAlgo smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the ArrayDestructuringAlgo smart contract using the `testNested((uint64,(uint512,uint512)))(uint64,(uint512,uint512),uint64,uint512)` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    testNested: (params: CallParams<ArrayDestructuringAlgoArgs['obj']['testNested((uint64,(uint512,uint512)))(uint64,(uint512,uint512),uint64,uint512)'] | ArrayDestructuringAlgoArgs['tuple']['testNested((uint64,(uint512,uint512)))(uint64,(uint512,uint512),uint64,uint512)']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(ArrayDestructuringAlgoParamsFactory.testNested(params))
    },

    /**
     * Makes a call to the ArrayDestructuringAlgo smart contract using the `test()void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    test: (params: CallParams<ArrayDestructuringAlgoArgs['obj']['test()void'] | ArrayDestructuringAlgoArgs['tuple']['test()void']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(ArrayDestructuringAlgoParamsFactory.test(params))
    },

    /**
     * Makes a call to the ArrayDestructuringAlgo smart contract using the `testLiteralDestructuring()void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    testLiteralDestructuring: (params: CallParams<ArrayDestructuringAlgoArgs['obj']['testLiteralDestructuring()void'] | ArrayDestructuringAlgoArgs['tuple']['testLiteralDestructuring()void']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(ArrayDestructuringAlgoParamsFactory.testLiteralDestructuring(params))
    },

    /**
     * Makes a call to the ArrayDestructuringAlgo smart contract using the `produceFixed()uint64[3]` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    produceFixed: (params: CallParams<ArrayDestructuringAlgoArgs['obj']['produceFixed()uint64[3]'] | ArrayDestructuringAlgoArgs['tuple']['produceFixed()uint64[3]']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(ArrayDestructuringAlgoParamsFactory.produceFixed(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Makes a clear_state call to an existing instance of the ArrayDestructuringAlgo smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the ArrayDestructuringAlgo smart contract using the `testNested((uint64,(uint512,uint512)))(uint64,(uint512,uint512),uint64,uint512)` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    testNested: async (params: CallParams<ArrayDestructuringAlgoArgs['obj']['testNested((uint64,(uint512,uint512)))(uint64,(uint512,uint512),uint64,uint512)'] | ArrayDestructuringAlgoArgs['tuple']['testNested((uint64,(uint512,uint512)))(uint64,(uint512,uint512),uint64,uint512)']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(ArrayDestructuringAlgoParamsFactory.testNested(params))
      return {...result, return: result.return as unknown as (undefined | ArrayDestructuringAlgoReturns['testNested((uint64,(uint512,uint512)))(uint64,(uint512,uint512),uint64,uint512)'])}
    },

    /**
     * Makes a call to the ArrayDestructuringAlgo smart contract using the `test()void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    test: async (params: CallParams<ArrayDestructuringAlgoArgs['obj']['test()void'] | ArrayDestructuringAlgoArgs['tuple']['test()void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(ArrayDestructuringAlgoParamsFactory.test(params))
      return {...result, return: result.return as unknown as (undefined | ArrayDestructuringAlgoReturns['test()void'])}
    },

    /**
     * Makes a call to the ArrayDestructuringAlgo smart contract using the `testLiteralDestructuring()void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    testLiteralDestructuring: async (params: CallParams<ArrayDestructuringAlgoArgs['obj']['testLiteralDestructuring()void'] | ArrayDestructuringAlgoArgs['tuple']['testLiteralDestructuring()void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(ArrayDestructuringAlgoParamsFactory.testLiteralDestructuring(params))
      return {...result, return: result.return as unknown as (undefined | ArrayDestructuringAlgoReturns['testLiteralDestructuring()void'])}
    },

    /**
     * Makes a call to the ArrayDestructuringAlgo smart contract using the `produceFixed()uint64[3]` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    produceFixed: async (params: CallParams<ArrayDestructuringAlgoArgs['obj']['produceFixed()uint64[3]'] | ArrayDestructuringAlgoArgs['tuple']['produceFixed()uint64[3]']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(ArrayDestructuringAlgoParamsFactory.produceFixed(params))
      return {...result, return: result.return as unknown as (undefined | ArrayDestructuringAlgoReturns['produceFixed()uint64[3]'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new ArrayDestructuringAlgoClient(this.appClient.clone(params))
  }

  /**
   * Methods to access state for the current ArrayDestructuringAlgo app
   */
  state = {
  }

  public newGroup(): ArrayDestructuringAlgoComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a testNested((uint64,(uint512,uint512)))(uint64,(uint512,uint512),uint64,uint512) method call against the ArrayDestructuringAlgo contract
       */
      testNested(params: CallParams<ArrayDestructuringAlgoArgs['obj']['testNested((uint64,(uint512,uint512)))(uint64,(uint512,uint512),uint64,uint512)'] | ArrayDestructuringAlgoArgs['tuple']['testNested((uint64,(uint512,uint512)))(uint64,(uint512,uint512),uint64,uint512)']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.testNested(params)))
        resultMappers.push((v) => client.decodeReturnValue('testNested((uint64,(uint512,uint512)))(uint64,(uint512,uint512),uint64,uint512)', v))
        return this
      },
      /**
       * Add a test()void method call against the ArrayDestructuringAlgo contract
       */
      test(params: CallParams<ArrayDestructuringAlgoArgs['obj']['test()void'] | ArrayDestructuringAlgoArgs['tuple']['test()void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.test(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a testLiteralDestructuring()void method call against the ArrayDestructuringAlgo contract
       */
      testLiteralDestructuring(params: CallParams<ArrayDestructuringAlgoArgs['obj']['testLiteralDestructuring()void'] | ArrayDestructuringAlgoArgs['tuple']['testLiteralDestructuring()void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.testLiteralDestructuring(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a produceFixed()uint64[3] method call against the ArrayDestructuringAlgo contract
       */
      produceFixed(params: CallParams<ArrayDestructuringAlgoArgs['obj']['produceFixed()uint64[3]'] | ArrayDestructuringAlgoArgs['tuple']['produceFixed()uint64[3]']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.produceFixed(params)))
        resultMappers.push((v) => client.decodeReturnValue('produceFixed()uint64[3]', v))
        return this
      },
      /**
       * Add a clear state call to the ArrayDestructuringAlgo contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as ArrayDestructuringAlgoComposer
  }
}
export type ArrayDestructuringAlgoComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the testNested((uint64,(uint512,uint512)))(uint64,(uint512,uint512),uint64,uint512) ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  testNested(params?: CallParams<ArrayDestructuringAlgoArgs['obj']['testNested((uint64,(uint512,uint512)))(uint64,(uint512,uint512),uint64,uint512)'] | ArrayDestructuringAlgoArgs['tuple']['testNested((uint64,(uint512,uint512)))(uint64,(uint512,uint512),uint64,uint512)']>): ArrayDestructuringAlgoComposer<[...TReturns, ArrayDestructuringAlgoReturns['testNested((uint64,(uint512,uint512)))(uint64,(uint512,uint512),uint64,uint512)'] | undefined]>

  /**
   * Calls the test()void ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  test(params?: CallParams<ArrayDestructuringAlgoArgs['obj']['test()void'] | ArrayDestructuringAlgoArgs['tuple']['test()void']>): ArrayDestructuringAlgoComposer<[...TReturns, ArrayDestructuringAlgoReturns['test()void'] | undefined]>

  /**
   * Calls the testLiteralDestructuring()void ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  testLiteralDestructuring(params?: CallParams<ArrayDestructuringAlgoArgs['obj']['testLiteralDestructuring()void'] | ArrayDestructuringAlgoArgs['tuple']['testLiteralDestructuring()void']>): ArrayDestructuringAlgoComposer<[...TReturns, ArrayDestructuringAlgoReturns['testLiteralDestructuring()void'] | undefined]>

  /**
   * Calls the produceFixed()uint64[3] ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  produceFixed(params?: CallParams<ArrayDestructuringAlgoArgs['obj']['produceFixed()uint64[3]'] | ArrayDestructuringAlgoArgs['tuple']['produceFixed()uint64[3]']>): ArrayDestructuringAlgoComposer<[...TReturns, ArrayDestructuringAlgoReturns['produceFixed()uint64[3]'] | undefined]>

  /**
   * Makes a clear_state call to an existing instance of the ArrayDestructuringAlgo smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): ArrayDestructuringAlgoComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): ArrayDestructuringAlgoComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<ArrayDestructuringAlgoComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<ArrayDestructuringAlgoComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<ArrayDestructuringAlgoComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<ArrayDestructuringAlgoComposerResults<TReturns>>
}
export type ArrayDestructuringAlgoComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

