/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"C2C","structs":{},"methods":[{"name":"testCallToIndex","args":[{"type":"address","name":"account"},{"type":"uint64","name":"appId"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}},{"name":"testCallToValue","args":[{"type":"address","name":"account"},{"type":"uint64","name":"appId"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}},{"name":"testCallToEchoResource","args":[],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}}],"arcs":[22,28],"networks":{},"state":{"schema":{"global":{"ints":0,"bytes":0},"local":{"ints":0,"bytes":0}},"keys":{"global":{},"local":{},"box":{}},"maps":{"global":{},"local":{},"box":{}}},"bareActions":{"create":["NoOp"],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[234,295,778,885],"errorMessage":"Bytes has valid prefix"},{"pc":[245,306],"errorMessage":"account funded"},{"pc":[827,931],"errorMessage":"assert target is match for conditions"},{"pc":[136,162],"errorMessage":"invalid number of bytes for arc4.static_array<arc4.uint8, 32>"},{"pc":[784,891],"errorMessage":"invalid number of bytes for arc4.tuple<asset,application,account>"},{"pc":[144,170],"errorMessage":"invalid number of bytes for arc4.uint64"},{"pc":[239,300],"errorMessage":"invalid number of bytes for uint64"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"events":[]} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


/**
 * The argument types for the C2C contract
 */
export type C2CArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'testCallToIndex(address,uint64)void': {
      account: string
      appId: bigint | number
    }
    'testCallToValue(address,uint64)void': {
      account: string
      appId: bigint | number
    }
    'testCallToEchoResource()void': Record<string, never>
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'testCallToIndex(address,uint64)void': [account: string, appId: bigint | number]
    'testCallToValue(address,uint64)void': [account: string, appId: bigint | number]
    'testCallToEchoResource()void': []
  }
}

/**
 * The return type for each method
 */
export type C2CReturns = {
  'testCallToIndex(address,uint64)void': void
  'testCallToValue(address,uint64)void': void
  'testCallToEchoResource()void': void
}

/**
 * Defines the types of available calls and state of the C2C smart contract.
 */
export type C2CTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'testCallToIndex(address,uint64)void' | 'testCallToIndex', {
      argsObj: C2CArgs['obj']['testCallToIndex(address,uint64)void']
      argsTuple: C2CArgs['tuple']['testCallToIndex(address,uint64)void']
      returns: C2CReturns['testCallToIndex(address,uint64)void']
    }>
    & Record<'testCallToValue(address,uint64)void' | 'testCallToValue', {
      argsObj: C2CArgs['obj']['testCallToValue(address,uint64)void']
      argsTuple: C2CArgs['tuple']['testCallToValue(address,uint64)void']
      returns: C2CReturns['testCallToValue(address,uint64)void']
    }>
    & Record<'testCallToEchoResource()void' | 'testCallToEchoResource', {
      argsObj: C2CArgs['obj']['testCallToEchoResource()void']
      argsTuple: C2CArgs['tuple']['testCallToEchoResource()void']
      returns: C2CReturns['testCallToEchoResource()void']
    }>
}

/**
 * Defines the possible abi call signatures.
 */
export type C2CSignatures = keyof C2CTypes['methods']
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the C2C smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends C2CSignatures> = C2CTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the C2C smart contract to the method's return type
 */
export type MethodReturn<TSignature extends C2CSignatures> = C2CTypes['methods'][TSignature]['returns']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the C2C smart contract
 */
export abstract class C2CParamsFactory {
  /**
   * Constructs a no op call for the testCallToIndex(address,uint64)void ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static testCallToIndex(params: CallParams<C2CArgs['obj']['testCallToIndex(address,uint64)void'] | C2CArgs['tuple']['testCallToIndex(address,uint64)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'testCallToIndex(address,uint64)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.account, params.args.appId],
    }
  }
  /**
   * Constructs a no op call for the testCallToValue(address,uint64)void ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static testCallToValue(params: CallParams<C2CArgs['obj']['testCallToValue(address,uint64)void'] | C2CArgs['tuple']['testCallToValue(address,uint64)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'testCallToValue(address,uint64)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.account, params.args.appId],
    }
  }
  /**
   * Constructs a no op call for the testCallToEchoResource()void ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static testCallToEchoResource(params: CallParams<C2CArgs['obj']['testCallToEchoResource()void'] | C2CArgs['tuple']['testCallToEchoResource()void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'testCallToEchoResource()void' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
}

/**
 * A client to make calls to the C2C smart contract
 */
export class C2CClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `C2CClient`
   *
   * @param appClient An `AppClient` instance which has been created with the C2C app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `C2CClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Returns a new `C2CClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<C2CClient> {
    return new C2CClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `C2CClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<C2CClient> {
    return new C2CClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Makes a clear_state call to an existing instance of the C2C smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the C2C smart contract using the `testCallToIndex(address,uint64)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    testCallToIndex: (params: CallParams<C2CArgs['obj']['testCallToIndex(address,uint64)void'] | C2CArgs['tuple']['testCallToIndex(address,uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(C2CParamsFactory.testCallToIndex(params))
    },

    /**
     * Makes a call to the C2C smart contract using the `testCallToValue(address,uint64)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    testCallToValue: (params: CallParams<C2CArgs['obj']['testCallToValue(address,uint64)void'] | C2CArgs['tuple']['testCallToValue(address,uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(C2CParamsFactory.testCallToValue(params))
    },

    /**
     * Makes a call to the C2C smart contract using the `testCallToEchoResource()void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    testCallToEchoResource: (params: CallParams<C2CArgs['obj']['testCallToEchoResource()void'] | C2CArgs['tuple']['testCallToEchoResource()void']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(C2CParamsFactory.testCallToEchoResource(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Makes a clear_state call to an existing instance of the C2C smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the C2C smart contract using the `testCallToIndex(address,uint64)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    testCallToIndex: (params: CallParams<C2CArgs['obj']['testCallToIndex(address,uint64)void'] | C2CArgs['tuple']['testCallToIndex(address,uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(C2CParamsFactory.testCallToIndex(params))
    },

    /**
     * Makes a call to the C2C smart contract using the `testCallToValue(address,uint64)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    testCallToValue: (params: CallParams<C2CArgs['obj']['testCallToValue(address,uint64)void'] | C2CArgs['tuple']['testCallToValue(address,uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(C2CParamsFactory.testCallToValue(params))
    },

    /**
     * Makes a call to the C2C smart contract using the `testCallToEchoResource()void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    testCallToEchoResource: (params: CallParams<C2CArgs['obj']['testCallToEchoResource()void'] | C2CArgs['tuple']['testCallToEchoResource()void']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(C2CParamsFactory.testCallToEchoResource(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Makes a clear_state call to an existing instance of the C2C smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the C2C smart contract using the `testCallToIndex(address,uint64)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    testCallToIndex: async (params: CallParams<C2CArgs['obj']['testCallToIndex(address,uint64)void'] | C2CArgs['tuple']['testCallToIndex(address,uint64)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(C2CParamsFactory.testCallToIndex(params))
      return {...result, return: result.return as unknown as (undefined | C2CReturns['testCallToIndex(address,uint64)void'])}
    },

    /**
     * Makes a call to the C2C smart contract using the `testCallToValue(address,uint64)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    testCallToValue: async (params: CallParams<C2CArgs['obj']['testCallToValue(address,uint64)void'] | C2CArgs['tuple']['testCallToValue(address,uint64)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(C2CParamsFactory.testCallToValue(params))
      return {...result, return: result.return as unknown as (undefined | C2CReturns['testCallToValue(address,uint64)void'])}
    },

    /**
     * Makes a call to the C2C smart contract using the `testCallToEchoResource()void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    testCallToEchoResource: async (params: CallParams<C2CArgs['obj']['testCallToEchoResource()void'] | C2CArgs['tuple']['testCallToEchoResource()void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(C2CParamsFactory.testCallToEchoResource(params))
      return {...result, return: result.return as unknown as (undefined | C2CReturns['testCallToEchoResource()void'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new C2CClient(this.appClient.clone(params))
  }

  /**
   * Methods to access state for the current C2C app
   */
  state = {
  }

  public newGroup(): C2CComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a testCallToIndex(address,uint64)void method call against the C2C contract
       */
      testCallToIndex(params: CallParams<C2CArgs['obj']['testCallToIndex(address,uint64)void'] | C2CArgs['tuple']['testCallToIndex(address,uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.testCallToIndex(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a testCallToValue(address,uint64)void method call against the C2C contract
       */
      testCallToValue(params: CallParams<C2CArgs['obj']['testCallToValue(address,uint64)void'] | C2CArgs['tuple']['testCallToValue(address,uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.testCallToValue(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a testCallToEchoResource()void method call against the C2C contract
       */
      testCallToEchoResource(params: CallParams<C2CArgs['obj']['testCallToEchoResource()void'] | C2CArgs['tuple']['testCallToEchoResource()void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.testCallToEchoResource(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a clear state call to the C2C contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as C2CComposer
  }
}
export type C2CComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the testCallToIndex(address,uint64)void ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  testCallToIndex(params?: CallParams<C2CArgs['obj']['testCallToIndex(address,uint64)void'] | C2CArgs['tuple']['testCallToIndex(address,uint64)void']>): C2CComposer<[...TReturns, C2CReturns['testCallToIndex(address,uint64)void'] | undefined]>

  /**
   * Calls the testCallToValue(address,uint64)void ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  testCallToValue(params?: CallParams<C2CArgs['obj']['testCallToValue(address,uint64)void'] | C2CArgs['tuple']['testCallToValue(address,uint64)void']>): C2CComposer<[...TReturns, C2CReturns['testCallToValue(address,uint64)void'] | undefined]>

  /**
   * Calls the testCallToEchoResource()void ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  testCallToEchoResource(params?: CallParams<C2CArgs['obj']['testCallToEchoResource()void'] | C2CArgs['tuple']['testCallToEchoResource()void']>): C2CComposer<[...TReturns, C2CReturns['testCallToEchoResource()void'] | undefined]>

  /**
   * Makes a clear_state call to an existing instance of the C2C smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): C2CComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): C2CComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<C2CComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<C2CComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<C2CComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<C2CComposerResults<TReturns>>
}
export type C2CComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

