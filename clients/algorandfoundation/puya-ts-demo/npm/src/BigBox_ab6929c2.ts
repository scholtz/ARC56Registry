/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"BigBox","structs":{"Metadata":[{"name":"start","type":"uint64"},{"name":"end","type":"uint64"},{"name":"status","type":"uint8"},{"name":"endSize","type":"uint64"}]},"methods":[{"name":"createApplication","args":[],"returns":{"type":"void"},"actions":{"create":["NoOp"],"call":[]},"readonly":false,"events":[],"recommendations":{}},{"name":"startUpload","args":[{"type":"string","name":"dataIdentifier","desc":"The unique identifier for the data"},{"type":"uint64","name":"numBoxes","desc":"The number of boxes that the data will take up"},{"type":"uint64","name":"endBoxSize","desc":"The size of the last box"},{"type":"pay","name":"mbrPayment","desc":"Payment from the uploader to cover the box MBR"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"\nAllocate boxes to begin data upload process","events":[],"recommendations":{}},{"name":"upload","args":[{"type":"string","name":"dataIdentifier","desc":"The unique identifier for the data"},{"type":"uint64","name":"boxIndex","desc":"The index of the box to upload the given chunk of data to"},{"type":"uint64","name":"offset","desc":"The offset within the box to start writing the data"},{"type":"byte[]","name":"data","desc":"The data to write"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"\nUpload data to a specific offset in a box","events":[],"recommendations":{}},{"name":"setStatus","args":[{"type":"string","name":"dataIdentifier","desc":"The unique identifier for the data"},{"type":"uint8","name":"status","desc":"The new status for the data"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"\nSet the status of the data","events":[],"recommendations":{}}],"arcs":[22,28],"networks":{},"state":{"schema":{"global":{"ints":1,"bytes":0},"local":{"ints":0,"bytes":0}},"keys":{"global":{"currentIndex":{"keyType":"AVMString","valueType":"AVMUint64","key":"Y3VycmVudEluZGV4"}},"local":{},"box":{}},"maps":{"global":{},"local":{},"box":{"dataBoxes":{"keyType":"uint64","valueType":"AVMBytes","prefix":""},"metadata":{"keyType":"AVMBytes","valueType":"Metadata","prefix":""}}}},"bareActions":{"create":[],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[232,320],"errorMessage":"Box must have value"},{"pc":[36],"errorMessage":"OnCompletion must be NoOp"},{"pc":[196],"errorMessage":"assert target is match for conditions"},{"pc":[114],"errorMessage":"check GlobalState exists"},{"pc":[110],"errorMessage":"transaction type is pay"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"events":[]} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


// Type definitions for ARC-56 structs

export type Metadata = {
  start: bigint,
  end: bigint,
  status: number,
  endSize: bigint
}


/**
 * Converts the ABI tuple representation of a Metadata to the struct representation
 */
export function MetadataFromTuple(abiTuple: [bigint, bigint, number, bigint]) {
  return getABIStructFromABITuple(abiTuple, APP_SPEC.structs.Metadata, APP_SPEC.structs) as Metadata
}

/**
 * The argument types for the BigBox contract
 */
export type BigBoxArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'createApplication()void': Record<string, never>
    'startUpload(string,uint64,uint64,pay)void': {
      /**
       * The unique identifier for the data
       */
      dataIdentifier: string
      /**
       * The number of boxes that the data will take up
       */
      numBoxes: bigint | number
      /**
       * The size of the last box
       */
      endBoxSize: bigint | number
      /**
       * Payment from the uploader to cover the box MBR
       */
      mbrPayment: AppMethodCallTransactionArgument
    }
    'upload(string,uint64,uint64,byte[])void': {
      /**
       * The unique identifier for the data
       */
      dataIdentifier: string
      /**
       * The index of the box to upload the given chunk of data to
       */
      boxIndex: bigint | number
      /**
       * The offset within the box to start writing the data
       */
      offset: bigint | number
      /**
       * The data to write
       */
      data: Uint8Array
    }
    'setStatus(string,uint8)void': {
      /**
       * The unique identifier for the data
       */
      dataIdentifier: string
      /**
       * The new status for the data
       */
      status: bigint | number
    }
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'createApplication()void': []
    'startUpload(string,uint64,uint64,pay)void': [dataIdentifier: string, numBoxes: bigint | number, endBoxSize: bigint | number, mbrPayment: AppMethodCallTransactionArgument]
    'upload(string,uint64,uint64,byte[])void': [dataIdentifier: string, boxIndex: bigint | number, offset: bigint | number, data: Uint8Array]
    'setStatus(string,uint8)void': [dataIdentifier: string, status: bigint | number]
  }
}

/**
 * The return type for each method
 */
export type BigBoxReturns = {
  'createApplication()void': void
  'startUpload(string,uint64,uint64,pay)void': void
  'upload(string,uint64,uint64,byte[])void': void
  'setStatus(string,uint8)void': void
}

/**
 * Defines the types of available calls and state of the BigBox smart contract.
 */
export type BigBoxTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'createApplication()void' | 'createApplication', {
      argsObj: BigBoxArgs['obj']['createApplication()void']
      argsTuple: BigBoxArgs['tuple']['createApplication()void']
      returns: BigBoxReturns['createApplication()void']
    }>
    & Record<'startUpload(string,uint64,uint64,pay)void' | 'startUpload', {
      argsObj: BigBoxArgs['obj']['startUpload(string,uint64,uint64,pay)void']
      argsTuple: BigBoxArgs['tuple']['startUpload(string,uint64,uint64,pay)void']
      returns: BigBoxReturns['startUpload(string,uint64,uint64,pay)void']
    }>
    & Record<'upload(string,uint64,uint64,byte[])void' | 'upload', {
      argsObj: BigBoxArgs['obj']['upload(string,uint64,uint64,byte[])void']
      argsTuple: BigBoxArgs['tuple']['upload(string,uint64,uint64,byte[])void']
      returns: BigBoxReturns['upload(string,uint64,uint64,byte[])void']
    }>
    & Record<'setStatus(string,uint8)void' | 'setStatus', {
      argsObj: BigBoxArgs['obj']['setStatus(string,uint8)void']
      argsTuple: BigBoxArgs['tuple']['setStatus(string,uint8)void']
      returns: BigBoxReturns['setStatus(string,uint8)void']
    }>
  /**
   * Defines the shape of the state of the application.
   */
  state: {
    global: {
      keys: {
        currentIndex: bigint
      }
      maps: {}
    }
    box: {
      keys: {}
      maps: {
        dataBoxes: Map<bigint | number, Uint8Array>
        metadata: Map<Uint8Array | string, Metadata>
      }
    }
  }
}

/**
 * Defines the possible abi call signatures.
 */
export type BigBoxSignatures = keyof BigBoxTypes['methods']
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the BigBox smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends BigBoxSignatures> = BigBoxTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the BigBox smart contract to the method's return type
 */
export type MethodReturn<TSignature extends BigBoxSignatures> = BigBoxTypes['methods'][TSignature]['returns']

/**
 * Defines the shape of the keyed global state of the application.
 */
export type GlobalKeysState = BigBoxTypes['state']['global']['keys']

/**
 * Defines the shape of the keyed box state of the application.
 */
export type BoxKeysState = BigBoxTypes['state']['box']['keys']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the BigBox smart contract
 */
export abstract class BigBoxParamsFactory {
  /**
   * Constructs a no op call for the startUpload(string,uint64,uint64,pay)void ABI method
   *
  *
  Allocate boxes to begin data upload process

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static startUpload(params: CallParams<BigBoxArgs['obj']['startUpload(string,uint64,uint64,pay)void'] | BigBoxArgs['tuple']['startUpload(string,uint64,uint64,pay)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'startUpload(string,uint64,uint64,pay)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.dataIdentifier, params.args.numBoxes, params.args.endBoxSize, params.args.mbrPayment],
    }
  }
  /**
   * Constructs a no op call for the upload(string,uint64,uint64,byte[])void ABI method
   *
  *
  Upload data to a specific offset in a box

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static upload(params: CallParams<BigBoxArgs['obj']['upload(string,uint64,uint64,byte[])void'] | BigBoxArgs['tuple']['upload(string,uint64,uint64,byte[])void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'upload(string,uint64,uint64,byte[])void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.dataIdentifier, params.args.boxIndex, params.args.offset, params.args.data],
    }
  }
  /**
   * Constructs a no op call for the setStatus(string,uint8)void ABI method
   *
  *
  Set the status of the data

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static setStatus(params: CallParams<BigBoxArgs['obj']['setStatus(string,uint8)void'] | BigBoxArgs['tuple']['setStatus(string,uint8)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'setStatus(string,uint8)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.dataIdentifier, params.args.status],
    }
  }
}

/**
 * A client to make calls to the BigBox smart contract
 */
export class BigBoxClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `BigBoxClient`
   *
   * @param appClient An `AppClient` instance which has been created with the BigBox app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `BigBoxClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Returns a new `BigBoxClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<BigBoxClient> {
    return new BigBoxClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `BigBoxClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<BigBoxClient> {
    return new BigBoxClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Makes a clear_state call to an existing instance of the BigBox smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the BigBox smart contract using the `startUpload(string,uint64,uint64,pay)void` ABI method.
     *
    *
    Allocate boxes to begin data upload process

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    startUpload: (params: CallParams<BigBoxArgs['obj']['startUpload(string,uint64,uint64,pay)void'] | BigBoxArgs['tuple']['startUpload(string,uint64,uint64,pay)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(BigBoxParamsFactory.startUpload(params))
    },

    /**
     * Makes a call to the BigBox smart contract using the `upload(string,uint64,uint64,byte[])void` ABI method.
     *
    *
    Upload data to a specific offset in a box

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    upload: (params: CallParams<BigBoxArgs['obj']['upload(string,uint64,uint64,byte[])void'] | BigBoxArgs['tuple']['upload(string,uint64,uint64,byte[])void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(BigBoxParamsFactory.upload(params))
    },

    /**
     * Makes a call to the BigBox smart contract using the `setStatus(string,uint8)void` ABI method.
     *
    *
    Set the status of the data

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    setStatus: (params: CallParams<BigBoxArgs['obj']['setStatus(string,uint8)void'] | BigBoxArgs['tuple']['setStatus(string,uint8)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(BigBoxParamsFactory.setStatus(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Makes a clear_state call to an existing instance of the BigBox smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the BigBox smart contract using the `startUpload(string,uint64,uint64,pay)void` ABI method.
     *
    *
    Allocate boxes to begin data upload process

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    startUpload: (params: CallParams<BigBoxArgs['obj']['startUpload(string,uint64,uint64,pay)void'] | BigBoxArgs['tuple']['startUpload(string,uint64,uint64,pay)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(BigBoxParamsFactory.startUpload(params))
    },

    /**
     * Makes a call to the BigBox smart contract using the `upload(string,uint64,uint64,byte[])void` ABI method.
     *
    *
    Upload data to a specific offset in a box

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    upload: (params: CallParams<BigBoxArgs['obj']['upload(string,uint64,uint64,byte[])void'] | BigBoxArgs['tuple']['upload(string,uint64,uint64,byte[])void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(BigBoxParamsFactory.upload(params))
    },

    /**
     * Makes a call to the BigBox smart contract using the `setStatus(string,uint8)void` ABI method.
     *
    *
    Set the status of the data

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    setStatus: (params: CallParams<BigBoxArgs['obj']['setStatus(string,uint8)void'] | BigBoxArgs['tuple']['setStatus(string,uint8)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(BigBoxParamsFactory.setStatus(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Makes a clear_state call to an existing instance of the BigBox smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the BigBox smart contract using the `startUpload(string,uint64,uint64,pay)void` ABI method.
     *
    *
    Allocate boxes to begin data upload process

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    startUpload: async (params: CallParams<BigBoxArgs['obj']['startUpload(string,uint64,uint64,pay)void'] | BigBoxArgs['tuple']['startUpload(string,uint64,uint64,pay)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(BigBoxParamsFactory.startUpload(params))
      return {...result, return: result.return as unknown as (undefined | BigBoxReturns['startUpload(string,uint64,uint64,pay)void'])}
    },

    /**
     * Makes a call to the BigBox smart contract using the `upload(string,uint64,uint64,byte[])void` ABI method.
     *
    *
    Upload data to a specific offset in a box

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    upload: async (params: CallParams<BigBoxArgs['obj']['upload(string,uint64,uint64,byte[])void'] | BigBoxArgs['tuple']['upload(string,uint64,uint64,byte[])void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(BigBoxParamsFactory.upload(params))
      return {...result, return: result.return as unknown as (undefined | BigBoxReturns['upload(string,uint64,uint64,byte[])void'])}
    },

    /**
     * Makes a call to the BigBox smart contract using the `setStatus(string,uint8)void` ABI method.
     *
    *
    Set the status of the data

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    setStatus: async (params: CallParams<BigBoxArgs['obj']['setStatus(string,uint8)void'] | BigBoxArgs['tuple']['setStatus(string,uint8)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(BigBoxParamsFactory.setStatus(params))
      return {...result, return: result.return as unknown as (undefined | BigBoxReturns['setStatus(string,uint8)void'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new BigBoxClient(this.appClient.clone(params))
  }

  /**
   * Methods to access state for the current BigBox app
   */
  state = {
    /**
     * Methods to access global state for the current BigBox app
     */
    global: {
      /**
       * Get all current keyed values from global state
       */
      getAll: async (): Promise<Partial<Expand<GlobalKeysState>>> => {
        const result = await this.appClient.state.global.getAll()
        return {
          currentIndex: result.currentIndex,
        }
      },
      /**
       * Get the current value of the currentIndex key in global state
       */
      currentIndex: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("currentIndex")) as bigint | undefined },
    },
    /**
     * Methods to access box state for the current BigBox app
     */
    box: {
      /**
       * Get all current keyed values from box state
       */
      getAll: async (): Promise<Partial<Expand<BoxKeysState>>> => {
        const result = await this.appClient.state.box.getAll()
        return {
        }
      },
      /**
       * Get values from the dataBoxes map in box state
       */
      dataBoxes: {
        /**
         * Get all current values of the dataBoxes map in box state
         */
        getMap: async (): Promise<Map<bigint, Uint8Array>> => { return (await this.appClient.state.box.getMap("dataBoxes")) as Map<bigint, Uint8Array> },
        /**
         * Get a current value of the dataBoxes map by key from box state
         */
        value: async (key: bigint | number): Promise<Uint8Array | undefined> => { return await this.appClient.state.box.getMapValue("dataBoxes", key) as Uint8Array | undefined },
      },
      /**
       * Get values from the metadata map in box state
       */
      metadata: {
        /**
         * Get all current values of the metadata map in box state
         */
        getMap: async (): Promise<Map<Uint8Array, Metadata>> => { return (await this.appClient.state.box.getMap("metadata")) as Map<Uint8Array, Metadata> },
        /**
         * Get a current value of the metadata map by key from box state
         */
        value: async (key: Uint8Array | string): Promise<Metadata | undefined> => { return await this.appClient.state.box.getMapValue("metadata", key) as Metadata | undefined },
      },
    },
  }

  public newGroup(): BigBoxComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a startUpload(string,uint64,uint64,pay)void method call against the BigBox contract
       */
      startUpload(params: CallParams<BigBoxArgs['obj']['startUpload(string,uint64,uint64,pay)void'] | BigBoxArgs['tuple']['startUpload(string,uint64,uint64,pay)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.startUpload(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a upload(string,uint64,uint64,byte[])void method call against the BigBox contract
       */
      upload(params: CallParams<BigBoxArgs['obj']['upload(string,uint64,uint64,byte[])void'] | BigBoxArgs['tuple']['upload(string,uint64,uint64,byte[])void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.upload(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a setStatus(string,uint8)void method call against the BigBox contract
       */
      setStatus(params: CallParams<BigBoxArgs['obj']['setStatus(string,uint8)void'] | BigBoxArgs['tuple']['setStatus(string,uint8)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.setStatus(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a clear state call to the BigBox contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as BigBoxComposer
  }
}
export type BigBoxComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the startUpload(string,uint64,uint64,pay)void ABI method.
   *
  *
  Allocate boxes to begin data upload process

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  startUpload(params?: CallParams<BigBoxArgs['obj']['startUpload(string,uint64,uint64,pay)void'] | BigBoxArgs['tuple']['startUpload(string,uint64,uint64,pay)void']>): BigBoxComposer<[...TReturns, BigBoxReturns['startUpload(string,uint64,uint64,pay)void'] | undefined]>

  /**
   * Calls the upload(string,uint64,uint64,byte[])void ABI method.
   *
  *
  Upload data to a specific offset in a box

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  upload(params?: CallParams<BigBoxArgs['obj']['upload(string,uint64,uint64,byte[])void'] | BigBoxArgs['tuple']['upload(string,uint64,uint64,byte[])void']>): BigBoxComposer<[...TReturns, BigBoxReturns['upload(string,uint64,uint64,byte[])void'] | undefined]>

  /**
   * Calls the setStatus(string,uint8)void ABI method.
   *
  *
  Set the status of the data

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  setStatus(params?: CallParams<BigBoxArgs['obj']['setStatus(string,uint8)void'] | BigBoxArgs['tuple']['setStatus(string,uint8)void']>): BigBoxComposer<[...TReturns, BigBoxReturns['setStatus(string,uint8)void'] | undefined]>

  /**
   * Makes a clear_state call to an existing instance of the BigBox smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): BigBoxComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): BigBoxComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<BigBoxComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<BigBoxComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<BigBoxComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<BigBoxComposerResults<TReturns>>
}
export type BigBoxComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

