/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"VotingContract","structs":{},"methods":[{"name":"setTopic","args":[{"type":"string","name":"topic"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}},{"name":"vote","args":[{"type":"pay","name":"pay"}],"returns":{"type":"bool"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}},{"name":"getVotes","args":[],"returns":{"type":"uint64"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}}],"arcs":[22,28],"networks":{},"state":{"schema":{"global":{"ints":1,"bytes":1},"local":{"ints":1,"bytes":0}},"keys":{"global":{"topic":{"keyType":"AVMString","valueType":"AVMBytes","key":"dG9waWM="},"votes":{"keyType":"AVMString","valueType":"AVMUint64","key":"dm90ZXM="}},"local":{"voted":{"keyType":"AVMString","valueType":"AVMUint64","key":"dm90ZWQ="}},"box":{}},"maps":{"global":{},"local":{},"box":{}}},"bareActions":{"create":["NoOp"],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[130],"errorMessage":"Expected 2 transactions"},{"pc":[138],"errorMessage":"Incorrect payment amount"},{"pc":[63],"errorMessage":"OnCompletion must be NoOp"},{"pc":[103],"errorMessage":"OnCompletion must be NoOp && can only call when creating"},{"pc":[144],"errorMessage":"Payment sender must match transaction sender"},{"pc":[167,187],"errorMessage":"check GlobalState exists"},{"pc":[124],"errorMessage":"transaction type is pay"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"events":[]} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


/**
 * The argument types for the VotingContract contract
 */
export type VotingContractArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'setTopic(string)void': {
      topic: string
    }
    'vote(pay)bool': {
      pay: AppMethodCallTransactionArgument
    }
    'getVotes()uint64': Record<string, never>
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'setTopic(string)void': [topic: string]
    'vote(pay)bool': [pay: AppMethodCallTransactionArgument]
    'getVotes()uint64': []
  }
}

/**
 * The return type for each method
 */
export type VotingContractReturns = {
  'setTopic(string)void': void
  'vote(pay)bool': boolean
  'getVotes()uint64': bigint
}

/**
 * Defines the types of available calls and state of the VotingContract smart contract.
 */
export type VotingContractTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'setTopic(string)void' | 'setTopic', {
      argsObj: VotingContractArgs['obj']['setTopic(string)void']
      argsTuple: VotingContractArgs['tuple']['setTopic(string)void']
      returns: VotingContractReturns['setTopic(string)void']
    }>
    & Record<'vote(pay)bool' | 'vote', {
      argsObj: VotingContractArgs['obj']['vote(pay)bool']
      argsTuple: VotingContractArgs['tuple']['vote(pay)bool']
      returns: VotingContractReturns['vote(pay)bool']
    }>
    & Record<'getVotes()uint64' | 'getVotes', {
      argsObj: VotingContractArgs['obj']['getVotes()uint64']
      argsTuple: VotingContractArgs['tuple']['getVotes()uint64']
      returns: VotingContractReturns['getVotes()uint64']
    }>
  /**
   * Defines the shape of the state of the application.
   */
  state: {
    global: {
      keys: {
        topic: BinaryState
        votes: bigint
      }
      maps: {}
    }
    local: {
      keys: {
        voted: bigint
      }
      maps: {}
    }
  }
}

/**
 * Defines the possible abi call signatures.
 */
export type VotingContractSignatures = keyof VotingContractTypes['methods']
/**
 * Defines the possible abi call signatures for methods that return a non-void value.
 */
export type VotingContractNonVoidMethodSignatures = keyof VotingContractTypes['methods'] extends infer T ? T extends keyof VotingContractTypes['methods'] ? MethodReturn<T> extends void ? never : T  : never : never
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the VotingContract smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends VotingContractSignatures> = VotingContractTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the VotingContract smart contract to the method's return type
 */
export type MethodReturn<TSignature extends VotingContractSignatures> = VotingContractTypes['methods'][TSignature]['returns']

/**
 * Defines the shape of the keyed global state of the application.
 */
export type GlobalKeysState = VotingContractTypes['state']['global']['keys']

/**
 * Defines the shape of the keyed local state of the application.
 */
export type LocalKeysState = VotingContractTypes['state']['local']['keys']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the VotingContract smart contract
 */
export abstract class VotingContractParamsFactory {
  /**
   * Constructs a no op call for the setTopic(string)void ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static setTopic(params: CallParams<VotingContractArgs['obj']['setTopic(string)void'] | VotingContractArgs['tuple']['setTopic(string)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'setTopic(string)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.topic],
    }
  }
  /**
   * Constructs a no op call for the vote(pay)bool ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static vote(params: CallParams<VotingContractArgs['obj']['vote(pay)bool'] | VotingContractArgs['tuple']['vote(pay)bool']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'vote(pay)bool' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.pay],
    }
  }
  /**
   * Constructs a no op call for the getVotes()uint64 ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static getVotes(params: CallParams<VotingContractArgs['obj']['getVotes()uint64'] | VotingContractArgs['tuple']['getVotes()uint64']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'getVotes()uint64' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
}

/**
 * A client to make calls to the VotingContract smart contract
 */
export class VotingContractClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `VotingContractClient`
   *
   * @param appClient An `AppClient` instance which has been created with the VotingContract app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `VotingContractClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Checks for decode errors on the given return value and maps the return value to the return type for the given method
   * @returns The typed return value or undefined if there was no value
   */
  decodeReturnValue<TSignature extends VotingContractNonVoidMethodSignatures>(method: TSignature, returnValue: ABIReturn | undefined) {
    return returnValue !== undefined ? getArc56ReturnValue<MethodReturn<TSignature>>(returnValue, this.appClient.getABIMethod(method), APP_SPEC.structs) : undefined
  }

  /**
   * Returns a new `VotingContractClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<VotingContractClient> {
    return new VotingContractClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `VotingContractClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<VotingContractClient> {
    return new VotingContractClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Makes a clear_state call to an existing instance of the VotingContract smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the VotingContract smart contract using the `setTopic(string)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    setTopic: (params: CallParams<VotingContractArgs['obj']['setTopic(string)void'] | VotingContractArgs['tuple']['setTopic(string)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(VotingContractParamsFactory.setTopic(params))
    },

    /**
     * Makes a call to the VotingContract smart contract using the `vote(pay)bool` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    vote: (params: CallParams<VotingContractArgs['obj']['vote(pay)bool'] | VotingContractArgs['tuple']['vote(pay)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(VotingContractParamsFactory.vote(params))
    },

    /**
     * Makes a call to the VotingContract smart contract using the `getVotes()uint64` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    getVotes: (params: CallParams<VotingContractArgs['obj']['getVotes()uint64'] | VotingContractArgs['tuple']['getVotes()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(VotingContractParamsFactory.getVotes(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Makes a clear_state call to an existing instance of the VotingContract smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the VotingContract smart contract using the `setTopic(string)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    setTopic: (params: CallParams<VotingContractArgs['obj']['setTopic(string)void'] | VotingContractArgs['tuple']['setTopic(string)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(VotingContractParamsFactory.setTopic(params))
    },

    /**
     * Makes a call to the VotingContract smart contract using the `vote(pay)bool` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    vote: (params: CallParams<VotingContractArgs['obj']['vote(pay)bool'] | VotingContractArgs['tuple']['vote(pay)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(VotingContractParamsFactory.vote(params))
    },

    /**
     * Makes a call to the VotingContract smart contract using the `getVotes()uint64` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    getVotes: (params: CallParams<VotingContractArgs['obj']['getVotes()uint64'] | VotingContractArgs['tuple']['getVotes()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(VotingContractParamsFactory.getVotes(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Makes a clear_state call to an existing instance of the VotingContract smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the VotingContract smart contract using the `setTopic(string)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    setTopic: async (params: CallParams<VotingContractArgs['obj']['setTopic(string)void'] | VotingContractArgs['tuple']['setTopic(string)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(VotingContractParamsFactory.setTopic(params))
      return {...result, return: result.return as unknown as (undefined | VotingContractReturns['setTopic(string)void'])}
    },

    /**
     * Makes a call to the VotingContract smart contract using the `vote(pay)bool` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    vote: async (params: CallParams<VotingContractArgs['obj']['vote(pay)bool'] | VotingContractArgs['tuple']['vote(pay)bool']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(VotingContractParamsFactory.vote(params))
      return {...result, return: result.return as unknown as (undefined | VotingContractReturns['vote(pay)bool'])}
    },

    /**
     * Makes a call to the VotingContract smart contract using the `getVotes()uint64` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    getVotes: async (params: CallParams<VotingContractArgs['obj']['getVotes()uint64'] | VotingContractArgs['tuple']['getVotes()uint64']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(VotingContractParamsFactory.getVotes(params))
      return {...result, return: result.return as unknown as (undefined | VotingContractReturns['getVotes()uint64'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new VotingContractClient(this.appClient.clone(params))
  }

  /**
   * Makes a readonly (simulated) call to the VotingContract smart contract using the `getVotes()uint64` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async getVotes(params: CallParams<VotingContractArgs['obj']['getVotes()uint64'] | VotingContractArgs['tuple']['getVotes()uint64']> = {args: []}) {
    const result = await this.appClient.send.call(VotingContractParamsFactory.getVotes(params))
    return result.return as unknown as VotingContractReturns['getVotes()uint64']
  }

  /**
   * Methods to access state for the current VotingContract app
   */
  state = {
    /**
     * Methods to access global state for the current VotingContract app
     */
    global: {
      /**
       * Get all current keyed values from global state
       */
      getAll: async (): Promise<Partial<Expand<GlobalKeysState>>> => {
        const result = await this.appClient.state.global.getAll()
        return {
          topic: new BinaryStateValue(result.topic),
          votes: result.votes,
        }
      },
      /**
       * Get the current value of the topic key in global state
       */
      topic: async (): Promise<BinaryState> => { return new BinaryStateValue((await this.appClient.state.global.getValue("topic")) as Uint8Array | undefined) },
      /**
       * Get the current value of the votes key in global state
       */
      votes: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("votes")) as bigint | undefined },
    },
    /**
     * Methods to access local state for the current VotingContract app
     */
    local: (address: string | Address) => {
      const encodedAddress = typeof address === 'string' ? address : encodeAddress(address.publicKey)
      return {
        /**
         * Get all current keyed values from local state
         */
        getAll: async (): Promise<Partial<Expand<LocalKeysState>>> => {
          const result = await this.appClient.state.local(encodedAddress).getAll()
          return {
            voted: result.voted,
          }
        },
        /**
         * Get the current value of the voted key in local state
         */
        voted: async (): Promise<bigint | undefined> => { return (await this.appClient.state.local(encodedAddress).getValue("voted")) as bigint | undefined },
      }
    },
  }

  public newGroup(): VotingContractComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a setTopic(string)void method call against the VotingContract contract
       */
      setTopic(params: CallParams<VotingContractArgs['obj']['setTopic(string)void'] | VotingContractArgs['tuple']['setTopic(string)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.setTopic(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a vote(pay)bool method call against the VotingContract contract
       */
      vote(params: CallParams<VotingContractArgs['obj']['vote(pay)bool'] | VotingContractArgs['tuple']['vote(pay)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.vote(params)))
        resultMappers.push((v) => client.decodeReturnValue('vote(pay)bool', v))
        return this
      },
      /**
       * Add a getVotes()uint64 method call against the VotingContract contract
       */
      getVotes(params: CallParams<VotingContractArgs['obj']['getVotes()uint64'] | VotingContractArgs['tuple']['getVotes()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.getVotes(params)))
        resultMappers.push((v) => client.decodeReturnValue('getVotes()uint64', v))
        return this
      },
      /**
       * Add a clear state call to the VotingContract contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as VotingContractComposer
  }
}
export type VotingContractComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the setTopic(string)void ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  setTopic(params?: CallParams<VotingContractArgs['obj']['setTopic(string)void'] | VotingContractArgs['tuple']['setTopic(string)void']>): VotingContractComposer<[...TReturns, VotingContractReturns['setTopic(string)void'] | undefined]>

  /**
   * Calls the vote(pay)bool ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  vote(params?: CallParams<VotingContractArgs['obj']['vote(pay)bool'] | VotingContractArgs['tuple']['vote(pay)bool']>): VotingContractComposer<[...TReturns, VotingContractReturns['vote(pay)bool'] | undefined]>

  /**
   * Calls the getVotes()uint64 ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  getVotes(params?: CallParams<VotingContractArgs['obj']['getVotes()uint64'] | VotingContractArgs['tuple']['getVotes()uint64']>): VotingContractComposer<[...TReturns, VotingContractReturns['getVotes()uint64'] | undefined]>

  /**
   * Makes a clear_state call to an existing instance of the VotingContract smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): VotingContractComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): VotingContractComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<VotingContractComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<VotingContractComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<VotingContractComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<VotingContractComposerResults<TReturns>>
}
export type VotingContractComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

