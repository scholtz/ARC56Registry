/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"Arc4Tuple","structs":{},"methods":[{"name":"add_contact_info","args":[{"type":"(string,string,uint64)","name":"contact"}],"returns":{"type":"uint64"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"An arc4.Tuple is a heterogeneous collection of arc4 types.","events":[],"recommendations":{}},{"name":"return_contact","args":[],"returns":{"type":"(string,string,uint64)"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"An arc4.Tuple can be returned when more than one return value is needed.","events":[],"recommendations":{}}],"arcs":[22,28],"networks":{},"state":{"schema":{"global":{"ints":0,"bytes":1},"local":{"ints":0,"bytes":0}},"keys":{"global":{"contact_info":{"keyType":"AVMString","valueType":"(string,string,uint64)","key":"Y29udGFjdF9pbmZv"}},"local":{},"box":{}},"maps":{"global":{},"local":{},"box":{}}},"bareActions":{"create":["NoOp"],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[58],"errorMessage":"OnCompletion must be NoOp"},{"pc":[91],"errorMessage":"OnCompletion must be NoOp && can only call when creating"},{"pc":[192],"errorMessage":"check self.contact_info exists"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"events":[]} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


/**
 * The argument types for the Arc4Tuple contract
 */
export type Arc4TupleArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'add_contact_info((string,string,uint64))uint64': {
      contact: [string, string, bigint | number]
    }
    'return_contact()(string,string,uint64)': Record<string, never>
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'add_contact_info((string,string,uint64))uint64': [contact: [string, string, bigint | number]]
    'return_contact()(string,string,uint64)': []
  }
}

/**
 * The return type for each method
 */
export type Arc4TupleReturns = {
  'add_contact_info((string,string,uint64))uint64': bigint
  'return_contact()(string,string,uint64)': [string, string, bigint]
}

/**
 * Defines the types of available calls and state of the Arc4Tuple smart contract.
 */
export type Arc4TupleTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'add_contact_info((string,string,uint64))uint64' | 'add_contact_info', {
      argsObj: Arc4TupleArgs['obj']['add_contact_info((string,string,uint64))uint64']
      argsTuple: Arc4TupleArgs['tuple']['add_contact_info((string,string,uint64))uint64']
      returns: Arc4TupleReturns['add_contact_info((string,string,uint64))uint64']
    }>
    & Record<'return_contact()(string,string,uint64)' | 'return_contact', {
      argsObj: Arc4TupleArgs['obj']['return_contact()(string,string,uint64)']
      argsTuple: Arc4TupleArgs['tuple']['return_contact()(string,string,uint64)']
      returns: Arc4TupleReturns['return_contact()(string,string,uint64)']
    }>
  /**
   * Defines the shape of the state of the application.
   */
  state: {
    global: {
      keys: {
        contactInfo: [string, string, bigint]
      }
      maps: {}
    }
  }
}

/**
 * Defines the possible abi call signatures.
 */
export type Arc4TupleSignatures = keyof Arc4TupleTypes['methods']
/**
 * Defines the possible abi call signatures for methods that return a non-void value.
 */
export type Arc4TupleNonVoidMethodSignatures = keyof Arc4TupleTypes['methods'] extends infer T ? T extends keyof Arc4TupleTypes['methods'] ? MethodReturn<T> extends void ? never : T  : never : never
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the Arc4Tuple smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends Arc4TupleSignatures> = Arc4TupleTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the Arc4Tuple smart contract to the method's return type
 */
export type MethodReturn<TSignature extends Arc4TupleSignatures> = Arc4TupleTypes['methods'][TSignature]['returns']

/**
 * Defines the shape of the keyed global state of the application.
 */
export type GlobalKeysState = Arc4TupleTypes['state']['global']['keys']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the Arc4Tuple smart contract
 */
export abstract class Arc4TupleParamsFactory {
  /**
   * Constructs a no op call for the add_contact_info((string,string,uint64))uint64 ABI method
   *
   * An arc4.Tuple is a heterogeneous collection of arc4 types.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static addContactInfo(params: CallParams<Arc4TupleArgs['obj']['add_contact_info((string,string,uint64))uint64'] | Arc4TupleArgs['tuple']['add_contact_info((string,string,uint64))uint64']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'add_contact_info((string,string,uint64))uint64' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.contact],
    }
  }
  /**
   * Constructs a no op call for the return_contact()(string,string,uint64) ABI method
   *
   * An arc4.Tuple can be returned when more than one return value is needed.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static returnContact(params: CallParams<Arc4TupleArgs['obj']['return_contact()(string,string,uint64)'] | Arc4TupleArgs['tuple']['return_contact()(string,string,uint64)']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'return_contact()(string,string,uint64)' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
}

/**
 * A client to make calls to the Arc4Tuple smart contract
 */
export class Arc4TupleClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `Arc4TupleClient`
   *
   * @param appClient An `AppClient` instance which has been created with the Arc4Tuple app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `Arc4TupleClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Checks for decode errors on the given return value and maps the return value to the return type for the given method
   * @returns The typed return value or undefined if there was no value
   */
  decodeReturnValue<TSignature extends Arc4TupleNonVoidMethodSignatures>(method: TSignature, returnValue: ABIReturn | undefined) {
    return returnValue !== undefined ? getArc56ReturnValue<MethodReturn<TSignature>>(returnValue, this.appClient.getABIMethod(method), APP_SPEC.structs) : undefined
  }

  /**
   * Returns a new `Arc4TupleClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<Arc4TupleClient> {
    return new Arc4TupleClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `Arc4TupleClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<Arc4TupleClient> {
    return new Arc4TupleClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Makes a clear_state call to an existing instance of the Arc4Tuple smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the Arc4Tuple smart contract using the `add_contact_info((string,string,uint64))uint64` ABI method.
     *
     * An arc4.Tuple is a heterogeneous collection of arc4 types.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    addContactInfo: (params: CallParams<Arc4TupleArgs['obj']['add_contact_info((string,string,uint64))uint64'] | Arc4TupleArgs['tuple']['add_contact_info((string,string,uint64))uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(Arc4TupleParamsFactory.addContactInfo(params))
    },

    /**
     * Makes a call to the Arc4Tuple smart contract using the `return_contact()(string,string,uint64)` ABI method.
     *
     * An arc4.Tuple can be returned when more than one return value is needed.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    returnContact: (params: CallParams<Arc4TupleArgs['obj']['return_contact()(string,string,uint64)'] | Arc4TupleArgs['tuple']['return_contact()(string,string,uint64)']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(Arc4TupleParamsFactory.returnContact(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Makes a clear_state call to an existing instance of the Arc4Tuple smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the Arc4Tuple smart contract using the `add_contact_info((string,string,uint64))uint64` ABI method.
     *
     * An arc4.Tuple is a heterogeneous collection of arc4 types.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    addContactInfo: (params: CallParams<Arc4TupleArgs['obj']['add_contact_info((string,string,uint64))uint64'] | Arc4TupleArgs['tuple']['add_contact_info((string,string,uint64))uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(Arc4TupleParamsFactory.addContactInfo(params))
    },

    /**
     * Makes a call to the Arc4Tuple smart contract using the `return_contact()(string,string,uint64)` ABI method.
     *
     * An arc4.Tuple can be returned when more than one return value is needed.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    returnContact: (params: CallParams<Arc4TupleArgs['obj']['return_contact()(string,string,uint64)'] | Arc4TupleArgs['tuple']['return_contact()(string,string,uint64)']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(Arc4TupleParamsFactory.returnContact(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Makes a clear_state call to an existing instance of the Arc4Tuple smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the Arc4Tuple smart contract using the `add_contact_info((string,string,uint64))uint64` ABI method.
     *
     * An arc4.Tuple is a heterogeneous collection of arc4 types.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    addContactInfo: async (params: CallParams<Arc4TupleArgs['obj']['add_contact_info((string,string,uint64))uint64'] | Arc4TupleArgs['tuple']['add_contact_info((string,string,uint64))uint64']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(Arc4TupleParamsFactory.addContactInfo(params))
      return {...result, return: result.return as unknown as (undefined | Arc4TupleReturns['add_contact_info((string,string,uint64))uint64'])}
    },

    /**
     * Makes a call to the Arc4Tuple smart contract using the `return_contact()(string,string,uint64)` ABI method.
     *
     * An arc4.Tuple can be returned when more than one return value is needed.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    returnContact: async (params: CallParams<Arc4TupleArgs['obj']['return_contact()(string,string,uint64)'] | Arc4TupleArgs['tuple']['return_contact()(string,string,uint64)']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(Arc4TupleParamsFactory.returnContact(params))
      return {...result, return: result.return as unknown as (undefined | Arc4TupleReturns['return_contact()(string,string,uint64)'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new Arc4TupleClient(this.appClient.clone(params))
  }

  /**
   * Methods to access state for the current Arc4Tuple app
   */
  state = {
    /**
     * Methods to access global state for the current Arc4Tuple app
     */
    global: {
      /**
       * Get all current keyed values from global state
       */
      getAll: async (): Promise<Partial<Expand<GlobalKeysState>>> => {
        const result = await this.appClient.state.global.getAll()
        return {
          contactInfo: result.contact_info,
        }
      },
      /**
       * Get the current value of the contact_info key in global state
       */
      contactInfo: async (): Promise<[string, string, bigint] | undefined> => { return (await this.appClient.state.global.getValue("contact_info")) as [string, string, bigint] | undefined },
    },
  }

  public newGroup(): Arc4TupleComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a add_contact_info((string,string,uint64))uint64 method call against the Arc4Tuple contract
       */
      addContactInfo(params: CallParams<Arc4TupleArgs['obj']['add_contact_info((string,string,uint64))uint64'] | Arc4TupleArgs['tuple']['add_contact_info((string,string,uint64))uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.addContactInfo(params)))
        resultMappers.push((v) => client.decodeReturnValue('add_contact_info((string,string,uint64))uint64', v))
        return this
      },
      /**
       * Add a return_contact()(string,string,uint64) method call against the Arc4Tuple contract
       */
      returnContact(params: CallParams<Arc4TupleArgs['obj']['return_contact()(string,string,uint64)'] | Arc4TupleArgs['tuple']['return_contact()(string,string,uint64)']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.returnContact(params)))
        resultMappers.push((v) => client.decodeReturnValue('return_contact()(string,string,uint64)', v))
        return this
      },
      /**
       * Add a clear state call to the Arc4Tuple contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as Arc4TupleComposer
  }
}
export type Arc4TupleComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the add_contact_info((string,string,uint64))uint64 ABI method.
   *
   * An arc4.Tuple is a heterogeneous collection of arc4 types.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  addContactInfo(params?: CallParams<Arc4TupleArgs['obj']['add_contact_info((string,string,uint64))uint64'] | Arc4TupleArgs['tuple']['add_contact_info((string,string,uint64))uint64']>): Arc4TupleComposer<[...TReturns, Arc4TupleReturns['add_contact_info((string,string,uint64))uint64'] | undefined]>

  /**
   * Calls the return_contact()(string,string,uint64) ABI method.
   *
   * An arc4.Tuple can be returned when more than one return value is needed.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  returnContact(params?: CallParams<Arc4TupleArgs['obj']['return_contact()(string,string,uint64)'] | Arc4TupleArgs['tuple']['return_contact()(string,string,uint64)']>): Arc4TupleComposer<[...TReturns, Arc4TupleReturns['return_contact()(string,string,uint64)'] | undefined]>

  /**
   * Makes a clear_state call to an existing instance of the Arc4Tuple smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): Arc4TupleComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): Arc4TupleComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<Arc4TupleComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<Arc4TupleComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<Arc4TupleComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<Arc4TupleComposerResults<TReturns>>
}
export type Arc4TupleComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

