/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"ReferenceAccountAsset","structs":{},"methods":[{"name":"getAssetBalance","args":[],"returns":{"type":"uint64","desc":"The asset balance of the account"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"desc":"Returns the balance of a specific asset in a hardcoded account","events":[],"recommendations":{}},{"name":"getAssetBalanceWithArg","args":[{"type":"address","name":"account","desc":"The account to check the asset balance for"},{"type":"uint64","name":"asset","desc":"The asset to check the balance of"}],"returns":{"type":"uint64","desc":"The asset balance of the account"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"desc":"Returns the balance of a specific asset in a provided account","events":[],"recommendations":{}}],"arcs":[22,28],"desc":"A contract that demonstrates how to reference both accounts and assets in a smart contract","networks":{},"state":{"schema":{"global":{"ints":0,"bytes":0},"local":{"ints":0,"bytes":0}},"keys":{"global":{},"local":{},"box":{}},"maps":{"global":{},"local":{},"box":{}}},"bareActions":{"create":["NoOp"],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[94,119],"errorMessage":"Account is not opted in to the asset"},{"pc":[54],"errorMessage":"OnCompletion must be NoOp"},{"pc":[87],"errorMessage":"OnCompletion must be NoOp && can only call when creating"},{"pc":[99,122],"errorMessage":"account opted into asset"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"events":[]} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


/**
 * The argument types for the ReferenceAccountAsset contract
 */
export type ReferenceAccountAssetArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'getAssetBalance()uint64': Record<string, never>
    'getAssetBalanceWithArg(address,uint64)uint64': {
      /**
       * The account to check the asset balance for
       */
      account: string
      /**
       * The asset to check the balance of
       */
      asset: bigint | number
    }
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'getAssetBalance()uint64': []
    'getAssetBalanceWithArg(address,uint64)uint64': [account: string, asset: bigint | number]
  }
}

/**
 * The return type for each method
 */
export type ReferenceAccountAssetReturns = {
  'getAssetBalance()uint64': bigint
  'getAssetBalanceWithArg(address,uint64)uint64': bigint
}

/**
 * Defines the types of available calls and state of the ReferenceAccountAsset smart contract.
 */
export type ReferenceAccountAssetTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'getAssetBalance()uint64' | 'getAssetBalance', {
      argsObj: ReferenceAccountAssetArgs['obj']['getAssetBalance()uint64']
      argsTuple: ReferenceAccountAssetArgs['tuple']['getAssetBalance()uint64']
      /**
       * The asset balance of the account
       */
      returns: ReferenceAccountAssetReturns['getAssetBalance()uint64']
    }>
    & Record<'getAssetBalanceWithArg(address,uint64)uint64' | 'getAssetBalanceWithArg', {
      argsObj: ReferenceAccountAssetArgs['obj']['getAssetBalanceWithArg(address,uint64)uint64']
      argsTuple: ReferenceAccountAssetArgs['tuple']['getAssetBalanceWithArg(address,uint64)uint64']
      /**
       * The asset balance of the account
       */
      returns: ReferenceAccountAssetReturns['getAssetBalanceWithArg(address,uint64)uint64']
    }>
}

/**
 * Defines the possible abi call signatures.
 */
export type ReferenceAccountAssetSignatures = keyof ReferenceAccountAssetTypes['methods']
/**
 * Defines the possible abi call signatures for methods that return a non-void value.
 */
export type ReferenceAccountAssetNonVoidMethodSignatures = keyof ReferenceAccountAssetTypes['methods'] extends infer T ? T extends keyof ReferenceAccountAssetTypes['methods'] ? MethodReturn<T> extends void ? never : T  : never : never
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the ReferenceAccountAsset smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends ReferenceAccountAssetSignatures> = ReferenceAccountAssetTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the ReferenceAccountAsset smart contract to the method's return type
 */
export type MethodReturn<TSignature extends ReferenceAccountAssetSignatures> = ReferenceAccountAssetTypes['methods'][TSignature]['returns']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the ReferenceAccountAsset smart contract
 */
export abstract class ReferenceAccountAssetParamsFactory {
  /**
   * Constructs a no op call for the getAssetBalance()uint64 ABI method
   *
   * Returns the balance of a specific asset in a hardcoded account
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static getAssetBalance(params: CallParams<ReferenceAccountAssetArgs['obj']['getAssetBalance()uint64'] | ReferenceAccountAssetArgs['tuple']['getAssetBalance()uint64']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'getAssetBalance()uint64' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the getAssetBalanceWithArg(address,uint64)uint64 ABI method
   *
   * Returns the balance of a specific asset in a provided account
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static getAssetBalanceWithArg(params: CallParams<ReferenceAccountAssetArgs['obj']['getAssetBalanceWithArg(address,uint64)uint64'] | ReferenceAccountAssetArgs['tuple']['getAssetBalanceWithArg(address,uint64)uint64']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'getAssetBalanceWithArg(address,uint64)uint64' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.account, params.args.asset],
    }
  }
}

/**
 * A client to make calls to the ReferenceAccountAsset smart contract
 */
export class ReferenceAccountAssetClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `ReferenceAccountAssetClient`
   *
   * @param appClient An `AppClient` instance which has been created with the ReferenceAccountAsset app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `ReferenceAccountAssetClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Checks for decode errors on the given return value and maps the return value to the return type for the given method
   * @returns The typed return value or undefined if there was no value
   */
  decodeReturnValue<TSignature extends ReferenceAccountAssetNonVoidMethodSignatures>(method: TSignature, returnValue: ABIReturn | undefined) {
    return returnValue !== undefined ? getArc56ReturnValue<MethodReturn<TSignature>>(returnValue, this.appClient.getABIMethod(method), APP_SPEC.structs) : undefined
  }

  /**
   * Returns a new `ReferenceAccountAssetClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<ReferenceAccountAssetClient> {
    return new ReferenceAccountAssetClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `ReferenceAccountAssetClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<ReferenceAccountAssetClient> {
    return new ReferenceAccountAssetClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Makes a clear_state call to an existing instance of the ReferenceAccountAsset smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the ReferenceAccountAsset smart contract using the `getAssetBalance()uint64` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * Returns the balance of a specific asset in a hardcoded account
     *
     * @param params The params for the smart contract call
     * @returns The call params: The asset balance of the account
     */
    getAssetBalance: (params: CallParams<ReferenceAccountAssetArgs['obj']['getAssetBalance()uint64'] | ReferenceAccountAssetArgs['tuple']['getAssetBalance()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(ReferenceAccountAssetParamsFactory.getAssetBalance(params))
    },

    /**
     * Makes a call to the ReferenceAccountAsset smart contract using the `getAssetBalanceWithArg(address,uint64)uint64` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * Returns the balance of a specific asset in a provided account
     *
     * @param params The params for the smart contract call
     * @returns The call params: The asset balance of the account
     */
    getAssetBalanceWithArg: (params: CallParams<ReferenceAccountAssetArgs['obj']['getAssetBalanceWithArg(address,uint64)uint64'] | ReferenceAccountAssetArgs['tuple']['getAssetBalanceWithArg(address,uint64)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(ReferenceAccountAssetParamsFactory.getAssetBalanceWithArg(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Makes a clear_state call to an existing instance of the ReferenceAccountAsset smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the ReferenceAccountAsset smart contract using the `getAssetBalance()uint64` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * Returns the balance of a specific asset in a hardcoded account
     *
     * @param params The params for the smart contract call
     * @returns The call transaction: The asset balance of the account
     */
    getAssetBalance: (params: CallParams<ReferenceAccountAssetArgs['obj']['getAssetBalance()uint64'] | ReferenceAccountAssetArgs['tuple']['getAssetBalance()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(ReferenceAccountAssetParamsFactory.getAssetBalance(params))
    },

    /**
     * Makes a call to the ReferenceAccountAsset smart contract using the `getAssetBalanceWithArg(address,uint64)uint64` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * Returns the balance of a specific asset in a provided account
     *
     * @param params The params for the smart contract call
     * @returns The call transaction: The asset balance of the account
     */
    getAssetBalanceWithArg: (params: CallParams<ReferenceAccountAssetArgs['obj']['getAssetBalanceWithArg(address,uint64)uint64'] | ReferenceAccountAssetArgs['tuple']['getAssetBalanceWithArg(address,uint64)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(ReferenceAccountAssetParamsFactory.getAssetBalanceWithArg(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Makes a clear_state call to an existing instance of the ReferenceAccountAsset smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the ReferenceAccountAsset smart contract using the `getAssetBalance()uint64` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * Returns the balance of a specific asset in a hardcoded account
     *
     * @param params The params for the smart contract call
     * @returns The call result: The asset balance of the account
     */
    getAssetBalance: async (params: CallParams<ReferenceAccountAssetArgs['obj']['getAssetBalance()uint64'] | ReferenceAccountAssetArgs['tuple']['getAssetBalance()uint64']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(ReferenceAccountAssetParamsFactory.getAssetBalance(params))
      return {...result, return: result.return as unknown as (undefined | ReferenceAccountAssetReturns['getAssetBalance()uint64'])}
    },

    /**
     * Makes a call to the ReferenceAccountAsset smart contract using the `getAssetBalanceWithArg(address,uint64)uint64` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * Returns the balance of a specific asset in a provided account
     *
     * @param params The params for the smart contract call
     * @returns The call result: The asset balance of the account
     */
    getAssetBalanceWithArg: async (params: CallParams<ReferenceAccountAssetArgs['obj']['getAssetBalanceWithArg(address,uint64)uint64'] | ReferenceAccountAssetArgs['tuple']['getAssetBalanceWithArg(address,uint64)uint64']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(ReferenceAccountAssetParamsFactory.getAssetBalanceWithArg(params))
      return {...result, return: result.return as unknown as (undefined | ReferenceAccountAssetReturns['getAssetBalanceWithArg(address,uint64)uint64'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new ReferenceAccountAssetClient(this.appClient.clone(params))
  }

  /**
   * Makes a readonly (simulated) call to the ReferenceAccountAsset smart contract using the `getAssetBalance()uint64` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * Returns the balance of a specific asset in a hardcoded account
   *
   * @param params The params for the smart contract call
   * @returns The call result: The asset balance of the account
   */
  async getAssetBalance(params: CallParams<ReferenceAccountAssetArgs['obj']['getAssetBalance()uint64'] | ReferenceAccountAssetArgs['tuple']['getAssetBalance()uint64']> = {args: []}) {
    const result = await this.appClient.send.call(ReferenceAccountAssetParamsFactory.getAssetBalance(params))
    return result.return as unknown as ReferenceAccountAssetReturns['getAssetBalance()uint64']
  }

  /**
   * Makes a readonly (simulated) call to the ReferenceAccountAsset smart contract using the `getAssetBalanceWithArg(address,uint64)uint64` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * Returns the balance of a specific asset in a provided account
   *
   * @param params The params for the smart contract call
   * @returns The call result: The asset balance of the account
   */
  async getAssetBalanceWithArg(params: CallParams<ReferenceAccountAssetArgs['obj']['getAssetBalanceWithArg(address,uint64)uint64'] | ReferenceAccountAssetArgs['tuple']['getAssetBalanceWithArg(address,uint64)uint64']>) {
    const result = await this.appClient.send.call(ReferenceAccountAssetParamsFactory.getAssetBalanceWithArg(params))
    return result.return as unknown as ReferenceAccountAssetReturns['getAssetBalanceWithArg(address,uint64)uint64']
  }

  /**
   * Methods to access state for the current ReferenceAccountAsset app
   */
  state = {
  }

  public newGroup(): ReferenceAccountAssetComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a getAssetBalance()uint64 method call against the ReferenceAccountAsset contract
       */
      getAssetBalance(params: CallParams<ReferenceAccountAssetArgs['obj']['getAssetBalance()uint64'] | ReferenceAccountAssetArgs['tuple']['getAssetBalance()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.getAssetBalance(params)))
        resultMappers.push((v) => client.decodeReturnValue('getAssetBalance()uint64', v))
        return this
      },
      /**
       * Add a getAssetBalanceWithArg(address,uint64)uint64 method call against the ReferenceAccountAsset contract
       */
      getAssetBalanceWithArg(params: CallParams<ReferenceAccountAssetArgs['obj']['getAssetBalanceWithArg(address,uint64)uint64'] | ReferenceAccountAssetArgs['tuple']['getAssetBalanceWithArg(address,uint64)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.getAssetBalanceWithArg(params)))
        resultMappers.push((v) => client.decodeReturnValue('getAssetBalanceWithArg(address,uint64)uint64', v))
        return this
      },
      /**
       * Add a clear state call to the ReferenceAccountAsset contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as ReferenceAccountAssetComposer
  }
}
export type ReferenceAccountAssetComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the getAssetBalance()uint64 ABI method.
   *
   * Returns the balance of a specific asset in a hardcoded account
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  getAssetBalance(params?: CallParams<ReferenceAccountAssetArgs['obj']['getAssetBalance()uint64'] | ReferenceAccountAssetArgs['tuple']['getAssetBalance()uint64']>): ReferenceAccountAssetComposer<[...TReturns, ReferenceAccountAssetReturns['getAssetBalance()uint64'] | undefined]>

  /**
   * Calls the getAssetBalanceWithArg(address,uint64)uint64 ABI method.
   *
   * Returns the balance of a specific asset in a provided account
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  getAssetBalanceWithArg(params?: CallParams<ReferenceAccountAssetArgs['obj']['getAssetBalanceWithArg(address,uint64)uint64'] | ReferenceAccountAssetArgs['tuple']['getAssetBalanceWithArg(address,uint64)uint64']>): ReferenceAccountAssetComposer<[...TReturns, ReferenceAccountAssetReturns['getAssetBalanceWithArg(address,uint64)uint64'] | undefined]>

  /**
   * Makes a clear_state call to an existing instance of the ReferenceAccountAsset smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): ReferenceAccountAssetComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): ReferenceAccountAssetComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<ReferenceAccountAssetComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<ReferenceAccountAssetComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<ReferenceAccountAssetComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<ReferenceAccountAssetComposerResults<TReturns>>
}
export type ReferenceAccountAssetComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

