/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"Arc4DynamicArray","structs":{},"methods":[{"name":"goodbye","args":[{"type":"string","name":"name"}],"returns":{"type":"string[]"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}},{"name":"hello","args":[{"type":"string","name":"name"}],"returns":{"type":"string"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Dynamic Arrays have variable size and capacity.\nThey are similar to native Python lists because they can also append, extend, and pop.","events":[],"recommendations":{}},{"name":"arc4_dynamic_bytes","args":[],"returns":{"type":"byte[]"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"arc4.DynamicBytes is essentially an arc4.DynamicArray[arc4.Byte] with additional convenience methods","events":[],"recommendations":{}}],"arcs":[22,28],"networks":{},"state":{"schema":{"global":{"ints":0,"bytes":0},"local":{"ints":0,"bytes":0}},"keys":{"global":{},"local":{},"box":{}},"maps":{"global":{},"local":{},"box":{}}},"bareActions":{"create":["NoOp"],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[14],"errorMessage":"OnCompletion must be NoOp"},{"pc":[71],"errorMessage":"OnCompletion must be NoOp && can only call when creating"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"events":[]} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


/**
 * The argument types for the Arc4DynamicArray contract
 */
export type Arc4DynamicArrayArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'goodbye(string)string[]': {
      name: string
    }
    'hello(string)string': {
      name: string
    }
    'arc4_dynamic_bytes()byte[]': Record<string, never>
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'goodbye(string)string[]': [name: string]
    'hello(string)string': [name: string]
    'arc4_dynamic_bytes()byte[]': []
  }
}

/**
 * The return type for each method
 */
export type Arc4DynamicArrayReturns = {
  'goodbye(string)string[]': string[]
  'hello(string)string': string
  'arc4_dynamic_bytes()byte[]': Uint8Array
}

/**
 * Defines the types of available calls and state of the Arc4DynamicArray smart contract.
 */
export type Arc4DynamicArrayTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'goodbye(string)string[]' | 'goodbye', {
      argsObj: Arc4DynamicArrayArgs['obj']['goodbye(string)string[]']
      argsTuple: Arc4DynamicArrayArgs['tuple']['goodbye(string)string[]']
      returns: Arc4DynamicArrayReturns['goodbye(string)string[]']
    }>
    & Record<'hello(string)string' | 'hello', {
      argsObj: Arc4DynamicArrayArgs['obj']['hello(string)string']
      argsTuple: Arc4DynamicArrayArgs['tuple']['hello(string)string']
      returns: Arc4DynamicArrayReturns['hello(string)string']
    }>
    & Record<'arc4_dynamic_bytes()byte[]' | 'arc4_dynamic_bytes', {
      argsObj: Arc4DynamicArrayArgs['obj']['arc4_dynamic_bytes()byte[]']
      argsTuple: Arc4DynamicArrayArgs['tuple']['arc4_dynamic_bytes()byte[]']
      returns: Arc4DynamicArrayReturns['arc4_dynamic_bytes()byte[]']
    }>
}

/**
 * Defines the possible abi call signatures.
 */
export type Arc4DynamicArraySignatures = keyof Arc4DynamicArrayTypes['methods']
/**
 * Defines the possible abi call signatures for methods that return a non-void value.
 */
export type Arc4DynamicArrayNonVoidMethodSignatures = keyof Arc4DynamicArrayTypes['methods'] extends infer T ? T extends keyof Arc4DynamicArrayTypes['methods'] ? MethodReturn<T> extends void ? never : T  : never : never
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the Arc4DynamicArray smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends Arc4DynamicArraySignatures> = Arc4DynamicArrayTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the Arc4DynamicArray smart contract to the method's return type
 */
export type MethodReturn<TSignature extends Arc4DynamicArraySignatures> = Arc4DynamicArrayTypes['methods'][TSignature]['returns']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the Arc4DynamicArray smart contract
 */
export abstract class Arc4DynamicArrayParamsFactory {
  /**
   * Constructs a no op call for the goodbye(string)string[] ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static goodbye(params: CallParams<Arc4DynamicArrayArgs['obj']['goodbye(string)string[]'] | Arc4DynamicArrayArgs['tuple']['goodbye(string)string[]']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'goodbye(string)string[]' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.name],
    }
  }
  /**
   * Constructs a no op call for the hello(string)string ABI method
   *
  * Dynamic Arrays have variable size and capacity.
  They are similar to native Python lists because they can also append, extend, and pop.

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static hello(params: CallParams<Arc4DynamicArrayArgs['obj']['hello(string)string'] | Arc4DynamicArrayArgs['tuple']['hello(string)string']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'hello(string)string' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.name],
    }
  }
  /**
   * Constructs a no op call for the arc4_dynamic_bytes()byte[] ABI method
   *
   * arc4.DynamicBytes is essentially an arc4.DynamicArray[arc4.Byte] with additional convenience methods
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static arc4DynamicBytes(params: CallParams<Arc4DynamicArrayArgs['obj']['arc4_dynamic_bytes()byte[]'] | Arc4DynamicArrayArgs['tuple']['arc4_dynamic_bytes()byte[]']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'arc4_dynamic_bytes()byte[]' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
}

/**
 * A client to make calls to the Arc4DynamicArray smart contract
 */
export class Arc4DynamicArrayClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `Arc4DynamicArrayClient`
   *
   * @param appClient An `AppClient` instance which has been created with the Arc4DynamicArray app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `Arc4DynamicArrayClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Checks for decode errors on the given return value and maps the return value to the return type for the given method
   * @returns The typed return value or undefined if there was no value
   */
  decodeReturnValue<TSignature extends Arc4DynamicArrayNonVoidMethodSignatures>(method: TSignature, returnValue: ABIReturn | undefined) {
    return returnValue !== undefined ? getArc56ReturnValue<MethodReturn<TSignature>>(returnValue, this.appClient.getABIMethod(method), APP_SPEC.structs) : undefined
  }

  /**
   * Returns a new `Arc4DynamicArrayClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<Arc4DynamicArrayClient> {
    return new Arc4DynamicArrayClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `Arc4DynamicArrayClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<Arc4DynamicArrayClient> {
    return new Arc4DynamicArrayClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Makes a clear_state call to an existing instance of the Arc4DynamicArray smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the Arc4DynamicArray smart contract using the `goodbye(string)string[]` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    goodbye: (params: CallParams<Arc4DynamicArrayArgs['obj']['goodbye(string)string[]'] | Arc4DynamicArrayArgs['tuple']['goodbye(string)string[]']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(Arc4DynamicArrayParamsFactory.goodbye(params))
    },

    /**
     * Makes a call to the Arc4DynamicArray smart contract using the `hello(string)string` ABI method.
     *
    * Dynamic Arrays have variable size and capacity.
    They are similar to native Python lists because they can also append, extend, and pop.

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    hello: (params: CallParams<Arc4DynamicArrayArgs['obj']['hello(string)string'] | Arc4DynamicArrayArgs['tuple']['hello(string)string']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(Arc4DynamicArrayParamsFactory.hello(params))
    },

    /**
     * Makes a call to the Arc4DynamicArray smart contract using the `arc4_dynamic_bytes()byte[]` ABI method.
     *
     * arc4.DynamicBytes is essentially an arc4.DynamicArray[arc4.Byte] with additional convenience methods
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    arc4DynamicBytes: (params: CallParams<Arc4DynamicArrayArgs['obj']['arc4_dynamic_bytes()byte[]'] | Arc4DynamicArrayArgs['tuple']['arc4_dynamic_bytes()byte[]']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(Arc4DynamicArrayParamsFactory.arc4DynamicBytes(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Makes a clear_state call to an existing instance of the Arc4DynamicArray smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the Arc4DynamicArray smart contract using the `goodbye(string)string[]` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    goodbye: (params: CallParams<Arc4DynamicArrayArgs['obj']['goodbye(string)string[]'] | Arc4DynamicArrayArgs['tuple']['goodbye(string)string[]']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(Arc4DynamicArrayParamsFactory.goodbye(params))
    },

    /**
     * Makes a call to the Arc4DynamicArray smart contract using the `hello(string)string` ABI method.
     *
    * Dynamic Arrays have variable size and capacity.
    They are similar to native Python lists because they can also append, extend, and pop.

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    hello: (params: CallParams<Arc4DynamicArrayArgs['obj']['hello(string)string'] | Arc4DynamicArrayArgs['tuple']['hello(string)string']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(Arc4DynamicArrayParamsFactory.hello(params))
    },

    /**
     * Makes a call to the Arc4DynamicArray smart contract using the `arc4_dynamic_bytes()byte[]` ABI method.
     *
     * arc4.DynamicBytes is essentially an arc4.DynamicArray[arc4.Byte] with additional convenience methods
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    arc4DynamicBytes: (params: CallParams<Arc4DynamicArrayArgs['obj']['arc4_dynamic_bytes()byte[]'] | Arc4DynamicArrayArgs['tuple']['arc4_dynamic_bytes()byte[]']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(Arc4DynamicArrayParamsFactory.arc4DynamicBytes(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Makes a clear_state call to an existing instance of the Arc4DynamicArray smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the Arc4DynamicArray smart contract using the `goodbye(string)string[]` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    goodbye: async (params: CallParams<Arc4DynamicArrayArgs['obj']['goodbye(string)string[]'] | Arc4DynamicArrayArgs['tuple']['goodbye(string)string[]']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(Arc4DynamicArrayParamsFactory.goodbye(params))
      return {...result, return: result.return as unknown as (undefined | Arc4DynamicArrayReturns['goodbye(string)string[]'])}
    },

    /**
     * Makes a call to the Arc4DynamicArray smart contract using the `hello(string)string` ABI method.
     *
    * Dynamic Arrays have variable size and capacity.
    They are similar to native Python lists because they can also append, extend, and pop.

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    hello: async (params: CallParams<Arc4DynamicArrayArgs['obj']['hello(string)string'] | Arc4DynamicArrayArgs['tuple']['hello(string)string']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(Arc4DynamicArrayParamsFactory.hello(params))
      return {...result, return: result.return as unknown as (undefined | Arc4DynamicArrayReturns['hello(string)string'])}
    },

    /**
     * Makes a call to the Arc4DynamicArray smart contract using the `arc4_dynamic_bytes()byte[]` ABI method.
     *
     * arc4.DynamicBytes is essentially an arc4.DynamicArray[arc4.Byte] with additional convenience methods
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    arc4DynamicBytes: async (params: CallParams<Arc4DynamicArrayArgs['obj']['arc4_dynamic_bytes()byte[]'] | Arc4DynamicArrayArgs['tuple']['arc4_dynamic_bytes()byte[]']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(Arc4DynamicArrayParamsFactory.arc4DynamicBytes(params))
      return {...result, return: result.return as unknown as (undefined | Arc4DynamicArrayReturns['arc4_dynamic_bytes()byte[]'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new Arc4DynamicArrayClient(this.appClient.clone(params))
  }

  /**
   * Methods to access state for the current Arc4DynamicArray app
   */
  state = {
  }

  public newGroup(): Arc4DynamicArrayComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a goodbye(string)string[] method call against the Arc4DynamicArray contract
       */
      goodbye(params: CallParams<Arc4DynamicArrayArgs['obj']['goodbye(string)string[]'] | Arc4DynamicArrayArgs['tuple']['goodbye(string)string[]']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.goodbye(params)))
        resultMappers.push((v) => client.decodeReturnValue('goodbye(string)string[]', v))
        return this
      },
      /**
       * Add a hello(string)string method call against the Arc4DynamicArray contract
       */
      hello(params: CallParams<Arc4DynamicArrayArgs['obj']['hello(string)string'] | Arc4DynamicArrayArgs['tuple']['hello(string)string']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.hello(params)))
        resultMappers.push((v) => client.decodeReturnValue('hello(string)string', v))
        return this
      },
      /**
       * Add a arc4_dynamic_bytes()byte[] method call against the Arc4DynamicArray contract
       */
      arc4DynamicBytes(params: CallParams<Arc4DynamicArrayArgs['obj']['arc4_dynamic_bytes()byte[]'] | Arc4DynamicArrayArgs['tuple']['arc4_dynamic_bytes()byte[]']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.arc4DynamicBytes(params)))
        resultMappers.push((v) => client.decodeReturnValue('arc4_dynamic_bytes()byte[]', v))
        return this
      },
      /**
       * Add a clear state call to the Arc4DynamicArray contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as Arc4DynamicArrayComposer
  }
}
export type Arc4DynamicArrayComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the goodbye(string)string[] ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  goodbye(params?: CallParams<Arc4DynamicArrayArgs['obj']['goodbye(string)string[]'] | Arc4DynamicArrayArgs['tuple']['goodbye(string)string[]']>): Arc4DynamicArrayComposer<[...TReturns, Arc4DynamicArrayReturns['goodbye(string)string[]'] | undefined]>

  /**
   * Calls the hello(string)string ABI method.
   *
  * Dynamic Arrays have variable size and capacity.
  They are similar to native Python lists because they can also append, extend, and pop.

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  hello(params?: CallParams<Arc4DynamicArrayArgs['obj']['hello(string)string'] | Arc4DynamicArrayArgs['tuple']['hello(string)string']>): Arc4DynamicArrayComposer<[...TReturns, Arc4DynamicArrayReturns['hello(string)string'] | undefined]>

  /**
   * Calls the arc4_dynamic_bytes()byte[] ABI method.
   *
   * arc4.DynamicBytes is essentially an arc4.DynamicArray[arc4.Byte] with additional convenience methods
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  arc4DynamicBytes(params?: CallParams<Arc4DynamicArrayArgs['obj']['arc4_dynamic_bytes()byte[]'] | Arc4DynamicArrayArgs['tuple']['arc4_dynamic_bytes()byte[]']>): Arc4DynamicArrayComposer<[...TReturns, Arc4DynamicArrayReturns['arc4_dynamic_bytes()byte[]'] | undefined]>

  /**
   * Makes a clear_state call to an existing instance of the Arc4DynamicArray smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): Arc4DynamicArrayComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): Arc4DynamicArrayComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<Arc4DynamicArrayComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<Arc4DynamicArrayComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<Arc4DynamicArrayComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<Arc4DynamicArrayComposerResults<TReturns>>
}
export type Arc4DynamicArrayComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

