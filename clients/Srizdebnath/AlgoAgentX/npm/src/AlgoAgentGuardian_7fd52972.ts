/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"AlgoAgentGuardian","structs":{},"methods":[{"name":"setup_guardian","args":[{"type":"address","name":"agent_address"},{"type":"uint64","name":"daily_limit"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}},{"name":"execute_payment","args":[{"type":"uint64","name":"amount"},{"type":"address","name":"receiver"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}},{"name":"toggle_kill_switch","args":[],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}}],"arcs":[22,28],"networks":{},"state":{"schema":{"global":{"ints":4,"bytes":2},"local":{"ints":0,"bytes":0}},"keys":{"global":{"owner":{"keyType":"AVMString","valueType":"address","key":"b3duZXI="},"agent_address":{"keyType":"AVMString","valueType":"address","key":"YWdlbnRfYWRkcmVzcw=="},"daily_limit":{"keyType":"AVMString","valueType":"AVMUint64","key":"ZGFpbHlfbGltaXQ="},"spent_today":{"keyType":"AVMString","valueType":"AVMUint64","key":"c3BlbnRfdG9kYXk="},"last_spend_timestamp":{"keyType":"AVMString","valueType":"AVMUint64","key":"bGFzdF9zcGVuZF90aW1lc3RhbXA="},"is_active":{"keyType":"AVMString","valueType":"AVMUint64","key":"aXNfYWN0aXZl"}},"local":{},"box":{}},"maps":{"global":{},"local":{},"box":{}}},"bareActions":{"create":["NoOp"],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[216],"errorMessage":"Contract is not active"},{"pc":[262],"errorMessage":"Daily limit exceeded"},{"pc":[224],"errorMessage":"Only agent can execute payments"},{"pc":[184],"errorMessage":"Only owner can setup"},{"pc":[297],"errorMessage":"Only owner can toggle"},{"pc":[222],"errorMessage":"check self.agent_address exists"},{"pc":[258],"errorMessage":"check self.daily_limit exists"},{"pc":[215,301],"errorMessage":"check self.is_active exists"},{"pc":[231],"errorMessage":"check self.last_spend_timestamp exists"},{"pc":[182,295],"errorMessage":"check self.owner exists"},{"pc":[247],"errorMessage":"check self.spent_today exists"},{"pc":[167,211],"errorMessage":"invalid number of bytes for arc4.static_array<arc4.uint8, 32>"},{"pc":[175,202],"errorMessage":"invalid number of bytes for arc4.uint64"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"events":[]} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


/**
 * The argument types for the AlgoAgentGuardian contract
 */
export type AlgoAgentGuardianArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'setup_guardian(address,uint64)void': {
      agentAddress: string
      dailyLimit: bigint | number
    }
    'execute_payment(uint64,address)void': {
      amount: bigint | number
      receiver: string
    }
    'toggle_kill_switch()void': Record<string, never>
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'setup_guardian(address,uint64)void': [agentAddress: string, dailyLimit: bigint | number]
    'execute_payment(uint64,address)void': [amount: bigint | number, receiver: string]
    'toggle_kill_switch()void': []
  }
}

/**
 * The return type for each method
 */
export type AlgoAgentGuardianReturns = {
  'setup_guardian(address,uint64)void': void
  'execute_payment(uint64,address)void': void
  'toggle_kill_switch()void': void
}

/**
 * Defines the types of available calls and state of the AlgoAgentGuardian smart contract.
 */
export type AlgoAgentGuardianTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'setup_guardian(address,uint64)void' | 'setup_guardian', {
      argsObj: AlgoAgentGuardianArgs['obj']['setup_guardian(address,uint64)void']
      argsTuple: AlgoAgentGuardianArgs['tuple']['setup_guardian(address,uint64)void']
      returns: AlgoAgentGuardianReturns['setup_guardian(address,uint64)void']
    }>
    & Record<'execute_payment(uint64,address)void' | 'execute_payment', {
      argsObj: AlgoAgentGuardianArgs['obj']['execute_payment(uint64,address)void']
      argsTuple: AlgoAgentGuardianArgs['tuple']['execute_payment(uint64,address)void']
      returns: AlgoAgentGuardianReturns['execute_payment(uint64,address)void']
    }>
    & Record<'toggle_kill_switch()void' | 'toggle_kill_switch', {
      argsObj: AlgoAgentGuardianArgs['obj']['toggle_kill_switch()void']
      argsTuple: AlgoAgentGuardianArgs['tuple']['toggle_kill_switch()void']
      returns: AlgoAgentGuardianReturns['toggle_kill_switch()void']
    }>
  /**
   * Defines the shape of the state of the application.
   */
  state: {
    global: {
      keys: {
        owner: string
        agentAddress: string
        dailyLimit: bigint
        spentToday: bigint
        lastSpendTimestamp: bigint
        isActive: bigint
      }
      maps: {}
    }
  }
}

/**
 * Defines the possible abi call signatures.
 */
export type AlgoAgentGuardianSignatures = keyof AlgoAgentGuardianTypes['methods']
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the AlgoAgentGuardian smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends AlgoAgentGuardianSignatures> = AlgoAgentGuardianTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the AlgoAgentGuardian smart contract to the method's return type
 */
export type MethodReturn<TSignature extends AlgoAgentGuardianSignatures> = AlgoAgentGuardianTypes['methods'][TSignature]['returns']

/**
 * Defines the shape of the keyed global state of the application.
 */
export type GlobalKeysState = AlgoAgentGuardianTypes['state']['global']['keys']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the AlgoAgentGuardian smart contract
 */
export abstract class AlgoAgentGuardianParamsFactory {
  /**
   * Constructs a no op call for the setup_guardian(address,uint64)void ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static setupGuardian(params: CallParams<AlgoAgentGuardianArgs['obj']['setup_guardian(address,uint64)void'] | AlgoAgentGuardianArgs['tuple']['setup_guardian(address,uint64)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'setup_guardian(address,uint64)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.agentAddress, params.args.dailyLimit],
    }
  }
  /**
   * Constructs a no op call for the execute_payment(uint64,address)void ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static executePayment(params: CallParams<AlgoAgentGuardianArgs['obj']['execute_payment(uint64,address)void'] | AlgoAgentGuardianArgs['tuple']['execute_payment(uint64,address)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'execute_payment(uint64,address)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.amount, params.args.receiver],
    }
  }
  /**
   * Constructs a no op call for the toggle_kill_switch()void ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static toggleKillSwitch(params: CallParams<AlgoAgentGuardianArgs['obj']['toggle_kill_switch()void'] | AlgoAgentGuardianArgs['tuple']['toggle_kill_switch()void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'toggle_kill_switch()void' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
}

/**
 * A client to make calls to the AlgoAgentGuardian smart contract
 */
export class AlgoAgentGuardianClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `AlgoAgentGuardianClient`
   *
   * @param appClient An `AppClient` instance which has been created with the AlgoAgentGuardian app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `AlgoAgentGuardianClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Returns a new `AlgoAgentGuardianClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<AlgoAgentGuardianClient> {
    return new AlgoAgentGuardianClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `AlgoAgentGuardianClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<AlgoAgentGuardianClient> {
    return new AlgoAgentGuardianClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Makes a clear_state call to an existing instance of the AlgoAgentGuardian smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the AlgoAgentGuardian smart contract using the `setup_guardian(address,uint64)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    setupGuardian: (params: CallParams<AlgoAgentGuardianArgs['obj']['setup_guardian(address,uint64)void'] | AlgoAgentGuardianArgs['tuple']['setup_guardian(address,uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(AlgoAgentGuardianParamsFactory.setupGuardian(params))
    },

    /**
     * Makes a call to the AlgoAgentGuardian smart contract using the `execute_payment(uint64,address)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    executePayment: (params: CallParams<AlgoAgentGuardianArgs['obj']['execute_payment(uint64,address)void'] | AlgoAgentGuardianArgs['tuple']['execute_payment(uint64,address)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(AlgoAgentGuardianParamsFactory.executePayment(params))
    },

    /**
     * Makes a call to the AlgoAgentGuardian smart contract using the `toggle_kill_switch()void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    toggleKillSwitch: (params: CallParams<AlgoAgentGuardianArgs['obj']['toggle_kill_switch()void'] | AlgoAgentGuardianArgs['tuple']['toggle_kill_switch()void']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(AlgoAgentGuardianParamsFactory.toggleKillSwitch(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Makes a clear_state call to an existing instance of the AlgoAgentGuardian smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the AlgoAgentGuardian smart contract using the `setup_guardian(address,uint64)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    setupGuardian: (params: CallParams<AlgoAgentGuardianArgs['obj']['setup_guardian(address,uint64)void'] | AlgoAgentGuardianArgs['tuple']['setup_guardian(address,uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(AlgoAgentGuardianParamsFactory.setupGuardian(params))
    },

    /**
     * Makes a call to the AlgoAgentGuardian smart contract using the `execute_payment(uint64,address)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    executePayment: (params: CallParams<AlgoAgentGuardianArgs['obj']['execute_payment(uint64,address)void'] | AlgoAgentGuardianArgs['tuple']['execute_payment(uint64,address)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(AlgoAgentGuardianParamsFactory.executePayment(params))
    },

    /**
     * Makes a call to the AlgoAgentGuardian smart contract using the `toggle_kill_switch()void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    toggleKillSwitch: (params: CallParams<AlgoAgentGuardianArgs['obj']['toggle_kill_switch()void'] | AlgoAgentGuardianArgs['tuple']['toggle_kill_switch()void']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(AlgoAgentGuardianParamsFactory.toggleKillSwitch(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Makes a clear_state call to an existing instance of the AlgoAgentGuardian smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the AlgoAgentGuardian smart contract using the `setup_guardian(address,uint64)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    setupGuardian: async (params: CallParams<AlgoAgentGuardianArgs['obj']['setup_guardian(address,uint64)void'] | AlgoAgentGuardianArgs['tuple']['setup_guardian(address,uint64)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(AlgoAgentGuardianParamsFactory.setupGuardian(params))
      return {...result, return: result.return as unknown as (undefined | AlgoAgentGuardianReturns['setup_guardian(address,uint64)void'])}
    },

    /**
     * Makes a call to the AlgoAgentGuardian smart contract using the `execute_payment(uint64,address)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    executePayment: async (params: CallParams<AlgoAgentGuardianArgs['obj']['execute_payment(uint64,address)void'] | AlgoAgentGuardianArgs['tuple']['execute_payment(uint64,address)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(AlgoAgentGuardianParamsFactory.executePayment(params))
      return {...result, return: result.return as unknown as (undefined | AlgoAgentGuardianReturns['execute_payment(uint64,address)void'])}
    },

    /**
     * Makes a call to the AlgoAgentGuardian smart contract using the `toggle_kill_switch()void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    toggleKillSwitch: async (params: CallParams<AlgoAgentGuardianArgs['obj']['toggle_kill_switch()void'] | AlgoAgentGuardianArgs['tuple']['toggle_kill_switch()void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(AlgoAgentGuardianParamsFactory.toggleKillSwitch(params))
      return {...result, return: result.return as unknown as (undefined | AlgoAgentGuardianReturns['toggle_kill_switch()void'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new AlgoAgentGuardianClient(this.appClient.clone(params))
  }

  /**
   * Methods to access state for the current AlgoAgentGuardian app
   */
  state = {
    /**
     * Methods to access global state for the current AlgoAgentGuardian app
     */
    global: {
      /**
       * Get all current keyed values from global state
       */
      getAll: async (): Promise<Partial<Expand<GlobalKeysState>>> => {
        const result = await this.appClient.state.global.getAll()
        return {
          owner: result.owner,
          agentAddress: result.agent_address,
          dailyLimit: result.daily_limit,
          spentToday: result.spent_today,
          lastSpendTimestamp: result.last_spend_timestamp,
          isActive: result.is_active,
        }
      },
      /**
       * Get the current value of the owner key in global state
       */
      owner: async (): Promise<string | undefined> => { return (await this.appClient.state.global.getValue("owner")) as string | undefined },
      /**
       * Get the current value of the agent_address key in global state
       */
      agentAddress: async (): Promise<string | undefined> => { return (await this.appClient.state.global.getValue("agent_address")) as string | undefined },
      /**
       * Get the current value of the daily_limit key in global state
       */
      dailyLimit: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("daily_limit")) as bigint | undefined },
      /**
       * Get the current value of the spent_today key in global state
       */
      spentToday: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("spent_today")) as bigint | undefined },
      /**
       * Get the current value of the last_spend_timestamp key in global state
       */
      lastSpendTimestamp: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("last_spend_timestamp")) as bigint | undefined },
      /**
       * Get the current value of the is_active key in global state
       */
      isActive: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("is_active")) as bigint | undefined },
    },
  }

  public newGroup(): AlgoAgentGuardianComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a setup_guardian(address,uint64)void method call against the AlgoAgentGuardian contract
       */
      setupGuardian(params: CallParams<AlgoAgentGuardianArgs['obj']['setup_guardian(address,uint64)void'] | AlgoAgentGuardianArgs['tuple']['setup_guardian(address,uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.setupGuardian(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a execute_payment(uint64,address)void method call against the AlgoAgentGuardian contract
       */
      executePayment(params: CallParams<AlgoAgentGuardianArgs['obj']['execute_payment(uint64,address)void'] | AlgoAgentGuardianArgs['tuple']['execute_payment(uint64,address)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.executePayment(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a toggle_kill_switch()void method call against the AlgoAgentGuardian contract
       */
      toggleKillSwitch(params: CallParams<AlgoAgentGuardianArgs['obj']['toggle_kill_switch()void'] | AlgoAgentGuardianArgs['tuple']['toggle_kill_switch()void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.toggleKillSwitch(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a clear state call to the AlgoAgentGuardian contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as AlgoAgentGuardianComposer
  }
}
export type AlgoAgentGuardianComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the setup_guardian(address,uint64)void ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  setupGuardian(params?: CallParams<AlgoAgentGuardianArgs['obj']['setup_guardian(address,uint64)void'] | AlgoAgentGuardianArgs['tuple']['setup_guardian(address,uint64)void']>): AlgoAgentGuardianComposer<[...TReturns, AlgoAgentGuardianReturns['setup_guardian(address,uint64)void'] | undefined]>

  /**
   * Calls the execute_payment(uint64,address)void ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  executePayment(params?: CallParams<AlgoAgentGuardianArgs['obj']['execute_payment(uint64,address)void'] | AlgoAgentGuardianArgs['tuple']['execute_payment(uint64,address)void']>): AlgoAgentGuardianComposer<[...TReturns, AlgoAgentGuardianReturns['execute_payment(uint64,address)void'] | undefined]>

  /**
   * Calls the toggle_kill_switch()void ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  toggleKillSwitch(params?: CallParams<AlgoAgentGuardianArgs['obj']['toggle_kill_switch()void'] | AlgoAgentGuardianArgs['tuple']['toggle_kill_switch()void']>): AlgoAgentGuardianComposer<[...TReturns, AlgoAgentGuardianReturns['toggle_kill_switch()void'] | undefined]>

  /**
   * Makes a clear_state call to an existing instance of the AlgoAgentGuardian smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): AlgoAgentGuardianComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): AlgoAgentGuardianComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<AlgoAgentGuardianComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<AlgoAgentGuardianComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<AlgoAgentGuardianComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<AlgoAgentGuardianComposerResults<TReturns>>
}
export type AlgoAgentGuardianComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

