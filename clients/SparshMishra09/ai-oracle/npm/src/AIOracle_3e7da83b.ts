/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"AIOracle","structs":{},"methods":[{"name":"request_data","args":[{"type":"string","name":"question"},{"type":"pay","name":"payment"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"A user posts a question and locks a bounty.","events":[],"recommendations":{}},{"name":"submit_answer","args":[{"type":"string","name":"answer"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"An AI agent provides an answer to the current question.","events":[],"recommendations":{}},{"name":"approve_and_pay","args":[],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"The requester approves the answer and pays the AI agent.","events":[],"recommendations":{}}],"arcs":[22,28],"networks":{},"state":{"schema":{"global":{"ints":2,"bytes":4},"local":{"ints":0,"bytes":0}},"keys":{"global":{"requester":{"keyType":"AVMString","valueType":"address","key":"cmVxdWVzdGVy","desc":"Address of the user asking the question"},"provider":{"keyType":"AVMString","valueType":"address","key":"cHJvdmlkZXI=","desc":"Address of the AI agent providing the answer"},"question":{"keyType":"AVMString","valueType":"AVMString","key":"cXVlc3Rpb24=","desc":"The data being requested"},"answer":{"keyType":"AVMString","valueType":"AVMString","key":"YW5zd2Vy","desc":"The data provided by the AI"},"bounty":{"keyType":"AVMString","valueType":"AVMUint64","key":"Ym91bnR5","desc":"The amount of ALGO locked in escrow"},"status":{"keyType":"AVMString","valueType":"AVMUint64","key":"c3RhdHVz","desc":"0: Idle, 1: Requested, 2: Answered"}},"local":{},"box":{}},"maps":{"global":{},"local":{},"box":{}}},"bareActions":{"create":["NoOp"],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[178],"errorMessage":"Bounty must be greater than 0"},{"pc":[166],"errorMessage":"Contract is not Idle"},{"pc":[241],"errorMessage":"Contract is not in Answered state"},{"pc":[220],"errorMessage":"Contract is not in Requested state"},{"pc":[249],"errorMessage":"Only the requester can approve"},{"pc":[173],"errorMessage":"Payment must be sent to contract"},{"pc":[259],"errorMessage":"check self.bounty exists"},{"pc":[255],"errorMessage":"check self.provider exists"},{"pc":[247],"errorMessage":"check self.requester exists"},{"pc":[164,217,238],"errorMessage":"check self.status exists"},{"pc":[140,203],"errorMessage":"invalid array length header"},{"pc":[147,210],"errorMessage":"invalid number of bytes for arc4.dynamic_array<arc4.uint8>"},{"pc":[160],"errorMessage":"transaction type is pay"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"events":[]} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


/**
 * The argument types for the AiOracle contract
 */
export type AiOracleArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'request_data(string,pay)void': {
      question: string
      payment: AppMethodCallTransactionArgument
    }
    'submit_answer(string)void': {
      answer: string
    }
    'approve_and_pay()void': Record<string, never>
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'request_data(string,pay)void': [question: string, payment: AppMethodCallTransactionArgument]
    'submit_answer(string)void': [answer: string]
    'approve_and_pay()void': []
  }
}

/**
 * The return type for each method
 */
export type AiOracleReturns = {
  'request_data(string,pay)void': void
  'submit_answer(string)void': void
  'approve_and_pay()void': void
}

/**
 * Defines the types of available calls and state of the AiOracle smart contract.
 */
export type AiOracleTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'request_data(string,pay)void' | 'request_data', {
      argsObj: AiOracleArgs['obj']['request_data(string,pay)void']
      argsTuple: AiOracleArgs['tuple']['request_data(string,pay)void']
      returns: AiOracleReturns['request_data(string,pay)void']
    }>
    & Record<'submit_answer(string)void' | 'submit_answer', {
      argsObj: AiOracleArgs['obj']['submit_answer(string)void']
      argsTuple: AiOracleArgs['tuple']['submit_answer(string)void']
      returns: AiOracleReturns['submit_answer(string)void']
    }>
    & Record<'approve_and_pay()void' | 'approve_and_pay', {
      argsObj: AiOracleArgs['obj']['approve_and_pay()void']
      argsTuple: AiOracleArgs['tuple']['approve_and_pay()void']
      returns: AiOracleReturns['approve_and_pay()void']
    }>
  /**
   * Defines the shape of the state of the application.
   */
  state: {
    global: {
      keys: {
        /**
         * Address of the user asking the question
         */
        requester: string
        /**
         * Address of the AI agent providing the answer
         */
        provider: string
        /**
         * The data being requested
         */
        question: string
        /**
         * The data provided by the AI
         */
        answer: string
        /**
         * The amount of ALGO locked in escrow
         */
        bounty: bigint
        /**
         * 0: Idle, 1: Requested, 2: Answered
         */
        status: bigint
      }
      maps: {}
    }
  }
}

/**
 * Defines the possible abi call signatures.
 */
export type AiOracleSignatures = keyof AiOracleTypes['methods']
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the AiOracle smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends AiOracleSignatures> = AiOracleTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the AiOracle smart contract to the method's return type
 */
export type MethodReturn<TSignature extends AiOracleSignatures> = AiOracleTypes['methods'][TSignature]['returns']

/**
 * Defines the shape of the keyed global state of the application.
 */
export type GlobalKeysState = AiOracleTypes['state']['global']['keys']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the AiOracle smart contract
 */
export abstract class AiOracleParamsFactory {
  /**
   * Constructs a no op call for the request_data(string,pay)void ABI method
   *
   * A user posts a question and locks a bounty.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static requestData(params: CallParams<AiOracleArgs['obj']['request_data(string,pay)void'] | AiOracleArgs['tuple']['request_data(string,pay)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'request_data(string,pay)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.question, params.args.payment],
    }
  }
  /**
   * Constructs a no op call for the submit_answer(string)void ABI method
   *
   * An AI agent provides an answer to the current question.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static submitAnswer(params: CallParams<AiOracleArgs['obj']['submit_answer(string)void'] | AiOracleArgs['tuple']['submit_answer(string)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'submit_answer(string)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.answer],
    }
  }
  /**
   * Constructs a no op call for the approve_and_pay()void ABI method
   *
   * The requester approves the answer and pays the AI agent.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static approveAndPay(params: CallParams<AiOracleArgs['obj']['approve_and_pay()void'] | AiOracleArgs['tuple']['approve_and_pay()void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'approve_and_pay()void' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
}

/**
 * A client to make calls to the AIOracle smart contract
 */
export class AiOracleClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `AiOracleClient`
   *
   * @param appClient An `AppClient` instance which has been created with the AiOracle app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `AiOracleClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Returns a new `AiOracleClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<AiOracleClient> {
    return new AiOracleClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `AiOracleClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<AiOracleClient> {
    return new AiOracleClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Makes a clear_state call to an existing instance of the AIOracle smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the AIOracle smart contract using the `request_data(string,pay)void` ABI method.
     *
     * A user posts a question and locks a bounty.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    requestData: (params: CallParams<AiOracleArgs['obj']['request_data(string,pay)void'] | AiOracleArgs['tuple']['request_data(string,pay)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(AiOracleParamsFactory.requestData(params))
    },

    /**
     * Makes a call to the AIOracle smart contract using the `submit_answer(string)void` ABI method.
     *
     * An AI agent provides an answer to the current question.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    submitAnswer: (params: CallParams<AiOracleArgs['obj']['submit_answer(string)void'] | AiOracleArgs['tuple']['submit_answer(string)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(AiOracleParamsFactory.submitAnswer(params))
    },

    /**
     * Makes a call to the AIOracle smart contract using the `approve_and_pay()void` ABI method.
     *
     * The requester approves the answer and pays the AI agent.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    approveAndPay: (params: CallParams<AiOracleArgs['obj']['approve_and_pay()void'] | AiOracleArgs['tuple']['approve_and_pay()void']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(AiOracleParamsFactory.approveAndPay(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Makes a clear_state call to an existing instance of the AIOracle smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the AIOracle smart contract using the `request_data(string,pay)void` ABI method.
     *
     * A user posts a question and locks a bounty.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    requestData: (params: CallParams<AiOracleArgs['obj']['request_data(string,pay)void'] | AiOracleArgs['tuple']['request_data(string,pay)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(AiOracleParamsFactory.requestData(params))
    },

    /**
     * Makes a call to the AIOracle smart contract using the `submit_answer(string)void` ABI method.
     *
     * An AI agent provides an answer to the current question.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    submitAnswer: (params: CallParams<AiOracleArgs['obj']['submit_answer(string)void'] | AiOracleArgs['tuple']['submit_answer(string)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(AiOracleParamsFactory.submitAnswer(params))
    },

    /**
     * Makes a call to the AIOracle smart contract using the `approve_and_pay()void` ABI method.
     *
     * The requester approves the answer and pays the AI agent.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    approveAndPay: (params: CallParams<AiOracleArgs['obj']['approve_and_pay()void'] | AiOracleArgs['tuple']['approve_and_pay()void']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(AiOracleParamsFactory.approveAndPay(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Makes a clear_state call to an existing instance of the AIOracle smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the AIOracle smart contract using the `request_data(string,pay)void` ABI method.
     *
     * A user posts a question and locks a bounty.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    requestData: async (params: CallParams<AiOracleArgs['obj']['request_data(string,pay)void'] | AiOracleArgs['tuple']['request_data(string,pay)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(AiOracleParamsFactory.requestData(params))
      return {...result, return: result.return as unknown as (undefined | AiOracleReturns['request_data(string,pay)void'])}
    },

    /**
     * Makes a call to the AIOracle smart contract using the `submit_answer(string)void` ABI method.
     *
     * An AI agent provides an answer to the current question.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    submitAnswer: async (params: CallParams<AiOracleArgs['obj']['submit_answer(string)void'] | AiOracleArgs['tuple']['submit_answer(string)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(AiOracleParamsFactory.submitAnswer(params))
      return {...result, return: result.return as unknown as (undefined | AiOracleReturns['submit_answer(string)void'])}
    },

    /**
     * Makes a call to the AIOracle smart contract using the `approve_and_pay()void` ABI method.
     *
     * The requester approves the answer and pays the AI agent.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    approveAndPay: async (params: CallParams<AiOracleArgs['obj']['approve_and_pay()void'] | AiOracleArgs['tuple']['approve_and_pay()void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(AiOracleParamsFactory.approveAndPay(params))
      return {...result, return: result.return as unknown as (undefined | AiOracleReturns['approve_and_pay()void'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new AiOracleClient(this.appClient.clone(params))
  }

  /**
   * Methods to access state for the current AIOracle app
   */
  state = {
    /**
     * Methods to access global state for the current AIOracle app
     */
    global: {
      /**
       * Get all current keyed values from global state
       */
      getAll: async (): Promise<Partial<Expand<GlobalKeysState>>> => {
        const result = await this.appClient.state.global.getAll()
        return {
          requester: result.requester,
          provider: result.provider,
          question: result.question,
          answer: result.answer,
          bounty: result.bounty,
          status: result.status,
        }
      },
      /**
       * Get the current value of the requester key in global state
       */
      requester: async (): Promise<string | undefined> => { return (await this.appClient.state.global.getValue("requester")) as string | undefined },
      /**
       * Get the current value of the provider key in global state
       */
      provider: async (): Promise<string | undefined> => { return (await this.appClient.state.global.getValue("provider")) as string | undefined },
      /**
       * Get the current value of the question key in global state
       */
      question: async (): Promise<string | undefined> => { return (await this.appClient.state.global.getValue("question")) as string | undefined },
      /**
       * Get the current value of the answer key in global state
       */
      answer: async (): Promise<string | undefined> => { return (await this.appClient.state.global.getValue("answer")) as string | undefined },
      /**
       * Get the current value of the bounty key in global state
       */
      bounty: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("bounty")) as bigint | undefined },
      /**
       * Get the current value of the status key in global state
       */
      status: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("status")) as bigint | undefined },
    },
  }

  public newGroup(): AiOracleComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a request_data(string,pay)void method call against the AIOracle contract
       */
      requestData(params: CallParams<AiOracleArgs['obj']['request_data(string,pay)void'] | AiOracleArgs['tuple']['request_data(string,pay)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.requestData(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a submit_answer(string)void method call against the AIOracle contract
       */
      submitAnswer(params: CallParams<AiOracleArgs['obj']['submit_answer(string)void'] | AiOracleArgs['tuple']['submit_answer(string)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.submitAnswer(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a approve_and_pay()void method call against the AIOracle contract
       */
      approveAndPay(params: CallParams<AiOracleArgs['obj']['approve_and_pay()void'] | AiOracleArgs['tuple']['approve_and_pay()void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.approveAndPay(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a clear state call to the AIOracle contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as AiOracleComposer
  }
}
export type AiOracleComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the request_data(string,pay)void ABI method.
   *
   * A user posts a question and locks a bounty.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  requestData(params?: CallParams<AiOracleArgs['obj']['request_data(string,pay)void'] | AiOracleArgs['tuple']['request_data(string,pay)void']>): AiOracleComposer<[...TReturns, AiOracleReturns['request_data(string,pay)void'] | undefined]>

  /**
   * Calls the submit_answer(string)void ABI method.
   *
   * An AI agent provides an answer to the current question.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  submitAnswer(params?: CallParams<AiOracleArgs['obj']['submit_answer(string)void'] | AiOracleArgs['tuple']['submit_answer(string)void']>): AiOracleComposer<[...TReturns, AiOracleReturns['submit_answer(string)void'] | undefined]>

  /**
   * Calls the approve_and_pay()void ABI method.
   *
   * The requester approves the answer and pays the AI agent.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  approveAndPay(params?: CallParams<AiOracleArgs['obj']['approve_and_pay()void'] | AiOracleArgs['tuple']['approve_and_pay()void']>): AiOracleComposer<[...TReturns, AiOracleReturns['approve_and_pay()void'] | undefined]>

  /**
   * Makes a clear_state call to an existing instance of the AIOracle smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): AiOracleComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): AiOracleComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<AiOracleComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<AiOracleComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<AiOracleComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<AiOracleComposerResults<TReturns>>
}
export type AiOracleComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

