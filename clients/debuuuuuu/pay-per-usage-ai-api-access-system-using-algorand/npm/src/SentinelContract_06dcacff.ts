/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"SentinelContract","structs":{},"methods":[{"name":"create_application","args":[{"type":"uint64","name":"min_amount"}],"returns":{"type":"void"},"actions":{"create":["NoOp"],"call":[]},"readonly":false,"desc":"Set minimum payment (microAlgos) on deploy.","events":[],"recommendations":{}},{"name":"purchase","args":[{"type":"pay","name":"pay"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Process a valid top-up: Payment txn must be in the atomic group (reference arg).","events":[],"recommendations":{}},{"name":"read_stats","args":[],"returns":{"type":"(uint64,uint64,uint64)"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"desc":"Return (min_payment, total_purchases, total_algo_received_micro).","events":[],"recommendations":{}}],"arcs":[22,28],"desc":"Accepts grouped Payment + App call; tracks purchases and ALGO received in global state.","networks":{},"state":{"schema":{"global":{"ints":3,"bytes":0},"local":{"ints":0,"bytes":0}},"keys":{"global":{"min_payment":{"keyType":"AVMString","valueType":"AVMUint64","key":"bWluX3BheW1lbnQ="},"total_purchases":{"keyType":"AVMString","valueType":"AVMUint64","key":"dG90YWxfcHVyY2hhc2Vz"},"total_algo_received":{"keyType":"AVMString","valueType":"AVMUint64","key":"dG90YWxfYWxnb19yZWNlaXZlZA=="}},"local":{},"box":{}},"maps":{"global":{},"local":{},"box":{}}},"bareActions":{"create":[],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[161],"errorMessage":"below minimum"},{"pc":[157,184],"errorMessage":"check self.min_payment exists"},{"pc":[174,194],"errorMessage":"check self.total_algo_received exists"},{"pc":[165,189],"errorMessage":"check self.total_purchases exists"},{"pc":[122],"errorMessage":"invalid number of bytes for arc4.uint64"},{"pc":[151],"errorMessage":"receiver must be app account"},{"pc":[144],"errorMessage":"transaction type is pay"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"events":[]} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


/**
 * The argument types for the SentinelContract contract
 */
export type SentinelContractArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'create_application(uint64)void': {
      minAmount: bigint | number
    }
    'purchase(pay)void': {
      pay: AppMethodCallTransactionArgument
    }
    'read_stats()(uint64,uint64,uint64)': Record<string, never>
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'create_application(uint64)void': [minAmount: bigint | number]
    'purchase(pay)void': [pay: AppMethodCallTransactionArgument]
    'read_stats()(uint64,uint64,uint64)': []
  }
}

/**
 * The return type for each method
 */
export type SentinelContractReturns = {
  'create_application(uint64)void': void
  'purchase(pay)void': void
  'read_stats()(uint64,uint64,uint64)': [bigint, bigint, bigint]
}

/**
 * Defines the types of available calls and state of the SentinelContract smart contract.
 */
export type SentinelContractTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'create_application(uint64)void' | 'create_application', {
      argsObj: SentinelContractArgs['obj']['create_application(uint64)void']
      argsTuple: SentinelContractArgs['tuple']['create_application(uint64)void']
      returns: SentinelContractReturns['create_application(uint64)void']
    }>
    & Record<'purchase(pay)void' | 'purchase', {
      argsObj: SentinelContractArgs['obj']['purchase(pay)void']
      argsTuple: SentinelContractArgs['tuple']['purchase(pay)void']
      returns: SentinelContractReturns['purchase(pay)void']
    }>
    & Record<'read_stats()(uint64,uint64,uint64)' | 'read_stats', {
      argsObj: SentinelContractArgs['obj']['read_stats()(uint64,uint64,uint64)']
      argsTuple: SentinelContractArgs['tuple']['read_stats()(uint64,uint64,uint64)']
      returns: SentinelContractReturns['read_stats()(uint64,uint64,uint64)']
    }>
  /**
   * Defines the shape of the state of the application.
   */
  state: {
    global: {
      keys: {
        minPayment: bigint
        totalPurchases: bigint
        totalAlgoReceived: bigint
      }
      maps: {}
    }
  }
}

/**
 * Defines the possible abi call signatures.
 */
export type SentinelContractSignatures = keyof SentinelContractTypes['methods']
/**
 * Defines the possible abi call signatures for methods that return a non-void value.
 */
export type SentinelContractNonVoidMethodSignatures = keyof SentinelContractTypes['methods'] extends infer T ? T extends keyof SentinelContractTypes['methods'] ? MethodReturn<T> extends void ? never : T  : never : never
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the SentinelContract smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends SentinelContractSignatures> = SentinelContractTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the SentinelContract smart contract to the method's return type
 */
export type MethodReturn<TSignature extends SentinelContractSignatures> = SentinelContractTypes['methods'][TSignature]['returns']

/**
 * Defines the shape of the keyed global state of the application.
 */
export type GlobalKeysState = SentinelContractTypes['state']['global']['keys']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the SentinelContract smart contract
 */
export abstract class SentinelContractParamsFactory {
  /**
   * Constructs a no op call for the purchase(pay)void ABI method
   *
   * Process a valid top-up: Payment txn must be in the atomic group (reference arg).
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static purchase(params: CallParams<SentinelContractArgs['obj']['purchase(pay)void'] | SentinelContractArgs['tuple']['purchase(pay)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'purchase(pay)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.pay],
    }
  }
  /**
   * Constructs a no op call for the read_stats()(uint64,uint64,uint64) ABI method
   *
   * Return (min_payment, total_purchases, total_algo_received_micro).
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static readStats(params: CallParams<SentinelContractArgs['obj']['read_stats()(uint64,uint64,uint64)'] | SentinelContractArgs['tuple']['read_stats()(uint64,uint64,uint64)']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'read_stats()(uint64,uint64,uint64)' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
}

/**
 * A client to make calls to the SentinelContract smart contract
 */
export class SentinelContractClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `SentinelContractClient`
   *
   * @param appClient An `AppClient` instance which has been created with the SentinelContract app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `SentinelContractClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Checks for decode errors on the given return value and maps the return value to the return type for the given method
   * @returns The typed return value or undefined if there was no value
   */
  decodeReturnValue<TSignature extends SentinelContractNonVoidMethodSignatures>(method: TSignature, returnValue: ABIReturn | undefined) {
    return returnValue !== undefined ? getArc56ReturnValue<MethodReturn<TSignature>>(returnValue, this.appClient.getABIMethod(method), APP_SPEC.structs) : undefined
  }

  /**
   * Returns a new `SentinelContractClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<SentinelContractClient> {
    return new SentinelContractClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `SentinelContractClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<SentinelContractClient> {
    return new SentinelContractClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Makes a clear_state call to an existing instance of the SentinelContract smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the SentinelContract smart contract using the `purchase(pay)void` ABI method.
     *
     * Process a valid top-up: Payment txn must be in the atomic group (reference arg).
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    purchase: (params: CallParams<SentinelContractArgs['obj']['purchase(pay)void'] | SentinelContractArgs['tuple']['purchase(pay)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(SentinelContractParamsFactory.purchase(params))
    },

    /**
     * Makes a call to the SentinelContract smart contract using the `read_stats()(uint64,uint64,uint64)` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * Return (min_payment, total_purchases, total_algo_received_micro).
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    readStats: (params: CallParams<SentinelContractArgs['obj']['read_stats()(uint64,uint64,uint64)'] | SentinelContractArgs['tuple']['read_stats()(uint64,uint64,uint64)']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(SentinelContractParamsFactory.readStats(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Makes a clear_state call to an existing instance of the SentinelContract smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the SentinelContract smart contract using the `purchase(pay)void` ABI method.
     *
     * Process a valid top-up: Payment txn must be in the atomic group (reference arg).
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    purchase: (params: CallParams<SentinelContractArgs['obj']['purchase(pay)void'] | SentinelContractArgs['tuple']['purchase(pay)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(SentinelContractParamsFactory.purchase(params))
    },

    /**
     * Makes a call to the SentinelContract smart contract using the `read_stats()(uint64,uint64,uint64)` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * Return (min_payment, total_purchases, total_algo_received_micro).
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    readStats: (params: CallParams<SentinelContractArgs['obj']['read_stats()(uint64,uint64,uint64)'] | SentinelContractArgs['tuple']['read_stats()(uint64,uint64,uint64)']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(SentinelContractParamsFactory.readStats(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Makes a clear_state call to an existing instance of the SentinelContract smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the SentinelContract smart contract using the `purchase(pay)void` ABI method.
     *
     * Process a valid top-up: Payment txn must be in the atomic group (reference arg).
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    purchase: async (params: CallParams<SentinelContractArgs['obj']['purchase(pay)void'] | SentinelContractArgs['tuple']['purchase(pay)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(SentinelContractParamsFactory.purchase(params))
      return {...result, return: result.return as unknown as (undefined | SentinelContractReturns['purchase(pay)void'])}
    },

    /**
     * Makes a call to the SentinelContract smart contract using the `read_stats()(uint64,uint64,uint64)` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * Return (min_payment, total_purchases, total_algo_received_micro).
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    readStats: async (params: CallParams<SentinelContractArgs['obj']['read_stats()(uint64,uint64,uint64)'] | SentinelContractArgs['tuple']['read_stats()(uint64,uint64,uint64)']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(SentinelContractParamsFactory.readStats(params))
      return {...result, return: result.return as unknown as (undefined | SentinelContractReturns['read_stats()(uint64,uint64,uint64)'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new SentinelContractClient(this.appClient.clone(params))
  }

  /**
   * Makes a readonly (simulated) call to the SentinelContract smart contract using the `read_stats()(uint64,uint64,uint64)` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * Return (min_payment, total_purchases, total_algo_received_micro).
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async readStats(params: CallParams<SentinelContractArgs['obj']['read_stats()(uint64,uint64,uint64)'] | SentinelContractArgs['tuple']['read_stats()(uint64,uint64,uint64)']> = {args: []}) {
    const result = await this.appClient.send.call(SentinelContractParamsFactory.readStats(params))
    return result.return as unknown as SentinelContractReturns['read_stats()(uint64,uint64,uint64)']
  }

  /**
   * Methods to access state for the current SentinelContract app
   */
  state = {
    /**
     * Methods to access global state for the current SentinelContract app
     */
    global: {
      /**
       * Get all current keyed values from global state
       */
      getAll: async (): Promise<Partial<Expand<GlobalKeysState>>> => {
        const result = await this.appClient.state.global.getAll()
        return {
          minPayment: result.min_payment,
          totalPurchases: result.total_purchases,
          totalAlgoReceived: result.total_algo_received,
        }
      },
      /**
       * Get the current value of the min_payment key in global state
       */
      minPayment: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("min_payment")) as bigint | undefined },
      /**
       * Get the current value of the total_purchases key in global state
       */
      totalPurchases: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("total_purchases")) as bigint | undefined },
      /**
       * Get the current value of the total_algo_received key in global state
       */
      totalAlgoReceived: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("total_algo_received")) as bigint | undefined },
    },
  }

  public newGroup(): SentinelContractComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a purchase(pay)void method call against the SentinelContract contract
       */
      purchase(params: CallParams<SentinelContractArgs['obj']['purchase(pay)void'] | SentinelContractArgs['tuple']['purchase(pay)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.purchase(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a read_stats()(uint64,uint64,uint64) method call against the SentinelContract contract
       */
      readStats(params: CallParams<SentinelContractArgs['obj']['read_stats()(uint64,uint64,uint64)'] | SentinelContractArgs['tuple']['read_stats()(uint64,uint64,uint64)']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.readStats(params)))
        resultMappers.push((v) => client.decodeReturnValue('read_stats()(uint64,uint64,uint64)', v))
        return this
      },
      /**
       * Add a clear state call to the SentinelContract contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as SentinelContractComposer
  }
}
export type SentinelContractComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the purchase(pay)void ABI method.
   *
   * Process a valid top-up: Payment txn must be in the atomic group (reference arg).
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  purchase(params?: CallParams<SentinelContractArgs['obj']['purchase(pay)void'] | SentinelContractArgs['tuple']['purchase(pay)void']>): SentinelContractComposer<[...TReturns, SentinelContractReturns['purchase(pay)void'] | undefined]>

  /**
   * Calls the read_stats()(uint64,uint64,uint64) ABI method.
   *
   * Return (min_payment, total_purchases, total_algo_received_micro).
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  readStats(params?: CallParams<SentinelContractArgs['obj']['read_stats()(uint64,uint64,uint64)'] | SentinelContractArgs['tuple']['read_stats()(uint64,uint64,uint64)']>): SentinelContractComposer<[...TReturns, SentinelContractReturns['read_stats()(uint64,uint64,uint64)'] | undefined]>

  /**
   * Makes a clear_state call to an existing instance of the SentinelContract smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): SentinelContractComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): SentinelContractComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<SentinelContractComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<SentinelContractComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<SentinelContractComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<SentinelContractComposerResults<TReturns>>
}
export type SentinelContractComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

