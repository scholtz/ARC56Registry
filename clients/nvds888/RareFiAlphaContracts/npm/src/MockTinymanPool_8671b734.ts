/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"MockTinymanPool","structs":{},"methods":[],"arcs":[22,28],"networks":{},"state":{"schema":{"global":{"ints":3,"bytes":1},"local":{"ints":5,"bytes":0}},"keys":{"global":{"asset1Id":{"keyType":"AVMString","valueType":"AVMUint64","key":"YXNzZXQxSWQ="},"asset2Id":{"keyType":"AVMString","valueType":"AVMUint64","key":"YXNzZXQySWQ="},"initialized":{"keyType":"AVMString","valueType":"AVMUint64","key":"aW5pdGlhbGl6ZWQ="},"stateHolder":{"keyType":"AVMString","valueType":"address","key":"c3RhdGVIb2xkZXI="}},"local":{"asset_1_id":{"keyType":"AVMString","valueType":"AVMUint64","key":"YXNzZXRfMV9pZA=="},"asset_2_id":{"keyType":"AVMString","valueType":"AVMUint64","key":"YXNzZXRfMl9pZA=="},"asset_1_reserves":{"keyType":"AVMString","valueType":"AVMUint64","key":"YXNzZXRfMV9yZXNlcnZlcw=="},"asset_2_reserves":{"keyType":"AVMString","valueType":"AVMUint64","key":"YXNzZXRfMl9yZXNlcnZlcw=="},"total_fee_share":{"keyType":"AVMString","valueType":"AVMUint64","key":"dG90YWxfZmVlX3NoYXJl"}},"box":{}},"maps":{"global":{},"local":{},"box":{}}},"bareActions":{"create":["NoOp"],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[444],"errorMessage":"Must follow asset transfer"},{"pc":[481],"errorMessage":"Must send to pool or state holder"},{"pc":[406],"errorMessage":"Need 3 args for swap"},{"pc":[430],"errorMessage":"Only fixed-input supported"},{"pc":[546,569],"errorMessage":"Overflow"},{"pc":[247],"errorMessage":"Pool already initialized"},{"pc":[573],"errorMessage":"Slippage exceeded"},{"pc":[660],"errorMessage":"Unknown asset"},{"pc":[245,256,277,352,372,440,508,521,577,656,667,706,746],"errorMessage":"check GlobalState exists"},{"pc":[492,500,504,751,765],"errorMessage":"check LocalState exists"},{"pc":[452],"errorMessage":"transaction type is axfer"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"events":[]} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


/**
 * The argument types for the MockTinymanPool contract
 */
export type MockTinymanPoolArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
  }
}

/**
 * The return type for each method
 */
export type MockTinymanPoolReturns = {
}

/**
 * Defines the types of available calls and state of the MockTinymanPool smart contract.
 */
export type MockTinymanPoolTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods: {}
  /**
   * Defines the shape of the state of the application.
   */
  state: {
    global: {
      keys: {
        asset1Id: bigint
        asset2Id: bigint
        initialized: bigint
        stateHolder: string
      }
      maps: {}
    }
    local: {
      keys: {
        asset_1Id: bigint
        asset_2Id: bigint
        asset_1Reserves: bigint
        asset_2Reserves: bigint
        totalFeeShare: bigint
      }
      maps: {}
    }
  }
}

/**
 * Defines the possible abi call signatures.
 */
export type MockTinymanPoolSignatures = keyof MockTinymanPoolTypes['methods']
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the MockTinymanPool smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends MockTinymanPoolSignatures> = MockTinymanPoolTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the MockTinymanPool smart contract to the method's return type
 */
export type MethodReturn<TSignature extends MockTinymanPoolSignatures> = MockTinymanPoolTypes['methods'][TSignature]['returns']

/**
 * Defines the shape of the keyed global state of the application.
 */
export type GlobalKeysState = MockTinymanPoolTypes['state']['global']['keys']

/**
 * Defines the shape of the keyed local state of the application.
 */
export type LocalKeysState = MockTinymanPoolTypes['state']['local']['keys']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the MockTinymanPool smart contract
 */
export abstract class MockTinymanPoolParamsFactory {
}

/**
 * A client to make calls to the MockTinymanPool smart contract
 */
export class MockTinymanPoolClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `MockTinymanPoolClient`
   *
   * @param appClient An `AppClient` instance which has been created with the MockTinymanPool app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `MockTinymanPoolClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Returns a new `MockTinymanPoolClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<MockTinymanPoolClient> {
    return new MockTinymanPoolClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `MockTinymanPoolClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<MockTinymanPoolClient> {
    return new MockTinymanPoolClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Makes a clear_state call to an existing instance of the MockTinymanPool smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Makes a clear_state call to an existing instance of the MockTinymanPool smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Makes a clear_state call to an existing instance of the MockTinymanPool smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new MockTinymanPoolClient(this.appClient.clone(params))
  }

  /**
   * Methods to access state for the current MockTinymanPool app
   */
  state = {
    /**
     * Methods to access global state for the current MockTinymanPool app
     */
    global: {
      /**
       * Get all current keyed values from global state
       */
      getAll: async (): Promise<Partial<Expand<GlobalKeysState>>> => {
        const result = await this.appClient.state.global.getAll()
        return {
          asset1Id: result.asset1Id,
          asset2Id: result.asset2Id,
          initialized: result.initialized,
          stateHolder: result.stateHolder,
        }
      },
      /**
       * Get the current value of the asset1Id key in global state
       */
      asset1Id: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("asset1Id")) as bigint | undefined },
      /**
       * Get the current value of the asset2Id key in global state
       */
      asset2Id: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("asset2Id")) as bigint | undefined },
      /**
       * Get the current value of the initialized key in global state
       */
      initialized: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("initialized")) as bigint | undefined },
      /**
       * Get the current value of the stateHolder key in global state
       */
      stateHolder: async (): Promise<string | undefined> => { return (await this.appClient.state.global.getValue("stateHolder")) as string | undefined },
    },
    /**
     * Methods to access local state for the current MockTinymanPool app
     */
    local: (address: string | Address) => {
      const encodedAddress = typeof address === 'string' ? address : encodeAddress(address.publicKey)
      return {
        /**
         * Get all current keyed values from local state
         */
        getAll: async (): Promise<Partial<Expand<LocalKeysState>>> => {
          const result = await this.appClient.state.local(encodedAddress).getAll()
          return {
            asset_1Id: result.asset_1_id,
            asset_2Id: result.asset_2_id,
            asset_1Reserves: result.asset_1_reserves,
            asset_2Reserves: result.asset_2_reserves,
            totalFeeShare: result.total_fee_share,
          }
        },
        /**
         * Get the current value of the asset_1_id key in local state
         */
        asset_1Id: async (): Promise<bigint | undefined> => { return (await this.appClient.state.local(encodedAddress).getValue("asset_1_id")) as bigint | undefined },
        /**
         * Get the current value of the asset_2_id key in local state
         */
        asset_2Id: async (): Promise<bigint | undefined> => { return (await this.appClient.state.local(encodedAddress).getValue("asset_2_id")) as bigint | undefined },
        /**
         * Get the current value of the asset_1_reserves key in local state
         */
        asset_1Reserves: async (): Promise<bigint | undefined> => { return (await this.appClient.state.local(encodedAddress).getValue("asset_1_reserves")) as bigint | undefined },
        /**
         * Get the current value of the asset_2_reserves key in local state
         */
        asset_2Reserves: async (): Promise<bigint | undefined> => { return (await this.appClient.state.local(encodedAddress).getValue("asset_2_reserves")) as bigint | undefined },
        /**
         * Get the current value of the total_fee_share key in local state
         */
        totalFeeShare: async (): Promise<bigint | undefined> => { return (await this.appClient.state.local(encodedAddress).getValue("total_fee_share")) as bigint | undefined },
      }
    },
  }

  public newGroup(): MockTinymanPoolComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a clear state call to the MockTinymanPool contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as MockTinymanPoolComposer
  }
}
export type MockTinymanPoolComposer<TReturns extends [...any[]] = []> = {
  /**
   * Makes a clear_state call to an existing instance of the MockTinymanPool smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): MockTinymanPoolComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): MockTinymanPoolComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<MockTinymanPoolComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<MockTinymanPoolComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<MockTinymanPoolComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<MockTinymanPoolComposerResults<TReturns>>
}
export type MockTinymanPoolComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

