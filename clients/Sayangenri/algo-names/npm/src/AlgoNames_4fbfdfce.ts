/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"AlgoNames","structs":{},"methods":[{"name":"register","args":[{"type":"string","name":"name"},{"type":"address","name":"owner"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}},{"name":"resolve","args":[{"type":"string","name":"name"}],"returns":{"type":"address"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"sendToName","args":[{"type":"string","name":"name"},{"type":"uint64","name":"amount"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}}],"arcs":[22,28],"networks":{},"state":{"schema":{"global":{"ints":0,"bytes":0},"local":{"ints":0,"bytes":0}},"keys":{"global":{},"local":{},"box":{}},"maps":{"global":{},"local":{},"box":{"names":{"keyType":"AVMString","valueType":"address","prefix":"bmFtZTo="}}}},"bareActions":{"create":["NoOp"],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[193],"errorMessage":"Amount must be greater than 0"},{"pc":[105],"errorMessage":"Name already registered"},{"pc":[95,129,181],"errorMessage":"Name cannot be empty"},{"pc":[137,190],"errorMessage":"Name not found"},{"pc":[23],"errorMessage":"OnCompletion must be NoOp"},{"pc":[63],"errorMessage":"OnCompletion must be NoOp && can only call when creating"},{"pc":[71,115,156],"errorMessage":"invalid array length header"},{"pc":[78,122,163],"errorMessage":"invalid number of bytes for arc4.dynamic_array<arc4.uint8>"},{"pc":[90],"errorMessage":"invalid number of bytes for arc4.static_array<arc4.uint8, 32>"},{"pc":[175],"errorMessage":"invalid number of bytes for arc4.uint64"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"events":[]} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


/**
 * The argument types for the AlgoNames contract
 */
export type AlgoNamesArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'register(string,address)void': {
      name: string
      owner: string
    }
    'resolve(string)address': {
      name: string
    }
    'sendToName(string,uint64)void': {
      name: string
      amount: bigint | number
    }
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'register(string,address)void': [name: string, owner: string]
    'resolve(string)address': [name: string]
    'sendToName(string,uint64)void': [name: string, amount: bigint | number]
  }
}

/**
 * The return type for each method
 */
export type AlgoNamesReturns = {
  'register(string,address)void': void
  'resolve(string)address': string
  'sendToName(string,uint64)void': void
}

/**
 * Defines the types of available calls and state of the AlgoNames smart contract.
 */
export type AlgoNamesTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'register(string,address)void' | 'register', {
      argsObj: AlgoNamesArgs['obj']['register(string,address)void']
      argsTuple: AlgoNamesArgs['tuple']['register(string,address)void']
      returns: AlgoNamesReturns['register(string,address)void']
    }>
    & Record<'resolve(string)address' | 'resolve', {
      argsObj: AlgoNamesArgs['obj']['resolve(string)address']
      argsTuple: AlgoNamesArgs['tuple']['resolve(string)address']
      returns: AlgoNamesReturns['resolve(string)address']
    }>
    & Record<'sendToName(string,uint64)void' | 'sendToName', {
      argsObj: AlgoNamesArgs['obj']['sendToName(string,uint64)void']
      argsTuple: AlgoNamesArgs['tuple']['sendToName(string,uint64)void']
      returns: AlgoNamesReturns['sendToName(string,uint64)void']
    }>
  /**
   * Defines the shape of the state of the application.
   */
  state: {
    box: {
      keys: {}
      maps: {
        names: Map<string, string>
      }
    }
  }
}

/**
 * Defines the possible abi call signatures.
 */
export type AlgoNamesSignatures = keyof AlgoNamesTypes['methods']
/**
 * Defines the possible abi call signatures for methods that return a non-void value.
 */
export type AlgoNamesNonVoidMethodSignatures = keyof AlgoNamesTypes['methods'] extends infer T ? T extends keyof AlgoNamesTypes['methods'] ? MethodReturn<T> extends void ? never : T  : never : never
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the AlgoNames smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends AlgoNamesSignatures> = AlgoNamesTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the AlgoNames smart contract to the method's return type
 */
export type MethodReturn<TSignature extends AlgoNamesSignatures> = AlgoNamesTypes['methods'][TSignature]['returns']

/**
 * Defines the shape of the keyed box state of the application.
 */
export type BoxKeysState = AlgoNamesTypes['state']['box']['keys']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the AlgoNames smart contract
 */
export abstract class AlgoNamesParamsFactory {
  /**
   * Constructs a no op call for the register(string,address)void ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static register(params: CallParams<AlgoNamesArgs['obj']['register(string,address)void'] | AlgoNamesArgs['tuple']['register(string,address)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'register(string,address)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.name, params.args.owner],
    }
  }
  /**
   * Constructs a no op call for the resolve(string)address ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static resolve(params: CallParams<AlgoNamesArgs['obj']['resolve(string)address'] | AlgoNamesArgs['tuple']['resolve(string)address']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'resolve(string)address' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.name],
    }
  }
  /**
   * Constructs a no op call for the sendToName(string,uint64)void ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static sendToName(params: CallParams<AlgoNamesArgs['obj']['sendToName(string,uint64)void'] | AlgoNamesArgs['tuple']['sendToName(string,uint64)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'sendToName(string,uint64)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.name, params.args.amount],
    }
  }
}

/**
 * A client to make calls to the AlgoNames smart contract
 */
export class AlgoNamesClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `AlgoNamesClient`
   *
   * @param appClient An `AppClient` instance which has been created with the AlgoNames app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `AlgoNamesClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Checks for decode errors on the given return value and maps the return value to the return type for the given method
   * @returns The typed return value or undefined if there was no value
   */
  decodeReturnValue<TSignature extends AlgoNamesNonVoidMethodSignatures>(method: TSignature, returnValue: ABIReturn | undefined) {
    return returnValue !== undefined ? getArc56ReturnValue<MethodReturn<TSignature>>(returnValue, this.appClient.getABIMethod(method), APP_SPEC.structs) : undefined
  }

  /**
   * Returns a new `AlgoNamesClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<AlgoNamesClient> {
    return new AlgoNamesClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `AlgoNamesClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<AlgoNamesClient> {
    return new AlgoNamesClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Makes a clear_state call to an existing instance of the AlgoNames smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the AlgoNames smart contract using the `register(string,address)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    register: (params: CallParams<AlgoNamesArgs['obj']['register(string,address)void'] | AlgoNamesArgs['tuple']['register(string,address)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(AlgoNamesParamsFactory.register(params))
    },

    /**
     * Makes a call to the AlgoNames smart contract using the `resolve(string)address` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    resolve: (params: CallParams<AlgoNamesArgs['obj']['resolve(string)address'] | AlgoNamesArgs['tuple']['resolve(string)address']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(AlgoNamesParamsFactory.resolve(params))
    },

    /**
     * Makes a call to the AlgoNames smart contract using the `sendToName(string,uint64)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    sendToName: (params: CallParams<AlgoNamesArgs['obj']['sendToName(string,uint64)void'] | AlgoNamesArgs['tuple']['sendToName(string,uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(AlgoNamesParamsFactory.sendToName(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Makes a clear_state call to an existing instance of the AlgoNames smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the AlgoNames smart contract using the `register(string,address)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    register: (params: CallParams<AlgoNamesArgs['obj']['register(string,address)void'] | AlgoNamesArgs['tuple']['register(string,address)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(AlgoNamesParamsFactory.register(params))
    },

    /**
     * Makes a call to the AlgoNames smart contract using the `resolve(string)address` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    resolve: (params: CallParams<AlgoNamesArgs['obj']['resolve(string)address'] | AlgoNamesArgs['tuple']['resolve(string)address']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(AlgoNamesParamsFactory.resolve(params))
    },

    /**
     * Makes a call to the AlgoNames smart contract using the `sendToName(string,uint64)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    sendToName: (params: CallParams<AlgoNamesArgs['obj']['sendToName(string,uint64)void'] | AlgoNamesArgs['tuple']['sendToName(string,uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(AlgoNamesParamsFactory.sendToName(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Makes a clear_state call to an existing instance of the AlgoNames smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the AlgoNames smart contract using the `register(string,address)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    register: async (params: CallParams<AlgoNamesArgs['obj']['register(string,address)void'] | AlgoNamesArgs['tuple']['register(string,address)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(AlgoNamesParamsFactory.register(params))
      return {...result, return: result.return as unknown as (undefined | AlgoNamesReturns['register(string,address)void'])}
    },

    /**
     * Makes a call to the AlgoNames smart contract using the `resolve(string)address` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    resolve: async (params: CallParams<AlgoNamesArgs['obj']['resolve(string)address'] | AlgoNamesArgs['tuple']['resolve(string)address']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(AlgoNamesParamsFactory.resolve(params))
      return {...result, return: result.return as unknown as (undefined | AlgoNamesReturns['resolve(string)address'])}
    },

    /**
     * Makes a call to the AlgoNames smart contract using the `sendToName(string,uint64)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    sendToName: async (params: CallParams<AlgoNamesArgs['obj']['sendToName(string,uint64)void'] | AlgoNamesArgs['tuple']['sendToName(string,uint64)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(AlgoNamesParamsFactory.sendToName(params))
      return {...result, return: result.return as unknown as (undefined | AlgoNamesReturns['sendToName(string,uint64)void'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new AlgoNamesClient(this.appClient.clone(params))
  }

  /**
   * Makes a readonly (simulated) call to the AlgoNames smart contract using the `resolve(string)address` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async resolve(params: CallParams<AlgoNamesArgs['obj']['resolve(string)address'] | AlgoNamesArgs['tuple']['resolve(string)address']>) {
    const result = await this.appClient.send.call(AlgoNamesParamsFactory.resolve(params))
    return result.return as unknown as AlgoNamesReturns['resolve(string)address']
  }

  /**
   * Methods to access state for the current AlgoNames app
   */
  state = {
    /**
     * Methods to access box state for the current AlgoNames app
     */
    box: {
      /**
       * Get all current keyed values from box state
       */
      getAll: async (): Promise<Partial<Expand<BoxKeysState>>> => {
        const result = await this.appClient.state.box.getAll()
        return {
        }
      },
      /**
       * Get values from the names map in box state
       */
      names: {
        /**
         * Get all current values of the names map in box state
         */
        getMap: async (): Promise<Map<string, string>> => { return (await this.appClient.state.box.getMap("names")) as Map<string, string> },
        /**
         * Get a current value of the names map by key from box state
         */
        value: async (key: string): Promise<string | undefined> => { return await this.appClient.state.box.getMapValue("names", key) as string | undefined },
      },
    },
  }

  public newGroup(): AlgoNamesComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a register(string,address)void method call against the AlgoNames contract
       */
      register(params: CallParams<AlgoNamesArgs['obj']['register(string,address)void'] | AlgoNamesArgs['tuple']['register(string,address)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.register(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a resolve(string)address method call against the AlgoNames contract
       */
      resolve(params: CallParams<AlgoNamesArgs['obj']['resolve(string)address'] | AlgoNamesArgs['tuple']['resolve(string)address']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.resolve(params)))
        resultMappers.push((v) => client.decodeReturnValue('resolve(string)address', v))
        return this
      },
      /**
       * Add a sendToName(string,uint64)void method call against the AlgoNames contract
       */
      sendToName(params: CallParams<AlgoNamesArgs['obj']['sendToName(string,uint64)void'] | AlgoNamesArgs['tuple']['sendToName(string,uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.sendToName(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a clear state call to the AlgoNames contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as AlgoNamesComposer
  }
}
export type AlgoNamesComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the register(string,address)void ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  register(params?: CallParams<AlgoNamesArgs['obj']['register(string,address)void'] | AlgoNamesArgs['tuple']['register(string,address)void']>): AlgoNamesComposer<[...TReturns, AlgoNamesReturns['register(string,address)void'] | undefined]>

  /**
   * Calls the resolve(string)address ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  resolve(params?: CallParams<AlgoNamesArgs['obj']['resolve(string)address'] | AlgoNamesArgs['tuple']['resolve(string)address']>): AlgoNamesComposer<[...TReturns, AlgoNamesReturns['resolve(string)address'] | undefined]>

  /**
   * Calls the sendToName(string,uint64)void ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  sendToName(params?: CallParams<AlgoNamesArgs['obj']['sendToName(string,uint64)void'] | AlgoNamesArgs['tuple']['sendToName(string,uint64)void']>): AlgoNamesComposer<[...TReturns, AlgoNamesReturns['sendToName(string,uint64)void'] | undefined]>

  /**
   * Makes a clear_state call to an existing instance of the AlgoNames smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): AlgoNamesComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): AlgoNamesComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<AlgoNamesComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<AlgoNamesComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<AlgoNamesComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<AlgoNamesComposerResults<TReturns>>
}
export type AlgoNamesComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

