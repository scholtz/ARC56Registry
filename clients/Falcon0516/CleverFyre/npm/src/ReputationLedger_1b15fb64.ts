/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"ReputationLedger","structs":{"ReputationRecord":[{"name":"score","type":"uint64"},{"name":"driftEvents","type":"uint64"}],"VouchRecord":[{"name":"stakeAmount","type":"uint64"},{"name":"roundCreated","type":"uint64"}]},"methods":[{"name":"create","args":[{"type":"uint64","name":"sentinel_escrow_app_id"},{"type":"uint64","name":"policy_vault_app_id"}],"returns":{"type":"void"},"actions":{"create":["NoOp"],"call":[]},"readonly":false,"desc":"Record the two trusted caller app IDs on deployment.","events":[],"recommendations":{}},{"name":"register_agent","args":[{"type":"address","name":"agent"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Create a reputation record with neutral score = 500.\nCallable by anyone (idempotent guard inside).","events":[],"recommendations":{}},{"name":"update_score","args":[{"type":"address","name":"agent"},{"type":"uint64","name":"delta"},{"type":"uint64","name":"is_negative"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Adjust agent's reputation score.\ndelta       : amount to add or subtract. is_negative : 1 = subtract delta, 0 = add delta. Result is clamped to [0, 1000].\nONLY callable by SentinelEscrow or PolicyVault app (via inner txn). On-chain caller verification uses Txn.sender being the trusted app address.","events":[],"recommendations":{}},{"name":"record_drift","args":[{"type":"address","name":"agent"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Increment drift_events counter for a flagged agent.","events":[],"recommendations":{}},{"name":"vouch","args":[{"type":"address","name":"voucher"},{"type":"address","name":"vouchee"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Stake 1 ALGO vouching for a peer agent's future behaviour.\nSlash conditions (enforced off-chain by oracle, then calls slash_voucher):   - vouchee score drops below 400 → slash 50% of stake. Reward condition (oracle calls reward_voucher):   - vouchee score rises above 800 → return stake + 10% bonus.","events":[],"recommendations":{}},{"name":"slash_voucher","args":[{"type":"address","name":"voucher"},{"type":"address","name":"vouchee"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Slash 50% of the voucher's stake because vouchee score dropped < 400.\nCalled by oracle. Remaining 50% is refunded to voucher.","events":[],"recommendations":{}},{"name":"reward_voucher","args":[{"type":"address","name":"voucher"},{"type":"address","name":"vouchee"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Return stake + 10% bonus when vouchee score exceeds 800.\nCalled by the oracle.","events":[],"recommendations":{}},{"name":"get_score","args":[{"type":"address","name":"agent"}],"returns":{"type":"uint64"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Return the current reputation score for an agent.","events":[],"recommendations":{}},{"name":"get_tier","args":[{"type":"address","name":"agent"}],"returns":{"type":"uint64"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Returns tier: 0=BLACKLISTED, 1=RESTRICTED, 2=CAUTION, 3=GOOD, 4=EXCELLENT.","events":[],"recommendations":{}},{"name":"get_drift_events","args":[{"type":"address","name":"agent"}],"returns":{"type":"uint64"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Return total flagged drift events for forensic audit.","events":[],"recommendations":{}}],"arcs":[22,28],"desc":"\n    Social trust layer — score 0-1000 per agent.\n\n    Tier mapping:\n      800+     EXCELLENT  — unlimited (within policy), auto-release\n      600-799  GOOD       — up to 5 ALGO/call\n      400-599  CAUTION    — up to 0.5 ALGO/call, intent logging mandatory\n      200-399  RESTRICTED — quarantine, human review required\n      0-199    BLACKLISTED — SentinelEscrow rejects all attempts\n    ","networks":{},"state":{"schema":{"global":{"ints":2,"bytes":0},"local":{"ints":0,"bytes":0}},"keys":{"global":{"sentinel_app_id":{"keyType":"AVMString","valueType":"AVMUint64","key":"c2VudGluZWxfYXBwX2lk"},"policy_vault_app_id":{"keyType":"AVMString","valueType":"AVMUint64","key":"cG9saWN5X3ZhdWx0X2FwcF9pZA=="}},"local":{},"box":{}},"maps":{"global":{},"local":{},"box":{"rep_records":{"keyType":"address","valueType":"ReputationRecord","prefix":"cmVwOg=="},"vouch_stakes":{"keyType":"AVMBytes","valueType":"VouchRecord","prefix":"dmM6"}}}},"bareActions":{"create":[],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[212],"errorMessage":"agent already registered"},{"pc":[274,360,602,629,708],"errorMessage":"agent not registered"},{"pc":[404],"errorMessage":"already vouching for this agent"},{"pc":[203,243,352,385,393,429,437,511,519,594,621,700],"errorMessage":"invalid number of bytes for arc4.static_array<arc4.uint8, 32>"},{"pc":[175,184,251,261],"errorMessage":"invalid number of bytes for arc4.uint64"},{"pc":[410],"errorMessage":"must attach 1 ALGO to vouch"},{"pc":[525],"errorMessage":"only oracle/admin can reward"},{"pc":[443],"errorMessage":"only oracle/admin can slash"},{"pc":[456,538],"errorMessage":"vouch record not found"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"events":[]} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


// Type definitions for ARC-56 structs

export type ReputationRecord = {
  score: bigint,
  driftEvents: bigint
}


/**
 * Converts the ABI tuple representation of a ReputationRecord to the struct representation
 */
export function ReputationRecordFromTuple(abiTuple: [bigint, bigint]) {
  return getABIStructFromABITuple(abiTuple, APP_SPEC.structs.ReputationRecord, APP_SPEC.structs) as ReputationRecord
}

export type VouchRecord = {
  stakeAmount: bigint,
  roundCreated: bigint
}


/**
 * Converts the ABI tuple representation of a VouchRecord to the struct representation
 */
export function VouchRecordFromTuple(abiTuple: [bigint, bigint]) {
  return getABIStructFromABITuple(abiTuple, APP_SPEC.structs.VouchRecord, APP_SPEC.structs) as VouchRecord
}

/**
 * The argument types for the ReputationLedger contract
 */
export type ReputationLedgerArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'create(uint64,uint64)void': {
      sentinelEscrowAppId: bigint | number
      policyVaultAppId: bigint | number
    }
    'register_agent(address)void': {
      agent: string
    }
    'update_score(address,uint64,uint64)void': {
      agent: string
      delta: bigint | number
      isNegative: bigint | number
    }
    'record_drift(address)void': {
      agent: string
    }
    'vouch(address,address)void': {
      voucher: string
      vouchee: string
    }
    'slash_voucher(address,address)void': {
      voucher: string
      vouchee: string
    }
    'reward_voucher(address,address)void': {
      voucher: string
      vouchee: string
    }
    'get_score(address)uint64': {
      agent: string
    }
    'get_tier(address)uint64': {
      agent: string
    }
    'get_drift_events(address)uint64': {
      agent: string
    }
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'create(uint64,uint64)void': [sentinelEscrowAppId: bigint | number, policyVaultAppId: bigint | number]
    'register_agent(address)void': [agent: string]
    'update_score(address,uint64,uint64)void': [agent: string, delta: bigint | number, isNegative: bigint | number]
    'record_drift(address)void': [agent: string]
    'vouch(address,address)void': [voucher: string, vouchee: string]
    'slash_voucher(address,address)void': [voucher: string, vouchee: string]
    'reward_voucher(address,address)void': [voucher: string, vouchee: string]
    'get_score(address)uint64': [agent: string]
    'get_tier(address)uint64': [agent: string]
    'get_drift_events(address)uint64': [agent: string]
  }
}

/**
 * The return type for each method
 */
export type ReputationLedgerReturns = {
  'create(uint64,uint64)void': void
  'register_agent(address)void': void
  'update_score(address,uint64,uint64)void': void
  'record_drift(address)void': void
  'vouch(address,address)void': void
  'slash_voucher(address,address)void': void
  'reward_voucher(address,address)void': void
  'get_score(address)uint64': bigint
  'get_tier(address)uint64': bigint
  'get_drift_events(address)uint64': bigint
}

/**
 * Defines the types of available calls and state of the ReputationLedger smart contract.
 */
export type ReputationLedgerTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'create(uint64,uint64)void' | 'create', {
      argsObj: ReputationLedgerArgs['obj']['create(uint64,uint64)void']
      argsTuple: ReputationLedgerArgs['tuple']['create(uint64,uint64)void']
      returns: ReputationLedgerReturns['create(uint64,uint64)void']
    }>
    & Record<'register_agent(address)void' | 'register_agent', {
      argsObj: ReputationLedgerArgs['obj']['register_agent(address)void']
      argsTuple: ReputationLedgerArgs['tuple']['register_agent(address)void']
      returns: ReputationLedgerReturns['register_agent(address)void']
    }>
    & Record<'update_score(address,uint64,uint64)void' | 'update_score', {
      argsObj: ReputationLedgerArgs['obj']['update_score(address,uint64,uint64)void']
      argsTuple: ReputationLedgerArgs['tuple']['update_score(address,uint64,uint64)void']
      returns: ReputationLedgerReturns['update_score(address,uint64,uint64)void']
    }>
    & Record<'record_drift(address)void' | 'record_drift', {
      argsObj: ReputationLedgerArgs['obj']['record_drift(address)void']
      argsTuple: ReputationLedgerArgs['tuple']['record_drift(address)void']
      returns: ReputationLedgerReturns['record_drift(address)void']
    }>
    & Record<'vouch(address,address)void' | 'vouch', {
      argsObj: ReputationLedgerArgs['obj']['vouch(address,address)void']
      argsTuple: ReputationLedgerArgs['tuple']['vouch(address,address)void']
      returns: ReputationLedgerReturns['vouch(address,address)void']
    }>
    & Record<'slash_voucher(address,address)void' | 'slash_voucher', {
      argsObj: ReputationLedgerArgs['obj']['slash_voucher(address,address)void']
      argsTuple: ReputationLedgerArgs['tuple']['slash_voucher(address,address)void']
      returns: ReputationLedgerReturns['slash_voucher(address,address)void']
    }>
    & Record<'reward_voucher(address,address)void' | 'reward_voucher', {
      argsObj: ReputationLedgerArgs['obj']['reward_voucher(address,address)void']
      argsTuple: ReputationLedgerArgs['tuple']['reward_voucher(address,address)void']
      returns: ReputationLedgerReturns['reward_voucher(address,address)void']
    }>
    & Record<'get_score(address)uint64' | 'get_score', {
      argsObj: ReputationLedgerArgs['obj']['get_score(address)uint64']
      argsTuple: ReputationLedgerArgs['tuple']['get_score(address)uint64']
      returns: ReputationLedgerReturns['get_score(address)uint64']
    }>
    & Record<'get_tier(address)uint64' | 'get_tier', {
      argsObj: ReputationLedgerArgs['obj']['get_tier(address)uint64']
      argsTuple: ReputationLedgerArgs['tuple']['get_tier(address)uint64']
      returns: ReputationLedgerReturns['get_tier(address)uint64']
    }>
    & Record<'get_drift_events(address)uint64' | 'get_drift_events', {
      argsObj: ReputationLedgerArgs['obj']['get_drift_events(address)uint64']
      argsTuple: ReputationLedgerArgs['tuple']['get_drift_events(address)uint64']
      returns: ReputationLedgerReturns['get_drift_events(address)uint64']
    }>
  /**
   * Defines the shape of the state of the application.
   */
  state: {
    global: {
      keys: {
        sentinelAppId: bigint
        policyVaultAppId: bigint
      }
      maps: {}
    }
    box: {
      keys: {}
      maps: {
        repRecords: Map<string, ReputationRecord>
        vouchStakes: Map<Uint8Array | string, VouchRecord>
      }
    }
  }
}

/**
 * Defines the possible abi call signatures.
 */
export type ReputationLedgerSignatures = keyof ReputationLedgerTypes['methods']
/**
 * Defines the possible abi call signatures for methods that return a non-void value.
 */
export type ReputationLedgerNonVoidMethodSignatures = keyof ReputationLedgerTypes['methods'] extends infer T ? T extends keyof ReputationLedgerTypes['methods'] ? MethodReturn<T> extends void ? never : T  : never : never
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the ReputationLedger smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends ReputationLedgerSignatures> = ReputationLedgerTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the ReputationLedger smart contract to the method's return type
 */
export type MethodReturn<TSignature extends ReputationLedgerSignatures> = ReputationLedgerTypes['methods'][TSignature]['returns']

/**
 * Defines the shape of the keyed global state of the application.
 */
export type GlobalKeysState = ReputationLedgerTypes['state']['global']['keys']

/**
 * Defines the shape of the keyed box state of the application.
 */
export type BoxKeysState = ReputationLedgerTypes['state']['box']['keys']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the ReputationLedger smart contract
 */
export abstract class ReputationLedgerParamsFactory {
  /**
   * Constructs a no op call for the register_agent(address)void ABI method
   *
  * Create a reputation record with neutral score = 500.
  Callable by anyone (idempotent guard inside).

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static registerAgent(params: CallParams<ReputationLedgerArgs['obj']['register_agent(address)void'] | ReputationLedgerArgs['tuple']['register_agent(address)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'register_agent(address)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.agent],
    }
  }
  /**
   * Constructs a no op call for the update_score(address,uint64,uint64)void ABI method
   *
  * Adjust agent's reputation score.
  delta       : amount to add or subtract. is_negative : 1 = subtract delta, 0 = add delta. Result is clamped to [0, 1000].
  ONLY callable by SentinelEscrow or PolicyVault app (via inner txn). On-chain caller verification uses Txn.sender being the trusted app address.

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static updateScore(params: CallParams<ReputationLedgerArgs['obj']['update_score(address,uint64,uint64)void'] | ReputationLedgerArgs['tuple']['update_score(address,uint64,uint64)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'update_score(address,uint64,uint64)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.agent, params.args.delta, params.args.isNegative],
    }
  }
  /**
   * Constructs a no op call for the record_drift(address)void ABI method
   *
   * Increment drift_events counter for a flagged agent.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static recordDrift(params: CallParams<ReputationLedgerArgs['obj']['record_drift(address)void'] | ReputationLedgerArgs['tuple']['record_drift(address)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'record_drift(address)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.agent],
    }
  }
  /**
   * Constructs a no op call for the vouch(address,address)void ABI method
   *
  * Stake 1 ALGO vouching for a peer agent's future behaviour.
  Slash conditions (enforced off-chain by oracle, then calls slash_voucher):   - vouchee score drops below 400 → slash 50% of stake. Reward condition (oracle calls reward_voucher):   - vouchee score rises above 800 → return stake + 10% bonus.

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static vouch(params: CallParams<ReputationLedgerArgs['obj']['vouch(address,address)void'] | ReputationLedgerArgs['tuple']['vouch(address,address)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'vouch(address,address)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.voucher, params.args.vouchee],
    }
  }
  /**
   * Constructs a no op call for the slash_voucher(address,address)void ABI method
   *
  * Slash 50% of the voucher's stake because vouchee score dropped < 400.
  Called by oracle. Remaining 50% is refunded to voucher.

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static slashVoucher(params: CallParams<ReputationLedgerArgs['obj']['slash_voucher(address,address)void'] | ReputationLedgerArgs['tuple']['slash_voucher(address,address)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'slash_voucher(address,address)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.voucher, params.args.vouchee],
    }
  }
  /**
   * Constructs a no op call for the reward_voucher(address,address)void ABI method
   *
  * Return stake + 10% bonus when vouchee score exceeds 800.
  Called by the oracle.

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static rewardVoucher(params: CallParams<ReputationLedgerArgs['obj']['reward_voucher(address,address)void'] | ReputationLedgerArgs['tuple']['reward_voucher(address,address)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'reward_voucher(address,address)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.voucher, params.args.vouchee],
    }
  }
  /**
   * Constructs a no op call for the get_score(address)uint64 ABI method
   *
   * Return the current reputation score for an agent.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static getScore(params: CallParams<ReputationLedgerArgs['obj']['get_score(address)uint64'] | ReputationLedgerArgs['tuple']['get_score(address)uint64']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'get_score(address)uint64' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.agent],
    }
  }
  /**
   * Constructs a no op call for the get_tier(address)uint64 ABI method
   *
   * Returns tier: 0=BLACKLISTED, 1=RESTRICTED, 2=CAUTION, 3=GOOD, 4=EXCELLENT.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static getTier(params: CallParams<ReputationLedgerArgs['obj']['get_tier(address)uint64'] | ReputationLedgerArgs['tuple']['get_tier(address)uint64']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'get_tier(address)uint64' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.agent],
    }
  }
  /**
   * Constructs a no op call for the get_drift_events(address)uint64 ABI method
   *
   * Return total flagged drift events for forensic audit.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static getDriftEvents(params: CallParams<ReputationLedgerArgs['obj']['get_drift_events(address)uint64'] | ReputationLedgerArgs['tuple']['get_drift_events(address)uint64']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'get_drift_events(address)uint64' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.agent],
    }
  }
}

/**
 * A client to make calls to the ReputationLedger smart contract
 */
export class ReputationLedgerClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `ReputationLedgerClient`
   *
   * @param appClient An `AppClient` instance which has been created with the ReputationLedger app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `ReputationLedgerClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Checks for decode errors on the given return value and maps the return value to the return type for the given method
   * @returns The typed return value or undefined if there was no value
   */
  decodeReturnValue<TSignature extends ReputationLedgerNonVoidMethodSignatures>(method: TSignature, returnValue: ABIReturn | undefined) {
    return returnValue !== undefined ? getArc56ReturnValue<MethodReturn<TSignature>>(returnValue, this.appClient.getABIMethod(method), APP_SPEC.structs) : undefined
  }

  /**
   * Returns a new `ReputationLedgerClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<ReputationLedgerClient> {
    return new ReputationLedgerClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `ReputationLedgerClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<ReputationLedgerClient> {
    return new ReputationLedgerClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Makes a clear_state call to an existing instance of the ReputationLedger smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the ReputationLedger smart contract using the `register_agent(address)void` ABI method.
     *
    * Create a reputation record with neutral score = 500.
    Callable by anyone (idempotent guard inside).

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    registerAgent: (params: CallParams<ReputationLedgerArgs['obj']['register_agent(address)void'] | ReputationLedgerArgs['tuple']['register_agent(address)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(ReputationLedgerParamsFactory.registerAgent(params))
    },

    /**
     * Makes a call to the ReputationLedger smart contract using the `update_score(address,uint64,uint64)void` ABI method.
     *
    * Adjust agent's reputation score.
    delta       : amount to add or subtract. is_negative : 1 = subtract delta, 0 = add delta. Result is clamped to [0, 1000].
    ONLY callable by SentinelEscrow or PolicyVault app (via inner txn). On-chain caller verification uses Txn.sender being the trusted app address.

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    updateScore: (params: CallParams<ReputationLedgerArgs['obj']['update_score(address,uint64,uint64)void'] | ReputationLedgerArgs['tuple']['update_score(address,uint64,uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(ReputationLedgerParamsFactory.updateScore(params))
    },

    /**
     * Makes a call to the ReputationLedger smart contract using the `record_drift(address)void` ABI method.
     *
     * Increment drift_events counter for a flagged agent.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    recordDrift: (params: CallParams<ReputationLedgerArgs['obj']['record_drift(address)void'] | ReputationLedgerArgs['tuple']['record_drift(address)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(ReputationLedgerParamsFactory.recordDrift(params))
    },

    /**
     * Makes a call to the ReputationLedger smart contract using the `vouch(address,address)void` ABI method.
     *
    * Stake 1 ALGO vouching for a peer agent's future behaviour.
    Slash conditions (enforced off-chain by oracle, then calls slash_voucher):   - vouchee score drops below 400 → slash 50% of stake. Reward condition (oracle calls reward_voucher):   - vouchee score rises above 800 → return stake + 10% bonus.

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    vouch: (params: CallParams<ReputationLedgerArgs['obj']['vouch(address,address)void'] | ReputationLedgerArgs['tuple']['vouch(address,address)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(ReputationLedgerParamsFactory.vouch(params))
    },

    /**
     * Makes a call to the ReputationLedger smart contract using the `slash_voucher(address,address)void` ABI method.
     *
    * Slash 50% of the voucher's stake because vouchee score dropped < 400.
    Called by oracle. Remaining 50% is refunded to voucher.

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    slashVoucher: (params: CallParams<ReputationLedgerArgs['obj']['slash_voucher(address,address)void'] | ReputationLedgerArgs['tuple']['slash_voucher(address,address)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(ReputationLedgerParamsFactory.slashVoucher(params))
    },

    /**
     * Makes a call to the ReputationLedger smart contract using the `reward_voucher(address,address)void` ABI method.
     *
    * Return stake + 10% bonus when vouchee score exceeds 800.
    Called by the oracle.

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    rewardVoucher: (params: CallParams<ReputationLedgerArgs['obj']['reward_voucher(address,address)void'] | ReputationLedgerArgs['tuple']['reward_voucher(address,address)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(ReputationLedgerParamsFactory.rewardVoucher(params))
    },

    /**
     * Makes a call to the ReputationLedger smart contract using the `get_score(address)uint64` ABI method.
     *
     * Return the current reputation score for an agent.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    getScore: (params: CallParams<ReputationLedgerArgs['obj']['get_score(address)uint64'] | ReputationLedgerArgs['tuple']['get_score(address)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(ReputationLedgerParamsFactory.getScore(params))
    },

    /**
     * Makes a call to the ReputationLedger smart contract using the `get_tier(address)uint64` ABI method.
     *
     * Returns tier: 0=BLACKLISTED, 1=RESTRICTED, 2=CAUTION, 3=GOOD, 4=EXCELLENT.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    getTier: (params: CallParams<ReputationLedgerArgs['obj']['get_tier(address)uint64'] | ReputationLedgerArgs['tuple']['get_tier(address)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(ReputationLedgerParamsFactory.getTier(params))
    },

    /**
     * Makes a call to the ReputationLedger smart contract using the `get_drift_events(address)uint64` ABI method.
     *
     * Return total flagged drift events for forensic audit.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    getDriftEvents: (params: CallParams<ReputationLedgerArgs['obj']['get_drift_events(address)uint64'] | ReputationLedgerArgs['tuple']['get_drift_events(address)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(ReputationLedgerParamsFactory.getDriftEvents(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Makes a clear_state call to an existing instance of the ReputationLedger smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the ReputationLedger smart contract using the `register_agent(address)void` ABI method.
     *
    * Create a reputation record with neutral score = 500.
    Callable by anyone (idempotent guard inside).

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    registerAgent: (params: CallParams<ReputationLedgerArgs['obj']['register_agent(address)void'] | ReputationLedgerArgs['tuple']['register_agent(address)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(ReputationLedgerParamsFactory.registerAgent(params))
    },

    /**
     * Makes a call to the ReputationLedger smart contract using the `update_score(address,uint64,uint64)void` ABI method.
     *
    * Adjust agent's reputation score.
    delta       : amount to add or subtract. is_negative : 1 = subtract delta, 0 = add delta. Result is clamped to [0, 1000].
    ONLY callable by SentinelEscrow or PolicyVault app (via inner txn). On-chain caller verification uses Txn.sender being the trusted app address.

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    updateScore: (params: CallParams<ReputationLedgerArgs['obj']['update_score(address,uint64,uint64)void'] | ReputationLedgerArgs['tuple']['update_score(address,uint64,uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(ReputationLedgerParamsFactory.updateScore(params))
    },

    /**
     * Makes a call to the ReputationLedger smart contract using the `record_drift(address)void` ABI method.
     *
     * Increment drift_events counter for a flagged agent.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    recordDrift: (params: CallParams<ReputationLedgerArgs['obj']['record_drift(address)void'] | ReputationLedgerArgs['tuple']['record_drift(address)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(ReputationLedgerParamsFactory.recordDrift(params))
    },

    /**
     * Makes a call to the ReputationLedger smart contract using the `vouch(address,address)void` ABI method.
     *
    * Stake 1 ALGO vouching for a peer agent's future behaviour.
    Slash conditions (enforced off-chain by oracle, then calls slash_voucher):   - vouchee score drops below 400 → slash 50% of stake. Reward condition (oracle calls reward_voucher):   - vouchee score rises above 800 → return stake + 10% bonus.

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    vouch: (params: CallParams<ReputationLedgerArgs['obj']['vouch(address,address)void'] | ReputationLedgerArgs['tuple']['vouch(address,address)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(ReputationLedgerParamsFactory.vouch(params))
    },

    /**
     * Makes a call to the ReputationLedger smart contract using the `slash_voucher(address,address)void` ABI method.
     *
    * Slash 50% of the voucher's stake because vouchee score dropped < 400.
    Called by oracle. Remaining 50% is refunded to voucher.

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    slashVoucher: (params: CallParams<ReputationLedgerArgs['obj']['slash_voucher(address,address)void'] | ReputationLedgerArgs['tuple']['slash_voucher(address,address)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(ReputationLedgerParamsFactory.slashVoucher(params))
    },

    /**
     * Makes a call to the ReputationLedger smart contract using the `reward_voucher(address,address)void` ABI method.
     *
    * Return stake + 10% bonus when vouchee score exceeds 800.
    Called by the oracle.

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    rewardVoucher: (params: CallParams<ReputationLedgerArgs['obj']['reward_voucher(address,address)void'] | ReputationLedgerArgs['tuple']['reward_voucher(address,address)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(ReputationLedgerParamsFactory.rewardVoucher(params))
    },

    /**
     * Makes a call to the ReputationLedger smart contract using the `get_score(address)uint64` ABI method.
     *
     * Return the current reputation score for an agent.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    getScore: (params: CallParams<ReputationLedgerArgs['obj']['get_score(address)uint64'] | ReputationLedgerArgs['tuple']['get_score(address)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(ReputationLedgerParamsFactory.getScore(params))
    },

    /**
     * Makes a call to the ReputationLedger smart contract using the `get_tier(address)uint64` ABI method.
     *
     * Returns tier: 0=BLACKLISTED, 1=RESTRICTED, 2=CAUTION, 3=GOOD, 4=EXCELLENT.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    getTier: (params: CallParams<ReputationLedgerArgs['obj']['get_tier(address)uint64'] | ReputationLedgerArgs['tuple']['get_tier(address)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(ReputationLedgerParamsFactory.getTier(params))
    },

    /**
     * Makes a call to the ReputationLedger smart contract using the `get_drift_events(address)uint64` ABI method.
     *
     * Return total flagged drift events for forensic audit.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    getDriftEvents: (params: CallParams<ReputationLedgerArgs['obj']['get_drift_events(address)uint64'] | ReputationLedgerArgs['tuple']['get_drift_events(address)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(ReputationLedgerParamsFactory.getDriftEvents(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Makes a clear_state call to an existing instance of the ReputationLedger smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the ReputationLedger smart contract using the `register_agent(address)void` ABI method.
     *
    * Create a reputation record with neutral score = 500.
    Callable by anyone (idempotent guard inside).

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    registerAgent: async (params: CallParams<ReputationLedgerArgs['obj']['register_agent(address)void'] | ReputationLedgerArgs['tuple']['register_agent(address)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(ReputationLedgerParamsFactory.registerAgent(params))
      return {...result, return: result.return as unknown as (undefined | ReputationLedgerReturns['register_agent(address)void'])}
    },

    /**
     * Makes a call to the ReputationLedger smart contract using the `update_score(address,uint64,uint64)void` ABI method.
     *
    * Adjust agent's reputation score.
    delta       : amount to add or subtract. is_negative : 1 = subtract delta, 0 = add delta. Result is clamped to [0, 1000].
    ONLY callable by SentinelEscrow or PolicyVault app (via inner txn). On-chain caller verification uses Txn.sender being the trusted app address.

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    updateScore: async (params: CallParams<ReputationLedgerArgs['obj']['update_score(address,uint64,uint64)void'] | ReputationLedgerArgs['tuple']['update_score(address,uint64,uint64)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(ReputationLedgerParamsFactory.updateScore(params))
      return {...result, return: result.return as unknown as (undefined | ReputationLedgerReturns['update_score(address,uint64,uint64)void'])}
    },

    /**
     * Makes a call to the ReputationLedger smart contract using the `record_drift(address)void` ABI method.
     *
     * Increment drift_events counter for a flagged agent.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    recordDrift: async (params: CallParams<ReputationLedgerArgs['obj']['record_drift(address)void'] | ReputationLedgerArgs['tuple']['record_drift(address)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(ReputationLedgerParamsFactory.recordDrift(params))
      return {...result, return: result.return as unknown as (undefined | ReputationLedgerReturns['record_drift(address)void'])}
    },

    /**
     * Makes a call to the ReputationLedger smart contract using the `vouch(address,address)void` ABI method.
     *
    * Stake 1 ALGO vouching for a peer agent's future behaviour.
    Slash conditions (enforced off-chain by oracle, then calls slash_voucher):   - vouchee score drops below 400 → slash 50% of stake. Reward condition (oracle calls reward_voucher):   - vouchee score rises above 800 → return stake + 10% bonus.

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    vouch: async (params: CallParams<ReputationLedgerArgs['obj']['vouch(address,address)void'] | ReputationLedgerArgs['tuple']['vouch(address,address)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(ReputationLedgerParamsFactory.vouch(params))
      return {...result, return: result.return as unknown as (undefined | ReputationLedgerReturns['vouch(address,address)void'])}
    },

    /**
     * Makes a call to the ReputationLedger smart contract using the `slash_voucher(address,address)void` ABI method.
     *
    * Slash 50% of the voucher's stake because vouchee score dropped < 400.
    Called by oracle. Remaining 50% is refunded to voucher.

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    slashVoucher: async (params: CallParams<ReputationLedgerArgs['obj']['slash_voucher(address,address)void'] | ReputationLedgerArgs['tuple']['slash_voucher(address,address)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(ReputationLedgerParamsFactory.slashVoucher(params))
      return {...result, return: result.return as unknown as (undefined | ReputationLedgerReturns['slash_voucher(address,address)void'])}
    },

    /**
     * Makes a call to the ReputationLedger smart contract using the `reward_voucher(address,address)void` ABI method.
     *
    * Return stake + 10% bonus when vouchee score exceeds 800.
    Called by the oracle.

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    rewardVoucher: async (params: CallParams<ReputationLedgerArgs['obj']['reward_voucher(address,address)void'] | ReputationLedgerArgs['tuple']['reward_voucher(address,address)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(ReputationLedgerParamsFactory.rewardVoucher(params))
      return {...result, return: result.return as unknown as (undefined | ReputationLedgerReturns['reward_voucher(address,address)void'])}
    },

    /**
     * Makes a call to the ReputationLedger smart contract using the `get_score(address)uint64` ABI method.
     *
     * Return the current reputation score for an agent.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    getScore: async (params: CallParams<ReputationLedgerArgs['obj']['get_score(address)uint64'] | ReputationLedgerArgs['tuple']['get_score(address)uint64']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(ReputationLedgerParamsFactory.getScore(params))
      return {...result, return: result.return as unknown as (undefined | ReputationLedgerReturns['get_score(address)uint64'])}
    },

    /**
     * Makes a call to the ReputationLedger smart contract using the `get_tier(address)uint64` ABI method.
     *
     * Returns tier: 0=BLACKLISTED, 1=RESTRICTED, 2=CAUTION, 3=GOOD, 4=EXCELLENT.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    getTier: async (params: CallParams<ReputationLedgerArgs['obj']['get_tier(address)uint64'] | ReputationLedgerArgs['tuple']['get_tier(address)uint64']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(ReputationLedgerParamsFactory.getTier(params))
      return {...result, return: result.return as unknown as (undefined | ReputationLedgerReturns['get_tier(address)uint64'])}
    },

    /**
     * Makes a call to the ReputationLedger smart contract using the `get_drift_events(address)uint64` ABI method.
     *
     * Return total flagged drift events for forensic audit.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    getDriftEvents: async (params: CallParams<ReputationLedgerArgs['obj']['get_drift_events(address)uint64'] | ReputationLedgerArgs['tuple']['get_drift_events(address)uint64']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(ReputationLedgerParamsFactory.getDriftEvents(params))
      return {...result, return: result.return as unknown as (undefined | ReputationLedgerReturns['get_drift_events(address)uint64'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new ReputationLedgerClient(this.appClient.clone(params))
  }

  /**
   * Methods to access state for the current ReputationLedger app
   */
  state = {
    /**
     * Methods to access global state for the current ReputationLedger app
     */
    global: {
      /**
       * Get all current keyed values from global state
       */
      getAll: async (): Promise<Partial<Expand<GlobalKeysState>>> => {
        const result = await this.appClient.state.global.getAll()
        return {
          sentinelAppId: result.sentinel_app_id,
          policyVaultAppId: result.policy_vault_app_id,
        }
      },
      /**
       * Get the current value of the sentinel_app_id key in global state
       */
      sentinelAppId: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("sentinel_app_id")) as bigint | undefined },
      /**
       * Get the current value of the policy_vault_app_id key in global state
       */
      policyVaultAppId: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("policy_vault_app_id")) as bigint | undefined },
    },
    /**
     * Methods to access box state for the current ReputationLedger app
     */
    box: {
      /**
       * Get all current keyed values from box state
       */
      getAll: async (): Promise<Partial<Expand<BoxKeysState>>> => {
        const result = await this.appClient.state.box.getAll()
        return {
        }
      },
      /**
       * Get values from the rep_records map in box state
       */
      repRecords: {
        /**
         * Get all current values of the rep_records map in box state
         */
        getMap: async (): Promise<Map<string, ReputationRecord>> => { return (await this.appClient.state.box.getMap("rep_records")) as Map<string, ReputationRecord> },
        /**
         * Get a current value of the rep_records map by key from box state
         */
        value: async (key: string): Promise<ReputationRecord | undefined> => { return await this.appClient.state.box.getMapValue("rep_records", key) as ReputationRecord | undefined },
      },
      /**
       * Get values from the vouch_stakes map in box state
       */
      vouchStakes: {
        /**
         * Get all current values of the vouch_stakes map in box state
         */
        getMap: async (): Promise<Map<Uint8Array, VouchRecord>> => { return (await this.appClient.state.box.getMap("vouch_stakes")) as Map<Uint8Array, VouchRecord> },
        /**
         * Get a current value of the vouch_stakes map by key from box state
         */
        value: async (key: Uint8Array | string): Promise<VouchRecord | undefined> => { return await this.appClient.state.box.getMapValue("vouch_stakes", key) as VouchRecord | undefined },
      },
    },
  }

  public newGroup(): ReputationLedgerComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a register_agent(address)void method call against the ReputationLedger contract
       */
      registerAgent(params: CallParams<ReputationLedgerArgs['obj']['register_agent(address)void'] | ReputationLedgerArgs['tuple']['register_agent(address)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.registerAgent(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a update_score(address,uint64,uint64)void method call against the ReputationLedger contract
       */
      updateScore(params: CallParams<ReputationLedgerArgs['obj']['update_score(address,uint64,uint64)void'] | ReputationLedgerArgs['tuple']['update_score(address,uint64,uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.updateScore(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a record_drift(address)void method call against the ReputationLedger contract
       */
      recordDrift(params: CallParams<ReputationLedgerArgs['obj']['record_drift(address)void'] | ReputationLedgerArgs['tuple']['record_drift(address)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.recordDrift(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a vouch(address,address)void method call against the ReputationLedger contract
       */
      vouch(params: CallParams<ReputationLedgerArgs['obj']['vouch(address,address)void'] | ReputationLedgerArgs['tuple']['vouch(address,address)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.vouch(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a slash_voucher(address,address)void method call against the ReputationLedger contract
       */
      slashVoucher(params: CallParams<ReputationLedgerArgs['obj']['slash_voucher(address,address)void'] | ReputationLedgerArgs['tuple']['slash_voucher(address,address)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.slashVoucher(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a reward_voucher(address,address)void method call against the ReputationLedger contract
       */
      rewardVoucher(params: CallParams<ReputationLedgerArgs['obj']['reward_voucher(address,address)void'] | ReputationLedgerArgs['tuple']['reward_voucher(address,address)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.rewardVoucher(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a get_score(address)uint64 method call against the ReputationLedger contract
       */
      getScore(params: CallParams<ReputationLedgerArgs['obj']['get_score(address)uint64'] | ReputationLedgerArgs['tuple']['get_score(address)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.getScore(params)))
        resultMappers.push((v) => client.decodeReturnValue('get_score(address)uint64', v))
        return this
      },
      /**
       * Add a get_tier(address)uint64 method call against the ReputationLedger contract
       */
      getTier(params: CallParams<ReputationLedgerArgs['obj']['get_tier(address)uint64'] | ReputationLedgerArgs['tuple']['get_tier(address)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.getTier(params)))
        resultMappers.push((v) => client.decodeReturnValue('get_tier(address)uint64', v))
        return this
      },
      /**
       * Add a get_drift_events(address)uint64 method call against the ReputationLedger contract
       */
      getDriftEvents(params: CallParams<ReputationLedgerArgs['obj']['get_drift_events(address)uint64'] | ReputationLedgerArgs['tuple']['get_drift_events(address)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.getDriftEvents(params)))
        resultMappers.push((v) => client.decodeReturnValue('get_drift_events(address)uint64', v))
        return this
      },
      /**
       * Add a clear state call to the ReputationLedger contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as ReputationLedgerComposer
  }
}
export type ReputationLedgerComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the register_agent(address)void ABI method.
   *
  * Create a reputation record with neutral score = 500.
  Callable by anyone (idempotent guard inside).

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  registerAgent(params?: CallParams<ReputationLedgerArgs['obj']['register_agent(address)void'] | ReputationLedgerArgs['tuple']['register_agent(address)void']>): ReputationLedgerComposer<[...TReturns, ReputationLedgerReturns['register_agent(address)void'] | undefined]>

  /**
   * Calls the update_score(address,uint64,uint64)void ABI method.
   *
  * Adjust agent's reputation score.
  delta       : amount to add or subtract. is_negative : 1 = subtract delta, 0 = add delta. Result is clamped to [0, 1000].
  ONLY callable by SentinelEscrow or PolicyVault app (via inner txn). On-chain caller verification uses Txn.sender being the trusted app address.

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  updateScore(params?: CallParams<ReputationLedgerArgs['obj']['update_score(address,uint64,uint64)void'] | ReputationLedgerArgs['tuple']['update_score(address,uint64,uint64)void']>): ReputationLedgerComposer<[...TReturns, ReputationLedgerReturns['update_score(address,uint64,uint64)void'] | undefined]>

  /**
   * Calls the record_drift(address)void ABI method.
   *
   * Increment drift_events counter for a flagged agent.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  recordDrift(params?: CallParams<ReputationLedgerArgs['obj']['record_drift(address)void'] | ReputationLedgerArgs['tuple']['record_drift(address)void']>): ReputationLedgerComposer<[...TReturns, ReputationLedgerReturns['record_drift(address)void'] | undefined]>

  /**
   * Calls the vouch(address,address)void ABI method.
   *
  * Stake 1 ALGO vouching for a peer agent's future behaviour.
  Slash conditions (enforced off-chain by oracle, then calls slash_voucher):   - vouchee score drops below 400 → slash 50% of stake. Reward condition (oracle calls reward_voucher):   - vouchee score rises above 800 → return stake + 10% bonus.

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  vouch(params?: CallParams<ReputationLedgerArgs['obj']['vouch(address,address)void'] | ReputationLedgerArgs['tuple']['vouch(address,address)void']>): ReputationLedgerComposer<[...TReturns, ReputationLedgerReturns['vouch(address,address)void'] | undefined]>

  /**
   * Calls the slash_voucher(address,address)void ABI method.
   *
  * Slash 50% of the voucher's stake because vouchee score dropped < 400.
  Called by oracle. Remaining 50% is refunded to voucher.

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  slashVoucher(params?: CallParams<ReputationLedgerArgs['obj']['slash_voucher(address,address)void'] | ReputationLedgerArgs['tuple']['slash_voucher(address,address)void']>): ReputationLedgerComposer<[...TReturns, ReputationLedgerReturns['slash_voucher(address,address)void'] | undefined]>

  /**
   * Calls the reward_voucher(address,address)void ABI method.
   *
  * Return stake + 10% bonus when vouchee score exceeds 800.
  Called by the oracle.

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  rewardVoucher(params?: CallParams<ReputationLedgerArgs['obj']['reward_voucher(address,address)void'] | ReputationLedgerArgs['tuple']['reward_voucher(address,address)void']>): ReputationLedgerComposer<[...TReturns, ReputationLedgerReturns['reward_voucher(address,address)void'] | undefined]>

  /**
   * Calls the get_score(address)uint64 ABI method.
   *
   * Return the current reputation score for an agent.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  getScore(params?: CallParams<ReputationLedgerArgs['obj']['get_score(address)uint64'] | ReputationLedgerArgs['tuple']['get_score(address)uint64']>): ReputationLedgerComposer<[...TReturns, ReputationLedgerReturns['get_score(address)uint64'] | undefined]>

  /**
   * Calls the get_tier(address)uint64 ABI method.
   *
   * Returns tier: 0=BLACKLISTED, 1=RESTRICTED, 2=CAUTION, 3=GOOD, 4=EXCELLENT.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  getTier(params?: CallParams<ReputationLedgerArgs['obj']['get_tier(address)uint64'] | ReputationLedgerArgs['tuple']['get_tier(address)uint64']>): ReputationLedgerComposer<[...TReturns, ReputationLedgerReturns['get_tier(address)uint64'] | undefined]>

  /**
   * Calls the get_drift_events(address)uint64 ABI method.
   *
   * Return total flagged drift events for forensic audit.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  getDriftEvents(params?: CallParams<ReputationLedgerArgs['obj']['get_drift_events(address)uint64'] | ReputationLedgerArgs['tuple']['get_drift_events(address)uint64']>): ReputationLedgerComposer<[...TReturns, ReputationLedgerReturns['get_drift_events(address)uint64'] | undefined]>

  /**
   * Makes a clear_state call to an existing instance of the ReputationLedger smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): ReputationLedgerComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): ReputationLedgerComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<ReputationLedgerComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<ReputationLedgerComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<ReputationLedgerComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<ReputationLedgerComposerResults<TReturns>>
}
export type ReputationLedgerComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

