/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"PaymentDNARegistry","structs":{"DNARecord":[{"name":"vector","type":"byte[]"},{"name":"lastUpdated","type":"uint64"},{"name":"totalPayments","type":"uint64"},{"name":"driftEvents","type":"uint64"}]},"methods":[{"name":"create","args":[{"type":"uint64","name":"sentinel_escrow_app_id"}],"returns":{"type":"void"},"actions":{"create":["NoOp"],"call":[]},"readonly":false,"desc":"Called once on deployment to lock in the trusted caller.","events":[],"recommendations":{}},{"name":"initialize_dna","args":[{"type":"address","name":"agent"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Create a zero-vector DNA record for a new agent.\nCallable by anyone (agent must not already have a record).","events":[],"recommendations":{}},{"name":"update_dna","args":[{"type":"address","name":"agent"},{"type":"byte[]","name":"observation"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Merge a 32-byte observation into the agent's DNA vector using EMA.\nONLY callable by the SentinelEscrow application (inner-txn caller).\nobservation : 32 bytes (int8 × 32) from the off-chain SDK.","events":[],"recommendations":{}},{"name":"record_drift_event","args":[{"type":"address","name":"agent"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Increment the drift event counter for forensic audit.","events":[],"recommendations":{}},{"name":"get_dna_vector","args":[{"type":"address","name":"agent"}],"returns":{"type":"byte[]"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Return the raw 32-byte DNA vector for an agent.","events":[],"recommendations":{}},{"name":"get_drift_events","args":[{"type":"address","name":"agent"}],"returns":{"type":"uint64"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Return total recorded drift events for an agent.","events":[],"recommendations":{}},{"name":"get_total_payments","args":[{"type":"address","name":"agent"}],"returns":{"type":"uint64"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Return total successful payments recorded for an agent.","events":[],"recommendations":{}}],"arcs":[22,28],"desc":"\n    Stores one DNARecord per agent address.\n    Only SentinelEscrow (stored app_id) may call update_dna().\n    ","networks":{},"state":{"schema":{"global":{"ints":1,"bytes":0},"local":{"ints":0,"bytes":0}},"keys":{"global":{"sentinel_app_id":{"keyType":"AVMString","valueType":"AVMUint64","key":"c2VudGluZWxfYXBwX2lk"}},"local":{},"box":{}},"maps":{"global":{},"local":{},"box":{"dna_records":{"keyType":"address","valueType":"DNARecord","prefix":"ZG5hOg=="}}}},"bareActions":{"create":[],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[150],"errorMessage":"DNA already initialised for agent"},{"pc":[323,412,446,472],"errorMessage":"DNA not initialised"},{"pc":[257],"errorMessage":"DNA not initialised — call initialize_dna first"},{"pc":[242],"errorMessage":"caller check placeholder"},{"pc":[265,274,346,353,360,450,476],"errorMessage":"index out of bounds"},{"pc":[224],"errorMessage":"invalid array length header"},{"pc":[231],"errorMessage":"invalid number of bytes for arc4.dynamic_array<arc4.uint8>"},{"pc":[141,217,315,404,438,464],"errorMessage":"invalid number of bytes for arc4.static_array<arc4.uint8, 32>"},{"pc":[127],"errorMessage":"invalid number of bytes for arc4.uint64"},{"pc":[248],"errorMessage":"observation must be 32 bytes"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"events":[]} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


// Type definitions for ARC-56 structs

export type DnaRecord = {
  vector: Uint8Array,
  lastUpdated: bigint,
  totalPayments: bigint,
  driftEvents: bigint
}


/**
 * Converts the ABI tuple representation of a DNARecord to the struct representation
 */
export function DnaRecordFromTuple(abiTuple: [Uint8Array, bigint, bigint, bigint]) {
  return getABIStructFromABITuple(abiTuple, APP_SPEC.structs.DNARecord, APP_SPEC.structs) as DnaRecord
}

/**
 * The argument types for the PaymentDnaRegistry contract
 */
export type PaymentDnaRegistryArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'create(uint64)void': {
      sentinelEscrowAppId: bigint | number
    }
    'initialize_dna(address)void': {
      agent: string
    }
    'update_dna(address,byte[])void': {
      agent: string
      observation: Uint8Array
    }
    'record_drift_event(address)void': {
      agent: string
    }
    'get_dna_vector(address)byte[]': {
      agent: string
    }
    'get_drift_events(address)uint64': {
      agent: string
    }
    'get_total_payments(address)uint64': {
      agent: string
    }
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'create(uint64)void': [sentinelEscrowAppId: bigint | number]
    'initialize_dna(address)void': [agent: string]
    'update_dna(address,byte[])void': [agent: string, observation: Uint8Array]
    'record_drift_event(address)void': [agent: string]
    'get_dna_vector(address)byte[]': [agent: string]
    'get_drift_events(address)uint64': [agent: string]
    'get_total_payments(address)uint64': [agent: string]
  }
}

/**
 * The return type for each method
 */
export type PaymentDnaRegistryReturns = {
  'create(uint64)void': void
  'initialize_dna(address)void': void
  'update_dna(address,byte[])void': void
  'record_drift_event(address)void': void
  'get_dna_vector(address)byte[]': Uint8Array
  'get_drift_events(address)uint64': bigint
  'get_total_payments(address)uint64': bigint
}

/**
 * Defines the types of available calls and state of the PaymentDnaRegistry smart contract.
 */
export type PaymentDnaRegistryTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'create(uint64)void' | 'create', {
      argsObj: PaymentDnaRegistryArgs['obj']['create(uint64)void']
      argsTuple: PaymentDnaRegistryArgs['tuple']['create(uint64)void']
      returns: PaymentDnaRegistryReturns['create(uint64)void']
    }>
    & Record<'initialize_dna(address)void' | 'initialize_dna', {
      argsObj: PaymentDnaRegistryArgs['obj']['initialize_dna(address)void']
      argsTuple: PaymentDnaRegistryArgs['tuple']['initialize_dna(address)void']
      returns: PaymentDnaRegistryReturns['initialize_dna(address)void']
    }>
    & Record<'update_dna(address,byte[])void' | 'update_dna', {
      argsObj: PaymentDnaRegistryArgs['obj']['update_dna(address,byte[])void']
      argsTuple: PaymentDnaRegistryArgs['tuple']['update_dna(address,byte[])void']
      returns: PaymentDnaRegistryReturns['update_dna(address,byte[])void']
    }>
    & Record<'record_drift_event(address)void' | 'record_drift_event', {
      argsObj: PaymentDnaRegistryArgs['obj']['record_drift_event(address)void']
      argsTuple: PaymentDnaRegistryArgs['tuple']['record_drift_event(address)void']
      returns: PaymentDnaRegistryReturns['record_drift_event(address)void']
    }>
    & Record<'get_dna_vector(address)byte[]' | 'get_dna_vector', {
      argsObj: PaymentDnaRegistryArgs['obj']['get_dna_vector(address)byte[]']
      argsTuple: PaymentDnaRegistryArgs['tuple']['get_dna_vector(address)byte[]']
      returns: PaymentDnaRegistryReturns['get_dna_vector(address)byte[]']
    }>
    & Record<'get_drift_events(address)uint64' | 'get_drift_events', {
      argsObj: PaymentDnaRegistryArgs['obj']['get_drift_events(address)uint64']
      argsTuple: PaymentDnaRegistryArgs['tuple']['get_drift_events(address)uint64']
      returns: PaymentDnaRegistryReturns['get_drift_events(address)uint64']
    }>
    & Record<'get_total_payments(address)uint64' | 'get_total_payments', {
      argsObj: PaymentDnaRegistryArgs['obj']['get_total_payments(address)uint64']
      argsTuple: PaymentDnaRegistryArgs['tuple']['get_total_payments(address)uint64']
      returns: PaymentDnaRegistryReturns['get_total_payments(address)uint64']
    }>
  /**
   * Defines the shape of the state of the application.
   */
  state: {
    global: {
      keys: {
        sentinelAppId: bigint
      }
      maps: {}
    }
    box: {
      keys: {}
      maps: {
        dnaRecords: Map<string, DnaRecord>
      }
    }
  }
}

/**
 * Defines the possible abi call signatures.
 */
export type PaymentDnaRegistrySignatures = keyof PaymentDnaRegistryTypes['methods']
/**
 * Defines the possible abi call signatures for methods that return a non-void value.
 */
export type PaymentDnaRegistryNonVoidMethodSignatures = keyof PaymentDnaRegistryTypes['methods'] extends infer T ? T extends keyof PaymentDnaRegistryTypes['methods'] ? MethodReturn<T> extends void ? never : T  : never : never
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the PaymentDnaRegistry smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends PaymentDnaRegistrySignatures> = PaymentDnaRegistryTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the PaymentDnaRegistry smart contract to the method's return type
 */
export type MethodReturn<TSignature extends PaymentDnaRegistrySignatures> = PaymentDnaRegistryTypes['methods'][TSignature]['returns']

/**
 * Defines the shape of the keyed global state of the application.
 */
export type GlobalKeysState = PaymentDnaRegistryTypes['state']['global']['keys']

/**
 * Defines the shape of the keyed box state of the application.
 */
export type BoxKeysState = PaymentDnaRegistryTypes['state']['box']['keys']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the PaymentDnaRegistry smart contract
 */
export abstract class PaymentDnaRegistryParamsFactory {
  /**
   * Constructs a no op call for the initialize_dna(address)void ABI method
   *
  * Create a zero-vector DNA record for a new agent.
  Callable by anyone (agent must not already have a record).

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static initializeDna(params: CallParams<PaymentDnaRegistryArgs['obj']['initialize_dna(address)void'] | PaymentDnaRegistryArgs['tuple']['initialize_dna(address)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'initialize_dna(address)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.agent],
    }
  }
  /**
   * Constructs a no op call for the update_dna(address,byte[])void ABI method
   *
  * Merge a 32-byte observation into the agent's DNA vector using EMA.
  ONLY callable by the SentinelEscrow application (inner-txn caller).
  observation : 32 bytes (int8 × 32) from the off-chain SDK.

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static updateDna(params: CallParams<PaymentDnaRegistryArgs['obj']['update_dna(address,byte[])void'] | PaymentDnaRegistryArgs['tuple']['update_dna(address,byte[])void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'update_dna(address,byte[])void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.agent, params.args.observation],
    }
  }
  /**
   * Constructs a no op call for the record_drift_event(address)void ABI method
   *
   * Increment the drift event counter for forensic audit.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static recordDriftEvent(params: CallParams<PaymentDnaRegistryArgs['obj']['record_drift_event(address)void'] | PaymentDnaRegistryArgs['tuple']['record_drift_event(address)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'record_drift_event(address)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.agent],
    }
  }
  /**
   * Constructs a no op call for the get_dna_vector(address)byte[] ABI method
   *
   * Return the raw 32-byte DNA vector for an agent.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static getDnaVector(params: CallParams<PaymentDnaRegistryArgs['obj']['get_dna_vector(address)byte[]'] | PaymentDnaRegistryArgs['tuple']['get_dna_vector(address)byte[]']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'get_dna_vector(address)byte[]' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.agent],
    }
  }
  /**
   * Constructs a no op call for the get_drift_events(address)uint64 ABI method
   *
   * Return total recorded drift events for an agent.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static getDriftEvents(params: CallParams<PaymentDnaRegistryArgs['obj']['get_drift_events(address)uint64'] | PaymentDnaRegistryArgs['tuple']['get_drift_events(address)uint64']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'get_drift_events(address)uint64' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.agent],
    }
  }
  /**
   * Constructs a no op call for the get_total_payments(address)uint64 ABI method
   *
   * Return total successful payments recorded for an agent.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static getTotalPayments(params: CallParams<PaymentDnaRegistryArgs['obj']['get_total_payments(address)uint64'] | PaymentDnaRegistryArgs['tuple']['get_total_payments(address)uint64']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'get_total_payments(address)uint64' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.agent],
    }
  }
}

/**
 * A client to make calls to the PaymentDNARegistry smart contract
 */
export class PaymentDnaRegistryClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `PaymentDnaRegistryClient`
   *
   * @param appClient An `AppClient` instance which has been created with the PaymentDnaRegistry app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `PaymentDnaRegistryClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Checks for decode errors on the given return value and maps the return value to the return type for the given method
   * @returns The typed return value or undefined if there was no value
   */
  decodeReturnValue<TSignature extends PaymentDnaRegistryNonVoidMethodSignatures>(method: TSignature, returnValue: ABIReturn | undefined) {
    return returnValue !== undefined ? getArc56ReturnValue<MethodReturn<TSignature>>(returnValue, this.appClient.getABIMethod(method), APP_SPEC.structs) : undefined
  }

  /**
   * Returns a new `PaymentDnaRegistryClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<PaymentDnaRegistryClient> {
    return new PaymentDnaRegistryClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `PaymentDnaRegistryClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<PaymentDnaRegistryClient> {
    return new PaymentDnaRegistryClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Makes a clear_state call to an existing instance of the PaymentDNARegistry smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the PaymentDNARegistry smart contract using the `initialize_dna(address)void` ABI method.
     *
    * Create a zero-vector DNA record for a new agent.
    Callable by anyone (agent must not already have a record).

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    initializeDna: (params: CallParams<PaymentDnaRegistryArgs['obj']['initialize_dna(address)void'] | PaymentDnaRegistryArgs['tuple']['initialize_dna(address)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(PaymentDnaRegistryParamsFactory.initializeDna(params))
    },

    /**
     * Makes a call to the PaymentDNARegistry smart contract using the `update_dna(address,byte[])void` ABI method.
     *
    * Merge a 32-byte observation into the agent's DNA vector using EMA.
    ONLY callable by the SentinelEscrow application (inner-txn caller).
    observation : 32 bytes (int8 × 32) from the off-chain SDK.

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    updateDna: (params: CallParams<PaymentDnaRegistryArgs['obj']['update_dna(address,byte[])void'] | PaymentDnaRegistryArgs['tuple']['update_dna(address,byte[])void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(PaymentDnaRegistryParamsFactory.updateDna(params))
    },

    /**
     * Makes a call to the PaymentDNARegistry smart contract using the `record_drift_event(address)void` ABI method.
     *
     * Increment the drift event counter for forensic audit.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    recordDriftEvent: (params: CallParams<PaymentDnaRegistryArgs['obj']['record_drift_event(address)void'] | PaymentDnaRegistryArgs['tuple']['record_drift_event(address)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(PaymentDnaRegistryParamsFactory.recordDriftEvent(params))
    },

    /**
     * Makes a call to the PaymentDNARegistry smart contract using the `get_dna_vector(address)byte[]` ABI method.
     *
     * Return the raw 32-byte DNA vector for an agent.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    getDnaVector: (params: CallParams<PaymentDnaRegistryArgs['obj']['get_dna_vector(address)byte[]'] | PaymentDnaRegistryArgs['tuple']['get_dna_vector(address)byte[]']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(PaymentDnaRegistryParamsFactory.getDnaVector(params))
    },

    /**
     * Makes a call to the PaymentDNARegistry smart contract using the `get_drift_events(address)uint64` ABI method.
     *
     * Return total recorded drift events for an agent.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    getDriftEvents: (params: CallParams<PaymentDnaRegistryArgs['obj']['get_drift_events(address)uint64'] | PaymentDnaRegistryArgs['tuple']['get_drift_events(address)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(PaymentDnaRegistryParamsFactory.getDriftEvents(params))
    },

    /**
     * Makes a call to the PaymentDNARegistry smart contract using the `get_total_payments(address)uint64` ABI method.
     *
     * Return total successful payments recorded for an agent.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    getTotalPayments: (params: CallParams<PaymentDnaRegistryArgs['obj']['get_total_payments(address)uint64'] | PaymentDnaRegistryArgs['tuple']['get_total_payments(address)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(PaymentDnaRegistryParamsFactory.getTotalPayments(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Makes a clear_state call to an existing instance of the PaymentDNARegistry smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the PaymentDNARegistry smart contract using the `initialize_dna(address)void` ABI method.
     *
    * Create a zero-vector DNA record for a new agent.
    Callable by anyone (agent must not already have a record).

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    initializeDna: (params: CallParams<PaymentDnaRegistryArgs['obj']['initialize_dna(address)void'] | PaymentDnaRegistryArgs['tuple']['initialize_dna(address)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(PaymentDnaRegistryParamsFactory.initializeDna(params))
    },

    /**
     * Makes a call to the PaymentDNARegistry smart contract using the `update_dna(address,byte[])void` ABI method.
     *
    * Merge a 32-byte observation into the agent's DNA vector using EMA.
    ONLY callable by the SentinelEscrow application (inner-txn caller).
    observation : 32 bytes (int8 × 32) from the off-chain SDK.

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    updateDna: (params: CallParams<PaymentDnaRegistryArgs['obj']['update_dna(address,byte[])void'] | PaymentDnaRegistryArgs['tuple']['update_dna(address,byte[])void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(PaymentDnaRegistryParamsFactory.updateDna(params))
    },

    /**
     * Makes a call to the PaymentDNARegistry smart contract using the `record_drift_event(address)void` ABI method.
     *
     * Increment the drift event counter for forensic audit.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    recordDriftEvent: (params: CallParams<PaymentDnaRegistryArgs['obj']['record_drift_event(address)void'] | PaymentDnaRegistryArgs['tuple']['record_drift_event(address)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(PaymentDnaRegistryParamsFactory.recordDriftEvent(params))
    },

    /**
     * Makes a call to the PaymentDNARegistry smart contract using the `get_dna_vector(address)byte[]` ABI method.
     *
     * Return the raw 32-byte DNA vector for an agent.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    getDnaVector: (params: CallParams<PaymentDnaRegistryArgs['obj']['get_dna_vector(address)byte[]'] | PaymentDnaRegistryArgs['tuple']['get_dna_vector(address)byte[]']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(PaymentDnaRegistryParamsFactory.getDnaVector(params))
    },

    /**
     * Makes a call to the PaymentDNARegistry smart contract using the `get_drift_events(address)uint64` ABI method.
     *
     * Return total recorded drift events for an agent.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    getDriftEvents: (params: CallParams<PaymentDnaRegistryArgs['obj']['get_drift_events(address)uint64'] | PaymentDnaRegistryArgs['tuple']['get_drift_events(address)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(PaymentDnaRegistryParamsFactory.getDriftEvents(params))
    },

    /**
     * Makes a call to the PaymentDNARegistry smart contract using the `get_total_payments(address)uint64` ABI method.
     *
     * Return total successful payments recorded for an agent.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    getTotalPayments: (params: CallParams<PaymentDnaRegistryArgs['obj']['get_total_payments(address)uint64'] | PaymentDnaRegistryArgs['tuple']['get_total_payments(address)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(PaymentDnaRegistryParamsFactory.getTotalPayments(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Makes a clear_state call to an existing instance of the PaymentDNARegistry smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the PaymentDNARegistry smart contract using the `initialize_dna(address)void` ABI method.
     *
    * Create a zero-vector DNA record for a new agent.
    Callable by anyone (agent must not already have a record).

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    initializeDna: async (params: CallParams<PaymentDnaRegistryArgs['obj']['initialize_dna(address)void'] | PaymentDnaRegistryArgs['tuple']['initialize_dna(address)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(PaymentDnaRegistryParamsFactory.initializeDna(params))
      return {...result, return: result.return as unknown as (undefined | PaymentDnaRegistryReturns['initialize_dna(address)void'])}
    },

    /**
     * Makes a call to the PaymentDNARegistry smart contract using the `update_dna(address,byte[])void` ABI method.
     *
    * Merge a 32-byte observation into the agent's DNA vector using EMA.
    ONLY callable by the SentinelEscrow application (inner-txn caller).
    observation : 32 bytes (int8 × 32) from the off-chain SDK.

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    updateDna: async (params: CallParams<PaymentDnaRegistryArgs['obj']['update_dna(address,byte[])void'] | PaymentDnaRegistryArgs['tuple']['update_dna(address,byte[])void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(PaymentDnaRegistryParamsFactory.updateDna(params))
      return {...result, return: result.return as unknown as (undefined | PaymentDnaRegistryReturns['update_dna(address,byte[])void'])}
    },

    /**
     * Makes a call to the PaymentDNARegistry smart contract using the `record_drift_event(address)void` ABI method.
     *
     * Increment the drift event counter for forensic audit.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    recordDriftEvent: async (params: CallParams<PaymentDnaRegistryArgs['obj']['record_drift_event(address)void'] | PaymentDnaRegistryArgs['tuple']['record_drift_event(address)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(PaymentDnaRegistryParamsFactory.recordDriftEvent(params))
      return {...result, return: result.return as unknown as (undefined | PaymentDnaRegistryReturns['record_drift_event(address)void'])}
    },

    /**
     * Makes a call to the PaymentDNARegistry smart contract using the `get_dna_vector(address)byte[]` ABI method.
     *
     * Return the raw 32-byte DNA vector for an agent.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    getDnaVector: async (params: CallParams<PaymentDnaRegistryArgs['obj']['get_dna_vector(address)byte[]'] | PaymentDnaRegistryArgs['tuple']['get_dna_vector(address)byte[]']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(PaymentDnaRegistryParamsFactory.getDnaVector(params))
      return {...result, return: result.return as unknown as (undefined | PaymentDnaRegistryReturns['get_dna_vector(address)byte[]'])}
    },

    /**
     * Makes a call to the PaymentDNARegistry smart contract using the `get_drift_events(address)uint64` ABI method.
     *
     * Return total recorded drift events for an agent.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    getDriftEvents: async (params: CallParams<PaymentDnaRegistryArgs['obj']['get_drift_events(address)uint64'] | PaymentDnaRegistryArgs['tuple']['get_drift_events(address)uint64']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(PaymentDnaRegistryParamsFactory.getDriftEvents(params))
      return {...result, return: result.return as unknown as (undefined | PaymentDnaRegistryReturns['get_drift_events(address)uint64'])}
    },

    /**
     * Makes a call to the PaymentDNARegistry smart contract using the `get_total_payments(address)uint64` ABI method.
     *
     * Return total successful payments recorded for an agent.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    getTotalPayments: async (params: CallParams<PaymentDnaRegistryArgs['obj']['get_total_payments(address)uint64'] | PaymentDnaRegistryArgs['tuple']['get_total_payments(address)uint64']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(PaymentDnaRegistryParamsFactory.getTotalPayments(params))
      return {...result, return: result.return as unknown as (undefined | PaymentDnaRegistryReturns['get_total_payments(address)uint64'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new PaymentDnaRegistryClient(this.appClient.clone(params))
  }

  /**
   * Methods to access state for the current PaymentDNARegistry app
   */
  state = {
    /**
     * Methods to access global state for the current PaymentDNARegistry app
     */
    global: {
      /**
       * Get all current keyed values from global state
       */
      getAll: async (): Promise<Partial<Expand<GlobalKeysState>>> => {
        const result = await this.appClient.state.global.getAll()
        return {
          sentinelAppId: result.sentinel_app_id,
        }
      },
      /**
       * Get the current value of the sentinel_app_id key in global state
       */
      sentinelAppId: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("sentinel_app_id")) as bigint | undefined },
    },
    /**
     * Methods to access box state for the current PaymentDNARegistry app
     */
    box: {
      /**
       * Get all current keyed values from box state
       */
      getAll: async (): Promise<Partial<Expand<BoxKeysState>>> => {
        const result = await this.appClient.state.box.getAll()
        return {
        }
      },
      /**
       * Get values from the dna_records map in box state
       */
      dnaRecords: {
        /**
         * Get all current values of the dna_records map in box state
         */
        getMap: async (): Promise<Map<string, DnaRecord>> => { return (await this.appClient.state.box.getMap("dna_records")) as Map<string, DnaRecord> },
        /**
         * Get a current value of the dna_records map by key from box state
         */
        value: async (key: string): Promise<DnaRecord | undefined> => { return await this.appClient.state.box.getMapValue("dna_records", key) as DnaRecord | undefined },
      },
    },
  }

  public newGroup(): PaymentDnaRegistryComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a initialize_dna(address)void method call against the PaymentDNARegistry contract
       */
      initializeDna(params: CallParams<PaymentDnaRegistryArgs['obj']['initialize_dna(address)void'] | PaymentDnaRegistryArgs['tuple']['initialize_dna(address)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.initializeDna(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a update_dna(address,byte[])void method call against the PaymentDNARegistry contract
       */
      updateDna(params: CallParams<PaymentDnaRegistryArgs['obj']['update_dna(address,byte[])void'] | PaymentDnaRegistryArgs['tuple']['update_dna(address,byte[])void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.updateDna(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a record_drift_event(address)void method call against the PaymentDNARegistry contract
       */
      recordDriftEvent(params: CallParams<PaymentDnaRegistryArgs['obj']['record_drift_event(address)void'] | PaymentDnaRegistryArgs['tuple']['record_drift_event(address)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.recordDriftEvent(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a get_dna_vector(address)byte[] method call against the PaymentDNARegistry contract
       */
      getDnaVector(params: CallParams<PaymentDnaRegistryArgs['obj']['get_dna_vector(address)byte[]'] | PaymentDnaRegistryArgs['tuple']['get_dna_vector(address)byte[]']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.getDnaVector(params)))
        resultMappers.push((v) => client.decodeReturnValue('get_dna_vector(address)byte[]', v))
        return this
      },
      /**
       * Add a get_drift_events(address)uint64 method call against the PaymentDNARegistry contract
       */
      getDriftEvents(params: CallParams<PaymentDnaRegistryArgs['obj']['get_drift_events(address)uint64'] | PaymentDnaRegistryArgs['tuple']['get_drift_events(address)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.getDriftEvents(params)))
        resultMappers.push((v) => client.decodeReturnValue('get_drift_events(address)uint64', v))
        return this
      },
      /**
       * Add a get_total_payments(address)uint64 method call against the PaymentDNARegistry contract
       */
      getTotalPayments(params: CallParams<PaymentDnaRegistryArgs['obj']['get_total_payments(address)uint64'] | PaymentDnaRegistryArgs['tuple']['get_total_payments(address)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.getTotalPayments(params)))
        resultMappers.push((v) => client.decodeReturnValue('get_total_payments(address)uint64', v))
        return this
      },
      /**
       * Add a clear state call to the PaymentDNARegistry contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as PaymentDnaRegistryComposer
  }
}
export type PaymentDnaRegistryComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the initialize_dna(address)void ABI method.
   *
  * Create a zero-vector DNA record for a new agent.
  Callable by anyone (agent must not already have a record).

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  initializeDna(params?: CallParams<PaymentDnaRegistryArgs['obj']['initialize_dna(address)void'] | PaymentDnaRegistryArgs['tuple']['initialize_dna(address)void']>): PaymentDnaRegistryComposer<[...TReturns, PaymentDnaRegistryReturns['initialize_dna(address)void'] | undefined]>

  /**
   * Calls the update_dna(address,byte[])void ABI method.
   *
  * Merge a 32-byte observation into the agent's DNA vector using EMA.
  ONLY callable by the SentinelEscrow application (inner-txn caller).
  observation : 32 bytes (int8 × 32) from the off-chain SDK.

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  updateDna(params?: CallParams<PaymentDnaRegistryArgs['obj']['update_dna(address,byte[])void'] | PaymentDnaRegistryArgs['tuple']['update_dna(address,byte[])void']>): PaymentDnaRegistryComposer<[...TReturns, PaymentDnaRegistryReturns['update_dna(address,byte[])void'] | undefined]>

  /**
   * Calls the record_drift_event(address)void ABI method.
   *
   * Increment the drift event counter for forensic audit.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  recordDriftEvent(params?: CallParams<PaymentDnaRegistryArgs['obj']['record_drift_event(address)void'] | PaymentDnaRegistryArgs['tuple']['record_drift_event(address)void']>): PaymentDnaRegistryComposer<[...TReturns, PaymentDnaRegistryReturns['record_drift_event(address)void'] | undefined]>

  /**
   * Calls the get_dna_vector(address)byte[] ABI method.
   *
   * Return the raw 32-byte DNA vector for an agent.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  getDnaVector(params?: CallParams<PaymentDnaRegistryArgs['obj']['get_dna_vector(address)byte[]'] | PaymentDnaRegistryArgs['tuple']['get_dna_vector(address)byte[]']>): PaymentDnaRegistryComposer<[...TReturns, PaymentDnaRegistryReturns['get_dna_vector(address)byte[]'] | undefined]>

  /**
   * Calls the get_drift_events(address)uint64 ABI method.
   *
   * Return total recorded drift events for an agent.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  getDriftEvents(params?: CallParams<PaymentDnaRegistryArgs['obj']['get_drift_events(address)uint64'] | PaymentDnaRegistryArgs['tuple']['get_drift_events(address)uint64']>): PaymentDnaRegistryComposer<[...TReturns, PaymentDnaRegistryReturns['get_drift_events(address)uint64'] | undefined]>

  /**
   * Calls the get_total_payments(address)uint64 ABI method.
   *
   * Return total successful payments recorded for an agent.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  getTotalPayments(params?: CallParams<PaymentDnaRegistryArgs['obj']['get_total_payments(address)uint64'] | PaymentDnaRegistryArgs['tuple']['get_total_payments(address)uint64']>): PaymentDnaRegistryComposer<[...TReturns, PaymentDnaRegistryReturns['get_total_payments(address)uint64'] | undefined]>

  /**
   * Makes a clear_state call to an existing instance of the PaymentDNARegistry smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): PaymentDnaRegistryComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): PaymentDnaRegistryComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<PaymentDnaRegistryComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<PaymentDnaRegistryComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<PaymentDnaRegistryComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<PaymentDnaRegistryComposerResults<TReturns>>
}
export type PaymentDnaRegistryComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

