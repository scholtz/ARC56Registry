/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"SentinelEscrow","structs":{"BlockedEvent":[{"name":"escrowId","type":"byte[]"},{"name":"reasonCode","type":"uint64"},{"name":"blockedRound","type":"uint64"},{"name":"payer","type":"address"}],"EscrowRecord":[{"name":"payer","type":"address"},{"name":"provider","type":"address"},{"name":"amount","type":"uint64"},{"name":"deadlineRound","type":"uint64"},{"name":"quarantineFlag","type":"uint64"},{"name":"consensusRequired","type":"uint64"},{"name":"intentHash","type":"byte[]"},{"name":"status","type":"uint64"}]},"methods":[{"name":"create","args":[{"type":"uint64","name":"policy_vault_app_id"}],"returns":{"type":"void"},"actions":{"create":["NoOp"],"call":[]},"readonly":false,"desc":"Called once on deploy. Records the PolicyVault app ID.","events":[],"recommendations":{}},{"name":"deposit","args":[{"type":"address","name":"provider"},{"type":"byte[]","name":"intent_hash"},{"type":"uint64","name":"deadline_rounds"},{"type":"uint64","name":"requires_consensus"}],"returns":{"type":"byte[]"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Accept an incoming ALGO payment and open an escrow record.\nThe caller must attach a PaymentTransaction (inner group) whose receiver is this app's address.  Amount is read from that txn.\nReturns the 32-byte escrow_id = sha256(payer_bytes + intent_hash + round_bytes).","events":[],"recommendations":{}},{"name":"release","args":[{"type":"byte[]","name":"escrow_id"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"SLA passed — transfer ALGO to provider.\nOracle calls this when HTTP 200 + latency within threshold. Emits b\"x402:axiom:RELEASE\" in inner-txn note.","events":[],"recommendations":{}},{"name":"refund","args":[{"type":"byte[]","name":"escrow_id"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"SLA failed or deadline passed — return ALGO to payer.\nEmits b\"x402:axiom:REFUND\" in inner-txn note.","events":[],"recommendations":{}},{"name":"quarantine","args":[{"type":"byte[]","name":"escrow_id"},{"type":"uint64","name":"reason_code"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Flag a payment for human review.\nTriggered by: anomaly detector, burst check, DNA drift > threshold. reason_code:   1 = anomaly detected   2 = burst rate exceeded   3 = DNA drift > 0.30   4 = reputation too low   5 = semantic mismatch   6 = policy expired","events":[],"recommendations":{}},{"name":"admin_resolve","args":[{"type":"byte[]","name":"escrow_id"},{"type":"uint64","name":"approved"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Operator approves or rejects a quarantined payment.\napproved=1 -> release()  |  approved=0 -> refund() Must be called by the contract creator (org admin).","events":[],"recommendations":{}},{"name":"get_escrow_status","args":[{"type":"byte[]","name":"escrow_id"}],"returns":{"type":"uint64"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Returns status: 0=open, 1=released, 2=refunded, 3=quarantined.","events":[],"recommendations":{}},{"name":"get_escrow_amount","args":[{"type":"byte[]","name":"escrow_id"}],"returns":{"type":"uint64"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Returns the escrowed microALGO amount.","events":[],"recommendations":{}}],"arcs":[22,28],"desc":"\n    Payment hub — ALL ALGO for AXIOM-governed payments flows through here.\n\n    Deposit -> (oracle evaluates SLA) -> Release  OR  Refund\n                                      -> Quarantine -> admin_resolve\n\n    Note prefix on all inner transactions: b\"x402:axiom:\"\n    ","networks":{},"state":{"schema":{"global":{"ints":3,"bytes":0},"local":{"ints":0,"bytes":0}},"keys":{"global":{"policy_vault_app_id":{"keyType":"AVMString","valueType":"AVMUint64","key":"cG9saWN5X3ZhdWx0X2FwcF9pZA=="},"reputation_ledger_app_id":{"keyType":"AVMString","valueType":"AVMUint64","key":"cmVwdXRhdGlvbl9sZWRnZXJfYXBwX2lk"},"dna_registry_app_id":{"keyType":"AVMString","valueType":"AVMUint64","key":"ZG5hX3JlZ2lzdHJ5X2FwcF9pZA=="}},"local":{},"box":{}},"maps":{"global":{},"local":{},"box":{"escrows":{"keyType":"AVMBytes","valueType":"EscrowRecord","prefix":"ZXM6"},"blocked_log":{"keyType":"AVMBytes","valueType":"BlockedEvent","prefix":"Ymw6"}}}},"bareActions":{"create":[],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[266],"errorMessage":"Address length is 32 bytes"},{"pc":[424,833,1004],"errorMessage":"escrow already settled"},{"pc":[625],"errorMessage":"escrow is not quarantined"},{"pc":[842],"errorMessage":"escrow is quarantined"},{"pc":[416,615,762,796,825,996],"errorMessage":"escrow not found"},{"pc":[421,429,435,441,448,455,620,630,636,642,649,656,766,800,830,838,849,855,898,904,911,1001,1011,1037,1043,1085,1091,1098],"errorMessage":"index out of bounds"},{"pc":[245],"errorMessage":"intent_hash must be 32 bytes"},{"pc":[209,338,359,380,580,744,778],"errorMessage":"invalid array length header"},{"pc":[407,816,986],"errorMessage":"invalid escrow_id"},{"pc":[216,345,366,387,587,751,785],"errorMessage":"invalid number of bytes for arc4.dynamic_array<arc4.uint8>"},{"pc":[203],"errorMessage":"invalid number of bytes for arc4.static_array<arc4.uint8, 32>"},{"pc":[187,227,236,398,599],"errorMessage":"invalid number of bytes for arc4.uint64"},{"pc":[1029],"errorMessage":"not yet overdue and caller is not oracle"},{"pc":[606],"errorMessage":"only admin can resolve"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"events":[]} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


// Type definitions for ARC-56 structs

export type BlockedEvent = {
  escrowId: Uint8Array,
  reasonCode: bigint,
  blockedRound: bigint,
  payer: string
}


/**
 * Converts the ABI tuple representation of a BlockedEvent to the struct representation
 */
export function BlockedEventFromTuple(abiTuple: [Uint8Array, bigint, bigint, string]) {
  return getABIStructFromABITuple(abiTuple, APP_SPEC.structs.BlockedEvent, APP_SPEC.structs) as BlockedEvent
}

export type EscrowRecord = {
  payer: string,
  provider: string,
  amount: bigint,
  deadlineRound: bigint,
  quarantineFlag: bigint,
  consensusRequired: bigint,
  intentHash: Uint8Array,
  status: bigint
}


/**
 * Converts the ABI tuple representation of a EscrowRecord to the struct representation
 */
export function EscrowRecordFromTuple(abiTuple: [string, string, bigint, bigint, bigint, bigint, Uint8Array, bigint]) {
  return getABIStructFromABITuple(abiTuple, APP_SPEC.structs.EscrowRecord, APP_SPEC.structs) as EscrowRecord
}

/**
 * The argument types for the SentinelEscrow contract
 */
export type SentinelEscrowArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'create(uint64)void': {
      policyVaultAppId: bigint | number
    }
    'deposit(address,byte[],uint64,uint64)byte[]': {
      provider: string
      intentHash: Uint8Array
      deadlineRounds: bigint | number
      requiresConsensus: bigint | number
    }
    'release(byte[])void': {
      escrowId: Uint8Array
    }
    'refund(byte[])void': {
      escrowId: Uint8Array
    }
    'quarantine(byte[],uint64)void': {
      escrowId: Uint8Array
      reasonCode: bigint | number
    }
    'admin_resolve(byte[],uint64)void': {
      escrowId: Uint8Array
      approved: bigint | number
    }
    'get_escrow_status(byte[])uint64': {
      escrowId: Uint8Array
    }
    'get_escrow_amount(byte[])uint64': {
      escrowId: Uint8Array
    }
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'create(uint64)void': [policyVaultAppId: bigint | number]
    'deposit(address,byte[],uint64,uint64)byte[]': [provider: string, intentHash: Uint8Array, deadlineRounds: bigint | number, requiresConsensus: bigint | number]
    'release(byte[])void': [escrowId: Uint8Array]
    'refund(byte[])void': [escrowId: Uint8Array]
    'quarantine(byte[],uint64)void': [escrowId: Uint8Array, reasonCode: bigint | number]
    'admin_resolve(byte[],uint64)void': [escrowId: Uint8Array, approved: bigint | number]
    'get_escrow_status(byte[])uint64': [escrowId: Uint8Array]
    'get_escrow_amount(byte[])uint64': [escrowId: Uint8Array]
  }
}

/**
 * The return type for each method
 */
export type SentinelEscrowReturns = {
  'create(uint64)void': void
  'deposit(address,byte[],uint64,uint64)byte[]': Uint8Array
  'release(byte[])void': void
  'refund(byte[])void': void
  'quarantine(byte[],uint64)void': void
  'admin_resolve(byte[],uint64)void': void
  'get_escrow_status(byte[])uint64': bigint
  'get_escrow_amount(byte[])uint64': bigint
}

/**
 * Defines the types of available calls and state of the SentinelEscrow smart contract.
 */
export type SentinelEscrowTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'create(uint64)void' | 'create', {
      argsObj: SentinelEscrowArgs['obj']['create(uint64)void']
      argsTuple: SentinelEscrowArgs['tuple']['create(uint64)void']
      returns: SentinelEscrowReturns['create(uint64)void']
    }>
    & Record<'deposit(address,byte[],uint64,uint64)byte[]' | 'deposit', {
      argsObj: SentinelEscrowArgs['obj']['deposit(address,byte[],uint64,uint64)byte[]']
      argsTuple: SentinelEscrowArgs['tuple']['deposit(address,byte[],uint64,uint64)byte[]']
      returns: SentinelEscrowReturns['deposit(address,byte[],uint64,uint64)byte[]']
    }>
    & Record<'release(byte[])void' | 'release', {
      argsObj: SentinelEscrowArgs['obj']['release(byte[])void']
      argsTuple: SentinelEscrowArgs['tuple']['release(byte[])void']
      returns: SentinelEscrowReturns['release(byte[])void']
    }>
    & Record<'refund(byte[])void' | 'refund', {
      argsObj: SentinelEscrowArgs['obj']['refund(byte[])void']
      argsTuple: SentinelEscrowArgs['tuple']['refund(byte[])void']
      returns: SentinelEscrowReturns['refund(byte[])void']
    }>
    & Record<'quarantine(byte[],uint64)void' | 'quarantine', {
      argsObj: SentinelEscrowArgs['obj']['quarantine(byte[],uint64)void']
      argsTuple: SentinelEscrowArgs['tuple']['quarantine(byte[],uint64)void']
      returns: SentinelEscrowReturns['quarantine(byte[],uint64)void']
    }>
    & Record<'admin_resolve(byte[],uint64)void' | 'admin_resolve', {
      argsObj: SentinelEscrowArgs['obj']['admin_resolve(byte[],uint64)void']
      argsTuple: SentinelEscrowArgs['tuple']['admin_resolve(byte[],uint64)void']
      returns: SentinelEscrowReturns['admin_resolve(byte[],uint64)void']
    }>
    & Record<'get_escrow_status(byte[])uint64' | 'get_escrow_status', {
      argsObj: SentinelEscrowArgs['obj']['get_escrow_status(byte[])uint64']
      argsTuple: SentinelEscrowArgs['tuple']['get_escrow_status(byte[])uint64']
      returns: SentinelEscrowReturns['get_escrow_status(byte[])uint64']
    }>
    & Record<'get_escrow_amount(byte[])uint64' | 'get_escrow_amount', {
      argsObj: SentinelEscrowArgs['obj']['get_escrow_amount(byte[])uint64']
      argsTuple: SentinelEscrowArgs['tuple']['get_escrow_amount(byte[])uint64']
      returns: SentinelEscrowReturns['get_escrow_amount(byte[])uint64']
    }>
  /**
   * Defines the shape of the state of the application.
   */
  state: {
    global: {
      keys: {
        policyVaultAppId: bigint
        reputationLedgerAppId: bigint
        dnaRegistryAppId: bigint
      }
      maps: {}
    }
    box: {
      keys: {}
      maps: {
        escrows: Map<Uint8Array | string, EscrowRecord>
        blockedLog: Map<Uint8Array | string, BlockedEvent>
      }
    }
  }
}

/**
 * Defines the possible abi call signatures.
 */
export type SentinelEscrowSignatures = keyof SentinelEscrowTypes['methods']
/**
 * Defines the possible abi call signatures for methods that return a non-void value.
 */
export type SentinelEscrowNonVoidMethodSignatures = keyof SentinelEscrowTypes['methods'] extends infer T ? T extends keyof SentinelEscrowTypes['methods'] ? MethodReturn<T> extends void ? never : T  : never : never
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the SentinelEscrow smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends SentinelEscrowSignatures> = SentinelEscrowTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the SentinelEscrow smart contract to the method's return type
 */
export type MethodReturn<TSignature extends SentinelEscrowSignatures> = SentinelEscrowTypes['methods'][TSignature]['returns']

/**
 * Defines the shape of the keyed global state of the application.
 */
export type GlobalKeysState = SentinelEscrowTypes['state']['global']['keys']

/**
 * Defines the shape of the keyed box state of the application.
 */
export type BoxKeysState = SentinelEscrowTypes['state']['box']['keys']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the SentinelEscrow smart contract
 */
export abstract class SentinelEscrowParamsFactory {
  /**
   * Constructs a no op call for the deposit(address,byte[],uint64,uint64)byte[] ABI method
   *
  * Accept an incoming ALGO payment and open an escrow record.
  The caller must attach a PaymentTransaction (inner group) whose receiver is this app's address.  Amount is read from that txn.
  Returns the 32-byte escrow_id = sha256(payer_bytes + intent_hash + round_bytes).

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static deposit(params: CallParams<SentinelEscrowArgs['obj']['deposit(address,byte[],uint64,uint64)byte[]'] | SentinelEscrowArgs['tuple']['deposit(address,byte[],uint64,uint64)byte[]']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'deposit(address,byte[],uint64,uint64)byte[]' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.provider, params.args.intentHash, params.args.deadlineRounds, params.args.requiresConsensus],
    }
  }
  /**
   * Constructs a no op call for the release(byte[])void ABI method
   *
  * SLA passed — transfer ALGO to provider.
  Oracle calls this when HTTP 200 + latency within threshold. Emits b"x402:axiom:RELEASE" in inner-txn note.

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static release(params: CallParams<SentinelEscrowArgs['obj']['release(byte[])void'] | SentinelEscrowArgs['tuple']['release(byte[])void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'release(byte[])void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.escrowId],
    }
  }
  /**
   * Constructs a no op call for the refund(byte[])void ABI method
   *
  * SLA failed or deadline passed — return ALGO to payer.
  Emits b"x402:axiom:REFUND" in inner-txn note.

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static refund(params: CallParams<SentinelEscrowArgs['obj']['refund(byte[])void'] | SentinelEscrowArgs['tuple']['refund(byte[])void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'refund(byte[])void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.escrowId],
    }
  }
  /**
   * Constructs a no op call for the quarantine(byte[],uint64)void ABI method
   *
  * Flag a payment for human review.
  Triggered by: anomaly detector, burst check, DNA drift > threshold. reason_code:   1 = anomaly detected   2 = burst rate exceeded   3 = DNA drift > 0.30   4 = reputation too low   5 = semantic mismatch   6 = policy expired

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static quarantine(params: CallParams<SentinelEscrowArgs['obj']['quarantine(byte[],uint64)void'] | SentinelEscrowArgs['tuple']['quarantine(byte[],uint64)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'quarantine(byte[],uint64)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.escrowId, params.args.reasonCode],
    }
  }
  /**
   * Constructs a no op call for the admin_resolve(byte[],uint64)void ABI method
   *
  * Operator approves or rejects a quarantined payment.
  approved=1 -> release()  |  approved=0 -> refund() Must be called by the contract creator (org admin).

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static adminResolve(params: CallParams<SentinelEscrowArgs['obj']['admin_resolve(byte[],uint64)void'] | SentinelEscrowArgs['tuple']['admin_resolve(byte[],uint64)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'admin_resolve(byte[],uint64)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.escrowId, params.args.approved],
    }
  }
  /**
   * Constructs a no op call for the get_escrow_status(byte[])uint64 ABI method
   *
   * Returns status: 0=open, 1=released, 2=refunded, 3=quarantined.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static getEscrowStatus(params: CallParams<SentinelEscrowArgs['obj']['get_escrow_status(byte[])uint64'] | SentinelEscrowArgs['tuple']['get_escrow_status(byte[])uint64']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'get_escrow_status(byte[])uint64' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.escrowId],
    }
  }
  /**
   * Constructs a no op call for the get_escrow_amount(byte[])uint64 ABI method
   *
   * Returns the escrowed microALGO amount.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static getEscrowAmount(params: CallParams<SentinelEscrowArgs['obj']['get_escrow_amount(byte[])uint64'] | SentinelEscrowArgs['tuple']['get_escrow_amount(byte[])uint64']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'get_escrow_amount(byte[])uint64' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.escrowId],
    }
  }
}

/**
 * A client to make calls to the SentinelEscrow smart contract
 */
export class SentinelEscrowClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `SentinelEscrowClient`
   *
   * @param appClient An `AppClient` instance which has been created with the SentinelEscrow app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `SentinelEscrowClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Checks for decode errors on the given return value and maps the return value to the return type for the given method
   * @returns The typed return value or undefined if there was no value
   */
  decodeReturnValue<TSignature extends SentinelEscrowNonVoidMethodSignatures>(method: TSignature, returnValue: ABIReturn | undefined) {
    return returnValue !== undefined ? getArc56ReturnValue<MethodReturn<TSignature>>(returnValue, this.appClient.getABIMethod(method), APP_SPEC.structs) : undefined
  }

  /**
   * Returns a new `SentinelEscrowClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<SentinelEscrowClient> {
    return new SentinelEscrowClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `SentinelEscrowClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<SentinelEscrowClient> {
    return new SentinelEscrowClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Makes a clear_state call to an existing instance of the SentinelEscrow smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the SentinelEscrow smart contract using the `deposit(address,byte[],uint64,uint64)byte[]` ABI method.
     *
    * Accept an incoming ALGO payment and open an escrow record.
    The caller must attach a PaymentTransaction (inner group) whose receiver is this app's address.  Amount is read from that txn.
    Returns the 32-byte escrow_id = sha256(payer_bytes + intent_hash + round_bytes).

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    deposit: (params: CallParams<SentinelEscrowArgs['obj']['deposit(address,byte[],uint64,uint64)byte[]'] | SentinelEscrowArgs['tuple']['deposit(address,byte[],uint64,uint64)byte[]']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(SentinelEscrowParamsFactory.deposit(params))
    },

    /**
     * Makes a call to the SentinelEscrow smart contract using the `release(byte[])void` ABI method.
     *
    * SLA passed — transfer ALGO to provider.
    Oracle calls this when HTTP 200 + latency within threshold. Emits b"x402:axiom:RELEASE" in inner-txn note.

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    release: (params: CallParams<SentinelEscrowArgs['obj']['release(byte[])void'] | SentinelEscrowArgs['tuple']['release(byte[])void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(SentinelEscrowParamsFactory.release(params))
    },

    /**
     * Makes a call to the SentinelEscrow smart contract using the `refund(byte[])void` ABI method.
     *
    * SLA failed or deadline passed — return ALGO to payer.
    Emits b"x402:axiom:REFUND" in inner-txn note.

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    refund: (params: CallParams<SentinelEscrowArgs['obj']['refund(byte[])void'] | SentinelEscrowArgs['tuple']['refund(byte[])void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(SentinelEscrowParamsFactory.refund(params))
    },

    /**
     * Makes a call to the SentinelEscrow smart contract using the `quarantine(byte[],uint64)void` ABI method.
     *
    * Flag a payment for human review.
    Triggered by: anomaly detector, burst check, DNA drift > threshold. reason_code:   1 = anomaly detected   2 = burst rate exceeded   3 = DNA drift > 0.30   4 = reputation too low   5 = semantic mismatch   6 = policy expired

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    quarantine: (params: CallParams<SentinelEscrowArgs['obj']['quarantine(byte[],uint64)void'] | SentinelEscrowArgs['tuple']['quarantine(byte[],uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(SentinelEscrowParamsFactory.quarantine(params))
    },

    /**
     * Makes a call to the SentinelEscrow smart contract using the `admin_resolve(byte[],uint64)void` ABI method.
     *
    * Operator approves or rejects a quarantined payment.
    approved=1 -> release()  |  approved=0 -> refund() Must be called by the contract creator (org admin).

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    adminResolve: (params: CallParams<SentinelEscrowArgs['obj']['admin_resolve(byte[],uint64)void'] | SentinelEscrowArgs['tuple']['admin_resolve(byte[],uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(SentinelEscrowParamsFactory.adminResolve(params))
    },

    /**
     * Makes a call to the SentinelEscrow smart contract using the `get_escrow_status(byte[])uint64` ABI method.
     *
     * Returns status: 0=open, 1=released, 2=refunded, 3=quarantined.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    getEscrowStatus: (params: CallParams<SentinelEscrowArgs['obj']['get_escrow_status(byte[])uint64'] | SentinelEscrowArgs['tuple']['get_escrow_status(byte[])uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(SentinelEscrowParamsFactory.getEscrowStatus(params))
    },

    /**
     * Makes a call to the SentinelEscrow smart contract using the `get_escrow_amount(byte[])uint64` ABI method.
     *
     * Returns the escrowed microALGO amount.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    getEscrowAmount: (params: CallParams<SentinelEscrowArgs['obj']['get_escrow_amount(byte[])uint64'] | SentinelEscrowArgs['tuple']['get_escrow_amount(byte[])uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(SentinelEscrowParamsFactory.getEscrowAmount(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Makes a clear_state call to an existing instance of the SentinelEscrow smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the SentinelEscrow smart contract using the `deposit(address,byte[],uint64,uint64)byte[]` ABI method.
     *
    * Accept an incoming ALGO payment and open an escrow record.
    The caller must attach a PaymentTransaction (inner group) whose receiver is this app's address.  Amount is read from that txn.
    Returns the 32-byte escrow_id = sha256(payer_bytes + intent_hash + round_bytes).

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    deposit: (params: CallParams<SentinelEscrowArgs['obj']['deposit(address,byte[],uint64,uint64)byte[]'] | SentinelEscrowArgs['tuple']['deposit(address,byte[],uint64,uint64)byte[]']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(SentinelEscrowParamsFactory.deposit(params))
    },

    /**
     * Makes a call to the SentinelEscrow smart contract using the `release(byte[])void` ABI method.
     *
    * SLA passed — transfer ALGO to provider.
    Oracle calls this when HTTP 200 + latency within threshold. Emits b"x402:axiom:RELEASE" in inner-txn note.

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    release: (params: CallParams<SentinelEscrowArgs['obj']['release(byte[])void'] | SentinelEscrowArgs['tuple']['release(byte[])void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(SentinelEscrowParamsFactory.release(params))
    },

    /**
     * Makes a call to the SentinelEscrow smart contract using the `refund(byte[])void` ABI method.
     *
    * SLA failed or deadline passed — return ALGO to payer.
    Emits b"x402:axiom:REFUND" in inner-txn note.

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    refund: (params: CallParams<SentinelEscrowArgs['obj']['refund(byte[])void'] | SentinelEscrowArgs['tuple']['refund(byte[])void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(SentinelEscrowParamsFactory.refund(params))
    },

    /**
     * Makes a call to the SentinelEscrow smart contract using the `quarantine(byte[],uint64)void` ABI method.
     *
    * Flag a payment for human review.
    Triggered by: anomaly detector, burst check, DNA drift > threshold. reason_code:   1 = anomaly detected   2 = burst rate exceeded   3 = DNA drift > 0.30   4 = reputation too low   5 = semantic mismatch   6 = policy expired

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    quarantine: (params: CallParams<SentinelEscrowArgs['obj']['quarantine(byte[],uint64)void'] | SentinelEscrowArgs['tuple']['quarantine(byte[],uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(SentinelEscrowParamsFactory.quarantine(params))
    },

    /**
     * Makes a call to the SentinelEscrow smart contract using the `admin_resolve(byte[],uint64)void` ABI method.
     *
    * Operator approves or rejects a quarantined payment.
    approved=1 -> release()  |  approved=0 -> refund() Must be called by the contract creator (org admin).

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    adminResolve: (params: CallParams<SentinelEscrowArgs['obj']['admin_resolve(byte[],uint64)void'] | SentinelEscrowArgs['tuple']['admin_resolve(byte[],uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(SentinelEscrowParamsFactory.adminResolve(params))
    },

    /**
     * Makes a call to the SentinelEscrow smart contract using the `get_escrow_status(byte[])uint64` ABI method.
     *
     * Returns status: 0=open, 1=released, 2=refunded, 3=quarantined.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    getEscrowStatus: (params: CallParams<SentinelEscrowArgs['obj']['get_escrow_status(byte[])uint64'] | SentinelEscrowArgs['tuple']['get_escrow_status(byte[])uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(SentinelEscrowParamsFactory.getEscrowStatus(params))
    },

    /**
     * Makes a call to the SentinelEscrow smart contract using the `get_escrow_amount(byte[])uint64` ABI method.
     *
     * Returns the escrowed microALGO amount.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    getEscrowAmount: (params: CallParams<SentinelEscrowArgs['obj']['get_escrow_amount(byte[])uint64'] | SentinelEscrowArgs['tuple']['get_escrow_amount(byte[])uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(SentinelEscrowParamsFactory.getEscrowAmount(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Makes a clear_state call to an existing instance of the SentinelEscrow smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the SentinelEscrow smart contract using the `deposit(address,byte[],uint64,uint64)byte[]` ABI method.
     *
    * Accept an incoming ALGO payment and open an escrow record.
    The caller must attach a PaymentTransaction (inner group) whose receiver is this app's address.  Amount is read from that txn.
    Returns the 32-byte escrow_id = sha256(payer_bytes + intent_hash + round_bytes).

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    deposit: async (params: CallParams<SentinelEscrowArgs['obj']['deposit(address,byte[],uint64,uint64)byte[]'] | SentinelEscrowArgs['tuple']['deposit(address,byte[],uint64,uint64)byte[]']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(SentinelEscrowParamsFactory.deposit(params))
      return {...result, return: result.return as unknown as (undefined | SentinelEscrowReturns['deposit(address,byte[],uint64,uint64)byte[]'])}
    },

    /**
     * Makes a call to the SentinelEscrow smart contract using the `release(byte[])void` ABI method.
     *
    * SLA passed — transfer ALGO to provider.
    Oracle calls this when HTTP 200 + latency within threshold. Emits b"x402:axiom:RELEASE" in inner-txn note.

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    release: async (params: CallParams<SentinelEscrowArgs['obj']['release(byte[])void'] | SentinelEscrowArgs['tuple']['release(byte[])void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(SentinelEscrowParamsFactory.release(params))
      return {...result, return: result.return as unknown as (undefined | SentinelEscrowReturns['release(byte[])void'])}
    },

    /**
     * Makes a call to the SentinelEscrow smart contract using the `refund(byte[])void` ABI method.
     *
    * SLA failed or deadline passed — return ALGO to payer.
    Emits b"x402:axiom:REFUND" in inner-txn note.

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    refund: async (params: CallParams<SentinelEscrowArgs['obj']['refund(byte[])void'] | SentinelEscrowArgs['tuple']['refund(byte[])void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(SentinelEscrowParamsFactory.refund(params))
      return {...result, return: result.return as unknown as (undefined | SentinelEscrowReturns['refund(byte[])void'])}
    },

    /**
     * Makes a call to the SentinelEscrow smart contract using the `quarantine(byte[],uint64)void` ABI method.
     *
    * Flag a payment for human review.
    Triggered by: anomaly detector, burst check, DNA drift > threshold. reason_code:   1 = anomaly detected   2 = burst rate exceeded   3 = DNA drift > 0.30   4 = reputation too low   5 = semantic mismatch   6 = policy expired

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    quarantine: async (params: CallParams<SentinelEscrowArgs['obj']['quarantine(byte[],uint64)void'] | SentinelEscrowArgs['tuple']['quarantine(byte[],uint64)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(SentinelEscrowParamsFactory.quarantine(params))
      return {...result, return: result.return as unknown as (undefined | SentinelEscrowReturns['quarantine(byte[],uint64)void'])}
    },

    /**
     * Makes a call to the SentinelEscrow smart contract using the `admin_resolve(byte[],uint64)void` ABI method.
     *
    * Operator approves or rejects a quarantined payment.
    approved=1 -> release()  |  approved=0 -> refund() Must be called by the contract creator (org admin).

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    adminResolve: async (params: CallParams<SentinelEscrowArgs['obj']['admin_resolve(byte[],uint64)void'] | SentinelEscrowArgs['tuple']['admin_resolve(byte[],uint64)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(SentinelEscrowParamsFactory.adminResolve(params))
      return {...result, return: result.return as unknown as (undefined | SentinelEscrowReturns['admin_resolve(byte[],uint64)void'])}
    },

    /**
     * Makes a call to the SentinelEscrow smart contract using the `get_escrow_status(byte[])uint64` ABI method.
     *
     * Returns status: 0=open, 1=released, 2=refunded, 3=quarantined.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    getEscrowStatus: async (params: CallParams<SentinelEscrowArgs['obj']['get_escrow_status(byte[])uint64'] | SentinelEscrowArgs['tuple']['get_escrow_status(byte[])uint64']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(SentinelEscrowParamsFactory.getEscrowStatus(params))
      return {...result, return: result.return as unknown as (undefined | SentinelEscrowReturns['get_escrow_status(byte[])uint64'])}
    },

    /**
     * Makes a call to the SentinelEscrow smart contract using the `get_escrow_amount(byte[])uint64` ABI method.
     *
     * Returns the escrowed microALGO amount.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    getEscrowAmount: async (params: CallParams<SentinelEscrowArgs['obj']['get_escrow_amount(byte[])uint64'] | SentinelEscrowArgs['tuple']['get_escrow_amount(byte[])uint64']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(SentinelEscrowParamsFactory.getEscrowAmount(params))
      return {...result, return: result.return as unknown as (undefined | SentinelEscrowReturns['get_escrow_amount(byte[])uint64'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new SentinelEscrowClient(this.appClient.clone(params))
  }

  /**
   * Methods to access state for the current SentinelEscrow app
   */
  state = {
    /**
     * Methods to access global state for the current SentinelEscrow app
     */
    global: {
      /**
       * Get all current keyed values from global state
       */
      getAll: async (): Promise<Partial<Expand<GlobalKeysState>>> => {
        const result = await this.appClient.state.global.getAll()
        return {
          policyVaultAppId: result.policy_vault_app_id,
          reputationLedgerAppId: result.reputation_ledger_app_id,
          dnaRegistryAppId: result.dna_registry_app_id,
        }
      },
      /**
       * Get the current value of the policy_vault_app_id key in global state
       */
      policyVaultAppId: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("policy_vault_app_id")) as bigint | undefined },
      /**
       * Get the current value of the reputation_ledger_app_id key in global state
       */
      reputationLedgerAppId: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("reputation_ledger_app_id")) as bigint | undefined },
      /**
       * Get the current value of the dna_registry_app_id key in global state
       */
      dnaRegistryAppId: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("dna_registry_app_id")) as bigint | undefined },
    },
    /**
     * Methods to access box state for the current SentinelEscrow app
     */
    box: {
      /**
       * Get all current keyed values from box state
       */
      getAll: async (): Promise<Partial<Expand<BoxKeysState>>> => {
        const result = await this.appClient.state.box.getAll()
        return {
        }
      },
      /**
       * Get values from the escrows map in box state
       */
      escrows: {
        /**
         * Get all current values of the escrows map in box state
         */
        getMap: async (): Promise<Map<Uint8Array, EscrowRecord>> => { return (await this.appClient.state.box.getMap("escrows")) as Map<Uint8Array, EscrowRecord> },
        /**
         * Get a current value of the escrows map by key from box state
         */
        value: async (key: Uint8Array | string): Promise<EscrowRecord | undefined> => { return await this.appClient.state.box.getMapValue("escrows", key) as EscrowRecord | undefined },
      },
      /**
       * Get values from the blocked_log map in box state
       */
      blockedLog: {
        /**
         * Get all current values of the blocked_log map in box state
         */
        getMap: async (): Promise<Map<Uint8Array, BlockedEvent>> => { return (await this.appClient.state.box.getMap("blocked_log")) as Map<Uint8Array, BlockedEvent> },
        /**
         * Get a current value of the blocked_log map by key from box state
         */
        value: async (key: Uint8Array | string): Promise<BlockedEvent | undefined> => { return await this.appClient.state.box.getMapValue("blocked_log", key) as BlockedEvent | undefined },
      },
    },
  }

  public newGroup(): SentinelEscrowComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a deposit(address,byte[],uint64,uint64)byte[] method call against the SentinelEscrow contract
       */
      deposit(params: CallParams<SentinelEscrowArgs['obj']['deposit(address,byte[],uint64,uint64)byte[]'] | SentinelEscrowArgs['tuple']['deposit(address,byte[],uint64,uint64)byte[]']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.deposit(params)))
        resultMappers.push((v) => client.decodeReturnValue('deposit(address,byte[],uint64,uint64)byte[]', v))
        return this
      },
      /**
       * Add a release(byte[])void method call against the SentinelEscrow contract
       */
      release(params: CallParams<SentinelEscrowArgs['obj']['release(byte[])void'] | SentinelEscrowArgs['tuple']['release(byte[])void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.release(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a refund(byte[])void method call against the SentinelEscrow contract
       */
      refund(params: CallParams<SentinelEscrowArgs['obj']['refund(byte[])void'] | SentinelEscrowArgs['tuple']['refund(byte[])void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.refund(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a quarantine(byte[],uint64)void method call against the SentinelEscrow contract
       */
      quarantine(params: CallParams<SentinelEscrowArgs['obj']['quarantine(byte[],uint64)void'] | SentinelEscrowArgs['tuple']['quarantine(byte[],uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.quarantine(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a admin_resolve(byte[],uint64)void method call against the SentinelEscrow contract
       */
      adminResolve(params: CallParams<SentinelEscrowArgs['obj']['admin_resolve(byte[],uint64)void'] | SentinelEscrowArgs['tuple']['admin_resolve(byte[],uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.adminResolve(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a get_escrow_status(byte[])uint64 method call against the SentinelEscrow contract
       */
      getEscrowStatus(params: CallParams<SentinelEscrowArgs['obj']['get_escrow_status(byte[])uint64'] | SentinelEscrowArgs['tuple']['get_escrow_status(byte[])uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.getEscrowStatus(params)))
        resultMappers.push((v) => client.decodeReturnValue('get_escrow_status(byte[])uint64', v))
        return this
      },
      /**
       * Add a get_escrow_amount(byte[])uint64 method call against the SentinelEscrow contract
       */
      getEscrowAmount(params: CallParams<SentinelEscrowArgs['obj']['get_escrow_amount(byte[])uint64'] | SentinelEscrowArgs['tuple']['get_escrow_amount(byte[])uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.getEscrowAmount(params)))
        resultMappers.push((v) => client.decodeReturnValue('get_escrow_amount(byte[])uint64', v))
        return this
      },
      /**
       * Add a clear state call to the SentinelEscrow contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as SentinelEscrowComposer
  }
}
export type SentinelEscrowComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the deposit(address,byte[],uint64,uint64)byte[] ABI method.
   *
  * Accept an incoming ALGO payment and open an escrow record.
  The caller must attach a PaymentTransaction (inner group) whose receiver is this app's address.  Amount is read from that txn.
  Returns the 32-byte escrow_id = sha256(payer_bytes + intent_hash + round_bytes).

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  deposit(params?: CallParams<SentinelEscrowArgs['obj']['deposit(address,byte[],uint64,uint64)byte[]'] | SentinelEscrowArgs['tuple']['deposit(address,byte[],uint64,uint64)byte[]']>): SentinelEscrowComposer<[...TReturns, SentinelEscrowReturns['deposit(address,byte[],uint64,uint64)byte[]'] | undefined]>

  /**
   * Calls the release(byte[])void ABI method.
   *
  * SLA passed — transfer ALGO to provider.
  Oracle calls this when HTTP 200 + latency within threshold. Emits b"x402:axiom:RELEASE" in inner-txn note.

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  release(params?: CallParams<SentinelEscrowArgs['obj']['release(byte[])void'] | SentinelEscrowArgs['tuple']['release(byte[])void']>): SentinelEscrowComposer<[...TReturns, SentinelEscrowReturns['release(byte[])void'] | undefined]>

  /**
   * Calls the refund(byte[])void ABI method.
   *
  * SLA failed or deadline passed — return ALGO to payer.
  Emits b"x402:axiom:REFUND" in inner-txn note.

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  refund(params?: CallParams<SentinelEscrowArgs['obj']['refund(byte[])void'] | SentinelEscrowArgs['tuple']['refund(byte[])void']>): SentinelEscrowComposer<[...TReturns, SentinelEscrowReturns['refund(byte[])void'] | undefined]>

  /**
   * Calls the quarantine(byte[],uint64)void ABI method.
   *
  * Flag a payment for human review.
  Triggered by: anomaly detector, burst check, DNA drift > threshold. reason_code:   1 = anomaly detected   2 = burst rate exceeded   3 = DNA drift > 0.30   4 = reputation too low   5 = semantic mismatch   6 = policy expired

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  quarantine(params?: CallParams<SentinelEscrowArgs['obj']['quarantine(byte[],uint64)void'] | SentinelEscrowArgs['tuple']['quarantine(byte[],uint64)void']>): SentinelEscrowComposer<[...TReturns, SentinelEscrowReturns['quarantine(byte[],uint64)void'] | undefined]>

  /**
   * Calls the admin_resolve(byte[],uint64)void ABI method.
   *
  * Operator approves or rejects a quarantined payment.
  approved=1 -> release()  |  approved=0 -> refund() Must be called by the contract creator (org admin).

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  adminResolve(params?: CallParams<SentinelEscrowArgs['obj']['admin_resolve(byte[],uint64)void'] | SentinelEscrowArgs['tuple']['admin_resolve(byte[],uint64)void']>): SentinelEscrowComposer<[...TReturns, SentinelEscrowReturns['admin_resolve(byte[],uint64)void'] | undefined]>

  /**
   * Calls the get_escrow_status(byte[])uint64 ABI method.
   *
   * Returns status: 0=open, 1=released, 2=refunded, 3=quarantined.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  getEscrowStatus(params?: CallParams<SentinelEscrowArgs['obj']['get_escrow_status(byte[])uint64'] | SentinelEscrowArgs['tuple']['get_escrow_status(byte[])uint64']>): SentinelEscrowComposer<[...TReturns, SentinelEscrowReturns['get_escrow_status(byte[])uint64'] | undefined]>

  /**
   * Calls the get_escrow_amount(byte[])uint64 ABI method.
   *
   * Returns the escrowed microALGO amount.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  getEscrowAmount(params?: CallParams<SentinelEscrowArgs['obj']['get_escrow_amount(byte[])uint64'] | SentinelEscrowArgs['tuple']['get_escrow_amount(byte[])uint64']>): SentinelEscrowComposer<[...TReturns, SentinelEscrowReturns['get_escrow_amount(byte[])uint64'] | undefined]>

  /**
   * Makes a clear_state call to an existing instance of the SentinelEscrow smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): SentinelEscrowComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): SentinelEscrowComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<SentinelEscrowComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<SentinelEscrowComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<SentinelEscrowComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<SentinelEscrowComposerResults<TReturns>>
}
export type SentinelEscrowComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

