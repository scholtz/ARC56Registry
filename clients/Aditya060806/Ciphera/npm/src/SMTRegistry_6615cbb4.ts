/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"SMTRegistry","structs":{},"methods":[{"name":"create","args":[{"type":"byte[]","name":"initial_root"}],"returns":{"type":"void"},"actions":{"create":["NoOp"],"call":[]},"readonly":false,"desc":"Initialize with the genesis SMT root (32 zero bytes = empty tree).","events":[],"recommendations":{}},{"name":"update_root","args":[{"type":"byte[]","name":"new_root"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Update SMT root. Issuer-only.\nCalled after every credential issuance or revocation. 1 Algorand tx = all revocations reflected immediately.","events":[],"recommendations":{}},{"name":"get_root","args":[],"returns":{"type":"byte[]"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"desc":"Return current SMT root for client-side SMT inclusion proof verification.","events":[],"recommendations":{}},{"name":"get_last_updated_round","args":[],"returns":{"type":"uint64"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"desc":"Algorand round at which root was last updated.","events":[],"recommendations":{}},{"name":"get_update_count","args":[],"returns":{"type":"uint64"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"desc":"Total root updates (audit counter).","events":[],"recommendations":{}},{"name":"update_issuer","args":[{"type":"address","name":"new_issuer"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Transfer issuer role. Issuer-only.","events":[],"recommendations":{}}],"arcs":[22,28],"desc":"On-chain store for the Sparse Merkle Tree root.","networks":{},"state":{"schema":{"global":{"ints":2,"bytes":2},"local":{"ints":0,"bytes":0}},"keys":{"global":{"issuer":{"keyType":"AVMString","valueType":"address","key":"aXNzdWVy"},"smt_root":{"keyType":"AVMString","valueType":"AVMBytes","key":"c210X3Jvb3Q="},"last_updated_round":{"keyType":"AVMString","valueType":"AVMUint64","key":"bGFzdF91cGRhdGVkX3JvdW5k"},"update_count":{"keyType":"AVMString","valueType":"AVMUint64","key":"dXBkYXRlX2NvdW50"}},"local":{},"box":{}},"maps":{"global":{},"local":{},"box":{}}},"bareActions":{"create":[],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[201,279],"errorMessage":"Only issuer"},{"pc":[199,277],"errorMessage":"check self.issuer exists"},{"pc":[242],"errorMessage":"check self.last_updated_round exists"},{"pc":[223],"errorMessage":"check self.smt_root exists"},{"pc":[212,254],"errorMessage":"check self.update_count exists"},{"pc":[151,183],"errorMessage":"invalid array length header"},{"pc":[158,190],"errorMessage":"invalid number of bytes for arc4.dynamic_array<arc4.uint8>"},{"pc":[271],"errorMessage":"invalid number of bytes for arc4.static_array<arc4.uint8, 32>"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"events":[]} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


/**
 * The argument types for the SmtRegistry contract
 */
export type SmtRegistryArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'create(byte[])void': {
      initialRoot: Uint8Array
    }
    'update_root(byte[])void': {
      newRoot: Uint8Array
    }
    'get_root()byte[]': Record<string, never>
    'get_last_updated_round()uint64': Record<string, never>
    'get_update_count()uint64': Record<string, never>
    'update_issuer(address)void': {
      newIssuer: string
    }
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'create(byte[])void': [initialRoot: Uint8Array]
    'update_root(byte[])void': [newRoot: Uint8Array]
    'get_root()byte[]': []
    'get_last_updated_round()uint64': []
    'get_update_count()uint64': []
    'update_issuer(address)void': [newIssuer: string]
  }
}

/**
 * The return type for each method
 */
export type SmtRegistryReturns = {
  'create(byte[])void': void
  'update_root(byte[])void': void
  'get_root()byte[]': Uint8Array
  'get_last_updated_round()uint64': bigint
  'get_update_count()uint64': bigint
  'update_issuer(address)void': void
}

/**
 * Defines the types of available calls and state of the SmtRegistry smart contract.
 */
export type SmtRegistryTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'create(byte[])void' | 'create', {
      argsObj: SmtRegistryArgs['obj']['create(byte[])void']
      argsTuple: SmtRegistryArgs['tuple']['create(byte[])void']
      returns: SmtRegistryReturns['create(byte[])void']
    }>
    & Record<'update_root(byte[])void' | 'update_root', {
      argsObj: SmtRegistryArgs['obj']['update_root(byte[])void']
      argsTuple: SmtRegistryArgs['tuple']['update_root(byte[])void']
      returns: SmtRegistryReturns['update_root(byte[])void']
    }>
    & Record<'get_root()byte[]' | 'get_root', {
      argsObj: SmtRegistryArgs['obj']['get_root()byte[]']
      argsTuple: SmtRegistryArgs['tuple']['get_root()byte[]']
      returns: SmtRegistryReturns['get_root()byte[]']
    }>
    & Record<'get_last_updated_round()uint64' | 'get_last_updated_round', {
      argsObj: SmtRegistryArgs['obj']['get_last_updated_round()uint64']
      argsTuple: SmtRegistryArgs['tuple']['get_last_updated_round()uint64']
      returns: SmtRegistryReturns['get_last_updated_round()uint64']
    }>
    & Record<'get_update_count()uint64' | 'get_update_count', {
      argsObj: SmtRegistryArgs['obj']['get_update_count()uint64']
      argsTuple: SmtRegistryArgs['tuple']['get_update_count()uint64']
      returns: SmtRegistryReturns['get_update_count()uint64']
    }>
    & Record<'update_issuer(address)void' | 'update_issuer', {
      argsObj: SmtRegistryArgs['obj']['update_issuer(address)void']
      argsTuple: SmtRegistryArgs['tuple']['update_issuer(address)void']
      returns: SmtRegistryReturns['update_issuer(address)void']
    }>
  /**
   * Defines the shape of the state of the application.
   */
  state: {
    global: {
      keys: {
        issuer: string
        smtRoot: BinaryState
        lastUpdatedRound: bigint
        updateCount: bigint
      }
      maps: {}
    }
  }
}

/**
 * Defines the possible abi call signatures.
 */
export type SmtRegistrySignatures = keyof SmtRegistryTypes['methods']
/**
 * Defines the possible abi call signatures for methods that return a non-void value.
 */
export type SmtRegistryNonVoidMethodSignatures = keyof SmtRegistryTypes['methods'] extends infer T ? T extends keyof SmtRegistryTypes['methods'] ? MethodReturn<T> extends void ? never : T  : never : never
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the SmtRegistry smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends SmtRegistrySignatures> = SmtRegistryTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the SmtRegistry smart contract to the method's return type
 */
export type MethodReturn<TSignature extends SmtRegistrySignatures> = SmtRegistryTypes['methods'][TSignature]['returns']

/**
 * Defines the shape of the keyed global state of the application.
 */
export type GlobalKeysState = SmtRegistryTypes['state']['global']['keys']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the SmtRegistry smart contract
 */
export abstract class SmtRegistryParamsFactory {
  /**
   * Constructs a no op call for the update_root(byte[])void ABI method
   *
  * Update SMT root. Issuer-only.
  Called after every credential issuance or revocation. 1 Algorand tx = all revocations reflected immediately.

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static updateRoot(params: CallParams<SmtRegistryArgs['obj']['update_root(byte[])void'] | SmtRegistryArgs['tuple']['update_root(byte[])void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'update_root(byte[])void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.newRoot],
    }
  }
  /**
   * Constructs a no op call for the get_root()byte[] ABI method
   *
   * Return current SMT root for client-side SMT inclusion proof verification.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static getRoot(params: CallParams<SmtRegistryArgs['obj']['get_root()byte[]'] | SmtRegistryArgs['tuple']['get_root()byte[]']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'get_root()byte[]' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the get_last_updated_round()uint64 ABI method
   *
   * Algorand round at which root was last updated.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static getLastUpdatedRound(params: CallParams<SmtRegistryArgs['obj']['get_last_updated_round()uint64'] | SmtRegistryArgs['tuple']['get_last_updated_round()uint64']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'get_last_updated_round()uint64' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the get_update_count()uint64 ABI method
   *
   * Total root updates (audit counter).
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static getUpdateCount(params: CallParams<SmtRegistryArgs['obj']['get_update_count()uint64'] | SmtRegistryArgs['tuple']['get_update_count()uint64']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'get_update_count()uint64' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the update_issuer(address)void ABI method
   *
   * Transfer issuer role. Issuer-only.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static updateIssuer(params: CallParams<SmtRegistryArgs['obj']['update_issuer(address)void'] | SmtRegistryArgs['tuple']['update_issuer(address)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'update_issuer(address)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.newIssuer],
    }
  }
}

/**
 * A client to make calls to the SMTRegistry smart contract
 */
export class SmtRegistryClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `SmtRegistryClient`
   *
   * @param appClient An `AppClient` instance which has been created with the SmtRegistry app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `SmtRegistryClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Checks for decode errors on the given return value and maps the return value to the return type for the given method
   * @returns The typed return value or undefined if there was no value
   */
  decodeReturnValue<TSignature extends SmtRegistryNonVoidMethodSignatures>(method: TSignature, returnValue: ABIReturn | undefined) {
    return returnValue !== undefined ? getArc56ReturnValue<MethodReturn<TSignature>>(returnValue, this.appClient.getABIMethod(method), APP_SPEC.structs) : undefined
  }

  /**
   * Returns a new `SmtRegistryClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<SmtRegistryClient> {
    return new SmtRegistryClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `SmtRegistryClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<SmtRegistryClient> {
    return new SmtRegistryClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Makes a clear_state call to an existing instance of the SMTRegistry smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the SMTRegistry smart contract using the `update_root(byte[])void` ABI method.
     *
    * Update SMT root. Issuer-only.
    Called after every credential issuance or revocation. 1 Algorand tx = all revocations reflected immediately.

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    updateRoot: (params: CallParams<SmtRegistryArgs['obj']['update_root(byte[])void'] | SmtRegistryArgs['tuple']['update_root(byte[])void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(SmtRegistryParamsFactory.updateRoot(params))
    },

    /**
     * Makes a call to the SMTRegistry smart contract using the `get_root()byte[]` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * Return current SMT root for client-side SMT inclusion proof verification.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    getRoot: (params: CallParams<SmtRegistryArgs['obj']['get_root()byte[]'] | SmtRegistryArgs['tuple']['get_root()byte[]']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(SmtRegistryParamsFactory.getRoot(params))
    },

    /**
     * Makes a call to the SMTRegistry smart contract using the `get_last_updated_round()uint64` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * Algorand round at which root was last updated.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    getLastUpdatedRound: (params: CallParams<SmtRegistryArgs['obj']['get_last_updated_round()uint64'] | SmtRegistryArgs['tuple']['get_last_updated_round()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(SmtRegistryParamsFactory.getLastUpdatedRound(params))
    },

    /**
     * Makes a call to the SMTRegistry smart contract using the `get_update_count()uint64` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * Total root updates (audit counter).
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    getUpdateCount: (params: CallParams<SmtRegistryArgs['obj']['get_update_count()uint64'] | SmtRegistryArgs['tuple']['get_update_count()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(SmtRegistryParamsFactory.getUpdateCount(params))
    },

    /**
     * Makes a call to the SMTRegistry smart contract using the `update_issuer(address)void` ABI method.
     *
     * Transfer issuer role. Issuer-only.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    updateIssuer: (params: CallParams<SmtRegistryArgs['obj']['update_issuer(address)void'] | SmtRegistryArgs['tuple']['update_issuer(address)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(SmtRegistryParamsFactory.updateIssuer(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Makes a clear_state call to an existing instance of the SMTRegistry smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the SMTRegistry smart contract using the `update_root(byte[])void` ABI method.
     *
    * Update SMT root. Issuer-only.
    Called after every credential issuance or revocation. 1 Algorand tx = all revocations reflected immediately.

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    updateRoot: (params: CallParams<SmtRegistryArgs['obj']['update_root(byte[])void'] | SmtRegistryArgs['tuple']['update_root(byte[])void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(SmtRegistryParamsFactory.updateRoot(params))
    },

    /**
     * Makes a call to the SMTRegistry smart contract using the `get_root()byte[]` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * Return current SMT root for client-side SMT inclusion proof verification.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    getRoot: (params: CallParams<SmtRegistryArgs['obj']['get_root()byte[]'] | SmtRegistryArgs['tuple']['get_root()byte[]']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(SmtRegistryParamsFactory.getRoot(params))
    },

    /**
     * Makes a call to the SMTRegistry smart contract using the `get_last_updated_round()uint64` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * Algorand round at which root was last updated.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    getLastUpdatedRound: (params: CallParams<SmtRegistryArgs['obj']['get_last_updated_round()uint64'] | SmtRegistryArgs['tuple']['get_last_updated_round()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(SmtRegistryParamsFactory.getLastUpdatedRound(params))
    },

    /**
     * Makes a call to the SMTRegistry smart contract using the `get_update_count()uint64` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * Total root updates (audit counter).
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    getUpdateCount: (params: CallParams<SmtRegistryArgs['obj']['get_update_count()uint64'] | SmtRegistryArgs['tuple']['get_update_count()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(SmtRegistryParamsFactory.getUpdateCount(params))
    },

    /**
     * Makes a call to the SMTRegistry smart contract using the `update_issuer(address)void` ABI method.
     *
     * Transfer issuer role. Issuer-only.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    updateIssuer: (params: CallParams<SmtRegistryArgs['obj']['update_issuer(address)void'] | SmtRegistryArgs['tuple']['update_issuer(address)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(SmtRegistryParamsFactory.updateIssuer(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Makes a clear_state call to an existing instance of the SMTRegistry smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the SMTRegistry smart contract using the `update_root(byte[])void` ABI method.
     *
    * Update SMT root. Issuer-only.
    Called after every credential issuance or revocation. 1 Algorand tx = all revocations reflected immediately.

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    updateRoot: async (params: CallParams<SmtRegistryArgs['obj']['update_root(byte[])void'] | SmtRegistryArgs['tuple']['update_root(byte[])void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(SmtRegistryParamsFactory.updateRoot(params))
      return {...result, return: result.return as unknown as (undefined | SmtRegistryReturns['update_root(byte[])void'])}
    },

    /**
     * Makes a call to the SMTRegistry smart contract using the `get_root()byte[]` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * Return current SMT root for client-side SMT inclusion proof verification.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    getRoot: async (params: CallParams<SmtRegistryArgs['obj']['get_root()byte[]'] | SmtRegistryArgs['tuple']['get_root()byte[]']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(SmtRegistryParamsFactory.getRoot(params))
      return {...result, return: result.return as unknown as (undefined | SmtRegistryReturns['get_root()byte[]'])}
    },

    /**
     * Makes a call to the SMTRegistry smart contract using the `get_last_updated_round()uint64` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * Algorand round at which root was last updated.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    getLastUpdatedRound: async (params: CallParams<SmtRegistryArgs['obj']['get_last_updated_round()uint64'] | SmtRegistryArgs['tuple']['get_last_updated_round()uint64']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(SmtRegistryParamsFactory.getLastUpdatedRound(params))
      return {...result, return: result.return as unknown as (undefined | SmtRegistryReturns['get_last_updated_round()uint64'])}
    },

    /**
     * Makes a call to the SMTRegistry smart contract using the `get_update_count()uint64` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * Total root updates (audit counter).
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    getUpdateCount: async (params: CallParams<SmtRegistryArgs['obj']['get_update_count()uint64'] | SmtRegistryArgs['tuple']['get_update_count()uint64']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(SmtRegistryParamsFactory.getUpdateCount(params))
      return {...result, return: result.return as unknown as (undefined | SmtRegistryReturns['get_update_count()uint64'])}
    },

    /**
     * Makes a call to the SMTRegistry smart contract using the `update_issuer(address)void` ABI method.
     *
     * Transfer issuer role. Issuer-only.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    updateIssuer: async (params: CallParams<SmtRegistryArgs['obj']['update_issuer(address)void'] | SmtRegistryArgs['tuple']['update_issuer(address)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(SmtRegistryParamsFactory.updateIssuer(params))
      return {...result, return: result.return as unknown as (undefined | SmtRegistryReturns['update_issuer(address)void'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new SmtRegistryClient(this.appClient.clone(params))
  }

  /**
   * Makes a readonly (simulated) call to the SMTRegistry smart contract using the `get_root()byte[]` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * Return current SMT root for client-side SMT inclusion proof verification.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async getRoot(params: CallParams<SmtRegistryArgs['obj']['get_root()byte[]'] | SmtRegistryArgs['tuple']['get_root()byte[]']> = {args: []}) {
    const result = await this.appClient.send.call(SmtRegistryParamsFactory.getRoot(params))
    return result.return as unknown as SmtRegistryReturns['get_root()byte[]']
  }

  /**
   * Makes a readonly (simulated) call to the SMTRegistry smart contract using the `get_last_updated_round()uint64` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * Algorand round at which root was last updated.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async getLastUpdatedRound(params: CallParams<SmtRegistryArgs['obj']['get_last_updated_round()uint64'] | SmtRegistryArgs['tuple']['get_last_updated_round()uint64']> = {args: []}) {
    const result = await this.appClient.send.call(SmtRegistryParamsFactory.getLastUpdatedRound(params))
    return result.return as unknown as SmtRegistryReturns['get_last_updated_round()uint64']
  }

  /**
   * Makes a readonly (simulated) call to the SMTRegistry smart contract using the `get_update_count()uint64` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * Total root updates (audit counter).
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async getUpdateCount(params: CallParams<SmtRegistryArgs['obj']['get_update_count()uint64'] | SmtRegistryArgs['tuple']['get_update_count()uint64']> = {args: []}) {
    const result = await this.appClient.send.call(SmtRegistryParamsFactory.getUpdateCount(params))
    return result.return as unknown as SmtRegistryReturns['get_update_count()uint64']
  }

  /**
   * Methods to access state for the current SMTRegistry app
   */
  state = {
    /**
     * Methods to access global state for the current SMTRegistry app
     */
    global: {
      /**
       * Get all current keyed values from global state
       */
      getAll: async (): Promise<Partial<Expand<GlobalKeysState>>> => {
        const result = await this.appClient.state.global.getAll()
        return {
          issuer: result.issuer,
          smtRoot: new BinaryStateValue(result.smt_root),
          lastUpdatedRound: result.last_updated_round,
          updateCount: result.update_count,
        }
      },
      /**
       * Get the current value of the issuer key in global state
       */
      issuer: async (): Promise<string | undefined> => { return (await this.appClient.state.global.getValue("issuer")) as string | undefined },
      /**
       * Get the current value of the smt_root key in global state
       */
      smtRoot: async (): Promise<BinaryState> => { return new BinaryStateValue((await this.appClient.state.global.getValue("smt_root")) as Uint8Array | undefined) },
      /**
       * Get the current value of the last_updated_round key in global state
       */
      lastUpdatedRound: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("last_updated_round")) as bigint | undefined },
      /**
       * Get the current value of the update_count key in global state
       */
      updateCount: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("update_count")) as bigint | undefined },
    },
  }

  public newGroup(): SmtRegistryComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a update_root(byte[])void method call against the SMTRegistry contract
       */
      updateRoot(params: CallParams<SmtRegistryArgs['obj']['update_root(byte[])void'] | SmtRegistryArgs['tuple']['update_root(byte[])void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.updateRoot(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a get_root()byte[] method call against the SMTRegistry contract
       */
      getRoot(params: CallParams<SmtRegistryArgs['obj']['get_root()byte[]'] | SmtRegistryArgs['tuple']['get_root()byte[]']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.getRoot(params)))
        resultMappers.push((v) => client.decodeReturnValue('get_root()byte[]', v))
        return this
      },
      /**
       * Add a get_last_updated_round()uint64 method call against the SMTRegistry contract
       */
      getLastUpdatedRound(params: CallParams<SmtRegistryArgs['obj']['get_last_updated_round()uint64'] | SmtRegistryArgs['tuple']['get_last_updated_round()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.getLastUpdatedRound(params)))
        resultMappers.push((v) => client.decodeReturnValue('get_last_updated_round()uint64', v))
        return this
      },
      /**
       * Add a get_update_count()uint64 method call against the SMTRegistry contract
       */
      getUpdateCount(params: CallParams<SmtRegistryArgs['obj']['get_update_count()uint64'] | SmtRegistryArgs['tuple']['get_update_count()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.getUpdateCount(params)))
        resultMappers.push((v) => client.decodeReturnValue('get_update_count()uint64', v))
        return this
      },
      /**
       * Add a update_issuer(address)void method call against the SMTRegistry contract
       */
      updateIssuer(params: CallParams<SmtRegistryArgs['obj']['update_issuer(address)void'] | SmtRegistryArgs['tuple']['update_issuer(address)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.updateIssuer(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a clear state call to the SMTRegistry contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as SmtRegistryComposer
  }
}
export type SmtRegistryComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the update_root(byte[])void ABI method.
   *
  * Update SMT root. Issuer-only.
  Called after every credential issuance or revocation. 1 Algorand tx = all revocations reflected immediately.

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  updateRoot(params?: CallParams<SmtRegistryArgs['obj']['update_root(byte[])void'] | SmtRegistryArgs['tuple']['update_root(byte[])void']>): SmtRegistryComposer<[...TReturns, SmtRegistryReturns['update_root(byte[])void'] | undefined]>

  /**
   * Calls the get_root()byte[] ABI method.
   *
   * Return current SMT root for client-side SMT inclusion proof verification.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  getRoot(params?: CallParams<SmtRegistryArgs['obj']['get_root()byte[]'] | SmtRegistryArgs['tuple']['get_root()byte[]']>): SmtRegistryComposer<[...TReturns, SmtRegistryReturns['get_root()byte[]'] | undefined]>

  /**
   * Calls the get_last_updated_round()uint64 ABI method.
   *
   * Algorand round at which root was last updated.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  getLastUpdatedRound(params?: CallParams<SmtRegistryArgs['obj']['get_last_updated_round()uint64'] | SmtRegistryArgs['tuple']['get_last_updated_round()uint64']>): SmtRegistryComposer<[...TReturns, SmtRegistryReturns['get_last_updated_round()uint64'] | undefined]>

  /**
   * Calls the get_update_count()uint64 ABI method.
   *
   * Total root updates (audit counter).
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  getUpdateCount(params?: CallParams<SmtRegistryArgs['obj']['get_update_count()uint64'] | SmtRegistryArgs['tuple']['get_update_count()uint64']>): SmtRegistryComposer<[...TReturns, SmtRegistryReturns['get_update_count()uint64'] | undefined]>

  /**
   * Calls the update_issuer(address)void ABI method.
   *
   * Transfer issuer role. Issuer-only.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  updateIssuer(params?: CallParams<SmtRegistryArgs['obj']['update_issuer(address)void'] | SmtRegistryArgs['tuple']['update_issuer(address)void']>): SmtRegistryComposer<[...TReturns, SmtRegistryReturns['update_issuer(address)void'] | undefined]>

  /**
   * Makes a clear_state call to an existing instance of the SMTRegistry smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): SmtRegistryComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): SmtRegistryComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<SmtRegistryComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<SmtRegistryComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<SmtRegistryComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<SmtRegistryComposerResults<TReturns>>
}
export type SmtRegistryComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

