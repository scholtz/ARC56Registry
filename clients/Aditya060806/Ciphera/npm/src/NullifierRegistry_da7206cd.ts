/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"NullifierRegistry","structs":{},"methods":[{"name":"create","args":[],"returns":{"type":"void"},"actions":{"create":["NoOp"],"call":[]},"readonly":false,"desc":"Initialize — caller becomes the issuer.","events":[],"recommendations":{}},{"name":"register","args":[{"type":"byte[]","name":"nullifier"},{"type":"address","name":"wallet"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Register nullifier → wallet. Issuer-only.\nFails if nullifier already registered (one Aadhaar per wallet).","events":[],"recommendations":{}},{"name":"is_registered","args":[{"type":"byte[]","name":"nullifier"}],"returns":{"type":"bool"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"desc":"Check if a nullifier has been registered.","events":[],"recommendations":{}},{"name":"get_wallet","args":[{"type":"byte[]","name":"nullifier"}],"returns":{"type":"byte[]"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"desc":"Retrieve wallet bytes for a nullifier (court order lookup).","events":[],"recommendations":{}},{"name":"invalidate","args":[{"type":"byte[]","name":"nullifier"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Remove nullifier registration — issuer-only, used on revocation.","events":[],"recommendations":{}},{"name":"get_total_registered","args":[],"returns":{"type":"uint64"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"desc":"Total registered credentials.","events":[],"recommendations":{}},{"name":"update_issuer","args":[{"type":"address","name":"new_issuer"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Transfer issuer role. Issuer-only.","events":[],"recommendations":{}}],"arcs":[22,28],"desc":"Registry mapping nullifier_hash → wallet for Sybil prevention.","networks":{},"state":{"schema":{"global":{"ints":1,"bytes":1},"local":{"ints":0,"bytes":0}},"keys":{"global":{"issuer":{"keyType":"AVMString","valueType":"address","key":"aXNzdWVy"},"total_registered":{"keyType":"AVMString","valueType":"AVMUint64","key":"dG90YWxfcmVnaXN0ZXJlZA=="}},"local":{},"box":{}},"maps":{"global":{},"local":{},"box":{"nullifiers":{"keyType":"AVMBytes","valueType":"AVMBytes","prefix":"bnVsXw=="}}}},"bareActions":{"create":[],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[178],"errorMessage":"Nullifier already registered"},{"pc":[253,301],"errorMessage":"Nullifier not found"},{"pc":[168,293,348],"errorMessage":"Only issuer"},{"pc":[166,291,346],"errorMessage":"check self.issuer exists"},{"pc":[187,307,314,325],"errorMessage":"check self.total_registered exists"},{"pc":[142,200,235,275],"errorMessage":"invalid array length header"},{"pc":[149,207,242,282],"errorMessage":"invalid number of bytes for arc4.dynamic_array<arc4.uint8>"},{"pc":[160,340],"errorMessage":"invalid number of bytes for arc4.static_array<arc4.uint8, 32>"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"events":[]} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


/**
 * The argument types for the NullifierRegistry contract
 */
export type NullifierRegistryArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'create()void': Record<string, never>
    'register(byte[],address)void': {
      nullifier: Uint8Array
      wallet: string
    }
    'is_registered(byte[])bool': {
      nullifier: Uint8Array
    }
    'get_wallet(byte[])byte[]': {
      nullifier: Uint8Array
    }
    'invalidate(byte[])void': {
      nullifier: Uint8Array
    }
    'get_total_registered()uint64': Record<string, never>
    'update_issuer(address)void': {
      newIssuer: string
    }
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'create()void': []
    'register(byte[],address)void': [nullifier: Uint8Array, wallet: string]
    'is_registered(byte[])bool': [nullifier: Uint8Array]
    'get_wallet(byte[])byte[]': [nullifier: Uint8Array]
    'invalidate(byte[])void': [nullifier: Uint8Array]
    'get_total_registered()uint64': []
    'update_issuer(address)void': [newIssuer: string]
  }
}

/**
 * The return type for each method
 */
export type NullifierRegistryReturns = {
  'create()void': void
  'register(byte[],address)void': void
  'is_registered(byte[])bool': boolean
  'get_wallet(byte[])byte[]': Uint8Array
  'invalidate(byte[])void': void
  'get_total_registered()uint64': bigint
  'update_issuer(address)void': void
}

/**
 * Defines the types of available calls and state of the NullifierRegistry smart contract.
 */
export type NullifierRegistryTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'create()void' | 'create', {
      argsObj: NullifierRegistryArgs['obj']['create()void']
      argsTuple: NullifierRegistryArgs['tuple']['create()void']
      returns: NullifierRegistryReturns['create()void']
    }>
    & Record<'register(byte[],address)void' | 'register', {
      argsObj: NullifierRegistryArgs['obj']['register(byte[],address)void']
      argsTuple: NullifierRegistryArgs['tuple']['register(byte[],address)void']
      returns: NullifierRegistryReturns['register(byte[],address)void']
    }>
    & Record<'is_registered(byte[])bool' | 'is_registered', {
      argsObj: NullifierRegistryArgs['obj']['is_registered(byte[])bool']
      argsTuple: NullifierRegistryArgs['tuple']['is_registered(byte[])bool']
      returns: NullifierRegistryReturns['is_registered(byte[])bool']
    }>
    & Record<'get_wallet(byte[])byte[]' | 'get_wallet', {
      argsObj: NullifierRegistryArgs['obj']['get_wallet(byte[])byte[]']
      argsTuple: NullifierRegistryArgs['tuple']['get_wallet(byte[])byte[]']
      returns: NullifierRegistryReturns['get_wallet(byte[])byte[]']
    }>
    & Record<'invalidate(byte[])void' | 'invalidate', {
      argsObj: NullifierRegistryArgs['obj']['invalidate(byte[])void']
      argsTuple: NullifierRegistryArgs['tuple']['invalidate(byte[])void']
      returns: NullifierRegistryReturns['invalidate(byte[])void']
    }>
    & Record<'get_total_registered()uint64' | 'get_total_registered', {
      argsObj: NullifierRegistryArgs['obj']['get_total_registered()uint64']
      argsTuple: NullifierRegistryArgs['tuple']['get_total_registered()uint64']
      returns: NullifierRegistryReturns['get_total_registered()uint64']
    }>
    & Record<'update_issuer(address)void' | 'update_issuer', {
      argsObj: NullifierRegistryArgs['obj']['update_issuer(address)void']
      argsTuple: NullifierRegistryArgs['tuple']['update_issuer(address)void']
      returns: NullifierRegistryReturns['update_issuer(address)void']
    }>
  /**
   * Defines the shape of the state of the application.
   */
  state: {
    global: {
      keys: {
        issuer: string
        totalRegistered: bigint
      }
      maps: {}
    }
    box: {
      keys: {}
      maps: {
        nullifiers: Map<Uint8Array | string, Uint8Array>
      }
    }
  }
}

/**
 * Defines the possible abi call signatures.
 */
export type NullifierRegistrySignatures = keyof NullifierRegistryTypes['methods']
/**
 * Defines the possible abi call signatures for methods that return a non-void value.
 */
export type NullifierRegistryNonVoidMethodSignatures = keyof NullifierRegistryTypes['methods'] extends infer T ? T extends keyof NullifierRegistryTypes['methods'] ? MethodReturn<T> extends void ? never : T  : never : never
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the NullifierRegistry smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends NullifierRegistrySignatures> = NullifierRegistryTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the NullifierRegistry smart contract to the method's return type
 */
export type MethodReturn<TSignature extends NullifierRegistrySignatures> = NullifierRegistryTypes['methods'][TSignature]['returns']

/**
 * Defines the shape of the keyed global state of the application.
 */
export type GlobalKeysState = NullifierRegistryTypes['state']['global']['keys']

/**
 * Defines the shape of the keyed box state of the application.
 */
export type BoxKeysState = NullifierRegistryTypes['state']['box']['keys']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the NullifierRegistry smart contract
 */
export abstract class NullifierRegistryParamsFactory {
  /**
   * Constructs a no op call for the register(byte[],address)void ABI method
   *
  * Register nullifier → wallet. Issuer-only.
  Fails if nullifier already registered (one Aadhaar per wallet).

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static register(params: CallParams<NullifierRegistryArgs['obj']['register(byte[],address)void'] | NullifierRegistryArgs['tuple']['register(byte[],address)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'register(byte[],address)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.nullifier, params.args.wallet],
    }
  }
  /**
   * Constructs a no op call for the is_registered(byte[])bool ABI method
   *
   * Check if a nullifier has been registered.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static isRegistered(params: CallParams<NullifierRegistryArgs['obj']['is_registered(byte[])bool'] | NullifierRegistryArgs['tuple']['is_registered(byte[])bool']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'is_registered(byte[])bool' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.nullifier],
    }
  }
  /**
   * Constructs a no op call for the get_wallet(byte[])byte[] ABI method
   *
   * Retrieve wallet bytes for a nullifier (court order lookup).
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static getWallet(params: CallParams<NullifierRegistryArgs['obj']['get_wallet(byte[])byte[]'] | NullifierRegistryArgs['tuple']['get_wallet(byte[])byte[]']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'get_wallet(byte[])byte[]' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.nullifier],
    }
  }
  /**
   * Constructs a no op call for the invalidate(byte[])void ABI method
   *
   * Remove nullifier registration — issuer-only, used on revocation.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static invalidate(params: CallParams<NullifierRegistryArgs['obj']['invalidate(byte[])void'] | NullifierRegistryArgs['tuple']['invalidate(byte[])void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'invalidate(byte[])void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.nullifier],
    }
  }
  /**
   * Constructs a no op call for the get_total_registered()uint64 ABI method
   *
   * Total registered credentials.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static getTotalRegistered(params: CallParams<NullifierRegistryArgs['obj']['get_total_registered()uint64'] | NullifierRegistryArgs['tuple']['get_total_registered()uint64']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'get_total_registered()uint64' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the update_issuer(address)void ABI method
   *
   * Transfer issuer role. Issuer-only.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static updateIssuer(params: CallParams<NullifierRegistryArgs['obj']['update_issuer(address)void'] | NullifierRegistryArgs['tuple']['update_issuer(address)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'update_issuer(address)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.newIssuer],
    }
  }
}

/**
 * A client to make calls to the NullifierRegistry smart contract
 */
export class NullifierRegistryClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `NullifierRegistryClient`
   *
   * @param appClient An `AppClient` instance which has been created with the NullifierRegistry app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `NullifierRegistryClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Checks for decode errors on the given return value and maps the return value to the return type for the given method
   * @returns The typed return value or undefined if there was no value
   */
  decodeReturnValue<TSignature extends NullifierRegistryNonVoidMethodSignatures>(method: TSignature, returnValue: ABIReturn | undefined) {
    return returnValue !== undefined ? getArc56ReturnValue<MethodReturn<TSignature>>(returnValue, this.appClient.getABIMethod(method), APP_SPEC.structs) : undefined
  }

  /**
   * Returns a new `NullifierRegistryClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<NullifierRegistryClient> {
    return new NullifierRegistryClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `NullifierRegistryClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<NullifierRegistryClient> {
    return new NullifierRegistryClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Makes a clear_state call to an existing instance of the NullifierRegistry smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the NullifierRegistry smart contract using the `register(byte[],address)void` ABI method.
     *
    * Register nullifier → wallet. Issuer-only.
    Fails if nullifier already registered (one Aadhaar per wallet).

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    register: (params: CallParams<NullifierRegistryArgs['obj']['register(byte[],address)void'] | NullifierRegistryArgs['tuple']['register(byte[],address)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(NullifierRegistryParamsFactory.register(params))
    },

    /**
     * Makes a call to the NullifierRegistry smart contract using the `is_registered(byte[])bool` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * Check if a nullifier has been registered.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    isRegistered: (params: CallParams<NullifierRegistryArgs['obj']['is_registered(byte[])bool'] | NullifierRegistryArgs['tuple']['is_registered(byte[])bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(NullifierRegistryParamsFactory.isRegistered(params))
    },

    /**
     * Makes a call to the NullifierRegistry smart contract using the `get_wallet(byte[])byte[]` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * Retrieve wallet bytes for a nullifier (court order lookup).
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    getWallet: (params: CallParams<NullifierRegistryArgs['obj']['get_wallet(byte[])byte[]'] | NullifierRegistryArgs['tuple']['get_wallet(byte[])byte[]']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(NullifierRegistryParamsFactory.getWallet(params))
    },

    /**
     * Makes a call to the NullifierRegistry smart contract using the `invalidate(byte[])void` ABI method.
     *
     * Remove nullifier registration — issuer-only, used on revocation.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    invalidate: (params: CallParams<NullifierRegistryArgs['obj']['invalidate(byte[])void'] | NullifierRegistryArgs['tuple']['invalidate(byte[])void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(NullifierRegistryParamsFactory.invalidate(params))
    },

    /**
     * Makes a call to the NullifierRegistry smart contract using the `get_total_registered()uint64` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * Total registered credentials.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    getTotalRegistered: (params: CallParams<NullifierRegistryArgs['obj']['get_total_registered()uint64'] | NullifierRegistryArgs['tuple']['get_total_registered()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(NullifierRegistryParamsFactory.getTotalRegistered(params))
    },

    /**
     * Makes a call to the NullifierRegistry smart contract using the `update_issuer(address)void` ABI method.
     *
     * Transfer issuer role. Issuer-only.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    updateIssuer: (params: CallParams<NullifierRegistryArgs['obj']['update_issuer(address)void'] | NullifierRegistryArgs['tuple']['update_issuer(address)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(NullifierRegistryParamsFactory.updateIssuer(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Makes a clear_state call to an existing instance of the NullifierRegistry smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the NullifierRegistry smart contract using the `register(byte[],address)void` ABI method.
     *
    * Register nullifier → wallet. Issuer-only.
    Fails if nullifier already registered (one Aadhaar per wallet).

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    register: (params: CallParams<NullifierRegistryArgs['obj']['register(byte[],address)void'] | NullifierRegistryArgs['tuple']['register(byte[],address)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(NullifierRegistryParamsFactory.register(params))
    },

    /**
     * Makes a call to the NullifierRegistry smart contract using the `is_registered(byte[])bool` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * Check if a nullifier has been registered.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    isRegistered: (params: CallParams<NullifierRegistryArgs['obj']['is_registered(byte[])bool'] | NullifierRegistryArgs['tuple']['is_registered(byte[])bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(NullifierRegistryParamsFactory.isRegistered(params))
    },

    /**
     * Makes a call to the NullifierRegistry smart contract using the `get_wallet(byte[])byte[]` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * Retrieve wallet bytes for a nullifier (court order lookup).
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    getWallet: (params: CallParams<NullifierRegistryArgs['obj']['get_wallet(byte[])byte[]'] | NullifierRegistryArgs['tuple']['get_wallet(byte[])byte[]']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(NullifierRegistryParamsFactory.getWallet(params))
    },

    /**
     * Makes a call to the NullifierRegistry smart contract using the `invalidate(byte[])void` ABI method.
     *
     * Remove nullifier registration — issuer-only, used on revocation.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    invalidate: (params: CallParams<NullifierRegistryArgs['obj']['invalidate(byte[])void'] | NullifierRegistryArgs['tuple']['invalidate(byte[])void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(NullifierRegistryParamsFactory.invalidate(params))
    },

    /**
     * Makes a call to the NullifierRegistry smart contract using the `get_total_registered()uint64` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * Total registered credentials.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    getTotalRegistered: (params: CallParams<NullifierRegistryArgs['obj']['get_total_registered()uint64'] | NullifierRegistryArgs['tuple']['get_total_registered()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(NullifierRegistryParamsFactory.getTotalRegistered(params))
    },

    /**
     * Makes a call to the NullifierRegistry smart contract using the `update_issuer(address)void` ABI method.
     *
     * Transfer issuer role. Issuer-only.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    updateIssuer: (params: CallParams<NullifierRegistryArgs['obj']['update_issuer(address)void'] | NullifierRegistryArgs['tuple']['update_issuer(address)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(NullifierRegistryParamsFactory.updateIssuer(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Makes a clear_state call to an existing instance of the NullifierRegistry smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the NullifierRegistry smart contract using the `register(byte[],address)void` ABI method.
     *
    * Register nullifier → wallet. Issuer-only.
    Fails if nullifier already registered (one Aadhaar per wallet).

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    register: async (params: CallParams<NullifierRegistryArgs['obj']['register(byte[],address)void'] | NullifierRegistryArgs['tuple']['register(byte[],address)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(NullifierRegistryParamsFactory.register(params))
      return {...result, return: result.return as unknown as (undefined | NullifierRegistryReturns['register(byte[],address)void'])}
    },

    /**
     * Makes a call to the NullifierRegistry smart contract using the `is_registered(byte[])bool` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * Check if a nullifier has been registered.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    isRegistered: async (params: CallParams<NullifierRegistryArgs['obj']['is_registered(byte[])bool'] | NullifierRegistryArgs['tuple']['is_registered(byte[])bool']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(NullifierRegistryParamsFactory.isRegistered(params))
      return {...result, return: result.return as unknown as (undefined | NullifierRegistryReturns['is_registered(byte[])bool'])}
    },

    /**
     * Makes a call to the NullifierRegistry smart contract using the `get_wallet(byte[])byte[]` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * Retrieve wallet bytes for a nullifier (court order lookup).
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    getWallet: async (params: CallParams<NullifierRegistryArgs['obj']['get_wallet(byte[])byte[]'] | NullifierRegistryArgs['tuple']['get_wallet(byte[])byte[]']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(NullifierRegistryParamsFactory.getWallet(params))
      return {...result, return: result.return as unknown as (undefined | NullifierRegistryReturns['get_wallet(byte[])byte[]'])}
    },

    /**
     * Makes a call to the NullifierRegistry smart contract using the `invalidate(byte[])void` ABI method.
     *
     * Remove nullifier registration — issuer-only, used on revocation.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    invalidate: async (params: CallParams<NullifierRegistryArgs['obj']['invalidate(byte[])void'] | NullifierRegistryArgs['tuple']['invalidate(byte[])void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(NullifierRegistryParamsFactory.invalidate(params))
      return {...result, return: result.return as unknown as (undefined | NullifierRegistryReturns['invalidate(byte[])void'])}
    },

    /**
     * Makes a call to the NullifierRegistry smart contract using the `get_total_registered()uint64` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * Total registered credentials.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    getTotalRegistered: async (params: CallParams<NullifierRegistryArgs['obj']['get_total_registered()uint64'] | NullifierRegistryArgs['tuple']['get_total_registered()uint64']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(NullifierRegistryParamsFactory.getTotalRegistered(params))
      return {...result, return: result.return as unknown as (undefined | NullifierRegistryReturns['get_total_registered()uint64'])}
    },

    /**
     * Makes a call to the NullifierRegistry smart contract using the `update_issuer(address)void` ABI method.
     *
     * Transfer issuer role. Issuer-only.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    updateIssuer: async (params: CallParams<NullifierRegistryArgs['obj']['update_issuer(address)void'] | NullifierRegistryArgs['tuple']['update_issuer(address)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(NullifierRegistryParamsFactory.updateIssuer(params))
      return {...result, return: result.return as unknown as (undefined | NullifierRegistryReturns['update_issuer(address)void'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new NullifierRegistryClient(this.appClient.clone(params))
  }

  /**
   * Makes a readonly (simulated) call to the NullifierRegistry smart contract using the `is_registered(byte[])bool` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * Check if a nullifier has been registered.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async isRegistered(params: CallParams<NullifierRegistryArgs['obj']['is_registered(byte[])bool'] | NullifierRegistryArgs['tuple']['is_registered(byte[])bool']>) {
    const result = await this.appClient.send.call(NullifierRegistryParamsFactory.isRegistered(params))
    return result.return as unknown as NullifierRegistryReturns['is_registered(byte[])bool']
  }

  /**
   * Makes a readonly (simulated) call to the NullifierRegistry smart contract using the `get_wallet(byte[])byte[]` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * Retrieve wallet bytes for a nullifier (court order lookup).
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async getWallet(params: CallParams<NullifierRegistryArgs['obj']['get_wallet(byte[])byte[]'] | NullifierRegistryArgs['tuple']['get_wallet(byte[])byte[]']>) {
    const result = await this.appClient.send.call(NullifierRegistryParamsFactory.getWallet(params))
    return result.return as unknown as NullifierRegistryReturns['get_wallet(byte[])byte[]']
  }

  /**
   * Makes a readonly (simulated) call to the NullifierRegistry smart contract using the `get_total_registered()uint64` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * Total registered credentials.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async getTotalRegistered(params: CallParams<NullifierRegistryArgs['obj']['get_total_registered()uint64'] | NullifierRegistryArgs['tuple']['get_total_registered()uint64']> = {args: []}) {
    const result = await this.appClient.send.call(NullifierRegistryParamsFactory.getTotalRegistered(params))
    return result.return as unknown as NullifierRegistryReturns['get_total_registered()uint64']
  }

  /**
   * Methods to access state for the current NullifierRegistry app
   */
  state = {
    /**
     * Methods to access global state for the current NullifierRegistry app
     */
    global: {
      /**
       * Get all current keyed values from global state
       */
      getAll: async (): Promise<Partial<Expand<GlobalKeysState>>> => {
        const result = await this.appClient.state.global.getAll()
        return {
          issuer: result.issuer,
          totalRegistered: result.total_registered,
        }
      },
      /**
       * Get the current value of the issuer key in global state
       */
      issuer: async (): Promise<string | undefined> => { return (await this.appClient.state.global.getValue("issuer")) as string | undefined },
      /**
       * Get the current value of the total_registered key in global state
       */
      totalRegistered: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("total_registered")) as bigint | undefined },
    },
    /**
     * Methods to access box state for the current NullifierRegistry app
     */
    box: {
      /**
       * Get all current keyed values from box state
       */
      getAll: async (): Promise<Partial<Expand<BoxKeysState>>> => {
        const result = await this.appClient.state.box.getAll()
        return {
        }
      },
      /**
       * Get values from the nullifiers map in box state
       */
      nullifiers: {
        /**
         * Get all current values of the nullifiers map in box state
         */
        getMap: async (): Promise<Map<Uint8Array, Uint8Array>> => { return (await this.appClient.state.box.getMap("nullifiers")) as Map<Uint8Array, Uint8Array> },
        /**
         * Get a current value of the nullifiers map by key from box state
         */
        value: async (key: Uint8Array | string): Promise<Uint8Array | undefined> => { return await this.appClient.state.box.getMapValue("nullifiers", key) as Uint8Array | undefined },
      },
    },
  }

  public newGroup(): NullifierRegistryComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a register(byte[],address)void method call against the NullifierRegistry contract
       */
      register(params: CallParams<NullifierRegistryArgs['obj']['register(byte[],address)void'] | NullifierRegistryArgs['tuple']['register(byte[],address)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.register(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a is_registered(byte[])bool method call against the NullifierRegistry contract
       */
      isRegistered(params: CallParams<NullifierRegistryArgs['obj']['is_registered(byte[])bool'] | NullifierRegistryArgs['tuple']['is_registered(byte[])bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.isRegistered(params)))
        resultMappers.push((v) => client.decodeReturnValue('is_registered(byte[])bool', v))
        return this
      },
      /**
       * Add a get_wallet(byte[])byte[] method call against the NullifierRegistry contract
       */
      getWallet(params: CallParams<NullifierRegistryArgs['obj']['get_wallet(byte[])byte[]'] | NullifierRegistryArgs['tuple']['get_wallet(byte[])byte[]']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.getWallet(params)))
        resultMappers.push((v) => client.decodeReturnValue('get_wallet(byte[])byte[]', v))
        return this
      },
      /**
       * Add a invalidate(byte[])void method call against the NullifierRegistry contract
       */
      invalidate(params: CallParams<NullifierRegistryArgs['obj']['invalidate(byte[])void'] | NullifierRegistryArgs['tuple']['invalidate(byte[])void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.invalidate(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a get_total_registered()uint64 method call against the NullifierRegistry contract
       */
      getTotalRegistered(params: CallParams<NullifierRegistryArgs['obj']['get_total_registered()uint64'] | NullifierRegistryArgs['tuple']['get_total_registered()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.getTotalRegistered(params)))
        resultMappers.push((v) => client.decodeReturnValue('get_total_registered()uint64', v))
        return this
      },
      /**
       * Add a update_issuer(address)void method call against the NullifierRegistry contract
       */
      updateIssuer(params: CallParams<NullifierRegistryArgs['obj']['update_issuer(address)void'] | NullifierRegistryArgs['tuple']['update_issuer(address)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.updateIssuer(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a clear state call to the NullifierRegistry contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as NullifierRegistryComposer
  }
}
export type NullifierRegistryComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the register(byte[],address)void ABI method.
   *
  * Register nullifier → wallet. Issuer-only.
  Fails if nullifier already registered (one Aadhaar per wallet).

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  register(params?: CallParams<NullifierRegistryArgs['obj']['register(byte[],address)void'] | NullifierRegistryArgs['tuple']['register(byte[],address)void']>): NullifierRegistryComposer<[...TReturns, NullifierRegistryReturns['register(byte[],address)void'] | undefined]>

  /**
   * Calls the is_registered(byte[])bool ABI method.
   *
   * Check if a nullifier has been registered.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  isRegistered(params?: CallParams<NullifierRegistryArgs['obj']['is_registered(byte[])bool'] | NullifierRegistryArgs['tuple']['is_registered(byte[])bool']>): NullifierRegistryComposer<[...TReturns, NullifierRegistryReturns['is_registered(byte[])bool'] | undefined]>

  /**
   * Calls the get_wallet(byte[])byte[] ABI method.
   *
   * Retrieve wallet bytes for a nullifier (court order lookup).
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  getWallet(params?: CallParams<NullifierRegistryArgs['obj']['get_wallet(byte[])byte[]'] | NullifierRegistryArgs['tuple']['get_wallet(byte[])byte[]']>): NullifierRegistryComposer<[...TReturns, NullifierRegistryReturns['get_wallet(byte[])byte[]'] | undefined]>

  /**
   * Calls the invalidate(byte[])void ABI method.
   *
   * Remove nullifier registration — issuer-only, used on revocation.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  invalidate(params?: CallParams<NullifierRegistryArgs['obj']['invalidate(byte[])void'] | NullifierRegistryArgs['tuple']['invalidate(byte[])void']>): NullifierRegistryComposer<[...TReturns, NullifierRegistryReturns['invalidate(byte[])void'] | undefined]>

  /**
   * Calls the get_total_registered()uint64 ABI method.
   *
   * Total registered credentials.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  getTotalRegistered(params?: CallParams<NullifierRegistryArgs['obj']['get_total_registered()uint64'] | NullifierRegistryArgs['tuple']['get_total_registered()uint64']>): NullifierRegistryComposer<[...TReturns, NullifierRegistryReturns['get_total_registered()uint64'] | undefined]>

  /**
   * Calls the update_issuer(address)void ABI method.
   *
   * Transfer issuer role. Issuer-only.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  updateIssuer(params?: CallParams<NullifierRegistryArgs['obj']['update_issuer(address)void'] | NullifierRegistryArgs['tuple']['update_issuer(address)void']>): NullifierRegistryComposer<[...TReturns, NullifierRegistryReturns['update_issuer(address)void'] | undefined]>

  /**
   * Makes a clear_state call to an existing instance of the NullifierRegistry smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): NullifierRegistryComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): NullifierRegistryComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<NullifierRegistryComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<NullifierRegistryComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<NullifierRegistryComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<NullifierRegistryComposerResults<TReturns>>
}
export type NullifierRegistryComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

