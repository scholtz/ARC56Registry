/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"CredentialManager","structs":{},"methods":[{"name":"create","args":[],"returns":{"type":"void"},"actions":{"create":["NoOp"],"call":[]},"readonly":false,"desc":"Initialize — deployer becomes issuer. Fund app with 0.3 ALGO then call initialize_asa().","events":[],"recommendations":{}},{"name":"initialize_asa","args":[],"returns":{"type":"uint64"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Create the KYC credential ASA. Must be called AFTER funding app with >= 0.3 ALGO.\nReturns the new ASA ID. App address is manager/clawback/freeze.","events":[],"recommendations":{}},{"name":"issue_credential","args":[{"type":"address","name":"recipient"},{"type":"byte[]","name":"nullifier"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Issue a KYC credential to a verified wallet. Issuer-only.","events":[],"recommendations":{}},{"name":"revoke_credential","args":[{"type":"address","name":"wallet"},{"type":"byte[]","name":"nullifier"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Revoke credential via clawback. Issuer-only.","events":[],"recommendations":{}},{"name":"get_credential_asa_id","args":[],"returns":{"type":"uint64"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"desc":"Return the credential ASA ID.","events":[],"recommendations":{}},{"name":"get_total_issued","args":[],"returns":{"type":"uint64"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"get_total_revoked","args":[],"returns":{"type":"uint64"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"update_issuer","args":[{"type":"address","name":"new_issuer"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Transfer issuer role. Issuer-only.","events":[],"recommendations":{}}],"arcs":[22,28],"desc":"Issues and revokes non-transferable KYC credential ASAs.","networks":{},"state":{"schema":{"global":{"ints":3,"bytes":1},"local":{"ints":0,"bytes":0}},"keys":{"global":{"issuer":{"keyType":"AVMString","valueType":"address","key":"aXNzdWVy"},"credential_asa_id":{"keyType":"AVMString","valueType":"AVMUint64","key":"Y3JlZGVudGlhbF9hc2FfaWQ="},"total_issued":{"keyType":"AVMString","valueType":"AVMUint64","key":"dG90YWxfaXNzdWVk"},"total_revoked":{"keyType":"AVMString","valueType":"AVMUint64","key":"dG90YWxfcmV2b2tlZA=="}},"local":{},"box":{}},"maps":{"global":{},"local":{},"box":{}}},"bareActions":{"create":[],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[189],"errorMessage":"ASA already created"},{"pc":[183,325,429,520],"errorMessage":"Only issuer"},{"pc":[187,329,434,472],"errorMessage":"check self.credential_asa_id exists"},{"pc":[181,323,427,518],"errorMessage":"check self.issuer exists"},{"pc":[390,484],"errorMessage":"check self.total_issued exists"},{"pc":[461,496],"errorMessage":"check self.total_revoked exists"},{"pc":[307,411],"errorMessage":"invalid array length header"},{"pc":[314,418],"errorMessage":"invalid number of bytes for arc4.dynamic_array<arc4.uint8>"},{"pc":[301,405,512],"errorMessage":"invalid number of bytes for arc4.static_array<arc4.uint8, 32>"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"events":[]} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


/**
 * The argument types for the CredentialManager contract
 */
export type CredentialManagerArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'create()void': Record<string, never>
    'initialize_asa()uint64': Record<string, never>
    'issue_credential(address,byte[])void': {
      recipient: string
      nullifier: Uint8Array
    }
    'revoke_credential(address,byte[])void': {
      wallet: string
      nullifier: Uint8Array
    }
    'get_credential_asa_id()uint64': Record<string, never>
    'get_total_issued()uint64': Record<string, never>
    'get_total_revoked()uint64': Record<string, never>
    'update_issuer(address)void': {
      newIssuer: string
    }
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'create()void': []
    'initialize_asa()uint64': []
    'issue_credential(address,byte[])void': [recipient: string, nullifier: Uint8Array]
    'revoke_credential(address,byte[])void': [wallet: string, nullifier: Uint8Array]
    'get_credential_asa_id()uint64': []
    'get_total_issued()uint64': []
    'get_total_revoked()uint64': []
    'update_issuer(address)void': [newIssuer: string]
  }
}

/**
 * The return type for each method
 */
export type CredentialManagerReturns = {
  'create()void': void
  'initialize_asa()uint64': bigint
  'issue_credential(address,byte[])void': void
  'revoke_credential(address,byte[])void': void
  'get_credential_asa_id()uint64': bigint
  'get_total_issued()uint64': bigint
  'get_total_revoked()uint64': bigint
  'update_issuer(address)void': void
}

/**
 * Defines the types of available calls and state of the CredentialManager smart contract.
 */
export type CredentialManagerTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'create()void' | 'create', {
      argsObj: CredentialManagerArgs['obj']['create()void']
      argsTuple: CredentialManagerArgs['tuple']['create()void']
      returns: CredentialManagerReturns['create()void']
    }>
    & Record<'initialize_asa()uint64' | 'initialize_asa', {
      argsObj: CredentialManagerArgs['obj']['initialize_asa()uint64']
      argsTuple: CredentialManagerArgs['tuple']['initialize_asa()uint64']
      returns: CredentialManagerReturns['initialize_asa()uint64']
    }>
    & Record<'issue_credential(address,byte[])void' | 'issue_credential', {
      argsObj: CredentialManagerArgs['obj']['issue_credential(address,byte[])void']
      argsTuple: CredentialManagerArgs['tuple']['issue_credential(address,byte[])void']
      returns: CredentialManagerReturns['issue_credential(address,byte[])void']
    }>
    & Record<'revoke_credential(address,byte[])void' | 'revoke_credential', {
      argsObj: CredentialManagerArgs['obj']['revoke_credential(address,byte[])void']
      argsTuple: CredentialManagerArgs['tuple']['revoke_credential(address,byte[])void']
      returns: CredentialManagerReturns['revoke_credential(address,byte[])void']
    }>
    & Record<'get_credential_asa_id()uint64' | 'get_credential_asa_id', {
      argsObj: CredentialManagerArgs['obj']['get_credential_asa_id()uint64']
      argsTuple: CredentialManagerArgs['tuple']['get_credential_asa_id()uint64']
      returns: CredentialManagerReturns['get_credential_asa_id()uint64']
    }>
    & Record<'get_total_issued()uint64' | 'get_total_issued', {
      argsObj: CredentialManagerArgs['obj']['get_total_issued()uint64']
      argsTuple: CredentialManagerArgs['tuple']['get_total_issued()uint64']
      returns: CredentialManagerReturns['get_total_issued()uint64']
    }>
    & Record<'get_total_revoked()uint64' | 'get_total_revoked', {
      argsObj: CredentialManagerArgs['obj']['get_total_revoked()uint64']
      argsTuple: CredentialManagerArgs['tuple']['get_total_revoked()uint64']
      returns: CredentialManagerReturns['get_total_revoked()uint64']
    }>
    & Record<'update_issuer(address)void' | 'update_issuer', {
      argsObj: CredentialManagerArgs['obj']['update_issuer(address)void']
      argsTuple: CredentialManagerArgs['tuple']['update_issuer(address)void']
      returns: CredentialManagerReturns['update_issuer(address)void']
    }>
  /**
   * Defines the shape of the state of the application.
   */
  state: {
    global: {
      keys: {
        issuer: string
        credentialAsaId: bigint
        totalIssued: bigint
        totalRevoked: bigint
      }
      maps: {}
    }
  }
}

/**
 * Defines the possible abi call signatures.
 */
export type CredentialManagerSignatures = keyof CredentialManagerTypes['methods']
/**
 * Defines the possible abi call signatures for methods that return a non-void value.
 */
export type CredentialManagerNonVoidMethodSignatures = keyof CredentialManagerTypes['methods'] extends infer T ? T extends keyof CredentialManagerTypes['methods'] ? MethodReturn<T> extends void ? never : T  : never : never
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the CredentialManager smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends CredentialManagerSignatures> = CredentialManagerTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the CredentialManager smart contract to the method's return type
 */
export type MethodReturn<TSignature extends CredentialManagerSignatures> = CredentialManagerTypes['methods'][TSignature]['returns']

/**
 * Defines the shape of the keyed global state of the application.
 */
export type GlobalKeysState = CredentialManagerTypes['state']['global']['keys']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the CredentialManager smart contract
 */
export abstract class CredentialManagerParamsFactory {
  /**
   * Constructs a no op call for the initialize_asa()uint64 ABI method
   *
  * Create the KYC credential ASA. Must be called AFTER funding app with >= 0.3 ALGO.
  Returns the new ASA ID. App address is manager/clawback/freeze.

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static initializeAsa(params: CallParams<CredentialManagerArgs['obj']['initialize_asa()uint64'] | CredentialManagerArgs['tuple']['initialize_asa()uint64']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'initialize_asa()uint64' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the issue_credential(address,byte[])void ABI method
   *
   * Issue a KYC credential to a verified wallet. Issuer-only.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static issueCredential(params: CallParams<CredentialManagerArgs['obj']['issue_credential(address,byte[])void'] | CredentialManagerArgs['tuple']['issue_credential(address,byte[])void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'issue_credential(address,byte[])void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.recipient, params.args.nullifier],
    }
  }
  /**
   * Constructs a no op call for the revoke_credential(address,byte[])void ABI method
   *
   * Revoke credential via clawback. Issuer-only.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static revokeCredential(params: CallParams<CredentialManagerArgs['obj']['revoke_credential(address,byte[])void'] | CredentialManagerArgs['tuple']['revoke_credential(address,byte[])void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'revoke_credential(address,byte[])void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.wallet, params.args.nullifier],
    }
  }
  /**
   * Constructs a no op call for the get_credential_asa_id()uint64 ABI method
   *
   * Return the credential ASA ID.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static getCredentialAsaId(params: CallParams<CredentialManagerArgs['obj']['get_credential_asa_id()uint64'] | CredentialManagerArgs['tuple']['get_credential_asa_id()uint64']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'get_credential_asa_id()uint64' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the get_total_issued()uint64 ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static getTotalIssued(params: CallParams<CredentialManagerArgs['obj']['get_total_issued()uint64'] | CredentialManagerArgs['tuple']['get_total_issued()uint64']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'get_total_issued()uint64' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the get_total_revoked()uint64 ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static getTotalRevoked(params: CallParams<CredentialManagerArgs['obj']['get_total_revoked()uint64'] | CredentialManagerArgs['tuple']['get_total_revoked()uint64']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'get_total_revoked()uint64' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the update_issuer(address)void ABI method
   *
   * Transfer issuer role. Issuer-only.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static updateIssuer(params: CallParams<CredentialManagerArgs['obj']['update_issuer(address)void'] | CredentialManagerArgs['tuple']['update_issuer(address)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'update_issuer(address)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.newIssuer],
    }
  }
}

/**
 * A client to make calls to the CredentialManager smart contract
 */
export class CredentialManagerClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `CredentialManagerClient`
   *
   * @param appClient An `AppClient` instance which has been created with the CredentialManager app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `CredentialManagerClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Checks for decode errors on the given return value and maps the return value to the return type for the given method
   * @returns The typed return value or undefined if there was no value
   */
  decodeReturnValue<TSignature extends CredentialManagerNonVoidMethodSignatures>(method: TSignature, returnValue: ABIReturn | undefined) {
    return returnValue !== undefined ? getArc56ReturnValue<MethodReturn<TSignature>>(returnValue, this.appClient.getABIMethod(method), APP_SPEC.structs) : undefined
  }

  /**
   * Returns a new `CredentialManagerClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<CredentialManagerClient> {
    return new CredentialManagerClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `CredentialManagerClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<CredentialManagerClient> {
    return new CredentialManagerClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Makes a clear_state call to an existing instance of the CredentialManager smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the CredentialManager smart contract using the `initialize_asa()uint64` ABI method.
     *
    * Create the KYC credential ASA. Must be called AFTER funding app with >= 0.3 ALGO.
    Returns the new ASA ID. App address is manager/clawback/freeze.

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    initializeAsa: (params: CallParams<CredentialManagerArgs['obj']['initialize_asa()uint64'] | CredentialManagerArgs['tuple']['initialize_asa()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(CredentialManagerParamsFactory.initializeAsa(params))
    },

    /**
     * Makes a call to the CredentialManager smart contract using the `issue_credential(address,byte[])void` ABI method.
     *
     * Issue a KYC credential to a verified wallet. Issuer-only.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    issueCredential: (params: CallParams<CredentialManagerArgs['obj']['issue_credential(address,byte[])void'] | CredentialManagerArgs['tuple']['issue_credential(address,byte[])void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(CredentialManagerParamsFactory.issueCredential(params))
    },

    /**
     * Makes a call to the CredentialManager smart contract using the `revoke_credential(address,byte[])void` ABI method.
     *
     * Revoke credential via clawback. Issuer-only.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    revokeCredential: (params: CallParams<CredentialManagerArgs['obj']['revoke_credential(address,byte[])void'] | CredentialManagerArgs['tuple']['revoke_credential(address,byte[])void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(CredentialManagerParamsFactory.revokeCredential(params))
    },

    /**
     * Makes a call to the CredentialManager smart contract using the `get_credential_asa_id()uint64` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * Return the credential ASA ID.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    getCredentialAsaId: (params: CallParams<CredentialManagerArgs['obj']['get_credential_asa_id()uint64'] | CredentialManagerArgs['tuple']['get_credential_asa_id()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(CredentialManagerParamsFactory.getCredentialAsaId(params))
    },

    /**
     * Makes a call to the CredentialManager smart contract using the `get_total_issued()uint64` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    getTotalIssued: (params: CallParams<CredentialManagerArgs['obj']['get_total_issued()uint64'] | CredentialManagerArgs['tuple']['get_total_issued()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(CredentialManagerParamsFactory.getTotalIssued(params))
    },

    /**
     * Makes a call to the CredentialManager smart contract using the `get_total_revoked()uint64` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    getTotalRevoked: (params: CallParams<CredentialManagerArgs['obj']['get_total_revoked()uint64'] | CredentialManagerArgs['tuple']['get_total_revoked()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(CredentialManagerParamsFactory.getTotalRevoked(params))
    },

    /**
     * Makes a call to the CredentialManager smart contract using the `update_issuer(address)void` ABI method.
     *
     * Transfer issuer role. Issuer-only.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    updateIssuer: (params: CallParams<CredentialManagerArgs['obj']['update_issuer(address)void'] | CredentialManagerArgs['tuple']['update_issuer(address)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(CredentialManagerParamsFactory.updateIssuer(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Makes a clear_state call to an existing instance of the CredentialManager smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the CredentialManager smart contract using the `initialize_asa()uint64` ABI method.
     *
    * Create the KYC credential ASA. Must be called AFTER funding app with >= 0.3 ALGO.
    Returns the new ASA ID. App address is manager/clawback/freeze.

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    initializeAsa: (params: CallParams<CredentialManagerArgs['obj']['initialize_asa()uint64'] | CredentialManagerArgs['tuple']['initialize_asa()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(CredentialManagerParamsFactory.initializeAsa(params))
    },

    /**
     * Makes a call to the CredentialManager smart contract using the `issue_credential(address,byte[])void` ABI method.
     *
     * Issue a KYC credential to a verified wallet. Issuer-only.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    issueCredential: (params: CallParams<CredentialManagerArgs['obj']['issue_credential(address,byte[])void'] | CredentialManagerArgs['tuple']['issue_credential(address,byte[])void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(CredentialManagerParamsFactory.issueCredential(params))
    },

    /**
     * Makes a call to the CredentialManager smart contract using the `revoke_credential(address,byte[])void` ABI method.
     *
     * Revoke credential via clawback. Issuer-only.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    revokeCredential: (params: CallParams<CredentialManagerArgs['obj']['revoke_credential(address,byte[])void'] | CredentialManagerArgs['tuple']['revoke_credential(address,byte[])void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(CredentialManagerParamsFactory.revokeCredential(params))
    },

    /**
     * Makes a call to the CredentialManager smart contract using the `get_credential_asa_id()uint64` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * Return the credential ASA ID.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    getCredentialAsaId: (params: CallParams<CredentialManagerArgs['obj']['get_credential_asa_id()uint64'] | CredentialManagerArgs['tuple']['get_credential_asa_id()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(CredentialManagerParamsFactory.getCredentialAsaId(params))
    },

    /**
     * Makes a call to the CredentialManager smart contract using the `get_total_issued()uint64` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    getTotalIssued: (params: CallParams<CredentialManagerArgs['obj']['get_total_issued()uint64'] | CredentialManagerArgs['tuple']['get_total_issued()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(CredentialManagerParamsFactory.getTotalIssued(params))
    },

    /**
     * Makes a call to the CredentialManager smart contract using the `get_total_revoked()uint64` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    getTotalRevoked: (params: CallParams<CredentialManagerArgs['obj']['get_total_revoked()uint64'] | CredentialManagerArgs['tuple']['get_total_revoked()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(CredentialManagerParamsFactory.getTotalRevoked(params))
    },

    /**
     * Makes a call to the CredentialManager smart contract using the `update_issuer(address)void` ABI method.
     *
     * Transfer issuer role. Issuer-only.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    updateIssuer: (params: CallParams<CredentialManagerArgs['obj']['update_issuer(address)void'] | CredentialManagerArgs['tuple']['update_issuer(address)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(CredentialManagerParamsFactory.updateIssuer(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Makes a clear_state call to an existing instance of the CredentialManager smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the CredentialManager smart contract using the `initialize_asa()uint64` ABI method.
     *
    * Create the KYC credential ASA. Must be called AFTER funding app with >= 0.3 ALGO.
    Returns the new ASA ID. App address is manager/clawback/freeze.

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    initializeAsa: async (params: CallParams<CredentialManagerArgs['obj']['initialize_asa()uint64'] | CredentialManagerArgs['tuple']['initialize_asa()uint64']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(CredentialManagerParamsFactory.initializeAsa(params))
      return {...result, return: result.return as unknown as (undefined | CredentialManagerReturns['initialize_asa()uint64'])}
    },

    /**
     * Makes a call to the CredentialManager smart contract using the `issue_credential(address,byte[])void` ABI method.
     *
     * Issue a KYC credential to a verified wallet. Issuer-only.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    issueCredential: async (params: CallParams<CredentialManagerArgs['obj']['issue_credential(address,byte[])void'] | CredentialManagerArgs['tuple']['issue_credential(address,byte[])void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(CredentialManagerParamsFactory.issueCredential(params))
      return {...result, return: result.return as unknown as (undefined | CredentialManagerReturns['issue_credential(address,byte[])void'])}
    },

    /**
     * Makes a call to the CredentialManager smart contract using the `revoke_credential(address,byte[])void` ABI method.
     *
     * Revoke credential via clawback. Issuer-only.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    revokeCredential: async (params: CallParams<CredentialManagerArgs['obj']['revoke_credential(address,byte[])void'] | CredentialManagerArgs['tuple']['revoke_credential(address,byte[])void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(CredentialManagerParamsFactory.revokeCredential(params))
      return {...result, return: result.return as unknown as (undefined | CredentialManagerReturns['revoke_credential(address,byte[])void'])}
    },

    /**
     * Makes a call to the CredentialManager smart contract using the `get_credential_asa_id()uint64` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * Return the credential ASA ID.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    getCredentialAsaId: async (params: CallParams<CredentialManagerArgs['obj']['get_credential_asa_id()uint64'] | CredentialManagerArgs['tuple']['get_credential_asa_id()uint64']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(CredentialManagerParamsFactory.getCredentialAsaId(params))
      return {...result, return: result.return as unknown as (undefined | CredentialManagerReturns['get_credential_asa_id()uint64'])}
    },

    /**
     * Makes a call to the CredentialManager smart contract using the `get_total_issued()uint64` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    getTotalIssued: async (params: CallParams<CredentialManagerArgs['obj']['get_total_issued()uint64'] | CredentialManagerArgs['tuple']['get_total_issued()uint64']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(CredentialManagerParamsFactory.getTotalIssued(params))
      return {...result, return: result.return as unknown as (undefined | CredentialManagerReturns['get_total_issued()uint64'])}
    },

    /**
     * Makes a call to the CredentialManager smart contract using the `get_total_revoked()uint64` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    getTotalRevoked: async (params: CallParams<CredentialManagerArgs['obj']['get_total_revoked()uint64'] | CredentialManagerArgs['tuple']['get_total_revoked()uint64']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(CredentialManagerParamsFactory.getTotalRevoked(params))
      return {...result, return: result.return as unknown as (undefined | CredentialManagerReturns['get_total_revoked()uint64'])}
    },

    /**
     * Makes a call to the CredentialManager smart contract using the `update_issuer(address)void` ABI method.
     *
     * Transfer issuer role. Issuer-only.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    updateIssuer: async (params: CallParams<CredentialManagerArgs['obj']['update_issuer(address)void'] | CredentialManagerArgs['tuple']['update_issuer(address)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(CredentialManagerParamsFactory.updateIssuer(params))
      return {...result, return: result.return as unknown as (undefined | CredentialManagerReturns['update_issuer(address)void'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new CredentialManagerClient(this.appClient.clone(params))
  }

  /**
   * Makes a readonly (simulated) call to the CredentialManager smart contract using the `get_credential_asa_id()uint64` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * Return the credential ASA ID.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async getCredentialAsaId(params: CallParams<CredentialManagerArgs['obj']['get_credential_asa_id()uint64'] | CredentialManagerArgs['tuple']['get_credential_asa_id()uint64']> = {args: []}) {
    const result = await this.appClient.send.call(CredentialManagerParamsFactory.getCredentialAsaId(params))
    return result.return as unknown as CredentialManagerReturns['get_credential_asa_id()uint64']
  }

  /**
   * Makes a readonly (simulated) call to the CredentialManager smart contract using the `get_total_issued()uint64` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async getTotalIssued(params: CallParams<CredentialManagerArgs['obj']['get_total_issued()uint64'] | CredentialManagerArgs['tuple']['get_total_issued()uint64']> = {args: []}) {
    const result = await this.appClient.send.call(CredentialManagerParamsFactory.getTotalIssued(params))
    return result.return as unknown as CredentialManagerReturns['get_total_issued()uint64']
  }

  /**
   * Makes a readonly (simulated) call to the CredentialManager smart contract using the `get_total_revoked()uint64` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async getTotalRevoked(params: CallParams<CredentialManagerArgs['obj']['get_total_revoked()uint64'] | CredentialManagerArgs['tuple']['get_total_revoked()uint64']> = {args: []}) {
    const result = await this.appClient.send.call(CredentialManagerParamsFactory.getTotalRevoked(params))
    return result.return as unknown as CredentialManagerReturns['get_total_revoked()uint64']
  }

  /**
   * Methods to access state for the current CredentialManager app
   */
  state = {
    /**
     * Methods to access global state for the current CredentialManager app
     */
    global: {
      /**
       * Get all current keyed values from global state
       */
      getAll: async (): Promise<Partial<Expand<GlobalKeysState>>> => {
        const result = await this.appClient.state.global.getAll()
        return {
          issuer: result.issuer,
          credentialAsaId: result.credential_asa_id,
          totalIssued: result.total_issued,
          totalRevoked: result.total_revoked,
        }
      },
      /**
       * Get the current value of the issuer key in global state
       */
      issuer: async (): Promise<string | undefined> => { return (await this.appClient.state.global.getValue("issuer")) as string | undefined },
      /**
       * Get the current value of the credential_asa_id key in global state
       */
      credentialAsaId: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("credential_asa_id")) as bigint | undefined },
      /**
       * Get the current value of the total_issued key in global state
       */
      totalIssued: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("total_issued")) as bigint | undefined },
      /**
       * Get the current value of the total_revoked key in global state
       */
      totalRevoked: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("total_revoked")) as bigint | undefined },
    },
  }

  public newGroup(): CredentialManagerComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a initialize_asa()uint64 method call against the CredentialManager contract
       */
      initializeAsa(params: CallParams<CredentialManagerArgs['obj']['initialize_asa()uint64'] | CredentialManagerArgs['tuple']['initialize_asa()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.initializeAsa(params)))
        resultMappers.push((v) => client.decodeReturnValue('initialize_asa()uint64', v))
        return this
      },
      /**
       * Add a issue_credential(address,byte[])void method call against the CredentialManager contract
       */
      issueCredential(params: CallParams<CredentialManagerArgs['obj']['issue_credential(address,byte[])void'] | CredentialManagerArgs['tuple']['issue_credential(address,byte[])void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.issueCredential(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a revoke_credential(address,byte[])void method call against the CredentialManager contract
       */
      revokeCredential(params: CallParams<CredentialManagerArgs['obj']['revoke_credential(address,byte[])void'] | CredentialManagerArgs['tuple']['revoke_credential(address,byte[])void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.revokeCredential(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a get_credential_asa_id()uint64 method call against the CredentialManager contract
       */
      getCredentialAsaId(params: CallParams<CredentialManagerArgs['obj']['get_credential_asa_id()uint64'] | CredentialManagerArgs['tuple']['get_credential_asa_id()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.getCredentialAsaId(params)))
        resultMappers.push((v) => client.decodeReturnValue('get_credential_asa_id()uint64', v))
        return this
      },
      /**
       * Add a get_total_issued()uint64 method call against the CredentialManager contract
       */
      getTotalIssued(params: CallParams<CredentialManagerArgs['obj']['get_total_issued()uint64'] | CredentialManagerArgs['tuple']['get_total_issued()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.getTotalIssued(params)))
        resultMappers.push((v) => client.decodeReturnValue('get_total_issued()uint64', v))
        return this
      },
      /**
       * Add a get_total_revoked()uint64 method call against the CredentialManager contract
       */
      getTotalRevoked(params: CallParams<CredentialManagerArgs['obj']['get_total_revoked()uint64'] | CredentialManagerArgs['tuple']['get_total_revoked()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.getTotalRevoked(params)))
        resultMappers.push((v) => client.decodeReturnValue('get_total_revoked()uint64', v))
        return this
      },
      /**
       * Add a update_issuer(address)void method call against the CredentialManager contract
       */
      updateIssuer(params: CallParams<CredentialManagerArgs['obj']['update_issuer(address)void'] | CredentialManagerArgs['tuple']['update_issuer(address)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.updateIssuer(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a clear state call to the CredentialManager contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as CredentialManagerComposer
  }
}
export type CredentialManagerComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the initialize_asa()uint64 ABI method.
   *
  * Create the KYC credential ASA. Must be called AFTER funding app with >= 0.3 ALGO.
  Returns the new ASA ID. App address is manager/clawback/freeze.

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  initializeAsa(params?: CallParams<CredentialManagerArgs['obj']['initialize_asa()uint64'] | CredentialManagerArgs['tuple']['initialize_asa()uint64']>): CredentialManagerComposer<[...TReturns, CredentialManagerReturns['initialize_asa()uint64'] | undefined]>

  /**
   * Calls the issue_credential(address,byte[])void ABI method.
   *
   * Issue a KYC credential to a verified wallet. Issuer-only.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  issueCredential(params?: CallParams<CredentialManagerArgs['obj']['issue_credential(address,byte[])void'] | CredentialManagerArgs['tuple']['issue_credential(address,byte[])void']>): CredentialManagerComposer<[...TReturns, CredentialManagerReturns['issue_credential(address,byte[])void'] | undefined]>

  /**
   * Calls the revoke_credential(address,byte[])void ABI method.
   *
   * Revoke credential via clawback. Issuer-only.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  revokeCredential(params?: CallParams<CredentialManagerArgs['obj']['revoke_credential(address,byte[])void'] | CredentialManagerArgs['tuple']['revoke_credential(address,byte[])void']>): CredentialManagerComposer<[...TReturns, CredentialManagerReturns['revoke_credential(address,byte[])void'] | undefined]>

  /**
   * Calls the get_credential_asa_id()uint64 ABI method.
   *
   * Return the credential ASA ID.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  getCredentialAsaId(params?: CallParams<CredentialManagerArgs['obj']['get_credential_asa_id()uint64'] | CredentialManagerArgs['tuple']['get_credential_asa_id()uint64']>): CredentialManagerComposer<[...TReturns, CredentialManagerReturns['get_credential_asa_id()uint64'] | undefined]>

  /**
   * Calls the get_total_issued()uint64 ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  getTotalIssued(params?: CallParams<CredentialManagerArgs['obj']['get_total_issued()uint64'] | CredentialManagerArgs['tuple']['get_total_issued()uint64']>): CredentialManagerComposer<[...TReturns, CredentialManagerReturns['get_total_issued()uint64'] | undefined]>

  /**
   * Calls the get_total_revoked()uint64 ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  getTotalRevoked(params?: CallParams<CredentialManagerArgs['obj']['get_total_revoked()uint64'] | CredentialManagerArgs['tuple']['get_total_revoked()uint64']>): CredentialManagerComposer<[...TReturns, CredentialManagerReturns['get_total_revoked()uint64'] | undefined]>

  /**
   * Calls the update_issuer(address)void ABI method.
   *
   * Transfer issuer role. Issuer-only.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  updateIssuer(params?: CallParams<CredentialManagerArgs['obj']['update_issuer(address)void'] | CredentialManagerArgs['tuple']['update_issuer(address)void']>): CredentialManagerComposer<[...TReturns, CredentialManagerReturns['update_issuer(address)void'] | undefined]>

  /**
   * Makes a clear_state call to an existing instance of the CredentialManager smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): CredentialManagerComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): CredentialManagerComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<CredentialManagerComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<CredentialManagerComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<CredentialManagerComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<CredentialManagerComposerResults<TReturns>>
}
export type CredentialManagerComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

