/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"CampusMarketplace","structs":{},"methods":[{"name":"create","args":[],"returns":{"type":"void"},"actions":{"create":["NoOp"],"call":[]},"readonly":false,"desc":"Called once when the app is first created.","events":[],"recommendations":{}},{"name":"list_item","args":[{"type":"uint64","name":"price"}],"returns":{"type":"uint64"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"List a new item for sale.\nOnly the seller (contract creator) can call this. Price is in microALGO (1 ALGO = 1,000,000 microALGO). Returns the assigned item_id.","events":[],"recommendations":{}},{"name":"buy_item","args":[{"type":"uint64","name":"item_id"},{"type":"pay","name":"pay_txn"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Buy an item. Must be called in an atomic group with a payment txn.\nThe payment amount must match the listed price. The payment receiver must be the seller (creator).","events":[],"recommendations":{}},{"name":"get_price","args":[{"type":"uint64","name":"item_id"}],"returns":{"type":"uint64"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"desc":"Get the price of an item. Returns 0 if sold or not listed.","events":[],"recommendations":{}},{"name":"get_item_count","args":[],"returns":{"type":"uint64"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"desc":"How many items have been listed total.","events":[],"recommendations":{}}],"arcs":[22,28],"desc":"\n    Marketplace contract.\n    - The deployer is the seller\n    - Seller lists items with prices (in microALGO)\n    - Buyer pays ALGO to purchase items\n    - Payment goes directly to the seller\n    ","networks":{},"state":{"schema":{"global":{"ints":1,"bytes":0},"local":{"ints":0,"bytes":0}},"keys":{"global":{"item_count":{"keyType":"AVMString","valueType":"AVMUint64","key":"aXRlbV9jb3VudA=="}},"local":{},"box":{}},"maps":{"global":{},"local":{},"box":{"prices":{"keyType":"uint64","valueType":"uint64","prefix":"cHJpY2Vz"}}}},"bareActions":{"create":[],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[123,238],"errorMessage":"check self.item_count exists"},{"pc":[171],"errorMessage":"check self.prices entry exists"},{"pc":[110,152,201],"errorMessage":"invalid number of bytes for arc4.uint64"},{"pc":[174],"errorMessage":"item not available"},{"pc":[116],"errorMessage":"only seller can list"},{"pc":[182],"errorMessage":"pay the seller"},{"pc":[119],"errorMessage":"price must be > 0"},{"pc":[162],"errorMessage":"transaction type is pay"},{"pc":[188],"errorMessage":"wrong payment amount"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"events":[]} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


/**
 * The argument types for the CampusMarketplace contract
 */
export type CampusMarketplaceArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'create()void': Record<string, never>
    'list_item(uint64)uint64': {
      price: bigint | number
    }
    'buy_item(uint64,pay)void': {
      itemId: bigint | number
      payTxn: AppMethodCallTransactionArgument
    }
    'get_price(uint64)uint64': {
      itemId: bigint | number
    }
    'get_item_count()uint64': Record<string, never>
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'create()void': []
    'list_item(uint64)uint64': [price: bigint | number]
    'buy_item(uint64,pay)void': [itemId: bigint | number, payTxn: AppMethodCallTransactionArgument]
    'get_price(uint64)uint64': [itemId: bigint | number]
    'get_item_count()uint64': []
  }
}

/**
 * The return type for each method
 */
export type CampusMarketplaceReturns = {
  'create()void': void
  'list_item(uint64)uint64': bigint
  'buy_item(uint64,pay)void': void
  'get_price(uint64)uint64': bigint
  'get_item_count()uint64': bigint
}

/**
 * Defines the types of available calls and state of the CampusMarketplace smart contract.
 */
export type CampusMarketplaceTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'create()void' | 'create', {
      argsObj: CampusMarketplaceArgs['obj']['create()void']
      argsTuple: CampusMarketplaceArgs['tuple']['create()void']
      returns: CampusMarketplaceReturns['create()void']
    }>
    & Record<'list_item(uint64)uint64' | 'list_item', {
      argsObj: CampusMarketplaceArgs['obj']['list_item(uint64)uint64']
      argsTuple: CampusMarketplaceArgs['tuple']['list_item(uint64)uint64']
      returns: CampusMarketplaceReturns['list_item(uint64)uint64']
    }>
    & Record<'buy_item(uint64,pay)void' | 'buy_item', {
      argsObj: CampusMarketplaceArgs['obj']['buy_item(uint64,pay)void']
      argsTuple: CampusMarketplaceArgs['tuple']['buy_item(uint64,pay)void']
      returns: CampusMarketplaceReturns['buy_item(uint64,pay)void']
    }>
    & Record<'get_price(uint64)uint64' | 'get_price', {
      argsObj: CampusMarketplaceArgs['obj']['get_price(uint64)uint64']
      argsTuple: CampusMarketplaceArgs['tuple']['get_price(uint64)uint64']
      returns: CampusMarketplaceReturns['get_price(uint64)uint64']
    }>
    & Record<'get_item_count()uint64' | 'get_item_count', {
      argsObj: CampusMarketplaceArgs['obj']['get_item_count()uint64']
      argsTuple: CampusMarketplaceArgs['tuple']['get_item_count()uint64']
      returns: CampusMarketplaceReturns['get_item_count()uint64']
    }>
  /**
   * Defines the shape of the state of the application.
   */
  state: {
    global: {
      keys: {
        itemCount: bigint
      }
      maps: {}
    }
    box: {
      keys: {}
      maps: {
        prices: Map<bigint | number, bigint>
      }
    }
  }
}

/**
 * Defines the possible abi call signatures.
 */
export type CampusMarketplaceSignatures = keyof CampusMarketplaceTypes['methods']
/**
 * Defines the possible abi call signatures for methods that return a non-void value.
 */
export type CampusMarketplaceNonVoidMethodSignatures = keyof CampusMarketplaceTypes['methods'] extends infer T ? T extends keyof CampusMarketplaceTypes['methods'] ? MethodReturn<T> extends void ? never : T  : never : never
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the CampusMarketplace smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends CampusMarketplaceSignatures> = CampusMarketplaceTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the CampusMarketplace smart contract to the method's return type
 */
export type MethodReturn<TSignature extends CampusMarketplaceSignatures> = CampusMarketplaceTypes['methods'][TSignature]['returns']

/**
 * Defines the shape of the keyed global state of the application.
 */
export type GlobalKeysState = CampusMarketplaceTypes['state']['global']['keys']

/**
 * Defines the shape of the keyed box state of the application.
 */
export type BoxKeysState = CampusMarketplaceTypes['state']['box']['keys']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the CampusMarketplace smart contract
 */
export abstract class CampusMarketplaceParamsFactory {
  /**
   * Constructs a no op call for the list_item(uint64)uint64 ABI method
   *
  * List a new item for sale.
  Only the seller (contract creator) can call this. Price is in microALGO (1 ALGO = 1,000,000 microALGO). Returns the assigned item_id.

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static listItem(params: CallParams<CampusMarketplaceArgs['obj']['list_item(uint64)uint64'] | CampusMarketplaceArgs['tuple']['list_item(uint64)uint64']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'list_item(uint64)uint64' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.price],
    }
  }
  /**
   * Constructs a no op call for the buy_item(uint64,pay)void ABI method
   *
  * Buy an item. Must be called in an atomic group with a payment txn.
  The payment amount must match the listed price. The payment receiver must be the seller (creator).

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static buyItem(params: CallParams<CampusMarketplaceArgs['obj']['buy_item(uint64,pay)void'] | CampusMarketplaceArgs['tuple']['buy_item(uint64,pay)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'buy_item(uint64,pay)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.itemId, params.args.payTxn],
    }
  }
  /**
   * Constructs a no op call for the get_price(uint64)uint64 ABI method
   *
   * Get the price of an item. Returns 0 if sold or not listed.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static getPrice(params: CallParams<CampusMarketplaceArgs['obj']['get_price(uint64)uint64'] | CampusMarketplaceArgs['tuple']['get_price(uint64)uint64']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'get_price(uint64)uint64' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.itemId],
    }
  }
  /**
   * Constructs a no op call for the get_item_count()uint64 ABI method
   *
   * How many items have been listed total.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static getItemCount(params: CallParams<CampusMarketplaceArgs['obj']['get_item_count()uint64'] | CampusMarketplaceArgs['tuple']['get_item_count()uint64']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'get_item_count()uint64' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
}

/**
 * A client to make calls to the CampusMarketplace smart contract
 */
export class CampusMarketplaceClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `CampusMarketplaceClient`
   *
   * @param appClient An `AppClient` instance which has been created with the CampusMarketplace app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `CampusMarketplaceClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Checks for decode errors on the given return value and maps the return value to the return type for the given method
   * @returns The typed return value or undefined if there was no value
   */
  decodeReturnValue<TSignature extends CampusMarketplaceNonVoidMethodSignatures>(method: TSignature, returnValue: ABIReturn | undefined) {
    return returnValue !== undefined ? getArc56ReturnValue<MethodReturn<TSignature>>(returnValue, this.appClient.getABIMethod(method), APP_SPEC.structs) : undefined
  }

  /**
   * Returns a new `CampusMarketplaceClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<CampusMarketplaceClient> {
    return new CampusMarketplaceClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `CampusMarketplaceClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<CampusMarketplaceClient> {
    return new CampusMarketplaceClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Makes a clear_state call to an existing instance of the CampusMarketplace smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the CampusMarketplace smart contract using the `list_item(uint64)uint64` ABI method.
     *
    * List a new item for sale.
    Only the seller (contract creator) can call this. Price is in microALGO (1 ALGO = 1,000,000 microALGO). Returns the assigned item_id.

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    listItem: (params: CallParams<CampusMarketplaceArgs['obj']['list_item(uint64)uint64'] | CampusMarketplaceArgs['tuple']['list_item(uint64)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(CampusMarketplaceParamsFactory.listItem(params))
    },

    /**
     * Makes a call to the CampusMarketplace smart contract using the `buy_item(uint64,pay)void` ABI method.
     *
    * Buy an item. Must be called in an atomic group with a payment txn.
    The payment amount must match the listed price. The payment receiver must be the seller (creator).

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    buyItem: (params: CallParams<CampusMarketplaceArgs['obj']['buy_item(uint64,pay)void'] | CampusMarketplaceArgs['tuple']['buy_item(uint64,pay)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(CampusMarketplaceParamsFactory.buyItem(params))
    },

    /**
     * Makes a call to the CampusMarketplace smart contract using the `get_price(uint64)uint64` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * Get the price of an item. Returns 0 if sold or not listed.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    getPrice: (params: CallParams<CampusMarketplaceArgs['obj']['get_price(uint64)uint64'] | CampusMarketplaceArgs['tuple']['get_price(uint64)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(CampusMarketplaceParamsFactory.getPrice(params))
    },

    /**
     * Makes a call to the CampusMarketplace smart contract using the `get_item_count()uint64` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * How many items have been listed total.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    getItemCount: (params: CallParams<CampusMarketplaceArgs['obj']['get_item_count()uint64'] | CampusMarketplaceArgs['tuple']['get_item_count()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(CampusMarketplaceParamsFactory.getItemCount(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Makes a clear_state call to an existing instance of the CampusMarketplace smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the CampusMarketplace smart contract using the `list_item(uint64)uint64` ABI method.
     *
    * List a new item for sale.
    Only the seller (contract creator) can call this. Price is in microALGO (1 ALGO = 1,000,000 microALGO). Returns the assigned item_id.

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    listItem: (params: CallParams<CampusMarketplaceArgs['obj']['list_item(uint64)uint64'] | CampusMarketplaceArgs['tuple']['list_item(uint64)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(CampusMarketplaceParamsFactory.listItem(params))
    },

    /**
     * Makes a call to the CampusMarketplace smart contract using the `buy_item(uint64,pay)void` ABI method.
     *
    * Buy an item. Must be called in an atomic group with a payment txn.
    The payment amount must match the listed price. The payment receiver must be the seller (creator).

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    buyItem: (params: CallParams<CampusMarketplaceArgs['obj']['buy_item(uint64,pay)void'] | CampusMarketplaceArgs['tuple']['buy_item(uint64,pay)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(CampusMarketplaceParamsFactory.buyItem(params))
    },

    /**
     * Makes a call to the CampusMarketplace smart contract using the `get_price(uint64)uint64` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * Get the price of an item. Returns 0 if sold or not listed.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    getPrice: (params: CallParams<CampusMarketplaceArgs['obj']['get_price(uint64)uint64'] | CampusMarketplaceArgs['tuple']['get_price(uint64)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(CampusMarketplaceParamsFactory.getPrice(params))
    },

    /**
     * Makes a call to the CampusMarketplace smart contract using the `get_item_count()uint64` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * How many items have been listed total.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    getItemCount: (params: CallParams<CampusMarketplaceArgs['obj']['get_item_count()uint64'] | CampusMarketplaceArgs['tuple']['get_item_count()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(CampusMarketplaceParamsFactory.getItemCount(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Makes a clear_state call to an existing instance of the CampusMarketplace smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the CampusMarketplace smart contract using the `list_item(uint64)uint64` ABI method.
     *
    * List a new item for sale.
    Only the seller (contract creator) can call this. Price is in microALGO (1 ALGO = 1,000,000 microALGO). Returns the assigned item_id.

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    listItem: async (params: CallParams<CampusMarketplaceArgs['obj']['list_item(uint64)uint64'] | CampusMarketplaceArgs['tuple']['list_item(uint64)uint64']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(CampusMarketplaceParamsFactory.listItem(params))
      return {...result, return: result.return as unknown as (undefined | CampusMarketplaceReturns['list_item(uint64)uint64'])}
    },

    /**
     * Makes a call to the CampusMarketplace smart contract using the `buy_item(uint64,pay)void` ABI method.
     *
    * Buy an item. Must be called in an atomic group with a payment txn.
    The payment amount must match the listed price. The payment receiver must be the seller (creator).

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    buyItem: async (params: CallParams<CampusMarketplaceArgs['obj']['buy_item(uint64,pay)void'] | CampusMarketplaceArgs['tuple']['buy_item(uint64,pay)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(CampusMarketplaceParamsFactory.buyItem(params))
      return {...result, return: result.return as unknown as (undefined | CampusMarketplaceReturns['buy_item(uint64,pay)void'])}
    },

    /**
     * Makes a call to the CampusMarketplace smart contract using the `get_price(uint64)uint64` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * Get the price of an item. Returns 0 if sold or not listed.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    getPrice: async (params: CallParams<CampusMarketplaceArgs['obj']['get_price(uint64)uint64'] | CampusMarketplaceArgs['tuple']['get_price(uint64)uint64']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(CampusMarketplaceParamsFactory.getPrice(params))
      return {...result, return: result.return as unknown as (undefined | CampusMarketplaceReturns['get_price(uint64)uint64'])}
    },

    /**
     * Makes a call to the CampusMarketplace smart contract using the `get_item_count()uint64` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * How many items have been listed total.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    getItemCount: async (params: CallParams<CampusMarketplaceArgs['obj']['get_item_count()uint64'] | CampusMarketplaceArgs['tuple']['get_item_count()uint64']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(CampusMarketplaceParamsFactory.getItemCount(params))
      return {...result, return: result.return as unknown as (undefined | CampusMarketplaceReturns['get_item_count()uint64'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new CampusMarketplaceClient(this.appClient.clone(params))
  }

  /**
   * Makes a readonly (simulated) call to the CampusMarketplace smart contract using the `get_price(uint64)uint64` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * Get the price of an item. Returns 0 if sold or not listed.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async getPrice(params: CallParams<CampusMarketplaceArgs['obj']['get_price(uint64)uint64'] | CampusMarketplaceArgs['tuple']['get_price(uint64)uint64']>) {
    const result = await this.appClient.send.call(CampusMarketplaceParamsFactory.getPrice(params))
    return result.return as unknown as CampusMarketplaceReturns['get_price(uint64)uint64']
  }

  /**
   * Makes a readonly (simulated) call to the CampusMarketplace smart contract using the `get_item_count()uint64` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * How many items have been listed total.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async getItemCount(params: CallParams<CampusMarketplaceArgs['obj']['get_item_count()uint64'] | CampusMarketplaceArgs['tuple']['get_item_count()uint64']> = {args: []}) {
    const result = await this.appClient.send.call(CampusMarketplaceParamsFactory.getItemCount(params))
    return result.return as unknown as CampusMarketplaceReturns['get_item_count()uint64']
  }

  /**
   * Methods to access state for the current CampusMarketplace app
   */
  state = {
    /**
     * Methods to access global state for the current CampusMarketplace app
     */
    global: {
      /**
       * Get all current keyed values from global state
       */
      getAll: async (): Promise<Partial<Expand<GlobalKeysState>>> => {
        const result = await this.appClient.state.global.getAll()
        return {
          itemCount: result.item_count,
        }
      },
      /**
       * Get the current value of the item_count key in global state
       */
      itemCount: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("item_count")) as bigint | undefined },
    },
    /**
     * Methods to access box state for the current CampusMarketplace app
     */
    box: {
      /**
       * Get all current keyed values from box state
       */
      getAll: async (): Promise<Partial<Expand<BoxKeysState>>> => {
        const result = await this.appClient.state.box.getAll()
        return {
        }
      },
      /**
       * Get values from the prices map in box state
       */
      prices: {
        /**
         * Get all current values of the prices map in box state
         */
        getMap: async (): Promise<Map<bigint, bigint>> => { return (await this.appClient.state.box.getMap("prices")) as Map<bigint, bigint> },
        /**
         * Get a current value of the prices map by key from box state
         */
        value: async (key: bigint | number): Promise<bigint | undefined> => { return await this.appClient.state.box.getMapValue("prices", key) as bigint | undefined },
      },
    },
  }

  public newGroup(): CampusMarketplaceComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a list_item(uint64)uint64 method call against the CampusMarketplace contract
       */
      listItem(params: CallParams<CampusMarketplaceArgs['obj']['list_item(uint64)uint64'] | CampusMarketplaceArgs['tuple']['list_item(uint64)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.listItem(params)))
        resultMappers.push((v) => client.decodeReturnValue('list_item(uint64)uint64', v))
        return this
      },
      /**
       * Add a buy_item(uint64,pay)void method call against the CampusMarketplace contract
       */
      buyItem(params: CallParams<CampusMarketplaceArgs['obj']['buy_item(uint64,pay)void'] | CampusMarketplaceArgs['tuple']['buy_item(uint64,pay)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.buyItem(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a get_price(uint64)uint64 method call against the CampusMarketplace contract
       */
      getPrice(params: CallParams<CampusMarketplaceArgs['obj']['get_price(uint64)uint64'] | CampusMarketplaceArgs['tuple']['get_price(uint64)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.getPrice(params)))
        resultMappers.push((v) => client.decodeReturnValue('get_price(uint64)uint64', v))
        return this
      },
      /**
       * Add a get_item_count()uint64 method call against the CampusMarketplace contract
       */
      getItemCount(params: CallParams<CampusMarketplaceArgs['obj']['get_item_count()uint64'] | CampusMarketplaceArgs['tuple']['get_item_count()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.getItemCount(params)))
        resultMappers.push((v) => client.decodeReturnValue('get_item_count()uint64', v))
        return this
      },
      /**
       * Add a clear state call to the CampusMarketplace contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as CampusMarketplaceComposer
  }
}
export type CampusMarketplaceComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the list_item(uint64)uint64 ABI method.
   *
  * List a new item for sale.
  Only the seller (contract creator) can call this. Price is in microALGO (1 ALGO = 1,000,000 microALGO). Returns the assigned item_id.

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  listItem(params?: CallParams<CampusMarketplaceArgs['obj']['list_item(uint64)uint64'] | CampusMarketplaceArgs['tuple']['list_item(uint64)uint64']>): CampusMarketplaceComposer<[...TReturns, CampusMarketplaceReturns['list_item(uint64)uint64'] | undefined]>

  /**
   * Calls the buy_item(uint64,pay)void ABI method.
   *
  * Buy an item. Must be called in an atomic group with a payment txn.
  The payment amount must match the listed price. The payment receiver must be the seller (creator).

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  buyItem(params?: CallParams<CampusMarketplaceArgs['obj']['buy_item(uint64,pay)void'] | CampusMarketplaceArgs['tuple']['buy_item(uint64,pay)void']>): CampusMarketplaceComposer<[...TReturns, CampusMarketplaceReturns['buy_item(uint64,pay)void'] | undefined]>

  /**
   * Calls the get_price(uint64)uint64 ABI method.
   *
   * Get the price of an item. Returns 0 if sold or not listed.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  getPrice(params?: CallParams<CampusMarketplaceArgs['obj']['get_price(uint64)uint64'] | CampusMarketplaceArgs['tuple']['get_price(uint64)uint64']>): CampusMarketplaceComposer<[...TReturns, CampusMarketplaceReturns['get_price(uint64)uint64'] | undefined]>

  /**
   * Calls the get_item_count()uint64 ABI method.
   *
   * How many items have been listed total.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  getItemCount(params?: CallParams<CampusMarketplaceArgs['obj']['get_item_count()uint64'] | CampusMarketplaceArgs['tuple']['get_item_count()uint64']>): CampusMarketplaceComposer<[...TReturns, CampusMarketplaceReturns['get_item_count()uint64'] | undefined]>

  /**
   * Makes a clear_state call to an existing instance of the CampusMarketplace smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): CampusMarketplaceComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): CampusMarketplaceComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<CampusMarketplaceComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<CampusMarketplaceComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<CampusMarketplaceComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<CampusMarketplaceComposerResults<TReturns>>
}
export type CampusMarketplaceComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

