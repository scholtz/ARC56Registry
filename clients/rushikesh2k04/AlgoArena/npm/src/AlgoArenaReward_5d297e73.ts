/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"AlgoArenaReward","structs":{},"methods":[{"name":"create","args":[],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Sets the contract owner to the sender on application creation.","events":[],"recommendations":{}},{"name":"reward_user","args":[{"type":"address","name":"recipient_address"},{"type":"uint64","name":"algo_reward_amount"},{"type":"uint64","name":"nft_asset_id"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Distributes Algos and an NFT to a recipient.","events":[],"recommendations":{}}],"arcs":[22,28],"desc":"\n    ARC-4 Contract for distributing Algo and NFT rewards to AlgoArena users.\n    The contract owner (game server) is the only entity authorized to call \n    the reward_user method, which executes two Inner Transactions atomically:\n    1. An Algo payment to the winner.\n    2. An Asset Transfer (NFT) to the winner.\n    ","networks":{},"state":{"schema":{"global":{"ints":0,"bytes":1},"local":{"ints":0,"bytes":0}},"keys":{"global":{"owner":{"keyType":"AVMString","valueType":"address","key":"b3duZXJfYWRkcg=="}},"local":{},"box":{}},"maps":{"global":{},"local":{},"box":{}}},"bareActions":{"create":["NoOp"],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[36],"errorMessage":"OnCompletion must be NoOp"},{"pc":[69],"errorMessage":"OnCompletion must be NoOp && can only call when creating"},{"pc":[110],"errorMessage":"Only the AlgoArena owner/server can issue rewards."},{"pc":[106],"errorMessage":"check self.owner exists"},{"pc":[86],"errorMessage":"invalid number of bytes for arc4.static_array<arc4.uint8, 32>"},{"pc":[94,102],"errorMessage":"invalid number of bytes for arc4.uint64"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"events":[]} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


/**
 * The argument types for the AlgoArenaReward contract
 */
export type AlgoArenaRewardArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'create()void': Record<string, never>
    'reward_user(address,uint64,uint64)void': {
      recipientAddress: string
      algoRewardAmount: bigint | number
      nftAssetId: bigint | number
    }
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'create()void': []
    'reward_user(address,uint64,uint64)void': [recipientAddress: string, algoRewardAmount: bigint | number, nftAssetId: bigint | number]
  }
}

/**
 * The return type for each method
 */
export type AlgoArenaRewardReturns = {
  'create()void': void
  'reward_user(address,uint64,uint64)void': void
}

/**
 * Defines the types of available calls and state of the AlgoArenaReward smart contract.
 */
export type AlgoArenaRewardTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'create()void' | 'create', {
      argsObj: AlgoArenaRewardArgs['obj']['create()void']
      argsTuple: AlgoArenaRewardArgs['tuple']['create()void']
      returns: AlgoArenaRewardReturns['create()void']
    }>
    & Record<'reward_user(address,uint64,uint64)void' | 'reward_user', {
      argsObj: AlgoArenaRewardArgs['obj']['reward_user(address,uint64,uint64)void']
      argsTuple: AlgoArenaRewardArgs['tuple']['reward_user(address,uint64,uint64)void']
      returns: AlgoArenaRewardReturns['reward_user(address,uint64,uint64)void']
    }>
  /**
   * Defines the shape of the state of the application.
   */
  state: {
    global: {
      keys: {
        owner: string
      }
      maps: {}
    }
  }
}

/**
 * Defines the possible abi call signatures.
 */
export type AlgoArenaRewardSignatures = keyof AlgoArenaRewardTypes['methods']
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the AlgoArenaReward smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends AlgoArenaRewardSignatures> = AlgoArenaRewardTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the AlgoArenaReward smart contract to the method's return type
 */
export type MethodReturn<TSignature extends AlgoArenaRewardSignatures> = AlgoArenaRewardTypes['methods'][TSignature]['returns']

/**
 * Defines the shape of the keyed global state of the application.
 */
export type GlobalKeysState = AlgoArenaRewardTypes['state']['global']['keys']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the AlgoArenaReward smart contract
 */
export abstract class AlgoArenaRewardParamsFactory {
  /**
   * Constructs a no op call for the create()void ABI method
   *
   * Sets the contract owner to the sender on application creation.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static create(params: CallParams<AlgoArenaRewardArgs['obj']['create()void'] | AlgoArenaRewardArgs['tuple']['create()void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'create()void' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the reward_user(address,uint64,uint64)void ABI method
   *
   * Distributes Algos and an NFT to a recipient.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static rewardUser(params: CallParams<AlgoArenaRewardArgs['obj']['reward_user(address,uint64,uint64)void'] | AlgoArenaRewardArgs['tuple']['reward_user(address,uint64,uint64)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'reward_user(address,uint64,uint64)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.recipientAddress, params.args.algoRewardAmount, params.args.nftAssetId],
    }
  }
}

/**
 * A client to make calls to the AlgoArenaReward smart contract
 */
export class AlgoArenaRewardClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `AlgoArenaRewardClient`
   *
   * @param appClient An `AppClient` instance which has been created with the AlgoArenaReward app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `AlgoArenaRewardClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Returns a new `AlgoArenaRewardClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<AlgoArenaRewardClient> {
    return new AlgoArenaRewardClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `AlgoArenaRewardClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<AlgoArenaRewardClient> {
    return new AlgoArenaRewardClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Makes a clear_state call to an existing instance of the AlgoArenaReward smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the AlgoArenaReward smart contract using the `create()void` ABI method.
     *
     * Sets the contract owner to the sender on application creation.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    create: (params: CallParams<AlgoArenaRewardArgs['obj']['create()void'] | AlgoArenaRewardArgs['tuple']['create()void']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(AlgoArenaRewardParamsFactory.create(params))
    },

    /**
     * Makes a call to the AlgoArenaReward smart contract using the `reward_user(address,uint64,uint64)void` ABI method.
     *
     * Distributes Algos and an NFT to a recipient.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    rewardUser: (params: CallParams<AlgoArenaRewardArgs['obj']['reward_user(address,uint64,uint64)void'] | AlgoArenaRewardArgs['tuple']['reward_user(address,uint64,uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(AlgoArenaRewardParamsFactory.rewardUser(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Makes a clear_state call to an existing instance of the AlgoArenaReward smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the AlgoArenaReward smart contract using the `create()void` ABI method.
     *
     * Sets the contract owner to the sender on application creation.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    create: (params: CallParams<AlgoArenaRewardArgs['obj']['create()void'] | AlgoArenaRewardArgs['tuple']['create()void']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(AlgoArenaRewardParamsFactory.create(params))
    },

    /**
     * Makes a call to the AlgoArenaReward smart contract using the `reward_user(address,uint64,uint64)void` ABI method.
     *
     * Distributes Algos and an NFT to a recipient.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    rewardUser: (params: CallParams<AlgoArenaRewardArgs['obj']['reward_user(address,uint64,uint64)void'] | AlgoArenaRewardArgs['tuple']['reward_user(address,uint64,uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(AlgoArenaRewardParamsFactory.rewardUser(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Makes a clear_state call to an existing instance of the AlgoArenaReward smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the AlgoArenaReward smart contract using the `create()void` ABI method.
     *
     * Sets the contract owner to the sender on application creation.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    create: async (params: CallParams<AlgoArenaRewardArgs['obj']['create()void'] | AlgoArenaRewardArgs['tuple']['create()void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(AlgoArenaRewardParamsFactory.create(params))
      return {...result, return: result.return as unknown as (undefined | AlgoArenaRewardReturns['create()void'])}
    },

    /**
     * Makes a call to the AlgoArenaReward smart contract using the `reward_user(address,uint64,uint64)void` ABI method.
     *
     * Distributes Algos and an NFT to a recipient.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    rewardUser: async (params: CallParams<AlgoArenaRewardArgs['obj']['reward_user(address,uint64,uint64)void'] | AlgoArenaRewardArgs['tuple']['reward_user(address,uint64,uint64)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(AlgoArenaRewardParamsFactory.rewardUser(params))
      return {...result, return: result.return as unknown as (undefined | AlgoArenaRewardReturns['reward_user(address,uint64,uint64)void'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new AlgoArenaRewardClient(this.appClient.clone(params))
  }

  /**
   * Methods to access state for the current AlgoArenaReward app
   */
  state = {
    /**
     * Methods to access global state for the current AlgoArenaReward app
     */
    global: {
      /**
       * Get all current keyed values from global state
       */
      getAll: async (): Promise<Partial<Expand<GlobalKeysState>>> => {
        const result = await this.appClient.state.global.getAll()
        return {
          owner: result.owner,
        }
      },
      /**
       * Get the current value of the owner key in global state
       */
      owner: async (): Promise<string | undefined> => { return (await this.appClient.state.global.getValue("owner")) as string | undefined },
    },
  }

  public newGroup(): AlgoArenaRewardComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a create()void method call against the AlgoArenaReward contract
       */
      create(params: CallParams<AlgoArenaRewardArgs['obj']['create()void'] | AlgoArenaRewardArgs['tuple']['create()void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.create(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a reward_user(address,uint64,uint64)void method call against the AlgoArenaReward contract
       */
      rewardUser(params: CallParams<AlgoArenaRewardArgs['obj']['reward_user(address,uint64,uint64)void'] | AlgoArenaRewardArgs['tuple']['reward_user(address,uint64,uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.rewardUser(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a clear state call to the AlgoArenaReward contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as AlgoArenaRewardComposer
  }
}
export type AlgoArenaRewardComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the create()void ABI method.
   *
   * Sets the contract owner to the sender on application creation.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  create(params?: CallParams<AlgoArenaRewardArgs['obj']['create()void'] | AlgoArenaRewardArgs['tuple']['create()void']>): AlgoArenaRewardComposer<[...TReturns, AlgoArenaRewardReturns['create()void'] | undefined]>

  /**
   * Calls the reward_user(address,uint64,uint64)void ABI method.
   *
   * Distributes Algos and an NFT to a recipient.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  rewardUser(params?: CallParams<AlgoArenaRewardArgs['obj']['reward_user(address,uint64,uint64)void'] | AlgoArenaRewardArgs['tuple']['reward_user(address,uint64,uint64)void']>): AlgoArenaRewardComposer<[...TReturns, AlgoArenaRewardReturns['reward_user(address,uint64,uint64)void'] | undefined]>

  /**
   * Makes a clear_state call to an existing instance of the AlgoArenaReward smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): AlgoArenaRewardComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): AlgoArenaRewardComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<AlgoArenaRewardComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<AlgoArenaRewardComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<AlgoArenaRewardComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<AlgoArenaRewardComposerResults<TReturns>>
}
export type AlgoArenaRewardComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

