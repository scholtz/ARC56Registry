/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"CrescaBucketProtocol","structs":{"Bucket":[{"name":"asset0","type":"uint64"},{"name":"asset1","type":"uint64"},{"name":"asset2","type":"uint64"},{"name":"asset3","type":"uint64"},{"name":"asset4","type":"uint64"},{"name":"asset5","type":"uint64"},{"name":"asset6","type":"uint64"},{"name":"asset7","type":"uint64"},{"name":"weight0","type":"uint64"},{"name":"weight1","type":"uint64"},{"name":"weight2","type":"uint64"},{"name":"weight3","type":"uint64"},{"name":"weight4","type":"uint64"},{"name":"weight5","type":"uint64"},{"name":"weight6","type":"uint64"},{"name":"weight7","type":"uint64"},{"name":"assetCount","type":"uint64"},{"name":"leverage","type":"uint64"},{"name":"owner","type":"address"},{"name":"exists","type":"bool"}],"Position":[{"name":"bucketId","type":"uint64"},{"name":"isLong","type":"bool"},{"name":"margin","type":"uint64"},{"name":"entryPrice","type":"uint64"},{"name":"owner","type":"address"},{"name":"active","type":"bool"},{"name":"openTimestamp","type":"uint64"}],"PriceData":[{"name":"price","type":"uint64"},{"name":"timestamp","type":"uint64"}]},"methods":[{"name":"create_bucket","args":[{"type":"uint64[]","name":"assets"},{"type":"uint64[]","name":"weights"},{"type":"uint64","name":"leverage"}],"returns":{"type":"uint64"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Create a custom asset basket.\nassets  — up to 8 Algorand ASA IDs (use 0 for ALGO itself) weights — percentage allocation per asset (must sum to 100) leverage — 1 to 150","events":[{"name":"BucketCreated","args":[{"type":"uint64","name":"field1"},{"type":"address","name":"field2"},{"type":"uint64[]","name":"field3"},{"type":"uint64[]","name":"field4"},{"type":"uint64","name":"field5"}]}],"recommendations":{}},{"name":"deposit_collateral","args":[{"type":"pay","name":"payment_txn"}],"returns":{"type":"bool"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Deposit ALGO as collateral for trading.\nAttach a PaymentTransaction to this contract in the same group.","events":[{"name":"CollateralDeposited","args":[{"type":"address","name":"field1"},{"type":"uint64","name":"field2"}]}],"recommendations":{}},{"name":"withdraw_collateral","args":[{"type":"uint64","name":"amount"}],"returns":{"type":"bool"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Withdraw ALGO collateral back to sender.","events":[{"name":"CollateralWithdrawn","args":[{"type":"address","name":"field1"},{"type":"uint64","name":"field2"}]}],"recommendations":{}},{"name":"open_position","args":[{"type":"uint64","name":"bucket_id"},{"type":"bool","name":"is_long"},{"type":"uint64","name":"margin"}],"returns":{"type":"uint64"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Open a leveraged long or short position on a bucket.\nbucket_id — ID of the bucket owned by Txn.sender is_long   — True = long, False = short margin    — μALGO to lock from collateral balance","events":[{"name":"PositionOpened","args":[{"type":"uint64","name":"field1"},{"type":"uint64","name":"field2"},{"type":"address","name":"field3"},{"type":"bool","name":"field4"},{"type":"uint64","name":"field5"},{"type":"uint64","name":"field6"}]}],"recommendations":{}},{"name":"close_position","args":[{"type":"uint64","name":"position_id"}],"returns":{"type":"uint64"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Close a position and realise P&L back into the collateral balance.\nReturns absolute P&L in μALGO.","events":[{"name":"PositionClosed","args":[{"type":"uint64","name":"field1"},{"type":"address","name":"field2"},{"type":"uint64","name":"field3"},{"type":"bool","name":"field4"}]}],"recommendations":{}},{"name":"rebalance_bucket","args":[{"type":"uint64","name":"bucket_id"},{"type":"uint64[]","name":"new_weights"}],"returns":{"type":"bool"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Update the weight allocation of an existing bucket (owner only).","events":[{"name":"BucketRebalanced","args":[{"type":"uint64","name":"field1"},{"type":"uint64[]","name":"field2"}]}],"recommendations":{}},{"name":"update_oracle","args":[{"type":"uint64[]","name":"asset_ids"},{"type":"uint64[]","name":"asset_prices"}],"returns":{"type":"bool"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Update mock oracle prices. In production, consume a Pyth price feed\nvia a Pyth Algorand oracle contract instead.\nPrices must use 8-decimal precision (e.g. 1 ALGO = 100_000_000).","events":[{"name":"OracleUpdated","args":[{"type":"uint64","name":"field1"},{"type":"uint64","name":"field2"}]}],"recommendations":{}},{"name":"liquidate_position","args":[{"type":"address","name":"owner"},{"type":"uint64","name":"position_id"}],"returns":{"type":"bool"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Liquidate an undercollateralised position.\nAnyone may call this (keeper/bot pattern — same as Solidity version).","events":[{"name":"Liquidation","args":[{"type":"uint64","name":"field1"},{"type":"address","name":"field2"},{"type":"byte[]","name":"field3"}]}],"recommendations":{}},{"name":"get_collateral_balance","args":[{"type":"address","name":"user"}],"returns":{"type":"uint64"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"get_unrealized_pnl","args":[{"type":"address","name":"owner"},{"type":"uint64","name":"position_id"}],"returns":{"type":"uint64"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"get_oracle_updated_at","args":[],"returns":{"type":"uint64"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"desc":"Returns Unix timestamp of the last oracle update (0 if never updated).","events":[],"recommendations":{}},{"name":"get_total_positions","args":[],"returns":{"type":"uint64"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"get_total_buckets","args":[],"returns":{"type":"uint64"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"fund_contract","args":[{"type":"pay","name":"payment_txn"}],"returns":{"type":"bool"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Increase contract's minimum balance to cover Box storage costs.","events":[],"recommendations":{}}],"arcs":[22,28],"desc":"\n    Leveraged basket trading protocol on Algorand.\n\n    State:\n    - Global: total_positions, total_buckets, oracle_updated_at\n    - Boxes:  Bucket, Position, PriceData per entry\n    ","networks":{},"state":{"schema":{"global":{"ints":3,"bytes":0},"local":{"ints":0,"bytes":0}},"keys":{"global":{"total_positions":{"keyType":"AVMString","valueType":"AVMUint64","key":"dG90YWxfcG9zaXRpb25z"},"total_buckets":{"keyType":"AVMString","valueType":"AVMUint64","key":"dG90YWxfYnVja2V0cw=="},"oracle_updated_at":{"keyType":"AVMString","valueType":"AVMUint64","key":"b3JhY2xlX3VwZGF0ZWRfYXQ="}},"local":{},"box":{}},"maps":{"global":{},"local":{},"box":{"collateral":{"keyType":"address","valueType":"uint64","prefix":"Y29sXw=="},"bucket_counts":{"keyType":"address","valueType":"uint64","prefix":"YmtjXw=="},"buckets":{"keyType":"AVMBytes","valueType":"Bucket","prefix":""},"positions":{"keyType":"AVMBytes","valueType":"Position","prefix":""},"prices":{"keyType":"AVMBytes","valueType":"PriceData","prefix":""}}}},"bareActions":{"create":["NoOp"],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[896,2454],"errorMessage":"Amount must be > 0"},{"pc":[1072,1087,1532],"errorMessage":"Bucket not found"},{"pc":[979,1115],"errorMessage":"Insufficient collateral"},{"pc":[315,1951],"errorMessage":"Length mismatch"},{"pc":[330],"errorMessage":"Leverage too high"},{"pc":[325],"errorMessage":"Leverage too low"},{"pc":[322],"errorMessage":"Max 8 assets per bucket"},{"pc":[318],"errorMessage":"Must have at least 1 asset"},{"pc":[892,2451],"errorMessage":"Must pay contract"},{"pc":[969,1105],"errorMessage":"No collateral balance"},{"pc":[1095,1548],"errorMessage":"Not your bucket"},{"pc":[1275],"errorMessage":"Not your position"},{"pc":[1053,1230,2056],"errorMessage":"Oracle price stale"},{"pc":[1267,2087],"errorMessage":"Position already closed"},{"pc":[1252,2073],"errorMessage":"Position not found"},{"pc":[2153],"errorMessage":"Position not liquidatable"},{"pc":[1560],"errorMessage":"Weight count mismatch"},{"pc":[370,1600],"errorMessage":"Weights must sum to 100"},{"pc":[396],"errorMessage":"check self.bucket_counts entry exists"},{"pc":[1291,2100,2385],"errorMessage":"check self.buckets entry exists"},{"pc":[914,1409,2292],"errorMessage":"check self.collateral entry exists"},{"pc":[1048,1225,2051,2403],"errorMessage":"check self.oracle_updated_at exists"},{"pc":[2353],"errorMessage":"check self.positions entry exists"},{"pc":[2515],"errorMessage":"check self.prices entry exists"},{"pc":[726,2427],"errorMessage":"check self.total_buckets exists"},{"pc":[1127,2415],"errorMessage":"check self.total_positions exists"},{"pc":[1976,1985],"errorMessage":"index access is out of bounds"},{"pc":[260,285,1498,1917,1939],"errorMessage":"invalid array length header"},{"pc":[1034],"errorMessage":"invalid number of bytes for arc4.bool"},{"pc":[276,298,1511,1930,1949],"errorMessage":"invalid number of bytes for arc4.dynamic_array<arc4.uint64>"},{"pc":[2034,2278,2313],"errorMessage":"invalid number of bytes for arc4.static_array<arc4.uint8, 32>"},{"pc":[309,957,1026,1042,1219,1489,2045,2321],"errorMessage":"invalid number of bytes for arc4.uint64"},{"pc":[885,2444],"errorMessage":"transaction type is pay"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"events":[{"name":"BucketCreated","args":[{"type":"uint64","name":"field1"},{"type":"address","name":"field2"},{"type":"uint64[]","name":"field3"},{"type":"uint64[]","name":"field4"},{"type":"uint64","name":"field5"}]},{"name":"CollateralDeposited","args":[{"type":"address","name":"field1"},{"type":"uint64","name":"field2"}]},{"name":"CollateralWithdrawn","args":[{"type":"address","name":"field1"},{"type":"uint64","name":"field2"}]},{"name":"PositionOpened","args":[{"type":"uint64","name":"field1"},{"type":"uint64","name":"field2"},{"type":"address","name":"field3"},{"type":"bool","name":"field4"},{"type":"uint64","name":"field5"},{"type":"uint64","name":"field6"}]},{"name":"PositionClosed","args":[{"type":"uint64","name":"field1"},{"type":"address","name":"field2"},{"type":"uint64","name":"field3"},{"type":"bool","name":"field4"}]},{"name":"BucketRebalanced","args":[{"type":"uint64","name":"field1"},{"type":"uint64[]","name":"field2"}]},{"name":"OracleUpdated","args":[{"type":"uint64","name":"field1"},{"type":"uint64","name":"field2"}]},{"name":"Liquidation","args":[{"type":"uint64","name":"field1"},{"type":"address","name":"field2"},{"type":"byte[]","name":"field3"}]}]} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


// Type definitions for ARC-56 structs

export type Bucket = {
  asset0: bigint,
  asset1: bigint,
  asset2: bigint,
  asset3: bigint,
  asset4: bigint,
  asset5: bigint,
  asset6: bigint,
  asset7: bigint,
  weight0: bigint,
  weight1: bigint,
  weight2: bigint,
  weight3: bigint,
  weight4: bigint,
  weight5: bigint,
  weight6: bigint,
  weight7: bigint,
  assetCount: bigint,
  leverage: bigint,
  owner: string,
  exists: boolean
}


/**
 * Converts the ABI tuple representation of a Bucket to the struct representation
 */
export function BucketFromTuple(abiTuple: [bigint, bigint, bigint, bigint, bigint, bigint, bigint, bigint, bigint, bigint, bigint, bigint, bigint, bigint, bigint, bigint, bigint, bigint, string, boolean]) {
  return getABIStructFromABITuple(abiTuple, APP_SPEC.structs.Bucket, APP_SPEC.structs) as Bucket
}

export type Position = {
  bucketId: bigint,
  isLong: boolean,
  margin: bigint,
  entryPrice: bigint,
  owner: string,
  active: boolean,
  openTimestamp: bigint
}


/**
 * Converts the ABI tuple representation of a Position to the struct representation
 */
export function PositionFromTuple(abiTuple: [bigint, boolean, bigint, bigint, string, boolean, bigint]) {
  return getABIStructFromABITuple(abiTuple, APP_SPEC.structs.Position, APP_SPEC.structs) as Position
}

export type PriceData = {
  price: bigint,
  timestamp: bigint
}


/**
 * Converts the ABI tuple representation of a PriceData to the struct representation
 */
export function PriceDataFromTuple(abiTuple: [bigint, bigint]) {
  return getABIStructFromABITuple(abiTuple, APP_SPEC.structs.PriceData, APP_SPEC.structs) as PriceData
}

/**
 * The argument types for the CrescaBucketProtocol contract
 */
export type CrescaBucketProtocolArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'create_bucket(uint64[],uint64[],uint64)uint64': {
      assets: bigint[] | number[]
      weights: bigint[] | number[]
      leverage: bigint | number
    }
    'deposit_collateral(pay)bool': {
      paymentTxn: AppMethodCallTransactionArgument
    }
    'withdraw_collateral(uint64)bool': {
      amount: bigint | number
    }
    'open_position(uint64,bool,uint64)uint64': {
      bucketId: bigint | number
      isLong: boolean
      margin: bigint | number
    }
    'close_position(uint64)uint64': {
      positionId: bigint | number
    }
    'rebalance_bucket(uint64,uint64[])bool': {
      bucketId: bigint | number
      newWeights: bigint[] | number[]
    }
    'update_oracle(uint64[],uint64[])bool': {
      assetIds: bigint[] | number[]
      assetPrices: bigint[] | number[]
    }
    'liquidate_position(address,uint64)bool': {
      owner: string
      positionId: bigint | number
    }
    'get_collateral_balance(address)uint64': {
      user: string
    }
    'get_unrealized_pnl(address,uint64)uint64': {
      owner: string
      positionId: bigint | number
    }
    'get_oracle_updated_at()uint64': Record<string, never>
    'get_total_positions()uint64': Record<string, never>
    'get_total_buckets()uint64': Record<string, never>
    'fund_contract(pay)bool': {
      paymentTxn: AppMethodCallTransactionArgument
    }
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'create_bucket(uint64[],uint64[],uint64)uint64': [assets: bigint[] | number[], weights: bigint[] | number[], leverage: bigint | number]
    'deposit_collateral(pay)bool': [paymentTxn: AppMethodCallTransactionArgument]
    'withdraw_collateral(uint64)bool': [amount: bigint | number]
    'open_position(uint64,bool,uint64)uint64': [bucketId: bigint | number, isLong: boolean, margin: bigint | number]
    'close_position(uint64)uint64': [positionId: bigint | number]
    'rebalance_bucket(uint64,uint64[])bool': [bucketId: bigint | number, newWeights: bigint[] | number[]]
    'update_oracle(uint64[],uint64[])bool': [assetIds: bigint[] | number[], assetPrices: bigint[] | number[]]
    'liquidate_position(address,uint64)bool': [owner: string, positionId: bigint | number]
    'get_collateral_balance(address)uint64': [user: string]
    'get_unrealized_pnl(address,uint64)uint64': [owner: string, positionId: bigint | number]
    'get_oracle_updated_at()uint64': []
    'get_total_positions()uint64': []
    'get_total_buckets()uint64': []
    'fund_contract(pay)bool': [paymentTxn: AppMethodCallTransactionArgument]
  }
}

/**
 * The return type for each method
 */
export type CrescaBucketProtocolReturns = {
  'create_bucket(uint64[],uint64[],uint64)uint64': bigint
  'deposit_collateral(pay)bool': boolean
  'withdraw_collateral(uint64)bool': boolean
  'open_position(uint64,bool,uint64)uint64': bigint
  'close_position(uint64)uint64': bigint
  'rebalance_bucket(uint64,uint64[])bool': boolean
  'update_oracle(uint64[],uint64[])bool': boolean
  'liquidate_position(address,uint64)bool': boolean
  'get_collateral_balance(address)uint64': bigint
  'get_unrealized_pnl(address,uint64)uint64': bigint
  'get_oracle_updated_at()uint64': bigint
  'get_total_positions()uint64': bigint
  'get_total_buckets()uint64': bigint
  'fund_contract(pay)bool': boolean
}

/**
 * Defines the types of available calls and state of the CrescaBucketProtocol smart contract.
 */
export type CrescaBucketProtocolTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'create_bucket(uint64[],uint64[],uint64)uint64' | 'create_bucket', {
      argsObj: CrescaBucketProtocolArgs['obj']['create_bucket(uint64[],uint64[],uint64)uint64']
      argsTuple: CrescaBucketProtocolArgs['tuple']['create_bucket(uint64[],uint64[],uint64)uint64']
      returns: CrescaBucketProtocolReturns['create_bucket(uint64[],uint64[],uint64)uint64']
    }>
    & Record<'deposit_collateral(pay)bool' | 'deposit_collateral', {
      argsObj: CrescaBucketProtocolArgs['obj']['deposit_collateral(pay)bool']
      argsTuple: CrescaBucketProtocolArgs['tuple']['deposit_collateral(pay)bool']
      returns: CrescaBucketProtocolReturns['deposit_collateral(pay)bool']
    }>
    & Record<'withdraw_collateral(uint64)bool' | 'withdraw_collateral', {
      argsObj: CrescaBucketProtocolArgs['obj']['withdraw_collateral(uint64)bool']
      argsTuple: CrescaBucketProtocolArgs['tuple']['withdraw_collateral(uint64)bool']
      returns: CrescaBucketProtocolReturns['withdraw_collateral(uint64)bool']
    }>
    & Record<'open_position(uint64,bool,uint64)uint64' | 'open_position', {
      argsObj: CrescaBucketProtocolArgs['obj']['open_position(uint64,bool,uint64)uint64']
      argsTuple: CrescaBucketProtocolArgs['tuple']['open_position(uint64,bool,uint64)uint64']
      returns: CrescaBucketProtocolReturns['open_position(uint64,bool,uint64)uint64']
    }>
    & Record<'close_position(uint64)uint64' | 'close_position', {
      argsObj: CrescaBucketProtocolArgs['obj']['close_position(uint64)uint64']
      argsTuple: CrescaBucketProtocolArgs['tuple']['close_position(uint64)uint64']
      returns: CrescaBucketProtocolReturns['close_position(uint64)uint64']
    }>
    & Record<'rebalance_bucket(uint64,uint64[])bool' | 'rebalance_bucket', {
      argsObj: CrescaBucketProtocolArgs['obj']['rebalance_bucket(uint64,uint64[])bool']
      argsTuple: CrescaBucketProtocolArgs['tuple']['rebalance_bucket(uint64,uint64[])bool']
      returns: CrescaBucketProtocolReturns['rebalance_bucket(uint64,uint64[])bool']
    }>
    & Record<'update_oracle(uint64[],uint64[])bool' | 'update_oracle', {
      argsObj: CrescaBucketProtocolArgs['obj']['update_oracle(uint64[],uint64[])bool']
      argsTuple: CrescaBucketProtocolArgs['tuple']['update_oracle(uint64[],uint64[])bool']
      returns: CrescaBucketProtocolReturns['update_oracle(uint64[],uint64[])bool']
    }>
    & Record<'liquidate_position(address,uint64)bool' | 'liquidate_position', {
      argsObj: CrescaBucketProtocolArgs['obj']['liquidate_position(address,uint64)bool']
      argsTuple: CrescaBucketProtocolArgs['tuple']['liquidate_position(address,uint64)bool']
      returns: CrescaBucketProtocolReturns['liquidate_position(address,uint64)bool']
    }>
    & Record<'get_collateral_balance(address)uint64' | 'get_collateral_balance', {
      argsObj: CrescaBucketProtocolArgs['obj']['get_collateral_balance(address)uint64']
      argsTuple: CrescaBucketProtocolArgs['tuple']['get_collateral_balance(address)uint64']
      returns: CrescaBucketProtocolReturns['get_collateral_balance(address)uint64']
    }>
    & Record<'get_unrealized_pnl(address,uint64)uint64' | 'get_unrealized_pnl', {
      argsObj: CrescaBucketProtocolArgs['obj']['get_unrealized_pnl(address,uint64)uint64']
      argsTuple: CrescaBucketProtocolArgs['tuple']['get_unrealized_pnl(address,uint64)uint64']
      returns: CrescaBucketProtocolReturns['get_unrealized_pnl(address,uint64)uint64']
    }>
    & Record<'get_oracle_updated_at()uint64' | 'get_oracle_updated_at', {
      argsObj: CrescaBucketProtocolArgs['obj']['get_oracle_updated_at()uint64']
      argsTuple: CrescaBucketProtocolArgs['tuple']['get_oracle_updated_at()uint64']
      returns: CrescaBucketProtocolReturns['get_oracle_updated_at()uint64']
    }>
    & Record<'get_total_positions()uint64' | 'get_total_positions', {
      argsObj: CrescaBucketProtocolArgs['obj']['get_total_positions()uint64']
      argsTuple: CrescaBucketProtocolArgs['tuple']['get_total_positions()uint64']
      returns: CrescaBucketProtocolReturns['get_total_positions()uint64']
    }>
    & Record<'get_total_buckets()uint64' | 'get_total_buckets', {
      argsObj: CrescaBucketProtocolArgs['obj']['get_total_buckets()uint64']
      argsTuple: CrescaBucketProtocolArgs['tuple']['get_total_buckets()uint64']
      returns: CrescaBucketProtocolReturns['get_total_buckets()uint64']
    }>
    & Record<'fund_contract(pay)bool' | 'fund_contract', {
      argsObj: CrescaBucketProtocolArgs['obj']['fund_contract(pay)bool']
      argsTuple: CrescaBucketProtocolArgs['tuple']['fund_contract(pay)bool']
      returns: CrescaBucketProtocolReturns['fund_contract(pay)bool']
    }>
  /**
   * Defines the shape of the state of the application.
   */
  state: {
    global: {
      keys: {
        totalPositions: bigint
        totalBuckets: bigint
        oracleUpdatedAt: bigint
      }
      maps: {}
    }
    box: {
      keys: {}
      maps: {
        collateral: Map<string, bigint>
        bucketCounts: Map<string, bigint>
        buckets: Map<Uint8Array | string, Bucket>
        positions: Map<Uint8Array | string, Position>
        prices: Map<Uint8Array | string, PriceData>
      }
    }
  }
}

/**
 * Defines the possible abi call signatures.
 */
export type CrescaBucketProtocolSignatures = keyof CrescaBucketProtocolTypes['methods']
/**
 * Defines the possible abi call signatures for methods that return a non-void value.
 */
export type CrescaBucketProtocolNonVoidMethodSignatures = keyof CrescaBucketProtocolTypes['methods'] extends infer T ? T extends keyof CrescaBucketProtocolTypes['methods'] ? MethodReturn<T> extends void ? never : T  : never : never
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the CrescaBucketProtocol smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends CrescaBucketProtocolSignatures> = CrescaBucketProtocolTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the CrescaBucketProtocol smart contract to the method's return type
 */
export type MethodReturn<TSignature extends CrescaBucketProtocolSignatures> = CrescaBucketProtocolTypes['methods'][TSignature]['returns']

/**
 * Defines the shape of the keyed global state of the application.
 */
export type GlobalKeysState = CrescaBucketProtocolTypes['state']['global']['keys']

/**
 * Defines the shape of the keyed box state of the application.
 */
export type BoxKeysState = CrescaBucketProtocolTypes['state']['box']['keys']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the CrescaBucketProtocol smart contract
 */
export abstract class CrescaBucketProtocolParamsFactory {
  /**
   * Constructs a no op call for the create_bucket(uint64[],uint64[],uint64)uint64 ABI method
   *
  * Create a custom asset basket.
  assets  — up to 8 Algorand ASA IDs (use 0 for ALGO itself) weights — percentage allocation per asset (must sum to 100) leverage — 1 to 150

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static createBucket(params: CallParams<CrescaBucketProtocolArgs['obj']['create_bucket(uint64[],uint64[],uint64)uint64'] | CrescaBucketProtocolArgs['tuple']['create_bucket(uint64[],uint64[],uint64)uint64']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'create_bucket(uint64[],uint64[],uint64)uint64' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.assets, params.args.weights, params.args.leverage],
    }
  }
  /**
   * Constructs a no op call for the deposit_collateral(pay)bool ABI method
   *
  * Deposit ALGO as collateral for trading.
  Attach a PaymentTransaction to this contract in the same group.

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static depositCollateral(params: CallParams<CrescaBucketProtocolArgs['obj']['deposit_collateral(pay)bool'] | CrescaBucketProtocolArgs['tuple']['deposit_collateral(pay)bool']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'deposit_collateral(pay)bool' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.paymentTxn],
    }
  }
  /**
   * Constructs a no op call for the withdraw_collateral(uint64)bool ABI method
   *
   * Withdraw ALGO collateral back to sender.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static withdrawCollateral(params: CallParams<CrescaBucketProtocolArgs['obj']['withdraw_collateral(uint64)bool'] | CrescaBucketProtocolArgs['tuple']['withdraw_collateral(uint64)bool']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'withdraw_collateral(uint64)bool' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.amount],
    }
  }
  /**
   * Constructs a no op call for the open_position(uint64,bool,uint64)uint64 ABI method
   *
  * Open a leveraged long or short position on a bucket.
  bucket_id — ID of the bucket owned by Txn.sender is_long   — True = long, False = short margin    — μALGO to lock from collateral balance

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static openPosition(params: CallParams<CrescaBucketProtocolArgs['obj']['open_position(uint64,bool,uint64)uint64'] | CrescaBucketProtocolArgs['tuple']['open_position(uint64,bool,uint64)uint64']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'open_position(uint64,bool,uint64)uint64' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.bucketId, params.args.isLong, params.args.margin],
    }
  }
  /**
   * Constructs a no op call for the close_position(uint64)uint64 ABI method
   *
  * Close a position and realise P&L back into the collateral balance.
  Returns absolute P&L in μALGO.

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static closePosition(params: CallParams<CrescaBucketProtocolArgs['obj']['close_position(uint64)uint64'] | CrescaBucketProtocolArgs['tuple']['close_position(uint64)uint64']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'close_position(uint64)uint64' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.positionId],
    }
  }
  /**
   * Constructs a no op call for the rebalance_bucket(uint64,uint64[])bool ABI method
   *
   * Update the weight allocation of an existing bucket (owner only).
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static rebalanceBucket(params: CallParams<CrescaBucketProtocolArgs['obj']['rebalance_bucket(uint64,uint64[])bool'] | CrescaBucketProtocolArgs['tuple']['rebalance_bucket(uint64,uint64[])bool']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'rebalance_bucket(uint64,uint64[])bool' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.bucketId, params.args.newWeights],
    }
  }
  /**
   * Constructs a no op call for the update_oracle(uint64[],uint64[])bool ABI method
   *
  * Update mock oracle prices. In production, consume a Pyth price feed
  via a Pyth Algorand oracle contract instead.
  Prices must use 8-decimal precision (e.g. 1 ALGO = 100_000_000).

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static updateOracle(params: CallParams<CrescaBucketProtocolArgs['obj']['update_oracle(uint64[],uint64[])bool'] | CrescaBucketProtocolArgs['tuple']['update_oracle(uint64[],uint64[])bool']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'update_oracle(uint64[],uint64[])bool' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.assetIds, params.args.assetPrices],
    }
  }
  /**
   * Constructs a no op call for the liquidate_position(address,uint64)bool ABI method
   *
  * Liquidate an undercollateralised position.
  Anyone may call this (keeper/bot pattern — same as Solidity version).

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static liquidatePosition(params: CallParams<CrescaBucketProtocolArgs['obj']['liquidate_position(address,uint64)bool'] | CrescaBucketProtocolArgs['tuple']['liquidate_position(address,uint64)bool']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'liquidate_position(address,uint64)bool' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.owner, params.args.positionId],
    }
  }
  /**
   * Constructs a no op call for the get_collateral_balance(address)uint64 ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static getCollateralBalance(params: CallParams<CrescaBucketProtocolArgs['obj']['get_collateral_balance(address)uint64'] | CrescaBucketProtocolArgs['tuple']['get_collateral_balance(address)uint64']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'get_collateral_balance(address)uint64' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.user],
    }
  }
  /**
   * Constructs a no op call for the get_unrealized_pnl(address,uint64)uint64 ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static getUnrealizedPnl(params: CallParams<CrescaBucketProtocolArgs['obj']['get_unrealized_pnl(address,uint64)uint64'] | CrescaBucketProtocolArgs['tuple']['get_unrealized_pnl(address,uint64)uint64']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'get_unrealized_pnl(address,uint64)uint64' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.owner, params.args.positionId],
    }
  }
  /**
   * Constructs a no op call for the get_oracle_updated_at()uint64 ABI method
   *
   * Returns Unix timestamp of the last oracle update (0 if never updated).
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static getOracleUpdatedAt(params: CallParams<CrescaBucketProtocolArgs['obj']['get_oracle_updated_at()uint64'] | CrescaBucketProtocolArgs['tuple']['get_oracle_updated_at()uint64']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'get_oracle_updated_at()uint64' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the get_total_positions()uint64 ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static getTotalPositions(params: CallParams<CrescaBucketProtocolArgs['obj']['get_total_positions()uint64'] | CrescaBucketProtocolArgs['tuple']['get_total_positions()uint64']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'get_total_positions()uint64' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the get_total_buckets()uint64 ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static getTotalBuckets(params: CallParams<CrescaBucketProtocolArgs['obj']['get_total_buckets()uint64'] | CrescaBucketProtocolArgs['tuple']['get_total_buckets()uint64']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'get_total_buckets()uint64' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the fund_contract(pay)bool ABI method
   *
   * Increase contract's minimum balance to cover Box storage costs.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static fundContract(params: CallParams<CrescaBucketProtocolArgs['obj']['fund_contract(pay)bool'] | CrescaBucketProtocolArgs['tuple']['fund_contract(pay)bool']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'fund_contract(pay)bool' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.paymentTxn],
    }
  }
}

/**
 * A client to make calls to the CrescaBucketProtocol smart contract
 */
export class CrescaBucketProtocolClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `CrescaBucketProtocolClient`
   *
   * @param appClient An `AppClient` instance which has been created with the CrescaBucketProtocol app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `CrescaBucketProtocolClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Checks for decode errors on the given return value and maps the return value to the return type for the given method
   * @returns The typed return value or undefined if there was no value
   */
  decodeReturnValue<TSignature extends CrescaBucketProtocolNonVoidMethodSignatures>(method: TSignature, returnValue: ABIReturn | undefined) {
    return returnValue !== undefined ? getArc56ReturnValue<MethodReturn<TSignature>>(returnValue, this.appClient.getABIMethod(method), APP_SPEC.structs) : undefined
  }

  /**
   * Returns a new `CrescaBucketProtocolClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<CrescaBucketProtocolClient> {
    return new CrescaBucketProtocolClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `CrescaBucketProtocolClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<CrescaBucketProtocolClient> {
    return new CrescaBucketProtocolClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Makes a clear_state call to an existing instance of the CrescaBucketProtocol smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the CrescaBucketProtocol smart contract using the `create_bucket(uint64[],uint64[],uint64)uint64` ABI method.
     *
    * Create a custom asset basket.
    assets  — up to 8 Algorand ASA IDs (use 0 for ALGO itself) weights — percentage allocation per asset (must sum to 100) leverage — 1 to 150

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    createBucket: (params: CallParams<CrescaBucketProtocolArgs['obj']['create_bucket(uint64[],uint64[],uint64)uint64'] | CrescaBucketProtocolArgs['tuple']['create_bucket(uint64[],uint64[],uint64)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(CrescaBucketProtocolParamsFactory.createBucket(params))
    },

    /**
     * Makes a call to the CrescaBucketProtocol smart contract using the `deposit_collateral(pay)bool` ABI method.
     *
    * Deposit ALGO as collateral for trading.
    Attach a PaymentTransaction to this contract in the same group.

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    depositCollateral: (params: CallParams<CrescaBucketProtocolArgs['obj']['deposit_collateral(pay)bool'] | CrescaBucketProtocolArgs['tuple']['deposit_collateral(pay)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(CrescaBucketProtocolParamsFactory.depositCollateral(params))
    },

    /**
     * Makes a call to the CrescaBucketProtocol smart contract using the `withdraw_collateral(uint64)bool` ABI method.
     *
     * Withdraw ALGO collateral back to sender.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    withdrawCollateral: (params: CallParams<CrescaBucketProtocolArgs['obj']['withdraw_collateral(uint64)bool'] | CrescaBucketProtocolArgs['tuple']['withdraw_collateral(uint64)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(CrescaBucketProtocolParamsFactory.withdrawCollateral(params))
    },

    /**
     * Makes a call to the CrescaBucketProtocol smart contract using the `open_position(uint64,bool,uint64)uint64` ABI method.
     *
    * Open a leveraged long or short position on a bucket.
    bucket_id — ID of the bucket owned by Txn.sender is_long   — True = long, False = short margin    — μALGO to lock from collateral balance

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    openPosition: (params: CallParams<CrescaBucketProtocolArgs['obj']['open_position(uint64,bool,uint64)uint64'] | CrescaBucketProtocolArgs['tuple']['open_position(uint64,bool,uint64)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(CrescaBucketProtocolParamsFactory.openPosition(params))
    },

    /**
     * Makes a call to the CrescaBucketProtocol smart contract using the `close_position(uint64)uint64` ABI method.
     *
    * Close a position and realise P&L back into the collateral balance.
    Returns absolute P&L in μALGO.

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    closePosition: (params: CallParams<CrescaBucketProtocolArgs['obj']['close_position(uint64)uint64'] | CrescaBucketProtocolArgs['tuple']['close_position(uint64)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(CrescaBucketProtocolParamsFactory.closePosition(params))
    },

    /**
     * Makes a call to the CrescaBucketProtocol smart contract using the `rebalance_bucket(uint64,uint64[])bool` ABI method.
     *
     * Update the weight allocation of an existing bucket (owner only).
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    rebalanceBucket: (params: CallParams<CrescaBucketProtocolArgs['obj']['rebalance_bucket(uint64,uint64[])bool'] | CrescaBucketProtocolArgs['tuple']['rebalance_bucket(uint64,uint64[])bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(CrescaBucketProtocolParamsFactory.rebalanceBucket(params))
    },

    /**
     * Makes a call to the CrescaBucketProtocol smart contract using the `update_oracle(uint64[],uint64[])bool` ABI method.
     *
    * Update mock oracle prices. In production, consume a Pyth price feed
    via a Pyth Algorand oracle contract instead.
    Prices must use 8-decimal precision (e.g. 1 ALGO = 100_000_000).

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    updateOracle: (params: CallParams<CrescaBucketProtocolArgs['obj']['update_oracle(uint64[],uint64[])bool'] | CrescaBucketProtocolArgs['tuple']['update_oracle(uint64[],uint64[])bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(CrescaBucketProtocolParamsFactory.updateOracle(params))
    },

    /**
     * Makes a call to the CrescaBucketProtocol smart contract using the `liquidate_position(address,uint64)bool` ABI method.
     *
    * Liquidate an undercollateralised position.
    Anyone may call this (keeper/bot pattern — same as Solidity version).

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    liquidatePosition: (params: CallParams<CrescaBucketProtocolArgs['obj']['liquidate_position(address,uint64)bool'] | CrescaBucketProtocolArgs['tuple']['liquidate_position(address,uint64)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(CrescaBucketProtocolParamsFactory.liquidatePosition(params))
    },

    /**
     * Makes a call to the CrescaBucketProtocol smart contract using the `get_collateral_balance(address)uint64` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    getCollateralBalance: (params: CallParams<CrescaBucketProtocolArgs['obj']['get_collateral_balance(address)uint64'] | CrescaBucketProtocolArgs['tuple']['get_collateral_balance(address)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(CrescaBucketProtocolParamsFactory.getCollateralBalance(params))
    },

    /**
     * Makes a call to the CrescaBucketProtocol smart contract using the `get_unrealized_pnl(address,uint64)uint64` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    getUnrealizedPnl: (params: CallParams<CrescaBucketProtocolArgs['obj']['get_unrealized_pnl(address,uint64)uint64'] | CrescaBucketProtocolArgs['tuple']['get_unrealized_pnl(address,uint64)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(CrescaBucketProtocolParamsFactory.getUnrealizedPnl(params))
    },

    /**
     * Makes a call to the CrescaBucketProtocol smart contract using the `get_oracle_updated_at()uint64` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * Returns Unix timestamp of the last oracle update (0 if never updated).
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    getOracleUpdatedAt: (params: CallParams<CrescaBucketProtocolArgs['obj']['get_oracle_updated_at()uint64'] | CrescaBucketProtocolArgs['tuple']['get_oracle_updated_at()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(CrescaBucketProtocolParamsFactory.getOracleUpdatedAt(params))
    },

    /**
     * Makes a call to the CrescaBucketProtocol smart contract using the `get_total_positions()uint64` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    getTotalPositions: (params: CallParams<CrescaBucketProtocolArgs['obj']['get_total_positions()uint64'] | CrescaBucketProtocolArgs['tuple']['get_total_positions()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(CrescaBucketProtocolParamsFactory.getTotalPositions(params))
    },

    /**
     * Makes a call to the CrescaBucketProtocol smart contract using the `get_total_buckets()uint64` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    getTotalBuckets: (params: CallParams<CrescaBucketProtocolArgs['obj']['get_total_buckets()uint64'] | CrescaBucketProtocolArgs['tuple']['get_total_buckets()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(CrescaBucketProtocolParamsFactory.getTotalBuckets(params))
    },

    /**
     * Makes a call to the CrescaBucketProtocol smart contract using the `fund_contract(pay)bool` ABI method.
     *
     * Increase contract's minimum balance to cover Box storage costs.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    fundContract: (params: CallParams<CrescaBucketProtocolArgs['obj']['fund_contract(pay)bool'] | CrescaBucketProtocolArgs['tuple']['fund_contract(pay)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(CrescaBucketProtocolParamsFactory.fundContract(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Makes a clear_state call to an existing instance of the CrescaBucketProtocol smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the CrescaBucketProtocol smart contract using the `create_bucket(uint64[],uint64[],uint64)uint64` ABI method.
     *
    * Create a custom asset basket.
    assets  — up to 8 Algorand ASA IDs (use 0 for ALGO itself) weights — percentage allocation per asset (must sum to 100) leverage — 1 to 150

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    createBucket: (params: CallParams<CrescaBucketProtocolArgs['obj']['create_bucket(uint64[],uint64[],uint64)uint64'] | CrescaBucketProtocolArgs['tuple']['create_bucket(uint64[],uint64[],uint64)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(CrescaBucketProtocolParamsFactory.createBucket(params))
    },

    /**
     * Makes a call to the CrescaBucketProtocol smart contract using the `deposit_collateral(pay)bool` ABI method.
     *
    * Deposit ALGO as collateral for trading.
    Attach a PaymentTransaction to this contract in the same group.

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    depositCollateral: (params: CallParams<CrescaBucketProtocolArgs['obj']['deposit_collateral(pay)bool'] | CrescaBucketProtocolArgs['tuple']['deposit_collateral(pay)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(CrescaBucketProtocolParamsFactory.depositCollateral(params))
    },

    /**
     * Makes a call to the CrescaBucketProtocol smart contract using the `withdraw_collateral(uint64)bool` ABI method.
     *
     * Withdraw ALGO collateral back to sender.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    withdrawCollateral: (params: CallParams<CrescaBucketProtocolArgs['obj']['withdraw_collateral(uint64)bool'] | CrescaBucketProtocolArgs['tuple']['withdraw_collateral(uint64)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(CrescaBucketProtocolParamsFactory.withdrawCollateral(params))
    },

    /**
     * Makes a call to the CrescaBucketProtocol smart contract using the `open_position(uint64,bool,uint64)uint64` ABI method.
     *
    * Open a leveraged long or short position on a bucket.
    bucket_id — ID of the bucket owned by Txn.sender is_long   — True = long, False = short margin    — μALGO to lock from collateral balance

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    openPosition: (params: CallParams<CrescaBucketProtocolArgs['obj']['open_position(uint64,bool,uint64)uint64'] | CrescaBucketProtocolArgs['tuple']['open_position(uint64,bool,uint64)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(CrescaBucketProtocolParamsFactory.openPosition(params))
    },

    /**
     * Makes a call to the CrescaBucketProtocol smart contract using the `close_position(uint64)uint64` ABI method.
     *
    * Close a position and realise P&L back into the collateral balance.
    Returns absolute P&L in μALGO.

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    closePosition: (params: CallParams<CrescaBucketProtocolArgs['obj']['close_position(uint64)uint64'] | CrescaBucketProtocolArgs['tuple']['close_position(uint64)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(CrescaBucketProtocolParamsFactory.closePosition(params))
    },

    /**
     * Makes a call to the CrescaBucketProtocol smart contract using the `rebalance_bucket(uint64,uint64[])bool` ABI method.
     *
     * Update the weight allocation of an existing bucket (owner only).
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    rebalanceBucket: (params: CallParams<CrescaBucketProtocolArgs['obj']['rebalance_bucket(uint64,uint64[])bool'] | CrescaBucketProtocolArgs['tuple']['rebalance_bucket(uint64,uint64[])bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(CrescaBucketProtocolParamsFactory.rebalanceBucket(params))
    },

    /**
     * Makes a call to the CrescaBucketProtocol smart contract using the `update_oracle(uint64[],uint64[])bool` ABI method.
     *
    * Update mock oracle prices. In production, consume a Pyth price feed
    via a Pyth Algorand oracle contract instead.
    Prices must use 8-decimal precision (e.g. 1 ALGO = 100_000_000).

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    updateOracle: (params: CallParams<CrescaBucketProtocolArgs['obj']['update_oracle(uint64[],uint64[])bool'] | CrescaBucketProtocolArgs['tuple']['update_oracle(uint64[],uint64[])bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(CrescaBucketProtocolParamsFactory.updateOracle(params))
    },

    /**
     * Makes a call to the CrescaBucketProtocol smart contract using the `liquidate_position(address,uint64)bool` ABI method.
     *
    * Liquidate an undercollateralised position.
    Anyone may call this (keeper/bot pattern — same as Solidity version).

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    liquidatePosition: (params: CallParams<CrescaBucketProtocolArgs['obj']['liquidate_position(address,uint64)bool'] | CrescaBucketProtocolArgs['tuple']['liquidate_position(address,uint64)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(CrescaBucketProtocolParamsFactory.liquidatePosition(params))
    },

    /**
     * Makes a call to the CrescaBucketProtocol smart contract using the `get_collateral_balance(address)uint64` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    getCollateralBalance: (params: CallParams<CrescaBucketProtocolArgs['obj']['get_collateral_balance(address)uint64'] | CrescaBucketProtocolArgs['tuple']['get_collateral_balance(address)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(CrescaBucketProtocolParamsFactory.getCollateralBalance(params))
    },

    /**
     * Makes a call to the CrescaBucketProtocol smart contract using the `get_unrealized_pnl(address,uint64)uint64` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    getUnrealizedPnl: (params: CallParams<CrescaBucketProtocolArgs['obj']['get_unrealized_pnl(address,uint64)uint64'] | CrescaBucketProtocolArgs['tuple']['get_unrealized_pnl(address,uint64)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(CrescaBucketProtocolParamsFactory.getUnrealizedPnl(params))
    },

    /**
     * Makes a call to the CrescaBucketProtocol smart contract using the `get_oracle_updated_at()uint64` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * Returns Unix timestamp of the last oracle update (0 if never updated).
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    getOracleUpdatedAt: (params: CallParams<CrescaBucketProtocolArgs['obj']['get_oracle_updated_at()uint64'] | CrescaBucketProtocolArgs['tuple']['get_oracle_updated_at()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(CrescaBucketProtocolParamsFactory.getOracleUpdatedAt(params))
    },

    /**
     * Makes a call to the CrescaBucketProtocol smart contract using the `get_total_positions()uint64` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    getTotalPositions: (params: CallParams<CrescaBucketProtocolArgs['obj']['get_total_positions()uint64'] | CrescaBucketProtocolArgs['tuple']['get_total_positions()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(CrescaBucketProtocolParamsFactory.getTotalPositions(params))
    },

    /**
     * Makes a call to the CrescaBucketProtocol smart contract using the `get_total_buckets()uint64` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    getTotalBuckets: (params: CallParams<CrescaBucketProtocolArgs['obj']['get_total_buckets()uint64'] | CrescaBucketProtocolArgs['tuple']['get_total_buckets()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(CrescaBucketProtocolParamsFactory.getTotalBuckets(params))
    },

    /**
     * Makes a call to the CrescaBucketProtocol smart contract using the `fund_contract(pay)bool` ABI method.
     *
     * Increase contract's minimum balance to cover Box storage costs.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    fundContract: (params: CallParams<CrescaBucketProtocolArgs['obj']['fund_contract(pay)bool'] | CrescaBucketProtocolArgs['tuple']['fund_contract(pay)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(CrescaBucketProtocolParamsFactory.fundContract(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Makes a clear_state call to an existing instance of the CrescaBucketProtocol smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the CrescaBucketProtocol smart contract using the `create_bucket(uint64[],uint64[],uint64)uint64` ABI method.
     *
    * Create a custom asset basket.
    assets  — up to 8 Algorand ASA IDs (use 0 for ALGO itself) weights — percentage allocation per asset (must sum to 100) leverage — 1 to 150

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    createBucket: async (params: CallParams<CrescaBucketProtocolArgs['obj']['create_bucket(uint64[],uint64[],uint64)uint64'] | CrescaBucketProtocolArgs['tuple']['create_bucket(uint64[],uint64[],uint64)uint64']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(CrescaBucketProtocolParamsFactory.createBucket(params))
      return {...result, return: result.return as unknown as (undefined | CrescaBucketProtocolReturns['create_bucket(uint64[],uint64[],uint64)uint64'])}
    },

    /**
     * Makes a call to the CrescaBucketProtocol smart contract using the `deposit_collateral(pay)bool` ABI method.
     *
    * Deposit ALGO as collateral for trading.
    Attach a PaymentTransaction to this contract in the same group.

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    depositCollateral: async (params: CallParams<CrescaBucketProtocolArgs['obj']['deposit_collateral(pay)bool'] | CrescaBucketProtocolArgs['tuple']['deposit_collateral(pay)bool']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(CrescaBucketProtocolParamsFactory.depositCollateral(params))
      return {...result, return: result.return as unknown as (undefined | CrescaBucketProtocolReturns['deposit_collateral(pay)bool'])}
    },

    /**
     * Makes a call to the CrescaBucketProtocol smart contract using the `withdraw_collateral(uint64)bool` ABI method.
     *
     * Withdraw ALGO collateral back to sender.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    withdrawCollateral: async (params: CallParams<CrescaBucketProtocolArgs['obj']['withdraw_collateral(uint64)bool'] | CrescaBucketProtocolArgs['tuple']['withdraw_collateral(uint64)bool']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(CrescaBucketProtocolParamsFactory.withdrawCollateral(params))
      return {...result, return: result.return as unknown as (undefined | CrescaBucketProtocolReturns['withdraw_collateral(uint64)bool'])}
    },

    /**
     * Makes a call to the CrescaBucketProtocol smart contract using the `open_position(uint64,bool,uint64)uint64` ABI method.
     *
    * Open a leveraged long or short position on a bucket.
    bucket_id — ID of the bucket owned by Txn.sender is_long   — True = long, False = short margin    — μALGO to lock from collateral balance

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    openPosition: async (params: CallParams<CrescaBucketProtocolArgs['obj']['open_position(uint64,bool,uint64)uint64'] | CrescaBucketProtocolArgs['tuple']['open_position(uint64,bool,uint64)uint64']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(CrescaBucketProtocolParamsFactory.openPosition(params))
      return {...result, return: result.return as unknown as (undefined | CrescaBucketProtocolReturns['open_position(uint64,bool,uint64)uint64'])}
    },

    /**
     * Makes a call to the CrescaBucketProtocol smart contract using the `close_position(uint64)uint64` ABI method.
     *
    * Close a position and realise P&L back into the collateral balance.
    Returns absolute P&L in μALGO.

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    closePosition: async (params: CallParams<CrescaBucketProtocolArgs['obj']['close_position(uint64)uint64'] | CrescaBucketProtocolArgs['tuple']['close_position(uint64)uint64']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(CrescaBucketProtocolParamsFactory.closePosition(params))
      return {...result, return: result.return as unknown as (undefined | CrescaBucketProtocolReturns['close_position(uint64)uint64'])}
    },

    /**
     * Makes a call to the CrescaBucketProtocol smart contract using the `rebalance_bucket(uint64,uint64[])bool` ABI method.
     *
     * Update the weight allocation of an existing bucket (owner only).
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    rebalanceBucket: async (params: CallParams<CrescaBucketProtocolArgs['obj']['rebalance_bucket(uint64,uint64[])bool'] | CrescaBucketProtocolArgs['tuple']['rebalance_bucket(uint64,uint64[])bool']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(CrescaBucketProtocolParamsFactory.rebalanceBucket(params))
      return {...result, return: result.return as unknown as (undefined | CrescaBucketProtocolReturns['rebalance_bucket(uint64,uint64[])bool'])}
    },

    /**
     * Makes a call to the CrescaBucketProtocol smart contract using the `update_oracle(uint64[],uint64[])bool` ABI method.
     *
    * Update mock oracle prices. In production, consume a Pyth price feed
    via a Pyth Algorand oracle contract instead.
    Prices must use 8-decimal precision (e.g. 1 ALGO = 100_000_000).

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    updateOracle: async (params: CallParams<CrescaBucketProtocolArgs['obj']['update_oracle(uint64[],uint64[])bool'] | CrescaBucketProtocolArgs['tuple']['update_oracle(uint64[],uint64[])bool']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(CrescaBucketProtocolParamsFactory.updateOracle(params))
      return {...result, return: result.return as unknown as (undefined | CrescaBucketProtocolReturns['update_oracle(uint64[],uint64[])bool'])}
    },

    /**
     * Makes a call to the CrescaBucketProtocol smart contract using the `liquidate_position(address,uint64)bool` ABI method.
     *
    * Liquidate an undercollateralised position.
    Anyone may call this (keeper/bot pattern — same as Solidity version).

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    liquidatePosition: async (params: CallParams<CrescaBucketProtocolArgs['obj']['liquidate_position(address,uint64)bool'] | CrescaBucketProtocolArgs['tuple']['liquidate_position(address,uint64)bool']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(CrescaBucketProtocolParamsFactory.liquidatePosition(params))
      return {...result, return: result.return as unknown as (undefined | CrescaBucketProtocolReturns['liquidate_position(address,uint64)bool'])}
    },

    /**
     * Makes a call to the CrescaBucketProtocol smart contract using the `get_collateral_balance(address)uint64` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    getCollateralBalance: async (params: CallParams<CrescaBucketProtocolArgs['obj']['get_collateral_balance(address)uint64'] | CrescaBucketProtocolArgs['tuple']['get_collateral_balance(address)uint64']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(CrescaBucketProtocolParamsFactory.getCollateralBalance(params))
      return {...result, return: result.return as unknown as (undefined | CrescaBucketProtocolReturns['get_collateral_balance(address)uint64'])}
    },

    /**
     * Makes a call to the CrescaBucketProtocol smart contract using the `get_unrealized_pnl(address,uint64)uint64` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    getUnrealizedPnl: async (params: CallParams<CrescaBucketProtocolArgs['obj']['get_unrealized_pnl(address,uint64)uint64'] | CrescaBucketProtocolArgs['tuple']['get_unrealized_pnl(address,uint64)uint64']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(CrescaBucketProtocolParamsFactory.getUnrealizedPnl(params))
      return {...result, return: result.return as unknown as (undefined | CrescaBucketProtocolReturns['get_unrealized_pnl(address,uint64)uint64'])}
    },

    /**
     * Makes a call to the CrescaBucketProtocol smart contract using the `get_oracle_updated_at()uint64` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * Returns Unix timestamp of the last oracle update (0 if never updated).
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    getOracleUpdatedAt: async (params: CallParams<CrescaBucketProtocolArgs['obj']['get_oracle_updated_at()uint64'] | CrescaBucketProtocolArgs['tuple']['get_oracle_updated_at()uint64']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(CrescaBucketProtocolParamsFactory.getOracleUpdatedAt(params))
      return {...result, return: result.return as unknown as (undefined | CrescaBucketProtocolReturns['get_oracle_updated_at()uint64'])}
    },

    /**
     * Makes a call to the CrescaBucketProtocol smart contract using the `get_total_positions()uint64` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    getTotalPositions: async (params: CallParams<CrescaBucketProtocolArgs['obj']['get_total_positions()uint64'] | CrescaBucketProtocolArgs['tuple']['get_total_positions()uint64']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(CrescaBucketProtocolParamsFactory.getTotalPositions(params))
      return {...result, return: result.return as unknown as (undefined | CrescaBucketProtocolReturns['get_total_positions()uint64'])}
    },

    /**
     * Makes a call to the CrescaBucketProtocol smart contract using the `get_total_buckets()uint64` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    getTotalBuckets: async (params: CallParams<CrescaBucketProtocolArgs['obj']['get_total_buckets()uint64'] | CrescaBucketProtocolArgs['tuple']['get_total_buckets()uint64']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(CrescaBucketProtocolParamsFactory.getTotalBuckets(params))
      return {...result, return: result.return as unknown as (undefined | CrescaBucketProtocolReturns['get_total_buckets()uint64'])}
    },

    /**
     * Makes a call to the CrescaBucketProtocol smart contract using the `fund_contract(pay)bool` ABI method.
     *
     * Increase contract's minimum balance to cover Box storage costs.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    fundContract: async (params: CallParams<CrescaBucketProtocolArgs['obj']['fund_contract(pay)bool'] | CrescaBucketProtocolArgs['tuple']['fund_contract(pay)bool']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(CrescaBucketProtocolParamsFactory.fundContract(params))
      return {...result, return: result.return as unknown as (undefined | CrescaBucketProtocolReturns['fund_contract(pay)bool'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new CrescaBucketProtocolClient(this.appClient.clone(params))
  }

  /**
   * Makes a readonly (simulated) call to the CrescaBucketProtocol smart contract using the `get_collateral_balance(address)uint64` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async getCollateralBalance(params: CallParams<CrescaBucketProtocolArgs['obj']['get_collateral_balance(address)uint64'] | CrescaBucketProtocolArgs['tuple']['get_collateral_balance(address)uint64']>) {
    const result = await this.appClient.send.call(CrescaBucketProtocolParamsFactory.getCollateralBalance(params))
    return result.return as unknown as CrescaBucketProtocolReturns['get_collateral_balance(address)uint64']
  }

  /**
   * Makes a readonly (simulated) call to the CrescaBucketProtocol smart contract using the `get_unrealized_pnl(address,uint64)uint64` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async getUnrealizedPnl(params: CallParams<CrescaBucketProtocolArgs['obj']['get_unrealized_pnl(address,uint64)uint64'] | CrescaBucketProtocolArgs['tuple']['get_unrealized_pnl(address,uint64)uint64']>) {
    const result = await this.appClient.send.call(CrescaBucketProtocolParamsFactory.getUnrealizedPnl(params))
    return result.return as unknown as CrescaBucketProtocolReturns['get_unrealized_pnl(address,uint64)uint64']
  }

  /**
   * Makes a readonly (simulated) call to the CrescaBucketProtocol smart contract using the `get_oracle_updated_at()uint64` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * Returns Unix timestamp of the last oracle update (0 if never updated).
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async getOracleUpdatedAt(params: CallParams<CrescaBucketProtocolArgs['obj']['get_oracle_updated_at()uint64'] | CrescaBucketProtocolArgs['tuple']['get_oracle_updated_at()uint64']> = {args: []}) {
    const result = await this.appClient.send.call(CrescaBucketProtocolParamsFactory.getOracleUpdatedAt(params))
    return result.return as unknown as CrescaBucketProtocolReturns['get_oracle_updated_at()uint64']
  }

  /**
   * Makes a readonly (simulated) call to the CrescaBucketProtocol smart contract using the `get_total_positions()uint64` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async getTotalPositions(params: CallParams<CrescaBucketProtocolArgs['obj']['get_total_positions()uint64'] | CrescaBucketProtocolArgs['tuple']['get_total_positions()uint64']> = {args: []}) {
    const result = await this.appClient.send.call(CrescaBucketProtocolParamsFactory.getTotalPositions(params))
    return result.return as unknown as CrescaBucketProtocolReturns['get_total_positions()uint64']
  }

  /**
   * Makes a readonly (simulated) call to the CrescaBucketProtocol smart contract using the `get_total_buckets()uint64` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async getTotalBuckets(params: CallParams<CrescaBucketProtocolArgs['obj']['get_total_buckets()uint64'] | CrescaBucketProtocolArgs['tuple']['get_total_buckets()uint64']> = {args: []}) {
    const result = await this.appClient.send.call(CrescaBucketProtocolParamsFactory.getTotalBuckets(params))
    return result.return as unknown as CrescaBucketProtocolReturns['get_total_buckets()uint64']
  }

  /**
   * Methods to access state for the current CrescaBucketProtocol app
   */
  state = {
    /**
     * Methods to access global state for the current CrescaBucketProtocol app
     */
    global: {
      /**
       * Get all current keyed values from global state
       */
      getAll: async (): Promise<Partial<Expand<GlobalKeysState>>> => {
        const result = await this.appClient.state.global.getAll()
        return {
          totalPositions: result.total_positions,
          totalBuckets: result.total_buckets,
          oracleUpdatedAt: result.oracle_updated_at,
        }
      },
      /**
       * Get the current value of the total_positions key in global state
       */
      totalPositions: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("total_positions")) as bigint | undefined },
      /**
       * Get the current value of the total_buckets key in global state
       */
      totalBuckets: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("total_buckets")) as bigint | undefined },
      /**
       * Get the current value of the oracle_updated_at key in global state
       */
      oracleUpdatedAt: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("oracle_updated_at")) as bigint | undefined },
    },
    /**
     * Methods to access box state for the current CrescaBucketProtocol app
     */
    box: {
      /**
       * Get all current keyed values from box state
       */
      getAll: async (): Promise<Partial<Expand<BoxKeysState>>> => {
        const result = await this.appClient.state.box.getAll()
        return {
        }
      },
      /**
       * Get values from the collateral map in box state
       */
      collateral: {
        /**
         * Get all current values of the collateral map in box state
         */
        getMap: async (): Promise<Map<string, bigint>> => { return (await this.appClient.state.box.getMap("collateral")) as Map<string, bigint> },
        /**
         * Get a current value of the collateral map by key from box state
         */
        value: async (key: string): Promise<bigint | undefined> => { return await this.appClient.state.box.getMapValue("collateral", key) as bigint | undefined },
      },
      /**
       * Get values from the bucket_counts map in box state
       */
      bucketCounts: {
        /**
         * Get all current values of the bucket_counts map in box state
         */
        getMap: async (): Promise<Map<string, bigint>> => { return (await this.appClient.state.box.getMap("bucket_counts")) as Map<string, bigint> },
        /**
         * Get a current value of the bucket_counts map by key from box state
         */
        value: async (key: string): Promise<bigint | undefined> => { return await this.appClient.state.box.getMapValue("bucket_counts", key) as bigint | undefined },
      },
      /**
       * Get values from the buckets map in box state
       */
      buckets: {
        /**
         * Get all current values of the buckets map in box state
         */
        getMap: async (): Promise<Map<Uint8Array, Bucket>> => { return (await this.appClient.state.box.getMap("buckets")) as Map<Uint8Array, Bucket> },
        /**
         * Get a current value of the buckets map by key from box state
         */
        value: async (key: Uint8Array | string): Promise<Bucket | undefined> => { return await this.appClient.state.box.getMapValue("buckets", key) as Bucket | undefined },
      },
      /**
       * Get values from the positions map in box state
       */
      positions: {
        /**
         * Get all current values of the positions map in box state
         */
        getMap: async (): Promise<Map<Uint8Array, Position>> => { return (await this.appClient.state.box.getMap("positions")) as Map<Uint8Array, Position> },
        /**
         * Get a current value of the positions map by key from box state
         */
        value: async (key: Uint8Array | string): Promise<Position | undefined> => { return await this.appClient.state.box.getMapValue("positions", key) as Position | undefined },
      },
      /**
       * Get values from the prices map in box state
       */
      prices: {
        /**
         * Get all current values of the prices map in box state
         */
        getMap: async (): Promise<Map<Uint8Array, PriceData>> => { return (await this.appClient.state.box.getMap("prices")) as Map<Uint8Array, PriceData> },
        /**
         * Get a current value of the prices map by key from box state
         */
        value: async (key: Uint8Array | string): Promise<PriceData | undefined> => { return await this.appClient.state.box.getMapValue("prices", key) as PriceData | undefined },
      },
    },
  }

  public newGroup(): CrescaBucketProtocolComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a create_bucket(uint64[],uint64[],uint64)uint64 method call against the CrescaBucketProtocol contract
       */
      createBucket(params: CallParams<CrescaBucketProtocolArgs['obj']['create_bucket(uint64[],uint64[],uint64)uint64'] | CrescaBucketProtocolArgs['tuple']['create_bucket(uint64[],uint64[],uint64)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.createBucket(params)))
        resultMappers.push((v) => client.decodeReturnValue('create_bucket(uint64[],uint64[],uint64)uint64', v))
        return this
      },
      /**
       * Add a deposit_collateral(pay)bool method call against the CrescaBucketProtocol contract
       */
      depositCollateral(params: CallParams<CrescaBucketProtocolArgs['obj']['deposit_collateral(pay)bool'] | CrescaBucketProtocolArgs['tuple']['deposit_collateral(pay)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.depositCollateral(params)))
        resultMappers.push((v) => client.decodeReturnValue('deposit_collateral(pay)bool', v))
        return this
      },
      /**
       * Add a withdraw_collateral(uint64)bool method call against the CrescaBucketProtocol contract
       */
      withdrawCollateral(params: CallParams<CrescaBucketProtocolArgs['obj']['withdraw_collateral(uint64)bool'] | CrescaBucketProtocolArgs['tuple']['withdraw_collateral(uint64)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.withdrawCollateral(params)))
        resultMappers.push((v) => client.decodeReturnValue('withdraw_collateral(uint64)bool', v))
        return this
      },
      /**
       * Add a open_position(uint64,bool,uint64)uint64 method call against the CrescaBucketProtocol contract
       */
      openPosition(params: CallParams<CrescaBucketProtocolArgs['obj']['open_position(uint64,bool,uint64)uint64'] | CrescaBucketProtocolArgs['tuple']['open_position(uint64,bool,uint64)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.openPosition(params)))
        resultMappers.push((v) => client.decodeReturnValue('open_position(uint64,bool,uint64)uint64', v))
        return this
      },
      /**
       * Add a close_position(uint64)uint64 method call against the CrescaBucketProtocol contract
       */
      closePosition(params: CallParams<CrescaBucketProtocolArgs['obj']['close_position(uint64)uint64'] | CrescaBucketProtocolArgs['tuple']['close_position(uint64)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.closePosition(params)))
        resultMappers.push((v) => client.decodeReturnValue('close_position(uint64)uint64', v))
        return this
      },
      /**
       * Add a rebalance_bucket(uint64,uint64[])bool method call against the CrescaBucketProtocol contract
       */
      rebalanceBucket(params: CallParams<CrescaBucketProtocolArgs['obj']['rebalance_bucket(uint64,uint64[])bool'] | CrescaBucketProtocolArgs['tuple']['rebalance_bucket(uint64,uint64[])bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.rebalanceBucket(params)))
        resultMappers.push((v) => client.decodeReturnValue('rebalance_bucket(uint64,uint64[])bool', v))
        return this
      },
      /**
       * Add a update_oracle(uint64[],uint64[])bool method call against the CrescaBucketProtocol contract
       */
      updateOracle(params: CallParams<CrescaBucketProtocolArgs['obj']['update_oracle(uint64[],uint64[])bool'] | CrescaBucketProtocolArgs['tuple']['update_oracle(uint64[],uint64[])bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.updateOracle(params)))
        resultMappers.push((v) => client.decodeReturnValue('update_oracle(uint64[],uint64[])bool', v))
        return this
      },
      /**
       * Add a liquidate_position(address,uint64)bool method call against the CrescaBucketProtocol contract
       */
      liquidatePosition(params: CallParams<CrescaBucketProtocolArgs['obj']['liquidate_position(address,uint64)bool'] | CrescaBucketProtocolArgs['tuple']['liquidate_position(address,uint64)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.liquidatePosition(params)))
        resultMappers.push((v) => client.decodeReturnValue('liquidate_position(address,uint64)bool', v))
        return this
      },
      /**
       * Add a get_collateral_balance(address)uint64 method call against the CrescaBucketProtocol contract
       */
      getCollateralBalance(params: CallParams<CrescaBucketProtocolArgs['obj']['get_collateral_balance(address)uint64'] | CrescaBucketProtocolArgs['tuple']['get_collateral_balance(address)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.getCollateralBalance(params)))
        resultMappers.push((v) => client.decodeReturnValue('get_collateral_balance(address)uint64', v))
        return this
      },
      /**
       * Add a get_unrealized_pnl(address,uint64)uint64 method call against the CrescaBucketProtocol contract
       */
      getUnrealizedPnl(params: CallParams<CrescaBucketProtocolArgs['obj']['get_unrealized_pnl(address,uint64)uint64'] | CrescaBucketProtocolArgs['tuple']['get_unrealized_pnl(address,uint64)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.getUnrealizedPnl(params)))
        resultMappers.push((v) => client.decodeReturnValue('get_unrealized_pnl(address,uint64)uint64', v))
        return this
      },
      /**
       * Add a get_oracle_updated_at()uint64 method call against the CrescaBucketProtocol contract
       */
      getOracleUpdatedAt(params: CallParams<CrescaBucketProtocolArgs['obj']['get_oracle_updated_at()uint64'] | CrescaBucketProtocolArgs['tuple']['get_oracle_updated_at()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.getOracleUpdatedAt(params)))
        resultMappers.push((v) => client.decodeReturnValue('get_oracle_updated_at()uint64', v))
        return this
      },
      /**
       * Add a get_total_positions()uint64 method call against the CrescaBucketProtocol contract
       */
      getTotalPositions(params: CallParams<CrescaBucketProtocolArgs['obj']['get_total_positions()uint64'] | CrescaBucketProtocolArgs['tuple']['get_total_positions()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.getTotalPositions(params)))
        resultMappers.push((v) => client.decodeReturnValue('get_total_positions()uint64', v))
        return this
      },
      /**
       * Add a get_total_buckets()uint64 method call against the CrescaBucketProtocol contract
       */
      getTotalBuckets(params: CallParams<CrescaBucketProtocolArgs['obj']['get_total_buckets()uint64'] | CrescaBucketProtocolArgs['tuple']['get_total_buckets()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.getTotalBuckets(params)))
        resultMappers.push((v) => client.decodeReturnValue('get_total_buckets()uint64', v))
        return this
      },
      /**
       * Add a fund_contract(pay)bool method call against the CrescaBucketProtocol contract
       */
      fundContract(params: CallParams<CrescaBucketProtocolArgs['obj']['fund_contract(pay)bool'] | CrescaBucketProtocolArgs['tuple']['fund_contract(pay)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.fundContract(params)))
        resultMappers.push((v) => client.decodeReturnValue('fund_contract(pay)bool', v))
        return this
      },
      /**
       * Add a clear state call to the CrescaBucketProtocol contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as CrescaBucketProtocolComposer
  }
}
export type CrescaBucketProtocolComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the create_bucket(uint64[],uint64[],uint64)uint64 ABI method.
   *
  * Create a custom asset basket.
  assets  — up to 8 Algorand ASA IDs (use 0 for ALGO itself) weights — percentage allocation per asset (must sum to 100) leverage — 1 to 150

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  createBucket(params?: CallParams<CrescaBucketProtocolArgs['obj']['create_bucket(uint64[],uint64[],uint64)uint64'] | CrescaBucketProtocolArgs['tuple']['create_bucket(uint64[],uint64[],uint64)uint64']>): CrescaBucketProtocolComposer<[...TReturns, CrescaBucketProtocolReturns['create_bucket(uint64[],uint64[],uint64)uint64'] | undefined]>

  /**
   * Calls the deposit_collateral(pay)bool ABI method.
   *
  * Deposit ALGO as collateral for trading.
  Attach a PaymentTransaction to this contract in the same group.

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  depositCollateral(params?: CallParams<CrescaBucketProtocolArgs['obj']['deposit_collateral(pay)bool'] | CrescaBucketProtocolArgs['tuple']['deposit_collateral(pay)bool']>): CrescaBucketProtocolComposer<[...TReturns, CrescaBucketProtocolReturns['deposit_collateral(pay)bool'] | undefined]>

  /**
   * Calls the withdraw_collateral(uint64)bool ABI method.
   *
   * Withdraw ALGO collateral back to sender.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  withdrawCollateral(params?: CallParams<CrescaBucketProtocolArgs['obj']['withdraw_collateral(uint64)bool'] | CrescaBucketProtocolArgs['tuple']['withdraw_collateral(uint64)bool']>): CrescaBucketProtocolComposer<[...TReturns, CrescaBucketProtocolReturns['withdraw_collateral(uint64)bool'] | undefined]>

  /**
   * Calls the open_position(uint64,bool,uint64)uint64 ABI method.
   *
  * Open a leveraged long or short position on a bucket.
  bucket_id — ID of the bucket owned by Txn.sender is_long   — True = long, False = short margin    — μALGO to lock from collateral balance

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  openPosition(params?: CallParams<CrescaBucketProtocolArgs['obj']['open_position(uint64,bool,uint64)uint64'] | CrescaBucketProtocolArgs['tuple']['open_position(uint64,bool,uint64)uint64']>): CrescaBucketProtocolComposer<[...TReturns, CrescaBucketProtocolReturns['open_position(uint64,bool,uint64)uint64'] | undefined]>

  /**
   * Calls the close_position(uint64)uint64 ABI method.
   *
  * Close a position and realise P&L back into the collateral balance.
  Returns absolute P&L in μALGO.

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  closePosition(params?: CallParams<CrescaBucketProtocolArgs['obj']['close_position(uint64)uint64'] | CrescaBucketProtocolArgs['tuple']['close_position(uint64)uint64']>): CrescaBucketProtocolComposer<[...TReturns, CrescaBucketProtocolReturns['close_position(uint64)uint64'] | undefined]>

  /**
   * Calls the rebalance_bucket(uint64,uint64[])bool ABI method.
   *
   * Update the weight allocation of an existing bucket (owner only).
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  rebalanceBucket(params?: CallParams<CrescaBucketProtocolArgs['obj']['rebalance_bucket(uint64,uint64[])bool'] | CrescaBucketProtocolArgs['tuple']['rebalance_bucket(uint64,uint64[])bool']>): CrescaBucketProtocolComposer<[...TReturns, CrescaBucketProtocolReturns['rebalance_bucket(uint64,uint64[])bool'] | undefined]>

  /**
   * Calls the update_oracle(uint64[],uint64[])bool ABI method.
   *
  * Update mock oracle prices. In production, consume a Pyth price feed
  via a Pyth Algorand oracle contract instead.
  Prices must use 8-decimal precision (e.g. 1 ALGO = 100_000_000).

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  updateOracle(params?: CallParams<CrescaBucketProtocolArgs['obj']['update_oracle(uint64[],uint64[])bool'] | CrescaBucketProtocolArgs['tuple']['update_oracle(uint64[],uint64[])bool']>): CrescaBucketProtocolComposer<[...TReturns, CrescaBucketProtocolReturns['update_oracle(uint64[],uint64[])bool'] | undefined]>

  /**
   * Calls the liquidate_position(address,uint64)bool ABI method.
   *
  * Liquidate an undercollateralised position.
  Anyone may call this (keeper/bot pattern — same as Solidity version).

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  liquidatePosition(params?: CallParams<CrescaBucketProtocolArgs['obj']['liquidate_position(address,uint64)bool'] | CrescaBucketProtocolArgs['tuple']['liquidate_position(address,uint64)bool']>): CrescaBucketProtocolComposer<[...TReturns, CrescaBucketProtocolReturns['liquidate_position(address,uint64)bool'] | undefined]>

  /**
   * Calls the get_collateral_balance(address)uint64 ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  getCollateralBalance(params?: CallParams<CrescaBucketProtocolArgs['obj']['get_collateral_balance(address)uint64'] | CrescaBucketProtocolArgs['tuple']['get_collateral_balance(address)uint64']>): CrescaBucketProtocolComposer<[...TReturns, CrescaBucketProtocolReturns['get_collateral_balance(address)uint64'] | undefined]>

  /**
   * Calls the get_unrealized_pnl(address,uint64)uint64 ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  getUnrealizedPnl(params?: CallParams<CrescaBucketProtocolArgs['obj']['get_unrealized_pnl(address,uint64)uint64'] | CrescaBucketProtocolArgs['tuple']['get_unrealized_pnl(address,uint64)uint64']>): CrescaBucketProtocolComposer<[...TReturns, CrescaBucketProtocolReturns['get_unrealized_pnl(address,uint64)uint64'] | undefined]>

  /**
   * Calls the get_oracle_updated_at()uint64 ABI method.
   *
   * Returns Unix timestamp of the last oracle update (0 if never updated).
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  getOracleUpdatedAt(params?: CallParams<CrescaBucketProtocolArgs['obj']['get_oracle_updated_at()uint64'] | CrescaBucketProtocolArgs['tuple']['get_oracle_updated_at()uint64']>): CrescaBucketProtocolComposer<[...TReturns, CrescaBucketProtocolReturns['get_oracle_updated_at()uint64'] | undefined]>

  /**
   * Calls the get_total_positions()uint64 ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  getTotalPositions(params?: CallParams<CrescaBucketProtocolArgs['obj']['get_total_positions()uint64'] | CrescaBucketProtocolArgs['tuple']['get_total_positions()uint64']>): CrescaBucketProtocolComposer<[...TReturns, CrescaBucketProtocolReturns['get_total_positions()uint64'] | undefined]>

  /**
   * Calls the get_total_buckets()uint64 ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  getTotalBuckets(params?: CallParams<CrescaBucketProtocolArgs['obj']['get_total_buckets()uint64'] | CrescaBucketProtocolArgs['tuple']['get_total_buckets()uint64']>): CrescaBucketProtocolComposer<[...TReturns, CrescaBucketProtocolReturns['get_total_buckets()uint64'] | undefined]>

  /**
   * Calls the fund_contract(pay)bool ABI method.
   *
   * Increase contract's minimum balance to cover Box storage costs.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  fundContract(params?: CallParams<CrescaBucketProtocolArgs['obj']['fund_contract(pay)bool'] | CrescaBucketProtocolArgs['tuple']['fund_contract(pay)bool']>): CrescaBucketProtocolComposer<[...TReturns, CrescaBucketProtocolReturns['fund_contract(pay)bool'] | undefined]>

  /**
   * Makes a clear_state call to an existing instance of the CrescaBucketProtocol smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): CrescaBucketProtocolComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): CrescaBucketProtocolComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<CrescaBucketProtocolComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<CrescaBucketProtocolComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<CrescaBucketProtocolComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<CrescaBucketProtocolComposerResults<TReturns>>
}
export type CrescaBucketProtocolComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

