/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"CrescaPayments","structs":{},"methods":[{"name":"send_payment","args":[{"type":"pay","name":"payment_txn"},{"type":"address","name":"receiver"},{"type":"string","name":"memo"}],"returns":{"type":"byte[]"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Send ALGO to `receiver` with an optional memo.\nCaller must attach a PaymentTransaction TO this contract as the preceding transaction in the same atomic group.\nReturns a sha256 payment-ID usable for off-chain lookups.","events":[{"name":"PaymentSent","args":[{"type":"address","name":"field1"},{"type":"address","name":"field2"},{"type":"uint64","name":"field3"},{"type":"uint64","name":"field4"},{"type":"byte[]","name":"field5"}]}],"recommendations":{}},{"name":"tap_to_pay","args":[{"type":"pay","name":"payment_txn"},{"type":"address","name":"receiver"}],"returns":{"type":"bool"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Quick send without a memo — tap-to-pay use case.","events":[{"name":"TapToPayCompleted","args":[{"type":"address","name":"field1"},{"type":"address","name":"field2"},{"type":"uint64","name":"field3"}]}],"recommendations":{}},{"name":"batch_send","args":[{"type":"pay","name":"payment_txn"},{"type":"address[]","name":"recipients"},{"type":"uint64[]","name":"amounts"}],"returns":{"type":"bool"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Batch send to multiple recipients (max ~8 due to AVM inner-txn limit).\nThe attached `payment_txn` must cover sum(amounts). Excess μALGO is refunded to the caller.","events":[{"name":"PaymentSent","args":[{"type":"address","name":"field1"},{"type":"address","name":"field2"},{"type":"uint64","name":"field3"},{"type":"uint64","name":"field4"},{"type":"byte[]","name":"field5"}]}],"recommendations":{}},{"name":"get_total_payments","args":[],"returns":{"type":"uint64"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"get_total_volume","args":[],"returns":{"type":"uint64"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"desc":"Returns total μALGO volume processed.","events":[],"recommendations":{}}],"arcs":[22,28],"desc":"\n    Instant P2P payment contract on Algorand.\n\n    Key differences vs Solidity version:\n    - Amounts in μALGO (not wei).\n    - Payment history emitted as ARC-28 events; index off-chain via Algorand Indexer.\n    - Funds forwarded atomically via AVM inner transactions — the contract holds\n      funds only transiently within the same atomic group.\n    - Batch send limited to ~8 recipients per call (AVM inner-txn budget).\n    ","networks":{},"state":{"schema":{"global":{"ints":2,"bytes":0},"local":{"ints":0,"bytes":0}},"keys":{"global":{"total_payments":{"keyType":"AVMString","valueType":"AVMUint64","key":"dG90YWxfcGF5bWVudHM="},"total_volume":{"keyType":"AVMString","valueType":"AVMUint64","key":"dG90YWxfdm9sdW1l"}},"local":{},"box":{}},"maps":{"global":{},"local":{},"box":{}}},"bareActions":{"create":["NoOp"],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[307,421],"errorMessage":"Amount must be > 0"},{"pc":[609],"errorMessage":"Index access is out of bounds"},{"pc":[569],"errorMessage":"Insufficient funds"},{"pc":[514],"errorMessage":"Length mismatch"},{"pc":[113,129,145,202,236],"errorMessage":"OnCompletion is not NoOp"},{"pc":[301,415,522],"errorMessage":"Payment must target contract"},{"pc":[288],"errorMessage":"can only call when creating"},{"pc":[116,132,148,205,239],"errorMessage":"can only call when not creating"},{"pc":[333,371,454,698,753],"errorMessage":"check self.total_payments exists"},{"pc":[342,463,708,759],"errorMessage":"check self.total_volume exists"},{"pc":[164,179,263],"errorMessage":"invalid array length header"},{"pc":[173],"errorMessage":"invalid number of bytes for arc4.dynamic_array<arc4.static_array<arc4.uint8, 32>>"},{"pc":[189],"errorMessage":"invalid number of bytes for arc4.dynamic_array<arc4.uint64>"},{"pc":[270],"errorMessage":"invalid number of bytes for arc4.dynamic_array<arc4.uint8>"},{"pc":[223,257],"errorMessage":"invalid number of bytes for arc4.static_array<arc4.uint8, 32>"},{"pc":[158,215,249],"errorMessage":"transaction type is pay"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"events":[{"name":"PaymentSent","args":[{"type":"address","name":"field1"},{"type":"address","name":"field2"},{"type":"uint64","name":"field3"},{"type":"uint64","name":"field4"},{"type":"byte[]","name":"field5"}]},{"name":"TapToPayCompleted","args":[{"type":"address","name":"field1"},{"type":"address","name":"field2"},{"type":"uint64","name":"field3"}]}]} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


/**
 * The argument types for the CrescaPayments contract
 */
export type CrescaPaymentsArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'send_payment(pay,address,string)byte[]': {
      paymentTxn: AppMethodCallTransactionArgument
      receiver: string
      memo: string
    }
    'tap_to_pay(pay,address)bool': {
      paymentTxn: AppMethodCallTransactionArgument
      receiver: string
    }
    'batch_send(pay,address[],uint64[])bool': {
      paymentTxn: AppMethodCallTransactionArgument
      recipients: string[]
      amounts: bigint[] | number[]
    }
    'get_total_payments()uint64': Record<string, never>
    'get_total_volume()uint64': Record<string, never>
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'send_payment(pay,address,string)byte[]': [paymentTxn: AppMethodCallTransactionArgument, receiver: string, memo: string]
    'tap_to_pay(pay,address)bool': [paymentTxn: AppMethodCallTransactionArgument, receiver: string]
    'batch_send(pay,address[],uint64[])bool': [paymentTxn: AppMethodCallTransactionArgument, recipients: string[], amounts: bigint[] | number[]]
    'get_total_payments()uint64': []
    'get_total_volume()uint64': []
  }
}

/**
 * The return type for each method
 */
export type CrescaPaymentsReturns = {
  'send_payment(pay,address,string)byte[]': Uint8Array
  'tap_to_pay(pay,address)bool': boolean
  'batch_send(pay,address[],uint64[])bool': boolean
  'get_total_payments()uint64': bigint
  'get_total_volume()uint64': bigint
}

/**
 * Defines the types of available calls and state of the CrescaPayments smart contract.
 */
export type CrescaPaymentsTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'send_payment(pay,address,string)byte[]' | 'send_payment', {
      argsObj: CrescaPaymentsArgs['obj']['send_payment(pay,address,string)byte[]']
      argsTuple: CrescaPaymentsArgs['tuple']['send_payment(pay,address,string)byte[]']
      returns: CrescaPaymentsReturns['send_payment(pay,address,string)byte[]']
    }>
    & Record<'tap_to_pay(pay,address)bool' | 'tap_to_pay', {
      argsObj: CrescaPaymentsArgs['obj']['tap_to_pay(pay,address)bool']
      argsTuple: CrescaPaymentsArgs['tuple']['tap_to_pay(pay,address)bool']
      returns: CrescaPaymentsReturns['tap_to_pay(pay,address)bool']
    }>
    & Record<'batch_send(pay,address[],uint64[])bool' | 'batch_send', {
      argsObj: CrescaPaymentsArgs['obj']['batch_send(pay,address[],uint64[])bool']
      argsTuple: CrescaPaymentsArgs['tuple']['batch_send(pay,address[],uint64[])bool']
      returns: CrescaPaymentsReturns['batch_send(pay,address[],uint64[])bool']
    }>
    & Record<'get_total_payments()uint64' | 'get_total_payments', {
      argsObj: CrescaPaymentsArgs['obj']['get_total_payments()uint64']
      argsTuple: CrescaPaymentsArgs['tuple']['get_total_payments()uint64']
      returns: CrescaPaymentsReturns['get_total_payments()uint64']
    }>
    & Record<'get_total_volume()uint64' | 'get_total_volume', {
      argsObj: CrescaPaymentsArgs['obj']['get_total_volume()uint64']
      argsTuple: CrescaPaymentsArgs['tuple']['get_total_volume()uint64']
      returns: CrescaPaymentsReturns['get_total_volume()uint64']
    }>
  /**
   * Defines the shape of the state of the application.
   */
  state: {
    global: {
      keys: {
        totalPayments: bigint
        totalVolume: bigint
      }
      maps: {}
    }
  }
}

/**
 * Defines the possible abi call signatures.
 */
export type CrescaPaymentsSignatures = keyof CrescaPaymentsTypes['methods']
/**
 * Defines the possible abi call signatures for methods that return a non-void value.
 */
export type CrescaPaymentsNonVoidMethodSignatures = keyof CrescaPaymentsTypes['methods'] extends infer T ? T extends keyof CrescaPaymentsTypes['methods'] ? MethodReturn<T> extends void ? never : T  : never : never
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the CrescaPayments smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends CrescaPaymentsSignatures> = CrescaPaymentsTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the CrescaPayments smart contract to the method's return type
 */
export type MethodReturn<TSignature extends CrescaPaymentsSignatures> = CrescaPaymentsTypes['methods'][TSignature]['returns']

/**
 * Defines the shape of the keyed global state of the application.
 */
export type GlobalKeysState = CrescaPaymentsTypes['state']['global']['keys']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the CrescaPayments smart contract
 */
export abstract class CrescaPaymentsParamsFactory {
  /**
   * Constructs a no op call for the send_payment(pay,address,string)byte[] ABI method
   *
  * Send ALGO to `receiver` with an optional memo.
  Caller must attach a PaymentTransaction TO this contract as the preceding transaction in the same atomic group.
  Returns a sha256 payment-ID usable for off-chain lookups.

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static sendPayment(params: CallParams<CrescaPaymentsArgs['obj']['send_payment(pay,address,string)byte[]'] | CrescaPaymentsArgs['tuple']['send_payment(pay,address,string)byte[]']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'send_payment(pay,address,string)byte[]' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.paymentTxn, params.args.receiver, params.args.memo],
    }
  }
  /**
   * Constructs a no op call for the tap_to_pay(pay,address)bool ABI method
   *
   * Quick send without a memo — tap-to-pay use case.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static tapToPay(params: CallParams<CrescaPaymentsArgs['obj']['tap_to_pay(pay,address)bool'] | CrescaPaymentsArgs['tuple']['tap_to_pay(pay,address)bool']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'tap_to_pay(pay,address)bool' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.paymentTxn, params.args.receiver],
    }
  }
  /**
   * Constructs a no op call for the batch_send(pay,address[],uint64[])bool ABI method
   *
  * Batch send to multiple recipients (max ~8 due to AVM inner-txn limit).
  The attached `payment_txn` must cover sum(amounts). Excess μALGO is refunded to the caller.

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static batchSend(params: CallParams<CrescaPaymentsArgs['obj']['batch_send(pay,address[],uint64[])bool'] | CrescaPaymentsArgs['tuple']['batch_send(pay,address[],uint64[])bool']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'batch_send(pay,address[],uint64[])bool' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.paymentTxn, params.args.recipients, params.args.amounts],
    }
  }
  /**
   * Constructs a no op call for the get_total_payments()uint64 ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static getTotalPayments(params: CallParams<CrescaPaymentsArgs['obj']['get_total_payments()uint64'] | CrescaPaymentsArgs['tuple']['get_total_payments()uint64']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'get_total_payments()uint64' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the get_total_volume()uint64 ABI method
   *
   * Returns total μALGO volume processed.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static getTotalVolume(params: CallParams<CrescaPaymentsArgs['obj']['get_total_volume()uint64'] | CrescaPaymentsArgs['tuple']['get_total_volume()uint64']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'get_total_volume()uint64' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
}

/**
 * A client to make calls to the CrescaPayments smart contract
 */
export class CrescaPaymentsClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `CrescaPaymentsClient`
   *
   * @param appClient An `AppClient` instance which has been created with the CrescaPayments app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `CrescaPaymentsClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Checks for decode errors on the given return value and maps the return value to the return type for the given method
   * @returns The typed return value or undefined if there was no value
   */
  decodeReturnValue<TSignature extends CrescaPaymentsNonVoidMethodSignatures>(method: TSignature, returnValue: ABIReturn | undefined) {
    return returnValue !== undefined ? getArc56ReturnValue<MethodReturn<TSignature>>(returnValue, this.appClient.getABIMethod(method), APP_SPEC.structs) : undefined
  }

  /**
   * Returns a new `CrescaPaymentsClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<CrescaPaymentsClient> {
    return new CrescaPaymentsClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `CrescaPaymentsClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<CrescaPaymentsClient> {
    return new CrescaPaymentsClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Makes a clear_state call to an existing instance of the CrescaPayments smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the CrescaPayments smart contract using the `send_payment(pay,address,string)byte[]` ABI method.
     *
    * Send ALGO to `receiver` with an optional memo.
    Caller must attach a PaymentTransaction TO this contract as the preceding transaction in the same atomic group.
    Returns a sha256 payment-ID usable for off-chain lookups.

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    sendPayment: (params: CallParams<CrescaPaymentsArgs['obj']['send_payment(pay,address,string)byte[]'] | CrescaPaymentsArgs['tuple']['send_payment(pay,address,string)byte[]']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(CrescaPaymentsParamsFactory.sendPayment(params))
    },

    /**
     * Makes a call to the CrescaPayments smart contract using the `tap_to_pay(pay,address)bool` ABI method.
     *
     * Quick send without a memo — tap-to-pay use case.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    tapToPay: (params: CallParams<CrescaPaymentsArgs['obj']['tap_to_pay(pay,address)bool'] | CrescaPaymentsArgs['tuple']['tap_to_pay(pay,address)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(CrescaPaymentsParamsFactory.tapToPay(params))
    },

    /**
     * Makes a call to the CrescaPayments smart contract using the `batch_send(pay,address[],uint64[])bool` ABI method.
     *
    * Batch send to multiple recipients (max ~8 due to AVM inner-txn limit).
    The attached `payment_txn` must cover sum(amounts). Excess μALGO is refunded to the caller.

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    batchSend: (params: CallParams<CrescaPaymentsArgs['obj']['batch_send(pay,address[],uint64[])bool'] | CrescaPaymentsArgs['tuple']['batch_send(pay,address[],uint64[])bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(CrescaPaymentsParamsFactory.batchSend(params))
    },

    /**
     * Makes a call to the CrescaPayments smart contract using the `get_total_payments()uint64` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    getTotalPayments: (params: CallParams<CrescaPaymentsArgs['obj']['get_total_payments()uint64'] | CrescaPaymentsArgs['tuple']['get_total_payments()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(CrescaPaymentsParamsFactory.getTotalPayments(params))
    },

    /**
     * Makes a call to the CrescaPayments smart contract using the `get_total_volume()uint64` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * Returns total μALGO volume processed.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    getTotalVolume: (params: CallParams<CrescaPaymentsArgs['obj']['get_total_volume()uint64'] | CrescaPaymentsArgs['tuple']['get_total_volume()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(CrescaPaymentsParamsFactory.getTotalVolume(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Makes a clear_state call to an existing instance of the CrescaPayments smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the CrescaPayments smart contract using the `send_payment(pay,address,string)byte[]` ABI method.
     *
    * Send ALGO to `receiver` with an optional memo.
    Caller must attach a PaymentTransaction TO this contract as the preceding transaction in the same atomic group.
    Returns a sha256 payment-ID usable for off-chain lookups.

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    sendPayment: (params: CallParams<CrescaPaymentsArgs['obj']['send_payment(pay,address,string)byte[]'] | CrescaPaymentsArgs['tuple']['send_payment(pay,address,string)byte[]']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(CrescaPaymentsParamsFactory.sendPayment(params))
    },

    /**
     * Makes a call to the CrescaPayments smart contract using the `tap_to_pay(pay,address)bool` ABI method.
     *
     * Quick send without a memo — tap-to-pay use case.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    tapToPay: (params: CallParams<CrescaPaymentsArgs['obj']['tap_to_pay(pay,address)bool'] | CrescaPaymentsArgs['tuple']['tap_to_pay(pay,address)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(CrescaPaymentsParamsFactory.tapToPay(params))
    },

    /**
     * Makes a call to the CrescaPayments smart contract using the `batch_send(pay,address[],uint64[])bool` ABI method.
     *
    * Batch send to multiple recipients (max ~8 due to AVM inner-txn limit).
    The attached `payment_txn` must cover sum(amounts). Excess μALGO is refunded to the caller.

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    batchSend: (params: CallParams<CrescaPaymentsArgs['obj']['batch_send(pay,address[],uint64[])bool'] | CrescaPaymentsArgs['tuple']['batch_send(pay,address[],uint64[])bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(CrescaPaymentsParamsFactory.batchSend(params))
    },

    /**
     * Makes a call to the CrescaPayments smart contract using the `get_total_payments()uint64` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    getTotalPayments: (params: CallParams<CrescaPaymentsArgs['obj']['get_total_payments()uint64'] | CrescaPaymentsArgs['tuple']['get_total_payments()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(CrescaPaymentsParamsFactory.getTotalPayments(params))
    },

    /**
     * Makes a call to the CrescaPayments smart contract using the `get_total_volume()uint64` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * Returns total μALGO volume processed.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    getTotalVolume: (params: CallParams<CrescaPaymentsArgs['obj']['get_total_volume()uint64'] | CrescaPaymentsArgs['tuple']['get_total_volume()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(CrescaPaymentsParamsFactory.getTotalVolume(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Makes a clear_state call to an existing instance of the CrescaPayments smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the CrescaPayments smart contract using the `send_payment(pay,address,string)byte[]` ABI method.
     *
    * Send ALGO to `receiver` with an optional memo.
    Caller must attach a PaymentTransaction TO this contract as the preceding transaction in the same atomic group.
    Returns a sha256 payment-ID usable for off-chain lookups.

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    sendPayment: async (params: CallParams<CrescaPaymentsArgs['obj']['send_payment(pay,address,string)byte[]'] | CrescaPaymentsArgs['tuple']['send_payment(pay,address,string)byte[]']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(CrescaPaymentsParamsFactory.sendPayment(params))
      return {...result, return: result.return as unknown as (undefined | CrescaPaymentsReturns['send_payment(pay,address,string)byte[]'])}
    },

    /**
     * Makes a call to the CrescaPayments smart contract using the `tap_to_pay(pay,address)bool` ABI method.
     *
     * Quick send without a memo — tap-to-pay use case.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    tapToPay: async (params: CallParams<CrescaPaymentsArgs['obj']['tap_to_pay(pay,address)bool'] | CrescaPaymentsArgs['tuple']['tap_to_pay(pay,address)bool']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(CrescaPaymentsParamsFactory.tapToPay(params))
      return {...result, return: result.return as unknown as (undefined | CrescaPaymentsReturns['tap_to_pay(pay,address)bool'])}
    },

    /**
     * Makes a call to the CrescaPayments smart contract using the `batch_send(pay,address[],uint64[])bool` ABI method.
     *
    * Batch send to multiple recipients (max ~8 due to AVM inner-txn limit).
    The attached `payment_txn` must cover sum(amounts). Excess μALGO is refunded to the caller.

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    batchSend: async (params: CallParams<CrescaPaymentsArgs['obj']['batch_send(pay,address[],uint64[])bool'] | CrescaPaymentsArgs['tuple']['batch_send(pay,address[],uint64[])bool']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(CrescaPaymentsParamsFactory.batchSend(params))
      return {...result, return: result.return as unknown as (undefined | CrescaPaymentsReturns['batch_send(pay,address[],uint64[])bool'])}
    },

    /**
     * Makes a call to the CrescaPayments smart contract using the `get_total_payments()uint64` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    getTotalPayments: async (params: CallParams<CrescaPaymentsArgs['obj']['get_total_payments()uint64'] | CrescaPaymentsArgs['tuple']['get_total_payments()uint64']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(CrescaPaymentsParamsFactory.getTotalPayments(params))
      return {...result, return: result.return as unknown as (undefined | CrescaPaymentsReturns['get_total_payments()uint64'])}
    },

    /**
     * Makes a call to the CrescaPayments smart contract using the `get_total_volume()uint64` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * Returns total μALGO volume processed.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    getTotalVolume: async (params: CallParams<CrescaPaymentsArgs['obj']['get_total_volume()uint64'] | CrescaPaymentsArgs['tuple']['get_total_volume()uint64']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(CrescaPaymentsParamsFactory.getTotalVolume(params))
      return {...result, return: result.return as unknown as (undefined | CrescaPaymentsReturns['get_total_volume()uint64'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new CrescaPaymentsClient(this.appClient.clone(params))
  }

  /**
   * Makes a readonly (simulated) call to the CrescaPayments smart contract using the `get_total_payments()uint64` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async getTotalPayments(params: CallParams<CrescaPaymentsArgs['obj']['get_total_payments()uint64'] | CrescaPaymentsArgs['tuple']['get_total_payments()uint64']> = {args: []}) {
    const result = await this.appClient.send.call(CrescaPaymentsParamsFactory.getTotalPayments(params))
    return result.return as unknown as CrescaPaymentsReturns['get_total_payments()uint64']
  }

  /**
   * Makes a readonly (simulated) call to the CrescaPayments smart contract using the `get_total_volume()uint64` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * Returns total μALGO volume processed.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async getTotalVolume(params: CallParams<CrescaPaymentsArgs['obj']['get_total_volume()uint64'] | CrescaPaymentsArgs['tuple']['get_total_volume()uint64']> = {args: []}) {
    const result = await this.appClient.send.call(CrescaPaymentsParamsFactory.getTotalVolume(params))
    return result.return as unknown as CrescaPaymentsReturns['get_total_volume()uint64']
  }

  /**
   * Methods to access state for the current CrescaPayments app
   */
  state = {
    /**
     * Methods to access global state for the current CrescaPayments app
     */
    global: {
      /**
       * Get all current keyed values from global state
       */
      getAll: async (): Promise<Partial<Expand<GlobalKeysState>>> => {
        const result = await this.appClient.state.global.getAll()
        return {
          totalPayments: result.total_payments,
          totalVolume: result.total_volume,
        }
      },
      /**
       * Get the current value of the total_payments key in global state
       */
      totalPayments: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("total_payments")) as bigint | undefined },
      /**
       * Get the current value of the total_volume key in global state
       */
      totalVolume: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("total_volume")) as bigint | undefined },
    },
  }

  public newGroup(): CrescaPaymentsComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a send_payment(pay,address,string)byte[] method call against the CrescaPayments contract
       */
      sendPayment(params: CallParams<CrescaPaymentsArgs['obj']['send_payment(pay,address,string)byte[]'] | CrescaPaymentsArgs['tuple']['send_payment(pay,address,string)byte[]']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.sendPayment(params)))
        resultMappers.push((v) => client.decodeReturnValue('send_payment(pay,address,string)byte[]', v))
        return this
      },
      /**
       * Add a tap_to_pay(pay,address)bool method call against the CrescaPayments contract
       */
      tapToPay(params: CallParams<CrescaPaymentsArgs['obj']['tap_to_pay(pay,address)bool'] | CrescaPaymentsArgs['tuple']['tap_to_pay(pay,address)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.tapToPay(params)))
        resultMappers.push((v) => client.decodeReturnValue('tap_to_pay(pay,address)bool', v))
        return this
      },
      /**
       * Add a batch_send(pay,address[],uint64[])bool method call against the CrescaPayments contract
       */
      batchSend(params: CallParams<CrescaPaymentsArgs['obj']['batch_send(pay,address[],uint64[])bool'] | CrescaPaymentsArgs['tuple']['batch_send(pay,address[],uint64[])bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.batchSend(params)))
        resultMappers.push((v) => client.decodeReturnValue('batch_send(pay,address[],uint64[])bool', v))
        return this
      },
      /**
       * Add a get_total_payments()uint64 method call against the CrescaPayments contract
       */
      getTotalPayments(params: CallParams<CrescaPaymentsArgs['obj']['get_total_payments()uint64'] | CrescaPaymentsArgs['tuple']['get_total_payments()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.getTotalPayments(params)))
        resultMappers.push((v) => client.decodeReturnValue('get_total_payments()uint64', v))
        return this
      },
      /**
       * Add a get_total_volume()uint64 method call against the CrescaPayments contract
       */
      getTotalVolume(params: CallParams<CrescaPaymentsArgs['obj']['get_total_volume()uint64'] | CrescaPaymentsArgs['tuple']['get_total_volume()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.getTotalVolume(params)))
        resultMappers.push((v) => client.decodeReturnValue('get_total_volume()uint64', v))
        return this
      },
      /**
       * Add a clear state call to the CrescaPayments contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as CrescaPaymentsComposer
  }
}
export type CrescaPaymentsComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the send_payment(pay,address,string)byte[] ABI method.
   *
  * Send ALGO to `receiver` with an optional memo.
  Caller must attach a PaymentTransaction TO this contract as the preceding transaction in the same atomic group.
  Returns a sha256 payment-ID usable for off-chain lookups.

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  sendPayment(params?: CallParams<CrescaPaymentsArgs['obj']['send_payment(pay,address,string)byte[]'] | CrescaPaymentsArgs['tuple']['send_payment(pay,address,string)byte[]']>): CrescaPaymentsComposer<[...TReturns, CrescaPaymentsReturns['send_payment(pay,address,string)byte[]'] | undefined]>

  /**
   * Calls the tap_to_pay(pay,address)bool ABI method.
   *
   * Quick send without a memo — tap-to-pay use case.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  tapToPay(params?: CallParams<CrescaPaymentsArgs['obj']['tap_to_pay(pay,address)bool'] | CrescaPaymentsArgs['tuple']['tap_to_pay(pay,address)bool']>): CrescaPaymentsComposer<[...TReturns, CrescaPaymentsReturns['tap_to_pay(pay,address)bool'] | undefined]>

  /**
   * Calls the batch_send(pay,address[],uint64[])bool ABI method.
   *
  * Batch send to multiple recipients (max ~8 due to AVM inner-txn limit).
  The attached `payment_txn` must cover sum(amounts). Excess μALGO is refunded to the caller.

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  batchSend(params?: CallParams<CrescaPaymentsArgs['obj']['batch_send(pay,address[],uint64[])bool'] | CrescaPaymentsArgs['tuple']['batch_send(pay,address[],uint64[])bool']>): CrescaPaymentsComposer<[...TReturns, CrescaPaymentsReturns['batch_send(pay,address[],uint64[])bool'] | undefined]>

  /**
   * Calls the get_total_payments()uint64 ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  getTotalPayments(params?: CallParams<CrescaPaymentsArgs['obj']['get_total_payments()uint64'] | CrescaPaymentsArgs['tuple']['get_total_payments()uint64']>): CrescaPaymentsComposer<[...TReturns, CrescaPaymentsReturns['get_total_payments()uint64'] | undefined]>

  /**
   * Calls the get_total_volume()uint64 ABI method.
   *
   * Returns total μALGO volume processed.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  getTotalVolume(params?: CallParams<CrescaPaymentsArgs['obj']['get_total_volume()uint64'] | CrescaPaymentsArgs['tuple']['get_total_volume()uint64']>): CrescaPaymentsComposer<[...TReturns, CrescaPaymentsReturns['get_total_volume()uint64'] | undefined]>

  /**
   * Makes a clear_state call to an existing instance of the CrescaPayments smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): CrescaPaymentsComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): CrescaPaymentsComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<CrescaPaymentsComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<CrescaPaymentsComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<CrescaPaymentsComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<CrescaPaymentsComposerResults<TReturns>>
}
export type CrescaPaymentsComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

