/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"Lottery","structs":{},"methods":[{"name":"create_application","args":[{"type":"uint64","name":"entry_fee"}],"returns":{"type":"void"},"actions":{"create":["NoOp"],"call":[]},"readonly":false,"desc":"Initialize the lottery contract with an entry fee.","events":[],"recommendations":{}},{"name":"enter_lottery","args":[{"type":"pay","name":"payment_txn"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Allow users to enter the lottery by sending the entry fee.","events":[],"recommendations":{}},{"name":"pick_winner","args":[],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Allows the contract creator to randomly pick a winner.","events":[],"recommendations":{}},{"name":"delete_application","args":[],"returns":{"type":"void"},"actions":{"create":[],"call":["DeleteApplication"]},"readonly":false,"desc":"Allows the creator to delete the application.","events":[],"recommendations":{}}],"arcs":[22,28],"networks":{},"state":{"schema":{"global":{"ints":2,"bytes":1},"local":{"ints":0,"bytes":0}},"keys":{"global":{"entry_fee":{"keyType":"AVMString","valueType":"AVMUint64","key":"ZW50cnlfZmVl"},"total_entries":{"keyType":"AVMString","valueType":"AVMUint64","key":"dG90YWxfZW50cmllcw=="},"creator_address":{"keyType":"AVMString","valueType":"AVMBytes","key":"Y3JlYXRvcl9hZGRyZXNz"}},"local":{},"box":{}},"maps":{"global":{},"local":{},"box":{}}},"bareActions":{"create":[],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[96],"errorMessage":"OnCompletion is not DeleteApplication"},{"pc":[108,120,142],"errorMessage":"OnCompletion is not NoOp"},{"pc":[236],"errorMessage":"account funded"},{"pc":[146],"errorMessage":"can only call when creating"},{"pc":[99,111,123],"errorMessage":"can only call when not creating"},{"pc":[207,259,266,270],"errorMessage":"check self.creator_address exists"},{"pc":[189],"errorMessage":"check self.entry_fee exists"},{"pc":[195,213,222],"errorMessage":"check self.total_entries exists"},{"pc":[133],"errorMessage":"transaction type is pay"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"events":[]} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


/**
 * The argument types for the Lottery contract
 */
export type LotteryArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'create_application(uint64)void': {
      entryFee: bigint | number
    }
    'enter_lottery(pay)void': {
      paymentTxn: AppMethodCallTransactionArgument
    }
    'pick_winner()void': Record<string, never>
    'delete_application()void': Record<string, never>
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'create_application(uint64)void': [entryFee: bigint | number]
    'enter_lottery(pay)void': [paymentTxn: AppMethodCallTransactionArgument]
    'pick_winner()void': []
    'delete_application()void': []
  }
}

/**
 * The return type for each method
 */
export type LotteryReturns = {
  'create_application(uint64)void': void
  'enter_lottery(pay)void': void
  'pick_winner()void': void
  'delete_application()void': void
}

/**
 * Defines the types of available calls and state of the Lottery smart contract.
 */
export type LotteryTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'create_application(uint64)void' | 'create_application', {
      argsObj: LotteryArgs['obj']['create_application(uint64)void']
      argsTuple: LotteryArgs['tuple']['create_application(uint64)void']
      returns: LotteryReturns['create_application(uint64)void']
    }>
    & Record<'enter_lottery(pay)void' | 'enter_lottery', {
      argsObj: LotteryArgs['obj']['enter_lottery(pay)void']
      argsTuple: LotteryArgs['tuple']['enter_lottery(pay)void']
      returns: LotteryReturns['enter_lottery(pay)void']
    }>
    & Record<'pick_winner()void' | 'pick_winner', {
      argsObj: LotteryArgs['obj']['pick_winner()void']
      argsTuple: LotteryArgs['tuple']['pick_winner()void']
      returns: LotteryReturns['pick_winner()void']
    }>
    & Record<'delete_application()void' | 'delete_application', {
      argsObj: LotteryArgs['obj']['delete_application()void']
      argsTuple: LotteryArgs['tuple']['delete_application()void']
      returns: LotteryReturns['delete_application()void']
    }>
  /**
   * Defines the shape of the state of the application.
   */
  state: {
    global: {
      keys: {
        entryFee: bigint
        totalEntries: bigint
        creatorAddress: BinaryState
      }
      maps: {}
    }
  }
}

/**
 * Defines the possible abi call signatures.
 */
export type LotterySignatures = keyof LotteryTypes['methods']
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the Lottery smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends LotterySignatures> = LotteryTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the Lottery smart contract to the method's return type
 */
export type MethodReturn<TSignature extends LotterySignatures> = LotteryTypes['methods'][TSignature]['returns']

/**
 * Defines the shape of the keyed global state of the application.
 */
export type GlobalKeysState = LotteryTypes['state']['global']['keys']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the Lottery smart contract
 */
export abstract class LotteryParamsFactory {
  /**
   * Constructs a no op call for the enter_lottery(pay)void ABI method
   *
   * Allow users to enter the lottery by sending the entry fee.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static enterLottery(params: CallParams<LotteryArgs['obj']['enter_lottery(pay)void'] | LotteryArgs['tuple']['enter_lottery(pay)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'enter_lottery(pay)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.paymentTxn],
    }
  }
  /**
   * Constructs a no op call for the pick_winner()void ABI method
   *
   * Allows the contract creator to randomly pick a winner.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static pickWinner(params: CallParams<LotteryArgs['obj']['pick_winner()void'] | LotteryArgs['tuple']['pick_winner()void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'pick_winner()void' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
}

/**
 * A client to make calls to the Lottery smart contract
 */
export class LotteryClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `LotteryClient`
   *
   * @param appClient An `AppClient` instance which has been created with the Lottery app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `LotteryClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Returns a new `LotteryClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<LotteryClient> {
    return new LotteryClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `LotteryClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<LotteryClient> {
    return new LotteryClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Makes a clear_state call to an existing instance of the Lottery smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the Lottery smart contract using the `enter_lottery(pay)void` ABI method.
     *
     * Allow users to enter the lottery by sending the entry fee.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    enterLottery: (params: CallParams<LotteryArgs['obj']['enter_lottery(pay)void'] | LotteryArgs['tuple']['enter_lottery(pay)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(LotteryParamsFactory.enterLottery(params))
    },

    /**
     * Makes a call to the Lottery smart contract using the `pick_winner()void` ABI method.
     *
     * Allows the contract creator to randomly pick a winner.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    pickWinner: (params: CallParams<LotteryArgs['obj']['pick_winner()void'] | LotteryArgs['tuple']['pick_winner()void']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(LotteryParamsFactory.pickWinner(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Makes a clear_state call to an existing instance of the Lottery smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the Lottery smart contract using the `enter_lottery(pay)void` ABI method.
     *
     * Allow users to enter the lottery by sending the entry fee.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    enterLottery: (params: CallParams<LotteryArgs['obj']['enter_lottery(pay)void'] | LotteryArgs['tuple']['enter_lottery(pay)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(LotteryParamsFactory.enterLottery(params))
    },

    /**
     * Makes a call to the Lottery smart contract using the `pick_winner()void` ABI method.
     *
     * Allows the contract creator to randomly pick a winner.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    pickWinner: (params: CallParams<LotteryArgs['obj']['pick_winner()void'] | LotteryArgs['tuple']['pick_winner()void']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(LotteryParamsFactory.pickWinner(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Makes a clear_state call to an existing instance of the Lottery smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the Lottery smart contract using the `enter_lottery(pay)void` ABI method.
     *
     * Allow users to enter the lottery by sending the entry fee.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    enterLottery: async (params: CallParams<LotteryArgs['obj']['enter_lottery(pay)void'] | LotteryArgs['tuple']['enter_lottery(pay)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(LotteryParamsFactory.enterLottery(params))
      return {...result, return: result.return as unknown as (undefined | LotteryReturns['enter_lottery(pay)void'])}
    },

    /**
     * Makes a call to the Lottery smart contract using the `pick_winner()void` ABI method.
     *
     * Allows the contract creator to randomly pick a winner.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    pickWinner: async (params: CallParams<LotteryArgs['obj']['pick_winner()void'] | LotteryArgs['tuple']['pick_winner()void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(LotteryParamsFactory.pickWinner(params))
      return {...result, return: result.return as unknown as (undefined | LotteryReturns['pick_winner()void'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new LotteryClient(this.appClient.clone(params))
  }

  /**
   * Methods to access state for the current Lottery app
   */
  state = {
    /**
     * Methods to access global state for the current Lottery app
     */
    global: {
      /**
       * Get all current keyed values from global state
       */
      getAll: async (): Promise<Partial<Expand<GlobalKeysState>>> => {
        const result = await this.appClient.state.global.getAll()
        return {
          entryFee: result.entry_fee,
          totalEntries: result.total_entries,
          creatorAddress: new BinaryStateValue(result.creator_address),
        }
      },
      /**
       * Get the current value of the entry_fee key in global state
       */
      entryFee: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("entry_fee")) as bigint | undefined },
      /**
       * Get the current value of the total_entries key in global state
       */
      totalEntries: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("total_entries")) as bigint | undefined },
      /**
       * Get the current value of the creator_address key in global state
       */
      creatorAddress: async (): Promise<BinaryState> => { return new BinaryStateValue((await this.appClient.state.global.getValue("creator_address")) as Uint8Array | undefined) },
    },
  }

  public newGroup(): LotteryComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a enter_lottery(pay)void method call against the Lottery contract
       */
      enterLottery(params: CallParams<LotteryArgs['obj']['enter_lottery(pay)void'] | LotteryArgs['tuple']['enter_lottery(pay)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.enterLottery(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a pick_winner()void method call against the Lottery contract
       */
      pickWinner(params: CallParams<LotteryArgs['obj']['pick_winner()void'] | LotteryArgs['tuple']['pick_winner()void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.pickWinner(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a clear state call to the Lottery contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as LotteryComposer
  }
}
export type LotteryComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the enter_lottery(pay)void ABI method.
   *
   * Allow users to enter the lottery by sending the entry fee.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  enterLottery(params?: CallParams<LotteryArgs['obj']['enter_lottery(pay)void'] | LotteryArgs['tuple']['enter_lottery(pay)void']>): LotteryComposer<[...TReturns, LotteryReturns['enter_lottery(pay)void'] | undefined]>

  /**
   * Calls the pick_winner()void ABI method.
   *
   * Allows the contract creator to randomly pick a winner.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  pickWinner(params?: CallParams<LotteryArgs['obj']['pick_winner()void'] | LotteryArgs['tuple']['pick_winner()void']>): LotteryComposer<[...TReturns, LotteryReturns['pick_winner()void'] | undefined]>

  /**
   * Makes a clear_state call to an existing instance of the Lottery smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): LotteryComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): LotteryComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<LotteryComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<LotteryComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<LotteryComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<LotteryComposerResults<TReturns>>
}
export type LotteryComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

