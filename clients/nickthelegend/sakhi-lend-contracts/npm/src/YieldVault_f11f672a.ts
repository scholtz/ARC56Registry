/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"YieldVault","structs":{},"methods":[{"name":"createApplication","args":[],"returns":{"type":"void"},"actions":{"create":["NoOp"],"call":[]},"readonly":false,"events":[],"recommendations":{}},{"name":"bootstrap","args":[{"type":"uint64","name":"asset"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Bootstraps the vault: sets USDC asset and opts-in.\nAdmin only.","events":[],"recommendations":{}},{"name":"depositFirst","args":[{"type":"axfer","name":"axfer"},{"type":"pay","name":"mbrPayment"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Deposits USDC into the vault for the first time.\nRequires a payment txn to cover MBR.","events":[],"recommendations":{}},{"name":"depositMore","args":[{"type":"axfer","name":"axfer"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Deposits more USDC into the vault for existing users.","events":[],"recommendations":{}},{"name":"withdraw","args":[{"type":"uint64","name":"amount"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Withdraws USDC from the vault.","events":[],"recommendations":{}},{"name":"fundReserves","args":[{"type":"axfer","name":"axfer"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Admin method to fund the yield reserves.","events":[],"recommendations":{}},{"name":"setPause","args":[{"type":"bool","name":"paused"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Pause or unpause the protocol.","events":[],"recommendations":{}},{"name":"getBalance","args":[{"type":"address","name":"user"}],"returns":{"type":"uint64"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"External view of balance including accrued yield.","events":[],"recommendations":{}},{"name":"deleteApplication","args":[],"returns":{"type":"void"},"actions":{"create":[],"call":["DeleteApplication"]},"readonly":false,"desc":"Allows the admin to delete the application.","events":[],"recommendations":{}}],"arcs":[22,28],"networks":{},"state":{"schema":{"global":{"ints":3,"bytes":1},"local":{"ints":0,"bytes":0}},"keys":{"global":{"creator":{"keyType":"AVMString","valueType":"address","key":"Y3JlYXRvcg==","desc":"The creator of the application"},"usdcAssetId":{"keyType":"AVMString","valueType":"AVMUint64","key":"dXNkY0Fzc2V0SWQ=","desc":"The asset ID of the USDC used in this vault"},"reserveBalance":{"keyType":"AVMString","valueType":"AVMUint64","key":"cmVzZXJ2ZUJhbGFuY2U=","desc":"Balance of the vault that belongs to reserves (yield runway)"},"isPaused":{"keyType":"AVMString","valueType":"AVMUint64","key":"aXNQYXVzZWQ=","desc":"Protocol pause state"}},"local":{},"box":{}},"maps":{"global":{},"local":{},"box":{"deposits":{"keyType":"address","valueType":"uint64","desc":"User balance in the vault (USDC microunits)","prefix":"ZA=="},"lastBlocks":{"keyType":"address","valueType":"uint64","desc":"Last block round yield was accrued for this user","prefix":"Yg=="}}}},"bareActions":{"create":[],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[254,623,707,816],"errorMessage":"Admin only"},{"pc":[261],"errorMessage":"Already bootstrapped"},{"pc":[365,458],"errorMessage":"Below min deposit"},{"pc":[478],"errorMessage":"Box must have value"},{"pc":[396],"errorMessage":"Insufficient MBR payment"},{"pc":[541],"errorMessage":"Insufficient balance"},{"pc":[349,444,633],"errorMessage":"Invalid asset"},{"pc":[386],"errorMessage":"MBR payment to vault"},{"pc":[357,451,640],"errorMessage":"Must deposit to vault"},{"pc":[209],"errorMessage":"OnCompletion must be DeleteApplication && can only call when not creating"},{"pc":[124],"errorMessage":"OnCompletion must be NoOp"},{"pc":[339,435,516],"errorMessage":"Protocol is paused"},{"pc":[378],"errorMessage":"User already exists"},{"pc":[470],"errorMessage":"User does not exist"},{"pc":[240,337,347,433,442,514,560,609,631,645,693,802],"errorMessage":"check GlobalState exists"},{"pc":[685],"errorMessage":"invalid number of bytes for arc4.bool"},{"pc":[726],"errorMessage":"invalid number of bytes for arc4.static_array<arc4.uint8, 32>"},{"pc":[233,509],"errorMessage":"invalid number of bytes for arc4.uint64"},{"pc":[323,429,603],"errorMessage":"transaction type is axfer"},{"pc":[333],"errorMessage":"transaction type is pay"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"events":[]} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


/**
 * The argument types for the YieldVault contract
 */
export type YieldVaultArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'createApplication()void': Record<string, never>
    'bootstrap(uint64)void': {
      asset: bigint | number
    }
    'depositFirst(axfer,pay)void': {
      axfer: AppMethodCallTransactionArgument
      mbrPayment: AppMethodCallTransactionArgument
    }
    'depositMore(axfer)void': {
      axfer: AppMethodCallTransactionArgument
    }
    'withdraw(uint64)void': {
      amount: bigint | number
    }
    'fundReserves(axfer)void': {
      axfer: AppMethodCallTransactionArgument
    }
    'setPause(bool)void': {
      paused: boolean
    }
    'getBalance(address)uint64': {
      user: string
    }
    'deleteApplication()void': Record<string, never>
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'createApplication()void': []
    'bootstrap(uint64)void': [asset: bigint | number]
    'depositFirst(axfer,pay)void': [axfer: AppMethodCallTransactionArgument, mbrPayment: AppMethodCallTransactionArgument]
    'depositMore(axfer)void': [axfer: AppMethodCallTransactionArgument]
    'withdraw(uint64)void': [amount: bigint | number]
    'fundReserves(axfer)void': [axfer: AppMethodCallTransactionArgument]
    'setPause(bool)void': [paused: boolean]
    'getBalance(address)uint64': [user: string]
    'deleteApplication()void': []
  }
}

/**
 * The return type for each method
 */
export type YieldVaultReturns = {
  'createApplication()void': void
  'bootstrap(uint64)void': void
  'depositFirst(axfer,pay)void': void
  'depositMore(axfer)void': void
  'withdraw(uint64)void': void
  'fundReserves(axfer)void': void
  'setPause(bool)void': void
  'getBalance(address)uint64': bigint
  'deleteApplication()void': void
}

/**
 * Defines the types of available calls and state of the YieldVault smart contract.
 */
export type YieldVaultTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'createApplication()void' | 'createApplication', {
      argsObj: YieldVaultArgs['obj']['createApplication()void']
      argsTuple: YieldVaultArgs['tuple']['createApplication()void']
      returns: YieldVaultReturns['createApplication()void']
    }>
    & Record<'bootstrap(uint64)void' | 'bootstrap', {
      argsObj: YieldVaultArgs['obj']['bootstrap(uint64)void']
      argsTuple: YieldVaultArgs['tuple']['bootstrap(uint64)void']
      returns: YieldVaultReturns['bootstrap(uint64)void']
    }>
    & Record<'depositFirst(axfer,pay)void' | 'depositFirst', {
      argsObj: YieldVaultArgs['obj']['depositFirst(axfer,pay)void']
      argsTuple: YieldVaultArgs['tuple']['depositFirst(axfer,pay)void']
      returns: YieldVaultReturns['depositFirst(axfer,pay)void']
    }>
    & Record<'depositMore(axfer)void' | 'depositMore', {
      argsObj: YieldVaultArgs['obj']['depositMore(axfer)void']
      argsTuple: YieldVaultArgs['tuple']['depositMore(axfer)void']
      returns: YieldVaultReturns['depositMore(axfer)void']
    }>
    & Record<'withdraw(uint64)void' | 'withdraw', {
      argsObj: YieldVaultArgs['obj']['withdraw(uint64)void']
      argsTuple: YieldVaultArgs['tuple']['withdraw(uint64)void']
      returns: YieldVaultReturns['withdraw(uint64)void']
    }>
    & Record<'fundReserves(axfer)void' | 'fundReserves', {
      argsObj: YieldVaultArgs['obj']['fundReserves(axfer)void']
      argsTuple: YieldVaultArgs['tuple']['fundReserves(axfer)void']
      returns: YieldVaultReturns['fundReserves(axfer)void']
    }>
    & Record<'setPause(bool)void' | 'setPause', {
      argsObj: YieldVaultArgs['obj']['setPause(bool)void']
      argsTuple: YieldVaultArgs['tuple']['setPause(bool)void']
      returns: YieldVaultReturns['setPause(bool)void']
    }>
    & Record<'getBalance(address)uint64' | 'getBalance', {
      argsObj: YieldVaultArgs['obj']['getBalance(address)uint64']
      argsTuple: YieldVaultArgs['tuple']['getBalance(address)uint64']
      returns: YieldVaultReturns['getBalance(address)uint64']
    }>
    & Record<'deleteApplication()void' | 'deleteApplication', {
      argsObj: YieldVaultArgs['obj']['deleteApplication()void']
      argsTuple: YieldVaultArgs['tuple']['deleteApplication()void']
      returns: YieldVaultReturns['deleteApplication()void']
    }>
  /**
   * Defines the shape of the state of the application.
   */
  state: {
    global: {
      keys: {
        /**
         * The creator of the application
         */
        creator: string
        /**
         * The asset ID of the USDC used in this vault
         */
        usdcAssetId: bigint
        /**
         * Balance of the vault that belongs to reserves (yield runway)
         */
        reserveBalance: bigint
        /**
         * Protocol pause state
         */
        isPaused: bigint
      }
      maps: {}
    }
    box: {
      keys: {}
      maps: {
        /**
         * User balance in the vault (USDC microunits)
         */
        deposits: Map<string, bigint>
        /**
         * Last block round yield was accrued for this user
         */
        lastBlocks: Map<string, bigint>
      }
    }
  }
}

/**
 * Defines the possible abi call signatures.
 */
export type YieldVaultSignatures = keyof YieldVaultTypes['methods']
/**
 * Defines the possible abi call signatures for methods that return a non-void value.
 */
export type YieldVaultNonVoidMethodSignatures = keyof YieldVaultTypes['methods'] extends infer T ? T extends keyof YieldVaultTypes['methods'] ? MethodReturn<T> extends void ? never : T  : never : never
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the YieldVault smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends YieldVaultSignatures> = YieldVaultTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the YieldVault smart contract to the method's return type
 */
export type MethodReturn<TSignature extends YieldVaultSignatures> = YieldVaultTypes['methods'][TSignature]['returns']

/**
 * Defines the shape of the keyed global state of the application.
 */
export type GlobalKeysState = YieldVaultTypes['state']['global']['keys']

/**
 * Defines the shape of the keyed box state of the application.
 */
export type BoxKeysState = YieldVaultTypes['state']['box']['keys']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the YieldVault smart contract
 */
export abstract class YieldVaultParamsFactory {
  /**
   * Constructs a no op call for the bootstrap(uint64)void ABI method
   *
  * Bootstraps the vault: sets USDC asset and opts-in.
  Admin only.

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static bootstrap(params: CallParams<YieldVaultArgs['obj']['bootstrap(uint64)void'] | YieldVaultArgs['tuple']['bootstrap(uint64)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'bootstrap(uint64)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.asset],
    }
  }
  /**
   * Constructs a no op call for the depositFirst(axfer,pay)void ABI method
   *
  * Deposits USDC into the vault for the first time.
  Requires a payment txn to cover MBR.

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static depositFirst(params: CallParams<YieldVaultArgs['obj']['depositFirst(axfer,pay)void'] | YieldVaultArgs['tuple']['depositFirst(axfer,pay)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'depositFirst(axfer,pay)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.axfer, params.args.mbrPayment],
    }
  }
  /**
   * Constructs a no op call for the depositMore(axfer)void ABI method
   *
   * Deposits more USDC into the vault for existing users.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static depositMore(params: CallParams<YieldVaultArgs['obj']['depositMore(axfer)void'] | YieldVaultArgs['tuple']['depositMore(axfer)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'depositMore(axfer)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.axfer],
    }
  }
  /**
   * Constructs a no op call for the withdraw(uint64)void ABI method
   *
   * Withdraws USDC from the vault.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static withdraw(params: CallParams<YieldVaultArgs['obj']['withdraw(uint64)void'] | YieldVaultArgs['tuple']['withdraw(uint64)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'withdraw(uint64)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.amount],
    }
  }
  /**
   * Constructs a no op call for the fundReserves(axfer)void ABI method
   *
   * Admin method to fund the yield reserves.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static fundReserves(params: CallParams<YieldVaultArgs['obj']['fundReserves(axfer)void'] | YieldVaultArgs['tuple']['fundReserves(axfer)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'fundReserves(axfer)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.axfer],
    }
  }
  /**
   * Constructs a no op call for the setPause(bool)void ABI method
   *
   * Pause or unpause the protocol.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static setPause(params: CallParams<YieldVaultArgs['obj']['setPause(bool)void'] | YieldVaultArgs['tuple']['setPause(bool)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'setPause(bool)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.paused],
    }
  }
  /**
   * Constructs a no op call for the getBalance(address)uint64 ABI method
   *
   * External view of balance including accrued yield.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static getBalance(params: CallParams<YieldVaultArgs['obj']['getBalance(address)uint64'] | YieldVaultArgs['tuple']['getBalance(address)uint64']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'getBalance(address)uint64' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.user],
    }
  }
}

/**
 * A client to make calls to the YieldVault smart contract
 */
export class YieldVaultClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `YieldVaultClient`
   *
   * @param appClient An `AppClient` instance which has been created with the YieldVault app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `YieldVaultClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Checks for decode errors on the given return value and maps the return value to the return type for the given method
   * @returns The typed return value or undefined if there was no value
   */
  decodeReturnValue<TSignature extends YieldVaultNonVoidMethodSignatures>(method: TSignature, returnValue: ABIReturn | undefined) {
    return returnValue !== undefined ? getArc56ReturnValue<MethodReturn<TSignature>>(returnValue, this.appClient.getABIMethod(method), APP_SPEC.structs) : undefined
  }

  /**
   * Returns a new `YieldVaultClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<YieldVaultClient> {
    return new YieldVaultClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `YieldVaultClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<YieldVaultClient> {
    return new YieldVaultClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Makes a clear_state call to an existing instance of the YieldVault smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the YieldVault smart contract using the `bootstrap(uint64)void` ABI method.
     *
    * Bootstraps the vault: sets USDC asset and opts-in.
    Admin only.

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    bootstrap: (params: CallParams<YieldVaultArgs['obj']['bootstrap(uint64)void'] | YieldVaultArgs['tuple']['bootstrap(uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(YieldVaultParamsFactory.bootstrap(params))
    },

    /**
     * Makes a call to the YieldVault smart contract using the `depositFirst(axfer,pay)void` ABI method.
     *
    * Deposits USDC into the vault for the first time.
    Requires a payment txn to cover MBR.

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    depositFirst: (params: CallParams<YieldVaultArgs['obj']['depositFirst(axfer,pay)void'] | YieldVaultArgs['tuple']['depositFirst(axfer,pay)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(YieldVaultParamsFactory.depositFirst(params))
    },

    /**
     * Makes a call to the YieldVault smart contract using the `depositMore(axfer)void` ABI method.
     *
     * Deposits more USDC into the vault for existing users.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    depositMore: (params: CallParams<YieldVaultArgs['obj']['depositMore(axfer)void'] | YieldVaultArgs['tuple']['depositMore(axfer)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(YieldVaultParamsFactory.depositMore(params))
    },

    /**
     * Makes a call to the YieldVault smart contract using the `withdraw(uint64)void` ABI method.
     *
     * Withdraws USDC from the vault.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    withdraw: (params: CallParams<YieldVaultArgs['obj']['withdraw(uint64)void'] | YieldVaultArgs['tuple']['withdraw(uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(YieldVaultParamsFactory.withdraw(params))
    },

    /**
     * Makes a call to the YieldVault smart contract using the `fundReserves(axfer)void` ABI method.
     *
     * Admin method to fund the yield reserves.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    fundReserves: (params: CallParams<YieldVaultArgs['obj']['fundReserves(axfer)void'] | YieldVaultArgs['tuple']['fundReserves(axfer)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(YieldVaultParamsFactory.fundReserves(params))
    },

    /**
     * Makes a call to the YieldVault smart contract using the `setPause(bool)void` ABI method.
     *
     * Pause or unpause the protocol.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    setPause: (params: CallParams<YieldVaultArgs['obj']['setPause(bool)void'] | YieldVaultArgs['tuple']['setPause(bool)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(YieldVaultParamsFactory.setPause(params))
    },

    /**
     * Makes a call to the YieldVault smart contract using the `getBalance(address)uint64` ABI method.
     *
     * External view of balance including accrued yield.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    getBalance: (params: CallParams<YieldVaultArgs['obj']['getBalance(address)uint64'] | YieldVaultArgs['tuple']['getBalance(address)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(YieldVaultParamsFactory.getBalance(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Makes a clear_state call to an existing instance of the YieldVault smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the YieldVault smart contract using the `bootstrap(uint64)void` ABI method.
     *
    * Bootstraps the vault: sets USDC asset and opts-in.
    Admin only.

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    bootstrap: (params: CallParams<YieldVaultArgs['obj']['bootstrap(uint64)void'] | YieldVaultArgs['tuple']['bootstrap(uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(YieldVaultParamsFactory.bootstrap(params))
    },

    /**
     * Makes a call to the YieldVault smart contract using the `depositFirst(axfer,pay)void` ABI method.
     *
    * Deposits USDC into the vault for the first time.
    Requires a payment txn to cover MBR.

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    depositFirst: (params: CallParams<YieldVaultArgs['obj']['depositFirst(axfer,pay)void'] | YieldVaultArgs['tuple']['depositFirst(axfer,pay)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(YieldVaultParamsFactory.depositFirst(params))
    },

    /**
     * Makes a call to the YieldVault smart contract using the `depositMore(axfer)void` ABI method.
     *
     * Deposits more USDC into the vault for existing users.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    depositMore: (params: CallParams<YieldVaultArgs['obj']['depositMore(axfer)void'] | YieldVaultArgs['tuple']['depositMore(axfer)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(YieldVaultParamsFactory.depositMore(params))
    },

    /**
     * Makes a call to the YieldVault smart contract using the `withdraw(uint64)void` ABI method.
     *
     * Withdraws USDC from the vault.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    withdraw: (params: CallParams<YieldVaultArgs['obj']['withdraw(uint64)void'] | YieldVaultArgs['tuple']['withdraw(uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(YieldVaultParamsFactory.withdraw(params))
    },

    /**
     * Makes a call to the YieldVault smart contract using the `fundReserves(axfer)void` ABI method.
     *
     * Admin method to fund the yield reserves.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    fundReserves: (params: CallParams<YieldVaultArgs['obj']['fundReserves(axfer)void'] | YieldVaultArgs['tuple']['fundReserves(axfer)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(YieldVaultParamsFactory.fundReserves(params))
    },

    /**
     * Makes a call to the YieldVault smart contract using the `setPause(bool)void` ABI method.
     *
     * Pause or unpause the protocol.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    setPause: (params: CallParams<YieldVaultArgs['obj']['setPause(bool)void'] | YieldVaultArgs['tuple']['setPause(bool)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(YieldVaultParamsFactory.setPause(params))
    },

    /**
     * Makes a call to the YieldVault smart contract using the `getBalance(address)uint64` ABI method.
     *
     * External view of balance including accrued yield.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    getBalance: (params: CallParams<YieldVaultArgs['obj']['getBalance(address)uint64'] | YieldVaultArgs['tuple']['getBalance(address)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(YieldVaultParamsFactory.getBalance(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Makes a clear_state call to an existing instance of the YieldVault smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the YieldVault smart contract using the `bootstrap(uint64)void` ABI method.
     *
    * Bootstraps the vault: sets USDC asset and opts-in.
    Admin only.

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    bootstrap: async (params: CallParams<YieldVaultArgs['obj']['bootstrap(uint64)void'] | YieldVaultArgs['tuple']['bootstrap(uint64)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(YieldVaultParamsFactory.bootstrap(params))
      return {...result, return: result.return as unknown as (undefined | YieldVaultReturns['bootstrap(uint64)void'])}
    },

    /**
     * Makes a call to the YieldVault smart contract using the `depositFirst(axfer,pay)void` ABI method.
     *
    * Deposits USDC into the vault for the first time.
    Requires a payment txn to cover MBR.

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    depositFirst: async (params: CallParams<YieldVaultArgs['obj']['depositFirst(axfer,pay)void'] | YieldVaultArgs['tuple']['depositFirst(axfer,pay)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(YieldVaultParamsFactory.depositFirst(params))
      return {...result, return: result.return as unknown as (undefined | YieldVaultReturns['depositFirst(axfer,pay)void'])}
    },

    /**
     * Makes a call to the YieldVault smart contract using the `depositMore(axfer)void` ABI method.
     *
     * Deposits more USDC into the vault for existing users.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    depositMore: async (params: CallParams<YieldVaultArgs['obj']['depositMore(axfer)void'] | YieldVaultArgs['tuple']['depositMore(axfer)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(YieldVaultParamsFactory.depositMore(params))
      return {...result, return: result.return as unknown as (undefined | YieldVaultReturns['depositMore(axfer)void'])}
    },

    /**
     * Makes a call to the YieldVault smart contract using the `withdraw(uint64)void` ABI method.
     *
     * Withdraws USDC from the vault.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    withdraw: async (params: CallParams<YieldVaultArgs['obj']['withdraw(uint64)void'] | YieldVaultArgs['tuple']['withdraw(uint64)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(YieldVaultParamsFactory.withdraw(params))
      return {...result, return: result.return as unknown as (undefined | YieldVaultReturns['withdraw(uint64)void'])}
    },

    /**
     * Makes a call to the YieldVault smart contract using the `fundReserves(axfer)void` ABI method.
     *
     * Admin method to fund the yield reserves.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    fundReserves: async (params: CallParams<YieldVaultArgs['obj']['fundReserves(axfer)void'] | YieldVaultArgs['tuple']['fundReserves(axfer)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(YieldVaultParamsFactory.fundReserves(params))
      return {...result, return: result.return as unknown as (undefined | YieldVaultReturns['fundReserves(axfer)void'])}
    },

    /**
     * Makes a call to the YieldVault smart contract using the `setPause(bool)void` ABI method.
     *
     * Pause or unpause the protocol.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    setPause: async (params: CallParams<YieldVaultArgs['obj']['setPause(bool)void'] | YieldVaultArgs['tuple']['setPause(bool)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(YieldVaultParamsFactory.setPause(params))
      return {...result, return: result.return as unknown as (undefined | YieldVaultReturns['setPause(bool)void'])}
    },

    /**
     * Makes a call to the YieldVault smart contract using the `getBalance(address)uint64` ABI method.
     *
     * External view of balance including accrued yield.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    getBalance: async (params: CallParams<YieldVaultArgs['obj']['getBalance(address)uint64'] | YieldVaultArgs['tuple']['getBalance(address)uint64']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(YieldVaultParamsFactory.getBalance(params))
      return {...result, return: result.return as unknown as (undefined | YieldVaultReturns['getBalance(address)uint64'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new YieldVaultClient(this.appClient.clone(params))
  }

  /**
   * Methods to access state for the current YieldVault app
   */
  state = {
    /**
     * Methods to access global state for the current YieldVault app
     */
    global: {
      /**
       * Get all current keyed values from global state
       */
      getAll: async (): Promise<Partial<Expand<GlobalKeysState>>> => {
        const result = await this.appClient.state.global.getAll()
        return {
          creator: result.creator,
          usdcAssetId: result.usdcAssetId,
          reserveBalance: result.reserveBalance,
          isPaused: result.isPaused,
        }
      },
      /**
       * Get the current value of the creator key in global state
       */
      creator: async (): Promise<string | undefined> => { return (await this.appClient.state.global.getValue("creator")) as string | undefined },
      /**
       * Get the current value of the usdcAssetId key in global state
       */
      usdcAssetId: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("usdcAssetId")) as bigint | undefined },
      /**
       * Get the current value of the reserveBalance key in global state
       */
      reserveBalance: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("reserveBalance")) as bigint | undefined },
      /**
       * Get the current value of the isPaused key in global state
       */
      isPaused: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("isPaused")) as bigint | undefined },
    },
    /**
     * Methods to access box state for the current YieldVault app
     */
    box: {
      /**
       * Get all current keyed values from box state
       */
      getAll: async (): Promise<Partial<Expand<BoxKeysState>>> => {
        const result = await this.appClient.state.box.getAll()
        return {
        }
      },
      /**
       * Get values from the deposits map in box state
       */
      deposits: {
        /**
         * Get all current values of the deposits map in box state
         */
        getMap: async (): Promise<Map<string, bigint>> => { return (await this.appClient.state.box.getMap("deposits")) as Map<string, bigint> },
        /**
         * Get a current value of the deposits map by key from box state
         */
        value: async (key: string): Promise<bigint | undefined> => { return await this.appClient.state.box.getMapValue("deposits", key) as bigint | undefined },
      },
      /**
       * Get values from the lastBlocks map in box state
       */
      lastBlocks: {
        /**
         * Get all current values of the lastBlocks map in box state
         */
        getMap: async (): Promise<Map<string, bigint>> => { return (await this.appClient.state.box.getMap("lastBlocks")) as Map<string, bigint> },
        /**
         * Get a current value of the lastBlocks map by key from box state
         */
        value: async (key: string): Promise<bigint | undefined> => { return await this.appClient.state.box.getMapValue("lastBlocks", key) as bigint | undefined },
      },
    },
  }

  public newGroup(): YieldVaultComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a bootstrap(uint64)void method call against the YieldVault contract
       */
      bootstrap(params: CallParams<YieldVaultArgs['obj']['bootstrap(uint64)void'] | YieldVaultArgs['tuple']['bootstrap(uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.bootstrap(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a depositFirst(axfer,pay)void method call against the YieldVault contract
       */
      depositFirst(params: CallParams<YieldVaultArgs['obj']['depositFirst(axfer,pay)void'] | YieldVaultArgs['tuple']['depositFirst(axfer,pay)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.depositFirst(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a depositMore(axfer)void method call against the YieldVault contract
       */
      depositMore(params: CallParams<YieldVaultArgs['obj']['depositMore(axfer)void'] | YieldVaultArgs['tuple']['depositMore(axfer)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.depositMore(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a withdraw(uint64)void method call against the YieldVault contract
       */
      withdraw(params: CallParams<YieldVaultArgs['obj']['withdraw(uint64)void'] | YieldVaultArgs['tuple']['withdraw(uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.withdraw(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a fundReserves(axfer)void method call against the YieldVault contract
       */
      fundReserves(params: CallParams<YieldVaultArgs['obj']['fundReserves(axfer)void'] | YieldVaultArgs['tuple']['fundReserves(axfer)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.fundReserves(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a setPause(bool)void method call against the YieldVault contract
       */
      setPause(params: CallParams<YieldVaultArgs['obj']['setPause(bool)void'] | YieldVaultArgs['tuple']['setPause(bool)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.setPause(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a getBalance(address)uint64 method call against the YieldVault contract
       */
      getBalance(params: CallParams<YieldVaultArgs['obj']['getBalance(address)uint64'] | YieldVaultArgs['tuple']['getBalance(address)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.getBalance(params)))
        resultMappers.push((v) => client.decodeReturnValue('getBalance(address)uint64', v))
        return this
      },
      /**
       * Add a clear state call to the YieldVault contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as YieldVaultComposer
  }
}
export type YieldVaultComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the bootstrap(uint64)void ABI method.
   *
  * Bootstraps the vault: sets USDC asset and opts-in.
  Admin only.

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  bootstrap(params?: CallParams<YieldVaultArgs['obj']['bootstrap(uint64)void'] | YieldVaultArgs['tuple']['bootstrap(uint64)void']>): YieldVaultComposer<[...TReturns, YieldVaultReturns['bootstrap(uint64)void'] | undefined]>

  /**
   * Calls the depositFirst(axfer,pay)void ABI method.
   *
  * Deposits USDC into the vault for the first time.
  Requires a payment txn to cover MBR.

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  depositFirst(params?: CallParams<YieldVaultArgs['obj']['depositFirst(axfer,pay)void'] | YieldVaultArgs['tuple']['depositFirst(axfer,pay)void']>): YieldVaultComposer<[...TReturns, YieldVaultReturns['depositFirst(axfer,pay)void'] | undefined]>

  /**
   * Calls the depositMore(axfer)void ABI method.
   *
   * Deposits more USDC into the vault for existing users.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  depositMore(params?: CallParams<YieldVaultArgs['obj']['depositMore(axfer)void'] | YieldVaultArgs['tuple']['depositMore(axfer)void']>): YieldVaultComposer<[...TReturns, YieldVaultReturns['depositMore(axfer)void'] | undefined]>

  /**
   * Calls the withdraw(uint64)void ABI method.
   *
   * Withdraws USDC from the vault.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  withdraw(params?: CallParams<YieldVaultArgs['obj']['withdraw(uint64)void'] | YieldVaultArgs['tuple']['withdraw(uint64)void']>): YieldVaultComposer<[...TReturns, YieldVaultReturns['withdraw(uint64)void'] | undefined]>

  /**
   * Calls the fundReserves(axfer)void ABI method.
   *
   * Admin method to fund the yield reserves.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  fundReserves(params?: CallParams<YieldVaultArgs['obj']['fundReserves(axfer)void'] | YieldVaultArgs['tuple']['fundReserves(axfer)void']>): YieldVaultComposer<[...TReturns, YieldVaultReturns['fundReserves(axfer)void'] | undefined]>

  /**
   * Calls the setPause(bool)void ABI method.
   *
   * Pause or unpause the protocol.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  setPause(params?: CallParams<YieldVaultArgs['obj']['setPause(bool)void'] | YieldVaultArgs['tuple']['setPause(bool)void']>): YieldVaultComposer<[...TReturns, YieldVaultReturns['setPause(bool)void'] | undefined]>

  /**
   * Calls the getBalance(address)uint64 ABI method.
   *
   * External view of balance including accrued yield.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  getBalance(params?: CallParams<YieldVaultArgs['obj']['getBalance(address)uint64'] | YieldVaultArgs['tuple']['getBalance(address)uint64']>): YieldVaultComposer<[...TReturns, YieldVaultReturns['getBalance(address)uint64'] | undefined]>

  /**
   * Makes a clear_state call to an existing instance of the YieldVault smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): YieldVaultComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): YieldVaultComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<YieldVaultComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<YieldVaultComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<YieldVaultComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<YieldVaultComposerResults<TReturns>>
}
export type YieldVaultComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

