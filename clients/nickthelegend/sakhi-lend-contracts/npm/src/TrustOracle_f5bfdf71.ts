/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"TrustOracle","structs":{},"methods":[{"name":"createApplication","args":[],"returns":{"type":"void"},"actions":{"create":["NoOp"],"call":[]},"readonly":false,"events":[],"recommendations":{}},{"name":"attest","args":[{"type":"address","name":"user"},{"type":"uint64","name":"score"},{"type":"pay","name":"mbrPayment"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Attests to a user's creditworthiness with a TTF score.\nAdmin only. Requires MBR payment for boxes.","events":[],"recommendations":{}},{"name":"revokeAttestation","args":[{"type":"address","name":"user"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Revokes a user's verification status.\nAdmin only.","events":[],"recommendations":{}},{"name":"getScore","args":[{"type":"address","name":"user"}],"returns":{"type":"uint64"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Returns a user's score.","events":[],"recommendations":{}},{"name":"isVerified","args":[{"type":"address","name":"user"}],"returns":{"type":"bool"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Returns whether a user is verified.","events":[],"recommendations":{}},{"name":"deleteApplication","args":[],"returns":{"type":"void"},"actions":{"create":[],"call":["DeleteApplication"]},"readonly":false,"desc":"Allows the admin to delete the application.","events":[],"recommendations":{}}],"arcs":[22,28],"networks":{},"state":{"schema":{"global":{"ints":0,"bytes":1},"local":{"ints":0,"bytes":0}},"keys":{"global":{"creator":{"keyType":"AVMString","valueType":"address","key":"Y3JlYXRvcg==","desc":"The creator of the application (oracle admin)"}},"local":{},"box":{}},"maps":{"global":{},"local":{},"box":{"scores":{"keyType":"address","valueType":"uint64","desc":"Scores (0-1000) for verified users","prefix":"cw=="},"verified":{"keyType":"address","valueType":"uint64","desc":"Verification status","prefix":"dg=="},"attestedAt":{"keyType":"address","valueType":"uint64","desc":"Last attestation round","prefix":"YQ=="}}}},"bareActions":{"create":[],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[195,308,431],"errorMessage":"Admin only"},{"pc":[231],"errorMessage":"Insufficient MBR for boxes"},{"pc":[223],"errorMessage":"MBR to oracle"},{"pc":[138],"errorMessage":"OnCompletion must be DeleteApplication && can only call when not creating"},{"pc":[74],"errorMessage":"OnCompletion must be NoOp"},{"pc":[202],"errorMessage":"Score must be <= 1000"},{"pc":[182,295,418],"errorMessage":"check GlobalState exists"},{"pc":[156,289,360,387],"errorMessage":"invalid number of bytes for arc4.static_array<arc4.uint8, 32>"},{"pc":[165],"errorMessage":"invalid number of bytes for arc4.uint64"},{"pc":[176],"errorMessage":"transaction type is pay"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"events":[]} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


/**
 * The argument types for the TrustOracle contract
 */
export type TrustOracleArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'createApplication()void': Record<string, never>
    'attest(address,uint64,pay)void': {
      user: string
      score: bigint | number
      mbrPayment: AppMethodCallTransactionArgument
    }
    'revokeAttestation(address)void': {
      user: string
    }
    'getScore(address)uint64': {
      user: string
    }
    'isVerified(address)bool': {
      user: string
    }
    'deleteApplication()void': Record<string, never>
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'createApplication()void': []
    'attest(address,uint64,pay)void': [user: string, score: bigint | number, mbrPayment: AppMethodCallTransactionArgument]
    'revokeAttestation(address)void': [user: string]
    'getScore(address)uint64': [user: string]
    'isVerified(address)bool': [user: string]
    'deleteApplication()void': []
  }
}

/**
 * The return type for each method
 */
export type TrustOracleReturns = {
  'createApplication()void': void
  'attest(address,uint64,pay)void': void
  'revokeAttestation(address)void': void
  'getScore(address)uint64': bigint
  'isVerified(address)bool': boolean
  'deleteApplication()void': void
}

/**
 * Defines the types of available calls and state of the TrustOracle smart contract.
 */
export type TrustOracleTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'createApplication()void' | 'createApplication', {
      argsObj: TrustOracleArgs['obj']['createApplication()void']
      argsTuple: TrustOracleArgs['tuple']['createApplication()void']
      returns: TrustOracleReturns['createApplication()void']
    }>
    & Record<'attest(address,uint64,pay)void' | 'attest', {
      argsObj: TrustOracleArgs['obj']['attest(address,uint64,pay)void']
      argsTuple: TrustOracleArgs['tuple']['attest(address,uint64,pay)void']
      returns: TrustOracleReturns['attest(address,uint64,pay)void']
    }>
    & Record<'revokeAttestation(address)void' | 'revokeAttestation', {
      argsObj: TrustOracleArgs['obj']['revokeAttestation(address)void']
      argsTuple: TrustOracleArgs['tuple']['revokeAttestation(address)void']
      returns: TrustOracleReturns['revokeAttestation(address)void']
    }>
    & Record<'getScore(address)uint64' | 'getScore', {
      argsObj: TrustOracleArgs['obj']['getScore(address)uint64']
      argsTuple: TrustOracleArgs['tuple']['getScore(address)uint64']
      returns: TrustOracleReturns['getScore(address)uint64']
    }>
    & Record<'isVerified(address)bool' | 'isVerified', {
      argsObj: TrustOracleArgs['obj']['isVerified(address)bool']
      argsTuple: TrustOracleArgs['tuple']['isVerified(address)bool']
      returns: TrustOracleReturns['isVerified(address)bool']
    }>
    & Record<'deleteApplication()void' | 'deleteApplication', {
      argsObj: TrustOracleArgs['obj']['deleteApplication()void']
      argsTuple: TrustOracleArgs['tuple']['deleteApplication()void']
      returns: TrustOracleReturns['deleteApplication()void']
    }>
  /**
   * Defines the shape of the state of the application.
   */
  state: {
    global: {
      keys: {
        /**
         * The creator of the application (oracle admin)
         */
        creator: string
      }
      maps: {}
    }
    box: {
      keys: {}
      maps: {
        /**
         * Scores (0-1000) for verified users
         */
        scores: Map<string, bigint>
        /**
         * Verification status
         */
        verified: Map<string, bigint>
        /**
         * Last attestation round
         */
        attestedAt: Map<string, bigint>
      }
    }
  }
}

/**
 * Defines the possible abi call signatures.
 */
export type TrustOracleSignatures = keyof TrustOracleTypes['methods']
/**
 * Defines the possible abi call signatures for methods that return a non-void value.
 */
export type TrustOracleNonVoidMethodSignatures = keyof TrustOracleTypes['methods'] extends infer T ? T extends keyof TrustOracleTypes['methods'] ? MethodReturn<T> extends void ? never : T  : never : never
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the TrustOracle smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends TrustOracleSignatures> = TrustOracleTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the TrustOracle smart contract to the method's return type
 */
export type MethodReturn<TSignature extends TrustOracleSignatures> = TrustOracleTypes['methods'][TSignature]['returns']

/**
 * Defines the shape of the keyed global state of the application.
 */
export type GlobalKeysState = TrustOracleTypes['state']['global']['keys']

/**
 * Defines the shape of the keyed box state of the application.
 */
export type BoxKeysState = TrustOracleTypes['state']['box']['keys']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the TrustOracle smart contract
 */
export abstract class TrustOracleParamsFactory {
  /**
   * Constructs a no op call for the attest(address,uint64,pay)void ABI method
   *
  * Attests to a user's creditworthiness with a TTF score.
  Admin only. Requires MBR payment for boxes.

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static attest(params: CallParams<TrustOracleArgs['obj']['attest(address,uint64,pay)void'] | TrustOracleArgs['tuple']['attest(address,uint64,pay)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'attest(address,uint64,pay)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.user, params.args.score, params.args.mbrPayment],
    }
  }
  /**
   * Constructs a no op call for the revokeAttestation(address)void ABI method
   *
  * Revokes a user's verification status.
  Admin only.

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static revokeAttestation(params: CallParams<TrustOracleArgs['obj']['revokeAttestation(address)void'] | TrustOracleArgs['tuple']['revokeAttestation(address)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'revokeAttestation(address)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.user],
    }
  }
  /**
   * Constructs a no op call for the getScore(address)uint64 ABI method
   *
   * Returns a user's score.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static getScore(params: CallParams<TrustOracleArgs['obj']['getScore(address)uint64'] | TrustOracleArgs['tuple']['getScore(address)uint64']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'getScore(address)uint64' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.user],
    }
  }
  /**
   * Constructs a no op call for the isVerified(address)bool ABI method
   *
   * Returns whether a user is verified.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static isVerified(params: CallParams<TrustOracleArgs['obj']['isVerified(address)bool'] | TrustOracleArgs['tuple']['isVerified(address)bool']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'isVerified(address)bool' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.user],
    }
  }
}

/**
 * A client to make calls to the TrustOracle smart contract
 */
export class TrustOracleClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `TrustOracleClient`
   *
   * @param appClient An `AppClient` instance which has been created with the TrustOracle app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `TrustOracleClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Checks for decode errors on the given return value and maps the return value to the return type for the given method
   * @returns The typed return value or undefined if there was no value
   */
  decodeReturnValue<TSignature extends TrustOracleNonVoidMethodSignatures>(method: TSignature, returnValue: ABIReturn | undefined) {
    return returnValue !== undefined ? getArc56ReturnValue<MethodReturn<TSignature>>(returnValue, this.appClient.getABIMethod(method), APP_SPEC.structs) : undefined
  }

  /**
   * Returns a new `TrustOracleClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<TrustOracleClient> {
    return new TrustOracleClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `TrustOracleClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<TrustOracleClient> {
    return new TrustOracleClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Makes a clear_state call to an existing instance of the TrustOracle smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the TrustOracle smart contract using the `attest(address,uint64,pay)void` ABI method.
     *
    * Attests to a user's creditworthiness with a TTF score.
    Admin only. Requires MBR payment for boxes.

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    attest: (params: CallParams<TrustOracleArgs['obj']['attest(address,uint64,pay)void'] | TrustOracleArgs['tuple']['attest(address,uint64,pay)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(TrustOracleParamsFactory.attest(params))
    },

    /**
     * Makes a call to the TrustOracle smart contract using the `revokeAttestation(address)void` ABI method.
     *
    * Revokes a user's verification status.
    Admin only.

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    revokeAttestation: (params: CallParams<TrustOracleArgs['obj']['revokeAttestation(address)void'] | TrustOracleArgs['tuple']['revokeAttestation(address)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(TrustOracleParamsFactory.revokeAttestation(params))
    },

    /**
     * Makes a call to the TrustOracle smart contract using the `getScore(address)uint64` ABI method.
     *
     * Returns a user's score.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    getScore: (params: CallParams<TrustOracleArgs['obj']['getScore(address)uint64'] | TrustOracleArgs['tuple']['getScore(address)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(TrustOracleParamsFactory.getScore(params))
    },

    /**
     * Makes a call to the TrustOracle smart contract using the `isVerified(address)bool` ABI method.
     *
     * Returns whether a user is verified.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    isVerified: (params: CallParams<TrustOracleArgs['obj']['isVerified(address)bool'] | TrustOracleArgs['tuple']['isVerified(address)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(TrustOracleParamsFactory.isVerified(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Makes a clear_state call to an existing instance of the TrustOracle smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the TrustOracle smart contract using the `attest(address,uint64,pay)void` ABI method.
     *
    * Attests to a user's creditworthiness with a TTF score.
    Admin only. Requires MBR payment for boxes.

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    attest: (params: CallParams<TrustOracleArgs['obj']['attest(address,uint64,pay)void'] | TrustOracleArgs['tuple']['attest(address,uint64,pay)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(TrustOracleParamsFactory.attest(params))
    },

    /**
     * Makes a call to the TrustOracle smart contract using the `revokeAttestation(address)void` ABI method.
     *
    * Revokes a user's verification status.
    Admin only.

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    revokeAttestation: (params: CallParams<TrustOracleArgs['obj']['revokeAttestation(address)void'] | TrustOracleArgs['tuple']['revokeAttestation(address)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(TrustOracleParamsFactory.revokeAttestation(params))
    },

    /**
     * Makes a call to the TrustOracle smart contract using the `getScore(address)uint64` ABI method.
     *
     * Returns a user's score.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    getScore: (params: CallParams<TrustOracleArgs['obj']['getScore(address)uint64'] | TrustOracleArgs['tuple']['getScore(address)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(TrustOracleParamsFactory.getScore(params))
    },

    /**
     * Makes a call to the TrustOracle smart contract using the `isVerified(address)bool` ABI method.
     *
     * Returns whether a user is verified.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    isVerified: (params: CallParams<TrustOracleArgs['obj']['isVerified(address)bool'] | TrustOracleArgs['tuple']['isVerified(address)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(TrustOracleParamsFactory.isVerified(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Makes a clear_state call to an existing instance of the TrustOracle smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the TrustOracle smart contract using the `attest(address,uint64,pay)void` ABI method.
     *
    * Attests to a user's creditworthiness with a TTF score.
    Admin only. Requires MBR payment for boxes.

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    attest: async (params: CallParams<TrustOracleArgs['obj']['attest(address,uint64,pay)void'] | TrustOracleArgs['tuple']['attest(address,uint64,pay)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(TrustOracleParamsFactory.attest(params))
      return {...result, return: result.return as unknown as (undefined | TrustOracleReturns['attest(address,uint64,pay)void'])}
    },

    /**
     * Makes a call to the TrustOracle smart contract using the `revokeAttestation(address)void` ABI method.
     *
    * Revokes a user's verification status.
    Admin only.

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    revokeAttestation: async (params: CallParams<TrustOracleArgs['obj']['revokeAttestation(address)void'] | TrustOracleArgs['tuple']['revokeAttestation(address)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(TrustOracleParamsFactory.revokeAttestation(params))
      return {...result, return: result.return as unknown as (undefined | TrustOracleReturns['revokeAttestation(address)void'])}
    },

    /**
     * Makes a call to the TrustOracle smart contract using the `getScore(address)uint64` ABI method.
     *
     * Returns a user's score.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    getScore: async (params: CallParams<TrustOracleArgs['obj']['getScore(address)uint64'] | TrustOracleArgs['tuple']['getScore(address)uint64']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(TrustOracleParamsFactory.getScore(params))
      return {...result, return: result.return as unknown as (undefined | TrustOracleReturns['getScore(address)uint64'])}
    },

    /**
     * Makes a call to the TrustOracle smart contract using the `isVerified(address)bool` ABI method.
     *
     * Returns whether a user is verified.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    isVerified: async (params: CallParams<TrustOracleArgs['obj']['isVerified(address)bool'] | TrustOracleArgs['tuple']['isVerified(address)bool']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(TrustOracleParamsFactory.isVerified(params))
      return {...result, return: result.return as unknown as (undefined | TrustOracleReturns['isVerified(address)bool'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new TrustOracleClient(this.appClient.clone(params))
  }

  /**
   * Methods to access state for the current TrustOracle app
   */
  state = {
    /**
     * Methods to access global state for the current TrustOracle app
     */
    global: {
      /**
       * Get all current keyed values from global state
       */
      getAll: async (): Promise<Partial<Expand<GlobalKeysState>>> => {
        const result = await this.appClient.state.global.getAll()
        return {
          creator: result.creator,
        }
      },
      /**
       * Get the current value of the creator key in global state
       */
      creator: async (): Promise<string | undefined> => { return (await this.appClient.state.global.getValue("creator")) as string | undefined },
    },
    /**
     * Methods to access box state for the current TrustOracle app
     */
    box: {
      /**
       * Get all current keyed values from box state
       */
      getAll: async (): Promise<Partial<Expand<BoxKeysState>>> => {
        const result = await this.appClient.state.box.getAll()
        return {
        }
      },
      /**
       * Get values from the scores map in box state
       */
      scores: {
        /**
         * Get all current values of the scores map in box state
         */
        getMap: async (): Promise<Map<string, bigint>> => { return (await this.appClient.state.box.getMap("scores")) as Map<string, bigint> },
        /**
         * Get a current value of the scores map by key from box state
         */
        value: async (key: string): Promise<bigint | undefined> => { return await this.appClient.state.box.getMapValue("scores", key) as bigint | undefined },
      },
      /**
       * Get values from the verified map in box state
       */
      verified: {
        /**
         * Get all current values of the verified map in box state
         */
        getMap: async (): Promise<Map<string, bigint>> => { return (await this.appClient.state.box.getMap("verified")) as Map<string, bigint> },
        /**
         * Get a current value of the verified map by key from box state
         */
        value: async (key: string): Promise<bigint | undefined> => { return await this.appClient.state.box.getMapValue("verified", key) as bigint | undefined },
      },
      /**
       * Get values from the attestedAt map in box state
       */
      attestedAt: {
        /**
         * Get all current values of the attestedAt map in box state
         */
        getMap: async (): Promise<Map<string, bigint>> => { return (await this.appClient.state.box.getMap("attestedAt")) as Map<string, bigint> },
        /**
         * Get a current value of the attestedAt map by key from box state
         */
        value: async (key: string): Promise<bigint | undefined> => { return await this.appClient.state.box.getMapValue("attestedAt", key) as bigint | undefined },
      },
    },
  }

  public newGroup(): TrustOracleComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a attest(address,uint64,pay)void method call against the TrustOracle contract
       */
      attest(params: CallParams<TrustOracleArgs['obj']['attest(address,uint64,pay)void'] | TrustOracleArgs['tuple']['attest(address,uint64,pay)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.attest(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a revokeAttestation(address)void method call against the TrustOracle contract
       */
      revokeAttestation(params: CallParams<TrustOracleArgs['obj']['revokeAttestation(address)void'] | TrustOracleArgs['tuple']['revokeAttestation(address)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.revokeAttestation(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a getScore(address)uint64 method call against the TrustOracle contract
       */
      getScore(params: CallParams<TrustOracleArgs['obj']['getScore(address)uint64'] | TrustOracleArgs['tuple']['getScore(address)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.getScore(params)))
        resultMappers.push((v) => client.decodeReturnValue('getScore(address)uint64', v))
        return this
      },
      /**
       * Add a isVerified(address)bool method call against the TrustOracle contract
       */
      isVerified(params: CallParams<TrustOracleArgs['obj']['isVerified(address)bool'] | TrustOracleArgs['tuple']['isVerified(address)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.isVerified(params)))
        resultMappers.push((v) => client.decodeReturnValue('isVerified(address)bool', v))
        return this
      },
      /**
       * Add a clear state call to the TrustOracle contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as TrustOracleComposer
  }
}
export type TrustOracleComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the attest(address,uint64,pay)void ABI method.
   *
  * Attests to a user's creditworthiness with a TTF score.
  Admin only. Requires MBR payment for boxes.

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  attest(params?: CallParams<TrustOracleArgs['obj']['attest(address,uint64,pay)void'] | TrustOracleArgs['tuple']['attest(address,uint64,pay)void']>): TrustOracleComposer<[...TReturns, TrustOracleReturns['attest(address,uint64,pay)void'] | undefined]>

  /**
   * Calls the revokeAttestation(address)void ABI method.
   *
  * Revokes a user's verification status.
  Admin only.

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  revokeAttestation(params?: CallParams<TrustOracleArgs['obj']['revokeAttestation(address)void'] | TrustOracleArgs['tuple']['revokeAttestation(address)void']>): TrustOracleComposer<[...TReturns, TrustOracleReturns['revokeAttestation(address)void'] | undefined]>

  /**
   * Calls the getScore(address)uint64 ABI method.
   *
   * Returns a user's score.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  getScore(params?: CallParams<TrustOracleArgs['obj']['getScore(address)uint64'] | TrustOracleArgs['tuple']['getScore(address)uint64']>): TrustOracleComposer<[...TReturns, TrustOracleReturns['getScore(address)uint64'] | undefined]>

  /**
   * Calls the isVerified(address)bool ABI method.
   *
   * Returns whether a user is verified.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  isVerified(params?: CallParams<TrustOracleArgs['obj']['isVerified(address)bool'] | TrustOracleArgs['tuple']['isVerified(address)bool']>): TrustOracleComposer<[...TReturns, TrustOracleReturns['isVerified(address)bool'] | undefined]>

  /**
   * Makes a clear_state call to an existing instance of the TrustOracle smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): TrustOracleComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): TrustOracleComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<TrustOracleComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<TrustOracleComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<TrustOracleComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<TrustOracleComposerResults<TReturns>>
}
export type TrustOracleComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

