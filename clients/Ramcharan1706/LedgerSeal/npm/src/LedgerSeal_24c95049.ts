/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"LedgerSeal","structs":{},"methods":[{"name":"register_evidence","args":[{"type":"byte[]","name":"file_hash"},{"type":"byte[]","name":"metadata_hash"},{"type":"address","name":"owner"}],"returns":{"type":"bool"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Register new evidence with owner and metadata. Rejects duplicates.","events":[],"recommendations":{}},{"name":"get_owner","args":[{"type":"byte[]","name":"file_hash"}],"returns":{"type":"address"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Get current owner of evidence","events":[],"recommendations":{}},{"name":"evidence_exists","args":[{"type":"byte[]","name":"file_hash"}],"returns":{"type":"bool"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Check if evidence is registered","events":[],"recommendations":{}},{"name":"update_metadata","args":[{"type":"byte[]","name":"file_hash"},{"type":"byte[]","name":"metadata_hash"}],"returns":{"type":"bool"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Update metadata hash. Only current owner can update.","events":[],"recommendations":{}},{"name":"transfer_ownership","args":[{"type":"byte[]","name":"file_hash"},{"type":"address","name":"new_owner"}],"returns":{"type":"bool"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Transfer ownership to another wallet. Only current owner can transfer.","events":[],"recommendations":{}},{"name":"metadata_exists","args":[{"type":"byte[]","name":"metadata_hash"}],"returns":{"type":"bool"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Check if metadata exists","events":[],"recommendations":{}},{"name":"get_timestamp","args":[{"type":"byte[]","name":"file_hash"}],"returns":{"type":"uint64"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Get evidence exists check","events":[],"recommendations":{}},{"name":"get_last_transfer","args":[{"type":"byte[]","name":"file_hash"}],"returns":{"type":"uint64"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Get last transfer check","events":[],"recommendations":{}},{"name":"delete_evidence","args":[{"type":"byte[]","name":"file_hash"}],"returns":{"type":"bool"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Delete evidence. Only app creator allowed.","events":[],"recommendations":{}},{"name":"verify_evidence","args":[{"type":"byte[]","name":"file_hash"}],"returns":{"type":"bool"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Verify if evidence exists and is valid","events":[],"recommendations":{}},{"name":"add_log","args":[{"type":"byte[]","name":"file_hash"},{"type":"string","name":"note"}],"returns":{"type":"bool"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Store log entry for chain-of-custody tracking","events":[],"recommendations":{}}],"arcs":[22,28],"desc":"LedgerSeal - Blockchain-based evidence management contract for Algorand","networks":{},"state":{"schema":{"global":{"ints":0,"bytes":0},"local":{"ints":0,"bytes":0}},"keys":{"global":{},"local":{},"box":{}},"maps":{"global":{},"local":{},"box":{}}},"bareActions":{"create":["NoOp"],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[215,309,378],"errorMessage":"check Box exists"},{"pc":[126,143,203,227,257,274,333,398,428,454,480,534,564,581],"errorMessage":"invalid array length header"},{"pc":[133,150,210,234,264,281,340,405,435,461,487,541,571,588],"errorMessage":"invalid number of bytes for arc4.dynamic_array<arc4.uint8>"},{"pc":[164,354],"errorMessage":"invalid number of bytes for arc4.static_array<arc4.uint8, 32>"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"events":[]} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


/**
 * The argument types for the LedgerSeal contract
 */
export type LedgerSealArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'register_evidence(byte[],byte[],address)bool': {
      fileHash: Uint8Array
      metadataHash: Uint8Array
      owner: string
    }
    'get_owner(byte[])address': {
      fileHash: Uint8Array
    }
    'evidence_exists(byte[])bool': {
      fileHash: Uint8Array
    }
    'update_metadata(byte[],byte[])bool': {
      fileHash: Uint8Array
      metadataHash: Uint8Array
    }
    'transfer_ownership(byte[],address)bool': {
      fileHash: Uint8Array
      newOwner: string
    }
    'metadata_exists(byte[])bool': {
      metadataHash: Uint8Array
    }
    'get_timestamp(byte[])uint64': {
      fileHash: Uint8Array
    }
    'get_last_transfer(byte[])uint64': {
      fileHash: Uint8Array
    }
    'delete_evidence(byte[])bool': {
      fileHash: Uint8Array
    }
    'verify_evidence(byte[])bool': {
      fileHash: Uint8Array
    }
    'add_log(byte[],string)bool': {
      fileHash: Uint8Array
      note: string
    }
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'register_evidence(byte[],byte[],address)bool': [fileHash: Uint8Array, metadataHash: Uint8Array, owner: string]
    'get_owner(byte[])address': [fileHash: Uint8Array]
    'evidence_exists(byte[])bool': [fileHash: Uint8Array]
    'update_metadata(byte[],byte[])bool': [fileHash: Uint8Array, metadataHash: Uint8Array]
    'transfer_ownership(byte[],address)bool': [fileHash: Uint8Array, newOwner: string]
    'metadata_exists(byte[])bool': [metadataHash: Uint8Array]
    'get_timestamp(byte[])uint64': [fileHash: Uint8Array]
    'get_last_transfer(byte[])uint64': [fileHash: Uint8Array]
    'delete_evidence(byte[])bool': [fileHash: Uint8Array]
    'verify_evidence(byte[])bool': [fileHash: Uint8Array]
    'add_log(byte[],string)bool': [fileHash: Uint8Array, note: string]
  }
}

/**
 * The return type for each method
 */
export type LedgerSealReturns = {
  'register_evidence(byte[],byte[],address)bool': boolean
  'get_owner(byte[])address': string
  'evidence_exists(byte[])bool': boolean
  'update_metadata(byte[],byte[])bool': boolean
  'transfer_ownership(byte[],address)bool': boolean
  'metadata_exists(byte[])bool': boolean
  'get_timestamp(byte[])uint64': bigint
  'get_last_transfer(byte[])uint64': bigint
  'delete_evidence(byte[])bool': boolean
  'verify_evidence(byte[])bool': boolean
  'add_log(byte[],string)bool': boolean
}

/**
 * Defines the types of available calls and state of the LedgerSeal smart contract.
 */
export type LedgerSealTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'register_evidence(byte[],byte[],address)bool' | 'register_evidence', {
      argsObj: LedgerSealArgs['obj']['register_evidence(byte[],byte[],address)bool']
      argsTuple: LedgerSealArgs['tuple']['register_evidence(byte[],byte[],address)bool']
      returns: LedgerSealReturns['register_evidence(byte[],byte[],address)bool']
    }>
    & Record<'get_owner(byte[])address' | 'get_owner', {
      argsObj: LedgerSealArgs['obj']['get_owner(byte[])address']
      argsTuple: LedgerSealArgs['tuple']['get_owner(byte[])address']
      returns: LedgerSealReturns['get_owner(byte[])address']
    }>
    & Record<'evidence_exists(byte[])bool' | 'evidence_exists', {
      argsObj: LedgerSealArgs['obj']['evidence_exists(byte[])bool']
      argsTuple: LedgerSealArgs['tuple']['evidence_exists(byte[])bool']
      returns: LedgerSealReturns['evidence_exists(byte[])bool']
    }>
    & Record<'update_metadata(byte[],byte[])bool' | 'update_metadata', {
      argsObj: LedgerSealArgs['obj']['update_metadata(byte[],byte[])bool']
      argsTuple: LedgerSealArgs['tuple']['update_metadata(byte[],byte[])bool']
      returns: LedgerSealReturns['update_metadata(byte[],byte[])bool']
    }>
    & Record<'transfer_ownership(byte[],address)bool' | 'transfer_ownership', {
      argsObj: LedgerSealArgs['obj']['transfer_ownership(byte[],address)bool']
      argsTuple: LedgerSealArgs['tuple']['transfer_ownership(byte[],address)bool']
      returns: LedgerSealReturns['transfer_ownership(byte[],address)bool']
    }>
    & Record<'metadata_exists(byte[])bool' | 'metadata_exists', {
      argsObj: LedgerSealArgs['obj']['metadata_exists(byte[])bool']
      argsTuple: LedgerSealArgs['tuple']['metadata_exists(byte[])bool']
      returns: LedgerSealReturns['metadata_exists(byte[])bool']
    }>
    & Record<'get_timestamp(byte[])uint64' | 'get_timestamp', {
      argsObj: LedgerSealArgs['obj']['get_timestamp(byte[])uint64']
      argsTuple: LedgerSealArgs['tuple']['get_timestamp(byte[])uint64']
      returns: LedgerSealReturns['get_timestamp(byte[])uint64']
    }>
    & Record<'get_last_transfer(byte[])uint64' | 'get_last_transfer', {
      argsObj: LedgerSealArgs['obj']['get_last_transfer(byte[])uint64']
      argsTuple: LedgerSealArgs['tuple']['get_last_transfer(byte[])uint64']
      returns: LedgerSealReturns['get_last_transfer(byte[])uint64']
    }>
    & Record<'delete_evidence(byte[])bool' | 'delete_evidence', {
      argsObj: LedgerSealArgs['obj']['delete_evidence(byte[])bool']
      argsTuple: LedgerSealArgs['tuple']['delete_evidence(byte[])bool']
      returns: LedgerSealReturns['delete_evidence(byte[])bool']
    }>
    & Record<'verify_evidence(byte[])bool' | 'verify_evidence', {
      argsObj: LedgerSealArgs['obj']['verify_evidence(byte[])bool']
      argsTuple: LedgerSealArgs['tuple']['verify_evidence(byte[])bool']
      returns: LedgerSealReturns['verify_evidence(byte[])bool']
    }>
    & Record<'add_log(byte[],string)bool' | 'add_log', {
      argsObj: LedgerSealArgs['obj']['add_log(byte[],string)bool']
      argsTuple: LedgerSealArgs['tuple']['add_log(byte[],string)bool']
      returns: LedgerSealReturns['add_log(byte[],string)bool']
    }>
}

/**
 * Defines the possible abi call signatures.
 */
export type LedgerSealSignatures = keyof LedgerSealTypes['methods']
/**
 * Defines the possible abi call signatures for methods that return a non-void value.
 */
export type LedgerSealNonVoidMethodSignatures = keyof LedgerSealTypes['methods'] extends infer T ? T extends keyof LedgerSealTypes['methods'] ? MethodReturn<T> extends void ? never : T  : never : never
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the LedgerSeal smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends LedgerSealSignatures> = LedgerSealTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the LedgerSeal smart contract to the method's return type
 */
export type MethodReturn<TSignature extends LedgerSealSignatures> = LedgerSealTypes['methods'][TSignature]['returns']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the LedgerSeal smart contract
 */
export abstract class LedgerSealParamsFactory {
  /**
   * Constructs a no op call for the register_evidence(byte[],byte[],address)bool ABI method
   *
   * Register new evidence with owner and metadata. Rejects duplicates.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static registerEvidence(params: CallParams<LedgerSealArgs['obj']['register_evidence(byte[],byte[],address)bool'] | LedgerSealArgs['tuple']['register_evidence(byte[],byte[],address)bool']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'register_evidence(byte[],byte[],address)bool' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.fileHash, params.args.metadataHash, params.args.owner],
    }
  }
  /**
   * Constructs a no op call for the get_owner(byte[])address ABI method
   *
   * Get current owner of evidence
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static getOwner(params: CallParams<LedgerSealArgs['obj']['get_owner(byte[])address'] | LedgerSealArgs['tuple']['get_owner(byte[])address']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'get_owner(byte[])address' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.fileHash],
    }
  }
  /**
   * Constructs a no op call for the evidence_exists(byte[])bool ABI method
   *
   * Check if evidence is registered
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static evidenceExists(params: CallParams<LedgerSealArgs['obj']['evidence_exists(byte[])bool'] | LedgerSealArgs['tuple']['evidence_exists(byte[])bool']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'evidence_exists(byte[])bool' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.fileHash],
    }
  }
  /**
   * Constructs a no op call for the update_metadata(byte[],byte[])bool ABI method
   *
   * Update metadata hash. Only current owner can update.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static updateMetadata(params: CallParams<LedgerSealArgs['obj']['update_metadata(byte[],byte[])bool'] | LedgerSealArgs['tuple']['update_metadata(byte[],byte[])bool']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'update_metadata(byte[],byte[])bool' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.fileHash, params.args.metadataHash],
    }
  }
  /**
   * Constructs a no op call for the transfer_ownership(byte[],address)bool ABI method
   *
   * Transfer ownership to another wallet. Only current owner can transfer.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static transferOwnership(params: CallParams<LedgerSealArgs['obj']['transfer_ownership(byte[],address)bool'] | LedgerSealArgs['tuple']['transfer_ownership(byte[],address)bool']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'transfer_ownership(byte[],address)bool' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.fileHash, params.args.newOwner],
    }
  }
  /**
   * Constructs a no op call for the metadata_exists(byte[])bool ABI method
   *
   * Check if metadata exists
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static metadataExists(params: CallParams<LedgerSealArgs['obj']['metadata_exists(byte[])bool'] | LedgerSealArgs['tuple']['metadata_exists(byte[])bool']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'metadata_exists(byte[])bool' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.metadataHash],
    }
  }
  /**
   * Constructs a no op call for the get_timestamp(byte[])uint64 ABI method
   *
   * Get evidence exists check
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static getTimestamp(params: CallParams<LedgerSealArgs['obj']['get_timestamp(byte[])uint64'] | LedgerSealArgs['tuple']['get_timestamp(byte[])uint64']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'get_timestamp(byte[])uint64' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.fileHash],
    }
  }
  /**
   * Constructs a no op call for the get_last_transfer(byte[])uint64 ABI method
   *
   * Get last transfer check
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static getLastTransfer(params: CallParams<LedgerSealArgs['obj']['get_last_transfer(byte[])uint64'] | LedgerSealArgs['tuple']['get_last_transfer(byte[])uint64']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'get_last_transfer(byte[])uint64' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.fileHash],
    }
  }
  /**
   * Constructs a no op call for the delete_evidence(byte[])bool ABI method
   *
   * Delete evidence. Only app creator allowed.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static deleteEvidence(params: CallParams<LedgerSealArgs['obj']['delete_evidence(byte[])bool'] | LedgerSealArgs['tuple']['delete_evidence(byte[])bool']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'delete_evidence(byte[])bool' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.fileHash],
    }
  }
  /**
   * Constructs a no op call for the verify_evidence(byte[])bool ABI method
   *
   * Verify if evidence exists and is valid
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static verifyEvidence(params: CallParams<LedgerSealArgs['obj']['verify_evidence(byte[])bool'] | LedgerSealArgs['tuple']['verify_evidence(byte[])bool']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'verify_evidence(byte[])bool' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.fileHash],
    }
  }
  /**
   * Constructs a no op call for the add_log(byte[],string)bool ABI method
   *
   * Store log entry for chain-of-custody tracking
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static addLog(params: CallParams<LedgerSealArgs['obj']['add_log(byte[],string)bool'] | LedgerSealArgs['tuple']['add_log(byte[],string)bool']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'add_log(byte[],string)bool' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.fileHash, params.args.note],
    }
  }
}

/**
 * A client to make calls to the LedgerSeal smart contract
 */
export class LedgerSealClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `LedgerSealClient`
   *
   * @param appClient An `AppClient` instance which has been created with the LedgerSeal app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `LedgerSealClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Checks for decode errors on the given return value and maps the return value to the return type for the given method
   * @returns The typed return value or undefined if there was no value
   */
  decodeReturnValue<TSignature extends LedgerSealNonVoidMethodSignatures>(method: TSignature, returnValue: ABIReturn | undefined) {
    return returnValue !== undefined ? getArc56ReturnValue<MethodReturn<TSignature>>(returnValue, this.appClient.getABIMethod(method), APP_SPEC.structs) : undefined
  }

  /**
   * Returns a new `LedgerSealClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<LedgerSealClient> {
    return new LedgerSealClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `LedgerSealClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<LedgerSealClient> {
    return new LedgerSealClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Makes a clear_state call to an existing instance of the LedgerSeal smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the LedgerSeal smart contract using the `register_evidence(byte[],byte[],address)bool` ABI method.
     *
     * Register new evidence with owner and metadata. Rejects duplicates.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    registerEvidence: (params: CallParams<LedgerSealArgs['obj']['register_evidence(byte[],byte[],address)bool'] | LedgerSealArgs['tuple']['register_evidence(byte[],byte[],address)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(LedgerSealParamsFactory.registerEvidence(params))
    },

    /**
     * Makes a call to the LedgerSeal smart contract using the `get_owner(byte[])address` ABI method.
     *
     * Get current owner of evidence
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    getOwner: (params: CallParams<LedgerSealArgs['obj']['get_owner(byte[])address'] | LedgerSealArgs['tuple']['get_owner(byte[])address']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(LedgerSealParamsFactory.getOwner(params))
    },

    /**
     * Makes a call to the LedgerSeal smart contract using the `evidence_exists(byte[])bool` ABI method.
     *
     * Check if evidence is registered
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    evidenceExists: (params: CallParams<LedgerSealArgs['obj']['evidence_exists(byte[])bool'] | LedgerSealArgs['tuple']['evidence_exists(byte[])bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(LedgerSealParamsFactory.evidenceExists(params))
    },

    /**
     * Makes a call to the LedgerSeal smart contract using the `update_metadata(byte[],byte[])bool` ABI method.
     *
     * Update metadata hash. Only current owner can update.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    updateMetadata: (params: CallParams<LedgerSealArgs['obj']['update_metadata(byte[],byte[])bool'] | LedgerSealArgs['tuple']['update_metadata(byte[],byte[])bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(LedgerSealParamsFactory.updateMetadata(params))
    },

    /**
     * Makes a call to the LedgerSeal smart contract using the `transfer_ownership(byte[],address)bool` ABI method.
     *
     * Transfer ownership to another wallet. Only current owner can transfer.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    transferOwnership: (params: CallParams<LedgerSealArgs['obj']['transfer_ownership(byte[],address)bool'] | LedgerSealArgs['tuple']['transfer_ownership(byte[],address)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(LedgerSealParamsFactory.transferOwnership(params))
    },

    /**
     * Makes a call to the LedgerSeal smart contract using the `metadata_exists(byte[])bool` ABI method.
     *
     * Check if metadata exists
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    metadataExists: (params: CallParams<LedgerSealArgs['obj']['metadata_exists(byte[])bool'] | LedgerSealArgs['tuple']['metadata_exists(byte[])bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(LedgerSealParamsFactory.metadataExists(params))
    },

    /**
     * Makes a call to the LedgerSeal smart contract using the `get_timestamp(byte[])uint64` ABI method.
     *
     * Get evidence exists check
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    getTimestamp: (params: CallParams<LedgerSealArgs['obj']['get_timestamp(byte[])uint64'] | LedgerSealArgs['tuple']['get_timestamp(byte[])uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(LedgerSealParamsFactory.getTimestamp(params))
    },

    /**
     * Makes a call to the LedgerSeal smart contract using the `get_last_transfer(byte[])uint64` ABI method.
     *
     * Get last transfer check
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    getLastTransfer: (params: CallParams<LedgerSealArgs['obj']['get_last_transfer(byte[])uint64'] | LedgerSealArgs['tuple']['get_last_transfer(byte[])uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(LedgerSealParamsFactory.getLastTransfer(params))
    },

    /**
     * Makes a call to the LedgerSeal smart contract using the `delete_evidence(byte[])bool` ABI method.
     *
     * Delete evidence. Only app creator allowed.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    deleteEvidence: (params: CallParams<LedgerSealArgs['obj']['delete_evidence(byte[])bool'] | LedgerSealArgs['tuple']['delete_evidence(byte[])bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(LedgerSealParamsFactory.deleteEvidence(params))
    },

    /**
     * Makes a call to the LedgerSeal smart contract using the `verify_evidence(byte[])bool` ABI method.
     *
     * Verify if evidence exists and is valid
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    verifyEvidence: (params: CallParams<LedgerSealArgs['obj']['verify_evidence(byte[])bool'] | LedgerSealArgs['tuple']['verify_evidence(byte[])bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(LedgerSealParamsFactory.verifyEvidence(params))
    },

    /**
     * Makes a call to the LedgerSeal smart contract using the `add_log(byte[],string)bool` ABI method.
     *
     * Store log entry for chain-of-custody tracking
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    addLog: (params: CallParams<LedgerSealArgs['obj']['add_log(byte[],string)bool'] | LedgerSealArgs['tuple']['add_log(byte[],string)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(LedgerSealParamsFactory.addLog(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Makes a clear_state call to an existing instance of the LedgerSeal smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the LedgerSeal smart contract using the `register_evidence(byte[],byte[],address)bool` ABI method.
     *
     * Register new evidence with owner and metadata. Rejects duplicates.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    registerEvidence: (params: CallParams<LedgerSealArgs['obj']['register_evidence(byte[],byte[],address)bool'] | LedgerSealArgs['tuple']['register_evidence(byte[],byte[],address)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(LedgerSealParamsFactory.registerEvidence(params))
    },

    /**
     * Makes a call to the LedgerSeal smart contract using the `get_owner(byte[])address` ABI method.
     *
     * Get current owner of evidence
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    getOwner: (params: CallParams<LedgerSealArgs['obj']['get_owner(byte[])address'] | LedgerSealArgs['tuple']['get_owner(byte[])address']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(LedgerSealParamsFactory.getOwner(params))
    },

    /**
     * Makes a call to the LedgerSeal smart contract using the `evidence_exists(byte[])bool` ABI method.
     *
     * Check if evidence is registered
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    evidenceExists: (params: CallParams<LedgerSealArgs['obj']['evidence_exists(byte[])bool'] | LedgerSealArgs['tuple']['evidence_exists(byte[])bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(LedgerSealParamsFactory.evidenceExists(params))
    },

    /**
     * Makes a call to the LedgerSeal smart contract using the `update_metadata(byte[],byte[])bool` ABI method.
     *
     * Update metadata hash. Only current owner can update.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    updateMetadata: (params: CallParams<LedgerSealArgs['obj']['update_metadata(byte[],byte[])bool'] | LedgerSealArgs['tuple']['update_metadata(byte[],byte[])bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(LedgerSealParamsFactory.updateMetadata(params))
    },

    /**
     * Makes a call to the LedgerSeal smart contract using the `transfer_ownership(byte[],address)bool` ABI method.
     *
     * Transfer ownership to another wallet. Only current owner can transfer.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    transferOwnership: (params: CallParams<LedgerSealArgs['obj']['transfer_ownership(byte[],address)bool'] | LedgerSealArgs['tuple']['transfer_ownership(byte[],address)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(LedgerSealParamsFactory.transferOwnership(params))
    },

    /**
     * Makes a call to the LedgerSeal smart contract using the `metadata_exists(byte[])bool` ABI method.
     *
     * Check if metadata exists
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    metadataExists: (params: CallParams<LedgerSealArgs['obj']['metadata_exists(byte[])bool'] | LedgerSealArgs['tuple']['metadata_exists(byte[])bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(LedgerSealParamsFactory.metadataExists(params))
    },

    /**
     * Makes a call to the LedgerSeal smart contract using the `get_timestamp(byte[])uint64` ABI method.
     *
     * Get evidence exists check
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    getTimestamp: (params: CallParams<LedgerSealArgs['obj']['get_timestamp(byte[])uint64'] | LedgerSealArgs['tuple']['get_timestamp(byte[])uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(LedgerSealParamsFactory.getTimestamp(params))
    },

    /**
     * Makes a call to the LedgerSeal smart contract using the `get_last_transfer(byte[])uint64` ABI method.
     *
     * Get last transfer check
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    getLastTransfer: (params: CallParams<LedgerSealArgs['obj']['get_last_transfer(byte[])uint64'] | LedgerSealArgs['tuple']['get_last_transfer(byte[])uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(LedgerSealParamsFactory.getLastTransfer(params))
    },

    /**
     * Makes a call to the LedgerSeal smart contract using the `delete_evidence(byte[])bool` ABI method.
     *
     * Delete evidence. Only app creator allowed.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    deleteEvidence: (params: CallParams<LedgerSealArgs['obj']['delete_evidence(byte[])bool'] | LedgerSealArgs['tuple']['delete_evidence(byte[])bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(LedgerSealParamsFactory.deleteEvidence(params))
    },

    /**
     * Makes a call to the LedgerSeal smart contract using the `verify_evidence(byte[])bool` ABI method.
     *
     * Verify if evidence exists and is valid
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    verifyEvidence: (params: CallParams<LedgerSealArgs['obj']['verify_evidence(byte[])bool'] | LedgerSealArgs['tuple']['verify_evidence(byte[])bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(LedgerSealParamsFactory.verifyEvidence(params))
    },

    /**
     * Makes a call to the LedgerSeal smart contract using the `add_log(byte[],string)bool` ABI method.
     *
     * Store log entry for chain-of-custody tracking
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    addLog: (params: CallParams<LedgerSealArgs['obj']['add_log(byte[],string)bool'] | LedgerSealArgs['tuple']['add_log(byte[],string)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(LedgerSealParamsFactory.addLog(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Makes a clear_state call to an existing instance of the LedgerSeal smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the LedgerSeal smart contract using the `register_evidence(byte[],byte[],address)bool` ABI method.
     *
     * Register new evidence with owner and metadata. Rejects duplicates.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    registerEvidence: async (params: CallParams<LedgerSealArgs['obj']['register_evidence(byte[],byte[],address)bool'] | LedgerSealArgs['tuple']['register_evidence(byte[],byte[],address)bool']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(LedgerSealParamsFactory.registerEvidence(params))
      return {...result, return: result.return as unknown as (undefined | LedgerSealReturns['register_evidence(byte[],byte[],address)bool'])}
    },

    /**
     * Makes a call to the LedgerSeal smart contract using the `get_owner(byte[])address` ABI method.
     *
     * Get current owner of evidence
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    getOwner: async (params: CallParams<LedgerSealArgs['obj']['get_owner(byte[])address'] | LedgerSealArgs['tuple']['get_owner(byte[])address']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(LedgerSealParamsFactory.getOwner(params))
      return {...result, return: result.return as unknown as (undefined | LedgerSealReturns['get_owner(byte[])address'])}
    },

    /**
     * Makes a call to the LedgerSeal smart contract using the `evidence_exists(byte[])bool` ABI method.
     *
     * Check if evidence is registered
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    evidenceExists: async (params: CallParams<LedgerSealArgs['obj']['evidence_exists(byte[])bool'] | LedgerSealArgs['tuple']['evidence_exists(byte[])bool']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(LedgerSealParamsFactory.evidenceExists(params))
      return {...result, return: result.return as unknown as (undefined | LedgerSealReturns['evidence_exists(byte[])bool'])}
    },

    /**
     * Makes a call to the LedgerSeal smart contract using the `update_metadata(byte[],byte[])bool` ABI method.
     *
     * Update metadata hash. Only current owner can update.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    updateMetadata: async (params: CallParams<LedgerSealArgs['obj']['update_metadata(byte[],byte[])bool'] | LedgerSealArgs['tuple']['update_metadata(byte[],byte[])bool']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(LedgerSealParamsFactory.updateMetadata(params))
      return {...result, return: result.return as unknown as (undefined | LedgerSealReturns['update_metadata(byte[],byte[])bool'])}
    },

    /**
     * Makes a call to the LedgerSeal smart contract using the `transfer_ownership(byte[],address)bool` ABI method.
     *
     * Transfer ownership to another wallet. Only current owner can transfer.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    transferOwnership: async (params: CallParams<LedgerSealArgs['obj']['transfer_ownership(byte[],address)bool'] | LedgerSealArgs['tuple']['transfer_ownership(byte[],address)bool']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(LedgerSealParamsFactory.transferOwnership(params))
      return {...result, return: result.return as unknown as (undefined | LedgerSealReturns['transfer_ownership(byte[],address)bool'])}
    },

    /**
     * Makes a call to the LedgerSeal smart contract using the `metadata_exists(byte[])bool` ABI method.
     *
     * Check if metadata exists
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    metadataExists: async (params: CallParams<LedgerSealArgs['obj']['metadata_exists(byte[])bool'] | LedgerSealArgs['tuple']['metadata_exists(byte[])bool']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(LedgerSealParamsFactory.metadataExists(params))
      return {...result, return: result.return as unknown as (undefined | LedgerSealReturns['metadata_exists(byte[])bool'])}
    },

    /**
     * Makes a call to the LedgerSeal smart contract using the `get_timestamp(byte[])uint64` ABI method.
     *
     * Get evidence exists check
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    getTimestamp: async (params: CallParams<LedgerSealArgs['obj']['get_timestamp(byte[])uint64'] | LedgerSealArgs['tuple']['get_timestamp(byte[])uint64']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(LedgerSealParamsFactory.getTimestamp(params))
      return {...result, return: result.return as unknown as (undefined | LedgerSealReturns['get_timestamp(byte[])uint64'])}
    },

    /**
     * Makes a call to the LedgerSeal smart contract using the `get_last_transfer(byte[])uint64` ABI method.
     *
     * Get last transfer check
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    getLastTransfer: async (params: CallParams<LedgerSealArgs['obj']['get_last_transfer(byte[])uint64'] | LedgerSealArgs['tuple']['get_last_transfer(byte[])uint64']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(LedgerSealParamsFactory.getLastTransfer(params))
      return {...result, return: result.return as unknown as (undefined | LedgerSealReturns['get_last_transfer(byte[])uint64'])}
    },

    /**
     * Makes a call to the LedgerSeal smart contract using the `delete_evidence(byte[])bool` ABI method.
     *
     * Delete evidence. Only app creator allowed.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    deleteEvidence: async (params: CallParams<LedgerSealArgs['obj']['delete_evidence(byte[])bool'] | LedgerSealArgs['tuple']['delete_evidence(byte[])bool']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(LedgerSealParamsFactory.deleteEvidence(params))
      return {...result, return: result.return as unknown as (undefined | LedgerSealReturns['delete_evidence(byte[])bool'])}
    },

    /**
     * Makes a call to the LedgerSeal smart contract using the `verify_evidence(byte[])bool` ABI method.
     *
     * Verify if evidence exists and is valid
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    verifyEvidence: async (params: CallParams<LedgerSealArgs['obj']['verify_evidence(byte[])bool'] | LedgerSealArgs['tuple']['verify_evidence(byte[])bool']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(LedgerSealParamsFactory.verifyEvidence(params))
      return {...result, return: result.return as unknown as (undefined | LedgerSealReturns['verify_evidence(byte[])bool'])}
    },

    /**
     * Makes a call to the LedgerSeal smart contract using the `add_log(byte[],string)bool` ABI method.
     *
     * Store log entry for chain-of-custody tracking
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    addLog: async (params: CallParams<LedgerSealArgs['obj']['add_log(byte[],string)bool'] | LedgerSealArgs['tuple']['add_log(byte[],string)bool']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(LedgerSealParamsFactory.addLog(params))
      return {...result, return: result.return as unknown as (undefined | LedgerSealReturns['add_log(byte[],string)bool'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new LedgerSealClient(this.appClient.clone(params))
  }

  /**
   * Methods to access state for the current LedgerSeal app
   */
  state = {
  }

  public newGroup(): LedgerSealComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a register_evidence(byte[],byte[],address)bool method call against the LedgerSeal contract
       */
      registerEvidence(params: CallParams<LedgerSealArgs['obj']['register_evidence(byte[],byte[],address)bool'] | LedgerSealArgs['tuple']['register_evidence(byte[],byte[],address)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.registerEvidence(params)))
        resultMappers.push((v) => client.decodeReturnValue('register_evidence(byte[],byte[],address)bool', v))
        return this
      },
      /**
       * Add a get_owner(byte[])address method call against the LedgerSeal contract
       */
      getOwner(params: CallParams<LedgerSealArgs['obj']['get_owner(byte[])address'] | LedgerSealArgs['tuple']['get_owner(byte[])address']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.getOwner(params)))
        resultMappers.push((v) => client.decodeReturnValue('get_owner(byte[])address', v))
        return this
      },
      /**
       * Add a evidence_exists(byte[])bool method call against the LedgerSeal contract
       */
      evidenceExists(params: CallParams<LedgerSealArgs['obj']['evidence_exists(byte[])bool'] | LedgerSealArgs['tuple']['evidence_exists(byte[])bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.evidenceExists(params)))
        resultMappers.push((v) => client.decodeReturnValue('evidence_exists(byte[])bool', v))
        return this
      },
      /**
       * Add a update_metadata(byte[],byte[])bool method call against the LedgerSeal contract
       */
      updateMetadata(params: CallParams<LedgerSealArgs['obj']['update_metadata(byte[],byte[])bool'] | LedgerSealArgs['tuple']['update_metadata(byte[],byte[])bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.updateMetadata(params)))
        resultMappers.push((v) => client.decodeReturnValue('update_metadata(byte[],byte[])bool', v))
        return this
      },
      /**
       * Add a transfer_ownership(byte[],address)bool method call against the LedgerSeal contract
       */
      transferOwnership(params: CallParams<LedgerSealArgs['obj']['transfer_ownership(byte[],address)bool'] | LedgerSealArgs['tuple']['transfer_ownership(byte[],address)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.transferOwnership(params)))
        resultMappers.push((v) => client.decodeReturnValue('transfer_ownership(byte[],address)bool', v))
        return this
      },
      /**
       * Add a metadata_exists(byte[])bool method call against the LedgerSeal contract
       */
      metadataExists(params: CallParams<LedgerSealArgs['obj']['metadata_exists(byte[])bool'] | LedgerSealArgs['tuple']['metadata_exists(byte[])bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.metadataExists(params)))
        resultMappers.push((v) => client.decodeReturnValue('metadata_exists(byte[])bool', v))
        return this
      },
      /**
       * Add a get_timestamp(byte[])uint64 method call against the LedgerSeal contract
       */
      getTimestamp(params: CallParams<LedgerSealArgs['obj']['get_timestamp(byte[])uint64'] | LedgerSealArgs['tuple']['get_timestamp(byte[])uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.getTimestamp(params)))
        resultMappers.push((v) => client.decodeReturnValue('get_timestamp(byte[])uint64', v))
        return this
      },
      /**
       * Add a get_last_transfer(byte[])uint64 method call against the LedgerSeal contract
       */
      getLastTransfer(params: CallParams<LedgerSealArgs['obj']['get_last_transfer(byte[])uint64'] | LedgerSealArgs['tuple']['get_last_transfer(byte[])uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.getLastTransfer(params)))
        resultMappers.push((v) => client.decodeReturnValue('get_last_transfer(byte[])uint64', v))
        return this
      },
      /**
       * Add a delete_evidence(byte[])bool method call against the LedgerSeal contract
       */
      deleteEvidence(params: CallParams<LedgerSealArgs['obj']['delete_evidence(byte[])bool'] | LedgerSealArgs['tuple']['delete_evidence(byte[])bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.deleteEvidence(params)))
        resultMappers.push((v) => client.decodeReturnValue('delete_evidence(byte[])bool', v))
        return this
      },
      /**
       * Add a verify_evidence(byte[])bool method call against the LedgerSeal contract
       */
      verifyEvidence(params: CallParams<LedgerSealArgs['obj']['verify_evidence(byte[])bool'] | LedgerSealArgs['tuple']['verify_evidence(byte[])bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.verifyEvidence(params)))
        resultMappers.push((v) => client.decodeReturnValue('verify_evidence(byte[])bool', v))
        return this
      },
      /**
       * Add a add_log(byte[],string)bool method call against the LedgerSeal contract
       */
      addLog(params: CallParams<LedgerSealArgs['obj']['add_log(byte[],string)bool'] | LedgerSealArgs['tuple']['add_log(byte[],string)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.addLog(params)))
        resultMappers.push((v) => client.decodeReturnValue('add_log(byte[],string)bool', v))
        return this
      },
      /**
       * Add a clear state call to the LedgerSeal contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as LedgerSealComposer
  }
}
export type LedgerSealComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the register_evidence(byte[],byte[],address)bool ABI method.
   *
   * Register new evidence with owner and metadata. Rejects duplicates.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  registerEvidence(params?: CallParams<LedgerSealArgs['obj']['register_evidence(byte[],byte[],address)bool'] | LedgerSealArgs['tuple']['register_evidence(byte[],byte[],address)bool']>): LedgerSealComposer<[...TReturns, LedgerSealReturns['register_evidence(byte[],byte[],address)bool'] | undefined]>

  /**
   * Calls the get_owner(byte[])address ABI method.
   *
   * Get current owner of evidence
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  getOwner(params?: CallParams<LedgerSealArgs['obj']['get_owner(byte[])address'] | LedgerSealArgs['tuple']['get_owner(byte[])address']>): LedgerSealComposer<[...TReturns, LedgerSealReturns['get_owner(byte[])address'] | undefined]>

  /**
   * Calls the evidence_exists(byte[])bool ABI method.
   *
   * Check if evidence is registered
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  evidenceExists(params?: CallParams<LedgerSealArgs['obj']['evidence_exists(byte[])bool'] | LedgerSealArgs['tuple']['evidence_exists(byte[])bool']>): LedgerSealComposer<[...TReturns, LedgerSealReturns['evidence_exists(byte[])bool'] | undefined]>

  /**
   * Calls the update_metadata(byte[],byte[])bool ABI method.
   *
   * Update metadata hash. Only current owner can update.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  updateMetadata(params?: CallParams<LedgerSealArgs['obj']['update_metadata(byte[],byte[])bool'] | LedgerSealArgs['tuple']['update_metadata(byte[],byte[])bool']>): LedgerSealComposer<[...TReturns, LedgerSealReturns['update_metadata(byte[],byte[])bool'] | undefined]>

  /**
   * Calls the transfer_ownership(byte[],address)bool ABI method.
   *
   * Transfer ownership to another wallet. Only current owner can transfer.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  transferOwnership(params?: CallParams<LedgerSealArgs['obj']['transfer_ownership(byte[],address)bool'] | LedgerSealArgs['tuple']['transfer_ownership(byte[],address)bool']>): LedgerSealComposer<[...TReturns, LedgerSealReturns['transfer_ownership(byte[],address)bool'] | undefined]>

  /**
   * Calls the metadata_exists(byte[])bool ABI method.
   *
   * Check if metadata exists
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  metadataExists(params?: CallParams<LedgerSealArgs['obj']['metadata_exists(byte[])bool'] | LedgerSealArgs['tuple']['metadata_exists(byte[])bool']>): LedgerSealComposer<[...TReturns, LedgerSealReturns['metadata_exists(byte[])bool'] | undefined]>

  /**
   * Calls the get_timestamp(byte[])uint64 ABI method.
   *
   * Get evidence exists check
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  getTimestamp(params?: CallParams<LedgerSealArgs['obj']['get_timestamp(byte[])uint64'] | LedgerSealArgs['tuple']['get_timestamp(byte[])uint64']>): LedgerSealComposer<[...TReturns, LedgerSealReturns['get_timestamp(byte[])uint64'] | undefined]>

  /**
   * Calls the get_last_transfer(byte[])uint64 ABI method.
   *
   * Get last transfer check
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  getLastTransfer(params?: CallParams<LedgerSealArgs['obj']['get_last_transfer(byte[])uint64'] | LedgerSealArgs['tuple']['get_last_transfer(byte[])uint64']>): LedgerSealComposer<[...TReturns, LedgerSealReturns['get_last_transfer(byte[])uint64'] | undefined]>

  /**
   * Calls the delete_evidence(byte[])bool ABI method.
   *
   * Delete evidence. Only app creator allowed.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  deleteEvidence(params?: CallParams<LedgerSealArgs['obj']['delete_evidence(byte[])bool'] | LedgerSealArgs['tuple']['delete_evidence(byte[])bool']>): LedgerSealComposer<[...TReturns, LedgerSealReturns['delete_evidence(byte[])bool'] | undefined]>

  /**
   * Calls the verify_evidence(byte[])bool ABI method.
   *
   * Verify if evidence exists and is valid
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  verifyEvidence(params?: CallParams<LedgerSealArgs['obj']['verify_evidence(byte[])bool'] | LedgerSealArgs['tuple']['verify_evidence(byte[])bool']>): LedgerSealComposer<[...TReturns, LedgerSealReturns['verify_evidence(byte[])bool'] | undefined]>

  /**
   * Calls the add_log(byte[],string)bool ABI method.
   *
   * Store log entry for chain-of-custody tracking
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  addLog(params?: CallParams<LedgerSealArgs['obj']['add_log(byte[],string)bool'] | LedgerSealArgs['tuple']['add_log(byte[],string)bool']>): LedgerSealComposer<[...TReturns, LedgerSealReturns['add_log(byte[],string)bool'] | undefined]>

  /**
   * Makes a clear_state call to an existing instance of the LedgerSeal smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): LedgerSealComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): LedgerSealComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<LedgerSealComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<LedgerSealComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<LedgerSealComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<LedgerSealComposerResults<TReturns>>
}
export type LedgerSealComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

