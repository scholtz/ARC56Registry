/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"EscrowVault","structs":{},"methods":[{"name":"create","args":[{"type":"address","name":"recipient"}],"returns":{"type":"void"},"actions":{"create":["NoOp"],"call":[]},"readonly":false,"desc":"Deploy the vault and set creator + recipient.","events":[],"recommendations":{}},{"name":"release","args":[],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Release locked funds to recipient. Only callable by creator.","events":[],"recommendations":{}},{"name":"kill","args":[],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Reclaim funds back to creator. Only callable by creator.","events":[],"recommendations":{}},{"name":"delete","args":[],"returns":{"type":"void"},"actions":{"create":[],"call":["DeleteApplication"]},"readonly":false,"desc":"Delete the application. Only allowed after release or kill.","events":[],"recommendations":{}}],"arcs":[22,28],"desc":"\n    Standard Escrow Vault (ARC-4 compliant).\n\n    Creator locks funds for a recipient. Creator can release funds to the\n    recipient or kill the vault to reclaim them.\n\n    GlobalState:\n        creator      — account that deployed and funded the vault\n        recipient    — account that receives funds on release\n        status       — 0 = locked, 1 = released, 2 = killed\n    ","networks":{},"state":{"schema":{"global":{"ints":1,"bytes":2},"local":{"ints":0,"bytes":0}},"keys":{"global":{"creator":{"keyType":"AVMString","valueType":"address","key":"Y3JlYXRvcg==","desc":"Vault creator address"},"recipient":{"keyType":"AVMString","valueType":"address","key":"cmVjaXBpZW50","desc":"Fund recipient address"},"status":{"keyType":"AVMString","valueType":"AVMUint64","key":"c3RhdHVz","desc":"0=locked 1=released 2=killed"}},"local":{},"box":{}},"maps":{"global":{},"local":{},"box":{}}},"bareActions":{"create":[],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[217],"errorMessage":"Only creator can delete"},{"pc":[175],"errorMessage":"Only creator can kill"},{"pc":[130],"errorMessage":"Only creator can release"},{"pc":[136,181],"errorMessage":"Vault is not locked"},{"pc":[222],"errorMessage":"Vault still locked — release or kill first"},{"pc":[128,170,212],"errorMessage":"check self.creator exists"},{"pc":[148],"errorMessage":"check self.recipient exists"},{"pc":[134,179,221],"errorMessage":"check self.status exists"},{"pc":[110],"errorMessage":"invalid number of bytes for arc4.static_array<arc4.uint8, 32>"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"events":[]} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


/**
 * The argument types for the EscrowVault contract
 */
export type EscrowVaultArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'create(address)void': {
      recipient: string
    }
    'release()void': Record<string, never>
    'kill()void': Record<string, never>
    'delete()void': Record<string, never>
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'create(address)void': [recipient: string]
    'release()void': []
    'kill()void': []
    'delete()void': []
  }
}

/**
 * The return type for each method
 */
export type EscrowVaultReturns = {
  'create(address)void': void
  'release()void': void
  'kill()void': void
  'delete()void': void
}

/**
 * Defines the types of available calls and state of the EscrowVault smart contract.
 */
export type EscrowVaultTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'create(address)void' | 'create', {
      argsObj: EscrowVaultArgs['obj']['create(address)void']
      argsTuple: EscrowVaultArgs['tuple']['create(address)void']
      returns: EscrowVaultReturns['create(address)void']
    }>
    & Record<'release()void' | 'release', {
      argsObj: EscrowVaultArgs['obj']['release()void']
      argsTuple: EscrowVaultArgs['tuple']['release()void']
      returns: EscrowVaultReturns['release()void']
    }>
    & Record<'kill()void' | 'kill', {
      argsObj: EscrowVaultArgs['obj']['kill()void']
      argsTuple: EscrowVaultArgs['tuple']['kill()void']
      returns: EscrowVaultReturns['kill()void']
    }>
    & Record<'delete()void' | 'delete', {
      argsObj: EscrowVaultArgs['obj']['delete()void']
      argsTuple: EscrowVaultArgs['tuple']['delete()void']
      returns: EscrowVaultReturns['delete()void']
    }>
  /**
   * Defines the shape of the state of the application.
   */
  state: {
    global: {
      keys: {
        /**
         * Vault creator address
         */
        creator: string
        /**
         * Fund recipient address
         */
        recipient: string
        /**
         * 0=locked 1=released 2=killed
         */
        status: bigint
      }
      maps: {}
    }
  }
}

/**
 * Defines the possible abi call signatures.
 */
export type EscrowVaultSignatures = keyof EscrowVaultTypes['methods']
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the EscrowVault smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends EscrowVaultSignatures> = EscrowVaultTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the EscrowVault smart contract to the method's return type
 */
export type MethodReturn<TSignature extends EscrowVaultSignatures> = EscrowVaultTypes['methods'][TSignature]['returns']

/**
 * Defines the shape of the keyed global state of the application.
 */
export type GlobalKeysState = EscrowVaultTypes['state']['global']['keys']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the EscrowVault smart contract
 */
export abstract class EscrowVaultParamsFactory {
  /**
   * Constructs a no op call for the release()void ABI method
   *
   * Release locked funds to recipient. Only callable by creator.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static release(params: CallParams<EscrowVaultArgs['obj']['release()void'] | EscrowVaultArgs['tuple']['release()void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'release()void' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the kill()void ABI method
   *
   * Reclaim funds back to creator. Only callable by creator.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static kill(params: CallParams<EscrowVaultArgs['obj']['kill()void'] | EscrowVaultArgs['tuple']['kill()void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'kill()void' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
}

/**
 * A client to make calls to the EscrowVault smart contract
 */
export class EscrowVaultClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `EscrowVaultClient`
   *
   * @param appClient An `AppClient` instance which has been created with the EscrowVault app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `EscrowVaultClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Returns a new `EscrowVaultClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<EscrowVaultClient> {
    return new EscrowVaultClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `EscrowVaultClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<EscrowVaultClient> {
    return new EscrowVaultClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Makes a clear_state call to an existing instance of the EscrowVault smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the EscrowVault smart contract using the `release()void` ABI method.
     *
     * Release locked funds to recipient. Only callable by creator.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    release: (params: CallParams<EscrowVaultArgs['obj']['release()void'] | EscrowVaultArgs['tuple']['release()void']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(EscrowVaultParamsFactory.release(params))
    },

    /**
     * Makes a call to the EscrowVault smart contract using the `kill()void` ABI method.
     *
     * Reclaim funds back to creator. Only callable by creator.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    kill: (params: CallParams<EscrowVaultArgs['obj']['kill()void'] | EscrowVaultArgs['tuple']['kill()void']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(EscrowVaultParamsFactory.kill(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Makes a clear_state call to an existing instance of the EscrowVault smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the EscrowVault smart contract using the `release()void` ABI method.
     *
     * Release locked funds to recipient. Only callable by creator.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    release: (params: CallParams<EscrowVaultArgs['obj']['release()void'] | EscrowVaultArgs['tuple']['release()void']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(EscrowVaultParamsFactory.release(params))
    },

    /**
     * Makes a call to the EscrowVault smart contract using the `kill()void` ABI method.
     *
     * Reclaim funds back to creator. Only callable by creator.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    kill: (params: CallParams<EscrowVaultArgs['obj']['kill()void'] | EscrowVaultArgs['tuple']['kill()void']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(EscrowVaultParamsFactory.kill(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Makes a clear_state call to an existing instance of the EscrowVault smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the EscrowVault smart contract using the `release()void` ABI method.
     *
     * Release locked funds to recipient. Only callable by creator.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    release: async (params: CallParams<EscrowVaultArgs['obj']['release()void'] | EscrowVaultArgs['tuple']['release()void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(EscrowVaultParamsFactory.release(params))
      return {...result, return: result.return as unknown as (undefined | EscrowVaultReturns['release()void'])}
    },

    /**
     * Makes a call to the EscrowVault smart contract using the `kill()void` ABI method.
     *
     * Reclaim funds back to creator. Only callable by creator.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    kill: async (params: CallParams<EscrowVaultArgs['obj']['kill()void'] | EscrowVaultArgs['tuple']['kill()void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(EscrowVaultParamsFactory.kill(params))
      return {...result, return: result.return as unknown as (undefined | EscrowVaultReturns['kill()void'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new EscrowVaultClient(this.appClient.clone(params))
  }

  /**
   * Methods to access state for the current EscrowVault app
   */
  state = {
    /**
     * Methods to access global state for the current EscrowVault app
     */
    global: {
      /**
       * Get all current keyed values from global state
       */
      getAll: async (): Promise<Partial<Expand<GlobalKeysState>>> => {
        const result = await this.appClient.state.global.getAll()
        return {
          creator: result.creator,
          recipient: result.recipient,
          status: result.status,
        }
      },
      /**
       * Get the current value of the creator key in global state
       */
      creator: async (): Promise<string | undefined> => { return (await this.appClient.state.global.getValue("creator")) as string | undefined },
      /**
       * Get the current value of the recipient key in global state
       */
      recipient: async (): Promise<string | undefined> => { return (await this.appClient.state.global.getValue("recipient")) as string | undefined },
      /**
       * Get the current value of the status key in global state
       */
      status: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("status")) as bigint | undefined },
    },
  }

  public newGroup(): EscrowVaultComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a release()void method call against the EscrowVault contract
       */
      release(params: CallParams<EscrowVaultArgs['obj']['release()void'] | EscrowVaultArgs['tuple']['release()void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.release(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a kill()void method call against the EscrowVault contract
       */
      kill(params: CallParams<EscrowVaultArgs['obj']['kill()void'] | EscrowVaultArgs['tuple']['kill()void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.kill(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a clear state call to the EscrowVault contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as EscrowVaultComposer
  }
}
export type EscrowVaultComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the release()void ABI method.
   *
   * Release locked funds to recipient. Only callable by creator.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  release(params?: CallParams<EscrowVaultArgs['obj']['release()void'] | EscrowVaultArgs['tuple']['release()void']>): EscrowVaultComposer<[...TReturns, EscrowVaultReturns['release()void'] | undefined]>

  /**
   * Calls the kill()void ABI method.
   *
   * Reclaim funds back to creator. Only callable by creator.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  kill(params?: CallParams<EscrowVaultArgs['obj']['kill()void'] | EscrowVaultArgs['tuple']['kill()void']>): EscrowVaultComposer<[...TReturns, EscrowVaultReturns['kill()void'] | undefined]>

  /**
   * Makes a clear_state call to an existing instance of the EscrowVault smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): EscrowVaultComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): EscrowVaultComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<EscrowVaultComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<EscrowVaultComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<EscrowVaultComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<EscrowVaultComposerResults<TReturns>>
}
export type EscrowVaultComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

