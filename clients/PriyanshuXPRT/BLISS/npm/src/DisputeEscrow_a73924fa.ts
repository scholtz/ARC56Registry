/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"DisputeEscrow","structs":{},"methods":[{"name":"create","args":[{"type":"address","name":"recipient"},{"type":"address","name":"arbitrator"}],"returns":{"type":"void"},"actions":{"create":["NoOp"],"call":[]},"readonly":false,"desc":"Deploy the vault with a designated arbitrator.","events":[],"recommendations":{}},{"name":"release","args":[],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Release funds to recipient. Callable by creator or arbitrator.","events":[],"recommendations":{}},{"name":"kill","args":[],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Reclaim funds to creator. Callable by creator or arbitrator.","events":[],"recommendations":{}},{"name":"delete","args":[],"returns":{"type":"void"},"actions":{"create":[],"call":["DeleteApplication"]},"readonly":false,"desc":"Delete the application after it is no longer locked.","events":[],"recommendations":{}}],"arcs":[22,28],"desc":"\n    Dispute-Resolution Escrow Vault (ARC-4 compliant).\n\n    A neutral arbitrator is designated at creation. Either the original\n    creator OR the arbitrator can call release() or kill() to resolve a\n    dispute — giving the arbitrator power to decide the outcome.\n\n    GlobalState:\n        creator     — account that deployed and funded the vault\n        recipient   — account that receives funds on release\n        arbitrator  — neutral third party with release/kill powers\n        status      — 0 = locked, 1 = released, 2 = killed\n    ","networks":{},"state":{"schema":{"global":{"ints":1,"bytes":3},"local":{"ints":0,"bytes":0}},"keys":{"global":{"creator":{"keyType":"AVMString","valueType":"address","key":"Y3JlYXRvcg==","desc":"Vault creator"},"recipient":{"keyType":"AVMString","valueType":"address","key":"cmVjaXBpZW50","desc":"Fund recipient"},"arbitrator":{"keyType":"AVMString","valueType":"address","key":"YXJiaXRyYXRvcg==","desc":"Dispute arbitrator"},"status":{"keyType":"AVMString","valueType":"AVMUint64","key":"c3RhdHVz","desc":"0=locked 1=released 2=killed"}},"local":{},"box":{}},"maps":{"global":{},"local":{},"box":{}}},"bareActions":{"create":[],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[256],"errorMessage":"Only creator can delete"},{"pc":[214],"errorMessage":"Only creator or arbitrator can kill"},{"pc":[163],"errorMessage":"Only creator or arbitrator can release"},{"pc":[169,220],"errorMessage":"Vault is not locked"},{"pc":[261],"errorMessage":"Vault still locked"},{"pc":[158,209],"errorMessage":"check self.arbitrator exists"},{"pc":[151,203,251],"errorMessage":"check self.creator exists"},{"pc":[181],"errorMessage":"check self.recipient exists"},{"pc":[167,218,260],"errorMessage":"check self.status exists"},{"pc":[121,129],"errorMessage":"invalid number of bytes for arc4.static_array<arc4.uint8, 32>"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"events":[]} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


/**
 * The argument types for the DisputeEscrow contract
 */
export type DisputeEscrowArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'create(address,address)void': {
      recipient: string
      arbitrator: string
    }
    'release()void': Record<string, never>
    'kill()void': Record<string, never>
    'delete()void': Record<string, never>
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'create(address,address)void': [recipient: string, arbitrator: string]
    'release()void': []
    'kill()void': []
    'delete()void': []
  }
}

/**
 * The return type for each method
 */
export type DisputeEscrowReturns = {
  'create(address,address)void': void
  'release()void': void
  'kill()void': void
  'delete()void': void
}

/**
 * Defines the types of available calls and state of the DisputeEscrow smart contract.
 */
export type DisputeEscrowTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'create(address,address)void' | 'create', {
      argsObj: DisputeEscrowArgs['obj']['create(address,address)void']
      argsTuple: DisputeEscrowArgs['tuple']['create(address,address)void']
      returns: DisputeEscrowReturns['create(address,address)void']
    }>
    & Record<'release()void' | 'release', {
      argsObj: DisputeEscrowArgs['obj']['release()void']
      argsTuple: DisputeEscrowArgs['tuple']['release()void']
      returns: DisputeEscrowReturns['release()void']
    }>
    & Record<'kill()void' | 'kill', {
      argsObj: DisputeEscrowArgs['obj']['kill()void']
      argsTuple: DisputeEscrowArgs['tuple']['kill()void']
      returns: DisputeEscrowReturns['kill()void']
    }>
    & Record<'delete()void' | 'delete', {
      argsObj: DisputeEscrowArgs['obj']['delete()void']
      argsTuple: DisputeEscrowArgs['tuple']['delete()void']
      returns: DisputeEscrowReturns['delete()void']
    }>
  /**
   * Defines the shape of the state of the application.
   */
  state: {
    global: {
      keys: {
        /**
         * Vault creator
         */
        creator: string
        /**
         * Fund recipient
         */
        recipient: string
        /**
         * Dispute arbitrator
         */
        arbitrator: string
        /**
         * 0=locked 1=released 2=killed
         */
        status: bigint
      }
      maps: {}
    }
  }
}

/**
 * Defines the possible abi call signatures.
 */
export type DisputeEscrowSignatures = keyof DisputeEscrowTypes['methods']
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the DisputeEscrow smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends DisputeEscrowSignatures> = DisputeEscrowTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the DisputeEscrow smart contract to the method's return type
 */
export type MethodReturn<TSignature extends DisputeEscrowSignatures> = DisputeEscrowTypes['methods'][TSignature]['returns']

/**
 * Defines the shape of the keyed global state of the application.
 */
export type GlobalKeysState = DisputeEscrowTypes['state']['global']['keys']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the DisputeEscrow smart contract
 */
export abstract class DisputeEscrowParamsFactory {
  /**
   * Constructs a no op call for the release()void ABI method
   *
   * Release funds to recipient. Callable by creator or arbitrator.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static release(params: CallParams<DisputeEscrowArgs['obj']['release()void'] | DisputeEscrowArgs['tuple']['release()void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'release()void' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the kill()void ABI method
   *
   * Reclaim funds to creator. Callable by creator or arbitrator.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static kill(params: CallParams<DisputeEscrowArgs['obj']['kill()void'] | DisputeEscrowArgs['tuple']['kill()void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'kill()void' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
}

/**
 * A client to make calls to the DisputeEscrow smart contract
 */
export class DisputeEscrowClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `DisputeEscrowClient`
   *
   * @param appClient An `AppClient` instance which has been created with the DisputeEscrow app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `DisputeEscrowClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Returns a new `DisputeEscrowClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<DisputeEscrowClient> {
    return new DisputeEscrowClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `DisputeEscrowClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<DisputeEscrowClient> {
    return new DisputeEscrowClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Makes a clear_state call to an existing instance of the DisputeEscrow smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the DisputeEscrow smart contract using the `release()void` ABI method.
     *
     * Release funds to recipient. Callable by creator or arbitrator.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    release: (params: CallParams<DisputeEscrowArgs['obj']['release()void'] | DisputeEscrowArgs['tuple']['release()void']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(DisputeEscrowParamsFactory.release(params))
    },

    /**
     * Makes a call to the DisputeEscrow smart contract using the `kill()void` ABI method.
     *
     * Reclaim funds to creator. Callable by creator or arbitrator.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    kill: (params: CallParams<DisputeEscrowArgs['obj']['kill()void'] | DisputeEscrowArgs['tuple']['kill()void']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(DisputeEscrowParamsFactory.kill(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Makes a clear_state call to an existing instance of the DisputeEscrow smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the DisputeEscrow smart contract using the `release()void` ABI method.
     *
     * Release funds to recipient. Callable by creator or arbitrator.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    release: (params: CallParams<DisputeEscrowArgs['obj']['release()void'] | DisputeEscrowArgs['tuple']['release()void']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(DisputeEscrowParamsFactory.release(params))
    },

    /**
     * Makes a call to the DisputeEscrow smart contract using the `kill()void` ABI method.
     *
     * Reclaim funds to creator. Callable by creator or arbitrator.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    kill: (params: CallParams<DisputeEscrowArgs['obj']['kill()void'] | DisputeEscrowArgs['tuple']['kill()void']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(DisputeEscrowParamsFactory.kill(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Makes a clear_state call to an existing instance of the DisputeEscrow smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the DisputeEscrow smart contract using the `release()void` ABI method.
     *
     * Release funds to recipient. Callable by creator or arbitrator.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    release: async (params: CallParams<DisputeEscrowArgs['obj']['release()void'] | DisputeEscrowArgs['tuple']['release()void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(DisputeEscrowParamsFactory.release(params))
      return {...result, return: result.return as unknown as (undefined | DisputeEscrowReturns['release()void'])}
    },

    /**
     * Makes a call to the DisputeEscrow smart contract using the `kill()void` ABI method.
     *
     * Reclaim funds to creator. Callable by creator or arbitrator.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    kill: async (params: CallParams<DisputeEscrowArgs['obj']['kill()void'] | DisputeEscrowArgs['tuple']['kill()void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(DisputeEscrowParamsFactory.kill(params))
      return {...result, return: result.return as unknown as (undefined | DisputeEscrowReturns['kill()void'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new DisputeEscrowClient(this.appClient.clone(params))
  }

  /**
   * Methods to access state for the current DisputeEscrow app
   */
  state = {
    /**
     * Methods to access global state for the current DisputeEscrow app
     */
    global: {
      /**
       * Get all current keyed values from global state
       */
      getAll: async (): Promise<Partial<Expand<GlobalKeysState>>> => {
        const result = await this.appClient.state.global.getAll()
        return {
          creator: result.creator,
          recipient: result.recipient,
          arbitrator: result.arbitrator,
          status: result.status,
        }
      },
      /**
       * Get the current value of the creator key in global state
       */
      creator: async (): Promise<string | undefined> => { return (await this.appClient.state.global.getValue("creator")) as string | undefined },
      /**
       * Get the current value of the recipient key in global state
       */
      recipient: async (): Promise<string | undefined> => { return (await this.appClient.state.global.getValue("recipient")) as string | undefined },
      /**
       * Get the current value of the arbitrator key in global state
       */
      arbitrator: async (): Promise<string | undefined> => { return (await this.appClient.state.global.getValue("arbitrator")) as string | undefined },
      /**
       * Get the current value of the status key in global state
       */
      status: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("status")) as bigint | undefined },
    },
  }

  public newGroup(): DisputeEscrowComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a release()void method call against the DisputeEscrow contract
       */
      release(params: CallParams<DisputeEscrowArgs['obj']['release()void'] | DisputeEscrowArgs['tuple']['release()void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.release(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a kill()void method call against the DisputeEscrow contract
       */
      kill(params: CallParams<DisputeEscrowArgs['obj']['kill()void'] | DisputeEscrowArgs['tuple']['kill()void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.kill(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a clear state call to the DisputeEscrow contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as DisputeEscrowComposer
  }
}
export type DisputeEscrowComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the release()void ABI method.
   *
   * Release funds to recipient. Callable by creator or arbitrator.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  release(params?: CallParams<DisputeEscrowArgs['obj']['release()void'] | DisputeEscrowArgs['tuple']['release()void']>): DisputeEscrowComposer<[...TReturns, DisputeEscrowReturns['release()void'] | undefined]>

  /**
   * Calls the kill()void ABI method.
   *
   * Reclaim funds to creator. Callable by creator or arbitrator.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  kill(params?: CallParams<DisputeEscrowArgs['obj']['kill()void'] | DisputeEscrowArgs['tuple']['kill()void']>): DisputeEscrowComposer<[...TReturns, DisputeEscrowReturns['kill()void'] | undefined]>

  /**
   * Makes a clear_state call to an existing instance of the DisputeEscrow smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): DisputeEscrowComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): DisputeEscrowComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<DisputeEscrowComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<DisputeEscrowComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<DisputeEscrowComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<DisputeEscrowComposerResults<TReturns>>
}
export type DisputeEscrowComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

