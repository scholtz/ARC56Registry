/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"MultiSigEscrow","structs":{},"methods":[{"name":"create","args":[{"type":"address","name":"recipient"},{"type":"address","name":"co_signer"}],"returns":{"type":"void"},"actions":{"create":["NoOp"],"call":[]},"readonly":false,"desc":"Deploy the vault and record both parties.","events":[],"recommendations":{}},{"name":"approve","args":[],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Record approval from creator or co-signer. Auto-releases when both approve.","events":[],"recommendations":{}},{"name":"kill","args":[],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Creator can kill the vault and reclaim funds.","events":[],"recommendations":{}},{"name":"delete","args":[],"returns":{"type":"void"},"actions":{"create":[],"call":["DeleteApplication"]},"readonly":false,"desc":"Delete the application after it is no longer locked.","events":[],"recommendations":{}}],"arcs":[22,28],"desc":"\n    Multi-Signature Escrow Vault (ARC-4 compliant).\n\n    Both creator AND co-signer must call approve(). When both have approved,\n    funds are automatically released to recipient.\n\n    GlobalState:\n        creator              — account that deployed and funded the vault\n        recipient            — account that receives funds on release\n        co_signer            — second approver\n        status               — 0 = locked, 1 = released, 2 = killed\n        creator_approved     — 0/1 flag\n        cosigner_approved    — 0/1 flag\n    ","networks":{},"state":{"schema":{"global":{"ints":3,"bytes":3},"local":{"ints":0,"bytes":0}},"keys":{"global":{"creator":{"keyType":"AVMString","valueType":"address","key":"Y3JlYXRvcg==","desc":"Vault creator"},"recipient":{"keyType":"AVMString","valueType":"address","key":"cmVjaXBpZW50","desc":"Fund recipient"},"co_signer":{"keyType":"AVMString","valueType":"address","key":"Y29fc2lnbmVy","desc":"Co-signer address"},"status":{"keyType":"AVMString","valueType":"AVMUint64","key":"c3RhdHVz","desc":"0=locked 1=released 2=killed"},"creator_approved":{"keyType":"AVMString","valueType":"AVMUint64","key":"Y3JlYXRvcl9hcHByb3ZlZA==","desc":"1 if creator approved"},"cosigner_approved":{"keyType":"AVMString","valueType":"AVMUint64","key":"Y29zaWduZXJfYXBwcm92ZWQ=","desc":"1 if cosigner approved"}},"local":{},"box":{}},"maps":{"global":{},"local":{},"box":{}}},"bareActions":{"create":[],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[322],"errorMessage":"Only creator can delete"},{"pc":[280],"errorMessage":"Only creator can kill"},{"pc":[263],"errorMessage":"Unauthorized approver"},{"pc":[193,286],"errorMessage":"Vault is not locked"},{"pc":[327],"errorMessage":"Vault still locked"},{"pc":[259],"errorMessage":"check self.co_signer exists"},{"pc":[220],"errorMessage":"check self.cosigner_approved exists"},{"pc":[200,275,317],"errorMessage":"check self.creator exists"},{"pc":[211],"errorMessage":"check self.creator_approved exists"},{"pc":[238],"errorMessage":"check self.recipient exists"},{"pc":[191,284,326],"errorMessage":"check self.status exists"},{"pc":[155,163],"errorMessage":"invalid number of bytes for arc4.static_array<arc4.uint8, 32>"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"events":[]} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


/**
 * The argument types for the MultiSigEscrow contract
 */
export type MultiSigEscrowArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'create(address,address)void': {
      recipient: string
      coSigner: string
    }
    'approve()void': Record<string, never>
    'kill()void': Record<string, never>
    'delete()void': Record<string, never>
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'create(address,address)void': [recipient: string, coSigner: string]
    'approve()void': []
    'kill()void': []
    'delete()void': []
  }
}

/**
 * The return type for each method
 */
export type MultiSigEscrowReturns = {
  'create(address,address)void': void
  'approve()void': void
  'kill()void': void
  'delete()void': void
}

/**
 * Defines the types of available calls and state of the MultiSigEscrow smart contract.
 */
export type MultiSigEscrowTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'create(address,address)void' | 'create', {
      argsObj: MultiSigEscrowArgs['obj']['create(address,address)void']
      argsTuple: MultiSigEscrowArgs['tuple']['create(address,address)void']
      returns: MultiSigEscrowReturns['create(address,address)void']
    }>
    & Record<'approve()void' | 'approve', {
      argsObj: MultiSigEscrowArgs['obj']['approve()void']
      argsTuple: MultiSigEscrowArgs['tuple']['approve()void']
      returns: MultiSigEscrowReturns['approve()void']
    }>
    & Record<'kill()void' | 'kill', {
      argsObj: MultiSigEscrowArgs['obj']['kill()void']
      argsTuple: MultiSigEscrowArgs['tuple']['kill()void']
      returns: MultiSigEscrowReturns['kill()void']
    }>
    & Record<'delete()void' | 'delete', {
      argsObj: MultiSigEscrowArgs['obj']['delete()void']
      argsTuple: MultiSigEscrowArgs['tuple']['delete()void']
      returns: MultiSigEscrowReturns['delete()void']
    }>
  /**
   * Defines the shape of the state of the application.
   */
  state: {
    global: {
      keys: {
        /**
         * Vault creator
         */
        creator: string
        /**
         * Fund recipient
         */
        recipient: string
        /**
         * Co-signer address
         */
        coSigner: string
        /**
         * 0=locked 1=released 2=killed
         */
        status: bigint
        /**
         * 1 if creator approved
         */
        creatorApproved: bigint
        /**
         * 1 if cosigner approved
         */
        cosignerApproved: bigint
      }
      maps: {}
    }
  }
}

/**
 * Defines the possible abi call signatures.
 */
export type MultiSigEscrowSignatures = keyof MultiSigEscrowTypes['methods']
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the MultiSigEscrow smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends MultiSigEscrowSignatures> = MultiSigEscrowTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the MultiSigEscrow smart contract to the method's return type
 */
export type MethodReturn<TSignature extends MultiSigEscrowSignatures> = MultiSigEscrowTypes['methods'][TSignature]['returns']

/**
 * Defines the shape of the keyed global state of the application.
 */
export type GlobalKeysState = MultiSigEscrowTypes['state']['global']['keys']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the MultiSigEscrow smart contract
 */
export abstract class MultiSigEscrowParamsFactory {
  /**
   * Constructs a no op call for the approve()void ABI method
   *
   * Record approval from creator or co-signer. Auto-releases when both approve.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static approve(params: CallParams<MultiSigEscrowArgs['obj']['approve()void'] | MultiSigEscrowArgs['tuple']['approve()void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'approve()void' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the kill()void ABI method
   *
   * Creator can kill the vault and reclaim funds.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static kill(params: CallParams<MultiSigEscrowArgs['obj']['kill()void'] | MultiSigEscrowArgs['tuple']['kill()void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'kill()void' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
}

/**
 * A client to make calls to the MultiSigEscrow smart contract
 */
export class MultiSigEscrowClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `MultiSigEscrowClient`
   *
   * @param appClient An `AppClient` instance which has been created with the MultiSigEscrow app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `MultiSigEscrowClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Returns a new `MultiSigEscrowClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<MultiSigEscrowClient> {
    return new MultiSigEscrowClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `MultiSigEscrowClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<MultiSigEscrowClient> {
    return new MultiSigEscrowClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Makes a clear_state call to an existing instance of the MultiSigEscrow smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the MultiSigEscrow smart contract using the `approve()void` ABI method.
     *
     * Record approval from creator or co-signer. Auto-releases when both approve.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    approve: (params: CallParams<MultiSigEscrowArgs['obj']['approve()void'] | MultiSigEscrowArgs['tuple']['approve()void']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(MultiSigEscrowParamsFactory.approve(params))
    },

    /**
     * Makes a call to the MultiSigEscrow smart contract using the `kill()void` ABI method.
     *
     * Creator can kill the vault and reclaim funds.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    kill: (params: CallParams<MultiSigEscrowArgs['obj']['kill()void'] | MultiSigEscrowArgs['tuple']['kill()void']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(MultiSigEscrowParamsFactory.kill(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Makes a clear_state call to an existing instance of the MultiSigEscrow smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the MultiSigEscrow smart contract using the `approve()void` ABI method.
     *
     * Record approval from creator or co-signer. Auto-releases when both approve.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    approve: (params: CallParams<MultiSigEscrowArgs['obj']['approve()void'] | MultiSigEscrowArgs['tuple']['approve()void']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(MultiSigEscrowParamsFactory.approve(params))
    },

    /**
     * Makes a call to the MultiSigEscrow smart contract using the `kill()void` ABI method.
     *
     * Creator can kill the vault and reclaim funds.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    kill: (params: CallParams<MultiSigEscrowArgs['obj']['kill()void'] | MultiSigEscrowArgs['tuple']['kill()void']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(MultiSigEscrowParamsFactory.kill(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Makes a clear_state call to an existing instance of the MultiSigEscrow smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the MultiSigEscrow smart contract using the `approve()void` ABI method.
     *
     * Record approval from creator or co-signer. Auto-releases when both approve.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    approve: async (params: CallParams<MultiSigEscrowArgs['obj']['approve()void'] | MultiSigEscrowArgs['tuple']['approve()void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(MultiSigEscrowParamsFactory.approve(params))
      return {...result, return: result.return as unknown as (undefined | MultiSigEscrowReturns['approve()void'])}
    },

    /**
     * Makes a call to the MultiSigEscrow smart contract using the `kill()void` ABI method.
     *
     * Creator can kill the vault and reclaim funds.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    kill: async (params: CallParams<MultiSigEscrowArgs['obj']['kill()void'] | MultiSigEscrowArgs['tuple']['kill()void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(MultiSigEscrowParamsFactory.kill(params))
      return {...result, return: result.return as unknown as (undefined | MultiSigEscrowReturns['kill()void'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new MultiSigEscrowClient(this.appClient.clone(params))
  }

  /**
   * Methods to access state for the current MultiSigEscrow app
   */
  state = {
    /**
     * Methods to access global state for the current MultiSigEscrow app
     */
    global: {
      /**
       * Get all current keyed values from global state
       */
      getAll: async (): Promise<Partial<Expand<GlobalKeysState>>> => {
        const result = await this.appClient.state.global.getAll()
        return {
          creator: result.creator,
          recipient: result.recipient,
          coSigner: result.co_signer,
          status: result.status,
          creatorApproved: result.creator_approved,
          cosignerApproved: result.cosigner_approved,
        }
      },
      /**
       * Get the current value of the creator key in global state
       */
      creator: async (): Promise<string | undefined> => { return (await this.appClient.state.global.getValue("creator")) as string | undefined },
      /**
       * Get the current value of the recipient key in global state
       */
      recipient: async (): Promise<string | undefined> => { return (await this.appClient.state.global.getValue("recipient")) as string | undefined },
      /**
       * Get the current value of the co_signer key in global state
       */
      coSigner: async (): Promise<string | undefined> => { return (await this.appClient.state.global.getValue("co_signer")) as string | undefined },
      /**
       * Get the current value of the status key in global state
       */
      status: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("status")) as bigint | undefined },
      /**
       * Get the current value of the creator_approved key in global state
       */
      creatorApproved: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("creator_approved")) as bigint | undefined },
      /**
       * Get the current value of the cosigner_approved key in global state
       */
      cosignerApproved: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("cosigner_approved")) as bigint | undefined },
    },
  }

  public newGroup(): MultiSigEscrowComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a approve()void method call against the MultiSigEscrow contract
       */
      approve(params: CallParams<MultiSigEscrowArgs['obj']['approve()void'] | MultiSigEscrowArgs['tuple']['approve()void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.approve(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a kill()void method call against the MultiSigEscrow contract
       */
      kill(params: CallParams<MultiSigEscrowArgs['obj']['kill()void'] | MultiSigEscrowArgs['tuple']['kill()void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.kill(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a clear state call to the MultiSigEscrow contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as MultiSigEscrowComposer
  }
}
export type MultiSigEscrowComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the approve()void ABI method.
   *
   * Record approval from creator or co-signer. Auto-releases when both approve.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  approve(params?: CallParams<MultiSigEscrowArgs['obj']['approve()void'] | MultiSigEscrowArgs['tuple']['approve()void']>): MultiSigEscrowComposer<[...TReturns, MultiSigEscrowReturns['approve()void'] | undefined]>

  /**
   * Calls the kill()void ABI method.
   *
   * Creator can kill the vault and reclaim funds.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  kill(params?: CallParams<MultiSigEscrowArgs['obj']['kill()void'] | MultiSigEscrowArgs['tuple']['kill()void']>): MultiSigEscrowComposer<[...TReturns, MultiSigEscrowReturns['kill()void'] | undefined]>

  /**
   * Makes a clear_state call to an existing instance of the MultiSigEscrow smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): MultiSigEscrowComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): MultiSigEscrowComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<MultiSigEscrowComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<MultiSigEscrowComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<MultiSigEscrowComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<MultiSigEscrowComposerResults<TReturns>>
}
export type MultiSigEscrowComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

