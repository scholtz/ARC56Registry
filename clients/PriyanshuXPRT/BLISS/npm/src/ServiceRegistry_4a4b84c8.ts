/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"ServiceRegistry","structs":{"ServiceListing":[{"name":"provider","type":"address"},{"name":"priceMicroalgos","type":"uint64"},{"name":"cycleDays","type":"uint64"},{"name":"name","type":"string"}]},"methods":[{"name":"create","args":[{"type":"uint64","name":"fee_micro"}],"returns":{"type":"void"},"actions":{"create":["NoOp"],"call":[]},"readonly":false,"desc":"Initialize the registry. Sender becomes admin.","events":[],"recommendations":{}},{"name":"register","args":[{"type":"string","name":"service_id"},{"type":"uint64","name":"price_microalgos"},{"type":"uint64","name":"cycle_days"},{"type":"string","name":"name"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Register or update a service. Caller must have paid `fee_micro`\nin a grouped Payment txn (verified by min-balance accounting). Only the original provider can re-register the same service_id.","events":[],"recommendations":{}},{"name":"deregister","args":[{"type":"string","name":"service_id"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Provider removes their own listing.","events":[],"recommendations":{}},{"name":"get_listing","args":[{"type":"string","name":"service_id"}],"returns":{"type":"(address,uint64,uint64,string)","struct":"ServiceListing"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"desc":"Read-only lookup used by agents during discovery.","events":[],"recommendations":{}},{"name":"get_count","args":[],"returns":{"type":"uint64"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"desc":"Number of registered services.","events":[],"recommendations":{}}],"arcs":[22,28],"desc":"\n    A2A Service Registry — autonomous agents discover services here.\n\n    Implements the discovery half of the A2A Agentic Commerce Framework\n    (Track: Agentic Commerce → Advanced #7).\n\n    Each registered service is stored in its OWN box (Box Storage), so the\n    registry scales to thousands of services without bumping global schema.\n\n    State:\n        Box \"svc:<service_id>\"  → ServiceListing (provider, price, cycle, name)\n        GlobalState.admin       → registry admin (can prune stale entries)\n        GlobalState.fee_micro   → registration fee (anti-spam)\n        GlobalState.count       → total registered services\n\n    Anyone can register a service by paying `fee_micro` ALGO; only the\n    provider can update or delete their own entry.\n    ","networks":{},"state":{"schema":{"global":{"ints":2,"bytes":1},"local":{"ints":0,"bytes":0}},"keys":{"global":{"admin":{"keyType":"AVMString","valueType":"address","key":"YWRtaW4=","desc":"Registry admin"},"fee_micro":{"keyType":"AVMString","valueType":"AVMUint64","key":"ZmVlX21pY3Jv","desc":"Registration fee in microalgos"},"count":{"keyType":"AVMString","valueType":"AVMUint64","key":"Y291bnQ=","desc":"Total registered services"}},"local":{},"box":{}},"maps":{"global":{},"local":{},"box":{"listings":{"keyType":"string","valueType":"ServiceListing","prefix":"c3ZjOg=="}}}},"bareActions":{"create":[],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[188],"errorMessage":"Only original provider can update this service"},{"pc":[270],"errorMessage":"Only provider or admin can deregister"},{"pc":[247,309],"errorMessage":"Service not found"},{"pc":[264],"errorMessage":"check self.admin exists"},{"pc":[217,277,321],"errorMessage":"check self.count exists"},{"pc":[186,254],"errorMessage":"index out of bounds"},{"pc":[126,162,231,294],"errorMessage":"invalid array length header"},{"pc":[133,168,238,301],"errorMessage":"invalid number of bytes for arc4.dynamic_array<arc4.uint8>"},{"pc":[97,143,153],"errorMessage":"invalid number of bytes for arc4.uint64"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"events":[]} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


// Type definitions for ARC-56 structs

export type ServiceListing = {
  provider: string,
  priceMicroalgos: bigint,
  cycleDays: bigint,
  name: string
}


/**
 * Converts the ABI tuple representation of a ServiceListing to the struct representation
 */
export function ServiceListingFromTuple(abiTuple: [string, bigint, bigint, string]) {
  return getABIStructFromABITuple(abiTuple, APP_SPEC.structs.ServiceListing, APP_SPEC.structs) as ServiceListing
}

/**
 * The argument types for the ServiceRegistry contract
 */
export type ServiceRegistryArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'create(uint64)void': {
      feeMicro: bigint | number
    }
    'register(string,uint64,uint64,string)void': {
      serviceId: string
      priceMicroalgos: bigint | number
      cycleDays: bigint | number
      name: string
    }
    'deregister(string)void': {
      serviceId: string
    }
    'get_listing(string)(address,uint64,uint64,string)': {
      serviceId: string
    }
    'get_count()uint64': Record<string, never>
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'create(uint64)void': [feeMicro: bigint | number]
    'register(string,uint64,uint64,string)void': [serviceId: string, priceMicroalgos: bigint | number, cycleDays: bigint | number, name: string]
    'deregister(string)void': [serviceId: string]
    'get_listing(string)(address,uint64,uint64,string)': [serviceId: string]
    'get_count()uint64': []
  }
}

/**
 * The return type for each method
 */
export type ServiceRegistryReturns = {
  'create(uint64)void': void
  'register(string,uint64,uint64,string)void': void
  'deregister(string)void': void
  'get_listing(string)(address,uint64,uint64,string)': ServiceListing
  'get_count()uint64': bigint
}

/**
 * Defines the types of available calls and state of the ServiceRegistry smart contract.
 */
export type ServiceRegistryTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'create(uint64)void' | 'create', {
      argsObj: ServiceRegistryArgs['obj']['create(uint64)void']
      argsTuple: ServiceRegistryArgs['tuple']['create(uint64)void']
      returns: ServiceRegistryReturns['create(uint64)void']
    }>
    & Record<'register(string,uint64,uint64,string)void' | 'register', {
      argsObj: ServiceRegistryArgs['obj']['register(string,uint64,uint64,string)void']
      argsTuple: ServiceRegistryArgs['tuple']['register(string,uint64,uint64,string)void']
      returns: ServiceRegistryReturns['register(string,uint64,uint64,string)void']
    }>
    & Record<'deregister(string)void' | 'deregister', {
      argsObj: ServiceRegistryArgs['obj']['deregister(string)void']
      argsTuple: ServiceRegistryArgs['tuple']['deregister(string)void']
      returns: ServiceRegistryReturns['deregister(string)void']
    }>
    & Record<'get_listing(string)(address,uint64,uint64,string)' | 'get_listing', {
      argsObj: ServiceRegistryArgs['obj']['get_listing(string)(address,uint64,uint64,string)']
      argsTuple: ServiceRegistryArgs['tuple']['get_listing(string)(address,uint64,uint64,string)']
      returns: ServiceRegistryReturns['get_listing(string)(address,uint64,uint64,string)']
    }>
    & Record<'get_count()uint64' | 'get_count', {
      argsObj: ServiceRegistryArgs['obj']['get_count()uint64']
      argsTuple: ServiceRegistryArgs['tuple']['get_count()uint64']
      returns: ServiceRegistryReturns['get_count()uint64']
    }>
  /**
   * Defines the shape of the state of the application.
   */
  state: {
    global: {
      keys: {
        /**
         * Registry admin
         */
        admin: string
        /**
         * Registration fee in microalgos
         */
        feeMicro: bigint
        /**
         * Total registered services
         */
        count: bigint
      }
      maps: {}
    }
    box: {
      keys: {}
      maps: {
        listings: Map<string, ServiceListing>
      }
    }
  }
}

/**
 * Defines the possible abi call signatures.
 */
export type ServiceRegistrySignatures = keyof ServiceRegistryTypes['methods']
/**
 * Defines the possible abi call signatures for methods that return a non-void value.
 */
export type ServiceRegistryNonVoidMethodSignatures = keyof ServiceRegistryTypes['methods'] extends infer T ? T extends keyof ServiceRegistryTypes['methods'] ? MethodReturn<T> extends void ? never : T  : never : never
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the ServiceRegistry smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends ServiceRegistrySignatures> = ServiceRegistryTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the ServiceRegistry smart contract to the method's return type
 */
export type MethodReturn<TSignature extends ServiceRegistrySignatures> = ServiceRegistryTypes['methods'][TSignature]['returns']

/**
 * Defines the shape of the keyed global state of the application.
 */
export type GlobalKeysState = ServiceRegistryTypes['state']['global']['keys']

/**
 * Defines the shape of the keyed box state of the application.
 */
export type BoxKeysState = ServiceRegistryTypes['state']['box']['keys']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the ServiceRegistry smart contract
 */
export abstract class ServiceRegistryParamsFactory {
  /**
   * Constructs a no op call for the register(string,uint64,uint64,string)void ABI method
   *
  * Register or update a service. Caller must have paid `fee_micro`
  in a grouped Payment txn (verified by min-balance accounting). Only the original provider can re-register the same service_id.

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static register(params: CallParams<ServiceRegistryArgs['obj']['register(string,uint64,uint64,string)void'] | ServiceRegistryArgs['tuple']['register(string,uint64,uint64,string)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'register(string,uint64,uint64,string)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.serviceId, params.args.priceMicroalgos, params.args.cycleDays, params.args.name],
    }
  }
  /**
   * Constructs a no op call for the deregister(string)void ABI method
   *
   * Provider removes their own listing.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static deregister(params: CallParams<ServiceRegistryArgs['obj']['deregister(string)void'] | ServiceRegistryArgs['tuple']['deregister(string)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'deregister(string)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.serviceId],
    }
  }
  /**
   * Constructs a no op call for the get_listing(string)(address,uint64,uint64,string) ABI method
   *
   * Read-only lookup used by agents during discovery.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static getListing(params: CallParams<ServiceRegistryArgs['obj']['get_listing(string)(address,uint64,uint64,string)'] | ServiceRegistryArgs['tuple']['get_listing(string)(address,uint64,uint64,string)']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'get_listing(string)(address,uint64,uint64,string)' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.serviceId],
    }
  }
  /**
   * Constructs a no op call for the get_count()uint64 ABI method
   *
   * Number of registered services.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static getCount(params: CallParams<ServiceRegistryArgs['obj']['get_count()uint64'] | ServiceRegistryArgs['tuple']['get_count()uint64']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'get_count()uint64' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
}

/**
 * A client to make calls to the ServiceRegistry smart contract
 */
export class ServiceRegistryClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `ServiceRegistryClient`
   *
   * @param appClient An `AppClient` instance which has been created with the ServiceRegistry app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `ServiceRegistryClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Checks for decode errors on the given return value and maps the return value to the return type for the given method
   * @returns The typed return value or undefined if there was no value
   */
  decodeReturnValue<TSignature extends ServiceRegistryNonVoidMethodSignatures>(method: TSignature, returnValue: ABIReturn | undefined) {
    return returnValue !== undefined ? getArc56ReturnValue<MethodReturn<TSignature>>(returnValue, this.appClient.getABIMethod(method), APP_SPEC.structs) : undefined
  }

  /**
   * Returns a new `ServiceRegistryClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<ServiceRegistryClient> {
    return new ServiceRegistryClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `ServiceRegistryClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<ServiceRegistryClient> {
    return new ServiceRegistryClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Makes a clear_state call to an existing instance of the ServiceRegistry smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the ServiceRegistry smart contract using the `register(string,uint64,uint64,string)void` ABI method.
     *
    * Register or update a service. Caller must have paid `fee_micro`
    in a grouped Payment txn (verified by min-balance accounting). Only the original provider can re-register the same service_id.

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    register: (params: CallParams<ServiceRegistryArgs['obj']['register(string,uint64,uint64,string)void'] | ServiceRegistryArgs['tuple']['register(string,uint64,uint64,string)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(ServiceRegistryParamsFactory.register(params))
    },

    /**
     * Makes a call to the ServiceRegistry smart contract using the `deregister(string)void` ABI method.
     *
     * Provider removes their own listing.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    deregister: (params: CallParams<ServiceRegistryArgs['obj']['deregister(string)void'] | ServiceRegistryArgs['tuple']['deregister(string)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(ServiceRegistryParamsFactory.deregister(params))
    },

    /**
     * Makes a call to the ServiceRegistry smart contract using the `get_listing(string)(address,uint64,uint64,string)` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * Read-only lookup used by agents during discovery.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    getListing: (params: CallParams<ServiceRegistryArgs['obj']['get_listing(string)(address,uint64,uint64,string)'] | ServiceRegistryArgs['tuple']['get_listing(string)(address,uint64,uint64,string)']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(ServiceRegistryParamsFactory.getListing(params))
    },

    /**
     * Makes a call to the ServiceRegistry smart contract using the `get_count()uint64` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * Number of registered services.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    getCount: (params: CallParams<ServiceRegistryArgs['obj']['get_count()uint64'] | ServiceRegistryArgs['tuple']['get_count()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(ServiceRegistryParamsFactory.getCount(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Makes a clear_state call to an existing instance of the ServiceRegistry smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the ServiceRegistry smart contract using the `register(string,uint64,uint64,string)void` ABI method.
     *
    * Register or update a service. Caller must have paid `fee_micro`
    in a grouped Payment txn (verified by min-balance accounting). Only the original provider can re-register the same service_id.

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    register: (params: CallParams<ServiceRegistryArgs['obj']['register(string,uint64,uint64,string)void'] | ServiceRegistryArgs['tuple']['register(string,uint64,uint64,string)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(ServiceRegistryParamsFactory.register(params))
    },

    /**
     * Makes a call to the ServiceRegistry smart contract using the `deregister(string)void` ABI method.
     *
     * Provider removes their own listing.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    deregister: (params: CallParams<ServiceRegistryArgs['obj']['deregister(string)void'] | ServiceRegistryArgs['tuple']['deregister(string)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(ServiceRegistryParamsFactory.deregister(params))
    },

    /**
     * Makes a call to the ServiceRegistry smart contract using the `get_listing(string)(address,uint64,uint64,string)` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * Read-only lookup used by agents during discovery.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    getListing: (params: CallParams<ServiceRegistryArgs['obj']['get_listing(string)(address,uint64,uint64,string)'] | ServiceRegistryArgs['tuple']['get_listing(string)(address,uint64,uint64,string)']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(ServiceRegistryParamsFactory.getListing(params))
    },

    /**
     * Makes a call to the ServiceRegistry smart contract using the `get_count()uint64` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * Number of registered services.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    getCount: (params: CallParams<ServiceRegistryArgs['obj']['get_count()uint64'] | ServiceRegistryArgs['tuple']['get_count()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(ServiceRegistryParamsFactory.getCount(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Makes a clear_state call to an existing instance of the ServiceRegistry smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the ServiceRegistry smart contract using the `register(string,uint64,uint64,string)void` ABI method.
     *
    * Register or update a service. Caller must have paid `fee_micro`
    in a grouped Payment txn (verified by min-balance accounting). Only the original provider can re-register the same service_id.

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    register: async (params: CallParams<ServiceRegistryArgs['obj']['register(string,uint64,uint64,string)void'] | ServiceRegistryArgs['tuple']['register(string,uint64,uint64,string)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(ServiceRegistryParamsFactory.register(params))
      return {...result, return: result.return as unknown as (undefined | ServiceRegistryReturns['register(string,uint64,uint64,string)void'])}
    },

    /**
     * Makes a call to the ServiceRegistry smart contract using the `deregister(string)void` ABI method.
     *
     * Provider removes their own listing.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    deregister: async (params: CallParams<ServiceRegistryArgs['obj']['deregister(string)void'] | ServiceRegistryArgs['tuple']['deregister(string)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(ServiceRegistryParamsFactory.deregister(params))
      return {...result, return: result.return as unknown as (undefined | ServiceRegistryReturns['deregister(string)void'])}
    },

    /**
     * Makes a call to the ServiceRegistry smart contract using the `get_listing(string)(address,uint64,uint64,string)` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * Read-only lookup used by agents during discovery.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    getListing: async (params: CallParams<ServiceRegistryArgs['obj']['get_listing(string)(address,uint64,uint64,string)'] | ServiceRegistryArgs['tuple']['get_listing(string)(address,uint64,uint64,string)']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(ServiceRegistryParamsFactory.getListing(params))
      return {...result, return: result.return as unknown as (undefined | ServiceRegistryReturns['get_listing(string)(address,uint64,uint64,string)'])}
    },

    /**
     * Makes a call to the ServiceRegistry smart contract using the `get_count()uint64` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * Number of registered services.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    getCount: async (params: CallParams<ServiceRegistryArgs['obj']['get_count()uint64'] | ServiceRegistryArgs['tuple']['get_count()uint64']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(ServiceRegistryParamsFactory.getCount(params))
      return {...result, return: result.return as unknown as (undefined | ServiceRegistryReturns['get_count()uint64'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new ServiceRegistryClient(this.appClient.clone(params))
  }

  /**
   * Makes a readonly (simulated) call to the ServiceRegistry smart contract using the `get_listing(string)(address,uint64,uint64,string)` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * Read-only lookup used by agents during discovery.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async getListing(params: CallParams<ServiceRegistryArgs['obj']['get_listing(string)(address,uint64,uint64,string)'] | ServiceRegistryArgs['tuple']['get_listing(string)(address,uint64,uint64,string)']>) {
    const result = await this.appClient.send.call(ServiceRegistryParamsFactory.getListing(params))
    return result.return as unknown as ServiceRegistryReturns['get_listing(string)(address,uint64,uint64,string)']
  }

  /**
   * Makes a readonly (simulated) call to the ServiceRegistry smart contract using the `get_count()uint64` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * Number of registered services.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async getCount(params: CallParams<ServiceRegistryArgs['obj']['get_count()uint64'] | ServiceRegistryArgs['tuple']['get_count()uint64']> = {args: []}) {
    const result = await this.appClient.send.call(ServiceRegistryParamsFactory.getCount(params))
    return result.return as unknown as ServiceRegistryReturns['get_count()uint64']
  }

  /**
   * Methods to access state for the current ServiceRegistry app
   */
  state = {
    /**
     * Methods to access global state for the current ServiceRegistry app
     */
    global: {
      /**
       * Get all current keyed values from global state
       */
      getAll: async (): Promise<Partial<Expand<GlobalKeysState>>> => {
        const result = await this.appClient.state.global.getAll()
        return {
          admin: result.admin,
          feeMicro: result.fee_micro,
          count: result.count,
        }
      },
      /**
       * Get the current value of the admin key in global state
       */
      admin: async (): Promise<string | undefined> => { return (await this.appClient.state.global.getValue("admin")) as string | undefined },
      /**
       * Get the current value of the fee_micro key in global state
       */
      feeMicro: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("fee_micro")) as bigint | undefined },
      /**
       * Get the current value of the count key in global state
       */
      count: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("count")) as bigint | undefined },
    },
    /**
     * Methods to access box state for the current ServiceRegistry app
     */
    box: {
      /**
       * Get all current keyed values from box state
       */
      getAll: async (): Promise<Partial<Expand<BoxKeysState>>> => {
        const result = await this.appClient.state.box.getAll()
        return {
        }
      },
      /**
       * Get values from the listings map in box state
       */
      listings: {
        /**
         * Get all current values of the listings map in box state
         */
        getMap: async (): Promise<Map<string, ServiceListing>> => { return (await this.appClient.state.box.getMap("listings")) as Map<string, ServiceListing> },
        /**
         * Get a current value of the listings map by key from box state
         */
        value: async (key: string): Promise<ServiceListing | undefined> => { return await this.appClient.state.box.getMapValue("listings", key) as ServiceListing | undefined },
      },
    },
  }

  public newGroup(): ServiceRegistryComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a register(string,uint64,uint64,string)void method call against the ServiceRegistry contract
       */
      register(params: CallParams<ServiceRegistryArgs['obj']['register(string,uint64,uint64,string)void'] | ServiceRegistryArgs['tuple']['register(string,uint64,uint64,string)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.register(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a deregister(string)void method call against the ServiceRegistry contract
       */
      deregister(params: CallParams<ServiceRegistryArgs['obj']['deregister(string)void'] | ServiceRegistryArgs['tuple']['deregister(string)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.deregister(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a get_listing(string)(address,uint64,uint64,string) method call against the ServiceRegistry contract
       */
      getListing(params: CallParams<ServiceRegistryArgs['obj']['get_listing(string)(address,uint64,uint64,string)'] | ServiceRegistryArgs['tuple']['get_listing(string)(address,uint64,uint64,string)']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.getListing(params)))
        resultMappers.push((v) => client.decodeReturnValue('get_listing(string)(address,uint64,uint64,string)', v))
        return this
      },
      /**
       * Add a get_count()uint64 method call against the ServiceRegistry contract
       */
      getCount(params: CallParams<ServiceRegistryArgs['obj']['get_count()uint64'] | ServiceRegistryArgs['tuple']['get_count()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.getCount(params)))
        resultMappers.push((v) => client.decodeReturnValue('get_count()uint64', v))
        return this
      },
      /**
       * Add a clear state call to the ServiceRegistry contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as ServiceRegistryComposer
  }
}
export type ServiceRegistryComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the register(string,uint64,uint64,string)void ABI method.
   *
  * Register or update a service. Caller must have paid `fee_micro`
  in a grouped Payment txn (verified by min-balance accounting). Only the original provider can re-register the same service_id.

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  register(params?: CallParams<ServiceRegistryArgs['obj']['register(string,uint64,uint64,string)void'] | ServiceRegistryArgs['tuple']['register(string,uint64,uint64,string)void']>): ServiceRegistryComposer<[...TReturns, ServiceRegistryReturns['register(string,uint64,uint64,string)void'] | undefined]>

  /**
   * Calls the deregister(string)void ABI method.
   *
   * Provider removes their own listing.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  deregister(params?: CallParams<ServiceRegistryArgs['obj']['deregister(string)void'] | ServiceRegistryArgs['tuple']['deregister(string)void']>): ServiceRegistryComposer<[...TReturns, ServiceRegistryReturns['deregister(string)void'] | undefined]>

  /**
   * Calls the get_listing(string)(address,uint64,uint64,string) ABI method.
   *
   * Read-only lookup used by agents during discovery.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  getListing(params?: CallParams<ServiceRegistryArgs['obj']['get_listing(string)(address,uint64,uint64,string)'] | ServiceRegistryArgs['tuple']['get_listing(string)(address,uint64,uint64,string)']>): ServiceRegistryComposer<[...TReturns, ServiceRegistryReturns['get_listing(string)(address,uint64,uint64,string)'] | undefined]>

  /**
   * Calls the get_count()uint64 ABI method.
   *
   * Number of registered services.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  getCount(params?: CallParams<ServiceRegistryArgs['obj']['get_count()uint64'] | ServiceRegistryArgs['tuple']['get_count()uint64']>): ServiceRegistryComposer<[...TReturns, ServiceRegistryReturns['get_count()uint64'] | undefined]>

  /**
   * Makes a clear_state call to an existing instance of the ServiceRegistry smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): ServiceRegistryComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): ServiceRegistryComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<ServiceRegistryComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<ServiceRegistryComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<ServiceRegistryComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<ServiceRegistryComposerResults<TReturns>>
}
export type ServiceRegistryComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

