/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"ASAEscrow","structs":{},"methods":[{"name":"create","args":[{"type":"address","name":"recipient"},{"type":"uint64","name":"asa_id"}],"returns":{"type":"void"},"actions":{"create":["NoOp"],"call":[]},"readonly":false,"desc":"Deploy the ASA vault and record creator, recipient, and asset.","events":[],"recommendations":{}},{"name":"optin","args":[],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Opt the application account into the ASA so it can receive tokens.\nMust be called (and funded) by creator before transferring tokens.","events":[],"recommendations":{}},{"name":"release","args":[],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Transfer all held ASA tokens to recipient. Only callable by creator.","events":[],"recommendations":{}},{"name":"kill","args":[],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Return all held ASA tokens to creator. Only callable by creator.","events":[],"recommendations":{}},{"name":"delete","args":[],"returns":{"type":"void"},"actions":{"create":[],"call":["DeleteApplication"]},"readonly":false,"desc":"Delete the application after release or kill.","events":[],"recommendations":{}}],"arcs":[22,28],"desc":"\n    Algorand Standard Asset (ASA / Token) Escrow Vault (ARC-4 compliant).\n\n    Handles escrow of ASA tokens instead of native ALGO. The application\n    must opt in to the ASA before tokens can be transferred to it.\n\n    GlobalState:\n        creator    — account that deployed the vault\n        recipient  — account that receives tokens on release\n        asa_id     — ASA / token ID being held in escrow\n        status     — 0 = locked, 1 = released, 2 = killed\n    ","networks":{},"state":{"schema":{"global":{"ints":2,"bytes":2},"local":{"ints":0,"bytes":0}},"keys":{"global":{"creator":{"keyType":"AVMString","valueType":"address","key":"Y3JlYXRvcg==","desc":"Vault creator"},"recipient":{"keyType":"AVMString","valueType":"address","key":"cmVjaXBpZW50","desc":"Token recipient"},"asa_id":{"keyType":"AVMString","valueType":"AVMUint64","key":"YXNhX2lk","desc":"ASA ID held in escrow"},"status":{"keyType":"AVMString","valueType":"AVMUint64","key":"c3RhdHVz","desc":"0=locked 1=released 2=killed"}},"local":{},"box":{}},"maps":{"global":{},"local":{},"box":{}}},"bareActions":{"create":[],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[296],"errorMessage":"Only creator can delete"},{"pc":[244],"errorMessage":"Only creator can kill"},{"pc":[159],"errorMessage":"Only creator can opt in"},{"pc":[190],"errorMessage":"Only creator can release"},{"pc":[196,250],"errorMessage":"Vault is not locked"},{"pc":[301],"errorMessage":"Vault still locked"},{"pc":[163,200,254],"errorMessage":"check self.asa_id exists"},{"pc":[157,188,239,291],"errorMessage":"check self.creator exists"},{"pc":[212],"errorMessage":"check self.recipient exists"},{"pc":[194,248,300],"errorMessage":"check self.status exists"},{"pc":[125],"errorMessage":"invalid number of bytes for arc4.static_array<arc4.uint8, 32>"},{"pc":[134],"errorMessage":"invalid number of bytes for arc4.uint64"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"events":[]} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


/**
 * The argument types for the AsaEscrow contract
 */
export type AsaEscrowArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'create(address,uint64)void': {
      recipient: string
      asaId: bigint | number
    }
    'optin()void': Record<string, never>
    'release()void': Record<string, never>
    'kill()void': Record<string, never>
    'delete()void': Record<string, never>
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'create(address,uint64)void': [recipient: string, asaId: bigint | number]
    'optin()void': []
    'release()void': []
    'kill()void': []
    'delete()void': []
  }
}

/**
 * The return type for each method
 */
export type AsaEscrowReturns = {
  'create(address,uint64)void': void
  'optin()void': void
  'release()void': void
  'kill()void': void
  'delete()void': void
}

/**
 * Defines the types of available calls and state of the AsaEscrow smart contract.
 */
export type AsaEscrowTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'create(address,uint64)void' | 'create', {
      argsObj: AsaEscrowArgs['obj']['create(address,uint64)void']
      argsTuple: AsaEscrowArgs['tuple']['create(address,uint64)void']
      returns: AsaEscrowReturns['create(address,uint64)void']
    }>
    & Record<'optin()void' | 'optin', {
      argsObj: AsaEscrowArgs['obj']['optin()void']
      argsTuple: AsaEscrowArgs['tuple']['optin()void']
      returns: AsaEscrowReturns['optin()void']
    }>
    & Record<'release()void' | 'release', {
      argsObj: AsaEscrowArgs['obj']['release()void']
      argsTuple: AsaEscrowArgs['tuple']['release()void']
      returns: AsaEscrowReturns['release()void']
    }>
    & Record<'kill()void' | 'kill', {
      argsObj: AsaEscrowArgs['obj']['kill()void']
      argsTuple: AsaEscrowArgs['tuple']['kill()void']
      returns: AsaEscrowReturns['kill()void']
    }>
    & Record<'delete()void' | 'delete', {
      argsObj: AsaEscrowArgs['obj']['delete()void']
      argsTuple: AsaEscrowArgs['tuple']['delete()void']
      returns: AsaEscrowReturns['delete()void']
    }>
  /**
   * Defines the shape of the state of the application.
   */
  state: {
    global: {
      keys: {
        /**
         * Vault creator
         */
        creator: string
        /**
         * Token recipient
         */
        recipient: string
        /**
         * ASA ID held in escrow
         */
        asaId: bigint
        /**
         * 0=locked 1=released 2=killed
         */
        status: bigint
      }
      maps: {}
    }
  }
}

/**
 * Defines the possible abi call signatures.
 */
export type AsaEscrowSignatures = keyof AsaEscrowTypes['methods']
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the AsaEscrow smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends AsaEscrowSignatures> = AsaEscrowTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the AsaEscrow smart contract to the method's return type
 */
export type MethodReturn<TSignature extends AsaEscrowSignatures> = AsaEscrowTypes['methods'][TSignature]['returns']

/**
 * Defines the shape of the keyed global state of the application.
 */
export type GlobalKeysState = AsaEscrowTypes['state']['global']['keys']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the AsaEscrow smart contract
 */
export abstract class AsaEscrowParamsFactory {
  /**
   * Constructs a no op call for the optin()void ABI method
   *
  * Opt the application account into the ASA so it can receive tokens.
  Must be called (and funded) by creator before transferring tokens.

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static optin(params: CallParams<AsaEscrowArgs['obj']['optin()void'] | AsaEscrowArgs['tuple']['optin()void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'optin()void' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the release()void ABI method
   *
   * Transfer all held ASA tokens to recipient. Only callable by creator.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static release(params: CallParams<AsaEscrowArgs['obj']['release()void'] | AsaEscrowArgs['tuple']['release()void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'release()void' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the kill()void ABI method
   *
   * Return all held ASA tokens to creator. Only callable by creator.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static kill(params: CallParams<AsaEscrowArgs['obj']['kill()void'] | AsaEscrowArgs['tuple']['kill()void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'kill()void' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
}

/**
 * A client to make calls to the ASAEscrow smart contract
 */
export class AsaEscrowClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `AsaEscrowClient`
   *
   * @param appClient An `AppClient` instance which has been created with the AsaEscrow app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `AsaEscrowClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Returns a new `AsaEscrowClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<AsaEscrowClient> {
    return new AsaEscrowClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `AsaEscrowClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<AsaEscrowClient> {
    return new AsaEscrowClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Makes a clear_state call to an existing instance of the ASAEscrow smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the ASAEscrow smart contract using the `optin()void` ABI method.
     *
    * Opt the application account into the ASA so it can receive tokens.
    Must be called (and funded) by creator before transferring tokens.

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    optin: (params: CallParams<AsaEscrowArgs['obj']['optin()void'] | AsaEscrowArgs['tuple']['optin()void']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(AsaEscrowParamsFactory.optin(params))
    },

    /**
     * Makes a call to the ASAEscrow smart contract using the `release()void` ABI method.
     *
     * Transfer all held ASA tokens to recipient. Only callable by creator.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    release: (params: CallParams<AsaEscrowArgs['obj']['release()void'] | AsaEscrowArgs['tuple']['release()void']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(AsaEscrowParamsFactory.release(params))
    },

    /**
     * Makes a call to the ASAEscrow smart contract using the `kill()void` ABI method.
     *
     * Return all held ASA tokens to creator. Only callable by creator.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    kill: (params: CallParams<AsaEscrowArgs['obj']['kill()void'] | AsaEscrowArgs['tuple']['kill()void']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(AsaEscrowParamsFactory.kill(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Makes a clear_state call to an existing instance of the ASAEscrow smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the ASAEscrow smart contract using the `optin()void` ABI method.
     *
    * Opt the application account into the ASA so it can receive tokens.
    Must be called (and funded) by creator before transferring tokens.

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    optin: (params: CallParams<AsaEscrowArgs['obj']['optin()void'] | AsaEscrowArgs['tuple']['optin()void']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(AsaEscrowParamsFactory.optin(params))
    },

    /**
     * Makes a call to the ASAEscrow smart contract using the `release()void` ABI method.
     *
     * Transfer all held ASA tokens to recipient. Only callable by creator.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    release: (params: CallParams<AsaEscrowArgs['obj']['release()void'] | AsaEscrowArgs['tuple']['release()void']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(AsaEscrowParamsFactory.release(params))
    },

    /**
     * Makes a call to the ASAEscrow smart contract using the `kill()void` ABI method.
     *
     * Return all held ASA tokens to creator. Only callable by creator.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    kill: (params: CallParams<AsaEscrowArgs['obj']['kill()void'] | AsaEscrowArgs['tuple']['kill()void']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(AsaEscrowParamsFactory.kill(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Makes a clear_state call to an existing instance of the ASAEscrow smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the ASAEscrow smart contract using the `optin()void` ABI method.
     *
    * Opt the application account into the ASA so it can receive tokens.
    Must be called (and funded) by creator before transferring tokens.

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    optin: async (params: CallParams<AsaEscrowArgs['obj']['optin()void'] | AsaEscrowArgs['tuple']['optin()void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(AsaEscrowParamsFactory.optin(params))
      return {...result, return: result.return as unknown as (undefined | AsaEscrowReturns['optin()void'])}
    },

    /**
     * Makes a call to the ASAEscrow smart contract using the `release()void` ABI method.
     *
     * Transfer all held ASA tokens to recipient. Only callable by creator.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    release: async (params: CallParams<AsaEscrowArgs['obj']['release()void'] | AsaEscrowArgs['tuple']['release()void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(AsaEscrowParamsFactory.release(params))
      return {...result, return: result.return as unknown as (undefined | AsaEscrowReturns['release()void'])}
    },

    /**
     * Makes a call to the ASAEscrow smart contract using the `kill()void` ABI method.
     *
     * Return all held ASA tokens to creator. Only callable by creator.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    kill: async (params: CallParams<AsaEscrowArgs['obj']['kill()void'] | AsaEscrowArgs['tuple']['kill()void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(AsaEscrowParamsFactory.kill(params))
      return {...result, return: result.return as unknown as (undefined | AsaEscrowReturns['kill()void'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new AsaEscrowClient(this.appClient.clone(params))
  }

  /**
   * Methods to access state for the current ASAEscrow app
   */
  state = {
    /**
     * Methods to access global state for the current ASAEscrow app
     */
    global: {
      /**
       * Get all current keyed values from global state
       */
      getAll: async (): Promise<Partial<Expand<GlobalKeysState>>> => {
        const result = await this.appClient.state.global.getAll()
        return {
          creator: result.creator,
          recipient: result.recipient,
          asaId: result.asa_id,
          status: result.status,
        }
      },
      /**
       * Get the current value of the creator key in global state
       */
      creator: async (): Promise<string | undefined> => { return (await this.appClient.state.global.getValue("creator")) as string | undefined },
      /**
       * Get the current value of the recipient key in global state
       */
      recipient: async (): Promise<string | undefined> => { return (await this.appClient.state.global.getValue("recipient")) as string | undefined },
      /**
       * Get the current value of the asa_id key in global state
       */
      asaId: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("asa_id")) as bigint | undefined },
      /**
       * Get the current value of the status key in global state
       */
      status: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("status")) as bigint | undefined },
    },
  }

  public newGroup(): AsaEscrowComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a optin()void method call against the ASAEscrow contract
       */
      optin(params: CallParams<AsaEscrowArgs['obj']['optin()void'] | AsaEscrowArgs['tuple']['optin()void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.optin(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a release()void method call against the ASAEscrow contract
       */
      release(params: CallParams<AsaEscrowArgs['obj']['release()void'] | AsaEscrowArgs['tuple']['release()void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.release(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a kill()void method call against the ASAEscrow contract
       */
      kill(params: CallParams<AsaEscrowArgs['obj']['kill()void'] | AsaEscrowArgs['tuple']['kill()void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.kill(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a clear state call to the ASAEscrow contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as AsaEscrowComposer
  }
}
export type AsaEscrowComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the optin()void ABI method.
   *
  * Opt the application account into the ASA so it can receive tokens.
  Must be called (and funded) by creator before transferring tokens.

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  optin(params?: CallParams<AsaEscrowArgs['obj']['optin()void'] | AsaEscrowArgs['tuple']['optin()void']>): AsaEscrowComposer<[...TReturns, AsaEscrowReturns['optin()void'] | undefined]>

  /**
   * Calls the release()void ABI method.
   *
   * Transfer all held ASA tokens to recipient. Only callable by creator.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  release(params?: CallParams<AsaEscrowArgs['obj']['release()void'] | AsaEscrowArgs['tuple']['release()void']>): AsaEscrowComposer<[...TReturns, AsaEscrowReturns['release()void'] | undefined]>

  /**
   * Calls the kill()void ABI method.
   *
   * Return all held ASA tokens to creator. Only callable by creator.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  kill(params?: CallParams<AsaEscrowArgs['obj']['kill()void'] | AsaEscrowArgs['tuple']['kill()void']>): AsaEscrowComposer<[...TReturns, AsaEscrowReturns['kill()void'] | undefined]>

  /**
   * Makes a clear_state call to an existing instance of the ASAEscrow smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): AsaEscrowComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): AsaEscrowComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<AsaEscrowComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<AsaEscrowComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<AsaEscrowComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<AsaEscrowComposerResults<TReturns>>
}
export type AsaEscrowComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

