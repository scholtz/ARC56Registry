/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"MilestoneEscrow","structs":{},"methods":[{"name":"create_escrow","args":[{"type":"address","name":"freelancer_addr"},{"type":"uint64","name":"amount"}],"returns":{"type":"string"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Create a new escrow contract.","events":[],"recommendations":{}},{"name":"fund_escrow","args":[],"returns":{"type":"string"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Fund the escrow (client sends ALGO).","events":[],"recommendations":{}},{"name":"release_payment","args":[{"type":"uint64","name":"amount"}],"returns":{"type":"string"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Release payment to freelancer (called by client).","events":[],"recommendations":{}},{"name":"cancel_escrow","args":[],"returns":{"type":"string"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Cancel escrow and refund client.","events":[],"recommendations":{}},{"name":"get_info","args":[],"returns":{"type":"(address,address,uint64,uint64,uint64)"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"desc":"Get escrow information.","events":[],"recommendations":{}}],"arcs":[22,28],"desc":"\n    Milestone-Based Escrow for CampusNexus\n    \n    Flow:\n    1. Client creates escrow and defines total amount\n    2. Client funds the escrow\n    3. Freelancer marks work complete\n    4. Client approves and funds release\n    ","networks":{},"state":{"schema":{"global":{"ints":3,"bytes":2},"local":{"ints":0,"bytes":0}},"keys":{"global":{"client":{"keyType":"AVMString","valueType":"address","key":"Y2xpZW50"},"freelancer":{"keyType":"AVMString","valueType":"address","key":"ZnJlZWxhbmNlcg=="},"total_amount":{"keyType":"AVMString","valueType":"AVMUint64","key":"dG90YWxfYW1vdW50"},"released_amount":{"keyType":"AVMString","valueType":"AVMUint64","key":"cmVsZWFzZWRfYW1vdW50"},"status":{"keyType":"AVMString","valueType":"AVMUint64","key":"c3RhdHVz"}},"local":{},"box":{}},"maps":{"global":{},"local":{},"box":{}}},"bareActions":{"create":["NoOp"],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[301],"errorMessage":"Amount exceeds total"},{"pc":[432],"errorMessage":"Cannot cancel after release"},{"pc":[167],"errorMessage":"Escrow already exists"},{"pc":[238,286,426],"errorMessage":"Escrow not active"},{"pc":[419],"errorMessage":"Only client can cancel"},{"pc":[231],"errorMessage":"Only client can fund"},{"pc":[279],"errorMessage":"Only client can release"},{"pc":[229,277,417,467],"errorMessage":"check self.client exists"},{"pc":[307,472],"errorMessage":"check self.freelancer exists"},{"pc":[290,430,480],"errorMessage":"check self.released_amount exists"},{"pc":[165,235,283,423,484],"errorMessage":"check self.status exists"},{"pc":[297,328,476],"errorMessage":"check self.total_amount exists"},{"pc":[152],"errorMessage":"invalid number of bytes for arc4.static_array<arc4.uint8, 32>"},{"pc":[160,270],"errorMessage":"invalid number of bytes for arc4.uint64"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"events":[]} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


/**
 * The argument types for the MilestoneEscrow contract
 */
export type MilestoneEscrowArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'create_escrow(address,uint64)string': {
      freelancerAddr: string
      amount: bigint | number
    }
    'fund_escrow()string': Record<string, never>
    'release_payment(uint64)string': {
      amount: bigint | number
    }
    'cancel_escrow()string': Record<string, never>
    'get_info()(address,address,uint64,uint64,uint64)': Record<string, never>
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'create_escrow(address,uint64)string': [freelancerAddr: string, amount: bigint | number]
    'fund_escrow()string': []
    'release_payment(uint64)string': [amount: bigint | number]
    'cancel_escrow()string': []
    'get_info()(address,address,uint64,uint64,uint64)': []
  }
}

/**
 * The return type for each method
 */
export type MilestoneEscrowReturns = {
  'create_escrow(address,uint64)string': string
  'fund_escrow()string': string
  'release_payment(uint64)string': string
  'cancel_escrow()string': string
  'get_info()(address,address,uint64,uint64,uint64)': [string, string, bigint, bigint, bigint]
}

/**
 * Defines the types of available calls and state of the MilestoneEscrow smart contract.
 */
export type MilestoneEscrowTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'create_escrow(address,uint64)string' | 'create_escrow', {
      argsObj: MilestoneEscrowArgs['obj']['create_escrow(address,uint64)string']
      argsTuple: MilestoneEscrowArgs['tuple']['create_escrow(address,uint64)string']
      returns: MilestoneEscrowReturns['create_escrow(address,uint64)string']
    }>
    & Record<'fund_escrow()string' | 'fund_escrow', {
      argsObj: MilestoneEscrowArgs['obj']['fund_escrow()string']
      argsTuple: MilestoneEscrowArgs['tuple']['fund_escrow()string']
      returns: MilestoneEscrowReturns['fund_escrow()string']
    }>
    & Record<'release_payment(uint64)string' | 'release_payment', {
      argsObj: MilestoneEscrowArgs['obj']['release_payment(uint64)string']
      argsTuple: MilestoneEscrowArgs['tuple']['release_payment(uint64)string']
      returns: MilestoneEscrowReturns['release_payment(uint64)string']
    }>
    & Record<'cancel_escrow()string' | 'cancel_escrow', {
      argsObj: MilestoneEscrowArgs['obj']['cancel_escrow()string']
      argsTuple: MilestoneEscrowArgs['tuple']['cancel_escrow()string']
      returns: MilestoneEscrowReturns['cancel_escrow()string']
    }>
    & Record<'get_info()(address,address,uint64,uint64,uint64)' | 'get_info', {
      argsObj: MilestoneEscrowArgs['obj']['get_info()(address,address,uint64,uint64,uint64)']
      argsTuple: MilestoneEscrowArgs['tuple']['get_info()(address,address,uint64,uint64,uint64)']
      returns: MilestoneEscrowReturns['get_info()(address,address,uint64,uint64,uint64)']
    }>
  /**
   * Defines the shape of the state of the application.
   */
  state: {
    global: {
      keys: {
        client: string
        freelancer: string
        totalAmount: bigint
        releasedAmount: bigint
        status: bigint
      }
      maps: {}
    }
  }
}

/**
 * Defines the possible abi call signatures.
 */
export type MilestoneEscrowSignatures = keyof MilestoneEscrowTypes['methods']
/**
 * Defines the possible abi call signatures for methods that return a non-void value.
 */
export type MilestoneEscrowNonVoidMethodSignatures = keyof MilestoneEscrowTypes['methods'] extends infer T ? T extends keyof MilestoneEscrowTypes['methods'] ? MethodReturn<T> extends void ? never : T  : never : never
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the MilestoneEscrow smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends MilestoneEscrowSignatures> = MilestoneEscrowTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the MilestoneEscrow smart contract to the method's return type
 */
export type MethodReturn<TSignature extends MilestoneEscrowSignatures> = MilestoneEscrowTypes['methods'][TSignature]['returns']

/**
 * Defines the shape of the keyed global state of the application.
 */
export type GlobalKeysState = MilestoneEscrowTypes['state']['global']['keys']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the MilestoneEscrow smart contract
 */
export abstract class MilestoneEscrowParamsFactory {
  /**
   * Constructs a no op call for the create_escrow(address,uint64)string ABI method
   *
   * Create a new escrow contract.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static createEscrow(params: CallParams<MilestoneEscrowArgs['obj']['create_escrow(address,uint64)string'] | MilestoneEscrowArgs['tuple']['create_escrow(address,uint64)string']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'create_escrow(address,uint64)string' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.freelancerAddr, params.args.amount],
    }
  }
  /**
   * Constructs a no op call for the fund_escrow()string ABI method
   *
   * Fund the escrow (client sends ALGO).
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static fundEscrow(params: CallParams<MilestoneEscrowArgs['obj']['fund_escrow()string'] | MilestoneEscrowArgs['tuple']['fund_escrow()string']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'fund_escrow()string' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the release_payment(uint64)string ABI method
   *
   * Release payment to freelancer (called by client).
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static releasePayment(params: CallParams<MilestoneEscrowArgs['obj']['release_payment(uint64)string'] | MilestoneEscrowArgs['tuple']['release_payment(uint64)string']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'release_payment(uint64)string' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.amount],
    }
  }
  /**
   * Constructs a no op call for the cancel_escrow()string ABI method
   *
   * Cancel escrow and refund client.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static cancelEscrow(params: CallParams<MilestoneEscrowArgs['obj']['cancel_escrow()string'] | MilestoneEscrowArgs['tuple']['cancel_escrow()string']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'cancel_escrow()string' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the get_info()(address,address,uint64,uint64,uint64) ABI method
   *
   * Get escrow information.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static getInfo(params: CallParams<MilestoneEscrowArgs['obj']['get_info()(address,address,uint64,uint64,uint64)'] | MilestoneEscrowArgs['tuple']['get_info()(address,address,uint64,uint64,uint64)']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'get_info()(address,address,uint64,uint64,uint64)' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
}

/**
 * A client to make calls to the MilestoneEscrow smart contract
 */
export class MilestoneEscrowClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `MilestoneEscrowClient`
   *
   * @param appClient An `AppClient` instance which has been created with the MilestoneEscrow app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `MilestoneEscrowClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Checks for decode errors on the given return value and maps the return value to the return type for the given method
   * @returns The typed return value or undefined if there was no value
   */
  decodeReturnValue<TSignature extends MilestoneEscrowNonVoidMethodSignatures>(method: TSignature, returnValue: ABIReturn | undefined) {
    return returnValue !== undefined ? getArc56ReturnValue<MethodReturn<TSignature>>(returnValue, this.appClient.getABIMethod(method), APP_SPEC.structs) : undefined
  }

  /**
   * Returns a new `MilestoneEscrowClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<MilestoneEscrowClient> {
    return new MilestoneEscrowClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `MilestoneEscrowClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<MilestoneEscrowClient> {
    return new MilestoneEscrowClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Makes a clear_state call to an existing instance of the MilestoneEscrow smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the MilestoneEscrow smart contract using the `create_escrow(address,uint64)string` ABI method.
     *
     * Create a new escrow contract.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    createEscrow: (params: CallParams<MilestoneEscrowArgs['obj']['create_escrow(address,uint64)string'] | MilestoneEscrowArgs['tuple']['create_escrow(address,uint64)string']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(MilestoneEscrowParamsFactory.createEscrow(params))
    },

    /**
     * Makes a call to the MilestoneEscrow smart contract using the `fund_escrow()string` ABI method.
     *
     * Fund the escrow (client sends ALGO).
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    fundEscrow: (params: CallParams<MilestoneEscrowArgs['obj']['fund_escrow()string'] | MilestoneEscrowArgs['tuple']['fund_escrow()string']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(MilestoneEscrowParamsFactory.fundEscrow(params))
    },

    /**
     * Makes a call to the MilestoneEscrow smart contract using the `release_payment(uint64)string` ABI method.
     *
     * Release payment to freelancer (called by client).
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    releasePayment: (params: CallParams<MilestoneEscrowArgs['obj']['release_payment(uint64)string'] | MilestoneEscrowArgs['tuple']['release_payment(uint64)string']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(MilestoneEscrowParamsFactory.releasePayment(params))
    },

    /**
     * Makes a call to the MilestoneEscrow smart contract using the `cancel_escrow()string` ABI method.
     *
     * Cancel escrow and refund client.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    cancelEscrow: (params: CallParams<MilestoneEscrowArgs['obj']['cancel_escrow()string'] | MilestoneEscrowArgs['tuple']['cancel_escrow()string']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(MilestoneEscrowParamsFactory.cancelEscrow(params))
    },

    /**
     * Makes a call to the MilestoneEscrow smart contract using the `get_info()(address,address,uint64,uint64,uint64)` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * Get escrow information.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    getInfo: (params: CallParams<MilestoneEscrowArgs['obj']['get_info()(address,address,uint64,uint64,uint64)'] | MilestoneEscrowArgs['tuple']['get_info()(address,address,uint64,uint64,uint64)']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(MilestoneEscrowParamsFactory.getInfo(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Makes a clear_state call to an existing instance of the MilestoneEscrow smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the MilestoneEscrow smart contract using the `create_escrow(address,uint64)string` ABI method.
     *
     * Create a new escrow contract.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    createEscrow: (params: CallParams<MilestoneEscrowArgs['obj']['create_escrow(address,uint64)string'] | MilestoneEscrowArgs['tuple']['create_escrow(address,uint64)string']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(MilestoneEscrowParamsFactory.createEscrow(params))
    },

    /**
     * Makes a call to the MilestoneEscrow smart contract using the `fund_escrow()string` ABI method.
     *
     * Fund the escrow (client sends ALGO).
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    fundEscrow: (params: CallParams<MilestoneEscrowArgs['obj']['fund_escrow()string'] | MilestoneEscrowArgs['tuple']['fund_escrow()string']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(MilestoneEscrowParamsFactory.fundEscrow(params))
    },

    /**
     * Makes a call to the MilestoneEscrow smart contract using the `release_payment(uint64)string` ABI method.
     *
     * Release payment to freelancer (called by client).
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    releasePayment: (params: CallParams<MilestoneEscrowArgs['obj']['release_payment(uint64)string'] | MilestoneEscrowArgs['tuple']['release_payment(uint64)string']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(MilestoneEscrowParamsFactory.releasePayment(params))
    },

    /**
     * Makes a call to the MilestoneEscrow smart contract using the `cancel_escrow()string` ABI method.
     *
     * Cancel escrow and refund client.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    cancelEscrow: (params: CallParams<MilestoneEscrowArgs['obj']['cancel_escrow()string'] | MilestoneEscrowArgs['tuple']['cancel_escrow()string']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(MilestoneEscrowParamsFactory.cancelEscrow(params))
    },

    /**
     * Makes a call to the MilestoneEscrow smart contract using the `get_info()(address,address,uint64,uint64,uint64)` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * Get escrow information.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    getInfo: (params: CallParams<MilestoneEscrowArgs['obj']['get_info()(address,address,uint64,uint64,uint64)'] | MilestoneEscrowArgs['tuple']['get_info()(address,address,uint64,uint64,uint64)']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(MilestoneEscrowParamsFactory.getInfo(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Makes a clear_state call to an existing instance of the MilestoneEscrow smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the MilestoneEscrow smart contract using the `create_escrow(address,uint64)string` ABI method.
     *
     * Create a new escrow contract.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    createEscrow: async (params: CallParams<MilestoneEscrowArgs['obj']['create_escrow(address,uint64)string'] | MilestoneEscrowArgs['tuple']['create_escrow(address,uint64)string']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(MilestoneEscrowParamsFactory.createEscrow(params))
      return {...result, return: result.return as unknown as (undefined | MilestoneEscrowReturns['create_escrow(address,uint64)string'])}
    },

    /**
     * Makes a call to the MilestoneEscrow smart contract using the `fund_escrow()string` ABI method.
     *
     * Fund the escrow (client sends ALGO).
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    fundEscrow: async (params: CallParams<MilestoneEscrowArgs['obj']['fund_escrow()string'] | MilestoneEscrowArgs['tuple']['fund_escrow()string']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(MilestoneEscrowParamsFactory.fundEscrow(params))
      return {...result, return: result.return as unknown as (undefined | MilestoneEscrowReturns['fund_escrow()string'])}
    },

    /**
     * Makes a call to the MilestoneEscrow smart contract using the `release_payment(uint64)string` ABI method.
     *
     * Release payment to freelancer (called by client).
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    releasePayment: async (params: CallParams<MilestoneEscrowArgs['obj']['release_payment(uint64)string'] | MilestoneEscrowArgs['tuple']['release_payment(uint64)string']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(MilestoneEscrowParamsFactory.releasePayment(params))
      return {...result, return: result.return as unknown as (undefined | MilestoneEscrowReturns['release_payment(uint64)string'])}
    },

    /**
     * Makes a call to the MilestoneEscrow smart contract using the `cancel_escrow()string` ABI method.
     *
     * Cancel escrow and refund client.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    cancelEscrow: async (params: CallParams<MilestoneEscrowArgs['obj']['cancel_escrow()string'] | MilestoneEscrowArgs['tuple']['cancel_escrow()string']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(MilestoneEscrowParamsFactory.cancelEscrow(params))
      return {...result, return: result.return as unknown as (undefined | MilestoneEscrowReturns['cancel_escrow()string'])}
    },

    /**
     * Makes a call to the MilestoneEscrow smart contract using the `get_info()(address,address,uint64,uint64,uint64)` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * Get escrow information.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    getInfo: async (params: CallParams<MilestoneEscrowArgs['obj']['get_info()(address,address,uint64,uint64,uint64)'] | MilestoneEscrowArgs['tuple']['get_info()(address,address,uint64,uint64,uint64)']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(MilestoneEscrowParamsFactory.getInfo(params))
      return {...result, return: result.return as unknown as (undefined | MilestoneEscrowReturns['get_info()(address,address,uint64,uint64,uint64)'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new MilestoneEscrowClient(this.appClient.clone(params))
  }

  /**
   * Makes a readonly (simulated) call to the MilestoneEscrow smart contract using the `get_info()(address,address,uint64,uint64,uint64)` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * Get escrow information.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async getInfo(params: CallParams<MilestoneEscrowArgs['obj']['get_info()(address,address,uint64,uint64,uint64)'] | MilestoneEscrowArgs['tuple']['get_info()(address,address,uint64,uint64,uint64)']> = {args: []}) {
    const result = await this.appClient.send.call(MilestoneEscrowParamsFactory.getInfo(params))
    return result.return as unknown as MilestoneEscrowReturns['get_info()(address,address,uint64,uint64,uint64)']
  }

  /**
   * Methods to access state for the current MilestoneEscrow app
   */
  state = {
    /**
     * Methods to access global state for the current MilestoneEscrow app
     */
    global: {
      /**
       * Get all current keyed values from global state
       */
      getAll: async (): Promise<Partial<Expand<GlobalKeysState>>> => {
        const result = await this.appClient.state.global.getAll()
        return {
          client: result.client,
          freelancer: result.freelancer,
          totalAmount: result.total_amount,
          releasedAmount: result.released_amount,
          status: result.status,
        }
      },
      /**
       * Get the current value of the client key in global state
       */
      client: async (): Promise<string | undefined> => { return (await this.appClient.state.global.getValue("client")) as string | undefined },
      /**
       * Get the current value of the freelancer key in global state
       */
      freelancer: async (): Promise<string | undefined> => { return (await this.appClient.state.global.getValue("freelancer")) as string | undefined },
      /**
       * Get the current value of the total_amount key in global state
       */
      totalAmount: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("total_amount")) as bigint | undefined },
      /**
       * Get the current value of the released_amount key in global state
       */
      releasedAmount: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("released_amount")) as bigint | undefined },
      /**
       * Get the current value of the status key in global state
       */
      status: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("status")) as bigint | undefined },
    },
  }

  public newGroup(): MilestoneEscrowComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a create_escrow(address,uint64)string method call against the MilestoneEscrow contract
       */
      createEscrow(params: CallParams<MilestoneEscrowArgs['obj']['create_escrow(address,uint64)string'] | MilestoneEscrowArgs['tuple']['create_escrow(address,uint64)string']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.createEscrow(params)))
        resultMappers.push((v) => client.decodeReturnValue('create_escrow(address,uint64)string', v))
        return this
      },
      /**
       * Add a fund_escrow()string method call against the MilestoneEscrow contract
       */
      fundEscrow(params: CallParams<MilestoneEscrowArgs['obj']['fund_escrow()string'] | MilestoneEscrowArgs['tuple']['fund_escrow()string']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.fundEscrow(params)))
        resultMappers.push((v) => client.decodeReturnValue('fund_escrow()string', v))
        return this
      },
      /**
       * Add a release_payment(uint64)string method call against the MilestoneEscrow contract
       */
      releasePayment(params: CallParams<MilestoneEscrowArgs['obj']['release_payment(uint64)string'] | MilestoneEscrowArgs['tuple']['release_payment(uint64)string']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.releasePayment(params)))
        resultMappers.push((v) => client.decodeReturnValue('release_payment(uint64)string', v))
        return this
      },
      /**
       * Add a cancel_escrow()string method call against the MilestoneEscrow contract
       */
      cancelEscrow(params: CallParams<MilestoneEscrowArgs['obj']['cancel_escrow()string'] | MilestoneEscrowArgs['tuple']['cancel_escrow()string']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.cancelEscrow(params)))
        resultMappers.push((v) => client.decodeReturnValue('cancel_escrow()string', v))
        return this
      },
      /**
       * Add a get_info()(address,address,uint64,uint64,uint64) method call against the MilestoneEscrow contract
       */
      getInfo(params: CallParams<MilestoneEscrowArgs['obj']['get_info()(address,address,uint64,uint64,uint64)'] | MilestoneEscrowArgs['tuple']['get_info()(address,address,uint64,uint64,uint64)']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.getInfo(params)))
        resultMappers.push((v) => client.decodeReturnValue('get_info()(address,address,uint64,uint64,uint64)', v))
        return this
      },
      /**
       * Add a clear state call to the MilestoneEscrow contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as MilestoneEscrowComposer
  }
}
export type MilestoneEscrowComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the create_escrow(address,uint64)string ABI method.
   *
   * Create a new escrow contract.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  createEscrow(params?: CallParams<MilestoneEscrowArgs['obj']['create_escrow(address,uint64)string'] | MilestoneEscrowArgs['tuple']['create_escrow(address,uint64)string']>): MilestoneEscrowComposer<[...TReturns, MilestoneEscrowReturns['create_escrow(address,uint64)string'] | undefined]>

  /**
   * Calls the fund_escrow()string ABI method.
   *
   * Fund the escrow (client sends ALGO).
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  fundEscrow(params?: CallParams<MilestoneEscrowArgs['obj']['fund_escrow()string'] | MilestoneEscrowArgs['tuple']['fund_escrow()string']>): MilestoneEscrowComposer<[...TReturns, MilestoneEscrowReturns['fund_escrow()string'] | undefined]>

  /**
   * Calls the release_payment(uint64)string ABI method.
   *
   * Release payment to freelancer (called by client).
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  releasePayment(params?: CallParams<MilestoneEscrowArgs['obj']['release_payment(uint64)string'] | MilestoneEscrowArgs['tuple']['release_payment(uint64)string']>): MilestoneEscrowComposer<[...TReturns, MilestoneEscrowReturns['release_payment(uint64)string'] | undefined]>

  /**
   * Calls the cancel_escrow()string ABI method.
   *
   * Cancel escrow and refund client.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  cancelEscrow(params?: CallParams<MilestoneEscrowArgs['obj']['cancel_escrow()string'] | MilestoneEscrowArgs['tuple']['cancel_escrow()string']>): MilestoneEscrowComposer<[...TReturns, MilestoneEscrowReturns['cancel_escrow()string'] | undefined]>

  /**
   * Calls the get_info()(address,address,uint64,uint64,uint64) ABI method.
   *
   * Get escrow information.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  getInfo(params?: CallParams<MilestoneEscrowArgs['obj']['get_info()(address,address,uint64,uint64,uint64)'] | MilestoneEscrowArgs['tuple']['get_info()(address,address,uint64,uint64,uint64)']>): MilestoneEscrowComposer<[...TReturns, MilestoneEscrowReturns['get_info()(address,address,uint64,uint64,uint64)'] | undefined]>

  /**
   * Makes a clear_state call to an existing instance of the MilestoneEscrow smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): MilestoneEscrowComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): MilestoneEscrowComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<MilestoneEscrowComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<MilestoneEscrowComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<MilestoneEscrowComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<MilestoneEscrowComposerResults<TReturns>>
}
export type MilestoneEscrowComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

