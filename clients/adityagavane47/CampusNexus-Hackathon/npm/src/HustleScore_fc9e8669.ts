/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"HustleScore","structs":{},"methods":[{"name":"create","args":[],"returns":{"type":"string"},"actions":{"create":["NoOp"],"call":[]},"readonly":false,"desc":"Initialize the contract with creator as admin.","events":[],"recommendations":{}},{"name":"add_reputation","args":[{"type":"address","name":"student"},{"type":"uint64","name":"points"}],"returns":{"type":"string"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Add reputation points to a student (admin only).","events":[],"recommendations":{}},{"name":"remove_reputation","args":[{"type":"address","name":"student"},{"type":"uint64","name":"points"}],"returns":{"type":"string"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Remove reputation points (for disputes, admin only).","events":[],"recommendations":{}},{"name":"get_total_minted","args":[],"returns":{"type":"uint64"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"desc":"Get total reputation points minted.","events":[],"recommendations":{}},{"name":"get_admin","args":[],"returns":{"type":"address"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"desc":"Get the admin address.","events":[],"recommendations":{}}],"arcs":[22,28],"desc":"\n    Hustle Score SBT for CampusNexus\n    \n    Features:\n    - Non-transferable (soulbound)\n    - Score increases with completed projects\n    - Admin-managed minting\n    ","networks":{},"state":{"schema":{"global":{"ints":1,"bytes":1},"local":{"ints":0,"bytes":0}},"keys":{"global":{"admin":{"keyType":"AVMString","valueType":"address","key":"YWRtaW4="},"total_minted":{"keyType":"AVMString","valueType":"AVMUint64","key":"dG90YWxfbWludGVk"}},"local":{},"box":{}},"maps":{"global":{},"local":{},"box":{}}},"bareActions":{"create":[],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[168],"errorMessage":"Only admin can add reputation"},{"pc":[225],"errorMessage":"Only admin can remove reputation"},{"pc":[166,223,269],"errorMessage":"check self.admin exists"},{"pc":[172,258],"errorMessage":"check self.total_minted exists"},{"pc":[151,210],"errorMessage":"invalid number of bytes for arc4.static_array<arc4.uint8, 32>"},{"pc":[159,217],"errorMessage":"invalid number of bytes for arc4.uint64"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"events":[]} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


/**
 * The argument types for the HustleScore contract
 */
export type HustleScoreArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'create()string': Record<string, never>
    'add_reputation(address,uint64)string': {
      student: string
      points: bigint | number
    }
    'remove_reputation(address,uint64)string': {
      student: string
      points: bigint | number
    }
    'get_total_minted()uint64': Record<string, never>
    'get_admin()address': Record<string, never>
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'create()string': []
    'add_reputation(address,uint64)string': [student: string, points: bigint | number]
    'remove_reputation(address,uint64)string': [student: string, points: bigint | number]
    'get_total_minted()uint64': []
    'get_admin()address': []
  }
}

/**
 * The return type for each method
 */
export type HustleScoreReturns = {
  'create()string': string
  'add_reputation(address,uint64)string': string
  'remove_reputation(address,uint64)string': string
  'get_total_minted()uint64': bigint
  'get_admin()address': string
}

/**
 * Defines the types of available calls and state of the HustleScore smart contract.
 */
export type HustleScoreTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'create()string' | 'create', {
      argsObj: HustleScoreArgs['obj']['create()string']
      argsTuple: HustleScoreArgs['tuple']['create()string']
      returns: HustleScoreReturns['create()string']
    }>
    & Record<'add_reputation(address,uint64)string' | 'add_reputation', {
      argsObj: HustleScoreArgs['obj']['add_reputation(address,uint64)string']
      argsTuple: HustleScoreArgs['tuple']['add_reputation(address,uint64)string']
      returns: HustleScoreReturns['add_reputation(address,uint64)string']
    }>
    & Record<'remove_reputation(address,uint64)string' | 'remove_reputation', {
      argsObj: HustleScoreArgs['obj']['remove_reputation(address,uint64)string']
      argsTuple: HustleScoreArgs['tuple']['remove_reputation(address,uint64)string']
      returns: HustleScoreReturns['remove_reputation(address,uint64)string']
    }>
    & Record<'get_total_minted()uint64' | 'get_total_minted', {
      argsObj: HustleScoreArgs['obj']['get_total_minted()uint64']
      argsTuple: HustleScoreArgs['tuple']['get_total_minted()uint64']
      returns: HustleScoreReturns['get_total_minted()uint64']
    }>
    & Record<'get_admin()address' | 'get_admin', {
      argsObj: HustleScoreArgs['obj']['get_admin()address']
      argsTuple: HustleScoreArgs['tuple']['get_admin()address']
      returns: HustleScoreReturns['get_admin()address']
    }>
  /**
   * Defines the shape of the state of the application.
   */
  state: {
    global: {
      keys: {
        admin: string
        totalMinted: bigint
      }
      maps: {}
    }
  }
}

/**
 * Defines the possible abi call signatures.
 */
export type HustleScoreSignatures = keyof HustleScoreTypes['methods']
/**
 * Defines the possible abi call signatures for methods that return a non-void value.
 */
export type HustleScoreNonVoidMethodSignatures = keyof HustleScoreTypes['methods'] extends infer T ? T extends keyof HustleScoreTypes['methods'] ? MethodReturn<T> extends void ? never : T  : never : never
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the HustleScore smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends HustleScoreSignatures> = HustleScoreTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the HustleScore smart contract to the method's return type
 */
export type MethodReturn<TSignature extends HustleScoreSignatures> = HustleScoreTypes['methods'][TSignature]['returns']

/**
 * Defines the shape of the keyed global state of the application.
 */
export type GlobalKeysState = HustleScoreTypes['state']['global']['keys']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the HustleScore smart contract
 */
export abstract class HustleScoreParamsFactory {
  /**
   * Constructs a no op call for the add_reputation(address,uint64)string ABI method
   *
   * Add reputation points to a student (admin only).
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static addReputation(params: CallParams<HustleScoreArgs['obj']['add_reputation(address,uint64)string'] | HustleScoreArgs['tuple']['add_reputation(address,uint64)string']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'add_reputation(address,uint64)string' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.student, params.args.points],
    }
  }
  /**
   * Constructs a no op call for the remove_reputation(address,uint64)string ABI method
   *
   * Remove reputation points (for disputes, admin only).
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static removeReputation(params: CallParams<HustleScoreArgs['obj']['remove_reputation(address,uint64)string'] | HustleScoreArgs['tuple']['remove_reputation(address,uint64)string']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'remove_reputation(address,uint64)string' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.student, params.args.points],
    }
  }
  /**
   * Constructs a no op call for the get_total_minted()uint64 ABI method
   *
   * Get total reputation points minted.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static getTotalMinted(params: CallParams<HustleScoreArgs['obj']['get_total_minted()uint64'] | HustleScoreArgs['tuple']['get_total_minted()uint64']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'get_total_minted()uint64' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the get_admin()address ABI method
   *
   * Get the admin address.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static getAdmin(params: CallParams<HustleScoreArgs['obj']['get_admin()address'] | HustleScoreArgs['tuple']['get_admin()address']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'get_admin()address' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
}

/**
 * A client to make calls to the HustleScore smart contract
 */
export class HustleScoreClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `HustleScoreClient`
   *
   * @param appClient An `AppClient` instance which has been created with the HustleScore app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `HustleScoreClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Checks for decode errors on the given return value and maps the return value to the return type for the given method
   * @returns The typed return value or undefined if there was no value
   */
  decodeReturnValue<TSignature extends HustleScoreNonVoidMethodSignatures>(method: TSignature, returnValue: ABIReturn | undefined) {
    return returnValue !== undefined ? getArc56ReturnValue<MethodReturn<TSignature>>(returnValue, this.appClient.getABIMethod(method), APP_SPEC.structs) : undefined
  }

  /**
   * Returns a new `HustleScoreClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<HustleScoreClient> {
    return new HustleScoreClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `HustleScoreClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<HustleScoreClient> {
    return new HustleScoreClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Makes a clear_state call to an existing instance of the HustleScore smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the HustleScore smart contract using the `add_reputation(address,uint64)string` ABI method.
     *
     * Add reputation points to a student (admin only).
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    addReputation: (params: CallParams<HustleScoreArgs['obj']['add_reputation(address,uint64)string'] | HustleScoreArgs['tuple']['add_reputation(address,uint64)string']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(HustleScoreParamsFactory.addReputation(params))
    },

    /**
     * Makes a call to the HustleScore smart contract using the `remove_reputation(address,uint64)string` ABI method.
     *
     * Remove reputation points (for disputes, admin only).
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    removeReputation: (params: CallParams<HustleScoreArgs['obj']['remove_reputation(address,uint64)string'] | HustleScoreArgs['tuple']['remove_reputation(address,uint64)string']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(HustleScoreParamsFactory.removeReputation(params))
    },

    /**
     * Makes a call to the HustleScore smart contract using the `get_total_minted()uint64` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * Get total reputation points minted.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    getTotalMinted: (params: CallParams<HustleScoreArgs['obj']['get_total_minted()uint64'] | HustleScoreArgs['tuple']['get_total_minted()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(HustleScoreParamsFactory.getTotalMinted(params))
    },

    /**
     * Makes a call to the HustleScore smart contract using the `get_admin()address` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * Get the admin address.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    getAdmin: (params: CallParams<HustleScoreArgs['obj']['get_admin()address'] | HustleScoreArgs['tuple']['get_admin()address']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(HustleScoreParamsFactory.getAdmin(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Makes a clear_state call to an existing instance of the HustleScore smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the HustleScore smart contract using the `add_reputation(address,uint64)string` ABI method.
     *
     * Add reputation points to a student (admin only).
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    addReputation: (params: CallParams<HustleScoreArgs['obj']['add_reputation(address,uint64)string'] | HustleScoreArgs['tuple']['add_reputation(address,uint64)string']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(HustleScoreParamsFactory.addReputation(params))
    },

    /**
     * Makes a call to the HustleScore smart contract using the `remove_reputation(address,uint64)string` ABI method.
     *
     * Remove reputation points (for disputes, admin only).
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    removeReputation: (params: CallParams<HustleScoreArgs['obj']['remove_reputation(address,uint64)string'] | HustleScoreArgs['tuple']['remove_reputation(address,uint64)string']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(HustleScoreParamsFactory.removeReputation(params))
    },

    /**
     * Makes a call to the HustleScore smart contract using the `get_total_minted()uint64` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * Get total reputation points minted.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    getTotalMinted: (params: CallParams<HustleScoreArgs['obj']['get_total_minted()uint64'] | HustleScoreArgs['tuple']['get_total_minted()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(HustleScoreParamsFactory.getTotalMinted(params))
    },

    /**
     * Makes a call to the HustleScore smart contract using the `get_admin()address` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * Get the admin address.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    getAdmin: (params: CallParams<HustleScoreArgs['obj']['get_admin()address'] | HustleScoreArgs['tuple']['get_admin()address']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(HustleScoreParamsFactory.getAdmin(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Makes a clear_state call to an existing instance of the HustleScore smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the HustleScore smart contract using the `add_reputation(address,uint64)string` ABI method.
     *
     * Add reputation points to a student (admin only).
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    addReputation: async (params: CallParams<HustleScoreArgs['obj']['add_reputation(address,uint64)string'] | HustleScoreArgs['tuple']['add_reputation(address,uint64)string']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(HustleScoreParamsFactory.addReputation(params))
      return {...result, return: result.return as unknown as (undefined | HustleScoreReturns['add_reputation(address,uint64)string'])}
    },

    /**
     * Makes a call to the HustleScore smart contract using the `remove_reputation(address,uint64)string` ABI method.
     *
     * Remove reputation points (for disputes, admin only).
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    removeReputation: async (params: CallParams<HustleScoreArgs['obj']['remove_reputation(address,uint64)string'] | HustleScoreArgs['tuple']['remove_reputation(address,uint64)string']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(HustleScoreParamsFactory.removeReputation(params))
      return {...result, return: result.return as unknown as (undefined | HustleScoreReturns['remove_reputation(address,uint64)string'])}
    },

    /**
     * Makes a call to the HustleScore smart contract using the `get_total_minted()uint64` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * Get total reputation points minted.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    getTotalMinted: async (params: CallParams<HustleScoreArgs['obj']['get_total_minted()uint64'] | HustleScoreArgs['tuple']['get_total_minted()uint64']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(HustleScoreParamsFactory.getTotalMinted(params))
      return {...result, return: result.return as unknown as (undefined | HustleScoreReturns['get_total_minted()uint64'])}
    },

    /**
     * Makes a call to the HustleScore smart contract using the `get_admin()address` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * Get the admin address.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    getAdmin: async (params: CallParams<HustleScoreArgs['obj']['get_admin()address'] | HustleScoreArgs['tuple']['get_admin()address']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(HustleScoreParamsFactory.getAdmin(params))
      return {...result, return: result.return as unknown as (undefined | HustleScoreReturns['get_admin()address'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new HustleScoreClient(this.appClient.clone(params))
  }

  /**
   * Makes a readonly (simulated) call to the HustleScore smart contract using the `get_total_minted()uint64` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * Get total reputation points minted.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async getTotalMinted(params: CallParams<HustleScoreArgs['obj']['get_total_minted()uint64'] | HustleScoreArgs['tuple']['get_total_minted()uint64']> = {args: []}) {
    const result = await this.appClient.send.call(HustleScoreParamsFactory.getTotalMinted(params))
    return result.return as unknown as HustleScoreReturns['get_total_minted()uint64']
  }

  /**
   * Makes a readonly (simulated) call to the HustleScore smart contract using the `get_admin()address` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * Get the admin address.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async getAdmin(params: CallParams<HustleScoreArgs['obj']['get_admin()address'] | HustleScoreArgs['tuple']['get_admin()address']> = {args: []}) {
    const result = await this.appClient.send.call(HustleScoreParamsFactory.getAdmin(params))
    return result.return as unknown as HustleScoreReturns['get_admin()address']
  }

  /**
   * Methods to access state for the current HustleScore app
   */
  state = {
    /**
     * Methods to access global state for the current HustleScore app
     */
    global: {
      /**
       * Get all current keyed values from global state
       */
      getAll: async (): Promise<Partial<Expand<GlobalKeysState>>> => {
        const result = await this.appClient.state.global.getAll()
        return {
          admin: result.admin,
          totalMinted: result.total_minted,
        }
      },
      /**
       * Get the current value of the admin key in global state
       */
      admin: async (): Promise<string | undefined> => { return (await this.appClient.state.global.getValue("admin")) as string | undefined },
      /**
       * Get the current value of the total_minted key in global state
       */
      totalMinted: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("total_minted")) as bigint | undefined },
    },
  }

  public newGroup(): HustleScoreComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a add_reputation(address,uint64)string method call against the HustleScore contract
       */
      addReputation(params: CallParams<HustleScoreArgs['obj']['add_reputation(address,uint64)string'] | HustleScoreArgs['tuple']['add_reputation(address,uint64)string']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.addReputation(params)))
        resultMappers.push((v) => client.decodeReturnValue('add_reputation(address,uint64)string', v))
        return this
      },
      /**
       * Add a remove_reputation(address,uint64)string method call against the HustleScore contract
       */
      removeReputation(params: CallParams<HustleScoreArgs['obj']['remove_reputation(address,uint64)string'] | HustleScoreArgs['tuple']['remove_reputation(address,uint64)string']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.removeReputation(params)))
        resultMappers.push((v) => client.decodeReturnValue('remove_reputation(address,uint64)string', v))
        return this
      },
      /**
       * Add a get_total_minted()uint64 method call against the HustleScore contract
       */
      getTotalMinted(params: CallParams<HustleScoreArgs['obj']['get_total_minted()uint64'] | HustleScoreArgs['tuple']['get_total_minted()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.getTotalMinted(params)))
        resultMappers.push((v) => client.decodeReturnValue('get_total_minted()uint64', v))
        return this
      },
      /**
       * Add a get_admin()address method call against the HustleScore contract
       */
      getAdmin(params: CallParams<HustleScoreArgs['obj']['get_admin()address'] | HustleScoreArgs['tuple']['get_admin()address']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.getAdmin(params)))
        resultMappers.push((v) => client.decodeReturnValue('get_admin()address', v))
        return this
      },
      /**
       * Add a clear state call to the HustleScore contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as HustleScoreComposer
  }
}
export type HustleScoreComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the add_reputation(address,uint64)string ABI method.
   *
   * Add reputation points to a student (admin only).
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  addReputation(params?: CallParams<HustleScoreArgs['obj']['add_reputation(address,uint64)string'] | HustleScoreArgs['tuple']['add_reputation(address,uint64)string']>): HustleScoreComposer<[...TReturns, HustleScoreReturns['add_reputation(address,uint64)string'] | undefined]>

  /**
   * Calls the remove_reputation(address,uint64)string ABI method.
   *
   * Remove reputation points (for disputes, admin only).
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  removeReputation(params?: CallParams<HustleScoreArgs['obj']['remove_reputation(address,uint64)string'] | HustleScoreArgs['tuple']['remove_reputation(address,uint64)string']>): HustleScoreComposer<[...TReturns, HustleScoreReturns['remove_reputation(address,uint64)string'] | undefined]>

  /**
   * Calls the get_total_minted()uint64 ABI method.
   *
   * Get total reputation points minted.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  getTotalMinted(params?: CallParams<HustleScoreArgs['obj']['get_total_minted()uint64'] | HustleScoreArgs['tuple']['get_total_minted()uint64']>): HustleScoreComposer<[...TReturns, HustleScoreReturns['get_total_minted()uint64'] | undefined]>

  /**
   * Calls the get_admin()address ABI method.
   *
   * Get the admin address.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  getAdmin(params?: CallParams<HustleScoreArgs['obj']['get_admin()address'] | HustleScoreArgs['tuple']['get_admin()address']>): HustleScoreComposer<[...TReturns, HustleScoreReturns['get_admin()address'] | undefined]>

  /**
   * Makes a clear_state call to an existing instance of the HustleScore smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): HustleScoreComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): HustleScoreComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<HustleScoreComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<HustleScoreComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<HustleScoreComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<HustleScoreComposerResults<TReturns>>
}
export type HustleScoreComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

