/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"Reputation","structs":{},"methods":[{"name":"update_score","args":[{"type":"address","name":"seller","desc":"Algorand wallet address to score."},{"type":"uint64","name":"new_score","desc":"New reputation score (0-100 typically, no hard limit in contract)."}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Update or create a seller reputation score on-chain.\nPurpose: Record seller's current reputation (0-100 scale). Called during seller verification flow or post-purchase reputation updates.\nActions: 1. Store new_score in boxes under seller's address. 2. Overwrites any previous score (last-write-wins). 3. Emit event log for audit trail.","events":[],"recommendations":{}},{"name":"get_score","args":[{"type":"address","name":"seller","desc":"Algorand wallet address to look up."}],"returns":{"type":"uint64","desc":"Seller's reputation score (uint64). Returns 0 if seller not found (default: untrusted)."},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"desc":"Read a seller's current reputation score on-chain.\nPurpose: Public query for seller trust score. Called by buyer agent during evaluation. Readonly method (no state mutations, viewable via any algod node).","events":[],"recommendations":{}}],"arcs":[22,28],"desc":"On-chain seller reputation scoring system.\n    \n    State:\n        seller_scores: BoxMap(seller_address => score) storing seller trust scores (uint64).\n    \n    Purpose: Decentralized oracle for buyer purchase decisions + seller track record.\n    ","networks":{},"state":{"schema":{"global":{"ints":0,"bytes":0},"local":{"ints":0,"bytes":0}},"keys":{"global":{},"local":{},"box":{}},"maps":{"global":{},"local":{},"box":{"seller_scores":{"keyType":"address","valueType":"uint64","prefix":"cmVw"}}}},"bareActions":{"create":["NoOp"],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[60,85],"errorMessage":"invalid number of bytes for arc4.static_array<arc4.uint8, 32>"},{"pc":[69],"errorMessage":"invalid number of bytes for arc4.uint64"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"events":[]} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


/**
 * The argument types for the Reputation contract
 */
export type ReputationArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'update_score(address,uint64)void': {
      /**
       * Algorand wallet address to score.
       */
      seller: string
      /**
       * New reputation score (0-100 typically, no hard limit in contract).
       */
      newScore: bigint | number
    }
    'get_score(address)uint64': {
      /**
       * Algorand wallet address to look up.
       */
      seller: string
    }
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'update_score(address,uint64)void': [seller: string, newScore: bigint | number]
    'get_score(address)uint64': [seller: string]
  }
}

/**
 * The return type for each method
 */
export type ReputationReturns = {
  'update_score(address,uint64)void': void
  'get_score(address)uint64': bigint
}

/**
 * Defines the types of available calls and state of the Reputation smart contract.
 */
export type ReputationTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'update_score(address,uint64)void' | 'update_score', {
      argsObj: ReputationArgs['obj']['update_score(address,uint64)void']
      argsTuple: ReputationArgs['tuple']['update_score(address,uint64)void']
      returns: ReputationReturns['update_score(address,uint64)void']
    }>
    & Record<'get_score(address)uint64' | 'get_score', {
      argsObj: ReputationArgs['obj']['get_score(address)uint64']
      argsTuple: ReputationArgs['tuple']['get_score(address)uint64']
      /**
       * Seller's reputation score (uint64). Returns 0 if seller not found (default: untrusted).
       */
      returns: ReputationReturns['get_score(address)uint64']
    }>
  /**
   * Defines the shape of the state of the application.
   */
  state: {
    box: {
      keys: {}
      maps: {
        sellerScores: Map<string, bigint>
      }
    }
  }
}

/**
 * Defines the possible abi call signatures.
 */
export type ReputationSignatures = keyof ReputationTypes['methods']
/**
 * Defines the possible abi call signatures for methods that return a non-void value.
 */
export type ReputationNonVoidMethodSignatures = keyof ReputationTypes['methods'] extends infer T ? T extends keyof ReputationTypes['methods'] ? MethodReturn<T> extends void ? never : T  : never : never
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the Reputation smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends ReputationSignatures> = ReputationTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the Reputation smart contract to the method's return type
 */
export type MethodReturn<TSignature extends ReputationSignatures> = ReputationTypes['methods'][TSignature]['returns']

/**
 * Defines the shape of the keyed box state of the application.
 */
export type BoxKeysState = ReputationTypes['state']['box']['keys']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the Reputation smart contract
 */
export abstract class ReputationParamsFactory {
  /**
   * Constructs a no op call for the update_score(address,uint64)void ABI method
   *
  * Update or create a seller reputation score on-chain.
  Purpose: Record seller's current reputation (0-100 scale). Called during seller verification flow or post-purchase reputation updates.
  Actions: 1. Store new_score in boxes under seller's address. 2. Overwrites any previous score (last-write-wins). 3. Emit event log for audit trail.

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static updateScore(params: CallParams<ReputationArgs['obj']['update_score(address,uint64)void'] | ReputationArgs['tuple']['update_score(address,uint64)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'update_score(address,uint64)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.seller, params.args.newScore],
    }
  }
  /**
   * Constructs a no op call for the get_score(address)uint64 ABI method
   *
  * Read a seller's current reputation score on-chain.
  Purpose: Public query for seller trust score. Called by buyer agent during evaluation. Readonly method (no state mutations, viewable via any algod node).

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static getScore(params: CallParams<ReputationArgs['obj']['get_score(address)uint64'] | ReputationArgs['tuple']['get_score(address)uint64']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'get_score(address)uint64' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.seller],
    }
  }
}

/**
 * A client to make calls to the Reputation smart contract
 */
export class ReputationClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `ReputationClient`
   *
   * @param appClient An `AppClient` instance which has been created with the Reputation app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `ReputationClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Checks for decode errors on the given return value and maps the return value to the return type for the given method
   * @returns The typed return value or undefined if there was no value
   */
  decodeReturnValue<TSignature extends ReputationNonVoidMethodSignatures>(method: TSignature, returnValue: ABIReturn | undefined) {
    return returnValue !== undefined ? getArc56ReturnValue<MethodReturn<TSignature>>(returnValue, this.appClient.getABIMethod(method), APP_SPEC.structs) : undefined
  }

  /**
   * Returns a new `ReputationClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<ReputationClient> {
    return new ReputationClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `ReputationClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<ReputationClient> {
    return new ReputationClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Makes a clear_state call to an existing instance of the Reputation smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the Reputation smart contract using the `update_score(address,uint64)void` ABI method.
     *
    * Update or create a seller reputation score on-chain.
    Purpose: Record seller's current reputation (0-100 scale). Called during seller verification flow or post-purchase reputation updates.
    Actions: 1. Store new_score in boxes under seller's address. 2. Overwrites any previous score (last-write-wins). 3. Emit event log for audit trail.

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    updateScore: (params: CallParams<ReputationArgs['obj']['update_score(address,uint64)void'] | ReputationArgs['tuple']['update_score(address,uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(ReputationParamsFactory.updateScore(params))
    },

    /**
     * Makes a call to the Reputation smart contract using the `get_score(address)uint64` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
    * Read a seller's current reputation score on-chain.
    Purpose: Public query for seller trust score. Called by buyer agent during evaluation. Readonly method (no state mutations, viewable via any algod node).

     *
     * @param params The params for the smart contract call
     * @returns The call params: Seller's reputation score (uint64). Returns 0 if seller not found (default: untrusted).
     */
    getScore: (params: CallParams<ReputationArgs['obj']['get_score(address)uint64'] | ReputationArgs['tuple']['get_score(address)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(ReputationParamsFactory.getScore(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Makes a clear_state call to an existing instance of the Reputation smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the Reputation smart contract using the `update_score(address,uint64)void` ABI method.
     *
    * Update or create a seller reputation score on-chain.
    Purpose: Record seller's current reputation (0-100 scale). Called during seller verification flow or post-purchase reputation updates.
    Actions: 1. Store new_score in boxes under seller's address. 2. Overwrites any previous score (last-write-wins). 3. Emit event log for audit trail.

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    updateScore: (params: CallParams<ReputationArgs['obj']['update_score(address,uint64)void'] | ReputationArgs['tuple']['update_score(address,uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(ReputationParamsFactory.updateScore(params))
    },

    /**
     * Makes a call to the Reputation smart contract using the `get_score(address)uint64` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
    * Read a seller's current reputation score on-chain.
    Purpose: Public query for seller trust score. Called by buyer agent during evaluation. Readonly method (no state mutations, viewable via any algod node).

     *
     * @param params The params for the smart contract call
     * @returns The call transaction: Seller's reputation score (uint64). Returns 0 if seller not found (default: untrusted).
     */
    getScore: (params: CallParams<ReputationArgs['obj']['get_score(address)uint64'] | ReputationArgs['tuple']['get_score(address)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(ReputationParamsFactory.getScore(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Makes a clear_state call to an existing instance of the Reputation smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the Reputation smart contract using the `update_score(address,uint64)void` ABI method.
     *
    * Update or create a seller reputation score on-chain.
    Purpose: Record seller's current reputation (0-100 scale). Called during seller verification flow or post-purchase reputation updates.
    Actions: 1. Store new_score in boxes under seller's address. 2. Overwrites any previous score (last-write-wins). 3. Emit event log for audit trail.

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    updateScore: async (params: CallParams<ReputationArgs['obj']['update_score(address,uint64)void'] | ReputationArgs['tuple']['update_score(address,uint64)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(ReputationParamsFactory.updateScore(params))
      return {...result, return: result.return as unknown as (undefined | ReputationReturns['update_score(address,uint64)void'])}
    },

    /**
     * Makes a call to the Reputation smart contract using the `get_score(address)uint64` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
    * Read a seller's current reputation score on-chain.
    Purpose: Public query for seller trust score. Called by buyer agent during evaluation. Readonly method (no state mutations, viewable via any algod node).

     *
     * @param params The params for the smart contract call
     * @returns The call result: Seller's reputation score (uint64). Returns 0 if seller not found (default: untrusted).
     */
    getScore: async (params: CallParams<ReputationArgs['obj']['get_score(address)uint64'] | ReputationArgs['tuple']['get_score(address)uint64']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(ReputationParamsFactory.getScore(params))
      return {...result, return: result.return as unknown as (undefined | ReputationReturns['get_score(address)uint64'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new ReputationClient(this.appClient.clone(params))
  }

  /**
   * Makes a readonly (simulated) call to the Reputation smart contract using the `get_score(address)uint64` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
  * Read a seller's current reputation score on-chain.
  Purpose: Public query for seller trust score. Called by buyer agent during evaluation. Readonly method (no state mutations, viewable via any algod node).

   *
   * @param params The params for the smart contract call
   * @returns The call result: Seller's reputation score (uint64). Returns 0 if seller not found (default: untrusted).
   */
  async getScore(params: CallParams<ReputationArgs['obj']['get_score(address)uint64'] | ReputationArgs['tuple']['get_score(address)uint64']>) {
    const result = await this.appClient.send.call(ReputationParamsFactory.getScore(params))
    return result.return as unknown as ReputationReturns['get_score(address)uint64']
  }

  /**
   * Methods to access state for the current Reputation app
   */
  state = {
    /**
     * Methods to access box state for the current Reputation app
     */
    box: {
      /**
       * Get all current keyed values from box state
       */
      getAll: async (): Promise<Partial<Expand<BoxKeysState>>> => {
        const result = await this.appClient.state.box.getAll()
        return {
        }
      },
      /**
       * Get values from the seller_scores map in box state
       */
      sellerScores: {
        /**
         * Get all current values of the seller_scores map in box state
         */
        getMap: async (): Promise<Map<string, bigint>> => { return (await this.appClient.state.box.getMap("seller_scores")) as Map<string, bigint> },
        /**
         * Get a current value of the seller_scores map by key from box state
         */
        value: async (key: string): Promise<bigint | undefined> => { return await this.appClient.state.box.getMapValue("seller_scores", key) as bigint | undefined },
      },
    },
  }

  public newGroup(): ReputationComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a update_score(address,uint64)void method call against the Reputation contract
       */
      updateScore(params: CallParams<ReputationArgs['obj']['update_score(address,uint64)void'] | ReputationArgs['tuple']['update_score(address,uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.updateScore(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a get_score(address)uint64 method call against the Reputation contract
       */
      getScore(params: CallParams<ReputationArgs['obj']['get_score(address)uint64'] | ReputationArgs['tuple']['get_score(address)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.getScore(params)))
        resultMappers.push((v) => client.decodeReturnValue('get_score(address)uint64', v))
        return this
      },
      /**
       * Add a clear state call to the Reputation contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as ReputationComposer
  }
}
export type ReputationComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the update_score(address,uint64)void ABI method.
   *
  * Update or create a seller reputation score on-chain.
  Purpose: Record seller's current reputation (0-100 scale). Called during seller verification flow or post-purchase reputation updates.
  Actions: 1. Store new_score in boxes under seller's address. 2. Overwrites any previous score (last-write-wins). 3. Emit event log for audit trail.

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  updateScore(params?: CallParams<ReputationArgs['obj']['update_score(address,uint64)void'] | ReputationArgs['tuple']['update_score(address,uint64)void']>): ReputationComposer<[...TReturns, ReputationReturns['update_score(address,uint64)void'] | undefined]>

  /**
   * Calls the get_score(address)uint64 ABI method.
   *
  * Read a seller's current reputation score on-chain.
  Purpose: Public query for seller trust score. Called by buyer agent during evaluation. Readonly method (no state mutations, viewable via any algod node).

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  getScore(params?: CallParams<ReputationArgs['obj']['get_score(address)uint64'] | ReputationArgs['tuple']['get_score(address)uint64']>): ReputationComposer<[...TReturns, ReputationReturns['get_score(address)uint64'] | undefined]>

  /**
   * Makes a clear_state call to an existing instance of the Reputation smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): ReputationComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): ReputationComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<ReputationComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<ReputationComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<ReputationComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<ReputationComposerResults<TReturns>>
}
export type ReputationComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

