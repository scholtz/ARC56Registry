/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"InsightListing","structs":{"Listing":[{"name":"price","type":"uint64"},{"name":"seller","type":"address"},{"name":"ipfsHash","type":"string"},{"name":"asaId","type":"uint64"}]},"methods":[{"name":"create_listing","args":[{"type":"uint64","name":"price"},{"type":"address","name":"seller"},{"type":"string","name":"ipfs_hash"}],"returns":{"type":"uint64"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Publish a new trading insight and receive seller ASA for proof-of-ownership.\nPurpose: Allocate unique listing ID and link IPFS content permanently on-chain.\nActions: 1. Verify seller is registered in AgentRegistry before proceeding (cross-contract call). 2. Increment next_listing_id counter → allocate new listing ID. 3. Call itxn.AssetConfig to mint one ASA with:    - INSIGHT token name, Mercator Insight asset name, IpfsHash as URL.    - Seller as manager + reserve + freeze + clawback (seller controls proof token).    - Fee=1000 micro-Algo. 4. Store Listing struct in boxes: {price, seller, ipfs_hash, asa_id}. 5. Increment counter for next invocation. 6. Call AgentRegistry.increment_transaction_count to record seller activity. 7. Return listing_id to caller (frontend caches this).\nArgs:     price: Settlement amount (micro-USDC). e.g., 5_000_000 = 5 USDC.     seller: Seller's Algorand wallet (58-char checksummed address).     ipfs_hash: Content CID (e.g., \"Qm...\") pinned on Pinata.\nReturns:     listing_id: Unique identifier (used in /discover results, x402 payment, escrow).\nRaises:     AssertionError if seller is not registered in AgentRegistry.","events":[],"recommendations":{}}],"arcs":[22,28],"desc":"On-chain listing registry for trading insights.\n    \n    State:\n        registry_app_id: Global reference to AgentRegistry app (UInt64).\n        next_listing_id: Global counter allocating unique listing IDs (UInt64).\n        listings: BoxMap(listing_id => Listing), storing all current listings.\n    \n    Purpose: Seller-facing contract for publishing insights + buyer-facing registry for discovery.\n    ","networks":{},"state":{"schema":{"global":{"ints":2,"bytes":0},"local":{"ints":0,"bytes":0}},"keys":{"global":{"registry_app_id":{"keyType":"AVMString","valueType":"AVMUint64","key":"cmVnaXN0cnlfYXBwX2lk"},"next_listing_id":{"keyType":"AVMString","valueType":"AVMUint64","key":"bmV4dF9saXN0aW5nX2lk"}},"local":{},"box":{}},"maps":{"global":{},"local":{},"box":{"listings":{"keyType":"uint64","valueType":"Listing","prefix":"bGlzdGluZw=="}}}},"bareActions":{"create":["NoOp"],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[168],"errorMessage":"Unregistered agent — call AgentRegistry.register() first"},{"pc":[155],"errorMessage":"application log value is not the result of an ABI return"},{"pc":[172],"errorMessage":"check self.next_listing_id exists"},{"pc":[116,124],"errorMessage":"check self.registry_app_id exists"},{"pc":[105],"errorMessage":"invalid array length header"},{"pc":[163],"errorMessage":"invalid number of bytes for arc4.bool"},{"pc":[112],"errorMessage":"invalid number of bytes for arc4.dynamic_array<arc4.uint8>"},{"pc":[98],"errorMessage":"invalid number of bytes for arc4.static_array<arc4.uint8, 32>"},{"pc":[89],"errorMessage":"invalid number of bytes for arc4.uint64"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"events":[]} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


// Type definitions for ARC-56 structs

export type Listing = {
  price: bigint,
  seller: string,
  ipfsHash: string,
  asaId: bigint
}


/**
 * Converts the ABI tuple representation of a Listing to the struct representation
 */
export function ListingFromTuple(abiTuple: [bigint, string, string, bigint]) {
  return getABIStructFromABITuple(abiTuple, APP_SPEC.structs.Listing, APP_SPEC.structs) as Listing
}

/**
 * The argument types for the InsightListing contract
 */
export type InsightListingArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'create_listing(uint64,address,string)uint64': {
      price: bigint | number
      seller: string
      ipfsHash: string
    }
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'create_listing(uint64,address,string)uint64': [price: bigint | number, seller: string, ipfsHash: string]
  }
}

/**
 * The return type for each method
 */
export type InsightListingReturns = {
  'create_listing(uint64,address,string)uint64': bigint
}

/**
 * Defines the types of available calls and state of the InsightListing smart contract.
 */
export type InsightListingTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'create_listing(uint64,address,string)uint64' | 'create_listing', {
      argsObj: InsightListingArgs['obj']['create_listing(uint64,address,string)uint64']
      argsTuple: InsightListingArgs['tuple']['create_listing(uint64,address,string)uint64']
      returns: InsightListingReturns['create_listing(uint64,address,string)uint64']
    }>
  /**
   * Defines the shape of the state of the application.
   */
  state: {
    global: {
      keys: {
        registryAppId: bigint
        nextListingId: bigint
      }
      maps: {}
    }
    box: {
      keys: {}
      maps: {
        listings: Map<bigint | number, Listing>
      }
    }
  }
}

/**
 * Defines the possible abi call signatures.
 */
export type InsightListingSignatures = keyof InsightListingTypes['methods']
/**
 * Defines the possible abi call signatures for methods that return a non-void value.
 */
export type InsightListingNonVoidMethodSignatures = keyof InsightListingTypes['methods'] extends infer T ? T extends keyof InsightListingTypes['methods'] ? MethodReturn<T> extends void ? never : T  : never : never
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the InsightListing smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends InsightListingSignatures> = InsightListingTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the InsightListing smart contract to the method's return type
 */
export type MethodReturn<TSignature extends InsightListingSignatures> = InsightListingTypes['methods'][TSignature]['returns']

/**
 * Defines the shape of the keyed global state of the application.
 */
export type GlobalKeysState = InsightListingTypes['state']['global']['keys']

/**
 * Defines the shape of the keyed box state of the application.
 */
export type BoxKeysState = InsightListingTypes['state']['box']['keys']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the InsightListing smart contract
 */
export abstract class InsightListingParamsFactory {
  /**
   * Constructs a no op call for the create_listing(uint64,address,string)uint64 ABI method
   *
  * Publish a new trading insight and receive seller ASA for proof-of-ownership.
  Purpose: Allocate unique listing ID and link IPFS content permanently on-chain.
  Actions: 1. Verify seller is registered in AgentRegistry before proceeding (cross-contract call). 2. Increment next_listing_id counter → allocate new listing ID. 3. Call itxn.AssetConfig to mint one ASA with:    - INSIGHT token name, Mercator Insight asset name, IpfsHash as URL.    - Seller as manager + reserve + freeze + clawback (seller controls proof token).    - Fee=1000 micro-Algo. 4. Store Listing struct in boxes: {price, seller, ipfs_hash, asa_id}. 5. Increment counter for next invocation. 6. Call AgentRegistry.increment_transaction_count to record seller activity. 7. Return listing_id to caller (frontend caches this).
  Args:     price: Settlement amount (micro-USDC). e.g., 5_000_000 = 5 USDC.     seller: Seller's Algorand wallet (58-char checksummed address).     ipfs_hash: Content CID (e.g., "Qm...") pinned on Pinata.
  Returns:     listing_id: Unique identifier (used in /discover results, x402 payment, escrow).
  Raises:     AssertionError if seller is not registered in AgentRegistry.

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static createListing(params: CallParams<InsightListingArgs['obj']['create_listing(uint64,address,string)uint64'] | InsightListingArgs['tuple']['create_listing(uint64,address,string)uint64']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'create_listing(uint64,address,string)uint64' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.price, params.args.seller, params.args.ipfsHash],
    }
  }
}

/**
 * A client to make calls to the InsightListing smart contract
 */
export class InsightListingClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `InsightListingClient`
   *
   * @param appClient An `AppClient` instance which has been created with the InsightListing app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `InsightListingClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Checks for decode errors on the given return value and maps the return value to the return type for the given method
   * @returns The typed return value or undefined if there was no value
   */
  decodeReturnValue<TSignature extends InsightListingNonVoidMethodSignatures>(method: TSignature, returnValue: ABIReturn | undefined) {
    return returnValue !== undefined ? getArc56ReturnValue<MethodReturn<TSignature>>(returnValue, this.appClient.getABIMethod(method), APP_SPEC.structs) : undefined
  }

  /**
   * Returns a new `InsightListingClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<InsightListingClient> {
    return new InsightListingClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `InsightListingClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<InsightListingClient> {
    return new InsightListingClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Makes a clear_state call to an existing instance of the InsightListing smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the InsightListing smart contract using the `create_listing(uint64,address,string)uint64` ABI method.
     *
    * Publish a new trading insight and receive seller ASA for proof-of-ownership.
    Purpose: Allocate unique listing ID and link IPFS content permanently on-chain.
    Actions: 1. Verify seller is registered in AgentRegistry before proceeding (cross-contract call). 2. Increment next_listing_id counter → allocate new listing ID. 3. Call itxn.AssetConfig to mint one ASA with:    - INSIGHT token name, Mercator Insight asset name, IpfsHash as URL.    - Seller as manager + reserve + freeze + clawback (seller controls proof token).    - Fee=1000 micro-Algo. 4. Store Listing struct in boxes: {price, seller, ipfs_hash, asa_id}. 5. Increment counter for next invocation. 6. Call AgentRegistry.increment_transaction_count to record seller activity. 7. Return listing_id to caller (frontend caches this).
    Args:     price: Settlement amount (micro-USDC). e.g., 5_000_000 = 5 USDC.     seller: Seller's Algorand wallet (58-char checksummed address).     ipfs_hash: Content CID (e.g., "Qm...") pinned on Pinata.
    Returns:     listing_id: Unique identifier (used in /discover results, x402 payment, escrow).
    Raises:     AssertionError if seller is not registered in AgentRegistry.

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    createListing: (params: CallParams<InsightListingArgs['obj']['create_listing(uint64,address,string)uint64'] | InsightListingArgs['tuple']['create_listing(uint64,address,string)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(InsightListingParamsFactory.createListing(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Makes a clear_state call to an existing instance of the InsightListing smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the InsightListing smart contract using the `create_listing(uint64,address,string)uint64` ABI method.
     *
    * Publish a new trading insight and receive seller ASA for proof-of-ownership.
    Purpose: Allocate unique listing ID and link IPFS content permanently on-chain.
    Actions: 1. Verify seller is registered in AgentRegistry before proceeding (cross-contract call). 2. Increment next_listing_id counter → allocate new listing ID. 3. Call itxn.AssetConfig to mint one ASA with:    - INSIGHT token name, Mercator Insight asset name, IpfsHash as URL.    - Seller as manager + reserve + freeze + clawback (seller controls proof token).    - Fee=1000 micro-Algo. 4. Store Listing struct in boxes: {price, seller, ipfs_hash, asa_id}. 5. Increment counter for next invocation. 6. Call AgentRegistry.increment_transaction_count to record seller activity. 7. Return listing_id to caller (frontend caches this).
    Args:     price: Settlement amount (micro-USDC). e.g., 5_000_000 = 5 USDC.     seller: Seller's Algorand wallet (58-char checksummed address).     ipfs_hash: Content CID (e.g., "Qm...") pinned on Pinata.
    Returns:     listing_id: Unique identifier (used in /discover results, x402 payment, escrow).
    Raises:     AssertionError if seller is not registered in AgentRegistry.

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    createListing: (params: CallParams<InsightListingArgs['obj']['create_listing(uint64,address,string)uint64'] | InsightListingArgs['tuple']['create_listing(uint64,address,string)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(InsightListingParamsFactory.createListing(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Makes a clear_state call to an existing instance of the InsightListing smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the InsightListing smart contract using the `create_listing(uint64,address,string)uint64` ABI method.
     *
    * Publish a new trading insight and receive seller ASA for proof-of-ownership.
    Purpose: Allocate unique listing ID and link IPFS content permanently on-chain.
    Actions: 1. Verify seller is registered in AgentRegistry before proceeding (cross-contract call). 2. Increment next_listing_id counter → allocate new listing ID. 3. Call itxn.AssetConfig to mint one ASA with:    - INSIGHT token name, Mercator Insight asset name, IpfsHash as URL.    - Seller as manager + reserve + freeze + clawback (seller controls proof token).    - Fee=1000 micro-Algo. 4. Store Listing struct in boxes: {price, seller, ipfs_hash, asa_id}. 5. Increment counter for next invocation. 6. Call AgentRegistry.increment_transaction_count to record seller activity. 7. Return listing_id to caller (frontend caches this).
    Args:     price: Settlement amount (micro-USDC). e.g., 5_000_000 = 5 USDC.     seller: Seller's Algorand wallet (58-char checksummed address).     ipfs_hash: Content CID (e.g., "Qm...") pinned on Pinata.
    Returns:     listing_id: Unique identifier (used in /discover results, x402 payment, escrow).
    Raises:     AssertionError if seller is not registered in AgentRegistry.

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    createListing: async (params: CallParams<InsightListingArgs['obj']['create_listing(uint64,address,string)uint64'] | InsightListingArgs['tuple']['create_listing(uint64,address,string)uint64']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(InsightListingParamsFactory.createListing(params))
      return {...result, return: result.return as unknown as (undefined | InsightListingReturns['create_listing(uint64,address,string)uint64'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new InsightListingClient(this.appClient.clone(params))
  }

  /**
   * Methods to access state for the current InsightListing app
   */
  state = {
    /**
     * Methods to access global state for the current InsightListing app
     */
    global: {
      /**
       * Get all current keyed values from global state
       */
      getAll: async (): Promise<Partial<Expand<GlobalKeysState>>> => {
        const result = await this.appClient.state.global.getAll()
        return {
          registryAppId: result.registry_app_id,
          nextListingId: result.next_listing_id,
        }
      },
      /**
       * Get the current value of the registry_app_id key in global state
       */
      registryAppId: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("registry_app_id")) as bigint | undefined },
      /**
       * Get the current value of the next_listing_id key in global state
       */
      nextListingId: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("next_listing_id")) as bigint | undefined },
    },
    /**
     * Methods to access box state for the current InsightListing app
     */
    box: {
      /**
       * Get all current keyed values from box state
       */
      getAll: async (): Promise<Partial<Expand<BoxKeysState>>> => {
        const result = await this.appClient.state.box.getAll()
        return {
        }
      },
      /**
       * Get values from the listings map in box state
       */
      listings: {
        /**
         * Get all current values of the listings map in box state
         */
        getMap: async (): Promise<Map<bigint, Listing>> => { return (await this.appClient.state.box.getMap("listings")) as Map<bigint, Listing> },
        /**
         * Get a current value of the listings map by key from box state
         */
        value: async (key: bigint | number): Promise<Listing | undefined> => { return await this.appClient.state.box.getMapValue("listings", key) as Listing | undefined },
      },
    },
  }

  public newGroup(): InsightListingComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a create_listing(uint64,address,string)uint64 method call against the InsightListing contract
       */
      createListing(params: CallParams<InsightListingArgs['obj']['create_listing(uint64,address,string)uint64'] | InsightListingArgs['tuple']['create_listing(uint64,address,string)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.createListing(params)))
        resultMappers.push((v) => client.decodeReturnValue('create_listing(uint64,address,string)uint64', v))
        return this
      },
      /**
       * Add a clear state call to the InsightListing contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as InsightListingComposer
  }
}
export type InsightListingComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the create_listing(uint64,address,string)uint64 ABI method.
   *
  * Publish a new trading insight and receive seller ASA for proof-of-ownership.
  Purpose: Allocate unique listing ID and link IPFS content permanently on-chain.
  Actions: 1. Verify seller is registered in AgentRegistry before proceeding (cross-contract call). 2. Increment next_listing_id counter → allocate new listing ID. 3. Call itxn.AssetConfig to mint one ASA with:    - INSIGHT token name, Mercator Insight asset name, IpfsHash as URL.    - Seller as manager + reserve + freeze + clawback (seller controls proof token).    - Fee=1000 micro-Algo. 4. Store Listing struct in boxes: {price, seller, ipfs_hash, asa_id}. 5. Increment counter for next invocation. 6. Call AgentRegistry.increment_transaction_count to record seller activity. 7. Return listing_id to caller (frontend caches this).
  Args:     price: Settlement amount (micro-USDC). e.g., 5_000_000 = 5 USDC.     seller: Seller's Algorand wallet (58-char checksummed address).     ipfs_hash: Content CID (e.g., "Qm...") pinned on Pinata.
  Returns:     listing_id: Unique identifier (used in /discover results, x402 payment, escrow).
  Raises:     AssertionError if seller is not registered in AgentRegistry.

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  createListing(params?: CallParams<InsightListingArgs['obj']['create_listing(uint64,address,string)uint64'] | InsightListingArgs['tuple']['create_listing(uint64,address,string)uint64']>): InsightListingComposer<[...TReturns, InsightListingReturns['create_listing(uint64,address,string)uint64'] | undefined]>

  /**
   * Makes a clear_state call to an existing instance of the InsightListing smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): InsightListingComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): InsightListingComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<InsightListingComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<InsightListingComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<InsightListingComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<InsightListingComposerResults<TReturns>>
}
export type InsightListingComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

