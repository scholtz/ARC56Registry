/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"HelloWorld","structs":{},"methods":[{"name":"create_application","args":[],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Initialize application state on creation","events":[],"recommendations":{}},{"name":"store_tender_hash","args":[{"type":"string","name":"tender_id"},{"type":"string","name":"hash_value"}],"returns":{"type":"string"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Store tender hash on blockchain with state management\nBlockchain Features: - Global state storage for counters - Transaction validation - Immutable record keeping - Access control via transaction sender","events":[],"recommendations":{}},{"name":"store_bid_hash","args":[{"type":"string","name":"bid_id"},{"type":"string","name":"tender_id"},{"type":"string","name":"hash_value"}],"returns":{"type":"string"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Store bid hash with tender linkage and state tracking\nBlockchain Features: - Global state management - Transaction validation - Cryptographic hash storage - Immutable audit trail","events":[],"recommendations":{}},{"name":"verify_hash","args":[{"type":"string","name":"record_id"},{"type":"string","name":"hash_value"}],"returns":{"type":"string"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Cryptographic verification of hash integrity\nBlockchain Features: - Hash comparison for data integrity - Immutability verification - Transaction-based validation","events":[],"recommendations":{}},{"name":"get_stats","args":[],"returns":{"type":"string"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Get blockchain statistics from global state\nDemonstrates: - Global state reading - On-chain data persistence - State management","events":[],"recommendations":{}}],"arcs":[22,28],"desc":"TenderVault Smart Contract - Immutable Tender and Bid Storage with Cryptographic Verification","networks":{},"state":{"schema":{"global":{"ints":2,"bytes":0},"local":{"ints":0,"bytes":0}},"keys":{"global":{"tender_count":{"keyType":"AVMString","valueType":"AVMUint64","key":"dGVuZGVyX2NvdW50"},"bid_count":{"keyType":"AVMString","valueType":"AVMUint64","key":"YmlkX2NvdW50"}},"local":{},"box":{}},"maps":{"global":{},"local":{},"box":{}}},"bareActions":{"create":["NoOp"],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[284,474],"errorMessage":"check self.bid_count exists"},{"pc":[163,469],"errorMessage":"check self.tender_count exists"},{"pc":[133,149,238,254,270,362,378],"errorMessage":"invalid array length header"},{"pc":[140,156,245,261,277,369,385],"errorMessage":"invalid number of bytes for arc4.dynamic_array<arc4.uint8>"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"events":[]} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


/**
 * The argument types for the HelloWorld contract
 */
export type HelloWorldArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'create_application()void': Record<string, never>
    'store_tender_hash(string,string)string': {
      tenderId: string
      hashValue: string
    }
    'store_bid_hash(string,string,string)string': {
      bidId: string
      tenderId: string
      hashValue: string
    }
    'verify_hash(string,string)string': {
      recordId: string
      hashValue: string
    }
    'get_stats()string': Record<string, never>
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'create_application()void': []
    'store_tender_hash(string,string)string': [tenderId: string, hashValue: string]
    'store_bid_hash(string,string,string)string': [bidId: string, tenderId: string, hashValue: string]
    'verify_hash(string,string)string': [recordId: string, hashValue: string]
    'get_stats()string': []
  }
}

/**
 * The return type for each method
 */
export type HelloWorldReturns = {
  'create_application()void': void
  'store_tender_hash(string,string)string': string
  'store_bid_hash(string,string,string)string': string
  'verify_hash(string,string)string': string
  'get_stats()string': string
}

/**
 * Defines the types of available calls and state of the HelloWorld smart contract.
 */
export type HelloWorldTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'create_application()void' | 'create_application', {
      argsObj: HelloWorldArgs['obj']['create_application()void']
      argsTuple: HelloWorldArgs['tuple']['create_application()void']
      returns: HelloWorldReturns['create_application()void']
    }>
    & Record<'store_tender_hash(string,string)string' | 'store_tender_hash', {
      argsObj: HelloWorldArgs['obj']['store_tender_hash(string,string)string']
      argsTuple: HelloWorldArgs['tuple']['store_tender_hash(string,string)string']
      returns: HelloWorldReturns['store_tender_hash(string,string)string']
    }>
    & Record<'store_bid_hash(string,string,string)string' | 'store_bid_hash', {
      argsObj: HelloWorldArgs['obj']['store_bid_hash(string,string,string)string']
      argsTuple: HelloWorldArgs['tuple']['store_bid_hash(string,string,string)string']
      returns: HelloWorldReturns['store_bid_hash(string,string,string)string']
    }>
    & Record<'verify_hash(string,string)string' | 'verify_hash', {
      argsObj: HelloWorldArgs['obj']['verify_hash(string,string)string']
      argsTuple: HelloWorldArgs['tuple']['verify_hash(string,string)string']
      returns: HelloWorldReturns['verify_hash(string,string)string']
    }>
    & Record<'get_stats()string' | 'get_stats', {
      argsObj: HelloWorldArgs['obj']['get_stats()string']
      argsTuple: HelloWorldArgs['tuple']['get_stats()string']
      returns: HelloWorldReturns['get_stats()string']
    }>
  /**
   * Defines the shape of the state of the application.
   */
  state: {
    global: {
      keys: {
        tenderCount: bigint
        bidCount: bigint
      }
      maps: {}
    }
  }
}

/**
 * Defines the possible abi call signatures.
 */
export type HelloWorldSignatures = keyof HelloWorldTypes['methods']
/**
 * Defines the possible abi call signatures for methods that return a non-void value.
 */
export type HelloWorldNonVoidMethodSignatures = keyof HelloWorldTypes['methods'] extends infer T ? T extends keyof HelloWorldTypes['methods'] ? MethodReturn<T> extends void ? never : T  : never : never
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the HelloWorld smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends HelloWorldSignatures> = HelloWorldTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the HelloWorld smart contract to the method's return type
 */
export type MethodReturn<TSignature extends HelloWorldSignatures> = HelloWorldTypes['methods'][TSignature]['returns']

/**
 * Defines the shape of the keyed global state of the application.
 */
export type GlobalKeysState = HelloWorldTypes['state']['global']['keys']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the HelloWorld smart contract
 */
export abstract class HelloWorldParamsFactory {
  /**
   * Constructs a no op call for the create_application()void ABI method
   *
   * Initialize application state on creation
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static createApplication(params: CallParams<HelloWorldArgs['obj']['create_application()void'] | HelloWorldArgs['tuple']['create_application()void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'create_application()void' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the store_tender_hash(string,string)string ABI method
   *
  * Store tender hash on blockchain with state management
  Blockchain Features: - Global state storage for counters - Transaction validation - Immutable record keeping - Access control via transaction sender

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static storeTenderHash(params: CallParams<HelloWorldArgs['obj']['store_tender_hash(string,string)string'] | HelloWorldArgs['tuple']['store_tender_hash(string,string)string']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'store_tender_hash(string,string)string' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.tenderId, params.args.hashValue],
    }
  }
  /**
   * Constructs a no op call for the store_bid_hash(string,string,string)string ABI method
   *
  * Store bid hash with tender linkage and state tracking
  Blockchain Features: - Global state management - Transaction validation - Cryptographic hash storage - Immutable audit trail

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static storeBidHash(params: CallParams<HelloWorldArgs['obj']['store_bid_hash(string,string,string)string'] | HelloWorldArgs['tuple']['store_bid_hash(string,string,string)string']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'store_bid_hash(string,string,string)string' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.bidId, params.args.tenderId, params.args.hashValue],
    }
  }
  /**
   * Constructs a no op call for the verify_hash(string,string)string ABI method
   *
  * Cryptographic verification of hash integrity
  Blockchain Features: - Hash comparison for data integrity - Immutability verification - Transaction-based validation

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static verifyHash(params: CallParams<HelloWorldArgs['obj']['verify_hash(string,string)string'] | HelloWorldArgs['tuple']['verify_hash(string,string)string']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'verify_hash(string,string)string' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.recordId, params.args.hashValue],
    }
  }
  /**
   * Constructs a no op call for the get_stats()string ABI method
   *
  * Get blockchain statistics from global state
  Demonstrates: - Global state reading - On-chain data persistence - State management

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static getStats(params: CallParams<HelloWorldArgs['obj']['get_stats()string'] | HelloWorldArgs['tuple']['get_stats()string']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'get_stats()string' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
}

/**
 * A client to make calls to the HelloWorld smart contract
 */
export class HelloWorldClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `HelloWorldClient`
   *
   * @param appClient An `AppClient` instance which has been created with the HelloWorld app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `HelloWorldClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Checks for decode errors on the given return value and maps the return value to the return type for the given method
   * @returns The typed return value or undefined if there was no value
   */
  decodeReturnValue<TSignature extends HelloWorldNonVoidMethodSignatures>(method: TSignature, returnValue: ABIReturn | undefined) {
    return returnValue !== undefined ? getArc56ReturnValue<MethodReturn<TSignature>>(returnValue, this.appClient.getABIMethod(method), APP_SPEC.structs) : undefined
  }

  /**
   * Returns a new `HelloWorldClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<HelloWorldClient> {
    return new HelloWorldClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `HelloWorldClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<HelloWorldClient> {
    return new HelloWorldClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Makes a clear_state call to an existing instance of the HelloWorld smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the HelloWorld smart contract using the `create_application()void` ABI method.
     *
     * Initialize application state on creation
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    createApplication: (params: CallParams<HelloWorldArgs['obj']['create_application()void'] | HelloWorldArgs['tuple']['create_application()void']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(HelloWorldParamsFactory.createApplication(params))
    },

    /**
     * Makes a call to the HelloWorld smart contract using the `store_tender_hash(string,string)string` ABI method.
     *
    * Store tender hash on blockchain with state management
    Blockchain Features: - Global state storage for counters - Transaction validation - Immutable record keeping - Access control via transaction sender

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    storeTenderHash: (params: CallParams<HelloWorldArgs['obj']['store_tender_hash(string,string)string'] | HelloWorldArgs['tuple']['store_tender_hash(string,string)string']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(HelloWorldParamsFactory.storeTenderHash(params))
    },

    /**
     * Makes a call to the HelloWorld smart contract using the `store_bid_hash(string,string,string)string` ABI method.
     *
    * Store bid hash with tender linkage and state tracking
    Blockchain Features: - Global state management - Transaction validation - Cryptographic hash storage - Immutable audit trail

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    storeBidHash: (params: CallParams<HelloWorldArgs['obj']['store_bid_hash(string,string,string)string'] | HelloWorldArgs['tuple']['store_bid_hash(string,string,string)string']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(HelloWorldParamsFactory.storeBidHash(params))
    },

    /**
     * Makes a call to the HelloWorld smart contract using the `verify_hash(string,string)string` ABI method.
     *
    * Cryptographic verification of hash integrity
    Blockchain Features: - Hash comparison for data integrity - Immutability verification - Transaction-based validation

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    verifyHash: (params: CallParams<HelloWorldArgs['obj']['verify_hash(string,string)string'] | HelloWorldArgs['tuple']['verify_hash(string,string)string']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(HelloWorldParamsFactory.verifyHash(params))
    },

    /**
     * Makes a call to the HelloWorld smart contract using the `get_stats()string` ABI method.
     *
    * Get blockchain statistics from global state
    Demonstrates: - Global state reading - On-chain data persistence - State management

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    getStats: (params: CallParams<HelloWorldArgs['obj']['get_stats()string'] | HelloWorldArgs['tuple']['get_stats()string']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(HelloWorldParamsFactory.getStats(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Makes a clear_state call to an existing instance of the HelloWorld smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the HelloWorld smart contract using the `create_application()void` ABI method.
     *
     * Initialize application state on creation
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    createApplication: (params: CallParams<HelloWorldArgs['obj']['create_application()void'] | HelloWorldArgs['tuple']['create_application()void']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(HelloWorldParamsFactory.createApplication(params))
    },

    /**
     * Makes a call to the HelloWorld smart contract using the `store_tender_hash(string,string)string` ABI method.
     *
    * Store tender hash on blockchain with state management
    Blockchain Features: - Global state storage for counters - Transaction validation - Immutable record keeping - Access control via transaction sender

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    storeTenderHash: (params: CallParams<HelloWorldArgs['obj']['store_tender_hash(string,string)string'] | HelloWorldArgs['tuple']['store_tender_hash(string,string)string']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(HelloWorldParamsFactory.storeTenderHash(params))
    },

    /**
     * Makes a call to the HelloWorld smart contract using the `store_bid_hash(string,string,string)string` ABI method.
     *
    * Store bid hash with tender linkage and state tracking
    Blockchain Features: - Global state management - Transaction validation - Cryptographic hash storage - Immutable audit trail

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    storeBidHash: (params: CallParams<HelloWorldArgs['obj']['store_bid_hash(string,string,string)string'] | HelloWorldArgs['tuple']['store_bid_hash(string,string,string)string']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(HelloWorldParamsFactory.storeBidHash(params))
    },

    /**
     * Makes a call to the HelloWorld smart contract using the `verify_hash(string,string)string` ABI method.
     *
    * Cryptographic verification of hash integrity
    Blockchain Features: - Hash comparison for data integrity - Immutability verification - Transaction-based validation

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    verifyHash: (params: CallParams<HelloWorldArgs['obj']['verify_hash(string,string)string'] | HelloWorldArgs['tuple']['verify_hash(string,string)string']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(HelloWorldParamsFactory.verifyHash(params))
    },

    /**
     * Makes a call to the HelloWorld smart contract using the `get_stats()string` ABI method.
     *
    * Get blockchain statistics from global state
    Demonstrates: - Global state reading - On-chain data persistence - State management

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    getStats: (params: CallParams<HelloWorldArgs['obj']['get_stats()string'] | HelloWorldArgs['tuple']['get_stats()string']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(HelloWorldParamsFactory.getStats(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Makes a clear_state call to an existing instance of the HelloWorld smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the HelloWorld smart contract using the `create_application()void` ABI method.
     *
     * Initialize application state on creation
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    createApplication: async (params: CallParams<HelloWorldArgs['obj']['create_application()void'] | HelloWorldArgs['tuple']['create_application()void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(HelloWorldParamsFactory.createApplication(params))
      return {...result, return: result.return as unknown as (undefined | HelloWorldReturns['create_application()void'])}
    },

    /**
     * Makes a call to the HelloWorld smart contract using the `store_tender_hash(string,string)string` ABI method.
     *
    * Store tender hash on blockchain with state management
    Blockchain Features: - Global state storage for counters - Transaction validation - Immutable record keeping - Access control via transaction sender

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    storeTenderHash: async (params: CallParams<HelloWorldArgs['obj']['store_tender_hash(string,string)string'] | HelloWorldArgs['tuple']['store_tender_hash(string,string)string']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(HelloWorldParamsFactory.storeTenderHash(params))
      return {...result, return: result.return as unknown as (undefined | HelloWorldReturns['store_tender_hash(string,string)string'])}
    },

    /**
     * Makes a call to the HelloWorld smart contract using the `store_bid_hash(string,string,string)string` ABI method.
     *
    * Store bid hash with tender linkage and state tracking
    Blockchain Features: - Global state management - Transaction validation - Cryptographic hash storage - Immutable audit trail

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    storeBidHash: async (params: CallParams<HelloWorldArgs['obj']['store_bid_hash(string,string,string)string'] | HelloWorldArgs['tuple']['store_bid_hash(string,string,string)string']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(HelloWorldParamsFactory.storeBidHash(params))
      return {...result, return: result.return as unknown as (undefined | HelloWorldReturns['store_bid_hash(string,string,string)string'])}
    },

    /**
     * Makes a call to the HelloWorld smart contract using the `verify_hash(string,string)string` ABI method.
     *
    * Cryptographic verification of hash integrity
    Blockchain Features: - Hash comparison for data integrity - Immutability verification - Transaction-based validation

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    verifyHash: async (params: CallParams<HelloWorldArgs['obj']['verify_hash(string,string)string'] | HelloWorldArgs['tuple']['verify_hash(string,string)string']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(HelloWorldParamsFactory.verifyHash(params))
      return {...result, return: result.return as unknown as (undefined | HelloWorldReturns['verify_hash(string,string)string'])}
    },

    /**
     * Makes a call to the HelloWorld smart contract using the `get_stats()string` ABI method.
     *
    * Get blockchain statistics from global state
    Demonstrates: - Global state reading - On-chain data persistence - State management

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    getStats: async (params: CallParams<HelloWorldArgs['obj']['get_stats()string'] | HelloWorldArgs['tuple']['get_stats()string']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(HelloWorldParamsFactory.getStats(params))
      return {...result, return: result.return as unknown as (undefined | HelloWorldReturns['get_stats()string'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new HelloWorldClient(this.appClient.clone(params))
  }

  /**
   * Methods to access state for the current HelloWorld app
   */
  state = {
    /**
     * Methods to access global state for the current HelloWorld app
     */
    global: {
      /**
       * Get all current keyed values from global state
       */
      getAll: async (): Promise<Partial<Expand<GlobalKeysState>>> => {
        const result = await this.appClient.state.global.getAll()
        return {
          tenderCount: result.tender_count,
          bidCount: result.bid_count,
        }
      },
      /**
       * Get the current value of the tender_count key in global state
       */
      tenderCount: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("tender_count")) as bigint | undefined },
      /**
       * Get the current value of the bid_count key in global state
       */
      bidCount: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("bid_count")) as bigint | undefined },
    },
  }

  public newGroup(): HelloWorldComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a create_application()void method call against the HelloWorld contract
       */
      createApplication(params: CallParams<HelloWorldArgs['obj']['create_application()void'] | HelloWorldArgs['tuple']['create_application()void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.createApplication(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a store_tender_hash(string,string)string method call against the HelloWorld contract
       */
      storeTenderHash(params: CallParams<HelloWorldArgs['obj']['store_tender_hash(string,string)string'] | HelloWorldArgs['tuple']['store_tender_hash(string,string)string']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.storeTenderHash(params)))
        resultMappers.push((v) => client.decodeReturnValue('store_tender_hash(string,string)string', v))
        return this
      },
      /**
       * Add a store_bid_hash(string,string,string)string method call against the HelloWorld contract
       */
      storeBidHash(params: CallParams<HelloWorldArgs['obj']['store_bid_hash(string,string,string)string'] | HelloWorldArgs['tuple']['store_bid_hash(string,string,string)string']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.storeBidHash(params)))
        resultMappers.push((v) => client.decodeReturnValue('store_bid_hash(string,string,string)string', v))
        return this
      },
      /**
       * Add a verify_hash(string,string)string method call against the HelloWorld contract
       */
      verifyHash(params: CallParams<HelloWorldArgs['obj']['verify_hash(string,string)string'] | HelloWorldArgs['tuple']['verify_hash(string,string)string']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.verifyHash(params)))
        resultMappers.push((v) => client.decodeReturnValue('verify_hash(string,string)string', v))
        return this
      },
      /**
       * Add a get_stats()string method call against the HelloWorld contract
       */
      getStats(params: CallParams<HelloWorldArgs['obj']['get_stats()string'] | HelloWorldArgs['tuple']['get_stats()string']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.getStats(params)))
        resultMappers.push((v) => client.decodeReturnValue('get_stats()string', v))
        return this
      },
      /**
       * Add a clear state call to the HelloWorld contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as HelloWorldComposer
  }
}
export type HelloWorldComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the create_application()void ABI method.
   *
   * Initialize application state on creation
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  createApplication(params?: CallParams<HelloWorldArgs['obj']['create_application()void'] | HelloWorldArgs['tuple']['create_application()void']>): HelloWorldComposer<[...TReturns, HelloWorldReturns['create_application()void'] | undefined]>

  /**
   * Calls the store_tender_hash(string,string)string ABI method.
   *
  * Store tender hash on blockchain with state management
  Blockchain Features: - Global state storage for counters - Transaction validation - Immutable record keeping - Access control via transaction sender

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  storeTenderHash(params?: CallParams<HelloWorldArgs['obj']['store_tender_hash(string,string)string'] | HelloWorldArgs['tuple']['store_tender_hash(string,string)string']>): HelloWorldComposer<[...TReturns, HelloWorldReturns['store_tender_hash(string,string)string'] | undefined]>

  /**
   * Calls the store_bid_hash(string,string,string)string ABI method.
   *
  * Store bid hash with tender linkage and state tracking
  Blockchain Features: - Global state management - Transaction validation - Cryptographic hash storage - Immutable audit trail

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  storeBidHash(params?: CallParams<HelloWorldArgs['obj']['store_bid_hash(string,string,string)string'] | HelloWorldArgs['tuple']['store_bid_hash(string,string,string)string']>): HelloWorldComposer<[...TReturns, HelloWorldReturns['store_bid_hash(string,string,string)string'] | undefined]>

  /**
   * Calls the verify_hash(string,string)string ABI method.
   *
  * Cryptographic verification of hash integrity
  Blockchain Features: - Hash comparison for data integrity - Immutability verification - Transaction-based validation

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  verifyHash(params?: CallParams<HelloWorldArgs['obj']['verify_hash(string,string)string'] | HelloWorldArgs['tuple']['verify_hash(string,string)string']>): HelloWorldComposer<[...TReturns, HelloWorldReturns['verify_hash(string,string)string'] | undefined]>

  /**
   * Calls the get_stats()string ABI method.
   *
  * Get blockchain statistics from global state
  Demonstrates: - Global state reading - On-chain data persistence - State management

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  getStats(params?: CallParams<HelloWorldArgs['obj']['get_stats()string'] | HelloWorldArgs['tuple']['get_stats()string']>): HelloWorldComposer<[...TReturns, HelloWorldReturns['get_stats()string'] | undefined]>

  /**
   * Makes a clear_state call to an existing instance of the HelloWorld smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): HelloWorldComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): HelloWorldComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<HelloWorldComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<HelloWorldComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<HelloWorldComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<HelloWorldComposerResults<TReturns>>
}
export type HelloWorldComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

