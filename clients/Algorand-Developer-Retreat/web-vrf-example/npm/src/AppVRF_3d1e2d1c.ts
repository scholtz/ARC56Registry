/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"AppVRF","structs":{},"methods":[{"name":"config","args":[{"type":"account","name":"publicVrfKey"},{"type":"account","name":"lsigAddress"},{"type":"uint64","name":"randomRange"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}},{"name":"commit","args":[{"type":"uint64","name":"block"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}},{"name":"verify","args":[{"type":"byte[]","name":"message"},{"type":"byte[]","name":"proof"},{"type":"account","name":"publicVrfKey"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}}],"arcs":[22,28],"networks":{},"state":{"schema":{"global":{"ints":3,"bytes":2},"local":{"ints":0,"bytes":0}},"keys":{"global":{"publicVrfKey":{"keyType":"AVMString","valueType":"AVMBytes","key":"cHVibGljVnJmS2V5"},"lsigAddress":{"keyType":"AVMString","valueType":"AVMBytes","key":"bHNpZ0FkZHJlc3M="},"blockCommitted":{"keyType":"AVMString","valueType":"AVMUint64","key":"YmxvY2tDb21taXR0ZWQ="},"randomRange":{"keyType":"AVMString","valueType":"AVMUint64","key":"cmFuZG9tUmFuZ2U="},"randomValue":{"keyType":"AVMString","valueType":"AVMUint64","key":"cmFuZG9tVmFsdWU="}},"local":{},"box":{}},"maps":{"global":{},"local":{},"box":{}}},"bareActions":{"create":["NoOp"],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[135,165,181],"errorMessage":"OnCompletion is not NoOp"},{"pc":[268],"errorMessage":"block already committed"},{"pc":[274],"errorMessage":"block cannot be past round"},{"pc":[319],"errorMessage":"block not in range"},{"pc":[214],"errorMessage":"can only call when creating"},{"pc":[138,168,184],"errorMessage":"can only call when not creating"},{"pc":[299],"errorMessage":"cannot change public key"},{"pc":[229,262,289,295,303,313,354,361,369,377,385],"errorMessage":"check GlobalState exists"},{"pc":[291],"errorMessage":"lsig has to verify"},{"pc":[309],"errorMessage":"message must be committed block seed"},{"pc":[225,258],"errorMessage":"only creator can call"},{"pc":[235],"errorMessage":"public key cannot be changed before resolving commit"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"events":[]} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


/**
 * The argument types for the AppVrf contract
 */
export type AppVrfArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'config(account,account,uint64)void': {
      publicVrfKey: Uint8Array | string
      lsigAddress: Uint8Array | string
      randomRange: bigint | number
    }
    'commit(uint64)void': {
      block: bigint | number
    }
    'verify(byte[],byte[],account)void': {
      message: Uint8Array
      proof: Uint8Array
      publicVrfKey: Uint8Array | string
    }
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'config(account,account,uint64)void': [publicVrfKey: Uint8Array | string, lsigAddress: Uint8Array | string, randomRange: bigint | number]
    'commit(uint64)void': [block: bigint | number]
    'verify(byte[],byte[],account)void': [message: Uint8Array, proof: Uint8Array, publicVrfKey: Uint8Array | string]
  }
}

/**
 * The return type for each method
 */
export type AppVrfReturns = {
  'config(account,account,uint64)void': void
  'commit(uint64)void': void
  'verify(byte[],byte[],account)void': void
}

/**
 * Defines the types of available calls and state of the AppVrf smart contract.
 */
export type AppVrfTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'config(account,account,uint64)void' | 'config', {
      argsObj: AppVrfArgs['obj']['config(account,account,uint64)void']
      argsTuple: AppVrfArgs['tuple']['config(account,account,uint64)void']
      returns: AppVrfReturns['config(account,account,uint64)void']
    }>
    & Record<'commit(uint64)void' | 'commit', {
      argsObj: AppVrfArgs['obj']['commit(uint64)void']
      argsTuple: AppVrfArgs['tuple']['commit(uint64)void']
      returns: AppVrfReturns['commit(uint64)void']
    }>
    & Record<'verify(byte[],byte[],account)void' | 'verify', {
      argsObj: AppVrfArgs['obj']['verify(byte[],byte[],account)void']
      argsTuple: AppVrfArgs['tuple']['verify(byte[],byte[],account)void']
      returns: AppVrfReturns['verify(byte[],byte[],account)void']
    }>
  /**
   * Defines the shape of the state of the application.
   */
  state: {
    global: {
      keys: {
        publicVrfKey: BinaryState
        lsigAddress: BinaryState
        blockCommitted: bigint
        randomRange: bigint
        randomValue: bigint
      }
      maps: {}
    }
  }
}

/**
 * Defines the possible abi call signatures.
 */
export type AppVrfSignatures = keyof AppVrfTypes['methods']
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the AppVrf smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends AppVrfSignatures> = AppVrfTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the AppVrf smart contract to the method's return type
 */
export type MethodReturn<TSignature extends AppVrfSignatures> = AppVrfTypes['methods'][TSignature]['returns']

/**
 * Defines the shape of the keyed global state of the application.
 */
export type GlobalKeysState = AppVrfTypes['state']['global']['keys']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the AppVrf smart contract
 */
export abstract class AppVrfParamsFactory {
  /**
   * Constructs a no op call for the config(account,account,uint64)void ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static config(params: CallParams<AppVrfArgs['obj']['config(account,account,uint64)void'] | AppVrfArgs['tuple']['config(account,account,uint64)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'config(account,account,uint64)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.publicVrfKey, params.args.lsigAddress, params.args.randomRange],
    }
  }
  /**
   * Constructs a no op call for the commit(uint64)void ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static commit(params: CallParams<AppVrfArgs['obj']['commit(uint64)void'] | AppVrfArgs['tuple']['commit(uint64)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'commit(uint64)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.block],
    }
  }
  /**
   * Constructs a no op call for the verify(byte[],byte[],account)void ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static verify(params: CallParams<AppVrfArgs['obj']['verify(byte[],byte[],account)void'] | AppVrfArgs['tuple']['verify(byte[],byte[],account)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'verify(byte[],byte[],account)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.message, params.args.proof, params.args.publicVrfKey],
    }
  }
}

/**
 * A client to make calls to the AppVRF smart contract
 */
export class AppVrfClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `AppVrfClient`
   *
   * @param appClient An `AppClient` instance which has been created with the AppVrf app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `AppVrfClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Returns a new `AppVrfClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<AppVrfClient> {
    return new AppVrfClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `AppVrfClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<AppVrfClient> {
    return new AppVrfClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Makes a clear_state call to an existing instance of the AppVRF smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the AppVRF smart contract using the `config(account,account,uint64)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    config: (params: CallParams<AppVrfArgs['obj']['config(account,account,uint64)void'] | AppVrfArgs['tuple']['config(account,account,uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(AppVrfParamsFactory.config(params))
    },

    /**
     * Makes a call to the AppVRF smart contract using the `commit(uint64)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    commit: (params: CallParams<AppVrfArgs['obj']['commit(uint64)void'] | AppVrfArgs['tuple']['commit(uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(AppVrfParamsFactory.commit(params))
    },

    /**
     * Makes a call to the AppVRF smart contract using the `verify(byte[],byte[],account)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    verify: (params: CallParams<AppVrfArgs['obj']['verify(byte[],byte[],account)void'] | AppVrfArgs['tuple']['verify(byte[],byte[],account)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(AppVrfParamsFactory.verify(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Makes a clear_state call to an existing instance of the AppVRF smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the AppVRF smart contract using the `config(account,account,uint64)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    config: (params: CallParams<AppVrfArgs['obj']['config(account,account,uint64)void'] | AppVrfArgs['tuple']['config(account,account,uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(AppVrfParamsFactory.config(params))
    },

    /**
     * Makes a call to the AppVRF smart contract using the `commit(uint64)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    commit: (params: CallParams<AppVrfArgs['obj']['commit(uint64)void'] | AppVrfArgs['tuple']['commit(uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(AppVrfParamsFactory.commit(params))
    },

    /**
     * Makes a call to the AppVRF smart contract using the `verify(byte[],byte[],account)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    verify: (params: CallParams<AppVrfArgs['obj']['verify(byte[],byte[],account)void'] | AppVrfArgs['tuple']['verify(byte[],byte[],account)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(AppVrfParamsFactory.verify(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Makes a clear_state call to an existing instance of the AppVRF smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the AppVRF smart contract using the `config(account,account,uint64)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    config: async (params: CallParams<AppVrfArgs['obj']['config(account,account,uint64)void'] | AppVrfArgs['tuple']['config(account,account,uint64)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(AppVrfParamsFactory.config(params))
      return {...result, return: result.return as unknown as (undefined | AppVrfReturns['config(account,account,uint64)void'])}
    },

    /**
     * Makes a call to the AppVRF smart contract using the `commit(uint64)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    commit: async (params: CallParams<AppVrfArgs['obj']['commit(uint64)void'] | AppVrfArgs['tuple']['commit(uint64)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(AppVrfParamsFactory.commit(params))
      return {...result, return: result.return as unknown as (undefined | AppVrfReturns['commit(uint64)void'])}
    },

    /**
     * Makes a call to the AppVRF smart contract using the `verify(byte[],byte[],account)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    verify: async (params: CallParams<AppVrfArgs['obj']['verify(byte[],byte[],account)void'] | AppVrfArgs['tuple']['verify(byte[],byte[],account)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(AppVrfParamsFactory.verify(params))
      return {...result, return: result.return as unknown as (undefined | AppVrfReturns['verify(byte[],byte[],account)void'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new AppVrfClient(this.appClient.clone(params))
  }

  /**
   * Methods to access state for the current AppVRF app
   */
  state = {
    /**
     * Methods to access global state for the current AppVRF app
     */
    global: {
      /**
       * Get all current keyed values from global state
       */
      getAll: async (): Promise<Partial<Expand<GlobalKeysState>>> => {
        const result = await this.appClient.state.global.getAll()
        return {
          publicVrfKey: new BinaryStateValue(result.publicVrfKey),
          lsigAddress: new BinaryStateValue(result.lsigAddress),
          blockCommitted: result.blockCommitted,
          randomRange: result.randomRange,
          randomValue: result.randomValue,
        }
      },
      /**
       * Get the current value of the publicVrfKey key in global state
       */
      publicVrfKey: async (): Promise<BinaryState> => { return new BinaryStateValue((await this.appClient.state.global.getValue("publicVrfKey")) as Uint8Array | undefined) },
      /**
       * Get the current value of the lsigAddress key in global state
       */
      lsigAddress: async (): Promise<BinaryState> => { return new BinaryStateValue((await this.appClient.state.global.getValue("lsigAddress")) as Uint8Array | undefined) },
      /**
       * Get the current value of the blockCommitted key in global state
       */
      blockCommitted: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("blockCommitted")) as bigint | undefined },
      /**
       * Get the current value of the randomRange key in global state
       */
      randomRange: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("randomRange")) as bigint | undefined },
      /**
       * Get the current value of the randomValue key in global state
       */
      randomValue: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("randomValue")) as bigint | undefined },
    },
  }

  public newGroup(): AppVrfComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a config(account,account,uint64)void method call against the AppVRF contract
       */
      config(params: CallParams<AppVrfArgs['obj']['config(account,account,uint64)void'] | AppVrfArgs['tuple']['config(account,account,uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.config(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a commit(uint64)void method call against the AppVRF contract
       */
      commit(params: CallParams<AppVrfArgs['obj']['commit(uint64)void'] | AppVrfArgs['tuple']['commit(uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.commit(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a verify(byte[],byte[],account)void method call against the AppVRF contract
       */
      verify(params: CallParams<AppVrfArgs['obj']['verify(byte[],byte[],account)void'] | AppVrfArgs['tuple']['verify(byte[],byte[],account)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.verify(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a clear state call to the AppVRF contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as AppVrfComposer
  }
}
export type AppVrfComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the config(account,account,uint64)void ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  config(params?: CallParams<AppVrfArgs['obj']['config(account,account,uint64)void'] | AppVrfArgs['tuple']['config(account,account,uint64)void']>): AppVrfComposer<[...TReturns, AppVrfReturns['config(account,account,uint64)void'] | undefined]>

  /**
   * Calls the commit(uint64)void ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  commit(params?: CallParams<AppVrfArgs['obj']['commit(uint64)void'] | AppVrfArgs['tuple']['commit(uint64)void']>): AppVrfComposer<[...TReturns, AppVrfReturns['commit(uint64)void'] | undefined]>

  /**
   * Calls the verify(byte[],byte[],account)void ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  verify(params?: CallParams<AppVrfArgs['obj']['verify(byte[],byte[],account)void'] | AppVrfArgs['tuple']['verify(byte[],byte[],account)void']>): AppVrfComposer<[...TReturns, AppVrfReturns['verify(byte[],byte[],account)void'] | undefined]>

  /**
   * Makes a clear_state call to an existing instance of the AppVRF smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): AppVrfComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): AppVrfComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<AppVrfComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<AppVrfComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<AppVrfComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<AppVrfComposerResults<TReturns>>
}
export type AppVrfComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

