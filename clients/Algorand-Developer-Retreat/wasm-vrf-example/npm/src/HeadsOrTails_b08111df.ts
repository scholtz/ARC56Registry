/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"HeadsOrTails","desc":"","methods":[{"name":"createGame","desc":"Create a new HeadsOrTails game","args":[{"name":"boxFeeTxn","type":"pay","desc":"PayTxn covering the mbr increase"},{"name":"pk","type":"byte[32]","desc":"public key of the generated vrf keypair"},{"name":"heads","type":"bool","desc":"does user think the user will be heads?"}],"returns":{"type":"uint64","desc":"uint64 the commitmentRound when participant can complete the game"},"actions":{"create":[],"call":["NoOp"]}},{"name":"completeGame","desc":"Completes a game and refunds the user their box storage fees","args":[{"name":"proof","type":"byte[80]","desc":"the VRF proof signed by the participant's private key (using the block seed at commitmentRound)"}],"returns":{"type":"bool","desc":"boolean if the user won or not"},"actions":{"create":[],"call":["NoOp"]}},{"name":"cancelGame","desc":"Cancels a game and refunds the user their box storage fees","args":[],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]}},{"name":"createApplication","args":[],"returns":{"type":"void"},"actions":{"create":["NoOp"],"call":[]}}],"arcs":[4,56],"structs":{"HeadsOrTailsGame":[{"name":"pk","type":"byte[32]"},{"name":"heads","type":"bool"},{"name":"commitmentRound","type":"uint64"}]},"state":{"schema":{"global":{"bytes":0,"ints":0},"local":{"bytes":0,"ints":0}},"keys":{"global":{},"local":{},"box":{}},"maps":{"global":{},"local":{},"box":{"game":{"keyType":"address","valueType":"HeadsOrTailsGame"}}}},"bareActions":{"create":[],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[84],"errorMessage":"The requested action is not implemented in this contract. Are you using the correct OnComplete? Did you set your app ID?","teal":25},{"pc":[93],"errorMessage":"argument 0 (heads) for createGame must be a bool","teal":40},{"pc":[104],"errorMessage":"argument 1 (pk) for createGame must be a byte[32]","teal":52},{"pc":[114],"errorMessage":"argument 2 (boxFeeTxn) for createGame must be a pay transaction","teal":64},{"pc":[133],"errorMessage":"must not have a game in progress","teal":97},{"pc":[141],"errorMessage":"transaction verification failed: {\"txn\":\"boxFeeTxn\",\"field\":\"receiver\",\"expected\":\"this.app.address\"}","teal":113},{"pc":[148],"errorMessage":"transaction verification failed: {\"txn\":\"boxFeeTxn\",\"field\":\"amount\",\"condition\":\"greaterThanEqualTo\",\"expected\":\">=BOX_STORAGE_COST\"}","teal":122},{"pc":[186],"errorMessage":"argument 0 (proof) for completeGame must be a byte[80]","teal":170},{"pc":[213],"errorMessage":"box value does not exist: this.game(this.txn.sender).value","teal":207},{"pc":[264],"errorMessage":"box value does not exist: this.game(this.txn.sender).value","teal":250},{"pc":[280],"errorMessage":"the vrf must be verified","teal":263},{"pc":[296],"errorMessage":"box value does not exist: this.game(this.txn.sender).value","teal":281},{"pc":[318],"errorMessage":"box value does not exist: this.game(this.txn.sender).value","teal":299},{"pc":[374],"errorMessage":"box value does not exist: this.game(this.txn.sender).value","teal":391},{"pc":[428],"errorMessage":"this contract does not implement the given ABI method for create NoOp","teal":452},{"pc":[458],"errorMessage":"this contract does not implement the given ABI method for call NoOp","teal":462}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}}} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


// Type definitions for ARC-56 structs

export type HeadsOrTailsGame = {
  pk: Uint8Array,
  heads: boolean,
  commitmentRound: bigint
}


/**
 * Converts the ABI tuple representation of a HeadsOrTailsGame to the struct representation
 */
export function HeadsOrTailsGameFromTuple(abiTuple: [Uint8Array, boolean, bigint]) {
  return getABIStructFromABITuple(abiTuple, APP_SPEC.structs.HeadsOrTailsGame, APP_SPEC.structs) as HeadsOrTailsGame
}

/**
 * The argument types for the HeadsOrTails contract
 */
export type HeadsOrTailsArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'createGame(pay,byte[32],bool)uint64': {
      /**
       * PayTxn covering the mbr increase
       */
      boxFeeTxn: AppMethodCallTransactionArgument
      /**
       * public key of the generated vrf keypair
       */
      pk: Uint8Array
      /**
       * does user think the user will be heads?
       */
      heads: boolean
    }
    'completeGame(byte[80])bool': {
      /**
       * the VRF proof signed by the participant's private key (using the block seed at commitmentRound)
       */
      proof: Uint8Array
    }
    'cancelGame()void': Record<string, never>
    'createApplication()void': Record<string, never>
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'createGame(pay,byte[32],bool)uint64': [boxFeeTxn: AppMethodCallTransactionArgument, pk: Uint8Array, heads: boolean]
    'completeGame(byte[80])bool': [proof: Uint8Array]
    'cancelGame()void': []
    'createApplication()void': []
  }
}

/**
 * The return type for each method
 */
export type HeadsOrTailsReturns = {
  'createGame(pay,byte[32],bool)uint64': bigint
  'completeGame(byte[80])bool': boolean
  'cancelGame()void': void
  'createApplication()void': void
}

/**
 * Defines the types of available calls and state of the HeadsOrTails smart contract.
 */
export type HeadsOrTailsTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'createGame(pay,byte[32],bool)uint64' | 'createGame', {
      argsObj: HeadsOrTailsArgs['obj']['createGame(pay,byte[32],bool)uint64']
      argsTuple: HeadsOrTailsArgs['tuple']['createGame(pay,byte[32],bool)uint64']
      /**
       * uint64 the commitmentRound when participant can complete the game
       */
      returns: HeadsOrTailsReturns['createGame(pay,byte[32],bool)uint64']
    }>
    & Record<'completeGame(byte[80])bool' | 'completeGame', {
      argsObj: HeadsOrTailsArgs['obj']['completeGame(byte[80])bool']
      argsTuple: HeadsOrTailsArgs['tuple']['completeGame(byte[80])bool']
      /**
       * boolean if the user won or not
       */
      returns: HeadsOrTailsReturns['completeGame(byte[80])bool']
    }>
    & Record<'cancelGame()void' | 'cancelGame', {
      argsObj: HeadsOrTailsArgs['obj']['cancelGame()void']
      argsTuple: HeadsOrTailsArgs['tuple']['cancelGame()void']
      returns: HeadsOrTailsReturns['cancelGame()void']
    }>
    & Record<'createApplication()void' | 'createApplication', {
      argsObj: HeadsOrTailsArgs['obj']['createApplication()void']
      argsTuple: HeadsOrTailsArgs['tuple']['createApplication()void']
      returns: HeadsOrTailsReturns['createApplication()void']
    }>
  /**
   * Defines the shape of the state of the application.
   */
  state: {
    box: {
      keys: {}
      maps: {
        game: Map<string, HeadsOrTailsGame>
      }
    }
  }
}

/**
 * Defines the possible abi call signatures.
 */
export type HeadsOrTailsSignatures = keyof HeadsOrTailsTypes['methods']
/**
 * Defines the possible abi call signatures for methods that return a non-void value.
 */
export type HeadsOrTailsNonVoidMethodSignatures = keyof HeadsOrTailsTypes['methods'] extends infer T ? T extends keyof HeadsOrTailsTypes['methods'] ? MethodReturn<T> extends void ? never : T  : never : never
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the HeadsOrTails smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends HeadsOrTailsSignatures> = HeadsOrTailsTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the HeadsOrTails smart contract to the method's return type
 */
export type MethodReturn<TSignature extends HeadsOrTailsSignatures> = HeadsOrTailsTypes['methods'][TSignature]['returns']

/**
 * Defines the shape of the keyed box state of the application.
 */
export type BoxKeysState = HeadsOrTailsTypes['state']['box']['keys']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the HeadsOrTails smart contract
 */
export abstract class HeadsOrTailsParamsFactory {
  /**
   * Constructs a no op call for the createGame(pay,byte[32],bool)uint64 ABI method
   *
   * Create a new HeadsOrTails game
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static createGame(params: CallParams<HeadsOrTailsArgs['obj']['createGame(pay,byte[32],bool)uint64'] | HeadsOrTailsArgs['tuple']['createGame(pay,byte[32],bool)uint64']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'createGame(pay,byte[32],bool)uint64' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.boxFeeTxn, params.args.pk, params.args.heads],
    }
  }
  /**
   * Constructs a no op call for the completeGame(byte[80])bool ABI method
   *
   * Completes a game and refunds the user their box storage fees
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static completeGame(params: CallParams<HeadsOrTailsArgs['obj']['completeGame(byte[80])bool'] | HeadsOrTailsArgs['tuple']['completeGame(byte[80])bool']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'completeGame(byte[80])bool' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.proof],
    }
  }
  /**
   * Constructs a no op call for the cancelGame()void ABI method
   *
   * Cancels a game and refunds the user their box storage fees
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static cancelGame(params: CallParams<HeadsOrTailsArgs['obj']['cancelGame()void'] | HeadsOrTailsArgs['tuple']['cancelGame()void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'cancelGame()void' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
}

/**
 * A client to make calls to the HeadsOrTails smart contract
 */
export class HeadsOrTailsClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `HeadsOrTailsClient`
   *
   * @param appClient An `AppClient` instance which has been created with the HeadsOrTails app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `HeadsOrTailsClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Checks for decode errors on the given return value and maps the return value to the return type for the given method
   * @returns The typed return value or undefined if there was no value
   */
  decodeReturnValue<TSignature extends HeadsOrTailsNonVoidMethodSignatures>(method: TSignature, returnValue: ABIReturn | undefined) {
    return returnValue !== undefined ? getArc56ReturnValue<MethodReturn<TSignature>>(returnValue, this.appClient.getABIMethod(method), APP_SPEC.structs) : undefined
  }

  /**
   * Returns a new `HeadsOrTailsClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<HeadsOrTailsClient> {
    return new HeadsOrTailsClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `HeadsOrTailsClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<HeadsOrTailsClient> {
    return new HeadsOrTailsClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Makes a clear_state call to an existing instance of the HeadsOrTails smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the HeadsOrTails smart contract using the `createGame(pay,byte[32],bool)uint64` ABI method.
     *
     * Create a new HeadsOrTails game
     *
     * @param params The params for the smart contract call
     * @returns The call params: uint64 the commitmentRound when participant can complete the game
     */
    createGame: (params: CallParams<HeadsOrTailsArgs['obj']['createGame(pay,byte[32],bool)uint64'] | HeadsOrTailsArgs['tuple']['createGame(pay,byte[32],bool)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(HeadsOrTailsParamsFactory.createGame(params))
    },

    /**
     * Makes a call to the HeadsOrTails smart contract using the `completeGame(byte[80])bool` ABI method.
     *
     * Completes a game and refunds the user their box storage fees
     *
     * @param params The params for the smart contract call
     * @returns The call params: boolean if the user won or not
     */
    completeGame: (params: CallParams<HeadsOrTailsArgs['obj']['completeGame(byte[80])bool'] | HeadsOrTailsArgs['tuple']['completeGame(byte[80])bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(HeadsOrTailsParamsFactory.completeGame(params))
    },

    /**
     * Makes a call to the HeadsOrTails smart contract using the `cancelGame()void` ABI method.
     *
     * Cancels a game and refunds the user their box storage fees
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    cancelGame: (params: CallParams<HeadsOrTailsArgs['obj']['cancelGame()void'] | HeadsOrTailsArgs['tuple']['cancelGame()void']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(HeadsOrTailsParamsFactory.cancelGame(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Makes a clear_state call to an existing instance of the HeadsOrTails smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the HeadsOrTails smart contract using the `createGame(pay,byte[32],bool)uint64` ABI method.
     *
     * Create a new HeadsOrTails game
     *
     * @param params The params for the smart contract call
     * @returns The call transaction: uint64 the commitmentRound when participant can complete the game
     */
    createGame: (params: CallParams<HeadsOrTailsArgs['obj']['createGame(pay,byte[32],bool)uint64'] | HeadsOrTailsArgs['tuple']['createGame(pay,byte[32],bool)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(HeadsOrTailsParamsFactory.createGame(params))
    },

    /**
     * Makes a call to the HeadsOrTails smart contract using the `completeGame(byte[80])bool` ABI method.
     *
     * Completes a game and refunds the user their box storage fees
     *
     * @param params The params for the smart contract call
     * @returns The call transaction: boolean if the user won or not
     */
    completeGame: (params: CallParams<HeadsOrTailsArgs['obj']['completeGame(byte[80])bool'] | HeadsOrTailsArgs['tuple']['completeGame(byte[80])bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(HeadsOrTailsParamsFactory.completeGame(params))
    },

    /**
     * Makes a call to the HeadsOrTails smart contract using the `cancelGame()void` ABI method.
     *
     * Cancels a game and refunds the user their box storage fees
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    cancelGame: (params: CallParams<HeadsOrTailsArgs['obj']['cancelGame()void'] | HeadsOrTailsArgs['tuple']['cancelGame()void']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(HeadsOrTailsParamsFactory.cancelGame(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Makes a clear_state call to an existing instance of the HeadsOrTails smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the HeadsOrTails smart contract using the `createGame(pay,byte[32],bool)uint64` ABI method.
     *
     * Create a new HeadsOrTails game
     *
     * @param params The params for the smart contract call
     * @returns The call result: uint64 the commitmentRound when participant can complete the game
     */
    createGame: async (params: CallParams<HeadsOrTailsArgs['obj']['createGame(pay,byte[32],bool)uint64'] | HeadsOrTailsArgs['tuple']['createGame(pay,byte[32],bool)uint64']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(HeadsOrTailsParamsFactory.createGame(params))
      return {...result, return: result.return as unknown as (undefined | HeadsOrTailsReturns['createGame(pay,byte[32],bool)uint64'])}
    },

    /**
     * Makes a call to the HeadsOrTails smart contract using the `completeGame(byte[80])bool` ABI method.
     *
     * Completes a game and refunds the user their box storage fees
     *
     * @param params The params for the smart contract call
     * @returns The call result: boolean if the user won or not
     */
    completeGame: async (params: CallParams<HeadsOrTailsArgs['obj']['completeGame(byte[80])bool'] | HeadsOrTailsArgs['tuple']['completeGame(byte[80])bool']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(HeadsOrTailsParamsFactory.completeGame(params))
      return {...result, return: result.return as unknown as (undefined | HeadsOrTailsReturns['completeGame(byte[80])bool'])}
    },

    /**
     * Makes a call to the HeadsOrTails smart contract using the `cancelGame()void` ABI method.
     *
     * Cancels a game and refunds the user their box storage fees
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    cancelGame: async (params: CallParams<HeadsOrTailsArgs['obj']['cancelGame()void'] | HeadsOrTailsArgs['tuple']['cancelGame()void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(HeadsOrTailsParamsFactory.cancelGame(params))
      return {...result, return: result.return as unknown as (undefined | HeadsOrTailsReturns['cancelGame()void'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new HeadsOrTailsClient(this.appClient.clone(params))
  }

  /**
   * Methods to access state for the current HeadsOrTails app
   */
  state = {
    /**
     * Methods to access box state for the current HeadsOrTails app
     */
    box: {
      /**
       * Get all current keyed values from box state
       */
      getAll: async (): Promise<Partial<Expand<BoxKeysState>>> => {
        const result = await this.appClient.state.box.getAll()
        return {
        }
      },
      /**
       * Get values from the game map in box state
       */
      game: {
        /**
         * Get all current values of the game map in box state
         */
        getMap: async (): Promise<Map<string, HeadsOrTailsGame>> => { return (await this.appClient.state.box.getMap("game")) as Map<string, HeadsOrTailsGame> },
        /**
         * Get a current value of the game map by key from box state
         */
        value: async (key: string): Promise<HeadsOrTailsGame | undefined> => { return await this.appClient.state.box.getMapValue("game", key) as HeadsOrTailsGame | undefined },
      },
    },
  }

  public newGroup(): HeadsOrTailsComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a createGame(pay,byte[32],bool)uint64 method call against the HeadsOrTails contract
       */
      createGame(params: CallParams<HeadsOrTailsArgs['obj']['createGame(pay,byte[32],bool)uint64'] | HeadsOrTailsArgs['tuple']['createGame(pay,byte[32],bool)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.createGame(params)))
        resultMappers.push((v) => client.decodeReturnValue('createGame(pay,byte[32],bool)uint64', v))
        return this
      },
      /**
       * Add a completeGame(byte[80])bool method call against the HeadsOrTails contract
       */
      completeGame(params: CallParams<HeadsOrTailsArgs['obj']['completeGame(byte[80])bool'] | HeadsOrTailsArgs['tuple']['completeGame(byte[80])bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.completeGame(params)))
        resultMappers.push((v) => client.decodeReturnValue('completeGame(byte[80])bool', v))
        return this
      },
      /**
       * Add a cancelGame()void method call against the HeadsOrTails contract
       */
      cancelGame(params: CallParams<HeadsOrTailsArgs['obj']['cancelGame()void'] | HeadsOrTailsArgs['tuple']['cancelGame()void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.cancelGame(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a clear state call to the HeadsOrTails contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as HeadsOrTailsComposer
  }
}
export type HeadsOrTailsComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the createGame(pay,byte[32],bool)uint64 ABI method.
   *
   * Create a new HeadsOrTails game
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  createGame(params?: CallParams<HeadsOrTailsArgs['obj']['createGame(pay,byte[32],bool)uint64'] | HeadsOrTailsArgs['tuple']['createGame(pay,byte[32],bool)uint64']>): HeadsOrTailsComposer<[...TReturns, HeadsOrTailsReturns['createGame(pay,byte[32],bool)uint64'] | undefined]>

  /**
   * Calls the completeGame(byte[80])bool ABI method.
   *
   * Completes a game and refunds the user their box storage fees
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  completeGame(params?: CallParams<HeadsOrTailsArgs['obj']['completeGame(byte[80])bool'] | HeadsOrTailsArgs['tuple']['completeGame(byte[80])bool']>): HeadsOrTailsComposer<[...TReturns, HeadsOrTailsReturns['completeGame(byte[80])bool'] | undefined]>

  /**
   * Calls the cancelGame()void ABI method.
   *
   * Cancels a game and refunds the user their box storage fees
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  cancelGame(params?: CallParams<HeadsOrTailsArgs['obj']['cancelGame()void'] | HeadsOrTailsArgs['tuple']['cancelGame()void']>): HeadsOrTailsComposer<[...TReturns, HeadsOrTailsReturns['cancelGame()void'] | undefined]>

  /**
   * Makes a clear_state call to an existing instance of the HeadsOrTails smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): HeadsOrTailsComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): HeadsOrTailsComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<HeadsOrTailsComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<HeadsOrTailsComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<HeadsOrTailsComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<HeadsOrTailsComposerResults<TReturns>>
}
export type HeadsOrTailsComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

