/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"AgentEscrowVaultV2","structs":{"BillingRecord":[{"name":"timestamp","type":"uint64"},{"name":"amount","type":"uint64"},{"name":"cycleIndex","type":"uint64"}]},"methods":[{"name":"create","args":[{"type":"address","name":"recipient"},{"type":"address","name":"agent"}],"returns":{"type":"void"},"actions":{"create":["NoOp"],"call":[]},"readonly":false,"desc":"Deploy the v2 vault.","events":[],"recommendations":{}},{"name":"release","args":[{"type":"uint64","name":"amount"}],"returns":{"type":"uint64"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Release `amount` microalgos to recipient and append a BillingRecord.\nReturns the new cycle index. Callable by creator OR agent.","events":[],"recommendations":{}},{"name":"kill","args":[],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Refund remaining balance to creator and mark vault killed.","events":[],"recommendations":{}},{"name":"get_history","args":[{"type":"uint64","name":"cycle"}],"returns":{"type":"(uint64,uint64,uint64)","struct":"BillingRecord"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"desc":"Read a single release record.","events":[],"recommendations":{}},{"name":"get_cycle_count","args":[],"returns":{"type":"uint64"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"desc":"Total number of releases.","events":[],"recommendations":{}},{"name":"delete","args":[],"returns":{"type":"void"},"actions":{"create":[],"call":["DeleteApplication"]},"readonly":false,"desc":"Delete the vault. Only after kill, only by creator.","events":[],"recommendations":{}}],"arcs":[22,28],"desc":"\n    Agent Escrow Vault v2 — A2A Autonomous Payments with on-chain billing history.\n\n    Same authorization model as v1 (creator OR agent can release), but instead\n    of clobbering one global \"status\" slot per release, every release writes a\n    BillingRecord into Box Storage keyed by the cycle index. The full payment\n    history is permanently auditable on-chain.\n\n    State:\n        GlobalState.creator     — user wallet (only one who can kill)\n        GlobalState.recipient   — service provider address\n        GlobalState.agent       — autonomous agent wallet\n        GlobalState.cycle_index — monotonic counter, incremented on each release\n        GlobalState.killed      — 0 = alive, 1 = killed by creator\n        Box \"h:<cycle_index>\"   — BillingRecord for that release\n    ","networks":{},"state":{"schema":{"global":{"ints":2,"bytes":3},"local":{"ints":0,"bytes":0}},"keys":{"global":{"creator":{"keyType":"AVMString","valueType":"address","key":"Y3JlYXRvcg==","desc":"User wallet (creator)"},"recipient":{"keyType":"AVMString","valueType":"address","key":"cmVjaXBpZW50","desc":"Payment recipient"},"agent":{"keyType":"AVMString","valueType":"address","key":"YWdlbnQ=","desc":"Autonomous release agent"},"cycle_index":{"keyType":"AVMString","valueType":"AVMUint64","key":"Y3ljbGVfaW5kZXg=","desc":"Monotonic billing cycle"},"killed":{"keyType":"AVMString","valueType":"AVMUint64","key":"a2lsbGVk","desc":"0=alive 1=killed"}},"local":{},"box":{}},"maps":{"global":{},"local":{},"box":{"history":{"keyType":"uint64","valueType":"BillingRecord","prefix":"aDo="}}}},"bareActions":{"create":[],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[326],"errorMessage":"Already killed"},{"pc":[367],"errorMessage":"Cycle not found"},{"pc":[247],"errorMessage":"Invalid payout amount"},{"pc":[404],"errorMessage":"Kill before delete"},{"pc":[397],"errorMessage":"Only creator can delete"},{"pc":[320],"errorMessage":"Only creator can kill"},{"pc":[220],"errorMessage":"Only creator or agent can release"},{"pc":[197],"errorMessage":"Vault is killed"},{"pc":[214],"errorMessage":"check self.agent exists"},{"pc":[203,315,392],"errorMessage":"check self.creator exists"},{"pc":[270,379],"errorMessage":"check self.cycle_index exists"},{"pc":[195,324,401],"errorMessage":"check self.killed exists"},{"pc":[253],"errorMessage":"check self.recipient exists"},{"pc":[151,159],"errorMessage":"invalid number of bytes for arc4.static_array<arc4.uint8, 32>"},{"pc":[191,358],"errorMessage":"invalid number of bytes for arc4.uint64"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"events":[]} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


// Type definitions for ARC-56 structs

export type BillingRecord = {
  timestamp: bigint,
  amount: bigint,
  cycleIndex: bigint
}


/**
 * Converts the ABI tuple representation of a BillingRecord to the struct representation
 */
export function BillingRecordFromTuple(abiTuple: [bigint, bigint, bigint]) {
  return getABIStructFromABITuple(abiTuple, APP_SPEC.structs.BillingRecord, APP_SPEC.structs) as BillingRecord
}

/**
 * The argument types for the AgentEscrowVaultV2 contract
 */
export type AgentEscrowVaultV2Args = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'create(address,address)void': {
      recipient: string
      agent: string
    }
    'release(uint64)uint64': {
      amount: bigint | number
    }
    'kill()void': Record<string, never>
    'get_history(uint64)(uint64,uint64,uint64)': {
      cycle: bigint | number
    }
    'get_cycle_count()uint64': Record<string, never>
    'delete()void': Record<string, never>
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'create(address,address)void': [recipient: string, agent: string]
    'release(uint64)uint64': [amount: bigint | number]
    'kill()void': []
    'get_history(uint64)(uint64,uint64,uint64)': [cycle: bigint | number]
    'get_cycle_count()uint64': []
    'delete()void': []
  }
}

/**
 * The return type for each method
 */
export type AgentEscrowVaultV2Returns = {
  'create(address,address)void': void
  'release(uint64)uint64': bigint
  'kill()void': void
  'get_history(uint64)(uint64,uint64,uint64)': BillingRecord
  'get_cycle_count()uint64': bigint
  'delete()void': void
}

/**
 * Defines the types of available calls and state of the AgentEscrowVaultV2 smart contract.
 */
export type AgentEscrowVaultV2Types = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'create(address,address)void' | 'create', {
      argsObj: AgentEscrowVaultV2Args['obj']['create(address,address)void']
      argsTuple: AgentEscrowVaultV2Args['tuple']['create(address,address)void']
      returns: AgentEscrowVaultV2Returns['create(address,address)void']
    }>
    & Record<'release(uint64)uint64' | 'release', {
      argsObj: AgentEscrowVaultV2Args['obj']['release(uint64)uint64']
      argsTuple: AgentEscrowVaultV2Args['tuple']['release(uint64)uint64']
      returns: AgentEscrowVaultV2Returns['release(uint64)uint64']
    }>
    & Record<'kill()void' | 'kill', {
      argsObj: AgentEscrowVaultV2Args['obj']['kill()void']
      argsTuple: AgentEscrowVaultV2Args['tuple']['kill()void']
      returns: AgentEscrowVaultV2Returns['kill()void']
    }>
    & Record<'get_history(uint64)(uint64,uint64,uint64)' | 'get_history', {
      argsObj: AgentEscrowVaultV2Args['obj']['get_history(uint64)(uint64,uint64,uint64)']
      argsTuple: AgentEscrowVaultV2Args['tuple']['get_history(uint64)(uint64,uint64,uint64)']
      returns: AgentEscrowVaultV2Returns['get_history(uint64)(uint64,uint64,uint64)']
    }>
    & Record<'get_cycle_count()uint64' | 'get_cycle_count', {
      argsObj: AgentEscrowVaultV2Args['obj']['get_cycle_count()uint64']
      argsTuple: AgentEscrowVaultV2Args['tuple']['get_cycle_count()uint64']
      returns: AgentEscrowVaultV2Returns['get_cycle_count()uint64']
    }>
    & Record<'delete()void' | 'delete', {
      argsObj: AgentEscrowVaultV2Args['obj']['delete()void']
      argsTuple: AgentEscrowVaultV2Args['tuple']['delete()void']
      returns: AgentEscrowVaultV2Returns['delete()void']
    }>
  /**
   * Defines the shape of the state of the application.
   */
  state: {
    global: {
      keys: {
        /**
         * User wallet (creator)
         */
        creator: string
        /**
         * Payment recipient
         */
        recipient: string
        /**
         * Autonomous release agent
         */
        agent: string
        /**
         * Monotonic billing cycle
         */
        cycleIndex: bigint
        /**
         * 0=alive 1=killed
         */
        killed: bigint
      }
      maps: {}
    }
    box: {
      keys: {}
      maps: {
        history: Map<bigint | number, BillingRecord>
      }
    }
  }
}

/**
 * Defines the possible abi call signatures.
 */
export type AgentEscrowVaultV2Signatures = keyof AgentEscrowVaultV2Types['methods']
/**
 * Defines the possible abi call signatures for methods that return a non-void value.
 */
export type AgentEscrowVaultV2NonVoidMethodSignatures = keyof AgentEscrowVaultV2Types['methods'] extends infer T ? T extends keyof AgentEscrowVaultV2Types['methods'] ? MethodReturn<T> extends void ? never : T  : never : never
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the AgentEscrowVaultV2 smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends AgentEscrowVaultV2Signatures> = AgentEscrowVaultV2Types['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the AgentEscrowVaultV2 smart contract to the method's return type
 */
export type MethodReturn<TSignature extends AgentEscrowVaultV2Signatures> = AgentEscrowVaultV2Types['methods'][TSignature]['returns']

/**
 * Defines the shape of the keyed global state of the application.
 */
export type GlobalKeysState = AgentEscrowVaultV2Types['state']['global']['keys']

/**
 * Defines the shape of the keyed box state of the application.
 */
export type BoxKeysState = AgentEscrowVaultV2Types['state']['box']['keys']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the AgentEscrowVaultV2 smart contract
 */
export abstract class AgentEscrowVaultV2ParamsFactory {
  /**
   * Constructs a no op call for the release(uint64)uint64 ABI method
   *
  * Release `amount` microalgos to recipient and append a BillingRecord.
  Returns the new cycle index. Callable by creator OR agent.

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static release(params: CallParams<AgentEscrowVaultV2Args['obj']['release(uint64)uint64'] | AgentEscrowVaultV2Args['tuple']['release(uint64)uint64']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'release(uint64)uint64' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.amount],
    }
  }
  /**
   * Constructs a no op call for the kill()void ABI method
   *
   * Refund remaining balance to creator and mark vault killed.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static kill(params: CallParams<AgentEscrowVaultV2Args['obj']['kill()void'] | AgentEscrowVaultV2Args['tuple']['kill()void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'kill()void' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the get_history(uint64)(uint64,uint64,uint64) ABI method
   *
   * Read a single release record.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static getHistory(params: CallParams<AgentEscrowVaultV2Args['obj']['get_history(uint64)(uint64,uint64,uint64)'] | AgentEscrowVaultV2Args['tuple']['get_history(uint64)(uint64,uint64,uint64)']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'get_history(uint64)(uint64,uint64,uint64)' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.cycle],
    }
  }
  /**
   * Constructs a no op call for the get_cycle_count()uint64 ABI method
   *
   * Total number of releases.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static getCycleCount(params: CallParams<AgentEscrowVaultV2Args['obj']['get_cycle_count()uint64'] | AgentEscrowVaultV2Args['tuple']['get_cycle_count()uint64']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'get_cycle_count()uint64' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
}

/**
 * A client to make calls to the AgentEscrowVaultV2 smart contract
 */
export class AgentEscrowVaultV2Client {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `AgentEscrowVaultV2Client`
   *
   * @param appClient An `AppClient` instance which has been created with the AgentEscrowVaultV2 app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `AgentEscrowVaultV2Client`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Checks for decode errors on the given return value and maps the return value to the return type for the given method
   * @returns The typed return value or undefined if there was no value
   */
  decodeReturnValue<TSignature extends AgentEscrowVaultV2NonVoidMethodSignatures>(method: TSignature, returnValue: ABIReturn | undefined) {
    return returnValue !== undefined ? getArc56ReturnValue<MethodReturn<TSignature>>(returnValue, this.appClient.getABIMethod(method), APP_SPEC.structs) : undefined
  }

  /**
   * Returns a new `AgentEscrowVaultV2Client` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<AgentEscrowVaultV2Client> {
    return new AgentEscrowVaultV2Client(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `AgentEscrowVaultV2Client` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<AgentEscrowVaultV2Client> {
    return new AgentEscrowVaultV2Client(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Makes a clear_state call to an existing instance of the AgentEscrowVaultV2 smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the AgentEscrowVaultV2 smart contract using the `release(uint64)uint64` ABI method.
     *
    * Release `amount` microalgos to recipient and append a BillingRecord.
    Returns the new cycle index. Callable by creator OR agent.

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    release: (params: CallParams<AgentEscrowVaultV2Args['obj']['release(uint64)uint64'] | AgentEscrowVaultV2Args['tuple']['release(uint64)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(AgentEscrowVaultV2ParamsFactory.release(params))
    },

    /**
     * Makes a call to the AgentEscrowVaultV2 smart contract using the `kill()void` ABI method.
     *
     * Refund remaining balance to creator and mark vault killed.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    kill: (params: CallParams<AgentEscrowVaultV2Args['obj']['kill()void'] | AgentEscrowVaultV2Args['tuple']['kill()void']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(AgentEscrowVaultV2ParamsFactory.kill(params))
    },

    /**
     * Makes a call to the AgentEscrowVaultV2 smart contract using the `get_history(uint64)(uint64,uint64,uint64)` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * Read a single release record.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    getHistory: (params: CallParams<AgentEscrowVaultV2Args['obj']['get_history(uint64)(uint64,uint64,uint64)'] | AgentEscrowVaultV2Args['tuple']['get_history(uint64)(uint64,uint64,uint64)']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(AgentEscrowVaultV2ParamsFactory.getHistory(params))
    },

    /**
     * Makes a call to the AgentEscrowVaultV2 smart contract using the `get_cycle_count()uint64` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * Total number of releases.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    getCycleCount: (params: CallParams<AgentEscrowVaultV2Args['obj']['get_cycle_count()uint64'] | AgentEscrowVaultV2Args['tuple']['get_cycle_count()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(AgentEscrowVaultV2ParamsFactory.getCycleCount(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Makes a clear_state call to an existing instance of the AgentEscrowVaultV2 smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the AgentEscrowVaultV2 smart contract using the `release(uint64)uint64` ABI method.
     *
    * Release `amount` microalgos to recipient and append a BillingRecord.
    Returns the new cycle index. Callable by creator OR agent.

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    release: (params: CallParams<AgentEscrowVaultV2Args['obj']['release(uint64)uint64'] | AgentEscrowVaultV2Args['tuple']['release(uint64)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(AgentEscrowVaultV2ParamsFactory.release(params))
    },

    /**
     * Makes a call to the AgentEscrowVaultV2 smart contract using the `kill()void` ABI method.
     *
     * Refund remaining balance to creator and mark vault killed.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    kill: (params: CallParams<AgentEscrowVaultV2Args['obj']['kill()void'] | AgentEscrowVaultV2Args['tuple']['kill()void']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(AgentEscrowVaultV2ParamsFactory.kill(params))
    },

    /**
     * Makes a call to the AgentEscrowVaultV2 smart contract using the `get_history(uint64)(uint64,uint64,uint64)` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * Read a single release record.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    getHistory: (params: CallParams<AgentEscrowVaultV2Args['obj']['get_history(uint64)(uint64,uint64,uint64)'] | AgentEscrowVaultV2Args['tuple']['get_history(uint64)(uint64,uint64,uint64)']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(AgentEscrowVaultV2ParamsFactory.getHistory(params))
    },

    /**
     * Makes a call to the AgentEscrowVaultV2 smart contract using the `get_cycle_count()uint64` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * Total number of releases.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    getCycleCount: (params: CallParams<AgentEscrowVaultV2Args['obj']['get_cycle_count()uint64'] | AgentEscrowVaultV2Args['tuple']['get_cycle_count()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(AgentEscrowVaultV2ParamsFactory.getCycleCount(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Makes a clear_state call to an existing instance of the AgentEscrowVaultV2 smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the AgentEscrowVaultV2 smart contract using the `release(uint64)uint64` ABI method.
     *
    * Release `amount` microalgos to recipient and append a BillingRecord.
    Returns the new cycle index. Callable by creator OR agent.

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    release: async (params: CallParams<AgentEscrowVaultV2Args['obj']['release(uint64)uint64'] | AgentEscrowVaultV2Args['tuple']['release(uint64)uint64']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(AgentEscrowVaultV2ParamsFactory.release(params))
      return {...result, return: result.return as unknown as (undefined | AgentEscrowVaultV2Returns['release(uint64)uint64'])}
    },

    /**
     * Makes a call to the AgentEscrowVaultV2 smart contract using the `kill()void` ABI method.
     *
     * Refund remaining balance to creator and mark vault killed.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    kill: async (params: CallParams<AgentEscrowVaultV2Args['obj']['kill()void'] | AgentEscrowVaultV2Args['tuple']['kill()void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(AgentEscrowVaultV2ParamsFactory.kill(params))
      return {...result, return: result.return as unknown as (undefined | AgentEscrowVaultV2Returns['kill()void'])}
    },

    /**
     * Makes a call to the AgentEscrowVaultV2 smart contract using the `get_history(uint64)(uint64,uint64,uint64)` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * Read a single release record.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    getHistory: async (params: CallParams<AgentEscrowVaultV2Args['obj']['get_history(uint64)(uint64,uint64,uint64)'] | AgentEscrowVaultV2Args['tuple']['get_history(uint64)(uint64,uint64,uint64)']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(AgentEscrowVaultV2ParamsFactory.getHistory(params))
      return {...result, return: result.return as unknown as (undefined | AgentEscrowVaultV2Returns['get_history(uint64)(uint64,uint64,uint64)'])}
    },

    /**
     * Makes a call to the AgentEscrowVaultV2 smart contract using the `get_cycle_count()uint64` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * Total number of releases.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    getCycleCount: async (params: CallParams<AgentEscrowVaultV2Args['obj']['get_cycle_count()uint64'] | AgentEscrowVaultV2Args['tuple']['get_cycle_count()uint64']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(AgentEscrowVaultV2ParamsFactory.getCycleCount(params))
      return {...result, return: result.return as unknown as (undefined | AgentEscrowVaultV2Returns['get_cycle_count()uint64'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new AgentEscrowVaultV2Client(this.appClient.clone(params))
  }

  /**
   * Makes a readonly (simulated) call to the AgentEscrowVaultV2 smart contract using the `get_history(uint64)(uint64,uint64,uint64)` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * Read a single release record.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async getHistory(params: CallParams<AgentEscrowVaultV2Args['obj']['get_history(uint64)(uint64,uint64,uint64)'] | AgentEscrowVaultV2Args['tuple']['get_history(uint64)(uint64,uint64,uint64)']>) {
    const result = await this.appClient.send.call(AgentEscrowVaultV2ParamsFactory.getHistory(params))
    return result.return as unknown as AgentEscrowVaultV2Returns['get_history(uint64)(uint64,uint64,uint64)']
  }

  /**
   * Makes a readonly (simulated) call to the AgentEscrowVaultV2 smart contract using the `get_cycle_count()uint64` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * Total number of releases.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async getCycleCount(params: CallParams<AgentEscrowVaultV2Args['obj']['get_cycle_count()uint64'] | AgentEscrowVaultV2Args['tuple']['get_cycle_count()uint64']> = {args: []}) {
    const result = await this.appClient.send.call(AgentEscrowVaultV2ParamsFactory.getCycleCount(params))
    return result.return as unknown as AgentEscrowVaultV2Returns['get_cycle_count()uint64']
  }

  /**
   * Methods to access state for the current AgentEscrowVaultV2 app
   */
  state = {
    /**
     * Methods to access global state for the current AgentEscrowVaultV2 app
     */
    global: {
      /**
       * Get all current keyed values from global state
       */
      getAll: async (): Promise<Partial<Expand<GlobalKeysState>>> => {
        const result = await this.appClient.state.global.getAll()
        return {
          creator: result.creator,
          recipient: result.recipient,
          agent: result.agent,
          cycleIndex: result.cycle_index,
          killed: result.killed,
        }
      },
      /**
       * Get the current value of the creator key in global state
       */
      creator: async (): Promise<string | undefined> => { return (await this.appClient.state.global.getValue("creator")) as string | undefined },
      /**
       * Get the current value of the recipient key in global state
       */
      recipient: async (): Promise<string | undefined> => { return (await this.appClient.state.global.getValue("recipient")) as string | undefined },
      /**
       * Get the current value of the agent key in global state
       */
      agent: async (): Promise<string | undefined> => { return (await this.appClient.state.global.getValue("agent")) as string | undefined },
      /**
       * Get the current value of the cycle_index key in global state
       */
      cycleIndex: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("cycle_index")) as bigint | undefined },
      /**
       * Get the current value of the killed key in global state
       */
      killed: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("killed")) as bigint | undefined },
    },
    /**
     * Methods to access box state for the current AgentEscrowVaultV2 app
     */
    box: {
      /**
       * Get all current keyed values from box state
       */
      getAll: async (): Promise<Partial<Expand<BoxKeysState>>> => {
        const result = await this.appClient.state.box.getAll()
        return {
        }
      },
      /**
       * Get values from the history map in box state
       */
      history: {
        /**
         * Get all current values of the history map in box state
         */
        getMap: async (): Promise<Map<bigint, BillingRecord>> => { return (await this.appClient.state.box.getMap("history")) as Map<bigint, BillingRecord> },
        /**
         * Get a current value of the history map by key from box state
         */
        value: async (key: bigint | number): Promise<BillingRecord | undefined> => { return await this.appClient.state.box.getMapValue("history", key) as BillingRecord | undefined },
      },
    },
  }

  public newGroup(): AgentEscrowVaultV2Composer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a release(uint64)uint64 method call against the AgentEscrowVaultV2 contract
       */
      release(params: CallParams<AgentEscrowVaultV2Args['obj']['release(uint64)uint64'] | AgentEscrowVaultV2Args['tuple']['release(uint64)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.release(params)))
        resultMappers.push((v) => client.decodeReturnValue('release(uint64)uint64', v))
        return this
      },
      /**
       * Add a kill()void method call against the AgentEscrowVaultV2 contract
       */
      kill(params: CallParams<AgentEscrowVaultV2Args['obj']['kill()void'] | AgentEscrowVaultV2Args['tuple']['kill()void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.kill(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a get_history(uint64)(uint64,uint64,uint64) method call against the AgentEscrowVaultV2 contract
       */
      getHistory(params: CallParams<AgentEscrowVaultV2Args['obj']['get_history(uint64)(uint64,uint64,uint64)'] | AgentEscrowVaultV2Args['tuple']['get_history(uint64)(uint64,uint64,uint64)']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.getHistory(params)))
        resultMappers.push((v) => client.decodeReturnValue('get_history(uint64)(uint64,uint64,uint64)', v))
        return this
      },
      /**
       * Add a get_cycle_count()uint64 method call against the AgentEscrowVaultV2 contract
       */
      getCycleCount(params: CallParams<AgentEscrowVaultV2Args['obj']['get_cycle_count()uint64'] | AgentEscrowVaultV2Args['tuple']['get_cycle_count()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.getCycleCount(params)))
        resultMappers.push((v) => client.decodeReturnValue('get_cycle_count()uint64', v))
        return this
      },
      /**
       * Add a clear state call to the AgentEscrowVaultV2 contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as AgentEscrowVaultV2Composer
  }
}
export type AgentEscrowVaultV2Composer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the release(uint64)uint64 ABI method.
   *
  * Release `amount` microalgos to recipient and append a BillingRecord.
  Returns the new cycle index. Callable by creator OR agent.

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  release(params?: CallParams<AgentEscrowVaultV2Args['obj']['release(uint64)uint64'] | AgentEscrowVaultV2Args['tuple']['release(uint64)uint64']>): AgentEscrowVaultV2Composer<[...TReturns, AgentEscrowVaultV2Returns['release(uint64)uint64'] | undefined]>

  /**
   * Calls the kill()void ABI method.
   *
   * Refund remaining balance to creator and mark vault killed.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  kill(params?: CallParams<AgentEscrowVaultV2Args['obj']['kill()void'] | AgentEscrowVaultV2Args['tuple']['kill()void']>): AgentEscrowVaultV2Composer<[...TReturns, AgentEscrowVaultV2Returns['kill()void'] | undefined]>

  /**
   * Calls the get_history(uint64)(uint64,uint64,uint64) ABI method.
   *
   * Read a single release record.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  getHistory(params?: CallParams<AgentEscrowVaultV2Args['obj']['get_history(uint64)(uint64,uint64,uint64)'] | AgentEscrowVaultV2Args['tuple']['get_history(uint64)(uint64,uint64,uint64)']>): AgentEscrowVaultV2Composer<[...TReturns, AgentEscrowVaultV2Returns['get_history(uint64)(uint64,uint64,uint64)'] | undefined]>

  /**
   * Calls the get_cycle_count()uint64 ABI method.
   *
   * Total number of releases.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  getCycleCount(params?: CallParams<AgentEscrowVaultV2Args['obj']['get_cycle_count()uint64'] | AgentEscrowVaultV2Args['tuple']['get_cycle_count()uint64']>): AgentEscrowVaultV2Composer<[...TReturns, AgentEscrowVaultV2Returns['get_cycle_count()uint64'] | undefined]>

  /**
   * Makes a clear_state call to an existing instance of the AgentEscrowVaultV2 smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): AgentEscrowVaultV2Composer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): AgentEscrowVaultV2Composer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<AgentEscrowVaultV2ComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<AgentEscrowVaultV2ComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<AgentEscrowVaultV2ComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<AgentEscrowVaultV2ComposerResults<TReturns>>
}
export type AgentEscrowVaultV2ComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

