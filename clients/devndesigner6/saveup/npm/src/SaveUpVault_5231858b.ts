/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"SaveUpVault","structs":{},"methods":[{"name":"create","args":[],"returns":{"type":"void"},"actions":{"create":["NoOp"],"call":[]},"readonly":false,"desc":"Deploy the contract and seed milestone thresholds in global state.","events":[],"recommendations":{}},{"name":"opt_in","args":[],"returns":{"type":"void"},"actions":{"create":[],"call":["OptIn"]},"readonly":false,"desc":"Initialise local state so a user can start depositing.","events":[],"recommendations":{}},{"name":"deposit","args":[{"type":"uint64","name":"amount"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Record a deposit.\nMust be called as part of a 2-transaction atomic group:     txn[0] — PaymentTransaction  (user → app address)     txn[1] — ApplicationCall     (this method)","events":[],"recommendations":{}},{"name":"withdraw","args":[{"type":"uint64","name":"amount"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Withdraw ALGO back to the user's wallet.\nReverts if the vault lock period has not yet expired.","events":[],"recommendations":{}},{"name":"withdraw_early","args":[{"type":"uint64","name":"amount"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Withdraw before the lock period expires, paying a 10% penalty.\nThe penalty stays in the contract; only the net amount is returned.","events":[],"recommendations":{}},{"name":"set_lock","args":[{"type":"uint64","name":"lock_until_round"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Lock the vault until a specific Algorand round number.","events":[],"recommendations":{}}],"arcs":[22,28],"desc":"On-chain gamified savings vault with tiered milestone badges.","networks":{},"state":{"schema":{"global":{"ints":6,"bytes":0},"local":{"ints":4,"bytes":0}},"keys":{"global":{"m1":{"keyType":"AVMString","valueType":"AVMUint64","key":"bTE="},"m2":{"keyType":"AVMString","valueType":"AVMUint64","key":"bTI="},"m3":{"keyType":"AVMString","valueType":"AVMUint64","key":"bTM="},"m4":{"keyType":"AVMString","valueType":"AVMUint64","key":"bTQ="},"m5":{"keyType":"AVMString","valueType":"AVMUint64","key":"bTU="},"total":{"keyType":"AVMString","valueType":"AVMUint64","key":"dG90YWw="}},"local":{"saved":{"keyType":"AVMString","valueType":"AVMUint64","key":"c2F2ZWQ="},"count":{"keyType":"AVMString","valueType":"AVMUint64","key":"Y291bnQ="},"lock":{"keyType":"AVMString","valueType":"AVMUint64","key":"bG9jaw=="},"level":{"keyType":"AVMString","valueType":"AVMUint64","key":"bGV2ZWw="}},"box":{}},"maps":{"global":{},"local":{},"box":{}}},"bareActions":{"create":[],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[411,472],"errorMessage":"Insufficient vault balance"},{"pc":[229],"errorMessage":"Payment must go to app"},{"pc":[236],"errorMessage":"Payment sender must match caller"},{"pc":[216],"errorMessage":"Requires a 2-txn atomic group"},{"pc":[401],"errorMessage":"Vault is still locked"},{"pc":[260],"errorMessage":"check self.count exists for account"},{"pc":[397],"errorMessage":"check self.lock exists for account"},{"pc":[367],"errorMessage":"check self.m1 exists"},{"pc":[348],"errorMessage":"check self.m2 exists"},{"pc":[328],"errorMessage":"check self.m3 exists"},{"pc":[308],"errorMessage":"check self.m4 exists"},{"pc":[291],"errorMessage":"check self.m5 exists"},{"pc":[245,286,407,433,468,500],"errorMessage":"check self.saved exists for account"},{"pc":[272,446,513],"errorMessage":"check self.total exists"},{"pc":[211,389,461,528],"errorMessage":"invalid number of bytes for arc4.uint64"},{"pc":[222],"errorMessage":"transaction type is pay"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"events":[]} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


/**
 * The argument types for the SaveUpVault contract
 */
export type SaveUpVaultArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'create()void': Record<string, never>
    'opt_in()void': Record<string, never>
    'deposit(uint64)void': {
      amount: bigint | number
    }
    'withdraw(uint64)void': {
      amount: bigint | number
    }
    'withdraw_early(uint64)void': {
      amount: bigint | number
    }
    'set_lock(uint64)void': {
      lockUntilRound: bigint | number
    }
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'create()void': []
    'opt_in()void': []
    'deposit(uint64)void': [amount: bigint | number]
    'withdraw(uint64)void': [amount: bigint | number]
    'withdraw_early(uint64)void': [amount: bigint | number]
    'set_lock(uint64)void': [lockUntilRound: bigint | number]
  }
}

/**
 * The return type for each method
 */
export type SaveUpVaultReturns = {
  'create()void': void
  'opt_in()void': void
  'deposit(uint64)void': void
  'withdraw(uint64)void': void
  'withdraw_early(uint64)void': void
  'set_lock(uint64)void': void
}

/**
 * Defines the types of available calls and state of the SaveUpVault smart contract.
 */
export type SaveUpVaultTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'create()void' | 'create', {
      argsObj: SaveUpVaultArgs['obj']['create()void']
      argsTuple: SaveUpVaultArgs['tuple']['create()void']
      returns: SaveUpVaultReturns['create()void']
    }>
    & Record<'opt_in()void' | 'opt_in', {
      argsObj: SaveUpVaultArgs['obj']['opt_in()void']
      argsTuple: SaveUpVaultArgs['tuple']['opt_in()void']
      returns: SaveUpVaultReturns['opt_in()void']
    }>
    & Record<'deposit(uint64)void' | 'deposit', {
      argsObj: SaveUpVaultArgs['obj']['deposit(uint64)void']
      argsTuple: SaveUpVaultArgs['tuple']['deposit(uint64)void']
      returns: SaveUpVaultReturns['deposit(uint64)void']
    }>
    & Record<'withdraw(uint64)void' | 'withdraw', {
      argsObj: SaveUpVaultArgs['obj']['withdraw(uint64)void']
      argsTuple: SaveUpVaultArgs['tuple']['withdraw(uint64)void']
      returns: SaveUpVaultReturns['withdraw(uint64)void']
    }>
    & Record<'withdraw_early(uint64)void' | 'withdraw_early', {
      argsObj: SaveUpVaultArgs['obj']['withdraw_early(uint64)void']
      argsTuple: SaveUpVaultArgs['tuple']['withdraw_early(uint64)void']
      returns: SaveUpVaultReturns['withdraw_early(uint64)void']
    }>
    & Record<'set_lock(uint64)void' | 'set_lock', {
      argsObj: SaveUpVaultArgs['obj']['set_lock(uint64)void']
      argsTuple: SaveUpVaultArgs['tuple']['set_lock(uint64)void']
      returns: SaveUpVaultReturns['set_lock(uint64)void']
    }>
  /**
   * Defines the shape of the state of the application.
   */
  state: {
    global: {
      keys: {
        m1: bigint
        m2: bigint
        m3: bigint
        m4: bigint
        m5: bigint
        total: bigint
      }
      maps: {}
    }
    local: {
      keys: {
        saved: bigint
        count: bigint
        lock: bigint
        level: bigint
      }
      maps: {}
    }
  }
}

/**
 * Defines the possible abi call signatures.
 */
export type SaveUpVaultSignatures = keyof SaveUpVaultTypes['methods']
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the SaveUpVault smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends SaveUpVaultSignatures> = SaveUpVaultTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the SaveUpVault smart contract to the method's return type
 */
export type MethodReturn<TSignature extends SaveUpVaultSignatures> = SaveUpVaultTypes['methods'][TSignature]['returns']

/**
 * Defines the shape of the keyed global state of the application.
 */
export type GlobalKeysState = SaveUpVaultTypes['state']['global']['keys']

/**
 * Defines the shape of the keyed local state of the application.
 */
export type LocalKeysState = SaveUpVaultTypes['state']['local']['keys']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the SaveUpVault smart contract
 */
export abstract class SaveUpVaultParamsFactory {
  /**
   * Gets available optIn ABI call param factories
   */
  static get optIn() {
    return {
      /**
       * Constructs opt-in ABI call params for the SaveUpVault smart contract using the opt_in()void ABI method
       *
       * @param params Parameters for the call
       * @returns An `AppClientMethodCallParams` object for the call
       */
      optIn(params: CallParams<SaveUpVaultArgs['obj']['opt_in()void'] | SaveUpVaultArgs['tuple']['opt_in()void']>): AppClientMethodCallParams {
        return {
          ...params,
          method: 'opt_in()void' as const,
          args: Array.isArray(params.args) ? params.args : [],
        }
      },
    }
  }

  /**
   * Constructs a no op call for the deposit(uint64)void ABI method
   *
  * Record a deposit.
  Must be called as part of a 2-transaction atomic group:     txn[0] — PaymentTransaction  (user → app address)     txn[1] — ApplicationCall     (this method)

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static deposit(params: CallParams<SaveUpVaultArgs['obj']['deposit(uint64)void'] | SaveUpVaultArgs['tuple']['deposit(uint64)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'deposit(uint64)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.amount],
    }
  }
  /**
   * Constructs a no op call for the withdraw(uint64)void ABI method
   *
  * Withdraw ALGO back to the user's wallet.
  Reverts if the vault lock period has not yet expired.

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static withdraw(params: CallParams<SaveUpVaultArgs['obj']['withdraw(uint64)void'] | SaveUpVaultArgs['tuple']['withdraw(uint64)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'withdraw(uint64)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.amount],
    }
  }
  /**
   * Constructs a no op call for the withdraw_early(uint64)void ABI method
   *
  * Withdraw before the lock period expires, paying a 10% penalty.
  The penalty stays in the contract; only the net amount is returned.

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static withdrawEarly(params: CallParams<SaveUpVaultArgs['obj']['withdraw_early(uint64)void'] | SaveUpVaultArgs['tuple']['withdraw_early(uint64)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'withdraw_early(uint64)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.amount],
    }
  }
  /**
   * Constructs a no op call for the set_lock(uint64)void ABI method
   *
   * Lock the vault until a specific Algorand round number.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static setLock(params: CallParams<SaveUpVaultArgs['obj']['set_lock(uint64)void'] | SaveUpVaultArgs['tuple']['set_lock(uint64)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'set_lock(uint64)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.lockUntilRound],
    }
  }
}

/**
 * A client to make calls to the SaveUpVault smart contract
 */
export class SaveUpVaultClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `SaveUpVaultClient`
   *
   * @param appClient An `AppClient` instance which has been created with the SaveUpVault app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `SaveUpVaultClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Returns a new `SaveUpVaultClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<SaveUpVaultClient> {
    return new SaveUpVaultClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `SaveUpVaultClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<SaveUpVaultClient> {
    return new SaveUpVaultClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Gets available optIn methods
     */
    optIn: {
      /**
       * Opts the user into an existing instance of the SaveUpVault smart contract using the `opt_in()void` ABI method.
       *
       * Initialise local state so a user can start depositing.
       *
       * @param params The params for the smart contract call
       * @returns The optIn params
       */
      optIn: (params: CallParams<SaveUpVaultArgs['obj']['opt_in()void'] | SaveUpVaultArgs['tuple']['opt_in()void']> = {args: []}) => {
        return this.appClient.params.optIn(SaveUpVaultParamsFactory.optIn.optIn(params))
      },

    },

    /**
     * Makes a clear_state call to an existing instance of the SaveUpVault smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the SaveUpVault smart contract using the `deposit(uint64)void` ABI method.
     *
    * Record a deposit.
    Must be called as part of a 2-transaction atomic group:     txn[0] — PaymentTransaction  (user → app address)     txn[1] — ApplicationCall     (this method)

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    deposit: (params: CallParams<SaveUpVaultArgs['obj']['deposit(uint64)void'] | SaveUpVaultArgs['tuple']['deposit(uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(SaveUpVaultParamsFactory.deposit(params))
    },

    /**
     * Makes a call to the SaveUpVault smart contract using the `withdraw(uint64)void` ABI method.
     *
    * Withdraw ALGO back to the user's wallet.
    Reverts if the vault lock period has not yet expired.

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    withdraw: (params: CallParams<SaveUpVaultArgs['obj']['withdraw(uint64)void'] | SaveUpVaultArgs['tuple']['withdraw(uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(SaveUpVaultParamsFactory.withdraw(params))
    },

    /**
     * Makes a call to the SaveUpVault smart contract using the `withdraw_early(uint64)void` ABI method.
     *
    * Withdraw before the lock period expires, paying a 10% penalty.
    The penalty stays in the contract; only the net amount is returned.

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    withdrawEarly: (params: CallParams<SaveUpVaultArgs['obj']['withdraw_early(uint64)void'] | SaveUpVaultArgs['tuple']['withdraw_early(uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(SaveUpVaultParamsFactory.withdrawEarly(params))
    },

    /**
     * Makes a call to the SaveUpVault smart contract using the `set_lock(uint64)void` ABI method.
     *
     * Lock the vault until a specific Algorand round number.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    setLock: (params: CallParams<SaveUpVaultArgs['obj']['set_lock(uint64)void'] | SaveUpVaultArgs['tuple']['set_lock(uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(SaveUpVaultParamsFactory.setLock(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Gets available optIn methods
     */
    optIn: {
      /**
       * Opts the user into an existing instance of the SaveUpVault smart contract using the `opt_in()void` ABI method.
       *
       * Initialise local state so a user can start depositing.
       *
       * @param params The params for the smart contract call
       * @returns The optIn transaction
       */
      optIn: (params: CallParams<SaveUpVaultArgs['obj']['opt_in()void'] | SaveUpVaultArgs['tuple']['opt_in()void']> = {args: []}) => {
        return this.appClient.createTransaction.optIn(SaveUpVaultParamsFactory.optIn.optIn(params))
      },

    },

    /**
     * Makes a clear_state call to an existing instance of the SaveUpVault smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the SaveUpVault smart contract using the `deposit(uint64)void` ABI method.
     *
    * Record a deposit.
    Must be called as part of a 2-transaction atomic group:     txn[0] — PaymentTransaction  (user → app address)     txn[1] — ApplicationCall     (this method)

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    deposit: (params: CallParams<SaveUpVaultArgs['obj']['deposit(uint64)void'] | SaveUpVaultArgs['tuple']['deposit(uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(SaveUpVaultParamsFactory.deposit(params))
    },

    /**
     * Makes a call to the SaveUpVault smart contract using the `withdraw(uint64)void` ABI method.
     *
    * Withdraw ALGO back to the user's wallet.
    Reverts if the vault lock period has not yet expired.

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    withdraw: (params: CallParams<SaveUpVaultArgs['obj']['withdraw(uint64)void'] | SaveUpVaultArgs['tuple']['withdraw(uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(SaveUpVaultParamsFactory.withdraw(params))
    },

    /**
     * Makes a call to the SaveUpVault smart contract using the `withdraw_early(uint64)void` ABI method.
     *
    * Withdraw before the lock period expires, paying a 10% penalty.
    The penalty stays in the contract; only the net amount is returned.

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    withdrawEarly: (params: CallParams<SaveUpVaultArgs['obj']['withdraw_early(uint64)void'] | SaveUpVaultArgs['tuple']['withdraw_early(uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(SaveUpVaultParamsFactory.withdrawEarly(params))
    },

    /**
     * Makes a call to the SaveUpVault smart contract using the `set_lock(uint64)void` ABI method.
     *
     * Lock the vault until a specific Algorand round number.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    setLock: (params: CallParams<SaveUpVaultArgs['obj']['set_lock(uint64)void'] | SaveUpVaultArgs['tuple']['set_lock(uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(SaveUpVaultParamsFactory.setLock(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Gets available optIn methods
     */
    optIn: {
      /**
       * Opts the user into an existing instance of the SaveUpVault smart contract using the `opt_in()void` ABI method.
       *
       * Initialise local state so a user can start depositing.
       *
       * @param params The params for the smart contract call
       * @returns The optIn result
       */
      optIn: async (params: CallParams<SaveUpVaultArgs['obj']['opt_in()void'] | SaveUpVaultArgs['tuple']['opt_in()void']> & SendParams = {args: []}) => {
        const result = await this.appClient.send.optIn(SaveUpVaultParamsFactory.optIn.optIn(params))
        return {...result, return: result.return as unknown as (undefined | SaveUpVaultReturns['opt_in()void'])}
      },

    },

    /**
     * Makes a clear_state call to an existing instance of the SaveUpVault smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the SaveUpVault smart contract using the `deposit(uint64)void` ABI method.
     *
    * Record a deposit.
    Must be called as part of a 2-transaction atomic group:     txn[0] — PaymentTransaction  (user → app address)     txn[1] — ApplicationCall     (this method)

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    deposit: async (params: CallParams<SaveUpVaultArgs['obj']['deposit(uint64)void'] | SaveUpVaultArgs['tuple']['deposit(uint64)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(SaveUpVaultParamsFactory.deposit(params))
      return {...result, return: result.return as unknown as (undefined | SaveUpVaultReturns['deposit(uint64)void'])}
    },

    /**
     * Makes a call to the SaveUpVault smart contract using the `withdraw(uint64)void` ABI method.
     *
    * Withdraw ALGO back to the user's wallet.
    Reverts if the vault lock period has not yet expired.

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    withdraw: async (params: CallParams<SaveUpVaultArgs['obj']['withdraw(uint64)void'] | SaveUpVaultArgs['tuple']['withdraw(uint64)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(SaveUpVaultParamsFactory.withdraw(params))
      return {...result, return: result.return as unknown as (undefined | SaveUpVaultReturns['withdraw(uint64)void'])}
    },

    /**
     * Makes a call to the SaveUpVault smart contract using the `withdraw_early(uint64)void` ABI method.
     *
    * Withdraw before the lock period expires, paying a 10% penalty.
    The penalty stays in the contract; only the net amount is returned.

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    withdrawEarly: async (params: CallParams<SaveUpVaultArgs['obj']['withdraw_early(uint64)void'] | SaveUpVaultArgs['tuple']['withdraw_early(uint64)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(SaveUpVaultParamsFactory.withdrawEarly(params))
      return {...result, return: result.return as unknown as (undefined | SaveUpVaultReturns['withdraw_early(uint64)void'])}
    },

    /**
     * Makes a call to the SaveUpVault smart contract using the `set_lock(uint64)void` ABI method.
     *
     * Lock the vault until a specific Algorand round number.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    setLock: async (params: CallParams<SaveUpVaultArgs['obj']['set_lock(uint64)void'] | SaveUpVaultArgs['tuple']['set_lock(uint64)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(SaveUpVaultParamsFactory.setLock(params))
      return {...result, return: result.return as unknown as (undefined | SaveUpVaultReturns['set_lock(uint64)void'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new SaveUpVaultClient(this.appClient.clone(params))
  }

  /**
   * Methods to access state for the current SaveUpVault app
   */
  state = {
    /**
     * Methods to access global state for the current SaveUpVault app
     */
    global: {
      /**
       * Get all current keyed values from global state
       */
      getAll: async (): Promise<Partial<Expand<GlobalKeysState>>> => {
        const result = await this.appClient.state.global.getAll()
        return {
          m1: result.m1,
          m2: result.m2,
          m3: result.m3,
          m4: result.m4,
          m5: result.m5,
          total: result.total,
        }
      },
      /**
       * Get the current value of the m1 key in global state
       */
      m1: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("m1")) as bigint | undefined },
      /**
       * Get the current value of the m2 key in global state
       */
      m2: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("m2")) as bigint | undefined },
      /**
       * Get the current value of the m3 key in global state
       */
      m3: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("m3")) as bigint | undefined },
      /**
       * Get the current value of the m4 key in global state
       */
      m4: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("m4")) as bigint | undefined },
      /**
       * Get the current value of the m5 key in global state
       */
      m5: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("m5")) as bigint | undefined },
      /**
       * Get the current value of the total key in global state
       */
      total: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("total")) as bigint | undefined },
    },
    /**
     * Methods to access local state for the current SaveUpVault app
     */
    local: (address: string | Address) => {
      const encodedAddress = typeof address === 'string' ? address : encodeAddress(address.publicKey)
      return {
        /**
         * Get all current keyed values from local state
         */
        getAll: async (): Promise<Partial<Expand<LocalKeysState>>> => {
          const result = await this.appClient.state.local(encodedAddress).getAll()
          return {
            saved: result.saved,
            count: result.count,
            lock: result.lock,
            level: result.level,
          }
        },
        /**
         * Get the current value of the saved key in local state
         */
        saved: async (): Promise<bigint | undefined> => { return (await this.appClient.state.local(encodedAddress).getValue("saved")) as bigint | undefined },
        /**
         * Get the current value of the count key in local state
         */
        count: async (): Promise<bigint | undefined> => { return (await this.appClient.state.local(encodedAddress).getValue("count")) as bigint | undefined },
        /**
         * Get the current value of the lock key in local state
         */
        lock: async (): Promise<bigint | undefined> => { return (await this.appClient.state.local(encodedAddress).getValue("lock")) as bigint | undefined },
        /**
         * Get the current value of the level key in local state
         */
        level: async (): Promise<bigint | undefined> => { return (await this.appClient.state.local(encodedAddress).getValue("level")) as bigint | undefined },
      }
    },
  }

  public newGroup(): SaveUpVaultComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a deposit(uint64)void method call against the SaveUpVault contract
       */
      deposit(params: CallParams<SaveUpVaultArgs['obj']['deposit(uint64)void'] | SaveUpVaultArgs['tuple']['deposit(uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.deposit(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a withdraw(uint64)void method call against the SaveUpVault contract
       */
      withdraw(params: CallParams<SaveUpVaultArgs['obj']['withdraw(uint64)void'] | SaveUpVaultArgs['tuple']['withdraw(uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.withdraw(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a withdraw_early(uint64)void method call against the SaveUpVault contract
       */
      withdrawEarly(params: CallParams<SaveUpVaultArgs['obj']['withdraw_early(uint64)void'] | SaveUpVaultArgs['tuple']['withdraw_early(uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.withdrawEarly(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a set_lock(uint64)void method call against the SaveUpVault contract
       */
      setLock(params: CallParams<SaveUpVaultArgs['obj']['set_lock(uint64)void'] | SaveUpVaultArgs['tuple']['set_lock(uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.setLock(params)))
        resultMappers.push(undefined)
        return this
      },
      get optIn() {
        return {
          optIn: (params: CallParams<SaveUpVaultArgs['obj']['opt_in()void'] | SaveUpVaultArgs['tuple']['opt_in()void']>) => {
            promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.optIn.optIn(params)))
            resultMappers.push(undefined)
            return this
          },
        }
      },
      /**
       * Add a clear state call to the SaveUpVault contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as SaveUpVaultComposer
  }
}
export type SaveUpVaultComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the deposit(uint64)void ABI method.
   *
  * Record a deposit.
  Must be called as part of a 2-transaction atomic group:     txn[0] — PaymentTransaction  (user → app address)     txn[1] — ApplicationCall     (this method)

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  deposit(params?: CallParams<SaveUpVaultArgs['obj']['deposit(uint64)void'] | SaveUpVaultArgs['tuple']['deposit(uint64)void']>): SaveUpVaultComposer<[...TReturns, SaveUpVaultReturns['deposit(uint64)void'] | undefined]>

  /**
   * Calls the withdraw(uint64)void ABI method.
   *
  * Withdraw ALGO back to the user's wallet.
  Reverts if the vault lock period has not yet expired.

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  withdraw(params?: CallParams<SaveUpVaultArgs['obj']['withdraw(uint64)void'] | SaveUpVaultArgs['tuple']['withdraw(uint64)void']>): SaveUpVaultComposer<[...TReturns, SaveUpVaultReturns['withdraw(uint64)void'] | undefined]>

  /**
   * Calls the withdraw_early(uint64)void ABI method.
   *
  * Withdraw before the lock period expires, paying a 10% penalty.
  The penalty stays in the contract; only the net amount is returned.

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  withdrawEarly(params?: CallParams<SaveUpVaultArgs['obj']['withdraw_early(uint64)void'] | SaveUpVaultArgs['tuple']['withdraw_early(uint64)void']>): SaveUpVaultComposer<[...TReturns, SaveUpVaultReturns['withdraw_early(uint64)void'] | undefined]>

  /**
   * Calls the set_lock(uint64)void ABI method.
   *
   * Lock the vault until a specific Algorand round number.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  setLock(params?: CallParams<SaveUpVaultArgs['obj']['set_lock(uint64)void'] | SaveUpVaultArgs['tuple']['set_lock(uint64)void']>): SaveUpVaultComposer<[...TReturns, SaveUpVaultReturns['set_lock(uint64)void'] | undefined]>

  /**
   * Gets available optIn methods
   */
  readonly optIn: {
    /**
     * Opts the user into an existing instance of the SaveUpVault smart contract using the opt_in()void ABI method.
     *
     * @param args The arguments for the smart contract call
     * @param params Any additional parameters for the call
     * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
     */
    optIn(params?: CallParams<SaveUpVaultArgs['obj']['opt_in()void'] | SaveUpVaultArgs['tuple']['opt_in()void']>): SaveUpVaultComposer<[...TReturns, SaveUpVaultReturns['opt_in()void'] | undefined]>
  }

  /**
   * Makes a clear_state call to an existing instance of the SaveUpVault smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): SaveUpVaultComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): SaveUpVaultComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<SaveUpVaultComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<SaveUpVaultComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<SaveUpVaultComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<SaveUpVaultComposerResults<TReturns>>
}
export type SaveUpVaultComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

