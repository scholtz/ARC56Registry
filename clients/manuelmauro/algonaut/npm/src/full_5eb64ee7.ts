/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"arcs":[22,28,56],"name":"Vault","desc":"A vault contract exercising the full ARC-56 schema","networks":{"wGHE2Pwdvd7S12BL5FaOP20EGYesN73ktiC1qzkkit8=":{"appID":1001},"SGO1GKSzyE7IEPItTxCByw9x8FmnrCDexi9/cOUJOiI=":{"appID":2002}},"structs":{"Pair":[{"name":"first","type":"uint64"},{"name":"second","type":"uint64"}],"Outer":[{"name":"inner","type":[{"name":"a","type":"uint64"}]}]},"methods":[{"name":"store","desc":"Store a pair scaled by a factor","args":[{"type":"(uint64,uint64)","struct":"Pair","name":"p"},{"type":"uint64","name":"factor","defaultValue":{"source":"literal","data":"AAAAAAAAAAE=","type":"uint64"}}],"returns":{"type":"(uint64,uint64)","struct":"Pair","desc":"the stored pair"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{"innerTransactionCount":1,"boxes":{"key":"Ym94","readBytes":100,"writeBytes":100},"apps":[123],"accounts":[],"assets":[]}},{"name":"get_total","args":[],"returns":{"type":"uint64"},"actions":{"create":[],"call":["NoOp"]},"readonly":true}],"state":{"schema":{"global":{"ints":1,"bytes":0},"local":{"ints":0,"bytes":0}},"keys":{"global":{"total":{"keyType":"AVMString","valueType":"uint64","key":"dG90YWw="}},"local":{},"box":{}},"maps":{"global":{},"local":{},"box":{"balances":{"keyType":"address","valueType":"uint64","prefix":"Yg=="}}}},"bareActions":{"create":["NoOp"],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[0,1],"errorMessage":"assert failed","teal":5}],"pcOffsetMethod":"cblocks"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"events":[{"name":"Stored","desc":"emitted on store","args":[{"type":"uint64","name":"value"}]}]} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


// Type definitions for ARC-56 structs

export type Pair = {
  first: bigint,
  second: bigint
}


/**
 * Converts the ABI tuple representation of a Pair to the struct representation
 */
export function PairFromTuple(abiTuple: [bigint, bigint]) {
  return getABIStructFromABITuple(abiTuple, APP_SPEC.structs.Pair, APP_SPEC.structs) as Pair
}

export type Outer = {
  inner: {
    a: bigint
  }
}


/**
 * Converts the ABI tuple representation of a Outer to the struct representation
 */
export function OuterFromTuple(abiTuple: [[bigint]]) {
  return getABIStructFromABITuple(abiTuple, APP_SPEC.structs.Outer, APP_SPEC.structs) as Outer
}

/**
 * The argument types for the Vault contract
 */
export type VaultArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'store((uint64,uint64),uint64)(uint64,uint64)': {
      p: Pair
      factor?: bigint | number
    }
    'get_total()uint64': Record<string, never>
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'store((uint64,uint64),uint64)(uint64,uint64)': [p: Pair, factor: bigint | number | undefined]
    'get_total()uint64': []
  }
}

/**
 * The return type for each method
 */
export type VaultReturns = {
  'store((uint64,uint64),uint64)(uint64,uint64)': Pair
  'get_total()uint64': bigint
}

/**
 * Defines the types of available calls and state of the Vault smart contract.
 */
export type VaultTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'store((uint64,uint64),uint64)(uint64,uint64)' | 'store', {
      argsObj: VaultArgs['obj']['store((uint64,uint64),uint64)(uint64,uint64)']
      argsTuple: VaultArgs['tuple']['store((uint64,uint64),uint64)(uint64,uint64)']
      /**
       * the stored pair
       */
      returns: VaultReturns['store((uint64,uint64),uint64)(uint64,uint64)']
    }>
    & Record<'get_total()uint64' | 'get_total', {
      argsObj: VaultArgs['obj']['get_total()uint64']
      argsTuple: VaultArgs['tuple']['get_total()uint64']
      returns: VaultReturns['get_total()uint64']
    }>
  /**
   * Defines the shape of the state of the application.
   */
  state: {
    global: {
      keys: {
        total: bigint
      }
      maps: {}
    }
    box: {
      keys: {}
      maps: {
        balances: Map<string, bigint>
      }
    }
  }
}

/**
 * Defines the possible abi call signatures.
 */
export type VaultSignatures = keyof VaultTypes['methods']
/**
 * Defines the possible abi call signatures for methods that return a non-void value.
 */
export type VaultNonVoidMethodSignatures = keyof VaultTypes['methods'] extends infer T ? T extends keyof VaultTypes['methods'] ? MethodReturn<T> extends void ? never : T  : never : never
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the Vault smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends VaultSignatures> = VaultTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the Vault smart contract to the method's return type
 */
export type MethodReturn<TSignature extends VaultSignatures> = VaultTypes['methods'][TSignature]['returns']

/**
 * Defines the shape of the keyed global state of the application.
 */
export type GlobalKeysState = VaultTypes['state']['global']['keys']

/**
 * Defines the shape of the keyed box state of the application.
 */
export type BoxKeysState = VaultTypes['state']['box']['keys']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the Vault smart contract
 */
export abstract class VaultParamsFactory {
  /**
   * Constructs a no op call for the store((uint64,uint64),uint64)(uint64,uint64) ABI method
   *
   * Store a pair scaled by a factor
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static store(params: CallParams<VaultArgs['obj']['store((uint64,uint64),uint64)(uint64,uint64)'] | VaultArgs['tuple']['store((uint64,uint64),uint64)(uint64,uint64)']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'store((uint64,uint64),uint64)(uint64,uint64)' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.p, params.args.factor],
    }
  }
  /**
   * Constructs a no op call for the get_total()uint64 ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static getTotal(params: CallParams<VaultArgs['obj']['get_total()uint64'] | VaultArgs['tuple']['get_total()uint64']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'get_total()uint64' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
}

/**
 * A client to make calls to the Vault smart contract
 */
export class VaultClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `VaultClient`
   *
   * @param appClient An `AppClient` instance which has been created with the Vault app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `VaultClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Checks for decode errors on the given return value and maps the return value to the return type for the given method
   * @returns The typed return value or undefined if there was no value
   */
  decodeReturnValue<TSignature extends VaultNonVoidMethodSignatures>(method: TSignature, returnValue: ABIReturn | undefined) {
    return returnValue !== undefined ? getArc56ReturnValue<MethodReturn<TSignature>>(returnValue, this.appClient.getABIMethod(method), APP_SPEC.structs) : undefined
  }

  /**
   * Returns a new `VaultClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<VaultClient> {
    return new VaultClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `VaultClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<VaultClient> {
    return new VaultClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Makes a clear_state call to an existing instance of the Vault smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the Vault smart contract using the `store((uint64,uint64),uint64)(uint64,uint64)` ABI method.
     *
     * Store a pair scaled by a factor
     *
     * @param params The params for the smart contract call
     * @returns The call params: the stored pair
     */
    store: (params: CallParams<VaultArgs['obj']['store((uint64,uint64),uint64)(uint64,uint64)'] | VaultArgs['tuple']['store((uint64,uint64),uint64)(uint64,uint64)']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(VaultParamsFactory.store(params))
    },

    /**
     * Makes a call to the Vault smart contract using the `get_total()uint64` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    getTotal: (params: CallParams<VaultArgs['obj']['get_total()uint64'] | VaultArgs['tuple']['get_total()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(VaultParamsFactory.getTotal(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Makes a clear_state call to an existing instance of the Vault smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the Vault smart contract using the `store((uint64,uint64),uint64)(uint64,uint64)` ABI method.
     *
     * Store a pair scaled by a factor
     *
     * @param params The params for the smart contract call
     * @returns The call transaction: the stored pair
     */
    store: (params: CallParams<VaultArgs['obj']['store((uint64,uint64),uint64)(uint64,uint64)'] | VaultArgs['tuple']['store((uint64,uint64),uint64)(uint64,uint64)']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(VaultParamsFactory.store(params))
    },

    /**
     * Makes a call to the Vault smart contract using the `get_total()uint64` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    getTotal: (params: CallParams<VaultArgs['obj']['get_total()uint64'] | VaultArgs['tuple']['get_total()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(VaultParamsFactory.getTotal(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Makes a clear_state call to an existing instance of the Vault smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the Vault smart contract using the `store((uint64,uint64),uint64)(uint64,uint64)` ABI method.
     *
     * Store a pair scaled by a factor
     *
     * @param params The params for the smart contract call
     * @returns The call result: the stored pair
     */
    store: async (params: CallParams<VaultArgs['obj']['store((uint64,uint64),uint64)(uint64,uint64)'] | VaultArgs['tuple']['store((uint64,uint64),uint64)(uint64,uint64)']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(VaultParamsFactory.store(params))
      return {...result, return: result.return as unknown as (undefined | VaultReturns['store((uint64,uint64),uint64)(uint64,uint64)'])}
    },

    /**
     * Makes a call to the Vault smart contract using the `get_total()uint64` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    getTotal: async (params: CallParams<VaultArgs['obj']['get_total()uint64'] | VaultArgs['tuple']['get_total()uint64']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(VaultParamsFactory.getTotal(params))
      return {...result, return: result.return as unknown as (undefined | VaultReturns['get_total()uint64'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new VaultClient(this.appClient.clone(params))
  }

  /**
   * Makes a readonly (simulated) call to the Vault smart contract using the `get_total()uint64` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async getTotal(params: CallParams<VaultArgs['obj']['get_total()uint64'] | VaultArgs['tuple']['get_total()uint64']> = {args: []}) {
    const result = await this.appClient.send.call(VaultParamsFactory.getTotal(params))
    return result.return as unknown as VaultReturns['get_total()uint64']
  }

  /**
   * Methods to access state for the current Vault app
   */
  state = {
    /**
     * Methods to access global state for the current Vault app
     */
    global: {
      /**
       * Get all current keyed values from global state
       */
      getAll: async (): Promise<Partial<Expand<GlobalKeysState>>> => {
        const result = await this.appClient.state.global.getAll()
        return {
          total: result.total,
        }
      },
      /**
       * Get the current value of the total key in global state
       */
      total: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("total")) as bigint | undefined },
    },
    /**
     * Methods to access box state for the current Vault app
     */
    box: {
      /**
       * Get all current keyed values from box state
       */
      getAll: async (): Promise<Partial<Expand<BoxKeysState>>> => {
        const result = await this.appClient.state.box.getAll()
        return {
        }
      },
      /**
       * Get values from the balances map in box state
       */
      balances: {
        /**
         * Get all current values of the balances map in box state
         */
        getMap: async (): Promise<Map<string, bigint>> => { return (await this.appClient.state.box.getMap("balances")) as Map<string, bigint> },
        /**
         * Get a current value of the balances map by key from box state
         */
        value: async (key: string): Promise<bigint | undefined> => { return await this.appClient.state.box.getMapValue("balances", key) as bigint | undefined },
      },
    },
  }

  public newGroup(): VaultComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a store((uint64,uint64),uint64)(uint64,uint64) method call against the Vault contract
       */
      store(params: CallParams<VaultArgs['obj']['store((uint64,uint64),uint64)(uint64,uint64)'] | VaultArgs['tuple']['store((uint64,uint64),uint64)(uint64,uint64)']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.store(params)))
        resultMappers.push((v) => client.decodeReturnValue('store((uint64,uint64),uint64)(uint64,uint64)', v))
        return this
      },
      /**
       * Add a get_total()uint64 method call against the Vault contract
       */
      getTotal(params: CallParams<VaultArgs['obj']['get_total()uint64'] | VaultArgs['tuple']['get_total()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.getTotal(params)))
        resultMappers.push((v) => client.decodeReturnValue('get_total()uint64', v))
        return this
      },
      /**
       * Add a clear state call to the Vault contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as VaultComposer
  }
}
export type VaultComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the store((uint64,uint64),uint64)(uint64,uint64) ABI method.
   *
   * Store a pair scaled by a factor
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  store(params?: CallParams<VaultArgs['obj']['store((uint64,uint64),uint64)(uint64,uint64)'] | VaultArgs['tuple']['store((uint64,uint64),uint64)(uint64,uint64)']>): VaultComposer<[...TReturns, VaultReturns['store((uint64,uint64),uint64)(uint64,uint64)'] | undefined]>

  /**
   * Calls the get_total()uint64 ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  getTotal(params?: CallParams<VaultArgs['obj']['get_total()uint64'] | VaultArgs['tuple']['get_total()uint64']>): VaultComposer<[...TReturns, VaultReturns['get_total()uint64'] | undefined]>

  /**
   * Makes a clear_state call to an existing instance of the Vault smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): VaultComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): VaultComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<VaultComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<VaultComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<VaultComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<VaultComposerResults<TReturns>>
}
export type VaultComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

