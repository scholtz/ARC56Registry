/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"CertificateManager","structs":{},"methods":[{"name":"issue_certificate","args":[{"type":"account","name":"wallet"},{"type":"string","name":"metadata_hash"},{"type":"uint64","name":"expires_at_round"}],"returns":{"type":"uint64"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}},{"name":"revoke_certificate","args":[{"type":"uint64","name":"certificate_id"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}},{"name":"verify_certificate","args":[{"type":"uint64","name":"certificate_id"},{"type":"account","name":"wallet"}],"returns":{"type":"bool"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"get_certificate_metadata","args":[{"type":"uint64","name":"certificate_id"}],"returns":{"type":"string"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"is_certificate_active","args":[{"type":"uint64","name":"certificate_id"}],"returns":{"type":"bool"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}}],"arcs":[22,28],"networks":{},"state":{"schema":{"global":{"ints":1,"bytes":0},"local":{"ints":0,"bytes":0}},"keys":{"global":{"next_certificate_id":{"keyType":"AVMString","valueType":"AVMUint64","key":"bmV4dF9jZXJ0aWZpY2F0ZV9pZA=="}},"local":{},"box":{}},"maps":{"global":{},"local":{},"box":{"certificate_owner":{"keyType":"uint64","valueType":"address","prefix":"Y21fb3duZXJf"},"certificate_metadata":{"keyType":"uint64","valueType":"AVMString","prefix":"Y21fbWV0YV8="},"certificate_expiry":{"keyType":"uint64","valueType":"uint64","prefix":"Y21fZXhwXw=="},"certificate_active":{"keyType":"uint64","valueType":"uint64","prefix":"Y21fYWN0Xw=="}}}},"bareActions":{"create":["NoOp"],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[352],"errorMessage":"Certificate not found"},{"pc":[285],"errorMessage":"Expiry round must be > 0"},{"pc":[282],"errorMessage":"Metadata hash required"},{"pc":[129,154,182,213,229],"errorMessage":"OnCompletion is not NoOp"},{"pc":[278],"errorMessage":"Only creator can issue certificates"},{"pc":[341],"errorMessage":"Only creator can revoke certificates"},{"pc":[267],"errorMessage":"can only call when creating"},{"pc":[132,157,185,216,232],"errorMessage":"can only call when not creating"},{"pc":[289],"errorMessage":"check self.next_certificate_id exists"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"events":[]} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


/**
 * The argument types for the CertificateManager contract
 */
export type CertificateManagerArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'issue_certificate(account,string,uint64)uint64': {
      wallet: Uint8Array | string
      metadataHash: string
      expiresAtRound: bigint | number
    }
    'revoke_certificate(uint64)void': {
      certificateId: bigint | number
    }
    'verify_certificate(uint64,account)bool': {
      certificateId: bigint | number
      wallet: Uint8Array | string
    }
    'get_certificate_metadata(uint64)string': {
      certificateId: bigint | number
    }
    'is_certificate_active(uint64)bool': {
      certificateId: bigint | number
    }
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'issue_certificate(account,string,uint64)uint64': [wallet: Uint8Array | string, metadataHash: string, expiresAtRound: bigint | number]
    'revoke_certificate(uint64)void': [certificateId: bigint | number]
    'verify_certificate(uint64,account)bool': [certificateId: bigint | number, wallet: Uint8Array | string]
    'get_certificate_metadata(uint64)string': [certificateId: bigint | number]
    'is_certificate_active(uint64)bool': [certificateId: bigint | number]
  }
}

/**
 * The return type for each method
 */
export type CertificateManagerReturns = {
  'issue_certificate(account,string,uint64)uint64': bigint
  'revoke_certificate(uint64)void': void
  'verify_certificate(uint64,account)bool': boolean
  'get_certificate_metadata(uint64)string': string
  'is_certificate_active(uint64)bool': boolean
}

/**
 * Defines the types of available calls and state of the CertificateManager smart contract.
 */
export type CertificateManagerTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'issue_certificate(account,string,uint64)uint64' | 'issue_certificate', {
      argsObj: CertificateManagerArgs['obj']['issue_certificate(account,string,uint64)uint64']
      argsTuple: CertificateManagerArgs['tuple']['issue_certificate(account,string,uint64)uint64']
      returns: CertificateManagerReturns['issue_certificate(account,string,uint64)uint64']
    }>
    & Record<'revoke_certificate(uint64)void' | 'revoke_certificate', {
      argsObj: CertificateManagerArgs['obj']['revoke_certificate(uint64)void']
      argsTuple: CertificateManagerArgs['tuple']['revoke_certificate(uint64)void']
      returns: CertificateManagerReturns['revoke_certificate(uint64)void']
    }>
    & Record<'verify_certificate(uint64,account)bool' | 'verify_certificate', {
      argsObj: CertificateManagerArgs['obj']['verify_certificate(uint64,account)bool']
      argsTuple: CertificateManagerArgs['tuple']['verify_certificate(uint64,account)bool']
      returns: CertificateManagerReturns['verify_certificate(uint64,account)bool']
    }>
    & Record<'get_certificate_metadata(uint64)string' | 'get_certificate_metadata', {
      argsObj: CertificateManagerArgs['obj']['get_certificate_metadata(uint64)string']
      argsTuple: CertificateManagerArgs['tuple']['get_certificate_metadata(uint64)string']
      returns: CertificateManagerReturns['get_certificate_metadata(uint64)string']
    }>
    & Record<'is_certificate_active(uint64)bool' | 'is_certificate_active', {
      argsObj: CertificateManagerArgs['obj']['is_certificate_active(uint64)bool']
      argsTuple: CertificateManagerArgs['tuple']['is_certificate_active(uint64)bool']
      returns: CertificateManagerReturns['is_certificate_active(uint64)bool']
    }>
  /**
   * Defines the shape of the state of the application.
   */
  state: {
    global: {
      keys: {
        nextCertificateId: bigint
      }
      maps: {}
    }
    box: {
      keys: {}
      maps: {
        certificateOwner: Map<bigint | number, string>
        certificateMetadata: Map<bigint | number, string>
        certificateExpiry: Map<bigint | number, bigint>
        certificateActive: Map<bigint | number, bigint>
      }
    }
  }
}

/**
 * Defines the possible abi call signatures.
 */
export type CertificateManagerSignatures = keyof CertificateManagerTypes['methods']
/**
 * Defines the possible abi call signatures for methods that return a non-void value.
 */
export type CertificateManagerNonVoidMethodSignatures = keyof CertificateManagerTypes['methods'] extends infer T ? T extends keyof CertificateManagerTypes['methods'] ? MethodReturn<T> extends void ? never : T  : never : never
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the CertificateManager smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends CertificateManagerSignatures> = CertificateManagerTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the CertificateManager smart contract to the method's return type
 */
export type MethodReturn<TSignature extends CertificateManagerSignatures> = CertificateManagerTypes['methods'][TSignature]['returns']

/**
 * Defines the shape of the keyed global state of the application.
 */
export type GlobalKeysState = CertificateManagerTypes['state']['global']['keys']

/**
 * Defines the shape of the keyed box state of the application.
 */
export type BoxKeysState = CertificateManagerTypes['state']['box']['keys']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the CertificateManager smart contract
 */
export abstract class CertificateManagerParamsFactory {
  /**
   * Constructs a no op call for the issue_certificate(account,string,uint64)uint64 ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static issueCertificate(params: CallParams<CertificateManagerArgs['obj']['issue_certificate(account,string,uint64)uint64'] | CertificateManagerArgs['tuple']['issue_certificate(account,string,uint64)uint64']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'issue_certificate(account,string,uint64)uint64' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.wallet, params.args.metadataHash, params.args.expiresAtRound],
    }
  }
  /**
   * Constructs a no op call for the revoke_certificate(uint64)void ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static revokeCertificate(params: CallParams<CertificateManagerArgs['obj']['revoke_certificate(uint64)void'] | CertificateManagerArgs['tuple']['revoke_certificate(uint64)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'revoke_certificate(uint64)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.certificateId],
    }
  }
  /**
   * Constructs a no op call for the verify_certificate(uint64,account)bool ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static verifyCertificate(params: CallParams<CertificateManagerArgs['obj']['verify_certificate(uint64,account)bool'] | CertificateManagerArgs['tuple']['verify_certificate(uint64,account)bool']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'verify_certificate(uint64,account)bool' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.certificateId, params.args.wallet],
    }
  }
  /**
   * Constructs a no op call for the get_certificate_metadata(uint64)string ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static getCertificateMetadata(params: CallParams<CertificateManagerArgs['obj']['get_certificate_metadata(uint64)string'] | CertificateManagerArgs['tuple']['get_certificate_metadata(uint64)string']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'get_certificate_metadata(uint64)string' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.certificateId],
    }
  }
  /**
   * Constructs a no op call for the is_certificate_active(uint64)bool ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static isCertificateActive(params: CallParams<CertificateManagerArgs['obj']['is_certificate_active(uint64)bool'] | CertificateManagerArgs['tuple']['is_certificate_active(uint64)bool']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'is_certificate_active(uint64)bool' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.certificateId],
    }
  }
}

/**
 * A client to make calls to the CertificateManager smart contract
 */
export class CertificateManagerClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `CertificateManagerClient`
   *
   * @param appClient An `AppClient` instance which has been created with the CertificateManager app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `CertificateManagerClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Checks for decode errors on the given return value and maps the return value to the return type for the given method
   * @returns The typed return value or undefined if there was no value
   */
  decodeReturnValue<TSignature extends CertificateManagerNonVoidMethodSignatures>(method: TSignature, returnValue: ABIReturn | undefined) {
    return returnValue !== undefined ? getArc56ReturnValue<MethodReturn<TSignature>>(returnValue, this.appClient.getABIMethod(method), APP_SPEC.structs) : undefined
  }

  /**
   * Returns a new `CertificateManagerClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<CertificateManagerClient> {
    return new CertificateManagerClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `CertificateManagerClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<CertificateManagerClient> {
    return new CertificateManagerClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Makes a clear_state call to an existing instance of the CertificateManager smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the CertificateManager smart contract using the `issue_certificate(account,string,uint64)uint64` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    issueCertificate: (params: CallParams<CertificateManagerArgs['obj']['issue_certificate(account,string,uint64)uint64'] | CertificateManagerArgs['tuple']['issue_certificate(account,string,uint64)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(CertificateManagerParamsFactory.issueCertificate(params))
    },

    /**
     * Makes a call to the CertificateManager smart contract using the `revoke_certificate(uint64)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    revokeCertificate: (params: CallParams<CertificateManagerArgs['obj']['revoke_certificate(uint64)void'] | CertificateManagerArgs['tuple']['revoke_certificate(uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(CertificateManagerParamsFactory.revokeCertificate(params))
    },

    /**
     * Makes a call to the CertificateManager smart contract using the `verify_certificate(uint64,account)bool` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    verifyCertificate: (params: CallParams<CertificateManagerArgs['obj']['verify_certificate(uint64,account)bool'] | CertificateManagerArgs['tuple']['verify_certificate(uint64,account)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(CertificateManagerParamsFactory.verifyCertificate(params))
    },

    /**
     * Makes a call to the CertificateManager smart contract using the `get_certificate_metadata(uint64)string` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    getCertificateMetadata: (params: CallParams<CertificateManagerArgs['obj']['get_certificate_metadata(uint64)string'] | CertificateManagerArgs['tuple']['get_certificate_metadata(uint64)string']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(CertificateManagerParamsFactory.getCertificateMetadata(params))
    },

    /**
     * Makes a call to the CertificateManager smart contract using the `is_certificate_active(uint64)bool` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    isCertificateActive: (params: CallParams<CertificateManagerArgs['obj']['is_certificate_active(uint64)bool'] | CertificateManagerArgs['tuple']['is_certificate_active(uint64)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(CertificateManagerParamsFactory.isCertificateActive(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Makes a clear_state call to an existing instance of the CertificateManager smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the CertificateManager smart contract using the `issue_certificate(account,string,uint64)uint64` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    issueCertificate: (params: CallParams<CertificateManagerArgs['obj']['issue_certificate(account,string,uint64)uint64'] | CertificateManagerArgs['tuple']['issue_certificate(account,string,uint64)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(CertificateManagerParamsFactory.issueCertificate(params))
    },

    /**
     * Makes a call to the CertificateManager smart contract using the `revoke_certificate(uint64)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    revokeCertificate: (params: CallParams<CertificateManagerArgs['obj']['revoke_certificate(uint64)void'] | CertificateManagerArgs['tuple']['revoke_certificate(uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(CertificateManagerParamsFactory.revokeCertificate(params))
    },

    /**
     * Makes a call to the CertificateManager smart contract using the `verify_certificate(uint64,account)bool` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    verifyCertificate: (params: CallParams<CertificateManagerArgs['obj']['verify_certificate(uint64,account)bool'] | CertificateManagerArgs['tuple']['verify_certificate(uint64,account)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(CertificateManagerParamsFactory.verifyCertificate(params))
    },

    /**
     * Makes a call to the CertificateManager smart contract using the `get_certificate_metadata(uint64)string` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    getCertificateMetadata: (params: CallParams<CertificateManagerArgs['obj']['get_certificate_metadata(uint64)string'] | CertificateManagerArgs['tuple']['get_certificate_metadata(uint64)string']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(CertificateManagerParamsFactory.getCertificateMetadata(params))
    },

    /**
     * Makes a call to the CertificateManager smart contract using the `is_certificate_active(uint64)bool` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    isCertificateActive: (params: CallParams<CertificateManagerArgs['obj']['is_certificate_active(uint64)bool'] | CertificateManagerArgs['tuple']['is_certificate_active(uint64)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(CertificateManagerParamsFactory.isCertificateActive(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Makes a clear_state call to an existing instance of the CertificateManager smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the CertificateManager smart contract using the `issue_certificate(account,string,uint64)uint64` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    issueCertificate: async (params: CallParams<CertificateManagerArgs['obj']['issue_certificate(account,string,uint64)uint64'] | CertificateManagerArgs['tuple']['issue_certificate(account,string,uint64)uint64']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(CertificateManagerParamsFactory.issueCertificate(params))
      return {...result, return: result.return as unknown as (undefined | CertificateManagerReturns['issue_certificate(account,string,uint64)uint64'])}
    },

    /**
     * Makes a call to the CertificateManager smart contract using the `revoke_certificate(uint64)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    revokeCertificate: async (params: CallParams<CertificateManagerArgs['obj']['revoke_certificate(uint64)void'] | CertificateManagerArgs['tuple']['revoke_certificate(uint64)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(CertificateManagerParamsFactory.revokeCertificate(params))
      return {...result, return: result.return as unknown as (undefined | CertificateManagerReturns['revoke_certificate(uint64)void'])}
    },

    /**
     * Makes a call to the CertificateManager smart contract using the `verify_certificate(uint64,account)bool` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    verifyCertificate: async (params: CallParams<CertificateManagerArgs['obj']['verify_certificate(uint64,account)bool'] | CertificateManagerArgs['tuple']['verify_certificate(uint64,account)bool']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(CertificateManagerParamsFactory.verifyCertificate(params))
      return {...result, return: result.return as unknown as (undefined | CertificateManagerReturns['verify_certificate(uint64,account)bool'])}
    },

    /**
     * Makes a call to the CertificateManager smart contract using the `get_certificate_metadata(uint64)string` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    getCertificateMetadata: async (params: CallParams<CertificateManagerArgs['obj']['get_certificate_metadata(uint64)string'] | CertificateManagerArgs['tuple']['get_certificate_metadata(uint64)string']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(CertificateManagerParamsFactory.getCertificateMetadata(params))
      return {...result, return: result.return as unknown as (undefined | CertificateManagerReturns['get_certificate_metadata(uint64)string'])}
    },

    /**
     * Makes a call to the CertificateManager smart contract using the `is_certificate_active(uint64)bool` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    isCertificateActive: async (params: CallParams<CertificateManagerArgs['obj']['is_certificate_active(uint64)bool'] | CertificateManagerArgs['tuple']['is_certificate_active(uint64)bool']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(CertificateManagerParamsFactory.isCertificateActive(params))
      return {...result, return: result.return as unknown as (undefined | CertificateManagerReturns['is_certificate_active(uint64)bool'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new CertificateManagerClient(this.appClient.clone(params))
  }

  /**
   * Makes a readonly (simulated) call to the CertificateManager smart contract using the `verify_certificate(uint64,account)bool` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async verifyCertificate(params: CallParams<CertificateManagerArgs['obj']['verify_certificate(uint64,account)bool'] | CertificateManagerArgs['tuple']['verify_certificate(uint64,account)bool']>) {
    const result = await this.appClient.send.call(CertificateManagerParamsFactory.verifyCertificate(params))
    return result.return as unknown as CertificateManagerReturns['verify_certificate(uint64,account)bool']
  }

  /**
   * Makes a readonly (simulated) call to the CertificateManager smart contract using the `get_certificate_metadata(uint64)string` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async getCertificateMetadata(params: CallParams<CertificateManagerArgs['obj']['get_certificate_metadata(uint64)string'] | CertificateManagerArgs['tuple']['get_certificate_metadata(uint64)string']>) {
    const result = await this.appClient.send.call(CertificateManagerParamsFactory.getCertificateMetadata(params))
    return result.return as unknown as CertificateManagerReturns['get_certificate_metadata(uint64)string']
  }

  /**
   * Makes a readonly (simulated) call to the CertificateManager smart contract using the `is_certificate_active(uint64)bool` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async isCertificateActive(params: CallParams<CertificateManagerArgs['obj']['is_certificate_active(uint64)bool'] | CertificateManagerArgs['tuple']['is_certificate_active(uint64)bool']>) {
    const result = await this.appClient.send.call(CertificateManagerParamsFactory.isCertificateActive(params))
    return result.return as unknown as CertificateManagerReturns['is_certificate_active(uint64)bool']
  }

  /**
   * Methods to access state for the current CertificateManager app
   */
  state = {
    /**
     * Methods to access global state for the current CertificateManager app
     */
    global: {
      /**
       * Get all current keyed values from global state
       */
      getAll: async (): Promise<Partial<Expand<GlobalKeysState>>> => {
        const result = await this.appClient.state.global.getAll()
        return {
          nextCertificateId: result.next_certificate_id,
        }
      },
      /**
       * Get the current value of the next_certificate_id key in global state
       */
      nextCertificateId: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("next_certificate_id")) as bigint | undefined },
    },
    /**
     * Methods to access box state for the current CertificateManager app
     */
    box: {
      /**
       * Get all current keyed values from box state
       */
      getAll: async (): Promise<Partial<Expand<BoxKeysState>>> => {
        const result = await this.appClient.state.box.getAll()
        return {
        }
      },
      /**
       * Get values from the certificate_owner map in box state
       */
      certificateOwner: {
        /**
         * Get all current values of the certificate_owner map in box state
         */
        getMap: async (): Promise<Map<bigint, string>> => { return (await this.appClient.state.box.getMap("certificate_owner")) as Map<bigint, string> },
        /**
         * Get a current value of the certificate_owner map by key from box state
         */
        value: async (key: bigint | number): Promise<string | undefined> => { return await this.appClient.state.box.getMapValue("certificate_owner", key) as string | undefined },
      },
      /**
       * Get values from the certificate_metadata map in box state
       */
      certificateMetadata: {
        /**
         * Get all current values of the certificate_metadata map in box state
         */
        getMap: async (): Promise<Map<bigint, string>> => { return (await this.appClient.state.box.getMap("certificate_metadata")) as Map<bigint, string> },
        /**
         * Get a current value of the certificate_metadata map by key from box state
         */
        value: async (key: bigint | number): Promise<string | undefined> => { return await this.appClient.state.box.getMapValue("certificate_metadata", key) as string | undefined },
      },
      /**
       * Get values from the certificate_expiry map in box state
       */
      certificateExpiry: {
        /**
         * Get all current values of the certificate_expiry map in box state
         */
        getMap: async (): Promise<Map<bigint, bigint>> => { return (await this.appClient.state.box.getMap("certificate_expiry")) as Map<bigint, bigint> },
        /**
         * Get a current value of the certificate_expiry map by key from box state
         */
        value: async (key: bigint | number): Promise<bigint | undefined> => { return await this.appClient.state.box.getMapValue("certificate_expiry", key) as bigint | undefined },
      },
      /**
       * Get values from the certificate_active map in box state
       */
      certificateActive: {
        /**
         * Get all current values of the certificate_active map in box state
         */
        getMap: async (): Promise<Map<bigint, bigint>> => { return (await this.appClient.state.box.getMap("certificate_active")) as Map<bigint, bigint> },
        /**
         * Get a current value of the certificate_active map by key from box state
         */
        value: async (key: bigint | number): Promise<bigint | undefined> => { return await this.appClient.state.box.getMapValue("certificate_active", key) as bigint | undefined },
      },
    },
  }

  public newGroup(): CertificateManagerComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a issue_certificate(account,string,uint64)uint64 method call against the CertificateManager contract
       */
      issueCertificate(params: CallParams<CertificateManagerArgs['obj']['issue_certificate(account,string,uint64)uint64'] | CertificateManagerArgs['tuple']['issue_certificate(account,string,uint64)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.issueCertificate(params)))
        resultMappers.push((v) => client.decodeReturnValue('issue_certificate(account,string,uint64)uint64', v))
        return this
      },
      /**
       * Add a revoke_certificate(uint64)void method call against the CertificateManager contract
       */
      revokeCertificate(params: CallParams<CertificateManagerArgs['obj']['revoke_certificate(uint64)void'] | CertificateManagerArgs['tuple']['revoke_certificate(uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.revokeCertificate(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a verify_certificate(uint64,account)bool method call against the CertificateManager contract
       */
      verifyCertificate(params: CallParams<CertificateManagerArgs['obj']['verify_certificate(uint64,account)bool'] | CertificateManagerArgs['tuple']['verify_certificate(uint64,account)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.verifyCertificate(params)))
        resultMappers.push((v) => client.decodeReturnValue('verify_certificate(uint64,account)bool', v))
        return this
      },
      /**
       * Add a get_certificate_metadata(uint64)string method call against the CertificateManager contract
       */
      getCertificateMetadata(params: CallParams<CertificateManagerArgs['obj']['get_certificate_metadata(uint64)string'] | CertificateManagerArgs['tuple']['get_certificate_metadata(uint64)string']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.getCertificateMetadata(params)))
        resultMappers.push((v) => client.decodeReturnValue('get_certificate_metadata(uint64)string', v))
        return this
      },
      /**
       * Add a is_certificate_active(uint64)bool method call against the CertificateManager contract
       */
      isCertificateActive(params: CallParams<CertificateManagerArgs['obj']['is_certificate_active(uint64)bool'] | CertificateManagerArgs['tuple']['is_certificate_active(uint64)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.isCertificateActive(params)))
        resultMappers.push((v) => client.decodeReturnValue('is_certificate_active(uint64)bool', v))
        return this
      },
      /**
       * Add a clear state call to the CertificateManager contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as CertificateManagerComposer
  }
}
export type CertificateManagerComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the issue_certificate(account,string,uint64)uint64 ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  issueCertificate(params?: CallParams<CertificateManagerArgs['obj']['issue_certificate(account,string,uint64)uint64'] | CertificateManagerArgs['tuple']['issue_certificate(account,string,uint64)uint64']>): CertificateManagerComposer<[...TReturns, CertificateManagerReturns['issue_certificate(account,string,uint64)uint64'] | undefined]>

  /**
   * Calls the revoke_certificate(uint64)void ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  revokeCertificate(params?: CallParams<CertificateManagerArgs['obj']['revoke_certificate(uint64)void'] | CertificateManagerArgs['tuple']['revoke_certificate(uint64)void']>): CertificateManagerComposer<[...TReturns, CertificateManagerReturns['revoke_certificate(uint64)void'] | undefined]>

  /**
   * Calls the verify_certificate(uint64,account)bool ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  verifyCertificate(params?: CallParams<CertificateManagerArgs['obj']['verify_certificate(uint64,account)bool'] | CertificateManagerArgs['tuple']['verify_certificate(uint64,account)bool']>): CertificateManagerComposer<[...TReturns, CertificateManagerReturns['verify_certificate(uint64,account)bool'] | undefined]>

  /**
   * Calls the get_certificate_metadata(uint64)string ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  getCertificateMetadata(params?: CallParams<CertificateManagerArgs['obj']['get_certificate_metadata(uint64)string'] | CertificateManagerArgs['tuple']['get_certificate_metadata(uint64)string']>): CertificateManagerComposer<[...TReturns, CertificateManagerReturns['get_certificate_metadata(uint64)string'] | undefined]>

  /**
   * Calls the is_certificate_active(uint64)bool ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  isCertificateActive(params?: CallParams<CertificateManagerArgs['obj']['is_certificate_active(uint64)bool'] | CertificateManagerArgs['tuple']['is_certificate_active(uint64)bool']>): CertificateManagerComposer<[...TReturns, CertificateManagerReturns['is_certificate_active(uint64)bool'] | undefined]>

  /**
   * Makes a clear_state call to an existing instance of the CertificateManager smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): CertificateManagerComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): CertificateManagerComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<CertificateManagerComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<CertificateManagerComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<CertificateManagerComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<CertificateManagerComposerResults<TReturns>>
}
export type CertificateManagerComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

