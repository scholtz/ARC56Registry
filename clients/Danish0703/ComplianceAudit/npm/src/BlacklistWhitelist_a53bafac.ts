/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"BlacklistWhitelist","structs":{},"methods":[{"name":"set_blacklist_status","args":[{"type":"account","name":"wallet"},{"type":"bool","name":"blocked"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}},{"name":"set_whitelist_status","args":[{"type":"account","name":"wallet"},{"type":"bool","name":"approved"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}},{"name":"is_blacklisted","args":[{"type":"account","name":"wallet"}],"returns":{"type":"bool"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"is_whitelisted","args":[{"type":"account","name":"wallet"}],"returns":{"type":"bool"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"get_wallet_status","args":[{"type":"account","name":"wallet"}],"returns":{"type":"string"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}}],"arcs":[22,28],"networks":{},"state":{"schema":{"global":{"ints":0,"bytes":0},"local":{"ints":0,"bytes":0}},"keys":{"global":{},"local":{},"box":{}},"maps":{"global":{},"local":{},"box":{"blacklist":{"keyType":"address","valueType":"uint64","prefix":"YndfYmxhY2tf"},"whitelist":{"keyType":"address","valueType":"uint64","prefix":"Yndfd2hpdGVf"}}}},"bareActions":{"create":["NoOp"],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[86,116,143,170,193],"errorMessage":"OnCompletion is not NoOp"},{"pc":[232],"errorMessage":"Only creator can update blacklist"},{"pc":[250],"errorMessage":"Only creator can update whitelist"},{"pc":[221],"errorMessage":"can only call when creating"},{"pc":[89,119,146,173,196],"errorMessage":"can only call when not creating"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"events":[]} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


/**
 * The argument types for the BlacklistWhitelist contract
 */
export type BlacklistWhitelistArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'set_blacklist_status(account,bool)void': {
      wallet: Uint8Array | string
      blocked: boolean
    }
    'set_whitelist_status(account,bool)void': {
      wallet: Uint8Array | string
      approved: boolean
    }
    'is_blacklisted(account)bool': {
      wallet: Uint8Array | string
    }
    'is_whitelisted(account)bool': {
      wallet: Uint8Array | string
    }
    'get_wallet_status(account)string': {
      wallet: Uint8Array | string
    }
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'set_blacklist_status(account,bool)void': [wallet: Uint8Array | string, blocked: boolean]
    'set_whitelist_status(account,bool)void': [wallet: Uint8Array | string, approved: boolean]
    'is_blacklisted(account)bool': [wallet: Uint8Array | string]
    'is_whitelisted(account)bool': [wallet: Uint8Array | string]
    'get_wallet_status(account)string': [wallet: Uint8Array | string]
  }
}

/**
 * The return type for each method
 */
export type BlacklistWhitelistReturns = {
  'set_blacklist_status(account,bool)void': void
  'set_whitelist_status(account,bool)void': void
  'is_blacklisted(account)bool': boolean
  'is_whitelisted(account)bool': boolean
  'get_wallet_status(account)string': string
}

/**
 * Defines the types of available calls and state of the BlacklistWhitelist smart contract.
 */
export type BlacklistWhitelistTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'set_blacklist_status(account,bool)void' | 'set_blacklist_status', {
      argsObj: BlacklistWhitelistArgs['obj']['set_blacklist_status(account,bool)void']
      argsTuple: BlacklistWhitelistArgs['tuple']['set_blacklist_status(account,bool)void']
      returns: BlacklistWhitelistReturns['set_blacklist_status(account,bool)void']
    }>
    & Record<'set_whitelist_status(account,bool)void' | 'set_whitelist_status', {
      argsObj: BlacklistWhitelistArgs['obj']['set_whitelist_status(account,bool)void']
      argsTuple: BlacklistWhitelistArgs['tuple']['set_whitelist_status(account,bool)void']
      returns: BlacklistWhitelistReturns['set_whitelist_status(account,bool)void']
    }>
    & Record<'is_blacklisted(account)bool' | 'is_blacklisted', {
      argsObj: BlacklistWhitelistArgs['obj']['is_blacklisted(account)bool']
      argsTuple: BlacklistWhitelistArgs['tuple']['is_blacklisted(account)bool']
      returns: BlacklistWhitelistReturns['is_blacklisted(account)bool']
    }>
    & Record<'is_whitelisted(account)bool' | 'is_whitelisted', {
      argsObj: BlacklistWhitelistArgs['obj']['is_whitelisted(account)bool']
      argsTuple: BlacklistWhitelistArgs['tuple']['is_whitelisted(account)bool']
      returns: BlacklistWhitelistReturns['is_whitelisted(account)bool']
    }>
    & Record<'get_wallet_status(account)string' | 'get_wallet_status', {
      argsObj: BlacklistWhitelistArgs['obj']['get_wallet_status(account)string']
      argsTuple: BlacklistWhitelistArgs['tuple']['get_wallet_status(account)string']
      returns: BlacklistWhitelistReturns['get_wallet_status(account)string']
    }>
  /**
   * Defines the shape of the state of the application.
   */
  state: {
    box: {
      keys: {}
      maps: {
        blacklist: Map<string, bigint>
        whitelist: Map<string, bigint>
      }
    }
  }
}

/**
 * Defines the possible abi call signatures.
 */
export type BlacklistWhitelistSignatures = keyof BlacklistWhitelistTypes['methods']
/**
 * Defines the possible abi call signatures for methods that return a non-void value.
 */
export type BlacklistWhitelistNonVoidMethodSignatures = keyof BlacklistWhitelistTypes['methods'] extends infer T ? T extends keyof BlacklistWhitelistTypes['methods'] ? MethodReturn<T> extends void ? never : T  : never : never
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the BlacklistWhitelist smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends BlacklistWhitelistSignatures> = BlacklistWhitelistTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the BlacklistWhitelist smart contract to the method's return type
 */
export type MethodReturn<TSignature extends BlacklistWhitelistSignatures> = BlacklistWhitelistTypes['methods'][TSignature]['returns']

/**
 * Defines the shape of the keyed box state of the application.
 */
export type BoxKeysState = BlacklistWhitelistTypes['state']['box']['keys']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the BlacklistWhitelist smart contract
 */
export abstract class BlacklistWhitelistParamsFactory {
  /**
   * Constructs a no op call for the set_blacklist_status(account,bool)void ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static setBlacklistStatus(params: CallParams<BlacklistWhitelistArgs['obj']['set_blacklist_status(account,bool)void'] | BlacklistWhitelistArgs['tuple']['set_blacklist_status(account,bool)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'set_blacklist_status(account,bool)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.wallet, params.args.blocked],
    }
  }
  /**
   * Constructs a no op call for the set_whitelist_status(account,bool)void ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static setWhitelistStatus(params: CallParams<BlacklistWhitelistArgs['obj']['set_whitelist_status(account,bool)void'] | BlacklistWhitelistArgs['tuple']['set_whitelist_status(account,bool)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'set_whitelist_status(account,bool)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.wallet, params.args.approved],
    }
  }
  /**
   * Constructs a no op call for the is_blacklisted(account)bool ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static isBlacklisted(params: CallParams<BlacklistWhitelistArgs['obj']['is_blacklisted(account)bool'] | BlacklistWhitelistArgs['tuple']['is_blacklisted(account)bool']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'is_blacklisted(account)bool' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.wallet],
    }
  }
  /**
   * Constructs a no op call for the is_whitelisted(account)bool ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static isWhitelisted(params: CallParams<BlacklistWhitelistArgs['obj']['is_whitelisted(account)bool'] | BlacklistWhitelistArgs['tuple']['is_whitelisted(account)bool']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'is_whitelisted(account)bool' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.wallet],
    }
  }
  /**
   * Constructs a no op call for the get_wallet_status(account)string ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static getWalletStatus(params: CallParams<BlacklistWhitelistArgs['obj']['get_wallet_status(account)string'] | BlacklistWhitelistArgs['tuple']['get_wallet_status(account)string']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'get_wallet_status(account)string' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.wallet],
    }
  }
}

/**
 * A client to make calls to the BlacklistWhitelist smart contract
 */
export class BlacklistWhitelistClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `BlacklistWhitelistClient`
   *
   * @param appClient An `AppClient` instance which has been created with the BlacklistWhitelist app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `BlacklistWhitelistClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Checks for decode errors on the given return value and maps the return value to the return type for the given method
   * @returns The typed return value or undefined if there was no value
   */
  decodeReturnValue<TSignature extends BlacklistWhitelistNonVoidMethodSignatures>(method: TSignature, returnValue: ABIReturn | undefined) {
    return returnValue !== undefined ? getArc56ReturnValue<MethodReturn<TSignature>>(returnValue, this.appClient.getABIMethod(method), APP_SPEC.structs) : undefined
  }

  /**
   * Returns a new `BlacklistWhitelistClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<BlacklistWhitelistClient> {
    return new BlacklistWhitelistClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `BlacklistWhitelistClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<BlacklistWhitelistClient> {
    return new BlacklistWhitelistClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Makes a clear_state call to an existing instance of the BlacklistWhitelist smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the BlacklistWhitelist smart contract using the `set_blacklist_status(account,bool)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    setBlacklistStatus: (params: CallParams<BlacklistWhitelistArgs['obj']['set_blacklist_status(account,bool)void'] | BlacklistWhitelistArgs['tuple']['set_blacklist_status(account,bool)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(BlacklistWhitelistParamsFactory.setBlacklistStatus(params))
    },

    /**
     * Makes a call to the BlacklistWhitelist smart contract using the `set_whitelist_status(account,bool)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    setWhitelistStatus: (params: CallParams<BlacklistWhitelistArgs['obj']['set_whitelist_status(account,bool)void'] | BlacklistWhitelistArgs['tuple']['set_whitelist_status(account,bool)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(BlacklistWhitelistParamsFactory.setWhitelistStatus(params))
    },

    /**
     * Makes a call to the BlacklistWhitelist smart contract using the `is_blacklisted(account)bool` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    isBlacklisted: (params: CallParams<BlacklistWhitelistArgs['obj']['is_blacklisted(account)bool'] | BlacklistWhitelistArgs['tuple']['is_blacklisted(account)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(BlacklistWhitelistParamsFactory.isBlacklisted(params))
    },

    /**
     * Makes a call to the BlacklistWhitelist smart contract using the `is_whitelisted(account)bool` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    isWhitelisted: (params: CallParams<BlacklistWhitelistArgs['obj']['is_whitelisted(account)bool'] | BlacklistWhitelistArgs['tuple']['is_whitelisted(account)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(BlacklistWhitelistParamsFactory.isWhitelisted(params))
    },

    /**
     * Makes a call to the BlacklistWhitelist smart contract using the `get_wallet_status(account)string` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    getWalletStatus: (params: CallParams<BlacklistWhitelistArgs['obj']['get_wallet_status(account)string'] | BlacklistWhitelistArgs['tuple']['get_wallet_status(account)string']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(BlacklistWhitelistParamsFactory.getWalletStatus(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Makes a clear_state call to an existing instance of the BlacklistWhitelist smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the BlacklistWhitelist smart contract using the `set_blacklist_status(account,bool)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    setBlacklistStatus: (params: CallParams<BlacklistWhitelistArgs['obj']['set_blacklist_status(account,bool)void'] | BlacklistWhitelistArgs['tuple']['set_blacklist_status(account,bool)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(BlacklistWhitelistParamsFactory.setBlacklistStatus(params))
    },

    /**
     * Makes a call to the BlacklistWhitelist smart contract using the `set_whitelist_status(account,bool)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    setWhitelistStatus: (params: CallParams<BlacklistWhitelistArgs['obj']['set_whitelist_status(account,bool)void'] | BlacklistWhitelistArgs['tuple']['set_whitelist_status(account,bool)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(BlacklistWhitelistParamsFactory.setWhitelistStatus(params))
    },

    /**
     * Makes a call to the BlacklistWhitelist smart contract using the `is_blacklisted(account)bool` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    isBlacklisted: (params: CallParams<BlacklistWhitelistArgs['obj']['is_blacklisted(account)bool'] | BlacklistWhitelistArgs['tuple']['is_blacklisted(account)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(BlacklistWhitelistParamsFactory.isBlacklisted(params))
    },

    /**
     * Makes a call to the BlacklistWhitelist smart contract using the `is_whitelisted(account)bool` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    isWhitelisted: (params: CallParams<BlacklistWhitelistArgs['obj']['is_whitelisted(account)bool'] | BlacklistWhitelistArgs['tuple']['is_whitelisted(account)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(BlacklistWhitelistParamsFactory.isWhitelisted(params))
    },

    /**
     * Makes a call to the BlacklistWhitelist smart contract using the `get_wallet_status(account)string` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    getWalletStatus: (params: CallParams<BlacklistWhitelistArgs['obj']['get_wallet_status(account)string'] | BlacklistWhitelistArgs['tuple']['get_wallet_status(account)string']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(BlacklistWhitelistParamsFactory.getWalletStatus(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Makes a clear_state call to an existing instance of the BlacklistWhitelist smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the BlacklistWhitelist smart contract using the `set_blacklist_status(account,bool)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    setBlacklistStatus: async (params: CallParams<BlacklistWhitelistArgs['obj']['set_blacklist_status(account,bool)void'] | BlacklistWhitelistArgs['tuple']['set_blacklist_status(account,bool)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(BlacklistWhitelistParamsFactory.setBlacklistStatus(params))
      return {...result, return: result.return as unknown as (undefined | BlacklistWhitelistReturns['set_blacklist_status(account,bool)void'])}
    },

    /**
     * Makes a call to the BlacklistWhitelist smart contract using the `set_whitelist_status(account,bool)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    setWhitelistStatus: async (params: CallParams<BlacklistWhitelistArgs['obj']['set_whitelist_status(account,bool)void'] | BlacklistWhitelistArgs['tuple']['set_whitelist_status(account,bool)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(BlacklistWhitelistParamsFactory.setWhitelistStatus(params))
      return {...result, return: result.return as unknown as (undefined | BlacklistWhitelistReturns['set_whitelist_status(account,bool)void'])}
    },

    /**
     * Makes a call to the BlacklistWhitelist smart contract using the `is_blacklisted(account)bool` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    isBlacklisted: async (params: CallParams<BlacklistWhitelistArgs['obj']['is_blacklisted(account)bool'] | BlacklistWhitelistArgs['tuple']['is_blacklisted(account)bool']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(BlacklistWhitelistParamsFactory.isBlacklisted(params))
      return {...result, return: result.return as unknown as (undefined | BlacklistWhitelistReturns['is_blacklisted(account)bool'])}
    },

    /**
     * Makes a call to the BlacklistWhitelist smart contract using the `is_whitelisted(account)bool` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    isWhitelisted: async (params: CallParams<BlacklistWhitelistArgs['obj']['is_whitelisted(account)bool'] | BlacklistWhitelistArgs['tuple']['is_whitelisted(account)bool']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(BlacklistWhitelistParamsFactory.isWhitelisted(params))
      return {...result, return: result.return as unknown as (undefined | BlacklistWhitelistReturns['is_whitelisted(account)bool'])}
    },

    /**
     * Makes a call to the BlacklistWhitelist smart contract using the `get_wallet_status(account)string` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    getWalletStatus: async (params: CallParams<BlacklistWhitelistArgs['obj']['get_wallet_status(account)string'] | BlacklistWhitelistArgs['tuple']['get_wallet_status(account)string']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(BlacklistWhitelistParamsFactory.getWalletStatus(params))
      return {...result, return: result.return as unknown as (undefined | BlacklistWhitelistReturns['get_wallet_status(account)string'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new BlacklistWhitelistClient(this.appClient.clone(params))
  }

  /**
   * Makes a readonly (simulated) call to the BlacklistWhitelist smart contract using the `is_blacklisted(account)bool` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async isBlacklisted(params: CallParams<BlacklistWhitelistArgs['obj']['is_blacklisted(account)bool'] | BlacklistWhitelistArgs['tuple']['is_blacklisted(account)bool']>) {
    const result = await this.appClient.send.call(BlacklistWhitelistParamsFactory.isBlacklisted(params))
    return result.return as unknown as BlacklistWhitelistReturns['is_blacklisted(account)bool']
  }

  /**
   * Makes a readonly (simulated) call to the BlacklistWhitelist smart contract using the `is_whitelisted(account)bool` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async isWhitelisted(params: CallParams<BlacklistWhitelistArgs['obj']['is_whitelisted(account)bool'] | BlacklistWhitelistArgs['tuple']['is_whitelisted(account)bool']>) {
    const result = await this.appClient.send.call(BlacklistWhitelistParamsFactory.isWhitelisted(params))
    return result.return as unknown as BlacklistWhitelistReturns['is_whitelisted(account)bool']
  }

  /**
   * Makes a readonly (simulated) call to the BlacklistWhitelist smart contract using the `get_wallet_status(account)string` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async getWalletStatus(params: CallParams<BlacklistWhitelistArgs['obj']['get_wallet_status(account)string'] | BlacklistWhitelistArgs['tuple']['get_wallet_status(account)string']>) {
    const result = await this.appClient.send.call(BlacklistWhitelistParamsFactory.getWalletStatus(params))
    return result.return as unknown as BlacklistWhitelistReturns['get_wallet_status(account)string']
  }

  /**
   * Methods to access state for the current BlacklistWhitelist app
   */
  state = {
    /**
     * Methods to access box state for the current BlacklistWhitelist app
     */
    box: {
      /**
       * Get all current keyed values from box state
       */
      getAll: async (): Promise<Partial<Expand<BoxKeysState>>> => {
        const result = await this.appClient.state.box.getAll()
        return {
        }
      },
      /**
       * Get values from the blacklist map in box state
       */
      blacklist: {
        /**
         * Get all current values of the blacklist map in box state
         */
        getMap: async (): Promise<Map<string, bigint>> => { return (await this.appClient.state.box.getMap("blacklist")) as Map<string, bigint> },
        /**
         * Get a current value of the blacklist map by key from box state
         */
        value: async (key: string): Promise<bigint | undefined> => { return await this.appClient.state.box.getMapValue("blacklist", key) as bigint | undefined },
      },
      /**
       * Get values from the whitelist map in box state
       */
      whitelist: {
        /**
         * Get all current values of the whitelist map in box state
         */
        getMap: async (): Promise<Map<string, bigint>> => { return (await this.appClient.state.box.getMap("whitelist")) as Map<string, bigint> },
        /**
         * Get a current value of the whitelist map by key from box state
         */
        value: async (key: string): Promise<bigint | undefined> => { return await this.appClient.state.box.getMapValue("whitelist", key) as bigint | undefined },
      },
    },
  }

  public newGroup(): BlacklistWhitelistComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a set_blacklist_status(account,bool)void method call against the BlacklistWhitelist contract
       */
      setBlacklistStatus(params: CallParams<BlacklistWhitelistArgs['obj']['set_blacklist_status(account,bool)void'] | BlacklistWhitelistArgs['tuple']['set_blacklist_status(account,bool)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.setBlacklistStatus(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a set_whitelist_status(account,bool)void method call against the BlacklistWhitelist contract
       */
      setWhitelistStatus(params: CallParams<BlacklistWhitelistArgs['obj']['set_whitelist_status(account,bool)void'] | BlacklistWhitelistArgs['tuple']['set_whitelist_status(account,bool)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.setWhitelistStatus(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a is_blacklisted(account)bool method call against the BlacklistWhitelist contract
       */
      isBlacklisted(params: CallParams<BlacklistWhitelistArgs['obj']['is_blacklisted(account)bool'] | BlacklistWhitelistArgs['tuple']['is_blacklisted(account)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.isBlacklisted(params)))
        resultMappers.push((v) => client.decodeReturnValue('is_blacklisted(account)bool', v))
        return this
      },
      /**
       * Add a is_whitelisted(account)bool method call against the BlacklistWhitelist contract
       */
      isWhitelisted(params: CallParams<BlacklistWhitelistArgs['obj']['is_whitelisted(account)bool'] | BlacklistWhitelistArgs['tuple']['is_whitelisted(account)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.isWhitelisted(params)))
        resultMappers.push((v) => client.decodeReturnValue('is_whitelisted(account)bool', v))
        return this
      },
      /**
       * Add a get_wallet_status(account)string method call against the BlacklistWhitelist contract
       */
      getWalletStatus(params: CallParams<BlacklistWhitelistArgs['obj']['get_wallet_status(account)string'] | BlacklistWhitelistArgs['tuple']['get_wallet_status(account)string']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.getWalletStatus(params)))
        resultMappers.push((v) => client.decodeReturnValue('get_wallet_status(account)string', v))
        return this
      },
      /**
       * Add a clear state call to the BlacklistWhitelist contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as BlacklistWhitelistComposer
  }
}
export type BlacklistWhitelistComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the set_blacklist_status(account,bool)void ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  setBlacklistStatus(params?: CallParams<BlacklistWhitelistArgs['obj']['set_blacklist_status(account,bool)void'] | BlacklistWhitelistArgs['tuple']['set_blacklist_status(account,bool)void']>): BlacklistWhitelistComposer<[...TReturns, BlacklistWhitelistReturns['set_blacklist_status(account,bool)void'] | undefined]>

  /**
   * Calls the set_whitelist_status(account,bool)void ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  setWhitelistStatus(params?: CallParams<BlacklistWhitelistArgs['obj']['set_whitelist_status(account,bool)void'] | BlacklistWhitelistArgs['tuple']['set_whitelist_status(account,bool)void']>): BlacklistWhitelistComposer<[...TReturns, BlacklistWhitelistReturns['set_whitelist_status(account,bool)void'] | undefined]>

  /**
   * Calls the is_blacklisted(account)bool ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  isBlacklisted(params?: CallParams<BlacklistWhitelistArgs['obj']['is_blacklisted(account)bool'] | BlacklistWhitelistArgs['tuple']['is_blacklisted(account)bool']>): BlacklistWhitelistComposer<[...TReturns, BlacklistWhitelistReturns['is_blacklisted(account)bool'] | undefined]>

  /**
   * Calls the is_whitelisted(account)bool ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  isWhitelisted(params?: CallParams<BlacklistWhitelistArgs['obj']['is_whitelisted(account)bool'] | BlacklistWhitelistArgs['tuple']['is_whitelisted(account)bool']>): BlacklistWhitelistComposer<[...TReturns, BlacklistWhitelistReturns['is_whitelisted(account)bool'] | undefined]>

  /**
   * Calls the get_wallet_status(account)string ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  getWalletStatus(params?: CallParams<BlacklistWhitelistArgs['obj']['get_wallet_status(account)string'] | BlacklistWhitelistArgs['tuple']['get_wallet_status(account)string']>): BlacklistWhitelistComposer<[...TReturns, BlacklistWhitelistReturns['get_wallet_status(account)string'] | undefined]>

  /**
   * Makes a clear_state call to an existing instance of the BlacklistWhitelist smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): BlacklistWhitelistComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): BlacklistWhitelistComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<BlacklistWhitelistComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<BlacklistWhitelistComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<BlacklistWhitelistComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<BlacklistWhitelistComposerResults<TReturns>>
}
export type BlacklistWhitelistComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

