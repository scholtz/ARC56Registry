/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"GatePlugin","structs":{},"methods":[{"name":"create","args":[{"type":"uint64","name":"gateAppID"}],"returns":{"type":"void"},"actions":{"create":["NoOp"],"call":[]},"readonly":false,"events":[],"recommendations":{}},{"name":"register","args":[{"type":"uint64","name":"wallet"},{"type":"bool","name":"rekeyBack"},{"type":"(uint64,uint64,uint8)[]","name":"filters"},{"type":"byte[][]","name":"args"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}}],"arcs":[22,28],"networks":{},"state":{"schema":{"global":{"ints":1,"bytes":0},"local":{"ints":0,"bytes":0}},"keys":{"global":{"gateAppID":{"keyType":"AVMString","valueType":"AVMUint64","key":"Z2F0ZV9hcHBfaWQ="}},"local":{},"box":{}},"maps":{"global":{},"local":{},"box":{}}},"bareActions":{"create":[],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[261,339],"errorMessage":"Bytes has valid prefix"},{"pc":[272,350],"errorMessage":"application exists"},{"pc":[221],"errorMessage":"check GlobalState exists"},{"pc":[151],"errorMessage":"invalid array encoding"},{"pc":[105,121,165],"errorMessage":"invalid array length header"},{"pc":[96],"errorMessage":"invalid number of bytes for arc4.bool"},{"pc":[185],"errorMessage":"invalid number of bytes for arc4.dynamic_array<bytes>"},{"pc":[114],"errorMessage":"invalid number of bytes for arc4.dynamic_array<smart_contracts/gates/types.ts::GateFilter>"},{"pc":[70,87],"errorMessage":"invalid number of bytes for arc4.uint64"},{"pc":[266,343],"errorMessage":"invalid number of bytes for uint64"},{"pc":[159],"errorMessage":"invalid tail pointer for (len+(len+uint8[])[])"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"events":[]} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


/**
 * The argument types for the GatePlugin contract
 */
export type GatePluginArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'create(uint64)void': {
      gateAppId: bigint | number
    }
    'register(uint64,bool,(uint64,uint64,uint8)[],byte[][])void': {
      wallet: bigint | number
      rekeyBack: boolean
      filters: [bigint | number, bigint | number, bigint | number][]
      args: Uint8Array[]
    }
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'create(uint64)void': [gateAppId: bigint | number]
    'register(uint64,bool,(uint64,uint64,uint8)[],byte[][])void': [wallet: bigint | number, rekeyBack: boolean, filters: [bigint | number, bigint | number, bigint | number][], args: Uint8Array[]]
  }
}

/**
 * The return type for each method
 */
export type GatePluginReturns = {
  'create(uint64)void': void
  'register(uint64,bool,(uint64,uint64,uint8)[],byte[][])void': void
}

/**
 * Defines the types of available calls and state of the GatePlugin smart contract.
 */
export type GatePluginTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'create(uint64)void' | 'create', {
      argsObj: GatePluginArgs['obj']['create(uint64)void']
      argsTuple: GatePluginArgs['tuple']['create(uint64)void']
      returns: GatePluginReturns['create(uint64)void']
    }>
    & Record<'register(uint64,bool,(uint64,uint64,uint8)[],byte[][])void' | 'register', {
      argsObj: GatePluginArgs['obj']['register(uint64,bool,(uint64,uint64,uint8)[],byte[][])void']
      argsTuple: GatePluginArgs['tuple']['register(uint64,bool,(uint64,uint64,uint8)[],byte[][])void']
      returns: GatePluginReturns['register(uint64,bool,(uint64,uint64,uint8)[],byte[][])void']
    }>
  /**
   * Defines the shape of the state of the application.
   */
  state: {
    global: {
      keys: {
        gateAppId: bigint
      }
      maps: {}
    }
  }
}

/**
 * Defines the possible abi call signatures.
 */
export type GatePluginSignatures = keyof GatePluginTypes['methods']
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the GatePlugin smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends GatePluginSignatures> = GatePluginTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the GatePlugin smart contract to the method's return type
 */
export type MethodReturn<TSignature extends GatePluginSignatures> = GatePluginTypes['methods'][TSignature]['returns']

/**
 * Defines the shape of the keyed global state of the application.
 */
export type GlobalKeysState = GatePluginTypes['state']['global']['keys']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the GatePlugin smart contract
 */
export abstract class GatePluginParamsFactory {
  /**
   * Constructs a no op call for the register(uint64,bool,(uint64,uint64,uint8)[],byte[][])void ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static register(params: CallParams<GatePluginArgs['obj']['register(uint64,bool,(uint64,uint64,uint8)[],byte[][])void'] | GatePluginArgs['tuple']['register(uint64,bool,(uint64,uint64,uint8)[],byte[][])void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'register(uint64,bool,(uint64,uint64,uint8)[],byte[][])void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.wallet, params.args.rekeyBack, params.args.filters, params.args.args],
    }
  }
}

/**
 * A client to make calls to the GatePlugin smart contract
 */
export class GatePluginClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `GatePluginClient`
   *
   * @param appClient An `AppClient` instance which has been created with the GatePlugin app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `GatePluginClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Returns a new `GatePluginClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<GatePluginClient> {
    return new GatePluginClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `GatePluginClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<GatePluginClient> {
    return new GatePluginClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Makes a clear_state call to an existing instance of the GatePlugin smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the GatePlugin smart contract using the `register(uint64,bool,(uint64,uint64,uint8)[],byte[][])void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    register: (params: CallParams<GatePluginArgs['obj']['register(uint64,bool,(uint64,uint64,uint8)[],byte[][])void'] | GatePluginArgs['tuple']['register(uint64,bool,(uint64,uint64,uint8)[],byte[][])void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(GatePluginParamsFactory.register(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Makes a clear_state call to an existing instance of the GatePlugin smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the GatePlugin smart contract using the `register(uint64,bool,(uint64,uint64,uint8)[],byte[][])void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    register: (params: CallParams<GatePluginArgs['obj']['register(uint64,bool,(uint64,uint64,uint8)[],byte[][])void'] | GatePluginArgs['tuple']['register(uint64,bool,(uint64,uint64,uint8)[],byte[][])void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(GatePluginParamsFactory.register(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Makes a clear_state call to an existing instance of the GatePlugin smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the GatePlugin smart contract using the `register(uint64,bool,(uint64,uint64,uint8)[],byte[][])void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    register: async (params: CallParams<GatePluginArgs['obj']['register(uint64,bool,(uint64,uint64,uint8)[],byte[][])void'] | GatePluginArgs['tuple']['register(uint64,bool,(uint64,uint64,uint8)[],byte[][])void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(GatePluginParamsFactory.register(params))
      return {...result, return: result.return as unknown as (undefined | GatePluginReturns['register(uint64,bool,(uint64,uint64,uint8)[],byte[][])void'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new GatePluginClient(this.appClient.clone(params))
  }

  /**
   * Methods to access state for the current GatePlugin app
   */
  state = {
    /**
     * Methods to access global state for the current GatePlugin app
     */
    global: {
      /**
       * Get all current keyed values from global state
       */
      getAll: async (): Promise<Partial<Expand<GlobalKeysState>>> => {
        const result = await this.appClient.state.global.getAll()
        return {
          gateAppId: result.gateAppID,
        }
      },
      /**
       * Get the current value of the gateAppID key in global state
       */
      gateAppId: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("gateAppID")) as bigint | undefined },
    },
  }

  public newGroup(): GatePluginComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a register(uint64,bool,(uint64,uint64,uint8)[],byte[][])void method call against the GatePlugin contract
       */
      register(params: CallParams<GatePluginArgs['obj']['register(uint64,bool,(uint64,uint64,uint8)[],byte[][])void'] | GatePluginArgs['tuple']['register(uint64,bool,(uint64,uint64,uint8)[],byte[][])void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.register(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a clear state call to the GatePlugin contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as GatePluginComposer
  }
}
export type GatePluginComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the register(uint64,bool,(uint64,uint64,uint8)[],byte[][])void ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  register(params?: CallParams<GatePluginArgs['obj']['register(uint64,bool,(uint64,uint64,uint8)[],byte[][])void'] | GatePluginArgs['tuple']['register(uint64,bool,(uint64,uint64,uint8)[],byte[][])void']>): GatePluginComposer<[...TReturns, GatePluginReturns['register(uint64,bool,(uint64,uint64,uint8)[],byte[][])void'] | undefined]>

  /**
   * Makes a clear_state call to an existing instance of the GatePlugin smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): GatePluginComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): GatePluginComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<GatePluginComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<GatePluginComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<GatePluginComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<GatePluginComposerResults<TReturns>>
}
export type GatePluginComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

