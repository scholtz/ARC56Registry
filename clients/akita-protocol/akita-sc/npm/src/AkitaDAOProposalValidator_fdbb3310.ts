/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"AkitaDAOProposalValidator","structs":{},"methods":[{"name":"create","args":[],"returns":{"type":"void"},"actions":{"create":["NoOp"],"call":[]},"readonly":false,"events":[],"recommendations":{}},{"name":"validateActions","args":[{"type":"uint64","name":"wallet","desc":"The ARC-58 abstracted account that the proposal will act on.\nThe validator calls `arc58_get*` methods on this app to check\nplugin/escrow/allowance/execution existence."},{"type":"(uint8,byte[])[]","name":"actions","desc":"The proposal's actions. Each is decoded by type and validated.\n\nReverts if any action is malformed or conflicts with wallet state."}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Validate every action on a proposal against the state of the supplied wallet.","events":[],"recommendations":{}}],"arcs":[22,28],"desc":"Stateless proposal-action validator that the Akita DAO delegates to.\n\nThe DAO calls `validateActions` before accepting a new or edited proposal,\npassing its own ARC-58 wallet app so the validator can read plugin/escrow/\nallowance/execution state via `abiCall`. Keeping this logic off the DAO\nfrees up DAO bytecode and lets validation rules be upgraded independently\nof the DAO itself — point the DAO at a new validator app to roll forward.\n\nThe contract intentionally has no state and no `update` handler; each\nversion is immutable so callers can trust its response shape over time.","networks":{},"state":{"schema":{"global":{"ints":0,"bytes":0},"local":{"ints":0,"bytes":0}},"keys":{"global":{},"local":{},"box":{}},"maps":{"global":{},"local":{},"box":{}}},"bareActions":{"create":[],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[704,955,2431,2593,2749,2828],"errorMessage":"Bytes has valid prefix"},{"pc":[1730],"errorMessage":"ERR:ALZ0"},{"pc":[1362],"errorMessage":"ERR:EALW"},{"pc":[1508],"errorMessage":"ERR:EESC"},{"pc":[544,595],"errorMessage":"ERR:EPLG"},{"pc":[2225],"errorMessage":"ERR:IACT"},{"pc":[1663],"errorMessage":"ERR:ICID"},{"pc":[2342],"errorMessage":"ERR:IDUR"},{"pc":[363],"errorMessage":"ERR:IFV"},{"pc":[382],"errorMessage":"ERR:ILV"},{"pc":[2256],"errorMessage":"ERR:IMNA"},{"pc":[2302],"errorMessage":"ERR:IMNP"},{"pc":[2364],"errorMessage":"ERR:IMNW"},{"pc":[1776],"errorMessage":"ERR:IMRI"},{"pc":[2276],"errorMessage":"ERR:IMXA"},{"pc":[2322],"errorMessage":"ERR:IMXP"},{"pc":[2384],"errorMessage":"ERR:IMXW"},{"pc":[1712],"errorMessage":"ERR:IPAL"},{"pc":[1276,1423],"errorMessage":"ERR:LEMP"},{"pc":[1797],"errorMessage":"ERR:MRI0"},{"pc":[1818],"errorMessage":"ERR:MRI1K"},{"pc":[260],"errorMessage":"ERR:NACT"},{"pc":[1451],"errorMessage":"ERR:NALW"},{"pc":[1565],"errorMessage":"ERR:NESC"},{"pc":[837],"errorMessage":"ERR:NEXE"},{"pc":[1076],"errorMessage":"ERR:NEXK"},{"pc":[347],"errorMessage":"ERR:NGRP"},{"pc":[812,1161,1212],"errorMessage":"ERR:NPLG"},{"pc":[877],"errorMessage":"ERR:PEXP"},{"pc":[2204],"errorMessage":"ERR:UFLD"},{"pc":[1307,2869],"errorMessage":"index access is out of bounds"},{"pc":[195,744,995,2468,2630],"errorMessage":"invalid array encoding"},{"pc":[165,222,332,708,772,959,1022,1061,1266,1416,2435,2496,2597,2658,2752,2831],"errorMessage":"invalid array length header"},{"pc":[2843],"errorMessage":"invalid number of bytes for arc4.dynamic_array<smart_contracts/arc58/account/types.ts::AllowanceInfo>"},{"pc":[2761],"errorMessage":"invalid number of bytes for arc4.dynamic_array<smart_contracts/arc58/account/types.ts::EscrowInfo>"},{"pc":[1046],"errorMessage":"invalid number of bytes for arc4.dynamic_array<smart_contracts/arc58/account/types.ts::ExecutionInfo>"},{"pc":[796,2520,2682],"errorMessage":"invalid number of bytes for arc4.dynamic_array<smart_contracts/arc58/account/types.ts::PluginInfo>"},{"pc":[243],"errorMessage":"invalid number of bytes for arc4.dynamic_array<smart_contracts/arc58/dao/types.ts::ProposalAction>"},{"pc":[157],"errorMessage":"invalid number of bytes for arc4.uint64"},{"pc":[1018],"errorMessage":"invalid tail pointer at index 0 of ((len+uint8[32][]),uint64,uint64)"},{"pc":[218],"errorMessage":"invalid tail pointer at index 1 of (uint8,(len+uint8[]))"},{"pc":[768,2492,2654],"errorMessage":"invalid tail pointer at index 4 of (uint64,uint8,uint64,uint64,(len+(uint8[4],uint64,uint64)[]),bool1,bool1,bool1,bool1,bool1,uint64,uint64)"},{"pc":[1003],"errorMessage":"invalid tail pointer for (len+((len+uint8[32][]),uint64,uint64)[])"},{"pc":[752,2476,2638],"errorMessage":"invalid tail pointer for (len+(uint64,uint8,uint64,uint64,(len+(uint8[4],uint64,uint64)[]),bool1,bool1,bool1,bool1,bool1,uint64,uint64)[])"},{"pc":[203],"errorMessage":"invalid tail pointer for (len+(uint8,(len+uint8[]))[])"},{"pc":[213,763,1013,2487,2649],"errorMessage":"invalid tuple encoding"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"events":[]} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


/**
 * The argument types for the AkitaDaoProposalValidator contract
 */
export type AkitaDaoProposalValidatorArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'create()void': Record<string, never>
    'validateActions(uint64,(uint8,byte[])[])void': {
      /**
      * The ARC-58 abstracted account that the proposal will act on.
      The validator calls `arc58_get*` methods on this app to check
      plugin/escrow/allowance/execution existence.

       */
      wallet: bigint | number
      /**
      * The proposal's actions. Each is decoded by type and validated.
      
      Reverts if any action is malformed or conflicts with wallet state.

       */
      actions: [bigint | number, Uint8Array][]
    }
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'create()void': []
    'validateActions(uint64,(uint8,byte[])[])void': [wallet: bigint | number, actions: [bigint | number, Uint8Array][]]
  }
}

/**
 * The return type for each method
 */
export type AkitaDaoProposalValidatorReturns = {
  'create()void': void
  'validateActions(uint64,(uint8,byte[])[])void': void
}

/**
 * Defines the types of available calls and state of the AkitaDaoProposalValidator smart contract.
 */
export type AkitaDaoProposalValidatorTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'create()void' | 'create', {
      argsObj: AkitaDaoProposalValidatorArgs['obj']['create()void']
      argsTuple: AkitaDaoProposalValidatorArgs['tuple']['create()void']
      returns: AkitaDaoProposalValidatorReturns['create()void']
    }>
    & Record<'validateActions(uint64,(uint8,byte[])[])void' | 'validateActions', {
      argsObj: AkitaDaoProposalValidatorArgs['obj']['validateActions(uint64,(uint8,byte[])[])void']
      argsTuple: AkitaDaoProposalValidatorArgs['tuple']['validateActions(uint64,(uint8,byte[])[])void']
      returns: AkitaDaoProposalValidatorReturns['validateActions(uint64,(uint8,byte[])[])void']
    }>
}

/**
 * Defines the possible abi call signatures.
 */
export type AkitaDaoProposalValidatorSignatures = keyof AkitaDaoProposalValidatorTypes['methods']
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the AkitaDaoProposalValidator smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends AkitaDaoProposalValidatorSignatures> = AkitaDaoProposalValidatorTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the AkitaDaoProposalValidator smart contract to the method's return type
 */
export type MethodReturn<TSignature extends AkitaDaoProposalValidatorSignatures> = AkitaDaoProposalValidatorTypes['methods'][TSignature]['returns']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the AkitaDaoProposalValidator smart contract
 */
export abstract class AkitaDaoProposalValidatorParamsFactory {
  /**
   * Constructs a no op call for the validateActions(uint64,(uint8,byte[])[])void ABI method
   *
   * Validate every action on a proposal against the state of the supplied wallet.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static validateActions(params: CallParams<AkitaDaoProposalValidatorArgs['obj']['validateActions(uint64,(uint8,byte[])[])void'] | AkitaDaoProposalValidatorArgs['tuple']['validateActions(uint64,(uint8,byte[])[])void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'validateActions(uint64,(uint8,byte[])[])void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.wallet, params.args.actions],
    }
  }
}

/**
 * A client to make calls to the AkitaDAOProposalValidator smart contract
 */
export class AkitaDaoProposalValidatorClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `AkitaDaoProposalValidatorClient`
   *
   * @param appClient An `AppClient` instance which has been created with the AkitaDaoProposalValidator app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `AkitaDaoProposalValidatorClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Returns a new `AkitaDaoProposalValidatorClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<AkitaDaoProposalValidatorClient> {
    return new AkitaDaoProposalValidatorClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `AkitaDaoProposalValidatorClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<AkitaDaoProposalValidatorClient> {
    return new AkitaDaoProposalValidatorClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Makes a clear_state call to an existing instance of the AkitaDAOProposalValidator smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the AkitaDAOProposalValidator smart contract using the `validateActions(uint64,(uint8,byte[])[])void` ABI method.
     *
     * Validate every action on a proposal against the state of the supplied wallet.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    validateActions: (params: CallParams<AkitaDaoProposalValidatorArgs['obj']['validateActions(uint64,(uint8,byte[])[])void'] | AkitaDaoProposalValidatorArgs['tuple']['validateActions(uint64,(uint8,byte[])[])void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(AkitaDaoProposalValidatorParamsFactory.validateActions(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Makes a clear_state call to an existing instance of the AkitaDAOProposalValidator smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the AkitaDAOProposalValidator smart contract using the `validateActions(uint64,(uint8,byte[])[])void` ABI method.
     *
     * Validate every action on a proposal against the state of the supplied wallet.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    validateActions: (params: CallParams<AkitaDaoProposalValidatorArgs['obj']['validateActions(uint64,(uint8,byte[])[])void'] | AkitaDaoProposalValidatorArgs['tuple']['validateActions(uint64,(uint8,byte[])[])void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(AkitaDaoProposalValidatorParamsFactory.validateActions(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Makes a clear_state call to an existing instance of the AkitaDAOProposalValidator smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the AkitaDAOProposalValidator smart contract using the `validateActions(uint64,(uint8,byte[])[])void` ABI method.
     *
     * Validate every action on a proposal against the state of the supplied wallet.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    validateActions: async (params: CallParams<AkitaDaoProposalValidatorArgs['obj']['validateActions(uint64,(uint8,byte[])[])void'] | AkitaDaoProposalValidatorArgs['tuple']['validateActions(uint64,(uint8,byte[])[])void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(AkitaDaoProposalValidatorParamsFactory.validateActions(params))
      return {...result, return: result.return as unknown as (undefined | AkitaDaoProposalValidatorReturns['validateActions(uint64,(uint8,byte[])[])void'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new AkitaDaoProposalValidatorClient(this.appClient.clone(params))
  }

  /**
   * Methods to access state for the current AkitaDAOProposalValidator app
   */
  state = {
  }

  public newGroup(): AkitaDaoProposalValidatorComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a validateActions(uint64,(uint8,byte[])[])void method call against the AkitaDAOProposalValidator contract
       */
      validateActions(params: CallParams<AkitaDaoProposalValidatorArgs['obj']['validateActions(uint64,(uint8,byte[])[])void'] | AkitaDaoProposalValidatorArgs['tuple']['validateActions(uint64,(uint8,byte[])[])void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.validateActions(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a clear state call to the AkitaDAOProposalValidator contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as AkitaDaoProposalValidatorComposer
  }
}
export type AkitaDaoProposalValidatorComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the validateActions(uint64,(uint8,byte[])[])void ABI method.
   *
   * Validate every action on a proposal against the state of the supplied wallet.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  validateActions(params?: CallParams<AkitaDaoProposalValidatorArgs['obj']['validateActions(uint64,(uint8,byte[])[])void'] | AkitaDaoProposalValidatorArgs['tuple']['validateActions(uint64,(uint8,byte[])[])void']>): AkitaDaoProposalValidatorComposer<[...TReturns, AkitaDaoProposalValidatorReturns['validateActions(uint64,(uint8,byte[])[])void'] | undefined]>

  /**
   * Makes a clear_state call to an existing instance of the AkitaDAOProposalValidator smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): AkitaDaoProposalValidatorComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): AkitaDaoProposalValidatorComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<AkitaDaoProposalValidatorComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<AkitaDaoProposalValidatorComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<AkitaDaoProposalValidatorComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<AkitaDaoProposalValidatorComposerResults<TReturns>>
}
export type AkitaDaoProposalValidatorComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

