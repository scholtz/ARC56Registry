/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"AbstractedAccountBalanceReader","structs":{},"methods":[{"name":"create","args":[{"type":"uint64","name":"akitaDAO","desc":"The app ID of the Akita DAO used to resolve dependent apps"}],"returns":{"type":"void"},"actions":{"create":["NoOp"],"call":[]},"readonly":false,"events":[],"recommendations":{}},{"name":"balance","args":[{"type":"address","name":"address","desc":"The address to look up balances for"},{"type":"uint64[]","name":"assets","desc":"The asset IDs to check balances for (0 for ALGO)"}],"returns":{"type":"uint64[]","desc":"The total balance for each asset (wallet + staked hard + staked lock)"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"desc":"Get the balance of a set of assets at the given address, including any\namounts that address has staked in the Akita staking contract.","events":[],"recommendations":{}}],"arcs":[22,28],"desc":"Immutable readonly helper that reports the combined balance\n(wallet holdings + staked hard + staked lock) for an address.\n\nThis was extracted from `AbstractedAccount.balance()` to keep that logic\navailable for downstream contracts without keeping it on-chain in the\nwallet bytecode. The contract intentionally has no UpdateApplication\nhandler, so once deployed it cannot be re-keyed / upgraded and callers\ncan trust its response shape over time.\n\nA balance returned by this contract is still only as trustworthy as\nthe underlying AkitaDAO + Staking apps it queries — do not use it as\na security-critical balance source for an adversarial wallet.","networks":{},"state":{"schema":{"global":{"ints":0,"bytes":1},"local":{"ints":0,"bytes":0}},"keys":{"global":{"akitaDAO":{"keyType":"AVMString","valueType":"AVMUint64","key":"YWtpdGFfZGFv","desc":"The app ID of the Akita DAO used to resolve the Staking app"}},"local":{},"box":{}},"maps":{"global":{},"local":{},"box":{}}},"bareActions":{"create":[],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[204],"errorMessage":"Bytes has valid prefix"},{"pc":[150],"errorMessage":"account funded"},{"pc":[155],"errorMessage":"check GlobalState exists"},{"pc":[133],"errorMessage":"index access is out of bounds"},{"pc":[94,235],"errorMessage":"invalid array length header"},{"pc":[106],"errorMessage":"invalid number of bytes for arc4.dynamic_array<uint64>"},{"pc":[87],"errorMessage":"invalid number of bytes for arc4.static_array<arc4.uint8, 32>"},{"pc":[67],"errorMessage":"invalid number of bytes for arc4.uint64"},{"pc":[209],"errorMessage":"invalid number of bytes for smart_contracts/staking/types.ts::Escrow"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"events":[]} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


/**
 * The argument types for the AbstractedAccountBalanceReader contract
 */
export type AbstractedAccountBalanceReaderArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'create(uint64)void': {
      /**
       * The app ID of the Akita DAO used to resolve dependent apps
       */
      akitaDao: bigint | number
    }
    'balance(address,uint64[])uint64[]': {
      /**
       * The address to look up balances for
       */
      address: string
      /**
       * The asset IDs to check balances for (0 for ALGO)
       */
      assets: bigint[] | number[]
    }
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'create(uint64)void': [akitaDao: bigint | number]
    'balance(address,uint64[])uint64[]': [address: string, assets: bigint[] | number[]]
  }
}

/**
 * The return type for each method
 */
export type AbstractedAccountBalanceReaderReturns = {
  'create(uint64)void': void
  'balance(address,uint64[])uint64[]': bigint[]
}

/**
 * Defines the types of available calls and state of the AbstractedAccountBalanceReader smart contract.
 */
export type AbstractedAccountBalanceReaderTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'create(uint64)void' | 'create', {
      argsObj: AbstractedAccountBalanceReaderArgs['obj']['create(uint64)void']
      argsTuple: AbstractedAccountBalanceReaderArgs['tuple']['create(uint64)void']
      returns: AbstractedAccountBalanceReaderReturns['create(uint64)void']
    }>
    & Record<'balance(address,uint64[])uint64[]' | 'balance', {
      argsObj: AbstractedAccountBalanceReaderArgs['obj']['balance(address,uint64[])uint64[]']
      argsTuple: AbstractedAccountBalanceReaderArgs['tuple']['balance(address,uint64[])uint64[]']
      /**
       * The total balance for each asset (wallet + staked hard + staked lock)
       */
      returns: AbstractedAccountBalanceReaderReturns['balance(address,uint64[])uint64[]']
    }>
  /**
   * Defines the shape of the state of the application.
   */
  state: {
    global: {
      keys: {
        /**
         * The app ID of the Akita DAO used to resolve the Staking app
         */
        akitaDao: bigint
      }
      maps: {}
    }
  }
}

/**
 * Defines the possible abi call signatures.
 */
export type AbstractedAccountBalanceReaderSignatures = keyof AbstractedAccountBalanceReaderTypes['methods']
/**
 * Defines the possible abi call signatures for methods that return a non-void value.
 */
export type AbstractedAccountBalanceReaderNonVoidMethodSignatures = keyof AbstractedAccountBalanceReaderTypes['methods'] extends infer T ? T extends keyof AbstractedAccountBalanceReaderTypes['methods'] ? MethodReturn<T> extends void ? never : T  : never : never
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the AbstractedAccountBalanceReader smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends AbstractedAccountBalanceReaderSignatures> = AbstractedAccountBalanceReaderTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the AbstractedAccountBalanceReader smart contract to the method's return type
 */
export type MethodReturn<TSignature extends AbstractedAccountBalanceReaderSignatures> = AbstractedAccountBalanceReaderTypes['methods'][TSignature]['returns']

/**
 * Defines the shape of the keyed global state of the application.
 */
export type GlobalKeysState = AbstractedAccountBalanceReaderTypes['state']['global']['keys']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the AbstractedAccountBalanceReader smart contract
 */
export abstract class AbstractedAccountBalanceReaderParamsFactory {
  /**
   * Constructs a no op call for the balance(address,uint64[])uint64[] ABI method
   *
  * Get the balance of a set of assets at the given address, including any
  amounts that address has staked in the Akita staking contract.

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static balance(params: CallParams<AbstractedAccountBalanceReaderArgs['obj']['balance(address,uint64[])uint64[]'] | AbstractedAccountBalanceReaderArgs['tuple']['balance(address,uint64[])uint64[]']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'balance(address,uint64[])uint64[]' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.address, params.args.assets],
    }
  }
}

/**
 * A client to make calls to the AbstractedAccountBalanceReader smart contract
 */
export class AbstractedAccountBalanceReaderClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `AbstractedAccountBalanceReaderClient`
   *
   * @param appClient An `AppClient` instance which has been created with the AbstractedAccountBalanceReader app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `AbstractedAccountBalanceReaderClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Checks for decode errors on the given return value and maps the return value to the return type for the given method
   * @returns The typed return value or undefined if there was no value
   */
  decodeReturnValue<TSignature extends AbstractedAccountBalanceReaderNonVoidMethodSignatures>(method: TSignature, returnValue: ABIReturn | undefined) {
    return returnValue !== undefined ? getArc56ReturnValue<MethodReturn<TSignature>>(returnValue, this.appClient.getABIMethod(method), APP_SPEC.structs) : undefined
  }

  /**
   * Returns a new `AbstractedAccountBalanceReaderClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<AbstractedAccountBalanceReaderClient> {
    return new AbstractedAccountBalanceReaderClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `AbstractedAccountBalanceReaderClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<AbstractedAccountBalanceReaderClient> {
    return new AbstractedAccountBalanceReaderClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Makes a clear_state call to an existing instance of the AbstractedAccountBalanceReader smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the AbstractedAccountBalanceReader smart contract using the `balance(address,uint64[])uint64[]` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
    * Get the balance of a set of assets at the given address, including any
    amounts that address has staked in the Akita staking contract.

     *
     * @param params The params for the smart contract call
     * @returns The call params: The total balance for each asset (wallet + staked hard + staked lock)
     */
    balance: (params: CallParams<AbstractedAccountBalanceReaderArgs['obj']['balance(address,uint64[])uint64[]'] | AbstractedAccountBalanceReaderArgs['tuple']['balance(address,uint64[])uint64[]']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(AbstractedAccountBalanceReaderParamsFactory.balance(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Makes a clear_state call to an existing instance of the AbstractedAccountBalanceReader smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the AbstractedAccountBalanceReader smart contract using the `balance(address,uint64[])uint64[]` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
    * Get the balance of a set of assets at the given address, including any
    amounts that address has staked in the Akita staking contract.

     *
     * @param params The params for the smart contract call
     * @returns The call transaction: The total balance for each asset (wallet + staked hard + staked lock)
     */
    balance: (params: CallParams<AbstractedAccountBalanceReaderArgs['obj']['balance(address,uint64[])uint64[]'] | AbstractedAccountBalanceReaderArgs['tuple']['balance(address,uint64[])uint64[]']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(AbstractedAccountBalanceReaderParamsFactory.balance(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Makes a clear_state call to an existing instance of the AbstractedAccountBalanceReader smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the AbstractedAccountBalanceReader smart contract using the `balance(address,uint64[])uint64[]` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
    * Get the balance of a set of assets at the given address, including any
    amounts that address has staked in the Akita staking contract.

     *
     * @param params The params for the smart contract call
     * @returns The call result: The total balance for each asset (wallet + staked hard + staked lock)
     */
    balance: async (params: CallParams<AbstractedAccountBalanceReaderArgs['obj']['balance(address,uint64[])uint64[]'] | AbstractedAccountBalanceReaderArgs['tuple']['balance(address,uint64[])uint64[]']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(AbstractedAccountBalanceReaderParamsFactory.balance(params))
      return {...result, return: result.return as unknown as (undefined | AbstractedAccountBalanceReaderReturns['balance(address,uint64[])uint64[]'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new AbstractedAccountBalanceReaderClient(this.appClient.clone(params))
  }

  /**
   * Makes a readonly (simulated) call to the AbstractedAccountBalanceReader smart contract using the `balance(address,uint64[])uint64[]` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
  * Get the balance of a set of assets at the given address, including any
  amounts that address has staked in the Akita staking contract.

   *
   * @param params The params for the smart contract call
   * @returns The call result: The total balance for each asset (wallet + staked hard + staked lock)
   */
  async balance(params: CallParams<AbstractedAccountBalanceReaderArgs['obj']['balance(address,uint64[])uint64[]'] | AbstractedAccountBalanceReaderArgs['tuple']['balance(address,uint64[])uint64[]']>) {
    const result = await this.appClient.send.call(AbstractedAccountBalanceReaderParamsFactory.balance(params))
    return result.return as unknown as AbstractedAccountBalanceReaderReturns['balance(address,uint64[])uint64[]']
  }

  /**
   * Methods to access state for the current AbstractedAccountBalanceReader app
   */
  state = {
    /**
     * Methods to access global state for the current AbstractedAccountBalanceReader app
     */
    global: {
      /**
       * Get all current keyed values from global state
       */
      getAll: async (): Promise<Partial<Expand<GlobalKeysState>>> => {
        const result = await this.appClient.state.global.getAll()
        return {
          akitaDao: result.akitaDAO,
        }
      },
      /**
       * Get the current value of the akitaDAO key in global state
       */
      akitaDao: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("akitaDAO")) as bigint | undefined },
    },
  }

  public newGroup(): AbstractedAccountBalanceReaderComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a balance(address,uint64[])uint64[] method call against the AbstractedAccountBalanceReader contract
       */
      balance(params: CallParams<AbstractedAccountBalanceReaderArgs['obj']['balance(address,uint64[])uint64[]'] | AbstractedAccountBalanceReaderArgs['tuple']['balance(address,uint64[])uint64[]']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.balance(params)))
        resultMappers.push((v) => client.decodeReturnValue('balance(address,uint64[])uint64[]', v))
        return this
      },
      /**
       * Add a clear state call to the AbstractedAccountBalanceReader contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as AbstractedAccountBalanceReaderComposer
  }
}
export type AbstractedAccountBalanceReaderComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the balance(address,uint64[])uint64[] ABI method.
   *
  * Get the balance of a set of assets at the given address, including any
  amounts that address has staked in the Akita staking contract.

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  balance(params?: CallParams<AbstractedAccountBalanceReaderArgs['obj']['balance(address,uint64[])uint64[]'] | AbstractedAccountBalanceReaderArgs['tuple']['balance(address,uint64[])uint64[]']>): AbstractedAccountBalanceReaderComposer<[...TReturns, AbstractedAccountBalanceReaderReturns['balance(address,uint64[])uint64[]'] | undefined]>

  /**
   * Makes a clear_state call to an existing instance of the AbstractedAccountBalanceReader smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): AbstractedAccountBalanceReaderComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): AbstractedAccountBalanceReaderComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<AbstractedAccountBalanceReaderComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<AbstractedAccountBalanceReaderComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<AbstractedAccountBalanceReaderComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<AbstractedAccountBalanceReaderComposerResults<TReturns>>
}
export type AbstractedAccountBalanceReaderComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

