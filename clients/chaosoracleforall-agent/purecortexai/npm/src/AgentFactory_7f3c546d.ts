/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"AgentFactory","structs":{},"methods":[{"name":"bootstrap_protocol","args":[],"returns":{"type":"uint64"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Creates the master $CORTEX utility token.\nOnly callable by the application creator.","events":[],"recommendations":{}},{"name":"create_agent","args":[{"type":"axfer","name":"cortex_payment"},{"type":"string","name":"name"},{"type":"string","name":"unit_name"}],"returns":{"type":"uint64"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Creates a new Agent Token. Requires a CORTEX fee (default 100).","events":[],"recommendations":{}},{"name":"calculate_buy_price","args":[{"type":"uint64","name":"asset"},{"type":"uint64","name":"amount"}],"returns":{"type":"uint64"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"desc":"Calculates ALGO required to buy 'amount' of tokens along the curve.\nPrice = integral of (BASE_PRICE + SLOPE * supply) from S to S+amount.","events":[],"recommendations":{}},{"name":"buy_tokens","args":[{"type":"pay","name":"payment"},{"type":"uint64","name":"asset"},{"type":"uint64","name":"amount"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Buys tokens from the bonding curve by paying ALGO.\nA buy fee (default 1%) is added on top of the curve price.","events":[],"recommendations":{}},{"name":"calculate_sell_price","args":[{"type":"uint64","name":"asset"},{"type":"uint64","name":"amount"}],"returns":{"type":"uint64"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"desc":"Calculate ALGO returned for selling 'amount' tokens.\nIntegral of curve from (supply - amount) to supply, minus sell fee.","events":[],"recommendations":{}},{"name":"sell_tokens","args":[{"type":"axfer","name":"token_transfer"},{"type":"uint64","name":"asset"},{"type":"uint64","name":"amount"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Sells tokens back to the bonding curve for ALGO.\nThe seller must send an asset transfer of the agent token to the contract in the same group transaction.","events":[],"recommendations":{}},{"name":"check_graduation","args":[{"type":"uint64","name":"asset"}],"returns":{"type":"bool"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"desc":"Check if an agent token has met the graduation threshold.\nGraduation means the total value locked in the bonding curve exceeds the configured threshold, signaling readiness for DEX listing.","events":[],"recommendations":{}},{"name":"distribute_cortex","args":[{"type":"address","name":"receiver"},{"type":"uint64","name":"amount"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Distribute CORTEX tokens from the factory escrow.\nOnly callable by the application creator. Used for testnet distribution and genesis airdrop.","events":[],"recommendations":{}},{"name":"update_fee_parameters","args":[{"type":"uint64","name":"buy_fee"},{"type":"uint64","name":"sell_fee"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Update fee parameters.\nOnly callable by the application creator (later: governance contract). Fees are in basis points (100 = 1%). Max 10%.","events":[],"recommendations":{}},{"name":"update_creation_fee","args":[{"type":"uint64","name":"new_fee"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Update agent creation fee (in CORTEX micro-units).\nOnly callable by the application creator.","events":[],"recommendations":{}},{"name":"update_graduation_threshold","args":[{"type":"uint64","name":"new_threshold"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Update the graduation threshold.\nOnly callable by the application creator.","events":[],"recommendations":{}}],"arcs":[22,28],"networks":{},"state":{"schema":{"global":{"ints":8,"bytes":0},"local":{"ints":0,"bytes":0}},"keys":{"global":{"latest_asset_id":{"keyType":"AVMString","valueType":"AVMUint64","key":"bGF0ZXN0X2Fzc2V0X2lk"},"cortex_token":{"keyType":"AVMString","valueType":"AVMUint64","key":"Y29ydGV4X3Rva2Vu"},"BASE_PRICE":{"keyType":"AVMString","valueType":"AVMUint64","key":"QkFTRV9QUklDRQ=="},"SLOPE":{"keyType":"AVMString","valueType":"AVMUint64","key":"U0xPUEU="},"buy_fee_bps":{"keyType":"AVMString","valueType":"AVMUint64","key":"YnV5X2ZlZV9icHM="},"sell_fee_bps":{"keyType":"AVMString","valueType":"AVMUint64","key":"c2VsbF9mZWVfYnBz"},"creation_fee":{"keyType":"AVMString","valueType":"AVMUint64","key":"Y3JlYXRpb25fZmVl"},"GRADUATION_THRESHOLD":{"keyType":"AVMString","valueType":"AVMUint64","key":"R1JBRFVBVElPTl9USFJFU0hPTEQ="}},"local":{},"box":{}},"maps":{"global":{},"local":{},"box":{"agent_supplies":{"keyType":"uint64","valueType":"uint64","prefix":"YWdlbnRfc3VwcGxpZXM="}}}},"bareActions":{"create":["NoOp"],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[309],"errorMessage":"Already bootstrapped"},{"pc":[1068],"errorMessage":"Amount exceeds max supply"},{"pc":[951,1062,1125],"errorMessage":"Amount must be positive"},{"pc":[1152],"errorMessage":"Cannot sell more than supply"},{"pc":[498],"errorMessage":"Fee must go to factory"},{"pc":[473],"errorMessage":"Fee sender must match caller"},{"pc":[651],"errorMessage":"Insufficient ALGO payment including fee"},{"pc":[490],"errorMessage":"Insufficient creation fee"},{"pc":[787],"errorMessage":"Insufficient tokens sent"},{"pc":[479],"errorMessage":"Invalid fee asset"},{"pc":[998],"errorMessage":"Max buy fee is 10%"},{"pc":[1003],"errorMessage":"Max sell fee is 10%"},{"pc":[628],"errorMessage":"Minimum buy is 1000 micro-units"},{"pc":[792],"errorMessage":"Minimum sell is 1000 micro-units"},{"pc":[453],"errorMessage":"Name cannot be empty"},{"pc":[457],"errorMessage":"Name too long (max 32 bytes)"},{"pc":[303],"errorMessage":"Only creator can bootstrap"},{"pc":[942],"errorMessage":"Only creator can distribute"},{"pc":[623],"errorMessage":"Payment must be directed to the factory contract"},{"pc":[615],"errorMessage":"Payment sender must match caller"},{"pc":[448,948],"errorMessage":"Protocol not bootstrapped"},{"pc":[771],"errorMessage":"Token sender must match caller"},{"pc":[800],"errorMessage":"Tokens must go to factory"},{"pc":[992,1029,1050],"errorMessage":"Unauthorized"},{"pc":[462],"errorMessage":"Unit name cannot be empty"},{"pc":[465],"errorMessage":"Unit name too long (max 8 bytes)"},{"pc":[779],"errorMessage":"Wrong asset"},{"pc":[889,1092,1160],"errorMessage":"check self.BASE_PRICE exists"},{"pc":[914],"errorMessage":"check self.GRADUATION_THRESHOLD exists"},{"pc":[905,1112,1178],"errorMessage":"check self.SLOPE exists"},{"pc":[667,813,874,1085,1142],"errorMessage":"check self.agent_supplies entry exists"},{"pc":[639],"errorMessage":"check self.buy_fee_bps exists"},{"pc":[307,446,946],"errorMessage":"check self.cortex_token exists"},{"pc":[488],"errorMessage":"check self.creation_fee exists"},{"pc":[1190],"errorMessage":"check self.sell_fee_bps exists"},{"pc":[416,432],"errorMessage":"invalid array length header"},{"pc":[423,439],"errorMessage":"invalid number of bytes for arc4.dynamic_array<arc4.uint8>"},{"pc":[927],"errorMessage":"invalid number of bytes for arc4.static_array<arc4.uint8, 32>"},{"pc":[553,562,591,603,714,723,753,762,845,935,976,985,1022,1043],"errorMessage":"invalid number of bytes for arc4.uint64"},{"pc":[410,745],"errorMessage":"transaction type is axfer"},{"pc":[583],"errorMessage":"transaction type is pay"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"events":[]} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


/**
 * The argument types for the AgentFactory contract
 */
export type AgentFactoryArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'bootstrap_protocol()uint64': Record<string, never>
    'create_agent(axfer,string,string)uint64': {
      cortexPayment: AppMethodCallTransactionArgument
      name: string
      unitName: string
    }
    'calculate_buy_price(uint64,uint64)uint64': {
      asset: bigint | number
      amount: bigint | number
    }
    'buy_tokens(pay,uint64,uint64)void': {
      payment: AppMethodCallTransactionArgument
      asset: bigint | number
      amount: bigint | number
    }
    'calculate_sell_price(uint64,uint64)uint64': {
      asset: bigint | number
      amount: bigint | number
    }
    'sell_tokens(axfer,uint64,uint64)void': {
      tokenTransfer: AppMethodCallTransactionArgument
      asset: bigint | number
      amount: bigint | number
    }
    'check_graduation(uint64)bool': {
      asset: bigint | number
    }
    'distribute_cortex(address,uint64)void': {
      receiver: string
      amount: bigint | number
    }
    'update_fee_parameters(uint64,uint64)void': {
      buyFee: bigint | number
      sellFee: bigint | number
    }
    'update_creation_fee(uint64)void': {
      newFee: bigint | number
    }
    'update_graduation_threshold(uint64)void': {
      newThreshold: bigint | number
    }
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'bootstrap_protocol()uint64': []
    'create_agent(axfer,string,string)uint64': [cortexPayment: AppMethodCallTransactionArgument, name: string, unitName: string]
    'calculate_buy_price(uint64,uint64)uint64': [asset: bigint | number, amount: bigint | number]
    'buy_tokens(pay,uint64,uint64)void': [payment: AppMethodCallTransactionArgument, asset: bigint | number, amount: bigint | number]
    'calculate_sell_price(uint64,uint64)uint64': [asset: bigint | number, amount: bigint | number]
    'sell_tokens(axfer,uint64,uint64)void': [tokenTransfer: AppMethodCallTransactionArgument, asset: bigint | number, amount: bigint | number]
    'check_graduation(uint64)bool': [asset: bigint | number]
    'distribute_cortex(address,uint64)void': [receiver: string, amount: bigint | number]
    'update_fee_parameters(uint64,uint64)void': [buyFee: bigint | number, sellFee: bigint | number]
    'update_creation_fee(uint64)void': [newFee: bigint | number]
    'update_graduation_threshold(uint64)void': [newThreshold: bigint | number]
  }
}

/**
 * The return type for each method
 */
export type AgentFactoryReturns = {
  'bootstrap_protocol()uint64': bigint
  'create_agent(axfer,string,string)uint64': bigint
  'calculate_buy_price(uint64,uint64)uint64': bigint
  'buy_tokens(pay,uint64,uint64)void': void
  'calculate_sell_price(uint64,uint64)uint64': bigint
  'sell_tokens(axfer,uint64,uint64)void': void
  'check_graduation(uint64)bool': boolean
  'distribute_cortex(address,uint64)void': void
  'update_fee_parameters(uint64,uint64)void': void
  'update_creation_fee(uint64)void': void
  'update_graduation_threshold(uint64)void': void
}

/**
 * Defines the types of available calls and state of the AgentFactory smart contract.
 */
export type AgentFactoryTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'bootstrap_protocol()uint64' | 'bootstrap_protocol', {
      argsObj: AgentFactoryArgs['obj']['bootstrap_protocol()uint64']
      argsTuple: AgentFactoryArgs['tuple']['bootstrap_protocol()uint64']
      returns: AgentFactoryReturns['bootstrap_protocol()uint64']
    }>
    & Record<'create_agent(axfer,string,string)uint64' | 'create_agent', {
      argsObj: AgentFactoryArgs['obj']['create_agent(axfer,string,string)uint64']
      argsTuple: AgentFactoryArgs['tuple']['create_agent(axfer,string,string)uint64']
      returns: AgentFactoryReturns['create_agent(axfer,string,string)uint64']
    }>
    & Record<'calculate_buy_price(uint64,uint64)uint64' | 'calculate_buy_price', {
      argsObj: AgentFactoryArgs['obj']['calculate_buy_price(uint64,uint64)uint64']
      argsTuple: AgentFactoryArgs['tuple']['calculate_buy_price(uint64,uint64)uint64']
      returns: AgentFactoryReturns['calculate_buy_price(uint64,uint64)uint64']
    }>
    & Record<'buy_tokens(pay,uint64,uint64)void' | 'buy_tokens', {
      argsObj: AgentFactoryArgs['obj']['buy_tokens(pay,uint64,uint64)void']
      argsTuple: AgentFactoryArgs['tuple']['buy_tokens(pay,uint64,uint64)void']
      returns: AgentFactoryReturns['buy_tokens(pay,uint64,uint64)void']
    }>
    & Record<'calculate_sell_price(uint64,uint64)uint64' | 'calculate_sell_price', {
      argsObj: AgentFactoryArgs['obj']['calculate_sell_price(uint64,uint64)uint64']
      argsTuple: AgentFactoryArgs['tuple']['calculate_sell_price(uint64,uint64)uint64']
      returns: AgentFactoryReturns['calculate_sell_price(uint64,uint64)uint64']
    }>
    & Record<'sell_tokens(axfer,uint64,uint64)void' | 'sell_tokens', {
      argsObj: AgentFactoryArgs['obj']['sell_tokens(axfer,uint64,uint64)void']
      argsTuple: AgentFactoryArgs['tuple']['sell_tokens(axfer,uint64,uint64)void']
      returns: AgentFactoryReturns['sell_tokens(axfer,uint64,uint64)void']
    }>
    & Record<'check_graduation(uint64)bool' | 'check_graduation', {
      argsObj: AgentFactoryArgs['obj']['check_graduation(uint64)bool']
      argsTuple: AgentFactoryArgs['tuple']['check_graduation(uint64)bool']
      returns: AgentFactoryReturns['check_graduation(uint64)bool']
    }>
    & Record<'distribute_cortex(address,uint64)void' | 'distribute_cortex', {
      argsObj: AgentFactoryArgs['obj']['distribute_cortex(address,uint64)void']
      argsTuple: AgentFactoryArgs['tuple']['distribute_cortex(address,uint64)void']
      returns: AgentFactoryReturns['distribute_cortex(address,uint64)void']
    }>
    & Record<'update_fee_parameters(uint64,uint64)void' | 'update_fee_parameters', {
      argsObj: AgentFactoryArgs['obj']['update_fee_parameters(uint64,uint64)void']
      argsTuple: AgentFactoryArgs['tuple']['update_fee_parameters(uint64,uint64)void']
      returns: AgentFactoryReturns['update_fee_parameters(uint64,uint64)void']
    }>
    & Record<'update_creation_fee(uint64)void' | 'update_creation_fee', {
      argsObj: AgentFactoryArgs['obj']['update_creation_fee(uint64)void']
      argsTuple: AgentFactoryArgs['tuple']['update_creation_fee(uint64)void']
      returns: AgentFactoryReturns['update_creation_fee(uint64)void']
    }>
    & Record<'update_graduation_threshold(uint64)void' | 'update_graduation_threshold', {
      argsObj: AgentFactoryArgs['obj']['update_graduation_threshold(uint64)void']
      argsTuple: AgentFactoryArgs['tuple']['update_graduation_threshold(uint64)void']
      returns: AgentFactoryReturns['update_graduation_threshold(uint64)void']
    }>
  /**
   * Defines the shape of the state of the application.
   */
  state: {
    global: {
      keys: {
        latestAssetId: bigint
        cortexToken: bigint
        basePrice: bigint
        slope: bigint
        buyFeeBps: bigint
        sellFeeBps: bigint
        creationFee: bigint
        graduationThreshold: bigint
      }
      maps: {}
    }
    box: {
      keys: {}
      maps: {
        agentSupplies: Map<bigint | number, bigint>
      }
    }
  }
}

/**
 * Defines the possible abi call signatures.
 */
export type AgentFactorySignatures = keyof AgentFactoryTypes['methods']
/**
 * Defines the possible abi call signatures for methods that return a non-void value.
 */
export type AgentFactoryNonVoidMethodSignatures = keyof AgentFactoryTypes['methods'] extends infer T ? T extends keyof AgentFactoryTypes['methods'] ? MethodReturn<T> extends void ? never : T  : never : never
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the AgentFactory smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends AgentFactorySignatures> = AgentFactoryTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the AgentFactory smart contract to the method's return type
 */
export type MethodReturn<TSignature extends AgentFactorySignatures> = AgentFactoryTypes['methods'][TSignature]['returns']

/**
 * Defines the shape of the keyed global state of the application.
 */
export type GlobalKeysState = AgentFactoryTypes['state']['global']['keys']

/**
 * Defines the shape of the keyed box state of the application.
 */
export type BoxKeysState = AgentFactoryTypes['state']['box']['keys']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the AgentFactory smart contract
 */
export abstract class AgentFactoryParamsFactory {
  /**
   * Constructs a no op call for the bootstrap_protocol()uint64 ABI method
   *
  * Creates the master $CORTEX utility token.
  Only callable by the application creator.

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static bootstrapProtocol(params: CallParams<AgentFactoryArgs['obj']['bootstrap_protocol()uint64'] | AgentFactoryArgs['tuple']['bootstrap_protocol()uint64']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'bootstrap_protocol()uint64' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the create_agent(axfer,string,string)uint64 ABI method
   *
   * Creates a new Agent Token. Requires a CORTEX fee (default 100).
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static createAgent(params: CallParams<AgentFactoryArgs['obj']['create_agent(axfer,string,string)uint64'] | AgentFactoryArgs['tuple']['create_agent(axfer,string,string)uint64']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'create_agent(axfer,string,string)uint64' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.cortexPayment, params.args.name, params.args.unitName],
    }
  }
  /**
   * Constructs a no op call for the calculate_buy_price(uint64,uint64)uint64 ABI method
   *
  * Calculates ALGO required to buy 'amount' of tokens along the curve.
  Price = integral of (BASE_PRICE + SLOPE * supply) from S to S+amount.

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static calculateBuyPrice(params: CallParams<AgentFactoryArgs['obj']['calculate_buy_price(uint64,uint64)uint64'] | AgentFactoryArgs['tuple']['calculate_buy_price(uint64,uint64)uint64']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'calculate_buy_price(uint64,uint64)uint64' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.asset, params.args.amount],
    }
  }
  /**
   * Constructs a no op call for the buy_tokens(pay,uint64,uint64)void ABI method
   *
  * Buys tokens from the bonding curve by paying ALGO.
  A buy fee (default 1%) is added on top of the curve price.

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static buyTokens(params: CallParams<AgentFactoryArgs['obj']['buy_tokens(pay,uint64,uint64)void'] | AgentFactoryArgs['tuple']['buy_tokens(pay,uint64,uint64)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'buy_tokens(pay,uint64,uint64)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.payment, params.args.asset, params.args.amount],
    }
  }
  /**
   * Constructs a no op call for the calculate_sell_price(uint64,uint64)uint64 ABI method
   *
  * Calculate ALGO returned for selling 'amount' tokens.
  Integral of curve from (supply - amount) to supply, minus sell fee.

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static calculateSellPrice(params: CallParams<AgentFactoryArgs['obj']['calculate_sell_price(uint64,uint64)uint64'] | AgentFactoryArgs['tuple']['calculate_sell_price(uint64,uint64)uint64']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'calculate_sell_price(uint64,uint64)uint64' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.asset, params.args.amount],
    }
  }
  /**
   * Constructs a no op call for the sell_tokens(axfer,uint64,uint64)void ABI method
   *
  * Sells tokens back to the bonding curve for ALGO.
  The seller must send an asset transfer of the agent token to the contract in the same group transaction.

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static sellTokens(params: CallParams<AgentFactoryArgs['obj']['sell_tokens(axfer,uint64,uint64)void'] | AgentFactoryArgs['tuple']['sell_tokens(axfer,uint64,uint64)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'sell_tokens(axfer,uint64,uint64)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.tokenTransfer, params.args.asset, params.args.amount],
    }
  }
  /**
   * Constructs a no op call for the check_graduation(uint64)bool ABI method
   *
  * Check if an agent token has met the graduation threshold.
  Graduation means the total value locked in the bonding curve exceeds the configured threshold, signaling readiness for DEX listing.

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static checkGraduation(params: CallParams<AgentFactoryArgs['obj']['check_graduation(uint64)bool'] | AgentFactoryArgs['tuple']['check_graduation(uint64)bool']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'check_graduation(uint64)bool' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.asset],
    }
  }
  /**
   * Constructs a no op call for the distribute_cortex(address,uint64)void ABI method
   *
  * Distribute CORTEX tokens from the factory escrow.
  Only callable by the application creator. Used for testnet distribution and genesis airdrop.

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static distributeCortex(params: CallParams<AgentFactoryArgs['obj']['distribute_cortex(address,uint64)void'] | AgentFactoryArgs['tuple']['distribute_cortex(address,uint64)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'distribute_cortex(address,uint64)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.receiver, params.args.amount],
    }
  }
  /**
   * Constructs a no op call for the update_fee_parameters(uint64,uint64)void ABI method
   *
  * Update fee parameters.
  Only callable by the application creator (later: governance contract). Fees are in basis points (100 = 1%). Max 10%.

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static updateFeeParameters(params: CallParams<AgentFactoryArgs['obj']['update_fee_parameters(uint64,uint64)void'] | AgentFactoryArgs['tuple']['update_fee_parameters(uint64,uint64)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'update_fee_parameters(uint64,uint64)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.buyFee, params.args.sellFee],
    }
  }
  /**
   * Constructs a no op call for the update_creation_fee(uint64)void ABI method
   *
  * Update agent creation fee (in CORTEX micro-units).
  Only callable by the application creator.

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static updateCreationFee(params: CallParams<AgentFactoryArgs['obj']['update_creation_fee(uint64)void'] | AgentFactoryArgs['tuple']['update_creation_fee(uint64)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'update_creation_fee(uint64)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.newFee],
    }
  }
  /**
   * Constructs a no op call for the update_graduation_threshold(uint64)void ABI method
   *
  * Update the graduation threshold.
  Only callable by the application creator.

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static updateGraduationThreshold(params: CallParams<AgentFactoryArgs['obj']['update_graduation_threshold(uint64)void'] | AgentFactoryArgs['tuple']['update_graduation_threshold(uint64)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'update_graduation_threshold(uint64)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.newThreshold],
    }
  }
}

/**
 * A client to make calls to the AgentFactory smart contract
 */
export class AgentFactoryClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `AgentFactoryClient`
   *
   * @param appClient An `AppClient` instance which has been created with the AgentFactory app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `AgentFactoryClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Checks for decode errors on the given return value and maps the return value to the return type for the given method
   * @returns The typed return value or undefined if there was no value
   */
  decodeReturnValue<TSignature extends AgentFactoryNonVoidMethodSignatures>(method: TSignature, returnValue: ABIReturn | undefined) {
    return returnValue !== undefined ? getArc56ReturnValue<MethodReturn<TSignature>>(returnValue, this.appClient.getABIMethod(method), APP_SPEC.structs) : undefined
  }

  /**
   * Returns a new `AgentFactoryClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<AgentFactoryClient> {
    return new AgentFactoryClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `AgentFactoryClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<AgentFactoryClient> {
    return new AgentFactoryClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Makes a clear_state call to an existing instance of the AgentFactory smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the AgentFactory smart contract using the `bootstrap_protocol()uint64` ABI method.
     *
    * Creates the master $CORTEX utility token.
    Only callable by the application creator.

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    bootstrapProtocol: (params: CallParams<AgentFactoryArgs['obj']['bootstrap_protocol()uint64'] | AgentFactoryArgs['tuple']['bootstrap_protocol()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(AgentFactoryParamsFactory.bootstrapProtocol(params))
    },

    /**
     * Makes a call to the AgentFactory smart contract using the `create_agent(axfer,string,string)uint64` ABI method.
     *
     * Creates a new Agent Token. Requires a CORTEX fee (default 100).
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    createAgent: (params: CallParams<AgentFactoryArgs['obj']['create_agent(axfer,string,string)uint64'] | AgentFactoryArgs['tuple']['create_agent(axfer,string,string)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(AgentFactoryParamsFactory.createAgent(params))
    },

    /**
     * Makes a call to the AgentFactory smart contract using the `calculate_buy_price(uint64,uint64)uint64` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
    * Calculates ALGO required to buy 'amount' of tokens along the curve.
    Price = integral of (BASE_PRICE + SLOPE * supply) from S to S+amount.

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    calculateBuyPrice: (params: CallParams<AgentFactoryArgs['obj']['calculate_buy_price(uint64,uint64)uint64'] | AgentFactoryArgs['tuple']['calculate_buy_price(uint64,uint64)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(AgentFactoryParamsFactory.calculateBuyPrice(params))
    },

    /**
     * Makes a call to the AgentFactory smart contract using the `buy_tokens(pay,uint64,uint64)void` ABI method.
     *
    * Buys tokens from the bonding curve by paying ALGO.
    A buy fee (default 1%) is added on top of the curve price.

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    buyTokens: (params: CallParams<AgentFactoryArgs['obj']['buy_tokens(pay,uint64,uint64)void'] | AgentFactoryArgs['tuple']['buy_tokens(pay,uint64,uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(AgentFactoryParamsFactory.buyTokens(params))
    },

    /**
     * Makes a call to the AgentFactory smart contract using the `calculate_sell_price(uint64,uint64)uint64` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
    * Calculate ALGO returned for selling 'amount' tokens.
    Integral of curve from (supply - amount) to supply, minus sell fee.

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    calculateSellPrice: (params: CallParams<AgentFactoryArgs['obj']['calculate_sell_price(uint64,uint64)uint64'] | AgentFactoryArgs['tuple']['calculate_sell_price(uint64,uint64)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(AgentFactoryParamsFactory.calculateSellPrice(params))
    },

    /**
     * Makes a call to the AgentFactory smart contract using the `sell_tokens(axfer,uint64,uint64)void` ABI method.
     *
    * Sells tokens back to the bonding curve for ALGO.
    The seller must send an asset transfer of the agent token to the contract in the same group transaction.

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    sellTokens: (params: CallParams<AgentFactoryArgs['obj']['sell_tokens(axfer,uint64,uint64)void'] | AgentFactoryArgs['tuple']['sell_tokens(axfer,uint64,uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(AgentFactoryParamsFactory.sellTokens(params))
    },

    /**
     * Makes a call to the AgentFactory smart contract using the `check_graduation(uint64)bool` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
    * Check if an agent token has met the graduation threshold.
    Graduation means the total value locked in the bonding curve exceeds the configured threshold, signaling readiness for DEX listing.

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    checkGraduation: (params: CallParams<AgentFactoryArgs['obj']['check_graduation(uint64)bool'] | AgentFactoryArgs['tuple']['check_graduation(uint64)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(AgentFactoryParamsFactory.checkGraduation(params))
    },

    /**
     * Makes a call to the AgentFactory smart contract using the `distribute_cortex(address,uint64)void` ABI method.
     *
    * Distribute CORTEX tokens from the factory escrow.
    Only callable by the application creator. Used for testnet distribution and genesis airdrop.

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    distributeCortex: (params: CallParams<AgentFactoryArgs['obj']['distribute_cortex(address,uint64)void'] | AgentFactoryArgs['tuple']['distribute_cortex(address,uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(AgentFactoryParamsFactory.distributeCortex(params))
    },

    /**
     * Makes a call to the AgentFactory smart contract using the `update_fee_parameters(uint64,uint64)void` ABI method.
     *
    * Update fee parameters.
    Only callable by the application creator (later: governance contract). Fees are in basis points (100 = 1%). Max 10%.

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    updateFeeParameters: (params: CallParams<AgentFactoryArgs['obj']['update_fee_parameters(uint64,uint64)void'] | AgentFactoryArgs['tuple']['update_fee_parameters(uint64,uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(AgentFactoryParamsFactory.updateFeeParameters(params))
    },

    /**
     * Makes a call to the AgentFactory smart contract using the `update_creation_fee(uint64)void` ABI method.
     *
    * Update agent creation fee (in CORTEX micro-units).
    Only callable by the application creator.

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    updateCreationFee: (params: CallParams<AgentFactoryArgs['obj']['update_creation_fee(uint64)void'] | AgentFactoryArgs['tuple']['update_creation_fee(uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(AgentFactoryParamsFactory.updateCreationFee(params))
    },

    /**
     * Makes a call to the AgentFactory smart contract using the `update_graduation_threshold(uint64)void` ABI method.
     *
    * Update the graduation threshold.
    Only callable by the application creator.

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    updateGraduationThreshold: (params: CallParams<AgentFactoryArgs['obj']['update_graduation_threshold(uint64)void'] | AgentFactoryArgs['tuple']['update_graduation_threshold(uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(AgentFactoryParamsFactory.updateGraduationThreshold(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Makes a clear_state call to an existing instance of the AgentFactory smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the AgentFactory smart contract using the `bootstrap_protocol()uint64` ABI method.
     *
    * Creates the master $CORTEX utility token.
    Only callable by the application creator.

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    bootstrapProtocol: (params: CallParams<AgentFactoryArgs['obj']['bootstrap_protocol()uint64'] | AgentFactoryArgs['tuple']['bootstrap_protocol()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(AgentFactoryParamsFactory.bootstrapProtocol(params))
    },

    /**
     * Makes a call to the AgentFactory smart contract using the `create_agent(axfer,string,string)uint64` ABI method.
     *
     * Creates a new Agent Token. Requires a CORTEX fee (default 100).
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    createAgent: (params: CallParams<AgentFactoryArgs['obj']['create_agent(axfer,string,string)uint64'] | AgentFactoryArgs['tuple']['create_agent(axfer,string,string)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(AgentFactoryParamsFactory.createAgent(params))
    },

    /**
     * Makes a call to the AgentFactory smart contract using the `calculate_buy_price(uint64,uint64)uint64` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
    * Calculates ALGO required to buy 'amount' of tokens along the curve.
    Price = integral of (BASE_PRICE + SLOPE * supply) from S to S+amount.

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    calculateBuyPrice: (params: CallParams<AgentFactoryArgs['obj']['calculate_buy_price(uint64,uint64)uint64'] | AgentFactoryArgs['tuple']['calculate_buy_price(uint64,uint64)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(AgentFactoryParamsFactory.calculateBuyPrice(params))
    },

    /**
     * Makes a call to the AgentFactory smart contract using the `buy_tokens(pay,uint64,uint64)void` ABI method.
     *
    * Buys tokens from the bonding curve by paying ALGO.
    A buy fee (default 1%) is added on top of the curve price.

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    buyTokens: (params: CallParams<AgentFactoryArgs['obj']['buy_tokens(pay,uint64,uint64)void'] | AgentFactoryArgs['tuple']['buy_tokens(pay,uint64,uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(AgentFactoryParamsFactory.buyTokens(params))
    },

    /**
     * Makes a call to the AgentFactory smart contract using the `calculate_sell_price(uint64,uint64)uint64` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
    * Calculate ALGO returned for selling 'amount' tokens.
    Integral of curve from (supply - amount) to supply, minus sell fee.

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    calculateSellPrice: (params: CallParams<AgentFactoryArgs['obj']['calculate_sell_price(uint64,uint64)uint64'] | AgentFactoryArgs['tuple']['calculate_sell_price(uint64,uint64)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(AgentFactoryParamsFactory.calculateSellPrice(params))
    },

    /**
     * Makes a call to the AgentFactory smart contract using the `sell_tokens(axfer,uint64,uint64)void` ABI method.
     *
    * Sells tokens back to the bonding curve for ALGO.
    The seller must send an asset transfer of the agent token to the contract in the same group transaction.

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    sellTokens: (params: CallParams<AgentFactoryArgs['obj']['sell_tokens(axfer,uint64,uint64)void'] | AgentFactoryArgs['tuple']['sell_tokens(axfer,uint64,uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(AgentFactoryParamsFactory.sellTokens(params))
    },

    /**
     * Makes a call to the AgentFactory smart contract using the `check_graduation(uint64)bool` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
    * Check if an agent token has met the graduation threshold.
    Graduation means the total value locked in the bonding curve exceeds the configured threshold, signaling readiness for DEX listing.

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    checkGraduation: (params: CallParams<AgentFactoryArgs['obj']['check_graduation(uint64)bool'] | AgentFactoryArgs['tuple']['check_graduation(uint64)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(AgentFactoryParamsFactory.checkGraduation(params))
    },

    /**
     * Makes a call to the AgentFactory smart contract using the `distribute_cortex(address,uint64)void` ABI method.
     *
    * Distribute CORTEX tokens from the factory escrow.
    Only callable by the application creator. Used for testnet distribution and genesis airdrop.

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    distributeCortex: (params: CallParams<AgentFactoryArgs['obj']['distribute_cortex(address,uint64)void'] | AgentFactoryArgs['tuple']['distribute_cortex(address,uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(AgentFactoryParamsFactory.distributeCortex(params))
    },

    /**
     * Makes a call to the AgentFactory smart contract using the `update_fee_parameters(uint64,uint64)void` ABI method.
     *
    * Update fee parameters.
    Only callable by the application creator (later: governance contract). Fees are in basis points (100 = 1%). Max 10%.

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    updateFeeParameters: (params: CallParams<AgentFactoryArgs['obj']['update_fee_parameters(uint64,uint64)void'] | AgentFactoryArgs['tuple']['update_fee_parameters(uint64,uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(AgentFactoryParamsFactory.updateFeeParameters(params))
    },

    /**
     * Makes a call to the AgentFactory smart contract using the `update_creation_fee(uint64)void` ABI method.
     *
    * Update agent creation fee (in CORTEX micro-units).
    Only callable by the application creator.

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    updateCreationFee: (params: CallParams<AgentFactoryArgs['obj']['update_creation_fee(uint64)void'] | AgentFactoryArgs['tuple']['update_creation_fee(uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(AgentFactoryParamsFactory.updateCreationFee(params))
    },

    /**
     * Makes a call to the AgentFactory smart contract using the `update_graduation_threshold(uint64)void` ABI method.
     *
    * Update the graduation threshold.
    Only callable by the application creator.

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    updateGraduationThreshold: (params: CallParams<AgentFactoryArgs['obj']['update_graduation_threshold(uint64)void'] | AgentFactoryArgs['tuple']['update_graduation_threshold(uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(AgentFactoryParamsFactory.updateGraduationThreshold(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Makes a clear_state call to an existing instance of the AgentFactory smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the AgentFactory smart contract using the `bootstrap_protocol()uint64` ABI method.
     *
    * Creates the master $CORTEX utility token.
    Only callable by the application creator.

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    bootstrapProtocol: async (params: CallParams<AgentFactoryArgs['obj']['bootstrap_protocol()uint64'] | AgentFactoryArgs['tuple']['bootstrap_protocol()uint64']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(AgentFactoryParamsFactory.bootstrapProtocol(params))
      return {...result, return: result.return as unknown as (undefined | AgentFactoryReturns['bootstrap_protocol()uint64'])}
    },

    /**
     * Makes a call to the AgentFactory smart contract using the `create_agent(axfer,string,string)uint64` ABI method.
     *
     * Creates a new Agent Token. Requires a CORTEX fee (default 100).
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    createAgent: async (params: CallParams<AgentFactoryArgs['obj']['create_agent(axfer,string,string)uint64'] | AgentFactoryArgs['tuple']['create_agent(axfer,string,string)uint64']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(AgentFactoryParamsFactory.createAgent(params))
      return {...result, return: result.return as unknown as (undefined | AgentFactoryReturns['create_agent(axfer,string,string)uint64'])}
    },

    /**
     * Makes a call to the AgentFactory smart contract using the `calculate_buy_price(uint64,uint64)uint64` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
    * Calculates ALGO required to buy 'amount' of tokens along the curve.
    Price = integral of (BASE_PRICE + SLOPE * supply) from S to S+amount.

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    calculateBuyPrice: async (params: CallParams<AgentFactoryArgs['obj']['calculate_buy_price(uint64,uint64)uint64'] | AgentFactoryArgs['tuple']['calculate_buy_price(uint64,uint64)uint64']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(AgentFactoryParamsFactory.calculateBuyPrice(params))
      return {...result, return: result.return as unknown as (undefined | AgentFactoryReturns['calculate_buy_price(uint64,uint64)uint64'])}
    },

    /**
     * Makes a call to the AgentFactory smart contract using the `buy_tokens(pay,uint64,uint64)void` ABI method.
     *
    * Buys tokens from the bonding curve by paying ALGO.
    A buy fee (default 1%) is added on top of the curve price.

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    buyTokens: async (params: CallParams<AgentFactoryArgs['obj']['buy_tokens(pay,uint64,uint64)void'] | AgentFactoryArgs['tuple']['buy_tokens(pay,uint64,uint64)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(AgentFactoryParamsFactory.buyTokens(params))
      return {...result, return: result.return as unknown as (undefined | AgentFactoryReturns['buy_tokens(pay,uint64,uint64)void'])}
    },

    /**
     * Makes a call to the AgentFactory smart contract using the `calculate_sell_price(uint64,uint64)uint64` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
    * Calculate ALGO returned for selling 'amount' tokens.
    Integral of curve from (supply - amount) to supply, minus sell fee.

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    calculateSellPrice: async (params: CallParams<AgentFactoryArgs['obj']['calculate_sell_price(uint64,uint64)uint64'] | AgentFactoryArgs['tuple']['calculate_sell_price(uint64,uint64)uint64']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(AgentFactoryParamsFactory.calculateSellPrice(params))
      return {...result, return: result.return as unknown as (undefined | AgentFactoryReturns['calculate_sell_price(uint64,uint64)uint64'])}
    },

    /**
     * Makes a call to the AgentFactory smart contract using the `sell_tokens(axfer,uint64,uint64)void` ABI method.
     *
    * Sells tokens back to the bonding curve for ALGO.
    The seller must send an asset transfer of the agent token to the contract in the same group transaction.

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    sellTokens: async (params: CallParams<AgentFactoryArgs['obj']['sell_tokens(axfer,uint64,uint64)void'] | AgentFactoryArgs['tuple']['sell_tokens(axfer,uint64,uint64)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(AgentFactoryParamsFactory.sellTokens(params))
      return {...result, return: result.return as unknown as (undefined | AgentFactoryReturns['sell_tokens(axfer,uint64,uint64)void'])}
    },

    /**
     * Makes a call to the AgentFactory smart contract using the `check_graduation(uint64)bool` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
    * Check if an agent token has met the graduation threshold.
    Graduation means the total value locked in the bonding curve exceeds the configured threshold, signaling readiness for DEX listing.

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    checkGraduation: async (params: CallParams<AgentFactoryArgs['obj']['check_graduation(uint64)bool'] | AgentFactoryArgs['tuple']['check_graduation(uint64)bool']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(AgentFactoryParamsFactory.checkGraduation(params))
      return {...result, return: result.return as unknown as (undefined | AgentFactoryReturns['check_graduation(uint64)bool'])}
    },

    /**
     * Makes a call to the AgentFactory smart contract using the `distribute_cortex(address,uint64)void` ABI method.
     *
    * Distribute CORTEX tokens from the factory escrow.
    Only callable by the application creator. Used for testnet distribution and genesis airdrop.

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    distributeCortex: async (params: CallParams<AgentFactoryArgs['obj']['distribute_cortex(address,uint64)void'] | AgentFactoryArgs['tuple']['distribute_cortex(address,uint64)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(AgentFactoryParamsFactory.distributeCortex(params))
      return {...result, return: result.return as unknown as (undefined | AgentFactoryReturns['distribute_cortex(address,uint64)void'])}
    },

    /**
     * Makes a call to the AgentFactory smart contract using the `update_fee_parameters(uint64,uint64)void` ABI method.
     *
    * Update fee parameters.
    Only callable by the application creator (later: governance contract). Fees are in basis points (100 = 1%). Max 10%.

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    updateFeeParameters: async (params: CallParams<AgentFactoryArgs['obj']['update_fee_parameters(uint64,uint64)void'] | AgentFactoryArgs['tuple']['update_fee_parameters(uint64,uint64)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(AgentFactoryParamsFactory.updateFeeParameters(params))
      return {...result, return: result.return as unknown as (undefined | AgentFactoryReturns['update_fee_parameters(uint64,uint64)void'])}
    },

    /**
     * Makes a call to the AgentFactory smart contract using the `update_creation_fee(uint64)void` ABI method.
     *
    * Update agent creation fee (in CORTEX micro-units).
    Only callable by the application creator.

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    updateCreationFee: async (params: CallParams<AgentFactoryArgs['obj']['update_creation_fee(uint64)void'] | AgentFactoryArgs['tuple']['update_creation_fee(uint64)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(AgentFactoryParamsFactory.updateCreationFee(params))
      return {...result, return: result.return as unknown as (undefined | AgentFactoryReturns['update_creation_fee(uint64)void'])}
    },

    /**
     * Makes a call to the AgentFactory smart contract using the `update_graduation_threshold(uint64)void` ABI method.
     *
    * Update the graduation threshold.
    Only callable by the application creator.

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    updateGraduationThreshold: async (params: CallParams<AgentFactoryArgs['obj']['update_graduation_threshold(uint64)void'] | AgentFactoryArgs['tuple']['update_graduation_threshold(uint64)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(AgentFactoryParamsFactory.updateGraduationThreshold(params))
      return {...result, return: result.return as unknown as (undefined | AgentFactoryReturns['update_graduation_threshold(uint64)void'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new AgentFactoryClient(this.appClient.clone(params))
  }

  /**
   * Makes a readonly (simulated) call to the AgentFactory smart contract using the `calculate_buy_price(uint64,uint64)uint64` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
  * Calculates ALGO required to buy 'amount' of tokens along the curve.
  Price = integral of (BASE_PRICE + SLOPE * supply) from S to S+amount.

   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async calculateBuyPrice(params: CallParams<AgentFactoryArgs['obj']['calculate_buy_price(uint64,uint64)uint64'] | AgentFactoryArgs['tuple']['calculate_buy_price(uint64,uint64)uint64']>) {
    const result = await this.appClient.send.call(AgentFactoryParamsFactory.calculateBuyPrice(params))
    return result.return as unknown as AgentFactoryReturns['calculate_buy_price(uint64,uint64)uint64']
  }

  /**
   * Makes a readonly (simulated) call to the AgentFactory smart contract using the `calculate_sell_price(uint64,uint64)uint64` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
  * Calculate ALGO returned for selling 'amount' tokens.
  Integral of curve from (supply - amount) to supply, minus sell fee.

   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async calculateSellPrice(params: CallParams<AgentFactoryArgs['obj']['calculate_sell_price(uint64,uint64)uint64'] | AgentFactoryArgs['tuple']['calculate_sell_price(uint64,uint64)uint64']>) {
    const result = await this.appClient.send.call(AgentFactoryParamsFactory.calculateSellPrice(params))
    return result.return as unknown as AgentFactoryReturns['calculate_sell_price(uint64,uint64)uint64']
  }

  /**
   * Makes a readonly (simulated) call to the AgentFactory smart contract using the `check_graduation(uint64)bool` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
  * Check if an agent token has met the graduation threshold.
  Graduation means the total value locked in the bonding curve exceeds the configured threshold, signaling readiness for DEX listing.

   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async checkGraduation(params: CallParams<AgentFactoryArgs['obj']['check_graduation(uint64)bool'] | AgentFactoryArgs['tuple']['check_graduation(uint64)bool']>) {
    const result = await this.appClient.send.call(AgentFactoryParamsFactory.checkGraduation(params))
    return result.return as unknown as AgentFactoryReturns['check_graduation(uint64)bool']
  }

  /**
   * Methods to access state for the current AgentFactory app
   */
  state = {
    /**
     * Methods to access global state for the current AgentFactory app
     */
    global: {
      /**
       * Get all current keyed values from global state
       */
      getAll: async (): Promise<Partial<Expand<GlobalKeysState>>> => {
        const result = await this.appClient.state.global.getAll()
        return {
          latestAssetId: result.latest_asset_id,
          cortexToken: result.cortex_token,
          basePrice: result.BASE_PRICE,
          slope: result.SLOPE,
          buyFeeBps: result.buy_fee_bps,
          sellFeeBps: result.sell_fee_bps,
          creationFee: result.creation_fee,
          graduationThreshold: result.GRADUATION_THRESHOLD,
        }
      },
      /**
       * Get the current value of the latest_asset_id key in global state
       */
      latestAssetId: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("latest_asset_id")) as bigint | undefined },
      /**
       * Get the current value of the cortex_token key in global state
       */
      cortexToken: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("cortex_token")) as bigint | undefined },
      /**
       * Get the current value of the BASE_PRICE key in global state
       */
      basePrice: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("BASE_PRICE")) as bigint | undefined },
      /**
       * Get the current value of the SLOPE key in global state
       */
      slope: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("SLOPE")) as bigint | undefined },
      /**
       * Get the current value of the buy_fee_bps key in global state
       */
      buyFeeBps: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("buy_fee_bps")) as bigint | undefined },
      /**
       * Get the current value of the sell_fee_bps key in global state
       */
      sellFeeBps: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("sell_fee_bps")) as bigint | undefined },
      /**
       * Get the current value of the creation_fee key in global state
       */
      creationFee: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("creation_fee")) as bigint | undefined },
      /**
       * Get the current value of the GRADUATION_THRESHOLD key in global state
       */
      graduationThreshold: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("GRADUATION_THRESHOLD")) as bigint | undefined },
    },
    /**
     * Methods to access box state for the current AgentFactory app
     */
    box: {
      /**
       * Get all current keyed values from box state
       */
      getAll: async (): Promise<Partial<Expand<BoxKeysState>>> => {
        const result = await this.appClient.state.box.getAll()
        return {
        }
      },
      /**
       * Get values from the agent_supplies map in box state
       */
      agentSupplies: {
        /**
         * Get all current values of the agent_supplies map in box state
         */
        getMap: async (): Promise<Map<bigint, bigint>> => { return (await this.appClient.state.box.getMap("agent_supplies")) as Map<bigint, bigint> },
        /**
         * Get a current value of the agent_supplies map by key from box state
         */
        value: async (key: bigint | number): Promise<bigint | undefined> => { return await this.appClient.state.box.getMapValue("agent_supplies", key) as bigint | undefined },
      },
    },
  }

  public newGroup(): AgentFactoryComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a bootstrap_protocol()uint64 method call against the AgentFactory contract
       */
      bootstrapProtocol(params: CallParams<AgentFactoryArgs['obj']['bootstrap_protocol()uint64'] | AgentFactoryArgs['tuple']['bootstrap_protocol()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.bootstrapProtocol(params)))
        resultMappers.push((v) => client.decodeReturnValue('bootstrap_protocol()uint64', v))
        return this
      },
      /**
       * Add a create_agent(axfer,string,string)uint64 method call against the AgentFactory contract
       */
      createAgent(params: CallParams<AgentFactoryArgs['obj']['create_agent(axfer,string,string)uint64'] | AgentFactoryArgs['tuple']['create_agent(axfer,string,string)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.createAgent(params)))
        resultMappers.push((v) => client.decodeReturnValue('create_agent(axfer,string,string)uint64', v))
        return this
      },
      /**
       * Add a calculate_buy_price(uint64,uint64)uint64 method call against the AgentFactory contract
       */
      calculateBuyPrice(params: CallParams<AgentFactoryArgs['obj']['calculate_buy_price(uint64,uint64)uint64'] | AgentFactoryArgs['tuple']['calculate_buy_price(uint64,uint64)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.calculateBuyPrice(params)))
        resultMappers.push((v) => client.decodeReturnValue('calculate_buy_price(uint64,uint64)uint64', v))
        return this
      },
      /**
       * Add a buy_tokens(pay,uint64,uint64)void method call against the AgentFactory contract
       */
      buyTokens(params: CallParams<AgentFactoryArgs['obj']['buy_tokens(pay,uint64,uint64)void'] | AgentFactoryArgs['tuple']['buy_tokens(pay,uint64,uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.buyTokens(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a calculate_sell_price(uint64,uint64)uint64 method call against the AgentFactory contract
       */
      calculateSellPrice(params: CallParams<AgentFactoryArgs['obj']['calculate_sell_price(uint64,uint64)uint64'] | AgentFactoryArgs['tuple']['calculate_sell_price(uint64,uint64)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.calculateSellPrice(params)))
        resultMappers.push((v) => client.decodeReturnValue('calculate_sell_price(uint64,uint64)uint64', v))
        return this
      },
      /**
       * Add a sell_tokens(axfer,uint64,uint64)void method call against the AgentFactory contract
       */
      sellTokens(params: CallParams<AgentFactoryArgs['obj']['sell_tokens(axfer,uint64,uint64)void'] | AgentFactoryArgs['tuple']['sell_tokens(axfer,uint64,uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.sellTokens(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a check_graduation(uint64)bool method call against the AgentFactory contract
       */
      checkGraduation(params: CallParams<AgentFactoryArgs['obj']['check_graduation(uint64)bool'] | AgentFactoryArgs['tuple']['check_graduation(uint64)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.checkGraduation(params)))
        resultMappers.push((v) => client.decodeReturnValue('check_graduation(uint64)bool', v))
        return this
      },
      /**
       * Add a distribute_cortex(address,uint64)void method call against the AgentFactory contract
       */
      distributeCortex(params: CallParams<AgentFactoryArgs['obj']['distribute_cortex(address,uint64)void'] | AgentFactoryArgs['tuple']['distribute_cortex(address,uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.distributeCortex(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a update_fee_parameters(uint64,uint64)void method call against the AgentFactory contract
       */
      updateFeeParameters(params: CallParams<AgentFactoryArgs['obj']['update_fee_parameters(uint64,uint64)void'] | AgentFactoryArgs['tuple']['update_fee_parameters(uint64,uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.updateFeeParameters(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a update_creation_fee(uint64)void method call against the AgentFactory contract
       */
      updateCreationFee(params: CallParams<AgentFactoryArgs['obj']['update_creation_fee(uint64)void'] | AgentFactoryArgs['tuple']['update_creation_fee(uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.updateCreationFee(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a update_graduation_threshold(uint64)void method call against the AgentFactory contract
       */
      updateGraduationThreshold(params: CallParams<AgentFactoryArgs['obj']['update_graduation_threshold(uint64)void'] | AgentFactoryArgs['tuple']['update_graduation_threshold(uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.updateGraduationThreshold(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a clear state call to the AgentFactory contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as AgentFactoryComposer
  }
}
export type AgentFactoryComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the bootstrap_protocol()uint64 ABI method.
   *
  * Creates the master $CORTEX utility token.
  Only callable by the application creator.

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  bootstrapProtocol(params?: CallParams<AgentFactoryArgs['obj']['bootstrap_protocol()uint64'] | AgentFactoryArgs['tuple']['bootstrap_protocol()uint64']>): AgentFactoryComposer<[...TReturns, AgentFactoryReturns['bootstrap_protocol()uint64'] | undefined]>

  /**
   * Calls the create_agent(axfer,string,string)uint64 ABI method.
   *
   * Creates a new Agent Token. Requires a CORTEX fee (default 100).
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  createAgent(params?: CallParams<AgentFactoryArgs['obj']['create_agent(axfer,string,string)uint64'] | AgentFactoryArgs['tuple']['create_agent(axfer,string,string)uint64']>): AgentFactoryComposer<[...TReturns, AgentFactoryReturns['create_agent(axfer,string,string)uint64'] | undefined]>

  /**
   * Calls the calculate_buy_price(uint64,uint64)uint64 ABI method.
   *
  * Calculates ALGO required to buy 'amount' of tokens along the curve.
  Price = integral of (BASE_PRICE + SLOPE * supply) from S to S+amount.

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  calculateBuyPrice(params?: CallParams<AgentFactoryArgs['obj']['calculate_buy_price(uint64,uint64)uint64'] | AgentFactoryArgs['tuple']['calculate_buy_price(uint64,uint64)uint64']>): AgentFactoryComposer<[...TReturns, AgentFactoryReturns['calculate_buy_price(uint64,uint64)uint64'] | undefined]>

  /**
   * Calls the buy_tokens(pay,uint64,uint64)void ABI method.
   *
  * Buys tokens from the bonding curve by paying ALGO.
  A buy fee (default 1%) is added on top of the curve price.

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  buyTokens(params?: CallParams<AgentFactoryArgs['obj']['buy_tokens(pay,uint64,uint64)void'] | AgentFactoryArgs['tuple']['buy_tokens(pay,uint64,uint64)void']>): AgentFactoryComposer<[...TReturns, AgentFactoryReturns['buy_tokens(pay,uint64,uint64)void'] | undefined]>

  /**
   * Calls the calculate_sell_price(uint64,uint64)uint64 ABI method.
   *
  * Calculate ALGO returned for selling 'amount' tokens.
  Integral of curve from (supply - amount) to supply, minus sell fee.

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  calculateSellPrice(params?: CallParams<AgentFactoryArgs['obj']['calculate_sell_price(uint64,uint64)uint64'] | AgentFactoryArgs['tuple']['calculate_sell_price(uint64,uint64)uint64']>): AgentFactoryComposer<[...TReturns, AgentFactoryReturns['calculate_sell_price(uint64,uint64)uint64'] | undefined]>

  /**
   * Calls the sell_tokens(axfer,uint64,uint64)void ABI method.
   *
  * Sells tokens back to the bonding curve for ALGO.
  The seller must send an asset transfer of the agent token to the contract in the same group transaction.

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  sellTokens(params?: CallParams<AgentFactoryArgs['obj']['sell_tokens(axfer,uint64,uint64)void'] | AgentFactoryArgs['tuple']['sell_tokens(axfer,uint64,uint64)void']>): AgentFactoryComposer<[...TReturns, AgentFactoryReturns['sell_tokens(axfer,uint64,uint64)void'] | undefined]>

  /**
   * Calls the check_graduation(uint64)bool ABI method.
   *
  * Check if an agent token has met the graduation threshold.
  Graduation means the total value locked in the bonding curve exceeds the configured threshold, signaling readiness for DEX listing.

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  checkGraduation(params?: CallParams<AgentFactoryArgs['obj']['check_graduation(uint64)bool'] | AgentFactoryArgs['tuple']['check_graduation(uint64)bool']>): AgentFactoryComposer<[...TReturns, AgentFactoryReturns['check_graduation(uint64)bool'] | undefined]>

  /**
   * Calls the distribute_cortex(address,uint64)void ABI method.
   *
  * Distribute CORTEX tokens from the factory escrow.
  Only callable by the application creator. Used for testnet distribution and genesis airdrop.

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  distributeCortex(params?: CallParams<AgentFactoryArgs['obj']['distribute_cortex(address,uint64)void'] | AgentFactoryArgs['tuple']['distribute_cortex(address,uint64)void']>): AgentFactoryComposer<[...TReturns, AgentFactoryReturns['distribute_cortex(address,uint64)void'] | undefined]>

  /**
   * Calls the update_fee_parameters(uint64,uint64)void ABI method.
   *
  * Update fee parameters.
  Only callable by the application creator (later: governance contract). Fees are in basis points (100 = 1%). Max 10%.

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  updateFeeParameters(params?: CallParams<AgentFactoryArgs['obj']['update_fee_parameters(uint64,uint64)void'] | AgentFactoryArgs['tuple']['update_fee_parameters(uint64,uint64)void']>): AgentFactoryComposer<[...TReturns, AgentFactoryReturns['update_fee_parameters(uint64,uint64)void'] | undefined]>

  /**
   * Calls the update_creation_fee(uint64)void ABI method.
   *
  * Update agent creation fee (in CORTEX micro-units).
  Only callable by the application creator.

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  updateCreationFee(params?: CallParams<AgentFactoryArgs['obj']['update_creation_fee(uint64)void'] | AgentFactoryArgs['tuple']['update_creation_fee(uint64)void']>): AgentFactoryComposer<[...TReturns, AgentFactoryReturns['update_creation_fee(uint64)void'] | undefined]>

  /**
   * Calls the update_graduation_threshold(uint64)void ABI method.
   *
  * Update the graduation threshold.
  Only callable by the application creator.

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  updateGraduationThreshold(params?: CallParams<AgentFactoryArgs['obj']['update_graduation_threshold(uint64)void'] | AgentFactoryArgs['tuple']['update_graduation_threshold(uint64)void']>): AgentFactoryComposer<[...TReturns, AgentFactoryReturns['update_graduation_threshold(uint64)void'] | undefined]>

  /**
   * Makes a clear_state call to an existing instance of the AgentFactory smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): AgentFactoryComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): AgentFactoryComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<AgentFactoryComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<AgentFactoryComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<AgentFactoryComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<AgentFactoryComposerResults<TReturns>>
}
export type AgentFactoryComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

