/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"VeCortexStaking","structs":{},"methods":[{"name":"initialize","args":[{"type":"uint64","name":"cortex_asset"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Initialize the staking contract with the CORTEX token reference.\nMust opt-in to the asset so the contract can hold and return tokens. Only callable by the application creator.","events":[],"recommendations":{}},{"name":"stake","args":[{"type":"axfer","name":"cortex_transfer"},{"type":"uint64","name":"lock_days"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Stake CORTEX tokens with a specified lock duration.\nThe caller must include an asset transfer of CORTEX to this contract in the same group transaction. Lock duration determines the veCORTEX boost multiplier (1x at 7 days, up to 2.5x at 4 years).","events":[],"recommendations":{}},{"name":"unstake","args":[],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Unstake CORTEX tokens after the lock period has expired.\nReturns the original staked amount to the sender. Removes the stake record and any active delegation.","events":[],"recommendations":{}},{"name":"delegate","args":[{"type":"address","name":"lawmaker"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Delegate veCORTEX voting power to a Lawmaker agent address.\nThe delegatee can then vote on governance proposals on your behalf.","events":[],"recommendations":{}},{"name":"revoke_delegation","args":[],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Revoke an existing delegation.\nTakes effect in the next governance epoch.","events":[],"recommendations":{}},{"name":"get_ve_power","args":[{"type":"address","name":"account"}],"returns":{"type":"uint64"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"desc":"Get the veCORTEX voting power for an account.","events":[],"recommendations":{}},{"name":"get_stake_info","args":[{"type":"address","name":"account"}],"returns":{"type":"byte[]"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"desc":"Get the full stake info for an account.\nReturns 32 bytes: amount(8) + unlock_round(8) + ve_power(8) + boost(8). Returns empty bytes if no stake exists.","events":[],"recommendations":{}},{"name":"get_delegate","args":[{"type":"address","name":"account"}],"returns":{"type":"byte[]"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"desc":"Get the delegate address for an account.\nReturns 32-byte address or empty bytes if no delegation.","events":[],"recommendations":{}},{"name":"get_total_staked","args":[],"returns":{"type":"uint64"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"desc":"Get the total CORTEX currently staked across all users.","events":[],"recommendations":{}},{"name":"fund_reward_pool","args":[{"type":"axfer","name":"cortex_transfer"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Add CORTEX to the staking reward pool.\nTypically called by the protocol to distribute the 24% emission allocation.","events":[],"recommendations":{}},{"name":"update_lock_parameters","args":[{"type":"uint64","name":"min_days"},{"type":"uint64","name":"max_days"},{"type":"uint64","name":"max_boost"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Update staking lock parameters. Only callable by creator.\nmax_boost is in basis points (2500 = 2.5x).","events":[],"recommendations":{}}],"arcs":[22,28],"desc":"\n    veCORTEX staking contract.\n\n    Users lock CORTEX tokens for a configurable duration (1 week to 4 years)\n    and receive veCORTEX voting power with a time-weighted boost up to 2.5x.\n\n    Features:\n    - Time-locked staking with linear boost scaling\n    - Delegation of voting power to Lawmaker agents\n    - Reward pool tracking for the 24% emission allocation\n    - Clean unstake after lock expiry\n    ","networks":{},"state":{"schema":{"global":{"ints":7,"bytes":0},"local":{"ints":0,"bytes":0}},"keys":{"global":{"cortex_token":{"keyType":"AVMString","valueType":"AVMUint64","key":"Y29ydGV4X3Rva2Vu"},"total_staked":{"keyType":"AVMString","valueType":"AVMUint64","key":"dG90YWxfc3Rha2Vk"},"reward_pool":{"keyType":"AVMString","valueType":"AVMUint64","key":"cmV3YXJkX3Bvb2w="},"MIN_LOCK_DAYS":{"keyType":"AVMString","valueType":"AVMUint64","key":"TUlOX0xPQ0tfREFZUw=="},"MAX_LOCK_DAYS":{"keyType":"AVMString","valueType":"AVMUint64","key":"TUFYX0xPQ0tfREFZUw=="},"MAX_BOOST":{"keyType":"AVMString","valueType":"AVMUint64","key":"TUFYX0JPT1NU"},"ROUNDS_PER_DAY":{"keyType":"AVMString","valueType":"AVMUint64","key":"Uk9VTkRTX1BFUl9EQVk="}},"local":{},"box":{}},"maps":{"global":{},"local":{},"box":{"stakes":{"keyType":"AVMBytes","valueType":"AVMBytes","prefix":"c3Rha2Vz"},"delegations":{"keyType":"AVMBytes","valueType":"AVMBytes","prefix":"ZGVsZWdhdGlvbnM="}}}},"bareActions":{"create":["NoOp"],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[289],"errorMessage":"Already initialized"},{"pc":[386],"errorMessage":"Already staking — unstake first"},{"pc":[829],"errorMessage":"Boost cannot exceed 10x"},{"pc":[823],"errorMessage":"Boost must be at least 1x"},{"pc":[499],"errorMessage":"Lock period not expired"},{"pc":[360],"errorMessage":"Lock too long"},{"pc":[352],"errorMessage":"Lock too short"},{"pc":[818],"errorMessage":"Max must exceed min"},{"pc":[812],"errorMessage":"Minimum 1 day"},{"pc":[368,761],"errorMessage":"Must send to contract"},{"pc":[376],"errorMessage":"Must stake a positive amount"},{"pc":[478,564,583],"errorMessage":"No active stake"},{"pc":[337],"errorMessage":"Not initialized"},{"pc":[283,745,809],"errorMessage":"Unauthorized"},{"pc":[343,754],"errorMessage":"Wrong token"},{"pc":[414,425],"errorMessage":"check self.MAX_BOOST exists"},{"pc":[357],"errorMessage":"check self.MAX_LOCK_DAYS exists"},{"pc":[348],"errorMessage":"check self.MIN_LOCK_DAYS exists"},{"pc":[393],"errorMessage":"check self.ROUNDS_PER_DAY exists"},{"pc":[287,335,504,752],"errorMessage":"check self.cortex_token exists"},{"pc":[714],"errorMessage":"check self.delegations entry exists"},{"pc":[766],"errorMessage":"check self.reward_pool exists"},{"pc":[485,626,671],"errorMessage":"check self.stakes entry exists"},{"pc":[464,540,721],"errorMessage":"check self.total_staked exists"},{"pc":[557,605,642,684],"errorMessage":"invalid number of bytes for arc4.static_array<arc4.uint8, 32>"},{"pc":[276,330,784,793,802],"errorMessage":"invalid number of bytes for arc4.uint64"},{"pc":[322,739],"errorMessage":"transaction type is axfer"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"events":[]} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


/**
 * The argument types for the VeCortexStaking contract
 */
export type VeCortexStakingArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'initialize(uint64)void': {
      cortexAsset: bigint | number
    }
    'stake(axfer,uint64)void': {
      cortexTransfer: AppMethodCallTransactionArgument
      lockDays: bigint | number
    }
    'unstake()void': Record<string, never>
    'delegate(address)void': {
      lawmaker: string
    }
    'revoke_delegation()void': Record<string, never>
    'get_ve_power(address)uint64': {
      account: string
    }
    'get_stake_info(address)byte[]': {
      account: string
    }
    'get_delegate(address)byte[]': {
      account: string
    }
    'get_total_staked()uint64': Record<string, never>
    'fund_reward_pool(axfer)void': {
      cortexTransfer: AppMethodCallTransactionArgument
    }
    'update_lock_parameters(uint64,uint64,uint64)void': {
      minDays: bigint | number
      maxDays: bigint | number
      maxBoost: bigint | number
    }
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'initialize(uint64)void': [cortexAsset: bigint | number]
    'stake(axfer,uint64)void': [cortexTransfer: AppMethodCallTransactionArgument, lockDays: bigint | number]
    'unstake()void': []
    'delegate(address)void': [lawmaker: string]
    'revoke_delegation()void': []
    'get_ve_power(address)uint64': [account: string]
    'get_stake_info(address)byte[]': [account: string]
    'get_delegate(address)byte[]': [account: string]
    'get_total_staked()uint64': []
    'fund_reward_pool(axfer)void': [cortexTransfer: AppMethodCallTransactionArgument]
    'update_lock_parameters(uint64,uint64,uint64)void': [minDays: bigint | number, maxDays: bigint | number, maxBoost: bigint | number]
  }
}

/**
 * The return type for each method
 */
export type VeCortexStakingReturns = {
  'initialize(uint64)void': void
  'stake(axfer,uint64)void': void
  'unstake()void': void
  'delegate(address)void': void
  'revoke_delegation()void': void
  'get_ve_power(address)uint64': bigint
  'get_stake_info(address)byte[]': Uint8Array
  'get_delegate(address)byte[]': Uint8Array
  'get_total_staked()uint64': bigint
  'fund_reward_pool(axfer)void': void
  'update_lock_parameters(uint64,uint64,uint64)void': void
}

/**
 * Defines the types of available calls and state of the VeCortexStaking smart contract.
 */
export type VeCortexStakingTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'initialize(uint64)void' | 'initialize', {
      argsObj: VeCortexStakingArgs['obj']['initialize(uint64)void']
      argsTuple: VeCortexStakingArgs['tuple']['initialize(uint64)void']
      returns: VeCortexStakingReturns['initialize(uint64)void']
    }>
    & Record<'stake(axfer,uint64)void' | 'stake', {
      argsObj: VeCortexStakingArgs['obj']['stake(axfer,uint64)void']
      argsTuple: VeCortexStakingArgs['tuple']['stake(axfer,uint64)void']
      returns: VeCortexStakingReturns['stake(axfer,uint64)void']
    }>
    & Record<'unstake()void' | 'unstake', {
      argsObj: VeCortexStakingArgs['obj']['unstake()void']
      argsTuple: VeCortexStakingArgs['tuple']['unstake()void']
      returns: VeCortexStakingReturns['unstake()void']
    }>
    & Record<'delegate(address)void' | 'delegate', {
      argsObj: VeCortexStakingArgs['obj']['delegate(address)void']
      argsTuple: VeCortexStakingArgs['tuple']['delegate(address)void']
      returns: VeCortexStakingReturns['delegate(address)void']
    }>
    & Record<'revoke_delegation()void' | 'revoke_delegation', {
      argsObj: VeCortexStakingArgs['obj']['revoke_delegation()void']
      argsTuple: VeCortexStakingArgs['tuple']['revoke_delegation()void']
      returns: VeCortexStakingReturns['revoke_delegation()void']
    }>
    & Record<'get_ve_power(address)uint64' | 'get_ve_power', {
      argsObj: VeCortexStakingArgs['obj']['get_ve_power(address)uint64']
      argsTuple: VeCortexStakingArgs['tuple']['get_ve_power(address)uint64']
      returns: VeCortexStakingReturns['get_ve_power(address)uint64']
    }>
    & Record<'get_stake_info(address)byte[]' | 'get_stake_info', {
      argsObj: VeCortexStakingArgs['obj']['get_stake_info(address)byte[]']
      argsTuple: VeCortexStakingArgs['tuple']['get_stake_info(address)byte[]']
      returns: VeCortexStakingReturns['get_stake_info(address)byte[]']
    }>
    & Record<'get_delegate(address)byte[]' | 'get_delegate', {
      argsObj: VeCortexStakingArgs['obj']['get_delegate(address)byte[]']
      argsTuple: VeCortexStakingArgs['tuple']['get_delegate(address)byte[]']
      returns: VeCortexStakingReturns['get_delegate(address)byte[]']
    }>
    & Record<'get_total_staked()uint64' | 'get_total_staked', {
      argsObj: VeCortexStakingArgs['obj']['get_total_staked()uint64']
      argsTuple: VeCortexStakingArgs['tuple']['get_total_staked()uint64']
      returns: VeCortexStakingReturns['get_total_staked()uint64']
    }>
    & Record<'fund_reward_pool(axfer)void' | 'fund_reward_pool', {
      argsObj: VeCortexStakingArgs['obj']['fund_reward_pool(axfer)void']
      argsTuple: VeCortexStakingArgs['tuple']['fund_reward_pool(axfer)void']
      returns: VeCortexStakingReturns['fund_reward_pool(axfer)void']
    }>
    & Record<'update_lock_parameters(uint64,uint64,uint64)void' | 'update_lock_parameters', {
      argsObj: VeCortexStakingArgs['obj']['update_lock_parameters(uint64,uint64,uint64)void']
      argsTuple: VeCortexStakingArgs['tuple']['update_lock_parameters(uint64,uint64,uint64)void']
      returns: VeCortexStakingReturns['update_lock_parameters(uint64,uint64,uint64)void']
    }>
  /**
   * Defines the shape of the state of the application.
   */
  state: {
    global: {
      keys: {
        cortexToken: bigint
        totalStaked: bigint
        rewardPool: bigint
        minLockDays: bigint
        maxLockDays: bigint
        maxBoost: bigint
        roundsPerDay: bigint
      }
      maps: {}
    }
    box: {
      keys: {}
      maps: {
        stakes: Map<Uint8Array | string, Uint8Array>
        delegations: Map<Uint8Array | string, Uint8Array>
      }
    }
  }
}

/**
 * Defines the possible abi call signatures.
 */
export type VeCortexStakingSignatures = keyof VeCortexStakingTypes['methods']
/**
 * Defines the possible abi call signatures for methods that return a non-void value.
 */
export type VeCortexStakingNonVoidMethodSignatures = keyof VeCortexStakingTypes['methods'] extends infer T ? T extends keyof VeCortexStakingTypes['methods'] ? MethodReturn<T> extends void ? never : T  : never : never
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the VeCortexStaking smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends VeCortexStakingSignatures> = VeCortexStakingTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the VeCortexStaking smart contract to the method's return type
 */
export type MethodReturn<TSignature extends VeCortexStakingSignatures> = VeCortexStakingTypes['methods'][TSignature]['returns']

/**
 * Defines the shape of the keyed global state of the application.
 */
export type GlobalKeysState = VeCortexStakingTypes['state']['global']['keys']

/**
 * Defines the shape of the keyed box state of the application.
 */
export type BoxKeysState = VeCortexStakingTypes['state']['box']['keys']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the VeCortexStaking smart contract
 */
export abstract class VeCortexStakingParamsFactory {
  /**
   * Constructs a no op call for the initialize(uint64)void ABI method
   *
  * Initialize the staking contract with the CORTEX token reference.
  Must opt-in to the asset so the contract can hold and return tokens. Only callable by the application creator.

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static initialize(params: CallParams<VeCortexStakingArgs['obj']['initialize(uint64)void'] | VeCortexStakingArgs['tuple']['initialize(uint64)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'initialize(uint64)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.cortexAsset],
    }
  }
  /**
   * Constructs a no op call for the stake(axfer,uint64)void ABI method
   *
  * Stake CORTEX tokens with a specified lock duration.
  The caller must include an asset transfer of CORTEX to this contract in the same group transaction. Lock duration determines the veCORTEX boost multiplier (1x at 7 days, up to 2.5x at 4 years).

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static stake(params: CallParams<VeCortexStakingArgs['obj']['stake(axfer,uint64)void'] | VeCortexStakingArgs['tuple']['stake(axfer,uint64)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'stake(axfer,uint64)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.cortexTransfer, params.args.lockDays],
    }
  }
  /**
   * Constructs a no op call for the unstake()void ABI method
   *
  * Unstake CORTEX tokens after the lock period has expired.
  Returns the original staked amount to the sender. Removes the stake record and any active delegation.

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static unstake(params: CallParams<VeCortexStakingArgs['obj']['unstake()void'] | VeCortexStakingArgs['tuple']['unstake()void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'unstake()void' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the delegate(address)void ABI method
   *
  * Delegate veCORTEX voting power to a Lawmaker agent address.
  The delegatee can then vote on governance proposals on your behalf.

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static delegate(params: CallParams<VeCortexStakingArgs['obj']['delegate(address)void'] | VeCortexStakingArgs['tuple']['delegate(address)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'delegate(address)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.lawmaker],
    }
  }
  /**
   * Constructs a no op call for the revoke_delegation()void ABI method
   *
  * Revoke an existing delegation.
  Takes effect in the next governance epoch.

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static revokeDelegation(params: CallParams<VeCortexStakingArgs['obj']['revoke_delegation()void'] | VeCortexStakingArgs['tuple']['revoke_delegation()void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'revoke_delegation()void' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the get_ve_power(address)uint64 ABI method
   *
   * Get the veCORTEX voting power for an account.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static getVePower(params: CallParams<VeCortexStakingArgs['obj']['get_ve_power(address)uint64'] | VeCortexStakingArgs['tuple']['get_ve_power(address)uint64']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'get_ve_power(address)uint64' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.account],
    }
  }
  /**
   * Constructs a no op call for the get_stake_info(address)byte[] ABI method
   *
  * Get the full stake info for an account.
  Returns 32 bytes: amount(8) + unlock_round(8) + ve_power(8) + boost(8). Returns empty bytes if no stake exists.

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static getStakeInfo(params: CallParams<VeCortexStakingArgs['obj']['get_stake_info(address)byte[]'] | VeCortexStakingArgs['tuple']['get_stake_info(address)byte[]']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'get_stake_info(address)byte[]' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.account],
    }
  }
  /**
   * Constructs a no op call for the get_delegate(address)byte[] ABI method
   *
  * Get the delegate address for an account.
  Returns 32-byte address or empty bytes if no delegation.

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static getDelegate(params: CallParams<VeCortexStakingArgs['obj']['get_delegate(address)byte[]'] | VeCortexStakingArgs['tuple']['get_delegate(address)byte[]']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'get_delegate(address)byte[]' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.account],
    }
  }
  /**
   * Constructs a no op call for the get_total_staked()uint64 ABI method
   *
   * Get the total CORTEX currently staked across all users.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static getTotalStaked(params: CallParams<VeCortexStakingArgs['obj']['get_total_staked()uint64'] | VeCortexStakingArgs['tuple']['get_total_staked()uint64']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'get_total_staked()uint64' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the fund_reward_pool(axfer)void ABI method
   *
  * Add CORTEX to the staking reward pool.
  Typically called by the protocol to distribute the 24% emission allocation.

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static fundRewardPool(params: CallParams<VeCortexStakingArgs['obj']['fund_reward_pool(axfer)void'] | VeCortexStakingArgs['tuple']['fund_reward_pool(axfer)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'fund_reward_pool(axfer)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.cortexTransfer],
    }
  }
  /**
   * Constructs a no op call for the update_lock_parameters(uint64,uint64,uint64)void ABI method
   *
  * Update staking lock parameters. Only callable by creator.
  max_boost is in basis points (2500 = 2.5x).

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static updateLockParameters(params: CallParams<VeCortexStakingArgs['obj']['update_lock_parameters(uint64,uint64,uint64)void'] | VeCortexStakingArgs['tuple']['update_lock_parameters(uint64,uint64,uint64)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'update_lock_parameters(uint64,uint64,uint64)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.minDays, params.args.maxDays, params.args.maxBoost],
    }
  }
}

/**
 * A client to make calls to the VeCortexStaking smart contract
 */
export class VeCortexStakingClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `VeCortexStakingClient`
   *
   * @param appClient An `AppClient` instance which has been created with the VeCortexStaking app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `VeCortexStakingClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Checks for decode errors on the given return value and maps the return value to the return type for the given method
   * @returns The typed return value or undefined if there was no value
   */
  decodeReturnValue<TSignature extends VeCortexStakingNonVoidMethodSignatures>(method: TSignature, returnValue: ABIReturn | undefined) {
    return returnValue !== undefined ? getArc56ReturnValue<MethodReturn<TSignature>>(returnValue, this.appClient.getABIMethod(method), APP_SPEC.structs) : undefined
  }

  /**
   * Returns a new `VeCortexStakingClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<VeCortexStakingClient> {
    return new VeCortexStakingClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `VeCortexStakingClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<VeCortexStakingClient> {
    return new VeCortexStakingClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Makes a clear_state call to an existing instance of the VeCortexStaking smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the VeCortexStaking smart contract using the `initialize(uint64)void` ABI method.
     *
    * Initialize the staking contract with the CORTEX token reference.
    Must opt-in to the asset so the contract can hold and return tokens. Only callable by the application creator.

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    initialize: (params: CallParams<VeCortexStakingArgs['obj']['initialize(uint64)void'] | VeCortexStakingArgs['tuple']['initialize(uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(VeCortexStakingParamsFactory.initialize(params))
    },

    /**
     * Makes a call to the VeCortexStaking smart contract using the `stake(axfer,uint64)void` ABI method.
     *
    * Stake CORTEX tokens with a specified lock duration.
    The caller must include an asset transfer of CORTEX to this contract in the same group transaction. Lock duration determines the veCORTEX boost multiplier (1x at 7 days, up to 2.5x at 4 years).

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    stake: (params: CallParams<VeCortexStakingArgs['obj']['stake(axfer,uint64)void'] | VeCortexStakingArgs['tuple']['stake(axfer,uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(VeCortexStakingParamsFactory.stake(params))
    },

    /**
     * Makes a call to the VeCortexStaking smart contract using the `unstake()void` ABI method.
     *
    * Unstake CORTEX tokens after the lock period has expired.
    Returns the original staked amount to the sender. Removes the stake record and any active delegation.

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    unstake: (params: CallParams<VeCortexStakingArgs['obj']['unstake()void'] | VeCortexStakingArgs['tuple']['unstake()void']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(VeCortexStakingParamsFactory.unstake(params))
    },

    /**
     * Makes a call to the VeCortexStaking smart contract using the `delegate(address)void` ABI method.
     *
    * Delegate veCORTEX voting power to a Lawmaker agent address.
    The delegatee can then vote on governance proposals on your behalf.

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    delegate: (params: CallParams<VeCortexStakingArgs['obj']['delegate(address)void'] | VeCortexStakingArgs['tuple']['delegate(address)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(VeCortexStakingParamsFactory.delegate(params))
    },

    /**
     * Makes a call to the VeCortexStaking smart contract using the `revoke_delegation()void` ABI method.
     *
    * Revoke an existing delegation.
    Takes effect in the next governance epoch.

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    revokeDelegation: (params: CallParams<VeCortexStakingArgs['obj']['revoke_delegation()void'] | VeCortexStakingArgs['tuple']['revoke_delegation()void']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(VeCortexStakingParamsFactory.revokeDelegation(params))
    },

    /**
     * Makes a call to the VeCortexStaking smart contract using the `get_ve_power(address)uint64` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * Get the veCORTEX voting power for an account.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    getVePower: (params: CallParams<VeCortexStakingArgs['obj']['get_ve_power(address)uint64'] | VeCortexStakingArgs['tuple']['get_ve_power(address)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(VeCortexStakingParamsFactory.getVePower(params))
    },

    /**
     * Makes a call to the VeCortexStaking smart contract using the `get_stake_info(address)byte[]` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
    * Get the full stake info for an account.
    Returns 32 bytes: amount(8) + unlock_round(8) + ve_power(8) + boost(8). Returns empty bytes if no stake exists.

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    getStakeInfo: (params: CallParams<VeCortexStakingArgs['obj']['get_stake_info(address)byte[]'] | VeCortexStakingArgs['tuple']['get_stake_info(address)byte[]']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(VeCortexStakingParamsFactory.getStakeInfo(params))
    },

    /**
     * Makes a call to the VeCortexStaking smart contract using the `get_delegate(address)byte[]` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
    * Get the delegate address for an account.
    Returns 32-byte address or empty bytes if no delegation.

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    getDelegate: (params: CallParams<VeCortexStakingArgs['obj']['get_delegate(address)byte[]'] | VeCortexStakingArgs['tuple']['get_delegate(address)byte[]']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(VeCortexStakingParamsFactory.getDelegate(params))
    },

    /**
     * Makes a call to the VeCortexStaking smart contract using the `get_total_staked()uint64` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * Get the total CORTEX currently staked across all users.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    getTotalStaked: (params: CallParams<VeCortexStakingArgs['obj']['get_total_staked()uint64'] | VeCortexStakingArgs['tuple']['get_total_staked()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(VeCortexStakingParamsFactory.getTotalStaked(params))
    },

    /**
     * Makes a call to the VeCortexStaking smart contract using the `fund_reward_pool(axfer)void` ABI method.
     *
    * Add CORTEX to the staking reward pool.
    Typically called by the protocol to distribute the 24% emission allocation.

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    fundRewardPool: (params: CallParams<VeCortexStakingArgs['obj']['fund_reward_pool(axfer)void'] | VeCortexStakingArgs['tuple']['fund_reward_pool(axfer)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(VeCortexStakingParamsFactory.fundRewardPool(params))
    },

    /**
     * Makes a call to the VeCortexStaking smart contract using the `update_lock_parameters(uint64,uint64,uint64)void` ABI method.
     *
    * Update staking lock parameters. Only callable by creator.
    max_boost is in basis points (2500 = 2.5x).

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    updateLockParameters: (params: CallParams<VeCortexStakingArgs['obj']['update_lock_parameters(uint64,uint64,uint64)void'] | VeCortexStakingArgs['tuple']['update_lock_parameters(uint64,uint64,uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(VeCortexStakingParamsFactory.updateLockParameters(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Makes a clear_state call to an existing instance of the VeCortexStaking smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the VeCortexStaking smart contract using the `initialize(uint64)void` ABI method.
     *
    * Initialize the staking contract with the CORTEX token reference.
    Must opt-in to the asset so the contract can hold and return tokens. Only callable by the application creator.

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    initialize: (params: CallParams<VeCortexStakingArgs['obj']['initialize(uint64)void'] | VeCortexStakingArgs['tuple']['initialize(uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(VeCortexStakingParamsFactory.initialize(params))
    },

    /**
     * Makes a call to the VeCortexStaking smart contract using the `stake(axfer,uint64)void` ABI method.
     *
    * Stake CORTEX tokens with a specified lock duration.
    The caller must include an asset transfer of CORTEX to this contract in the same group transaction. Lock duration determines the veCORTEX boost multiplier (1x at 7 days, up to 2.5x at 4 years).

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    stake: (params: CallParams<VeCortexStakingArgs['obj']['stake(axfer,uint64)void'] | VeCortexStakingArgs['tuple']['stake(axfer,uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(VeCortexStakingParamsFactory.stake(params))
    },

    /**
     * Makes a call to the VeCortexStaking smart contract using the `unstake()void` ABI method.
     *
    * Unstake CORTEX tokens after the lock period has expired.
    Returns the original staked amount to the sender. Removes the stake record and any active delegation.

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    unstake: (params: CallParams<VeCortexStakingArgs['obj']['unstake()void'] | VeCortexStakingArgs['tuple']['unstake()void']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(VeCortexStakingParamsFactory.unstake(params))
    },

    /**
     * Makes a call to the VeCortexStaking smart contract using the `delegate(address)void` ABI method.
     *
    * Delegate veCORTEX voting power to a Lawmaker agent address.
    The delegatee can then vote on governance proposals on your behalf.

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    delegate: (params: CallParams<VeCortexStakingArgs['obj']['delegate(address)void'] | VeCortexStakingArgs['tuple']['delegate(address)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(VeCortexStakingParamsFactory.delegate(params))
    },

    /**
     * Makes a call to the VeCortexStaking smart contract using the `revoke_delegation()void` ABI method.
     *
    * Revoke an existing delegation.
    Takes effect in the next governance epoch.

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    revokeDelegation: (params: CallParams<VeCortexStakingArgs['obj']['revoke_delegation()void'] | VeCortexStakingArgs['tuple']['revoke_delegation()void']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(VeCortexStakingParamsFactory.revokeDelegation(params))
    },

    /**
     * Makes a call to the VeCortexStaking smart contract using the `get_ve_power(address)uint64` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * Get the veCORTEX voting power for an account.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    getVePower: (params: CallParams<VeCortexStakingArgs['obj']['get_ve_power(address)uint64'] | VeCortexStakingArgs['tuple']['get_ve_power(address)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(VeCortexStakingParamsFactory.getVePower(params))
    },

    /**
     * Makes a call to the VeCortexStaking smart contract using the `get_stake_info(address)byte[]` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
    * Get the full stake info for an account.
    Returns 32 bytes: amount(8) + unlock_round(8) + ve_power(8) + boost(8). Returns empty bytes if no stake exists.

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    getStakeInfo: (params: CallParams<VeCortexStakingArgs['obj']['get_stake_info(address)byte[]'] | VeCortexStakingArgs['tuple']['get_stake_info(address)byte[]']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(VeCortexStakingParamsFactory.getStakeInfo(params))
    },

    /**
     * Makes a call to the VeCortexStaking smart contract using the `get_delegate(address)byte[]` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
    * Get the delegate address for an account.
    Returns 32-byte address or empty bytes if no delegation.

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    getDelegate: (params: CallParams<VeCortexStakingArgs['obj']['get_delegate(address)byte[]'] | VeCortexStakingArgs['tuple']['get_delegate(address)byte[]']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(VeCortexStakingParamsFactory.getDelegate(params))
    },

    /**
     * Makes a call to the VeCortexStaking smart contract using the `get_total_staked()uint64` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * Get the total CORTEX currently staked across all users.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    getTotalStaked: (params: CallParams<VeCortexStakingArgs['obj']['get_total_staked()uint64'] | VeCortexStakingArgs['tuple']['get_total_staked()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(VeCortexStakingParamsFactory.getTotalStaked(params))
    },

    /**
     * Makes a call to the VeCortexStaking smart contract using the `fund_reward_pool(axfer)void` ABI method.
     *
    * Add CORTEX to the staking reward pool.
    Typically called by the protocol to distribute the 24% emission allocation.

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    fundRewardPool: (params: CallParams<VeCortexStakingArgs['obj']['fund_reward_pool(axfer)void'] | VeCortexStakingArgs['tuple']['fund_reward_pool(axfer)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(VeCortexStakingParamsFactory.fundRewardPool(params))
    },

    /**
     * Makes a call to the VeCortexStaking smart contract using the `update_lock_parameters(uint64,uint64,uint64)void` ABI method.
     *
    * Update staking lock parameters. Only callable by creator.
    max_boost is in basis points (2500 = 2.5x).

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    updateLockParameters: (params: CallParams<VeCortexStakingArgs['obj']['update_lock_parameters(uint64,uint64,uint64)void'] | VeCortexStakingArgs['tuple']['update_lock_parameters(uint64,uint64,uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(VeCortexStakingParamsFactory.updateLockParameters(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Makes a clear_state call to an existing instance of the VeCortexStaking smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the VeCortexStaking smart contract using the `initialize(uint64)void` ABI method.
     *
    * Initialize the staking contract with the CORTEX token reference.
    Must opt-in to the asset so the contract can hold and return tokens. Only callable by the application creator.

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    initialize: async (params: CallParams<VeCortexStakingArgs['obj']['initialize(uint64)void'] | VeCortexStakingArgs['tuple']['initialize(uint64)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(VeCortexStakingParamsFactory.initialize(params))
      return {...result, return: result.return as unknown as (undefined | VeCortexStakingReturns['initialize(uint64)void'])}
    },

    /**
     * Makes a call to the VeCortexStaking smart contract using the `stake(axfer,uint64)void` ABI method.
     *
    * Stake CORTEX tokens with a specified lock duration.
    The caller must include an asset transfer of CORTEX to this contract in the same group transaction. Lock duration determines the veCORTEX boost multiplier (1x at 7 days, up to 2.5x at 4 years).

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    stake: async (params: CallParams<VeCortexStakingArgs['obj']['stake(axfer,uint64)void'] | VeCortexStakingArgs['tuple']['stake(axfer,uint64)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(VeCortexStakingParamsFactory.stake(params))
      return {...result, return: result.return as unknown as (undefined | VeCortexStakingReturns['stake(axfer,uint64)void'])}
    },

    /**
     * Makes a call to the VeCortexStaking smart contract using the `unstake()void` ABI method.
     *
    * Unstake CORTEX tokens after the lock period has expired.
    Returns the original staked amount to the sender. Removes the stake record and any active delegation.

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    unstake: async (params: CallParams<VeCortexStakingArgs['obj']['unstake()void'] | VeCortexStakingArgs['tuple']['unstake()void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(VeCortexStakingParamsFactory.unstake(params))
      return {...result, return: result.return as unknown as (undefined | VeCortexStakingReturns['unstake()void'])}
    },

    /**
     * Makes a call to the VeCortexStaking smart contract using the `delegate(address)void` ABI method.
     *
    * Delegate veCORTEX voting power to a Lawmaker agent address.
    The delegatee can then vote on governance proposals on your behalf.

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    delegate: async (params: CallParams<VeCortexStakingArgs['obj']['delegate(address)void'] | VeCortexStakingArgs['tuple']['delegate(address)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(VeCortexStakingParamsFactory.delegate(params))
      return {...result, return: result.return as unknown as (undefined | VeCortexStakingReturns['delegate(address)void'])}
    },

    /**
     * Makes a call to the VeCortexStaking smart contract using the `revoke_delegation()void` ABI method.
     *
    * Revoke an existing delegation.
    Takes effect in the next governance epoch.

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    revokeDelegation: async (params: CallParams<VeCortexStakingArgs['obj']['revoke_delegation()void'] | VeCortexStakingArgs['tuple']['revoke_delegation()void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(VeCortexStakingParamsFactory.revokeDelegation(params))
      return {...result, return: result.return as unknown as (undefined | VeCortexStakingReturns['revoke_delegation()void'])}
    },

    /**
     * Makes a call to the VeCortexStaking smart contract using the `get_ve_power(address)uint64` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * Get the veCORTEX voting power for an account.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    getVePower: async (params: CallParams<VeCortexStakingArgs['obj']['get_ve_power(address)uint64'] | VeCortexStakingArgs['tuple']['get_ve_power(address)uint64']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(VeCortexStakingParamsFactory.getVePower(params))
      return {...result, return: result.return as unknown as (undefined | VeCortexStakingReturns['get_ve_power(address)uint64'])}
    },

    /**
     * Makes a call to the VeCortexStaking smart contract using the `get_stake_info(address)byte[]` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
    * Get the full stake info for an account.
    Returns 32 bytes: amount(8) + unlock_round(8) + ve_power(8) + boost(8). Returns empty bytes if no stake exists.

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    getStakeInfo: async (params: CallParams<VeCortexStakingArgs['obj']['get_stake_info(address)byte[]'] | VeCortexStakingArgs['tuple']['get_stake_info(address)byte[]']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(VeCortexStakingParamsFactory.getStakeInfo(params))
      return {...result, return: result.return as unknown as (undefined | VeCortexStakingReturns['get_stake_info(address)byte[]'])}
    },

    /**
     * Makes a call to the VeCortexStaking smart contract using the `get_delegate(address)byte[]` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
    * Get the delegate address for an account.
    Returns 32-byte address or empty bytes if no delegation.

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    getDelegate: async (params: CallParams<VeCortexStakingArgs['obj']['get_delegate(address)byte[]'] | VeCortexStakingArgs['tuple']['get_delegate(address)byte[]']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(VeCortexStakingParamsFactory.getDelegate(params))
      return {...result, return: result.return as unknown as (undefined | VeCortexStakingReturns['get_delegate(address)byte[]'])}
    },

    /**
     * Makes a call to the VeCortexStaking smart contract using the `get_total_staked()uint64` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * Get the total CORTEX currently staked across all users.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    getTotalStaked: async (params: CallParams<VeCortexStakingArgs['obj']['get_total_staked()uint64'] | VeCortexStakingArgs['tuple']['get_total_staked()uint64']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(VeCortexStakingParamsFactory.getTotalStaked(params))
      return {...result, return: result.return as unknown as (undefined | VeCortexStakingReturns['get_total_staked()uint64'])}
    },

    /**
     * Makes a call to the VeCortexStaking smart contract using the `fund_reward_pool(axfer)void` ABI method.
     *
    * Add CORTEX to the staking reward pool.
    Typically called by the protocol to distribute the 24% emission allocation.

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    fundRewardPool: async (params: CallParams<VeCortexStakingArgs['obj']['fund_reward_pool(axfer)void'] | VeCortexStakingArgs['tuple']['fund_reward_pool(axfer)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(VeCortexStakingParamsFactory.fundRewardPool(params))
      return {...result, return: result.return as unknown as (undefined | VeCortexStakingReturns['fund_reward_pool(axfer)void'])}
    },

    /**
     * Makes a call to the VeCortexStaking smart contract using the `update_lock_parameters(uint64,uint64,uint64)void` ABI method.
     *
    * Update staking lock parameters. Only callable by creator.
    max_boost is in basis points (2500 = 2.5x).

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    updateLockParameters: async (params: CallParams<VeCortexStakingArgs['obj']['update_lock_parameters(uint64,uint64,uint64)void'] | VeCortexStakingArgs['tuple']['update_lock_parameters(uint64,uint64,uint64)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(VeCortexStakingParamsFactory.updateLockParameters(params))
      return {...result, return: result.return as unknown as (undefined | VeCortexStakingReturns['update_lock_parameters(uint64,uint64,uint64)void'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new VeCortexStakingClient(this.appClient.clone(params))
  }

  /**
   * Makes a readonly (simulated) call to the VeCortexStaking smart contract using the `get_ve_power(address)uint64` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * Get the veCORTEX voting power for an account.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async getVePower(params: CallParams<VeCortexStakingArgs['obj']['get_ve_power(address)uint64'] | VeCortexStakingArgs['tuple']['get_ve_power(address)uint64']>) {
    const result = await this.appClient.send.call(VeCortexStakingParamsFactory.getVePower(params))
    return result.return as unknown as VeCortexStakingReturns['get_ve_power(address)uint64']
  }

  /**
   * Makes a readonly (simulated) call to the VeCortexStaking smart contract using the `get_stake_info(address)byte[]` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
  * Get the full stake info for an account.
  Returns 32 bytes: amount(8) + unlock_round(8) + ve_power(8) + boost(8). Returns empty bytes if no stake exists.

   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async getStakeInfo(params: CallParams<VeCortexStakingArgs['obj']['get_stake_info(address)byte[]'] | VeCortexStakingArgs['tuple']['get_stake_info(address)byte[]']>) {
    const result = await this.appClient.send.call(VeCortexStakingParamsFactory.getStakeInfo(params))
    return result.return as unknown as VeCortexStakingReturns['get_stake_info(address)byte[]']
  }

  /**
   * Makes a readonly (simulated) call to the VeCortexStaking smart contract using the `get_delegate(address)byte[]` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
  * Get the delegate address for an account.
  Returns 32-byte address or empty bytes if no delegation.

   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async getDelegate(params: CallParams<VeCortexStakingArgs['obj']['get_delegate(address)byte[]'] | VeCortexStakingArgs['tuple']['get_delegate(address)byte[]']>) {
    const result = await this.appClient.send.call(VeCortexStakingParamsFactory.getDelegate(params))
    return result.return as unknown as VeCortexStakingReturns['get_delegate(address)byte[]']
  }

  /**
   * Makes a readonly (simulated) call to the VeCortexStaking smart contract using the `get_total_staked()uint64` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * Get the total CORTEX currently staked across all users.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async getTotalStaked(params: CallParams<VeCortexStakingArgs['obj']['get_total_staked()uint64'] | VeCortexStakingArgs['tuple']['get_total_staked()uint64']> = {args: []}) {
    const result = await this.appClient.send.call(VeCortexStakingParamsFactory.getTotalStaked(params))
    return result.return as unknown as VeCortexStakingReturns['get_total_staked()uint64']
  }

  /**
   * Methods to access state for the current VeCortexStaking app
   */
  state = {
    /**
     * Methods to access global state for the current VeCortexStaking app
     */
    global: {
      /**
       * Get all current keyed values from global state
       */
      getAll: async (): Promise<Partial<Expand<GlobalKeysState>>> => {
        const result = await this.appClient.state.global.getAll()
        return {
          cortexToken: result.cortex_token,
          totalStaked: result.total_staked,
          rewardPool: result.reward_pool,
          minLockDays: result.MIN_LOCK_DAYS,
          maxLockDays: result.MAX_LOCK_DAYS,
          maxBoost: result.MAX_BOOST,
          roundsPerDay: result.ROUNDS_PER_DAY,
        }
      },
      /**
       * Get the current value of the cortex_token key in global state
       */
      cortexToken: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("cortex_token")) as bigint | undefined },
      /**
       * Get the current value of the total_staked key in global state
       */
      totalStaked: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("total_staked")) as bigint | undefined },
      /**
       * Get the current value of the reward_pool key in global state
       */
      rewardPool: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("reward_pool")) as bigint | undefined },
      /**
       * Get the current value of the MIN_LOCK_DAYS key in global state
       */
      minLockDays: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("MIN_LOCK_DAYS")) as bigint | undefined },
      /**
       * Get the current value of the MAX_LOCK_DAYS key in global state
       */
      maxLockDays: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("MAX_LOCK_DAYS")) as bigint | undefined },
      /**
       * Get the current value of the MAX_BOOST key in global state
       */
      maxBoost: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("MAX_BOOST")) as bigint | undefined },
      /**
       * Get the current value of the ROUNDS_PER_DAY key in global state
       */
      roundsPerDay: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("ROUNDS_PER_DAY")) as bigint | undefined },
    },
    /**
     * Methods to access box state for the current VeCortexStaking app
     */
    box: {
      /**
       * Get all current keyed values from box state
       */
      getAll: async (): Promise<Partial<Expand<BoxKeysState>>> => {
        const result = await this.appClient.state.box.getAll()
        return {
        }
      },
      /**
       * Get values from the stakes map in box state
       */
      stakes: {
        /**
         * Get all current values of the stakes map in box state
         */
        getMap: async (): Promise<Map<Uint8Array, Uint8Array>> => { return (await this.appClient.state.box.getMap("stakes")) as Map<Uint8Array, Uint8Array> },
        /**
         * Get a current value of the stakes map by key from box state
         */
        value: async (key: Uint8Array | string): Promise<Uint8Array | undefined> => { return await this.appClient.state.box.getMapValue("stakes", key) as Uint8Array | undefined },
      },
      /**
       * Get values from the delegations map in box state
       */
      delegations: {
        /**
         * Get all current values of the delegations map in box state
         */
        getMap: async (): Promise<Map<Uint8Array, Uint8Array>> => { return (await this.appClient.state.box.getMap("delegations")) as Map<Uint8Array, Uint8Array> },
        /**
         * Get a current value of the delegations map by key from box state
         */
        value: async (key: Uint8Array | string): Promise<Uint8Array | undefined> => { return await this.appClient.state.box.getMapValue("delegations", key) as Uint8Array | undefined },
      },
    },
  }

  public newGroup(): VeCortexStakingComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a initialize(uint64)void method call against the VeCortexStaking contract
       */
      initialize(params: CallParams<VeCortexStakingArgs['obj']['initialize(uint64)void'] | VeCortexStakingArgs['tuple']['initialize(uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.initialize(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a stake(axfer,uint64)void method call against the VeCortexStaking contract
       */
      stake(params: CallParams<VeCortexStakingArgs['obj']['stake(axfer,uint64)void'] | VeCortexStakingArgs['tuple']['stake(axfer,uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.stake(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a unstake()void method call against the VeCortexStaking contract
       */
      unstake(params: CallParams<VeCortexStakingArgs['obj']['unstake()void'] | VeCortexStakingArgs['tuple']['unstake()void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.unstake(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a delegate(address)void method call against the VeCortexStaking contract
       */
      delegate(params: CallParams<VeCortexStakingArgs['obj']['delegate(address)void'] | VeCortexStakingArgs['tuple']['delegate(address)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.delegate(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a revoke_delegation()void method call against the VeCortexStaking contract
       */
      revokeDelegation(params: CallParams<VeCortexStakingArgs['obj']['revoke_delegation()void'] | VeCortexStakingArgs['tuple']['revoke_delegation()void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.revokeDelegation(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a get_ve_power(address)uint64 method call against the VeCortexStaking contract
       */
      getVePower(params: CallParams<VeCortexStakingArgs['obj']['get_ve_power(address)uint64'] | VeCortexStakingArgs['tuple']['get_ve_power(address)uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.getVePower(params)))
        resultMappers.push((v) => client.decodeReturnValue('get_ve_power(address)uint64', v))
        return this
      },
      /**
       * Add a get_stake_info(address)byte[] method call against the VeCortexStaking contract
       */
      getStakeInfo(params: CallParams<VeCortexStakingArgs['obj']['get_stake_info(address)byte[]'] | VeCortexStakingArgs['tuple']['get_stake_info(address)byte[]']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.getStakeInfo(params)))
        resultMappers.push((v) => client.decodeReturnValue('get_stake_info(address)byte[]', v))
        return this
      },
      /**
       * Add a get_delegate(address)byte[] method call against the VeCortexStaking contract
       */
      getDelegate(params: CallParams<VeCortexStakingArgs['obj']['get_delegate(address)byte[]'] | VeCortexStakingArgs['tuple']['get_delegate(address)byte[]']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.getDelegate(params)))
        resultMappers.push((v) => client.decodeReturnValue('get_delegate(address)byte[]', v))
        return this
      },
      /**
       * Add a get_total_staked()uint64 method call against the VeCortexStaking contract
       */
      getTotalStaked(params: CallParams<VeCortexStakingArgs['obj']['get_total_staked()uint64'] | VeCortexStakingArgs['tuple']['get_total_staked()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.getTotalStaked(params)))
        resultMappers.push((v) => client.decodeReturnValue('get_total_staked()uint64', v))
        return this
      },
      /**
       * Add a fund_reward_pool(axfer)void method call against the VeCortexStaking contract
       */
      fundRewardPool(params: CallParams<VeCortexStakingArgs['obj']['fund_reward_pool(axfer)void'] | VeCortexStakingArgs['tuple']['fund_reward_pool(axfer)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.fundRewardPool(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a update_lock_parameters(uint64,uint64,uint64)void method call against the VeCortexStaking contract
       */
      updateLockParameters(params: CallParams<VeCortexStakingArgs['obj']['update_lock_parameters(uint64,uint64,uint64)void'] | VeCortexStakingArgs['tuple']['update_lock_parameters(uint64,uint64,uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.updateLockParameters(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a clear state call to the VeCortexStaking contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as VeCortexStakingComposer
  }
}
export type VeCortexStakingComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the initialize(uint64)void ABI method.
   *
  * Initialize the staking contract with the CORTEX token reference.
  Must opt-in to the asset so the contract can hold and return tokens. Only callable by the application creator.

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  initialize(params?: CallParams<VeCortexStakingArgs['obj']['initialize(uint64)void'] | VeCortexStakingArgs['tuple']['initialize(uint64)void']>): VeCortexStakingComposer<[...TReturns, VeCortexStakingReturns['initialize(uint64)void'] | undefined]>

  /**
   * Calls the stake(axfer,uint64)void ABI method.
   *
  * Stake CORTEX tokens with a specified lock duration.
  The caller must include an asset transfer of CORTEX to this contract in the same group transaction. Lock duration determines the veCORTEX boost multiplier (1x at 7 days, up to 2.5x at 4 years).

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  stake(params?: CallParams<VeCortexStakingArgs['obj']['stake(axfer,uint64)void'] | VeCortexStakingArgs['tuple']['stake(axfer,uint64)void']>): VeCortexStakingComposer<[...TReturns, VeCortexStakingReturns['stake(axfer,uint64)void'] | undefined]>

  /**
   * Calls the unstake()void ABI method.
   *
  * Unstake CORTEX tokens after the lock period has expired.
  Returns the original staked amount to the sender. Removes the stake record and any active delegation.

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  unstake(params?: CallParams<VeCortexStakingArgs['obj']['unstake()void'] | VeCortexStakingArgs['tuple']['unstake()void']>): VeCortexStakingComposer<[...TReturns, VeCortexStakingReturns['unstake()void'] | undefined]>

  /**
   * Calls the delegate(address)void ABI method.
   *
  * Delegate veCORTEX voting power to a Lawmaker agent address.
  The delegatee can then vote on governance proposals on your behalf.

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  delegate(params?: CallParams<VeCortexStakingArgs['obj']['delegate(address)void'] | VeCortexStakingArgs['tuple']['delegate(address)void']>): VeCortexStakingComposer<[...TReturns, VeCortexStakingReturns['delegate(address)void'] | undefined]>

  /**
   * Calls the revoke_delegation()void ABI method.
   *
  * Revoke an existing delegation.
  Takes effect in the next governance epoch.

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  revokeDelegation(params?: CallParams<VeCortexStakingArgs['obj']['revoke_delegation()void'] | VeCortexStakingArgs['tuple']['revoke_delegation()void']>): VeCortexStakingComposer<[...TReturns, VeCortexStakingReturns['revoke_delegation()void'] | undefined]>

  /**
   * Calls the get_ve_power(address)uint64 ABI method.
   *
   * Get the veCORTEX voting power for an account.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  getVePower(params?: CallParams<VeCortexStakingArgs['obj']['get_ve_power(address)uint64'] | VeCortexStakingArgs['tuple']['get_ve_power(address)uint64']>): VeCortexStakingComposer<[...TReturns, VeCortexStakingReturns['get_ve_power(address)uint64'] | undefined]>

  /**
   * Calls the get_stake_info(address)byte[] ABI method.
   *
  * Get the full stake info for an account.
  Returns 32 bytes: amount(8) + unlock_round(8) + ve_power(8) + boost(8). Returns empty bytes if no stake exists.

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  getStakeInfo(params?: CallParams<VeCortexStakingArgs['obj']['get_stake_info(address)byte[]'] | VeCortexStakingArgs['tuple']['get_stake_info(address)byte[]']>): VeCortexStakingComposer<[...TReturns, VeCortexStakingReturns['get_stake_info(address)byte[]'] | undefined]>

  /**
   * Calls the get_delegate(address)byte[] ABI method.
   *
  * Get the delegate address for an account.
  Returns 32-byte address or empty bytes if no delegation.

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  getDelegate(params?: CallParams<VeCortexStakingArgs['obj']['get_delegate(address)byte[]'] | VeCortexStakingArgs['tuple']['get_delegate(address)byte[]']>): VeCortexStakingComposer<[...TReturns, VeCortexStakingReturns['get_delegate(address)byte[]'] | undefined]>

  /**
   * Calls the get_total_staked()uint64 ABI method.
   *
   * Get the total CORTEX currently staked across all users.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  getTotalStaked(params?: CallParams<VeCortexStakingArgs['obj']['get_total_staked()uint64'] | VeCortexStakingArgs['tuple']['get_total_staked()uint64']>): VeCortexStakingComposer<[...TReturns, VeCortexStakingReturns['get_total_staked()uint64'] | undefined]>

  /**
   * Calls the fund_reward_pool(axfer)void ABI method.
   *
  * Add CORTEX to the staking reward pool.
  Typically called by the protocol to distribute the 24% emission allocation.

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  fundRewardPool(params?: CallParams<VeCortexStakingArgs['obj']['fund_reward_pool(axfer)void'] | VeCortexStakingArgs['tuple']['fund_reward_pool(axfer)void']>): VeCortexStakingComposer<[...TReturns, VeCortexStakingReturns['fund_reward_pool(axfer)void'] | undefined]>

  /**
   * Calls the update_lock_parameters(uint64,uint64,uint64)void ABI method.
   *
  * Update staking lock parameters. Only callable by creator.
  max_boost is in basis points (2500 = 2.5x).

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  updateLockParameters(params?: CallParams<VeCortexStakingArgs['obj']['update_lock_parameters(uint64,uint64,uint64)void'] | VeCortexStakingArgs['tuple']['update_lock_parameters(uint64,uint64,uint64)void']>): VeCortexStakingComposer<[...TReturns, VeCortexStakingReturns['update_lock_parameters(uint64,uint64,uint64)void'] | undefined]>

  /**
   * Makes a clear_state call to an existing instance of the VeCortexStaking smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): VeCortexStakingComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): VeCortexStakingComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<VeCortexStakingComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<VeCortexStakingComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<VeCortexStakingComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<VeCortexStakingComposerResults<TReturns>>
}
export type VeCortexStakingComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

