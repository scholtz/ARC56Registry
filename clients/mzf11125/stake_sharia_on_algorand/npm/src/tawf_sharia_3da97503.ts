/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"tawf_sharia","structs":{},"methods":[{"name":"lease_validation_rights","args":[{"type":"pay","name":"payment"}],"returns":{"type":"string"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}},{"name":"claim_ujrah","args":[],"returns":{"type":"string"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}},{"name":"terminate_lease","args":[{"type":"uint64","name":"amount"}],"returns":{"type":"string"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}},{"name":"get_my_ijarah_info","args":[],"returns":{"type":"(uint64,uint64,uint64)"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}},{"name":"check_lessor_info","args":[{"type":"address","name":"lessor"}],"returns":{"type":"(uint64,uint64)"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}},{"name":"total_contract_balance","args":[],"returns":{"type":"uint64"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}}],"arcs":[22,28],"networks":{},"state":{"schema":{"global":{"ints":2,"bytes":0},"local":{"ints":0,"bytes":0}},"keys":{"global":{"ANNUAL_UJRAH_RATE_BP":{"keyType":"AVMString","valueType":"AVMUint64","key":"QU5OVUFMX1VKUkFIX1JBVEVfQlA="},"SECONDS_PER_YEAR":{"keyType":"AVMString","valueType":"AVMUint64","key":"U0VDT05EU19QRVJfWUVBUg=="}},"local":{},"box":{}},"maps":{"global":{},"local":{},"box":{}}},"bareActions":{"create":["NoOp"],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[395],"errorMessage":"Exceeds balance"},{"pc":[383],"errorMessage":"Invalid amount"},{"pc":[164],"errorMessage":"Invalid receiver"},{"pc":[173],"errorMessage":"Min 1 ALGO"},{"pc":[283],"errorMessage":"No ujrah yet"},{"pc":[84],"errorMessage":"OnCompletion must be NoOp"},{"pc":[145],"errorMessage":"OnCompletion must be NoOp && can only call when creating"},{"pc":[622],"errorMessage":"check self.ANNUAL_UJRAH_RATE_BP exists"},{"pc":[627],"errorMessage":"check self.SECONDS_PER_YEAR exists"},{"pc":[556],"errorMessage":"invalid number of bytes for arc4.static_array<arc4.uint8, 32>"},{"pc":[368],"errorMessage":"invalid number of bytes for arc4.uint64"},{"pc":[157],"errorMessage":"transaction type is pay"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"events":[]} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


/**
 * The argument types for the TawfSharia contract
 */
export type TawfShariaArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'lease_validation_rights(pay)string': {
      payment: AppMethodCallTransactionArgument
    }
    'claim_ujrah()string': Record<string, never>
    'terminate_lease(uint64)string': {
      amount: bigint | number
    }
    'get_my_ijarah_info()(uint64,uint64,uint64)': Record<string, never>
    'check_lessor_info(address)(uint64,uint64)': {
      lessor: string
    }
    'total_contract_balance()uint64': Record<string, never>
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'lease_validation_rights(pay)string': [payment: AppMethodCallTransactionArgument]
    'claim_ujrah()string': []
    'terminate_lease(uint64)string': [amount: bigint | number]
    'get_my_ijarah_info()(uint64,uint64,uint64)': []
    'check_lessor_info(address)(uint64,uint64)': [lessor: string]
    'total_contract_balance()uint64': []
  }
}

/**
 * The return type for each method
 */
export type TawfShariaReturns = {
  'lease_validation_rights(pay)string': string
  'claim_ujrah()string': string
  'terminate_lease(uint64)string': string
  'get_my_ijarah_info()(uint64,uint64,uint64)': [bigint, bigint, bigint]
  'check_lessor_info(address)(uint64,uint64)': [bigint, bigint]
  'total_contract_balance()uint64': bigint
}

/**
 * Defines the types of available calls and state of the TawfSharia smart contract.
 */
export type TawfShariaTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'lease_validation_rights(pay)string' | 'lease_validation_rights', {
      argsObj: TawfShariaArgs['obj']['lease_validation_rights(pay)string']
      argsTuple: TawfShariaArgs['tuple']['lease_validation_rights(pay)string']
      returns: TawfShariaReturns['lease_validation_rights(pay)string']
    }>
    & Record<'claim_ujrah()string' | 'claim_ujrah', {
      argsObj: TawfShariaArgs['obj']['claim_ujrah()string']
      argsTuple: TawfShariaArgs['tuple']['claim_ujrah()string']
      returns: TawfShariaReturns['claim_ujrah()string']
    }>
    & Record<'terminate_lease(uint64)string' | 'terminate_lease', {
      argsObj: TawfShariaArgs['obj']['terminate_lease(uint64)string']
      argsTuple: TawfShariaArgs['tuple']['terminate_lease(uint64)string']
      returns: TawfShariaReturns['terminate_lease(uint64)string']
    }>
    & Record<'get_my_ijarah_info()(uint64,uint64,uint64)' | 'get_my_ijarah_info', {
      argsObj: TawfShariaArgs['obj']['get_my_ijarah_info()(uint64,uint64,uint64)']
      argsTuple: TawfShariaArgs['tuple']['get_my_ijarah_info()(uint64,uint64,uint64)']
      returns: TawfShariaReturns['get_my_ijarah_info()(uint64,uint64,uint64)']
    }>
    & Record<'check_lessor_info(address)(uint64,uint64)' | 'check_lessor_info', {
      argsObj: TawfShariaArgs['obj']['check_lessor_info(address)(uint64,uint64)']
      argsTuple: TawfShariaArgs['tuple']['check_lessor_info(address)(uint64,uint64)']
      returns: TawfShariaReturns['check_lessor_info(address)(uint64,uint64)']
    }>
    & Record<'total_contract_balance()uint64' | 'total_contract_balance', {
      argsObj: TawfShariaArgs['obj']['total_contract_balance()uint64']
      argsTuple: TawfShariaArgs['tuple']['total_contract_balance()uint64']
      returns: TawfShariaReturns['total_contract_balance()uint64']
    }>
  /**
   * Defines the shape of the state of the application.
   */
  state: {
    global: {
      keys: {
        annualUjrahRateBp: bigint
        secondsPerYear: bigint
      }
      maps: {}
    }
  }
}

/**
 * Defines the possible abi call signatures.
 */
export type TawfShariaSignatures = keyof TawfShariaTypes['methods']
/**
 * Defines the possible abi call signatures for methods that return a non-void value.
 */
export type TawfShariaNonVoidMethodSignatures = keyof TawfShariaTypes['methods'] extends infer T ? T extends keyof TawfShariaTypes['methods'] ? MethodReturn<T> extends void ? never : T  : never : never
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the TawfSharia smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends TawfShariaSignatures> = TawfShariaTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the TawfSharia smart contract to the method's return type
 */
export type MethodReturn<TSignature extends TawfShariaSignatures> = TawfShariaTypes['methods'][TSignature]['returns']

/**
 * Defines the shape of the keyed global state of the application.
 */
export type GlobalKeysState = TawfShariaTypes['state']['global']['keys']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the TawfSharia smart contract
 */
export abstract class TawfShariaParamsFactory {
  /**
   * Constructs a no op call for the lease_validation_rights(pay)string ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static leaseValidationRights(params: CallParams<TawfShariaArgs['obj']['lease_validation_rights(pay)string'] | TawfShariaArgs['tuple']['lease_validation_rights(pay)string']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'lease_validation_rights(pay)string' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.payment],
    }
  }
  /**
   * Constructs a no op call for the claim_ujrah()string ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static claimUjrah(params: CallParams<TawfShariaArgs['obj']['claim_ujrah()string'] | TawfShariaArgs['tuple']['claim_ujrah()string']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'claim_ujrah()string' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the terminate_lease(uint64)string ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static terminateLease(params: CallParams<TawfShariaArgs['obj']['terminate_lease(uint64)string'] | TawfShariaArgs['tuple']['terminate_lease(uint64)string']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'terminate_lease(uint64)string' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.amount],
    }
  }
  /**
   * Constructs a no op call for the get_my_ijarah_info()(uint64,uint64,uint64) ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static getMyIjarahInfo(params: CallParams<TawfShariaArgs['obj']['get_my_ijarah_info()(uint64,uint64,uint64)'] | TawfShariaArgs['tuple']['get_my_ijarah_info()(uint64,uint64,uint64)']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'get_my_ijarah_info()(uint64,uint64,uint64)' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the check_lessor_info(address)(uint64,uint64) ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static checkLessorInfo(params: CallParams<TawfShariaArgs['obj']['check_lessor_info(address)(uint64,uint64)'] | TawfShariaArgs['tuple']['check_lessor_info(address)(uint64,uint64)']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'check_lessor_info(address)(uint64,uint64)' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.lessor],
    }
  }
  /**
   * Constructs a no op call for the total_contract_balance()uint64 ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static totalContractBalance(params: CallParams<TawfShariaArgs['obj']['total_contract_balance()uint64'] | TawfShariaArgs['tuple']['total_contract_balance()uint64']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'total_contract_balance()uint64' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
}

/**
 * A client to make calls to the tawf_sharia smart contract
 */
export class TawfShariaClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `TawfShariaClient`
   *
   * @param appClient An `AppClient` instance which has been created with the TawfSharia app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `TawfShariaClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Checks for decode errors on the given return value and maps the return value to the return type for the given method
   * @returns The typed return value or undefined if there was no value
   */
  decodeReturnValue<TSignature extends TawfShariaNonVoidMethodSignatures>(method: TSignature, returnValue: ABIReturn | undefined) {
    return returnValue !== undefined ? getArc56ReturnValue<MethodReturn<TSignature>>(returnValue, this.appClient.getABIMethod(method), APP_SPEC.structs) : undefined
  }

  /**
   * Returns a new `TawfShariaClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<TawfShariaClient> {
    return new TawfShariaClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `TawfShariaClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<TawfShariaClient> {
    return new TawfShariaClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Makes a clear_state call to an existing instance of the tawf_sharia smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the tawf_sharia smart contract using the `lease_validation_rights(pay)string` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    leaseValidationRights: (params: CallParams<TawfShariaArgs['obj']['lease_validation_rights(pay)string'] | TawfShariaArgs['tuple']['lease_validation_rights(pay)string']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(TawfShariaParamsFactory.leaseValidationRights(params))
    },

    /**
     * Makes a call to the tawf_sharia smart contract using the `claim_ujrah()string` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    claimUjrah: (params: CallParams<TawfShariaArgs['obj']['claim_ujrah()string'] | TawfShariaArgs['tuple']['claim_ujrah()string']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(TawfShariaParamsFactory.claimUjrah(params))
    },

    /**
     * Makes a call to the tawf_sharia smart contract using the `terminate_lease(uint64)string` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    terminateLease: (params: CallParams<TawfShariaArgs['obj']['terminate_lease(uint64)string'] | TawfShariaArgs['tuple']['terminate_lease(uint64)string']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(TawfShariaParamsFactory.terminateLease(params))
    },

    /**
     * Makes a call to the tawf_sharia smart contract using the `get_my_ijarah_info()(uint64,uint64,uint64)` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    getMyIjarahInfo: (params: CallParams<TawfShariaArgs['obj']['get_my_ijarah_info()(uint64,uint64,uint64)'] | TawfShariaArgs['tuple']['get_my_ijarah_info()(uint64,uint64,uint64)']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(TawfShariaParamsFactory.getMyIjarahInfo(params))
    },

    /**
     * Makes a call to the tawf_sharia smart contract using the `check_lessor_info(address)(uint64,uint64)` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    checkLessorInfo: (params: CallParams<TawfShariaArgs['obj']['check_lessor_info(address)(uint64,uint64)'] | TawfShariaArgs['tuple']['check_lessor_info(address)(uint64,uint64)']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(TawfShariaParamsFactory.checkLessorInfo(params))
    },

    /**
     * Makes a call to the tawf_sharia smart contract using the `total_contract_balance()uint64` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    totalContractBalance: (params: CallParams<TawfShariaArgs['obj']['total_contract_balance()uint64'] | TawfShariaArgs['tuple']['total_contract_balance()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(TawfShariaParamsFactory.totalContractBalance(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Makes a clear_state call to an existing instance of the tawf_sharia smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the tawf_sharia smart contract using the `lease_validation_rights(pay)string` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    leaseValidationRights: (params: CallParams<TawfShariaArgs['obj']['lease_validation_rights(pay)string'] | TawfShariaArgs['tuple']['lease_validation_rights(pay)string']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(TawfShariaParamsFactory.leaseValidationRights(params))
    },

    /**
     * Makes a call to the tawf_sharia smart contract using the `claim_ujrah()string` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    claimUjrah: (params: CallParams<TawfShariaArgs['obj']['claim_ujrah()string'] | TawfShariaArgs['tuple']['claim_ujrah()string']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(TawfShariaParamsFactory.claimUjrah(params))
    },

    /**
     * Makes a call to the tawf_sharia smart contract using the `terminate_lease(uint64)string` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    terminateLease: (params: CallParams<TawfShariaArgs['obj']['terminate_lease(uint64)string'] | TawfShariaArgs['tuple']['terminate_lease(uint64)string']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(TawfShariaParamsFactory.terminateLease(params))
    },

    /**
     * Makes a call to the tawf_sharia smart contract using the `get_my_ijarah_info()(uint64,uint64,uint64)` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    getMyIjarahInfo: (params: CallParams<TawfShariaArgs['obj']['get_my_ijarah_info()(uint64,uint64,uint64)'] | TawfShariaArgs['tuple']['get_my_ijarah_info()(uint64,uint64,uint64)']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(TawfShariaParamsFactory.getMyIjarahInfo(params))
    },

    /**
     * Makes a call to the tawf_sharia smart contract using the `check_lessor_info(address)(uint64,uint64)` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    checkLessorInfo: (params: CallParams<TawfShariaArgs['obj']['check_lessor_info(address)(uint64,uint64)'] | TawfShariaArgs['tuple']['check_lessor_info(address)(uint64,uint64)']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(TawfShariaParamsFactory.checkLessorInfo(params))
    },

    /**
     * Makes a call to the tawf_sharia smart contract using the `total_contract_balance()uint64` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    totalContractBalance: (params: CallParams<TawfShariaArgs['obj']['total_contract_balance()uint64'] | TawfShariaArgs['tuple']['total_contract_balance()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(TawfShariaParamsFactory.totalContractBalance(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Makes a clear_state call to an existing instance of the tawf_sharia smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the tawf_sharia smart contract using the `lease_validation_rights(pay)string` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    leaseValidationRights: async (params: CallParams<TawfShariaArgs['obj']['lease_validation_rights(pay)string'] | TawfShariaArgs['tuple']['lease_validation_rights(pay)string']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(TawfShariaParamsFactory.leaseValidationRights(params))
      return {...result, return: result.return as unknown as (undefined | TawfShariaReturns['lease_validation_rights(pay)string'])}
    },

    /**
     * Makes a call to the tawf_sharia smart contract using the `claim_ujrah()string` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    claimUjrah: async (params: CallParams<TawfShariaArgs['obj']['claim_ujrah()string'] | TawfShariaArgs['tuple']['claim_ujrah()string']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(TawfShariaParamsFactory.claimUjrah(params))
      return {...result, return: result.return as unknown as (undefined | TawfShariaReturns['claim_ujrah()string'])}
    },

    /**
     * Makes a call to the tawf_sharia smart contract using the `terminate_lease(uint64)string` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    terminateLease: async (params: CallParams<TawfShariaArgs['obj']['terminate_lease(uint64)string'] | TawfShariaArgs['tuple']['terminate_lease(uint64)string']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(TawfShariaParamsFactory.terminateLease(params))
      return {...result, return: result.return as unknown as (undefined | TawfShariaReturns['terminate_lease(uint64)string'])}
    },

    /**
     * Makes a call to the tawf_sharia smart contract using the `get_my_ijarah_info()(uint64,uint64,uint64)` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    getMyIjarahInfo: async (params: CallParams<TawfShariaArgs['obj']['get_my_ijarah_info()(uint64,uint64,uint64)'] | TawfShariaArgs['tuple']['get_my_ijarah_info()(uint64,uint64,uint64)']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(TawfShariaParamsFactory.getMyIjarahInfo(params))
      return {...result, return: result.return as unknown as (undefined | TawfShariaReturns['get_my_ijarah_info()(uint64,uint64,uint64)'])}
    },

    /**
     * Makes a call to the tawf_sharia smart contract using the `check_lessor_info(address)(uint64,uint64)` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    checkLessorInfo: async (params: CallParams<TawfShariaArgs['obj']['check_lessor_info(address)(uint64,uint64)'] | TawfShariaArgs['tuple']['check_lessor_info(address)(uint64,uint64)']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(TawfShariaParamsFactory.checkLessorInfo(params))
      return {...result, return: result.return as unknown as (undefined | TawfShariaReturns['check_lessor_info(address)(uint64,uint64)'])}
    },

    /**
     * Makes a call to the tawf_sharia smart contract using the `total_contract_balance()uint64` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    totalContractBalance: async (params: CallParams<TawfShariaArgs['obj']['total_contract_balance()uint64'] | TawfShariaArgs['tuple']['total_contract_balance()uint64']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(TawfShariaParamsFactory.totalContractBalance(params))
      return {...result, return: result.return as unknown as (undefined | TawfShariaReturns['total_contract_balance()uint64'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new TawfShariaClient(this.appClient.clone(params))
  }

  /**
   * Methods to access state for the current tawf_sharia app
   */
  state = {
    /**
     * Methods to access global state for the current tawf_sharia app
     */
    global: {
      /**
       * Get all current keyed values from global state
       */
      getAll: async (): Promise<Partial<Expand<GlobalKeysState>>> => {
        const result = await this.appClient.state.global.getAll()
        return {
          annualUjrahRateBp: result.ANNUAL_UJRAH_RATE_BP,
          secondsPerYear: result.SECONDS_PER_YEAR,
        }
      },
      /**
       * Get the current value of the ANNUAL_UJRAH_RATE_BP key in global state
       */
      annualUjrahRateBp: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("ANNUAL_UJRAH_RATE_BP")) as bigint | undefined },
      /**
       * Get the current value of the SECONDS_PER_YEAR key in global state
       */
      secondsPerYear: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("SECONDS_PER_YEAR")) as bigint | undefined },
    },
  }

  public newGroup(): TawfShariaComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a lease_validation_rights(pay)string method call against the tawf_sharia contract
       */
      leaseValidationRights(params: CallParams<TawfShariaArgs['obj']['lease_validation_rights(pay)string'] | TawfShariaArgs['tuple']['lease_validation_rights(pay)string']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.leaseValidationRights(params)))
        resultMappers.push((v) => client.decodeReturnValue('lease_validation_rights(pay)string', v))
        return this
      },
      /**
       * Add a claim_ujrah()string method call against the tawf_sharia contract
       */
      claimUjrah(params: CallParams<TawfShariaArgs['obj']['claim_ujrah()string'] | TawfShariaArgs['tuple']['claim_ujrah()string']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.claimUjrah(params)))
        resultMappers.push((v) => client.decodeReturnValue('claim_ujrah()string', v))
        return this
      },
      /**
       * Add a terminate_lease(uint64)string method call against the tawf_sharia contract
       */
      terminateLease(params: CallParams<TawfShariaArgs['obj']['terminate_lease(uint64)string'] | TawfShariaArgs['tuple']['terminate_lease(uint64)string']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.terminateLease(params)))
        resultMappers.push((v) => client.decodeReturnValue('terminate_lease(uint64)string', v))
        return this
      },
      /**
       * Add a get_my_ijarah_info()(uint64,uint64,uint64) method call against the tawf_sharia contract
       */
      getMyIjarahInfo(params: CallParams<TawfShariaArgs['obj']['get_my_ijarah_info()(uint64,uint64,uint64)'] | TawfShariaArgs['tuple']['get_my_ijarah_info()(uint64,uint64,uint64)']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.getMyIjarahInfo(params)))
        resultMappers.push((v) => client.decodeReturnValue('get_my_ijarah_info()(uint64,uint64,uint64)', v))
        return this
      },
      /**
       * Add a check_lessor_info(address)(uint64,uint64) method call against the tawf_sharia contract
       */
      checkLessorInfo(params: CallParams<TawfShariaArgs['obj']['check_lessor_info(address)(uint64,uint64)'] | TawfShariaArgs['tuple']['check_lessor_info(address)(uint64,uint64)']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.checkLessorInfo(params)))
        resultMappers.push((v) => client.decodeReturnValue('check_lessor_info(address)(uint64,uint64)', v))
        return this
      },
      /**
       * Add a total_contract_balance()uint64 method call against the tawf_sharia contract
       */
      totalContractBalance(params: CallParams<TawfShariaArgs['obj']['total_contract_balance()uint64'] | TawfShariaArgs['tuple']['total_contract_balance()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.totalContractBalance(params)))
        resultMappers.push((v) => client.decodeReturnValue('total_contract_balance()uint64', v))
        return this
      },
      /**
       * Add a clear state call to the tawf_sharia contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as TawfShariaComposer
  }
}
export type TawfShariaComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the lease_validation_rights(pay)string ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  leaseValidationRights(params?: CallParams<TawfShariaArgs['obj']['lease_validation_rights(pay)string'] | TawfShariaArgs['tuple']['lease_validation_rights(pay)string']>): TawfShariaComposer<[...TReturns, TawfShariaReturns['lease_validation_rights(pay)string'] | undefined]>

  /**
   * Calls the claim_ujrah()string ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  claimUjrah(params?: CallParams<TawfShariaArgs['obj']['claim_ujrah()string'] | TawfShariaArgs['tuple']['claim_ujrah()string']>): TawfShariaComposer<[...TReturns, TawfShariaReturns['claim_ujrah()string'] | undefined]>

  /**
   * Calls the terminate_lease(uint64)string ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  terminateLease(params?: CallParams<TawfShariaArgs['obj']['terminate_lease(uint64)string'] | TawfShariaArgs['tuple']['terminate_lease(uint64)string']>): TawfShariaComposer<[...TReturns, TawfShariaReturns['terminate_lease(uint64)string'] | undefined]>

  /**
   * Calls the get_my_ijarah_info()(uint64,uint64,uint64) ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  getMyIjarahInfo(params?: CallParams<TawfShariaArgs['obj']['get_my_ijarah_info()(uint64,uint64,uint64)'] | TawfShariaArgs['tuple']['get_my_ijarah_info()(uint64,uint64,uint64)']>): TawfShariaComposer<[...TReturns, TawfShariaReturns['get_my_ijarah_info()(uint64,uint64,uint64)'] | undefined]>

  /**
   * Calls the check_lessor_info(address)(uint64,uint64) ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  checkLessorInfo(params?: CallParams<TawfShariaArgs['obj']['check_lessor_info(address)(uint64,uint64)'] | TawfShariaArgs['tuple']['check_lessor_info(address)(uint64,uint64)']>): TawfShariaComposer<[...TReturns, TawfShariaReturns['check_lessor_info(address)(uint64,uint64)'] | undefined]>

  /**
   * Calls the total_contract_balance()uint64 ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  totalContractBalance(params?: CallParams<TawfShariaArgs['obj']['total_contract_balance()uint64'] | TawfShariaArgs['tuple']['total_contract_balance()uint64']>): TawfShariaComposer<[...TReturns, TawfShariaReturns['total_contract_balance()uint64'] | undefined]>

  /**
   * Makes a clear_state call to an existing instance of the tawf_sharia smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): TawfShariaComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): TawfShariaComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<TawfShariaComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<TawfShariaComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<TawfShariaComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<TawfShariaComposerResults<TReturns>>
}
export type TawfShariaComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

