/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"KudosEscrowContract","structs":{},"methods":[{"name":"create_application","args":[{"type":"address","name":"student"},{"type":"uint64","name":"total_amount"},{"type":"uint64","name":"milestone_count"}],"returns":{"type":"void"},"actions":{"create":["NoOp"],"call":[]},"readonly":false,"desc":"Initialize grant application state.\nCan only happen at app creation.","events":[],"recommendations":{}},{"name":"fund_contract","args":[],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Group-validated funding flow:\ngroup[0] = payment to app address group[1] = app call fund_contract()","events":[],"recommendations":{}},{"name":"approve_milestone","args":[],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Sponsor approves current milestone and releases payout from app escrow to student.","events":[],"recommendations":{}},{"name":"emergency_clawback","args":[],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Sponsor can reclaim remaining escrow while grant is still active.","events":[],"recommendations":{}}],"arcs":[22,28],"desc":"\n    Kudos grant escrow contract (ARC-4 / Puya).\n\n    On-chain scope is strictly financial logic and state machine transitions.\n    Off-chain metadata (proof links, notifications, proposals) is intentionally not stored on-chain.\n    ","networks":{},"state":{"schema":{"global":{"ints":5,"bytes":2},"local":{"ints":0,"bytes":0}},"keys":{"global":{"sponsor":{"keyType":"AVMString","valueType":"address","key":"c3BvbnNvcg==","desc":"Grant sponsor account"},"student":{"keyType":"AVMString","valueType":"address","key":"c3R1ZGVudA==","desc":"Grant student account"},"total_amount":{"keyType":"AVMString","valueType":"AVMUint64","key":"dG90YWxfYW1vdW50","desc":"Total escrowed amount"},"milestone_count":{"keyType":"AVMString","valueType":"AVMUint64","key":"bWlsZXN0b25lX2NvdW50","desc":"Total milestones"},"milestone_index":{"keyType":"AVMString","valueType":"AVMUint64","key":"bWlsZXN0b25lX2luZGV4","desc":"Current approved milestone index (0-based)"},"status":{"keyType":"AVMString","valueType":"AVMUint64","key":"c3RhdHVz","desc":"0 pending, 1 active, 2 completed, 3 cancelled"},"funded_amount":{"keyType":"AVMString","valueType":"AVMUint64","key":"ZnVuZGVkX2Ftb3VudA==","desc":"Amount funded into escrow"}},"local":{},"box":{}},"maps":{"global":{},"local":{},"box":{}}},"bareActions":{"create":[],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[322],"errorMessage":"All milestones already approved"},{"pc":[245],"errorMessage":"Already funded"},{"pc":[174],"errorMessage":"Already initialized"},{"pc":[255],"errorMessage":"App call must be second txn"},{"pc":[456,463],"errorMessage":"Contract not initialized"},{"pc":[285],"errorMessage":"Funding amount mismatch"},{"pc":[273],"errorMessage":"Funding receiver must be app address"},{"pc":[266],"errorMessage":"Funding sender must be sponsor"},{"pc":[416],"errorMessage":"Grant already finalized"},{"pc":[312,406],"errorMessage":"Grant not active"},{"pc":[250],"errorMessage":"Invalid group size"},{"pc":[238],"errorMessage":"Invalid status"},{"pc":[421],"errorMessage":"No escrow remaining"},{"pc":[305],"errorMessage":"Only sponsor can approve"},{"pc":[399],"errorMessage":"Only sponsor can clawback"},{"pc":[232],"errorMessage":"Only sponsor can fund"},{"pc":[344],"errorMessage":"Payout must be > 0"},{"pc":[243,480],"errorMessage":"check self.funded_amount exists"},{"pc":[320,329,375,414,441,473],"errorMessage":"check self.milestone_count exists"},{"pc":[316,333,365,410,487],"errorMessage":"check self.milestone_index exists"},{"pc":[227,303,397,426],"errorMessage":"check self.sponsor exists"},{"pc":[236,309,403],"errorMessage":"check self.status exists"},{"pc":[350],"errorMessage":"check self.student exists"},{"pc":[281,469],"errorMessage":"check self.total_amount exists"},{"pc":[151],"errorMessage":"invalid number of bytes for arc4.static_array<arc4.uint8, 32>"},{"pc":[159,167],"errorMessage":"invalid number of bytes for arc4.uint64"},{"pc":[189],"errorMessage":"invalid student"},{"pc":[183],"errorMessage":"milestone_count must be > 0"},{"pc":[181],"errorMessage":"total_amount must be > 0"},{"pc":[261],"errorMessage":"transaction type is pay"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"events":[]} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


/**
 * The argument types for the KudosEscrowContract contract
 */
export type KudosEscrowContractArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'create_application(address,uint64,uint64)void': {
      student: string
      totalAmount: bigint | number
      milestoneCount: bigint | number
    }
    'fund_contract()void': Record<string, never>
    'approve_milestone()void': Record<string, never>
    'emergency_clawback()void': Record<string, never>
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'create_application(address,uint64,uint64)void': [student: string, totalAmount: bigint | number, milestoneCount: bigint | number]
    'fund_contract()void': []
    'approve_milestone()void': []
    'emergency_clawback()void': []
  }
}

/**
 * The return type for each method
 */
export type KudosEscrowContractReturns = {
  'create_application(address,uint64,uint64)void': void
  'fund_contract()void': void
  'approve_milestone()void': void
  'emergency_clawback()void': void
}

/**
 * Defines the types of available calls and state of the KudosEscrowContract smart contract.
 */
export type KudosEscrowContractTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'create_application(address,uint64,uint64)void' | 'create_application', {
      argsObj: KudosEscrowContractArgs['obj']['create_application(address,uint64,uint64)void']
      argsTuple: KudosEscrowContractArgs['tuple']['create_application(address,uint64,uint64)void']
      returns: KudosEscrowContractReturns['create_application(address,uint64,uint64)void']
    }>
    & Record<'fund_contract()void' | 'fund_contract', {
      argsObj: KudosEscrowContractArgs['obj']['fund_contract()void']
      argsTuple: KudosEscrowContractArgs['tuple']['fund_contract()void']
      returns: KudosEscrowContractReturns['fund_contract()void']
    }>
    & Record<'approve_milestone()void' | 'approve_milestone', {
      argsObj: KudosEscrowContractArgs['obj']['approve_milestone()void']
      argsTuple: KudosEscrowContractArgs['tuple']['approve_milestone()void']
      returns: KudosEscrowContractReturns['approve_milestone()void']
    }>
    & Record<'emergency_clawback()void' | 'emergency_clawback', {
      argsObj: KudosEscrowContractArgs['obj']['emergency_clawback()void']
      argsTuple: KudosEscrowContractArgs['tuple']['emergency_clawback()void']
      returns: KudosEscrowContractReturns['emergency_clawback()void']
    }>
  /**
   * Defines the shape of the state of the application.
   */
  state: {
    global: {
      keys: {
        /**
         * Grant sponsor account
         */
        sponsor: string
        /**
         * Grant student account
         */
        student: string
        /**
         * Total escrowed amount
         */
        totalAmount: bigint
        /**
         * Total milestones
         */
        milestoneCount: bigint
        /**
         * Current approved milestone index (0-based)
         */
        milestoneIndex: bigint
        /**
         * 0 pending, 1 active, 2 completed, 3 cancelled
         */
        status: bigint
        /**
         * Amount funded into escrow
         */
        fundedAmount: bigint
      }
      maps: {}
    }
  }
}

/**
 * Defines the possible abi call signatures.
 */
export type KudosEscrowContractSignatures = keyof KudosEscrowContractTypes['methods']
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the KudosEscrowContract smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends KudosEscrowContractSignatures> = KudosEscrowContractTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the KudosEscrowContract smart contract to the method's return type
 */
export type MethodReturn<TSignature extends KudosEscrowContractSignatures> = KudosEscrowContractTypes['methods'][TSignature]['returns']

/**
 * Defines the shape of the keyed global state of the application.
 */
export type GlobalKeysState = KudosEscrowContractTypes['state']['global']['keys']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the KudosEscrowContract smart contract
 */
export abstract class KudosEscrowContractParamsFactory {
  /**
   * Constructs a no op call for the fund_contract()void ABI method
   *
  * Group-validated funding flow:
  group[0] = payment to app address group[1] = app call fund_contract()

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static fundContract(params: CallParams<KudosEscrowContractArgs['obj']['fund_contract()void'] | KudosEscrowContractArgs['tuple']['fund_contract()void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'fund_contract()void' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the approve_milestone()void ABI method
   *
   * Sponsor approves current milestone and releases payout from app escrow to student.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static approveMilestone(params: CallParams<KudosEscrowContractArgs['obj']['approve_milestone()void'] | KudosEscrowContractArgs['tuple']['approve_milestone()void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'approve_milestone()void' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the emergency_clawback()void ABI method
   *
   * Sponsor can reclaim remaining escrow while grant is still active.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static emergencyClawback(params: CallParams<KudosEscrowContractArgs['obj']['emergency_clawback()void'] | KudosEscrowContractArgs['tuple']['emergency_clawback()void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'emergency_clawback()void' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
}

/**
 * A client to make calls to the KudosEscrowContract smart contract
 */
export class KudosEscrowContractClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `KudosEscrowContractClient`
   *
   * @param appClient An `AppClient` instance which has been created with the KudosEscrowContract app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `KudosEscrowContractClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Returns a new `KudosEscrowContractClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<KudosEscrowContractClient> {
    return new KudosEscrowContractClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `KudosEscrowContractClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<KudosEscrowContractClient> {
    return new KudosEscrowContractClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Makes a clear_state call to an existing instance of the KudosEscrowContract smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the KudosEscrowContract smart contract using the `fund_contract()void` ABI method.
     *
    * Group-validated funding flow:
    group[0] = payment to app address group[1] = app call fund_contract()

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    fundContract: (params: CallParams<KudosEscrowContractArgs['obj']['fund_contract()void'] | KudosEscrowContractArgs['tuple']['fund_contract()void']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(KudosEscrowContractParamsFactory.fundContract(params))
    },

    /**
     * Makes a call to the KudosEscrowContract smart contract using the `approve_milestone()void` ABI method.
     *
     * Sponsor approves current milestone and releases payout from app escrow to student.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    approveMilestone: (params: CallParams<KudosEscrowContractArgs['obj']['approve_milestone()void'] | KudosEscrowContractArgs['tuple']['approve_milestone()void']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(KudosEscrowContractParamsFactory.approveMilestone(params))
    },

    /**
     * Makes a call to the KudosEscrowContract smart contract using the `emergency_clawback()void` ABI method.
     *
     * Sponsor can reclaim remaining escrow while grant is still active.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    emergencyClawback: (params: CallParams<KudosEscrowContractArgs['obj']['emergency_clawback()void'] | KudosEscrowContractArgs['tuple']['emergency_clawback()void']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(KudosEscrowContractParamsFactory.emergencyClawback(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Makes a clear_state call to an existing instance of the KudosEscrowContract smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the KudosEscrowContract smart contract using the `fund_contract()void` ABI method.
     *
    * Group-validated funding flow:
    group[0] = payment to app address group[1] = app call fund_contract()

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    fundContract: (params: CallParams<KudosEscrowContractArgs['obj']['fund_contract()void'] | KudosEscrowContractArgs['tuple']['fund_contract()void']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(KudosEscrowContractParamsFactory.fundContract(params))
    },

    /**
     * Makes a call to the KudosEscrowContract smart contract using the `approve_milestone()void` ABI method.
     *
     * Sponsor approves current milestone and releases payout from app escrow to student.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    approveMilestone: (params: CallParams<KudosEscrowContractArgs['obj']['approve_milestone()void'] | KudosEscrowContractArgs['tuple']['approve_milestone()void']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(KudosEscrowContractParamsFactory.approveMilestone(params))
    },

    /**
     * Makes a call to the KudosEscrowContract smart contract using the `emergency_clawback()void` ABI method.
     *
     * Sponsor can reclaim remaining escrow while grant is still active.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    emergencyClawback: (params: CallParams<KudosEscrowContractArgs['obj']['emergency_clawback()void'] | KudosEscrowContractArgs['tuple']['emergency_clawback()void']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(KudosEscrowContractParamsFactory.emergencyClawback(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Makes a clear_state call to an existing instance of the KudosEscrowContract smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the KudosEscrowContract smart contract using the `fund_contract()void` ABI method.
     *
    * Group-validated funding flow:
    group[0] = payment to app address group[1] = app call fund_contract()

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    fundContract: async (params: CallParams<KudosEscrowContractArgs['obj']['fund_contract()void'] | KudosEscrowContractArgs['tuple']['fund_contract()void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(KudosEscrowContractParamsFactory.fundContract(params))
      return {...result, return: result.return as unknown as (undefined | KudosEscrowContractReturns['fund_contract()void'])}
    },

    /**
     * Makes a call to the KudosEscrowContract smart contract using the `approve_milestone()void` ABI method.
     *
     * Sponsor approves current milestone and releases payout from app escrow to student.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    approveMilestone: async (params: CallParams<KudosEscrowContractArgs['obj']['approve_milestone()void'] | KudosEscrowContractArgs['tuple']['approve_milestone()void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(KudosEscrowContractParamsFactory.approveMilestone(params))
      return {...result, return: result.return as unknown as (undefined | KudosEscrowContractReturns['approve_milestone()void'])}
    },

    /**
     * Makes a call to the KudosEscrowContract smart contract using the `emergency_clawback()void` ABI method.
     *
     * Sponsor can reclaim remaining escrow while grant is still active.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    emergencyClawback: async (params: CallParams<KudosEscrowContractArgs['obj']['emergency_clawback()void'] | KudosEscrowContractArgs['tuple']['emergency_clawback()void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(KudosEscrowContractParamsFactory.emergencyClawback(params))
      return {...result, return: result.return as unknown as (undefined | KudosEscrowContractReturns['emergency_clawback()void'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new KudosEscrowContractClient(this.appClient.clone(params))
  }

  /**
   * Methods to access state for the current KudosEscrowContract app
   */
  state = {
    /**
     * Methods to access global state for the current KudosEscrowContract app
     */
    global: {
      /**
       * Get all current keyed values from global state
       */
      getAll: async (): Promise<Partial<Expand<GlobalKeysState>>> => {
        const result = await this.appClient.state.global.getAll()
        return {
          sponsor: result.sponsor,
          student: result.student,
          totalAmount: result.total_amount,
          milestoneCount: result.milestone_count,
          milestoneIndex: result.milestone_index,
          status: result.status,
          fundedAmount: result.funded_amount,
        }
      },
      /**
       * Get the current value of the sponsor key in global state
       */
      sponsor: async (): Promise<string | undefined> => { return (await this.appClient.state.global.getValue("sponsor")) as string | undefined },
      /**
       * Get the current value of the student key in global state
       */
      student: async (): Promise<string | undefined> => { return (await this.appClient.state.global.getValue("student")) as string | undefined },
      /**
       * Get the current value of the total_amount key in global state
       */
      totalAmount: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("total_amount")) as bigint | undefined },
      /**
       * Get the current value of the milestone_count key in global state
       */
      milestoneCount: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("milestone_count")) as bigint | undefined },
      /**
       * Get the current value of the milestone_index key in global state
       */
      milestoneIndex: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("milestone_index")) as bigint | undefined },
      /**
       * Get the current value of the status key in global state
       */
      status: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("status")) as bigint | undefined },
      /**
       * Get the current value of the funded_amount key in global state
       */
      fundedAmount: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("funded_amount")) as bigint | undefined },
    },
  }

  public newGroup(): KudosEscrowContractComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a fund_contract()void method call against the KudosEscrowContract contract
       */
      fundContract(params: CallParams<KudosEscrowContractArgs['obj']['fund_contract()void'] | KudosEscrowContractArgs['tuple']['fund_contract()void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.fundContract(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a approve_milestone()void method call against the KudosEscrowContract contract
       */
      approveMilestone(params: CallParams<KudosEscrowContractArgs['obj']['approve_milestone()void'] | KudosEscrowContractArgs['tuple']['approve_milestone()void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.approveMilestone(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a emergency_clawback()void method call against the KudosEscrowContract contract
       */
      emergencyClawback(params: CallParams<KudosEscrowContractArgs['obj']['emergency_clawback()void'] | KudosEscrowContractArgs['tuple']['emergency_clawback()void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.emergencyClawback(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a clear state call to the KudosEscrowContract contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as KudosEscrowContractComposer
  }
}
export type KudosEscrowContractComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the fund_contract()void ABI method.
   *
  * Group-validated funding flow:
  group[0] = payment to app address group[1] = app call fund_contract()

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  fundContract(params?: CallParams<KudosEscrowContractArgs['obj']['fund_contract()void'] | KudosEscrowContractArgs['tuple']['fund_contract()void']>): KudosEscrowContractComposer<[...TReturns, KudosEscrowContractReturns['fund_contract()void'] | undefined]>

  /**
   * Calls the approve_milestone()void ABI method.
   *
   * Sponsor approves current milestone and releases payout from app escrow to student.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  approveMilestone(params?: CallParams<KudosEscrowContractArgs['obj']['approve_milestone()void'] | KudosEscrowContractArgs['tuple']['approve_milestone()void']>): KudosEscrowContractComposer<[...TReturns, KudosEscrowContractReturns['approve_milestone()void'] | undefined]>

  /**
   * Calls the emergency_clawback()void ABI method.
   *
   * Sponsor can reclaim remaining escrow while grant is still active.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  emergencyClawback(params?: CallParams<KudosEscrowContractArgs['obj']['emergency_clawback()void'] | KudosEscrowContractArgs['tuple']['emergency_clawback()void']>): KudosEscrowContractComposer<[...TReturns, KudosEscrowContractReturns['emergency_clawback()void'] | undefined]>

  /**
   * Makes a clear_state call to an existing instance of the KudosEscrowContract smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): KudosEscrowContractComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): KudosEscrowContractComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<KudosEscrowContractComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<KudosEscrowContractComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<KudosEscrowContractComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<KudosEscrowContractComposerResults<TReturns>>
}
export type KudosEscrowContractComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

