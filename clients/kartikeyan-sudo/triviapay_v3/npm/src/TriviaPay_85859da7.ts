/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'

import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"TriviaPay","structs":{},"methods":[{"name":"createApplication","args":[],"returns":{"type":"void"},"actions":{"create":["NoOp"],"call":[]},"readonly":false,"desc":"Application creation","events":[],"recommendations":{}},{"name":"deposit","args":[{"type":"pay","name":"payment"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Deposit ALGO via grouped PaymentTxn to the app address.","events":[],"recommendations":{}},{"name":"pay","args":[{"type":"address","name":"recipient"},{"type":"uint64","name":"amount"},{"type":"string","name":"note"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Owner pays a recipient from escrow.","events":[],"recommendations":{}},{"name":"withdraw","args":[{"type":"uint64","name":"amount"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Owner withdraws funds back to themselves.","events":[],"recommendations":{}},{"name":"balance","args":[],"returns":{"type":"uint64"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"get_owner","args":[],"returns":{"type":"address"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}}],"arcs":[22,28],"desc":"TriviaPay escrow / payment-pool contract (Algorand TypeScript / Puya).\r\n\r\nMethods:\r\n- createApplication(): bootstrap owner + counters\r\n- deposit(payment): group with a PaymentTxn to app address\r\n- pay(recipient, amount, note): owner-only payout from escrow\r\n- withdraw(amount): owner-only withdrawal back to owner\r\n- balance(): view total escrowed microAlgos\r\n- get_owner(): view owner account","networks":{},"state":{"schema":{"global":{"ints":1,"bytes":1},"local":{"ints":0,"bytes":0}},"keys":{"global":{"owner":{"keyType":"AVMString","valueType":"address","key":"b3duZXI="},"totalDeposited":{"keyType":"AVMString","valueType":"AVMUint64","key":"dG90YWw="}},"local":{},"box":{}},"maps":{"global":{},"local":{},"box":{}}},"bareActions":{"create":[],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[176,234],"errorMessage":"Amount must be > 0"},{"pc":[120],"errorMessage":"Deposit amount must be > 0"},{"pc":[185,243],"errorMessage":"Insufficient escrow balance"},{"pc":[28],"errorMessage":"OnCompletion must be NoOp"},{"pc":[173],"errorMessage":"Only owner can call pay()"},{"pc":[231],"errorMessage":"Only owner can withdraw"},{"pc":[116],"errorMessage":"Payment must go to this contract address"},{"pc":[124,171,180,226,238,269,280],"errorMessage":"check GlobalState exists"},{"pc":[154],"errorMessage":"invalid array length header"},{"pc":[162],"errorMessage":"invalid number of bytes for arc4.dynamic_array<arc4.uint8>"},{"pc":[139],"errorMessage":"invalid number of bytes for arc4.static_array<arc4.uint8, 32>"},{"pc":[147,219],"errorMessage":"invalid number of bytes for arc4.uint64"},{"pc":[109],"errorMessage":"transaction type is pay"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"events":[]} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


/**
 * The argument types for the TriviaPay contract
 */
export type TriviaPayArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'createApplication()void': Record<string, never>
    'deposit(pay)void': {
      payment: AppMethodCallTransactionArgument
    }
    'pay(address,uint64,string)void': {
      recipient: string
      amount: bigint | number
      note: string
    }
    'withdraw(uint64)void': {
      amount: bigint | number
    }
    'balance()uint64': Record<string, never>
    'get_owner()address': Record<string, never>
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'createApplication()void': []
    'deposit(pay)void': [payment: AppMethodCallTransactionArgument]
    'pay(address,uint64,string)void': [recipient: string, amount: bigint | number, note: string]
    'withdraw(uint64)void': [amount: bigint | number]
    'balance()uint64': []
    'get_owner()address': []
  }
}

/**
 * The return type for each method
 */
export type TriviaPayReturns = {
  'createApplication()void': void
  'deposit(pay)void': void
  'pay(address,uint64,string)void': void
  'withdraw(uint64)void': void
  'balance()uint64': bigint
  'get_owner()address': string
}

/**
 * Defines the types of available calls and state of the TriviaPay smart contract.
 */
export type TriviaPayTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'createApplication()void' | 'createApplication', {
      argsObj: TriviaPayArgs['obj']['createApplication()void']
      argsTuple: TriviaPayArgs['tuple']['createApplication()void']
      returns: TriviaPayReturns['createApplication()void']
    }>
    & Record<'deposit(pay)void' | 'deposit', {
      argsObj: TriviaPayArgs['obj']['deposit(pay)void']
      argsTuple: TriviaPayArgs['tuple']['deposit(pay)void']
      returns: TriviaPayReturns['deposit(pay)void']
    }>
    & Record<'pay(address,uint64,string)void' | 'pay', {
      argsObj: TriviaPayArgs['obj']['pay(address,uint64,string)void']
      argsTuple: TriviaPayArgs['tuple']['pay(address,uint64,string)void']
      returns: TriviaPayReturns['pay(address,uint64,string)void']
    }>
    & Record<'withdraw(uint64)void' | 'withdraw', {
      argsObj: TriviaPayArgs['obj']['withdraw(uint64)void']
      argsTuple: TriviaPayArgs['tuple']['withdraw(uint64)void']
      returns: TriviaPayReturns['withdraw(uint64)void']
    }>
    & Record<'balance()uint64' | 'balance', {
      argsObj: TriviaPayArgs['obj']['balance()uint64']
      argsTuple: TriviaPayArgs['tuple']['balance()uint64']
      returns: TriviaPayReturns['balance()uint64']
    }>
    & Record<'get_owner()address' | 'get_owner', {
      argsObj: TriviaPayArgs['obj']['get_owner()address']
      argsTuple: TriviaPayArgs['tuple']['get_owner()address']
      returns: TriviaPayReturns['get_owner()address']
    }>
  /**
   * Defines the shape of the state of the application.
   */
  state: {
    global: {
      keys: {
        owner: string
        totalDeposited: bigint
      }
      maps: {}
    }
  }
}

/**
 * Defines the possible abi call signatures.
 */
export type TriviaPaySignatures = keyof TriviaPayTypes['methods']
/**
 * Defines the possible abi call signatures for methods that return a non-void value.
 */
export type TriviaPayNonVoidMethodSignatures = keyof TriviaPayTypes['methods'] extends infer T ? T extends keyof TriviaPayTypes['methods'] ? MethodReturn<T> extends void ? never : T  : never : never
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the TriviaPay smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends TriviaPaySignatures> = TriviaPayTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the TriviaPay smart contract to the method's return type
 */
export type MethodReturn<TSignature extends TriviaPaySignatures> = TriviaPayTypes['methods'][TSignature]['returns']

/**
 * Defines the shape of the keyed global state of the application.
 */
export type GlobalKeysState = TriviaPayTypes['state']['global']['keys']



/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the TriviaPay smart contract
 */
export abstract class TriviaPayParamsFactory {
  /**
   * Constructs a no op call for the deposit(pay)void ABI method
   *
   * Deposit ALGO via grouped PaymentTxn to the app address.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static deposit(params: CallParams<TriviaPayArgs['obj']['deposit(pay)void'] | TriviaPayArgs['tuple']['deposit(pay)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'deposit(pay)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.payment],
    }
  }
  /**
   * Constructs a no op call for the pay(address,uint64,string)void ABI method
   *
   * Owner pays a recipient from escrow.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static pay(params: CallParams<TriviaPayArgs['obj']['pay(address,uint64,string)void'] | TriviaPayArgs['tuple']['pay(address,uint64,string)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'pay(address,uint64,string)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.recipient, params.args.amount, params.args.note],
    }
  }
  /**
   * Constructs a no op call for the withdraw(uint64)void ABI method
   *
   * Owner withdraws funds back to themselves.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static withdraw(params: CallParams<TriviaPayArgs['obj']['withdraw(uint64)void'] | TriviaPayArgs['tuple']['withdraw(uint64)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'withdraw(uint64)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.amount],
    }
  }
  /**
   * Constructs a no op call for the balance()uint64 ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static balance(params: CallParams<TriviaPayArgs['obj']['balance()uint64'] | TriviaPayArgs['tuple']['balance()uint64']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'balance()uint64' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
  /**
   * Constructs a no op call for the get_owner()address ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static getOwner(params: CallParams<TriviaPayArgs['obj']['get_owner()address'] | TriviaPayArgs['tuple']['get_owner()address']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'get_owner()address' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
}

/**
 * A client to make calls to the TriviaPay smart contract
 */
export class TriviaPayClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `TriviaPayClient`
   *
   * @param appClient An `AppClient` instance which has been created with the TriviaPay app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `TriviaPayClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Checks for decode errors on the given return value and maps the return value to the return type for the given method
   * @returns The typed return value or undefined if there was no value
   */
  decodeReturnValue<TSignature extends TriviaPayNonVoidMethodSignatures>(method: TSignature, returnValue: ABIReturn | undefined) {
    return returnValue !== undefined ? getArc56ReturnValue<MethodReturn<TSignature>>(returnValue, this.appClient.getABIMethod(method), APP_SPEC.structs) : undefined
  }

  /**
   * Returns a new `TriviaPayClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<TriviaPayClient> {
    return new TriviaPayClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `TriviaPayClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<TriviaPayClient> {
    return new TriviaPayClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Makes a clear_state call to an existing instance of the TriviaPay smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the TriviaPay smart contract using the `deposit(pay)void` ABI method.
     *
     * Deposit ALGO via grouped PaymentTxn to the app address.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    deposit: (params: CallParams<TriviaPayArgs['obj']['deposit(pay)void'] | TriviaPayArgs['tuple']['deposit(pay)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(TriviaPayParamsFactory.deposit(params))
    },

    /**
     * Makes a call to the TriviaPay smart contract using the `pay(address,uint64,string)void` ABI method.
     *
     * Owner pays a recipient from escrow.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    pay: (params: CallParams<TriviaPayArgs['obj']['pay(address,uint64,string)void'] | TriviaPayArgs['tuple']['pay(address,uint64,string)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(TriviaPayParamsFactory.pay(params))
    },

    /**
     * Makes a call to the TriviaPay smart contract using the `withdraw(uint64)void` ABI method.
     *
     * Owner withdraws funds back to themselves.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    withdraw: (params: CallParams<TriviaPayArgs['obj']['withdraw(uint64)void'] | TriviaPayArgs['tuple']['withdraw(uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(TriviaPayParamsFactory.withdraw(params))
    },

    /**
     * Makes a call to the TriviaPay smart contract using the `balance()uint64` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    balance: (params: CallParams<TriviaPayArgs['obj']['balance()uint64'] | TriviaPayArgs['tuple']['balance()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(TriviaPayParamsFactory.balance(params))
    },

    /**
     * Makes a call to the TriviaPay smart contract using the `get_owner()address` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    getOwner: (params: CallParams<TriviaPayArgs['obj']['get_owner()address'] | TriviaPayArgs['tuple']['get_owner()address']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(TriviaPayParamsFactory.getOwner(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Makes a clear_state call to an existing instance of the TriviaPay smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the TriviaPay smart contract using the `deposit(pay)void` ABI method.
     *
     * Deposit ALGO via grouped PaymentTxn to the app address.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    deposit: (params: CallParams<TriviaPayArgs['obj']['deposit(pay)void'] | TriviaPayArgs['tuple']['deposit(pay)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(TriviaPayParamsFactory.deposit(params))
    },

    /**
     * Makes a call to the TriviaPay smart contract using the `pay(address,uint64,string)void` ABI method.
     *
     * Owner pays a recipient from escrow.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    pay: (params: CallParams<TriviaPayArgs['obj']['pay(address,uint64,string)void'] | TriviaPayArgs['tuple']['pay(address,uint64,string)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(TriviaPayParamsFactory.pay(params))
    },

    /**
     * Makes a call to the TriviaPay smart contract using the `withdraw(uint64)void` ABI method.
     *
     * Owner withdraws funds back to themselves.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    withdraw: (params: CallParams<TriviaPayArgs['obj']['withdraw(uint64)void'] | TriviaPayArgs['tuple']['withdraw(uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(TriviaPayParamsFactory.withdraw(params))
    },

    /**
     * Makes a call to the TriviaPay smart contract using the `balance()uint64` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    balance: (params: CallParams<TriviaPayArgs['obj']['balance()uint64'] | TriviaPayArgs['tuple']['balance()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(TriviaPayParamsFactory.balance(params))
    },

    /**
     * Makes a call to the TriviaPay smart contract using the `get_owner()address` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    getOwner: (params: CallParams<TriviaPayArgs['obj']['get_owner()address'] | TriviaPayArgs['tuple']['get_owner()address']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(TriviaPayParamsFactory.getOwner(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Makes a clear_state call to an existing instance of the TriviaPay smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the TriviaPay smart contract using the `deposit(pay)void` ABI method.
     *
     * Deposit ALGO via grouped PaymentTxn to the app address.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    deposit: async (params: CallParams<TriviaPayArgs['obj']['deposit(pay)void'] | TriviaPayArgs['tuple']['deposit(pay)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(TriviaPayParamsFactory.deposit(params))
      return {...result, return: result.return as unknown as (undefined | TriviaPayReturns['deposit(pay)void'])}
    },

    /**
     * Makes a call to the TriviaPay smart contract using the `pay(address,uint64,string)void` ABI method.
     *
     * Owner pays a recipient from escrow.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    pay: async (params: CallParams<TriviaPayArgs['obj']['pay(address,uint64,string)void'] | TriviaPayArgs['tuple']['pay(address,uint64,string)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(TriviaPayParamsFactory.pay(params))
      return {...result, return: result.return as unknown as (undefined | TriviaPayReturns['pay(address,uint64,string)void'])}
    },

    /**
     * Makes a call to the TriviaPay smart contract using the `withdraw(uint64)void` ABI method.
     *
     * Owner withdraws funds back to themselves.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    withdraw: async (params: CallParams<TriviaPayArgs['obj']['withdraw(uint64)void'] | TriviaPayArgs['tuple']['withdraw(uint64)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(TriviaPayParamsFactory.withdraw(params))
      return {...result, return: result.return as unknown as (undefined | TriviaPayReturns['withdraw(uint64)void'])}
    },

    /**
     * Makes a call to the TriviaPay smart contract using the `balance()uint64` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    balance: async (params: CallParams<TriviaPayArgs['obj']['balance()uint64'] | TriviaPayArgs['tuple']['balance()uint64']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(TriviaPayParamsFactory.balance(params))
      return {...result, return: result.return as unknown as (undefined | TriviaPayReturns['balance()uint64'])}
    },

    /**
     * Makes a call to the TriviaPay smart contract using the `get_owner()address` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    getOwner: async (params: CallParams<TriviaPayArgs['obj']['get_owner()address'] | TriviaPayArgs['tuple']['get_owner()address']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(TriviaPayParamsFactory.getOwner(params))
      return {...result, return: result.return as unknown as (undefined | TriviaPayReturns['get_owner()address'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new TriviaPayClient(this.appClient.clone(params))
  }

  /**
   * Makes a readonly (simulated) call to the TriviaPay smart contract using the `balance()uint64` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async balance(params: CallParams<TriviaPayArgs['obj']['balance()uint64'] | TriviaPayArgs['tuple']['balance()uint64']> = {args: []}) {
    const result = await this.appClient.send.call(TriviaPayParamsFactory.balance(params))
    return result.return as unknown as TriviaPayReturns['balance()uint64']
  }

  /**
   * Makes a readonly (simulated) call to the TriviaPay smart contract using the `get_owner()address` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async getOwner(params: CallParams<TriviaPayArgs['obj']['get_owner()address'] | TriviaPayArgs['tuple']['get_owner()address']> = {args: []}) {
    const result = await this.appClient.send.call(TriviaPayParamsFactory.getOwner(params))
    return result.return as unknown as TriviaPayReturns['get_owner()address']
  }

  /**
   * Methods to access state for the current TriviaPay app
   */
  state = {
    /**
     * Methods to access global state for the current TriviaPay app
     */
    global: {
      /**
       * Get all current keyed values from global state
       */
      getAll: async (): Promise<Partial<Expand<GlobalKeysState>>> => {
        const result = await this.appClient.state.global.getAll()
        return {
          owner: result.owner,
          totalDeposited: result.totalDeposited,
        }
      },
      /**
       * Get the current value of the owner key in global state
       */
      owner: async (): Promise<string | undefined> => { return (await this.appClient.state.global.getValue("owner")) as string | undefined },
      /**
       * Get the current value of the totalDeposited key in global state
       */
      totalDeposited: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("totalDeposited")) as bigint | undefined },
    },
  }

  public newGroup(): TriviaPayComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a deposit(pay)void method call against the TriviaPay contract
       */
      deposit(params: CallParams<TriviaPayArgs['obj']['deposit(pay)void'] | TriviaPayArgs['tuple']['deposit(pay)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.deposit(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a pay(address,uint64,string)void method call against the TriviaPay contract
       */
      pay(params: CallParams<TriviaPayArgs['obj']['pay(address,uint64,string)void'] | TriviaPayArgs['tuple']['pay(address,uint64,string)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.pay(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a withdraw(uint64)void method call against the TriviaPay contract
       */
      withdraw(params: CallParams<TriviaPayArgs['obj']['withdraw(uint64)void'] | TriviaPayArgs['tuple']['withdraw(uint64)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.withdraw(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a balance()uint64 method call against the TriviaPay contract
       */
      balance(params: CallParams<TriviaPayArgs['obj']['balance()uint64'] | TriviaPayArgs['tuple']['balance()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.balance(params)))
        resultMappers.push((v) => client.decodeReturnValue('balance()uint64', v))
        return this
      },
      /**
       * Add a get_owner()address method call against the TriviaPay contract
       */
      getOwner(params: CallParams<TriviaPayArgs['obj']['get_owner()address'] | TriviaPayArgs['tuple']['get_owner()address']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.getOwner(params)))
        resultMappers.push((v) => client.decodeReturnValue('get_owner()address', v))
        return this
      },
      /**
       * Add a clear state call to the TriviaPay contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as TriviaPayComposer
  }
}
export type TriviaPayComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the deposit(pay)void ABI method.
   *
   * Deposit ALGO via grouped PaymentTxn to the app address.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  deposit(params?: CallParams<TriviaPayArgs['obj']['deposit(pay)void'] | TriviaPayArgs['tuple']['deposit(pay)void']>): TriviaPayComposer<[...TReturns, TriviaPayReturns['deposit(pay)void'] | undefined]>

  /**
   * Calls the pay(address,uint64,string)void ABI method.
   *
   * Owner pays a recipient from escrow.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  pay(params?: CallParams<TriviaPayArgs['obj']['pay(address,uint64,string)void'] | TriviaPayArgs['tuple']['pay(address,uint64,string)void']>): TriviaPayComposer<[...TReturns, TriviaPayReturns['pay(address,uint64,string)void'] | undefined]>

  /**
   * Calls the withdraw(uint64)void ABI method.
   *
   * Owner withdraws funds back to themselves.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  withdraw(params?: CallParams<TriviaPayArgs['obj']['withdraw(uint64)void'] | TriviaPayArgs['tuple']['withdraw(uint64)void']>): TriviaPayComposer<[...TReturns, TriviaPayReturns['withdraw(uint64)void'] | undefined]>

  /**
   * Calls the balance()uint64 ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  balance(params?: CallParams<TriviaPayArgs['obj']['balance()uint64'] | TriviaPayArgs['tuple']['balance()uint64']>): TriviaPayComposer<[...TReturns, TriviaPayReturns['balance()uint64'] | undefined]>

  /**
   * Calls the get_owner()address ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  getOwner(params?: CallParams<TriviaPayArgs['obj']['get_owner()address'] | TriviaPayArgs['tuple']['get_owner()address']>): TriviaPayComposer<[...TReturns, TriviaPayReturns['get_owner()address'] | undefined]>

  /**
   * Makes a clear_state call to an existing instance of the TriviaPay smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): TriviaPayComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): TriviaPayComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<TriviaPayComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<TriviaPayComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<TriviaPayComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<TriviaPayComposerResults<TReturns>>
}
export type TriviaPayComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

