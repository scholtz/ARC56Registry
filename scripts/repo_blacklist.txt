# Repos to always ignore when searching for ARC-56 files (update_arc56_links.py).
# One "owner/repo" per line, case-insensitive. Blank lines and lines starting
# with "#" are ignored. Add a repo here if you decide its ARC-56 files (real
# or template/example) should never be picked up by the registry.
#
# This repo is excluded because any *.arc56.json files committed here
# (fixtures, docs examples, etc.) are about this registry itself, not a
# contract meant to be registered in it.
scholtz/ARC56Registry
